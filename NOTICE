# NOTICE

> **Do not move any of this into `LICENSE`.** That file must stay verbatim MIT
> text. GitHub's license detection (`licensee`) matches the whole file, so even
> an appended attribution paragraph downgrades the repository from `MIT` to
> `NOASSERTION` / "Other" — losing the license badge, license-based search, and
> a machine-readable SPDX identifier for downstream consumers. This was
> observed, not assumed: an earlier revision of `LICENSE` carried a trailing
> attribution block and the GitHub API reported `NOASSERTION`. Attribution
> lives here; `README.md` links here.

## Attribution

CodeGraph Go began as a ground-up Go rewrite of CodeGraph (MIT) — <https://github.com/colbymchenry/codegraph>.

The upstream copyright notice, reproduced verbatim from that project's `LICENSE`
(retrieved 2026-08-01 via `gh api repos/colbymchenry/codegraph/license`):

```
MIT License

Copyright (c) 2026 Colby Mchenry
```

> Do not "correct" the capitalization of that name. It is transcribed exactly as
> the copyright holder wrote it in their own `LICENSE`, which is the only form
> that discharges the attribution. The plausible-looking correction is wrong.

## Third-party dependencies

Build-time and runtime dependencies are enumerated in `go.mod`, and a complete
SBOM (SPDX) is published as a release asset for every platform binary. To
inspect the exact dependency set compiled into a given release:

    gh release download <tag> -p 'codegraph_<tag>_<goos>_<goarch>.spdx.json'

Notable components:

- **tree-sitter** and its per-language grammars (MIT) — parsing. This is the
  project's one CGo dependency; the rationale is recorded in
  `PARSER-DECISION.md`.
- **CockroachDB Pebble** (BSD-3-Clause) — the embedded graph store.
- **modelcontextprotocol/go-sdk** (Apache-2.0) — MCP server transport and tool registration.
- **spf13/cobra** (Apache-2.0) — CLI framework.

Each dependency retains its own license; nothing here supersedes them.
