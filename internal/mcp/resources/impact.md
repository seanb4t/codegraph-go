# codegraph_impact

Depth-bounded reverse blast radius of a symbol.

## Arguments

- `symbol` (string, required) — Symbol name.
- `depth` (integer, optional) — BFS depth (default 2, max 50).
- `path` (string, optional) — Repo path (default: server cwd).

## Result

Markdown text.
