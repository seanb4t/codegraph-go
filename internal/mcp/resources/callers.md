# codegraph_callers

List a symbol's reverse callers.

## Arguments

- `symbol` (string, required) — Symbol name.
- `limit` (integer, optional) — Cap on results returned.
- `path` (string, optional) — Repo path (default: server cwd).

## Result

Markdown text.
