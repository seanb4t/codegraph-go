---
milestone: v0.14.0
audited: 2026-09-19
status: tech_debt
scores:
  requirements: 55/55
  phases: 7/7
  integration: 7/7
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
nyquist:
  compliant_phases: [1, 2, 3, 4, 5, 6, 7]
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: compliant
security:
  phases_audited: 7
  threats_open: 0
tech_debt:
  - phase: 03-verb-fold
    items:
      - "The two hidden rename stubs (`query`, `unlock`) are scheduled for removal in v0.15.0 (backlog phase 999.5)."
  - phase: 04-cli-glow-up
    items:
      - "T-04-30 (low, open): `rewriteEnviron`'s auto branch does not drop `CLICOLOR_FORCE` when `NO_COLOR` is truthy, so `NO_COLOR=1 CLICOLOR_FORCE=1 codegraph version | cat` emits 8 ESC bytes. Human-output verbs only; the JSON/agent/MCP paths were re-probed clean."
      - "T-04-31 / AR-04-09 (accepted): `sanitizeControl` uses Go's latin-1-scoped `unicode.IsControl`, so bidi overrides and isolates (U+202E, U+2066-U+2069) pass through. Trojan-Source display deception, not escape injection."
      - "IN-01 (accepted): `RenderStatus`'s `Project:` value is sanitized but unstyled."
      - "The `styledTabWidth` / `TabWidth` / `NoTabConversion` advisory."
  - phase: 06-claude-code-pretooluse-nudge
    items:
      - "AR-06-08 (deferred): Claude's registered SessionStart and PreToolUse commands are unquoted, so a project dir or $HOME containing whitespace splits before the guard runs — a non-blocking hook-error notice, never a block. Changing it changes the owned identity, so it was deferred. Codex's commands are quoted from day one (D-20), so the milestone did not inherit this."
      - "AR-06-09 (accepted): ownership is block-granular on write — a handler a user adds INSIDE a codegraph-owned block is dropped on the next refresh."
  - phase: 07-codex-parity
    items:
      - "FIX-03's real-PTY tmux assertion is unexecuted: the local run was skipped by maintainer decision (tmux retired in favour of herdr) and CI's `tmux-e2e` job has not run because the branch is unpushed. Accepted override in 07-VERIFICATION.md; GitHub issue #75 tracks the harness's future."
      - "AR-07-06: `codex.go` derives global paths from `os.UserHomeDir()` and ignores `CODEX_HOME`, so a user who relocates `CODEX_HOME` gets an install against `~/.codex` instead — ineffective, never destructive."
      - "AR-07-07: `ActionRemoved` labels a hooks.json line `removed:` when the file was rewritten keeping a foreign group."
      - "AR-07-09 / IN-01: the Codex argv-shape decoder takes the last array element and has never been exercised live (Codex sends a string). A missed nudge at worst."
      - "AR-07-10: uninstall leaves emptied `.agents/`, `.agents/skills/` and `.codex/hooks/` directories behind (carried todo, deliberate: no recursive delete)."
      - "AR-07-11: removing codegraph's hook group shifts later foreign groups, and Codex's position-keyed trust re-flags them for review. Fail-safe, and normally unreachable since our group is appended last."
      - "AR-07-12: Codex's hook-trust hash covers the registered command, not the guard script's bytes — the deliberate intent of D-19's byte-stable registration."
  - phase: cross-phase
    items:
      - "`.mcp.json` is left as `{}` rather than deleted when it held only the codegraph entry (deliberate: the file may be shared with other MCP servers; tested)."
      - "`test/tmux/poll_contract_test.go` is not gofmt-clean under either toolchain; gofmt's doc-comment rewrite would corrupt a documented literal `''` example. Pre-existing since v0.13.0."
---

# v0.14.0 "Polish & Agent Reach" — Milestone Audit

**Audited:** 2026-09-19 · **Status:** tech_debt (no blockers; deferred items listed above need a review pass) · **Phases:** 7/7 complete

## Requirements

All 55 requirements in `REQUIREMENTS.md` are checked, each traced to a declaring plan's frontmatter and to a phase VERIFICATION.md. No orphans: every ID assigned to a phase appears in at least one plan's `requirements:` field and in that phase's verification table. No requirement is `unsatisfied` or `partial`.

## Phase Verifications

| Phase | Status | Score | Validation | Security |
|-------|--------|-------|------------|----------|
| 1 — Defect & Flake Burn-down | passed | 7/7 | validated, compliant | 0 open |
| 2 — Guards, CI Wiring, Docs | passed | 9/9 | validated, compliant | 0 open |
| 3 — Verb Fold | passed | 8/8 | validated, compliant | 0 open |
| 4 — CLI Glow-up | passed | 8/8 (+79 plan-level must_haves) | validated, compliant *(retroactive, 2026-09-19)* | 0 open *(retroactive; 33 threats, 1 low open below the gate)* |
| 5 — Agent Reach | passed | 8/9 (1 routed to human) | validated, compliant | 0 open |
| 6 — Claude Code PreToolUse Nudge | passed | 9/9 (1 routed to human) | validated, compliant | 0 open |
| 7 — Codex Parity | passed | 6/6 (5 + 1 accepted override) | validated, compliant | 0 open |

**Phase 4's debt was cleared during this audit.** Its `verify:post` hooks were never run when the phase closed, so it had no SECURITY.md and a `draft` VALIDATION.md. Both were produced on 2026-09-19: the security audit registered 33 threats (0 open at or above the `high` gate) and the validation audit found 0 gaps with every Wave 0 item shipped and green.

**Verification freshness:** Phases 1–6 read `stale` against their recorded digests, which is the expected and correct signal — each later phase legitimately modified files inside an earlier phase's `covered_files` (Phase 7 alone touched `internal/agents/shared.go`, `capabilities.go` and `internal/cli/install.go`). Staleness here means "the code moved since this report was written", not "the report was wrong". Every claim in those reports was re-exercised at HEAD by this audit's integration pass and by the full suite: 54 packages plus `internal/daemon` green at HEAD.

## Integration (7/7 seams)

An independent integration check traced every seam where two phases meet, at HEAD, with a real-binary end-to-end pass in an isolated HOME:

1. **Phase 5's capability table is still the single source of truth.** Codex's Phase 7 flip goes through `Capabilities()`; `--print-config-style` derives every field from the table for all 8 targets; the published doc is diffed against the table by a drift test for 16 rows.
2. **Phase 6's `internal/nudge` core is shared, not forked.** One `Qualifies`, one `Gate`, one `Text`, one hidden subcommand with two harness envelopes.
3. **Phase 4's colour boundary holds** after Phases 5–7 added output: the archtests pass, the agent/MCP path stays ESC-free, and 31/31 plain goldens pass.
4. **Phase 3's folded verbs still behave**: both rename stubs print once and exit 1; `search --full` and `daemon unlock` carry `--color`.
5. **Shared-file seams hold**: `TestSharedAgentsMD_UninstallOrders` covers every order, and the shared skill manifest survived 1554 generated install/uninstall sequences.
6. **The published capability table agrees with the code** and is linked from the README.
7. **E2E**: init → install (both scopes, two agents) → the MCP entry, skill and instructions block all present → uninstall removes everything it created.

The integration check surfaced one warning, now fixed: the T-04-24 sanitizer fix had claimed parity with the sibling file and error lines, but those sanitized only in the styled branch. Both now sanitize before the branch (`50d680b1`), pinned by `TestInstall_PlainBranchPathsAreControlCharacterSanitized`.

## Security

Seven phase audits, 0 threats open at or above the `high` gate in every one. Phase 7's audit closed 37/37 and independently reproduced four mutation families; Phase 4's retroactive audit closed 31/33 with two non-blocking lows, one of which (T-04-24, terminal escape injection through install notes) was a live regression this milestone introduced and fixed during the audit under mutation Family (k1).

## Nyquist

All seven phases are `status: validated` with `nyquist_compliant: true`. Manual-only rows are recorded as such rather than counted as gaps: colour readability by eye, the `TERM` matrix, OSC-11 latency, the `less -R` pager check, the Codex live sessions (CODEX-01/05/06, all PASS) and FIX-03's real-PTY assertion (deferred to CI).

## Verdict

No blockers. The milestone's definition of done is met: every requirement satisfied, every phase verified, validated and security-audited, and every cross-phase seam traced at HEAD. The status is `tech_debt` because the deferred items above deserve a review pass before or during v0.15.0 — most notably FIX-03's outstanding CI evidence, the `NO_COLOR`/`CLICOLOR_FORCE` precedence gap (T-04-30), AR-06-08's unquoted Claude hook commands, and the `CODEX_HOME` advisory.
