---
phase: 06-agent-skill-package-skill-md-sessionstart-nudge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/hooks/session-nudge.sh
  - .claude/settings.json
  - .claude/hooks/hooks.json
  - internal/agents/hookpackage_test.go
autonomous: true
requirements: [NUDGE-01, NUDGE-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running .claude/hooks/session-nudge.sh with CLAUDE_PROJECT_DIR pointed at a tree containing a .codegraph directory writes exactly one line to stdout, writes nothing to stderr, and exits 0 (NUDGE-01)"
    - "Running the same script with CLAUDE_PROJECT_DIR pointed at a tree containing no .codegraph entry writes zero bytes to stdout, zero bytes to stderr, and exits 0 (NUDGE-02)"
    - "Boundary: a regular FILE named .codegraph is treated as absent — the script emits nothing, because the check is a directory test, not a name test"
    - "Boundary: an EMPTY .codegraph directory is treated as present — the script emits the nudge without opening, reading, or listing anything inside it (NUDGE-01's no-index-read constraint)"
    - "Boundary: with CLAUDE_PROJECT_DIR unset entirely, the script resolves the check against its own working directory rather than erroring or emitting unconditionally"
    - "Precision: the emitted line is byte-identical to the single pinned literal, terminated by exactly one newline, with no leading or trailing whitespace variance"
    - "Concurrency: eight simultaneous invocations against the same tree each emit byte-identical output, and the script creates, modifies, or deletes no file anywhere — it holds no lock and keeps no state, so interruption leaves nothing behind"
    - ".claude/settings.json registers the script under hooks.SessionStart on both the startup matcher and the resume matcher, each referencing the ${CLAUDE_PROJECT_DIR}-anchored path rather than a bare script name (D-07, T-06-02)"
    - ".claude/hooks/hooks.json carries a SessionStart block structurally equal to .claude/settings.json's, so Phase 7's go:embed source cannot drift from what actually runs here (D-04, A2)"
    - "Applying internal/agents' own addClaudeAllowPermission to the committed .claude/settings.json leaves the hooks block intact and unchanged (T-06-03)"
  artifacts:
    - ".claude/hooks/session-nudge.sh (executable, POSIX sh)"
    - ".claude/settings.json (net-new, committed, project-scoped)"
    - ".claude/hooks/hooks.json (Phase-7 go:embed source fragment)"
    - "internal/agents/hookpackage_test.go"
  key_links:
    - "settings.json hooks.SessionStart[].hooks[].command -> the on-disk .claude/hooks/session-nudge.sh path (a typo here makes the nudge silently never fire)"
    - "hooks.json SessionStart block -> settings.json SessionStart block (kept equal by test, not by discipline)"
    - "session-nudge.sh -> the .codegraph directory existence check only; never the codegraph binary, never the MCP server"
  prohibitions:
    - "The nudge script must not invoke the codegraph binary, open or read the index, parse its stdin payload, or use eval — its only filesystem operation is the single .codegraph directory existence check."
    - "The live SessionStart registration must not depend on .claude/hooks/hooks.json. Claude Code never reads that path in a non-plugin project directory; hooks.json exists in this repo solely as Phase 7's embed source."
    - "The nudge must not carry a tool list, worked example, fallback-syntax reminder, tool count, environment variable name, or absolute filesystem path — that detail belongs to SKILL.md and the codegraph:// resources, not to a session-start pointer (D-06)."
---

<objective>
Ship the SessionStart nudge as a working, dogfooded package in this repository: a POSIX shell script that decides on one directory-existence check, a project-scoped `.claude/settings.json` that actually registers it with Claude Code, a `hooks.json` fragment held equal to that registration for Phase 7 to embed, and a Go test that proves the behavior by executing the shipped script in both trees rather than reading it.

Purpose: NUDGE-01/NUDGE-02 are the half of this phase that is fully mechanically verifiable. Proving the whole path end to end first — registration through script through observed stdout — retires the phase's single highest-consequence known pitfall (a hook registered in a file Claude Code never reads) before any content authoring is built on top of it.

Output: `.claude/hooks/session-nudge.sh`, `.claude/settings.json`, `.claude/hooks/hooks.json`, `internal/agents/hookpackage_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-CONTEXT.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-RESEARCH.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 6 (not pre-existing — exclude from drift verification against the current tree):

| Artifact | Kind | Created by |
|---|---|---|
| `.claude/hooks/session-nudge.sh` | shell script | 06-01 |
| `.claude/settings.json` | JSON config (net-new, committed) | 06-01 |
| `.claude/hooks/hooks.json` | JSON fragment (Phase-7 embed source) | 06-01 |
| `internal/agents/hookpackage_test.go` | Go test file | 06-01 |
| `TestSessionNudgeBehavesPerIndexPresence` | Go test func | 06-01 |
| `TestSessionNudgeOutputIsPinnedAndStateless` | Go test func | 06-01 |
| `runSessionNudge` | Go test helper | 06-01 |
| `nudgeLine` | Go test const | 06-01 |
| `sessionNudgeScriptPath`, `claudeSettingsFilePath`, `claudeHooksFragmentPath` | Go test consts | 06-01 |
| `TestHookRegistrationMatchesFragmentAndScript` | Go test func | 06-01 |
| `TestClaudeInstallPreservesHooksBlock` | Go test func | 06-01 |
| `sessionStartBlock` | Go test helper | 06-01 |
| `.claude/skills/codegraph/SKILL.md` | agent skill content | 06-02 / 06-03 |
| `internal/mcp/skill_claims_drift_test.go` | Go test file | 06-02 / 06-03 |
| `.claude/skills/codegraph/verification/*.md` | rehearsal artifacts | 06-04 |

<assumption_delta_decision>
The `assumption-delta` scan fired on a `pluralization` signal — ROADMAP success criterion 2's phrase "becoming a **second**, drifting copy of the reference".

- **Noun now primary:** the Phase-5 `codegraph://tools/*` MCP resource remains the single primary carrier of per-tool reference facts.
- **Decision:** `no-change`.
- **Rationale:** the signal is the roadmap naming the failure mode it forbids, not a generalization being introduced. SKILL.md is deliberately a different kind of artifact (a decision procedure) that *references* the reference by URI; it is not a second instance of the same noun. Promoting or adding-alongside would be exactly the duplication Phase 5's own D-01 and this plan's first prohibition rule out. Non-blocking, recorded for traceability.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end SessionStart nudge — one path, script through live registration</name>
  <files>internal/agents/hookpackage_test.go, .claude/hooks/session-nudge.sh, .claude/settings.json</files>
  <read_first>
    - `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-RESEARCH.md` — read the "Critical Correction" paragraph in Summary and "Pattern 2" in Architecture Patterns in full before writing anything. CONTEXT.md's D-03/D-04 name `.claude/hooks/hooks.json` as the registration file; that is wrong for a non-plugin project and this task must not follow it.
    - `internal/agents/claude.go` — `claudeSettingsPath` (line ~108) already resolves `.claude/settings.json` for `LocationLocal`, and `addClaudeAllowPermission` (line ~120) already merges into it. The file you create must survive that merge.
    - `internal/mcp/instructions_contract_test.go` lines 10-33 — the `readmePath = "../../README.md"` convention for reaching a repo-root file from a package test, including its rule that a missing file is a loud `t.Fatalf`, never a `t.Skip`.
    - `internal/agents/shared_test.go` and `internal/agents/testhelpers_test.go` — existing helper and table-test conventions for package `agents`.
    - `internal/mcp/resources_schema_drift_test.go` lines 68-97 and 419-460 — the mandatory "prove the checker itself can fire" sub-test shape every new guard in this repository carries.
  </read_first>
  <behavior>
    Write these as failing tests FIRST, in `internal/agents/hookpackage_test.go` (package `agents`), before the script exists. A missing script must produce a loud failure, not a skip.

    `TestSessionNudgeBehavesPerIndexPresence` — table-driven, each case builds a fresh `t.TempDir()`, runs the shipped script via `exec.Command(sessionNudgeScriptPath)` with `cmd.Env = append(os.Environ(), "CLAUDE_PROJECT_DIR="+dir)`, and captures stdout, stderr and exit code separately:
    - `.codegraph` present as a directory -> stdout equals `nudgeLine + "\n"` exactly; stderr empty; exit 0.
    - no `.codegraph` entry at all -> stdout empty; stderr empty; exit 0.
    - `.codegraph` present as a REGULAR FILE -> stdout empty; stderr empty; exit 0 (directory test, not name test).
    - `.codegraph` present as an EMPTY directory -> stdout equals `nudgeLine + "\n"`; nothing inside it is read.
    - `CLAUDE_PROJECT_DIR` removed from the environment entirely, `cmd.Dir` set to an indexed temp tree -> stdout equals `nudgeLine + "\n"`; exit 0.
    - `CLAUDE_PROJECT_DIR` removed, `cmd.Dir` set to an unindexed temp tree -> stdout empty; exit 0.

    `TestSessionNudgeOutputIsPinnedAndStateless`:
    - precision: the indexed-tree stdout is byte-equal to `nudgeLine + "\n"`, and `strings.Count(stdout, "\n")` is exactly 1.
    - concurrency: 8 goroutines invoke the script simultaneously against one shared indexed temp tree; every returned stdout is byte-identical and every exit code is 0.
    - statelessness: snapshot the temp tree's `filepath.WalkDir` entry set before and after the concurrent burst and assert the two sets are equal — the script must create, modify, or remove nothing.
    - guard-the-guard: assert `nudgeLine` is non-empty and that a deliberately wrong expectation would not match, following `TestResourceCountCheckerIsNotVacuous`'s shape.
  </behavior>
  <action>
    Create `internal/agents/hookpackage_test.go` in package `agents` with:
    - `const sessionNudgeScriptPath = "../../.claude/hooks/session-nudge.sh"` and `const claudeSettingsFilePath = "../../.claude/settings.json"`, each with a doc comment citing `instructions_contract_test.go`'s `readmePath` convention as the precedent and stating that a missing file is a regression, not a skip.
    - `const nudgeLine` holding the D-06 message verbatim: This repo has a codegraph index — prefer codegraph_explore / `codegraph explore` over grep for where-is-X / how-does-Y questions.
      Document at the declaration site WHY this one literal is hand-typed rather than derived: the message is a fixed one-line pointer with no runtime source to derive from, exactly the situation `allowlistEnvName` in `instructions_contract_test.go` documents for itself. Note that the derived-honesty guards over this same text (real tool names only, no env var, no host path, no counts) land in 06-03's `internal/mcp/skill_claims_drift_test.go`, so this literal pin is the first of two layers, not the only one.
    - `runSessionNudge(t *testing.T, dir string, useEnv bool) (stdout, stderr string, exitCode int)` — a helper that runs the script, returns all three, and `t.Fatalf`s on any error that is not a non-zero exit status.
    - The two test functions described in `<behavior>`.

    Run the tests and confirm they fail because the script does not exist (RED). Only then author `.claude/hooks/session-nudge.sh`.

    `.claude/hooks/session-nudge.sh` contents, per RESEARCH.md Pattern 2 and D-06:
    - shebang `#!/bin/sh`
    - a comment naming the file's purpose and the two requirement IDs NUDGE-01 / NUDGE-02, and stating that no stdin parsing is needed because the settings.json matcher already filters to startup and resume
    - one `if [ -d "${CLAUDE_PROJECT_DIR:-.}/.codegraph" ]; then` guard whose body is a single `printf` with format `'%s\n'` and the message as a SINGLE-QUOTED argument. Use `printf`, not `echo`: `echo`'s escape handling varies across `sh` implementations, and the message contains backticks that would become command substitution under double quotes. Single quotes make both problems structurally impossible.
    - `exit 0` unconditionally after the `fi`.
    - The script must not consume its stdin, must not use any dynamic-evaluation builtin, must not use command substitution in any form, must not expand a variable inside the printf argument, and must perform no second filesystem operation of any kind. Task 1's acceptance criteria gate each of these mechanically.
    Make it executable (mode 0755) and confirm the executable bit is what git records.

    Then create `.claude/settings.json` (net-new; this repository has `.claude/settings.local.json` only, and that file is gitignored globally). Top-level key `hooks`, containing key `SessionStart` whose value is an array of exactly two entries. Entry one has `"matcher": "startup"`; entry two has `"matcher": "resume"`. Each entry has a `"hooks"` array holding one object with `"type": "command"` and `"command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/session-nudge.sh"`. The `${CLAUDE_PROJECT_DIR}` anchor is a security control (T-06-02), not cosmetics — a bare script name would be resolvable through `$PATH` by a shadowing binary. Do not add `permissions`, `enabledMcpjsonServers`, or any other key: `addClaudeAllowPermission` adds `permissions` itself at install time and this plan must not pre-empt it.

    Re-run the tests and confirm GREEN.
  </action>
  <verify>
    <automated>go test ./internal/agents/... -run 'TestSessionNudge' -count=1 -v</automated>
    <automated>test -x .claude/hooks/session-nudge.sh &amp;&amp; echo EXEC_BIT_OK</automated>
    <automated>node -e 'const s=require("./.claude/settings.json");const a=s.hooks.SessionStart;if(a.length!==2)throw new Error("want 2 SessionStart entries, got "+a.length);const m=a.map(e=>e.matcher).sort();if(JSON.stringify(m)!==JSON.stringify(["resume","startup"]))throw new Error("matchers: "+m);a.forEach(e=>{if(e.hooks.length!==1||e.hooks[0].type!=="command"||e.hooks[0].command!=="${CLAUDE_PROJECT_DIR}/.claude/hooks/session-nudge.sh")throw new Error("bad hook entry: "+JSON.stringify(e))});console.log("SETTINGS_OK")'</automated>
  </verify>
  <reversibility rating="costly">D-04 makes these exact paths load-bearing for Phase 7's `go:embed` directive; relocating later means re-pointing the embed and re-verifying install/uninstall round-trips across Phase 7's agent targets.</reversibility>
  <acceptance_criteria>
    - `go test ./internal/agents/... -run 'TestSessionNudge' -count=1` exits 0 with both test functions and every sub-case reported as passing.
    - The test file was observed RED before the script existed, and that observation is recorded in the plan summary.
    - `.claude/hooks/session-nudge.sh` is executable on disk (`test -x` succeeds) and the mode git records once staged is `100755` (confirm with `git ls-files -s .claude/hooks/session-nudge.sh` after `git add`).
    - Counting only non-comment lines, the script body contains exactly one `printf` and exactly one `-d` test: `grep -v '^#' .claude/hooks/session-nudge.sh | grep -c 'printf' || true` prints `1`, and `grep -v '^#' .claude/hooks/session-nudge.sh | grep -c -- '-d ' || true` prints `1`.
    - Counting only non-comment lines, the script performs no dynamic evaluation and no command substitution. Run `grep -v '^#' .claude/hooks/session-nudge.sh | grep -cE 'eval|[$][(]|[\x60]' || true` — it must print `0`.
    - The stdout observed in the indexed-tree case is byte-equal to the `nudgeLine` constant plus one `\n`, asserted with `==` on the raw string, not with `strings.Contains`.
    - The `node -e` settings.json shape check prints `SETTINGS_OK`.
    - `.claude/settings.json` has exactly one top-level key, `hooks`.
  </acceptance_criteria>
  <done>A session-start hook that is registered where Claude Code actually reads it, decided by one directory test, whose emitted bytes are pinned and whose absence-path emits nothing — all four properties proven by executing the shipped script, not by reading it.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-7 embed fragment held equal to the live registration, and proven to survive `codegraph install`</name>
  <files>.claude/hooks/hooks.json, internal/agents/hookpackage_test.go</files>
  <read_first>
    - `.claude/settings.json` — the file Task 1 just created; the fragment must mirror its `SessionStart` block exactly.
    - `internal/agents/claude.go` lines 118-190 — `addClaudeAllowPermission` and `removeClaudeAllowPermission`. Note that both round-trip the whole decoded map (`existing`) and only touch `permissions`, which is why a `hooks` key survives; this task pins that property rather than assuming it.
    - `internal/agents/claude_test.go` — existing conventions for driving `addClaudeAllowPermission` against a temp file in this package.
    - `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-RESEARCH.md` — assumption A2 (the two-file drift risk this task closes) and Open Question 1 (which this plan answers by keeping both files and gating them with a test rather than dropping one).
  </read_first>
  <action>
    Create `.claude/hooks/hooks.json` carrying a single top-level `hooks` key whose `SessionStart` value is structurally identical to `.claude/settings.json`'s. Add a leading comment-free structure (JSON permits no comments) — instead, record the file's role in this plan and in the summary: it is NOT read by Claude Code in this repository; it exists as the versioned source Phase 7's `go:embed` directive points at, per D-04.

    Extend `internal/agents/hookpackage_test.go` with:
    - `const claudeHooksFragmentPath = "../../.claude/hooks/hooks.json"`.
    - `sessionStartBlock(t *testing.T, path string) any` — decodes the file with `encoding/json` into `map[string]any` and returns the value at `hooks.SessionStart`, `t.Fatalf`ing if the file is missing or the path is absent.
    - `TestHookRegistrationMatchesFragmentAndScript`: asserts `reflect.DeepEqual(sessionStartBlock(t, claudeSettingsFilePath), sessionStartBlock(t, claudeHooksFragmentPath))`, with a failure message that names both paths and states that Phase 7 would embed a fragment that differs from what actually runs here. Then, from the decoded settings block, extract every `command` string, replace the literal `${CLAUDE_PROJECT_DIR}` prefix with `../..`, and assert each resulting path exists and is executable via `os.Stat` and a `Mode()&0111 != 0` check — so a rename of the script fails this test instead of silently disabling the nudge.
    - Guard-the-guard: a sub-test feeding `sessionStartBlock`-shaped synthetic values through the same comparison, covering equal blocks, a differing matcher, and a differing command, proving the comparison discriminates (the `TestResourceStemSetDiffIsNotVacuous` pattern).
    - `TestClaudeInstallPreservesHooksBlock`: copy the committed `.claude/settings.json` into a `t.TempDir()`, call `addClaudeAllowPermission` on the copy, re-decode it, and assert (a) the `hooks` key is still present and `reflect.DeepEqual` to the original block, and (b) `permissions.allow` now contains `claudeAllowToken`. Then call `removeClaudeAllowPermission` and assert the `hooks` block is STILL equal. This pins T-06-03: `codegraph install --local` followed by `codegraph uninstall --local` must not disturb the dogfooded hook registration.
  </action>
  <verify>
    <automated>go test ./internal/agents/... -run 'TestHookRegistration|TestClaudeInstallPreservesHooksBlock' -count=1 -v</automated>
    <automated>node -e 'const a=require("./.claude/settings.json").hooks.SessionStart,b=require("./.claude/hooks/hooks.json").hooks.SessionStart;const s=JSON.stringify;if(s(a)!==s(b))throw new Error("drift:\n"+s(a)+"\n"+s(b));console.log("FRAGMENT_PARITY_OK")'</automated>
    <automated>go test ./internal/agents/... -count=1</automated>
  </verify>
  <reversibility rating="costly">Keeping `.claude/hooks/hooks.json` as a second file (rather than letting Phase 7 extract from `settings.json`) is the answer to RESEARCH Open Question 1 and is what D-04 names; reversing it means changing Phase 7's embed source.</reversibility>
  <acceptance_criteria>
    - `go test ./internal/agents/... -count=1` exits 0 — the whole package, not only the new tests.
    - The `node -e` parity check prints `FRAGMENT_PARITY_OK`.
    - `TestHookRegistrationMatchesFragmentAndScript` fails when the fragment is mutated: apply one mutation (change the `resume` matcher to `clear` in `.claude/hooks/hooks.json`), observe the failure text naming both files, revert byte-clean, and re-run green. Record the observed failure text in the plan summary.
    - `TestClaudeInstallPreservesHooksBlock` asserts the hooks block after BOTH `addClaudeAllowPermission` and `removeClaudeAllowPermission`, not only after add.
    - The command-path resolution sub-assertion uses `os.Stat` plus an executable-bit check on the real file, not a string comparison against a hard-coded path.
    - Every new checker has a non-vacuity sub-test or a demonstrated-red mutation recorded.
  </acceptance_criteria>
  <done>Phase 7 has a stable, versioned embed source that cannot silently diverge from the registration this repository actually runs, and `codegraph install`/`uninstall` are proven not to clobber it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code hook dispatcher -> `session-nudge.sh` | The harness invokes a repo-controlled script with the user's own shell permissions and pipes a JSON event payload to its stdin |
| `session-nudge.sh` -> filesystem | The script reads `CLAUDE_PROJECT_DIR` from its environment and performs one path test against it |
| `codegraph install --local` -> `.claude/settings.json` | A binary this project ships decodes, mutates, and re-writes a file this plan creates and commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | `.claude/hooks/session-nudge.sh` | medium | mitigate | Script is a committed, reviewable, ~6-line POSIX file with no `eval`, no stdin read, no command substitution and no variable expansion inside its single-quoted `printf` argument. Task 1's acceptance criteria grep-gate each of those absences. |
| T-06-02 | Spoofing | `.claude/settings.json` `command` field | medium | mitigate | The command is the `${CLAUDE_PROJECT_DIR}`-anchored absolute path, never a bare script name resolvable through `$PATH`. Pinned exactly by Task 1's `node -e` shape check and by Task 2's path-resolution assertion. |
| T-06-03 | Tampering | `.claude/settings.json` under `addClaudeAllowPermission` | high | mitigate | `TestClaudeInstallPreservesHooksBlock` drives the real install and uninstall merge functions against a copy of the committed file and asserts the `hooks` block is `reflect.DeepEqual` before and after both. This is the file-safety class that produced v1.0 Phase 5's reproduced data-loss Criticals. |
| T-06-04 | Information Disclosure | nudge stdout (injected into agent context) | low | mitigate | The emitted bytes are a compile-time literal pinned by byte-equality; the script interpolates no path, hostname, or environment value into its output. Task 1's precision assertion is the gate. |
| T-06-05 | Denial of Service | hook execution on every session start | low | accept | One `test -d` and at most one `printf` per session-start event. Cost is below measurement noise; no rate limiting, caching, or timeout is warranted, and adding one would introduce more failure surface than it removes. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no external package (RESEARCH.md "Package Legitimacy Audit": skipped per its own scope condition). No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `go test ./internal/agents/... -count=1` green.
- `task test` green (full suite) — this plan touches no Go production code path, so any failure elsewhere is a pre-existing condition and must be identified as such, not absorbed.
- The nudge script executed against both an indexed and an unindexed tree, with the two stdout captures diffed and the diff recorded — ROADMAP success criteria 4 and 5 require this be *established by executing*, not by reading.
- One demonstrated-red mutation against the fragment-parity guard, applied, observed, and reverted byte-clean, following `test/wireoracle/MUTATION-PROOF.md`'s discipline.
</verification>

<success_criteria>
- NUDGE-01: a session started in a `.codegraph/`-indexed tree receives exactly one short pointer line, decided by one directory-existence check, with no codegraph invocation and no index read anywhere in the path.
- NUDGE-02: a tree with no `.codegraph/` produces zero output bytes and performs no filesystem work beyond that single check.
- The registration lives in the one file Claude Code actually reads for a project-scoped `SessionStart` hook, and the Phase-7 embed fragment is gated equal to it by a test.
- Installing and uninstalling codegraph into this repository leaves the hook registration untouched.
</success_criteria>

<output>
Create `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-01-SUMMARY.md` when done.
</output>
