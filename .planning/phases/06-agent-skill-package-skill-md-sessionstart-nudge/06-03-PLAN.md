---
phase: 06-agent-skill-package-skill-md-sessionstart-nudge
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - .claude/skills/codegraph/SKILL.md
  - internal/mcp/skill_claims_drift_test.go
  - test/wireoracle/MUTATION-PROOF.md
autonomous: true
requirements: [SKILL-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SKILL.md carries exactly three worked examples, each under its own level-3 heading inside one level-2 worked-examples section that sits after the decision table (SKILL-02; D-05 locks the count at three and locks their order)"
    - "Worked example 1 reproduces the 2026-08-08 misdirection incident end to end: the question that was asked, the grep-first path that misled the session, and the codegraph call that would have answered it — sourced from the resolved debug log's own root-cause record, not paraphrased from memory"
    - "Worked example 1 frames the incident in the past tense against the pre-fix instructions string and does not imply the defect is still live — the current instructions constant already names all three visibility mechanisms"
    - "Worked example 2 routes a signature-change blast-radius question to codegraph_impact rather than to manual call-site grepping"
    - "Worked example 3 routes a symbol lookup that crosses an interface implementation or dynamic-dispatch hop to codegraph_explore — the case a text search structurally cannot follow"
    - "Adding the worked examples leaves every 06-02 guard green: the decision table still precedes the second level-2 heading, the body stays inside both budget limits, and no example introduces a numeric default claim, an unregistered tool name, a dead resource URI, an unreal environment variable, or an absolute host path"
    - "The derived-honesty guards extend to the nudge script's emitted text as well as to SKILL.md, so the message pinned byte-exact in 06-01 is additionally checked against the real tool roster and the real environment-variable name"
    - "Every guard added or extended in this phase has been observed failing against a real mutation of the real tree and the mutation reverted byte-clean, recorded in test/wireoracle/MUTATION-PROOF.md"
    - "Boundary (edge-probe resolution for SKILL-02): the worked-example count gate fails at two examples and fails at four; exactly three passes"
  artifacts:
    - ".claude/skills/codegraph/SKILL.md (worked-examples section, three examples)"
    - "internal/mcp/skill_claims_drift_test.go (extended: worked-example count gate, nudge-text derived guards)"
    - "test/wireoracle/MUTATION-PROOF.md (new mutations appended, numbering continued from the file's actual state)"
  key_links:
    - "worked example 1 -> .planning/debug/resolved/mcp-server-one-tool-only.md's root_cause and Symptoms sections"
    - "worked example 2 -> codegraph_impact and the codegraph impact CLI command"
    - "nudge script text -> allToolNames(), allowlistEnvName, hostFactsIn (the second guard layer over 06-01's byte-exact literal pin)"
  prohibitions:
    - "No worked example may state a parameter name, default value, maximum, or return shape. A worked example shows which call answers which question; the codegraph://tools/<stem> resource carries the call's signature."
    - "Worked example 1 must not present the 2026-08-08 defect as current behaviour. The instructions string it misled a session with has already been corrected; presenting it as live would make the skill itself the next stale claim."
    - "Mutation numbering in test/wireoracle/MUTATION-PROOF.md must not be assumed from this plan's text. Read the file's actual highest mutation number and continue from it — 05-03 already had to correct exactly this assumption once."
---

<objective>
Add SKILL-02's three worked examples to SKILL.md, extend the drift guard to gate their count and to cover the nudge script's text with the same derived checks, and prove every guard this phase introduced can actually fire by mutating the real tree.

Purpose: RESEARCH Pitfall 1 is that a skill reads well and changes nothing. Worked examples phrased the way a real user actually asks are the difference between a document an agent has read and a procedure an agent follows — and the first example is this milestone's own origin incident, which makes the skill's argument from evidence rather than from assertion.

Output: SKILL.md's worked-examples section, the extended guard, and appended mutation proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-CONTEXT.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-RESEARCH.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-PATTERNS.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-02-SUMMARY.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 6 (not pre-existing — exclude from drift verification against the current tree):

| Artifact | Kind | Created by |
|---|---|---|
| `.claude/skills/codegraph/SKILL.md` worked-examples section | agent skill content | 06-03 |
| `workedExampleHeadingRe`, `countSkillWorkedExamples`, `skillWorkedExampleCount` | Go test var/helper/const | 06-03 |
| `TestSkillCarriesExactlyThreeWorkedExamples` | Go test func | 06-03 |
| `TestSkillWorkedExampleCounterIsNotVacuous` | Go test func | 06-03 |
| `nudgeScriptPath`, `TestNudgeTextNamesOnlyRealTools`, `TestNudgeTextCarriesNoUnpinnedFacts` | Go test consts/funcs | 06-03 |
| `test/wireoracle/MUTATION-PROOF.md` new mutation entries | verification record | 06-03 |
| `.claude/hooks/session-nudge.sh`, `.claude/settings.json`, `.claude/hooks/hooks.json`, `internal/agents/hookpackage_test.go` | hook package | 06-01 |
| `internal/mcp/skill_claims_drift_test.go` (base) | Go test file | 06-02 |
| `.claude/skills/codegraph/verification/*.md` | rehearsal artifacts | 06-04 |

<tasks>

<task type="auto">
  <name>Task 1: The three worked examples (SKILL-02)</name>
  <files>.claude/skills/codegraph/SKILL.md</files>
  <read_first>
    - `.planning/debug/resolved/mcp-server-one-tool-only.md` — read the frontmatter `root_cause`, `fix` and `oracle_type` blocks and the `## Symptoms` section in full. Example 1 is sourced from this record; quote and cite rather than paraphrase. The AND-gate-of-three root cause and the "a missing index produces ZERO tools, never one" reasoning are the load-bearing details.
    - `internal/mcp/server.go` line 56 — the CURRENT `instructions` constant. It already names all three visibility mechanisms. Example 1 must be written against the fact that this string has been fixed.
    - `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-RESEARCH.md`, the "Code Examples" section — carries the verified shape of all three examples, including the note for the planner about not implying the bug is still live, and the verified CLI surface for example 2 (`internal/mcp/tools.go` lines 264-268 and `internal/cli/impact.go` lines 16-23, 64-66).
    - `.claude/skills/codegraph/SKILL.md` — the current file as 06-02 left it; you are inserting a section, not rewriting the file.
    - `internal/mcp/skill_claims_drift_test.go` — every constraint your prose must satisfy is already written down here.
    - `internal/cli/explore.go` lines 1-40 — `codegraph explore <query...>` is variadic and emits markdown text only; example 3's shell fallback must be spelled correctly.
  </read_first>
  <action>
    Insert one new `## ` worked-examples section into `.claude/skills/codegraph/SKILL.md`, positioned AFTER the decision table section and after the skip-condition section, and before or after the reference-pointer section as reads best — but never before the decision table, which `TestSkillLeadsWithDecisionTable` gates.

    Three `### ` sub-headings, in D-05's locked order.

    Example 1 — the 2026-08-08 misdirection incident. Give it the shape the incident actually had: the user report ("the mcp server is only showing one tool"), the first move that misled the session (searching the instructions string and README for an explanation), why that path misled (the instructions string of the day attributed tool visibility solely to index state, and a missing index structurally produces zero tools, never one, so the single explanation offered was the one cause that could not produce the symptom), and what a codegraph call would have done instead (an explore query against the server's own source surfaces the allowlist-resolution path directly rather than trusting a wire string). Close with one sentence stating plainly that the instructions string has since been corrected and now names every visibility mechanism, so this example is a lesson about the failure mode and not a live defect. Cite the debug log by repository-relative path.

    Example 2 — impact analysis before a refactor. A signature-change question, routed to `codegraph_impact` with `codegraph impact <symbol>` named as the shell fallback, with one sentence on why a depth-bounded reverse blast radius beats grepping for call sites by hand. State no flag defaults and no maximum; if depth matters to the example at all, point at the tool's `codegraph://tools/impact` resource for the parameter's semantics.

    Example 3 — cross-file symbol lookup across dynamic dispatch. A where-is-X or how-does-Y-work question whose answer sits behind an interface implementation or a dynamic-dispatch hop. Make the structural argument explicit: a text search matches the call site's spelling, not the implementation it resolves to, so the hop is exactly what it cannot follow; `codegraph_explore`'s call-graph resolution crosses it and returns the verbatim source plus the path. Name `codegraph explore` as the shell fallback.

    Keep the section tight — the body budget is a gate, and the decision table plus these three examples plus the existing sections must fit inside it. If the budget binds, cut prose from the examples before cutting the decision table.
  </action>
  <verify>
    <automated>go test ./internal/mcp/... -run 'TestSkill' -count=1 -v</automated>
    <automated>grep -c '^### ' .claude/skills/codegraph/SKILL.md</automated>
    <automated>wc -l -c .claude/skills/codegraph/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/mcp/... -run 'TestSkill' -count=1` exits 0 — every 06-02 guard still green after the insertion, including the body-budget and decision-table-ordering gates.
    - `grep -c '^### ' .claude/skills/codegraph/SKILL.md` prints exactly `3`.
    - Example 1 cites `.planning/debug/resolved/mcp-server-one-tool-only.md` by path, and its closing sentence states the instructions string has been corrected. Confirm the citation with `grep -c 'mcp-server-one-tool-only' .claude/skills/codegraph/SKILL.md || true` printing at least `1`.
    - Example 1 names `CODEGRAPH_MCP_TOOLS` as the mechanism that actually governed the observed tool list.
    - Example 2 names `codegraph_impact`; example 3 names `codegraph_explore`. Both survive `TestSkillNamesOnlyRealTools`.
    - `TestSkillDefersNumericFactsToResources` still passes, so no example introduced a default-or-maximum claim.
    - `wc -c` reports at most 20000 bytes and `wc -l` at most 500 lines.
  </acceptance_criteria>
  <done>Three worked examples an agent can pattern-match against a real user's phrasing, the first of them the documented incident this milestone exists to prevent recurring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the example count, and extend the derived guards to the nudge text</name>
  <files>internal/mcp/skill_claims_drift_test.go</files>
  <read_first>
    - `internal/mcp/skill_claims_drift_test.go` — the file 06-02 wrote; you are extending it, reusing its helpers and its doc-comment register.
    - `.claude/hooks/session-nudge.sh` — the script 06-01 shipped; its emitted line is what the new derived guards read.
    - `internal/agents/hookpackage_test.go` — the `nudgeLine` constant and its doc comment explaining that the byte-exact pin is the FIRST of two layers and that this file is the second. Keep the two comments consistent with each other.
    - `internal/mcp/resources_schema_drift_test.go` lines 419-460 — the `TestResourceCountCheckerIsNotVacuous` shape your new counter's non-vacuity test mirrors.
    - `internal/mcp/instructions_contract_test.go` lines 124-157 — `docNamesCompanionsWithoutTheFilter`, which you apply to the nudge text as well.
  </read_first>
  <behavior>
    - `TestSkillCarriesExactlyThreeWorkedExamples`: counts `### ` headings inside the worked-examples `## ` section and asserts the count equals 3. The failure message must state the found count and name D-05 as the source of the number. Boundary behaviour is what matters: two examples fail, four examples fail, three pass.
    - `TestSkillWorkedExampleCounterIsNotVacuous`: synthetic documents with zero, one, two, three and four `### ` headings, plus one where a `### ` heading appears OUTSIDE the worked-examples section and must not be counted, plus one where a line beginning with `####` must not be counted as a `###`.
    - `TestNudgeTextNamesOnlyRealTools`: reads `.claude/hooks/session-nudge.sh`, extracts every `toolNameTokenRe` match, and asserts each is a member of `allToolNames()`; fatal if zero matches were found. Additionally asserts `docNamesCompanionsWithoutTheFilter` returns nil for the script's text — the nudge names only the one tool no filter can remove, so it makes no claim the filter could falsify.
    - `TestNudgeTextCarriesNoUnpinnedFacts`: asserts `hostFactsIn` finds nothing in the script, `numericClaimsMultiset` is empty, `countClaimsIn` returns nothing, and every `envVarTokenRe` match (if any) equals `allowlistEnvName`.
  </behavior>
  <action>
    Extend `internal/mcp/skill_claims_drift_test.go` with:
    - `workedExampleHeadingRe = regexp.MustCompile("(?m)^### ")` and `countSkillWorkedExamples(body string) int`, which locates the worked-examples `## ` section by finding the level-2 heading whose text contains "example" case-insensitively, bounds it at the next `## ` heading or end of document, and counts `### ` headings strictly within those bounds. Document why the bounding matters: an unbounded count would silently absorb a `### ` heading added to any other section later.
    - `const skillWorkedExampleCount = 3`, with a doc comment citing D-05 in `06-CONTEXT.md` as its only source — this is a locked user decision, not a value derivable from code, and the comment must say so rather than leave a bare literal to be mistaken for a derived one.
    - `const nudgeScriptPath = "../../.claude/hooks/session-nudge.sh"`, with a doc comment stating that this file's checks are the SECOND layer over the nudge text: `internal/agents/hookpackage_test.go` pins the emitted bytes exactly, and these checks pin the same text against the live tool roster and environment-variable name, so a future edit to the message cannot pass both layers while naming something that does not exist.
    - The four test functions described in `<behavior>`.

    Write the two new tests over the real files LAST, after their non-vacuity cases pass, and record whether each was green on first run against the already-authored files or required a correction.
  </action>
  <verify>
    <automated>go test ./internal/mcp/... -run 'TestSkill|TestNudgeText' -count=1 -v</automated>
    <automated>go test ./internal/mcp/... -count=1</automated>
    <automated>go vet ./internal/mcp/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/mcp/... -count=1` exits 0.
    - `TestSkillWorkedExampleCounterIsNotVacuous` covers at minimum the counts 0, 1, 2, 3 and 4, plus the out-of-section case and the `####` case, and every sub-case passes.
    - `TestSkillCarriesExactlyThreeWorkedExamples` is demonstrated red at BOTH boundaries against the real file: delete one example, observe the failure naming a count of 2, restore; duplicate one example, observe the failure naming a count of 4, restore byte-clean. Record both verbatim failure texts in the plan summary.
    - `TestNudgeTextNamesOnlyRealTools` fatals rather than passing when the script contains zero tool-name tokens — confirm by the guard-the-guard fatal check being present in the source.
    - The expected worked-example count appears exactly once in the file, as a documented named constant, not inline in an assertion.
    - `grep -c 'nudgeScriptPath' internal/mcp/skill_claims_drift_test.go` prints at least `3` (declaration plus both new tests reading it).
  </acceptance_criteria>
  <done>The example count and the nudge's factual claims are both gated by derived checks, and the counter is proven to discriminate at both of its boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Demonstrate every Phase-6 guard red against the real tree</name>
  <files>test/wireoracle/MUTATION-PROOF.md</files>
  <read_first>
    - `test/wireoracle/MUTATION-PROOF.md` — read the whole file, including the "A note on numbering" section 05-03 added. Determine the actual highest existing mutation number by running `grep -c '^## Mutation' test/wireoracle/MUTATION-PROOF.md` and reading the last such heading; continue from there. Do NOT assume a number from this plan's text — 05-03's summary records that exact assumption failing once already.
    - `.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-03-SUMMARY.md` — the format each mutation entry takes: the mutation applied, the command run, the verbatim failure text observed, and the byte-clean revert confirmation.
    - `internal/mcp/skill_claims_drift_test.go` and `internal/agents/hookpackage_test.go` — the guards being proven.
  </read_first>
  <action>
    Append one mutation entry per guard family this phase introduced, continuing the file's existing numbering. Each entry states the mutation, the exact command, the verbatim failure text observed, and confirmation that `git diff` was empty after revert.

    Mutations to apply, observe, and revert:
    1. Rename one registered tool in `internal/mcp/server.go`'s `companionNames` and confirm that `TestSkillNamesOnlyRealTools` goes red naming the now-unregistered token in SKILL.md — the skill's half of GUARD-02's reach.
    2. Change one `codegraph://` URI in SKILL.md to a stem absent from `resourceURIFor` and confirm `TestSkillResourceURIsResolve` goes red. Note in the entry which OTHER guards stayed green, following 05-03's asymmetry-recording discipline.
    3. Change the `resume` matcher in `.claude/hooks/hooks.json` and confirm `TestHookRegistrationMatchesFragmentAndScript` goes red naming both files. (If 06-01 already recorded this mutation in its own summary, cite that record here rather than repeating the run, and say so explicitly.)
    4. Change one character of the nudge message in `.claude/hooks/session-nudge.sh` and confirm `TestSessionNudgeBehavesPerIndexPresence` goes red on the byte-equality assertion — proving the emitted-bytes pin is not a substring check.
    5. Rename `.claude/hooks/session-nudge.sh` and confirm the command-path resolution assertion in `TestHookRegistrationMatchesFragmentAndScript` goes red — the check that a silently-disabled nudge fails the build instead of failing quietly.

    After the final revert, run the full suite on the clean tree and record the result.
  </action>
  <verify>
    <automated>go test ./... -count=1</automated>
    <automated>git status --porcelain</automated>
    <automated>grep -c '^## Mutation' test/wireoracle/MUTATION-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - Every mutation listed above was applied to the real tree, its failure observed, its verbatim failure text recorded, and the tree reverted byte-clean.
    - `git status --porcelain` shows no unintended modification after the final revert — only `test/wireoracle/MUTATION-PROOF.md` differs from the pre-task tree.
    - `grep -c '^## Mutation' test/wireoracle/MUTATION-PROOF.md` prints the pre-task count plus the number of entries actually appended, and the plan summary states both numbers explicitly.
    - The appended numbering continues from the file's real state, verified by reading it, and the summary records the starting number that was found.
    - `go test ./... -count=1` exits 0 on the reverted tree. If `internal/daemon`'s documented load-dependent flake fires, identify it as the known pre-existing condition recorded in STATE.md and re-run; do not absorb it as a new failure.
    - For mutation 2, the entry names at least one guard that stayed green, recording the asymmetry rather than only the failure.
  </acceptance_criteria>
  <done>Every gate this phase added has been shown to fire against a real change to the real tree, so its silence afterwards means something.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worked-example prose -> agent behaviour | An example an agent pattern-matches against shapes which tool it reaches for first; a wrong or stale example misroutes it |
| Historical incident record -> worked example 1 | Content is lifted from a resolved debug log describing behaviour that has since changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering | worked example 1's account of `instructions` | medium | mitigate | Task 1 requires a closing sentence stating the string has been corrected, and requires the example be written after reading the current constant at `internal/mcp/server.go` line 56. An example presenting a fixed defect as live would make the skill the next stale claim — the precise bug class this milestone retires. |
| T-06-05 | Tampering | worked-example factual claims | medium | mitigate | The 06-02 guards apply to the whole document, so the examples inherit them: tool names pinned to `allToolNames()`, URIs to `resourceURIFor`, counts to derived lengths, and no numeric default permitted at all. Task 1's acceptance criteria re-run every one of them. |
| T-06-07 | Repudiation | Phase-6 guards as a whole | high | mitigate | Task 3 applies five real mutations across the skill guard, the hook-parity guard and the nudge byte-pin, records each verbatim failure, and reverts byte-clean — this repository's standing rule that a gate is not trusted until demonstrated red. Without it the phase would ship five untested controls. |
| T-06-09 | Denial of Service | body budget under example growth | low | mitigate | `TestSkillStaysWithinBudget` runs on every package test; Task 1 states the cut order (prose from examples before the decision table) so a budget breach has a decided resolution rather than an ad-hoc one. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no external package and adds no Go module. |
</threat_model>

<verification>
- `go test ./internal/mcp/... -count=1` and `go test ./internal/agents/... -count=1` green.
- `go test ./... -count=1` green on the reverted tree at task 3's close.
- Five demonstrated-red mutations recorded in `test/wireoracle/MUTATION-PROOF.md` with verbatim failure text and byte-clean reverts.
- `task test` green at plan close.
</verification>

<success_criteria>
- SKILL-02: the skill carries exactly three worked examples, the first of which reproduces the 2026-08-08 misdirection incident end to end — question asked, misleading path taken, correct codegraph call — sourced from the incident's own record and framed as history rather than as live behaviour.
- Every guard introduced across plans 06-01, 06-02 and 06-03 has been observed failing against a real mutation and the mutation reverted byte-clean.
</success_criteria>

<output>
Create `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-03-SUMMARY.md` when done.
</output>
