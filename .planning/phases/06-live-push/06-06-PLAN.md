---
phase: 06
plan: 06
type: execute
wave: 5
depends_on: ["06-04", "06-05"]
files_modified:
  - web/scripts/live-push-multitab-check.mjs
  - web/scripts/live-push-stable-proxy.mjs
  - corpora/live-push-multitab-check.json
  - scripts/live-push-concurrency-check.sh
  - scripts/live-push-probe.go
  - corpora/live-push-concurrency-check.json
  - web/build
autonomous: true
requirements: [LIV-01, LIV-03]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Three or more real browser tabs each receive EVERY triggered generation, proven by a per-tab received-generation set, so the inter-arrival timing bound cannot hold vacuously over a single receipt."
    - "A deliberately slow tab does not delay the others, and the healthy tabs' timing is measured while it is verifiably blocked."
    - "A dropped connection reconnects with a growing, jittered delay — not a reconnect storm — against a STABLE origin, so the tabs can actually reach the restarted server."
    - "With a real daemon and a real MCP server running against the same store, repeated real re-index flushes all complete while a live-push session is open, and flush duration is bounded against a measured no-UI baseline rather than against nothing."
    - "The lifecycle-bookkeeping verdict against this repository's prior counter-corruption bug is recorded with evidence, not asserted."
    - "The committed app bundle matches the phase's web sources, and NOTHING under web/ is left unstaged or uncommitted after the bundle commit."
  artifacts:
    - web/scripts/live-push-multitab-check.mjs
    - web/scripts/live-push-stable-proxy.mjs
    - corpora/live-push-multitab-check.json
    - scripts/live-push-concurrency-check.sh
    - scripts/live-push-probe.go
    - corpora/live-push-concurrency-check.json
  key_links:
    - "real daemon writer -> shared Pebble exclusive directory lock -> UI publisher's open/read/close -> live-push session stays open without denying the writer"
    - "browser tabs -> fixed-port loopback proxy (Host/Origin rewritten to the upstream authority) -> restartable `codegraph ui` on an ephemeral port"
---

<objective>
Close the two criteria no ordinary test can satisfy, and leave the repository consistent.

Purpose: the ROADMAP calls criterion 5 non-negotiable and explains why — the property it must
not violate is only observable under genuine concurrent multi-process use, and running
`codegraph ui` alone is the dev workflow that hides it. `pebble.Open` takes an EXCLUSIVE
directory lock for the store's whole open lifetime, so a UI that holds the store does not
slow a concurrent re-index; it denies it. A stub is explicitly disallowed, precisely because
a stub is the shape that would pass while the property is violated.

Output: two committed real-world gates, the recorded lifecycle verdict, and a correctly
staged bundle.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-push/06-CONTEXT.md
@.planning/phases/06-live-push/06-VALIDATION.md
@.planning/phases/06-live-push/06-04-SUMMARY.md
@.planning/phases/06-live-push/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three real tabs — per-message timing, fan-out isolation, and a jittered reconnect against a stable origin</name>
  <files>web/scripts/live-push-multitab-check.mjs, web/scripts/live-push-stable-proxy.mjs, corpora/live-push-multitab-check.json</files>
  <read_first>
    - web/scripts/graph-collapse-affordance-check.mjs lines 1-80 and 290-335 — the established script shape and the write-a-record-on-every-run convention.
    - .planning/phases/06-live-push/06-VALIDATION.md — the "Criterion 2 — assert per-message TIMING, not arrival" block.
    - web/src/lib/live/live-client.ts and web/src/lib/live/live-store.ts (from 06-03) — the client whose reconnect behaviour this exercises for real, including its connection-epoch rule.
    - internal/cli/ui.go lines 24-33 and 44-84 in full — the doc comment stating there is "no flag for a bind address, port, hostname, token or any other credential", the "binds an ephemeral loopback port" text, `uiserver.Listen(uiserver.Options{RepoPath: start})` with no `Addr`, the URL printed to stdout before `Serve` blocks, and the only two registered flags (`--path`/`-p`, `--no-open`).
    - internal/uiserver/originguard.go lines 42-67 — the guard the proxy must satisfy: `Host` must be in the allowlist for the UPSTREAM's port, AND `Origin`, when present, must equal `"http://" + r.Host`. Membership alone is not enough; the pairing is the whole point.
  </read_first>
  <precondition>A `codegraph` binary is built (`task build`), chromium is installed for Playwright, and a small indexed repository is available to point `codegraph ui` at (this repository's own `.codegraph` index is sufficient — the corpus scale is 06-05's concern, not this one).</precondition>
  <action>
    ## First: a stable origin, because `codegraph ui` has no port flag

    `web/scripts/live-push-stable-proxy.mjs` — a tiny loopback reverse proxy, started ONCE and
    kept alive for the whole run, in front of a `codegraph ui` that may be stopped and
    restarted behind it.

    This exists because the previous draft's RECONNECT scenario could not pass. `codegraph ui`
    binds an ephemeral loopback port and exposes no bind/port flag (`internal/cli/ui.go:26`,
    `:40`, `:81-82` — the only flags are `--path` and `--no-open`). Stop and restart it and
    the tabs are pointed at a dead URL forever; their reconnect loop can never reach the new
    process, so "every tab is receiving events again afterwards" is unsatisfiable. Adding a
    port flag is NOT the fix — that changes `SRV-01`/`SRV-03`'s shipped command surface and
    reopens D-08, for a test harness. The proxy changes nothing that ships.

    Requirements on the proxy, each load-bearing:
    - Bind once on loopback. The port may be ephemeral; what matters is that it never changes,
      so the tabs' ORIGIN survives the upstream restart.
    - Forward to a MUTABLE `upstreamURL`, updated when the UI is restarted. Expose that update
      to the check script (a module export, or a tiny control endpoint on the same listener).
    - REWRITE `Host` and `Origin` to the upstream's own authority before forwarding. The
      upstream's exact-match guard (`originguard.go:42-67`) requires `Host` to be in the
      allowlist for the UPSTREAM's port AND `Origin == "http://" + Host`; forwarding the
      proxy's own authority is rejected with 403, and rewriting only one of the two is
      rejected by the pairing check. Rewrite both, to the same spelling.
    - STREAM the response. `res.writeHead` from the upstream's status and headers, then pipe —
      no buffering middleware, no response accumulation. This is not merely for latency: the
      FAN-OUT scenario below runs through the proxy and would DEADLOCK on a buffering proxy at
      its "wait until every tab received generation k" step, which is exactly the desired
      failure mode. The proxy is therefore self-policing and needs no separate assertion.
    - Return a 502 and destroy the socket when the upstream is down, so the browser sees a
      genuine stream failure and the client's own backoff engages.

    ## Then the check script

    Write `web/scripts/live-push-multitab-check.mjs` following the established script shape:
    `repoRoot()`, `pollUntil`, real navigation and real input only, and a diagnostic JSON record
    written to `corpora/live-push-multitab-check.json` on every run including failure. Use
    `try/finally` so the record is written and every process is killed on every exit path.

    Start the proxy, then `codegraph ui --no-open` against the target repository, read its
    printed URL, point the proxy at it. Open at least THREE real tabs in one browser context
    ON THE PROXY'S URL, each on a route that subscribes to live push.

    Instrument each tab to record, per event, the generation and a client-side receipt
    timestamp. Note that each tab's FIRST event is the seeded current state (`06-02`'s
    `Subscribe` contract) and is not a triggered generation — record it separately as
    `seedGenerationPerTab` and exclude it from the triggered accounting below.

    Then run three scenarios and record evidence for each:

    FAN-OUT AND TIMING. Trigger at least three REAL re-indexes, spaced deliberately, waiting
    each time until every tab has received that generation before triggering the next. Record
    `triggeredGenerations` (the array of generation values the triggers produced) and
    `receivedGenerationsPerTab` (per tab, the array of generations received after the seed).
    Every tab's received set must be a SUPERSET of `triggeredGenerations`, and the recorded
    inter-arrival gap between consecutive TRIGGERED receipts, per tab, must be at least the
    trigger spacing. A test asserting only that N messages arrived is vacuous here — silent
    buffering delivers all N at the end and passes — and so is an inter-arrival MINIMUM
    computed over a single receipt, which is vacuously satisfied. The superset check is what
    pairs the timing bound with a real receipt floor; the blocking structure is what makes
    buffering deadlock rather than pass.

    FAN-OUT ISOLATION (this replaces the previous "BACKPRESSURE" scenario — see below). Occupy
    one tab's main thread for longer than the trigger interval, keep the other two normal, and
    trigger several re-indexes. Record: `blockedTabBlockedMs` (the measured block duration),
    `blockedTabReceiptsDuringBlock` (which MUST be 0 — the proof the tab really was blocked, not
    merely believed to be), the two healthy tabs' per-generation receipt latencies during the
    block, and the blocked tab's generation on recovery. Assert the healthy tabs received every
    triggered generation with their inter-arrival timing still bounded by the trigger spacing,
    and that the recovered tab is at the NEWEST generation.

    **This scenario deliberately does NOT claim to prove server-side backpressure, and must not
    be written as though it does.** Blocking a tab's main thread does not stop the browser and
    the kernel from draining a handful of tiny Connect envelopes into their own buffers, so the
    slow tab may well receive every intermediate generation when it resumes — meaning
    `slowTabFinalGeneration === newestGeneration` passes whether or not the publisher's
    capacity-1 coalescing channel ever engaged. Real transport pressure at re-index pacing
    (seconds apart, ~40-byte messages) cannot be manufactured in a browser: the send loop always
    wins. The coalescing claim is therefore carried where it CAN be made deterministic and
    non-vacuous — `06-04`'s `TestWatchGraphHandlerCoalescesForANonReadingClient`, which opens a
    real stream over real HTTP/1.1, stops receiving, publishes 2000 generations, and asserts
    `received < published` with the newest surviving. Record that division of labour in the
    record as `coalescingProvenBy` and repeat it in the SUMMARY, naming that test. What this
    browser scenario proves — fan-out isolation, that a stalled tab does not degrade healthy
    ones — is genuinely what T-06-34 is about and is genuinely only observable here.

    RECONNECT. Stop the `codegraph ui` process, start a NEW one against the same repository,
    and point the proxy at the new upstream — the tabs' origin never moves. Record each tab's
    reconnect attempt timestamps and the raw delay arrays. Assert every tab is receiving events
    again afterwards. Because the restarted publisher's generation counter begins again at 1,
    the post-reconnect receipts will carry LOWER generation values than the pre-restart ones;
    that is expected and is exactly what `06-03`'s connection-epoch rule exists to handle — a
    tab that applies nothing after the restart is a FAILURE of that rule, so assert each tab
    applied at least one post-reconnect event. Record `resumeCursorSentPerTab` (the
    `since_generation` each tab sent on its reconnect request) and assert it is greater than
    zero — the client resumed from what it had seen rather than restarting from scratch.

    Record the per-tab attempt-count and total-attempt figures explicitly: a reconnect storm is a
    high attempt count in a short window, so the record must carry the number, not a verdict.
    Record the raw delay arrays too — the verify command RECOMPUTES growth and cross-tab
    distinctness from them rather than reading a self-reported boolean.
  </action>
  <verify>
    <automated>node web/scripts/live-push-multitab-check.mjs; node -e 'const r=require("./corpora/live-push-multitab-check.json"); const supersetOK=r.receivedGenerationsPerTab.every(g=>r.triggeredGenerations.every(t=>g.includes(t))); const grew=r.reconnectDelaysPerTab.every(d=>d.length>=2 && d.every((v,i)=>i===0||v>d[i-1])); const distinct=new Set(r.reconnectDelaysPerTab.map(d=>d[0])).size===r.reconnectDelaysPerTab.length; console.log(JSON.stringify({tabs:r.tabCount,triggered:r.triggeredGenerations,receivedPerTab:r.receivedGenerationsPerTab.map(g=>g.length),supersetOK,minGapMs:r.minInterArrivalMs,triggerSpacingMs:r.triggerSpacingMs,blockedMs:r.blockedTabBlockedMs,blockedDuringBlock:r.blockedTabReceiptsDuringBlock,slowTabFinalGen:r.slowTabFinalGeneration,newestGen:r.newestGeneration,reconnectAttempts:r.reconnectAttemptsPerTab,postReconnectApplied:r.postReconnectAppliedPerTab,resumeCursors:r.resumeCursorSentPerTab,delays:r.reconnectDelaysPerTab,grew,distinct,pageErrors:r.pageErrorCount,success:r.success})); const fail=[]; if(r.success!==true)fail.push("scriptReportedFailure"); if(!(r.tabCount>=3))fail.push("tabCount"); if(!(r.triggeredGenerations.length>=3))fail.push("generationsTriggered"); if(!supersetOK)fail.push("aTabMissedATriggeredGeneration"); if(!(r.minInterArrivalMs>=r.triggerSpacingMs))fail.push("interArrival"); if(!(r.blockedTabBlockedMs>=r.triggerSpacingMs))fail.push("blockedTabNotActuallyBlocked"); if(r.blockedTabReceiptsDuringBlock!==0)fail.push("blockedTabWasNotBlocked"); if(r.slowTabFinalGeneration!==r.newestGeneration)fail.push("slowTabNotNewest"); if(!grew)fail.push("backoffNotGrowing"); if(!distinct)fail.push("noJitter"); if(!(Math.max(...r.reconnectAttemptsPerTab)<=6))fail.push("reconnectStorm"); if(!(Math.min(...r.reconnectAttemptsPerTab)>=1))fail.push("neverReconnected"); if(!(Math.min(...r.postReconnectAppliedPerTab)>=1))fail.push("tabDeafAfterRestart"); if(!(Math.min(...r.resumeCursorSentPerTab)>0))fail.push("resumeCursorNotAdvanced"); if(r.pageErrorCount!==0)fail.push("pageErrors"); if(fail.length){console.error("FAIL: "+fail.join(","));process.exit(1)} console.log("PASS")'</automated>
  </verify>
  <acceptance_criteria>
    - `corpora/live-push-multitab-check.json` is committed and carries every field the verify command reads — including `triggeredGenerations`, `receivedGenerationsPerTab`, `seedGenerationPerTab`, `blockedTabBlockedMs`, `blockedTabReceiptsDuringBlock`, `reconnectDelaysPerTab`, `postReconnectAppliedPerTab`, `resumeCursorSentPerTab`, `coalescingProvenBy` — plus `pageErrorCount`, `pageErrors`, `browserIdentity` and `success`.
    - The script invocation is chained with `;`, not `&&`, so the numbers are printed on the RED path too. All load-bearing values print BEFORE any comparison, and every failed check is reported by name rather than the first.
    - **Criterion 2's browser-side pairing is MECHANIZED, not prose.** `supersetOK` is computed in the verify command from `receivedGenerationsPerTab` and `triggeredGenerations`: every tab must have received every triggered generation. Without it, `minInterArrivalMs >= triggerSpacingMs` is vacuously satisfied by a single receipt per tab — an inter-arrival minimum over one message has no consecutive pair to bound, which is exactly the vacuity this plan's own prose calls out. The earlier draft asserted the pairing in prose while the verify command read nine fields, none of which was a per-tab receipt count.
    - `tabCount` is at least 3 and `triggeredGenerations.length` is at least 3.
    - The blocked tab is PROVEN blocked before its isolation result is believed: `blockedTabBlockedMs >= triggerSpacingMs` AND `blockedTabReceiptsDuringBlock === 0`. Without both, "the healthy tabs were unaffected" is a statement about three healthy tabs.
    - The reconnect attempt count is bounded ABOVE (at most 6 per tab, which a storm would exceed) AND bounded BELOW (at least 1 per tab, so "no storm" cannot be satisfied by never having reconnected).
    - **Backoff growth and cross-tab jitter are RECOMPUTED from the raw delay arrays** by the verify command, not read as self-reported booleans. `reconnectDelaysPerTab` must have at least 2 entries per tab and be strictly increasing; the tabs' first delays must be pairwise distinct. A boolean the script computes about itself is the script grading its own homework.
    - **Every tab applied at least one event after the reconnect** (`postReconnectAppliedPerTab`). The restarted publisher's generations begin again at 1, so this is the live proof of `06-03`'s connection-epoch rule; under a bare monotonic generation gate every tab would be permanently deaf here and this check is what catches it.
    - `resumeCursorSentPerTab` is greater than zero for every tab — the client resumed from its last-seen generation rather than restarting from scratch.
    - The slow tab's final generation equals the newest. The record carries `coalescingProvenBy` naming `06-04`'s `TestWatchGraphHandlerCoalescesForANonReadingClient`, because a skipped-generation count observed in a browser is not evidence of server-side coalescing either way — see the action block.
    - The record is written on failure too: run once with the proxy's upstream deliberately never repointed after the restart, confirm the reconnect section produces `success: false` with a stated reason, and record that observation.
  </acceptance_criteria>
  <done>Three real tabs, real timing paired with a real per-tab receipt set, real fan-out isolation, and a real reconnect against an origin that survives the restart — all measured and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Criterion 5 — a real daemon and a real MCP server against the same store, not a stub</name>
  <files>scripts/live-push-concurrency-check.sh, scripts/live-push-probe.go, corpora/live-push-concurrency-check.json</files>
  <read_first>
    - internal/graphstore/pebble_store.go lines 67-160 — the exclusive directory lock, `openLockRetryAttempts` (5) times `openLockRetryBackoff` (100ms), `ErrStoreLocked` and `classifyOpenError`. This is the mechanism criterion 5 is about.
    - internal/cli/daemon.go lines 110-140 — `newDaemonStartCmd`'s own doc comment: *"the explicit **foreground** blocking watch/index server"*. It does NOT daemonize; the script must background it itself and retain the PID.
    - internal/cli/serve.go lines 172-245 — `codegraph serve --mcp`: *"Run the codegraph MCP server over **stdio**"* (`:179`). There is no socket and no port. Holding it open and later issuing a real request needs persistent bidirectional pipe management, not a one-shot shell invocation.
    - internal/cli/ui.go lines 44-84 — `codegraph ui` prints its URL and runs in the foreground with no lifecycle shared with the daemon.
    - internal/cli/index.go and internal/cli/sync.go — how a re-index is triggered from the command line.
    - internal/uiproto/uiv1/uiv1connect/ui.connect.go — the generated `NewUIServiceClient` constructor and `UIServiceWatchGraphProcedure`, which the probe program below uses directly.
    - .planning/phases/06-live-push/06-CONTEXT.md — the criterion-5 block and its binding constraint on D-01's implementation.
  </read_first>
  <precondition>A `codegraph` binary is built (`task build`) and a scratch git checkout with source files that can be modified repeatedly is available for the three processes to share. Do not run this against this repository's own working tree.</precondition>
  <action>
    Write `scripts/live-push-concurrency-check.sh` — a real-process gate, never a stub. The
    criterion says so explicitly, and a stub is precisely the shape that would pass while the
    property is violated.

    ## The probe program — because curl and a one-shot shell cannot do this

    Write `scripts/live-push-probe.go`, a single Go program carrying `//go:build ignore` on its
    first line so `go build ./...` and `go vet ./...` skip it entirely while `go run
    scripts/live-push-probe.go ...` still compiles and runs it in module context. Verified this
    session in a scratch module: `go build ./...` and `go vet ./...` both ignored such a file,
    and `go run` on it printed its output. No new dependency is introduced — it uses the
    generated `uiv1connect` client and `connectrpc.com/connect`, both already in `go.mod`.

    Two subcommands, both of which the previous draft could not express:

    `stream --url <base> --out <file> --seconds N`
      Opens `WatchGraph` with the real generated Connect client over plain HTTP/1.1 against
      the UI's ephemeral base URL, and appends one JSON line per received event carrying the
      generation and a receipt timestamp. Raw `curl` CANNOT do this: Connect streaming frames
      each message in a length-prefixed envelope, so counting messages or reading generations
      out of a curl body requires decoding those envelopes — a curl invocation with the right
      Host and Origin headers proves a connection was accepted and nothing about what arrived.
      The generated client already handles framing, the origin/host pairing (it sends the base
      URL's own authority as both) and incremental delivery.

    `mcp --path <repo> --seconds N`
      Spawns `codegraph serve --mcp --path <repo>` with stdin and stdout PIPES held open,
      performs the MCP `initialize` handshake, then keeps the pipes open for N seconds — so
      this is genuinely one of the three concurrent processes for the whole run, not a
      process started at the end to answer one question. After N seconds it issues ONE real
      tool call, validates the response, prints a JSON verdict and exits. `serve --mcp` is
      stdio-only (`internal/cli/serve.go:179`), so there is no socket to poll and a one-shot
      invocation would neither hold the store open during the run nor prove liveness after it.

    ## The gate script

    Set up a scratch repository, index it once, and then run all three real processes against
    that one store simultaneously:
    - `codegraph daemon start` on that path (the writer). It is a FOREGROUND process
      (`internal/cli/daemon.go:113-124` — "the explicit foreground blocking watch/index
      server"), so background it with `&`, capture `$!`, and keep that PID for the liveness
      check and the trap. Do not assume `daemon start` daemonizes; it does not.
    - `go run scripts/live-push-probe.go mcp --path <scratch> --seconds N &` (a second real
      reader whose own startup performs a real reconcile sync against the same store),
    - `codegraph ui --no-open --path <scratch>` on that path, backgrounded, its printed URL
      read from its stdout,
    - and `go run scripts/live-push-probe.go stream --url <that URL> --out <file> --seconds N &`
      holding a live-push session OPEN for the whole run.

    **Measure the no-UI BASELINE first.** Before starting the UI and the stream probe, drive
    the same five real re-index flushes with only the daemon running, and record
    `baselineMaxFlushDurationMs`. Without it `maxFlushDurationMs` is recorded and bounded by
    nothing, and a severely degraded but eventually-completing flush scores a clean pass —
    which is precisely the "slowed, not denied" half of criterion 5 that a bare completion
    count cannot see.

    Then drive at least FIVE real re-index flushes with all three processes running: modify a
    source file in the scratch repository, wait for the daemon's own debounce, and confirm the
    index actually moved (`last_sync_unix_ms` advanced).

    **Define "starved" operationally, not as a feeling.** A flush counts as STARVED when
    either (a) the daemon's own output for that flush reports a store-lock failure — match on
    `ErrStoreLocked`'s user-visible text or the daemon's lock-contention message, and record the
    matched line verbatim — or (b) the flush did not complete within `flushTimeoutMs`. Record
    the timeout value used and, for every flush, its measured duration and whether either
    condition fired. "Whether any store-lock contention was reported" without a rule for
    reading it is not a definition.

    Record to `corpora/live-push-concurrency-check.json`: `flushesAttempted`,
    `flushesCompleted`, `flushesStarved`, `starvationEvidence` (the matched lines, empty when
    none), `flushDurationsMs` (the array), `maxFlushDurationMs`, `baselineMaxFlushDurationMs`,
    `flushTimeoutMs`, `liveEventsReceived`, `liveEventsExcludingSeed`, `sessionHeldSeconds`,
    `mcpAliveAfterRun`, `mcpToolCallResponse`, `daemonAliveAfterRun`, `uiAliveAfterRun`, the
    three process identities and PIDs, and `success`. Write it on every run including failure.

    Pass condition, all together: `flushesStarved` is 0; `flushesCompleted` equals
    `flushesAttempted` and is at least 5; `liveEventsExcludingSeed` is at least 5 (the session
    stayed live throughout, so "nothing was starved" cannot be satisfied by nothing having
    happened — and the seeded first message is excluded so the floor counts real change events);
    `maxFlushDurationMs` is within a stated multiple of `baselineMaxFlushDurationMs`; and all
    three processes are still alive at the end.

    Clean up every process and the scratch directory on exit, including on failure, with a trap
    covering EXIT, INT and TERM.

    Finally, demonstrate the gate can go RED: run it once with the publisher deliberately holding
    the store open across the session (a temporary local edit, reverted immediately), confirm
    `flushesStarved` is non-zero, and record that observation. A gate never seen red is a gate
    that proves nothing — this repository's own recorded lesson, confirmed six times last phase.
  </action>
  <verify>
    <automated>bash scripts/live-push-concurrency-check.sh; node -e 'const r=require("./corpora/live-push-concurrency-check.json"); console.log(JSON.stringify({attempted:r.flushesAttempted,completed:r.flushesCompleted,starved:r.flushesStarved,evidence:r.starvationEvidence,durationsMs:r.flushDurationsMs,maxMs:r.maxFlushDurationMs,baselineMaxMs:r.baselineMaxFlushDurationMs,events:r.liveEventsExcludingSeed,mcp:r.mcpAliveAfterRun,daemon:r.daemonAliveAfterRun,ui:r.uiAliveAfterRun,success:r.success})); const fail=[]; if(r.success!==true)fail.push("scriptReportedFailure"); if(!(r.flushesAttempted>=5))fail.push("tooFewFlushes"); if(r.flushesCompleted!==r.flushesAttempted)fail.push("flushIncomplete"); if(r.flushesStarved!==0)fail.push("flushStarved"); if(!(r.liveEventsExcludingSeed>=5))fail.push("sessionNotLive"); if(!(r.baselineMaxFlushDurationMs>=1))fail.push("noBaselineMeasured"); if(!(r.maxFlushDurationMs>=1))fail.push("noDurationsMeasured"); if(!(r.maxFlushDurationMs<=3*r.baselineMaxFlushDurationMs+1000))fail.push("flushDegraded:"+r.maxFlushDurationMs+"vs"+r.baselineMaxFlushDurationMs); if(r.mcpAliveAfterRun!==true)fail.push("mcpDied"); if(typeof r.mcpToolCallResponse!=="string"||r.mcpToolCallResponse.length===0)fail.push("mcpAnsweredNothing"); if(r.daemonAliveAfterRun!==true)fail.push("daemonDied"); if(r.uiAliveAfterRun!==true)fail.push("uiDied"); if(fail.length){console.error("FAIL: "+fail.join(","));process.exit(1)} console.log("PASS")'</automated>
  </verify>
  <acceptance_criteria>
    - `corpora/live-push-concurrency-check.json` is committed with every field above.
    - The script invocation is chained with `;`, not `&&`, so the numbers print on the RED path too. All load-bearing values print BEFORE comparison, and every failed check is named.
    - `flushesStarved` is 0 AND `flushesCompleted` equals `flushesAttempted` with a floor of 5 — the zero is paired with a non-zero floor on work actually attempted. `starvationEvidence` carries the matched daemon output lines, so a zero is auditable rather than a bare claim.
    - **`maxFlushDurationMs` is BOUNDED, against a measured baseline rather than against nothing:** at most `3 * baselineMaxFlushDurationMs + 1000`, with both values floored at 1ms so "within bounds" cannot be satisfied by never having measured a duration. Criterion 5 forbids starving a sync, and a flush that completes ten times slower than it does without the UI is a starved sync that happened to finish. Completion counts alone cannot see that.
    - `liveEventsExcludingSeed` is at least 5 — the live-push session was genuinely open and delivering real change events throughout, so the starvation-free result is not the trivial result of no session existing. The seeded first message is excluded so the floor cannot be met by the connect-time snapshot.
    - All three of `daemonAliveAfterRun`, `mcpAliveAfterRun`, `uiAliveAfterRun` are true, and the MCP liveness is proven by a real tool-call RESPONSE recorded as a non-empty string, not by a process-table check and not by a boolean the script sets about itself.
    - The script uses the real binary for all three processes; no in-process test double appears anywhere in it. The stream probe and MCP harness are real client programs built from this module's own generated code, driven by `go run` on a `//go:build ignore` file so they never enter the shipped build.
    - The SUMMARY records the observed RED run (publisher holding the store open) with its non-zero `flushesStarved` and the matched `starvationEvidence` lines, as evidence the gate discriminates.
  </acceptance_criteria>
  <done>Under three real concurrent processes, five real re-index flushes complete while a live-push session stays open — and the gate has been seen to fail when it should.</done>
</task>

<task type="auto">
  <name>Task 3: Record the lifecycle verdict, rebuild the bundle, and leave the tree consistent</name>
  <files>web/build</files>
  <read_first>
    - internal/mcp/server.go lines 430-541 — the `pendingWriter` counter, its client-initiated meaning, and the server-initiated notification path that once corrupted it.
    - internal/uiserver/livepublish.go and internal/uiserver/livehandler.go as they now stand — the two separated counters and the handler's per-request subscriber registration.
    - Taskfile.yml lines 649-720 (`web:build` and the build-manifest marker it writes inside `web/build/`) and lines 1097 onward (`web:drift` and its two digests).
    - .planning/phases/06-live-push/06-CONTEXT.md — the "Criterion 3's recorded verdict" block and carried-forward item WINDOWS 29.
  </read_first>
  <action>
    First, establish and record the verdict LIV-03 requires. It has two halves and both belong
    in the SUMMARY as evidence, not as reassurance:

    HALF ONE — no pre-existing analogue. Enumerate every non-test Go file in `internal/uiserver`
    and search for the counter shape the earlier bug had (an in-flight or pending count of
    client-initiated work). Report the count. Pair it with a POSITIVE CONTROL: run the identical
    search against `internal/mcp/server.go`, which demonstrably HAS such a counter, and report
    that count too. A zero with no control is indistinguishable from a search that matched
    nothing because it was pointed at the wrong place — this repository has been bitten by
    exactly that four times. Put both numbers, the exact command and its output in the SUMMARY.

    Make the control DISCRIMINATE on the counter's shape, not on the word. `rg -c 'pendingWriter'
    internal/mcp/server.go` returns 9 today, and six of those are comments or bare type
    references (`:219`, `:282`, `:487`, `:498` are prose; `:237`, `:493`, `:540` are uses). It
    proves the file contains the word — not that the search used for the no-analogue verdict
    can tell a counter from a mention. Target the declaration instead:
    `rg -c 'type pendingWriter struct' internal/mcp/server.go` is exactly 1 (verified this
    session at `internal/mcp/server.go:466`), and report the mutation site alongside it.

    HALF TWO — the new code cannot reintroduce it. State the structural fact and name the test
    that pins it: the publisher's server-initiated send accounting and the registry's
    client-initiated subscriber count are separate values that never share storage, asserted in
    both directions by the counter-separation test written in `06-02`. Quote the test name and
    its passing output.

    Second, rebuild and stage the bundle. This phase changed web sources across `06-01`
    (regenerated client and the generated-client type assertion), `06-03` (live client, store,
    status, routes) and `06-05` (graph), and the bundle rebuild was deliberately deferred to
    here so it happens exactly once. Run `task web:build`, then `task web:drift`.

    The drift gate CANNOT detect an incompletely-staged bundle (WINDOWS 29): its output half
    enumerates the filesystem while its source half enumerates tracked files, so a
    partially-staged `web/build` passes it. Do not rely on it for staging. That reasoning is
    correct and stands.

    **But the assertion goes AFTER the commit, not after the `git add`.** The previously
    specified form —

    ```sh
    git add web/build && test -z "$(git status --porcelain web/build)"
    ```

    — is mechanically inverted. Reproduced in a scratch repository this session: with a real
    change under `web/build`, after `git add` the porcelain prints `M  web/build/a.txt` and
    `test -z` FAILS; after the commit the porcelain is empty and it PASSES. So as placed it
    fails precisely when the rebuilt bundle differs — the only case it exists to cover — and
    passes only when nothing changed at all, where it proves nothing about staging.

    The correct sequence: `git add -A web/build`, COMMIT, then assert
    `git status --porcelain -- web/build` is empty. The verify command performs the commit
    itself, guarded by `git diff --cached --quiet -- web/build ||` so it is idempotent — if
    the executor already committed the bundle as part of this task, the guard short-circuits
    and the post-commit assertion still runs. A bare `git commit` in a verify chain would fail
    with "nothing to commit" on the second run and turn a green tree red. Verified in the same
    scratch repository:
    a full stage then commit → porcelain empty → PASS; staging only ONE of two changed files
    then committing → porcelain prints ` M web/build/b.txt` → FAIL. The partial stage is
    caught, which is exactly WINDOWS 29's concern, and the gate no longer punishes a bundle
    for having changed.

    Pair it with a positive control so an empty porcelain cannot come from a wrong or
    untracked path: `git ls-files --error-unmatch web/build/.build-manifest` must succeed.
    That marker is written INSIDE `web/build/` by `task web:build` (Taskfile.yml `web:build`,
    deliberately inside so a rebuild that forgets it leaves it absent), so it is both the
    proof that the path is tracked and the proof that a rebuild actually happened. Verified
    this session: the control correctly fails against a bogus path.

    **Demonstrate the corrected gate RED before accepting it green.** Stage only part of
    `web/build`, commit, observe the porcelain assertion fail, then reset and do it properly.
    Record that observation in the SUMMARY — a staging gate never seen red is how the previous
    form survived review in the first place.

    Third, leave the tree consistent: full Go suite, full web suite, proto drift, and both
    real-world records from Tasks 1 and 2 committed.

    Do not use a commit message that skips CI under any spelling — the protected branch requires
    all six checks and a skipped head blocks the pull request permanently.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.5 task test:unit && task web:test && task proto:drift && task web:build && task web:drift && git add -A web/build && { git diff --cached --quiet -- web/build || git commit -m "build(06): rebuild the app bundle for the live-push phase" -- web/build; } && test -z "$(git status --porcelain -- web/build)" && git ls-files --error-unmatch web/build/.build-manifest && test "$(rg -c 'type pendingWriter struct' internal/mcp/server.go || echo 0)" -eq 1 && test -f corpora/live-push-multitab-check.json && test -f corpora/live-push-concurrency-check.json && test -f corpora/graph-live-update-check.json && echo "ALL GATES GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `task test:unit`, `task web:test`, `task proto:drift`, `task web:build` and `task web:drift` all exit 0. (`test:unit` deliberately excludes `internal/daemon`, whose watchdog test is timing-sensitive under parallel load; that exclusion is intentional and must not be changed.)
    - **`git status --porcelain -- web/build` is empty AFTER the commit, not after the `git add`.** This is the direct staging assertion WINDOWS 29 calls for, in the only placement where it works. Verified in a scratch repository this session: after `git add` with a real bundle change the porcelain prints `M  web/build/a.txt` and the emptiness test FAILS; after the commit it is empty and PASSES; and with only one of two changed files staged before committing it prints ` M web/build/b.txt` and FAILS — the partial stage is caught.
    - The staging assertion is paired with `git ls-files --error-unmatch web/build/.build-manifest`, which succeeds only if that path is tracked, so an empty porcelain cannot be the result of a mistyped or untracked pathspec. Verified: the control fails against a bogus path.
    - The RED demonstration of the staging gate was performed (partial stage, commit, observe failure, reset) and is recorded in the SUMMARY.
    - The positive control for the verdict search is proven live AND discriminating: `rg -c 'type pendingWriter struct' internal/mcp/server.go` is exactly 1 — the declaration, not the word. The previous `rg -c 'pendingWriter'` form returns 9, counting comments and bare type references, and so proved only that the file contains the string. A zero-count claim about `internal/uiserver` is only meaningful alongside a control that discriminates on the shape being searched for.
    - All three committed diagnostic records exist: the multi-tab record, the concurrency record, and the graph displacement record from `06-05`.
    - The SUMMARY contains: both halves of the verdict with the exact commands, both counts, and the named passing test; the FIVE RED observations from this phase — `06-04` Task 1 (middleware removed), `06-04` Task 3 (buffering send loop vs the tracer), `06-05` Task 3 (write-back disabled), `06-06` Task 1 (proxy upstream never repointed) and `06-06` Task 2 (store held open) — plus this task's own staging RED; the read-deadline verdict carried forward from `06-04`'s SUMMARY; the `coalescingProvenBy` division of labour between the browser fan-out check and `06-04`'s non-reading-client test; and an explicit statement of which of the five phase criteria each artifact satisfies.
    - No commit in this plan carries a CI-skipping directive in any spelling.
  </acceptance_criteria>
  <done>The verdict is recorded with evidence, the bundle matches the sources, nothing under web/ is left uncommitted, and every gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| three concurrent processes -> one Pebble store | Cross-process exclusive directory lock; the failure mode is denial, not slowdown |
| committed bundle -> shipped binary | The embedded app is served from the committed build output |
| dependency manifests -> build | Supply chain surface for the phase as a whole |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-32 | Denial of Service | UI live push starving a concurrent daemon sync | critical | mitigate | Task 2's real three-process gate, with a zero-starvation result paired with a floor of 5 completed flushes and at least 5 live events delivered, and demonstrated red when the store is deliberately held open. |
| T-06-33 | Denial of Service | reconnect storm across many tabs after a restart | high | mitigate | Task 1 bounds reconnect attempts per tab ABOVE and BELOW, and the verify command RECOMPUTES delay growth and cross-tab distinctness from the committed raw arrays rather than reading a boolean the script set about itself. The tabs reach the restarted server at all because the harness keeps a fixed-port proxy in front of the ephemeral `codegraph ui`. |
| T-06-34 | Denial of Service | a stalled tab degrading the others | high | mitigate | Task 1's FAN-OUT ISOLATION scenario measures the two healthy tabs' per-generation timing while the third is PROVEN blocked (`blockedTabBlockedMs >= triggerSpacingMs` and `blockedTabReceiptsDuringBlock === 0`), so "the healthy tabs were unaffected" is not a statement about three healthy tabs. The server-side coalescing claim is carried by `06-04`'s `TestWatchGraphHandlerCoalescesForANonReadingClient` and recorded here as `coalescingProvenBy` — a browser cannot manufacture transport pressure at re-index pacing. |
| T-06-35 | Tampering | server-initiated sends corrupting client-initiated state | high | mitigate | The recorded verdict, in both halves: no pre-existing analogue (with a live positive control that discriminates on `type pendingWriter struct`, not on the bare word) and a named passing test pinning the separation in both directions. |
| T-06-36 | Tampering | a partially staged app bundle shipping stale UI | high | mitigate | `git add -A web/build`, COMMIT, then assert `git status --porcelain -- web/build` is empty — the assertion placed AFTER the commit, where an unstaged remnant is still visible. Paired with `git ls-files --error-unmatch web/build/.build-manifest` so an empty porcelain cannot come from an untracked pathspec. The drift gate's asymmetric enumeration (WINDOWS 29) cannot detect this case, and the pre-commit placement of the same assertion was mechanically inverted — it failed whenever the bundle changed. Both directions reproduced in a scratch repository. |
| T-06-37 | Tampering | the concurrency script leaving stray processes or scratch state | medium | mitigate | A trap kills all three processes and removes the scratch directory on every exit path, including failure; process liveness is recorded before teardown. |
| T-06-38 | Denial of Service | protected-branch checks skipped by a commit directive | medium | mitigate | No CI-skipping directive in any commit message; the protected branch requires all six checks and a skipped head blocks the pull request permanently. |
| T-06-45 | Spoofing | the test harness's fixed-port proxy weakening the origin/host guard it forwards to | medium | mitigate | The proxy is a test-only loopback process in `web/scripts/`, never built into or shipped with the binary, and it strengthens rather than bypasses the guard under test: it rewrites BOTH `Host` and `Origin` to the upstream's own authority, so the upstream's exact-match membership AND pairing checks (`originguard.go:42-67`) are exercised exactly as a real browser would exercise them. No guard code changes, and `codegraph ui` gains no flag — the alternative (adding a bind/port flag) would have altered `SRV-01`/`SRV-03`'s shipped surface and reopened D-08 for a test's convenience. |
| T-06-46 | Denial of Service | a live-push session SLOWING a concurrent sync rather than denying it — completion counted, degradation invisible | high | mitigate | Task 2 measures `baselineMaxFlushDurationMs` with the daemon alone BEFORE starting the UI, then bounds `maxFlushDurationMs` against it (at most `3x + 1000ms`), with both floored at 1ms so "within bounds" cannot be satisfied by never having measured. A completion count alone cannot see a flush that finishes ten times slower than it should. |
| T-06-SC | Tampering | supply chain — package installs | high | mitigate | This phase introduces ZERO new packages; every library it uses was audited and installed in earlier phases, including the `//go:build ignore` probe program, which uses only `connectrpc.com/connect` and this module's own generated code. Asserted directly: `go.mod`, `go.sum`, `web/package.json` and `web/pnpm-lock.yaml` must all be unchanged across the phase (`git diff --stat` against the phase's base commit shows none of the four), so no package-legitimacy checkpoint is required and none is bypassed. |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.5 task test:unit`, `task web:test`, `task proto:drift`, `task web:build`, `task web:drift` all green.
- `GOTOOLCHAIN=go1.26.5 go build ./...` and `go vet ./...` both green WITH `scripts/live-push-probe.go` present — proving its `//go:build ignore` tag excludes it from the shipped build, while `go run scripts/live-push-probe.go` still runs it.
- Both new real-world records committed and passing their own verify commands.
- `git diff --stat <phase-base>..HEAD -- go.mod go.sum web/package.json web/pnpm-lock.yaml` is empty (T-06-SC).
- `git status --porcelain -- web/build` empty AFTER the bundle commit, with `git ls-files --error-unmatch web/build/.build-manifest` succeeding.
</verification>

<success_criteria>
- Criterion 2 is satisfied by a timing assertion at both the Go and browser levels, never an arrival count — and at the browser level the timing bound is mechanically paired with a per-tab received-generation superset check, so it cannot hold over a single receipt.
- Criterion 3 is satisfied by three real tabs, a provably blocked tab with the healthy tabs' timing measured beside it, a real reconnect against a stable origin, and a recorded verdict with evidence.
- Criterion 5 is satisfied against real `codegraph daemon` and `serve --mcp` processes sharing one store — with completion, a starvation definition, and flush duration bounded against a measured no-UI baseline — and the gate demonstrated red.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Where |
|---|---|---|
| `web/scripts/live-push-multitab-check.mjs` | Playwright real-browser gate (3+ tabs, timing, fan-out isolation, reconnect) | `web/scripts/` |
| `web/scripts/live-push-stable-proxy.mjs` | fixed-port loopback reverse proxy with Host/Origin rewrite — the stable origin the reconnect scenario needs | `web/scripts/` |
| `corpora/live-push-multitab-check.json` | committed diagnostic record | `corpora/` |
| `scripts/live-push-concurrency-check.sh` | real three-process gate (criterion 5) | `scripts/` |
| `scripts/live-push-probe.go` | `//go:build ignore` helper: `stream` (real Connect client, envelope-decoding) and `mcp` (persistent stdio harness) | `scripts/` |
| `corpora/live-push-concurrency-check.json` | committed diagnostic record | `corpora/` |
| rebuilt app bundle + build manifest marker | build output | `web/build/` |
| the recorded lifecycle-bookkeeping verdict | SUMMARY prose with commands, counts and a named test | `06-06-SUMMARY.md` |
</artifacts>

<output>
Create `.planning/phases/06-live-push/06-06-SUMMARY.md` when done. It must carry the verdict in
both halves with its positive control, all three RED observations, and a criterion-by-criterion
map from each of the five ROADMAP success criteria to the artifact that satisfies it.
</output>
