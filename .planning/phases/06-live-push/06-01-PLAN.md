---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/uiproto/uiv1/ui.proto
  - internal/uiproto/uiv1/ui.pb.go
  - internal/uiproto/uiv1/uiv1connect/ui.connect.go
  - web/src/lib/gen/ui_pb.ts
  - internal/uiserver/readonly_test.go
  - internal/uiserver/rpcname_test.go
  - internal/uiserver/livehandler.go
autonomous: false
requirements: [RPC-04]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The fourteenth rpc name is proven CLEAN against the live mutatingVerbs fixture by a committed test that also proves a known-colliding control name still collides."
    - "The generated Go handler interface declares exactly 14 methods and the generated TypeScript client exposes the streaming method."
    - "Every field of every new message is pinned by name and number in the descriptor fixture, so a later renumber fails loudly (D-02a)."
    - "A human approved the frozen field numbers before regeneration was committed."
  artifacts:
    - internal/uiproto/uiv1/ui.proto (WatchGraph rpc + WatchGraphRequest + WatchGraphEvent)
    - internal/uiserver/rpcname_test.go
    - internal/uiserver/livehandler.go
    - web/src/lib/gen/ui_pb.ts (regenerated)
  key_links:
    - "ui.proto service block -> uiv1connect.UIServiceHandler interface -> readonly_test.go's wantUIServiceMethods set-equality guard"
    - "ui.proto message fields -> generated descriptor -> readonly_test.go's uiProtoFieldNumbers two-direction coverage guard"
---

<objective>
Freeze the wire surface for live push: one Connect server-streaming rpc added additively to
`service UIService`, its request and event messages, and every fixture that pins them.

Purpose: every downstream plan in this phase (the publisher, the handler, the browser client,
the graph wiring) consumes generated code that does not exist until this plan lands. The rpc
name is also a one-way door — `internal/uiserver/readonly_test.go`'s `mutatingVerbs` fixture
rejects three of the obvious candidates by bare substring match, and proto field numbers are
additive-only forever (D-02a).

Output: a regenerated Go + TypeScript wire surface, a committed name guard, and the two
descriptor fixtures extended.

Ordering note: the phase's end-to-end tracer assertion — a real Connect client receiving a
real, watcher-triggered event over real HTTP/1.1 — lives in `06-04` Task 3 and is marked
`type="tracer"` there. It cannot be this plan's first task because the generated client it
drives does not exist until this plan's Task 3 runs, and the field numbers it carries earn a
blocking human review first.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-push/06-CONTEXT.md
@.planning/phases/06-live-push/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Name the rpc, prove the name clean against the live fixture, and author the frozen proto block</name>
  <files>internal/uiserver/rpcname_test.go, internal/uiproto/uiv1/ui.proto</files>
  <read_first>
    - internal/uiserver/readonly_test.go lines 57-70 (wantUIServiceMethods), 118-121 (mutatingVerbs), 150-231 (uiProtoFieldNumber type and the chain of length constants), 460-585 (the two-direction descriptor guard)
    - internal/uiproto/uiv1/ui.proto lines 1-50 (the additive-only D-02a header) and lines 50-109 (the existing 13-rpc service block, for comment-density and per-rpc doc-comment convention)
    - .planning/phases/06-live-push/06-CONTEXT.md — the naming-constraint block and D-07's exact event field set
  </read_first>
  <behavior>
    - `internal/uiserver/rpcname_test.go` reads `mutatingVerbs` (the same package-level var, never a second transcription) and asserts the chosen rpc name contains none of its members.
    - The same test asserts a POSITIVE CONTROL: the string `GetIndexHealth` DOES contain a member of `mutatingVerbs`. A checker that cannot discriminate is a checker that proves nothing (rule 84d1gfpywd) — if the control stops colliding, the guard fails.
    - The same test asserts `len(mutatingVerbs)` is a non-zero, named count and that a known member (`Reindex`) is still present, so the guard cannot pass by the verb list having been emptied.
    - `mutatingVerbs` itself is NEVER edited by this task.
  </behavior>
  <action>
    Choose `WatchGraph` as the fourteenth rpc name — verified CLEAN against the live 19-member
    `mutatingVerbs` fixture this session, with `GetIndexHealth` confirmed still colliding on
    `Index` as the positive control. Do not use `WatchIndex`, `IndexEvents`, `StreamIndex` or
    `LiveUpdates`; all four collide.

    Write `internal/uiserver/rpcname_test.go` implementing the behaviors above, in package
    `uiserver` so it reads `mutatingVerbs` directly rather than re-transcribing it.

    Then author the frozen block in `internal/uiproto/uiv1/ui.proto`, appended to the existing
    `service UIService` block after `FileSymbols`, following the same per-rpc doc-comment
    convention every rpc from `GetPermalink` onward uses (what it projects, why it exists
    separately, and the read-only/additive assertion):

    - `rpc WatchGraph(WatchGraphRequest) returns (stream WatchGraphEvent);`
    - `message WatchGraphRequest` with exactly one field, `int64 since_generation = 1`, the
      resume point a reconnecting client sends (Claude's Discretion, CONTEXT.md).
    - `message WatchGraphEvent` with exactly six fields, in D-07's order and numbering:
      `int64 generation = 1`, `bool initialized = 2`, `bool stale = 3`, `bool store_exists = 4`,
      `bool indexing_in_progress = 5`, `string commit_sha = 6`.

    D-07 is binding and supersedes D-05's field set: the five state fields deliberately mirror
    `GetStatusResponse`'s own field NAMES so `web/src/lib/status.ts`'s `classifyStatus` can be
    fed the event directly with no translation layer and no second representation of the same
    state. `node_count`, `edge_count`, `healthy` and `health_message` are deliberately absent —
    no shipped component renders them. Record that reasoning in the message's doc comment
    without naming any planning artifact, plan id or phase number (this repository has twice
    had to strip that leak from shipped comments).

    Per D-01, `Meta.last_sync_unix_ms` stays the server-side change DETECTOR and is deliberately
    not a field on the event: detecting a change and describing the new state are different
    jobs. Say so in the doc comment.

    Do NOT regenerate anything in this task. Do NOT edit `wantUIServiceMethods` yet — the
    package must still compile and its existing 13-method guard must still pass at this commit.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.5 go test ./internal/uiserver/ -run '^(TestRPCName|TestUIServiceMethodSetIsExactlyTheReadSet|TestUIServiceDeclaresNoMutatingMethod)' -v -count=1 > /tmp/06-01-t1.log 2>&1; test "$(rg -c '^--- PASS: TestRPCName' /tmp/06-01-t1.log || echo 0)" -ge 1 && test "$(rg -c '^--- PASS: TestUIService' /tmp/06-01-t1.log || echo 0)" -ge 2 && test "$(rg -c '^--- FAIL' /tmp/06-01-t1.log || echo 0)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The named `TestRPCName*` test appears as a `--- PASS:` line (counted, never inferred from exit status — `go test -run` exits 0 when its pattern matches nothing, a trap this repository has hit three times).
    - Both pre-existing `TestUIService*` guards still appear as `--- PASS:` lines, proving the proto edit did not disturb the 13-method surface before regeneration.
    - `git diff internal/uiserver/readonly_test.go` is empty at this commit AND `rg -c '"Reindex"' internal/uiserver/readonly_test.go` is at least 1 — an unchanged-diff assertion paired with a positive presence check, so "unchanged" cannot be satisfied by the file having been gutted.
    - `rg -c 'rpc WatchGraph\(WatchGraphRequest\) returns \(stream WatchGraphEvent\);' internal/uiproto/uiv1/ui.proto` is exactly 1.
    - All seven frozen field declarations are present as real declarations, not as comment prose: `test "$(rg -v '^\s*//' internal/uiproto/uiv1/ui.proto | rg -c 'since_generation = 1;|int64 generation = 1;|initialized = 2;|stale = 3;|store_exists = 4;|indexing_in_progress = 5;|commit_sha = 6;')" -ge 7` (comment lines stripped first — a bare count over the whole file would be satisfied by the doc comment describing the fields).
  </acceptance_criteria>
  <done>The rpc name is proven clean by a committed, discriminating guard, and the proto block is authored but not yet regenerated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Proto freeze review — field numbers are additive-only forever</name>
  <files>internal/uiproto/uiv1/ui.proto</files>
  <read_first>
    - internal/uiproto/uiv1/ui.proto (the block Task 1 just authored)
    - internal/uiproto/uiv1/ui.proto lines 6-11 (the additive-only D-02a discipline this freeze is bound by)
  </read_first>
  <what-built>
    The frozen wire shape for live push, authored but NOT yet regenerated: `rpc WatchGraph(WatchGraphRequest) returns (stream WatchGraphEvent)` appended to `service UIService` in `internal/uiproto/uiv1/ui.proto`, with `WatchGraphRequest` carrying one field (`since_generation = 1`) and `WatchGraphEvent` carrying six (`generation = 1`, `initialized = 2`, `stale = 3`, `store_exists = 4`, `indexing_in_progress = 5`, `commit_sha = 6`). No generated Go or TypeScript file has been written.
  </what-built>
  <how-to-verify>
    1. Read the authored block: `rg -n 'rpc WatchGraph' -A 4 internal/uiproto/uiv1/ui.proto` and `rg -n 'message WatchGraph(Request|Event)' -A 30 internal/uiproto/uiv1/ui.proto`.
    2. Confirm no generated artifact was written ahead of this review: `git status --porcelain internal/uiproto/uiv1/ui.pb.go internal/uiproto/uiv1/uiv1connect/ui.connect.go web/src/lib/gen/ui_pb.ts` is empty.
    3. Answer the four questions, each of which is expensive to change later because within this package a field number is never renumbered and never reused (D-02a) — retiring one means a `reserved` clause, not a deletion:
       a. Is `WatchGraph` the name to live with? It is verified clean against the read-only guard's 19 mutating-verb substrings; `WatchIndex`, `IndexEvents`, `StreamIndex` and `LiveUpdates` are all rejected by it.
       b. Is `since_generation` the right resume handle, or should the request carry more?
       c. Is the six-field event set complete — is anything the chrome will need in this milestone missing while the numbers are still free? (`node_count`, `edge_count`, `healthy` and `health_message` were deliberately dropped: nothing shipped renders them.)
       d. Is `stream WatchGraphEvent` — server-streaming, not bidirectional — the intended shape?
  </how-to-verify>
  <resume-signal>Type "approved" to freeze these seven field numbers permanently, or name the change you want and the block will be amended and re-presented.</resume-signal>
  <action>
    STOP. Do not run `task proto:gen`. Present the authored block to the developer for
    approval before any generated artifact is committed.

    Show, verbatim from the file: the `rpc WatchGraph` line, the complete `WatchGraphRequest`
    message, and the complete `WatchGraphEvent` message with all six field numbers.

    State plainly what approval commits to: within `codegraph.ui.v1`, a field number is never
    renumbered and never reused (D-02a). `since_generation = 1` on the request and the six
    numbers 1-6 on the event are permanent from the moment the generated code is committed.
    Retiring any of them later means a `reserved` clause, not a deletion.

    Ask the four questions that a later change would be expensive to answer wrongly:
    1. Is `WatchGraph` the name to live with? (Verified clean; `WatchIndex`/`IndexEvents`/
       `StreamIndex`/`LiveUpdates` are all rejected by the read-only guard.)
    2. Is `since_generation` the right resume handle, or should the request carry more?
    3. Is D-07's six-field event set complete — is anything the chrome will need in this
       milestone missing while the numbers are still free?
    4. Is `stream WatchGraphEvent` (server-streaming, not bidi) the intended shape?

    This checkpoint is BLOCKING and is never auto-approvable, regardless of the project's
    auto-advance setting. Every rpc in this milestone has earned one.

    On approval, proceed to Task 3. On any change request, amend the proto and re-present.
  </action>
  <verify>
    <automated>test -n "$(rg -c 'rpc WatchGraph' internal/uiproto/uiv1/ui.proto || echo '')" && test ! -f internal/uiproto/uiv1/ui.pb.go.orig</automated>
    <human-check>The developer has explicitly approved the four questions above and the six frozen event field numbers.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer's approval (or amendment) is recorded in the plan SUMMARY, naming the four questions and the answers given.
    - No generated file was written before approval.
  </acceptance_criteria>
  <done>A human has approved the frozen wire shape.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate both language surfaces and extend the two descriptor fixtures</name>
  <files>internal/uiproto/uiv1/ui.pb.go, internal/uiproto/uiv1/uiv1connect/ui.connect.go, web/src/lib/gen/ui_pb.ts, internal/uiserver/readonly_test.go, internal/uiserver/livehandler.go</files>
  <read_first>
    - Taskfile.yml lines 215-278 (proto:gen) and 279-390 (proto:drift — the byte-identity gate and its floor of 4 generated files)
    - internal/uiserver/readonly_test.go lines 57-108 (wantUIServiceMethods and the hard-coded 13 in TestUIServiceMethodSetIsExactlyTheReadSet), 161-231 (the length-constant chain), 483-490 (the length comparison), 540-556 (Direction 2 — every declared field must be covered by a fixture entry)
    - internal/uiserver/handlers.go lines 26-66 (openEngine/withEngine — the shape Task 3's placeholder must NOT imitate; the streaming handler has no unary precedent)
  </read_first>
  <action>
    Run `task proto:gen`. It regenerates `internal/uiproto/uiv1/ui.pb.go`,
    `internal/uiproto/uiv1/uiv1connect/ui.connect.go` and `web/src/lib/gen/ui_pb.ts` through
    the pinned buf toolchain. Never hand-edit a generated file.

    The regenerated `uiv1connect.UIServiceHandler` interface now requires a fourteenth method,
    so `internal/uiserver` will not compile until `uiService` declares it. Create
    `internal/uiserver/livehandler.go` with a minimal `WatchGraph` method whose only job at
    this wave is to satisfy the interface: it returns a Connect error carrying the
    not-yet-implemented code, with a doc comment stating that `06-04` replaces the body with
    the real subscriber-register / Send-loop / deregister lifecycle. Do not build any
    subscriber machinery here. `06-04`'s own end-to-end test — a real client receiving a real
    event — cannot pass while this body survives, which is the structural gate that stops the
    placeholder from shipping; no grep is needed and none is used.

    Then extend BOTH fixtures in `internal/uiserver/readonly_test.go`. These are value-only
    edits filling in shapes the fixture already declares:

    (a) Add `"WatchGraph": {}` to `wantUIServiceMethods`, and change the two `13` literals in
    `TestUIServiceMethodSetIsExactlyTheReadSet` (the `len(got) != 13` comparison and its
    failure message) to `14`. Update the fixture's leading doc comment the same way every
    prior wave did — a short paragraph naming `WatchGraph` as the fourteenth read-only rpc and
    why a stream is still a read: it performs no network operation and mutates nothing.

    (b) `TestKnownUIProtoFieldNumbersAreStable`'s Direction 2 requires that EVERY field of
    EVERY message in the generated descriptor be covered by a `uiProtoFieldNumbers` entry — a
    new message with no entries fails it. Append the seven new entries (one for
    `WatchGraphRequest.since_generation`, six for `WatchGraphEvent`'s fields) and declare a
    new length constant defined in terms of the previous one, exactly as every prior wave did:
    the new constant equals `uiProtoFieldFixtureLenAtPlan0506 + 7`. Update the length
    comparison at the top of that test to reference the new constant. Never write a bare
    literal — the chain is compiler-checkable by construction.

    Do NOT touch `mutatingVerbs`.

    Stage the regenerated `web/src/lib/gen/ui_pb.ts` with the Go files. The `web/build` bundle
    rebuild for this phase is deliberately deferred to `06-06`, once every web source change
    in the phase has landed — do not rebuild it here.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.5 go build ./... && GOTOOLCHAIN=go1.26.5 go test ./internal/uiserver/ -run '^(TestUIServiceMethodSetIsExactlyTheReadSet|TestUIServiceDeclaresNoMutatingMethod|TestKnownUIProtoFieldNumbersAreStable|TestRPCName)' -v -count=1 > /tmp/06-01-t3.log 2>&1; test "$(rg -c '^--- PASS: Test' /tmp/06-01-t3.log || echo 0)" -ge 4 && test "$(rg -c '^--- FAIL' /tmp/06-01-t3.log || echo 0)" -eq 0 && task proto:drift</automated>
  </verify>
  <acceptance_criteria>
    - At least four distinct `--- PASS: Test` lines and zero `--- FAIL` lines in the captured log.
    - `task proto:drift` exits 0 and its own output reports comparing at least 4 generated files — the committed generated tree is byte-identical to the pinned toolchain's regeneration.
    - The TypeScript half was regenerated too, not silently skipped: `node -e 'const s=require("fs").readFileSync("web/src/lib/gen/ui_pb.ts","utf8"); const hits=(s.match(/WatchGraph/g)||[]).length; if(hits<3){console.error("generated TS client mentions WatchGraph only "+hits+" times — expected the rpc plus both message schemas");process.exit(1)} console.log("TS client exposes WatchGraph, "+hits+" references")'` succeeds.
    - The new length constant is defined in terms of `uiProtoFieldFixtureLenAtPlan0506`, not as a bare number: `rg -c 'uiProtoFieldFixtureLenAtPlan0506 \+ 7' internal/uiserver/readonly_test.go` is exactly 1.
    - `git diff --stat internal/uiserver/readonly_test.go` shows changes ONLY inside the fixture bodies and the length constants, and `rg -c '"Reindex"' internal/uiserver/readonly_test.go` is still at least 1.
  </acceptance_criteria>
  <done>Both language surfaces are regenerated, byte-identical to the pinned toolchain, and both descriptor fixtures cover the new messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> loopback Connect listener | Untrusted request path; the new rpc joins an existing guarded surface |
| proto schema -> every generated consumer | A wrong or renumbered field silently changes meaning for both languages at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | `service UIService` gains a 14th method | high | mitigate | The rpc is read-only by construction (it publishes state, accepts only a resume cursor) and is proven so by the two existing guards: set-equality against `wantUIServiceMethods` and the substring check against the unmodified `mutatingVerbs`, both re-run in Task 3's verify. |
| T-06-02 | Tampering | proto field numbering (D-02a) | high | mitigate | Seven new entries pinned in `uiProtoFieldNumbers` with a compiler-checked length constant; `TestKnownUIProtoFieldNumbersAreStable`'s two directions fail on a renumber AND on an unpinned new field. |
| T-06-03 | Tampering | generated code drift between the two languages | medium | mitigate | `task proto:drift` regenerates into a scratch tree through the pinned toolchain and compares byte-for-byte, with its own floor of 4 enumerated files so a broken enumeration cannot read as clean. |
| T-06-04 | Information Disclosure | `WatchGraphEvent.commit_sha` on the wire | low | accept | `commit_sha` is already on `GetStatusResponse` (field 9) and `GetHealthResponse` over the same loopback-only, same-origin-guarded surface; this event exposes nothing the browser cannot already read. |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.5 go build ./...` succeeds.
- `GOTOOLCHAIN=go1.26.5 task test:unit` is green (this target deliberately excludes `internal/daemon`, whose watchdog test is timing-sensitive under parallel load — that exclusion is intentional and must not be "fixed").
- `task proto:drift` is green.
- The blocking human checkpoint was answered, not auto-advanced.
</verification>

<success_criteria>
- `uiv1connect.UIServiceHandler` declares exactly 14 methods and the fixture agrees.
- Seven new field numbers are pinned and covered in both directions.
- Both generated language surfaces are committed and byte-identical to a fresh regeneration.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Where |
|---|---|---|
| `WatchGraph` | Connect server-streaming rpc | `internal/uiproto/uiv1/ui.proto` |
| `WatchGraphRequest` (`since_generation`) | proto message | `internal/uiproto/uiv1/ui.proto` |
| `WatchGraphEvent` (`generation`, `initialized`, `stale`, `store_exists`, `indexing_in_progress`, `commit_sha`) | proto message | `internal/uiproto/uiv1/ui.proto` |
| `uiv1.WatchGraphRequest`, `uiv1.WatchGraphEvent` | generated Go types | `internal/uiproto/uiv1/ui.pb.go` |
| `UIServiceWatchGraphProcedure`, handler/client streaming plumbing | generated Go | `internal/uiproto/uiv1/uiv1connect/ui.connect.go` |
| `WatchGraphRequestSchema`, `WatchGraphEventSchema`, `watchGraph` client method | generated TypeScript | `web/src/lib/gen/ui_pb.ts` |
| `(*uiService).WatchGraph` | Go method (placeholder body; real body lands in 06-04) | `internal/uiserver/livehandler.go` |
| `TestRPCNameIsCleanAgainstMutatingVerbs` | Go test | `internal/uiserver/rpcname_test.go` |
| `uiProtoFieldFixtureLenAtPlan0601` | Go const | `internal/uiserver/readonly_test.go` |
</artifacts>

<output>
Create `.planning/phases/06-live-push/06-01-SUMMARY.md` when done. Record the checkpoint's four
answers verbatim, and the exact seven (message, field, number) triples frozen.
</output>
