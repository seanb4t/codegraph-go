---
phase: 02-golden-harness-re-authoring-re-freeze
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - testdata/golden/gocapture/main.go
  - Taskfile.yml
  - testdata/golden/behavioral_test.go
  - testdata/golden/behavioral_java_test.go
  - testdata/golden/behavioral_tsjs_test.go
  - testdata/golden/behavioral_csharp_test.go
  - testdata/golden/behavioral_python_test.go
  - testdata/golden/golden_test.go
  - internal/indexer/capability/matrix.go
  - docs/LANGUAGE-CAPABILITY-MATRIX.md
autonomous: true
requirements: [FIXT-06]
user_setup:
  - service: none (locked corpora are fetched via task corpora:fetch)
    why: "The re-freeze and hermetic tests read the locked corpora from ${CORPUS_ROOT} (Phase 1)."
    env_vars:
      - name: CODEGRAPH_CORPUS_DIR
        source: "Optional override of the XDG cache default (~/.cache/codegraph/corpora)"
    precondition: "task corpora:fetch && task corpora:assert (4/4) before running gocapture or the locked-corpus tests"
estimate:
  tokens: 125000
  raw_tokens: 62500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "gocapture is extended, not replaced: it gains one corpusSpec per locked corpus (hugo, guava, serilog, requests) whose source resolves ONLY through internal/corpora Entry.Dir(root) / CorpusRoot(), never a hardcoded SHA or path (D-05, D-10, constraint 4)."
    - "Every golden gocapture writes goes through capture-to-temp-then-move with a non-empty + marker({ before install — a bare or half-written golden fails loudly, never lands (T-02-03, carryover 01-03 / Pattern 2)."
    - "gocapture FAILS CLOSED for a missing locked/behavioral source: it is no longer skip-warn; a mandatory corpus whose tree is absent makes golden:regen exit non-zero rather than leaving stale goldens green (MED: regenerateCorpus fail-closed)."
    - "A single Taskfile target, golden:regen, runs corpora:fetch + corpora:assert + gocapture — 'regenerate all goldens' is one reviewable command."
    - "A committed, explicit language-to-locked-slug map resolves EVERY priority-4 language to at least one locked corpus: go->hugo, tsjs->hugo (hugo's 25 JS files supply the tsjs leg despite its manifest language:'go'), java->guava, csharp->serilog, python->requests. A mapping that leaves a priority language unresolvable is a test failure, not a skip (H3)."
    - "The re-authored locked-corpus tests resolve corpora through internal/corpora via the language map and FAIL LOUDLY (t.Fatalf) on an absent or integrity-failed locked tree — they never t.Skip on a missing corpus (constraint 2, D-10, rule 84d1gfpywd)."
    - "The four behavioral_*_language tests run against the locked corpora and assert shape/coverage (index twice, node/edge kind counts); removing a locked corpus turns them red. Aggregate count equality does NOT stand in for golden-byte ordering — only the repeated-capture byte-stability check proves ordering (LOW: determinism overstatement)."
    - "A bare/missing re-frozen golden cannot read as satisfied: TestReFrozenGoldensValid enumerates the EXPECTED golden set from an authoritative source (the gocapture spec table / case map — never a glob of existing files), requires each expected file to exist AND be non-empty AND be byte-stable across a repeated capture, and positively asserts the count of goldens verified (H5, A3, T-02-03, rule 84d1gfpywd)."
    - "Output ordering is deterministic across captures: the repeated golden:regen byte-stability check (02-04) is what proves it; the suite must not cite index-count assertions as enforcing ordering (LOW)."
    - "Adjacency at a tie is inclusive, never a merge: overloaded same-named symbols stay distinct definitions at equal/adjacent locations (COVERAGE/FIXT-06 adjacency)."
    - "The matrix gap prose is re-synchronized to the fail-loud posture this plan establishes: matrix.go and the mirrored docs/LANGUAGE-CAPABILITY-MATRIX.md no longer claim the tests 'self-skip' or use a 'source-as-spec fallback' once they resolve locked corpora and fail loudly (MED: matrix prose staleness)."
  artifacts:
    - testdata/golden/gocapture/main.go (lockedCorpusSpec + behavioralCorpusSpec + temp-then-move writeCapture + fail-closed source + MCP staging)
    - Taskfile.yml golden:regen target
    - An explicit language->locked-slug map (typed, committed — used by gocapture resolution, lockedCorpusDir, and the completeness guard)
    - TestPriorityLanguagesResolveToLockedCorpus (proves every priority language resolves, fails not skips)
    - TestCorpusBehaviorLockedCorpora (locked-corpus subtests)
    - TestCorpusBehavior_Go/_Java/_CSharp/_Python/_TSJS hermetic locked-corpus resolution (t.Fatalf, no skip)
    - TestReFrozenGoldensValid (expected-set enumeration + non-empty + marker + byte-stability + positive count guard)
  key_links:
    - "the language->slug map <-> internal/corpora LockedEntries (Load(corpora/manifest.json) -> e.Dir(CorpusRoot())); every priority language must resolve (H3)"
    - "gocapture corpusSpec.resolveSource <-> internal/corpora (Load(corpora/manifest.json) -> LockedEntries -> e.Dir(CorpusRoot()))"
    - "golden:regen target <-> task corpora:fetch / corpora:assert (four-part integrity before any locked-tree read, T-02-04)"
    - "TestReFrozenGoldensValid expected set <-> the gocapture spec table (not a glob — H5)"
  prohibitions:
    - "Never hardcode a locked-corpus SHA or on-disk path inside gocapture or any test — the manifest is the sole pin authority (D-09); resolve via internal/corpora and the language map (constraint 4, H3)."
    - "Never reuse a live TS server / mcp-capture.mjs for the MCP-surface capture — use the in-process internalmcp.BuildServer + go-sdk client already proven by the CLI==MCP trio (T-02-02); the MCP surface reads a temp indexed repo via query.OpenAt."
    - "Renamed tests MUST NOT t.Skip on a missing locked corpus (constraint 2); gocapture must NOT skip-warn a missing mandatory source (fail closed, MED)."
    - "TestReFrozenGoldensValid MUST enumerate expected goldens from authority — a glob-over-existing guard is forbidden (H5, rule 84d1gfpywd)."
    - "Never put [ci skip] / [skip ci] in a commit message (f18zrdsgx5); every go test carries -count=1."
    - "Do not index an unverified checkout: task corpora:assert (four-part) precedes any gocapture read of a locked tree (T-02-04)."
---

<objective>
Build the second reviewed diff's machinery (FIXT-06, Diff B part 1): extend `testdata/golden/gocapture` from a weft/colbymchenry/synthetic capture tool into the sole authority that captures every golden from codegraph-go's own output against the Phase-1-locked corpora and the committed behavioral corpus; add capture-to-temp-then-move so a bare golden can never land (T-02-03); add a one-command `golden:regen` Taskfile target; and re-point the re-authored tests to hermetic locked-corpus resolution through `internal/corpora` that FAIL LOUDLY instead of skipping (D-10, constraint 2). This plan extends the tooling and the test resolution; it does NOT yet run the re-freeze or change any golden byte — that is plan 02-04.

Purpose: FIXT-06 requires every golden to be produced by the Go-side capture path from codegraph-go's own output against the locked corpora. That requires (a) a capture mechanism that resolves the locked trees without restating SHAs, (b) a write discipline that cannot emit a bare oracle, and (c) tests that prove they ran against the real pinned trees rather than skipped. All three land here; the physical re-freeze run + review is plan 02-04.

Output: An extended gocapture (locked + behavioral specs, temp-then-move), a `golden:regen` target, hermetic fail-loud locked-corpus tests, and a byte-identity guard over every golden.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-CONTEXT.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-RESEARCH.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-PATTERNS.md

# Prior-plan SUMMARY context (the patterns this plan carries forward)
@.planning/phases/01-corpus-selection-by-measurement/01-03-SUMMARY.md   # capture-to-temp-then-move discipline (Pattern 2)
@.planning/phases/01-corpus-selection-by-measurement/01-04-SUMMARY.md   # internal/corpora Entry.Dir/CorpusRoot/LockedEntries as the resolution seam
# [from Phase 1] the wire-oracle re-freeze (01-03) is the reviewed-diff precedent this plan's gocapture extension feeds; the corpora:assert four-part check (01-04) is the T-02-04 integrity gate this plan's precondition relies on.

# Surfaces this plan rewrites
@testdata/golden/gocapture/main.go
@testdata/golden/behavioral_test.go
@testdata/golden/behavioral_java_test.go
@testdata/golden/behavioral_tsjs_test.go
@testdata/golden/behavioral_csharp_test.go
@testdata/golden/behavioral_python_test.go
@testdata/golden/golden_test.go
@Taskfile.yml
@internal/corpora/manifest.go   # Entry.Dir / CorpusRoot / LockedEntries (188-229)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend gocapture — locked-corpus + behavioral specs, per-spec output dirs, capture-to-temp-then-move, CLI+MCP surfaces</name>
  <files>testdata/golden/gocapture/main.go</files>
  <read_first>
    testdata/golden/gocapture/main.go   # corpusSpec struct (69-89), main() spec list + corpusDir derivation (91-115), regenerateCorpus (117-207), writeCapture (209-219), syntheticParitySpec/behavioralCorpusSpec (276-290)
    internal/corpora/manifest.go        # Entry.Dir(root) (188-192), CorpusRoot() (205-217), LockedEntries(m) (221-229), Load
    internal/corpora/coverage_test.go   # the corpora.Load(filepath.Join("..","..","corpora","manifest.json")) loading shape (123-152)
  </read_first>
  <action>
    Extend gocapture per D-05/FIXT-06, keeping it a `main` program driven manually/CI and guarded by the existing TestGoSideFixturesRegenerated. Do NOT replace it, and do NOT re-add any TS/capture dependency (T-02-02).

    - Add a locked-corpus resolver helper (H3): load the manifest (`internal/corpora.Load` from the repo-root `corpora/manifest.json`), and resolve sources via `root, _ := corpora.CorpusRoot()` then `e.Dir(root)` for each `e` in `corpora.LockedEntries(m)`. Resolve langs via an EXPLICIT committed language->locked-slug map (typed, a task artifact), NOT `e.Language == language` equality — the manifest has only go(hugo)/java(guava)/csharp(serilog)/python(requests) locked, and hugo supplies the tsjs leg from its JS files even though its manifest `language` is "go". The map is: go->hugo, tsjs->hugo, java->guava, csharp->serilog, python->requests. The resolver returns each locked tree's directory — NEVER a hardcoded SHA or path (constraint 4, D-09).
    - Fail closed on a missing mandatory source (MED: regenerateCorpus skip-warn): a locked or behavioral corpusSpec whose resolved source is absent(/not-a-directory) is now a HARD error for that spec — the corpus is mandatory, so `regenerateCorpus` returns the error and `main()` exits non-zero. Remove the old skip-warn contract; it fit the optional weft/colbymchenry/online-clone corpora that no longer exist. A stale golden left behind by a skipped capture must not read as green (the fail-closed error is what prevents it).
    - Extend the `corpusSpec` seam with an output-dir field (e.g. `outDir func() string` or an explicit per-spec out path) so goldens land where their readers expect them:
      - locked-corpus specs -> `testdata/golden/corpus/<slug or lang>/` under the existing `corpusDir` derivation (e.g. a `<slug>` per-locked-corpus dir; the exact dir layout is discretion so long as the reader tests and gocapture agree).
      - the behavioral spec -> repo-root `corpus/behavioral/` (filepath.Dir(goldenDir)+"/corpus/behavioral"), since that is where the moved go-* goldens and src now live.
    - Add the locked-corpus corpusSpecs to the `specs` slice in main(), one per locked corpus with the per-corpus symbol/query parameters (select symbols that resolve under Go extraction for each; planner/executor discretion within the "capture both explore and node, CLI and MCP surfaces" clause — ROADMAP Notes). Keep `behavioralCorpusSpec` (safe per 02-02).
    - MCP-surface capture (the "both CLI and MCP surfaces in the re-freeze" clause; MED: MCP staging + expected set): for each spec, capture the MCP surface through the IN-PROCESS server already proven by the CLI==MCP trio — `internalmcp.BuildServer` + a go-sdk in-process client (`newGoldenSession`/`callExploreViaMCP`/`callNodeViaMCP` analog), NEVER a live TS server or `mcp-capture.mjs`. Because the MCP handlers reopen an index via `query.OpenAt` (not the in-memory Engine), stage the corpus as a temp INDEXED repo: copy the source tree into a `t.TempDir()` (or gocapture-equivalent temp dir), `indexer.Run` it so `.codegraph/store` exists beneath it, then call the in-process MCP server against that staged repo. Emit MCP outputs as `*-mcp.json` goldens alongside the CLI `*` / `*-multi.json` ones, under the same {command,output} `goldenCapture` envelope. Give the per-spec symbol/query and the expected output set a concrete, committed value so `golden:regen`'s completeness check (and 02-04's expected-count review) has a stable number: per locked corpus, exactly {explore (baseline, CLI), node (baseline, CLI), explore-multi (CLI), node-multi (CLI), explore-mcp (MCP), node-mcp (MCP)} — 6 goldens per corpus + the behavioral corpus's own CLI/MCP set — all named in the spec table. (The exact per-corpus symbols/queries are executor discretion within the spec table, but the SET of expected filenames per corpus is committed and shared with TestReFrozenGoldensValid.)
    - Convert `writeCapture` (209-219) to capture-to-temp-then-move (Pattern 2, carryover 01-03): write to a temp path in the same directory (`os.CreateTemp`), `json.Encode` the envelope + `\n`, flush/fsync (or at least close) the temp, assert the temp is non-empty and byte-prefixed with the marker `{` (or `{"`), then `os.Rename(temp, path)` onto the committed golden path. Any failure (empty/marker-less/temp-create/rename) returns an error — never leaves a bare golden on the committed path and never partially writes.
    - Make the multi-symbol best-effort write FAIL on a bare golden for the locked corpora: today a failed multi-symbol only warns (warn-not-fatal, research A3). For locked-corpus goldens, a failed symbol that would leave a missing/bare golden must now be a hard error for that corpus (fold into the corpus's overall error return so `main()` exits non-zero), because FIXT-06 must not ship a "satisfied but bare" locked-corpus golden. Keep warn-not-fatal only for the behavioral spec where it is not load-bearing. (Executor: if converting warn->fatal for all locked goldens is prohibitive, prefer asserting every targeted locked-corpus golden exists + non-empty at the end of the spec's capture instead — the effect, a hard failure on a bare locked golden, is what FIXT-06/A3 requires.)
    - Do NOT run the capture in this task (that is 02-04). This task only extends the tool.
    - No `[ci skip]` / `[skip ci]`; `-count=1`.

    Commit: conventional `feat(02-03):` naming the cause (D-05/FIXT-06): gocapture extended with locked-corpus + behavioral specs, per-spec output dirs, capture-to-temp-then-move, and CLI+MCP surface capture.
  </action>
  <verify>
    <automated>go build ./testdata/golden/gocapture/... && go vet ./testdata/golden/gocapture/... && go test -count=1 ./internal/corpora/...</automated>
  </verify>
  <done>
    - gocapture builds and vets clean; it now lists locked-corpus specs (hugo/guava/serilog/requests) + behavioral, all resolving sources via internal/corpora Entry.Dir/CorpusRoot (no hardcoded SHA/path in main.go).
    - writeCapture writes to temp then renames, asserting non-empty + `{` marker before install.
    - MCP-surface capture uses the in-process BuildServer + go-sdk client (no TS dependency, no mcp-capture.mjs).
    - `go test -count=1 ./internal/corpora/...` green (resolution package unchanged).
  </done>
  <acceptance_criteria>
    - `rg -n "CorpusRoot|LockedEntries|e\.Dir|CorporaRoot|internal/corpora" testdata/golden/gocapture/main.go` shows the resolution seam imported/used.
    - `rg -n '"[0-9a-f]{40}"|hugo@|guava@|serilog@|requests@' testdata/golden/gocapture/main.go` returns no hardcoded SHA.
    - `go build ./testdata/golden/gocapture/...` exits 0.
    - `rg -n "os\.Rename|CreateTemp|todo.*marker|\{\"" testdata/golden/gocapture/main.go` shows the temp-then-move + non-empty/marker assertions.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-point the locked-corpus tests to hermetic resolution through internal/corpora that fails loudly, never skips</name>
  <files>
    testdata/golden/behavioral_test.go
    testdata/golden/behavioral_java_test.go
    testdata/golden/behavioral_tsjs_test.go
    testdata/golden/behavioral_csharp_test.go
    testdata/golden/behavioral_python_test.go
    internal/indexer/capability/matrix.go
    docs/LANGUAGE-CAPABILITY-MATRIX.md
  </files>
  <read_first>
    testdata/golden/behavioral_java_test.go   # resolveJavaCorpus (38-65, env CODEGRAPH_JAVA_CORPUS + sibling + t.Skip), TestCorpusBehavior_Java (81-151, indexer.Run + shape/determinism)
    testdata/golden/behavioral_python_test.go  # resolvePythonCorpus, TestCorpusBehavior_Python (same shape)
    testdata/golden/behavioral_tsjs_test.go   # resolveTSJSCorpus, TestCorpusBehavior_TSJS
    testdata/golden/behavioral_csharp_test.go  # resolveCSharpCorpus, TestCorpusBehavior_CSharp
    testdata/golden/behavioral_test.go         # TestCorpusBehaviorLockedCorpora scaffold
    internal/corpora/manifest.go               # Load / LockedEntries / Entry.Dir / CorpusRoot (188-229); locked entries have language go/java/csharp/python ONLY (no tsjs)
    internal/indexer/capability/matrix.go       # gap prose that claims Python/TSJS "self-skip" and Java/C# use "source-as-spec fallback" (lines ~113,136,147) — factually false after these tests fail-loud
    docs/LANGUAGE-CAPABILITY-MATRIX.md          # mirrors the same gap strings verbatim (TestMatrix_DocMirrorsDescriptor)
  </read_first>
  <action>
    Per D-10 + constraint 2: the re-authored hermetic tests must resolve the locked corpus for their language through `internal/corpora` and FAIL LOUDLY on an absent or integrity-failed tree — never `t.Skip`. This is what makes FIXT-03 (Phase 3) achievable and closes the "skips on missing corpus passes vacuously" anti-pattern (rule 84d1gfpywd).

    - Define the EXPLICIT language->locked-slug map as a committed task artifact (H3): `go->hugo, tsjs->hugo, java->guava, csharp->serilog, python->requests`. The manifest locks hugo only with `language:"go"`, so `lockedCorpusDir(t,"tsjs")` must NOT match `e.Language == "tsjs"` (that finds nothing and would `t.Fatalf`). Instead it resolves through the map to the hugo slug. The map is the single source for gocapture resolution (Task 1), for the hermetic tests (Task 2), and for the completeness guard (Task 3).
    - Add ONE shared hermetic resolver helper (e.g. in behavioral_test.go, same package `golden`): `func lockedCorpusDir(t *testing.T, language string) string` that loads the manifest (`internal/corpora.Load(filepath.Join("..","..","corpora","manifest.json"))`), looks up the language's locked slug in the language map, finds that locked entry, resolves `e.Dir(root)` via `corpora.CorpusRoot()`, and on any failure — map missing for a priority language, manifest unreadable, no locked entry for the slug, or the resolved dir is not a directory — calls `t.Fatalf` with the named cause. This helper is the single hermetic resolution path; no test re-implements it, and no test reads an env-var default or uses `t.Skip`.
    - Add `TestPriorityLanguagesResolveToLockedCorpus` (H3 positive guard): iterate the five priority-4 languages (go, java, csharp, python, tsjs), call `lockedCorpusDir(t, lang)` for each, and assert every one resolves to a directory that exists. A language whose map entry or locked corpus is missing FAILS (rule 84d1gfpywd) — it never skips and never silently covers less than all five languages.
    - In the four `behavioral_<lang>_test.go`: replace `resolveJavaCorpus`/`resolveTSJSCorpus`/`resolveCSharpCorpus`/`resolvePythonCorpus` (their env `CODEGRAPH_{LANG}_CORPUS` + sibling-checkout + `t.Skip` paths are deleted) with `TestCorpusBehavior_<Lang>` calling `lockedCorpusDir(t, "<lang>")` directly. Keep the shape assertions (per-language node-kind coverage, non-zero `calls` edges, and the from-scratch re-run determinism check asserting byte-identical `stats.Files/Nodes/Edges`). NOTE (LOW: determinism overstatement): the Files/Nodes/Edges aggregate-equality check proves index determinism, NOT golden-byte ordering (Go map iteration is randomized); do NOT cite it as enforcing golden ordering — that is the repeated-capture byte-stability check's job (Task 3/02-04). These tests run against the pinned locked tree on every run; if the tree is absent (offline/not-fetched), they FAIL — that loud failure is the intended D-10/FIXT-03 posture, and the plan's precondition (`task corpora:fetch && corpora:assert`) is how the executor makes them pass in this environment (the four locked corpora are already fetched locally per the environment probe).
    - Re-synchronize the matrix prose to the fail-loud posture (MED: matrix prose staleness). In the same diff (02-03 establishes the fail-loud posture), update the gap strings in `internal/indexer/capability/matrix.go` that currently claim Python/TSJS "self-skips by default" and Java/C# use "source-as-spec plus fallback" (lines ~113,136,147 and any TSJS copies) to state they resolve the locked corpus and FAIL loudly, and mirror the same text verbatim into `docs/LANGUAGE-CAPABILITY-MATRIX.md` so `TestMatrix_DocMirrorsDescriptor` (verbatim string equality) stays green. This is a factual synchronization tied to this plan's posture change, not a Phase-4 sweep offramp.
    - In `TestCorpusBehaviorLockedCorpora` (behavioral_test.go): fill in the locked-corpus `t.Run` subtests this plan's predecessor left as a scaffold — one `t.Run` per locked corpus (hugo, guava, serilog, requests) resolving via `lockedCorpusDir` and running `buildEngineAt` + the shape-only assertions (`assertExploreShape` over a representative multi-word query, `assertNodeMultiDefShape` over a representative symbol), asserting non-empty output / no crash. This is the D-09 property-shape tier over the locked corpora; the byte-level goldens are the re-freeze's regression snapshots (02-04).
    - Update the CLI==MCP trio case lists in behavioral_test.go: retire the weft-gated rows (deleted in 02-02) and add locked-corpus + corpus/behavioral rows — `{name:"corpus/behavioral", src: corpus/behavioral/src, query:"user account"}` and one per locked corpus (`{name:"hugo", src: lockedCorpusDir(t,"go"), ...}`, etc.) so the CLI==MCP byte-identity oracle covers both the behavioral and locked surfaces.
    - Precondition for this task's verify: `task corpora:fetch` + `task corpora:assert` (4/4) — the locked trees must be present and integrity-checked before the tests run (T-02-04). Declare and assert this precondition at the start.
    - No `t.Skip` added anywhere in these tests (constraint 2). No `[ci skip]` / `[skip ci]`; `-count=1`.

    Commit: conventional `test(02-03):` naming the cause (D-10): locked-corpus tests resolved hermetically through internal/corpora, failing loudly instead of skipping.
  </action>
  <verify>
    <automated>task corpora:assert && go test -count=1 ./internal/indexer/capability/... && go test -count=1 ./... && go test -count=1 ./testdata/golden/... -run 'TestCorpusBehavior|TestPriorityLanguagesResolveToLockedCorpus'</automated>
  </verify>
  <done>
    - The five priority languages resolve via the committed language->slug map through the shared `lockedCorpusDir` helper (internal/corpora); no env-var-default first, no `t.Skip`, no `e.Language == language` false-miss on tsjs (H3).
    - `TestPriorityLanguagesResolveToLockedCorpus` proves all five languages (go/java/csharp/python/tsjs) resolve to a locked corpus and fails, not skips, if one does not (rule 84d1gfpywd).
    - Shape/coverage assertions retained and green with corpora present; aggregate-count-check wording does NOT claim to enforce golden ordering (LOW).
    - Removing/hiding a locked corpus turns these tests red (a mutation-by-absence rehearsal is recommended: temporarily point `lockedCorpusDir` at a bogus dir and confirm `t.Fatalf` — then revert) — proving they fail loudly, never pass vacuously.
    - matrix.go + docs/LANGUAGE-CAPABILITY-MATRIX.md gap prose re-synchronized to the fail-loud posture; TestMatrix_DocMirrorsDescriptor still green (MED).
    - CLI==MCP trio case lists now include corpus/behavioral + locked corpora.
  </done>
  <acceptance_criteria>
    - `rg -n "t\.Skip" testdata/golden/behavioral_test.go testdata/golden/behavioral_java_test.go testdata/golden/behavioral_tsjs_test.go testdata/golden/behavioral_csharp_test.go testdata/golden/behavioral_python_test.go` returns nothing.
    - `rg -n "CODEGRAPH_(JAVA|TSJS|CSHARP|PYTHON)_CORPUS" testdata/golden/` returns nothing.
    - `go test -count=1 ./testdata/golden/... -run TestCorpusBehavior_Java -v` reports it indexed the guava locked tree (non-zero kind counts) and passed.
    - `go vet ./testdata/golden/...` clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add a byte-identity/non-empty/marker guard over every re-frozen golden, and a golden:regen Taskfile target</name>
  <files>
    testdata/golden/golden_test.go
    Taskfile.yml
  </files>
  <read_first>
    testdata/golden/golden_test.go   # TestGoldenFixturesExist / TestGoSideFixturesRegenerated (65-177) — the existing existence/volatile guard pattern
    Taskfile.yml                     # test:golden (line 59) + corpora:fetch / corpora:assert (3268-3560) — where golden:regen sits
  </read_first>
  <action>
    Per A3 (research) + T-02-03 + H5: a bare or missing re-frozen golden must not read as satisfied, and a glob-over-existing guard is VACUOUS against a missing golden (rule 84d1gfpywd). Add `TestReFrozenGoldensValid` to testdata/golden/golden_test.go (same package) that enumerates the EXPECTED golden set from an AUTHORITATIVE source — the committed gocapture spec table (the per-corpus expected filenames from Task 1: per locked corpus, {explore, node, explore-multi, node-multi, explore-mcp, node-mcp}; plus the behavioral corpus's {go-explore-multi, go-node-multi, + any CLI/MCP behavioral goldens}) — NEVER from `filepath.Glob` of existing files. For each expected golden the guard:
      1. asserts the file EXISTS (a missing expected golden fails the suite — no "assert existing OR gate it" escape);
      2. asserts it is non-empty and byte-prefixed with the `{` envelope marker;
      3. parses it as the `goldenCapture{Command,Output}` envelope and asserts a non-empty `output` for command-invocation goldens (the COVERAGE/empty resolution: a corpus case that genuinely yields zero output still ships a syntactically-valid, non-empty-envelope document);
      4. positively asserts a COUNT of goldens verified (e.g. `t.Logf`/explicit len check) so a guard that ran over zero expected goldens fails instead of passing (H5).
    This plan (03) ships the guard with the behavioral expected set (which exists at 03) so it is immediately non-vacuous; 02-04 extends the expected set to the full locked+behavioral set AFTER the capture so the strict completeness check (every expected locked golden present) turns on. Do NOT declare the guard "complete" at 03 with only current fixtures globbed — the authoritative expected set is what makes it non-vacuous.

    Add a `golden:regen` Taskfile target (the planner-required one-command "regenerate all goldens", from research Pattern 2): its `cmds` run, in order, `task corpora:fetch`, `task corpora:assert`, and `go run ./testdata/golden/gocapture`. Give it a `desc` naming it the human-reviewed re-freeze entrypoint. Do NOT give it an in-place-`-update`-style silent flag; regenerate-then-review is the discipline (the wire-oracle precedent, 01-03). Keep the commit message clean of `[ci skip]`/`[skip ci]` (f18zrdsgx5). Every test command carries `-count=1`.

    Commit: conventional `test(02-03):` naming the cause (A3/T-02-03): byte-identity/non-empty marker guard over every re-frozen golden + golden:regen target.
  </action>
  <verify>
    <automated>task golden:regen --help >/dev/null 2>&1; go test -count=1 ./testdata/golden/... -run TestReFrozenGoldensValid -v && go test -count=1 ./...</automated>
  </verify>
  <done>
    - `TestReFrozenGoldensValid` enumerates the EXPECTED golden set from the gocapture spec table (not a glob); every expected golden in the currently-active set is asserted to exist, be non-empty, carry the `{` marker, parse, and a positive verified-count is reported (H5). The behavioral expected set is active and non-vacuous at 03.
    - `golden:regen` target runs fetch -> assert -> gocapture and is the documented one-command re-freeze entrypoint; gocapture fails closed on a missing locked/behavioral source so a skip cannot leave stale goldens green (MED).
    - The guard and target keep `go test -count=1 ./...` green this plan; the strict full-set completeness check is armed only by 02-04's post-capture extension.
  </done>
  <acceptance_criteria>
    - `rg -n "golden:regen" Taskfile.yml` shows a target whose body is `task corpora:fetch`, `task corpora:assert`, `go run ./testdata/golden/gocapture` in order.
    - `rg -n "TestReFrozenGoldensValid" testdata/golden/golden_test.go` present, and its body references an authoritative expected list by name (the spec table const), NOT a `filepath.Glob` over existing fixtures.
    - `go test -count=1 ./testdata/golden/... -run TestReFrozenGoldensValid -v 2>&1 | grep -i "goldens verified"` shows a positive, non-zero count.
    - `go test -count=1 ./internal/indexer/capability/... -run TestMatrix_DocMirrorsDescriptor` passes (matrix/doc prose re-synced to fail-loud posture, MED).
    - `task --list | grep golden:regen` resolves.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched locked corpus (out-of-tree) -> gocapture / tests | Untrusted third-party source tree, read only after the four-part integrity check; Directory-injection risk if resolution trusts a path without verification |
| capture write -> committed golden path | A capture failure could truncate/misplace a golden if written in place |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Spoofing/supply-chain | capture path | high | mitigate | gocapture extended on the Go-side path only; MCP-surface capture uses the in-process BuildServer + go-sdk client already in-tree — never a live TS server or mcp-capture.mjs (T-02-02). No new dependency/install. |
| T-02-03 | Tampering (integrity) | golden write path | high | mitigate | writeCapture converted to capture-to-temp-then-move with non-empty + `{` marker assertion before os.Rename onto the committed path (Pattern 2, carryover 01-03); TestReFrozenGoldensValid provides the positive guard that a bare/missing golden fails loudly. |
| T-02-04 | Tampering | locked-tree resolution | high | mitigate | gocapture and the hermetic tests resolve only via internal/corpora; the Taskfile/`golden:regen` and Task 2 precondition run `task corpora:assert` (four-part: real .git, HEAD==sha, clean tree, HEAD^{tree}) before any locked tree is read (D-12 seam, first-wins). |
| T-02-01 | Tampering | sweep boundary | low | accept | Not active in this tooling/test plan (no file sweep); carried for completeness. |
| T-02-SC | Tampering | go.mod / installs | low | accept | No dependency added; gocapture depends only on in-repo packages. Package-legitimacy gate not triggered. |
</threat_model>

<verification>
[Phase-level checks this plan satisfies]
- FIXT-06's mechanism: gocapture extended to the sole capture authority, resolving locked corpora via internal/corpora (never hardcoded SHAs), capturing CLI+MCP surfaces for explore+node, temp-then-move write.
- The one-command `golden:regen` re-freeze entrypoint exists.
- D-10 observed: hermetic locked-corpus resolution, fail loud, no skip.
- Positive guard over every re-frozen golden (A3, T-02-03).
</verification>

<success_criteria>
[Measurable completion]
- gocapture (extended) builds/vets and resolves all four locked corpora via internal/corpora.
- Behavioral/locked-corpus tests run against the real pinned trees and fail loudly, not skip.
- TestReFrozenGoldensValid + golden:regen target present; full + golden suites green this plan.
</success_criteria>

<output>
Create `.planning/phases/02-golden-harness-re-authoring-re-freeze/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase plan produces

- **New test symbols**: `TestReFrozenGoldensValid` (expected-set enumeration + non-empty + `{` marker + parse + positive verified-count guard, H5), `lockedCorpusDir(t, language) string` (shared hermetic resolver via the language map), `TestPriorityLanguagesResolveToLockedCorpus` (all five priority languages resolve, fail-not-skip, H3), locked-corpus `t.Run` subtests inside `TestCorpusBehaviorLockedCorpora`, updated CLI==MCP case-list rows (corpus/behavioral + locked corpora).
- **New data/config symbol**: the committed language->locked-slug map (`go->hugo, tsjs->hugo, java->guava, csharp->serilog, python->requests`), shared by gocapture resolution, `lockedCorpusDir`, and the completeness guard (H3).
- **Renamed/retired test symbols**: `resolveJavaCorpus`, `resolveTSJSCorpus`, `resolveCSharpCorpus`, `resolvePythonCorpus` (deleted; replaced by `lockedCorpusDir`); env vars `CODEGRAPH_{JAVA,TSJS,CSHARP,PYTHON}_CORPUS` no longer read.
- **Modified**: `testdata/golden/gocapture/main.go` (lockedCorpusSpec + behavioralCorpusSpec + per-spec outDir + temp-then-move writeCapture + CLI+MCP surface capture with temp indexed-repo staging + fail-closed missing-source), `Taskfile.yml` (new target `golden:regen`), `testdata/golden/golden_test.go` (TestReFrozenGoldensValid), the four behavioral_*_test.go + behavioral_test.go, `internal/indexer/capability/matrix.go` + `docs/LANGUAGE-CAPABILITY-MATRIX.md` (fail-loud posture prose re-sync, MED).
- **Not yet produced here**: the re-frozen golden bytes (02-04 runs the capture and arms the guard's strict full-set completeness check; at 03 the behavioral expected set is active and non-vacuous).
