---
phase: 02-golden-harness-re-authoring-re-freeze
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - testdata/golden/corpus/*/ (re-frozen locked-corpus goldens, net-new from gocapture)
  - corpus/behavioral/go-explore-multi.json
  - corpus/behavioral/go-node-multi.json
  - corpus/behavioral/*-multi.json / *-mcp.json (CLI+MCP re-captured behavioral goldens)
  - testdata/golden/golden_test.go (if the final-form guard's expected-fixture list is asserted)
autonomous: false
requirements: [FIXT-06]
user_setup:
  - service: none (network +git only if corpora not already fetched)
    why: "The re-freeze capture reads the locked corpora at their pinned SHAs."
    precondition: "task corpora:assert (4/4) — the four locked trees must be present and integrity-checked before gocapture reads them (T-02-04)"
estimate:
  tokens: 75000
  raw_tokens: 37500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every golden under testdata/golden/corpus/ and corpus/behavioral/ was produced by gocapture from codegraph-go's own output against the locked corpora / the committed behavioral corpus (FIXT-06). No golden is a TS-captured fixture and none derives from a deleted corpus."
    - "The re-freeze diff changes NO identifier: no test function, file name, or directory name is renamed or removed in this diff — every changed line traces to the single cause 're-captured from codegraph-go's own output against the locked corpora' (D-06 criterion 2, constraint 3)."
    - "Every re-frozen golden is non-empty and carries its {command,output} envelope marker (T-02-03, A3) — TestReFrozenGoldensValid passes over the complete set."
    - "The re-freeze is reproducible: re-running golden:regen against the same locked trees reproduces byte-identical goldens (COVERAGE/FIXT-06 ordering) — a non-deterministic order is a capture failure, not a random re-baseline."
    - "Both explore and node, on both the CLI and MCP surfaces, are re-frozen (ROADMAP Notes)."
    - "Byte-equality is the oracle: the re-freeze satisfies the byte-identity guard and the goldens are compared byte-for-byte (COVERAGE/encoding)."
    - "Re-captured goldens with zero output cannot read as satisfied: the envelope is still a valid non-empty {command,output} document (COVERAGE/FIXT-06 empty)."
  artifacts:
    - testdata/golden/corpus/<slug-or-lang>/*.json (locked-corpus goldens: CLI explore/node + multi + MCP, per corpus)
    - corpus/behavioral/go-explore-multi.json / go-node-multi.json (re-frozen from gocapture, kept byte-stable or re-baselined)
  key_links:
    - "gocapture output <-> TestReFrozenGoldensValid (byte-identity/non-empty/marker guard)"
    - "re-freeze diff <-> the one-diff-one-cause invariant (rename diff changed no golden byte; this diff changes no identifier)"
  prohibitions:
    - "This diff MUST NOT rename, delete, or add any identifier or test file — it may change only golden JSON bytes and, at most, touch golden_test.go's expected-fixture bookkeeping (D-06 constraint 3). Any identifier change is a diff-attribution violation and must be pulled into 02-03 or rejected."
    - "Never re-add a TS/capture dependency or re-execute unvetted binaries (T-02-02); the capture is 100% gocapture on the Go-side path."
    - "Never put [ci skip] / [skip ci] in the commit message (f18zrdsgx5); every test command carries -count=1."
---

<objective>
Run the second reviewed diff's capture and land it as ONE attributable re-freeze diff (FIXT-06, Diff B part 2): execute `task golden:regen` against the integrity-checked locked corpora + the committed behavioral corpus, have gocapture write every golden to temp then move it into place (T-02-03), review the resulting golden-byte diff so every changed line traces to the single cause "re-captured from codegraph-go's own output against the locked corpora" and NO identifier moved (D-06 criterion 2, constraint 3), and commit it.

Purpose: This is the re-baseline. It is deliberately the LAST step and a human-reviewed diff (autonomous: false with a review gate) because a re-baseline that authors and approves its own change in the same breath certifies its own oracle — the exact thing Phase 3 / FIXT-07 exists to distrust. Criterion 2's attributability is only preserved if the rename diff (02-01/02-02) moved no golden byte and this diff moves no identifier, so this plan's review gate verifies BOTH halves of that invariant before commit.

Output: A committed re-freeze diff whose every changed line is a golden byte attributed to the capture, with the full + golden suites green under the byte-identity guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-CONTEXT.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-RESEARCH.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-PATTERNS.md

# Prior-plan SUMMARY context — the reviewed-diff discipline this plan executes
@.planning/phases/01-corpus-selection-by-measurement/01-03-SUMMARY.md   # how the wire-oracle transcript was re-frozen (temp->assert->move) and the diff kept attributable
# [from Phase 1] 01-03's re-freeze is the exact precedent: capture to temp, assert non-empty + marker, then move; review the diff as one named-cause change.

# Surfaces the capture reads/writes
@testdata/golden/gocapture/main.go      # extended in 02-03; this plan runs it
@corpora/manifest.json                  # the four locked entries (sole pin authority)
@internal/corpora/manifest.go           # Entry.Dir/CorpusRoot/LockedEntries the gocapture resolver uses
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run the re-freeze capture and assert every golden is non-empty, marker-bearing, and covered by the byte-identity guard</name>
  <files>
    testdata/golden/corpus/*/ (locked-corpus goldens)
    corpus/behavioral/go-explore-multi.json
    corpus/behavioral/go-node-multi.json
    corpus/behavioral/*-multi.json
    corpus/behavioral/*-mcp.json
  </files>
  <read_first>
    testdata/golden/gocapture/main.go   # the extended specs + temp-then-move writeCapture this plan invokes
  </read_first>
  <action>
    Precondition (assert first, halt on unmet): `task corpora:assert` exits 0 reporting 4/4 (the four locked trees present and integrity-checked — T-02-04). If it does not, halt: DO NOT run gocapture against an unverified or absent set.
    - Run the one-command re-freeze entrypoint: `task golden:regen` (which runs `task corpora:fetch` (idempotent), `task corpora:assert`, then `go run ./testdata/golden/gocapture`). Each golden is written by the 02-03-extended writeCapture to a temp path, asserted non-empty + `{` marker, then moved onto its committed path — so a bare/half-written golden fails the capture rather than landing (T-02-03).
    - After the run, assert the capture produced the complete expected set: every locked-corpus golden exists (one CLI `*`/`*-multi` + `*-mcp` pair per locked corpus for explore and node), the behavioral corpus goldens exist under `corpus/behavioral/`, and NOTHING under `testdata/golden/corpus/` was left from the deleted corpora (weft-go / colbymchenry-codegraph must not reappear). Any locked-corpus golden that gocapture hard-failed on (the 02-03 hard-error for a bare locked golden) is a halt — a missing locked golden is not an acceptable outcome this plan.
    - Run the full + golden suites: `go test -count=1 ./...` and `go test -count=1 ./testdata/golden/...` must both be green, including `TestReFrozenGoldensValid` (the byte-identity/non-empty/marker guard over every re-frozen golden). The deterministic capture is implicitly verified by the determinism assertions in the behavioral/locked tests (COVERAGE/ordering).
    - Do NOT commit yet — the diff review is Task 2, and this diff must be reviewed as one attributable change before it is committed.
    - No `[ci skip]` / `[skip ci]`; `-count=1` everywhere.
  </action>
  <verify>
    <automated>task corpora:assert >/dev/null && task golden:regen && go test -count=1 ./... && go test -count=1 ./testdata/golden/...</automated>
  </verify>
  <done>
    - task corpora:assert reported 4/4 before capture.
    - task golden:regen completed with no locked-corpus hard-failure; every expected golden exists and is non-empty + marker-bearing.
    - Both go test invocations green, including TestReFrozenGoldensValid over the complete set.
  </done>
  <acceptance_criteria>
    - `find testdata/golden/corpus -name '*.json' | wc -l` equals the expected locked-corpus golden count (CLI multi + MCP per corpus), and no path contains `weft` or `colbymchenry`.
    - `python3 -c "import sys,json,glob;[json.load(open(p)) for p in glob.glob('testdata/golden/corpus/*/*.json')+glob.glob('corpus/behavioral/*.json')]"` — all goldens parse.
    - `go test -count=1 ./testdata/golden/... -run TestReFrozenGoldensValid -v` passes.
    - `test -d testdata/golden/corpus && ! find testdata/golden/corpus -iname '*weft*' | grep -q . && ! find testdata/golden/corpus -iname '*colbymchenry*' | grep -q .`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Review the re-freeze diff for single-cause attribution and zero identifier change, then commit it as one reviewed diff</name>
  <files>
    testdata/golden/corpus/*/
    corpus/behavioral/
    testdata/golden/golden_test.go
  </files>
  <read_first>
    testdata/golden/golden_test.go   # the final-form TestReFrozenGoldensValid expected-fixture list (if this plan updates its bookkeeping)
    testdata/golden/golden_parity_test.go -> behavioral_test.go (must NOT appear in this diff)
  </read_first>
  <action>
    The re-freeze is the second reviewed diff; criterion 2 / constraint 3 require every changed line trace to one named cause and require that this diff changes NO identifier. Review before commit:
    - Identify the changed set: `git status --short` and `git diff --stat`. Confirm the diff touches ONLY golden JSON files under `testdata/golden/corpus/**` and `corpus/behavioral/**`, plus at most `testdata/golden/golden_test.go` IF the final form of `TestReFrozenGoldensValid` needs its expected-fixture bookkeeping updated (a non-identifier test-body bookkeeping edit, permissible in this diff). The diff MUST NOT touch any `*_test.go` beyond that bookkeeping, any non-.json source under testdata/golden/, gocapture/main.go, Taskfile.yml, or internal/.
    - Prove NO identifier changed: run `git diff --name-only -- 'testdata/golden/*_test.go' 'testdata/golden/gocapture/main.go' 'Taskfile.yml' 'internal/**'` and confirm it is empty (or, for golden_test.go, that the only change is the expected-fixture bookkeeping, not an identifier rename). Also `git diff --name-status` must show NO renames (no `R` lines) in this diff.
    - Prove every changed golden byte is a capture: `git diff --stat` line count per golden should be a full-file rewrite of the `{command,output}` envelope (a re-freeze replaces the whole golden content from Go output), and NO change should introduce or remove a comparison-framing string (grep the diff for any reintroduced parity/vs-TS framing — it must be absent).
    - Sanity the reproducibility claim: re-run `task golden:regen` once more and confirm `git diff` is unchanged (idempotent / deterministic capture → byte-stable goldens, COVERAGE/ordering). If the second run produces different bytes, the capture is non-deterministic — halt and investigate before committing (a flaky re-baseline is exactly what FIXT-03/ordering forbids).
    - Commit the single named-cause diff: conventional `test(02-04):` with NO `[ci skip]` / `[skip ci]` (f18zrdsgx5). Message states the single cause: re-frozen every golden from codegraph-go's own output against the locked corpora (FIXT-06); note the rename diff changed no golden byte and this diff changes no identifier, so attribution holds.

    This task ENDS with a blocking human-verify gate (the diff review): the re-freeze is the phase's `costly`/re-baseline decision, and criterion 2's attribution is a manual-review judgment — VALIDATION.md's Manual-Only Verifications table marks it as such. Do not consider the plan complete until the gate passes.
  </action>
  <verify>
    <automated>! git diff --name-only HEAD -- 'testdata/golden/*_test.go' | grep -v 'golden_test.go' | grep -q . && go test -count=1 ./... && go test -count=1 ./testdata/golden/...</automated>
  </verify>
  <done>
    - The re-freeze diff touches only golden JSONs (+ the one allowed golden_test.go bookkeeping edit, if needed); no identifier/file/directory was renamed (no `R` status lines).
    - A second `task golden:regen` reproduces byte-identical goldens (deterministic).
    - The single-cause commit landed with `-count=1` suites green; human diff-review gate passed.
  </done>
  <acceptance_criteria>
    - `git diff --name-status HEAD~1 HEAD` contains no `R` (rename) lines.
    - `git diff --name-only HEAD~1 HEAD -- 'testdata/golden/gocapture/main.go' 'Taskfile.yml' 'internal/**' 'testdata/golden/behavioral_test.go' 'testdata/golden/behavioral_java_test.go' 'testdata/golden/behavioral_tsjs_test.go' 'testdata/golden/behavioral_csharp_test.go' 'testdata/golden/behavioral_python_test.go'` is empty.
    - Re-running `task golden:regen` yields `git status` clean (no further diff).
    - Commit message contains no `[ci skip]`/`[skip ci]` and names FIXT-06 as the single cause.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human review of the re-freeze diff attribution</name>
  <what-built>
    The re-freeze reviewed diff (FIXT-06): every golden re-captured from codegraph-go's own output against the locked corpora by the extended gocapture, landed as one committed diff that changes no identifier.
  </what-built>
  <how-to-verify>
    The diff must satisfy the D-06 two-diff attribution invariant. Review:
    1. `git show --stat HEAD` — the changed set is ONLY golden JSON files under `testdata/golden/corpus/` and `corpus/behavioral/` (plus the allowed golden_test.go bookkeeping). No test file, gocapture/main.go, Taskfile, or internal/ file appears.
    2. `git show HEAD --stat | grep -i 'rename\|=>'` — confirm no file was renamed (no identifier/directory moved) in the re-freeze.
    3. Spot-read one re-frozen golden (`git diff HEAD~1 HEAD -- <a locked-corpus golden>`) — it is a full `{command,output}` envelope rewritten from Go output; the `"command"` line is a normal Go `explore`/`node` invocation, no TS/capture.sh/mcp-capture framing.
    4. `git diff HEAD~1 HEAD | grep -iE 'parity|vs TS|synthetic-parity|golden_parity|colbymchenry|weft-go'` — expect no returned framing lines (any hit is a violation).
    5. `go test -count=1 ./... && go test -count=1 ./testdata/golden/...` — both green.
    Confirm the rename diff (02-01/02-02) moved no golden byte and this diff moves no identifier, so the two-diff attribution holds end-to-end.
  </how-to-verify>
  <resume-signal>
    Type "approved" to confirm the re-freeze diff is attributable and green, or describe any framing/identifier/diff-scope issue to fix before it is final.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched locked corpus (out-of-tree) -> gocapture | Trusted-source-index-read of third-party trees; integrity must precede read |
| golden bytes -> committed path | The re-baseline permanently replaces old bytes; a bad write or non-deterministic capture is expensive to detect late |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering (integrity) | golden write path | high | mitigate | gocapture writes temp->assert(non-empty, `{`) -> move; Task 1 halts on any locked-corpus hard-failure; TestReFrozenGoldensValid asserts every golden is a valid non-empty envelope. A bare/missing golden cannot land or read as satisfied. |
| T-02-04 | Tampering | locked-tree resolution integrity | high | mitigate | Task 1 asserts `task corpora:assert` 4/4 (real .git, HEAD==sha, clean tree, HEAD^{tree}) BEFORE gocapture reads any locked tree (T-02-04). Never index an unverified checkout. |
| T-02-02 | Spoofing/supply-chain | capture path | medium | mitigate | Capture is 100% gocapture (Go-side); no TS dependency, no mcp-capture.mjs, no live TS server. Review gate greps the diff for reintroduced framing. |
| T-02-01 | Tampering | sweep boundary | low | accept | Not active in this re-baseline (no file sweep), carried for completeness. |
| T-02-SC | Tampering | go.mod / installs | low | accept | No dependency added; gocapture depends only on in-repo packages. |
</threat_model>

<verification>
[Phase-level checks this plan satisfies]
- FIXT-06: every golden produced by gocapture from Go output against the locked corpora, landed as one attributable reviewed diff.
- Criterion 2 / constraint 3: the re-freeze diff changes no identifier (review gate + grep) and every changed line traces to the single capture cause.
- The byte-identity oracle (TestReFrozenGoldensValid) and determinism (idempotent golden:regen) hold.
- Both explore and node, CLI and MCP, are re-frozen (ROADMAP Notes).
</verification>

<success_criteria>
[Measurable completion]
- task golden:regen reproduces byte-identical goldens; full + golden suites green with -count=1.
- The re-freeze committed as one diff with zero identifier changes and no framing reintroduced; human review gate passed.
- Every golden validated non-empty + marker-bearing by the byte-identity guard.
</success_criteria>

<output>
Create `.planning/phases/02-golden-harness-re-authoring-re-freeze/02-04-SUMMARY.md` when done
</output>

## Artifacts this phase plan produces

- **Re-frozen goldens** (net-new from gocapture, per locked corpus hugo/guava/serilog/requests): CLI `*`/`*-multi` + `*-mcp` explore and node JSON envelopes under `testdata/golden/corpus/<slug-or-lang>/`.
- **Re-frozen behavioral goldens**: `corpus/behavioral/go-explore-multi.json`, `corpus/behavioral/go-node-multi.json`, plus any re-captured CLI+MCP behavioral goldens.
- **Modified (bookkeeping only, if needed)**: `testdata/golden/golden_test.go` expected-fixture list in TestReFrozenGoldensValid.
- **No new test names, no file renames, no new source identifiers** — this diff is golden bytes only (D-06 constraint 3, verified by review).
