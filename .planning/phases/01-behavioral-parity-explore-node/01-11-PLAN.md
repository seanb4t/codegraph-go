---
phase: 01-behavioral-parity-explore-node
plan: 11
type: tdd
wave: 3
depends_on: ["01-03"]
files_modified:
  - internal/query/expand.go
  - internal/query/expand_test.go
autonomous: true
requirements: [EXPL-02]

must_haves:
  truths:
    - "Type-hierarchy expansion pulls ancestors/descendants via extends/implements within a ceil(maxNodes/4) budget over 2 passes"
    - "BFS traversal is bounded by maxNodes=200, traversalDepth=3, minScore=0.2, searchLimit=8 (explore's overrides)"
    - "Glue-node injection adds callers+callees of each root ONLY when they live in a file the subgraph already surfaces, capped at GLUE_NODE_CAP=60"
  artifacts:
    - "internal/query/expand.go (H10 type-hierarchy, H11 BFS bounds, H12 glue-node injection)"
  key_links:
    - "gathered candidates -> expansion -> bounded subgraph {nodes, edges, roots} consumed by RWR (plan 06) + scoring (plan 13)"
---

<objective>
Port the subgraph-construction heuristics — H10, H11, H12 (RESEARCH §C.2) — that turn the gathered candidate set into the bounded subgraph RWR ranks over. These are the DoS-bounding stage (the maxNodes/GLUE caps the RESEARCH Security Domain requires be ported alongside RWR).

Heuristics (constants VERBATIM):
- H10 Type-hierarchy expansion: class/interface/struct/trait/protocol; budget `ceil(maxNodes/4)`; 2 passes (focal ancestors/descendants via extends/implements, then newly-found parents' siblings).
- H11 BFS traversal bounds: `maxNodes=200, traversalDepth=3, minScore=0.2, searchLimit=8` (explore's overrides of the defaults).
- H12 Glue-node injection: callers+callees of each root, ONLY if in a file the subgraph already surfaces; `GLUE_NODE_CAP=60`.

Pure functions over a graphstore.Reader + a candidate/root set — unit-testable against a synthetic index. Depends on plan 03 (tokenizers, if the expansion needs re-tokenization) and consumes the gather output shape at wire time (plan 16). New file expand.go — no same-wave file conflict.

Note H10 now benefits directly from D-09: the new extends edges (plan 05) make type-hierarchy expansion real for Go/Java/C# where before only implements existed.

Purpose: bounds the graph RWR runs on (T-01-10 mitigation) and injects the structural context that makes graph-relevance beat lexical.
Output: internal/query/expand.go + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/query/traverse.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN type-hierarchy expansion (H10) + BFS bounds (H11)</name>
  <files>internal/query/expand.go, internal/query/expand_test.go</files>
  <read_first>
    - 01-RESEARCH.md §C.2 rows H10 (type kinds, ceil(maxNodes/4) budget, 2-pass ancestors/descendants+siblings) and H11 (maxNodes=200/traversalDepth=3/minScore=0.2/searchLimit=8)
    - internal/query/traverse.go Impact BFS (bounded frontier expansion + WR-04 dangling-skip to mirror)
    - 01-PATTERNS.md "internal/query/rwr.go" analog notes (BuildReverseAdjacency / Impact BFS)
  </read_first>
  <behavior>
    - a focal class -> its extends/implements ancestors AND descendants added, capped at ceil(200/4)=50
    - BFS stops at depth 3, at 200 nodes, and prunes candidates with score < 0.2
    - a second pass adds newly-found parents' siblings (per §C.2 H10 2-pass rule)
  </behavior>
  <action>
    Create expand.go (package query). Implement H10 type-hierarchy expansion over the type kinds in §C.2: pass 1 walks extends/implements ancestors and descendants of focal type nodes; pass 2 adds newly-found parents' siblings; bound the whole expansion to ceil(maxNodes/4) nodes. Implement H11 BFS with explore's exact override bounds (maxNodes=200, traversalDepth=3, minScore=0.2, searchLimit=8), mirroring Impact's bounded frontier + WR-04 dangling-skip. Keep deterministic frontier ordering (sort). Write RED tests asserting the budget cap, the depth/node/minScore bounds, and the 2-pass sibling addition first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestTypeHierarchyExpansion|TestBFSBounds' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: expand.go implements type-hierarchy expansion (ceil(maxNodes/4), 2-pass) and BFS with maxNodes=200/depth=3/minScore=0.2/searchLimit=8
    - behavior: expansion respects the budget cap; BFS respects all four bounds
    - test-command: `go test ./internal/query/ -run 'TestTypeHierarchyExpansion|TestBFSBounds' -count=1`
  </acceptance_criteria>
  <done>H10 type-hierarchy expansion + H11 bounded BFS land with verbatim constants, deterministic, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN glue-node injection (H12) with GLUE_NODE_CAP</name>
  <files>internal/query/expand.go, internal/query/expand_test.go</files>
  <read_first>
    - 01-RESEARCH.md §C.2 row H12 (callers+callees of each root, same-file-only constraint, GLUE_NODE_CAP=60)
    - internal/query/traverse.go BuildReverseAdjacency (for caller lookup) + Callers/callees
  </read_first>
  <behavior>
    - a root's caller living in a file already in the subgraph -> injected as a glue node
    - a root's caller in a file NOT in the subgraph -> NOT injected
    - total glue nodes capped at 60
  </behavior>
  <action>
    Implement H12: for each subgraph root, gather its callers and callees (reuse BuildReverseAdjacency for callers), and inject each ONLY if it lives in a file the subgraph already surfaces (same-file-only constraint per §C.2), stopping at GLUE_NODE_CAP=60 total glue nodes. Deterministic ordering for which glue nodes are kept when the cap binds (sort by Id). Write RED tests for the same-file constraint and the cap first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestGlueNodeInjection|TestGlueNodeCap' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: expand.go injects only callers/callees in already-surfaced files, capped at GLUE_NODE_CAP=60
    - behavior: off-subgraph-file callers are excluded; the cap binds deterministically
    - test-command: `go test ./internal/query/ -run 'TestGlueNodeInjection|TestGlueNodeCap' -count=1`
  </acceptance_criteria>
  <done>H12 glue-node injection lands with the same-file constraint + GLUE_NODE_CAP=60, deterministic, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| candidate set -> subgraph expansion | In-memory expansion bounded by explicit caps before RWR |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Denial of Service | unbounded subgraph growth | high | mitigate | maxNodes=200, traversalDepth=3, minScore=0.2, ceil(maxNodes/4) hierarchy budget, and GLUE_NODE_CAP=60 all ported verbatim — these are exactly the caps that bound the RWR input (RESEARCH Security Domain: "do not port RWR without its bounds"); asserted by the bound/cap tests |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — reuses traverse.go BFS/adjacency patterns |
</threat_model>

<verification>
- `go test ./internal/query/ -run 'TestTypeHierarchyExpansion|TestBFSBounds|TestGlueNodeInjection|TestGlueNodeCap' -count=1` green.
- All caps (200/3/0.2/8, ceil(200/4), 60) enforced.
</verification>

<success_criteria>
Type-hierarchy expansion, bounded BFS, and glue-node injection (H10-H12) are ported with verbatim TS caps that bound the RWR input, benefiting from the new D-09 extends edges, TDD-verified.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-11-SUMMARY.md` when done.
</output>
