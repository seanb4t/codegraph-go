---
phase: 01-behavioral-parity-explore-node
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/golden/capture.sh
  - testdata/golden/corpus/synthetic-parity/
  - testdata/golden/README.md
autonomous: true
requirements: [TEST-01, EXPL-05, NODE-04]
user_setup:
  - service: ts-codegraph
    why: "D-01 time-sensitive golden capture — the live TS 1.3.1 CLI + its stdio MCP server are the parity oracle; capture must run while TS 1.3.1 is still installed"
    dashboard_config:
      - task: "Confirm TS 1.3.1 on PATH before capture: `codegraph --version` must print 1.3.1"
        location: "/opt/homebrew/lib/node_modules/@colbymchenry/codegraph (confirmed present)"

must_haves:
  truths:
    - "TS 1.3.1 golden outputs exist for multi-word explore, overloaded node, Test*-heavy, and structural-beats-lexical cases on BOTH the TS CLI and TS MCP surfaces"
    - "A synthetic-parity corpus exists exercising exactly v0.1's blind spot (multi-def enumeration + multi-file ranking), separate from the pinned real corpora"
  artifacts:
    - "testdata/golden/corpus/synthetic-parity/ (source tree + captured TS goldens)"
    - "testdata/golden/capture.sh (extended with behavioral + MCP-surface invocations)"
    - "testdata/golden/README.md (updated provenance for the new behavioral fixtures)"
  key_links:
    - "capture.sh MCP driver -> TS stdio codegraph_explore/codegraph_node (the oracle the Go harness diffs against in plan 17)"
---

<objective>
Land the behavioral fixture harness and the TS 1.3.1 ground-truth oracle FIRST (D-06: fixtures land before/with the algorithm; template-parity != behavior-parity). This plan does NOT touch runtime Go code — it builds the test infrastructure the entire phase is validated against.

Three deliverables: (1) a purpose-built synthetic-parity corpus that exercises the exact v0.1 blind spots (overloaded/same-named symbols, multi-word queries, Test*-heavy weakly-connected code, structurally-connected-beats-lexical); (2) capture.sh extended with NEW behavioral invocations (multi-word `explore` WITHOUT `--max-files 1`, overloaded `node` WITHOUT `-f`) plus a TS-MCP-surface capture path (No Analog Found — new driver); (3) a capture run producing the committed TS 1.3.1 goldens on both corpora + the synthetic set, on both the CLI and MCP surfaces.

Purpose: Without a captured oracle, EXPL-02's RWR port has nothing to prove parity against. This is the single highest-risk contract in the phase (STATE.md blocker) and MUST exist before ranking work begins.
Output: extended capture.sh, synthetic-parity corpus + TS goldens, updated README.

Artifacts this plan produces: testdata/golden/corpus/synthetic-parity/ (new), extended capture.sh, TS CLI+MCP golden JSON envelopes for the new behavioral cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-behavioral-parity-explore-node/01-CONTEXT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@testdata/golden/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the synthetic-parity corpus source tree</name>
  <files>testdata/golden/corpus/synthetic-parity/src/, testdata/golden/corpus/synthetic-parity/README.md</files>
  <read_first>
    - testdata/golden/corpus/weft-go/explore.json + node.json (the JSON envelope shape to mirror — role-match analog per PATTERNS.md)
    - 01-CONTEXT.md D-03 (the four required fixture cases: a/b/c/d)
  </read_first>
  <action>
    Create a small self-contained source tree under testdata/golden/corpus/synthetic-parity/src/ (Go is fine — the priority-4 reference language) that deliberately contains all four D-03 cases in ONE indexable project: (a) an OVERLOADED name — at least two distinct top-level definitions sharing an identical symbol name across different files, so `node <name>` returns 2+ defs; (b) a symbol reachable via a MULTI-WORD query (e.g. a `UserAccountManager` type so `explore user account` must tokenize CamelCase/plain and match); (c) a Test*-heavy, weakly-connected cluster — a `Test*`-named function with a name-lexical match to the query but ZERO inbound graph edges, alongside a structurally-connected non-test symbol, so the file-relevance gate (EXPL-03) has something to prune; (d) a structural-beats-lexical pair — symbol A whose NAME loosely matches the query but is graph-isolated, and symbol B that does NOT name-match but is heavily called-by/calls the query's true target, so RWR must rank B above A. Add a synthetic-parity/README.md documenting which files embody which case (a/b/c/d) so plan 17's assertions can target them by name. Do NOT hand-write any expected output here — outputs are captured, never authored (README.md's frozen-ground-truth policy).
  </action>
  <verify>
    <automated>test -d testdata/golden/corpus/synthetic-parity/src && grep -v '^#' testdata/golden/corpus/synthetic-parity/README.md | grep -c -iE 'overload|multi-word|test|structural' | awk '$1>=4{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - source: testdata/golden/corpus/synthetic-parity/src/ contains 2+ files with an identical duplicated symbol name (case a)
    - behavior: indexing the corpus with the current Go binary produces a graph where the duplicated name has 2+ nodes and case-(d)'s symbol B has strictly more graph edges than symbol A
    - test-command: `go run ./cmd/codegraph index --force -q testdata/golden/corpus/synthetic-parity && go run ./cmd/codegraph node <dup-name> -p testdata/golden/corpus/synthetic-parity` lists 2+ definitions
  </acceptance_criteria>
  <done>The synthetic-parity corpus exists, indexes cleanly, and its README maps each source file to D-03 case a/b/c/d.</done>
</task>

<task type="auto">
  <name>Task 2: Extend capture.sh with behavioral + MCP-surface invocations</name>
  <files>testdata/golden/capture.sh</files>
  <read_first>
    - testdata/golden/capture.sh (the existing capture_repo function + wrap_text/strip_json helpers — extend, do NOT edit the existing --max-files 1 / -f baseline invocations)
    - 01-RESEARCH.md Pitfall 3 (the existing capture.sh under-captures) + Wave 0 Gaps (MCP-surface capture path)
    - 01-PATTERNS.md "Golden fixture capture — capture_repo extension pattern"
  </read_first>
  <action>
    Add NEW capture invocations to capture.sh (leave the existing `--max-files 1` explore and `-f` node calls untouched as the template-parity baseline). For each corpus (colbymchenry-codegraph, weft-go, synthetic-parity) add: a multi-word `explore` call WITHOUT `--max-files 1` (captured to `explore-multi.json` via wrap_text), and a bare `node <name>` call WITHOUT `-f` on a name with 2+ defs (captured to `node-multi.json`). Then add a TS-MCP-surface capture path (No Analog Found — this is new): drive the TS stdio MCP server programmatically and call the `codegraph_explore` and `codegraph_node` tools with the same queries, capturing to `explore-mcp.json` / `node-mcp.json`. Use a small Node harness script (testdata/golden/mcp-capture.mjs) that spawns the TS `codegraph` MCP server over stdio, sends a JSON-RPC `initialize` then `tools/call` for each tool, and writes the tool result text through the same wrap_text/strip_json normalization the CLI path uses. Route TS output through the EXISTING strip_json/strip_sql_timestamps normalizers so volatile fields (timestamps, absolute paths -> <CORPUS_PATH>) are canonicalized identically to the CLI fixtures (D-02 canonicalization). Gate the whole script on `codegraph --version` == 1.3.1 and hard-fail with a clear message if TS is absent (do not silently capture against a wrong version).
  </action>
  <verify>
    <automated>bash -n testdata/golden/capture.sh && grep -v '^#' testdata/golden/capture.sh | grep -c -E 'explore-multi|node-multi|mcp-capture|explore-mcp|node-mcp' | awk '$1>=3{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - source: capture.sh contains new invocations for explore-multi, node-multi, and an MCP driver (mcp-capture.mjs) producing explore-mcp/node-mcp
    - behavior: `bash -n capture.sh` parses; the existing baseline explore/node invocations are unchanged (still present)
    - test-command: `bash -n testdata/golden/capture.sh` exits 0
  </acceptance_criteria>
  <done>capture.sh gains behavioral (no-cap explore, no-f node) + TS-MCP-surface invocations, normalized identically to the CLI path, without altering the existing baseline calls.</done>
</task>

<task type="auto">
  <name>Task 3: Capture TS 1.3.1 goldens + update README provenance</name>
  <files>testdata/golden/corpus/**/explore-multi.json, testdata/golden/corpus/**/node-multi.json, testdata/golden/corpus/**/explore-mcp.json, testdata/golden/corpus/**/node-mcp.json, testdata/golden/README.md</files>
  <read_first>
    - testdata/golden/README.md (frozen-ground-truth / capture-protocol policy — extend it, do not contradict it)
    - 01-VALIDATION.md "Manual-Only Verifications" (the D-01 time-sensitive capture note)
  </read_first>
  <action>
    Run the extended `./capture.sh` with the live TS 1.3.1 `codegraph` on PATH to produce the committed TS golden envelopes for the new behavioral cases on all three corpora, both CLI and MCP surfaces. Commit the captured JSON as the frozen oracle. Update README.md to document: (1) the new synthetic-parity corpus and its D-03 case map; (2) the new behavioral fixtures (explore-multi/node-multi) and MCP-surface fixtures (explore-mcp/node-mcp) and what each proves; (3) that these are TS-side goldens ONLY — the Go-side expected fixtures are regenerated later (plan 17, F5) after the RWR pipeline + edge-kind re-index land. If TS 1.3.1 is NOT available at execution time, STOP and report — do not hand-author fixtures (README frozen-ground-truth policy); the goldens must come from the live oracle.
  </action>
  <verify>
    <automated>ls testdata/golden/corpus/synthetic-parity/explore-multi.json testdata/golden/corpus/synthetic-parity/node-multi.json && jq -e '.output' testdata/golden/corpus/synthetic-parity/node-multi.json >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - source: explore-multi/node-multi/explore-mcp/node-mcp JSON envelopes exist for each corpus and validate as JSON with an `.output` field
    - behavior: node-multi.json for the synthetic corpus shows TS enumerating 2+ definitions (multi-def header text present in .output)
    - test-command: `jq -e '.output | contains("definitions named")' testdata/golden/corpus/synthetic-parity/node-multi.json`
  </acceptance_criteria>
  <done>TS 1.3.1 goldens for all new behavioral + MCP cases are captured and committed; README documents provenance and the deferred Go-side regen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness -> TS MCP stdio | The capture driver spawns and reads an external process; output is normalized before commit |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | committed golden fixtures | medium | mitigate | Goldens captured only from the live TS 1.3.1 oracle (version-gated), never hand-authored — enforced by README frozen-ground-truth policy + the `codegraph --version` gate in capture.sh |
| T-01-02 | Information Disclosure | absolute paths / timestamps in fixtures | low | mitigate | All TS output routed through existing strip_json/strip_sql_timestamps normalizers (paths -> <CORPUS_PATH>, epoch-ms -> <EPOCH_MS>) before commit |
| T-01-SC | Tampering | package installs | low | accept | No npm/pip/cargo/go installs in this plan — no supply-chain surface introduced |
</threat_model>

<verification>
- `bash -n testdata/golden/capture.sh` parses.
- Synthetic corpus indexes with the current Go binary and exposes an overloaded name (2+ nodes) + a structural-beats-lexical edge asymmetry.
- TS goldens exist for explore-multi/node-multi/explore-mcp/node-mcp on all three corpora and validate as JSON.
</verification>

<success_criteria>
The behavioral oracle exists and is committed BEFORE any ranking algorithm is written: synthetic-parity corpus + TS 1.3.1 goldens (CLI + MCP) for multi-word explore and overloaded node, with README provenance updated and the Go-side regen explicitly deferred to plan 17.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-01-SUMMARY.md` when done.
</output>
