---
phase: 01-behavioral-parity-explore-node
plan: 15
type: execute
wave: 4
depends_on: ["01-05", "01-08", "01-09"]
files_modified: []
autonomous: true
requirements: [EXPL-02, TEST-01]

must_haves:
  truths:
    - "This repo's own .codegraph index carries the 6 new edge kinds after a forced re-index (sync would skip unchanged files)"
    - "The golden test corpora (colbymchenry-codegraph, weft-go, synthetic-parity) are re-indexed with the new extractors so their graphs carry the new kinds"
  artifacts:
    - "re-indexed .codegraph/ for this repo + the golden corpora (operational, not committed source)"
  key_links:
    - "extractor emission (plans 05/08/09) -> index --force -> graph with 9 edge kinds -> RWR ranks over the full set (validated in plan 17)"
---

<objective>
F4 of the D-09 foundation wave: re-index with `index --force` so the graphs actually CARRY the new edge kinds the extractors now emit. Critical operational fact (RESEARCH §A F4): `sync`'s stat pre-filter will NOT re-extract unchanged files, so a forced re-index is required — use `index --force`, NOT `sync`.

Two targets: (1) this repo's own `.codegraph/` (the index the MCP server this session uses), so its graph carries the new kinds; (2) the golden test corpora under testdata/golden/corpus/ (colbymchenry-codegraph, weft-go, synthetic-parity), so plan 17's golden regen + parity assertions run over graphs that have the new edges.

No code changes — purely operational. Depends on ALL priority-4 extractor work landing (plans 05, 08, 09) so every language's new kinds are present before re-indexing. This is the hinge between "extractors emit new kinds" and "RWR ranks over them."

Note: this does NOT touch internal/migrate (RESEARCH §A: migration passes all 9 kinds through verbatim already; a migrated TS index already has them). Only native re-indexing produces the new kinds for a natively-built index.

Purpose: without the re-index, the RWR graph is stale w.r.t. the new extractor output and would rank over the old 5-kind set.
Output: re-indexed graphs (operational).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@internal/indexer/goextract/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force re-index this repo and confirm new edge kinds are present</name>
  <files>(operational — this repo's .codegraph/)</files>
  <read_first>
    - 01-RESEARCH.md §A F4 (why index --force, not sync) and §C.1 (the 9 kinds to expect)
    - internal/indexer/goextract/types.go (the new constants, to confirm the emitted kind strings)
  </read_first>
  <action>
    Build the current binary and run `codegraph index --force` on this repository so its own .codegraph carries the new extractor output. Then confirm the graph now contains the 6 new edge kinds (extends, references, overrides, instantiates, returns, type_of) by querying the index (e.g. an explore/callers run that surfaces the new relationships, or a direct edge-kind count over the store). If ANY of the 6 new kinds is entirely absent, STOP — it means an extractor emit site (plan 05/08/09) did not land; report which kind is missing rather than proceeding. Do NOT use `sync` (it skips unchanged files and would leave the graph stale).
  </action>
  <verify>
    <automated>go build ./... && go run ./cmd/codegraph index --force -q . >/dev/null && go run ./cmd/codegraph status -p . --json | grep -c -E 'edges|nodes' | awk '$1>=1{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - source: the repo's .codegraph is rebuilt via index --force (not sync)
    - behavior: all 6 new edge kinds are observable in the rebuilt graph (none entirely absent)
    - test-command: `go run ./cmd/codegraph index --force -q . && go run ./cmd/codegraph explore <a-known-symbol> -p .` surfaces at least one new-kind relationship
  </acceptance_criteria>
  <done>This repo is force-re-indexed and all 6 new edge kinds are confirmed present in its graph.</done>
</task>

<task type="auto">
  <name>Task 2: Force re-index the golden corpora</name>
  <files>(operational — testdata/golden/corpus/*/.codegraph/)</files>
  <read_first>
    - testdata/golden/capture.sh (the index --force invocation pattern it already uses per corpus)
    - 01-RESEARCH.md §A F5 (why the committed explore.json fixtures go stale after this — regen happens in plan 17)
  </read_first>
  <action>
    Force re-index each golden corpus (colbymchenry-codegraph, weft-go, synthetic-parity) with `index --force` so their graphs carry the new edge kinds — mirroring the `index --force` call capture.sh already performs per corpus. Do this so plan 17's Go-side golden regen and parity diffs run over graphs with the full 9-kind edge set. Note explicitly (for plan 17) that the committed Go-side explore fixtures are now STALE and MUST be regenerated in plan 17 (F5) — do not attempt to hand-edit them here.
  </action>
  <verify>
    <automated>for c in colbymchenry-codegraph weft-go synthetic-parity; do go run ./cmd/codegraph index --force -q testdata/golden/corpus/$c >/dev/null || exit 1; done; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - source: each corpus under testdata/golden/corpus/ is re-indexed via index --force
    - behavior: each corpus's graph rebuilds without error and carries the new edge kinds
    - test-command: the for-loop above exits 0 for all three corpora
  </acceptance_criteria>
  <done>All three golden corpora are force-re-indexed with the new extractors; plan 17 is flagged to regenerate the now-stale Go-side fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| corpus source -> re-index | Re-indexing arbitrary corpus code via the (unchanged) extractor pipeline |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-23 | Tampering | stale index serving old edge kinds | medium | mitigate | index --force (not sync) guarantees every file is re-extracted so no stale 5-kind graph survives; the missing-kind STOP check catches an incomplete extractor landing |
| T-01-24 | Tampering | index-format compatibility | low | accept | New kinds are additive string values at fresh Pebble keys (no SchemaVersion bump, plan 02) — a force re-index writes them without migrating existing keys |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — runs the existing binary |
</threat_model>

<verification>
- `go build ./...` then `index --force` on the repo + all three corpora succeed.
- All 6 new edge kinds observable in the rebuilt repo graph.
- Plan 17 is flagged that Go-side fixtures are stale (F5 regen pending).
</verification>

<success_criteria>
This repo and all three golden corpora are force-re-indexed so their graphs carry the full 9-kind edge set, with all 6 new kinds confirmed present — the RWR now ranks over real new-kind edges. sync was NOT used.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-15-SUMMARY.md` when done.
</output>
