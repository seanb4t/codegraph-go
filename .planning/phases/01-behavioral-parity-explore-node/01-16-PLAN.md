---
phase: 01-behavioral-parity-explore-node
plan: 16
type: tdd
wave: 5
depends_on: ["01-03", "01-06", "01-07", "01-10", "01-11", "01-12", "01-13", "01-14", "01-04"]
files_modified:
  - internal/query/explore.go
  - internal/query/explore_test.go
  - internal/query/render_markdown.go
  - internal/query/render_markdown_test.go
  - internal/cli/explore.go
autonomous: true
requirements: [EXPL-01, EXPL-02, EXPL-04]

must_haves:
  truths:
    - "Explore wires the full TS pipeline in order: tokenize -> hybrid gather -> rerank -> expansion -> named seeding -> RWR -> per-file scoring -> gate -> 5-tier sort -> render"
    - "explore accepts a multi-word variadic <query...> from the CLI (cobra.MinimumNArgs(1) + strings.Join), no longer 0-matching multi-word queries"
    - "explore emits the exact per-root '⚠️ no covering tests found' warning when a root has direct callers but no covering test files"
    - "render-time skeletonization (>=3 polymorphic siblings) and adaptive output budget (by project file count) are applied"
  artifacts:
    - "internal/query/explore.go (pipeline orchestration wiring all stages)"
    - "internal/query/render_markdown.go (EXPL-04 warning, H20 skeletonization, H21 adaptive budget)"
    - "internal/cli/explore.go (variadic <query...>)"
  key_links:
    - "all plan-07..14 building blocks -> Explore orchestration -> RenderExplore -> shared Engine visible identically on CLI + MCP (no MCP plumbing needed)"
---

<objective>
Wire the explore pipeline end to end. Every building block (tokenizers, gather+rerank, expansion, seeding, RWR, scoring, gate+sort) is built and unit-tested in isolation (plans 03,06,07,10-14); this plan connects them in TS's exact pipeline order inside Engine.Explore, adds the CLI variadic-args fix (EXPL-01), the EXPL-04 warning, and the two render-time heuristics H20/H21 — completing D-10's H1-H21 coverage.

Wiring order (RESEARCH Architecture Diagram): tokenize -> hybrid gather (H3-H6) -> rerank (H7-H9) -> type-hierarchy/BFS/glue expansion (H10-H12) -> named-symbol seeding (H13) -> RWR (computeGraphRelevance) -> per-file scoring/exclusion/rescue (H14-H16) -> gate + central + 5-tier sort (H17-H19) -> render. The RWR ranking now feeds groupMatchesByFile INSTEAD of the lexical matchNodes order (RESEARCH Pitfall 1: replace the INPUT construction at explore.go's matchNodes call site; keep groupMatchesByFile/buildBlastEntry/readSourceFile downstream). Preserve the WR-05 empty-query guard and the maxFiles clamp verbatim.

Remaining heuristics ported here:
- H20 Polymorphic-sibling skeletonization (render-time): an off-spine file whose classes share a supertype with `>= MIN_SIBLINGS=3` implementers -> skeletonize (signatures only).
- H21 Adaptive output budget: `getExploreOutputBudget(fileCount)` sets maxFiles/relationship caps/whole-file ceilings by project size; maxFiles clamped [1,20].
- EXPL-04: the per-root `; ⚠️ no covering tests found` warning (exact string, RESEARCH §5) appended in renderBlastBullet when CallerCount>0 AND no test files cover the root.

EXPL-05/NODE-04 are structural: Engine.Explore is the shared CLI+MCP code path (mcp/tools.go already delegates) — NO MCP plumbing; the change is automatically visible on both surfaces.

Purpose: this is where explore actually becomes graph-relevance-ranked and multi-word-capable end to end.
Output: wired explore.go + render + CLI + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-CONTEXT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/query/explore.go
@internal/query/render_markdown.go
@internal/cli/explore.go
@internal/mcp/tools.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN wire the full pipeline into Engine.Explore</name>
  <files>internal/query/explore.go, internal/query/explore_test.go</files>
  <read_first>
    - internal/query/explore.go (Explore orchestrator :105-159; the matchNodes call site :114 is the swap point; keep groupMatchesByFile/buildBlastEntry/readSourceFile) and the WR-05 guard + maxFiles clamp
    - 01-RESEARCH.md Architecture Diagram (the exact stage order) + Pitfall 1 (replace INPUT construction, not a rerank) + §C.2 rows H20/H21
    - 01-PATTERNS.md "internal/query/explore.go (extend)"
    - the building-block signatures from plans 07/10/11/12/06/13/14 (gather, rerank, expand, seeding, rwr, scoring, gate/sort)
  </read_first>
  <behavior>
    - a structurally-connected symbol outranks a lexical-only name-match on the synthetic structural-beats-lexical case (D-03d)
    - a weakly-connected Test* func no longer tops results (D-03c)
    - a multi-word query returns non-empty ranked results (not 0-match)
    - the pipeline runs the stages in TS order and feeds RWR output into groupMatchesByFile
  </behavior>
  <action>
    Replace the lexical matchNodes INPUT construction at explore.go's call site with the full pipeline: extractSymbolsFromQuery+extractSearchTerms (plan 03) -> hybrid gather (plan 07) -> rerank (plan 10) -> expansion (plan 11) -> named seeding (plan 12) -> computeGraphRelevance (plan 06) over the expanded subgraph seeded by H13's seeds -> per-file scoring/exclusion/rescue (plan 13) -> gate+central+5-tier sort (plan 14) -> feed the ranked files into the EXISTING groupMatchesByFile/buildBlastEntry/readSourceFile (unchanged). Add H21 adaptive output budget (getExploreOutputBudget(fileCount), maxFiles clamped [1,20]) to size the render. Preserve the WR-05 empty-query guard and the maxFiles validate+clamp verbatim. Keep the whole thing deterministic (D-04). If a building-block signature needs a minor adjustment to wire cleanly, adjust it and its test here. Write RED integration tests against the synthetic-parity corpus asserting the structural-beats-lexical ordering and the Test*-not-top behavior first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestExploreStructuralBeatsLexical|TestExploreMultiWord|TestExploreTestNotTop' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: Explore invokes the stages in TS order and feeds RWR output (not lexical order) into groupMatchesByFile; WR-05 guard + maxFiles clamp intact
    - behavior: structural-beats-lexical + Test*-not-top + multi-word-non-empty all hold on the synthetic corpus
    - test-command: `go test ./internal/query/ -run 'TestExploreStructuralBeatsLexical|TestExploreMultiWord|TestExploreTestNotTop' -count=1`
  </acceptance_criteria>
  <done>The full RWR pipeline is wired into Engine.Explore in TS order with H21 budget, deterministic, integration tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN EXPL-04 no-covering-tests warning + H20 skeletonization</name>
  <files>internal/query/render_markdown.go, internal/query/render_markdown_test.go</files>
  <read_first>
    - internal/query/render_markdown.go renderBlastBullet (:105-117, the existing "; tests: ..." clause to extend) and RenderExplore (:138)
    - 01-RESEARCH.md §5 (the EXACT `; ⚠️ no covering tests found` string + trigger: CallerCount>0 and no test files; a root with zero callers gets NEITHER clause) and §C.2 row H20 (MIN_SIBLINGS=3 skeletonization)
    - 01-PATTERNS.md "internal/query/render_markdown.go (extend)" (the exact else-branch shape + plain-text/no-ANSI constraint)
  </read_first>
  <behavior>
    - a root with >=1 direct caller and no covering test file -> bullet ends with `; ⚠️ no covering tests found`
    - a root with covering test files -> the existing `; tests: ...` clause (no warning)
    - a root with zero callers -> neither clause (no warning)
    - an off-spine file whose classes share a supertype with >=3 implementers -> rendered skeletonized (signatures only)
  </behavior>
  <action>
    Extend renderBlastBullet: when bl.CallerCount > 0 AND the root has no covering test files, append the EXACT string `; ⚠️ no covering tests found` (verbatim from RESEARCH §5 — note "found", the emoji, and that it is appended, not a standalone line); keep the existing `; tests: ...` clause for the has-tests case; a root with zero callers gets NEITHER (mirrors TS's early-continue). Add H20 render-time skeletonization: an off-spine file whose classes share a supertype with >= MIN_SIBLINGS=3 implementers renders signatures-only instead of full bodies. Keep everything plain-text markdown (no ANSI — Phase 6 archtest enforces later). Write RED tests for the three warning cases and the skeletonization trigger first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestNoCoveringTestsWarning|TestSkeletonization' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: renderBlastBullet appends the exact `; ⚠️ no covering tests found` string only when CallerCount>0 and no test files; H20 skeletonizes at MIN_SIBLINGS=3
    - behavior: warning fires for callers-but-no-tests, not for has-tests, not for zero-callers; skeletonization triggers at 3 siblings
    - test-command: `go test ./internal/query/ -run 'TestNoCoveringTestsWarning|TestSkeletonization' -count=1`
  </acceptance_criteria>
  <done>EXPL-04 warning (exact string, exact trigger) + H20 skeletonization land plain-text, TDD-verified.</done>
</task>

<task type="auto">
  <name>Task 3: CLI variadic multi-word query (EXPL-01)</name>
  <files>internal/cli/explore.go</files>
  <read_first>
    - internal/cli/explore.go (:24 cobra.ExactArgs(1) bug; :37 eng.Explore(args[0], maxFiles) call site)
    - internal/cli/node.go (cobra.MaximumNArgs(1) variadic-arg sibling pattern)
    - 01-RESEARCH.md §10 (the exact fix: MinimumNArgs(1) + strings.Join(args, " "))
  </read_first>
  <action>
    Change explore.go's Args from cobra.ExactArgs(1) to cobra.MinimumNArgs(1), and join the args with a space before calling Explore: `query := strings.Join(args, " ")` then `eng.Explore(query, maxFiles)`. Add "strings" to the import block. This makes `explore user account manager` tokenize as a multi-word query instead of being rejected. No MCP change (the MCP surface already passes a single string arg to the same Engine.Explore).
  </action>
  <verify>
    <automated>go build ./internal/cli/ && go run ./cmd/codegraph explore user account manager -p testdata/golden/corpus/synthetic-parity >/dev/null 2>&1 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - source: explore.go uses cobra.MinimumNArgs(1) and strings.Join(args, " ")
    - behavior: a 3-word `explore` invocation is accepted and runs (no ExactArgs rejection)
    - test-command: `go run ./cmd/codegraph explore user account manager -p testdata/golden/corpus/synthetic-parity` exits 0
  </acceptance_criteria>
  <done>The CLI accepts a variadic multi-word query and joins it before Explore; EXPL-01 CLI surface complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI/MCP query -> Engine.Explore | Untrusted query drives the full ranking pipeline over the index |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-25 | Elevation / Input Validation | empty query -> match everything | high | mitigate | The WR-05 empty-query guard (strings.TrimSpace(query)=="" -> error) is preserved verbatim at the top of Explore; the tokenizers also return empty for empty input (plan 03) — two layers |
| T-01-26 | Denial of Service | pipeline cost on pathological query | high | mitigate | The subgraph caps (maxNodes=200/GLUE_NODE_CAP=60/searchLimit) from plans 07/11 bound the RWR input; H21 clamps maxFiles to [1,20]; RWR is fixed 25 iters |
| T-01-27 | Tampering | ANSI leaking to the agent surface | medium | mitigate | Render stays plain-text markdown only (no ANSI); shared Engine means CLI==MCP output (EXPL-05); Phase 6 archtest will enforce the seam later |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — stdlib strings + existing building blocks |
</threat_model>

<verification>
- `go test ./internal/query/ -count=1` full package green (integration + all building-block tests).
- `go build ./...` green.
- Multi-word CLI explore runs and returns ranked results on the synthetic corpus.
- EXPL-05 sanity: the same query via CLI and via the MCP handler path produce identical output (asserted formally in plan 17).
</verification>

<success_criteria>
Engine.Explore runs the full TS pipeline (all H1-H21 wired) in order, feeding RWR output into the render; multi-word variadic queries work from the CLI; the exact no-covering-tests warning fires on the right trigger; skeletonization + adaptive budget apply; output stays plain-text and identical across CLI + MCP.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-16-SUMMARY.md` when done.
</output>
