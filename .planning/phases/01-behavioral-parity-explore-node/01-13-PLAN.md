---
phase: 01-behavioral-parity-explore-node
plan: 13
type: tdd
wave: 3
depends_on: ["01-06", "01-07"]
files_modified:
  - internal/query/scoring.go
  - internal/query/scoring_test.go
autonomous: true
requirements: [EXPL-02, EXPL-03]

must_haves:
  truths:
    - "Per-file score tiers apply named-seed +50, entry +10, connected +3, other +1, keeping files with score >= 3"
    - "Test/spec/low-value files are hard-excluded unless the query mentions test AND >=2 non-test candidates remain"
    - "Change-surface buried-rescue force-keeps a genuinely-buried signature-type file (fileGraphScore < maxGraph*0.06 AND termHits < 2) with score = max(score,45)"
  artifacts:
    - "internal/query/scoring.go (H14 score tiers, H15 hard exclusion, H16 buried-rescue)"
  key_links:
    - "RWR per-node mass (plan 06) -> aggregated per-file graph score -> H16 buried-rescue trigger + H14 tiers -> gate input (plan 14)"
---

<objective>
Port the per-file scoring, exclusion, and rescue heuristics — H14, H15, H16 (RESEARCH §C.2) — that convert node-level RWR mass + candidate signals into per-file scores and decide which files survive to the gate. D-10 full-fidelity.

Heuristics (constants VERBATIM):
- H14 Per-file score tiers: named-seed `+50`, entry (root/named) `+10`, connected-to-entry `+3`, other `+1`; keep files with `score >= 3`.
- H15 Hard test/spec exclusion: drop all low-value (test/spec/icon/i18n) files UNLESS the query mentions test AND >=2 non-test candidates remain.
- H16 Change-surface buried-rescue: for each tier-seed callable's signature types (references/type_of/returns edges), surface ONLY if genuinely buried (`fileGraphScore < maxGraph*0.06` AND `termHits < 2`); a rescued file is force-kept with `score = max(score,45)`.

H16 depends directly on the new D-09 edges (references/type_of/returns) existing — this is where the edge-kind expansion pays off. Consumes RWR per-node mass (plan 06) aggregated per file, and isTestFile (plan 07). New file scoring.go — no same-wave conflict.

Purpose: the per-file scores + exclusions are the gate's (plan 14) input; H15 is a primary lever for EXPL-03 (Test* funcs don't top results).
Output: internal/query/scoring.go + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/query/rwr.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN per-file score tiers (H14) + aggregated per-file graph score</name>
  <files>internal/query/scoring.go, internal/query/scoring_test.go</files>
  <read_first>
    - 01-RESEARCH.md §C.2 row H14 (named-seed +50, entry +10, connected +3, other +1, keep score>=3) and §4 (fileGraphScore = sum of node-level RWR mass per file)
    - internal/query/rwr.go computeGraphRelevance (the per-node mass map to aggregate)
  </read_first>
  <behavior>
    - a file containing a named-seed node -> +50
    - an entry (root/named) file -> +10; connected-to-entry -> +3; other -> +1
    - a file scoring <3 -> dropped
    - fileGraphScore(file) = sum of RWR mass over that file's nodes
  </behavior>
  <action>
    Create scoring.go (package query). Aggregate RWR per-node mass into fileGraphScore per file (sum of node mass, per RESEARCH §4). Apply the H14 per-file score tiers exactly: +50 named-seed, +10 entry, +3 connected-to-entry, +1 other, then keep only files with score >= 3. Deterministic ordering. Write RED tests for the tier arithmetic, the score>=3 cutoff, and the per-file mass aggregation first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestFileScoreTiers|TestFileGraphScoreAgg' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: scoring.go applies +50/+10/+3/+1 tiers with a >=3 keep threshold and sums RWR mass per file
    - behavior: tier arithmetic + cutoff match §C.2/§4
    - test-command: `go test ./internal/query/ -run 'TestFileScoreTiers|TestFileGraphScoreAgg' -count=1`
  </acceptance_criteria>
  <done>H14 score tiers + per-file mass aggregation land verbatim, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN hard test exclusion (H15) + change-surface buried-rescue (H16)</name>
  <files>internal/query/scoring.go, internal/query/scoring_test.go</files>
  <read_first>
    - 01-RESEARCH.md §C.2 rows H15 (drop low-value files unless query mentions test AND >=2 non-test remain) and H16 (buried-rescue via references/type_of/returns signature-type edges; buried = fileGraphScore<maxGraph*0.06 AND termHits<2; force-keep score=max(score,45))
    - internal/query/gather.go isTestFile (the shared predicate, plan 07) and rwr.go RankEdges (the new signature-type edges)
  </read_first>
  <behavior>
    - test/spec/icon/i18n files -> dropped unless query mentions test AND >=2 non-test candidates remain
    - a tier-seed's signature-type file with fileGraphScore < maxGraph*0.06 AND termHits < 2 -> rescued, score forced to max(score,45)
    - a signature-type file that is NOT buried (mass >= threshold or termHits>=2) -> NOT rescued (rescue is for genuinely-buried only)
  </behavior>
  <action>
    Implement H15: identify low-value files (test/spec via the shared isTestFile, plus icon/i18n per §C.2) and drop them UNLESS the query mentions test AND >=2 non-test candidates remain. Implement H16 change-surface buried-rescue: for each tier-seed callable, follow its signature-type edges (references/type_of/returns — now available from D-09) to their files; rescue a file ONLY when it is genuinely buried (fileGraphScore < maxGraph*0.06 AND termHits < 2), force-keeping it with score = max(score,45). Deterministic ordering. Write RED tests for the exclusion guard, the buried-only rescue trigger, and the score=max(score,45) force-keep first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestHardTestExclusion|TestBuriedRescue' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: scoring.go drops low-value files (guarded) and rescues only buried signature-type files with score=max(score,45)
    - behavior: exclusion guard honors the test-mention+>=2-non-test rule; rescue fires only for buried files
    - test-command: `go test ./internal/query/ -run 'TestHardTestExclusion|TestBuriedRescue' -count=1`
  </acceptance_criteria>
  <done>H15 exclusion + H16 buried-rescue land verbatim (using the new D-09 signature-type edges), tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| RWR mass + candidates -> per-file scoring | In-memory scoring of a bounded file set |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20 | Denial of Service | rescue-driven file explosion | low | accept | Buried-rescue only fires for genuinely-buried signature-type files (a bounded set from the tier seeds' signature edges); score>=3 cutoff + exclusion prune the file set first |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies |
</threat_model>

<verification>
- `go test ./internal/query/ -run 'TestFileScoreTiers|TestFileGraphScoreAgg|TestHardTestExclusion|TestBuriedRescue' -count=1` green.
- Constants (+50/+10/+3/+1, >=3, 0.06, termHits<2, max(score,45)) match §C.2 exactly.
</verification>

<success_criteria>
Per-file score tiers, hard test exclusion, and change-surface buried-rescue (H14-H16) are ported verbatim — with buried-rescue exercising the new D-09 signature-type edges — TDD-verified.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-13-SUMMARY.md` when done.
</output>
