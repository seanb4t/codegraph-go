---
phase: 05-mcp-resources-capability-claims-drift-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/mcp/resources.go
  - internal/mcp/resources/explore.md
  - internal/mcp/resources_test.go
  - internal/mcp/server.go
  - test/wireoracle/scenarios.go
  - test/wireoracle/anchors.go
  - test/wireoracle/COVERAGE-BASELINE.md
  - testdata/wireoracle/transcripts/
autonomous: true
requirements: [RSRC-01, RSRC-02, RSRC-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A live client against a real `serve --mcp` subprocess sees `capabilities.resources` in the `initialize` result (RSRC-01)."
    - "`resources/list` on the wire advertises `codegraph://tools/explore` with `mimeType: text/markdown` (RSRC-01)."
    - "`resources/read` on `codegraph://tools/explore` returns non-empty `text/markdown` on the wire (RSRC-02)."
    - "`BuildServer(false, ...)` — no index — still lists and serves that resource (RSRC-03)."
    - "`task test:wireoracle` is green at the end of this plan, with every moved transcript byte attributed to one named cause."
  artifacts:
    - internal/mcp/resources.go
    - internal/mcp/resources/explore.md
    - internal/mcp/resources_test.go
    - testdata/wireoracle/transcripts/resources-list.golden
    - testdata/wireoracle/transcripts/resources-read-explore.golden
  key_links:
    - "`BuildServer` calls `registerResources(s)` UNCONDITIONALLY, outside the `if hasIndex` branch — this is RSRC-03's structural property and the single most likely thing to get wrong."
    - "`ServerCapabilities.Resources` is set EXPLICITLY to the zero `&mcp.ResourceCapabilities{}`; leaving it nil lets go-sdk's `capabilities()` auto-populate `listChanged: true`, which this phase does not implement."
    - "`resourceURIFor` is the ONE map both `registerResources` and every later drift guard read; no second hand-typed URI list exists anywhere."
    - "`ExpectedScenarioCount` ↔ `Scenarios()` ↔ `testdata/wireoracle/transcripts/` stay a three-way match (already guarded by `TestScenarioCountIsExact` + `TestTranscriptSetMatchesScenarioSet`)."
---

<objective>
Prove the whole MCP Resources architecture end-to-end on ONE URI — `codegraph://tools/explore` —
registered, listed, read, and observed on the wire by the SDK-independent oracle, and re-freeze
the frozen transcript corpus that turning `capabilities.resources` on necessarily moves.

Purpose: this is the tracer slice. It touches every layer the phase will modify (embedded
markdown → `embed.FS` → `resourceURIFor` → `registerResources` → `ServerCapabilities` →
cacheScope middleware → unit session test → wire scenario → frozen golden), so an architectural
dead end surfaces after one plan instead of after ten. Everything after this plan is horizontal
expansion out from a proven slice.

Output: a live, wire-observed MCP Resources capability serving one production-quality fact-sheet,
a green `task test:wireoracle`, and a reviewed-diff attribution for every transcript byte moved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-CONTEXT.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-RESEARCH.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-PATTERNS.md
</context>

## Artifacts this phase produces

Created by THIS plan:

| Symbol / file | Kind | Notes |
|---|---|---|
| `internal/mcp/resources/explore.md` | new file | The `codegraph://tools/explore` fact-sheet (D-01) |
| `internal/mcp/resources.go` | new file | Holds everything below |
| `resourcesFS` | new package var (`embed.FS`) | `//go:embed resources/*.md` — first `go:embed` use in this repo |
| `resourceURIFor` | new package var (`map[string]string`) | filename stem+ext → URI. THE single source of truth for the URI scheme (D-08/D-09/D-10) |
| `resourceDescriptionFor` | new package func | Returns a resource's `Description`, derived from `exploreTool()`/`companionTool()` for tool stems |
| `registerResources` | new package func | One loop, one seam; called unconditionally from `BuildServer` |
| `internal/mcp/resources_test.go` | new file | `newTestSession`-based list/read coverage |
| `listResourceURIs` | new test helper | Sibling of `listToolNames` in `server_test.go` |
| `resourcesListRequest`, `resourceReadRequest` | new funcs in `test/wireoracle/scenarios.go` | Mirror `toolsListRequest`/`toolCallRequest` |
| `resources-list`, `resources-read-explore` | new wire scenarios + goldens | |
| `assertResourceCacheControl` | new func in `test/wireoracle/anchors.go` | Pins the cacheScope decision on the wire |

Created by LATER plans in this phase (listed so no plan re-creates them): the 9 remaining
`internal/mcp/resources/*.md` files (05-02); `internal/mcp/resources_schema_drift_test.go` with
`TestMCPResourceNumericClaimsMatchToolSchemas`, `TestResourceFileSetMatchesToolNames`,
`TestResourceContentCarriesNoHostFacts` and their non-vacuity siblings (05-03); the 11 remaining
wire scenarios and `TestEveryAdvertisedResourceURIHasASuccessfulReadScenario` (05-04).

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One resource URI live end-to-end — embed, register, capability, cacheScope</name>

  <files>internal/mcp/resources_test.go, internal/mcp/resources.go, internal/mcp/resources/explore.md, internal/mcp/server.go</files>

  <read_first>
    - `internal/mcp/server.go` — read `BuildServer` in full (lines 453-752). You need three things from it: the `D-11` capability comment block and the `mcp.NewServer(...)` literal at lines 515-529 (the insertion point for `Capabilities.Resources`); the `if hasIndex { ... }` tool-registration branch at 559-561 (registration must go OUTSIDE it); and the `AddReceivingMiddleware` closure at 641-749, specifically its SECOND `switch method` (the one after `next(...)` returns, at line 661) where `tools/list` and `server/discover` already correct `CacheScope`.
    - `internal/mcp/server.go` lines 36-62 — the `instructions` constant's doc comment. Its no-interpolation constraint ("no repository path, no resolved index root, no hostname, no environment value") is the content constraint every resource markdown file inherits.
    - `internal/mcp/tools.go` lines 157-179 — `exploreTool()` and `ExploreArgs`. These jsonschema tags are the ONLY source for `explore.md`'s content (D-01).
    - `internal/mcp/server.go` lines 397-451 — `allToolNames`, `registerTools`, `unregisterTools`. `registerResources` mirrors `registerTools`' shape: one function, one seam, explicit iteration order.
    - `internal/mcp/server_test.go` lines 74-127 — `newTestSession` and `listToolNames`. Reuse `newTestSession` directly (same package); write `listResourceURIs` as its sibling.
  </read_first>

  <behavior>
    Write these tests in `internal/mcp/resources_test.go` FIRST and watch them fail before writing
    any of `resources.go`, `explore.md`, or the `server.go` edits.

    - `TestResourcesListAdvertisesRegisteredURIs`: `BuildServer(true, allCompanions, dir, dir)`; a
      `newTestSession` client's `ListResources` returns a set containing `codegraph://tools/explore`,
      and that entry's `MIMEType` is `text/markdown`.
    - `TestResourcesReadReturnsMarkdown`: for every URI `ListResources` advertised,
      `ReadResource` returns exactly one `Contents` element whose `Text` is non-empty and whose
      `MIMEType` is `text/markdown`. Drive the loop off the LIST result, never off a re-typed URI
      literal — this is the assertion that keeps growing for free as later plans add resources.
    - `TestResourcesRegisterWithoutIndex`: `BuildServer(false, nil, dir, dir)` — the zero-tools
      path (MCP-03) — still returns the identical URI set from `ListResources`, and `ReadResource`
      still returns non-empty `text/markdown`. This is RSRC-03 proven as a behavior, not inferred
      from where a call sits in the source.
    - `TestResourcesReadIsNotVacuous`: `ReadResource` on `codegraph://tools/does-not-exist`
      returns an error. Without this, a handler that returned empty-but-successful for every URI
      would pass the three tests above forever.
  </behavior>

  <action>
Create `internal/mcp/resources/explore.md` as a fact-sheet for `codegraph_explore`, restating
ONLY what `internal/mcp/tools.go` already carries (D-01): the tool name `codegraph_explore`, its
description "Explore relevant symbols: verbatim source, call paths, blast radius", and its three
arguments from `ExploreArgs` — `query` (string, required, "Natural-language or symbol/file
query"), `max_files` (integer, optional, "Cap on distinct files returned (default 5)"), and
`path` (string, optional, "Repo path (default: server cwd)"). State that the result is markdown
text. Markdown structure and headers within the file are your discretion. Three content
constraints are not: (a) no worked examples and no "use this instead of X" cross-tool
recommendations (D-01) — Phase 6's SKILL.md owns that surface; (b) no "empty until indexed"
caveat (D-02) — the `codegraph://index-state` resource owns it; (c) no restatement that the tool
is read-only or safe (D-04) — `ToolAnnotations` in `tools.go` already carries that over the
protocol. The file inherits the `instructions` constant's no-interpolation rule: it is static at
build time and must contain no absolute filesystem path, hostname, username, or environment
value. Write the numeric "default 5" claim in a form that `(?i)\b(default|max)\s+(\d+)` matches,
since plan 05-03's guard pins claims through exactly that regex.

Create `internal/mcp/resources.go` with: a `//go:embed resources/*.md` directive over an
`embed.FS` named `resourcesFS`; a `resourceURIFor` map from filename (`explore.md`) to URI
(`codegraph://tools/explore`) carrying exactly one entry in this plan; a `resourceDescriptionFor`
helper that, for a stem naming a codegraph tool, returns that tool's own `Description` by calling
`exploreTool()` or `companionTool(stem)` — never a hand-typed copy, so the `resources/list`
payload's description can never drift from `tools.go` — and returns a value from a small explicit
literal map for the two behavior-doc stems `tools-filter` and `index-state` that plan 05-02 adds;
and `registerResources(s *mcp.Server)`, which iterates `fs.ReadDir(resourcesFS, "resources")`,
looks each filename up in `resourceURIFor`, reads its bytes via `resourcesFS.ReadFile`, and calls
`s.AddResource` with `URI` from the map, `Name` set to the filename stem, `Description` from
`resourceDescriptionFor`, and `MIMEType` set to `text/markdown`. Leave `Title` unset. The handler
closure returns a `*mcp.ReadResourceResult` whose single `*mcp.ResourceContents` carries `URI`
from `req.Params.URI` and `Text` from the bytes captured at registration time — it must set no
`MIMEType`, because go-sdk's `readResource` back-fills it from the registered `Resource.MIMEType`
(server.go:1040-1048). Follow this package's fatal-at-construction convention for programmer
error: a `ReadDir` failure, a filename with no `resourceURIFor` entry, or a `ReadFile` failure all
`panic` with a message naming the filename — these are build-time invariants, not runtime
conditions, mirroring how `companionTool` and `companionHandler` already panic on an unknown name.
Tag the non-obvious structural choices with inline `D-07`/`D-08`/`D-09`/`RSRC-03` comments the way
`server.go` already tags `D-11`/`D-13`.

Edit `internal/mcp/server.go` in three places. First, add `Resources: &mcp.ResourceCapabilities{}`
to the `mcp.ServerCapabilities` literal at lines 525-527, alongside the existing `Tools` field,
with a comment extending D-11's own rationale: go-sdk's `capabilities()` (server.go:645-653) would
auto-populate `caps.Resources` with `ListChanged: true` purely because `s.resources.len() > 0`,
and this phase implements neither `listChanged` nor `subscribe` — the explicit zero value is what
keeps the advertised capability truthful. Second, call `registerResources(s)` immediately after
the `mcp.NewServer(...)` literal and BEFORE the `if hasIndex {` branch at line 559, tagged
`RSRC-03`, with a comment stating that its position outside that branch is the structural property
that makes resources available in an unindexed repository. Third, in the middleware's post-`next`
`switch method` at line 661, add a combined `case "resources/list", "resources/read":` that sets
`CacheScope` to `private` on a `*mcp.ListResourcesResult` or `*mcp.ReadResourceResult`
respectively (both embed `Cacheable`). Do NOT add either method to the pre-`next` `switch method`
at line 652 that drives `recheckCatalog` — resources are index-independent by construction, and
adding them there would pay an unbounded per-request cost for no observable benefit (the same
reasoning T-03-15 already applied to notifications).

The cacheScope value is a decision, resolved here rather than defaulted (05-RESEARCH.md Open
Question 1). Record the rationale in the comment: go-sdk's `setDefaultCacheableValues`
(protocol.go:1195-1197) writes `public` unconditionally, and the existing middleware's
method-literal switch never saw resources. `private` is chosen over the SDK default for three
reasons — it keeps every codegraph response on the wire uniform, so a reviewer never has to
remember which method families differ; STATE.md's standing decision holds `ttlMs: 0` and
`cacheScope: "private"` to be two halves of one correctness property rather than independent
options, and resources inherit the `ttlMs: 0` half already; and `public` is fail-open for a future
resource whose content is repository-dependent, whereas `private` is fail-safe and costs one
`case`. Tag the comment with the two IN-CODE decision IDs it extends — the `tools/list`
correction and the `server/discover` correction already tagged in the middleware at server.go:715
and server.go:728. Note in the comment that those in-code IDs live in a different numbering space
from 05-CONTEXT.md's own D-NN decisions, which is why the tag names the file it came from.
  </action>

  <verify>
    <automated>go build ./... && go test ./internal/mcp/... -run 'TestResources' -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/mcp/... -count=1` passes.
    - `go test ./internal/mcp/... -run 'TestResourcesRegisterWithoutIndex' -count=1 -v` passes — this is RSRC-03 proven by behavior against `BuildServer(false, ...)`, not by source position.
    - `go test ./internal/mcp/... -run 'TestResourcesReadIsNotVacuous' -count=1 -v` passes, so the read assertions above are proven to discriminate.
    - `rg -c 'codegraph://' internal/mcp/resources.go` returns 1 — the URI literal exists in exactly one place in production code (`resourceURIFor`).
    - `rg -n 'Resources: &mcp.ResourceCapabilities\{\}' internal/mcp/server.go` matches, and the matched line number is inside the `mcp.NewServer` literal (between the lines matched by `rg -n 'Tools: &mcp.ToolCapabilities' internal/mcp/server.go` and `rg -n 'Instructions: instructions' internal/mcp/server.go`).
    - `go test ./internal/mcp/... -run 'TestMCPToolSchemaNumericClaimsMatchEngineConstants|TestInstructions|TestREADME' -count=1` still passes — the existing guards are untouched.
    - `go vet ./internal/mcp/...` is clean.
    - EXPECTED AND NOT A FAILURE at the end of this task: `go test ./test/wireoracle/...` is RED. Turning `capabilities.resources` on moves the frozen bytes of every transcript carrying a `capabilities` object. Task 2 closes it; do not "fix" it by reverting the capability.
  </acceptance_criteria>

  <reversibility rating="costly">
    The `cacheScope: "private"` choice and the `codegraph://` URI scheme both become published wire
    contracts once frozen into transcripts and, from Phase 6 onward, named by SKILL.md and the
    `instructions` string. Changing either later costs a reviewed-diff re-freeze plus a downstream
    edit in every artifact that names a URI. Not one-way: the URI scheme is already locked by user
    decisions D-08/D-09/D-10, and cacheScope is one middleware `case` plus a re-freeze — the exact
    mechanism this repository has already run five times.
  </reversibility>

  <done>
    `resources/list` and `resources/read` work over an in-memory session for
    `codegraph://tools/explore`, with and without an index, `MIMEType` is `text/markdown` on both
    the listed resource and the read contents, and `go test ./internal/mcp/...` is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-freeze the transcripts that capabilities.resources moved, with attribution</name>

  <files>testdata/wireoracle/transcripts/</files>

  <read_first>
    - `test/wireoracle/COVERAGE-BASELINE.md` — read the whole file, especially the closing section "Instruction for whoever next extends this corpus". Its four rules bind this task.
    - `test/wireoracle/cmd/wireoracle/main.go` — the capture entrypoint. Note its doc comment: there is deliberately NO in-place regeneration flag; freezing is a human-run `> file` redirect.
    - `test/wireoracle/oracle_test.go` lines 40-115 — `fixtureDir`, `mustCaptureScenario`, `readFrozenTranscript`, `TestFrozenTranscriptsMatch`, so you know exactly what bytes are being compared.
    - `test/wireoracle/MUTATION-PROOF.md` lines 335-400 — Mutation 5, the most recent capability-flip re-freeze, for the shape of an attributed diff.
  </read_first>

  <action>
Build the binary the oracle captures against, then re-capture EVERY scenario in
`Scenarios()` with the capture CLI (`go run ./test/wireoracle/cmd/wireoracle -bin <built binary>
-fixture testdata/wireoracle/fixture -scenario <name>`), redirecting stdout to
`testdata/wireoracle/transcripts/<name>.golden`. Never hand-edit a golden file.

The expected blast radius is 25 transcripts, not 29. This corrects 05-RESEARCH.md Pitfall 2,
which predicted 29 and told you to treat "fewer than 29" as evidence the capability had not
reached every code path — that warning is wrong and would raise a false alarm here. Four of the
29 scenarios carry no `capabilities` object at all, because none of them produces an `initialize`
or `server/discover` result: `edge-call-before-initialize`, `modern-listen-catalog-change`,
`modern-meta-invalid-params`, and `modern-meta-unsupported-version`. Those four must come back
BYTE-IDENTICAL, and that is the expected outcome, not a gap. Measure the expected count before
you start rather than trusting either number: `rg -l '"capabilities"' testdata/wireoracle/transcripts/`
counted against the pre-change tree is the authority.

Run the reviewed-diff pass the corpus's own D-06 mechanism requires — that is the wire-oracle
D-06 recorded in `COVERAGE-BASELINE.md`, a different numbering space from 05-CONTEXT.md's D-06,
which is about `CODEGRAPH_MCP_TOOLS`. Concretely: `git diff --stat testdata/wireoracle/transcripts/` for
the shape, then `git diff testdata/wireoracle/transcripts/` read in full. There is exactly ONE
named cause for this pass — `capabilities.resources` now appears in every `initialize` and
`server/discover` result, emitted before `tools` because Go's `encoding/json` marshals
`ServerCapabilities`' fields in declaration order and `Resources` is declared before `Tools`
(protocol.go:2264-2294). Every changed line must be an instance of that one cause. A changed line
that is not — for example a moved `instructions` byte, a changed tool array, or a `cacheScope`
flip on a pre-existing method — is a real regression: stop and investigate rather than freezing
it. Record the cause and its measured count in the commit message body, following the
convention every prior re-freeze in this repository used; there is no ledger file and no sign-off
step.
  </action>

  <verify>
    <automated>go test ./test/wireoracle/... -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./test/wireoracle/... -count=1` passes, including `TestFrozenTranscriptsMatch`, `TestScenarioCountIsExact`, `TestTranscriptSetMatchesScenarioSet`, `TestSpecAnchorsHold`, and `TestEveryRegisteredToolHasASuccessfulCallScenario`.
    - `git diff --name-only testdata/wireoracle/transcripts/ | wc -l` outputs 25.
    - `git diff --name-only testdata/wireoracle/transcripts/` does NOT list `edge-call-before-initialize.golden`, `modern-listen-catalog-change.golden`, `modern-meta-invalid-params.golden`, or `modern-meta-unsupported-version.golden`.
    - Every one of the 25 changed files matches `rg -l '"resources":\{\}' testdata/wireoracle/transcripts/` — i.e. each changed transcript now carries the new capability key.
    - `git diff -U0 testdata/wireoracle/transcripts/ | rg -c '^[+-][^+-]' ` — every counted line, read individually, is an instance of the single named cause. Attribute the count in the commit message.
    - `go test ./... -count=1` passes.
  </acceptance_criteria>

  <done>
    Main is green: `task test:wireoracle` and the full suite both pass, 25 transcripts moved, 4 provably did not, and one named cause with its count is recorded in the commit message.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire-observe the tracer URI — resources/list and resources/read scenarios</name>

  <files>test/wireoracle/scenarios.go, test/wireoracle/anchors.go, testdata/wireoracle/transcripts/resources-list.golden, testdata/wireoracle/transcripts/resources-read-explore.golden, test/wireoracle/COVERAGE-BASELINE.md</files>

  <read_first>
    - `test/wireoracle/scenarios.go` lines 54-101 — `initializeRequest`, `toolsListRequest`, `toolCallRequest`. The two new helpers copy this literal-map shape exactly.
    - `test/wireoracle/scenarios.go` lines 430-505 — the async-dispatch invariant comment and `ExpectedScenarioCount`'s doc comment. Both bind this task; the count's doc comment must gain a sentence for every scenario added.
    - `test/wireoracle/scenarios.go` lines 506-620 — how `handshake-explore`, `toolslist-default`, and `toolslist-no-index` are declared, including `Index`, `Env`, and `ExpectTools`.
    - `test/wireoracle/capture.go` lines 30-100 — the `Scenario` struct's fields, so you set `Index` and `ExpectTools` deliberately rather than by copy-paste.
    - `test/wireoracle/anchors.go` lines 155-262 — `assertDiscoverCacheControl` and how anchors are registered. `assertResourceCacheControl` is its direct sibling.
  </read_first>

  <action>
Add `resourcesListRequest(id int)` and `resourceReadRequest(id int, uri string)` to
`test/wireoracle/scenarios.go`, immediately after `toolCallRequest`, built as the same literal
`map[string]any` shape: `jsonrpc` `2.0`, the given `id`, `method` of `resources/list` and
`resources/read` respectively, and for the read a `params` map carrying a single `uri` key.

Add two `Scenario` entries. `resources-list`: `Index: true`, requests
`initializeRequest(1)` then `resourcesListRequest(2)`, `ExpectTools: 8`. `resources-read-explore`:
`Index: true`, requests `initializeRequest(1)` then `resourceReadRequest(2, "codegraph://tools/explore")`,
`ExpectTools: 8`. Both scenarios carry exactly one async request and it is last — this is not
stylistic caution, it is now a VERIFIED constraint rather than the `[ASSUMED]` one 05-RESEARCH.md
recorded as A2. go-sdk dispatches every call except `initialize` through `jsonrpc2.Async(ctx)`
(server.go:1910-1915), so `resources/list` and `resources/read` race each other and race any
request queued after them exactly as `tools/call` does. The `toolslist-repeat` ordering flake
already open as a MAJOR todo in STATE.md is this same class; do not add more of it. Comment the
constraint on the new scenarios with that source citation so the next author does not have to
re-derive it.

Bump `ExpectedScenarioCount` from 29 to 31 in the same change, and extend its doc comment with
the two additions and their reason, matching the running-tally style the comment already uses.

Add `assertResourceCacheControl` to `test/wireoracle/anchors.go`, modelled directly on
`assertDiscoverCacheControl`: decode only the id-2 response frame's `result.cacheScope` and
`result.ttlMs` out of the captured stdout, and fail naming the scenario and quoting the observed
line unless `cacheScope` is `private` and `ttlMs` is absent or zero. Register it as two anchors,
one against `resources-list` and one against `resources-read-explore`, so Task 1's cacheScope
decision is pinned on the wire and cannot silently revert to the SDK's `public` default.

Capture both new goldens with the capture CLI, exactly as Task 2 did. Then update
`test/wireoracle/COVERAGE-BASELINE.md`: bump the header scenario count to 31, add a new
`### MCP Resources (2) — plan 05-01` category table describing both scenarios, update the Total
line's arithmetic, and add a bullet to the "History: how this corpus grew across phases" section
recording that Phase 5 turned `capabilities.resources` on, moved 25 transcripts under one named
cause, and added these two scenarios. Note in that bullet that plan 05-04 extends the category
further, so a reader mid-phase is not misled by a partial table.
  </action>

  <verify>
    <automated>go test ./test/wireoracle/... -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./test/wireoracle/... -count=1` passes, including `TestScenarioCountIsExact` at 31 and `TestTranscriptSetMatchesScenarioSet`.
    - `rg -n 'ExpectedScenarioCount = 31' test/wireoracle/scenarios.go` matches.
    - `ls testdata/wireoracle/transcripts/ | wc -l` outputs 31.
    - `rg -c '"cacheScope":"private"' testdata/wireoracle/transcripts/resources-list.golden` returns at least 1, and the same for `resources-read-explore.golden`.
    - `rg -c '"mimeType":"text/markdown"' testdata/wireoracle/transcripts/resources-read-explore.golden` returns at least 1, and `rg -c '"uri":"codegraph://tools/explore"' testdata/wireoracle/transcripts/resources-list.golden` returns at least 1.
    - `go test ./test/wireoracle/... -run TestSpecAnchorsHold -count=1 -v` shows the two new resource cacheScope anchors executing and passing.
    - `rg -n 'Scenario count' test/wireoracle/COVERAGE-BASELINE.md` shows 31, and the file's Total line arithmetic sums to 31.
    - `go test ./... -count=1` passes.
  </acceptance_criteria>

  <done>
    A real `serve --mcp` subprocess, driven over raw stdio by the SDK-independent oracle, is frozen advertising `codegraph://tools/explore` from `resources/list` and returning non-empty `text/markdown` from `resources/read` — with `cacheScope: private` anchored on both.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → `serve --mcp` stdio | Untrusted JSON-RPC crosses here, including the client-supplied `resources/read` URI string |
| Build tree → binary | `internal/mcp/resources/*.md` is compiled into the binary at build time; nothing reads it from disk at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `resources/read` response body (`internal/mcp/resources/*.md`) | medium | mitigate | Resource content is a compile-time `go:embed` artifact with no interpolation of any kind, inheriting the constraint `server.go:47-49` already places on `instructions` — no repository path, resolved index root, hostname, username, or environment value. Structurally impossible to vary per deployment. Plan 05-03 adds `TestResourceContentCarriesNoHostFacts` to make it enforced rather than merely intended. |
| T-05-02 | Tampering / Information Disclosure | `resources/read` URI lookup | low | accept | Structurally avoided rather than defended: handlers close over bytes selected at REGISTRATION time via exact-match `resourceURIFor`; no client-supplied string ever reaches `os` or `path/filepath` at request time. A URI outside the registered set fails go-sdk's own exact-URI lookup with `ResourceNotFoundError` (`-32602`) before any codegraph code runs (`server.go:1020-1025`). `TestResourcesReadIsNotVacuous` in Task 1 exercises this; plan 05-04 freezes it on the wire. codegraph deliberately does NOT use go-sdk's `readFileResource` helper, which is the only path that would resolve a URI against a filesystem root. |
| T-05-03 | Spoofing / Repudiation | `capabilities.resources` advertisement | low | accept | The advertised capability is set to an explicit zero `ResourceCapabilities{}`. Leaving it nil would let go-sdk auto-advertise `listChanged: true`, a capability this phase does not implement — a server making a protocol promise it cannot keep. The explicit value plus the 25-transcript frozen record is the control; no runtime mitigation is applicable. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this phase — zero new Go module dependencies (see `COVERAGE.md`). The package-legitimacy gate does not apply. |
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` clean.
- `go test ./internal/mcp/... -count=1` green, including all four new `TestResources*` tests.
- `go test ./... -count=1` green.
- `task test:wireoracle` green at 31 scenarios / 31 transcripts.
- `git diff --name-only testdata/wireoracle/transcripts/` accounts for exactly 25 re-frozen files plus 2 new ones, with one named cause and its count recorded in the commit message.
</verification>

<success_criteria>
- A real `serve --mcp` subprocess advertises `capabilities.resources` in `initialize`, lists `codegraph://tools/explore` with `mimeType: text/markdown`, and returns non-empty markdown for it — all three observed on the wire by the SDK-independent oracle (RSRC-01, RSRC-02).
- `BuildServer(false, ...)` serves the same resource, proven by behavior (RSRC-03).
- `cacheScope: "private"` is anchored on both new resource transcripts.
- Main is green; no re-freeze is left outstanding at the end of this plan.
</success_criteria>

<output>
Create `.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-01-SUMMARY.md` when done.
</output>
