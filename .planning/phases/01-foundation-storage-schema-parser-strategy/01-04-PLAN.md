---
phase: 01-foundation-storage-schema-parser-strategy
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - testdata/golden/capture.sh
  - testdata/golden/ts-version.txt
  - testdata/golden/ts-schema.sql
  - testdata/golden/ts-schema.dump.sql
  - testdata/golden/README.md
  - testdata/golden/golden_test.go
  - testdata/golden/corpus/
autonomous: true
requirements: [MCP-04]
must_haves:
  truths:
    - "The TS .codegraph/ SQLite schema DDL is captured from the live TS CodeGraph v1.3.1 as committed fixtures"
    - "Golden JSON snapshots of codegraph_explore and companion tool outputs are captured on a small pinned corpus"
    - "Non-deterministic fields (FTS score floats, *_at/*At timestamps) are stripped so fixtures reproduce byte-for-byte"
    - "The exact TS version and capture date are recorded alongside the fixtures"
  artifacts:
    - path: "testdata/golden/capture.sh"
      provides: "reproducible capture harness shelling out to codegraph CLI + sqlite3 (D-06), with determinism-stripping"
      contains: "codegraph"
    - path: "testdata/golden/ts-schema.sql"
      provides: "TS .codegraph/ schema DDL (.schema) — ground truth for Phase 7 migration reader"
      min_lines: 5
    - path: "testdata/golden/golden_test.go"
      provides: "TestGoldenFixturesExist smoke test (non-empty, determinism-stripped)"
      contains: "TestGoldenFixturesExist"
    - path: "testdata/golden/ts-version.txt"
      provides: "recorded TS version + capture date (D-06)"
      contains: "1.3.1"
  key_links:
    - from: "testdata/golden/capture.sh"
      to: "/opt/homebrew/bin/codegraph"
      via: "shells out to the live TS CLI + sqlite3 against a real .codegraph/ index"
      pattern: "codegraph"
---

<objective>
Capture the time-sensitive TS ground truth (D-06) while the live TS CodeGraph v1.3.1 install is still available: (a) the `.codegraph/` SQLite schema DDL (`.schema` + a representative `.dump`) from a real aged index, and (b) golden JSON snapshots of `codegraph_explore` and the companion tool outputs on a small, pinned corpus (D-06a: a compact Go repo plus the `colbymchenry/codegraph` repo). Commit both as version-pinned fixtures under `testdata/golden/`, with non-deterministic fields stripped so they reproduce byte-for-byte.

Purpose: This is the ground truth Phase 3 (MCP output-shape parity, MCP-04), Phase 4 (sync), and Phase 7 (migration reader) diff against. Capture must happen now — the live TS version will drift or be uninstalled. This plan produces fixtures + a reproducible harness + a smoke test; it does NOT build any converter (that is Phase 7).
Output: capture harness, DDL fixtures, golden JSON corpus, version stamp, README documenting volatile fields, and a smoke test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-storage-schema-parser-strategy/01-CONTEXT.md
@.planning/phases/01-foundation-storage-schema-parser-strategy/01-RESEARCH.md
@.planning/phases/01-foundation-storage-schema-parser-strategy/01-VALIDATION.md
</context>

<artifacts_produced>
Produces `testdata/golden/`: `capture.sh` (the reproducible harness), `ts-version.txt` (TS v1.3.1 + capture date), `ts-schema.sql` and `ts-schema.dump.sql` (captured DDL), `README.md` (documents which fields are volatile and stripped), `corpus/` (per-repo golden JSON of `codegraph_explore` + companion tools), and `golden_test.go` (`TestGoldenFixturesExist`). These fixtures are the parity oracle consumed by Phase 3 (MCP-04), Phase 4, and Phase 7.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author the capture harness and capture TS schema DDL + golden corpus</name>
  <files>testdata/golden/capture.sh, testdata/golden/ts-version.txt, testdata/golden/ts-schema.sql, testdata/golden/ts-schema.dump.sql, testdata/golden/corpus/, testdata/golden/README.md</files>
  <read_first>
    - .planning/phases/01-foundation-storage-schema-parser-strategy/01-RESEARCH.md §"Common Pitfalls" Pitfall 1 (non-deterministic fixtures: FTS BM25 `score` floats and `updatedAt`/`indexed_at` timestamps) and Pitfall 2 (edge-dedup bug #1034 in the captured schema)
    - .planning/phases/01-foundation-storage-schema-parser-strategy/01-RESEARCH.md §"Environment Availability" (codegraph v1.3.1 at /opt/homebrew/bin/codegraph; sqlite3 3.54.0; real indexes at weft/fovea/holomush) and §"Open Questions" 1 and 2 (corpus repo choice; re-index --force first)
    - .planning/phases/01-foundation-storage-schema-parser-strategy/01-CONTEXT.md D-06, D-06a (corpus = compact Go repo + colbymchenry/codegraph)
  </read_first>
  <action>
    Write `testdata/golden/capture.sh` (bash, `set -euo pipefail`) that, per D-06/D-06a: (1) selects a compact Go corpus repo (use the local `weft` repo per RESEARCH Open Question 1, or an equivalent small committable Go repo) and the `colbymchenry/codegraph` TS repo; (2) re-runs `codegraph index --force` on the corpus index first (RESEARCH Open Question 2 — the on-disk index carries a stale `builtWithVersion` stamp; force a clean v1.3.1 end-to-end build) before capturing; (3) captures the SQLite schema via `sqlite3 <path>/.codegraph/<db> '.schema'` into `ts-schema.sql` and a representative `sqlite3 ... '.dump'` into `ts-schema.dump.sql`; (4) runs `codegraph explore` / `codegraph node` / `codegraph search` / `codegraph status --json` and the companion tools on the corpus, writing JSON into `corpus/<repo>/<tool>.json`; (5) pipes every captured JSON through a normalization step that strips the volatile fields RESEARCH Pitfall 1 identified — the FTS BM25 `score` float and all `*_at` / `*At` epoch-millisecond timestamps — so a second capture run reproduces byte-for-byte. Record the exact CLI version (`codegraph version`) and capture date into `ts-version.txt`. Write `testdata/golden/README.md` documenting: the TS version captured, which fields are volatile/stripped and why, the corpus repos + their pinned commit/state, and a note flagging the historical edge-dedup bug #1034 (Pitfall 2) that Phase 2 edge-identity design must account for. Run the harness to actually produce the fixtures (do not commit an empty skeleton). Keep the committed corpus small enough to live in git and re-run deterministically.
  </action>
  <verify>
    <automated>test -s testdata/golden/ts-schema.sql && test -s testdata/golden/ts-version.txt && ls testdata/golden/corpus/*/*.json >/dev/null 2>&1 && grep -q '1.3.1' testdata/golden/ts-version.txt</automated>
  </verify>
  <acceptance_criteria>
    - `testdata/golden/ts-schema.sql` is non-empty and contains SQLite DDL (`CREATE TABLE`)
    - `testdata/golden/ts-version.txt` records `1.3.1` and a capture date
    - `testdata/golden/corpus/` contains at least one repo directory with `codegraph_explore` (explore) JSON plus companion-tool JSON
    - Captured JSON contains no raw FTS `score` float and no `*_at`/`*At` timestamp values (stripped per Pitfall 1)
    - `testdata/golden/README.md` documents volatile fields and the #1034 edge-dedup note
    - `testdata/golden/capture.sh` is re-runnable (`set -euo pipefail`, `codegraph index --force` first)
  </acceptance_criteria>
  <done>Ground-truth DDL + golden tool outputs are captured, determinism-stripped, version-stamped, and committed under testdata/golden/.</done>
</task>

<task type="auto">
  <name>Task 2: Add the golden-fixtures smoke test</name>
  <files>testdata/golden/golden_test.go</files>
  <read_first>
    - .planning/phases/01-foundation-storage-schema-parser-strategy/01-VALIDATION.md (Success Criterion 4 row: TestGoldenFixturesExist)
    - .planning/phases/01-foundation-storage-schema-parser-strategy/01-RESEARCH.md §"Validation Architecture" (smoke-test intent) and §"Common Pitfalls" Pitfall 1
    - testdata/golden/ (fixtures produced in Task 1)
  </read_first>
  <action>
    Create `testdata/golden/golden_test.go` with `TestGoldenFixturesExist` (package `golden`) that asserts, as a fast Go smoke test: `ts-schema.sql` exists and is non-empty; `ts-version.txt` exists and contains a version string; at least one `corpus/*/*.json` fixture exists and parses as valid JSON; and — the determinism guard — none of the corpus JSON fixtures contain a raw FTS `score` numeric field or a `*_at`/`*At` timestamp key (scan the parsed JSON keys). This encodes RESEARCH Pitfall 1 as an automated invariant so a future re-capture that forgets to strip volatile fields fails loudly. Use only the standard `testing`, `os`, `encoding/json`, `path/filepath` packages.
  </action>
  <verify>
    <automated>go test ./testdata/golden/ -run TestGoldenFixturesExist -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./testdata/golden/ -run TestGoldenFixturesExist` exits 0
    - The test fails if a corpus JSON fixture contains a raw `score` float or a `*_at`/`*At` timestamp key
    - The test asserts `ts-schema.sql` and `ts-version.txt` are present and non-empty
    - `testdata/golden/golden_test.go` declares `func TestGoldenFixturesExist`
  </acceptance_criteria>
  <done>A fast automated smoke test guarantees the golden fixtures exist, parse, and stay determinism-stripped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external TS CLI + on-disk SQLite → committed fixtures | Captured data becomes a committed parity oracle; correctness/reproducibility matter, not confidentiality |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering (data integrity) | non-deterministic golden fixtures | medium | mitigate | Volatile fields (FTS `score`, `*_at`/`*At` timestamps) stripped by `capture.sh` and enforced by `TestGoldenFixturesExist` (RESEARCH Pitfall 1) — prevents spurious parity failures in Phases 3/4/7 |
| T-01-08 | Information Disclosure | committing a private local repo corpus | low | mitigate | Corpus kept small and reviewable; `README.md` records exactly which repos/state were captured. If the local `weft` repo is private, substitute a pinned public repo or a purpose-built minimal Go fixture per D-06a discretion |
| T-01-09 | Repudiation | unrecorded capture provenance | low | mitigate | `ts-version.txt` stamps exact CLI version + capture date; `README.md` records corpus provenance |
</threat_model>

<verification>
- `go test ./testdata/golden/ -run TestGoldenFixturesExist -v` passes
- `ts-schema.sql`, `ts-schema.dump.sql`, `ts-version.txt` present and non-empty; version == 1.3.1
- `corpus/` holds golden JSON for explore + companion tools, determinism-stripped
- `capture.sh` re-runs deterministically (`index --force` first, volatile-field stripping)
</verification>

<success_criteria>
A golden-output corpus and the TS `.codegraph/` schema DDL are captured from the live TS CodeGraph v1.3.1 and committed as reproducible, version-pinned fixtures — the parity ground truth for later phases (Success Criterion 4 / MCP-04 upstream).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-storage-schema-parser-strategy/01-04-SUMMARY.md` when done.
</output>
