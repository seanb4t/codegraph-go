---
phase: 01-foundation-storage-schema-parser-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/graphstore/doc.go
  - internal/schema/doc.go
  - internal/parser/doc.go
  - .gitignore
autonomous: true
requirements: [INDX-05, ARCH-01]
must_haves:
  truths:
    - "go build ./... and go test ./... run against a real Go module (github.com/seanb4t/codegraph-go)"
    - "All Phase 1 dependencies are pinned at the researcher-verified versions, on the correct import paths (pebble/v2, not bare pebble)"
    - "The internal/graphstore, internal/schema, internal/parser packages exist and compile empty"
  artifacts:
    - path: "go.mod"
      provides: "Module declaration + pinned require directives (D-01, D-02, D-05)"
      contains: "github.com/cockroachdb/pebble/v2"
    - path: "internal/graphstore/doc.go"
      provides: "graphstore package boundary (D-04 target package)"
      contains: "package graphstore"
    - path: "internal/schema/doc.go"
      provides: "schema package boundary (D-02 target package)"
      contains: "package schema"
    - path: "internal/parser/doc.go"
      provides: "parser package boundary (D-05b target package)"
      contains: "package parser"
  key_links:
    - from: "go.mod"
      to: "github.com/cockroachdb/pebble/v2"
      via: "require directive on the /v2 module path (Pitfall 4 guard)"
      pattern: "cockroachdb/pebble/v2"
---

<objective>
Bootstrap the greenfield Go module and package skeleton that every other Phase 1 plan builds on. Initialize `github.com/seanb4t/codegraph-go`, pin the exact researcher-verified dependency versions on their correct import paths (critically `github.com/cockroachdb/pebble/v2`, not the stale bare `pebble` v1 path — see RESEARCH Pitfall 4), and create the three empty-but-compiling internal packages (`graphstore`, `schema`, `parser`) that Waves 2–3 populate.

Purpose: This is Wave 0 of the Validation strategy — no test in the phase can compile until `go.mod` and the package skeleton exist. It is pure enabling work; it ships no behavior.
Output: `go.mod`/`go.sum` with pinned deps, three package `doc.go` stubs, updated `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-storage-schema-parser-strategy/01-CONTEXT.md
@.planning/phases/01-foundation-storage-schema-parser-strategy/01-RESEARCH.md
@.claude/CLAUDE.md
</context>

<artifacts_produced>
This plan creates: the Go module (`go.mod`, `go.sum`), and empty package boundaries `internal/graphstore`, `internal/schema`, `internal/parser`. It pins (does not yet import) the full Phase 1 dependency set: `github.com/cockroachdb/pebble/v2@v2.1.6` (D-01), `google.golang.org/protobuf@v1.36.11` (D-02), `github.com/tree-sitter/go-tree-sitter@v0.25.0` + `tree-sitter-go@v0.25.0` + `tree-sitter-python@v0.25.0` (D-05), `github.com/tetratelabs/wazero@v1.12.0` (D-05 spike arm), `golang.org/x/tools@latest` (D-04a import-graph test). Downstream symbols (`GraphStore`, protobuf messages, key encoders, `Parser`, spike harness, golden fixtures) are produced by later plans against these pinned versions.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Initialize the Go module and pin Phase 1 dependencies</name>
  <files>go.mod, go.sum, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-storage-schema-parser-strategy/01-RESEARCH.md §"Standard Stack" (verified versions + Installation block) and §"Common Pitfalls" Pitfall 4 (pebble v1/v2 import-path confusion)
    - .claude/CLAUDE.md §"Storage — the new graph format" and §"The Parser Decision"
    - ./.gitignore (existing file — extend, do not clobber)
  </read_first>
  <action>
    Run `go mod init github.com/seanb4t/codegraph-go`. Then `go get` each dependency at the exact version verified in RESEARCH §"Standard Stack", using the researcher's import paths: `github.com/cockroachdb/pebble/v2@v2.1.6` (D-01 — the `/v2` suffix is mandatory; the bare `github.com/cockroachdb/pebble` path is the deprecated v1 module per Pitfall 4), `google.golang.org/protobuf@v1.36.11` (D-02), `github.com/tree-sitter/go-tree-sitter@v0.25.0`, `github.com/tree-sitter/tree-sitter-go@v0.25.0`, `github.com/tree-sitter/tree-sitter-python@v0.25.0` (D-05 — Python is the external-scanner spike partner, not C#), `github.com/tetratelabs/wazero@v1.12.0` (D-05 spike Option B), and `golang.org/x/tools@latest` (D-04a). Do NOT run `go mod tidy` yet — nothing imports these packages until Wave 2, and tidy would strip the requires; leave them as explicit direct requires for later plans to consume. Set the `go` directive to the installed toolchain line (1.26). Extend `.gitignore` to exclude Go build/test artifacts (`/dist/`, `*.test`, coverage output) without removing existing entries.
  </action>
  <verify>
    <automated>go list -m github.com/cockroachdb/pebble/v2 && go list -m google.golang.org/protobuf && go list -m github.com/tree-sitter/go-tree-sitter && go list -m github.com/tree-sitter/tree-sitter-python && go list -m github.com/tetratelabs/wazero</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` declares `module github.com/seanb4t/codegraph-go`
    - `go list -m github.com/cockroachdb/pebble/v2` prints `github.com/cockroachdb/pebble/v2 v2.1.6` (exit 0)
    - `go list -m github.com/cockroachdb/pebble` (bare path) exits non-zero — the v1 module is NOT a dependency (Pitfall 4 guard)
    - `go.mod` require block lists protobuf v1.36.11, go-tree-sitter v0.25.0, tree-sitter-go v0.25.0, tree-sitter-python v0.25.0, wazero v1.12.0, and golang.org/x/tools
    - `.gitignore` still contains its pre-existing entries
  </acceptance_criteria>
  <done>The module resolves all six pinned dependencies on their correct import paths; pebble is present only on the `/v2` path.</done>
</task>

<task type="auto">
  <name>Task 2: Create the three internal package boundaries as empty compiling stubs</name>
  <files>internal/graphstore/doc.go, internal/schema/doc.go, internal/parser/doc.go</files>
  <read_first>
    - .planning/phases/01-foundation-storage-schema-parser-strategy/01-RESEARCH.md §"Architecture Patterns" → "Recommended Project Structure" (the directory tree) and §"Architectural Responsibility Map"
    - .planning/phases/01-foundation-storage-schema-parser-strategy/01-CONTEXT.md D-04 (GraphStore boundary), D-02 (schema), D-05b (parser interface)
  </read_first>
  <action>
    Create one `doc.go` per package with only a package clause and a one-line package doc comment describing the boundary role from the Architectural Responsibility Map: `internal/graphstore` = the sole legal caller of pebble/v2 (D-04); `internal/schema` = protobuf serialization layer, schema-versioned records (D-02); `internal/parser` = narrow backend-swappable Parser boundary (D-05b). Do NOT declare any interfaces, types, or imports here — those are owned by Waves 2–3 (Plan 01-02 schema, Plan 01-05 keys, Plan 01-06 store, Plan 01-03 parser). These stubs exist only so the packages compile empty and later plans add files without a chicken-and-egg import problem. Keep the package doc comment plain prose — do not restate any identifier a later negative-grep gate checks for.
  </action>
  <verify>
    <automated>go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 with the three packages present
    - `internal/graphstore/doc.go` declares `package graphstore` and imports nothing
    - `internal/schema/doc.go` declares `package schema` and imports nothing
    - `internal/parser/doc.go` declares `package parser` and imports nothing
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>Three empty internal packages compile; no interfaces or engine imports leak into the stubs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go module proxy → local build | Third-party module code enters the build via `go get`; versions must be the audited ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `go get` dependency resolution | medium | accept | Go modules are out of scope for the npm/pip/cargo legitimacy gate; RESEARCH §"Package Legitimacy Audit" already verified every module against the official Go module proxy (`proxy.golang.org`) — all org-maintained (CockroachDB, Google, tree-sitter org, Tetrate, Go team), all `OK`/Approved, zero `[SUS]`/`[SLOP]`. Versions are pinned exactly; `go.sum` records checksums. No blocking-human checkpoint required for Go modules. |
| T-01-04 | Tampering | pebble import path | low | mitigate | Explicit `/v2` pin + `go list -m` verification prevents silently building against the deprecated v1 module (Pitfall 4) |
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` exit 0
- `go list -m github.com/cockroachdb/pebble/v2` resolves; bare `github.com/cockroachdb/pebble` does not
- All six pinned dependencies appear in `go.mod` at the verified versions
</verification>

<success_criteria>
The Go module exists, all Phase 1 dependencies are pinned on correct import paths, and the three internal package boundaries compile empty — unblocking every Wave 2 plan.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-storage-schema-parser-strategy/01-01-SUMMARY.md` when done.
</output>
