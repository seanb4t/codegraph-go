---
phase: 01-protocol-scoping-the-sdk-independent-wire-oracle
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/mcp/session_line.go
  - internal/mcp/session_line_test.go
  - internal/mcp/archtest/protocol_version_test.go
  - internal/mcp/archtest/protocol_version_selftest_test.go
  - internal/cli/archtest/mcp_sdk_confinement_test.go
  - internal/cli/archtest/mcp_sdk_selftest_test.go
  - internal/mcp/server_test.go
  - testdata/golden/golden_parity_test.go
  - test/integration/mcp_stdout_purity_test.go
  - test/integration/watch_live_sync_test.go
  - test/integration/watch_default_test.go
  - test/integration/worktree_notice_test.go
autonomous: true
requirements: [VRFY-02, VRFY-03, SDK-02]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The server's declared protocol version reads from the repo-owned literal `internal/mcp.ProtocolVersion`, and a CI-run archtest fails if any externally-defined protocol-version constant is referenced anywhere in the tree, including `_test.go` files and `testdata/golden` (VRFY-02)"
    - "The VRFY-02 guard forbids the class — an externally-defined *constant* whose name matches a protocol-version pattern — rather than one library's import path, so it still fires after Phase 2's SDK swap; its scope is `*types.Const` only, so functions, types, methods, package-level vars and struct fields named `ProtocolVersion` are not false positives (CONTEXT Claude's Discretion)"
    - "The guard's limitation is documented rather than overstated: a name heuristic cannot catch an SDK constant spelled `LatestVersion` or `CurrentRevision`, so the spelling-independent complement is `internal/cli/archtest`'s import-path list, and Phase 2 must confirm the replacement SDK's constant is caught (CONTEXT Claude's Discretion)"
    - "Every VRFY-02 violation message names the rule, the offending package/file/line/identifier, and the two sanctioned escapes, so an eventual false positive is self-explanatory (VRFY-02)"
    - "The VRFY-02 guard is demonstrated red against a confirmed-applied synthetic violation injected via `packages.Config.Overlay` before it is trusted green"
    - "`internal/cli`'s direct production imports contain no MCP SDK package, enforced by a `go/packages` archtest with a self-defeat guard rather than by a text search (SDK-02)"
    - "A `clientInfo.name` or `version` containing a newline, carriage return, or other control character cannot inject a second diagnostic line into the session-line stream (VRFY-03, RESEARCH Pitfall 3)"
    - "A `clientInfo` field longer than 256 bytes is truncated in the session line and the line still parses as prefix plus key=value pairs (VRFY-03 boundary edge)"
    - "When the client omits `clientInfo` or sends empty name/version, the session line prints the literal unknown marker rather than an empty or malformed key (VRFY-03 empty edge)"
    - "Client-supplied fields are truncated on a UTF-8 rune boundary, never mid-rune, and invalid UTF-8 bytes are replaced with U+FFFD (VRFY-03 encoding edge)"
    - "VRFY-03's always-on guarantee is structural, not conventional: `NewStdioServer` panics on a nil session-log writer with `io.Discard` named as the explicit opt-out, so no future caller can silently disable the milestone's only mitigation for a spec-sanctioned silent version mismatch (VRFY-03)"
    - "The VRFY-02 archtest holds no lock and mutates nothing; `go test ./internal/mcp/archtest/... -count=2` yields identical results (VRFY-02 concurrency edge)"
    - statement: "Concurrent or repeated `initialize` on one stdio session never produces a partially-written or interleaved session line"
      verification: backstop
  artifacts:
    - path: "internal/mcp/archtest/protocol_version_test.go"
      provides: "VRFY-02 identity-agnostic protocol-version constant guard"
      contains: "TestNoExternalProtocolVersionConstantReferences"
    - path: "internal/mcp/archtest/protocol_version_selftest_test.go"
      provides: "Overlay-injected self-defeat proof that the VRFY-02 guard can fire"
    - path: "internal/cli/archtest/mcp_sdk_confinement_test.go"
      provides: "SDK-02 negative-import guard on internal/cli"
      contains: "TestInternalCLIImportsNoMCPSDK"
    - path: "internal/mcp/session_line_test.go"
      provides: "D-14/D-16 session-line format contract and adversarial sanitization tests"
  key_links:
    - from: "internal/mcp/archtest/protocol_version_test.go"
      to: "internal/mcp/protocol_version.go"
      via: "the guard permits this module's own literal and forbids every external one"
      pattern: "modulePathPrefix"
    - from: "internal/cli/archtest/mcp_sdk_confinement_test.go"
      to: "internal/cli/serve.go"
      via: "direct-import scan of package internal/cli"
      pattern: "internal/cli"
  prohibitions:
    - statement: "A guard must never be reported as passing without first having been demonstrated red against a confirmed-applied mutation; a mutation that no-ops proves nothing."
    - statement: "An enforcement gate for this phase must never be implemented as a text search over source files — this repository has already shipped an inverted text-search gate."
---

<objective>
Make the phase's two structural guarantees permanent and non-vacuous: the repo owns its protocol
version literal and no SDK-owned equivalent is referenced anywhere in the tree (VRFY-02), and
`internal/cli` never re-acquires a direct MCP SDK import (SDK-02). Add the session-line contract
tests that pin D-14's format and prove the D-16 additive-only contract cannot be broken by a
hostile or malformed `clientInfo`.

Purpose: a dependency bump must never move wire behavior silently, and the one production-code SDK
leak must stay closed after Phase 2 swaps the SDK underneath it.

Output: two new archtest packages with self-defeat companions, the six known SDK-constant sites
migrated onto the repo-owned literal, and a session-line contract test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-CONTEXT.md
@.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-RESEARCH.md
@.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-PATTERNS.md
@.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| package | `internal/mcp/archtest` |
| func | `internal/mcp/archtest.isExternalProtocolVersionConstant` |
| func | `internal/mcp/archtest.loadWholeModule` |
| func | `internal/mcp/archtest.scanForProtocolVersionRefs` |
| var | `internal/mcp/archtest.protocolVersionNamePattern` |
| const | `internal/mcp/archtest.modulePathPrefix` |
| test | `TestNoExternalProtocolVersionConstantReferences` |
| test | `TestProtocolVersionGuardCatchesOverlaidViolation` |
| test | `TestProtocolVersionGuardHelpersFailLoudly` |
| test | `TestIsExternalProtocolVersionConstantMatrix` |
| func | `internal/mcp/archtest.isModuleOwnedPath` |
| package | `internal/cli/archtest` |
| var | `internal/cli/archtest.forbiddenMCPSDKPrefixes` |
| func | `internal/cli/archtest.assertMCPSDKImporterExists` |
| test | `TestInternalCLIImportsNoMCPSDK` |
| test | `TestInternalCLIImportsNoMCPSDK_PlantedImportIsError` |
| file | `internal/mcp/session_line_test.go` |
| test | `TestSessionLineFormat` |
| test | `TestSessionLineSanitizesHostileClientInfo` |
| test | `TestSanitizeClientFieldFailLoudly` |
| test | `TestSessionLineIsAlwaysOnByConstruction` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session-line contract and adversarial sanitization tests (VRFY-03, D-14/D-16)</name>

  <files>internal/mcp/session_line_test.go, internal/mcp/session_line.go</files>

  <read_first>
    - `internal/mcp/session_line.go` — the current `formatSessionLine` and `sanitizeClientField` implementations landed by the tracer
    - `internal/mcp/server.go` — where the `AddAfterInitialize` hook is registered and how `toolCount` is computed
    - `internal/upgrade/release_workflow_shape_test.go:583` — `TestWorkflowSourceHelpersFailLoudly`, the table-of-`{name, fn}` convention for proving a parser helper rejects malformed input
    - `internal/mcp/server_test.go` — the set-equality assertion style used across this package
  </read_first>

  <behavior>
    Written test-first; each case below must be observed red against a deliberately weakened
    `sanitizeClientField` before the file is trusted green.

    - `TestSessionLineFormat`: `formatSessionLine("2025-11-25", "2025-11-25", "claude-code", "1.2.3", 1)` returns exactly `codegraph: mcp-session requested=2025-11-25 negotiated=2025-11-25 client=claude-code/1.2.3 tools=1` followed by a single newline. Key order is fixed and asserted positionally, not by set membership.
    - Same test, equal-versions adjacency case: when requested and negotiated are the same string, both keys are still present with their own values — never collapsed into one key.
    - Same test, zero case: `tools=0` is emitted when the count is zero; the key is never dropped.
    - `TestSessionLineSanitizesHostileClientInfo`, table-driven, one case per hostile shape:
      a client name containing a line feed; one containing a carriage return; one containing a
      NUL; one containing an embedded space; one containing the session-line prefix itself; one
      that is 1024 bytes of ASCII; one that is 300 bytes of multi-byte runes; one containing an
      invalid UTF-8 byte sequence; and one that is the empty string. For every case the produced
      line contains exactly one newline (at the end), begins with the prefix, splits into exactly
      four key=value fields, and — for the empty case — carries the literal unknown marker rather
      than an empty value. The 300-byte multi-byte case additionally asserts the truncated value
      is valid UTF-8, proving truncation lands on a rune boundary.
    - `TestSanitizeClientFieldFailLoudly`-shaped table: each helper this file introduces (a
      session-line parser used by the tests, if one is added) rejects malformed input with a
      non-nil error rather than returning a zero value.
    - `TestSessionLineIsAlwaysOnByConstruction`: `mcp.NewStdioServer` called with a nil session-log
      writer panics, and the panic message names `io.Discard` as the explicit opt-out. This is the
      permanent encoding of the tracer's nil-guard: VRFY-03's always-on line is the milestone's only
      mitigation for a spec-sanctioned silent version mismatch, and "always on" must not degrade to
      "on because today's single caller happens to pass `cmd.ErrOrStderr()`". Addresses review
      concern: "the session line is always-on only by convention at one call site."
  </behavior>

  <action>
    Add `internal/mcp/session_line_test.go` covering every case in the behavior block. Run each
    case against the current implementation first; where a case fails, harden
    `sanitizeClientField` in `internal/mcp/session_line.go` until it passes — do not weaken the
    assertion to match the implementation.

    The load-bearing property is D-14's parseability guarantee: the format is greppable by prefix
    and parseable without a JSON decoder precisely because it assumes no embedded newline and no
    embedded space in a value. A client that supplies a name carrying a line feed followed by the
    session-line prefix must not be able to synthesize a second, fabricated diagnostic line in the
    same stream (T-03-01, RESEARCH Pitfall 3). That is why the hostile table includes the prefix
    string itself as a client name.

    If the tests reveal that the tracer's sanitizer stops short of any of: control-character
    replacement, space replacement, invalid-UTF-8 replacement with U+FFFD, rune-boundary
    truncation at 256 bytes, or the unknown marker for an empty input — extend it. Keep the
    truncation limit as a named constant so the boundary is a single documented number rather
    than a magic value repeated across the file.

    Add a doc comment on `sessionLinePrefix` recording the D-16 additive-only contract: the prefix
    and the four key names shipped in this phase never change or disappear; later phases may add
    keys, and both the wire oracle and `tools/mcpaudit` parse defensively so additions do not
    break them. Name the specific consumers so a future renamer sees the blast radius.
  </action>

  <verify>
    <automated>go test ./internal/mcp/... -run 'TestSessionLine|TestSanitizeClientField' -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/mcp/... -run 'TestSessionLine|TestSanitizeClientField' -count=1` exits 0.
    - `internal/mcp/session_line_test.go` contains a table with at least nine hostile-input cases, including one whose client name embeds the session-line prefix and one that is invalid UTF-8.
    - Behavior: `formatSessionLine` output for every hostile case contains exactly one `\n` character and splits on spaces into exactly five tokens after the two-token prefix.
    - Behavior: `formatSessionLine` with an empty client name and empty client version produces a `client=` value containing the unknown marker on both sides of the slash.
    - `internal/mcp/session_line.go` declares the truncation limit as a named constant referenced by `sanitizeClientField`.
    - `go test ./internal/mcp/... -run 'TestSessionLineIsAlwaysOnByConstruction' -count=1` exits 0 — a nil session-log writer panics with a message naming `io.Discard`.
    - `go build ./... && go vet ./...` exit 0.
  </acceptance_criteria>

  <done>The session line's format is pinned by a positional assertion and no client-supplied string can inject a second line, break the four-field shape, or truncate mid-rune.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VRFY-02 — identity-agnostic protocol-version constant guard, proven red first, and the six known sites migrated</name>

  <files>
    internal/mcp/archtest/protocol_version_test.go,
    internal/mcp/archtest/protocol_version_selftest_test.go,
    internal/mcp/server_test.go,
    testdata/golden/golden_parity_test.go,
    test/integration/mcp_stdout_purity_test.go,
    test/integration/watch_live_sync_test.go,
    test/integration/watch_default_test.go,
    test/integration/worktree_notice_test.go
  </files>

  <read_first>
    - `internal/graphstore/archtest/import_graph_test.go` — the flat whole-module `packages.Load` with `Tests: true` and the "resolved zero packages" fail-loud guard; this is the load shape to copy
    - `internal/graphstore/archtest/stdout_confinement_test.go` — the `*ast.SelectorExpr` + `info.Uses` predicate shape (`isOSStdoutRef`, lines 237-254) and the fuller `packages.Config.Mode` needed for syntax/type info
    - `internal/graphstore/archtest/stdout_closure_selftest_test.go` — the `packages.Config.Overlay` technique for injecting a synthetic violation with no on-disk edit; read the whole file
    - `.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-RESEARCH.md` § "Pattern 2" — `isExternalProtocolVersionConstant` written out in full to this repo's conventions
    - `internal/mcp/protocol_version.go` — the repo-owned literal the six sites migrate onto
    - The six current sites, at `internal/mcp/server_test.go:81`, `testdata/golden/golden_parity_test.go:1477`, `test/integration/mcp_stdout_purity_test.go:120`, `test/integration/watch_live_sync_test.go:47`, `test/integration/watch_default_test.go:119`, `test/integration/worktree_notice_test.go:44`
  </read_first>

  <behavior>
    - `TestProtocolVersionGuardCatchesOverlaidViolation` (must be observed RED-then-GREEN in that order): with a synthetic file injected via `packages.Config.Overlay` that imports the SDK's `mcp` package and reads its protocol-version constant, `scanForProtocolVersionRefs` returns at least one violation naming that package. Before trusting the result, assert the overlaid package actually appears in the loaded set — an overlay that silently failed to apply proves nothing.
    - `TestIsExternalProtocolVersionConstantMatrix`, table-driven, one subtest per row — this is the false-positive half of the predicate and the reason it is restricted to `*types.Const`:

      | overlaid reference | expected |
      |---|---|
      | external **const** `mcp.LATEST_PROTOCOL_VERSION` | violation |
      | external **func** call `somepkg.ProtocolVersion()` | allowed |
      | external **struct field** read/write `req.Params.ProtocolVersion` | allowed |
      | external **type** name `somepkg.ProtocolVersion` used in a declaration | allowed |
      | external package-level **var** `somepkg.ProtocolVersion` | allowed, with the reason recorded in a comment: vars are out of scope until a real case forces them in |
      | repo-owned const `mcp.ProtocolVersion` (this module) | allowed |
      | lookalike module path `github.com/seanb4t/codegraph-go-fork` const `ProtocolVersion` | violation — the boundary-aware ownership check must not admit it |
      | external const spelled `LatestVersion` (no `protocol` in the name) | allowed, **recorded as a known heuristic gap** in the same subtest's comment, not silently omitted |

      `req.Params.ProtocolVersion` specifically must keep compiling — it is legitimate wire-shape usage at all four `test/integration` sites today.
    - `TestNoExternalProtocolVersionConstantReferences`: after the six sites are migrated, the whole-module scan returns zero violations.
    - Coverage self-defeat: the same test asserts the loaded package set is non-empty AND contains `github.com/seanb4t/codegraph-go/testdata/golden` — the go tool skips any directory named `testdata` when expanding `./...`, so a scan that only passes `./...` would be blind to the exact file holding one of the six known sites (GOLDEN-01's failure class).
    - `TestProtocolVersionGuardHelpersFailLoudly`: each parsing/classification helper introduced here returns a non-nil error (or a documented false) on empty or malformed input rather than a silent zero value.
  </behavior>

  <action>
    Create package `internal/mcp/archtest` implementing VRFY-02's guard as a `go/packages`
    archtest — the house style, never a text search (this repository has already shipped an
    inverted text-search gate, which is why CONTEXT names an archtest explicitly).

    `loadWholeModule(t, overlay map[string][]byte) []*packages.Package` calls `packages.Load` with
    `Mode: NeedName|NeedFiles|NeedCompiledGoFiles|NeedImports|NeedTypes|NeedSyntax|NeedTypesInfo`,
    `Tests: true`, `Overlay: overlay`, and **two** patterns:
    `github.com/seanb4t/codegraph-go/...` and, explicitly,
    `github.com/seanb4t/codegraph-go/testdata/golden`. Do not add `NeedDeps` unless the predicate
    demonstrably cannot resolve external identifiers without it — measure before adding it
    (RESEARCH Pitfall 5). Record the **command** and an explicitly-labelled indicative measurement
    in the file's doc comment — `go test ./internal/mcp/archtest/... -count=1`, "roughly Ns on the
    author's machine, 2026-08; indicative only, not a threshold" — and put the authoritative number
    in the plan summary. A bare wall-clock figure pinned in source goes stale the moment CI or
    developer hardware differs, and a reader who treats it as a budget will be misled; the summary
    is the dated record. Use the flat scan, not the
    six-package reachability closure `stdout_confinement_test.go` uses: VRFY-02 asks "did anyone
    write this anywhere", not "is this reachable from serve".

    `isExternalProtocolVersionConstant(sel *ast.SelectorExpr, info *types.Info) bool` returns true
    only when ALL of the following hold:
    - `info.Uses[sel.Sel]` resolves and the object's package is non-nil;
    - the object's package path is not owned by this module — checked boundary-aware, not by a raw
      prefix: the path must be neither exactly `github.com/seanb4t/codegraph-go` nor prefixed by
      `github.com/seanb4t/codegraph-go/`, so a lookalike module such as
      `github.com/seanb4t/codegraph-go-fork` is correctly treated as external rather than admitted
      by a naive `strings.HasPrefix`;
    - the object's name matches `protocolVersionNamePattern` (`(?i)protocol.?version`);
    - **and the object is a `*types.Const`.** This is a type-switch on the resolved object, not a
      negative filter. The pre-review draft accepted "any external object except a struct field",
      which admits functions, types, methods and package-level vars — a materially wider net than
      "an SDK-owned protocol-version constant", and the source of the false-positive surface both
      reviewers flagged. If a real case later requires also forbidding a package-level `*types.Var`,
      add it as a second explicit arm with a comment naming the case that forced it; do not
      re-broaden to a negative filter.

    Keying on the identifier NAME plus external-package identity rather than on a list of SDK import
    paths is deliberate: it forbids the class rather than one library's spelling, so it still fires
    after Phase 2 replaces the SDK without allowlist maintenance.

    **Document the heuristic's limit rather than overstating it.** The name pattern cannot catch an
    SDK constant spelled `LatestVersion`, `CurrentRevision`, or `SpecRev` — this guard forbids the
    *named* class, not the *semantic* class, and the earlier claim of "zero-maintenance survival
    across arbitrary future SDKs" was overstated. State in the package doc comment: what the pattern
    does catch, what it provably cannot, and that Phase 2's SDK swap must confirm the replacement
    SDK's protocol-version constant is caught by the pattern or add its spelling deliberately. The
    complementary guard that does not depend on spelling is `internal/cli/archtest`'s import-path
    list (task 3), which is why both exist.

    **Put remediation in the violation message.** Each message must name the rule
    (`VRFY-02: no externally-defined protocol-version constant may be referenced in this module`),
    the offending package/file/line/identifier, and the two sanctioned escapes: vendor the value
    into `internal/mcp.ProtocolVersion` and reference that, or — if the flagged symbol is an
    unrelated dependency's identifier that merely matches the name pattern — record a reviewed
    exception. This guard will false-positive someday and the message is what makes that cheap.
    Addresses review concerns: "the protocol-version predicate does not match its own stated scope"
    (both reviewers), "module ownership checking with a raw prefix should be boundary-aware", and
    "the failure message should name the rule and the escape hatch".

    `scanForProtocolVersionRefs(pkgs []*packages.Package) []string` walks every package's `Syntax`
    trees with `ast.Inspect`, applies the predicate to each `*ast.SelectorExpr`, and returns one
    message per violation naming the package path, the file, the line, and the offending
    identifier.

    Write `protocol_version_selftest_test.go` with the two overlay tests from the behavior block,
    modelled directly on `stdout_closure_selftest_test.go`: resolve the target file's real on-disk
    path via `packages.Load` itself rather than constructing it by hand, so the overlay key
    matches the path `packages.Load` uses internally. Insert the synthetic reference after the
    package clause and preserve every original declaration untouched so the rest of the module
    still type-checks during the overlaid load. Assert the overlaid package is present in the
    loaded set before trusting the violation result — a no-op mutation proves nothing.

    Then migrate the six known sites to `internal/mcp.ProtocolVersion`. Five are outside package
    `mcp` and reference it as `mcp.ProtocolVersion` after adjusting the import; the site in
    `internal/mcp/server_test.go` references it unqualified. Where a file's only remaining use of
    the SDK's `mcp` package was that constant, drop the import entirely; where the file still
    needs SDK types, keep it. Update each site's surrounding comment to say the value is the
    repo-owned literal and that a dependency bump must not move it.

    Run the guard and confirm it is green against the real tree only AFTER the six sites are
    migrated — and only after the overlay self-defeat test has been observed red-then-green. A
    guard trusted before its mutation proof is exactly the class of failure this repo's standing
    decision forbids.

    Record the decision CONTEXT left to discretion: this guard runs on every PR, inside the
    existing `task test:unit` leg, because `internal/mcp/archtest` is a normal package reached by
    `go list ./...`. Justify it with the measured load cost noted in the doc comment; if that
    measurement exceeds two seconds, say so in the doc comment and in the plan summary rather
    than silently accepting it.
  </action>

  <verify>
    <automated>go build ./... &amp;&amp; go vet ./... &amp;&amp; go test ./internal/mcp/archtest/... -count=1 -v &amp;&amp; go test ./internal/mcp/... ./test/integration/... ./testdata/golden/... -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/mcp/archtest/... -count=1` exits 0 and `-v` output shows `TestNoExternalProtocolVersionConstantReferences`, `TestProtocolVersionGuardCatchesOverlaidViolation`, `TestIsExternalProtocolVersionConstantMatrix` (with all eight named subtests), and `TestProtocolVersionGuardHelpersFailLoudly` all passing.
    - `isExternalProtocolVersionConstant` reaches its `true` return only through a `*types.Const` arm; the file contains no predicate branch of the form "external and not a struct field".
    - The violation message produced by `scanForProtocolVersionRefs` names the rule ID `VRFY-02`, the package, file, line and identifier, and both sanctioned escapes (vendor into `internal/mcp.ProtocolVersion`, or record a reviewed exception). Verified by asserting on the message text in `TestProtocolVersionGuardCatchesOverlaidViolation`.
    - `internal/mcp/archtest/protocol_version_test.go`'s package doc comment states what the `(?i)protocol.?version` heuristic cannot catch (an SDK constant spelled `LatestVersion` / `CurrentRevision`) and names `internal/cli/archtest`'s import-path list as the spelling-independent complement. The plan summary repeats that limitation rather than claiming the guard survives arbitrary future SDKs unattended.
    - `go test ./internal/mcp/archtest/... -count=2` exits 0 with identical results, proving the scan is read-only and repeatable.
    - `go test ./internal/mcp/... ./test/integration/... ./testdata/golden/... ./test/wireoracle/... -count=1` exits 0 — every migrated site still compiles and passes.
    - `internal/mcp/archtest/protocol_version_test.go` passes both `github.com/seanb4t/codegraph-go/...` and `github.com/seanb4t/codegraph-go/testdata/golden` to `packages.Load`, and asserts the loaded set contains the `testdata/golden` package.
    - `internal/mcp/archtest/protocol_version_test.go`'s doc comment records the measurement command and an indicative figure explicitly labelled as indicative, and the plan summary records the authoritative measured `packages.Load` wall-clock cost with the date and machine.
    - The plan summary records that the overlay self-defeat test was observed failing before the guard was trusted, quoting the failure message.
    - `go build ./... && go vet ./...` exit 0.
  </acceptance_criteria>

  <done>No externally-defined protocol-version constant is referenced anywhere in the module — including `_test.go` files and `testdata/golden` — enforced by a guard proven able to fire against an injected violation and proven not to fire on legitimate struct-field access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SDK-02 — permanent no-MCP-SDK-in-internal/cli import guard</name>

  <files>internal/cli/archtest/mcp_sdk_confinement_test.go, internal/cli/archtest/mcp_sdk_selftest_test.go</files>

  <read_first>
    - `internal/cli/present/archtest/import_graph_test.go` — `TestNoCharmInServeReachablePackages`, the closest sibling: forbidden-path list, `assertCharmImporterExists` self-defeat guard, and the "resolved fewer packages than requested" fail-loud check
    - `internal/cli/serve.go` — the post-tracer import block and the `mcp.NewStdioServer` bootstrap
    - `internal/mcp/server.go` — the package that legitimately owns the sole SDK import
  </read_first>

  <behavior>
    - `TestInternalCLIImportsNoMCPSDK`: loading `github.com/seanb4t/codegraph-go/internal/cli` with `Tests: false` yields exactly one package whose direct `Imports` map contains no key beginning with any forbidden MCP SDK module prefix. The check is on DIRECT imports, not the transitive closure — `internal/cli` legitimately reaches the SDK through `internal/mcp`, and SDK-02's criterion is specifically that `serve.go` names no SDK package itself.
    - Self-defeat: the same test asserts some package in the module DOES directly import one of the forbidden prefixes (today `internal/mcp`), so the guard cannot pass vacuously because the SDK left the tree entirely or the prefix list went stale.
    - Fail-loud: `packages.Load` resolving zero packages, or more than one, fails the test naming what was resolved.
    - `TestInternalCLIImportsNoMCPSDK_PlantedImportIsError`: with a `packages.Config.Overlay` injecting a synthetic direct SDK import into an `internal/cli` production file, the same predicate reports a violation — the planted-defect companion the repo's `Test<Name>_<WhatIsWrong>IsError` convention requires.
  </behavior>

  <action>
    Create package `internal/cli/archtest` (a sibling of the existing `internal/cli/present/archtest`).

    Declare `var forbiddenMCPSDKPrefixes = []string{"github.com/mark3labs/mcp-go", "github.com/modelcontextprotocol/go-sdk"}` with a doc comment stating that the second entry is
    deliberately forward-declared: Phase 2 replaces the first with the second, and a guard that
    named only today's dependency would go silently vacuous at exactly the moment it matters most.
    Any future MCP SDK must be added here, and the self-defeat guard below is what makes a stale
    list fail loudly instead of passing.

    `TestInternalCLIImportsNoMCPSDK` loads `github.com/seanb4t/codegraph-go/internal/cli` with
    `Mode: NeedName|NeedImports`, `Tests: false` (production files only — SDK-02 is about the
    production import list), asserts exactly one package resolved, and fails naming
    `pkg.PkgPath` and the offending import path if any direct import carries a forbidden prefix.

    `assertMCPSDKImporterExists(t)` performs a whole-module `Tests: true` load and fails loudly if
    no package directly imports any forbidden prefix, mirroring `assertCharmImporterExists`. Call
    it from the main test.

    `mcp_sdk_selftest_test.go` implements the planted-import companion using
    `packages.Config.Overlay`, resolving the overlay key via `packages.Load` rather than by
    constructing the path by hand, and asserting the overlaid package is present in the loaded set
    before trusting the violation.

    **The overlay must insert a syntactically *used* reference, not a bare import.** Go rejects an
    unused import at type-check time, so an overlay that only adds `import "…/mcp-go/server"` fails
    the load for an unrelated reason and the predicate never runs — a self-test that "passes"
    because compilation broke proves nothing. Add both the import and a package-level declaration
    that references the imported package (for example a `var _ = server.ServeStdio`-shaped binding),
    and assert the overlaid package loaded with **zero** type errors before trusting the violation
    result. Apply the same requirement to `internal/mcp/archtest`'s overlay in task 2. Addresses
    review concern: "the planted SDK-import self-test may fail during type checking for unrelated
    reasons."

    Do not implement this as a text search over `serve.go` — a text search misses aliased imports,
    build-tag-gated files, and test variants, and the archtest is the established house style
    (`internal/graphstore/archtest`, `internal/cli/present/archtest`).
  </action>

  <verify>
    <automated>go test ./internal/cli/archtest/... -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/cli/archtest/... -count=1` exits 0 and `-v` shows both `TestInternalCLIImportsNoMCPSDK` and `TestInternalCLIImportsNoMCPSDK_PlantedImportIsError` passing.
    - `internal/cli/archtest/mcp_sdk_confinement_test.go` names both the current and the Phase 2 SDK module prefixes in `forbiddenMCPSDKPrefixes`.
    - The test loads `internal/cli` with `Tests: false` and asserts exactly one package resolved.
    - Behavior: with the SDK import restored by hand into `internal/cli/serve.go`, `go test ./internal/cli/archtest/...` fails naming `github.com/seanb4t/codegraph-go/internal/cli` and the offending import path; the plan summary quotes that failure message and confirms the edit was reverted.
    - `go build ./... && go vet ./...` exit 0.
  </acceptance_criteria>

  <done>`internal/cli`'s direct production imports are permanently free of any MCP SDK package, guarded by a test proven able to fire and unable to pass vacuously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → `serve --mcp` stderr | Client-supplied `clientInfo` and protocol-version strings reach an always-on, human- and machine-parsed diagnostic stream |
| dependency upgrade → wire behavior | A `go.mod` bump can change what the server declares on the wire without any first-party source edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `internal/mcp/session_line.go` — a hostile `clientInfo.name` embedding a newline plus the session-line prefix could synthesize a fabricated second diagnostic line | high | mitigate | `TestSessionLineSanitizesHostileClientInfo` pins nine hostile shapes including the prefix-injection case; `sanitizeClientField` replaces control characters and spaces and truncates on a rune boundary |
| T-03-02 | Tampering | `go.mod` dependency bump silently moving the declared protocol version | high | mitigate | `TestNoExternalProtocolVersionConstantReferences` forbids referencing any external protocol-version constant tree-wide, so the declared value can only change by a reviewed edit to `internal/mcp/protocol_version.go` |
| T-03-03 | Repudiation | A guard that passes vacuously certifies a property nobody verified | high | mitigate | Both archtests carry an Overlay-injected planted-defect companion and a self-defeat guard asserting the thing being guarded still exists; the `testdata/golden` load pattern closes the `./...`-skips-testdata blind spot |
| T-03-04 | Information Disclosure | Truncating a client-supplied field mid-rune could emit an invalid UTF-8 byte into a log stream | low | mitigate | Truncation is rune-boundary-aware and invalid UTF-8 is replaced with U+FFFD; asserted by the 300-byte multi-byte case |
| T-03-SC | Tampering | package-manager installs | high | accept | No package-manager install occurs in this plan; `golang.org/x/tools` is a pre-existing, already-audited `go.mod` entry and `go.mod` is unchanged |
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean.
- `task test:unit && task test:golden && task test:integration && task test:wireoracle` green.
- Both archtests observed RED against a confirmed-applied injected violation before being trusted green,
  with the failure messages quoted in the plan summary.

## Flagged planner assumption (VRFY-02 mechanism)

See plan 01's `<verification>` section for the full statement. In short: `internal/mcp.ProtocolVersion`
is an **asserted compatibility pin**, not the source of the negotiated value —
`mark3labs/mcp-go v0.56.0` decides the revision in the unexported `(*MCPServer).protocolVersion`
(`server/server.go:1196`) and exposes no `WithProtocolVersion` option, so no caller can inject it.
This plan's guard is what makes the pin load-bearing: it forbids reading the SDK's constant
anywhere in the tree, so the declared value can only move by a reviewed edit to
`internal/mcp/protocol_version.go`. REQUIREMENTS.md VRFY-02's "asserted against" is satisfied;
ROADMAP success criterion 3's "reads from" is not satisfiable in Phase 1 and must not be scored as
met by this phase without an explicit developer decision. Neither ROADMAP.md nor REQUIREMENTS.md is
edited by these plans.

## Flagged planner assumption (unresolved edge)

The deterministic edge probe returned one `unclassified` row for **SDK-02** that no category rule
resolved. It is surfaced here rather than silently dropped: the probe could not derive an edge
predicate from SDK-02's text because the requirement is a structural import-graph property with no
input domain to probe (no boundary, no ordering, no empty case). The planner's judgement is that
SDK-02's correctness is fully expressed by `TestInternalCLIImportsNoMCPSDK` plus its planted-import
companion, and that no additional edge predicate exists to author. If a reviewer disagrees, the
missing predicate should be named explicitly rather than assumed absent.
</verification>

<success_criteria>
- The server's declared protocol version reads from a repo-owned literal and CI fails if any
  SDK-owned equivalent is referenced anywhere in the tree, including `_test.go` and `testdata/golden` (VRFY-02).
- `internal/cli` imports no MCP SDK package, enforced permanently and non-vacuously (SDK-02).
- The always-on session line's format is pinned and cannot be broken by client-supplied input (VRFY-03).
</success_criteria>

<output>
Create `.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-03-SUMMARY.md` when done
</output>
