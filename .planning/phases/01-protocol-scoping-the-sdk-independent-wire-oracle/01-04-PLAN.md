---
phase: 01-protocol-scoping-the-sdk-independent-wire-oracle
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/wireoracle/scenarios.go
  - test/wireoracle/anchors.go
  - test/wireoracle/oracle_test.go
  - testdata/wireoracle/transcripts/
autonomous: false
requirements: [VRFY-01, VRFY-04]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The oracle covers all 8 registered MCP tools, all three `tools/list` variants (explore-only default, allowlisted subset, zero-tools-no-index), the four error shapes (unknown method, unknown tool, malformed args, confinement reject) and the one statelessness edge (call before initialize) — captured against the pre-migration `mark3labs`-backed binary (VRFY-01, VRFY-04, D-05)"
    - "Coverage of all 8 tools is asserted structurally, not inherited by prose: a test derives the registered tool names and requires each to appear in some scenario's `tools/call` whose captured result decodes to `isError` false (VRFY-04, D-05)"
    - "This plan adds exactly 16 scenarios, bringing the suite to 17; plan 05 adds 6 for a phase total of 23. Every scenario-count assertion in this milestone is exact equality against `ExpectedScenarioCount`, never a lower bound (D-07, PITFALLS Trap D)"
    - "Expected values are captured-and-frozen for the bulk plus a small hand-authored anchor set that does not derive from the capture — the JSON-RPC error code for an unknown method, the error code for invalid params, the `protocolVersion` literal, and the JSON-RPC framing invariants (D-02)"
    - "`tools/list` returns tools in a deterministic order across repeated calls; two consecutive `tools/list` responses in one session are byte-compared to each other, not merely both non-empty (VRFY-04 ordering edge)"
    - "`tools/list` assertions are exact-set and exact-zero, never non-empty (PITFALLS Trap D, preserved from `TestDefaultToolVisibility`/`TestNoIndexZeroTools`)"
    - "A `tools/call` sent as the very first message with no prior `initialize` is captured as a currently-passing behavior rather than treated as an error, locking in the statelessness shape today's server already tolerates (RESEARCH Pitfall 2)"
    - "Every transcript in this plan was captured while `github.com/mark3labs/mcp-go v0.56.0` was still the pinned MCP dependency (VRFY-04)"
  artifacts:
    - path: "test/wireoracle/scenarios.go"
      provides: "The full D-05 scenario list"
      exports: ["Scenarios", "ScenarioByName", "TranscriptPath"]
    - path: "test/wireoracle/anchors.go"
      provides: "Hand-authored spec anchors that do not derive from any capture"
      exports: ["Anchors", "Anchor"]
    - path: "testdata/wireoracle/transcripts/"
      provides: "Frozen pre-migration transcripts, one per scenario"
  key_links:
    - from: "test/wireoracle/oracle_test.go"
      to: "test/wireoracle/anchors.go"
      via: "each anchor is asserted against the captured bytes independently of the frozen transcript"
      pattern: "Anchors\\(\\)"
    - from: "test/wireoracle/scenarios.go"
      to: "testdata/wireoracle/transcripts/"
      via: "TranscriptPath(scenario.Name) resolves one frozen file per scenario"
      pattern: "TranscriptPath"
  prohibitions:
    - statement: "A frozen transcript must never be regenerated in the same change that could have caused the diff."
    - statement: "A `tools/list` assertion must never be weakened from exact set equality to non-emptiness; a non-empty check certifies nothing about which tools are visible."
---

<objective>
Expand the proven tracer path to the full coverage bar: every one of the 8 registered MCP tools,
all three `tools/list` variants, the four error shapes and the one statelessness edge — captured
against today's unmodified `mark3labs`-backed binary and frozen.

**Counts used throughout this plan, stated once and authoritatively.** This plan adds **16**
scenarios (4 `tools/list` variants + 7 `tools/call` + 4 error shapes + 1 statelessness edge),
bringing the suite from the tracer's 1 to **17**. Plan 05 adds 6 era scenarios for a phase total of
**23**, which is the value of `ExpectedScenarioCount` (declared in plan 07). The error shapes number
**four** — unknown method, unknown tool, malformed args, confinement reject — and
`edge-call-before-initialize` is counted separately as the statelessness edge, never as a fifth
error. If a blocking checkpoint in this plan or plan 05 approves additional scenarios, the
constant is updated in the same commit that adds them and the constant, not this prose, is the
assertion. Addresses review concerns: "`Scenarios()` returns at least 17 scenarios" is a forbidden
lower bound, and "four error/edge shapes" versus "the five error/edge shapes named above" was an
internal contradiction.

Purpose: D-05's bar exists so Phase 2's literal success criterion ("every existing MCP tool
produces byte-identical output") has no interpretation gap. Any scenario not captured now can
never be captured against the pre-migration server — once `mark3labs` leaves `go.mod` the baseline
is gone. Under-scoping here is unrecoverable; over-scoping costs only build time.

Output: 16 additional scenarios with frozen transcripts, plus the hand-authored spec anchors that
go red independently of the captured bulk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-CONTEXT.md
@.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-RESEARCH.md
@.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| type | `test/wireoracle.Anchor` |
| func | `test/wireoracle.Anchors` |
| file | `test/wireoracle/anchors.go` |
| const | `test/wireoracle.codeMethodNotFound` |
| const | `test/wireoracle.codeInvalidParams` |
| test | `TestSpecAnchorsHold` |
| test | `TestToolsListOrderIsDeterministic` |
| test | `TestToolsListExactSets` |
| test | `TestEveryRegisteredToolHasASuccessfulCallScenario` |
| golden | `testdata/wireoracle/transcripts/toolslist-default.golden` |
| golden | `testdata/wireoracle/transcripts/toolslist-allowlist.golden` |
| golden | `testdata/wireoracle/transcripts/toolslist-no-index.golden` |
| golden | `testdata/wireoracle/transcripts/toolslist-repeat.golden` |
| golden | `testdata/wireoracle/transcripts/call-node.golden` |
| golden | `testdata/wireoracle/transcripts/call-search.golden` |
| golden | `testdata/wireoracle/transcripts/call-callers.golden` |
| golden | `testdata/wireoracle/transcripts/call-callees.golden` |
| golden | `testdata/wireoracle/transcripts/call-impact.golden` |
| golden | `testdata/wireoracle/transcripts/call-files.golden` |
| golden | `testdata/wireoracle/transcripts/call-status.golden` |
| golden | `testdata/wireoracle/transcripts/error-unknown-method.golden` |
| golden | `testdata/wireoracle/transcripts/error-unknown-tool.golden` |
| golden | `testdata/wireoracle/transcripts/error-malformed-args.golden` |
| golden | `testdata/wireoracle/transcripts/error-confinement-reject.golden` |
| golden | `testdata/wireoracle/transcripts/edge-call-before-initialize.golden` |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: One-way gate — approve the D-05 pre-migration coverage bar before capture</name>
  <decision>Freeze the pre-migration wire baseline at the D-05 coverage bar — all 8 tools, all three `tools/list` variants, four error shapes and one statelessness edge, 16 scenarios added here for a suite of 17 — before Phase 2 removes `mark3labs/mcp-go` from `go.mod`.</decision>
  <context>
    This is a one-way door. Every transcript captured in this plan records what the CURRENT,
    `mark3labs`-backed server puts on the wire. Once Phase 2 swaps the SDK, that baseline cannot be
    reconstructed — there is no way to re-run a capture against a dependency that is no longer in
    `go.mod`. A scenario omitted here is permanently unavailable as a migration comparison.

    The undo cost is asymmetric and known: under-scoping is unrecoverable, over-scoping costs only
    CI build time (roughly one subprocess spawn per scenario). CONTEXT D-05 already records the
    decision to take the full bar; this gate exists so the door is walked through deliberately, and
    so the developer can add a scenario now rather than discover its absence in Phase 2.
  </context>
  <options>
    <option id="full-bar">
      <name>Capture the full D-05 bar as scoped (recommended — matches CONTEXT D-05)</name>
      <pros>Phase 2's "byte-identical output for every existing MCP tool" criterion has no interpretation gap; every tool, every list variant, every error shape has a real pre-migration baseline</pros>
      <cons>This plan adds 16 scenarios, bringing the suite to 17; plan 05 adds 6 more for a phase total of 23 subprocess-spawning scenarios per CI run of `task test:wireoracle`. The measured wall-clock cost is recorded in task 2 and re-measured at full scope in plan 07</cons>
    </option>
    <option id="full-bar-plus">
      <name>Capture the full bar plus additional scenarios the developer names now</name>
      <pros>Closes any gap the developer can see that this plan did not scope</pros>
      <cons>More CI time; each added scenario needs its own frozen transcript</cons>
    </option>
  </options>
  <resume-signal>Select: full-bar, or full-bar-plus (and name the extra scenarios)</resume-signal>
  <note>
    A third `narrower` option was present in the pre-review draft of this plan and has been
    **deleted**. Its own `<cons>` read "Permanently unrecoverable coverage gap; contradicts CONTEXT
    D-05, which already weighed and rejected this" — a selectable option that the plan itself
    documents as violating a locked decision, on a one-way door, is a hazard even with the warning
    label attached. D-05 is locked; this gate exists to walk through the door deliberately, not to
    reopen the decision about how wide it is. Addresses review concern: "plan 04's checkpoint
    re-litigates locked D-05" (CONFIRMED).
  </note>
</task>

<task type="auto">
  <name>Task 2: Script and freeze the full D-05 scenario set against the pre-migration binary</name>

  <reversibility rating="one-way">These transcripts record what the `mark3labs`-backed server puts
  on the wire; once Phase 2 removes that dependency from `go.mod` the baseline cannot be
  recaptured, so a scenario omitted here is permanently unavailable.</reversibility>

  <precondition>`github.com/mark3labs/mcp-go v0.56.0` is still the pinned MCP dependency in `go.mod` — the capture must happen pre-migration (VRFY-04).</precondition>

  <files>test/wireoracle/scenarios.go, testdata/wireoracle/transcripts/</files>

  <read_first>
    - `test/wireoracle/scenarios.go` — the tracer's single-scenario shape, the `Scenario` struct fields, and `TranscriptPath`
    - `test/wireoracle/capture.go` — how `Env`, `Index`, `Requests` and `ExpectTools` are consumed
    - `internal/mcp/server.go` — `companionNames` (the fixed 7-name vocabulary) and `ParseAllowlist`, which the `CODEGRAPH_MCP_TOOLS` scenarios drive
    - `internal/mcp/tools.go` — all 8 tool names and each tool's argument schema, so each `tools/call` sends arguments the handler actually accepts
    - `internal/mcp/server_test.go:118-170` — `TestDefaultToolVisibility` / `TestAllowlist` / `TestNoIndexZeroTools`, the exact-set and exact-zero assertion discipline these scenarios must preserve
    - `testdata/wireoracle/fixture/` — the frozen fixture the scenarios query; symbol names in the scenario arguments must exist in it
  </read_first>

  <action>
    Extend `Scenarios()` with the following, keeping the tracer's `handshake-explore` unchanged and
    its frozen transcript untouched. Every scenario begins with the same `initialize` frame the
    tracer uses (same hand-authored `2025-11-25` literal, same `codegraph-wire-oracle/0.0.0`
    client identity) EXCEPT `edge-call-before-initialize`.

    **`tools/list` variants (D-05's three, plus a determinism probe):**
    - `toolslist-default` — `Index: true`, no `CODEGRAPH_MCP_TOOLS`; expect the explore-only default.
    - `toolslist-allowlist` — `Index: true`, `Env: ["CODEGRAPH_MCP_TOOLS=node,status"]`; expect explore plus exactly those two.
    - `toolslist-no-index` — `Index: false` (fixture copied but never indexed), `Env: ["CODEGRAPH_MCP_TOOLS=node,status"]`; expect zero tools, and note in a comment that `initialize` still succeeds.
    - `toolslist-repeat` — `Index: true`, all seven companions allowlisted, two consecutive `tools/list` requests (ids 2 and 3) in one session. This is the deterministic-ordering probe the `2026-07-28` changelog's minor change #3 makes relevant; it confirms rather than assumes the order is stable.

    **One `tools/call` per remaining tool**, each with `Index: true` and
    `Env: ["CODEGRAPH_MCP_TOOLS=node,search,callers,callees,impact,files,status"]`:
    `call-node`, `call-search`, `call-callers`, `call-callees`, `call-impact`, `call-files`,
    `call-status`. Give each call arguments that resolve against the frozen fixture — read
    `internal/mcp/tools.go` for each tool's parameter names and read the fixture for real symbol
    and file names, rather than inventing identifiers that would make every one of these scenarios
    freeze an error result.

    The eighth tool, `codegraph_explore`, is covered by the tracer's `handshake-explore` scenario —
    but **that coverage is verified here, not inherited**. Plan 01's revised scenario supplies the
    schema-required `query` argument and asserts `isError` false; task 3 below adds
    `TestEveryRegisteredToolHasASuccessfulCallScenario`, which derives the registered tool names and
    fails if any of the 8 lacks a scenario whose captured `tools/call` result decodes to `isError`
    false. Before capturing anything in this task, confirm `handshake-explore.golden` on disk
    carries a successful explore result; if it does not, stop and repair plan 01's scenario first —
    once `mark3labs` leaves `go.mod` in Phase 2 that baseline can never be recaptured. Addresses
    review concern: "plan 04 counts the malformed plan 01 explore call as coverage" (CONFIRMED, and
    the one-way half is the serious half).

    **The four error shapes, plus the one statelessness edge (counted separately, not as a fifth error):**
    - `error-unknown-method` — a request with `method` set to a method the server does not implement.
    - `error-unknown-tool` — `tools/call` naming a tool that is not registered.
    - `error-malformed-args` — `tools/call` on a registered tool with an argument of the wrong JSON type.
    - `error-confinement-reject` — `tools/call codegraph_node` with an explicit `path` argument resolving outside the index root, exercising the `confineToRepoRoot` trust boundary. **The `path` value must be a stable, portable literal, not a host-specific temp sibling.** `confineToRepoRoot` embeds the rejected path verbatim in its error via `fmt.Errorf("mcp: path %q is outside the server's configured repo root", path)` (`internal/mcp/tools.go:42`), so whatever is sent lands in the frozen bytes. Use the absolute literal `/codegraph-wire-oracle-outside-root` — a path that is identical on every machine, cannot exist, and is not reachable by the repo-root substitution because it shares no prefix with the fixture directory. Do NOT use `filepath.Join(t.TempDir(), "..", "elsewhere")` or any sibling of the capture directory: that would freeze a host-specific absolute path into a checked-in transcript. If a future scenario genuinely needs a host path in a rejected-path position, it gets its own named-field normalization rule satisfying D-04, never a value-wide substitution. Addresses review concern: "the outside-path scenario can leak an unnormalized host path."
    - `edge-call-before-initialize` — a `tools/call` sent as the very first message with NO prior `initialize`. Today's server tolerates this because it never gates `tools/list`/`tools/call` on session initialization; capture that as a currently-passing behavior and record in a comment that it is an accidental but real asset for Phase 3's statelessness work. Do not add server code for this — only the scenario. This is the statelessness edge, not an error shape; the error-shape count is four.

    Set each scenario's `ExpectTools` to the count the session-line assertion should see, and set
    it to zero for `toolslist-no-index`. For `edge-call-before-initialize`, mark the scenario as
    not expecting a session line at all (no `initialize` means the hook never fires) — add a
    boolean field to `Scenario` for this rather than special-casing the scenario name in the test.

    Then freeze: build the binary once and, for each new scenario, run the
    `test/wireoracle/cmd/wireoracle` CLI redirecting its stdout into
    `testdata/wireoracle/transcripts/<name>.golden`. Do not add any flag, environment variable, or
    test hook that writes these files from inside `go test` (D-03).

    Before trusting any frozen file, read it. A transcript whose tool result is an unexpected error
    means the scenario's arguments are wrong, not that the server is broken — fix the scenario and
    recapture rather than freezing a mistake. Record in the plan summary, per scenario, one line
    stating what the captured result was (success or a named error code).
  </action>

  <verify>
    <automated>go test ./test/wireoracle/... -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./test/wireoracle/... -count=1` exits 0.
    - `testdata/wireoracle/transcripts/` contains one non-empty `.golden` file per scenario returned by `Scenarios()`, and no orphan `.golden` file without a matching scenario.
    - `len(Scenarios())` equals exactly **17** after this task (the tracer's 1 plus this plan's 16), covering: all 8 tool names from `internal/mcp/tools.go`, the three `tools/list` variants, the repeated-`tools/list` determinism probe, the four error shapes, and the one statelessness edge. Exact equality — never "at least". If the blocking checkpoint above selected `full-bar-plus`, the number is 17 plus the count of scenarios the developer named, recorded in the plan summary and carried into `ExpectedScenarioCount`.
    - No frozen transcript contains an absolute path beginning with the host temp directory prefix (`$TMPDIR`, `/tmp`, `/var/folders`, `/private/var/folders`), and `error-confinement-reject.golden` contains the portable literal `/codegraph-wire-oracle-outside-root` rather than any host-specific path. The presence of the `<REPO>` placeholder is asserted per-rule by the ledger, not per-file — see plan 01's `ExpectFires` mechanism and plan 07's `TestEveryDeclaredFiringRuleActuallyFires`.
    - The measured wall-clock time of `task test:wireoracle` at 17 scenarios is recorded in the plan summary, alongside plan 01's 1-scenario baseline, so the per-scenario marginal cost of the required PR leg is a measured number.
    - `toolslist-no-index.golden` records a successful `initialize` response and a `tools/list` response with an empty tool array — not an error.
    - `edge-call-before-initialize.golden` records a response to a `tools/call` sent with no prior `initialize`.
    - Behavior: re-running the capture CLI for any scenario produces stdout byte-identical to that scenario's frozen file.
    - The plan summary lists every scenario with its captured outcome (success or named error code).
  </acceptance_criteria>

  <done>Every one of the 8 MCP tools, all three `tools/list` variants, the four error shapes and the one statelessness edge have a frozen pre-migration transcript, byte-compared green by `go test ./test/wireoracle/...`, with the suite at exactly 17 scenarios and no host-specific path in any frozen file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hand-authored spec anchors and exact-set list assertions</name>

  <files>test/wireoracle/anchors.go, test/wireoracle/oracle_test.go</files>

  <read_first>
    - `test/wireoracle/oracle_test.go` — the tracer's comparison test and its existing spec anchor for `protocolVersion`
    - `test/wireoracle/scenarios.go` — the scenario names the anchors bind to
    - `internal/mcp/server_test.go:118-170` — the exact-set / exact-zero discipline (`equalStrings(got, want)`, `len(got) != 0`) these assertions must mirror
    - `.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-RESEARCH.md` § "Code Examples" — the two verified JSON-RPC error code values and the explicit instruction NOT to hand-author a code for the unsupported-version scenario
  </read_first>

  <behavior>
    - `TestSpecAnchorsHold`: for every anchor in `Anchors()`, the named scenario's captured stdout satisfies the anchor independently of the frozen transcript. Each anchor is asserted by decoding only the one field it names out of the raw line — never by unmarshalling into an SDK type.
      - `error-unknown-method`: the response's `error.code` equals the JSON-RPC method-not-found code, -32601.
      - `error-malformed-args`: the response's `error.code` equals the invalid-params code, -32602.
      - `handshake-explore`: the `initialize` result's `protocolVersion` equals `internal/mcp.ProtocolVersion`.
      - Framing invariant, applied to every scenario: every stdout line parses as a JSON object carrying a `jsonrpc` field equal to `"2.0"`, and every request id sent has exactly one response line with that id.
    - `TestToolsListOrderIsDeterministic`: in `toolslist-repeat`, the two `tools/list` response objects' `result.tools` arrays are byte-identical to each other after normalizing only their `id` fields. Not "both non-empty" — byte equality.
    - `TestToolsListExactSets`: `toolslist-default` advertises exactly `["codegraph_explore"]`; `toolslist-allowlist` advertises exactly `["codegraph_explore","codegraph_node","codegraph_status"]` as a set; `toolslist-no-index` advertises exactly zero. Exact set and exact zero — never a length-greater-than check.
    - `TestEveryRegisteredToolHasASuccessfulCallScenario`: derive the full registered tool-name set from the `toolslist-allowlist`-style capture with every companion allowlisted (explore plus the seven `companionNames` at `internal/mcp/server.go:32`), then require that for **each** of those 8 names there exists a scenario whose request list contains a `tools/call` naming it AND whose captured response for that call decodes to `result.isError` false. Failure names the specific tool with no successful scenario. This is the structural replacement for the prose delegation "`handshake-explore` already covers `codegraph_explore`" — the eighth tool's coverage is now asserted, and a scenario that silently degrades into an error result cannot pass as coverage.
    - A deliberately corrupted copy of any anchor's expected value makes `TestSpecAnchorsHold` fail naming the anchor and quoting the observed value.
  </behavior>

  <action>
    Add `test/wireoracle/anchors.go` declaring
    `type Anchor struct { Scenario string; Name string; Assert func(t *testing.T, stdout []byte) }`
    — or an equivalent data-plus-function shape — and `func Anchors() []Anchor`. Declare the two
    JSON-RPC codes as package constants named for what they mean, with a comment citing that both
    are spec-pinned values, not properties of the current SDK. These are D-02's structural point:
    if the SDK swap changes an accident, the transcript diff shows it; if it changes something the
    spec pins, the anchor goes red on its own.

    Do NOT hand-author an error code for the unsupported-protocol-version scenario. Today's server
    silently coerces an unrecognized client version to its own latest rather than returning an
    error, so an anchor asserting an error there would be asserting a behavior that never fires.
    That scenario is captured-and-frozen in plan 05 instead. Put that reasoning in a comment beside
    the anchor list so a future reader does not "fix" the omission.

    Extend `oracle_test.go` with `TestSpecAnchorsHold`, `TestToolsListOrderIsDeterministic` and
    `TestToolsListExactSets` per the behavior block, reusing the tracer's capture path so anchors
    run against freshly captured bytes rather than against the frozen file — an anchor read from
    the golden would be circular.

    For the exact-set assertions, extract the advertised tool names by decoding only
    `result.tools[].name` out of the relevant response line into a `[]string`, sort, and compare
    with a full set equality against a literal expected slice. Mirror `server_test.go`'s
    `equalStrings(got, want)` / `len(got) != 0` shapes exactly — PITFALLS Trap D names this repo's
    existing exact-set discipline as a positive pattern to preserve through the migration's test
    churn, and a relaxed assertion here is the quiet way it gets lost.

    Prove non-vacuity before trusting green: temporarily change one anchor's expected value,
    confirm `TestSpecAnchorsHold` fails naming that anchor, then revert. Confirm the edit landed
    before trusting the red — a mutation that no-ops proves nothing. Quote the failure message in
    the plan summary.
  </action>

  <verify>
    <automated>go test ./test/wireoracle/... -run 'TestSpecAnchorsHold|TestToolsListOrderIsDeterministic|TestToolsListExactSets|TestFrozenTranscriptsMatch' -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./test/wireoracle/... -count=1` exits 0 and `-v` shows `TestSpecAnchorsHold`, `TestToolsListOrderIsDeterministic`, `TestToolsListExactSets`, `TestEveryRegisteredToolHasASuccessfulCallScenario` and `TestFrozenTranscriptsMatch` all passing.
    - `TestEveryRegisteredToolHasASuccessfulCallScenario` names all 8 tools in its failure path, and was observed red by temporarily pointing one `tools/call` at a nonexistent tool name; the plan summary quotes that failure message and confirms the edit was reverted.
    - `test/wireoracle/anchors.go` declares the method-not-found and invalid-params codes as named constants with the values -32601 and -32602.
    - `test/wireoracle/anchors.go` contains a comment explaining why no error-code anchor exists for the unsupported-protocol-version scenario.
    - `TestToolsListExactSets` compares against literal expected slices with full set equality; no assertion in the file tests only that a tool list is non-empty.
    - Behavior: with one anchor's expected value altered, `go test ./test/wireoracle/...` fails naming that anchor and quoting the observed value; the plan summary quotes that message and confirms the edit was reverted.
    - The output of `go list -deps -f '{{.ImportPath}}' github.com/seanb4t/codegraph-go/test/wireoracle` contains no line under the MCP SDK's module path — the oracle still never uses the SDK under test as its own decoder (VRFY-01).
  </acceptance_criteria>

  <done>Spec-pinned values are asserted by hand-authored anchors that fail independently of the frozen transcripts, and every `tools/list` assertion is exact-set or exact-zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| binary-under-test → wire oracle | Captured stdout/stderr bytes are untrusted input to the harness |
| scenario arguments → `confineToRepoRoot` | The `error-confinement-reject` scenario deliberately drives the existing client-supplied-path trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | `test/wireoracle` — 17 subprocess spawns per suite run after this plan (23 after plan 05), any of which can wedge | medium | mitigate | Every scenario is bounded by `Capture`'s single 30-second `context.WithTimeout`, kill-on-error and unconditional `Wait` (plan 01 Layer 6); the suite fails loudly on timeout rather than hanging CI, and the measured wall-clock cost is recorded here and again at full scope in plan 07 |
| T-04-02 | Information Disclosure | frozen transcripts checked into the repository | medium | mitigate | Acceptance criteria assert no absolute host path survives normalization; `error-confinement-reject` sends the portable literal `/codegraph-wire-oracle-outside-root` so `confineToRepoRoot`'s `%q`-quoted echo of the rejected path (`internal/mcp/tools.go:42`) cannot embed a host-specific path in a checked-in file; the dedicated fixture contains only purpose-written source, never real user code |
| T-04-03 | Elevation of Privilege | `error-confinement-reject` — a client-supplied `path` escaping the index root | high | mitigate | The scenario asserts the existing `confineToRepoRoot` rejection is what lands on the wire, and freezes it, so a migration that silently loosens confinement produces a transcript diff |
| T-04-04 | Repudiation | An anchor that cannot fail certifies nothing | high | mitigate | Task 3 requires the anchor set be demonstrated red against a confirmed-applied altered expected value before being trusted, with the failure message quoted in the summary |
| T-04-SC | Tampering | package-manager installs | high | accept | No package-manager install occurs in this plan; `go.mod` is deliberately unchanged this phase |
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean.
- `task test:wireoracle` green with all 17 scenarios' transcripts frozen, and its measured wall-clock
  time recorded alongside plan 01's 1-scenario baseline.
- Every frozen transcript read by a human before being trusted, with its captured outcome (success
  or named error code) recorded per scenario in the plan summary.
- The anchor set observed red against a confirmed-applied altered expected value, then reverted.
- `TestEveryRegisteredToolHasASuccessfulCallScenario` observed red against a confirmed-applied
  broken tool name, then reverted.
</verification>

<success_criteria>
- All 8 MCP tools, three `tools/list` variants, four error shapes and one statelessness edge have
  frozen pre-migration transcripts — 17 scenarios exactly (VRFY-01, VRFY-04, D-05).
- All-8-tool coverage is asserted structurally by
  `TestEveryRegisteredToolHasASuccessfulCallScenario`, including a successful `codegraph_explore`
  result, rather than delegated to another plan by prose.
- Hand-authored spec anchors fail independently of the captured bulk (D-02).
- `tools/list` ordering is confirmed deterministic by byte comparison, and every list assertion is
  exact-set or exact-zero.
</success_criteria>

<output>
Create `.planning/phases/01-protocol-scoping-the-sdk-independent-wire-oracle/01-04-SUMMARY.md` when done
</output>
