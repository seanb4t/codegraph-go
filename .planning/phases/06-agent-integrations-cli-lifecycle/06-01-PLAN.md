---
phase: 06-agent-integrations-cli-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - internal/agents/types.go
  - internal/agents/shared.go
  - internal/agents/registry.go
  - internal/agents/instructions.go
  - internal/agents/shared_test.go
  - internal/agents/registry_test.go
  - internal/agents/testhelpers_test.go
autonomous: true
requirements: [AGNT-01, AGNT-02]
must_haves:
  truths:
    - "A marker-fenced section can be inserted, replaced in place idempotently, and removed, returning a file to its pre-insert bytes"
    - "A codegraph MCP entry can be set into an existing JSON config while every unrelated key is preserved byte-for-byte"
    - "Re-running the same insert produces identical bytes (no-op on repeat)"
    - "Every AgentTarget can be looked up by stable ID and listed in deterministic sorted order"
  prohibitions:
    - "shared.go must NOT panic on malformed/unparseable existing config (empty-map fallback, error surfaced cleanly)"
    - "internal/agents must NOT import internal/graphstore, internal/indexer, or internal/query (install writes config files; it never touches the graph)"
    - "The marker text must NOT deviate from the exact TS strings <!-- CODEGRAPH_START --> / <!-- CODEGRAPH_END --> (D-01a cross-implementation contract)"
  artifacts:
    - path: internal/agents/types.go
      provides: "AgentTarget interface + Location/TargetID/DetectionResult/FileResult/WriteResult/InstallOptions value types (D-02)"
      contains: "type AgentTarget interface"
    - path: internal/agents/shared.go
      provides: "readJSONFile/writeJSONFile/jsonDeepEqual/writeMcpEntry/replaceOrAppendMarkedSection/removeMarkedSection/upsertInstructionsEntry/atomicWriteFile (D-07)"
    - path: internal/agents/registry.go
      provides: "registerTarget/GetTarget/AllTargetIDs/AllTargets/ResolveTargetFlag/DetectAll (D-02, D-03)"
    - path: internal/agents/instructions.go
      provides: "codegraphSectionStart/codegraphSectionEnd markers + short codegraphInstructionsBlock const (D-01a)"
  key_links:
    - from: internal/agents/registry.go
      to: internal/agents/types.go
      via: "registry map keyed by TargetID storing AgentTarget implementations"
      pattern: "map\\[TargetID\\]AgentTarget"
    - from: internal/agents/shared.go
      to: internal/agents/instructions.go
      via: "upsertInstructionsEntry writes codegraphInstructionsBlock fenced by the section markers"
      pattern: "codegraphSectionStart"
---

<objective>
Establish the `internal/agents` package foundation: the `AgentTarget` interface and value types (D-02), the surgical format-preserving write helpers every agent shares (D-07), the ID-keyed registry (D-02, D-03), and the short marker-fenced instructions block (D-01a). Pin the one agents-format external dependency (`github.com/tailscale/hujson`) up front, unimported, per the project's established pin-then-import convention.

This is the contract-defining plan: every per-agent target file (06-02, 06-03) and the install/uninstall commands (06-04) build against the interface and helpers created here. Parity is behavioral, measured structurally against the TS installer source (D-01) — there is no golden-output corpus because install writes external config files, not codegraph output.

Purpose: Give downstream plans a stable interface + tested surgical-write primitives so no per-agent file re-derives JSON/marker read-modify-write logic.
Output: `internal/agents` package skeleton (types, shared helpers, registry, instructions) with RED→GREEN tested round-trip/idempotency invariants; `hujson` pinned in go.mod.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md
@.planning/phases/06-agent-integrations-cli-lifecycle/06-PATTERNS.md

<interfaces>
<!-- The AgentTarget contract this plan CREATES (source: 06-RESEARCH.md Pattern 1, a mechanical port of TS src/installer/targets/types.ts). Downstream plans 06-02/06-03/06-04 implement/consume this — they must not re-derive it. -->

Value types (internal/agents/types.go):
- Location (string): LocationGlobal="global", LocationLocal="local"
- TargetID (string): Claude, Cursor, Codex, Opencode, Hermes, Gemini, Antigravity, Kiro
- DetectionResult{ Installed bool; AlreadyConfigured bool; ConfigPath string }
- FileAction (string): ActionCreated, ActionUpdated, ActionUnchanged, ActionRemoved, ActionNotFound, ActionKept
- FileResult{ Path string; Action FileAction }
- WriteResult{ Files []FileResult; Notes []string }
- InstallOptions{ AutoAllow bool; ExecPath string } // ExecPath = os.Executable() resolved by the CLI (D-04); AutoAllow drives Claude-only permission writes

AgentTarget interface (internal/agents/types.go):
- ID() TargetID
- DisplayName() string
- SupportsLocation(loc Location) bool
- Detect(loc Location) DetectionResult
- Install(loc Location, opts InstallOptions) WriteResult
- Uninstall(loc Location) WriteResult
- DescribePaths(loc Location) []string

Analog to mirror mechanically: internal/indexer/languages.go (registry map keyed by stable ID + per-variant init() self-registration + sorted list-all export) and internal/indexer/languages.go's ProjectDescriptor+LanguageSpec doc-comment convention (every exported symbol cites the D-NN decision that motivates it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin hujson + scaffold package types.go + test harness</name>
  <files>go.mod, internal/agents/types.go, internal/agents/testhelpers_test.go</files>
  <read_first>
    - go.mod (the require block + STATE.md's "manually promoted ... instead of go mod tidy" convention)
    - internal/indexer/languages.go (lines 10-60: ProjectDescriptor/LanguageSpec doc-comment-cites-D-NN convention to mirror)
    - internal/cli/cli_test.go (the t.TempDir() + t.Setenv fixture-isolation idiom used across the project)
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (Pattern 1 code example for the exact type set)
  </read_first>
  <action>
    Add `github.com/tailscale/hujson v0.0.0-20260302212456-ecc657c15afd` to go.mod's direct require block by hand (pinned to the exact pseudo-version RESEARCH verified against proxy.golang.org). Do NOT run `go mod tidy` — it strips the still-unimported require; this matches the project's every-phase pin-then-import convention (STATE.md). Run `go mod download github.com/tailscale/hujson` to populate go.sum. The module stays unimported until 06-03 lands opencode.go.

    Create internal/agents/types.go with the package doc comment (cite D-02: install/uninstall logic lives here, never in internal/cli) and the value types + AgentTarget interface exactly as listed in the context <interfaces> block. Give every exported type/method a one-sentence doc comment citing the decision it serves (D-02 interface shape, D-03 location support, D-04 ExecPath, D-07 surgical writes, D-08 uninstall status). Add InstallOptions.ExecPath (the absolute os.Executable() path the CLI resolves once and passes down, D-04) and AutoAllow (Claude-only permissions, D-05).

    Create internal/agents/testhelpers_test.go: a `fakeHome(t)` helper that calls t.Setenv("HOME", t.TempDir()) (plus t.Setenv for XDG_CONFIG_HOME and HERMES_HOME set to subdirs of the same temp home) and returns the temp home path, so every agent's global-scope test runs against an isolated fake home — mirrors TS's per-test os.homedir() mock. Add a `readFile(t, path)`/`writeFile(t, path, content)` convenience pair.
  </action>
  <verify>
    <automated>go build ./internal/agents/... && go vet ./internal/agents/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/agents/...` exits 0 (package compiles with the interface + value types)
    - go.mod contains a direct require line for github.com/tailscale/hujson at the pinned pseudo-version; go.sum has its hash
    - internal/agents/types.go declares `type AgentTarget interface` with the 7 methods listed in the context block
    - `grep -rn "internal/graphstore\|internal/indexer\|internal/query" internal/agents/` returns no matches (boundary discipline)
  </acceptance_criteria>
  <done>Package compiles; hujson pinned unimported; AgentTarget interface + value types + isolated-fake-home test harness exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shared.go surgical write helpers (RED→GREEN)</name>
  <files>internal/agents/shared.go, internal/agents/shared_test.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (Pattern 2 writeMcpEntry, Pattern 3 + Code Examples replaceOrAppendMarkedSection/removeMarkedSection — the mechanical ports of TS shared.ts)
    - internal/cli/init.go (writeGitignoreHint: the project's "small scoped os.WriteFile under a known-safe path, safety justified in the doc comment" idiom to carry over)
    - internal/agents/types.go (FileResult/FileAction/WriteResult created in Task 1)
  </read_first>
  <behavior>
    - replaceOrAppendMarkedSection on a missing file → creates it with the body, returns ActionCreated
    - replaceOrAppendMarkedSection when the exact block already present → returns ActionUnchanged, bytes identical (idempotent)
    - replaceOrAppendMarkedSection when a different block is present → replaces only the marked span, content outside markers preserved verbatim
    - replaceOrAppendMarkedSection when no markers present → appends after existing content with a separating blank line, existing content preserved
    - removeMarkedSection removes only the marked span (and its surrounding separator) → file returns to its pre-insert bytes; on a file with no markers → ActionNotFound/ActionKept, bytes unchanged
    - writeMcpEntry sets mcpServers.codegraph into an existing JSON object → every sibling key preserved; re-run with the same entry → ActionUnchanged; a changed entry → ActionUpdated
    - readJSONFile on a missing/empty/unparseable file → returns an empty map and no panic (defensive parse, V5)
    - Round-trip: writeMcpEntry then delete-the-entry returns mcpServers to its prior state; if that empties mcpServers, the empty object is removed (D-07 keep-clean)
  </behavior>
  <action>
    Write shared_test.go FIRST (RED, commit `test(06-01): ...`) covering every case in the behavior block, using the fakeHome/readFile helpers from Task 1 and a t.TempDir() scratch file. Then implement shared.go (GREEN, commit `feat(06-01): ...`): readJSONFile (empty-map fallback on missing/unparseable, never panics), writeJSONFile (marshal with 2-space indent + trailing newline via atomicWriteFile), jsonDeepEqual (order-independent deep compare of the before/after codegraph entry), writeMcpEntry (the Pattern 2 read→compare→write-only-if-different returning created/updated/unchanged), replaceOrAppendMarkedSection + removeMarkedSection (the Code Examples marker upsert/strip, using the markers from instructions.go — created in Task 3, so keep the marker args as parameters here rather than importing the const, or land Task 3's const first), upsertInstructionsEntry (thin wrapper binding the codegraph block + markers to replaceOrAppendMarkedSection), and atomicWriteFile (temp-in-same-dir + os.Rename, V12 — every write in this package goes through it; doc-comment the safety justification since these helpers write arbitrary external tool configs). REFACTOR only if an obvious extraction appears; keep tests green.
  </action>
  <verify>
    <automated>go test ./internal/agents/ -run 'TestShared|TestMarker|TestMcpEntry|TestReadJSON|TestRoundTrip' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -run 'TestShared|TestMarker|TestMcpEntry|TestReadJSON|TestRoundTrip' -race` exits 0
    - git log shows a `test(06-01):` commit preceding the `feat(06-01):` commit for shared.go (RED before GREEN)
    - A test asserts writeMcpEntry preserves a pre-existing sibling MCP server key
    - A test asserts readJSONFile on malformed JSON returns an empty map without panicking
    - A test asserts insert→remove returns the file to its exact pre-insert bytes
  </acceptance_criteria>
  <done>All surgical-write helpers pass the behavior-block tests under -race; RED preceded GREEN; no panic on malformed input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: registry.go + instructions.go (RED→GREEN)</name>
  <files>internal/agents/registry.go, internal/agents/instructions.go, internal/agents/registry_test.go</files>
  <read_first>
    - internal/indexer/languages.go (lines 62-110: registry map + registerLanguage + RegisteredLanguageIDs sorted export — the exact structural template)
    - .claude/CLAUDE.md (the "## CodeGraph" section — the shape/tone of the short pointer block install injects: reach for codegraph_explore / `codegraph explore` before grep, skip if no .codegraph/ exists)
    - internal/agents/types.go (TargetID/AgentTarget from Task 1)
  </read_first>
  <behavior>
    - registerTarget(t) then GetTarget(t.ID()) returns t, true; GetTarget of an unknown id returns _, false
    - AllTargetIDs() returns every registered id in ascending sorted order (deterministic — map iteration order is non-deterministic, so the sort is load-bearing for --target all and the multi-select display)
    - ResolveTargetFlag("all") returns all registered targets; ResolveTargetFlag("none") returns empty; ResolveTargetFlag("claude,cursor") returns exactly those two; an unknown id in the csv surfaces an error
    - ResolveTargetFlag("auto") returns only targets whose Detect(loc).Installed is true; when auto detects zero installed targets it falls back to just the Claude target (RESEARCH least-surprise recommendation)
    - codegraphInstructionsBlock is a single const that begins with codegraphSectionStart and ends with codegraphSectionEnd, is non-empty between them, and references the codegraph_explore tool
  </behavior>
  <action>
    Write registry_test.go + instructions.go's test cases FIRST (RED). Register two tiny fake AgentTarget stubs in the test to exercise the registry without depending on real agents. Implement instructions.go (GREEN): the two marker consts (exact TS strings — hard parity contract D-01a, do not invent new text) and codegraphInstructionsBlock as a single backtick const holding the SHORT pointer block (points at the codegraph_explore MCP tool and the `codegraph explore` CLI, notes to skip when no .codegraph/ exists — do NOT reproduce the old full playbook; MCP initialize is the tool-guidance source of truth). Implement registry.go: `var registry = map[TargetID]AgentTarget{}`, registerTarget, GetTarget, AllTargetIDs (sorted like RegisteredLanguageIDs), AllTargets (sorted), DetectAll(loc), and ResolveTargetFlag(spec string, loc Location) ([]AgentTarget, error) implementing auto|all|none|csv with the auto→Claude-fallback (D-03 + RESEARCH). Each real per-agent file (06-02/06-03) will self-register via its own init(); this file only provides the machinery.
  </action>
  <verify>
    <automated>go test ./internal/agents/ -run 'TestRegistry|TestResolveTargetFlag|TestInstructionsBlock' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -run 'TestRegistry|TestResolveTargetFlag|TestInstructionsBlock'` exits 0
    - A test asserts AllTargetIDs() is returned in sorted order
    - A test asserts ResolveTargetFlag("auto") with zero detected targets falls back to the Claude target
    - A test asserts codegraphInstructionsBlock contains both marker strings and the codegraph_explore reference
    - git log shows `test(06-01):` before `feat(06-01):` for the registry
  </acceptance_criteria>
  <done>Registry lookup/list/resolve behaviors pass; short instructions block const exists with exact marker fences; RED preceded GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codegraph → external agent config files | Every helper reads/writes files the tool does not own (user-editable JSON/Markdown); untrusted-in-the-sense-of-user-edited input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / DoS | readJSONFile on malformed existing config | medium | mitigate | Defensive parse: empty-map fallback on missing/unparseable, never panic, surface errors to the caller (V5). Test in Task 2 asserts no panic on malformed JSON. |
| T-06-02 | Tampering | replaceOrAppendMarkedSection clobbering user edits outside the marked span | high | mitigate | Surgical span-only replace/remove; insert→remove round-trip byte-invariant test (Task 2) proves content outside markers is preserved verbatim. |
| T-06-03 | Tampering / DoS | partial write on crash mid-write corrupts a user config | medium | mitigate | atomicWriteFile (temp-in-same-dir + os.Rename) is the sole write path for the package (V12). |
| T-06-SC | Tampering | go.mod dependency addition (hujson) | low | accept | RESEARCH Package Legitimacy Audit verified existence + pinned version against proxy.golang.org and confirmed Tailscale-org (High) reputation; verdict OK/Approved. Go modules are outside the npm/pip/cargo legitimacy gate; pinned to an exact pseudo-version (no floating @latest). |
</threat_model>

<verification>
- `go build ./internal/agents/...` compiles
- `go test ./internal/agents/... -race` green
- `grep -rn "internal/graphstore\|internal/indexer\|internal/query" internal/agents/` returns nothing (boundary discipline)
- go.mod has hujson pinned; go.sum populated
</verification>

<success_criteria>
- AgentTarget interface + value types compile and are documented with D-NN citations
- Surgical-write helpers pass round-trip + idempotency + preserve-siblings + no-panic tests under -race
- Registry lookup/sorted-list/resolve-flag (incl. auto→Claude fallback) pass
- Short instructions const carries the exact TS marker fences
- hujson pinned unimported per project convention
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-01)

New package `internal/agents` with:
- Interface: `AgentTarget` (methods: ID, DisplayName, SupportsLocation, Detect, Install, Uninstall, DescribePaths)
- Value types: `Location` (LocationGlobal/LocationLocal), `TargetID` (Claude/Cursor/Codex/Opencode/Hermes/Gemini/Antigravity/Kiro), `DetectionResult`, `FileAction` (ActionCreated/ActionUpdated/ActionUnchanged/ActionRemoved/ActionNotFound/ActionKept), `FileResult`, `WriteResult`, `InstallOptions` (AutoAllow, ExecPath)
- Shared functions: `readJSONFile`, `writeJSONFile`, `jsonDeepEqual`, `writeMcpEntry`, `replaceOrAppendMarkedSection`, `removeMarkedSection`, `upsertInstructionsEntry`, `atomicWriteFile`
- Registry functions: `registerTarget`, `GetTarget`, `AllTargetIDs`, `AllTargets`, `DetectAll`, `ResolveTargetFlag`
- Consts: `codegraphSectionStart`, `codegraphSectionEnd`, `codegraphInstructionsBlock`
- New go.mod direct dependency: `github.com/tailscale/hujson` (pinned, unimported until 06-03)
</artifacts_produced>

<output>
Create `.planning/phases/06-agent-integrations-cli-lifecycle/06-01-SUMMARY.md` when done.
</output>
