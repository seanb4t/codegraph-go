---
phase: 03-watcher-on-mcp-default
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - test/integration/watch_default_test.go
autonomous: true
requirements: [TEST-04, WATCH-01, WATCH-02]
must_haves:
  truths:
    - "A spawned default-on `serve --mcp` (NO watch flag in argv) completes initialize → tools/list promptly — the watcher never delays the handshake (D-21, WATCH-01/WATCH-02 end-to-end)"
    - "CODEGRAPH_NO_WATCH=1 in the child env disables the watcher, observable via the verbatim disabled stderr message (D-21, WATCH-03 end-to-end)"
    - "These cases ride the SAME harness substrate (binary build + helpers) as 03-04 — no new dependency, no second TestMain"
  artifacts:
    - test/integration/watch_default_test.go
  key_links:
    - "Reuses main_test.go's binPath + runBinary + fixtures from 03-04"
    - "Observes the child's stderr via mcp-go client.GetStderr (existing dep) for the verbatim disabled message"
    - "Confirms the 03-03 default flip and 03-01 verbatim message reach production over real argv/stdio"
---

<objective>
Add TEST-04's WATCH-coverage cases to the subprocess harness (D-21): prove on the REAL spawned binary that (a) bare `serve --mcp` watches by default and still completes the handshake promptly, and (b) `CODEGRAPH_NO_WATCH=1` in the child environment disables the watcher, observed via the verbatim disabled stderr message.

These ride the harness substrate built in 03-04 (the once-built binary + git/glyph/exec helpers) — a separate, lighter plan so the first-time mcp-go stdio wiring risk was isolated in 03-04's anchor case, and this plan reuses the now-proven client pattern.

Implements D-21 (default-on handshake-prompt case; NO_WATCH-off case observed via the verbatim stderr message; remember CODEGRAPH_MCP_TOOLS only matters for companion tools, not codegraph_explore).

Purpose: close the loop — the 03-03 default flip and the 03-01 verbatim disabled message are proven reachable in production over real argv/stdio, not just in-process.
Output: `test/integration/watch_default_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-watcher-on-mcp-default/03-CONTEXT.md
@.planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md
@.planning/phases/03-watcher-on-mcp-default/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Default-on handshake-prompt case (WATCH-01/02) in watch_default_test.go</name>
  <files>test/integration/watch_default_test.go</files>
  <read_first>
    - test/integration/main_test.go (03-04 substrate — binPath, runBinary, runGitI, the indexed-fixture builder; reuse, do not re-create)
    - test/integration/worktree_notice_test.go (03-04 — the proven NewStdioMCPClient → Initialize → CallTool pattern to mirror; how the bounded context is set)
    - internal/cli/serve.go (03-03 — default-on behavior: bare `serve --mcp` watches when hasIndex; no --watch needed)
    - .planning/phases/03-watcher-on-mcp-default/03-CONTEXT.md D-21 (the two required cases + the CODEGRAPH_MCP_TOOLS note)
    - .planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md "Environment Availability" (git present/skip; WSL covered by unit tests, not here)
  </read_first>
  <action>
    Create `test/integration/watch_default_test.go` (same package as 03-04's files) and add `TestDefaultWatchHandshakePrompt` (name at discretion).

    Build a plain indexed fixture (an indexed main checkout, no worktree needed — reuse the 03-04 fixture builder or a simpler indexed-dir helper via runBinary "init"/"index"). Start `client.NewStdioMCPClient(binPath, nil, "serve", "--mcp", "-p", <indexedRoot>)` with NO watch flag in argv (default-on path). Under a bounded context (context.WithTimeout, e.g. 15-30s), call `c.Initialize(...)` then `c.ListTools(ctx, mcp.ListToolsRequest{})` (or a CallTool codegraph_explore) and assert both complete without the context deadline firing — i.e. the default-on watcher did NOT delay the handshake or first-tool availability (WATCH-02). Assert at least one tool (codegraph_explore) is advertised, confirming the server came up on the default-on path (WATCH-01). Close the client (defer).

    This is a promptness/reachability assertion, not a timing microbenchmark — the bound just has to be comfortably longer than a healthy handshake and short enough that a watcher-startup stall (the WSL2 failure mode) would trip it. Skip cleanly (t.Skip) if git is unavailable for the fixture.
  </action>
  <verify>
    <automated>go test ./test/integration/... -run 'TestDefaultWatchHandshakePrompt' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The test spawns bare `serve --mcp -p <root>` (NO `--watch`/`--no-watch` in argv) and completes Initialize + ListTools/CallTool within a bounded context — proving default-on watching does not delay the handshake (WATCH-01/WATCH-02).
    - codegraph_explore is present in the advertised tools (server is up on the default path).
    - It reuses 03-04's binPath + helpers (no second TestMain, no rebuilt binary).
    - Skips (not fails) when git is unavailable.
  </acceptance_criteria>
  <done>The real default-on serve --mcp binary completes the handshake promptly with the watcher running — WATCH-01/WATCH-02 proven end-to-end over stdio.</done>
</task>

<task type="auto">
  <name>Task 2: CODEGRAPH_NO_WATCH off-switch case observed via verbatim stderr message</name>
  <files>test/integration/watch_default_test.go</files>
  <read_first>
    - test/integration/main_test.go (binPath, runBinary, fixtures)
    - internal/watch/policy.go (the reason string `CODEGRAPH_NO_WATCH=1 is set` — 03-01) and internal/cli/serve.go (the verbatim `[CodeGraph MCP] File watcher disabled — ...` message routed to stderr — 03-03)
    - $GOMODCACHE/github.com/mark3labs/mcp-go@v0.56.0/client/stdio.go (GetStderr(c) returns the subprocess stderr reader — the mechanism to observe the disabled message)
    - .planning/phases/03-watcher-on-mcp-default/03-CONTEXT.md D-12/D-21 (verbatim stderr message; the message is model-invisible/log-surfaced)
  </read_first>
  <action>
    Add `TestNoWatchEnvDisablesViaStderr` (name at discretion) to `test/integration/watch_default_test.go`.

    Start `client.NewStdioMCPClient(binPath, env, "serve", "--mcp", "-p", <indexedRoot>)` where `env` includes `CODEGRAPH_NO_WATCH=1` (pass the full child env explicitly — append to os.Environ()-equivalent or provide the minimal set the binary needs plus the toggle; the client's env param is the child process environment). Obtain the child's stderr reader via `client.GetStderr(c)` and read it (in a goroutine or after Initialize) accumulating output under a bounded context. Complete Initialize (the server still comes up — disabling the watcher must NOT fail serve, per the best-effort/never-block philosophy). Assert the accumulated stderr contains the verbatim substring `File watcher disabled — CODEGRAPH_NO_WATCH=1 is set` (the D-12 message with the D-13/D-01 reason). Optionally assert the message also mentions `codegraph sync` to refresh. Close the client (defer).

    Note: because the disabled message is printed by serve.go's goroutine (which runs off the handshake path), read stderr with a small grace window after Initialize returns rather than expecting it strictly before — the assertion is "the message appears", not "before initialize". codegraph_explore needs no CODEGRAPH_MCP_TOOLS allowlist; only companion tools would (not used here).
  </action>
  <verify>
    <automated>go test ./test/integration/... -run 'TestNoWatchEnvDisablesViaStderr' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - With `CODEGRAPH_NO_WATCH=1` in the child env, the spawned serve --mcp still completes Initialize (never-block: disabling the watcher does not fail serve).
    - The child's stderr (via client.GetStderr) contains the verbatim `File watcher disabled — CODEGRAPH_NO_WATCH=1 is set` message.
    - Uses a bounded context + grace window (no unbounded read); skips cleanly on git absence.
    - Reuses 03-04's substrate; introduces no new dependency.
  </acceptance_criteria>
  <done>The verbatim disabled message reaches the real child's stderr when CODEGRAPH_NO_WATCH=1 — proving the 03-01 policy + 03-03 wiring are live in production, not just unit-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → spawned binary | argv + child env (CODEGRAPH_NO_WATCH) the harness passes to the real subprocess |
| subprocess stderr → harness | the disabled-message diagnostic the harness reads back |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Tampering | child env injection (CODEGRAPH_NO_WATCH) in the harness | low | mitigate | Env is test-authored and passed via exec.Command's env param (no shell); the toggle is exactly the input-validation surface WATCH-03 gates with strict `== "1"` |
| T-03-13 | Denial of Service | unbounded stderr read / hung handshake | medium | mitigate | Bounded context + grace window on the stderr read; Initialize under context.WithTimeout; git-absence skips |
| T-03-SC | Tampering (supply chain) | package installs | n/a | accept | Zero new dependencies — mcp-go client + GetStderr are existing pinned deps |
</threat_model>

<verification>
- `go test ./test/integration/... -count=1` green (anchor from 03-04 + both watch cases here).
- Full-suite: `go test ./... && go test ./testdata/golden/... && go test ./test/integration/...` green.
- The verbatim disabled message is observed on the real child's stderr under CODEGRAPH_NO_WATCH=1.
</verification>

<success_criteria>
TEST-04's WATCH coverage rides the subprocess harness: default-on `serve --mcp` completes the handshake promptly (WATCH-01/02) and CODEGRAPH_NO_WATCH=1 disables the watcher with the verbatim stderr message (WATCH-03) — all on the real spawned binary, zero new dependencies.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **Files:** `test/integration/watch_default_test.go`
- **Tests:** `TestDefaultWatchHandshakePrompt` (WATCH-01/02 default-on handshake-prompt), `TestNoWatchEnvDisablesViaStderr` (WATCH-03 NO_WATCH off-switch via verbatim stderr message)
- **Reused (no new artifacts):** 03-04's `binPath`, `runBinary`, `runGitI`, fixtures; mcp-go `client.GetStderr`, `NewStdioMCPClient`, `Initialize`, `ListTools`, `CallTool`
</artifacts_produced>

<output>
Create `.planning/phases/03-watcher-on-mcp-default/03-05-SUMMARY.md` when done.
</output>
