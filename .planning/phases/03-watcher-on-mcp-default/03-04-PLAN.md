---
phase: 03-watcher-on-mcp-default
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - test/integration/main_test.go
  - test/integration/worktree_notice_test.go
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "TestMain builds the real release binary once per test run (go build -o <tmp>/codegraph <module>/cmd/codegraph) — hermetic, no reliance on a pre-built artifact or PATH (D-18)"
    - "A spawned real `serve --mcp` session with cwd inside .claude/worktrees/<name>/ returns a codegraph_explore payload carrying the BARE U+26A0 worktree notice; a main-checkout control run shows none (D-20)"
    - "The harness is a normal Go package (test/integration/, never testdata/) reached by go test ./... AND by an explicit named CI step (D-17)"
    - "Reverting serve.go's two-arg BuildServer distinction (passing repoPath for both args, the CR-01 root cause) turns the anchor test red"
  artifacts:
    - test/integration/main_test.go
    - test/integration/worktree_notice_test.go
    - .github/workflows/ci.yml
  key_links:
    - "The harness drives serve.go's real cwd/argv → path-derivation → BuildServer wiring that in-process BuildServer→CallTool tests structurally bypass (D-19)"
    - "mcp-go's client.NewStdioMCPClient (existing dep) is the stdio JSON-RPC driver — zero new dependencies"
    - "The bare-glyph helper distinguishes the WORK-02 notice (U+26A0) from EXPL-04's emoji-presentation warning (U+26A0+U+FE0F)"
---

<objective>
Build TEST-04's subprocess integration harness substrate and its mandatory CR-01 anchor case, wired into CI. This retroactively guards Phase-2's worktree-notice wiring against the exact class of bug that made it dead-in-production for a whole phase while every in-process test passed.

Implements: D-17 (normal Go package `test/integration/`, never `testdata/`; explicit named CI step alongside the golden step), D-18 (TestMain builds the real binary once via `go build`), D-19 (drive the SPAWNED binary over real stdio JSON-RPC using mcp-go's client package — the seam in-process `BuildServer`→`CallTool` tests bypass), D-20 (the anchor: real git worktree in `.claude/worktrees/<name>`, cwd inside it, `codegraph_explore` payload carries the bare U+26A0 notice; main-checkout control shows none; mutation-proof against the CR-01 root cause).

Purpose: mechanize the session-long "drive the real entry point, never infer reachability across surfaces" lesson.
Output: `test/integration/main_test.go` (TestMain binary build + package-local git/glyph helpers + subprocess fixture), `test/integration/worktree_notice_test.go` (anchor + control), and a new explicit CI step in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-watcher-on-mcp-default/03-CONTEXT.md
@.planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md
@.planning/phases/03-watcher-on-mcp-default/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness substrate — main_test.go (TestMain binary build + runGitI + glyph helpers + subprocess worktree fixture)</name>
  <files>test/integration/main_test.go</files>
  <read_first>
    - internal/cli/notice_test.go lines 13-110 (runGitC, statusWorktreeMismatchFixture, noticeGlyph, containsBareNoticeGlyph — the exact shapes to copy as fourth package-local copies)
    - internal/cli/cli_test.go lines 15-49 (copyFixture — how the gofixture corpus is staged into a temp dir)
    - internal/gitmeta (Mismatch.Notice() — the single source of truth for the U+26A0 glyph bytes that noticeGlyph reads)
    - cmd/codegraph/main.go (the binary entrypoint being built)
    - .github/workflows/ci.yml lines 62-85 (how go list ./... and the explicit golden step interact; the -count=1 daemon isolation)
    - .planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md "Code Examples" (real-git fixture, mcp-go stdio client shape) + "Pitfall 4" (never testdata/) + "Pitfall 5" (bare-glyph vs emoji)
    - .planning/phases/03-watcher-on-mcp-default/03-PATTERNS.md "test/integration/ (new package)" (helper copies + TestMain build pattern)
  </read_first>
  <action>
    Create `test/integration/main_test.go` (package integration_test or a plain `package integration` test package — Claude's discretion, but it MUST be a normal, non-testdata Go package so `go list ./...` reaches it).

    TestMain (D-18): build the real binary once per `go test` run. Use `exec.Command("go", "build", "-o", binPath, "github.com/seanb4t/codegraph-go/cmd/codegraph")` where binPath is under a package-level temp dir (os.MkdirTemp — TestMain has no *testing.T; clean it up after m.Run()). Store binPath in a package-level var read by every test. If the build fails, m.Run must not proceed — print the build output and os.Exit(1). Guard the whole harness so it degrades gracefully where the go toolchain is unavailable is unnecessary (CI always has it); but keep git-dependent FIXTURES on the t.Skip path (below).

    Copy, as deliberate FOURTH package-local copies (Go test helpers are not importable across packages — the established convention; three copies already exist as runGitW/runGitM/runGitC):
    - `runGitI(t, dir, args...)`: verbatim shape of runGitC — same hermetic `-c` flags (init.defaultBranch=main, user.name, user.email, commit.gpgsign=false, protocol.file.allow=always), and t.Skipf (never t.Fatal) on any git failure.
    - `containsBareNoticeGlyph(s, glyph)`: verbatim from notice_test.go lines 95-110 — matches U+26A0 NOT followed by the U+FE0F variation selector, so it never false-positives against EXPL-04's `⚠️` warning.
    - `noticeGlyph(t)`: sources the glyph from `gitmeta.Mismatch{WorktreeRoot:"/a", IndexRoot:"/b"}.Notice()` first rune — one source of truth, not a pasted literal.

    Subprocess helpers:
    - `runBinary(t, dir string, env []string, args ...string) (stdout, stderr string, err error)`: `exec.Command(binPath, args...)` with cmd.Dir=dir (drives the REAL cwd/argv seam, D-19) — use for CLI-side and for `init`/`index`.
    - A worktree fixture builder (adapt statusWorktreeMismatchFixture): stage copyFixture-equivalent gofixture content into a temp main dir, `runGitI` init/add/commit, `runGitI worktree add -b probe <main>/.claude/worktrees/probe`, then index the MAIN checkout via the SUBPROCESS binary (`runBinary(t, main, nil, "init", "-p", main)` and, if init does not index, `runBinary(t, main, nil, "index", "-p", main)` — match whatever the CLI actually does; read init.go if unsure). Return absolute worktree-start and main-root paths. This is the D-19 seam distinction: index via the spawned binary, not an in-process indexer call.

    Do NOT place this package under any directory named testdata/ (Pitfall 4). Do NOT gate it behind a build tag.
  </action>
  <verify>
    <automated>go vet ./test/integration/... && go test ./test/integration/... -run 'TestMainBuildsBinary|TestNothing' -count=1 2>&1 | grep -Eq 'ok|no test files|PASS|RUN' && echo SUBSTRATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `go list ./... | grep -q '/test/integration$'` succeeds (the package IS reached by `./...` — not testdata-hidden).
    - The package builds and `go vet ./test/integration/...` is clean.
    - TestMain builds the binary at `github.com/seanb4t/codegraph-go/cmd/codegraph` into a temp path stored in a package-level var; a build failure aborts the run (os.Exit(1)) rather than silently proceeding.
    - `runGitI`, `containsBareNoticeGlyph`, `noticeGlyph`, `runBinary`, and the worktree fixture builder all exist; git failures skip (t.Skip), never Fatal.
    - `grep -rncq 'testdata' <(go list -f '{{.Dir}}' ./test/integration)` — the package dir path contains no `testdata` segment.
  </acceptance_criteria>
  <done>The subprocess harness substrate exists as a normal Go package: it builds the real binary once, and provides git-fixture + bare-glyph + subprocess-exec helpers reused by the anchor and watch cases.</done>
</task>

<task type="auto">
  <name>Task 2: CR-01 anchor — worktree_notice_test.go (serve --mcp explore payload carries the bare notice; main-checkout control none)</name>
  <files>test/integration/worktree_notice_test.go</files>
  <read_first>
    - test/integration/main_test.go (the substrate from Task 1 — binPath, runGitI, worktree fixture, noticeGlyph, containsBareNoticeGlyph, runBinary)
    - internal/cli/notice_test.go lines 144-183 (TestNoticeOnWorktreeMismatch — the in-process analog whose reachability gap this test closes)
    - internal/mcp (BuildServer's two-arg CR-01 wiring; how codegraph_explore renders the notice into its payload)
    - .planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md "Code Examples — mcp-go stdio client" (NewStdioMCPClient(binPath, env, "serve","--mcp","-p",repoPath); Initialize; CallTool codegraph_explore; result.Content[0].(mcp.TextContent).Text) + "Environment Availability" (git 2.55.0 present; skip on absence)
    - .planning/phases/03-watcher-on-mcp-default/03-CONTEXT.md D-19/D-20 (the anchor spec + mutation-proof requirement)
  </read_first>
  <action>
    Create `test/integration/worktree_notice_test.go` with the mandatory D-20 anchor case (name e.g. `TestWorktreeNoticeReachesServeMCPExplore`).

    Doc-comment discipline (WR-01/CR-01 lineage): state that this test drives the REAL spawned `serve --mcp` over stdio JSON-RPC — the exact cwd/argv→path-derivation→BuildServer seam in-process tests bypass — and that reintroducing CR-01 at its root cause (serve.go passing repoPath for BOTH BuildServer args) must turn this test red.

    Body:
    - Build the worktree fixture (Task 1 helper): indexed main checkout + `.claude/worktrees/probe` linked worktree; skip via the runGitI path if git is unavailable.
    - Start the MCP client with cwd INSIDE the worktree: `c, err := client.NewStdioMCPClient(binPath, nil, "serve", "--mcp")` — but NewStdioMCPClient does not set cwd, so instead pass the worktree as the working directory. Since the client spawns via its own exec, drive cwd through argv: run `serve --mcp -p <worktreeStart>` is NOT the anchor (that sets the confinement root, not the caller cwd). The anchor requires the PROCESS cwd to be the worktree. Use `client.NewStdioMCPClientWithOptions(binPath, nil, []string{"serve","--mcp"}, transport.WithCommandFunc(...))` if a cwd hook is available, OR the simplest robust path: set the child's cwd by wrapping — start the client against a small argv that runs in the worktree dir. Read $GOMODCACHE/github.com/mark3labs/mcp-go@v0.56.0/client/transport/stdio.go to find how to set the subprocess working directory (a Cmd customization option); use it to set cmd.Dir = worktreeStart. If no such option exists, fall back to launching with `-p <worktreeStart>` AND documenting that the derivation seam is still exercised because serve.go resolves start from -p when set (verify against serve.go's resolveStartPath: -p supplies `start`, which is the caller-cwd analog BuildServer receives as its 4th arg — so `-p worktreeStart` DOES reproduce the CR-01 seam). Prefer real cwd; use -p as the documented, seam-equivalent fallback.
    - `c.Initialize(ctx, mcp.InitializeRequest{...})` with a protocol version + clientInfo (leave ProtocolVersion empty to let the client default to LATEST; set a clientInfo name/version).
    - `c.CallTool(ctx, mcp.CallToolRequest{Params: mcp.CallToolParams{Name: "codegraph_explore", Arguments: map[string]any{"query": <a symbol the gofixture corpus resolves, e.g. "Alpha">}}})`. codegraph_explore is always visible — no CODEGRAPH_MCP_TOOLS allowlist needed.
    - Extract the text payload (result.Content[0] type-asserted to mcp.TextContent, `.Text`) and assert `containsBareNoticeGlyph(text, noticeGlyph(t))` is TRUE.
    - Control run: a SECOND client whose cwd/`-p` is the MAIN checkout (not the worktree) — same explore call — assert `containsBareNoticeGlyph(text, glyph)` is FALSE (no borrowed-index notice).
    - Close both clients (defer c.Close()); use a bounded context (context.WithTimeout, e.g. 30-60s) so a hung handshake fails the test rather than blocking CI. Guard slow cases with testing.Short() skip if desired (D-17).
  </action>
  <verify>
    <automated>go test ./test/integration/... -run 'TestWorktreeNoticeReachesServeMCPExplore' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The anchor test passes: the worktree-cwd (or `-p worktree`, documented seam-equivalent) `codegraph_explore` payload contains the bare U+26A0 notice; the main-checkout control payload does not (containsBareNoticeGlyph distinguishes it from EXPL-04's `⚠️`).
    - The client is mcp-go's `client.NewStdioMCPClient`/`...WithOptions` driving the SPAWNED binary — not an in-process BuildServer/CallTool.
    - A bounded context guards the handshake (no unbounded block); the test skips cleanly if git is unavailable.
    - Mutation proof (documented in SUMMARY, not committed): temporarily editing serve.go to `mcp.BuildServer(hasIndex, allowlist, repoPath, repoPath)` makes this test fail (the CR-01 root cause), while the in-process suite stays green — the exact reachability gap TEST-04 exists to close.
    - The test doc comment names that mutation explicitly.
  </acceptance_criteria>
  <done>The CR-01 anchor drives the real serve --mcp binary end-to-end and proves the worktree notice reaches a live codegraph_explore payload from a borrowed worktree — mutation-proof against the two-arg regression.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the explicit integration CI step in ci.yml (D-17 belt-and-braces)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 45-86 (the test job: Build, the filtered `go list ./...` test step, the explicit golden-suite step lines 68-76 with its GOLDEN-01 comment, the isolated daemon step)
    - .planning/phases/03-watcher-on-mcp-default/03-PATTERNS.md "Explicit named CI step" (the sibling step to add)
    - .planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md "Pitfall 4" (why the explicit step is required even though go list ./... already reaches test/integration)
  </read_first>
  <action>
    Add a new explicit step to the `test` job in `.github/workflows/ci.yml`, as a sibling of the existing golden-suite step (do NOT fold it into the filtered `go list ./...` step). Place it after the golden step (lines 75-76) and before or after the isolated daemon step — Claude's discretion. Name it clearly, e.g. "Test subprocess integration harness (test/integration)", with `run: go test ./test/integration/...`. Add a short comment citing D-17/GOLDEN-01: the explicit step is belt-and-braces so a future refactor of the filtered `go list` line cannot silently drop the harness, matching the exact rationale the golden step's comment already documents.

    Do not alter any other job (govulncheck, reproducibility, perf-regression) or the existing steps. Keep every Action pinned to its existing SHA (this edit adds only a `run:` step, no new Action).
  </action>
  <verify>
    <automated>grep -q 'go test ./test/integration/' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo CI-OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `run: go test ./test/integration/...` step in the `test` job, sibling to the golden step.
    - The file is valid YAML (parses without error).
    - No other job/step is modified (git diff shows only the added step + comment); all Actions remain SHA-pinned.
    - A comment on the new step cites the D-17/GOLDEN-01 belt-and-braces rationale.
  </acceptance_criteria>
  <done>CI runs the subprocess harness as an explicit named step alongside the golden suite, immune to a future `go list` refactor silently dropping it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → spawned binary | argv/env/cwd the harness passes to the real codegraph subprocess |
| git → fixture | git worktree/commit operations on hermetic t.TempDir content |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | subprocess argv/env injection in the harness | low | mitigate | All fixture content is test-authored, hermetic t.TempDir(); exec.Command (no shell interpolation) — the established execCmd pattern; no user-controlled input reaches the spawned binary |
| T-03-10 | Denial of Service | a hung serve --mcp handshake blocking CI indefinitely | medium | mitigate | Bounded context.WithTimeout on Initialize/CallTool; git-absence skips (t.Skip); TestMain build failure aborts fast |
| T-03-11 | Repudiation/Reachability | the harness silently not running (GOLDEN-01 recurrence) | high | mitigate | Normal Go package (not testdata/) reached by go list ./... PLUS an explicit named CI step (D-17) — Task 3 |
| T-03-SC | Tampering (supply chain) | package installs | n/a | accept | Zero new dependencies — mcp-go client is an existing pinned dep |
</threat_model>

<verification>
- `go test ./test/integration/... -count=1` green (substrate + anchor + control).
- `go list ./... | grep test/integration` succeeds; ci.yml has the explicit step and parses as YAML.
- Documented mutation flip (BuildServer two-arg → one-arg) turns the anchor red.
</verification>

<success_criteria>
TEST-04's harness spawns the real binary and proves — over real stdio JSON-RPC from inside a borrowed worktree — that the WORK-02 notice reaches a live codegraph_explore payload, with a clean control, mutation-proof against CR-01, and wired into CI as an explicit step that cannot be silently skipped.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **New package:** `test/integration/` (normal Go package, NOT testdata/)
- **Files:** `test/integration/main_test.go`, `test/integration/worktree_notice_test.go`, `.github/workflows/ci.yml` (modified)
- **Test infrastructure:** `TestMain` binary build (package-level `binPath` var); helpers `runGitI`, `runBinary`, `containsBareNoticeGlyph`, `noticeGlyph`, and a subprocess worktree fixture builder (fourth package-local copies of the runGit*/glyph pattern)
- **Tests:** `TestWorktreeNoticeReachesServeMCPExplore` (CR-01 anchor + main-checkout control)
- **CI step name:** "Test subprocess integration harness (test/integration)" → `go test ./test/integration/...`
</artifacts_produced>

<output>
Create `.planning/phases/03-watcher-on-mcp-default/03-04-SUMMARY.md` when done.
</output>
