---
phase: 05-process-ci-in-tree-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/root.go
  - internal/cli/migrate.go (deleted)
  - internal/cli/migrate_test.go (deleted)
  - internal/migrate/ (whole package deleted)
  - internal/graphstore/store.go
  - internal/graphstore/batch.go
  - internal/graphstore/pebble_store.go
  - internal/graphstore/keys.go
  - internal/graphstore/migration_record_test.go (deleted)
  - internal/indexer/resolve_test.go
  - internal/query/expand_test.go
  - internal/query/gather_test.go
  - internal/query/scoring_test.go
  - internal/query/search_test.go
  - internal/query/seeding_test.go
  - internal/query/traverse_test.go
  - testdata/golden/golden_test.go
  - testdata/golden/ts-schema.sql (deleted)
  - testdata/golden/ts-schema.dump.sql (deleted)
  - testdata/golden/ts-version.txt (deleted)
  - testdata/golden/README.md
  - internal/indexer/nodeid/nodeid.go
  - README.md
  - go.mod
  - go.sum
autonomous: true
requirements: [CODE-03]
user_setup: []

estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `codegraph migrate` command is gone from the CLI surface: `internal/cli/root.go` no longer registers newMigrateCmd (root.go:59) and its two doc comments (root.go:9, root.go:35) no longer enumerate migrate; the whole `internal/migrate/` package (4,018+ lines incl. tests, migratetest fixture, archtest confinement test), `internal/cli/migrate.go`, and `internal/cli/migrate_test.go` are deleted; `go build ./...` and `go vet ./...` exit 0 (per D-04, maintainer ruling 2026-08-15): the one-step TS-SQLite->Pebble conversion no longer exists; a TS user installs the Go binary and re-indexes from source."
    - "The graphstore migration-cursor API is gone in the same change: `Reader.GetMigration` (store.go:51-57), `Writer.PutMigration` (store.go:199-205), the pebble/batch impls (pebble_store.go:278-282, batch.go:113-118), the `migrationRecordName` meta-key const (keys.go:195-204), the 173-line `migration_record_test.go`, and the 8 test-fake stubs (resolve_test.go:1033 PutMigration; query/{expand,gather,scoring,search,seeding,traverse}_test.go GetMigration) — nothing left in `internal/` implements or calls them, and the underlying key name never appears again (CODE-03 nothing-left-referencing-it)."
    - "The three ts-* fixtures and their existence subtests are deleted in the same edit: `testdata/golden/ts-schema.sql`, `ts-schema.dump.sql`, `ts-version.txt` are gone, and the two `TestGoldenFixturesExist` subtests (golden_test.go:66-72 and :74-88) that hard-fail on their absence are removed at the same time — `go test -count=1 ./testdata/golden/...` exits 0 immediately after this diff (research Pitfall 1: `go test ./...` never runs testdata/, so the task must run the explicit golden path)."
    - "`modernc.org/sqlite` and its indirect deps (`modernc.org/{libc,mathutil,memory}`) are gone: `go mod tidy` drops the go.mod require block (lines :40, :160-162) and all 28 modernc.org go.sum lines; `rg modernc go.mod go.sum` returns zero after tidy; `go build ./...` still exits 0 (sole-use verified by `go mod why`)."
    - "The README Commands table has no `migrate` row (:137); the `testdata/golden/README.md:10-13` bullet describing the migration reader and the :59-85 pitfall note are rewritten with the fixture gone; the `internal/indexer/nodeid/nodeid.go:36-38` comment no longer cites `testdata/golden/ts-schema.dump.sql`; the `internal/cli/man.go` needs no edit (Cobra tree auto-excludes the removed command)."
    - "No file under `internal/`, `tools/`, or `testdata/golden/` still references any of the removed `parity_*` names in a way that points at a deleted file (becomes the stale-filename census owned by 05-04/05-05); the golden suite stays green both ways."
  prohibitions:
    - "no `codegraph migrate` command in the CLI tree"
    - "no GetMigration/PutMigration/migrationRecordName symbol anywhere in internal/"
    - "no ts-* fixture file or golden_test subtest for them remaining"
    - "no modernc / mathutil-marked go.mod or go.sum lines remaining"
    - "no README-row or nodeid.go citation still pointing at the deleted ts fixtures"
    - "no regex sweep over the word 'migrate' or 'TS' (the census is term-by-term with recorded reasons, per D-02)"
  artifacts:
    - internal/cli/root.go edited (registration + doc comments)
    - the graphstore {store,batch,pebble_store,keys}.go cursor API removed, migration_record_test.go deleted
    - internal/indexer/resolve_test.go and the six internal/query test files' stub methods removed
    - go.mod, go.sum edited (modernc block gone)
    - testdata/golden/golden_test.go edited (two t.Run subtests removed)
    - testdata/golden/README.md edited (ts-fixture bullets gone), nodeid.go edited, README.md edited
  key_links:
    - "root.go:59 AddCommand ↔ root.go:9/:35 doc comments — all three name migrate; remove registration and rewind comments in the same edit"
    - "graphstore interfaces ↔ pebble/batch impls ↔ migrationRecordName const ↔ 8 test fakes — a compile-safe interface removal only works because no call site exists outside internal/migrate and the stubs; the same commit removes them all"
    - "ts-* fixtures ↔ TestGoldenFixturesExist subtests golden_test.go:66/88 — deleting the fixtures without deleting the subtests hard-fails `go test ./testdata/golden/...` (research Pitfall 1)"
---

<objective>
Remove the `codegraph migrate` capability entirely (CODE-03, maintainer ruling D-04): the command registration and package docs in `internal/cli/root.go`, the whole `internal/migrate/` package plus the CLI command files, the `modernc.org/sqlite` sole-use dependency and its transitive closure in go.mod/go.sum, the graphstore cursor API that only the migration progress writer used (`GetMigration`/`PutMigration`/`migrationRecordName` + `migration_record_test.go` + 8 test-fake stubs), the three `testdata/golden/ts-*` legacy-schema fixtures and their `TestGoldenFixturesExist` subtests, and every product-doc reference (README command row, testdata/golden/README bullet, nodeid.go comment) — all in one coherent cut so nothing references the removed capability (CODE-03 "nothing left referencing it").

Purpose: This is the phase's tracer. The migrate removal exercises the phase's entire end-to-end blast radius — CLI registration → whole package deletion → dependency pruning → graphstore API cut → golden-suite subtest coupling → doc-reference sweep — proving on the largest single diff that the phase's cut discipline (delete the consumer AND the served API together) holds. It also retires the only reason `modernc` was in the module graph, shrinking the supply chain. The end-to-end verify (`go build`, `go vet`, `go test ./testdata/golden/...` with `-count=1`, `git diff --stat` showing only deletions) is the pattern every later plan in the phase reuses.

Output: No `codegraph migrate`, no `internal/migrate`, no `modernc` in go.mod/go.sum; golden suite green; the tree has no consumer for the graphstore cursor key.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-RESEARCH.md

# The primary surfaces (read the current state first)
@internal/cli/root.go (:9 migrate doc, :35 daemon+migrate doc, :59 AddCommand with newMigrateCmd)
@go.mod (:40 modernc.org/sqlite direct; :160-162 indirect modernc libc/mathutil/memory)
@testdata/golden/golden_test.go (:66-88 the two TestGoldenFixturesExist ts subtests)
@testdata/golden/README.md (:10-13 the ts-fixtures bullet)
@README.md (:137 the Commands-table migrate row)

# The deletion surface (read the tree listings to confirm the file set, then delete)
@internal/migrate/ (migrate.go/reader.go/translate.go/swap.go/validate.go/progress.go/batchwriter_test.go + its *_test.go + migratetest/ + archtest/)
@internal/graphstore/store.go (:51-56 Reader.GetMigration, :197-203 Writer.PutMigration)
@internal/graphstore/batch.go (:113-118), pebble_store.go (:278-282), keys.go (:195-204)
@internal/graphstore/migration_record_test.go

# Test-fakes and consumers
@internal/indexer/resolve_test.go (:1033 PutMigration stub)
@internal/query/{expand,gather,scoring,search,seeding,traverse}_test.go (the GetMigration stubs)
</context>

<tasks>

<task type="tracer">
  <name>Cut the command, package, dep — codegraph migrate and internal/migrate and modernc go (whole surface at once)</name>
  <files>internal/cli/root.go, internal/cli/migrate.go, internal/cli/migrate_test.go, internal/migrate/, go.mod, go.sum</files>
  <read_first>
    internal/cli/root.go — the AddCommand call at :59 (newMigrateCmd list position), the package doc at :8-13 naming migrate, and the newRootCmd comment at :35 naming the one-step conversion; re-read before deleting any identifier so you know the complete doc-cited set
    internal/cli/migrate.go and internal/cli/migrate_test.go — confirm both files are fully within the delete set (research §3.2)
    go.mod — line 40 (modernc.org/sqlite direct), lines 160-162 (modernc.org indirect) — your go mod tidy target
  </read_first>
  <action>
    Delete the `codegraph migrate` end-to-end in one commit:

    1. `rm -rf` the entire `internal/migrate/` directory (migrate.go, reader.go, translate.go, swap.go, validate.go, progress.go, batchwriter_test.go, all their _test.go, migratetest/ fixture, archtest/ confinement test). Do NOT keep any file — the archtest/modernc_confinement_test.go was itself a vacancy guard asserting modernc is confined, and it dies with the package.
    2. Delete `internal/cli/migrate.go` and `internal/cli/migrate_test.go` (the newMigrateCmd builder, dirNonEmpty, printMigrateReport).
    3. Edit `internal/cli/root.go`: remove `newMigrateCmd()` from the AddCommand list at :59; edit the package doc at :9-13 to drop the sentence enumerating migrate; edit the newRootCmd comment at :35-39 to drop "and migrate (D-08), the one-step TS CodeGraph SQLite -> new-format Pebble store conversion". The remaining sentence about sync/daemon/unlock/githooks/man stays intact.
    4. Run `go mod tidy` (go 1.26.5). Confirm the modernc.org require lines drop — do NOT hand-edit go.mod beyond what tidy does; then confirm `go build ./...` and `go vet ./...` exit 0.
    5. Scan term-by-term (never a word-alone regex over "migrate"): confirm no remaining Go import of internal/migrate and no remaining modernc reference anywhere in the module. The `testdata/golden/ts-*` fixtures and the graphstore cursor API are LATER tasks in this plan — do not delete them here; this task is specifically the package, the command, and the dependency.
  </action>
  <verify>
    <automated>bash -c 'set -e; test -d internal/migrate && { echo "internal/migrate still exists"; exit 1; }; test -f internal/cli/migrate.go && { echo "cli migrate.go still exists"; exit 1; }; go build ./... && go vet ./...; n=$(rg -c "modernc" go.mod go.sum | awk -F: "{s+=$2} END {print s+0}"); echo "modernc references: $n"; test "$n" = "0"; m=$(rg -ln "internal/migrate" internal/ --glob "*.go" | wc -l | tr -d " "); echo "internal/migrate importers: $m"; test "$m" = "0"'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/migrate/` directory absent (`git status --short` shows D for all its path)
    - `internal/cli/migrate.go` and migrate_test.go absent
    - `internal/cli/root.go` has no line containing `newMigrateCmd` and doc-enum "the one-step conversion" gone
    - `go build ./...` and `go vet ./...` exit 0
    - `rg -n 'modernc' go.mod go.sum` emits nothing (the whole block pruned by tidy; 28 go.sum lines gone)
    - `rg -ln internal/migrate internal/` emits nothing — the archtest, the migratetest, all imports resolved
  </acceptance_criteria>
  <done>codegraph migrate no longer exists as a command and internal/migrate no longer exists as code; the module no longer imports modernc anywhere; build and vet are green.</done>
</task>

<task type="auto">
  <name>Cut the graphstore m/migration cursor — Reader/Writer methods, impls, const, test, and 8 fakes in one diff</name>
  <files>internal/graphstore/store.go, internal/graphstore/pebble_store.go, internal/graphstore/batch.go, internal/graphstore/keys.go, internal/graphstore/migration_record_test.go, internal/indexer/resolve_test.go, internal/query/expand_test.go, internal/query/gather_test.go, internal/query/scoring_test.go, internal/query/search_test.go, internal/query/seeding_test.go, internal/query/traverse_test.go</files>
  <read_first>
    internal/graphstore/store.go — the Reader interface block at :51-56 and the Writer interface block at :197-203, plus the imports line so you can drop nothing else
    internal/graphstore/keys.go — the migrationRecordName const block at :195-204 (do NOT remove the other consts like the schema meta key)
    internal/graphstore/migration_record_test.go — read the header to know why it exists, then delete it wholesale
    internal/indexer/resolve_test.go around :1033 and internal/query/{expand,gather,scoring,search,seeding,traverse}_test.go — locate each test fake's GetMigration stub (they implement the old Reader interface)
  </read_first>
  <action>
    Remove the migration-progress cursor API in the same change as you cut the package (it had no call site outside internal/migrate — verified). In `internal/graphstore/store.go` delete the `GetMigration` method from the Reader interface (:51-56) and the `PutMigration` method from the Writer interface (:197-203). Delete the impls at `pebble_store.go:278-282` (GetMigration) and `batch.go:113-118` (PutMigration). In `internal/graphstore/keys.go` delete `migrationRecordName = "migration"` (:195-204) — keep every OTHER meta-key const (the schema meta key etc.) intact. Delete `internal/graphstore/migration_record_test.go` (the whole 173-line round-trip/idempotence/ErrNotFound test). Then remove the eight test-fake stubs that exist only to satisfy the old Reader/Writer methods: `PutMigration` in `internal/indexer/resolve_test.go:1033` and the `GetMigration` stubs in `internal/query/expand_test.go`, `gather_test.go`, `scoring_test.go`, `search_test.go`, `seeding_test.go`, `traverse_test.go`. Removing the two methods from the interfaces is compile-safe for all remaining implementors (Go only breaks on a call site; with the call sites gone in the same cut, the fakes have no reason to exist). If a test file still has other test funcs after its stub is removed, keep the file.

    Re-check with a positive scan that no production or test code in `internal/` implements, calls, or names `GetMigration`/`PutMigration`/`migrationRecordName` any more, and that the `m`...`migration` meta-key name is gone from `keys.go`. The word "migrate" will still appear in past-tense commit-level notes if there are any — that is expected; the CODE-03 sweep is term-by-term with recorded reasons, never a blanket regex over the word alone (D-02).
  </action>
  <verify>
    <automated>bash -c 'set -e; go build ./... >/dev/null; n=$(rg -c "GetMigration|PutMigration|migrationRecordName|m/migration" internal/ --glob "*.go" | awk -F: "{s+=$2} END {print s+0}"); echo "cursor-API refs remaining: $n"; test "$n" = "0"; go test -count=1 ./internal/graphstore/... >/dev/null && go test -count=1 ./internal/indexer/... >/dev/null && go test -count=1 ./internal/query/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "GetMigration|PutMigration|migrationRecordName" internal/ --glob '*.go'` returns 0 aggregate
    - `internal/graphstore/migration_record_test.go` gone
    - `go build ./...` green (interface cut is compile-safe because Fakes went too)
    - `go test -count=1 ./internal/graphstore/... ./internal/indexer/... ./internal/query/...` green
    - The `m`...`migration` meta-key name no longer appears in `keys.go`
  </acceptance_criteria>
  <done>No migration-cursor API exists in internal/graphstore — the exported methods, their impls, the meta-key const, the round-trip test, and every test fake are gone in the same change; internal/graphstore, internal/indexer, and internal/query tests go green.</done>
</task>

<task type="auto">
  <name>Delete the ts-* fixtures and their golden-suite subtests in the same edit — golden_test, golden/README, nodeid comment, README row</name>
  <files>testdata/golden/golden_test.go, testdata/golden/ts-schema.sql, testdata/golden/ts-schema.dump.sql, testdata/golden/ts-version.txt, testdata/golden/README.md, internal/indexer/nodeid/nodeid.go, README.md</files>
  <read_first>
    testdata/golden/golden_test.go — the two ts-* subtests in TestGoldenFixturesExist at :66-75 and :77-88, and the rest of the test file so you only delete those two t.Run blocks
    testdata/golden/README.md — the section at :10-13 that describes the ts-* fixtures and the "Phase 7 one-way migration reader" (plus any "Historical bug note" at :59-85 referencing the TS project which this plan rewrites)
    internal/indexer/nodeid/nodeid.go — the comment at :36-38 that cites `testdata/golden/ts-schema.dump.sql` for the id-shape
    README.md — the Commands table and the migrate row at :137 (identify the exact table extent so removing one row keeps the table well-formed)
  </read_first>
  <action>
    Delete the three `ts-*` files (`ts-schema.sql`, `ts-schema.dump.sql`, `ts-version.txt`) AND in the SAME edit remove the two `TestGoldenFixturesExist` subtests that assert them (golden_test.go:66-75 "ts-schema.sql exists and is non-empty" and :77-88 "ts-version.txt exists, non-empty... records a version"). Do not remove the third subtest ("at least one corpus JSON fixture exists") — it still passes against the behavioral corpus. Then edit `testdata/golden/README.md` (:10-13) so it no longer describes the ts fixtures or the migration reader — rewrite that bullet to describe only the current contents (behavioral corpus + goldens) and drop the "Historical bug note (Pitfall 2)" TS-project/upstream issue references at :59-85. Then reword `internal/indexer/nodeid/nodeid.go:36-38` to drop the citation to the deleted `ts-schema.dump.sql` and describe the id-shape on its own terms, e.g. "matching the project's `<kind>:<32-hex>` id shape". Finally remove the `| migrate |` row from the Commands table in `README.md:137`.

    Verification trap (Pitfall 1): `go test ./...` will NOT run `test/testdata/...` because the Go tool excludes `testdata/`. So after editing, verify the golden suite explicitly with `go test -count=1 ./testdata/golden/...` — that is the check that would have failed if the subtests were left behind while fixtures were removed. Also run `go test -count=1 ./...` after it for the whole tree.
  </action>
  <verify>
    <automated>bash -c 'set -e; test ! -e testdata/golden/ts-version.txt; test ! -e testdata/golden/ts-schema.sql; test ! -e testdata/golden/ts-schema.dump.sql; go test -count=1 ./testdata/golden/... >/dev/null; go test -count=1 ./... >/dev/null; n=$(rg -c "migrate" README.md | awk -F: "{s+=$2} END {print s+0}"); echo "migrate refs in README: $n"; test "$n" = "0"'</automated>
  </verify>
  <acceptance_criteria>
    - `test -e` fails for all three `ts-*` fixture files (they are deleted)
    - the two `TestGoldenFixturesExist` ts-subtests are gone (a `rg -n "ts-schema|ts-version" testdata/golden/golden_test.go` returns nothing for the subtest blocks; the third subtest is untouched)
    - `go test -count=1 ./testdata/golden/...` exits 0 (explicit — this is the command that would catch the pitfall; `./...` alone hides it)
    - `go test -count=1 ./...` exits 0 (whole tree)
    - `README.md` has no remaining `migrate` mention (`rg` count 0)
    - `testdata/golden/README.md` no longer names the ts fixtures or the migration reader; `internal/indexer/nodeid/nodeid.go` no longer cites `ts-schema.dump.sql`
  </acceptance_criteria>
  <done>The ts-* fixtures and their subtests are gone in the same edit; the golden suite passes explicitly (not hidden behind ./...); README's migrate row, the golden README's fixture bullet, and nodeid's ts-schema citation no longer exist; full `go test -count=1 ./...` is green.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| `internal/migrate/` package | deleted | 4,018 lines incl. migratetest/ + archtest/modernc_confinement_test.go |
| `internal/cli/migrate.go` / `migrate_test.go` | deleted | 258 lines |
| `internal/cli/root.go` | edited | AddCommand list: newMigrateCmd removed; doc/comment-sentence de-migrated |
| `modernc.org/sqlite` (+ libc/mathutil/memory indirect) | removed | go.mod :40/:160-162 + 28 go.sum lines, via `go mod tidy` |
| `graphstore` cursor API | edited/deleted | store.go |:51-56/:197-203; pebble_store.go :278-282; batch.go :113-118; keys.go :195-204 const; migration_record_test.go whole |
| 8 test-fake stubs | edited/deleted | resolve_test.go:1033 + query/expand,gather,scoring,search,seeding,traverse_test.go that GetMigration/PutMigration |
| 3 ts fixtures | deleted | testdata/golden/ts-schema.sql / ts-schema.dump.sql / ts-version.txt |
| 2 TestGoldenFixturesExist subtests | deleted | golden_test.go:66-88 |
| testdata/golden/README.md | edited | ts-fixture & migration reader bullets dropped / past-tense P1-2 |
| internal/indexer/nodeid/nodeid.go | edited | the ts-schema.dump.sql citation is reworded to describe the id shape |
| README.md | edited | migrate row (—:137) taken out of the Commands table |

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI tree → module graph | Removing the command must also remove its only dependency; a stale `modernc` require after the cut is supply-chain surface with no consumer. |
| graphstore API ↔ test fakes | removing the interface always leaves the impls and the fakes in sync; an API cut that leaves one fake behind keeps the retired word alive in test code. |
| golden fixtures ↔ subtests | deleting a fixture that a subtest asserts breaks the golden suite; both must go in the same edit (and `go test ./...` misses testdata/golden). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------------|-----------------|
| T-05-01 | DoS | migrate removal — `modernc` left in go.mod | high | mitigate | `go mod tidy` + verify `rg modernc go.mod go.sum` empty in task 1's automated verify; build must still link. |
| T-05-02 | Tampering | ts-* fixtures deleted, subtests left | high | mitigate | fixtures+subtests in the same task's edit, verified by an explicit `go test -count=1 ./testdata/golden/...` (Pitfall 1). |
| T-05-03 | Tampering | cursor API removed but one fake remains | high | mitigate | task 2 runs a positive scan (`rg GetMigration\|PutMigration\|...` expecting 0 aggregate) after build. |
| T-05-04 | DoS (silent pass) | `go test ./...` passes while golden suite broken | medium | mitigate | task 3 is done only when `go test -count=1 ./testdata/golden/...` also exits 0 (./... excludes testdata). |
| T-05-05 | Tampering | same-change re-freeze interactions | medium | mitigate | golden re-freeze is plan 05-05 (D-07); this plan only deletes the ts fixtures, and `TestGoldenScenarioCountIsExact` (30) does not change. |

<verification>
- `go build ./...` && `go vet ./...` && `go test -count=1 ./...` all exit 0 at the end of the plan.
- `go test -count=1 ./testdata/golden/...` explicit (not hidden) green after the ts-* delete.
- `rg -n modern/go.sum` empty; `rg -ln internal/migrate internal/` empty (post-task-1).
- `git show --stat` for the plan shows only deletions plus small edits in root.go/store-tests/keys/nodeid/README/golden README.
</verification>

<success_criteria>
- `codegraph migrate` is not a command (no registration, no man page, no docs).
- `internal/migrate/` + `cli/migrate*` are deleted and nothing imports them.
- `GetMigration`/`PutMigration`/`migrationRecordName` and the m ...migration meta-key exist only in git history.
- no `modern` require/go.sum lines; `go mod tidy` changes nothing further.
- 2 ts-fixtures are gone with their 2 subtests, and the explicit golden run is green.
</success_criteria>

Reversibility notes: The command/package/dependency deletion is a one-way decision (D-04, maintainer ruling 2026-08-15). Restorability via git history only — the archived build phase `milestones/v0.1-phases/07-migration-tool` stays in the repo. The cursor API and the fixtures are parts of the same cut; a rollback is a revert of the original diff, not a re-author.

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-01-SUMMARY.md` when done
</output>