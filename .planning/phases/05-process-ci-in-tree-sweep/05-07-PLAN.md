---
phase: 05-process-ci-in-tree-sweep
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - internal/cli/search.go
  - internal/cli/node.go
  - internal/cli/files.go
  - internal/cli/uninit.go
  - internal/cli/serve.go
  - internal/cli/githooks_test.go
  - internal/mcp/tools.go
  - internal/agents/codex.go
  - internal/agents/opencode.go
  - internal/agents/claude.go
  - internal/agents/instructions.go
  - internal/query/traverse_test.go
  - .planning/WINDOWS.md
autonomous: true
requirements: [CODE-01]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader of `internal/cli/search.go` and `internal/cli/node.go` finds the compact-worktree-notice PLACEMENT rationale (inside the human-output branch, after the `--json` early return; after the query succeeds so a failing query leaves no bare notice on stdout) stated as codegraph-go's own design call — the WORK-02/D-12/WR-05 citations survive and no prior implementation is cited as the baseline that justifies the placement (D-01)."
    - "`internal/cli/files.go` still documents that `--dir` is a directory-path prefix match, orthogonal to and AND-composed with the language `--filter` (SURF-02, CONTEXT D-03 add-alongside), and its `--dir` flag help text still names the same behavior — with the comparison clause gone from both the doc comment and the user-visible help string."
    - "`internal/cli/serve.go` still writes the D-12/D-13 disabled message BYTE-IDENTICALLY (`[CodeGraph MCP] File watcher disabled — %s. The graph will not auto-update; run \\`codegraph sync\\` (or install the git sync hooks via \\`codegraph init\\`) to refresh.\\n`) and still documents that it is stderr-only/model-invisible, terminal (no retry), IN-05 reason-extraction and the IN-02 `errors.As` gating — only the sentence attributing the wording to another implementation is reworded."
    - "`internal/cli/githooks_test.go`'s `markerBeginBytes` constant is still exactly `# >>> codegraph sync hook >>>` and its doc comment still says it is a local copy used only as an on-disk detection string (D-03) — reworded on the same terms `internal/githooks/githooks.go:18-20` already uses after 05-05."
    - "`internal/cli/uninit.go` still documents D-06's best-effort hook cleanup contract in full (githooks.Remove degrades rather than errors; its outcome is never propagated up RunE so cleanup can never fail uninit; per-hook failures are still surfaced as WR-01 warnings via printHookErrors) with the cross-implementation source citation gone."
    - "`internal/mcp/tools.go` still documents the deliberate markdown-vs-JSON asymmetry (each `Marshal*JSON` helper has exactly one caller — the CLI `--json` path, a parser consumer and testdata/golden's shape oracle; each `Render*Markdown` has exactly one caller — this file, a language-model consumer) and still documents why `codegraph_status` is excluded from the compact worktree notice (RenderStatusMarkdown embeds its own verbose blockquote, D-17) — with neither framed as a divergence from, or a mirror of, another implementation."
    - "`internal/agents/codex.go` still documents global-scope-only support (`SupportsLocation(LocationLocal)` false, local Install/Uninstall are no-ops) and the hand-rolled single-table TOML splice in `toml.go` (chosen to avoid a general TOML dependency), with no prior implementation cited as the reason for either."
    - "`internal/query/traverse_test.go`'s `affectedDepthFixtureNodesEdges` doc comment still explains the three-hop reverse chain and the test-files-as-leaves pruning property it proves (SURF-04/D-05, RESEARCH Pitfall 2) on codegraph-go's own terms."
    - "The wrap-tolerant (`rg -U`) framing census returns ZERO across all 9 edited Go files, and the same instrument is proven live first: every regex in the pattern set matches a synthetic wrapped positive control, and a line-based run of the same patterns over the same wrapped input returns strictly fewer matches — the measurement defect that let these 10 locations survive a green sweep is demonstrated, not assumed."
    - "`internal/agents` is audited across all 13 non-test source files (not the 10-name sample 05-05 used); `opencode.go`, `claude.go` and `instructions.go`'s issue-number citations are adjudicated explicitly and recorded as KEEP or FIXED with a reason."
  prohibitions:
    - "NO capability, behavior, flag-name, env-var, assertion, string-literal, regex-literal or marker-byte change anywhere — these are comment and help-text edits only. `internal/cli/files.go:90`'s help string is the ONE user-visible string that changes, and only by deleting the parenthetical comparison clause."
    - "NO find-and-replace over `TS`, `TypeScript`, `parity`, `port`, `original` or `upstream`. Every retained use is resolved term-by-term with a recorded reason (D-02). An unbounded bare-word regex already mis-fired this phase (`ports` matching inside `reports`); word-bound every term."
    - "NO deletion of a comment whose technical content is not restated. Deleting the D-12/D-13 contract note, the worktree-notice placement rationale, the markdown-vs-JSON asymmetry note, the D-06 cleanup contract, or the marker-byte note is a documentation regression, not progress — reword, do not remove."
    - "NO edits to `internal/query/status.go` or `internal/query/files_status_test.go` — 05-04-SUMMARY.md's KEEP ledger already adjudicated `status.go:21,25,35,41,44,76` (the D-05 one-time schema-migration decision table) and `files_status_test.go:489,511` (frozen-fixture provenance) as recorded keeps. Do not re-litigate a settled adjudication."
    - "NO edits to `internal/bench/**` or `tools/bench/**` — Phase 6's BENCH-02 removes the comparison runner; WINDOWS #16 records this deferral by design."
    - "NO edits to `testdata/golden/behavioral_test.go` — plan 05-08 owns that file. Touching it here creates the concurrent-editor collision the wave discipline exists to prevent."
    - "NO `[ci skip]` or `[skip ci]` in any commit message (repo rule f18zrdsgx5) — protect-main requires 6 status checks and a skipped commit permanently blocks the PR."
    - "NO `rg -c` in any census (it counts matching LINES per file, not matches) and NO line-based `rg` for comment-prose censuses (it cannot see a phrase wrapped across a comment line break — this is the exact root cause of this gap)."
  artifacts:
    - "internal/cli/search.go, node.go, files.go, uninit.go, serve.go, githooks_test.go — comment prose (and files.go's one help string) on codegraph-go's own terms"
    - "internal/mcp/tools.go — the SURF-06/D-16 asymmetry note and the codegraph_status notice-exclusion note reworded"
    - "internal/agents/codex.go — package-doc reworded; opencode.go / claude.go / instructions.go adjudicated"
    - "internal/query/traverse_test.go — affectedDepthFixtureNodesEdges doc comment reworded"
    - ".planning/WINDOWS.md — any CODE-01 framing found outside this plan's declared scope, logged and committed rather than silently swept"
  key_links:
    - "internal/cli/serve.go's reworded comment <-> the unchanged fmt.Fprintf format string it describes <-> internal/cli/serve_test.go:160's `want` substring assertion (the byte contract is test-enforced; the test must stay green without editing it)"
    - "internal/cli/githooks_test.go's reworded comment <-> markerBeginBytes <-> internal/githooks/githooks.go:26's markerBegin (four copies of the same bytes across two packages must stay byte-identical)"
    - "internal/cli/files.go's --dir doc comment <-> the --dir flag help string (both carry the comparison clause; fixing one and not the other leaves the census non-zero)"
    - "The wrap-tolerant census instrument <-> its synthetic positive control (a zero from an unproven instrument is the failure this plan exists to close)"
---

<objective>
Close the CODE-01 gap in the 9 source files outside `testdata/golden/`: reword every remaining comparison-baseline comment onto codegraph-go's own terms while preserving the technical contract each comment documents, and do it behind a measurement instrument that can actually see wrapped comment prose.

Purpose: ROADMAP Phase 5 success criterion 4 (CODE-01) is the one criterion that FAILED verification. Criteria 1, 2, 3 and 5 pass and are out of scope. The failure is narrow — 10 comment-only locations — but its root cause is not: both flagship censuses (05-04, 05-05) used line-based `rg`, which returns ZERO for a phrase split across a comment line wrap. Nine of the ten locations were also outside every plan's declared file scope. This plan fixes both: a wrap-tolerant, positive-controlled instrument, and a generously declared file scope.

Output: 9 reworded Go files, zero behavior/assertion/literal change, and a per-file census that is zero because the instrument was proven live first.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-VERIFICATION.md
@.planning/phases/05-process-ci-in-tree-sweep/05-05-SUMMARY.md
@.planning/WINDOWS.md
</context>

<the_contract>
## What "comparison framing" means here (D-01/D-02, verbatim from 05-CONTEXT.md)

**REMOVE (D-01):** references to the prior implementation **as a comparison baseline** — "matches TS", "mirrors TS", "no TS precedent", "verbatim TS", "byte-identical TS", "Go-vs-TS divergence", "diverges from TS's output", "fewer than TS", "TS returns/integrates/pulls/renders", "TS's <anything>".

**KEEP (D-02), each with a recorded reason:**

| Class | Example in this tree | Reason |
|---|---|---|
| PRODUCT-SURFACE | `internal/indexer/tsextract`, the language registry, `TS/JS` language-group keys, tree-sitter TypeScript grammar node shapes in `internal/indexer/routes/` | TypeScript-the-indexed-language, not TypeScript-the-origin-project |
| PAST-TENSE-HISTORY | "The TS-era capture path ... is retired/gone" | D-02 explicitly carves out past-tense history |
| BUG-PRECEDENT-CITATION | `opencode.go` "TS issue #535", `claude.go` "TS issue #207", `instructions.go` "removed in #529/#704" | A historical bug-number citation, not a baseline claim |
| RECORDED-KEEP | `internal/query/status.go:21,25,35,41,44,76`; `internal/query/files_status_test.go:489,511` | Already adjudicated in 05-04-SUMMARY.md's KEEP ledger — settled, do not re-litigate |
| PHASE-6-DEFERRED | `internal/bench/**`, `tools/bench/**` | ROADMAP Phase 6 BENCH-02 removes the comparison runner; WINDOWS #16 |

**The naive-regex failure mode is named in the requirement and has already fired this phase:** an unbounded `ports` matched inside `reports` and an executor reworded innocent contributor prose instead of fixing the pattern. Word-bound every term. Resolve every retained use term-by-term with a reason; never by regex.
</the_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Build and positive-control the wrap-tolerant census, then sweep the three worktree-notice / flag-help sites</name>
  <files>internal/cli/search.go, internal/cli/node.go, internal/cli/files.go</files>
  <read_first>
    - `internal/cli/search.go:52-58` — the compact worktree notice call site and its 4-line wrapped rationale comment. The framing phrase `no TS / precedent for this CLI placement` is split across lines 55 and 56 — this is the canonical instance of the wrap that defeated the original census.
    - `internal/cli/node.go:58-64` — the same notice, same rationale, wrapped differently (`no TS precedent for this CLI placement; deliberate Go-side design; a failing query must not leave a bare notice on stdout`).
    - `internal/cli/explore.go` — the call site both comments point at for "the full rationale". Read it to confirm what that rationale actually says post-05-04/05-05, so the two pointers stay truthful.
    - `internal/cli/files.go:6-16` — `newFilesCmd`'s doc comment; the `--dir` clause at :18-19 carries `matches TS's files --filter <dir> semantics`.
    - `internal/cli/files.go:90` — the `--dir` flag registration; its help string carries `(matches TS --filter <dir>)`. This is USER-VISIBLE `--help` output. It is asserted by no test and no golden (confirmed: the only other occurrence of the string in the repo is `CHANGELOG.md`, which is release-please-owned and out of scope).
    - `internal/githooks/githooks.go:18-24` — the reword style 05-05 already established for a byte-contract comment; match its register.
  </read_first>
  <action>
    First, establish the instrument, because a zero from an unproven census is exactly what produced this gap.

    Write a synthetic positive-control fixture under `${TMPDIR:-/tmp}` containing each framing phrase family split across a Go comment line break. Run the wrap-tolerant patterns (`rg -U`, word-bounded, counted with `rg -o | wc -l` — never `rg -c`, which counts matching lines rather than matches) against it and confirm every pattern matches. Then run the SAME patterns without `-U` against the SAME fixture and confirm the line-based run finds strictly fewer. That comparison is the positive control: it demonstrates the defect rather than assuming it. Record both numbers in the SUMMARY.

    Then reword the three sites. Preserve every technical fact; delete only the clause whose sole content is the comparison.

    search.go:52-56 — keep: "Compact worktree notice (WORK-02, D-12)", that it lives strictly inside the human-output branch, that it is AFTER the `--json` early return, and the pointer to `explore.go`'s call site for the full rationale. Replace the parenthetical `(no TS precedent for this CLI placement)` with a statement that the placement is codegraph-go's own design call — the parenthetical asserts only that no prior implementation did this, which is a comparison claim carrying no independent technical content.

    node.go:58-63 — keep: "Compact worktree notice (WORK-02, D-12)", "printed AFTER the query succeeds (WR-05)", the pointer to explore.go, and the operative reason "a failing query must not leave a bare notice on stdout". Drop `no TS precedent for this CLI placement` and fold `deliberate Go-side design` into a self-standing statement that this is codegraph-go's own design call.

    files.go:18-19 — keep the whole `--dir` description: directory-path prefix match, SURF-02, orthogonal to and composed AND with the language `--filter`, CONTEXT D-03 add-alongside. Delete only `matches TS's files --filter <dir> semantics;` — the actual semantic (directory-path prefix match) is already stated in the same parenthetical, so the clause is pure comparison.

    files.go:90 — change the help string to `directory-path prefix filter`, dropping the `(matches TS --filter <dir>)` parenthetical. Do not rename the flag, change its default, or alter its type.

    If any CODE-01 framing turns up in an `internal/cli` file NOT in this plan's `files_modified`, append it to `.planning/WINDOWS.md` via `gsd-tools windows append --kind deviation ...` and COMMIT that file rather than widening the edit set silently.
  </action>
  <verify>
    <automated>bash -c 'set -e; S="[[:space:]/*]+"; F="${TMPDIR:-/tmp}/code01-poscontrol.txt"; printf "%b\n" "// rationale (no TS" "// precedent for this CLI placement)." "// the dir filter (matches" "// TS files semantics)." > "$F"; pc=$(rg -U -o -i -e "\bno${S}TS${S}precedent\b" -e "\b(matches|matching|mirrors|mirroring)${S}(the${S})?TS\b" "$F" | wc -l | tr -d " "); lb=$(rg -o -i -e "\bno${S}TS${S}precedent\b" -e "\b(matches|matching|mirrors|mirroring)${S}(the${S})?TS\b" "$F" | wc -l | tr -d " "); echo "positive control: multiline=$pc line-based=$lb (multiline MUST exceed line-based, and MUST exceed 0)"; test "$pc" -gt "$lb"; test "$pc" -gt 0; test -f internal/cli/search.go; n=$(rg -U -o -i -e "\bTS\b" internal/cli/search.go internal/cli/node.go internal/cli/files.go | wc -l | tr -d " "); echo "TS mentions left in search.go+node.go+files.go: $n"; test "$n" = "0"; h=$(rg -U -o -i -e "\bWORK-02\b" -e "\bD-12\b" -e "\bSURF-02\b" internal/cli/search.go internal/cli/node.go internal/cli/files.go | wc -l | tr -d " "); echo "surviving technical citations (WORK-02/D-12/SURF-02) — MUST exceed 3, proving the rationale was reworded not deleted: $h"; test "$h" -gt 3; go build ./... && go test -count=1 ./internal/cli/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - The positive control ran BEFORE the zero was trusted, and its two numbers (multiline vs line-based on identical wrapped input) are recorded in the SUMMARY.
    - `internal/cli/search.go`, `node.go` and `files.go` contain zero `\bTS\b` occurrences under a multiline census.
    - The WORK-02 / D-12 / WR-05 / SURF-02 / CONTEXT D-03 citations and the `explore.go` cross-references all survive — the gate's positive count proves the comments were reworded, not deleted.
    - `--dir`'s flag name, type, default and behavior are unchanged; only its help string's trailing parenthetical is gone.
    - `go build ./...` and `go test ./internal/cli/...` are green.
  </acceptance_criteria>
  <done>The wrap-tolerant instrument is built and demonstrated to see what the line-based one cannot; the three worktree-notice/flag-help sites read as codegraph-go's own design decisions with every technical citation intact.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the internal/cli byte-contract cluster — uninit, serve, githooks_test</name>
  <files>internal/cli/uninit.go, internal/cli/serve.go, internal/cli/githooks_test.go</files>
  <read_first>
    - `internal/cli/uninit.go:55-70` — the D-06 best-effort hook-cleanup comment. Framing is `mirrors TS bin/codegraph.js ~629-636's uninit cleanup`, wrapped across lines 56-57. The rest of the comment (githooks.Remove degrades rather than errors; its outcome is never propagated up RunE; per-hook failures still surface as WR-01 warnings via printHookErrors, matching the standalone `githooks remove` command) is the real contract and must survive.
    - `internal/cli/serve.go:136-153` — the `errors.As(runErr, &de)` branch. Framing is `D-12/D-13: verbatim TS disabled message` at :137. Everything else in that comment (stderr-only/model-invisible; terminal, no retry because policy does not change mid-session; Pitfall 2 `--no-watch` must still print it; IN-05 reason extracted from the typed error rather than re-derived; IN-02 round-5 `errors.As` gating symmetric with cli/daemon.go so a bare or wrapped sentinel falls through to default) is live engineering rationale.
    - `internal/cli/serve.go:151-153` — the `fmt.Fprintf(stderr, ...)` format string. THIS IS THE BYTE CONTRACT. Do not touch it.
    - `internal/cli/serve_test.go:141,160-164` — the test that asserts that message as a substring. Read it; do not edit it. It is the enforcement that proves the bytes survived.
    - `internal/cli/githooks_test.go:50-53` — the `markerBeginBytes` doc comment (`the verbatim TS sync/git-hooks.js begin marker (D-03)`) and the constant. The constant value `# >>> codegraph sync hook >>>` is the byte contract.
    - `internal/githooks/githooks.go:18-28` — 05-05 already reworded the sibling comment for the same bytes to "the documented marker bytes — a detection key, not documentation. Byte-identical markers mean hooks installed by any earlier install are recognized and managed by this binary." Match that register so the two packages read consistently.
  </read_first>
  <action>
    uninit.go:55-60 — drop `mirrors TS bin/codegraph.js ~629-636's uninit cleanup` and state the cleanup on its own terms: `codegraph uninit` removes the git sync hooks it installed, best-effort. Keep D-06, the githooks.Remove degradation contract, the never-propagated-up-RunE guarantee, the WR-01 warning surfacing, and the `githooks remove` cross-reference verbatim in substance.

    serve.go:137 — replace `verbatim TS disabled message` with a description of what the message actually is on this project's terms: the documented D-12/D-13 disabled message whose exact wording is fixed by contract and asserted by `serve_test.go`. Say that the wording is contractual so a future reader does not "improve" it. Change no other line of the comment and no line of the `fmt.Fprintf` call.

    githooks_test.go:50-52 — replace `the verbatim TS sync/git-hooks.js begin marker` with wording matching `internal/githooks/githooks.go:18-20`: the documented marker begin bytes (D-03) — a detection key, not documentation. Keep the two facts that justify the local copy: `internal/githooks` does not export it, and it is used here only as a detection string for on-disk assertions. Do not change the constant's value or name.

    Byte-invariance is the acceptance condition for this task, not a nice-to-have: after the edits, `git diff` for these three files must show changed lines ONLY inside `//` comments. If a diff hunk touches a non-comment line, revert it.

    Log anything found outside this plan's `files_modified` to `.planning/WINDOWS.md` and commit it rather than widening scope.
  </action>
  <verify>
    <automated>bash -c 'set -e; test -f internal/cli/uninit.go -a -f internal/cli/serve.go -a -f internal/cli/githooks_test.go -a -f internal/githooks/githooks.go; n=$(rg -U -o -i -e "\bTS\b" internal/cli/uninit.go internal/cli/serve.go internal/cli/githooks_test.go | wc -l | tr -d " "); echo "TS mentions left in uninit.go+serve.go+githooks_test.go (MUST be 0): $n"; test "$n" = "0"; m=$(rg -F -o -e "# >>> codegraph sync hook >>>" internal/cli/githooks_test.go internal/githooks/githooks.go | wc -l | tr -d " "); echo "intact marker-byte copies (MUST be 2): $m"; test "$m" = "2"; d=$(rg -F -o -e "[CodeGraph MCP] File watcher disabled — %s. " internal/cli/serve.go | wc -l | tr -d " "); echo "intact disabled-message format literal in serve.go (MUST be 1): $d"; test "$d" = "1"; k=$(rg -U -o -i -e "\bD-06\b" -e "\bD-12/D-13\b" -e "\bIN-05\b" -e "\bIN-02\b" -e "\bWR-01\b" -e "\bD-03\b" internal/cli/uninit.go internal/cli/serve.go internal/cli/githooks_test.go | wc -l | tr -d " "); echo "surviving contract citations (D-06/D-12-D-13/IN-05/IN-02/WR-01/D-03) — MUST exceed 5, proving reword not deletion: $k"; test "$k" -gt 5; D=$(git diff -U0 -- internal/cli/uninit.go internal/cli/serve.go internal/cli/githooks_test.go); if [ -z "$D" ]; then D=$(git diff -U0 HEAD~1 HEAD -- internal/cli/uninit.go internal/cli/serve.go internal/cli/githooks_test.go); fi; echo "$D" | rg -q "^[+-]"; nc=$(echo "$D" | rg "^[+-]" | rg -v "^(\+\+\+|---)" | rg -v "^[+-][[:space:]]*//" | wc -l | tr -d " "); echo "non-comment changed lines across the three files (MUST be 0; the diff was asserted non-empty first so this cannot pass vacuously): $nc"; test "$nc" = "0"; go build ./... && go test -count=1 ./internal/cli/... ./internal/githooks/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - Zero `\bTS\b` occurrences remain in the three files under a multiline census.
    - `internal/cli/serve.go`'s `fmt.Fprintf` format string is byte-identical to its pre-edit value and `internal/cli/serve_test.go` passes WITHOUT being edited.
    - `markerBeginBytes` is still exactly `# >>> codegraph sync hook >>>` and still matches `internal/githooks/githooks.go`'s `markerBegin`.
    - `git diff` for all three files shows changed lines only inside `//` comments — mechanically asserted, not eyeballed.
    - D-06's cleanup contract, D-12/D-13's stderr-only/terminal/IN-05/IN-02 rationale, and D-03's marker-detection note all still exist in reworded form.
  </acceptance_criteria>
  <done>The three byte-contract sites read on the project's own terms with every asserted byte, literal and contract citation intact and mechanically proven unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep internal/mcp/tools.go, the full internal/agents package, and internal/query/traverse_test.go</name>
  <files>internal/mcp/tools.go, internal/agents/codex.go, internal/agents/opencode.go, internal/agents/claude.go, internal/agents/instructions.go, internal/query/traverse_test.go</files>
  <read_first>
    - `internal/mcp/tools.go:358-382` — the SURF-06/D-16 asymmetry note. Framing is `This closes a Go-vs-TS divergence: TS returns markdown from every MCP tool; our JSON-shaped tools were the anomaly` at :368-370. The rest (each `Marshal*JSON` helper now has exactly one caller — the CLI `--json` path, whose consumer is a parser and which is testdata/golden's shape oracle; each `Render*Markdown` has exactly one caller — this file, whose consumer is a language model; the asymmetry is deliberate and must NOT be "unified") is the design record that stops a future reader from collapsing the two paths.
    - `internal/mcp/tools.go:524-532` — the `codegraph_status` exclusion note. Framing is `mirroring TS's withWorktreeNotice, which excludes codegraph_status for exactly this reason` at :528-530. The technical reason (Engine.Status already computes StatusResult.WorktreeMismatch and RenderStatusMarkdown embeds it as its own verbose blockquote, D-17, so a second compact prefix would duplicate it) stands on its own without the cross-reference.
    - `internal/agents/codex.go:12-20` — `codexTarget`'s doc comment. Two framing hits: `Codex has no per-project config concept TS integrates with` (:14) and `mirrors TS's own toml.ts — no general TOML dependency` (:17).
    - `internal/agents/opencode.go:39` and `:233-234`, `internal/agents/claude.go:22`, `internal/agents/instructions.go:19` — the `TS issue #535` / `TS issue #207` / `removed in #529/#704` citations. 05-05 flagged these as "appear to be the same historical-citation KEEP class ... but not verified line-by-line since these files were never in scope"; the verifier independently adjudicated them as legitimate keeps. Read each one IN CONTEXT and record an explicit KEEP or FIXED decision with a reason. Do not touch them on a hunch either way.
    - `internal/query/traverse_test.go:778-785` — `affectedDepthFixtureNodesEdges`'s doc comment. Framing is `TS test-files-as-leaves pruning`, wrapped across lines 780-781. The property it documents (SURF-04/D-05, RESEARCH Pitfall 2; TestSomething reachable only at depth>=2 through the non-test intermediary; a test dependent is a LEAF so its own dependent TestGrand never surfaces at any depth) is the whole point of the fixture.
    - The full `internal/agents` package listing — 13 non-test source files: antigravity.go, claude.go, codex.go, cursor.go, gemini.go, hermes.go, instructions.go, kiro.go, manifest.go, opencode.go, registry.go, shared.go, toml.go, types.go. 05-05 task 1 covered a 10-NAME SAMPLE. Enumerate the package's actual population and audit all of it, plus its `_test.go` files.
  </read_first>
  <action>
    tools.go:368-370 — keep the entire asymmetry record. Replace the closing sentence so it states the design on this project's terms: the MCP tools render markdown because their consumer is a language model, while the CLI `--json` path marshals JSON because its consumer is a parser — SURF-06/D-16 made the two consistent with their consumers. Drop `Go-vs-TS divergence` and `TS returns markdown from every MCP tool; our JSON-shaped tools were the anomaly` — the anomaly claim only has meaning against the comparison baseline.

    tools.go:528-530 — keep the D-17 reason in full and delete the `mirroring TS's withWorktreeNotice` clause. The exclusion is justified by RenderStatusMarkdown's own verbose blockquote; nothing else is needed.

    codex.go:13-19 — reword to: Codex CLI has no per-project config concept, so `SupportsLocation(LocationLocal)` is false and Install/Uninstall at local are no-ops. Keep the `~/.codex/config.toml` path, the hand-rolled single-table splice in `toml.go`, the reason for it (avoids taking a general TOML dependency), the marker-fenced `~/.codex/AGENTS.md` block, the D-05a/D-07 citations and the "one of only 4 of 8 targets that gets an instructions file" fact. Drop both `TS integrates with` and `mirrors TS's own toml.ts`.

    opencode.go / claude.go / instructions.go — adjudicate each issue-number citation term-by-term. If the citation's content is "this behavior exists because of a specific historical bug, tracked as issue #NNN", it is BUG-PRECEDENT-CITATION and stays (D-02). If it instead asserts an ongoing baseline ("we do X because that is what the other implementation does"), it is D-01 framing and gets reworded. Record the decision and reason for EACH of the four locations in the SUMMARY's KEEP/FIXED ledger. Note 05-05 already applied exactly this treatment to `internal/gitmeta/detect.go` and `worktree.go:27` (dropped the implementation name, kept the issue number) — that precedent is available if a reword is warranted.

    traverse_test.go:780-781 — drop the `TS` qualifier from `TS test-files-as-leaves pruning` and keep the property description and its SURF-04/D-05 + RESEARCH Pitfall 2 citations intact. The pruning rule is codegraph-go's own documented behavior; naming another implementation adds nothing.

    Finally, run a package-wide multiline audit over all of `internal/agents` (all 13 source files plus tests), not the 10-name sample. This audit is the reason this gap has a `codex.go` entry at all. Anything found outside this plan's `files_modified` goes to `.planning/WINDOWS.md` and gets committed.
  </action>
  <verify>
    <automated>bash -c 'set -e; S="[[:space:]/*]+"; test -f internal/mcp/tools.go -a -f internal/agents/codex.go -a -f internal/query/traverse_test.go; n=$(rg -U -o -i -e "\bTS\b" internal/mcp/tools.go internal/agents/codex.go internal/query/traverse_test.go | wc -l | tr -d " "); echo "TS mentions left in tools.go+codex.go+traverse_test.go (MUST be 0): $n"; test "$n" = "0"; a=$(rg -U -o -i --glob "*.go" -e "\bno${S}TS${S}precedent\b" -e "\b(matches|matching|mirrors|mirroring)${S}(the${S})?TS\b" -e "\bverbatim${S}TS\b" -e "\bTS${S}(returns|integrates|pulls|renders|appears)\b" -e "TS[\x27\x{2019}]s\b" internal/agents/ | wc -l | tr -d " "); echo "framing constructions left anywhere in internal/agents (MUST be 0): $a"; test "$a" = "0"; scanned=$(rg --files --glob "*.go" internal/agents/ | wc -l | tr -d " "); echo "internal/agents files audited (MUST exceed 25, proving the package population was enumerated not sampled): $scanned"; test "$scanned" -gt 25; cites=$(rg -U -o -i -e "\bTS${S}issue${S}#[0-9]+" -e "#529/#704" internal/agents/ | wc -l | tr -d " "); echo "surviving historical bug-precedent citations (recorded KEEP class — MUST exceed 0, proving the audit did not blanket-delete): $cites"; test "$cites" -gt 0; k=$(rg -U -o -i -e "\bSURF-06\b" -e "\bD-16\b" -e "\bD-17\b" -e "\bD-05a\b" -e "\bSURF-04\b" internal/mcp/tools.go internal/agents/codex.go internal/query/traverse_test.go | wc -l | tr -d " "); echo "surviving design citations (SURF-06/D-16/D-17/D-05a/SURF-04) — MUST exceed 4: $k"; test "$k" -gt 4; go build ./... && go test -count=1 ./internal/mcp/... ./internal/agents/... ./internal/query/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - Zero `\bTS\b` in `internal/mcp/tools.go`, `internal/agents/codex.go` and `internal/query/traverse_test.go` under a multiline census.
    - Zero framing constructions anywhere in `internal/agents` under a multiline census, while the historical bug-precedent citations survive with a positive count — proving the audit adjudicated rather than blanket-deleted.
    - The `internal/agents` audit enumerated the package's real population (13 source files + tests, 28 files total), not 05-05's 10-name sample; the file count is asserted, not asserted-by-narrative.
    - The SURF-06/D-16 markdown-vs-JSON asymmetry record, the D-17 status-exclusion reason, codex.go's global-scope-only + hand-rolled-TOML rationale, and traverse_test.go's SURF-04/D-05 pruning property all survive in reworded form.
    - Each of the four issue-number citations in opencode.go/claude.go/instructions.go carries an explicit KEEP-or-FIXED decision with a reason in the SUMMARY.
    - `go build ./...` green; `./internal/mcp/...`, `./internal/agents/...`, `./internal/query/...` tests green.
  </acceptance_criteria>
  <done>The MCP design record, the agents package (fully enumerated, not sampled), and the traverse fixture doc all read on codegraph-go's own terms, with the historical-citation KEEP class preserved and individually justified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan changes only Go comments and one Cobra flag help string. No input parsing, no I/O path, no privilege boundary, and no dependency is added, removed or upgraded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-01 | Tampering | `internal/cli/serve.go` D-12/D-13 disabled-message format literal | medium | mitigate | The literal is a documented user-facing contract asserted by `serve_test.go:160`. Task 2's gate asserts the exact format literal is present exactly once by fixed-string match AND that `git diff` shows zero non-comment changed lines, so a comment edit cannot silently drift the message. |
| T-05-07-02 | Tampering | `markerBeginBytes` / `markerBegin` git-hook marker bytes across `internal/cli` and `internal/githooks` | high | mitigate | A drifted begin marker orphans every hook a previous install wrote, leaving executable content in `.git/hooks` that this binary no longer recognizes or removes. Task 2's gate fixed-string asserts both copies are present and identical, plus the zero-non-comment-diff assertion, plus `./internal/githooks/...` tests. |
| T-05-07-03 | Information disclosure | `internal/cli/files.go:90` `--dir` help text | low | accept | Help text is already public; deleting a parenthetical reveals nothing. No test or golden asserts the string (verified repo-wide). |
| T-05-07-04 | Repudiation | `.planning/WINDOWS.md` ledger completeness | medium | mitigate | An out-of-scope find that is swept silently, or found and not logged, reproduces exactly the failure this plan closes. Each task requires the find be appended to WINDOWS.md and COMMITTED rather than the edit set widened. |
| T-05-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan; no dependency is added, removed or upgraded. `go.mod`/`go.sum` are not in `files_modified`. RESEARCH.md's Package Legitimacy Gate is therefore not engaged. |
</threat_model>

<verification>
1. `go build ./...` exits 0 and `go vet ./...` exits 0.
2. `go test -count=1 ./internal/cli/... ./internal/githooks/... ./internal/mcp/... ./internal/agents/... ./internal/query/...` all pass, with no test file edited to make them pass.
3. Every task's census gate is preceded by its positive control; a zero count is reported only alongside the instrument that produced it and the proof that instrument is live.
4. `git diff --stat` for this plan's commits touches only the files in `files_modified`. `git diff` shows no change to any string literal, regex literal, constant value, flag name, function signature or assertion — with `internal/cli/files.go:90`'s help string as the single declared exception.
5. `internal/query/status.go`, `internal/query/files_status_test.go`, `internal/bench/**`, `tools/bench/**` and `testdata/golden/behavioral_test.go` are untouched (`git diff --name-only` confirms).
6. The SUMMARY carries a KEEP/FIXED ledger in 05-04/05-05's established format: every adjudicated location with its class (PRODUCT-SURFACE / PAST-TENSE-HISTORY / BUG-PRECEDENT-CITATION / RECORDED-KEEP / PHASE-6-DEFERRED / FIXED) and a one-line reason.
7. No commit message contains `[ci skip]` or `[skip ci]` (repo rule f18zrdsgx5).
</verification>

<success_criteria>
- 9 Go files reworded; zero `\bTS\b` remaining in each under a multiline census.
- Zero framing constructions anywhere in `internal/agents` while its historical bug-precedent citations survive with a positive count.
- Every byte contract (`serve.go`'s disabled message, both marker-byte copies) mechanically proven unchanged.
- Every technical citation the swept comments carried (WORK-02, D-12, D-13, WR-05, WR-01, D-03, D-06, SURF-02, SURF-04, SURF-06, D-16, D-17, D-05, D-05a, IN-02, IN-05, CONTEXT D-03) still present, asserted by positive count rather than by narrative.
- Build, vet and the five affected package test suites green with no test edited.
- Any out-of-scope find logged to `.planning/WINDOWS.md` and committed.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Purpose |
|---|---|
| `internal/cli/search.go`, `node.go`, `files.go` | Worktree-notice placement rationale and `--dir` semantics stated as codegraph-go's own design; `--dir` help text de-framed |
| `internal/cli/uninit.go`, `serve.go`, `githooks_test.go` | D-06 cleanup contract, D-12/D-13 disabled-message contract and D-03 marker-detection note reworded with their bytes provably intact |
| `internal/mcp/tools.go` | SURF-06/D-16 markdown-vs-JSON design record and D-17 status-exclusion reason on own terms |
| `internal/agents/codex.go` (+ audited `opencode.go`, `claude.go`, `instructions.go`) | Codex target rationale de-framed; the package's real population audited rather than the prior 10-name sample |
| `internal/query/traverse_test.go` | `affectedDepthFixtureNodesEdges` pruning property on own terms |
| `.planning/WINDOWS.md` | Any CODE-01 framing outside declared scope, logged and committed |
| `05-07-SUMMARY.md` | KEEP/FIXED adjudication ledger + the recorded census instrument and its positive-control numbers |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-07-SUMMARY.md` when done.
</output>
