---
phase: 05-process-ci-in-tree-sweep
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - internal/query/expand.go
  - internal/query/explore_gate.go
  - internal/query/explore.go
  - internal/query/files.go
  - internal/query/worktree_notice.go
  - internal/query/engine.go
  - internal/query/gather.go
  - internal/query/node.go
  - internal/query/node_test.go
  - internal/query/render_markdown.go
  - internal/query/render_markdown_test.go
  - internal/query/render_results.go
  - internal/query/render_status.go
  - internal/query/render_status_test.go
  - internal/query/rwr.go
  - internal/query/scoring.go
  - internal/query/seeding.go
  - internal/query/tokenize.go
  - internal/query/search.go
  - internal/query/search_test.go
  - internal/query/validate.go
  - internal/query/status.go
  - internal/query/traverse.go
  - internal/query/files_status_test.go
  - internal/query/explore_test.go
  - internal/indexer/capability/matrix.go
  - internal/indexer/capability/matrix_test.go
  - testdata/golden/behavioral_test.go
autonomous: true
requirements: [CODE-01]
user_setup: []

estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every comment and doc sentence across `internal/query` reads on codegraph-go's own terms after the sweep: the file-header claims that the file 'ports TS CodeGraph 1.3.1's' algorithm are rewritten to behavioral descriptions (e.g. tokenize.go:9, expand.go:1, gather.go:1, scoring.go:1, seeding.go:1, explore_gate.go:1, rwr.go:1), every 'ported VERBATIM'//'ported verbatim' claim is reworded to describe the behavior orderly (node.go:92, search.go:192, validate/tokenize rows), and every 'TS's X is ...' implementation note that makes TS the comparison baseline is neutralized (render_status.go, render_markdown.go, watch/status rows). This is done row-by-row against the research census §4.1 — never a find-replace over the word 'TS' (D-02)."
    - "The golden-suite own-terms naming is restored where a stale filename is cited: the comments in render_results.go:12, render_markdown.go:170, and render_markdown_test.go:236 that mention the pre-Phase-2 file or the 'golden-parity' harness are reworded to name the current suite files (behavioral_*_test.go / testdata/golden) — these are the stale path references, not framing (constraint 4)."
    - "The identifier residuals are renamed to behavioral names in the SAME commit as their neighbors (D-09): copySyntheticParityFixture -> copyBehavioralFixture, syntheticParityEngine -> behavioralEngine (both in explore_test.go with all call sites), and syntheticParitySrc -> behavioralCorpusSrc inside testdata/golden/behavioral_test.go (:237-239) with its five call sites (:693, :858, :1267, :1310, :1311) updated; `rg 'syntheticParity' internal/ testdata/` returns zero after."
    - "The capability matrix's `golden-parity` code comments (matrix.go:18, :32 and matrix_test.go:217, :221) now read 'behavioral golden test' / 'golden harness' on the project's own terms, while the matrix DOC values and Gaps cells are unchanged — matrix_test.go keeps passing (constraint 5)."
    - "The past-tense history rows are preserved and recorded in the SUMMARY as KEEP with a reason: status.go:44's long-explanatory note that the D-05 --json-shape constraint was formally retired (it is past-tense history, product truth D-02); any 'iter/e-order contract' dataflow terms in traverse.go (KEEP, dataflow language not comparison)."
  prohibitions:
    - "no file-wide find-replace over 'TS', 'port', or the origin project's name (D-02 term-by-term, the census is the work list)"
    - "no deletion of golden files (they embed the corpus source — plan 05-06 owns the re-freeze; only the .go test-file identifiers and code comments in the listed files are edited, never the JSON corpora)"
    - "no change to the capability matrix's doc table values or coverage bullets (matrix_test.go byte-matches them)"
    - "no renaming of any identifier that does not carry the parity/source word (the 'behavioral' name family only; `TestGoldenParity*` was already renamed in Phase 2)"
    - "no rewriting of corpus/behavioral/src comment content (D-07, plan 05-06 owns the corpus reword + re-freeze)"
  artifacts:
    - internal/query/*.go + *_test.go (the ~20 query files) edited — comment prose on own terms
    - testdata/golden/behavioral_test.go edited — syntheticParitySrc -> behavioralCorpusSrc
    - internal/indexer/capability/matrix.go + matrix_test.go edited — golden-parity comment words
  key_links:
    - "the census (§4.1) ↔ the files — each row cites the file:line; the executor works row-by-row and records a reason per changed line in the SUMMARY"
    - "syntheticParitySrc ↔ its 5 call sites — an identifier rename without updating call sites breaks the tests; updated in the same task"
    - "the matrix doc ↔ matrix_test.go — the reword touches only code comments, not the table; matrix_test.go proves it"
---

<objective>
Apply CODE-01 to the flagship in-tree surface — the `internal/query` package (the largest concentration of comparison framing: six file headers claiming to port TS CodeGraph, dozens of "ported VERBATIM"/"TS's" body comments across the .go files and their _test.go rows) plus the two golden-side residuals the census flags (the `syntheticParity*` identifier family in explore_test.go and behavioral_test.go, and the `golden-parity` code comments in the capability matrix, constraint 5). Reword every hit row-by-row with a recorded reason, never by regex, per D-02 and the code census contract.

Purpose: `internal/query` is where a source-code reader is most likely to meet the retired framing — six package headers say "ports TS CodeGraph 1.3.1's ..." and dozens of lines explain why the code matches TS behavior. When this plan lands, an agent reading the source meets a project described on its own terms (that's the platform core-value the milestone restates), TypeScript-the-indexed-language and past-tense history stay (D-02), and the identifier/comment residuals D-03/D-09 flagged in Phase 4 are closed.

Output: `internal/query` reads as original behavioral descriptions; the behavioral golden-test identifiers carry the behavioral prefix; `matrix.go` comments reference the current harness; the three stale-filename rows point at the renamed files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-RESEARCH.md

# The census is the work list (research §4.1, plus D-09 rows)
@internal/query/ (the query package - 5 file headers + 23 row rows from §4.1)
@internal/indexer/capability/matrix.go / matrix_test.go (D-03 the golden-parity comments)
@testdata/golden/behavioral_test.go (the syntheticParitySrc identifier, lines 237-239)
</context>

<tasks>

<task type="tracer">
  <name>The flagship file headers and their direct body claims — incl. the five review-H3 rows (engine/explore/files/worktree_notice)</name>
  <files>internal/query/explore.go, internal/query/engine.go, internal/query/files.go, internal/query/worktree_notice.go, internal/query/expand.go, internal/query/explore_gate.go, internal/query/gather.go, internal/query/scoring.go, internal/query/seeding.go, internal/query/tokenize.go, internal/query/rwr.go, internal/query/node.go</files>
  <read_first>
    internal/query/expand.go — header :1-5 ('ports TS CodeGraph 1.3.1's pipeline'), :33-34 ported VERBATIM
    internal/query/explore.go — :111 'not TS's exact step function', :211 'wires the full TS-parity explore pipeline in TS's own stage order' (review H3, VERIFIED present)
    internal/query/engine.go — :179 'Absolutizing here mirrors TS's own …' (review H3, VERIFIED present)
    internal/query/files.go — :25/:97-99 'matching TS 1.3.1's files --filter' rows (review H3, VERIFIED present)
    internal/query/worktree_notice.go — :43 'matching TS's …' (review H3, VERIFIED present)
    internal/query/gather.go — :1-3 header, :89 'ported verbatim for behavioral parity', :218/:585/:749 'once getStemVariants is ported' variants
    internal/query/explore_gate.go — :1 header, :13-16 'ports under-selects' + 'ported independent'
    internal/query/scoring.go — :1 header, :170-172 'documented D-02 substitute for TS's' variations
    internal/query/seeding.go — :1 header, :17 'substituting for TS's', :154 'TS's own SELECT'
    internal/query/tokenize.go — :9-11 :45 'ported verbatim from TS 1.3.1', :99-101 'TS's getStemVariants', :155 'TS's exact ...'
    internal/query/rwr.go — :1-2 'ports' + 'explains RWR'
    internal/query/node.go — :92-96 'TS's GENERATED_PATTERNS ported VERBATIM', :122-127, :145-148 divergences
  </read_first>
  <action>
    Rewrite each of the eight file-header sentences and their front claims on the project's own terms:

    1. The "ports ... TS CodeGraph 1.3.1's" headers: replace the "ports the TS X's ..." phrase with a behavioral statement of what the file implements, e.g. expand.go's "ports the explore walk" becomes "implements the explore walk", scoring.go "implements the H1/H2 scoring scheme", tokenize.go "implements the camelCase/PascalCase tokenization scheme (H1/H2)", and so on for each of the six+ headers. Keep the paragraph's technical content (what the algorithm does, its inputs and ordering); remove the attribution "from TS CodeGraph 1.3.1" and any 'ported verbatim' claims (use "implements" or "derives from the project's documented design" — do NOT invent a non-existent internal source; if no such source exists, use a direct behavioral description, review-MEDIUM).
    2. Body claims "ported verbatim from TS": reword to describe behavior or the in-repo arrangement. If a literal algorithm was moved from an existing internal package (e.g. the GENERATED_PATTERNS constant list), keep the constant list (behavior) but reword the comment attribution to own-terms ("captured as a constant list in this file's generated-file taxonomy" — only if the taxonomy truly exists). Reserve "verbatim" for genuinely literal byte-transcription contexts and delete it elsewhere.
    3. "for parity"/"kept for parity": neutralize to "to keep the same documented shape" / "preserving the documented behavior".
    4. review-H3 rows (verified present in the tree): explore.go:111 ("not TS's exact step function") and explore.go:211 ("wires the full TS-parity explore pipeline in TS's own stage order") become own-terms stage-order descriptions; engine.go:179 ("Absolutizing here mirrors TS's own ...") and worktree_notice.go:43 ("matching TS's ...") become neutral behavioral notes; files.go:25/:97-99 ("matching TS 1.3.1's files --filter" / "TS's own implementation") become documented filter-semantics notes. Handle these five files (engine.go, explore.go, files.go, worktree_notice.go, render_status_test.go — the last one in task 2) in this task or task 2, and record each reason.
    5. The status.go:44-47 long historical note: LEAVE its content verbatim (past-tense history: the D-05 suppression and the retired Compatibility constraint) — record a D-02 KEEP reason in the SUMMARY. Do not delete or reword history.
    Do not change any function body, export, constant, or golden-listing code. Comments + doc-strings only.
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -n -e "ports CodeGraph" -e "ported VERBATIM" -e "ported verbatim" -e "matches CodeGraph 1\.3\.1" -e "TS-parity explore" -e "mirrors TS" -e "matching TS" internal/query/expand.go internal/query/explore_gate.go internal/query/explore.go internal/query/engine.go internal/query/files.go internal/query/worktree_notice.go internal/query/gather.go internal/query/scoring.go internal/query/seeding.go internal/query/tokenize.go internal/query/rwr.go internal/query/node.go | wc -l | tr -d " "); echo "flagged phrases left in the flagship files: $n"; test "$n" = "0"; go test -count=1 ./internal/query/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - the phase-grep over the twelve flagship files returns zero for `ports CodeGraph|ported VERBATIM|ported verbatim|matches CodeGraph 1.3.1|TS-parity explore|mirrors TS|matching TS` (each R-row of §4.1 AND the review-H3 rows handled)
    - each header now opens with "implements"/"provides" or an own-terms behavioral description naming its real job
    - `node.go` keeps the generated-file behavior but its comment no longer claims a byte-for-byte TS port
    - `status.go:44` KEEP row unchanged (the history note and its reason recorded)
    - `go test -count=1 ./internal/query/...` green
  </acceptance_criteria>
  <done>The flagship query headers read entirely on own terms — every port/verbatim/TS comparison claim resolved (incl. the review-H3 rows in engine/explore/files/worktree_notice); the status.go past-tense history note is preserved as a recorded KEEP; query tests green.</done>
</task>

<task type="auto">
  <name>The renderers, the tail files, and the stale harness renames — render_status/render_results/render_markdown + search/validate/status test rows</name>
  <files>internal/query/render_results.go, internal/query/render_status.go, internal/query/render_status_test.go, internal/query/render_markdown.go, internal/query/search.go, internal/query/search_test.go, internal/query/validate.go, internal/query/status.go, internal/query/files_status_test.go, internal/query/render_markdown_test.go</files>
  <read_first>
    internal/query/render_status.go — the 'ported from mcp/tools.js ~3890-3945' and ':211-212' claims; each :85/:163 'TS's padEnd' note, :225-278 the not-ported comparisons
    internal/query/render_markdown.go — the :58/:139/:155/:162/:269 references to 'TS's' renderNodeSection opts and the 'synthetic-parity "Validate"' case reference at :170
    internal/query/render_results.go — :9/:12 the golden-parity harness / stale `golden_parity_test.go` citation
    internal/query/search.go — :192-194 'camelCase keys matching the TS capture... TS-only boolean concepts ... literal false to keep JSON shape parity'; :436-441 (in search_test.go, the 'missing TS-only bool key' row)
    internal/query/validate.go — :44 SURF-01 'matches TS 1.3.1', :61/:83 TS-parity defaults
    internal/query/files_status_test.go — :554 'parity-stable', :569 'matching TS's own --json'
    internal/query/render_markdown_test.go — :236 'TS-parity tokenizers (H1/H2)'
    internal/query/render_status_test.go — :132 'not TS's string' comment and :143 the TS-backend assertion message (review H3, VERIFIED present)
  </read_first>
  <action>
    Work the second half of §4.1:

    1. `render_status.go`/`render_results.go`: reword the "ported from mcp/tools.js ~3890-3945" claims to "built to the documented CLI shape" (the pipelines' output is right with other status renderers), neutralize every "TS's padEnd/MJ/bold shows" line to "the padEnd/MJ style used by the documented output", and at render_results.go:9-13 replace "golden parity harness" -> "the golden suite" and the stale `golden_parity_test.go` name at :12 -> the current file name `behavioral_csharp_test.go`-ish sentence or a short "the behavioral golden suite" (it is a stale path per constraint 4).
    2. render_markdown.go:170 : "the synthetic-parity 'Validate' case" becomes "the behavioral corpus's 'Validate' case" (the corpus is now named behavioral, D-03 / Phase-2 rename); the other TS-behavior eyes at render_markdown.go :58/:139/:155/:162 are reworded as behavioral descriptions.
    3. search.go + search_test.go: at search.go :192-194 the "TS capture..." naming becomes "documented JSON key/camelCase" and the TS-only keyword is dropped; search_test.go:436-441 the 'missing TS-only bool key (D-05 shape parity)' text becomes 'missing JSON bool key (output shape contract)' — the test harness now says "shape contract", not "parity".
    4. validate.go: 'matches TS 1.3.1's/sur name' -> "is the documented default"; the two TS-parity defaults (affected) row.
    5. files_status_test.go:554 : 'golden JSON shape stays parity-stable' -> 'golden JSON shape stays stable under the golden suite'; :569 : "matching TS's own --json which... " -> "the JSON derives `languages` from this map and discards counts (the project's own shape)".
    6. render_markdown_test.go:236 'TS-parity tokenizers (H1/H2)' -> 'the H1/H2 tokenizer spec' (this file name does not carry a harness id).
    7. render_status_test.go :132-143 (review H3) — the test-description and the failure message say 'not TS's string' / 'contains TS's hardcoded backend string'; reword the COMMENT and the MESSAGE STRINGS to own-terms ('not the expected backend string' / 'contains the hardcoded backend string') WITHOUT changing which assertion runs or which sink compares. The assertion behavior (Go backend value, not any other string) stays identical.
    The exempt rows: status.go:44 remains KEEP (history). Files' imports + code unchanged.
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -n -e "TS CodeGraph" -e "TS's" -e "matching TS" -e "shape parity" -e "golden-parity harness" -e "golden_parity_test" internal/query/render_results.go internal/query/render_status.go internal/query/render_status_test.go internal/query/render_markdown.go internal/query/search.go internal/query/search_test.go internal/query/files_status_test.go internal/query/validate.go | wc -l | tr -d " "); echo "compare-text rows left: $n"; test "$n" = "0"; go test -count=1 ./internal/query/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - the phase-grep over the renderers/search/validate/test files returns zero for `TS CodeGraph|TS's|matching TS|shape parity|golden-parity harness|golden_parity_test` (each R-row plus render_status_test.go:132/:143 handled — review H3)
    - render_results no longer cross-references `golden_parity_test.go` (stale filename gone)
    - search shape rows mention "JSON shape / suite" not "parity"
    - `go test -count=1 ./internal/query/...` green
  </acceptance_criteria>
  <done>The renderers/search/validate/status rows all read on the project's own terms (incl. render_status_test:132/143); there are no stale golden-harness names citing deleted files; query tests pass.</done>
</task>

<task type="auto">
  <name>The identifier and matrix renames — syntheticParity*, behavioral_test.go, matrix.go golden-</name>
  <files>internal/query/explore_test.go, testdata/golden/behavioral_test.go, internal/indexer/capability/matrix.go, internal/indexer/capability/matrix_test.go</files>
  <read_first>
    internal/query/explore_test.go — copySyntheticParityFixture (:39) and syntheticParityEngine (:78) and their callers / comments
    testdata/golden/behavioral_test.go — :237-239 the syntheticParitySrc function and the five call sites :693,:858,:1267,:1310,:1311
    internal/indexer/capability/matrix.go — :18,:32 golden-parity comments
    internal/indexer/capability/matrix_test.go — :217,:221 golden-parity comments
  </read_first>
  <action>
    Perform the identifier renames (D-09, D-03) and the matrix comment rewrites in one commit:

    1. Rename the synthetic identifier family to behavioral names in the same task (D-09, Phase-2 behavioral vocabulary):
       - `copySyntheticParityFixture` -> `copyBehavioralFixture` (definition :39 + every call site :81/:98/:121/:144/:167/:175 in internal/query/explore_test.go; its "synthetic-parity corpus" comment rows -> "behavioral corpus")
       - `syntheticParityEngine` -> `behavioralEngine` (definition :78; all uses :98/:121/:167/:173)
       - `syntheticParitySrc` -> `behavioralCorpusSrc` (testdata/golden/behavioral_test.go:239; the 5 call sites :693/:858/:1267/:1310/:1311)
    2. capability/matrix.go + matrix_test.go:
       - matrix.go :18/:32 "a corresponding golden-parity test" -> "a corresponding behavioral golden fixture" (golden-parity is the retired harness name; the testdata/golden suite is now behavioral)
       - matrix_test.go :217 "green golden-parity test" -> "green behavioral golden test"; :221 "golden-parity harness" -> "golden harness"
    3. Do NOT change any doc table VALUES or Gaps bullets (matrix_test.go parses the doc verbatim).
   4. Check `rg -n "syntheticParity" internal/ testdata/` returns zero at the end. Note: the corpus source files (corpus/behavioral/src/**) may still carry 'synthetic-parity' comments — those are corpus content owned by plan 05-06 (D-07); do not touch them in this task.
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -c "syntheticParity" internal/ testdata/ | awk -F: "{s+=$2} END {print s+0}"); echo "syntheticParity references: $n"; test "$n" = "0"; go test -count=1 ./internal/query/... >/dev/null && go test -count=1 ./internal/indexer/capability/... >/dev/null && go test -count=1 ./testdata/golden/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'syntheticParity' internal/ testdata/` returns 0 (the D-09 renames landed)
    - call sites compile (go test on the three packages pass)
    - matrix.go/matrix_test.go no longer contain "golden-parity" as a harness name
    - the three capability-matrix DOC cells are byte-identical to HEAD (no value drift)
  </acceptance_criteria>
  <done>The syntheticParity identifier family is fully renamed to behavioral names; the matrix "golden-parity" comments say behavioral-golden/fixture meanings; query, capability, and golden packages test green; the code's own identifiers and the golden parser proper match the Phase-2 rename family.</done>
</task>

</tasks>

## Removed / edited words (captured here for the SUMMARY)

Expression families changed in this plan: "ports X TS" headers, "ported VERBATIM", "ported verbatim", "TS's", "matches TS", "for parity", "shape parity", "golden-parity", "golden_parity_test.go" (stale), "syntheticParity*" identifier strings, "the synthetic-P... corpus" pointer comments.

KEEP families (recorded with reason in the SUMMARY):
- status.go :44 historical note ("the Compatibility constraint was retired...") — past-tense history, product truth
- traverse.go:239 "iteration-order contract" — dataflow term
- render_markdown.go TS-body rows reworded to behavioral descriptions; where an actual output contract difference remains, the data term stays — no comparison sense survives
- matrix.go "corresponding golden-parity test" → "behavioral golden test"; doc values untouched
No work-item regex; each change is one census row, hand-edited, recorded in the SUMMARY "reason" column.

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment ↔ identifiers in test files | renaming an identifier must rename it everywhere (call sites), or the package tests break; renamed-in-noise (comments) that never targets call sites would go untested |
| matrix golden comments ↔ matrix_test.go | the doc cell values must match the Go descriptor byte-for-byte; only the code comments change |
| golden corpus files | the two corpus/*.json under D-07 are re-freeze targets owned by plan 05-06; this plan does not re-freeze them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------------|-----------------|
| T-05-13 | Tampering | internal/query sweep (CODE-01) | high | mitigate | census-driven term-by-term (never a find-replace); each R-row lands with a recorded reason; the final `rg` over the dedicated phrase families expects zero across the twelve flagship files + task-2 renderers — review H3 rows are in the file set |
| T-05-14 | Tampering | synthetic rename dropping a call site | high | mitigate | the rename edits the definition and every call site in the same commit; `go test -count=1 ./internal/query/... ./internal/indexer/capability/... ./testdata/golden/...` runs and `rg "syntheticParity" internal/ testdata/` must be zero |
| T-05-15 | Tampering | matrix golden-parity comment / DOC-cell drift | medium | mitigate | only code comments edited; verification runs `go test -count=1 ./internal/indexer/capability/...` (the doc-byte compare) |
| T-05-16 | DoS | the sweep de-listing a supported language | high | mitigate | the actions are file-by-file hand edits (D-02); tsextract/language-registry/capability-matrix are never de-listed; query tests green |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | no new packages (comment/identifier edits only), the package gate stays N/A |

<verification>
- `go test -count=1 ./internal/query/... ./internal/indexer/capability/... ./testdata/golden/...` green
- per-term zero on the sweep families over the flagships + renderers (review H3's engine/explore/files/worktree_notice/render_status_test rows included)
- `rg -n 'syntheticParity' internal/ testdata/` — zero
- A3 retained-use inventory gate (phase-wide, run after tasks 1-3): `rg -n -e "TS" -e "parity" -e "port" -e "original" -e "upstream" internal/query/` and CLASSIFY every hit as IMPORTANT (tsextract = indexed-language, status.go:44 history, dataflow terms) or REMAINING (framing) in the SUMMARY; any REMAINING hit beyond the KEEP list is a census miss to fix in this plan, not an acceptable residue. This proves "no comparison framing in internal/query" is positively verified, not asserted from absent pattern greps (review actionable 3).
- `go test -count=1 ./...` green (after 05-01 delivers a migration-free tree)
</verification>

<success_criteria>
- Code and test comments across internal/query (incl. the five review-H3 files), its renderers, and the capability matrix carry zero comparison framing; the retained-use inventory classifies every remaining TS/parity/port/original/upstream row with a reason
- The identifiers use behavioral names (both fixture files); `rg 'syntheticParity' internal/ testdata/` empty
- Stale golden_parity filename rows are gone, pointing to the real suite
- tsextract/language-registry/capability-matrix (product surface) untouched, TypeScript-the-indexed-language rows in those files unchanged
</success_criteria>

Reversibility notes: comment/identifier edits are reversible (D-01/D-09's own reversibility: reversible). The synthetic identifier renames, the matrix comment words, and the status notes are commit-rollback-able edges; the frozen-corpora files are explicitly NOT a part of this plan, so no re-freeze risk here.

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-04-SUMMARY.md` when done
</output>