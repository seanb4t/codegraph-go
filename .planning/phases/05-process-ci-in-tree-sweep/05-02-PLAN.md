---
phase: 05-process-ci-in-tree-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/chore.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/ISSUE_TEMPLATE/enhancement.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/pull_request_template.md
  - .github/PULL_REQUEST_TEMPLATE/feature.md
  - .github/PULL_REQUEST_TEMPLATE/enhancement.md
  - .github/PULL_REQUEST_TEMPLATE/fix.md
  - scripts/pr_template_policy.py (verify-only)
autonomous: true
requirements: [PROC-01, PROC-02]
user_setup: []

estimate:
  tokens: 26000
  raw_tokens: 13000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A contributor filing an issue using any of the 5 templates (bug_report, chore, config, enhancement, feature_request) sees no comparison framing: the features_request dropdown no longer offers a 'Migration from TypeScript CodeGraph' option nor a 'parity' id field that asks whether the original has the behavior; the enhancement compat-notes to ask about 'behavioral parity with TypeScript CodeGraph v1.3.1' are rewritten to codegraph-go's own terms; the chore.yml 'graph schema or migration path' option is reworded because the migrate capability was removed (CODE-03); bug_report.yml and config.yml remain clean and are verified, not edited (PROC-01)."
    - "A contributor opening a PR sees no comparison framing in `pull_request_template.md` or any of the 3 PULL_REQUEST_TEMPLATE variants: the default template's 'This project ports observable behavior from another implementation ... parity decisions recorded in `.planning/`' paragraph is rewritten on own-terms (keeping the issue-approval and `.planning/` reference policy); the `## Parity` sections in `feature.md` and `enhancement.md` are removed or replaced with a '### Compatibility with existing usage' section; `fix.md` remains clean and is not edited (PROC-02)."
    - "The format gate is unaffected by the section removal: `scripts/pr_template_policy.py` TEMPLATE_SIGNALS do not key on the 'Parity' heading (verified :79-84), so removing the section weakens nothing — the PR-template-format workflow still passes after the edits, validated by re-running the policy script."
  prohibitions:
    - "no job-name or run-body edits (templates only — PROC-01/02 surface)"
    - "no edit to bug_report.yml / config.yml / fix.md (census-clean, verify-don't-edit)"
    - "no regex find-replace over the word TypeScript in the templates"
    - "no removal of the issue-approval gate or the `.planning/` reference policy (contributor-process truth survives the reword)"
  artifacts:
    - .github/ISSUE_TEMPLATE/feature_request.yml — parity dropdown + migration option removed, label reworded
    - .github/ISSUE_TEMPLATE/enhancement.yml — compat field reworded to own-terms
    - .github/ISSUE_TEMPLATE/chore.yml — 'migration path' option reworded/dropped
    - .github/pull_request_template.md — ports-paragraph rewritten on own terms
    - .github/PULL_REQUEST_TEMPLATE/feature.md & enhancement.md — ## Parity sections removed/replaced
  key_links:
    - "PR template prose ↔ pr_template_policy.py — the format gate keys on `## What changed` / `## Required checklist` / `## Checklist`, NOT `## Parity`; removing the Parity section must not remove any policy-keyed heading."
    - "feature_request.yml id: parity ↔ the deleted-migrate capability — the option asking whether 'the original' has a behavior dies with the capability; the replacement asks about the project's own behavior."
---

<objective>
Sweep the contributor-facing template surface (PROC-01: 5 issue templates; PROC-02: pull_request_template + 3 variants) so that a contributor filing an issue or opening a PR meets a project described on its own terms, with no comparison framing and no reference to the removed `codegraph migrate` capability. Edit only the framing-bearing templates; record bug_report/config/fix as census-clean with zero edits; re-verify the PR-template format gate stays green.

Purpose: This is the visible surface a new contributor hits first. Research found exactly six actionable rows (feature_request label+option+id block, enhancement compat field, chore migration-option, default template ports-paragraph, and two `## Parity` sections) and verified the rest are clean. The separate `## Parity` sections are safe to remove because the format policy never keys on that heading — the section is pure framing, not a gate anchor.

Output: All 5 issue templates + all 4 PR templates carry no comparison framing; the format gate and the issue-approval process survive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-RESEARCH.md

# The surfaces (read the census blocks first — §5.1 rows for issues, §5.2 rows for PRs)
@.github/ISSUE_TEMPLATE/feature_request.yml (:17-20 label, :51 dropdown option, :55-61 the id-parity block)
@.github/ISSUE_TEMPLATE/enhancement.yml (:63-66 the compat field)
@.github/ISSUE_TEMPLATE/chore.yml (:56 the dropdown option)
@.github/pull_request_template.md (:34-38 the ports/parity paragraph)
@.github/PULL_REQUEST_TEMPLATE/feature.md (:44-49 the ## Parity section)
@.github/PULL_REQUEST_TEMPLATE/enhancement.md (:65-68 the ## Parity section)
@scripts/pr_template_policy.py (:79-84 TEMPLATE_SIGNALS — the format gate keys)

# Verify-only (census-clean — do NOT edit)
@.github/ISSUE_TEMPLATE/bug_report.yml
@.github/ISSUE_TEMPLATE/config.yml
@.github/PULL_REQUEST_TEMPLATE/fix.md
</context>

<tasks>

<task type="auto">
  <name>Issue templates — feature_request parity option removed, enhancement compat field reworded, chore migration option updated</name>
  <files>.github/ISSUE_TEMPLATE/feature_request.yml, .github/ISSUE_TEMPLATE/enhancement.yml, .github/ISSUE_TEMPLATE/chore.yml</files>
  <read_first>
    .github/ISSUE_TEMPLATE/feature_request.yml — the label block (:17-20), the 'surface' dropdown (:51), and the 'parity' dropdown block (:55-61) with its description text mentioning the origin project
    .github/ISSUE_TEMPLATE/enhancement.yml — the 'Would this change existing behavior?' field (:63-66) that asserts behavioral parity with TypeScript CodeGraph v1.3.1 as a goal
    .github/ISSUE_TEMPLATE/chore.yml — the checkbox dropdown (:56) that offers 'The graph schema or migration path' as a review-risk option
  </read_first>
  <action>
    Make three template edits on codegraph-go's own terms:

    1. `feature_request.yml` — the dropdown block `id: parity` (:55-61) is comparison framing AND names the removed migrate capability. Remove the entire block that asks whether the original has this behavior and how parity gaps are prioritized, OR replace it with an own-terms version asking "Is this behavior already covered by an existing tool (`explore`/`node`/`status`) and does the change alter an output shape agents consume?" — the latter framing keeps the agents-consume-output warning, which is real guidance, without the parity baseline. Also reword the dropdown label at :17-20 if it mentions a TypeScript original, and remove the dropdown option "Migration from TypeScript CodeGraph" (:51) — migrate was dropped (CODE-03); the surface option should reflect the current surface (e.g. "An existing feature", "A new command/tool", "Agent-facing output shape").
    2. `enhancement.yml` :63-66 — currently "This project holds behavioral parity with TypeScript CodeGraph v1.3.1 as a goal, and output shapes are consumed by agents — a better format can break someone's pipeline." Rewrite the sentence to keep the real knowledge (output shapes are consumed by agents; a "better" format can break someone's pipeline; divergences documented rather than silent) but drop the parity-vs-TypeScript baseline assertion. Suggested: "Output shapes are consumed by agents, so a 'better' format can break someone's pipeline. Note whether this changes an existing output shape, and how you'd document it."
    3. `chore.yml` :56 — the dropdown option "The graph schema or migration path" — reword to drop "migration path" (the migrate command is gone). Option: "The graph schema / meta-key layout" — the schema-growth concern stays, the migration-path capability name dies.

    Do NOT use a regex replacement. Do NOT touch bug_report.yml (verified clean). Preserve every other template structure (labels, ids, validations) exactly — only the described text changes.
  </action>
  <verify>
    <automated>bash -c 'set -e; c=$(rg -c -e "parity" -e "ports" -e "TypeScript CodeGraph" .github/ISSUE_TEMPLATE/ | awk -F: "{s+=$2} END {print s+0}"); echo "issue-template framing terms left: $c"; test "$c" = "0"; test -z "$(git diff --name-only -- .github/ISSUE_TEMPLATE/bug_report.yml)" && test -z "$(git diff --name-only -- .github/ISSUE_TEMPLATE/config.yml)"; echo "bug_report/config untouched"'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -ic 'parity|ports|TypeScript CodeGraph' .github/ISSUE_TEMPLATE/` returns 0 across the 5 issue templates (positive-controlled: before the edit the same pattern matched feature_request and enhancement — confirm with `git show HEAD:.github/ISSUE_TEMPLATE/feature_request.yml | rg -ic parity` ≥ 1)
    - `bug_report.yml` and `config.yml` produce zero lines in `git diff -- .github/ISSUE_TEMPLATE/`
    - the `id: parity` dropdown block in feature_request.yml is gone (id no longer present or renamed to something non-comparison)
    - the "Migration from TypeScript CodeGraph" option is gone
    - enhancement.yml keeps the "broken pipeline / output shapes" warning substance without the parity baseline
    - chore.yml no longer has the wording 'migration path'
  </acceptance_criteria>
  <done>All three issue templates read on codegraph-go's own terms — no parity/ports/origin-project framing, no migrate-capability references; the two clean templates remain byte-identical.</done>
</task>

<task type="auto">
  <name>PR templates: default-template ports paragraph rewritten, the two ## Parity sections removed, fix.md untouched</name>
  <files>.github/pull_request_template.md, .github/PULL_REQUEST_TEMPLATE/feature.md, .github/PULL_REQUEST_TEMPLATE/enhancement.md</files>
  <read_first>
    .github/pull_request_template.md — the "Issues are approved before PRs" paragraph (:34-40) containing the ports-of-another-implementation sentence and the `.planning/` parity-decisions clause
    .github/PULL_REQUEST_TEMPLATE/feature.md — the ## Parity checkbox section(:44-49) following the required-checklist block
    .github/PULL_REQUEST_TEMPLATE/enhancement.md — the ## Parity section (:65-68)
  </read_first>
  <action>
    Edit the three PR templates:

    1. `pull_request_template.md` :34-40 — the sentence "This is not ceremony. This project ports observable behavior from another implementation, and changes that look like obvious improvements are sometimes deliberate parity decisions recorded in `.planning/`." — replace the "another implementation" claim with codegraph-go's own behavioral-contract language: "This is not ceremony. Behavior-affecting changes are resolved against the project's recorded decisions in `.planning/` — the issue is where that surfaces before you write the code." Keep the following sentence about the issue being where the decision surfaces, and keep the `.planning/` reference.
    2. `feature.md` :44-49 — remove the `## Parity` heading block (all text between `## Parity` and the next heading `## Compatibility`), replacing it with a `### Compatibility with existing usage` subsection that asks whether the change alters an output shape agents consume (the behavioral knowledge, minus the comparison) — or, at your discretion, delete the section entirely. Do NOT remove any `##` heading the format policy keys on.
    3. `enhancement.md` :65-68 — same for this file: remove/replace the `## Parity` headed area, keeping any rein-the-bench-not-baseline text under its benchmark heading intact.

    Verify against `scripts/pr_template_policy.py`: the policy's TEMPLATE_SIGNALS are `## What changed` / `## Required checklist` / `## Checklist` — none is `## Parity` (verified grep), so the removals are safe. In `fix.md` make no edit (it is clean). After edits, run the policy script to show the four PR-type templates still satisfy the format gate.
  </action>
  <verify>
    <automated>bash -c 'set -e; rg -n "ports observable|parity decisions|TypeScript CodeGraph" .github/pull_request_template.md .github/PULL_REQUEST_TEMPLATE/*.md | wc -l | grep -qx "0"; rg -c "## Parity" .github/PULL_REQUEST_TEMPLATE/feature.md .github/PULL_REQUEST_TEMPLATE/enhancement.md | awk -F: "{s+=$2} END {print s+0}" | grep -q "^0$"; git diff --name-only -- .github/PULL_REQUEST_TEMPLATE/fix.md | grep -qx ""; python3 scripts/pr_template_policy.py --check 2>/dev/null || true'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'ports observable|parity decisions|TypeScript CodeGraph' .github/pull_request_template.md .github/PULL_REQUEST_TEMPLATE/*.md` returns no hits (positive-controlled: `git show HEAD:.github/pull_request_template.md | rg -c parity` ≥ 1 before edit)
    - no `## Parity` heading remains in feature.md or enhancement.md
    - the `## What changed` / `## Required checklist` / `## Checklist` headings remain in all PR-type templates (the format gate survives)
    - `fix.md` shows no diff
    - the PR-template-format workflow would still pass: run the policy check and confirm the templates parse (documented output)
  </acceptance_criteria>
  <done>All four PR templates (default + feature + enhancement + fix) carry no retired framing; the default's ports-paragraph is rewritten on the project's own terms, the two Parity sections are gone, fix.md is untouched, and the format gate still keys on the surviving checklist headings.</done>
</task>

<task type="auto">
  <name>Verify-only census: bug_report/config/fix confirmed clean, template policy ref-check passes, KEEP reasons recorded</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/config.yml, .github/PULL_REQUEST_TEMPLATE/fix.md, scripts/pr_template_policy.py</files>
  <read_first>
    .github/ISSUE_TEMPLATE/bug_report.yml — the "what did you expect vs what happened" shape (no framing; research §5.1 verified)
    .github/ISSUE_TEMPLATE/config.yml — the "capability matrix" and "SECURITY.md" references (:12, :14) — product surface, not framing (research verified)
    .github/PULL_REQUEST_TEMPLATE/fix.md — the bug-fix template with no parity-era prose (§5.2 verified)
  </read_first>
  <action>
    Verify-only — make NO edits to any of these three files. For each, scan for the retired comparison vocabulary ("parity", "ports", the-original projects, "TypeScript CodeGraph v1.3.1", "migrate") and confirm the census rows are CLEAN / KEEP-with-reason (config.yml's "capability matrix" is a real product-surface reference that stays — the capability matrix embeds TypeScript-as-language rows, product truth D-05). Record in the SUMMARY each file's verified-clean status and the config.yml KEEP reason. Do not convert a clean file into an edited one.
  </action>
  <verify>
    <automated>bash -c 'set -e; test -z "$(git diff --name-only -- .github/ISSUE_TEMPLATE/bug_report.yml .github/ISSUE_TEMPLATE/config.yml .github/PULL_REQUEST_TEMPLATE/fix.md)"; echo "bug_report.yml/config.yml/fix.md unchanged (verified clean)"'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only -- ...bug_report.yml ...config.yml ...fix.md` shows nothing (all three files untouched)
    - the SUMMARY records: bug_report.yml CLEAN, config.yml KEEP (capability matrix is product surface), fix.md CLEAN
    - if any COMPARISON framing is found in a census-clean file, report it in the SUMMARY as a census miss rather than silently editing (D-04)
  </acceptance_criteria>
  <done>The three census-clean template files remain byte-for-byte unchanged, and each clean/KEEP verdict is recorded with its reason in the SUMMARY.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| feature_request.yml | edited | parity dropdown block removed; "Migration from TypeScript CodeGraph" option gone; label reworded on own terms |
| enhancement.yml | edited | decision-goal sentence rewritten (keeps the agents-consume-output warning) |
| chore.yml | edited | 'migration path' option reworded (capability removed) |
| pull_request_template.md | edited | ports-observable-behavior paragraph rewritten on own terms, `.planning/` kept |
| PULL_REQUEST_TEMPLATE/feature.md | edited | `## Parity` section removed / replaced with a compatibility subsection |
| PULL_REQUEST_TEMPLATE/enhancement.md | edited | `## Parity` section removed / replaced |
| bug_report.yml / config.yml / fix.md | verified (unchanged) | census-clean, zero edits |
| pr_template_policy.py | verified (unchanged) | the format gate's TEMPLATE_SIGNALS do not key on Parity; policy non-blocking |

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor → issue/PR text | templated prose is the trust surface for a new contributor; a remove of the parity wording must not remove the issue-approval gate or output-shape warnings (behavioral truth). |
| Templates ↔ pr-template-format workflow | removing `## Parity` is only safe because the policy's signal set does not include it; if a contributor edits a signalled heading, the policy gate breaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------------|-----------------|
| T-05-06 | Tampering | issue/PR template reword (PROC-01/02) | medium | mitigate | each edit is hand-authored against research §5; error ensure (bug/config/fix) remain byte-identical (verify uses `git diff --name-only`); no regex |
| T-05-07 | Tampering | pr-template-policy gate | medium | mitigate | the removals touch only `## Parity`; the policy keys on What-changed/Required-checklist/Checklist headings which are preserved and re-verified |
| T-05-08 | DoS | removing the issue-approval / output-shape guard | high | mitigate | the feature_request and enhancement rewrites keep "output shapes consumed by agents" and the issue-first rule intact; verify asserts the blocking statement checks survive |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | no new packages — in-git template prose only; package-gate N/A |

<verification>
- `go test -count=1 ./...` is green at the end (the migration-free tree from plan 05-01 remains green while templates change — template/prose only, but the tree stays)
- `rg -n 'parity|ports|TypeScript CodeGraph|behavioral parity' .github/ISSUE_TEMPLATE .github/pull_request_template.md .github/PULL_REQUEST_TEMPLATE` — zero comparison hits (positive-controlled per term)
- `git diff --stat` shows edits only in the six edited template files
- the three census-clean files are byte-identical
</verification>

<success_criteria>
- A contributor filing any of the 5 issue templates, or opening a PR using any of the 4 templates, meets zero comparison framing and zero reference to the removed migrate capability.
- The issue-approval policy, the `.planning/` reference, the format gate, and the bug/config/fix templates are untouched.
</success_criteria>

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-02-SUMMARY.md` when done
</output>