---
phase: 05-process-ci-in-tree-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/chore.yml
  - .github/ISSUE_TEMPLATE/config.yml (verify-only)
  - .github/ISSUE_TEMPLATE/enhancement.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/pull_request_template.md
  - .github/PULL_REQUEST_TEMPLATE/feature.md
  - .github/PULL_REQUEST_TEMPLATE/enhancement.md
  - .github/PULL_REQUEST_TEMPLATE/fix.md (verify-only)
  - scripts/pr_template_policy.py (verify-only — no edit, drive it via env)
autonomous: true
requirements: [PROC-01, PROC-02]
user_setup: []

estimate:
  tokens: 26000
  raw_tokens: 13000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A contributor filing an issue using any of the 5 templates sees no comparison framing: the `bug_report.yml:77` install-method option `Migrated from TypeScript CodeGraph` is swept to an own-terms option (e.g. `Migrated from an older codegraph install`) — it is NOT clean, it carries PROC-01 framing and must be edited (review H1, verified bug_report.yml:77); the feature_request dropdown no longer offers the migrate-surface option, `feature_request.yml:55-61` parity id block is gone, the enhancement compat field's parity-vs-v1.3.1 goal is rewritten on own terms, chore.yml:56's 'graph schema or migration path' option is reworded (capability removed, D-06/PROC-01)."
    - "bug_report.yml is re-classified from census-clean to swept: it appears in `files_modified`, gets a task action, and NO plan keeps it byte-identical (review H1); config.yml is the only issue template that remains census-clean and byte-unchanged."
    - "A contributor opening a PR sees no comparison framing in `pull_request_template.md` or any of the 3 PULL_REQUEST_TEMPLATE variants: the default template's 'ports observable behavior from another implementation ... parity decisions recorded in `.planning/`' paragraph is rewritten on own-terms (keeping the issue-approval and `.planning/` reference policy); the `## Parity` sections in `feature.md` and `enhancement.md` are removed or replaced with a 'Compatibility with existing usage' section; `fix.md` remains clean and is not edited (D-06, PROC-02)."
    - "The format gate is unaffected by the section removal: `scripts/pr_template_policy.py` TEMPLATE_SIGNALS do not key on the 'Parity' heading (verified :79-84), so removing the section weakens nothing — the PR-template-format workflow still passes after the edits, validated by re-running the policy script."
  prohibitions:
    - "no job-name or run-body edits (templates only — PROC-01/02 surface)"
    - "no edit to config.yml / fix.md (the only census-clean surfaces; bug_report.yml is swept — H1 fix)"
    - "no regex find-replace over the word TypeScript in the templates"
    - "no removal of the issue-approval gate or the `.planning/` reference policy (contributor-process truth survives the reword)"
  artifacts:
    - .github/ISSUE_TEMPLATE/bug_report.yml (H1) — 'Migrated from TypeScript CodeGraph' install option swept to own terms or removed
    - .github/ISSUE_TEMPLATE/feature_request.yml — parity dropdown + migration option removed, label reworded
    - .github/ISSUE_TEMPLATE/enhancement.yml — compat field reworded to own-terms
    - .github/ISSUE_TEMPLATE/chore.yml — 'migration path' option reworded/dropped
    - .github/pull_request_template.md — ports-paragraph rewritten on own terms
    - .github/PULL_REQUEST_TEMPLATE/feature.md & enhancement.md — parity sections removed/replaced
    - config.yml, fix.md — verified byte-identical (the only census-clean surfaces)
  key_links:
    - "PR template prose ↔ pr_template_policy.py — the format gate keys on `## What changed` / `## Required checklist` / `## Checklist`, NOT `## Parity`; removing the Parity section must not remove any policy-keyed heading."
    - "feature_request.yml id: parity ↔ the deleted-migrate capability — the option asking whether 'the original' has a behavior dies with the capability; the replacement asks about the project's own behavior."
---

<objective>
Sweep the contributor-facing template surface (PROC-01: 5 issue templates; PROC-02: pull_request_template + 3 variants) so that a contributor filing an issue or opening a PR meets a project described on its own terms, with no comparison framing and no reference to the removed `codegraph migrate` capability. Edit the framing-bearing templates (bug_report, feature_request, enhancement, chore, the default PR template, feature.md, enhancement.md); record config.yml and fix.md as census-clean with zero edits; re-verify the PR-template format gate ENFORCINGLY (no `|| true` drop).

Purpose: This is the visible surface a new contributor hits first. The review (H1) found that `bug_report.yml:77` carries a `Migrated from TypeScript CodeGraph` install option the original census had missed — so bug_report is swept, not clean. Alongside it the feature_request label+option+id block, the enhancement compat field, the chore migration-option, the default-template ports-paragraph, and the two `## Parity` sections are removed. The separate `## Parity` sections are safe to remove because the format policy never keys on that heading, and the policy gate is invoked by driving the real env vars the script reads (PR_BODY/AUTHOR_ASSOCIATION/CHANGED_FILES), asserting its exit code.

Output: All 5 issue templates + all 4 PR templates carry no comparison framing; the format gate and the issue-approval process survive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-RESEARCH.md

# The surfaces (read the census blocks first — §5.1 rows for issues, §5.2 rows for PRs)
@.github/ISSUE_TEMPLATE/feature_request.yml (:17-20 label, :51 dropdown option, :55-61 the id-parity block)
@.github/ISSUE_TEMPLATE/enhancement.yml (:63-66 the compat field)
@.github/ISSUE_TEMPLATE/chore.yml (:56 the dropdown option)
@.github/pull_request_template.md (:34-38 the ports/parity paragraph)
@.github/PULL_REQUEST_TEMPLATE/feature.md (:44-49 the ## Parity section)
@.github/PULL_REQUEST_TEMPLATE/enhancement.md (:65-68 the ## Parity section)
@scripts/pr_template_policy.py (:79-84 TEMPLATE_SIGNALS — the format gate keys)

# Swept (bug_report carries the migrate option — H1)
@.github/ISSUE_TEMPLATE/bug_report.yml (:77 the install-method option 'Migrated from TypeScript CodeGraph')

# Verify-only (census-clean — do NOT edit)
@.github/ISSUE_TEMPLATE/config.yml
@.github/PULL_REQUEST_TEMPLATE/fix.md
</context>

<tasks>

<task type="auto">
  <name>Issue templates: bug_report migrate-option swept (H1), feature_request parity block removed, enhancement compat reworded, chore option updated</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml, .github/ISSUE_TEMPLATE/enhancement.yml, .github/ISSUE_TEMPLATE/chore.yml</files>
  <read_first>
    .github/ISSUE_TEMPLATE/bug_report.yml — the install-method dropdown at :70-80 whose option 'Migrated from TypeScript CodeGraph' (:77) references both the origin project and the removed capability (H1 — re-verified present)
    .github/ISSUE_TEMPLATE/feature_request.yml — the label block (:17-20), the 'surface' dropdown option (:49-53), and the 'parity' dropdown block (:55-61) with description text naming the origin project
    .github/ISSUE_TEMPLATE/enhancement.yml — the 'Would this change existing behavior?' field (:63-66) asserting behavioral parity with TypeScript CodeGraph v1.3.1 as a goal
    .github/ISSUE_TEMPLATE/chore.yml — the checkbox dropdown (:56-57) offering 'The graph schema or migration path' as a review-risk option
  </read_first>
  <action>
    Make four template edits on codegraph-go's own terms — bug_report included (review H1):

    1. `bug_report.yml` (:77) — the install-method option `Migrated from TypeScript CodeGraph` is both an origin-project reference and a disallowed migrate capability reference, so it is swept, not preserved: change the option to an own-terms equivalent, e.g. "Migrated from the `codegraph upgrade` path" if such an upgrade migration path actually exists, otherwise remove the option entirely and keep the remaining four options. bug_report.yml is NO LONGER census-clean; it must carry zero `TypeScript CodeGraph` occurrences after the edit.
    2. `feature_request.yml` — the dropdown block `id: parity` (:55-61) is comparison framing: remove the whole block that asks whether the original has this behavior / prioritizes parity gaps, or replace with an own-terms version asking "Is this behavior already covered by an existing tool (`explore`/`node`/`status`) and does the change alter an output shape an agent consumes?" (the agents-consume-output warning is real guidance). Also reword the dropdown label at :17-20 if it names a TypeScript original and remove the "Migration from TypeScript CodeGraph" option in the 'surface' dropdown (:51) — the migrate surface group died with CODE-03; replace with current-surface options ("An existing feature", "A new command/tool", "Agent-facing output shape").
    2. `enhancement.yml` :63-66 — currently "This project holds behavioral parity with TypeScript CodeGraph v1.3.1 as a goal, and output shapes are consumed by agents — a better format can break someone's pipeline." Rewrite the sentence to keep the real knowledge (output shapes are consumed by agents; a "better" format can break someone's pipeline; divergences documented rather than silent) but drop the parity-vs-TypeScript baseline assertion. Suggested: "Output shapes are consumed by agents, so a 'better' format can break someone's pipeline. Note whether this changes an existing output shape, and how you'd document it."
    3. `chore.yml` :56 — the dropdown option "The graph schema or migration path" — reword to drop "migration path" (the migrate command is gone). Option: "The graph schema / meta-key layout" — the schema-growth concern stays, the migration-path capability name dies.

    Do NOT use a regex replacement. Do NOT touch config.yml (verified clean). Preserve every other template structure (labels, ids, validations) exactly — only the described text changes. bug_report.yml IS edited in this task (migrate option swept in step 1).
  </action>
  <verify>
    <automated>bash -c 'set -e; c=$(rg -c -e "parity" -e "ports" -e "TypeScript CodeGraph" -e "Behavioral Parity" .github/ISSUE_TEMPLATE/ | awk -F: "{s+=$2} END {print s+0}"); echo "issue-template framing terms left: $c"; test "$c" = "0"; n=$(rg -c "Migrated from TypeScript" .github/ISSUE_TEMPLATE/bug_report.yml | awk -F: "{s+=$2} END {print s+0}"); echo "bug_report migrate-origin options left: $n"; test "$n" = "0"; test -z "$(git diff --name-only -- .github/ISSUE_TEMPLATE/config.yml)"; echo "config.yml untouched"'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -ic 'parity|ports|TypeScript CodeGraph' .github/ISSUE_TEMPLATE/` returns 0 across the 4 swept issue templates (positive-controlled: before the edit the pattern matches bug_report (`Migrated from TypeScript CodeGraph`), feature_request, enhancement — confirm with `git show HEAD:.github/ISSUE_TEMPLATE/bug_report.yml | rg -c "TypeScript CodeGraph"` ≥ 1)
    - `git diff -- .github/ISSUE_TEMPLATE/bug_report.yml` shows the install-method option swept (the `Migrated from TypeScript CodeGraph` string is gone)
    - `config.yml` produces zero lines in `git diff -- .github/ISSUE_TEMPLATE/` (the one truly clean issue template)
    - the `id: parity` dropdown block in feature_request.yml is gone
    - the "Migration from TypeScript CodeGraph" option is gone
    - enhancement.yml keeps the "broken pipeline / output shapes" warning substance without the parity baseline
    - chore.yml no longer has the wording 'migration path'
  </acceptance_criteria>
  <done>All four issue templates (bug_report, feature_request, enhancement, chore) read on codegraph-go's own terms — no parity/ports/origin-project framing and no migrate-capability references; config.yml remains byte-identical.</done>
</task>

<task type="auto">
  <name>PR templates: default-template ports paragraph rewritten, the two ## Parity sections removed, fix.md untouched</name>
  <files>.github/pull_request_template.md, .github/PULL_REQUEST_TEMPLATE/feature.md, .github/PULL_REQUEST_TEMPLATE/enhancement.md</files>
  <read_first>
    .github/pull_request_template.md — the "Issues are approved before PRs" paragraph (:34-40) containing the ports-of-another-implementation sentence and the `.planning/` parity-decisions clause
    .github/PULL_REQUEST_TEMPLATE/feature.md — the ## Parity checkbox section(:44-49) following the required-checklist block
    .github/PULL_REQUEST_TEMPLATE/enhancement.md — the ## Parity section (:65-68)
  </read_first>
  <action>
    Edit the three PR templates:

    1. `pull_request_template.md` :34-40 — the sentence "This is not ceremony. This project ports observable behavior from another implementation, and changes that look like obvious improvements are sometimes deliberate parity decisions recorded in `.planning/`." — replace the "another implementation" claim with codegraph-go's own behavioral-contract language: "This is not ceremony. Behavior-affecting changes are resolved against the project's recorded decisions in `.planning/` — the issue is where that surfaces before you write the code." Keep the following sentence about the issue being where the decision surfaces, and keep the `.planning/` reference.
    2. `feature.md` :44-49 — remove the `## Parity` heading block (all text between `## Parity` and the next heading `## Compatibility`), replacing it with a `### Compatibility with existing usage` subsection that asks whether the change alters an output shape agents consume (the behavioral knowledge, minus the comparison) — or delete the section entirely. Check the surrounding heading structure first so the replacement heading matches the existing hierarchy (the review LOW: pick one predetermined shape after inspecting each file). Do NOT remove any `##` heading the format policy keys on.
    3. `enhancement.md` :65-68 — same for this file: remove/replace the `## Parity` headed area, keeping any rein-the-bench-not-baseline text under its benchmark heading intact.

    Verify ENFORCINGLY against `scripts/pr_template_policy.py` (review A1: the script has NO `--check` flag — it reads `PR_BODY`/`AUTHOR_ASSOCIATION`/`CHANGED_FILES` from the environment and returns 0/1): for each of the three PR templates (default, feature, enhancement) run the script with `PR_BODY="$(cat <template body>)"` (plus AUTHOR_ASSOCIATION=OWNER and CHANGED_FILES pointing at the template's own files — a docs-only change, satisfying the EXEMPT_EXACT check) and assert exit 0 — WITHOUT `|| true` masking a failure. The policy's TEMPLATE_SIGNALS are `## What changed` / `## Required checklist` / `## Checklist` (scripts/pr_template_policy.py:79-84) — none is `## Parity`, so the removals are safe, but the gate must be proven by the real script, not assumed. In `fix.md` make no edit (it is clean).
  </action>
  <verify>
    <automated>bash -c 'set -e; rg -n "ports observable|parity decisions|TypeScript CodeGraph" .github/pull_request_template.md .github/PULL_REQUEST_TEMPLATE/*.md | wc -l | tr -d " " | grep -qx "0"; rg -c "## Parity" .github/PULL_REQUEST_TEMPLATE/feature.md .github/PULL_REQUEST_TEMPLATE/enhancement.md | awk -F: "{s+=$2} END {print s+0}" | grep -q "^0$"; test -z "$(git diff --name-only -- .github/PULL_REQUEST_TEMPLATE/fix.md)"; for t in pull_request_template.md PULL_REQUEST_TEMPLATE/feature.md PULL_REQUEST_TEMPLATE/enhancement.md; do PR_BODY="$(cat .github/$t)" AUTHOR_ASSOCIATION=OWNER CHANGED_FILES=$(printf "%.docs/x") python3 scripts/pr_template_policy.py >/dev/null 2>&1 || { echo "policy exit nonzero for $t"; exit 1; }; done; echo "policy gate green for all three templates (no || true)"'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'ports observable|parity|TypeScript CodeGraph' .github/pull_request_template.md .github/PULL_REQUEST_TEMPLATE/*.md` returns zero (positive-controlled: `git show HEAD:.github/pull_request_template.md | rg -c parity` ≥ 1 before edit)
    - no `## Parity` heading remains in feature.md or enhancement.md
    - the `## What changed` / `## Required checklist` / `## Checklist` headings remain in all PR-type templates
    - `fix.md` shows no diff, and the three edited templates each exit 0 from a `PR_BODY`-driven `python3 scripts/pr_template_policy.py` run (no `|| true`, no `--check` flag)
  </acceptance_criteria>
  <done>All four PR templates (default + feature + enhancement + fix) carry no retired framing; the default's ports-paragraph is rewritten on the project's own terms, the two Parity sections are gone, fix.md is untouched, and the format gate still keys on the surviving checklist headings and passes the real policy script run.</done>
</task>

<task type="auto">
  <name>Verify-only census: config.yml/fix.md confirmed clean; template-policy gate re-asserted; KEEP reasons recorded</name>
  <files>.github/ISSUE_TEMPLATE/config.yml, .github/PULL_REQUEST_TEMPLATE/fix.md, scripts/pr_template_policy.py</files>
  <read_first>
    .github/ISSUE_TEMPLATE/config.yml — the "capability matrix" and "SECURITY.md" references (:12, :14) — product surface, not framing (research verified)
    .github/PULL_REQUEST_TEMPLATE/fix.md — the bug-fix template with no parity-era prose (§5.2 verified)
  </read_first>
  <action>
    Verify-only — make NO edits to config.yml or fix.md (bug_report.yml is swept in task 1, NOT clean — review H1). For each of the two files, scan for the retired comparison vocabulary ("parity", "ports", the-original wording, "TypeScript CodeGraph v1.3.1", "migrate") and confirm the rows are CLEAN / KEEP-with-reason (config.yml's "capability matrix" is a real product-surface reference that stays — the capability matrix embeds TypeScript-as-language rows, product truth D-05). Record in the SUMMARY each file's verified-clean status and the config.yml KEEP reason. Then re-run the ENFORCING policy gate from task 2 once more (PR_BODY-driven, no `|| true`) as a phase-final cross-check that bug_report's swept option did not disturb the format gate.
  </action>
  <verify>
    <automated>bash -c 'set -e; test -z "$(git diff --name-only -- .github/ISSUE_TEMPLATE/config.yml .github/PULL_REQUEST_TEMPLATE/fix.md)"; for t in pull_request_template.md PULL_REQUEST_TEMPLATE/feature.md PULL_REQUEST_TEMPLATE/enhancement.md; do PR_BODY="$(cat .github/$t)" AUTHOR_ASSOCIATION=OWNER python3 scripts/pr_template_policy.py >/dev/null 2>&1 || { echo "policy exit nonzero for $t"; exit 1; }; done; echo "config.yml/fix.md unchanged; final policy gate green"'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only -- .github/ISSUE_TEMPLATE/config.yml .github/PULL_REQUEST_TEMPLATE/fix.md` shows nothing (both files untouched)
    - the SUMMARY records: config.yml KEEP (capability matrix is product surface), fix.md CLEAN — and bug_report.yml as SWEPT not clean
    - if any COMPARISON framing is found in config.yml or fix.md, report it in the SUMMARY as a census miss rather than silently editing (D-04)
    - `python3 scripts/pr_template_policy.py` (PR_BODY-driven, no `--check`, no `|| true`) exits 0 for all three edited PR templates — the codex team's PY policy validation point is enforced
  </acceptance_criteria>
  <done>The two census-clean template files (config.yml, fix.md) remain byte-for-byte unchanged, each verdict (clean/KEEP) is recorded with its reason, and the format gate's live policy exit code is enforced, not assumed.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| feature_request.yml | edited | parity dropdown block removed; "Migration from TypeScript CodeGraph" option gone; label reworded on own terms |
| enhancement.yml | edited | decision-goal sentence rewritten (keeps the agents-consume-output warning) |
| chore.yml | edited | 'migration path' option reworded (capability removed) |
| pull_request_template.md | edited | ports-observable-behavior paragraph rewritten on own terms, `.planning/` kept |
| PULL_REQUEST_TEMPLATE/feature.md | edited | `## Parity` section removed / replaced with a compatibility subsection |
| PULL_REQUEST_TEMPLATE/enhancement.md | edited | `## Parity` section removed / replaced |
| bug_report.yml | edited (H1) | the `Migrated from TypeScript CodeGraph` install option swept (no longer clean) |
| config.yml / fix.md | verified (unchanged) | the only census-clean surfaces (config.yml KEEP for the capability-matrix reference) |
| pr_template_policy.py | verified (unchanged) | the gate's TEMPLATE_SIGNALS do not key on Parity; the policy itself is run with PR_BODY-derived env and its exit code asserted (no `--check`, no `|| true`) |

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor → issue/PR text | templated prose is the trust surface for a new contributor; a remove of the parity wording must not remove the issue-approval gate or output-shape warnings (behavioral truth). |
| Templates ↔ pr-template-format workflow | removing `## Parity` is only safe because the policy's signal set does not include it; if a contributor edits a signalled heading, the policy gate breaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------------|-----------------|
| T-05-06 | Tampering | issue/PR template reword (PROC-01/02) | medium | mitigate | each edit is hand-authored against research §5 plus the H1 catch (bug_report:77); only config.yml/fix.md remain byte-identical (verify uses `git diff --name-only`); no regex |
| T-05-07 | Tampering | pr-template-policy gate | medium | mitigate | the removals touch only `## Parity`; the policy keys on What-changed/Required-checklist/Checklist headings which are preserved and re-verified |
| T-05-08 | DoS | removing the issue-approval / output-shape guard | high | mitigate | the feature_request and enhancement rewrites keep "output shapes consumed by agents" and the issue-first rule intact; verify asserts the blocking statement checks survive |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | no new packages — in-git template prose only; package-gate N/A |

<verification>
- `go test -count=1 ./...` is green at the end (the migration-free tree from plan 05-01 remains green while templates change — template/prose only, but the tree stays)
- `rg -n 'parity|ports|TypeScript CodeGraph|behavioral parity' .github/ISSUE_TEMPLATE .github/pull_request_template.md .github/PULL_REQUEST_TEMPLATE` — zero comparison hits (positive-controlled per term)
- `git diff --stat` shows edits only in the six edited template files (bug_report, feature_request, enhancement, chore, default PR, feature.md, enhancement.md)
- config.yml and fix.md are byte-identical after the sweep
- the PR-template policy gate exits 0 on each edited template with PR_BODY-driven env (no `|| true`)
</verification>

<success_criteria>
- A contributor filing any of the 5 issue templates, or opening a PR using any of the 4 templates, meets zero comparison framing and zero reference to the removed migrate capability — bug_report included.
- The issue-approval policy, the `.planning/` reference, and the format gate survive; only config.yml and fix.md are untouched.
</success_criteria>

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-02-SUMMARY.md` when done
</output>