---
phase: 05-process-ci-in-tree-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/require-issue-link.yml
  - .github/workflows/auto-close-unsolicited-prs.yml
  - .github/workflows/corpora.yml
  - .github/workflows/bench.yml
  - .github/workflows/ci.yml (verify-only)
  - .github/workflows/release.yml (verify-only)
  - .github/workflows/linux-cross-canary.yml (verify-only)
  - .github/workflows/post-release-verify.yml (verify-only)
  - .github/workflows/release-please.yml (verify-only)
autonomous: true
requirements: [PROC-03]
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Workflow job names, step names, and comments carry no retired comparison framing across the swept files (D-06, PROC-03): the inline-JS string in `require-issue-link.yml` (:125, about parity decisions) and the inline-JS string in `auto-close-unsolicited-prs.yml` (:91, 'This project ports observable behavior from another implementation ... deliberate parity decisions recorded in `.planning/`') are both reworded on codegraph-go's own terms (keeping each file's issue-first instruction and the `.planning/` reference where it serves process); the `corpora.yml` :266 step name `Run golden parity suite (testdata/golden)` becomes an own-terms name with the `run: task test:golden` body byte-identical."
    - "`bench.yml` comparison framing is fully swept — INCLUDING the job and step display names: the non-required job display name `head-to-head publish (PERF-01, non-blocking)` (:96, job `headtohead:`) and the step name `Run head-to-head benchmark` (:127) are renamed to own-terms names (`benchmark publish (PERF-01, non-blocking)` / `Run benchmark`) because PROC-03 requires JOB and step names to carry no retired framing and these names are NOT among the 7 required-check contexts bound to ruleset 20157557 (review H2, verified bench.yml:96/:127). The comparison capability itself (the runner and the installed reference binary) stays in the job because Phase 6 owns de-coupling — only the framing NAME is this phase's."
    - "The required-check binding protects EXACTLY the 7 names, not every job: the literal fixture in `internal/upgrade/taskfile_shape_test.go:36-51` (`test`, `govulncheck (DIST-03, blocking)`, `reproducibility (double-build hash-diff, DIST-04)`, `perf regression gate (PERF-02, INDX-06)`, `actionlint (workflow static analysis)`, `goreleaser check (config validation, DIST-01)`, `pr-title`) is byte-identical, and NO other INTERNAL job name was gratuitously renamed except the framing-bearing `bench.yml` headtohead job/step names — renaming one of the 7 required names would break branch protection (ruleset 20157557); renaming a free name that carries retired framing is MANDATORY under PROC-03."
    - "Every workflow still passes `actionlint` 1.7.12 (all ~14 files exit 0) and the workflow-shape guards still hold: `TestWorkflowRunBodiesInvokeTask` (binds the shell `run:` bodies) is green because no shell `run:` body changed, and `TestWorkflowRequiredChecks` is green because the required-check names are untouched. The two inline-JS changes live inside `with.script` action bodies (not YAML `run:` fields), so their syntax is validated by `node --check` (review A4) rather than by the run-body guard."
    - "The KEEP rows are preserved with recorded reasons in the SUMMARY: `release.yml` :65/:183 and `linux-cross-canary.yml` :26 name `TestGoreleaserPinParity` — an in-repo pin consistency guard, not comparison framing (D-01 scope exclusion); `release-please.yml` :26 'Ports docs/RELEASE-PROCEDURES' — an in-repo procedure-to-steps restatement, not origin framing; `post-release-verify.yml` (:220, :258) uses 'upstream' in the GitHub-Actions fork workflow trigger sense; `ci.yml` :189-191 uses 'upstream unmaintained' for a third-party advisory (sigstore/cosign), not the origin project."
  prohibitions:
    - "zero edits to the 7 required-check job names (ruleset 20157557 binding); framing-bearing FREE job-step names MUST be renamed (bench.yml head-to-head)"
    - "no changes to any shell `run:` body — step name edits, comments, and the two inline-JS literals inside action `with.script` bodies are rewritten BUT the JavaScript must still parse and behave identically (review A4: they are executable action scripts, not YAML run: fields; validate via a `node --check` of the embedded script)"
    - "no removal or de-coupling of the bench.yml comparison runner or any benchmark CAPABILITY step (Phase 6 removes the runner; Phase 5 only renames framing)"
    - "no regex sweep over the word TS (each change is hand-reworded)"
  artifacts:
    - .github/workflows/require-issue-link.yml — inline-JS message reworded at :125
    - .github/workflows/auto-close-unsolicited-prs.yml — inline-JS message reworded at :91
    - .github/workflows/corpora.yml — step name at :266 de-paritied
    - .github/workflows/bench.yml — comments at :4/:31/:45/:128-130/:510 reworded on own terms; light-touch step-name text update
  key_links:
    - "requiredCheckNames fixture (taskfile_shape_test.go:36-51) ↔ ruleset 20157557 job names — zero edits to those 7 names keeps the guard byte-identical; free framing-bearing names (bench head-to-head) are renamed, not preserved (review H2)"
    - "corpora.yml:266 step name ↔ TestWorkflowRunBodiesInvokeTask — the `run: task test:golden` body stays byte-identical; only the name changes"
    - "bench.yml :119/:121 step names ↔ Phase 6 de-coupling — the steps stay (the capability), the 'reference'/(comparing) words are swept in this phase only"
---

<objective>
Sweep the workflow files' job names, step names, comments, and inline-JS prose so every workflow passes `actionlint` and its own required status checks while carrying no retired comparison framing (PROC-03). The sweep is name-and-comment only: ZERO edits to the seven required-check job names (the required-check set bound by ruleset 20157557 and `internal/upgrade/taskfile_shape_test.go:36-51`) is the hard rule, and any FREE framing-bearing name — e.g. bench.yml's `head-to-head` job/step display names (review H2) — MUST be renamed instead of preserved; zero shell `run:`-body changes (bound by `TestWorkflowRunBodiesInvokeTask`), the two inline JS action bodies reworded within `with.script` and re-validated (A4). bench.yml's own de-coupling is explicitly out of scope (Phase 6 owns it); this plan touches only its framing.

Purpose: A contributor opening a PR meets the project's evidence only on its own terms. This plan kills the same "ports observable behavior … parity decisions" sentence in two CI script strings that the PR-template fix (05-02) removed from the template body — the CI twin of that fix — and re-frames the benchmark workflow without touching the benchmark machinery Phase 6 retires.

Output: All workflows pass `actionlint` and the workflow-shape guards; the retired framing words are gone from every job/step name and comment; KEEP rows recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-RESEARCH.md

# The workflows being edited (read the census rows first)
@.github/workflows/require-issue-link.yml (:119-129 — the inline JS :125 is a reword target)
@.github/workflows/auto-close-unsolicited-prs.yml (:87-95 — the inline JS :91 is a reword target)
@.github/workflows/corpora.yml (:266 the step name 'Run golden suite (testdata/golden)')
@.github/workflows/bench.yml (:4, :31, :45, :116-121, :128-130, :510 framing comments)

# The verify-only workflows (KEEP rows recorded, not edited)
@.github/workflows/ci.yml (:189-191 KEEP upstream advisory)
@.github/workflows/release.yml (:65/:183 KEEP in-repo pin guard)
@.github/workflows/linux-cross-canary.yml (:26 KEEP in-repo pin guard)
@.github/workflows/post-release-verify.yml (:220/:258 KEEP upstream = Actions fork-trigger sense)
@.github/workflows/pr-template-format.yml, pr-title.yml (not framing-carrying — verify)

# The binding constraints (read before any edit)
@internal/upgrade/taskfile_shape_test.go (:36-51 requiredCheckNames fixture; the workflow-body guards)
</context>

<tasks>

<task type="tracer">
  <name>Corpora and the two inline-JS comment strings — the phase's workflow framing heart</name>
  <files>.github/workflows/require-issue-link.yml, .github/workflows/auto-close-unsolicited-prs.yml, .github/workflows/corpora.yml</files>
  <read_first>
    .github/workflows/require-issue-link.yml — the message-string around :119-129 that names 'parity decisions', plus the enclosing JS so you only reword the string
    .github/workflows/auto-close-unsolicited-prs.yml — the close-reason around :85-95 that says 'ports observable behavior ... parity decisions recorded in `.planning/`'
    .github/workflows/corpora.yml — the step at :266 whose name is 'Run golden parity suite (testdata/golden)'; keep its `run: task test:golden` body byte-identical
    internal/upgrade/taskfile_shape_test.go — before/after check of the TestWorkflowRunBodiesInvokeTask guard's own `run:` handling: the guard reads every step's `run:` value; a name edit is safe because the guard keys body only
  </read_first>
  <action>
    Make three reword edits on the project's own terms — no shell `run:`-body change, no required-check-name change:

    1. `require-issue-link.yml` :125 — the inline JS message "…open one first — it is where parity decisions and prior context surface before code gets written" becomes "…open one first — it is where prior context and project decisions surface before code gets written" (keep the link-out and the instruction exactly as-is). The string sits inside a JavaScript action's `with.script` block — it is executable action code, NOT a YAML `run:` body, so the surrounding JS syntax must stay intact and the edited script must pass a `node --check` (review A4).
    2. `auto-close-unsolicited-prs.yml` :91 — the JS string "…This project ports observable behavior from another implementation, so changes that look like clear improvements are sometimes deliberate parity decisions recorded in `.planning/`. Agreeing on the issue first is how that surfaces before you spend time on code." becomes "…Behavior-affecting changes are worked out against the project's recorded decisions in `.planning/`. Agreeing on the issue first is how that surfaces before you spend time on code." — keep `.planning/` and the issue-first guidance, drop the ports/parity framing, preserve the template-literal shape, and validate the action script parses afterwards (same `node --check` discipline as above: A4).
    3. `corpora.yml` :266 — rename the step `Run golden parity suite (testdata/golden)` to `Run the golden suite (testdata/golden)` (or any own-terms name still naming testdata/golden). Do NOT change the `run: task test:golden` shell body or the comments below unless they describe the parity frame (the comment below already says 'golden suite', honest).
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -n -e "parity" -e "ports observable" -e "another implementation" .github/workflows/require-issue-link.yml .github/workflows/auto-close-unsolicited-prs.yml | wc -l | tr -d " "); echo "framing lines left in the two JS files: $n"; test "$n" = "0"; rg -n "Run .*golden" .github/workflows/corpora.yml | wc -l | grep -qx "1"; echo "=== JS syntax validation (review A4: with.script is executable action code) ==="; d=$(mktemp -d); for f in require-issue-link auto-close-unsolicited-prs; do python3 - "$f" "$d" >/dev/null 2>&1 <<PYEOF
import sys, yaml
p = f".github/workflows/{sys.argv[1]}.yml"
data = yaml.safe_load(open(p))
for job in data.get("jobs", {}).values():
    for step in (job or {}).get("steps", []) or []:
        sc = (step.get("with") or {}).get("script") or step.get("script")
        if sc:
            import re
            name = re.sub(r"[^A-Za-z0-9_-]", "-", step.get("name") or "step")[:30]
            open(f"{sys.argv[2]}/{sys.argv[1]}-{name}.mjs", "w").write(sc)
PYEOF
done; for js in "$d"/*.mjs; do node --check "$js" || { echo "JS parse failed: $js"; exit 1; }; done; echo "all inline JS parses"; go test -count=1 ./internal/upgrade/... -run TestWorkflowRunBodiesInvokeTask >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'parity|ports observable|another implementation'` on the three edited files returns zero
    - the diff for `require-issue-link.yml` and `auto-close-unsolicited-prs.yml` shows only the message-string text at :125 and :91 inside the respective `with.script` blocks; the surrounding JS syntax is unchanged and each edited script passes `node --check` (reviewed A4 — they are action-script bodies, not YAML run: fields)
    - the diff for corpora.yml shows only the step `name:` line; the `run: task test:golden` shell body is byte-identical
    - `go test -count=1 ./internal/upgrade/... -run 'TestWorkflow'` passes afterwards (guards green)
    - zero changes to any `run:` shell body in the two files (only the JS inside `with.script`, whose parse is assert-checked)
  </acceptance_criteria>
  <done>Corpora's step plus the two inline-JS scripts read on the project's own terms; the JS still parses (A4); the required-check names and all shell run: bodies are byte-identical.</done>
</task>

<task type="auto">
  <name>bench.yml: re-frame comments AND rename the free head-to-head job/step display names (H2)</name>
  <files>.github/workflows/bench.yml</files>
  <read_first>
    .github/workflows/bench.yml — the header comment :1-45 (Go-vs-TS phrasing, :4/:31/:45), the job declaration at :96 (`headtohead:` with display name `head-to-head publish (PERF-01, non-blocking)`), the step labels at :116-127 (Setup Node / Install TS reference binary / Run head-to-head benchmark at :127), the pipeline comments at :128-130, and the :510 stale-comparison mention
  </read_first>
  <action>
    Rewrite the framings inside `bench.yml` while keeping every step, every `run:` shell body, every `uses:` action, and the comparison-capability steps themselves (Phase 6 de-couples the runner; this plan does not touch it). CRUCIALLY the two free display-name occurrences are RENAMED, because PROC-03's criterion 3 covers job and step names and these names are not bound by the ruleset (review H2):

    1. Header (:1-45): the "head-to-head Go-vs-TS" phrasing is reworded to neutral project-own language (absolute Go measurements plus the comparator harness it runs), never "the project is a TS equivalent".
    2. JOB display name — `headtohead:` job at :96: its `name: head-to-head publish (PERF-01, non-blocking)` becomes an own-terms name, e.g. `name: benchmark publish (PERF-01, non-blocking)`. The job ID `headtohead` is a workflow-internal key used in `if:` conditions and `workflow_dispatch` inputs — renaming the KEY would risk actionlint/CI parity, so rename ONLY the display `name:` string and keep the job ID unchanged unless the rename's consequence is fully verified; if you judge the `headtohead` key itself framing and rename it, verify the `if:` and inputs the refer to it align in the same edit.
    3. STEP name :127 `Run head-to-head benchmark` becomes `Run benchmark` (or own-terms equivalent that keeps meaning for the reader).
    4. Step labels at :119/:121 the TS/reference wording is neutralized (e.g. "Set up Node ()" / "Install the benchmark binary @1.3.1"); :128-130 comments reword "installed TS reference" -> "installed comparison binary"; :510 "stale reference" -> "the older committed baseline".
    5. Sweep any other comparison phrasing the census flags (e.g. :31, :45), then re-grep to prove `head-to-head`, `headtohead` (display/comment), `reference binary`, `TS reference` are gone from DISPLAY names and comments alike.
    No benchmark-run capability step, `run:`, `uses:`, `needs:`, permission, or job ID is edited beyond the display `name:` if the ID must stay.
  </action>
  <verify>
    <automated>bash -c "set -e; c=$(rg -n -e 'head-to-head' -e 'vs TS' -e 'TS reference' -e 'reference binary' .github/workflows/bench.yml | wc -l | tr -d ' '); echo 'bench framing terms left: $c'; test \"'$c'\" = '0'; actionlint .github/workflows/bench.yml; diff-preserve=$(git diff --numstat .github/workflows/bench.yml | awk '{print $1+$2}'); echo \"changed lines: $diff-preserve\"; go test -count=1 ./internal/upgrade/... -run 'TestWorkflowRequiredChecks|TestWorkflowRunBodiesInvokeTask' >/dev/null"</automated>
  </verify>
  <acceptance_criteria>
    - after the sweep `rg -n -e 'head-to-head' -e 'vs TS' -e 'TS reference' -e 'reference binary'` over the whole `bench.yml` returns zero (job display names, step names and comments inclusive)
    - the job ID `headtohead` (if kept) is consistent across the pipeline (that exact key, not a second rename)
    - `git diff -- .github/workflows/bench.yml` shows only `#` comments, the two display-name strings for step+job, and the two benchmark step labels; zero `-run:`, `-uses:`, or required-check job `-name` changes
    - the required-check names (`test` / `govulncheck` / `reproducibility` / `perf regression gate` / `actionlint` / `goreleaser check` / `pr-title`) in `internal/upgrade/taskfile_shape_test.go` and in the workflows are byte-identical (bench's job is not one of them)
    - `actionlint .github/workflows/bench.yml` exits 0
  </acceptance_criteria>
  <done>bench.yml carries zero retired framing in any name or comment; the benchmark capability steps and the required-check job names are untouched; the free head-to-head job/step display names are renamed on own terms.</done>
</task>

<task type="auto">
  <name>Re-verify the whole phase — actionlint and workflow guards green, verify-only rows untouched, KEEP reasons recorded</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, .github/workflows/linux-cross-canary.yml, .github/workflows/post-release-verify.yml, .github/workflows/release-please.yml</files>
  <read_first>
    .github/workflows/ci.yml — the required-check job names are present (:the test/actionlint etc. job `names`); the :189-191 third-party advisory comment you will KEEP
    .github/workflows/release.yml (:65/:183) — 'machine-enforced by TestGoreleaserPinParity' guarding the in-repo tool-modfile pin; keep
    .github/workflows/linux-cross-canary.yml (:26) — the test-goreleaserpin comment
    internal/upgrade/taskfile_shape_test.go — the fixture at :36-51 (read once to assert the required-check names are unchanged)
  </read_first>
  <action>
    Verify-only across the whole workflow surface and the repository guards. (1) Run `task lint:actions` (actionlint) against ALL 14 workflows — must exit 0. (2) Run the workflow-shape guard tests: `go test -count=1 ./internal/upgrade/...` — with the required-check set unchanged. (3) Record the KEEP reasons in the SUMMARY for the verify-only rows: release:65/:183 (`TestGoreleaserPinParity` = the in-repo pin-consistency guard, not comparison framing — D-01 exclusion), linux-cross-canary:26 (the same pin-guard name), release-please:26 (`Ports docs/RELEASE-PROCEDURES` = an in-repo procedure restatement), post-release-verify:220/:258 ('upstream' as a fork-trigger concept), ci:189-191 ('upstream unmaintained' as a third-party dependency advisory). (4) Assert the verify-only files have no diff: `git diff --name-only` over ci/release/… must be empty. (5) Read-only confirm with `gh api repos/seanb4t/codegraph-go/rulesets/20157557` that the seven required-check names are unchanged.
  </action>
  <verify>
    <automated>bash -c 'set -e; actionlint .github/workflows/*.yml; go test -count=1 ./internal/upgrade/... >/dev/null; test -z "$(git diff --name-only -- .github/workflows/ci.yml .github/workflows/release.yml .github/workflows/linux-cross-canary.yml .github/workflows/post-release-verify.yml .github/workflows/release-please.yml)"; echo "verify-only workflows unchanged, actionlint+guards green"'</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/*.yml` exits 0 (all 14)
    - `go test -count=1 ./internal/upgrade/...` includes TestWorkflowRequiredChecks and passes
    - `git diff --name-only -- ci/release/canary/post/…` shows nothing (verify-only untouched)
    - if the live-ruleset `gh api` runs and differs from the fixture names, STOP and report (do not fix the fixture) — branch protection should be the authority
  </acceptance_criteria>
  <done>All workflows pass the checks after the sweep; the seven required-check names are byte-identical (ruleset binding); the free framing-bearing bench names were renamed (H2); verify-only rows are untouched; every KEEP-row is recorded in the SUMMARY.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| require-issue-link.yml | edited | the inline-JS :125 message de-paritied (issue-first + prior context) |
| auto-close-unsolicited-prs.yml | edited | the inline-JS :91 message de-paritied (keeps `.planning/` + issue-first) |
| corpora.yml | edited | :266 step `name:` de-paritied; `run: task test:golden` unchanged |
| bench.yml | edited | comments at :4/:31/:128/:510 reworded; step names at :119/:121 neutralized; **the free job display name at :96 and step display name at :127 renamed off `head-to-head`** (H2); capability steps and `run:`/`uses:`/`needs:` bodies untouched (Phase 6 decouples the runner) |
| ci/release/canary/post-release-verify/release-please + the 14 workflows | verified | only the four swept workflow files have diffs; every verify-only workflow untouched; KEEP reasons in the SUMMARY |
| the two inline-JS action scripts | validated | the `with.script` blocks re-parse (`node --check`) after the reword (review A4 — they are not YAML `run:` bodies) |

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| required-check names ↔ ruleset 20157557 | renaming one of the 7 required-context names detaches branch protection — the sweep keeps exactly those byte-identical and renames ONLY free framing-bearing names (bench head-to-head, H2) |
| inline-JS ↔ PR experience | the two reworded strings are user-visible in PR threads; they must keep the issue-first/planning instruction and stay valid JS (A4) |
| bench.yml ↔ Phase 6 | Phase 5 renames/rewrites only the framing (names + comments); the comparison RUNNER and the benchmark steps stay for Phase 6 to decouple |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------------|-----------------|
| T-05-09 | Tampering | required-check job-name edit (breaking the ruleset binding) | critical | mitigate | the 7 required names stay byte-identical (diff review + `TestWorkflowRequiredChecks`); only FREE framing-bearing names (bench headtohead display/step) are renamed in the same diff (H2) |
| T-05-10 | Tampering | shell `run:`-body accident or JS-parse break | medium | mitigate | edits confined to display names/comments/string literals; `TestWorkflowRunBodiesInvokeTask` proves shell `run:` bodies green; the two `with.script` snippets `node --check` (A4) |
| T-05-11 | DoS | accidentally decoupling the bench runner (Phase-6 scope) | medium | mitigate | the plan keeps every bench step and `run:`/`uses:`/`needs:`; only display `name:` strings and comments change |
| T-05-12 | Tampering | the sweep degenerating into a regex queue | medium | mitigate | each change is hand-written per the census row (05-02 or 05-03), never a bare-word find-replace |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | no new external packages this phase — in-workflow-string edits only |

<verification>
- `actionlint .github/workflows/*.yml` (14 files) exit 0
- `go test -count=1 ./internal/upgrade/...` (TestWorkflowRequiredChecks + TestWorkflowRunBodiesInvokeTask) green
- `rg -n 'head-to-head|vs TS|TS reference|reference binary' .github/workflows/bench.yml` — zero (display names, step names and comments)
- `git diff --name-only` shows only require-issue-link / auto-close-unsolicited-prs / corpora / bench changed; all verify-only workflows empty
- the two inline-JS scripts parse (`node --check`); zero shell `run:` body diff
</verification>

<success_criteria>
- The seven required-check names are byte-identical and every required-check guard green; all shell `run:` bodies identical
- zero retired framing in job display names, step names, or comments across the workflows (incl. the free bench head-to-head names, H2)
- bench.yml runs exactly as before: the comparison runner and all steps remain; only the framing (names + comments) changed; runner decoupling is explicit Phase-6 work
</success_criteria>

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-03-SUMMARY.md` when done
</output>