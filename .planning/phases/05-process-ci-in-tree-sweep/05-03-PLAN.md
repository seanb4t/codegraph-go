---
phase: 05-process-ci-in-tree-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/require-issue-link.yml
  - .github/workflows/auto-close-unsolicited-prs.yml
  - .github/workflows/corpora.yml
  - .github/workflows/bench.yml
  - .github/workflows/ci.yml (verify-only)
  - .github/workflows/release.yml (verify-only)
  - .github/workflows/linux-cross-canary.yml (verify-only)
  - .github/workflows/post-release-verify.yml (verify-only)
  - .github/workflows/release-please.yml (verify-only)
autonomous: true
requirements: [PROC-03]
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Workflow job names, step names, and comments carry no retired comparison framing across the swept files: the inline-JS string in `require-issue-link.yml` (:125, 'parity decisions and prior context surface') and the inline-JS string in `auto-close-unsolicited-prs.yml` (:91, 'This project ports observable behavior from another implementation ... deliberate parity decisions recorded in `.planning/`') are both reworded on codegraph-go's own terms (keeping each file's issue-first instruction and the `.planning/` reference where it serves process); the `corpora.yml` :266 step name `Run golden parity suite (testdata/golden)` becomes an own-terms name on the `run: task test:golden` body being byte-identical."
    - "`bench.yml` framing prose (the header comment about Go-vs-TS head-to-head numbers, the 'reference binary' phrasing in step names/comments, and the it-is-as-each-other claims) is swept without removing, renaming, re-scoping, or re-run-ing any step or job — the comparison capability stays in the job because Phase 6 owns bench.yml's de-coupling; the frame becomes neutral ('the installed binary' / 'the benchmark binary') and the raw-numbers comment reads on the project's own terms."
    - "Zero job-name edits occurred: the 7 required-check job names bound by the literal fixture in `internal/upgrade/taskfile_shape_test.go:36-51` (`test`, `govulncheck (DIST-03, blocking)`, `reproducibility (double-build hash-diff, DIST-04)`, `perf regression gate (PERF-02, INDX-06)`, `actionlint (workflow static analysis)`, `goreleaser check (config validation, DIST-01)`, `pr-title`) are byte-identical, and no other job `name:` was edited either — renaming one would break branch protection (ruleset 20157557)."
    - "Every workflow still passes `actionlint` 1.7.12 (all ~14 files exit 0) and the workflow-shape guards still hold: `TestWorkflowRunBodiesInvokeTask` (binds `run:` bodies) is green because no `run:` body in any workflow changed, and `TestWorkflowRequiredChecks` is green because the required-check names are untouched."
    - "The KEEP rows are preserved with recorded reasons in the SUMMARY: `release.yml` :65/:183 and `linux-cross-canary.yml` :26 name `TestGoreleaserPinParity` — an in-repo pin consistency guard, not comparison framing (D-01 scope exclusion); `release-please.yml` :26 'Ports docs/RELEASE-PROCEDURES' — an in-repo procedure-to-steps restatement, not origin framing; `post-release-verify.yml` (:220, :258) uses 'upstream' in the GitHub-Actions fork workflow trigger sense; `ci.yml` :189-191 uses 'upstream unmaintained' for a third-party advisory (sigstore/cosign), not the origin project."
  prohibitions:
    - "zero job-name edits of any kind (required-check binding, ruleset 20157557)"
    - "no changes to any `run:` body — only step `name:`s, comments, and inline-JS string literals"
    - "no removal or de-coupling of the bench.yml comparison runner or any benchmark step (Phase 6)"
    - "no regex sweep over the word TS (the two inline-JS strings are hand-reworded)"
  artifacts:
    - .github/workflows/require-issue-link.yml — inline-JS message reworded at :125
    - .github/workflows/auto-close-unsolicited-prs.yml — inline-JS message reworded at :91
    - .github/workflows/corpora.yml — step name at :266 de-paritied
    - .github/workflows/bench.yml — comments at :4/:31/:45/:128-130/:510 reworded on own terms; light-touch step-name text update
  key_links:
    - "requiredCheckNames fixture (taskfile_shape_test.go:36-51) ↔ ruleset 20157557 job names — zero job-name edits keeps that guard byte-identical"
    - "corpora.yml:266 step name ↔ TestWorkflowRunBodiesInvokeTask — the `run: task test:golden` body stays byte-identical; only the name changes"
    - "bench.yml :119/:121 step names ↔ Phase 6 de-coupling — the steps stay (the capability), the 'reference'/(comparing) words are swept in this phase only"
---

<objective>
Sweep the workflow files' job names, step names, comments, and inline-JS prose so every workflow passes `actionlint` and its own required status checks while carrying no retired comparison framing (PROC-03). The sweep is name-and-comment only: zero job-name edits (job names are bound to the required-check set by ruleset 20157557 and `internal/upgrade/taskfile_shape_test.go`), zero `run:`-body changes (bound by `TestWorkflowRunBodiesInvokeTask`), and bench.yml's own de-coupling is explicitly out of scope (Phase 6 owns it; this plan touches only its framing).

Purpose: A contributor opening a PR meets the project's evidence only on its own terms. This plan kills the same "ports observable behavior … parity decisions" sentence in two CI script strings that the PR-template fix (05-02) removed from the template body — the CI twin of that fix — and re-frames the benchmark workflow without touching the benchmark machinery Phase 6 retires.

Output: All workflows pass `actionlint` and the workflow-shape guards; the retired framing words are gone from every job/step name and comment; KEEP rows recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-RESEARCH.md

# The workflows being edited (read the census rows first)
@.github/workflows/require-issue-link.yml (:119-129 — the inline JS :125 is a reword target)
@.github/workflows/auto-close-unsolicited-prs.yml (:87-95 — the inline JS :91 is a reword target)
@.github/workflows/corpora.yml (:266 the step name 'Run golden suite (testdata/golden)')
@.github/workflows/bench.yml (:4, :31, :45, :116-121, :128-130, :510 framing comments)

# The verify-only workflows (KEEP rows recorded, not edited)
@.github/workflows/ci.yml (:189-191 KEEP upstream advisory)
@.github/workflows/release.yml (:65/:183 KEEP in-repo pin guard)
@.github/workflows/linux-cross-canary.yml (:26 KEEP in-repo pin guard)
@.github/workflows/post-release-verify.yml (:220/:258 KEEP upstream = Actions fork-trigger sense)
@.github/workflows/pr-template-format.yml, pr-title.yml (not framing-carrying — verify)

# The binding constraints (read before any edit)
@internal/upgrade/taskfile_shape_test.go (:36-51 requiredCheckNames fixture; the workflow-body guards)
</context>

<tasks>

<task type="tracer">
  <name>Corpora and the two inline-JS comment strings — the phase's workflow framing heart</name>
  <files>.github/workflows/require-issue-link.yml, .github/workflows/auto-close-unsolicited-prs.yml, .github/workflows/corpora.yml</files>
  <read_first>
    .github/workflows/require-issue-link.yml — the message-string around :119-129 that names 'parity decisions', plus the enclosing JS so you only reword the string
    .github/workflows/auto-close-unsolicited-prs.yml — the close-reason around :85-95 that says 'ports observable behavior ... parity decisions recorded in `.planning/`'
    .github/workflows/corpora.yml — the step at :266 whose name is 'Run golden parity suite (testdata/golden)'; keep its `run: task test:golden` body byte-identical
    internal/upgrade/taskfile_shape_test.go — before/after check of the TestWorkflowRunBodiesInvokeTask guard's own `run:` handling: the guard reads every step's `run:` value; a name edit is safe because the guard keys body only
  </read_first>
  <action>
    Make three reword edits on the project's own terms — no `run:`-body, no job-name:

    1. `require-issue-link.yml` :125 — the inline JS message "…open one first — it is where parity decisions and prior context surface before code gets written" becomes "…open one first — it is where prior context and project decisions surface before code gets written" (keep the link-out and the instruction exactly as-is).
    2. `auto-close-unsolicited-prs.yml` :91 — the JS message "…This project ports observable behavior from another implementation, so changes that look like clear improvements are sometimes deliberate parity decisions recorded in `.planning/`. Agreeing on the issue first is how that surfaces before you spend time on code." becomes "…Behavior-affecting changes are worked out against the project's recorded decisions in `.planning/`. Agreeing on the issue first is how that surfaces before you spend time on code." — keep the `.planning/` name and the issue-first guidance, drop only the ports/parity framing.
    3. `corpora.yml` :266 — rename the step `Run golden parity suite (testdata/golden)` to `Run the golden suite (testdata/golden)` (or any own-terms name that still names testdata/golden explicitly). Do NOT change `run: task test:golden` or the comment below it beyond fixing it (the comments below already use 'golden suite'; if any inline comment at :266 describes the parity frame, reword it the same way).
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -n -e "parity" -e "ports observable" -e "another implementation" .github/workflows/require-issue-link.yml .github/workflows/auto-close-unsolicited-prs.yml | wc -l | tr -d " "); echo "framing terms left in the two JS files: $n"; test "$n" = "0"; rg -n "Run .*golden" .github/workflows/corpora.yml | wc -l | grep -qx "1"; go test -count=1 ./internal/upgrade/... -run TestWorkflowRunBodiesInvokeTask >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'parity|ports observable|another implementation'` on the three edited files returns zero
    - the diff for `require-issue-link.yml` and `auto-close-unsolicited-prs.yml` shows only the string literal text at :125 and :91 (and the PR-body column)
    - the diff for corpora.yml shows only the step `name:` and (if any) the comment; `run: task test:golden` is author-identical (confirm via diff review)
    - `go test -count=1 ./internal/upgrade/... -run 'TestWorkflow'` passes afterwards (guards green)
  </acceptance_criteria>
  <done>Corpora's step and the two inline-JS closures read on the project's own terms; the required-check and run-body guards stay green.</done>
</task>

<task type="auto">
  <name>bench.yml re-frame — comments and step-name labels only; no bench step, job, or run-body change</name>
  <files>.github/workflows/bench.yml</files>
  <read_first>
    .github/workflows/bench.yml — the header comment :1-45 (Go-vs-TS phrasing, :4/:31/:45), the step names at :116/:119-122 (Setup Node / Install TS reference binary), the pipeline comments at :128-130, and the :510 stale-comparison mention
  </read_first>
  <action>
    Rewrite the framings inside `bench.yml` while keeping every step, job, `run:` body, `uses:` action, and the comparison-capability steps themselves (Phase 6 de-couples the runner; this plan does not touch it).

    1. Header (:1-45): the "head-to-head Go-vs-TS" phrasing is reworded to neutral project-own language describing what the job measures (absolute Go measurements plus the comparator harness it runs), never "the project is a TS equivalent".
    2. Step labels at :119 and :121 where the text reads "for the installed TS reference binary" and "TS codegraph@1.3.1 reference": reword only the label wording to keep the purpose clear without the reference-to-an-implementation positioning (e.g. "Set up Node (for the benchmark binary)" and "Install the TS comparison binary @1.3.1" or equivalent own-terms wording). Do not delete the steps or change their `run:`/`uses:`.
    3. Pipeline comments around :128 ("Runs both the freshly built Go binary and the installed TS reference over ...") — reword "installed TS reference" to "installed comparison binary" (same meaning, project-own terms).
    4. :510 "Comparing today's tmpfs numbers against that stale reference" — reword "stale reference" to "the older committed baseline" (a prior measurement, not a competitive artifact).
    5. Sweep any other comparison phrasing the census flags (e.g. :31, :45) with the same term-by-term discipline, then re-grep to prove zero remain. Pipeline and job `name:` values are NOT touched.
    Tools/bench code itself is Phase-6 territory; this plan changes only the prose in the workflow file.
  </action>
  <verify>
    <automated>bash -c 'set -e; rg -n -e "vs TS" -e "head-to-head" -e "TS reference" -e "reference binary" .github/workflows/bench.yml | wc -l | tr -d " " | grep -qx "0"; actionlint .github/workflows/bench.yml'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'vs TS|head-to-head|TS reference|reference binary' .github/workflows/bench.yml` returns zero (the comparison frame is gone, the pipeline stands)
    - `git diff -- .github/workflows/bench.yml` shows only `#` comments plus the two step-label strings; zero `-run:`, `-uses:`, or job `-name:` lines (step-name strings are editable — they sit below the job layer and are not required-checks)
    - `actionlint .github/workflows/bench.yml` exits 0
  </acceptance_criteria>
  <done>bench.yml's comparison wording is fully neutralized; every step performs exactly what it did before; the job-layer names (required checks) are untouched.</done>
</task>

<task type="auto">
  <name>Re-verify the whole phase — actionlint and workflow guards green, verify-only rows untouched, KEEP reasons recorded</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, .github/workflows/linux-cross-canary.yml, .github/workflows/post-release-verify.yml, .github/workflows/release-please.yml</files>
  <read_first>
    .github/workflows/ci.yml — the required-check job names are present (:the test/actionlint etc. job `names`); the :189-191 third-party advisory comment you will KEEP
    .github/workflows/release.yml (:65/:183) — 'machine-enforced by TestGoreleaserPinParity' guarding the in-repo tool-modfile pin; keep
    .github/workflows/linux-cross-canary.yml (:26) — the test-goreleaserpin comment
    internal/upgrade/taskfile_shape_test.go — the fixture at :36-51 (read once to assert the required-check names are unchanged)
  </read_first>
  <action>
    Verify-only across the whole workflow surface and the repository guards. (1) Run `task lint:actions` (actionlint) against ALL 14 workflows — must exit 0. (2) Run the workflow-shape guard tests: `go test -count=1 ./internal/upgrade/...` — with the required-check set unchanged. (3) Record the KEEP reasons in the SUMMARY for the verify-only rows: release:65/:183 (`TestGoreleaserPinParity` = the in-repo pin-consistency guard, not comparison framing — D-01 exclusion), linux-cross-canary:26 (the same pin-guard name), release-please:26 (`Ports docs/RELEASE-PROCEDURES` = an in-repo procedure restatement), post-release-verify:220/:258 ('upstream' as a fork-trigger concept), ci:189-191 ('upstream unmaintained' as a third-party dependency advisory). (4) Assert the verify-only files have no diff: `git diff --name-only` over ci/release/… must be empty. (5) Read-only confirm with `gh api repos/seanb4t/codegraph-go/rulesets/20157557` that the seven required-check names are unchanged.
  </action>
  <verify>
    <automated>bash -c 'set -e; actionlint .github/workflows/*.yml; go test -count=1 ./internal/upgrade/... >/dev/null; test -z "$(git diff --name-only -- .github/workflows/ci.yml .github/workflows/release.yml .github/workflows/linux-cross-canary.yml .github/workflows/post-release-verify.yml .github/workflows/release-please.yml)"; echo "verify-only workflows unchanged, actionlint+guards green"'</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/*.yml` exits 0 (all 14)
    - `go test -count=1 ./internal/upgrade/...` includes TestWorkflowRequiredChecks and passes
    - `git diff --name-only -- ci/release/canary/post/…` shows nothing (verify-only untouched)
    - if the live-ruleset `gh api` runs and differs from the fixture names, STOP and report (do not fix the fixture) — branch protection should be the authority
  </acceptance_criteria>
  <done>All workflows pass the checks after the swipe; the required-check binding is unchanged (no job-rename); verify-only rows are untouched; every KEEP-row is recorded in the SUMMARY.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| require-issue-link.yml | edited | the inline-JS :125 message de-paritied (issue-first + prior context) |
| auto-close-unsolicited-prs.yml | edited | the inline-JS :91 message de-paritied (keeps `.planning/` + issue-first) |
| corpora.yml | edited | :266 step `name:` de-paritied; `run: task test:golden` unchanged |
| bench.yml | edited | comments at :4/:31/:128/:510 reworded; step names at :119/:121 neutralized; capability steps and `run:` bodies untouched (Phase 6 de-couples) |
| ci/release/canary/post-release-verify/release-please + all 14 | verified | verify-only rows — actionlint + workflow guards green; KEEP reasons in the SUMMARY |
| (no new tests) | — | the workflow-shape guards were already bound; nothing new created |

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow names ↔ required-checks | job-name edits detach the required-Check contract of the ruleset (branch protection) — the sweep imposes a zero-job-name constraint |
| Inline-JS ↔ PR experience | the two reworked strings are user-visible in PR health; they must keep the issue-first/planning instruction |
| bench.yml ↔ Phase 6 | the plan only rewrites bench.yml prose; the runner and comparison steps stay but are the next phase's responsibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------------|-----------------|
| T-05-09 | Tampering | job-name edit breaking required-check binding | critical | mitigate | zero-job-names rule + guard run; verify asserts `TestWorkflowRequiredChecks` passes and the live `gh api` ruleset read (optional) matches the fixture; any job rename fails the verify |
| T-05-10 | Tampering | `run:`-body accident | medium | mitigate | edits confined to names/comments/strings; `TestWorkflowRunBodiesInvokeTask` runs and proves the bodies byte-identical |
| T-05-11 | DoS | accidentally de-coupling bench (Phase-6 scope) | medium | mitigate | the plan keeps every bench step and `run:`/`uses:`; the final diff review shows no `-run:` or job `-name:` lines |
| T-05-12 | Tampering | the sweep degenerating into a regex queue | medium | mitigate | each change is hand-written per the census row (05-02 or 05-03), never find-replaced over a bare word |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | no new external packages this phase — in-workflow-string edits only |

<verification>
- `actionlint .github/workflows/*.yml` (14 files) exit 0
- `go test -count=1 ./internal/upgrade/...` (TestWorkflowRunBodies + RequiredChecks) green
- `git diff --name-only` on all verify-only workflows empty

</verification>

<success_criteria>
- Every job name (incl. the seven required) is byte-identical; every `run:` body identical; every required-check guard green
- zero retired framing in step names/comments across the workflows
- bench.yml: all benchmark steps and `run:` bodies remain; only the comparison wording is own-terms; the de-couple is explicit Phase-6 work
</success_criteria>

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-03-SUMMARY.md` when done
</output>