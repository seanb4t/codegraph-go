---
phase: 05-process-ci-in-tree-sweep
plan: 06
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - corpus/behavioral/src/accounts/manager.go (comment)
  - corpus/behavioral/src/accounts/validate.go (comment)
  - corpus/behavioral/src/orders/validate.go (comment)
  - corpus/behavioral/src/recovery/recovery_test.go (comment)
  - corpus/behavioral/go-explore-multi.json (re-freeze)
  - corpus/behavioral/go-node-multi.json (re-freeze)
  - corpus/behavioral/CASES.json (comment row, if it carries the framing name)
  - testdata/golden/README.md (verified unchanged by this plan; the ts-* rewrite belongs to 05-01)
autonomous: true
requirements: [CODE-01]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The four corpus source-comment rows that still say 'synthetic-parity corpus' are reworded to name the corpus as the behavioral one (e.g. 'the behavioral corpus'), so a future reader of corpus/behavioral/src no longer meets the retired name (D-03, D-07; research §4.8)."
    - "The golden re-freeze that riding along with the corpus reword happens IN THE SAME PHASE via `task golden:regen` against the locked corpora, and the ONLY content diff in the resulting golden JSON is the rewording of those corpus comments — no other golden byte changes (the re-freeze is attributed to this small source change, D-07, and is NOT Phase-2-like full re-baseline)."
    - "`go test -count=1 ./testdata/golden/...` (plus `task test:golden`) exits 0 after the re-freeze — the golden suite is green against the adjusted corpus, `TestGoldenScenarioCountIsExact` still sees the same scenario count (the corpus CASES-map is unchanged)."
    - "This plan does NOT touch internal/query or the capability matrix (those are 05-04's ships): it is limited to the corpus/behavioral src comment rows and the traversal that re-freezes the JSON transcripts."
    - "If any golden diff shows a content change beyond the intended four comment rewords, STOP and investigate rather than commit — an unexpected diff means a nondeterministic capture (my plan is a review only; the golden:reg target is the re-freeze gate)."
  prohibitions:
    - "no re-freeze of the whole golden suite as a re-baseline (the lock corpora stay the same; only the source-comment bytes change)"
    - "no change to `CASES.json`/`go-explore-multi.json` scenario-count or to the case map"
    - "no touching of test/query/golden_test/task-guard files (plan 05-01 owns testdata/golden/golden_test.go edits; 05-04 owns the query test)"
    - "no regex over the corpus source — every corpus comment is hand-reworded"
  artifacts:
    - corpus/behavioral/src/{accounts,orders,recovery} comment rows de-synthetic-parity
    - corpus/behavioral/go-explore-multi.json / go-node-multi.json re-frozen after the attached corpus change
  key_links:
    - "the corpus/behavioral src comment bytes <> the two go-* transcript files — the transcripts EMBED the comment bytes, so a comment edit forces `task golden:regen`, and the resulting diff is reviewable to show only 4 small byte-run changes"
    - "the CASE map (CASES.json) is the stable address — the reword must not change any caption the case map uses for the four cases"
---

<objective>
Reword the four corpus/behavioral src comments that still carry the retired 'synthetic-parity' corpus name onto the behavioral vocabulary (D-03/D-07), and re-freeze the two behavioral JSON transcripts that embed those comment bytes (`task golden:regen` against the locked corpora). This is the small attributed golden re-freeze the CONTEXT explicitly allows to ride along in the same phase — the golden files embed the corpus source, so a comment reword forces a re-freeze; doing both keeps the corpus and its goldens consistent rather than leaving a stale frozen oracle that would go red on the next capture (D-07).

Purpose: this is the single golden byte-change of the milestone outside the earlier Phases — a deliberate, small, attributed exception (D-07). It belongs here and NOT in a comment sweep plan because the phase discipline is "a comment change and a golden change never share a diff with another cause"; this plan pairs the corpus src reword with its own re-freeze as one self-attributing change.

Output: no re-baseline. The four corpus comments are behavioral, the JSON goldens are consistent with them, and the golden suite stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-RESEARCH.md

# The corpus comments to reword (D-07 rows, §4.8)
@corpus/behavioral/src/accounts/manager.go (:3 'the synthetic-parity corpus')
@corpus/behavioral/src/accounts/validate.go (:6 'case (a) of the synthetic-parity corpus')
@corpus/behavioral/src/orders/validate.go (:6 'case (a) of the synthetic-parity corpus')
@corpus/behavioral/src/recovery/recovery_test.go (:5 'case (c) of the synthetic-parity corpus')

# The frozen transcripts that embed those bytes (re-freeze targets)
@corpus/behavioral/go-explore-multi.json
@corpus/behavioral/go-node-multi.json
# The re-freeze entry point
@Taskfile.yml (golden:regen / corpora:fetch targets — read the task's behavior to run it with `-count=1` and to know the corpora requirement)
</context>

<tasks>

<task type="auto">
  <name>Reword the four corpus/behavioral src comments from synthetic-parity to behavioral</name>
  <files>corpus/behavioral/src/accounts/manager.go, corpus/behavioral/src/accounts/validate.go, corpus/behavioral/src/orders/validate.go, corpus/behavioral/src/recovery/recovery_test.go</files>
  <read_first>
    corpus/behavioral/src/accounts/manager.go — the doc comment at :3-6 mentioning case (b) of "the synthetic-parity corpus"
    corpus/behavioral/src/accounts/validate.go — the doc comment at :6 'case (a) of the synthetic-parity corpus'
    corpus/behavioral/src/orders/validate.go — the doc comment at :6 'case (a) of the synthetic-parity corpus — see ...'
    corpus/behavioral/src/recovery/recovery_test.go — the doc comment at :5-7 'case (c) of the synthetic-parity corpus'
  </read_first>
  <action>
    For each of the four files, reword ONLY the occurrence of the retired corpus name in the doc comment: replace "synthetic-parity corpus" with "behavioral corpus" (and, where the sentence says "see orders/validate.go", keep the cross-file pointer as-is). Do NOT change any Go code, any other comment, or the case-caption references (the "case (a)/(b)/(c)" labels are used by the case map and the golden transcript names — they stay). Every comment now says the behavioral corpus, matching the Phase-2 renamed directory and the D-03 vocabulary. This is a comment-text reword: one English phrase per file, four files total.
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -c "synthetic-parity corpus" corpus/behavioral/src/ | awk -F: "{s+=$2} END {print s+0}"); echo "synthetic-parity mentions left in src: $n"; test "$n" = "0"; rg -c "behavioral corpus" corpus/behavioral/src/accounts/manager.go corpus/behavioral/src/accounts/validate.go corpus/behavioral/src/orders/validate.go corpus/behavioral/src/recovery/recovery_test.go | awk -F: "{s+=$2} END {print s+0}" | grep -q "4"'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "synthetic-parity corpus" corpus/behavioral/src/` returns 0
    - exactly four src files carry "behavioral corpus" in their corpus comment (all four touched)
    - no Go code, type, symbol, or case-caption changed (`git diff --stat` on the four files shows comment-only edits)
  </acceptance_criteria>
  <done>The four corpus source comments now name the behavioral corpus; no code changed; the case map is untouched.</done>
</task>

<task type="auto">
  <name>Re-freeze the two behavioral goldens via task golden:regen and verify the self-consistent suite</name>
  <files>corpus/behavioral/go-explore-multi.json, corpus/behavioral/go-node-multi.json, Taskfile.yml (run target only)</files>
  <read_first>
    Taskfile.yml — the golden:regen target (~:70) that runs corpora fetch + assert + gocapture, and test:golden (~:59) that runs `go test -count=1 ./testdata/golden/...`
    The current corpus/behavioral/go-explore-multi.json + go-node-multi.json — so you can confirm the JSON content after regen matches the reworded source comments exactly
  </read_first>
  <action>
    Run the re-freeze loop:
    1. `task golden:regen` — this fetches/verifies the locked corpora (must be available from the corpora cache or network; the target fails closed if the corpora are missing per FIXT-02) and regenerates the Go-side golden JSON.
    2. Inspect `git status`/`git diff` — the ONLY expected diff is: the four corpus src comments from task 1 PLUS the two or few behavior JSON transcripts whose embedded comment bytes changed. If a larger set of golden files changed (e.g. all of testdata/golden/corpus/*), STOP — that means the capture is nondeterministic or the locked corpora state is wrong, and the correct response is to revert and report, not to commit a re-baseline.
    3. Sanity that the JSON change is the comment reword only: e.g. diff the JSON and confirm the "synthetic-parity corpus" strings are now "behavioral corpus" inline.
    4. Run `go test -count=1 ./testdata/golden/...` and `task test:golden` — green.
    Do NOT run `go mod tidy` or touch other files; do NOT accept a full re-baseline.
  </action>
  <verify>
    <automated>bash -c 'set -e; task golden:regen >/dev/null 2>&1; n=$(git diff --name-only | wc -l | tr -d " "); echo "files in diff: $n"; rg -l "synthetic-parity corpus" corpus/behavioral/*.json | wc -l | grep -qx "0"; go test -count=1 ./testdata/golden/... >/dev/null; task test:golden >/dev/null; echo redefine green and consistent"'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "synthetic-parity corpus" corpus/behavioral/*.json` returns zero (the two transcripts contain the new name inline)
    - `git diff --stat` shows ONLY: the 4 src comments + the 2 behavioral .json + (at most) the case-map if embedded — no locked- (hugo-guva…) golden changed
    - `go test -count=1 ./testdata/golden/...` green
    - the golden scenario count (TestScenarioCountIsExact) unchanged
    - if an unexpected diff appears in locked corpora — revert and STOP (report the mismatch as evidence of a capture/nondeterminism problem)
  </acceptance_criteria>
  <done>The behavioral goldens are consistent with the reworded corpus source: their embedded bytes now read behavioral; the golden suite passes and the scenario count is unchanged.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| 4 corpus src comments | edited | accounts/manager.go, accounts/validate.go, orders/validate.go, recovery/recovery_test.go — "synthetic-parity corpus" -> "behavioral corpus" |
| go-explore-multi.json / go-node-multi.json | re-frozen | the two behavioral transcripts re-generated so the embedded source comment bytes match |
| (locked corpora transcripts) | untouched | the regen target re-captures the two behavioral outputs; only the two behavioral JSON files change in the diff |

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus src comment bytes ↔ frozen transcript | the transcript EMBEDS the src, so a comment edit without re-freeze leaves a stale frozen golden that goes red on the same corpus; this plan always re-freezes right after rewording |
| same-phase corpus/golden change ↔ a general comment sweep | the CONTEXT D-07 discipline: a corpus change and its re-freeze must be a self-consistent unit, never the same unit as an unrelated comment change — it is isolated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------------|-----------------|
| T-05-21 | Tampering | the corpus reword without re-freeze | high | mitigate | the two tasks are bound — task 2 runs `task golden:regen` and verifies the golden log; a comment-only change without the goldens would fail the check. |
| T-05-22 | DoS (silent) | a full golden re-baseline slipping in | high | mitigate | the verify checks only the four-src + two-json arrow is in the diff (STOP on anything else) |
| T-05-23 | Tampering | accidental case-caption change | medium | mitigate | the case labels (case (a)/(b)/(c), names) are spelled out as no-touch (task 1 action) |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | no new dependencies — corpora fetch is the already-wired locked-tree target (FIXT-02); no install gate applies |

<verification>
- After both tasks: `task golden:regen` green, `go test -count=1 ./testdata/golden/...` green, `git diff --stat` narrow.
- corpus/behavioral/* contains zero "synthetic-parity" strings.
</verification>

<success_criteria>
- The corpus source and its goldens agree byte-for-byte on the behavioral name (no stale frozen oracle).
- No golden changed beyond the two behavioral files; no re-baseline was taken.
- The golden suite passes with `-count=1`.
</success_criteria>

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-06-SUMMARY.md` when done
</output>