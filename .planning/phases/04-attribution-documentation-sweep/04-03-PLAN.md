---
phase: 04-attribution-documentation-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - docs/LANGUAGE-CAPABILITY-MATRIX.md
autonomous: true
requirements: [DOCS-01, DOCS-03, DOCS-04]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader of CONTRIBUTING.md encounters no comparison framing: the Issue-first rationale (:11-14) no longer says the project 'ports observable behavior from another implementation' or records 'parity decisions' (and the dangling docs/FLAG-PARITY.md link at :13 is gone — a DOCS-02 site owned here because it lives inside the same paragraph); the golden list at :83 drops the stale 'golden parity' name; the Tests paragraph (:191) no longer claims goldens 'diff against frozen TypeScript CodeGraph v1.3.1 output' (DOCS-01, DOCS-02 site 3)."
    - "README.md's DOCS-01 rows are already handled by plan 04-01 (Performance words + Status claim) — this plan verifies them, it does not re-edit README."
    - "SECURITY.md, CODE_OF_CONDUCT.md, and PARSER-DECISION.md are verified clean (census rows all CLEAN / false positives) and receive NO edits (DOCS-01)."
    - "docs/LANGUAGE-CAPABILITY-MATRIX.md states per-language capability on codegraph-go's own terms: the stale 'golden-parity' names at :13 and :192 are reworded, the 'reference implementation' positioning of Go plus the retired `weft` corpus at :59-60 is rewritten on own terms, the :181 'original' KEEP row is recorded with its reason — and NO coverage value or gap bullet changes, so matrix_test.go stays green (DOCS-03)."
    - "docs/RELEASE.md, docs/RELEASE-PROCEDURES.md, docs/MCP-2026-07-28-SCOPING.md, and docs/MCP-8-AGENT-AUDIT.md carry no retired framing: the two RELEASE-PROCEDURES KEEP rows (:60 historical branch name, :581 process-'upstream') and the product-truth rows (RELEASE:345 TS/JSX grammar, CORPUS-MEASUREMENT:121/:198 TS/JS corpus data) are recorded with reasons, not edited (DOCS-04, D-05)."
    - "The concurrency predicate (DOCS-04 specless edge): this sweep is term-by-term sequential edits across independent docs; a CI run mid-sweep observes a partial sweep. The phase gate (positive-controlled rg per term + go test -count=1 ./...) is the only point at which the no-framing end-state is asserted."
  artifacts:
    - CONTRIBUTING.md — Issue-first rationale rewritten (:11-14), golden list reworded (:83), Tests paragraph rewritten (:191)
    - docs/LANGUAGE-CAPABILITY-MATRIX.md — legend reworded (:13), Go row rewritten on own terms (:59-61), shared-caveats line reworded (:192); :181 recorded KEEP
  key_links:
    - "capability matrix doc ↔ matrix.go / matrix_test.go — the byte-for-byte coverage contract must survive; a reword touching a coverage value or gap bullet fails the matrix test (research: matrix_test.go asserts values + gap bullets match matrix.go exactly)."
    - "CONTRIBUTING Issue-first paragraph ↔ the retained `.planning/` reference — the rewrite drops the FLAG-PARITY link but keeps `.planning/` as the place decisions are recorded (research: referencing .planning/ is fine; rewriting it is out of scope)."
  prohibitions:
    - "no coverage-value or gap-bullet changes in the capability matrix"
    - "no TS/TSX/JS indexed-language row removal or de-listing"
    - "no regex sweep over 'TypeScript'/'TS' (breaks tsextract and de-lists indexed languages)"
    - "no edits to SECURITY.md, CODE_OF_CONDUCT.md, PARSER-DECISION.md, docs/RELEASE.md, docs/MCP-2026-07-28-SCOPING.md, or docs/MCP-8-AGENT-AUDIT.md (verified clean — verify, don't edit)"
    - "no touch to docs/BENCHMARKS.md (Phase 6) or docs/CLI-REFERENCE.md (DOCS-05, deferred)"
    - "no touch to the golden-parity code comments in internal/indexer/capability/matrix.go / matrix_test.go (Phase 5 CODE-01)"
---

<objective>
Sweep the doc surfaces that carry comparison framing (DOCS-01 CONTRIBUTING rows, DOCS-03 capability matrix rows), verify the already-clean surfaces carry none (DOCS-01 SECURITY/CoC/PARSER-DECISION; DOCS-04 remaining docs/*), and record every KEEP-with-reason decision. The README DOCS-01 rows land in plan 04-01; this plan owns CONTRIBUTING.md and the capability matrix, and verify-only all other in-scope doc surfaces.

Purpose: DOCS-01/03/04 are the phase's breadth sweep — the census identified framing in exactly 4 files (README handled by 04-01, CONTRIBUTING and the matrix here), and verified the rest are already clean. This plan removes the framing from the two files it owns and records the per-instance reasons for every borderline KEEP so a reviewer can see why each reference was retained (D-04: term-by-term with recorded reasons, never regex). The capability-matrix reword must preserve the doc↔code byte-for-byte contract, and the CONTRIBUTING rewrite must drop the dangling FLAG-PARITY link that lives inside the Issue-first paragraph (DOCS-02 site 3).

Output: CONTRIBUTING.md and the capability matrix read on codegraph-go's own terms; the verify-only surfaces confirmed clean with recorded KEEP reasons; `go test -count=1 ./...` green including the capability matrix test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attribution-documentation-sweep/04-CONTEXT.md
@.planning/phases/04-attribution-documentation-sweep/04-RESEARCH.md
@.planning/phases/04-attribution-documentation-sweep/04-VALIDATION.md

# The two files this plan edits (read current state first — Phase 2 touched the matrix)
@CONTRIBUTING.md (:11-14 the Issue-first rationale, :83 the golden list, :191 the Tests paragraph)
@docs/LANGUAGE-CAPABILITY-MATRIX.md (:12-13 the coverage legend, :59-61 the Go row, :181 the fwcd KEEP row, :192 the shared-caveats line)

# The verify-only surfaces (census rows confirm clean — read to confirm, do not edit)
@SECURITY.md, CODE_OF_CONDUCT.md, PARSER-DECISION.md
@docs/RELEASE.md (:345 KEEP — TS/JSX grammar coverage), docs/RELEASE-PROCEDURES.md (:60 KEEP — historical branch name, :581 KEEP — process 'upstream'), docs/MCP-2026-07-28-SCOPING.md, docs/MCP-8-AGENT-AUDIT.md, docs/CORPUS-MEASUREMENT.md (:121, :198 KEEP — TS/JS corpus data)
</context>

<tasks>

<task type="auto">
  <name>CONTRIBUTING.md rewrite — Issue-first rationale, golden list, Tests paragraph</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    CONTRIBUTING.md — the Issue-first section (:6-30) containing the ports/parity paragraph (:11-14), the Approval gates / workflow list (:75-95) containing the 'golden parity' leg at :83, and the Tests section (:184-192) containing the TS-diff claim at :190-191
  </read_first>
  <action>
    Make three coordinated rewrites to CONTRIBUTING.md:

    1. Issue-first rationale (:11-14). Current text: "This is not bureaucracy for its own sake: this project ports observable behavior from another implementation, and a change that looks like an obvious improvement is sometimes a deliberate parity decision recorded in `docs/FLAG-PARITY.md` or `.planning/`. The issue is where we find that out before you've written the code." Rewrite on codegraph-go's own terms: the project has a documented behavioral contract (the golden-corpus suite), and a change that looks like an obvious improvement can be a deliberate behavior decision that must be surfaced before the code is written. The rewrite MUST (a) drop "ports observable behavior from another implementation", (b) drop "parity decision" and the word "parity", (c) drop the `docs/FLAG-PARITY.md` link (this is DOCS-02 site 3 — the dangling reference to the deleted doc lives inside this paragraph), and (d) may keep "or `.planning/`" (referencing `.planning/` as the place decisions are recorded is fine — rewriting it is out of scope). Keep the paragraph's purpose: the issue is where an obvious-improvement-vs-deliberate-decision conflict is found out before code is written.

    2. Golden list (:83). Current text: "`task test` covers every host-only leg — unit, golden parity, subprocess integration, isolated daemon, and race". Reword "golden parity" to "golden" (the golden test files are now `behavioral_*.go`, renamed by CODE-02 in Phase 2) — the leg is the golden suite, not "golden parity".

    3. Tests paragraph (:190-191). Current text: "The suite is a mix of unit tests, golden-corpus behavioral fixtures diffed against frozen TypeScript CodeGraph v1.3.1 output, and workflow-shape guards that parse the real `.yml` files." Rewrite the middle clause to describe the CURRENT harness truthfully — the goldens no longer diff against TS v1.3.1 output (FIXT-04/06 removed that capture path; the suite now asserts named behavioral properties over the in-repo behavioral corpus, frozen from codegraph-go's own output). At your discretion: "The suite is a mix of unit tests, golden-corpus behavioral fixtures that assert named properties over the in-repo behavioral corpus, and workflow-shape guards that parse the real `.yml` files." The rewrite must drop "diffed against frozen TypeScript CodeGraph v1.3.1 output" and must not invent a harness that does not exist.

    Do NOT touch anything else in CONTRIBUTING.md — the census found framing only at these three rows. Do NOT touch the `.planning/` references elsewhere (out of scope). Do NOT use a regex replacement.
  </action>
  <verify>
    <automated>bash -c 'set -e; rg -n "ports observable behavior|parity|FLAG-PARITY|frozen TypeScript CodeGraph" CONTRIBUTING.md | wc -l | grep -qx "0"; rg -c "golden, subprocess integration" CONTRIBUTING.md | grep -qx "1"; rg -c "Issue first" CONTRIBUTING.md >/dev/null; CGO_ENABLED=1 go test -count=1 ./... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ports observable behavior|parity|FLAG-PARITY|frozen TypeScript CodeGraph" CONTRIBUTING.md` returns zero hits — positive-controlled before the edit (`git show HEAD:CONTRIBUTING.md | rg -c "parity"` returns 2)
    - `rg -c "golden, subprocess integration" CONTRIBUTING.md` returns 1 (the reworded leg list)
    - the `## Issue first` heading and the `.planning/` mention survive
    - `CGO_ENABLED=1 go test -count=1 ./...` exits 0
  </acceptance_criteria>
  <done>CONTRIBUTING.md's three census rows carry no comparison framing and no FLAG-PARITY reference; the Issue-first rationale still stands but on codegraph-go's own terms.</done>
</task>

<task type="auto">
  <name>Capability matrix reword — golden-parity names, Go row on own terms, shared-caveats line</name>
  <files>docs/LANGUAGE-CAPABILITY-MATRIX.md</files>
  <read_first>
    docs/LANGUAGE-CAPABILITY-MATRIX.md — the coverage legend (:12-13, "golden-parity test"), the Coverage Table (:21-41, coverage values MUST NOT change), the Go row under Per-Language Gaps (:59-61, "Go is the reference implementation ... pinned `weft` golden-parity corpus"), the Kotlin row (:149, the `original` fwcd reference at :180-183), and the shared-caveats line (:192, "golden-parity/self-consistency tests")
  </read_first>
  <action>
    Make three doc-side rewrites to docs/LANGUAGE-CAPABILITY-MATRIX.md and record one KEEP:

    1. Coverage legend (:13): change "full = validated end-to-end (priority-4 via a corresponding golden-parity test in `testdata/golden/`, mainstream-tier via that axis's own dedicated test coverage)" to "full = validated end-to-end (priority-4 via a corresponding golden corpus behavioral test in `testdata/golden/`, mainstream-tier via that axis's own dedicated test coverage)" — drop the stale "golden-parity" name (the tests are `behavioral_csharp_test.go` etc., `TestCorpusBehavior_*`).

    2. Go row (:59-61): current text: "No named gaps — Go is the reference implementation, validated end-to-end since Phase 2/3 against the pinned `weft` golden-parity corpus (`testdata/golden/behavioral_test.go#TestCorpusBehavior_Go`)." Rewrite on own terms: "No named gaps — Go is the first-class language for the in-repo behavioral corpus, validated end-to-end since Phase 2/3 (`testdata/golden/behavioral_test.go#TestCorpusBehavior_Go`)." This drops BOTH the "reference implementation" positioning and the retired `weft` corpus name (FIXT-04 removed weft; TestCorpusBehavior_Go runs over corpus/behavioral). Do NOT change the `#TestCorpusBehavior_Go` anchor.

    3. Shared-caveats line (:192): change "golden-parity/self-consistency tests do prove resolves" to "golden/self-consistency tests do prove resolves" — drop the stale "golden-parity" name.

    4. KEEP with a recorded reason (:180-183 Kotlin row): the "original `fwcd` recommendation" phrase uses "original" in the sense of the earlier research-doc recommendation, NOT CodeGraph-the-original-project — no edit, but record the reason in the SUMMARY.

    CRITICAL: do NOT change any coverage value or gap bullet — matrix_test.go asserts every coverage value and gap bullet matches internal/indexer/capability/matrix.go byte-for-byte. The legend and prose sentences above are NOT coverage values or gap bullets, so editing them is safe, but verify the matrix test afterwards. Do NOT touch the `golden-parity` CODE comments in internal/indexer/capability/matrix.go:18,32 or matrix_test.go:217,221 (Phase 5 CODE-01). Do NOT touch the TSX/TS/JS rows (indexed-language product truth, D-05). Do NOT touch docs/BENCHMARKS.md.
  </action>
  <verify>
    <automated>bash -c 'set -e; rg -n "golden-parity|reference implementation|\bweft\b" docs/LANGUAGE-CAPABILITY-MATRIX.md | wc -l | grep -qx "0"; CGO_ENABLED=1 go test -count=1 ./internal/indexer/capability/... >/dev/null; CGO_ENABLED=1 go test -count=1 ./... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "golden-parity|reference implementation|\bweft\b" docs/LANGUAGE-CAPABILITY-MATRIX.md` returns zero hits (all three stale terms gone from the doc)
    - `CGO_ENABLED=1 go test -count=1 ./internal/indexer/capability/...` exits 0 (matrix_test.go — the byte-for-byte coverage contract — survived)
    - the Coverage Table rows (go/java/csharp/python/typescript/tsx/javascript/...) are byte-identical to HEAD (no value changed)
    - the TSX/TS/JS rows and the Kotlin `original` sentence survive unchanged
    - the SUMMARY records the :180-183 KEEP reason ("original" = earlier research recommendation, not CodeGraph-the-original)
  </acceptance_criteria>
  <done>The capability matrix states per-language capability on codegraph-go's own terms — no golden-parity/reference-implementation/weft prose, all coverage values and gap bullets byte-identical, and matrix_test.go green.</done>
</task>

<task type="auto">
  <name>Verify-only sweep — SECURITY/CoC/PARSER-DECISION (DOCS-01) and remaining docs/* (DOCS-04)</name>
  <files>SECURITY.md, CODE_OF_CONDUCT.md, PARSER-DECISION.md, docs/RELEASE.md, docs/RELEASE-PROCEDURES.md, docs/MCP-2026-07-28-SCOPING.md, docs/MCP-8-AGENT-AUDIT.md, docs/CORPUS-MEASUREMENT.md</files>
  <read_first>
    SECURITY.md — census row CLEAN (all matched lines are "report"/"reported"/"derived from the workflow" false positives)
    CODE_OF_CONDUCT.md — census row CLEAN ("reported" false positives only)
    PARSER-DECISION.md — census row CLEAN (tight regex: zero hits)
    docs/RELEASE.md — :345 KEEP (TS/JSX grammar coverage — product truth, D-05)
    docs/RELEASE-PROCEDURES.md — :60 KEEP (historical branch name `gsd/v1.0-drop-in-parity-human-ux`, a proper noun), :581 KEEP ("upstream" in the process/git sense)
    docs/MCP-2026-07-28-SCOPING.md — census row CLEAN (all hits are codegraph-go/codegraph_* product references)
    docs/MCP-8-AGENT-AUDIT.md — census row CLEAN (all hits are codegraph binary/tool references)
    docs/CORPUS-MEASUREMENT.md — :121, :198 KEEP (TypeScript/JS-as-indexed-language corpus data — product truth, D-05)
  </read_first>
  <action>
    VERIFY ONLY — make NO edits to any file in this task. For each surface, run the term census (word-boundary-aware: "parity", "the original", "upstream", "drop-in", "head-to-head", "vs TS", "based on", "ported from", "reimplementation", "port(s)", "TS original", "reference implementation", "derived from") and classify every hit as one of: CLEAN-false-positive (e.g. "report"/"reported"/"supported" matching the loose port pattern), KEEP-with-reason (the enumerated rows), or COMPARISON (which would be a census miss — report it, do not silently edit).

    Expected outcomes:
    - SECURITY.md, CODE_OF_CONDUCT.md, PARSER-DECISION.md, docs/MCP-2026-07-28-SCOPING.md, docs/MCP-8-AGENT-AUDIT.md: zero COMPARISON hits — confirm clean, no edits.
    - docs/RELEASE.md:345 "covers TS/JSX parsing" — KEEP (TypeScript-the-indexed-language grammar coverage, product truth D-05), record reason.
    - docs/RELEASE-PROCEDURES.md:60 "`gsd/v1.0-drop-in-parity-human-ux`" — KEEP (quoted historical branch name; rewording would falsify the record), record reason. :581 "nothing upstream of it" — KEEP ("upstream" in the process/git sense, not comparison framing), record reason.
    - docs/CORPUS-MEASUREMENT.md:121 "typescript: 1684" and :198 "TS/JS is supplied by gohugoio/hugo's javascript files" — KEEP (indexed-language corpus data, product truth D-05), record reason.

    Record every KEEP reason in the SUMMARY (the VALIDATION manual-verification row requires "each borderline reference classified with a reason"). If a COMPARISON hit is found in a surface the census marked clean, STOP and report it in the SUMMARY as a census miss rather than editing (D-04: never regex, term-by-term with recorded reasons).
  </action>
  <verify>
    <automated>bash -c 'set -e; for f in SECURITY.md CODE_OF_CONDUCT.md PARSER-DECISION.md docs/RELEASE.md docs/RELEASE-PROCEDURES.md docs/MCP-2026-07-28-SCOPING.md docs/MCP-8-AGENT-AUDIT.md docs/CORPUS-MEASUREMENT.md; do git diff --quiet -- "$f" || { echo "$f CHANGED"; exit 1; }; done; echo "all verify-only surfaces unchanged"'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- SECURITY.md CODE_OF_CONDUCT.md PARSER-DECISION.md docs/RELEASE.md docs/RELEASE-PROCEDURES.md docs/MCP-2026-07-28-SCOPING.md docs/MCP-8-AGENT-AUDIT.md docs/CORPUS-MEASUREMENT.md` shows zero changes (no edits)
    - the SUMMARY records each KEEP-with-reason row (RELEASE:345, RELEASE-PROCEDURES:60/:581, CORPUS-MEASUREMENT:121/:198) and confirms the five CLEAN surfaces carry no COMPARISON framing
    - `CGO_ENABLED=1 go test -count=1 ./...` exits 0 (unchanged tree)
  </acceptance_criteria>
  <done>All verify-only surfaces confirmed clean (or KEEP-with-reason recorded), zero edits made to them, and the doc-sweep leaves no comparison framing in any in-scope doc.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| CONTRIBUTING.md Issue-first rationale | edited | `CONTRIBUTING.md` :11-14 — ports/parity/FLAG-PARITY framing dropped, on own terms |
| CONTRIBUTING.md golden list | edited | `CONTRIBUTING.md` :83 — "golden parity" → "golden" |
| CONTRIBUTING.md Tests paragraph | edited | `CONTRIBUTING.md` :190-191 — TS-diff claim rewritten to the current fail-loud suite |
| Capability matrix coverage legend | edited | `docs/LANGUAGE-CAPABILITY-MATRIX.md` :13 — "golden-parity test" → "golden corpus behavioral test" |
| Capability matrix Go row | edited | `docs/LANGUAGE-CAPABILITY-MATRIX.md` :59-61 — "reference implementation ... pinned weft golden-parity corpus" → own-terms first-class-language statement |
| Capability matrix shared caveat | edited | `docs/LANGUAGE-CAPABILITY-MATRIX.md` :192 — "golden-parity/self-consistency" → "golden/self-consistency" |
| KEEP-with-reason records | recorded | SUMMARY — RELEASE:345, RELEASE-PROCEDURES:60/:581, CORPUS-MEASUREMENT:121/:198, matrix :180-183 |
| Verify-only surfaces (unchanged) | verified | SECURITY.md, CODE_OF_CONDUCT.md, PARSER-DECISION.md, docs/RELEASE.md, docs/RELEASE-PROCEDURES.md, docs/MCP-2026-07-28-SCOPING.md, docs/MCP-8-AGENT-AUDIT.md, docs/CORPUS-MEASUREMENT.md — confirmed clean, zero edits |

## Spec-less probe resolutions (COVERAGE)

The 6 applicable probe edges, per the fallback contract (no 04-SPEC.md):

- **DOCS-04/concurrency** → resolved explicit: this sweep is term-by-term sequential edits across independent docs; a CI run mid-sweep sees a partial sweep. The phase gate (positive-controlled rg per term + `go test -count=1 ./...`) is the only point at which the no-framing end-state is asserted — authored into this plan's `must_haves.truths` and the phase `<verification>`.
- **The 5 unclassified edges (ATTR-01/02/03, DOCS-01, DOCS-03)** → stay flagged unresolved: doc-editing requirements have no probe-shaped edge category. Not auto-dismissed — the verification for each is the static positive-controlled `rg` sweep plus the live license check, tracked here so nothing drops.

**6 counted: 1 concurrency explicit + 5 flagged.**

## Deferred to Phase 5 (flagged, not touched)

- The `golden-parity` **code comments** in `internal/indexer/capability/matrix.go:18,32` and `matrix_test.go:217,221` — Phase 5 CODE-01; this plan reworded only the doc rows.
- The `parity`/`ports` framing in the `.github` templates and `internal/mcp` comments — Phase 5 PROC-01/02/03, CODE-01 (dangling links removed in plan 04-02).
- `syntheticParitySrc` at `testdata/golden/behavioral_test.go:239` — Phase 5 CODE-01/CODE-02 follow-up (flagged in plan 04-01).

## Assumption Delta Decision

```json
{
  "kind": "pluralization",
  "term": "another",
  "snippet": "… on codegraph-go's own terms without reference to another implementation's coverage (DOCS-01, DOCS-03, DOCS…)",
  "noun_now_primary": "codegraph-go's own capability (singular)",
  "decision": "no-change",
  "rationale": "Assessed as a false positive. The matched word \"another\" describes the implementation being REMOVED from the description (\"without reference to another implementation's coverage\" is a prohibition on comparison framing, not a singular→plural transition in the phase's identity model). The phase's identity model is singular throughout — codegraph-go as a standalone project acknowledging its origin once — so no promote or add-alongside action is warranted. The 5 unclassified probe edges remain flagged separately per the specless-probe contract; this dismissal is recorded, not silent."
}
```

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability matrix doc → matrix.go / matrix_test.go | The doc is the human-readable half of a byte-for-byte coverage contract; an edit crossing into a coverage value or gap bullet breaks matrix_test.go. |
| CONTRIBUTING doc → contributor expectations | The Issue-first paragraph is the contract contributors read; the rewrite must drop comparison framing without weakening the issue-before-PR rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | DoS (capability loss) | capability matrix reword (DOCS-03) | high | mitigate | TS/TSX/JS rows are product truth (D-05) and coverage values are contract-locked: the action forbids row/value/gap-bullet changes and the verify runs `go test -count=1 ./internal/indexer/capability/...` — a de-listing or a value drift fails the matrix test. |
| T-04-09 | Tampering | CONTRIBUTING Issue-first rewrite (DOCS-01) | medium | mitigate | The rewrite drops the dangling FLAG-PARITY link (DOCS-02 site 3) inside the same paragraph while keeping the `.planning/` reference and the paragraph's purpose; the verify asserts zero parity/ports/FLAG-PARITY hits in CONTRIBUTING.md. |
| T-04-10 | Tampering | verify-only surfaces edited despite CLEAN census | medium | mitigate | The third task's verify is a `git diff --quiet` over all 8 verify-only surfaces — any edit fails the task; the action instructs reporting a census miss rather than editing (D-04). |
| T-04-11 | Tampering | stale rewrites inventing a nonexistent harness | medium | mitigate | The three stale rows (CONTRIBUTING:191, matrix:59-60, plus README:189 in 04-01) must describe the current fail-loud behavioral suite truthfully (FIXT-04/06 changed the harness); the actions name the current reality explicitly. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No new external packages this phase — in-git markdown edits only. Package-legitimacy gate N/A. |
</threat_model>

<verification>
- `rg -n "ports observable behavior|parity|FLAG-PARITY|frozen TypeScript CodeGraph" CONTRIBUTING.md` — empty
- `rg -n "golden-parity|reference implementation|\bweft\b" docs/LANGUAGE-CAPABILITY-MATRIX.md` — empty
- `CGO_ENABLED=1 go test -count=1 ./internal/indexer/capability/...` and `CGO_ENABLED=1 go test -count=1 ./...` — green
- `git diff --quiet` over the 8 verify-only surfaces — no edits
- the SUMMARY records every KEEP-with-reason row and confirms the five CLEAN surfaces carry no COMPARISON framing
</verification>

<success_criteria>
- CONTRIBUTING.md and the capability matrix read on codegraph-go's own terms, with every borderline reference classified and recorded (D-04).
- The verify-only surfaces (SECURITY, CoC, PARSER-DECISION, and the remaining docs/*) carry no retired framing — confirmed, not edited.
- The capability matrix's byte-for-byte contract with matrix.go survives (matrix_test.go green), and the TS/TSX/JS indexed-language rows are intact (D-05).
</success_criteria>

<output>
Create `.planning/phases/04-attribution-documentation-sweep/04-03-SUMMARY.md` when done
</output>
