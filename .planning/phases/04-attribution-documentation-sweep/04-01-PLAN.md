---
phase: 04-attribution-documentation-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - NOTICE
  - README.md
autonomous: true
requirements: [ATTR-01, ATTR-02, ATTR-03]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "NOTICE contains exactly the MIT copyright transcription (verbatim 'Copyright (c) 2026 Colby Mchenry'), the LICENSE warning block (NOTICE:3-10), the Third-party dependencies section (NOTICE:52-69), and ONE sentence of origin — with the drop-in / ported-heuristics / flag-parity / benchmark argument (NOTICE:14-37) gone (ATTR-01, D-01, D-05)."
    - "README.md has no `## Relationship to the original` section; its only origin mention is one past-tense clause inside `## License` linking to NOTICE (ATTR-02, D-02)."
    - "GitHub's license detection reports MIT live after the NOTICE edit — `gh api repos/seanb4t/codegraph-go/license --jq '.license.spdx_id'` returns MIT, not assumed — AND the working-tree LICENSE is byte-identical to HEAD (`git diff --exit-code HEAD -- LICENSE`), because the API reads hosted state, not the uncommitted tree (ATTR-03)."
    - "The two extra README framing sites beyond the named section are swept words-only: Performance (:157 'it ports', :163 'the TS original', :166-167 'including the flattering one') and Status (:189 the stale 'Behavioral parity with TypeScript CodeGraph v1.3.x' claim) — the numbers table stays (Phase 6 retires it) and `codegraph migrate` (:137) plus the TS/TSX/JS language rows (:149-153) survive (DOCS-01, D-05)."
    - "The concurrency predicate (DOCS-04 specless edge): the NOTICE trim is a term-by-term sequential edit — a CI run mid-edit observes a partial state; the phase gate (full-tree rg + go test -count=1 + live gh api) is the only point at which the attribution end-state is asserted."
  artifacts:
    - NOTICE — Attribution section rewritten: warning block kept, one origin sentence, verbatim transcription, fidelity guard, Third-party deps kept
    - README.md — Relationship section removed, License clause added, Performance words swept, Status parity claim rewritten on own terms
  key_links:
    - "NOTICE transcription ↔ the one-sentence origin — the legal acknowledgment pair; deleting either is a licence-compliance error (T-04-01)."
    - "README ## License clause → NOTICE link — the only origin mention allowed in README."
    - "README migrate row (:137) + TS/TSX/JS rows (:149-153) ↔ the sweep — product truth that must survive (D-05)."
  prohibitions:
    - "no LICENSE edit — it must stay verbatim MIT (NOASSERTION history, NOTICE:3-10)"
    - "no deletion of the MIT transcription, the LICENSE warning block, or the Third-party dependencies section"
    - "no sweeping of `codegraph migrate` or the TypeScript/TSX/JavaScript language rows"
    - "no regex replacement over 'TypeScript'/'TS' (breaks tsextract and de-lists indexed languages)"
---

<objective>
Trim NOTICE to the legal minimum — MIT copyright transcription plus ONE sentence of origin, with the drop-in / ported-heuristics / flag-parity / benchmark argument removed (ATTR-01) — and make README's only origin mention a single past-tense clause inside `## License` linking to NOTICE, with the `## Relationship to the original` section gone and the two additional framing sites (Performance words + Status parity claim) swept (ATTR-02). Verify live that LICENSE is still detected as MIT after the change (ATTR-03).

Purpose: This is the phase's core legal act and its tracer. The NOTICE trim is the single path that exercises the phase's entire verification machinery — positive-controlled vocabulary greps, `go test -count=1 ./...` green (proving the FLAG-PARITY blast radius is safe to touch in plan 02), and the live `gh api` license check — on the most delicate surface in the repo (the one where an earlier edit downgraded detection to NOASSERTION). Proving this loop on NOTICE de-risks every later doc edit.

Output: NOTICE holds transcription + one origin sentence and nothing else of the argument; README holds one past-tense origin clause and no Relationship section; the live license check returns MIT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attribution-documentation-sweep/04-CONTEXT.md
@.planning/phases/04-attribution-documentation-sweep/04-RESEARCH.md
@.planning/phases/04-attribution-documentation-sweep/04-VALIDATION.md

# The two surfaces being edited (read in full first)
@NOTICE (warning block :3-10, attribution argument :14-37, transcription :39-46, fidelity guard :48-50, Third-party deps :52-69)
@README.md (Commands table :130-141 incl. the migrate row at :137, Language support :143-153, Performance :155-169, Status :185-191, Relationship :193-202, License :218-220)

# The unchanged license file (verify-only — never edited)
@LICENSE (verbatim MIT, 21 lines)
</context>

<tasks>

<task type="tracer">
  <name>NOTICE attribution trim — remove the drop-in/ported-heuristics/flag-parity argument, keep transcription + one origin sentence</name>
  <files>NOTICE</files>
  <read_first>
    NOTICE — the full current content (:1-70): the LICENSE warning block (:3-10), the Attribution section (:12-50) containing the argument (:14-37), the MIT transcription (:39-46), the transcription-fidelity guard (:48-50), and the Third-party dependencies section (:52-69)
  </read_first>
  <action>
    Rewrite NOTICE's `## Attribution` section (NOTICE:12-50) so the section holds exactly three things and nothing else:

    1. ONE sentence of origin (D-01): "CodeGraph Go began as a ground-up Go rewrite of CodeGraph (MIT)." The URL to https://github.com/colbymchenry/codegraph may be folded into this sentence at your discretion (it must remain one sentence). Do NOT add any rationale, comparison, or "originally based on / ported from / drop-in / ported-heuristics / flag-parity" language.
    2. The MIT copyright transcription, reproduced verbatim exactly as it is today at NOTICE:39-46, inside a fenced code block:
       MIT License
       Copyright (c) 2026 Colby Mchenry
       Keep the lead-in line that says it is reproduced from the origin project's LICENSE (the "retrieved 2026-08-01 via gh api" wording may stay or be simplified, but the transcription itself must be byte-identical).
    3. The transcription-fidelity guard at NOTICE:48-50 ("Do not 'correct' the capitalization of that name..."), kept verbatim — it protects the transcription's fidelity and is not an origin argument (research Open Question 4).

    DELETE the entire argument at NOTICE:14-37: the independent-reimplementation origin statement (:14-16), the "This port reimplements its behavior" line (:18-19), the drop-in-replacement bullets (:21-30, which carry the first docs/FLAG-PARITY.md reference at :25), the "Behavioral divergences..." line (:32-33, which carries the second docs/FLAG-PARITY.md reference at :33), and the benchmark-reference paragraph (:35-37, which names docs/BENCHMARKS.md).

    DO NOT TOUCH the LICENSE warning block at NOTICE:3-10 (it directly serves ATTR-03 — it documents why LICENSE must never be edited) and DO NOT TOUCH the `## Third-party dependencies` section at NOTICE:52-69 (the SBOM/disclosure pointer; deleting it is a compliance reduction, not a framing fix).

    Also: because this task removes the two NOTICE references to docs/FLAG-PARITY.md (:25 and :33), it is a DOCS-02 reference-site sweep for those two rows — the deletion plan (04-02) is then clean of NOTICE.

    Do NOT edit LICENSE in any way. Do NOT use a regex replacement over "TypeScript"/"TS" — this is a hand-authored section rewrite.
  </action>
  <verify>
    <automated>bash -c 'set -e; test -f NOTICE; rg -c "Copyright \(c\) 2026 Colby Mchenry" NOTICE | grep -qx "1"; rg -c "began as a ground-up Go rewrite" NOTICE | grep -qx "1"; rg -c "Third-party dependencies" NOTICE | grep -qx "1"; rg -n "drop-in|\bported\b|FLAG-PARITY|BENCHMARKS" NOTICE | wc -l | grep -qx "0"; git diff --exit-code HEAD -- LICENSE; gh api repos/seanb4t/codegraph-go/license --jq .license.spdx_id | grep -qx "MIT"; CGO_ENABLED=1 go test -count=1 ./... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Copyright \(c\) 2026 Colby Mchenry" NOTICE` returns 1 (transcription kept verbatim)
    - `rg -c "began as a ground-up Go rewrite" NOTICE` returns 1 (the one origin sentence)
    - `rg -c "Third-party dependencies" NOTICE` returns 1 (deps section kept)
    - `rg -c "Do not move any of this" NOTICE` returns 1 (LICENSE warning block kept)
    - `rg -n "drop-in|\bported\b|FLAG-PARITY|BENCHMARKS" NOTICE` returns zero hits — and the grep was positive-controlled: first confirm the same pattern matches the pre-edit text (e.g. `git show HEAD:NOTICE | rg -c "drop-in"` returns 1), proving the grep can find the term when present; `\bported\b` is word-bounded so the kept LICENSE-warning block's "reported" (`NOTICE:9`) is not a false positive
    - `gh api repos/seanb4t/codegraph-go/license --jq .license.spdx_id` returns `MIT` LIVE after the edit (ATTR-03) — and `git diff --exit-code HEAD -- LICENSE` exits 0, proving the working-tree LICENSE is byte-identical to HEAD (the API reads hosted state, so the local diff is the mechanical pre-push guarantee; the live check proves hosted detection)
    - `CGO_ENABLED=1 go test -count=1 ./...` exits 0 (49 packages)
    - `git diff -- NOTICE` shows: no line of LICENSE touched; the transcription block unchanged; only the Attribution section rewritten
  </acceptance_criteria>
  <done>NOTICE reads as the LICENSE warning block, one origin sentence, the verbatim MIT transcription with its fidelity guard, and the Third-party dependencies section — with no drop-in/ported/flag-parity/benchmark argument anywhere in the file, and the live license check still reports MIT.</done>
</task>

<task type="auto">
  <name>README edits — remove Relationship section, add one License clause, sweep Performance words + Status parity claim</name>
  <files>README.md</files>
  <read_first>
    README.md — Commands table (:130-141) with the migrate row at :137 (D-05 product truth, keep), Language support table (:143-153) with the Full TS/TSX/JS rows (D-05 product truth, keep), Performance section (:155-169), Status section (:185-191), Relationship to the original (:193-202), License (:218-220)
  </read_first>
  <action>
    Make three coordinated edits to README.md:

    1. DELETE the `## Relationship to the original` section entirely (README:193-202), including the trailing blank line, per ATTR-02. Nothing below it is moved.
    2. Rewrite `## License` (README:218-220) to add ONE past-tense clause linking to NOTICE, per D-02. The section becomes: "MIT — see [LICENSE](LICENSE). This project began as a Go rewrite of CodeGraph; see [NOTICE](NOTICE) for the original copyright." (exact wording at your discretion, but it must be one clause, past tense, and name NOTICE as the origin-copyright location).
    3. Sweep the two additional framing sites beyond the named section (words-only — the numbers table belongs to Phase 6 BENCH):
       - Performance intro (:157): change "Measured against the TypeScript implementation it ports — median-of-3, on three real repositories, on the same machine:" to "Median-of-3, on three real repositories, on the same machine:" — dropping the vs-TS positioning while keeping the table.
       - Performance table row (:163): retitle "`codegraph` (the TS original)" to "`codegraph` (TypeScript)" — the repo identity is product truth, the *original* positioning is not.
       - Performance caveat (:166-167): drop the phrase "including the flattering one" — it implies the flattery is vs-TS; record the reason (census row README:166-167, BORDERLINE → remove).
       - Status (:189): rewrite the stale parity claim "Behavioral parity with TypeScript CodeGraph v1.3.x is covered by a fixture harness that diffs against frozen goldens; known divergences are logged, not hidden." on codegraph-go's own terms AND describe the current harness truthfully (the goldens are no longer TS-diffed — FIXT-04/06 removed that capture path; the suite is now named property assertions over the in-repo behavioral corpus, frozen from codegraph-go's own output). At your discretion, offer a FULLY own-terms alternative — "divergence" is itself comparison framing (it implies a baseline to diverge from) and must NOT survive in any form. Example: "Behavioral regression coverage is provided by the golden corpus suite — named property assertions over the in-repo behavioral corpus, re-frozen from codegraph-go's own output." The rewrite must drop "parity"/"TypeScript CodeGraph v1.3.x"/"diffs against frozen goldens"/"divergence(s)" and must not invent a harness that does not exist.

    DO NOT touch the Commands table (:130-141) — the `migrate` row at :137 is product truth (D-05). DO NOT touch the Language support table (:143-153) — the Full TypeScript/TSX/JavaScript rows are indexed-language product truth (D-05). DO NOT touch the numbers in the Performance table — the head-to-head figures are Phase 6's retirement, this phase sweeps only the framing words. Do NOT use a regex replacement over "TypeScript"/"TS".
  </action>
  <verify>
    <automated>bash -c 'set -e; rg -c "Relationship to the original" README.md | grep -qx "0"; rg -c "began as a Go rewrite of CodeGraph" README.md | grep -qx "1"; rg -n "the TS original|it ports|including the flattering one|Behavioral parity|divergence" README.md | wc -l | grep -qx "0"; rg -c "import an existing TypeScript CodeGraph index" README.md | grep -qx "1"; rg -c "TypeScript" README.md >/dev/null; CGO_ENABLED=1 go test -count=1 ./... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Relationship to the original" README.md` returns 0 (section gone)
    - `rg -c "began as a Go rewrite of CodeGraph" README.md` returns 1 (the one License clause)
    - `rg -n "the TS original|it ports|including the flattering one|Behavioral parity|divergence" README.md` returns zero hits — positive-controlled before the edit (`git show HEAD:README.md | rg -c "the TS original"` returns 1); the Status rewrite's own-terms text carries NO "divergence" (finding 4)
    - `rg -c "import an existing TypeScript CodeGraph index" README.md` returns 1 (migrate row preserved — D-05)
    - `rg -c "\| Full \| Go, Java, C#, TypeScript, TSX" README.md` returns 1 (language table preserved — D-05)
    - the Performance numbers table (:160-164) is byte-identical to HEAD except the :163 row label
    - `CGO_ENABLED=1 go test -count=1 ./...` exits 0
  </acceptance_criteria>
  <done>README has no Relationship section, exactly one past-tense origin clause inside ## License that links to NOTICE, no framing words in the Performance section, and a Status section describing the current fail-loud golden suite — with the migrate row and TS/TSX/JS language rows intact.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| NOTICE `## Attribution` rewrite | edited | `NOTICE` :12-50 — argument (:14-37) removed; one origin sentence + verbatim transcription (:39-46) + fidelity guard (:48-50) kept |
| NOTICE kept blocks | edited (unchanged) | `NOTICE` :3-10 LICENSE warning block, :52-69 Third-party deps — verified present after edit |
| README `## Relationship to the original` | deleted | `README.md` :193-202 — section removed entirely (ATTR-02) |
| README `## License` clause | edited | `README.md` :218-220 — one past-tense clause added linking to NOTICE |
| README Performance framing words | edited | `README.md` :157/:163/:166-167 — vs-TS words dropped, numbers table intact |
| README Status parity claim | edited | `README.md` :189 — stale TS-diff claim rewritten on own terms |

## Spec-less probe resolutions (COVERAGE)

The 6 applicable probe edges, per the fallback contract (no 04-SPEC.md):

- **DOCS-04/concurrency** → resolved explicit: the attribution edits are term-by-term sequential; a CI run mid-sweep sees a partial sweep. The phase gate (full-tree rg + `go test -count=1 ./...` + live `gh api`) is the only point at which the end-state is asserted — authored into this plan's `must_haves.truths` and the phase `<verification>`.
- **ATTR-01/unclassified, ATTR-02/unclassified, ATTR-03/unclassified, DOCS-01/unclassified, DOCS-03/unclassified** → stay flagged unresolved: doc-editing requirements have no probe-shaped edge category (the probe's edge taxonomy does not model legal-attribution trims). Not auto-dismissed — the verification for each is the static positive-controlled `rg` sweep plus the live license check, tracked here so nothing drops.

**6 counted: 1 concurrency resolved explicit + 5 flagged with reasons.**

## Deferred to Phase 5 (flagged, not touched)

- `syntheticParitySrc` at `testdata/golden/behavioral_test.go:239` — a surviving CODE-02 identifier residual, out of Phase 4's doc-only scope. Flagged here so Phase 5 (CODE-01/CODE-02 follow-up) owns it; this plan does not touch it.
- The `golden-parity` **code comments** in `internal/indexer/capability/matrix.go:18,32` and `matrix_test.go:217,221` — Phase 5 CODE-01; the *doc* rows in the capability matrix are Phase 4 (plan 04-03).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NOTICE (in-git) → LICENSE (in-git) | The NOTICE trim must never spill an attribution line into LICENSE; LICENSE is the boundary the project's own history says must not be crossed (NOASSERTION regression, NOTICE:3-10). |
| README sweep → product surface | The README sweep crosses the framing/capability boundary: it removes comparison words but must not remove the migrate row or the TS/TSX/JS indexed-language rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | NOTICE trim (ATTR-01) | high | mitigate | The action carries an explicit keep-list (transcription :39-46, LICENSE warning block :3-10, Third-party deps :52-69) and the verify positive-asserts each block's presence after the edit (`rg -c` for transcription/deps/warning = 1) — deleting licence content fails the verify. |
| T-04-02 | DoS (capability loss) | README sweep (ATTR-02) | high | mitigate | The migrate row (:137) and TS/TSX/JS rows (:149-153) are D-05 product truth; the action forbids touching the Commands/Language tables and the verify asserts the migrate row and Full TSX row still present — a sweep that de-lists a language fails. |
| T-04-ATTR03 | Tampering | LICENSE untouched (ATTR-03) | high | mitigate | LICENSE is never opened for edit; the live `gh api ... --jq .license.spdx_id` check is re-run after the NOTICE edit and must return `MIT` (the NOASSERTION history is documented at NOTICE:3-10). |
| T-04-01-P1 | Tampering | NOTICE transcription fidelity | medium | mitigate | The transcription is byte-identical to HEAD (`git diff` review criterion); the fidelity guard (:48-50) is kept. |
| T-04-04 | Tampering | doc↔product-truth boundary | medium | mitigate | Census-driven edits only (the research's README rows); no regex over "TypeScript"/"TS". |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No new external packages this phase — all edits are in-git markdown. Package-legitimacy gate N/A. |
</threat_model>

<verification>
- `CGO_ENABLED=1 go test -count=1 ./...` green (49 packages) after both tasks
- `rg -n "drop-in|ported|FLAG-PARITY|BENCHMARKS" NOTICE` empty; `rg -c "Copyright \(c\) 2026 Colby Mchenry" NOTICE` = 1; `rg -c "Third-party dependencies" NOTICE` = 1
- `rg -c "Relationship to the original" README.md` = 0; `rg -c "began as a Go rewrite of CodeGraph" README.md` = 1; `rg -n "the TS original|it ports|including the flattering one|Behavioral parity" README.md` empty
- `gh api repos/seanb4t/codegraph-go/license --jq .license.spdx_id` = `MIT` (live, after the NOTICE edit)
</verification>

<success_criteria>
- NOTICE reads as the LICENSE warning block, one origin sentence, the verbatim MIT transcription with fidelity guard, and Third-party deps — with the drop-in/ported/flag-parity/benchmark argument gone.
- README has no `## Relationship to the original`; its only origin mention is one past-tense clause inside `## License` linking to NOTICE; the Performance framing words and the Status parity claim are swept without removing the numbers table or de-listing indexed languages.
- GitHub's license detection reports MIT live after the change.
</success_criteria>

<output>
Create `.planning/phases/04-attribution-documentation-sweep/04-01-SUMMARY.md` when done
</output>
