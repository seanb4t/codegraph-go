---
phase: 06-claude-code-pretooluse-nudge
plan: 06
type: execute
wave: 6
depends_on: ["06-05"]
files_modified:
  - .planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md
autonomous: false
requirements: [NUDGE-05, NUDGE-03, NUDGE-04]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The D-18 sessions are run by the ORCHESTRATOR in sibling Herdr panes as fresh `claude --debug-file <path>` sessions — never by an executor subagent (a backgrounded subagent cannot drive another pane's TTY; the 05-06 precedent); the executor's part is the scaffold (Task 1) and the post-resume completeness gate."
    - "The scratch setup is exactly D-18's: an indexed scratch repo with a LOCAL `codegraph install --target claude --location local --pretool-nudge` from a binary built at this phase's HEAD, plus an un-indexed control repo with the same local install and no `.codegraph/`; the maintainer's global Claude configuration is not touched (`~/.claude/settings.json` sha256 identical before and after; no `~/.claude/hooks/pretooluse-nudge.sh`)."
    - "NUDGE-03's 'verified against the current hooks reference, not memory': 06-LIVE-SESSIONS.md quotes, with URL and fetch date, the current hooks reference's lines for PreToolUse `hookSpecificOutput.additionalContext`, the per-handler `if` field, the matcher grammar, exit-code-2 blocking and the default timeout, plus the `claude --version` the sessions used."
    - "D-18's pass bar, locked before any session ran, is recorded criterion by criterion in fixed lines (C1-C7) with verbatim transcript/debug-log excerpts, and `D-18 verdict: PASS` is written only when all seven are PASS."
    - "Every absence claim (C2's no-fire window, C6's zero fires, C7's zero hook errors) is backed by a grep that is first shown to FIND the pinned text or the error marker when present (the STATE standing rule: a transcript grep is a claim about the grep)."
    - "Recorded, not gated (D-18): matched-call count, fires, fire rate, true-positive share, uptake (did the agent then use codegraph_explore or `codegraph explore`), and the hook's wall time per run from the debug log; plus the five points the docs do not confirm — additionalContext with no decision, whether a subagent carries the parent's session_id, the CLAUDE_CODE_SESSION_ID export, dedup of same-command handlers with different `if` rules, and stdin key-order stability."
    - statement: "C1: in a fresh main-thread session in the indexed repo, the first matched call produces exactly one fire, visible as the added context in the transcript or debug log."
      verification: backstop
    - statement: "C2 + C3: no two fires share a key within 60 s, and after a gap of at least 60 s the next matched call fires again."
      verification: backstop
    - statement: "C4 + C5: a subagent's first matched call fires once for that subagent whatever the main thread's cooldown, and after /clear the next matched call fires."
      verification: backstop
    - statement: "C6 + C7: the un-indexed control has 0 fires, and there are 0 'hook error' notices and no permission prompt, deny or block attributable to the hook."
      verification: backstop
  artifacts:
    - path: ".planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md"
      provides: "D-18 live evidence: pre-flight, hooks-reference quotes, scaffold, per-session commands and excerpts, C1-C7 verdict lines, recorded metrics, the five points"
      contains: "D-18 verdict:"
  key_links:
    - from: "06-LIVE-SESSIONS.md `D-18 verdict:` line"
      to: "06-07-PLAN.md phase gate"
      via: "precondition read by the next plan"
      pattern: "^D-18 verdict: PASS$"
  prohibitions:
    - statement: "MUST NOT record a criterion PASS without a verbatim transcript or debug-log excerpt whose search was positive-controlled, and MUST NOT record C6 without the un-indexed control session (D-18, research pitfall 12)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT change the maintainer's global Claude Code configuration: `~/.claude/settings.json` is byte-identical after the sessions and no global guard is installed."
      status: resolved
      verification: test
    - statement: "MUST NOT patch code in this plan on a FAIL: a failed criterion is recorded verbatim and escalated to the maintainer, whose answer is recorded as a `Maintainer decision:` line before the plan resumes."
      status: resolved
      verification: judgment
---

<objective>
Settle, in genuinely fresh live Claude Code sessions, what no unit test may claim (D-00): that Claude Code
matches the registered PreToolUse handlers, delivers the added context, keys the cooldown per session and per
subagent as designed, stays silent in an un-indexed repo, and never shows a hook error or a prompt caused by
the hook — against D-18's pass bar, locked before any session runs. The executor scaffolds the scratch repos
and the evidence document; the orchestrator (which holds Herdr pane access) drives the sessions and writes the
verdicts; the executor then gates the document's completeness and commits it.

Purpose: NUDGE-05's "fire rate measured in a genuinely fresh live session", NUDGE-03's delivery and
"verified against the current hooks reference", NUDGE-04's live cooldown/subagent behaviour. The five
unconfirmed points recorded here feed CODEX-05 in Phase 7.

Ordering: after every write path, the cooldown and the dogfood registration exist (06-01…06-05), and before
the reference regeneration and phase gate (06-07).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-VALIDATION.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-05-SUMMARY.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-06-PLAN.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md
</context>

<interfaces>
Herdr (confirmed on this machine, 2026-09-19):
```bash
herdr agent start <name> --kind claude --pane <id> -- --debug-file /tmp/06-live/debug/<name>.log
herdr agent prompt <name> "<text>" --wait
herdr agent read <name> --source recent-unwrapped
```
Local: `claude` 2.1.278 at planning time (record the version actually used). Scratch fixture:
`internal/indexer/testdata/gofixture/` (symbol `Alpha` in pkga). The pinned nudge text's distinctive substring
for searches: `returns the matching symbols' source and call paths`. Session transcripts:
`~/.claude/projects/<cwd-slug>/<session-id>.jsonl` (subagent sidechains alongside, per the running version).
Sentinels: `${TMPDIR}/codegraph-nudge-$(id -u)/` (64-hex names, zero bytes).
</interfaces>

## Artifacts this phase produces

See `06-01-PLAN.md` → "Artifacts this phase produces". This plan creates `06-LIVE-SESSIONS.md`.

<!-- planner-discipline-allow: PENDING -->

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the scratch repos, the pre-flight record, the hooks-reference quotes, and the evidence document</name>
  <files>.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md (D-18 in full — the pass bar and the recorded-not-gated list; the "Current hooks reference" block)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-06-PLAN.md and 05-LIVE-SESSIONS.md (the scaffold → orchestrator → completeness-gate shape and the document layout to mirror)
    - internal/indexer/testdata/gofixture/ (the scratch fixture)
  </read_first>
  <action>
    Build and scaffold, outside the repo under /tmp/06-live (nothing under $HOME is written by this task):
    - `rm -rf /tmp/06-live && mkdir -p /tmp/06-live/debug /tmp/06-live/transcripts && GOTOOLCHAIN=go1.26.6 go build -o /tmp/06-live/codegraph ./cmd/codegraph`.
    - For `indexed` and `unindexed`: copy internal/indexer/testdata/gofixture/. into /tmp/06-live/<name>/ and `git -C <dir> init -q`. In `indexed` only: `CODEGRAPH_NO_WATCH=1 /tmp/06-live/codegraph init /tmp/06-live/indexed`. In BOTH: `(cd <dir> && /tmp/06-live/codegraph install --target claude --location local --pretool-nudge)`. Positive assertions: both have an executable `.claude/hooks/pretooluse-nudge.sh` whose `codegraph_bin=` line names `/tmp/06-live/codegraph`, and `.claude/settings.json` with `hooks.PreToolUse`; `indexed/.codegraph` is a directory; `unindexed/.codegraph` does not exist.
    - Read-only pre-flight: `claude --version`; `shasum -a 256 ~/.claude/settings.json`; `test ! -e ~/.claude/hooks/pretooluse-nudge.sh`; `echo "$TMPDIR"` and `id -u` (the sentinel dir the hook will use).
    - Hooks reference (NUDGE-03): fetch https://code.claude.com/docs/en/hooks (WebFetch) and quote, verbatim with the fetch date, the lines covering PreToolUse `hookSpecificOutput.additionalContext`, the handler `if` field, matcher grammar (exact names vs regex), exit code 2 blocking / non-zero "hook error", and the default timeout. If the fetch fails, write `Hooks reference: fetch failed <date> — relying on 06-CONTEXT's verified block (2026-09-19, claude 2.1.277)` and keep going.
    Write 06-LIVE-SESSIONS.md: title; Method (D-18 orchestrator-run Herdr sessions with `--debug-file`; transcripts saved under /tmp/06-live/transcripts; fire counting = occurrences of the pinned substring in the session JSONL(s) and debug log, attributed per key by transcript (main vs subagent sidechain) with timestamps; the STATE rule that every absence grep is first shown to find the thing when present); Pre-flight; Hooks reference; Scaffold (commands + the positive-assertion listing); Session A protocol (indexed; the prompt list from Task 2 verbatim); Session B protocol (un-indexed; Task 3); a Fire log table (key | fire timestamps | gaps); then these lines, each pre-filled `PENDING`:
    `C1 first matched call fires once: PENDING`, `C2 no same-key fire within 60 s: PENDING`, `C3 fires again after a >= 60 s gap: PENDING`, `C4 subagent first matched call fires once: PENDING`, `C5 fires after /clear: PENDING`, `C6 un-indexed control: PENDING`, `C7 no hook error, prompt, deny or block from the hook: PENDING`, `D-18 verdict: PENDING`, `Matched calls: PENDING`, `Fires: PENDING`, `Fire rate: PENDING`, `True-positive fires: PENDING`, `Uptake: PENDING`, `Hook wall time: PENDING`, `Bash rg path fired: PENDING`, `FP check (git log | rg): PENDING`, `Point additionalContext without a decision: PENDING`, `Point subagent session_id: PENDING`, `Point CLAUDE_CODE_SESSION_ID export: PENDING`, `Point same-command handlers with different if: PENDING`, `Point stdin key order: PENDING`, `Global config unchanged: PENDING`.
    Commit `docs(06-06): scaffold D-18 live-session evidence`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md && test -x /tmp/06-live/codegraph && test -x /tmp/06-live/indexed/.claude/hooks/pretooluse-nudge.sh && test -x /tmp/06-live/unindexed/.claude/hooks/pretooluse-nudge.sh && rg -q "^codegraph_bin='(/private)?/tmp/06-live/codegraph'" /tmp/06-live/indexed/.claude/hooks/pretooluse-nudge.sh && test -d /tmp/06-live/indexed/.codegraph && test ! -e /tmp/06-live/unindexed/.codegraph && jq -e '.hooks.PreToolUse | length == 4' /tmp/06-live/indexed/.claude/settings.json > /dev/null && jq -e '.hooks.PreToolUse | length == 4' /tmp/06-live/unindexed/.claude/settings.json > /dev/null && test ! -e "$HOME/.claude/hooks/pretooluse-nudge.sh" && test "$(rg -c ': PENDING$' "$D")" = "22" && rg -q '^D-18 verdict: PENDING$' "$D" && rg -q 'code\.claude\.com/docs/en/hooks' "$D" && rg -q 'claude --version|Claude Code\)' "$D" && rg -q 'shasum -a 256' "$D" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(06-06\): scaffold'</automated>
    <fails_when>non-zero exit; the scratch binary or either guard missing/non-executable; the indexed guard not rendered with the scratch binary path; indexed lacking or unindexed having `.codegraph`; either scratch settings.json without 4 PreToolUse blocks; a global guard present under $HOME; not exactly 22 PENDING lines; the D-18 verdict line, hooks-reference URL, claude version or settings.json checksum absent from the document; or no scaffold commit</fails_when>
  </verify>
  <acceptance_criteria>
    - nothing under $HOME was written by this task (only read-only version/checksum/test commands)
    - every PENDING line is one of the 22 fixed lines named in this task, each appearing exactly once
  </acceptance_criteria>
  <done>Two scratch repos, a HEAD-built binary and an evidence skeleton with the locked pass bar and the current hooks reference quoted are ready for the orchestrator.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: ORCHESTRATOR — Session A in the indexed repo: C1-C5, the Bash path, the FP check, the metrics and the five points</name>
  <precondition>The orchestrating session has Herdr control (HERDR_ENV=1; `herdr agent start --help` lists kind claude) and Task 1's /tmp/06-live scaffold exists.</precondition>
  <files>.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md (Task 1's skeleton)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md (D-18 pass bar; D-05/D-06/D-07 for what a "key" is)
  </read_first>
  <action>The orchestrator (not an executor subagent) drives one fresh Claude Code session in a sibling Herdr pane whose cwd is /tmp/06-live/indexed, following the timed prompt protocol below, and writes C1-C5, the recorded metrics and the five points into 06-LIVE-SESSIONS.md; everything automatable was scaffolded in Task 1.</action>
  <instructions>
    Clear stale state first: `rm -rf "${TMPDIR}/codegraph-nudge-$(id -u)"`. Start: `herdr agent start live-a --kind claude --pane <id> -- --debug-file /tmp/06-live/debug/live-a.log`. Prompts, in order, each with `herdr agent prompt live-a "<text>" --wait`, noting wall-clock times:
    1. `Use the Grep tool to find where the function Alpha is defined in this repository, and tell me the file and line.` → expect exactly one fire (C1).
    2. Immediately (well inside 60 s of prompt 1's fire): `Use the Grep tool to find every call site of Alpha, then use the Glob tool to list the .go files under pkgb, then Read pkgb/pkgb.go.` → several matched calls, expect 0 fires (C2).
    3. Wait at least 65 s after prompt 1's fire (`sleep 65` in the orchestrator's own shell), then: `Use the Grep tool to find where Beta is defined.` → expect exactly one fire (C3).
    4. Within 60 s of prompt 3's fire: `Use the Task tool to start one general-purpose subagent. Tell it to use the Grep tool to find where Alpha is defined and report the line. Do not search yourself.` → expect exactly one fire inside the subagent, none in the main thread (C4).
    5. Wait at least 65 s, then: `Run exactly this with the Bash tool: rg -n Alpha .` → record `Bash rg path fired: yes|no` and, from the debug log, how many times the guard ran for that one call (dedup point).
    5b. Wait at least 65 s, then: `Run exactly this with the Bash tool: find . -name '*.go' -newer go.mod` → record whether it fired and, from the debug log, how many times the guard ran. Since the 2026-09-19 amendment (06-05) the three Bash rules are three single-handler blocks with the SAME command, so fold this `find` result into the dedup Point line beside the `rg` result: if either `rg` or `find` never fires because Claude Code deduplicated same-command handlers across blocks, that is an ESCALATE.
    6. Wait at least 65 s, then: `Run exactly this with the Bash tool: git log --oneline | rg -c .` → expect 0 fires: `FP check (git log | rg): <n> fires`.
    7. `Run exactly this with the Bash tool: echo "$CLAUDE_CODE_SESSION_ID"` → CLAUDE_CODE_SESSION_ID export point (compare with the transcript's session id).
    8. `/clear`, then: `Use the Grep tool to find where Alpha is defined.` → expect exactly one fire (C5).
    Save `herdr agent read live-a --source recent-unwrapped > /tmp/06-live/transcripts/live-a.txt`; copy the session JSONL(s) (main and subagent) and the debug log path into the evidence. Count fires by the pinned substring `returns the matching symbols' source and call paths` in the JSONL(s) with timestamps — FIRST show that search finds prompt 1's fire (positive control), then use the same search for every window. Fill the Fire log table (per key: main-before-clear, subagent, main-after-clear).
    Replace the PENDING lines: C1-C5 as `PASS` or `FAIL (<reason>)`; `Matched calls: <n>`; `Fires: <n>`; `Fire rate: <fires>/<matched>`; `True-positive fires: <n>/<fires>`; `Uptake: <what the agent did after each fire — codegraph_explore, codegraph explore, or kept grepping>`; `Hook wall time: <min–max ms from the debug log>`; `Bash rg path fired: yes|no`; `FP check (git log | rg): <n> fires`; the five `Point …:` lines with what was observed (or `not observed` with the reason). Paste ≤ 40 verbatim lines of excerpt per criterion.
    ESCALATE before resuming, patching nothing: any FAIL, any "hook error", or a Bash path that never fires because same-command handlers were deduplicated — surface the excerpt to the maintainer and record their answer as a `Maintainer decision: …` line under the relevant criterion.
  </instructions>
  <verification>No PENDING remains on C1-C5, the eight recorded lines or the five Point lines; each criterion has an excerpt; any FAIL carries a Maintainer decision line.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md && test "$(rg -c '^C[1-5] [^:]+: (PASS|FAIL \(.+\))$' "$D")" = "5" && rg -q '^Matched calls: [0-9]+$' "$D" && rg -q '^Fires: [0-9]+$' "$D" && rg -q '^Fire rate: [0-9]+/[0-9]+$' "$D" && rg -q '^True-positive fires: [0-9]+/[0-9]+$' "$D" && rg -q '^Uptake: .+$' "$D" && rg -q '^Hook wall time: .+ms' "$D" && rg -q '^Bash rg path fired: (yes|no)$' "$D" && rg -q '^FP check \(git log \| rg\): [0-9]+ fires$' "$D" && test "$(rg -c '^Point [^:]+: .+$' "$D")" = "5" && ! rg -q '^(C[1-5] |Matched|Fires|Fire rate|True-positive|Uptake|Hook wall|Bash rg|FP check|Point ).*: PENDING$' "$D" && if rg -q '^C[1-5] [^:]+: FAIL' "$D"; then rg -q '^Maintainer decision: ' "$D"; fi</automated>
    <fails_when>non-zero exit; fewer than 5 C1-C5 lines in an allowed form; any recorded metric or Point line missing, malformed or still PENDING; or a FAIL without a "Maintainer decision:" line</fails_when>
  </verify>
  <acceptance_criteria>
    - the Fire log table lists every fire with its timestamp and key, and every same-key gap is shown
    - the positive-control search result (prompt 1's fire found by the pinned substring) is quoted before any zero-count claim
  </acceptance_criteria>
  <resume-signal>Reply with the lines as written, e.g. "C1=PASS C2=PASS C3=PASS C4=PASS C5=PASS; fires 4/11; bash-rg=yes; fp=0" — only after they are in 06-LIVE-SESSIONS.md.</resume-signal>
  <done>Claude Code's matching, delivery, per-key cooldown, subagent keying and /clear behaviour are live evidence against the locked bar, with the fire rate and the five open points recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: ORCHESTRATOR — Session B, the un-indexed negative control (C6), C7 across both sessions, the D-18 verdict; then the completeness gate</name>
  <precondition>Task 2 has been resumed with its lines written into 06-LIVE-SESSIONS.md.</precondition>
  <files>.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md
    - .planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md (D-18 criteria 6-7)
  </read_first>
  <action>The orchestrator runs a fresh Claude Code session in /tmp/06-live/unindexed with the same search prompts, records C6 and C7 (over both sessions' transcripts and debug logs) and the D-18 verdict, re-checks the global config; the executor then runs the completeness gate and commits 06-LIVE-SESSIONS.md.</action>
  <instructions>
    1. `herdr agent start live-b --kind claude --pane <id> -- --debug-file /tmp/06-live/debug/live-b.log` with cwd /tmp/06-live/unindexed; prompts 1 and 5 from Task 2 (`Use the Grep tool to find where the function Alpha is defined…` and `Run exactly this with the Bash tool: rg -n Alpha .`). Save the transcript. Count fires with the SAME pinned-substring search already positive-controlled on session A → `C6 un-indexed control: PASS (0 fires)` or `FAIL (<n> fires)`. Also confirm from live-b's debug log that the guard ran and exited 0 (it is registered there) — the zero-overhead claim is "no binary started", which the 06-01 unit suite proves; record what the debug log shows.
    2. C7 over BOTH sessions: search both debug logs and transcripts for a hook-error notice and for any permission prompt, deny or block whose source is the `pretooluse-nudge.sh` command (the maintainer's own global hooks may deny or rewrite commands — attribute by hook command, not by symptom); first show the search finds a known marker (e.g. the guard's command string in the debug log) → `C7 no hook error, prompt, deny or block from the hook: PASS` or `FAIL (<reason>)`.
    3. `Global config unchanged: <yes — sha256 matches pre-flight>` or `no (<diff>)` after re-running `shasum -a 256 ~/.claude/settings.json` and `test ! -e ~/.claude/hooks/pretooluse-nudge.sh`.
    4. `D-18 verdict: PASS` only if C1-C7 are all PASS; otherwise `D-18 verdict: FAIL` and ESCALATE — record `Maintainer decision: …` and stop (06-07's gate requires PASS; a FAIL routes to gap closure, not to a patch here).
    After resume the executor runs the completeness gate below and commits `docs(06-06): record D-18 live-session evidence`.
  </instructions>
  <verification>No PENDING anywhere; C6, C7, Global config and D-18 verdict lines present in allowed forms; verdict PASS consistent with seven PASS lines; the evidence commit exists.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md && ! rg -q 'PENDING' "$D" && rg -q '^C6 un-indexed control: (PASS \(0 fires\)|FAIL \([0-9]+ fires\))$' "$D" && rg -q '^C7 [^:]+: (PASS|FAIL \(.+\))$' "$D" && rg -q '^Global config unchanged: yes' "$D" && rg -q '^D-18 verdict: (PASS|FAIL)$' "$D" && if rg -q '^D-18 verdict: PASS$' "$D"; then test "$(rg -c '^C[1-7] [^:]+: PASS' "$D")" = "7"; else rg -q '^Maintainer decision: ' "$D"; fi && test ! -e "$HOME/.claude/hooks/pretooluse-nudge.sh" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(06-06\): record D-18 live-session evidence'</automated>
    <fails_when>non-zero exit; any PENDING left; the C6, C7, global-config or verdict line missing or outside its allowed form; a PASS verdict with fewer than 7 PASS criteria; a FAIL verdict with no "Maintainer decision:" line; a global guard present under $HOME; or no evidence commit</fails_when>
  </verify>
  <acceptance_criteria>
    - the un-indexed session has its own transcript excerpt and the same positive-controlled search as session A
    - the maintainer's `~/.claude/settings.json` checksum after equals the pre-flight value recorded in Task 1
  </acceptance_criteria>
  <resume-signal>Reply "C6=PASS (0 fires) C7=PASS global=unchanged verdict=PASS" (or the FAIL lines plus the maintainer decision) once written.</resume-signal>
  <done>The negative control, the no-error/no-block criterion and the locked D-18 verdict are live evidence, and the whole evidence document is complete and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live session → maintainer's real Claude Code environment | sessions run with the real `~/.claude` (auth, global hooks, plugins) |
| transcript → recorded verdict | a summary could overstate what Claude Code did |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-27 | Tampering | maintainer's global Claude config | medium | mitigate | local-scope installs only in /tmp scratch repos; sha256 of ~/.claude/settings.json before/after; no global guard; verify asserts both |
| T-06-28 | Spoofing (of evidence) | verdict lines | medium | mitigate | fixed verdict lines gated on verbatim excerpts; positive-controlled searches; an un-indexed negative control; the pass bar locked in CONTEXT before any session |
| T-06-29 | Repudiation (misattributed denial) | C7 | low | mitigate | denials/prompts attributed by hook command in the debug log, not by symptom — the maintainer's own global hooks are excluded explicitly |
| T-06-30 | Information Disclosure | transcripts pasted into the repo | low | accept | scratch fixture repos only (gofixture); the orchestrator redacts unrelated personal paths or tokens before pasting |
</threat_model>

<verification>
- 06-LIVE-SESSIONS.md complete: C1-C7 verdicts with excerpts, D-18 verdict, recorded metrics, the five points, hooks-reference quotes, global config unchanged
</verification>

<success_criteria>
- NUDGE-05: fire rate against matched-tool-call count measured in a genuinely fresh live session (roadmap criterion 3's live half)
- NUDGE-03: delivery and the never-deny contract observed live, output shape checked against the current hooks reference (roadmap criterion 1's live half)
- NUDGE-04: first-call fire, 60 s per-key spacing, subagent separation, /clear, un-indexed silence observed live (roadmap criterion 2's live half)
- D-18 executed against its locked pass bar
</success_criteria>

<output>
Create `.planning/phases/06-claude-code-pretooluse-nudge/06-06-SUMMARY.md` when done — every verdict line, the recorded metrics, the five points, and any maintainer decision (with what it means for CODEX-05).
</output>
