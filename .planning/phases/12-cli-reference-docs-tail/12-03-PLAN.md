---
phase: 12-cli-reference-docs-tail
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - .planning/phases/12-cli-reference-docs-tail/12-MUTATION-LOG.md
  - .planning/phases/12-cli-reference-docs-tail/12-SECURITY.md
  - .planning/phases/12-cli-reference-docs-tail/12-VALIDATION.md
autonomous: true
requirements: [DOCS-05, DOCS-06, DOCS-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-10: `12-MUTATION-LOG.md` follows the 11-MUTATION-LOG house format (header Phase/Date/Scope; the pre-mutation cleanliness-gate convention section with a real `git diff --quiet -- <the three files>; echo $?` = 0 transcript; per family `**Instrument:**`, `**Pre-mutation cleanliness gate:**`, the mutation as a diff, `**Confirmed applied**`, the RED output pasted verbatim, the revert with a second `git diff --quiet` exit 0 and the instrument GREEN again; a `## Closing` section) for exactly three families: (a) a throwaway hidden flag `--zz-throwaway` registered on `codegraph ui` → `TestEveryRegisteredFlagIsAccountedFor` RED naming `codegraph ui --zz-throwaway` WHILE `task docs:cli:drift` stays green (the generator's blind spot, closed by the guard); (b) the `--no-open` Options line deleted from the committed `docs/CLI-REFERENCE.md` → `task docs:cli:drift` RED naming the file with `compared 1 generated file` printed first and the `diff -u | head -40` excerpt, AND the guard RED naming `codegraph ui --no-open`; (c) a bogus entry `codegraph ui --zz-bogus` appended to the allowlist → the guard RED with `stale allowlist entry: codegraph ui --zz-bogus`"
    - "D-10: every mutation is applied for real to the tracked file, its RED captured, and reverted byte-clean (`git checkout --`), with `git diff --quiet` exit 0 before the mutation and after the revert; no mutation is ever committed; a family that does NOT go RED is recorded as an observed negative with its root cause (08-MUTATION-LOG family (d) precedent) — never rewritten until it fails"
    - "D-10 / D-09: no family exercises `cobra/doc`'s own behaviour — (a) perturbs this project's Cobra tree, (b) this project's committed file, (c) this project's allowlist; the log says so in its Scope line"
    - "D-13: `12-SECURITY.md` is in the 11-SECURITY house format (frontmatter `phase: \"12\"`, `slug`, `status: verified`, `threats_open`, `asvs_level: 1`, `created`; Register origin paragraph; Trust Boundaries table + one-sentence boundary statement; Threat Register where EVERY T-12-xx row from Plans 01–03 carries a named test, a verify command, or an explicit verdict with Status; Accepted Risks; Notes) — `threats_open: 0` is honest only because T-12-01 (drift gate, family (b) RED), T-12-02 (guard, family (a) RED) and T-12-08 (D-11 wording, source assertions + diff review — a verdict, D-12) each cite the landed evidence; the rows D-13 names are present: UF-2 docs-as-security-guidance framing, hidden-flag accounting blind spot, generated-file tampering caught by drift, allowlist rot, exported constructor widens nothing"
    - "D-13: `12-VALIDATION.md`'s Per-Task Verification Map has every `TBD` replaced with the real plan / task / command that landed (`12-01 / T1 / task -s docs:cli:drift`, `12-01 / T2 / TestEveryRegisteredFlagIsAccountedFor`, `12-01 / T1 / rg … ci.yml`, `12-02 / T1 / RELEASE.md assertions (one-time)`, `12-02 / T2 / README link`), `File Exists` ✅ and `Status` ✅ green for every row whose gate passed in the phase-close run, the Wave 0 checklist ticked, `wave_0_complete: true` and `nyquist_compliant: true` set, `status: draft` left for validate-phase — values only, no new headings, columns, rows of a new shape"
    - "D-14: phase-close gate at the final commit, every `go` prefixed `GOTOOLCHAIN=go1.26.6`: `go vet ./...`, `go build ./...`, `task test:unit` (reaches the guard), `task test:golden`, `task lint:go`, `task proto:drift`, `task -s web:drift` (both halves MATCH — unchanged by this phase), `task -s docs:cli:drift` (`compared 1 generated file`), and the guard alone with `-v` showing its counts line — tails pasted in the SUMMARY; `git status --porcelain` empty at the final commit"
  artifacts:
    - ".planning/phases/12-cli-reference-docs-tail/12-MUTATION-LOG.md — families (a), (b), (c), each RED then byte-clean reverted"
    - ".planning/phases/12-cli-reference-docs-tail/12-SECURITY.md — T-12-01 … T-12-13 and T-12-SC, `threats_open: 0`"
    - ".planning/phases/12-cli-reference-docs-tail/12-VALIDATION.md — map filled, Wave 0 ticked, flags set"
  key_links:
    - "family (a) → `internal/cli/ui.go` flag block (after `--no-editor-url`) → `TestEveryRegisteredFlagIsAccountedFor` `unaccounted flag: codegraph ui --zz-throwaway`; `task -s docs:cli:drift` unchanged (green) — the two instruments together show what the guard adds over the generator"
    - "family (b) → `docs/CLI-REFERENCE.md` `## codegraph ui` Options `--no-open` line → `task -s docs:cli:drift` exit 1 naming the file + the guard naming `codegraph ui --no-open`"
    - "family (c) → `internal/cli/testdata/cli-reference-allowlist.txt` appended line → the guard `stale allowlist entry: codegraph ui --zz-bogus`"
    - "`12-SECURITY.md` rows ← the `<threat_model>` blocks of 12-01 (T-12-01…07, SC), 12-02 (T-12-08…11, SC), this plan (T-12-12, T-12-13, SC)"
  prohibitions:
    - "No mutation may be committed — every family ends with `git diff --quiet -- <file>` exit 0 and `git status --porcelain` clean for that path"
    - "A non-failing family MUST NOT be rewritten until it fails — report it as an observed negative with root cause"
    - "No family may exercise `cobra/doc` itself, add a positive control that renders through it, or add section-scoped matching (D-09, ROADMAP Notes)"
    - "`12-VALIDATION.md`, STATE.md and ROADMAP.md MUST NOT gain invented headings, columns or shapes — values only in tool-owned files"
    - "`docs/CLI-REFERENCE.md` MUST NOT be regenerated or hand-edited except transiently by family (b), reverted byte-clean before anything else runs (D-01)"
    - "`threats_open` MUST equal the count of `open` rows at severity `high` — never written as 0 by assertion"
    - "No `rg -c`, no `rg -h`, no `go` without `GOTOOLCHAIN=go1.26.6`, no `codegraph ui` process left running (D-14)"
---

<objective>
Close the phase with evidence: three hand-applied RED demonstrations proving the two gates discriminate (the guard on a throwaway hidden flag and on a rotten allowlist entry; the drift gate on a deleted line), the security register with every threat row test-or-verdict and an honest `threats_open`, the validation map filled with what actually landed, and the full gate suite green at the final commit.

Purpose: a gate is not trusted until it has been demonstrated RED against a confirmed-applied mutation (STATE.md standing decision); D-10 names the three families, D-13 the artefacts.

Decision coverage (all locked, `12-CONTEXT.md`): implemented here — D-10: the three RED families · D-13: `12-MUTATION-LOG.md`, `12-SECURITY.md`, `12-VALIDATION.md` fill · D-14: `GOTOOLCHAIN=go1.26.6`, `rg -o … | wc -l`, count-before-compare shown in family (b). Evidenced here — D-01: family (b) proves the committed file cannot be hand-edited unnoticed · D-05: family (a)'s hidden flag stays out of the reference by design and the guard is what notices · D-08: family (c) proves rot fails · D-09: no family touches `cobra/doc`. Landed earlier — D-02: D-03: D-04: D-07: (12-01) · D-06: D-11: D-12: (12-02).

Output: `12-MUTATION-LOG.md`, `12-SECURITY.md`, `12-VALIDATION.md` filled, phase-close transcripts in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cli-reference-docs-tail/12-CONTEXT.md
@.planning/phases/12-cli-reference-docs-tail/12-VALIDATION.md
@.planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md
@.planning/phases/11-graph-view-community-clustering/11-SECURITY.md
@.planning/phases/12-cli-reference-docs-tail/12-01-SUMMARY.md
@.planning/phases/12-cli-reference-docs-tail/12-02-SUMMARY.md

Every Go command: `GOTOOLCHAIN=go1.26.6`. Mutations are applied to tracked files with an editor, run, pasted, and reverted with `git checkout -- <file>`; agent commits MAY use `-c commit.gpgsign=false`; never `[ci skip]`. The guard's messages (from 12-01): `unaccounted flag: <path> --<name> (<why> — …)`, `stale allowlist entry: <key> (matches no registered command or flag)`, and the counts line `walked N commands (hidden included), inspected M flags: …`. The drift target's lines: `docs:cli:drift: compared 1 generated file`, `::error::docs:cli:drift: docs/CLI-REFERENCE.md differs …`, then `diff -u` head. Note for family (b): the `--editor-url` line is deliberately NOT the one deleted — `ui`'s Synopsis repeats that name, so the whole-document guard would stay green (the DOCS-06 assumption recorded in 12-01's must_haves); `--no-open` appears only in the Options line, so both instruments fire.
</context>

<tasks>

<!-- planner-discipline-allow: zz-throwaway -->
<!-- planner-discipline-allow: zz-bogus -->
<!-- planner-discipline-allow: zz-throwaway|zz-bogus -->

<task type="auto">
  <name>Task 1: `12-MUTATION-LOG.md` — families (a) throwaway hidden flag on `ui`, (b) one line deleted from the committed reference, (c) bogus allowlist entry — each RED then byte-clean reverted (D-10)</name>
  <reversibility rating="reversible">Documentation of temporary, reverted mutations; nothing persists in source.</reversibility>
  <files>.planning/phases/12-cli-reference-docs-tail/12-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md (whole — header, the cleanliness-gate convention section, per-family Instrument / cleanliness gate / Mutation applied (diff) / Confirmed applied / RED pasted verbatim / Revert structure; the `## Closing` section with its three subsections)
    - .planning/phases/08-tmux-real-pty-harness/08-MUTATION-LOG.md family (d) (how a non-failing demonstration is recorded honestly) — load only that section
    - internal/cli/ui.go:110-125 (the flag block: `--path`, `--no-open`, `--editor-url`, `--no-editor-url` — family (a) adds one `Bool` + `MarkHidden` after `--no-editor-url`)
    - internal/cli/cli_reference_test.go (the message formats and the counts line; `cliReferenceAllowlistPath`)
    - internal/cli/testdata/cli-reference-allowlist.txt (the one `codegraph man` entry and the tab-separated format family (c) mimics)
    - docs/CLI-REFERENCE.md `## codegraph ui` section (the `### Options` block; the `--no-open` line to delete in family (b))
    - Taskfile.yml `docs:cli:drift` (12-01 — the count line, the `::error::` line, the `diff -u … | head -40 || true` excerpt)
  </read_first>
  <action>
    Author `12-MUTATION-LOG.md` in the 11-MUTATION-LOG house format: header `# 12-MUTATION-LOG — CLI Reference & Docs Tail`, `**Phase:** 12-cli-reference-docs-tail`, `**Date:**`, `**Scope:**` naming the three families and stating that none exercises `cobra/doc` itself (D-10); the cleanliness-gate convention section with a real `git diff --quiet -- internal/cli/ui.go docs/CLI-REFERENCE.md internal/cli/testdata/cli-reference-allowlist.txt; echo $?` = 0 transcript. Then, one family at a time under a `## Family (a) — …` / `## Family (b) — …` / `## Family (c) — …` heading (the 11-MUTATION-LOG shape), gate → mutate → confirm applied → run → paste → revert → gate → re-run GREEN:

    (a) DOCS-06 — throwaway hidden flag. Cleanliness gate on `internal/cli/ui.go`. Mutation: after the `--no-editor-url` registration add `cmd.Flags().Bool("zz-throwaway", false, "MUTATION 12-03 family (a) — reverted")` and `_ = cmd.Flags().MarkHidden("zz-throwaway")`, shown as a diff. Confirm applied with `rg -n 'zz-throwaway' internal/cli/ui.go` (2 lines) and `GOTOOLCHAIN=go1.26.6 go build ./internal/cli/`. Instrument 1: `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestEveryRegisteredFlagIsAccountedFor' ./internal/cli/`. Expected RED: `unaccounted flag: codegraph ui --zz-throwaway (hidden flag — add an allowlist entry with a reason)` with the counts line reporting 116 flags inspected (one more than 12-01's 115) — paste both. Instrument 2, run with the mutation still applied: `GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift; echo "exit=$?"` — expected GREEN (`compared 1 generated file`, exit 0): the hidden flag never reaches the generated reference, which is exactly why the guard exists (D-05) — paste and say so. Revert `git checkout -- internal/cli/ui.go`; gate; instrument 1 GREEN with 115 flags.

    (b) DOCS-05 — one line deleted from the committed reference. Cleanliness gate on `docs/CLI-REFERENCE.md`. Mutation: delete the single Options line carrying `--no-open` under `## codegraph ui` (the line reads `      --no-open   do not open a browser automatically` or similar — delete the whole line, nothing else), shown as a diff; confirm with `rg -o -- '--no-open' docs/CLI-REFERENCE.md | wc -l | tr -d ' '` = 0. Instrument 1: `GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift; echo "exit=$?"`. Expected RED: `docs:cli:drift: compared 1 generated file` printed FIRST (D-14: count before compare), then `::error::docs:cli:drift: docs/CLI-REFERENCE.md differs …`, then the `diff -u` excerpt showing the `+      --no-open` line, `exit=1` — paste all of it. Instrument 2: the guard — expected RED `unaccounted flag: codegraph ui --no-open (visible flag on a documented command — missing from docs/CLI-REFERENCE.md; run task docs:cli)` — paste. Record the note from the plan context: deleting the `--editor-url` line instead would have left the guard green because the Synopsis repeats the name — the drift gate, not the guard, is the content check (D-09), and that is the recorded DOCS-06 assumption. Revert `git checkout -- docs/CLI-REFERENCE.md`; gate; both instruments GREEN.

    (c) DOCS-06 — allowlist rot. Cleanliness gate on `internal/cli/testdata/cli-reference-allowlist.txt`. Mutation: append the line `codegraph ui --zz-bogus<TAB>MUTATION 12-03 family (c) — bogus entry for a flag that does not exist, reverted` (a real tab), shown as a diff; confirm with `rg -o 'zz-bogus' … | wc -l` = 1. Instrument: the guard. Expected RED: `stale allowlist entry: codegraph ui --zz-bogus (matches no registered command or flag)` with the counts line still reporting 115 flags and `1 accepted via` the allowlist (the bogus entry accepted nothing) — paste. Revert; gate; GREEN.

    Close with a `## Closing` section: the three instruments RED and GREEN summarised, a non-vacuity note (every RED names the exact mutated item; the guard's counts line printed on every run), and the byte-clean proof: all three files `git diff --quiet` exit 0, `git status --porcelain` shows only the new log. Commit the log alone: `docs(12-03): 12-MUTATION-LOG.md — three families RED and reverted`.
  </action>
  <verify>
    <automated>M=.planning/phases/12-cli-reference-docs-tail/12-MUTATION-LOG.md; test -f "$M" &amp;&amp; test "$(rg -o '^## Family \([abc]\)' "$M" | wc -l | tr -d ' ')" = "3" &amp;&amp; test "$(rg -o '\*\*Instrument' "$M" | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test "$(rg -o '\*\*Pre-mutation cleanliness gate:\*\*' "$M" | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test "$(rg -o 'unaccounted flag: codegraph ui --zz-throwaway' "$M" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '::error::docs:cli:drift: docs/CLI-REFERENCE\.md differs' "$M" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'docs:cli:drift: compared 1 generated file' "$M" | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o 'unaccounted flag: codegraph ui --no-open' "$M" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'stale allowlist entry: codegraph ui --zz-bogus' "$M" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '^## Closing' "$M" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'cobra/doc' "$M" | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>the log lacks three family sections with instruments and cleanliness gates, any family's expected RED line is not pasted (a family that did not go RED must instead carry an "observed negative" paragraph — if so, this gate is adjusted ONLY by the executor recording the negative honestly, never by weakening a guard), the drift count line is not shown printed before the error in (b) and green in (a), the Closing section is missing, or the Scope does not state that no family exercises cobra/doc</fails_when>
    <automated>git diff --quiet -- internal/cli/ui.go docs/CLI-REFERENCE.md internal/cli/testdata/cli-reference-allowlist.txt &amp;&amp; test "$(rg -o 'zz-throwaway|zz-bogus' internal/cli/ui.go internal/cli/testdata/cli-reference-allowlist.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestEveryRegisteredFlagIsAccountedFor' ./internal/cli/ 2&gt;&amp;1 | tee /tmp/12-03-revert.txt | tail -3; test "$(rg -o -- '--- PASS: TestEveryRegisteredFlagIsAccountedFor' /tmp/12-03-revert.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '1 accepted via testdata/cli-reference-allowlist\.txt' /tmp/12-03-revert.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift &gt;/tmp/12-03-drift.txt 2&gt;&amp;1; RC=$?; test "$RC" = "0" &amp;&amp; test "$(rg -o 'compared 1 generated file' /tmp/12-03-drift.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(git show --name-only --format= HEAD | wc -l | tr -d ' ')" = "1" &amp;&amp; test -z "$(git status --porcelain)"</automated>
    <fails_when>any mutated file still differs from HEAD (a mutation leaked), a throwaway identifier survived, the guard or the drift gate is not green again after the reverts, or the log's commit carries anything but the log</fails_when>
  </verify>
  <acceptance_criteria>
    - Each family section shows the cleanliness gate BEFORE the mutation and BEFORE the revert, both `0`, and a "Confirmed applied" check
    - Family (a) pastes BOTH instruments: the guard RED naming `codegraph ui --zz-throwaway` and the drift gate GREEN under the same mutation, with the sentence explaining why that pair is the point of DOCS-06
    - Family (b) shows the count line printed before the error and the `+ --no-open` diff excerpt, plus the guard RED; the `--editor-url` note is recorded
    - Family (c) shows the rot line with the counts line still reporting 1 allowlist-accepted flag
    - The Closing section shows `git status --porcelain` with only the new log file
  </acceptance_criteria>
  <done>Both gates have been watched failing against confirmed-applied mutations of this project's own artefacts — the guard twice (hidden flag; rotten entry), the drift gate once (deleted line) — and restored byte-clean, with every transcript pasted.</done>
</task>

<task type="auto">
  <name>Task 2: `12-SECURITY.md` (every threat row test-or-verdict, `threats_open` honest), `12-VALIDATION.md` map filled (values only), and the phase-close gate green at the final commit (D-13, D-14)</name>
  <reversibility rating="reversible">Planning artefacts and a gate run.</reversibility>
  <files>.planning/phases/12-cli-reference-docs-tail/12-SECURITY.md, .planning/phases/12-cli-reference-docs-tail/12-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-graph-view-community-clustering/11-SECURITY.md (whole — frontmatter keys, Register origin paragraph, Trust Boundaries table with `Data Crossing` column + the bold boundary sentence, Threat Register columns incl. Status, Accepted Risks, Notes)
    - The `<threat_model>` blocks of 12-01-PLAN.md (T-12-01 … T-12-07, T-12-SC), 12-02-PLAN.md (T-12-08 … T-12-11, T-12-SC) and this plan (T-12-12, T-12-13, T-12-SC)
    - .planning/phases/12-cli-reference-docs-tail/12-VALIDATION.md (the Per-Task Verification Map's `TBD` cells, the Wave 0 checklist, the Validation Sign-Off list — fill values only)
    - .planning/phases/12-cli-reference-docs-tail/12-01-SUMMARY.md and 12-02-SUMMARY.md (the real commands, counts, commit SHAs, the D-04 audit outcome, the `--version` addition, the todo-tool invocation)
    - .planning/phases/12-cli-reference-docs-tail/12-RESEARCH.md § Security Domain (the four threat patterns and the `threats_open: 0` expectation) and § Assumptions Log (A1 anchors, A2 root section — both resolved by 12-01; record them as such)
  </read_first>
  <action>
    Author `12-SECURITY.md` in the 11-SECURITY house format: frontmatter (`phase: "12"`, `slug: "cli-reference-docs-tail"`, `status: verified`, `threats_open: <honest count of OPEN rows at severity >= high>`, `asvs_level: 1`, `created: <today>`); Register origin (`register_authored_at_plan_time: true`, three plans carry `<threat_model>`; union deduplicated by id; the shared `T-12-SC` row merges the three plans' text tagged `(12-0N)`). Trust Boundaries: (1) live Cobra tree → `tools/clidoc` → `docs/CLI-REFERENCE.md` (in-process data, no user input; the risk is drift or a hand edit); (2) `docs/CLI-REFERENCE.md` + allowlist → `cli_reference_test.go` (two committed files by fixed relative paths, fail-closed; the risk is a silent omission or a stale exemption); (3) `internal/cli` → `tools/clidoc` via `NewRootCmd` (an API widening that returns the tree `Execute()` already runs); (4) `docs/RELEASE.md` → a Homebrew user's shell (the brew-trust instruction switches off a Homebrew control — UF-2); (5) `Taskfile.yml`/`ci.yml` → the developer working tree (the drift target regenerates into scratch, never in place); boundary sentence: the phase's trust surface is a generated document that must equal what the binary's own tree says, a guard that must see every flag the tree registers, and one sentence of security guidance whose narrowness sets a user's blast radius. Threat Register rows T-12-01 … T-12-13 and T-12-SC, each with Severity, Disposition, the Mitigation citing the landed test / target / verify command by name and plan tag (T-12-01 → `docs:cli:drift` + family (b); T-12-02 → `TestEveryRegisteredFlagIsAccountedFor` + family (a); T-12-03 → the rot rule + family (c); T-12-06 → `DisableAutoGenTag` + the two identical drift transcripts; T-12-07 → floors + `InitDefaultCompletionCmd`/`InitDefaultVersionFlag` in both walks; T-12-08 → D-11 wording + the one-time assertions + diff review, Status `closed (verdict)` since D-12 rules out a gate; T-12-09 → the trimmed quote; T-12-12/13 → this plan's own gates), and Status `closed` / `closed (accepted)` / `closed (verdict)` / `open`. Accepted Risks: T-12-04, T-12-10, T-12-SC. Notes: the D-13 rows are called out by name (UF-2 framing; hidden-flag blind spot; generated-file tampering; allowlist rot; exported constructor widens nothing); the DOCS-06 and DOCS-07 `unclassified` edge rows recorded verbatim as explicit unresolved assumptions (whole-document flag match; no committed gate for the wording); research A1/A2 resolved by 12-01 (anchors verified by the `#codegraph-githooks-status` assertion; root section present); the `--version` planner addition; no new dependency of any kind (`go.mod`/`go.sum` unchanged, asserted in 12-01).

    Fill `12-VALIDATION.md` (values only): every `TBD` in the Per-Task Verification Map replaced with the real Plan / Task and the command that landed (`12-01 / T1 / GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift`; `12-01 / T2 / GOTOOLCHAIN=go1.26.6 go test -count=1 -run TestEveryRegisteredFlagIsAccountedFor -v ./internal/cli/`; `12-01 / T1 / rg -o 'run: task docs:cli:drift' .github/workflows/ci.yml`; `12-02 / T1 / one-time RELEASE.md assertions (D-12: no committed test)`; `12-02 / T2 / rg -o 'docs/CLI-REFERENCE.md' README.md`); `File Exists` → ✅ and `Status` → ✅ green for every row whose gate passed in the phase-close run below (the DOCS-07 row's status reflects the diff review + one-time assertions, noted as such in its existing cell); Wave 0 checklist items ticked; frontmatter `wave_0_complete: true`, `nyquist_compliant: true`; `status: draft` untouched (validate-phase owns it). Do not add headings, columns, or rows of a new shape.

    Then run the phase-close gate in this order and paste every tail in the SUMMARY: `GOTOOLCHAIN=go1.26.6 go vet ./...`; `GOTOOLCHAIN=go1.26.6 go build ./...`; `GOTOOLCHAIN=go1.26.6 task test:unit`; `GOTOOLCHAIN=go1.26.6 task test:golden`; `GOTOOLCHAIN=go1.26.6 task lint:go`; `GOTOOLCHAIN=go1.26.6 task proto:drift`; `task -s web:drift`; `GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift`; `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestEveryRegisteredFlagIsAccountedFor' ./internal/cli/`. Commit the two artefacts: `docs(12-03): 12-SECURITY.md register and 12-VALIDATION.md map`.
  </action>
  <verify>
    <automated>F=.planning/phases/12-cli-reference-docs-tail/12-SECURITY.md; test -f "$F" &amp;&amp; test "$(rg -o '^threats_open: [0-9]+' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^asvs_level: 1' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^\| T-12-(0[1-9]|1[0-3]|SC) ' "$F" | wc -l | tr -d ' ')" -ge 14 &amp;&amp; test "$(rg -o '## Trust Boundaries|## Threat Register|## Accepted Risks|## Notes' "$F" | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o 'UF-2' "$F" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'unclassified' "$F" | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o '\| high \| mitigate \|[^|]*\| closed' "$F" | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test "$(rg -o '\| high \| [a-z]+ \|[^|]*\| open' "$F" | wc -l | tr -d ' ')" = "$(rg -o '^threats_open: [0-9]+' "$F" | rg -o '[0-9]+')" &amp;&amp; test "$(rg -o 'TestEveryRegisteredFlagIsAccountedFor' "$F" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'docs:cli:drift' "$F" | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>the register lacks the house sections, fewer than 14 T-12 rows (13 numbered + SC), the UF-2 row, the two recorded `unclassified` assumptions, fewer than three closed high rows (T-12-01, T-12-02, T-12-08), a mitigation naming the guard and the drift target, or `threats_open` disagrees with the count of open high rows</fails_when>
    <automated>V=.planning/phases/12-cli-reference-docs-tail/12-VALIDATION.md; test "$(rg -o '\| TBD \|' "$V" | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^wave_0_complete: true' "$V" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^nyquist_compliant: true' "$V" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^status: draft' "$V" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^- \[x\]' "$V" | wc -l | tr -d ' ')" -ge 7 &amp;&amp; test "$(rg -o '^## ' "$V" | wc -l | tr -d ' ')" = "$(git show ca015c4b:.planning/phases/12-cli-reference-docs-tail/12-VALIDATION.md | rg -o '^## ' | wc -l | tr -d ' ')" &amp;&amp; test "$(rg -o '12-01 / T[12]' "$V" | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test "$(rg -o '12-02 / T[12]' "$V" | wc -l | tr -d ' ')" -ge 2</automated>
    <fails_when>a `TBD` cell remains, the two frontmatter flags are not set, `status` was changed (validate-phase owns it), fewer than seven Wave 0 items are ticked, the number of `##` headings changed (invented structure in a tool-owned file), or the map rows do not name the real plan/task pairs</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./... &amp;&amp; GOTOOLCHAIN=go1.26.6 go build ./... &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:unit &gt;/tmp/12-03-unit.txt 2&gt;&amp;1; RC=$?; tail -5 /tmp/12-03-unit.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^FAIL' /tmp/12-03-unit.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^ok\s+github\.com/seanb4t/codegraph-go/internal/cli\s' /tmp/12-03-unit.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:golden &gt;/tmp/12-03-golden.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/12-03-golden.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^ok' /tmp/12-03-golden.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>vet/build fail, test:unit exits non-zero, prints a FAIL line, or does not list `internal/cli` as ok (the guard must be reached by the suite with no extra wiring), or test:golden is not green with at least one `ok`</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task lint:go &gt;/tmp/12-03-lint.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/12-03-lint.txt; test "$RC" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task proto:drift &gt;/tmp/12-03-proto.txt 2&gt;&amp;1; RC=$?; tail -2 /tmp/12-03-proto.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i 'mismatch|drift detected|::error::' /tmp/12-03-proto.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; task -s web:drift &gt;/tmp/12-03-webdrift.txt 2&gt;&amp;1; RC=$?; test "$RC" = "0" &amp;&amp; test "$(rg -o '\bhalf MATCH\b' /tmp/12-03-webdrift.txt | wc -l | tr -d ' ')" = "2"</automated>
    <fails_when>golangci-lint reports an issue in the new Go files (`tools/clidoc/main.go`, `cli_reference_test.go`, `root.go`), proto:drift finds a generated file out of step, or web:drift does not MATCH both halves (nothing under `web/` changed this phase — a mismatch means a mutation leaked)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift &gt;/tmp/12-03-clidrift.txt 2&gt;&amp;1; RC=$?; cat /tmp/12-03-clidrift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o 'compared 1 generated file' /tmp/12-03-clidrift.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '::error::' /tmp/12-03-clidrift.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestEveryRegisteredFlagIsAccountedFor' ./internal/cli/ 2&gt;&amp;1 | tee /tmp/12-03-guard.txt | tail -3; test "$(rg -o -- '--- PASS: TestEveryRegisteredFlagIsAccountedFor' /tmp/12-03-guard.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'walked 36 commands' /tmp/12-03-guard.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; ! rg -q 'no tests to run' /tmp/12-03-guard.txt &amp;&amp; git diff --quiet -- docs/CLI-REFERENCE.md internal/cli/ui.go internal/cli/testdata/cli-reference-allowlist.txt &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; test "$(ls .planning/todos/pending | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the drift gate or the guard is not green at phase close, the guard's counts line is missing, any family file still differs from HEAD, the tree is dirty at the final commit, or the brew-trust todo reappeared in pending</fails_when>
  </verify>
  <acceptance_criteria>
    - `12-SECURITY.md`'s `threats_open` equals the number of `open` rows at severity `high` (expected 0, with T-12-01 / T-12-02 / T-12-08 closed by name — the third by verdict)
    - `12-SECURITY.md` Notes record the five D-13 rows by name, both `unclassified` assumptions verbatim, research A1/A2 resolved, the `--version` addition, and "no new dependency"
    - `12-VALIDATION.md` has zero `TBD`, ticked Wave 0, both flags true, `status: draft`, and its heading set unchanged
    - The SUMMARY pastes all nine phase-close tails and states `git status --porcelain` is empty at the final commit
  </acceptance_criteria>
  <done>The register carries every threat with a named test or verdict and an honest open count, the validation map records what actually landed, and every gate in the phase — Go, lint, proto, web, the new CLI drift gate, the new guard — is green at the final commit with no mutation left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan's transient mutations → tracked files | three deliberate, reverted edits (`ui.go`, the generated reference, the allowlist); the risk is one leaking into a commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-12 | Tampering | a mutation from families (a)–(c) committed by accident — a hidden throwaway flag in the shipped binary, a stale reference, or a bogus allowlist entry | medium | mitigate | cleanliness gate before every mutation and revert; Task 1 verify asserts `git diff --quiet` on all three files and that the log's commit carries one file; Task 2's phase-close gate re-runs the guard and the drift gate green |
| T-12-13 | Repudiation | a `threats_open: 0` written without the closing evidence | medium | mitigate | Task 2 verify ties `threats_open` to the count of `open` high rows and requires three `high | mitigate | … | closed` rows naming the guard, the drift target and the D-11 verdict |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `12-MUTATION-LOG.md`: three families, each RED pasted and reverted byte-clean; family (a) also shows the drift gate green under the hidden flag
- `12-SECURITY.md`: every T-12 row test-or-verdict; `threats_open` honest
- `12-VALIDATION.md`: zero `TBD`, Wave 0 ticked, flags set, headings unchanged
- Phase-close gate: vet, build, test:unit, test:golden, lint:go, proto:drift, web:drift, docs:cli:drift, the guard — all green; tree clean
</verification>

<success_criteria>
- D-10 delivered: the guard was watched failing on a throwaway hidden flag (ROADMAP criterion 2's RED demonstration) and on a rotten entry; the drift gate on a deleted line (criterion 1's "fails when the committed file is stale")
- D-13 delivered: the threat register and validation contract reflect what landed; all three roadmap success criteria have their gates green at the final commit
</success_criteria>

## Artifacts this phase produces (complete list — Plan 03 lands the items marked ★)

- Go `internal/cli` (12-01): `NewRootCmd()`; `cli_reference_test.go` (`TestEveryRegisteredFlagIsAccountedFor`); `testdata/cli-reference-allowlist.txt` (one entry: `codegraph man`)
- Go `tools/clidoc` (12-01): `main.go`
- Docs: `docs/CLI-REFERENCE.md` (12-01, generated); `README.md` link (12-02, D-06); `docs/RELEASE.md` brew-trust wording (12-02, D-11)
- Taskfile (12-01): `docs:cli`, `docs:cli:drift`; CI (12-01): `CLI reference drift guard (DOCS-05)` step
- Planning: `.planning/todos/completed/2026-08-10-brew-trust-…md` (12-02); ★ `12-MUTATION-LOG.md` families (a) throwaway hidden flag on `ui`, (b) `--no-open` line deleted from the reference, (c) bogus allowlist entry; ★ `12-SECURITY.md` (`threats_open: 0`); ★ `12-VALIDATION.md` map filled

<output>
Create `.planning/phases/12-cli-reference-docs-tail/12-03-SUMMARY.md` when done — include the three family RED lines (and family (a)'s green drift transcript), the nine phase-close gate tails, `threats_open`, and the final commit SHA.
</output>
