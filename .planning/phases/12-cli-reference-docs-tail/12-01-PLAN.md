---
phase: 12-cli-reference-docs-tail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/root.go
  - tools/clidoc/main.go
  - docs/CLI-REFERENCE.md
  - Taskfile.yml
  - .github/workflows/ci.yml
  - internal/cli/cli_reference_test.go
  - internal/cli/testdata/cli-reference-allowlist.txt
autonomous: true
requirements: [DOCS-05, DOCS-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01: `docs/CLI-REFERENCE.md` is ONE committed file whose every byte comes from `GOTOOLCHAIN=go1.26.6 go run ./tools/clidoc -out docs/CLI-REFERENCE.md` — `task -s docs:cli:drift` exits 0 twice in a row printing `docs:cli:drift: compared 1 generated file` exactly once per run, and the two transcripts are byte-identical (byte-stable across runs and days because `root.DisableAutoGenTag = true` suppresses the date footer — research finding 1)"
    - "D-02: `tools/clidoc/main.go` builds the tree via the exported `internalcli.NewRootCmd()`, completes it with `root.InitDefaultCompletionCmd()` (research finding 2 — without it `completion` and its four shells are invisible) and `root.InitDefaultVersionFlag()` (planner addition, same reasoning: `codegraph --version` is registered at execute time and would otherwise be silently absent from BOTH artefacts), walks `cmd.Commands()` pre-order in Cobra's own alphabetical order, calls `doc.GenMarkdownCustom(cmd, &buf, linkHandler)` once per documented command into ONE buffer, and writes only the `-out` path"
    - "D-05: the committed reference carries exactly 35 `## codegraph…` command headings (root + 24 visible top-level + `daemon start|stop` + `githooks install|remove|status` + `completion` + `bash|zsh|fish|powershell`), no heading for the hidden `man` command, four `--no-descriptions` lines, at least one `--editor-url` line (the milestone's new flag, ROADMAP criterion 1), one `-v, --version` line, and zero cobra date-footer lines"
    - "D-03: `task docs:cli` regenerates in place; `task docs:cli:drift` enumerates the committed file with `git ls-files`, prints the compared count BEFORE comparing, regenerates into a `mktemp -d` scratch directory (never in place), byte-compares with `cmp -s`, and on mismatch names `docs/CLI-REFERENCE.md`, prints `diff -u … | head -40`, and exits 1; `.github/workflows/ci.yml` runs `task docs:cli:drift` as a step immediately after `task proto:drift` in the same `test` job (D-14: every `go` inside both targets is prefixed `GOTOOLCHAIN=go1.26.6`)"
    - "D-07: `TestEveryRegisteredFlagIsAccountedFor` in `internal/cli/cli_reference_test.go` builds the identical tree (`NewRootCmd()` + `InitDefaultCompletionCmd()` + `InitDefaultVersionFlag()`), walks EVERY command recursively — hidden `man` included — calling `cmd.InitDefaultHelpFlag()` per node exactly as `GenMarkdownCustom` does, visits `cmd.Flags()` and `cmd.PersistentFlags()` with `VisitAll`, de-duplicates by `CommandPath() + \" --\" + Name`, and attributes an inherited persistent flag to its declaring command only (pointer-identity check against every ancestor's `PersistentFlags().Lookup`)"
    - "D-08: one rule — a flag on a command whose whole ancestry is `IsAvailableCommand() && !IsAdditionalHelpTopicCommand()`, itself neither `Hidden` nor `Deprecated`, must match `--<name>` at a word boundary somewhere in `docs/CLI-REFERENCE.md`; every other flag must match an entry of `internal/cli/testdata/cli-reference-allowlist.txt` (`<command path>[ --flag]<TAB><reason>`, `#` comments, blank lines); an allowlist entry that matched nothing is itself a failure (`stale allowlist entry: …`); the committed allowlist has exactly one entry, `codegraph man`, with the v0.5.0 D-02 hidden-command reason"
    - "D-09: the guard `t.Logf`s and asserts `walked 36 commands` (floor ≥ 26) and `inspected 115 flags` (floor ≥ 50; 114 measured by research + root `--version`), reports `114 accepted via docs/CLI-REFERENCE.md, 1 accepted via testdata/cli-reference-allowlist.txt`, asserts the accounting identity reference + allowlist + unaccounted == inspected, and `t.Fatalf`s fail-closed when either file is unreadable or empty; it scope-matches nothing to sections and tests nothing about `cobra/doc`"
    - "D-04: resolved at plan time — no command gains a `Long` this phase: every Short-only command's `Use` line plus its flag usage strings already state its argument semantics (`node [symbol]` + `--file`/`--line`, `affected [files...]` + `--stdin`, `explore <query...>`, …), and `ui`'s existing `Long` already documents the `--editor-url` / `CODEGRAPH_EDITOR_URL` / `--no-editor-url` precedence, so the reference documents it automatically; `internal/cli/*.go` command prose is untouched (only `root.go` changes, and only by adding `NewRootCmd`)"
    - "DOCS-05 adjacency (explicit): two commands sharing a leaf name at different depths — `codegraph status` and `codegraph githooks status` — render under distinct `## codegraph status` / `## codegraph githooks status` headings, each exactly once, with distinct SEE ALSO anchors `#codegraph-status` / `#codegraph-githooks-status`; nothing merges"
    - "DOCS-05 empty (explicit): a subcommand-only parent with no flags of its own (`codegraph daemon`, `codegraph githooks`, `codegraph completion`) still gets its `## ` heading, its Short, and an `### Options` block carrying only `-h, --help`; a comment-only allowlist parses to zero entries and is not itself a failure — the walk then reports `codegraph man --help` unaccounted (Task 2's second RED); a zero-command walk can never pass the ≥ 26 floor"
    - "DOCS-05 ordering (explicit): `cobra.EnableCommandSorting` (default true, never overridden in `root.go`) and pflag's `SortFlags` default order commands and flags by name, and the generator adds no sort of its own — `rg '^## ' docs/CLI-REFERENCE.md | LC_ALL=C sort -c` exits 0 (pre-order alphabetical traversal is lexicographic on the heading lines), and the drift gate's two consecutive MATCH runs are the backstop-free proof of byte-stability"
    - "ASSUMPTION (DOCS-06 edge row `unclassified`, unresolved — recorded, not resolved): the guard's `--<name>` presence check is a whole-document word-boundary match, deliberately NOT scoped to the command's own section (D-09) — a name shared by many commands (`--path`, `--json`, `--help`) is accepted once it appears anywhere; a section-scoped omission cannot occur without `task docs:cli:drift` also going RED, because the doc is exactly the generator's output. Surfaced as an assumption, not proven by a test."
  artifacts:
    - "internal/cli/root.go — `func NewRootCmd() *cobra.Command { return newRootCmd() }` with a doc comment naming `tools/clidoc` as the out-of-package consumer; `newRootCmd()` NOT renamed; `Execute()` unchanged"
    - "tools/clidoc/main.go — package `main`; `-out` flag (default `docs/CLI-REFERENCE.md`); `linkHandler` (`.md` → `#anchor`, `_` → `-`); `documented(cmd)` predicate; pre-order `walk`; banner + H1 + one intro line before the first `## codegraph` section"
    - "docs/CLI-REFERENCE.md — generated, committed in the SAME commit as the generator, the Taskfile targets, the ci step and the export"
    - "Taskfile.yml — `docs:cli` and `docs:cli:drift` (inserted after `proto:drift`, before `web:deps`), each with a `desc:` stating DOCS-05 / D-01..D-03 / D-14 and the regenerate-into-temp shape"
    - ".github/workflows/ci.yml — step `CLI reference drift guard (DOCS-05)` / `run: task docs:cli:drift` immediately after `Proto codegen drift guard (BLD-04)`"
    - "internal/cli/cli_reference_test.go — `cliReferenceDocPath`, `cliReferenceAllowlistPath`, `cliReferenceMinCommands = 26`, `cliReferenceMinFlags = 50`, `cliReferenceTree()`, `documentedByReference()`, `inheritedFromAncestor()`, `parseCLIReferenceAllowlist()`, `docMentionsFlag()`, `TestEveryRegisteredFlagIsAccountedFor`"
    - "internal/cli/testdata/cli-reference-allowlist.txt — header comment lines + exactly one entry line: `codegraph man<TAB><reason>`"
  key_links:
    - "`internal/cli.NewRootCmd()` → `tools/clidoc` (`go run`) → `docs/CLI-REFERENCE.md` → `task docs:cli:drift` (`git ls-files` + `cmp -s`) → `ci.yml` step — the whole DOCS-05 chain is one binary invoked from two Taskfile call sites with different `-out` paths"
    - "`cliReferenceTree()` in the guard and the tree construction in `tools/clidoc/main.go` MUST stay in lockstep (`NewRootCmd` + `InitDefaultCompletionCmd` + `InitDefaultVersionFlag`) — the guard's `must appear in the doc` branch is only sound because both walks see the same universe"
    - "`documentedByReference(cmd)` in the guard ≡ the generator's `documented(cmd)` predicate applied to every ancestor (the generator never descends into an unavailable command, exactly like `GenMarkdownTreeCustom`)"
    - "`docs/CLI-REFERENCE.md` ← read by the guard at `../../docs/CLI-REFERENCE.md`; `testdata/cli-reference-allowlist.txt` ← read at `testdata/…` (package-relative, `go test` cwd)"
  prohibitions:
    - "`docs/CLI-REFERENCE.md` MUST NOT be hand-edited at any point — every byte comes from `task docs:cli`; a hand edit is exactly what `docs:cli:drift` exists to catch (D-01)"
    - "The generator MUST NOT emit one file per command, filter hidden commands by its own rule instead of `IsAvailableCommand()`/`IsAdditionalHelpTopicCommand()`, add its own sort, or write anywhere except the `-out` path (D-02, D-05)"
    - "The guard MUST NOT carry a per-flag exemption list in Go, scope-match a flag to its doc section, skip `--help` by name, or assert anything about `cobra/doc`'s rendering — only this project's artefacts are under test (D-08, D-09)"
    - "The generator and the guard MUST NOT build different trees — any `Init*` call added to one is added to the other in the same commit"
    - "`newRootCmd()` MUST NOT be renamed; `cmd/codegraph/main.go`, `go.mod` and `go.sum` MUST NOT change (`cobra/doc` is already required via `internal/cli/man.go`)"
    - "`task docs:cli:drift` MUST NOT write into the working tree — regeneration goes to a `mktemp -d` scratch path removed on EXIT"
    - "No `Long` is added to any command this phase (D-04 resolved: none needed); no `internal/cli/*.go` other than `root.go` and the new test file changes"
    - "No `rg -c`, no `rg -h`, no `go` without `GOTOOLCHAIN=go1.26.6`, no watch-mode flags, no `codegraph ui` process left running (D-14)"
---

<objective>
Land the DOCS-05 chain end-to-end as one tracer: export `NewRootCmd`, generate `docs/CLI-REFERENCE.md` from the live Cobra tree with `cobra/doc`, commit it beside the generator, and prove it current with a `docs:cli:drift` gate wired into CI — then close the generator's one blind spot with the DOCS-06 accounting guard and its one-entry allowlist, each demonstrated RED before it is trusted.

Purpose: a stale committed reference is the one real failure mode this phase guards against, and a hidden flag silently absent from both the reference and any record is the one thing a generator cannot see (ROADMAP Phase 12 Notes). Both gates exist only if they have been watched failing.

Decision coverage (all locked, `12-CONTEXT.md`): D-01: one generated committed file · D-02: `tools/clidoc` + exported constructor · D-03: `docs:cli` / `docs:cli:drift` + ci.yml · D-04: prose lives in `Short`/`Long`, no `Long` added · D-05: hidden commands and flags stay out of the reference · D-07: the walk shape · D-08: the one rule + allowlist + rot · D-09: positive counts, no section scoping, no cobra/doc test · D-14: `GOTOOLCHAIN=go1.26.6`, `rg -o … | wc -l`, count-before-compare. Honored downstream: D-06: README link (12-02) · D-10: RED families (12-03) · D-11: brew-trust wording (12-02) · D-12: no DOCS-07 test (12-02) · D-13: house artefacts (12-03).

Output: `NewRootCmd`, `tools/clidoc/main.go`, `docs/CLI-REFERENCE.md`, `docs:cli`, `docs:cli:drift`, the ci.yml step, `TestEveryRegisteredFlagIsAccountedFor`, `internal/cli/testdata/cli-reference-allowlist.txt`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cli-reference-docs-tail/12-CONTEXT.md
@.planning/phases/12-cli-reference-docs-tail/12-RESEARCH.md
@.planning/phases/12-cli-reference-docs-tail/12-PATTERNS.md
@.planning/phases/12-cli-reference-docs-tail/12-VALIDATION.md
@.planning/phases/11-graph-view-community-clustering/11-03-SUMMARY.md

Toolchain landmine: every local `go build` / `go test` / `go run` / `go vet` / `go list` MUST be prefixed `GOTOOLCHAIN=go1.26.6` (local Go is 1.27.1; `go.mod` pins 1.26.6), including the `go run ./tools/clidoc` lines INSIDE the two new Taskfile targets (D-14). `rg -h` is `--help`; never `rg -c`; positive counts via `rg -o … | wc -l | tr -d ' '`. `task -s` for grep-counted output. Under `set -euo pipefail` a `diff | head` pipeline needs `|| true` (11-03 lesson: the leftmost non-zero exit propagates). No Go-template literal (`{{`) appears in either new target, so no `{{"{{"}}` escape is needed — if one is ever added, escape it. The plan-time base commit is `ca015c4b`; "unchanged" assertions below diff against it.

Facts verified this planning session against `$GOMODCACHE/github.com/spf13/cobra@v1.10.2`: (1) `GenMarkdownCustom` calls `cmd.InitDefaultHelpCmd()` and `cmd.InitDefaultHelpFlag()` first, writes `## <CommandPath()>`, Short, `### Synopsis` (Long), a fenced `UseLine()` only when `Runnable()`, `### Examples`, `### Options` (`NonInheritedFlags()`), `### Options inherited from parent commands`, `### SEE ALSO` (parent + available children, links `<path with _>.md` passed through `linkHandler`), and a date footer unless `DisableAutoGenTag` — which it copies down from any ancestor via `VisitParents`, so setting it on root suffices; (2) `IsAvailableCommand()` is false for `Hidden`, `Deprecated`, and the auto `help` command, true for a non-runnable parent with available children (so root, `daemon`, `githooks`, `completion` are documented); (3) `InitDefaultVersionFlag()` is called only from `execute()`, adds `-v, --version` to root because `Version: versionLine()` is non-empty, and its usage text (`version for codegraph`) is static — the version string itself is never rendered; (4) `InitDefaultHelpFlag()` calls `mergePersistentFlags()`, which folds every ancestor's `PersistentFlags()` into `cmd.Flags()` — the guard must skip those by pointer identity or it double-counts (zero persistent flags exist today, so this is a design property, not a live count change); (5) every flag default in `internal/cli` is a literal and every `Short`/`Long`/`Example` is a static string — nothing machine- or date-dependent reaches the rendered text; (6) the full recursive tree with the completion family is 36 commands (root + 25 top-level incl. hidden `man` + `daemon start|stop` + `githooks install|remove|status` + `completion` + 4 shells), 35 of them documented.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "one generated CLI reference, kept current" — `NewRootCmd` → `tools/clidoc` → `docs/CLI-REFERENCE.md` committed → `task docs:cli:drift` MATCH → `ci.yml` step (D-01, D-02, D-03, D-04, D-05, D-14)</name>
  <precondition>`GOTOOLCHAIN=go1.26.6 go version` prints `go1.26.6` and `task --version` prints a 3.x version — every gate below runs the generator through `go run` and the drift target through Task</precondition>
  <reversibility rating="reversible">A generated doc, a generator program, two Taskfile targets, one ci.yml step and an exported wrapper — all deletable in one commit with no stored contract behind them.</reversibility>
  <files>internal/cli/root.go, tools/clidoc/main.go, docs/CLI-REFERENCE.md, Taskfile.yml, .github/workflows/ci.yml</files>
  <read_first>
    - internal/cli/root.go:33-70 (`newRootCmd()` doc comment and body — the 25 `AddCommand` args; `Execute()` — the wrapper goes directly below it in the same doc-comment style)
    - internal/cli/man.go:1-63 (the existing `github.com/spf13/cobra/doc` import and `doc.GenManTree(newRootCmd(), …)` call — the same package `tools/clidoc` uses; the `Hidden: true` D-02 rationale the allowlist reason will cite)
    - tools/graphcluster/main.go:1-13 (package doc-comment convention: mechanism, what it never does, exit codes)
    - $GOMODCACHE/github.com/spf13/cobra@v1.10.2/doc/md_docs.go (`GenMarkdownCustom` whole; `GenMarkdownTreeCustom`'s skip predicate at the top of its loop — copy that predicate, not the function) and doc/util.go (`hasSeeAlso`)
    - $GOMODCACHE/github.com/spf13/cobra@v1.10.2/command.go (`InitDefaultVersionFlag`, `InitDefaultCompletionCmd` call in `ExecuteC`, `IsAvailableCommand`, `IsAdditionalHelpTopicCommand`, `Commands()` sorting)
    - Taskfile.yml:305-480 (`proto:gen` / `proto:drift` — the exact regenerate-into-temp shape: `set -euo pipefail`, `scratch=$(mktemp -d)`, `trap … EXIT`, `git ls-files` enumeration, `nfiles` echo BEFORE the floor check, `::error::` messages, `cmp -s`, closing "temporary tree only — source tree untouched" line) and :1589-1600 (`check:gonum` desc: style)
    - .github/workflows/ci.yml:188-204 (the `Proto codegen drift guard (BLD-04)` step and its comment block — the new step's shape and position)
    - internal/upgrade/proto_task_test.go:105-205 (what the Taskfile shape guards parse — the new targets must stay plain `desc:` + `cmds:` YAML)
    - 12-RESEARCH.md § Code Examples (`tools/clidoc/main.go` skeleton — the walk shape to implement, with the additions below) and § Common Pitfalls 1–5
  </read_first>
  <behavior>
    - RED (drift gate, before the doc is tracked): with `docs/CLI-REFERENCE.md` generated but NOT yet `git add`ed, `GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift` prints `docs:cli:drift: compared 0 generated file`, then `::error::docs:cli:drift: enumerated only 0 committed generated file …`, and exits 1 — the floor fires (rule `84d1gfpywd`), never a vacuous pass.
    - GREEN: after the commit, `task -s docs:cli:drift` exits 0, prints `docs:cli:drift: compared 1 generated file` exactly once and a `byte-identical` closing line; a second run produces a byte-identical transcript.
    - Shape: exactly 35 `## codegraph…` headings in `LC_ALL=C` sorted order; no hidden `man` section; `--no-descriptions` ×4; `--editor-url` ≥ 1; `-v, --version` ×1; zero date footers; every SEE ALSO link is an in-file `#anchor`, never a `.md` file link; `## codegraph status` and `## codegraph githooks status` each exactly once.
  </behavior>
  <action>
    **Export (D-02, Claude's Discretion resolved: wrapper, not rename).** In `internal/cli/root.go`, directly after `Execute()`, add `func NewRootCmd() *cobra.Command { return newRootCmd() }` with a doc comment in the file's existing style: it returns a freshly built root tree identical to what `Execute()` runs, exists so `tools/clidoc` (an out-of-package doc generator) can obtain the same tree without this package importing anything new, and changes no behaviour of the shipped binary. Do not rename `newRootCmd()` (three test call sites and `man.go:61` keep working untouched); do not touch `cmd/codegraph/main.go`.

    **Generator (D-01, D-02, D-05).** Create `tools/clidoc/main.go`, package `main`, with a package doc comment in the `tools/graphcluster` convention: it is DOCS-05's generator; it builds the full `codegraph` tree via `internalcli.NewRootCmd()`, completes it with `root.InitDefaultCompletionCmd()` (the completion family is otherwise registered only inside `Command.ExecuteC()` — 12-RESEARCH Pitfall 2) and `root.InitDefaultVersionFlag()` (same class of execute-time registration: `codegraph --version` is real and would otherwise be absent from the reference AND invisible to the guard, the silent-agreement shape this milestone eliminates — a planner addition beyond CONTEXT, recorded here), sets `root.DisableAutoGenTag = true` (Pitfall 1 — the footer would embed today's date and break the drift gate daily; one assignment on root suffices because `GenMarkdownCustom` copies it down via `VisitParents`), walks every documented command in Cobra's own order, writes one `docs/CLI-REFERENCE.md`, never more than one file, never anything outside `-out`; exit 0 on success, 1 on any error. Imports: `bytes`, `flag`, `fmt`, `os`, `regexp` or `strings` as needed, `github.com/spf13/cobra`, `github.com/spf13/cobra/doc`, `internalcli "github.com/seanb4t/codegraph-go/internal/cli"`. No `go.mod` change (`cobra/doc` is already a direct requirement through `man.go`).

    `main`: `out := flag.String("out", "docs/CLI-REFERENCE.md", …)`; `flag.Parse()`; build and complete the tree as above; write the header into a `bytes.Buffer`: line 1 an HTML comment naming the file as generated by `tools/clidoc` from the live command tree, never hand-edited, regenerated by `task docs:cli` and checked by `task docs:cli:drift`; then a blank line, an H1 `# codegraph CLI reference`, a blank line, ONE static intro sentence (what the file is, that hidden commands are omitted by design, and that `codegraph <command> --help` prints the same text), and a blank line. Define `documented(c *cobra.Command) bool` returning `c.IsAvailableCommand() && !c.IsAdditionalHelpTopicCommand()` — the exact predicate cobra's own tree generator uses to decide what to descend into (md_docs.go, the loop at the top of the tree function). Define `walk(c)`: call `doc.GenMarkdownCustom(c, &buf, linkHandler)` (wrap an error with the command path), then for each `sub` in `c.Commands()`: if `!documented(sub)` continue; else `walk(sub)`. Call `walk(root)` — root itself is emitted (open question 1: yes; root is available because it has available children, so the top of the file is the `## codegraph` section a real `codegraph --help` implies). Never descend into an undocumented command (so a hidden command's children are omitted with it, exactly as cobra's tree generator does); never filter by any rule of your own; never sort (Cobra's `Commands()` and pflag's `SortFlags` already do — Pitfall 4; the file imports no sort package). In comments refer to cobra's one-file-per-command generator by that description, not by its `doc.`-qualified identifier — the source assertion below forbids the call form. Keep `### SEE ALSO` blocks (Claude's Discretion: harmless in one file, and they give every section a link to its parent and children). `linkHandler(link string) string`: `strings.TrimSuffix(link, ".md")`, then `strings.ReplaceAll(_, "_", "-")`, prefixed with `#` — cobra already replaced spaces with `_`, and every `CommandPath()` in this tree is lowercase ASCII, so the result is GitHub's own auto-anchor for the matching `## ` heading (12-RESEARCH A1). Finally `os.WriteFile(*out, buf.Bytes(), 0o644)`.

    **D-04 (Claude's Discretion resolved: no `Long` added).** Before generating, apply the D-04 criterion to every Short-only command — `affected`, `callees`, `callers`, `explore`, `files`, `impact`, `index`, `init`, `node`, `query`, `search`, `status`, `sync`, `uninit`, `unlock`, `daemon start|stop`, `githooks install|remove|status` — by reading `codegraph <cmd> --help` (build once: `GOTOOLCHAIN=go1.26.6 go build -o /tmp/12-01-codegraph ./cmd/codegraph`). The plan-time reading: each `Use` line plus its flag usage strings already states the argument semantics (`node [symbol]` with `--file`/`--line` explaining both modes; `affected [files...]` with `--stdin` explaining the union; `explore <query...>`; `impact <symbol>` with `--depth (default 2, max 50)`), and `ui`'s existing `Long` already documents the `--editor-url` precedence chain. Record in the SUMMARY that the criterion was applied and no `Long` was added — do not gold-plate, and do not edit any command file.

    **Taskfile (D-03, D-14).** Insert two targets immediately after `proto:drift` (before `web:deps`), plain `desc:` + `cmds:` map entries so `internal/upgrade`'s Taskfile parsers keep working. `docs:cli`: desc states DOCS-05 / D-01–D-03, that the file is regenerated IN PLACE from the live Cobra tree by `tools/clidoc`, that it is the only sanctioned way to change `docs/CLI-REFERENCE.md`, and that every `go` line is `GOTOOLCHAIN=go1.26.6`-prefixed (D-14); cmds: `set -euo pipefail`, `GOTOOLCHAIN=go1.26.6 go run ./tools/clidoc -out docs/CLI-REFERENCE.md`, then an `echo "docs:cli: regenerated docs/CLI-REFERENCE.md from the live Cobra tree via tools/clidoc"`. `docs:cli:drift`: desc mirrors `proto:drift`'s — regenerates with the same pinned toolchain into a TEMPORARY directory, never in place, so a developer's uncommitted edit survives; reports `compared N generated file` BEFORE comparing and fails with a named error when N is below the floor of 1 (rule `84d1gfpywd` — a broken enumeration must never read as a clean pass); a header-only or whitespace-only difference IS drift; wired into ci.yml's `test` job right after `proto:drift`; preconditions `command -v go` and `command -v git` with messages in the house style. cmds (one `|` block): `set -euo pipefail`; `scratch=$(mktemp -d)`; `trap 'rm -rf "${scratch}"' EXIT`; `files=$(git ls-files -- 'docs/CLI-REFERENCE.md')`; `nfiles=0` then the `proto:drift` `printf … | wc -l | tr -d ' '` idiom; `echo "docs:cli:drift: compared ${nfiles} generated file"`; if `nfiles -lt 1` echo `::error::docs:cli:drift: enumerated only ${nfiles} committed generated file at docs/CLI-REFERENCE.md (expected exactly 1) — a broken enumeration must fail loud, never read as a clean pass` and `exit 1`; `GOTOOLCHAIN=go1.26.6 go run ./tools/clidoc -out "${scratch}/CLI-REFERENCE.md"`; `if ! cmp -s docs/CLI-REFERENCE.md "${scratch}/CLI-REFERENCE.md"; then` echo `::error::docs:cli:drift: docs/CLI-REFERENCE.md differs from a fresh regeneration by the pinned toolchain — run \`task docs:cli\` and commit the result`, then `diff -u docs/CLI-REFERENCE.md "${scratch}/CLI-REFERENCE.md" | head -40 || true` (the `|| true` is mandatory under pipefail — `diff` exits 1 on any difference), then `exit 1`; `fi`; closing `echo "docs:cli:drift: docs/CLI-REFERENCE.md byte-identical to a fresh regeneration (temporary file only — source tree untouched)"`.

    **ci.yml (D-03).** In the `test` job, directly after the `Proto codegen drift guard (BLD-04)` step, add a comment block (DOCS-05, v0.13.0 Phase 12: proves the committed reference still matches what `tools/clidoc` generates from the live tree; regenerated into a temp file, never in place; fires on every flag addition that forgets `task docs:cli`) and the step `- name: CLI reference drift guard (DOCS-05)` / `run: task docs:cli:drift`. No new job, no new setup.

    **Sequence and commit.** (1) Export + generator + Taskfile + ci.yml written. (2) `GOTOOLCHAIN=go1.26.6 task docs:cli` → the doc exists, untracked. (3) Run the RED drift gate below and paste its transcript in the SUMMARY (the floor fires on the untracked file). (4) Run the shape gates on the generated file; if any fails, fix the GENERATOR (never the doc) and regenerate. (5) `git add` all five paths and commit ONCE: `feat(12-01): generate docs/CLI-REFERENCE.md from the live Cobra tree with a docs:cli:drift gate` — the doc lands in the same commit as the generator, the targets and the ci step (12-VALIDATION Wave 0). (6) Run the GREEN drift gate twice. Also run `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/upgrade/...` to prove the Taskfile/ci shape guards still pass with the new targets and step.
  </action>
  <verify>
    <automated>test -f docs/CLI-REFERENCE.md &amp;&amp; test -z "$(git ls-files -- docs/CLI-REFERENCE.md)" &amp;&amp; GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift &gt;/tmp/12-01-drift-red.txt 2&gt;&amp;1; RC=$?; cat /tmp/12-01-drift-red.txt; test "$RC" != "0" &amp;&amp; test "$(rg -o 'compared 0 generated file' /tmp/12-01-drift-red.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'enumerated only 0' /tmp/12-01-drift-red.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>RED gate (run AFTER `task docs:cli` and BEFORE `git add docs/CLI-REFERENCE.md`): the untracked doc must make the drift target report a compared count of 0 and fail loud on the floor; a green run here means the enumeration does not use the tracked set or the floor is missing</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift &gt;/tmp/12-01-drift.txt 2&gt;&amp;1; RC=$?; cat /tmp/12-01-drift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o 'compared 1 generated file' /tmp/12-01-drift.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'byte-identical' /tmp/12-01-drift.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '::error::' /tmp/12-01-drift.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift &gt;/tmp/12-01-drift2.txt 2&gt;&amp;1 &amp;&amp; cmp -s /tmp/12-01-drift.txt /tmp/12-01-drift2.txt &amp;&amp; test -z "$(git status --porcelain -- docs/CLI-REFERENCE.md)"</automated>
    <fails_when>the committed doc is not byte-identical to a fresh regeneration, the count line is missing or printed twice, an error line leaks, the second run's transcript differs from the first (a date, path or machine-dependent byte reached the output), or the drift run left the working tree dirty (it regenerated in place)</fails_when>
    <automated>test "$(head -1 docs/CLI-REFERENCE.md | rg -o 'task docs:cli' | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '^## codegraph' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "35" &amp;&amp; test "$(rg -o '^## codegraph man$' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^## codegraph completion (bash|zsh|fish|powershell)$' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o -- '--no-descriptions' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o -- '--editor-url' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o -- '-v, --version' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'Auto generated by spf13/cobra' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "0" &amp;&amp; rg '^## ' docs/CLI-REFERENCE.md | LC_ALL=C sort -c &amp;&amp; test "$(rg -o '^## codegraph status$' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^## codegraph githooks status$' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '\]\(#codegraph-githooks-status\)' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '\]\(codegraph[_a-z]*\.md\)' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^### Options inherited from parent commands' docs/CLI-REFERENCE.md | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the banner does not name the regenerating task, the heading count is not 35 (completion family missing → 30; root missing → 34; hidden `man` leaked → 36), any completion shell or `--no-descriptions` line is missing, `--editor-url` or root `--version` is absent, a date footer survived, headings are out of Cobra's alphabetical pre-order, the two same-leaf `status` commands did not both render, a SEE ALSO link still points at a `.md` file instead of an in-file anchor, or an inherited-options block appeared (no persistent flags exist today — one appearing means the tree changed under the plan)</fails_when>
    <automated>test "$(rg -o '^func NewRootCmd\(\) \*cobra\.Command' internal/cli/root.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^func newRootCmd\(\) \*cobra\.Command' internal/cli/root.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'internalcli\.NewRootCmd\(\)' tools/clidoc/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'InitDefaultCompletionCmd\(\)' tools/clidoc/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'InitDefaultVersionFlag\(\)' tools/clidoc/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'DisableAutoGenTag = true' tools/clidoc/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'doc\.GenMarkdownCustom\(' tools/clidoc/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'IsAvailableCommand\(\)' tools/clidoc/main.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'IsAdditionalHelpTopicCommand\(\)' tools/clidoc/main.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'doc\.GenMarkdownTree|"sort"' tools/clidoc/main.go | wc -l | tr -d ' ')" = "0" &amp;&amp; git diff --quiet ca015c4b -- go.mod go.sum cmd/codegraph/main.go &amp;&amp; test "$(git diff --name-only ca015c4b HEAD -- internal/cli | rg -v '^internal/cli/(root\.go|cli_reference_test\.go|testdata/cli-reference-allowlist\.txt)$' | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the exported wrapper or the unrenamed constructor is missing, the generator does not build the tree exactly as specified (completion + version + no auto-gen tag + `GenMarkdownCustom` behind the two availability predicates), the generator grew a tree generator or a sort of its own, `go.mod`/`go.sum`/`main.go` moved, or any `internal/cli` file other than `root.go` and the guard pair changed (a `Long` was added against D-04)</fails_when>
    <automated>test "$(rg -o '^  docs:cli:$' Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^  docs:cli:drift:$' Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'GOTOOLCHAIN=go1\.26\.6 go run \./tools/clidoc' Taskfile.yml | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o "git ls-files -- 'docs/CLI-REFERENCE\.md'" Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'compared \$\{nfiles\} generated file' Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'cmp -s docs/CLI-REFERENCE\.md' Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'head -40 \|\| true' Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'run: task docs:cli:drift' .github/workflows/ci.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; L1=$(rg -n 'run: task proto:drift' .github/workflows/ci.yml | cut -d: -f1) &amp;&amp; L2=$(rg -n 'run: task docs:cli:drift' .github/workflows/ci.yml | cut -d: -f1) &amp;&amp; test "$L2" -gt "$L1" &amp;&amp; test $((L2 - L1)) -le 12 &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/upgrade/... 2&gt;&amp;1 | tee /tmp/12-01-shape.txt | tail -2; test "$(rg -o '^ok' /tmp/12-01-shape.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^FAIL' /tmp/12-01-shape.txt | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>either target is missing or misnamed, a `go run` lost its `GOTOOLCHAIN` prefix, the drift target does not enumerate via the tracked set / report before comparing / `cmp -s` / guard its diff pipeline, the ci step is absent or not within a dozen lines after the proto:drift step, or the `internal/upgrade` Taskfile/workflow shape guards went red on the new YAML</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/ ./tools/clidoc/ &amp;&amp; GOTOOLCHAIN=go1.26.6 go build ./... &amp;&amp; test "$(git show --name-only --format= HEAD | rg -o '^(internal/cli/root\.go|tools/clidoc/main\.go|docs/CLI-REFERENCE\.md|Taskfile\.yml|\.github/workflows/ci\.yml)$' | wc -l | tr -d ' ')" = "5" &amp;&amp; test -z "$(git status --porcelain)"</automated>
    <fails_when>vet or build fails, the tracer commit does not carry exactly the five files together (the doc must land with its generator and gates), or the tree is dirty at task end</fails_when>
  </verify>
  <acceptance_criteria>
    - The RED drift transcript (count 0, floor error, exit 1) and both GREEN transcripts (count 1, byte-identical, exit 0, identical to each other) are pasted in the SUMMARY
    - `docs/CLI-REFERENCE.md` has 35 command headings in sorted pre-order, no `man` section, the four completion shells, `--editor-url`, `-v, --version`, and no date footer
    - The five files land in ONE commit; `go.mod`, `go.sum`, `cmd/codegraph/main.go` and every other `internal/cli` file are unchanged from `ca015c4b`
    - The SUMMARY records the D-04 audit outcome (no `Long` added, criterion applied to the listed Short-only commands) and the `--version` planner addition with its reason
  </acceptance_criteria>
  <done>A real `codegraph` tree, completed the way `Execute()` completes it, is rendered by `cobra/doc` into one committed reference; the drift gate has been seen failing on an untracked file and passing byte-stably twice on the committed one; CI runs the gate right after `proto:drift`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DOCS-06 accounting guard — `TestEveryRegisteredFlagIsAccountedFor` RED on a missing allowlist, RED naming `codegraph man --help` on an empty one, GREEN with the one-entry allowlist and logged counts (D-05, D-07, D-08, D-09)</name>
  <reversibility rating="reversible">A test file and a text fixture; deletable without trace.</reversibility>
  <files>internal/cli/cli_reference_test.go, internal/cli/testdata/cli-reference-allowlist.txt</files>
  <read_first>
    - `git show 5139e60c^:internal/cli/flag_parity_test.go` (the deleted 08-06 walk: package-scope path constant, fail-closed `os.ReadFile` + `t.Fatalf`, recursive `walk` over `cmd.Commands()`, sorted `missing` list in the failure message — the skeleton; D-07 diverges in three named ways: `InitDefaultHelpFlag()` per node instead of skipping `help` by name, `Flags()` + `PersistentFlags()` with declaring-command attribution, hidden commands walked and routed to the allowlist branch)
    - internal/cli/man_test.go:95-115 (`TestManCmd_HiddenOnRootCommand` — reading the command OBJECT, never rendered help text; rule `84d1gfpywd`)
    - internal/upgrade/taskfile_shape_test.go:22-45 (package-scope relative path constants; `t.Fatalf` naming the exact missing artefact)
    - tools/clidoc/main.go (Task 1 — the tree construction the guard MUST mirror line for line: `NewRootCmd()`, `InitDefaultCompletionCmd()`, `InitDefaultVersionFlag()`; the `documented` predicate)
    - $GOMODCACHE/github.com/spf13/cobra@v1.10.2/command.go (`InitDefaultHelpFlag` — note it calls `mergePersistentFlags()`, folding ancestors' persistent flags into `Flags()`; `LocalFlags` — the `f != c.parentsPflags.Lookup(f.Name)` pointer-identity idiom to copy for inherited detection) and $GOMODCACHE/github.com/spf13/pflag@v1.0.10/flag.go (`Flag.Hidden`, `Flag.Deprecated`, `VisitAll` visits hidden and deprecated flags)
    - 12-RESEARCH.md § Anti-Patterns (a deprecated flag DOES render in the doc — its appearance there is NOT a pass condition; only the allowlist counts) and § Code Examples (guard skeleton)
  </read_first>
  <behavior>
    - RED 1 (fail-closed, before `testdata/cli-reference-allowlist.txt` exists): `go test -run TestEveryRegisteredFlagIsAccountedFor ./internal/cli/` fails with `fail-closed: testdata/cli-reference-allowlist.txt must exist and be readable`.
    - RED 2 (empty allowlist — a file containing only `#` comment lines): the test fails with exactly one unaccounted line, `unaccounted flag: codegraph man --help (flag on a hidden command — add an allowlist entry with a reason)`, and still logs the counts (36 commands, 115 flags) — the walk is real even when the verdict is red.
    - GREEN (one entry `codegraph man<TAB>…`): passes; `t.Logf` prints `walked 36 commands (hidden included), inspected 115 flags: 114 accepted via ../../docs/CLI-REFERENCE.md, 1 accepted via testdata/cli-reference-allowlist.txt`; both floors clear.
    - Rot is enforced: an entry that matches nothing produces `stale allowlist entry: <entry> (matches no registered command or flag)` — exercised for real in 12-03 family (c), not here.
    - Abuse is impossible: an allowlist entry naming an available command's visible flag is never consulted (that flag takes the reference branch) and therefore reports as stale — the allowlist cannot be used to hide a documented flag.
  </behavior>
  <action>
    **Test file (D-07, D-08, D-09).** Create `internal/cli/cli_reference_test.go` (package `cli`, imports `os`, `regexp`, `sort`, `strings`, `testing`, `github.com/spf13/cobra`, `github.com/spf13/pflag`). Package-scope constants: `cliReferenceDocPath = "../../docs/CLI-REFERENCE.md"`, `cliReferenceAllowlistPath = "testdata/cli-reference-allowlist.txt"`, `cliReferenceMinCommands = 26`, `cliReferenceMinFlags = 50` (D-09 floors; doc comments say the real tree is 36 / 115 today and the floors exist so a walk that finds nothing can never pass — rule `84d1gfpywd`). Doc comment on the test: it is DOCS-06's guard for the one thing a generated reference cannot show — hidden, deprecated, and hidden-command flags vanish from `cobra/doc` output by design (D-05) — so every registered flag must be accounted for either by the reference or by a committed, reason-carrying allowlist; it tests this project's artefacts only, never `cobra/doc` (D-09); it does not scope flags to sections because `task docs:cli:drift` already guarantees the doc's content is exactly the generator's.

    Helpers: `cliReferenceTree() *cobra.Command` — `NewRootCmd()`, `root.InitDefaultCompletionCmd()`, `root.InitDefaultVersionFlag()`, with a comment that this MUST stay in lockstep with `tools/clidoc/main.go` (the reference branch is only sound because both walks see the same universe). `documentedByReference(c *cobra.Command) bool` — true iff `c` and every ancestor up to root satisfy `IsAvailableCommand() && !IsAdditionalHelpTopicCommand()` (the generator never descends into an undocumented command, so a hidden command's children are undocumented too). `inheritedFromAncestor(c *cobra.Command, f *pflag.Flag) bool` — walks `c.Parent()` upward and returns true when some ancestor's `PersistentFlags().Lookup(f.Name) == f` by pointer (the `LocalFlags` idiom): such a flag was merged into `c.Flags()` by `InitDefaultHelpFlag()`'s `mergePersistentFlags()` and is accounted for at its declaring command (D-07: never re-counted on a descendant). `parseCLIReferenceAllowlist(data []byte) (map[string]string, error)` — one entry per non-blank, non-`#` line, split on the FIRST tab into key and reason; key is `<command path>` or `<command path> --<flag>` (single spaces); a line without a tab or with an empty reason is an error (fail closed — a reason is mandatory, D-08); a duplicate key is an error. `docMentionsFlag(doc string, name string) bool` — `regexp.MustCompile("(?m)(^|[^A-Za-z0-9_-])--" + regexp.QuoteMeta(name) + "([^A-Za-z0-9_-]|$)")` so `--no` never passes on the strength of `--no-descriptions` (Claude's Discretion: a word-boundary match, still whole-document, still not section-scoped).

    `TestEveryRegisteredFlagIsAccountedFor`: read both files fail-closed (`t.Fatalf("fail-closed: %s must exist and be readable: %v", …)`; also fatal when the doc is zero bytes); parse the allowlist (fatal on error); `used := map[string]bool{}` over allowlist keys. Build `root := cliReferenceTree()`. Walk recursively: per command `cmd.InitDefaultHelpFlag()` (so the walk sees the `--help` the generator prints — 12-RESEARCH Pitfall 3), `cmdCount++`; `visit := func(f *pflag.Flag)` keyed by `cmd.CommandPath() + " --" + f.Name` in a `seen` set (de-dup — a persistent flag appears in both `Flags()` post-merge and `PersistentFlags()`); on first sight `flagCount++` and classify: `eligible := documentedByReference(cmd) && !f.Hidden && f.Deprecated == ""`; if eligible → `docMentionsFlag` ? `viaReference++` : append `unaccounted flag: <path> --<name> (visible flag on a documented command — missing from docs/CLI-REFERENCE.md; run task docs:cli)`; else → if `allow[path+" --"+name]` or `allow[path]` exists → mark it used, `viaAllowlist++`; else append `unaccounted flag: <path> --<name> (<why: flag on a hidden command | hidden flag | deprecated flag> — add an allowlist entry with a reason)`. Visit `cmd.Flags()` with `VisitAll`, skipping any `f` for which `inheritedFromAncestor(cmd, f)`; then `cmd.PersistentFlags().VisitAll(visit)`; recurse into ALL of `cmd.Commands()` — hidden included, no availability filter here (D-07: EVERY command). Keep two slices: `unaccounted` (flag-level lines) and `stale` (allowlist-level lines). After the walk: for every allowlist key not in `used` append to `stale` the line `stale allowlist entry: <key> (matches no registered command or flag)` (D-08 rot rule). Then `t.Logf("walked %d commands (hidden included), inspected %d flags: %d accepted via %s, %d accepted via %s", cmdCount, flagCount, viaReference, cliReferenceDocPath, viaAllowlist, cliReferenceAllowlistPath)`; `t.Fatalf` if `cmdCount < cliReferenceMinCommands` or `flagCount < cliReferenceMinFlags` (naming the floor and the count); `t.Fatalf` if `viaReference + viaAllowlist + len(unaccounted) != flagCount` (the accounting identity — every inspected flag landed in exactly one bucket); finally, if `unaccounted` or `stale` is non-empty, sort each and `t.Fatalf("%d problem(s):\n%s", …)` listing both. The test file's own comments must not call or name the merged flag views (`InheritedFlags`, `NonInheritedFlags`, `LocalFlags` — 12-RESEARCH Pitfall 5) and must not import the cobra doc package — the source assertions below check both.

    **Allowlist fixture (D-08, D-05).** Create `internal/cli/testdata/cli-reference-allowlist.txt`: three `#` header lines (what the file is; the line format `<full command path>[ --flag]<TAB><reason>` with a bare command path covering every flag on that command; that an entry matching nothing FAILS the guard, so the list can only shrink when the thing it covers is removed) and exactly ONE entry: `codegraph man` TAB `hidden by v0.5.0 D-02 — the Homebrew cask post-install hook's man-page generator, never an interactive command; its only flag is cobra's --help (12-CONTEXT D-05)`. Use a real tab character.

    **Sequence.** Write the test FIRST and run it → RED 1 (allowlist missing) — paste. Create the allowlist with only the header comments → run → RED 2 naming `codegraph man --help` — paste (this is the guard doing exactly its job on the one hidden command). Add the entry → GREEN with the counts line — paste. Run the whole `./internal/cli/` package to prove nothing else moved. Commit test + fixture together: `test(12-01): TestEveryRegisteredFlagIsAccountedFor — every registered flag is in the reference or the reason-carrying allowlist` (no separate RED commit: `task test:unit` must be green at every commit — the 10-01 lesson; the RED transcripts live in the SUMMARY).
  </action>
  <verify>
    <automated>test ! -f internal/cli/testdata/cli-reference-allowlist.txt &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestEveryRegisteredFlagIsAccountedFor' ./internal/cli/ 2&gt;&amp;1 | tee /tmp/12-01-guard-red1.txt; test "$(rg -o 'fail-closed: testdata/cli-reference-allowlist\.txt' /tmp/12-01-guard-red1.txt | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o -- '--- FAIL: TestEveryRegisteredFlagIsAccountedFor' /tmp/12-01-guard-red1.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>RED 1 gate (run BEFORE the allowlist file exists): the guard must fail closed naming the missing allowlist; a pass or a different failure means the fail-closed read is missing or misordered</fails_when>
    <automated>test "$(rg -v '^#' internal/cli/testdata/cli-reference-allowlist.txt | rg -v '^\s*$' | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestEveryRegisteredFlagIsAccountedFor' ./internal/cli/ 2&gt;&amp;1 | tee /tmp/12-01-guard-red2.txt; test "$(rg -o 'unaccounted flag: codegraph man --help' /tmp/12-01-guard-red2.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'unaccounted flag:' /tmp/12-01-guard-red2.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'walked 36 commands' /tmp/12-01-guard-red2.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o -- '--- FAIL: TestEveryRegisteredFlagIsAccountedFor' /tmp/12-01-guard-red2.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>RED 2 gate (run with the comment-only allowlist): the ONLY unaccounted item must be the hidden `man` command's `--help` and the counts line must still print — any other unaccounted line means the reference branch or the tree construction disagrees with the generator; no counts line means the walk aborted before reporting</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestEveryRegisteredFlagIsAccountedFor' ./internal/cli/ 2&gt;&amp;1 | tee /tmp/12-01-guard-green.txt | tail -4; test "$(rg -o -- '--- PASS: TestEveryRegisteredFlagIsAccountedFor' /tmp/12-01-guard-green.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/12-01-guard-green.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'walked 36 commands \(hidden included\), inspected 11[4-9] flags: 11[3-9] accepted via \.\./\.\./docs/CLI-REFERENCE\.md, 1 accepted via testdata/cli-reference-allowlist\.txt' /tmp/12-01-guard-green.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; ! rg -q 'no tests to run' /tmp/12-01-guard-green.txt</automated>
    <fails_when>the named PASS line is missing, any FAIL appears, the counts line is absent or reports other than 36 commands / 114–119 flags / exactly 1 allowlist-accepted flag (the completion family or `--version` fell out of one walk, or `man` grew flags), or the pattern matched no test</fails_when>
    <automated>test "$(rg -v '^#' internal/cli/testdata/cli-reference-allowlist.txt | rg -v '^\s*$' | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o $'^codegraph man\t[^\t]+$' internal/cli/testdata/cli-reference-allowlist.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'NewRootCmd\(\)' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'InitDefaultCompletionCmd\(\)' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'InitDefaultVersionFlag\(\)' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'InitDefaultHelpFlag\(\)' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'PersistentFlags\(\)\.VisitAll' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'Flags\(\)\.VisitAll' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o 'stale allowlist entry' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'cliReferenceMinCommands\s*= 26' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'cliReferenceMinFlags\s*= 50' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'f\.Name == "help"' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '\.InheritedFlags\(\)|\.NonInheritedFlags\(\)|\.LocalFlags\(\)' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'spf13/cobra/doc"' internal/cli/cli_reference_test.go | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the allowlist has other than exactly one tab-separated `codegraph man` entry, the guard builds a different tree than the generator (missing completion/version init), skips `--help` by name (the deleted test's shortcut D-07 replaces), CALLS the merged/inherited flag views the research warns double-count, lacks the rot message or the two floors, or imports the cobra doc package into the test (testing the library instead of the artefacts)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/ &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/cli/ ./internal/cli/archtest/ 2&gt;&amp;1 | tee /tmp/12-01-pkgs.txt | tail -3; test "$(rg -o '^ok' /tmp/12-01-pkgs.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o '^FAIL' /tmp/12-01-pkgs.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(git show --name-only --format= HEAD | rg -o '^internal/cli/(cli_reference_test\.go|testdata/cli-reference-allowlist\.txt)$' | wc -l | tr -d ' ')" = "2" &amp;&amp; test -z "$(git status --porcelain)"</automated>
    <fails_when>vet fails, `internal/cli` or its archtest prints FAIL (the wrapper or the test tripped an import-confinement guard), the guard and its fixture are not committed together, or the tree is dirty at task end</fails_when>
  </verify>
  <acceptance_criteria>
    - Both RED transcripts (missing allowlist; comment-only allowlist naming `codegraph man --help`) and the GREEN transcript with the counts line are pasted in the SUMMARY
    - The guard's counts line reads 36 commands, 115 flags (114 + root `--version`), 114 via reference, 1 via allowlist — or the SUMMARY explains any deviation against the tree, never by loosening a floor
    - The allowlist has exactly one entry, `codegraph man`, tab-separated, with a reason citing the v0.5.0 hidden-command decision
    - `task test:unit` reaches the guard with no extra wiring (`internal/cli` is not excluded) — stated in the SUMMARY
  </acceptance_criteria>
  <done>Every flag the binary registers is accounted for by name — visible flags in the generated reference, the hidden `man` command's flag in a committed allowlist with a reason — with the walk's own counts asserted and logged, and the guard has been watched failing twice (fail-closed, and on the exact blind spot it exists to close) before it was trusted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Cobra tree → `tools/clidoc` → `docs/CLI-REFERENCE.md` | in-process data only — no user input; the risk is the committed file drifting from (or being edited away from) what the tree says |
| `docs/CLI-REFERENCE.md` + allowlist → `cli_reference_test.go` | two committed files read by fixed relative paths; the risk is a silent omission (a flag in neither) or a stale exemption |
| `internal/cli` → `tools/clidoc` (new exported `NewRootCmd`) | a package API widening that must return exactly the tree `Execute()` already runs |
| `Taskfile.yml` / `ci.yml` → developer working tree | a drift target that regenerated in place could clobber uncommitted edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | `docs/CLI-REFERENCE.md` hand-edited after generation (a flag removed, misleading text inserted) or left stale after a flag change | high | mitigate | `task docs:cli:drift` (Task 1) regenerates into `mktemp -d` and `cmp -s`-compares byte-for-byte, names the file, prints the diff head and exits 1; wired into ci.yml's `test` job; watched RED in 12-03 family (b) |
| T-12-02 | Information Disclosure (avoidance failure) | a hidden / deprecated / hidden-command flag absent from BOTH the reference and any record — the generator's blind spot | high | mitigate | `TestEveryRegisteredFlagIsAccountedFor` (Task 2) walks every command including hidden ones and fails naming `<path> --<flag>` unless the reference or a reason-carrying allowlist entry covers it; runs in `task test:unit`; watched RED in 12-03 family (a) |
| T-12-03 | Tampering | allowlist rot — an entry lingers after its flag/command is removed, or is added to hide a visible flag | medium | mitigate | every entry must match ≥ 1 inspected flag or the guard fails (`stale allowlist entry`); visible flags on documented commands never consult the allowlist, so an entry for one is stale by construction; watched RED in 12-03 family (c) |
| T-12-04 | Elevation of Privilege | exported `NewRootCmd` widens `internal/cli`'s API | low | accept | it returns `newRootCmd()` unchanged — same tree `Execute()` runs, no new call path in the shipped binary; `cmd/codegraph/main.go` asserted unchanged; `internal/` cannot be imported outside this module |
| T-12-05 | Tampering | the generator or drift target writes outside its output path / clobbers a working-tree edit | low | mitigate | `clidoc` writes only `-out`; `docs:cli:drift` writes only under a `mktemp -d` scratch removed on EXIT; Task 1 asserts `git status --porcelain` clean after a drift run |
| T-12-06 | Denial of Service (gate fatigue) | a date stamp or machine-dependent default makes `docs:cli:drift` permanently red, teaching developers to ignore it | medium | mitigate | `root.DisableAutoGenTag = true`; every flag default and prose string in `internal/cli` verified literal at plan time; Task 1 runs the gate twice and requires identical transcripts |
| T-12-07 | Repudiation | a vacuous guard — a walk over an incomplete tree (no completion family, no `--version`) or over nothing passes green | medium | mitigate | both walks call `InitDefaultCompletionCmd()` + `InitDefaultVersionFlag()`; floors ≥ 26 commands / ≥ 50 flags asserted; counts logged; accounting identity asserted; the two walks' tree construction is source-asserted in lockstep |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | none — no new dependency of any kind; `cobra/doc` is already required via `internal/cli/man.go`; `go.mod`/`go.sum` asserted unchanged |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift` — exit 0, `compared 1 generated file`, byte-identical across two runs
- `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run TestEveryRegisteredFlagIsAccountedFor ./internal/cli/` — PASS with `walked 36 commands …, 1 accepted via testdata/cli-reference-allowlist.txt`
- `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/cli/ ./internal/cli/archtest/ ./internal/upgrade/` — all `ok`
- `rg -o 'run: task docs:cli:drift' .github/workflows/ci.yml | wc -l` = 1, within a dozen lines after `run: task proto:drift`
- `git diff --quiet ca015c4b -- go.mod go.sum cmd/codegraph/main.go`
</verification>

<success_criteria>
- ROADMAP criterion 1 (DOCS-05): the committed reference is `cobra/doc` output for every non-hidden command and flag including `--editor-url`, produced by `task docs:cli`, and `task docs:cli:drift` fails when it is stale — demonstrated RED on the untracked file here and RED on a deleted line in 12-03
- ROADMAP criterion 2 (DOCS-06) up to its RED demonstration: the guard walks the live tree hidden-included, reports its counts, and fails on any unaccounted flag; the throwaway-hidden-flag RED is 12-03 family (a)
- The generator and the guard agree on one tree, and neither tests `cobra/doc`
</success_criteria>

## Artifacts this phase produces (complete list — Plan 01 lands the items marked ★; later plans land the rest)

- Go `internal/cli`: ★ `NewRootCmd()` exported wrapper in `root.go` (no rename, `Execute()` untouched); ★ `cli_reference_test.go` (`cliReferenceDocPath`, `cliReferenceAllowlistPath`, `cliReferenceMinCommands`, `cliReferenceMinFlags`, `cliReferenceTree`, `documentedByReference`, `inheritedFromAncestor`, `parseCLIReferenceAllowlist`, `docMentionsFlag`, `TestEveryRegisteredFlagIsAccountedFor`); ★ `testdata/cli-reference-allowlist.txt` (one entry: `codegraph man`)
- Go `tools/clidoc`: ★ `main.go` (`-out`, `documented`, `walk`, `linkHandler`, banner + H1 + intro line, `DisableAutoGenTag`, `InitDefaultCompletionCmd`, `InitDefaultVersionFlag`)
- Docs: ★ `docs/CLI-REFERENCE.md` (generated, 35 command sections); `README.md` one link to it in the Homebrew paragraph (12-02, D-06); `docs/RELEASE.md` brew-trust wording — narrow `--cask` grant, one framing sentence, tap-wide grant named not spelled, quoted error trimmed (12-02, D-11)
- Taskfile: ★ `docs:cli`, ★ `docs:cli:drift`
- CI: ★ `.github/workflows/ci.yml` step `CLI reference drift guard (DOCS-05)` after `proto:drift`
- Planning: `.planning/todos/completed/2026-08-10-brew-trust-…md` (12-02, via `gsd_run todo complete`); `12-MUTATION-LOG.md` families (a) throwaway hidden flag on `ui`, (b) one line deleted from the reference, (c) bogus allowlist entry (12-03, D-10); `12-SECURITY.md` (`threats_open: 0` expected) and `12-VALIDATION.md` map filled (12-03, D-13)

<output>
Create `.planning/phases/12-cli-reference-docs-tail/12-01-SUMMARY.md` when done — include the drift RED and two GREEN transcripts, the guard's two RED and one GREEN transcripts with the counts line, the D-04 audit outcome, the `--version` planner addition, and the two commit SHAs.
</output>
