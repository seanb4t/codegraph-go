---
phase: 02-phase-close-fragment-capability
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - gsd-capability-changie:README.md
  - gsd-capability-changie:LICENSE
  - gsd-capability-changie:skills/changie-fragments/SKILL.md
  - .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md
autonomous: true
requirements: [CAP-01, CAP-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`gsd-capability-changie:LICENSE` is byte-identical to codegraph-go's `LICENSE`: verbatim MIT text, no appended paragraph (D-10)"
    - "`gsd-capability-changie:README.md` covers what the capability does, both config keys, the per-clone install command with `#v0.1.0`, the per-machine Claude Code step and why it exists, the manual re-run `/gsd-changie-fragments <phase> [--pr <n>]`, every skip reason, and the trailer semantics (D-10, CAP-01)"
    - "Following SKILL.md exactly as written, against the fixture phase (one user-visible change written in jargon, one planning-only change), produces exactly one fragment: kind Features, a plain sentence mentioning `--json` with no WIDGET-, D-NN or plan-number token, and `PR: \"4242\"`. It is committed as `docs(01): add changelog fragments` with Changie-Summaries `01-01,01-02`. The planning-only SUMMARY yields no fragment (D-06, D-09)"
    - "Three guards in write-fragments.sh are each shown RED against a confirmed-applied mutation in a disposable worktree, reverted byte-cleanly, and GREEN again, with the transcripts in 02-CAPABILITY-LOG.md: the disabled-key skip, the explicit-pathspec staging and the trailer idempotency check (D-09, rule 84d1gfpywd)"
    - "After this plan the capability repository's HEAD still passes 29 of 29 legs, its working tree is clean, and it has exactly one worktree"
  artifacts:
    - path: "gsd-capability-changie:README.md"
      provides: "install, config, dispatch, manual re-run, skip reasons and trailer documentation"
      contains: "Changie-Summaries"
    - path: "gsd-capability-changie:LICENSE"
      provides: "verbatim MIT license"
      contains: "MIT License"
    - path: ".planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md"
      provides: "judgment rehearsal transcript and three mutation families"
      contains: "Judgment rehearsal"
  key_links:
    - from: "gsd-capability-changie:README.md"
      to: "gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh"
      via: "skip-reason table mirrors the script's reason ids"
      pattern: "no-user-visible-change"
    - from: "gsd-capability-changie:README.md"
      to: "gsd-core capability install"
      via: "per-clone install command pinned to the tag"
      pattern: "gsd-capability-changie.git#v0.1.0 --scope project"
---

<objective>
Finish the capability repository before it is published. Add the README and LICENSE D-10 requires. Prove the judgment half (D-06) by having the executor follow SKILL.md exactly as written against the fixture phase before the tag makes any wording change costly. Show three guards of the deterministic half RED against confirmed mutations (D-09).

Purpose: the tag cut in 02-03 is costly to move (D-10), so everything a user or the maintainer would find wrong in v0.1.0 must be found here. That covers docs, license, SKILL.md judgment behaviour, and guards that pass vacuously.

Output: `README.md` and `LICENSE` in the capability repository, possibly a SKILL.md wording fix, and two new sections in codegraph-go's `02-CAPABILITY-LOG.md`.

`$CAP` means `/Volumes/Code/github.com/seanb4t/gsd-capability-changie`.
</objective>

<execution_context>
@/Users/sean/.claude/gsd-core/workflows/execute-plan.md
@/Users/sean/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md
@.planning/phases/02-phase-close-fragment-capability/02-01-SUMMARY.md
@.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md
@.planning/phases/01-changie-baseline/01-MUTATION-LOG.md
@LICENSE
</context>

<tasks>

<task type="auto">
  <name>Task 1: README, verbatim LICENSE, and a pre-publication judgment rehearsal of SKILL.md against the fixture phase</name>
  <files>gsd-capability-changie:README.md, gsd-capability-changie:LICENSE, gsd-capability-changie:skills/changie-fragments/SKILL.md, .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md</files>
  <read_first>
    - gsd-capability-changie:skills/changie-fragments/SKILL.md (as committed by 02-01)
    - gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh, its header comment: the skip and error ids the README tables mirror
    - gsd-capability-changie:capability.json
    - gsd-capability-changie:test/fixtures/phase/01-01-SUMMARY.md and 01-02-SUMMARY.md
    - LICENSE (codegraph-go): the verbatim source
    - CONTRIBUTING.md lines 118-130: the prose voice the README follows (direct, technical)
    - .planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md D-06 and D-10
    - .planning/phases/02-phase-close-fragment-capability/02-01-PLAN.md `<interfaces>` block: the script and SKILL.md contract and the verified facts, including install-profiles.cjs:876,881 (only SKILL.md is materialized) and :733 (the global capability root)
  </read_first>
  <action>
1. LICENSE (D-10): `cp -f /Volumes/Code/github.com/seanb4t/codegraph-go/LICENSE "$CAP/LICENSE"`. Verbatim MIT only; an appended paragraph would degrade GitHub's license detection (project CLAUDE.md).

2. README.md (D-10), in CONTRIBUTING's direct voice, with these sections.
   - What it does: the phase-close flow; the judgment/deterministic split.
   - Requirements: gsd-core 1.14.x (`engines.gsd` `^1.14.0`), git, gh (optional; without it the PR lookup skips), and changie or the configured command.
   - Install, per clone: `gsd-tools capability install git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope project`, then set the keys with `gsd-tools query config-set`, never by hand-editing config.json.
   - Making the skill dispatchable in Claude Code, per machine. gsd-core materializes a third-party skill for Claude Code only from the GLOBAL capability root (`${GSD_HOME:-$HOME}/.gsd/capabilities/<id>/`), and only its SKILL.md. So a project-scope install alone leaves `/gsd-changie-fragments` unresolvable.
     - The per-machine step is `gsd-tools capability install <same spec> --scope global`, then `gsd-tools capability set changie --enable --runtime claude --scope global`.
     - Side effect: the step becomes active in every GSD project on that machine, because the key defaults to true. Projects without `.changie.yaml` get the no-changie-config skip. Set `workflow.changie_fragments false` wherever it is unwanted.
     - Tell the reader codegraph-go's CONTRIBUTING records the procedure verified on the maintainer's machine.
   - Configuration: a table of `workflow.changie_fragments` and `workflow.changie_command` with type, default and meaning. Include the whitespace-split rule for the command (no quoting) and the example `task changie --`.
   - Manual re-run: `/gsd-changie-fragments <phase> [--pr <n>] [--repo <dir>]`.
   - Skip reasons: a table of all 12 script skip ids plus the SKILL.md-level `script-missing`, each with its cause.
   - Errors: a table of the 6 error ids with their exit codes.
   - Idempotency and trailers: `Changie-Phase` and `Changie-Summaries`; the base..HEAD range and how the base is chosen; gap-closure SUMMARYs; no commit when there is no user-visible change.
   - Commit convention: subject shape, explicit pathspec only, never publishes to a remote, never a CI-skip marker.
   - Development: `bash test/run.sh`, and the env overrides `CHANGIE_BIN`, `GSD_TOOLS` and `CHANGIE_FRAGMENTS_GH`.
   - CI: none. This is the Claude's-discretion call under D-10. The suite needs a host gsd-core install and a changie build, and the committed test/run.sh is the proof.
   - License: MIT.

3. Judgment rehearsal (D-06, D-09), pre-publication. The real Skill-tool dispatch comes in 02-05, after materialization.
   - In `R=$(mktemp -d)`, build the same fixture project test/run.sh builds:
     - git init -b main with local identity and `commit.gpgsign false`
     - `.changie.yaml` from the fixture, and `.changes/unreleased/.gitkeep`
     - `gsd-tools config-new-project`, and a minimal ROADMAP with `### Phase 1: Fixture`
     - `.planning/phases/01-fixture/` with the two fixture SUMMARYs
     - a baseline commit, then branch `gsd/fixture-milestone`
   - Install `$CAP` at project scope. Set `workflow.changie_command` with config-set to the absolute path of codegraph-go's pinned changie, from `GOWORK=off go tool -modfile=go.tool-changie.mod -n changie` run in codegraph-go.
   - Read `$CAP/skills/changie-fragments/SKILL.md` and follow it exactly as written, with the arguments `1 --pr 4242 --repo "$R"`. You are the model half here. Do not use knowledge the SKILL.md does not give.
   - Then assert and record each output:
     - `git -C "$R" log -1 --format=%s` is `docs(01): add changelog fragments`.
     - The Changie-Summaries trailer is `01-01,01-02`.
     - `git -C "$R" show --name-only --format= HEAD` lists exactly one file.
     - That file contains `kind: Features` and `PR: "4242"`.
     - Its `body:` line contains `--json`.
     - `rg '^body:' <file> | rg -o 'WIDGET-|\bD-[0-9]+|\b0[0-9]-0[0-9]\b' | wc -l` prints 0.
   - If any assertion fails, SKILL.md's wording is the defect. Fix it in `$CAP`, re-run test/run.sh to 29 of 29, repeat the rehearsal in a fresh scratch project, and commit the fix as `fix(02-02): <what the wording now says>`.

4. Commit README.md and LICENSE in `$CAP` as `docs(02-02): add README and MIT license`. Run shellcheck and the suite once more on the final HEAD.

5. Append a `## Judgment rehearsal (SKILL.md followed by the executor, pre-publication)` section to 02-CAPABILITY-LOG.md. It holds: the SKILL.md commit it ran against; the `--list` output; the entries file content; the script's last line; the fragment file content; the trailer read; and every assertion with its result. Commit in codegraph-go as `docs(02-02): record the judgment rehearsal`.
  </action>
  <verify>
    <automated>cmp /Volumes/Code/github.com/seanb4t/codegraph-go/LICENSE /Volumes/Code/github.com/seanb4t/gsd-capability-changie/LICENSE</automated>
    <fails_when>non-zero exit, or cmp prints "differ" or "EOF on"</fails_when>
    <automated>for t in 'gsd-capability-changie.git#v0.1.0 --scope project' '--scope global' 'workflow.changie_fragments' 'workflow.changie_command' '/gsd-changie-fragments' 'Changie-Phase' 'Changie-Summaries' 'script-missing' 'no-user-visible-change' 'changie-unavailable' 'already-recorded'; do test "$(rg -F -o -- "$t" /Volumes/Code/github.com/seanb4t/gsd-capability-changie/README.md | wc -l | tr -d ' ')" -ge 1 || { echo "README missing: $t"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or a line starting `README missing:`</fails_when>
    <automated>out=$(CHANGIE_BIN="$(cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOWORK=off go tool -modfile=go.tool-changie.mod -n changie)" /bin/bash /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/run.sh) && printf '%s\n' "$out" | tail -n 1 | rg -x -F 'run.sh: 29 of 29 legs passed against a scratch project'</automated>
    <fails_when>non-zero exit, or a `::error::run.sh:` line, or the last line is not exactly `run.sh: 29 of 29 legs passed against a scratch project`</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -F -o 'Judgment rehearsal' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 1.
    - `rg -F -o 'Changie-Summaries: 01-01,01-02' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 1, or the log records the trailer read as `01-01,01-02` on its own line.
    - `rg -F -o 'kind: Features' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 1.
    - `l=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie log --format=%s) && test "$(printf '%s\n' "$l" | rg -x -F 'docs(02-02): add README and MIT license' | wc -l | tr -d ' ')" = 1` exits 0.
    - `test -z "$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie status --porcelain)"` exits 0.
    - `rg -F -o '.claude/' /Volumes/Code/github.com/seanb4t/gsd-capability-changie/skills/changie-fragments/SKILL.md | wc -l` prints 0, and still does after any rehearsal fix.
  </acceptance_criteria>
  <done>The capability repository has a verbatim MIT LICENSE and a README covering every D-10 item. Following SKILL.md as written turns the fixture phase into exactly one plain, jargon-free Features fragment committed with the right trailers. The evidence is in codegraph-go.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation evidence: three write-fragments.sh guards shown RED in a disposable worktree, reverted byte-cleanly</name>
  <files>.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md</files>
  <read_first>
    - .planning/phases/01-changie-baseline/01-MUTATION-LOG.md lines 1-120: the convention (pre-mutation cleanliness gate, applied confirmation, verbatim RED, byte-clean revert, GREEN re-run, disposable detached worktree)
    - gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh: locate the disabled-key branch, the staging call and the covered-set computation
    - gsd-capability-changie:test/run.sh: the [skip:disabled], [write], [commit-scope] and [idempotent] legs
    - .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md
  </read_first>
  <action>
Per D-09, a guard is not trusted until it is shown RED. Work only in a disposable detached worktree; never mutate `$CAP`'s own tree.

1. Set up:
   - Record `git -C "$CAP" worktree list` (one entry) and `git -C "$CAP" rev-parse HEAD`.
   - Run `S=$(mktemp -d)` and `git -C "$CAP" worktree add --detach "$S/wt" HEAD`.
   - Positive control: run `/bin/bash "$S/wt/test/run.sh"` with CHANGIE_BIN set to codegraph-go's pinned changie. It must print 29 of 29. test/run.sh resolves CAP_ROOT from its own location, so it installs the worktree copy.

2. Run three families. Each follows 01-MUTATION-LOG's sequence:
   1. The pre-mutation gate: `git -C "$S/wt" diff --quiet` exits 0.
   2. The mutation, applied to `skills/changie-fragments/scripts/write-fragments.sh` only.
   3. The applied confirmation: `git -C "$S/wt" diff --numstat` shows exactly that one file.
   4. The RED run: non-zero exit, with the transcript naming the leg.
   5. The byte-clean revert: `git -C "$S/wt" checkout -- skills/changie-fragments/scripts/write-fragments.sh`, then `git -C "$S/wt" diff --quiet` exits 0.
   6. The GREEN re-run: 29 of 29.

   The families:
   - (a) Skip guard. Neutralize the branch that returns skip disabled when `workflow.changie_fragments` is false, so the run continues. Expected RED leg: [skip:disabled].
   - (b) Write guard. Broaden the staging call from the explicit list of written fragment paths to the whole working tree. Expected RED: the first leg that asserts HEAD's file set, [write] or [commit-scope]. Record which one fired and why.
   - (c) Idempotency guard. Force the covered set read from the Changie-Summaries trailers to be empty. Expected RED leg: [idempotent].

3. Tear down: `git -C "$CAP" worktree remove "$S/wt"`, then confirm `git -C "$CAP" worktree list` is back to one entry, `git -C "$CAP" status --porcelain` is empty, and HEAD is unchanged.

4. Append `## Mutation families` to 02-CAPABILITY-LOG.md in 01-MUTATION-LOG's per-family layout: what is tested and why, pre-mutation gate, mutation applied (described, with the `git diff` excerpt), applied confirmation, verbatim RED transcript, revert, GREEN. Include the setup and teardown transcripts. Commit in codegraph-go as `docs(02-02): record capability mutation evidence`. Never put a CI-skip marker in a commit message (rule f18zrdsgx5).
  </action>
  <verify>
    <automated>for l in '::error::run.sh: [skip:disabled]' '::error::run.sh: [idempotent]'; do test "$(rg -F -o -- "$l" /Volumes/Code/github.com/seanb4t/codegraph-go/.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l | tr -d ' ')" -ge 1 || { echo "log missing: $l"; exit 1; }; done; test "$(rg -o '::error::run\.sh: \[(write|commit-scope)\]' /Volumes/Code/github.com/seanb4t/codegraph-go/.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>non-zero exit, or a line starting `log missing:`, or no `::error::run.sh: [write]` / `[commit-scope]` RED transcript in the log</fails_when>
    <automated>w=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie worktree list) && test "$(printf '%s\n' "$w" | wc -l | tr -d ' ')" = 1 && s=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie status --porcelain) && test -z "$s"</automated>
    <fails_when>non-zero exit: a leftover worktree, or the capability repository's working tree is dirty after the mutations</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -F -o 'Mutation families' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 1.
    - `rg -F -o 'run.sh: 29 of 29 legs passed' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 5: 02-01's final run, the positive control, and the three post-revert GREEN runs.
    - `rg -F -o 'diff --numstat' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 3 (one applied confirmation per family).
    - `git log --format=%s -1 -- .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md` prints `docs(02-02): record capability mutation evidence`.
  </acceptance_criteria>
  <done>The disabled-key skip, the explicit-pathspec staging and the trailer idempotency check each go RED on a named leg against a confirmed mutation and return to 29 of 29 after a byte-clean revert. No worktree or edit is left behind.</done>
</task>

</tasks>

## Phase source coverage audit

The whole-phase audit table is in 02-01-PLAN.md, and every row is COVERED. This plan carries CAP-01 (README, LICENSE), CAP-03 (the judgment half rehearsal) and D-09 (mutation RED) and D-10 (README contents, LICENSE, and the no-CI discretion call).

## Artifacts this phase produces

This list is identical in all five phase plans. The "Plan" column says which plan produces each item.

| Artifact | Kind | Plan |
|---|---|---|
| Repository `seanb4t/gsd-capability-changie`, checkout `/Volumes/Code/github.com/seanb4t/gsd-capability-changie` | new repository (local, then private remote) | 02-01, 02-03 |
| `gsd-capability-changie:capability.json`: id `changie`, role `feature`, version `0.1.0`, `engines.gsd` `^1.14.0`, `runtimeCompat`, one `verify:post` step | new file | 02-01 |
| Federated config keys `workflow.changie_fragments` (boolean, default true) and `workflow.changie_command` (string, default `changie`) | config keys | 02-01 (declared), 02-04 (set in codegraph-go) |
| `gsd-capability-changie:skills/changie-fragments/SKILL.md`: skill stem `changie-fragments`, frontmatter `name: gsd-changie-fragments`, command `/gsd-changie-fragments <phase> [--pr <n>] [--repo <dir>]` | new file | 02-01 |
| `gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh`: flags `--phase`, `--list`, `--write`, `--summaries`, `--entries`, `--pr`, `--repo`, `--base`; env `GSD_TOOLS`, `CHANGIE_FRAGMENTS_GH`; output prefix `changie-fragments:`; 12 skip ids; error ids `usage`, `bad-pr`, `malformed-entry`, `undeclared-kind`, `changie-failed`, `commit-failed`; lock `<git-dir>/changie-fragments.lock` | new file | 02-01 |
| SKILL.md-level skip id `script-missing` | skip reason | 02-01 |
| Git trailers `Changie-Phase` and `Changie-Summaries`; fragment commit subject `docs(<padded-phase>): add changelog fragments` | commit convention | 02-01 |
| `gsd-capability-changie:test/run.sh`: 29 labelled legs; env `CHANGIE_BIN`, `GSD_TOOLS` | new file | 02-01 |
| `gsd-capability-changie:test/fixtures/changie.yaml`, `test/fixtures/phase/01-01-SUMMARY.md`, `test/fixtures/phase/01-02-SUMMARY.md`, `test/fixtures/bin/gh` (env `STUB_GH_MODE`, `STUB_GH_PR`), `test/fixtures/bin/fake-task` (env `CHANGIE_BIN`, `FAKE_TASK_FAIL_ON_NEW`, `FAKE_TASK_COUNTER`) | new files | 02-01 |
| `gsd-capability-changie:README.md`, `gsd-capability-changie:LICENSE` | new files | 02-02 |
| Annotated tag `v0.1.0` in `seanb4t/gsd-capability-changie` | release tag | 02-03 |
| Draft PR `gsd/v0.15.0-milestone` → `main` in codegraph-go | GitHub PR | 02-03 |
| STATE.md Blockers/Concerns entry on the /gsd-ship existing-PR hazard (via `gsd-tools state add-blocker`) | planning record | 02-03 |
| `.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md` | new planning artifact | 02-01 (extended in 02-02) |
| `.gitignore` line `.gsd-capabilities.json` | edited file | 02-04 |
| `.planning/config.json` values `workflow.changie_fragments: true` and `workflow.changie_command: "task changie --"` (via config-set) | tool-owned value fill | 02-04 |
| codegraph-go install output `.gsd/capabilities/changie/` and `.gsd-capabilities.json` (ignored, untracked) | install output | 02-04 |
| Maintainer-machine state: `~/.gsd/capabilities/changie/`, `~/.gsd-capabilities.json`, and `${CLAUDE_CONFIG_DIR:-~/.claude}/skills/gsd-changie-fragments/` holding `SKILL.md` and `.gsd-capability-skill` (depends on the 02-04 decision) | global install and materialization | 02-04 |
| `CONTRIBUTING.md` subsection `### Changelog fragments at phase close` | edited doc | 02-05 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → every agent that dispatches it | Installed verbatim and not content-scanned (gsd-core's install disclosure); its wording steers the model at every phase close |
| mutation worktree → capability repository | Deliberately broken code must never reach the tagged tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | SKILL.md instructions (agent-instruction surface) | medium | mitigate | A pre-publication rehearsal follows SKILL.md as written and asserts its concrete outcome: one fragment, the right kind, no jargon, the right trailers. A wording defect is fixed before the tag exists, and the file keeps its no-`.claude/`-path rule. |
| T-02-09 | Tampering | mutation demonstrations | low | mitigate | Mutations happen only in a detached `git worktree` under `mktemp -d`. Each is reverted with `git checkout --` and confirmed with `git diff --quiet`. Teardown asserts one worktree, a clean `$CAP` status and an unchanged HEAD. |
| T-02-10 | Information Disclosure | README and 02-CAPABILITY-LOG.md | low | accept | They contain command transcripts from throw-away scratch projects and a fixture PR number (4242), with no credentials. The capability repository is private (02-03). |
</threat_model>

<verification>
- `cmp` of the two LICENSE files succeeds, and the README token loop prints nothing.
- The suite passes 29 of 29 on the capability repository's final HEAD.
- 02-CAPABILITY-LOG.md holds the judgment rehearsal and three mutation families, each with its RED and GREEN transcripts.
- The capability repository has one worktree, a clean status, and no remote yet.
</verification>

<success_criteria>
- CAP-01's README and LICENSE items are satisfied.
- CAP-03's judgment half behaves per D-06 when SKILL.md is followed as written: one user-visible change becomes one plain fragment, and a planning-only change becomes none.
- Three deterministic guards are proven non-vacuous by recorded mutation (D-09, rule 84d1gfpywd).
</success_criteria>

<output>
Create `.planning/phases/02-phase-close-fragment-capability/02-02-SUMMARY.md` when done. It must include:
- the README section list;
- the rehearsal outcome, including any SKILL.md fix and its commit;
- the three mutation families' RED leg names;
- the capability repository's final HEAD SHA, which 02-03 tags.
</output>
