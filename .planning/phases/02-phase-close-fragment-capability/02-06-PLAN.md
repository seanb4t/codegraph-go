---
phase: 02-phase-close-fragment-capability
plan: 06
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - internal/upgrade/changie_shape_test.go
  - .changie.yaml
  - Taskfile.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md
  - .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md
  - gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh
  - gsd-capability-changie:skills/changie-fragments/SKILL.md
  - gsd-capability-changie:test/run.sh
  - gsd-capability-changie:test/fixtures/changie.yaml
  - gsd-capability-changie:test/fixtures/bin/gh
  - gsd-capability-changie:README.md
  - gsd-capability-changie:capability.json
autonomous: true
requirements: [CAP-03, CAP-04, CHG-01, CHG-04]

estimate:
  tokens: 180000
  raw_tokens: 180000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "codegraph-go's `.changie.yaml` declares the `PR` custom field as `type: int`, `minInt: 1` and `optional: true`, and its header comment says why (D-13). TestChangieConfigShape requires the custom key set to be exactly {key, minInt, optional, type} with optional `true`. That test change landed as a test(02-06) commit before the feat(02-06) config change, and its `--- FAIL: TestChangieConfigShape` transcript is pasted (CHG-01, D-13, rule x1cjy9vyhq)"
    - "`task check:changie` ends `check:changie: 12 of 12 checks passed against a scratch copy (source tree byte-unchanged)`. Leg 3 still byte-reproduces CHANGELOG.md. Leg 5 proves a fragment without PR is accepted and carries no PR field. Legs 6-8 still refuse an undeclared kind, `PR=0` and `PR=abc`. Leg 12 proves `changie batch auto --dry-run` renders the no-PR fragment as a plain `- <body>` line and a PR fragment with its link, and writes nothing (CHG-03 kept, CHG-04 amended, D-13)"
    - "Each re-pointed codegraph-go guard goes RED against a confirmed-applied, byte-cleanly-reverted mutation in a detached scratch worktree, then GREEN again. Removing the optional key fails leg 5 and the Go test. Removing the changeFormat PR conditional fails leg 12. Removing minInt fails leg 7. 02-MUTATION-LOG.md records all three families (rule 84d1gfpywd)"
    - "write-fragments.sh v0.1.1 resolves the PR in three steps. An explicit `--pr` must be a positive integer, and `--pr 0` still exits 2 with `error: bad-pr`. Otherwise gh's open-PR lookup for the current branch is used, but only when it prints a positive integer. Otherwise there is no PR. With no PR, the script prints exactly one `changie-fragments: note: <id>: <detail>` line (ids gh-missing, pr-lookup-failed, no-open-pr, pr-lookup-invalid), `--list` prints `changie-fragments: pr none`, and `--write` runs `changie new` without `-m PR=`. No note is a skip, and each body still reaches changie as one argv element (CAP-03, D-13)"
    - "With no PR, the committed fragment has no `PR:` line and no `custom:` line, the commit carries the usual Changie-Phase and Changie-Summaries trailers, and `changie batch auto --dry-run` renders it as `- <body>` with no link. A host whose `.changie.yaml` still requires PR refuses the no-PR write with `error: changie-failed`, exit 1, and nothing written (D-13)"
    - "Tokens in gh's stderr (`ghp_`, `gho_`, `ghu_`, `ghs_`, `ghr_` or `github_pat_` prefixes) reach a note only as `[REDACTED]`. gh's stdout is never echoed. The only gh output that can reach a fragment is a validated positive integer (threat T-02-22, T-02-23)"
    - "gsd-capability-changie's test/run.sh passes 32 of 32 labelled legs. Its fixture `test/fixtures/changie.yaml` is byte-identical to codegraph-go's `.changie.yaml`. `git -C $CAP log --reverse --format=%s v0.1.0..v0.1.1` shows test(02-06), feat(02-06), test(02-06), feat(02-06) and docs(02-06), in that order"
    - "Annotated tag `v0.1.1` is on `$CAP`'s `main` tip, and both `main` and the tag are pushed over HTTPS to the private repository. Remote `v0.1.0` is unchanged: tag object `60d4967a…` still peels to `5700e60f…`. Visibility is still PRIVATE. A clean HTTPS clone at `v0.1.1` passes 32 of 32. The README installs from `https://github.com/seanb4t/gsd-capability-changie.git#v0.1.1` only (D-13 part 3, D-11 amendment)"
    - "codegraph-go's project-scope install and the maintainer's global install are both at version 0.1.1 from the `#v0.1.1` HTTPS spec, through gsd-core's own `capability install` (a reinstall over an existing entry is gsd-core's upgrade swap). The project bundle's capability.json and the materialized `gsd-changie-fragments/SKILL.md` are each byte-identical to the tag's copies. The marker names `changie`. The global skills listing gained and lost nothing, and no other skill's file content changed. `loop render-hooks verify:post` in codegraph-go still shows exactly one changie step, and the installs changed no tracked codegraph-go file (CAP-04, D-13 part 4, D-14)"
    - "REQUIREMENTS.md CHG-01 names an optional `PR` field. CHG-04 says a fragment without `PR` is accepted and `PR=0` is still refused by `minInt: 1`. ROADMAP.md Phase 1 SC2/SC4 and the first Phase 2 Notes sentence say the same. The edits add no heading (CHG-01, CHG-04, D-13)"
  prohibitions:
    - statement: "MUST NOT re-point, delete or re-push tag v0.1.0, MUST NOT force-push, and MUST NOT change the capability repository's visibility (D-13 part 3)"
      requirement_id: CAP-04
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT write a placeholder PR number, and MUST NOT let raw gh output (stdout or unredacted stderr) reach a fragment or a note. Only a validated positive integer reaches `-m PR=` (D-13, T-02-22, T-02-23)"
      requirement_id: CAP-03
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT hand-copy or hand-edit anything under the global skills directory, `~/.gsd` or `.planning/config.json`, and MUST NOT set GSD_HOME to redirect gsd-core. Installs and materialization go only through gsd-core verbs (planning-artifacts rule, T-02-16)"
      requirement_id: CAP-04
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: ".changie.yaml"
      provides: "PR custom field optional, type int, minInt 1"
      contains: "optional: true"
    - path: "internal/upgrade/changie_shape_test.go"
      provides: "TestChangieConfigShape pinned to the optional PR key set"
      contains: "\"optional\""
    - path: "Taskfile.yml"
      provides: "check:changie with leg 5 re-pointed and leg 12 (no-PR render)"
      contains: "12 of 12 checks passed"
    - path: ".planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md"
      provides: "RED-first transcripts and three mutation families for the re-pointed codegraph-go guards"
      contains: "## Family (c)"
    - path: "gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh"
      provides: "v0.1.1 PR resolution: explicit, open PR, else none with a note"
      contains: "note: pr-lookup-invalid"
    - path: "gsd-capability-changie:test/run.sh"
      provides: "32 labelled legs"
      contains: "EXPECTED_LEGS=32"
    - path: "gsd-capability-changie:README.md"
      provides: "HTTPS install spec at v0.1.1, PR resolution, notes table"
      contains: "gsd-capability-changie.git#v0.1.1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CHG-01 and CHG-04 amended in place"
      contains: "PR=0` is still refused by `minInt: 1`"
  key_links:
    - from: ".changie.yaml custom PR entry"
      to: "internal/upgrade/changie_shape_test.go TestChangieConfigShape"
      via: "exact custom key set {key, minInt, optional, type} and optional == true"
      pattern: "\"optional\""
    - from: ".changie.yaml changeFormat `{{if .Custom.PR}}`"
      to: "Taskfile.yml check:changie leg 12"
      via: "changie batch auto --dry-run renders the no-PR fragment without a link"
      pattern: "batch auto --dry-run"
    - from: "gsd-capability-changie:test/fixtures/changie.yaml"
      to: ".changie.yaml"
      via: "byte copy, proven with cmp"
      pattern: "optional: true"
    - from: "gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh PR resolution"
      to: "changie new argv"
      via: "-m PR=<n> appended only when a validated PR exists"
      pattern: "pr none"
    - from: "codegraph-go .gsd/capabilities/changie/ and ~/.gsd/capabilities/changie/"
      to: "gsd-capability-changie tag v0.1.1"
      via: "gsd-tools capability install over the existing entry (upgrade swap)"
      pattern: "gsd-capability-changie.git#v0.1.1"
---

<objective>
Implement maintainer decision D-13: the changie `PR` field becomes optional. Its four parts, in order, are this plan's contract:
1. codegraph-go config and guards.
2. Capability `v0.1.1`.
3. Publish.
4. Upgrade both installs.

D-14's global install is part 4's second target.

Purpose: under the milestone-PR workflow, every fragment in a milestone would carry the same PR number. A required PR therefore forced a draft PR to be opened at each milestone start, and that PR then collides with `/gsd-ship`'s create step. After this plan, a phase close with no open PR still writes its fragments, without a number. Nothing prompts, nothing blocks, and nothing uses a placeholder.

The plan leads with a tracer, which wires one path through every layer:
- codegraph-go's `.changie.yaml` plus its RED-first Go guard;
- the capability script's no-open-PR path;
- changie;
- a render with no link.

Two expansion tasks follow:
- Task 2 makes every remaining guard RED-proven in both repositories.
- Task 3 documents, publishes, upgrades and amends the requirement text.

Names used below:
- `$CG` means `/Volumes/Code/github.com/seanb4t/codegraph-go`.
- `$CAP` means `/Volumes/Code/github.com/seanb4t/gsd-capability-changie`. Files in frontmatter carry the `gsd-capability-changie:` prefix, and capability-repo evidence is pasted into this phase's `02-CAPABILITY-LOG.md` (D-09).
- `$CHB` means `$(cd $CG && GOWORK=off go tool -modfile=go.tool-changie.mod -n changie)`, codegraph-go's pinned changie build.
- `$SPEC` means `https://github.com/seanb4t/gsd-capability-changie.git#v0.1.1`.
- `$SKILLS` means `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills`.
- `$GSD_LIB` means `$HOME/.claude/gsd-core/bin/lib` (read-only).

Output:
- In codegraph-go: the config and test change, check:changie's twelve legs, `02-MUTATION-LOG.md`, the CHG-01/CHG-04 and ROADMAP value edits, and a STATE concern.
- In `$CAP`: v0.1.1, tagged and pushed.
- On the maintainer's machine: both installs at 0.1.1.
</objective>

<execution_context>
@/Users/sean/.claude/gsd-core/workflows/execute-plan.md
@/Users/sean/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md
@.planning/phases/02-phase-close-fragment-capability/02-01-PLAN.md
@.planning/phases/02-phase-close-fragment-capability/02-03-SUMMARY.md
@.planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md
@.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md
@.planning/phases/01-changie-baseline/01-MUTATION-LOG.md
@.changie.yaml
@internal/upgrade/changie_shape_test.go

<interfaces>
v0.1.1 contract delta for write-fragments.sh. Everything not listed here is 02-01's `<interfaces>` contract and does not change.

```text
PR resolution (runs where it runs today: after entries validation on --write,
after already-recorded on --list). It never skips and never exits:
  --pr <n> given     -> must match ^[1-9][0-9]*$, else `error: bad-pr` exit 2 at
                        argument parsing (unchanged); gh is not consulted
  else gh = ${CHANGIE_FRAGMENTS_GH:-gh}:
    not resolvable by command -v      -> note gh-missing: names the executable
    lookup exits non-zero             -> note pr-lookup-failed: "gh exited <s>: <first stderr
                                         line, tokens redacted>"
    output empty or null              -> note no-open-pr: names the branch (or detached HEAD)
    output not ^[1-9][0-9]*$          -> note pr-lookup-invalid: fixed text, gh output NOT echoed
    output a positive integer         -> PR=<n>, no note
Note line (stdout, never exits):  changie-fragments: note: <note-id>: <detail>
  At most one per run, printed at resolution time: before the `phase` line on
  --list, and before the `wrote` line on --write.
--list pr line:   changie-fragments: pr <n>   |   changie-fragments: pr none
--write argv:     new -k <Kind> -b <Body>, plus -m PR=<n> only when PR is set
Redaction of gh's first stderr line, the only gh text that reaches a note:
  sed -E 's/(gh[pousr]_[A-Za-z0-9_]+|github_pat_[A-Za-z0-9_]+)/[REDACTED]/g'
Skip ids (9):  gsd-tools-missing disabled changie-unavailable no-changie-config
               phase-not-found no-summaries locked already-recorded no-user-visible-change
Note ids (4):  gh-missing pr-lookup-failed no-open-pr pr-lookup-invalid
Error ids:     unchanged. A host whose .changie.yaml still requires PR refuses a
               no-PR `changie new`: error: changie-failed, exit 1, own fragments
               rolled back, lock released, no commit.
```

check:changie legs after this plan: 1 latest, 2 next auto empty, 3 merge byte reproduction, 4 valid new, 5 no-PR accepted (re-pointed), 6 undeclared kind refused, 7 PR=0 refused, 8 PR=abc refused, 9 collision (count to 4), 10 patch successor, 11 minor successor, 12 no-PR render (new).

test/run.sh legs after this plan, in order (32):
- ordering, install, manifest, dispatch-default, dispatch-false, dispatch-true
- skip:gsd-tools-missing, skip:disabled, skip:changie-unavailable, skip:changie-empty, skip:no-changie-config, skip:phase-not-found, skip:no-summaries
- note:gh-missing, note:pr-lookup-failed, note:no-open-pr, note:pr-lookup-invalid
- pr-override, bad-pr, write, idempotent, gap-closure
- skip:no-user-visible-change, undeclared-kind, malformed-entry, rollback, skip:locked
- argv-literal, commit-scope, write-no-pr, no-pr-required-host, no-side-effects

Verified facts this plan relies on. Each was checked 2026-09-26 on this machine; source lines refer to files under `$GSD_LIB`.
- Upgrading to a new tag:
  - `capability update <id>` calls `upgradeCapability(entry.source, …)` with the ledger's recorded spec (capability-command-router.cjs:366-381). The ledgers record `https://…#v0.1.0`, so `update` would re-fetch v0.1.0. It cannot move to a new tag.
  - `capability install <new spec>` over an existing ledger entry and bundle is gsd-core's upgrade swap (capability-lifecycle.cjs:907-989). It sets `isUpgradeLike`, records a `_pending.kind: 'upgrade'` intent, promotes with a backup, and rewrites the ledger entry with the new source and version. It returns `status: "installed"`.
- Git sources:
  - resolveGit runs `git clone --depth 1 -- <url> <dir>`, then `git -C <dir> checkout <ref> --` (capability-source.cjs:846-857). A depth-1 clone of the default branch fetches only the tags at its tip.
  - A scratch probe confirmed it: after a second tagged commit, `checkout v1` failed with `fatal: invalid reference: v1`, and `checkout v2` succeeded.
  - Consequence: once `main` moves past `v0.1.0`, the `#v0.1.0` spec no longer installs. Only a tag at `main`'s tip installs.
- Materialization:
  - `capability set <id> --enable --runtime claude --scope global` always runs applySurface when `--runtime` is given (capability-writer.cjs:259-297).
  - applySurface re-stages SKILL.md from `${GSD_HOME:-$HOME}/.gsd/capabilities/<id>/skills/<stem>/SKILL.md` (install-profiles.cjs:733-745) and writes it, with the `.gsd-capability-skill` marker, into the skills directory (install-profiles.cjs:876-881).
- changie v1.26.0 probes, run with `$CHB` against a copy of `.changie.yaml` that had `optional: true` added to PR:
  - `CI=true changie new -k Fixes -b "probe without PR"` exits 0 and writes `kind:`, `body:` and `time:` with no `custom:` block.
  - `-m PR=0` is still refused with `input below minimum: 0 < 1`.
  - `changie batch auto --dry-run` exits 0 and writes nothing. It prints `- probe without PR` and `- probe with PR ([#7](https://github.com/seanb4t/codegraph-go/pull/7))`. It works with no seeded version files, and with a fragment that has no `time:`.
  - `changie merge --dry-run` is unaffected by the PR change.
- 02-04 left the installs in this state. Both are at 0.1.0 from `https://github.com/seanb4t/gsd-capability-changie.git#v0.1.0`: project scope in codegraph-go and global scope on this machine. The global install is materialized to `$SKILLS/gsd-changie-fragments/`. Project render-hooks shows 1 changie step. Draft PR #88 is open.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer, RED first. codegraph-go's config accepts a fragment without PR, and the capability's no-open-PR path writes one that renders without a link</name>
  <precondition>02-04 is complete: `.planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md` exists, and `git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie rev-parse HEAD` equals `git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie rev-parse 'v0.1.0^{commit}'` with a clean `status --porcelain`</precondition>
  <reversibility rating="reversible">D-13: to re-require PR, remove one config line and revert the guards. The tracer publishes nothing.</reversibility>
  <files>internal/upgrade/changie_shape_test.go, .changie.yaml, Taskfile.yml, .planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md, gsd-capability-changie:test/fixtures/changie.yaml, gsd-capability-changie:test/run.sh, gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh, .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md</files>
  <read_first>
    - .planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md: D-13 (the contract), D-03 (what D-13 supersedes), D-05/D-07/D-08 (script rules that do not change)
    - .changie.yaml (whole file): the header comment at lines 1-24 and the custom block at lines 45-48
    - internal/upgrade/changie_shape_test.go lines 1-10 (file header) and 252-273 (the custom[0] assertions)
    - Taskfile.yml lines 694-796: check:changie legs 5-9, the lines whose counts change
    - gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh lines 1-75 (header, helpers, skip-id comment block) and 378-452 (PR resolution, list output, write loop)
    - gsd-capability-changie:test/run.sh lines 116-147 (helpers), 389-397 (the no-open-pr leg), 436-514 ([write]), 693-773 (the tail where the new leg goes)
    - gsd-capability-changie:test/fixtures/bin/gh (stub modes)
    - .planning/phases/01-changie-baseline/01-MUTATION-LOG.md lines 1-24: the header and convention that 02-MUTATION-LOG.md copies
  </read_first>
  <behavior>
    - [Go] TestChangieConfigShape: custom[0] key set is exactly [key minInt optional type]; optional is `true`, type is `int`, minInt is `1`; every other assertion is unchanged
    - [check:changie 5/11] with no `-m PR=`, `changie new` exits 0 and the fragment count becomes 2. The one file whose body is the probe sentence has `kind: Fixes`, no `PR:` line and no `custom:` line. Legs 6-8 then assert the count is still 2, and leg 9 asserts it is 4
    - [note:no-open-pr] With STUB_GH_MODE=none, `--list` exits 0 and prints no `skip:` line. It prints `changie-fragments: note: no-open-pr:` with a detail naming the branch, the line `changie-fragments: pr none`, and both `pending 01-01` and `pending 01-02`
    - [write-no-pr] 01-04-SUMMARY.md is added and committed. `--write --summaries 01-04` with STUB_GH_MODE=none and the single entry `Features<TAB>Added a --quiet flag to widget list.`:
      - exits 0; the output holds the no-open-pr note, and its last line starts with `changie-fragments: wrote 1 fragment(s) in `
      - HEAD's file set is exactly the one new fragment, which has `kind: Features`, the body, no `PR:` line and no `custom:` line
      - trailers: Changie-Phase `1` and Changie-Summaries `01-04`
      - `changie batch auto --dry-run` in the fixture project exits 0. It prints the exact line `- Added a --quiet flag to widget list.` and the exact line `- First user-visible feature ([#4242](https://github.com/seanb4t/codegraph-go/pull/4242))`, and afterwards the unreleased fragment count and HEAD are unchanged
  </behavior>
  <action>
Two repositories. Agent commits in either may use `git -c commit.gpgsign=false` (rule xmz3xknbj0). They carry the session's required Co-Authored-By byline and never contain a CI-skip marker (rule f18zrdsgx5).

codegraph-go, RED first (rule x1cjy9vyhq, D-13 part 1):
1. In internal/upgrade/changie_shape_test.go, change TestChangieConfigShape's custom[0] check:
   - wantCustomKeys becomes key, minInt, optional and type.
   - Add an assertion that the `optional` value node is `true`, with a message naming the value.
   - Reword the key-set failure message: an absent `optional` key, or any other key, fails. Cite 02-CONTEXT D-13, which supersedes D-11.
   - Update the file header's decision list to mention D-13.
   - Touch nothing else in the file.

   Run `GOWORK=off go test ./internal/upgrade/ -run '^TestChangieConfigShape$' -count=1 -v`. It must print `--- FAIL: TestChangieConfigShape` naming the custom[0] key set, which is an assertion failure, not a build failure. Keep the transcript. Commit only the test file as `test(02-06): expect the changie PR field to be optional (D-13)`. Do not run or gate on `gsd-tools check tdd-red-evidence`.

2. Create `.planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md`:
   - Header in 01-MUTATION-LOG.md's shape: Phase, Date, Scope. The scope is the re-pointed codegraph-go guards for D-13.
   - The pre-mutation cleanliness-gate paragraph.
   - A `## RED-first transcripts (Task 1)` section. Paste the Go transcript into it now.

3. Re-point check:changie leg 5 in Taskfile.yml. Do not edit .changie.yaml yet.
   - The leg runs the same no-PR `new` and now requires exit 0.
   - It then requires the fragment count to be 2. It finds the new file with `grep -lF` on its body line and requires exactly one match. That file must contain `kind: Fixes` and have no line containing `PR:` and no `custom:` line.
   - The label becomes `check:changie: [5/11] changie new with no PR accepted: fragment written without a PR field — ok`.
   - Each failure path gets a named `::error::check:changie: [5/11] …` line.
   - The refusal substring leg 5 used to assert is removed entirely.
   - Legs 6, 7 and 8 now expect the count still at 2, and leg 9 expects 4, with their messages updated to match.

   Run `task check:changie`. It must fail with an `::error::check:changie: [5/11]` line, because PR is still required. That is the leg's RED against the real pre-change config. Paste it into the log's RED-first section.

4. GREEN: edit .changie.yaml (D-13).
   - Directly under `    minInt: 1` in the PR entry, add a line `    optional: true`. Keep `type: int` and `minInt: 1`.
   - Rewrite header lines 5-12 to say four things:
     - A change is recorded with `task changie -- new -k <Kind> -b "<sentence>"`, with `-m PR=<n>` optional.
     - Kinds, auto bumps, the PR field and the formats are locked by CHG-01 (01-CONTEXT D-10).
     - PR is optional per 02-CONTEXT D-13, which supersedes D-11: every fragment of a milestone would carry the same milestone-PR number, so a fragment may omit it.
     - changeFormat's PR conditional renders such an entry without a link, and minInt still refuses PR=0.
   - Do not repeat the new key/value line inside the comment.
   - Leave the newlines and fragmentFileFormat comment paragraphs and the Breaking inline comment untouched.

   Run the verify commands: the TestChangie* tests pass, check:changie ends 11 of 11, and the merge `cmp` is clean. Commit .changie.yaml and Taskfile.yml as `feat(02-06): make the changie PR field optional and re-point the missing-PR leg (CHG-01, CHG-04, D-13)`. check:changie is never red at a commit, and Phase 1's byte reproduction stays green.

gsd-capability-changie, RED first:
5. Refresh the fixture: `cp -f $CG/.changie.yaml $CAP/test/fixtures/changie.yaml`, then `cmp` the two. The fixture stays a byte copy of the real host config (02-01's rule).
6. In test/run.sh:
   - Replace the [skip:no-open-pr] leg with [note:no-open-pr], exactly as the behavior block states. Assert with fixed-string greps.
   - Its ok line is `ok [note:no-open-pr] no open PR for the branch is a note naming the branch, and --list continues with pr none`.
   - Add a [write-no-pr] leg after [commit-scope] and before [no-side-effects]. It first writes and commits a realistic `01-04-SUMMARY.md` (frontmatter `phase: 01-fixture`, `plan: 04`, adding a `--quiet` flag), then asserts every bullet of the behavior block.
   - Write the entries file with printf into `$scratch`.
   - Run the render with `(cd "$work" && CI=true "$CHANGIE" batch auto --dry-run)` into a scratch file. Match exact lines with `grep -qxF --`.
   - Its ok line is `ok [write-no-pr] with no PR the fragment is committed without a PR field and renders without a link`.
   - Set EXPECTED_LEGS=30.

   Run `CHANGIE_BIN=$CHB /bin/bash $CAP/test/run.sh`. It must pass legs 1-15 and then print `::error::run.sh: [note:no-open-pr]`, because the script still skips. Keep that transcript. Commit the fixture and run.sh as `test(02-06): add failing no-open-PR note and no-PR write proofs`.

7. GREEN, in write-fragments.sh:
   - Add a `note()` helper that echoes `changie-fragments: note: $1: $2` on stdout and does not exit.
   - In PR resolution, empty or `null` lookup output now calls `note no-open-pr` with the existing branch-naming detail, and leaves PR empty. gh-missing and a failed lookup stay skips until Task 2.
   - The list output prints `pr none` when PR is empty.
   - The write loop builds its changie argv in an array: `new`, `-k "$kind"`, `-b "$body"`, plus `-m "PR=${PR}"` only when PR is non-empty. It invokes `CI=true "${CHANGIE_ARGV[@]}" "${NEW_ARGV[@]}"`. The body stays one argv element and is never re-evaluated (T-02-01).
   - Keep /bin/bash 3.2 compatibility: no mapfile and no associative arrays. Guard empty arrays under `set -u`.
   - In the header comment, move no-open-pr from the skip list to a new note list, and add a `note: no-open-pr` line beside the skip-id comment block.

   Run the suite: 30 of 30. Run shellcheck on the script, run.sh and both fixture executables. Commit as `feat(02-06): write fragments without a PR number when no open PR is found (D-13)`.

8. Append `## 02-06 Task 1 — no-PR tracer` to 02-CAPABILITY-LOG.md with:
   - the `$CAP` commits since v0.1.0, as SHA and subject;
   - the RED transcript;
   - the 30-of-30 GREEN transcript;
   - the [write-no-pr] fragment file content and the `batch auto --dry-run` output.

   Add the codegraph-go dry-run transcript from the verify block to 02-MUTATION-LOG.md's RED-first section. Commit both logs in codegraph-go as `docs(02-06): record the no-PR tracer evidence`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && out=$(GOWORK=off go test ./internal/upgrade/ -run '^TestChangie' -count=1 -v 2>&1) && test "$(printf '%s\n' "$out" | rg -o '^--- PASS: TestChangie' | wc -l | tr -d ' ')" -ge 8</automated>
    <fails_when>non-zero exit, or "--- FAIL" in the output, or fewer than 8 `--- PASS: TestChangie` lines, or "no tests to run"</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && out=$(task check:changie 2>&1) && printf '%s\n' "$out" | tail -n 1 | rg -x -F 'check:changie: 11 of 11 checks passed against a scratch copy (source tree byte-unchanged)' && printf '%s\n' "$out" | rg -F '[5/11] changie new with no PR accepted: fragment written without a PR field — ok'</automated>
    <fails_when>non-zero exit, or any `::error::check:changie` line, or the last line is not exactly the 11-of-11 line, or leg 5's accepted label is missing</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task changie -- merge --dry-run | cmp - CHANGELOG.md && git diff --quiet main -- CHANGELOG.md</automated>
    <fails_when>non-zero exit, or cmp prints "differ" or "EOF on": Phase 1's byte reproduction broke</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && out=$(CI=true task changie -- new --dry-run -k Fixes -b "no-PR tracer probe") && printf '%s\n' "$out" | rg -x -F 'body: no-PR tracer probe' && test "$(printf '%s\n' "$out" | rg -o 'PR:' | wc -l | tr -d ' ')" = 0 && s=$(git status --porcelain -- .changes) && test -z "$s"</automated>
    <fails_when>non-zero exit: the real host config refused a no-PR fragment, printed a PR field, or the dry run wrote into .changes/</fails_when>
    <automated>cmp /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/fixtures/changie.yaml /Volumes/Code/github.com/seanb4t/codegraph-go/.changie.yaml</automated>
    <fails_when>non-zero exit: the capability fixture is no longer a byte copy of the host config</fails_when>
    <automated>out=$(CHANGIE_BIN="$(cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOWORK=off go tool -modfile=go.tool-changie.mod -n changie)" /bin/bash /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/run.sh) && printf '%s\n' "$out" | tail -n 1 | rg -x -F 'run.sh: 30 of 30 legs passed against a scratch project' && printf '%s\n' "$out" | rg -x -F 'ok [write-no-pr] with no PR the fragment is committed without a PR field and renders without a link' && test "$(printf '%s\n' "$out" | rg -o '^ok \[[a-z:-]+\]' | LC_ALL=C sort -u | wc -l | tr -d ' ')" = 30</automated>
    <fails_when>non-zero exit, or a `::error::run.sh:` line, or the last line is not exactly the 30-of-30 line, or the [write-no-pr] ok line is missing, or fewer than 30 distinct ok labels</fails_when>
    <automated>shellcheck /Volumes/Code/github.com/seanb4t/gsd-capability-changie/skills/changie-fragments/scripts/write-fragments.sh /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/run.sh /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/fixtures/bin/gh /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/fixtures/bin/fake-task</automated>
    <fails_when>non-zero exit, or any `SC[0-9]` finding printed</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd /Volumes/Code/github.com/seanb4t/codegraph-go && l=$(git log --format=%s -n 2 -- .changie.yaml internal/upgrade/changie_shape_test.go) && test "$(printf '%s\n' "$l" | rg -o '^(test|feat)\(02-06\)' | tr '\n' ' ')" = "feat(02-06) test(02-06) "` exits 0. The test commit precedes the config commit (rule x1cjy9vyhq).
    - `cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(rg -F -o 'optional: true' .changie.yaml | wc -l | tr -d ' ')" = 1` exits 0, so the comment does not repeat the key.
    - `cd /Volumes/Code/github.com/seanb4t/codegraph-go && gofmt -l internal/upgrade/` prints nothing.
    - `l=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie log --reverse --format=%s v0.1.0..HEAD) && test "$(printf '%s\n' "$l" | rg -o '^(test|feat)\(02-06\)' | tr '\n' ' ')" = "test(02-06) feat(02-06) "` exits 0.
    - `rg -F -o -- '--- FAIL: TestChangieConfigShape' .planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md | wc -l` prints at least 1, and `rg -F -o '::error::check:changie: [5/11]' .planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md | wc -l` prints at least 1.
    - `rg -F -o '::error::run.sh: [note:no-open-pr]' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 1, and `rg -F -o 'run.sh: 30 of 30 legs passed' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 1.
  </acceptance_criteria>
  <done>A fragment without PR is accepted by codegraph-go's real config and by check:changie. In the capability, a branch with no open PR produces a note, a committed fragment without a PR field, and a rendered line without a link. Each guard went RED before GREEN and has its evidence recorded, and Phase 1's byte reproduction stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand every remaining guard, RED-proven. All gh failure modes become notes with redaction, a required-PR host is proven, check:changie renders the no-PR fragment, and three mutation families are logged</name>
  <files>gsd-capability-changie:test/fixtures/bin/gh, gsd-capability-changie:test/run.sh, gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh, Taskfile.yml, .planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md, .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md</files>
  <read_first>
    - gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh, gsd-capability-changie:test/run.sh and gsd-capability-changie:test/fixtures/bin/gh (as committed by Task 1)
    - .planning/phases/02-phase-close-fragment-capability/02-06-PLAN.md `<interfaces>`: the note ids, redaction expression and leg order this task completes
    - Taskfile.yml: the check:changie target (desc, legs 1-11, the closing counter)
    - .planning/phases/01-changie-baseline/01-MUTATION-LOG.md: the per-family section shape (gate, mutation, applied confirmation, RED, revert, byte-clean proof, GREEN)
    - .planning/phases/02-phase-close-fragment-capability/02-RESEARCH.md lines 531-550 (Security Domain)
  </read_first>
  <behavior>
    - [note:gh-missing] With CHANGIE_FRAGMENTS_GH=/no/such/gh-binary, `--list` exits 0 with no `skip:` line, prints `note: gh-missing:` naming that path, prints `pr none`, and still lists the pending ids
    - [note:pr-lookup-failed] With STUB_GH_MODE=noauth, the note detail contains `You are not logged into any GitHub hosts`. With the new STUB_GH_MODE=noauth-token, the note contains `[REDACTED]` and neither `ghp_` nor `github_pat_`. Both runs exit 0, print `pr none` and print no `skip:` line
    - [note:pr-lookup-invalid] With STUB_GH_PR=0 and with STUB_GH_PR=12abc (mode pr), each run exits 0 and prints `note: pr-lookup-invalid:` and `pr none`. The 12abc run's output does not contain `12abc`
    - [pr-override] With STUB_GH_MODE=noauth, `--pr 77` prints `pr 77`, and `--pr 1` prints `pr 1`. Neither prints a `skip:` or a `note:` line, so gh was not consulted
    - [no-pr-required-host] 01-05-SUMMARY.md is added and committed. The work tree's `.changie.yaml` is replaced by the fixture with the `    optional: true` line deleted, and a grep confirms the deletion applied. A no-PR `--write --summaries 01-05`:
      - exits 1 with `error: changie-failed:` whose detail contains `custom key 'PR'`, and also prints the no-open-pr note
      - leaves the fragment count and HEAD unchanged, and leaves no lock directory
      - is followed by `git checkout -- .changie.yaml`, after which that file's porcelain status is empty
    - [check:changie 12/12] `changie batch auto --dry-run` in the scratch copy exits 0 and prints the exact line `- check:changie probe without PR` and the exact line `- check:changie probe fragment ([#1](https://github.com/seanb4t/codegraph-go/pull/1))`. Afterwards the scratch copy still holds nseeds version files and 5 fragments
  </behavior>
  <action>
Capability, RED first:
1. Add mode `noauth-token` to test/fixtures/bin/gh.
   - It writes one stderr line, exits 1, and is documented in the stub's header.
   - The line is gh's bad-credentials shape, carrying one fake `ghp_` token of 36 or more characters and one fake `github_pat_` token.
   - The fake tokens contain the substring `FAKE`, so no real credential shape is committed.

2. In test/run.sh:
   - Replace [skip:gh-missing] with [note:gh-missing], and [skip:pr-lookup-failed] with [note:pr-lookup-failed].
   - Add [note:pr-lookup-invalid] right after [note:no-open-pr].
   - In [pr-override], add the no-`note:` assertion.
   - Add [no-pr-required-host] after [write-no-pr]. It generates the required-PR variant with `sed` from `$CAP_ROOT/test/fixtures/changie.yaml`, so no new fixture file is needed.
   - Every leg follows the behavior block exactly and keeps the file's check helpers and `ok [label]` line style. The ok lines are:
     - `ok [note:gh-missing] an unresolvable gh executable is a note, and --list continues with pr none`
     - `ok [note:pr-lookup-failed] a failing gh lookup is a note carrying gh's stderr with tokens redacted`
     - `ok [note:pr-lookup-invalid] a gh lookup result that is not a positive integer is a note and never reaches a fragment`
     - `ok [no-pr-required-host] a host that still requires PR refuses a no-PR write with error: changie-failed and nothing is written`
   - Set EXPECTED_LEGS=32. The leg order must equal `<interfaces>`.

   Run the suite. It must go RED at `::error::run.sh: [note:gh-missing]`, a skip where a note is planned. Keep the transcript. Commit the stub and run.sh as `test(02-06): add failing gh-failure note, redaction, invalid-lookup and required-PR-host proofs`.

3. GREEN, in write-fragments.sh:
   - gh unresolvable becomes `note gh-missing` naming the executable.
   - A non-zero lookup becomes `note pr-lookup-failed` with the detail `gh exited <status>: <first stderr line>`. That line first passes through the `<interfaces>` redaction expression, so no gh token reaches stdout, a log or a fragment (T-02-22).
   - Non-empty output that fails `^[1-9][0-9]*$` becomes `note pr-lookup-invalid` with a fixed detail that does not echo the output (T-02-23).
   - Each note leaves PR empty and continues.
   - Update the header: the preflight order, the 9 skip ids, the 4 note ids, and a sentence saying a host that requires PR refuses a no-PR write as changie-failed.
   - Beside the skip-id comment block, keep one comment block spelling out `note: gh-missing`, `note: pr-lookup-failed`, `note: no-open-pr` and `note: pr-lookup-invalid`, the same way 02-01 spelled out every skip id.
   - Run the suite to 32 of 32 and run shellcheck clean. Commit as `feat(02-06): turn gh failures into notes with redacted stderr and validate the looked-up PR (D-13)`.

codegraph-go:
4. Add check:changie leg 12 after leg 11. It runs `changie batch auto --dry-run` in `${work}` into a scratch file, with the `set +e`/status/`set -e` idiom, and asserts:
   - exit 0;
   - the two exact lines from the behavior block, each via `grep -qxF --`;
   - `.changes/v*.md` in the scratch copy still counts nseeds, and the fragment count is still 5, so the dry run wrote nothing.

   Each failure is a named `::error::check:changie: [12/12] …` line. The ok label is `check:changie: [12/12] changie batch auto --dry-run renders the no-PR fragment without a link and a PR fragment with its link (dry run wrote nothing) — ok`.

   Renumber every `[N/11]` to `[N/12]`. Set the executed-leg counter to 12, with the final line `check:changie: 12 of 12 checks passed against a scratch copy (source tree byte-unchanged)`. In the desc, change eleven to twelve and 11 to 12, and add one sentence: leg 5 proves a fragment without PR is accepted (D-13), and leg 12 proves it renders without a link.

   Run `task check:changie` twice to confirm it is idempotent, and run `GOWORK=off go test ./internal/upgrade/ -count=1`. Commit Taskfile.yml as `feat(02-06): prove no-PR fragments render without a link in check:changie (CHG-04, D-13)`.

5. Run the mutation families (D-13 part 1, rule 84d1gfpywd). All of them run in a scratch worktree only; the main tree is never mutated and no mutation is committed.
   - Setup:
     - Record `git worktree list`.
     - `S=$(mktemp -d)` and `git worktree add --detach "$S/wt" HEAD`. The repository configures no hooks, and no commit is made in the worktree.
     - Positive control: `task -d "$S/wt" check:changie` ends 12 of 12.
   - Every family, in 01-MUTATION-LOG.md's shape:
     - The pre-mutation gate: `git -C "$S/wt" diff --quiet -- .changie.yaml` exits 0.
     - Apply exactly one mutation to `$S/wt/.changie.yaml` with portable `sed -i.bak` plus `rm -f` of the .bak.
     - Confirm it applied.
     - Capture both RED transcripts: `task -d "$S/wt" check:changie`, and `(cd "$S/wt" && GOWORK=off go test ./internal/upgrade/ -run '^TestChangieConfigShape$' -count=1 -v)`.
     - Revert with `git -C "$S/wt" checkout -- .changie.yaml`, and prove `git -C "$S/wt" status --porcelain` is empty.
     - Re-run both to GREEN.
   - Family (a), PR optional removed:
     - Delete the `    optional: true` line; the applied check is the literal's count in the file, 0.
     - Expected RED: `::error::check:changie: [5/12]`, and a `--- FAIL: TestChangieConfigShape` naming the custom[0] key set.
   - Family (b), changeFormat conditional removed:
     - Replace the changeFormat line with the unconditional form, which keeps the link text and drops the `{{if .Custom.PR}}` and `{{end}}` wrappers; the applied check is that the `{{if .Custom.PR}}` literal's count in the file is 0.
     - Expected RED: `::error::check:changie: [12/12]`, plus the Go test failing on changeFormat.
   - Family (c), minInt removed:
     - Delete the `    minInt: 1` line; the applied check is the `minInt:` literal's count in the file, 0.
     - Expected RED: `::error::check:changie: [7/12]`, because PR=0 is now accepted, plus the Go test failing on the key set. This proves the amended CHG-04's "PR=0 is still refused" half has teeth under the optional config.
   - If a different leg than expected names the RED, record which one fired and why, as 02-02's family (b) did. Do not change the guard.
   - Teardown: `git worktree remove --force "$S/wt"`. The worktree listing must equal the one recorded at setup, and `git status --porcelain -- .changie.yaml .changes CHANGELOG.md` in the main tree must be empty.

   Append `## Family (a)`, `## Family (b)` and `## Family (c)` to 02-MUTATION-LOG.md, with the setup, positive control and teardown transcripts. End with the line `3 of 3 mutation families went RED on the named guard and back to GREEN`. Commit as `docs(02-06): record PR-optional mutation evidence`.

6. Append `## 02-06 Task 2 — note legs and required-PR host` to 02-CAPABILITY-LOG.md with the `$CAP` commits, the RED transcript and the 32-of-32 GREEN transcript. Commit in codegraph-go as `docs(02-06): record capability note-leg evidence`.
  </action>
  <verify>
    <automated>out=$(CHANGIE_BIN="$(cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOWORK=off go tool -modfile=go.tool-changie.mod -n changie)" /bin/bash /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/run.sh) && printf '%s\n' "$out" | tail -n 1 | rg -x -F 'run.sh: 32 of 32 legs passed against a scratch project' && test "$(printf '%s\n' "$out" | rg -o '^ok \[[a-z:-]+\]' | LC_ALL=C sort -u | wc -l | tr -d ' ')" = 32</automated>
    <fails_when>non-zero exit, or a `::error::run.sh:` line, or the last line is not exactly the 32-of-32 line, or fewer than 32 distinct ok labels</fails_when>
    <automated>shellcheck /Volumes/Code/github.com/seanb4t/gsd-capability-changie/skills/changie-fragments/scripts/write-fragments.sh /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/run.sh /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/fixtures/bin/gh /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/fixtures/bin/fake-task</automated>
    <fails_when>non-zero exit, or any `SC[0-9]` finding printed</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && out=$(task check:changie 2>&1) && printf '%s\n' "$out" | tail -n 1 | rg -x -F 'check:changie: 12 of 12 checks passed against a scratch copy (source tree byte-unchanged)' && test "$(printf '%s\n' "$out" | rg -o '^check:changie: \[[0-9]+/12\] .* — ok$' | wc -l | tr -d ' ')" = 12</automated>
    <fails_when>non-zero exit, or any `::error::check:changie` line, or the last line is not exactly the 12-of-12 line, or fewer than 12 `[N/12] … — ok` lines</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOWORK=off go test ./internal/upgrade/ -count=1</automated>
    <fails_when>non-zero exit, or "FAIL" in the output</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(rg -o '^## Family \([abc]\)' .planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md | wc -l | tr -d ' ')" = 3 && test "$(rg -F -o 'check:changie: 12 of 12 checks passed' .planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md | wc -l | tr -d ' ')" -ge 4</automated>
    <fails_when>non-zero exit: a family section is missing, or fewer than four 12-of-12 transcripts (positive control plus three GREEN re-runs)</fails_when>
  </verify>
  <acceptance_criteria>
    - `l=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie log --reverse --format=%s v0.1.0..HEAD) && test "$(printf '%s\n' "$l" | rg -o '^(test|feat)\(02-06\)' | tr '\n' ' ')" = "test(02-06) feat(02-06) test(02-06) feat(02-06) "` exits 0.
    - `f=/Volumes/Code/github.com/seanb4t/gsd-capability-changie/skills/changie-fragments/scripts/write-fragments.sh; for id in gsd-tools-missing disabled changie-unavailable no-changie-config phase-not-found no-summaries locked already-recorded no-user-visible-change; do rg -q -F "skip: $id" "$f" || echo "missing skip $id"; done; for id in gh-missing pr-lookup-failed no-open-pr pr-lookup-invalid; do rg -q -F "note: $id" "$f" || echo "missing note $id"; done` prints nothing.
    - `rg -F -o 'noauth-token' /Volumes/Code/github.com/seanb4t/gsd-capability-changie/test/fixtures/bin/gh | wc -l` prints at least 2 (the header and the mode).
    - 02-MUTATION-LOG.md contains each of `::error::check:changie: [5/12]`, `::error::check:changie: [12/12]` and `::error::check:changie: [7/12]` at least once (`rg -F -o` then `wc -l`), unless a family recorded a different firing leg with its reason. It also contains `3 of 3 mutation families went RED`.
    - `rg -F -o '::error::run.sh: [note:gh-missing]' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 1, and `rg -F -o 'run.sh: 32 of 32 legs passed' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints at least 1.
    - `cd /Volumes/Code/github.com/seanb4t/codegraph-go && s=$(git status --porcelain -- .changie.yaml .changes CHANGELOG.md) && test -z "$s" && w=$(git worktree list) && test "$(printf '%s\n' "$w" | wc -l | tr -d ' ')" = 1` exits 0: no mutation leaked and no worktree was left.
  </acceptance_criteria>
  <done>
- Every gh failure mode is a named note, not a skip, and a gh token never survives into a note.
- A non-integer lookup never reaches a fragment.
- A host that still requires PR refuses cleanly with nothing written.
- check:changie proves the no-PR render in CI.
- Each re-pointed codegraph-go guard is shown RED against a confirmed, reverted mutation.
</done>
</task>

<task type="auto">
  <name>Task 3: Document, release and upgrade. Amend CHG-01/CHG-04, write the v0.1.1 README and SKILL.md, tag and push, prove the tag from a clean HTTPS clone, and upgrade the project and global installs through gsd-core verbs</name>
  <precondition>`gh auth status` exits 0. `GIT_TERMINAL_PROMPT=0 git ls-remote https://github.com/seanb4t/gsd-capability-changie.git refs/tags/v0.1.0` prints `60d4967ad300643b30f4d947e64a459974c9ed9f` then a tab and `refs/tags/v0.1.0`. `node -e 'process.exit(require(process.env.HOME+"/.gsd-capabilities.json").entries.changie.version==="0.1.0"?0:1)'` exits 0, meaning 02-04's global install is present</precondition>
  <reversibility rating="costly">Tag v0.1.1 is pinned by CAP-04, SHIP-04 and 02-05's CONTRIBUTING text, so moving it means re-installing and re-recording (D-10). Pushing `main` past v0.1.0 also stops gsd-core 1.14.0 from installing `#v0.1.0` (depth-1 clone, see `<interfaces>`). The push itself is pre-approved by the maintainer (D-13 part 3).</reversibility>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, gsd-capability-changie:README.md, gsd-capability-changie:skills/changie-fragments/SKILL.md, gsd-capability-changie:capability.json, .planning/STATE.md, .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 18 and 21 (CHG-01, CHG-04)
    - .planning/ROADMAP.md lines 164, 166 and 192 (Phase 1 SC2, SC4, and the first Phase 2 Notes sentence)
    - gsd-capability-changie:README.md (whole file), gsd-capability-changie:skills/changie-fragments/SKILL.md (whole file), gsd-capability-changie:capability.json
    - .planning/phases/02-phase-close-fragment-capability/02-03-SUMMARY.md (the publish and clean-clone proof shape) and 02-04-SUMMARY.md `## Skill materialization` (the listing, cmp and marker proof shape)
    - $HOME/.claude/gsd-core/bin/lib/capability-lifecycle.cjs lines 900-990 and capability-source.cjs lines 834-866 (read-only; the upgrade swap and the depth-1 clone)
  </read_first>
  <action>
Requirement text first, in codegraph-go (D-13 part 1). These are value edits inside existing lines. Add no heading, bullet, table row or section.
1. Edit REQUIREMENTS.md:
   - In CHG-01, replace "a required custom `PR` field (`type: int`, `minInt: 1`)" with "an optional custom `PR` field (`type: int`, `minInt: 1`, optional since Phase 2 D-13, 2026-09-26)".
   - In CHG-04, replace "a fragment missing `PR` or using an undeclared kind is refused — proven both ways" with "a fragment without `PR` is accepted, `PR=0` is still refused by `minInt: 1`, and a fragment using an undeclared kind is refused — proven both ways (amended by Phase 2 D-13, 2026-09-26)".

   Edit ROADMAP.md:
   - In Phase 1 SC2, "the required integer `PR` field with `minInt: 1`" becomes "the integer `PR` field with `minInt: 1` (optional since Phase 2 D-13)".
   - In Phase 1 SC4, "while a fragment missing `PR` and a fragment naming an undeclared kind are each refused" becomes "while a fragment naming an undeclared kind and a `PR=0` fragment are each refused and a fragment without `PR` is accepted (Phase 2 D-13)".
   - The first Phase 2 Notes sentence becomes: "At phase close on the milestone branch no milestone PR number may exist yet; D-03 and D-13 resolve it: the skill uses an explicit `--pr`, else the branch's open PR, else writes the fragment without a PR number (`PR` is optional since D-13 and `minInt: 1` still refuses `PR=0`), never a placeholder number."

   Commit both files as `docs(02-06): amend CHG-01 and CHG-04 for the optional PR field (D-13)`.

2. In `$CAP`, write the v0.1.1 documentation (D-13 part 2, D-11 amendment).
   - README.md:
     - Every install line uses `$SPEC`, at project and at global scope. No SSH-form spec remains. Add one sentence that the private repository authenticates over HTTPS through gh's credential helper (`gh auth setup-git`).
     - Under Requirements, gh is optional: without it, or when its lookup fails or finds nothing, fragments are written without a PR number.
     - Add a `## PR number` section. It gives the resolution order from `<interfaces>` and states that `--pr 0` is refused. It says the no-PR path needs the host `.changie.yaml`'s `PR` custom field to be optional. A host that still requires PR refuses a no-PR write as `error: changie-failed`, with nothing written; pass `--pr` or make the field optional.
     - Remove gh-missing, pr-lookup-failed and no-open-pr from the skip table. Add a `## Notes` table with the four note ids and their causes, and state that gh tokens are redacted and gh's stdout is never echoed.
     - Add an `## Upgrading` paragraph:
       - Re-run the install commands with the new tag and re-run the `capability set` line. `capability update` re-fetches the spec already recorded in the ledger, so it cannot move to a new tag.
       - gsd-core 1.14.0 fetches a git source with a depth-1 clone of the default branch, so only the tag at `main`'s tip installs. Install the newest tag.
   - SKILL.md:
     - Step 3 says a `skip:` line is reported verbatim and ends the run. A `note:` line is not a skip: report it verbatim and continue, because the fragments will be written without a PR number.
     - The PR rule becomes: never invent or guess a PR number and never pass a placeholder with `--pr`; when the script finds no PR it prints a note and writes the fragments without one.
     - Set `metadata.version` to `"0.1.1"`.
     - Keep the body free of any host-specific installed-skill directory path, as 02-02 fixed.
   - capability.json: set `version` to `"0.1.1"`, and change nothing else.

   Run the suite (32 of 32) and shellcheck. Commit README.md, SKILL.md and capability.json as `docs(02-06): document the optional PR, HTTPS install and notes for v0.1.1`.

3. Tag and publish (D-13 part 3, pre-approved; no checkpoint). Use `GIT_TERMINAL_PROMPT=0` on every git network call; never use SSH.
   1. Create the release note with the file-writing tool in a temp file. Its title line is `gsd-capability-changie v0.1.1`, followed by the changes: optional PR resolution with notes instead of skips, token redaction, looked-up PR validation, the HTTPS install spec and the required-PR-host behaviour. The capability repository has no CHANGELOG file, so this annotated tag message is its release note.
   2. Run `git -C "$CAP" -c tag.gpgsign=false tag -a v0.1.1 -F <file>` on HEAD.
   3. Run `git -C "$CAP" fetch origin`. Require `git -C "$CAP" merge-base --is-ancestor origin/main main`, so the push is a fast-forward.
   4. Push `git -C "$CAP" push origin main`, then `git -C "$CAP" push origin refs/tags/v0.1.1`.
   5. Never use `--force`, `--tags`, `--delete` or `--mirror`. Never touch v0.1.0. Never run `gh repo edit`.
   6. Then prove each of these:
      - `gh repo view seanb4t/gsd-capability-changie --json visibility --jq .visibility` prints `PRIVATE`.
      - `git ls-remote https://github.com/seanb4t/gsd-capability-changie.git` shows: v0.1.0's tag object `60d4967ad300643b30f4d947e64a459974c9ed9f` peeling to `5700e60fb497e033d87ff953c4e957444993628a`; v0.1.1 peeling to the same commit as `refs/heads/main`; and v0.1.1's tag object equal to the local `git -C "$CAP" rev-parse v0.1.1`.
      - A fresh `git clone --branch v0.1.1 https://github.com/seanb4t/gsd-capability-changie.git` into `mktemp -d` runs its own `test/run.sh` with `CHANGIE_BIN=$CHB` to 32 of 32.
      - In a fresh scratch GSD project (`git init -q -b main`, then `gsd-tools config-new-project`), `gsd-tools capability install "$SPEC" --scope project` exits 0, and its `.gsd/capabilities/changie/capability.json` `cmp`s equal to `git -C "$CAP" show v0.1.1:capability.json`.
      - In another fresh scratch project, `gsd-tools capability install https://github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope project` is expected to exit non-zero with a `git checkout "v0.1.0" failed` reason. Record the transcript either way. If it succeeds, record that the `<interfaces>` prediction was wrong and flag it in the SUMMARY; do not re-tag.

4. Upgrade codegraph-go's project install (D-13 part 4).
   1. Record the before state: `node -e` prints the version and source of `.gsd-capabilities.json`'s changie entry (expected 0.1.0 and `#v0.1.0`), plus `git status --porcelain --untracked-files=all -- . ':(exclude).planning/phases'`, which must be empty.
   2. From `$CG`, run `gsd-tools capability install "$SPEC" --scope project`. It must exit 0 with status installed, version 0.1.1. This is gsd-core's reinstall-as-upgrade swap; `capability update` is not used, per `<interfaces>`.
   3. Prove the result:
      - `.gsd/capabilities/changie/capability.json` `cmp`s equal to `git -C "$CAP" show v0.1.1:capability.json`.
      - The ledger entry now reads version 0.1.1 with source `$SPEC`.
      - `gsd-tools query config-get workflow.changie_fragments --raw` prints `true`, and `workflow.changie_command` prints `task changie --`.
      - The render-hooks changie-step count, parsed with node, is 1.
      - `bash .gsd/capabilities/changie/skills/changie-fragments/scripts/write-fragments.sh --phase 2 --list` prints `pr 88` while draft PR #88 is open.
      - The same command run with `CHANGIE_FRAGMENTS_GH=/no/such/gh-binary` prints `note: gh-missing` and `pr none` and no `skip:` line. This is the live no-PR path.
      - The porcelain status is identical before and after both `--list` runs, and empty outside `.planning/phases/`.

5. Upgrade the global install (D-13 part 4, D-14). Use gsd-core verbs only: never copy, link or write into `$SKILLS` or `~/.gsd` by hand, and never set GSD_HOME.
   1. Capture the before state in `B=$(mktemp -d)`:
      - the names listing: `ls -1A "$SKILLS" | LC_ALL=C sort`;
      - a content snapshot of every regular file under `$SKILLS` outside `gsd-changie-fragments/`, via `find "$SKILLS" -type f ! -path "$SKILLS/gsd-changie-fragments/*" -exec cksum {} + | LC_ALL=C sort`. Print its line count before any comparison, and require it to be above zero;
      - a copy of the current materialized SKILL.md;
      - the global ledger entry.
   2. Run `gsd-tools capability install "$SPEC" --scope global`. It must exit 0 at version 0.1.1.
   3. Run `gsd-tools capability set changie --enable --runtime claude --scope global`, with no `--config-dir`.
   4. Prove the result:
      - `comm -3` of the names listings prints nothing: nothing added, nothing removed.
      - The content snapshot is byte-equal before and after.
      - The materialized SKILL.md differs from the saved copy, and `cmp`s equal to `git -C "$CAP" show v0.1.1:skills/changie-fragments/SKILL.md`.
      - The `.gsd-capability-skill` marker contains `changie`.
      - `~/.gsd/capabilities/changie/capability.json` `cmp`s equal to the tag's copy.
      - The global ledger reads 0.1.1 with source `$SPEC`.
      - codegraph-go's render-hooks count is still 1.
   5. If anything other than the materialized SKILL.md content changed in the skills directory, STOP and report the full diff; do not repair it.

6. Record the install constraint through the tool verb. Run `gsd-tools state add-blocker --text`. The text says:
   - gsd-core 1.14.0 installs a git capability from a depth-1 clone of the default branch, so only the tag at gsd-capability-changie's `main` tip installs. `#v0.1.0` stopped resolving when v0.1.1 was pushed.
   - Every later capability release must move `main` to its tag, and 02-05's CONTRIBUTING text, CAP-04 and SHIP-04 must name that newest tag.
   - This is an upstream gap in resolveGit. Filing it upstream needs the maintainer's authorization.

   Commit STATE.md as `docs(02-06): record the capability tag-at-main-tip install constraint`.

7. Append `## 02-06 Task 3 — v0.1.1 publish and upgrade` to 02-CAPABILITY-LOG.md. It contains:
   - the `$CAP` commit trail `v0.1.0..v0.1.1`;
   - the tag and ls-remote SHAs and the visibility;
   - the clean-clone 32-of-32 transcript;
   - both scratch-install transcripts;
   - the project and global before/after records;
   - the comm output, the snapshot comparison, the cmp results, the marker and both render-hooks counts.

   Commit as `docs(02-06): record v0.1.1 publish and upgrade evidence`.
  </action>
  <verify>
    <automated>test "$(gh repo view seanb4t/gsd-capability-changie --json visibility --jq .visibility)" = PRIVATE && r=$(GIT_TERMINAL_PROMPT=0 git ls-remote https://github.com/seanb4t/gsd-capability-changie.git) && printf '%s\n' "$r" | rg -x -F "$(printf '60d4967ad300643b30f4d947e64a459974c9ed9f\trefs/tags/v0.1.0')" && printf '%s\n' "$r" | rg -x -F "$(printf '5700e60fb497e033d87ff953c4e957444993628a\trefs/tags/v0.1.0^{}')" && c=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie rev-parse 'v0.1.1^{commit}') && printf '%s\n' "$r" | rg -x -F "$(printf '%s\trefs/tags/v0.1.1^{}' "$c")" && printf '%s\n' "$r" | rg -x -F "$(printf '%s\trefs/heads/main' "$c")" && test "$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie cat-file -t v0.1.1)" = tag</automated>
    <fails_when>non-zero exit: the repository is not private, v0.1.0 moved, v0.1.1 is missing or lightweight, or remote main is not at v0.1.1</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && t=$(mktemp) && git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie show v0.1.1:capability.json > "$t" && cmp "$t" .gsd/capabilities/changie/capability.json && cmp "$t" "$HOME/.gsd/capabilities/changie/capability.json" && node -e 'const s="https://github.com/seanb4t/gsd-capability-changie.git#v0.1.1";for(const f of [process.argv[1],process.argv[2]]){const e=require(f).entries.changie;if(e.version!=="0.1.1"||e.source!==s)process.exit(1)}' "$PWD/.gsd-capabilities.json" "$HOME/.gsd-capabilities.json"</automated>
    <fails_when>non-zero exit: either installed bundle differs from the tag, or either ledger is not at 0.1.1 from the HTTPS v0.1.1 spec</fails_when>
    <automated>S="${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills/gsd-changie-fragments" && t=$(mktemp) && git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie show v0.1.1:skills/changie-fragments/SKILL.md > "$t" && cmp "$t" "$S/SKILL.md" && rg -x -F changie "$S/.gsd-capability-skill"</automated>
    <fails_when>non-zero exit: the materialized skill is not v0.1.1's SKILL.md byte for byte, or the marker does not name changie</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && h=$(gsd-tools loop render-hooks verify:post --raw) && n=$(printf '%s' "$h" | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log((j.activeHooks||[]).filter(h=>h.capId==="changie").length)') && test "$n" = 1 && s=$(git status --porcelain --untracked-files=all -- . ':(exclude).planning/phases') && test -z "$s"</automated>
    <fails_when>non-zero exit: the changie step count in codegraph-go is not exactly 1, or the upgrade left a tracked or unignored change outside .planning/phases/</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -F 'an optional custom `PR` field' .planning/REQUIREMENTS.md && rg -F 'a fragment without `PR` is accepted, `PR=0` is still refused by `minInt: 1`' .planning/REQUIREMENTS.md && rg -F '(optional since Phase 2 D-13)' .planning/ROADMAP.md && rg -F 'a fragment without `PR` is accepted (Phase 2 D-13)' .planning/ROADMAP.md && c=$(git log -1 --format=%H -- .planning/ROADMAP.md) && d=$(git diff -U0 "$c~1" "$c" -- .planning/ROADMAP.md .planning/REQUIREMENTS.md) && test -n "$d" && test -z "$(printf '%s\n' "$d" | rg '^\+#')"</automated>
    <fails_when>non-zero exit: an amended phrase is missing, the requirement-text commit is empty or unreadable, or it added a heading line</fails_when>
  </verify>
  <acceptance_criteria>
    - `l=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie log --reverse --format=%s v0.1.0..v0.1.1) && test "$(printf '%s\n' "$l" | rg -o '^(test|feat|docs)\(02-06\)' | tr '\n' ' ')" = "test(02-06) feat(02-06) test(02-06) feat(02-06) docs(02-06) "` exits 0.
    - `f=/Volumes/Code/github.com/seanb4t/gsd-capability-changie/README.md; test -f "$f" && test "$(rg -F -o 'git+ssh' "$f" | wc -l | tr -d ' ')" = 0 && test "$(rg -F -o 'gsd-capability-changie.git#v0.1.1' "$f" | wc -l | tr -d ' ')" -ge 2` exits 0: HTTPS-only install lines at v0.1.1.
    - `f=/Volumes/Code/github.com/seanb4t/gsd-capability-changie/skills/changie-fragments/SKILL.md; test -f "$f" && test "$(rg -F -o '.claude/' "$f" | wc -l | tr -d ' ')" = 0 && rg -q -F 'version: "0.1.1"' "$f" && rg -q -F 'note:' "$f"` exits 0.
    - `node -e 'process.exit(require("/Volumes/Code/github.com/seanb4t/gsd-capability-changie/capability.json").version==="0.1.1"?0:1)'` exits 0.
    - `rg -F -o '## 02-06 Task 3 — v0.1.1 publish and upgrade' .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md | wc -l` prints 1. The section contains `run.sh: 32 of 32 legs passed`, and the recorded `comm -3` output is empty.
    - `rg -F -o 'depth-1' .planning/STATE.md | wc -l` prints at least 1: the install constraint is recorded through `state add-blocker`.
  </acceptance_criteria>
  <done>
- v0.1.1 is published privately as main's annotated tip, v0.1.0 is untouched, and the tag is proven from a clean HTTPS clone.
- codegraph-go's project install and the maintainer's global install both run v0.1.1, installed through gsd-core verbs, with nothing else in the global skill set changed.
- CHG-01 and CHG-04 state the optional PR field.
- The depth-1 install constraint is recorded as an upstream gap.
</done>
</task>

</tasks>

## Phase source coverage audit (02-06 additions)

The whole-phase table is in 02-01-PLAN.md. That table predates D-13 and D-14, and 02-01 is not edited, so this plan audits its own sources here.

| Source | ID | Item | Where | Status |
|---|---|---|---|---|
| CONTEXT | D-13 part 1 | `.changie.yaml` PR gets `optional: true`, keeping `type: int` and `minInt: 1`, and its header comment is updated | Task 1 | COVERED |
| CONTEXT | D-13 part 1 | CHG-01 and CHG-04 amended in place | Task 3 step 1 | COVERED |
| CONTEXT | D-13 part 1 | Shape test and the check:changie refusal leg re-pointed; each RED against a confirmed, reverted mutation; Go test RED-first; pass-count line updated | Task 1 (RED-first, leg 5), Task 2 (leg 12, 12 of 12, families a-c) | COVERED |
| CONTEXT | D-13 part 1 | Phase 1 byte reproduction stays green | Task 1 and Task 2 verify | COVERED |
| CONTEXT | D-13 part 2 | Resolution is explicit `--pr`, else the open PR, else no PR with a note, never a skip | Task 1 (no-open-pr), Task 2 (gh-missing, lookup failure, invalid lookup) | COVERED |
| CONTEXT | D-13 part 2 | `--pr 0` still refused | [bad-pr] kept; Task 3 README | COVERED |
| CONTEXT | D-13 part 2 | test/run.sh legs RED first, then GREEN, with an executed-leg count | Task 1 (30), Task 2 (32) | COVERED |
| CONTEXT | D-13 part 2 | README install line switches to HTTPS; release note | Task 3 step 2 (README), step 3 (annotated tag message; no CHANGELOG exists in the capability repo) | COVERED |
| CONTEXT | D-13 part 3 | Push `main` and annotated `v0.1.1`; prove from a clean HTTPS clone; never re-point v0.1.0; never change visibility | Task 3 step 3 | COVERED |
| CONTEXT | D-13 part 4 | Upgrade the project and global installs through gsd-core verbs; re-verify capability.json identity, render-hooks and the materialized skill | Task 3 steps 4-5 | COVERED |
| CONTEXT | D-13 kept | Draft PR #88 stays open; the lookup may still yield #88; the Phase 4 gate is the backstop | Task 3 step 4 (`pr 88`) | COVERED |
| CONTEXT | D-14 | The global-install path is upgraded; 02-05 documents it | Task 3 step 5; 02-05 Task 2 item 3 | COVERED |
| CONTEXT | D-11 amendment | HTTPS spec everywhere, no SSH | Task 3 steps 2-5 | COVERED |
| REQ | CAP-03 | Changed skip and PR behaviour | Tasks 1-2 | COVERED |
| REQ | CAP-04 | Installs upgraded to v0.1.1 | Task 3 | COVERED |
| REQ | CHG-01 | Optional `PR`, type int, minInt 1 | Tasks 1 and 3 | COVERED |
| REQ | CHG-04 | No-PR accepted, `PR=0` refused, undeclared kind refused | Tasks 1-3 | COVERED |
| RESEARCH | new, this plan | `capability update` cannot move tags; `install` over an entry is the upgrade swap | `<interfaces>`, Task 3 | COVERED |
| RESEARCH | new, this plan | A depth-1 clone installs only the tag at `main`'s tip | `<interfaces>`, Task 3 steps 3 and 6, README | COVERED |

Superseded, not gaps: D-03's "never makes `PR` optional" and its fourth skip family (D-13). Out of scope: CAP-05 (Phase 3), the fragment-required gate (Phase 4), filing the upstream gsd-core issue (needs separate authorization).

## Artifacts this phase produces

02-01 to 02-05 carry this table as it stood before D-13. Those plans are not edited, so this copy is the current one.

| Artifact | Kind | Plan |
|---|---|---|
| Repository `seanb4t/gsd-capability-changie`, checkout `/Volumes/Code/github.com/seanb4t/gsd-capability-changie` | new repository (local, then private remote) | 02-01, 02-03 |
| `gsd-capability-changie:capability.json`: id `changie`, role `feature`, `engines.gsd` `^1.14.0`, `runtimeCompat`, one `verify:post` step; version `0.1.0`, then `0.1.1` | new file, edited | 02-01, 02-06 |
| Federated config keys `workflow.changie_fragments` (boolean, default true) and `workflow.changie_command` (string, default `changie`) | config keys | 02-01 (declared), 02-04 (set in codegraph-go) |
| `gsd-capability-changie:skills/changie-fragments/SKILL.md`: frontmatter `name: gsd-changie-fragments`, command `/gsd-changie-fragments <phase> [--pr <n>] [--repo <dir>]`; v0.1.1 reports `note:` lines and continues | new file, edited | 02-01, 02-06 |
| `gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh`: flags `--phase`, `--list`, `--write`, `--summaries`, `--entries`, `--pr`, `--repo`, `--base`; env `GSD_TOOLS`, `CHANGIE_FRAGMENTS_GH`; v0.1.1 has 9 skip ids, 4 note ids (`gh-missing`, `pr-lookup-failed`, `no-open-pr`, `pr-lookup-invalid`), the `pr none` list line and gh stderr redaction | new file, edited | 02-01, 02-06 |
| SKILL.md-level skip id `script-missing` | skip reason | 02-01 |
| Git trailers `Changie-Phase` and `Changie-Summaries`; fragment commit subject `docs(<padded-phase>): add changelog fragments` | commit convention | 02-01 |
| `gsd-capability-changie:test/run.sh`: 29 labelled legs, then 32 (`note:*`, `write-no-pr`, `no-pr-required-host`) | new file, edited | 02-01, 02-06 |
| `gsd-capability-changie:test/fixtures/changie.yaml` (byte copy of codegraph-go's `.changie.yaml`, refreshed for D-13), `test/fixtures/phase/01-01-SUMMARY.md`, `01-02-SUMMARY.md`, `test/fixtures/bin/gh` (env `STUB_GH_MODE` with modes `pr`, `none`, `noauth`, `noauth-token`; `STUB_GH_PR`), `test/fixtures/bin/fake-task` | new files, edited | 02-01, 02-06 |
| `gsd-capability-changie:README.md` (HTTPS install at v0.1.1, `## PR number`, `## Notes`, `## Upgrading`), `gsd-capability-changie:LICENSE` | new files, edited | 02-02, 02-06 |
| Annotated tag `v0.1.0` in `seanb4t/gsd-capability-changie` | release tag | 02-03 |
| Annotated tag `v0.1.1` at `main`'s tip in `seanb4t/gsd-capability-changie`; its message is the release note | release tag | 02-06 |
| Draft PR `gsd/v0.15.0-milestone` → `main` in codegraph-go (#88) | GitHub PR | 02-03 |
| STATE.md Blockers/Concerns entries: the /gsd-ship existing-PR hazard, and the tag-at-main-tip install constraint (both via `gsd-tools state add-blocker`) | planning record | 02-03, 02-06 |
| `.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md` | new planning artifact | 02-01 (extended in 02-02 and 02-06) |
| `.planning/phases/02-phase-close-fragment-capability/02-MUTATION-LOG.md` | new planning artifact | 02-06 |
| `.gitignore` line `.gsd-capabilities.json` | edited file | 02-04 |
| `.planning/config.json` values `workflow.changie_fragments: true` and `workflow.changie_command: "task changie --"` (via config-set) | tool-owned value fill | 02-04 |
| `.changie.yaml` `PR` custom field `optional: true`, with an updated header comment | edited file | 02-06 |
| `internal/upgrade/changie_shape_test.go` TestChangieConfigShape custom key set {key, minInt, optional, type} | edited Go test | 02-06 |
| `Taskfile.yml` `check:changie`: leg 5 re-pointed (no-PR accepted), leg 12 added (no-PR render), 12 legs | edited Taskfile target | 02-06 |
| `.planning/REQUIREMENTS.md` CHG-01 and CHG-04 text; `.planning/ROADMAP.md` Phase 1 SC2/SC4 and Phase 2 Notes text | value edits | 02-06 |
| codegraph-go install output `.gsd/capabilities/changie/` and `.gsd-capabilities.json` (ignored, untracked): 0.1.0, then 0.1.1 | install output | 02-04, 02-06 |
| Maintainer-machine state: `~/.gsd/capabilities/changie/`, `~/.gsd-capabilities.json`, and `${CLAUDE_CONFIG_DIR:-~/.claude}/skills/gsd-changie-fragments/` holding `SKILL.md` and `.gsd-capability-skill`: 0.1.0, then 0.1.1 | global install and materialization | 02-04, 02-06 |
| `CONTRIBUTING.md` subsection `### Changelog fragments at phase close` | edited doc | 02-05 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SUMMARY text → shell | Model-chosen, SUMMARY-derived body text crosses into `changie new` argv; the no-PR path builds a new argv array |
| gh → script | gh's stdout (the PR number) and stderr (error text) cross into the script's notes and changie argv |
| capability checkout → private GitHub repository | A push of `main` and an annotated tag over HTTPS, using gh's stored credential |
| private tag → project and global installs | The bundle and SKILL.md are installed verbatim; the global surface pass rewrites the skills directory |
| codegraph-go config → CHG-04 guarantee | Relaxing `PR` to optional weakens a Phase 1 refusal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | write-fragments.sh no-PR argv | high | mitigate | The body stays one element of an argv array (`-b "$body"`), and `-m PR=` is appended as a separate element only when a PR is set; nothing is re-evaluated. [argv-literal] stays green in the 30- and 32-leg suites. |
| T-02-22 | Information Disclosure | gh stderr reaching a note, the skill transcript or evidence logs | high | mitigate | Only gh's first stderr line reaches a note, after the `<interfaces>` redaction expression replaces every `gh[pousr]_…` and `github_pat_…` token with `[REDACTED]`. gh stdout is never echoed. [note:pr-lookup-failed] with the `noauth-token` stub proves neither prefix survives. |
| T-02-23 | Tampering / Spoofing | a looked-up PR number reaching a fragment | medium | mitigate | Lookup output must match `^[1-9][0-9]*$`; anything else is `note pr-lookup-invalid` with PR unset, and the fixed detail does not echo the output. [note:pr-lookup-invalid] covers `0` and `12abc`. An explicit `--pr` keeps the exit-2 `bad-pr` refusal. |
| T-02-24 | Repudiation | a fragment silently written without a PR | low | mitigate | Every no-PR run prints exactly one named note. The commit keeps both trailers. D-13 accepts a PR-less fragment by design, and the Phase 4 fragment-required gate stays the backstop. |
| T-02-25 | Tampering | `.changie.yaml` relaxation beyond D-13 | medium | mitigate | TestChangieConfigShape pins the exact custom key set, with optional `true`, type `int` and minInt `1`. check:changie legs 5-8 prove no-PR accepted, undeclared kind, `PR=0` and `PR=abc` refused. Mutation families (a)-(c) prove each guard fires. |
| T-02-SC | Tampering | v0.1.1 source for both installs | high | mitigate | The tag is annotated on the tested `main` tip, pushed fast-forward only (never `--force`, never `--tags`), and v0.1.0 is verified unchanged by ls-remote. A clean HTTPS clone passes 32 of 32. Both installed capability.json files and the materialized SKILL.md are `cmp`-equal to `git show v0.1.1:…`. The manifest declares no hooks, so there is no executable surface. No npm, pip or cargo install occurs, so the package-legitimacy gate does not apply. |
| T-02-16 | Elevation of Privilege / Tampering | global surface pass over the skills directory | high | mitigate | gsd-core verbs only, never a hand copy or a GSD_HOME redirect. The names listing must show no addition or removal, a checksum snapshot of every other skill's files must be byte-equal, and the one changed file must equal v0.1.1's SKILL.md. Any other change is a stop. The scope was authorized by D-14 and the upgrade by D-13 part 4. |
| T-02-26 | Denial of Service | a host that still requires PR | low | accept | The no-PR write is refused by the host's own changie as `changie-failed`, rolled back with nothing written, and the manifest step's `onError: skip` keeps the host workflow unblocked. The README tells such hosts to pass `--pr` or make the field optional. |
| T-02-27 | Information Disclosure | HTTPS credentials during push and clone | low | mitigate | `GIT_TERMINAL_PROMPT=0` on every network call. The URL carries no embedded credential, since gh's credential helper supplies it. The pasted transcripts contain only URLs and SHAs. |
</threat_model>

<verification>
- `GOWORK=off go test ./internal/upgrade/ -count=1` passes. `task check:changie` ends 12 of 12. `task changie -- merge --dry-run | cmp - CHANGELOG.md` exits 0.
- The capability suite passes 32 of 32 at `$CAP` HEAD and from a clean HTTPS clone at v0.1.1. shellcheck is clean on all four executables.
- 02-MUTATION-LOG.md holds the Go RED-first transcript, the leg-5 RED against the pre-change config, and families (a)-(c). 02-CAPABILITY-LOG.md holds the three 02-06 sections.
- Remote v0.1.0 is unchanged. v0.1.1 is main's annotated tip. The repository is still PRIVATE.
- Both installs are at 0.1.1 and byte-identical to the tag. The materialized skill is v0.1.1's SKILL.md, the skill set is otherwise unchanged, and render-hooks shows one changie step in codegraph-go.
- For the phase verifier:
  - CHG-04 is amended, not violated. Phase 1's missing-PR refusal leg now proves acceptance (D-13), and the `PR=0` and undeclared-kind refusals are still proven.
  - CAP-03's skip cases lose the three gh/PR skips, which become notes. Its literal "changie not on PATH" reading (02-01 SUMMARY) is unchanged.
</verification>

<success_criteria>
- D-13 parts 1-4 are delivered: the `PR` field is optional in codegraph-go with RED-proven guards; capability v0.1.1 writes without a PR and notes why; it is published and proven; and both installs are upgraded through gsd-core verbs.
- ROADMAP Phase 2 SC3 (CAP-03) holds under the new behaviour: no case prompts or blocks, and the only remaining skips are the named ones.
- ROADMAP Phase 2 SC4 (CAP-04): codegraph-go's project install runs the tag that 02-05's CONTRIBUTING text will name, v0.1.1.
- Phase 1's byte reproduction and every other internal/upgrade guard stay green.
</success_criteria>

<output>
Create `.planning/phases/02-phase-close-fragment-capability/02-06-SUMMARY.md` when done. It must include:
- the codegraph-go and `$CAP` commit trails;
- a `## TDD Gate Compliance` section with the pasted `--- FAIL: TestChangieConfigShape` transcript (rule x1cjy9vyhq) and the `$CAP` test/feat order;
- pointers to the 02-MUTATION-LOG.md and 02-CAPABILITY-LOG.md sections;
- an `## Upgrade` section giving the exact commands that upgraded the project and global installs, which 02-05 copies into CONTRIBUTING, and noting that `capability update` cannot move tags;
- an `## Upstream gap` section on the depth-1 clone that installs only the tag at `main`'s tip, with the `#v0.1.0` scratch-install transcript;
- the interpretation that the annotated tag message is the capability's release note, because the repository has no CHANGELOG.
</output>
