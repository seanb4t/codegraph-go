---
phase: 02-phase-close-fragment-capability
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - .gitignore
  - .planning/config.json
autonomous: false
requirements: [CAP-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "codegraph-go has capability `changie` installed at project scope from `git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0`, and `.gsd/capabilities/changie/capability.json` is byte-identical to `git -C <checkout> show v0.1.0:capability.json` (CAP-04, D-11)"
    - "`.planning/config.json` has `workflow.changie_fragments` set to boolean true and `workflow.changie_command` set to `task changie --`, both written by `gsd-tools query config-set` after the install (CAP-04, D-02, D-11)"
    - "`.gsd/` and the new sibling line `.gsd-capabilities.json` each ignore only their own path. `git check-ignore -v` attributes `.gsd/capabilities/changie/capability.json` to the `.gsd/` line and `.gsd-capabilities.json` to its own line, and neither pattern covers the other (edge CAP-04/adjacency, RESEARCH Pitfall 2)"
    - "The install commit touches exactly `.gitignore` and `.planning/config.json`, and after it `git status --porcelain --untracked-files=all` outside `.planning/phases/` is empty (D-11)"
    - "Run read-only in codegraph-go with `--phase 2 --list`, the installed script resolves `task changie --` without a changie-unavailable skip, prints `kinds Breaking,Features,Fixes,Performance,Dependencies`, prints `pr <n>` equal to the open draft milestone PR from 02-03, and leaves the tree byte-unchanged (D-02, D-03, D-04)"
    - "`gsd-changie-fragments` is resolvable by Claude Code on the maintainer's machine. `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills/gsd-changie-fragments/SKILL.md` exists, byte-identical to the tag's `skills/changie-fragments/SKILL.md`, and a `.gsd-capability-skill` marker beside it names `changie`. The global skills listing gained exactly that one directory and lost none (D-01, RESEARCH Pitfall 1). If the maintainer chooses report-gap, this truth is recorded as unmet with CAP-05 at risk, not papered over"
  prohibitions:
    - statement: "MUST NOT commit capability install output (.gsd/, .gsd-capabilities.json) to codegraph-go; the ledger and bundle are per-machine state"
      requirement_id: CAP-04
      category: privacy
      status: resolved
      verification: test
    - statement: "MUST NOT mutate the maintainer's global ~/.claude or ~/.gsd state without the maintainer's blocking-human answer, and MUST NOT hand-copy a skill into any runtime skills directory. Materialization goes only through gsd-core's own verbs"
      requirement_id: CAP-04
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT hand-edit .planning/config.json. Both keys are written by gsd-tools query config-set (planning-artifacts rule)"
      requirement_id: CAP-04
      category: values
      status: resolved
      verification: judgment
  artifacts:
    - path: ".gitignore"
      provides: "ignore line for the root-level install ledger, sibling of .gsd/"
      contains: ".gsd-capabilities.json"
    - path: ".planning/config.json"
      provides: "workflow.changie_fragments and workflow.changie_command values"
      contains: "changie_command"
  key_links:
    - from: ".planning/config.json workflow.changie_command"
      to: "Taskfile.yml changie target"
      via: "the script runs `task changie -- new …` from the repo root, and `task changie -- --version` is its availability probe"
      pattern: "task changie --"
    - from: "codegraph-go .gsd/capabilities/changie/"
      to: "gsd-capability-changie tag v0.1.0"
      via: "git+ssh install spec"
      pattern: "gsd-capability-changie.git#v0.1.0"
    - from: "${CLAUDE_CONFIG_DIR:-~/.claude}/skills/gsd-changie-fragments/SKILL.md"
      to: "verify:post dispatch Skill(gsd-changie-fragments)"
      via: "Claude Code derives a personal skill's command name from its directory name"
      pattern: "gsd-changie-fragments"
---

<objective>
Install the published capability in codegraph-go at project scope and configure it (CAP-04, D-11). Then, at a maintainer checkpoint, make its skill actually dispatchable by Claude Code on the maintainer's machine (RESEARCH Pitfall 1).

Purpose: "installed" and "dispatchable" are different claims.
- Research showed that a project-scope install gives Claude Code no skill at all. The claude runtime's local artifact layout has no `skills` kind.
- Reading the gsd-core 1.14.0 source for this plan adds a second fact. The materializer reads a third-party SKILL.md only from the GLOBAL capability root: `~/.claude/gsd-core/bin/lib/install-profiles.cjs:733`, `${GSD_HOME || homedir}/.gsd/capabilities/<id>/skills/<stem>/SKILL.md`. So RESEARCH's suggested `capability set changie --enable --runtime claude --scope global` run against a project-only install is expected to stage nothing.
- Global state belongs to the maintainer, so the approach is the maintainer's decision.

Output:
- in codegraph-go: the `.gitignore` line and two config values, committed together
- on the maintainer's machine: the materialized skill, or a recorded gap
- where every install path landed, which answers D-01's research item

`$CAP` means `/Volumes/Code/github.com/seanb4t/gsd-capability-changie`. `SKILLS` means `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills`.
</objective>

<execution_context>
@/Users/sean/.claude/gsd-core/workflows/execute-plan.md
@/Users/sean/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md
@.planning/phases/02-phase-close-fragment-capability/02-RESEARCH.md
@.planning/phases/02-phase-close-fragment-capability/02-03-SUMMARY.md
@.gitignore
@Taskfile.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install v0.1.0 at project scope, set both keys through config-set, ignore the ledger, and prove the live preflight finds the draft PR</name>
  <precondition>`git ls-remote --tags git@github.com:seanb4t/gsd-capability-changie.git refs/tags/v0.1.0` prints a line, and `GH_PROMPT_DISABLED=1 gh pr list --head gsd/v0.15.0-milestone --state open --json number --jq '.[0].number // empty'` prints the PR number recorded in 02-03-SUMMARY.md</precondition>
  <files>.gitignore, .planning/config.json</files>
  <read_first>
    - .gitignore lines 40-50: the `.gsd/` line at 46 and its comment voice
    - .planning/config.json: the `workflow` block, read only to see its current state; it is written by config-set only
    - Taskfile.yml lines 13 and 538-550: `GO_TOOL_CHANGIE` and the `changie` wrapper target that `task changie --` runs
    - .planning/phases/02-phase-close-fragment-capability/02-RESEARCH.md lines 302-312 (Pitfalls 1-2) and 405-414 (the D-11 ordering, verified both ways)
    - .planning/phases/02-phase-close-fragment-capability/02-03-SUMMARY.md: the PR number and tag SHAs
    - .planning/phases/02-phase-close-fragment-capability/02-01-PLAN.md `<interfaces>`: the `--list` output shape
  </read_first>
  <action>
Install first, then configure. D-11 requires that order: a federated key is refused with `Unknown config key` until the capability that declares it is installed. 02-01's [ordering] leg proves that.

1. Record `git status --porcelain --untracked-files=all -- . ':(exclude).planning/phases'` before anything. It is expected to be empty.

2. From codegraph-go's root, with `GIT_SSH_COMMAND='ssh -o BatchMode=yes'`, run `gsd-tools capability install git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope project`. It must exit 0 and needs no consent flag, because the manifest has no hooks. Then:
   - Write `git -C "$CAP" show v0.1.0:capability.json` to a temp file; that command must exit 0. Then `cmp` the temp file against `.gsd/capabilities/changie/capability.json`, which must exit 0.
   - List every path the install created, using `git status --porcelain --untracked-files=all --ignored` and `find .gsd/capabilities/changie -type f`, for the SUMMARY's "where files land" record (D-01).

3. Edit `.gitignore`. Directly after the `.gsd/` line, add a blank-line-separated comment and the line `.gsd-capabilities.json`. The comment says this is the gsd-core capability install ledger, which lands at the project root beside `.gsd/`, not inside it (capability-ledger.cjs joins it to the project root directly), so the `.gsd/` line does not cover it. Add no other line; if step 2's listing shows any other unignored install path, STOP and report it rather than guess.

4. Configure through the tool's verb only, never by editing the file:
   - `gsd-tools query config-set workflow.changie_fragments true`
   - `gsd-tools query config-set workflow.changie_command "task changie --"`, where the value is one argv element

   Then confirm with `gsd-tools query config-get workflow.changie_fragments --raw` (prints `true`) and `gsd-tools query config-get workflow.changie_command --raw` (prints `task changie --`).

5. Prove the host decision and the live preflight:
   - `gsd-tools loop render-hooks verify:post --raw`, parsed with node, has exactly one activeHooks entry with capId changie. Record the count.
   - Record `git status --porcelain --untracked-files=all` before and after running `bash .gsd/capabilities/changie/skills/changie-fragments/scripts/write-fragments.sh --phase 2 --list`. The two must be identical.
   - The `--list` output must include `kinds Breaking,Features,Fixes,Performance,Dependencies` and `pr <n>`, where n is 02-03's PR, plus one `pending` line per existing `02-*-SUMMARY.md`, and must contain no `skip:` line.
   - If it prints `skip: changie-unavailable`, `task changie -- --version` failed from the repo root. Diagnose that and do not change the configured command.

6. Stage with `git add -- .gitignore .planning/config.json` and commit as `chore(02-04): install the changie capability v0.1.0 at project scope (CAP-04)`. Agent commits may use `-c commit.gpgsign=false` (rule xmz3xknbj0). Never put a CI-skip marker in a commit message.

7. Put in 02-04-SUMMARY.md:
   - the install output
   - the "where files land" listing
   - the config-get outputs
   - the render-hooks count
   - the full `--list` transcript
   - the before/after status comparison
  </action>
  <verify>
    <automated>a=$(git -C /Volumes/Code/github.com/seanb4t/codegraph-go check-ignore -v .gsd-capabilities.json) && printf '%s\n' "$a" | rg -F ':.gsd-capabilities.json' && b=$(git -C /Volumes/Code/github.com/seanb4t/codegraph-go check-ignore -v .gsd/capabilities/changie/capability.json) && printf '%s\n' "$b" | rg -F ':.gsd/'</automated>
    <fails_when>non-zero exit: either path is not ignored, or it is ignored by a pattern other than its own line</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node -e 'const c=require("./.planning/config.json");process.exit(c.workflow.changie_fragments===true&&c.workflow.changie_command==="task changie --"?0:1)'</automated>
    <fails_when>non-zero exit: a key is missing, changie_fragments is not boolean true, or changie_command is not exactly `task changie --`</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && c=$(git log -1 --format=%H -- .gitignore) && f=$(git show --name-only --format= "$c") && test "$(printf '%s\n' "$f" | LC_ALL=C sort | tr '\n' ' ')" = ".gitignore .planning/config.json " && s=$(git status --porcelain --untracked-files=all -- . ':(exclude).planning/phases') && test -z "$s"</automated>
    <fails_when>non-zero exit: the install commit touched other files, or install output (or anything else outside .planning/phases/) is left unignored or uncommitted</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd /Volumes/Code/github.com/seanb4t/codegraph-go && t=$(mktemp) && git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie show v0.1.0:capability.json > "$t" && cmp "$t" .gsd/capabilities/changie/capability.json` exits 0.
    - `git ls-files -- .gsd .gsd-capabilities.json` prints nothing.
    - `cd /Volumes/Code/github.com/seanb4t/codegraph-go && out=$(bash .gsd/capabilities/changie/skills/changie-fragments/scripts/write-fragments.sh --phase 2 --list) && printf '%s\n' "$out" | rg -x -F 'changie-fragments: kinds Breaking,Features,Fixes,Performance,Dependencies' && test "$(printf '%s\n' "$out" | rg -o '^changie-fragments: pr [0-9]+$' | wc -l | tr -d ' ')" = 1` exits 0.
    - `git log -1 --format=%s -- .gitignore` prints `chore(02-04): install the changie capability v0.1.0 at project scope (CAP-04)`.
    - `rg -F -o 'where files land' .planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md | wc -l` prints at least 1.
  </acceptance_criteria>
  <done>The capability is installed at project scope from the pinned tag. Both keys are set through config-set. Every install path is ignored and none is committed. The live read-only preflight in codegraph-go resolves `task changie --`, the five kinds and the draft PR number.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Maintainer decision: how gsd-changie-fragments becomes dispatchable by Claude Code on this machine</name>
  <decision>Choose how to materialize the skill into the global Claude Code skills directory, which mutates the maintainer's global ~/.gsd and ~/.claude state, or record the gap instead.</decision>
  <context>
The facts, all read-only and verified 2026-09-25 against gsd-core 1.14.0:
- Project scope never yields a Claude skill. The claude runtime descriptor's `artifactLayout.local` is [commands, agents], with no skills kind (RESEARCH Pitfall 1, capability-registry.cjs). A project-scope install ran through local materialization wrote zero skill files.
- Third-party skills are read only from the GLOBAL capability root. `readInstalledCapabilitySkill` (install-profiles.cjs:733-734) reads `${GSD_HOME || homedir}/.gsd/capabilities/<capId>/skills/<stem>/SKILL.md`. codegraph-go's project install lives at `<repo>/.gsd/capabilities/changie/`, so a global materialization without a global install finds nothing, and skips silently.
- Only SKILL.md is materialized, plus a `.gsd-capability-skill` marker (install-profiles.cjs:876,881). The script stays in the installed bundle, which SKILL.md already locates.
- Claude Code names a personal skill after its directory (`gsd-changie-fragments`) and picks up a new `~/.claude/skills/<dir>` within a running session (code.claude.com/docs/en/skills).
- `capability set … --runtime claude --scope global` runs gsd-core's surface pass over the whole global skills directory, so it can touch more than this one skill. The executor records the skills listing before and after, and stops if anything but the one new directory changed.
- A global install makes the verify:post step active in EVERY GSD project on this machine, because the key defaults to true. Projects without `.changie.yaml` get `skip: no-changie-config` at each phase close (never a block). `workflow.changie_fragments false` silences it per project.
- Whether a project-scope and a global-scope install of the same id coexist cleanly (one hook or two in codegraph-go's render-hooks) has not been observed. D-07's trailer idempotency makes a double dispatch harmless, and the executor records the count.
  </context>
  <options>
    <option id="global-install">
      <name>Install v0.1.0 at global scope too, then materialize for Claude Code (recommended)</name>
      <pros>It is the only path the 1.14.0 source supports for a third-party Claude skill. It uses gsd-core verbs only, and the same pinned tag as the project install.</pros>
      <cons>It adds global state (`~/.gsd/capabilities/changie/`, `~/.gsd-capabilities.json`, `SKILLS/gsd-changie-fragments/`). It activates the step, as a named skip, in every GSD project on this machine. Contributors repeat it once per machine, and CONTRIBUTING documents that in 02-05.</cons>
    </option>
    <option id="project-only-probe">
      <name>First try RESEARCH's command without a global install; fall back if nothing lands</name>
      <pros>It observes the research's recommendation directly, with no global capability install if it somehow works.</pros>
      <cons>The source predicts it stages nothing, yet it still runs the global surface pass. If nothing lands, the executor returns here for global-install or report-gap.</cons>
    </option>
    <option id="report-gap">
      <name>Materialize nothing; record the upstream gap</name>
      <pros>It leaves global state untouched.</pros>
      <cons>`/gsd-changie-fragments` stays unresolvable, so no phase close can dispatch it and CAP-05 is at risk. The dispatchable truth in this plan is recorded as unmet. Filing an upstream issue would need separate authorization.</cons>
    </option>
  </options>
  <resume-signal>Reply `global-install`, `project-only-probe` or `report-gap`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Carry out the chosen materialization and prove the skill resolves, with nothing else in the global skills set changed</name>
  <precondition>Task 2 has a recorded answer from the maintainer</precondition>
  <files>.planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02-phase-close-fragment-capability/02-04-PLAN.md Task 2 `<context>`: the facts behind each option
    - .planning/phases/02-phase-close-fragment-capability/02-03-SUMMARY.md: the tag SHAs
    - gsd-capability-changie:skills/changie-fragments/SKILL.md at tag v0.1.0 (`git -C "$CAP" show v0.1.0:skills/changie-fragments/SKILL.md`)
  </read_first>
  <action>
1. Before any mutation, capture read-only state:
   - `ls -1 "$SKILLS" | LC_ALL=C sort` to `$B/skills.before`, with `B=$(mktemp -d)`
   - `gsd-tools capability list --json`
   - `ls -la ~/.gsd/capabilities 2>/dev/null`
   - the changie hook count in codegraph-go's `gsd-tools loop render-hooks verify:post --raw`

2. Act on the answer.
   - `global-install`, with `GIT_SSH_COMMAND='ssh -o BatchMode=yes'`:
     1. `gsd-tools capability install git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope global`
     2. then `gsd-tools capability set changie --enable --runtime claude --scope global`, with no `--config-dir`, so the real global home is used
   - `project-only-probe`: run only `gsd-tools capability set changie --enable --runtime claude --scope global` from codegraph-go's root. If `$SKILLS/gsd-changie-fragments/SKILL.md` does not exist afterwards, record the outcome and STOP. Return to the maintainer with Task 2's remaining options, `global-install` or `report-gap`.
   - `report-gap`: mutate nothing.
     - Record in 02-04-SUMMARY.md, under `## Upstream gap`, that gsd-core 1.14.0 cannot surface a project-scope third-party skill for Claude Code (install-profiles.cjs:733 reads the global root only).
     - Record the same through `gsd-tools state add-blocker --text`, noting that CAP-05 is at risk until the skill is dispatchable.
     - Do not file an issue without the maintainer's separate authorization.

3. Prove the result (global-install, or a probe that landed):
   - `test -f "$SKILLS/gsd-changie-fragments/SKILL.md"`.
   - Write `git -C "$CAP" show v0.1.0:skills/changie-fragments/SKILL.md` to a temp file (the command must exit 0), then `cmp` it against `$SKILLS/gsd-changie-fragments/SKILL.md`, which must exit 0. SKILL.md has no `.claude/` path, so the surface pass's path rewrite must leave it byte-identical.
   - `$SKILLS/gsd-changie-fragments/.gsd-capability-skill` contains `changie`.
   - `comm -3 "$B/skills.before" <(ls -1 "$SKILLS" | LC_ALL=C sort)` prints exactly one line, `gsd-changie-fragments`, in the added column. Anything else is an unexpected change to the global skill set: STOP and report it with the full diff. Do not repair it by hand.
   - Record `ls -la ~/.gsd/capabilities/changie` and the new changie hook count in codegraph-go's render-hooks. If the count is 2, record it and name D-07's idempotency as the reason a double dispatch is harmless.

4. Never copy, link or write a file into `$SKILLS`, `~/.gsd` or a project `.claude/` by hand, and never set GSD_HOME to redirect the tool (planning-artifacts rule: report the gap, do not work around it).

5. In 02-04-SUMMARY.md, add `## Skill materialization` with:
   - the answer
   - the before and after listings and the comm output
   - the cmp result and the marker content
   - the global paths that now exist
   - both render-hooks counts
   - the complete "where files land" table, project and global (D-01)
  </action>
  <verify>
    <automated>S="${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills/gsd-changie-fragments" && test -f "$S/SKILL.md" && t=$(mktemp) && git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie show v0.1.0:skills/changie-fragments/SKILL.md > "$t" && cmp "$t" "$S/SKILL.md" && rg -x -F changie "$S/.gsd-capability-skill"</automated>
    <fails_when>non-zero exit: the skill directory or SKILL.md is missing (expected, and recorded as the unmet truth, only on `report-gap`), its content differs from the tag's SKILL.md, or the marker does not name changie</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -F -o '## Skill materialization' .planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md | wc -l` prints 1.
    - The comm output recorded in the SUMMARY is exactly one added entry, `gsd-changie-fragments`, and no removed entry (global-install or a landed probe). On report-gap, `rg -F -o '## Upstream gap' .planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md | wc -l` prints 1 and `.planning/STATE.md` carries the add-blocker entry naming CAP-05.
    - `git -C /Volumes/Code/github.com/seanb4t/codegraph-go status --porcelain -- .gitignore .planning/config.json` prints nothing, so materialization changed no tracked codegraph-go file.
  </acceptance_criteria>
  <done>The skill's command name `gsd-changie-fragments` resolves from the global skills directory with the tag's exact SKILL.md, and nothing else in the global skill set changed. Or the maintainer chose report-gap, and the gap and CAP-05 risk are recorded without a workaround.</done>
</task>

</tasks>

## Phase source coverage audit

The whole-phase audit table is in 02-01-PLAN.md, and every row is COVERED. This plan carries:
- CAP-04: project-scope install from the git+ssh tag, the key set in config.json, and `.gsd/` plus the ledger ignored
- D-11: install, then config-set, then gitignore, with the porcelain proof
- D-02: `task changie --`
- D-03 and D-04: live lookup of the draft PR
- D-01: where files land and the resolvable command name
- RESEARCH Pitfalls 1-2, A3 and Open Question 1

## Artifacts this phase produces

This list is identical in all five phase plans. The "Plan" column says which plan produces each item.

| Artifact | Kind | Plan |
|---|---|---|
| Repository `seanb4t/gsd-capability-changie`, checkout `/Volumes/Code/github.com/seanb4t/gsd-capability-changie` | new repository (local, then private remote) | 02-01, 02-03 |
| `gsd-capability-changie:capability.json`: id `changie`, role `feature`, version `0.1.0`, `engines.gsd` `^1.14.0`, `runtimeCompat`, one `verify:post` step | new file | 02-01 |
| Federated config keys `workflow.changie_fragments` (boolean, default true) and `workflow.changie_command` (string, default `changie`) | config keys | 02-01 (declared), 02-04 (set in codegraph-go) |
| `gsd-capability-changie:skills/changie-fragments/SKILL.md`: skill stem `changie-fragments`, frontmatter `name: gsd-changie-fragments`, command `/gsd-changie-fragments <phase> [--pr <n>] [--repo <dir>]` | new file | 02-01 |
| `gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh`: flags `--phase`, `--list`, `--write`, `--summaries`, `--entries`, `--pr`, `--repo`, `--base`; env `GSD_TOOLS`, `CHANGIE_FRAGMENTS_GH`; output prefix `changie-fragments:`; 12 skip ids; error ids `usage`, `bad-pr`, `malformed-entry`, `undeclared-kind`, `changie-failed`, `commit-failed`; lock `<git-dir>/changie-fragments.lock` | new file | 02-01 |
| SKILL.md-level skip id `script-missing` | skip reason | 02-01 |
| Git trailers `Changie-Phase` and `Changie-Summaries`; fragment commit subject `docs(<padded-phase>): add changelog fragments` | commit convention | 02-01 |
| `gsd-capability-changie:test/run.sh`: 29 labelled legs; env `CHANGIE_BIN`, `GSD_TOOLS` | new file | 02-01 |
| `gsd-capability-changie:test/fixtures/changie.yaml`, `test/fixtures/phase/01-01-SUMMARY.md`, `test/fixtures/phase/01-02-SUMMARY.md`, `test/fixtures/bin/gh` (env `STUB_GH_MODE`, `STUB_GH_PR`), `test/fixtures/bin/fake-task` (env `CHANGIE_BIN`, `FAKE_TASK_FAIL_ON_NEW`, `FAKE_TASK_COUNTER`) | new files | 02-01 |
| `gsd-capability-changie:README.md`, `gsd-capability-changie:LICENSE` | new files | 02-02 |
| Annotated tag `v0.1.0` in `seanb4t/gsd-capability-changie` | release tag | 02-03 |
| Draft PR `gsd/v0.15.0-milestone` → `main` in codegraph-go | GitHub PR | 02-03 |
| STATE.md Blockers/Concerns entry on the /gsd-ship existing-PR hazard (via `gsd-tools state add-blocker`) | planning record | 02-03 |
| `.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md` | new planning artifact | 02-01 (extended in 02-02) |
| `.gitignore` line `.gsd-capabilities.json` | edited file | 02-04 |
| `.planning/config.json` values `workflow.changie_fragments: true` and `workflow.changie_command: "task changie --"` (via config-set) | tool-owned value fill | 02-04 |
| codegraph-go install output `.gsd/capabilities/changie/` and `.gsd-capabilities.json` (ignored, untracked) | install output | 02-04 |
| Maintainer-machine state: `~/.gsd/capabilities/changie/`, `~/.gsd-capabilities.json`, and `${CLAUDE_CONFIG_DIR:-~/.claude}/skills/gsd-changie-fragments/` holding `SKILL.md` and `.gsd-capability-skill` (depends on the 02-04 decision) | global install and materialization | 02-04 |
| `CONTRIBUTING.md` subsection `### Changelog fragments at phase close` | edited doc | 02-05 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private GitHub tag → codegraph-go and the global capability root | The bundle and SKILL.md are installed verbatim |
| gsd-core surface pass → the maintainer's global Claude skill set | One verb can re-sync every skill in `~/.claude/skills` |
| codegraph-go working tree → git history | Machine-specific ledgers must not enter the published repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Elevation of Privilege / Tampering | global materialization into `~/.claude/skills` | high | mitigate | A `blocking-human` checkpoint gates it (Task 2), and only gsd-core verbs are used, never a hand copy or a GSD_HOME redirect. The listing captured before and after must differ by exactly one added directory, and anything else is a stop. The materialized SKILL.md is byte-compared against the tag's. |
| T-02-SC | Tampering | install source for project and global scope | high | mitigate | Both installs use the same annotated tag `#v0.1.0` over SSH in batch mode. The project bundle's capability.json and the materialized SKILL.md are each compared against `git show v0.1.0:…`. No hooks are declared, so there is no executable surface. |
| T-02-17 | Information Disclosure | `.gsd-capabilities.json` and `.gsd/` in codegraph-go | medium | mitigate | A dedicated `.gitignore` line. `git check-ignore -v` attributes each path to its own line, `git ls-files` shows neither, and the install commit's file set is exactly `.gitignore` and `.planning/config.json`. |
| T-02-18 | Tampering | `.planning/config.json` (tool-owned) | medium | mitigate | Written only by `gsd-tools query config-set`, after install, and confirmed by config-get and a strict node check. Federated keys are refused before install, so this fails closed. |
| T-02-19 | Denial of Service | the step active in every GSD project after a global install | low | accept | It skips without blocking (no-changie-config) where `.changie.yaml` is absent, and `workflow.changie_fragments false` turns it off per project. The maintainer accepts this at Task 2. |
</threat_model>

<verification>
- The install, the ignore lines, the config values and the single-purpose commit are proven by the Task 1 verify commands.
- The live `--list` in codegraph-go resolves `task changie --`, the five host kinds and the draft PR, read-only.
- The materialized skill exists, is byte-identical to the tag's SKILL.md, carries the changie marker, and is the only change to the global skill set. Or the report-gap outcome is recorded with the CAP-05 risk.
- For the phase verifier: CAP-04's "installs at project scope" is satisfied by Task 1. The dispatchability proof in Task 3 is the RESEARCH Pitfall 1 requirement the orchestrator added; it is not in CAP-04's literal text, but CAP-05 depends on it.
</verification>

<success_criteria>
- ROADMAP Phase 2 SC4 (CAP-04), install half: the capability is installed at project scope from the git+ssh tag, `workflow.changie_fragments` is set in `.planning/config.json`, and `.gsd/` stays ignored with the ledger ignored beside it. The CONTRIBUTING half is in 02-05.
- The skill is dispatchable as `gsd-changie-fragments` on the maintainer's machine, so Phase 3's close can fire it (CAP-05).
</success_criteria>

<output>
Create `.planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md` when done, with the Task 1 record, `## Skill materialization` (or `## Upstream gap`), and the "where files land" table.
</output>
