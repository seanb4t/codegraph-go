---
phase: 02-phase-close-fragment-capability
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - CONTRIBUTING.md
autonomous: true
requirements: [CAP-03, CAP-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A real `Skill(skill=\"gsd-changie-fragments\")` dispatch, with arguments `1 --pr 4242 --repo <scratch>` against the fixture phase (one user-visible change written in jargon, one planning-only change), writes exactly one fragment: kind Features, a plain sentence mentioning `--json` with no WIDGET-, D-NN or plan-number token, and `PR: \"4242\"`. It is committed as `docs(01): add changelog fragments` with Changie-Phase `1` and Changie-Summaries `01-01,01-02`, and its transcript is in 02-05-SUMMARY.md (CAP-03, D-06, D-09)"
    - "CONTRIBUTING.md's `## What .planning/ is` section gains `### Changelog fragments at phase close`. It names the one-time per-clone install (`git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope project`) and the per-machine Claude Code step exactly as 02-04 executed it. It says the repository is private, so the capability is optional without access. It gives the manual fallback `task changie -- new …`, names the `fragment-required` check as the backstop, and shows the manual re-run `/gsd-changie-fragments <phase> [--pr <n>]` (CAP-04, D-12)"
    - "The CONTRIBUTING diff touches only that section, and `TestContributingReferencesRealTaskTargets` still passes (D-12, DOCS-12 keeps §Pull requests)"
  prohibitions:
    - statement: "MUST NOT write a fragment for a planning-only, CI, test, refactor or contributor-tooling change, and MUST NOT put requirement IDs, decision IDs, plan numbers or phase jargon in a fragment body (D-06)"
      requirement_id: CAP-03
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "per-clone and per-machine capability setup, fallback, backstop and manual re-run"
      contains: "### Changelog fragments at phase close"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "Taskfile.yml changie target"
      via: "the manual fallback names `task changie -- new`, and the `task changie` span is checked by TestContributingReferencesRealTaskTargets"
      pattern: "task changie"
    - from: "CONTRIBUTING.md"
      to: "gsd-capability-changie tag v0.1.0"
      via: "the documented install spec"
      pattern: "gsd-capability-changie.git#v0.1.0"
---

<objective>
Prove the judgment half through the real dispatch path, and document the setup for contributors.
- Invoke the materialized skill through the Skill tool, exactly as the host's verify:post dispatch does, against the fixture phase, and assert its outcome (CAP-03, D-06, D-09).
- Add the D-12 subsection to CONTRIBUTING.md, recording the install and materialization procedure 02-04 actually executed (CAP-04).

Purpose: 02-02's rehearsal followed SKILL.md by hand, before publication. This run goes through `Skill(gsd-changie-fragments)`, the same resolution a phase close uses, so CAP-05 at Phase 3 starts from a proven path. CONTRIBUTING can only describe the per-machine step once 02-04 has shown which step works.

Output: the dispatch transcript in 02-05-SUMMARY.md, and the CONTRIBUTING.md subsection.

`$CAP` means `/Volumes/Code/github.com/seanb4t/gsd-capability-changie`.
</objective>

<execution_context>
@/Users/sean/.claude/gsd-core/workflows/execute-plan.md
@/Users/sean/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md
@.planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md
@.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch gsd-changie-fragments through the Skill tool against the fixture phase and assert the judgment outcome</name>
  <precondition>`${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills/gsd-changie-fragments/SKILL.md` exists (materialized by 02-04 Task 3)</precondition>
  <files>.planning/phases/02-phase-close-fragment-capability/02-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md, `## Skill materialization`
    - .planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md, `## Judgment rehearsal`: the scaffold and the assertions this run repeats through the real dispatch path
    - gsd-capability-changie:test/fixtures/phase/01-01-SUMMARY.md and 01-02-SUMMARY.md (at tag v0.1.0)
    - .planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md D-06 and D-09
  </read_first>
  <action>
1. Build the fixture project in `R=$(mktemp -d)`, exactly as 02-02's rehearsal did:
   - `git init -b main` with local identity and `commit.gpgsign false`
   - `.changie.yaml` from `git -C "$CAP" show v0.1.0:test/fixtures/changie.yaml`, and `.changes/unreleased/.gitkeep`
   - `gsd-tools config-new-project`, and a minimal ROADMAP with `### Phase 1: Fixture`
   - `.planning/phases/01-fixture/` with both fixture SUMMARYs taken from the tag
   - a baseline commit, then branch `gsd/fixture-milestone`

   Install the published capability: `gsd-tools capability install git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope project`, with `GIT_SSH_COMMAND='ssh -o BatchMode=yes'`. Set `workflow.changie_command` with config-set to the absolute path of codegraph-go's pinned changie, from `GOWORK=off go tool -modfile=go.tool-changie.mod -n changie` run in codegraph-go. Print `$R` and keep the directory until the assertions are recorded.

2. Invoke the Skill tool with skill `gsd-changie-fragments` and args `1 --pr 4242 --repo <R>`, and carry out what the loaded skill instructs.
   - If the Skill tool reports the skill as unknown in this session, STOP. Return a `checkpoint:human-action` that asks the maintainer to run `/gsd-changie-fragments 1 --pr 4242 --repo <R>` in a fresh Claude Code session and paste the transcript. Leave `$R` in place.
   - Never substitute a by-hand reading of SKILL.md here. 02-02 already covered that, and this task exists to prove the dispatch path.

3. Assert and record each output:
   - `git -C "$R" log -1 --format=%s` is `docs(01): add changelog fragments`.
   - The Changie-Phase trailer is `1` and the Changie-Summaries trailer is `01-01,01-02`.
   - `git -C "$R" show --name-only --format= HEAD` lists exactly one `.changes/unreleased/*.yaml` file.
   - That file contains `kind: Features` and `PR: "4242"`.
   - Its `body:` line contains `--json`.
   - `rg '^body:' <file> | rg -o 'WIDGET-|\bD-[0-9]+|\b0[0-9]-0[0-9]\b' | wc -l` prints 0.
   - A second dispatch with the same arguments reports `skip: already-recorded` (D-07 through the real path).

4. If an assertion fails, the defect is in SKILL.md's judgment wording, which is now tagged. STOP and return a `checkpoint:decision` presenting the failing output and a proposed wording fix. The fix would ship as a new tag `v0.1.1`, with 02-04's installs re-pointed; v0.1.0 is never moved (D-10). Do not edit anything under the materialized skills directory.

5. Put `## Real skill dispatch (judgment half)` in 02-05-SUMMARY.md with the whole transcript: the Skill invocation, the `--list` output, the entries the model chose, the script's last line, the fragment content, the trailer reads, the second-dispatch skip, and every assertion with its result.
  </action>
  <verify>
    <automated>test "$(rg -F -o 'docs(01): add changelog fragments' /Volumes/Code/github.com/seanb4t/codegraph-go/.planning/phases/02-phase-close-fragment-capability/02-05-SUMMARY.md | wc -l | tr -d ' ')" -ge 1 && test "$(rg -F -o 'skip: already-recorded' /Volumes/Code/github.com/seanb4t/codegraph-go/.planning/phases/02-phase-close-fragment-capability/02-05-SUMMARY.md | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>non-zero exit: the SUMMARY lacks the dispatch's fragment commit subject or the second-dispatch already-recorded skip, so the transcript was not recorded</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -F -o '## Real skill dispatch (judgment half)' .planning/phases/02-phase-close-fragment-capability/02-05-SUMMARY.md | wc -l` prints 1.
    - `rg -F -o 'kind: Features' .planning/phases/02-phase-close-fragment-capability/02-05-SUMMARY.md | wc -l` prints at least 1.
    - `rg -F -o '01-01,01-02' .planning/phases/02-phase-close-fragment-capability/02-05-SUMMARY.md | wc -l` prints at least 1.
    - The SUMMARY records the Skill invocation as a Skill-tool call on `gsd-changie-fragments`, not a by-hand reading of SKILL.md. `rg -F -o 'gsd-changie-fragments' .planning/phases/02-phase-close-fragment-capability/02-05-SUMMARY.md | wc -l` prints at least 1.
  </acceptance_criteria>
  <done>The skill, resolved and dispatched by name as a phase close would dispatch it, turns the fixture phase into exactly one plain, jargon-free Features fragment committed with the D-07 trailers, and a second dispatch is a named no-op. The transcript is in the phase SUMMARY (D-09).</done>
</task>

<task type="auto">
  <name>Task 2: Document the per-clone and per-machine capability setup in CONTRIBUTING (D-12)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md lines 118-130 (the changie tool bullet: the voice to match) and 196-206 (`## What .planning/ is`: the subsection goes after its second paragraph and before `## Code of conduct`)
    - .planning/phases/02-phase-close-fragment-capability/02-04-SUMMARY.md, the Task 1 record and `## Skill materialization`: the exact commands that worked on the maintainer's machine
    - .planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md D-12
    - internal/upgrade/taskfile_shape_test.go lines 2079-2107: TestContributingReferencesRealTaskTargets, which checks backtick `task <target>` spans against Taskfile.yml
  </read_first>
  <action>
Per D-12, add `### Changelog fragments at phase close` inside `## What .planning/ is`, after its two existing paragraphs and before `## Code of conduct`. It must not go under §Pull requests, whose prose is DOCS-12 in Phase 5. Write in the section's plain, direct voice; a short paragraph or two plus a command list is enough. It says:

1. Closing a GSD phase in this repository writes that phase's changelog fragments automatically. A private gsd-core capability does it: its `gsd-changie-fragments` skill runs at verify:post. The configuration is already committed in `.planning/config.json`.

2. One-time setup per clone: `gsd-tools capability install git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope project`.

3. One-time setup per machine, so Claude Code can dispatch the skill: the exact commands 02-04 executed and verified, copied from 02-04-SUMMARY.md, never re-derived. With `global-install` these are the global-scope install of the same tag and `gsd-tools capability set changie --enable --runtime claude --scope global`. Give one clause on why: gsd-core surfaces third-party skills for Claude Code only from the global capability root. If 02-04 recorded report-gap, say instead that dispatch is not yet available on this gsd-core version, and point to the manual fallback.

4. The repository is private, so the capability is optional. Contributors without access write fragments by hand with `task changie -- new -k <Kind> -b "<sentence>" -m PR=<n>`. Also write the plain `task changie` span once, closed right after the target name, so TestContributingReferencesRealTaskTargets covers it.

5. Either way, the `fragment-required` pull-request check, added by Phase 4 of this milestone, is the backstop.

6. To re-run the skill by hand: `/gsd-changie-fragments <phase> [--pr <n>]`. Explicit `--pr` wins over the lookup of the branch's open PR.

Commit as `docs(02-05): document the changelog-fragment capability setup (CAP-04)`. Never put a CI-skip marker in a commit message.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOWORK=off go test ./internal/upgrade/ -run '^TestContributingReferencesRealTaskTargets$' -count=1 -v</automated>
    <fails_when>non-zero exit, or "--- FAIL" in the output, or "no tests to run" in the output</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && for t in '### Changelog fragments at phase close' 'gsd-capability-changie.git#v0.1.0 --scope project' '/gsd-changie-fragments' 'fragment-required' 'task changie -- new' 'private'; do test "$(rg -F -o -- "$t" CONTRIBUTING.md | wc -l | tr -d ' ')" -ge 1 || { echo "CONTRIBUTING missing: $t"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or a line starting `CONTRIBUTING missing:`</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd /Volumes/Code/github.com/seanb4t/codegraph-go && s=$(rg -n -x -F '## What `.planning/` is' CONTRIBUTING.md | cut -d: -f1) && e=$(rg -n -x -F '## Code of conduct' CONTRIBUTING.md | cut -d: -f1) && h=$(rg -n -x -F '### Changelog fragments at phase close' CONTRIBUTING.md | cut -d: -f1) && test "$s" -lt "$h" && test "$h" -lt "$e"` exits 0: the subsection sits inside the section.
    - `cd /Volumes/Code/github.com/seanb4t/codegraph-go && c=$(git log -1 --format=%H -- CONTRIBUTING.md) && d=$(git diff -U0 "$c~1" "$c" -- CONTRIBUTING.md) && printf '%s\n' "$d" | rg -o '^@@ -[0-9]+' | wc -l` prints 1: the documenting commit has one hunk, so no other section changed.
    - `git log -1 --format=%s -- CONTRIBUTING.md` prints `docs(02-05): document the changelog-fragment capability setup (CAP-04)`.
  </acceptance_criteria>
  <done>A contributor reading CONTRIBUTING learns the per-clone install, the per-machine step as actually verified, that the capability is optional without access, the manual `task changie -- new` fallback, the fragment-required backstop, and the manual re-run. The only CONTRIBUTING change is this subsection, and the task-target guard stays green.</done>
</task>

</tasks>

## Phase source coverage audit

The whole-phase audit table is in 02-01-PLAN.md, and every row is COVERED. This plan carries:
- CAP-03 and D-06: the judgment half through the real dispatch path
- D-09: the one real skill run, with its transcript in this phase's SUMMARY
- CAP-04 and D-12: CONTRIBUTING documents the one-time install

## Artifacts this phase produces

This list is identical in all five phase plans. The "Plan" column says which plan produces each item.

| Artifact | Kind | Plan |
|---|---|---|
| Repository `seanb4t/gsd-capability-changie`, checkout `/Volumes/Code/github.com/seanb4t/gsd-capability-changie` | new repository (local, then private remote) | 02-01, 02-03 |
| `gsd-capability-changie:capability.json`: id `changie`, role `feature`, version `0.1.0`, `engines.gsd` `^1.14.0`, `runtimeCompat`, one `verify:post` step | new file | 02-01 |
| Federated config keys `workflow.changie_fragments` (boolean, default true) and `workflow.changie_command` (string, default `changie`) | config keys | 02-01 (declared), 02-04 (set in codegraph-go) |
| `gsd-capability-changie:skills/changie-fragments/SKILL.md`: skill stem `changie-fragments`, frontmatter `name: gsd-changie-fragments`, command `/gsd-changie-fragments <phase> [--pr <n>] [--repo <dir>]` | new file | 02-01 |
| `gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh`: flags `--phase`, `--list`, `--write`, `--summaries`, `--entries`, `--pr`, `--repo`, `--base`; env `GSD_TOOLS`, `CHANGIE_FRAGMENTS_GH`; output prefix `changie-fragments:`; 12 skip ids; error ids `usage`, `bad-pr`, `malformed-entry`, `undeclared-kind`, `changie-failed`, `commit-failed`; lock `<git-dir>/changie-fragments.lock` | new file | 02-01 |
| SKILL.md-level skip id `script-missing` | skip reason | 02-01 |
| Git trailers `Changie-Phase` and `Changie-Summaries`; fragment commit subject `docs(<padded-phase>): add changelog fragments` | commit convention | 02-01 |
| `gsd-capability-changie:test/run.sh`: 29 labelled legs; env `CHANGIE_BIN`, `GSD_TOOLS` | new file | 02-01 |
| `gsd-capability-changie:test/fixtures/changie.yaml`, `test/fixtures/phase/01-01-SUMMARY.md`, `test/fixtures/phase/01-02-SUMMARY.md`, `test/fixtures/bin/gh` (env `STUB_GH_MODE`, `STUB_GH_PR`), `test/fixtures/bin/fake-task` (env `CHANGIE_BIN`, `FAKE_TASK_FAIL_ON_NEW`, `FAKE_TASK_COUNTER`) | new files | 02-01 |
| `gsd-capability-changie:README.md`, `gsd-capability-changie:LICENSE` | new files | 02-02 |
| Annotated tag `v0.1.0` in `seanb4t/gsd-capability-changie` | release tag | 02-03 |
| Draft PR `gsd/v0.15.0-milestone` → `main` in codegraph-go | GitHub PR | 02-03 |
| STATE.md Blockers/Concerns entry on the /gsd-ship existing-PR hazard (via `gsd-tools state add-blocker`) | planning record | 02-03 |
| `.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md` | new planning artifact | 02-01 (extended in 02-02) |
| `.gitignore` line `.gsd-capabilities.json` | edited file | 02-04 |
| `.planning/config.json` values `workflow.changie_fragments: true` and `workflow.changie_command: "task changie --"` (via config-set) | tool-owned value fill | 02-04 |
| codegraph-go install output `.gsd/capabilities/changie/` and `.gsd-capabilities.json` (ignored, untracked) | install output | 02-04 |
| Maintainer-machine state: `~/.gsd/capabilities/changie/`, `~/.gsd-capabilities.json`, and `${CLAUDE_CONFIG_DIR:-~/.claude}/skills/gsd-changie-fragments/` holding `SKILL.md` and `.gsd-capability-skill` (depends on the 02-04 decision) | global install and materialization | 02-04 |
| `CONTRIBUTING.md` subsection `### Changelog fragments at phase close` | edited doc | 02-05 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture SUMMARY text → the model → the script | The same untrusted-text path as a real phase close, through the real dispatch |
| CONTRIBUTING.md → contributors | Documented commands are run by people with and without access to the private repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | SUMMARY-derived bodies in the real dispatch | high | mitigate | This inherits 02-01's argv discipline: entries go through a file written with the file tool, and the script passes each body as one argv element. This run happens in a throw-away `mktemp -d` repository, never in codegraph-go. |
| T-02-20 | Spoofing | a stale or mislabelled changelog entry from a misjudged dispatch | medium | mitigate | The outcome is asserted: one fragment, the right kind, a plain sentence, no jargon tokens, and the right trailers. A failure stops for a maintainer decision, and the fix ships as a new tag, never a moved one. |
| T-02-21 | Information Disclosure | CONTRIBUTING references a private repository | low | accept | Only the name and install spec are published, which CAP-01/CAP-04 already make public in ROADMAP and REQUIREMENTS. Access stays GitHub-permission-gated, and the manual fallback is documented for everyone else. |
</threat_model>

<verification>
- The real Skill-tool dispatch produces exactly the asserted fragment and trailers, and a second dispatch reports already-recorded. The transcript is in 02-05-SUMMARY.md.
- CONTRIBUTING.md has the D-12 subsection in the right section, as one hunk, and `TestContributingReferencesRealTaskTargets` passes.
- Expected at this phase's own close, observed rather than planned: the verify:post step may dispatch for Phase 2 itself, now that it is installed and materialized. Phase 2's SUMMARYs describe planning, contributor tooling and a private capability repository, all not user-visible per D-06, so the expected result is `skip: no-user-visible-change` with no commit. Record it in VERIFICATION as a first observation of the skip path. It neither satisfies nor blocks CAP-05, which needs a written fragment at Phase 3's close. If a Phase 2 fragment is written anyway, that is a D-06 misjudgment: remove it before batch and file the SKILL.md wording fix as a new tag.
- CAP-03's "changie not on PATH" skip case is implemented as `skip: changie-unavailable` for the configured command (D-02). The interpretation is recorded in 02-01-SUMMARY.md for the verifier.
</verification>

<success_criteria>
- ROADMAP Phase 2 SC3 (CAP-03), judgment half: through the real dispatch path, the skill writes one fragment per user-visible change, with host kinds and a plain body, and none for planning-only work.
- ROADMAP Phase 2 SC4 (CAP-04), documentation half: CONTRIBUTING.md documents the one-time per-clone install, and the per-machine step 02-04 verified.
</success_criteria>

<output>
Create `.planning/phases/02-phase-close-fragment-capability/02-05-SUMMARY.md` when done, with `## Real skill dispatch (judgment half)` and the CONTRIBUTING commit.
</output>
