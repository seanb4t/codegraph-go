---
phase: 02-phase-close-fragment-capability
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [CAP-01, CAP-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`seanb4t/gsd-capability-changie` exists on GitHub with visibility PRIVATE, and its `main` equals the capability repository HEAD that 02-02 finished and tested (CAP-01, D-10)"
    - "Annotated tag `v0.1.0` exists locally and on the remote with the same tag-object SHA, and peels to that tested commit (CAP-01, D-10)"
    - "A fresh `git clone --branch v0.1.0` of the published repository passes `test/run.sh` 29 of 29, and `gsd-tools capability install git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope project` on a scratch project exits 0 with an installed capability.json byte-identical to the tag's. The published content, not the local tree, is what was proven (CAP-01)"
    - "codegraph-go has an open DRAFT PR from `gsd/v0.15.0-milestone` to `main`. The skill's exact lookup, `gh pr list --head gsd/v0.15.0-milestone --state open --json number --jq '.[0].number // empty'`, prints its number, and it is still OPEN after the close-draft-prs workflow has run for it. The maintainer opened it as OWNER, which that workflow exempts (D-03, D-04)"
    - "STATE.md Blockers/Concerns records, through `gsd-tools state add-blocker`, that /gsd-ship's create_pr step runs `gh pr create` unconditionally and will fail at milestone close because the milestone PR already exists, and names the draft PR's number and the alternative (`gh pr ready <n>`) (D-04, RESEARCH Pitfall 4)"
    - "Neither the repository creation, the tag push, the milestone-branch push nor the PR creation happens before the maintainer answers the blocking-human checkpoint"
  prohibitions:
    - statement: "MUST NOT create or switch seanb4t/gsd-capability-changie to public visibility. Making it public is deferred beyond this milestone (D-10, Deferred Ideas)"
      requirement_id: CAP-01
      category: privacy
      status: resolved
      verification: test
    - statement: "MUST NOT move, delete or re-point tag v0.1.0 once it is pushed. A later fix ships as a new tag such as v0.1.1 (D-10: CAP-04 and SHIP-04 pin the tag)"
      requirement_id: CAP-01
      category: values
      status: resolved
      verification: test
  artifacts:
    - path: ".planning/STATE.md"
      provides: "the /gsd-ship existing-PR hazard, written by the state tool's own verb"
      contains: "gh pr ready"
  key_links:
    - from: "codegraph-go draft PR (gsd/v0.15.0-milestone -> main)"
      to: "gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh"
      via: "the script's gh pr list --head <branch> lookup returns this PR's number at every phase close of the milestone"
      pattern: "gh pr list --head gsd/v0.15.0-milestone --state open"
    - from: "tag v0.1.0"
      to: "codegraph-go install (02-04)"
      via: "git+ssh install spec pinned to #v0.1.0"
      pattern: "gsd-capability-changie.git#v0.1.0"
---

<objective>
Publish the capability and open the milestone PR, after the maintainer approves both outward-facing actions at one blocking-human checkpoint.
- Create the private repository `seanb4t/gsd-capability-changie`, push `main`, and cut and push the annotated tag `v0.1.0` (CAP-01, D-10).
- Push `gsd/v0.15.0-milestone` and open it as a draft PR to `main`, so that every fragment this milestone writes carries a real PR number (D-03, D-04).

Purpose: CAP-04 installs from `git+ssh://…#v0.1.0`, which must exist first. The skill's PR lookup needs an open PR on the milestone branch before Phase 3 closes. Both are outward-facing, so neither is autonomous. CAP-01 authorizes the repository, and D-04 authorizes the PR, but only at a maintainer-approved checkpoint.

Output: the private GitHub repository with tag `v0.1.0`, the draft milestone PR, and one STATE.md Blockers/Concerns entry recording the /gsd-ship hazard.

`$CAP` means `/Volumes/Code/github.com/seanb4t/gsd-capability-changie`. All git network operations use `GIT_SSH_COMMAND='ssh -o BatchMode=yes'`.
</objective>

<execution_context>
@/Users/sean/.claude/gsd-core/workflows/execute-plan.md
@/Users/sean/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md
@.planning/phases/02-phase-close-fragment-capability/02-02-SUMMARY.md
@.github/workflows/close-draft-prs.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Maintainer approval for the outward-facing GitHub actions: publish the private capability and tag v0.1.0; push the milestone branch and open the draft PR</name>
  <decision>Approve (a) creating private repo seanb4t/gsd-capability-changie, pushing main, and pushing annotated tag v0.1.0, and (b) pushing gsd/v0.15.0-milestone to origin and opening a draft PR to main.</decision>
  <context>
Before presenting this checkpoint, the executor runs these read-only commands and shows their output:
- `git -C "$CAP" log --oneline`
- `git -C "$CAP" status --porcelain` (must be empty)
- `git -C "$CAP" rev-parse HEAD` (must equal 02-02-SUMMARY's final HEAD)
- the last line of the 29-of-29 run from 02-02
- `gh auth status` (the account that will act)
- `git ls-remote --heads origin gsd/v0.15.0-milestone` (empty today: the branch has never been pushed)
- `gh pr list --head gsd/v0.15.0-milestone --state all --json number,state` (`[]` today)

(a) Capability publication. CAP-01 names the repository, so creating it is authorized scope, and visibility stays private (D-10). Commands:
- `gh repo create seanb4t/gsd-capability-changie --private --source "$CAP" --remote origin --push`
- `git -C "$CAP" tag -a v0.1.0 -m "gsd-capability-changie v0.1.0"`
- `git -C "$CAP" push origin v0.1.0`

The tag is costly to move (D-10), because 02-04's install and SHIP-04 pin it. A fix after this point ships as v0.1.1; v0.1.0 is never moved.

(b) Draft milestone PR (D-04). Commands:
- `git push -u origin gsd/v0.15.0-milestone`
- `gh pr create --draft --base main --head gsd/v0.15.0-milestone --title <title> --body-file <file>`

Proposed title, following v0.14.0's milestone PR #76: `feat: v0.15.0 — changie release management`. Proposed body: the milestone goal from ROADMAP. It says this PR is opened early as a draft so that phase-close changelog fragments carry its number (Phase 2 D-04), and that it will be marked ready at milestone close.

What to expect once it is open:
- `.github/workflows/close-draft-prs.yml` closes draft PRs only for authors who are not OWNER, MEMBER or COLLABORATOR, so a PR opened as seanb4t (OWNER) stays open.
- CI runs on every push to the branch.
- Checks that judge a draft body, such as require-issue-link and pr-template-format, may fail. D-04 says those are expected and do not block this phase.

Known milestone-close hazard (RESEARCH Pitfall 4, ~/.claude/gsd-core/workflows/ship.md create_pr step): /gsd-ship calls `gh pr create` unconditionally and has no existing-PR probe, so it will FAIL for this branch. Editing gsd-core is out of scope. Task 2 records the alternative in STATE.md: mark this PR ready with `gh pr ready <n>` and update its body.

Rejected alternatives (D-04): a PR number held in config, which is stale next milestone and wrong on other branches; and a placeholder such as `PR=1`, which would link the wrong PR.
  </context>
  <options>
    <option id="publish-both">
      <name>Publish the capability and open the draft milestone PR</name>
      <pros>02-04 can install from the tag. The skill finds a real PR number at Phase 3's close, so CAP-05 can pass without a manual --pr.</pros>
      <cons>The milestone branch becomes public now, not at milestone close. Draft-body checks show red until the PR is marked ready.</cons>
    </option>
    <option id="publish-capability-only">
      <name>Publish the capability; do not open the PR yet</name>
      <pros>The milestone branch stays local.</pros>
      <cons>At Phase 3's close the skill prints `skip: no-open-pr`, so CAP-05 cannot pass until a PR exists or someone re-runs the skill by hand with `--pr`.</cons>
    </option>
    <option id="hold">
      <name>Hold both</name>
      <pros>Nothing leaves the machine.</pros>
      <cons>02-04 cannot install from `git+ssh://…#v0.1.0`, so the phase stops here.</cons>
    </option>
  </options>
  <resume-signal>Reply `publish-both`, `publish-capability-only` or `hold`, optionally with a replacement PR title.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Perform the approved actions and prove the published tag, not the local tree, and the live PR lookup</name>
  <precondition>`gh auth status` exits 0 for account seanb4t, and `git -C "$CAP" rev-parse HEAD` equals the final HEAD recorded in 02-02-SUMMARY.md</precondition>
  <reversibility rating="costly">D-10: 02-04's install and SHIP-04 pin tag v0.1.0; moving it means re-installing and re-recording, so a fix ships as a new tag instead.</reversibility>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/phases/02-phase-close-fragment-capability/02-02-SUMMARY.md: the capability HEAD to tag
    - .planning/phases/02-phase-close-fragment-capability/02-CONTEXT.md D-03, D-04 and D-10
    - .planning/phases/02-phase-close-fragment-capability/02-RESEARCH.md lines 320-333 (Pitfall 4, the /gsd-ship create_pr step)
    - .github/workflows/close-draft-prs.yml: the OWNER, MEMBER and COLLABORATOR exemption
    - .planning/STATE.md, the `### Blockers/Concerns` section only (written by `gsd-tools state add-blocker`, never by hand)
  </read_first>
  <action>
Act only on what Task 1's answer approved. On `hold`, do nothing and record the hold in 02-03-SUMMARY.md. On `publish-capability-only`, skip steps 4-7, and in step 7 instead record with `gsd-tools state add-blocker --text` that no milestone PR is open, so the skill will skip no-open-pr at Phase 3's close unless a PR is opened or it is re-run with `--pr`.

1. Publish the capability (CAP-01, D-10):
   - Run `gh repo create seanb4t/gsd-capability-changie --private --description "gsd-core capability: changie changelog fragments at phase close" --source "$CAP" --remote origin --push`.
   - Confirm `gh repo view seanb4t/gsd-capability-changie --json visibility --jq .visibility` prints `PRIVATE`.
   - Run `git -C "$CAP" tag -a v0.1.0 -m "gsd-capability-changie v0.1.0"` on the approved HEAD, then `git -C "$CAP" push origin v0.1.0`.
   - Record three SHAs: `git -C "$CAP" rev-parse v0.1.0` (the tag object), `git -C "$CAP" rev-parse 'v0.1.0^{commit}'`, and `git ls-remote --tags git@github.com:seanb4t/gsd-capability-changie.git 'v0.1.0*'`. The remote `refs/tags/v0.1.0` must equal the local tag object, and `refs/tags/v0.1.0^{}` the tested commit.

2. Prove the published content:
   - `C=$(mktemp -d)`, then `git clone --depth 1 --branch v0.1.0 git@github.com:seanb4t/gsd-capability-changie.git "$C/clone"`.
   - Run `/bin/bash "$C/clone/test/run.sh"` with CHANGIE_BIN from codegraph-go's pinned changie. It must print 29 of 29.

3. Prove the install spec 02-04 will use:
   - In `P=$(mktemp -d)`: `git init -b main`, then `gsd-tools config-new-project`.
   - Run `gsd-tools capability install git+ssh://git@github.com/seanb4t/gsd-capability-changie.git#v0.1.0 --scope project`. It must exit 0.
   - Write `git -C "$CAP" show v0.1.0:capability.json` to a temp file; that command must exit 0. Then `cmp` the temp file against `"$P/.gsd/capabilities/changie/capability.json"`, which must exit 0.

4. Open the milestone PR (D-04):
   - From codegraph-go, run `git push -u origin gsd/v0.15.0-milestone`.
   - Write the approved body to a file under `mktemp -d`.
   - Run `gh pr create --draft --base main --head gsd/v0.15.0-milestone --title "<approved title>" --body-file <file>`, and capture the URL and number `<n>`.

5. Prove the PR as the skill will see it:
   - `gh pr view <n> --json number,isDraft,state,headRefName,baseRefName` shows isDraft true, state OPEN, head `gsd/v0.15.0-milestone` and base `main`.
   - `GH_PROMPT_DISABLED=1 gh pr list --head gsd/v0.15.0-milestone --state open --json number --jq '.[0].number // empty'` prints exactly `<n>`. This is the script's own lookup.

6. Confirm it survived the draft closer:
   - Poll `gh run list --workflow close-draft-prs.yml --limit 3 --json status,conclusion,createdAt,event` until the run created for this PR's `opened` event is completed. It is expected to be `skipped`, because OWNER is exempt.
   - Then `gh pr view <n> --json state --jq .state` must print `OPEN`.
   - If the workflow closed the PR, STOP and report it. Do not reopen it without the maintainer.

7. Record the milestone-close hazard through the tool's verb, never by editing STATE.md by hand. Run `gsd-tools state add-blocker --text` with: "[Phase 2] Milestone PR #<n> (gsd/v0.15.0-milestone → main) is already open as a draft. /gsd-ship's create_pr step runs gh pr create unconditionally (no existing-PR probe, ship.md create_pr) and will fail at milestone close. Mark #<n> ready with gh pr ready <n> and update its body instead of running ship's create step. The fragment-writing skill resolves its PR number from this PR." Then check with `git diff -- .planning/STATE.md` that the only change is that entry.

8. Commit STATE.md as `docs(02-03): record the milestone-PR ship hazard`. Never put a CI-skip marker in a commit message (rule f18zrdsgx5).

9. In 02-03-SUMMARY.md, record:
   - the answer given
   - the repository URL and visibility
   - the tag object and peeled SHAs, local and remote
   - the clone-at-tag 29-of-29 transcript
   - the install-from-tag cmp result
   - the PR number and URL, and its view JSON
   - the lookup output
   - the close-draft-prs run conclusion
  </action>
  <verify>
    <automated>test "$(gh repo view seanb4t/gsd-capability-changie --json visibility --jq .visibility)" = PRIVATE</automated>
    <fails_when>non-zero exit: the repository is missing or its visibility is not PRIVATE</fails_when>
    <automated>r=$(GIT_SSH_COMMAND='ssh -o BatchMode=yes' git ls-remote --tags git@github.com:seanb4t/gsd-capability-changie.git 'refs/tags/v0.1.0') && test -n "$r" && t=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie rev-parse v0.1.0) && test "$(printf '%s\n' "$r" | cut -f1)" = "$t" && k=$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie cat-file -t v0.1.0) && test "$k" = tag</automated>
    <fails_when>non-zero exit: the remote tag is missing, differs from the local tag object, or the tag is lightweight rather than annotated</fails_when>
    <automated>n=$(GH_PROMPT_DISABLED=1 gh pr list --head gsd/v0.15.0-milestone --state open --json number --jq '.[0].number // empty') && test -n "$n" && test "$(gh pr view "$n" --json isDraft,state,baseRefName --jq '"\(.isDraft) \(.state) \(.baseRefName)"')" = "true OPEN main"</automated>
    <fails_when>non-zero exit: no open PR for the milestone branch, or it is not a draft, not OPEN, or not based on main (on `publish-capability-only` or `hold` this check is expected to fail and the SUMMARY records why)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -F -o 'gh pr ready' .planning/STATE.md | wc -l` prints at least 1.
    - `l=$(git log --format=%s -- .planning/STATE.md) && test "$(printf '%s\n' "$l" | rg -x -F 'docs(02-03): record the milestone-PR ship hazard' | wc -l | tr -d ' ')" = 1` exits 0.
    - `rg -F -o 'run.sh: 29 of 29 legs passed' .planning/phases/02-phase-close-fragment-capability/02-03-SUMMARY.md | wc -l` prints at least 1 (the clone-at-tag run).
    - `test "$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie rev-parse 'v0.1.0^{commit}')" = "$(git -C /Volumes/Code/github.com/seanb4t/gsd-capability-changie rev-parse HEAD)"` exits 0: the tag is on the tested HEAD.
    - `gh pr view "$(GH_PROMPT_DISABLED=1 gh pr list --head gsd/v0.15.0-milestone --state open --json number --jq '.[0].number // empty')" --json number --jq .number` prints the same number recorded in 02-03-SUMMARY.md.
  </acceptance_criteria>
  <done>The capability is published privately with annotated tag v0.1.0 on the tested commit, and a clone of the tag passes its own suite and installs by the git+ssh spec. The milestone branch has an open draft PR that the skill's lookup finds, and the /gsd-ship hazard is recorded in STATE.md through the tool's verb.</done>
</task>

</tasks>

## Phase source coverage audit

The whole-phase audit table is in 02-01-PLAN.md, and every row is COVERED. This plan carries:
- CAP-01: private repository and tagged release
- D-10: private visibility, annotated tag v0.1.0
- D-04: draft milestone PR at a maintainer checkpoint
- RESEARCH Pitfall 4: /gsd-ship fails on an existing PR, recorded rather than patched
- CAP-04: the PR the installed skill resolves in codegraph-go

## Artifacts this phase produces

This list is identical in all five phase plans. The "Plan" column says which plan produces each item.

| Artifact | Kind | Plan |
|---|---|---|
| Repository `seanb4t/gsd-capability-changie`, checkout `/Volumes/Code/github.com/seanb4t/gsd-capability-changie` | new repository (local, then private remote) | 02-01, 02-03 |
| `gsd-capability-changie:capability.json`: id `changie`, role `feature`, version `0.1.0`, `engines.gsd` `^1.14.0`, `runtimeCompat`, one `verify:post` step | new file | 02-01 |
| Federated config keys `workflow.changie_fragments` (boolean, default true) and `workflow.changie_command` (string, default `changie`) | config keys | 02-01 (declared), 02-04 (set in codegraph-go) |
| `gsd-capability-changie:skills/changie-fragments/SKILL.md`: skill stem `changie-fragments`, frontmatter `name: gsd-changie-fragments`, command `/gsd-changie-fragments <phase> [--pr <n>] [--repo <dir>]` | new file | 02-01 |
| `gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh`: flags `--phase`, `--list`, `--write`, `--summaries`, `--entries`, `--pr`, `--repo`, `--base`; env `GSD_TOOLS`, `CHANGIE_FRAGMENTS_GH`; output prefix `changie-fragments:`; 12 skip ids; error ids `usage`, `bad-pr`, `malformed-entry`, `undeclared-kind`, `changie-failed`, `commit-failed`; lock `<git-dir>/changie-fragments.lock` | new file | 02-01 |
| SKILL.md-level skip id `script-missing` | skip reason | 02-01 |
| Git trailers `Changie-Phase` and `Changie-Summaries`; fragment commit subject `docs(<padded-phase>): add changelog fragments` | commit convention | 02-01 |
| `gsd-capability-changie:test/run.sh`: 29 labelled legs; env `CHANGIE_BIN`, `GSD_TOOLS` | new file | 02-01 |
| `gsd-capability-changie:test/fixtures/changie.yaml`, `test/fixtures/phase/01-01-SUMMARY.md`, `test/fixtures/phase/01-02-SUMMARY.md`, `test/fixtures/bin/gh` (env `STUB_GH_MODE`, `STUB_GH_PR`), `test/fixtures/bin/fake-task` (env `CHANGIE_BIN`, `FAKE_TASK_FAIL_ON_NEW`, `FAKE_TASK_COUNTER`) | new files | 02-01 |
| `gsd-capability-changie:README.md`, `gsd-capability-changie:LICENSE` | new files | 02-02 |
| Annotated tag `v0.1.0` in `seanb4t/gsd-capability-changie` | release tag | 02-03 |
| Draft PR `gsd/v0.15.0-milestone` → `main` in codegraph-go | GitHub PR | 02-03 |
| STATE.md Blockers/Concerns entry on the /gsd-ship existing-PR hazard (via `gsd-tools state add-blocker`) | planning record | 02-03 |
| `.planning/phases/02-phase-close-fragment-capability/02-CAPABILITY-LOG.md` | new planning artifact | 02-01 (extended in 02-02) |
| `.gitignore` line `.gsd-capabilities.json` | edited file | 02-04 |
| `.planning/config.json` values `workflow.changie_fragments: true` and `workflow.changie_command: "task changie --"` (via config-set) | tool-owned value fill | 02-04 |
| codegraph-go install output `.gsd/capabilities/changie/` and `.gsd-capabilities.json` (ignored, untracked) | install output | 02-04 |
| Maintainer-machine state: `~/.gsd/capabilities/changie/`, `~/.gsd-capabilities.json`, and `${CLAUDE_CONFIG_DIR:-~/.claude}/skills/gsd-changie-fragments/` holding `SKILL.md` and `.gsd-capability-skill` (depends on the 02-04 decision) | global install and materialization | 02-04 |
| `CONTRIBUTING.md` subsection `### Changelog fragments at phase close` | edited doc | 02-05 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer machine → GitHub | A repository, a tag, a branch and a PR are created under the maintainer's credentials |
| GitHub tag → every installing clone | Whatever tag v0.1.0 points at is installed verbatim; skill bodies are not content-scanned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Repudiation / Elevation of Privilege | outward-facing GitHub actions | high | mitigate | Task 1 is a `blocking-human` checkpoint that auto-mode cannot approve. Task 2 acts only on the recorded answer, and the SUMMARY records the answer and every resulting SHA, URL and number. |
| T-02-SC | Tampering | capability source consumed by `capability install git+ssh://…#v0.1.0` | high | mitigate | The repository is private. The pin is an annotated tag, not a branch, so a later commit cannot propagate silently. The local tag object equals the remote. A fresh clone at the tag passes the full suite, and the git+ssh install's capability.json is byte-compared against the tag. The manifest declares no `hooks`, so there is no executable surface. |
| T-02-12 | Tampering | tag v0.1.0 integrity over time | medium | mitigate | The SHAs are recorded in the SUMMARY. The prohibition forbids moving the tag, and a fix ships as a new tag (D-10). 02-04 re-checks the installed capability.json against the tag. |
| T-02-13 | Information Disclosure | repository visibility | medium | mitigate | The repository is created with `--private` and verified PRIVATE by `gh repo view`. Visibility changes are deferred beyond this milestone. |
| T-02-14 | Denial of Service | draft PR closed by `close-draft-prs.yml` | low | mitigate | The PR is opened as seanb4t (OWNER), which the workflow exempts. Task 2 waits for that workflow's run and asserts the PR is still OPEN, and stops rather than reopening. |
| T-02-15 | Information Disclosure | pushing the milestone branch before milestone close | low | accept | The branch's planning and code content is published deliberately (CONTRIBUTING: `.planning/` is public) and will be public at milestone close anyway. The maintainer chooses the timing at Task 1. |
</threat_model>

<verification>
- `gh repo view` shows PRIVATE. The remote tag equals the local annotated tag object and peels to the tested HEAD.
- The clone at v0.1.0 passes 29 of 29, and the git+ssh install from the tag produces a byte-identical capability.json.
- The milestone PR is a draft, OPEN and based on main, and the skill's own lookup returns its number.
- STATE.md carries the /gsd-ship hazard entry, written by `gsd-tools state add-blocker`.
- Milestone-close note for the maintainer (not work for this phase): do not run /gsd-ship's create step for this branch. Mark the PR ready (`gh pr ready <n>`) and update its body, because ship.md's create_pr has no existing-PR path and gsd-core workflow edits are out of scope.
</verification>

<success_criteria>
- ROADMAP Phase 2 SC1 (CAP-01): the private repository exists with a tagged release (README and LICENSE from 02-02), and the tagged content installs and passes its suite.
- D-04: the milestone PR is open as a draft before Phase 3 closes, so CAP-05's fragment can carry a real PR number.
</success_criteria>

<output>
Create `.planning/phases/02-phase-close-fragment-capability/02-03-SUMMARY.md` when done, carrying every item in Task 2 step 9.
</output>
