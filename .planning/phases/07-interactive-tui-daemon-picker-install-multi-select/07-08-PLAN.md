---
phase: 07-interactive-tui-daemon-picker-install-multi-select
plan: 08
type: execute
wave: 5
depends_on: ["07-06", "07-07"]
files_modified:
  - internal/githooks/githooks_test.go
  - test/integration/piped_never_hang_test.go
autonomous: true
requirements: [TEST-03, TUI-04]
must_haves:
  truths:
    - "A githooks test proves install → user edit OUTSIDE the marker block → remove returns the hook file BYTE-IDENTICAL to the pre-install original (marker block fully stripped, user content intact) — D-16 / TEST-03."
    - "The subprocess harness spawns the real binary for `daemon` (bare) and `install` with piped/closed stdin+stdout under a bounded context.WithTimeout, asserting prompt exit with the non-interactive output and NO hang — adding bubbletea must not regress this (D-17, TUI-04)."
    - statement: "githooks install→install is a fixed point (stable from the second install onward per Phase-5's documented one-blank-line first-install quirk) and remove→remove is a clean no-op — TEST-03 idempotency edge; asserted alongside the byte-invariance round trip."
      verification: backstop
    - statement: "Interactive components under piped/closed stdio exit within the timeout; a hang fails the test (via a goroutine + select against ctx/time.After) rather than blocking the suite — TEST-03/TUI-04 concurrency edge."
      verification: backstop
  artifacts:
    - "internal/githooks/githooks_test.go — TestInstall_EditThenRemove_ByteInvariant."
    - "test/integration/piped_never_hang_test.go — daemon-bare + install piped never-hang under a timeout."
  key_links:
    - "piped_never_hang_test.go → main_test.go runBinary/binPath (real spawned binary, piped bytes.Buffer stdio)."
    - "byte-invariance test → githooks.Install/Remove (Phase-5 strip-then-restore semantics)."
  prohibitions:
    - "The never-hang assertion must fail (not block go test) on a hang — wrap runBinary in a goroutine + select against a bounded timeout."
---

<objective>
Land TEST-03: the githooks byte-invariance unit test and the piped-stream never-hang integration assertions that prove adding the interactive TUI never causes a hang.

Purpose: Per D-16 a focused `internal/githooks` test asserts install → edit-outside-marker → remove == the pre-install original (the genuine gap RESEARCH identified — the existing `TestRemove_WithUserContent_PreservesRemainderBytes` only covers remove-preserving-remainder, not the full round trip). Per D-17 the existing `test/integration/` subprocess harness gains cases that spawn the real binary for `daemon` (bare) and `install` with piped/closed stdin+stdout under a timeout, asserting prompt non-interactive exit. This is the last plan: it depends on the daemon tree (07-07) and install wiring (07-06) being wired into the real binary.

Output: a new githooks test + a new integration test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-CONTEXT.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-RESEARCH.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-PATTERNS.md
@internal/githooks/githooks_test.go
@internal/githooks/githooks.go
@test/integration/main_test.go
@test/integration/watch_default_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: githooks install→edit→remove byte-invariance test (D-16)</name>
  <files>internal/githooks/githooks_test.go</files>
  <read_first>
    - internal/githooks/githooks_test.go (TestRemove_WithUserContent_PreservesRemainderBytes lines 381-411 — the fixture/assertion shape to extend; initRepo helper line 38; markerBlock helper)
    - internal/githooks/githooks.go (Install line 241 / Remove line 325 — strip-then-restore semantics; the documented from-scratch-vs-round-tripped one-blank-line quirk noted in STATE.md Phase 5)
    - STATE.md Phase-5 note on the "from-scratch install seed differs from the round-tripped form by one blank line, stable from the second install onward"
  </read_first>
  <action>
    Add `TestInstall_EditThenRemove_ByteInvariant` to internal/githooks/githooks_test.go, using the existing `initRepo` fixture. Steps: (1) write a pristine original hook file (e.g. post-commit) with real user content and capture its exact bytes; (2) call `Install(ctx, root)`; (3) simulate a user edit OUTSIDE the marker block (append/prepend real content to the file, mirroring the withUser shape but as a post-install edit); (4) call `Remove(ctx, root)`; (5) assert `os.ReadFile` returns the file byte-identical to the captured pre-install original PLUS the user's out-of-marker edit — i.e. the codegraph marker block is fully stripped and only the non-codegraph bytes (original + user edit) remain, byte-for-byte. Also assert the idempotency backstop: a second `Install` after the first equals the first install's result (fixed point from the second install onward, honoring the documented one-blank-line first-install quirk), and a second `Remove` is a clean no-op. Keep the assertion on exact bytes, not substring presence.
  </action>
  <verify>
    <automated>go test ./internal/githooks/ -run 'TestInstall_EditThenRemove_ByteInvariant'</automated>
  </verify>
  <acceptance_criteria>
    - The test performs install → out-of-marker edit → remove and asserts byte-identity to (original + user edit) with the marker block gone.
    - install→install fixed point and remove→remove no-op asserted.
  </acceptance_criteria>
  <done>TEST-03's byte-invariance half is proven against the real Phase-5 strip/restore semantics.</done>
</task>

<task type="auto">
  <name>Task 2: piped never-hang integration cases for daemon + install (D-17)</name>
  <files>test/integration/piped_never_hang_test.go</files>
  <read_first>
    - test/integration/main_test.go (runBinary lines 165-183 — spawns binPath with bytes.Buffer stdout/stderr, i.e. non-TTY piped stdio; binPath built once by TestMain; copyFixture helper)
    - test/integration/watch_default_test.go (lines 33-37 — the context.WithTimeout bounded-context convention to copy)
    - 07-PATTERNS.md "test/integration/piped_never_hang_test.go" section (the goroutine + select-against-time.After wrapper so a hang FAILS rather than blocks go test)
    - internal/cli/daemon.go + install.go (the non-interactive fallbacks the assertions expect: daemon bare → plain list exit 0; install → auto)
  </read_first>
  <action>
    Create `test/integration/piped_never_hang_test.go` (package integration). Add a test that, for each of `daemon` (bare, no args) and `install`, runs the real binary via `runBinary` (stdio is already piped bytes.Buffers — the exact closed/piped-stdin+stdout shape D-17 needs) inside a goroutine, with a `select` against a bounded `time.After` (mirror watch_default_test.go's 30s convention; use ~10s) so a hang triggers `t.Fatal("… did not exit — likely blocked on tea.NewProgram()")` instead of blocking the suite. Assert: `daemon` exits 0 and emits the plain running-daemon list (or "no running daemons") on stdout, no ANSI/interactive escape; `install` exits (0 on success path) via the auto fallback and never prompts. Use a fixture dir (copyFixture / a fresh temp) as cmd.Dir. For install, prefer `--target none` or run in a throwaway HOME so it does not mutate the developer's real agent configs — set env HOME to a temp dir via runBinary's env arg. Keep the assertion tolerant of the actual non-interactive output wording but strict on "exited before the timeout".
  </action>
  <verify>
    <automated>go test ./test/integration/ -run 'TestPipedNeverHang' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Spawning the real binary for `daemon` and `install` with piped stdio exits before the timeout (a hang fails the test).
    - `daemon` bare → plain list, exit 0; `install` → auto fallback, no prompt.
    - The test lives in package integration and reuses binPath/runBinary (no new build step).
  </acceptance_criteria>
  <done>Adding bubbletea is proven not to hang any piped/CI invocation — TUI-04's never-hang guarantee is enforced against the real binary.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- NEW test `internal/githooks/githooks_test.go::TestInstall_EditThenRemove_ByteInvariant`.
- NEW `test/integration/piped_never_hang_test.go` (daemon + install piped never-hang under timeout).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real binary | The subprocess test drives the real production argv/stdio seam |
| piped stdio → interactive code | Confirms the never-hang invariant holds against the shipped binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08-01 | Denial of Service | interactive hang on piped stdio | high | mitigate | The bounded-timeout goroutine+select turns any hang into a test FAILURE, catching a mis-gated tea.NewProgram before release (TUI-04) |
| T-07-08-02 | Tampering | install mutating real HOME during test | low | mitigate | Run install under a temp HOME / --target none so the developer's real agent configs are untouched |
</threat_model>

<verification>
- `go test ./internal/githooks/` green (byte-invariance).
- `go test ./test/integration/ -run TestPipedNeverHang` green (never-hang).
- Full-suite phase gate: `go build ./... && GOOS=windows GOARCH=amd64 go vet ./internal/daemon/ ./internal/graphstore/ && go test ./... && go test ./testdata/golden/... && go test ./test/integration/...`.
</verification>

<success_criteria>
- Byte-invariance round trip proven; piped daemon + install never hang against the real binary.
</success_criteria>

<output>
Create `.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-08-SUMMARY.md` when done.
</output>
