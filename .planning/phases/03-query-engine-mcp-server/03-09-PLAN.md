---
phase: 03-query-engine-mcp-server
plan: 09
type: tdd
wave: 7
depends_on: ["03-08"]
files_modified:
  - testdata/golden/golden_parity_test.go
autonomous: true
requirements: [MCP-04]
must_haves:
  truths:
    - "The Go engine's output shapes match the TS golden corpus for query/callers/callees/impact/status/explore/node after the documented D-05 normalizations"
    - "The parity test is CI-reproducible: it resolves the weft corpus at its pinned commit or skips loudly with a documented reason"
    - "The D-05 divergences (node ids, edge multiplicity, status remapping, absent score) are encoded as explicit normalizers, not hand-waved"
  artifacts:
    - "testdata/golden/golden_parity_test.go — TestGoldenParity + the weft-corpus resolver + the D-05 normalizers"
  key_links:
    - "Reuses isVolatileKey/findVolatileKeys from golden_test.go (same package) — no reimplementation"
    - "Indexes the weft source tree via indexer.Run, runs internal/query.Engine for the golden's exact commands, and diffs shapes"
    - "Weft resolution: CODEGRAPH_WEFT_CORPUS env, else a sibling checkout, verified at commit f89ae3ea4e4c37509f7302fd4e37986212a72079; t.Skip if absent"
---

<objective>
Build the golden parity harness (MCP-04, success criterion 4): prove the Go query engine's output shapes match the TS CodeGraph v1.3.1 golden corpus (`testdata/golden/corpus/weft-go/*.json`) for the seven captured tools, after the documented D-05 normalizations (ignore `id`; tolerate edge-multiplicity differences; remap `status` TS-only fields; expect no `score`). The test indexes the real `weft` source tree, runs `internal/query.Engine` for the golden's exact commands, and diffs the results against the fixtures.

Purpose: This is the acceptance gate for MCP-04 and the whole parity claim. It extends the existing `testdata/golden/golden_test.go` harness, reusing its volatile-field stripping. It resolves VALIDATION's Wave 0 corpus-availability gap: the weft source must be reachable at its pinned commit for CI reproducibility — the resolver uses `CODEGRAPH_WEFT_CORPUS` (or a sibling checkout) verified at the pinned commit, and skips loudly (documenting how to enable) when absent so `go test ./...` stays green everywhere.
Output: `golden_parity_test.go` with `TestGoldenParity` + corpus resolver + D-05 normalizers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-engine-mcp-server/03-PATTERNS.md
@testdata/golden/golden_test.go
@testdata/golden/README.md
@testdata/golden/corpus/weft-go/query.json
@testdata/golden/corpus/weft-go/impact.json
@testdata/golden/corpus/weft-go/status.json
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): TestGoldenParity + weft-corpus resolver (skip-if-absent) asserting shape parity</name>
  <read_first>
    - testdata/golden/golden_test.go (isVolatileKey/findVolatileKeys + fixture-loading conventions to reuse)
    - testdata/golden/README.md (corpus provenance; the pinned weft commit; volatile-field rules)
    - testdata/golden/corpus/weft-go/{query,callers,callees,impact,status,explore,node}.json (the shapes to match; the exact commands each was captured with)
    - internal/query/engine.go + search.go + traverse.go + status.go + explore.go + node.go (the methods to invoke)
    - internal/indexer (indexer.Run to build a weft graph into a temp store)
    - .planning/phases/03-query-engine-mcp-server/03-RESEARCH.md (D-05 normalizations; Open Question 1 impact arithmetic; §Validation Architecture)
  </read_first>
  <files>testdata/golden/golden_parity_test.go</files>
  <behavior>
    - resolveWeftCorpus(t) returns the weft source dir from CODEGRAPH_WEFT_CORPUS, else a conventional sibling checkout; it verifies the git HEAD equals the pinned commit f89ae3ea4e4c37509f7302fd4e37986212a72079 and t.Skip()s with a clear, actionable message (how to set the env / checkout the pin) when the corpus is absent or at the wrong commit.
    - TestGoldenParity indexes the resolved weft tree via indexer.Run into a temp store, opens internal/query.Engine, and for each captured command runs the Go equivalent for the same symbol/args the golden used (e.g. impact "mergeStyle" depth 2; callers/callees "mergeStyle"; status; node "mergeStyle" -f internal/cli/finish.go; explore "main function" --max-files 1; query for the captured term).
    - It marshals each result and diffs against the corpus fixture after normalization: strip volatile keys (reuse findVolatileKeys), ignore `id` fields (compare records on name/kind/filePath/line), tolerate edge-multiplicity differences (compare callers/impact as sets on stable fields), apply the status TS→Go key remapping, and expect no `score`.
    - The impact case asserts the nodeCount/edgeCount semantics agreed in 03-04 against impact.json.
  </behavior>
  <action>
    Add `testdata/golden/golden_parity_test.go` in package `golden` (so it can call `isVolatileKey`/`findVolatileKeys` directly). Implement `resolveWeftCorpus(t)` with the env/sibling resolution + pinned-commit verification + documented `t.Skip`. Implement the normalizers as small helpers: a recursive `id`-key stripper, a stable-field projector for location arrays (name/kind/filePath/startLine) compared as sets, and the status key-remap allowance mirroring 03-05's table. Write `TestGoldenParity` driving `internal/query.Engine` for the seven captured commands and diffing normalized shapes against the fixtures. Include a short comment block documenting each divergence being normalized (D-05a..e) so the intent is auditable. This is RED — the normalizers/parity assertions are written first; the executor confirms the impact arithmetic and status remapping against the fixtures during GREEN. Do not run `go mod tidy`.
  </action>
  <verify>
    <automated>go test ./testdata/golden/... -run TestGoldenParity -count=1 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `testdata/golden/golden_parity_test.go` exists in package `golden` and defines `TestGoldenParity`
    - It calls `resolveWeftCorpus(` and reuses `findVolatileKeys(`
    - The resolver references the pinned commit `f89ae3ea4e4c37509f7302fd4e37986212a72079`
    - When the corpus is absent, the test SKIPs (does not fail) with an actionable message
    - Commit message prefix `test(03-09):`
  </acceptance_criteria>
  <done>Parity test + corpus resolver + normalizers committed; skips cleanly without the corpus, exercises real shapes with it.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Reconcile normalizers until parity passes against the corpus</name>
  <read_first>
    - testdata/golden/golden_parity_test.go (the RED assertions from Task 1)
    - testdata/golden/corpus/weft-go/impact.json + callers.json + callees.json (cross-check the impact arithmetic)
    - internal/query/status.go (the status key-mapping table to align the normalizer to)
  </read_first>
  <files>testdata/golden/golden_parity_test.go</files>
  <action>
    With the weft corpus present at the pinned commit, run the parity test and reconcile each normalizer against reality: confirm the impact `nodeCount`/`edgeCount` semantics match impact.json (re-derive from callers.json/callees.json if the arithmetic disagrees — this closes RESEARCH Open Question 1), confirm the status normalizer's remap allowance matches 03-05's `StatusResult` shape (Open Question 2), and confirm the location-set comparison tolerates the D-05 edge-multiplicity divergence without masking a genuine mismatch. If a normalization reveals an actual engine-shape bug (not an expected divergence), record it as a finding for the affected engine plan rather than loosening the normalizer to hide it. Leave the seven-command parity assertions passing.
  </action>
  <verify>
    <automated>CODEGRAPH_WEFT_CORPUS="${CODEGRAPH_WEFT_CORPUS:-/Volumes/Code/github.com/seanb4t/weft}" go test ./testdata/golden/... -run 'TestGoldenParity|TestGoldenFixturesExist' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - With the corpus present, `go test ./testdata/golden/... -run TestGoldenParity -count=1` exits 0
    - `TestGoldenFixturesExist` still passes (existing invariant intact)
    - Every normalized divergence is documented in a comment (id ignore, edge multiplicity, status remap, no score)
    - Impact nodeCount/edgeCount confirmed against impact.json (Open Question 1 closed)
    - `go test ./...` green (full suite)
    - Commit message prefix `feat(03-09):` (or `test(03-09):` if only test code changed)
  </acceptance_criteria>
  <done>The Go engine's output shapes match the golden corpus for all seven captured tools after the documented normalizations; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → weft corpus | The corpus is a trusted, pinned local checkout; the resolver verifies the commit before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09-Repro | Tampering | corpus resolution | medium | mitigate | Verify the weft git HEAD equals the pinned commit before diffing; skip loudly (not silently pass) when absent, so a green run always means real parity or an explicit skip |
| T-03-09-Mask | Tampering | normalizer looseness | medium | mitigate | Normalizers strip only the documented D-05 divergences; a genuine shape mismatch is reported as a finding, not absorbed by widening a normalizer |
| T-03-SC | Tampering | package installs | low | accept | No new dependency; test-only code |
</threat_model>

<verification>
- With the corpus: `go test ./testdata/golden/... -count=1` green (parity + existing invariants)
- Without the corpus: the parity test SKIPs and `go test ./...` stays green
- Each D-05 divergence documented as an explicit normalizer
</verification>

<success_criteria>
MCP tool / CLI output shapes match TS CodeGraph v1.3.x, verified against the Phase-1 golden corpus for query/callers/callees/impact/status/explore/node with the documented divergences normalized — reproducibly, at the pinned weft commit, or an explicit skip.
</success_criteria>

<output>
Create `.planning/phases/03-query-engine-mcp-server/03-09-SUMMARY.md` when done.
</output>
