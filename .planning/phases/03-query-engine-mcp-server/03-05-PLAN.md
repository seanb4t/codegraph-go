---
phase: 03-query-engine-mcp-server
plan: 05
type: tdd
wave: 3
depends_on: ["03-02"]
files_modified:
  - internal/query/files.go
  - internal/query/status.go
  - internal/query/files_status_test.go
autonomous: true
requirements: [QRY-07, QRY-09]
must_haves:
  truths:
    - "files browses the indexed file structure from the graph (not a filesystem scan) with format/filter/pattern/depth options"
    - "status --json reports index health, file/node/edge counts, nodesByKind, and languages from the frozen graph"
    - "status TS-only keys are remapped to Go/Pebble-truthful values or dropped per an explicit per-key table (D-05)"
  artifacts:
    - "internal/query/files.go — Engine.Files + browse options + JSON shaping"
    - "internal/query/status.go — Engine.Status + the TS→Go key-mapping table + JSON shaping"
    - "internal/query/files_status_test.go — TestFiles / TestStatus"
  key_links:
    - "files reads reader.IterateFiles(); status reads IterateNodes()+IterateEdges()+GetMeta() (all from 03-01/existing)"
    - "status.languages/nodesByKind reflect Go-only extraction until Phase 5 (a Go repo reads [\"go\"])"
---

<objective>
Implement `Engine.Files` (QRY-07 — browse the indexed structure from the graph with format/filter/pattern/depth options) and `Engine.Status` (QRY-09 — `status --json` index health/counts). `files` reads `reader.IterateFiles()` (from 03-01) and never scans the filesystem. `status` computes `fileCount`/`nodeCount`/`edgeCount` from enumeration + `GetMeta()`, derives `nodesByKind` and `languages` from a node scan (Go-only until Phase 5), and remaps the golden's TS-SQLite-specific fields to Go/Pebble-truthful values via an explicit per-key table (D-05).

Purpose: These are the "index browse + health" commands. `files` has no golden fixture (D-07a) — assert structural correctness. `status` diffs against the golden `status.json` shape after the documented remapping. This plan resolves RESEARCH Open Question 2 (the concrete TS-key→Go-analog table) as a decision baked into the renderer.
Output: `Engine.Files`/`Engine.Status` + the status key-mapping table + JSON shaping, tested.

**Wave-3 test-file isolation (shared infra):** this plan runs in Wave 3 in parallel with 03-03 and 03-04, all in package `query`. It **reuses** the `copyFixture`/`indexFixture` helpers from `engine_test.go` (03-02 Wave-0 shared infrastructure) **at runtime only** and adds all its own test cases to its **own isolated file `internal/query/files_status_test.go`**. It MUST NOT edit `engine_test.go` (that would clobber under parallel execution) — `engine_test.go` is therefore intentionally absent from this plan's `files_modified`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-engine-mcp-server/03-PATTERNS.md
@internal/graphstore/store.go
@internal/schema/meta.go
@internal/query/engine.go
@testdata/golden/corpus/weft-go/status.json
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): TestFiles/TestStatus pin browse options + the status key-mapping table</name>
  <read_first>
    - internal/query/engine.go (Engine + OpenAt) and internal/query/search.go (Location, shaping approach)
    - internal/query/engine_test.go (copyFixture/indexFixture harness to REUSE at runtime — do NOT modify this file; add your cases to files_status_test.go)
    - internal/graphstore/store.go (IterateFiles/IterateNodes/IterateEdges/GetMeta)
    - internal/schema/meta.go (SchemaVersion, IsCurrentSchemaVersion — the Go analog for TS version/extraction fields)
    - internal/schema/graph.proto (File and Node fields; node.Kind values for nodesByKind; node.Language for languages)
    - testdata/golden/corpus/weft-go/status.json (the exact key set: backend/journalMode/nodesByKind/languages/pendingChanges/worktreeMismatch/index.*)
    - .planning/phases/03-query-engine-mcp-server/03-RESEARCH.md (§status.json field mapping; Open Question 2; Assumption A2)
  </read_first>
  <files>internal/query/files_status_test.go</files>
  <behavior>
    - TestFiles: Files returns the indexed files from the graph (matches what IterateFiles yields); a pattern/filter narrows the set; a depth option limits directory nesting; format option toggles the projection. Assert files come from the graph, not a live os.ReadDir of the repo.
    - TestStatus: Status reports initialized=true, non-zero fileCount/nodeCount/edgeCount consistent with the fixture graph, a nodesByKind map keyed by node.Kind, and languages == ["go"] for the Go fixture.
    - TestStatus remapping: `backend` renders a Pebble-truthful value (not "node-sqlite"); the TS version/extraction fields map to schema.SchemaVersion-derived values; Phase-4-only keys (pendingChanges/worktreeMismatch) render as inert zero/null placeholders (shape parity) per the documented table; no volatile keys (dbSizeBytes/lastIndexed/*_at) appear.
  </behavior>
  <action>
    Add all test cases to `internal/query/files_status_test.go` (package `query`) — this plan's own isolated test file — using the `copyFixture`/`indexFixture` helpers from `engine_test.go` at runtime (do NOT edit `engine_test.go`). Build the fixture graph via the harness. For `files`, assert the returned set equals the graph's file records and that pattern/filter/depth/format options behave. For `status`, assert the counts, nodesByKind, and `languages == ["go"]`, and pin the concrete remapping decisions: `backend` = a pebble identifier, version/extraction fields sourced from `schema.SchemaVersion`, `reindexRecommended` derived from `IsCurrentSchemaVersion`, and Phase-4-only keys present-but-inert. Encode this as the authoritative per-key expectation table in the test. RED — Files/Status do not exist yet.
  </action>
  <verify>
    <automated>go test ./internal/query/... -run 'TestFiles|TestStatus' -count=1 2>&1 | tail -5; echo "expect: undefined Files/Status (RED)"</automated>
  </verify>
  <acceptance_criteria>
    - `internal/query/files_status_test.go` references `e.Files(` and `e.Status(`
    - The status test pins `backend` to a non-SQLite value and `languages` to `["go"]`
    - RED confirmed on undefined methods
    - Commit message prefix `test(03-05):`
  </acceptance_criteria>
  <done>Failing TestFiles/TestStatus pin browse behavior and the concrete status remapping table.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement Engine.Files + Engine.Status with the D-05 status remapping</name>
  <read_first>
    - internal/query/files_status_test.go (RED expectations + the key table)
    - internal/schema/meta.go (SchemaVersion / IsCurrentSchemaVersion)
  </read_first>
  <files>internal/query/files.go, internal/query/status.go</files>
  <action>
    Implement `func (e *Engine) Files(opts FilesOptions) (...)` in `files.go`, reading `e.r.IterateFiles()` and applying the format/filter/pattern/depth options (QRY-07) entirely over the graph's file records — no `os.ReadDir`. Implement `func (e *Engine) Status() (StatusResult, error)` in `status.go`: count files via `IterateFiles`, nodes via `IterateNodes` (also building `nodesByKind` keyed by `node.Kind` and the distinct `languages` set from `node.Language`), edges via `IterateEdges("")` or `GetMeta()`, and populate the golden key set with the documented remapping — `backend` = a Pebble identifier, version/extraction fields from `schema.SchemaVersion`, `reindexRecommended` from `IsCurrentSchemaVersion(meta)`, and Phase-4-only `pendingChanges`/`worktreeMismatch` as inert zero/null placeholders. Colocate the `FilesOptions`/`StatusResult` structs + JSON shaping in these two files (no shared render_json.go). Document the per-key mapping decisions in a doc comment on `StatusResult`. Do not run `go mod tidy`.
  </action>
  <verify>
    <automated>go test ./internal/query/... -run 'TestFiles|TestStatus' -count=1 && go test ./internal/graphstore/archtest/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/query/... -run TestFiles -count=1` exits 0
    - `go test ./internal/query/... -run TestStatus -count=1` exits 0
    - `internal/query/status.go` contains `func (e *Engine) Status(` and a documented key-mapping comment
    - `internal/query/files.go` contains `func (e *Engine) Files(` reading `IterateFiles`
    - `go vet ./internal/query/...` clean
    - Commit message prefix `feat(03-05):`
  </acceptance_criteria>
  <done>files browses graph structure with options; status reports counts/health/nodesByKind/languages with the documented Go/Pebble remapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP → Files/Status | `files` pattern/filter/depth options are untrusted; both read only enumerated graph records |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-DoS | Denial of Service | files depth/filter | medium | mitigate | Bound the browse depth option and cap the returned set; reject absurd depths (reuse validate helpers) |
| T-03-05-Leak | Information Disclosure | status projectPath/indexPath | low | mitigate | Follow the golden convention of not echoing raw absolute host paths in agent-facing output; keep the key shape, normalize the value |
| T-03-SC | Tampering | package installs | low | accept | No dependency added here |
</threat_model>

<verification>
- `go test ./internal/query/... -run 'TestFiles|TestStatus' -count=1` green
- `files` reads from the graph, not the filesystem (asserted in test)
- Status key-mapping table documented in `status.go`
</verification>

<success_criteria>
`files` browses the indexed structure from the graph with format/filter/pattern/depth options, and `status --json` reports health/counts/nodesByKind/languages with TS-only fields remapped to Go/Pebble-truthful values per a documented table.
</success_criteria>

<output>
Create `.planning/phases/03-query-engine-mcp-server/03-05-SUMMARY.md` when done.
</output>
