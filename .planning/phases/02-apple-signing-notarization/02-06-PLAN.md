---
phase: 02-apple-signing-notarization
plan: 06
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/post-release-verify.yml
  - Taskfile.yml
  - internal/upgrade/release_workflow_shape_test.go
  - internal/upgrade/taskfile_shape_test.go
autonomous: true
requirements: [SIGN-01, SIGN-02]

user_setup:
  - service: github-actions-secrets
    why: "The notarize pipe reads its Apple credentials from the release job's environment (SIGN-01). Without these repository secrets the `enabled:` template evaluates false and the release ships un-notarized binaries with a green log."
    env_vars:
      - name: MACOS_SIGN_P12
        source: "GitHub -> repo Settings -> Secrets and variables -> Actions -> New repository secret. Value: the base64-encoded Developer ID Application .p12 (same value used for the local rehearsal in plan 02-04)."
      - name: MACOS_SIGN_PASSWORD
        source: "Same location. Value: the .p12 export password."
      - name: MACOS_NOTARY_ISSUER_ID
        source: "Same location. Value: the App Store Connect issuer id."
      - name: MACOS_NOTARY_KEY_ID
        source: "Same location. Value: the App Store Connect key id."
      - name: MACOS_NOTARY_KEY
        source: "Same location. Value: the base64-encoded App Store Connect .p8."
    dashboard_config:
      - task: "Create them as REPOSITORY secrets, not environment secrets — the release job declares no `environment:` and would not receive an environment-scoped value, which would fail silently as an un-notarized release"
        location: "GitHub -> repo Settings -> Secrets and variables -> Actions"
      - task: "Confirm no organization-level secret with the same name shadows or conflicts with these"
        location: "GitHub -> org Settings -> Secrets and variables -> Actions"

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The five Apple credentials are readable by exactly one job in exactly one workflow — the tag-triggered release job that already holds the OIDC write scope — and by no pull-request-triggerable workflow anywhere in the repository, asserted by a test rather than by inspection"
    - "The release workflow still declares exactly one job holding `id-token: write`, and its file name and tag trigger are unchanged, so the certificate subject the upgrade path pins still resolves"
    - "Each new post-release job carries the event-aware conclusion guard verbatim, so a manual re-verification dispatch runs the job instead of skipping it and reporting green"
    - "The Gatekeeper job assesses the RE-DOWNLOADED published asset with the quarantine attribute confirmed present, on both darwin architectures, and fails the workflow when the verdict is not accepted"
    - "The integration-suite job runs against the re-downloaded published binary and cannot silently fall back to a locally built one — a missing or unreadable download aborts the job before any test result is printed"
  artifacts:
    - ".github/workflows/release.yml — the five Apple credentials on the release step's environment"
    - ".github/workflows/post-release-verify.yml — a gatekeeper job and a notarized-suite job"
    - "Taskfile.yml target `verify:notarized-suite`"
    - "internal/upgrade/release_workflow_shape_test.go — TestAppleSecretsScopedToSingleReleaseJob"
  key_links:
    - "repository secrets → the release job's step environment → the notarize block's templates → quill — the only path by which the pipe becomes enabled"
    - "`needs.resolve-tag.outputs.tag` → `verify:gatekeeper` and `verify:notarized-suite` — both consume the validated tag, never the raw upstream branch field"
    - "the event-aware guard on each new job → whether a manual dispatch verifies anything at all"
---

<objective>
Wire the credentials into the one job allowed to hold them, and make the release's
own claims re-prove themselves after every publish.

Purpose: SIGN-01 requires the certificate and API key to be held as CI secrets.
SIGN-02's green criterion must be checked against the re-downloaded published
asset, and criterion 4's suite must run against the notarized binary itself. Both
belong in the existing post-release verification workflow, which already
re-downloads assets for its signature and attestation checks — reusing proven
machinery rather than lengthening the single unrehearsable publish path, and
catching a failure after publish where the recovery is patch-forward.

Three standing constraints govern every edit here, and each has already cost this
project something. Exactly one job may hold the OIDC write scope, and the workflow
file name and tag trigger are pinned by the upgrade path's certificate-subject
check. Granting that scope or these secrets to anything a pull request can trigger
is a security regression, not a fix. And a conclusion guard without its
event-aware disjunct makes every job skip silently and report green.

Output: the credentials in exactly one place, and two new verification jobs that
can actually fail.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-apple-signing-notarization/02-CONTEXT.md
@.planning/phases/02-apple-signing-notarization/02-PATTERNS.md
@.planning/phases/02-apple-signing-notarization/02-EVIDENCE.md
@.planning/phases/02-apple-signing-notarization/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apple credentials on the single OIDC-bearing release job, and a test that keeps them there</name>
  <read_first>
    - `.github/workflows/release.yml` in full — the locked-contract header explaining why the file name and tag trigger cannot change, the single `release` job's `permissions:` block and its inline reasoning, the tool-install steps, and the `Release` step whose environment currently carries one variable
    - `internal/upgrade/release_workflow_shape_test.go` — the existing workflow-parsing helpers and `TestOIDCWriteScopedToSingleGoreleaserJob`, whose structure the new secrets-scoping test mirrors
    - `internal/upgrade/verify.go` — the release-workflow reference pattern that anchors the certificate subject to this file path and tag ref, so the blast radius of a rename is concrete rather than abstract
    - `.github/workflows/darwin-toolchain-canary.yml` and `.github/workflows/linux-cross-canary.yml` — their triggers, to establish which workflows are pull-request-reachable
    - `.planning/phases/02-apple-signing-notarization/02-EVIDENCE.md` § "Assumption A5" — whether the rehearsal found secret material in error output, which decides whether an explicit masking step is required here
  </read_first>
  <files>.github/workflows/release.yml, internal/upgrade/release_workflow_shape_test.go</files>
  <action>
Add the five Apple credential variables to the existing `Release` step's
environment in the release job — the same job that already holds the OIDC and
attestation write scopes. Add no new job, no new permission, and no
`environment:` key. Do not touch the workflow's name, its file path, or its tag
trigger.

Two carried-forward constraints bind this edit absolutely. D-14: exactly one job
in this file may hold the OIDC write scope, and the certificate subject pattern in
the upgrade path's verifier anchors on this exact workflow file name and this exact
tag trigger — collapsing jobs is fine, renaming the file or changing the trigger is
not. D-17: granting that scope, or these credentials, to anything a pull request
can trigger is a security regression, not a fix.

Add a comment above the step's environment block recording three things: that
these five variables are what makes the notarize block's enabled template evaluate
true, that the template evaluating false here would ship un-notarized binaries
with a green run, and that no other workflow in this repository may reference
them. Point at the config block by name so a reader can follow the chain.

If the rehearsal recorded secret material appearing in the signing library's error
output, add an explicit masking step before the release step that registers each
value as a masked string, and cite the evidence section that made it necessary. If
the rehearsal found no leakage, state that in the comment instead — an absent
mitigation with a recorded reason is honest; an absent mitigation with no
explanation reads as an oversight.

Then add `TestAppleSecretsScopedToSingleReleaseJob` to
`internal/upgrade/release_workflow_shape_test.go`, mirroring the existing
OIDC-scoping test's structure. It must, over EVERY workflow file in
`.github/workflows/`:

- assert that a reference to any of the five credential names appears only within
  the release workflow, and within it only in the release job;
- assert that no workflow whose triggers include a pull-request event references
  any of them, and that no such workflow declares the OIDC write scope;
- assert that the release workflow still declares exactly one job holding the OIDC
  write scope, so this change cannot have widened it;
- enumerate the workflow directory at runtime rather than from a fixture list, so
  a newly added workflow is covered the day it lands rather than the day someone
  remembers to add it — and fail loudly if the directory scan finds zero files,
  which would make the whole test vacuous.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/ -run 'TestAppleSecretsScopedToSingleReleaseJob|TestOIDCWriteScopedToSingleGoreleaserJob|TestWorkflowRunBodiesInvokeTask' -v && task lint:actionlint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'MACOS_SIGN_P12' .github/workflows/` returns exactly 1, and that line is in `release.yml` inside the release job's `Release` step environment.
    - `rg -l 'MACOS_NOTARY_KEY|MACOS_SIGN_PASSWORD|MACOS_NOTARY_ISSUER_ID|MACOS_NOTARY_KEY_ID' .github/workflows/` lists exactly one file.
    - `rg -c 'id-token: write' .github/workflows/release.yml` returns 1 — unchanged.
    - `git diff .github/workflows/release.yml` shows no change to the `name:`, `on:` or file path, and no new job.
    - `TestAppleSecretsScopedToSingleReleaseJob` passes and is proven non-vacuous: temporarily add one credential reference to a pull-request-triggerable workflow, confirm the test goes red, revert, and record both outcomes in the summary.
    - The actionlint target (whichever name `task --list-all` reports) exits 0.
    - `task test:unit` passes.
  </acceptance_criteria>
  <done>The credentials exist in exactly one job, a runtime-enumerating test keeps them there, and the test has been observed failing when the rule is broken.</done>
</task>

<task type="auto">
  <name>Task 2: The post-release Gatekeeper job — the same gate that was shown RED, now expected green</name>
  <read_first>
    - `.github/workflows/post-release-verify.yml` in full — the header explaining the trigger choice and the event-aware guard, the `resolve-tag` job and its validated output, the `verify-supply-chain` job skeleton, and the `self-upgrade` job's matrix and its deliberate independence comment
    - `Taskfile.yml`'s `verify:gatekeeper` as committed by plan 02-01 — the exact input variables and expected-verdict parameter the new job must supply
    - `.planning/phases/02-apple-signing-notarization/02-EVIDENCE.md` — the RED baseline and the Open Question 1 verdict, which decide whether the distribution-policy check is asserted or informational in CI
    - `.planning/phases/02-apple-signing-notarization/02-PATTERNS.md` § "post-release-verify.yml" — the verbatim guard line and the job skeleton to mirror
  </read_first>
  <files>.github/workflows/post-release-verify.yml</files>
  <action>
Add one job to `post-release-verify.yml` that runs the Gatekeeper gate against the
published assets, and add nothing else to that file in this task.

Shape it on the existing `self-upgrade` job: depends on the tag-resolution job
only; a matrix over both darwin architectures with the macOS runner class, and
`fail-fast: false` so one architecture's failure does not hide the other's;
checkout, Go setup and the Task install action, in the established order and with
the existing pinned action commits.

The conclusion guard must be the same disjunct that appears on every other job in
this file, character for character — this is D-11's first half, and it applies to
every job added here. Do not simplify it. The bare form evaluates
false under a manual dispatch because the upstream-run object is null, which makes
every job skip and the workflow report a clean green run while verifying nothing —
the failure this file's header was written to prevent.

The step invokes `task verify:gatekeeper` with the validated tag output, the
repository, the platform pair from the matrix, a workflow token, and the expected
verdict set to accepted. Consume the tag only from the resolution job's output —
never the upstream run's branch field and never the dispatch input directly.

Declare the job independent of the supply-chain job, and record why in a comment
mirroring the existing one: a broken Gatekeeper result and a broken signature are
independent evidence, and both should report on a bad release so neither masks the
other.

Two release-walking hazards to handle explicitly, both already burned in:

- D-11's second half: the zero-asset release is permanent and the un-notarized
  baseline release is permanent. This job resolves its asset by the validated tag
  rather than by walking the release list, so neither is reachable by accident —
  but say so in a comment, because the next person to add a job here will be
  tempted to walk, and the existing self-upgrade target's zero-asset filter is the
  precedent to follow rather than re-derive.
- The gate must not be able to pass by skipping. If the target reports that it
  could not obtain a verdict, that is a failure, not a skip. Confirm that
  `verify:gatekeeper` already treats an unclassifiable verdict as fatal; if plan
  02-01's implementation left any path that exits zero without a classified
  verdict, close it here and note the change.
  </action>
  <verify>
    <automated>task lint:actionlint && go test ./internal/upgrade/ -run 'TestWorkflowRunBodiesInvokeTask|TestOIDCWriteScopedToSingleGoreleaserJob' -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "github.event_name != 'workflow_run' \|\| github.event.workflow_run.conclusion == 'success'" .github/workflows/post-release-verify.yml` returns one more than at HEAD, and every job in the file has exactly one such line.
    - The new job's `runs-on` resolves to the macOS runner class used by the existing self-upgrade matrix leg, and its matrix names both darwin architectures with `fail-fast: false`.
    - The new job's step environment supplies `GATEKEEPER_EXPECT` set to the accepted verdict and takes its tag from `needs.resolve-tag.outputs.tag`; `rg -c 'head_branch' .github/workflows/post-release-verify.yml` is unchanged from HEAD, i.e. the new job did not reintroduce a raw branch read.
    - `rg -c 'permissions:' .github/workflows/post-release-verify.yml` is unchanged and the file still grants no write scope of any kind — `rg -c 'write' .github/workflows/post-release-verify.yml` returns 0.
    - The actionlint target exits 0.
    - A dry evaluation of the guard is recorded in the summary: state, for each of the two trigger events, whether the new job runs — and confirm that under a manual dispatch it runs rather than skips.
  </acceptance_criteria>
  <done>The Gatekeeper gate re-fires against the published assets on every release and on every manual re-verification, on both darwin architectures, and cannot pass by skipping.</done>
</task>

<task type="auto">
  <name>Task 3: The integration suite against the notarized published binary</name>
  <read_first>
    - `Taskfile.yml` lines 924-1119 (`verify:release-assets`) — the download-into-a-temp-dir pattern, the two-temp-dirs discipline and the reason for it, and the precondition set to mirror
    - `test/integration/main_test.go` and `test/wireoracle/main_test.go` as committed by plan 02-03 — the override variable name, the absolute-path requirement, and the abort-on-bad-path contract
    - `.planning/phases/02-apple-signing-notarization/02-03-SUMMARY.md` — the recorded verdict on whether the wireoracle suite is usable against a published release binary, which decides this job's scope
    - `.github/workflows/post-release-verify.yml` including the job added in Task 2 — the skeleton and guard to mirror
    - `internal/upgrade/taskfile_shape_test.go` — the shape-test idiom for the new target's preconditions
  </read_first>
  <files>Taskfile.yml, internal/upgrade/taskfile_shape_test.go, .github/workflows/post-release-verify.yml</files>
  <action>
Add a `verify:notarized-suite` Taskfile target and the post-release job that calls
it. Keeping the body in the Taskfile rather than inline in the workflow follows
this repository's single-definition rule for CI job bodies.

The target: preconditions on the tag, repository, platform pair and token,
matching the style `verify:release-assets` already uses; tool preconditions on the
release CLI and the JSON processor. Body, in order: download the raw
per-platform asset for the resolved tag into a temp dir cleaned by a trap; make it
executable, since a downloaded asset does not carry the mode bit; record its
sha256 and print it on a labelled line so this observation can be joined to the
byte-identity evidence; then run the integration suite with the binary-override
variable pointed at the ABSOLUTE path of the downloaded file, with test caching
disabled so a cached result cannot stand in for a real run.

Scope by what plan 02-03 measured. If it recorded the wireoracle suite as usable
against a published binary, run it too, in the same target, against the same
file. If it recorded a transcript field that differs between a locally built and a
published binary, run only the integration suite and put the recorded reason in
the target's description — a stated scope, never a silent omission.

Assert the run's honesty, not just its exit code. Before the suite runs, confirm
the downloaded file exists and is executable and hard-fail by name if not; after
it runs, confirm the suite actually executed tests rather than reporting no test
files. A green result from a suite that ran nothing is the failure mode this whole
phase is organised against.

Add `TestVerifyNotarizedSuiteDeclaresNamedPreconditions` to
`internal/upgrade/taskfile_shape_test.go`, mirroring the two shape tests already
added in this phase.

The job: same skeleton and same verbatim conclusion guard as Task 2's (D-11);
depends on the tag-resolution job only; runs on the macOS runner class, because the point is
to execute a hardened-runtime darwin binary on real macOS — a linux runner would
make the job green and meaningless. Matrix over darwin architectures only if the
runner class can execute both; if it cannot execute the non-native architecture,
scope the job to the native one and record that limitation in a comment rather
than pretending to cover both.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/ -run 'TestVerifyNotarizedSuiteDeclaresNamedPreconditions|TestVerifyGatekeeperDeclaresNamedPreconditions|TestRehearseNotarizeDeclaresCredentialPreconditions' -v && task lint:actionlint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '^  verify:notarized-suite:' Taskfile.yml` returns exactly one line, and the target declares named preconditions for the tag, repository, platform pair and token, each with a non-empty message.
    - `TestVerifyNotarizedSuiteDeclaresNamedPreconditions` passes and fails when the target is removed.
    - The target passes an ABSOLUTE path in the binary-override variable, and disables test caching on the suite invocation; `rg -c 'count=1' Taskfile.yml` reflects the addition.
    - The target hard-fails by name when the downloaded file is missing or not executable, on a path reachable before the suite runs.
    - The target verifies the suite executed tests rather than reporting no test files, and fails if it did not.
    - The new job carries the verbatim event-aware guard, runs on the macOS runner class, and takes its tag from the resolution job's output.
    - The target's description states, in one clause, whether the wireoracle suite is in scope and the recorded reason.
    - The actionlint target exits 0; `task test:unit` passes.
  </acceptance_criteria>
  <done>A CI job downloads the published notarized binary and runs the real suite against it, on real macOS, and cannot report green without having executed tests against that file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repository secrets → the release job's process environment | An Apple code-signing private key and an App Store Connect API key become readable by every step and every action inside that job |
| Pull-request-triggerable workflows → the same secret namespace | A fork-controllable trigger must never reach these values or the OIDC write scope |
| Post-release job results → the project's public verification claims | These jobs are what convert a documented claim into a re-proved one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Information Disclosure | Apple credentials reachable from a pull-request trigger | critical | mitigate | The credentials are added to exactly one step of one job in the tag-triggered workflow, with no `environment:` key and no new job. `TestAppleSecretsScopedToSingleReleaseJob` enumerates the workflow directory at runtime and fails if any credential name appears outside that job or in any pull-request-triggerable workflow — and the test is proven non-vacuous by observing it red against a deliberate violation. Reinforced by the standing rule that granting these to a pull-request-reachable workflow is a regression, not a fix. |
| T-02-17 | Elevation of Privilege | The OIDC write scope | high | mitigate | A second job holding that scope, or a renamed workflow file or changed tag trigger, silently breaks the certificate subject the upgrade path pins — and does so on a genuine, correctly-published release. Mitigated by adding no job and no permission, by an acceptance criterion diffing the workflow name, trigger and job set, and by the existing single-scope test remaining green. |
| T-02-18 | Repudiation | The conclusion guard on the two new jobs | high | mitigate | Under a manual dispatch the upstream-run object is null, so a bare conclusion test makes the job skip and the workflow report green while verifying nothing — and the retained historical re-verification trigger would be dead from the day it shipped. Mitigated by requiring the event-aware disjunct verbatim on both new jobs, by a count assertion over the file, and by a recorded dry evaluation of the guard under both trigger events. |
| T-02-19 | Tampering | The downloaded asset the suite executes | medium | mitigate | The job makes a network-fetched binary executable and runs it on a CI runner. The same workflow independently verifies that asset's Sigstore signature and build attestation against the same published release, so a tampered asset fails there; this job additionally binds its own run to a recorded sha256 so the executed bytes are identifiable after the fact. No second signature implementation is added inside the test harness — that would drift from the upgrade path's own verifier. |

**Canon referral (not minted as a bespoke prohibition):** CI secret scoping,
least-privilege workflow permissions and fork-trigger exposure are canon —
covered by `/gsd-secure-phase` and by the `T-02-16` / `T-02-17` register rows.
Breadcrumb only; no bespoke prohibition minted.
</threat_model>

<artifacts_this_phase_produces>
Created by this plan (new symbols — not drift):

- GitHub Actions repository secrets consumed by `release.yml`: `MACOS_SIGN_P12`, `MACOS_SIGN_PASSWORD`, `MACOS_NOTARY_ISSUER_ID`, `MACOS_NOTARY_KEY_ID`, `MACOS_NOTARY_KEY`
- `post-release-verify.yml` job ids: `gatekeeper`, `notarized-suite`
- Taskfile target: `verify:notarized-suite`
- Go test functions: `TestAppleSecretsScopedToSingleReleaseJob` (`internal/upgrade/release_workflow_shape_test.go`), `TestVerifyNotarizedSuiteDeclaresNamedPreconditions` (`internal/upgrade/taskfile_shape_test.go`)
</artifacts_this_phase_produces>

<verification>
- `go test ./internal/upgrade/ -v` passes, including the three Taskfile shape tests and both workflow-scoping tests.
- The actionlint target exits 0.
- Exactly one workflow file references any Apple credential name; exactly one job holds the OIDC write scope.
- Every job in `post-release-verify.yml`, including both new ones, carries the event-aware conclusion guard.
- The secrets-scoping test has been observed red against a deliberate violation and green after revert.
</verification>

<success_criteria>
SIGN-01's CI half is in place and provably contained, and the two verification
jobs that will produce ROADMAP Phase 2 criteria 2 and 4 exist, run on real macOS,
consume the validated tag, and cannot report green by skipping. Whether they
actually pass is plan 02-07's evidence.
</success_criteria>

<output>
Create `.planning/phases/02-apple-signing-notarization/02-06-SUMMARY.md` when done
</output>
