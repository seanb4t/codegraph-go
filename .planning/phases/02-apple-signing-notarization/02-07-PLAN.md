---
phase: 02-apple-signing-notarization
plan: 07
type: execute
wave: 5
depends_on: ["02-05", "02-06"]
files_modified:
  - Taskfile.yml
  - .github/workflows/release.yml
  - docs/RELEASE.md
  - .planning/phases/02-apple-signing-notarization/02-EVIDENCE.md
autonomous: false
requirements: [SIGN-01, SIGN-02, SIGN-03, SIGN-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`spctl -a -vv -t exec` on the notarized, PUBLISHED darwin asset — quarantine attribute confirmed present via `xattr -p` immediately before the run — reports accepted with the notarized Developer ID source, on both darwin architectures"
    - "For each darwin binary, four sha256 values are equal: the value recorded in the release job immediately after the notarize pipe, the value of the re-downloaded published asset, the subject the cosign bundle verifies against, and the subject the build attestation verifies against"
    - "The full CLI (and, where in scope, MCP) integration suite runs green against the re-downloaded published notarized binary itself — a hardened-runtime load failure would surface here as a test failure rather than as a user's first-run crash"
    - "The RED baseline recorded in plan 02-01 and this GREEN result are the same command, against the same kind of asset, differing only in which release they name — so the green result is trustworthy for the reason the red one was"
    - "Every claim in `docs/RELEASE.md` previously marked pending is now either confirmed against this release or still marked pending with a stated reason"
  artifacts:
    - "Taskfile.yml — post-notarize hash recording invoked from the release job"
    - ".planning/phases/02-apple-signing-notarization/02-EVIDENCE.md — GREEN Gatekeeper section, four-point byte-identity section, and criterion-4 suite section"
    - "docs/RELEASE.md with no unresolved pending markers left unexplained"
  key_links:
    - "the release job's post-notarize hash record → the re-downloaded asset's hash → the cosign subject → the attestation subject — the four-point chain criterion 3 is made of"
    - "the published release → `verify:gatekeeper` with the accepted verdict expected → the phase's central claim"
    - "the published release → `verify:notarized-suite` → criterion 4"
  prohibitions:
    - requirement_id: SIGN-04
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT record a Gatekeeper verdict, a notarization outcome, or a byte-identity comparison as evidence unless it was observed against the RE-DOWNLOADED published asset — a local build-output copy, a rehearsal artifact, or a green job log must never be presented as, or substituted for, the shipped bytes"
---

<objective>
Cut the release, then make it prove every claim this phase has been building
toward — against the bytes a user actually downloads.

Purpose: everything before this plan is mechanism and rehearsal. ROADMAP Phase 2
criteria 2, 3 and 4 are all statements about a published release, and none of them
can be observed anywhere else. Criterion 2 needs the notarized published asset
assessed with a confirmed quarantine attribute. Criterion 3 needs four sha256
values to agree, one of which only exists after the release job runs and another
of which only exists after a re-download. Criterion 4 needs the real suite against
the real binary.

The two rules that shape how this plan behaves when something goes wrong. D-13:
release-please is the sole tag authority — no hand-created version tag, because
such a tag would match the release workflow's own trigger and falsely fire the
pipeline. D-12: a published release or tag is never deleted or re-pushed —
recovery is a new version. So a failure here is recorded and patched forward, not
erased.

D-05 governs how the byte-identity claim is proven: four sha256 values, one of
which must come from the RE-DOWNLOADED published asset. Re-downloading is not
optional; the prior phase's verifiers produced three false negatives, one of them
precisely because a verifier never downloaded the artifact it then read. D-02
governs the Gatekeeper half: the assessment and distribution-policy commands are
the oracle, never the notarization tool's own success report.

Output: the phase's central claim, measured, with the evidence a third party could
re-run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-apple-signing-notarization/02-CONTEXT.md
@.planning/phases/02-apple-signing-notarization/02-EVIDENCE.md
@.planning/phases/02-apple-signing-notarization/02-04-SUMMARY.md
@.planning/phases/02-apple-signing-notarization/02-06-SUMMARY.md
@docs/RELEASE-PROCEDURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the first byte-identity comparison point observable, and run the pre-flight</name>
  <read_first>
    - `Taskfile.yml`'s `release:goreleaser` in full — the precondition set, the prefer-installed-then-build algorithm, the pinned-version assertion, and the reason the version resolution uses a field-extracting text tool rather than a template flag (the task runner consumes template braces before the shell sees them; that exact defect published a release with zero assets)
    - `Taskfile.yml`'s `release:rehearse-notarize` as committed by plan 02-04 — the artifact-metadata reader with its double-count filter, and the evidence-line format to reuse verbatim so rehearsal and release output are directly comparable
    - `.github/workflows/release.yml` as committed by plan 02-06 — the step order, and the attestation step that must remain last
    - `docs/RELEASE-PROCEDURES.md` — the release runbook this plan's checkpoint follows
    - `.planning/phases/02-apple-signing-notarization/02-EVIDENCE.md` — every section recorded so far, since the pre-flight is a check that each precondition of the release still holds
  </read_first>
  <files>Taskfile.yml, .github/workflows/release.yml</files>
  <action>
Criterion 3's first comparison point — the hash immediately after the notarize
pipe — does not exist anywhere observable today. Nothing in the release path prints
it, and the release job does not publish its artifact metadata. Make it
observable, additively and read-only.

Add a `release:record-notarize-hashes` Taskfile target that reads
`dist/artifacts.json` and prints one `SIGN04-PUBLISH-EVIDENCE` line per darwin
platform carrying the platform pair, the artifact name, and the recomputed sha256
of the file the metadata names. Reuse plan 02-04's reader verbatim, including the
binary-format filter that stops it counting eight artifacts where four exist. The
target writes nothing, uploads nothing, and must not be able to fail the release:
if the metadata file is absent or a platform is missing, print a labelled
NOT-FOUND line and exit zero, because a recording step that can break a publish is
a worse trade than a missing observation. Say that in a comment.

Invoke it from `release.yml` as its own step, placed after the release step and
before the attestation step. It reads only from the working directory and needs no
new permission and no secret.

Then run the pre-flight and record its result in the plan summary — this is the
last point at which anything can be corrected cheaply:

- every automated gate from plans 02-01 through 02-06 green, named individually
  rather than as a single suite invocation;
- the working tree clean and the config file byte-identical to what plan 02-04's
  rehearsal measured — if it has changed since, the rehearsal evidence describes a
  different configuration and must be re-run before proceeding;
- the notarize block's enabled template confirmed to resolve true under the five
  credential variables and false without them, per the test from plan 02-02;
- `docs/RELEASE.md`'s pending markers enumerated, so Task 3 knows exactly which
  ones this release is expected to close.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/ -v && task check:goreleaser && task lint:actionlint && task test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '^  release:record-notarize-hashes:' Taskfile.yml` returns exactly one line, and the target contains no path that exits non-zero.
    - `rg -c 'SIGN04-PUBLISH-EVIDENCE' Taskfile.yml` returns at least 2 — the found and not-found paths both emit a labelled line.
    - `.github/workflows/release.yml` invokes the target in a step positioned after the release step and before the attestation step; `git diff` shows no new permission, no new secret reference, and no change to the workflow name, trigger or job set.
    - The recording target is exercised locally before the release: run a snapshot release, then the target, and paste the resulting evidence lines into the summary — proving the step will print something rather than silently no-op on the day it matters.
    - The pre-flight report in the summary lists every gate by name with its result, states the config file's hash against what plan 02-04 measured, and enumerates the pending markers currently in `docs/RELEASE.md`.
    - `go test ./internal/upgrade/ -v`, the config-check target, the actionlint target and the unit suite all exit 0.
  </acceptance_criteria>
  <done>The first of criterion 3's four comparison points will be printed by the release job, the recording step cannot break a publish, and every gate is green before anything irreversible happens.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cut the release and observe every claim it must now prove</name>
  <what-built>
    The complete notarization pipeline: darwin binaries Developer-ID-signed and
    notarized inside the release run, cosign signing the post-notarization bytes,
    and two post-release jobs that re-download the published assets to check
    Gatekeeper's verdict and run the real test suite against them.

    Nothing has been published yet. This checkpoint is the release itself and the
    observation of its aftermath.
  </what-built>
  <how-to-verify>
**Before cutting anything.**

1. Confirm the five repository secrets exist. Claude cannot read them, and their
   absence is silent — the notarize block's enabled template resolves false, the
   pipe skips, and the release ships un-notarized with a green log. Run
   `gh secret list --repo seanb4t/codegraph-go` and paste the five names (values
   are not shown, which is correct). If any is missing, stop here.

**Cutting the release.**

2. Merge the open release-please pull request. Do NOT create a tag by hand — the
   release automation is the sole tag authority, and a hand-made version tag would
   match the release workflow's own trigger and fire the pipeline falsely.
3. Watch the release workflow run to completion. Paste: the run URL, the
   notarization pipe's log lines (they should name both darwin build ids and show
   a submission to Apple, not a skip), and both `SIGN04-PUBLISH-EVIDENCE` lines.
   WATCH FOR the specific silent failure this phase exists to prevent: a green run
   whose logs show the notarize pipe skipping because nothing matched its id
   filter. If you see a skip, the release has shipped un-notarized — say so
   plainly; do not delete the release, and do not re-push the tag.

**Observing the aftermath.**

4. Watch the post-release verification workflow. Paste, for each job: its
   conclusion, and for the Gatekeeper job every `GATEKEEPER-EVIDENCE` line for
   both darwin architectures.
   EXPECTED: accepted, with the notarized Developer ID source named, and the
   quarantine attribute confirmed present before each assessment.
5. Paste the notarized-suite job's result, including the line showing how many
   tests ran. A green job that ran zero tests is a failure, not a pass.
6. Manually dispatch the post-release verification workflow against the same tag.
   EXPECTED: every job RUNS — none skip. This is the guard check; a workflow that
   reports all-green having skipped everything is the exact failure this file's
   header was written against. Paste each job's conclusion.

**Independent confirmation, from your own machine.**

7. Follow the reproduction commands in `docs/RELEASE.md` verbatim, as a reader
   would, against the new tag's darwin arm64 asset. Paste every command and its
   output. If any command in the document does not work as written, that is a
   documentation defect to report here, not to work around.
8. Download the same asset through a real browser, and confirm it launches without
   a Gatekeeper block — the actual user experience this phase is about. State
   whether any dialog appeared.
  </how-to-verify>
  <resume-signal>Paste the secret list, the release run evidence, all four post-release observations, the manual-dispatch conclusions and your own reproduction output, then type "approved" — or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record criteria 2, 3 and 4 against the published release and close the pending claims</name>
  <read_first>
    - `.planning/phases/02-apple-signing-notarization/02-EVIDENCE.md` — every existing section; this task appends and cross-references rather than restating
    - The checkpoint output from Task 2 — the sole source of truth for every value recorded here
    - `docs/RELEASE.md` — the pending markers enumerated by Task 1's pre-flight, each of which this task either closes or re-justifies
    - `Taskfile.yml`'s `verify:release-assets` — the cosign and attestation commands whose success is what binds the signed and attested subjects to the re-downloaded bytes
    - `.planning/phases/02-apple-signing-notarization/02-RESEARCH.md` § "Assumptions Log" A3 — the last assumption this phase can close, and the only place it can be closed
  </read_first>
  <files>.planning/phases/02-apple-signing-notarization/02-EVIDENCE.md, docs/RELEASE.md</files>
  <action>
Append three sections to `02-EVIDENCE.md`, then reconcile the documentation.

`## SIGN-02 — GREEN Gatekeeper verdict on the published release`: the tag; for
each darwin architecture the published asset name, the quarantine attribute
written and its read-back, the verbatim assessment output including the source
string, the distribution-policy check output, and the full evidence line. Place a
short table immediately beneath it putting the plan 02-01 RED observation and this
GREEN observation side by side, and state in one sentence why the pair is
trustworthy: the same target, the same procedure, differing only in which release
it names.

`## SIGN-04 — four-point byte identity on the published release`: per darwin
architecture, the four sha256 values with their provenance named — the value the
release job recorded after the notarize pipe; the value recomputed from the
re-downloaded published asset; the subject the cosign bundle verified against; and
the subject the build attestation verified against. Where a verification command
proves the binding rather than printing a hash, say so explicitly and quote the
command and its result, rather than implying a value was read that was not.
Then state the verdict on assumption A3 — whether any pipe between the
release-scoped sign pipe and publish re-mutated a binary — which the four values
agreeing now settles, and which plan 02-04 could not settle because publish was
never reached. If they do not all agree, record the divergent pair, name which
stage introduced it, and state the patch-forward recovery rather than proposing to
delete anything.

`## Criterion 4 — the suite against the notarized binary`: the job result, the
number of tests executed, the sha256 of the binary they ran against joined to the
value in the byte-identity section, and the scope statement carried from plan
02-03 about whether the wireoracle suite was included and why. State explicitly
that a hardened-runtime load failure would have surfaced here, and that it did
not.

Also record the outcome of the manual-dispatch guard check and the maintainer's
own browser-download launch observation — the latter is the only place in this
phase where the actual user experience, rather than a proxy for it, is recorded.

Then reconcile `docs/RELEASE.md`: close every pending marker this release settles,
citing the evidence section that settles it; leave any marker this release did not
settle in place with an added sentence naming what would settle it. Do not close a
marker by deleting it without a citation.

Finally, if the release shipped un-notarized or any criterion failed, do not
adjust a criterion to match the outcome. Record the failure, record the
patch-forward plan, and leave the requirement open — the whole value of this
phase's discipline is that a gate is allowed to say no.
  </action>
  <verify>
    <automated>rg -c 'SIGN04-PUBLISH-EVIDENCE|GATEKEEPER-EVIDENCE' .planning/phases/02-apple-signing-notarization/02-EVIDENCE.md && go test ./internal/upgrade/ -v && task test:unit</automated>
  </verify>
  <acceptance_criteria>
    - The GREEN section records, for both darwin architectures, an assessment output containing the accepted verdict and the notarized Developer ID source string, each preceded by its quarantine attribute read-back.
    - The RED-versus-GREEN table names both releases and both verdicts, and states that the same target produced them.
    - The byte-identity section records, per darwin architecture, four provenance-labelled entries; where the entry is a verification result rather than a printed hash, the command and its result are quoted rather than a value implied.
    - Every recorded sha256 is a 64-character hex string, and for each darwin architecture the values that are printed hashes are IDENTICAL to one another — or, if not, the divergence is recorded with the stage that introduced it and a patch-forward plan.
    - The evidence file carries an explicit verdict string for assumption A3.
    - The criterion-4 section records a non-zero executed-test count.
    - The manual-dispatch guard check is recorded with a per-job conclusion showing no job skipped.
    - `rg -c 'pending' docs/RELEASE.md` is reported in the summary, and every remaining occurrence is justified in the summary by what would settle it.
    - No published release or tag was deleted or re-pushed at any point: `gh release list --repo seanb4t/codegraph-go --limit 10` still lists every release named in `docs/RELEASE-PROCEDURES.md` §7.1, including the zero-asset one and the un-notarized baseline.
  </acceptance_criteria>
  <done>Criteria 2, 3 and 4 are recorded against the published release with named provenance, assumption A3 carries a verdict, and the documentation's pending claims are each closed with a citation or re-justified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The release run → the permanently-published artifact set | This is the irreversible boundary; nothing published here can be withdrawn, only superseded |
| Published assets → every downstream consumer | The Homebrew cask in the next phase, the upgrade path, and browser downloaders all consume exactly these bytes |
| Recorded evidence → the phase's completion claim | If the evidence describes anything other than the published bytes, the claim is false regardless of how green the jobs were |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-20 | Tampering | A published release that silently shipped un-notarized | critical | mitigate | The notarize pipe treats an empty id filter as a non-fatal skip, so an id or template mistake ships un-notarized binaries behind a green run. Mitigated by three independent observers: the checkpoint requires reading the pipe's own log lines for a submission rather than a skip; the Gatekeeper job independently assesses the published asset and fails on a non-accepted verdict; and the maintainer's own browser download in checkpoint step 8 is the unproxied user experience. Recovery is patch-forward — a new version — never deletion. |
| T-02-21 | Repudiation | Evidence recorded from a source other than the published asset | high | mitigate | Presenting a rehearsal artifact or a green job log as the shipped bytes would make the phase's completion claim unfounded while appearing complete — the failure mode that produced three false negatives in the prior phase. Mitigated by the plan-level prohibition, by requiring provenance labels on all four byte-identity entries, by requiring quoted commands where a verification result stands in for a hash, and by an acceptance criterion asserting no published release was deleted or re-pushed. |
</threat_model>

<artifacts_this_phase_produces>
Created by this plan (new symbols — not drift):

- Taskfile target: `release:record-notarize-hashes`
- Evidence line prefix: `SIGN04-PUBLISH-EVIDENCE`
- `release.yml` step: the post-notarize hash recording step, positioned between the release step and the attestation step
- `02-EVIDENCE.md` sections: `## SIGN-02 — GREEN Gatekeeper verdict on the published release`, `## SIGN-04 — four-point byte identity on the published release`, `## Criterion 4 — the suite against the notarized binary`
</artifacts_this_phase_produces>

<verification>
- `go test ./internal/upgrade/ -v`, the config-check target, the actionlint target and the unit suite all pass before the release is cut.
- The published release's Gatekeeper verdict is accepted with the notarized Developer ID source, on both darwin architectures, with the quarantine attribute confirmed each time.
- The four byte-identity entries agree per darwin architecture, with named provenance.
- The integration suite ran a non-zero number of tests against the re-downloaded published binary and passed.
- A manual dispatch of the post-release workflow runs every job rather than skipping.
- No release or tag was deleted or re-pushed.
</verification>

<success_criteria>
ROADMAP Phase 2 criteria 2, 3 and 4 are satisfied and recorded — and together with
criterion 1 (plan 02-01) and criterion 5 (plan 02-05), the phase goal holds: a
macOS user who downloads a release asset in a browser can run it without
Gatekeeper blocking them, and the project can prove that claim with a check it has
already watched fail.
</success_criteria>

<output>
Create `.planning/phases/02-apple-signing-notarization/02-07-SUMMARY.md` when done
</output>
