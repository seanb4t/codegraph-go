---
phase: 04-codegraph-upgrade-homebrew
plan: 06
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - .planning/phases/04-codegraph-upgrade-homebrew/04-EVIDENCE.md
autonomous: true
requirements: [UPGR-01, UPGR-02, UPGR-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Detection fires against a genuinely Homebrew-authored install tree produced by `brew install` from the real seanb4t/tap, not a constructed fixture (the ROADMAP Depends-on clause's whole point)."
    - "The real install's resolved binary path, install directory, cask token, version and receipt path are recorded verbatim, so a future reader can check the shape rather than trust it."
    - "A codegraph binary carrying this phase's detection code, executed from inside that genuine tree, exits non-zero on bare `upgrade` and exits zero on `upgrade --check`, with both messages captured verbatim."
    - "The machine is left in the state it started in — no codegraph cask, tap, binary or man page left behind that was not there before, AND nothing removed that this run did not create — restored by an EXIT trap installed before the first mutating byte, and proved by a receipt carrying equal before/after payload hashes, `RESTORE_VERDICT=ok`, `RESTORE_INVOCATIONS=1` and a `TAP_ACTION` that agrees with Task 1's recorded `TAP_PREEXISTING`, rather than by an absence check."
    - "The tap is untapped ONLY if this run added it. A maintainer who already had `seanb4t/tap` keeps it (review cycle 2, HIGH: the cycle-2 trap untapped unconditionally, which both destroyed pre-existing maintainer state and made Task 2's own \"four probes match baseline\" criterion fail by construction)."
    - "The restoration function is idempotent — a trapped INT or TERM disarms the trap, runs cleanup once, and exits, so a second invocation can never report a confusing second verdict (review cycle 2, MEDIUM)."
    - "The run refuses to start at all if a codegraph cask is already installed, because an arbitrary prior cask version is not reliably restorable from a tap that publishes only its current cask (review cycle 1, HIGH; matches the repository's own rehearsal posture at `Taskfile.yml:1671`)."
    - "The run refuses to start at all if ANY `codegraph*.1` man page already exists in the shared Homebrew man directory, because the cask's uninstall hook (`.goreleaser.yaml:621`) is an unconditional `FileUtils.rm_f` glob over that SHARED directory and would delete pages this run never created (review cycle 2, HIGH; `03-EVIDENCE.md` records orphaned `codegraph*.1` pages as a real prior state on this project, and the cask precondition excludes only a *cask*, not orphaned pages)."
    - "The one thing not proved — a released binary that itself carries this code, installed by brew through the fully natural path — is named as a residual gap with its closing condition, never claimed."
  artifacts:
    - .planning/phases/04-codegraph-upgrade-homebrew/04-EVIDENCE.md
  key_links:
    - "TestDetectBrewManaged_RealInstall (plan 04-01) is the harness leg 1 drives; it aborts by name when pointed at a bad path rather than skipping, which is what makes a green result meaningful here."
    - "The three unresolved spec-less-probe rows recorded in 04-01-PLAN.md are closed by this plan's observations, or carried forward explicitly if an observation does not close them."
  prohibitions:
    - "MUST NOT present the payload-swapped observation as equivalent to a naturally-installed release binary; the two have different evidentiary status and this evidence file must say which is which."
    - "MUST NOT leave a mutated Caskroom, an installed cask, or an added tap behind on the maintainer's machine after the run, including on the failure path."
    - "MUST NOT remove a tap, a man page, or any other Homebrew-owned artifact that this run did not itself create. Restoration means returning to the recorded baseline in BOTH directions, never merely reaching a clean-looking machine."
---

<objective>
Observe the refusal against a real Homebrew install produced by the real Phase-3 tap, and
record what was and was not proved.

Purpose: the ROADMAP's Depends-on clause states the reason this plan exists — a synthetic
layout can only prove the mechanism, not that it matches what actually ships. Plan 04-01's
table proves the predicate against fixtures this project constructed; only a genuine
`brew install` proves that Homebrew's real Caskroom layout and its real
`INSTALL_RECEIPT.json` placement are what the predicate matches. The seam assertion (D-08)
already carries the "never mutates" half, so this run only has to observe the refusal.

Output: `04-EVIDENCE.md`, recording three legs with distinct evidentiary status, the verbatim
outputs and exit codes of each, the disposition of the three spec-less-probe assumptions, and
the residual gap with its closing condition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-CONTEXT.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-VALIDATION.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-01-SUMMARY.md
@.planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md
</context>

## Artifacts this phase produces

| Artifact | Kind | File |
|---|---|---|
| `04-EVIDENCE.md` | new evidence document | `.planning/phases/04-codegraph-upgrade-homebrew/04-EVIDENCE.md` |
| `UPGR-ACCEPTANCE-EVIDENCE` | machine-parseable evidence line | recorded inside `04-EVIDENCE.md` |

Consumes symbols created by plan 04-01 (`detectBrewManaged`, `brewPointerMessage`,
`TestDetectBrewManaged_RealInstall`, `CODEGRAPH_BREW_ACCEPTANCE_PATH`) — see
`04-01-PLAN.md` § "Artifacts this phase produces". Creates no code symbols.

**Spec-less probe note:** the three `unresolved` probe rows (UPGR-01/02/03) enumerated as
flagged assumptions in `04-01-PLAN.md` are dispositioned by Task 3 of this plan. None is
dropped; any that this run does not close is carried forward by name.

<review_dispositions>
## Cross-AI review cycle 1 — findings landing in this plan

Both cycle-1 workstation-safety HIGHs targeted this plan. Codex rated overall phase risk HIGH
almost entirely on their account; both are now closed by mechanism rather than by intent, which
is the specific gap Codex named.

| # | Severity | Finding | Disposition and location |
|---|---|---|---|
| 4 | HIGH | No mechanical rollback after overwriting a real Homebrew-owned payload — Task 2 claimed unconditional restoration but specified no `trap`, cleanup helper or subprocess harness, while deliberately running commands expected to exit non-zero | **Incorporated.** Task 2's `<action>` now mandates that the entire mutating sequence run inside ONE dedicated script with a fixed six-step structure: `set -uo pipefail` (deliberately not `-e`, so expected non-zero exits do not abort into the restoration path), preserve-and-hash, **install `trap restore_and_cleanup EXIT INT TERM` before the first mutating byte**, mutate, observe, fall off the end. The trap restores bytes FIRST (unrecoverable by hand) and does cask/tap cleanup second. T-04-19's severity is raised medium → high and its mitigation rewritten to enumerate the four mechanisms |
| 5 | HIGH | Cannot reliably restore an arbitrary pre-existing cask version — "reinstall to the recorded prior version" is not a mechanism a tap generally supports, and Task 1 ran `brew install` regardless of the baseline | **Incorporated by removing the promise.** A pre-existing codegraph cask is now a BLOCKING `<precondition>` on Task 1 that HALTS the run, matching the repository's own rehearsal posture at `Taskfile.yml:1671`. Task 1's action enforces it before the baseline capture and before any mutation. Task 2's restoration branch for a prior version is deleted outright with the rationale recorded inline, since with the precondition in force the restoration target is unambiguous. A new `must_haves.truths` entry states the refusal-to-start |
| 15 | MEDIUM | Task 2's restoration gate has two escape hatches — it passes when `brew` is absent from PATH (`2>/dev/null \| wc -l` yields 0) and is short-circuited by `CODEGRAPH_CASK_PREEXISTING` — and is all-negative with no positive floor | **Incorporated.** The env-var short-circuit is deleted (it is unnecessary now that a pre-existing cask halts the run). The gate asserts `command -v brew` FIRST and fails loudly if absent, so "could not check" can never read as "clean". The positive floor is the trap's receipt: it must exist (proving the trap ran), carry exactly one `RESTORE_VERDICT=ok`, and its `PAYLOAD_SHA256_BEFORE`/`_AFTER` must be equal (proving the bytes put back are the bytes taken away) — repo rule `84d1gfpywd`'s "a guard must carry a positive assertion that it did its work" |
| 7 (this plan's instances) | MEDIUM | `rg -c -e 'UPGR-01' -e 'UPGR-02' -e 'UPGR-03' … -ge 3` and `-e 'Leg 1' -e 'Leg 2' -e 'Leg 3' … -ge 3` do not establish that all three tokens occur — three lines all naming `UPGR-01` pass | **Incorporated.** Task 3's `<automated>` replaces both combined thresholds with explicit loops asserting ONE token at a time and naming the missing token on failure, using `-F` so no token is read as a regex. The criterion-mapping check is likewise split per criterion |
| 17 | LOW | The evidence-ID gate does not enforce the ID↔disposition association its prose requires | **Incorporated.** Task 3's action now fixes the disposition table's shape — heading `## Carried assumption dispositions`, requirement ID as the row's first cell — and the gate asserts `^\| UPGR-0N \|` exactly once per ID, three independent assertions. A document mentioning the IDs only in criterion citations now fails |
| 19 | LOW | The `--force` "identical message" criterion is stricter than the unit tests' substring bar and would fail spuriously if cobra annotated the two flag combinations differently | **Incorporated.** Relaxed to "contains the same resolved-path pointer sentence as bare `upgrade`" — both outputs must contain the same `<prefix>/Caskroom/codegraph/<version>` path and the same literal `brew upgrade codegraph`. The `<human-check>` is reworded to match |

## Cross-AI review cycle 2 — findings landing in this plan

| # | Severity | Finding | Disposition and location |
|---|---|---|---|
| C2-1 | HIGH | Tasks 2 and 3 left their automated-verify blocks unterminated — anchored open count 3, close count 1. The commands at `:268` and `:356` ran straight into their `human-check` siblings, so the two gates the cycle-1 revision added to close findings #15 and #7 were structurally unreachable by an executor | **Incorporated.** Both commands now carry their terminating tag. Verified by anchored per-file open/close counts, NOT by `gsd-tools verify plan-structure`, which returns `valid: true` on an unbalanced file and does not detect this class at all. Post-fix this file matches all five siblings at open=3 / close=3. Note for future revisions: count opens with `rg -c '^\s*<automated>'` and closes anchored to end-of-line, because an unanchored close pattern also matches prose mentions inside tables such as this one |
| C2-2 | HIGH | The trap unconditionally ran `brew untap seanb4t/tap` even though Task 1 records a possibly-pre-existing tap as the restoration target, so a maintainer who already had the tap finished in a different state and Task 2's "four probes match baseline" criterion failed by construction | **Incorporated.** Task 1 now probes tap pre-existence BEFORE tapping and writes `TAP_PREEXISTING=yes\|no` to a fixed-path baseline file. Task 2's trap reads that file and untaps ONLY when this run added the tap, recording `TAP_ACTION=untapped\|left-in-place` in the receipt. Task 2's `<automated>` asserts the two agree and re-probes the machine's actual final tap state |
| C2-3 | HIGH | Cleanup can delete pre-existing `codegraph*.1` man pages: `brew uninstall --cask codegraph` fires `.goreleaser.yaml:621`'s unconditional `FileUtils.rm_f` glob over the SHARED man directory, while the baseline probe tolerated a nonzero count | **Incorporated by halting rather than by restoring.** A nonzero pre-existing `codegraph*.1` count is now a BLOCKING precondition on Task 1 that HALTS the run. Byte-for-byte preservation was rejected: it would have this plan reimplement Homebrew's own file bookkeeping to protect state we did not create, where refusing to start is exact, cheap and honest — the same shape the pre-existing-cask precondition already takes. With the baseline provably zero, the uninstall hook's glob can only remove pages this run created |
| C2-4 | MEDIUM | `trap restore_and_cleanup EXIT INT TERM` double-invokes on INT/TERM, contradicting the plan's own "do not call twice" note | **Incorporated.** The function opens with an idempotency guard, counts its own effective invocations into `RESTORE_INVOCATIONS`, and the signal legs disarm all three traps before calling cleanup and then exit with the conventional signal code. The task's `<automated>` asserts `RESTORE_INVOCATIONS=1` exactly once — a positive assertion that cleanup ran exactly once, per repo rule `84d1gfpywd` |
| C2-5 | MEDIUM | `04-VALIDATION.md` row `04-06-03` is a weaker subset of the task command — it drops the Leg-token presence loop | **Incorporated in `04-VALIDATION.md`,** not here: rows `04-06-02` and `04-06-03` are synchronized with the task commands below, including the Leg loop, the tap-agreement check and the man-page final probe |

**Not changed, with rationale (so the decision is visible rather than silent):** OpenCode's LOW
on `brew trust --tap` security framing. T-04-21 already accepts this by name and already records
that the published-instruction defect (`docs/RELEASE.md:515-535`) is a deliberately-unfolded
Phase-3 documentation todo, not something this phase closes. The reviewer's own words were "fine
as scoped; worth restating at ship time" — restating it at ship time is a `/gsd-ship` concern, and
no plan change is warranted. The existing T-04-21 row is the record.
</review_dispositions>

<tasks>

<task type="auto">
  <name>Task 1: Leg 1 — install from the real tap and run the detector against the genuine tree, mutating nothing</name>
  <files>.planning/phases/04-codegraph-upgrade-homebrew/04-EVIDENCE.md</files>
  <precondition>Homebrew is installed and `brew --version` reports a 6.x release, the machine has network access to github.com, and the published `seanb4t/homebrew-tap` repository is reachable — the acceptance run is meaningless against anything else and must halt rather than substitute a local rehearsal tap.</precondition>
  <precondition>NO codegraph cask is currently installed: `brew list --cask --versions codegraph` must report nothing. If it reports a version, HALT and report to the maintainer rather than proceeding — do not install over it, do not record it as a "pre-existing install" to be restored later. (Review cycle 1, HIGH: "reinstall to the recorded prior version" is not a mechanism a tap generally supports — a tap normally exposes only its current cask, and `brew install`/`reinstall` cannot be relied on to recover an arbitrary older version — so a plan that overwrites a pre-existing install has no honest restoration path. This matches the safety posture the repository's own rehearsal already takes at `Taskfile.yml:1671`, which refuses to run over a pre-existing cask.) The maintainer's remedy is to uninstall their existing cask deliberately, recording its version, and reinstall it themselves afterwards; that is a human decision, not something this plan may take on their behalf.</precondition>
  <precondition>`brew` is resolvable on PATH (`command -v brew` exits 0). Asserted explicitly because several of this plan's probes pipe `brew` output through `wc -l`, where a missing `brew` yields 0 and would read as "clean" rather than as "could not check".</precondition>
  <precondition>NO `codegraph*.1` man page exists in the shared Homebrew man directory: `ls "$(brew --prefix)/share/man/man1/" | rg -c '^codegraph'` must report 0. If it reports anything higher, HALT and report the count and the filenames to the maintainer rather than proceeding. (Review cycle 2, HIGH: the cask's uninstall hook at `.goreleaser.yaml:621` is `Dir.glob((man_dir/"codegraph*.1").to_s).each { |f| FileUtils.rm_f(f) }` — an UNCONDITIONAL glob removal over a SHARED directory, so Task 2's `brew uninstall --cask codegraph` would delete pages this run never created. This is not hypothetical: `03-EVIDENCE.md` records orphaned `codegraph*.1` pages as a real prior state on this project, and the pre-existing-cask precondition excludes only a cask, not orphaned pages.) Halting is deliberately chosen over preserving-and-restoring the pages byte-for-byte: preservation would have this plan reimplement bookkeeping for files Homebrew owns in order to protect state we did not create, whereas refusing to start is exact, cheap, and the same shape the pre-existing-cask precondition already takes. The maintainer's remedy is to remove or move their orphaned pages deliberately, which is a human decision.</precondition>
  <read_first>
    - `.planning/phases/04-codegraph-upgrade-homebrew/04-VALIDATION.md` § "Manual-Only Verifications"
      (the recorded procedure and the `brew trust` requirement)
    - `docs/RELEASE.md` around lines 515-535 (the untrusted-tap refusal and the exact `brew trust`
      command Homebrew 6.0.16 requires before a first cask install from a third-party tap)
    - `internal/upgrade/brew_test.go` as written by plan 04-01 (`TestDetectBrewManaged_RealInstall`'s
      contract: it aborts by name when the env var names a nonexistent path, and never skips once set)
    - `.planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md` § "BREW-01 — the cold install" (the
      recorded Caskroom paths this run should agree with, and the evidence-recording style to follow)
  </read_first>
  <action>
Enforce the blocking preconditions FIRST, before the baseline capture and before any mutation.
Assert `command -v brew` exits 0, and assert `brew list --cask --versions codegraph` reports no
version. If a codegraph cask IS installed, HALT with a message naming the installed version and
telling the maintainer to uninstall it themselves first — do not install over it and do not
attempt to restore it later (review cycle 1, HIGH: an arbitrary prior cask version is not
reliably restorable from a tap that publishes only its current cask, so proceeding would be a
promise this plan cannot keep). Record the halt verbatim if it fires; a halted run is a correct
outcome, not a failure to work around.

Enforce the man-page precondition in the same pass: `ls "$(brew --prefix)/share/man/man1/" |
rg -c '^codegraph'` must report 0. If it reports higher, HALT naming the count and the
filenames — do NOT proceed and do NOT plan to restore them (review cycle 2, HIGH: the cask's
uninstall hook globs `codegraph*.1` out of that SHARED directory unconditionally, so
proceeding would delete a maintainer's orphaned pages and the final count probe alone cannot
distinguish "restored" from "deleted the maintainer's").

Record the machine's starting state next, verbatim, before touching anything:
`brew --version`, `brew list --cask --versions codegraph` (which the precondition has already
established is empty — recorded anyway as the restoration target),
`brew tap | rg seanb4t` (tolerating no match — **this probe runs BEFORE `brew tap`, and its
result is the restoration target for the tap**), and
`ls "$(brew --prefix)/share/man/man1/" | rg -c '^codegraph'` (which the precondition has
established is 0). This is both the evidence baseline and the restoration target — Task 2
restores to exactly this state.

**Write the baseline to a fixed-path, machine-readable file so Task 2's trap can restore
against it rather than against an assumption** (review cycle 2, HIGH): create
`${TMPDIR:-/tmp}/codegraph-04-06-baseline.env` carrying exactly four `KEY=value` lines —
`TAP_PREEXISTING=yes` or `TAP_PREEXISTING=no` (derived from the `brew tap` probe ABOVE, taken
before this task taps anything), `CASK_PREEXISTING=no` (the precondition guarantees it),
`MAN_PAGES_BEFORE=0` (likewise), and `BREW_PREFIX=<the value of brew --prefix>`. Delete any
stale baseline file at that path first, so a previous run's baseline can never be mistaken for
this one's. The distinction this file exists to carry is the one cycle 2 found missing: a tap
this run added must be removed, and a tap the maintainer already had must be left alone, and
after `brew tap` runs the two are indistinguishable from the machine alone.

Install from the real tap: `brew tap seanb4t/tap`, then `brew trust --tap seanb4t/tap`
(Homebrew 6.0.16 refuses a first cask install from an untrusted third-party tap and this is
the documented remedy), then `brew install codegraph`. Capture each command's full output and
exit code. If the install fails, stop and record the failure verbatim — do not fall back to a
local rehearsal tap, which would prove the mechanism against a fixture again and answer none
of this plan's question.

Record the genuine layout, verbatim: the `bin/codegraph` symlink and its target
(`ls -l "$(brew --prefix)/bin/codegraph"` and a realpath resolution), the Caskroom tree
(`ls -la "$(brew --prefix)/Caskroom/codegraph"` and of its versioned subdirectory), and the
receipt (`ls -la "$(brew --prefix)/Caskroom/codegraph/.metadata/"` plus a listing that shows
the `INSTALL_RECEIPT.json` path in full). These listings are the answer to the question a
constructed fixture cannot answer.

Run the harness against that real tree with no mutation whatsoever:
`CODEGRAPH_BREW_ACCEPTANCE_PATH="$(brew --prefix)/bin/codegraph" go test ./internal/upgrade/ -run '^TestDetectBrewManaged_RealInstall$' -v`
Capture the full verbose output, including the `t.Logf` line reporting the resolved binary,
install directory, tree, token, version and receipt. Compare each logged value against the
listings recorded above and state explicitly whether they agree.

Leave the install in place for Task 2. Do not uninstall in this task.
  </action>
  <verify>
    <automated>CODEGRAPH_BREW_ACCEPTANCE_PATH="$(brew --prefix)/bin/codegraph" go test ./internal/upgrade/ -run '^TestDetectBrewManaged_RealInstall$' -v 2>&1 | rg -c -e '--- PASS: TestDetectBrewManaged_RealInstall'</automated>
  </verify>
  <acceptance_criteria>
    - `brew install codegraph` from `seanb4t/tap` exits 0, and its full output plus the preceding
      `brew tap` and `brew trust` outputs are recorded verbatim.
    - `CODEGRAPH_BREW_ACCEPTANCE_PATH="$(brew --prefix)/bin/codegraph" go test ./internal/upgrade/ -run '^TestDetectBrewManaged_RealInstall$' -v`
      prints exactly one `--- PASS: TestDetectBrewManaged_RealInstall` line. Because the harness
      aborts by name on a missing path and never skips once the variable is set, this PASS is a
      positive detection against the real tree, not an absent-test no-op.
    - The harness's logged install directory ends with `/Caskroom/codegraph/<version>`, and that
      `<version>` equals the version `brew list --cask --versions codegraph` reports.
    - The harness's logged receipt path exists on disk: `test -f "<logged path>"` exits 0, and the
      path's directory component is `.metadata` under the Caskroom token directory — the placement
      D-03 was designed around, now observed rather than assumed.
    - The recorded `ls -l "$(brew --prefix)/bin/codegraph"` output shows a symlink, and its resolved
      target lies under the same Caskroom versioned directory the harness logged — proving the
      symlink resolution did real work on the real layout.
    - The starting-state capture is recorded and includes all four probes named in the action.
    - The baseline file `${TMPDIR:-/tmp}/codegraph-04-06-baseline.env` exists and carries exactly one
      `TAP_PREEXISTING=yes` or `TAP_PREEXISTING=no` line, one `CASK_PREEXISTING=no` line, one
      `MAN_PAGES_BEFORE=0` line and one `BREW_PREFIX=` line — a positive artifact proving the
      restoration target was recorded, not merely observed (repo rule `84d1gfpywd`). The
      `TAP_PREEXISTING` value must have been derived from a `brew tap` probe taken BEFORE this task
      ran `brew tap seanb4t/tap`; record both the probe output and the derived value in the evidence
      so the ordering is checkable by a reader who was not present.
    - `MAN_PAGES_BEFORE=0` specifically: a nonzero value means the run should have HALTED at the
      precondition and did not (review cycle 2, HIGH). The evidence must record the zero count, not
      infer it.
  </acceptance_criteria>
  <done>codegraph is installed from the real published tap, the genuine Caskroom layout and receipt placement are recorded verbatim, and the detector reports a positive detection against them with every logged field cross-checked against the on-disk listings.</done>
</task>

<task type="auto">
  <name>Task 2: Leg 2 — observe the real refusal and the real step-aside from inside the genuine tree, then restore the machine</name>
  <files>.planning/phases/04-codegraph-upgrade-homebrew/04-EVIDENCE.md</files>
  <precondition>Task 1's install is still present and `brew list --cask --versions codegraph` reports a version — this task observes behaviour from inside that specific tree and must halt rather than reconstruct one.</precondition>
  <read_first>
    - Task 1's recorded listings (the exact resolved payload path this task overwrites and restores)
    - `internal/upgrade/brew.go` and `internal/upgrade/upgrade.go` as written by plan 04-01 (the
      exact message and the two exit behaviours being observed)
    - `internal/cli/upgrade.go` as modified by plan 04-04 (the help text, observed here against a
      real binary rather than a unit test)
    - `.planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md` § "Scope reduction, recorded plainly"
      (the established form for recording an observation weaker than the ideal one, plainly)
  </read_first>
  <action>
Build the current tree's binary for this host: `go build -o <scratch>/codegraph ./cmd/codegraph`
(use the scratch directory, never `dist/`). Record its `version --json` output and its sha256.

**Run every step from here to the end of this task inside ONE dedicated shell script** — write
it to the scratch directory and execute it — rather than as a sequence of independent tool
calls. This is a hard requirement, not a style preference (review cycle 1, HIGH): the task
deliberately runs commands expected to exit non-zero, over a Homebrew-owned executable in the
maintainer's real Caskroom, and a sequence of independent calls has no mechanism that survives
an interruption or an unexpected failure. Independent calls cannot carry a trap.

The script's structure is fixed:

  1. `set -uo pipefail` — deliberately NOT `-e`: several observed commands are expected to exit
     non-zero, and the script must reach its restoration path rather than abort into it.
  2. Preserve the original payload BEFORE anything else mutates: `cp` the resolved Caskroom
     payload Task 1 recorded to `<scratch>/codegraph.original`, then compute and store its
     sha256 in a variable and echo it. If the copy or the hash fails, exit before mutating.
  3. **Install the EXIT trap now — before the first mutating byte**, and never after. Source
     Task 1's baseline file `${TMPDIR:-/tmp}/codegraph-04-06-baseline.env` first and fail
     immediately if it is absent or if it carries no `TAP_PREEXISTING=yes|no` line: without it
     the restoration target is unknown and no mutation may proceed.

     The trap function OPENS WITH AN IDEMPOTENCY GUARD (review cycle 2, MEDIUM): a script-level
     flag variable initialised to empty before the trap is installed; on entry, if it is already
     set the function returns immediately without doing anything, and otherwise it sets the flag
     and increments an invocation counter. This is what makes the "do not restore twice" rule a
     mechanism rather than an instruction — see the signal handling below, which is the path
     that made the cycle-2 version double-invoke.

     The trap function must then, in this order: (a) copy `<scratch>/codegraph.original` back
     over the Caskroom payload path; (b) recompute the restored payload's sha256 and compare it
     to the preserved value, then WRITE A MACHINE-READABLE RECEIPT to the fixed path
     `${TMPDIR:-/tmp}/codegraph-04-06-restore-receipt.txt` containing exactly five lines —
     `PAYLOAD_SHA256_BEFORE=<64 hex>`, `PAYLOAD_SHA256_AFTER=<64 hex>`,
     `RESTORE_VERDICT=ok` or `RESTORE_VERDICT=MISMATCH`, `RESTORE_INVOCATIONS=<count>` (the
     idempotency counter, which must read 1), and `TAP_ACTION=untapped` or
     `TAP_ACTION=left-in-place` — and echo the same five lines to the transcript. The receipt is
     what makes this task's automated gate a POSITIVE assertion that restoration happened
     rather than a negative that passes when nothing ran (repo rule `84d1gfpywd`; review cycle 1,
     MEDIUM: the cycle-1 gate was all-negative, passed when `brew` was absent from PATH because
     `2>/dev/null | wc -l` yielded 0, and was short-circuited entirely by an env var). Delete any
     stale receipt at that path at script start, so a previous run's receipt can never be
     mistaken for this one's; (c) only then perform `brew uninstall --cask codegraph`, and
     **untap CONDITIONALLY: run `brew untap seanb4t/tap` if and only if the sourced
     `TAP_PREEXISTING` is `no`, recording `TAP_ACTION=untapped`; if it is `yes`, leave the tap
     in place and record `TAP_ACTION=left-in-place`** (review cycle 2, HIGH: the cycle-2 trap
     untapped unconditionally, which destroyed a maintainer's pre-existing tap AND made this
     task's own "four probes match baseline" criterion fail by construction — restoration means
     returning to the recorded baseline in BOTH directions, not reaching a clean-looking
     machine); (d) re-run the four baseline probes and echo them alongside Task 1's recorded
     values. Restoration of the bytes comes first because it is the step that cannot be redone
     by hand from a transcript; the cask/tap cleanup is recoverable.

     Register the trap on all three conditions, but NOT with one shared three-signal
     registration: `trap restore_and_cleanup EXIT` for the normal path, and for each of INT and
     TERM a handler that FIRST disarms all three traps, THEN calls the cleanup function, THEN
     exits with the conventional signal code (130 for INT, 143 for TERM). Registering
     `trap restore_and_cleanup EXIT INT TERM` as one line — the cycle-2 shape — double-invokes
     on a signal, because the handler returns and the subsequent shell exit fires the EXIT trap
     again, which contradicts this task's own "do not restore twice" rule and would let a second
     `brew uninstall`/`brew untap` obscure a successful first restoration in the receipt. The
     idempotency guard above makes the second invocation harmless even so; the disarm-then-exit
     handlers make it not happen. Both are required — the guard is what the automated gate can
     assert (`RESTORE_INVOCATIONS=1`), and the handlers are what keep the exit code honest.
  4. Only now copy the freshly built binary over the resolved Caskroom payload path.
  5. Run the four observations.
  6. Fall off the end of the script; the EXIT trap performs restoration. Do not also call the
     trap function directly — the idempotency guard would make the second call a no-op, but
     `RESTORE_INVOCATIONS` is asserted to be exactly 1 and a direct call obscures which path
     actually restored the machine.

The tree, the symlink, the receipt and Homebrew's own bookkeeping are untouched — only the
payload changes, which is precisely the variable that must change and nothing else. Record this
plainly as what it is; it is a genuine layout with a substituted payload, not a naturally-installed
release.

Observe both behaviours through the installed `bin/codegraph` symlink, never the Caskroom path
directly — invoking through the symlink is what exercises the symlink resolution end to end:
  - `"$(brew --prefix)/bin/codegraph" upgrade`; capture stdout, stderr and `$?`.
  - `"$(brew --prefix)/bin/codegraph" upgrade --check`; capture stdout, stderr and `$?`.
  - `"$(brew --prefix)/bin/codegraph" upgrade --force`; capture stdout, stderr and `$?`.
  - `"$(brew --prefix)/bin/codegraph" upgrade --help`; capture stdout.
Run all four with networking available but note that none should require it — if any is slow
enough to suggest a network round trip, record that, because D-11's whole point is that this
path is offline.

Restoration is performed by the EXIT trap installed in step 3, which is what makes
"unconditional, including on any failure path" a mechanism rather than an intention. Because
Task 1's preconditions established that NO codegraph cask was installed and NO `codegraph*.1`
man page existed before this run, the restoration target for those two is unambiguous:
uninstall the cask, and let the uninstall hook's man-page glob run knowing every page it can
match was created by this run. There is no "reinstall to the recorded prior version" branch,
because there is no prior version — that branch was removed in review cycle 1 as an
unimplementable promise. The TAP, by contrast, is NOT unambiguous and must not be treated as
if it were: it may legitimately have pre-existed, which is why step 3 reads Task 1's recorded
`TAP_PREEXISTING` and untaps only when this run added it (review cycle 2, HIGH).

Verify the restoration with the same four probes Task 1 used for the baseline and record the
before/after pair side by side. "Match the baseline" is the criterion in both directions:
leaving a mutated Caskroom or an unexpected cask behind is a failure of this task, and so is
removing a tap or a man page that was there when the run started, regardless of what was
observed.

Capture the script's full transcript including the trap's own output — the two sha256 values
and the `RESTORE_VERDICT` line are load-bearing evidence, not incidental logging.
  </action>
  <verify>
    <automated>R="${TMPDIR:-/tmp}/codegraph-04-06-restore-receipt.txt"; B="${TMPDIR:-/tmp}/codegraph-04-06-baseline.env"; command -v brew >/dev/null || { echo "brew not on PATH — restoration is UNVERIFIED, not clean" >&2; exit 1; }; test -f "$R" || { echo "no restore receipt at $R — the EXIT trap did not run" >&2; exit 1; }; test -f "$B" || { echo "no Task 1 baseline at $B — the restoration target was never recorded" >&2; exit 1; }; test "$(rg -c -e '^RESTORE_VERDICT=ok$' "$R")" -eq 1 && test "$(rg -c -e '^RESTORE_INVOCATIONS=1$' "$R")" -eq 1 && test "$(rg -o -e '^PAYLOAD_SHA256_(BEFORE|AFTER)=[0-9a-f]{64}$' "$R" | rg -o -e '[0-9a-f]{64}$' | sort -u | wc -l | tr -d ' ')" -eq 1 && test "$(brew list --cask --versions codegraph | wc -l | tr -d ' ')" -eq 0 && B="$B" R="$R" node -e 'const fs=require("fs");const pre=(/^TAP_PREEXISTING=(yes|no)$/m.exec(fs.readFileSync(process.env.B,"utf8"))||[])[1];const act=(/^TAP_ACTION=(untapped|left-in-place)$/m.exec(fs.readFileSync(process.env.R,"utf8"))||[])[1];if(!pre)throw new Error("baseline carries no TAP_PREEXISTING=yes|no line");if(!act)throw new Error("receipt carries no TAP_ACTION=untapped|left-in-place line");const want=pre==="yes"?"left-in-place":"untapped";if(act!==want)throw new Error("tap restoration wrong: TAP_PREEXISTING="+pre+" requires TAP_ACTION="+want+", receipt says "+act);console.log("tap restoration OK: "+pre+" -> "+act)' && test "$(brew tap | rg -c -e '^seanb4t/tap$' || echo 0)" -eq "$(rg -c -e '^TAP_PREEXISTING=yes$' "$B" || echo 0)" && test "$(ls "$(brew --prefix)/share/man/man1/" | rg -c -e '^codegraph' || echo 0)" -eq 0</automated>
    <human-check>Confirm from the recorded transcripts that bare `upgrade` exited non-zero with a message naming the real Caskroom versioned directory and `brew upgrade codegraph`; that `upgrade --check` exited 0 with the same pointer and reported no version number; that `upgrade --force` exited non-zero carrying the same resolved-path pointer sentence as bare `upgrade`; that the EXIT trap's receipt reports `RESTORE_VERDICT=ok` with equal before/after payload hashes; and that the machine's four restoration probes match its four baseline probes exactly.</human-check>
  </verify>
  <acceptance_criteria>
    - Bare `upgrade` exit code is non-zero, and its captured message contains both the real
      `<prefix>/Caskroom/codegraph/<version>` path recorded in Task 1 and the literal
      `brew upgrade codegraph`.
    - `upgrade --check` exit code is 0, its captured output contains the same two strings, and it
      contains no release version number other than the installed cask's own — brew owns that answer
      and this command must not have produced a second one.
    - `upgrade --force` exit code is non-zero and its captured output CONTAINS THE SAME
      RESOLVED-PATH POINTER SENTENCE as bare `upgrade`'s — specifically, both outputs contain the
      same `<prefix>/Caskroom/codegraph/<version>` path and the same literal
      `brew upgrade codegraph`. Calibrated in review cycle 1 (LOW) from the cycle-1 wording
      "identical to bare `upgrade`'s": byte-identity is stricter than what the unit tests assert
      (substring) and would fail spuriously if cobra annotated the two flag combinations
      differently. The property under test is that the reinstall-anyway option is powerless
      against the refusal, and sentence containment establishes exactly that.
    - `upgrade --help` output names both exit behaviours and `brew upgrade codegraph`, observed
      against a real binary.
    - **Positive restoration floor, not an absence check** (review cycle 1, MEDIUM): the receipt at
      `${TMPDIR:-/tmp}/codegraph-04-06-restore-receipt.txt` exists, carries exactly one
      `RESTORE_VERDICT=ok` line, and its `PAYLOAD_SHA256_BEFORE` and `PAYLOAD_SHA256_AFTER` values
      are equal — proving the EXIT trap ran AND that the bytes it put back are the bytes it took
      away. Record both hashes in the evidence document. The gate additionally asserts `brew` is
      on PATH before consulting it, so "could not check" can never read as "clean", and it carries
      no environment-variable escape hatch.
    - **Cleanup ran exactly once** (review cycle 2, MEDIUM): the receipt carries exactly one
      `RESTORE_INVOCATIONS=1` line. A value of 2 means the trap double-invoked — the defect the
      single `trap … EXIT INT TERM` registration produced on a signal — and a missing line means the
      idempotency counter was never wired. This is a positive assertion that cleanup ran, and ran
      once, rather than a check that nothing is left over (repo rule `84d1gfpywd`).
    - **The tap is restored to its recorded baseline in BOTH directions** (review cycle 2, HIGH):
      Task 1's baseline file carries `TAP_PREEXISTING=yes` or `=no`; the receipt carries
      `TAP_ACTION=left-in-place` or `=untapped`; and the two agree under the rule
      `yes ⇒ left-in-place`, `no ⇒ untapped`. The `<automated>` command asserts that agreement with
      a `node -e` check that throws by name on a missing line on either side or on a disagreement,
      AND separately re-probes the machine itself: the final `brew tap | rg -c '^seanb4t/tap$'`
      count equals 1 when `TAP_PREEXISTING=yes` and 0 when it is `no`. Asserting the receipt alone
      would prove only that the script described what it did.
    - **No man page is left behind and none was destroyed** (review cycle 2, HIGH): the final
      `ls "$(brew --prefix)/share/man/man1/" | rg -c '^codegraph'` reports 0, which equals Task 1's
      `MAN_PAGES_BEFORE=0`. The equality is only meaningful BECAUSE Task 1's precondition halted the
      run on any nonzero baseline — without it a final count of 0 is equally consistent with "the
      uninstall hook's unconditional `codegraph*.1` glob deleted the maintainer's orphaned pages",
      which the count alone cannot distinguish.
    - The four restoration probes (`brew list --cask --versions codegraph`, `brew tap | rg seanb4t`,
      the man-page count, and `ls "$(brew --prefix)/bin/codegraph"`) each match their Task 1 baseline
      values. Record the baseline and final values side by side. "Match" is bidirectional: for the
      tap probe specifically, matching a `TAP_PREEXISTING=yes` baseline means the tap is STILL
      PRESENT at the end, not absent.
    - Every command's full output and exit code is captured verbatim, not summarised.
  </acceptance_criteria>
  <done>Both exit behaviours and the force case are observed from inside a genuine Homebrew tree through the installed symlink, every transcript is captured verbatim, the substitution is recorded plainly as a substitution, and the machine is provably back to its starting state.</done>
</task>

<task type="auto">
  <name>Task 3: Write 04-EVIDENCE.md with the three-leg split and disposition every carried assumption</name>
  <files>.planning/phases/04-codegraph-upgrade-homebrew/04-EVIDENCE.md</files>
  <read_first>
    - The verbatim transcripts captured by Tasks 1 and 2
    - `.planning/phases/04-codegraph-upgrade-homebrew/04-01-PLAN.md` § `<flagged_assumptions>` (the
      three unresolved spec-less-probe rows and their stated closing conditions)
    - `.planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md` (the document shape, the machine-parseable
      evidence-line convention, and the "BREW-06, half one / half two" split — the established form for
      recording two claims of different evidentiary status without letting one borrow the other's weight)
    - `.planning/ROADMAP.md` Phase-4 Success Criteria as amended by plan 04-03
  </read_first>
  <action>
Create `04-EVIDENCE.md` following `03-EVIDENCE.md`'s document shape. Structure it around three
legs with explicitly different evidentiary status, and never let a later leg borrow an earlier
one's weight:

  - **Leg 1 — the genuine layout, no mutation. Executed evidence.** The real tap install, the
    recorded Caskroom and receipt listings, and the detector's positive result against them.
    This is what a constructed fixture could not prove.
  - **Leg 2 — the observed behaviour, genuine layout with a substituted payload. Executed
    evidence, with a named substitution.** The four captured transcripts and exit codes, the
    payload sha256 before and after, and the restoration probe pair. State the substitution in
    the leg's own heading, not in a footnote.
  - **Leg 3 — the fully natural path. NOT executed, NOT claimed.** A released binary that itself
    carries this phase's detection code, installed by brew with no substitution. Name its
    closing condition precisely: the next release cut after this phase merges republishes the
    cask, and a `brew install` from it exercises the path end to end with nothing substituted.
    State that no run of it is scheduled and that this is an accepted, named gap in the same
    style Phase 3 used for its own scope reduction — never a silently dropped claim.

Map each amended ROADMAP Phase-4 success criterion to the leg that evidences it, with a direct
citation to the transcript, and state for each whether it is fully evidenced, partially
evidenced, or evidenced by a unit-level proof rather than this run (criterion 4, the brew-absent
machine, is satisfied by construction — detection never shells out to `brew` — and by plan
04-01's table; say so rather than staging a machine without Homebrew).

Disposition all three spec-less-probe assumptions carried from `04-01-PLAN.md` explicitly, in a
**table under the heading `## Carried assumption dispositions`, with one row per requirement ID
and the ID as the row's FIRST cell** — so the row shape is `| UPGR-01 | <closed|carried> | … |`.
The fixed shape is what lets the automated gate assert the ID↔disposition ASSOCIATION rather
than merely that enough lines matched somewhere in the document (review cycle 1, LOW: the
cycle-1 gate counted matching lines, which the prose requirement — "each ID must appear in a
disposition statement" — does not follow from). Each row states whether this run closed the
assumption, and if it did not, what would. A dropped row is a failure of this task.

Close with a machine-parseable `UPGR-ACCEPTANCE-EVIDENCE` line in this repository's established
convention — one line, fixed key order, sentinels for anything unavailable, never an omitted
key — carrying at minimum: the brew version, the cask version installed, the resolved install
directory, the bare-upgrade exit code, the check exit code, the force exit code, the payload
sha256 before and after restoration, and the restoration verdict.
  </action>
  <verify>
    <automated>E=.planning/phases/04-codegraph-upgrade-homebrew/04-EVIDENCE.md; test -s "$E" && test "$(rg -c -e 'UPGR-ACCEPTANCE-EVIDENCE' "$E")" -eq 1 && for t in 'Leg 1' 'Leg 2' 'Leg 3'; do test "$(rg -c -F -e "$t" "$E")" -ge 1 || { echo "missing heading token: $t" >&2; exit 1; }; done && for r in UPGR-01 UPGR-02 UPGR-03; do test "$(rg -c -F -e "$r" "$E")" -ge 1 || { echo "missing requirement ID: $r" >&2; exit 1; }; done && for r in UPGR-01 UPGR-02 UPGR-03; do test "$(rg -c -e "^\\| $r \\|" "$E")" -eq 1 || { echo "no disposition-table row for $r" >&2; exit 1; }; done</automated>
    <human-check>Read 04-EVIDENCE.md end to end and confirm that leg 3 is stated as not executed and not claimed, that leg 2's payload substitution is named in its own heading rather than buried, and that no criterion cites a leg whose evidentiary status does not support it.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-codegraph-upgrade-homebrew/04-EVIDENCE.md` exists and is non-empty.
    - It contains exactly one `UPGR-ACCEPTANCE-EVIDENCE` line, and that line carries every key named
      in the action with no key omitted (a sentinel value is required where a value is unavailable).
    - All three legs are present as headed sections, asserted ONE TOKEN PER CHECK rather than by a
      combined threshold (review cycle 1, MEDIUM: `rg -c` counts matching lines, so
      `-e 'Leg 1' -e 'Leg 2' -e 'Leg 3' … -ge 3` is satisfied by three lines that all say `Leg 1`).
      Each of `rg -c -F -e 'Leg 1' …`, `rg -c -F -e 'Leg 2' …` and `rg -c -F -e 'Leg 3' …` reports
      at least 1 — the `<automated>` command runs exactly these three as a loop that names the
      missing token. Additionally, leg 3's own heading states that it was not executed.
    - Every amended Phase-4 success criterion (1 through 4) appears with a leg citation and a stated
      evidentiary status, asserted per criterion rather than by one threshold: each of
      `rg -c -F -e 'Criterion 1' …` through `rg -c -F -e 'Criterion 4' …` reports at least 1.
    - All three spec-less-probe assumptions are dispositioned by name AND the ID↔disposition
      association is enforced, not merely the line count (review cycle 1, LOW). Under
      `## Carried assumption dispositions`, each of `UPGR-01`, `UPGR-02` and `UPGR-03` heads exactly
      one table row: `rg -c -e '^\| UPGR-01 \|' …` reports exactly 1, and likewise for `UPGR-02` and
      `UPGR-03` — three independent assertions. A document that mentions the IDs only in criterion
      citations fails this criterion.
    - The verbatim transcripts from Tasks 1 and 2 are embedded in fenced blocks — the document must be
      checkable by a reader who was not present, so summaries alone fail this criterion.
    - `task test` exits 0 and `git status --porcelain` shows no unexpected working-tree changes on the
      maintainer's machine beyond this document.
  </acceptance_criteria>
  <done>04-EVIDENCE.md records three legs with distinct and clearly-labelled evidentiary status, maps every amended criterion to its evidence, dispositions all three carried assumptions by name, and emits one machine-parseable acceptance line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the published tap -> the maintainer's machine | This plan installs and executes software fetched from a public tap on a real workstation, and temporarily writes into a Homebrew-owned directory. |
| observed evidence -> the phase's completion claim | This document is what a later reader and `/gsd-verify-work` treat as proof that the phase's headline claim holds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-19 | Tampering | the maintainer's Homebrew install | high | mitigate | Task 2 writes over a Homebrew-owned Caskroom payload while deliberately running commands expected to exit non-zero, so an interruption or an unexpected failure could leave a substituted payload installed on a real workstation. **Severity raised from medium in review cycle 1 (HIGH ×2).** Mitigated by four mechanisms, not by intent: (1) a blocking precondition — the run HALTS if any codegraph cask is already installed, so the restoration target is unambiguous and no unimplementable "reinstall the prior version" promise is made; (2) the whole mutating sequence runs inside ONE script with `trap restore_and_cleanup EXIT INT TERM` installed BEFORE the first mutating byte, restoring bytes first and performing cask/tap cleanup second; (3) the trap emits a machine-readable receipt with before/after sha256 and a `RESTORE_VERDICT`, which the task's automated gate asserts POSITIVELY — replacing a cycle-1 all-negative gate that passed when `brew` was absent from PATH and was short-circuitable by an environment variable; (4) the four baseline probes are compared before and after. **Extended in review cycle 2 (HIGH ×2, MEDIUM ×1)** with three further mechanisms, because "restore" was being read as "reach a clean machine" rather than "return to the recorded baseline": (5) a second blocking precondition HALTS the run if any `codegraph*.1` page already exists, because the cask's uninstall hook at `.goreleaser.yaml:621` globs that SHARED directory unconditionally and would delete pages this run never created; (6) the tap is untapped ONLY when Task 1's recorded `TAP_PREEXISTING=no` says this run added it, with the decision carried in the receipt as `TAP_ACTION` and cross-checked against the machine's real final state; (7) the cleanup function is idempotent and the INT/TERM legs disarm the traps before running it and then exit, so the receipt's asserted `RESTORE_INVOCATIONS=1` is a true statement rather than an aspiration. |
| T-04-20 | Repudiation | `04-EVIDENCE.md` | high | mitigate | Presenting the payload-substituted observation as a naturally-installed release binary would be the milestone's most consequential available overclaim: it would close UPGR-01 on evidence that does not support it. Mitigated by the three-leg structure, by naming the substitution in leg 2's own heading, and by an acceptance criterion requiring leg 3's heading to state it was not executed. |
| T-04-21 | Spoofing | `brew trust --tap` | medium | accept | Trusting a third-party tap is a genuine reduction of a Homebrew security control, and it is the documented published contract for installing this cask. Accepted for this acceptance run against a tap this project owns; the broader concern that the published instructions recommend the wider `--tap` grant with no security framing is a reviewed, deliberately-unfolded Phase-3 documentation todo, recorded here so this run is not mistaken for closing it. |
| T-04-22 | Information Disclosure | captured transcripts | low | mitigate | Transcripts embed absolute paths from the maintainer's machine. Mitigated by keeping them to Homebrew-prefix-relative and repository paths and by not capturing environment dumps or credentials. |
| T-04-SC | Tampering | dependency surface | low | mitigate | No package-manager install of a code dependency occurs; the only install is this project's own cask from its own tap, whose contents are already covered by the release pipeline's signing and attestation. Asserted by `git diff --exit-code go.mod go.sum` being clean at plan completion. |
</threat_model>

<verification>
- `brew list --cask --versions codegraph`, `brew tap | rg seanb4t` and the `codegraph*.1` man-page
  count each return the machine to their Task 1 recorded baseline values at plan end — in both
  directions, so a pre-existing tap is still present and no pre-existing page was removed.
- `task test` exits 0.
- `git diff --exit-code go.mod go.sum` exits 0.
- `04-EVIDENCE.md` exists, carries three headed legs, four criterion mappings, three assumption
  dispositions and exactly one `UPGR-ACCEPTANCE-EVIDENCE` line.
</verification>

<success_criteria>
- Detection is observed firing against a genuinely Homebrew-authored tree from the real tap
  (UPGR-02, and the ROADMAP Depends-on clause's stated reason for this plan).
- The refusal and the step-aside are observed with their real exit codes and messages
  (UPGR-01, UPGR-03).
- The machine is provably restored.
- The one unproved leg is named with its closing condition and is not claimed.
</success_criteria>

<output>
Create `.planning/phases/04-codegraph-upgrade-homebrew/04-06-SUMMARY.md` when done.
</output>
