---
phase: 04-codegraph-upgrade-homebrew
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-04"]
files_modified:
  - Taskfile.yml
  - internal/upgrade/taskfile_shape_test.go
  - .planning/todos/pending/2026-08-09-verify-self-upgrade-download-then-execute-has-no-signature-check.md
autonomous: true
requirements: [UPGR-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`verify:self-upgrade` verifies the prior release binary's cosign signature BEFORE the file is made executable, and a failed verification is a hard failure reached before any execution."
    - "The prior release's `.sigstore.json` bundle is downloaded into a temp directory separate from the binary's, and one trap removes both."
    - "Every `--certificate-identity-regexp` literal published or executed by this repository accepts and rejects exactly the same certificate subjects as the compiled `releaseWorkflowRefPattern` that `codegraph upgrade` itself enforces."
    - "The agreement guard fails loudly rather than passing vacuously when it finds fewer identity literals than the repository actually carries."
  artifacts:
    - Taskfile.yml
    - internal/upgrade/taskfile_shape_test.go
  key_links:
    - "internal/upgrade/verify.go's releaseWorkflowRefPattern is the single source of truth for this project's release identity; the Taskfile and the published docs restate it in POSIX form, and the new shape test is what keeps the restatements honest."
    - "verify:self-upgrade executes a downloaded binary; the cosign step is the only thing between the download and that execution, and it must sit above the chmod, not beside it."
  prohibitions:
    - "MUST NOT introduce a second, looser certificate-identity policy anywhere — a policy that accepts more than `releaseWorkflowRefPattern` accepts would prove less than the published instruction claims (D-13, folded-todo planner note)."
    - "MUST NOT make `verify:self-upgrade` depend on the outcome of a sibling CI job to achieve execution safety; the reporting independence of those jobs is a recorded, deliberate choice and this fix must hold regardless of it."
    - "MUST NOT re-test Sigstore. This verifies this project's own signing wiring using the identity policy `internal/upgrade` already enforces."
---

<objective>
Close the download-then-execute hazard in `verify:self-upgrade`, and make the repository's
several restatements of its own release-identity policy provably agree with the one the
upgrade path actually enforces.

Purpose: `verify:self-upgrade` downloads a prior release's raw binary, `chmod +x`es it, and
executes it, with no signature check anywhere in between. That is the identical ordering
hazard this milestone already fixed for `verify:notarized-suite` (T-02-19). Leaving it
unremarked three targets away in the same file reads as ignorance rather than a scoped
decision. The certificate-identity regex is currently restated by hand in four places with
nothing asserting they agree with the compiled pattern — exactly the "second policy that can
drift" the phase context warned against.

**Requirement linkage, stated rather than fudged:** this plan carries `UPGR-01` because
`verify:self-upgrade` is the CI target that exercises `codegraph upgrade` end to end — the
same command whose brew-refusal branch UPGR-01 defines. The work itself is a folded todo,
not a direct requirement deliverable, and this SUMMARY should say so.

Output: a cosign gate above the chmod in `verify:self-upgrade`, and a shape test proving
every identity literal in the repository is behaviourally identical to
`releaseWorkflowRefPattern`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-CONTEXT.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-PATTERNS.md
@internal/upgrade/verify.go
</context>

## Artifacts this phase produces

| Artifact | Kind | File |
|---|---|---|
| `SIG_DIR` (in `verify:self-upgrade`) | shell variable | `Taskfile.yml` |
| `PRIOR_SIG` | shell variable | `Taskfile.yml` |
| `SELF-UPGRADE-VERIFY-EVIDENCE` | evidence line | `Taskfile.yml` |
| `TestCosignIdentityPolicyMatchesCompiledPattern` | test func | `internal/upgrade/taskfile_shape_test.go` |
| `extractCosignIdentityLiterals` | test helper func | `internal/upgrade/taskfile_shape_test.go` |
| `TestCosignIdentityPolicyMatchesCompiledPattern_ZeroLiteralsIsError` | test func (self-test) | `internal/upgrade/taskfile_shape_test.go` |
| `cosignIdentitySANCorpus` | test fixture (var) | `internal/upgrade/taskfile_shape_test.go` |

Depends on the sentinel-free `Taskfile.yml` produced by plan 04-02 and the amended
`README.md` / `docs/RELEASE.md` produced by plan 04-04 — the new shape test reads all three
files, so both must have settled first. Adds no new production symbols to `internal/upgrade`
beyond test-scoped helpers; `releaseWorkflowRefPattern` and `verifyRelease` already exist and
are reused, not modified.

**Spec-less probe note:** the three `unresolved` probe rows (UPGR-01/02/03) are enumerated
in full as flagged assumptions in `04-01-PLAN.md`. This plan authors none of them and drops
none of them.

<tasks>

<task type="auto">
  <name>Task 1: Verify the prior release binary's signature before it is made executable</name>
  <files>Taskfile.yml</files>
  <read_first>
    - `Taskfile.yml` lines 2554-2750 (the whole `verify:self-upgrade` target — note the preconditions
      block, the `DL_DIR`/trap at 2708-2709, the `gh release download` at 2712-2713, the `chmod +x`
      at 2715, and the execution at 2724; the todo's own line citations are stale, the target has
      moved since it was filed)
    - `Taskfile.yml` lines 3040-3110 (`verify:notarized-suite` — the exact two-temp-dirs discipline,
      the single `cleanup()` trap covering all of them, the cosign invocation with its issuer and
      identity flags, the "only NOW does the file become executable" ordering comment, and the
      belt-and-braces existence/executability re-check after chmod; mirror this shape)
    - `Taskfile.yml` lines 2521-2547 (`verify:release-assets` — the same cosign flags and the
      two-temp-dirs comment explaining why the sidecar must not land next to the binary it signs)
    - `.planning/todos/pending/2026-08-09-verify-self-upgrade-download-then-execute-has-no-signature-check.md`
    - `.github/workflows/post-release-verify.yml` around line 280 (the job that invokes this target,
      and the recorded reason it deliberately does not declare `needs: verify-supply-chain`)
  </read_first>
  <action>
Add a `SIG_DIR="$(mktemp -d)"` alongside the existing `DL_DIR` in `verify:self-upgrade`, and
replace the single-directory trap with one `cleanup()` function removing both — the same
shape `verify:notarized-suite` uses. Both directories must exist before the trap is installed,
so a failure between the two `mktemp` calls cannot leak one of them.

Download the prior release's `${PRIOR_ASSET}.sigstore.json` into `SIG_DIR` with the same
`gh release download --pattern` form already used for the binary, into `PRIOR_SIG`. Hard-fail
with the established `echo "::error::…"` form, naming the expected bundle filename, if the
file is not present after the download — `gh release download` can exit 0 having matched
nothing, which is the precise failure that produced this repository's own v0.5.1 post-release
break.

Insert `cosign verify-blob` between the download and the existing `chmod +x`, with
`--bundle "${PRIOR_SIG}"`, `--certificate-oidc-issuer https://token.actions.githubusercontent.com`,
and the identity regexp exactly as `verify:release-assets` and `verify:notarized-suite`
already spell it — character for character, never a looser pattern. Echo a
`SELF-UPGRADE-VERIFY-EVIDENCE` line naming the prior tag, the bundle filename and the verdict,
in this file's established machine-parseable evidence style (fixed key order, sentinels never
omitted).

Move `chmod +x "${PRIOR_BIN}"` and the existing `PRIOR_SHA256` computation to strictly AFTER
the cosign call, and add the belt-and-braces re-check `verify:notarized-suite` carries: if the
binary is missing or not executable after download-plus-chmod, fail by name rather than
proceeding. Add a comment stating the ordering constraint and why it is independent of CI job
ordering: the `self-upgrade` job's deliberate lack of `needs: verify-supply-chain` is about
reporting independence and says nothing about execution safety, so the safety must live inside
this target.

Add a `cosign` precondition to the target's existing `preconditions:` block, matching the
style of the `gh` and `jq` entries — a missing cosign must fail with a named message before
anything is downloaded, not mid-target after a network fetch.
  </action>
  <verify>
    <automated>test "$(rg -n -e 'cosign verify-blob' -e 'chmod \+x "\$\{PRIOR_BIN\}"' Taskfile.yml | rg -c -e 'cosign verify-blob')" -ge 3 && node -e 'const fs=require("fs");const t=fs.readFileSync("Taskfile.yml","utf8");const s=t.indexOf("verify:self-upgrade:");const e=t.indexOf("verify:gatekeeper:");const b=t.slice(s,e);const c=b.indexOf("cosign verify-blob");const m=b.indexOf("chmod +x");if(c<0)throw new Error("no cosign verify-blob in verify:self-upgrade");if(m<0)throw new Error("no chmod +x in verify:self-upgrade");if(c>m)throw new Error("cosign verify-blob appears AFTER chmod +x");console.log("ordering OK: cosign at "+c+", chmod at "+m)'</automated>
  </verify>
  <acceptance_criteria>
    - Region-scoped ordering assertion (a file-wide grep cannot express this): within the
      `verify:self-upgrade` target body only — delimited by the `verify:self-upgrade:` and
      `verify:gatekeeper:` target keys — the offset of `cosign verify-blob` is strictly less than the
      offset of `chmod +x`. The `<automated>` command above is that assertion and throws by name on
      each of the three failure modes (missing cosign call, missing chmod, wrong order).
    - `rg -c -e 'cosign verify-blob' Taskfile.yml` reports at least 3 (release-assets,
      notarized-suite, self-upgrade), up from 2 before this task. Record both counts in the SUMMARY.
    - `rg -c -e 'SIG_DIR' Taskfile.yml` increases by at least 3 over its pre-task value, and
      `rg -c -e 'SELF-UPGRADE-VERIFY-EVIDENCE' Taskfile.yml` reports at least 1.
    - The target's `preconditions:` block gains a `cosign` entry:
      `rg -n -A 30 -e '^  verify:self-upgrade:' Taskfile.yml | rg -c -e 'command -v cosign'` reports
      exactly 1.
    - `task --list-all` exits 0 and lists `verify:self-upgrade` — the YAML still parses.
    - `go test ./internal/upgrade/ -run '^TestWorkflowRunBodiesInvokeTask$' -v 2>&1 | rg -c -e '--- PASS: TestWorkflowRunBodiesInvokeTask'`
      reports exactly 1 — `post-release-verify.yml` still invokes this target through `task`, not
      inline shell.
    - The cosign step's own RED-then-GREEN proof requires a real published release and network, and
      is NOT claimed by this task. Record that explicitly in the SUMMARY as a named gap: the next
      natural `post-release-verify.yml` run on a real tag is what first executes this path, and a
      broken bundle download or a mismatched identity will surface there. Do not write an assertion
      that pretends otherwise.
  </acceptance_criteria>
  <done>`verify:self-upgrade` downloads the prior release's cosign bundle into its own temp directory, verifies the binary against the project's identity policy, and only then makes it executable — with a region-scoped ordering assertion proving the sequence and the unexecuted-path gap recorded by name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One release-identity policy, proved by behavioural equality across every restatement</name>
  <files>internal/upgrade/taskfile_shape_test.go</files>
  <read_first>
    - `internal/upgrade/verify.go` lines 20-50 (`releaseWorkflowRefPattern`'s definition, its
      full-match anchoring rationale (WR-08), and `releaseRepoSlug`)
    - `internal/upgrade/verify_test.go` lines 115-140 (the existing accept/reject assertions against
      `releaseWorkflowRefPattern` — the SAN strings used there are the seed corpus for this test)
    - `internal/upgrade/release_workflow_shape_test.go` lines 860-910 (the existing test that builds
      release.yml's real tag-push SAN and asserts the compiled pattern accepts it and rejects a
      different workflow filename and a branch ref — reuse those three cases verbatim)
    - `internal/upgrade/taskfile_shape_test.go` lines 20-40 (the path constants block) and the
      `TestTaskfileGatesFailLoud` / `TestTaskfileGatesFailLoud_EmptyFileIsError` pair, which is the
      established idiom for a shape test plus its own vacuity self-test
    - `Taskfile.yml`, `README.md`, `docs/RELEASE.md` — the four current restatements of the identity
      regexp, as they stand after plans 04-02 and 04-04
  </read_first>
  <behavior>
    - Test: the extractor finds at least 4 identity-regexp literals across Taskfile.yml, README.md
      and docs/RELEASE.md; fewer is a hard failure, not a pass.
    - Test: every extracted literal compiles as a Go regexp.
    - Test: for every SAN in the corpus, each extracted literal's match result equals
      `releaseWorkflowRefPattern`'s match result.
    - Test: the corpus contains at least 2 SANs the compiled pattern accepts and at least 4 it
      rejects, asserted before the comparison loop runs.
    - Self-test: given an empty input, the extractor returns zero literals and the caller treats that
      as an error.
  </behavior>
  <action>
Add `extractCosignIdentityLiterals(contents string) []string` to
`internal/upgrade/taskfile_shape_test.go`: scan for the single-quoted regexp literal that
follows a `--certificate-identity-regexp` occurrence (the literal is on its own continuation
line in every current site) and return each one with the surrounding single quotes stripped.
Return an empty slice rather than panicking on unparseable input — the caller decides whether
zero is an error, which is what makes the self-test possible.

Add `cosignIdentitySANCorpus`, a fixed table of certificate subjects with the expected verdict
under this project's policy. Seed it from the SANs the existing tests already use: the real
tag-push SAN for both `release.yml` and `release.yaml`; a different workflow filename in the
same repository at a tag ref; `release.yml` at a branch ref; a different repository owner at a
tag ref; and the real SAN with a trailing space-plus-token, which the full-match anchoring must
reject.

Add `TestCosignIdentityPolicyMatchesCompiledPattern`. Read `Taskfile.yml`, `README.md` and
`docs/RELEASE.md`; extract the literals from each; `t.Fatalf` if the total is below 4, naming
the count found and the files searched — a guard whose input scan came up empty must fail
loudly, because a loop over an empty match set with only per-item assertions passes vacuously.
`t.Fatalf` as well if the corpus itself carries fewer than 2 accepted or fewer than 4 rejected
SANs, so the comparison cannot be satisfied by a degenerate corpus. Then, for each extracted
literal and each corpus SAN, compile the literal with `regexp.MustCompile` and require its
`MatchString` result to equal the compiled `releaseWorkflowRefPattern`'s result; on a mismatch,
`t.Errorf` naming the source file, the literal, the SAN and both verdicts.

Behavioural equality, not string equality, is deliberate: the Go pattern spells the non-space
class one way and the shell/POSIX restatements spell it another, and both are correct. A
literal string comparison would fail on a legitimate difference and would still miss a
semantic drift written in the same spelling.

Add `TestCosignIdentityPolicyMatchesCompiledPattern_ZeroLiteralsIsError`, the vacuity self-test,
in the same shape as `TestTaskfileGatesFailLoud_EmptyFileIsError`: call the extractor with an
empty string and with a string containing the flag name but no quoted literal, and assert it
returns zero literals in both cases — pinning that the main test's `t.Fatalf` guard is reachable.
  </action>
  <verify>
    <automated>test "$(go test ./internal/upgrade/ -run '^TestCosignIdentityPolicy' -v 2>&1 | rg -c -e '^--- PASS: TestCosignIdentityPolicy')" -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/upgrade/ -run '^TestCosignIdentityPolicy' -v` prints exactly 2 top-level
      `--- PASS: TestCosignIdentityPolicy` lines (count-asserted — `go test -run` exits 0 on a
      pattern that matches nothing, so an exit-status-only criterion would be vacuous here).
    - The test's own log records the number of literals found and the files they came from; that
      number is at least 4. Capture the verbose output and record the number in the SUMMARY.
    - `rg -c -e 'releaseWorkflowRefPattern' internal/upgrade/taskfile_shape_test.go` reports at least
      1 — the test compares against the compiled pattern rather than a second hand-typed copy.
    - `rg -c -e 'regexp' internal/upgrade/taskfile_shape_test.go` reports at least 1 and
      `go vet ./internal/upgrade/` exits 0.
    - `go test ./internal/upgrade/...` exits 0.
  </acceptance_criteria>
  <done>Every identity-regexp restatement in the repository is proved to accept and reject exactly what the compiled upgrade-path policy does, the guard fails loudly on an empty match set, and the vacuity self-test pins that guard as reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Demonstrate the agreement guard RED twice, then file the todo</name>
  <files>internal/upgrade/taskfile_shape_test.go, Taskfile.yml, .planning/todos/pending/2026-08-09-verify-self-upgrade-download-then-execute-has-no-signature-check.md</files>
  <read_first>
    - `internal/upgrade/taskfile_shape_test.go` as written by Task 2
    - `Taskfile.yml` as written by Tasks 1 and 2
    - `.planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md` — the established form for recording a
      confirmed-applied, byte-clean-reverted mutation (mutation applied, verdict observed, revert
      proved by `git diff --exit-code`)
    - `.planning/todos/pending/2026-08-09-verify-self-upgrade-download-then-execute-has-no-signature-check.md`
  </read_first>
  <action>
Mutation 1 — semantic drift. Widen one `--certificate-identity-regexp` literal in `Taskfile.yml`
so it accepts a branch ref as well as a tag ref (a realistic loosening, not a syntax break).
Run `go test ./internal/upgrade/ -run '^TestCosignIdentityPolicyMatchesCompiledPattern$' -v` and
capture the failure verbatim: it must name the source file, the differing SAN and both verdicts.
Revert with `git checkout -- Taskfile.yml` and prove the revert with `git diff --exit-code Taskfile.yml`.

Mutation 2 — vacuity. Temporarily point the test's file list at a path with no identity literals
(or delete all of them from a scratch copy) and confirm the guard `t.Fatalf`s on the literal
count rather than passing with an empty loop. Capture that failure verbatim too. Revert and
prove it byte-clean.

Re-run the full package green after both reverts. A gate is not trusted in this repository until
it has been observed RED against a confirmed-applied mutation, and both mutations above are
confirmed-applied by their captured failure text, not by the fact that they were typed.

Record both observations in the SUMMARY with the applied mutation, the verbatim failure output,
and the `git diff --exit-code` proof of a clean revert.

Then move
`.planning/todos/pending/2026-08-09-verify-self-upgrade-download-then-execute-has-no-signature-check.md`
to `.planning/todos/completed/` with `git mv` and append a resolution note that is honest about
what was and was not proved: the cosign step is wired and its ordering is asserted structurally;
the drift guard is demonstrated RED twice; the end-to-end RED proof against a tampered prior
release binary is NOT claimed and first executes on the next natural release. Do not touch the
other pending todos.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/... 2>&1 | rg -q -e '^ok' && git diff --exit-code Taskfile.yml internal/upgrade/taskfile_shape_test.go && test "$(ls .planning/todos/pending/ | wc -l | tr -d ' ')" -eq 7</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains the verbatim failure output of both mutations, each showing the assertion
      that fired and the values it named. A description of a mutation without its captured output does
      not satisfy this.
    - Both mutations are proved reverted byte-clean:
      `git diff --exit-code Taskfile.yml internal/upgrade/taskfile_shape_test.go` exits 0 at task end.
    - `go test ./internal/upgrade/...` exits 0 after the reverts, and
      `go test ./internal/upgrade/ -run '^TestCosignIdentityPolicy' -v 2>&1 | rg -c -e '^--- PASS: TestCosignIdentityPolicy'`
      reports 2.
    - `ls .planning/todos/pending/ | wc -l` reports 7 (down from 8 after plan 04-02's two closures),
      and `ls .planning/todos/completed/ | rg -c -e 'no-signature-check'` reports 1.
    - The resolution note names, in the todo file itself, the one thing that is not proved: the
      end-to-end verification of a tampered prior-release binary. A resolution note claiming a full
      RED-then-GREEN for the cosign step would be false and fails this criterion.
    - `task test` exits 0.
  </acceptance_criteria>
  <done>The drift guard has been observed failing on a real semantic loosening and on an empty match set, both mutations are reverted byte-clean, the suite is green, and the todo is filed with an honest statement of what remains unexecuted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases (network) -> CI runner execution | `verify:self-upgrade` fetches a binary over the network and executes it on the runner. Everything downstream of that execution runs with the runner's privileges and secrets. |
| compiled identity policy -> its hand-typed restatements | `releaseWorkflowRefPattern` is enforced in-process by `codegraph upgrade`; four hand-typed copies enforce or publish the same claim with nothing asserting they agree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Elevation of Privilege | `verify:self-upgrade` download-then-execute | high | mitigate | A network-fetched, unverified binary is made executable and run on a runner that holds release-adjacent credentials, with no ordering guarantee from any sibling job. Mitigated by Task 1's `cosign verify-blob` placed strictly above the `chmod +x`, proved by a region-scoped ordering assertion rather than a file-wide grep. |
| T-04-16 | Spoofing | identity-regexp drift | high | mitigate | A single restatement loosened to accept a branch ref, a different workflow, or a different owner would silently accept an artifact `codegraph upgrade` itself would reject — a verification that proves less than the published instruction claims. Mitigated by Task 2's behavioural-equality test against the compiled pattern, demonstrated RED in Task 3 against exactly that loosening. |
| T-04-17 | Tampering | bundle download | medium | mitigate | `gh release download --pattern` can exit 0 having matched nothing, leaving cosign pointed at an absent bundle — this repository's own v0.5.1 failure. Mitigated by the explicit post-download existence check that fails by name. |
| T-04-18 | Repudiation | unexecuted RED proof | medium | accept | The end-to-end RED — a tampered prior-release binary observed failing the new step — requires a real published release and network, and is not claimed. Accepted and named in both the SUMMARY and the filed todo, rather than papered over with a weaker assertion presented as the same thing (D-13's third question: the next natural release run is what exercises it). |
| T-04-SC | Tampering | dependency surface | low | mitigate | No package-manager install occurs in this plan; cosign is already a documented CI tool and is added as a precondition, not a dependency. Asserted by `git diff --exit-code go.mod go.sum` being clean at plan completion. |
</threat_model>

<verification>
- `task --list-all` exits 0.
- `go test ./internal/upgrade/...` exits 0.
- `task test` exits 0.
- `git diff --exit-code go.mod go.sum` exits 0.
- The region-scoped ordering assertion in Task 1's `<automated>` passes.
</verification>

<success_criteria>
- The prior release binary is signature-verified before it is executable, with the ordering
  asserted structurally.
- Every identity-regexp restatement is proved behaviourally identical to the compiled policy.
- The drift guard was observed RED twice and reverted byte-clean.
- The folded todo is filed with an explicit statement of what remains unexecuted.
</success_criteria>

<output>
Create `.planning/phases/04-codegraph-upgrade-homebrew/04-05-SUMMARY.md` when done.
</output>
