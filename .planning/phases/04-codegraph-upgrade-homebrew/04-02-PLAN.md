---
phase: 04-codegraph-upgrade-homebrew
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .goreleaser.yaml
  - Taskfile.yml
  - .planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md
  - .planning/todos/pending/2026-08-10-03-evidence-falsely-claims-a-failed-install-can-strand-the-phase-4-sentinel.md
  - .planning/todos/pending/2026-08-10-post-install-man-page-assertion-can-be-satisfied-by-stale-pages.md
autonomous: true
requirements: [UPGR-02]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The cask's post-install hook no longer writes a marker file into the Caskroom, and the post-uninstall hook no longer tries to remove one (D-02)."
    - "BREW-05's two install-gate assertions — more than one man page, and reported version equals declared version — survive the removal completely unchanged (D-02, 03-CONTEXT D-11)."
    - "The post-install man-page assertion distinguishes 'this install wrote pages' from 'pages happen to be present', so residue from a prior failed install can no longer satisfy it (UF-5, repo rule 84d1gfpywd)."
    - "The freshness assertion still passes on `brew reinstall` — the rehearsal's idempotency step stays green (03-02 D-11)."
    - "Step 5d's reinstall-freshness assertion is measured against a marker created immediately before `brew reinstall`, so pages written by the FIRST install cannot satisfy it and the check can actually go RED when a reinstall writes nothing (review cycle 3, MEDIUM). Each marker has exactly one creation point and exactly one freshness consumer: pre-install ⇒ Step 5, pre-reinstall ⇒ Step 5d."
    - "`release:rehearse-cask` asserts no marker file and still positively asserts every non-marker install effect it asserted before."
    - "03-EVIDENCE.md no longer claims a failed install can strand a marker file, and states what actually leaks (man pages) with checkable citations."
  artifacts:
    - .goreleaser.yaml
    - Taskfile.yml
  key_links:
    - "`.goreleaser.yaml` hooks.post.install is the only mechanism that can fail a `brew install` for the first user (Cask::Installer re-raises system_command failures); everything this plan removes sits below that gate, and everything this plan adds sits inside it."
    - "`Taskfile.yml`'s release:rehearse-cask is the single definition of the cask rehearsal CI body; a gate removed from the hook must be removed here too, and a gate added to the hook must be independently re-asserted here."
  prohibitions:
    - "MUST NOT weaken, reorder, or delete either of BREW-05's two post-install assertions while removing the marker-file machinery."
    - "MUST NOT replace the removed marker-file assertion with an assertion about Homebrew's own rollback behaviour — that is Homebrew's code, not ours (D-13)."
---

<objective>
Remove the Phase-3 marker file and its four assertion sites now that Phase 4's detection
is structural (D-02), and close the folded UF-5 defect in the same hook while it is open.

Purpose: the marker file existed for exactly one consumer — Phase 4's detection — and
Phase 4 chose structure instead (D-03). Leaving it would be dead weight that a future
reader mistakes for a live contract, and a second signal whose only effect is to hide a
structural bug behind it on every real install. The same `hooks.post.install` block also
carries UF-5: its man-page assertion globs a shared directory with no baseline, so
residue from a prior failed install satisfies it vacuously.

Output: a `.goreleaser.yaml` cask block with no marker file and a freshness-based
man-page assertion, a `release:rehearse-cask` target with no marker assertions and an
independent freshness re-check, and a corrected `03-EVIDENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-CONTEXT.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-PATTERNS.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-RESEARCH.md
</context>

## Artifacts this phase produces

New symbols and identifiers introduced by this plan (none exist today):

| Artifact | Kind | File |
|---|---|---|
| `man_pages_before` | Ruby local in `hooks.post.install` (path→mtime baseline snapshot; replaces the cycle-1 `hook_started_at` wall-clock local) | `.goreleaser.yaml` |
| `fresh_man_pages` | Ruby local in `hooks.post.install` | `.goreleaser.yaml` |
| `MAN_BASELINE_MARKER` | shell variable in `release:rehearse-cask` (pre-INSTALL mtime marker; consumed ONLY by Step 5) | `Taskfile.yml` |
| `MAN_REINSTALL_MARKER` | shell variable in `release:rehearse-cask` (pre-REINSTALL mtime marker; consumed ONLY by Step 5d — added in review cycle 3, MEDIUM) | `Taskfile.yml` |
| `FRESH_MAN_PAGE_COUNT` | shell variable in `release:rehearse-cask` | `Taskfile.yml` |
| `CASK-REHEARSE-EVIDENCE schema=3` | evidence-line schema revision | `Taskfile.yml` |

Removed identifiers (deletions, listed so a reviewer does not read their absence as drift):
`SENTINEL_PATH`, `SENTINEL_VERDICT`, `SENTINEL_FIRST_LINE`, `REAL_BIN`, `REAL_BIN_DIR`
(`Taskfile.yml`); `real_bin_dir` and the `sentinel` Ruby locals (`.goreleaser.yaml`).

**Spec-less probe note:** the three `unresolved` probe rows (UPGR-01/02/03) are enumerated
in full as flagged assumptions in `04-01-PLAN.md`. This plan authors none of them and drops
none of them.

<!-- planner-discipline-allow: codegraph-brew-install -->
<!-- planner-discipline-allow: SENTINEL_PATH -->
<!-- planner-discipline-allow: SENTINEL_VERDICT -->
<!-- planner-discipline-allow: Time.now -->

<review_dispositions>
## Cross-AI review cycle 1 — findings landing in this plan

| # | Severity | Finding | Disposition and location |
|---|---|---|---|
| 6 | HIGH | Task 2's `go test … -run '^TestTaskfile\|^TestWorkflowRunBodiesInvokeTask$' \| rg -q 'ok\|PASS'` is a confirmed vacuous pass — `go test -run` exits 0 and prints `ok … [no tests to run]` on an empty match set, and `rg -q 'ok'` matches that literal | **Incorporated.** Task 2's `<automated>` and `<acceptance_criteria>` now carry TWO independent count-asserted named-PASS gates: `^--- PASS: TestTaskfile` at least 5 and `^--- PASS: TestWorkflowRunBodiesInvokeTask` exactly 1. Measured baselines recorded inline (5 and 1 as of 2026-08-11) |
| 14 | MEDIUM | Task 1's `^TestGoreleaser` gate is mis-scoped: at package scope it matches exactly one test, `TestGoreleaserPinParity`, which reads `tools/go.mod` and `release.yml` and nothing in `.goreleaser.yaml`, so the criterion's gloss is false and the gate is green regardless of what Task 1 does | **Incorporated.** Retargeted to `^TestHomebrewCask` (5 tests in `goreleaser_shape_test.go`, including `TestHomebrewCaskHooksHaveStructuralProperties` at `:1183`, which does parse the hook block), the gloss corrected, and the measured baseline of 5 recorded |
| 11 | MEDIUM | `hook_started_at = Time.now - 1` is a weak freshness oracle — a one-second window admits a page written by a previous run, and it is sensitive to filesystem timestamp precision | **Incorporated.** Replaced with a per-path before/after snapshot (`man_pages_before`, a path→mtime Hash) of the exact `codegraph*.1` set; a page is fresh when it is newly present OR its mtime advanced. Acceptance now additionally asserts `Time.now` is absent from `.goreleaser.yaml`, so the wall-clock oracle is provably gone rather than merely supplemented. The coarse-granularity residual is named in the action and backstopped by the rehearsal's independent `find -newer` probe |
| 18 | LOW | Task 2 names two different marker-creation points ("immediately BEFORE the `brew install --cask` invocation in Step 4" vs "created immediately before the reinstall") | **Incorporated.** One creation point fixed by name — pre-install in Step 4, reused by the Step 5d reinstall check — stated in BOTH sentences, with an acceptance gate asserting exactly one `mktemp` assignment paired with a floor of at least 4 total references so the "exactly 1" cannot pass by deletion |

## Cross-AI review cycle 2 — findings landing in this plan

| # | Severity | Finding | Disposition and location |
|---|---|---|---|
| C2-2 | MEDIUM | The cycle-1 fix to finding #11 promised a man-page SIZE fallback that cannot work: `man_pages_before` was specified as path→`File.mtime` only, so there was no baseline size to compare against and the residual the plan claimed to handle was not handled | **Incorporated.** Task 1's snapshot now records `{mtime:, size:}` per path, and freshness is a three-way test — newly present, mtime advanced, OR size differs. A new acceptance criterion asserts `File.size` appears at least twice in `.goreleaser.yaml` (snapshot plus comparison) and the `<automated>` command carries that leg, so the fix is executable rather than prose-only. The genuinely irreducible residual — a rewrite byte-identical in both length and timestamp — is now named as such instead of being presented as covered |
| C2-3 | LOW | Task 1 scopes Edit 2's deletion to `.goreleaser.yaml:570-588` and says "delete nothing above line 570", but `atomic_write` also occurs in a trailing comment at `:592`, outside that range, while the acceptance gate requires a count of 0 | **Incorporated.** Edit 5 now explicitly owns removing the `:592` mention in the same pass that rewrites the surrounding idempotency comment, and the acceptance criterion names both occurrences and which edit owns each. The `atomic_write == 0` leg is additionally promoted into the `<automated>` command so the gate runs rather than only being described |

## Cross-AI review cycle 3 — findings landing in this plan

| # | Severity | Finding | Disposition and location |
|---|---|---|---|
| C3-2 | MEDIUM | Step 5d's reinstall-freshness assertion is pre-satisfied by the FIRST install's own output. The marker was created at exactly one point — before the first `brew install --cask` — and the plan explicitly forbade a second ("Create no second marker" / "never create a second marker", citing cycle-1 LOW). Pages written by the first install are already newer than that marker, so if the reinstall writes nothing they remain and the check passes anyway. The plan's own justification — "reusing the pre-install marker is strictly stronger, because a reinstall rewrites the pages" — assumed the property under test. Not *unconditionally* vacuous, because `brew reinstall --cask` fires `.goreleaser.yaml:621`'s unconditional `rm_f` glob first; but that makes its ability to fire contingent on an unstated assumption about a hook THIS PHASE IS EDITING — the exact `84d1gfpywd` shape | **Incorporated by splitting one marker into two, each with exactly one creation point and exactly one freshness consumer.** `MAN_BASELINE_MARKER` is still created once, immediately before the first `brew install --cask`, and is now consumed ONLY by Step 5's `FRESH_MAN_PAGE_COUNT` check — where a single pre-install marker is correct and RED-able, because a genuinely stale page predating the rehearsal is older than it. `MAN_REINSTALL_MARKER` is created once, immediately before `brew reinstall --cask`, and is consumed ONLY by Step 5d — so a reinstall that writes nothing leaves zero pages newer than it and Step 5d goes RED on its own evidence, with no dependence on what the uninstall hook's glob happens to do. The blanket "never create a second marker" instruction is LIFTED and replaced by the per-marker one-creation/one-consumer rule, which is what cycle-1 LOW actually needed (its concern was an executor making an *unspecified* extra marker or misplacing the first, not the count itself). Cycle-1 LOW's paired gate shape survives per marker: exactly one `mktemp` assignment each, floored by a reference count so "exactly 1" cannot pass by deletion |
| C3-1 / C3-3 | HIGH / LOW | (land in `04-06`) | Recorded there; neither touches this plan |

**One threshold moved in cycle 3, named here so it is visible rather than buried.** Task 2's
`rg -c -e 'MAN_BASELINE_MARKER' Taskfile.yml` floor drops **4 → 3**, because the second marker
takes over Step 5d and the first marker's honest reference inventory becomes creation + Step-5
`find -newer` consumer + cleanup. `MAN_REINSTALL_MARKER` gets the same `-ge 3` shape for its own
inventory. This is a *reference-inventory* count local to the marker redesign this finding
mandates, not one of the do-not-regress floors — cosign `-ge 5`, identity `-ge 7`, named-PASS
5/1/5, detection-table `-ge 16`, `File.size` `-ge 2`, `fresh_man_pages` `-ge 2`,
`FRESH_MAN_PAGE_COUNT` `-ge 3`, `schema=3` `-eq 2` and the todo counts are all untouched. Both
new floors are RED today: live `rg -c 'MAN_BASELINE_MARKER' Taskfile.yml` and
`rg -c 'MAN_REINSTALL_MARKER' Taskfile.yml` are **0**. Padding the count back to 4 with a
contrived extra reference was considered and rejected — inventing a reference to preserve a
number is exactly the brittle-count swap the review process keeps flagging.

## Cross-AI review cycle 6 — findings landing in this plan

Cycle 5 returned `current_high=0`; cycle 6 is the final pass and carries exactly two MEDIUMs
phase-wide, one of them here (its sibling half lands in `04-05`, and the other lands in `04-06`).
**No task was added, removed or renumbered; the phase stays at 6 plans / 4 waves / 15 tasks / 15
VALIDATION rows; no numeric floor or comparison operator was raised or lowered** — a count was
replaced by named set membership, which is not a threshold change.

| # | Severity | Finding | Disposition and location |
|---|---|---|---|
| C6-2 | MEDIUM | Task 3's `test "$(ls .planning/todos/pending/ \| wc -l \| tr -d ' ')" -eq 8` asserts a DIRECTORY TOTAL where set membership is meant. **The arithmetic is correct and was not broken** — this plan is wave 1 and closes 2 of the 10 live pending todos, so 8 is the right post-state, and `04-05`'s wave-3 `-eq 7` follows from it; an earlier report that these gates were guaranteed-RED compared a post-state gate against the current pre-state and was withdrawn. The defect is that the gate measures the WRONG QUANTITY: a total couples it to every unrelated `/gsd-capture` between planning and execution, and commit `ffad9ee` on this branch is exactly that event — one unrelated todo capture flips the gate RED for a reason that has nothing to do with this task's work, and a compensating edit to the number teaches nobody anything | **Incorporated by replacing the total with per-name set membership.** For each of the two todos this task folds, the `<automated>` command now asserts the file is ABSENT from `pending/` and PRESENT exactly once in `completed/`, naming the offending file on failure — a negative leg paired with its positive (repo rule `84d1gfpywd`). The totals (10 → 8) are demoted to a recorded SUMMARY observation and are no longer a gate. The acceptance criterion's old `ls … completed/ \| rg -c -e 'stale-pages' -e 'strand-the-phase-4-sentinel'` leg goes with it, because a multi-`-e` count of 2 is the cycle-1 set-membership defect in its original form: two lines naming the same todo satisfy it. The action's "do not touch the eight other pending todos" is restated by exclusion for the same reason. Mirrored into `04-VALIDATION.md` row `04-02-03`. **This is the cycle-1 `rg -c` set-membership lesson applied to a directory listing: assert the members you care about by name, not a count that any unrelated change perturbs.** |
</review_dispositions>

<tasks>

<task type="auto">
  <name>Task 1: Strip the marker file from the cask hooks and make the man-page assertion prove freshness</name>
  <files>.goreleaser.yaml</files>
  <read_first>
    - `.goreleaser.yaml` lines 412-460 (the `homebrew_casks:` discretionary-metadata comment block;
      line 451 carries the now-falsified claim about how Phase 4 detects)
    - `.goreleaser.yaml` lines 524-594 (`hooks.post.install` — lines 542-568 are BREW-05's gate and
      must survive byte-identical in behaviour; 570-588 are the marker write to delete; 589-594 are
      the idempotency comment that must be updated to stop naming the deleted step)
    - `.goreleaser.yaml` lines 595-625 (`hooks.post.uninstall` — 620-621 are the man-page cleanup
      that stays, 623-624 are the removal to delete)
    - `.planning/todos/pending/2026-08-10-post-install-man-page-assertion-can-be-satisfied-by-stale-pages.md`
      (the full UF-5 statement and its three candidate fix shapes)
    - `.planning/phases/04-codegraph-upgrade-homebrew/04-PATTERNS.md` § `.goreleaser.yaml`
  </read_first>
  <action>
Before editing, re-run the search RESEARCH.md's Assumption A3 recorded as a point-in-time
result: `rg -n -e 'sentinel' -e 'brew-install' internal/upgrade/goreleaser_shape_test.go
internal/upgrade/taskfile_shape_test.go`. If it now returns matches, those assertions must
be updated in this same task rather than left pinning a block that no longer exists; record
the outcome of the search in the SUMMARY either way.

Edit 1 — the stale comment at line 451. It currently attributes Phase 4's detection to the
marker file, which D-02 falsifies. Rewrite that one line to say that codegraph's own
self-updater refuses under a brew-managed install, detected structurally from the resolved
install path plus Homebrew's own install receipt. Leave the surrounding `auto_updates`
context intact — the point of that bullet is that the stanza is not settable through this
pipe, and that is still true.

Edit 2 — delete the marker write from `hooks.post.install` in full: the explanatory comment
block, the `real_bin_dir` assignment, the `sentinel` assignment, and the entire
`atomic_write` heredoc (lines 570-588). Delete nothing above line 570. BREW-05's two
`raise` assertions are the entire permanent standing guard for this cask and are out of
scope for removal.

Edit 3 — delete the removal from `hooks.post.uninstall`: the `sentinel` assignment and its
`FileUtils.rm_f` call (lines 623-624). Keep the `man_dir` glob-and-remove lines above them.
Trim the block's leading comment so it no longer describes a two-removal symmetry or the
`staged_path` resolution that existed only to locate the deleted file; it must still explain
why the man pages are removed explicitly (brew does not track hook-written files).

Edit 4 — close UF-5 inside `hooks.post.install` with a BEFORE/AFTER SNAPSHOT of the exact
`codegraph*.1` set, not a wall-clock comparison.

Immediately before the existing `system_command binary, args: ["man", man_dir]` line, take the
baseline snapshot: glob `#{man_dir}/codegraph*.1` and build `man_pages_before`, a Hash mapping
each existing path to **BOTH its `File.mtime` AND its `File.size`** — a two-key Hash per path,
not a bare mtime (review cycle 2, MEDIUM: the cycle-2 version stored `File.mtime` alone while
promising a size fallback below, so the residual it claimed to handle was not handled — there
was no baseline size to compare against). Missing pages are simply absent from the Hash — their
absence is what makes a newly-created page detectable.

After the existing glob (`man_pages`), compute `fresh_man_pages` as the subset of `man_pages`
for which ANY of the following holds: the path is absent from `man_pages_before` (the page was
created by this run); `File.mtime(p)` is strictly greater than the recorded mtime (the page was
rewritten by this run); or `File.size(p)` differs from the recorded size (the page was rewritten
within the same timestamp tick — the coarse-granularity case, now actually implementable because
the size was recorded). Raise when that subset is empty, with a message naming the man directory,
the before count, the total count and the fresh count. Keep the existing `man_pages.length <= 1`
raise exactly as it is — the two assertions answer different questions and both are wanted.

The snapshot replaces `Time.now`-based freshness (review cycle 1, MEDIUM): a `hook_started_at =
Time.now - 1` oracle carries a one-second window in which a page written by a PREVIOUS run
counts as fresh, and it is additionally sensitive to filesystem timestamp precision and to clock
adjustment. A per-path before/after comparison of the exact `codegraph*.1` set has no wall-clock
slack at all and no dependence on timestamp granularity for the created-page case, which is the
dominant one. Record that reasoning in the hook comment.

Freshness rather than growth is deliberate and load-bearing: a `brew reinstall` rewrites the
same pages, so a count-grew assertion would break the idempotency the rehearsal's Step 5d
requires, while the snapshot comparison passes on reinstall (the pages are rewritten, so their
mtimes advance) and still fails on the exact case UF-5 names — a binary that ran, exited 0, and
wrote nothing over a directory of residue. Record that reasoning in the hook comment too, so a
future reader does not "simplify" it back to a count comparison.

**Residual, stated rather than hidden — and now actually handled, not merely promised**
(review cycle 2, MEDIUM): if a filesystem's mtime granularity is coarse enough that a rewrite
within the same tick leaves the mtime byte-identical, a pure-rewrite reinstall could read as
not-fresh. The size leg of the three-way freshness test above is what handles it, and it works
ONLY because the snapshot records size alongside mtime — the cycle-2 draft claimed this fallback
while snapshotting mtime alone, which made the claim unimplementable as specified. Name the
remaining residual honestly in the hook comment: a rewrite that is byte-identical in both length
and timestamp is indistinguishable from no rewrite at all, and no metadata comparison can close
that; the rehearsal's independent Step 5 re-check (Task 2) uses `find -newer` against a real
marker file and is the second, independent probe of the same property.

Edit 5 — update the idempotency comment at the tail of `hooks.post.install` so it no longer
names the deleted write step, and so it states that the freshness assertion is itself
reinstall-safe. **This edit also OWNS the remaining `atomic_write` mention in that trailing
comment at line 592** (review cycle 2, LOW): Edit 2's deletion range is scoped to lines 570-588,
but `atomic_write` occurs twice in the file — once inside that range at `:579` and once in this
trailing comment at `:592`, outside it — while this task's acceptance gate requires
`rg -c 'atomic_write' .goreleaser.yaml` to report 0. The gate is self-correcting, but leaving the
second occurrence unowned sends the executor hunting for which edit was supposed to cover it.
Edit 5 removes it, in the same pass that rewrites the comment around it.
  </action>
  <verify>
    <automated>test "$(rg -c -e 'codegraph-brew-install' .goreleaser.yaml || echo 0)" -eq 0 && test "$(rg -c -e 'atomic_write' .goreleaser.yaml || echo 0)" -eq 0 && test "$(rg -c -e 'system_command binary, args: \["man", man_dir\]' .goreleaser.yaml)" -eq 1 && test "$(rg -c -e 'fresh_man_pages' .goreleaser.yaml)" -ge 2 && test "$(rg -c -e 'File\.size' .goreleaser.yaml)" -ge 2 && task check:goreleaser</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c -e 'codegraph-brew-install' .goreleaser.yaml` reports 0 matches, AND
      `rg -c -e 'system_command binary, args: \["man", man_dir\]' .goreleaser.yaml` reports exactly 1
      and `rg -c -e 'raise "codegraph cask post-install' .goreleaser.yaml` reports at least 3. The
      positive counts make the negative non-vacuous: the file still exists, still contains the cask
      hook, and still carries BREW-05's raises plus the new one.
    - `rg -c -e 'atomic_write' .goreleaser.yaml` reports 0 matches and `rg -c -e 'FileUtils.rm_f' .goreleaser.yaml`
      reports exactly 1 (the man-page cleanup survives; the marker removal is gone). Both occurrences
      are explicitly owned by an edit (review cycle 2, LOW): Edit 2 removes the one at `:579` inside
      its 570-588 range, and Edit 5 removes the one in the trailing comment at `:592`, which lies
      outside that range.
    - The freshness snapshot records SIZE as well as mtime, so the size fallback the hook comment
      claims is actually implementable (review cycle 2, MEDIUM):
      `rg -c -e 'File\.size' .goreleaser.yaml` reports at least 2 — one in the baseline snapshot and
      one in the freshness comparison — paired with the existing `man_pages_before` assertions, so
      the count cannot be met by an unrelated size check elsewhere in the file. A hook that snapshots
      `File.mtime` alone while its comment promises a size fallback fails this criterion.
    - The baseline snapshot is taken BEFORE the generation runs, proved by a scripted offset
      assertion rather than by comparing line numbers by eye:
      `node -e 'const s=require("fs").readFileSync(".goreleaser.yaml","utf8");const b=s.indexOf("man_pages_before");const g=s.indexOf("system_command binary, args: [\"man\", man_dir]");if(b<0)throw new Error("no man_pages_before snapshot");if(g<0)throw new Error("no man generation call");if(b>g)throw new Error("snapshot taken AFTER generation");console.log("ok")'`
      exits 0. AND `rg -c -e 'Time\.now' .goreleaser.yaml` reports 0 — the wall-clock oracle is gone,
      not merely supplemented (review cycle 1, MEDIUM).
    - `rg -n -e "D-08's sentinel" .goreleaser.yaml` reports no match, AND
      `rg -c -e 'auto_updates' .goreleaser.yaml` still reports at least 1 — the falsified claim is
      gone and its surrounding context is not.
    - `task check:goreleaser` exits 0 (the config still validates against the pinned GoReleaser).
    - The shape tests that actually pin THIS file's cask-hook invariants still pass:
      `go test ./internal/upgrade/ -run '^TestHomebrewCask' -v 2>&1 | rg -c -e '^--- PASS: TestHomebrewCask'`
      reports at least 5, and `go test ./internal/upgrade/...` exits 0.
      Retargeted in review cycle 1 (MEDIUM, mis-scoped coverage claim): the previous pattern
      `^TestGoreleaser` is package-scoped and matches exactly ONE test, `TestGoreleaserPinParity`
      (`internal/upgrade/taskfile_shape_test.go:761`), which reads `tools/go.mod` and
      `.github/workflows/release.yml` for version-pin parity and reads NOTHING in `.goreleaser.yaml` —
      so it was green before and after this task regardless of what happened to the cask hooks, and
      the criterion's gloss was false. `^TestHomebrewCask` reaches the five tests in
      `internal/upgrade/goreleaser_shape_test.go` that do parse this file, including
      `TestHomebrewCaskHooksHaveStructuralProperties` (`:1183`), which is the one that pins the hook
      block. Measured baseline 2026-08-11: 5 PASS.
  </acceptance_criteria>
  <done>The cask writes and removes no marker file; BREW-05's gate is unchanged; the man-page assertion fails when the install produced no fresh pages and passes on reinstall; the falsified line-451 claim is corrected.</done>
  <reversibility rating="costly">CONTEXT.md D-02 rates re-introducing a marker file costly: every already-installed user would be undetectable-by-marker until they reinstall. Structural detection (plan 04-01) has no such dependency, which is part of why it wins. Flagged, not gated — no checkpoint required.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Remove the rehearsal's marker assertions and re-assert man-page freshness independently</name>
  <files>Taskfile.yml</files>
  <read_first>
    - `Taskfile.yml` lines 1694-1745 (the `release:rehearse-cask` cleanup trap and its fail-path
      `CASK-REHEARSE-EVIDENCE` echo at line 1738 — note it is already `schema=1` while the pass path
      emits `schema=2`, a pre-existing inconsistency this task resolves)
    - `Taskfile.yml` lines 1956-2096 (Step 5's positive install assertions, Step 5b's marker
      assertions to delete, Step 5c's completions, Step 5d's idempotency check at 2072, Step 6b's
      post-uninstall symmetry at 2087-2095)
    - `Taskfile.yml` lines 2204-2217 (the pass-path evidence echo)
    - `.goreleaser.yaml` `hooks.post.install` as modified by Task 1 — the rehearsal must re-assert the
      new freshness property independently rather than trust the hook's exit code
    - `.planning/phases/04-codegraph-upgrade-homebrew/04-PATTERNS.md` § `Taskfile.yml`
  </read_first>
  <action>
Delete Step 5b in full — its header comment, the `OK=1` initialiser's neighbouring
resolution of the real binary directory, the marker-path variable, the existence check, the
schema-line check, and the six-key loop. Keep the `OK=1` accumulator itself: Steps 5c, 5d,
6b and 7 all still use it. Keep the `echo "resolved installed-binary directory ..."` line
only if it still has a consumer after the deletion; otherwise remove it with its variables.

Fix the three downstream references the deletion orphans:
  - the idempotency check (line 2072) currently requires the man page AND the marker after
    `brew reinstall`; reduce it to the man page, and extend it to the new freshness property
    by requiring at least one `codegraph*.1` newer than **`MAN_REINSTALL_MARKER` — a SECOND
    marker, created once with `mktemp` immediately before the real
    `REINSTALL_OUT="$(brew reinstall --cask …)"` invocation, and consumed ONLY here.**

    **This replaces cycle-1's "reuse the pre-install marker, create no second marker"
    instruction, which is hereby lifted** (review cycle 3, MEDIUM). Reusing the pre-install
    marker made this check pre-satisfied by the FIRST install's own output: pages the first
    install wrote are already newer than a marker created before it, so a reinstall that wrote
    nothing would still leave them there and the check would pass. The plan's old justification
    — "a reinstall rewrites the pages and so they are newer than the pre-install marker too" —
    assumed the very property the check exists to test. It was not *unconditionally* vacuous,
    because `brew reinstall --cask` fires the uninstall hook's unconditional
    `codegraph*.1` `rm_f` glob (`.goreleaser.yaml:621`) before rewriting; but that made the
    check's ability to fire depend on an unstated assumption about a hook Task 1 of THIS PLAN
    is editing — a guard that stops working the moment someone makes that glob conditional,
    with nothing to announce it. A marker created immediately before the reinstall removes the
    dependence entirely: nothing on disk is newer than it unless the reinstall itself wrote it.

    The rule that replaces "never create a second marker" is **one creation point and one
    freshness consumer PER marker**, which is what cycle-1 LOW actually needed — its concern
    was an executor inventing an *unspecified* extra marker or misplacing the first, not the
    number of markers as such. So: `MAN_BASELINE_MARKER` is created once before the first
    install and read once, by Step 5; `MAN_REINSTALL_MARKER` is created once before the
    reinstall and read once, by Step 5d. Neither is read by the other's step. Clean up both.
  - the post-uninstall symmetry check (lines 2091-2095) currently asserts both the man pages
    and the marker are absent; reduce it to the man pages and update the verdict echo to stop
    reporting a marker verdict.
  - the pass-path evidence line (2215) currently interpolates a marker verdict; replace that
    field with `fresh_man_pages=${FRESH_MAN_PAGE_COUNT}` and bump the schema from 2 to 3,
    because the key set changed. Bump the fail-path echo in the cleanup trap to `schema=3`
    as well and give it the same key set with the established `:-` defaults, resolving the
    pre-existing schema=1/schema=2 mismatch rather than carrying it forward.

Add the independent freshness re-check in Step 5, alongside the existing positive man-page
assertions and before Step 5c. Create `MAN_BASELINE_MARKER` with `mktemp` at exactly ONE
point — immediately BEFORE the real `INSTALL_OUT="$(brew install --cask …)"` invocation in
Step 4 (`Taskfile.yml:1907` today; note the Step-4 HEADER COMMENT at `:1875` also contains the
words `brew install --cask`, so "before the install" means before the invocation that assigns
`INSTALL_OUT`, not before the comment). Then after a successful install count
`codegraph*.1` files under the man directory that are newer than that marker with
`find "${MAN_DIR}" -maxdepth 1 -type f -name 'codegraph*.1' -newer "${MAN_BASELINE_MARKER}" | wc -l`
into `FRESH_MAN_PAGE_COUNT`. Fail with the established GitHub Actions annotation form when
that count is zero, naming the man directory, the total count and the fresh count. This is
the rehearsal-side independent confirmation of Task 1's hook assertion — the same
belt-and-braces relationship Step 5 already has with BREW-05's raises.

`MAN_BASELINE_MARKER` is read HERE and nowhere else. Step 5d reads `MAN_REINSTALL_MARKER`,
created at its own single point immediately before the real
`REINSTALL_OUT="$(brew reinstall --cask …)"` invocation (`Taskfile.yml:2064` today; the echo at
`:2062` also contains the words `brew reinstall --cask`, so again the anchor is the invocation
that assigns `REINSTALL_OUT`). Clean up both marker files on the same path the target already
uses for its other temporaries.

Every new failure path uses `echo "::error::<what failed and why>"` followed by setting `OK=0`
(inside the accumulating region) or an explicit non-zero exit (outside it), matching the
surrounding convention. Never a bare `exit 1` with no message.
  </action>
  <verify>
    <automated>test "$(rg -c -e 'SENTINEL' Taskfile.yml || echo 0)" -eq 0 && test "$(rg -c -e 'FRESH_MAN_PAGE_COUNT' Taskfile.yml)" -ge 3 && test "$(rg -c -e 'CASK-REHEARSE-EVIDENCE schema=3' Taskfile.yml)" -eq 2 && test "$(go test ./internal/upgrade/ -run '^TestTaskfile' -v 2>&1 | rg -c -e '^--- PASS: TestTaskfile')" -ge 5 && test "$(go test ./internal/upgrade/ -run '^TestWorkflowRunBodiesInvokeTask$' -v 2>&1 | rg -c -e '^--- PASS: TestWorkflowRunBodiesInvokeTask')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c -e 'SENTINEL' Taskfile.yml` reports 0 matches, AND `rg -c -e 'release:rehearse-cask' Taskfile.yml`
      reports at least 1 and `rg -c -e 'CASK-REHEARSE-EVIDENCE' Taskfile.yml` reports exactly 2 — the
      negative cannot pass by the target having been deleted or the file emptied.
    - `rg -c -e 'CASK-REHEARSE-EVIDENCE schema=3' Taskfile.yml` reports exactly 2 (both the pass path
      and the fail-path trap), and `rg -c -e 'CASK-REHEARSE-EVIDENCE schema=1' -e 'CASK-REHEARSE-EVIDENCE schema=2' Taskfile.yml`
      reports 0 — the two echoes now agree on one schema.
    - `rg -c -e 'FRESH_MAN_PAGE_COUNT' Taskfile.yml` reports at least 3 (assignment, failure check,
      evidence interpolation).
    - Exactly ONE creation point exists PER MARKER (review cycle 1, LOW, preserved in shape;
      extended to two markers in review cycle 3, MEDIUM):
      `rg -c -e 'MAN_BASELINE_MARKER="\$\(mktemp' Taskfile.yml` reports exactly 1 and
      `rg -c -e 'MAN_REINSTALL_MARKER="\$\(mktemp' Taskfile.yml` reports exactly 1; paired with
      `rg -c -e 'MAN_BASELINE_MARKER' Taskfile.yml` at least 3 (creation, the Step 5 `find -newer`
      consumer, cleanup) and `rg -c -e 'MAN_REINSTALL_MARKER' Taskfile.yml` at least 3 (creation,
      the Step 5d `find -newer` consumer, cleanup). The pairing is what stops "exactly 1 creation"
      passing by the variable having been dropped entirely. **The baseline floor is 3, not the
      cycle-2 value of 4** — the fourth reference was Step 5d, which now belongs to the second
      marker; see the threshold note in `<review_dispositions>`. Both floors are RED today (live
      counts: 0 and 0).
    - Neither marker is read by the other's step — one creation point, one freshness consumer per
      marker (review cycle 3, MEDIUM). Step 5's `find -newer` names `MAN_BASELINE_MARKER` and Step
      5d's names `MAN_REINSTALL_MARKER`; a Step 5d check still measured against the pre-install
      marker fails this criterion, because pages the FIRST install wrote are already newer than it
      and the reinstall check could not go RED on its own evidence.
    - Each creation precedes the invocation it baselines, proved by a scripted offset assertion
      anchored on the ASSIGNING invocation rather than on any mention of the command — the Step-4
      header comment (`Taskfile.yml:1875`) and the Step-5d echo (`:2062`) both contain the literal
      command words, so an anchor on `brew install --cask` alone matches the comment and the gate
      could never go green:
      `node -e 'const s=require("fs").readFileSync("Taskfile.yml","utf8");const b=s.indexOf("release:rehearse-cask:");if(b<0)throw new Error("no release:rehearse-cask target");const at=(n)=>{const k=s.indexOf(n,b);if(k<0)throw new Error("not found in rehearsal: "+n);return k};const e=at("MAN_BASELINE_MARKER"),i=at("INSTALL_OUT=\"$(brew install --cask");const r=at("MAN_REINSTALL_MARKER"),ri=at("REINSTALL_OUT=\"$(brew reinstall --cask");if(e>i)throw new Error("MAN_BASELINE_MARKER created AFTER the install invocation");if(r>ri)throw new Error("MAN_REINSTALL_MARKER created AFTER the reinstall invocation");if(r<i)throw new Error("MAN_REINSTALL_MARKER created before the install — it must baseline the reinstall, not the install");console.log("ok")'`
      exits 0.
    - `rg -c -e 'OK=0' Taskfile.yml` is unchanged or higher than before this task (count it before
      editing and record both numbers in the SUMMARY) — assertions were removed, but the accumulator
      discipline was not weakened.
    - The shape tests are proved to have RUN, by count-asserted named PASS lines — never by a
      package-level `ok` match (review cycle 1, HIGH: the previous leg
      `go test … -run '^TestTaskfile|^TestWorkflowRunBodiesInvokeTask$' | rg -q 'ok|PASS'` matched the
      literal `ok` in `ok  github.com/…  0.17s [no tests to run]` and could not distinguish "the
      tests ran and passed" from "no tests ran"; verified empirically against this package):
      `go test ./internal/upgrade/ -run '^TestTaskfile' -v 2>&1 | rg -c -e '^--- PASS: TestTaskfile'`
      reports at least 5 (measured baseline 2026-08-11: exactly 5 —
      `TestTaskfileShapeHelpersFailLoudly`, `TestTaskfileGatesFailLoud`,
      `TestTaskfileGatesFailLoud_EmptyFileIsError`, `TestTaskfileWrapperIsSerial`,
      `TestTaskfileWrapperIsSerial_MissingWrapperIsError`), AND
      `go test ./internal/upgrade/ -run '^TestWorkflowRunBodiesInvokeTask$' -v 2>&1 | rg -c -e '^--- PASS: TestWorkflowRunBodiesInvokeTask'`
      reports exactly 1. Two independent assertions, one per intended test set — a single combined
      `-run` pattern with one threshold cannot prove both ran.
    - `go test ./internal/upgrade/...` exits 0.
    - `task --list-all` exits 0 and lists `release:rehearse-cask` — the YAML still parses.
  </acceptance_criteria>
  <done>The rehearsal asserts no marker file, still positively asserts every other install effect, independently re-checks man-page freshness, and emits one consistent evidence schema on both the pass and the fail path.</done>
</task>

<task type="auto">
  <name>Task 3: Correct 03-EVIDENCE.md's false stranding claim and retire the two folded todos</name>
  <files>.planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md, .planning/todos/pending/2026-08-10-03-evidence-falsely-claims-a-failed-install-can-strand-the-phase-4-sentinel.md, .planning/todos/pending/2026-08-10-post-install-man-page-assertion-can-be-satisfied-by-stale-pages.md</files>
  <read_first>
    - `.planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md` lines 815-835 (the first false passage,
      ending at line 831) and lines 1030-1045 (the second, at line 1039)
    - `.planning/todos/pending/2026-08-10-03-evidence-falsely-claims-a-failed-install-can-strand-the-phase-4-sentinel.md`
      (the falsification argument and the exact line citations it supplies)
    - `.goreleaser.yaml` as modified by Task 1 (the marker no longer exists at all, which is the
      second half of the correction)
  </read_first>
  <action>
Correct the two passages at `03-EVIDENCE.md:831` and `:1039` with a scoped `Edit` each —
never a whole-file rewrite. Each correction must state two things and cite them:

  1. The original claim was false when written. The marker was written strictly AFTER both
     of the hook's raises, so a gate failure raised before it existed and no failed install
     could ever strand it. Cite the ordering the todo records (assertion one raised at
     `.goreleaser.yaml:553-555`, assertion two at `:566-568`, the write at `:577-587` — line
     numbers as of Phase 3, before this plan's deletion). Note that the same evidence file
     already contradicted itself two hundred lines earlier by recording 30 orphaned man
     pages while the marker search returned nothing.
  2. What actually leaks on a failed install is the man pages, and only the man pages,
     because they are written into the shared man directory before assertion one evaluates
     and Homebrew's rollback purges the Caskroom without invoking the cask's uninstall hook.
     That asymmetry — the genuinely valuable finding in the passage — stays.

Add a dated amendment note to each passage in this document's existing amendment style,
recording that the marker was removed outright in Phase 4 (D-02), so the corrected claim is
now also moot. Do not delete the surrounding measured evidence: the 30-orphaned-pages
observation is real and is exactly what motivated the freshness assertion Task 1 added.

Move both todo files from `.planning/todos/pending/` to `.planning/todos/completed/` with
`git mv`, and append a one-line resolution note to each naming this plan and the commit that
closed it. Do not touch ANY other pending todo — stated by exclusion rather than by a count, because
the number of unrelated pending todos changes with every `/gsd-capture` and is not this task's
business (review cycle 6, MEDIUM).
  </action>
  <verify>
    <automated>test "$(rg -c -e 'leave the sentinel behind' .planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md || echo 0)" -eq 0 && test "$(rg -c -e '30 orphaned' .planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md)" -ge 1 && for t in 2026-08-10-03-evidence-falsely-claims-a-failed-install-can-strand-the-phase-4-sentinel.md 2026-08-10-post-install-man-page-assertion-can-be-satisfied-by-stale-pages.md; do test ! -e ".planning/todos/pending/$t" || { echo "todo not folded — still present in pending/: $t" >&2; exit 1; }; test "$(ls .planning/todos/completed/ | rg -c -F -e "$t" || echo 0)" -eq 1 || { echo "todo not filed exactly once in completed/: $t" >&2; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c -e 'leave the sentinel behind' .planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md`
      reports 0 matches, AND `rg -c -e '30 orphaned' .planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md`
      reports at least 1 and `wc -l < .planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md` is within
      40 lines of its pre-edit value — the false claim is gone and the file was corrected, not gutted.
    - Both corrected passages contain the literal line citations `553-555`, `566-568` and `577-587`,
      so a reader can check the ordering claim rather than take it on trust:
      `rg -c -e '577-587' .planning/phases/03-homebrew-tap-cask/03-EVIDENCE.md` reports at least 1.
    - Both corrected passages carry a dated 2026 amendment note naming D-02.
    - **Both folded todos are asserted BY NAME, never by a directory total** (review cycle 6,
      MEDIUM). For each of
      `2026-08-10-03-evidence-falsely-claims-a-failed-install-can-strand-the-phase-4-sentinel.md`
      and `2026-08-10-post-install-man-page-assertion-can-be-satisfied-by-stale-pages.md`: the file
      is ABSENT from `.planning/todos/pending/`, and PRESENT exactly once in
      `.planning/todos/completed/`. The `<automated>` command runs exactly these two legs per name
      as a loop that names the offending file on failure, so a run that folded one todo and missed
      the other says which. The absent-from-pending leg is negative; the present-in-completed leg is
      its paired positive assertion that the move actually happened (repo rule `84d1gfpywd`).
      The directory total `ls .planning/todos/pending/ | wc -l` is RECORDED IN THE SUMMARY as an
      observation — 10 before this task, 8 after, measured 2026-08-11 — and is **NOT a gate**: a
      total couples this check to every unrelated `/gsd-capture` between planning and execution, and
      commit `ffad9ee` on this branch is exactly that event, so the number flips for reasons that
      have nothing to do with this task's work. Assert the members you care about by name; a count
      any unrelated change perturbs is the wrong quantity. The prior
      `ls … completed/ | rg -c -e 'stale-pages' -e 'strand-the-phase-4-sentinel'` leg is replaced for
      the reason cycle 1 gave for multi-`-e` counts: a total of 2 does not establish that BOTH names
      are present — two lines naming the same todo satisfy it.
    - `git status --porcelain .planning/todos/` shows two renames and no deletions.
  </acceptance_criteria>
  <done>Both false passages are corrected with checkable citations and a D-02 amendment note, the genuine man-page asymmetry finding is preserved, and both folded todos are filed as completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GoReleaser cask render -> a user's machine | `hooks.post.install` is Ruby that runs on the user's machine at `brew install` time with their privileges. It is the only mechanism that can fail an install before the user hits a broken binary. |
| prior install residue -> current install's gate | A previous failed install leaves files in the shared man directory that the current install's assertion can read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | `hooks.post.install` man-page assertion | medium | mitigate | UF-5: residue from a prior failed install satisfies the glob, so a binary that runs, exits 0 and writes nothing passes the gate. Mitigated by Task 1's per-path before/after snapshot of the exact `codegraph*.1` set (`man_pages_before`) — recalibrated in review cycle 1 away from a `Time.now - 1` wall-clock oracle, whose one-second window admitted a page written by a previous run — and independently re-checked by the rehearsal's `find -newer` probes in Task 2. **Extended in review cycle 3 (MEDIUM):** those probes are now TWO, one per mutation they baseline — `MAN_BASELINE_MARKER` created immediately before the first install and read only by Step 5, `MAN_REINSTALL_MARKER` created immediately before `brew reinstall` and read only by Step 5d. The single-marker version left Step 5d pre-satisfied by the first install's own pages, so its ability to fire depended on the uninstall hook's unconditional `codegraph*.1` glob (`.goreleaser.yaml:621`) — an unstated assumption about a hook Task 1 of this plan is itself editing. Two markers remove that coupling: nothing is newer than the pre-reinstall marker unless the reinstall wrote it. |
| T-04-06 | Denial of Service | removal of the marker write | low | accept | An already-installed user keeps a stale marker file until their next `brew upgrade`/`brew uninstall`. Accepted, not mitigated: the file lives inside the Caskroom versioned directory Homebrew purges wholesale on both paths (`03-05-SUMMARY.md`), so no migration is possible or needed. |
| T-04-07 | Tampering | `release:rehearse-cask` assertion removal | medium | mitigate | Deleting a CI gate can silently delete coverage. Mitigated by counting `OK=0` sites before and after the edit and by the positive-count assertions in Task 2's acceptance criteria, so a removal that takes an unrelated gate with it is caught. |
| T-04-08 | Repudiation | `03-EVIDENCE.md` | low | mitigate | A false claim in the milestone's evidence record misleads future readers about what was measured. Mitigated by replacing it with a citation-bearing correction rather than a deletion. |
| T-04-SC | Tampering | dependency surface | low | mitigate | No package-manager install occurs in this plan; it deletes and edits YAML/Ruby/Markdown only. Asserted by `git diff --exit-code go.mod go.sum` being clean at plan completion. |
</threat_model>

<verification>
- `task check:goreleaser` exits 0.
- `task --list-all` exits 0.
- `go test ./internal/upgrade/...` exits 0.
- `git diff --exit-code go.mod go.sum` exits 0.
- `rg -c -e 'codegraph-brew-install' -e 'SENTINEL' .goreleaser.yaml Taskfile.yml` reports 0
  while `rg -c -e 'homebrew_casks:' .goreleaser.yaml` and
  `rg -c -e 'release:rehearse-cask' Taskfile.yml` each report at least 1.
</verification>

<success_criteria>
- The marker file is gone from both cask hooks and from every rehearsal assertion (D-02).
- BREW-05's install gate is behaviourally unchanged and still raises independently.
- The man-page assertion now proves the current install produced pages (UF-5 closed) and
  still passes on `brew reinstall`.
- `03-EVIDENCE.md` states what is true, with citations; both folded todos are filed.
</success_criteria>

<output>
Create `.planning/phases/04-codegraph-upgrade-homebrew/04-02-SUMMARY.md` when done.
</output>
