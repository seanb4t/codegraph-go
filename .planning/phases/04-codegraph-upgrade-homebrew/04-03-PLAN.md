---
phase: 04-codegraph-upgrade-homebrew
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [UPGR-01, UPGR-02, UPGR-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every normative statement of this phase's scope names the tree this project actually ships into (Caskroom), and names the formula tree only where formula detection is deliberately also covered (D-01)."
    - "ROADMAP criterion 1 states the seam-based proof — Options.download and Options.swap never invoked — instead of a sha256/mtime comparison (D-08)."
    - "ROADMAP criterion 2 states that `--check` steps aside rather than reporting an available version, matching REQUIREMENTS UPGR-03's own wording (D-09)."
    - "ROADMAP criterion 3's linuxbrew leg covers both the Caskroom and the Cellar shape, and carries no claim that the Caskroom leg is unreachable on Linux (D-04R)."
    - "The amendment reaches the Phase-4 checkbox list item under `## Phases`, not only the Success Criteria block (Phase-3 UF-1)."
    - "The Phase-3 Notes claim that a post-install marker is 'the most robust signal Phase 4 can key on' is amended, because D-02 falsified it."
    - "STATE.md's mirrored decision line is recorded as a known-stale, deliberately-unedited tool-owned artifact rather than silently left unmentioned."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
  key_links:
    - "REQUIREMENTS.md UPGR-03 is already correct and is the target ROADMAP criterion 2 is amended TOWARD, never away from (D-09)."
    - "PROJECT.md § Key Decisions is what STATE.md's own Accumulated Context points at as authoritative, so correcting PROJECT.md is what makes the mirrored STATE.md line safe to leave alone."
  prohibitions:
    - "MUST NOT rewrite `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` wholesale — every change is a scoped `Edit` on the specific passage, because these files are read by scope-sensitive tooling and a whole-file write can silently drop phases."
    - "MUST NOT add, remove, or restructure any heading, checkbox list shape, or table shape in the tool-owned planning artifacts; only the wording inside existing shapes changes."
    - "MUST NOT delete the Cellar/formula wording outright — formula detection is deliberately kept (D-01), so the tree must still be named alongside Caskroom."
---

<objective>
Amend every normative statement of Phase 4's scope so it describes the mechanism that is
actually being built, before any of it is built against the stale text.

The four amendments trace to D-01 (the tree this project ships into), D-08 (criterion 1's
proof), D-09 (criterion 2's `--check` behaviour), and D-04R — which replaced D-04 outright
after research falsified its premise on 2026-08-11. D-04's original rationale, that the
Caskroom leg is unreachable on Linux by construction, must not survive anywhere in the
amended text; it is recorded here only so a reader knows why the wording changed.

Purpose: the ROADMAP criteria were written before this milestone corrected `brews:` to
`homebrew_casks:`. A detector matching `Cellar` literally would never fire on this
project's own install, and criterion 3's false-positive leg would be probing a string that
never appears — the exact vacuous-gate shape repo rule `84d1gfpywd` exists for. Two further
amendments follow from maintainer rulings taken after the criteria were written: criterion
1's proof (D-08) and criterion 2's `--check` behaviour (D-09). A fourth (D-04R) replaces a
premise research falsified.

Output: amended Phase-4 criteria 1-3, an amended Phase-4 checkbox item, an amended Phase-3
Notes claim, amended UPGR-01/UPGR-02 wording, and amended PROJECT.md decision text — each
carrying a dated amendment note in this repository's established style.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-CONTEXT.md
@.planning/phases/04-codegraph-upgrade-homebrew/04-RESEARCH.md
</context>

## Artifacts this phase produces

This plan creates no new code symbols. It produces amended passages in three existing
planning artifacts, each carrying a dated amendment note:

| Artifact | Kind | File |
|---|---|---|
| Phase-4 Success Criteria 1, 2, 3 (amended) | ROADMAP passage | `.planning/ROADMAP.md` |
| Phase-4 Goal and Depends-on lines (amended) | ROADMAP passage | `.planning/ROADMAP.md` |
| Phase-4 checkbox list item under `## Phases` (amended) | ROADMAP list item | `.planning/ROADMAP.md` |
| Milestone-ordering "Brew detection (Phase 4)" bullet (amended) | ROADMAP passage | `.planning/ROADMAP.md` |
| Phase-3 Notes closing clause (amended) | ROADMAP passage | `.planning/ROADMAP.md` |
| UPGR-01, UPGR-02 (amended) | requirement text | `.planning/REQUIREMENTS.md` |
| Key-Decisions brew-detection bullet and table row (amended) | PROJECT passage | `.planning/PROJECT.md` |

**Spec-less probe note:** the three `unresolved` probe rows (UPGR-01/02/03) are enumerated
in full as flagged assumptions in `04-01-PLAN.md`. This plan authors none of them into
`must_haves.truths` and drops none of them.

<!-- planner-discipline-allow: Cellar -->
<!-- planner-discipline-allow: sentinel -->

<review_dispositions>
## Cross-AI review cycle 1 — findings landing in this plan

| # | Severity | Finding | Disposition and location |
|---|---|---|---|
| 7 (this plan's instance) | MEDIUM | `rg -c -e 'Options.download' -e 'Options.swap' … -ge 1` proves neither string individually — `rg -c` counts matching lines, and with multiple `-e` patterns a threshold does not establish set membership. The unescaped `.` additionally matches any character | **Incorporated.** Task 1 `<acceptance_criteria>` split into two independent per-token assertions with the `.` escaped (`Options\.download`, `Options\.swap`), and the paired negative (`sha256 and mtime` absent) restated separately so it cannot pass by the criterion being deleted |

No other cycle-1 finding targets this plan. The other `rg -c` set-membership instances land in
`04-06` (`UPGR-0x`, `Leg 1/2/3`) and `04-05` (`SIG_DIR`, `cosign verify-blob`) and are dispositioned
in those files.
</review_dispositions>

<tasks>

<task type="auto">
  <name>Task 1: Amend the ROADMAP — criteria 1-3, the goal, the depends-on, the checkbox item, and the two prose claims outside the criteria block</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 80-98 (the milestone ordering block, including the "Brew detection
      (Phase 4)" bullet at :91 and the Phase-4 checkbox item at :97)
    - `.planning/ROADMAP.md` lines 174-208 (the Phase-3 section, whose Notes at :187 close with the
      now-falsified claim about what Phase 4 can key on)
    - `.planning/ROADMAP.md` lines 209-225 (the Phase-4 section: Goal, Depends on, Requirements,
      the four Success Criteria, and the Plans stub)
    - `.planning/REQUIREMENTS.md` lines 38-42 (UPGR-01/02/03 — UPGR-03 is already correct and is what
      criterion 2 must be amended toward)
    - `.planning/phases/04-codegraph-upgrade-homebrew/04-CONTEXT.md` § D-01, D-04R, D-08, D-09 and
      § "Established Patterns" (the Phase-3 UF-1 lesson about amendment scope)
    - `$HOME/.claude/rules/planning-artifacts.md` — the standing rule on tool-owned generated files
  </read_first>
  <action>
Apply seven scoped `Edit` calls. Never a whole-file `Write`: `extractCurrentMilestone` ends
the active-milestone section at the next version-bearing heading, so a whole-file rewrite of
this document can silently truncate the milestone. Change only the wording inside existing
shapes — add no heading, change no checkbox glyph, add no table column.

Edit 1 — the Phase-4 checkbox item under `## Phases` (line 97). This is the item Phase 3's
UF-1 left stale because eight verification legs all scoped themselves to criteria. Restate
its trailing clause so it names the Caskroom tree this project ships into and the Cellar tree
formula detection also covers, rather than the Cellar tree alone.

Edit 2 — the milestone-ordering bullet at line 91 ("Brew detection (Phase 4) can be developed
in parallel…"). It describes the constructed test tree as Cellar-shaped. Restate it as
Caskroom-and-Cellar-shaped and keep the rest of the bullet — the point it makes about
acceptance needing the real tap is unchanged and still correct.

Edit 3 — the Phase-3 Notes closing clause at line 187, which asserts that a
`homebrew_casks.hooks.post.install` marker file "is also the most robust signal Phase 4 can
key on". D-02 falsified this: Phase 4 chose structural detection and removed the marker
outright. Amend the clause in this document's established dated-amendment style, naming
D-02 and the date, and stating what replaced it (resolved install path plus Homebrew's own
install receipt). Do not delete the surrounding Notes context about `homebrew_casks:` vs
`brews:` or the least-privilege PAT — both remain true.

Edit 4 — the Phase-4 Goal line (211). Restate the closing clause so it names the tree the
cask actually stages into.

Edit 5 — the Phase-4 Depends-on line (212). It describes the parallel-development tree as
Cellar-shaped; restate it to cover both shapes. Preserve the sentence about a synthetic
layout proving the mechanism but not what ships — that is the reason plan 04-06 exists.

Edit 6 — Success Criteria 1, 2 and 3, each with a dated amendment note in the style criteria
already use elsewhere in this document (see the Phase-2 criterion 2 and Phase-3 criterion 1
amendments for the exact form: the amended text first, then a bolded dated parenthetical
explaining what it previously said and why that was wrong).
  - Criterion 1: name the Caskroom tree (D-01), and replace the sha256-and-mtime proof with
    the seam assertion (D-08) — the standing test asserts the refusal fires, names
    `brew upgrade codegraph`, and neither `Options.download` nor `Options.swap` is ever
    invoked. State why: a hash comparison is an indirect proxy for "did our code call swap"
    and drags Homebrew's filesystem and rollback semantics into a test of our own branch.
    The real-tap acceptance run is retained but now only has to observe the refusal.
  - Criterion 2: replace "reports the available version and how to get it" with the
    step-aside behaviour (D-09) — `--check` does not resolve a version, because
    `brew outdated codegraph` owns that answer for brew installs and a second source can
    disagree. State that this is a criterion amendment, not a requirement change: UPGR-03
    already says "reports how to upgrade" and never said "reports the available version".
    Replace the sha256/mtime evidence clause with the same seam assertion plus the exit-0
    behaviour (D-10).
  - Criterion 3: name both trees (Caskroom and Cellar) at all four prefixes, and state that
    the linuxbrew leg covers BOTH shapes (D-04R). Record the falsification explicitly: the
    prior scoping rested on "Homebrew on Linux does not support casks", which is false for
    the current release line — Homebrew PR #19121 allows `binary`/`zap`-only casks on Linux
    and Homebrew 6.0.0 shipped four further Linux-cask items — and codegraph's own cask
    declares `binaries: [codegraph]` with no `app`/`pkg`/`zap` stanza, exactly that shape.
    Keep the false-positive sentence verbatim; it is the load-bearing half of this criterion.
    Add that the false-positive case is now specifically "a matching path shape with no
    Homebrew install receipt above it".

Edit 7 — leave criterion 4 untouched. It is correct as written and D-11 makes it trivially
satisfiable; say so in the SUMMARY rather than editing the criterion to say it.
  </action>
  <verify>
    <automated>test "$(rg --no-heading -n -e 'Cellar' .planning/ROADMAP.md | rg -v -e 'Caskroom' | wc -l | tr -d ' ')" -eq 0 && test "$(rg --no-heading -n -e 'Cellar' .planning/ROADMAP.md | wc -l | tr -d ' ')" -ge 4 && test "$(rg -n -e '^- \[.\] \*\*Phase 4:' .planning/ROADMAP.md | rg -c -e 'Caskroom')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Set check, both directions: `rg --no-heading -n -e 'Cellar' .planning/ROADMAP.md | rg -v -e 'Caskroom' | wc -l`
      reports 0 (no line names the formula tree alone), AND
      `rg --no-heading -n -e 'Cellar' .planning/ROADMAP.md | wc -l` reports at least 4 (the formula
      tree is still deliberately named, so the first check cannot pass by the word being deleted).
    - The checkbox list item is covered, not just the criteria:
      `rg -n -e '^- \[.\] \*\*Phase 4:' .planning/ROADMAP.md` returns exactly one line, and that line
      contains `Caskroom`.
    - Both seam names are present, asserted INDEPENDENTLY — one check per token, with the `.`
      escaped (review cycle 1, MEDIUM: `rg -c` counts matching lines, so a combined
      `-e 'Options.download' -e 'Options.swap' … -ge 1` proves neither string individually, and an
      unescaped `.` matches any character, so `Options.download` would also match `OptionsXdownload`):
      `rg -c -e 'Options\.download' .planning/ROADMAP.md` reports at least 1, AND
      `rg -c -e 'Options\.swap' .planning/ROADMAP.md` reports at least 1.
    - The replaced proxy is gone: `rg -c -e 'sha256 and mtime' .planning/ROADMAP.md` reports 0 within
      the Phase-4 criteria block (confirm the block boundaries with
      `rg -n -e '### Phase 4:' -e '## Progress' .planning/ROADMAP.md` and read the range). Paired with
      the two positive assertions above, so the negative cannot pass by the criterion being deleted.
    - Criterion 2 no longer promises a version: within the Phase-4 criteria block,
      `rg -n -e 'reports the available version' .planning/ROADMAP.md` returns no match, while
      `rg -c -e 'brew outdated' .planning/ROADMAP.md` reports at least 1 — the claim was replaced,
      not deleted.
    - Criterion 3 names both shapes at linuxbrew: the criterion-3 line contains `linuxbrew` and
      `#19121`, and `rg -c -e 'unreachable' .planning/ROADMAP.md` reports 0 within the Phase-4 block.
    - The Phase-3 Notes amendment landed: `rg -n -e 'most robust signal Phase 4 can key on' .planning/ROADMAP.md`
      returns no match, AND `rg -c -e 'homebrew_casks:. is the block' -e 'Homebrew/brew #20291' .planning/ROADMAP.md`
      reports at least 1 — the falsified clause is gone and the surrounding Notes survive.
    - `node gsd-core/bin/lib/roadmap-parser.cjs` is not required, but the milestone scope must be
      re-proved intact after editing: run
      `node -e 'const f=require(process.env.HOME+"/.claude/gsd-core/bin/lib/roadmap-parser.cjs").getMilestonePhaseFilter(process.cwd()); console.log(require("fs").readdirSync(".planning/phases").filter(f))'`
      and confirm it still lists the Phase-4 directory. Record the output in the SUMMARY.
    - `git diff --stat .planning/ROADMAP.md` shows changed lines only in the ranges named in this
      task; no phase entry outside those ranges was touched.
  </acceptance_criteria>
  <done>Every ROADMAP statement of Phase 4's scope — criteria, goal, depends-on, the checkbox item, the milestone-ordering bullet, and Phase 3's Notes claim — names the mechanism actually being built, each with a dated amendment note, and the milestone scope still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Amend REQUIREMENTS UPGR-01/UPGR-02 and PROJECT.md's Key Decisions, and record the tool-owned STATE.md mirror</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 38-43 (the UPGR block) and lines 24-36 (the SIGN/BREW blocks,
      for the established in-line amendment-note style this repo uses — see SIGN-02 and BREW-05)
    - `.planning/PROJECT.md` line 55 (the scope bullet) and line 184 (the Key Decisions table row)
    - `.planning/STATE.md` line 191 (the mirrored decision line — read it, do not edit it)
    - `.planning/phases/04-codegraph-upgrade-homebrew/04-CONTEXT.md` § D-01, D-03, D-04R
    - `$HOME/.claude/rules/planning-artifacts.md`
  </read_first>
  <action>
Amend REQUIREMENTS.md with scoped `Edit` calls only.

UPGR-01 currently closes with a clause naming the formula tree as the thing that must not be
modified. Restate it to name the tree the cask actually stages into, keeping the formula tree
named as the second covered shape, and append a dated amendment note in the same bracketed
style SIGN-02 and BREW-05 already use — stating what it previously said, that this project
ships a cask and has never had a formula tree entry, and citing the measured evidence
(`03-02-SUMMARY.md:128` recording the resolved Caskroom path, `03-EVIDENCE.md:160` recording
the directory listing).

UPGR-02 is correct in mechanism but silent on which tree shapes the four prefixes must cover.
Extend it to state that both shapes are recognised at every prefix, including linuxbrew
(D-04R), with a dated amendment note recording that the prior scoping premise — that Homebrew
on Linux has no cask support — was falsified by research on 2026-08-11 and citing Homebrew PR
#19121 and the 6.0.0 release notes.

Leave UPGR-03 untouched. It is already correct and is what ROADMAP criterion 2 was amended
toward; say so in the SUMMARY rather than editing it.

Amend PROJECT.md at both sites with scoped `Edit` calls: the scope bullet and the Key
Decisions table row. Both name the formula tree as the thing not to mutate; both must name
the cask tree, keep the formula tree as the second covered shape, and keep the rest of each
statement — "detected by resolving symlinks to the real install path, never a path-prefix
guess" is exactly what shipped and must survive verbatim in substance. Add the receipt
requirement (D-03) to the Key Decisions row's mechanism column, since "resolve symlinks" alone
understates what defeats the false-positive leg. Do not add a table column or a new row.

Do NOT edit `.planning/STATE.md`. Its line 191 mirrors PROJECT.md's decision and carries the
same stale word, but STATE.md is a tool-owned generated artifact under the standing
planning-artifacts rule, and its own Accumulated Context section names PROJECT.md → Key
Decisions as the authoritative source. Record this explicitly in the SUMMARY as a deliberate,
reasoned exclusion with its reason — a surfaced exception, never a silent skip — and note
that the next tool-driven state write will carry the corrected text forward from PROJECT.md.
  </action>
  <verify>
    <automated>test "$(rg --no-heading -n -e 'Cellar' .planning/REQUIREMENTS.md .planning/PROJECT.md | rg -v -e 'Caskroom' | wc -l | tr -d ' ')" -eq 0 && test "$(rg --no-heading -n -e 'Caskroom' .planning/REQUIREMENTS.md .planning/PROJECT.md | wc -l | tr -d ' ')" -ge 3 && test "$(rg -c -e '#19121' .planning/REQUIREMENTS.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Set check, both directions: `rg --no-heading -n -e 'Cellar' .planning/REQUIREMENTS.md .planning/PROJECT.md | rg -v -e 'Caskroom' | wc -l`
      reports 0, AND `rg --no-heading -n -e 'Caskroom' .planning/REQUIREMENTS.md .planning/PROJECT.md | wc -l`
      reports at least 3 — the negative gate cannot pass by the word simply being removed.
    - UPGR-01 and UPGR-02 each carry a dated 2026-08 amendment note:
      `rg -n -e '^- \[ \] \*\*UPGR-0[12]\*\*' -A 0 .planning/REQUIREMENTS.md` returns two lines and
      each contains `Amended 2026-08-1`.
    - UPGR-03's text is byte-unchanged: `git diff .planning/REQUIREMENTS.md` shows no hunk touching the
      UPGR-03 line.
    - `rg -c -e '#19121' .planning/REQUIREMENTS.md` reports at least 1 and
      `rg -c -e 'INSTALL_RECEIPT' .planning/PROJECT.md` reports at least 1 — both falsification
      citations and the receipt mechanism are recorded where a future reader will find them.
    - `rg -c -e 'path-prefix guess' .planning/PROJECT.md` reports at least 2 — the surviving substance
      of the original decision was preserved at both sites, not replaced.
    - `git diff --exit-code .planning/STATE.md` exits 0 — STATE.md was deliberately not edited, and
      the SUMMARY records why by name.
    - `rg -c -e 'Traceability' .planning/REQUIREMENTS.md` reports at least 1 and the UPGR rows in the
      traceability table are unchanged — no table shape was restructured.
  </acceptance_criteria>
  <done>UPGR-01 and UPGR-02 name the shipped layout with dated amendment notes and falsification citations, PROJECT.md's two decision sites match, UPGR-03 is untouched, and STATE.md's stale mirror is recorded as a reasoned exclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand edits -> tool-parsed planning artifacts | `ROADMAP.md` is read by scope-sensitive parsers (`extractCurrentMilestone`, `getMilestonePhaseFilter`). A structural change here silently alters what every downstream GSD command sees, with no error. |
| amended criteria -> future verification | The criteria are the contract `/gsd-verify-work` checks against. A criterion amended to describe less than what shipped would make verification pass on incomplete work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Tampering | `.planning/ROADMAP.md` structure | high | mitigate | A whole-file write, an invented heading, or a stray version-bearing `###` heading truncates the active milestone and silently drops phases from every scope query. Mitigated by the prohibition on whole-file writes, by scoped `Edit` calls only, and by Task 1's acceptance criterion that re-runs `getMilestonePhaseFilter` and confirms the Phase-4 directory still resolves. |
| T-04-10 | Repudiation | amended criteria | medium | mitigate | An amendment that quietly reduces scope reads later as "this was always the plan". Mitigated by requiring each amendment to carry a dated note stating what it previously said and why that was wrong, matching the form Phase 2 and Phase 3 already used. |
| T-04-11 | Information Disclosure | falsification citations | low | mitigate | Without the citation, a future reader re-derives the falsified premise from the same 2022 source. Mitigated by embedding PR #19121 and the 6.0.0 release-notes citations directly in the amended requirement text. |
| T-04-SC | Tampering | dependency surface | low | mitigate | No package-manager install occurs in this plan; it edits Markdown only. Asserted by `git diff --exit-code go.mod go.sum` being clean at plan completion. |
</threat_model>

<verification>
- `rg --no-heading -n -e 'Cellar' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md | rg -v -e 'Caskroom' | wc -l`
  reports 0, and the same search without the filter reports at least 7.
- `git diff --exit-code .planning/STATE.md` exits 0.
- `git diff --exit-code go.mod go.sum` exits 0.
- The milestone-scope re-proof (`getMilestonePhaseFilter`) still lists the Phase-4 directory.

**Deliberate exclusion, recorded not skipped:** `.planning/STATE.md:191` mirrors the amended
PROJECT.md decision and still carries the stale wording. It is a tool-owned generated
artifact; the standing planning-artifacts rule forbids hand-shaping it, and STATE.md's own
text names PROJECT.md → Key Decisions as the authoritative record. The correction lands there.
</verification>

<success_criteria>
- Criteria 1-3, the goal, the depends-on, the Phase-4 checkbox item, the milestone-ordering
  bullet and Phase 3's Notes all describe the shipped mechanism (D-01, D-04R, D-08, D-09).
- UPGR-01 and UPGR-02 are amended with dated notes and falsification citations; UPGR-03 is
  untouched.
- PROJECT.md's two decision sites match, including the receipt mechanism.
- No planning artifact's structure changed, and the milestone scope still parses.
</success_criteria>

<output>
Create `.planning/phases/04-codegraph-upgrade-homebrew/04-03-SUMMARY.md` when done.
</output>
