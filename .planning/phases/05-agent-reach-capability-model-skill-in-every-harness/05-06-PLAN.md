---
phase: 05-agent-reach-capability-model-skill-in-every-harness
plan: 06
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md
autonomous: false
requirements: [AGENT-09, AGENT-04, AGENT-06, AGENT-07, AGENT-10, AGENT-11]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "05-LIVE-SESSIONS.md records, for Cursor, opencode and Antigravity, the exact commands, a verbatim transcript excerpt read back with `herdr agent read <name> --source recent-unwrapped` (RESEARCH Pitfall 6), a negative control in a scratch project with nothing installed, the negative-space check (did the agent reach for codegraph or for grep/find/Read), and a verdict in a fixed form (`<Harness> verdict: read` | `not read`) — the v0.10.0 live-session standard (D-09, D-12, research pitfall 12)."
    - "The sessions are run by the ORCHESTRATOR in sibling Herdr panes, never by an executor subagent (D-09 — a backgrounded subagent cannot drive another pane's TTY reliably); the executor's part is the scaffold (Task 1) and the post-resume completeness gate."
    - "Cursor and opencode run at PROJECT scope in /tmp scratch repos with Claude's project package also installed, so the D-12 duplicate-skill check (`<Harness> duplicate: once` | `twice` | `warning: …`) is observed, not assumed (RESEARCH Pitfall 3: opencode's own docs flag duplicate names as a loading problem)."
    - "D-11: Cursor's repo-root AGENTS.md pickup is probed in a fresh `cursor-agent` session in a scratch repo whose AGENTS.md carries a distinctive sentinel and which has no `.cursor/rules/` file, with a prompt that never names AGENTS.md; the outcome is recorded either way as `D-11 verdict: pickup observed` | `not observed` BEFORE any Cursor instructions-target change (05-07 consumes it)."
    - "Antigravity (global-only) runs against the real $HOME with the D-12 pre-flight (`readlink ~/.claude/skills/codegraph ~/.agents/skills/codegraph` recorded), a negative control BEFORE install, before/after name listings and sha256 sums of every touched file, a `codegraph uninstall` cleanup, and restoration of any pre-existing file the probe altered — the maintainer's $HOME ends byte-identical (D-12 correction)."
    - "AGENT-07's instructions half is probed as D-06(c) reads it: a sentinel appended to `~/.gemini/GEMINI.md` (backed up first, restored and `cmp`-verified after) and a fresh `agy` asked to repeat its global rules — recorded as `GEMINI.md read: yes` | `no`; no AGENTS.md is written for Antigravity."
    - "Gemini CLI and Kiro, not installed on the maintainer's machine (D-10), are recorded `[ASSUMED]` with the doc URL and fetch date 2026-09-18 and the exact paths codegraph writes — never claimed verified; if either binary is present at execution time the same protocol is run and the row upgraded. The global shared path `~/.agents/skills/codegraph` for Cursor/opencode is likewise `[ASSUMED]` (doc-cited) because the sessions run at project scope."
    - statement: "In a genuinely fresh Cursor session at project scope with codegraph installed, the codegraph skill is listed and loaded from `.agents/skills/codegraph`, and it is absent in the negative control (05-LIVE-SESSIONS.md transcript excerpts)."
      verification: backstop
    - statement: "In a genuinely fresh opencode session at project scope, the codegraph skill loads from `.agents/skills/codegraph` without a frontmatter validation error, and is absent in the negative control."
      verification: backstop
    - statement: "In a genuinely fresh `agy` session, the codegraph skill is listed from `~/.gemini/antigravity-cli/skills/codegraph` after install and absent before it; `~/.gemini/GEMINI.md` content reaches the agent."
      verification: backstop
  artifacts:
    - path: ".planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md"
      provides: "per-harness live evidence: commands, transcript excerpts, negative controls, verdicts, D-11 verdict, D-12 duplicate checks, [ASSUMED] rows"
      contains: "D-11 verdict:"
  key_links:
    - from: "05-LIVE-SESSIONS.md `D-11 verdict:` line"
      to: "05-07-PLAN.md Task 1 (Cursor instructions target)"
      via: "precondition read by the next plan"
      pattern: "^D-11 verdict: (pickup observed|not observed)$"
  prohibitions:
    - statement: "MUST NOT record a harness as verified without a real transcript excerpt AND a negative control; a harness that could not be driven stays `[ASSUMED]` with URL and date (D-09, D-10, D-12)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT leave the maintainer's real $HOME changed after the Antigravity session — every file the probe or the install touched is restored and proven identical (sha256 before/after), and the user's `~/.claude/skills/codegraph` symlink is never removed."
      status: resolved
      verification: test
    - statement: "MUST NOT silently patch a `not read` outcome into a harness-specific directory: a `not read`, a doubled skill, or a loading warning is recorded and escalated to the maintainer before the plan resumes (D-06 escape valve and D-12 guard decision are the maintainer's)."
      status: resolved
      verification: judgment
---

<objective>
Establish, in genuinely fresh live sessions, whether each installed harness actually READS the skill this
phase writes — the evidence no unit test may claim (D-00). The executor scaffolds the scratch projects and
the evidence document; the orchestrator (which holds the Herdr pane access) drives Cursor, opencode and
Antigravity sessions with negative controls, probes Cursor's repo-root AGENTS.md pickup (D-11), checks for
doubled skills (D-12), and resumes the plan with the evidence written into `05-LIVE-SESSIONS.md`; the
executor then gates the document's completeness and commits it. Gemini CLI and Kiro are recorded
`[ASSUMED]` (D-10).

Purpose: AGENT-09 (each harness documented as reading the shared path verified live), AGENT-04, AGENT-06,
AGENT-07, and the recorded `[ASSUMED]` status of AGENT-10/AGENT-11. The D-11 verdict decides 05-07.

Ordering (orchestrator directive 1): after every write path exists (05-01…05-05) and before the
`docs/CLI-REFERENCE.md` regeneration and phase close (05-07).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-VALIDATION.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-05-SUMMARY.md
</context>

<interfaces>
Herdr commands confirmed on this machine (05-RESEARCH Pitfall 6 / Code Examples):
```bash
herdr agent start <name> --kind cursor|opencode|agy --pane <id>   # start a fresh agent in an existing pane
herdr agent prompt <name> "<text>" --wait                          # submit and wait for idle
herdr agent read <name> --source recent-unwrapped                  # transcript without line-wrap corruption
```
Local harness CLIs (2026-09-18): cursor-agent 2025.09.12, opencode 1.18.30, agy 1.1.11; absent: gemini,
kiro/kiro-cli. Scratch fixture: `internal/indexer/testdata/gofixture/` (symbol `Alpha` in pkga).
Paths codegraph writes (from `codegraph install --print-config-style`): cursor/opencode local
`.agents/skills/codegraph`; antigravity global `~/.gemini/antigravity-cli/skills/codegraph`; gemini
`.gemini/skills/codegraph` | `~/.gemini/skills/codegraph`; kiro `.kiro/skills/codegraph` | `~/.kiro/skills/codegraph`.
</interfaces>

## Artifacts this phase produces

See `05-01-PLAN.md` → "Artifacts this phase produces". This plan creates `05-LIVE-SESSIONS.md`.

<!-- planner-discipline-allow: PENDING -->

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the scratch projects, the pre-flight record, and the evidence document with the [ASSUMED] rows</name>
  <files>.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-09…D-12 with the corrections)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md (Critical Findings 1-4, Pitfalls 2, 3, 6; the Sources list with URLs and fetch dates)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-VALIDATION.md (Manual-Only table)
    - internal/indexer/testdata/gofixture/ (the scratch fixture)
  </read_first>
  <action>
    Build and scaffold (all outside the repo, under /tmp/05-live; nothing in $HOME is touched by this task):
    - `rm -rf /tmp/05-live && mkdir -p /tmp/05-live/transcripts && GOTOOLCHAIN=go1.26.6 go build -o /tmp/05-live/codegraph ./cmd/codegraph`.
    - For each of cursor-installed, cursor-control, opencode-installed, opencode-control, agy-project: copy internal/indexer/testdata/gofixture/. into /tmp/05-live/<name>/, `git -C <dir> init -q`, and `CODEGRAPH_NO_WATCH=1 /tmp/05-live/codegraph init <dir>` (indexed, so a where-is-X question is answerable; no agent install).
    - `(cd /tmp/05-live/cursor-installed && /tmp/05-live/codegraph install --target claude,cursor --location local)` and `(cd /tmp/05-live/opencode-installed && /tmp/05-live/codegraph install --target claude,opencode --location local)` — Claude's project package is installed alongside on purpose (D-12 duplicate check). Positive assertion: each installed project has `.agents/skills/codegraph/SKILL.md`, a manifest whose targets are ["cursor"] resp. ["opencode"], and `.claude/skills/codegraph/SKILL.md`; each control has none of them.
    - /tmp/05-live/cursor-agentsmd-probe: fixture copy + `git init`, an AGENTS.md whose only content is `# Project instructions`, a blank line, and `The project codename is PLUMTREE-4471. Mention PLUMTREE-4471 whenever you describe this project's instructions.` — no codegraph install, no `.cursor/` dir.
    - Record (read-only) `readlink ~/.claude/skills/codegraph ~/.agents/skills/codegraph` and `command -v gemini kiro kiro-cli` outputs.
    Write 05-LIVE-SESSIONS.md: title; Method (D-09: orchestrator-run Herdr sessions, `--source recent-unwrapped`, transcripts saved under /tmp/05-live/transcripts/, the negative-space rule, and the STATE standing rule that a transcript grep is a claim about the grep — before recording an absence, prove the same search finds the thing in the positive transcript); Pre-flight (the recorded readlink and command -v output); Scaffold (the commands above and the positive-assertion listing); then one section per harness with the fixed verdict lines pre-filled as `PENDING`: Cursor (skill read; negative control; duplicate; where-is-X), Cursor D-11 probe (`D-11 verdict: PENDING`), opencode (skill read; negative control; duplicate; frontmatter; where-is-X), Antigravity (pre-flight; negative control; skill read; where-is-X; GEMINI.md read; GEMINI.md restored; cleanup); a Global shared path row (`~/.agents/skills/codegraph` for Cursor/opencode — `[ASSUMED]`, sessions ran at project scope; cite cursor.com/docs/skills.md and opencode.ai/docs/skills.md, fetched 2026-09-18); Gemini CLI — `[ASSUMED]`, raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/skills.md fetched 2026-09-18, paths `.gemini/skills/codegraph` and `~/.gemini/skills/codegraph`, discoverable via `activate_skill` per that doc, the `.agents/skills/` alias outranking `.gemini/skills/` within a tier; Kiro — `[ASSUMED]`, kiro.dev/docs/skills/ and kiro.dev/docs/steering/ fetched 2026-09-18, paths `.kiro/skills/codegraph` and `~/.kiro/skills/codegraph`, AGENTS.md read as steering by Kiro itself (no codegraph write). If `command -v gemini` or `kiro-cli` succeeded, mark that row `PENDING` instead and note the orchestrator must run the Task 2 protocol for it (D-10 escape hatch). End with a Summary table (harness | scope | path | verdict). Commit `docs(05-06): scaffold live-session evidence and the [ASSUMED] rows for Gemini CLI and Kiro`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md && test -x /tmp/05-live/codegraph && test -f /tmp/05-live/cursor-installed/.agents/skills/codegraph/SKILL.md && test -f /tmp/05-live/opencode-installed/.agents/skills/codegraph/.codegraph-manifest.json && test -f /tmp/05-live/cursor-installed/.claude/skills/codegraph/SKILL.md && test ! -e /tmp/05-live/cursor-control/.agents && test ! -e /tmp/05-live/opencode-control/.agents && test ! -e /tmp/05-live/cursor-agentsmd-probe/.cursor && rg -q 'PLUMTREE-4471' /tmp/05-live/cursor-agentsmd-probe/AGENTS.md && rg -q '"cursor"' /tmp/05-live/cursor-installed/.agents/skills/codegraph/.codegraph-manifest.json && rg -q '^D-11 verdict: PENDING$' "$D" && rg -q 'gemini-cli/main/docs/cli/skills\.md' "$D" && rg -q 'kiro\.dev/docs/steering' "$D" && test "$(rg -c '2026-09-18' "$D")" -ge 3 && test "$(rg -c '\[ASSUMED\]' "$D")" -ge 3 && rg -q 'readlink' "$D" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(05-06\): scaffold'</automated>
    <fails_when>non-zero exit; the scratch binary missing; an installed project lacking the shared SKILL.md, its manifest or Claude's project SKILL.md, or its manifest not naming its requester; a control project containing `.agents/`; the probe repo lacking the sentinel or containing `.cursor/`; 05-LIVE-SESSIONS.md lacking the `D-11 verdict: PENDING` line, the Gemini or Kiro doc URLs, at least 3 fetch dates, at least 3 [ASSUMED] rows, or the readlink pre-flight; or no scaffold commit</fails_when>
  </verify>
  <acceptance_criteria>
    - 05-LIVE-SESSIONS.md has one section each for Cursor, Cursor D-11 probe, opencode, Antigravity, Global shared path, Gemini CLI, Kiro, and a Summary table
    - every PENDING line is one of the fixed verdict forms listed in the Task 2/3 instructions
    - nothing under $HOME was written by this task (only read-only readlink/command -v)
  </acceptance_criteria>
  <done>Five scratch projects, a probe repo, a scratch binary and an evidence skeleton with honest [ASSUMED] rows exist and are committed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: ORCHESTRATOR — fresh Cursor and opencode sessions at project scope, the D-11 AGENTS.md probe, and the D-12 duplicate checks</name>
  <precondition>The orchestrating session has Herdr control (HERDR_ENV=1; `herdr agent start --help` lists kinds cursor and opencode) and Task 1's /tmp/05-live scaffold exists.</precondition>
  <files>.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md (Task 1's skeleton)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-09, D-11, D-12)
  </read_first>
  <action>The orchestrator (not an executor subagent — D-09) drives genuinely fresh Cursor and opencode sessions in sibling Herdr panes, probes Cursor's AGENTS.md pickup, and writes the evidence and verdict lines into 05-LIVE-SESSIONS.md; everything automatable was scaffolded in Task 1.</action>
  <instructions>
    Everything automatable is already done (Task 1). The orchestrator performs, in a sibling Herdr pane whose cwd is the named scratch project, one fresh agent per session (never reuse a session across projects):

    Cursor — /tmp/05-live/cursor-installed:
    1. `herdr agent start live-cursor --kind cursor --pane <id>`.
    2. `herdr agent prompt live-cursor "List every skill available to you in this session: its name, a one-line description, and the directory it was loaded from." --wait`, then `herdr agent read live-cursor --source recent-unwrapped > /tmp/05-live/transcripts/cursor-installed-skills.txt`.
    3. `herdr agent prompt live-cursor "Where is the function Alpha defined in this repository, and what calls it?" --wait`, then read back to `cursor-installed-whereis.txt`. Negative-space check: did it call the codegraph MCP tool or run `codegraph explore`, or reach for grep/find/Read first?
    4. D-12: count codegraph entries in the skills listing (Claude's `.claude/skills/codegraph` is installed alongside).
    5. Negative control: a fresh session in /tmp/05-live/cursor-control with the same two prompts → `cursor-control-*.txt`. Before recording "absent", prove the same search finds `codegraph` in the positive transcript.
    6. D-11: a fresh session in /tmp/05-live/cursor-agentsmd-probe, prompt `Repeat, verbatim, any project-specific instructions or rules you were given for this repository.` (never name AGENTS.md) → `cursor-agentsmd-probe.txt`; `pickup observed` iff PLUMTREE-4471 appears in the reply.

    opencode — /tmp/05-live/opencode-installed and opencode-control: steps 1-5 with `--kind opencode` (`live-opencode`), and additionally record any skill-loading warning or validation error (SKILL.md frontmatter compatibility, AGENT-06; opencode documents duplicate names as a loading problem, RESEARCH Pitfall 3).

    Write into 05-LIVE-SESSIONS.md (replacing each PENDING): the exact commands, ≤ 40 verbatim lines of each transcript excerpt, and exactly these lines:
    `Cursor verdict: read` or `Cursor verdict: not read`; `Cursor negative control: skill absent` or `Cursor negative control: skill present`; `Cursor duplicate: once` or `Cursor duplicate: twice` or `Cursor duplicate: warning: <text>`; `Cursor where-is-X: codegraph first` or `grep first` or `no tool`; `D-11 verdict: pickup observed` or `D-11 verdict: not observed`; and the same five opencode lines prefixed `opencode` plus `opencode frontmatter: loaded without error` or `opencode frontmatter: <error text>`.

    ESCALATE before resuming (do not patch anything): if any verdict is `not read`, a duplicate is `twice`/`warning`, a negative control shows the skill, or opencode reports a frontmatter error — surface the transcript to the maintainer. A `not read` is D-06's escape-valve decision (a harness-specific dir); a doubled skill is D-12's "does the shared write need a guard for that pairing" decision (migrating Claude's package remains a declined deferred idea unless the maintainer reopens it). Record the maintainer's answer as a `Maintainer decision: …` line in the harness's section, then resume.
  </instructions>
  <verification>05-LIVE-SESSIONS.md has no PENDING line in the Cursor, D-11 or opencode sections; every fixed verdict line is present with an allowed value; each section carries at least one verbatim transcript excerpt; any escalated finding carries a `Maintainer decision:` line.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md && rg -q '^Cursor verdict: (read|not read)$' "$D" && rg -q '^Cursor negative control: skill (absent|present)$' "$D" && rg -q '^Cursor duplicate: (once|twice|warning: .+)$' "$D" && rg -q '^Cursor where-is-X: (codegraph first|grep first|no tool)$' "$D" && rg -q '^D-11 verdict: (pickup observed|not observed)$' "$D" && rg -q '^opencode verdict: (read|not read)$' "$D" && rg -q '^opencode negative control: skill (absent|present)$' "$D" && rg -q '^opencode duplicate: (once|twice|warning: .+)$' "$D" && rg -q '^opencode frontmatter: .+$' "$D" && rg -q '^opencode where-is-X: (codegraph first|grep first|no tool)$' "$D" && ! rg -q '^(Cursor|opencode|D-11) .*: PENDING$' "$D" && if rg -q '^(Cursor|opencode) verdict: not read$|^(Cursor|opencode) duplicate: (twice|warning)|^(Cursor|opencode) negative control: skill present$' "$D"; then rg -q '^Maintainer decision: ' "$D"; fi</automated>
    <fails_when>non-zero exit; any required Cursor/D-11/opencode verdict line missing or holding a value outside its allowed set; a Cursor/opencode/D-11 line still PENDING; or an escalation-worthy outcome (not read, doubled/warning skill, skill present in a control) recorded without a "Maintainer decision:" line</fails_when>
  </verify>
  <acceptance_criteria>
    - the D-11 verdict line exists exactly once
    - each of the four Cursor/opencode sessions (installed + control) has an excerpt block and the commands used
  </acceptance_criteria>
  <resume-signal>Reply with the verdict lines as written, e.g. "cursor=read neg=absent dup=once whereis=codegraph first; d11=not observed; opencode=read neg=absent dup=once frontmatter=ok" — only after they are written into 05-LIVE-SESSIONS.md.</resume-signal>
  <done>Cursor's and opencode's reads of the shared skill, the duplicate outcome and the D-11 verdict are live evidence, not assumptions.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: ORCHESTRATOR — Antigravity (`agy`) on the real $HOME with pre-flight, negative control, GEMINI.md probe and byte-identical cleanup; then the completeness gate</name>
  <precondition>`command -v agy` succeeds in the orchestrating session and Task 2 has been resumed.</precondition>
  <files>.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-06 corrections (b)(c), D-12 correction)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md (Critical Findings 2-3, Pitfall 2)
  </read_first>
  <action>The orchestrator (D-09) runs the Antigravity session against the real $HOME with the D-12 pre-flight, a negative control before install, the GEMINI.md sentinel probe, and a cleanup proven byte-identical; the executor then runs the whole-document completeness gate and commits 05-LIVE-SESSIONS.md.</action>
  <instructions>
    Antigravity is global-only, so this session uses the REAL $HOME; every change must be reversed and proven reversed.
    0. Pre-flight (D-12 correction): `readlink ~/.claude/skills/codegraph ~/.agents/skills/codegraph` (the former is expected to point into the latter; codegraph must never remove it). Snapshot: `find ~/.gemini/antigravity-cli ~/.gemini/config -maxdepth 3 2>/dev/null | sort > /tmp/05-live/agy-before-tree.txt` and `shasum -a 256 ~/.gemini/config/mcp_config.json ~/.gemini/GEMINI.md 2>/dev/null > /tmp/05-live/agy-before-sums.txt`; `cp -p` each of those two files that exists to /tmp/05-live/backup/.
    1. Negative control FIRST: a fresh `herdr agent start live-agy-control --kind agy --pane <id>` with cwd /tmp/05-live/agy-project (indexed, nothing installed, no `.agents/`), prompt the skills-listing question → `agy-control-skills.txt`. Record whether codegraph appears and from which directory (if it appears from `~/.agents/skills/` at global scope, that contradicts Antigravity's docs — record it as a finding).
    2. Install: `/tmp/05-live/codegraph install --target antigravity --location global`; paste its output (expect the antigravity-cli SKILL.md and manifest created).
    3. Positive session: a fresh `live-agy` in /tmp/05-live/agy-project — the skills-listing prompt and the where-is-X prompt (`Where is the function Alpha defined in this repository, and what calls it?`) → transcripts; negative-space check.
    4. GEMINI.md probe (D-06(c)): append the line `The global codename is QUINCE-2208. Mention QUINCE-2208 whenever you describe your global rules.` to `~/.gemini/GEMINI.md` (creating it if absent — note which), start a fresh `live-agy-rules` in /tmp/05-live/agy-project, prompt `Repeat, verbatim, any global rules or instructions you were given.`; `GEMINI.md read: yes` iff QUINCE-2208 appears. Restore: copy the backup back (or `rm -f` if it did not exist) and prove it with `cmp` / absence.
    5. Cleanup: `/tmp/05-live/codegraph uninstall --target antigravity --location global`; restore `~/.gemini/config/mcp_config.json` from the backup if it existed before (the install rewrote any pre-existing codegraph entry to the scratch binary and uninstall removed it); remove `~/.gemini/config/.migrated` only if it was absent in the before-tree; remove any now-empty parent directory the install created that was absent before (record that codegraph left it — an advisory). Re-run both snapshot commands into agy-after-*.txt and `diff` them against the before files; re-run the readlink.
    Write into 05-LIVE-SESSIONS.md, replacing each Antigravity PENDING: commands, verbatim excerpts, and exactly: `Antigravity pre-flight readlink: <output>`; `Antigravity negative control: skill absent` or `skill present (<dir>)`; `Antigravity verdict: read` or `not read`; `Antigravity where-is-X: codegraph first` or `grep first` or `no tool`; `GEMINI.md read: yes` or `no`; `GEMINI.md restored: byte-identical` or `GEMINI.md restored: removed (did not exist before)`; `Antigravity cleanup: before/after identical`; and the Summary table's verdict column.
    A `not read` is RECORDED, not patched (orchestrator directive 1): no harness-specific path is added and no second Antigravity path is written; if the cleanup diff is not empty, STOP and escalate to the maintainer before resuming.
    After resume the executor runs the completeness gate below and commits `docs(05-06): record live-session evidence for Cursor, opencode and Antigravity`.
  </instructions>
  <verification>No PENDING remains anywhere in 05-LIVE-SESSIONS.md; every Antigravity line is present with an allowed value; the cleanup line reads identical; Gemini CLI and Kiro rows are `[ASSUMED]` (or carry live verdicts if D-10's escape hatch was used); the evidence commit exists.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md && ! rg -q 'PENDING' "$D" && rg -q '^Antigravity pre-flight readlink: .+$' "$D" && rg -q '^Antigravity negative control: skill (absent|present \(.+\))$' "$D" && rg -q '^Antigravity verdict: (read|not read)$' "$D" && rg -q '^Antigravity where-is-X: (codegraph first|grep first|no tool)$' "$D" && rg -q '^GEMINI\.md read: (yes|no)$' "$D" && rg -q '^GEMINI\.md restored: (byte-identical|removed \(did not exist before\))$' "$D" && rg -q '^Antigravity cleanup: before/after identical$' "$D" && rg -q '^D-11 verdict: (pickup observed|not observed)$' "$D" && test "$(rg -c '\[ASSUMED\]' "$D")" -ge 2 && test "$(rg -c '^(Cursor|opencode|Antigravity) verdict: ' "$D")" = "3" && test -L "$HOME/.claude/skills/codegraph" -o ! -e "$HOME/.claude/skills/codegraph" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(05-06\): record live-session evidence'</automated>
    <fails_when>non-zero exit; any PENDING left in 05-LIVE-SESSIONS.md; a required Antigravity line missing or outside its allowed values; the cleanup line not "before/after identical"; the D-11 verdict line missing; fewer than 2 [ASSUMED] rows (Gemini CLI, Kiro) when neither was installed; not exactly three harness verdict lines; a pre-existing ~/.claude/skills/codegraph symlink turned into something else; or no evidence commit</fails_when>
  </verify>
  <acceptance_criteria>
    - the maintainer's `~/.gemini/config/mcp_config.json` and `~/.gemini/GEMINI.md` sha256 sums after equal those before (recorded in the Antigravity section)
    - `~/.claude/skills/codegraph` is still the same symlink the pre-flight recorded
  </acceptance_criteria>
  <resume-signal>Reply "agy=read|not read neg=absent|present gemini_md=yes|no cleanup=identical" once the lines are written and the before/after diff is empty.</resume-signal>
  <done>Antigravity's read of the CLI skill dir and of GEMINI.md is live evidence, the maintainer's $HOME is byte-identical, and the whole evidence document is complete and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live session → maintainer's real $HOME | Antigravity is global-only; install/probe/uninstall touch real user files |
| transcript → recorded verdict | a summary could overstate what the agent did |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-23 | Tampering | maintainer's `~/.gemini/config/mcp_config.json`, `~/.gemini/GEMINI.md`, `.migrated`, skill dirs | medium | mitigate | backups + sha256 before/after + tree diff + restore steps; Cursor/opencode confined to /tmp scratch projects; STOP-and-escalate on a non-empty diff |
| T-05-24 | Tampering | the user's `~/.claude/skills/codegraph` symlink | medium | mitigate | readlink pre-flight and post-check; the Antigravity install never touches `.claude/` or `.agents/`; verify asserts the link survives |
| T-05-25 | Spoofing (of evidence) | verdicts without transcripts | medium | mitigate | fixed verdict lines gated on verbatim excerpts + negative controls; `[ASSUMED]` for un-drivable harnesses; the transcript-grep standing rule |
| T-05-26 | Information Disclosure | transcripts pasted into the repo | low | accept | scratch fixture projects only (gofixture); the orchestrator redacts any unrelated personal path or token before pasting |
</threat_model>

<verification>
- 05-LIVE-SESSIONS.md complete: 3 live verdicts, D-11 verdict, 2 duplicate checks, Gemini/Kiro [ASSUMED] with URL+date, cleanup identical
</verification>

<success_criteria>
- AGENT-09 roadmap criterion 2's live half; AGENT-04's D-11 probe outcome recorded either way; AGENT-06/AGENT-07 read evidence; AGENT-10/AGENT-11 honestly [ASSUMED] (D-09…D-12)
</success_criteria>

<output>
Create `.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-06-SUMMARY.md` when done — list every verdict line, any maintainer decision, and the D-11 verdict 05-07 consumes.
</output>
