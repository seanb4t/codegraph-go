---
phase: 05-agent-reach-capability-model-skill-in-every-harness
plan: 07
type: execute
wave: 7
depends_on: ["05-06"]
files_modified:
  - internal/agents/capabilities.go
  - internal/agents/capabilities_test.go
  - internal/agents/cursor.go
  - internal/agents/cursor_test.go
  - internal/agents/opencode.go
  - internal/agents/opencode_test.go
  - internal/cli/testdata/plain/print-config-style-local.golden
  - internal/cli/install.go
  - internal/cli/uninstall.go
  - docs/CLI-REFERENCE.md
autonomous: true
requirements: [AGENT-04, AGENT-08]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-11 is applied exactly as recorded in 05-LIVE-SESSIONS.md: when the verdict is `not observed` or `not probed` (maintainer decision 2026-09-18: no Cursor account), Cursor's code is unchanged — it still writes no instructions file (its legacy `.cursor/rules/codegraph.mdc` self-heal is untouched) and `--print-config-style` still prints `instructions=none` for cursor; when the verdict is `pickup observed`, Cursor's local instructions target becomes the repo-root `AGENTS.md` (global stays none — Cursor's global rules are app settings, not a file) and nothing else changes."
    - "Only if `pickup observed`: the repo-root AGENTS.md block becomes a SHARED instructions file (Cursor and opencode at local scope), so `uninstallDeclaredInstructions` removes the marker block only when no OTHER registered target declaring the same instructions path at that location still reports `Detect(loc).AlreadyConfigured`, and otherwise reports that file `kept`; `uninstall --target all` (sorted order: cursor before opencode) and every single-target order leave AGENTS.md byte-identical to its pre-install bytes once the last of the two is uninstalled; the D-13 ownership guard covers Cursor's new write automatically through the table."
    - "`install` and `uninstall` help text names the skill package (SKILL.md plus sidecar manifest at each agent's skill directory, a shared directory removed only when no requesting agent remains) and `--print-config-style`, and `docs/CLI-REFERENCE.md` is regenerated only through `task docs:cli`; `task docs:cli:drift` and `TestEveryRegisteredFlagIsAccountedFor` are green (D-04)."
    - "Phase gate: `GOTOOLCHAIN=go1.26.6 go build ./...` and the full suite are green (module minus internal/daemon, then internal/daemon alone — WINDOWS #37), `task docs:cli:drift` exit 0, 05-MUTATION-LOG.md holds Families (a1), (a2), (b1), (b2) each with a byte-clean revert, and 05-LIVE-SESSIONS.md has no PENDING line."
    - "Out of scope and untouched (Phase 7, AGENT-14): `instructions.go`'s \"4 of 8\" comment, the MCP `instructions` skill sentence in internal/mcp/server.go, and the published per-harness capability table — `git diff` of those files across the phase is empty."
  artifacts:
    - path: "internal/cli/install.go"
      provides: "Long help naming the skill package and --print-config-style"
    - path: "internal/cli/uninstall.go"
      provides: "Long help naming the shared-skill last-requester rule"
    - path: "docs/CLI-REFERENCE.md"
      provides: "regenerated reference"
    - path: "internal/agents/capabilities.go"
      provides: "installDeclaredInstructions/uninstallDeclaredInstructions — ONLY if D-11 pickup observed"
  key_links:
    - from: ".planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md D-11 verdict"
      to: "internal/agents/cursor.go Capabilities().Instructions"
      via: "Task 1 precondition + branch"
      pattern: "^D-11 verdict: (pickup observed|not observed|not probed)$"
  prohibitions:
    - statement: "MUST NOT change Cursor's instructions target unless 05-LIVE-SESSIONS.md records `D-11 verdict: pickup observed` (D-11)."
      status: resolved
      verification: test
    - statement: "MUST NOT hand-edit docs/CLI-REFERENCE.md or add an allowlist entry for a visible flag; regeneration only via `task docs:cli`, proven by `task docs:cli:drift`."
      status: resolved
      verification: test
    - statement: "MUST NOT touch AGENT-14's surfaces (instructions.go's coverage comment, internal/mcp's instructions text, the docs capability table) — Phase 7 publishes them after the last harness write."
      status: resolved
      verification: test
---

<objective>
Close the phase on the live evidence: apply D-11's recorded verdict to Cursor's instructions target (no code
change when the pickup was not observed — the default; a small, shared-file-safe AGENTS.md write when it
was), bring `install`/`uninstall` help text in line with what now ships, regenerate `docs/CLI-REFERENCE.md`
through the drift gate, and run the phase gate (full suite, drift, mutation-log and live-evidence
completeness).

Purpose: AGENT-04 (Cursor's instructions decided by a live probe, recorded either way) and AGENT-08 (the
reference and help describe the table-driven installer). Ordering per orchestrator directive 1 and
CONTEXT's discretion list: after the live sessions, reference regeneration last.

TDD (Task 1, `pickup observed` branch only): `test(05-07):` RED commit with the pasted `--- FAIL:`
transcript, GREEN `feat(05-07):`. In the `not observed` branch Task 1 writes no code and records the
outcome in the SUMMARY. Never `[ci skip]`.

Output: D-11 applied, help text, regenerated reference, phase gate transcript.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-06-SUMMARY.md
@internal/agents/capabilities.go
@internal/agents/cursor.go
@internal/agents/opencode.go
@internal/cli/install.go
@internal/cli/uninstall.go
</context>

<interfaces>
Existing (internal/agents):
```go
func upsertInstructionsEntry(filePath, startMarker, endMarker, content string) (FileResult, error)
func removeMarkedSection(filePath, startMarker, endMarker string) (FileAction, error) // ActionKept when no markers
func instructionsBody() string
const codegraphSectionStart, codegraphSectionEnd
func AllTargets() []AgentTarget
func (c Capabilities) InstructionsPath(loc Location) (string, error)
func opencodeInstructionsPath(loc Location) (string, error) // local "AGENTS.md"
```
Created ONLY in the `pickup observed` branch (capabilities.go):
```go
func cursorInstructionsPath(loc Location) (string, error)                 // cursor.go: local "AGENTS.md", global ""
func installDeclaredInstructions(result *WriteResult, t AgentTarget, loc Location)
func uninstallDeclaredInstructions(result *WriteResult, t AgentTarget, loc Location)
```
</interfaces>

## Artifacts this phase produces

See `05-01-PLAN.md` → "Artifacts this phase produces". This plan produces the regenerated reference, the
updated help text and — only on `pickup observed` — Cursor's AGENTS.md write with the shared-file rule.

<!-- planner-discipline-allow: PENDING -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the recorded D-11 verdict to Cursor's instructions target</name>
  <precondition>05-LIVE-SESSIONS.md contains exactly one line matching `^D-11 verdict: (pickup observed|not observed|not probed)$` (05-06 complete).</precondition>
  <files>internal/agents/capabilities.go, internal/agents/capabilities_test.go, internal/agents/cursor.go, internal/agents/cursor_test.go, internal/agents/opencode.go, internal/agents/opencode_test.go, internal/cli/testdata/plain/print-config-style-local.golden</files>
  <read_first>
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md (the D-11 section: verdict line and transcript)
    - internal/agents/cursor.go (instructions: none; legacy .mdc self-heal) and opencode.go (local AGENTS.md block)
    - internal/agents/shared.go lines 567-683 (replaceOrAppendMarkedSection, removeMarkedSection, upsertInstructionsEntry)
    - internal/agents/capabilities_test.go TestCapabilitiesDeclared (the cursor row to update in the observed branch)
    - internal/agents/ownership_test.go (plants a foreign section in every declared instructions file — covers the new write automatically)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-11)
  </read_first>
  <behavior>
    - Branch `not observed`: no test or code change; TestCapabilitiesDeclared still pins cursor instructions none; the local golden's cursor line still reads `instructions=none`
    - Branch `pickup observed` — TestCursor_Install_WritesAgentsMdLocally: local Install writes the codegraph marker block into ./AGENTS.md; global Install writes no instructions file
    - Branch `pickup observed` — TestCursorOpencode_SharedAgentsMdKeptUntilLastConfigured (local, a pre-existing foreign AGENTS.md ending in one newline): install cursor + opencode; uninstall cursor → AGENTS.md reported `kept` and still holds the block; uninstall opencode → block removed and AGENTS.md byte-identical to the foreign original; the reverse order (opencode first) gives the same final bytes; `uninstall --target all` order (cursor then opencode) gives the same final bytes
    - Branch `pickup observed` — TestCapabilitiesDeclared: cursor instructions `none` (global) | `AGENTS.md` (local); TestOwnershipExactIdentity stays 32/32
  </behavior>
  <action>
    Read the verdict: `V=$(rg -o -r '$1' '^D-11 verdict: (pickup observed|not observed|not probed)$' .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md)`.

    If V is `not observed` (D-11 default): make NO code or test change. Record in the SUMMARY: the verdict, the transcript lines that support it, and that Cursor keeps writing no instructions file (its legacy `.cursor/rules/codegraph.mdc` self-heal unchanged) — the CONTEXT line describing `.cursor/rules/codegraph.mdc` as Cursor's current target was inaccurate (cursor.go:9-16); the operative "no change" is what ships. Then go to Task 2.

    If V is `pickup observed`: RED first — add the behavior-block tests and update TestCapabilitiesDeclared's cursor row; commit `test(05-07): add failing Cursor AGENTS.md instructions tests (D-11 pickup observed)`; paste the `--- FAIL:` lines. GREEN —
    - cursor.go: `cursorInstructionsPath(loc)` returns `"AGENTS.md"` for local and `""` for global (doc comment: D-11 verdict with the 05-LIVE-SESSIONS.md date; Cursor's global rules live in app settings per cursor.com/docs/rules.md, so there is no global file); literal gains `Instructions: cursorInstructionsPath`; Install/Uninstall call the two helpers below after the MCP step; the legacy .mdc self-heal stays.
    - capabilities.go: `installDeclaredInstructions(result, t, loc)` — path via Capabilities().InstructionsPath(loc); "" → return; upsertInstructionsEntry(path, codegraphSectionStart, codegraphSectionEnd, instructionsBody()) through recordFile. `uninstallDeclaredInstructions(result, t, loc)` — "" → return; if any OTHER target in AllTargets() supports loc, declares the same filepath.Clean'd instructions path at loc, and reports Detect(loc).AlreadyConfigured, append FileResult{path, ActionKept} and return; else removeMarkedSection through recordAction. Doc comment: the first shared instructions file in the installer (Cursor + opencode at local scope; Codex joins in Phase 7), removed only by its last configured requester — the instructions-file analogue of D-08.
    - opencode.go: its Uninstall instructions step calls uninstallDeclaredInstructions (so opencode also keeps the block while Cursor is configured); its Install step may keep its direct upsert (identical bytes).
    - Regenerate print-config-style-local.golden via `-update-plain-goldens`; only the cursor line changes (`instructions=AGENTS.md`).
    Commit `feat(05-07): Cursor writes the repo-root AGENTS.md block after the D-11 live probe observed pickup`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && L=.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md && test "$(rg -c '^D-11 verdict: (pickup observed|not observed|not probed)$' "$L")" = "1" && V=$(rg -o -r '$1' '^D-11 verdict: (pickup observed|not observed|not probed)$' "$L") && test -n "$V" && GOTOOLCHAIN=go1.26.6 go build ./... && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 -run 'TestCapabilitiesDeclared$|TestCursor|TestOpencode_|TestOwnershipExactIdentity$|TestPlainGolden$' -v 2>&1 | tee /tmp/05-07-t1.txt | tail -3; test "$(rg -c '^ok\s' /tmp/05-07-t1.txt)" = "2" && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-07-t1.txt && test "$(rg -c -- '--- PASS: TestOwnershipExactIdentity/[a-z]+/(global|local)/(foreign-codegraph-dir|clean) ' /tmp/05-07-t1.txt)" = "32" && if [ "$V" = "pickup observed" ]; then rg -q -- '--- PASS: TestCursorOpencode_SharedAgentsMdKeptUntilLastConfigured ' /tmp/05-07-t1.txt && rg -q '^cursor: scopes=global,local mcp=\.cursor/mcp\.json format=json instructions=AGENTS\.md ' internal/cli/testdata/plain/print-config-style-local.golden && S=$(git log --format=%s -10) && printf '%s\n' "$S" | rg -q '^feat\(05-07\): '; else rg -q '^cursor: scopes=global,local mcp=\.cursor/mcp\.json format=json instructions=none ' internal/cli/testdata/plain/print-config-style-local.golden; fi</automated>
    <fails_when>non-zero exit; the D-11 verdict line absent or not exactly one; build red; agents/cli not both "ok" or any "--- FAIL"/"FAIL" line; the ownership test not 32 PASS leaves; in the observed branch the shared-AGENTS.md test not PASS, the golden's cursor line not `instructions=AGENTS.md`, or no feat(05-07) commit; in the not-observed branch the golden's cursor line not `instructions=none`</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY states the D-11 verdict and quotes the supporting transcript lines
    - `not observed`: `git log --format=%s -- internal/agents/cursor.go` shows no 05-07 commit
    - `pickup observed`: RED `test(05-07):` commit precedes the `feat(05-07):` commit and the SUMMARY pastes its `--- FAIL:` lines
  </acceptance_criteria>
  <done>Cursor's instructions target is exactly what the live probe justified, recorded either way.</done>
</task>

<task type="auto">
  <name>Task 2: Help text, reference regeneration through the drift gate, and the phase gate</name>
  <files>internal/cli/install.go, internal/cli/uninstall.go, docs/CLI-REFERENCE.md</files>
  <read_first>
    - internal/cli/install.go and uninstall.go (Long/Example text)
    - Taskfile.yml docs:cli / docs:cli:drift (lines 481-535)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md and 05-LIVE-SESSIONS.md (completeness)
    - .planning/STATE.md Blockers (WINDOWS #37 — run internal/daemon alone)
  </read_first>
  <action>
    - install Long: after the MCP-entry and instruction-block sentence, add that install also writes the codegraph skill package (SKILL.md plus a sidecar manifest) into the skill directory each agent reads, that a directory shared by several agents holds one package owned jointly, that a `codegraph/` skill directory codegraph did not write is left untouched, and that `--print-config-style` prints what each agent receives without writing anything. uninstall Long: add that it removes codegraph's skill package, deleting a shared skill directory's package only when no other agent that installed it remains, and never touching a skill directory codegraph did not write. Keep the existing sentences; keep lines ≤ 80 columns like the current text.
    - `GOTOOLCHAIN=go1.26.6 task docs:cli`; review the diff of docs/CLI-REFERENCE.md (only the install/uninstall sections may change); commit `docs(05-07): describe the skill package and --print-config-style in install/uninstall help; regenerate CLI reference`.
    - Phase gate, transcripts to /tmp/05-07-*.txt and summarized in the SUMMARY: `GOTOOLCHAIN=go1.26.6 go build ./...`; `GOTOOLCHAIN=go1.26.6 go test -count=1 $(go list ./... | rg -v 'internal/daemon$')`; `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/daemon/` (alone — WINDOWS #37); `GOTOOLCHAIN=go1.26.6 task docs:cli:drift`; 05-MUTATION-LOG.md Families (a1), (a2), (b1), (b2) present; 05-LIVE-SESSIONS.md PENDING-free; AGENT-14 surfaces untouched across the phase.
    - Record in the SUMMARY, as advisories for the maintainer (not changed here): (1) the pre-existing name-based self-heal deletion of `.cursor/rules/codegraph.mdc` / `.kiro/steering/codegraph.md` (a same-name ownership gap outside this phase's writes — candidate todo); (2) `codegraph upgrade` refreshes only Claude's skill package, so the shared and harness-specific SKILL.md files refresh on the next `codegraph install`; (3) Kiro may read the codegraph block twice via AGENTS.md written by opencode/Codex (D-06(d) advisory); (4) AGENT-14 in Phase 7 must update instructions.go's "4 of 8" comment and the MCP instructions skill sentence; (5) the CONTEXT code-insights lines describing Cursor/Kiro instruction files were inaccurate — the code's actual behaviour shipped unchanged.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build ./... && (GOTOOLCHAIN=go1.26.6 go test -count=1 $(GOTOOLCHAIN=go1.26.6 go list ./... | rg -v 'internal/daemon$') > /tmp/05-07-suite.txt 2>&1; echo "exit=$?" >> /tmp/05-07-suite.txt); rg -q '^exit=0$' /tmp/05-07-suite.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-07-suite.txt && test "$(rg -c '^ok\s' /tmp/05-07-suite.txt)" -ge 50 && (GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/daemon/ > /tmp/05-07-daemon.txt 2>&1; echo "exit=$?" >> /tmp/05-07-daemon.txt); rg -q '^exit=0$' /tmp/05-07-daemon.txt && (GOTOOLCHAIN=go1.26.6 task docs:cli:drift > /tmp/05-07-drift.txt 2>&1; echo "exit=$?" >> /tmp/05-07-drift.txt); rg -q '^exit=0$' /tmp/05-07-drift.txt && rg -q 'byte-identical to a fresh regeneration' /tmp/05-07-drift.txt && rg -q 'skill package' docs/CLI-REFERENCE.md && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryRegisteredFlagIsAccountedFor$' 2>&1 | tail -1 | rg -q '^ok\s' && M=.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md && test "$(rg -c '^## Family \((a|b)' "$M")" -ge 2 && rg -q 'TestCapabilitiesTableDrivesDerivations/cursor' "$M" && rg -q 'TestCapabilitiesMatchInstallWrites/claude' "$M" && rg -q 'TestOwnershipExactIdentity/cursor/' "$M" && rg -q 'TestOwnershipExactIdentity/claude/' "$M" && ! rg -q 'PENDING' .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md && B=$(git log --format=%H -E --grep='^test\(05-01\): ' | tail -1) && test -n "$B" && git diff --quiet "$B^" HEAD -- internal/agents/instructions.go internal/mcp/ && echo "phase gate green"</automated>
    <fails_when>non-zero exit; build red; the module suite (minus internal/daemon) exiting non-zero, any "--- FAIL"/"FAIL" line, or fewer than 50 "ok" packages (52 test-bearing packages outside internal/daemon measured at planning, 2026-09-18); the daemon package run exiting non-zero; docs:cli:drift not exit 0 or its byte-identical line absent; the reference not mentioning the skill package; flag accounting not ok; the mutation log missing any of the four family transcripts; any PENDING left in 05-LIVE-SESSIONS.md; or instructions.go / internal/mcp changed since the phase's first commit</fails_when>
  </verify>
  <acceptance_criteria>
    - docs/CLI-REFERENCE.md changed only through `task docs:cli` (drift gate green) and only in the install/uninstall sections in this task's commit
    - the SUMMARY lists the five advisories and the full-suite/daemon/drift exit codes
    - no `[ci skip]`/`[skip ci]` in any Phase 5 commit: `L=$(git log --format=%B -E --grep='^(test|feat|fix|refactor|docs)\(05-0[1-7]\)') && ! printf '%s\n' "$L" | rg -q -i '\[(ci skip|skip ci)\]'`
  </acceptance_criteria>
  <done>The help text and generated reference describe what ships, and the phase closes with every gate green and every advisory on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-root AGENTS.md shared by several agents (only if D-11 pickup observed) | one agent's uninstall could remove another's instructions |
| generated reference ↔ live Cobra tree | a hand edit would drift silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-27 | Denial of Service / Tampering | uninstallDeclaredInstructions on a shared AGENTS.md | medium | mitigate | remove the block only when no other target declaring the same path is still configured; TestCursorOpencode_SharedAgentsMdKeptUntilLastConfigured over every order (observed branch only) |
| T-05-28 | Tampering | foreign content in AGENTS.md | medium | mitigate | marker-fenced upsert/remove (existing T-06-02 helpers); TestOwnershipExactIdentity plants a foreign section in every declared instructions file |
| T-05-29 | Tampering | docs/CLI-REFERENCE.md drift | low | mitigate | `task docs:cli` only; `task docs:cli:drift` + TestEveryRegisteredFlagIsAccountedFor |
</threat_model>

<verification>
- full suite (module minus internal/daemon, then internal/daemon alone), docs:cli:drift, mutation-log and live-evidence completeness — all green (Task 2 verify)
</verification>

<success_criteria>
- AGENT-04: Cursor's AGENTS.md pickup probed before any Cursor instructions change, outcome applied as recorded (roadmap criterion 3)
- AGENT-08: help and reference describe the table-driven installer; the phase gate is green
</success_criteria>

<output>
Create `.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-07-SUMMARY.md` when done — the D-11 branch taken, the gate transcripts' exit codes, and the five advisories.
</output>
