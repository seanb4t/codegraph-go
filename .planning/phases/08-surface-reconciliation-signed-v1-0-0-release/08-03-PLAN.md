---
phase: 08-surface-reconciliation-signed-v1-0-0-release
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/status.go
  - internal/cli/query.go
  - internal/cli/callers.go
  - internal/cli/callees.go
  - internal/cli/install.go
  - internal/cli/uninstall.go
  - internal/cli/upgrade.go
  - internal/upgrade/upgrade.go
  - internal/upgrade/upgrade_test.go
autonomous: true
requirements: [SURF-03]
must_haves:
  truths:
    - "status gains -j; query gains -l/-k/-j; callers and callees gain -l/-j; install and uninstall gain -t/-l — every short alias matching TS 1.3.1 where the letter is free"
    - "no cobra short-flag collision within any single command — go build and go test panic at command construction on any duplicate shorthand, so a green build proves collision-freedom"
    - "upgrade gains a --force/-f flag that, on the same version, forces a reinstall (download→verify→swap) instead of the no-op short-circuit; without --force a same-version upgrade is a no-op with an advisory"
    - "upgrade --force NEVER bypasses signature/provenance verification — verify() remains an unconditional, fail-closed step before swap regardless of --force"
    - "every established Go binding (-p=path, -q=quiet, -v=verbose, -y=yes, -f=file/force, -l=line on node) is preserved; no existing short is silently remapped"
  artifacts:
    - internal/cli/upgrade.go
    - internal/upgrade/upgrade.go
  key_links:
    - "internal/cli/upgrade.go --force → upgrade.Options.Force → internal/upgrade/upgrade.go Run same-version guard (verify() stays unconditional after it)"
  prohibitions:
    - "upgrade --force MUST NOT skip, reorder, or weaken the verify() call in internal/upgrade/upgrade.go Run — the fail-closed download→verify→swap ordering is invariant"
    - "MUST NOT remap any existing short flag (-p/-q/-v/-y/-f/-l) to a TS letter — keep the Go binding and record the divergence in SURF-05 instead"
    - "MUST NOT change the install --auto-allow default or files/query long-flag defaults in this plan — those are SURF-05 documented divergences, not changes"
---

<objective>
Add the missing short-flag aliases across commands where the TS 1.3.1 letter is free (SURF-03): `status -j`; `query -l/-k/-j`; `callers`/`callees -l/-j`; `install`/`uninstall -t/-l`. Separately, add the whole missing `upgrade --force/-f` flag (RESEARCH: an absent flag, not just a missing short) with a verification-safe same-version-reinstall semantic.

Purpose: Close the mechanical short-flag gap so a drop-in user's TS muscle memory works, while preserving every established Go binding and never weakening the release-verification path.
Output: `*Var`→`*VarP` edits across six commands + a new `upgrade --force` flag gating a same-version guard, verify() untouched.

## Artifacts this phase produces (this plan)
- New CLI short flags: `status -j`, `query -l/-k/-j`, `callers -l/-j`, `callees -l/-j`, `install -t/-l`, `uninstall -t/-l`.
- New CLI flag: `upgrade --force` / `-f`.
- New engine option: `upgrade.Options.Force`.

Note: `impact` (-d/-j), `files` (-j, --dir), and `affected` (its new shorts) are handled by plans 08-01/08-02/08-05 respectively to avoid file-ownership conflicts in the same wave.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-RESEARCH.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-PATTERNS.md
@internal/cli/upgrade.go
@internal/upgrade/upgrade.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: mechanical short-flag aliases (status/query/callers/callees/install/uninstall)</name>
  <files>internal/cli/status.go, internal/cli/query.go, internal/cli/callers.go, internal/cli/callees.go, internal/cli/install.go, internal/cli/uninstall.go</files>
  <read_first>
    - internal/cli/status.go (flags block ~74-75), query.go (~88-91), callers.go (~61-63), callees.go (~62-64), install.go (~105-108), uninstall.go (~70-72)
    - 08-RESEARCH.md §"SURF-03 full per-command flag map" (per-command free-letter table)
    - 08-PATTERNS.md §"SURF-03 short-flag idiom" (change `*Var(` → `*VarP(`, insert the short-letter arg, no other body change)
  </read_first>
  <action>
    For each command, change the named flag registrations from `*Var` to `*VarP` inserting the short letter, following the `&var, name, short, default, help` argument order — no other change to any RunE body. Apply CONTEXT D-04's collision policy: adopt TS's letter only where it is free in that command; where a Go binding already holds the letter, keep the Go binding and record the divergence in SURF-05 (do not remap). Specifically: status `--json`→add `-j`; query `--limit`→`-l`, `--kind`→`-k`, `--json`→`-j`; callers `--limit`→`-l`, `--json`→`-j`; callees `--limit`→`-l`, `--json`→`-j`; install `--target`→`-t`, `--location`→`-l`; uninstall `--target`→`-t`, `--location`→`-l`. Do not touch `-p`/`-y`/`--auto-allow` or any long-flag default. Each letter is verified free in its own command per RESEARCH's table.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/cli/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `codegraph status -h` shows `-j`; `query -h` shows `-l`,`-k`,`-j`; `callers -h`/`callees -h` show `-l`,`-j`; `install -h`/`uninstall -h` show `-t`,`-l`
    - `go build ./...` exits 0 (cobra panics on any within-command duplicate shorthand — none)
    - `go test ./internal/cli/... -count=1` exits 0
    - no existing short flag (`-p`/`-q`/`-v`/`-y`) changed meaning in any of the six files
  </acceptance_criteria>
  <done>Six commands expose their TS-parity short aliases with no collision and no existing binding remapped; build and CLI tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: upgrade --force/-f flag + verification-safe same-version guard</name>
  <files>internal/cli/upgrade.go, internal/upgrade/upgrade.go, internal/upgrade/upgrade_test.go</files>
  <read_first>
    - internal/cli/upgrade.go (whole file — the `upgrade.Options{}` wiring, lines 49-53; only `--check` registered today)
    - internal/upgrade/upgrade.go (`Options` struct lines 48-65; `Run` lines 74-133 — the non-check path currently always downloads→verifies→swaps with NO same-version short-circuit; verify() is fatal/fail-closed at lines 119-128)
    - internal/upgrade/upgrade_test.go (existing injectable-seam fakes for resolveLatest/download/verify/swap)
    - 08-RESEARCH.md §"SURF-03 map" upgrade row + §"Security Domain" (release-verification integrity)
  </read_first>
  <behavior>
    - Run with Force=false and resolved target == currentVersion → prints an "already on <version>" advisory and returns nil WITHOUT calling download/verify/swap
    - Run with Force=true and target == currentVersion → proceeds through download → verify → swap (a fake verify that errors still makes Run fatal — swap never called)
    - Run with target != currentVersion → unchanged from today (download → verify → swap), independent of Force
    - In every path where a swap occurs, verify() was called first and its error is fatal (fail-closed ordering preserved)
  </behavior>
  <action>
    Add `Force bool` to `upgrade.Options` with a doc comment stating it forces a same-version reinstall and explicitly does NOT affect verification. In `Run`, after resolving `target` (post-Check), add a same-version guard: when `target == currentVersion && !opts.Force`, print an advisory to `out` and return nil before `checkWritable`/download. Leave the download→verify→swap sequence and the fatal verify() error handling byte-for-byte intact. In internal/cli/upgrade.go, register `cmd.Flags().BoolVarP(&force, "force", "f", false, "reinstall even if already on the latest version")` and thread `Force: force` into the `upgrade.Options{}` literal. Write the RED tests (same-version no-op vs force-reinstall, and force-still-verifies) against the existing fake seams first.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/... ./internal/cli/... -run 'Upgrade|Force' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/upgrade/upgrade.go` declares `Force bool` in `Options` and a same-version guard gated on `!opts.Force`
    - the `verify(binary, bundleJSON)` call and its fatal-error branch in `Run` are unchanged (grep confirms the "signature verification failed" fatal wording still present and still precedes any swap)
    - a test proves Force=true + failing fake verify → Run returns an error and swap is NOT invoked
    - `codegraph upgrade -h` shows `-f, --force`
    - `go test ./internal/upgrade/... ./internal/cli/... -count=1` exits 0
  </acceptance_criteria>
  <done>`upgrade --force` forces a same-version reinstall; verification stays an unconditional fail-closed gate before every swap; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → upgrade CLI → internal/upgrade Run → verify.go | `--force` alters the same-version guard but must not reach the signature-verification decision |
| cobra command construction | duplicate shorthand within a command is a build-time panic (availability) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Tampering | `upgrade --force` → `Run` | high | mitigate | `--force` only bypasses the same-version *no-op* guard; the download→verify→swap ordering and the fatal verify() error are unchanged, proven by a Force=true+failing-verify test that asserts swap is never called |
| T-08-03-02 | Denial of Service (build) | cobra shorthand registration | low | mitigate | A within-command duplicate shorthand panics at build/first-run; `go build ./...` in the verify step catches it before merge |
| T-08-03-03 | Spoofing | preserved short bindings | low | accept | No existing `-p/-q/-v/-y/-f/-l` binding is remapped; divergences are documented in SURF-05, not silently changed |
</threat_model>

<verification>
- `go build ./...` green (proves no within-command shorthand collision across all six edited commands + upgrade)
- `go test ./internal/cli/... ./internal/upgrade/... -count=1` green
- grep confirms verify() ordering in `Run` unchanged
</verification>

<success_criteria>
Every free TS short letter is present across the six commands with no collision and no remap; `upgrade --force` forces a same-version reinstall without ever weakening verification.
</success_criteria>

<output>
Create `.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-03-SUMMARY.md` when done.
</output>
