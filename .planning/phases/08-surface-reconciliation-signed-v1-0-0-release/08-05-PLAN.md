---
phase: 08-surface-reconciliation-signed-v1-0-0-release
plan: 05
type: tdd
wave: 3
depends_on: [08-04]
files_modified:
  - internal/cli/affected.go
  - internal/cli/affected_test.go
autonomous: true
requirements: [SURF-04, SURF-03]
must_haves:
  truths:
    - "affected gains --stdin (read changed paths from stdin, one per line), -d/--depth (BFS depth, engine default 5), -f/--filter <glob> (narrow affected test paths), -q/--quiet (paths-only machine output), and -j/--json — all short letters free in this command"
    - "affected --stdin reads bufio.NewScanner(cmd.InOrStdin()) line by line, trimming surrounding whitespace and CRLF, skipping blank lines, and NEVER hangs on a non-TTY / piped / empty stream (returns on EOF)"
    - "affected with no positional args AND empty/absent stdin prints a 'no files provided' advisory and exits 0 (prints nothing when --quiet); it is not an error"
    - "affected with files but zero affected tests prints a 'no test files affected' advisory in non-quiet/non-json mode, prints nothing in --quiet mode; --json always emits the full result object regardless of --quiet"
    - "affected --quiet emits ONLY the affected test file paths, one per line, plain (no ANSI, no worktree notice, no summary line) — safe for git-hook/CI piping"
    - "Args is relaxed from MinimumNArgs(1) so `git diff --name-only | codegraph affected --stdin` (zero positional args) is accepted, not rejected by cobra before RunE"
    - "affected --depth 0 uses the engine's defaultAffectedDepth=5 (08-04); negative depth is rejected; the --filter glob uses filepath.Match (linear, no backtracking), not a regex engine or new dependency"
  artifacts:
    - internal/cli/affected.go
    - internal/cli/affected_test.go
  key_links:
    - "internal/cli/affected.go stdin/args → paths → eng.Affected(files, depth) (08-04 BFS) → filepath.Match(--filter) → plain path list (--quiet) or human summary"
  prohibitions:
    - "affected --quiet / --stdin machine output MUST NOT be routed through present.RenderFiles or any styled/ANSI path — it stays a raw fmt.Fprintf loop over plain paths (Phase-6 rendering seam)"
    - "the worktree notice MUST NOT print when --quiet or --json is set — it lives only in the human, non-quiet branch"
    - "stdin-supplied paths MUST be used only as index-lookup keys — never shell-exec'd, path-joined against the filesystem, or interpolated into a command"
    - "the --filter glob MUST NOT be compiled into a backtracking regex or pull in a glob dependency — use filepath.Match (linear)"
---

<objective>
Wire `affected`'s scripting flags (SURF-04, CONTEXT D-05): `--stdin`, `-d/--depth`, `-f/--filter <glob>`, `-q/--quiet`, `-j/--json`, relaxing the `Args` validator so a zero-positional `--stdin` invocation is accepted. Reads stdin with `bufio.NewScanner(cmd.InOrStdin())` (never hangs when piped), passes depth into the 08-04 BFS, filters affected test paths with `filepath.Match`, and emits a clean plain path list under `--quiet` for git-hook/CI consumers — respecting the Phase-6 rendering seam.

Purpose: Make `affected` usable in `git diff --name-only | codegraph affected --stdin --quiet` pipelines with TS-parity flag names, defaults, and exit-code semantics.
Output: the full `affected` CLI surface + a CLI-level test covering stdin/quiet/exit-code/never-hang behavior.

## Artifacts this phase produces (this plan)
- New CLI flags: `affected --stdin`, `affected -d/--depth`, `affected -f/--filter <glob>`, `affected -q/--quiet`, `affected -j/--json`.
- New test file: `internal/cli/affected_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-RESEARCH.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-PATTERNS.md
@internal/cli/affected.go
@internal/cli/impact.go
@internal/cli/index.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: affected scripting flags + stdin parsing + Args relax (SURF-04/03)</name>
  <files>internal/cli/affected.go</files>
  <read_first>
    - internal/cli/affected.go (whole file — the `Args: cobra.MinimumNArgs(1)` at line 25, the `eng.Affected(args, 0)` call after 08-04, the worktree-notice/human branch at lines 51-61)
    - internal/cli/impact.go (the `-d/--depth` IntVarP idiom after 08-01) and internal/cli/index.go (lines 82-84 `-q`/`-f` BoolVarP idiom)
    - 08-RESEARCH.md §"Common Pitfalls Pitfall 2" (quiet/stdin/exit-code semantics) and §"Pitfall 3" (Args relax); §"TS affected registration" (flag names/defaults); §"Don't Hand-Roll" (glob via filepath.Match, not a dependency)
    - 08-PATTERNS.md §"internal/cli/affected.go" and §"Compact worktree notice ... ordering"
  </read_first>
  <behavior>
    - `--stdin` with piped "a.go\nb.go\n" → files == [a.go, b.go]; empty pipe → files == []
    - CRLF ("a.go\r\n") and blank/whitespace-only lines are trimmed/skipped
    - positional args and stdin both provided → union of both
    - `--depth 0` → passed as 0, engine applies defaultAffectedDepth=5; `--depth 3` → passed through
    - `--filter "*_test.go"` → only affected test paths matching the glob are emitted; a non-matching glob yields an empty set
    - `--quiet` → output is exactly the affected test file paths, one per line, no summary, no worktree notice; `--json` → full object even with `--quiet`
    - no args + empty stdin, non-quiet → advisory line, exit 0; same, quiet → no output, exit 0
  </behavior>
  <action>
    Relax `Args` to `cobra.ArbitraryArgs` (or a custom validator requiring `len(args) > 0 || stdinFlag`). Add flags: `--stdin` (BoolVar), `-d/--depth` (IntVarP, default 0), `-f/--filter` (StringVarP, default ""), `-q/--quiet` (BoolVarP, default false), `-j/--json` (BoolVarP on the existing json). In RunE, assemble the file list from positional args plus, when `--stdin` is set, `bufio.NewScanner(cmd.InOrStdin())` lines with `strings.TrimSpace` and blank-line skipping. Handle zero input (advisory + exit 0, silent under quiet) before opening the engine. Call `eng.Affected(files, depth)`. Apply `--filter` by keeping affected tests whose FilePath satisfies `filepath.Match(filter, path)` (skip filtering when filter==""). For `--quiet`, emit only unique test file paths via a raw `fmt.Fprintf(out, "%s\n", path)` loop — do NOT call present.RenderFiles and do NOT print the worktree notice. Keep the existing human branch (summary + per-test lines + worktree notice) for the non-quiet, non-json path only; keep `--json` emitting the full MarshalAffectedJSON object regardless of quiet.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/cli/... -run Affected -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `codegraph affected -h` shows `--stdin`, `-d/--depth`, `-f/--filter`, `-q/--quiet`, `-j/--json`
    - `affected.go` no longer uses `cobra.MinimumNArgs(1)`
    - stdin reading uses `bufio.NewScanner(cmd.InOrStdin())` (grep confirms)
    - the `--filter` match uses `filepath.Match` (grep: no `regexp`/`doublestar` added)
    - the `--quiet` output path does NOT reference `present.RenderFiles` and does NOT print `WorktreeNotice`
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>`affected` accepts the full scripting flag surface, reads stdin without hanging, and emits a plain path list under --quiet; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: affected CLI test — stdin/quiet/exit-code/never-hang</name>
  <files>internal/cli/affected_test.go</files>
  <read_first>
    - internal/cli/cli_test.go and internal/cli/query_cli_test.go (how existing CLI tests build the root command, set args, capture stdout, and seed a temp index)
    - internal/cli/status_cli_test.go (real-fixture CLI drive precedent)
    - 08-RESEARCH.md §"Validation Architecture / Wave 0 Gaps" (affected_test.go is a Wave-0 gap)
  </read_first>
  <behavior>
    - piped stdin "path\n" with --stdin (no positional args) → command exits 0, does not hang
    - empty stdin + --stdin + no args, non-quiet → advisory on stdout, exit 0
    - empty stdin + --stdin + no args + --quiet → empty stdout, exit 0
    - --quiet output over a seeded index contains only newline-delimited paths (no summary text, no "⚠" notice bytes)
    - --json + --quiet → stdout parses as the AffectedResult JSON object
  </behavior>
  <action>
    Create affected_test.go driving the real `newAffectedCmd()` (or the root command tree) with `cmd.SetIn(strings.NewReader(...))`, `cmd.SetArgs([]string{"affected", "--stdin", "--quiet", "-p", tmpIndex})`, capturing stdout via `cmd.SetOut`. Assert: never-hang (test completes under the go test timeout), exit-0 advisory/empty behaviors, quiet-mode paths-only output (no summary substring, no `⚠`), and json+quiet still emits the object. Seed a minimal index the same way existing CLI tests do. Write these as the RED tests before Task 1's implementation is complete where practical.
  </action>
  <verify>
    <automated>go test ./internal/cli/... -run Affected -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/affected_test.go` exists and drives the command with a piped stdin reader
    - a test asserts `--quiet` stdout contains no summary line and no `⚠` byte sequence
    - a test asserts empty-stdin+no-args exits 0 (advisory when non-quiet, empty when quiet)
    - `go test ./internal/cli/... -run Affected -count=1` exits 0
  </acceptance_criteria>
  <done>The affected CLI contract (stdin, quiet path-list, exit codes, never-hang) is proven by an automated test driving the real command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git hook / CI stdin → affected --stdin | untrusted newline-delimited path list ingested for scripting |
| affected --filter <glob> | untrusted glob pattern matched against affected paths |
| affected --quiet stdout → downstream shell | machine path list may be piped into another command |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Injection / Elevation | stdin path ingestion | high | mitigate | stdin lines are used ONLY as `fileSet` lookup keys against the frozen index — never shell-exec'd, os-path-joined, or interpolated; output is plain data. Verified by the quiet-output test asserting paths-only content |
| T-08-05-02 | Denial of Service (ReDoS) | --filter glob | medium | mitigate | `filepath.Match` is linear with no backtracking and no user-supplied regex; no glob dependency introduced |
| T-08-05-03 | Denial of Service (hang) | --stdin on non-TTY | medium | mitigate | `bufio.Scanner` over `cmd.InOrStdin()` returns on EOF; the CLI test drives a piped/empty stream and asserts the command completes (never blocks) |
| T-08-05-04 | Information Disclosure | worktree notice under quiet/json | low | mitigate | notice restricted to the human non-quiet/non-json branch; quiet output stays decoration-free per the rendering seam |
</threat_model>

<verification>
- `go build ./... && go test ./internal/cli/... -run Affected -count=1` green
- grep confirms stdin via bufio.Scanner, glob via filepath.Match, quiet path bypasses present.RenderFiles and WorktreeNotice
- `go test ./internal/cli/... -count=1` green (no regressions)
</verification>

<success_criteria>
`affected` supports `--stdin/--depth/--filter/--quiet/--json` with TS-parity semantics, never hangs when piped, emits a clean plain path list under --quiet, and treats stdin paths as data not code.
</success_criteria>

<output>
Create `.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-05-SUMMARY.md` when done.
</output>
