---
phase: 04-supply-chain-coverage-daemon-substrate-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Taskfile.yml
  - .github/workflows/ci.yml
  - testdata/vulnredpoc/main.go
autonomous: true
requirements: [VULN-01, VULN-02, VULN-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`task vuln` builds all four tool binaries from `go.tool.mod` and `go.tool-lint.mod` and scans each with `govulncheck -mode=binary` (VULN-01, per D-05)"
    - "A symbol-matched vulnerability in any of the four tool binaries fails the build: the task exits non-zero when any scan exits 3 (VULN-01)"
    - "A scan exit code that is neither 0 nor 3 (network failure, usage error) fails the build rather than being reported as clean"
    - "`task vuln:selftest` scans a deliberately vulnerable binary and fails unless that scan exits 3 and names GO-2026-5932 — the gate is proven able to fire (VULN-02)"
    - "ci.yml carries a new job whose job-level `name:` and whose two step `name:` fields state the blocking stance out loud (VULN-03)"
    - "ci.yml's existing `govulncheck (DIST-03, blocking)` job is byte-identical to its pre-plan state — VULN-01 adds coverage alongside it, never replaces it"
    - statement: "The red-proof program never enters the main module's package graph, so ci.yml's blocking DIST-03 source-mode gate stays green"
      verification: backstop
    - statement: "All four tool binaries build reproducibly from their pinned modfiles under GOWORK=off"
      verification: backstop
  artifacts:
    - Taskfile.yml
    - .github/workflows/ci.yml
    - testdata/vulnredpoc/main.go
  key_links:
    - "Taskfile.yml `vuln` target body -> the four `go build -modfile=` invocations -> `go tool -modfile=go.tool.mod govulncheck -mode=binary` -> aggregated exit code"
    - "ci.yml `tool-vuln` job -> `task vuln` and `task vuln:selftest` (single-definition property, D-01: job bodies live in Taskfile.yml only)"
    - "testdata/vulnredpoc/main.go -> `golang.org/x/crypto/openpgp` (already required at v0.54.0 in go.tool.mod) -> GO-2026-5932 symbol match -> exit 3"
  assumptions:
    - "ASSUMPTION (research A4, low risk): GO-2026-5932 stays permanently unfixed (`Fixed in: N/A`). If upstream ever fixes it, `task vuln:selftest` goes green-when-it-should-be-red and must be re-pointed at another advisory — the selftest's own exit-3 assertion is what surfaces that."
    - "ASSUMPTION (research A1, low risk): 357 unique modules across the four built binaries is the measured figure quoted in the task desc; ROADMAP's `~400` is the estimate it replaces."
  prohibitions:
    - "Do not modify, rename, or reorder ci.yml's `govulncheck` job or its `govulncheck (DIST-03, blocking)` name string"
    - "Do not add the red-proof program to the main module's package graph, and do not add any requirement to the root go.mod"
    - "Do not make the new job advisory or add `continue-on-error` to it (D-04: this one is blocking and says so)"
    - "Do not write the job body inline in ci.yml — Taskfile.yml is the single definition site (D-01, enforced by TestWorkflowRunBodiesInvokeTask)"
    - "Do not describe the new gate as call-graph-aware — `-mode=binary` is symbol-level and materially weaker"
---

<objective>
Replace `task vuln`'s no-op body with a real `govulncheck -mode=binary` scan over the four
binaries built from `go.tool.mod` and `go.tool-lint.mod`, wire it into ci.yml as a blocking
job that states its stance out loud, and prove the gate can actually fire by scanning a
deliberately vulnerable binary and asserting exit code 3.

Purpose: 357 measured third-party modules execute as credentialed CI tooling (repository
write access and secrets) and are currently scanned by nothing. `task vuln` today runs
`govulncheck ./...` over the main module only — its own description concedes it duplicates
ci.yml's DIST-03 gate and it never touches either tool modfile (D-05).

Output: a replaced `vuln` target, a new `vuln:selftest` target, a new blocking ci.yml job,
and a permanently-committed red-proof program.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supply-chain-coverage-daemon-substrate-fixes/04-CONTEXT.md
@.planning/phases/04-supply-chain-coverage-daemon-substrate-fixes/04-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Requirement |
|----------|------|-------------|
| `Taskfile.yml` — `vuln` target body replaced, `vuln:selftest` target added | 04-01 | VULN-01, VULN-02, VULN-03 |
| `.github/workflows/ci.yml` — new blocking `tool-vuln` job | 04-01 | VULN-01, VULN-03 |
| `testdata/vulnredpoc/main.go` — permanent red-proof program | 04-01 | VULN-02 |
| `internal/daemon/testbudget_test.go` — shared deadline-budget and Run-join helpers | 04-02 | MAINT-01, MAINT-02 |
| `internal/daemon/*_test.go` — join discipline on every failure path, budget-derived deadlines | 04-02 | MAINT-01, MAINT-02 |
| Measured before/after daemon failure-rate table (recorded in 04-02's SUMMARY) | 04-02 | MAINT-01, MAINT-02 |
| `internal/upgrade/taskfile_shape_test.go` — stance guard, GoReleaser pin-parity guard, `inScopeJobs` entry | 04-03 | VULN-03, MAINT-03 |
| `.github/workflows/release.yml` — `GORELEASER_VERSION` aligned with provenance recorded | 04-03 | MAINT-03 |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tool-modfile scan — one `task vuln` invocation, four binaries, real exit code</name>
  <files>Taskfile.yml</files>
  <read_first>
    - `Taskfile.yml:1-12` (the `GO_TOOL` / `GO_TOOL_LINT` vars and the single-definition-site header)
    - `Taskfile.yml:148-155` (the current `vuln` target — the no-op duplicate this replaces, D-05)
    - `Taskfile.yml:205-263` (`check:darwin-release-build` — the in-repo precedent for `set -euo pipefail` + `mktemp -d` + `trap` cleanup + explicit per-target failure reporting inside a Taskfile body)
    - `Taskfile.yml:293-311` (`check:transcript-freeze` — the in-repo precedent for stating a gate's stance inside `desc:`, D-04's sibling)
    - `go.tool.mod:1-41` (the isolation header, the `GOWORK=off` requirement, and the three-entry `tool (` block)
    - `go.tool-lint.mod:1-25` (the single `tool` directive for actionlint)
    - `.github/actions/install-task/action.yml` (the established `GOWORK=off go build -modfile=... -o ...` invocation shape)
    - 04-RESEARCH.md sections "Pattern 1", "State of the Art" (the exit-code contract read from `golang.org/x/vuln@v1.6.0/internal/scan/errors.go`), and "Anti-Patterns to Avoid"
  </read_first>
  <action>
    First, demonstrate the gap D-05 asserts rather than inheriting it: run the existing
    `task vuln` and confirm from its own output that it scans the main module only and
    names neither tool modfile. Record that observation for the SUMMARY. Do not skip this —
    it is the red-before-green step for this task.

    Then replace the `vuln` target's `cmds:` body with a single shell block under
    `set -euo pipefail` that does the following, in order:

    Create one scratch directory via `mktemp -d` and register a `trap` to remove it on
    EXIT, mirroring `check:darwin-release-build`'s existing pattern.

    Build four binaries into that directory, every invocation prefixed with `GOWORK=off`
    (go.tool.mod's header states workspace mode is incompatible with `-modfile`):
    three from `go.tool.mod` — `github.com/go-task/task/v3/cmd/task`,
    `github.com/goreleaser/goreleaser/v2`, and `golang.org/x/vuln/cmd/govulncheck` — and
    one from `go.tool-lint.mod` — `github.com/rhysd/actionlint/cmd/actionlint`. These are
    exactly the tool directives declared by the two modfiles; the set must be derived from
    them, not hardcoded independently of them.

    Scan every built binary with the repo's existing pinned scanner invocation
    (`{{.GO_TOOL}} govulncheck -mode=binary <path>`), capturing each scan's exit status
    without letting `set -e` abort the loop. Classify per the exit-code contract read from
    `golang.org/x/vuln@v1.6.0/internal/scan/errors.go`: 0 is a pass (clean, or a
    module-level-only match the scanner itself does not consider blocking); 3 is a
    symbol-matched vulnerability and a FAIL; any other status (usage error, network
    failure reaching the vulnerability database) is also a FAIL and must never be reported
    as clean.

    Scan all four before deciding the outcome, so one run reports every affected tool
    rather than stopping at the first. Emit a per-binary result line naming the binary and
    its status, then exit non-zero if any scan was classified FAIL, echoing which ones and
    why. Use the `::error::` prefix for failures, matching the convention already used by
    `check:darwin-release-build` and `check:cross`.

    Rewrite the target's `desc:` so it states, in order: that this gate is BLOCKING; that
    it covers the 357 measured third-party modules linked across the four tool binaries
    built from `go.tool.mod` and `go.tool-lint.mod`; that `-mode=binary` is symbol-level
    and cannot produce call graphs, which is a materially weaker guarantee than the
    source-mode analysis ci.yml's DIST-03 job performs over the main module (cite the
    govulncheck documentation's own statement that binary mode may also report false
    positives for unreachable code); that it does not replace or modify that DIST-03 gate,
    which continues to cover the main module unchanged; and the three-way exit-code
    classification above. The stance sentence must be legible on its own — this is VULN-03's
    Taskfile half, and 04-03 will assert on it.
  </action>
  <verify>
    <automated>task vuln</automated>
  </verify>
  <acceptance_criteria>
    - Running `task vuln` on the current tree exits 0 and its output names all four
      binaries (`task`, `goreleaser`, `govulncheck`, `actionlint`) with a per-binary status
    - The `vuln` target's command text contains exactly four `go build -modfile=` invocations,
      three referencing `go.tool.mod` and one referencing `go.tool-lint.mod`, each prefixed
      with `GOWORK=off`
    - The set of package paths built by the target is set-equal to the union of the `tool`
      directives in `go.tool.mod` and `go.tool-lint.mod` (verify by reading both modfiles
      and the target body side by side)
    - Temporarily replacing the scanned-binary path with a binary known to carry a
      symbol-level match makes `task vuln` exit non-zero; revert after observing it
    - `git diff Taskfile.yml` touches only the `vuln` task block — no other target's body
      or `desc:` changes
    - `task lint:actions` still passes and `go test ./internal/upgrade/` still passes
      (`TestTaskfileGatesFailLoud` must not see a new `status:` or `platforms:` key)
  </acceptance_criteria>
  <reversibility rating="reversible">Replacing a Taskfile target body is a single-file revert with no persisted state.</reversibility>
  <done>`task vuln` builds and scans all four tool binaries, exits 0 on a clean tree, exits non-zero on a symbol match or any non-0/non-3 status, and its `desc:` states the blocking stance and the binary-mode limitation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VULN-02 — a permanent red-proof binary and the selftest that requires it to fire</name>
  <files>testdata/vulnredpoc/main.go, Taskfile.yml</files>
  <read_first>
    - `Taskfile.yml` `vuln` target as written by Task 1 (the scanner invocation this reuses)
    - `Taskfile.yml:59-65` (`test:golden` — the in-repo precedent for a package under `testdata/` reached only by an explicit path, and the GOLDEN-01 note that the go tool ignores any directory named `testdata` when expanding `./...`)
    - `.github/workflows/ci.yml:164-179` (the `govulncheck (DIST-03, blocking)` job — the source-mode gate that must stay green, and the reason this program cannot live in the main module's package graph)
    - `go.tool.mod:378` (`golang.org/x/crypto v0.54.0`, already present in the closure)
    - `tools/transcriptfreeze/main.go:1-27` (the in-repo idiom for a `main` package whose doc comment carries its own exit-code contract)
    - 04-RESEARCH.md "Pattern 2" (the live-verified program shape, its captured output, and the exit code 3 it produced) and "Don't Hand-Roll" (why a manual one-time proof rots and a permanent selftest does not)
  </read_first>
  <behavior>
    - Scanning the built red-proof binary produces exit status 3, and the scan output names GO-2026-5932
    - `task vuln:selftest` exits non-zero if that scan exits anything other than 3 — a gate that cannot fire is itself the failure
    - `task vuln:selftest` exits non-zero if the scan output does not name GO-2026-5932, so a different advisory firing is not silently accepted as proof
    - `go list ./...` never names the red-proof package, so ci.yml's DIST-03 source-mode gate is unaffected by its presence
    - `go.mod` (the root module) gains no new requirement
    - `go build ./...` and `go vet ./...` still succeed and never compile the red-proof package
  </behavior>
  <action>
    Create `testdata/vulnredpoc/main.go` as a `package main` program that imports
    `golang.org/x/crypto/openpgp` and calls `openpgp.ReadArmoredKeyRing` against an empty
    reader, printing the returned error. The call must be reachable from `main` — a bare
    import is not enough, because a module-level-only match exits 0 and would prove nothing
    (04-RESEARCH.md records this exact distinction, measured live against the `task` binary).

    Give the file a doc comment that states four things: that this program exists only as
    VULN-02's proof that the tool-modfile scan can fire; that GO-2026-5932 is permanently
    unfixed upstream, which is why it is a stable target rather than a pin that needs
    refreshing; that it lives under a directory the go tool excludes from `./...` expansion
    specifically so it never enters the main module's package graph, where it would turn
    ci.yml's blocking DIST-03 source-mode gate permanently red and could reach a release
    binary; and that it is never built by `go build ./...` — only by the selftest target,
    against `go.tool.mod`.

    Add a `vuln:selftest` target to Taskfile.yml, immediately after `vuln`, whose body
    builds this program with `GOWORK=off go build -modfile=go.tool.mod` into a `mktemp -d`
    scratch directory (trap-cleaned, same shape as Task 1), scans it with the same pinned
    scanner invocation Task 1 uses, and asserts BOTH that the captured exit status is
    exactly 3 AND that the captured output names the advisory identifier. Any other outcome
    is a failure whose message states that the tool-modfile vulnerability gate could not be
    proven able to fire. Its `desc:` must say it is the non-vacuity proof for the `vuln`
    target and that it is blocking.

    If the build reports that `golang.org/x/crypto` must be a direct requirement of
    `go.tool.mod`, promote its existing require line and annotate it with a comment naming
    this selftest as the direct consumer, so a future `go mod tidy -modfile=go.tool.mod`
    does not silently demote it back. Do not add anything to the root `go.mod`.
  </action>
  <verify>
    <automated>task vuln:selftest &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go list ./... | grep -c vulnredpoc; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `task vuln:selftest` exits 0, and its output shows the underlying scan exited 3 and named the advisory identifier
    - Removing the vulnerable call from the program (leaving only the import) makes `task vuln:selftest` exit non-zero; revert after observing it
    - `go list ./...` produces no line containing `vulnredpoc`
    - `go build ./...`, `go vet ./...`, and `task test:unit` all succeed unchanged
    - `git diff go.mod` is empty
    - Running the repo's main-module source-mode scan (`GOWORK=off go tool -modfile=go.tool.mod govulncheck ./...`) still reports no vulnerabilities — the DIST-03 gate's local equivalent stays green with the red-proof program present
  </acceptance_criteria>
  <reversibility rating="reversible">Deleting one testdata file and one Taskfile target restores the prior state exactly.</reversibility>
  <done>A permanently-committed program produces a symbol-level match, `task vuln:selftest` fails unless that scan exits 3 and names GO-2026-5932, and neither the root module's build nor its source-mode vulnerability gate is affected.</done>
</task>

<task type="auto">
  <name>Task 3: The blocking CI job — stance stated in the job name, DIST-03 untouched</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml:1-45` (the header's per-gate rationale block and the pinned-SHA / env-indirection security conventions)
    - `.github/workflows/ci.yml:46-68` (the `test` job's checkout / setup-go / cache / install-task step preamble to mirror, including the pinned action SHAs)
    - `.github/workflows/ci.yml:164-179` (the `govulncheck (DIST-03, blocking)` job — read it so you can leave it byte-identical)
    - `.github/workflows/ci.yml:331-364` (the `transcript-freeze` job — the in-repo precedent for a step `name:` that states its gate's stance, `"Anti-regeneration guard (D-03, advisory since 03-02)"`)
    - `.github/workflows/ci.yml:366-388` (`goreleaser-check` — a compact job whose single step is exactly one `task` call)
    - `internal/upgrade/taskfile_shape_test.go:87-158` (`inScopeJobs`, `runBodyExceptions`, and `taskCallLineRe` — the shape every step in this new job must satisfy so 04-03 can bind it)
    - 04-RESEARCH.md "Pattern 3" (the two-place stance-statement pattern) and "Wave 0 Gaps" (the `inScopeJobs` note)
  </read_first>
  <action>
    Add one job to ci.yml with YAML key `tool-vuln`, placed immediately after the existing
    `govulncheck` job so the two vulnerability gates read as a pair. Its job-level `name:`
    must state the requirement ID and the blocking stance in the same shape the existing
    gate uses, so both are legible side by side and neither can be mistaken for the other's
    scope.

    Run it on `namespace-profile-linux-amd64-4x8` — the same class the `test` job uses.
    Building goreleaser pulls 322 modules, so the 2x4 class used by the lighter jobs is not
    appropriate here.

    Mirror the `test` job's step preamble exactly, reusing the same pinned action commit
    SHAs already present in this file: checkout, setup-go with `go-version-file: go.mod`
    and `cache: false`, the Namespace module/build cache, and the local `install-task`
    composite action.

    Then add exactly two `run:` steps. The first invokes the scan target; the second
    invokes the selftest target. Each step's `run:` body must be a single `task <target>`
    call and nothing else — this file is not a definition site (D-01), and 04-03 adds this
    job to `inScopeJobs` where `TestWorkflowRunBodiesInvokeTask` enforces exactly that
    shape. Name the first step so it states the blocking stance and what it covers; name
    the second so it states that it is the proof the first can fire. Do not add
    `continue-on-error` to either step or to the job.

    Add a comment block above the job, in the style of the header's existing per-gate
    entries, recording three things: that this job deliberately does not replace the
    DIST-03 job, which keeps covering the main module in source mode; that binary mode is
    symbol-level and therefore a weaker guarantee, stated here so a reader does not
    generalize DIST-03's call-graph framing onto it; and that making this job a
    branch-protection required context is a separate out-of-repo ruleset change this plan
    does not perform.

    Leave the `govulncheck` job's every line untouched.
  </action>
  <verify>
    <automated>task lint:actions</automated>
  </verify>
  <acceptance_criteria>
    - `task lint:actions` passes with the new job present
    - `git diff .github/workflows/ci.yml` shows no line added, removed, or modified between
      the `govulncheck:` job key and the job that followed it before this change
    - `go test ./internal/upgrade/ -run TestRequiredCheckNamesPreserved` still passes — every
      existing required-context name string is intact
    - Parsing ci.yml for the new job's steps yields exactly two `run:` bodies, each matching
      a single `task <target>` invocation after comments and blank lines are stripped
    - The job-level `name:` and both step `name:` fields each state the gate's stance in
      words, not only via the requirement ID
    - The new job's `runs-on` value is identical to the `test` job's
  </acceptance_criteria>
  <reversibility rating="reversible">Removing one job block from ci.yml restores the prior workflow exactly; no branch-protection ruleset is modified.</reversibility>
  <done>ci.yml carries a blocking `tool-vuln` job whose two steps are pure `task` calls, whose names state the stance, and whose addition leaves the DIST-03 job byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party module code → CI runner | `task` and `goreleaser` execute on a runner holding repository write access and release secrets (D-04); their transitive closure is arbitrary third-party code |
| vulnerability database (`vuln.go.dev`) → CI job | the scan's verdict depends on a network fetch the job does not control |
| repository tree → main-module build graph | a source file committed to this repo can be pulled into the module every other gate and every release binary is built from |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering, Elevation of Privilege | 357 modules linked into `task`/`goreleaser`/`govulncheck`/`actionlint`, executed with repo write access and secrets | high | mitigate | Task 1's blocking `-mode=binary` scan over all four binaries; a symbol match exits non-zero and fails the build |
| T-04-02 | Tampering | `-mode=binary` symbol-level analysis (no call graph; may also miss a vulnerable symbol the linker did not retain) | medium | accept | Limitation stated explicitly in the `vuln` target `desc:` and in the ci.yml job comment; the main module keeps its stronger source-mode DIST-03 gate |
| T-04-03 | Tampering | scan exit status other than 0 or 3 (usage error, `vuln.go.dev` unreachable) reported as a pass | medium | mitigate | Task 1 classifies any non-0/non-3 status as FAIL; a database fetch failure can never present as clean |
| T-04-04 | Tampering, Denial of Service | `testdata/vulnredpoc/main.go` importing a known-vulnerable package entering the main module's package graph — it would turn the blocking DIST-03 gate permanently red and could reach a release binary | high | mitigate | Housed under a directory the go tool excludes from `./...`; built only via `-modfile=go.tool.mod` by explicit path; Task 2 asserts `go list ./...` never names it and that the main-module source-mode scan stays clean |
| T-04-05 | Spoofing | a future edit converting this job to advisory (or adding `continue-on-error`) leaving a gate-shaped job that cannot fail | medium | mitigate | Stance stated in the job name, both step names, and the Taskfile `desc:`; 04-03 adds the assertion that all three keep saying it |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no new package. Both tool modfiles' contents are pre-existing and pinned; `golang.org/x/crypto` is already resolved at v0.54.0 in `go.tool.mod`'s closure. 04-RESEARCH.md's Package Legitimacy Audit records zero `[SLOP]` and zero `[SUS]` verdicts, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `task vuln` exits 0 on a clean tree and names four binaries with per-binary status
- `task vuln:selftest` exits 0 and reports an underlying exit status of 3 naming GO-2026-5932
- `task lint:actions` passes
- `go build ./...`, `go vet ./...`, `task test:unit`, `task test:golden` all pass
- `git diff go.mod` is empty
- `git diff .github/workflows/ci.yml` shows the `govulncheck` job unchanged
</verification>

<success_criteria>
- VULN-01: the four tool binaries built from `go.tool.mod` and `go.tool-lint.mod` are scanned by `task vuln`, which replaces the main-module duplicate body
- VULN-02: the scan is demonstrated able to fire, permanently, by `task vuln:selftest`'s exit-3 assertion against a real unfixed advisory
- VULN-03: the blocking stance is stated in the Taskfile `desc:`, the ci.yml job `name:`, and both step `name:` fields
- ci.yml's `govulncheck (DIST-03, blocking)` gate is byte-identical and still green
</success_criteria>

<output>
Create `.planning/phases/04-supply-chain-coverage-daemon-substrate-fixes/04-01-SUMMARY.md` when done.
Record in it: the observed output of the pre-replacement `task vuln` run (the D-05 gap
demonstration), the per-binary scan results, and the captured exit status and advisory
identifier from the selftest.
</output>
