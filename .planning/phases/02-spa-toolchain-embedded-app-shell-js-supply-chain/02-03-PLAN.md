---
phase: 02-spa-toolchain-embedded-app-shell-js-supply-chain
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - buf.gen.ts.yaml
  - Taskfile.yml
  - web/package.json
  - web/pnpm-lock.yaml
  - web/src/lib/gen/ui_pb.ts
  - web/src/lib/client.ts
autonomous: true
requirements: [BLD-01, BLD-02]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`task proto:gen` regenerates the browser's TypeScript Connect client from `internal/uiproto/uiv1/ui.proto` and emits exactly ONE file, `web/src/lib/gen/ui_pb.ts` (D-05, D-07, RESEARCH Pitfall 3)."
    - "`protoc-gen-es` never sees `internal/schema/graph.proto` — no TypeScript is generated for the on-disk storage schema, which the browser has no use for and which would then be committed and guarded forever (D-07)."
    - "`task proto:gen` fails loudly with a named error when the pnpm-installed plugin is absent, rather than skipping the TypeScript half and silently succeeding (D-05)."
    - "`task proto:drift` enumerates FOUR committed generated files, prints that count before comparing anything, and hard-fails below a floor of 4 (D-07)."
    - "The browser client is constructed with `createClient(UIService, transport)` over a Connect JSON transport — no separate `_connect.ts` module exists, because Connect-ES v2 removed `protoc-gen-connect-es` (D-05, D-08)."
  artifacts:
    - "buf.gen.ts.yaml — second buf template, `protoc-gen-es` only, scoped to `internal/uiproto/uiv1`, `opt: target=ts`"
    - "web/src/lib/gen/ui_pb.ts — committed, drift-guarded generated TypeScript (D-06)"
    - "web/src/lib/client.ts — the transport and client factory the app shell consumes"
    - "Taskfile.yml — `proto:gen` gains the second buf invocation plus a loud plugin precondition; `proto:drift` gains the TS glob and the floor of 4"
  key_links:
    - "buf resolves `local: protoc-gen-es` off PATH; `proto:gen` must prepend `web/node_modules/.bin` exactly as it already prepends its Go plugin scratch dir"
    - "The npm package is the SCOPED `@bufbuild/protoc-gen-es`; its published `bin` entry is named `protoc-gen-es`, which is the name buf looks up"
    - "`proto:drift`'s scratch regeneration must run BOTH templates into the same `${scratch}/gen` root, or the fourth enumerated file has nothing to compare against"
    - "The floor of 4 is only correct while `target=ts` holds — the default `js+dts` emits two files and would make 5 the wrong-but-plausible number"
  prohibitions:
    - "The `proto:drift` floor MUST NOT be raised to 5 — that number is correct only if `protoc-gen-es` emits the default `js+dts` pair, which D-05/D-06 forbid. Per rule `84d1gfpywd` the floor moves to the CORRECT number, not merely upward; setting it to 5 and generating both files is the failure that looks safe (RESEARCH Pitfall 3)."
    - "`opt: target=ts` MUST NOT be omitted from `buf.gen.ts.yaml` — the plugin default is `js+dts`, which emits two files and ships a `.js`/`.d.ts` pair instead of readable TypeScript."
    - "`protoc-gen-es` MUST NOT be added to the existing `buf.gen.yaml` — that template's plugins run module-wide and would generate TypeScript for `internal/schema/graph.proto` (D-07)."
    - "The TypeScript generation step MUST NOT be made conditional on the plugin being present — a skip-when-missing branch is exactly the silent-success failure D-05 forbids."
    - "`protoc-gen-connect-es` MUST NOT be installed or referenced — it was removed upstream in Connect-ES v2."
    - "The Go plugin pins MUST NOT move out of `go.tool-proto.mod`, and `@bufbuild/protoc-gen-es` MUST NOT be added to the root `go.mod` or to any Go tool modfile — the pnpm lockfile is its analogue."
---

<objective>
Generate the browser's TypeScript Connect client from Phase 1's proto surface, commit it, and
bring it under the existing codegen drift guard — resolving D-07's floor trap with a verified
file count rather than a plausible guess.

Purpose: without this, D-19's live `GetStatus` call in 02-05 has nothing to call through, and
the generated client would otherwise ship committed and drift-guarded without ever having been
executed. This plan produces the client and the guard; 02-05 executes it.

Output: `buf.gen.ts.yaml`, a committed `web/src/lib/gen/ui_pb.ts`, a `proto:gen` that fails
loudly without its plugin, and a `proto:drift` whose floor moved from 3 to the correct 4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-CONTEXT.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-RESEARCH.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-PATTERNS.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-01-SUMMARY.md
@buf.gen.yaml
@buf.yaml
@go.tool-proto.mod
@internal/uiproto/uiv1/ui.proto
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every item below is CREATED by Phase 2 and does not exist on `main`. A source-grounding pass
must treat this list as new, not as drift. The exhaustive phase-wide list lives in
`02-01-PLAN.md`; the entries this plan owns are:

**New files:** `buf.gen.ts.yaml`, `web/src/lib/gen/ui_pb.ts` (generated, committed),
`web/src/lib/client.ts`.

**New generated TS module:** `web/src/lib/gen/ui_pb.ts` — exports the `UIService` `GenService`
schema plus a `*Schema` export for every message in `codegraph.ui.v1` (`GetStatusRequestSchema`,
`GetStatusResponseSchema`, `SearchRequestSchema`, `SearchResponseSchema`, `FilesRequestSchema`,
`FilesResponseSchema`, `CallersRequestSchema`, `CallersResponseSchema`, `CalleesRequestSchema`,
`CalleesResponseSchema`, `ImpactRequestSchema`, `ImpactResponseSchema`, `AffectedRequestSchema`,
`AffectedResponseSchema`, `GetNodeDetailRequestSchema`, `GetNodeDetailResponseSchema`,
`ExploreRequestSchema`, `ExploreResponseSchema`, `SourceBlobSchema`, `NodeDefinitionSchema`,
`ExploreGroupSchema`, and the remaining nested message schemas `ui.proto` declares). The exact
export list is whatever the plugin emits — the plan does not fix it, the committed file does.

**New TS symbols (hand-written):** `web/src/lib/client.ts` exports `transport` and `uiClient`.

**New npm dependencies:** `@bufbuild/protoc-gen-es` (devDependency, provides the
`protoc-gen-es` bin), `@bufbuild/protobuf`, `@connectrpc/connect`, `@connectrpc/connect-web`.

**Modified Taskfile targets:** `proto:gen` (second buf invocation + plugin precondition),
`proto:drift` (fourth `git ls-files` glob, floor 3 → 4, second scratch regeneration).

**New CLI flags:** none. **New CI steps:** none (the existing `Proto codegen drift guard
(BLD-04)` step already invokes `task proto:drift` and picks up the extension for free).
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged Assumptions — spec-less edge probe returned ZERO classifications

No `*-SPEC.md` exists for Phase 2. The deterministic edge probe ran as the fallback and reported
`applicable: 7, resolved: 0, unresolved: 7` — it classified nothing and contributed zero
predicates. All seven rows stay unresolved and are surfaced here; none was auto-resolved with a
backstop marker.

| Requirement | Probe verdict | Status | Flagged assumption |
|---|---|---|---|
| RPC-03 | unclassified | unresolved | Assumed: `ServeMux` precedence separates RPC from SPA. Verified by 02-02. |
| BLD-01 | unclassified | unresolved | Assumed: adding a devDependency here leaves `pnpm install --frozen-lockfile` reproducible. **This plan re-asserts it after the install.** |
| BLD-02 | unclassified | unresolved | Assumed: the generated client compiles into the committed build. Verified when 02-05 rebuilds `web/build/` with the client imported. |
| BLD-03 | unclassified | unresolved | Assumed: source-tree hashing is a sound staleness proxy. 02-06 must demonstrate RED. |
| BLD-05 | unclassified | unresolved | Assumed: `strictDepBuilds` exits non-zero on an unreviewed script. **Relevant here** — the new devDependency may add an `allowBuilds` entry. 02-07 demonstrates RED. |
| BLD-06 | unclassified | unresolved | Assumed: `pnpm audit --json` distinguishes clean from failed. 02-07 confirms empirically. |
| BLD-07 | unclassified | unresolved | Assumed: release-path purity is a token scan over two files. **Note:** this plan adds a pnpm invocation to `Taskfile.yml`, which BLD-07 does NOT scan — only `.goreleaser.yaml` and `release.yml` are in scope, and neither is touched here. |
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Scoped second buf template and a `proto:gen` that fails loudly without its plugin</name>
  <files>buf.gen.ts.yaml, Taskfile.yml, web/package.json, web/pnpm-lock.yaml, web/src/lib/gen/ui_pb.ts</files>
  <read_first>
    - Taskfile.yml — the `proto:gen` target in full: its `desc:`, its `preconditions:` block, and the `PATH="${scratch}:${PATH}" {{.GO_TOOL_PROTO}} buf generate` invocation whose shape the second call must mirror
    - buf.gen.yaml — the existing template, which this plan does NOT modify
    - buf.yaml — the single root module and its `.claude/worktrees` exclude, and why that exclude exists
    - go.tool-proto.mod — how buf itself is pinned; the pnpm lockfile is the analogue for the TS plugin
    - .planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-RESEARCH.md — "Pattern 1", Pitfall 3, and Alternatives Considered's `--path` fallback
  </read_first>
  <precondition>`web/node_modules` exists from 02-01's install, and `pnpm` (or `corepack`) resolves on PATH. If not, halt — do not generate with an unpinned or globally-installed plugin.</precondition>
  <action>
    Install the plugin and wire the second template.

    (a) In `web/`, add `@bufbuild/protoc-gen-es` as a devDependency and `@bufbuild/protobuf`,
    `@connectrpc/connect` and `@connectrpc/connect-web` as runtime dependencies. **The plugin
    package is scoped** — `@bufbuild/protoc-gen-es`, verified against the registry on 2026-08-23.
    The unscoped name `protoc-gen-es` returns HTTP 404; 02-RESEARCH.md's installation snippet is
    wrong on this exact point and this plan corrects it. The scoped package's published `bin`
    entry is named `protoc-gen-es`, which is the name buf's `local:` lookup resolves out of
    `web/node_modules/.bin/`. Pin `@bufbuild/protobuf` and `@bufbuild/protoc-gen-es` to the same
    minor line (both are `2.14.0` as of 2026-08-23) — a runtime/plugin major skew produces
    generated code the runtime cannot load.

    (b) Create `buf.gen.ts.yaml` at the repo root, sibling to `buf.gen.yaml`: `version: v2`, an
    `inputs:` list whose single entry is `directory: internal/uiproto/uiv1`, and a `plugins:` list
    whose single entry is `local: protoc-gen-es` with `out: web/src/lib/gen` and an `opt:` list
    containing `target=ts`. Write a header comment stating that the `inputs:` scoping is what
    keeps `protoc-gen-es` away from `internal/schema/graph.proto` (D-07) and that the `target`
    option is mandatory because the plugin default emits a two-file pair (RESEARCH Pitfall 3),
    which would both ship unreadable output and silently make the drift floor wrong.

    (c) **Validate the input-scoping mechanism from inside a linked git worktree before
    committing to it.** This project runs plans in worktrees under `.claude/worktrees/`, and the
    existing `proto:drift` `desc:` documents that buf's upward workspace search misidentifies the
    controlling workspace from there — which is why the existing call passes an explicit
    absolute-path input rather than a bare `.`. Run the new template from a worktree and confirm
    it resolves `internal/uiproto/uiv1` under the worktree, not under an outer checkout. If it
    does not, switch to the fallback D-07's discretion clause permits and RESEARCH lists under
    Alternatives Considered: drop the `inputs:` block from `buf.gen.ts.yaml` and invoke it as
    `buf generate --template buf.gen.ts.yaml --path "${repo_root}/internal/uiproto/uiv1/ui.proto" "${repo_root}"`.
    Record in the SUMMARY which of the two mechanisms was adopted and the observed evidence.

    (d) Extend `Taskfile.yml`'s `proto:gen`: add a precondition asserting the plugin binary is
    executable at `web/node_modules/.bin/protoc-gen-es`, with a `msg:` naming
    `pnpm install --frozen-lockfile` in `web/` as the fix and stating that TypeScript generation
    is never skipped when the plugin is missing (D-05). Then append a second `buf generate`
    invocation with `--template buf.gen.ts.yaml`, prepending `web/node_modules/.bin` onto PATH in
    the same style the existing call prepends its Go plugin scratch dir. Extend the target's
    closing `echo` to name the TypeScript output as well as the Go output.

    (e) Extend `proto:gen`'s `desc:` to record that the TypeScript half resolves its plugin out of
    `web/node_modules`, that this is the target's only non-Go dependency, and that the dependency
    is deliberately hard rather than optional.

    (f) Run `task proto:gen` and commit the generated `web/src/lib/gen/ui_pb.ts`.
  </action>
  <verify>
    <automated>task proto:gen 2>&1 | tee /tmp/protogen.log && test "$(git ls-files web/src/lib/gen | wc -l | tr -d ' ')" -eq 1 && git ls-files web/src/lib/gen | grep -qx 'web/src/lib/gen/ui_pb.ts'</automated>
    <automated>rg -q 'target=ts' buf.gen.ts.yaml && rg -q 'protoc-gen-es' buf.gen.yaml && echo LEAK || echo SCOPED</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files web/src/lib/gen | wc -l` reports exactly `1`, and the single entry is exactly `web/src/lib/gen/ui_pb.ts`. A count of 2 means `target=ts` was dropped and the plugin emitted the `js`/`dts` pair — that is a hard failure, not a floor adjustment.
    - No TypeScript is generated for the storage schema: `git ls-files 'web/src/lib/gen/**'` contains no file whose name derives from `graph.proto`, and the second buf invocation's input is scoped to `internal/uiproto/uiv1`.
    - `buf.gen.yaml` is unmodified — `git diff --exit-code buf.gen.yaml` is clean, and the second verify command above prints `SCOPED` rather than `LEAK`.
    - With `web/node_modules` temporarily moved aside, `task proto:gen` exits non-zero and its output names the missing plugin and the `pnpm install --frozen-lockfile` remedy. Restore `web/node_modules` afterward and confirm the target is green again. Capture both outputs in the SUMMARY.
    - `web/src/lib/gen/ui_pb.ts` contains a `UIService` export — grep for the exported service identifier and confirm at least one match, proving Connect-ES v2 folded service-schema generation into this single file and no second module is needed.
    - The SUMMARY names which input-scoping mechanism was adopted (`inputs: directory:` or the `--path` fallback) and quotes the worktree evidence that decided it.
  </acceptance_criteria>
  <reversibility rating="costly">D-07's scoping mechanism and the floor it implies are read by CI. A wrong floor is a silently weakened guard rather than a build failure, so the emitted-file-count assertion in this task's acceptance criteria is what makes the choice safe to lock.</reversibility>
  <done>`task proto:gen` produces exactly one committed TypeScript file from `ui.proto` alone, and fails loudly with a named remedy when its pnpm-installed plugin is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Extend `proto:drift` to four files and demonstrate it RED against a staled TS client</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml — the `proto:drift` target in full: its `desc:`, the `files=$(git ls-files -- ...)` enumeration, the `echo "proto:drift: compared ${nfiles} generated files"` line, the `-lt 3` floor, the scratch regeneration, the `cmp -s` loop, and the closing success line
    - .planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-PATTERNS.md — the `proto:drift` section, which names line-by-line which parts transfer
    - .planning/ROADMAP.md — the milestone note that every guard reports a count of what it inspected and is demonstrated RED before being trusted green (rule `84d1gfpywd`)
    - buf.gen.ts.yaml — the template the scratch regeneration must also run
  </read_first>
  <action>
    Extend the existing `proto:drift` target in place. Four edits, no new target (D-07: one guard,
    one definition of codegen currency).

    (a) Add a fourth glob to the `git ls-files` enumeration covering the generated TypeScript
    directory, so `nfiles` counts it. Keep the existing three Go globs unchanged.

    (b) Move the vacuity floor from 3 to 4, and update the `::error::` message that names the
    expected minimum so it enumerates all four files rather than the original three. The floor is
    4 because `target=ts` makes the plugin emit exactly one file — record that dependency
    explicitly in the message, so a future reader who changes the `target` option sees why the
    number moved.

    (c) In the scratch-regeneration section, add a second `buf generate` call for
    `buf.gen.ts.yaml` writing into the SAME `${scratch}/gen` root the Go call already uses, and
    prepend `web/node_modules/.bin` onto PATH for it. Because `buf.gen.ts.yaml`'s `out` is
    `web/src/lib/gen`, the regenerated file lands at `${scratch}/gen/web/src/lib/gen/ui_pb.ts`,
    which the existing `fresh="${scratch}/gen/${f}"` loop already resolves correctly with no
    change to the comparison loop itself. Verify that path resolution empirically rather than
    assuming it; if the re-rooting differs, adjust the second call's `-o` rather than special-
    casing the comparison loop.

    (d) Add a precondition asserting the TS plugin is executable, with the same `msg:` shape Task
    1 added to `proto:gen` — `proto:drift` cannot compare a file it cannot regenerate, and
    skipping the fourth file silently is exactly the vacuous pass the floor exists to catch.

    (e) Update the target's `desc:` to state that the guard now covers three Go files and one
    TypeScript file, that the floor is 4 and why, and that a `target` option change invalidates
    the floor. Update the closing success `echo` so its wording covers all four.

    **Demonstrate RED, then revert byte-cleanly.** Hand-edit one character inside the committed
    `web/src/lib/gen/ui_pb.ts`, run `task proto:drift`, and capture output that (i) prints
    `compared 4 generated files` and (ii) names `web/src/lib/gen/ui_pb.ts` as differing. Then
    `git checkout -- web/src/lib/gen/ui_pb.ts` and confirm the guard is green. Separately, prove
    the floor fires: temporarily narrow the enumeration to three globs, confirm the guard exits
    non-zero with the new floor message, and restore. Capture both transcripts in the SUMMARY.
  </action>
  <verify>
    <automated>task proto:drift 2>&1 | tee /tmp/protodrift.log && grep -q 'compared 4 generated files' /tmp/protodrift.log && git diff --exit-code -- web/src/lib/gen internal/schema internal/uiproto</automated>
    <automated>rg -c -- '-lt 3' Taskfile.yml; test "$(rg -c -- '-lt 4' Taskfile.yml || echo 0)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `task proto:drift` prints the literal line reporting that it compared exactly 4 generated files, and prints it BEFORE any comparison runs — the positive assertion rule `84d1gfpywd` requires. Assert the count in the printed line, not merely a zero exit.
    - The floor comparison in `Taskfile.yml`'s `proto:drift` is against 4, and the old value no longer appears in that target. Verify with a filtered count that ignores comment lines: `rg -v '^\s*#' Taskfile.yml | rg -c -- '-lt 4'` is at least 1.
    - The floor is 4, not 5. The SUMMARY states the arithmetic explicitly: 3 Go files plus exactly 1 TypeScript file, and records the observed `git ls-files web/src/lib/gen` count of 1 as the evidence.
    - The SUMMARY carries the captured RED transcript from the one-character edit, showing both the `compared 4` line and the named differing file, and confirms `git diff --exit-code web/src/lib/gen` is clean after revert.
    - The SUMMARY carries the captured floor-fires transcript from the narrowed enumeration, showing a non-zero exit and the new floor message.
    - `task proto:drift` is green on the restored tree, and `git diff --exit-code -- web/src/lib/gen internal/schema internal/uiproto` is clean afterward — the guard regenerated into scratch only and left the source tree untouched.
  </acceptance_criteria>
  <done>One guard covers four generated files across two languages, reports the count before comparing, hard-fails below the correct floor of 4, and has been watched fail against both a staled file and a broken enumeration.</done>
</task>

<task type="auto">
  <name>Task 3: The browser client factory over Connect's JSON wire</name>
  <files>web/src/lib/client.ts</files>
  <read_first>
    - web/src/lib/gen/ui_pb.ts — the generated `UIService` export this module consumes (read the export block, not the whole file)
    - .planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-RESEARCH.md — "Connect-ES v2 browser client construction" and Assumptions Log row A1
    - .planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-CONTEXT.md — D-08 (Connect JSON, not binary protobuf) and its debugging-tool rationale
    - web/tsconfig.json — the module resolution mode the import path must satisfy
  </read_first>
  <action>
    Create `web/src/lib/client.ts` exporting a `transport` built with `createConnectTransport`
    from `@connectrpc/connect-web` and a `uiClient` built with `createClient` from
    `@connectrpc/connect` over the generated `UIService` schema.

    Set `baseUrl` to `/` — the Go binary serves both the SPA and the RPC surface from one origin,
    so an absolute URL would break the moment the ephemeral port changes (Phase 1 D-07 binds
    `127.0.0.1:0`). Set `useBinaryFormat` to `false` explicitly (D-08). RESEARCH's Assumption A1
    flags the default as unverified: read the installed `@connectrpc/connect-web` package's own
    type definitions in `web/node_modules` to determine what the default actually is, record the
    finding in the SUMMARY, and set the option explicitly regardless of the answer — an explicit
    value is what keeps D-08 true across a future dependency bump.

    Import the service schema from the generated module using the `$lib` alias SvelteKit
    configures, so the import survives a directory move. Give the module a doc comment naming
    D-05, D-08 and D-19, and stating that no `_connect.ts` companion module exists because
    Connect-ES v2 removed `protoc-gen-connect-es` and folded service-schema generation into
    `protoc-gen-es`.

    Do not add error handling, retries or a request-cancellation layer here — 02-05 renders one
    `GetStatus` call and handles its own failure state; anything more is scope this phase does not
    have.
  </action>
  <verify>
    <automated>cd web && pnpm exec svelte-check --threshold error --output human 2>&1 | tee /tmp/svelte-check.log; grep -qE '0 errors' /tmp/svelte-check.log</automated>
    <automated>cd web && pnpm build && test -f build/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec svelte-check --threshold error` reports 0 errors, proving the generated module's types resolve and the client construction type-checks against them.
    - `pnpm build` succeeds with `web/src/lib/client.ts` present and importable.
    - `web/src/lib/client.ts` sets `useBinaryFormat` explicitly to `false`; the SUMMARY records what the installed package's own default is, read from its type definitions, and states that the option is set explicitly regardless.
    - `web/src/lib/client.ts` sets `baseUrl` to the single-character same-origin root, not an absolute `http://127.0.0.1:...` URL.
    - No module named with a `_connect` suffix exists anywhere under `web/src/lib/gen/` — `git ls-files 'web/src/lib/gen/*'` is still exactly one file.
    - The module's doc comment names D-05, D-08 and D-19.
  </acceptance_criteria>
  <done>The app can construct a typed `UIService` client over Connect's JSON wire against the same origin that serves it, and the whole `web/` tree still type-checks and builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `web/node_modules/.bin/protoc-gen-es` | A code-generating binary from the JS ecosystem now runs during `task proto:gen` and writes committed source |
| `internal/uiproto/uiv1/ui.proto` → committed `ui_pb.ts` | Generated code enters the repository and is compiled into what every user's browser executes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-SC | Tampering | `@bufbuild/protoc-gen-es` and the three Connect/protobuf runtime packages | high | mitigate | Covered by 02-01's blocking-human package-legitimacy checkpoint (which vetted `@bufbuild/protobuf` explicitly) plus `strictDepBuilds` on the install. Pinned through `web/pnpm-lock.yaml`; scoped package name verified against the registry, and the unscoped homoglyph-adjacent name confirmed to 404 rather than resolve to a squatter. |
| T-02-03-02 | Tampering | Committed generated TypeScript drifting from its `.proto` source | high | mitigate | `task proto:drift` now enumerates four files, prints the count before comparing, hard-fails below the floor of 4, and has been watched RED against both a staled file and a narrowed enumeration. |
| T-02-03-03 | Spoofing | A generation step that silently skips when its plugin is absent | high | mitigate | `proto:gen` and `proto:drift` both gain a hard precondition on the plugin binary with a named remedy; the skip-when-missing branch is prohibited (D-05). |
| T-02-03-04 | Information disclosure | The storage schema's shape leaking into browser-shipped code | medium | mitigate | `protoc-gen-es` is scoped to `internal/uiproto/uiv1` so `internal/schema/graph.proto` never reaches it; asserted by the single-file `git ls-files` count. |
| T-02-03-05 | Tampering | Runtime/plugin major-version skew producing code the runtime cannot load | low | mitigate | `@bufbuild/protobuf` and `@bufbuild/protoc-gen-es` pinned to the same minor line in `web/package.json` and locked in `web/pnpm-lock.yaml`. |
</threat_model>

<verification>
- `task proto:gen` regenerates both surfaces and leaves `git diff --exit-code` clean.
- `task proto:drift` prints a compared-count of 4 and exits 0.
- `git ls-files web/src/lib/gen` reports exactly one file.
- `pnpm exec svelte-check --threshold error` reports 0 errors; `pnpm build` succeeds.
- `buf.gen.yaml` is byte-unchanged.
</verification>

<success_criteria>
The browser has a committed, drift-guarded, type-checking TypeScript Connect client generated
from `ui.proto` alone, and the single codegen drift guard covers it at the correct floor of 4.
</success_criteria>

<output>
Create `.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-03-SUMMARY.md` when done
</output>
