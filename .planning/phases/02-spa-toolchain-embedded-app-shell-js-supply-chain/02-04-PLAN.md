---
phase: 02-spa-toolchain-embedded-app-shell-js-supply-chain
plan: 04
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/upgrade/taskfile_shape_test.go
autonomous: true
requirements: [BLD-07]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No JavaScript-toolchain command is reachable anywhere in `.goreleaser.yaml` or `.github/workflows/release.yml`, proven structurally by parsing both files rather than by a text search over them (BLD-07)."
    - "No workflow step in the signed release path installs a JavaScript runtime or package manager via a marketplace action — the `uses:` values are scanned too, because an action that puts a runtime on the runner is a JS-toolchain invocation even though it names no command."
    - "The scanner is proven able to fire: a planted token in a synthetic document is found, and near-miss strings that legitimately appear in this repository are proven NOT to be flagged."
    - "A missing or unreadable scanned file surfaces as a loud error, never as a silently-passing empty result set."
  artifacts:
    - "internal/upgrade/taskfile_shape_test.go — the fixture pair, the structural scanner, the zero-guard, and the positive-control siblings"
  key_links:
    - "The scanned set is exactly two files: `.goreleaser.yaml` (already reachable via the existing `goreleaserPath` const) and `.github/workflows/release.yml`"
    - "The fixture lives in the same file as `requiredCheckNames` and `forbiddenToolPackages` and follows their doc-comment discipline: state the source of the list and how to re-verify it"
    - "The scanner tokenizes parsed YAML scalars on shell word boundaries and compares path basenames — a raw substring search would flag `pnpm-lock.yaml` and `protoc-gen-es`, both of which legitimately appear elsewhere in this repository"
  prohibitions:
    - "`.goreleaser.yaml` and `.github/workflows/release.yml` MUST NOT gain any JavaScript-runtime or package-manager invocation, in any form: a command in a `run:`/`hooks:` scalar, a marketplace action that installs one, or a script the release path shells out to (BLD-07)."
    - "The check MUST NOT be implemented as a raw substring search over the file text — the requirement says 'checked structurally rather than by design intent', and a substring search over this repository's own strings produces false positives that would be silenced by weakening the pattern until it matched nothing."
    - "The scanner MUST NOT be trusted on a zero result alone — a positive control that plants each forbidden form and confirms detection is required before the zero is evidence of anything (repo standing decision: a transcript grep is a claim about the grep, not about the product)."
    - "The forbidden-token fixture MUST NOT be widened silently — it is the literal list BLD-07's requirement text names; adding to it is a deliberate, documented act."
    - "This plan MUST NOT edit `.goreleaser.yaml` or `.github/workflows/release.yml` — both are verified clean today; the deliverable is the guard that keeps them that way."
---

<objective>
Author the structural proof that the signed release path stays pure Go: a fixture-backed scanner
over `.goreleaser.yaml` and `.github/workflows/release.yml` that parses both files, finds zero
JavaScript-toolchain invocations, and is itself proven able to find one.

Purpose: this is the half of ROADMAP criterion 5 that is not about `pnpm audit`. Phase 2 is the
milestone's first phase to put a JS toolchain anywhere in the repository; BLD-07 is what keeps it
out of the one place it must never reach. 02-CONTEXT.md verified the baseline clean today and
positive-controlled that zero — this plan converts a one-time observation into a permanent gate.

Output: `internal/upgrade/taskfile_shape_test.go` gains a fixture, a scanner, a zero-guard and a
positive control, following the file's own established shape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-CONTEXT.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-PATTERNS.md
@internal/upgrade/taskfile_shape_test.go
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is CREATED by Phase 2 and does not exist on `main`. A source-grounding pass
must treat this list as new, not as drift. The exhaustive phase-wide list lives in
`02-01-PLAN.md`; the entries this plan owns are:

**New Go symbols, package `upgrade` (all in `internal/upgrade/taskfile_shape_test.go`):**
`releasePathScanFiles` (fixture: the two scanned paths), `forbiddenJSToolchainCommands`
(fixture), `forbiddenJSToolchainActions` (fixture), `releasePathWorkflowPath` (const),
`scanForJSToolchain` (the structural scanner, returning findings and an error),
`jsToolchainFinding` (result type), `TestReleasePathHasNoJSToolchain`,
`TestReleasePathScanIsNonVacuous`, `TestReleasePathScanIgnoresNearMisses`,
`TestReleasePathMissingFileIsError`.

**New files:** none. **Modified files:** `internal/upgrade/taskfile_shape_test.go` only.
**New Taskfile targets:** none. **New CI steps:** none — the existing required `test` context
already runs this package. **New CLI flags:** none.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged Assumptions — spec-less edge probe returned ZERO classifications

No `*-SPEC.md` exists for Phase 2. The deterministic edge probe ran as the fallback and reported
`applicable: 7, resolved: 0, unresolved: 7` — it classified nothing and contributed zero
predicates. All seven rows stay unresolved and are surfaced here; none was auto-resolved with a
backstop marker.

| Requirement | Probe verdict | Status | Flagged assumption |
|---|---|---|---|
| RPC-03 | unclassified | unresolved | Assumed: `ServeMux` precedence separates RPC from SPA. Verified by 02-02. |
| BLD-01 | unclassified | unresolved | Assumed: `packageManager` alone pins pnpm. Verified by 02-01 and 02-07. |
| BLD-02 | unclassified | unresolved | Assumed: `all:` is the only embed-walk exclusion that matters. Verified by 02-01's file-list diff. |
| BLD-03 | unclassified | unresolved | Assumed: source-tree hashing is a sound staleness proxy. 02-06 must demonstrate RED. |
| BLD-05 | unclassified | unresolved | Assumed: `strictDepBuilds` exits non-zero on an unreviewed script. 02-07 demonstrates RED. |
| BLD-06 | unclassified | unresolved | Assumed: `pnpm audit --json` distinguishes clean from failed. 02-07 confirms empirically. |
| BLD-07 | unclassified | unresolved | Assumed: release-path purity is fully expressible as a structural scan over exactly two files, and that no third file is reachable from the release path in a way that reintroduces a JS toolchain. **This plan's Task 1 must state, in the fixture's doc comment, why the set is two and how to re-verify it.** |
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture-backed structural scanner over the two release-path files</name>
  <files>internal/upgrade/taskfile_shape_test.go</files>
  <read_first>
    - internal/upgrade/taskfile_shape_test.go (lines 1-60) — the import block, the fixture-path const block including `goreleaserPath`, and the `requiredCheckNames` / `forbiddenToolPackages` fixtures whose doc-comment discipline this task copies
    - internal/upgrade/taskfile_shape_test.go (lines 690-760) — `TestRequiredCheckNamesPreserved` and its `scanned == 0` zero-guard, plus its `_ZeroJobsIsError` sibling
    - .goreleaser.yaml — read the whole file; the scanner must not false-positive on anything actually in it
    - .github/workflows/release.yml — same
    - .planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-PATTERNS.md — the `taskfile_shape_test.go` section naming the exact shape to copy
  </read_first>
  <behavior>
    Write these first and watch them RED (they will not compile until the scanner exists):
    - `TestReleasePathHasNoJSToolchain`: scanning both release-path files returns zero findings,
      AND the number of files actually read is exactly 2, AND each file contributed a non-zero
      number of examined YAML scalar nodes. A finding names the file, the YAML path where it was
      found, and the offending token, so a failure is immediately actionable.
    - `TestReleasePathMissingFileIsError`: `scanForJSToolchain` against a path that does not exist
      returns a non-nil error, never a nil error with an empty finding slice — the same defect
      class every parser in this file is built to avoid.
  </behavior>
  <action>
    Add the fixture and the scanner to `internal/upgrade/taskfile_shape_test.go`, beside the
    existing fixtures rather than in a new file — this test file is where structural assertions
    about CI and release shape live in this repository.

    (a) Add a `releasePathWorkflowPath` const alongside the existing `goreleaserPath` const, and a
    `releasePathScanFiles` fixture naming exactly those two paths. Give it a doc comment stating
    that the set is two because those are the only two files the signed release path is defined
    by, that 02-CONTEXT.md verified both clean on 2026-08-23 with a positive control against a
    search for a Go-related token in the same files, and that any new file entering the release
    path must be added here.

    <!-- planner-discipline-allow: node -->
    <!-- planner-discipline-allow: npm -->
    <!-- planner-discipline-allow: npx -->
    <!-- planner-discipline-allow: pnpm -->
    (b) Add two fixtures. `forbiddenJSToolchainCommands` holds the four command names BLD-07's
    requirement text enumerates — the Node runtime, the npm CLI, the npm package runner, and the
    pnpm CLI. `forbiddenJSToolchainActions` holds the marketplace-action owner/repo prefixes that
    put a JavaScript runtime or package manager onto a runner without naming a command:
    `actions/setup-node` and `pnpm/action-setup`. Document why the second fixture exists: an
    action that installs a runtime is a JS-toolchain invocation even though it contains none of
    the four command names, and a scan that missed it would be honest-looking and wrong.

    (c) Implement `scanForJSToolchain(path string) ([]jsToolchainFinding, int, error)` returning
    the findings, the count of YAML scalar nodes examined, and an error. Parse the file with the
    `go.yaml.in/yaml/v3` reader already imported by this test file into a document node, then walk
    the node tree. For every scalar node, split its value on whitespace and shell metacharacters,
    take each word's path basename (the segment after the last slash), and record a finding when a
    basename equals one of `forbiddenJSToolchainCommands` exactly. Separately, when a mapping key
    is the workflow `uses` key, compare its scalar value's owner/repo prefix (everything before
    the first `@`) against `forbiddenJSToolchainActions` and record a finding on an exact match.
    Return a non-nil error when the file cannot be read or does not parse, and when the examined
    scalar count is zero — an empty parse must fail loudly, not pass.

    Basename comparison is what makes this a structural check rather than a text search: a raw
    substring search would flag `pnpm-lock.yaml`, `web/node_modules`, `protoc-gen-es` and
    `actions/setup-go`, all of which legitimately exist in this repository. Task 2 proves that
    property rather than asserting it.

    (d) Write `TestReleasePathHasNoJSToolchain` and `TestReleasePathMissingFileIsError` per
    `<behavior>`, copying `TestRequiredCheckNamesPreserved`'s zero-guard shape: count what was
    actually scanned and fail loudly on zero, so a broken path or an empty read cannot read as a
    clean pass.

    Do not modify `.goreleaser.yaml` or `.github/workflows/release.yml`. If the scanner reports a
    finding in either file, stop and report it rather than editing the file to satisfy the test —
    a real finding is a genuine BLD-07 violation and needs a decision, not a fix-up.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/... -run 'TestReleasePathHasNoJSToolchain|TestReleasePathMissingFileIsError' -v 2>&1 | tee /tmp/bld07.log && test "$(grep -c -- '--- PASS' /tmp/bld07.log)" -eq 2</automated>
    <automated>git diff --exit-code -- .goreleaser.yaml .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/upgrade/... -run 'TestReleasePathHasNoJSToolchain|TestReleasePathMissingFileIsError' -v` prints exactly two `--- PASS` lines. The count is asserted because `go test -run` exits 0 when the pattern matches nothing.
    - `TestReleasePathHasNoJSToolchain` asserts the scanned-file count equals 2 and that each file contributed a non-zero examined-scalar count — a zero on either is a hard failure with a named message.
    - The scanner is invoked through a YAML document walk; the test file gains no call that reads either release-path file as raw text for the purpose of matching.
    - `scanForJSToolchain` on a nonexistent path returns a non-nil error and is asserted to do so.
    - A finding carries the file, the YAML location, and the offending token, so a failure message identifies the exact line to fix.
    - `git diff --exit-code -- .goreleaser.yaml .github/workflows/release.yml` is clean — this plan proves the property, it does not create it.
    - `go vet ./internal/upgrade/...` is clean.
  </acceptance_criteria>
  <done>A structural, fixture-backed scanner proves both release-path files carry no JavaScript-toolchain invocation, and a missing file fails loudly rather than passing empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the scanner can fire, and prove it does not fire on near misses</name>
  <files>internal/upgrade/taskfile_shape_test.go</files>
  <read_first>
    - internal/upgrade/taskfile_shape_test.go — the scanner and fixtures added by Task 1
    - Taskfile.yml — the `vuln:selftest` target, this repository's canonical non-vacuity proof: it asserts an exact exit code AND a specific named value, never either alone
    - .planning/STATE.md — the standing decision "a transcript grep is a claim about the grep, not about the product. Before recording an absence from a transcript, prove the same search can find the thing when it is present"
  </read_first>
  <behavior>
    - `TestReleasePathScanIsNonVacuous`: table-driven. For each of the four forbidden command
      names, build a synthetic in-memory YAML document that places the command in a plausible
      release-path position (a workflow `run:` scalar and a goreleaser `hooks:` list entry are
      both exercised) and assert the scanner reports exactly one finding naming that token. For
      each of the two forbidden action prefixes, build a synthetic workflow step with that `uses:`
      value — including a version-suffixed form with an `@` pin, which is how it would actually be
      written — and assert a finding is reported.
    - `TestReleasePathScanIgnoresNearMisses`: table-driven over strings that legitimately appear
      in this repository and MUST NOT be flagged — the pnpm lockfile's filename, a path under a
      dependencies directory, the generated-plugin binary name, and the Go setup action's
      owner/repo. Assert zero findings for every row. A row that fails here means the scanner
      degenerated into a substring search and would have to be weakened until it matched nothing,
      which is the exact failure mode this test prevents.
  </behavior>
  <action>
    Add both tests to `internal/upgrade/taskfile_shape_test.go`. Drive the scanner over synthetic
    in-memory YAML rather than over temporary files where the scanner's signature allows it; if
    `scanForJSToolchain` only takes a path, refactor it into a thin path-reading wrapper around a
    content-taking core so the positive control never has to write to the real repository tree.
    That refactor is preferable to writing scratch files, and it is what makes the near-miss table
    cheap to extend.

    Build the near-miss table from strings that are actually present in this repository as of this
    phase, not invented ones — read them out of `web/package.json`, `Taskfile.yml`'s new
    `proto:gen` invocation and `.github/workflows/ci.yml`'s Go setup step, and cite each row's
    source in a comment. A near-miss table of invented strings proves nothing about this
    repository's real false-positive surface.

    Give `TestReleasePathScanIsNonVacuous` a doc comment in the voice of
    `TestRequiredCheckNamesPreserved_ZeroJobsIsError`: state that a zero-findings result from
    `TestReleasePathHasNoJSToolchain` is not evidence of anything until this test has shown the
    same scanner finds a planted token, and that the two tests are a pair — neither is meaningful
    alone.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/... -run 'TestReleasePathScanIsNonVacuous|TestReleasePathScanIgnoresNearMisses' -v 2>&1 | tee /tmp/bld07-nv.log && test "$(grep -c -- '--- PASS' /tmp/bld07-nv.log)" -ge 2</automated>
    <automated>go test ./internal/upgrade/... && go vet ./internal/upgrade/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/upgrade/... -run 'TestReleasePathScanIsNonVacuous|TestReleasePathScanIgnoresNearMisses' -v` prints at least two `--- PASS` lines, and the subtest output names every table row so the count of exercised rows is visible.
    - The positive control covers all four forbidden command names in both a workflow `run:` position and a goreleaser hook position, and both forbidden action prefixes in a version-pinned `uses:` position — six planted forms minimum, each asserted to produce a finding.
    - The near-miss table covers at least four strings, each sourced from a real file in this repository with the source cited in a comment, and each asserted to produce zero findings.
    - The SUMMARY quotes the planted-token failure output for at least one row, demonstrating the message names the file, the location and the token.
    - `go test ./internal/upgrade/...` and `go vet ./internal/upgrade/...` are both green.
    - `git diff --exit-code -- .goreleaser.yaml .github/workflows/release.yml` remains clean.
  </acceptance_criteria>
  <done>The BLD-07 scanner has been proven able to find every forbidden form it claims to guard against, and proven not to fire on the real strings this repository legitimately contains — so its zero result is evidence rather than a claim about itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source → signed, attested release artifacts | Anything executable that runs inside `.goreleaser.yaml` or `release.yml` runs with release credentials and lands in the SLSA provenance |
| JS ecosystem (new this phase) → release path | Phase 2 is the first phase to put Node and pnpm anywhere in this repository; BLD-07 is the boundary that keeps them out of the signed build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Tampering | A JS-toolchain invocation entering the signed release path | high | mitigate | Fixture-backed structural scan over both release-path files, run by the already-required `test` CI context, with a positive control proving the scanner fires. |
| T-02-04-02 | Elevation of privilege | A marketplace action installing a JS runtime on the release runner | high | mitigate | The second fixture scans `uses:` values by owner/repo prefix; an action that names no command but installs a runtime is caught. |
| T-02-04-03 | Spoofing | A guard that passes because it scanned nothing | high | mitigate | Zero-guard on the scanned-file count and on the examined-scalar count per file; a missing or empty file returns an error rather than an empty result. |
| T-02-04-04 | Spoofing | A guard weakened until it matches nothing, to silence false positives | medium | mitigate | The near-miss table pins the exact real strings that must NOT be flagged, so a future weakening of the matcher breaks the positive control instead of quietly passing. |
| T-02-04-05 | Tampering | A new file entering the release path outside the scanned set | medium | accept | Recorded in the fixture's doc comment with instructions to add it. No mechanism enumerates the release path automatically today; adding one is out of this phase's scope and is not what BLD-07 asks for. |
</threat_model>

<verification>
- `go test ./internal/upgrade/...` green, with all four new tests printing `--- PASS`.
- `go vet ./internal/upgrade/...` clean.
- `.goreleaser.yaml` and `.github/workflows/release.yml` byte-unchanged.
- The positive control's planted-token failure output is captured in the SUMMARY.
</verification>

<success_criteria>
BLD-07 holds as a permanent, structurally-checked gate rather than a one-time observation: the
release path is proven free of JavaScript-toolchain invocations by a scanner that has been shown
to find one when it is there and to leave this repository's real near-miss strings alone.
</success_criteria>

<output>
Create `.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-04-SUMMARY.md` when done
</output>
