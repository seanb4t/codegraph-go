---
phase: 02-spa-toolchain-embedded-app-shell-js-supply-chain
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/pnpm-workspace.yaml
  - web/vite.config.ts
  - web/components.json
  - web/src/app.css
  - web/src/lib/utils.ts
  - web/src/routes/+layout.svelte
  - web/src/routes/+page.svelte
  - web/src/routes/browse/+page.svelte
  - web/src/routes/workbench/+page.svelte
  - web/src/routes/graph/+page.svelte
  - web/src/routes/health/+page.svelte
  - web/build/
autonomous: true
requirements: [BLD-02, RPC-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tailwind v4 and shadcn-svelte's design tokens are installed and building, so Phase 3 opens with `shadcn-svelte add <component>` and no toolchain work (D-17)."
    - "`/` renders a root layout with navigation slots named for the four future views — Browse, Workbench, Graph, Health — and each slot is a real client-side route, not a dead link (D-18)."
    - "The shell makes a real `GetStatus` call through the generated TypeScript client and renders the result, proving the whole seam in the browser: embedded asset → generated client → Connect JSON → Phase 1's handler → real index data (D-19)."
    - "The rendered status is honest in the degraded state: when `initialized` is false the shell distinguishes 'never indexed' from 'a re-index is in progress' using the response's OWN `store_exists` (field 8) and `indexing_in_progress` (field 9) fields rather than inferring either from the other, and an empty commit SHA renders as unknown rather than as a failure (Phase 1 D-14/D-16, ENG-04, D-19)."
    - "All NINE `GetStatusResponse` fields are rendered. Addresses review concern (HIGH): the previous draft said 'all eight' and omitted `indexing_in_progress = 9` — the one field Phase 1 added specifically to distinguish the two false-`initialized` states, and therefore the field D-19's honesty requirement actually rests on."
    - "The committed `web/build/` is rebuilt from this source tree and contains real CSS output, so the drift guard 02-06 adds is exercised against a real app rather than a near-empty page."
  artifacts:
    - "web/src/app.css — shadcn-svelte's generated token set plus the Tailwind v4 import"
    - "web/components.json — shadcn-svelte configuration Phase 3's `add` commands read"
    - "web/src/lib/utils.ts — the `cn()` helper every shadcn-svelte component expects at `$lib/utils`"
    - "web/vite.config.ts — `@tailwindcss/vite` plugin wired in (D-17: Tailwind v4 is a Vite plugin, not PostCSS)"
    - "web/src/routes/+layout.svelte — the shell with four named navigation slots"
    - "web/src/routes/{browse,workbench,graph,health}/+page.svelte — four real placeholder routes"
    - "web/src/routes/+page.svelte — the live GetStatus render"
    - "web/build/** — rebuilt and recommitted"
  key_links:
    - "`web/src/lib/client.ts` (from 02-03) is the only place a transport is constructed; the page imports the client, never builds its own"
    - "`GetStatus` is the chosen RPC because it is the one call that answers even when the store is degraded (Phase 1 D-16) — the shell must therefore render something honest in every state, never a spinner that never resolves. 'Every state' is exactly three, and `indexing_in_progress` (field 9) is the direct signal for the third; `ui.proto`'s own comment on field 8 says field 8 exists to separate the two false-`initialized` states, and field 9's comment says `initialized` is always false when field 9 is true"
    - "The four placeholder routes are what make ROADMAP criterion 2 honestly testable: a single-route app cannot demonstrate SPA fallback, and a route invented purely for the test proves less"
    - "Rebuilding `web/build/` here means 02-06's `web/build/.build-manifest` marker is computed against a settled source tree rather than being invalidated immediately. 02-06 does make one further source edit (an output-directory override in `web/svelte.config.js`) and rebuilds once more before writing the marker, which is why the marker is written there and not here"
  prohibitions:
    - "The design tokens MUST NOT be hand-authored — `shadcn-svelte init`'s own generated `app.css` and `components.json` are what every future `shadcn-svelte add` expects, and a hand-rolled palette mismatches in ways the CLI does not detect."
    - "Tailwind MUST NOT be wired through PostCSS — Tailwind v4 wires in through `@tailwindcss/vite` (D-17)."
    - "`shadcn-svelte add <component>` MUST NOT be run in this phase — Phase 2 ships no components. The known limitation that `pnpm audit` structurally cannot see registry-vendored component source becomes live at Phase 3's first component, and this phase records that boundary rather than crossing it."
    - "Registry pinning or a provenance assertion for vendored shadcn-svelte components MUST NOT be built — deliberately deferred (02-CONTEXT.md Deferred Ideas); it is real scope growth and BLD-06's requirement text does not ask for it."
    - "The navigation slots MUST NOT be dead anchors — each must resolve to a real client-side route, or criterion 2's fallback behavior is untestable."
    - "The GetStatus render MUST NOT treat an empty commit SHA or `initialized: false` as an error state — both are documented, expected values (ENG-04, Phase 1 D-16)."
    - "The indexing-in-progress state MUST NOT be inferred from `store_exists && !initialized`. Field 9 (`indexing_in_progress`) is the direct signal Phase 1 added for exactly this; inferring it re-implements a server-side decision on the client, on worse data, and would silently diverge the moment the server's degrade classification changes."
    - "No view content beyond navigation placeholders MAY be built — Phases 3-6 fill the slots; this phase ships the shell and one live call, nothing more."
---

<objective>
Turn the tracer's minimal page into the actual app shell: Tailwind v4 plus shadcn-svelte's token
set, a root layout carrying navigation slots for the four future views, four real placeholder
routes behind them, and a live `GetStatus` call rendered on `/`.

Purpose: D-19 is the reason this plan matters more than it looks. Without a real call, the
generated TypeScript client would ship committed and drift-guarded **without ever having been
executed** — a client that generates cleanly but does not work would pass every other Phase 2
gate. This plan executes it. D-17 and D-18 are the other half: installing the design system here
means Phase 3 opens with `shadcn-svelte add` and no toolchain work, and four real routes are what
make ROADMAP criterion 2 honestly testable.

Output: the committed `web/build/` is rebuilt from a real app with real CSS, and the browser has
walked the full seam Phase 1 proved with `curl`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-CONTEXT.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-RESEARCH.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-01-SUMMARY.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-02-SUMMARY.md
@.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-03-SUMMARY.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@internal/uiproto/uiv1/ui.proto
@internal/uiserver/degrade.go
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every item below is CREATED by Phase 2 and does not exist on `main`. A source-grounding pass must
treat this list as new, not as drift. The exhaustive phase-wide list lives in `02-01-PLAN.md`;
the entries this plan owns are:

**New files:** `web/components.json`, `web/src/app.css`, `web/src/lib/utils.ts`,
`web/src/routes/browse/+page.svelte`, `web/src/routes/workbench/+page.svelte`,
`web/src/routes/graph/+page.svelte`, `web/src/routes/health/+page.svelte`.

**New TS/Svelte symbols:** `cn` (exported from `web/src/lib/utils.ts`); the four route components;
the layout's navigation slot definitions.

**New npm dependencies:** `tailwindcss`, `@tailwindcss/vite`, `tw-animate-css`, `clsx`,
`tailwind-merge` (plus whatever `shadcn-svelte init` adds, which must be reviewed in the diff).
`shadcn-svelte` itself is invoked with `pnpm dlx` and is NOT added as a dependency.

**Rebuilt, recommitted:** the whole `web/build/` tree, now containing real Tailwind CSS output
and four routes' worth of client chunks.

**New Go symbols:** none. **New Taskfile targets:** none. **New CI steps:** none.
**New CLI flags:** none.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged Assumptions — spec-less edge probe returned ZERO classifications

No `*-SPEC.md` exists for Phase 2. The deterministic edge probe ran as the fallback and reported
`applicable: 7, resolved: 0, unresolved: 7` — it classified nothing and contributed zero
predicates. All seven rows stay unresolved and are surfaced here; none was auto-resolved with a
backstop marker.

| Requirement | Probe verdict | Status | Flagged assumption |
|---|---|---|---|
| RPC-03 | unclassified | unresolved | Assumed: the four placeholder routes exercise the fallback path in the real browser the way 02-02's `httptest` cases exercise it in Go. **This plan's human-check is what confirms it end to end.** Note the frontmatter now declares `depends_on: 02-02` — addresses review concern: Task 3's verify asserts `--- PASS` for the seven `TestSPA*` tests 02-02 authors, so an executor scheduling on `depends_on` rather than on wave would otherwise have run this plan against a two-test tree. The wave number is unchanged at 3, because 02-02 is wave 2. |
| BLD-01 | unclassified | unresolved | Assumed: adding the Tailwind/shadcn dependency set leaves `pnpm install --frozen-lockfile` reproducible. Re-asserted after this plan's installs. |
| BLD-02 | unclassified | unresolved | Assumed: the rebuilt `web/build/` still satisfies 02-01's embedded-versus-on-disk file-list diff. Re-run that test after the rebuild. |
| BLD-03 | unclassified | unresolved | Assumed: source-tree hashing is a sound staleness proxy. 02-06 must demonstrate RED. |
| BLD-05 | unclassified | unresolved | Assumed: `strictDepBuilds` exits non-zero on an unreviewed script. **Live here** — the Tailwind/shadcn dependency set is the most likely source of new `allowBuilds` entries in this phase. |
| BLD-06 | unclassified | unresolved | Assumed: `pnpm audit --json` distinguishes clean from failed. 02-07 confirms empirically. **Note the recorded limitation:** the audit cannot see registry-vendored component source, which is why this phase ships no components. |
| BLD-07 | unclassified | unresolved | Assumed: release-path purity is a token scan over two files. 02-04 positive-controls it; this plan touches neither file. |
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind v4 and shadcn-svelte's design system, installed not hand-rolled</name>
  <files>web/package.json, web/pnpm-lock.yaml, web/pnpm-workspace.yaml, web/vite.config.ts, web/components.json, web/src/app.css, web/src/lib/utils.ts</files>
  <read_first>
    - web/vite.config.ts — the plugin array the Tailwind plugin joins
    - web/svelte.config.js — the `$lib` alias configuration the `cn()` import path depends on
    - web/pnpm-workspace.yaml — the `strictDepBuilds` and `allowBuilds` state 02-01 committed; new dependencies may need approval entries
    - .planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-RESEARCH.md — "Supporting (D-17's design-system slice)" version table and the "Don't Hand-Roll" table
    - .planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-CONTEXT.md — D-17 and the "Known Limitation" section on registry-vendored components
  </read_first>
  <action>
    Install the design system through its own CLI, not by hand.

    (a) Add `tailwindcss` and `@tailwindcss/vite` as devDependencies and wire the Tailwind plugin
    into `web/vite.config.ts`'s plugin array. Tailwind v4 is a Vite plugin, not a PostCSS plugin
    (D-17) — do not create a PostCSS config. Versions verified on the registry 2026-08-23:
    `tailwindcss` and `@tailwindcss/vite` both `4.3.3`; keep them on the same version.

    (b) Add `clsx` and `tailwind-merge` as runtime dependencies and `tw-animate-css` as a
    devDependency. `tw-animate-css` is the Tailwind v4 replacement for `tailwindcss-animate`;
    do not install the v3-era package.

    (c) Run `pnpm dlx shadcn-svelte@1.5.0 init` against the EXISTING project — an EXACT pinned
    version, never `@latest`. Addresses review concern: a `dlx`-executed CLI is not covered by
    `pnpm-lock.yaml` (and this plan deliberately keeps it out of `package.json`), so the version
    pin plus a pre-execution integrity check is its only provenance — in the phase whose whole
    subject is JS supply chain. `1.5.0` is the version resolved from the registry on 2026-08-24,
    whose published tarball integrity is
    `sha512-fcxTeUwEIvELkL/h0Vg2i/9Sf6exAmLG5u3GXc0aN8CA+3fNffKQnfrTdMgkjUEmx9XP619I+wcJOvlr5aDWYQ==`.
    Before running it, fetch `https://registry.npmjs.org/shadcn-svelte/1.5.0` and confirm
    `.dist.integrity` matches that string exactly; a mismatch means the fixed version was
    republished, which is a supply-chain event — STOP and report rather than proceeding. Record
    the confirmed version and integrity in the SUMMARY. `init` targets an
    existing project, not a scaffolding step (D-17). It generates `web/components.json`,
    `web/src/app.css` with the full token set (`--background`, `--foreground`, `--primary`, and
    the rest), and `web/src/lib/utils.ts` with the `cn()` helper. Do not hand-author any of the
    three: the token set is exactly what every future `shadcn-svelte add` expects, and a
    hand-rolled palette mismatches in ways the CLI does not detect. Invoke it with `pnpm dlx` so
    the CLI never enters `web/package.json` as a dependency.

    (d) If `init` prompts interactively, answer with the configuration this project needs and
    record every answer in the SUMMARY: the `$lib` alias SvelteKit already configures, the
    existing `web/src/app.css` path, and the Svelte 5 / Tailwind v4 combination the current
    shadcn-svelte requires.

    (e) Import `web/src/app.css` from the root layout so the tokens actually reach the rendered
    page — an `app.css` that is generated but never imported produces a build with no CSS output
    at all, which is the specific failure D-17's "exercised against real CSS output from day one"
    rationale exists to prevent.

    (f) Re-run `pnpm approve-builds` and commit any new `allowBuilds` entries the design-system
    dependency set introduces. Record the entry count before and after in the SUMMARY — 02-07
    needs both numbers to demonstrate BLD-05 RED against a removed entry.

    (g) Do NOT run `shadcn-svelte add` for any component. Phase 2 ships no components. Record in
    the SUMMARY the boundary 02-CONTEXT.md names: BLD-06's audit covers the dependency tree in
    `pnpm-lock.yaml` and does NOT cover registry-fetched component source, and that gap becomes
    live the moment Phase 3 adds its first component.
  </action>
  <verify>
    <automated>cd web && pnpm install --frozen-lockfile && pnpm build && test -d build/_app/immutable && css=$(find build/_app/immutable -type f -name '*.css' | LC_ALL=C sort | head -1) && test -n "$css" && test -s "$css"</automated>
    <automated>set -o pipefail; cd web && pnpm exec svelte-check --threshold error --output human 2>&1 | tee /tmp/sc1.log && test -s /tmp/sc1.log && grep -qE 'found 0 errors' /tmp/sc1.log</automated>
    <automated>test -s web/components.json && test "$(git ls-files 'web/src/lib/*' | wc -l | tr -d ' ')" -ge 1 && test "$(git ls-files 'web/src/lib/components/ui/*' | wc -l | tr -d ' ')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` emits at least one non-empty `.css` file under `web/build/_app/immutable/` — the path is captured into a variable, asserted non-empty, and only then size-checked. Addresses a vacuity the review's inventory did not reach: the previous form piped `find` into `xargs test -s`, and when `find` matched nothing `xargs` ran a bare `test -s`, which evaluates the single argument as a non-empty string and returns TRUE — so "no CSS was emitted at all" passed.
    - The `svelte-check` assertion is anchored to `found 0 errors`, joined with `&&`, run under `set -o pipefail`, and preceded by a non-empty-log check — so neither a count ending in zero nor a crashed `pnpm exec` nor a stale log can produce a green result.
    - `web/components.json` and `web/src/app.css` exist and were produced by `shadcn-svelte init`, not hand-authored; the SUMMARY records the CLI invocation and every prompt answer.
    - `web/src/lib/utils.ts` exports `cn` and is importable at the `$lib/utils` path every shadcn-svelte component assumes.
    - `web/vite.config.ts` includes the Tailwind Vite plugin in its plugin array, and no PostCSS configuration file exists anywhere under `web/`.
    - `shadcn-svelte` does not appear in `web/package.json`'s dependency or devDependency maps — it is invoked via `pnpm dlx` at the exact pinned version `1.5.0`, with its registry integrity confirmed beforehand, and no `@latest` tag appears in any command this task ran. The SUMMARY records the version and integrity string.
    - The no-vendored-component check carries a command-local positive control: the same command asserts `web/components.json` is non-empty AND that `git ls-files 'web/src/lib/*'` returns at least one entry, before asserting `git ls-files 'web/src/lib/components/ui/*'` returns exactly zero. Addresses review concern: a bare zero-entry `git ls-files` returns zero identically when the project, the alias root, or the whole `web/` tree is absent — so the absence proved nothing about the configured component root having been scanned.
    - `pnpm install --frozen-lockfile` exits 0 and leaves `git diff --exit-code web/pnpm-lock.yaml` clean after the installs are committed.
    - The SUMMARY records the `allowBuilds` entry count before and after this task.
  </acceptance_criteria>
  <done>Tailwind v4 and shadcn-svelte's token set are installed through their own tooling, the build emits real CSS, and Phase 3 can open with `shadcn-svelte add` and no toolchain work.</done>
</task>

<task type="auto">
  <name>Task 2: Root layout with four named navigation slots and four real routes</name>
  <files>web/src/routes/+layout.svelte, web/src/routes/browse/+page.svelte, web/src/routes/workbench/+page.svelte, web/src/routes/graph/+page.svelte, web/src/routes/health/+page.svelte</files>
  <read_first>
    - web/src/routes/+layout.svelte — the tracer's layout, and the `app.css` import Task 1 added
    - web/src/routes/+layout.ts — the `ssr = false` / `prerender = false` exports that make the fallback build legal; the new routes must not re-enable either
    - web/src/app.css — the token names available for styling the navigation
    - .planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-CONTEXT.md — D-18 and its rationale about criterion 2's testability
    - .planning/ROADMAP.md — the one-line goals for Phases 3, 4, 5 and 6, which are what the four slots are named for
  </read_first>
  <action>
    Build the shell.

    (a) Extend `web/src/routes/+layout.svelte` into the app shell: a persistent header or sidebar
    carrying four navigation entries labelled Browse, Workbench, Graph and Health, each an
    `href` to `/browse`, `/workbench`, `/graph` and `/health` respectively, plus the layout's
    children slot for the active route. Mark the active entry using SvelteKit's own page store
    rather than hand-tracking state.

    (b) Create the four route files, each a minimal placeholder naming the phase that will fill
    it and the one-line goal from the ROADMAP: Browse is Phase 3 (find any symbol or file, read
    its verbatim source with callers, callees and blast radius), Workbench is Phase 4 (run the
    four graph analyses interactively), Graph is Phase 5 (the whole repository as one readable
    picture at file/package granularity), Health is Phase 4's index-health half (tell at a glance
    whether the index is worth trusting). Keep each page to a heading and that one line — this is
    a slot to be filled, not a feature to be prototyped.

    (c) Confirm the four routes do NOT get prerendered into their own directories: after
    `pnpm build`, `web/build/` must contain the fallback `index.html` and no `browse/index.html`.
    If any route did prerender, the `+layout.ts` export was overridden somewhere and the SPA
    fallback for that route stops being exercised — fix the override rather than accepting the
    prerendered file.

    (d) Styling and labels are Claude's Discretion under D-18. Use the shadcn token variables so
    the shell inherits the design system rather than carrying its own colours. Keep it plain —
    Phases 3-6 will restructure the content area, not the chrome.

    Do not add any view content beyond the placeholder line. Do not add a search box, a data
    table, a graph canvas or a status widget to any of the four routes — those are Phases 3-6.
  </action>
  <verify>
    <automated>cd web && pnpm build && test -s build/index.html && test ! -e build/browse/index.html && test ! -e build/workbench/index.html && test ! -e build/graph/index.html && test ! -e build/health/index.html</automated>
    <automated>set -o pipefail; cd web && pnpm exec svelte-check --threshold error --output human 2>&1 | tee /tmp/sc2.log && test -s /tmp/sc2.log && grep -qE 'found 0 errors' /tmp/sc2.log</automated>
  </verify>
  <acceptance_criteria>
    - Four route files exist under `web/src/routes/` at the `browse`, `workbench`, `graph` and `health` paths, each containing a placeholder page.
    - `web/src/routes/+layout.svelte` contains four navigation entries whose `href` values are exactly the four route paths — verified by reading the file, and each label matches the view name the ROADMAP uses.
    - After `pnpm build`, `web/build/index.html` exists and is NON-EMPTY (the command-local positive control for the four absence assertions beside it), and no per-route `index.html` was prerendered — asserted for all four routes, not a sample of two.
    - `pnpm exec svelte-check --threshold error` prints `found 0 errors`, asserted with the anchored pattern under `set -o pipefail` against a non-empty log.
    - No route file contains a search input, a table, a canvas element or a status display — this task's diff adds a heading and one descriptive line per route and nothing else.
    - The SUMMARY records that the four slots exist to be filled by Phases 3-6 and names which phase owns which slot.
  </acceptance_criteria>
  <done>The shell has four named navigation slots backed by four real client-side routes, none of them prerendered, so Phases 3-6 fill a slot rather than restructuring and ROADMAP criterion 2 is honestly testable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The live GetStatus render — the seam proven in the browser</name>
  <files>web/src/routes/+page.svelte, web/build/</files>
  <read_first>
    - web/src/lib/client.ts — the exported client this page consumes; do not construct a second transport
    - web/src/lib/gen/ui_pb.ts — the `GetStatusResponse` field names as the plugin emitted them (read the response message's generated type, not the whole file); confirm the generated type carries NINE fields and record the observed list in the SUMMARY
    - internal/uiproto/uiv1/ui.proto (the `GetStatusResponse` message, lines 116-177 — the message ends at field 9, not field 8) — every field's documented meaning, especially field 8 `store_exists` (whose own comment says it exists to separate the two false-`initialized` states), field 9 `indexing_in_progress` (whose comment says `initialized` is always false when it is true), and why an empty `commit_sha` means unknown rather than error
    - internal/uiserver/degrade.go (lines 1-70) — `degradeNotInitialized` versus `degradeIndexingInProgress`, and the fact that GetStatus answers in BOTH from filesystem facts rather than failing
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md — D-16, the basis for choosing GetStatus as this phase's live call
  </read_first>
  <behavior>
    The verification here is a real end-to-end exercise, not a unit test — there is no JS test
    framework in this phase and none is required. The observable behaviours the human-check and
    the Go-side assertions must confirm:
    - Against a repository WITH an index: `/` renders non-placeholder values for node count, edge
      count, file count and schema version, taken from the live response.
    - Against a repository WITHOUT an index: `/` renders a "not indexed" state — `initialized`
      false, `store_exists` false, `indexing_in_progress` false.
    - Against a repository whose store is locked past the retry budget: `/` renders a
      "re-index in progress" state, selected by `indexing_in_progress` being true — read
      DIRECTLY off field 9, never inferred from `store_exists && !initialized`. Addresses review
      concern (HIGH): the previous draft named eight fields and omitted field 9, which forced the
      shell to re-derive a server-side classification on worse data.
    - An empty commit SHA renders as an explicit unknown, never as an error and never as an empty
      cell with no explanation.
    - A transport-level failure (server unreachable) renders a named error state, not an
      indefinite loading spinner.
  </behavior>
  <action>
    Replace the tracer's placeholder `web/src/routes/+page.svelte` with the live status render.

    (a) Import the client from `$lib/client` and call the `getStatus` method with an empty request
    message. Do not construct a transport here — 02-03's module is the single place a transport
    exists, so a future base-URL or wire-format change has one site.

    (b) Render all NINE fields the response carries: `initialized`, `version` (schema version),
    `node_count`, `edge_count`, `file_count`, `stale`, `commit_sha`, `store_exists`, and
    `indexing_in_progress`. The count is nine, not eight — verify it against
    `internal/uiproto/uiv1/ui.proto` before writing the template, and record the nine labels in
    the SUMMARY. Label each with what it means rather than with its wire name — this page is the
    first thing a developer sees and it is the proof the seam works, so it should read as a
    status panel, not as a JSON dump.

    (c) Handle the three states explicitly and name each one in the rendered output:
    initialized-and-healthy; not-initialized (no index anywhere up the resolution walk); and
    re-index-in-progress (an index exists but the store was locked past `graphstore.Open`'s
    bounded retry budget). Select the third state from `indexing_in_progress` DIRECTLY — Phase 1
    added field 9 for exactly this, and its own proto comment states that `initialized` is always
    false when it is true. Do not compute the third state as `store_exists && !initialized`:
    that re-implements a server-side classification on the client, on strictly worse data, and
    would diverge silently the moment the server's degrade classification changes. `store_exists`
    is still rendered on its own — it is what makes the not-initialized state legible — but it is
    not the selector.

    (d) Render an empty commit SHA as an explicit unknown with a one-line explanation that the
    graph predates the commit-aware schema field or was built outside a git checkout — both are
    documented, expected values (ENG-04), not failures.

    (e) Wrap the call so a transport-level rejection renders a named error state with the message,
    rather than leaving a loading state that never resolves. Keep it to one `catch` and one error
    view — this is a shell, not an error-handling framework.

    (f) Rebuild with `pnpm build` and commit the regenerated `web/build/` tree. This is the build
    02-06's `web/build/.build-manifest` marker will be computed against, so it must be the final one for the
    phase's source tree.

    (g) Re-run 02-01's embedded-versus-on-disk file-list diff test after the rebuild — the embed
    directive is unchanged but the file set is entirely different, and that test is the only thing
    that proves the two sides still agree.
  </action>
  <verify>
    <automated>set -o pipefail; cd web && pnpm build && cd .. && go build ./... && go test ./internal/uiserver/... -run 'TestEmbeddedFSMatchesOnDiskBuildTree|TestEmbeddedBuildTreeIsNonTrivial|TestSPA' -v 2>&1 | tee /tmp/shell.log && test -s /tmp/shell.log && test "$(grep -c -- '--- FAIL' /tmp/shell.log)" -eq 0 && for t in TestEmbeddedFSMatchesOnDiskBuildTree TestEmbeddedBuildTreeIsNonTrivial TestSPAServesEmbeddedIndexAtRoot TestSPAClientRouteFallsBackToIndex TestSPAImmutableAssetMissReturns404 TestSPAImmutableAssetHitIsCachedImmutable TestSPASetsCSPOnEveryResponse TestSPACSPHashesCoverEmbeddedInlineScripts TestSPARPCPathReachesConnectHandler; do test "$(grep -c -- "--- PASS: $t" /tmp/shell.log)" -eq 1 || exit 1; done</automated>
    <automated>set -o pipefail; cd web && pnpm exec svelte-check --threshold error --output human 2>&1 | tee /tmp/sc3.log && test -s /tmp/sc3.log && grep -qE 'found 0 errors' /tmp/sc3.log</automated>
    <human-check>Run `go build -o /tmp/cg ./cmd/codegraph && /tmp/cg ui` from this repository (which HAS an index) and open the printed URL. Confirm: (1) the status panel shows real non-zero node/edge/file counts and a schema version; (2) the four navigation slots are present and clicking each one loads its placeholder page without a full reload; (3) opening one of those routes directly in a fresh tab still loads the app; (4) the browser console shows no errors **and specifically no Content-Security-Policy violation reports** — 02-02's policy derives its `script-src` hashes from the embedded `index.html`, and this rebuild replaces those bytes, so this is the first run where the derived policy meets the real app. A CSP violation here means STOP and report; it does not mean loosen the policy. Then run the same binary with its working directory set to a temporary directory that has no index, and confirm the panel renders the not-indexed state rather than an error or a hung spinner. Record both screenshots or transcripts, and record the exact `Content-Security-Policy` response header observed on `GET /`.</human-check>
  </verify>
  <acceptance_criteria>
    - `web/src/routes/+page.svelte` imports the client from the `$lib/client` module and contains no `createConnectTransport` call of its own.
    - The page renders all **nine** `GetStatusResponse` fields, each with a human label; the SUMMARY lists the nine labels used and states the field numbers they map to. Addresses review concern (HIGH): the message declares `indexing_in_progress = 9` at `internal/uiproto/uiv1/ui.proto:177`, so "all eight" was an off-by-one that dropped the exact field D-19's honesty requirement depends on.
    - The three states (healthy, not-initialized, re-index-in-progress) are each named in the rendered output, and `indexing_in_progress` — not a `store_exists && !initialized` inference — is what selects the third. Verified by reading the template's branch conditions: the SUMMARY quotes them.
    - An empty commit SHA renders an explicit unknown with the one-line explanation; the SUMMARY quotes that string.
    - `pnpm exec svelte-check --threshold error` prints `found 0 errors` and `pnpm build` succeeds.
    - After the rebuild, `go test ./internal/uiserver/... -run 'TestEmbeddedFSMatchesOnDiskBuildTree|TestEmbeddedBuildTreeIsNonTrivial|TestSPA' -v` prints **exactly one** `--- PASS` line for each of the nine load-bearing test names asserted in the verify block and **zero** `--- FAIL` lines, under `set -o pipefail` — the embedded set still equals the on-disk set against a completely regenerated build tree, and the CSP hashes were re-derived against the new `index.html`. The SUMMARY records the total observed `--- PASS` count.
    - The human-check transcript in the SUMMARY records real non-zero counts from the indexed run AND the not-indexed state from the second run, proving the generated client was actually executed rather than merely compiled.
    - `git status --porcelain web/build` is empty after the commit — the rebuilt tree is committed, not left dirty.
  </acceptance_criteria>
  <done>The browser has walked the full seam — embedded asset, generated TypeScript client, Connect JSON, Phase 1's handler, real index data — and the shell renders honestly in the indexed and the degraded state alike.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn-svelte registry → committed `.svelte` source | Registry-fetched component source enters the repository outside the lockfile — not crossed in this phase, recorded because Phase 3 crosses it |
| npm registry → design-system dependency tree | A large new dependency set, the most likely source of new lifecycle-script approvals in this phase |
| Connect JSON response → rendered DOM | Server-controlled values are interpolated into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-SC | Tampering | Tailwind/shadcn dependency install | high | mitigate | `strictDepBuilds: true` is already in effect from 02-01, so any unreviewed lifecycle script in the new tree fails the install rather than running silently; new `allowBuilds` entries are reviewed in the diff and their count recorded. 02-01's blocking-human legitimacy checkpoint covered `bits-ui` and `shadcn-svelte` explicitly. |
| T-02-05-02 | Tampering | Registry-vendored component source bypassing `pnpm audit` | medium | accept | Recorded, not solved. Phase 2 ships zero components, asserted by the zero-entry check on the components directory, so the gap is not live. It becomes live at Phase 3's first `shadcn-svelte add`; the boundary is named in the SUMMARY so Phase 3 inherits it rather than discovering it. |
| T-02-05-03 | Tampering | Cross-site scripting via a status field rendered into the DOM | low | mitigate | Svelte escapes interpolated text by default; the page uses ordinary text interpolation and no raw-HTML directive. Verified by confirming no raw-HTML rendering directive appears in the page. |
| T-02-05-04 | Information disclosure | The status panel exposing repository paths to a page the user did not intend to open | low | accept | The listener is loopback-only and `originHostGuard` (Phase 1 SRV-02) already rejects cross-origin and rebound-Host requests before any handler runs. The panel shows counts and a commit SHA, not paths. |
| T-02-05-05 | Denial of service | A never-resolving status call leaving the shell in a permanent loading state | low | mitigate | The call is wrapped so a transport rejection renders a named error state; the acceptance criteria require the error branch to exist and be reachable. |
| T-02-05-06 | Tampering | Project-generation tooling executed from a floating registry tag | high | mitigate | `pnpm dlx shadcn-svelte@1.5.0` with its registry `dist.integrity` confirmed before execution; a mismatch halts the task. The CLI is deliberately absent from `web/package.json`, so it is never covered by `web/pnpm-lock.yaml` and the pin plus the integrity check is its only provenance. |
| T-02-05-07 | Spoofing | A status panel that misreports a locked store as "never indexed" | medium | mitigate | The re-index-in-progress state is selected from `indexing_in_progress` (field 9) directly rather than inferred from `store_exists && !initialized`, so the client cannot diverge from the server's own degrade classification. Rendering all nine fields is the acceptance criterion that keeps the signal present. |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile`, `pnpm exec svelte-check --threshold error` and `pnpm build` all green in `web/`.
- `go build ./...` and `go test ./internal/uiserver/...` green against the rebuilt embed tree.
- The build emits real, non-empty CSS under the immutable-asset directory.
- No component was vendored: the shadcn components directory is empty, asserted beside a positive control that the project and its `$lib` root were actually present when scanned.
- Both `pnpm dlx` invocations across this phase ran at exact pinned versions with registry integrity confirmed beforehand; no `@latest` tag remains anywhere in the phase.
- The human-check confirms live status data in the browser, the not-indexed state in a bare directory, and **zero Content-Security-Policy violations in the console** against the rebuilt bundle.
</verification>

<success_criteria>
The app shell exists with its four named slots, the design system is installed through its own
tooling, and the generated TypeScript client has been proven to actually work by rendering a live
`GetStatus` response in a real browser — in both the indexed and the degraded state.
</success_criteria>

<output>
Create `.planning/phases/02-spa-toolchain-embedded-app-shell-js-supply-chain/02-05-SUMMARY.md` when done
</output>
