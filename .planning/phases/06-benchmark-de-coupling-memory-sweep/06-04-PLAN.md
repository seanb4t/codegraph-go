---
phase: 06-benchmark-de-coupling-memory-sweep
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - docs/BENCHMARKS.md
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-CENSUS.md
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/COVERAGE.md
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-PREPLAN-SHA.txt
autonomous: false
requirements: [BENCH-01, BENCH-02, BENCH-03]

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`docs/BENCHMARKS.md` publishes absolute indexing throughput, query latency and peak RSS per corpus entry, each figure traceable to a recorded measurement run, and carries no head-to-head comparison table and no cross-implementation multiplier (BENCH-01)."
    - "`docs/BENCHMARKS.md` carries the measurement methodology (what is measured, median-of-5, OS-level peak RSS, pinned-SHA corpora) and the regression-gate description, so a reader can reproduce both the published numbers and the gate."
    - "Every published figure's provenance — measuring commit, date, goos, goarch, runner identity and median_of_trials — is stated in the document, copied verbatim from the measurement run's own JSON."
    - "A positive-controlled multiline census over the declared Phase 6 file surface reports a verbatim total of 0 retired-framing occurrences, after the instrument was shown live to catch a planted phrase wrapped across a comment line break."
    - "That census's `TS` alternation is bounded to its retired-framing senses, so `TS` meaning TypeScript-the-indexed-language survives untouched — including `TS/JS` in `corpora/manifest.json`'s LOCKED (01-06) corpus-language-coverage note, which a bare `\\bTS\\b` made the zero unreachable over (cycle-3 HIGH 2). The bare instrument is still run as a completeness control and every residual line is quoted and adjudicated in `06-CENSUS.md`."
    - "Both `TS` instruments in this plan are bounded the same way: the `docs/BENCHMARKS.md` document gate carries the identical class-fix-D senses as the phase census, so no criterion in the phase forbids `TS/JS` as an indexed-language pair while another criterion calls that same use legitimate (cycle-4 LOW 1)."
    - "Every census exclusion traces to a recorded prior adjudication, named in `06-CENSUS.md`; no exclusion was added to make the total read zero."
    - "BENCH-03 is never closed silently: `06-LIVE-VERIFICATIONS.md` carries exactly one of two verbatim status tokens for it, and a blocking decision gate resolves which — a local measurement satisfies BENCH-01's need for numbers but is not evidence that `bench.yml` ran (review HIGH 5)."
    - "A `closed-by-ci-run` token identifies a REAL run: the token line carries a well-formed GitHub Actions run URL and the record carries that run's `gh run view --json conclusion,event,headBranch,url` output showing a successful conclusion (cycle-2 MEDIUM 1)."
    - "Every published data cell is MECHANICALLY derived from `06-PUBLISH-RESULTS.json` by `tools/bench/publishcheck -emit-rows`, so no metric can be mis-transcribed without failing (review finding on numeric traceability)."
  artifacts:
    - "docs/BENCHMARKS.md — rewritten from scratch on absolute-measurement terms"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json — the raw measurement of record"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-CENSUS.md — the phase acceptance census, its instrument, its positive control and its exclusions"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md — the phase's live-verification ledger; created here with the BENCH-03 row, appended by 06-05 (MEM-02 file half) and 06-06 (MEM-02 store half)"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/COVERAGE.md — the reasoned no-external-API declaration"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-PREPLAN-SHA.txt — the pre-plan commit every tamper guard in this plan compares against (review class fix A)"
  key_links:
    - "tools/bench/runner -mode publish -> 06-PUBLISH-RESULTS.json -> docs/BENCHMARKS.md's absolute table: the document publishes measured numbers, never estimated ones"
    - "docs/BENCHMARKS.md's regeneration section -> the bench.yml publish job id and the runner mode string: both must name what 06-01 and 06-02 actually created"
    - "the census instrument's positive control -> every zero this phase reports: an unproven instrument makes every downstream zero meaningless"
  prohibitions:
    - "MUST NOT publish a performance figure that was not produced by a recorded measurement run — no figure may be carried over from the retired comparison tables, estimated, or rounded from memory."
    - "MUST NOT reword innocent third-party or contributor prose, or widen a census exclusion, to make the census total read zero — if a pattern flags ordinary English, bound the pattern and record the change."
    - "MUST NOT edit `NOTICE`: its upstream MIT attribution is a licence obligation, not framing."
    - "MUST NOT edit `CHANGELOG.md` or anything under `.planning/` archives as part of the sweep — the first is release-please-owned and the second is an append-only record parsed by scope-sensitive tooling."
---

<objective>
Produce the measurement of record on the de-coupled path, rewrite `docs/BENCHMARKS.md` from scratch
around it, and close the phase's in-tree work with a positive-controlled census.

Purpose: BENCH-01 requires the published document to carry absolute figures with methodology and
the regression-gate description, and no head-to-head content. D-02 makes this a rewrite rather than
an edit, which creates a hard task ordering: the current document's absolute figures live *inside*
the comparison tables being deleted, so a fresh measurement must exist before there is anything to
publish.

Output: a committed raw measurement, a rewritten benchmark document, and a census record whose
instrument was proven before its zero was believed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PATTERNS.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-SUMMARY.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-02-SUMMARY.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See `06-01-PLAN.md` → "Artifacts this phase produces" for the full created/deleted register.
This plan CREATES `06-PUBLISH-RESULTS.json`, `06-CENSUS.md`, `06-LIVE-VERIFICATIONS.md`,
`06-04-PREPLAN-SHA.txt` and `COVERAGE.md` in the phase directory, and rewrites
`docs/BENCHMARKS.md`. It CONSUMES `tools/bench/publishcheck` (created by 06-01) both as the JSON
verifier and as the generator of the published table's data rows. It deletes no symbol; it deletes document sections
(`### Comparison target`, the head-to-head raw-numbers tables, the ratio summary, the head-to-head
run-to-run repeatability section and the superseded v0.1 provisional table).
</artifacts_this_phase_produces>

<edge_probe_coverage>
Spec-less probe fallback (no SPEC.md for this phase). Rows owned by this plan:

| Requirement | Category | Status | Disposition |
|---|---|---|---|
| BENCH-01 | unclassified | **unresolved** | **Flagged assumption — not auto-resolved.** Assumption: "publishes absolute throughput / query-latency / peak-RSS figures with methodology" is satisfied by one row per corpus entry with the metric columns the retired table already used minus its subject column, plus an inline provenance block. `06-RESEARCH.md` Open Question 2 records that no table shape was specified; this is the plan's answer, and this row is where to correct it. |
| BENCH-02 | unclassified | **unresolved** | **Flagged assumption — not auto-resolved.** This plan contributes only the census half (the comparison runner is absent from `tools/bench`, measured rather than asserted). The gate-fires half is 06-03's. |
| BENCH-03 | unclassified | **unresolved** | **Flagged assumption — not auto-resolved.** The live `workflow_dispatch` is manual-only by nature. Following review HIGH 5, it is no longer a conditional check: Task 3 is a BLOCKING decision gate that resolves BENCH-03 into exactly one of two recorded states — closed by a successful run, or explicitly pending. The static structural verifier in 06-02 covers the "no second implementation" half; it does not and cannot cover "a run publishes". |
</edge_probe_coverage>

<review_incorporation>
## Cross-AI review findings incorporated (`06-REVIEWS.md`)

**HIGH 5 — BENCH-03 could close without `bench.yml` ever running.** The plan sanctioned a local
fallback, while BENCH-03's only live verification was a `<human-check>` scoped "If the preferred
CI path was used". Take the fallback and BENCH-03 has **zero** verification while the plan
completes. BENCH-03 says the workflow runs and publishes; a local runner invocation does not
establish that.

**Fix, and the line it deliberately does not cross.** D-02 requires a fresh absolute-only
measurement run — it does not mandate where that run happens, so the local fallback is NOT
reopened. What changes is that the fallback now satisfies **BENCH-01 only**. The two requirements
are separated:

- **BENCH-01** needs numbers to publish. A local run supplies them, with its reproducibility
  constraints recorded (see below).
- **BENCH-03** needs a `bench.yml` run. Only a successful dispatch supplies that. Task 3 is a
  blocking `checkpoint:decision` whose two outcomes are both recorded verbatim in
  `06-LIVE-VERIFICATIONS.md`: `BENCH03_STATUS=closed-by-ci-run <url>` or
  `BENCH03_STATUS=pending-no-ci-run <reason>`. There is no third path and no silent close.

`checkpoint:decision` is used rather than `checkpoint:human-verify` because this repository runs
`workflow.human_verify_mode: end-of-phase`, under which human-verify checkpoints are not emitted;
a decision gate is the sanctioned blocking instrument and 06-06 already uses one. This plan
therefore becomes `autonomous: false`.

| Finding | Fix |
|---|---|
| **HIGH 5** — BENCH-03 closable with no workflow run | Task 3 blocking decision gate + a two-token status ledger; the `<human-check>` is no longer the only gate and is no longer conditional. |
| `06-04:142` — byte identity to stdout not established (first-line-starts-with-`[` plus a SUMMARY sentence is not a comparison, and it depended on the file being staged) | Task 1 tees the runner's stdout to a temp file and the committed artifact in the same pipeline, then compares them with `cmp` and cross-checks the SHA-256 `publishcheck` prints for each. For the CI path, the downloaded artifact is compared the same way. |
| numeric traceability spot-checked only `files_per_sec`; query latency, peak RSS, bytes/s and cold start could be mis-transcribed | Task 2 does not hand-type any data cell. `tools/bench/publishcheck -emit-rows` generates the Markdown rows from the JSON, and the acceptance criterion re-runs the emitter and requires every emitted row to appear verbatim in the document. All ten cells, mechanically. |
| the local fallback imposes no stable runner conditions, so published figures become machine-specific | Recorded rather than waved through: a local measurement must publish its machine conditions in the provenance block and label the figures as locally measured on an unpinned runner. This does not contradict D-02; it makes the weaker reproducibility legible to a reader. |
| `06-04:143` — a `>= 12` file floor admits a shifted surface (a deleted critical file plus unrelated additions preserves the count) | Task 4 asserts an explicit per-file presence check over a named list of thirteen critical files, printed as a fraction, in addition to the floor. |
| `06-04:135` — the same undeclared Node dependency as `06-01:246` | Replaced by `tools/bench/publishcheck`, the pure-Go verifier 06-01 authors. |
| Class fixes A and B | The `NOTICE` / `CHANGELOG.md` / `baseline.json` guards compare against `06-04-PREPLAN-SHA.txt` and carry positive companions. |

## Cycle 2

| Cycle-2 finding | Fix |
|---|---|
| **MEDIUM 1** — the `closed-by-ci-run` token was counted but its `<url>` was never validated: nothing asserted a non-empty URL, a matching workflow, a matching ref or `conclusion == success`, so a mis-selected close passed the machine check | Task 3's option now states the evidence it obliges; Task 4 requires a well-formed `https://github.com/<owner>/<repo>/actions/runs/<id>` URL **on the token line** (`BENCH03_RUN_URL_ON_TOKEN=1`) and the verbatim `gh run view <id> --json conclusion,event,headBranch,url` output beneath it reporting success (`BENCH03_CONCLUSION_SUCCESS >= 1`). The check branches on the token, so the pending path prints `n/a-pending` and is not asserted — satisfiable on both paths, vacuous on neither. |
| **LOW 4** — `rg -c '^## ' docs/BENCHMARKS.md` returns `3` for any three headings and cannot show WHICH sections exist | Task 2's action pins the three `##` heading texts verbatim (`## 1. Methodology`, `## 2. Absolute numbers`, `## 3. Regression gate (PERF-02, INDX-06)` — the first unchanged from the current file, the third carried forward from `docs/BENCHMARKS.md:242`), and the criterion asserts each present exactly once. The count survives only as a printed floor companion. |
| **Class fix C** (see 06-03's `<review_incorporation>`) | This plan's only `rg -c` uses are the `NOTICE` copyright `= "1"` comparisons, which are fed by a real match and are unaffected. Checked, not assumed. |

## Cycle 3

| Cycle-3 finding | Fix |
|---|---|
| **HIGH 2** — `PHASE6_CENSUS_TOTAL = 0` could not be reached. The set carried a bare `-i \bTS\b` and the surface includes `corpora/manifest.json`, whose line 10 holds `TS/JS` in a `LOCKED (01-06)` corpus-language-coverage note. Measured over that file alone: **1** hit. The file is in no plan's `files_modified`; the text is this plan's own declared KEEP class (the ground on which it already excludes `internal/indexer/**` and `testdata/golden/**`); both executor escapes are prohibited by this plan's own prohibitions; and the sanctioned remedy — "bound the pattern" — was unavailable at execution time because the pattern is hard-coded inside an automated verify block | `TS` is bounded to its retired-framing senses (class fix D, shared with 06-01 and 06-03), pre-authorised here rather than improvised by the executor. Verified by measurement, per file rather than in aggregate: `corpora/manifest.json` → **0**; total → **170**, two MORE than the old set, because the bounded form catches camelCase `tsBinary` the bare word-bounded form missed. The full per-file table is in the `<action>` block and is required in `06-CENSUS.md`. A **completeness control** was added on top: the BARE `\bTS\b` is still run over the same surface (`BARE_TS_RESIDUAL_LINES >= 1`) and every residual line must be quoted and adjudicated in `06-CENSUS.md` as either already-covered or the KEEP sense — so an under-covering enumeration is visible to the executor instead of shipping a false zero. |
| **Cycle-2's aggregate coverage claim, refuted** — cycle 2 recorded "168 hits today, every one inside a declared sweep target". Re-measured per file, that was wrong for exactly one file | Every coverage claim in this plan is now stated per file. The table in the `<action>` block names each file, its hit count and the single plan that owns it. |
| **Trap closed in 06-01 while sweeping for class D** — `tools/bench` is inside this census's surface, and the obvious literal for publishcheck's wrong-subject fixture is `"ts"`, which this set counts as retired subject framing. That would have made this gate unreachable through no fault of this plan's executor | `06-01`'s `<behavior>` block now pins that fixture value to `"other"` and states why. Nothing to do here; recorded so the dependency is visible. |

## Cycle 4

| Cycle-4 finding | Fix |
|---|---|
| **LOW 1** — this plan's `>= 1` residual rationale contradicted its own document gate. Task 4's `BARE_TS_RESIDUAL_LINES >= 1` criterion justifies the floor on the ground that a rewritten `docs/BENCHMARKS.md` "may legitimately name `TS/JS` as an indexed-language pair", while Task 2's `BENCHMARKS_RETIRED_TERMS_TOTAL=0` gate ran a **bare** `-e '\bTS\b'` over that same file and forbade exactly that. It was the last bare `\bTS\b` to survive class fix D anywhere in the phase | The reviewer's **preferred option (a)** is applied: Task 2's alternation now carries the identical class-fix-D bounded senses used by 06-01, 06-03 and this plan's own phase census, and Task 4's line stands as written. Measured, not reasoned — the document does not exist yet, so the set was measured against the material Task 2 carries forward: **7** hits on **6** lines over `:7-48` and `:213-300`, all retired framing the `<action>` now names by line and directs to be re-authored or dropped; **52** over the whole current file against the bare set's **48** (a net strengthening, the same shape as the census's 168 → 170); `TS/JS` probe — bare **2**, bounded **0**; retired-sense probe — bounded **6/6**. Zero is reachable by the correct path, and reachable without forbidding the KEEP sense. |
| **Class-shape sweep, planner-originated (cycle-4 final pass)** — a collision the LOW's measurement exposed: Task 2's `<action>` told the executor to carry §2.x forward "near-verbatim" and asserted it "already reads on its own terms", but measured, three of its lines (`:215`, `:237`, `:240`) are cross-references to the head-to-head table being deleted, and the gate forbids them. A literal reading of "near-verbatim" produced a red gate on correct-looking work — the same defect class cycles 2 and 3 closed four times | The `<action>` now states the measurement, names all six flagged lines, and says plainly what "near-verbatim" does and does not preserve. No criterion was weakened to accommodate it. |
</review_incorporation>

<tasks>

<task type="auto">
  <name>Task 1: Produce and commit the measurement of record</name>
  <precondition>The de-coupled measurement path exists on this branch — `go run ./tools/bench/runner -mode publish -h 2>&1 | rg -q 'publish'` succeeds (06-01 landed) and `rg -q '^  publish:' .github/workflows/bench.yml` succeeds (06-02 landed). Additionally, `git` and network access to github.com are required: `git ls-remote --exit-code --heads https://github.com/cockroachdb/pebble` exits 0. Halt and surface a checkpoint if unmet; never publish a figure that was not measured.</precondition>
  <files>.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json, .planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-PREPLAN-SHA.txt</files>
  <read_first>
    - `.github/workflows/bench.yml` as left by 06-02 — the `publish` job id, its dispatch input value and the artifact name it uploads
    - `tools/bench/publishcheck/main.go` as left by 06-01 — the flags this task's verification uses, and the `SHA256=` value it prints (the byte-identity instrument; there is no portable checksum CLI shared by macOS and Linux, which is why the hash is computed in Go)
    - `tools/bench/runner/main.go` as left by 06-01 — the exact flags the run needs (`-mode`, `-go-binary`, `-runner`)
    - `internal/bench/metrics.go` — the json tags that carry each record's provenance (`goos`, `goarch`, `runner`, `scratch_fs`, `median_of_trials`)
    - `tools/bench/realcorpus/manifest.go` as left by 06-01 — the two corpus names and their pinned `CommitSHA` values, which the provenance record must cite
  </read_first>
  <action>
**Record the pre-plan SHA first**: write `git rev-parse HEAD` as the single line of
`.planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-PREPLAN-SHA.txt`, before any other
step. Every tamper guard in this plan compares against it rather than against HEAD (review class
fix A).

Then obtain absolute numbers from the de-coupled path and commit them raw, so the document that
publishes them cites a measurement rather than a recollection.

**Read this before choosing a path.** The two requirements this task feeds are NOT interchangeable
(review HIGH 5). BENCH-01 needs *numbers*, and either path below supplies them. BENCH-03 needs *a
`bench.yml` run*, and only the CI path supplies that. Whichever path is taken, Task 3's blocking
gate records BENCH-03's resulting status explicitly; a local run never closes it by implication.

**Preferred path — the pinned CI runner.** If the current branch is pushed and `gh` is
authenticated, dispatch the job that 06-02 authored:
`gh workflow run bench.yml --ref "$(git branch --show-current)" -f job=publish`, then wait for the
run (`gh run list --workflow=bench.yml --branch "$(git branch --show-current)" --limit 1` to find
its id, `gh run watch <id>`), then download its raw-results artifact and save it verbatim as
`.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json`. Record the run URL.
This path is preferred because `CODEGRAPH_BENCH_RUNNER` is set on that job, so every record carries
its runner identity — the property `internal/bench/regression.go`'s cross-runner refusal exists to
protect.

**Fallback — a local run, for BENCH-01 only.** If the branch is not pushed, `gh` is unavailable, or
the dispatch cannot complete, run locally instead, teeing stdout so byte identity is established by
comparison rather than asserted (review finding 06-04:142):
`go build -o ./codegraph-bench ./cmd/codegraph && go run ./tools/bench/runner -mode publish
-go-binary "$(pwd)/codegraph-bench" | tee "${TMPDIR:-/tmp}/06-publish-stdout.json" > .planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json`.
Leave `-runner` unset so the emitted `runner` field is empty and the record does not claim a runner
identity it does not have.

Record in the SUMMARY, explicitly, WHY the CI path was not used — an unrecorded fallback is
indistinguishable from a skipped step. **A local run does not verify BENCH-03**: it produces
numbers, not evidence that `bench.yml` ran. Task 3 resolves BENCH-03's status explicitly.

**Reproducibility constraints for a local measurement, recorded rather than waved through.** The
fallback imposes no stable runner conditions, so its figures are machine-specific in a way the
pinned CI profile's are not. This does not contradict D-02 — D-02 requires a fresh absolute-only
run, not a particular host — but the weaker reproducibility must be legible to a reader. Before
measuring locally: close other heavy processes, run on AC power rather than battery, and do not
run the measurement concurrently with a build or an index. Record for the provenance block: the
host OS and version, the CPU model, the core count, the physical memory, and whether the machine
was on AC power. Task 2 publishes those alongside the figures and labels the table as locally
measured on an unpinned runner.

Either way: save the runner's stdout byte-for-byte. Do not hand-edit, reformat, round, reorder or
prune the JSON. Do not merge in a figure from any earlier run. For the CI path, the downloaded
artifact IS the stdout capture — copy it verbatim and keep the download at
`"${TMPDIR:-/tmp}/06-publish-stdout.json"` so the same byte-identity comparison applies to both
paths.

Record in the SUMMARY, for the provenance block Task 2 will publish: the measuring commit SHA
(`git rev-parse HEAD`), the date, the path used (CI run URL or the local command), each record's
`goos` / `goarch` / `runner` / `scratch_fs` / `median_of_trials`, and both corpus `CommitSHA` pins
copied from the manifest.
  </action>
  <verify>
    <automated>A=.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json; B="${TMPDIR:-/tmp}/06-publish-stdout.json"; test -f "$A" && test -f "$B" && cmp -s "$A" "$B" && echo "PUBLISH_BYTE_IDENTICAL=true" && go run ./tools/bench/publishcheck -file "$A" -want-records 2 -want-repos weft-go,cockroachdb-pebble -want-subject go | tee "${TMPDIR:-/tmp}/06-publishcheck-artifact.txt" && go run ./tools/bench/publishcheck -file "$B" -want-records 2 -want-repos weft-go,cockroachdb-pebble -want-subject go | tee "${TMPDIR:-/tmp}/06-publishcheck-stdout.txt" && HA=$(rg -o 'SHA256=[0-9a-f]{64}' "${TMPDIR:-/tmp}/06-publishcheck-artifact.txt") && HB=$(rg -o 'SHA256=[0-9a-f]{64}' "${TMPDIR:-/tmp}/06-publishcheck-stdout.txt") && echo "ARTIFACT_$HA STDOUT_$HB" && test "$HA" = "$HB" && test -n "$HA" && BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-PREPLAN-SHA.txt) && git diff --quiet "$BASE" -- tools/bench/baseline.json internal/bench/ && test -f tools/bench/baseline.json && echo "GATE_INPUTS_UNCHANGED_SINCE=$BASE"</automated>
  </verify>
  <acceptance_criteria>
    - `06-PUBLISH-RESULTS.json` exists, parses, and is an array of exactly 2 records whose `repo` values are exactly `weft-go` and `cockroachdb-pebble`. `publishcheck` prints `PUBLISH_RECORDS=2 REPOS=cockroachdb-pebble,weft-go ALL_SUBJECT_GO=true ALL_METRICS_POSITIVE=true SHA256=<hex>` — a positive assertion; an empty or partial array FAILS rather than passing vacuously (rule `84d1gfpywd`). The verifier is the pure-Go `tools/bench/publishcheck` 06-01 authored; no verification step in this phase invokes Node (review finding 06-04:135).
    - Every record has `subject` equal to `go`; no record carries any other subject value.
    - Every one of `files_per_sec`, `bytes_per_sec`, `query_latency_median_ms`, `peak_rss_bytes`, `cold_start_ms` is strictly positive in both records.
    - **Byte identity is COMPARED, not asserted** (review finding 06-04:142 — the previous form checked only that the first line began with `[`, and depended on the file having been staged). The runner's stdout was teed to `${TMPDIR:-/tmp}/06-publish-stdout.json` in the same pipeline that wrote the committed artifact (or, on the CI path, is the downloaded artifact kept at that path), and the two files satisfy `cmp -s` (`PUBLISH_BYTE_IDENTICAL=true`) **and** yield the same non-empty `SHA256=` from two independent `publishcheck` invocations. Both hashes are echoed and recorded in the SUMMARY.
    - The SUMMARY records the measuring commit SHA, the date, and either the CI run URL or the exact local command plus the explicit reason the CI path was not used.
    - If the local fallback was taken, the SUMMARY also records the host conditions the provenance block will publish: OS and version, CPU model, core count, physical memory, and AC-power state. A locally measured figure published without them is not reproducible by a reader, which is the whole point of publishing it.
    - Gate inputs unchanged, pre-plan-SHA relative per class fix A, with a positive companion so it cannot pass against a deleted file: `git diff --quiet "$BASE" -- tools/bench/baseline.json internal/bench/` exits 0 and `test -f tools/bench/baseline.json` succeeds (`GATE_INPUTS_UNCHANGED_SINCE=$BASE`) — measuring changed no gate input.
  </acceptance_criteria>
  <done>A raw, unedited measurement from the de-coupled path is committed under the phase directory, proven byte-identical to the runner's stdout by an actual comparison, its two records carry positive absolute metrics for both surviving corpora, and its provenance — including host conditions if measured locally — is recorded well enough for the published document to cite it.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/BENCHMARKS.md from scratch on absolute-measurement terms</name>
  <precondition>`.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json` exists and holds two positive-metric records (Task 1 complete). Without it there are no measured numbers to publish and the rewrite would have to invent them.</precondition>
  <reversibility rating="costly">The surviving methodology rationale (median-of-5, OS-level peak RSS, pinned-SHA corpora, the regeneration procedure) must be re-authored rather than copied; losing it leaves the published numbers unreproducible (D-02).</reversibility>
  <files>docs/BENCHMARKS.md</files>
  <read_first>
    - `docs/BENCHMARKS.md` in full BEFORE replacing it — specifically §1 Methodology (7-48), `### Comparison target` (50-55), `### Pinned real repos` (57-73), `### Regenerating the numbers` (75-87), `### The one real, committed number: the synthetic regression baseline` (213-240) and `## 3. Regression gate (PERF-02, INDX-06)` (242-300). The last two survive near-verbatim and must be carried forward, not re-derived.
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md` §Pattern 1 — the section-by-section fate map; reuse it rather than re-deriving it
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json` — the numbers to publish
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-SUMMARY.md` notes from Task 1 (or the running SUMMARY) — the provenance to publish alongside them
    - `tools/bench/realcorpus/manifest.go` as left by 06-01 — corpus names, `SourceURL`, `CommitSHA`, `License`, `SelectionRule` and `QueryTerms` for the pinned-repos table
    - `.github/workflows/bench.yml` as left by 06-02 — the job id and dispatch value the regeneration section must cite
  </read_first>
  <action>
<!-- planner-discipline-allow: head-to-head -->
Replace `docs/BENCHMARKS.md` with a document written on this project's own terms, per D-02. Sections
to produce, in order.

**Use these three `##` heading lines verbatim** — the acceptance criterion asserts the exact texts,
because a heading COUNT is consistent with any three headings and proves nothing about which
sections exist (cycle-2 LOW 4):

```
## 1. Methodology
## 2. Absolute numbers
## 3. Regression gate (PERF-02, INDX-06)
```

The first is the current file's heading unchanged; the third is carried forward verbatim from the
current file (`docs/BENCHMARKS.md:242`), requirement IDs intact; only the second is new, replacing
`## 2. Raw numbers`. Sub-headings are free-form.

**Title and intro.** Name the document for what it is — this project's published performance
figures — with no second implementation in the title or the opening sentence.

**§1 Methodology.**
- *What is measured*: indexing throughput (files/s and bytes/s from a from-scratch
  `index --force`), query latency (median wall-clock of a single `query <term>` invocation), peak
  RSS, and cold start (`--version`). Keep the existing definitions.
- *median-of-5*: carry the existing discipline forward as this project's own choice, not a
  concession to a comparison.
- *Peak RSS is measured at the OS level*: re-author the justification on its own terms — an
  externally observed child-process `rusage` figure is the only peak-RSS number comparable across
  runs and machines, because an in-process Go runtime statistic measures the harness rather than
  the subject. Do not justify it by contrast with another runtime.
- *Pinned real repos*: a two-row table (`weft-go`, `cockroachdb-pebble`) carrying source URL,
  pinned `CommitSHA`, licence and selection rule copied from the manifest. Re-author each "why this
  repo" cell on absolute terms — compact mostly-Go tree versus a substantially larger real Go
  codebase. Keep the pinned-SHA-never-branch reproducibility rule.
- *Regenerating the numbers*: cite the current runner mode and the current `bench.yml` job id and
  dispatch value that 06-01 and 06-02 created. Verify both strings against those files rather than
  writing them from memory.

**§2 Absolute numbers.** One row per corpus entry, columns: files/s, bytes/s, query latency
(median, ms), peak RSS, cold start (ms). No ratio column, no multiplier, no second subject.

**Do not hand-type a single data cell.** Generate the table's data rows mechanically:
`go run ./tools/bench/publishcheck -file .planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json -emit-rows`
and paste its output verbatim as the table body. Ten cells across two rows were previously
spot-checked on `files_per_sec` alone, which left query latency, peak RSS, bytes/s and cold start
free to be mis-transcribed without failing anything (review finding on numeric traceability).
Generating the rows removes transcription from the process entirely, and the acceptance criterion
re-runs the emitter and requires every emitted row to appear verbatim in the document. Author the
header row and the column units by hand; the numbers come from the emitter.

Immediately above or below the table, a provenance block stating: measuring commit SHA, date,
`goos`, `goarch`, `runner` (state "unset — local measurement" if the field is empty rather than
omitting it), `scratch_fs`, `median_of_trials`, and the corpus `CommitSHA` pins measured. A figure
without its provenance is not publishable here.

If Task 1 took the local fallback, the provenance block ALSO states the host conditions Task 1
recorded (OS and version, CPU model, core count, memory, AC-power state) and labels the table
explicitly as measured on an unpinned local machine rather than the pinned CI profile. These
figures are reproducible only against a comparable host, and a reader is entitled to know that
from the document rather than from a plan.

**§2.x The one real, committed number: the synthetic regression baseline.** Carry the existing
section forward near-verbatim; it remains the one committed, reproducible number.

**But "near-verbatim" is not "unchanged" — six lines of the carried-forward material are flagged by
this task's own gate today, measured rather than assumed (cycle-4 LOW 1 sweep).** The gate below is a
zero over the rewritten document, and the ranges this action carries forward are not already at zero.
Measured during this replan over the two carry-forward ranges (`:7-48` and `:213-300` of the current
file), the gate's pattern set returns **7** hits on **6** lines: `:39`, `:42` and `:46`, all inside
the peak-RSS justification §1 above already directs you to re-author on this project's own terms; and
`:215`, `:237` and `:240` inside this very section, with `:237` carrying two. Those last three are
cross-references to the retired table that used to sit above this section and does not exist in the
new document. **Drop those clauses as you carry the section forward.** "Near-verbatim" means the
number itself, its rationale and its re-blessing procedure survive — not its pointers to a table that
is being deleted. Nothing here asks you to invent a justification: the section's own claim (a
committed, reproducible, offline-synthetic number) stands on its own once the pointers are gone.

**§3 Regression gate.** Carry the existing section forward near-verbatim, including the tolerance
bands, the offline synthetic corpus description and the re-blessing procedure. BENCH-01 explicitly
requires the rewritten document to carry the regression-gate description, so this section is not
optional. Keep its heading's existing requirement IDs — they name the requirements the gate
implements and are still accurate.

**Deleted outright, not reworded:** the comparison-target section, the head-to-head raw-numbers
tables, the ratio/multiplier summary, the head-to-head run-to-run repeatability section, and the
superseded provisional table. Their content is recoverable from git history.

**Requirement-ID labelling:** cite BENCH-01 in §1/§2 as the requirement this document now satisfies,
and leave the gate section's existing PERF-02/INDX-06 IDs alone. Do not retro-label the surviving
gate prose.
  </action>
  <verify>
    <automated>test -f docs/BENCHMARKS.md && W='[[:space:]/*#`>@-]*' && N=$(rg -U -o -i -e "\bTS${W}codegraph" -e "\bTS${W}Node\b" -e "\bTS[[:space:]-]*(binary|binaries|CLI|project|subject|runner|harness)\b" -e '\bts-binary\b' -e '\btsBinary\b' -e "\bGo[[:space:]/-]*vs\.?[[:space:]/-]*TS\b" -e '\bGo/TS\b' -e "\bTS[[:space:]]*1\.3\.1\b" -e '"ts"' -e '\|[[:space:]]*ts[[:space:]]*\|' -e '\bhead-to-head\b' -e '\bheadtohead\b' -e '\bcolbymchenry\b' -e '\bcodegraph-ts\b' -e '\bcomparator\b' -e '\b1\.3\.1\b' -e '[0-9]+(\.[0-9]+)?[x×] (faster|slower|more|less)' docs/BENCHMARKS.md | wc -l | tr -d ' '); echo "BENCHMARKS_RETIRED_TERMS_TOTAL=$N"; test "$N" = "0" && for tok in "files/s" "peak RSS" "query" "median-of-5" "CommitSHA" "weft-go" "cockroachdb-pebble" "publish" "CheckRegression" "baseline.json"; do C=$(rg -o -F "$tok" docs/BENCHMARKS.md | wc -l | tr -d ' '); echo "PRESENT[$tok]=$C"; test "$C" -ge 1 || exit 1; done && echo "BENCHMARKS_REQUIRED_CONTENT=ok"</automated>
  </verify>
  <acceptance_criteria>
    - Retired-framing census over `docs/BENCHMARKS.md` prints `BENCHMARKS_RETIRED_TERMS_TOTAL=0`, counted with `rg -o | wc -l`. The pattern set includes a multiplier shape (`<n>x faster/slower`) so a surviving ratio is caught even if it names no second implementation.
      **The `TS` alternation is BOUNDED here too — this was the phase's last bare `\bTS\b`, and it contradicted this plan's own residual-floor rationale (cycle-4 LOW 1).** The bare form forbade `TS/JS` inside `docs/BENCHMARKS.md` while the `BARE_TS_RESIDUAL_LINES >= 1` criterion below rests its `>= 1` (rather than `= 1`) floor on the ground that a rewritten `docs/BENCHMARKS.md` may legitimately name `TS/JS` as an indexed-language pair. One instrument forbade what the other called legitimate. The gate now runs the identical class-fix-D senses the phase census uses, so the two criteria agree, the line-below rationale stands as written, and no bare `\bTS\b` survives anywhere in the phase except the deliberate completeness control.
      **Measured basis, stated because the document does not exist yet.** The rewrite's inputs are this file's carried-forward sections, so the new set was measured against those rather than derived by reasoning. Over `docs/BENCHMARKS.md:7-48` (§1 Methodology) and `:213-300` (the committed-baseline section and §3 Regression gate) it returns **7** hits on **6** lines — `:39`, `:42`, `:46`, `:215`, `:237` (two) and `:240` — every one of them retired framing the `<action>` now explicitly directs to be re-authored or dropped, so zero is reachable by the correct path. Over the whole current file it returns **52** where the bare set returned **48**: the bounding is a net strengthening, the same shape class fix D had on the phase census (168 → 170), not a relaxation. Two probes fix the boundary in both directions: on a planted two-line fixture naming `TS/JS` as an indexed-language pair the bare form returns **2** and the bounded form returns **0**; on a planted five-line fixture carrying the retired senses (a `TS` binary reference, a `Go vs TS 1.3.1` summary line, a `TS/Node` subject, a bare `| ts |` table cell and a `TS CodeGraph` harness reference) the bounded form returns **6**. Zero is reachable, and it is reachable without forbidding the KEEP sense.
    - Required-content assertions all print a count >= 1 (`BENCHMARKS_REQUIRED_CONTENT=ok`): the throughput unit, peak RSS, query latency, the median-of-5 discipline, the pinned-SHA rule, both corpus names, the current runner mode / job name, the gate function and the committed baseline file. This is the positive half — a document that merely lacks the retired terms would otherwise pass vacuously (rule `84d1gfpywd`).
    - **Every data cell is traceable, not just `files_per_sec`** (review finding on numeric traceability). Re-run the emitter and require each emitted row verbatim in the document: `go run ./tools/bench/publishcheck -file .planning/phases/06-benchmark-de-coupling-memory-sweep/06-PUBLISH-RESULTS.json -emit-rows > "${TMPDIR:-/tmp}/06-rows.txt"; MATCHED=0; while IFS= read -r row; do rg -q -F "$row" docs/BENCHMARKS.md && MATCHED=$((MATCHED+1)); done < "${TMPDIR:-/tmp}/06-rows.txt"; EMITTED=$(wc -l < "${TMPDIR:-/tmp}/06-rows.txt" | tr -d ' '); echo "ROWS_EMITTED=$EMITTED ROWS_MATCHED=$MATCHED"; test "$EMITTED" = "2" && test "$MATCHED" = "$EMITTED"` — a mis-transcribed query latency, peak RSS, bytes/s or cold-start cell now fails, where previously only throughput was checked.
    - The provenance block names `goos`, `goarch`, `runner`, `scratch_fs`, `median_of_trials`, the measuring commit SHA and the date. If `runner` is empty in the JSON, the document says so explicitly rather than omitting the row. If Task 1 measured locally, the block additionally names the host OS, CPU model, core count, memory and AC-power state, and the section labels the figures as measured on an unpinned local machine.
    - **The three sections are asserted by heading TEXT, not by a heading count** (cycle-2 LOW 4 — `rg -c '^## '` returns `3` for any three headings and cannot show which ones exist): `H=$(rg -o -F -e '## 1. Methodology' -e '## 2. Absolute numbers' -e '## 3. Regression gate (PERF-02, INDX-06)' docs/BENCHMARKS.md | wc -l | tr -d ' '); TOP=$(rg '^## ' docs/BENCHMARKS.md | wc -l | tr -d ' '); echo "BENCHMARKS_REQUIRED_HEADINGS=$H BENCHMARKS_TOP_SECTIONS=$TOP"; test "$H" = "3" && test "$TOP" -ge 3` — each named heading present exactly once. `BENCHMARKS_TOP_SECTIONS` is the printed companion proving the extractor sees the file's sections at all; it is not itself a ceiling, since the document may legitimately grow a section the plan did not name.
    - `rg -o -F 'PERF-02' docs/BENCHMARKS.md | wc -l` is >= 1 — the surviving gate section kept its requirement IDs rather than being retro-labelled.
    - Protected files intact, pre-plan-SHA relative per class fix A and two-sided per rule `84d1gfpywd`: `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-PREPLAN-SHA.txt); test -f NOTICE && test -f CHANGELOG.md && test "$(rg -c -F 'Copyright (c) 2026 Colby Mchenry' NOTICE || true)" = "1" && git diff --quiet "$BASE" -- NOTICE CHANGELOG.md && echo "PROTECTED_FILES_PRESENT=true PROTECTED_FILES_UNCHANGED_SINCE=$BASE"`
  </acceptance_criteria>
  <done>`docs/BENCHMARKS.md` publishes measured absolute figures for both corpora with full provenance, carries the methodology and the regression-gate description, and contains no comparison table, no multiplier and no second implementation.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Resolve BENCH-03's status — closed by a real `bench.yml` run, or explicitly pending</name>
  <decision>Record BENCH-03's verification status in `06-LIVE-VERIFICATIONS.md` as exactly one of two verbatim tokens, before this plan can complete.</decision>
  <context>
BENCH-03 is "`bench.yml` runs and publishes the absolute numbers without invoking another
implementation". 06-02 proved the *shape* of the job structurally — exact runner pinning, exact
env contract, one inline publish-mode invocation, no gate call in any run body, a step-summary
block, and an upload guarded by `if-no-files-found: error`, all asserted against the parsed
`jobs.publish` subtree. What no static check can establish is that the job **ran**.

The cross-AI review's HIGH 5 found that this plan previously allowed BENCH-03 to close with zero
verification: the local fallback was sanctioned, and the only live check was scoped "If the
preferred CI path was used". Take the fallback and the plan completes with the requirement
unverified and unmarked.

This gate exists so that cannot happen. It does **not** reopen D-02 — D-02 requires a fresh
absolute-only measurement run and does not mandate where it happens, so the local fallback stands
as BENCH-01's supply of numbers. What is being decided is only how BENCH-03 is recorded.

Before choosing, if a run exists: open the recorded `bench.yml` run and confirm (a) the job summary
shows an absolute per-corpus table, (b) the raw-results artifact uploaded without tripping
`if-no-files-found: error`, and (c) no step in the job installed or invoked any implementation
other than the freshly-built Go binary.

Cost of getting this wrong: recording `closed-by-ci-run` without a real run puts a false
verification into the phase record, and the requirement would be marked satisfied on evidence that
does not exist. Recording `pending-no-ci-run` when a run did succeed costs only a later
re-verification.
  </context>
  <options>
    <option id="closed-by-ci-run">
      <name>A `bench.yml` publish run succeeded — close BENCH-03</name>
      <pros>BENCH-03 is verified by the mechanism the requirement names. The run URL, the summary observation and the artifact observation all go on the record.</pros>
      <cons>Requires a pushed branch, an authenticated `gh`, and a completed run. Select this only if the run genuinely completed and was inspected.</cons>
      <evidence-required>Selecting this option obliges you to supply a real run id. Task 4 will not accept the token without (a) a `https://github.com/&lt;owner&gt;/&lt;repo&gt;/actions/runs/&lt;id&gt;` URL on the token line itself and (b) the verbatim output of `gh run view &lt;id&gt; --json conclusion,event,headBranch,url` recorded beneath it, showing `conclusion` = `success` (cycle-2 MEDIUM 1 — the token was previously counted but its URL never validated, so a mis-selected close passed the machine check).</evidence-required>
    </option>
    <option id="pending-no-ci-run">
      <name>No successful run — record BENCH-03 as explicitly pending</name>
      <pros>The plan completes on its BENCH-01 work without laundering a local measurement into evidence for BENCH-03; the gap is visible to phase verification and to the next review cycle.</pros>
      <cons>BENCH-03 stays open and the phase cannot claim it. That is the correct outcome when the workflow has not run.</cons>
    </option>
    <option id="dispatch-now">
      <name>Pause and dispatch the run before deciding</name>
      <pros>Turns a pending status into a closed one; the right choice when the only obstacle is that nobody has pushed the branch yet.</pros>
      <cons>Blocks until the run completes.</cons>
    </option>
  </options>
  <resume-signal>Select: closed-by-ci-run (supplying the run URL), pending-no-ci-run (supplying the reason), or dispatch-now.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Phase acceptance census with a proven instrument, the BENCH-03 status record, and the coverage declaration</name>
  <precondition>The checkpoint above resolved with an explicit selection. The status token written below is the one that selection named — it is transcribed, never chosen here.</precondition>
  <files>.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CENSUS.md, .planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md, .planning/phases/06-benchmark-de-coupling-memory-sweep/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/05-process-ci-in-tree-sweep/05-08-PLAN.md` — the proven positive-controlled multiline census shape, including the scanned-file-count floor that guards against a wrong-cwd vacuous zero
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md` §D-04 — the instrument's rules
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-SUMMARY.md`, `06-02-SUMMARY.md`, `06-03-SUMMARY.md` — every pattern each plan narrowed and why; the phase census must not silently re-widen or re-narrow them
    - `.planning/STATE.md` §Accumulated Context → Decisions — the recorded rulings that `.planning/` archives and `CHANGELOG.md` are out of scope, and that a term blocklist was deliberately declined (VOCAB-01)
  </read_first>
  <action>
<!-- planner-discipline-allow: head-to-head -->
<!-- planner-discipline-allow: headtohead -->
<!-- planner-discipline-allow: colbymchenry -->
<!-- planner-discipline-allow: codegraph-ts -->
Run the phase acceptance census and record it in a new `06-CENSUS.md`. Prove the instrument before
trusting its zero — this repo produced three instances of the same meta-bug in Phase 5, where each
verification layer trusted the layer below's enumeration.

**Step 1 — plant the positive control.** Write a temp fixture containing the target phrases
deliberately wrapped across comment line breaks: a `TS` occurrence split from its sentence, a
`head-to-` / `head` split, and a `colbymchenry` / `codegraph-ts` occurrence inside a wrapped block
comment.

**Step 2 — prove the instrument.** Run the bounded pattern set over the fixture twice: once with
`rg -U -o` and once with plain `rg -o`. Assert the multiline count is strictly greater than the
line-based count AND strictly greater than zero. Echo both numbers. A pattern set that cannot beat
the line-based instrument on a planted wrapped phrase is not trusted for this file surface, and no
zero it later produces is believed.

**Step 3 — run the real census** over the declared Phase 6 file surface:
`docs/BENCHMARKS.md`, `tools/bench/`, `internal/bench/`, `internal/corpora/manifest.go`,
`corpora/manifest.json`, `.github/workflows/bench.yml`, `internal/upgrade/taskfile_shape_test.go`.
Count with `rg -o | wc -l` and echo the total. Never `rg -c` — it counts matching lines, not
matches, which is exactly how Phase 5's censuses under-reported.

**Step 4 — assert the scanned file SET, not only a floor.** Echo the number of files the census
walked and assert it exceeds a stated floor, AND assert that every one of the thirteen critical
files below appears in the walked list by name. The floor alone admits a shifted surface: a deleted
critical file plus unrelated added files preserves the count while the thing you meant to scan is
gone (review finding 06-04:143). The critical set is `docs/BENCHMARKS.md`,
`tools/bench/BASELINE.md`, `tools/bench/baseline.json`, `tools/bench/realcorpus/manifest.go`,
`tools/bench/realcorpus/manifest_test.go`, `tools/bench/runner/main.go`,
`tools/bench/runner/main_test.go`, `internal/bench/regression.go`, `internal/bench/metrics.go`,
`internal/bench/rss.go`, `internal/corpora/manifest.go`, `.github/workflows/bench.yml`,
`internal/upgrade/taskfile_shape_test.go`. Print the result as a fraction so a partial match is
visible rather than absorbed.

**Bounded pattern set.** Run with `rg -U -o -i`, `W='[[:space:]/*#`>@-]*'` and `S='[[:space:]/*#]+'`:

```
-e "\bTS${W}codegraph"                 -e "\bTS${W}Node\b"
-e "\bTS[[:space:]-]*(binary|binaries|CLI|project|subject|runner|harness)\b"
-e '\bts-binary\b'                     -e '\btsBinary\b'
-e "\bGo[[:space:]/-]*vs\.?[[:space:]/-]*TS\b"   -e '\bGo/TS\b'
-e "\bTS[[:space:]]*1\.3\.1\b"         -e '"ts"'
-e '\|[[:space:]]*ts[[:space:]]*\|'
-e '\bhead-to-head\b'                  -e '\bheadtohead\b'
-e '\bcolbymchenry\b'                  -e '\bcodegraph-ts\b'
-e '\bcomparator\b'                    -e '\b1\.3\.1\b'
-e "\bcomparison${S}(binary|harness|runner|target|subject|mode)\b"
```

Bare `comparison` is deliberately EXCLUDED: `internal/bench` and `tools/bench/BASELINE.md` use it
correctly for the gate comparing a baseline record against a current record, and Phase 5 already
recorded an incident where an unbounded pattern matched innocent prose and an executor reworded the
prose instead of bounding the pattern.

**Bare `\bTS\b` is EXCLUDED for exactly the same reason — class fix D, new in cycle 3.** `TS`
meaning TypeScript-the-indexed-language is this repository's recorded KEEP class (the plan already
excludes `internal/indexer/**` and `testdata/golden/**` on that ground), and leaving the alternation
bare made `PHASE6_CENSUS_TOTAL = 0` unreachable by any correct execution: measured at `fd02272`, the
old set returned exactly **1** hit in `corpora/manifest.json` — the substring `TS` in `TS/JS` inside
the `LOCKED (01-06)` corpus-language-coverage note at line 10 — in a file no plan modifies, whose
text every executor escape is prohibited from touching. The `-i` flag made it worse still, matching
any lowercase `ts` token anywhere on the surface.

Re-measured verbatim during this replan, per file rather than in aggregate (cycle 3 refuted cycle
2's aggregate claim by exactly this method):

| File | bounded-set hits today | owning plan (`files_modified`) |
|---|---|---|
| `docs/BENCHMARKS.md` | 47 | 06-04 (rewritten) |
| `tools/bench/runner/main.go` | 35 | 06-01 |
| `.github/workflows/bench.yml` | 26 | 06-02 |
| `tools/bench/realcorpus/manifest.go` | 12 | 06-01 |
| `tools/bench/runner/main_test.go` | 5 | 06-01 |
| `tools/bench/realcorpus/manifest_test.go` | 5 | 06-01 |
| `tools/bench/headtohead-*.json` (6 files) | 5 each | 06-01 (deleted) |
| `internal/bench/metrics.go` | 3 | 06-03 |
| `tools/bench/BASELINE.md` | 2 | 06-02 |
| `internal/bench/regression.go` | 2 | 06-03 |
| `internal/upgrade/taskfile_shape_test.go` | 1 | 06-02 |
| `internal/bench/rss.go` | 1 | 06-03 |
| `internal/bench/regression_test.go` | 1 | 06-03 |
| **`corpora/manifest.json`** | **0** | none — and none is now needed |

Total today: **170**, two MORE than the old set's 168, because the bounded form additionally catches
`tsBinary` in camelCase, which the bare word-bounded `\bTS\b` missed entirely. The bounding is
therefore a net strengthening, not a relaxation. Every file with a non-zero count is a declared
sweep target of exactly one plan — asserted per file, not as an aggregate.

**Record the bounding in `06-CENSUS.md` with the flagged text quoted**, exactly as the rule two
paragraphs below requires. Quote `corpora/manifest.json:10` verbatim, state that it is
TypeScript-the-indexed-language and not retired comparison framing, and cite the same Phase-5 KEEP-class
adjudication the `internal/indexer/**` exclusion cites. This is a pattern narrowing, pre-authorised
here because the pattern lives inside an automated verify block the executor cannot legitimately edit.

**Exclusions — every one traces to a recorded prior adjudication.** Record each in `06-CENSUS.md`
with its source: `NOTICE` (upstream MIT licence attribution — a legal obligation, adjudicated in
Phase 4); `CHANGELOG.md` (release-please-owned, STATE.md decision); `.planning/**` (append-only
record parsed by scope-sensitive tooling, STATE.md decision); `internal/indexer/**` and
`testdata/golden/**` (TypeScript-the-indexed-language product surface and its captured fixtures —
Phase 5's recorded KEEP class); `Taskfile.yml` and the wireoracle / query / TUI / agents / MCP
packages (outside the Phase 6 surface; their `comparison` uses are the ordinary sense). Adding an
exclusion that is not on this list, or is not backed by a citation, is forbidden — that is the
Repudiation threat this record exists to close.

**If a pattern flags text that is genuinely innocent, the finding is about the pattern.** Bound it
tighter and record the change with the flagged text quoted. Never reword the innocent text.

**Then write the live-verification ledger.** Create
`.planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md` with a
`## BENCH-03` section carrying exactly one status line, transcribed from the checkpoint's
selection — never chosen here:
`BENCH03_STATUS=closed-by-ci-run <run-url>` if a `bench.yml` publish run succeeded and was
inspected, or `BENCH03_STATUS=pending-no-ci-run <reason>` if it did not. Beneath the token, record
what was observed (job summary, artifact upload, absence of any second implementation in the job)
or, for the pending case, exactly what remains to be done and why it could not be done now.

**On the closed branch, the run must be identified and its outcome recorded — the token alone is
not evidence** (cycle-2 MEDIUM 1: the previous form counted the token and required exactly one, but
nothing asserted a non-empty URL, a matching workflow, a matching ref or a successful conclusion, so
a mis-selected close passed the machine check). Two additions, both mechanical:

1. `<run-url>` on the token line must be a real
   `https://github.com/<owner>/<repo>/actions/runs/<id>` URL, not a placeholder or a bare id.
2. Immediately beneath the token, paste the verbatim output of
   `gh run view <id> --json conclusion,event,headBranch,url` in a fenced block. It must show
   `conclusion` = `success`, and the SUMMARY must state that `headBranch` matches the branch the
   measurement was taken on and that `url` matches the token's URL. A run that concluded
   `failure`, `cancelled` or is still in progress does not close BENCH-03 — select
   `pending-no-ci-run` instead.

On the pending branch neither addition applies; the reason string is the record.

This ledger exists because BENCH-03 was previously closable by silence (review HIGH 5). 06-05 and
06-06 append their own MEM-02 rows to the same file, so a reader sees every live verification this
phase depends on in one place with a machine-checkable token each.

Finally, write `COVERAGE.md` in the phase directory containing the single reasoned line:
`No external API integration: this phase removes a comparison harness and sweeps documentation and
durable-memory framing; it adds no external API, SDK or service client.` The API-coverage detector
was run against this phase's scope during planning and returned `detected: false`; this declaration
records that result so the seal-time gate does not have to re-derive it.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-benchmark-de-coupling-memory-sweep/06-CENSUS.md && test -f .planning/phases/06-benchmark-de-coupling-memory-sweep/COVERAGE.md && rg -q '^No external API integration:' .planning/phases/06-benchmark-de-coupling-memory-sweep/COVERAGE.md && S='[[:space:]/*#]+' && W='[[:space:]/*#`>@-]*' && F="${TMPDIR:-/tmp}/06-poscontrol.txt" && printf '%s\n' '// the retired harness invoked a second (TS' '// codegraph@1.3.1) binary here.' '// this mirrored the head-to-' '// head runner from colbymchenry/' '// codegraph-ts.' > "$F" && PC=$(rg -U -o -i -e "\bTS${W}codegraph" -e '\bhead-to-head\b' -e '\bcolbymchenry\b' -e '\bcodegraph-ts\b' -e "\bhead-to-${S}head\b" -e "\bcolbymchenry/${S}codegraph-ts\b" "$F" | wc -l | tr -d ' ') && LB=$(rg -o -i -e "\bTS${W}codegraph" -e '\bhead-to-head\b' -e '\bcolbymchenry\b' -e '\bcodegraph-ts\b' -e "\bhead-to-${S}head\b" -e "\bcolbymchenry/${S}codegraph-ts\b" "$F" | wc -l | tr -d ' ') && echo "POSCTRL_MULTILINE=$PC POSCTRL_LINEBASED=$LB" && test "$PC" -gt "$LB" && test "$PC" -gt 0 && rg -U --files docs/BENCHMARKS.md tools/bench internal/bench internal/corpora/manifest.go corpora/manifest.json .github/workflows/bench.yml internal/upgrade/taskfile_shape_test.go | sort > "${TMPDIR:-/tmp}/06-census-files.txt" && FILES=$(wc -l < "${TMPDIR:-/tmp}/06-census-files.txt" | tr -d ' ') && CRIT=0; for c in docs/BENCHMARKS.md tools/bench/BASELINE.md tools/bench/baseline.json tools/bench/realcorpus/manifest.go tools/bench/realcorpus/manifest_test.go tools/bench/runner/main.go tools/bench/runner/main_test.go internal/bench/regression.go internal/bench/metrics.go internal/bench/rss.go internal/corpora/manifest.go .github/workflows/bench.yml internal/upgrade/taskfile_shape_test.go; do rg -q -F -x "$c" "${TMPDIR:-/tmp}/06-census-files.txt" && CRIT=$((CRIT+1)) || echo "MISSING FROM CENSUS SURFACE: $c"; done; echo "CENSUS_FILES_SCANNED=$FILES CENSUS_CRITICAL_PRESENT=$CRIT/13" && test "$FILES" -ge 12 && test "$CRIT" = "13" && test -f .planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md && L=.planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md && BT=$(rg -o -e 'BENCH03_STATUS=closed-by-ci-run' -e 'BENCH03_STATUS=pending-no-ci-run' "$L" | wc -l | tr -d ' ') && echo "BENCH03_STATUS_TOKENS=$BT" && test "$BT" = "1" && CLOSED=$(rg -o -F 'BENCH03_STATUS=closed-by-ci-run' "$L" | wc -l | tr -d ' ') && if test "$CLOSED" = "1"; then URLTOK=$(rg -o 'BENCH03_STATUS=closed-by-ci-run https://github\.com/[^/]+/[^/]+/actions/runs/[0-9]+' "$L" | wc -l | tr -d ' '); CONC=$(rg -o '"conclusion":\s*"success"' "$L" | wc -l | tr -d ' '); echo "BENCH03_RUN_URL_ON_TOKEN=$URLTOK BENCH03_CONCLUSION_SUCCESS=$CONC"; test "$URLTOK" = "1" && test "$CONC" -ge 1; else echo "BENCH03_RUN_URL_ON_TOKEN=n/a-pending BENCH03_CONCLUSION_SUCCESS=n/a-pending"; fi && TOTAL=$(rg -U -o -i -e "\bTS${W}codegraph" -e "\bTS${W}Node\b" -e "\bTS[[:space:]-]*(binary|binaries|CLI|project|subject|runner|harness)\b" -e '\bts-binary\b' -e '\btsBinary\b' -e "\bGo[[:space:]/-]*vs\.?[[:space:]/-]*TS\b" -e '\bGo/TS\b' -e "\bTS[[:space:]]*1\.3\.1\b" -e '"ts"' -e '\|[[:space:]]*ts[[:space:]]*\|' -e '\bhead-to-head\b' -e '\bheadtohead\b' -e '\bcolbymchenry\b' -e '\bcodegraph-ts\b' -e '\bcomparator\b' -e '\b1\.3\.1\b' -e "\bcomparison${S}(binary|harness|runner|target|subject|mode)\b" docs/BENCHMARKS.md tools/bench internal/bench internal/corpora/manifest.go corpora/manifest.json .github/workflows/bench.yml internal/upgrade/taskfile_shape_test.go | wc -l | tr -d ' ') && echo "PHASE6_CENSUS_TOTAL=$TOTAL" && test "$TOTAL" = "0" && RESID=$(rg -U -n -i '\bTS\b' docs/BENCHMARKS.md tools/bench internal/bench internal/corpora/manifest.go corpora/manifest.json .github/workflows/bench.yml internal/upgrade/taskfile_shape_test.go | wc -l | tr -d ' ') && echo "BARE_TS_RESIDUAL_LINES=$RESID" && test "$RESID" -ge 1 && go build ./... && go vet ./... && go test -count=1 ./internal/bench/... ./internal/corpora/... ./internal/upgrade/... ./tools/bench/...</automated>
  </verify>
  <acceptance_criteria>
    - The positive control passes before any real result is believed: `POSCTRL_MULTILINE` is strictly greater than `POSCTRL_LINEBASED` and strictly greater than 0. Both numbers are echoed and copied into `06-CENSUS.md`.
    - `CENSUS_FILES_SCANNED` is echoed and is at least 12 — the scanned-file floor that distinguishes a real zero from a wrong-cwd zero — **and** `CENSUS_CRITICAL_PRESENT=13/13`: every named critical file appears in the walked list. The floor alone admits a shifted surface, where a deleted critical file plus unrelated additions preserves the count (review finding 06-04:143). Any file missing from the surface is printed by name rather than absorbed into a number.
    - `BENCH03_STATUS_TOKENS=1`: `06-LIVE-VERIFICATIONS.md` carries exactly one BENCH-03 status token, and it is one of `BENCH03_STATUS=closed-by-ci-run <url>` or `BENCH03_STATUS=pending-no-ci-run <reason>`. Zero tokens fails and two tokens fails. BENCH-03 cannot be closed by silence, and a local measurement cannot stand in for a workflow run (review HIGH 5).
    - **On the closed branch the run itself is validated, not just the token** (cycle-2 MEDIUM 1 — the token was counted but its `<url>` was never checked, so a mis-selected close passed): `BENCH03_RUN_URL_ON_TOKEN=1`, requiring the token line to carry a well-formed `https://github.com/<owner>/<repo>/actions/runs/<id>` URL, **and** `BENCH03_CONCLUSION_SUCCESS >= 1`, requiring the pasted `gh run view <id> --json conclusion,event,headBranch,url` block to report a successful conclusion. The regex tolerates both `gh`'s pretty and compact JSON forms. On the pending branch both print `n/a-pending` and neither is asserted — the branch is detected from the token, so the criterion is satisfiable on both paths and vacuous on neither. The SUMMARY additionally states that the block's `headBranch` and `url` match the measured branch and the token's URL.
    - `PHASE6_CENSUS_TOTAL=0` is echoed verbatim. The number is the acceptance criterion, not the exit status (rule `84d1gfpywd`).
      **The `TS` alternation is BOUNDED to its retired-framing senses (cycle-3 HIGH 2, class fix D).** With a bare `-i \bTS\b` this gate could not reach zero on any correct execution: measured at `fd02272`, `corpora/manifest.json` alone returned **1** hit — `TS` inside `TS/JS` in the `LOCKED (01-06)` corpus-language-coverage note at line 10 — a file in no plan's `files_modified`, carrying text that is the phase's own KEEP class, with rewording and exclusion-widening both prohibited to the executor and the only sanctioned remedy ("bound the pattern") unavailable because the pattern is hard-coded here. Re-measured with the bounded set, **per file, not in aggregate**: `corpora/manifest.json` → **0**, total over the surface → **170** (two more than the old set, because the bounded form also catches camelCase `tsBinary`), and every file with a non-zero count is a declared sweep target of exactly one plan. The full per-file table is in the `<action>` block and MUST be copied into `06-CENSUS.md`.
    - **Completeness control on the bounding itself — the bare instrument is still run, and its residual must be adjudicated, not assumed away:** `BARE_TS_RESIDUAL_LINES >= 1`, where the residual is `rg -U -n -i '\bTS\b'` over the same surface. This is the positive companion proving the bounded zero is a real zero and not a wrong-cwd or wrong-surface zero. **Every residual line MUST be quoted in `06-CENSUS.md` and adjudicated as one of: (a) already covered by another pattern in the set, or (b) the TypeScript-the-indexed-language KEEP sense, cited to the same Phase-5 adjudication the `internal/indexer/**` exclusion cites.** A residual that is neither is a finding about the bounding — report it, do not absorb it. Expected on a correct execution: one line, `corpora/manifest.json:10`; the floor is `>= 1` rather than `= 1` deliberately, because a rewritten `docs/BENCHMARKS.md` may legitimately name `TS/JS` as an indexed-language pair and an exact count would then redden correct work — the same defect this cycle exists to remove.
    - `06-CENSUS.md` records: the exact pattern set, both positive-control numbers, the scanned-file count, the file surface, the full exclusion list with a citation per exclusion, and every pattern narrowed during the run with the flagged text quoted.
    - `rg -o -F 'bare `comparison`' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-CENSUS.md | wc -l` is >= 1 — the record states explicitly why the unbounded pattern was excluded, so a later reader sees a decision rather than an omission.
    - `rg -c '\bcomparison\b' internal/bench/regression.go tools/bench/BASELINE.md` still returns non-zero counts for both files — the legitimate uses survived, proving the phase's zero was not bought with a find-and-replace.
    - `COVERAGE.md` exists and its first content line begins `No external API integration:` with a reason.
    - Protected files intact, pre-plan-SHA relative per class fix A and two-sided: `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-PREPLAN-SHA.txt); test -f NOTICE && test -f CHANGELOG.md && test "$(rg -c -F 'Copyright (c) 2026 Colby Mchenry' NOTICE || true)" = "1" && git diff --quiet "$BASE" -- NOTICE CHANGELOG.md && echo "PROTECTED_FILES_UNCHANGED_SINCE=$BASE"`
    - `go build ./... && go vet ./... && go test -count=1 ./internal/bench/... ./internal/corpora/... ./internal/upgrade/... ./tools/bench/...` green.
  </acceptance_criteria>
  <done>A census whose instrument was demonstrated live to catch a planted wrapped phrase reports a verbatim zero across the declared Phase 6 file surface — with every critical file proven present in the scanned set, not merely a count — every exclusion is cited to a prior adjudication, the legitimate ordinary-sense prose survives as proof the zero was earned, BENCH-03's status is recorded as exactly one explicit token, and the phase's no-external-API position is declared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published document -> reader | figures are consumed as fact; an unprovenanced number is unfalsifiable |
| census exclusion list -> the phase's completion claim | a widened exclusion silently converts a real gap into a green result |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Repudiation | the phase acceptance census exclusion list | high | mitigate | Every exclusion cited to a recorded prior adjudication in `06-CENSUS.md`; the instrument is positive-controlled before any zero is trusted; a scanned-file floor guards against a wrong-cwd vacuous zero; the survival of legitimate `comparison` prose is asserted positively |
| T-06-12 | Spoofing | `docs/BENCHMARKS.md` figures | high | mitigate | Every published figure GENERATED from the committed raw `06-PUBLISH-RESULTS.json` by `publishcheck -emit-rows` and re-matched verbatim, so all ten cells are traceable rather than one; commit SHA, date, goos/goarch/runner, host conditions (if local) and corpus pins published alongside; a prohibition forbids carrying over, estimating or recalling a figure |
| T-06-23 | Repudiation | BENCH-03's completion claim | high | mitigate | A blocking `checkpoint:decision` resolves BENCH-03 into exactly one of two verbatim tokens in `06-LIVE-VERIFICATIONS.md`, asserted to be present exactly once; a local measurement cannot close it by implication (review HIGH 5) |
| T-06-24 | Tampering | the committed measurement versus the runner's actual stdout | medium | mitigate | The runner's stdout is teed to a temp file in the same pipeline that writes the artifact, and the two are compared with `cmp` plus matching SHA-256 values from two independent verifier invocations (review 06-04:142) |
| T-06-13 | Information disclosure | measurement artifacts committed under `.planning/` | low | accept | The runner emits only counts, durations, RSS figures and platform labels; no path outside the scratch root, no credential and no source content is present in the JSON |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no package-manager install. The phase's only package-manager change is the removal performed in 06-02. |
</threat_model>

<verification>
- `06-PUBLISH-RESULTS.json` holds two positive-metric `subject: "go"` records for the two surviving corpora, and is proven byte-identical to the runner's stdout by `cmp` plus matching SHA-256 values from two independent `publishcheck` runs.
- Every published data cell is generated by `publishcheck -emit-rows` and re-matched verbatim against the document (`ROWS_EMITTED=2 ROWS_MATCHED=2`), so all ten metric cells are traceable rather than one.
- `docs/BENCHMARKS.md` passes both the retired-term census (verbatim 0) and the required-content assertions (every token count >= 1). That document gate's `TS` alternation is bounded to the same class-fix-D senses as the phase census, so it no longer forbids the indexed-language `TS/JS` its own residual-floor rationale calls legitimate (cycle-4 LOW 1); no bare `\bTS\b` survives in the phase except the deliberate `BARE_TS_RESIDUAL_LINES` completeness control.
- The phase census prints its positive-control pair, its scanned-file count, `CENSUS_CRITICAL_PRESENT=13/13`, `PHASE6_CENSUS_TOTAL=0` and `BARE_TS_RESIDUAL_LINES >= 1` with every residual line quoted and adjudicated in `06-CENSUS.md` — the `TS` alternation is bounded to its retired-framing senses so TypeScript-the-indexed-language survives, and the bare instrument is still run as the completeness control on that bounding.
- `06-LIVE-VERIFICATIONS.md` carries exactly one BENCH-03 status token, resolved by the blocking decision gate; if that token is `closed-by-ci-run` it carries a well-formed run URL and the run's `gh run view --json` output showing a successful conclusion.
- `docs/BENCHMARKS.md` carries the three named `##` headings verbatim, asserted by text rather than by a count.
- Legitimate `comparison` prose survives in `internal/bench/regression.go` and `tools/bench/BASELINE.md`.
- Full build, vet and the bench/corpora/upgrade test packages green.
</verification>

<success_criteria>
BENCH-01 is satisfied by a document publishing measured, provenanced absolute figures with
methodology and the regression-gate description and no comparison content, every data cell
mechanically generated from the committed measurement. BENCH-02's in-tree half is closed by a
census whose instrument was proven before its zero was believed. **BENCH-03 is resolved, not
assumed**: its status is recorded as exactly one explicit token — closed by an inspected
`bench.yml` run, or pending with a stated reason. The plan can complete either way; what it cannot
do is complete with BENCH-03 unmarked.
</success_criteria>

<output>
Create `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-SUMMARY.md` when done.
Record: the measurement path used (and, if local, why, plus the host conditions), the two matching
SHA-256 values proving byte identity, the provenance values published, the emitted-versus-matched
row counts, the three heading-assertion numbers, the verbatim census numbers (positive control,
scanned files, critical-file fraction, total), the BENCH-03 status token the checkpoint selected —
with, on the closed branch, the run URL and the `gh run view --json conclusion,event,headBranch,url`
output plus a statement that its `headBranch` and `url` match — and every pattern narrowed with the
flagged text quoted.
</output>
