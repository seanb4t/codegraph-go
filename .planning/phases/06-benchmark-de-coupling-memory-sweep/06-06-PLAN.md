---
phase: 06-benchmark-de-coupling-memory-sweep
plan: 06
type: execute
wave: 5
depends_on: ["06-05"]
files_modified:
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md
autonomous: false
requirements: [MEM-01, MEM-02]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every engram record in the sweep scope whose durable content asserts the retired framing as a current standing fact or as a future obligation has been superseded by a corrected record — none overwritten, none deleted (MEM-01)."
    - "Every record that describes what was decided, measured or done at a point in time survives untouched (D-13)."
    - "The sweep scope covers the repo spine `repo:github.com/seanb4t/codegraph-go`, the rule scope `rule:repo:github.com/seanb4t/codegraph-go`, and every workspace overlay scope a fresh session recalls (D-14)."
    - "`06-MEMORY-SWEEP.md` enumerates every record in scope by `short_id` with a per-record verdict of supersede or leave-historical and a reason (D-15)."
    - "A record whose durable content mixes a point-in-time statement with a present-tense or forward-looking assertion of the retired framing is superseded, and the corrected record preserves the historical clause verbatim — a mixed record is neither collapsed into one verdict nor left uncorrected."
    - "A scope that enumerates to zero records is recorded with an explicit zero-record line naming the scope and the enumeration call that produced it — never omitted and never reported as nothing-to-do."
    - "`06-MEMORY-SWEEP.md` lists records grouped by scope and sorted by `short_id` ascending, so two independent enumerations produce the same order and a reviewer can diff them."
    - "The enumeration paged to exhaustion: every scope's listing was followed through its pagination cursor until no further page was returned, and the page count is recorded."
    - "The set of `short_id`s approved as `supersede` equals, exactly and without duplicates, the set of `short_id`s recorded as executed — asserted by set comparison, not by section presence (review finding 06-06:248)."
    - "The known-minimum-three-rules assertion is applied to the population the live tool surface actually exposes rules through, named explicitly, and is a membership check against the three known rules rather than a bare count (review finding 06-06:160)."
  artifacts:
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md — extended with the enumeration, the classification and the executed-supersede record"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md — appended with the MEM-02 store-half row (created by 06-04, extended by 06-05)"
  key_links:
    - "engram tool availability in the EXECUTING session is the precondition — not network reachability of the gateway host; a session with a reachable host but no registered tools cannot enumerate anything"
    - "the rule scope is injected as MUST-follow ground truth at session start, so framing there is the most load-bearing of all scopes (D-14)"
    - "memory short_id myywc0y9vm is the named maintainer correction this phase executes — a concrete anchor to start from before an unscoped enumeration"
  prohibitions:
    - "MUST NOT delete or overwrite any engram record. Correction is by supersede, which appends to the store's durable history; a record of real history that is removed cannot be recovered from git, because the store does not live in git."
    - "MUST NOT record the sweep as complete against a scope that could not be enumerated. If the tools are unavailable, a scope errors, or pagination cannot be exhausted, halt and surface it — a partial sweep reported as complete is the exact failure D-16 exists to prevent."
    - "MUST NOT supersede a record that states only what was decided, measured or done at a point in time. Those records are true and are the project's history."
    - "MUST NOT infer a record's content from its title or from recall context. Read the record's durable content before classifying it."
---

<objective>
Sweep the durable memory store so a session started after this phase recalls codegraph-go as a
project rather than as a port, correcting by supersede and never by deletion.

Purpose: MEM-01 requires every spine record asserting retired framing to be superseded — not
overwritten, not deleted — and MEM-02 requires the resulting recall to carry no present-tense port
or parity framing. The store lives outside git, so no CI gate can hold this; D-15 makes a committed
enumeration the evidence, and D-16 makes enumerability a hard precondition.

Output: `06-MEMORY-SWEEP.md` extended with a complete, ordered, per-scope enumeration carrying a
verdict and reason for every record, and the supersede operations actually executed against the
records classified for correction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PATTERNS.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See `06-01-PLAN.md` → "Artifacts this phase produces" for the full created/deleted register.
This plan EXTENDS `06-MEMORY-SWEEP.md` (created by 06-05) and appends the MEM-02 store-half row to
`06-LIVE-VERIFICATIONS.md` (created by 06-04, extended by 06-05). It creates no code symbol and
deletes nothing — by construction: correction in this store is an append, never a removal.
</artifacts_this_phase_produces>

<edge_probe_coverage>
Spec-less probe fallback (no SPEC.md for this phase). Rows owned by this plan:

| Requirement | Category | Status | Disposition |
|---|---|---|---|
| MEM-01 | adjacency | **resolved (explicit)** | Authored into `must_haves.truths`: a record mixing a historical clause with a present-tense or forward-looking assertion is superseded, and the corrected record preserves the historical clause verbatim. Mixed records neither merge into one verdict nor slip through uncorrected. |
| MEM-01 | empty | **resolved (explicit)** | Authored into `must_haves.truths`: a scope enumerating to zero records gets an explicit zero-record line naming the scope and the call that produced it. |
| MEM-01 | ordering | **resolved (explicit)** | Authored into `must_haves.truths`: records are grouped by scope and sorted by `short_id` ascending, so two enumerations are diffable. |
| MEM-02 | unclassified | **unresolved** | **Flagged assumption — not auto-resolved.** D-15 explicitly declines post-sweep re-query verification, so "a session started after the sweep recalls no retired framing" is asserted from the enumeration rather than demonstrated by a fresh recall. The maintainer accepted this trade-off knowingly; it is restated here so a later audit sees a decision, not an oversight. The full enumeration is what gives a reviewer the population to check against. |

**Recorded gap (accepted, D-15):** without re-query evidence, "the supersede actually landed" is
asserted rather than demonstrated — the vacuous-pass shape rule `84d1gfpywd` warns about. Task 3
mitigates as far as D-15 permits by recording each supersede call's returned identifier and the
operation name it was returned by, which is evidence the store accepted the write even though the
recall itself is not re-queried.
</edge_probe_coverage>

<review_incorporation>
## Cross-AI review findings incorporated (`06-REVIEWS.md`)

| Finding | Fix |
|---|---|
| `06-06:160` — the "at least three rules" assertion may target the wrong population: the live tool surface is unverified, so if rules surface through a distinct endpoint or pagination shape the check falsely reports a truncated enumeration | Task 1 now names the population explicitly after live discovery, and the assertion becomes a MEMBERSHIP check against the three rules known to exist (the commit-signing bypass rule, the positive-assertion guard rule `84d1gfpywd`, the CI-skip-marker prohibition) rather than a bare count. If rules turn out to be objects distinct from memory records, the check is applied to the rule objects and the divergence is recorded. |
| `06-06:248` — the automated closure check verifies section presence and the absence of delete/overwrite verdicts, but not the set equality the prose criterion requires | Task 3 extracts the approved-`supersede` `short_id` column and the executed-row `short_id` column, sorts each with `sort -u`, and requires an empty `diff` between them plus a duplicate check on each. The counts are printed, and the legitimately-zero case is handled explicitly rather than passing silently. |
| reviewer suggestion — record the supersede OPERATION NAME and response shape per write, not only an identifier | Adopted in Task 3's record format. |
| reviewer suggestion — mark MEM-02 as "accepted by D-15 evidence standard" rather than "demonstrated", unless the optional fresh-session check is actually performed | Adopted, and made mechanical: Task 3 writes exactly one of two verbatim status tokens into `06-LIVE-VERIFICATIONS.md`. |

**Explicitly NOT applied — locked decisions:**

- The reviewer's MEDIUM "a returned identifier proves write acceptance, not supersede semantics"
  is the **locked D-15 trade-off**, and the review itself classifies it as reported residual risk
  rather than a defect. Requiring post-sweep re-query evidence would reopen D-15, which the
  maintainer decided knowingly and recorded as a deliberate deferral in CONTEXT.md's Deferred
  Ideas. **Rejected on that ground**, not because the risk is imaginary: it is real, it is named
  in `must_haves`, in `<edge_probe_coverage>` and now in a status token, and a future phase may
  close it.
- MEM-02 remaining assertion-based rather than demonstrated is accepted by the same decision.
  The status token records which it is, so a later audit sees the standard that was met.

## Cycle 2

| Cycle-2 finding | Fix |
|---|---|
| **MEDIUM 4** — the executed-set extractor used a fixed `rg -A400` window, which truncates a long execution table (false set mismatch) and spills a short one into `## Completeness and accepted gaps`, whose rows also match the id pattern (false extra ids). This extractor is the very mechanism that closes cycle-1's 06-06:248, so it must not be the weak link | Replaced with `awk '/^## Supersedes executed/{f=1;next} /^## /{f=0} f'`, bounded at the next `^## ` heading regardless of table length. The acceptance criterion states the failure mode it replaces and additionally requires the extractor's raw line count in the SUMMARY, so a silently-empty extraction cannot be read as a legitimate empty-versus-empty match. |
| **Class fix C** (see 06-03's `<review_incorporation>`) | This plan's counts already come from `rg -o … \| wc -l` or `wc -l`; no `= "0"` comparison is fed by a bare `rg -c`. Checked, not assumed. |

**Preserved unchanged by this replan:** `autonomous: false` and the blocking
`checkpoint:decision` before the first durable write. Nothing in cycle 2 touches either.
</review_incorporation>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every record in every sweep scope, paging to exhaustion</name>
  <precondition>The executing session has engram MCP tools registered and callable — a read-only listing call against the repo spine scope returns a result rather than a tool-not-found error. This is the real precondition, NOT network reachability of the gateway host: engram is configured at user level and not in this repository's `.mcp.json`, so a session on a reachable network with no registered tools still cannot enumerate anything. If the tools are unavailable, HALT and surface a checkpoint. Do not substitute recall context, do not infer the population, and do not proceed to the checkpoint.</precondition>
  <files>.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md</files>
  <read_first>
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md` as left by 06-05 — the existing agent-facing-files section and its table shape, which this task extends rather than replaces
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md` §D-13, §D-14, §D-15, §D-16 — the classification test, the scope list, the evidence standard and the fail-loud precondition
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md` §Pattern 6 and §Assumptions Log A1 — the assumed workflow shape, explicitly flagged LOW confidence and never exercised against a live tool
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PATTERNS.md` §"06-MEMORY-SWEEP.md" — the recorded absence of any in-repo analog for this work
  </read_first>
  <action>
Enumerate first, classify second, mutate never — this task performs no write of any kind.

**Verify the tool surface live before using it.** RESEARCH.md's description of the engram tool
names, parameters and pagination field is marked LOW confidence and was never exercised against a
real call. Discover the actual tool names and their parameter shapes in this session, and record
what you found. Do not carry a parameter name over from the research document without confirming it.

**Scopes to enumerate (D-14) — all three, each recorded separately:**
1. the repo spine `repo:github.com/seanb4t/codegraph-go`
2. the rule scope `rule:repo:github.com/seanb4t/codegraph-go`. Three rules are known to exist: the
   commit-signing bypass rule, the positive-assertion guard rule (`84d1gfpywd`), and the
   CI-skip-marker prohibition. A listing that does not surface all three is evidence the listing
   is wrong, not that rules were removed.

   **Name the population before asserting against it** (review finding 06-06:160). RESEARCH.md's
   description of this surface is LOW confidence and was never exercised, so it is genuinely open
   whether rules are ordinary memory records carrying a rule scope, or distinct rule objects
   reached through their own listing operation with its own pagination shape. Discover which, state
   it in the record, and apply the assertion to THAT population. Applying a memory-record count to
   a surface that exposes rules separately would report a truncated enumeration that never
   happened — and, worse, would invite someone to "fix" a correct enumeration.

   **Assert membership, not just a count.** For each of the three known rules, record which
   enumerated entry corresponds to it, identified by its durable content. A count of three drawn
   from three unrelated entries would satisfy a bare floor while the rules themselves went
   unenumerated.
3. every workspace overlay scope this repository has. Enumerate the overlays that exist rather than
   assuming there are none; record the list found, including an empty list stated explicitly.

**Page to exhaustion.** For each scope, follow the listing's pagination cursor until no further page
is returned, and record the page count and the running record count per page. A first-page-only
enumeration is the specific failure mode this instruction exists to prevent — it would under-report
the population and every downstream verdict would be drawn from a truncated set.

**Start from the named anchor.** Fetch memory `myywc0y9vm` first — CONTEXT.md cites it as the
maintainer correction this phase executes — and record its content and verdict as the first row.
Then run the unscoped enumeration. Starting from a known record proves the read path works before
any absence is interpreted.

**Classify each record with D-13's test, reading its durable content — never its title, never recall
context:**
- `supersede` — the content states the retired framing as a **current standing fact** (parity with a
  prior implementation is the baseline) or as a **future obligation** (a later phase must maintain
  it, that behaviour is the compatibility target). Forward-looking records steer future sessions
  just as effectively as present-tense ones, which is why both classify the same way.
- `leave-historical` — the content describes what was decided, measured or done at a point in time.
  These are true statements and must survive untouched.
- A record whose content contains **both** classifies as `supersede`, and the correction must
  preserve the historical clause verbatim. Record the two clauses separately in the row so a
  reviewer can see what was corrected and what was carried forward.

**Write the enumeration** into `06-MEMORY-SWEEP.md` as new sections appended below 06-05's:
`## Engram enumeration (D-14)` with one subsection per scope, each carrying its enumeration call,
its page count, its record count, and a table of rows sorted by `short_id` ascending:
`short_id | scope | durable content (quoted, or summarised faithfully) | verdict | reason`.
A scope with zero records gets an explicit line stating the scope, the call made, and
`0 records enumerated` — never an omitted subsection.

Perform no supersede, store, update, delete or visibility call in this task.
  </action>
  <verify>
    <automated>F=.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md; test -f "$F" && rg -q '^## Engram enumeration' "$F" && SCOPES=$(rg -o -i '^### .*(spine|rule:repo|overlay)' "$F" | wc -l | tr -d ' '); ROWS=$(rg -o '^\| `?[a-z0-9]{8,}`? \|' "$F" | wc -l | tr -d ' '); SUP=$(rg -o -F '| supersede |' "$F" | wc -l | tr -d ' '); LEAVE=$(rg -o -F '| leave-historical |' "$F" | wc -l | tr -d ' '); PAGES=$(rg -o -i 'pages enumerated' "$F" | wc -l | tr -d ' '); echo "SCOPE_SECTIONS=$SCOPES ENUMERATED_ROWS=$ROWS SUPERSEDE_VERDICTS=$SUP LEAVE_VERDICTS=$LEAVE PAGE_COUNTS_RECORDED=$PAGES"; test "$SCOPES" -ge 3 && test "$ROWS" -ge 1 && test "$PAGES" -ge 3 && test $((SUP + LEAVE)) -eq "$ROWS" && rg -q 'myywc0y9vm' "$F" && echo "NAMED_ANCHOR_PRESENT=true"</automated>
  </verify>
  <acceptance_criteria>
    - The precondition was asserted and its result recorded. If the engram tools were unavailable the task HALTS with a checkpoint and this plan is not marked complete — a sweep recorded against a store that could not be read is the failure D-16 names.
    - `06-MEMORY-SWEEP.md` gains an `## Engram enumeration (D-14)` section with at least three scope subsections (`SCOPE_SECTIONS` >= 3): the repo spine, the rule scope, and workspace overlays (present even if the overlay list is empty, in which case it says so).
    - `PAGE_COUNTS_RECORDED` >= 3 — every scope records how many pages its listing returned. This is the positive assertion that pagination was followed to exhaustion rather than stopped at page one (rule `84d1gfpywd`).
    - `SUPERSEDE_VERDICTS + LEAVE_VERDICTS` equals `ENUMERATED_ROWS` exactly — every enumerated record carries exactly one verdict, so no record was silently dropped from the classification.
    - Every row has a non-empty reason. Every verdict is exactly `supersede` or `leave-historical`; no verdict is `delete` or `overwrite`.
    - Rows within each scope are sorted by `short_id` ascending. Verify by extracting the `short_id` column per scope and confirming it equals its own `sort` output; record the check in the SUMMARY.
    - The rule population is NAMED before it is counted (review finding 06-06:160): the record states whether rules surfaced as ordinary memory records in the rule scope or as distinct rule objects with their own listing operation, and the assertion is applied to that population. Its count is >= 3, AND each of the three known rules — the commit-signing bypass rule, the positive-assertion guard rule `84d1gfpywd`, the CI-skip-marker prohibition — is matched to a specific enumerated entry by its durable content. A count of three drawn from three unrelated entries fails. A listing that does not surface all three is reported as a suspect enumeration, not as a finding about the store.
    - `myywc0y9vm` appears as an enumerated row with content and a verdict (`NAMED_ANCHOR_PRESENT=true`).
    - The actual tool names and parameter shapes used are recorded in the section, together with any divergence from RESEARCH.md's assumed shape.
    - No write call of any kind was made: the SUMMARY states this explicitly and lists every tool call performed.
  </acceptance_criteria>
  <done>Every scope a fresh session recalls is enumerated to exhaustion, every record carries exactly one verdict with a reason, the rows are deterministically ordered, and not a single write was issued.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the supersede classification before any durable correction is issued</name>
  <decision>Approve the per-record supersede / leave-historical classification Task 1 enumerated, before any correction is written to the durable memory store.</decision>
  <context>
Superseding a record appends a correction to the store's durable history. It cannot be un-issued —
only further superseded — and the classification judgement is baked into what every future session
recalls. The store does not live in git, so there is no revert. CONTEXT.md rates D-13 `one-way` for
exactly this reason.

What is being confirmed is the **classification**, not the decision to sweep. D-13 itself is locked
and is not reopened here. Review Task 1's enumeration in `06-MEMORY-SWEEP.md` and confirm each row's
verdict — in particular the mixed rows (a historical clause plus a standing assertion), which are
where a wrong call either erases a true record or leaves misleading framing in place.

Undo cost if wrong: a record wrongly superseded needs a further supersede restoring the original
claim, and every session between the two recalls the wrong thing. A record wrongly left in place
keeps steering future sessions with retired framing, which is the outcome MEM-02 exists to prevent.
  </context>
  <options>
    <option id="approve-as-classified">
      <name>Approve the classification as enumerated</name>
      <pros>Executes exactly the reviewed verdicts; the record and the action match, with no drift between them.</pros>
      <cons>A misclassification in the reviewed table is carried into the store.</cons>
    </option>
    <option id="approve-with-amendments">
      <name>Approve with named per-row amendments</name>
      <pros>Corrects specific rows before anything durable is written; amendments are recorded alongside the original verdict, so the change of mind stays visible.</pros>
      <cons>Requires naming each amended short_id and its new verdict explicitly.</cons>
    </option>
    <option id="re-enumerate">
      <name>Reject and re-enumerate</name>
      <pros>The right response if the page counts, the rule-scope count or the overlay list look wrong — a truncated enumeration makes every verdict unsafe.</pros>
      <cons>Repeats Task 1 before any correction is issued.</cons>
    </option>
  </options>
  <resume-signal>Select: approve-as-classified, approve-with-amendments (naming each short_id and its new verdict), or re-enumerate.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the approved supersedes and close the sweep record</name>
  <precondition>The checkpoint above resolved with an explicit approval, and `06-MEMORY-SWEEP.md`'s enumeration reflects any amendments that approval named. No supersede is issued against an unreviewed classification.</precondition>
  <reversibility rating="one-way">A supersede is an append to the store's durable history; the correction cannot be un-issued, only further superseded, and the classification is baked into what future sessions recall (D-13).</reversibility>
  <files>.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md, .planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md</files>
  <read_first>
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md` as left by Task 1 and amended by the checkpoint — the approved verdict table this task executes
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md` as left by 06-04 and 06-05 — the ledger shape this task appends its MEM-02 store-half row to
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md` §D-13, §D-15
    - `.planning/PROJECT.md` `## Core Value` and `## Constraints` as left by 06-05 — the corrected wording each replacement record must be consistent with, so the store and the repository say the same thing
  </read_first>
  <action>
Issue one supersede per row whose approved verdict is `supersede`, and nothing else.

For each such record:
- Compose the corrected content on the project's own terms, consistent with the wording 06-05 left
  in `.planning/PROJECT.md`. Where the original mixed a historical clause with a standing assertion,
  carry the historical clause through **verbatim** and correct only the assertion — the point of a
  supersede is a corrected record, not a shorter one.
- Call the store's supersede operation, which appends a correction and marks the original
  superseded. Do NOT call an update, overwrite, delete or visibility operation. If the tool surface
  discovered in Task 1 offers no supersede semantics, HALT and surface it rather than substituting
  the nearest available write — substituting an overwrite for a supersede violates MEM-01 directly.
- Record, next to the row: the **operation name actually called**, the returned identifier (or the
  returned confirmation), and the shape of the response. Not the identifier alone — the operation
  name is what distinguishes a supersede from an update that happened to return an id, and the
  response shape is what a later reader needs to judge the evidence for themselves. This is the
  only positive evidence the store accepted the write that D-15's evidence standard permits, since
  post-sweep re-query is explicitly declined.

Issue nothing at all against rows whose verdict is `leave-historical`.

Then close `06-MEMORY-SWEEP.md`:
- Add a `## Supersedes executed (MEM-01)` section: one row per executed supersede carrying the
  original short_id, the scope, the operation name called, the returned identifier and response
  shape, the corrected content, and — for mixed records — the historical clause carried forward
  verbatim. Put the `short_id` in the FIRST column of every row, so the set-equality check below
  can extract it mechanically.
- **Reconcile the two sets explicitly** (review finding 06-06:248 — the prose criterion required
  equality but nothing enforced it). Extract the `short_id` of every approved `supersede` row and
  the `short_id` of every executed row, `sort -u` each, and `diff` them. Paste both sorted lists
  and the (empty) diff into the record. Also state the pre-`sort -u` counts alongside the post-
  counts, so a duplicated row is visible rather than silently collapsed. If the approved set is
  legitimately empty, say so in words and show both empty lists — an empty-versus-empty match is
  a real outcome here, but it must be stated, not inferred from a passing check.
- Add a `## Completeness and accepted gaps` section stating: the total records enumerated per scope,
  the supersede count, the leave-historical count, the arithmetic showing those two sum to the
  total, and the D-15 trade-off restated in this plan's own words — that post-sweep re-query
  evidence was deliberately not collected, so "the correction landed" is evidenced by the store's
  returned identifiers rather than demonstrated by a fresh recall. Name it as an accepted decision,
  not an oversight.
- State explicitly that no record was deleted and none overwritten.

Finally, append the MEM-02 store-half row to
`.planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md` (created by
06-04, extended by 06-05). Add a `## MEM-02 — store half` section carrying exactly one status
line: `MEM02_STORE_STATUS=verified-fresh-session` if the fresh-session recall check below was
actually performed, with what was observed; or
`MEM02_STORE_STATUS=accepted-by-d15-evidence-standard` if it was not — meaning the sweep's
completeness rests on the committed enumeration and the store's returned identifiers, which is
exactly the standard D-15 sets. The second token is a legitimate outcome, not a failure; what is
not legitimate is claiming MEM-02 demonstrated when the standard actually met was D-15's.
  </action>
  <verify>
    <automated>F=.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md; L=.planning/phases/06-benchmark-de-coupling-memory-sweep/06-LIVE-VERIFICATIONS.md; rg -q '^## Supersedes executed' "$F" && rg -q '^## Completeness and accepted gaps' "$F" && SUP=$(rg -o -F '| supersede |' "$F" | wc -l | tr -d ' '); DEL=$(rg -o -i -e '\| delete \|' -e '\| overwrite \|' "$F" | wc -l | tr -d ' '); echo "SUPERSEDE_VERDICTS=$SUP DELETE_OR_OVERWRITE_VERDICTS=$DEL"; test "$DEL" = "0" && rg -q -i 'no record was deleted' "$F" && rg -q -i 're-query' "$F" && rg -N '^\|' "$F" | rg -F '| supersede |' | sed 's/^| *//; s/ *|.*$//' | tr -d '`' | sort > "${TMPDIR:-/tmp}/06-06-approved.txt" && awk '/^## Supersedes executed/{f=1;next} /^## /{f=0} f' "$F" | rg -N '^\| `?[a-z0-9]{6,}`? \|' | sed 's/^| *//; s/ *|.*$//' | tr -d '`' | sort > "${TMPDIR:-/tmp}/06-06-executed.txt" && AC=$(wc -l < "${TMPDIR:-/tmp}/06-06-approved.txt" | tr -d ' ') && EC=$(wc -l < "${TMPDIR:-/tmp}/06-06-executed.txt" | tr -d ' ') && AU=$(sort -u "${TMPDIR:-/tmp}/06-06-approved.txt" | wc -l | tr -d ' ') && EU=$(sort -u "${TMPDIR:-/tmp}/06-06-executed.txt" | wc -l | tr -d ' ') && echo "APPROVED_SUPERSEDE_IDS=$AC APPROVED_UNIQUE=$AU EXECUTED_SUPERSEDE_IDS=$EC EXECUTED_UNIQUE=$EU" && test "$AC" = "$AU" && test "$EC" = "$EU" && diff "${TMPDIR:-/tmp}/06-06-approved.txt" "${TMPDIR:-/tmp}/06-06-executed.txt" && echo "SUPERSEDE_SET_EQUALITY=true" && test -f "$L" && MS=$(rg -o -e 'MEM02_STORE_STATUS=verified-fresh-session' -e 'MEM02_STORE_STATUS=accepted-by-d15-evidence-standard' "$L" | wc -l | tr -d ' ') && echo "MEM02_STORE_STATUS_TOKENS=$MS" && test "$MS" = "1" && echo "SWEEP_RECORD_CLOSED=true"</automated>
    <human-check>Start a fresh session in this repository and read the memory recall it surfaces at startup. Confirm nothing recalled describes codegraph-go as a port, a parity project or a drop-in replacement in the present tense, and nothing states a future obligation to another implementation's behaviour. This is MEM-02's live verification; D-15 declines to make it a required artifact, so the outcome goes into `06-LIVE-VERIFICATIONS.md` as a status token either way — `verified-fresh-session` if performed, `accepted-by-d15-evidence-standard` if not. The reviewer asked that MEM-02 be marked by the standard actually met rather than described as demonstrated; the token is that marking.</human-check>
  </verify>
  <acceptance_criteria>
    - Every row approved as `supersede` has a corresponding entry in `## Supersedes executed (MEM-01)` carrying the operation name called, the store's returned identifier and the response shape. A row with no returned identifier is an unproven write and fails this task; an identifier with no operation name does not distinguish a supersede from an update that happened to return one.
    - **The executed set is extracted by SECTION BOUNDARY, not by a fixed line window** (cycle-2 MEDIUM 4). The previous `rg -A400` form failed in both directions: a long execution table was truncated, producing a false set mismatch, and a short one spilled into `## Completeness and accepted gaps`, whose rows also match `^\| \`?[a-z0-9]{6,}\`? \|` and would have been counted as extra executed ids. The extractor is now `awk '/^## Supersedes executed/{f=1;next} /^## /{f=0} f'`, which ends at the next `^## ` heading whatever the table's length. Since this extractor IS the mechanism that closes review finding 06-06:248, record its raw line count in the SUMMARY alongside the four set counts so a silently-empty extraction is visible rather than read as an empty-versus-empty match.
    - **Set equality is enforced, not merely required in prose** (review finding 06-06:248): `SUPERSEDE_SET_EQUALITY=true`, where the sorted `short_id` set of approved-`supersede` rows `diff`s empty against the sorted `short_id` set of executed rows. Duplicates fail independently (`APPROVED_SUPERSEDE_IDS` equals `APPROVED_UNIQUE`, and likewise for executed). All four counts are printed. If both sets are legitimately empty, the record says so in words — an empty-versus-empty match is a valid outcome but must be stated, not inferred from a green check.
    - `DELETE_OR_OVERWRITE_VERDICTS=0` — no row anywhere in the record carries a delete or overwrite verdict, and the record states in words that no record was deleted and none overwritten.
    - The `## Completeness and accepted gaps` section shows the arithmetic: per scope, supersede count + leave-historical count = total enumerated. The numbers are printed, not implied (rule `84d1gfpywd`).
    - Every mixed record's entry shows the historical clause carried forward verbatim next to the corrected assertion, so a reviewer can confirm nothing true was dropped.
    - The D-15 trade-off is restated explicitly, naming that post-sweep re-query was declined and what stands in its place (`SWEEP_RECORD_CLOSED=true` checks the section is present and names it).
    - `MEM02_STORE_STATUS_TOKENS=1`: `06-LIVE-VERIFICATIONS.md` carries exactly one MEM-02 store-half token, either `verified-fresh-session` or `accepted-by-d15-evidence-standard`. This is the reviewer's suggestion that MEM-02 be marked by the standard actually met rather than described as demonstrated, made mechanical. Zero tokens fails; two fail.
    - No supersede was issued against a row whose approved verdict was `leave-historical`: the set-equality check above establishes this directly — an executed id absent from the approved set fails the `diff`.
    - If the tool surface offered no supersede semantics, the task HALTED and no write was substituted. The SUMMARY records which of the two paths was taken.
  </acceptance_criteria>
  <done>Every approved correction is issued as a supersede with the store's returned identifier recorded, nothing was deleted or overwritten, the enumerated population reconciles arithmetically, and the accepted evidence gap is named as the decision it is.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executing session -> engram MCP tool surface | writes cross into a durable store that lives outside git and has no revert |
| rule scope -> every future session | rules are injected as MUST-follow ground truth, so a wrong correction there propagates furthest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-18 | Tampering | engram records of real project history | high | mitigate | Enumerate-then-classify-then-approve ordering; a blocking `checkpoint:decision` before the first write; correction by supersede only; a prohibition on delete and overwrite asserted by a zero-count check over the record |
| T-06-19 | Repudiation | the completeness claim | high | mitigate | Per-scope page counts recorded; verdict counts must sum to the enumerated total; the rule population is named after live discovery and each of the three known rules matched by content, so the minimum is a membership check rather than a count applied to a possibly-wrong population; approved and executed supersede sets proven equal by `diff` with duplicate checks; D-15's accepted evidence gap is named rather than papered over, and MEM-02's store half carries the standard it met as a token |
| T-06-20 | Spoofing | tool surface assumptions | medium | mitigate | Task 1 verifies the live tool names and parameter shapes in-session and records any divergence from RESEARCH.md's LOW-confidence assumption, rather than hard-coding names from a document that never exercised them |
| T-06-21 | Denial of Service | an unavailable or partially readable store | high | mitigate | The precondition halts the task rather than degrading to a partial sweep; a prohibition forbids recording completion against a scope that could not be enumerated |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no package-manager install; the phase's only package-manager change is the removal performed in 06-02. |
</threat_model>

<verification>
- The engram tool precondition asserted live and its result recorded; unavailability halts rather than degrades.
- All three scopes enumerated to exhaustion with page counts recorded; the rule population named explicitly and each of the three known rules matched to a specific enumerated entry.
- Verdict counts sum exactly to the enumerated record count; rows deterministically ordered by short_id.
- Every approved supersede executed with the operation name, returned identifier and response shape recorded; zero deletes, zero overwrites.
- Approved-supersede and executed-supersede `short_id` sets proven equal by `diff`, with duplicate checks and all four counts printed, the executed set extracted by section boundary rather than by a fixed line window.
- D-15's declined re-query evidence named explicitly as an accepted decision, and MEM-02's store half marked with the standard it actually met.
</verification>

<success_criteria>
MEM-01 is satisfied: every record asserting the retired framing as a standing fact or a future
obligation has been superseded with a corrected record, nothing was overwritten or deleted, no
record of real history was removed, and the executed set is proven equal to the approved set rather
than assumed to be. MEM-02's store half is closed to the standard D-15 sets, and which standard
that is — a performed fresh-session recall, or the accepted enumeration-plus-returned-identifier
evidence — is recorded as an explicit token rather than left to a reader's inference.
</success_criteria>

<output>
Create `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-06-SUMMARY.md` when done.
Record: the tool names and shapes actually used (and any divergence from RESEARCH.md), which
population rules were enumerated through and how each of the three known rules was matched, the
per-scope page and record counts, the verdict arithmetic, the four set-equality counts and the
empty `diff`, the executed supersede operation names and identifiers, and the MEM-02 store-half
status token together with the outcome of the fresh-session recall check if it was performed.
</output>
