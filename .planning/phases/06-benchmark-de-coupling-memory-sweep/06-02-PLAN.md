---
phase: 06-benchmark-de-coupling-memory-sweep
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .github/workflows/bench.yml
  - tools/bench/BASELINE.md
  - internal/upgrade/taskfile_shape_test.go
  - internal/upgrade/bench_workflow_shape_test.go
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-02-PREPLAN-SHA.txt
autonomous: true
requirements: [BENCH-03]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A `bench.yml` run publishes absolute per-corpus numbers with no step in the job installing, resolving or invoking any implementation other than the freshly-built Go binary (BENCH-03)."
    - "The publishing job pins `runs-on: namespace-profile-linux-amd64-4x8` and exports the matching `CODEGRAPH_BENCH_RUNNER` value, so every published record carries its runner identity (D-06.1)."
    - "The publishing job's runner invocation is an inline `go run ./tools/bench/runner`, never a `task bench:*` target, keeping the baseline-overwriting flag off any tab-completable surface (D-06.2)."
    - "The publishing job never gates: a slower number reports and does not fail CI (D-06.3)."
    - "Raw results upload with `if-no-files-found: error` and a human-readable `GITHUB_STEP_SUMMARY` block (D-06.4)."
    - "The `rebless` job's block, its `contents: read` posture and every SHA-pinned `uses:` in the workflow are unchanged by this edit."
    - "All four D-06 properties are asserted STRUCTURALLY and PUBLISH-JOB-SCOPED by a Go test that parses the workflow YAML — never by a file-wide grep, which another job's pre-existing setting can already satisfy (review HIGH 1/2/3)."
  artifacts:
    - ".github/workflows/bench.yml — a `publish` job replacing the retired comparison job, plus a matching workflow_dispatch choice"
    - "internal/upgrade/bench_workflow_shape_test.go — TestBenchPublishJobShape (the publish-job-scoped D-06 verifier) and TestBenchReblessJobShape (the parsed-subtree fixture proving rebless is untouched)"
    - "tools/bench/BASELINE.md — investigation findings intact, comparison framing swept"
    - "internal/upgrade/taskfile_shape_test.go — the bench.yml exception comment names the current job ids"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-02-PREPLAN-SHA.txt — the pre-plan commit every tamper guard in this plan compares against (review class fix A)"
  key_links:
    - "workflow_dispatch.inputs.job choice value -> the publishing job's `if:` conditional -> the job id: all three must move together or the dispatch UI offers an option that runs nothing"
    - "the publishing job's `go run ./tools/bench/runner -mode publish` -> 06-01's runPublish: the mode string is the contract between them"
    - "internal/upgrade/taskfile_shape_test.go's inScopeJobs fixture deliberately excludes bench.yml — renaming a job there must not change that exclusion"
    - "internal/upgrade/bench_workflow_shape_test.go parses jobs.publish as a subtree: every D-06 assertion is anchored to that job key, so a property another job already satisfies cannot stand in for the publish job's own"
  prohibitions:
    - "MUST NOT add any `permissions:` block, write token, or `contents: write` to this workflow while restructuring a job next to `rebless`."
    - "MUST NOT replace any pinned `uses: owner/action@<40-hex-sha>` with a floating tag or branch reference while copying steps into the fresh job."
    - "MUST NOT add a `task bench:publish` (or equivalent) Taskfile target for this job — the inline invocation is the documented D-13/D-01 exception, not an oversight to tidy up."
    - "MUST NOT reword innocent third-party or contributor prose, or widen a census exclusion, to make a count read zero — if a pattern flags ordinary English, bound the pattern."
---

<objective>
Replace the comparison-invoking publish job in `.github/workflows/bench.yml` with a fresh
absolute-numbers job, and sweep the remaining comparison framing out of the benchmark
investigation record.

Purpose: BENCH-03 requires a `bench.yml` run to publish the absolute numbers without invoking
another implementation anywhere in the job. D-05 forbids a rename-and-strip; D-06 enumerates the
four load-bearing properties that live only in comments in the job being deleted, so the rewrite
is a checkable list rather than an open-ended one.

Output: a `publish` job carrying all four D-06 properties and no second implementation, a
`workflow_dispatch` choice list that matches it, and `tools/bench/BASELINE.md` reading on this
project's own terms with every investigation finding intact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See `06-01-PLAN.md` → "Artifacts this phase produces" for the full created/deleted register.
This plan CREATES the `publish` job id and the `publish-results` / `publish-results.json` artifact
name; it DELETES the `headtohead` job id, the `headtohead-results` artifact name, the
`actions/setup-node` step and the global npm install step.
</artifacts_this_phase_produces>

<edge_probe_coverage>
Spec-less probe fallback (no SPEC.md for this phase). Row owned by this plan:

| Requirement | Category | Status | Disposition |
|---|---|---|---|
| BENCH-03 | unclassified | **unresolved** | **Flagged assumption — not auto-resolved.** The probe could not classify BENCH-03. Assumption carried by this plan: "runs and publishes" is satisfied by a real `workflow_dispatch` of the `publish` job whose summary and artifact are inspected, and "without invoking another implementation" is read as *no step in the job installs, resolves, or executes a non-`codegraph-go` indexer* — including transitively via a package manager. A **structural** check over the parsed `publish` job is the automatable half; the dispatch is manual-only by nature (`06-VALIDATION.md` §Manual-Only Verifications) and 06-04 now gates BENCH-03's closure on it rather than leaving it conditional. |
</edge_probe_coverage>

<review_incorporation>
## Cross-AI review findings incorporated (`06-REVIEWS.md`)

This plan carried three of the review's five HIGH findings. All three share one root cause: **D-06
is a four-property locked checklist that was being checked with file-wide greps and prose.** A
grep over `bench.yml` cannot tell the publish job's own properties from another job's, and a
comment cannot constrain a run body. The fix is one instrument, not three patches: a Go test that
parses the workflow YAML and anchors every assertion to the `jobs.publish` subtree.

| Finding | Fix |
|---|---|
| **HIGH 1** — D-06.1 never asserted: nothing checked `runs-on` or `CODEGRAPH_BENCH_RUNNER`, so a rewrite onto a generic runner with no env var passed every criterion | `TestBenchPublishJobShape` asserts both values **exactly**, on the publish job specifically. `internal/bench/regression.go:72` refuses cross-runner comparison, so this is measurement validity, not cosmetics. |
| **HIGH 2** — D-06.4 checked workflow-wide and **already pre-satisfied**: `rg -c 'if-no-files-found: error' >= 1` is met by the untouched `rebless` upload at `bench.yml:274`, so the publish job's own upload guard could vanish undetected; and `GITHUB_STEP_SUMMARY` had no assertion at all | Both assertions move into the parsed publish-job subtree: exactly one upload step with `name`/`path`/`if-no-files-found` asserted, and exactly one run body containing the step-summary variable. Neither can be satisfied by another job. |
| **HIGH 3** — D-06.3's non-blocking publish-not-gate contract rested on a run-step comment | The shape test inspects **all** run bodies in the publish job and requires the concatenation to be non-empty and to contain no gate invocation. A "non-blocking" comment over a gating body now fails. |
| `06-02:166` — D-06.2 under-enforced: a diff proves this plan added no target, not that none exists | The Taskfile assertion becomes an exact set: the task keys beginning `bench` must equal exactly `{bench:regression}` (which legitimately exists, per D-14 and the in-file exception comment), and the shape test independently forbids any `task bench*` invocation inside the publish job. |
| `06-02:163` — SHA-pin equality is vacuous at `0 == 0` | Replaced with an expected arithmetic over three printed counts, including a non-zero pinned-action floor. This also **fixes a latent false failure**: the old form compared 22 `uses:` lines against 20 SHA matches, because `.github/actions/install-task` is referenced twice by local path and is legitimately unpinnable. |
| `06-02:165` — the `rebless` diff criterion is not mechanically checkable | Replaced by `TestBenchReblessJobShape`, a parsed-subtree fixture (runs-on, env, `if`, ordered step names, upload `with` map, absence of a job-level `permissions` key) transcribed from the pre-plan file. Durable beyond this phase. |
| `06-02:164`, `06-02:215` — inverted `rg -c` gates | Restated per class fix B (see 06-01's `<review_incorporation>` for the full statement of both class fixes), each paired with a positive companion count. |
| Class fix A | The `Taskfile.yml` and `BASELINE.md` guards compare against `06-02-PREPLAN-SHA.txt`, not HEAD. |

**Not applied, and why:** the reviewer's suggestion to compare the `rebless` job's parsed subtree
"before and after" as two hashes was implemented as a **literal fixture** instead. A before/after
hash is live only during the one task that computes it; a fixture is re-checked by CI on every
future change to the file, which is where the risk actually lives. No locked decision was
reopened by any fix above.
</review_incorporation>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the publish-job structural verifier, then the publish job it describes</name>
  <precondition>`tools/bench/runner` accepts `-mode publish` on this branch — `go run ./tools/bench/runner -mode publish -h 2>&1 | rg -q 'publish'` succeeds (06-01 landed). Without it the workflow would reference a mode that does not exist.</precondition>
  <reversibility rating="costly">Several load-bearing measurement properties exist only in the deleted job's body and comments; rebuilding without them silently degrades measurement validity (D-05).</reversibility>
  <files>.github/workflows/bench.yml, internal/upgrade/bench_workflow_shape_test.go, .planning/phases/06-benchmark-de-coupling-memory-sweep/06-02-PREPLAN-SHA.txt</files>
  <read_first>
    - `.github/workflows/bench.yml` lines 1-14 (the two-job header summary), 42-52 (the deliberate runner-profile asymmetry rationale), 63-90 (`workflow_dispatch.inputs` and the weekly schedule), 92-93 (the workflow-level `permissions`), 96-158 (the job being deleted), 160-181 (the no-Taskfile exception comment and the `rebless` job header), 172-312 (the whole `rebless` job — its runs-on, env, `if`, ordered step names and upload `with` map are transcribed into `TestBenchReblessJobShape`'s fixture and must be read from the PRE-EDIT file)
    - `internal/upgrade/taskfile_shape_test.go` lines 1-15 (the `go.yaml.in/yaml/v3` import and the `workflowsDir` constant this new test reuses) and 30-50 (`requiredCheckNames` — the established literal-fixture style to follow)
    - `tools/bench/runner/main.go` as left by 06-01 — confirm the exact `-mode` value and the flags the job must pass
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md` §Pattern 3 (the D-06 property-to-line map) and §Security Domain (the four workflow-edit threats)
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md` §D-05, §D-06
  </read_first>
  <behavior>
    Write `internal/upgrade/bench_workflow_shape_test.go` FIRST and watch it fail before the
    workflow is touched. This is the instrument that closes review HIGH 1, HIGH 2 and HIGH 3, and
    an instrument never observed failing is not evidence. Both tests parse
    `../../.github/workflows/bench.yml` with `go.yaml.in/yaml/v3` — the same dependency and the
    same relative path `taskfile_shape_test.go` already uses.

    `TestBenchPublishJobShape` selects `jobs.publish` and fails loudly if that key is absent: the
    job key is the anchor, and every assertion below is vacuous without it. Against that subtree:
    - `runs-on` equals `namespace-profile-linux-amd64-4x8` exactly (D-06.1).
    - `env.CODEGRAPH_BENCH_RUNNER` equals `namespace-profile-linux-amd64-4x8` exactly (D-06.1).
      Both values are asserted because `CheckRegression` refuses a cross-runner comparison, so a
      publish job on a generic runner emits records that can never be compared.
    - The `if:` expression contains both the scheduled-event term and the dispatch term naming
      this job id, so the weekly schedule and the dispatch UI both reach it.
    - Exactly one step's `run:` body invokes the runner inline via `go run ./tools/bench/runner`,
      and that body contains the publish mode flag (D-06.2).
    - The concatenation of ALL `run:` bodies in this job is NON-EMPTY (the positive assertion —
      a job with no run bodies must fail rather than pass every prohibition vacuously) and
      contains none of: a `task bench` invocation, the gate function name, the regression mode
      flag, the baseline-overwriting flag, `npm `, `npx `, `node `. This is D-06.3's
      publish-is-not-a-gate contract and BENCH-03's no-second-implementation requirement made
      structural rather than left to a comment (review HIGH 3).
    - Exactly one step's `run:` body references the job-summary environment variable (D-06.4,
      review HIGH 2 — previously unasserted anywhere).
    - Exactly one step `uses:` an upload-artifact action, and ITS OWN `with:` map has
      `name: publish-results`, `path: publish-results.json` and `if-no-files-found: error`
      (D-06.4). Scoped to this job, never file-wide: the untouched `rebless` upload at
      `bench.yml:274` already carries that setting and would otherwise satisfy the check on the
      publish job's behalf (review HIGH 2).
    - No step in this job uses an action whose identifier contains a Node-setup action name.
    - The set of action identifiers this job uses equals exactly
      `{actions/checkout, actions/setup-go, namespacelabs/nscloud-cache-action, actions/upload-artifact}`,
      each pinned to a 40-hex SHA. An expected SET, not a format equality — the previous form
      passed when every `uses:` line disappeared (review 06-02:163).
    - The job declares no `permissions:` key of its own, and the workflow-level `permissions` map
      equals exactly `{contents: read}`.
    - `on.workflow_dispatch.inputs.job.default` is `publish` and its `options` list contains
      `publish`, so the identifier family cannot drift apart.

    `TestBenchReblessJobShape` selects `jobs.rebless` and asserts it against a literal fixture
    transcribed from the PRE-EDIT file — this is the parsed-subtree comparison review finding
    `06-02:165` asked for, in a form that keeps checking after this phase ends:
    - `runs-on` is `ubuntu-latest` and `env.CODEGRAPH_BENCH_RUNNER` is `ubuntu-latest` (the
      deliberate asymmetry the file header documents).
    - The `if:` expression matches the fixture verbatim.
    - The ordered sequence of step `name:` values equals the fixture exactly — six steps, in
      order, starting with the checkout and ending with the candidate-survives-the-gate step. A
      sequence comparison catches an inserted, removed or reordered step; a count would not.
    - The upload step's `with:` map is `{name: baseline-candidate, path: tools/bench/baseline.json,
      if-no-files-found: error}`.
    - The job declares no `permissions:` key.
  </behavior>
  <action>
<!-- planner-discipline-allow: headtohead -->
<!-- planner-discipline-allow: ts-binary -->
<!-- planner-discipline-allow: colbymchenry -->
<!-- planner-discipline-allow: setup-node -->
<!-- planner-discipline-allow: npm install -->
<!-- planner-discipline-allow: task bench -->
<!-- planner-discipline-allow: CheckRegression -->
<!-- planner-discipline-allow: -mode regression -->
<!-- planner-discipline-allow: rebless -->
**Record the pre-plan SHA first.** Before any edit, write `git rev-parse HEAD` as the single line
of `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-02-PREPLAN-SHA.txt`. Every tamper
guard in this plan compares against it rather than against HEAD (review class fix A) — a
HEAD-relative guard cannot see a change this plan's own earlier task already committed.

**Then write the structural verifier before the job it verifies.** Author
`internal/upgrade/bench_workflow_shape_test.go` per the `<behavior>` block above, run it, and
paste the verbatim RED output into the SUMMARY. Transcribe `TestBenchReblessJobShape`'s fixture
from the PRE-EDIT workflow file — a fixture copied out of the edited file proves nothing, because
it would agree with whatever the edit happened to produce. Then author the job and re-run to
GREEN. Commit the test and the workflow edit together: this repo's own convention (see 06-01
Task 1) is that a subtractive edit and its paired test land in one commit rather than leaving a
red tree behind.

Delete the `headtohead:` job block (lines 96-158) in full and author a `publish:` job in its place,
per D-05. The four D-06 properties are a checklist, not guidance — each one below must be present
in the new job, and each one is now mechanically asserted against the parsed `jobs.publish`
subtree rather than by a grep over the whole file.

**Deletions (BENCH-03's core requirement — nothing in the job may reach another implementation):**
- the `Set up Node (for the installed comparison binary)` step (`actions/setup-node`, 117-120)
- the `Install the comparison binary @1.3.1` step (`npm install -g @colbymchenry/codegraph@1.3.1`,
  122-123)
- the `-ts-binary "$(command -v codegraph)"` argument on the runner invocation (140)

**Carry forward verbatim (D-06):**
1. `runs-on: namespace-profile-linux-amd64-4x8` and `env: CODEGRAPH_BENCH_RUNNER:
   namespace-profile-linux-amd64-4x8`. `internal/bench/regression.go:53` refuses cross-runner
   comparison, so runner identity is measurement validity, not cosmetics. Also update the
   file-header paragraph at 42-52 that explains why this job stays on the Namespace profile while
   `rebless` sits on `ubuntu-latest`: keep the asymmetry and its reasoning (this job is non-gating
   and self-consistent; Namespace measured ~20.8% faster on the synthetic control), restated
   without naming a second measured binary.
2. Keep the runner invocation inline as `go run ./tools/bench/runner -mode publish -go-binary
   "$(pwd)/codegraph-bench"`. Do NOT wrap it in a Taskfile target. Add a short comment in the new
   job noting that this job's mode has no baseline-overwriting flag at all, so the D-13/D-01
   exception documented above `rebless` (160-171) is inherited context here rather than a live
   risk — and that the exception itself still governs `rebless`.
3. Keep the non-blocking publish-not-gate contract, restated in the run step's comment: this step
   publishes and never gates; the blocking gate is `ci.yml`'s `perf-regression` job running
   `CheckRegression` against the committed `tools/bench/baseline.json`. A slower number must never
   fail this workflow.
4. Keep the `GITHUB_STEP_SUMMARY` block and the `actions/upload-artifact` step with
   `if-no-files-found: error` — the positive assertion that the measurement actually produced
   output (rule `84d1gfpywd`). Rename the artifact and file from the comparison-era name to
   `publish-results` / `publish-results.json`, and retitle the summary heading to name absolute
   per-corpus numbers under BENCH-01/BENCH-03 rather than a head-to-head result under PERF-01.

**Identifier family that must move together (all three, or the dispatch UI offers a dead option):**
- the job's YAML map key: `headtohead:` -> `publish:`
- `workflow_dispatch.inputs.job`'s `default:` and its `options:` list entry (69, 71)
- the job's `if:` conditional (99), which currently tests `inputs.job == 'headtohead'`

Also update the file-header two-job summary (1-14): rename the job in the bullet list, describe it
as publishing absolute Go throughput / query-latency / peak-RSS numbers, and repoint the
"Docs/BENCHMARKS.md ... is refreshed from" sentence at the new job. Keep the `rebless` bullet, the
"never triggered on pull_request/push" paragraph, the hardware-class investigation paragraph
(16-40) and the RSS scope note (54-57) intact.

Change nothing else in the file: the `rebless` job block, the `cpu-diag` / `scratch-fs-compare` /
`disk-control-github` jobs, the `trials` input, the weekly `schedule`, the workflow-level
`permissions: contents: read`, and every `uses:` SHA pin all stay exactly as they are.
  </action>
  <verify>
    <automated>task lint:actions && go test -count=1 ./internal/upgrade/... && go test -count=1 -v -run 'TestBenchPublishJobShape|TestBenchReblessJobShape' ./internal/upgrade/... && test "$(rg -U -o -e '\bheadtohead\b' -e '\bts-binary\b' -e '\bcolbymchenry\b' -e 'setup-node' -e 'npm install' -e '\bcomparator\b' -e '\bcomparison[[:space:]#]+(binary|harness|runner|target|subject)\b' .github/workflows/bench.yml | wc -l | tr -d ' ')" = "0" && echo "BENCH_YML_RETIRED_TERMS_TOTAL=0" && U_TOTAL=$(rg -o '^\s*uses: ' .github/workflows/bench.yml | wc -l | tr -d ' ') && U_LOCAL=$(rg -o '^\s*uses: \./' .github/workflows/bench.yml | wc -l | tr -d ' ') && U_PINNED=$(rg -o 'uses: [^@[:space:]]+@[0-9a-f]{40}' .github/workflows/bench.yml | wc -l | tr -d ' ') && echo "USES_TOTAL=$U_TOTAL USES_LOCAL=$U_LOCAL USES_PINNED=$U_PINNED" && test "$U_PINNED" = "19" && test "$U_LOCAL" = "2" && test "$((U_PINNED + U_LOCAL))" = "$U_TOTAL" && echo "ALL_REMOTE_USES_SHA_PINNED=true" && BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-02-PREPLAN-SHA.txt) && PERMDIFF=$(git diff "$BASE" -- .github/workflows/bench.yml | rg -c '^[-+].*permissions|^[-+].*contents: write' || true) && PERMPRESENT=$(rg -o -F 'contents: read' .github/workflows/bench.yml | wc -l | tr -d ' ') && echo "PERMISSION_DIFF_LINES=$PERMDIFF PERMISSION_LINES_PRESENT=$PERMPRESENT" && test "$PERMDIFF" = "0" && test "$PERMPRESENT" -ge 1 && BENCHTASKS=$(rg -o '^  bench[a-zA-Z0-9:_-]*:' Taskfile.yml | tr -d ' ' | sort | tr '\n' ',') && ALLTASKS=$(rg -o '^  [a-zA-Z][a-zA-Z0-9:_-]*:' Taskfile.yml | wc -l | tr -d ' ') && echo "TASKFILE_BENCH_TARGETS=$BENCHTASKS TASKFILE_TARGETS_TOTAL=$ALLTASKS" && test "$BENCHTASKS" = "bench:regression:," && test "$ALLTASKS" -ge 40</automated>
  </verify>
  <acceptance_criteria>
    - `task lint:actions` (actionlint) exits 0 on the edited workflow.
    - `go test -count=1 ./internal/upgrade/...` passes in full, including `TestWorkflowRunBodiesInvokeTask` (bench.yml stays outside the `inScopeJobs` fixture and no exception entry went stale) **and** the two new shape tests. `go test -v -run 'TestBenchPublishJobShape|TestBenchReblessJobShape'` names both as RUN/PASS — the named-test assertion exists so a deleted or renamed test cannot pass as a green package.
    - **D-06.1 (review HIGH 1) — asserted, not assumed:** `TestBenchPublishJobShape` proves the publish job's `runs-on` and its `CODEGRAPH_BENCH_RUNNER` env value are both exactly `namespace-profile-linux-amd64-4x8`. A rewrite onto a generic runner, or one that drops the env var, fails this test by name.
    - **D-06.4 (review HIGH 2) — publish-job-scoped, not workflow-wide:** the same test proves the publish job has exactly one upload step whose OWN `with:` map carries `name: publish-results`, `path: publish-results.json` and `if-no-files-found: error`, and exactly one run body referencing the job-summary variable. Neither assertion can be satisfied by the untouched `rebless` upload at `bench.yml:274`, which is what made the previous file-wide `rg -c` form pre-satisfied.
    - **D-06.3 (review HIGH 3) — structural, not prose:** the same test proves the concatenation of every run body in the publish job is non-empty and contains no gate invocation, no regression-mode flag, no baseline-overwriting flag, and no `task bench` call. A job that gates while keeping a "non-blocking" comment fails.
    - **D-06.2 (review 06-02:166) — the Taskfile is asserted as an exact set, not as a diff:** `TASKFILE_BENCH_TARGETS` equals exactly `bench:regression:,` — the one target D-14 sanctions — and `TASKFILE_TARGETS_TOTAL` is >= 40, the positive companion proving the instrument still sees the file's targets at all (rule `84d1gfpywd`). This holds whether or not this plan edited the file, which a diff could not establish.
    - `rg -o 'publish:' .github/workflows/bench.yml` finds the job key, and the shape test independently asserts the dispatch `default:` is `publish` and its `options:` list contains `publish`. Both are positive assertions; neither is inferred from the absence of the old name.
    - Retired-term census over the workflow prints `BENCH_YML_RETIRED_TERMS_TOTAL=0`, counted with `rg -o | wc -l` (never `rg -c`, which counts lines).
    - Action pinning is asserted as arithmetic over three printed counts rather than as an equality that passes at zero (review 06-02:163): `USES_PINNED=19`, `USES_LOCAL=2`, and `USES_PINNED + USES_LOCAL == USES_TOTAL`. The expected 19 is derived: the pre-edit file has 22 `uses:` lines, of which 2 are local composite-action paths (`./.github/actions/install-task`, legitimately unpinnable) and 20 are SHA-pinned; this edit deletes exactly one pinned action (the Node setup step), leaving 19. If the executor's count differs, that is a real finding to report — not a number to adjust. This closes T-06-04.
    - The permission posture is asserted two-sided and pre-plan-SHA relative (class fixes A and B): `PERMISSION_DIFF_LINES=0` against `$BASE` **and** `PERMISSION_LINES_PRESENT >= 1`, so the check cannot pass by the permissions block having been deleted outright. The shape test additionally asserts the workflow-level map is exactly `{contents: read}` and that neither job declares its own. This closes T-06-03.
    - **The `rebless` job is proven untouched structurally (review 06-02:165), not by grepping diff text:** `TestBenchReblessJobShape` compares the parsed subtree — runs-on, env, `if`, the ordered six-step name sequence, the upload `with:` map, and the absence of a job-level `permissions` key — against a fixture transcribed from the pre-edit file. The previous criterion ("no matches beyond comment-prose lines listed in the SUMMARY") was not mechanically checkable and is withdrawn.
  </acceptance_criteria>
  <done>`bench.yml` has a `publish` job that builds and measures only the Go binary and carries all four D-06 properties — each one proven by a Go test that parses the `jobs.publish` subtree and was observed RED before the job existed — is reachable from the dispatch UI and the weekly schedule, and leaves `rebless` (asserted as a parsed-subtree fixture), the workflow permissions and every action pin untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the benchmark investigation record and the workflow-exception comment</name>
  <files>tools/bench/BASELINE.md, internal/upgrade/taskfile_shape_test.go</files>
  <read_first>
    - `tools/bench/BASELINE.md` in full — specifically line 64 (the real-corpora captures sentence), line 241 (the job-name reference), and the guard-rationale sections at 39, 87, 97, 120 that must survive
    - `internal/upgrade/taskfile_shape_test.go` lines 96-112 (the `inScopeJobs` doc comment naming bench.yml's exempt jobs, and the fixture itself)
    - `.github/workflows/bench.yml` as left by Task 1 — the job id the prose must now name
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md` §D-03
  </read_first>
  <action>
<!-- planner-discipline-allow: headtohead -->
<!-- planner-discipline-allow: head-to-head -->
Per D-03, `tools/bench/BASELINE.md` is live engineering documentation, not an archive: its guard
rationale — why `CheckRegression` refuses a comparison across platform, runner class or
`scratch_fs` — is load-bearing for BENCH-02 and stays. Sweep only the framing.

`tools/bench/BASELINE.md`:
- Line 64's "On real corpora the head-to-head captures in this ..." sentence cites capture files
  06-01 deleted. Re-author it to state the real-corpora observation on its own terms and drop the
  dangling file reference, or drop the clause if the observation depended entirely on a second
  subject. State which of the two you did in the SUMMARY.
- Line 241's job-name reference is repointed at the `publish` job id Task 1 created.
- Every other use of "comparison" in this file describes `CheckRegression` comparing a baseline
  record against a current record. That is the ordinary English sense and is CORRECT — leave it.
  This is precisely the bounded-pattern discipline D-04 demands: an unbounded `comparison` pattern
  here would flag legitimate engineering prose, and the fix is a tighter pattern, never a reworded
  sentence.
- Change no measured figure, no percentage, no date and no section heading in this file.

`internal/upgrade/taskfile_shape_test.go`:
- The `inScopeJobs` doc comment (around line 102) names bench.yml's exempt jobs by their old ids.
  Update that list to name the current job ids. This is a comment-only edit: do not add, remove or
  reorder any entry in the `inScopeJobs` or `runBodyExceptions` fixtures — widening either is the
  exact failure mode that fixture's own doc comment warns about.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./internal/upgrade/... && test "$(rg -U -o -e '\bhead-to-head\b' -e '\bheadtohead\b' -e '\bcolbymchenry\b' -e '\bcodegraph-ts\b' -e '\bcomparator\b' tools/bench/BASELINE.md internal/upgrade/taskfile_shape_test.go | wc -l | tr -d ' ')" = "0" && echo "BASELINE_AND_SHAPETEST_RETIRED_TERMS_TOTAL=0" && test "$(git diff -U0 -- internal/upgrade/taskfile_shape_test.go | rg -c '^[+-]\s*\{Workflow:' || true)" = "0" && echo "SHAPETEST_FIXTURE_UNCHANGED=true"</automated>
  </verify>
  <acceptance_criteria>
    - `go test -count=1 ./internal/upgrade/...` passes, including `TestWorkflowRunBodiesInvokeTask`.
    - Retired-term census over the two files prints `BASELINE_AND_SHAPETEST_RETIRED_TERMS_TOTAL=0`, counted with `rg -o | wc -l`.
    - `rg -c '\bcomparison\b' tools/bench/BASELINE.md` is still >= 5 — the legitimate `CheckRegression` uses of the word were NOT swept. This is a positive assertion that the sweep was bounded rather than a find-and-replace.
    - `rg -o 'publish' tools/bench/BASELINE.md | wc -l` is >= 1 — the job-name reference was repointed, not merely deleted.
    - The `inScopeJobs` and `runBodyExceptions` fixture literals are unchanged, stated per class fix B and paired with a positive companion so the check cannot pass against a gutted fixture: `FIXDIFF=$(git diff -U0 -- internal/upgrade/taskfile_shape_test.go | rg -c '^[+-]\s*\{Workflow:' || true); FIXPRESENT=$(rg -o '^\s*\{Workflow:' internal/upgrade/taskfile_shape_test.go | wc -l | tr -d ' '); echo "SHAPETEST_FIXTURE_DIFF_LINES=$FIXDIFF SHAPETEST_FIXTURE_ENTRIES=$FIXPRESENT"; test "$FIXDIFF" = "0" && test "$FIXPRESENT" -ge 10` (`SHAPETEST_FIXTURE_UNCHANGED=true`). This one is deliberately HEAD-relative: it asserts that THIS task's own uncommitted edit is comment-only, which is exactly what HEAD compares.
    - No measured figure changed, restated per class fixes A and B (the previous form was a HEAD-relative inverted `rg -c` gate that also passed if every figure was deleted):
      `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-02-PREPLAN-SHA.txt); FIGDIFF=$(git diff "$BASE" -- tools/bench/BASELINE.md | rg -c '^[-+].*[0-9]+\.[0-9]+%|^[-+].*files/s' || true); FIGPRESENT=$(rg -o -e '[0-9]+\.[0-9]+%' -e 'files/s' tools/bench/BASELINE.md | wc -l | tr -d ' '); echo "BASELINE_FIGURE_DIFF_LINES=$FIGDIFF BASELINE_FIGURES_PRESENT=$FIGPRESENT"; test "$FIGDIFF" = "0" && test "$FIGPRESENT" -ge 5`
  </acceptance_criteria>
  <done>`tools/bench/BASELINE.md` reads on this project's own terms with every guard rationale, measured figure and investigation finding intact, the workflow-exception comment names the current job ids, and the legitimate uses of "comparison" survive as proof the sweep was bounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> third-party actions | every `uses:` executes third-party code inside the job |
| workflow permissions -> repository contents | `rebless` sits in the same file and deliberately holds no write token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Elevation of Privilege | `.github/workflows/bench.yml` `rebless` job | high | mitigate | Task 1 asserts, against the recorded pre-plan SHA, that the diff added or removed no `permissions:` / `contents: write` line AND that a `contents: read` line is still present; `TestBenchReblessJobShape` asserts `rebless`'s parsed subtree (runs-on, env, `if`, ordered step names, upload `with:` map, no job-level `permissions` key) against a pre-edit fixture |
| T-06-04 | Tampering | third-party action pins in `bench.yml` | high | mitigate | Task 1 asserts an expected arithmetic over three printed counts (`USES_PINNED=19`, `USES_LOCAL=2`, sum equals `USES_TOTAL`), which cannot pass at zero; `TestBenchPublishJobShape` additionally asserts the publish job's action identifier SET exactly |
| T-06-22 | Tampering | the publish job's own D-06 properties | high | mitigate | All four properties asserted against the parsed `jobs.publish` subtree by `TestBenchPublishJobShape`, never by a file-wide grep another job can pre-satisfy (review HIGH 1/2/3); the test was observed RED before the job existed |
| T-06-09 | Tampering | supply chain reachable from the workflow | high | mitigate | The global npm install of an external indexer is deleted from the job; after this plan the workflow installs no package-manager dependency at all, asserted by the retired-term census (`npm install` count 0) |
| T-06-08 | Repudiation | the bounded framing census | medium | mitigate | Task 2 asserts the legitimate `comparison` uses SURVIVE (count >= 5), so a green census cannot have been bought with a find-and-replace |
</threat_model>

<verification>
- `task lint:actions` green; `go test -count=1 ./internal/upgrade/...` green, with `TestBenchPublishJobShape` and `TestBenchReblessJobShape` named RUN/PASS.
- All four D-06 properties asserted against the parsed `jobs.publish` subtree: exact `runs-on`, exact `CODEGRAPH_BENCH_RUNNER`, one inline publish-mode runner invocation, no gate invocation in any run body, one step-summary reference, and one upload step with its own `if-no-files-found: error`.
- `publish` job key, matching `if:` conditional and matching dispatch choice all present.
- Retired-term census over `bench.yml`, `BASELINE.md` and `taskfile_shape_test.go` prints verbatim zeros.
- Action pinning proven by arithmetic (`USES_PINNED=19`, `USES_LOCAL=2`, sum equals total); permission posture proven two-sided against the pre-plan SHA; `rebless` proven untouched by parsed-subtree fixture.
- The Taskfile's `bench` target set equals exactly `{bench:regression}`, with a >= 40 total-target companion count.
- Legitimate `comparison` prose in `BASELINE.md` survives (count >= 5), and no measured figure changed (diff-zero plus a figures-present floor).
</verification>

<success_criteria>
BENCH-03 is structurally satisfied: a `bench.yml` dispatch of the `publish` job builds only the Go
binary, measures it over the pinned corpus, writes a job summary and uploads a raw-results
artifact under a positive `if-no-files-found: error` assertion, with no step anywhere in the job
installing, resolving or executing another implementation — every one of those properties asserted
against the parsed `jobs.publish` subtree rather than by a file-wide grep or a comment. The live
dispatch itself is BENCH-03's remaining half; 06-04 makes it a blocking decision gate rather than
a conditional check, so BENCH-03 cannot close without either a successful run or an explicitly
recorded pending status.
</success_criteria>

<output>
Create `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-02-SUMMARY.md` when done.
Record the verbatim census totals, the surviving-`comparison` count, and — for `BASELINE.md`
line 64 — whether the sentence was re-authored or dropped, and why. Also record the verbatim RED
output of `TestBenchPublishJobShape` taken before the publish job existed, the three `uses:`
counts, and the `TestBenchReblessJobShape` fixture's source (the pre-edit file, at the recorded
pre-plan SHA).
</output>
