---
phase: 06-benchmark-de-coupling-memory-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/bench/metrics.go
  - internal/bench/regression.go
  - internal/bench/regression_test.go
  - internal/bench/rss.go
  - internal/bench/baseline_gate_test.go
  - internal/corpora/manifest.go
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt
autonomous: true
requirements: [BENCH-02]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`internal/bench.CheckRegression` against the committed `tools/bench/baseline.json` still fails a real throughput regression and a real peak-RSS regression, demonstrated RED against a confirmed-applied mutation and not asserted from reading the gate (BENCH-02)."
    - "The committed `tools/bench/baseline.json` PARTICIPATES in that proof: a test loads that exact file into `bench.Metrics` and passes it to `CheckRegression` as the baseline argument, so the requirement's named mechanism is exercised rather than a same-shaped in-memory stand-in (ROADMAP success criterion 2, review HIGH 4)."
    - "Every mutation used for that demonstration was reverted byte-cleanly, proven by an empty `git diff --stat` and a green re-run of the same command; the phase's commits carry no mutation byte."
    - "The `internal/bench` package doc comments describe absolute, externally-observed measurement on their own terms and cite no deleted capture file and no second runtime."
    - "`internal/corpora`'s package doc and `Manifest.Note` cross-references to `tools/bench/realcorpus` remain accurate after that package's prose was re-authored."
    - "No behaviour, tolerance constant, guard, error string, test case value or json tag in `internal/bench` changed as a net result of this plan."
  artifacts:
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md — the FIXT-07-shaped rehearsal record, one block per mutation family"
    - "internal/bench/{metrics,regression,regression_test,rss}.go — comment-only sweep"
    - "internal/bench/baseline_gate_test.go — TestCheckRegressionAgainstCommittedBaseline: loads the committed tools/bench/baseline.json and drives the gate with it (review HIGH 4)"
    - "internal/corpora/manifest.go — cross-reference verified or corrected"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt — the pre-plan commit every tamper guard in this plan compares against (review class fix A)"
  key_links:
    - "internal/bench/regression.go's tolerance constants -> internal/bench/regression_test.go's named subtests: the mutation demonstrates the link is live, not vacuous"
    - "tools/bench/baseline.json -> internal/bench/baseline_gate_test.go -> CheckRegression: the committed data file is an INPUT to the proof, not merely an untouched bystander; the same mutation that reddens the table subtests must redden this test too"
    - "internal/corpora/manifest.go <-> tools/bench/realcorpus/manifest.go: a bidirectional prose cross-reference; 06-01 rewrote one half, this plan keeps the other accurate"
    - "corpora/manifest.json's top-level note field is committed DATA, so a stale pointer ships in the repo, not just in source comments"
  prohibitions:
    - "MUST NOT delete, weaken, reorder or add a case to `internal/bench/regression_test.go`'s table — the rehearsal drives the EXISTING suite; editing the oracle to produce the RED would certify its own result. Task 2's `internal/bench/baseline_gate_test.go` is a SEPARATE new file and is not an exception to this: it adds an oracle, it does not modify one."
    - "MUST NOT change any tolerance constant, guard branch, error message or json tag in `internal/bench` as a net result of this plan; the only permitted change to those lines is a mutation reverted within the same task."
    - "MUST NOT record a mutation family as demonstrated RED unless the observed failure names the tolerance being violated — a platform, runner or scratch-filesystem mismatch error proves nothing about the numeric band and must be reported as a failed rehearsal, not a passed one."
    - "MUST NOT modify `tools/bench/baseline.json` in any way, including transiently."
---

<objective>
Sweep the comparison framing out of `internal/bench`'s documentation without touching a byte of its
behaviour, then prove the regression gate still fires by demonstrating it RED against a
confirmed-applied, byte-cleanly-reverted mutation.

Purpose: BENCH-02 has two halves. 06-01 removes the comparison runner; this plan supplies the
other half — evidence, not assertion, that `CheckRegression` against the committed baseline still
catches a real regression. D-08 reuses Phase 3's FIXT-07 protocol verbatim rather than inventing a
new one, because that protocol's failure modes are already known in this repo. Per the cross-AI
review's HIGH 4, the committed `tools/bench/baseline.json` must PARTICIPATE in that proof rather
than merely be asserted untouched: ROADMAP success criterion 2 names it, and until this plan no Go
test loaded it.

Output: comment-only edits across four `internal/bench` files and one `internal/corpora` file, a
new `internal/bench/baseline_gate_test.go` that drives the gate with the committed baseline, and a
committed `06-MUTATION-LOG.md` recording each mutation, its confirmed application, its verbatim RED
output from BOTH oracles, its revert and its byte-clean proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PATTERNS.md
@.planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md
</context>

<artifacts_this_phase_produces>
See `06-01-PLAN.md` → "Artifacts this phase produces" for the full created/deleted register.
This plan CREATES `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md`,
`.planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt`, and
`internal/bench/baseline_gate_test.go` with `TestCheckRegressionAgainstCommittedBaseline`
(added in response to review HIGH 4). It deletes no symbol.
</artifacts_this_phase_produces>

<edge_probe_coverage>
Spec-less probe fallback (no SPEC.md for this phase). Row owned by this plan:

| Requirement | Category | Status | Disposition |
|---|---|---|---|
| BENCH-02 | unclassified | **unresolved** | **Flagged assumption — not auto-resolved.** Assumption carried by this plan: "still fires on a real regression" is demonstrated at the pure-function level, by mutating a tolerance constant and observing the pre-existing named subtests go RED. A full CI dispatch of `ci.yml`'s `perf-regression` job against a mutated live measurement is deliberately NOT the chosen oracle — `06-RESEARCH.md` Pitfall 2 shows that path fails with a platform/runner mismatch category error off the pinned runner, which would look like a firing gate while proving nothing. If a reviewer requires an end-to-end CI demonstration instead, this assumption is where to correct the plan. |

**Known adjacent gap, deliberately not closed here:** open issue #16 records that `CheckRegression`
never compares `Metrics.Repo` (corpus identity). That is a pre-existing gap, out of scope under
CONTEXT.md's "no change to `CheckRegression`'s gate semantics". This plan's rehearsal does not
close it and must not be read as having done so.
</edge_probe_coverage>

<review_incorporation>
## Cross-AI review findings incorporated (`06-REVIEWS.md`)

**HIGH 4 — the committed `baseline.json` never participated in the proof.** ROADMAP success
criterion 2 names the mechanism explicitly: "`internal/bench.CheckRegression` against the
**committed** `baseline.json`". The rehearsal proved the tolerance branches are live, but it drove
them with in-memory `Metrics` values; the reviewer verified by search that **no Go test loads
`tools/bench/baseline.json`** — the only test references write their own baseline into a temp
directory. Asserting the file was untouched is not the same as asserting it works.

Closed by a new **Task 2**, inserted between the comment sweep and the rehearsal:
`internal/bench/baseline_gate_test.go` loads the committed file, unmarshals it into
`bench.Metrics`, and drives `CheckRegression` with it. Ordering matters — the test lands BEFORE
the mutation rehearsal on purpose, so Task 3's mutations must redden **both** the pre-existing
table subtests and the new committed-baseline test. That makes the new test's own non-vacuity part
of the same demonstration rather than a separate claim.

| Finding | Fix |
|---|---|
| **HIGH 4** — the committed baseline is asserted untouched, never loaded | New Task 2 + `TestCheckRegressionAgainstCommittedBaseline`; Task 3's rehearsal now requires a RED from that test too. |
| `06-03:229` — oracle integrity is diff-relative and passes after an unintended prior commit | Task 3 asserts the two critical cases **by name and by value** (the 11%-slower throughput case and the 16%-larger peak-RSS case, each with its expected-error hint), plus a pre-plan-SHA-relative diff of the test file (class fix A). Both, not either. |
| `06-03:158` — "comment-only" allows wholesale comment DELETION: every changed line starting with `//` passes even if all semantic comments were replaced | Task 1 pairs the shape check with a comment-line floor: the count of `//` lines in each swept file after the edit must be within a stated band of the count before it, captured pre-edit. A sweep that deletes the guard rationale fails. |
| `06-03:158`, `06-03:228` — inverted `rg -c` gates | Restated per class fix B (full statement in 06-01's `<review_incorporation>`), each with a positive companion count. |
| Class fix A | The `baseline.json`, `internal/bench/` and `regression_test.go` guards compare against `06-03-PREPLAN-SHA.txt`. |

**Locked-decision check:** none of the above reopens a decision. D-08's FIXT-07 protocol is kept
verbatim; the committed-baseline test is **additive** and does not replace the rehearsal, exactly
as the reviewer recommended. CONTEXT.md's "no change to `CheckRegression`'s gate semantics and no
modification of `tools/bench/baseline.json`" is preserved: the new test only READS the file.
</review_incorporation>

<tasks>

<task type="auto">
  <name>Task 1: Comment-only framing sweep across internal/bench and internal/corpora</name>
  <files>internal/bench/metrics.go, internal/bench/regression.go, internal/bench/regression_test.go, internal/bench/rss.go, internal/corpora/manifest.go, .planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt</files>
  <read_first>
    - `internal/bench/rss.go` lines 1-10 — the package doc whose peak-RSS justification names a second runtime (`.planning/WINDOWS.md` ledger entry 16 flags this line and defers it here by design)
    - `internal/bench/metrics.go` lines 1-71 — the package/struct doc at 6-7, the `Subject` field comment at 9, and the participation notes at 26, 34, 46
    - `internal/bench/regression.go` lines 1-45 — the package-level rationale and the line-39 citation of the capture files 06-01 deletes
    - `internal/bench/regression_test.go` lines 110-130 — the comment quoting multipliers derived from those captures
    - `internal/corpora/manifest.go` lines 1-25 (package doc, incl. the recorded four-reason non-merge decision at 6-15) and 85-105 (the two `Note` field declarations)
    - `corpora/manifest.json` line 2 — the committed top-level `note` value that ships in the repo
    - `tools/bench/realcorpus/manifest.go` on `main` (and, once 06-01 lands, as it leaves it) — the text the cross-references must stay accurate against
  </read_first>
  <action>
**Record two baselines before the first edit.** Neither is reconstructible afterwards.

1. Write `git rev-parse HEAD` as the single line of
   `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt` — the anchor
   every tamper guard in this plan compares against (review class fix A).
2. Capture the per-file comment-line count over the four files this task sweeps:
   `for f in internal/bench/metrics.go internal/bench/regression.go internal/bench/regression_test.go internal/bench/rss.go; do echo "$f=$(rg -c '^\s*//' "$f" || true)"; done > "${TMPDIR:-/tmp}/06-03-comments-before.txt"`.
   The "every changed line is a comment" shape check cannot distinguish a re-authored comment from
   a deleted one — a sweep that removed every semantic comment would satisfy it (review finding
   06-03:158). The before/after count is what closes that.

Then: comment-only sweep. Change NO string literal, no numeric literal, no error message, no json
tag, no struct field, no test table value and no function signature. Every edit in this task lives
inside a `//` comment.

`internal/bench/rss.go` (package doc, 1-10): the peak-RSS sentence currently justifies OS-level
measurement by contrast with another runtime's process. Re-author it to state the rule on its own
terms — an externally observed child-process `rusage` figure is the only peak-RSS number that stays
comparable across runs and machines, because an in-process Go runtime statistic measures the harness
rather than the subject. Keep the "measurement helpers never panic — they return (T, error)"
paragraph and the no-network/no-crypto note verbatim.

`internal/bench/metrics.go`:
- The package/struct doc at 6-7 names two consumers, one of which no longer exists under that name.
  Re-author it to name the committed-baseline regression gate and the absolute-numbers publisher.
- The `Subject` field comment at 9 gives two example values, one of which is retired. Reduce the
  example to the surviving value and state that `Subject` is a provenance label, not part of a
  measurement's identity. **Do not remove or rename the field or its json tag** — `subject` is
  present in the committed `tools/bench/baseline.json` and the struct is shared with the gate.
- Leave the participation notes at 26, 34 and 46 alone: they use "comparison" in its ordinary sense
  about the gate comparing two records, which is correct.

`internal/bench/regression.go` line 39: this comment cites a capture-file glob that 06-01 deletes —
a dangling reference. Re-author the surrounding rationale so the hardware-class point stands on the
evidence that survives (`tools/bench/BASELINE.md`'s recorded investigation and the synthetic-corpus
control numbers already quoted in this file), citing no deleted path and no cross-implementation
multiplier. Change nothing else in this file — in particular no constant, no guard, no error string.

`internal/bench/regression_test.go` around line 120: this comment quotes throughput multipliers
derived from the deleted captures. Re-author it to make the same hardware-class-sensitivity point
from the surviving evidence, with no cross-implementation multiplier. Do not touch any test case,
table entry, name string or expected value.

`internal/corpora/manifest.go`: per D-10 and `06-RESEARCH.md` Pitfall 3 this is a **verification
pass**, not a rewrite. Read the package doc's non-merge paragraph (6-15) and both `Note` field
declarations against `tools/bench/realcorpus/manifest.go` and against `corpora/manifest.json`'s
committed top-level `note`. Confirm every cross-reference still names a real package, path and
reason. Edit ONLY a clause that has become inaccurate, and record in the SUMMARY either the exact
clause changed or an explicit statement that all cross-references were verified accurate and left
unchanged. Do not restate, merge or parameterise the two-manifest split — it is a recorded Phase 1
decision (`01-04-PLAN.md`) and this phase touches text only. If `corpora/manifest.json`'s `note`
needs a change, say so in the SUMMARY: it is committed data, so the edit ships in the repo.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./internal/bench/... ./internal/corpora/... && N=$(rg -U -o -e '\bhead-to-head\b' -e '\bheadtohead\b' -e '\bGo-vs-TS\b' -e '\bcolbymchenry\b' -e '\bcomparator\b' -e '\bTS\b' internal/bench/ internal/corpora/manifest.go corpora/manifest.json | wc -l | tr -d ' '); echo "BENCH_PKG_RETIRED_TERMS_TOTAL=$N"; test "$N" = "0" && BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt) && C=$(git diff -U0 "$BASE" -- internal/bench/ | rg -c '^[+-][^+-]' || true); NC=$(git diff -U0 "$BASE" -- internal/bench/ | rg '^[+-][^+-]' | rg -cv '^[+-]\s*//' || true); echo "DIFF_CONTENT_LINES=$C DIFF_NON_COMMENT_LINES=$NC"; test "$NC" = "0" && test "$C" -gt 0</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go vet ./... && go test -count=1 ./internal/bench/... ./internal/corpora/...` green.
    - Subtest count unchanged: `go test -count=1 ./internal/bench/... -run TestCheckRegression -v | rg -c '^=== RUN   TestCheckRegression/'` prints the same number before and after the edit. Record BOTH numbers verbatim in the SUMMARY.
    - Retired-term census prints `BENCH_PKG_RETIRED_TERMS_TOTAL=0`, counted with `rg -o | wc -l`. `rg -c` is not used — it counts matching lines, not matches.
    - Comment-only proof, restated per class fix B (the previous `rg -cv` form was an inverted gate: it exits 1 and prints nothing on the good path) and paired with the positive companion that there WAS a diff to check:
      `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt); CONTENT=$(git diff -U0 "$BASE" -- internal/bench/ | rg -c '^[+-][^+-]' || true); NONCOMMENT=$(git diff -U0 "$BASE" -- internal/bench/ | rg '^[+-][^+-]' | rg -cv '^[+-]\s*//' || true); echo "DIFF_CONTENT_LINES=$CONTENT DIFF_NON_COMMENT_LINES=$NONCOMMENT"; test "$NONCOMMENT" = "0" && test "$CONTENT" -gt "0"`
    - Comments were RE-AUTHORED, not deleted (review finding 06-03:158 — the shape check alone passes if every semantic comment is removed, because a removed `//` line still starts with `//`). For each of the four swept files, the post-edit `rg -c '^\s*//'` count must be within ±25% of the count captured before the first edit, and no file may drop below 90% of its pre-edit count. Print both numbers per file and record the pairs verbatim in the SUMMARY. A file that lost a quarter of its comment lines has lost the guard rationale this sweep exists to preserve, and that is a failure, not a tidy-up.
    - No tolerance constant moved, restated per class fix B with a positive companion asserting both constants are still declared with their committed values:
      `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt); TOLDIFF=$(git diff "$BASE" -- internal/bench/regression.go | rg -c '^[-+].*(DefaultThroughputTolerance|DefaultRSSTolerance|= 0\.)' || true); TOLPRESENT=$(rg -o -e 'DefaultThroughputTolerance = 0\.10' -e 'DefaultRSSTolerance = 0\.15' internal/bench/regression.go | wc -l | tr -d ' '); echo "TOLERANCE_DIFF_LINES=$TOLDIFF TOLERANCE_CONSTANTS_PRESENT=$TOLPRESENT"; test "$TOLDIFF" = "0" && test "$TOLPRESENT" = "2"`
    - `rg -c '\bcomparison\b' internal/bench/regression.go` is still >= 5 — the ordinary-sense uses about the gate comparing two records survive, proving the sweep was bounded rather than a find-and-replace.
    - `rg -o '"subject"' internal/bench/metrics.go | wc -l` is >= 1 — the json tag survives.
    - The committed baseline is untouched AND present, pre-plan-SHA relative per class fix A:
      `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt); git diff --quiet "$BASE" -- tools/bench/baseline.json && test -f tools/bench/baseline.json && test "$(rg -o -F '"files_per_sec"' tools/bench/baseline.json | wc -l | tr -d ' ')" = "1" && echo "BASELINE_UNCHANGED_SINCE=$BASE BASELINE_PRESENT=true"`
    - The SUMMARY states, for `internal/corpora`, either the exact clause changed or that all cross-references were verified accurate and left unchanged. "Looks fine" is not accepted.
  </acceptance_criteria>
  <done>Four `internal/bench` files and `internal/corpora/manifest.go` carry no retired framing and no dangling reference to a deleted path, the diff is provably comment-only, every tolerance constant and test case is byte-identical, and the surviving ordinary-sense uses of "comparison" prove the sweep was bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive the gate with the COMMITTED baseline.json, not an in-memory stand-in</name>
  <precondition>Task 1's comment sweep is committed and `tools/bench/baseline.json` is readable from the module root — `test -f tools/bench/baseline.json` succeeds. The committed file is this task's input, not its subject: nothing here writes to it.</precondition>
  <files>internal/bench/baseline_gate_test.go</files>
  <read_first>
    - `tools/bench/baseline.json` in full — the twelve committed fields, in particular `goos`, `goarch`, `runner`, `scratch_fs` (the four category guards read them) and `files_per_sec` / `peak_rss_bytes` (the two numeric bands)
    - `internal/bench/regression.go` lines 36-135 — the guard ORDER: platform, runner, scratch-filesystem and degenerate-baseline checks all fire BEFORE either numeric band, so a derived "current" record that differs in any of those four fields produces a category error rather than a tolerance failure
    - `internal/bench/metrics.go` — the json tags the committed file must unmarshal into
    - `internal/bench/regression_test.go` lines 8-80 — the existing table's construction style; this new file follows it rather than inventing a second idiom
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md` §Pitfall 2 — what a wrong-RED looks like and why naming the violated tolerance is the whole point
  </read_first>
  <behavior>
    Write `internal/bench/baseline_gate_test.go` as `TestCheckRegressionAgainstCommittedBaseline`,
    a table test whose baseline argument comes from the committed file on disk. It reads
    `../../tools/bench/baseline.json` (the same relative-path idiom `internal/upgrade`'s workflow
    guards already use), unmarshals it into `bench.Metrics`, and:

    - **Loads positively first.** Before any case runs, assert the loaded record is real: `Repo`
      is non-empty, `FilesPerSec > 0`, `PeakRSSBytes > 0`, `GOOS` and `GOARCH` are non-empty. A
      test that silently unmarshalled an empty object would otherwise "pass" every case below for
      the wrong reason (rule `84d1gfpywd`). Fail with the file path named if the file is missing —
      never skip. A skip here would make the whole proof disappear on the day the path moves.
    - **Control case — same frame, no regression.** A current record identical to the loaded
      baseline returns `nil`. This proves the four category guards are NOT firing, which is what
      makes the two failing cases below meaningful rather than accidental.
    - **Throughput case.** A current record copying the loaded baseline's `Repo`, `Subject`,
      `GOOS`, `GOARCH`, `Runner` and `ScratchFS` verbatim, with `FilesPerSec` set to 89% of the
      loaded value (an 11% regression, past the 10% band) and `PeakRSSBytes` unchanged, returns a
      non-nil error whose message names the throughput regression. Assert on the message content,
      not merely on non-nil: a platform / runner / scratch-filesystem mismatch is also non-nil and
      would prove nothing about the numeric band (`06-RESEARCH.md` Pitfall 2).
    - **Peak-RSS case.** The same frame, with `PeakRSSBytes` set to 116% of the loaded value (past
      the 15% band) and `FilesPerSec` unchanged, returns a non-nil error naming the peak-RSS
      growth.
    - **Explicit non-assertion.** Do not assert any absolute value from the committed file
      (17090.87… files/s, 845950976 bytes, and so on). The percentages are derived from whatever
      the file holds, so a legitimate future re-bless changes the numbers without breaking this
      test. Hard-coding them would convert a live proof into a maintenance trap.

    The percentages deliberately mirror the two existing table cases so that Task 3's mutations
    redden this test and the pre-existing subtests together.
  </behavior>
  <action>
Author `internal/bench/baseline_gate_test.go` per the `<behavior>` block. This closes the review's
HIGH 4: ROADMAP success criterion 2 names `CheckRegression` **against the committed
`baseline.json`**, and until now nothing loaded that file — the plan asserted only that it was not
touched, which is a different claim.

Read-only with respect to the committed data. This task adds one test file and changes nothing
else: no tolerance constant, no guard, no error string, no json tag, no existing test case, and
not one byte of `tools/bench/baseline.json`.

Derive the "current" record from the loaded baseline rather than writing a literal — copying the
four frame fields (`GOOS`, `GOARCH`, `Runner`, `ScratchFS`) verbatim is what keeps the category
guards quiet so the numeric bands are the thing actually under test. A literal current record
would drift out of frame the first time the baseline is re-blessed on a different runner class,
and would then fail with a mismatch error that looks like a firing gate while proving nothing.

If the loaded baseline ever fails the positive-load assertions, stop and surface it: that is a
finding about the committed file, not a reason to relax the test.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && test -f internal/bench/baseline_gate_test.go && go test -count=1 -v -run TestCheckRegressionAgainstCommittedBaseline ./internal/bench/... 2>&1 | tee "${TMPDIR:-/tmp}/06-03-baseline-gate.txt" | rg -q '^--- PASS: TestCheckRegressionAgainstCommittedBaseline' && SUBS=$(rg -c '^    --- PASS: TestCheckRegressionAgainstCommittedBaseline/' "${TMPDIR:-/tmp}/06-03-baseline-gate.txt" || true) && echo "COMMITTED_BASELINE_SUBTESTS_PASSED=$SUBS" && test "$SUBS" -ge 3 && test "$(rg -o -F 'tools/bench/baseline.json' internal/bench/baseline_gate_test.go | wc -l | tr -d ' ')" -ge 1 && echo "COMMITTED_BASELINE_PATH_REFERENCED=true" && go test -count=1 ./internal/bench/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/bench/baseline_gate_test.go` exists and references the committed path `tools/bench/baseline.json` (`COMMITTED_BASELINE_PATH_REFERENCED=true`). This is the criterion the review's HIGH 4 turns on: before this task, no Go test in the repository loaded that file, and the only test references wrote their own baseline into a temp directory.
    - `go test -v -run TestCheckRegressionAgainstCommittedBaseline ./internal/bench/...` passes with at least three named subtests (`COMMITTED_BASELINE_SUBTESTS_PASSED >= 3`): the no-regression control, the throughput case and the peak-RSS case. The count is printed; a run that executed zero subtests FAILS rather than passing as a green package (rule `84d1gfpywd`).
    - The two failing cases assert on the ERROR MESSAGE, naming the violated tolerance. A test that accepted any non-nil error would pass on a platform, runner or scratch-filesystem mismatch — the exact wrong-RED `06-RESEARCH.md` Pitfall 2 documents — and would prove nothing about the numeric bands.
    - The control case proves the four category guards are quiet for the derived frame. Without it, the two failing cases cannot be distinguished from category errors.
    - No absolute figure from the committed file appears as a literal in the test: `test "$(rg -o -e '17090' -e '845950976' -e '2588799' -e '258\.199' internal/bench/baseline_gate_test.go | wc -l | tr -d ' ')" = "0"` and the SUMMARY states the derivation used instead. A hard-coded figure would break on a legitimate re-bless and invite someone to weaken the test.
    - The committed baseline is byte-unchanged, pre-plan-SHA relative (class fix A): `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt); git diff --quiet "$BASE" -- tools/bench/baseline.json && test -f tools/bench/baseline.json && echo "BASELINE_UNCHANGED_SINCE=$BASE"`
    - `git diff --quiet "$BASE" -- internal/bench/regression.go internal/bench/regression_test.go internal/bench/metrics.go internal/bench/rss.go` exits 0 apart from Task 1's committed comment sweep — this task adds a file and edits none.
  </acceptance_criteria>
  <done>A Go test loads the committed `tools/bench/baseline.json`, passes it to `CheckRegression` as the baseline, and proves the gate returns nil in frame, names a throughput regression at 11% and names peak-RSS growth at 16% — so the mechanism ROADMAP success criterion 2 names is exercised rather than described.</done>
</task>

<task type="auto">
  <name>Task 3: FIXT-07 mutation rehearsal — demonstrate the gate RED, revert byte-cleanly, record it</name>
  <precondition>Tasks 1 and 2 are committed: `git diff --quiet -- internal/bench/regression.go` exits 0 and `git status --porcelain internal/bench/` prints nothing. The FIXT-07 protocol opens with a cleanliness gate, and a dirty tree makes both "the mutation actually applied" and "the revert was byte-clean" unprovable.</precondition>
  <files>.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md</files>
  <read_first>
    - `.planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md` in full — the five-step template, the pre-mutation cleanliness gate added as a review finding, and the closing "the final commit carries no mutation byte" statement
    - `internal/bench/regression.go` lines 1-20 and 105-130 — the two tolerance constants and the two comparison branches that consume them
    - `internal/bench/regression_test.go` lines 8-80 — the table-driven suite, specifically the two `wantErr: true` cases named `"throughput 11% slower: exceeds band fails"` (errHint `throughput`) and `"peak RSS 16% larger: exceeds band fails"` (errHint `RSS`)
    - `internal/bench/baseline_gate_test.go` as left by Task 2 — the committed-baseline oracle this rehearsal must ALSO drive RED; its subtest names are what the log records alongside the table's
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md` §Pattern 4 and §Pitfall 2 — why the pure-function target is chosen over a live measurement, and what a wrong-RED looks like
  </read_first>
  <action>
Run two mutation families against `internal/bench/regression.go` and record each in a new
`06-MUTATION-LOG.md`, following `03-MUTATION-LOG.md`'s five-step template verbatim per D-08. The
rehearsal drives the EXISTING `regression_test.go` suite; the test file is never touched.

Guard the whole rehearsal with a shell `trap ... EXIT` that restores
`internal/bench/regression.go` via `git checkout --`, so an interrupted run cannot leave a weakened
gate in the tree. This closes T-06-01, the highest-severity threat in this phase.

**Family A — throughput tolerance.**
1. Cleanliness gate: `git diff --quiet -- internal/bench/regression.go` must exit 0. Record the
   exit code.
2. Mutate `DefaultThroughputTolerance` (declared at `internal/bench/regression.go:9`) from its
   committed value to `1.0`, so an 11% regression no longer exceeds the band.
3. Confirm the mutation actually applied: `git diff --stat -- internal/bench/regression.go` must be
   NON-empty. Paste it. A rehearsal that skips this step cannot distinguish "the gate held" from
   "the edit never landed".
4. Observe RED **in both oracles**: `go test -count=1 ./internal/bench/... -run 'TestCheckRegression' -v`
   (the `-run` pattern matches both the pre-existing table suite and Task 2's
   `TestCheckRegressionAgainstCommittedBaseline`). Paste the failing output verbatim. Two failures
   are expected: the table subtest named for the 11%-slower throughput case, AND the
   committed-baseline test's throughput subtest — both reporting that the gate returned nil where
   an error was wanted. The second failure is what proves the committed-`baseline.json` path is
   itself non-vacuous; a mutation that reddens only the in-memory table would mean the new test
   passes for a reason unrelated to the tolerance.
5. Revert with `git checkout -- internal/bench/regression.go`; prove `git diff --stat` for that path
   is now EMPTY; re-run the same command and paste the green result.

**Family B — peak-RSS tolerance.** The identical five steps against `DefaultRSSTolerance` (declared
at `internal/bench/regression.go:15`), mutated to `1.0`. The expected RED is again two failures:
the table subtest named for the 16%-larger peak-RSS case and the committed-baseline test's
peak-RSS subtest.

**Failure-mode discipline (Pitfall 2).** If an observed failure message names a platform mismatch,
a runner mismatch or a scratch-filesystem mismatch instead of the tolerance being violated, the
rehearsal has NOT demonstrated the numeric band. Record it as a failed rehearsal and stop; do not
report it as a firing gate. The category-error guards fire before any numeric comparison, so a
category-error RED looks superficially like success and proves nothing.

Author `06-MUTATION-LOG.md` with: a header naming BOTH oracles this rehearsal drives (the
pre-existing `regression_test.go` table and Task 2's committed-baseline test) and stating both are
unchanged by the rehearsal; a "Pre-mutation cleanliness gate" section stating the gate was asserted
before every mutation AND before every revert; one block per family carrying all five steps with
verbatim command output and BOTH expected failures; and a closing statement that this phase's
commits carry no mutation byte, backed by the final `git diff --stat` output.

Do not mutate `tools/bench/baseline.json`, and do not drive this rehearsal through
`go run ./tools/bench/runner -mode regression` — off the pinned runner class that path fails on a
category error, which is exactly the wrong-RED this protocol exists to avoid.
  </action>
  <verify>
    <automated>git diff --quiet -- internal/bench/regression.go && echo "POST_REHEARSAL_TREE_CLEAN=true" && go test -count=1 ./internal/bench/... && test -f .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md && F=$(rg -o -e 'DefaultThroughputTolerance' -e 'DefaultRSSTolerance' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md | wc -l | tr -d ' '); R=$(rg -o -e '\-\-\- FAIL' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md | wc -l | tr -d ' '); echo "MUTATION_FAMILY_MENTIONS=$F RED_BLOCKS_RECORDED=$R"; test "$F" -ge 2 && test "$R" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet -- internal/bench/regression.go` exits 0 after the rehearsal and prints `POST_REHEARSAL_TREE_CLEAN=true`; `git status --porcelain internal/bench/` prints nothing.
    - `go test -count=1 ./internal/bench/...` is green at task end, and the whole-repo `go build ./... && go vet ./...` is green.
    - `06-MUTATION-LOG.md` exists and records BOTH families: the census prints `MUTATION_FAMILY_MENTIONS=<n>` with n >= 2 and `RED_BLOCKS_RECORDED=<n>` with n >= 2. These are printed counts, not exit codes.
    - Each family block contains, in order: the pre-mutation cleanliness exit code, a non-empty `git diff --stat` proving the mutation applied, verbatim `--- FAIL` output naming the violated tolerance, the revert command, an empty post-revert `git diff --stat`, and a verbatim green re-run.
    - **Each family reddened BOTH oracles** (review HIGH 4's non-vacuity half): every family block records a `--- FAIL` from the pre-existing table subtest AND one from `TestCheckRegressionAgainstCommittedBaseline`. Assert positively: `test "$(rg -o -F 'TestCheckRegressionAgainstCommittedBaseline' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md | wc -l | tr -d ' ')" -ge 2 && echo "COMMITTED_BASELINE_REDS_RECORDED=ok"`. If a mutation reddens only the in-memory table, the committed-baseline test is passing for a reason unrelated to the tolerance and that must be reported, not rationalised.
    - Each recorded RED names the tolerance being violated, restated per class fix B (the previous "returns no matches" form was an inverted `rg -c` gate) and paired with the positive count that RED blocks exist at all:
      `CATERR=$(rg -c 'platform mismatch|runner mismatch|scratch filesystem mismatch' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md || true); TOLNAMED=$(rg -o -e 'throughput regressed' -e 'peak RSS grew' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MUTATION-LOG.md | wc -l | tr -d ' '); echo "CATEGORY_ERROR_LINES=$CATERR TOLERANCE_NAMED_LINES=$TOLNAMED"; test "$CATERR" = "0" && test "$TOLNAMED" -ge 2`
      A category-error RED is not a demonstration (Pitfall 2), and a log with no tolerance-naming line is not one either.
    - Oracle integrity is asserted two ways, because a diff alone is HEAD-relative and would pass after an unintended prior commit (review finding 06-03:229). BOTH must hold:
      (a) pre-plan-SHA relative, per class fix A: `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-PREPLAN-SHA.txt); git diff --quiet "$BASE" -- tools/bench/baseline.json && echo "BASELINE_UNCHANGED_SINCE=$BASE"`. `internal/bench/regression_test.go` is compared against `$BASE` too, allowing only Task 1's committed comment-block change — the SUMMARY names those lines explicitly.
      (b) the two critical cases are asserted BY NAME AND VALUE, so a changed expectation is caught even if the file's diff looks innocuous: `test "$(rg -o -F 'throughput 11% slower: exceeds band fails' internal/bench/regression_test.go | wc -l | tr -d ' ')" = "1" && test "$(rg -o -F 'peak RSS 16% larger: exceeds band fails' internal/bench/regression_test.go | wc -l | tr -d ' ')" = "1" && echo "CRITICAL_ORACLE_CASES_PRESENT=2"`
    - The log's closing statement asserts no mutation byte survives into any commit, backed by the pasted final `git diff --stat`.
  </acceptance_criteria>
  <done>Two independent mutation families each drove a named pre-existing subtest AND the committed-baseline test RED with the tolerance violation reported verbatim, each was reverted byte-cleanly with an empty diff and a green re-run, and `06-MUTATION-LOG.md` carries the full five-step record for both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rehearsal mutation -> the committed working tree | a transient edit to gate logic exists on disk during the rehearsal window |
| `CheckRegression` -> `tools/bench/baseline.json` | committed data the gate reads on every CI run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `internal/bench/regression.go` during the D-08 rehearsal | high | mitigate | Pre-mutation `git diff --quiet` gate, a shell `trap ... EXIT` restoring the file on any exit path, an empty post-revert `git diff --stat`, a green re-run, and a task-level assertion that `git status --porcelain internal/bench/` is empty |
| T-06-02 | Tampering | `internal/bench/regression_test.go` (the oracle) | high | mitigate | Task 3 asserts the file is unchanged against the recorded PRE-PLAN SHA (not HEAD, which a prior commit in this plan would already have moved) AND that the two critical case names are each present exactly once; Task 1 asserts the subtest count is unchanged, so the suite cannot have been quietly trimmed to make anything pass |
| T-06-22 | Repudiation | the claim that the gate fires "against the committed baseline" | high | mitigate | Task 2's test loads `tools/bench/baseline.json` from disk and passes it as the baseline argument; Task 3 proves that test is itself non-vacuous by reddening it with the same mutation that reddens the pre-existing table |
| T-06-10 | Repudiation | the rehearsal record | medium | mitigate | Every step's output is pasted verbatim rather than summarised; a category-error RED is explicitly disqualified from counting as a demonstration |
| T-06-11 | Tampering | `tools/bench/baseline.json` | high | mitigate | Every task asserts `git diff --quiet "$BASE" -- tools/bench/baseline.json` against the recorded pre-plan SHA plus `test -f`, so neither an intervening commit nor a deletion passes (class fix A); the rehearsal deliberately targets the pure function rather than the committed data, and Task 2 reads that data without writing it |

**Deliberately HEAD-relative, and correctly so:** the rehearsal's pre-mutation and post-revert
`git diff --quiet -- internal/bench/regression.go` gates. Those assert that the WORKING TREE is
clean at that instant — the property FIXT-07's protocol is built on, and the thing that makes
"the mutation actually applied" and "the revert was byte-clean" provable at all. Anchoring them
to the pre-plan SHA would make them assert something else entirely and would fail against Task 1's
legitimate committed comment sweep.
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test -count=1 ./internal/bench/... ./internal/corpora/...` green.
- Retired-term census over `internal/bench/`, `internal/corpora/manifest.go` and `corpora/manifest.json` prints a verbatim `0`.
- The `internal/bench` diff from Task 1 is provably comment-only AND provably not a comment deletion (per-file `//` counts within band); tolerance constants and the test table are byte-identical, asserted both by diff and by the two critical case names being present exactly once each.
- `TestCheckRegressionAgainstCommittedBaseline` loads `tools/bench/baseline.json`, asserts it loaded non-degenerately, and proves the gate returns nil in frame and names the violated tolerance out of band.
- `06-MUTATION-LOG.md` records two families, each with a confirmed-applied mutation, verbatim RED **from both oracles**, byte-clean revert and green re-run.
- Post-rehearsal tree is clean and the baseline file is untouched since the recorded pre-plan SHA.
</verification>

<success_criteria>
BENCH-02's "the gate still fires" half is satisfied by demonstration rather than assertion — and
by demonstration against the mechanism ROADMAP success criterion 2 actually names, the committed
`tools/bench/baseline.json`, which a Go test now loads and passes to `CheckRegression`. The
demonstration is recorded in enough detail that a reviewer can re-run it, both oracles are shown
to redden under the same mutation, and the framing sweep across the gate's own package is proven
bounded, comment-only and not a comment deletion.
</success_criteria>

<output>
Create `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-03-SUMMARY.md` when done.
Record: the before/after subtest counts, the before/after per-file comment-line counts, the
verbatim census total, the `internal/corpora` verification statement, the committed-baseline
test's subtest names and the derivation it used instead of literal figures, and both mutation
families' RED subtest names from BOTH oracles.
</output>
