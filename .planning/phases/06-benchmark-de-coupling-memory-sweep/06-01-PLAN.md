---
phase: 06-benchmark-de-coupling-memory-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bench/realcorpus/manifest.go
  - tools/bench/realcorpus/manifest_test.go
  - tools/bench/runner/main.go
  - tools/bench/runner/main_test.go
  - tools/bench/publishcheck/main.go
  - tools/bench/publishcheck/main_test.go
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-PREPLAN-SHA.txt
  - tools/bench/headtohead-darwin-arm64-20260713-run1.json
  - tools/bench/headtohead-darwin-arm64-20260713-run2.json
  - tools/bench/headtohead-darwin-arm64-20260713-run3.json
  - tools/bench/headtohead-linux-amd64-ci-20260719-run1.json
  - tools/bench/headtohead-linux-amd64-ci-20260719-run2.json
  - tools/bench/headtohead-linux-amd64-ci-20260719-run3.json
autonomous: true
requirements: [BENCH-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`go run ./tools/bench/runner -mode publish` measures only the freshly-built Go binary and emits one bench.Metrics record per corpus entry, every record carrying subject \"go\"."
    - "`tools/bench/realcorpus.Corpora()` returns exactly two entries — weft-go and cockroachdb-pebble — with their existing CommitSHA pins byte-unchanged."
    - "The runner rejects the retired comparison-binary flag: parseFlags returns a non-nil error for it, proving the flag is gone rather than merely undocumented (D-07, rule 84d1gfpywd)."
    - "No committed head-to-head capture file remains tracked under tools/bench/ (D-01); git history still contains them."
    - "tools/bench/realcorpus and tools/bench/runner package/field prose describes absolute single-subject measurement, citing no second implementation (D-10)."
    - "The publish path's end-to-end verification runs on Go alone: tools/bench/publishcheck validates the emitted JSON, and no verification step in this phase requires a Node runtime (review finding 06-01:246)."
  artifacts:
    - "tools/bench/runner/main.go — runPublish replaces runHeadToHead; -mode publish replaces the retired comparison mode"
    - "tools/bench/runner/main_test.go — flag-parsing tests repointed at publish mode plus a positive removal assertion"
    - "tools/bench/realcorpus/manifest.go — two-entry manifest, absolute-measurement prose"
    - "tools/bench/realcorpus/manifest_test.go — provenance and golden-pin tests covering the two surviving entries"
    - "tools/bench/publishcheck/{main,main_test}.go — a pure-Go verifier for publish-mode JSON, itself positive-controlled against deliberately-bad fixtures"
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-PREPLAN-SHA.txt — the pre-plan commit every tamper guard in this plan compares against (review class fix A)"
  key_links:
    - "tools/bench/runner/main.go's mode switch -> runPublish -> measureSubject(\"go\", ...) -> internal/bench.Metrics JSON on stdout"
    - "realcorpus.Corpora() -> resolveOrClone -> measureSubject: dropping an entry changes what the publisher measures and therefore what docs/BENCHMARKS.md can publish"
    - "internal/bench.Metrics is shared with regression mode — publish mode must keep emitting the same json tags, so no struct field is added or removed"
  prohibitions:
    - "MUST NOT weaken, skip, or t.Skip a test in order to make the suite green after a deletion — a test asserting a removed surface is deleted or repointed, never neutered."
    - "MUST NOT change any behaviour in internal/bench (metrics shape, CheckRegression logic, baseline.json values) from this plan."
    - "MUST NOT reword innocent third-party or contributor prose, or widen a census exclusion, to make a count read zero — if a pattern flags ordinary English, bound the pattern."
---

<objective>
Cut the two-subject comparison architecture out of the benchmark measurement path and prove
the surviving single-subject path works end to end.

Purpose: BENCH-02 requires the comparison runner to be gone from `tools/bench`. This plan is
the tracer slice for the whole phase — it wires the corpus manifest through the runner to real
absolute-only `Metrics` JSON on stdout, so the architecture the CI job (06-02) and the published
document (06-04) both depend on is proven before either is built.

Output: a `publish` runner mode measuring the Go binary only, a two-entry benchmark corpus, the
six committed comparison-era capture files deleted, and the surviving prose re-authored on
absolute-measurement terms.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols and files this phase CREATES (new — not drift):

| Artifact | Kind | Plan |
|---|---|---|
| `runPublish` | Go func, `tools/bench/runner/main.go` | 06-01 |
| `-mode publish` | CLI mode value | 06-01 |
| `TestParseFlags_PublishOverridesApply` | Go test | 06-01 |
| `TestParseFlags_RejectsRetiredComparisonBinaryFlag` | Go test | 06-01 |
| `TestCorporaHasExactlyTwoEntries` | Go test | 06-01 |
| `tools/bench/publishcheck` (`main.go`, `main_test.go`) | Go command + its tests — the pure-Go publish-JSON verifier that replaces the Node one-liner (review 06-01:246, 06-04:135) | 06-01 |
| `06-NN-PREPLAN-SHA.txt` (one per plan, NN = 01..05) | phase record — the pre-plan commit every tamper guard compares against (review class fix A) | 06-01/02/03/04/05 |
| `publish` (job id) | `.github/workflows/bench.yml` | 06-02 |
| `publish-results` / `publish-results.json` | CI artifact name / path | 06-02 |
| `TestBenchPublishJobShape` | Go test, `internal/upgrade/bench_workflow_shape_test.go` — the publish-job-scoped structural verifier for all four D-06 properties (review HIGH 1/2/3) | 06-02 |
| `TestBenchReblessJobShape` | Go test, same file — the parsed-subtree fixture proving `rebless` is untouched (review 06-02:165) | 06-02 |
| `06-MUTATION-LOG.md` | phase record | 06-03 |
| `TestCheckRegressionAgainstCommittedBaseline` | Go test, `internal/bench/baseline_gate_test.go` — drives the gate with the committed `tools/bench/baseline.json` (review HIGH 4) | 06-03 |
| `06-LIVE-VERIFICATIONS.md` | phase record — the BENCH-03 / MEM-02 live-verification ledger, each row carrying a verbatim status token (review HIGH 5, 06-05:215) | 06-04 / 06-05 / 06-06 |
| `06-PUBLISH-RESULTS.json` | phase record (measurement of record) | 06-04 |
| `06-CENSUS.md` | phase record | 06-04 |
| `06-MEMORY-SWEEP.md` | phase record | 06-05 / 06-06 |
| `COVERAGE.md` | phase record (no-external-API declaration) | 06-04 |

Symbols and files this phase DELETES:

| Artifact | Kind | Plan |
|---|---|---|
| `runHeadToHead` | Go func | 06-01 |
| `resolveTSBinary` | Go func | 06-01 |
| `macOSHomebrewTSBinary` | Go const | 06-01 |
| `config.tsBinary` field and its `-ts-binary` flag | Go struct field / CLI flag | 06-01 |
| `-mode headtohead` | CLI mode value | 06-01 |
| `TestResolveTSBinary_FindsOnPath`, `TestResolveTSBinary_EmptyWhenNotFound` | Go tests | 06-01 |
| `colbymchenry-codegraph` realcorpus entry (`CODEGRAPH_TSCG_CORPUS`, `SiblingDir: codegraph-ts`) | manifest data | 06-01 |
| `tscgPinnedCommit` | Go test const | 06-01 |
| six `tools/bench/headtohead-*.json` capture files | committed data | 06-01 |
| `headtohead` job id, `headtohead-results` artifact, Node/npm setup + global install steps | CI config | 06-02 |
</artifacts_this_phase_produces>

<assumption_delta_decision>
The detector fired `pluralization` on "no **second** implementation" / "without invoking
**another** implementation". The transition here is plural -> singular (two measured subjects
become one), the inverse of a generalization.

- **Primary noun:** a measurement's identity stays `(Repo, GOOS, GOARCH, Runner, ScratchFS)`.
  `Metrics.Subject` was never the primary key; it is a provenance label.
- **Decision: `no-change`.** `Metrics.Subject` is kept and always set to `"go"` in publish mode.
  Rationale: `internal/bench.Metrics` is a shared type between publish mode and the regression
  gate, and `subject` is a json tag present in the committed `tools/bench/baseline.json`.
  Removing the field would change a committed data file this phase declares out of scope.
- Accepted debt: none. Re-promotion would only be forced if a future phase reintroduces a second
  measured subject, at which point `Subject` is already the right discriminator.
</assumption_delta_decision>

<edge_probe_coverage>
Spec-less probe fallback (no SPEC.md for this phase). Row owned by this plan:

| Requirement | Category | Status | Disposition |
|---|---|---|---|
| BENCH-02 | unclassified | **unresolved** | **Flagged assumption — not auto-resolved.** The probe could not classify BENCH-02 into an edge family. Assumption carried by this plan: "comparison runner removed" is interpreted as *the two-subject measurement architecture and its flag/helper surface are absent from `tools/bench`*, not merely that the mode string was renamed. If a reviewer reads BENCH-02 more narrowly or more broadly, this plan's acceptance criteria are the place to correct it. |
</edge_probe_coverage>

<review_incorporation>
## Cross-AI review findings incorporated (`06-REVIEWS.md`)

Two **class fixes** are applied across every plan in this phase, stated here so the next review
cycle can see they were systematic rather than one-at-a-time:

**Class fix A — tamper guards compare against the pre-plan SHA, never HEAD.**
`git diff --quiet -- <path>` is HEAD-relative, and GSD commits per task, so an earlier task's
COMMITTED change to a protected path is invisible to a later task's guard — the guard goes blind
in exactly the window it exists to watch. Task 1 of this plan therefore records
`git rev-parse HEAD` as the single line of `06-01-PREPLAN-SHA.txt` **before its first edit**, and
every tamper guard in this plan reads that value into `$BASE` and asserts
`git diff --quiet "$BASE" -- <path>`. Every such guard also carries a positive companion
assertion, because a diff-clean check passes vacuously against a file that is absent
(rule `84d1gfpywd`).

**Class fix B — no `rg -c` is used as a "returns no matches" gate.**
`rg -c` exits 1 and prints nothing on the *good* path, so an executor chaining it with `&&` reads
the clean outcome as a failure, and an executor reading exit status leniently reads a broken
command as a pass — the same inverted shape as this repo's documented `rg -qv` incident. Every
such gate is restated as `test "$( … | rg -c … || true )" = "0"` with a printed token, paired
with a companion count that MUST be > 0 so the instrument proves it can still see. `rg -c` used
as a FLOOR (`>= 5`, `>= 1`) or to count line-oriented things is deliberately left alone:
`rg -c <= rg -o`, so a floor is strictly conservative and is not the inverted shape.

**Findings closed in this plan:**

| Finding | Where it lands |
|---|---|
| `06-01:246` — undeclared Node dependency in a plan that removes Node | Task 1 authors `tools/bench/publishcheck`, a pure-Go verifier; the `<verify>` block no longer shells out to Node. The same binary closes `06-04:135`. |
| `06-01:257` — "no `t.Skip`/`//nolint` added" is negative-only | Task 1 captures `go test -list` before its first edit and asserts the removed and added test-name sets **exactly**, with a non-zero total. |
| `06-01:311` — `git diff --quiet -- NOTICE` passes if NOTICE is absent | Task 2 asserts `test -f NOTICE`, a positive match on its verbatim upstream copyright line, and a `$BASE`-relative diff (class fix A). |
| `06-01:255` — surviving-pin check is an inverted `rg -c` gate | Restated per class fix B, paired with a positive count that both surviving `CommitSHA` literals are still present in the file. |
| Class fix A | Both tasks' `internal/bench` / `baseline.json` / `NOTICE` guards. |
</review_incorporation>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end absolute-only publish path — corpus, runner mode, one real run</name>
  <precondition>`git` is on PATH and github.com is reachable — `git ls-remote --exit-code --heads https://github.com/cockroachdb/pebble` exits 0. The runner shallow-clones each pinned corpus when no local checkout sits at the pinned SHA, so an unreachable network makes the end-to-end verify fail for the wrong reason. Halt and surface a checkpoint if unmet; do not substitute a stub corpus.</precondition>
  <reversibility rating="costly">The emitted JSON shape changes with the mode, so the CI summary step and every consumer of that output change with it (D-07).</reversibility>
  <files>tools/bench/realcorpus/manifest.go, tools/bench/realcorpus/manifest_test.go, tools/bench/runner/main.go, tools/bench/runner/main_test.go, tools/bench/publishcheck/main.go, tools/bench/publishcheck/main_test.go, .planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-PREPLAN-SHA.txt</files>
  <read_first>
    - `tools/bench/runner/main.go` (full file — mode switch at 160-168, flag registration at 173-200, the retired PATH-lookup helper at 202-218, `runHeadToHead` at 306-340, `measureSubject` at 342-433, `resolveOrClone` at 435-464)
    - `tools/bench/runner/main_test.go` (lines 470-535 — the three test bodies that assert the surface this task deletes)
    - `tools/bench/realcorpus/manifest.go` (full file — package doc 1-26, `Entry` 37-79, `Corpora()` 81-137)
    - `tools/bench/realcorpus/manifest_test.go` (lines 11-79 — `TestCorporaProvenanceComplete`, `TestReferencesExistingGoldenCorpora` and its two pinned-commit consts)
    - `internal/bench/metrics.go` (the shared `Metrics` struct and its json tags — read to confirm no field is added or removed)
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md` §Pattern 2 and §Code Examples (the subtraction points and the `runPublish` skeleton, both file:line verified)
  </read_first>
  <behavior>
    Write these test expectations FIRST and watch them fail before touching production code.

    In `tools/bench/realcorpus/manifest_test.go`:
    - `TestCorporaHasExactlyTwoEntries` (new): `len(Corpora()) == 2`; the set of `Entry.Name`
      values is exactly `{"weft-go", "cockroachdb-pebble"}`. Fails today (three entries).
    - `TestReferencesExistingGoldenCorpora` (edited): asserts only the `weft-go` pin
      `f89ae3ea4e4c37509f7302fd4e37986212a72079`. The multi-language entry's lookup, its
      `t.Fatal`, its `CommitSHA` comparison and the now-unused `tscgPinnedCommit` const are
      removed in this same edit — a stale assertion on a removed entry is deleted, never skipped.
    - `TestCorporaProvenanceComplete` (unchanged assertions): still passes over the two
      survivors, so the provenance bar is not silently relaxed by the drop.

    In `tools/bench/runner/main_test.go`:
    - `TestParseFlags_PublishOverridesApply` (replaces `TestParseFlags_OverridesApply`):
      `parseFlags([]string{"-mode", "publish", "-count", "5"})` returns no error, `cfg.mode` is
      `"publish"`, `cfg.count` is `5`.
    - `TestParseFlags_RejectsRetiredComparisonBinaryFlag` (new, the positive removal assertion
      required by rule `84d1gfpywd`): `parseFlags` called with the retired comparison-binary flag
      returns a **non-nil** error, because the flag is no longer registered. This is what proves
      the flag is gone rather than merely undocumented.
    - The two direct tests of the retired PATH-lookup helper are deleted whole, together with the
      helper itself.

    In `tools/bench/publishcheck/main_test.go` (new — the verifier is itself under test, because a
    verification instrument that cannot be shown to reject a bad input is worth nothing; this is
    the same positive-control discipline D-04 imposes on the census):
    - A well-formed two-record fixture with both subjects `"go"`, all five metrics positive and
      `median_of_trials` 1 passes and yields the expected printed token line.
    - A fixture with one record FAILS the `-want-records 2` check.
    - A fixture whose second record has a subject other than `"go"` FAILS.
    - A fixture with a zero or negative value in any of `files_per_sec`, `bytes_per_sec`,
      `query_latency_median_ms`, `peak_rss_bytes`, `cold_start_ms` FAILS, naming the offending
      record and field.
    - A fixture whose `repo` set differs from `-want-repos` FAILS.
    - An empty JSON array FAILS rather than passing vacuously (rule `84d1gfpywd`).
    - The printed `SHA256=` value equals the SHA-256 of the input file's bytes, so two invocations
      over two files can be compared for byte identity without a platform-specific checksum tool.

    End-to-end (the tracer's own expectation, asserted by `<verify>` rather than by `go test`,
    because it shells out to a built binary and the network):
    - `-mode publish` emits a JSON array whose length equals `len(Corpora())`.
    - Every record has `subject` `"go"`; no record has any other subject value.
    - Every record has `files_per_sec > 0` and `median_of_trials == 1`.
  </behavior>
  <action>
**Before touching a single file, record two baselines this task's acceptance criteria depend on.**
Neither can be reconstructed afterwards, so capturing them first is not optional bookkeeping.

1. Write `git rev-parse HEAD` as the single line of
   `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-PREPLAN-SHA.txt`. This is the
   pre-plan commit every tamper guard in this plan compares against (review class fix A). A
   HEAD-relative guard would go blind the moment this task commits, which is precisely the window
   the guard exists to watch.
2. Capture the current test surface:
   `go test -list '.*' ./tools/bench/... | rg '^Test' | sort > "${TMPDIR:-/tmp}/06-01-tests-before.txt"`.
   This replaces the negative-only "no test was skipped" check with an exact before/after set
   comparison (review finding 06-01:257).

Then delete the two-subject measurement architecture from the runner and drop the
milestone-entangled corpus entry, in one commit with its paired test edits — per D-07 and D-09.
Per the Wave-0 hazard in `06-VALIDATION.md`, the subtractive test edits MUST land in this same
task; splitting them turns the suite red for the wrong reason.

`tools/bench/realcorpus/manifest.go`:
<!-- planner-discipline-allow: colbymchenry -->
<!-- planner-discipline-allow: codegraph-ts -->
<!-- planner-discipline-allow: CODEGRAPH_TSCG_CORPUS -->
- Remove the `colbymchenry-codegraph` `Entry` literal in `Corpora()` (currently the second of
  three, lines 105-118) together with its `EnvVar: "CODEGRAPH_TSCG_CORPUS"` and
  `SiblingDir: "codegraph-ts"`. Leave the `weft-go` and `cockroachdb-pebble` literals — including
  every `CommitSHA`, `License`, `QueryTerms`, `EnvVar` and `SiblingDir` value — byte-unchanged.
- Re-author the package doc (lines 1-26), the `Entry` doc (37-41), the `CommitSHA` field comment
  (54-57), the `Corpora()` doc (81-86) and the `resolveOrClone` cross-reference inside `Resolve`'s
  doc (154-161) so they describe a pinned real-repo corpus for **absolute single-subject
  throughput / query-latency / peak-RSS publication**. Keep every surviving engineering fact:
  the commit-SHA-not-branch reproducibility rule, the V5 "never HEAD" constraint, the
  no-network-I/O contract, `ErrNeedsClone`'s caller contract, and the `tools/spike/testdata/
  ATTRIBUTION.md` provenance-shape lineage. Update the "three entries" arithmetic in `Corpora()`'s
  ordering rationale to match two.
- Per D-11, re-justify keeping the BSD-3-Clause `cockroachdb-pebble` entry on its own terms —
  a fetch-only, never-vendored measurement corpus is a different redistribution question from
  `internal/corpora`'s stricter MIT/Apache-2.0 golden-fixture bar. No surviving comment may cite a
  benchmark against another implementation as the reason. Do NOT merge, parameterise or otherwise
  disturb the two-manifest split — it is a recorded Phase 1 decision (`01-04-PLAN.md`,
  `internal/corpora/manifest.go:6-15`); this phase touches text only.

`tools/bench/runner/main.go`:
<!-- planner-discipline-allow: ts-binary -->
<!-- planner-discipline-allow: resolveTSBinary -->
<!-- planner-discipline-allow: macOSHomebrewTSBinary -->
<!-- planner-discipline-allow: runHeadToHead -->
<!-- planner-discipline-allow: headtohead -->
- Delete `const macOSHomebrewTSBinary` (109-116), the `tsBinary` field on `config` (129), the
  `cfg.mode == "headtohead" && cfg.tsBinary == ""` resolution branch in `run` (156-158), the
  `-ts-binary` `fs.StringVar` registration (178), `func resolveTSBinary` (202-218) and
  `func runHeadToHead` (306-340).
- Add `func runPublish(cfg config) error` in `runHeadToHead`'s place. It keeps the scratch-root
  `os.MkdirTemp` + `defer os.RemoveAll`, the `realcorpus.Corpora()` loop, and the
  `resolveOrClone` skip-with-stderr-note behaviour verbatim; the `[]struct{name, binary}` loop is
  replaced by a single `measureSubject("go", cfg.goBinary, entry, srcDir, scratchRoot, cfg.runner)`
  call. Name the temp prefix `codegraph-bench-publish-`. Use the skeleton in `06-RESEARCH.md`
  §Code Examples as the shape.
- Change the mode switch (160-167): `case "publish": return runPublish(cfg)`, `case "regression":`
  unchanged, and the default error text to name `publish or regression`. Update the `-mode` flag
  usage string (176) to `benchmark mode: publish or regression (required)`.
- Re-author the command doc comment (1-36): describe two modes, `publish` (per-corpus absolute
  `Metrics` JSON for the Go binary, a published measurement and never a gate) and `regression`
  (unchanged). Rewrite the peak-RSS sentence (29-35) to state the OS-level `bench.PeakRSSBytes`
  rule on its own terms — an externally-observed child-process number is the only figure that
  stays comparable run to run — with no second runtime named. Keep the V5 subprocess-argument
  paragraph verbatim.
- Re-author `measureSubject`'s doc comment (342-348) to explain the surviving reason for the
  isolated per-invocation work directory: the measured binary writes its own `.codegraph/` store,
  which must not land in the operator-owned resolved source checkout nor collide with a prior
  run's state. Do not delete the isolation behaviour.
- Reword `measureSubject`'s `MedianOfTrials: 1` comment (421-426) so its opening clause names
  publish mode; the provenance-recording rationale itself is unchanged.
- Reword the `resolveOrClone` doc's mode reference (435-440) so the "the one place that touches
  the network" note names publish mode.

`tools/bench/publishcheck/main.go` (new — closes review finding 06-01:246):
- A `package main` command with no dependency outside the standard library. Flags: `-file` (path
  to a publish-mode JSON capture), `-want-records` (int), `-want-repos` (comma-separated set),
  `-want-subject` (string, default `go`), `-want-median-of-trials` (int, default 1), and
  `-emit-rows` (bool, off by default — see 06-04, which reuses this binary to generate the
  published Markdown data rows rather than hand-typing them).
- It unmarshals the file into `[]bench.Metrics`, then asserts: the value is a JSON array; its
  length equals `-want-records`; the sorted set of `repo` values equals the sorted `-want-repos`
  set; every record's `subject` equals `-want-subject`; every record's `median_of_trials` equals
  `-want-median-of-trials`; and every one of `files_per_sec`, `bytes_per_sec`,
  `query_latency_median_ms`, `peak_rss_bytes`, `cold_start_ms` is strictly positive in every
  record. Any violation exits non-zero naming the record and the field.
- On success it prints one line:
  `PUBLISH_RECORDS=<n> REPOS=<sorted,csv> ALL_SUBJECT_GO=true ALL_METRICS_POSITIVE=true SHA256=<hex>`,
  where `<hex>` is the SHA-256 of the input file's bytes. The hash exists so 06-04 can prove
  byte identity between a stdout capture and a committed artifact without depending on
  `sha256sum` (Linux) versus `shasum -a 256` (macOS).
- `-emit-rows` additionally prints one pipe-delimited Markdown data row per record, in sorted
  `repo` order, with the rounding the published table uses. This is the single source of the
  document's numbers.
- Reuse `internal/bench.Metrics` rather than redeclaring the shape: a divergent local struct would
  silently stop checking a renamed field. Do not add, remove or re-tag any field on that struct.

Do not add a `-corpus` filter, a `-subject` flag, or any other new capability to the runner. Do
not touch `internal/bench` in this task.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./tools/bench/... ./internal/bench/... && go build -o "${TMPDIR:-/tmp}/cg-bench-06" ./cmd/codegraph && go run ./tools/bench/runner -mode publish -go-binary "${TMPDIR:-/tmp}/cg-bench-06" > "${TMPDIR:-/tmp}/06-publish-smoke.json" && go run ./tools/bench/publishcheck -file "${TMPDIR:-/tmp}/06-publish-smoke.json" -want-records 2 -want-repos weft-go,cockroachdb-pebble -want-subject go -want-median-of-trials 1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go vet ./...` exit 0 — no dangling reference to any deleted symbol survives anywhere in the module.
    - `go test -count=1 ./tools/bench/...` passes with `TestCorporaHasExactlyTwoEntries`, `TestParseFlags_PublishOverridesApply` and `TestParseFlags_RejectsRetiredComparisonBinaryFlag` all present and green (`go test -run 'TestCorporaHasExactlyTwoEntries|TestParseFlags_' -v ./tools/bench/...` names all three as RUN/PASS).
    - The end-to-end run prints `PUBLISH_RECORDS=2 REPOS=cockroachdb-pebble,weft-go ALL_SUBJECT_GO=true ALL_METRICS_POSITIVE=true SHA256=<hex>`. A run that emits an empty array FAILS — the record count is a positive assertion, not an absence check (rule `84d1gfpywd`). The check runs on Go alone; no verification step in this plan invokes a Node runtime (review finding 06-01:246).
    - `publishcheck` is proven able to REJECT before its PASS is trusted: `go test -count=1 ./tools/bench/publishcheck/...` is green and `go test -run TestPublishCheck -v ./tools/bench/publishcheck/...` names the rejecting cases (short record count, wrong subject, non-positive metric, wrong repo set, empty array) as RUN/PASS. An instrument that has never been shown to fail is not evidence.
    - Retired-symbol absence, counted and recorded verbatim rather than inferred from an exit code:
      `N=$(rg -U -o -e 'ts-binary' -e 'resolveTSBinary' -e 'macOSHomebrewTSBinary' -e 'runHeadToHead' -e '\bheadtohead\b' tools/bench/runner/ | wc -l | tr -d ' '); echo "RETIRED_RUNNER_SYMBOLS_TOTAL=$N"; test "$N" = "0"`
    - Retired-corpus absence, likewise: `M=$(rg -U -o -e 'colbymchenry' -e 'codegraph-ts' -e 'CODEGRAPH_TSCG_CORPUS' -e 'tscgPinnedCommit' tools/bench/realcorpus/ | wc -l | tr -d ' '); echo "RETIRED_CORPUS_REFS_TOTAL=$M"; test "$M" = "0"`
    - Surviving-pin integrity, restated per review class fix B (the previous form was an inverted `rg -c` gate that exits 1 and prints nothing on the good path), and paired with the positive count that proves the instrument can still see the pins:
      `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-PREPLAN-SHA.txt); PINDIFF=$(git diff "$BASE" -- tools/bench/realcorpus/manifest.go | rg -c '^[-+].*f89ae3ea4e4c37509f7302fd4e37986212a72079|^[-+].*dbdc1acb859689dc4237b40ef8fcdbb877526a84' || true); PINPRESENT=$(rg -o -e 'f89ae3ea4e4c37509f7302fd4e37986212a72079' -e 'dbdc1acb859689dc4237b40ef8fcdbb877526a84' tools/bench/realcorpus/manifest.go | wc -l | tr -d ' '); echo "SURVIVING_PIN_DIFF_LINES=$PINDIFF SURVIVING_PINS_PRESENT=$PINPRESENT"; test "$PINDIFF" = "0" && test "$PINPRESENT" = "2"`
    - Gate-input integrity, pre-plan-SHA relative per review class fix A (a HEAD-relative guard cannot see a change this plan itself already committed), with a positive companion so it cannot pass against a missing file:
      `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-PREPLAN-SHA.txt); git diff --quiet "$BASE" -- internal/bench/ tools/bench/baseline.json && test -f tools/bench/baseline.json && test "$(rg -o -F '"subject": "go"' tools/bench/baseline.json | wc -l | tr -d ' ')" = "1" && echo "GATE_INPUTS_UNCHANGED_SINCE=$BASE BASELINE_PRESENT=true"`
    - Test-surface delta asserted exactly, replacing the negative-only "nothing was skipped" check (review finding 06-01:257). After the edit, re-list and diff against the capture taken before the first edit:
      `go test -list '.*' ./tools/bench/... | rg '^Test' | sort > "${TMPDIR:-/tmp}/06-01-tests-after.txt"; comm -23 "${TMPDIR:-/tmp}/06-01-tests-before.txt" "${TMPDIR:-/tmp}/06-01-tests-after.txt" > "${TMPDIR:-/tmp}/06-01-removed.txt"; comm -13 "${TMPDIR:-/tmp}/06-01-tests-before.txt" "${TMPDIR:-/tmp}/06-01-tests-after.txt" > "${TMPDIR:-/tmp}/06-01-added.txt"; echo "TESTS_AFTER=$(wc -l < "${TMPDIR:-/tmp}/06-01-tests-after.txt" | tr -d ' ') REMOVED=$(wc -l < "${TMPDIR:-/tmp}/06-01-removed.txt" | tr -d ' ') ADDED=$(wc -l < "${TMPDIR:-/tmp}/06-01-added.txt" | tr -d ' ')"`
      The removed set MUST equal exactly `TestParseFlags_OverridesApply`, `TestResolveTSBinary_EmptyWhenNotFound`, `TestResolveTSBinary_FindsOnPath`; the added set MUST equal exactly `TestCorporaHasExactlyTwoEntries`, `TestParseFlags_PublishOverridesApply`, `TestParseFlags_RejectsRetiredComparisonBinaryFlag`; and `TESTS_AFTER` MUST be > 0. Verify with `diff` against those two literal sorted lists and paste both `diff` results into the SUMMARY. A test that vanished for any other reason fails this criterion by name rather than being absorbed into a count.
      Companion (informational, not the gate): `echo "TOOLS_BENCH_SKIPS=$(rg -o -e 't\.Skip' -e '//nolint' tools/bench/ | wc -l | tr -d ' ')"` — recorded in the SUMMARY so a later reviewer sees the number rather than an implicit claim.
  </acceptance_criteria>
  <done>The runner has exactly two modes, `publish` and `regression`; publish mode measures only the Go binary over a two-entry corpus and a real run emits two `subject: "go"` records with positive throughput; every test asserting the removed surface was deleted or repointed in this same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the committed comparison-era captures and confirm the swept prose by count</name>
  <files>tools/bench/headtohead-darwin-arm64-20260713-run1.json, tools/bench/headtohead-darwin-arm64-20260713-run2.json, tools/bench/headtohead-darwin-arm64-20260713-run3.json, tools/bench/headtohead-linux-amd64-ci-20260719-run1.json, tools/bench/headtohead-linux-amd64-ci-20260719-run2.json, tools/bench/headtohead-linux-amd64-ci-20260719-run3.json, tools/bench/realcorpus/manifest.go, tools/bench/runner/main.go</files>
  <read_first>
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md` §D-01 and §D-04 (the deletion ruling and the census instrument's rules)
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-PATTERNS.md` §"Deletion-with-git-history-preserved, verified by absence" and §"The positive-controlled multiline census"
    - `tools/bench/realcorpus/manifest.go` and `tools/bench/runner/main.go` as left by Task 1 (read the current text before asserting anything about it)
  </read_first>
  <action>
<!-- planner-discipline-allow: headtohead -->
Remove the six committed capture files with `git rm`, one commit, per D-01: git history preserves
them, so nothing is archived in-tree. This mirrors the Phase 5 precedent of removing a retired
capability outright rather than reframing it.

Then close out Task 1's prose work by measuring it rather than asserting it. Build the bounded
pattern set once and prove the instrument catches a wrapped occurrence BEFORE trusting any zero,
exactly as D-04 requires:

1. Write a positive-control fixture to a temp file containing the target phrases deliberately
   wrapped across comment line breaks (a `// ... (TS` / `// codegraph@1.3.1) ...` pair and a
   `// ... head-to-` / `// head ...` pair).
2. Run the pattern set over the fixture with `rg -U -o` and with plain `rg -o`; assert the
   multiline count is strictly greater than the line-based count and strictly greater than zero.
   A pattern set that cannot beat the line-based instrument on a planted wrapped phrase is not
   trusted for this file surface.
3. Only then run the same `rg -U -o` set over `tools/bench/realcorpus/` and `tools/bench/runner/`,
   count with `wc -l`, and echo the total.

Bounded pattern set for this task (never bare `comparison` — that word has legitimate uses
elsewhere in this repo and an unbounded pattern is how Phase 5's census flagged innocent prose):
word-bounded `\bTS\b`, `\bhead-to-head\b`, `\bheadtohead\b`, `\bcolbymchenry\b`,
`\bcodegraph-ts\b`, `\bcomparison[[:space:]/*]+(binary|runner|target|subject|mode)\b`,
`\bcomparator\b`, `\b1\.3\.1\b`.

If a pattern flags text that is genuinely innocent, the finding is about the pattern — bound it
tighter and record the adjustment. Never reword the innocent text to turn the count green.

Do not touch `NOTICE`: its upstream MIT attribution is a licence obligation, not framing, and is
outside this phase's scope. Its guard is deliberately two-sided (review finding 06-01:311) — a
diff-clean assertion alone passes against a file that has been deleted, so the criterion below
also asserts the file exists and still carries its verbatim upstream copyright line. Do not
"correct" the capitalisation of the name on that line; `NOTICE` itself explains that the
plausible-looking correction is wrong and would break the attribution.
  </action>
  <verify>
    <automated>test "$(git ls-files 'tools/bench/headtohead-*.json' | wc -l | tr -d ' ')" = "0" && echo "TRACKED_CAPTURES=0" && for f in tools/bench/headtohead-darwin-arm64-20260713-run1.json tools/bench/headtohead-darwin-arm64-20260713-run2.json tools/bench/headtohead-darwin-arm64-20260713-run3.json tools/bench/headtohead-linux-amd64-ci-20260719-run1.json tools/bench/headtohead-linux-amd64-ci-20260719-run2.json tools/bench/headtohead-linux-amd64-ci-20260719-run3.json; do test ! -f "$f" || { echo "STILL PRESENT: $f"; exit 1; }; done && echo "ON_DISK_CAPTURES=0" && go build ./... && go test -count=1 ./tools/bench/...</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files 'tools/bench/headtohead-*.json' | wc -l` prints `0`, and each of the six paths fails `test -f`. Both the tracked-file assertion and the on-disk assertion print their result (`TRACKED_CAPTURES=0`, `ON_DISK_CAPTURES=0`) — a clean `git rm` exit code alone is not accepted as proof of absence.
    - `git log --oneline -1 -- tools/bench/headtohead-darwin-arm64-20260713-run1.json` still resolves, proving history preserved the deleted content (D-01's stated recovery path).
    - Positive control passes before any real census result is believed: the multiline count over the planted fixture is strictly greater than the line-based count over the same fixture, and strictly greater than zero. Both numbers are echoed.
    - The real census over `tools/bench/realcorpus/` and `tools/bench/runner/` echoes its total as `BENCH_TRACER_SURFACE_TOTAL=<n>` with `<n>` equal to `0`, counted with `rg -o | wc -l`. `rg -c` is not used anywhere — it counts matching lines, not matches.
    - Any pattern retired or narrowed during step 2 is recorded in the SUMMARY with the text that forced the change, so a later reviewer sees a bounded pattern rather than a silently dropped one.
    - `NOTICE` is intact, asserted positively as well as negatively (review finding 06-01:311 — the previous diff-only form passed if the file was absent), and pre-plan-SHA relative per class fix A:
      `BASE=$(cat .planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-PREPLAN-SHA.txt); test -f NOTICE && test "$(rg -c -F 'Copyright (c) 2026 Colby Mchenry' NOTICE || true)" = "1" && git diff --quiet "$BASE" -- NOTICE && echo "NOTICE_PRESENT=true NOTICE_COPYRIGHT_LINES=1 NOTICE_UNCHANGED_SINCE=$BASE"`
    - `go build ./... && go test -count=1 ./tools/bench/... ./tools/bench/publishcheck/...` green.
  </acceptance_criteria>
  <done>No comparison-era capture file is tracked or on disk, absence is proven by two printed positive assertions rather than an exit code, and the tracer surface's framing census reports a verbatim zero from an instrument shown live to catch a planted wrapped phrase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner -> shelled-out `codegraph` binary | fixed corpus paths and pinned SHAs only; no operator-supplied path reaches a subprocess argument after `-ts-binary` is removed |
| runner -> github.com (`git clone`/`fetch`/`checkout`) | remote source trees enter the measurement, always at a pinned `CommitSHA`, never `HEAD` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | `tools/bench/baseline.json` via the runner | high | mitigate | Publish mode never reads `-rebless` and never writes a baseline; Task 1 asserts `git diff --quiet "$BASE" -- internal/bench/ tools/bench/baseline.json` against the recorded pre-plan SHA (class fix A), paired with `test -f` and a positive content match so a deleted file cannot pass |
| T-06-07 | Spoofing | `resolveOrClone` corpus fetch | medium | mitigate | Unchanged by this plan: every clone is pinned to `Entry.CommitSHA` via explicit `fetch`+`checkout`, never `HEAD` (V5); Task 1 leaves both surviving `CommitSHA` literals byte-unchanged and asserts so via `git diff` |
| T-06-08 | Repudiation | the framing census in Task 2 | medium | mitigate | Positive control planted and proven before any zero is trusted; the total is echoed verbatim; every pattern narrowing is recorded in the SUMMARY |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This phase adds zero package-manager installs. The only package-manager change in the phase is a **removal** (`npm install -g` of the comparison binary, deleted in 06-02), a net reduction in supply-chain surface. No Package Legitimacy Gate run is required because there is nothing to check into a registry (`06-RESEARCH.md` §Package Legitimacy Audit). |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test -count=1 ./tools/bench/... ./internal/bench/...` green.
- A real `-mode publish` run emits exactly two records, all `subject: "go"`, all with positive metrics — verified by `tools/bench/publishcheck`, a pure-Go instrument whose own tests prove it rejects short, wrong-subject, non-positive and empty inputs. No verification step requires Node.
- The `tools/bench/` test-name set changed by exactly the three removals and three additions this plan names, proven by a before/after `go test -list` comparison rather than by the absence of a skip directive.
- Zero tracked or on-disk comparison-era capture files, each proven by a printed count.
- `NOTICE` exists, still carries its verbatim upstream copyright line, and is byte-unchanged since the recorded pre-plan SHA.
- Framing census over `tools/bench/realcorpus/` and `tools/bench/runner/` reports a verbatim `0` from a positive-controlled multiline instrument.
</verification>

<success_criteria>
BENCH-02's "the comparison runner is removed from `tools/bench`" half is satisfied and mechanically
demonstrated: the two-subject architecture, its flag, its PATH-lookup helper, its mode string and
its committed output captures are all gone, the surviving single-subject path runs end to end, and
every test that asserted the removed surface moved in the same commit as the surface itself.
</success_criteria>

<output>
Create `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-01-SUMMARY.md` when done.
Record in it: the verbatim census totals, the positive-control numbers, any pattern narrowed and
why, and the two-record end-to-end run output.
</output>
