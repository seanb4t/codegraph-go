---
phase: 06-benchmark-de-coupling-memory-sweep
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .claude/CLAUDE.md
  - .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md
autonomous: true
requirements: [MEM-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A session started after this plan reads `.claude/CLAUDE.md`, `.planning/PROJECT.md` and `.planning/STATE.md` with no statement asserting codegraph-go as a port or parity project in the present tense, and no forward-looking obligation to another implementation's behaviour (MEM-02)."
    - "Records of what was decided, measured or shipped at a point in time survive untouched — the sweep corrects standing assertions, never project history (D-13's test applied to files)."
    - "No standing constraint names a capability the project no longer has: the Compatibility constraint's binding clause no longer cites the migration command Phase 5 removed, and STATE.md's Core value line no longer describes importing a legacy index."
    - "`.claude/CLAUDE.md`'s GSD-generated blocks agree with their named source files, so the next regeneration cannot resurrect the swept wording."
    - "Every occurrence found in the three files carries a recorded verdict (sweep / keep-historical) with a reason in `06-MEMORY-SWEEP.md`; none was dropped silently."
  artifacts:
    - ".planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md — created here with its agent-facing-files section; 06-06 appends the engram sections"
    - ".planning/PROJECT.md — identity paragraph, Constraints and forward-looking backlog/context prose corrected"
    - ".claude/CLAUDE.md — generated blocks re-synced from their sources"
    - ".planning/STATE.md — Core value line corrected"
  key_links:
    - ".planning/PROJECT.md is the source for .claude/CLAUDE.md's GSD project block — editing only the mirror lets the next regeneration restore the swept text"
    - ".planning/STATE.md's Core value line mirrors .planning/PROJECT.md's Core Value section — the two must end up saying the same thing"
    - "PROJECT.md's Compatibility constraint cites a command Phase 5 removed — the framing defect and the stale-capability defect are the same line"
  prohibitions:
    - "MUST NOT invent, rename, reorder or remove any heading, front-matter key, table column or GSD marker comment in `.planning/PROJECT.md`, `.planning/STATE.md` or `.claude/CLAUDE.md`. These are tool-owned generated files: filling in a value is ordinary work, inventing structure silently changes what the tooling sees."
    - "MUST NOT rewrite a past-tense record of what was decided, measured or shipped — doing so falsifies project history, the same failure mode as editing `.planning/` archives."
    - "MUST NOT edit `NOTICE` or `LICENSE`: the upstream MIT attribution is a licence obligation and `NOTICE` is the single sanctioned place for it."
    - "MUST NOT touch `.planning/milestones/`, any earlier phase directory, or `CHANGELOG.md`."
---

<objective>
Correct the agent-facing framing files so a session started after this phase reads codegraph-go as
a project in its own right rather than as a port.

Purpose: MEM-02's stated outcome is defeated by these three files regardless of the engram spine's
state (D-12). Two of the occurrences are compound defects — they assert retired framing AND name a
capability the project no longer has, because the migration command was removed in Phase 5.

Output: corrected identity, constraint and core-value text in the three files, their GSD-generated
mirror blocks re-synced from their sources, and a `06-MEMORY-SWEEP.md` recording a verdict for every
occurrence found.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-RESEARCH.md
@.planning/phases/06-benchmark-de-coupling-memory-sweep/06-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See `06-01-PLAN.md` → "Artifacts this phase produces" for the full created/deleted register.
This plan CREATES `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md`
(agent-facing-files section only; 06-06 appends the engram sections to the same file). It deletes
no symbol and no file.
</artifacts_this_phase_produces>

<edge_probe_coverage>
Spec-less probe fallback (no SPEC.md for this phase). Row owned by this plan:

| Requirement | Category | Status | Disposition |
|---|---|---|---|
| MEM-02 | unclassified | **unresolved** | **Flagged assumption — not auto-resolved.** Assumption carried by this plan: "recalls no memory describing codegraph-go as a port or parity project in the present tense" covers BOTH the durable memory store and the in-repo files a session reads at startup, because a session's recall is the union of the two. This plan closes the file half; 06-06 closes the store half. If a reviewer reads MEM-02 as store-only, this plan's work is still correct but is scoped beyond the letter of the requirement — deliberately, per D-12. |
</edge_probe_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every framing occurrence in the three files and record a verdict for each</name>
  <files>.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md</files>
  <read_first>
    - `.claude/CLAUDE.md` in full — note the GSD start/end marker comments at lines 1, 19, 21, 141, 143, 148, 150, 155, 157, 162, 164, 177, 179, 185, which say which blocks are generated and from which source file
    - `.planning/PROJECT.md` in full — the identity paragraph (`## What This Is`, line 5), `## Core Value` (line 9), `## Constraints` (240-246), the Backlog bullets (219-220), the Context section (232-236), the Key Decisions table, and the footer change log
    - `.planning/STATE.md` lines 20-35 — the `## Project Reference` block and its Core value line
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CONTEXT.md` §D-12 and §D-13 — the file list and the present-tense-or-forward-looking test
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-CENSUS.md` as left by 06-04 — the bounded-pattern discipline to reuse
    - `$HOME/.claude/rules/planning-artifacts.md` — why these files may take value edits but never invented structure
  </read_first>
  <action>
Enumerate before editing. Run a bounded multiline census over exactly `.claude/CLAUDE.md`,
`.planning/PROJECT.md` and `.planning/STATE.md`, and record every hit with a verdict.

Pattern set (word-bounded, `rg -U -o`, counted with `wc -l`, never `rg -c`): parity, port, ported,
drop-in, upstream, "rewrite of", "TS CodeGraph", colbymchenry, reimplementation. Prove the instrument
on a planted wrapped fixture first — exactly as `06-CENSUS.md` records for the code surface — before
believing any count.

Apply D-13's test, unchanged, to prose:
- **Sweep** an occurrence that states the framing as a current standing fact ("X is the baseline")
  or as a future obligation ("must maintain X", "triggers once X lands").
- **Keep, untouched** an occurrence that records what was decided, measured or shipped at a point in
  time. Those statements are true, and rewriting them falsifies project history — the same failure
  mode as editing `.planning/` archives. PROJECT.md's milestone records, validated-requirement
  entries, Key Decisions outcomes and Context history are overwhelmingly this class, and the file's
  own footer already states they are deliberately left intact.

Occurrences already identified during planning — each needs a recorded verdict, and the census must
find them. If the census misses one, the instrument is wrong, not the finding.

| Location | What is there | Recommended verdict |
|---|---|---|
| `.planning/PROJECT.md` `## What This Is` (line 5) | the identity paragraph's closing origin clause | sweep — it sits in the paragraph a session reads as the project's current identity; the licence attribution stays in `NOTICE`, the single sanctioned site PROJECT.md's own Licensing constraint already names |
| `.claude/CLAUDE.md` line 7 | the same clause, inside the generated project block | sweep via the source — fix PROJECT.md, then re-sync the block |
| `.claude/CLAUDE.md` line 15 (Compatibility) | a standing "functionality baseline" constraint | sweep — this is a **stale mirror**: PROJECT.md line 244 already retired this constraint at v0.11.0 scoping. Re-sync rather than re-author |
| `.claude/CLAUDE.md` line 17 (Licensing) | "Original is MIT — port with attribution" | sweep — also a stale mirror of PROJECT.md line 246's current wording |
| `.planning/PROJECT.md` line 244 (Compatibility) | the clause claiming the legacy-index read contract still binds | sweep — a **compound defect**: a present-tense standing constraint that names a capability Phase 5 removed (CODE-03) |
| `.planning/PROJECT.md` line 219 (Backlog) | "triggers once parity lands" | sweep — forward-looking trigger condition |
| `.planning/PROJECT.md` line 220 (Backlog) | parity used as the scoping yardstick for worktree work | sweep — restate the scope by naming the shipped behaviour a future item would go beyond |
| `.planning/PROJECT.md` line 234 (Context) | a present-tense operating description plus a forward-looking consequence clause | sweep — present-tense, forward-looking, and factually stale |
| `.planning/STATE.md` line 26 (Core value) | an uninstall/install/import-index sentence | sweep — a **compound defect**: retired framing plus a removed capability. Replace with the statement PROJECT.md's `## Core Value` section already carries, so mirror and source agree |
| `.claude/CLAUDE.md` line 93 (stack alternatives table) | a "don't block v1 parity on this" aside | verdict required — check whether this block still matches its named source (`.planning/research/STACK.md`). If it is a stale mirror, re-sync; if the phrase is live in the source, sweep the source and re-sync |
| `.planning/PROJECT.md` lines 31, 40, 45, 50-51, 58, 62-66, 143-180, 236, 252 | milestone records, phase-validation entries, scoping notes, Key Decisions outcomes | keep-historical — past-tense records of what happened |
| `.planning/STATE.md` lines 115, 188 | deferral records citing a deleted document | keep-historical — records of a decision, not standing assertions |

Write `06-MEMORY-SWEEP.md` with front matter naming the phase, a short statement of D-13's test, and
an `## Agent-facing framing files (D-12)` section containing one table row per occurrence: file,
line, quoted text, verdict, reason. Include the keep-historical rows — an enumeration listing only
what changed cannot be checked for completeness. Record the census instrument, its positive-control
numbers and its raw hit count in the same section.

Do NOT edit any of the three files in this task. This task produces the decision record the next
task executes.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md && rg -q '^## Agent-facing framing files' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md && ROWS=$(rg -o '^\| `?\.(claude|planning)/' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md | wc -l | tr -d ' '); SWEEP=$(rg -o -F '| sweep |' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md | wc -l | tr -d ' '); KEEP=$(rg -o -F '| keep-historical |' .planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md | wc -l | tr -d ' '); echo "SWEEP_ROWS_TOTAL=$ROWS SWEEP_VERDICTS=$SWEEP KEEP_VERDICTS=$KEEP"; test "$ROWS" -ge 9 && test "$SWEEP" -ge 1 && test "$KEEP" -ge 1 && git diff --quiet -- .planning/PROJECT.md .planning/STATE.md .claude/CLAUDE.md && echo "TARGET_FILES_UNTOUCHED_THIS_TASK=true"</automated>
  </verify>
  <acceptance_criteria>
    - `06-MEMORY-SWEEP.md` exists with an `## Agent-facing framing files (D-12)` section.
    - The enumeration prints `SWEEP_ROWS_TOTAL=<n>` with n >= 9, and both `SWEEP_VERDICTS` and `KEEP_VERDICTS` >= 1. A record containing only sweep verdicts FAILS: an enumeration that omits the untouched population cannot be checked for completeness.
    - Every row carries a file, a line, quoted text, a verdict of exactly `sweep` or `keep-historical`, and a non-empty reason.
    - The census instrument, its positive-control numbers (multiline count strictly greater than the line-based count over the same planted fixture, and greater than zero) and its raw hit count are recorded verbatim.
    - Every occurrence named in the planning table appears as a row. If the census failed to find one, the SUMMARY records the pattern that missed it and the correction.
    - `git diff --quiet -- .planning/PROJECT.md .planning/STATE.md .claude/CLAUDE.md` exits 0 (`TARGET_FILES_UNTOUCHED_THIS_TASK=true`) — enumeration precedes editing, so the verdicts are a decision record rather than a post-hoc rationalisation.
  </acceptance_criteria>
  <done>Every framing occurrence across the three agent-facing files is enumerated with a verdict and a reason, the keep-historical population is listed alongside the sweep population, and the census instrument was proven before its counts were believed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the sweep verdicts and re-sync the generated mirror blocks</name>
  <precondition>`06-MEMORY-SWEEP.md`'s agent-facing-files section is committed and carries a verdict for every enumerated occurrence (Task 1 complete). Editing before the verdicts exist turns the record into a rationalisation.</precondition>
  <files>.planning/PROJECT.md, .planning/STATE.md, .claude/CLAUDE.md</files>
  <read_first>
    - `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-MEMORY-SWEEP.md` as left by Task 1 — the verdict table this task executes
    - `.planning/PROJECT.md` `## What This Is` (5), `## Core Value` (9), `## Constraints` (240-246), Backlog (219-220), Context (232-236), footer (299) — read the current text before replacing any of it
    - `.claude/CLAUDE.md` lines 1-19 and 21-141 — the generated project and stack blocks and their `source:` attributes
    - `.planning/research/STACK.md` — the named source for the stack block
    - `.planning/STATE.md` lines 20-35 — the `## Project Reference` block
    - `$HOME/.claude/rules/planning-artifacts.md`
  </read_first>
  <action>
Execute exactly the verdicts Task 1 recorded. Every edit is a **value** edit inside an existing
section: no heading, no front-matter key, no table column and no GSD marker comment is added,
renamed, reordered or removed. These files are parsed by scope-sensitive tooling, and inventing
structure silently changes what that tooling sees.

**Source-before-mirror.** For each occurrence inside a GSD-generated block in `.claude/CLAUDE.md`,
edit the named source file first, then bring the block into agreement with it. Editing only the
mirror leaves the swept wording to be restored at the next regeneration. Two CLAUDE.md occurrences
(the Compatibility and Licensing constraints) are already stale mirrors of PROJECT.md text corrected
at v0.11.0 scoping — those are re-syncs, not re-authorings, and the current PROJECT.md wording is
authoritative. If the stack block is likewise stale relative to `.planning/research/STACK.md`,
re-sync it and record in the SUMMARY that a fourth file was read (not necessarily edited) for that
purpose.

`.planning/PROJECT.md`:
- `## What This Is`: keep the description of what the project is; remove the origin clause from that
  paragraph. Do not restate the attribution anywhere in this file — the Licensing constraint already
  names `NOTICE` as the single sanctioned site, and that constraint stays.
- `## Constraints` → Compatibility: keep the retirement statement, its date, and the note that it
  supersedes the earlier ruling — all true records. Correct the binding clause: it names a migration
  command Phase 5 removed, so the sentence claiming that contract still binds is false. State
  plainly that nothing of that shape binds any more, and cite the Phase 5 removal so a reader can
  trace the change rather than wonder whether it was an oversight.
- Backlog bullets: restate the seed's trigger condition and the worktree scope on the project's own
  terms — name the shipped behaviour a future item would go beyond.
- Context section: correct the present-tense operating description and its forward-looking
  consequence clause. The surrounding historical paragraphs stay.
- Do not touch any milestone record, phase-validation entry or Key Decisions outcome. Append this
  phase's entry to the footer change log ONLY if that is the file's existing convention — match the
  existing pattern, never invent one.

`.planning/STATE.md`:
- `## Project Reference` → the Core value line: replace its text with the statement PROJECT.md's
  `## Core Value` section carries, so mirror and source agree. Keep the line's existing bold label
  and position. Change nothing else in the file — no front-matter key, no progress number, no
  heading, no table.

`.claude/CLAUDE.md`:
- Re-sync the generated project block from the corrected PROJECT.md, and the generated stack block
  from its source if Task 1's verdict found it stale. Leave the profile block entirely alone — it is
  generator-managed and its own comment says not to edit it manually.
- Byte-preserve every marker comment line.
  </action>
  <verify>
    <automated>N=$(rg -U -o -i -e '\bparity\b' -e '\bdrop-in\b' -e '\bported\b' -e '\brewrite of\b' -e '\bTS CodeGraph\b' -e '\bcolbymchenry\b' .claude/CLAUDE.md | wc -l | tr -d ' '); echo "CLAUDE_MD_FRAMING_TOTAL=$N"; test "$N" = "0" && M=$(rg -U -o -i -e 'migrate their indexes' -e 'uninstall TS CodeGraph' .planning/STATE.md | wc -l | tr -d ' '); echo "STATE_MD_RETIRED_CORE_VALUE=$M"; test "$M" = "0" && CB=$(rg -o -F 'Compatibility' .planning/PROJECT.md | wc -l | tr -d ' '); echo "PROJECT_MD_COMPATIBILITY_BULLET_PRESENT=$CB"; test "$CB" -ge 1 && CM=$(rg '^\- \*\*Compatibility\*\*' .planning/PROJECT.md | rg -o -F 'codegraph migrate' | wc -l | tr -d ' '); echo "COMPATIBILITY_BULLET_STALE_CAPABILITY_REFS=$CM"; test "$CM" = "0" && MK=$(rg -o 'GSD:[a-z-]+-(start|end)' .claude/CLAUDE.md | wc -l | tr -d ' '); echo "GSD_MARKERS=$MK"; test "$MK" = "14" && node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query state.load >/dev/null 2>&1 && echo "STATE_MD_PARSES=true"</automated>
    <human-check>Start a fresh session in this repository and read the startup context it assembles (`.claude/CLAUDE.md` plus whatever the harness injects from `.planning/`). Confirm nothing in it describes codegraph-go as a port, a parity project, or a drop-in replacement in the present tense, and nothing names a capability the binary no longer has. This is MEM-02's live verification and cannot be run by a test.</human-check>
  </verify>
  <acceptance_criteria>
    - `CLAUDE_MD_FRAMING_TOTAL=0`, counted with `rg -o | wc -l`. This is a whole-file gate for `.claude/CLAUDE.md` because that file is agent instructions, not project history.
    - `STATE_MD_RETIRED_CORE_VALUE=0` — the Core value line no longer describes uninstalling another tool or importing a legacy index.
    - Region-scoped, NOT file-wide: the Compatibility bullet still exists (`PROJECT_MD_COMPATIBILITY_BULLET_PRESENT` >= 1) AND no longer names the removed migration command (`COMPATIBILITY_BULLET_STALE_CAPABILITY_REFS=0`). A file-wide ban would be unsatisfiable — PROJECT.md legitimately records that command in its v0.1 milestone history, which must survive.
    - `GSD_MARKERS=14` — every generated-block marker comment survives byte-intact; none added, none removed.
    - `.planning/STATE.md` still parses through the GSD state reader (`STATE_MD_PARSES=true`), proving no structure was disturbed.
    - The keep-historical population is provably untouched: `git diff -U0 -- .planning/PROJECT.md | rg -c '^[+-]' ` is recorded verbatim in the SUMMARY and every changed line maps to a row whose verdict was `sweep`. A changed line with no matching sweep row fails this task.
    - `git diff --quiet -- NOTICE LICENSE CHANGELOG.md` exits 0.
    - `git status --porcelain .planning/milestones .planning/phases/01-* .planning/phases/02-* .planning/phases/03-* .planning/phases/04-* .planning/phases/05-*` prints nothing.
  </acceptance_criteria>
  <done>The three agent-facing files carry no present-tense or forward-looking retired framing, no standing constraint names a removed capability, the generated mirror blocks agree with their sources, every changed line traces to a recorded sweep verdict, and the project's historical records are byte-intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session startup -> agent instructions | `.claude/CLAUDE.md` is injected as ground truth into every session in this repository |
| GSD tooling -> `.planning/*.md` | scope-sensitive parsers read these files; invented structure changes what they see with no error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14 | Tampering | `.planning/PROJECT.md` / `.planning/STATE.md` structure | high | mitigate | Value-only edits; a prohibition forbids adding, renaming or removing any heading, front-matter key or marker; Task 2 asserts the marker count and re-parses STATE.md through the GSD state reader |
| T-06-15 | Repudiation | project history in `.planning/PROJECT.md` | high | mitigate | Enumerate-then-edit ordering; every changed line must map to a row whose recorded verdict was `sweep`; the keep-historical population is enumerated explicitly so its survival is checkable |
| T-06-16 | Tampering | `NOTICE` / `LICENSE` | medium | mitigate | Prohibition plus a `git diff --quiet` assertion — the upstream MIT attribution is a licence obligation and must not be swept as framing |
| T-06-17 | Spoofing | the swept wording's durability | medium | mitigate | Source-before-mirror rule: generated blocks are re-synced from their named sources so the next regeneration cannot restore the retired text |
</threat_model>

<verification>
- `.claude/CLAUDE.md` framing census prints a verbatim 0 across the whole file.
- `.planning/STATE.md`'s Core value line carries the same statement as PROJECT.md's Core Value section and parses through the GSD state reader.
- The Compatibility bullet survives and no longer names the removed migration command (region-scoped, not file-wide).
- All 14 GSD marker comments intact; `NOTICE`, `LICENSE`, `CHANGELOG.md` and every earlier phase directory untouched.
- `06-MEMORY-SWEEP.md` enumerates both the swept and the kept populations with reasons.
</verification>

<success_criteria>
MEM-02's file half is satisfied: the three files a session reads at startup describe codegraph-go on
its own terms, name no capability it no longer has, and keep every true record of what happened.
The store half is 06-06's.
</success_criteria>

<output>
Create `.planning/phases/06-benchmark-de-coupling-memory-sweep/06-05-SUMMARY.md` when done.
Record: the census numbers, the sweep/keep verdict counts, the verbatim changed-line count for
PROJECT.md with each line mapped to its sweep row, and whether the stack block needed a re-sync.
</output>
