---
phase: 05-language-coverage-resolution-breadth
plan: 13
type: execute
wave: 7
depends_on: [05-09, 05-10, 05-11, 05-12]
files_modified:
  - docs/LANGUAGE-CAPABILITY-MATRIX.md
  - internal/indexer/capability/matrix.go
  - internal/indexer/capability/matrix_test.go
autonomous: true
requirements: [LANG-06]
must_haves:
  truths:
    - "A committed human-readable capability matrix documents every language's extraction/resolution/dispatch/routing coverage as full | partial | none"
    - "A machine-readable capability descriptor per language mirrors the human-readable matrix and is consistency-checked against the registered languages"
    - "Priority-4 are full across the board; mainstream-6 are extraction + best-effort with every gap named; documented-partial gaps are written down, not silently missing"
    - "Wave A's generalization did not regress Go's own golden-parity fixture (cross-language regression pass green)"
  artifacts:
    - docs/LANGUAGE-CAPABILITY-MATRIX.md
    - internal/indexer/capability/matrix.go
  key_links:
    - "capability descriptor entries ↔ registered languages (a full entry must have a green golden-parity test; a partial entry must name its gap)"
---

<objective>
Ship the D-11 language capability matrix — a committed human-readable doc AND a machine-readable capability descriptor per language (extraction / resolution / dispatch / routing: full | partial | none) — and run the cross-language regression closeout. Priority-4 are full across the board; mainstream-6 are extraction + best-effort with every documented-partial gap named (including the C# partial-class decision, the shared external-scanner crash caveat, Swift/Kotlin coverage/none-coverage, and each mainstream language's enumerated gaps). Confirm Wave A's generalization did not regress Go's golden-parity fixture.

Purpose: The coverage contract that makes "documented-partial" mean "written down," and the final regression gate.
Output: `docs/LANGUAGE-CAPABILITY-MATRIX.md`, `internal/indexer/capability/matrix.go`, and a green full-suite regression.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@.planning/phases/05-language-coverage-resolution-breadth/05-VALIDATION.md
@internal/indexer/languages.go
@internal/indexer/goextract/types.go
@.planning/phases/05-language-coverage-resolution-breadth/05-05-SUMMARY.md
@.planning/phases/05-language-coverage-resolution-breadth/05-08-SUMMARY.md
@.planning/phases/05-language-coverage-resolution-breadth/05-10-SUMMARY.md
@.planning/phases/05-language-coverage-resolution-breadth/05-11-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's contribution)

- `docs/LANGUAGE-CAPABILITY-MATRIX.md` — human-readable matrix (rows: go, java, csharp, python, typescript, tsx, javascript, rust, ruby, php, c, cpp, swift, kotlin; columns: extraction/resolution/dispatch/routing = full|partial|none, with a named gap for every non-full cell)
- `internal/indexer/capability/matrix.go` — `map[string]CapabilityEntry` machine-readable descriptor + accessor
- `internal/indexer/capability/matrix_test.go` — consistency check (descriptor ↔ registered languages; full-entry ↔ green test; partial-entry ↔ named gap)

<tasks>

<task type="auto">
  <name>Task 1: machine-readable capability descriptor + human-readable matrix</name>
  <read_first>
    - internal/indexer/languages.go (registered language IDs — the matrix must cover exactly these)
    - internal/indexer/goextract/types.go (KindRoute + "implements" edge kind — what dispatch/routing coverage means)
    - .planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md D-11 (full|partial|none per capability; documented-partial = written down)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Validation Architecture" test map (row structure), §"Pitfall 4" (shared external-scanner caveat), §"Pitfall 5" (C# partial class)
    - The per-language SUMMARYs (05-04..05-11) for the actual coverage achieved + each enumerated gap and the Swift/Kotlin checkpoint outcome
  </read_first>
  <action>
    Create internal/indexer/capability/matrix.go (package capability) with a `CapabilityEntry` struct carrying per-capability coverage (`Extraction`, `Resolution`, `Dispatch`, `Routing`, each a `full|partial|none` value) plus a `Gaps []string` field naming each non-full gap, and a package-level `var matrix = map[string]CapabilityEntry{}` keyed by language ID following the registry-keyed-by-ID pattern, with an accessor (`Lookup(id string) (CapabilityEntry, bool)`). Populate it from the ACTUAL coverage achieved per the per-language SUMMARYs: priority-4 (go, java, csharp, python, typescript, tsx, javascript) = full extraction+resolution+dispatch, routing=full where a framework detector exists; mainstream (rust, ruby, php, c, cpp) = full/partial extraction + partial best-effort resolution + none dispatch/routing unless implemented, with each gap named; swift, kotlin = the coverage recorded by 05-11 (partial) or none (if 05-08's checkpoint was rejected). Write docs/LANGUAGE-CAPABILITY-MATRIX.md as a human-readable table mirroring the descriptor exactly, including the shared caveats (external-scanner in-process crash risk bounded by MaxSourceBytes; C# partial-class node-identity decision; `.h` C/C++ disposition; Swift/Kotlin `[SUS]` grammar provenance). Use the Validation Architecture test-map rows as the row structure. Every non-full cell MUST have a named gap in both artifacts (D-11 "documented-partial = written down, not silently missing").
  </action>
  <verify>
    <automated>go test ./internal/indexer/capability/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - matrix.go covers exactly the registered language IDs from languages.go (no missing, no extra) — asserted in matrix_test.go
    - Every capability cell is one of full|partial|none; every non-full cell has a corresponding named gap in `Gaps`
    - docs/LANGUAGE-CAPABILITY-MATRIX.md table matches the descriptor (same coverage values, same gaps)
    - Priority-4 rows are full across extraction+resolution+dispatch; the Swift/Kotlin rows match 05-08/05-11's outcome
  </acceptance_criteria>
  <done>The capability matrix exists in both human- and machine-readable form, covering every registered language with every gap named.</done>
</task>

<task type="auto">
  <name>Task 2: cross-language regression closeout + matrix↔validation consistency check</name>
  <read_first>
    - internal/indexer/capability/matrix.go (the descriptor to cross-check)
    - testdata/golden/golden_parity_test.go + the per-language parity tests (TestGoldenParity_Java/_CSharp/_Python/_TSJS) added in Waves 4
    - .planning/phases/05-language-coverage-resolution-breadth/05-VALIDATION.md §"Sampling Rate" / §"Phase gate" (a full entry must have a corresponding green golden-parity test)
  </read_first>
  <action>
    Add a matrix_test.go consistency check enforcing the D-11 phase gate: a `full` resolution/dispatch entry for a priority-4 language must correspond to an existing (self-skipping) golden-parity test function, and every `partial` entry must have a non-empty `Gaps` list. Run the full suite (`go test -race ./...`) as the cross-language regression closeout and confirm: (a) Go's own golden-parity fixture (weft-go) is still green — Wave A's generalization did not regress the first language; (b) the priority-4 parity tests self-skip cleanly when their corpora are absent and pass when present; (c) the determinism gate (two from-scratch rebuilds byte-identical) still holds across the multi-language pipeline. Record any residual gaps or fallbacks (e.g. live-TS-CLI-unavailable source-as-spec validation) in the matrix and the SUMMARY.
  </action>
  <verify>
    <automated>go test -race ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - matrix_test.go asserts every `full` priority-4 entry maps to an existing golden-parity test and every `partial` entry names ≥1 gap
    - `go test -race ./...` is green (Go golden-parity fixture not regressed; per-language parity tests self-skip or pass)
    - The determinism gate still passes across the generalized multi-language pipeline
  </acceptance_criteria>
  <done>The matrix is consistency-checked against actual validation coverage and the full multi-language suite is green with no Go regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability descriptor → agent/consumer | the matrix is the authoritative coverage contract consumers rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-Overclaim | Repudiation | capability matrix accuracy | medium | mitigate | matrix_test.go enforces full-entry↔green-test and partial-entry↔named-gap consistency, so the coverage contract cannot silently overclaim |
| T-05-Caveat | Information Disclosure | undocumented crash/coverage caveats | low | mitigate | The shared external-scanner in-process crash caveat and each language's gaps are written into the matrix (D-11), not left implicit |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package-manager installs in this plan |
</threat_model>

<verification>
- `go test -race ./... -count=1` green (full multi-language suite, Go not regressed)
- Matrix covers exactly the registered languages; every non-full cell has a named gap
- Determinism gate holds across the generalized pipeline
</verification>

<success_criteria>
The language capability matrix ships in human- and machine-readable form with every gap named and consistency-checked against actual validation coverage, and the full multi-language suite is green with no regression to Go's golden parity.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-13-SUMMARY.md` when done.
</output>
