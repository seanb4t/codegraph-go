---
phase: 05-language-coverage-resolution-breadth
plan: 10
type: tdd
wave: 5
depends_on: [05-08]
files_modified:
  - internal/indexer/mainstream/rustextract/rustextract.go
  - internal/indexer/mainstream/rustextract/rustextract_test.go
  - internal/indexer/mainstream/rubyextract/rubyextract.go
  - internal/indexer/mainstream/rubyextract/rubyextract_test.go
  - internal/indexer/mainstream/phpextract/phpextract.go
  - internal/indexer/mainstream/phpextract/phpextract_test.go
  - internal/indexer/languages_rust.go
  - internal/indexer/languages_ruby.go
  - internal/indexer/languages_php.go
autonomous: true
requirements: [LANG-06]
must_haves:
  truths:
    - "Rust, Ruby, and PHP each get structural extraction into the shared vocabulary at documented-partial coverage (extraction + best-effort same-file/same-module resolution)"
    - "Each is registered through the language registry with a project-descriptor hook (Cargo.toml, Gemfile/path, composer.json) and path-identity fallback"
    - "Every documented gap is named for the D-11 capability matrix — no silent omissions"
  artifacts:
    - internal/indexer/mainstream/rustextract/rustextract.go
    - internal/indexer/mainstream/rubyextract/rubyextract.go
    - internal/indexer/mainstream/phpextract/phpextract.go
    - "languages_rust.go / languages_ruby.go / languages_php.go registrations"
  key_links:
    - "languages_<lang>.go init() → registerLanguage(spec: New<Lang>Parser + <lang>extract.Extract + ModuleKey)"
---

<objective>
Deliver mainstream-tier extraction for Rust, Ruby, and PHP (LANG-06) at documented-partial coverage: extraction + best-effort same-file/same-module resolution per D-04's tiering, each registered through the language registry with a project-descriptor hook. Every coverage gap is named for the D-11 matrix.

Purpose: Half of the mainstream-6 tier; uses the grammar constructors from 05-08.
Output: three mainstream extractor packages + registrations + self-consistency tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@.planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md
@internal/indexer/goextract/goextract.go
@internal/indexer/goextract/types.go
@internal/indexer/languages.go
@internal/parser/cgo/parser_cgo.go
</context>

## Artifacts this phase produces (this plan's contribution)

- `internal/indexer/mainstream/rustextract/rustextract.go`, `rubyextract/rubyextract.go`, `phpextract/phpextract.go` — each `Extract(p, moduleKey, relPath, src)`
- `internal/indexer/languages_rust.go`, `languages_ruby.go`, `languages_php.go` — registrations (IDs "rust"/.rs→NewRustParser, "ruby"/.rb→NewRubyParser, "php"/.php→NewPHPParser)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rust + Ruby extractors + registrations</name>
  <behavior>
    - A Rust file (structs, enums, traits, impls, fns) extracts nodes mapped to the shared vocabulary (struct→KindStruct, trait→KindInterface, fn/method→KindFunction/KindMethod); use statements→RefKindImports; calls→RefKindCalls
    - A Ruby file (classes, modules, methods) extracts nodes (class→KindStruct, module→KindStruct or a documented mapping, def→KindMethod/KindFunction); require/require_relative→RefKindImports; calls→RefKindCalls
    - Both register by ID and extension and dispatch to their extractor; parse-failure skip contract preserved (both grammars have external scanners)
    - Coverage is documented-partial: any resolution gap (Rust macro-generated items, Ruby dynamic method definition/metaprogramming) is named, not silently missing
  </behavior>
  <read_first>
    - internal/indexer/goextract/goextract.go (skip contract + collect-phase ordering + call collection to mirror lightly)
    - internal/indexer/goextract/types.go (shared vocabulary)
    - internal/indexer/languages.go + languages_go.go (registration shape)
    - .planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md D-04 (mainstream = extraction + best-effort, gaps documented), D-11
    - .planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md §"mainstream/{rust,ruby,...}extract" (role-match, lighter)
  </read_first>
  <action>
    Create packages rustextract and rubyextract, each with `Extract(p, moduleKey, relPath, src)` reproducing goextract's skip/error contract and importing the shared `goextract` vocabulary. Rust (`tree-sitter-rust` v0.24.2): map `struct_item`→KindStruct, `enum_item`→KindStruct (documented), `trait_item`→KindInterface, `function_item`→KindFunction, `impl_item` methods→KindMethod, `use_declaration`→RefKindImports, `call_expression`→RefKindCalls; ModuleKey from Cargo.toml crate + module path (path-identity fallback). Ruby (`tree-sitter-ruby` v0.23.1): map `class`→KindStruct, `module`→KindStruct (documented), `method`/`singleton_method`→KindMethod, `call`→RefKindCalls, `require`/`require_relative`→RefKindImports; ModuleKey best-effort from require path / directory (path-identity fallback). Create languages_rust.go and languages_ruby.go registering each (IDs "rust"/".rs"/NewRustParser and "ruby"/".rb"/NewRubyParser). Keep resolution best-effort (same-file/same-module only per D-04); explicitly enumerate documented gaps (Rust macros, Ruby metaprogramming) for the D-11 matrix in the SUMMARY.
  </action>
  <verify>
    <automated>go test ./internal/indexer/mainstream/rustextract/ ./internal/indexer/mainstream/rubyextract/ ./internal/indexer/ -run 'TestRust|TestRuby|TestLanguageRegistry' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - rustextract_test.go and rubyextract_test.go (table-driven) assert node-kind mapping + RefKind emission for their core constructs
    - `lookupLanguageByID`/`lookupLanguageByExt` resolve "rust"/.rs and "ruby"/.rb
    - Parse-failure skip contract asserted for both
    - Documented gaps enumerated in SUMMARY for the matrix
  </acceptance_criteria>
  <done>Rust and Ruby extract into the shared vocabulary at documented-partial coverage and are registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PHP extractor + registration + self-consistency</name>
  <behavior>
    - A PHP file (classes, interfaces, traits, functions, methods) extracts nodes mapped to the shared vocabulary; use/namespace→RefKindImports; calls→RefKindCalls; `implements`/`extends` clauses emit supertype refs
    - The `<?php ?>` tag-switching external scanner parse-failure path preserves the skip contract
    - PHP registers by ID "php" / extension ".php" via NewPHPParser (php/src grammar)
    - A self-consistency test proves a from-scratch re-index of a PHP fixture is deterministic (byte-identical export after normalizing volatile fields)
  </behavior>
  <read_first>
    - internal/indexer/goextract/goextract.go (skip contract + call collection)
    - internal/indexer/goextract/types.go (shared vocabulary)
    - internal/indexer/languages.go (registration + ModuleKey)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Standard Stack" (php/src not php_only), §"Validation Architecture" (mainstream self-consistency)
  </read_first>
  <action>
    Create package phpextract with `Extract(p, moduleKey, relPath, src)` reproducing the skip contract and importing the shared vocabulary. PHP (`tree-sitter-php` v0.24.2, php/src grammar): map `class_declaration`→KindStruct, `interface_declaration`→KindInterface, `trait_declaration`→KindStruct (documented), `function_definition`/`method_declaration`→KindFunction/KindMethod, `namespace_use_declaration`→RefKindImports, `function_call_expression`/`member_call_expression`→RefKindCalls, `base_clause`/`class_interface_clause`→RefKindEmbeds-shaped supertype refs. ModuleKey from PHP namespace declaration (composer.json PSR-4 autoload map best-effort, path-identity fallback). Create languages_php.go registering ID "php"/".php"/NewPHPParser. Add a self-consistency test: index a small PHP fixture twice from scratch and assert byte-identical export after normalizing volatile fields (mirroring the INDX-02 determinism gate approach) — this is the mainstream-tier validation bar (D-12 lighter check). Enumerate documented gaps for the D-11 matrix.
  </action>
  <verify>
    <automated>go test ./internal/indexer/mainstream/phpextract/ ./internal/indexer/ -run 'TestPHP|TestLanguageRegistry' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - phpextract_test.go (table-driven) asserts node-kind mapping + RefKind emission incl. supertype clauses
    - `lookupLanguageByID("php")` / `lookupLanguageByExt(".php")` resolve; php/src grammar used
    - Self-consistency: two from-scratch indexes of the PHP fixture produce byte-identical export (normalized)
  </acceptance_criteria>
  <done>PHP extracts into the shared vocabulary at documented-partial coverage, is registered, and is deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust/Ruby/PHP source → grammars | untrusted source parsed in-process (all three carry external scanners) |
| Cargo.toml/Gemfile/composer.json → Descriptor | untrusted manifest read for module identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-DoS | Denial of Service | Rust/Ruby/PHP external-scanner grammars | high | mitigate | `MaxSourceBytes` bounds every parse; in-process crash risk is the accepted Phase-1 contract (Pitfall 4), documented as a shared caveat in the D-11 matrix |
| T-05-Manifest | Spoofing | Cargo.toml/Gemfile/composer.json Descriptor | low | accept | Malformed manifest → path-identity fallback; no crash/code-exec |
| T-05-SC | Tampering | tree-sitter-rust/ruby/php modules | low | accept | `tree-sitter`-org modules pinned by exact semver in 05-08, VERIFIED |
</threat_model>

<verification>
- `go test ./internal/indexer/mainstream/... ./internal/indexer/ -count=1` green
- Priority-4 + Go fixtures unaffected by the new registrations
</verification>

<success_criteria>
Rust, Ruby, and PHP extract into the shared vocabulary at documented-partial coverage, registered and deterministic, with every gap named for the D-11 matrix.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-10-SUMMARY.md` when done.
</output>
