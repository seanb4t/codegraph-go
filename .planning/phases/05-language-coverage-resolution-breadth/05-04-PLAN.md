---
phase: 05-language-coverage-resolution-breadth
plan: 04
type: tdd
wave: 4
depends_on: [05-03]
files_modified:
  - internal/indexer/javaextract/javaextract.go
  - internal/indexer/javaextract/types.go
  - internal/indexer/javaextract/javaextract_test.go
  - internal/indexer/languages_java.go
  - testdata/golden/parity_java_test.go
autonomous: true
requirements: [LANG-02]
must_haves:
  truths:
    - "A Java repo gets full structural extraction: classes, interfaces, methods, fields, imports mapped to the shared codegraph vocabulary"
    - "Cross-file resolution links Java imports, calls, and inheritance via the Java package-name module key"
    - "extends and implements supertype references are emitted as unresolved refs for resolve-time promotion (Pattern 2), not distinguished at parse time"
    - "Java parity is validated shape-not-byte against a real Java repo (or source-as-spec fallback)"
  artifacts:
    - internal/indexer/javaextract/javaextract.go
    - internal/indexer/languages_java.go
    - testdata/golden/parity_java_test.go
  key_links:
    - "languages_java.go init() → registerLanguage(java spec: NewJavaParser + javaextract.Extract + Java ModuleKey)"
    - "Java package declaration → ModuleKey → symbolIndex outer key"
---

<objective>
Deliver full Java structural extraction + cross-file resolution (LANG-02): a `javaextract` package mirroring `goextract`'s shape, a Java `LanguageSpec` registration (package-name module key + `pom.xml`/`build.gradle` descriptor), and a shape-not-byte golden-parity validation against a real Java repo. Java leads the priority-4 tier because the target user's work is Java/C#-heavy.

Purpose: First non-Go language through the Wave 1-3 seam — proves the generalized pipeline end-to-end.
Output: `javaextract.Extract`, Java registry entry, Java golden-parity test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@.planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md
@internal/indexer/goextract/goextract.go
@internal/indexer/goextract/types.go
@internal/indexer/goextract/goextract_test.go
@internal/indexer/languages.go
@internal/indexer/resolve.go
@testdata/golden/golden_parity_test.go
</context>

## Artifacts this phase produces (this plan's contribution)

- `internal/indexer/javaextract/javaextract.go` — `Extract(p parser.Parser, moduleKey, relPath string, src []byte) (goextract.FileResult, error)`
- `internal/indexer/languages_java.go` — Java `LanguageSpec` registration (ID "java", extension ".java", `cgo.NewJavaParser`, Java package-name `ModuleKey`, `pom.xml`/`build.gradle` `Descriptor`)
- `testdata/golden/parity_java_test.go` — `TestGoldenParity_Java` (self-skips when corpus absent)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: javaextract package + Java LanguageSpec registration</name>
  <behavior>
    - A Java source file with a class, an interface, methods, and fields extracts nodes with kinds mapped to the shared vocabulary (KindStruct/class→struct or a documented Java kind, KindInterface, KindMethod, KindFunction where applicable, fields per the Go field-skip precedent)
    - `import` statements produce RefKindImports unresolved refs; method invocations produce RefKindCalls; `extends`/`implements` supertypes produce RefKindEmbeds-shaped unresolved refs (for resolve-time promotion to implements — Pattern 2)
    - A parse failure sets FileResult.Err and returns nil error (per-file skip contract)
    - `types` are collected before methods so same-file receiver/enclosing lookups succeed regardless of declaration order
    - The Java LanguageSpec resolves by ID "java" and extension ".java"; its ModuleKey returns the declared `package com.foo.bar;` name (independent of directory), falling back to path identity when absent
  </behavior>
  <read_first>
    - internal/indexer/goextract/goextract.go (Extract skip contract 19-51, extractor struct + ordered collect phases 58-113, collectCalls/recordCall)
    - internal/indexer/goextract/types.go (shared Kind*/RefKind*/FileResult vocabulary — import, do NOT redefine)
    - internal/indexer/languages.go (LanguageSpec, registerLanguage, ProjectDescriptor)
    - internal/indexer/languages_go.go (registration shape to mirror)
    - .planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md §"javaextract/javaextract.go (and csharpextract/pyextract/tsextract — same shape)"
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Pattern 2" (extends→implements promotion), §"Don't Hand-Roll" (Java package/classpath resolution), Assumptions Log A1
  </read_first>
  <action>
    Create package javaextract with `Extract(p parser.Parser, moduleKey, relPath string, src []byte) (goextract.FileResult, error)` reproducing goextract's exact skip/error contract (parse failure → FileResult.Err, nil function error; unexpected tree type → Err). Import the shared vocabulary from `goextract` (FileResult/ExtractedNode/IntraEdge/UnresolvedRef/Kind*/RefKind*) — never redefine it. Walk the Java tree-sitter grammar (`tree-sitter-java` v0.23.5, no external scanner) mapping node types: `class_declaration`→KindStruct (Java has no distinct struct; document the class→struct mapping in types.go doc), `interface_declaration`→KindInterface, `method_declaration`/`constructor_declaration`→KindMethod, `field_declaration` per the Go field-skip precedent (skip field nodes unless a field node kind is warranted — mirror goextract's decision), `import_declaration`→RefKindImports. For `superclass` (extends) and `super_interfaces` (implements), emit a single unresolved supertype reference shaped like RefKindEmbeds — do NOT try to distinguish class-extends-class from class-implements-interface at parse time (Pattern 2; resolve-time promotion happens in Wave 6). Collect types before methods (ordering discipline). Optionally use the grammar's `queries/tags.scm` to cut boilerplate (D-02) but keep vocabulary mapping in Go. Create languages_java.go (package indexer) with `init()` → `registerLanguage` for ID "java", Extensions [".java"], NewParser `cgo.NewJavaParser`, Extract `javaextract.Extract`, ModuleKey returning the in-source `package` declaration name (parse it from the file, or via descriptor), Descriptor parsing `pom.xml`/`build.gradle` for the group/base identity (path-identity fallback when absent, per D-03).
  </action>
  <verify>
    <automated>go test ./internal/indexer/javaextract/ ./internal/indexer/ -run 'TestJava|TestLanguageRegistry' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - javaextract_test.go is table-driven (mirroring goextract_test.go) asserting node-kind mapping for class/interface/method and RefKind emission for import/call/extends/implements
    - `lookupLanguageByID("java")` and `lookupLanguageByExt(".java")` return the Java spec
    - Java ModuleKey returns the declared package name for a `package com.foo;` file and a path-based key when no package is declared
    - Parse-failure skip contract asserted (bad source → FileResult.Err, nil error)
  </acceptance_criteria>
  <done>Java files extract into the shared vocabulary and the Java language is registered and resolvable through the pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Java cross-file resolution validation + golden parity</name>
  <behavior>
    - A two-file Java fixture (caller in one file, callee class in another, same package) resolves the cross-file call into a calls edge via the Java package-name module key
    - An imported symbol from another package resolves via the import→ModuleKey mapping
    - The golden-parity test self-skips when the Java corpus is not checked out (mirroring resolveWeftCorpus), and otherwise diffs shape (node/edge kind counts), not bytes
  </behavior>
  <read_first>
    - testdata/golden/golden_parity_test.go (resolveWeftCorpus skip-if-absent pattern lines 65-95, TestGoldenParity structure 322+, shape-not-byte comparison)
    - internal/indexer/resolve.go (resolveNameRef / resolveUnqualified / resolveSelector — how the module-key index resolves per-language)
    - .planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md D-12 (shape-not-byte parity), D-04 (priority-4 full resolution)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Environment Availability" (live TS CLI may be unavailable — source-as-spec fallback)
  </read_first>
  <action>
    Add a small multi-file Java resolution fixture under internal/indexer/javaextract/testdata (or the indexer test tree) exercising same-package cross-file calls and cross-package imports; assert the resolver produces the expected calls/imports edges through the generalized module-key index. Create testdata/golden/parity_java_test.go with `TestGoldenParity_Java` mirroring golden_parity_test.go's `resolveWeftCorpus` skip-if-absent discipline: define a Java corpus resolver that skips the test when the repo isn't present (env var or well-known local path), runs `codegraph index` / the indexer over it, and compares SHAPE (per-kind node counts, per-kind edge counts) against captured golden output — never byte equality (D-12). If the live TS CodeGraph v1.3.x CLI is unavailable to capture fresh golden output (per RESEARCH Environment Availability), fall back to a self-consistency + source-as-specification check and record that choice in the SUMMARY and the D-11 matrix entry (Wave 7). Select a representative, license-clean real Java repo (Claude's Discretion) and document the choice.
  </action>
  <verify>
    <automated>go test ./internal/indexer/... ./testdata/golden/ -run 'TestJava|TestGoldenParity_Java' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Cross-file Java resolution fixture asserts same-package and cross-package references resolve into edges
    - `TestGoldenParity_Java` self-skips cleanly when the corpus is absent (no hard failure in CI)
    - When the corpus is present, the shape diff (node/edge kind counts) is within the documented parity tolerance
  </acceptance_criteria>
  <done>Java is validated at full extraction + cross-file resolution, shape-not-byte, with a self-skipping parity harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Java source → tree-sitter-java | untrusted Java source parsed in-process |
| pom.xml/build.gradle → Descriptor | untrusted manifest read for module identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-DoS | Denial of Service | javaextract over adversarial source | high | mitigate | tree-sitter-java has NO external scanner (VERIFIED) and every parse routes through `MaxSourceBytes` — lowest crash-risk of the priority-4 grammars |
| T-05-Manifest | Spoofing | pom.xml/build.gradle Descriptor | low | accept | Malformed manifest degrades to path-identity fallback; no code execution, no crash |
| T-05-SC | Tampering | tree-sitter-java module | low | accept | `tree-sitter`-org module pinned at v0.23.5, VERIFIED against proxy.golang.org — no `[SUS]`/human-verify gate needed |
</threat_model>

<verification>
- `go test ./internal/indexer/javaextract/ ./internal/indexer/ ./testdata/golden/ -count=1` green (parity self-skips if corpus absent)
- Go golden-parity fixture still green (Java registration must not regress Go)
</verification>

<success_criteria>
Java has full structural extraction and cross-file resolution validated shape-not-byte on a real repo, registered through the multi-language seam without regressing Go.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-04-SUMMARY.md` when done.
</output>
