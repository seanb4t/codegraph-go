---
phase: 03-non-vacuity-proof-unconditional-ci-execution
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - .planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md
autonomous: true
requirements: [FIXT-07]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # FIXT-07: every assertion family has a recorded RED demonstration —
    # mutation applied, observed failure observed, revert byte-clean.
    - "All five assertion families ((a) CASES.json behavioral, (b) golden byte-identity, (c) CLI==MCP trio, (d) hermetic fail-loud resolution, (e) coverage guard) have a defining mutation applied, the family observed to go RED with the failing test named, and the mutation reverted byte-clean — each recorded in 03-MUTATION-LOG.md."
    # The D-01 call, recorded in the plan's objective and the log's header: RE-MUTATE
    # all five. (d)/(e) have 'specified'-but-no-transcript prior records; (b) has a
    # count-only record (25/26); re-mutation is cheap and produces pasted evidence.
    - "The D-01 call is recorded in 03-MUTATION-LOG.md's header: all five families are RE-MUTATED this phase because the criterion is 'the observed failure ... recorded per family' and the prior summaries for (d)/(e) carry no observed-failure transcript, (b) only a count; prior records are cited as corroboration, not primary evidence."
    # FIXT-07 reversibility: nothing lands — the repo is byte-clean after rehearsal.
    - "After every family's rehearsal, the repository working tree is byte-clean: `git status --porcelain` returns only the 03-MUTATION-LOG.md artifact (plus any pre-existing untracked files), and the final commit carries no mutation byte in testdata/golden/, corpus/, corpora/, or internal/."
    # FIXT-07/empty, as it applies to the suite's non-vacuity: a deleted assertion
    # family (anything the suite silently stops exercising) becomes red by removal.
    - "Each mutation targets a named current assertion (e.g. the len(locs) != 2 boundary in the overloaded case, the 26-golden enumeration, the cliOut == mcpOut byte comparison, lockedCorpusDir's os.Stat, CheckCoverage's threshold) and is observed red in that exact assertion before it is reverted."
  artifacts:
    - .planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md — the per-family formal record: family, test names, mutation applied, observed failing output pasted, revert command, byte-clean proof
  key_links:
    - "Each family's rehearsal runs against the same suite final-form files Plan 03-01 produced (golden_test.go unchanged by these expectations beyond the landing count test) — the proof is applied to the suite in the form CI will run."
  prohibitions:
    - "no mutation committed"
    - "no golden byte change lands"
    - "no assertion rewrite lands"
    - "no skip or cache-gating introduced by the rehearsal process"
    - "no reliance on t.Skip in any family being rehearsed"
---

<objective>
Prove the re-baselined golden suite is non-vacuous (FIXT-07): for each of the five assertion families, apply one defining mutation that must make the family go RED, observe and capture the failure, then revert byte-clean and record the whole rhythm in a visible artifact (03-MUTATION-LOG.md). Nothing lands — rehearsals are applied and reverted within the phase, per the repo's standing rule ("A gate is not trusted until it has been demonstrated RED against a confirmed-applied mutation").

D-01 call, recorded explicitly: **RE-MUTATE all five families this phase.** The research shows families (d) hermetic resolution and (e) coverage guard were "specified" to be RED-proven in Phase 1/2 with no observed-failure transcript found in the summaries; family (b) has a count-only record (25/26) without the failing output. The FIXT-07 criterion is "the observed failure … recorded per family", so re-mutating everything this phase — against the suite in its final form — is the cheapest and most complete reading: pasted failing output per family in one artifact, prior records cited as corroboration only.

Output: 03-MUTATION-LOG.md with five family entries, each a complete apply → observe-red → revert → byte-clean-proof record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/03-non-vacuity-proof-unconditional-ci-execution/03-CONTEXT.md (D-01, the family matrix)
@.planning/03-non-vacuity-proof-unconditional-ci-execution/03-RESEARCH.md (the per-family mutation surface table)
@.planning/STATE.md (the standing mutation-demo rule)

# Prior-record evidence (corroboration for the D-01 call record)
@.planning/01-corpus-selection-by-measurement/01-04-SUMMARY.md (tamper-rehearsal precedent)
@.planning/01-corpus-selection-by-measurement/01-07-SUMMARY.md (coverage-guard RED specification)
@.planning/02-golden-harness-re-authoring-re-freeze/02-04-SUMMARY.md (delete-a-golden count-only record)

# The six files rehearsed (read each once; apply mutations, revert, verify)
@testdata/golden/behavioral_test.go
@testdata/golden/golden_test.go
@testdata/golden/behavioral_{java,tsjs,python,csharp}_test.go
@internal/corpora/coverage_test.go
@corpora/selection.json
</context>

<tasks>

<task type="auto">
  <name>Rehearse families (a) behavioral property assertions and (c) the CLI==MCP byte-identity trio</name>
  <files>testdata/golden/behavioral_test.go, .planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md</files>
  <read_first>
    testdata/golden/behavioral_test.go — TestCorpusBehaviorSynthetic case (a) assertion at lines ~872-884, TestExploreCLIMatchesMCP at 1247-1285, TestNodeCLIMatchesMCP at 1287+, TestNodeLineHintCLIMatchesMCP at 1339
  </read_first>
  <action>
    Rehearsal rhythm (apply → run family → capture RED → revert byte-clean → append to 03-MUTATION-LOG.md), performed twice.

    Family (a): In testdata/golden/behavioral_test.go, inside TestCorpusBehaviorSynthetic under case "overloaded-defs-distinct", weaken the assertion boundary: change the defs-count conditional from `!= 2` to `!= 1` (the fixed line that asserts exactly two distinct definitions for the Validate case). Run `go test -count=1 ./testdata/golden/... -run 'TestCorpusBehaviorSynthetic'` — MUST fail red with the defs-count message naming case a (Node("Validate") outputs 2 defs, so `!= 1` is true). Paste the observed failing lines into the log. Revert the file with `git checkout -- testdata/golden/behavioral_test.go` and confirm `go test -count=1 ./testdata/golden/... -run TestCorpusBehaviorSynthetic` is green again.

    Family (c): In testdata/golden/behavioral_test.go, inside TestExploreCLIMatchesMCP (the CLI==MCP byte-identity trio, of which TestNodeCLIMatchesMCP and TestNodeLineHintCLIMatchesMCP are the siblings), make ONE surface diverge on the first row — the behavioral row always in-repo, so no corpus fetch is needed for the red: change ONLY the MCP-side call `callExploreViaMCP(t, dir, tc.query)` to pass a one-word-suffixed query (`tc.query+" extra"`), leaving the CLI side's query untouched. Run `go test -count=1 ./testdata/golden/... -run 'TestExploreCLIMatchesMCP'` — MUST fail red with the "CLI and MCP output diverge" message; paste the observed lines into the log. Revert byte-clean, re-run green. TestExploreCLIMatchesMCP is the representative runner; its siblings share the identical structure, so the (c) family is demonstrated by its most direct case.
  </action>
  <verify>
    <automated>go test -count=1 ./testdata/golden/... -run 'TestCorpusBehaviorSynthetic'</automated>
    <automated>go test -count=1 ./testdata/golden/... -run 'TestExploreCLIMatchesMCP'</automated>
  </verify>
  <acceptance_criteria>
    - the log carries two entries: family (a) with the weakened defs-count assertion, the red observed, the revert; family (c) with the MCP-side query divergence, the red observed "CLI and MCP output diverge", the revert
    - After the rehearsals, `git diff --stat testdata/golden/behavioral_test.go` is empty (byte-clean revert proof) and `go test -count=1 ./testdata/golden/... -run 'TestCorpusBehaviorSynthetic'` + 'TestExploreCLIMatchesMCP' both pass
    - No other test was touched; no new test was written
  </acceptance_criteria>
  <done>Both logged families went red under defined mutations and returned byte-clean before the task finished.</done>
</task>

<task type="auto">
  <name>Rehearse families (b) the golden byte-identity guard and (d) the hermetic fail-loud resolution</name>
  <files>testdata/golden/corpus/hugo/go-explore.json (transient delete-restore), [corpus root] hugo@&lt;sha&gt; tree (transient mv), .planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md</files>
  <read_first>
    testdata/golden/golden_test.go — TestReFrozenGoldensValid (255-308) and the testdata/golden corpus directory layout
    testdata/golden/behavioral_test.go — lockedCorpusDir (67-106) and TestPriorityLanguagesResolveToLockedCorpus (985-1000)
    corpora/manifest.json — the four locked entries and their pinned SHAs (the (d) mutation's target name derivation)
  </read_first>
  <action>
    Rehearse rhythm as in Task 1.

    Family (b) along the byte-identity guard: TEMPORARILY delete one tracked golden — testdata/golden/corpus/hugo/go-explore.json (record the deletion). Run `go test -count=1 ./testdata/golden/... -run TestReFrozenGoldensValid` — MUST fail red at 25/26 naming the missing hugo/go-explore.json; paste the observed lines into the log. Restore with `git checkout --`, re-run green at 26/26.

    Family (d) along the hermetic fail-NOT-skip resolution: with the corpus root at the suite's resolved location (default ~/.cache/codegraph/corpora or the CODEGRAPH_CORPUS_DIR override — check which via the tests you run), `mv` the hugo tree directory (name pattern `<root>/hugo@<sha>`) to a temporary name in the SAME parent (e.g. append a `.muttmp` suffix). Run `go test -count=1 ./testdata/golden/... -run 'TestPriorityLanguagesResolveToLockedCorpus'` — MUST fail red, and the failure must be `lockedCorpusDir`'s t.Fatalf naming the missing tree (fail-not-skip — the suite never falls back to t.Skip). Paste the observed lines. `mv` it back to its exact original name and re-run green. The corpus is reverted byte-cleanly (it's a pure rename).

    Record both in 03-MUTATION-LOG.md with family, test names, mutation, pasted failure, revert, byte-clean proof.
  </action>
  <verify>
    <automated>go test -count=1 ./testdata/golden/... -run 'TestReFrozenGoldensValid'</automated>
    <automated>go test -count=1 ./testdata/golden/... -run 'TestPriorityLanguagesResolveToLockedCorpus'</automated>
  </verify>
  <acceptance_criteria>
    - the log has entries for (b) and (d) with the pasted observed failure for each
    - the deleted golden file exists again (the git checkout restored it) and the corpus tree directory reverted from `.muttmp` to its exact original name
    - `go test -count=1 ./testdata/golden/... -run 'TestReFrozenGoldensValid'` passes 26/26
    - `go test -count=1 ./testdata/golden/... -run 'TestPriorityLanguagesResolveToLockedCorpus'` passes 5/5
    - the log records the D-01 call result (re-mutated) at the family-(d) row
  </acceptance_criteria>
  <done>Both families are red-demonstrated and reverted; nothing in the golden fixture tree or the corpus cache moved at task end.</done>
</task>

<task type="auto">
  <name>Rehearse family (e) the coverage guard and complete 03-MUTATION-LOG.md</name>
  <files>corpora/selection.json (transient mutation-revert), .planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md</files>
  <read_first>
    internal/corpora/coverage_test.go — TestCorpusCoverageClaim (122-151) and the mutation-style sibling tests
    corpora/selection.json — the minEdgesPerKind key it reads (calls: 29405 in the locked state)
    corpora/observations.json — the observed per-kind best counts (the threshold comparison target)
  </read_first>
  <action>
    Rehearse the coverage guard (family (e)): TEMPORARILY raise the `calls` threshold in corpora/selection.json's minEdgesPerKind to a value above the observed best for that kind (the measured calls best is 58812, guava; raise it to 999999 — one scalar in the committed document). Run `go test -count=1 ./internal/corpora/... -run TestCorpusCoverageClaim` — MUST fail red with the "below threshold" failure; paste it. Revert with `git checkout -- corpora/selection.json` and re-run green.

    Then CONSOLIDATE: open .planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md (ensure it exists; create if not), and make it the complete, final, per-family record: the header table records the documented D-01 call (all five re-mutated this phase; families (d)/(e) previously specified-but-unrecorded so re-mutated; (b) previously count-only, re-mutated with full output; prior 01-04/01-07/02-04 records cited as corroboration), then one row per family: (a) behavioral, (b) golden, (c) CLI==MCP trio, (d) hermetic resolution, (e) coverage guard — each with test names, the mutation, the observed failing output pasted, the revert command(s), and the byte-clean proof (`git status --porcelain` clean except the log); plus an overall "suite ran 26/26 goldens, red in all five families observed" conclusion row that is the positive reading of FIXT-07.
  </action>
  <verify>
    <automated>go test -count=1 ./internal/corpora/... -run TestCorpusCoverageClaim</automated>
  </verify>
  <acceptance_criteria>
    - one log row per family, every row with a pasted observed failure and a byte-clean revert command
    - `git status --porcelain` shows nothing except the 03-MUTATION-LOG.md file (plus any pre-existing untracked files documented)
    - `go test -count=1 ./internal/corpora/... ./testdata/golden/... ./internal/upgrade/...` all green after the final revert
    - the log's header records the D-01 call resolution exactly
  </acceptance_criteria>
  <done>FIXT-07's five recorded RED demonstrations exist as one visible artifact, and the suite is green in its final form.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| 03-MUTATION-LOG.md | mutation log (new) | `.planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md` — five family rows + D-01 header record; each row: mutation applied, observed failure pasted, byte-clean revert, proof |
| each rehearsal's byte-clean state | verification artifact | workings tree must return to `git status --porcelain` clean (except the log) |

## Cross-check

The five rehearsals are exactly the five families D-01 names and the research's per-family mutation surface enumerates. Constraint 8 (nothing about golden content/assertions/corpus lock lands) is honored by the apply-revert discipline, which is the entire point of the exercise — the log records the mutation observed; nothing is committed to the tree.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutation rehearsal → git tree | A rehearsal applies a real mutation to the suite; if the revert is missed, a mutation lands in a golden-guard commit. |
| rehearsal → local corpus cache | Family (d) moves a corpus tree outside the repo; if not restored exactly, subsequent runs resolve a missing tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-P2-01 | Tampering | mutation rehearsal workflow | critical | mitigate | Every rehearsal is single-shot apply → run → revert before the NEXT apply; each task enforces order in the action body and ends by re-running the family green and confirming `git diff` empty for the mutated file; the final task asserts `git status --porcelain` clean except the log. |
| T-03-P2-02 | (integrity) | local corpus cache (family (d)) | medium | mitigate | The tree rename backs off to its exact (a sha-bearing) name in the same parent, verified by a re-run of the resolution test green; a `mv` leaves the tree byte-identical (i.e. a rename, not a copy/delete). |
| T-03-P2-03 | Repudiation | 03-MUTATION-LOG.md evidence | medium | mitigate | Each entry carries the pasted failing test output and the exact revert command; an entry without the pasted output does not satisfy the FIXT-07 criterion, so the pasted output is itself the acceptance condition. |
| T-03-P2-04 | DoS | rehearsal run in CI | low | accept | Rehearsals are local-only apply-revert acts and never run in CI; the permanent artifacts are the green suite plus the log. |

</threat_model>

<verification>
- Per-task family runs: green after revert — see each task's verify commands
- Phase gate: `go test -count=1 ./internal/corpora/... ./testdata/golden/... ./internal/upgrade/...` green; each family's observed RED in the log
</verification>

<success_criteria>
- Five recorded RED demonstrations (one per family) with pasted observed failures and byte-clean reverts in 03-MUTATION-LOG.md — the suite demonstrably did not go vacuous
- The log's header records the D-01 re-mutate-vs-cite call and the corroborating prior records
- The working tree ends byte-clean with only the log artifact added
</success_criteria>

<output>
Create `.planning/phases/03-non-vacuity-proof-unconditional-ci-execution/03-02-SUMMARY.md` when done (and the 03-MUTATION-LOG.md it records)
</output>