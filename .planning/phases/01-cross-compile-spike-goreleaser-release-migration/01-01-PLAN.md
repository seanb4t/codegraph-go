---
phase: 01-cross-compile-spike-goreleaser-release-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .goreleaser.yaml
  - .github/workflows/release.yml
  - .github/workflows/linux-cross-canary.yml
  - Taskfile.yml
  - CONTRIBUTING.md
  - internal/upgrade/goreleaser_shape_test.go
autonomous: false
requirements: [REL-05, REL-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single `goreleaser release --snapshot --skip=publish,sign` invocation on a darwin host produces all four release binaries — linux/amd64, linux/arm64, darwin/amd64, darwin/arm64 — with the two linux legs cross-compiled by `zig cc` (D-01, D-06)."
    - "The linux/amd64 and linux/arm64 binaries produced on the macOS host EXECUTE on real Linux hardware of their own architecture and index a real source tree — a non-zero symbol/file count from `codegraph status`, not a `--version` print and not a zero exit code (REL-05)."
    - "The linux/arm64 execution proof runs on `namespace-profile-linux-arm64-4x8` — real arm64 hardware, never emulation (D-02)."
    - "The spike is a permanent, `workflow_dispatch`-triggerable canary that re-proves zig-cross-from-macOS whenever zig, the runner image, `.goreleaser.yaml`, or the Go module graph changes (D-03)."
    - "The FAIL-bar variation list is written into the canary workflow header BEFORE the first dispatch, so exhausting it declares FAIL falsifiably rather than by post-hoc rationalization (D-04)."
    - "Both linux build ids in `.goreleaser.yaml` carry a `zig cc` / `zig c++` `CC`/`CXX` override; a mutation removing either turns a Go test red."
  artifacts:
    - ".github/workflows/linux-cross-canary.yml — permanent dispatchable canary with three jobs (macOS cross-build, linux/amd64 exec, linux/arm64 exec)"
    - "Taskfile.yml target `release:dry-run` — `goreleaser release --snapshot --skip=publish,sign` on a native darwin host (D-06)"
    - "Taskfile.yml target `check:linux-cross-exec` — index a real tree with an already-built binary and assert a non-zero graph"
    - "internal/upgrade/goreleaser_shape_test.go — new file carrying `parseGoreleaserBuildEnv`/`mustGoreleaserBuildEnv` and `TestLinuxBuildIdsCrossCompileViaZig`"
    - ".goreleaser.yaml `codegraph-linux-amd64` build entry carrying `CC=zig cc -target x86_64-linux-gnu` and `CXX=zig c++ -target x86_64-linux-gnu`"
  key_links:
    - "Taskfile.yml `release:dry-run` → `.goreleaser.yaml` builds: — the dry run is the only thing that exercises all four build entries in ONE invocation before a real tag exists"
    - "`.goreleaser.yaml` linux build env `CC`/`CXX` → `mlugg/setup-zig` on the macOS runner — the zig binary must be on PATH in the same process that GoReleaser spawns the Go toolchain from"
    - "canary macOS job artifact upload → canary linux exec jobs — the ONLY link that converts 'the linker accepted it' into 'it runs on real Linux'"
  prohibitions:
    - "A green `goreleaser` exit code, a successful link, or a `codegraph --version` print MUST NOT be recorded as REL-05 evidence."
    - "The linux/arm64 execution proof MUST NOT run under emulation (qemu, binfmt, Rosetta, or an amd64 host)."
    - "No GoReleaser Pro directive (`release --split`, `continue --merge`, `prebuilt` builder, or any Pro-only config key) may appear in `.goreleaser.yaml` or any workflow."
    - "No shell `run:` body added by this plan may interpolate `${{ }}` directly; workflow context passes through a step `env:` block and is referenced as `$VAR`."
    - "Every third-party Action referenced by the new canary MUST be pinned to a full commit SHA with a trailing version-tag comment — never a bare tag or branch."
    - "`.github/workflows/release.yml` MUST NOT be renamed and its `on: push: tags: v[0-9]*` trigger MUST NOT change."
---

<objective>
Prove or disprove, on measured evidence, that the OSS single-runner release architecture is
reachable: that `zig cc` invoked from a macOS host cross-compiles this repository's CGo
tree-sitter dependency to linux/amd64 AND linux/arm64, and that the resulting binaries
actually RUN on real Linux hardware of their own architecture while indexing a real source
tree (REL-05).

This plan is the phase's tracer slice. It wires ONE path through every layer the phase will
later modify — `Taskfile.yml` target → `.goreleaser.yaml` build config → macOS runner →
cross-compiled artifacts → real-Linux execution → Go shape test — end to end, in production
quality, before any expansion work is trusted. Every later plan in this phase (the
`goreleaser release` migration, the archives/checksum/sign/SBOM activation, the attestor
swap, the live release) is contingent on the answer this slice produces.

Purpose: the milestone's single unproven claim. If it fails, the architecture changes to the
costed GoReleaser Pro fallback with three named gate repairs entered as scope — and that
branch must be reached by exhausting an enumerated list written down in advance (D-04), not
by open-ended debugging.

Output: a permanent, dispatchable canary workflow; two new Taskfile targets; a zig-cross
override on the linux/amd64 build id; one new Go shape test file; and a recorded PASS/FAIL
decision backed by a re-inspectable CI run URL.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-CONTEXT.md
@.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-RESEARCH.md
@.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols and files created by THIS plan (excluded from drift verification — they do not
exist in the codebase yet):

| Kind | Name |
|---|---|
| workflow file | `.github/workflows/linux-cross-canary.yml` |
| workflow job id | `cross-build` (in `linux-cross-canary.yml`) |
| workflow job id | `exec-linux-amd64` (in `linux-cross-canary.yml`) |
| workflow job id | `exec-linux-arm64` (in `linux-cross-canary.yml`) |
| Taskfile target | `release:dry-run` |
| Taskfile target | `check:linux-cross-exec` |
| Go test file | `internal/upgrade/goreleaser_shape_test.go` |
| Go const | `goreleaserConfigPath` |
| Go func | `parseGoreleaserBuildEnv` |
| Go func | `mustGoreleaserBuildEnv` |
| Go test | `TestLinuxBuildIdsCrossCompileViaZig` |
| Go test | `TestParseGoreleaserBuildEnv_MissingBuildIDIsError` |
| `.goreleaser.yaml` env keys | `CC` / `CXX` on build id `codegraph-linux-amd64` |

Later plans in this phase additionally produce: `.goreleaser.yaml` `id: raw` / `id: zip` /
`id: binary-sbom` blocks, Taskfile target `release:goreleaser`, workflow job id `release`,
`.github/workflows/post-release-verify.yml`, and the Go tests listed in those plans'
equivalent sections.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "one goreleaser invocation cross-compiles all four targets from macOS" — one path only</name>

  <files>
    .goreleaser.yaml,
    .github/workflows/release.yml,
    Taskfile.yml,
    internal/upgrade/goreleaser_shape_test.go
  </files>

  <read_first>
    - `.goreleaser.yaml` (all 124 lines) — the 4 `builds:` entries, the `codegraph-linux-arm64` entry's existing `CC=zig cc -target aarch64-linux-gnu` / `CXX=zig c++ -target aarch64-linux-gnu` env pair (the exact shape to replicate for amd64), and header contracts (a)–(d)
    - `Taskfile.yml` lines 270–348 — `check:darwin-release-build`, whose `preconditions:` shape, native-GoReleaser-build ordering comment (the Rosetta/`xcrun` trap), `--snapshot` rationale comment, `dist/artifacts.json`-first artifact lookup, and `file -b` arch assertion this new target mirrors
    - `Taskfile.yml` lines 349–373 — `check:goreleaser` and `check:cross` (the `TestCheckCrossMatchesGoreleaserTargets` precedent)
    - `.github/workflows/release.yml` lines 76–148 — the `build` matrix, specifically the `needs_zig` flag on each entry and the `mlugg/setup-zig` step it gates
    - `internal/upgrade/release_workflow_shape_test.go` lines 1–60 — the documented `parseX(src) (T, error)` / `mustX(t, src) T` helper-pair convention (non-nil error on absence, never a usable zero value) and the `const releaseWorkflowPath` off-disk-read idiom this new file copies
    - `internal/upgrade/taskfile_shape_test.go` lines 560–640 — `parseGoreleaserCrossPairs`, which already parses `.goreleaser.yaml`'s `builds:` list; reuse its scanning technique rather than inventing a second one
  </read_first>

  <behavior>
    `TestLinuxBuildIdsCrossCompileViaZig` (new, in `internal/upgrade/goreleaser_shape_test.go`):
    - Reads `.goreleaser.yaml` off disk via a package-relative `goreleaserConfigPath` const (`"../../.goreleaser.yaml"`), mirroring `releaseWorkflowPath`.
    - For build id `codegraph-linux-amd64`: the parsed `env:` list contains a `CC=` entry whose value starts with `zig cc` and names target `x86_64-linux-gnu`, and a `CXX=` entry whose value starts with `zig c++` and names the same target.
    - For build id `codegraph-linux-arm64`: same assertions against target `aarch64-linux-gnu`.
    - For build ids `codegraph-darwin-amd64` and `codegraph-darwin-arm64`: NO `CC=` or `CXX=` entry is present — darwin must never cross-link via zig (the libresolv/DNS-resolver risk recorded in `.goreleaser.yaml`'s darwin comment and `release.yml`'s header).
    - The test enumerates all four build ids explicitly and fails if any is absent, so deleting a build entry cannot make it vacuously pass.

    `TestParseGoreleaserBuildEnv_MissingBuildIDIsError` (new): `parseGoreleaserBuildEnv(src, "does-not-exist")` returns a non-nil error naming the missing id — never a usable empty map.
  </behavior>

  <action>
    Work in RED → GREEN order.

    RED: create `internal/upgrade/goreleaser_shape_test.go` in package `upgrade`. Declare
    `const goreleaserConfigPath = "../../.goreleaser.yaml"`. Add the helper pair
    `parseGoreleaserBuildEnv(src, buildID string) (map[string]string, error)` +
    `mustGoreleaserBuildEnv(t *testing.T, src, buildID string) map[string]string` following the
    documented convention in `release_workflow_shape_test.go` lines 20–26: the parser locates the
    `- id: <buildID>` item under the column-0 `builds:` key, collects the `KEY=VALUE` entries under
    that item's `env:` list up to the next `- id:` line or the next column-0 key, and returns a
    non-nil error when the build id is absent or has no `env:` list. Write both tests from the
    `<behavior>` block. Run them and confirm `TestLinuxBuildIdsCrossCompileViaZig` FAILS on the
    `codegraph-linux-amd64` `CC` assertion — that entry has no `CC`/`CXX` today.

    GREEN, part 1 — `.goreleaser.yaml`: add to the `codegraph-linux-amd64` build entry's `env:`
    list, alongside the existing `CGO_ENABLED=1`, exactly two entries mirroring the arm64 entry's
    shape: `CC=zig cc -target x86_64-linux-gnu` and `CXX=zig c++ -target x86_64-linux-gnu`. Update
    the comment above the linux entries to state that BOTH linux legs now cross-compile via zig
    (the amd64 leg no longer uses the runner's native gcc) and that this is what makes a single
    macOS host able to produce all four targets (D-01/D-02, REL-05). Do not touch the darwin
    entries. Do not touch `flags:`, `ldflags:`, or `mod_timestamp:` on any entry — those are the
    DIST-04 reproducibility contract (header comment (b)).

    GREEN, part 2 — `.github/workflows/release.yml`: flip the linux/amd64 matrix entry's
    `needs_zig` from `false` to `true`. This is one line and it keeps the still-three-job pipeline
    coherent with the config change above: the amd64 leg now needs `zig` on PATH. Add a short
    comment noting this is transitional and that plan 01-03 collapses the matrix entirely. Leave
    the darwin entries' `needs_zig: false` untouched (`TestDarwinLegsBuildNatively` asserts it).

    GREEN, part 3 — `Taskfile.yml`: add a top-level target `release:dry-run` implementing D-06.
    Its `desc:` must state that it proves the FULL `goreleaser release` composition — all four
    build entries in ONE invocation — on a native darwin host without publishing or signing
    anything, and must explain the `--snapshot` rationale verbatim from `check:darwin-release-build`
    (a PR or dispatch HEAD carries no tag and GoReleaser hard-fails without one). Preconditions:
    `[ "$(go env GOHOSTOS)" = "darwin" ]` with a message pointing at the new canary workflow for
    non-darwin hosts, and `command -v zig` with a message naming the CI pin (`0.15.1`, installed by
    `mlugg/setup-zig`) and `brew install zig` as the local path. Body: build the GoReleaser binary
    natively first with `GOWORK=off go build -modfile=go.tool.mod -o "${GORELEASER_BIN}"
    github.com/goreleaser/goreleaser/v2` and NO `GOOS`/`GOARCH` in the environment — copy that
    ordering and its load-bearing Rosetta/`xcrun` comment from `check:darwin-release-build`, it is
    the same trap. Then invoke that binary once as `release --snapshot --skip=publish,sign --clean`.
    Critically: set NO `GOOS`/`GOARCH` on the invocation — `goreleaser release` resolves targets
    from each `builds:` entry's own `goos:`/`goarch:`, unlike `build --single-target` (RESEARCH.md
    Pattern 5 / Pitfall 2), and a leftover `GOOS` here is at best a no-op and at worst masks a
    misconfiguration. After the run, assert via `dist/artifacts.json` (jq, `type=="Binary"`, with
    the same `find(1)` fallback `check:darwin-release-build` uses) that exactly four binary entries
    exist covering linux/amd64, linux/arm64, darwin/amd64 and darwin/arm64, and assert each
    binary's `file -b` description with a `case` statement: the linux ones must report ELF of the
    matching machine (`x86-64` / `aarch64`), the darwin ones Mach-O of the matching arch. A green
    exit code alone is explicitly not the check.

    Add a `task release:dry-run` reference to `CONTRIBUTING.md`'s local-build section so
    `TestContributingReferencesRealTaskTargets` stays satisfied and a contributor can find it.
  </action>

  <precondition>`zig` is installed on the local darwin host and on `PATH` (`command -v zig`); if absent, install it before running `task release:dry-run` — the Taskfile precondition will otherwise halt the task.</precondition>

  <verify>
    <automated>go test ./internal/upgrade/... -run 'TestLinuxBuildIdsCrossCompileViaZig|TestParseGoreleaserBuildEnv_MissingBuildIDIsError' -v && go test ./internal/upgrade/... && task check:goreleaser && task lint:actions && task release:dry-run</automated>
    <human-check>Confirm `task release:dry-run` printed four binaries with the four expected `file -b` descriptions (two ELF, two Mach-O), not merely exit 0.</human-check>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/upgrade/... -run TestLinuxBuildIdsCrossCompileViaZig` exits 0
    - Mutation-RED demonstrated: deleting the `CC=` line from `.goreleaser.yaml`'s `codegraph-linux-amd64` `env:` makes `TestLinuxBuildIdsCrossCompileViaZig` fail; restoring it makes it pass. The failing output is recorded in the plan SUMMARY.
    - Mutation-RED demonstrated: adding a `CC=zig cc -target aarch64-macos` line to `codegraph-darwin-arm64` makes the same test fail; removing it makes it pass.
    - Source: `.goreleaser.yaml` contains `CC=zig cc -target x86_64-linux-gnu` and `CXX=zig c++ -target x86_64-linux-gnu` under build id `codegraph-linux-amd64`
    - Source: `rg -n 'CC=|CXX=' .goreleaser.yaml` returns exactly four lines, all under the two linux build ids
    - Source: `.github/workflows/release.yml`'s linux/amd64 matrix entry reads `needs_zig: true`; both darwin entries still read `needs_zig: false`
    - CLI: `task release:dry-run` exits 0 on the local darwin host and its output names four binaries
    - CLI: `dist/artifacts.json` after that run contains exactly four `"type":"Binary"` entries, one per (goos,goarch) pair in `.goreleaser.yaml`'s `builds:` list
    - CLI: `task check:goreleaser` exits 0 (DIST-01 config validity)
    - CLI: `task lint:actions` exits 0
    - Test: `go test ./internal/upgrade/...` exits 0 (no existing shape test regressed — in particular `TestDarwinLegsBuildNatively`, `TestCheckCrossMatchesGoreleaserTargets`, `TestContributingReferencesRealTaskTargets`, `TestGoreleaserPinParity`)
    - Source: no `${{` appears inside any `run:` body added or modified by this task
  </acceptance_criteria>

  <done>
    One `goreleaser release --snapshot --skip=publish,sign` invocation on a native darwin host
    produces all four release binaries with both linux legs zig-cross-compiled, the produced
    objects are asserted to be ELF/Mach-O of the right machine rather than merely present, and a
    Go shape test holds the zig-cross invariant with both directions of mutation demonstrated RED.
    Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Permanent linux-cross canary — build on macOS, EXECUTE on real Linux, index a real tree</name>

  <files>
    .github/workflows/linux-cross-canary.yml,
    Taskfile.yml,
    CONTRIBUTING.md
  </files>

  <read_first>
    - `.github/workflows/darwin-toolchain-canary.yml` (all 73 lines) — the exact canary shape to mirror: the header-comment contract (what property is at risk / why the tag-only `release.yml` trigger cannot observe it / why the failure mode is quiet), the `workflow_dispatch` + path-scoped `pull_request` trigger pair, `permissions: contents: read`, the deliberate absence of `nscloud-cache-action` on macOS, the `./.github/actions/install-task` composite step, and the "keep steps separate so a red run says WHICH layer broke" rationale
    - `.github/workflows/release.yml` lines 112–148 — the pinned SHAs for `actions/checkout`, `actions/setup-go`, `mlugg/setup-zig`, and lines 189–194 for `actions/upload-artifact`; lines 231–236 for `actions/download-artifact`
    - `.github/workflows/release.yml` lines 150–188 — the `env:`-not-`${{ }}` interpolation convention and its comment, plus the `dist/artifacts.json`-first / `find(1)`-fallback artifact lookup
    - `Taskfile.yml` (the `release:dry-run` target added in Task 1, plus `check:darwin-toolchain` at line 241 for its precondition/`desc` style)
    - `internal/cli/index.go` and `internal/cli/status.go` — the real subcommand names, flags, and the fields `codegraph status` reports, so the execution proof asserts a non-zero graph rather than guessing at output
    - `internal/upgrade/taskfile_shape_test.go` lines 99–165 — `inScopeJobs` and `runBodyExceptions`; note that `release.yml` is deliberately NOT in `inScopeJobs`, so `TestWorkflowRunBodiesInvokeTask` does not currently bind release-path workflows. This plan still routes every `run:` body through a Taskfile target because that is the project's single-definition convention (DEV-01), not because a test forces it.
    - `CONTRIBUTING.md` — the local-build section listing `task <target>` references
  </read_first>

  <action>
    Create `.github/workflows/linux-cross-canary.yml` as a permanent, dispatchable canary (D-03) —
    a sibling of `darwin-toolchain-canary.yml`, not a throwaway.

    Header comment block, following the darwin canary's structure: state that (a) the property at
    risk is whether `zig cc` invoked from a macOS host can cross-compile this repo's CGo
    tree-sitter dependency — including grammars carrying hand-written external C/C++ scanners — to
    linux/amd64 and linux/arm64 such that the resulting binaries actually PARSE on real Linux;
    (b) `release.yml` fires only on a `v[0-9]*` tag push, so without this canary nothing re-proves
    the property when zig, the Namespace runner image, `.goreleaser.yaml`, or a tree-sitter grammar
    moves; (c) the failure mode is quiet — a wrong toolchain still produces a binary that builds,
    signs, attests and prints `--version` cleanly, and only fails when the tree-sitter parse path
    is exercised, which is exactly why the pass condition is "indexes a real tree", not "exits 0"
    (REL-05, RESEARCH.md Pitfall 3).

    In the same header, write the D-04 FAIL-BAR VARIATION LIST verbatim, enumerated BEFORE the
    first dispatch. Its value is that the GoReleaser Pro decision stays falsifiable; a list
    written after a failure is rationalization. The list, in order:
      V1 — zig `0.15.1` (the `release.yml` pin), target triples `x86_64-linux-gnu` and
           `aarch64-linux-gnu`, default (dynamic) `CGO_LDFLAGS`.
      V2 — zig `0.15.1`, explicit glibc floor: triples `x86_64-linux-gnu.2.28` and
           `aarch64-linux-gnu.2.28`, default `CGO_LDFLAGS`.
      V3 — zig `0.15.1`, glibc floor `2.28`, `CGO_LDFLAGS=-static-libgcc` (deliberately NOT a full
           `-static` link: glibc + `dlopen` makes that its own failure class).
      V4 — one zig minor newer than the pin, default triples, default `CGO_LDFLAGS`.
      V5 — one zig minor newer than the pin, glibc floor `2.28`, default `CGO_LDFLAGS`.
    State explicitly: exhausting V1–V5 without BOTH linux legs indexing a real tree on real Linux
    declares REL-05 FAIL and escalates to the costed GoReleaser Pro fallback with its three named
    gate repairs (`check:goreleaser`/DIST-01, `TestGoreleaserPinParity`,
    `tool-vuln`/VULN-01-02-03) entered as scope. State that the canary ships wired to V1 and that
    a variation is applied by editing the zig `version:` input and/or the `.goreleaser.yaml`
    target triples, recording which V-number produced the recorded run.

    Triggers: `workflow_dispatch:` plus a path-scoped `pull_request:` filter covering
    `.github/workflows/release.yml`, `.github/workflows/linux-cross-canary.yml`,
    `.goreleaser.yaml`, `Taskfile.yml`, `go.mod`, `go.sum`. Permissions: `contents: read` only —
    this canary publishes nothing.

    Three jobs, deliberately separate so a red run names WHICH layer broke:

    `cross-build` — `runs-on: namespace-profile-macos-6x14-tahoe` (D-01, the same profile
    `release.yml` and the darwin canary already use). Steps: `actions/checkout` (with
    `fetch-depth: 0`, GoReleaser needs full history and tags for its templates),
    `actions/setup-go` with `go-version-file: go.mod` and `cache: false`, `mlugg/setup-zig` with
    `version: "0.15.1"` (unconditional — both linux legs now cross via zig), the
    `./.github/actions/install-task` composite, then a single `run: task release:dry-run` step.
    No `nscloud-cache-action` — same reasoning as the darwin canary: a canary should have the
    smallest possible failure surface. Then upload the two linux binaries as artifacts using
    `actions/upload-artifact` with `if-no-files-found: error`, naming them
    `codegraph-linux-amd64` and `codegraph-linux-arm64`; locate each via `dist/artifacts.json`
    (jq on `type=="Binary"` plus goos/goarch) with the `find(1)` fallback, and pass any workflow
    context through a step `env:` block referenced as `$VAR` — never inline `${{ }}` in the
    script body.

    `exec-linux-amd64` — `needs: cross-build`, `runs-on: namespace-profile-linux-amd64-4x8`.
    `exec-linux-arm64` — `needs: cross-build`, `runs-on: namespace-profile-linux-arm64-4x8`
    (D-02; these profiles are already provisioned on the Namespace account — this is a `runs-on:`
    value, not infrastructure work, and there is no provisioning task). Both jobs: checkout (a
    real source tree to index), `actions/download-artifact` for their own architecture's binary,
    `./.github/actions/install-task`, then one `run: task check:linux-cross-exec` step with the
    binary path passed via step `env:`.

    Add the Taskfile target `check:linux-cross-exec`: it takes the path to an already-built
    `codegraph` binary via an environment variable (default to a documented conventional path),
    `chmod +x` it, runs `uname -m` and `file -b` on it and asserts the reported ELF machine matches
    the host's architecture (so a wrong-arch artifact fails loudly rather than under a confusing
    exec-format error), then runs the binary's real indexing subcommand against the checked-out
    repository working tree and finally asserts `codegraph status` reports a NON-ZERO file and
    symbol count. A zero-count graph, an empty output, or a bare exit code is a FAIL — that is the
    whole point of the criterion. Its `desc:` must say so in one sentence, and must state that
    this target exists to convert "the linker accepted the object" into "the CGo tree-sitter parse
    path works on this OS and arch".

    Add `task check:linux-cross-exec` and a pointer to the new canary in `CONTRIBUTING.md`
    alongside the existing `task check:darwin-*` references.
  </action>

  <verify>
    <automated>task lint:actions && go test ./internal/upgrade/... && task --list | rg -q 'check:linux-cross-exec' && rg -c 'namespace-profile-linux-arm64-4x8' .github/workflows/linux-cross-canary.yml</automated>
  </verify>

  <acceptance_criteria>
    - Source: `.github/workflows/linux-cross-canary.yml` exists, its `on:` block contains `workflow_dispatch:`, and its `permissions:` block is exactly `contents: read`
    - Source: the file contains the literal runner labels `namespace-profile-macos-6x14-tahoe`, `namespace-profile-linux-amd64-4x8`, and `namespace-profile-linux-arm64-4x8`, one per job
    - Source: `rg -v '^\s*#' .github/workflows/linux-cross-canary.yml | rg -c 'uses:.*@[0-9a-f]{40}'` equals the number of `uses:` lines referencing a third-party action — every one SHA-pinned with a trailing version comment
    - Source: the header comment enumerates variations `V1` through `V5` and names the GoReleaser Pro fallback plus its three gate repairs
    - Source: no `${{` appears inside any `run:` body in the new file (`rg -n 'run:' -A 20 .github/workflows/linux-cross-canary.yml | rg '\$\{\{'` returns nothing)
    - CLI: `task lint:actions` exits 0 (actionlint accepts the new workflow)
    - CLI: `task --list` lists both `release:dry-run` and `check:linux-cross-exec`
    - Test: `go test ./internal/upgrade/...` exits 0 — in particular `TestContributingReferencesRealTaskTargets` (new targets referenced in CONTRIBUTING.md resolve to real Taskfile targets) and `TestRequiredCheckNamesPreserved` (no required-status-check job name was disturbed)
  </acceptance_criteria>

  <done>
    A permanent, dispatchable canary exists that cross-builds both linux legs on the macOS
    Namespace profile and executes each on real Linux hardware of its own architecture, asserting
    a non-zero indexed graph rather than an exit code. The D-04 FAIL-bar variation list is written
    into the file before any dispatch. Committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Dispatch the canary and record the REL-05 architecture decision</name>

  <what-built>
    A permanent `linux-cross-canary` workflow that cross-compiles both linux legs from
    `namespace-profile-macos-6x14-tahoe` in one `goreleaser release --snapshot` invocation, then
    executes each resulting binary on `namespace-profile-linux-amd64-4x8` and
    `namespace-profile-linux-arm64-4x8` — real hardware, no emulation — indexing the checked-out
    repository and asserting a non-zero graph. Plus a `release:dry-run` Taskfile target and a Go
    shape test holding the zig-cross invariant.
  </what-built>

  <how-to-verify>
    1. Dispatch the canary against this branch:
       `gh workflow run linux-cross-canary.yml --ref <this branch>`
    2. Watch it: `gh run watch $(gh run list --workflow=linux-cross-canary.yml --limit 1 --json databaseId --jq '.[0].databaseId')`
    3. All three jobs (`cross-build`, `exec-linux-amd64`, `exec-linux-arm64`) must be green.
    4. Open the two exec jobs' logs and confirm, for EACH:
       - `uname -m` reports the expected machine (`x86_64` / `aarch64`)
       - `file -b` on the downloaded binary reports ELF of that same machine
       - the indexing step completed and `codegraph status` printed a NON-ZERO file and symbol
         count. A zero count, an empty graph, or a green step with no counts is a FAIL.
    5. Record the run URL. It is REL-05's re-inspectable evidence and Success Criterion 1's
       artifact — paste it into the plan SUMMARY together with the two `codegraph status` outputs.

    IF EITHER LEG FAILS: do NOT debug open-endedly. Apply the next unused variation from the
    V1–V5 list in the canary header, re-dispatch, and record which V-number was tried. When V1–V5
    are exhausted without both legs passing, STOP: REL-05 is FAIL, the OSS single-runner
    architecture is unreachable, and the remaining plans in this phase (01-02 through 01-05) are
    invalid as written. Report the FAIL to the maintainer with the recorded runs, and the
    architecture decision moves to the costed GoReleaser Pro fallback with `check:goreleaser`/
    DIST-01, `TestGoreleaserPinParity`, and `tool-vuln`/VULN-01-02-03 entered as scope.
  </how-to-verify>

  <resume-signal>Type "PASS &lt;run-url&gt;" to record REL-05 as satisfied and unblock plans 01-02 onward, or "FAIL &lt;run-url&gt; V&lt;n&gt;" to record the exhausted variation and halt the phase for the Pro-fallback decision.</resume-signal>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback, §A)

This phase has no SPEC file, so `## Edge Coverage` is absent and the deterministic probe was run
in fallback mode. It returned 7 applicable rows, 0 resolved. The probe engine is tuned for
application-logic requirements and fits release-pipeline requirements poorly; per §A an
`unclassified` row is surfaced as a flagged assumption and is never auto-resolved with a
backstop, auto-dismissed, or silently dropped.

Rows owned by this plan (1 of 7):

| Requirement | Category | Probe | Flagged assumption |
|---|---|---|---|
| REL-05 | unclassified | "unclassified — review manually" | **Assumed:** the pass condition is fully expressed by the two truths already authored above (both linux legs execute on real, non-emulated Linux of their own architecture AND index a real tree to a non-zero graph). The probe surfaced no edge this pass condition does not already name. **Unverified:** whether an edge exists that the pass condition misses — e.g. a binary that indexes a small tree correctly but fails on a grammar with an external C scanner not present in this repository. Mitigated, not closed, by indexing the checked-out repository (a real multi-language tree) rather than a synthetic minimal fixture. |

Remaining rows are carried by plans 01-02 (REL-09 concurrency), 01-03 (REL-06, REL-07
unclassified) and 01-05 (REL-08 adjacency, empty, ordering). Total across the phase: 1 + 1 + 2 +
3 = 7. No row is dropped.

<threat_model>
**ASVS level: 1. Block on: `high`.**

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub workflow context → runner shell | `github.ref_name` and other workflow context are attacker-influenceable (a crafted tag/branch name) and cross into `run:` script text |
| Third-party Action → release-path runner | Any Action this canary runs executes with the runner's filesystem and network access on the same profile family the real release job uses |
| macOS build host → Linux exec host | Cross-compiled artifacts move between jobs as CI artifacts; the exec jobs execute whatever `cross-build` uploaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | new `run:` steps in `linux-cross-canary.yml` | high | mitigate | Pass workflow context through a step `env:` block and reference it as `$VAR`; never interpolate `${{ }}` into a script body. Enforced as an acceptance criterion (`rg` for `${{` inside `run:` bodies returns nothing) and carried as a plan prohibition. |
| T-01-02 | Tampering | third-party Actions newly referenced by the canary | high | mitigate | Every `uses:` pinned to a full 40-hex commit SHA with a trailing version-tag comment, reusing the exact SHAs already pinned in `release.yml`. Asserted by an acceptance criterion counting SHA-pinned `uses:` lines against total third-party `uses:` lines. |
| T-01-03 | Spoofing | cosign SAN anchor (`releaseWorkflowRefPattern`) | high | mitigate | This plan must not rename `release.yml` or alter its `on:` trigger; the only `release.yml` edit is a one-line `needs_zig` flip inside the matrix. `TestReleaseWorkflowFileMatchesPattern` and `TestReleaseWorkflowTriggerIsTagPushOnly` remain green (acceptance criterion: full `go test ./internal/upgrade/...`). Carried as a plan prohibition. |
| T-01-04 | Elevation of Privilege | canary workflow permissions | medium | mitigate | `permissions: contents: read` at file scope; the canary publishes nothing, mints no OIDC token, and declares no `id-token:`/`attestations:`/`contents: write`. Asserted as an acceptance criterion. |
| T-01-05 | Repudiation | REL-05 evidence | medium | mitigate | The pass condition is a recorded CI run URL plus the two `codegraph status` outputs, re-inspectable by a third party, not a maintainer assertion. The D-04 variation list is written before the first dispatch so a FAIL cannot be re-narrated after the fact. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds zero package-manager installs and zero new Go module dependencies (RESEARCH.md → Package Legitimacy Audit: "Not applicable"). The only new external surface is `mlugg/setup-zig`, already in the build graph and SHA-pinned. No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `go test ./internal/upgrade/...` green — no existing shape test regressed
- `task check:goreleaser` green — `.goreleaser.yaml` still valid after the zig-CC addition (DIST-01)
- `task lint:actions` green — actionlint accepts the new canary
- `task release:dry-run` green on the local darwin host, producing four correctly-typed objects
- Both mutation-RED demonstrations recorded in the SUMMARY with their failing output
- A green `linux-cross-canary` run URL recorded, with the two `codegraph status` non-zero counts quoted
</verification>

<success_criteria>
REL-05 is decided on evidence a third party can re-inspect: a recorded canary run in which a
`zig cc`-cross-compiled linux/amd64 binary AND a linux/arm64 binary, both built on
`namespace-profile-macos-6x14-tahoe` in a single `goreleaser release --snapshot` invocation,
both EXECUTE on real Linux hardware of their own architecture and index a real source tree to a
non-zero graph — or, on failure of either leg after exhausting V1–V5, a recorded FAIL that halts
the phase for the GoReleaser Pro fallback decision.
</success_criteria>

<output>
Create `.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-01-SUMMARY.md` when done
</output>
