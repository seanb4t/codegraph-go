---
phase: 01-cross-compile-spike-goreleaser-release-migration
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - .goreleaser.yaml
  - internal/upgrade/goreleaser_shape_test.go
autonomous: true
requirements: [REL-06, REL-09]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "For each of the four platforms the release config produces BOTH a raw `codegraph_<tag>_<goos>_<goarch>` asset (extension-free, directly executable) and a `codegraph_<tag>_<goos>_<goarch>.zip` — same stem plus `.zip` (REL-09, D-15)."
    - "The raw archive entry keeps `formats: [binary]`; a mutation changing it to any other format turns a Go test red rather than shipping an archive that verifies its signature and then bricks `codegraph upgrade` (REL-09)."
    - "`.goreleaser.yaml` carries a LIVE `checksum:` block that defines `codegraph_<tag>_checksums.txt` and is scoped by `ids: [raw, zip]` to exactly the 8 downloadable payloads — signature and SBOM sidecars and the checksums file itself are excluded (D-12). (That this block is the SOLE writer is a joint claim: it additionally requires `release.yml`'s hand-rolled `sha256sum` step to be gone, which lives in plan 01-03's file set. It is therefore NOT claimed here as if this plan could prove it — see `key_links`.)"
    - "`binary_signs:` produces a cosign bundle named exactly `internal/upgrade.releaseAssetName() + \".sigstore.json\"` for every one of the four platforms; perturbing the `signature:` template turns a Go test red (D-14)."
    - "`sboms:` runs per BINARY (not per archive) and preserves today's `<asset>.spdx.json` names, so DIST-03 carries forward unchanged and zips get no separate SBOM (D-17)."
    - "The `.zip` contains binary + LICENSE + README only; no shell completions and no man pages, so BREW-03/BREW-04 keep a single generated-at-cask-build-time source (D-16)."
    - "`.goreleaser.yaml`'s header contract comments no longer describe `archives:`/`checksum:` as dead configuration and no longer state that this file intentionally has no signing or SBOM block."
  artifacts:
    - ".goreleaser.yaml `archives:` entry `id: raw` with `formats: [binary]` and the unchanged `{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}` name_template"
    - ".goreleaser.yaml `archives:` entry `id: zip` with `formats: [zip]`"
    - ".goreleaser.yaml `checksum.ids: [raw, zip]`"
    - ".goreleaser.yaml `binary_signs:` block invoking `cosign sign-blob --bundle=${signature}` with `signature: \"${artifact}.sigstore.json\"` and `artifacts: binary`"
    - ".goreleaser.yaml `sboms:` block `id: binary-sbom` with `artifacts: binary` and `documents: [\"${artifact}.spdx.json\"]`"
    - "internal/upgrade/goreleaser_shape_test.go — five new tests plus their parse/must helper pairs"
  key_links:
    - "`.goreleaser.yaml` `archives[id=raw].name_template` → `internal/upgrade.releaseAssetName()` — two independent implementations of one string; `TestReleaseAssetNameMatchesGoReleaser` already pins them and must stay green after the entry is split"
    - "`.goreleaser.yaml` `binary_signs.signature` → `internal/upgrade`'s `assetName + \".sigstore.json\"` download path — the one contract whose breakage bricks every user's `codegraph upgrade`"
    - "`.goreleaser.yaml` `archives[id=zip]` → Phase 3's Homebrew cask URL — the zip this plan adds is what the cask will point at"
    - "`.goreleaser.yaml`'s live `checksum:` block + plan 01-03's deletion of `release.yml`'s hand-rolled `sha256sum` step → plan 01-05's `gh release view <tag> --json assets` check — 01-05 is the plan that CLOSES the single-writer invariant, proving against the PUBLISHED release that exactly one `codegraph_<tag>_checksums.txt` exists and that its contents cover every published downloadable asset exactly once (8 payloads, no sidecars, no self-reference). This plan proves only that the block exists, is live, and is scoped; plan 01-03 carries the branch-local two-sided check of the joint state."
  prohibitions:
    - "The raw archive entry's `name_template` MUST NOT change by a single character; the raw asset name shape is a published contract `codegraph upgrade` resolves at runtime."
    - "`.sigstore.json` and `.spdx.json` sidecars and the checksums file itself MUST NOT appear as checksum entries or attestation subjects — signing or attesting over a signature is circular (D-12)."
    - "Shell completions and man pages MUST NOT be added to the `.zip` contents (D-16)."
    - "No GoReleaser Pro directive (`release --split`, `continue --merge`, `prebuilt` builder, or any Pro-only config key) may be introduced."
    - "The deprecated `archives.builds:` field MUST NOT be used; this repo is on GoReleaser v2.17.1, well past the v2.8 rename to `ids:`."
---

<objective>
Make `.goreleaser.yaml` the single declarative definition of everything the release publishes
downstream of compilation: two archive shapes per platform (raw binary and `.zip`), one
checksums file scoped to exactly those 8 payloads, per-binary cosign signing that reproduces
today's `<asset>.sigstore.json` sidecar names byte-for-byte, and per-binary SBOMs that preserve
today's `<asset>.spdx.json` names.

Purpose: this is the half of REL-06 that lives in config. Plan 01-03 deletes the hand-rolled
shell that does this work today; these blocks are what replaces it. REL-09's `.zip` also lands
here — the one new user-facing thing this phase ships, and the asset Phase 3's Homebrew cask
will point at.

Output: an activated, extended `.goreleaser.yaml` whose every contract is held by a Go shape
test demonstrated RED against a confirmed-applied mutation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-CONTEXT.md
@.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-RESEARCH.md
@.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-PATTERNS.md
@.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by THIS plan (excluded from drift verification — they do not exist yet):

| Kind | Name |
|---|---|
| `.goreleaser.yaml` block id | `raw` (archives entry) |
| `.goreleaser.yaml` block id | `zip` (archives entry) |
| `.goreleaser.yaml` block id | `binary-sbom` (sboms entry) |
| `.goreleaser.yaml` top-level key | `binary_signs:` |
| `.goreleaser.yaml` top-level key | `sboms:` |
| `.goreleaser.yaml` key | `checksum.ids` |
| Go func | `parseGoreleaserArchives` / `mustGoreleaserArchives` |
| Go type | `goreleaserArchive` |
| Go func | `parseGoreleaserTopLevelBlock` / `mustGoreleaserTopLevelBlock` |
| Go test | `TestRawArchiveEntryStaysBinaryFormat` |
| Go test | `TestZipArchiveSharesRawAssetStem` |
| Go test | `TestChecksumCoversRawAndZipIdsOnly` |
| Go test | `TestBinarySignsSidecarMatchesUpgradeContract` |
| Go test | `TestSbomsArePerBinaryWithSpdxNames` |
| Go test | `TestParseGoreleaserArchives_NoArchivesBlockIsError` |

Pre-existing symbols this plan builds on (already in the codebase, not new):
`goreleaserConfigPath`, `parseGoreleaserBuildEnv`, `mustGoreleaserBuildEnv` (created by plan
01-01), `releaseAssetName`, `goReleaserAssetName`, `TestReleaseAssetNameMatchesGoReleaser`,
`parseGoreleaserCrossPairs`.

<assumption_delta_decision>
**Signal:** the deterministic assumption-delta detector flagged a `pluralization` signal on
"…while carrying `.zip` archives alongside the raw binaries `codegraph upgrade` consumes." The
release asset shape generalizes from one form (raw binary) to two (raw + `.zip`).

**Primary noun:** the raw per-platform binary asset, `codegraph_<tag>_<goos>_<goarch>` — UNCHANGED
and still primary.

**Decision: `add-alongside` — accepted, deliberately NOT `promote`.**

The checkpoint's default guidance is that a generalization should usually promote the new general
representation to primary and demote the specific one. This phase does not do that, and that is
correct: `codegraph upgrade`'s wire contract resolves the raw asset name at runtime through
`internal/upgrade.releaseAssetName()` and downloads that exact byte stream with no extraction
step. Promoting `.zip` to primary would require every already-shipped binary in the field to
learn to extract an archive — a change to a contract held by binaries we cannot update. D-02/
Finding 1 is therefore *preserved byte-unchanged*, not amended: the raw entry keeps
`formats: [binary]` and its exact `name_template`, and the zip is added as a second `id:`-keyed
entry.

**What would force a later promote:** any future change that makes an archive form the asset
`codegraph upgrade` itself consumes — for instance a decision to ship notarized, stapled
`.pkg`/`.dmg` payloads as the canonical download, or an upgrade path rewritten to extract. Until
then, add-alongside is the correct model, not debt to pay down.

**Invariant that encodes this:** `TestRawArchiveEntryStaysBinaryFormat` — the raw entry must stay
`formats: [binary]`, demonstrated RED by mutation. This is REL-09's own success criterion and it
is authored in Task 1 below.
</assumption_delta_decision>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Dual archives keyed by id — raw stays binary, zip is added alongside (REL-09, D-12, D-15, D-16)</name>

  <files>
    .goreleaser.yaml,
    internal/upgrade/goreleaser_shape_test.go
  </files>

  <read_first>
    - `.goreleaser.yaml` (all lines) — the current single `archives:` entry at the end of the file (`formats: [binary]` + the `{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}` name_template), the `checksum:` block below it, the four `builds:` entry ids, and the "dead configuration" note above `archives:` that this task makes false
    - `.goreleaser.yaml` header contract comments (a)–(d) — (a) and (c) both assert this file's archive/checksum blocks never execute and that there is deliberately no signing or SBOM block; both become false in this plan
    - `internal/upgrade/goreleaser_shape_test.go` (created by plan 01-01) — `goreleaserConfigPath`, and the `parseGoreleaserBuildEnv`/`mustGoreleaserBuildEnv` helper-pair shape to extend
    - `internal/upgrade/verify_release_e2e_test.go` lines 1–80 — `goReleaserAssetName` and `TestReleaseAssetNameMatchesGoReleaser`, which pin the raw asset name against `releaseAssetName()` for all four platforms and MUST stay green after the split
    - `.planning/phases/01-.../01-RESEARCH.md` § Code Examples — the vetted full `archives:` + `checksum:` config block (Patterns 1 and 4), including the note that `ids:` (plural) replaced the deprecated `builds:` field in GoReleaser v2.8
  </read_first>

  <behavior>
    `TestRawArchiveEntryStaysBinaryFormat` (new): the `archives:` entry with `id: raw` declares
    `formats:` containing exactly the single value `binary`. Fails if the entry is absent, if
    `formats:` names anything other than `binary`, or if it names more than one format. This is
    REL-09's success criterion 4 stated as a machine check.

    `TestZipArchiveSharesRawAssetStem` (new): the `archives:` entries with `id: raw` and `id: zip`
    declare byte-identical `name_template` values, and the `zip` entry declares `formats:`
    containing exactly `zip`. Combined with GoReleaser's own `.zip` suffixing, this is D-15's
    "same stem as the raw asset plus `.zip`". The test also asserts BOTH entries' `ids:` lists
    name all four build ids (`codegraph-linux-amd64`, `codegraph-linux-arm64`,
    `codegraph-darwin-amd64`, `codegraph-darwin-arm64`), so a platform cannot silently lose an
    archive shape.

    `TestChecksumCoversRawAndZipIdsOnly` (new): `checksum.ids` is exactly the set
    `{raw, zip}` — not a superset, not empty, not absent. D-12's "exactly 8 payloads" asserted by
    config rather than by absence-of-configuration.

    `TestParseGoreleaserArchives_NoArchivesBlockIsError` (new): `parseGoreleaserArchives("")`
    returns a non-nil error, never an empty-but-usable slice.
  </behavior>

  <action>
    RED first. Extend `internal/upgrade/goreleaser_shape_test.go` with a `goreleaserArchive` struct
    (fields: `ID`, `Formats []string`, `IDs []string`, `NameTemplate string`) and the helper pair
    `parseGoreleaserArchives(src string) ([]goreleaserArchive, error)` /
    `mustGoreleaserArchives(t *testing.T, src string) []goreleaserArchive`, following the
    documented `parseX`/`mustX` convention: return a non-nil error — never a usable zero value —
    when the `archives:` block is absent or contains no entries. Add a second helper pair
    `parseGoreleaserTopLevelBlock(src, key string) (map[string]string, error)` /
    `mustGoreleaserTopLevelBlock` for scalar-and-inline-list keys under a named column-0 block, used
    here for `checksum:` and reused by Task 2. Note the `name_template:` values in this file use
    YAML folded scalars (`>-`), so the parser must join and trim the folded continuation lines
    before comparing — compare the resolved template string, not the raw source lines.

    Write the four tests from the `<behavior>` block. Run them and confirm they FAIL: today there
    is exactly one unnamed `archives:` entry and no `checksum.ids` key.

    GREEN — edit `.goreleaser.yaml`:

    Split the single `archives:` entry into two `id:`-keyed entries, copying the vetted shape from
    RESEARCH.md's Code Examples verbatim. The `raw` entry: `id: raw`, `ids:` listing all four build
    ids, `formats: [binary]` UNCHANGED, and the `name_template` character-identical to what is in
    the file today (`{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}` as a `>-` folded scalar).
    Do not reformat, re-indent, or re-fold that template — `TestReleaseAssetNameMatchesGoReleaser`
    and every shipped `codegraph upgrade` depend on the string it resolves to. The `zip` entry:
    `id: zip`, the same four `ids:`, `formats: [zip]`, and the same `name_template`. Use `ids:`,
    never the deprecated `builds:` field.

    Add NO explicit `files:` override to the `zip` entry. GoReleaser's conventional default already
    includes `LICENSE*` and `README*` alongside the binary, which is exactly D-16. Add a comment
    on the `zip` entry stating that completions and man pages are deliberately excluded because
    BREW-03/BREW-04 generate those from the binary at cask-build time, and shipping them here would
    create a second, staler source.

    Add `ids: [raw, zip]` to the existing `checksum:` block, leaving `name_template` and
    `algorithm: sha256` unchanged. Add a comment stating the D-12 scope: 8 payloads — the 4 raw
    binaries and the 4 zips — with `.sigstore.json` and `.spdx.json` sidecars and the checksums
    file itself deliberately excluded, because signing or checksumming over a signature is
    circular.

    Replace the "NOTE (WR-01, Phase 8 re-review): this block ... is dead configuration" paragraph
    above `archives:`. It is now false. State instead that these blocks are LIVE under
    `goreleaser release` (REL-06) and are the single source of truth for the published asset set,
    that the raw entry's `name_template` is a runtime contract with
    `internal/upgrade.releaseAssetName()` pinned by `TestReleaseAssetNameMatchesGoReleaser`, and
    that the raw entry must stay `formats: [binary]` because `codegraph upgrade` downloads and
    swaps in that exact byte stream with no extraction step.

    After the edits, demonstrate RED for each new test by applying and then reverting one confirmed
    mutation apiece: change the raw entry to `formats: [tar.gz]`; change the zip entry's
    `name_template` to a different stem; change `checksum.ids` to `[raw]`. Record the failing
    output for each in the SUMMARY.
  </action>

  <verify>
    <automated>go test ./internal/upgrade/... -run 'TestRawArchiveEntryStaysBinaryFormat|TestZipArchiveSharesRawAssetStem|TestChecksumCoversRawAndZipIdsOnly|TestParseGoreleaserArchives_NoArchivesBlockIsError|TestReleaseAssetNameMatchesGoReleaser' -v && task check:goreleaser && go test ./internal/upgrade/...</automated>
  </verify>

  <acceptance_criteria>
    - Config: `.goreleaser.yaml` has an `archives:` entry with `id: raw` and `formats: [binary]`
    - Config: `.goreleaser.yaml` has a second `archives:` entry with `id: zip` and `formats: [zip]`
    - Config: both entries declare `ids:` listing exactly `codegraph-linux-amd64`, `codegraph-linux-arm64`, `codegraph-darwin-amd64`, `codegraph-darwin-arm64`
    - Config: `.goreleaser.yaml` contains `ids: [raw, zip]` under `checksum:`
    - Source: `rg -n 'builds:\s*\[' .goreleaser.yaml` returns nothing — the deprecated `archives.builds:` field is not used
    - Source: `git diff` on `.goreleaser.yaml` shows the raw entry's `name_template` value is unchanged (the diff for that template's resolved text is empty)
    - Source: the `zip` entry declares no `files:` key — GoReleaser's default (binary + LICENSE + README) is relied on, and no completion or man-page path appears anywhere in the `archives:` block
    - Mutation-RED demonstrated and recorded for all three: raw `formats: [tar.gz]`, zip stem change, `checksum.ids: [raw]`
    - CLI: `task check:goreleaser` exits 0
    - Test: `go test ./internal/upgrade/...` exits 0 — in particular `TestReleaseAssetNameMatchesGoReleaser` (the raw name contract) and `TestCheckCrossMatchesGoreleaserTargets` (build ids untouched)
  </acceptance_criteria>

  <done>
    Every platform gets a raw asset and a `.zip` with the same stem; the raw asset's format and
    name are provably unchanged; the checksums file is scoped by config to exactly the 8
    downloadable payloads; and all three properties are held by tests demonstrated RED. Committed.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: `binary_signs:` and `sboms:` reproduce today's sidecar names declaratively (D-14, D-17)</name>

  <files>
    .goreleaser.yaml,
    internal/upgrade/goreleaser_shape_test.go
  </files>

  <read_first>
    - `.goreleaser.yaml` after Task 1 — the two archive entries and the scoped `checksum:` block; header contract comment (c), which asserts this file intentionally has no `signs:` or `sbom:` block and that signing/SBOM happen in `release.yml`'s assemble job
    - `.github/workflows/release.yml` lines 246–278 — the hand-rolled `cosign sign-blob --bundle="${f}.sigstore.json" --yes "$f"` loop and the `syft "$f" -o spdx-json="${f}.spdx.json"` loop, including their shared `case` exclusion of `*_checksums.txt | *.sigstore.json | *.spdx.json`. These are the exact behaviors the new blocks must reproduce declaratively; plan 01-03 deletes them.
    - `internal/upgrade/upgrade.go` lines 180–215 — `releaseAssetName()` and the `downloadReleaseAsset(version, assetName+".sigstore.json")` call. The literal `assetName + ".sigstore.json"` is the contract under test.
    - `internal/upgrade/goreleaser_shape_test.go` after Task 1 — `parseGoreleaserTopLevelBlock`, `parseGoreleaserArchives`, and the `parseX`/`mustX` convention
    - `.planning/phases/01-.../01-RESEARCH.md` § Architecture Patterns 2 and 3 and § Code Examples — the vetted `binary_signs:` and `sboms:` blocks, including the warning that `sboms.artifacts` defaults to `archive` (not `binary`) and MUST be set explicitly or the SBOM pipe will catalog the new `.zip`s instead of the binaries
  </read_first>

  <behavior>
    `TestBinarySignsSidecarMatchesUpgradeContract` (new): reads `.goreleaser.yaml`'s
    `binary_signs:` block off disk. Asserts:
    - `cmd:` is `cosign`
    - `artifacts:` is `binary`
    - `args:` contains `sign-blob`, `--bundle=${signature}`, `${artifact}`, and `--yes`
    - `signature:` is exactly `${artifact}.sigstore.json`
    Then, for each of the four (goos, goarch) pairs, resolves what that template produces given
    the artifact name `goReleaserAssetName(tag, goos, goarch)` and asserts it equals
    `goReleaserAssetName(tag, goos, goarch) + ".sigstore.json"` — the literal string
    `internal/upgrade` downloads. Uses one pinned tag literal, mirroring
    `TestReleaseAssetNameMatchesGoReleaser`'s independent-literal discipline, and additionally
    cross-checks `releaseAssetName(tag) + ".sigstore.json"` for the current host's pair so a bug
    shared by both implementations cannot hide.

    `TestSbomsArePerBinaryWithSpdxNames` (new): the `sboms:` block declares `artifacts: binary`
    (explicitly present, not defaulted), `cmd: syft`, and `documents:` containing exactly
    `${artifact}.spdx.json`. Fails if `artifacts:` is absent — the default is `archive`, which
    would silently catalog the zips and break DIST-03's per-binary `.spdx.json` contract.
  </behavior>

  <action>
    RED first: write both tests from the `<behavior>` block into
    `internal/upgrade/goreleaser_shape_test.go`, reusing `parseGoreleaserTopLevelBlock` from Task 1
    and adding a list-aware variant if `args:`/`documents:` need it. Run them and confirm they
    FAIL — neither block exists in `.goreleaser.yaml` today.

    GREEN — add both blocks to `.goreleaser.yaml`, copying the vetted shapes from RESEARCH.md's
    Code Examples verbatim:

    `binary_signs:` — one entry with `cmd: cosign`, `signature: "${artifact}.sigstore.json"`,
    `args: ["sign-blob", "--bundle=${signature}", "${artifact}", "--yes"]`, and
    `artifacts: binary`. Use `binary_signs:`, NOT `signs:` — `binary_signs:` is format-independent
    and is the correct primitive once `.zip` archives coexist with raw binaries, whereas `signs:`
    with `artifacts: binary` is constrained by `archives.formats: binary` and would no longer
    cleanly apply. Add a comment recording why the exact `${artifact}.sigstore.json` template is
    load-bearing: `internal/upgrade`'s download path requests `assetName + ".sigstore.json"`
    literally, so a template drift here 404s every user's `codegraph upgrade` after it has already
    downloaded the binary. Name `TestBinarySignsSidecarMatchesUpgradeContract` in the comment as
    the machine holder of that contract.

    `sboms:` — one entry with `id: binary-sbom`, `artifacts: binary` (explicit — the pipe's own
    default is `archive`), `documents: ["${artifact}.spdx.json"]`, `cmd: syft`, and
    `args: ["$artifact", "--output", "spdx-json=$document"]`. Add a comment stating D-17: SBOMs
    stay per-binary so today's `<asset>.spdx.json` names and per-platform build-constraint
    precision carry forward unchanged, and the zips deliberately get no separate SBOM because its
    content would duplicate the binary's.

    Rewrite header contract comment (c). It currently states that signing and SBOM generation are
    performed by `release.yml`'s assemble job and that this file intentionally has no `signs:` or
    `sbom:` block. Replace it with the new contract: signing and SBOM generation are performed HERE
    by the `binary_signs:` and `sboms:` pipes under a single `goreleaser release` invocation
    (REL-06); the hand-rolled shell loops in `release.yml` are deleted in the same phase (plan
    01-03); and the native 4-target build remains OSS-only with no GoReleaser Pro
    `release --split`/`continue --merge`/prebuilt-builder directive anywhere. Also update header
    comment (a): `release.yml` no longer invokes `goreleaser build --single-target`, and the
    archive/checksum blocks are no longer inert.

    Demonstrate RED for each new test by applying and reverting one confirmed mutation apiece:
    change `signature:` to `"${artifact}.bundle.json"`; delete the `artifacts: binary` line from
    `sboms:`. Record the failing output for each.
  </action>

  <verify>
    <automated>go test ./internal/upgrade/... -run 'TestBinarySignsSidecarMatchesUpgradeContract|TestSbomsArePerBinaryWithSpdxNames' -v && task check:goreleaser && go test ./internal/upgrade/... && task release:dry-run</automated>
  </verify>

  <acceptance_criteria>
    - Config: `.goreleaser.yaml` has a top-level `binary_signs:` block whose single entry declares `cmd: cosign`, `signature: "${artifact}.sigstore.json"`, and `artifacts: binary`
    - Config: `.goreleaser.yaml` has a top-level `sboms:` block whose single entry declares `id: binary-sbom`, `artifacts: binary`, `cmd: syft`, and `documents: ["${artifact}.spdx.json"]`
    - Source: `rg -n '^signs:' .goreleaser.yaml` returns nothing — `binary_signs:` is used, not the format-constrained `signs:`
    - Source: `rg -v '^\s*#' .goreleaser.yaml | rg -c 'artifacts: binary'` equals 2 (one in `binary_signs:`, one in `sboms:`)
    - Source: header contract comments (a) and (c) no longer contain the strings describing the blocks as never executed or the file as having no signing/SBOM block
    - Mutation-RED demonstrated and recorded for both: `signature:` perturbed to `"${artifact}.bundle.json"`; `sboms.artifacts` deleted
    - CLI: `task check:goreleaser` exits 0
    - CLI: `task release:dry-run` exits 0 (the `--skip=publish,sign` dry run still composes cleanly with the new blocks present)
    - Test: `go test ./internal/upgrade/...` exits 0
  </acceptance_criteria>

  <done>
    `.goreleaser.yaml` declaratively produces the exact `<asset>.sigstore.json` and
    `<asset>.spdx.json` sidecar names the hand-rolled shell loops produce today, the sidecar-name
    contract with `internal/upgrade` is held by a test demonstrated RED, and the file's header
    contracts describe reality. Committed.
  </done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback, §A)

Rows owned by this plan (1 of 7):

| Requirement | Category | Probe | Flagged assumption |
|---|---|---|---|
| REL-09 | concurrency | "If interrupted or run in parallel, what is guaranteed?" | **Assumed:** a single `goreleaser release` process is the only writer of the archive set, so the raw and `.zip` entries for one platform cannot interleave or partially publish — GoReleaser builds all archives into `dist/` before the release pipe uploads any of them, and the phase's whole point (REL-07) is that exactly one process writes. **Unverified:** what a re-run of `goreleaser release` against an already-published tag does to an existing asset pair — whether the raw and zip are replaced atomically as a set or one at a time, leaving a window where the zip is new and the raw is stale. Not measured by this plan; the D-06 dry run runs with `--skip=publish`. Partially covered by plan 01-05's post-release verification, which re-downloads and re-verifies the published set. |

<threat_model>
**ASVS level: 1. Block on: `high`.**

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.goreleaser.yaml` config → cosign signing identity | The `binary_signs:` block determines WHICH artifacts are signed and under WHAT sidecar name; a drift here changes what a downstream verifier can prove |
| release config → `codegraph upgrade` runtime | Two independent implementations of one asset-name string (`archives[raw].name_template` and `releaseAssetName()`) must agree; the config is the side that can silently move |
| published asset set → external consumers | The checksums file's coverage set is a contract external scripts and the Phase-3 cask may depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | `binary_signs.signature` template | high | mitigate | `TestBinarySignsSidecarMatchesUpgradeContract` resolves the template for all four platforms against `goReleaserAssetName()` and cross-checks `releaseAssetName()` for the host pair, demonstrated RED by perturbing the template (D-14). This is the one contract whose breakage bricks every user's `codegraph upgrade` after the binary is already downloaded. |
| T-01-07 | Spoofing | raw asset name shape | high | mitigate | The raw entry's `name_template` is unchanged character-for-character; `TestReleaseAssetNameMatchesGoReleaser` (pre-existing) pins it against `releaseAssetName()` for all four platforms and is an acceptance criterion. Carried as a plan prohibition. |
| T-01-08 | Repudiation | checksum coverage set | medium | mitigate | `checksum.ids: [raw, zip]` makes the "exactly 8 payloads" invariant explicit config rather than a reliance on GoReleaser's default inclusion set (RESEARCH.md assumption A4), and `TestChecksumCoversRawAndZipIdsOnly` asserts it, demonstrated RED by narrowing to `[raw]`. |
| T-01-09 | Tampering | SBOM artifact scope | medium | mitigate | `sboms.artifacts` defaults to `archive`; leaving it defaulted would silently catalog the new `.zip`s and drop the per-binary `.spdx.json` DIST-03 contract. Set explicitly and asserted by `TestSbomsArePerBinaryWithSpdxNames`, demonstrated RED by deleting the key. |
| T-01-10 | Elevation of Privilege | circular signing | medium | mitigate | `.sigstore.json`/`.spdx.json` sidecars are excluded from both the checksum set and the sign set (`artifacts: binary`), so no signature is ever itself signed or checksummed (D-12). Carried as a plan prohibition. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs and zero new Go module dependencies in this plan (RESEARCH.md → Package Legitimacy Audit: "Not applicable"). `cosign` and `syft` are already installed by SHA-pinned Actions in `release.yml`; this plan changes only their caller. |
</threat_model>

<verification>
- `go test ./internal/upgrade/...` green, including the pre-existing `TestReleaseAssetNameMatchesGoReleaser`
- `task check:goreleaser` green (DIST-01)
- `task release:dry-run` green — the full composition still runs on a native darwin host with the new blocks present
- Five mutation-RED demonstrations recorded in the SUMMARY with their failing output
</verification>

<success_criteria>
For each platform the release configuration carries both a raw `codegraph_<tag>_<goos>_<goarch>`
asset — extension-free, directly executable, and byte-shape-identical to what
`internal/upgrade.releaseAssetName()` builds — and a distinctly-named `.zip`; a mutation changing
the raw entry away from `formats: [binary]` turns a test red rather than shipping an archive that
verifies its signature and then bricks the install. Checksums, signatures and SBOMs are all
produced by GoReleaser's own pipes, scoped to exactly the intended artifact sets.
</success_criteria>

<output>
Create `.planning/phases/01-cross-compile-spike-goreleaser-release-migration/01-02-SUMMARY.md` when done
</output>
