---
phase: 09-source-view-follow-through-breadcrumb-editor-handoff
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - web/src/lib/source-lines.ts
  - web/src/lib/breadcrumb.ts
  - web/src/lib/components/browse/SourcePane.svelte
  - web/tests/source-lines.test.ts
  - web/tests/breadcrumb.test.ts
  - web/tests/source-pane-breadcrumb.test.ts
  - web/scripts/breadcrumb-check.mjs
  - corpora/breadcrumb-check.json
  - web/build/.build-manifest
  - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md
autonomous: true
requirements: [BRW-10]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "In the file view, a single-line sticky bar (`data-testid=\"source-breadcrumb\"`) is pinned above the `<pre>` and stays at the viewport top while the PAGE scrolls; it names the innermost FileSymbols range containing the first fully visible source line under the bar, computed client-side with zero rpcs per scroll event (D-01, D-02, BRW-10)."
    - "When the current line is inside no symbol the bar STILL renders with the file path only, dimmed, and `data-testid=\"source-breadcrumb-symbol\"` carries `data-empty=\"true\"` and empty text — never the nearest preceding symbol (D-03)."
    - "BRW-10 adjacency edge: among ranges containing the line, the tighter span (smaller end-start) wins; equal spans → the later-starting one; a line whose top edge exactly equals the bar's bottom edge counts as fully visible (>=), one pixel above it does not — pinned by firstFullyVisibleLine's unit tests at exactly the boundary and one step either side."
    - "BRW-10 ordering edge: two symbols with identical (startLine, endLine) resolve to the later element in FileSymbols order, which the server already fixes as (startLine, then name) — deterministic and stable."
    - "BRW-10 empty edge: an empty symbol list, a list whose ranges never contain the line, and a file with zero lines all yield the empty state, never a thrown error."
    - "BRW-10 concurrency edge: the FileSymbols fetch is keyed by the file target's path, aborted via AbortController when the target changes, and a response is cached under the path it was requested for and never applied to a different target."
    - "Every source line is its own DOM row (`[data-line=\"N\"]`, id `L{N}`) with an always-rendered plain-text gutter cell (`data-testid=\"gutter-line-N\"`), in BOTH the file and the single-def branches — the ONE rendering change D-04's consequence and D-10 share; the row count equals the decoded text's newline-terminated line count."
    - "splitHighlightedLines re-arranges highlight.js's own `<span>`/`</span>` tokens so every line is tag-balanced; it never un-escapes text — a line containing `&lt;script&gt;` in its input contains `&lt;script&gt;` verbatim in its output — and the only real `{@html …}` directive usages under web/src remain in SourcePane.svelte."
    - "The breadcrumb is verified in a real Chromium against this repository's own real index by the committed `web/scripts/breadcrumb-check.mjs` (real wheel input, an independent JS oracle over the FileSymbols rpc, at least one non-empty and one empty observation), and the same script run against the pre-fix binary records `breadcrumbPresent: false` and exits non-zero — pasted verbatim into 09-MUTATION-LOG.md family (a) (BRW-10 success criterion 1)."
    - "The MaxFileSymbols cap is surfaced: when FileSymbolsResponse.truncated is true the bar carries `data-symbols-truncated=\"true\"` and a dimmed suffix naming the returned count and total_count, so an empty crumb past the cap is distinguishable from a genuine gap (D-04 consequence)."
  artifacts:
    - web/src/lib/source-lines.ts
    - web/src/lib/breadcrumb.ts
    - web/src/lib/components/browse/SourcePane.svelte
    - web/tests/source-lines.test.ts
    - web/tests/breadcrumb.test.ts
    - web/tests/source-pane-breadcrumb.test.ts
    - web/scripts/breadcrumb-check.mjs
    - corpora/breadcrumb-check.json
    - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md
  key_links:
    - "SourcePane.svelte calls splitHighlightedLines(highlightSource(text, language)) and renders one row per returned line; `use:callTargets` stays on the single-def `<code>` container, whose TreeWalker decorates text nodes inside the rows unchanged."
    - "SourcePane's `client` prop widens to SourcePaneClient: getPermalink required, fileSymbols and getEditorLink optional — existing stub clients in web/tests/source-pane.test.ts remain valid; a missing member behaves as an absent client for that feature (idle)."
    - "breadcrumb-check.mjs spawns `<binary> ui --no-open --path <repo>` itself (graph-live-update-check.mjs's startCodegraphUi shape) and derives its expected values from the FileSymbols rpc over HTTP, so the oracle is independent of the SPA's own derivation code."
    - "`task web:build` rebuilds web/build/ and its .build-manifest after the SPA change; the embedded binary the live gate drives is built from that committed output, so the gate exercises what ships."
  prohibitions:
    - statement: "The breadcrumb MUST NOT show the nearest preceding symbol, the previous line's symbol, or any name the current line is not inside — an honest empty state is the required answer for a gap (D-03)."
      status: resolved
      verification: judgment
    - statement: "The per-line splitter MUST NOT become a second markup-from-repository-bytes site: it may only re-arrange highlight.js's own tags around already-escaped text, and no code added by this plan may build HTML from raw source bytes."
      status: resolved
      verification: judgment
---

<objective>
Restructure `SourcePane`'s single `{@html}` blob into per-line DOM rows — the ONE rendering change both
the breadcrumb and the line-number gutter depend on — then build the "which symbol am I inside"
breadcrumb on top of it: a sticky single-line bar naming the innermost `FileSymbols` range containing
the first fully visible line, with an honest empty state, and prove it in a real browser against this
repository's own index, watched FAIL against the pre-fix build.

Purpose: BRW-10 is the ROADMAP's first item inside this phase and the first thing verified live after
the shared restructure; 09-RESEARCH.md Pitfall 3 is why the restructure lands once, here, and plan
09-04's gutter links reuse it rather than re-touching the `<pre>`.

Output: `source-lines.ts`, `breadcrumb.ts`, the restructured `SourcePane.svelte` with a plain gutter and
the sticky bar, jsdom coverage, the committed Playwright gate `web/scripts/breadcrumb-check.mjs` with
its record, a rebuilt `web/build/`, and `09-MUTATION-LOG.md` opened with family (a).

**Commit discipline (TDD mode).** Task 1 is `tdd="true"` with a vitest RED→GREEN: the `test(09-03)`
commit exports `splitHighlightedLines`, `innermostSymbolAt` and `firstFullyVisibleLine` with bodies
returning the wrong answer (`[]`, `null`, `0`) so the target tests fail on assertions rather than on
module resolution; the `feat(09-03)` commit implements them. Task 2 is `tdd="true"` at the component
level (the jsdom tests are written first and fail on missing test ids). Task 3 is the live gate and
the mutation-log entry — its RED is the pre-fix binary, not a test commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-CONTEXT.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-RESEARCH.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-PATTERNS.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-VALIDATION.md
@.planning/phases/08-tmux-real-pty-harness/08-MUTATION-LOG.md

<interfaces>
<!-- Existing contracts this plan consumes (read from source; do not re-implement). -->
web/src/lib/highlight.ts:
  export function highlightSource(code: string, language: string): string   // highlight.js markup (only <span class="hljs-…"> / </span> tags) or escaped plaintext
  export function languageForPath(path: string): string
web/src/lib/gen/ui_pb.ts:
  Node { id, kind, name, qualifiedName, filePath, language, startLine, endLine, startCol, endCol, … }   // startLine/endLine 1-based; startCol 0-based
  FileSymbolsResponse { symbols: Node[]; totalCount: number; truncated: boolean }
web/src/lib/browse-state.ts:
  BrowseTargetState — { kind: 'file'; path; source: Uint8Array; truncated; totalLines; returnedLines } | { kind: 'single-def'; node; calls; calledBy; source? } | idle | loading | failed | multi-def
web/src/lib/components/browse/SourcePane.svelte (today):
  props { state: BrowseTargetState; client?: PermalinkClient; indexStale?: boolean; onNavigate? }

<!-- This plan CREATES: -->
web/src/lib/source-lines.ts:
  export function splitHighlightedLines(html: string): string[]
web/src/lib/breadcrumb.ts:
  export interface SymbolRange { name: string; startLine: number; endLine: number }
  export function innermostSymbolAt<T extends SymbolRange>(symbols: readonly T[], line: number): T | null
  export function firstFullyVisibleLine(codeTop: number, lineHeight: number, barBottom: number, lineCount: number): number
SourcePane.svelte:
  interface FileSymbolsClient { fileSymbols(request: MessageInitShape<typeof FileSymbolsRequestSchema>, options?: { signal?: AbortSignal }): Promise<FileSymbolsResponse> }
  client prop type in THIS plan: PermalinkClient & Partial<FileSymbolsClient>   // plan 09-04 widens it to & Partial<EditorLinkClient> when it declares EditorLinkClient; do not pre-declare that interface here
  DOM: [data-testid="source-breadcrumb"][data-current-line][data-symbols-truncated] > [data-testid="source-breadcrumb-path"], [data-testid="source-breadcrumb-symbol"][data-empty]
       <pre><code> rows: span[data-line="N"]#L{N} > span[data-testid="gutter-line-N"] + line html
</interfaces>
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's subset of the phase inventory (the complete list is in 09-01-PLAN.md):

- `web/src/lib/source-lines.ts` — `splitHighlightedLines`
- `web/src/lib/breadcrumb.ts` — `SymbolRange`, `innermostSymbolAt`, `firstFullyVisibleLine`
- `web/src/lib/components/browse/SourcePane.svelte` — `FileSymbolsClient`, `SourcePaneClient`, the
  per-line rows and gutter cells, the sticky bar and its test ids, `fileSymbolsState`, `currentLine`,
  `crumb` (derived), `scrollToSymbolStart`
- `web/tests/source-lines.test.ts`, `web/tests/breadcrumb.test.ts`, `web/tests/source-pane-breadcrumb.test.ts`
- `web/scripts/breadcrumb-check.mjs` (args `--out`, `--binary`, `--repo`, `--file`; record fields
  `breadcrumbPresent`, `file`, `symbolCount`, `observations[]`, `nonEmptyObservations`,
  `emptyObservations`, `pageErrorCount`, `pageErrors`, `browserIdentity`, `success`) and
  `corpora/breadcrumb-check.json`
- rebuilt `web/build/**` + `web/build/.build-manifest`
- `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md`
  opened with the cleanliness-gate convention and family (a)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two pure functions — balanced per-line splitting of highlight.js markup, and innermost-symbol derivation with the first-visible-line rule</name>
  <reversibility rating="reversible">Both are private pure helpers behind stable exports; a different algorithm swaps in behind the same signature.</reversibility>
  <files>web/src/lib/source-lines.ts, web/src/lib/breadcrumb.ts, web/tests/source-lines.test.ts, web/tests/breadcrumb.test.ts</files>
  <read_first>
    - `web/src/lib/highlight.ts` lines 149-176 — `escapeHtml` and `highlightSource`: the input to the splitter is either highlight.js's `.value` (only `<span class="hljs-…">` and `</span>` tags around escaped text) or `escapeHtml(code)`; in both cases every `<` from source text is already `&lt;`, which is what makes a literal `<span`/`</span>` scan safe.
    - `web/src/lib/call-targets.ts` lines 1-45 — the rule that no second markup-from-repository-bytes site may exist; the splitter re-arranges tags, it never builds them.
    - `internal/query/filesymbols.go` lines 78-85 — the server's sort order (startLine, then name) the ordering edge relies on.
    - `web/tests/source-pane.test.ts` lines 14-56 — the `node(name, overrides)` fixture builder to copy for `breadcrumb.test.ts`.
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-CONTEXT.md` D-02, D-03 and § Claude's Discretion (per-line DOM shape, scroll tracking).
  </read_first>
  <behavior>
    - splitHighlightedLines('') returns [''] (one empty line).
    - splitHighlightedLines('a\nb\n') returns ['a', 'b'] — a trailing newline does not add an empty final row; splitHighlightedLines('a\nb') returns ['a', 'b'].
    - For input '<span class="hljs-comment">/* x\ny */</span>\nz', the result has 3 entries; entry 0 is '<span class="hljs-comment">/* x</span>', entry 1 is '<span class="hljs-comment">y */</span>', entry 2 is 'z' — every entry has equal counts of '<span' and '</span>'.
    - Nested spans carry across: '<span class="a"><span class="b">p\nq</span></span>' → ['<span class="a"><span class="b">p</span></span>', '<span class="a"><span class="b">q</span></span>'].
    - Text is never un-escaped: splitHighlightedLines('&lt;script&gt;\n&amp;') returns ['&lt;script&gt;', '&amp;'].
    - Property (loop over 50 generated inputs mixing spans and newlines): every output line is balanced and output.length equals the newline-terminated line count.
    - innermostSymbolAt([], 5) is null; innermostSymbolAt([{name:'A', startLine:10, endLine:20}], 5) is null.
    - innermostSymbolAt([A(1-100), B(10-20)], 15) is B (tighter); at line 5 is A; at line 10 and at line 20 is B (inclusive both ends); at line 21 is A.
    - Equal spans: innermostSymbolAt([A(10-20), B(10-20)], 12) is B (later in input order); different starts, same length: [A(10-20), B(15-25)] at 18 → B (later-starting wins the tie on span length).
    - A symbol with endLine < startLine or endLine 0 is treated as the single line startLine: innermostSymbolAt([{A, startLine:7, endLine:0}], 7) is A; at 8 is null.
    - firstFullyVisibleLine(codeTop=100, lineHeight=20, barBottom=100, lineCount=50) is 1 (a top edge exactly at the bar's bottom is fully visible); barBottom=101 → 2; barBottom=120 → 2; barBottom=119 → 2; barBottom=121 → 3; barBottom=0 (bar above the code) → 1; lineCount=0 → 0; a barBottom past the last line → lineCount (clamped); lineHeight=0 → 1 (never NaN or Infinity).
  </behavior>
  <action>
`web/src/lib/source-lines.ts`. Export `splitHighlightedLines(html: string): string[]`. Split on `\n`; if the input ends with `\n` and produced more than one segment, drop the final empty segment. Walk each segment left to right with a single linear scan for the literal tokens `<span` (through its closing `>`) and `</span>`, maintaining a stack of the open tag strings; each output line is the currently-open tags (in stack order) + the segment + one `</span>` per still-open tag at the segment's end, and the stack carries into the next segment. Document at the top that the input is highlight.js's own markup or `escapeHtml` output — every `<` from repository bytes is already `&lt;`, so this scan can never mistake source text for a tag — and that this function is NOT a second markup site: it emits only tags it found in its input (T-09-05). No regex with backtracking over the whole input; no DOM construction or parsing of any kind. Comment-text discipline: do not write the pane's unescaped-render directive token (the at-html directive) or DOM API call forms in this file's comments — Task 2's file-count gate and this task's call-form gate count them; refer to "the pane's single unescaped render site" instead.

`web/src/lib/breadcrumb.ts`. Export `SymbolRange`, `innermostSymbolAt`, `firstFullyVisibleLine`. `innermostSymbolAt(symbols, line)`: normalise each range to `[startLine, max(startLine, endLine)]`, keep those containing `line` inclusively, choose the smallest `(end - start)`; on a tie prefer the larger `startLine`; on a full tie prefer the later index. Doc comment: this is D-02's "innermost" and D-03's "never the nearest preceding symbol" in one function, and it does not filter by kind — it reports what FileSymbols returns. `firstFullyVisibleLine(codeTop, lineHeight, barBottom, lineCount)`: the 1-based index of the first line whose top edge is at or below `barBottom`, i.e. `ceil((barBottom - codeTop) / lineHeight) + 1`, clamped to `[1, lineCount]`; return 0 when `lineCount` is 0; treat a non-positive `lineHeight` as 1 line visible (return 1). Doc comment: "at or below" is the `>=` adjacency rule and the unit tests pin the boundary and one step either side.

Tests first (`web/tests/source-lines.test.ts`, `web/tests/breadcrumb.test.ts`): one `it` per `<behavior>` row, plus the 50-input generated property loop for balance and count. Commit `test(09-03): add failing source-line splitter and breadcrumb derivation tests` (with the three exports returning `[]`/`null`/`0`), then `feat(09-03): balanced per-line splitting and innermost-symbol derivation`.
  </action>
  <verify>
    <automated>pnpm -C web exec vitest run tests/source-lines.test.ts tests/breadcrumb.test.ts 2>&amp;1 | tee /tmp/09-03-red.txt; test "$(rg -c -- '✗|×|FAIL' /tmp/09-03-red.txt)" -ge 1</automated>
    <fails_when>RED GATE (at the test commit): vitest exits 0, or no failing-test marker in the output; a "Failed to resolve import" line is INVALID_RED (the exports must exist and return the wrong answer)</fails_when>
    <automated>pnpm -C web exec vitest run tests/source-lines.test.ts tests/breadcrumb.test.ts --reporter=json --outputFile=/tmp/09-03-unit.json &gt;/tmp/09-03-unit.log 2&gt;&amp;1; node -e 'const r=require("/tmp/09-03-unit.json");console.log("total",r.numTotalTests,"passed",r.numPassedTests,"failed",r.numFailedTests);if(!(r.numTotalTests>=20&amp;&amp;r.numPassedTests===r.numTotalTests&amp;&amp;r.numFailedTests===0))process.exit(1)'</automated>
    <fails_when>GREEN GATE: fewer than 20 total tests across the two files, any failed test, or passed != total (the counts are printed before the comparison so a zero-test run cannot read as green)</fails_when>
    <automated>test "$(rg -o 'innerHTML|createElement\(|new DOMParser\(' web/src/lib/source-lines.ts web/src/lib/breadcrumb.ts | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'export function' web/src/lib/source-lines.ts web/src/lib/breadcrumb.ts | wc -l | tr -d ' ')" = "3"</automated>
    <fails_when>any DOM construction in the pure modules, or an export count other than exactly 3 (the positive control that both files exist and carry the three named exports)</fails_when>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/source-lines.ts` exports exactly `splitHighlightedLines`; `web/src/lib/breadcrumb.ts` exports `innermostSymbolAt`, `firstFullyVisibleLine` and the `SymbolRange` type.
    - `web/tests/source-lines.test.ts` includes the trailing-newline, nested-span, never-unescape and generated-property cases; `web/tests/breadcrumb.test.ts` includes the inclusive-bounds, tighter-span, later-start tie, equal-span later-index, endLine-0 and every `firstFullyVisibleLine` boundary case listed in `<behavior>`.
    - The SUMMARY pastes the RED output (failing test names) and the GREEN counts.
  </acceptance_criteria>
  <done>Both pure functions pass their unit suites with the boundary, tie and empty cases pinned, and the splitter provably never un-escapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SourcePane renders per-line rows with a plain gutter and the sticky breadcrumb bar over FileSymbols ranges</name>
  <reversibility rating="reversible">Component-internal DOM shape behind unchanged props; test ids are the only contract and are owned by this phase.</reversibility>
  <files>web/src/lib/components/browse/SourcePane.svelte, web/tests/source-pane-breadcrumb.test.ts</files>
  <read_first>
    - `web/src/lib/components/browse/SourcePane.svelte` in full (254 lines) — the `PermalinkClient` interface (20-30) to sit `FileSymbolsClient` beside, the `$effect` + AbortController + degrade-to-idle fetch (109-142) to copy for FileSymbols, the two single-blob `<pre>` blocks (217-221 file, 244-249 single-def) that BOTH become per-line rows, and the pitfall that a local named `state` collides with `$state` (the prop is destructured as `target`).
    - `web/src/routes/graph/+page.svelte` lines 55-82 and 460-500 — the `fileSymbolsCache`/`pendingFileSymbols` pattern (reassign a NEW Map, never mutate in place; guard application against a changed consumer).
    - `web/src/routes/browse/+page.svelte` lines 250-258 — how SourcePane is mounted with `client={uiClient}` (uiClient satisfies fileSymbols already); no change needed there.
    - `web/src/lib/call-targets.ts` lines 268-279 — `callTargets` walks text nodes under the element it is applied to; keep `use:callTargets` on the single-def `<code>` container.
    - `web/tests/source-pane.test.ts` in full — the fixture builders (`node`, `fileState`, `singleDefState`) and the stub-client convention to copy; the existing tests must keep passing unchanged.
    - `web/tests/setup.ts` — jsdom has no layout: every rect is 0, so in jsdom the current line is always 1; the component-level tests assert at line 1 and the live gate (Task 3) covers real scrolling.
    - `web/src/lib/source-lines.ts`, `web/src/lib/breadcrumb.ts` (Task 1).
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-CONTEXT.md` D-01, D-02, D-03, D-04 (and its "Consequence" note), D-10.
  </read_first>
  <behavior>
    - Rendering fileState with source 'package q\n\nfunc A() {}\n' yields three elements matching [data-line] with values 1, 2, 3, ids L1, L2, L3, and three gutter cells gutter-line-1..3 whose text is the line number; no element with data-line="4".
    - Rendering singleDefState with a two-line source yields two [data-line] rows and two gutter cells; the existing permalink and copy tests in source-pane.test.ts still pass.
    - A file target with a client whose fileSymbols resolves {symbols:[node('A',{startLine:1,endLine:3})], totalCount:1, truncated:false}: source-breadcrumb exists, source-breadcrumb-path text is the path, source-breadcrumb-symbol text is 'A' with data-empty="false", data-current-line is "1".
    - Same but symbols [node('B',{startLine:2,endLine:3})]: source-breadcrumb-symbol has data-empty="true" and empty text; the bar still renders and source-breadcrumb-path is present.
    - Same with truncated:true, totalCount:2500, symbols length 1: the bar has data-symbols-truncated="true" and its text contains '2500'.
    - A file target with a client lacking fileSymbols: the bar renders in the empty state (no throw, no rejected promise).
    - A single-def target: no source-breadcrumb element at all (D-04, file view only).
    - fileSymbols is called exactly once per path across a re-render with the same target, and its AbortController is aborted when the target changes to a different path (assert via the stub's received signal).
    - Clicking the non-empty crumb calls scrollIntoView on the row whose data-line equals the symbol's startLine (stub Element.prototype.scrollIntoView in the test).
  </behavior>
  <action>
In `SourcePane.svelte`: add `FileSymbolsClient` beside `PermalinkClient` (same MessageInitShape convention, importing `FileSymbolsRequestSchema`/`FileSymbolsResponse` from `$lib/gen/ui_pb`) and widen the `client` prop's type to `PermalinkClient & Partial<FileSymbolsClient>`; plan 09-04 widens it again with the editor client — leave a one-line comment naming that. Add a `fileSymbolsState` rune — `{kind:'idle'} | {kind:'loading'} | {kind:'loaded'; path: string; response: FileSymbolsResponse}` — and an `$effect` keyed on `target.kind === 'file' ? target.path : undefined` that mirrors the permalink effect: no client member → idle; otherwise `loading`, one `client.fileSymbols({ path }, { signal })`, `.then` stores `{kind:'loaded', path, response}` ONLY if the path still matches the current target (the concurrency edge), `.catch` degrades to idle, cleanup aborts. Keep a component-local `Map<string, FileSymbolsResponse>` cache reassigned as a new Map on insert (the graph page's discipline) so navigating back to a path issues no second request.

Replace BOTH single-blob `<pre>` blocks with the per-line shape. Compute `lines = splitHighlightedLines(highlightSource(text, language))` in a `{@const}`; render `<pre class="overflow-x-auto rounded border p-4 text-sm"><code>` and, with `{#each lines as html, i}`, one `<span class="line" data-line={i + 1} id={`L${i + 1}`}>` containing a gutter `<span class="gutter" data-testid={`gutter-line-${i + 1}`} aria-hidden="true">{i + 1}</span>` followed by `{@html html}`, then a literal newline text node after each row so `<pre>` lays each line on its own row and copied text keeps its line breaks. Style the gutter as an inline-block, right-aligned, width sized from `String(lines.length).length` characters plus one, `user-select: none`, `text-muted-foreground`. The gutter is ALWAYS rendered and is plain text in this plan (D-10 — plan 09-04 turns the cells into links when the editor link is buildable; do not add link markup here). In the single-def branch keep `use:callTargets` on the `<code>` element exactly as today. `{@html}` remains used in this one file only.

Add the sticky bar in the file branch only (D-04), directly above the `<pre>` and inside the `data-testid="browse-source"` div: `<div data-testid="source-breadcrumb" class="sticky top-0 z-10 mb-2 flex items-center gap-2 truncate border-b bg-background px-2 py-1 text-xs" data-current-line={currentLine} data-symbols-truncated={truncated}>` with `<span data-testid="source-breadcrumb-path" class={crumb ? '' : 'text-muted-foreground'}>{target.path}</span>` and `<span data-testid="source-breadcrumb-symbol" data-empty={crumb ? 'false' : 'true'}>` rendering, when non-empty, a `<button type="button" class="underline decoration-dotted">` with the symbol's name whose click scrolls the row `#L{startLine}` into view via `scrollIntoView({ block: 'start' })` (a button, never a hash href — a fragment change would collide with the browse route's URL-driven navigation identity); when empty, nothing. When `fileSymbolsState` is loaded with `response.truncated`, append a dimmed `<span>` reading `symbols capped: {symbols.length} of {totalCount}`. `crumb` is `$derived(innermostSymbolAt(symbols, currentLine))` where `symbols` is the loaded response's `symbols` or `[]`. It is one line tall by construction (`truncate`, no wrapping).

`currentLine` is a `$state(1)` updated by a `scroll` listener on `window` plus a `resize` listener, throttled to one `requestAnimationFrame` per burst, computing `firstFullyVisibleLine(codeEl.getBoundingClientRect().top, lineHeight, barEl.getBoundingClientRect().bottom, lines.length)` where `lineHeight` is the first row's `getBoundingClientRect().height` re-measured on resize (all rows are uniform-height `<pre>` rows). Bind `codeEl` and `barEl` with `bind:this`; register listeners in an `$effect` that returns their removal; recompute once on mount and whenever `lines.length` changes. Zero rpcs are issued from this path.

`web/tests/source-pane-breadcrumb.test.ts` (written first): one `it` per `<behavior>` row, using `source-pane.test.ts`'s fixture builders (import them by copying the small functions; do not export from the other test file) and a stub client with `getPermalink` and `fileSymbols`. Commit `test(09-03): add failing SourcePane per-line and breadcrumb tests` then `feat(09-03): per-line source rows, plain gutter and sticky breadcrumb in SourcePane`.
  </action>
  <verify>
    <automated>pnpm -C web exec vitest run tests/source-pane-breadcrumb.test.ts 2>&amp;1 | tee /tmp/09-03-pane-red.txt; test "$(rg -c -- '✗|×|FAIL' /tmp/09-03-pane-red.txt)" -ge 1</automated>
    <fails_when>RED GATE (at the test commit): vitest exits 0 or no failing marker is present</fails_when>
    <automated>pnpm -C web exec vitest run tests/source-pane-breadcrumb.test.ts tests/source-pane.test.ts tests/browse-page.test.ts tests/browse-tracer.test.ts --reporter=json --outputFile=/tmp/09-03-pane.json &gt;/tmp/09-03-pane.log 2&gt;&amp;1; node -e 'const r=require("/tmp/09-03-pane.json");console.log("total",r.numTotalTests,"passed",r.numPassedTests,"failed",r.numFailedTests);if(!(r.numTotalTests>=20&amp;&amp;r.numPassedTests===r.numTotalTests))process.exit(1)'</automated>
    <fails_when>GREEN GATE: fewer than 20 tests across the four files, or any failure — the pre-existing SourcePane and browse-page suites must stay green through the restructure</fails_when>
    <automated>pnpm -C web check 2>&amp;1 | tee /tmp/09-03-check.txt | tail -3; rg -q '0 errors' /tmp/09-03-check.txt</automated>
    <fails_when>svelte-check's summary line does not report 0 errors (type or template errors in the restructured component)</fails_when>
    <automated>test "$(rg -o '\{@html [a-z]' web/src -g '*.svelte' | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -l '\{@html [a-z]' web/src | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'data-testid="source-breadcrumb"' web/src/lib/components/browse/SourcePane.svelte | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'splitHighlightedLines' web/src/lib/components/browse/SourcePane.svelte | wc -l | tr -d ' ')" -ge 2</automated>
    <fails_when>a real `{@html <expr>` directive appears in more than one file under web/src (a second unescaped-render site; the pattern requires an expression after the directive so `call-targets.ts`'s existing `{@html}` comment mention is not counted), fewer than 2 directive usages (the positive control — both branches render through it), a breadcrumb test-id count other than 1 (D-01: one element), or fewer than 2 splitter call sites (both branches must be restructured)</fails_when>
  </verify>
  <acceptance_criteria>
    - `SourcePane.svelte` declares `FileSymbolsClient`, widens `client` to `PermalinkClient & Partial<FileSymbolsClient>`, renders `[data-line]` rows with `gutter-line-N` cells in BOTH the file and single-def branches, and renders `source-breadcrumb` (with `source-breadcrumb-path`, `source-breadcrumb-symbol[data-empty]`, `data-current-line`, `data-symbols-truncated`) in the file branch only.
    - The breadcrumb's symbol is a `<button>`, not an `<a href="#…">`; the file contains no `href="#L`.
    - `use:callTargets` is still applied in the single-def branch.
    - `web/tests/source-pane-breadcrumb.test.ts` covers every `<behavior>` row including the once-per-path fetch and abort-on-change assertions.
    - `web/tests/source-pane.test.ts` is unmodified and green.
  </acceptance_criteria>
  <done>Every source line is its own row with a plain gutter in both views; the file view shows a sticky, single-line breadcrumb that names the innermost symbol at line 1 in jsdom and renders an honest empty state when line 1 is in no symbol.</done>
</task>

<task type="auto">
  <name>Task 3: The live-browser gate — `breadcrumb-check.mjs` against this repository's real index, watched FAIL against the pre-fix binary, recorded as family (a)</name>
  <precondition>This repository's own index exists at `.codegraph/store` (self-indexed via the daemon; `ls .codegraph/store` is non-empty), Playwright's Chromium is installed for `@playwright/test@1.62.1` (`cd web && pnpm exec playwright install chromium` was run at Phase 5), and a Go toolchain resolving to 1.26.6 is on PATH for the two binary builds this task performs.</precondition>
  <files>web/scripts/breadcrumb-check.mjs, corpora/breadcrumb-check.json, web/build/.build-manifest, .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md</files>
  <read_first>
    - `web/scripts/graph-live-update-check.mjs` lines 1-60 (header, `repoRoot`, `corpusDir`), 240-280 (`startCodegraphUi` — spawn `ui --no-open --path`, read the printed URL, reject on early exit), 340-362 (binary-path resolution: `<root>/codegraph`, built by `task build:release`) — the self-owning-lifecycle shape THIS script copies, including its try/finally teardown.
    - `web/scripts/graph-collapse-affordance-check.mjs` lines 1-60 (`pollUntil`, poll-don't-sleep), 140-160 (`chromium.launch`, viewport, `pageerror` capture), 288-332 (diagnostic record written on every exit path, `success`, exit-1-on-failure `main().catch`).
    - `Taskfile.yml` lines 94-116 (`build:release` → `./codegraph`) and 739-800 (`web:build` → rebuilds `web/build/` and writes `.build-manifest`; `web:drift` compares against it).
    - `.planning/phases/08-tmux-real-pty-harness/08-MUTATION-LOG.md` lines 1-60 — the log's header, the pre-mutation cleanliness-gate convention, and family (a)'s entry shape (test name, gate, mutation, confirmed-applied grep, verbatim failing output, revert, byte-clean proof). Family (a) here differs in one documented way: the RED is the pre-fix BUILD, not a mutation of correct code, so there is no tracked-file mutation and no revert step (07-MUTATION-LOG.md family (a) set this precedent).
    - `internal/uiserver/handlers.go` — the Connect JSON endpoint shape: `POST <url>/codegraph.ui.v1.UIService/FileSymbols` with `Content-Type: application/json` and body `{"path": "..."}` returns `{"symbols":[…],"totalCount":N,"truncated":false}` (protojson field names are lowerCamel); the script's oracle fetches this over HTTP.
    - `web/src/lib/breadcrumb.ts` (Task 1) — the script's oracle re-implements `innermostSymbolAt` in JS INDEPENDENTLY (not imported) so a wrong derivation in the SPA cannot agree with itself.
  </read_first>
  <action>
Write `web/scripts/breadcrumb-check.mjs` in the house shape. Header comment: what it proves (BRW-10 success criterion 1 in a real Chromium against a real index), that it owns the `codegraph ui` lifecycle like graph-live-update-check.mjs, that the oracle is independent of the SPA code, and that the same script is the RED instrument against the pre-fix binary. Arguments: `--out <path>` (default `<root>/corpora/breadcrumb-check.json`), `--binary <path>` (default `<root>/codegraph`), `--repo <path>` (default `<root>`), `--file <repo-relative path>` (default `internal/query/node.go`, 418 lines with several top-level functions and gaps between them). Steps: (1) resolve args; fail loudly if the binary or `<repo>/.codegraph/store` is missing; (2) `startCodegraphUi(binary, repo)` — spawn `[ui, --no-open, --path, repo]`, capture the printed URL, reject after 15 s or on early exit; (3) fetch FileSymbols for `--file` over HTTP via `fetch(url + '/codegraph.ui.v1.UIService/FileSymbols', {method:'POST', headers:{'content-type':'application/json'}, body: JSON.stringify({path: file})})`, fail if fewer than 2 symbols are returned (the oracle needs ranges and a gap); (4) launch real Chromium (viewport 1600×1000), capture `pageerror`s, `page.goto(url + '/browse?file=' + encodeURIComponent(file))` — `file` is the browse route's file parameter (`web/src/lib/browse-url.ts` line 24, `BROWSE_PARAM_KEYS`); (5) `pollUntil` a `[data-testid="source-breadcrumb"]` element exists (15 s) — on timeout write the record with `breadcrumbPresent: false` and throw (this is the pre-fix RED path); (6) observation loop: read `data-current-line` (L) and `source-breadcrumb-symbol`'s text and `data-empty`, compute `expected = oracleInnermost(symbols, L)` in the script's own JS, push `{line: L, shown, expected: expected?.name ?? null, empty, agrees}` to `observations`, then `page.mouse.wheel(0, 240)` (real wheel input, never a dispatched event) and `pollUntil` `data-current-line` changes or the page reports it can scroll no further (compare `window.scrollY` before/after); continue until at least one observation has `expected !== null` AND at least one has `expected === null`, or 60 wheel steps have elapsed; (7) `success` = `breadcrumbPresent && observations.length >= 3 && every observation agrees && nonEmptyObservations >= 1 && emptyObservations >= 1 && pageErrors.length === 0`; (8) write the record (`file`, `symbolCount`, `observations`, `nonEmptyObservations`, `emptyObservations`, `breadcrumbPresent`, `pageErrorCount`, `pageErrors`, `browserIdentity`, `success`) on EVERY exit path via try/finally, kill the child, close the browser, exit 1 when `success` is false. Print each observation as it is taken so a failing run's transcript shows the disagreement, not just a boolean.

Then run the gate GREEN: `task web:build` (commits a rebuilt `web/build/` — this closes the web:drift window Tasks 1-2 opened), `GOTOOLCHAIN=go1.26.6 task build:release` (produces `./codegraph`, which is gitignored), then `node web/scripts/breadcrumb-check.mjs`; commit the script and `corpora/breadcrumb-check.json`.

Then the RED: build the pre-fix binary from the commit before this plan's first SPA commit — `PREFIX=$(git rev-parse HEAD~N)` where N counts back to the last commit that predates Task 1's `test(09-03)` commit (name the hash in the log); `git worktree add "$TMPDIR/prefix-09" "$PREFIX"`, `GOTOOLCHAIN=go1.26.6 go build -o "$TMPDIR/codegraph-prefix" ./cmd/codegraph` inside that worktree, then `node web/scripts/breadcrumb-check.mjs --binary "$TMPDIR/codegraph-prefix" --out "$TMPDIR/breadcrumb-prefix.json"` from the main tree. Expect exit 1 with `breadcrumbPresent: false`. Remove the worktree (`git worktree remove --force`). This is success criterion 1's "demonstrated wrong (empty) against the pre-fix build".

Create `09-MUTATION-LOG.md` following 08-MUTATION-LOG.md's header and cleanliness-gate convention (`**Phase:**`, `**Date:**`, `**Scope:**` naming four families — (a) breadcrumb empty/absent vs the pre-fix build [this plan], (b) breadcrumb stale vs a nearest-preceding mutation, (c) the rejected-path test, (d) the scheme allowlist — with (b)-(d) recorded by plan 09-05), then `## Family (a) — BRW-10: the breadcrumb against the pre-fix build`: the instrument (`web/scripts/breadcrumb-check.mjs` and its oracle), the pre-fix commit hash, the exact build and run commands, the pasted verbatim failing transcript (the `breadcrumbPresent: false` record line and the thrown error), the GREEN re-run's transcript against `./codegraph` (observations printed, `success=true`), and a note that no tracked file was mutated so there is no revert step (07-MUTATION-LOG.md family (a) precedent). Commit as `test(09-03): live breadcrumb gate, pre-fix RED, mutation log family (a)`.
  </action>
  <verify>
    <automated>task web:build &amp;&amp; task web:drift &amp;&amp; GOTOOLCHAIN=go1.26.6 task build:release &amp;&amp; test -x ./codegraph</automated>
    <fails_when>non-zero exit from any of the three targets, or no executable ./codegraph afterwards — the live gate must drive a binary embedding the rebuilt SPA</fails_when>
    <automated>node web/scripts/breadcrumb-check.mjs; node -e 'const r=require("./corpora/breadcrumb-check.json");console.log(JSON.stringify({present:r.breadcrumbPresent,obs:r.observations.length,nonEmpty:r.nonEmptyObservations,empty:r.emptyObservations,agree:r.observations.every(o=>o.agrees),pageErrors:r.pageErrorCount,success:r.success}));const f=[];if(r.success!==true)f.push("scriptReportedFailure");if(r.breadcrumbPresent!==true)f.push("breadcrumbAbsent");if(!(r.observations.length>=3))f.push("tooFewObservations");if(!(r.nonEmptyObservations>=1))f.push("noNonEmptyObservation");if(!(r.emptyObservations>=1))f.push("noEmptyObservation");if(!r.observations.every(o=>o.agrees))f.push("oracleDisagreement");if(r.pageErrorCount!==0)f.push("pageErrors");if(f.length){console.error("FAIL: "+f.join(","));process.exit(1)}console.log("PASS")'</automated>
    <fails_when>the comparator prints FAIL naming any check (the numbers are printed BEFORE any comparison, and the reporter runs even when the script exited non-zero because the two commands are chained with `;`)</fails_when>
    <automated>test "$(rg -c '^## Family \(a\)' .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md)" = "1" &amp;&amp; rg -q '"breadcrumbPresent": false' .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md &amp;&amp; rg -q 'codegraph-prefix' .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md</automated>
    <fails_when>the family (a) heading is absent or duplicated, the pasted RED record does not show the breadcrumb absent, or the pre-fix binary's name is missing from the transcript (the RED was narrated, not pasted)</fails_when>
    <automated>test "$(rg -o 'page\.mouse\.wheel' web/scripts/breadcrumb-check.mjs | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'dispatchEvent|waitForTimeout\([0-9]{4,}' web/scripts/breadcrumb-check.mjs | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'breadcrumb\.ts|\$lib/breadcrumb|source-lines\.ts' web/scripts/breadcrumb-check.mjs | wc -l | tr -d ' ')" = "0" &amp;&amp; WT="$(git worktree list)" &amp;&amp; ! printf '%s' "$WT" | rg -q 'prefix-09'</automated>
    <fails_when>no real wheel input, any dispatched synthetic event or multi-second fixed wait, any import of the SPA's own breadcrumb module (the oracle must be independent), or a leftover `prefix-09` worktree</fails_when>
  </verify>
  <acceptance_criteria>
    - `web/scripts/breadcrumb-check.mjs` exists, accepts `--out/--binary/--repo/--file`, spawns the binary itself, fetches FileSymbols over HTTP for its oracle, uses `page.mouse.wheel`, writes a record on every exit path, and exits 1 on failure.
    - `corpora/breadcrumb-check.json` is committed with `success: true`, `breadcrumbPresent: true`, at least 3 observations that all agree, at least one non-empty and one empty observation, and `pageErrorCount: 0`.
    - `web/build/` and `web/build/.build-manifest` are rebuilt and committed; `task web:drift` is green.
    - `09-MUTATION-LOG.md` exists with the header, the cleanliness-gate convention, and family (a) carrying the pre-fix commit hash, the exact commands, the verbatim RED transcript (`breadcrumbPresent: false`, exit 1) and the GREEN re-run transcript.
    - The SUMMARY records the pre-fix hash, both transcripts, and that the `prefix-09` worktree was removed.
  </acceptance_criteria>
  <done>The breadcrumb is proven in a real Chromium against a real index with an independent oracle in both states, the same instrument was watched fail against the pre-fix binary, and the proof is committed as family (a).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository bytes → highlight.js markup → per-line splitter → `{@html}` | third-party source text becomes DOM; the splitter re-arranges tags around already-escaped text |
| FileSymbols response → breadcrumb derivation | index data drives what the bar names; never a file read |
| Playwright script → spawned `codegraph ui` child and `./codegraph` binary | the gate builds and runs the product against this repository's own index |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering (XSS) | `splitHighlightedLines` feeding `{@html}` | high | mitigate | the splitter scans only the literal `<span`/`</span>` tokens highlight.js emits and never un-escapes; `web/tests/source-lines.test.ts` asserts `&lt;script&gt;` survives verbatim and every line is balanced; Task 2's guard keeps `{@html` in exactly one file |
| T-09-17 | Denial of Service | a very long file producing thousands of rows and a per-scroll computation | low | mitigate | the current line is O(1) geometry (`firstFullyVisibleLine`), throttled to one rAF per burst, no per-row rect reads; source is already truncated server-side (SourceBlob.truncated) |
| T-09-18 | Repudiation | the live-gate record and the mutation-log RED transcript | medium | mitigate | the record is written on every exit path; family (a) pastes the failing record line and names the pre-fix commit hash and binary; the verify comparator prints numbers before judging them |
| T-09-19 | Tampering | `web/build/` drifting from `web/src` after the SPA change | medium | mitigate | `task web:build` + `task web:drift` in Task 3's verify; the embedded binary the gate drives is built from the committed output |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed by this plan |
</threat_model>

<verification>
- `cd web && pnpm test` green with the three new test files counted; `pnpm check` reports 0 errors.
- `task web:drift` green after the rebuild; `corpora/breadcrumb-check.json` committed with `success: true`.
- `09-MUTATION-LOG.md` family (a) carries a verbatim `breadcrumbPresent: false` RED against a named pre-fix hash.
</verification>

<success_criteria>
- Success criterion 1 holds in full: a single-line sticky breadcrumb computed client-side from FileSymbols ranges, verified in a live browser against a real index with an independent oracle, and demonstrated empty against the pre-fix build.
- The per-line restructure exists once, serving both the breadcrumb and (in 09-04) the gutter links.
</success_criteria>

<output>
Create `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-03-SUMMARY.md` when done, with `coverage:` entries whose `verification.ref` values name `web/tests/breadcrumb.test.ts`, `web/tests/source-pane-breadcrumb.test.ts`, `corpora/breadcrumb-check.json` and `09-MUTATION-LOG.md#Family (a)`, the RED transcripts for Tasks 1-2, and a `## TDD Gate Compliance` note with the `test(09-03)`/`feat(09-03)` hashes.
</output>
