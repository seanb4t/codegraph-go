---
phase: 09-source-view-follow-through-breadcrumb-editor-handoff
plan: 06
type: execute
wave: 5
depends_on: ["09-04", "09-05"]
files_modified:
  - web/src/lib/components/browse/SourcePane.svelte
  - web/build/.build-manifest
  - web/build/**
  - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md
autonomous: true
gap_closure: true
requirements: [BRW-11, BRW-12]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`pnpm -C web check` reports 0 errors at HEAD"
    - "`task web:drift` reports MATCH at HEAD"
    - "the CR-01 corrective re-probe path is unchanged (its regression test passes and `template: resolved` still appears exactly once in SourcePane.svelte)"
    - "the svelte-check gate gap is recorded in a phase artifact"
  artifacts:
    - web/src/lib/components/browse/SourcePane.svelte
    - web/build/.build-manifest
    - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md
  key_links:
    - "SourcePane's probe `$effect` captures `activeClient.getEditorLink` ONCE into a guard-narrowed local (`getEditorLink`) and BOTH the initial probe and the CR-01 corrective re-probe call that local — so the narrowing the top-of-body `!activeClient?.getEditorLink` guard establishes survives the async `.then` closure boundary, and `getEditorLink` stays referenced under web/src only from SourcePane.svelte and the generated client (D-11)."
    - "`templateForRequest(override, response.presets)` still resolves a pending PRESET override against the presets THIS response delivered, and the re-probe's `template: resolved` request is exactly what CR-01 shipped — presets come from the wire, never a client-side table (D-18), and the override still rides only as `GetEditorLinkRequest.template` on the loopback rpc (D-06)."
---

<objective>
Close the one structured gap in `09-VERIFICATION.md`: `web/src/lib/components/browse/SourcePane.svelte` fails `svelte-check` (2 errors at 446:31, "Cannot invoke an object which is possibly 'undefined'" / "'activeClient.getEditorLink' is possibly 'undefined'") at the verified HEAD `3c0f4bc7`, breaking the "0 svelte-check errors" gate that 09-03 Task 2 and 09-04 Tasks 2-3 each declared. The break was introduced by the code-review fix `28d5d795` (CR-01 corrective re-probe) and never re-checked because 09-05's phase-close gate list omits `pnpm -C web check`.

This is a type-narrowing hygiene fix with NO new behaviour: capture the guard-narrowed `getEditorLink` once, before the async boundary, and call that local from both the initial probe and the CR-01 re-probe. The re-probe's control flow, request shape and D-18 semantics (no client-side preset copy; the preset override resolves from the wire's `response.presets`) are untouched, and the existing CR-01 regression test in `web/tests/source-pane-editor-link.test.ts` is the behavioural pin that must keep passing unchanged. Then record — in one phase artifact — that this gate was red between `28d5d795` and the fix and absent from the phase-close list, so a future audit does not rediscover it.

Purpose: restore the phase's own repeatedly-declared acceptance bar (BRW-11's header-link/re-probe path, BRW-12's preset override) and leave a written trail of the gate-hygiene miss.
Output: a rebuilt, drift-clean SPA whose source type-checks clean, plus a dated addendum in `09-MUTATION-LOG.md`'s `## Closing` section. No new symbols, no new files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-VERIFICATION.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-CONTEXT.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-REVIEW-FIX.md
@web/src/lib/components/browse/SourcePane.svelte
@web/tests/source-pane-editor-link.test.ts
</context>

<gap_closure_source>
`09-VERIFICATION.md` frontmatter `gaps:` block, single entry — truth: "SourcePane.svelte compiles cleanly under strict TypeScript / svelte-check (0 errors)"; status: failed; artifact: `web/src/lib/components/browse/SourcePane.svelte` line 446 (the CR-01 re-probe inside the probe `$effect`'s `.then(async (response) => …)` closure calls `activeClient.getEditorLink(...)` where TypeScript's narrowing of the property access from the effect's top-of-body guard does not survive the closure boundary); missing: (1) re-guard/capture so the narrowing survives and `pnpm -C web check` reports 0 errors, (2) `task web:build && task web:drift` after the fix, (3) record the fix and the silently-broken gate in a phase artifact.

Reproduced at planning time (2026-09-12, HEAD `3c0f4bc7`): `pnpm -C web check` printed `COMPLETED 1169 FILES 2 ERRORS 0 WARNINGS 1 FILES_WITH_PROBLEMS` and exited 1. Note the summary is svelte-check's MACHINE format (uppercase `ERRORS`) when stdout is not a TTY — 09-04's literal `rg -q '0 errors'` does not match that casing, so the gates below are case-insensitive and anchored on the exit code.
</gap_closure_source>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capture the guard-narrowed getEditorLink once so the CR-01 re-probe type-checks, rebuild, drift-clean</name>
  <reversibility rating="reversible">One local `const` in one `$effect` of one component; undoing it is a two-line revert behind an unchanged `EditorLinkClient` interface and an unchanged rpc contract.</reversibility>
  <files>web/src/lib/components/browse/SourcePane.svelte, web/build/.build-manifest, web/build/**</files>
  <read_first>
    - `web/src/lib/components/browse/SourcePane.svelte` lines 67-77 (the `EditorLinkClient` interface: `getEditorLink(request, options?): Promise<GetEditorLinkResponse>`, declared OPTIONAL on the `client` prop at line 95 as `Partial<EditorLinkClient>`), lines 212-217 (`stableClient`), and the whole probe `$effect` at lines 411-471 — the top-of-body guard at 417, the initial probe call at 428-429, the CR-01 re-probe at 446, and the gutter handler at 485-495 which is NOT affected (its guard and call share one function scope; no closure boundary).
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-04-PLAN.md` Task 2 `<verify>` block (the inherited gates: the four-file vitest run with its floor of 28, the D-11 `rg -l getEditorLink` set-equality, `location.assign` count 1, and the svelte-check gate) and its `<verification>` section ("`pnpm check` reports 0 errors").
    - `web/tests/source-pane-editor-link.test.ts` lines 107-128 (`GetEditorLinkFn`, `stubClient` — the stub is a plain object whose `getEditorLink` is a `vi.fn`, so it is called with no reliance on `this`) and lines 178-189 (the CR-01 pin: "sends a pre-seeded PRESET override as the corrected template via a re-probe (CR-01)" — asserts two calls, the first with `template` undefined, the second with `cursor://file/{path}:{line}:{col}`).
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-REVIEW-FIX.md` (CR-01 landed in `28d5d795`; WR-05's later pass re-ran vitest/build/drift but not svelte-check).
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-CONTEXT.md` D-18 (the preset override resolves from the wire's presets, never a client table — this fix must not change how or when the re-probe resolves).
  </read_first>
  <action>
Gap reason (09-VERIFICATION.md): the effect's top-of-body guard `!activeClient?.getEditorLink` narrows the PROPERTY ACCESS for the synchronous body only; TypeScript does not carry property-access narrowing into the nested `async (response) => …` closure, so the second `activeClient.getEditorLink(...)` at line 446 is `possibly 'undefined'` to `svelte-check` even though the `const activeClient` is never reassigned. Fix the narrowing, not the behaviour.

Step 0 — watch it fail first. Before editing anything, run `pnpm -C web check` at the starting HEAD and confirm it exits non-zero with exactly two `ERROR` lines at `SourcePane.svelte" 446:31` (the RED gate in `<verify>` below). Paste its last three lines into the SUMMARY. If it is already green, STOP and report — the gap has been closed elsewhere and this task must not touch the file.

Step 1 — the smallest control-flow-preserving change (per D-18 the re-probe's resolution logic is not to be altered; per D-11 `getEditorLink` remains referenced only from this component and the generated client). Immediately after the existing guard's early `return` (i.e. once `activeClient.getEditorLink` is known-defined for the synchronous body — right before or right after `editorLinkState = { kind: 'loading' };`), declare one local: `const getEditorLink = activeClient.getEditorLink.bind(activeClient);`. Then make BOTH call sites use it: replace the initial probe's `activeClient` newline `.getEditorLink({ path, line, col, template }, { signal: controller.signal })` chain head with `getEditorLink({ path, line, col, template }, { signal: controller.signal })`, and replace the re-probe's `await activeClient.getEditorLink(` with `await getEditorLink(`. Because `getEditorLink` is a `const` local narrowed to a defined function type, TypeScript preserves that narrowing inside the closure. `.bind(activeClient)` costs nothing and keeps `this`-binding semantics for any client implementation that might rely on it; the production client (`createClient(UIService, transport)` from connect-es, `web/src/lib/rpc` — its methods are standalone closures) and the test stub (`vi.fn`) both work bound or unbound, and a bound `vi.fn` still records `mock.calls`, so the CR-01 test's `toHaveBeenCalledTimes(2)` / `mock.calls[1][0].template` assertions are unaffected. Acceptable alternative if the executor prefers it after reading the file: hoist `const getEditorLink = activeClient?.getEditorLink;` ABOVE the guard and test `!getEditorLink` in the guard instead of `!activeClient?.getEditorLink` — same semantics, same two call-site edits. Either way the change is a capture plus two call-site substitutions and nothing else.

Do NOT add an `if (!…) return;` re-check inside the `.then` closure (that adds an unreachable early-return branch — a control-flow change no test covers, and the verifier's suggested alternative is strictly larger than the capture). Do NOT touch the gutter click handler (`handleGutterClick`, ~485-495): `svelte-check` does not flag it, and its `activeClient?.getEditorLink` guard-then-call is one function scope. Do NOT edit `web/tests/**`, `web/src/lib/gen/**`, `web/src/lib/editor-prefs.ts` or `EditorLinkPicker.svelte` — the CR-01 test is the pin and must pass byte-identical. Do NOT rewrite the CR-01 comment block above the re-probe (it documents WHY the re-probe exists; leave it verbatim). Keep the `template: resolved` re-probe request exactly as shipped so the `rg -o 'template: resolved'` count stays 1. No new imports, no type assertions (`as`), no non-null `!` operators, and no TypeScript error-suppression directive comments of any kind — a suppression would pass the gate while hiding the narrowing loss this gap is about (the verify block negative-greps the two standard suppression directives; do not write them anywhere in the file, not even in a comment).

Step 2 — gates and rebuild. Run `pnpm -C web check` (must exit 0 and print a `0 ERRORS`/`0 errors` summary). Run the four-file vitest gate inherited from 09-04 Task 2 (floor 28, all passing) and `task web:test` (the repo target; it checks passed == total itself and prints `web:test: PASS — N of N tests passed`; the expected N at HEAD is 542). Then `task web:build && task web:drift` — a source change to SourcePane.svelte requires rebuilding the committed SPA; `web:build` EMPTIES and rewrites `web/build/**` including `.build-manifest`, and `web:drift` must then report both `source half MATCH` and `output half MATCH` and `web:drift: PASS`. Never pipe `task` or `pnpm` output into `tee` to read its exit status (the pipeline returns tee's) — redirect to a file and capture `$?` as the gates below do.

Step 3 — one commit. Stage `web/src/lib/components/browse/SourcePane.svelte` and every changed/added/deleted path under `web/build/` (content-hashed chunk names change; use `git add -A web/build web/src/lib/components/browse/SourcePane.svelte`) and commit as `fix(09-06): capture guard-narrowed getEditorLink once so the CR-01 re-probe type-checks` with a body naming `28d5d795` as the commit that introduced the narrowing loss and `09-VERIFICATION.md` as the finder. Agent commits MAY use `git -c commit.gpgsign=false commit …`. NEVER put `[ci skip]` in the message. Repo landmines: `rg -h` is `--help` (use `--no-filename`); count occurrences with `-o | wc -l`, never `-c`; every guard carries a positive assertion (rule 84d1gfpywd); Go toolchain pinning (`GOTOOLCHAIN=go1.26.6`) is not needed here — nothing in this task runs Go; never leave a `codegraph ui` process running (nothing here starts one; do not start the live gate — 09-VERIFICATION.md re-ran it green at the same HEAD and this fix changes no rendered behaviour).
  </action>
  <verify>
    <automated>pnpm -C web check &gt;/tmp/09-06-check-red.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/09-06-check-red.txt; test "$RC" != "0" &amp;&amp; test "$(rg -o 'SourcePane\.svelte" 446:31' /tmp/09-06-check-red.txt | wc -l | tr -d ' ')" = "2"</automated>
    <fails_when>RED GATE (run BEFORE the edit, at the starting HEAD): svelte-check exits 0, or fewer/more than exactly 2 error lines at SourcePane.svelte 446:31 — the gap is not reproducible as recorded and the executor must stop and report instead of editing</fails_when>
    <automated>pnpm -C web check &gt;/tmp/09-06-check.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/09-06-check.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i '\b0 errors\b' /tmp/09-06-check.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^[0-9]+ ERROR ' /tmp/09-06-check.txt | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>GREEN GATE: svelte-check exits non-zero, the summary line does not report zero errors (matched case-insensitively so both the machine `0 ERRORS` and the human `0 errors` formats count — the positive assertion), or any per-file `ERROR` line remains</fails_when>
    <automated>pnpm -C web exec vitest run tests/source-pane-editor-link.test.ts tests/source-pane-breadcrumb.test.ts tests/source-pane.test.ts tests/browse-page.test.ts --reporter=json --outputFile=/tmp/09-06-pane.json &gt;/tmp/09-06-pane.log 2&gt;&amp;1; node -e 'const r=require("/tmp/09-06-pane.json");console.log("total",r.numTotalTests,"passed",r.numPassedTests,"failed",r.numFailedTests);if(!(r.numTotalTests&gt;=28&amp;&amp;r.numPassedTests===r.numTotalTests&amp;&amp;r.numFailedTests===0))process.exit(1)' &amp;&amp; test "$(rg -o 'via a re-probe \(CR-01\)' web/tests/source-pane-editor-link.test.ts | wc -l | tr -d ' ')" = "1" &amp;&amp; PINNED=$(git diff --name-only 3c0f4bc7 -- web/tests web/src/lib/gen web/src/lib/editor-prefs.ts web/src/lib/components/browse/EditorLinkPicker.svelte); RC=$?; test "$RC" = "0" &amp;&amp; test -z "$PINNED"</automated>
    <fails_when>fewer than 28 tests across the four files (09-04's floor), any failure, passed != total, the CR-01 test name no longer present exactly once in the test file, or ANY diff since the verified HEAD `3c0f4bc7` under web/tests, the generated client, editor-prefs.ts or the picker — the behavioural pin must be byte-identical to what it was when it caught CR-01</fails_when>
    <automated>task web:test &gt;/tmp/09-06-webtest.txt 2&gt;&amp;1; RC=$?; tail -2 /tmp/09-06-webtest.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o 'web:test: PASS' /tmp/09-06-webtest.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'numPassedTests=[0-9]+' /tmp/09-06-webtest.txt | rg -o '[0-9]+')" -ge 542</automated>
    <fails_when>the repo's own web:test target exits non-zero (it fails by design on passed != total or a zero-test run), its `web:test: PASS` line is absent, or fewer than 542 tests passed (the count 09-VERIFICATION.md observed at the same HEAD — a drop means a test was deleted or skipped)</fails_when>
    <automated>task web:build &amp;&amp; task web:drift &gt;/tmp/09-06-drift.txt 2&gt;&amp;1; RC=$?; cat /tmp/09-06-drift.txt 2&gt;/dev/null; test "$RC" = "0" &amp;&amp; test "$(rg -o '\bhalf MATCH\b' /tmp/09-06-drift.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o 'web:drift: PASS' /tmp/09-06-drift.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>web:build or web:drift exits non-zero, fewer than both `source half MATCH` and `output half MATCH` lines are printed, or the `web:drift: PASS` line is absent — the committed SPA is stale relative to the edited source or was not rebuilt by `task web:build`</fails_when>
    <automated>test "$(rg -o 'template: resolved' web/src/lib/components/browse/SourcePane.svelte | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'bind\(activeClient\)|const getEditorLink = activeClient' web/src/lib/components/browse/SourcePane.svelte | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -l 'getEditorLink' web/src | sort | tr '\n' ' ')" = "web/src/lib/components/browse/SourcePane.svelte web/src/lib/gen/ui_pb.ts " &amp;&amp; test "$(rg -o 'location\.assign\(' web/src/lib/components/browse/SourcePane.svelte | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'window\.open\(' web/src/lib/components/browse/SourcePane.svelte | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '@ts-ignore|@ts-expect-error|getEditorLink!' web/src/lib/components/browse/SourcePane.svelte | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the CR-01 re-probe request (`template: resolved`) no longer appears exactly once (the re-probe was removed or duplicated), no captured `getEditorLink` local exists (the positive control that the fix is the capture, not a suppression), getEditorLink is referenced under web/src anywhere but SourcePane.svelte and the generated client (D-11 breach), a location.assign count other than 1, any window.open call, or any TypeScript suppression / non-null operator on the call</fails_when>
    <automated>test -z "$(git status --porcelain --untracked-files=no)" &amp;&amp; SUBJ=$(git log -1 --format=%s) &amp;&amp; printf '%s\n' "$SUBJ" | rg -q '^fix\(09-06\): ' &amp;&amp; FILES=$(git show --name-only --format= HEAD) &amp;&amp; printf '%s\n' "$FILES" | rg -q '^web/src/lib/components/browse/SourcePane\.svelte$' &amp;&amp; test "$(printf '%s\n' "$FILES" | rg -o '^web/build/' | wc -l | tr -d ' ')" -ge 1 &amp;&amp; BODY=$(git log -1 --format=%B) &amp;&amp; test "$(printf '%s\n' "$BODY" | rg -o -i 'ci skip' | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>uncommitted tracked changes remain, the HEAD subject is not a `fix(09-06): ` commit, the commit does not contain SourcePane.svelte, it contains no `web/build/` path (source and rebuilt SPA must land in the SAME commit so no intermediate commit is drift-red), or the message carries a CI-skip marker</fails_when>
  </verify>
  <acceptance_criteria>
    - Before the edit, `pnpm -C web check` at the starting HEAD exits non-zero with exactly two `ERROR` lines at `SourcePane.svelte" 446:31` (pasted into the SUMMARY as the RED transcript).
    - After the edit, `pnpm -C web check` exits 0 and its summary reports zero errors (`COMPLETED N FILES 0 ERRORS 0 WARNINGS` or the human-format equivalent).
    - `SourcePane.svelte`'s probe `$effect` declares exactly one captured `getEditorLink` local narrowed by the existing guard, and both the initial probe and the CR-01 re-probe call it; the guard's semantics, the re-probe's condition (`template === undefined && override?.kind === 'preset'`), and its `template: resolved` request are unchanged (D-18, D-06).
    - `web/tests/source-pane-editor-link.test.ts` is byte-identical to `3c0f4bc7` and its CR-01 test passes (two calls: first `template` undefined, second `cursor://file/{path}:{line}:{col}`); the four-file gate has ≥ 28 passing tests; `task web:test` reports PASS with ≥ 542 passed.
    - `task web:build` then `task web:drift` prints `source half MATCH`, `output half MATCH`, and `web:drift: PASS`.
    - D-11 scope guard still holds (`getEditorLink` under web/src only in SourcePane.svelte and `web/src/lib/gen/ui_pb.ts`); `location.assign` count is 1; no `window.open`; no TypeScript suppression comment or non-null operator added.
    - One `fix(09-06): …` commit contains SourcePane.svelte and the rebuilt `web/build/**` (including `.build-manifest`); tracked tree clean afterwards; no CI-skip marker.
  </acceptance_criteria>
  <done>`pnpm -C web check` is green at HEAD with the CR-01 re-probe behaviour and its regression test untouched, and the committed SPA is rebuilt and drift-clean in the same commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record the silently-red svelte-check gate in 09-MUTATION-LOG.md's Closing section</name>
  <reversibility rating="reversible">A dated addendum paragraph in a phase-authored markdown ledger; undo is deleting the subsection.</reversibility>
  <files>.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md</files>
  <read_first>
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md` `## Closing` section (lines 426-end): its existing subsections (`### Phase-wide byte-clean proof`, `### Non-vacuity assertion`, `### Family-to-requirement table`, `### Phase-close run (Task 3)`) and the house style of the last one — a dated, HEAD-hash-citing paragraph.
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-05-PLAN.md` Task 3 (lines 178-200): the phase-close gate enumeration (`proto:drift`, `web:drift`, `web:components:drift`, `test:unit`, `web:test`, the live gate, `TestEditorPresetsAreExactlyThreeAndNameNoZed`) and its `<verify>` gate that asserts EXACTLY four `## Family (` headings in this log — the addendum must not add one. 09-05-PLAN.md itself is NOT to be edited (plans are frozen); the gap's third `missing` item is discharged by recording the omission here.
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-VERIFICATION.md` Gaps Summary (lines 170-193) for the exact causal chain to cite.
    - `web/tests/source-pane-editor-link.test.ts` lines 178-189 (the CR-01 test this fix left untouched — name it in the note).
  </read_first>
  <action>
Append ONE new subsection at the very end of `09-MUTATION-LOG.md`, under the existing `## Closing` section, headed `### Gate-hygiene addendum (09-06)` — a `###` heading only; add no new `## ` heading of any kind (09-05's own gate counts family headings and asserts exactly four, and there must remain exactly one Closing heading). Write it in the same dated, hash-citing style as the `### Phase-close run (Task 3)` paragraph above it, three short paragraphs:

1. What was red and for how long: `pnpm -C web check` (svelte-check, strict TS) reported 2 errors at `web/src/lib/components/browse/SourcePane.svelte:446:31` ("'activeClient.getEditorLink' is possibly 'undefined'") from commit `28d5d795` (code-review iteration 1, CR-01 corrective re-probe) through the verified HEAD `3c0f4bc7`, and was fixed in the Task 1 commit (cite its short hash from `git rev-parse --short HEAD` after Task 1's commit) by capturing the guard-narrowed `getEditorLink` once and calling it from both the initial probe and the re-probe — a narrowing-hygiene change with no behavioural delta; the CR-01 regression test "sends a pre-seeded PRESET override as the corrected template via a re-probe (CR-01)" in `web/tests/source-pane-editor-link.test.ts` passed unchanged before and after, and `task web:build` + `task web:drift` were re-run green in the same commit (paste the two `half MATCH` lines).

2. Why nobody noticed: 09-05 Task 3's phase-close gate list (`proto:drift`, `web:drift`, `web:components:drift`, `test:unit`, `web:test`, the live gate, `TestEditorPresetsAreExactlyThreeAndNameNoZed`) did not include `pnpm -C web check`; the code-review re-verification passes (09-REVIEW.md iterations 2-3, which landed WR-01..WR-05 on top of CR-01) re-ran `go build`/`go vet`/`go test`/`pnpm -C web test`/`task web:drift` but never svelte-check; and 09-04-SUMMARY.md's pasted `0 ERRORS` transcript predates `28d5d795`, so no SUMMARY was wrong on its own terms — the gate was only re-executed by 09-VERIFICATION.md. Note in passing the casing landmine: svelte-check prints its MACHINE summary (`… 0 ERRORS 0 WARNINGS`) when piped, so a case-sensitive `rg -q '0 errors'` gate cannot match it — gates must be case-insensitive and anchored on the exit code.

3. Forward rule for this phase's record (not a plan edit — 09-05-PLAN.md stays frozen): `pnpm -C web check` belongs in every phase-close gate enumeration for any phase that touches `web/src`, run alongside `task web:test` and `task web:drift`, and it must be re-run after every late-landing code-review fix commit, not only at each plan's own GREEN capture. Date the addendum (today's date) and cite the HEAD it was written at.

Do not edit any other section of the log, and do not touch `09-SECURITY.md`, `09-05-PLAN.md`, `09-05-SUMMARY.md`, or `.planning/WINDOWS.md` (one artifact carries the record, per the gap-closure contract). Commit the single file as `docs(09-06): record the svelte-check gate that was red from 28d5d795 to the 09-06 fix`. Agent commits MAY use `git -c commit.gpgsign=false commit …`; never `[ci skip]`. `rg -h` is `--help` — use `--no-filename` if a filename-less grep is needed; count with `-o | wc -l`.
  </action>
  <verify>
    <automated>L=.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md; FIX=$(git log -1 --format=%h --grep='^fix(09-06): '); RC=$?; test "$RC" = "0" &amp;&amp; test -n "$FIX" &amp;&amp; test "$(rg -o 'pnpm -C web check' "$L" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '28d5d795' "$L" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o "$FIX" "$L" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '^### Gate-hygiene addendum \(09-06\)' "$L" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'via a re-probe \(CR-01\)' "$L" | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>no `fix(09-06): ` commit is reachable by subject (Task 1 did not land), the literal `pnpm -C web check` is absent from the log (the gap's acceptance test), the CR-01 commit hash `28d5d795` is not cited, the Task 1 fix commit's abbreviated hash (found by its unique subject, not by a relative HEAD anchor) is not cited, the addendum heading is missing or duplicated, or the CR-01 test name is not named as the unchanged behavioural pin</fails_when>
    <automated>L=.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md; test "$(rg -o '^## Family \(' "$L" | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o '^## Closing' "$L" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^### Phase-close run \(Task 3\)' "$L" | wc -l | tr -d ' ')" = "1" &amp;&amp; FILES=$(git show --name-only --format= HEAD); RC=$?; test "$RC" = "0" &amp;&amp; test "$(printf '%s\n' "$FILES" | wc -l | tr -d ' ')" = "1" &amp;&amp; printf '%s\n' "$FILES" | rg -q '09-MUTATION-LOG\.md$' &amp;&amp; SUBJ=$(git log -1 --format=%s) &amp;&amp; printf '%s\n' "$SUBJ" | rg -q '^docs\(09-06\): ' &amp;&amp; test -z "$(git status --porcelain --untracked-files=no)"</automated>
    <fails_when>the family-heading count is no longer exactly 4 or the Closing heading is no longer exactly 1 (09-05's frozen gate on this log would go red), the prior phase-close subsection was disturbed, the docs commit touches any file other than the mutation log, its subject is not `docs(09-06): `, or tracked changes remain uncommitted</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n "pnpm -C web check" 09-MUTATION-LOG.md` returns ≥ 1 line, all inside a single new `### Gate-hygiene addendum (09-06)` subsection under `## Closing`.
    - The addendum cites `28d5d795` (introducer), `3c0f4bc7` (verified-red HEAD), the Task 1 fix commit's short hash, the CR-01 test name, and the two `half MATCH` lines from the post-fix drift run.
    - The addendum names 09-05 Task 3's gate list as the enumeration that omitted svelte-check and states the forward rule (`pnpm -C web check` in every web-touching phase-close list, re-run after late review-fix commits).
    - `## Family (` heading count is still exactly 4 and `## Closing` exactly 1; no other section changed.
    - One `docs(09-06): …` commit touching only `09-MUTATION-LOG.md`; tracked tree clean.
  </acceptance_criteria>
  <done>The phase's own ledger records that its svelte-check gate was red between `28d5d795` and the 09-06 fix, why the phase-close and review passes missed it, and the forward rule — a future audit finds it by grepping the phase for `pnpm -C web check`.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

None new. No new symbols, files, rpcs, flags, or tests. This gap-closure plan modifies:
- `web/src/lib/components/browse/SourcePane.svelte` — one captured `getEditorLink` local inside the existing probe `$effect`; two call sites re-pointed at it (Task 1).
- `web/build/**` including `web/build/.build-manifest` — rebuilt by `task web:build`, drift-clean (Task 1, same commit).
- `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md` — one dated `### Gate-hygiene addendum (09-06)` subsection under `## Closing` (Task 2).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no trust boundary. The only rpc entry (`GetEditorLink` over the loopback Connect transport) and the only untrusted inputs (the persisted per-browser override riding as `GetEditorLinkRequest.template`, D-06/D-18; the server-resolved `url` rendered into `href` / `location.assign`) are exactly as 09-04 and 09-05 shipped and threat-modelled (`T-09-01`..`T-09-22` in `09-SECURITY.md`). Capturing a narrowed function reference changes which identifier names the call, not what is sent, when, or to whom. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06-01 | Tampering | `web/build/**` (rebuilt SPA bytes committed alongside the source change) | medium | mitigate | Task 1 runs `task web:build` then `task web:drift` and gates on both `source half MATCH` and `output half MATCH` plus `web:drift: PASS`; the rebuilt output and the source edit land in ONE `fix(09-06)` commit so no intermediate commit is drift-red (re-affirms `T-09-19`). |
| T-09-06-02 | Elevation of Privilege | `SourcePane.svelte` probe `$effect` — the narrowing capture (`getEditorLink = activeClient.getEditorLink.bind(activeClient)`) | low | accept | Verdict: no new capability. The capture is derived from the same optional `client` prop after the same guard; the request shape (`{path, line, col, template}`) and the `AbortController` signal are byte-identical; the CR-01 re-probe fires under the same condition. Prohibiting `as`/`!`/`@ts-ignore` in Task 1 ensures the fix restores the type proof rather than suppressing it. Behavioural pin: the unchanged CR-01 vitest case plus the D-11 scope guard (`getEditorLink` referenced only in SourcePane.svelte and the generated client). |
| T-09-06-03 | Repudiation | Phase record (`09-MUTATION-LOG.md`) | low | mitigate | Task 2 writes a dated, hash-citing addendum (introducer `28d5d795`, red HEAD `3c0f4bc7`, fix commit) so the gate regression and its cause are auditable without re-derivation; gated by `rg` counts. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Verdict: not applicable — no package is installed or upgraded by this plan; `pnpm -C web check`, `vitest`, and `task web:build` run against the existing frozen install (`web:deps`). No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `pnpm -C web check` exits 0 and its summary reports zero errors at HEAD (RED at `3c0f4bc7`: `COMPLETED 1169 FILES 2 ERRORS 0 WARNINGS 1 FILES_WITH_PROBLEMS`, exit 1 — reproduced at planning time and again by Task 1's RED gate before the edit).
- `task web:test` prints `web:test: PASS` with ≥ 542 passed; the four-file 09-04 gate passes with ≥ 28 tests; `web/tests/source-pane-editor-link.test.ts` and the CR-01 case inside it are byte-identical to `3c0f4bc7`.
- `task web:drift` prints `source half MATCH`, `output half MATCH`, `web:drift: PASS` at HEAD.
- `rg -o 'template: resolved' web/src/lib/components/browse/SourcePane.svelte | wc -l` = 1; `rg -l getEditorLink web/src` names only SourcePane.svelte and `web/src/lib/gen/ui_pb.ts`; `location.assign` count 1; no TypeScript suppression added.
- `rg -n "pnpm -C web check" 09-MUTATION-LOG.md` ≥ 1, inside exactly one `### Gate-hygiene addendum (09-06)` subsection; `## Family (` count still 4; `## Closing` count still 1.
- Two commits: `fix(09-06): …` (SourcePane.svelte + `web/build/**`) and `docs(09-06): …` (mutation log only); tracked tree clean; no CI-skip marker.
</verification>

<success_criteria>
- must_haves truth 1: `pnpm -C web check` reports 0 errors at HEAD — Task 1 GREEN gate (exit 0 + case-insensitive `0 errors` summary + zero `ERROR` lines).
- must_haves truth 2: `task web:drift` reports MATCH at HEAD — Task 1 drift gate (both halves MATCH + PASS line) after `task web:build`, committed with the source.
- must_haves truth 3: the CR-01 corrective re-probe path is unchanged — the CR-01 test passes with the test file byte-identical to `3c0f4bc7`, and `template: resolved` appears exactly once in SourcePane.svelte (D-18 preset resolution from the wire untouched; D-06 override still rides only as the rpc `template`).
- must_haves truth 4: the svelte-check gate gap is recorded in a phase artifact — Task 2's addendum in `09-MUTATION-LOG.md` naming `28d5d795`, `3c0f4bc7`, the fix commit, the 09-05 gate-list omission, and the forward rule, with `pnpm -C web check` grep-able ≥ 1.
- BRW-11 (header link / re-probe path) and BRW-12 (preset override) keep their verified status: no behavioural change, all 09-04 inherited invariants (D-11 scope, `location.assign` count, no `window.open`) still hold.
</success_criteria>

<output>
Create `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-06-SUMMARY.md` when done. Paste: the RED svelte-check tail (2 ERRORS at 446:31, exit 1) and the GREEN tail (0 ERRORS, exit 0); the four-file vitest counts and `task web:test`'s observed `numTotalTests`/`numPassedTests` line; both `half MATCH` lines and `web:drift: PASS`; the two commit hashes; and the exact diff hunk of the SourcePane.svelte change (it should be a capture line plus two call-site substitutions and nothing else).
</output>
