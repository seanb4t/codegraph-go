---
phase: 01-defect-flake-burn-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/scripts/graph-console-check.mjs
  - Taskfile.yml
  - corpora/graph-console-check.json
autonomous: true
requirements: [FIX-04, FIX-05]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`node web/scripts/graph-console-check.mjs --self-test` exits 0 and prints a non-zero count of synthetically-planted console entries it recaptured — a capture harness that cannot see a planted entry is never trusted (rule 84d1gfpywd)."
    - "Run against the CURRENT (pre-fix) embedded build, `graph-console-check.mjs --corpus self` exits non-zero and its verdict JSON records at least 2 console entries whose text names the invalid vertical-alignment style value — the RED demonstration for FIX-05's stylesheet half."
    - "Run against the CURRENT (pre-fix) embedded build on the pinned guava corpus, the verdict JSON records at least one console entry matching cytoscape's invalid-endpoints warning text, and for each such entry records the edge id, its source and target node ids, their positions and their bounding boxes — the diagnosis FIX-05's layout fix is derived from."
    - "`task check:graph-console` exists as a Taskfile target, declares preconditions for node, a built ./codegraph binary and a fetched+indexed guava corpus, and runs --self-test before the real check."
    - "The committed verdict at corpora/graph-console-check.json carries schemaVersion, the corpus repo+sha it was measured against, and the browser identity — the v0.13.0 GRF-09 committed-observation shape."
    - statement: "Concurrent GET requests for the same static SPA asset return identical bytes (the embedded FS is read-only), so a parallel page load cannot produce a torn asset that manifests as a console entry."
      verification: backstop
  artifacts:
    - web/scripts/graph-console-check.mjs
    - corpora/graph-console-check.json
    - "Taskfile.yml target `check:graph-console`"
  key_links:
    - "graph-console-check.mjs → ./codegraph ui --no-open --path <repo> → the //go:embed all:build SPA → /graph → cytoscape/cytoscape-elk console output"
    - "The invalid-endpoints console text → the edge id it names → cy.getElementById(id).source()/.target() positions and bounding boxes via page.evaluate — the only path from a warning string to a named overlapping pair"
  prohibitions:
    - "No new package is added to web/package.json — @playwright/test is already a devDependency and RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase."
    - "No jsdom or headless-DOM substitute for real Chromium — v0.12.0's recorded lesson is that jsdom never exercises the renderer path that produces these entries."
    - "No dependency patch: no `pnpm patch`, no `patches/` directory (D-06)."
    - "This gate is NOT wired into any .github/workflows/ file — guava is too heavy for every CI run (D-09)."

flagged_assumptions:
  - id: EDGE-FIX-04-01
    requirement: FIX-04
    probe: "unclassified — review manually"
    status: unresolved
    note: "Homed in 01-08 (the plan that lands the FIX-04 teardown fix); recorded here only so the phase-level count reconciles."
---

<objective>
Build the permanent live-Chromium console gate for `/graph`, and use it to record the
pre-fix RED evidence and the FIX-05 overlap diagnosis that the two later web plans
(01-08, 01-09) are derived from.

Purpose: FIX-04 and FIX-05 are the two highest-uncertainty items in this phase. The
mechanism for both is known from vendored source (01-RESEARCH.md Investigations 1 and 2)
but neither has been observed live, and FIX-05's actual fix cannot be chosen until a live
run names which collapsed pair overlaps. This plan produces the instrument first, proves
it can see the defects, and captures the diagnosis — so the fixes that follow are
informed rather than speculative.

Output: `web/scripts/graph-console-check.mjs`, a `check:graph-console` Taskfile target,
and a committed pre-fix verdict at `corpora/graph-console-check.json`.

**Tracer note (TRACER_MODE=true):** this phase closes nine defects across five disjoint
subsystems (web SPA, `internal/cli`, `internal/daemon`, `internal/bench`, GitHub Actions
workflows). No single path crosses all five, so there is no phase-wide vertical slice to
draw. Task 1 below is the phase's tracer: it is the thinnest path that touches every layer
of the deepest-unknown lane — Go binary boot → embedded SPA → real Chromium → cytoscape
runtime → committed verdict — and it carries a real end-to-end `<verify>`. It is
production-quality and permanent: it ships as the gate, not as a probe. The other Wave-1
plans are independent lanes in other subsystems, each leading with its own RED-demonstrated
gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-defect-flake-burn-down/01-CONTEXT.md
@.planning/phases/01-defect-flake-burn-down/01-RESEARCH.md
@.planning/phases/01-defect-flake-burn-down/01-PATTERNS.md
@.planning/phases/01-defect-flake-burn-down/01-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end console capture on this repo's graph — one corpus, one path</name>
  <files>web/scripts/graph-console-check.mjs, Taskfile.yml, corpora/graph-console-check.json</files>
  <read_first>
    - web/scripts/breadcrumb-check.mjs — `repoRoot()`, `startCodegraphUi()`, `stopChild()`, the binary-not-found and no-index error messages, and the argOr flag-parsing helper. This is the boot-the-real-binary shape to copy verbatim.
    - web/scripts/graph-expand-check.mjs — the `page.on('pageerror')`-registered-before-navigation shape, the `record` + `fs.writeFileSync(args.out, JSON.stringify(record, null, 2) + '\n')` verdict-write shape, the `browserIdentity` field, and the `isMain` entry-point block.
    - web/scripts/check-no-force-layout.mjs — the `--self-test` positive-control convention this repo already uses for a scan that must never be trusted until it has been seen to detect a planted violation.
    - corpora/graph-expand-check.json — the existing committed verdict shape for a `web/scripts/*-check.mjs` script.
    - corpora/graph-cluster-observations.json — the schemaVersion / corpus{repo,sha} committed-observation policy shape (v0.13.0 GRF-09).
    - Taskfile.yml lines 1796-1814 (`check:no-force-layout`) — the precondition/cmds target shape, including the `--self-test`-then-real-scan command ordering.
    - web/src/lib/components/graph/graph-style.ts lines 125-141 — the `node[?isSymbol]` selector whose invalid vertical-alignment value produces the two warnings this task must observe.
  </read_first>
  <action>
Create `web/scripts/graph-console-check.mjs` following the five sibling `web/scripts/*-check.mjs`
conventions exactly: ESM imports of `@playwright/test`'s `chromium`, `node:fs`, `node:path`,
`node:url`, `node:crypto` and `{ spawn }` from `node:child_process`; a `repoRoot()` helper; the
`isMain` guard at the bottom; a diagnostic JSON written on EVERY exit path including failure.

Flags: `--self-test`, `--corpus <self|guava|self,guava>` (default `self,guava`), `--binary <path>`
(default `<repoRoot>/codegraph`), `--out <path>` (default `<repoRoot>/corpora/graph-console-check.json`).
This task wires the `self` corpus only; Task 2 adds `guava`.

Capture contract — both halves, both registered BEFORE `page.goto`:
  - `page.on('pageerror', err => ...)` collecting `err.message` (D-08's "zero uncaught page errors").
  - `page.on('console', msg => ...)` collecting `{ type: msg.type(), text: msg.text(), location: msg.location() }`
    for every message whose `type()` is `warning` or `error`. This is the first script in this repo to
    register a console listener — there is no in-repo analog; use the Playwright API directly.

Classification: the verdict record carries `consoleWarnCount`, `consoleErrorCount`, `pageErrorCount`
and the full arrays. Carry an `allowlist` array in the script, EMPTY as shipped, where each future
entry would be `{ pattern, reason }`; the pass/fail verdict is computed against the non-allowlisted
remainder. D-08's bar is zero, so an empty allowlist is the correct initial state — do not
pre-populate it to make the current build pass.

`--self-test` is the positive control and MUST run before any real check: navigate to `about:blank`
(no binary boot needed), `page.evaluate` a block that emits one `console.warn`, one `console.error`
and throws one uncaught error from a `setTimeout`, then assert the harness recaptured at least one
of each. Exit non-zero with a named message if any of the three is missing. Print the three
recaptured counts — a self-test that prints nothing cannot distinguish "captured" from "ran nothing".

Real check per corpus: resolve the target repo path (for `self`, `repoRoot()`), assert the binary
exists (copy `breadcrumb-check.mjs`'s "run `task build:release` first" message shape) and assert
`<repoPath>/.codegraph/store` exists (copy its no-index message shape, pointing at this task's
`<precondition>`), boot `<binary> ui --no-open --path <repoPath>` via the `startCodegraphUi` shape,
navigate to `<url>/graph`, wait for the graph to reach a settled state using the same `pollUntil`
convention the sibling scripts use (a non-zero rendered node count, then a short quiescence window
so a late-resolving ELK layout's console output is not missed), then tear the child down with the
`stopChild` SIGTERM→SIGKILL shape.

Verdict record: `{ schemaVersion: 1, success, generatedAt, browserIdentity: { name, version },
runs: [ { corpus, repo, sha, url, pageErrorCount, pageErrors, consoleWarnCount, consoleErrorCount,
consoleMessages, allowlistedCount } ] }`. `success` is true only when every run's non-allowlisted
page-error, warn and error counts are all zero. Write it with `fs.writeFileSync(args.out,
JSON.stringify(record, null, 2) + '\n')` after `mkdir` of its dirname, then `process.exit(success ? 0 : 1)`.

Add a `check:graph-console` target to `Taskfile.yml` modelled on `check:no-force-layout`'s
precondition/cmds shape: preconditions for `command -v node`, for `test -x ./codegraph` (message:
run `task build:release` first) and for `test -d .codegraph/store` (message: run `./codegraph index --force .` first);
cmds run `node web/scripts/graph-console-check.mjs --self-test` then `node web/scripts/graph-console-check.mjs`.
Task 2 adds the guava precondition. Do NOT reference this target from any file under `.github/workflows/` (D-09).

Then run it against the CURRENT, unmodified embedded build and COMMIT the resulting failing verdict
to `corpora/graph-console-check.json`. That committed RED verdict is this task's evidence, not a
side effect: it is what proves the gate can see the defects the later plans close. Paste the
`--self-test` output and the failing-run counts into the task's SUMMARY.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node web/scripts/graph-console-check.mjs --self-test</automated>
    <fails_when>non-zero exit, or the output does not print three non-zero recapture counts (one console.warn, one console.error, one pageerror) — a self-test that prints "0 captured" for any of the three means the listener is not wired</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task build:release && node web/scripts/graph-console-check.mjs --corpus self --out corpora/graph-console-check.json; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` (meaning the check itself exited 0 against the pre-fix build — the gate is vacuous and cannot see the known-present defects), or `task build:release` reports a non-zero exit</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && jq -e '[.runs[0].consoleMessages[] | select(.text | test("valign"))] | length >= 2' corpora/graph-console-check.json</automated>
    <fails_when>non-zero exit from jq (fewer than 2 captured console entries name the vertical-alignment style value), or jq reports a parse error on the verdict file</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -q 'check:graph-console' Taskfile.yml && ! rg -q 'graph-console-check' .github/workflows/</automated>
    <fails_when>non-zero exit — either the Taskfile target is absent, or a workflow file references the script (D-09 forbids CI wiring)</fails_when>
  </verify>
  <acceptance_criteria>
    - `web/scripts/graph-console-check.mjs` exists and registers BOTH `page.on('pageerror', ...)` and `page.on('console', ...)` before its first `page.goto` call (`rg -n "page.on\('console'" web/scripts/graph-console-check.mjs` returns a line number lower than the first `page.goto` line).
    - The script's `allowlist` array is present and empty: `rg -n 'allowlist' web/scripts/graph-console-check.mjs` matches, and no `{ pattern:` entry exists inside it.
    - `node web/scripts/graph-console-check.mjs --self-test` exits 0 and prints a recaptured count of at least 1 for each of console.warn, console.error and pageerror.
    - Against the unmodified pre-fix embedded build, the script exits non-zero and `corpora/graph-console-check.json` records `"success": false` with at least 2 console entries naming the vertical-alignment style value.
    - `corpora/graph-console-check.json` contains `schemaVersion`, `browserIdentity.name`, `browserIdentity.version`, and a `runs` array.
    - `Taskfile.yml` contains a `check:graph-console` target whose cmds run `--self-test` before the real check.
    - No file under `.github/workflows/` mentions `graph-console-check`.
  </acceptance_criteria>
  <done>The console gate exists, has been proven able to see a planted entry (self-test) and the real pre-fix defects (committed RED verdict), and is invocable via `task check:graph-console`.</done>
  <precondition>`./codegraph` is built (`task build:release`) and this repository has an index at `.codegraph/store` (`./codegraph index --force .`); Playwright's Chromium browser binary is installed for the pinned `@playwright/test` 1.62.1.</precondition>
</task>

<task type="auto">
  <name>Task 2: Extend to the pinned guava corpus and diagnose the overlapping pair</name>
  <files>web/scripts/graph-console-check.mjs, Taskfile.yml, corpora/graph-console-check.json</files>
  <read_first>
    - web/scripts/graph-console-check.mjs — the file Task 1 just created; the run loop being extended.
    - web/scripts/graph-live-update-check.mjs lines 57-90 — the verbatim `corpusRoot()` / `corpusDir(repo, sha)` resolution helpers (XDG_CACHE_HOME / HOME fallback, slug + 8-char sha256 digest of the repo string, `@sha` suffix) and its no-index-at-corpus error message shape.
    - corpora/selection.json — the pinned guava entry `google/guava@94f39958baf7ad51ddf9c70e406ed6b188194daa` (the repo/sha pair `corpusDir` must be called with).
    - .planning/phases/01-defect-flake-burn-down/01-RESEARCH.md § Investigation 2 — the verbatim `checkForInvalidEdgeWarning` emission code, the once-per-edge `rs.loggedErr` guard, and the three-step diagnostic technique (capture the edge id from the warning text, then `page.evaluate` its endpoints' positions and bounding boxes, then cross-reference LAYOUT_OPTIONS).
    - web/src/lib/components/graph/GraphCanvas.svelte lines 139-165 — the current `LAYOUT_OPTIONS` ELK block, which sets `elk.hierarchyHandling`, three interactive-strategy keys and a per-parent `elk.padding`, and sets no node-node spacing key at all.
  </read_first>
  <action>
Extend `graph-console-check.mjs` with the `guava` corpus target and the overlap diagnostic.

Corpus resolution: copy `graph-live-update-check.mjs`'s `corpusRoot()` and `corpusDir(repo, sha)`
helpers verbatim (same cache-dir precedence, same slug+digest+`@sha` layout) and resolve
`google/guava` at sha `94f39958baf7ad51ddf9c70e406ed6b188194daa` — read the pair from
`corpora/selection.json`'s `lockedSet` rather than hard-coding a second copy of the sha, and fail
loudly with the entry name if it is absent. Assert the corpus checkout exists and carries a
`.codegraph/store`; if not, exit non-zero with a message naming `task corpora:fetch` and
`./codegraph index --force <corpusPath>`, following `graph-live-update-check.mjs`'s own
"corpus not indexed at ..." message shape.

Overlap diagnostic: after the console listener has collected the run's messages, for every captured
entry whose text matches cytoscape's invalid-endpoints wording, extract the backtick-quoted edge id
from the message text, then `page.evaluate` back into the live instance to read, for that edge:
`source().id()`, `target().id()`, each endpoint's `position()`, each endpoint's `boundingBox()`,
each endpoint's `isParent()` and child count, and the edge's own rendered `startX/startY/endX/endY`
scratch values if reachable. Attach the results as a `invalidEndpointDiagnostics` array on that
corpus's run record. Guard the evaluate so a missing element or an API shape the instance does not
expose yields a recorded `{ edgeId, error }` entry rather than throwing — a diagnostic that crashes
loses the whole verdict.

Also record, per run, `nodeCount`, `edgeCount` and `collapsedNodeCount` from the live instance, so
the later layout fix can be judged against the same density the diagnosis was taken at.

Add the guava precondition to the `check:graph-console` Taskfile target: a precondition asserting the
resolved corpus directory exists, with a message naming `task corpora:fetch`.

Run the extended script against BOTH corpora on the CURRENT pre-fix build and commit the updated
`corpora/graph-console-check.json`. The guava run's `invalidEndpointDiagnostics` entries are the
FIX-05 diagnosis that plan 01-09 consumes — name the overlapping pair(s) explicitly in the task's
SUMMARY, including whether either endpoint is a zero-child collapsed node whose bounding box
degenerates to a point.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node web/scripts/graph-console-check.mjs --self-test</automated>
    <fails_when>non-zero exit, or any of the three recapture counts prints as 0 — the Task 1 positive control must survive the Task 2 edits</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node web/scripts/graph-console-check.mjs --out corpora/graph-console-check.json; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — the two-corpus check passed against a pre-fix build, meaning it is not seeing the known-present defects on either corpus</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && jq -e '(.runs | length) == 2 and ([.runs[] | select(.corpus == "guava")] | length) == 1 and ([.runs[] | select(.corpus == "guava") | .invalidEndpointDiagnostics[]? | select(.sourceId != null and .targetId != null)] | length) >= 1' corpora/graph-console-check.json</automated>
    <fails_when>non-zero exit from jq — either fewer than two corpus runs were recorded, or the guava run produced no diagnostic entry carrying both a resolved sourceId and targetId, meaning the warning text was captured but never traced back to a named pair</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -q '94f39958baf7ad51ddf9c70e406ed6b188194daa' corpora/graph-console-check.json</automated>
    <fails_when>non-zero exit — the committed verdict does not record the pinned guava sha it was measured against, so the observation is not reproducible</fails_when>
  </verify>
  <acceptance_criteria>
    - `graph-console-check.mjs` resolves the guava corpus via a `corpusDir(repo, sha)` helper structurally identical to `graph-live-update-check.mjs`'s, and reads the repo/sha pair from `corpora/selection.json` rather than a second hard-coded literal.
    - Running with no `--corpus` flag produces a verdict with exactly two entries in `runs`, one `corpus: "self"` and one `corpus: "guava"`.
    - The guava run record contains a non-empty `invalidEndpointDiagnostics` array, and at least one entry carries a non-null `sourceId`, `targetId`, both endpoints' `position` and both endpoints' `boundingBox`.
    - Every run record contains `nodeCount`, `edgeCount` and `collapsedNodeCount`.
    - `corpora/graph-console-check.json` records the pinned guava sha `94f39958baf7ad51ddf9c70e406ed6b188194daa`.
    - The `check:graph-console` Taskfile target carries a precondition naming `task corpora:fetch`.
    - The task SUMMARY names the specific overlapping node pair(s) and states whether either endpoint is a zero-child collapsed node.
  </acceptance_criteria>
  <done>The gate covers both corpora, the pre-fix guava run has named the overlapping pair(s) with coordinates and bounding boxes, and the committed verdict records the diagnosis plan 01-09 consumes.</done>
  <precondition>The pinned guava corpus is fetched (`task corpora:fetch`) into the cache directory and has been indexed (`./codegraph index --force <corpusPath>`); this is external setup the plan cannot perform for a corpus it does not vendor.</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party corpus source → indexer → SPA | guava's source tree is untrusted third-party content; its file and symbol names flow through the index into cytoscape node labels rendered in a browser |
| local browser → spawned `codegraph ui` HTTP server | the check script drives a real browser against a locally-bound server; no external network origin is involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Tampering | `graph-console-check.mjs` allowlist | medium | mitigate | The allowlist ships EMPTY with a `{ pattern, reason }` shape; a future entry must carry a reason. An allowlist pre-populated to make the current build pass would convert D-08's zero bar into a vacuous gate — the `--self-test` positive control plus the committed RED verdict make that visible. |
| T-01-01-02 | Information Disclosure | `corpora/graph-console-check.json` | low | accept | The committed verdict carries corpus file/symbol paths from a public pinned corpus and this repository's own paths — already public. No credential, token or local absolute home path is written (paths are recorded relative to the corpus root). |
| T-01-01-03 | Denial of Service | spawned `codegraph ui` child process | low | mitigate | `stopChild` SIGTERM→SIGKILL teardown with a 3s grace window on every exit path, copied from `breadcrumb-check.mjs`, so a failing check never leaves an orphaned server holding a port or a store lock. |
| T-01-01-SC | Tampering | npm/pnpm installs | low | accept | This plan adds no package. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase; `@playwright/test` 1.62.1 is already a pinned devDependency. No package-legitimacy checkpoint is required because there is no install task. |
</threat_model>

<verification>
- `node web/scripts/graph-console-check.mjs --self-test` exits 0 with three non-zero recapture counts.
- The two-corpus run against the pre-fix build exits non-zero and `corpora/graph-console-check.json` records `"success": false`.
- The guava run names at least one overlapping pair with positions and bounding boxes.
- `task check:graph-console` is defined and no workflow references it.
</verification>

<success_criteria>
The phase has a permanent, self-tested, two-corpus `/graph` console gate; the pre-fix RED evidence
for FIX-04/FIX-05 is committed; and the FIX-05 overlap diagnosis is on record with coordinates.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Path / name |
|---|---|---|
| `graph-console-check.mjs` | new script | `web/scripts/graph-console-check.mjs` |
| `--self-test` flag | new CLI flag | `graph-console-check.mjs --self-test` |
| `--corpus` flag | new CLI flag | `graph-console-check.mjs --corpus <self\|guava\|self,guava>` |
| `--binary` flag | new CLI flag | `graph-console-check.mjs --binary <path>` |
| `--out` flag | new CLI flag | `graph-console-check.mjs --out <path>` |
| `check:graph-console` | new Taskfile target | `Taskfile.yml` |
| `graph-console-check.json` | new committed observation | `corpora/graph-console-check.json` |
| `invalidEndpointDiagnostics` | new verdict field | `corpora/graph-console-check.json` → `runs[].invalidEndpointDiagnostics` |
</artifacts_produced>

<output>
Create `.planning/phases/01-defect-flake-burn-down/01-01-SUMMARY.md` when done.
</output>
