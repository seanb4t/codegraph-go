---
phase: 01-engine-seam-wire-protocol-secure-transport
plan: 09
type: execute
wave: 5
depends_on: ["01-05", "01-08"]
files_modified:
  - internal/uiproto/uiv1/ui.proto
  - internal/uiproto/uiv1/ui.pb.go
  - internal/uiproto/uiv1/uiv1connect/ui.connect.go
  - internal/uiserver/handlers.go
  - internal/uiserver/handlers_test.go
  - internal/uiserver/readonly_test.go
autonomous: true
requirements: [RPC-01, RPC-02, SRV-03]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Node detail results reach the wire through `(*Engine).NodeDetail` and explore results through `(*Engine).ExploreDetail` — the same builders CLI and MCP go through, never a second query path (D-01)."
    - "The wire node-detail response carries all three shapes as a discriminated response — file, one definition, and many definitions with PER-CANDIDATE calls and called-by — so the multi-definition case is not flattened into a single slot on its way out (D-02)."
    - "The wire multi-definition response applies the UI's OWN candidate cap, declared as a named constant, and reports the true total so a client can render 'showing N of M'; it does not inherit the markdown renderer's budget, which exists for a different medium."
    - "`ExploreResponse` models sources PER GROUP, keyed to the group they belong to, because `ExploreResult.Sources` is a map keyed by group path and a single response-level blob would lose that association."
    - "No RPC can mutate the index: the service declares only read methods, and the method-name set is asserted by set equality in both directions so neither a mutating verb nor a dropped read can land without failing a test (SRV-03)."
    - "The UI proto's field numbers are pinned by a stable known-number fixture plus a duplicate-number check — not by a contiguity assertion, which additive-only evolution with `reserved` gaps would legitimately violate (RPC-01, D-02a)."
    - "Every entry in that fixture resolves to a field this plan actually declares: the test asserts `resolved == len(fixture)` and a non-zero fixture length, and names an unresolved entry in its failure rather than skipping it — so the guard is runnable in the wave that declares it and cannot pass vacuously (rule 84d1gfpywd, RPC-01)."
  artifacts:
    - internal/uiproto/uiv1/ui.proto
    - internal/uiserver/handlers.go
    - internal/uiserver/readonly_test.go
  key_links:
    - "`MultiDefDetail.Definition(n)` is LAZY and reads one candidate's source on demand. The handler calls it exactly `uiMultiDefCap` times and no more, so an out-of-cap candidate is no more read here than it is by `Node()`."
    - "`ExploreResult.Sources` is a `map[string][]byte` keyed by the group's `Path`. The wire `ExploreGroup` carries its own source field so the keying survives the mapping."
    - "The three messages plan 01-10 attaches `SourceBlob` to — `ExploreGroup`, `GetNodeDetailResponse` and `NodeDefinition` — are DECLARED here, one wave before 01-10's one-way checkpoint, so that checkpoint locks an attachment point whose containing message a human can already read in the tree. Their source field NUMBERS are not pre-allocated here: a protobuf descriptor carries no comments, so a commented number is invisible to a descriptor guard, and a `reserved` clause would forbid 01-10 the very use it needs. Intent is documentation in `ui.proto`; enforcement is the known-number fixture over what this plan declares, which 01-10 must extend without disturbing."
    - "The generated `UIServiceHandler` interface is the compile-time completeness check: adding an rpc to the proto and not implementing it fails the build."
  prohibitions:
    - "Never gate a task on a bare `go test ... | rg '--- PASS' | wc -l` count whose exit status is discarded. A shell pipeline's status is the LAST command's, so N passing subtests plus one `--- FAIL` satisfies a count-only gate. Every automated gate captures `go test`'s output and status BEFORE any pipe and requires BOTH: status 0 AND the `--- PASS` floor. The count alone is vacuous when a test fails; the status alone is vacuous when the `-run` pattern matches nothing. The sole scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence."
    - "Never renumber or reuse a protobuf field number once it has shipped; retiring a field means adding its number to a `reserved` clause."
    - "Never call `Node()` or `Explore()` and parse their markdown, and never re-derive their data with a bespoke traversal. The UI is a third consumer of one builder path, not a second implementation (D-01)."
    - "Never write `test -z \"$(git status --porcelain <path>)\"` without a preceding `test -d <path> &&` existence guard. `git status --porcelain` on a missing or typo'd pathspec exits 0 with EMPTY stdout — the 'could not open directory' warning goes to stderr — so the check passes vacuously against a path that was never opened."
    - "Never positive-control a zero-count gate against a DIFFERENT file than the one the gate guards. `rg` prints nothing for a nonexistent path just as it does for a clean file, so a typo'd or renamed target reads `0` while the guarded file is never opened. The control must run the identical pipeline against the SAME file with a pattern that file genuinely contains."
    - "Never score a count with a regex that accepts zero. `rg -q -e 'compared [0-9]+ generated files'` matches `compared 0 generated files`, so it is a presence check on the count and not a floor. Capture the number and assert the floor numerically."
    - "Never write an unanchored identifier grep as a forbidden-pattern gate. Anchor it to the declaration form — `func gather[A-Z]`, never `\\bgather[A-Z]` — because a trailing comment or a `/* */` block survives `rg -v '^\\s*//'` and turns the gate red, or green, for the wrong reason."
    - "Never assert emptiness with an unranged `git diff`. Under atomic-commit-per-task the working tree is clean at verification time, so `git diff --stat <path>` is empty by construction whether or not the file was edited. Assert over an explicit `<phase-base>..HEAD` revision range."
    - "Never write a gate whose floor can be met without the deliverable it names existing. This is the SCOPE half of gate hygiene and no shape check reaches it: a scope-vacuous gate is well-formed, parses, honors exit status and carries a same-file positive control, and still scores nothing the task owns. Two requirements, both mandatory and both checkable by reading the plan. (a) OWNERSHIP — every gate carries at least one leg whose test-selection pattern matches ONLY test functions this task itself declares, with a floor derived from that leg's own functions and subtests alone; where a pattern also matches an earlier plan's tests, the derivation must say so and say why those inherited tests are the intended subject of that leg. (b) HOMING — every behavior bullet is named by, or names, a test function or subtest that the task's own floor derivation counts, or the derivation states in writing that the bullet is covered by a specific named criterion or is deliberately out of scope and why. A bullet no derivation reaches is a described behavior with no executable owner, and a leg whose floor another plan's tests already clear cannot fail when this task writes nothing."
---

<objective>
Bring the two seam-backed reads onto the wire and make "read-only by construction" an
asserted property rather than a stated intention.

Purpose: `GetNodeDetail` and `Explore` are the two reads that did not have structured
results until plans 01-04 and 01-05 extracted them, which is why they land after the other
seven. Both carry shapes that resist a naive mapping. Node detail is a sum with a
per-candidate multi-definition case whose data is gathered lazily; explore carries source
blobs in a map keyed by group path. A response shape that flattens either one loses
information the Engine actually has, and every Phase 3 through 6 view reads these two
responses.

With all nine rpcs present, the method set is finally complete enough to pin. SRV-03's
read-only property is asserted here as a set equality in both directions rather than as a
verb blacklist, because a negative-only guard passes vacuously the moment its anchor stops
matching — the defect class this repository already carries in forty-nine places.

Output: `GetNodeDetail`, `Explore`, and three guards that each report what they inspected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-05-SUMMARY.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-08-SUMMARY.md
</context>

## Artifacts this phase produces

**Created by this plan (01-09):**

- `internal/uiproto/uiv1/ui.proto` gains rpcs `GetNodeDetail` and `Explore`
- Messages `GetNodeDetailRequest`/`GetNodeDetailResponse`, `NodeDefinition`, `ExploreRequest`/`ExploreResponse`, `ExploreGroup`, `BlastEntry`, and the `NodeDetailMode` enum
- Regenerated `ui.pb.go` and `uiv1connect/ui.connect.go` (`UIServiceHandler` reaches nine methods)
- `internal/uiserver/handlers.go` — `GetNodeDetail` and `Explore` handlers, the `uiMultiDefCap` constant, and the mappers `nodeDetailToProto`, `nodeDefinitionToProto`, `exploreResultToProto`, `exploreGroupToProto`, `blastEntryToProto`
- `internal/uiserver/readonly_test.go` — `TestUIServiceMethodSetIsExactlyTheReadSet`, `TestUIServiceDeclaresNoMutatingMethod`, `TestUIProtoFieldNumbersAreStableAndUnique`
- `internal/uiserver/handlers_test.go` gains `TestUIServiceNodeDetailCoversAllThreeModes`, `TestUIServiceNodeDetailGoesThroughEngineBuilder`, `TestUIServiceNodeDetailCapsCandidatesAndReportsTheTotal`, `TestUIServiceExploreGroupsCarryTheirOwnSources`, `TestUIServiceExploreZeroMatchIsNotAnError`

**Created elsewhere in this phase** (listed so drift verification sees the whole set):
`internal/goldenspec`; `testdata/golden/byte_identity_test.go`; `internal/uiserver`'s
`Options`, `DefaultAddr`, `Server`, `Listen`, `Serve`, `URL`, `Close`, `originHostGuard`,
`allowedHosts`, `allowedOrigins`, `uiService`, `openEngine`, `withEngine`,
`mapEngineError`, the seven earlier handlers and their mappers, `truncateSource`,
`sourceLineCap`, `sourceByteCap`, `transportSendMaxBytes`, `transportReadMaxBytes`,
`errIndexingInProgress`, `indexingInProgressMessage`, `degradedStatus`, `classifyDegrade`;
`internal/uiproto/uiv1`'s `SourceBlob` and the populated `IndexingInProgress`;
`internal/cli/ui.go`; `internal/textutil.TruncateOnRuneBoundary`; `internal/query`'s
`NodeDetail` family, `ExploreResult` family, `ErrNotFound`, `ErrInvalidArgument`,
`(*Engine).IndexMeta`, `(*Engine).SourceFor` and the `build*` builders;
`schema.Meta.commit_sha` field 8 and `schema.IndexedCommitSHA`;
`internal/indexer/commit.go`'s `resolveHeadCommitSHA` and `gitExecLookPath`;
`internal/mcp`'s `looksLikeJSONRPCResponse`, `sniffedOutbound`, `decrementPending`,
`pendingUnderflows`; `buf.yaml`, `buf.gen.yaml`, `go.tool-proto.mod`, `Taskfile.yml` tasks
`proto:gen` and `proto:drift`, `internal/upgrade/proto_task_test.go`.

## Gate conventions (phase-wide canon — byte-identical in all eleven plans, and checkable)

This section is replicated verbatim across `01-01-PLAN.md` … `01-11-PLAN.md`. The claim is
checkable rather than merely asserted — one command, run from the phase directory, prints
**one** distinct digest if the eleven copies agree and more than one if any has drifted:

```
for f in 01-*-PLAN.md; do sed -n '/^## Gate conventions/,/^<tasks>$/p' "$f" | shasum; done | sort -u | wc -l
```

A future planner editing one copy must edit all eleven, and this is how a reviewer catches it
if they do not. (Nothing at execution time changes this section, so no `<automated>` gate owns
it — the drift risk is at planning time, which is where the check belongs.)

**`--- PASS` counting.** `go test -v` prints one `--- PASS:` line for the parent test
function AND one for every `t.Run` subtest, including nested ones. Every PASS floor in this
phase counts BOTH: a test function with three subtests contributes FOUR lines, so
`rg -o -e '--- PASS' | wc -l` over `go test -v` output equals
`len(test functions) + len(all subtests)`. Derive every floor that way and show the
arithmetic in the acceptance criterion that states it, so a reviewer can check the floor
instead of re-deriving it.

**Echo label.** Every gate prints `PASS lines:` — never `PASS tests:`, never
`PASS subtests:`. The label names what is counted, and what is counted is lines.

**The conjunction (D-04 as corrected in `01-CONTEXT.md`).** A gate passes only when the
test command's exit status is 0 AND the `--- PASS` floor is met. Neither is sufficient
alone: `go test -run PATTERN` exits 0 when the pattern matches nothing, and a shell
pipeline's `$?` is the LAST command's, so a bare count passes with N passing subtests plus
one `--- FAIL`. The single scoped exception is a deliberate-mutation RED observation, where
a non-zero exit IS the expected evidence and the observation is scored by `--- FAIL` /
`--- PASS` counts rather than by status.

**The five vacuity shapes cycle 3 found in this phase's own gates.** Each is a prohibition
in `must_haves.prohibitions`. They are restated here together because they are one class —
a guard that reads green against a file it never opened:

1. `git status --porcelain <path>` exits 0 with empty stdout on a MISSING or typo'd path
   (the warning goes to stderr). Always prefix `test -d <path> &&`.
2. A zero-count gate positive-controlled against a DIFFERENT file proves nothing about the
   guarded file: `rg` prints nothing for a nonexistent path too. Control on the SAME file.
3. `rg -q 'compared [0-9]+ generated files'` accepts `compared 0 generated files`. Capture
   the number; assert the floor numerically.
4. An unanchored identifier grep (`\bgather[A-Z]`) is satisfiable by a trailing or `/* */`
   comment that `rg -v '^\s*//'` does not strip. Anchor to the declaration form.
5. An unranged `git diff` is empty by construction at verification time under
   atomic-commit-per-task. Assert over `<phase-base>..HEAD`.

**The scope shape cycle 5 found, which none of the five above reaches.** The five shapes are
all about how a gate is WRITTEN. A gate can satisfy every one of them and still be vacuous
because of what it is SCOPED to: if its floor is already cleared by test functions belonging
to an EARLIER plan that happen to share the selection pattern, then the pattern matching none
of this task's own tests costs nothing — `go test` exits 0 on a pattern that matches nothing,
and the inherited tests supply the count. The sixth prohibition in `must_haves.prohibitions`
closes it with two requirements: every gate carries at least one leg scoped to tests THIS task
declares with a floor derived from that leg alone, and every behavior bullet has a named home
in the derivation. Both are checkable by reading the plan, which is where the defect lives.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `GetNodeDetail` — all three modes on the wire, with per-candidate multi-def</name>
  <files>internal/uiproto/uiv1/ui.proto, internal/uiproto/uiv1/ui.pb.go, internal/uiproto/uiv1/uiv1connect/ui.connect.go, internal/uiserver/handlers.go, internal/uiserver/handlers_test.go</files>
  <read_first>
    - internal/query/detail.go — `NodeDetail`, `NodeDetailMode` and its constants, `FileDetail`, `DefinitionDetail`, `MultiDefDetail` and its lazy `Definition` method, exactly as plan 01-04 defined them, including the doc comment stating that the caller chooses its own cap
    - internal/query/render_markdown.go lines 138-148 — where `nodeMultiDefHardCap = 16` and `nodeMultiDefBodyBudget = 12000` are DECLARED (`:146-147`), the markdown renderer's OWN budget, which the wire must not inherit. The values are invisible at the use sites around `:211`/`:220`, so read the declaration block
    - internal/uiproto/uiv1/ui.proto — the shared `Node` message and the file-level additive-only comment plan 01-08 established
    - internal/uiserver/handlers.go — `withEngine`, `mapEngineError` and the mappers plan 01-08 wrote
    - internal/query/errors.go — `ErrNotFound` and `ErrInvalidArgument`
  </read_first>
  <behavior>
    - `TestUIServiceNodeDetailCoversAllThreeModes`: a file path returns the file mode with the path populated; a single-definition symbol returns the single-definition mode with node, calls and called-by; an overloaded symbol returns the multi-definition mode with the symbol and a populated candidate list.
    - `TestUIServiceNodeDetailGoesThroughEngineBuilder`: for a single-definition symbol, the RPC's calls and called-by sets equal `(*Engine).NodeDetail`'s for the same symbol, computed independently in the test. For an overloaded symbol, each returned candidate's calls and called-by equal that candidate's own per-candidate lookup.
    - `TestUIServiceNodeDetailCapsCandidatesAndReportsTheTotal`: a symbol with more definitions than `uiMultiDefCap` returns exactly `uiMultiDefCap` candidates carrying gathered detail, the remaining candidates listed without detail, and a total-candidate count equal to the real number of matches.
    - A not-found symbol returns `connect.CodeNotFound`; a request with neither a symbol nor a file returns `connect.CodeInvalidArgument`.
    - A per-candidate lookup that fails — for instance an unreadable candidate file within the cap — fails the RPC with the mapped code rather than returning a partially-populated response.
  </behavior>
  <action>
    Add the `NodeDetailMode` enum to `internal/uiproto/uiv1/ui.proto`, mirroring
    `internal/query`'s three modes with a zero-valued unspecified member as proto3 requires.

    Add `GetNodeDetailRequest` carrying symbol, file and an optional line, and
    `GetNodeDetailResponse` as a discriminated response: the mode, a path for the file
    shape, a node plus calls plus called-by for the single-definition shape, and a symbol
    plus a repeated `NodeDefinition` plus a total-candidate count for the multi-definition
    shape. Document on the message which fields each mode populates and that the others are
    empty — that asymmetry is the whole point of carrying the mode, and a client that reads
    the wrong field for a mode gets a zero value rather than an error.

    `NodeDefinition` carries one candidate's node, calls, called-by, and a
    detail-was-gathered flag. The flag exists because the response deliberately lists
    candidates beyond the cap without gathering them: a client rendering "showing N of M"
    needs to know which entries are complete.

    Do NOT try to pre-allocate the source field numbers plan 01-10 will use. Plan 01-10
    attaches a `SourceBlob`-typed source field to `GetNodeDetailResponse`, `NodeDefinition`
    and `ExploreGroup`, and protobuf offers no mechanism that makes a not-yet-declared
    number binding: a descriptor carries declared fields and `reserved` ranges and nothing
    else, so a comment is invisible to any descriptor-reading guard, and a real
    `reserved N;` clause would make `protoc`/`buf` REFUSE 01-10's use of that same number.
    An earlier revision of this plan tried the comment route and paired it with a
    descriptor-reading fixture; that combination could only fail in 01-09, pass vacuously,
    or block 01-10, so it is withdrawn.

    Record the intent as documentation instead, where both a reader and 01-10's checkpoint
    will find it: state in `ui.proto`'s file-level comment that plan 01-10 adds a
    `SourceBlob`-typed source field to those three messages at the next free number in each,
    and name what that next free number is for each of the three as of the end of this plan.
    That paragraph is intent, not a guard, and this plan does not pretend otherwise.

    The real cross-wave protection is the stability half of Task 3's known-number fixture:
    every number THIS plan allocates is pinned by a descriptor-backed assertion, so 01-10
    can add fields but cannot disturb one — which is the only property that was ever
    enforceable, because nothing has shipped yet and D-02a binds from the shipped commit
    onward.

    Regenerate with `task proto:gen`.

    Implement `GetNodeDetail` on `uiService` through `withEngine`, calling
    `(*Engine).NodeDetail` and nothing else. Do not call `Node()` and parse its markdown,
    and do not re-derive the same data with a bespoke traversal: the UI is a third consumer
    of one builder path, not a second implementation (D-01), and the seam exists precisely
    so this handler has nothing to reimplement.

    Map the three modes in `nodeDetailToProto`. For the multi-definition mode, declare a
    named constant `uiMultiDefCap` in `internal/uiserver` with a doc comment stating three
    things: what it bounds, that it is the UI's own cap and deliberately independent of
    `nodeMultiDefHardCap` because a browser and a markdown block are different media, and
    that the response reports the true total so the client can render the remainder. Ask
    the multi-definition value for exactly the first `uiMultiDefCap` matches and no more —
    the per-candidate lookup is lazy, so asking for every match would perform reads that
    neither `Node()` nor the Engine performs. List the remaining matches with their node
    record and the detail-gathered flag unset.

    When a per-candidate lookup returns an error, fail the whole RPC through
    `mapEngineError` rather than returning a partially-populated response. This matches
    `Node()`'s behavior exactly and avoids inventing partial-failure semantics that every
    Phase 3 through 6 view would then have to handle. Record in the SUMMARY that
    per-candidate partial failure was considered and deferred: it is an additive change to
    `NodeDefinition` if a real view ever needs it, and inventing it before a view asks would
    be a shape nobody validated.

    Extend `handlers_test.go` with the behavior block's cases, driven through a real Connect
    client against a real listener.
  </action>
  <verify>
    <automated>task proto:gen && test -d internal/uiproto && test -z "$(git status --porcelain internal/uiproto/)" && go build ./... && { OUT=$(go test -v -count=1 -race -run 'TestUIServiceNodeDetail' ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 7 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -race -run 'TestUIServiceNodeDetail' ./internal/uiserver/` emits at least 7 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** Four test functions match: `TestUIServiceNodeDetailCoversAllThreeModes` with three `t.Run` subtests (`file`, `single-def`, `multi-def`) = 4; `TestUIServiceNodeDetailGoesThroughEngineBuilder` = 1; `TestUIServiceNodeDetailCapsCandidatesAndReportsTheTotal` = 1; `TestUIServiceNodeDetailErrorClasses` with three (`symbol-not-found`, `neither-symbol-nor-file`, `unreadable-candidate-within-the-cap`) = 4, which is the named home for the behavior block's last two bullets. Total **10** against a floor of 7. **Behavior-bullet homes, completed for all five bullets:** bullets 1, 2 and 3 name their own test functions; bullets 4 and 5 (the not-found / neither-symbol-nor-file codes, and the failing per-candidate lookup) are `TestUIServiceNodeDetailErrorClasses`'s three subtests. No bullet is orphaned. **Ownership:** all four functions are declared by THIS task and the `TestUIServiceNodeDetail` atom matches no earlier plan's test, so the entire count is this task's own deliverable.
    - `TestUIServiceNodeDetailGoesThroughEngineBuilder` asserts the RPC's calls and called-by sets equal `(*Engine).NodeDetail`'s for the same symbol, computed independently in the test, for both the single-definition and the per-candidate multi-definition case.
    - `TestUIServiceNodeDetailCapsCandidatesAndReportsTheTotal` asserts the gathered-candidate count equals `uiMultiDefCap`, the total equals the real match count, and the out-of-cap entries carry their node record with the detail flag unset.
    - `uiMultiDefCap` is a named constant referenced from the handler and from its test, and from nowhere else — no bare numeric literal duplicating it appears at a call site.
    - A not-found symbol returns `connect.CodeNotFound` and an argument-less request returns `connect.CodeInvalidArgument`, both asserted via `connect.CodeOf(err)`.
    - `ui.proto`'s file-level comment names plan 01-10 as the owner of the forthcoming `SourceBlob`-typed source field on `GetNodeDetailResponse`, `NodeDefinition` and `ExploreGroup`, and states the next free number in each message as of this plan. No `reserved` clause is emitted for those numbers: `reserved` forbids use, and 01-10 must be able to use them.
    - No test in this plan asserts a field number for a field this plan does not declare. Every entry in Task 3's known-number fixture resolves to a real declared field in the generated descriptor, and the test fails — never skips — on an entry that does not.
    - `task proto:gen` followed by `git status --porcelain internal/uiproto/` produces empty output — run behind the `test -d internal/uiproto &&` existence guard, without which the check passes vacuously on a missing or typo'd path, `task proto:drift` passes with an unchanged compared-count, and `go build ./...` succeeds.
  </acceptance_criteria>
  <done>Node detail answers over the wire in all three modes, per-candidate data survives the mapping, and the UI's own cap is explicit and counted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `Explore` — groups carrying their own sources and blast entries</name>
  <files>internal/uiproto/uiv1/ui.proto, internal/uiproto/uiv1/ui.pb.go, internal/uiproto/uiv1/uiv1connect/ui.connect.go, internal/uiserver/handlers.go, internal/uiserver/handlers_test.go</files>
  <read_first>
    - internal/query/detail.go — `ExploreResult`'s field set as plan 01-05 defined it, including the doc note that `Sources` is keyed by the group's `Path`
    - internal/query/explore.go lines 1-30 — `ExploreFileGroup` and `ExploreBlast`, now exported, and what each field carries
    - internal/query/explore.go's final assembly — how `groups`, `blasts`, `sources` and `skeletonFiles` relate to one another, so the wire mapping preserves those relationships rather than emitting four parallel unkeyed lists
    - internal/uiproto/uiv1/ui.proto — the shared `Node` message, and the file-level intent paragraph Task 1 wrote naming plan 01-10's forthcoming source fields (documentation of intent, deliberately NOT a `reserved` clause and deliberately NOT a fixture entry)
  </read_first>
  <behavior>
    - `TestUIServiceExploreGroupsCarryTheirOwnSources`: for a query matching several files, every returned group's source corresponds to that group's path — verified by comparing against `ExploreResult.Sources` looked up by the same key, not by position.
    - Every returned group whose path is in `ExploreResult.SkeletonFiles` carries its skeletonized flag set, and no other group does.
    - Blast entries map one-to-one onto `ExploreResult.Blasts`, each carrying its symbol, caller count and test-file list.
    - `TestUIServiceExploreZeroMatchIsNotAnError`: a query matching nothing returns a successful response with an empty-result marker and no groups — not `CodeNotFound` and not `CodeInternal`.
    - An empty or whitespace-only query returns `connect.CodeInvalidArgument`, reflecting the Engine's own rejection.
  </behavior>
  <action>
    Add `ExploreRequest` carrying query and max-files, and `ExploreResponse` carrying the
    query, an empty-result marker, a staleness flag, a symbol count, repeated `ExploreGroup`
    and repeated `BlastEntry`.

    `ExploreGroup` carries the group's path, its repeated symbols, and a skeletonized flag.
    Plan 01-10 will attach its `SourceBlob`-typed per-group source here, at the next free
    number in this message — recorded in `ui.proto`'s file-level intent paragraph per Task
    1, and NOT via a `reserved` clause, which would forbid exactly the use 01-10 needs.
    This placement is the point: `ExploreResult.Sources`
    is a `map[string][]byte` keyed by group path, so a single response-level source blob
    would lose which file each blob belongs to, and a parallel unkeyed list would make the
    association positional and therefore fragile. Attaching the source to the group it
    belongs to is the only shape that survives the mapping intact, and 01-10's checkpoint
    locks that placement together with `SourceBlob`'s own fields.

    `BlastEntry` carries the symbol node, the caller count and the repeated test-file
    paths, mirroring `ExploreBlast` exactly.

    Fold the skeleton set into the groups rather than emitting it as a separate map: the
    Engine's `SkeletonFiles` is a set of paths and every path in it is a group path, so a
    per-group boolean carries the same information without a second lookup on the client.
    Say so in the field's doc comment, and note that a group path absent from the set simply
    has the flag unset.

    Regenerate with `task proto:gen`.

    Implement `Explore` on `uiService` through `withEngine`, calling
    `(*Engine).ExploreDetail` and nothing else. A zero-match result is a successful response
    carrying the empty marker, never an error: `ExploreResult` models the empty case
    explicitly for exactly this reason, and turning it into a `CodeNotFound` would make an
    ordinary "no results" indistinguishable from a missing symbol.

    Write `exploreResultToProto`, `exploreGroupToProto` and `blastEntryToProto` as named
    mappers. When mapping a group, look its source up by the group's own path — never by
    index into a parallel slice.

    Extend `handlers_test.go` with the behavior block's cases through a real Connect client.
  </action>
  <verify>
    <automated>task proto:gen && test -d internal/uiproto && test -z "$(git status --porcelain internal/uiproto/)" && go build ./... && { OUT=$(go test -v -count=1 -race -run 'TestUIServiceExplore' ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 5 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -race -run 'TestUIServiceExplore' ./internal/uiserver/` emits at least 5 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** Three test functions match: `TestUIServiceExploreGroupsCarryTheirOwnSources` with three `t.Run` subtests (`source-matches-group-path`, `skeletonized-flag-set-exactly-for-SkeletonFiles`, `blast-entries-map-one-to-one`) = 4, the named home for the behavior block's second and third bullets; `TestUIServiceExploreZeroMatchIsNotAnError` = 1; `TestUIServiceExploreRejectsEmptyQuery` = 1, the named home for the last bullet. Total **6** against a floor of 5. **Behavior-bullet homes, completed for all five bullets:** bullets 1 and 4 name their own test functions; bullets 2 and 3 are `TestUIServiceExploreGroupsCarryTheirOwnSources`'s `skeletonized-flag-set-exactly-for-SkeletonFiles` and `blast-entries-map-one-to-one` subtests; bullet 5 is `TestUIServiceExploreRejectsEmptyQuery`. No bullet is orphaned. **Ownership:** all three functions are declared by THIS task and the `TestUIServiceExplore` atom matches no earlier plan's test.
    - `TestUIServiceExploreGroupsCarryTheirOwnSources` verifies the source association by key lookup into `ExploreResult.Sources`, not by slice position, for a query producing at least three groups.
    - The skeletonized flag is asserted set for exactly the group paths present in `ExploreResult.SkeletonFiles` and unset for all others.
    - Blast entries are asserted to map one-to-one onto `ExploreResult.Blasts`, with the counts compared element by element.
    - `TestUIServiceExploreZeroMatchIsNotAnError` asserts a successful response with the empty marker set and zero groups.
    - An empty query returns `connect.CodeInvalidArgument`, asserted via `connect.CodeOf(err)`.
    - `ExploreGroup`'s next free field number is named in `ui.proto`'s file-level intent paragraph as plan 01-10's per-group source slot, and no `reserved` clause covers it.
    - `task proto:gen` followed by `git status --porcelain internal/uiproto/` produces empty output — run behind the `test -d internal/uiproto &&` existence guard, without which the check passes vacuously on a missing or typo'd path and `go build ./...` succeeds.
  </acceptance_criteria>
  <done>Explore answers over the wire with each group carrying its own source association and skeleton flag, and a zero-match query is an ordinary successful answer.</done>
</task>

<task type="auto">
  <name>Task 3: Assert read-only by construction and pin the field numbers (SRV-03)</name>
  <files>internal/uiserver/readonly_test.go</files>
  <read_first>
    - internal/uiproto/uiv1/uiv1connect/ui.connect.go — the generated `UIServiceHandler` interface and the procedure constants, which are the machine-readable method inventory this guard reads
    - internal/query/engine.go — the `Engine` struct's `graphstore.Reader` field, which is why no write path exists to reach
    - internal/graphstore/store.go — the `Reader` interface versus the `Writer` interface, confirming `Reader` has no mutator
    - internal/upgrade/taskfile_shape_test.go lines 1-60 — the literal-fixture-plus-provenance-comment convention this guard follows
    - internal/schema/meta_commit_test.go — plan 01-06's `TestKnownMetaFieldNumbersAreStable`, the subset-based descriptor guard this task's field-number test mirrors for the UI surface
    - .planning/REQUIREMENTS.md — SRV-03's exact text
    - .planning/STATE.md — the standing rule that a guard must carry a positive assertion that it did its work
  </read_first>
  <action>
    Make read-only an asserted property. A negative-only guard — "no method name contains a
    write verb" — passes vacuously the moment its anchor stops matching, which is the defect
    class this repository already carries in forty-nine places. Write it as a positive set
    equality instead.

    `TestUIServiceMethodSetIsExactlyTheReadSet` reflects over the generated
    `uiv1connect.UIServiceHandler` interface, collects its method names into a set, and
    asserts set equality against a literal fixture of exactly the nine expected names, plus
    a length assertion of 9. Carry a provenance comment on the fixture naming
    `internal/uiproto/uiv1/ui.proto`, the date it was transcribed, and — because the review
    asked for this reconciliation to be visible where a reader will hit it — the fact that
    `Engine.Query` is deliberately absent from the set, with the one-line rationale plan
    01-08 recorded in the proto's file-level comment. This test fails in both directions: an
    added method (mutating or otherwise) fails, and a removed method fails.

    `TestUIServiceDeclaresNoMutatingMethod` is the complementary check, written so it cannot
    go vacuous: it asserts a positive count of method names inspected before applying the
    verb check and fails when that count is zero. The verb set is a literal fixture with its
    own provenance comment.

    `TestUIProtoFieldNumbersAreStableAndUnique` replaces the contiguity check the previous
    version of this plan proposed. Contiguity is the wrong property: additive-only evolution
    with a `reserved` clause legitimately produces gaps, so a contiguity assertion would
    fail on correct code and would still not detect a renumber between revisions. Assert the
    two properties that D-02a actually protects instead:

    1. **Stability.** A literal fixture maps known `message.field` names to their numbers.
       Every known mapping must still hold in the generated descriptors. A renumber fails; a
       newly added field passes, as it should. The fixture carries a provenance comment
       naming `ui.proto` and the date.

       **The fixture's scope rule, corrected.** The rule is not "only fields THIS PLAN
       declares" — that phrasing left `GetStatusResponse` (declared by plans 01-01 and
       01-08, in earlier waves) covered by nothing at all, and plan 01-11 then claimed its
       fields 8 and 9 were "already pinned by plan 01-09's fixture" when they were not. The
       correct rule is: **the fixture covers every field of every message that EXISTS in the
       generated descriptor at THIS plan's wave.** That is a strictly larger set than this
       plan's own declarations and it is exactly the set that is resolvable — which is the
       property that makes the guard non-vacuous.

       Concretely, at wave 5 that set includes, and the fixture MUST contain entries for:
       `GetStatusRequest` and `GetStatusResponse` fields 1-7 (`commit_sha = 7` among them,
       allocated by 01-08), `IndexingInProgress`, and every message and field this plan
       declares. It must NOT contain an entry for plan 01-10's source fields or for
       `GetStatusResponse.store_exists = 8` / `.indexing_in_progress = 9`, because those do
       not exist yet: a descriptor carries declared fields and `reserved` ranges, never
       comments, so an entry for a field that does not yet exist could only fail this plan
       or be skipped — and a skipped entry is a vacuous guard, the exact defect class this
       test exists to avoid.

       Resolution is therefore mandatory, not best-effort: the test looks every fixture
       entry up in the descriptor and FAILS, with the entry named, when one does not
       resolve. It asserts the resolved-entry count equals the fixture length, and asserts
       the fixture length is non-zero, so neither a silently-skipped entry nor an emptied
       fixture can read green.

       **Pin the fixture's length as an exported-in-package named constant**, declared
       immediately above the fixture:
       `uiProtoFieldFixtureLenAtPlan0109` set to the literal entry count this plan writes,
       with `TestUIProtoFieldNumbersAreStableAndUnique` asserting
       `len(fixture) == uiProtoFieldFixtureLenAtPlan0109`. Without this, the later
       extensions have no anchored baseline: plans 01-10 and 01-11 each assert the fixture
       "has grown by exactly N", and if the baseline lives only in a prose SUMMARY carried
       across two waves then the arithmetic is unverifiable in code. The constant makes the
       chain checkable by the compiler and the test, in the tree, at every wave.

       Plans 01-10 AND 01-11 both EXTEND this fixture and re-run this same test — 01-10 with
       its nine source fields, 01-11 with `GetStatusResponse.store_exists = 8` and
       `.indexing_in_progress = 9`. Each declares its own length constant in terms of the
       previous one, never as a bare literal. Every entry this plan wrote must still hold at
       both of those points, which is the real and only enforceable cross-wave protection —
       a later plan may add numbers, it may not disturb one an earlier plan allocated. Say
       all of that in the fixture's provenance comment, naming both extending plans, so the
       next editor knows extension is the sanctioned move and rewriting is not.
    2. **Uniqueness.** No field number appears twice within any one message. This is a
       same-generation collision check and is cheap to make exhaustive. `protoc` already
       rejects a duplicate number outright, so this is a cheap belt-and-braces assertion
       over the generated descriptor rather than the primary defense.

    Both properties are reported: the test logs the number of messages and the number of
    fields it inspected, plus the fixture length and the resolved-entry count, and fails
    when any of them is zero.

    Then watch the method-set guard fail. Add a tenth name to the fixture in an isolated
    scratch tree (or behind an exact reverse patch), observe and record the failure output,
    and restore. Do not revert by discarding the file. A set-equality guard that has only
    been observed passing is the same unproven artifact as any other.

    Note what this plan does NOT re-assert, so a reader does not go looking: the flag-set
    equality guard proving no bind-address or credential flag exists on `codegraph ui`, and
    the reflected `uiService` field-type set proving no store handle is cached, both live in
    plan 01-01 and are not duplicated here.
  </action>
  <verify>
    <automated>{ OUT=$(go test -v -count=1 -run 'TestUIServiceMethodSetIsExactlyTheReadSet|TestUIServiceDeclaresNoMutatingMethod|TestUIProtoFieldNumbersAreStableAndUnique' ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 3 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -run 'TestUIServiceMethodSetIsExactlyTheReadSet|TestUIServiceDeclaresNoMutatingMethod|TestUIProtoFieldNumbersAreStableAndUnique' ./internal/uiserver/` emits at least 3 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** Three test functions match and none declares subtests: `TestUIServiceMethodSetIsExactlyTheReadSet`, `TestUIServiceDeclaresNoMutatingMethod`, `TestUIProtoFieldNumbersAreStableAndUnique`. Total **3** against a floor of 3: zero headroom by design. **Ownership:** every atom in this gate's `-run` pattern names a test function THIS task declares; no test from any other plan matches any atom, so the whole count is this task's own deliverable and the floor goes unmet if that deliverable is absent.
    - `TestUIServiceMethodSetIsExactlyTheReadSet` asserts set equality against a 9-name literal fixture and asserts the observed set length is 9, and its fixture's provenance comment records the deliberate absence of `Engine.Query` with its rationale.
    - The SUMMARY records the observed failure output from the deliberate tenth-name perturbation and the restore method used. No file was reverted by discarding it.
    - `TestUIServiceDeclaresNoMutatingMethod` logs the number of method names it inspected and fails when that number is zero.
    - `TestUIProtoFieldNumbersAreStableAndUnique` asserts every known `message.field` to number mapping still holds, asserts no duplicate number within any message, and logs plus asserts non-zero counts of messages and fields inspected. It makes no contiguity assertion.
    - Every fixture entry resolves to a real declared field in the generated descriptor. The test asserts `resolved == len(fixture)` and `len(fixture) > 0`, and names any unresolved entry in its failure message — it never skips one. Demonstrated by adding a fixture entry for a deliberately non-existent `message.field` in an isolated scratch tree, observing and recording the named failure, and restoring.
    - The fixture contains an entry for EVERY field of EVERY message that exists in the generated descriptor at this wave — explicitly including `GetStatusResponse` fields 1-7 (declared by plans 01-01 and 01-08, not by this one) and `IndexingInProgress`. Asserted structurally, not by inspection: the test walks the descriptor, collects every `message.field` it finds, and fails naming any that the fixture does not cover. A fixture scoped to only this plan's own declarations is what left `GetStatusResponse` pinned by nothing through cycle 3, so the coverage assertion runs in BOTH directions — every fixture entry resolves, and every declared field is covered.
    - The fixture contains NO entry for plan 01-10's source fields and none for `GetStatusResponse.store_exists = 8` or `.indexing_in_progress = 9`, because those fields do not exist at this wave and a descriptor cannot carry a commented-but-undeclared number. The fixture's provenance comment names BOTH extending plans — 01-10 (nine source fields) and 01-11 (the two degrade fields) — and states that every entry written here must still hold afterward.
    - The fixture's length is pinned by the named constant `uiProtoFieldFixtureLenAtPlan0109`, declared immediately above the fixture and asserted by `len(fixture) == uiProtoFieldFixtureLenAtPlan0109`. The constant's doc comment states that plans 01-10 and 01-11 each define their own constant in terms of this one and that no extension may restate a bare literal. Its value is recorded in the SUMMARY as well, but the constant — not the SUMMARY — is the baseline the later assertions reference.
    - Each literal fixture carries a provenance comment naming its source file and the date transcribed.
    - The `UIService` service block declares exactly 9 rpcs, asserted by counting rpc declarations in the proto and comparing against the literal 9.
  </acceptance_criteria>
  <done>The service's method set is pinned by set equality in both directions, the field numbers are pinned by stability and uniqueness rather than by contiguity, every guard reports what it inspected, and the method-set guard has been watched fail.</done>
</task>

</tasks>

## Review feedback dispositions

| Finding | Severity | Disposition |
|---|---|---|
| Cycle-2 HIGH C2-2: automated PASS-count gates discard the test command's exit status | HIGH | **Incorporated across every gate in this plan.** A shell pipeline's `$?` is the LAST command's, so `COUNT=$(go test ... | rg '--- PASS' | wc -l); [ "$COUNT" -ge N ]` passed with N passing subtests plus a `--- FAIL`. Every `<automated>` gate now captures `go test`'s output into `OUT` and its status into `STATUS` BEFORE any pipe, counts from `OUT`, dumps `OUT` on failure, and requires `STATUS -eq 0` AND the `--- PASS` floor. The conjunction is the rule: the count alone is vacuous when a test fails, the status alone is vacuous when `-run` matches nothing. A plan-level prohibition against the bare count-only form is in `must_haves.prohibitions`. The only scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence. |
| `ExploreResponse` per-group `SourceBlob` modeling unspecified, and the field numbers are locked by a one-way checkpoint in the same plan | HIGH | **Incorporated.** `ExploreGroup` is defined here, one wave BEFORE plan 01-10's checkpoint, so the checkpoint locks an attachment point whose containing message already exists in the tree. The same is done for `GetNodeDetailResponse` and `NodeDefinition`. |
| Cycle-2 C2-1: the cross-wave field-number guard cannot be executed as written | HIGH | **Incorporated by withdrawing the comment-based reservation.** A protobuf descriptor carries declared fields and `reserved` ranges, never comments, so pinning an undeclared number could only fail here, pass vacuously, or block 01-10 via `reserved`. Replaced with: (a) an intent paragraph in `ui.proto`'s file-level comment naming 01-10 as owner and the next free number in each of the three messages, explicitly documentation and not a guard; (b) a known-number fixture covering only fields declared here, whose every entry MUST resolve in the descriptor — `resolved == len(fixture)`, `len(fixture) > 0`, unresolved entries named and failing rather than skipped, demonstrated failing against a deliberately non-existent entry; and (c) plan 01-10 extending that same fixture and re-running the same test, so every number allocated here must still hold after 01-10 lands. That is the only cross-wave property that was ever enforceable. |
| `TestUIProtoFieldNumbersAreContiguousAndUnreserved` conflicts with additive-only evolution | MEDIUM | **Incorporated by replacement.** `TestUIProtoFieldNumbersAreStableAndUnique` asserts a known-number fixture plus per-message duplicate detection, with the reasoning for dropping contiguity written into the action. |
| `NodeDetail`'s multi-definition data must not be flattened on the way to the wire | HIGH (upstream of 01-04) | **Incorporated.** `NodeDefinition` carries per-candidate calls and called-by; the handler asks the lazy per-candidate lookup exactly `uiMultiDefCap` times so it performs no reads the Engine does not. |
| "Nine reads" omits `Query` while exposing `Search` | LOW | **Incorporated.** The rationale is recorded in plan 01-08's proto comment and repeated in this plan's method-set fixture provenance comment, where a reader auditing the set will actually encounter it. |
| Set-equality guard should be demonstrated failing | — | **Incorporated.** The tenth-name perturbation is required, with the failure output recorded and a non-destructive restore. |
| Old 01-06 was 105k with three tasks | MEDIUM (cross-plan) | **Split.** This plan carries the two seam-backed reads and the read-only guards at 3 tasks / 70k; the seven mechanical reads are plan 01-08. |
| Cycle-3 H1: C2-1 survives on the `01-08` → `01-11` path — `GetStatusResponse` fields are pinned by nothing, because this plan's fixture is scoped to "only fields this plan declares" | HIGH | **Incorporated by correcting the scope rule at its root.** The rule is now: the fixture covers every field of every message that EXISTS in the generated descriptor at THIS plan's wave — a strictly larger set than this plan's own declarations, and exactly the set that is resolvable, which is the property that makes the guard non-vacuous. At wave 5 that explicitly includes `GetStatusRequest`, `GetStatusResponse` fields 1-7 (`commit_sha = 7` allocated by 01-08) and `IndexingInProgress`. The coverage assertion now runs in BOTH directions: every fixture entry resolves, AND every declared field is covered, with the test walking the descriptor and naming any field the fixture misses. Fields 8 and 9 remain correctly excluded here — they do not exist at this wave — and plan 01-11 now extends the fixture with them in the wave they first exist. |
| Cycle-3 M10: the `+9` extension has no anchored baseline — this plan pins only `len(fixture) > 0` | MEDIUM | **Incorporated.** This plan declares `uiProtoFieldFixtureLenAtPlan0109` immediately above the fixture and asserts `len(fixture) == uiProtoFieldFixtureLenAtPlan0109`. Plan 01-10 declares `uiProtoFieldFixtureLenAtPlan0110 = uiProtoFieldFixtureLenAtPlan0109 + 9` and 01-11 declares `uiProtoFieldFixtureLenAtPlan0111 = uiProtoFieldFixtureLenAtPlan0110 + 2`, each in terms of the previous and never as a bare literal. The baseline is now carried by the compiler across three waves instead of by a number transcribed into a prose SUMMARY. |
| Cycle-3 M4: two `<automated>` gates carry the unguarded `git status --porcelain` form | MEDIUM | **Incorporated.** Both prefixed `test -d internal/uiproto &&`. Two of the nine phase-wide sites. |
| Cycle-3 L1: the `--- PASS` counting convention is never stated anywhere in the phase, and echo labels contradict each other | LOW | **Incorporated phase-wide.** A byte-identical `## Gate conventions (phase-wide canon)` section now sits immediately above `<tasks>` in ALL ELEVEN plans. It states the convention once and canonically — one `--- PASS` line per test function PLUS one per `t.Run` subtest — mandates the single echo label `PASS lines:`, restates the D-04 conjunction with its one scoped exception, and enumerates the five vacuity shapes cycle 3 found. Every floor in this plan now carries an explicit **Floor derivation** bullet showing the arithmetic, so a reviewer checks the derivation rather than re-deriving it. Label census: six distinct labels existed across the phase (`PASS subtests:`, `PASS tests:`, `PASS:`, `post-restore PASS lines:`, `PASS lines across both tests:`, `uiserver PASS lines:`); all six were normalised to `PASS lines:`. |
| Cycle-5: terminate the SCOPE class — a gate can satisfy all five shape prohibitions and still be unable to fail | HIGH (phase-wide) | **Incorporated phase-wide as a sixth, SEMANTIC prohibition**, identical in all eleven plans and restated in the `## Gate conventions` canon. The five existing prohibitions are all shape-based: they catch a malformed command. A gate can be well-formed, parse, honor exit status and carry a same-file positive control and STILL be unable to fail, because its floor is already cleared by test functions belonging to an EARLIER plan that share its selection pattern — the pattern matching none of this task's own tests then costs nothing. The sixth prohibition bans exactly that, with two checkable requirements: (a) OWNERSHIP — every gate carries at least one leg scoped to test functions the task itself declares, with a floor derived from that leg alone, and any pattern that also matches an earlier plan's tests must say so and say why those tests are the intended subject; (b) HOMING — every behavior bullet is named by, or names, a test function or subtest the task's own floor derivation counts, or the derivation states in writing that it is covered by a named criterion or deliberately out of scope. Unlike the five shape prohibitions it is not satisfiable by rewriting a command; it requires a structural leg and a written mapping. Every floor derivation in this plan now carries its ownership statement, and every task with a behavior block carries its bullet-home mapping. |
| Cycle-3: terminate the vacuity class rather than fixing instances | — | **Incorporated phase-wide.** Five new descriptor-less entries in `must_haves.prohibitions`, identical in all eleven plans, forbid: a `git status --porcelain` check without a `test -d` existence guard; a positive control that targets a different file than the assertion; a count-matching regex that accepts zero; an unanchored identifier grep; and an unranged `git diff` emptiness assertion. Each is a shape cycle 3 found in this phase's own gates, so a future author cannot reintroduce one without violating a stated prohibition. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `127.0.0.1:<port>` RPC surface | All nine methods are now reachable from any page the developer visits, behind plan 01-01's Host/Origin guard. The method set is the attack surface, so its size is now an asserted invariant. |
| `GetNodeDetail` / `Explore` argument → source read | Both reads reach verbatim file content. Every read goes through `readSourceFile`/`resolveSourcePath`'s two-stage repo-root confinement; the handler adds no read path of its own. |
| lazy per-candidate lookup → I/O amplification | The multi-definition value can read one file per invocation. The handler bounds invocations by a named constant rather than by the length of the candidate list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `codegraph.ui.v1.UIService` method set | high | mitigate | Set equality against a 9-name literal fixture in both directions, plus a non-vacuous verb check that reports its inspected count, plus the type-level fact that `Engine` wraps a `graphstore.Reader` with no mutator. Demonstrated failing against a deliberately added tenth method. |
| T-01-07 | Information Disclosure | `GetNodeDetail`, `Explore` source reads | high | mitigate | Both route through `(*Engine).NodeDetail` and `(*Engine).ExploreDetail`, which read source only via `readSourceFile`/`resolveSourcePath`'s two-stage repo-root confinement. The handler adds no read path of its own. |
| T-01-35 | Denial of Service | unbounded per-candidate source reads on an overloaded symbol | medium | mitigate | `uiMultiDefCap` bounds the number of per-candidate lookups the handler performs, asserted by a test that counts the gathered candidates against the constant and the total against the real match count. Out-of-cap candidates are listed, never read. |
| T-01-36 | Tampering | field-number collision or renumber between two plans in the same phase | medium | mitigate | Every number THIS plan allocates is pinned by a descriptor-backed known-number fixture whose entries must all resolve, so plan 01-10 may add fields but cannot renumber one. A same-message duplicate is rejected by `protoc` itself and re-asserted over the descriptor. Pre-allocating 01-10's own numbers is NOT attempted: a protobuf descriptor carries no comments, and a `reserved` clause would forbid the use 01-10 needs — the intent is recorded in `ui.proto`'s file-level comment as documentation, explicitly not as a guard. |
| T-01-23 | Information Disclosure | error text reaching the browser | medium | mitigate | Errors are classified by `errors.Is`/`errors.As` through `mapEngineError` into `CodeNotFound`, `CodeInvalidArgument` or `CodeInternal`. String matching is excluded, so a message change cannot silently reclassify. |
| T-01-24 | Denial of Service | store handle retained across calls | high | mitigate | Both new handlers go through the same `withEngine` seam over the single `openEngine` site, with the structural no-cached-handle assertion living in plan 01-01. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race -count=1 ./internal/uiserver/...` passes: the command exits 0 AND its `--- PASS` count meets its floor — both, never either alone.
- `task proto:drift` passes with an unchanged compared-count.
- `task test:unit` and `task test:golden` both pass; the goldens still report `attempted=26 completed=26 matched=26`, confirming the RPC expansion did not reach back into `internal/query`'s rendered output.
- `go vet ./internal/uiserver/... ./internal/uiproto/...` is clean.
</verification>

<success_criteria>
- Node detail and explore answer over the wire through the same builders CLI and MCP use, with per-candidate multi-definition data and per-group explore sources both intact.
- The UI's own multi-definition cap is a named constant and the true total is reported.
- No RPC can mutate the index, asserted by set equality in both directions and demonstrated failing.
- The UI proto's field numbers are pinned by stability and uniqueness over the fields this plan declares, with every fixture entry required to resolve against the generated descriptor, and plan 01-10 required to extend that fixture rather than rewrite it.
</success_criteria>

<output>
Create `.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-09-SUMMARY.md` when done
</output>
