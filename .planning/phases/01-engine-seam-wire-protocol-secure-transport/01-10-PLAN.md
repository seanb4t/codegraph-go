---
phase: 01-engine-seam-wire-protocol-secure-transport
plan: 10
type: tdd
wave: 6
depends_on: ["01-09"]
files_modified:
  - internal/uiproto/uiv1/ui.proto
  - internal/uiproto/uiv1/ui.pb.go
  - internal/uiproto/uiv1/uiv1connect/ui.connect.go
  - internal/textutil/truncate.go
  - internal/textutil/truncate_test.go
  - internal/mcp/session_line.go
  - internal/uiserver/truncate.go
  - internal/uiserver/truncate_test.go
  - internal/uiserver/handlers.go
  - internal/uiserver/readonly_test.go
  - internal/uiserver/sourceblob_test.go
  - internal/uiserver/server.go
autonomous: false
requirements: [RPC-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An oversized verbatim source response comes back bounded and explicitly marked truncated, carrying the true totals, rather than unbounded or as an error (RPC-05, D-11/D-12)."
    - "The line cap is the primary limit and a byte cap sits underneath it, because a pathological minified single-line file blows any byte budget regardless of line count (D-11)."
    - "Byte truncation lands on a UTF-8 rune boundary, so an input that was valid UTF-8 stays valid UTF-8 after truncation. The response field is `bytes` and carries no universal UTF-8 guarantee, because indexed source is not guaranteed valid UTF-8 and a proto3 `string` field would fail to marshal such a file rather than serving it (D-11)."
    - "Line counting is defined explicitly and identically at every site: the count is the number of newline characters plus one when the input is non-empty and does not end in a newline. Empty input is zero lines; a lone newline is one line; an unterminated final line counts (RPC-05)."
    - "Each of the three limits is one named constant referenced from both the truncation call site and its test — never a repeated literal (D-11, `session_line.go`'s stated discipline)."
    - "The rune-boundary cut lives in ONE shared internal helper consumed by both `internal/mcp` and `internal/uiserver`, so there is not a second security-relevant copy to drift (D-11)."
    - "The transport backstop sits strictly ABOVE the application cap, so correct truncation is never converted into a `resource_exhausted` error the user sees for no reason (D-13)."
    - "Plan 01-09's known-number fixture is EXTENDED, never rewritten: after this plan lands it still holds every entry 01-09 wrote and has grown by exactly nine — `SourceBlob`'s six fields plus the three attachment fields — proving this plan allocated numbers and renumbered none (D-02a, RPC-01)."
    - "An empty source file returns content of length zero with the truncated flag unset and totals of zero — not an error, and not a truncation (RPC-05 / empty edge)."
    - "Length is defined explicitly in two units and both are reported: lines for the primary cap and bytes for the secondary, with the byte cut on a rune boundary so the byte total is a byte count and never a code-point count (RPC-05 / encoding edge)."
  artifacts:
    - internal/textutil/truncate.go
    - internal/uiserver/truncate.go
    - internal/uiserver/truncate_test.go
    - internal/uiserver/sourceblob_test.go
  key_links:
    - "`connect.WithSendMaxBytes`/`WithReadMaxBytes` return `CodeResourceExhausted` — they reject, they do not truncate. They are a backstop above application truncation, never a substitute for it."
    - "`GetNodeDetailResponse`, `NodeDefinition` and `ExploreGroup` are DECLARED by plan 01-09; this plan adds their `SourceBlob`-typed source field at the next free number in each. Those numbers were NOT pre-allocated by 01-09 — a protobuf descriptor carries no comments and a `reserved` clause would forbid this very use — so 01-09 recorded them as intent in `ui.proto`'s file-level comment. This plan must EXTEND 01-09's known-number fixture with the numbers it actually allocates, and every entry 01-09 wrote must still hold afterward."
    - "`internal/query.(*Engine).SourceFor` is the sanctioned way this layer obtains single-definition source, and it wraps the existing repo-root confinement rather than adding a read path."
  prohibitions:
    - "Never gate a task on a bare `go test ... | rg '--- PASS' | wc -l` count whose exit status is discarded. A shell pipeline's status is the LAST command's, so N passing subtests plus one `--- FAIL` satisfies a count-only gate. Every automated gate captures `go test`'s output and status BEFORE any pipe and requires BOTH: status 0 AND the `--- PASS` floor. The count alone is vacuous when a test fails; the status alone is vacuous when the `-run` pattern matches nothing. The sole scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence."
    - "Never renumber or reuse a protobuf field number once it has shipped; retiring a field means adding its number to a `reserved` clause."
    - "Never signal truncation by returning an error; RPC-05 asks for a bounded response, and `CodeResourceExhausted` prevents an unbounded one without delivering a bounded one (D-12)."
    - "Never write `test -z \"$(git status --porcelain <path>)\"` without a preceding `test -d <path> &&` existence guard. `git status --porcelain` on a missing or typo'd pathspec exits 0 with EMPTY stdout — the 'could not open directory' warning goes to stderr — so the check passes vacuously against a path that was never opened."
    - "Never positive-control a zero-count gate against a DIFFERENT file than the one the gate guards. `rg` prints nothing for a nonexistent path just as it does for a clean file, so a typo'd or renamed target reads `0` while the guarded file is never opened. The control must run the identical pipeline against the SAME file with a pattern that file genuinely contains."
    - "Never score a count with a regex that accepts zero. `rg -q -e 'compared [0-9]+ generated files'` matches `compared 0 generated files`, so it is a presence check on the count and not a floor. Capture the number and assert the floor numerically."
    - "Never write an unanchored identifier grep as a forbidden-pattern gate. Anchor it to the declaration form — `func gather[A-Z]`, never `\\bgather[A-Z]` — because a trailing comment or a `/* */` block survives `rg -v '^\\s*//'` and turns the gate red, or green, for the wrong reason."
    - "Never assert emptiness with an unranged `git diff`. Under atomic-commit-per-task the working tree is clean at verification time, so `git diff --stat <path>` is empty by construction whether or not the file was edited. Assert over an explicit `<phase-base>..HEAD` revision range."
    - "Never write a gate whose floor can be met without the deliverable it names existing. This is the SCOPE half of gate hygiene and no shape check reaches it: a scope-vacuous gate is well-formed, parses, honors exit status and carries a same-file positive control, and still scores nothing the task owns. Two requirements, both mandatory and both checkable by reading the plan. (a) OWNERSHIP — every gate carries at least one leg whose test-selection pattern matches ONLY test functions this task itself declares, with a floor derived from that leg's own functions and subtests alone; where a pattern also matches an earlier plan's tests, the derivation must say so and say why those inherited tests are the intended subject of that leg. (b) HOMING — every behavior bullet is named by, or names, a test function or subtest that the task's own floor derivation counts, or the derivation states in writing that the bullet is covered by a specific named criterion or is deliberately out of scope and why. A bullet no derivation reaches is a described behavior with no executable owner, and a leg whose floor another plan's tests already clear cannot fail when this task writes nothing."
---

<objective>
Bound every source blob leaving the process, in two units, on a rune boundary, explicitly
marked — and put a transport backstop above it that never fires on correct output.

Purpose: `connect-go` defaults to unlimited on both send and receive, so RPC-05 is
satisfied by no default and must be built. But the transport limit rejects rather than
truncates — it returns `CodeResourceExhausted` — which prevents an unbounded response
without delivering a bounded one. So RPC-05 needs two independent layers: application
truncation that still hands the client usable content it can render as "showing first N of
M", and a transport backstop strictly above it that catches the case where application
truncation was missed.

The details that look cosmetic are the ones that become permanent. `SourceBlob`'s field
numbers can never be reused; where the blob attaches to `ExploreResponse` decides whether
the group-to-source association survives; whether the content field is `bytes` or `string`
decides what happens to a source file that is not valid UTF-8; and what "line count" means
for an unterminated final line is a number every Phase 3 through 6 view will render. All
four are settled in the checkpoint below, before anything is written, because after it they
are contract.

Output: a shared rune-boundary helper, two-tier truncation with explicit totals in both
units, and a transport backstop asserted to sit above the application cap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-09-SUMMARY.md
</context>

## Artifacts this phase produces

**Created by this plan (01-10):**

- `internal/textutil/truncate.go` — `TruncateOnRuneBoundary`, lifted from `internal/mcp/session_line.go` and consumed by both packages
- `internal/textutil/truncate_test.go` — `TestTruncateOnRuneBoundary`
- `internal/uiproto/uiv1/ui.proto` gains message `SourceBlob` with `content`, `truncated`, `total_lines`, `total_bytes`, `returned_lines`, `returned_bytes`
- `GetNodeDetailResponse`, `NodeDefinition` and `ExploreGroup` gain their `SourceBlob`-typed source fields at the next free number in each, matching the numbers 01-09 recorded as intent in `ui.proto`'s file-level comment
- `internal/uiserver/readonly_test.go` — plan 01-09's known-number fixture, EXTENDED with `SourceBlob`'s six numbers and the three attachment numbers
- Regenerated `ui.pb.go` and `uiv1connect/ui.connect.go`
- `internal/uiserver/truncate.go` — `truncateSource`, `countLines`, and the constants `sourceLineCap`, `sourceByteCap`, `transportSendMaxBytes`, `transportReadMaxBytes`
- `internal/uiserver/truncate_test.go` — `TestTruncateSourceUnderBothCaps`, `TestTruncateSourceExceedsLineCap`, `TestTruncateSourceExceedsByteCap`, `TestTruncateSourceLineCapBoundary`, `TestTruncateSourceNeverSplitsARune`, `TestTruncateSourceEmptyInput`, `TestTruncateSourceTotalsAreExact`, `TestCountLinesSemantics`, `TestTransportBackstopSitsAboveApplicationCap` (these are the names Task 1's floor derivation counts; the list and the derivation must name the same set, because a test named in one and not the other is a test no floor scores)
- `internal/uiserver/sourceblob_test.go` — Task 2's own end-to-end tests, the named home for every bullet in its behavior block: `TestUIServiceSourceBlobTruncatesAnOversizedDefinitionSource`, `TestUIServiceSourceBlobOnEveryAttachmentPoint`, `TestUIServiceSourceBlobGroupSourceMatchesItsGroupPath`, `TestUIServiceSourceBlobAtTheCapClearsTheTransportBackstop`, `TestUIServiceSourceBlobFileModeUsesTheSameTruncationPath`

**Created elsewhere in this phase** (listed so drift verification sees the whole set):
`internal/goldenspec`; `testdata/golden/byte_identity_test.go`; `internal/uiserver`'s
`Options`, `DefaultAddr`, `Server`, `Listen`, `Serve`, `URL`, `Close`, `originHostGuard`,
`allowedHosts`, `allowedOrigins`, `uiService`, `openEngine`, `withEngine`,
`mapEngineError`, the nine handlers and their mappers, `uiMultiDefCap`,
`errIndexingInProgress`, `indexingInProgressMessage`, `degradedStatus`, `classifyDegrade`;
`internal/uiproto/uiv1`'s nine rpcs and their messages plus `IndexingInProgress`;
`internal/cli/ui.go`; `internal/query`'s `NodeDetail` family, `ExploreResult` family,
`ErrNotFound`, `ErrInvalidArgument`, `(*Engine).IndexMeta`, `(*Engine).SourceFor` and the
`build*` builders; `schema.Meta.commit_sha` field 8 and `schema.IndexedCommitSHA`;
`internal/indexer/commit.go`'s `resolveHeadCommitSHA` and `gitExecLookPath`;
`internal/mcp`'s `looksLikeJSONRPCResponse`, `sniffedOutbound`, `decrementPending`,
`pendingUnderflows`; `buf.yaml`, `buf.gen.yaml`, `go.tool-proto.mod`, `Taskfile.yml` tasks
`proto:gen` and `proto:drift`, `internal/upgrade/proto_task_test.go`.

## Gate conventions (phase-wide canon — byte-identical in all eleven plans, and checkable)

This section is replicated verbatim across `01-01-PLAN.md` … `01-11-PLAN.md`. The claim is
checkable rather than merely asserted — one command, run from the phase directory, prints
**one** distinct digest if the eleven copies agree and more than one if any has drifted:

```
for f in 01-*-PLAN.md; do sed -n '/^## Gate conventions/,/^<tasks>$/p' "$f" | shasum; done | sort -u | wc -l
```

A future planner editing one copy must edit all eleven, and this is how a reviewer catches it
if they do not. (Nothing at execution time changes this section, so no `<automated>` gate owns
it — the drift risk is at planning time, which is where the check belongs.)

**`--- PASS` counting.** `go test -v` prints one `--- PASS:` line for the parent test
function AND one for every `t.Run` subtest, including nested ones. Every PASS floor in this
phase counts BOTH: a test function with three subtests contributes FOUR lines, so
`rg -o -e '--- PASS' | wc -l` over `go test -v` output equals
`len(test functions) + len(all subtests)`. Derive every floor that way and show the
arithmetic in the acceptance criterion that states it, so a reviewer can check the floor
instead of re-deriving it.

**Echo label.** Every gate prints `PASS lines:` — never `PASS tests:`, never
`PASS subtests:`. The label names what is counted, and what is counted is lines.

**The conjunction (D-04 as corrected in `01-CONTEXT.md`).** A gate passes only when the
test command's exit status is 0 AND the `--- PASS` floor is met. Neither is sufficient
alone: `go test -run PATTERN` exits 0 when the pattern matches nothing, and a shell
pipeline's `$?` is the LAST command's, so a bare count passes with N passing subtests plus
one `--- FAIL`. The single scoped exception is a deliberate-mutation RED observation, where
a non-zero exit IS the expected evidence and the observation is scored by `--- FAIL` /
`--- PASS` counts rather than by status.

**The five vacuity shapes cycle 3 found in this phase's own gates.** Each is a prohibition
in `must_haves.prohibitions`. They are restated here together because they are one class —
a guard that reads green against a file it never opened:

1. `git status --porcelain <path>` exits 0 with empty stdout on a MISSING or typo'd path
   (the warning goes to stderr). Always prefix `test -d <path> &&`.
2. A zero-count gate positive-controlled against a DIFFERENT file proves nothing about the
   guarded file: `rg` prints nothing for a nonexistent path too. Control on the SAME file.
3. `rg -q 'compared [0-9]+ generated files'` accepts `compared 0 generated files`. Capture
   the number; assert the floor numerically.
4. An unanchored identifier grep (`\bgather[A-Z]`) is satisfiable by a trailing or `/* */`
   comment that `rg -v '^\s*//'` does not strip. Anchor to the declaration form.
5. An unranged `git diff` is empty by construction at verification time under
   atomic-commit-per-task. Assert over `<phase-base>..HEAD`.

**The scope shape cycle 5 found, which none of the five above reaches.** The five shapes are
all about how a gate is WRITTEN. A gate can satisfy every one of them and still be vacuous
because of what it is SCOPED to: if its floor is already cleared by test functions belonging
to an EARLIER plan that happen to share the selection pattern, then the pattern matching none
of this task's own tests costs nothing — `go test` exits 0 on a pattern that matches nothing,
and the inherited tests supply the count. The sixth prohibition in `must_haves.prohibitions`
closes it with two requirements: every gate carries at least one leg scoped to tests THIS task
declares with a floor derived from that leg alone, and every behavior bullet has a named home
in the derivation. Both are checkable by reading the plan, which is where the defect lives.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: `SourceBlob`'s shape, its attachment points, and its counting semantics are one-way</name>
  <reversibility rating="one-way">Under D-02a's additive-only discipline, `SourceBlob`'s field numbers and its attachment field numbers on three messages can never be renumbered or reused once shipped. The semantics are additionally costly to reverse: every Phase 3 through 6 source view reads these fields, so changing what they mean later means touching every view.</reversibility>
  <decision>Lock `SourceBlob`'s six-field shape and numbers, its three attachment points, the `bytes` content type with its scoped UTF-8 guarantee, and the exact line-counting rule (D-11, D-12, RPC-05).</decision>
  <context>
    **Why this checkpoint is substantive, unlike plan 01-06's.** 01-CONTEXT.md's D-11 and
    D-12 lock the PRINCIPLE — a line cap primary, a byte cap underneath, truncation
    signalled by an explicit field and never by an error. They do not lock the field set,
    the numbers, the attachment points, the content type, or the counting rule. All five of
    those are new decisions being made here, and four of them are permanent. This is not a
    reconfirmation.

    What is being locked:

    1. **`SourceBlob` carries six fields**: the content, a truncated flag, the true total in
       lines, the true total in bytes, the returned count in lines, and the returned count
       in bytes — numbered 1 through 6 in that order. Both totals AND both returned counts,
       in both units, so the client can render "showing first N of M lines" without
       recomputing anything and without having to guess which unit the cap fired on.
       Numbers 1 through 6 are spent permanently in this message.

    2. **Three attachment points, on messages plan 01-09 already declared**:
       `GetNodeDetailResponse` (the single-definition shape's source),
       `NodeDefinition` (each gathered candidate's own source), and `ExploreGroup` (each
       group's own source). The `ExploreGroup` placement is the load-bearing one:
       `ExploreResult.Sources` is a `map[string][]byte` keyed by group path, so a single
       response-level blob would lose which file each blob belongs to and a parallel
       unkeyed list would make the association positional.

       All three CONTAINING messages already exist in the tree from plan 01-09, so what is
       being approved is an attachment onto a shape a human can read right now, not onto a
       message that does not exist yet. The field NUMBERS themselves are allocated by this
       plan, at the next free number in each message — 01-09 could not bind them, because a
       protobuf descriptor carries declared fields and `reserved` ranges and nothing else,
       so a commented number is invisible to a descriptor guard and a `reserved` clause
       would have forbidden this use outright. 01-09 recorded the intended numbers in
       `ui.proto`'s file-level comment; read them there before approving, and note that
       `SourceBlob`'s own 1 through 6 plus those three attachment numbers are what this
       decision spends permanently.

    3. **Content is `bytes`, and the UTF-8 guarantee is SCOPED, not universal.** A proto3
       `string` field requires valid UTF-8 and fails to marshal otherwise, so a single
       oddly-encoded file anywhere in an indexed repository would turn an ordinary source
       view into a server error. Indexed source is not guaranteed valid UTF-8. So the field
       is `bytes`, and the guarantee the contract makes is the one it can keep: *if the
       input was valid UTF-8, the output is valid UTF-8, because the byte cut never splits
       a rune.* A universal "this is always UTF-8" guarantee alongside a `bytes` field
       would be a contract the server cannot honor.

    4. **Line counting is defined exactly**: the count is the number of newline characters
       in the input, plus one when the input is non-empty and does not end in a newline. So
       empty input is 0 lines; a lone newline is 1; `a\nb\n` is 2; `a\nb` is 2. This rule
       is applied identically to the total and to the returned count, and it lives in one
       named helper referenced from both. Leaving this undefined would mean every Phase 3
       view renders a subtly different "of M".

    5. **Truncation is never an error.** `CodeResourceExhausted` was considered and
       rejected: it prevents an unbounded response but does not deliver a bounded one, and
       RPC-05 literally asks for a response that "comes back bounded and explicitly marked
       truncated". The client still gets usable content.

    Alternatives considered and rejected: byte-range paging (rejected for RPC-05 in favour
    of line-capped truncation; it would change what Phase 3's viewer must build); a single
    combined truncation-reason enum instead of two unit pairs (loses the totals the client
    needs to render the count); a `string` content field (above); counting an unterminated
    final line as zero (would make `a\nb` report 1 line while returning 2, which no client
    could reconcile).
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the six-field `SourceBlob`, three attachment points, `bytes` content and the stated line rule</name>
      <pros>Client renders "showing first N of M" with no recomputation and no guessing which cap fired; the group-to-source association survives; an oddly-encoded file is served rather than erroring; the line rule is unambiguous at every site</pros>
      <cons>Six field numbers plus three attachment numbers are spent permanently; a `bytes` field means a client must decode rather than receiving a string</cons>
    </option>
    <option id="respec">
      <name>Change the field set, the attachment points, the content type or the counting rule before it ships</name>
      <pros>Free to change now; permanent after any client reads them</pros>
      <cons>Phases 3 through 6 are planned against this shape, so a late change ripples into every source view; changing the attachment points also invalidates the intent 01-09 recorded in `ui.proto` and the extension this plan makes to 01-09's known-number fixture</cons>
    </option>
    <option id="totals-only">
      <name>Reduce to totals-only (four fields)</name>
      <pros>Smaller surface; two fewer permanently-spent numbers</pros>
      <cons>The client must infer which cap fired by comparing content length against a cap it does not know, which is exactly the recomputation the six-field shape exists to avoid</cons>
    </option>
  </options>
  <resume-signal>Reply "proceed" to lock the six-field `SourceBlob`, its three attachment points, the `bytes` content type with the scoped UTF-8 guarantee, and the stated line-counting rule — or name the alternative and the exact shape to use instead.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: One shared rune-boundary helper, then two-tier truncation with exact totals</name>
  <files>internal/textutil/truncate.go, internal/textutil/truncate_test.go, internal/mcp/session_line.go, internal/uiserver/truncate.go, internal/uiserver/truncate_test.go</files>
  <read_first>
    - internal/mcp/session_line.go lines 1-90 — `clientFieldMaxBytes`'s doc comment stating why the limit is "one number referenced from both the truncation call and this doc comment, rather than a magic 256 repeated at each call site", and `truncateOnRuneBoundary`'s exact implementation, which is what moves
    - internal/mcp/session_line_test.go lines 60-90 and 230-260 — `TestSanitizeClientFieldTruncatesOnRuneBoundary`'s shape: a repeated multi-byte rune, a length assertion, and a UTF-8 validity assertion with a failure message explaining why it matters
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md — Open Question 3 on choosing the numeric cap values, and the corpora observations it points at
  </read_first>
  <behavior>
    - `TestCountLinesSemantics`: empty input is 0; a lone newline is 1; two newline-terminated lines are 2; two lines with an unterminated final line are 2; content with no newline at all is 1.
    - A source of 100 lines under both caps returns unchanged content, the truncated flag unset, a total of 100 lines and 100 returned lines.
    - A source exceeding `sourceLineCap` returns exactly `sourceLineCap` lines, the truncated flag set, a total equal to the real line count, and a returned count equal to `sourceLineCap`.
    - A single-line source exceeding `sourceByteCap` returns at most `sourceByteCap` bytes, the truncated flag set, a total equal to the real byte length, and a returned byte count at most `sourceByteCap`.
    - A source of many multi-byte runes truncated at the byte cap yields valid UTF-8 — the cut never splits a rune.
    - An empty source returns content of length zero, the truncated flag unset, and all four counts zero. Not an error.
    - A source of exactly `sourceLineCap` lines returns the truncated flag unset — the boundary is inclusive, asserted rather than assumed — and one line more sets it.
    - `transportSendMaxBytes` is strictly greater than `sourceByteCap`, asserted directly so the ordering cannot silently invert.
  </behavior>
  <action>
    First, remove the duplication before it exists. `truncateOnRuneBoundary` is unexported in
    package `mcp` and therefore not importable, and copying it would create a second
    security-relevant copy of a cut that must never split a rune. Create
    `internal/textutil/truncate.go` with an exported `TruncateOnRuneBoundary`, moving the
    existing implementation and its doc comment across unchanged, and change
    `internal/mcp/session_line.go` to call it. One implementation, two consumers, no
    "these two copies must stay in step" comment to be ignored later. Move
    `internal/mcp`'s existing rune-boundary assertion into `internal/textutil/truncate_test.go`
    or duplicate the assertion there — the package that owns the function owns its proof —
    and leave `internal/mcp`'s own tests passing untouched.

    Create `internal/uiserver/truncate.go` following `internal/mcp/session_line.go`'s stated
    discipline exactly. Declare `sourceLineCap` and `sourceByteCap` as named constants with
    doc comments explaining what each bounds and why both are needed: the line cap is the
    primary limit because it matches how a file is read, and the byte cap sits underneath
    because a pathological minified single-line file blows any byte budget at one line, so
    the byte cap is not optional. Reference each constant from its declaration, its
    truncation call site and its test, and from nowhere else — a repeated literal is the
    thing this pattern exists to prevent. Choose values conservatively large enough that the
    vast majority of real source files are not visibly truncated; check the file-size
    distribution in the repository's own corpora observations before picking, and record the
    reasoning and the numbers chosen in the SUMMARY.

    Write `countLines(b []byte) int` implementing the locked rule in one place: the number
    of newline characters, plus one when the input is non-empty and does not end in a
    newline. Both the total and the returned count go through it. Put the four worked
    examples from the checkpoint into its doc comment, because "how many lines is `a\nb`"
    is the kind of question that gets answered differently at two call sites.

    Implement `truncateSource` returning the truncated content, a truncated flag, and the
    four counts. Count the true totals FIRST, over the full input, before any cutting — the
    totals are the client's "of M", so computing them from the truncated output would report
    the wrong number. Apply the line cap, then apply the byte cap to the result, cutting
    through `textutil.TruncateOnRuneBoundary`. Document that the UTF-8 guarantee is scoped:
    an input that was valid UTF-8 stays valid, and an input that was not is neither repaired
    nor rejected.

    Declare `transportSendMaxBytes` and `transportReadMaxBytes` as named constants beside
    the application caps, with `transportSendMaxBytes` strictly greater than `sourceByteCap`
    plus enough headroom for the surrounding message. The ordering is load-bearing: a
    backstop at or below the application cap silently converts correct truncation into a
    `resource_exhausted` error the user sees for no reason. Add
    `TestTransportBackstopSitsAboveApplicationCap` asserting the inequality directly, so the
    ordering cannot be inverted by a later edit without a test failing.

    Write `internal/uiserver/truncate_test.go` covering the behavior block.
  </action>
  <verify>
    <automated>go build ./... && { OUT=$(go test -v -count=1 -run 'TestTruncate|TestCountLinesSemantics|TestTransportBackstop' ./internal/uiserver/ ./internal/textutil/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 12 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -run 'TestTruncate|TestCountLinesSemantics|TestTransportBackstop' ./internal/uiserver/ ./internal/textutil/` emits at least 12 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** Every test this pattern matches is declared by THIS task. In `./internal/uiserver/`: `TestCountLinesSemantics` declares five `t.Run` subtests (`empty`, `lone-newline`, `two-terminated-lines`, `two-lines-unterminated-final`, `no-newline-at-all`) = 6; `TestTruncateSourceLineCapBoundary` declares two (`exactly-at-cap-not-truncated`, `one-line-over-truncated`) = 3; and `TestTruncateSourceUnderBothCaps`, `TestTruncateSourceExceedsLineCap`, `TestTruncateSourceExceedsByteCap`, `TestTruncateSourceNeverSplitsARune`, `TestTruncateSourceEmptyInput`, `TestTruncateSourceTotalsAreExact`, `TestTransportBackstopSitsAboveApplicationCap` contribute 1 each = 7. In `./internal/textutil/`: `TestTruncateOnRuneBoundary` also matches the `TestTruncate` atom and contributes 1 — it was omitted from the earlier form of this derivation, which stated 16. Total **17** against a floor of 12. No test declared by another plan matches either atom in either package, so this gate is scoped entirely to this task's own deliverable.
    - **Behavior-bullet homes.** Every bullet in this task's behavior block is owned by a test function or named subtest the derivation above counts, one to one and in order: bullet 1 names `TestCountLinesSemantics` and its five subtests are its five cases; bullet 2 is `TestTruncateSourceUnderBothCaps`; bullet 3 is `TestTruncateSourceExceedsLineCap`; bullet 4 is `TestTruncateSourceExceedsByteCap`; bullet 5 is `TestTruncateSourceNeverSplitsARune`; bullet 6 is `TestTruncateSourceEmptyInput`; bullet 7 is `TestTruncateSourceLineCapBoundary`'s two subtests; bullet 8 is `TestTransportBackstopSitsAboveApplicationCap`. `TestTruncateSourceTotalsAreExact` is additionally required by its own acceptance criterion below. No bullet is orphaned.
    - `TestCountLinesSemantics` covers all five cases in the behavior block explicitly, including the unterminated final line and the empty input.
    - `TestTruncateSourceNeverSplitsARune` builds its input from a repeated multi-byte rune, asserts the output length is within `sourceByteCap`, and asserts the output is valid UTF-8 — mirroring `TestSanitizeClientFieldTruncatesOnRuneBoundary`'s exact shape.
    - `TestTruncateSourceTotalsAreExact` asserts the line and byte totals equal the untruncated input's real counts, computed independently in the test, for both a truncated and an untruncated input.
    - `TestTruncateSourceEmptyInput` asserts zero-length content, the truncated flag unset, all four counts zero, and no error returned.
    - The line-cap boundary is asserted at exactly `sourceLineCap` (not truncated) and at one line more (truncated).
    - `TestTransportBackstopSitsAboveApplicationCap` asserts the inequality directly.
    - `rg -v '^\s*//' internal/uiserver/*.go | rg -o -e 'sourceLineCap|sourceByteCap' | wc -l` prints a count of at least 4 — each named constant referenced from both its call site and its test.
    - No bare numeric literal equal to `sourceLineCap` or `sourceByteCap` appears at any call site. This is a RUNNABLE, SCOPED, CONTROLLED gate, not a SUMMARY note: for each chosen value `V`, run
      `rg -v '^\s*//' --glob '!*.pb.go' --glob '!*.connect.go' -o -e "\b${V}\b" internal/uiserver/ internal/textutil/ internal/mcp/ | wc -l` and require it to print exactly `1` — the constant's own declaration and nothing else — positive-controlled in the SAME invocation by running the identical scoped pipeline for the identifier `sourceByteCap` and confirming a count of at least `4`.
      Three parts of that command are the assertion rather than incidental. **The path scope** (`internal/uiserver/ internal/textutil/ internal/mcp/`) is required: unscoped, the search runs over the whole repository and matches arbitrary digit runs inside the committed generated `internal/schema/graph.pb.go`, failing for a reason that has nothing to do with this plan. **The `*.pb.go` / `*.connect.go` exclusions** are required for the same reason within `internal/uiproto`, should the scope ever widen. **The positive control** is required because the searched value is chosen at execution time: without it, a mistyped `-e` pattern prints `0`, which is below `1` and would fail — but a mistyped PATH also prints `0`, and the control is what distinguishes the two. Record the two chosen values and both counts in the SUMMARY.
    - `rg -v '^\s*//' internal/mcp/session_line.go | rg -o -e 'func truncateOnRuneBoundary' | wc -l` prints `0`, positive-controlled by running the identical pipeline with the pattern `textutil\.` in the same invocation and confirming a non-zero count. Comment lines are stripped first.
    - `go test -race -count=1 ./internal/mcp/...` still passes — moving the helper changed no MCP behavior.
  </acceptance_criteria>
  <done>One shared rune-boundary cut, one line-counting rule, two named application caps and two named transport constants — each referenced from its call site and its test, with the ordering between the two tiers asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach `SourceBlob` to the three response points and mount the transport backstop</name>
  <files>internal/uiproto/uiv1/ui.proto, internal/uiproto/uiv1/ui.pb.go, internal/uiproto/uiv1/uiv1connect/ui.connect.go, internal/uiserver/handlers.go, internal/uiserver/readonly_test.go, internal/uiserver/sourceblob_test.go, internal/uiserver/server.go</files>
  <read_first>
    - internal/uiproto/uiv1/ui.proto — `GetNodeDetailResponse`, `NodeDefinition` and `ExploreGroup` as plan 01-09 declared them, and the file-level intent paragraph naming this plan as the owner of a `SourceBlob`-typed source field at the next free number in each
    - internal/uiserver/readonly_test.go — plan 01-09's known-number fixture and its provenance comment stating that extension is the sanctioned move; this task extends it, and its `resolved == len(fixture)` assertion means an entry added here must name a field that actually exists
    - internal/uiserver/handlers.go — the `GetNodeDetail` and `Explore` handlers whose responses gain the source field, and the mappers plan 01-09 wrote
    - internal/uiserver/server.go — the `NewUIServiceHandler` mount point inside `Listen`, where the transport options attach
    - internal/query/detail.go — `(*Engine).SourceFor`, the sanctioned single-definition source read, and `DefinitionDetail.Source`, which the multi-definition path already populates
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-01-SUMMARY.md — the recorded real signatures of `connect.WithSendMaxBytes` and `connect.WithReadMaxBytes`, verified when the dependency landed
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md — the Anti-Patterns entry on why those options are a backstop and not a substitute
    - internal/query/explore.go around line 236 — the existing comment citing the "two layers, per the threat model's T-01-25 disposition" reasoning, the in-repo precedent for exactly this two-layer shape
  </read_first>
  <behavior>
    - `TestUIServiceSourceBlobTruncatesAnOversizedDefinitionSource`: an end-to-end `GetNodeDetail` through a real Connect client on a single-definition symbol whose file exceeds `sourceByteCap` returns a truncated, valid-UTF-8 source blob with the truncated flag set and the true totals — not a `CodeResourceExhausted`.
    - `TestUIServiceSourceBlobOnEveryAttachmentPoint`: the same property at each of the three attachment points, as two `t.Run` subtests — `multi-def-candidates` (each gathered candidate on a multi-definition symbol) and `explore-groups` (each group on an `Explore` response). The single-definition attachment point is the bullet above; these are the other two, so all three attachments this plan declares are asserted end-to-end.
    - `TestUIServiceSourceBlobGroupSourceMatchesItsGroupPath`: a group's source blob corresponds to that group's path, verified by key lookup into `ExploreResult.Sources` rather than by slice position, on a query producing at least three groups so a positional coincidence cannot pass.
    - `TestUIServiceSourceBlobAtTheCapClearsTheTransportBackstop`: a response at exactly the application cap passes through the transport backstop without a `CodeResourceExhausted` — the failing input is a backstop mis-sized at or below `sourceByteCap`.
    - `TestUIServiceSourceBlobFileModeUsesTheSameTruncationPath`: a `GetNodeDetail` in file mode returns the file's source through the same truncation path as the definition modes, asserted by comparing against `truncateSource`'s output computed independently in the test for the same input.
  </behavior>
  <action>
    Add `SourceBlob` to `internal/uiproto/uiv1/ui.proto` with the six fields the checkpoint
    locked, numbered 1 through 6, each carrying a doc comment naming what it counts and in
    which unit. Give the content field a doc comment recording the scoped UTF-8 guarantee
    verbatim: an input that was valid UTF-8 stays valid because the byte cut never splits a
    rune, and the field is `bytes` rather than `string` because indexed source is not
    guaranteed valid UTF-8 and a proto3 `string` would fail to marshal such a file rather
    than serving it. Record the line-counting rule in the total-lines field's doc comment,
    with the worked examples, so a client implementer reads it where they need it.

    Attach it on `GetNodeDetailResponse`, on `NodeDefinition` and on `ExploreGroup`, at the
    next free number in each — the numbers plan 01-09 named in `ui.proto`'s file-level
    intent paragraph. Do NOT renumber any field 01-09 declared to make room; additive-only
    means the new field takes the next free slot. Regenerate with `task proto:gen`.

    Then EXTEND plan 01-09's known-number fixture in `internal/uiserver/readonly_test.go`
    with `SourceBlob`'s six numbers and the three attachment numbers, and re-run
    `TestUIProtoFieldNumbersAreStableAndUnique`. Every entry 01-09 wrote must still hold —
    that is the cross-wave protection, and it is the half that is actually enforceable.
    Extend the fixture; do not rewrite it, and do not drop an entry to make one pass. The
    fixture's `resolved == len(fixture)` assertion means each entry added here must name a
    field that now really exists in the generated descriptor, so a typo fails loudly rather
    than being skipped. Update the fixture's provenance comment with this plan's date.

    Wire truncation into the handlers so every source blob leaving the process goes through
    `truncateSource`:

    - `GetNodeDetail` in file mode already has the file's source from the Engine's file
      shape; truncate it.
    - `GetNodeDetail` in single-definition mode obtains source through
      `(*Engine).SourceFor` on the definition's file path — the sanctioned wrapper over the
      existing repo-root confinement — inside the same `withEngine` call. Note in a comment
      that this is a read the CLI's single-definition path does not perform, that it is a
      deliberate wire-layer addition rather than a change to `Node()`, and that it goes
      through the same confinement gate.
    - `GetNodeDetail` in multi-definition mode uses each gathered candidate's own source,
      which the per-candidate lookup already populated; out-of-cap candidates carry no
      source blob, consistent with carrying no gathered detail.
    - `Explore` uses each group's own source, looked up by the group's path key.

    Truncation is never signalled by an error: the client still receives usable content and
    can render "showing first N of M", which is what RPC-05 literally asks for.

    Add the transport backstop in `internal/uiserver/server.go`, on the
    `uiv1connect.NewUIServiceHandler` call inside `Listen`, using the two named constants
    and the option signatures plan 01-01 verified and recorded. `connect-go` defaults to
    unlimited on both sides, so RPC-05 is satisfied by no default.

    Write `internal/uiserver/sourceblob_test.go` containing the five test functions the
    behavior block names, one per bullet, each driving a REAL Connect client against a real
    listener obtained from `Listen` — not the `uiService` method directly — so the assertion
    covers the transport backstop as well as the application cap. This file is this task's
    own deliverable and its own scored leg: it is a NEW file rather than an extension of
    `readonly_test.go`, whose scope is the field-number fixture, and rather than an extension
    of plan 01-09's handler tests, because a gate that scores this task must be able to fail
    when this task writes nothing. Leaving these cases inside another plan's test functions
    is exactly the defect this separation exists to prevent.
  </action>
  <verify>
    <automated>task proto:gen && test -d internal/uiproto && test -z "$(git status --porcelain internal/uiproto/)" && go build ./... && { BOUT=$(go test -v -count=1 -race -run 'TestUIServiceSourceBlob' ./internal/uiserver/ 2>&1); BSTATUS=$?; BCOUNT=$(printf '%s\n' "$BOUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "sourceblob exit=$BSTATUS PASS lines: $BCOUNT"; [ "$BSTATUS" -eq 0 ] || printf '%s\n' "$BOUT"; [ "$BSTATUS" -eq 0 ] && [ "$BCOUNT" -ge 7 ]; } && { OUT=$(go test -v -count=1 -race -run 'TestUIServiceNodeDetail|TestUIServiceExplore' ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "inherited exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 14 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - The gate has TWO legs and both must pass. **Leg 1 (this task's own deliverable):** `go test -v -count=1 -race -run 'TestUIServiceSourceBlob' ./internal/uiserver/` emits at least 7 `--- PASS` lines and exits 0. **Leg 2 (inherited regression):** `go test -v -count=1 -race -run 'TestUIServiceNodeDetail|TestUIServiceExplore' ./internal/uiserver/` emits at least 14 `--- PASS` lines and exits 0. Each leg is the conjunction of its status and its floor, never either alone.
    - **Why two legs, and why the previous single leg could not fail.** The previous form ran one pattern, `TestUIServiceSourceBlob|TestUIServiceNodeDetail|TestUIServiceExplore`, against a floor of 14, and its own derivation said the inherited sixteen alone cleared it and the new blob tests were headroom. That is a gate that passes when this task's entire deliverable is never written: the `TestUIServiceSourceBlob` atom matches nothing, `go test` exits 0 on a pattern that matches nothing, and plan 01-09's tests supply the whole count. The failure is invisible to every shape check — the command parsed, honored exit status, and was positively controlled — because the defect is in what the gate is SCOPED to, not in how it is written. Splitting the legs is the fix required by this plan's sixth prohibition: leg 1 is scoped to tests THIS task declares and its floor is derived from those tests alone, so it goes red when they are absent; leg 2 states in writing that plan 01-09's tests are its intended subject, which is what makes an inherited-test leg legitimate rather than dilution.
    - **Leg 1 floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** Every test the atom matches is declared by THIS task, in `internal/uiserver/sourceblob_test.go`, one per behavior bullet: `TestUIServiceSourceBlobOnEveryAttachmentPoint` declares two `t.Run` subtests (`multi-def-candidates`, `explore-groups`) = 3; `TestUIServiceSourceBlobTruncatesAnOversizedDefinitionSource`, `TestUIServiceSourceBlobGroupSourceMatchesItsGroupPath`, `TestUIServiceSourceBlobAtTheCapClearsTheTransportBackstop` and `TestUIServiceSourceBlobFileModeUsesTheSameTruncationPath` contribute 1 each = 4. Total **7** against a floor of 7 — zero headroom by design, so dropping, renaming or failing to write any one of the five turns this leg red, which is the entire point of scoping it to this task's own tests. No plan 01-09 test matches this atom.
    - **Behavior-bullet homes.** All five bullets in this task's behavior block name their own test function, and all five of those functions are counted by leg 1's derivation above — so no bullet is orphaned and every bullet's owner is inside the leg that can fail.
    - **Leg 2 floor derivation, and why its subject is deliberately another plan's tests.** This leg matches only plan 01-09's `TestUIServiceNodeDetail*` (10 lines by 01-09's own derivation) and `TestUIServiceExplore*` (6 lines) = 16. It exists to prove that attaching `SourceBlob` to `GetNodeDetailResponse`, `NodeDefinition` and `ExploreGroup` did not break the responses 01-09 already shipped — the inherited tests ARE the intended subject here, which is why it is not dilution. Its floor is 14 rather than 16 so a legitimately skipped inherited subtest cannot turn this leg red for a reason belonging to another plan; a REGRESSION in those tests still fails through the `STATUS -eq 0` half of the conjunction, which is why the floor may be loose on this leg and the status clause may not. This leg carries no atom naming a test of this task's, so it makes no claim about this task's deliverable — leg 1 is the only leg that does.
    - An end-to-end test through a real Connect client retrieves a source blob larger than `sourceByteCap` and receives a truncated, valid-UTF-8 response with the truncated flag set — not a `CodeResourceExhausted`.
    - The explore end-to-end test verifies each group's source by key lookup against `ExploreResult.Sources`, not by slice position.
    - `TestUIProtoFieldNumbersAreStableAndUnique` (plan 01-09) still passes after `SourceBlob` is attached, with every fixture entry plan 01-09 wrote unchanged and still holding — proving this plan added numbers and renumbered none.
    - That fixture has GROWN by exactly nine entries (`SourceBlob`'s six fields plus the three attachment fields), asserted IN CODE against 01-09's named baseline constant and not against a number carried across two waves in a prose SUMMARY. This plan declares `uiProtoFieldFixtureLenAtPlan0110 = uiProtoFieldFixtureLenAtPlan0109 + 9` immediately above the extended fixture and asserts `len(fixture) == uiProtoFieldFixtureLenAtPlan0110`. Restating either side as a bare literal is forbidden: the whole point of the constant chain is that the compiler carries the baseline, so an edit to 01-09's fixture that changes its length breaks this assertion here rather than silently shifting the arithmetic. The fixture's `resolved == len(fixture)` assertion and its every-declared-field-is-covered assertion both still hold, so no added entry names a non-existent field and no newly declared field escapes coverage.
    - Plan 01-11 extends this fixture again with `GetStatusResponse.store_exists = 8` and `.indexing_in_progress = 9` and will declare `uiProtoFieldFixtureLenAtPlan0111 = uiProtoFieldFixtureLenAtPlan0110 + 2`. Record `uiProtoFieldFixtureLenAtPlan0110`'s resolved value in the SUMMARY and state in its doc comment that 01-11 is the next and final extender in this phase.
    - `SourceBlob`'s content field is `bytes` and its doc comment records the scoped UTF-8 guarantee; the total-lines field's doc comment records the counting rule with its worked examples.
    - Every source blob leaving a handler goes through `truncateSource`: asserted by a count of `truncateSource(` call sites in `internal/uiserver/handlers.go` equal to the number of source-producing paths, positive-controlled in the same invocation against the count of `SourceBlob` constructions, with comment lines stripped first.
    - `task proto:gen` followed by `git status --porcelain internal/uiproto/` produces empty output — run behind the `test -d internal/uiproto &&` existence guard, without which the check passes vacuously on a missing or typo'd path, `task proto:drift` passes with an unchanged compared-count, and `go build ./...` succeeds.
  </acceptance_criteria>
  <done>Every source blob leaving the process is bounded in two units, cut on a rune boundary, explicitly marked, attached to the response element it belongs to, and sits under a transport backstop that never fires on correct output.</done>
</task>

</tasks>

## Review feedback dispositions

| Finding | Severity | Disposition |
|---|---|---|
| Cycle-2 HIGH C2-2: automated PASS-count gates discard the test command's exit status | HIGH | **Incorporated across every gate in this plan.** A shell pipeline's `$?` is the LAST command's, so `COUNT=$(go test ... | rg '--- PASS' | wc -l); [ "$COUNT" -ge N ]` passed with N passing subtests plus a `--- FAIL`. Every `<automated>` gate now captures `go test`'s output into `OUT` and its status into `STATUS` BEFORE any pipe, counts from `OUT`, dumps `OUT` on failure, and requires `STATUS -eq 0` AND the `--- PASS` floor. The conjunction is the rule: the count alone is vacuous when a test fails, the status alone is vacuous when `-run` matches nothing. A plan-level prohibition against the bare count-only form is in `must_haves.prohibitions`. The only scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence. |
| `ExploreResponse` per-group `SourceBlob` modeling unspecified, and locked by a one-way checkpoint in the same plan | HIGH | **Incorporated across two plans.** Plan 01-09 declares `ExploreGroup` one wave earlier, so this checkpoint locks an attachment onto a message already visible in the tree rather than one that does not exist yet. |
| Cycle-2 C2-1: 01-09's comment-reserved source numbers cannot be descriptor-pinned one wave early | HIGH | **Incorporated.** The reservation was withdrawn from 01-09 (a descriptor carries no comments; a `reserved` clause would block this plan outright). This plan now allocates the three attachment numbers itself at the next free slot in each message, and EXTENDS 01-09's known-number fixture with all nine new numbers, re-running the same test so every number 01-09 allocated is proven undisturbed. The guard is runnable in the wave that declares it, on both sides, and each side carries a positive assertion: 01-09 asserts `resolved == len(fixture)` and a non-zero fixture length; this plan asserts the fixture grew by exactly nine. |
| `bytes content` plus a universal UTF-8 guarantee is contradictory | MEDIUM | **Incorporated, resolved toward `bytes` with a scoped guarantee.** Rationale recorded in the checkpoint and in the field's doc comment: indexed source is not guaranteed valid UTF-8 and proto3 `string` would fail to marshal such a file rather than serving it. |
| Line-count semantics undefined for empty input, trailing newline and an unterminated final line | MEDIUM | **Incorporated.** The rule is stated in the checkpoint with four worked examples, implemented in one `countLines` helper used for both the total and the returned count, and covered by `TestCountLinesSemantics`. |
| Duplicating `truncateOnRuneBoundary` creates two security-relevant copies | LOW | **Incorporated.** Extracted to `internal/textutil` and consumed by both packages, with a positive-controlled zero-gate proving the `mcp` copy is gone. The "record which was chosen" wording is removed — the choice is made here. |
| Old 01-07 was 95k with three tasks plus a checkpoint | MEDIUM (cross-plan) | **Split.** This plan carries RPC-05 only, at 2 tasks plus the checkpoint / 65k; SRV-04's degrade path is plan 01-11. |
| Both blocking checkpoints only reconfirm decisions already locked in CONTEXT | LOW (cross-plan) | **Rejected for this checkpoint, with rationale.** D-11/D-12 lock the principle, not the field set, numbers, attachment points, content type or counting rule — five new decisions, four of them permanent. The checkpoint's own context now says so explicitly and contrasts itself with plan 01-06's. |
| Cycle-3 M7: the numeric-literal check has no command, no path scope and no positive control, and unscoped would match the generated `graph.pb.go` | MEDIUM | **Incorporated.** It is now a runnable, scoped, controlled gate rather than a SUMMARY note: the search is scoped to `internal/uiserver/ internal/textutil/ internal/mcp/`, excludes `*.pb.go` and `*.connect.go` by glob, requires exactly `1` match per chosen value (the constant's own declaration), and is positive-controlled in the same invocation by the identifier form returning at least `4`. The criterion states why each of the three parts is the assertion rather than incidental — in particular that without the control a mistyped PATH and a mistyped PATTERN both print `0` and are indistinguishable. This was the one executable gate in the phase with no positive control at all. |
| Cycle-3 M10: the `+9` assertion compares against a length recorded in prose | MEDIUM | **Incorporated.** Asserted in code as `uiProtoFieldFixtureLenAtPlan0110 = uiProtoFieldFixtureLenAtPlan0109 + 9` with `len(fixture) == uiProtoFieldFixtureLenAtPlan0110`; neither side may be a bare literal, so an edit to 01-09's fixture breaks this assertion here rather than silently shifting the arithmetic. |
| Cycle-3 M4: the `git status --porcelain` gate passes vacuously on a missing path | MEDIUM | **Incorporated.** Prefixed `test -d internal/uiproto &&`. One of the nine phase-wide sites. |
| Cycle-3 L1: the `--- PASS` counting convention is never stated anywhere in the phase, and echo labels contradict each other | LOW | **Incorporated phase-wide.** A byte-identical `## Gate conventions (phase-wide canon)` section now sits immediately above `<tasks>` in ALL ELEVEN plans. It states the convention once and canonically — one `--- PASS` line per test function PLUS one per `t.Run` subtest — mandates the single echo label `PASS lines:`, restates the D-04 conjunction with its one scoped exception, and enumerates the five vacuity shapes cycle 3 found. Every floor in this plan now carries an explicit **Floor derivation** bullet showing the arithmetic, so a reviewer checks the derivation rather than re-deriving it. Label census: six distinct labels existed across the phase (`PASS subtests:`, `PASS tests:`, `PASS:`, `post-restore PASS lines:`, `PASS lines across both tests:`, `uiserver PASS lines:`); all six were normalised to `PASS lines:`. |
| Cycle-5 H3: Task 2's entire deliverable was guarded by nothing executable — `TestUIServiceSourceBlob` was declared nowhere, its gate's floor was cleared by plan 01-09's inherited tests, and the derivation said so in writing ("the new blob tests are headroom") | HIGH | **Incorporated at the root, not at the symptom.** Task 2 now declares its own test file `internal/uiserver/sourceblob_test.go`, names one test function per behavior bullet in the behavior block and in the artifacts manifest, adds the file to `<files>` and to `files_modified`, and splits the gate into two legs: leg 1 is scoped to `TestUIServiceSourceBlob` alone with a floor of 7 derived from those five functions and their two subtests and zero headroom, so it goes red if the deliverable is absent; leg 2 keeps the inherited `TestUIServiceNodeDetail|TestUIServiceExplore` regression with its floor of 14 and now states in writing that plan 01-09's tests are its intended subject. The generalisation is the sixth prohibition below. |
| Cycle-5: terminate the SCOPE class — a gate can satisfy all five shape prohibitions and still be unable to fail | HIGH (phase-wide) | **Incorporated phase-wide as a sixth, SEMANTIC prohibition**, identical in all eleven plans, and restated in the `## Gate conventions` canon. It bans a gate whose floor can be met without the deliverable it names existing, with two checkable requirements: (a) OWNERSHIP — every gate carries at least one leg scoped to test functions the task itself declares, with a floor derived from that leg alone, and any pattern that also matches an earlier plan's tests must say so and say why those tests are the intended subject; (b) HOMING — every behavior bullet is named by, or names, a test function or subtest the task's own floor derivation counts, or the derivation states in writing that it is covered by a named criterion or deliberately out of scope. Unlike the five shape prohibitions, this one is not satisfiable by rewriting a command; it requires a structural leg and a written mapping. |
| Cycle-3: terminate the vacuity class rather than fixing instances | — | **Incorporated phase-wide.** Five new descriptor-less entries in `must_haves.prohibitions`, identical in all eleven plans, forbid: a `git status --porcelain` check without a `test -d` existence guard; a positive control that targets a different file than the assertion; a count-matching regex that accepts zero; an unanchored identifier grep; and an unranged `git diff` emptiness assertion. Each is a shape cycle 3 found in this phase's own gates, so a future author cannot reintroduce one without violating a stated prohibition. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source file → RPC response body | Arbitrary file content, including pathological minified files and files that are not valid UTF-8, is read from disk and written into a response destined for a browser. |
| application cap → transport cap | Two independent limits must stay ordered. If the transport cap fell at or below the application cap, correct truncation would surface to the user as a transport error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Denial of Service | verbatim source response body | high | mitigate | Two independent layers: an application line cap with a byte cap underneath it, cut on a UTF-8 rune boundary and marked with explicit totals in both units; and `connect.WithSendMaxBytes`/`WithReadMaxBytes` as a transport backstop set strictly above the application cap, asserted by a direct inequality test so the ordering cannot invert. |
| T-01-37 | Tampering | rune-boundary cut duplicated across packages | medium | mitigate | The cut lives in one `internal/textutil` helper consumed by both `internal/mcp` and `internal/uiserver`; a positive-controlled zero-gate proves no second copy remains. |
| T-01-38 | Denial of Service | a non-UTF-8 source file failing to marshal | medium | mitigate | The content field is `bytes`, so an oddly-encoded file is served rather than converted into a marshal failure. The UTF-8 guarantee is scoped to inputs that were already valid, and stated that way on the field. |
| T-01-07 | Information Disclosure | single-definition source read added at the wire layer | high | mitigate | The read goes through `(*Engine).SourceFor`, a wrapper over the existing two-stage repo-root confinement, and not through a new read path. The addition is documented at the call site as a deliberate wire-layer read the CLI does not perform. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race -count=1 ./internal/uiserver/... ./internal/textutil/... ./internal/mcp/...` passes: the command exits 0 AND its `--- PASS` count meets its floor — both, never either alone.
- `task proto:drift` passes with an unchanged compared-count.
- `task test:unit` passes; `task test:golden` reports `attempted=26 completed=26 matched=26`.
- `go vet ./internal/uiserver/... ./internal/uiproto/... ./internal/textutil/...` is clean.
</verification>

<success_criteria>
- An oversized source response comes back bounded, cut on a rune boundary, and explicitly marked truncated with true totals in both units — never unbounded and never as an error.
- Line counting is defined once and behaves identically for empty input, a trailing newline and an unterminated final line.
- The rune-boundary cut exists in exactly one place, shared by both consumers.
- The transport backstop sits strictly above the application cap and never fires on correct output.
</success_criteria>

<output>
Create `.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-10-SUMMARY.md` when done
</output>
