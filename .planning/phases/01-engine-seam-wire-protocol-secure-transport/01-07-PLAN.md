---
phase: 01-engine-seam-wire-protocol-secure-transport
plan: 07
type: execute
wave: 3
depends_on: ["01-01", "01-06"]
files_modified:
  - Taskfile.yml
  - internal/upgrade/proto_task_test.go
  - .github/workflows/ci.yml
  - internal/schema/graph.pb.go
  - internal/uiproto/uiv1/ui.pb.go
autonomous: true
requirements: [BLD-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Regenerating BOTH protobuf surfaces — the new `internal/uiproto/uiv1/ui.proto` and the pre-existing `internal/schema/graph.proto` — produces output byte-identical to the committed generated files (BLD-04)."
    - "Regeneration happens into a TEMPORARY tree and the comparison is a byte-compare against the committed files, so the guard can never overwrite a developer's in-progress edit to a generated file before reporting on it."
    - "The drift guard reports how many generated files it actually compared, and fails with a named error when that count is zero or below the expected minimum — it does not merely report that no diff was found (rule 84d1gfpywd)."
    - "The guard has been watched fail against a deliberately stale checked-in generated file for EACH surface independently, so a guard that covers one and silently skips the other is caught, and the failure output has been recorded (BLD-04, success criterion 4)."
    - "A generated-header-only difference counts as drift. Plan 01-01 absorbed the one-time restamp caused by introducing a checked-in codegen invocation; from that commit onward the committed file must be exactly what the pinned toolchain emits, header included."
    - "The guard runs in CI as a Taskfile-defined job body, consistent with the repo's rule that every CI job body lives in `Taskfile.yml`."
  artifacts:
    - internal/upgrade/proto_task_test.go
  key_links:
    - "`Taskfile.yml`'s `proto:gen` task is plan 01-01's deliverable; this plan adds `proto:drift` beside it and must not re-author `proto:gen`."
    - "BLD-04 closes a PRE-EXISTING gap: `Taskfile.yml` had no proto regeneration task before this phase and `internal/schema/roundtrip_test.go` tests serialization, not codegen currency."
    - "`TestWorkflowRunBodiesInvokeTask` already enforces that every CI job body is a `task` invocation. The new CI step must satisfy it rather than work around it."
  prohibitions:
    - "Never gate a task on a bare `go test ... | rg '--- PASS' | wc -l` count whose exit status is discarded. A shell pipeline's status is the LAST command's, so N passing subtests plus one `--- FAIL` satisfies a count-only gate. Every automated gate captures `go test`'s output and status BEFORE any pipe and requires BOTH: status 0 AND the `--- PASS` floor. The count alone is vacuous when a test fails; the status alone is vacuous when the `-run` pattern matches nothing. The sole scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence."
    - "Never regenerate in place over committed generated files as part of the drift check; a dirty worktree containing a developer's edit would be overwritten before the guard reported anything."
    - "Never revert a working-tree mutation with `git checkout --` on a shared or dirty tree; capture an exact reverse patch or work in an isolated `git worktree` scratch tree."
    - "Never write `test -z \"$(git status --porcelain <path>)\"` without a preceding `test -d <path> &&` existence guard. `git status --porcelain` on a missing or typo'd pathspec exits 0 with EMPTY stdout — the 'could not open directory' warning goes to stderr — so the check passes vacuously against a path that was never opened."
    - "Never positive-control a zero-count gate against a DIFFERENT file than the one the gate guards. `rg` prints nothing for a nonexistent path just as it does for a clean file, so a typo'd or renamed target reads `0` while the guarded file is never opened. The control must run the identical pipeline against the SAME file with a pattern that file genuinely contains."
    - "Never score a count with a regex that accepts zero. `rg -q -e 'compared [0-9]+ generated files'` matches `compared 0 generated files`, so it is a presence check on the count and not a floor. Capture the number and assert the floor numerically."
    - "Never write an unanchored identifier grep as a forbidden-pattern gate. Anchor it to the declaration form — `func gather[A-Z]`, never `\\bgather[A-Z]` — because a trailing comment or a `/* */` block survives `rg -v '^\\s*//'` and turns the gate red, or green, for the wrong reason."
    - "Never assert emptiness with an unranged `git diff`. Under atomic-commit-per-task the working tree is clean at verification time, so `git diff --stat <path>` is empty by construction whether or not the file was edited. Assert over an explicit `<phase-base>..HEAD` revision range."
    - "Never write a gate whose floor can be met without the deliverable it names existing. This is the SCOPE half of gate hygiene and no shape check reaches it: a scope-vacuous gate is well-formed, parses, honors exit status and carries a same-file positive control, and still scores nothing the task owns. Cycle 5's form of this prohibition was STRUCTURAL where it had to be CAUSAL — OWNERSHIP constrained who DECLARES the counted tests, not what those tests ASSERT, and HOMING accepted a named criterion no gate executes — so a task declaring one self-contained arithmetic test and homing every behavior bullet to prose satisfied both halves literally while the deliverable was entirely absent. The amended rule closes that. Two requirements, both mandatory and both checkable by reading the plan. (a) OWNERSHIP AND CAUSATION — every gate carries at least one leg that turns RED when this task's named deliverable is absent or replaced by a NO-OP, in exactly ONE of three named forms, and the floor derivation must say which form it uses. A no-op is an implementation that still compiles and keeps the same signature but does nothing and returns zero values; for a generated file, schema or other declarative artifact it is the empty or default-valued version of it. REPLACEMENT, not deletion, is the standard, because deletion is satisfied by a mere compile dependency and only replacement separates a test that ASSERTS an artifact's behavior from one that merely MENTIONS it. FORM A1, OWN-TEST, the default: the leg's test-selection pattern matches ONLY test functions this task itself declares, its floor is derived from that leg's own functions and subtests alone, AND the derivation names the ARTIFACT UNDER TEST — a specific function, type, constant, generated file or task, cited from this task's files list, this plan's files_modified, this phase's artifact manifest, or this task's read_first — and states why replacing that artifact with a no-op turns this leg RED. A counted test that would still pass against that no-op does not satisfy A1 however exclusively this task declares it, and a counted test with no artifact under test at all — a self-contained arithmetic assertion is the canonical form — never satisfies it. Where the named artifact is one this task does not itself modify, which is the characterization-assertion case, the derivation must say so and name where the artifact is cited. FORM A2, DIRECT-ARTIFACT, for a deliverable that is a command, generated file or task rather than a test and is named in this task's files list, this plan's files_modified or this phase's artifact manifest: the leg EXECUTES that artifact and scores a numeric floor read out of that artifact's OWN output, and the derivation names it and states that a no-op replacement drives the measured number below the floor. FORM A3, OBSERVATION, available ONLY to a task that declares no test function of its own and whose named deliverable is a recorded observation rather than persisted code — a deliberate-mutation RED observation, or a by-hand exercise against real processes. Such a task writes no assertion, so no `--- PASS` floor can ever be its evidence, and D-04's scoped exception to the conjunction lives here; it qualifies only when ALL FOUR hold: every observation is scored by at least one NAMED numeric or verdict-bearing measurement taken from a named command's own output and recorded verbatim; a vacuous measurement — a zero failure count, a missing per-item verdict — is written down as a STOP CONDITION and not as a note; the gate still carries the strongest executable leg available to it; and the derivation states plainly which failures of THIS task that leg CAN catch and which it CANNOT. A task that declares even one test function of its own may never use A3, and A3 is never a way to avoid writing one. Where a leg's pattern ALSO matches an earlier plan's tests, the derivation must say so, say why those inherited tests are the intended subject of that leg, and show the leg still turns RED without this task's own tests by stating the count the inherited tests alone supply and showing it falls BELOW the floor; a leg whose floor the inherited tests alone already clear NEVER counts as the leg this requirement demands. (b) HOMING — every behavior bullet is named by, or names, a test function or subtest that the task's own floor derivation counts; or it is homed to a named acceptance criterion WHOSE COMMAND APPEARS IN THIS TASK'S `<automated>` GATE, because prose is not a home and a criterion no gate executes is not an executable owner; or the derivation states in writing that the bullet is deliberately out of scope and names the plan or phase that owns it instead, which a bullet describing work THIS task performs may never claim. A bullet no derivation reaches is a described behavior with no executable owner, and a leg whose floor another plan's tests already clear cannot fail when this task writes nothing."
---

<objective>
Give both protobuf surfaces a codegen drift guard that reports what it compared, cannot
destroy a developer's work while doing it, and has been watched fail against each surface
independently.

Purpose: BLD-04 closes a gap that predates this milestone. There was no proto codegen drift
guard in this repository and no regeneration task in `Taskfile.yml` — no `go:generate`
directive, no Makefile target, no CI step. `internal/schema/roundtrip_test.go` tests
serialization round-tripping, not codegen currency. So the guard must cover the
pre-existing `internal/schema/graph.proto` as well as the new UI schema, which is exactly
why plan 01-01 had to absorb a one-time generated-header restamp on `graph.pb.go` before
this guard could be honest about what "no diff" means.

Two design choices are load-bearing and both come out of the cross-AI review. The guard
regenerates into a temporary tree and byte-compares rather than regenerating in place and
asking git what changed: in-place regeneration on a worktree that already contains a
developer's edit to a generated file overwrites that edit before reporting on it, which
turns a diagnostic into a data-loss event. And the guard reports a positive count of files
compared, because a drift guard that enumerated nothing is precisely the vacuous shape
rule `84d1gfpywd` names, and this repository already carries that defect class more than
once.

Output: `proto:drift`, its companion Go shape guards, a CI step, and the recorded RED
evidence for each surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-01-SUMMARY.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-06-SUMMARY.md
</context>

## Artifacts this phase produces

**Created by this plan (01-07):**

- `Taskfile.yml` — new task `proto:drift`
- `internal/upgrade/proto_task_test.go` — `TestProtoTasksExist`, `TestProtoDriftGuardReportsAComparedCount`, `TestProtoDriftGeneratesIntoATemporaryTree`, `TestProtoDriftTaskIsInvokedByCI`
- `.github/workflows/ci.yml` — a step invoking `task proto:drift`

**Created elsewhere in this phase** (listed so drift verification sees the whole set):
`internal/goldenspec`; `testdata/golden/byte_identity_test.go`; `internal/uiproto/uiv1`
(package, `ui.proto`, `UIService` and its nine rpcs, their request/response messages, the
shared `Node`/`Location`/`FileEntry`/`FileTreeNode`/`ExploreGroup`/`BlastEntry`/
`SourceBlob`/`IndexingInProgress`/`NodeDetailMode` wire types, generated `ui.pb.go` and
`uiv1connect/ui.connect.go`); `internal/uiserver` (`Options`, `DefaultAddr`, `Server`,
`Listen`, `Serve`, `URL`, `Close`, `originHostGuard`, `allowedHosts`, `allowedOrigins`,
`uiService`, `openEngine`, `withEngine`, `mapEngineError`, the nine mappers,
`uiMultiDefCap`, `truncateSource`, `sourceLineCap`, `sourceByteCap`,
`transportSendMaxBytes`, `transportReadMaxBytes`, `errIndexingInProgress`,
`indexingInProgressMessage`, `degradedStatus`, `classifyDegrade`); `internal/cli/ui.go`;
`internal/textutil.TruncateOnRuneBoundary`; `internal/query`'s `NodeDetail` family,
`ExploreResult` family, `ErrNotFound`, `ErrInvalidArgument`, `(*Engine).IndexMeta`,
`(*Engine).SourceFor` and the `build*` builders; `schema.Meta.commit_sha` field 8 / `Meta.CommitSha` / `GetCommitSha` /
`schema.IndexedCommitSHA`; `internal/indexer/commit.go`'s `resolveHeadCommitSHA` and
`gitExecLookPath`; `internal/mcp`'s `looksLikeJSONRPCResponse`, `sniffedOutbound`,
`decrementPending`, `pendingUnderflows`; `buf.yaml`, `buf.gen.yaml`, `go.tool-proto.mod`,
`Taskfile.yml` task `proto:gen`.

## Gate conventions (phase-wide canon — byte-identical in all eleven plans, and checkable)

This section is replicated verbatim across `01-01-PLAN.md` … `01-11-PLAN.md`. The claim is
checkable rather than merely asserted — one command, run from the phase directory, prints
**one** distinct digest if the eleven copies agree and more than one if any has drifted:

```
for f in 01-*-PLAN.md; do sed -n '/^## Gate conventions/,/^<tasks>$/p' "$f" | shasum; done | sort -u | wc -l
```

A future planner editing one copy must edit all eleven, and this is how a reviewer catches it
if they do not. (Nothing at execution time changes this section, so no `<automated>` gate owns
it — the drift risk is at planning time, which is where the check belongs.)

**`--- PASS` counting.** `go test -v` prints one `--- PASS:` line for the parent test
function AND one for every `t.Run` subtest, including nested ones. Every PASS floor in this
phase counts BOTH: a test function with three subtests contributes FOUR lines, so
`rg -o -e '--- PASS' | wc -l` over `go test -v` output equals
`len(test functions) + len(all subtests)`. Derive every floor that way and show the
arithmetic in the acceptance criterion that states it, so a reviewer can check the floor
instead of re-deriving it.

**Echo label.** Every gate prints `PASS lines:` — never `PASS tests:`, never
`PASS subtests:`. The label names what is counted, and what is counted is lines.

**The conjunction (D-04 as corrected in `01-CONTEXT.md`).** A gate passes only when the
test command's exit status is 0 AND the `--- PASS` floor is met. Neither is sufficient
alone: `go test -run PATTERN` exits 0 when the pattern matches nothing, and a shell
pipeline's `$?` is the LAST command's, so a bare count passes with N passing subtests plus
one `--- FAIL`. The single scoped exception is a deliberate-mutation RED observation, where
a non-zero exit IS the expected evidence and the observation is scored by `--- FAIL` /
`--- PASS` counts rather than by status.

**The five vacuity shapes cycle 3 found in this phase's own gates.** Each is a prohibition
in `must_haves.prohibitions`. They are restated here together because they are one class —
a guard that reads green against a file it never opened:

1. `git status --porcelain <path>` exits 0 with empty stdout on a MISSING or typo'd path
   (the warning goes to stderr). Always prefix `test -d <path> &&`.
2. A zero-count gate positive-controlled against a DIFFERENT file proves nothing about the
   guarded file: `rg` prints nothing for a nonexistent path too. Control on the SAME file.
3. `rg -q 'compared [0-9]+ generated files'` accepts `compared 0 generated files`. Capture
   the number; assert the floor numerically.
4. An unanchored identifier grep (`\bgather[A-Z]`) is satisfiable by a trailing or `/* */`
   comment that `rg -v '^\s*//'` does not strip. Anchor to the declaration form.
5. An unranged `git diff` is empty by construction at verification time under
   atomic-commit-per-task. Assert over `<phase-base>..HEAD`.

**The scope shape cycle 5 found, which none of the five above reaches — and the cycle 7
amendment that made it causal.** The five shapes are all about how a gate is WRITTEN. A gate can
satisfy every one of them and still be vacuous because of what it is SCOPED to: if its floor is
already cleared by test functions belonging to an EARLIER plan that happen to share the selection
pattern, then the pattern matching none of this task's own tests costs nothing — `go test` exits 0
on a pattern that matches nothing, and the inherited tests supply the count. The sixth prohibition
in `must_haves.prohibitions` closes it. Cycle 5's form of that prohibition was STRUCTURAL where it
had to be CAUSAL: OWNERSHIP constrained who DECLARES the counted tests, not what those tests
ASSERT, and HOMING accepted a named criterion that no gate executes. Both halves were therefore
satisfiable literally by a task declaring one self-contained arithmetic test and homing every
behavior bullet to prose, with the deliverable entirely absent. The amended prohibition supplies
the missing causal clause. Half (a) now requires the leg to turn RED when the deliverable is
REPLACED BY A NO-OP — replacement, not deletion, because deletion is satisfied by a mere compile
dependency — and names the three forms a qualifying leg may take: A1, an own-test leg that names
its artifact under test; A2, a direct-artifact leg scoring a number read out of that artifact's own
output; and A3, an observation leg, open only to a task that declares no test at all and whose
deliverable is a recorded observation, which must carry a named measurement, a written STOP
CONDITION for a vacuous measurement, the strongest executable leg available to it, and a plain
statement of what that leg cannot catch. A3 is what keeps D-04's deliberate-mutation exception
representable. A leg whose pattern also matches an earlier plan's tests must show the inherited
count alone falls BELOW the floor, or it does not qualify. Half (b) now requires a criterion's
command to appear in the task's `<automated>` gate before that criterion counts as a home. Every
floor derivation in this phase names its form. All of it is checkable by reading the plan, which is
where the defect lives.

<tasks>

<task type="auto">
  <name>Task 1: `proto:drift` — regenerate into a temp tree, byte-compare, report the count</name>
  <files>Taskfile.yml, internal/upgrade/proto_task_test.go, .github/workflows/ci.yml</files>
  <read_first>
    - Taskfile.yml — the `corpora:drift` task: the closest existing precedent (regenerate, then diff, fail if non-empty) and the two gaps it has, which are that it reports no positive count of what it compared and that it regenerates in place
    - Taskfile.yml — the `proto:gen` task plan 01-01 added, which this task reuses rather than duplicates, and the `preconditions:` message style the existing `corpora:*` tasks use
    - internal/upgrade/taskfile_shape_test.go lines 1-60 — the literal-fixture-plus-parse-and-assert idiom: path constants, `requiredCheckNames`-style literal slices each carrying a provenance comment, and `yaml.v3` parsing of the real on-disk file. Nineteen of this repo's forty-nine non-vacuity guards live in this file
    - internal/upgrade/taskfile_shape_test.go around `TestWorkflowRunBodiesInvokeTask` — the rule the new CI step must satisfy
    - .github/workflows/ci.yml — the job structure and where the other structural guards run
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-01-SUMMARY.md — the recorded one-time generated-header restamp and the phase ruling that a header-only rewrite counts as drift from that commit onward
    - .planning/STATE.md — the standing rule that a guard must carry a positive assertion that it did its work, and that a negative-only guard passes vacuously
  </read_first>
  <action>
    Add a `proto:drift` task to `Taskfile.yml`, beside `proto:gen`. It proves two things,
    not one: that the committed generated files match what the pinned toolchain emits, and
    that it actually compared something.

    **Regenerate into a temporary directory, never in place.** Point `buf generate` at a
    `mktemp -d` output root, then compare each committed generated file against its
    freshly-generated counterpart byte for byte. The `corpora:drift` precedent regenerates
    in place and asks git what moved; copying that here would mean that on a worktree
    containing a developer's in-progress edit to a generated file, the guard destroys that
    edit before it reports anything. A temp tree makes the guard read-only with respect to
    the source tree, which is what a diagnostic should be. Remove the temp tree on exit,
    including on failure.

    **Count before comparing.** Enumerate the committed generated files under
    `internal/schema/` and `internal/uiproto/` into a variable, and echo the total in the
    form `proto:drift: compared N generated files`. Fail with a named, self-explaining
    error when N is zero, and fail when N is below the expected minimum. Then compare, and
    on any mismatch fail listing the specific files that differed — a failure reporting only
    that drift was detected, without naming what drifted, is insufficient and the task's
    output must be improved rather than the evidence weakened.

    **Both surfaces are in scope**: the new `internal/uiproto/uiv1/*.pb.go` and
    `*.connect.go`, and the pre-existing `internal/schema/graph.pb.go`. BLD-04 closes a
    pre-existing gap, not only a new one.

    **A header-only difference is drift.** Say so in the task's `desc:` and in a comment.
    Plan 01-01 absorbed the one-time restamp that introducing a checked-in codegen
    invocation caused; from that commit onward the committed file must be exactly what the
    pinned toolchain emits, generated header included. Do not add a header-stripping
    normalisation: a guard that ignores the version stamp cannot detect a toolchain
    downgrade, which is one of the two drift causes worth catching.

    Give the task `preconditions:` entries for `git`, `go` and the pinned `buf`, following
    the message style the existing `corpora:*` tasks use, so a missing tool reports itself
    rather than producing an empty enumeration that looks like a clean pass.

    Add `internal/upgrade/proto_task_test.go` as the companion Go-level shape guard,
    matching `taskfile_shape_test.go`'s idiom rather than inventing a new one. Each literal
    fixture carries a provenance comment naming what was verified and when, as the
    neighbouring file's fixtures do.

    - `TestProtoTasksExist` parses the real `Taskfile.yml` with `yaml.v3` and asserts both
      `proto:gen` and `proto:drift` are present with non-empty `desc` and `cmds`.
    - `TestProtoDriftGuardReportsAComparedCount` asserts the `proto:drift` command body
      contains a count-reporting step and a zero-count failure branch. That property is what
      makes the guard non-vacuous, so it is itself guarded.
    - `TestProtoDriftGeneratesIntoATemporaryTree` asserts the command body creates and
      cleans up a temporary output root and that the comparison reads from it. This is the
      structural half of the no-data-loss property.
    - `TestProtoDriftTaskIsInvokedByCI` parses the real `.github/workflows/ci.yml` from
      disk — not a fixture copy — and asserts a step invokes `task proto:drift`.

    Add the CI step. Place it where the other structural guards run, name it so a failure is
    self-explaining, and make its body a `task` invocation so `TestWorkflowRunBodiesInvokeTask`
    keeps passing.
  </action>
  <verify>
    <automated>DRIFT=$(task proto:drift 2>&1); DSTATUS=$?; printf '%s\n' "$DRIFT"; NFILES=$(printf '%s\n' "$DRIFT" | rg -o -e 'compared [0-9]+ generated files' | rg -o -e '[0-9]+' | head -1); NFILES=${NFILES:-0}; echo "drift exit=$DSTATUS compared=$NFILES"; [ "$DSTATUS" -eq 0 ] && [ "$NFILES" -ge 3 ] && { OUT=$(go test -v -count=1 -run 'TestProto' ./internal/upgrade/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 4 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `task proto:drift` exits 0 on a clean tree AND prints `compared N generated files` where the CAPTURED N is at least 3 (`graph.pb.go`, `ui.pb.go`, `ui.connect.go`). Both gates in this plan capture N into `NFILES` and assert `[ "$NFILES" -ge 3 ]` numerically. They must not fall back to `rg -q -e 'compared [0-9]+ generated files'`: `[0-9]+` matches `0`, so that form is a PRESENCE check on the count and not a floor — a drift run that enumerated nothing would have printed `compared 0 generated files` and read green, which is structurally the same vacuity the phase-wide conjunction rule exists to close, and doubly ironic in BLD-04's own "report how many files it compared" guard. `NFILES` defaults to `0` when the line is absent entirely, so a missing line fails the same numeric comparison rather than erroring the shell.
    - `go test -v -count=1 -run 'TestProto' ./internal/upgrade/` emits at least 4 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - The enumerated file set covers both surfaces: the printed count and the file list include at least one path under `internal/schema/` and at least one under `internal/uiproto/`.
    - Running `task proto:drift` on a tree where a committed generated file has an uncommitted local edit leaves that edit intact — the guard reports the difference and does not overwrite the file. Demonstrated once by hand and recorded in the SUMMARY.
    - `task proto:drift` fails with a named error, not a silent pass, when the enumeration finds zero files. Verified by temporarily pointing the enumeration at an empty directory and observing the failure, then restoring.
    - `TestProtoDriftGeneratesIntoATemporaryTree` and `TestProtoDriftGuardReportsAComparedCount` each assert a positive property of the command body rather than the absence of one.
    - `TestProtoDriftTaskIsInvokedByCI` parses the real `.github/workflows/ci.yml` from disk.
    - `TestWorkflowRunBodiesInvokeTask` still passes — the new CI step's body is a `task` invocation, not an inline script.
  </acceptance_criteria>
  <done>Both proto surfaces are covered by a drift guard that reports what it compared, cannot clobber a developer's edit, and runs in CI as a Taskfile-defined job body.</done>
</task>

<task type="auto">
  <name>Task 2: Watch the guard fail against a deliberately stale file, once per surface</name>
  <files>internal/schema/graph.pb.go, internal/uiproto/uiv1/ui.pb.go</files>
  <read_first>
    - Taskfile.yml — the `proto:drift` task Task 1 just added
    - .planning/STATE.md — the standing decision that a gate is not trusted until it has been demonstrated RED against a confirmed-applied mutation
    - .planning/ROADMAP.md — Phase 1 success criterion 4, which requires the guard be "watched fail against a deliberately stale checked-in file"
  </read_first>
  <action>
    A drift guard that has only ever been observed passing is not evidence. Demonstrate it
    RED against each surface separately, so a guard that covers one but silently skips the
    other is caught.

    Do not mutate and then discard files in the developer's working tree. Create an
    isolated scratch tree with `git worktree add` and do both exercises there, or capture an
    exact reverse patch with `git diff > <patch>` before each mutation and restore with
    `git apply -R <patch>`. A whole-file discard destroys any overlapping edit the
    developer already had, and this procedure runs twice against files that are also being
    regenerated elsewhere in the phase.

    For the pre-existing surface: edit the committed `internal/schema/graph.pb.go` to make
    it stale relative to `graph.proto` — changing a generated struct tag or a generated
    field name is enough and is unambiguously a regeneration artifact rather than a real
    code change. Confirm with `git diff --stat` that the edit is actually applied. Run
    `task proto:drift`, record the exit status, the printed compared-count and the named
    files it reported as drifted. Restore and confirm `git status --porcelain
    internal/schema/` is empty.

    Repeat independently for the new surface: make `internal/uiproto/uiv1/ui.pb.go` stale
    the same way, confirm applied, run, record, restore, confirm clean.

    Then run a third exercise that the first two cannot cover: make a HEADER-ONLY edit —
    change the recorded generator version line and nothing else — confirm applied, run, and
    record that the guard fails. This is the phase's stated ruling made executable: a
    header-only rewrite counts as drift, and without this exercise nothing proves the guard
    is not silently normalising the header away. Restore and confirm clean.

    Finally run `task proto:drift` once more on the fully restored tree and record the clean
    pass with its compared-count.

    Write all of it into the SUMMARY: all three edits verbatim, all three
    applied-confirmations, all three failure outputs with their compared-counts, the restore
    method used, all three restore-confirmations, and the final clean run. Success criterion
    4 asks for exactly this evidence, and it is the difference between a guard that is
    trusted and a guard that is merely green.
  </action>
  <verify>
    <automated>test -d internal/schema && test -d internal/uiproto && test -z "$(git status --porcelain internal/schema/ internal/uiproto/)" && { DRIFT=$(task proto:drift 2>&1); DSTATUS=$?; printf '%s\n' "$DRIFT"; NFILES=$(printf '%s\n' "$DRIFT" | rg -o -e 'compared [0-9]+ generated files' | rg -o -e '[0-9]+' | head -1); NFILES=${NFILES:-0}; echo "drift exit=$DSTATUS compared=$NFILES"; [ "$DSTATUS" -eq 0 ] && [ "$NFILES" -ge 3 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records, for `internal/schema/graph.pb.go`: the stale edit verbatim, the `git diff --stat` applied-confirmation, `task proto:drift`'s non-zero exit and its output naming that file as drifted, and the post-restore `git status --porcelain` empty confirmation.
    - The SUMMARY records the same items for `internal/uiproto/uiv1/ui.pb.go`.
    - The SUMMARY records the same items for the header-only edit, proving the guard does not normalise the generated header away.
    - All three recorded failures name the specific drifted file. A failure that reports only that drift was detected, without naming what drifted, is insufficient and the task's output must be improved rather than the evidence weakened.
    - All three recorded failures also print the compared-count, proving the guard enumerated before it compared.
    - A `task proto:drift` run that exits 0 against a confirmed-applied stale file is a STOP CONDITION, not a note: it means the guard cannot see that surface. Fix the guard and re-run the exercise; do not record the observation as inconclusive and move on.
    - The SUMMARY names the isolated worktree or the exact reverse patch used for each restore. No file was reverted by discarding it.
    - The final clean run's compared-count equals the count recorded in Task 1.
    - **Form A3 (observation), stated because this task declares no test function at all, with a form-A2 leg as its executable half.** This task's deliverable is three deliberate-staleness RED observations of `task proto:drift`, not code and not an assertion, so no `--- PASS` floor can ever be its evidence. All four of A3's requirements are discharged. (1) **Named measurements.** Each observation is scored by measurements taken from `task proto:drift`'s own output — its non-zero exit, its printed compared-count, and the specific file it names as drifted — recorded verbatim in the SUMMARY. (2) **Stop condition.** A `task proto:drift` run that exits 0 against a CONFIRMED-APPLIED stale file means the guard cannot see that surface; the criterion below states that as a STOP CONDITION, alongside the existing rule that a failure which does not NAME the drifted file is insufficient and the guard is improved rather than the evidence weakened. (3) **Strongest executable leg available, and it is form A2.** This gate EXECUTES `task proto:drift` — this plan's own deliverable, declared by Task 1 and named in this plan's artifact manifest — and scores `NFILES`, a number read out of that task's OWN output, against a floor of 3. A no-op `proto:drift` prints no compared-count, `NFILES` defaults to 0 and the leg turns RED. It carries no test-selection pattern at all, so it has no inherited-test dilution surface for another plan's tests to satisfy. (4) **What it CAN and CANNOT catch.** It CAN catch a generated surface this task mutated and failed to restore, and a `proto:drift` that stopped enumerating — both failures belonging to THIS task. It CANNOT catch this task doing nothing at all; the three separately-enumerated SUMMARY exercises above are what close that, which is why they are recorded one per surface rather than as a single claim that the guard works.
    - `git status --porcelain internal/schema/ internal/uiproto/` is empty — run behind the `test -d internal/schema && test -d internal/uiproto &&` existence guard, without which the check passes vacuously on a missing or typo'd path at task completion.
  </acceptance_criteria>
  <done>The drift guard has been watched fail independently against each proto surface and against a header-only staleness, and every mutation was reverted without collateral.</done>
</task>

</tasks>

## Flagged Assumptions (unresolved edge probes)

The spec-less edge probe classified BLD-04 as `unclassified`. Per the probe's rules it is
not auto-resolved into `must_haves`; it is surfaced here as an open assumption:

- **BLD-04 / unclassified** — assumed: "committed protobuf codegen matches its `.proto`
  source" is fully discharged by regenerating with the pinned toolchain and byte-comparing.
  A drift introduced by a toolchain version change rather than a source change would present
  identically, and the correct response in that case is to re-pin, not to re-commit. The
  guard cannot distinguish the two causes and is not asked to — but because the generated
  header is deliberately NOT normalised away, a toolchain change at least surfaces rather
  than passing silently.

## Review feedback dispositions

| Finding | Severity | Disposition |
|---|---|---|
| Cycle-2 C2-2 applied to the `proto:drift` pipeline gates | HIGH | **Incorporated.** Both gates in this plan piped `task proto:drift` into `rg -q`, which discarded `task`'s own exit status exactly as the PASS-count gates discarded `go test`'s. Both now capture the drift output into `DRIFT` and the status into `DSTATUS` before any pipe, print both, and require `DSTATUS -eq 0` AND the `compared [0-9]+ generated files` line — so a drift run that fails outright can no longer read green on a matching line, and a run that succeeds without enumerating anything still fails. |
| Cycle-2 LOW: cross-plan artifact manifest drift — `(*Engine).SourceFor` missing from some "Created elsewhere in this phase" manifests | LOW | **Incorporated, and widened beyond what was flagged.** The review named 01-02 and 01-05. Auditing all eleven manifests found the same omission in 01-01, 01-03, 01-06 and 01-07 as well. `(*Engine).SourceFor` (declared by 01-04) is now named in every plan's manifest, so drift verification sees the whole phase-wide new-symbol set from any plan it starts at. |
| Cycle-2 HIGH C2-2: automated PASS-count gates discard the test command's exit status | HIGH | **Incorporated across every gate in this plan.** A shell pipeline's `$?` is the LAST command's, so `COUNT=$(go test ... | rg '--- PASS' | wc -l); [ "$COUNT" -ge N ]` passed with N passing subtests plus a `--- FAIL`. Every `<automated>` gate now captures `go test`'s output into `OUT` and its status into `STATUS` BEFORE any pipe, counts from `OUT`, dumps `OUT` on failure, and requires `STATUS -eq 0` AND the `--- PASS` floor. The conjunction is the rule: the count alone is vacuous when a test fails, the status alone is vacuous when `-run` matches nothing. A plan-level prohibition against the bare count-only form is in `must_haves.prohibitions`. The only scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence. |
| In-place generation followed by `git diff` is unsafe on a dirty worktree containing user edits to generated files | MEDIUM | **Incorporated.** The guard regenerates into a `mktemp -d` root and byte-compares; `TestProtoDriftGeneratesIntoATemporaryTree` asserts it structurally, and Task 1 requires a recorded hand-demonstration that a local edit survives a guard run. |
| Generated-header version stamp will produce a guaranteed first-run drift diff; no ruling on whether header-only satisfies "no diff" (added, not raised by Codex) | MEDIUM | **Incorporated across two plans.** Plan 01-01 absorbs and commits the one-time restamp with the diff verified header-confined. This plan states the ruling — header-only IS drift — refuses to normalise the header away, and Task 2 adds a third RED exercise that makes the ruling executable. |
| 01-01's acceptance checked only `internal/uiproto` while its `proto:gen` regenerates both surfaces | MEDIUM | **Incorporated in 01-01.** Its acceptance now checks `internal/uiproto` and `internal/schema` together. |
| `git checkout --` mutation revert can destroy overlapping user edits | MEDIUM (cross-plan) | **Incorporated.** Isolated worktree or exact reverse patch, named per restore in the SUMMARY; whole-file discard is prohibited. |
| Old 01-05 was 90k with four tasks | — | **Split.** ENG-04 is plan 01-06; this plan is BLD-04 only, 2 tasks, 55k. |
| Cycle-3 M6: `rg -q -e 'compared [0-9]+ generated files'` accepts `compared 0`, so it is a presence check and not a floor | MEDIUM | **Incorporated in both gates.** Each now captures the number into `NFILES` (defaulting to `0` when the line is absent entirely, so a missing line fails the same numeric comparison rather than erroring the shell), echoes it, and asserts `[ "$NFILES" -ge 3 ]`. The acceptance criterion and the `<verification>` bullet both state the floor numerically and forbid the regex form. Phase-wide census: **two** sites used the zero-accepting shape, both in this plan, both fixed; every other `[0-9]+` occurrence in the phase is prohibition or convention prose. |
| Cycle-3 L2: `01-07:307` states a PASS floor with no exit-status clause — the count-only shape this plan's own prohibition forbids | LOW | **Incorporated.** The `<verification>` bullet now states the conjunction explicitly and records that the matching `<automated>` gate was already compliant, so this was a documentation inconsistency rather than an executable defect. Phase-wide census: this was the **only** such bullet across the eleven plans; `01-01:510` also omits the clause but is a sub-criterion of a parent that states the conjunction, so it is subsumed and not a finding. |
| Cycle-3 M4: the `git status --porcelain` gate passes vacuously on a missing path | MEDIUM | **Incorporated.** Prefixed `test -d internal/schema && test -d internal/uiproto &&`. One of the nine phase-wide sites. |
| Cycle-3 L1: the `--- PASS` counting convention is never stated anywhere in the phase, and echo labels contradict each other | LOW | **Incorporated phase-wide.** A byte-identical `## Gate conventions (phase-wide canon)` section now sits immediately above `<tasks>` in ALL ELEVEN plans. It states the convention once and canonically — one `--- PASS` line per test function PLUS one per `t.Run` subtest — mandates the single echo label `PASS lines:`, restates the D-04 conjunction with its one scoped exception, and enumerates the five vacuity shapes cycle 3 found. Every floor in this plan now carries an explicit **Floor derivation** bullet showing the arithmetic, so a reviewer checks the derivation rather than re-deriving it. Label census: six distinct labels existed across the phase (`PASS subtests:`, `PASS tests:`, `PASS:`, `post-restore PASS lines:`, `PASS lines across both tests:`, `uiserver PASS lines:`); all six were normalised to `PASS lines:`. |
| Cycle-5: terminate the SCOPE class — a gate can satisfy all five shape prohibitions and still be unable to fail | HIGH (phase-wide) | **Incorporated phase-wide as a sixth, SEMANTIC prohibition**, identical in all eleven plans and restated in the `## Gate conventions` canon. The five existing prohibitions are all shape-based: they catch a malformed command. A gate can be well-formed, parse, honor exit status and carry a same-file positive control and STILL be unable to fail, because its floor is already cleared by test functions belonging to an EARLIER plan that share its selection pattern — the pattern matching none of this task's own tests then costs nothing. The sixth prohibition bans exactly that, with two checkable requirements: (a) OWNERSHIP — every gate carries at least one leg scoped to test functions the task itself declares, with a floor derived from that leg alone, and any pattern that also matches an earlier plan's tests must say so and say why those tests are the intended subject; (b) HOMING — every behavior bullet is named by, or names, a test function or subtest the task's own floor derivation counts, or the derivation states in writing that it is covered by a named criterion or deliberately out of scope. Unlike the five shape prohibitions it is not satisfiable by rewriting a command; it requires a structural leg and a written mapping. Every floor derivation in this plan now carries its ownership statement, and every task with a behavior block carries its bullet-home mapping. |
| Cycle-3: terminate the vacuity class rather than fixing instances | — | **Incorporated phase-wide.** Five new descriptor-less entries in `must_haves.prohibitions`, identical in all eleven plans, forbid: a `git status --porcelain` check without a `test -d` existence guard; a positive control that targets a different file than the assertion; a count-matching regex that accepts zero; an unanchored identifier grep; and an unranged `git diff` emptiness assertion. Each is a shape cycle 3 found in this phase's own gates, so a future author cannot reintroduce one without violating a stated prohibition. |
| Cycle-7 H-7-1: the sixth prohibition's two halves do not entail its own headline — OWNERSHIP constrains who DECLARES the counted tests, not what those tests ASSERT, and HOMING accepts a named criterion that no gate executes | HIGH (phase-wide) | **Incorporated phase-wide by AMENDING the sixth prohibition, not replacing it.** The reviewer and Codex independently constructed the same counterexample: a task declaring one self-contained arithmetic test, a leg selecting only that test at a floor of 1, and every behavior bullet homed to a prose criterion, satisfies both halves literally while the deliverable is entirely absent. The prohibition was STRUCTURAL where it had to be CAUSAL. Half (a) is now OWNERSHIP AND CAUSATION: a qualifying leg must turn RED when the deliverable is REPLACED BY A NO-OP — an implementation that still compiles and keeps the same signature but does nothing and returns zero values, or for a generated or declarative artifact its empty or default-valued version. Replacement, not deletion, is the standard, because deletion is satisfied by a mere compile dependency and only replacement separates a test that ASSERTS an artifact's behavior from one that merely MENTIONS it. Three forms are named, and every floor derivation in this plan now states which one it uses: A1 own-test, which must name its artifact under test; A2 direct-artifact, which EXECUTES the deliverable and scores a number read out of that artifact's own output; and A3 observation, open only to a task that declares no test function at all, which must carry a named measurement, a written STOP CONDITION for a vacuous measurement, the strongest executable leg available to it, and a plain statement of what that leg cannot catch. A leg whose pattern also matches an earlier plan's tests must additionally show that the inherited count alone falls BELOW the floor, or it does not qualify. Half (b) now requires a criterion's command to appear in the task's `<automated>` gate before that criterion counts as a home — prose is not a home. The five shape prohibitions from cycle 4 are untouched; the amended sixth is byte-identical across all eleven plans and the `## Gate conventions` canon restates it once, still one digest across all copies. |
| Cycle-7 M-7-1 (applied here as well as in `01-05`): Task 2 declares no test function, so half (a) as written did not permit its gate either | MEDIUM (found by re-audit) | **Incorporated.** The cycle-7 review named only `01-05`, but re-auditing every task against the strictly-stronger amended rule found the same shape here and in `01-11` Task 3. Task 2's ownership bullet is restated as form A3 and discharges its four requirements: the named measurements are `task proto:drift`'s non-zero exit, its printed compared-count and the specific file it names as drifted; the STOP CONDITION — new, and added by this pass — is that a run exiting 0 against a confirmed-applied stale file means the guard cannot see that surface and the guard is fixed rather than the evidence weakened; the strongest executable leg is form A2, executing `task proto:drift` and scoring `NFILES` out of its own output against a floor of 3, which a no-op replacement drives to 0; and the honest limit is written out rather than glossed. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.proto` source → committed generated Go | Generated code is compiled into the shipped binary. A stale or hand-edited generated file is executable code that no longer matches its declared contract. |
| drift guard → the developer's working tree | A guard that regenerates in place can destroy uncommitted work while trying to report on it. The temp-tree design makes the guard read-only with respect to the source tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | `internal/schema/graph.pb.go`, `internal/uiproto/uiv1/*.pb.go`, `uiv1connect/*.connect.go` | high | mitigate | `proto:drift` regenerates both surfaces with the pinned toolchain into a temporary tree, reports a positive count of files compared, fails on a zero count, and fails on any byte difference including a header-only one. Demonstrated RED independently against each surface and against a header-only staleness, with all output recorded. |
| T-01-32 | Denial of Service | the guard overwriting uncommitted generated-file edits | medium | mitigate | Generation targets a temp root that is removed on exit including on failure; the committed tree is only read. Asserted structurally by a Taskfile shape test and demonstrated once by hand with a local edit shown surviving a guard run. |
| T-01-33 | Tampering | a toolchain downgrade presenting as no drift | medium | mitigate | The generated header is compared rather than normalised away, so a change in generator identity surfaces as a failure. The guard cannot attribute the cause and is not asked to; the flagged assumption records that limit explicitly. |
</threat_model>

<verification>
- `task proto:drift` exits 0 on a clean tree AND its printed compared-count, captured as a number, is at least 3 — asserted numerically, never with a `[0-9]+` presence regex that `compared 0 generated files` would satisfy.
- `go test -v -count=1 -run 'TestProto' ./internal/upgrade/` emits at least 4 `--- PASS` lines AND exits 0 — the conjunction, never either alone. This bullet previously stated the floor with no exit-status clause, which is exactly the count-only shape this plan's own prohibition forbids; the matching `<automated>` gate was already compliant, so this was a documentation inconsistency rather than an executable defect, and it is now consistent.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** Four test functions match `TestProto` and none declares subtests: `TestProtoTasksExist`, `TestProtoDriftGeneratesIntoATemporaryTree`, `TestProtoDriftGuardReportsAComparedCount`, `TestProtoDriftTaskIsInvokedByCI`. Total **4** against a floor of 4: zero headroom by design. **Ownership:** every atom in this gate's `-run` pattern names a test function THIS task declares; no test from any other plan matches any atom, so the whole count is this task's own deliverable and the floor goes unmet if that deliverable is absent. **Forms A1 and A2, both present in this one gate, with causation.** A1: the artifacts under test are the `proto:gen` and `proto:drift` pair this task adds to `Taskfile.yml` and the invocation it adds to `.github/workflows/ci.yml`, both named in this task's files list. For a declarative artifact the no-op is the empty or default-valued version — an empty `proto:drift` body — and `TestProtoDriftGeneratesIntoATemporaryTree` and `TestProtoDriftGuardReportsAComparedCount` both turn RED on the behavior they assert. `TestProtoTasksExist` alone WOULD survive that replacement, which is precisely why it is not the test this leg rests on. A2: the same gate additionally EXECUTES `task proto:drift` — this task's own deliverable — and scores `NFILES` read out of that task's OWN output against a floor of 3; a no-op replacement prints no compared-count, `NFILES` defaults to 0 and the leg turns RED.
- `TestWorkflowRunBodiesInvokeTask` passes with the new CI step present.
- `task test:unit` passes.
- `go vet ./internal/upgrade/...` is clean.
- `git status --porcelain internal/schema/ internal/uiproto/` is empty — run behind the `test -d internal/schema && test -d internal/uiproto &&` existence guard, without which the check passes vacuously on a missing or typo'd path.
</verification>

<success_criteria>
- Regenerating both protobuf surfaces produces byte-identical output to the committed files, verified from a temporary tree that cannot clobber the source tree.
- The guard reports how many generated files it compared and fails on a zero or short count.
- It has been watched fail against a deliberately stale checked-in file for each surface independently, and against a header-only staleness, with every failure recorded.
- The guard runs in CI as a Taskfile-defined job body.
</success_criteria>

<output>
Create `.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-07-SUMMARY.md` when done
</output>
