---
phase: 01-engine-seam-wire-protocol-secure-transport
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/mcp/server.go
  - internal/mcp/pending_writer_test.go
autonomous: true
requirements: [FIX-01]

estimate:
  tokens: 63000
  raw_tokens: 63000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A server-initiated notification written to stdout does not decrement the pending-response counter, because only client-initiated calls ever increment it."
    - "Only bytes the underlying writer reports as actually written are classified: a short write returning `n < len(b)` decrements for the responses inside `b[:n]` and for nothing beyond it, so a response that never reached stdout is never counted as delivered."
    - "`pendingWriter.Write` is ONE serialized critical section: a single mutex covers the underlying write AND the classification of `b[:n]`. \"Forward first\" means the underlying write happens before any interpretation of the bytes WITHIN that section — not that it happens outside synchronization. `io.Writer` promises nothing about concurrent calls, so a buffer-only mutex would let two concurrent writes reach the wire in one order and the classification buffer in another (FIX-01)."
    - "`pendingWriter` forwards bytes to the underlying writer BEFORE classifying and never delays a byte; the line buffer holds a COPY of `b[:n]` used solely for classification, because `pendingWriter` IS the protocol writer."
    - "Under concurrent `Write` calls with `-race`, the classification buffer's line order equals the underlying writer's byte order, and no line is reassembled from two different writes' fragments. Deliberately NOT tagged as a new edge probe: it restates, at the correct scope, the concurrency property this plan's never-goes-negative truth already carries under `-race`. The phase's probe ledger is unchanged at 21 — 15 edge-tagged `must_haves` (including 01-01's `verification: backstop`) plus 6 flagged assumptions (FIX-01)."
    - "The pending-response counter never goes negative, under any interleaving of client calls, server notifications and responses, verified under `-race`; and an attempted decrement below zero is counted in a test-visible underflow counter rather than silently absorbed."
    - "A reproduction that demonstrably showed the pending count reaching zero while a response was still owed — and `waitForDrain` therefore returning early — is committed and recorded, and the same reproduction passes after the fix (FIX-01, success criterion 5)."
    - "The fix is confined to `pendingWriter`'s own method set: the `Server` interface, `BuildServer`, `ServeStdio`'s signature and `internal/cli/serve.go` are all unchanged, and no wire-visible response content changes."
    - "Closing FIX-01 is proven NOT to close the `toolslist-repeat` ordering flake: a session with zero notification traffic still answers two pipelined `tools/list` calls in an order the protocol does not constrain, and the test matches them by JSON-RPC id."
  artifacts:
    - internal/mcp/pending_writer_test.go
  key_links:
    - "`stdinLingerReader.Read` increments via `looksLikeJSONRPCCall` (method AND id present). `pendingWriter.Write` must decrement on the symmetric outbound classification — a response carries an id and no method — or the two sides never balance."
    - "A single `Write` call may carry more than one newline-delimited message; the decrement must be per complete response line, not per call."
    - "`io.Writer.Write` may return `n < len(b)` together with an error. The classifier's input is `b[:n]`, never `b`."
    - "The mutex must wrap `p.w.Write(b)` itself, not just the buffer append. Locking only the buffer is the shape that looks correct and diverges under concurrency."
  prohibitions:
    - "Never gate a task on a bare `go test ... | rg '--- PASS' | wc -l` count whose exit status is discarded. A shell pipeline's status is the LAST command's, so N passing subtests plus one `--- FAIL` satisfies a count-only gate. Every automated gate captures `go test`'s output and status BEFORE any pipe and requires BOTH: status 0 AND the `--- PASS` floor. The count alone is vacuous when a test fails; the status alone is vacuous when the `-run` pattern matches nothing. The sole scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence."
    - "Never report FIX-01's closure as closing the `toolslist-repeat` ordering flake. They are different defects with different root causes, and conflating them retires a live finding by assertion."
    - "Never write `test -z \"$(git status --porcelain <path>)\"` without a preceding `test -d <path> &&` existence guard. `git status --porcelain` on a missing or typo'd pathspec exits 0 with EMPTY stdout — the 'could not open directory' warning goes to stderr — so the check passes vacuously against a path that was never opened."
    - "Never positive-control a zero-count gate against a DIFFERENT file than the one the gate guards. `rg` prints nothing for a nonexistent path just as it does for a clean file, so a typo'd or renamed target reads `0` while the guarded file is never opened. The control must run the identical pipeline against the SAME file with a pattern that file genuinely contains."
    - "Never score a count with a regex that accepts zero. `rg -q -e 'compared [0-9]+ generated files'` matches `compared 0 generated files`, so it is a presence check on the count and not a floor. Capture the number and assert the floor numerically."
    - "Never write an unanchored identifier grep as a forbidden-pattern gate. Anchor it to the declaration form — `func gather[A-Z]`, never `\\bgather[A-Z]` — because a trailing comment or a `/* */` block survives `rg -v '^\\s*//'` and turns the gate red, or green, for the wrong reason."
    - "Never assert emptiness with an unranged `git diff`. Under atomic-commit-per-task the working tree is clean at verification time, so `git diff --stat <path>` is empty by construction whether or not the file was edited. Assert over an explicit `<phase-base>..HEAD` revision range."
---

<objective>
Fix `internal/mcp/server.go`'s `pendingWriter` counter corruption (CR-01, scoped here as
FIX-01), and separately prove that fixing it does not close the `toolslist-repeat`
ordering flake.

Purpose: `pendingWriter.Write` calls `p.pending.Add(-1)` on EVERY write, while only
client-initiated request lines increment it inside `stdinLingerReader.Read`. Server-initiated
notifications — `notifications/tools/list_changed`,
`notifications/subscriptions/acknowledged` — go through the identical writer, so one
landing between a request's acceptance and its response being written drives the counter
to zero early and shortens `waitForDrain`'s effective wait. Confirmed reachable, not
theoretical.

This lands in Phase 1 deliberately out of dependency order: its root cause —
server-initiated writes decrementing a counter only client-initiated requests increment —
is exactly the shape Phase 6's ConnectRPC server-streaming lifecycle can reinvent, so
fixing it first makes it a worked example that phase's design review checks against.

Output: a balanced counter, a committed reproduction, and a recorded disproof of the
folded-todo hypothesis.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md
</context>

## Artifacts this phase produces

**Created by this plan (01-03):**

- `internal/mcp/pending_writer_test.go` — new file
- `internal/mcp` unexported `looksLikeJSONRPCResponse` (the outbound classifier symmetric to the existing `looksLikeJSONRPCCall`)
- `internal/mcp` unexported `sniffedOutbound` struct, if the outbound sniff needs a distinct shape from `sniffedMessage`
- `internal/mcp` unexported `decrementPending(p *atomic.Int64) bool` and the `pendingUnderflows` test-visible counter
- `internal/mcp` `pendingWriter`'s classification line buffer and its mutex
- `TestPendingWriterDecrementsOnlyResponses`
- `TestPendingWriterClassifiesOnlyBytesActuallyWritten`
- `TestPendingWriterForwardsBeforeClassifying`
- `TestPendingWriterSerializesWriteAndClassification`
- `TestPendingWriterDecrementsPerLineAcrossWriteBoundaries`
- `TestPendingWriterNeverGoesNegative`
- `TestPendingWriterDrainsEarlyWithoutFix` (the committed reproduction)
- `TestPipelinedToolsListResponsesAreMatchedByIDWithoutNotifications` (the separability disproof)

**Created elsewhere in this phase** (listed so drift verification sees the whole set):
`internal/goldenspec` (`PerCorpusArgs`, `LockedCorpusArgs`, `LanguageToLockedSlug`,
`SlugToRepo`, `GoldenCapture`, `CallExploreViaMCP`, `CallNodeViaMCP`,
`CallNodeViaMCPWithArgs`, `MCPResultText`); `testdata/golden/byte_identity_test.go`;
`internal/uiproto/uiv1` (package, `ui.proto`, `UIService` and its nine rpcs, their
request/response messages, the shared `Node`/`Location`/`FileEntry`/`FileTreeNode`/
`ExploreGroup`/`BlastEntry`/`SourceBlob`/`IndexingInProgress`/`NodeDetailMode` wire types,
generated `ui.pb.go` and `uiv1connect/ui.connect.go`); `internal/uiserver` (`Options`,
`DefaultAddr`, `Server`, `Listen`, `Serve`, `URL`, `Close`, `originHostGuard`,
`allowedHosts`, `allowedOrigins`, `uiService`, `openEngine`, `withEngine`,
`mapEngineError`, the nine mappers, `truncateSource`, `sourceLineCap`, `sourceByteCap`,
`transportSendMaxBytes`, `transportReadMaxBytes`, `errIndexingInProgress`,
`indexingInProgressMessage`, `degradedStatus`, `classifyDegrade`); `internal/cli/ui.go`
(`newUiCmd`, `shouldOpenBrowser`); `internal/textutil.TruncateOnRuneBoundary`;
`internal/query`'s `NodeDetail` / `NodeDetailMode` / `FileDetail` / `DefinitionDetail` /
`MultiDefDetail` / `(*Engine).NodeDetail` / `ExploreResult` / `(*Engine).ExploreDetail` /
`ExploreFileGroup` / `ExploreBlast` / `ErrNotFound` / `ErrInvalidArgument` /
`(*Engine).IndexMeta` / `(*Engine).SourceFor` and the `build*Detail` gathers;
`schema.Meta.commit_sha` field 8 and
`GetCommitSha`; `schema.IndexedCommitSHA`; `internal/indexer/commit.go`'s
`resolveHeadCommitSHA` and `gitExecLookPath`; `buf.yaml`, `buf.gen.yaml`,
`go.tool-proto.mod`, `Taskfile.yml` tasks `proto:gen` and `proto:drift`,
`internal/upgrade/proto_task_test.go`.

## Gate conventions (phase-wide canon — byte-identical in all eleven plans, and checkable)

This section is replicated verbatim across `01-01-PLAN.md` … `01-11-PLAN.md`. The claim is
checkable rather than merely asserted — one command, run from the phase directory, prints
**one** distinct digest if the eleven copies agree and more than one if any has drifted:

```
for f in 01-*-PLAN.md; do sed -n '/^## Gate conventions/,/^<tasks>$/p' "$f" | shasum; done | sort -u | wc -l
```

A future planner editing one copy must edit all eleven, and this is how a reviewer catches it
if they do not. (Nothing at execution time changes this section, so no `<automated>` gate owns
it — the drift risk is at planning time, which is where the check belongs.)

**`--- PASS` counting.** `go test -v` prints one `--- PASS:` line for the parent test
function AND one for every `t.Run` subtest, including nested ones. Every PASS floor in this
phase counts BOTH: a test function with three subtests contributes FOUR lines, so
`rg -o -e '--- PASS' | wc -l` over `go test -v` output equals
`len(test functions) + len(all subtests)`. Derive every floor that way and show the
arithmetic in the acceptance criterion that states it, so a reviewer can check the floor
instead of re-deriving it.

**Echo label.** Every gate prints `PASS lines:` — never `PASS tests:`, never
`PASS subtests:`. The label names what is counted, and what is counted is lines.

**The conjunction (D-04 as corrected in `01-CONTEXT.md`).** A gate passes only when the
test command's exit status is 0 AND the `--- PASS` floor is met. Neither is sufficient
alone: `go test -run PATTERN` exits 0 when the pattern matches nothing, and a shell
pipeline's `$?` is the LAST command's, so a bare count passes with N passing subtests plus
one `--- FAIL`. The single scoped exception is a deliberate-mutation RED observation, where
a non-zero exit IS the expected evidence and the observation is scored by `--- FAIL` /
`--- PASS` counts rather than by status.

**The five vacuity shapes cycle 3 found in this phase's own gates.** Each is a prohibition
in `must_haves.prohibitions`. They are restated here together because they are one class —
a guard that reads green against a file it never opened:

1. `git status --porcelain <path>` exits 0 with empty stdout on a MISSING or typo'd path
   (the warning goes to stderr). Always prefix `test -d <path> &&`.
2. A zero-count gate positive-controlled against a DIFFERENT file proves nothing about the
   guarded file: `rg` prints nothing for a nonexistent path too. Control on the SAME file.
3. `rg -q 'compared [0-9]+ generated files'` accepts `compared 0 generated files`. Capture
   the number; assert the floor numerically.
4. An unanchored identifier grep (`\bgather[A-Z]`) is satisfiable by a trailing or `/* */`
   comment that `rg -v '^\s*//'` does not strip. Anchor to the declaration form.
5. An unranged `git diff` is empty by construction at verification time under
   atomic-commit-per-task. Assert over `<phase-base>..HEAD`.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Reproduce the premature drain, then balance the counter</name>
  <files>internal/mcp/pending_writer_test.go, internal/mcp/server.go</files>
  <read_first>
    - internal/mcp/server.go lines 200-350 — `ServeStdio`'s doc comment explaining the invariant and why the increment must happen inside `Read`, `stdinLingerGrace`, `stdinLingerPollInterval`, `stdinLingerReader.Read` and its `unread` line-buffering shape, `waitForDrain`, `sniffedMessage`, `looksLikeJSONRPCCall`, and `pendingWriter.Write`
    - internal/mcp/session_line_test.go lines 60-90 and 230-260 — the table-driven `t.Run` idiom and the got/want-plus-why failure-message style
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md — the `internal/mcp/server.go` FIX-01 section, including the "confined to this method" constraint the file's own doc comment already states
    - .planning/STATE.md — the Blockers/Concerns entry describing the confirmed trace, and the standing rule that a gate is not trusted until demonstrated RED
  </read_first>
  <behavior>
    - `TestPendingWriterDrainsEarlyWithoutFix`: with the counter at 1 (one accepted client call in flight), a server-initiated notification line is written through `pendingWriter`; before the fix the counter reads 0 and `waitForDrain` returns immediately, so the reader stops waiting while a response is still owed. After the fix the counter still reads 1 and `waitForDrain` blocks until the response write lands.
    - `TestPendingWriterDecrementsOnlyResponses`: a table driven with `t.Run`, one subtest per case, over EXACTLY SEVEN outbound line shapes — a response with `id` and `result`; a response with `id` and `error`; a notification with `method` and no `id`; a server-initiated request with both `method` and `id`; a message with `id: null` and no `method` (JSON-RPC's parse-error response shape, which is not a reply to any pending call); an unparseable line; an empty line — asserting the counter delta each produces. Seven `t.Run` cases, not seven sequential assertions in one body: the PASS floor counts subtest lines and this test contributes 1 + 7 = 8 of them.
    - `TestPendingWriterClassifiesOnlyBytesActuallyWritten`: EXACTLY TWO `t.Run` subtests. `short-write` — an underlying writer that accepts only the first N bytes of a two-response buffer and returns an error decrements by exactly 1, not 2. `zero-bytes-written` — a writer that accepts zero bytes and errors decrements by 0. Two `t.Run` cases, contributing 1 + 2 = 3 PASS lines.
    - `TestPendingWriterDecrementsPerLineAcrossWriteBoundaries`: EXACTLY TWO `t.Run` subtests owning the two write-boundary cases below, which previously belonged to no test function at all. `two-responses-one-write` — a single `Write` carrying two newline-delimited responses decrements by exactly 2. `one-response-split-across-two-writes` — the first `Write` ends mid-line and decrements by 0; the second `Write` delivers the terminating newline and decrements by exactly 1, so the split response is counted once and neither twice nor zero times. Contributes 1 + 2 = 3 PASS lines.
    - `TestPendingWriterForwardsBeforeClassifying`: an underlying writer that records the exact byte sequence it received sees every byte of every `Write` in order, with no byte held back — including a `Write` ending mid-line. The classification buffer must not become a forwarding buffer.
    - `TestPendingWriterSerializesWriteAndClassification`: concurrent `Write` calls under `-race` against an instrumented underlying writer that records entry order and the exact bytes each call delivered. The classification buffer's line order equals the underlying writer's byte order for every observation, and no classified line is composed of fragments from two different `Write` calls. A buffer-only mutex — write outside the lock, append inside it — fails this test; a single critical section over both passes it.
    - `TestPendingWriterNeverGoesNegative`: an interleaving that writes more responses than there were accepted calls leaves the counter at zero or above, never negative, under `-race`, and the underflow counter records every attempted decrement below zero.
  </behavior>
  <action>
    Write `internal/mcp/pending_writer_test.go` FIRST and commit it RED. The reproduction
    is not optional colour — ROADMAP success criterion 5 requires the fix be "proven
    against a reproduction that showed the loss", so the RED run must be observed and its
    output recorded in the SUMMARY. Drive it at the `pendingWriter`/`stdinLingerReader`
    level rather than through a full `go-sdk` session: the counter and the drain loop are
    both reachable directly with an `atomic.Int64`, a `bytes.Buffer` as the underlying
    writer, and an `io.Pipe` for the reader side. That keeps the reproduction fast,
    deterministic and free of the async dispatch that Task 2 shows is a separate concern.

    **Name the evidence precisely.** What this reproduction demonstrates at the primitive
    level is a PREMATURE DRAIN: the pending count reaches zero while a response is still
    owed, so `waitForDrain` stops waiting and the reader propagates EOF. Whether a
    particular in-flight response is then actually abandoned depends on the surrounding
    go-sdk session and is not what this test observes. Word the reproduction's name, its
    doc comment, its failure messages and the SUMMARY entry as premature drain — a claim
    about what was observed. A full-stdio test confirming an abandoned response would be
    stronger evidence; it is not in scope here and must not be implied.

    Then fix `internal/mcp/server.go`. Add `looksLikeJSONRPCResponse(line []byte) bool` as
    the outbound classifier symmetric to the existing inbound `looksLikeJSONRPCCall`,
    documented the same way: a response carries an `id` and no `method`; a notification
    carries a `method` and no `id`; a server-initiated request carries both. Reuse or
    extend `sniffedMessage` rather than introducing a second JSON shape, keeping the
    "extract just enough to classify, never the full message" discipline the existing
    sniff states. Choose the conservative default deliberately and document it: an
    unparseable outbound line should NOT decrement, because over-decrementing is the exact
    defect being fixed, whereas under-decrementing costs at most a bounded wait against
    `stdinLingerGrace` — the same asymmetry the inbound sniff already reasons about in the
    opposite direction. Treat a `null` id the same as a response for classification
    purposes only if `looksLikeJSONRPCCall`'s inbound side would have counted it; state the
    chosen reading in the doc comment either way and cover it in the table.

    Rework `pendingWriter.Write` as ONE serialized critical section, in this exact order,
    and say so in a comment because both the serialization and the order are the
    correctness argument:

    0. `p.mu.Lock(); defer p.mu.Unlock()` as the FIRST statement — the mutex covers the
       underlying write AND the classification, not the classification alone. `io.Writer`
       promises nothing about concurrent calls, so a mutex taken only around the buffer
       would let two concurrent `Write`s reach the wire in one order and the classification
       buffer in another: the copied line stream would stop matching wire order and a split
       response could be reassembled across an interleaving write. "Forward first" means
       forward before interpreting the bytes *within* this serialized section — it does NOT
       mean write outside synchronization. Serializing the underlying write also gives
       `pendingWriter` the concurrency contract the go-sdk's writer needs but `io.Writer`
       does not promise.
    1. `n, err := p.w.Write(b)` — forward FIRST inside the lock. `pendingWriter` IS the
       protocol writer; it must never hold a byte back waiting for a newline. Nothing below
       may delay, reorder or re-chunk what reaches stdout, and no classification work
       happens before this call returns.
    2. Append a COPY of `b[:n]` — and only `b[:n]` — to the internal classification buffer.
       `io.Writer.Write` may return `n < len(b)` together with an error; classifying all of
       `b` would decrement for a response that never reached stdout, which breaks the
       authoritative-wire invariant this file's own doc comment states.
    3. Split the classification buffer on `\n`, classify each COMPLETE line, and retain any
       trailing partial line in the buffer for the next call. A single `Write` carrying two
       responses decrements twice; a `Write` carrying half a response decrements zero times
       and the remainder decrements once when its newline arrives.
    4. `return n, err` unchanged.

    The mutex is held across the underlying write, which means a slow downstream write
    blocks a concurrent one. That is the intended trade and it is the same serialization a
    line-oriented protocol writer needs anyway: two concurrent unserialized writes to one
    stdio stream can already interleave into a corrupt line. State that trade in the
    comment so a later reader does not "optimize" the lock down to the buffer and silently
    reintroduce the divergence.

    Replace the bare `Add(-1)` with `decrementPending(p *atomic.Int64) bool`: a
    compare-and-swap loop that refuses to go below zero and RETURNS whether a decrement
    actually happened. A clamp that silently absorbs an unowed decrement hides exactly the
    invariant violation this fix exists to detect, so a refused decrement increments a
    package-level `pendingUnderflows atomic.Int64` that the tests read. Document that
    production behavior is unchanged by the counter — it exists so a future regression
    shows up as a number rather than as a mysterious early EOF.

    Keep the change confined. `ServeStdio`'s own doc comment already states the invariant
    is "confined entirely to this method — no change to the `Server` interface, to
    `BuildServer`, to `internal/cli/serve.go`, or to any wire-visible response content",
    and that must remain true. Update that doc comment to record the corrected invariant:
    the increment counts accepted client calls, the decrement counts complete response
    lines whose bytes the underlying writer reported as written, and server-initiated
    notifications participate in neither.
  </action>
  <verify>
    <automated>{ OUT=$(go test -v -count=1 -race -run 'TestPendingWriter' ./internal/mcp/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 15 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -race -run 'TestPendingWriter' ./internal/mcp/` emits at least 15 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - The floor of 15 is DERIVABLE from this plan and is shown here so a reviewer checks the arithmetic instead of re-deriving it. Under the phase-wide counting convention (one `--- PASS` line for the parent test function plus one for every `t.Run` subtest), the `-run 'TestPendingWriter'` pattern matches seven test functions, of which three declare subtests: `TestPendingWriterDecrementsOnlyResponses` 1 + 7 = 8, `TestPendingWriterClassifiesOnlyBytesActuallyWritten` 1 + 2 = 3, `TestPendingWriterDecrementsPerLineAcrossWriteBoundaries` 1 + 2 = 3, plus `TestPendingWriterForwardsBeforeClassifying`, `TestPendingWriterSerializesWriteAndClassification`, `TestPendingWriterNeverGoesNegative` and `TestPendingWriterDrainsEarlyWithoutFix` at 1 each = 4. Total **18**, against a floor of 15 — three lines of headroom. The subtest structure is therefore not a stylistic choice: writing any of the three tables as sequential assertions in one body would drop the total below the floor and the gate could not pass.
    - The SUMMARY records the RED run's output for `TestPendingWriterDrainsEarlyWithoutFix` before the fix — the observed counter value and the observed early `waitForDrain` return — and the same test's PASS afterwards. The SUMMARY describes the observed behavior as a premature drain, not as a proven abandoned response.
    - `TestPendingWriterDecrementsOnlyResponses` covers all six outbound line shapes listed in the behavior block plus the `id: null` case, and asserts the counter delta for each.
    - `TestPendingWriterClassifiesOnlyBytesActuallyWritten` asserts a short write of a two-response buffer decrements by exactly 1, and a zero-byte erroring write decrements by 0.
    - `TestPendingWriterForwardsBeforeClassifying` asserts the underlying writer received every byte of every `Write` in order, including a `Write` that ends mid-line, so no byte was buffered before forwarding.
    - `TestPendingWriterSerializesWriteAndClassification` runs concurrent `Write` calls under `-race` against an instrumented underlying writer that records the order in which it was entered, and asserts the classification buffer's line order equals the underlying writer's byte order for every observation — the property a buffer-only mutex does not provide. It also asserts no line was ever reassembled across two different writers' fragments.
    - The mutex covers the underlying write: `rg -v '^\s*//' internal/mcp/server.go` shows the `Lock` call preceding the `p.w.Write` call inside `pendingWriter.Write`, asserted by a source-order check in the test file rather than by eye, positive-controlled by confirming both anchors were found before the ordering is compared.
    - `TestPendingWriterNeverGoesNegative` passes under `-race` with more response writes than accepted calls, asserts the counter is greater than or equal to zero at every observation point, and asserts `pendingUnderflows` recorded the expected number of refused decrements.
    - A `Write` carrying two newline-delimited responses is asserted to decrement by exactly 2, and a `Write` carrying a split response is asserted to decrement exactly once across the two calls.
    - `git diff --stat` for this task touches exactly `internal/mcp/server.go` and `internal/mcp/pending_writer_test.go`. The `Server` interface, `BuildServer` and `internal/cli/serve.go` are untouched.
    - `task test:unit` and the wire oracle suite both still pass, so no wire-visible response content changed.
  </acceptance_criteria>
  <done>The pending-response counter counts only what it was meant to count, over only the bytes that actually reached the wire, the premature drain is reproduced and then fixed, and the change is confined to the writer.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the `toolslist-repeat` flake is a different defect and is NOT closed here</name>
  <files>internal/mcp/pending_writer_test.go</files>
  <read_first>
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md — "Folded Todos" item 1 and its ⚠ HYPOTHESIS FALSIFIED BY RESEARCH block
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md — "Pitfall 2: FIX-01 and the toolslist-repeat flake are NOT the same bug", including the `go-sdk@v1.7.0` `mcp/server.go:1910-1916` `jsonrpc2.Async(ctx)` citation and the `internal/jsonrpc2/conn.go` `handleAsync` dequeue behavior
    - test/wireoracle/scenarios.go — the `toolslist-repeat` scenario, which sends two plain `tools/list` calls with no notification traffic at all
    - .planning/todos/pending/2026-08-07-wire-oracle-toolslist-repeat-response-ordering-flake.md — the two recorded sightings and the observed `"id":3` vs `"id":2` arrival diff
    - .planning/STATE.md — the standing rule that a transcript grep is a claim about the grep, not about the product
  </read_first>
  <action>
    The `toolslist-repeat` flake was folded into this phase on the hypothesis that it
    shared FIX-01's root cause, with an explicit obligation to prove it either way.
    Research discharged that obligation as a DISPROOF by reading
    `github.com/modelcontextprotocol/go-sdk@v1.7.0` in the local module cache: every
    JSON-RPC call except `initialize` has `jsonrpc2.Async(ctx)` invoked on it, so two
    pipelined `tools/list` calls run in separate goroutines with no response-ordering
    guarantee. That is a different defect from a counter decrementing on writes it never
    counted. This task makes the disproof executable rather than only written down.

    Add `TestPipelinedToolsListResponsesAreMatchedByIDWithoutNotifications` to the same
    test file. The name states what the test proves, which is all it proves: with zero
    notification traffic present, both pipelined responses arrive and are matched by
    JSON-RPC `id`. It does NOT demonstrate nondeterministic ordering — deliberately not
    asserting order is not the same as observing reordering — and the doc comment must say
    so in those words. What it establishes is separability by construction: the ordering
    behavior is exercised in a session where FIX-01's cause cannot be present, so FIX-01
    cannot be the cause.

    Drive two pipelined `tools/list` calls against a real `BuildServer` over an
    `io.Pipe`-backed `mcp.IOTransport`, collect the responses, and assert both ids are
    answered exactly once. Give the test a doc comment stating in full: that arrival order
    for pipelined calls is not a protocol guarantee, that go-sdk's documented async
    dispatch is the mechanism, citing `go-sdk#26`, and that this test exists to keep the
    `toolslist-repeat` finding separable from FIX-01 rather than to fix it.

    Record the verdict explicitly in the SUMMARY: FIX-01 is closed; the `toolslist-repeat`
    flake is NOT closed by it and remains an open finding whose correct fix is at the
    wire-oracle harness level — the harness freezes an arrival order the protocol never
    promised, so it should match responses by `id` rather than by position. That harness
    change carries no requirement ID in this milestone and is deliberately out of this
    phase's scope; it stays in its todo. Do not mark the todo resolved, and do not let the
    phase's completion report imply it was.
  </action>
  <verify>
    <automated>{ OUT=$(go test -v -count=1 -race -run 'TestPipelinedToolsListResponsesAreMatchedByIDWithoutNotifications' ./internal/mcp/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 1 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -race -run 'TestPipelinedToolsListResponsesAreMatchedByIDWithoutNotifications' ./internal/mcp/` emits at least 1 `--- PASS` line, and the command exits 0 — both conditions, never either alone.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** One test function matches and it declares no subtests: `TestPipelinedToolsListResponsesAreMatchedByIDWithoutNotifications`. Total **1** against a floor of 1 — the count proves the `-run` pattern matched something; `STATUS -eq 0` carries the rest.
    - The test's session sends exactly zero notifications, asserted positively by counting the outbound lines the test itself writes and asserting that count against the number of calls it made.
    - The test matches responses by JSON-RPC `id` and asserts each id is answered exactly once; it makes no assertion about arrival order, and its doc comment states explicitly that not asserting order is not a demonstration of reordering.
    - The test's doc comment cites `go-sdk@v1.7.0`'s `jsonrpc2.Async` dispatch and upstream issue `go-sdk#26` as the mechanism.
    - The SUMMARY states, in its own words, that FIX-01 is closed and that the `toolslist-repeat` flake is not, and names the wire-oracle harness as the correct locus for that separate fix.
    - `.planning/todos/pending/2026-08-07-wire-oracle-toolslist-repeat-response-ordering-flake.md` is still present and still in `pending/` after this plan completes.
  </acceptance_criteria>
  <done>The folded hypothesis is discharged as a disproof with an executable test behind it, named for what it actually proves, and the flake remains an open, correctly-attributed finding.</done>
</task>

</tasks>

## Review feedback dispositions

| Finding | Severity | Disposition |
|---|---|---|
| Cycle-2 LOW: cross-plan artifact manifest drift — `(*Engine).SourceFor` missing from some "Created elsewhere in this phase" manifests | LOW | **Incorporated, and widened beyond what was flagged.** The review named 01-02 and 01-05. Auditing all eleven manifests found the same omission in 01-01, 01-03, 01-06 and 01-07 as well. `(*Engine).SourceFor` (declared by 01-04) is now named in every plan's manifest, so drift verification sees the whole phase-wide new-symbol set from any plan it starts at. |
| Cycle-2 MEDIUM: forwarding happens outside the classification mutex | MEDIUM | **Incorporated.** `pendingWriter.Write` is now ONE serialized critical section — the mutex is the first statement and covers `p.w.Write(b)` as well as the `b[:n]` classification. "Forward first" is restated as first *within* the serialized section, not before it. `TestPendingWriterSerializesWriteAndClassification` asserts, under `-race` against an instrumented writer, that classification order equals wire order and that no line is reassembled from two writes' fragments — the property a buffer-only mutex does not provide. A source-order check asserts the `Lock` precedes the underlying write. The PASS floor rose from 14 to 15. The lock-across-write latency trade is documented at the call site so it is not later "optimized" back to the broken shape. |
| Cycle-2 HIGH C2-2: automated PASS-count gates discard the test command's exit status | HIGH | **Incorporated across every gate in this plan.** A shell pipeline's `$?` is the LAST command's, so `COUNT=$(go test ... | rg '--- PASS' | wc -l); [ "$COUNT" -ge N ]` passed with N passing subtests plus a `--- FAIL`. Every `<automated>` gate now captures `go test`'s output into `OUT` and its status into `STATUS` BEFORE any pipe, counts from `OUT`, dumps `OUT` on failure, and requires `STATUS -eq 0` AND the `--- PASS` floor. The conjunction is the rule: the count alone is vacuous when a test fails, the status alone is vacuous when `-run` matches nothing. A plan-level prohibition against the bare count-only form is in `must_haves.prohibitions`. The only scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence. |
| Outbound sniff must classify only `b[:n]` | HIGH | **Incorporated.** Step 2 of the rewritten `Write` copies only `b[:n]`; `TestPendingWriterClassifiesOnlyBytesActuallyWritten` asserts it for a short write and a zero-byte erroring write. |
| "Buffer partial lines" must not delay forwarding | HIGH | **Incorporated.** Forward-first is step 1 and the buffer is explicitly a classification-only copy; `TestPendingWriterForwardsBeforeClassifying` asserts byte-for-byte pass-through including a mid-line `Write`. |
| CAS clamp at zero hides invariant violations | MEDIUM | **Incorporated.** `decrementPending` returns whether a decrement happened and a refusal increments the test-visible `pendingUnderflows`. |
| Test name claims ordering nondeterminism it does not prove | MEDIUM | **Incorporated by rename.** `TestPipelinedToolsListResponsesAreMatchedByIDWithoutNotifications`, with a doc comment stating that not asserting order is not a demonstration of reordering. |
| "Lost response" reproduction actually demonstrates premature drain | LOW | **Incorporated.** Renamed `TestPendingWriterDrainsEarlyWithoutFix`; the action, criteria and SUMMARY wording all say premature drain, and explicitly disclaim the stronger claim. |
| Add cases for short writes, write errors, `id:null`, concurrent `Write` | — | **Incorporated.** All four are in the behavior block and the acceptance criteria; the classification buffer takes a mutex for the concurrent case. |
| Add a JSON-RPC batch-payload case | — | **Rejected with rationale.** The stdio transport this writer wraps is newline-delimited single messages; go-sdk's `IOTransport` does not emit array-framed batches on this path, so a batch case would test a shape the writer cannot receive. If a batch payload ever does reach this writer it will be an unparseable line under the classifier, which the conservative default already handles by not decrementing. |
| Cycle-3 M8: the PASS floor of 15 is not derivable, and behaviors `:136`/`:137` are orphaned | MEDIUM | **Incorporated.** The two orphaned write-boundary behaviors now belong to a named test, `TestPendingWriterDecrementsPerLineAcrossWriteBoundaries`, with exactly two `t.Run` subtests. The other two tables state their subtest structure explicitly: `TestPendingWriterDecrementsOnlyResponses` has exactly seven `t.Run` cases (an `id: null` case was added, making the classifier's coverage complete) and `TestPendingWriterClassifiesOnlyBytesActuallyWritten` exactly two. The floor of 15 is now derivable at **18** with the arithmetic written into the acceptance criteria, and the criterion states that writing any of the three tables as sequential assertions would drop the total below the floor. |
| Cycle-3 L4: the go-sdk `jsonrpc2.Async` citation reads `mcp/server.go:1908-1913`; the actual guarded call site is `:1910-1916` | LOW | **Incorporated.** Corrected to `1910-1916`, verified against the populated module cache for `go-sdk@v1.7.0`. |
| Cycle-3 L1: the `--- PASS` counting convention is never stated anywhere in the phase, and echo labels contradict each other | LOW | **Incorporated phase-wide.** A byte-identical `## Gate conventions (phase-wide canon)` section now sits immediately above `<tasks>` in ALL ELEVEN plans. It states the convention once and canonically — one `--- PASS` line per test function PLUS one per `t.Run` subtest — mandates the single echo label `PASS lines:`, restates the D-04 conjunction with its one scoped exception, and enumerates the five vacuity shapes cycle 3 found. Every floor in this plan now carries an explicit **Floor derivation** bullet showing the arithmetic, so a reviewer checks the derivation rather than re-deriving it. Label census: six distinct labels existed across the phase (`PASS subtests:`, `PASS tests:`, `PASS:`, `post-restore PASS lines:`, `PASS lines across both tests:`, `uiserver PASS lines:`); all six were normalised to `PASS lines:`. |
| Cycle-3: terminate the vacuity class rather than fixing instances | — | **Incorporated phase-wide.** Five new descriptor-less entries in `must_haves.prohibitions`, identical in all eleven plans, forbid: a `git status --porcelain` check without a `test -d` existence guard; a positive control that targets a different file than the assertion; a count-matching regex that accepts zero; an unanchored identifier grep; and an unranged `git diff` emptiness assertion. Each is a shape cycle 3 found in this phase's own gates, so a future author cannot reintroduce one without violating a stated prohibition. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → `codegraph serve --mcp` stdin | An agent process feeds newline-delimited JSON-RPC across the process boundary. `stdinLingerReader` sniffs those bytes before go-sdk's decoder sees them. |
| server → client stdout | Both client-owed responses and server-initiated notifications share one writer. Conflating them is the defect. |
| underlying writer's short-write report → the counter | The number of bytes the OS actually accepted is the only authoritative statement about what reached the wire, and it is now the classifier's sole input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Repudiation | `internal/mcp/server.go` `pendingWriter` | high | mitigate | Decrement only on complete outbound lines within `b[:n]` classified as responses by `looksLikeJSONRPCResponse`; forward before classifying so no byte is delayed; refuse to go below zero via `decrementPending` and count refusals in `pendingUnderflows`. Proven by a reproduction demonstrated RED before the fix. |
| T-01-27 | Repudiation | short write miscounted as delivered | high | mitigate | The classifier's input is `b[:n]` and never `b`, asserted directly by a test whose underlying writer accepts a partial buffer and returns an error. |
| T-01-14 | Denial of Service | `waitForDrain` under an over-counting sniff | medium | accept | The conservative default — an unparseable outbound line does not decrement — can only lengthen the wait, bounded by `stdinLingerGrace` at 5 seconds. That asymmetry is chosen deliberately: a bounded wait is strictly preferable to a premature drain. |
| T-01-15 | Tampering | malformed inbound line driving the sniff | low | accept | `looksLikeJSONRPCCall` already reports true conservatively on a parse failure, and the sniff reads at most `method` and `id`. No new parsing surface is introduced. |
</threat_model>

## Flagged Assumptions (unresolved edge probes)

The spec-less edge probe classified FIX-01 as `unclassified`. Per the probe's rules it is
not auto-resolved into `must_haves`; it is surfaced here as an open assumption:

- **FIX-01 / unclassified** — assumed: the counter's correct semantics are "accepted
  client calls owed a response", and the only two participants are the inbound call sniff
  and the outbound response sniff. If go-sdk ever begins issuing server-initiated requests
  that expect client responses, the inbound side would need a third case and this
  assumption lapses. The current code's own doc comment states codegraph's stdio server
  never initiates client-bound requests; that statement is the assumption's basis.

<verification>
- `go test -race -count=1 ./internal/mcp/...` passes.
- `task test:unit` passes.
- The wire oracle suite passes — no wire-visible response content changed.
- `go vet ./internal/mcp/...` is clean.
</verification>

<success_criteria>
- An MCP session no longer drains its pending count to zero when a server-initiated notification is written while a response is still owed, proven against a reproduction that showed the premature drain.
- Only bytes the underlying writer reported as written are ever classified, and no byte is delayed by classification.
- The counter never goes negative under any interleaving, and refused decrements are counted rather than silently absorbed.
- The `toolslist-repeat` flake is proven separable, left open, and correctly attributed.
</success_criteria>

<output>
Create `.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-03-SUMMARY.md` when done
</output>
