---
phase: 01-engine-seam-wire-protocol-secure-transport
plan: 11
type: tdd
wave: 7
depends_on: ["01-10"]
files_modified:
  - internal/uiproto/uiv1/ui.proto
  - internal/uiproto/uiv1/ui.pb.go
  - internal/uiproto/uiv1/uiv1connect/ui.connect.go
  - internal/uiserver/handlers.go
  - internal/uiserver/degrade.go
  - internal/uiserver/degrade_test.go
  - internal/uiserver/readonly_test.go
  - internal/graphstore/open_lock_test.go
autonomous: true
requirements: [SRV-04]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A store that stays locked past `graphstore.Open`'s retry budget renders as `CodeUnavailable` carrying a typed `IndexingInProgress` detail — never `CodeInternal`, never `CodeFailedPrecondition`, and never as an error the human reads as a failure (SRV-04, D-14)."
    - "A holder that releases WITHIN `graphstore.Open`'s bounded retry budget produces a NORMAL successful result from every RPC — not a degrade. This is the case that makes `codegraph ui` usable alongside the transient collisions `daemon` and `serve --mcp` actually produce. The release is triggered by a causal edge — a channel fired from the wrapped `openEngine` seam as the first RPC begins its open — never by a timer sized against the budget, so the outcome does not depend on CI load."
    - "No second retry layer is added above `graphstore.Open`'s existing bounded budget, proven by counting store opens per degraded RPC rather than by measuring wall-clock time (D-15)."
    - "`Status` still answers when the store cannot be opened at all, reporting repo path, store-exists and lock-held from filesystem facts alone, with graph-derived counts degraded (D-16)."
    - "A repository with no `.codegraph/` directory at all reports an explicit not-initialized state, distinct from and never conflated with the locked state (SRV-04 / empty edge)."
    - "`GetStatusResponse.store_exists = 8` and `.indexing_in_progress = 9` are PINNED by plan 01-09's known-number fixture, because THIS plan extends that fixture by exactly two entries in the wave the fields first exist. They were pinned by nothing before this plan: 01-09's fixture could not carry them (the fields did not exist at wave 5) and 01-08 recorded the allocation only in a proto comment, which no descriptor carries (D-02a, RPC-01)."
    - "The degrade precedence — not-initialized outranks locked — is an explicit ordered check in a named classifier, asserted by driving that classifier with an error satisfying both conditions, and the plan records that the combined FILESYSTEM state is unreachable by construction (SRV-04 / ordering edge)."
    - "Calling the same RPC twice against a locked store yields the identical `CodeUnavailable` and detail both times, mutates nothing, and retains no handle (SRV-04 / idempotency edge)."
    - "The boundary between a normal result and a degrade is `graphstore.Open`'s own budget and nothing else: a release inside it succeeds, a release outside it degrades, and the exact FINAL-ATTEMPT boundary is owned by `internal/graphstore`, where the unexported `openLockRetrySleep` seam lives — not re-measured from `internal/uiserver` with a wall clock. This plan WRITES that final-attempt test (`TestOpenSucceedsOnTheFinalAttempt`, added to `internal/graphstore/open_lock_test.go`) because no such test existed: the two Open tests on `main` are `TestOpenSecondOpenInProcessReturnsErrStoreLocked`, which is wall-clock and is the never-releases case, and `TestOpenConvergesWhenHolderCloses`, which IS event-synchronized but releases at attempt 2 — its own doc comment says \"some remaining attempt (budget: 5)\" — so it covers release-BETWEEN-attempts, not the final-attempt boundary (SRV-04 / adjacency edge)."
    - "Concurrent RPCs each open, snapshot and close independently; a holder running in parallel is never starved by the UI keeping the lock (SRV-04 / concurrency edge)."
  artifacts:
    - internal/uiserver/degrade.go
    - internal/uiserver/degrade_test.go
    - internal/graphstore/open_lock_test.go
  key_links:
    - "The final-attempt boundary test belongs in `internal/graphstore` and nowhere else, because `openLockRetrySleep` is unexported and unreachable from `internal/uiserver`. This plan writes it there and delegates to it by name; no other plan owns `internal/graphstore`, so there is no same-wave file-ownership conflict."
    - "The known-number fixture chain is `uiProtoFieldFixtureLenAtPlan0109` (01-09) → `+9` (01-10) → `+2` (this plan). Each constant is defined in terms of the previous one so the baseline is carried by the compiler across three waves rather than by a number transcribed into a prose SUMMARY."
    - "`graphstore.ErrStoreLocked` is an exported `errors.Is`-able sentinel with two existing consumers. The RPC layer is a third consumer of the same classification seam, not a new mechanism."
    - "`query.ResolveCodegraphDir` only stats the `.codegraph/` directory, so it succeeds independently of the store lock — which is what makes D-16's degraded Status answerable at all."
    - "`query.ErrNotInitialized` is returned ONLY when no `.codegraph/` exists anywhere up the walk. In that case there is no store directory whose lock could be contended, so the two conditions cannot co-occur on a real filesystem — the precedence rule is still worth encoding, and is tested at the classifier rather than through an impossible fixture."
    - "`openEngine` is the package-level opener var plan 01-01 introduced. Counting its invocations is how D-15's no-second-retry-layer property is proven without a wall clock."
  prohibitions:
    - "Never gate a task on a bare `go test ... | rg '--- PASS' | wc -l` count whose exit status is discarded. A shell pipeline's status is the LAST command's, so N passing subtests plus one `--- FAIL` satisfies a count-only gate. Every automated gate captures `go test`'s output and status BEFORE any pipe and requires BOTH: status 0 AND the `--- PASS` floor. The count alone is vacuous when a test fails; the status alone is vacuous when the `-run` pattern matches nothing. The sole scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence."
    - "Never renumber or reuse a protobuf field number once it has shipped; retiring a field means adding its number to a `reserved` clause. `GetStatusResponse` fields 8 and 9 were allocated to this plan by plan 01-08."
    - "Never embed a filesystem path, a lock-file name or a process id in a client-visible detail message."
    - "Never assert an outcome as 'succeeds OR degrades'. An assertion with no failing input is vacuous under rule 84d1gfpywd; each test names the single outcome it must see."
    - "Never write `test -z \"$(git status --porcelain <path>)\"` without a preceding `test -d <path> &&` existence guard. `git status --porcelain` on a missing or typo'd pathspec exits 0 with EMPTY stdout — the 'could not open directory' warning goes to stderr — so the check passes vacuously against a path that was never opened."
    - "Never positive-control a zero-count gate against a DIFFERENT file than the one the gate guards. `rg` prints nothing for a nonexistent path just as it does for a clean file, so a typo'd or renamed target reads `0` while the guarded file is never opened. The control must run the identical pipeline against the SAME file with a pattern that file genuinely contains."
    - "Never score a count with a regex that accepts zero. `rg -q -e 'compared [0-9]+ generated files'` matches `compared 0 generated files`, so it is a presence check on the count and not a floor. Capture the number and assert the floor numerically."
    - "Never write an unanchored identifier grep as a forbidden-pattern gate. Anchor it to the declaration form — `func gather[A-Z]`, never `\\bgather[A-Z]` — because a trailing comment or a `/* */` block survives `rg -v '^\\s*//'` and turns the gate red, or green, for the wrong reason."
    - "Never assert emptiness with an unranged `git diff`. Under atomic-commit-per-task the working tree is clean at verification time, so `git diff --stat <path>` is empty by construction whether or not the file was edited. Assert over an explicit `<phase-base>..HEAD` revision range."
    - "Never write a gate whose floor can be met without the deliverable it names existing. This is the SCOPE half of gate hygiene and no shape check reaches it: a scope-vacuous gate is well-formed, parses, honors exit status and carries a same-file positive control, and still scores nothing the task owns. Cycle 5's form of this prohibition was STRUCTURAL where it had to be CAUSAL — OWNERSHIP constrained who DECLARES the counted tests, not what those tests ASSERT, and HOMING accepted a named criterion no gate executes — so a task declaring one self-contained arithmetic test and homing every behavior bullet to prose satisfied both halves literally while the deliverable was entirely absent. The amended rule closes that. Two requirements, both mandatory and both checkable by reading the plan. (a) OWNERSHIP AND CAUSATION — every gate carries at least one leg that turns RED when this task's named deliverable is absent or replaced by a NO-OP, in exactly ONE of three named forms, and the floor derivation must say which form it uses. A no-op is an implementation that still compiles and keeps the same signature but does nothing and returns zero values; for a generated file, schema or other declarative artifact it is the empty or default-valued version of it. REPLACEMENT, not deletion, is the standard, because deletion is satisfied by a mere compile dependency and only replacement separates a test that ASSERTS an artifact's behavior from one that merely MENTIONS it. FORM A1, OWN-TEST, the default: the leg's test-selection pattern matches ONLY test functions this task itself declares, its floor is derived from that leg's own functions and subtests alone, AND the derivation names the ARTIFACT UNDER TEST — a specific function, type, constant, generated file or task, cited from this task's files list, this plan's files_modified, this phase's artifact manifest, or this task's read_first — and states why replacing that artifact with a no-op turns this leg RED. A counted test that would still pass against that no-op does not satisfy A1 however exclusively this task declares it, and a counted test with no artifact under test at all — a self-contained arithmetic assertion is the canonical form — never satisfies it. Where the named artifact is one this task does not itself modify, which is the characterization-assertion case, the derivation must say so and name where the artifact is cited. FORM A2, DIRECT-ARTIFACT, for a deliverable that is a command, generated file or task rather than a test and is named in this task's files list, this plan's files_modified or this phase's artifact manifest: the leg EXECUTES that artifact and scores a numeric floor read out of that artifact's OWN output, and the derivation names it and states that a no-op replacement drives the measured number below the floor. FORM A3, OBSERVATION, available ONLY to a task that declares no test function of its own and whose named deliverable is a recorded observation rather than persisted code — a deliberate-mutation RED observation, or a by-hand exercise against real processes. Such a task writes no assertion, so no `--- PASS` floor can ever be its evidence, and D-04's scoped exception to the conjunction lives here; it qualifies only when ALL FOUR hold: every observation is scored by at least one NAMED numeric or verdict-bearing measurement taken from a named command's own output and recorded verbatim; a vacuous measurement — a zero failure count, a missing per-item verdict — is written down as a STOP CONDITION and not as a note; the gate still carries the strongest executable leg available to it; and the derivation states plainly which failures of THIS task that leg CAN catch and which it CANNOT. A task that declares even one test function of its own may never use A3, and A3 is never a way to avoid writing one. Where a leg's pattern ALSO matches an earlier plan's tests, the derivation must say so, say why those inherited tests are the intended subject of that leg, and show the leg still turns RED without this task's own tests by stating the count the inherited tests alone supply and showing it falls BELOW the floor; a leg whose floor the inherited tests alone already clear NEVER counts as the leg this requirement demands. (b) HOMING — every behavior bullet is named by, or names, a test function or subtest that the task's own floor derivation counts; or it is homed to a named acceptance criterion WHOSE COMMAND APPEARS IN THIS TASK'S `<automated>` GATE, because prose is not a home and a criterion no gate executes is not an executable owner; or the derivation states in writing that the bullet is deliberately out of scope and names the plan or phase that owns it instead, which a bullet describing work THIS task performs may never claim. A bullet no derivation reaches is a described behavior with no executable owner, and a leg whose floor another plan's tests already clear cannot fail when this task writes nothing."
---

<objective>
Make the degraded states legible: a locked store reads as "indexing in progress", an
uninitialized repository reads as "not initialized", and `Status` answers in both cases.

Purpose: SRV-04's degrade path does not exist today. `Engine.Status()` runs only after a
successful `Engine` construction, and `query.OpenAt` fails outright when the store is
locked past `graphstore.Open`'s retry budget — so `Status()` never even runs in the exact
case the requirement describes. This is new plumbing at the RPC layer, not a reuse of an
existing fallback.

The first review of this phase corrected two things about how that plumbing gets proven,
and both corrections are built into this plan rather than left as notes. A test that
accepts "succeeded or degraded" has no failing input and proves nothing, so each test here
names the single outcome it must see — and which outcome that is depends on whether the
holder releases inside `graphstore.Open`'s budget, which is a property the test controls.
And a repository cannot be simultaneously uninitialized and lock-contended: if no
`.codegraph/` exists there is no store directory whose lock could be held. The precedence
rule is still worth encoding, so it is tested where it is actually decidable — at the
classifier, driven with an error that satisfies both conditions — with the filesystem
impossibility recorded rather than papered over.

Output: a typed degrade, a `Status` that answers when things are wrong, a counted proof
that no second retry layer exists, and recorded evidence from real concurrent processes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-10-SUMMARY.md
</context>

## Artifacts this phase produces

**Created by this plan (01-11):**

- `internal/uiproto/uiv1/ui.proto` — `GetStatusResponse` gains `store_exists` and `indexing_in_progress` at fields 8 and 9, the numbers plan 01-08 allocated; `IndexingInProgress.message` becomes populated
- Regenerated `ui.pb.go` and `uiv1connect/ui.connect.go`
- `internal/uiserver/degrade.go` — `degradeKind` and its constants, `classifyDegrade`, `indexingInProgressMessage`, `errIndexingInProgress`, `degradedStatus`
**Estimate note (cycle-3 scope):** this plan grew from 72k to 84k when H1 (the two-entry
fixture extension) and H2 (`TestOpenSucceedsOnTheFinalAttempt` in `internal/graphstore`)
landed here, putting it above the phase's other plans. No wave redesign is needed and no
correctness change is owed. If context pressure becomes material at execution, the natural
split point is BEFORE Task 3's manual multi-process evidence: Tasks 1 and 2 are the
executable contract and Task 3 is transcript capture, so splitting there costs nothing.

- `internal/uiserver/degrade_test.go` — `TestClassifyDegradePrecedence`, `TestStatusDegradesOnLock`, `TestStatusOnUninitializedRepo`, `TestNonStatusRPCsDegradeWhenAHolderNeverReleases`, `TestRPCsSucceedWhenAHolderReleasesWithinTheOpenBudget`, `TestDegradeIsIdempotent`, `TestDegradedRPCOpensTheStoreExactlyOnce`, `TestIndexingInProgressMessageLeaksNothing`, `TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete`
- `internal/graphstore/open_lock_test.go` — `TestOpenSucceedsOnTheFinalAttempt` (the final-attempt boundary test this phase adds; the two Open tests already on `main` cover the never-releases and release-between-attempts cases)
- `internal/uiserver/readonly_test.go` — `uiProtoFieldFixtureLenAtPlan0111` and the two-entry extension of plan 01-09's known-number fixture

**Created elsewhere in this phase** (listed so drift verification sees the whole set):
`internal/goldenspec`; `testdata/golden/byte_identity_test.go`; `internal/uiserver`'s
`Options`, `DefaultAddr`, `Server`, `Listen`, `Serve`, `URL`, `Close`, `originHostGuard`,
`allowedHosts`, `allowedOrigins`, `uiService`, `openEngine`, `withEngine`,
`mapEngineError`, the nine handlers and their mappers, `uiMultiDefCap`, `truncateSource`,
`countLines`, `sourceLineCap`, `sourceByteCap`, `transportSendMaxBytes`,
`transportReadMaxBytes`; `internal/textutil.TruncateOnRuneBoundary`;
`internal/uiproto/uiv1`'s nine rpcs, their messages and `SourceBlob`;
`internal/cli/ui.go`; `internal/query`'s `NodeDetail` family, `ExploreResult` family,
`ErrNotFound`, `ErrInvalidArgument`, `(*Engine).IndexMeta`, `(*Engine).SourceFor` and the
`build*` builders; `schema.Meta.commit_sha` field 8 and `schema.IndexedCommitSHA`;
`internal/indexer/commit.go`'s `resolveHeadCommitSHA` and `gitExecLookPath`;
`internal/mcp`'s `looksLikeJSONRPCResponse`, `sniffedOutbound`, `decrementPending`,
`pendingUnderflows`; `buf.yaml`, `buf.gen.yaml`, `go.tool-proto.mod`, `Taskfile.yml` tasks
`proto:gen` and `proto:drift`, `internal/upgrade/proto_task_test.go`.

## Gate conventions (phase-wide canon — byte-identical in all eleven plans, and checkable)

This section is replicated verbatim across `01-01-PLAN.md` … `01-11-PLAN.md`. The claim is
checkable rather than merely asserted — one command, run from the phase directory, prints
**one** distinct digest if the eleven copies agree and more than one if any has drifted:

```
for f in 01-*-PLAN.md; do sed -n '/^## Gate conventions/,/^<tasks>$/p' "$f" | shasum; done | sort -u | wc -l
```

A future planner editing one copy must edit all eleven, and this is how a reviewer catches it
if they do not. (Nothing at execution time changes this section, so no `<automated>` gate owns
it — the drift risk is at planning time, which is where the check belongs.)

**`--- PASS` counting.** `go test -v` prints one `--- PASS:` line for the parent test
function AND one for every `t.Run` subtest, including nested ones. Every PASS floor in this
phase counts BOTH: a test function with three subtests contributes FOUR lines, so
`rg -o -e '--- PASS' | wc -l` over `go test -v` output equals
`len(test functions) + len(all subtests)`. Derive every floor that way and show the
arithmetic in the acceptance criterion that states it, so a reviewer can check the floor
instead of re-deriving it.

**Echo label.** Every gate prints `PASS lines:` — never `PASS tests:`, never
`PASS subtests:`. The label names what is counted, and what is counted is lines.

**The conjunction (D-04 as corrected in `01-CONTEXT.md`).** A gate passes only when the
test command's exit status is 0 AND the `--- PASS` floor is met. Neither is sufficient
alone: `go test -run PATTERN` exits 0 when the pattern matches nothing, and a shell
pipeline's `$?` is the LAST command's, so a bare count passes with N passing subtests plus
one `--- FAIL`. The single scoped exception is a deliberate-mutation RED observation, where
a non-zero exit IS the expected evidence and the observation is scored by `--- FAIL` /
`--- PASS` counts rather than by status.

**The five vacuity shapes cycle 3 found in this phase's own gates.** Each is a prohibition
in `must_haves.prohibitions`. They are restated here together because they are one class —
a guard that reads green against a file it never opened:

1. `git status --porcelain <path>` exits 0 with empty stdout on a MISSING or typo'd path
   (the warning goes to stderr). Always prefix `test -d <path> &&`.
2. A zero-count gate positive-controlled against a DIFFERENT file proves nothing about the
   guarded file: `rg` prints nothing for a nonexistent path too. Control on the SAME file.
3. `rg -q 'compared [0-9]+ generated files'` accepts `compared 0 generated files`. Capture
   the number; assert the floor numerically.
4. An unanchored identifier grep (`\bgather[A-Z]`) is satisfiable by a trailing or `/* */`
   comment that `rg -v '^\s*//'` does not strip. Anchor to the declaration form.
5. An unranged `git diff` is empty by construction at verification time under
   atomic-commit-per-task. Assert over `<phase-base>..HEAD`.

**The scope shape cycle 5 found, which none of the five above reaches — and the cycle 7
amendment that made it causal.** The five shapes are all about how a gate is WRITTEN. A gate can
satisfy every one of them and still be vacuous because of what it is SCOPED to: if its floor is
already cleared by test functions belonging to an EARLIER plan that happen to share the selection
pattern, then the pattern matching none of this task's own tests costs nothing — `go test` exits 0
on a pattern that matches nothing, and the inherited tests supply the count. The sixth prohibition
in `must_haves.prohibitions` closes it. Cycle 5's form of that prohibition was STRUCTURAL where it
had to be CAUSAL: OWNERSHIP constrained who DECLARES the counted tests, not what those tests
ASSERT, and HOMING accepted a named criterion that no gate executes. Both halves were therefore
satisfiable literally by a task declaring one self-contained arithmetic test and homing every
behavior bullet to prose, with the deliverable entirely absent. The amended prohibition supplies
the missing causal clause. Half (a) now requires the leg to turn RED when the deliverable is
REPLACED BY A NO-OP — replacement, not deletion, because deletion is satisfied by a mere compile
dependency — and names the three forms a qualifying leg may take: A1, an own-test leg that names
its artifact under test; A2, a direct-artifact leg scoring a number read out of that artifact's own
output; and A3, an observation leg, open only to a task that declares no test at all and whose
deliverable is a recorded observation, which must carry a named measurement, a written STOP
CONDITION for a vacuous measurement, the strongest executable leg available to it, and a plain
statement of what that leg cannot catch. A3 is what keeps D-04's deliberate-mutation exception
representable. A leg whose pattern also matches an earlier plan's tests must show the inherited
count alone falls BELOW the floor, or it does not qualify. Half (b) now requires a criterion's
command to appear in the task's `<automated>` gate before that criterion counts as a home. Every
floor derivation in this phase names its form. All of it is checkable by reading the plan, which is
where the defect lives.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The degrade classifier, the typed error and a `Status` that answers without the store</name>
  <files>internal/uiproto/uiv1/ui.proto, internal/uiproto/uiv1/ui.pb.go, internal/uiproto/uiv1/uiv1connect/ui.connect.go, internal/uiserver/degrade.go, internal/uiserver/handlers.go, internal/uiserver/degrade_test.go, internal/uiserver/readonly_test.go</files>
  <read_first>
    - internal/graphstore/pebble_store.go lines 60-160 — `openLockRetryAttempts`, `openLockRetryBackoff`, `ErrStoreLocked`, `classifyOpenError`, and `Open`'s bounded retry loop, where classification happens exactly once inside the loop
    - internal/uiserver/readonly_test.go — plan 01-09's known-number fixture, its `uiProtoFieldFixtureLenAtPlan0109` baseline constant, plan 01-10's `uiProtoFieldFixtureLenAtPlan0110 = ... + 9` extension, and the provenance comment naming THIS plan as the final extender in the phase
    - internal/cli/serve.go around lines 205-250 — the existing `errors.Is(err, graphstore.ErrStoreLocked)` branch and its comment on why a broader check would let a permission error masquerade as lock contention
    - internal/daemon/daemon.go around lines 290-300 and 425-435 — the second existing `ErrStoreLocked` consumer
    - internal/query/resolve.go lines 18-60 — `ErrNotInitialized` and `ResolveCodegraphDir`, which only stats the `.codegraph/` directory and therefore succeeds independently of the store lock, and the upward walk that makes the not-initialized and locked conditions mutually exclusive on a real filesystem
    - internal/query/status.go lines 40-90 — `StatusResult`'s field set and the fact that `Status(ctx)` runs only on an already-open `Engine`
    - internal/uiserver/handlers.go — the nine handlers, `withEngine`, `openEngine` and `mapEngineError`, plus the named comment plan 01-08 left pointing at this plan for the `ErrStoreLocked` mapping
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-01-SUMMARY.md — the recorded real signatures of `connect.NewErrorDetail` and `connect.CodeUnavailable`
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md — "Pitfall 3: D-16's Status degrade path does not exist today", and the Code Examples section's error-detail shape
  </read_first>
  <behavior>
    - `TestClassifyDegradePrecedence`: `classifyDegrade` returns not-initialized for a `query.ErrNotInitialized`, indexing-in-progress for a `graphstore.ErrStoreLocked`, none for a nil error, none for an unrelated error, and — driven with a synthetic error that satisfies BOTH sentinels — not-initialized, proving the precedence is an ordered check rather than an accident of evaluation order.
    - `TestStatusDegradesOnLock`: with the store held past `Open`'s budget, `GetStatus` returns a SUCCESSFUL response reporting the repo path, that the store exists, and that indexing is in progress — with graph-derived counts zeroed. Not an error.
    - `TestStatusOnUninitializedRepo`: a directory with no `.codegraph/` returns a successful response with the initialized flag unset and the indexing flag unset, distinct from the locked state.
    - `TestIndexingInProgressMessageLeaksNothing`: the message equals the named constant exactly, and the constant contains no path separator of either kind — asserted with a helper that is positive-controlled by confirming it reports true for a string that does contain one.
  </behavior>
  <action>
    Add the degrade fields to `GetStatusResponse` in `internal/uiproto/uiv1/ui.proto` at the
    numbers plan 01-08 allocated: a store-exists flag at 8 and an indexing-in-progress flag
    at 9. The initialized flag already exists at field 1 from plan 01-01. Give each a doc
    comment stating what it means and what the graph-derived counts contain when it is set.
    Populate `IndexingInProgress.message`. Regenerate with `task proto:gen`.

    Create `internal/uiserver/degrade.go`.

    Write `classifyDegrade(err error) degradeKind` as an EXPLICIT ORDERED CHECK with a
    comment stating the precedence and why: not-initialized outranks locked, because a
    repository with no `.codegraph/` cannot be mid-index and reporting "indexing in
    progress" there would be actively misleading. Check `query.ErrNotInitialized` first,
    then `graphstore.ErrStoreLocked`, then fall through to none.

    Record honestly, in the function's doc comment, that on a real filesystem the two
    conditions cannot co-occur: `ResolveCodegraphDir` returns `ErrNotInitialized` only after
    walking to the root without finding a `.codegraph/` directory, and if no such directory
    exists there is no store whose lock could be contended. The precedence is encoded anyway
    because the classifier is the one place a future caller could hand it an error chain
    carrying both, and because an ordered check is cheaper to read than an argument about
    which branch happens to run first. The precedence test drives the classifier directly
    with a constructed multi-target error rather than trying to build an impossible
    repository — a test that cannot reach its own precondition proves nothing.

    Classify with `errors.Is` against the exported sentinels and nothing broader.
    `internal/cli/serve.go`'s own comment explains why: a permission error anywhere in the
    chain must never masquerade as lock contention. Two consumers of `ErrStoreLocked`
    already exist; the RPC layer is a third consumer of the same seam, not a new mechanism.

    Declare `indexingInProgressMessage` as a named constant — one fixed generic sentence
    naming the condition and suggesting a retry — and write `errIndexingInProgress()`
    returning a `CodeUnavailable` error with an `IndexingInProgress` detail carrying it.
    `CodeUnavailable` is the right code because the Connect protocol reference defines it as
    transient — back off and retry — which is exactly this situation; `CodeFailedPrecondition`
    is explicitly wrong because the user fixes nothing and it resolves itself. SRV-04's
    "never as an error" is read as never surfaced to the human as an error, which is the
    property the requirement protects, and the client special-cases this code to render the
    state.

    Keep the message generic. Do not embed the lock-file path, the holding process's id, the
    store directory, or any absolute filesystem path: this is an unauthenticated loopback
    caller and the message crosses into a browser. Assert that by comparing against the
    named constant exactly and by checking the constant carries no path separator — a
    targeted, explainable check rather than a ban on any digit sequence that might resemble
    a process id, which would also reject a harmless retry duration.

    Extend `mapEngineError` so a locked store maps to `errIndexingInProgress()` for every
    non-`Status` handler, replacing the named placeholder comment plan 01-08 left.

    Add NO retry above `graphstore.Open`'s existing budget. `Open`'s loop is already the
    bounded, never-block answer, and SRV-04 describes precisely the case where a re-index
    OUTLASTS that budget. A second retry layer would change the very condition the
    requirement names and make the degrade path harder to demonstrate.

    Write `degradedStatus(repoPath string) (*uiv1.GetStatusResponse, error)` for the D-16
    path. This is new plumbing, not a reused fallback: `Engine.Status()` runs only after a
    successful `Engine` construction, so it never runs in this case today and there is
    nothing to fall back to. Change `GetStatus` to be the one handler that does NOT go
    through `withEngine`'s error mapping: it calls `openEngine` directly, and when the open
    fails it runs `classifyDegrade` and builds the response from filesystem facts alone.
    `ResolveCodegraphDir` succeeding is independent of the store lock because it only stats
    the directory, so repo path, store-exists and the degrade kind are all answerable. Zero
    the graph-derived counts and set the matching flag. Document at the call site that
    `GetStatus` is the single, deliberate exception to the `withEngine` rule and why —
    Phase 4's index-health verdict depends on `Status` being reachable exactly when things
    are wrong, and the health view answering "the index is busy" is a health answer.

    **Extend plan 01-09's known-number fixture in `internal/uiserver/readonly_test.go` by
    exactly two entries, in this task, in the same commit that declares the fields.** Add
    `GetStatusResponse.store_exists = 8` and `GetStatusResponse.indexing_in_progress = 9`,
    and declare `uiProtoFieldFixtureLenAtPlan0111 = uiProtoFieldFixtureLenAtPlan0110 + 2`
    immediately above the fixture, asserting `len(fixture) == uiProtoFieldFixtureLenAtPlan0111`.
    Do not restate either side as a bare literal.

    This is not bookkeeping. Before this change, `GetStatusResponse` fields 8 and 9 were
    pinned by NOTHING: plan 01-08 recorded the allocation in a proto comment, and a
    descriptor carries declared fields and `reserved` ranges but never comments; plan
    01-09's fixture was scoped to fields 01-09 itself declared, and the fields did not exist
    at its wave anyway. The previous version of this plan asserted that 01-09's test "still
    passes with fields 8 and 9 landing where they were allocated" — it did pass, and it
    asserted nothing at all about them. Extending the fixture here, in the wave the fields
    first exist, is what converts that vacuous cross-wave claim into a real one, and it is
    the same move plan 01-10 makes for its nine source fields.

    Write the behavior block's cases into `internal/uiserver/degrade_test.go`. For the
    lock-contention cases, hold the store with a real `graphstore.Open` handle in the test
    process — a second goroutine holding the handle reproduces the same Pebble directory
    lock the daemon takes — rather than mocking the error, so the real classification path
    fires.
  </action>
  <verify>
    <automated>task proto:gen && test -d internal/uiproto && test -z "$(git status --porcelain internal/uiproto/)" && go build ./... && { OUT=$(go test -v -count=1 -race -run 'TestClassifyDegradePrecedence|TestStatusDegradesOnLock|TestStatusOnUninitializedRepo|TestIndexingInProgressMessageLeaksNothing|TestUIProtoFieldNumbersAreStableAndUnique' ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 9 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -race -run 'TestClassifyDegradePrecedence|TestStatusDegradesOnLock|TestStatusOnUninitializedRepo|TestIndexingInProgressMessageLeaksNothing|TestUIProtoFieldNumbersAreStableAndUnique' ./internal/uiserver/` emits at least 9 `--- PASS` lines, and the command exits 0 — both conditions, never either alone. `TestUIProtoFieldNumbersAreStableAndUnique` is in the `-run` pattern deliberately: this task extends its fixture, so the gate that scores this task must be the gate that runs it.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** Five test functions match: `TestClassifyDegradePrecedence` declares five `t.Run` subtests (`not-initialized`, `store-locked`, `nil-error`, `unrelated-error`, `satisfies-both-sentinels`) = 6; `TestStatusDegradesOnLock` = 1; `TestStatusOnUninitializedRepo` = 1; `TestIndexingInProgressMessageLeaksNothing` = 1; plan 01-09's `TestUIProtoFieldNumbersAreStableAndUnique`, re-run here because this task extends its fixture, = 1. Total **10** against a floor of 9. **Ownership:** four of the five atoms name tests THIS task declares and they supply 9 of the 10 lines, so the floor cannot be met by the inherited test alone — drop this task's tests and the count falls to 1, far below 9, and the gate goes red. The fifth atom is deliberately inherited and its subject is deliberately plan 01-09's test: this task EXTENDS that test's fixture, so the gate that scores this task must be the gate that runs it. That is why the inherited atom is present, stated here as this plan's sixth prohibition requires rather than left for a reviewer to infer. The inherited-subject clause additionally requires the leg still turn RED without this task's own tests, and the arithmetic above is that showing: the inherited test alone supplies 1 against a floor of 9, which is BELOW it. **Form A1 (own-test), causation.** The artifacts under test are `classifyDegrade`, `errIndexingInProgress`, `indexingInProgressMessage` and `degradedStatus` in `internal/uiserver/degrade.go` and fields 8 and 9 of `GetStatusResponse` in `ui.proto`, all named in this task's files list. Replace `classifyDegrade` with a no-op that still compiles and returns the zero classification and all five `TestClassifyDegradePrecedence` subtests turn RED on the outcome each names; replace `degradedStatus` the same way and `TestStatusDegradesOnLock` turns RED on the degraded response it asserts.
    - **Behavior-bullet homes.** All four bullets in this task's behavior block name their own test function, and all four are counted by the derivation above, so no bullet is orphaned.
    - `TestClassifyDegradePrecedence` includes a case driven with a synthetic error satisfying BOTH `query.ErrNotInitialized` and `graphstore.ErrStoreLocked`, and asserts not-initialized wins. No test attempts to construct a repository that is simultaneously uninitialized and lock-contended.
    - `classifyDegrade`'s doc comment records that the two conditions cannot co-occur on a real filesystem and why the precedence is encoded anyway.
    - `TestStatusDegradesOnLock` holds the store with a real `graphstore.Open` handle, not a mocked or injected error, and asserts a SUCCESSFUL response — not an error — carrying the repo path, the store-exists flag and the indexing flag, with graph-derived counts zeroed.
    - `TestStatusOnUninitializedRepo` asserts the initialized flag unset AND the indexing flag unset, so the two states are proven distinct rather than merely both non-error.
    - `TestIndexingInProgressMessageLeaksNothing` asserts exact equality against `indexingInProgressMessage` and asserts the constant contains no path separator, with the separator check positive-controlled against a string that does contain one in the same test.
    - `rg -v '^\s*//' internal/uiserver/degrade.go internal/uiserver/handlers.go | rg -o -e 'graphstore\.ErrStoreLocked' | wc -l` prints at least `1`.
    - No error classification in either file compares against an error message string. This is a SEPARATE, EXECUTABLE, positively-controlled assertion rather than a clause welded onto the presence check above — welded to that bullet it carried no pipeline at all and nothing could fail it. Run `rg -v '^\s*//' internal/uiserver/degrade.go internal/uiserver/handlers.go | rg -o -e 'strings\.(Contains|HasPrefix|HasSuffix|EqualFold)|Error\(\) ==' | wc -l` and require `0`, positive-controlled in the SAME invocation over the SAME two files by `rg -v '^\s*//' internal/uiserver/degrade.go internal/uiserver/handlers.go | rg -o -e 'errors\.(Is|As)' | wc -l` returning a NON-ZERO count. The control is what makes the zero mean something: `rg` prints nothing for a path that does not exist exactly as it does for a clean file, so without it a renamed or typo'd `degrade.go` reads green. Comment lines are stripped first so the files' own doc comments about not matching on message text cannot turn the gate red.
    - `task proto:gen` followed by `git status --porcelain internal/uiproto/` produces empty output (behind the `test -d` existence guard the gate carries) and `task proto:drift` passes with an unchanged compared-count.
    - Plan 01-09's `TestUIProtoFieldNumbersAreStableAndUnique` passes with `GetStatusResponse.store_exists = 8` and `.indexing_in_progress = 9` ASSERTED, not merely present. The previous wording — "still passes with fields 8 and 9 landing where they were allocated" — was vacuous: the test did pass, and it asserted nothing whatsoever about either field, because 01-09's fixture had no `GetStatusResponse` entry at all and nothing instructed this plan to add one. This task EXTENDS the fixture by exactly two entries and re-runs the test, mirroring plan 01-10's `+9` pattern.
    - The extension is asserted in code against the named baseline chain, not against a number carried in prose: this plan declares `uiProtoFieldFixtureLenAtPlan0111 = uiProtoFieldFixtureLenAtPlan0110 + 2` immediately above the extended fixture and asserts `len(fixture) == uiProtoFieldFixtureLenAtPlan0111`. Neither side may be restated as a bare literal.
    - Every fixture entry plans 01-09 and 01-10 wrote still holds unchanged after the extension, and the fixture's `resolved == len(fixture)` and every-declared-field-is-covered assertions both still hold — so this plan added two numbers and disturbed none, and neither new field escapes coverage.
  </acceptance_criteria>
  <done>A locked store classifies to one typed, generic `CodeUnavailable` detail; an uninitialized repository classifies to a distinct state; `Status` answers in both cases from filesystem facts; and the precedence is tested where it is decidable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Determinate degrade behavior — one named outcome per test, opens counted not timed</name>
  <files>internal/uiserver/degrade_test.go, internal/graphstore/open_lock_test.go</files>
  <read_first>
    - internal/uiserver/degrade.go — the classifier and error constructor Task 1 wrote
    - internal/uiserver/handlers.go — the `openEngine` package var plan 01-01 introduced, and `withEngine`
    - internal/graphstore/pebble_store.go lines 67-92 — `openLockRetryAttempts`, `openLockRetryBackoff` and the `openLockRetrySleep` seam, and the comment explaining that the bounded wait exists to ride out exactly the transient collisions `query.OpenAt`, `indexer.Sync` and the startup reconcile produce by design
    - internal/graphstore/open_lock_test.go lines 23-45 and 47-92 — BOTH existing Open tests, read for what they actually cover, not for what they are named: `TestOpenSecondOpenInProcessReturnsErrStoreLocked` at `:23` asserts elapsed wall-clock time against the budget and is the never-releases case; `TestOpenConvergesWhenHolderCloses` at `:62` IS event-synchronized but releases at attempt 2 (its doc comment at `:59-61` says "some remaining attempt (budget: 5)"), and its drain-loop trick works precisely BECAUSE a later sleep blocks until `Close` returns. Neither covers the final-attempt boundary; this task adds the test that does
    - .planning/STATE.md — rule `84d1gfpywd`: a guard must carry a positive assertion that it did its work, and an assertion with no failing input is vacuous
  </read_first>
  <behavior>
    - `TestRPCsSucceedWhenAHolderReleasesWithinTheOpenBudget`: a holder that acquires the store and releases it on a causal signal fired as the first RPC begins its open — not on a timer. EVERY one of the nine RPCs returns a NORMAL successful result. A degrade here is a failure, because riding out a transient collision is exactly what the bounded retry exists for and what `daemon` and `serve --mcp` actually produce.
    - `TestNonStatusRPCsDegradeWhenAHolderNeverReleases`: a holder that holds the store for the whole test. EVERY one of the eight non-`Status` RPCs returns exactly `connect.CodeUnavailable` with an `IndexingInProgress` detail. A success here is a failure, and so is a `CodeInternal`.
    - `TestDegradedRPCOpensTheStoreExactlyOnce`: with `openEngine` swapped for a counting wrapper, one degraded RPC call invokes it exactly once. A second retry layer above `graphstore.Open` would invoke it more.
    - `TestDegradeIsIdempotent`: two consecutive calls under the same permanent hold return equal codes and equal detail messages, and no file under the repository changed between them.
    - `TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete`: N concurrent RPCs are issued and ALL of them are awaited to completion; only THEN is the holder's `graphstore.Open` attempted, and it must succeed on the FIRST attempt with no retry consumed. This is a determinate retained-handle property with a real failing input — a `uiService` that kept a handle open across calls fails it — and it depends on no fairness guarantee.
  </behavior>
  <action>
    The previous version of this plan asserted that concurrent RPCs "each succeed or degrade
    independently". That assertion has no failing input: every possible outcome satisfies it.
    Rule `84d1gfpywd` names exactly that shape, and every other gate in this phase respects
    it. Split the case in two and let each name the single outcome it must see. The
    discriminator is real and under the test's control: whether the holder releases inside
    `graphstore.Open`'s bounded budget.

    Write `TestRPCsSucceedWhenAHolderReleasesWithinTheOpenBudget`. Acquire the store in the
    test process, then release it on a CAUSAL EDGE rather than on a wall clock: do NOT
    schedule the release with a timer sized against `graphstore.Open`'s budget. A timer
    "comfortably inside" a roughly-bounded retry period is still wall-clock coordination and
    can cross the boundary spuriously on a loaded `-race` runner, which is precisely the
    flake class this phase is trying not to author.

    Use the seam this package already owns. Wrap the `openEngine` package var so that, on
    its FIRST invocation only (`sync.Once`), it signals a channel and then proceeds to call
    `query.OpenAt`; restore the var with `t.Cleanup`. The holder goroutine blocks on that
    channel and closes the store the moment it fires. That establishes a happens-before edge
    between "an RPC is about to open the store" and "the holder has begun releasing it", so
    the interleaving no longer depends on how fast CI is running: the only way the RPC can
    fail is if a single `Close()` takes longer than the ENTIRE retry budget, which is a real
    defect worth failing on rather than a scheduling artifact. Say exactly that in the
    test's doc comment, including that a timer-based release was considered and rejected as
    timing-coupled.

    Then issue all nine RPCs through a real Connect client and assert every one returned a
    normal successful result. The first RPC is the one that races the release; the remaining
    eight run against a free store, and they are still worth issuing because a handler that
    cached a failed open would surface here.
    This is the case the roadmap's criterion 3 is actually about: nothing in this repository
    holds the store long-term — `internal/mcp/tools.go` opens per tool call and
    `internal/daemon/daemon.go` opens only inside its flush and closes — so the collisions a
    real `daemon` or `serve --mcp` produces are transient, and `Open`'s bounded retry rides
    them out. Say that in the test's doc comment, citing both call sites, so a future reader
    understands why success is the required outcome rather than a lucky one.

    Write `TestNonStatusRPCsDegradeWhenAHolderNeverReleases`. Hold the store for the
    duration, issue each of the eight non-`Status` RPCs, and assert each returned exactly
    `connect.CodeUnavailable` with a detail that unmarshals to an `IndexingInProgress`.
    Assert the code with `connect.CodeOf(err)` and assert on the unmarshalled detail, not on
    the error string. This is criterion 3's second clause — "a re-index that outlasts
    `graphstore.Open`'s retry budget renders as indexing in progress" — and the two tests
    together partition the space rather than leaving it open.

    Write `TestDegradedRPCOpensTheStoreExactlyOnce`, replacing the wall-clock timing
    assertion the previous version proposed. Swap the `openEngine` package var for a counting
    wrapper, restore it with `t.Cleanup`, issue one degraded RPC, and assert the counter is
    exactly 1. This proves D-15 directly: a second retry layer above `graphstore.Open` would
    have to open again, so the count would exceed 1. It is deterministic under parallel CI
    load, which a timing assertion around a 400 ms budget is not — and `graphstore`'s own
    `openLockRetrySleep` seam, which exists precisely to avoid wall-clock guessing, is
    unexported and not reachable from this package, so counting opens is the right
    instrumentation at this layer.

    **WRITE the final-attempt boundary test where it belongs, in `internal/graphstore`, and
    then delegate to it.** The previous version of this plan instructed the executor to cite
    "the existing event-synchronized final-attempt test" and to stop if none was found. That
    stop-instruction could not fire: a named event-synchronized test IS found —
    `TestOpenConvergesWhenHolderCloses` (`internal/graphstore/open_lock_test.go:62`) — but it
    releases the holder when ATTEMPT 2's backoff sleep begins, and its own doc comment says
    "some remaining attempt (budget: 5) deterministically finds the LOCK free". That is a
    release-BETWEEN-attempts test, not a final-attempt boundary test. The only other Open
    test, `TestOpenSecondOpenInProcessReturnsErrStoreLocked` (`:23`), asserts elapsed time
    against the budget — wall-clock — and is the never-releases case. So the executor would
    have cited a real name for a property it does not have, shipped a false attribution in a
    package comment, and left this plan's adjacency-edge probe unbacked.

    Add `TestOpenSucceedsOnTheFinalAttempt` to `internal/graphstore/open_lock_test.go`. It is
    deterministic and uses no wall clock, built on the same `openLockRetrySleep` seam
    `TestOpenConvergesWhenHolderCloses` already uses, with one addition — an acknowledgement
    channel — that is what makes the FINAL attempt reachable:

    - Open the store to acquire the holder.
    - Replace `openLockRetrySleep` with a function that increments a sleep ordinal, sends it
      on an unbuffered `sleeps` channel, and then BLOCKS on an unbuffered `resume` channel.
      Restore the original with `t.Cleanup`.
    - In a helper goroutine, receive each ordinal; when it equals `openLockRetryAttempts-1`
      — the fourth and LAST sleep, which immediately precedes the fifth and final attempt —
      close the holder; then, in every case, send on `resume`.
    - The `resume` acknowledgement is the load-bearing part and the reason
      `TestOpenConvergesWhenHolderCloses`'s drain-loop trick does not generalise here. That
      test relies on a LATER sleep blocking until `Close` returns; on the final sleep there
      is no later sleep, so without the acknowledgement the holder's `Close` would race the
      final `pebble.Open`. With it, `Close` happens-before the final attempt, deterministically.
    - Assert `Open` returned nil error, AND that the observed sleep ordinal equals
      `openLockRetryAttempts-1` — so the success is PROVEN to have occurred on the final
      attempt rather than on an earlier one. Without the ordinal assertion the test would
      pass identically if the release happened at attempt 2, which is the existing test.
    - Give it a doc comment stating that it covers the final-attempt boundary specifically,
      that `TestOpenConvergesWhenHolderCloses` covers release-between-attempts, and that
      `internal/uiserver` delegates the boundary here because `openLockRetrySleep` is
      unexported and unreachable from that package.

    Then, in `degrade_test.go`'s package comment, cite `TestOpenSucceedsOnTheFinalAttempt` BY
    NAME, state that the adjacency boundary is `graphstore.Open`'s budget and is owned there,
    and state that this file deliberately covers only the two sides of it behaviorally: one
    causally-released holder that must succeed, and one never-released holder that must
    degrade. Do NOT describe `TestOpenConvergesWhenHolderCloses` as a final-attempt test
    anywhere. No test in `internal/uiserver` may reach for `openLockRetrySleep` or
    reimplement a retry-aware clock.

    Write `TestDegradeIsIdempotent` as described in the behavior block.

    **Replace the starvation test.** The previous version,
    `TestConcurrentRPCsDoNotStarveAHolder`, required a holder acquiring the store WHILE N
    RPCs were in flight to succeed within `Open`'s budget. That requires a fairness property
    neither Pebble nor the retry loop provides: every open competes for one exclusive
    directory LOCK, each caller gets exactly five attempts on a fixed 100 ms backoff
    (`pebble_store.go:79-80`, loop at `:141`), and independent short-lived UI opens can
    legitimately reacquire between the holder's attempts. So the test could fail with NO
    retained handle present — a flake that would be read as a product defect — and it asserted
    more than SRV-04 requires.

    Write `TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete` instead. Issue N
    concurrent RPCs through a real Connect client, `Wait` on all of them, and only after every
    one has returned attempt `graphstore.Open` from the test process. Require it to succeed,
    and require it to succeed on the FIRST attempt with no retry consumed — instrument that by
    counting invocations of the wrapped `openEngine` var, or by asserting the acquisition is
    not preceded by any `ErrStoreLocked`. That is the property SRV-04 actually names: the UI
    retains no handle BETWEEN calls. It has a real failing input — a `uiService` caching a
    handle across calls leaves the lock held after the last RPC returns and the open fails —
    and it needs no fairness assumption at all, because by construction there is no contention
    left to be fair about. Say exactly that in the test's doc comment, including that the
    concurrent-with-in-flight-RPCs form was considered and rejected as requiring a fairness
    property Pebble does not promise. Keep the structural no-handle-field check
    (`TestUIServiceHoldsNoStoreTypedField`, plan 01-01) and the per-call open/close
    instrumentation (`TestDegradedRPCOpensTheStoreExactlyOnce`) as the primary SRV-04
    evidence; this test is the behavioral confirmation, not the whole case.
  </action>
  <verify>
    <automated>{ GOUT=$(go test -v -count=1 -race -run 'TestOpenSucceedsOnTheFinalAttempt' ./internal/graphstore/ 2>&1); GSTATUS=$?; GCOUNT=$(printf '%s\n' "$GOUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "graphstore exit=$GSTATUS PASS lines: $GCOUNT"; [ "$GSTATUS" -eq 0 ] || printf '%s\n' "$GOUT"; [ "$GSTATUS" -eq 0 ] && [ "$GCOUNT" -ge 1 ]; } && { OUT=$(go test -v -count=1 -race -run 'TestRPCsSucceedWhenAHolderReleasesWithinTheOpenBudget|TestNonStatusRPCsDegradeWhenAHolderNeverReleases|TestDegradedRPCOpensTheStoreExactlyOnce|TestDegradeIsIdempotent|TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete' ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 10 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -race -run 'TestRPCsSucceedWhenAHolderReleasesWithinTheOpenBudget|TestNonStatusRPCsDegradeWhenAHolderNeverReleases|TestDegradedRPCOpensTheStoreExactlyOnce|TestDegradeIsIdempotent|TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete' ./internal/uiserver/` emits at least 10 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - **Behavior-bullet homes, and ownership of the `uiserver` leg.** All five bullets in this task's behavior block name their own test function, and all five functions are counted by the derivation below, so no bullet is orphaned. Every atom in this leg names a test THIS task declares; no inherited test matches any of them. **Form A1 (own-test), causation.** The artifact under test is `withEngine`'s degrade path over the `openEngine` package var in `internal/uiserver/handlers.go`, wired by Task 1 of this plan and named in this plan's `files_modified`. Replace that path with a no-op that still compiles and returns a zero-valued response and `TestNonStatusRPCsDegradeWhenAHolderNeverReleases` turns RED on the `CodeUnavailable` and the `IndexingInProgress` detail it asserts for each of the eight rpcs, while `TestDegradedRPCOpensTheStoreExactlyOnce` turns RED on its counted invocations.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** `TestRPCsSucceedWhenAHolderReleasesWithinTheOpenBudget` declares one `t.Run` subtest per rpc (nine) = 10; `TestNonStatusRPCsDegradeWhenAHolderNeverReleases` declares one per non-`Status` rpc (eight) = 9; `TestDegradedRPCOpensTheStoreExactlyOnce` = 1; `TestDegradeIsIdempotent` = 1; `TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete` = 1. Total **22** against a floor of 10.
    - No test in `degrade_test.go` accepts more than one outcome for a given input. Asserted by review of each assertion, and stated in the SUMMARY: every degrade test names the single outcome it requires.
    - `TestRPCsSucceedWhenAHolderReleasesWithinTheOpenBudget` exercises all nine RPCs and requires a normal successful result from each. Its doc comment cites `internal/mcp/tools.go` and `internal/daemon/daemon.go` as the reason transient collisions are the realistic case.
    - `TestNonStatusRPCsDegradeWhenAHolderNeverReleases` exercises all eight non-`Status` RPCs and asserts `connect.CodeUnavailable` via `connect.CodeOf(err)` plus a detail that unmarshals to an `IndexingInProgress` for each.
    - `TestDegradedRPCOpensTheStoreExactlyOnce` reports the observed open count from the swapped `openEngine` var and asserts it is exactly 1. No test in this file asserts on elapsed wall-clock time.
    - `TestRPCsSucceedWhenAHolderReleasesWithinTheOpenBudget` releases its holder on a channel fired from the wrapped `openEngine` var, NOT on a timer. `rg -v '^\s*//' internal/uiserver/degrade_test.go | rg -o -e 'time\.(After|Sleep|AfterFunc|Tick|NewTimer|NewTicker)' | wc -l` prints `0`, positive-controlled in the same invocation on THAT SAME FILE by the pattern `openEngine` returning a non-zero count. Comment lines are stripped first, so the test's own prose about rejecting timers cannot invalidate its gate. `Tick` is in the alternation deliberately: `time.Tick` is the one timer constructor the earlier alternation omitted, and it is the easiest to reach for when a test needs a periodic poll.
    - The SANCTIONED bounding mechanism is named, because both degrade tests must still bound their work "within the budget" somehow and forbidding every `time.*` constructor without naming the alternative invites an executor to reach for a banned primitive or an unmentioned one. It is `context.WithTimeout` on the Connect client call's context, plus `go test`'s own `-timeout` for the outer bound. `context.WithTimeout` is NOT in the forbidden alternation and is not a coordination clock: it bounds a failure, it does not schedule a success. A test may bound how long it waits for a degrade to arrive; no test may schedule WHEN a holder releases. The two degrade tests state which of the two they are doing in their doc comments.
    - **Floor derivation for the `graphstore` leg (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** The `-run 'TestOpenSucceedsOnTheFinalAttempt'` atom matches exactly ONE test function, declared by THIS task, and that function declares no subtests: total **1** against a floor of 1. As with this phase's two other floor-1 gates (`01-02`'s locked-corpus gate and `01-03`'s pipelined-`tools/list` gate), the floor is doing no work on its own here — the `GSTATUS -eq 0` half of the conjunction is what this leg rests on, and the count of 1 exists solely to prove the pattern matched something rather than exiting 0 against nothing. The property itself is asserted by the source check below, not by the floor. The two existing `Open` tests in that file, `TestOpenConvergesWhenHolderCloses` and its sibling, do NOT match this atom, so this leg is scoped entirely to this task's own test. **Form A1 (own-test), causation, with the characterization case declared.** The artifact under test is `graphstore.Open`'s bounded retry loop together with `openLockRetryAttempts` and the `openLockRetrySleep` seam in `internal/graphstore/pebble_store.go` — cited in this task's read_first at lines 67-92 and NOT modified by this task, which is the characterization-assertion case this prohibition requires be declared rather than glossed. Replace the retry loop with a no-op that still compiles and returns on the first attempt and `TestOpenSucceedsOnTheFinalAttempt` turns RED on its assertion that the observed sleep ordinal equals `openLockRetryAttempts-1`. That assertion, not the floor of 1, is what this leg rests on.
    - `TestOpenSucceedsOnTheFinalAttempt` exists in `internal/graphstore/open_lock_test.go` AND has the property it is cited for. Existence alone is not the assertion — the previous form grepped only that a cited NAME was present, which is how a release-at-attempt-2 test came to be described as a final-attempt test. Both halves are checked:
      `go test -v -count=1 -race -run 'TestOpenSucceedsOnTheFinalAttempt' ./internal/graphstore/` exits 0 and emits at least 1 `--- PASS` line (the property, executed); and
      `rg -v '^\s*//' internal/graphstore/open_lock_test.go | rg -o -e 'openLockRetryAttempts[[:space:]]*-[[:space:]]*1' | wc -l` prints at least `2` — the release condition and the ordinal assertion — positive-controlled in the same invocation on THAT SAME FILE by `func TestOpenSucceedsOnTheFinalAttempt` returning `1`. A test that released at any earlier attempt would not reference the final ordinal twice. The pattern is deliberately whitespace-TOLERANT because `gofmt` decides the spacing and the correct answer is not one form: `gofmt` keeps a subtraction unspaced only when it is an operand of a lower-precedence operator or sits inside an index expression, and spaces it everywhere else. The nearest in-file precedent, `internal/graphstore/open_lock_test.go:43`, is the SPACED form and is in this task's `read_first`, so an executor following the file's own style would write a correct test against a red gate under the earlier unspaced-literal pattern — and the pressure would then be to edit the test to satisfy the grep. The failure direction was safe but it stalls execution on the exact criterion written to close H2.
    - `degrade_test.go`'s package comment cites `TestOpenSucceedsOnTheFinalAttempt` by name and states the boundary is owned in `internal/graphstore`. It does NOT describe `TestOpenConvergesWhenHolderCloses` as a final-attempt test: `rg -v '^\s*//' internal/uiserver/degrade_test.go | rg -o -e 'TestOpenConvergesWhenHolderCloses' | wc -l` is not the check (the citation lives IN a comment); instead the SUMMARY transcribes the package comment verbatim and states which test it names.
    - `go test -race -count=1 ./internal/graphstore/` still passes in full, so the added test did not disturb the two existing Open tests or the `openLockRetrySleep` seam's default.
    - `TestDegradeIsIdempotent` asserts equal codes and equal detail messages across two calls and asserts no file under the repository changed between them.
    - `TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete` awaits EVERY concurrent RPC to completion before attempting the holder's `graphstore.Open`, asserts that open succeeds on the first attempt with no retry consumed, and passes under `-race`. It asserts nothing about interleaving or fairness, and it makes no wall-clock assertion. Its doc comment records that the concurrent-with-in-flight-RPCs form was rejected because Pebble's single exclusive LOCK and the fixed five-attempt backoff give no fairness guarantee, so that form could fail with no retained handle present.
  </acceptance_criteria>
  <done>Every degrade assertion has a failing input, the no-second-retry property is counted rather than timed, and the two sides of `graphstore.Open`'s budget are each covered by a test that requires one specific outcome.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-process evidence, honestly scoped, and the phase verification close-out</name>
  <files>internal/uiserver/degrade_test.go</files>
  <read_first>
    - internal/uiserver/degrade_test.go — the tests Task 2 wrote
    - internal/cli/daemon.go — how `codegraph daemon start` acquires the store, and specifically that it opens inside its flush and closes rather than holding across the process lifetime
    - internal/mcp/tools.go around line 71 — the per-tool-call open, confirming `serve --mcp` holds no persistent handle either
    - internal/cli/serve.go around lines 205-250 — `serve --mcp`'s startup reconcile and its lock handling
    - .planning/ROADMAP.md — Phase 1 success criterion 3, which names `codegraph daemon` / `serve --mcp` explicitly, and its two clauses
    - .planning/STATE.md — the standing rule that a transcript grep is a claim about the grep, not about the product, and that an absence must be positive-controlled
  </read_first>
  <action>
    Success criterion 3 names real processes: "With `codegraph daemon` / `serve --mcp`
    already holding the store, `codegraph ui` still serves every RPC and holds no handle
    between calls; a re-index that outlasts `graphstore.Open`'s retry budget renders as
    'indexing in progress'". The two clauses partition the space rather than contradict:
    the first is the transient case, the second is the sustained case.

    There is a trap in exercising this, and the review caught it. **Neither `codegraph
    daemon` nor `serve --mcp` holds the store continuously** — the daemon opens inside its
    flush and closes, and the MCP server opens per tool call. So simply starting them and
    watching will most likely observe nothing at all: every RPC will succeed, which is
    correct behavior but is not evidence about the degrade path. Design the exercise around
    that fact rather than expecting a state that will not appear.

    Run three exercises by hand against real binaries and record each one separately.

    **Exercise A — the realistic concurrent case.** Build `codegraph`, index a fixture
    repository, start `codegraph daemon start` against it in one terminal, start `codegraph
    ui` against it in another, and issue every one of the nine RPCs from a Connect client at
    the printed URL while the daemon is running and syncing. Record the verdict for each
    RPC. The EXPECTED result is that every one returns a normal successful result, because
    the daemon's collisions are transient and `Open`'s bounded retry rides them out. A
    `CodeUnavailable` here is worth investigating, not shrugging at; a `CodeInternal` is a
    defect and is fixed, not annotated. Repeat with `codegraph serve --mcp` as the
    concurrent process.

    **Exercise B — the sustained case, deliberately constructed.** Because nothing ships a
    long-lived holder, construct one: a small throwaway program that calls
    `graphstore.Open` on the fixture store directory and sleeps. Start it, then issue every
    non-`Status` RPC and `GetStatus`. The EXPECTED result is `CodeUnavailable` with an
    `IndexingInProgress` detail for the eight, and a successful degraded response for
    `GetStatus`. Record each verdict. Say plainly in the SUMMARY that this holder was built
    for the exercise and does not correspond to any shipped process — the sustained case is
    real (a long re-index is exactly it) but is not reproducible from the shipped binaries
    alone.

    **Exercise C — no retained handle.** Stop the UI and confirm a fresh `graphstore.Open`
    on the store directory succeeds immediately, proving no handle was retained.

    Write the full transcript of all three into the SUMMARY: the commands, the per-RPC
    verdicts, the observed states, and the post-shutdown open. Note explicitly which parts
    were observed and which were not — an absence claimed from a transcript is a claim about
    the transcript, not about the product, so anything not directly observed is recorded as
    not observed rather than as absent. In particular, if Exercise A never produces a
    degrade, record that as "not observed, and expected not to be observed, because neither
    holder is long-lived" rather than as evidence that the degrade path is unreachable —
    Exercise B is what covers it.

    If any exercise surfaces behavior the in-process tests miss, do NOT declare a test for
    it in this task. Record it in the SUMMARY under the heading `## Deferred: build-tagged
    integration coverage`, naming the exercise, the RPC, and the observed behavior, and add
    one package-doc comment line in `internal/uiserver/degrade_test.go` pointing at that
    heading. That comment is the only edit this task makes to the file, and it is why the
    file is in this task's `<files>`. No plan in this phase owns the deferred test: 01-11 is
    the last plan of the last wave, and a build-tagged multi-process harness needs
    fixture-repo and CI wiring this phase never planned and cannot gate. Route the deferred
    item to the project backlog with `/gsd-capture`. This is not tidiness — declaring even
    one test function here would move this task out of form A3 at execution time and
    invalidate the floor derivation the task publishes, so the governing form is fixed at
    plan time and is A3 on every branch. If no exercise surfaces anything, record that
    finding and the reasoning rather than describing a test that would only restate an
    in-process one — "we ran it and it matched" is a result worth recording.
  </action>
  <verify>
    <automated>go build ./... && { OUT=$(go test -v -count=1 -race ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 60 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains Exercise A's transcript with a per-RPC verdict for all nine RPCs under a `codegraph daemon` holder, and again under a `serve --mcp` holder.
    - The SUMMARY contains Exercise B's transcript with a per-RPC verdict for all nine RPCs under a deliberately constructed long-lived holder, and states plainly that the holder was built for the exercise and corresponds to no shipped process.
    - The SUMMARY contains Exercise C's post-shutdown `graphstore.Open` result.
    - No RPC returned `CodeInternal` in any exercise. A `CodeInternal` is recorded as a defect and fixed, not annotated.
    - Anything not directly observed is recorded as not observed, never as absent; if Exercise A produced no degrade, that is recorded with the reason rather than as a gap.
    - An exercise whose transcript records no verdict for one or more of the nine RPCs is incomplete and is a STOP CONDITION, not a note. A verdict of `not observed, and expected not to be observed` IS a recorded verdict and satisfies this; a missing row does not.
    - `go test -v -count=1 -race ./internal/uiserver/` emits at least 60 `--- PASS` lines across the whole package, and the command exits 0 — both conditions, never either alone.
    - **Form A3 (observation), stated because this task declares no test function at all — unconditionally, decided at plan time rather than at execution time.** The action forbids declaring one on EVERY branch, including the branch where an exercise surfaces behavior the in-process tests miss: that finding is recorded in the SUMMARY under `## Deferred: build-tagged integration coverage`, pointed at by one package-doc comment line, and routed to the backlog, and no plan in this phase owns the resulting test. A3 eligibility is therefore established by reading the plan, not by what an exercise happens to produce, and the floor derivation below can never be invalidated mid-execution. Its deliverable is three recorded exercise transcripts against real processes — not code and not an assertion — so no `--- PASS` floor can ever be its evidence. All four of A3's requirements are discharged. (1) **Named measurements.** Each exercise is scored by verdict-bearing measurements taken from named commands: a per-RPC verdict for all nine RPCs under each holder in Exercises A and B, and the post-shutdown `graphstore.Open` result in Exercise C, all recorded verbatim in the SUMMARY. (2) **Stop condition.** An exercise whose transcript records NO verdict for one or more of the nine RPCs is incomplete and is a STOP CONDITION, and a `CodeInternal` in any exercise is a defect that is fixed rather than annotated. Note the distinction that matters here: a verdict of `not observed, and expected not to be observed` IS a recorded verdict and satisfies the requirement — Exercise A is expected to produce exactly that for the degrade path — so the stop condition is a MISSING row, never an unobserved outcome. (3) **Strongest executable leg available.** That is this whole-package `internal/uiserver` sweep, whose intended subject is deliberately and explicitly the UNION of every `internal/uiserver` test the phase built; it is the phase's only whole-package regression sweep and that is what it is for. (4) **What it CAN and CANNOT catch.** It CAN catch a regression in any unconstrained test in `internal/uiserver` — which today is all of them, verified rather than assumed (`rg -l '^//go:build' internal/uiserver/` returns nothing, and no plan in this phase adds a constrained file to that package), so the sweep's subject really is the whole package. It CANNOT execute any file carrying a `//go:build` constraint: the gate runs `go test -v -count=1 -race ./internal/uiserver/` with NO `-tags` flag, and the go tool excludes a constrained file from the build entirely rather than skipping it at run time — verified empirically in cycle 9 rather than reasoned about, an untagged `go test -v` emitting only the unconstrained test while the identical command plus `-tags integration` emits both. That limit is exactly why this task adds no build-tagged test on any branch: a gate that cannot execute what a task produces cannot score it, and an A3 form whose CAN-list overstated its reach would forfeit the honest-disclosure warrant that admits the form in the first place. It CANNOT catch this task doing nothing at all, and it makes no claim whatever about this task's own deliverable, which is three transcripts. A gate whose subject is another plan's tests is legitimate only when the plan says so and says why, and it may never be the only thing scoring a task that DOES declare tests of its own — this task declares none on any branch, which is what admits form A3 here.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** This is a whole-package run, so the floor is the SUM of every `internal/uiserver` test this phase built, each derived in its own plan: 01-01 contributes 28 (`TestOriginHostGuard` 1 + 20, `TestOriginHostGuardAdmitsOriginlessGET` 1, and six server-lifecycle functions); 01-08 contributes 23 (8 + 7 + 8); 01-09 contributes 19 (10 + 6 + 3); 01-10 contributes at least 16 in this package (its truncation lines, plus the 7 lines of its five `TestUIServiceSourceBlob*` functions and their two subtests, which plan 01-10 now declares by name); and this plan contributes 31 (9 + 22). A conservative lower bound is **111**, against a floor of 60. The floor is deliberately set well below the derived total because this gate spans five plans and must not turn red when a later plan legitimately renames or consolidates a subtest — a genuine regression still fails through the `STATUS -eq 0` half of the conjunction. What the floor DOES catch is the failure this gate exists for: a `-run`-less package run that somehow executes almost nothing.
    - `task test:unit` and `task test:golden` both pass; the goldens still report `attempted=26 completed=26 matched=26`.
    - `task proto:drift` passes with an unchanged compared-count.
  </acceptance_criteria>
  <done>The coexistence property is proven against real processes for the transient case and against a purpose-built holder for the sustained case, with each exercise's scope stated honestly.</done>
</task>

</tasks>

## Review feedback dispositions

| Finding | Severity | Disposition |
|---|---|---|
| Cycle-2 MEDIUM: the transient-lock case still coordinates on a wall clock | MEDIUM | **Incorporated by replacing the clock with a causal edge.** The holder no longer releases on a timer sized against `graphstore.Open`'s budget; it blocks on a channel fired from the wrapped `openEngine` package var on its first invocation, establishing a happens-before edge between "an RPC is about to open" and "the holder has begun releasing". The only remaining failure mode is a single `Close()` outlasting the entire retry budget, which is a real defect rather than a scheduling artifact. A negative source gate asserts no timer call appears in `degrade_test.go`, same-file positive-controlled, with `time.Tick` added to the alternation and `context.WithTimeout` named as the sanctioned bounding mechanism. The exact final-attempt boundary stays delegated to `internal/graphstore`'s existing event-synchronized test, cited BY NAME with an instruction to resolve the real name from `open_lock_test.go` and to stop rather than cite a test that does not exist. The alternative offered — dropping to one integration smoke case — was not needed once the coordination became causal, and both sides of the budget keep a test that names one required outcome. |
| Cycle-2 HIGH C2-2: automated PASS-count gates discard the test command's exit status | HIGH | **Incorporated across every gate in this plan.** A shell pipeline's `$?` is the LAST command's, so `COUNT=$(go test ... | rg '--- PASS' | wc -l); [ "$COUNT" -ge N ]` passed with N passing subtests plus a `--- FAIL`. Every `<automated>` gate now captures `go test`'s output into `OUT` and its status into `STATUS` BEFORE any pipe, counts from `OUT`, dumps `OUT` on failure, and requires `STATUS -eq 0` AND the `--- PASS` floor. The conjunction is the rule: the count alone is vacuous when a test fails, the status alone is vacuous when `-run` matches nothing. A plan-level prohibition against the bare count-only form is in `must_haves.prohibitions`. The only scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence. |
| `TestDegradePrecedence` describes a structurally impossible state | HIGH | **Incorporated.** The impossible fixture is gone. Precedence is tested at `classifyDegrade` with a constructed multi-target error, and the filesystem impossibility is recorded in the function's doc comment and in a must_have. |
| `TestRPCsCoexistWithAConcurrentStoreHolder` accepting "succeed OR degrade" is vacuous under rule `84d1gfpywd` | MEDIUM (adjudication re-aim) | **Incorporated.** Split into two tests, each requiring one named outcome, discriminated by whether the holder releases inside `graphstore.Open`'s budget. A prohibition forbids the "succeeds or degrades" shape anywhere in this plan. |
| "Locked-store RPCs cannot serve real results" is a requirements mismatch | HIGH (Codex) → DOWNGRADED by adjudication | **Rejected, per the adjudication.** Nothing in this repo holds the store long-term (`internal/mcp/tools.go` opens per call; `internal/daemon/daemon.go` opens only inside its flush), and criterion 3's two clauses partition rather than contradict. The plan encodes that reading: the transient case must SUCCEED and the sustained case must DEGRADE, each asserted separately. Not reintroduced as a HIGH. |
| Timing assertions around the 400 ms budget are flaky | MEDIUM | **Incorporated, with a correction.** The suggested `openLockRetrySleep` seam is unexported in `internal/graphstore` and unreachable from `internal/uiserver`, so the fix here is to count `openEngine` invocations instead — deterministic, and a direct proof of D-15. The exact final-attempt boundary is delegated to `internal/graphstore` — **but see cycle-3 H2 below: no such test existed, so this plan now WRITES `TestOpenSucceedsOnTheFinalAttempt` there rather than citing one.** |
| "Real holder plus trigger a re-index" may never observe transient indexing | MEDIUM | **Incorporated.** Task 3 is restructured into three exercises with the fact that no shipped process holds the store long-term stated up front, a deliberately constructed holder for the sustained case, and an explicit instruction to record a non-observation as such. |
| Banning any PID-like digit sequence in the detail message is brittle | MEDIUM | **Incorporated.** Replaced with exact equality against the named constant plus a positive-controlled path-separator check. The digit-run ban is dropped. |
| Duplicating `truncateOnRuneBoundary` creates two security-relevant copies | LOW | **Incorporated in plan 01-10**, which extracts it to `internal/textutil`. |
| Old 01-07 was 95k with three tasks plus a checkpoint | MEDIUM (cross-plan) | **Split.** RPC-05 is plan 01-10; this plan is SRV-04 only, 3 tasks, no checkpoint. The field numbers it uses were allocated by plan 01-08. **Correction (cycle 3, H1):** the earlier claim that they "are already pinned by plan 01-09's fixture" was FALSE — `GetStatusResponse` appeared zero times in 01-09, whose fixture was scoped to fields 01-09 itself declared, and fields 8 and 9 did not exist until this wave. This plan now extends that fixture by exactly two entries, in the wave the fields first exist, asserted against the `uiProtoFieldFixtureLenAtPlan0110 + 2` constant chain. No checkpoint is still correct — the allocation decision was made and gated in 01-08 — but the pinning is this plan's work, not a pre-existing fact. |
| Cycle-3 H1: `01-11:221` asserts 01-09's test "still passes with fields 8 and 9 landing where they were allocated" (vacuous — it does pass, and asserts nothing about them) and `01-11:429` states they "are already pinned by plan 01-09's fixture" (false) | HIGH | **Incorporated: the mechanism is fixed AND the false claim is deleted.** This plan now EXTENDS 01-09's fixture by exactly two entries — `GetStatusResponse.store_exists = 8` and `.indexing_in_progress = 9` — in Task 1, in the same commit that declares the fields, mirroring plan 01-10's `+9` pattern, and asserts the growth against `uiProtoFieldFixtureLenAtPlan0110 + 2`. `TestUIProtoFieldNumbersAreStableAndUnique` is added to Task 1's `-run` pattern so the gate that scores this task is the gate that runs it, and the floor rose from 8 to 9. The vacuous acceptance bullet is replaced with one that requires the fields ASSERTED, not merely present; the false statement in this table's split row is corrected in place with the reason it was false. Separately, 01-09's fixture scope rule was corrected at the root so a message declared in an earlier wave can no longer be covered by nothing. |
| Cycle-3 H2: this plan delegates the final-attempt boundary to a `graphstore` test that does not have the claimed property, and the stop-instruction cannot fire | HIGH | **Incorporated by writing the missing test rather than by weakening the claim.** The review's diagnosis is confirmed: `TestOpenConvergesWhenHolderCloses` (`open_lock_test.go:62`) IS event-synchronized but releases at attempt 2 — its own doc comment says "some remaining attempt (budget: 5)" — and the other Open test is wall-clock and is the never-releases case, so a NAMED event-synchronized test is found and the stop-instruction never fires. This plan now adds `TestOpenSucceedsOnTheFinalAttempt` to `internal/graphstore/open_lock_test.go` (both files are in `files_modified`; no other plan owns `internal/graphstore`, so there is no same-wave ownership conflict). It uses the same `openLockRetrySleep` seam plus one addition — an acknowledgement channel — which is what makes the FINAL attempt reachable: the existing test's drain-loop trick relies on a LATER sleep blocking until `Close` returns, and on the final sleep there is no later sleep. It asserts both that `Open` succeeded AND that the observed sleep ordinal equals `openLockRetryAttempts-1`, so the success is proven to be on the final attempt rather than an earlier one. The acceptance criterion checks the PROPERTY (the test runs and passes, and the file references the final ordinal twice, same-file positive-controlled), not merely that a cited name exists — which is the specific weakness that let a false attribution through. The probe stays edge-tagged and the ledger still reconciles to 21. |
| Cycle-3 M9: `TestConcurrentRPCsDoNotStarveAHolder` requires a fairness property Pebble and the retry loop do not provide | MEDIUM | **Incorporated.** Replaced with `TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete`: every concurrent RPC is awaited to completion, and only then is the holder's `graphstore.Open` attempted, which must succeed on the FIRST attempt with no retry consumed. That is the property SRV-04 actually names — no handle retained BETWEEN calls — it has a real failing input (a cached handle leaves the lock held after the last RPC returns), and it needs no fairness assumption because by construction there is no contention left to be fair about. The doc comment records that the in-flight form was considered and rejected, with the reason. The structural no-handle-field check and the per-call open-count instrumentation remain the primary SRV-04 evidence. |
| Cycle-3 L3: the forbidden-timer alternation omits `time.Tick`, and the sanctioned bounding mechanism is nowhere named | LOW | **Incorporated.** `Tick` is added to the alternation, the control is now same-file, and `context.WithTimeout` (plus `go test -timeout` for the outer bound) is named as the sanctioned mechanism with the distinction that matters stated: a test may bound how long it WAITS for a degrade, but no test may schedule WHEN a holder releases. |
| Cycle-3 M4: the `git status --porcelain` gate passes vacuously on a missing path | MEDIUM | **Incorporated.** Prefixed `test -d internal/uiproto &&`. One of the nine phase-wide sites. |
| Cycle-3 L1: the `--- PASS` counting convention is never stated anywhere in the phase, and echo labels contradict each other | LOW | **Incorporated phase-wide.** A byte-identical `## Gate conventions (phase-wide canon)` section now sits immediately above `<tasks>` in ALL ELEVEN plans. It states the convention once and canonically — one `--- PASS` line per test function PLUS one per `t.Run` subtest — mandates the single echo label `PASS lines:`, restates the D-04 conjunction with its one scoped exception, and enumerates the five vacuity shapes cycle 3 found. Every floor in this plan now carries an explicit **Floor derivation** bullet showing the arithmetic, so a reviewer checks the derivation rather than re-deriving it. Label census: six distinct labels existed across the phase (`PASS subtests:`, `PASS tests:`, `PASS:`, `post-restore PASS lines:`, `PASS lines across both tests:`, `uiserver PASS lines:`); all six were normalised to `PASS lines:`. |
| Cycle-5: terminate the SCOPE class — a gate can satisfy all five shape prohibitions and still be unable to fail | HIGH (phase-wide) | **Incorporated phase-wide as a sixth, SEMANTIC prohibition**, identical in all eleven plans and restated in the `## Gate conventions` canon. The five existing prohibitions are all shape-based: they catch a malformed command. A gate can be well-formed, parse, honor exit status and carry a same-file positive control and STILL be unable to fail, because its floor is already cleared by test functions belonging to an EARLIER plan that share its selection pattern — the pattern matching none of this task's own tests then costs nothing. The sixth prohibition bans exactly that, with two checkable requirements: (a) OWNERSHIP — every gate carries at least one leg scoped to test functions the task itself declares, with a floor derived from that leg alone, and any pattern that also matches an earlier plan's tests must say so and say why those tests are the intended subject; (b) HOMING — every behavior bullet is named by, or names, a test function or subtest the task's own floor derivation counts, or the derivation states in writing that it is covered by a named criterion or deliberately out of scope. Unlike the five shape prohibitions it is not satisfiable by rewriting a command; it requires a structural leg and a written mapping. Every floor derivation in this plan now carries its ownership statement, and every task with a behavior block carries its bullet-home mapping. |
| Cycle-5 M2: the H2 property grep `openLockRetryAttempts-1` is gofmt-FORM-DEPENDENT, and the same file's existing idiom is the non-matching spaced form | MEDIUM | **Incorporated, with the corrected diagnosis.** Codex called the grep impossible after `gofmt`; that is overstated and the correction changes the fix. Measured: `gofmt` keeps the subtraction unspaced only when it is an operand of a lower-precedence operator or sits inside an index expression, and spaces it everywhere else — so the pattern was satisfiable, but only if both cited sites happened to be direct comparison operands, which the plan never said. The nearest in-file precedent (`open_lock_test.go:43`) is the SPACED form and is in this task's `read_first`, so an executor following the file's own style would write a correct test against a red gate and the pressure would be to edit the test to satisfy the grep. The pattern is now whitespace-tolerant, keeping the same-file declaration positive control and the floor of 2. The failure direction was safe — a false FAIL, not vacuity — but it stalled execution on the exact criterion written to close H2. |
| Cycle-5 M3: the `graphstore` leg's floor of 1 carries no derivation — the one exception to "all 26 floors carry derivations" | MEDIUM | **Incorporated.** A derivation bullet is added stating that the atom matches exactly one test function with no subtests, total 1 against a floor of 1, that the floor does no work on its own and the `GSTATUS -eq 0` half is what the leg rests on, and that the property itself is asserted by the source check rather than by the count — the same thing this phase's two other floor-1 gates already say. It also records that the file's two existing `Open` tests do not match the atom, so the leg is scoped to this task's own test. |
| Cycle-5 L3: "…and no error classification in either file compares against an error message string" is welded onto an executable presence check with no pipeline of its own | LOW | **Incorporated.** The clause is split into its own executable, positively-controlled bullet over the same two files: the string-comparison forms must print `0`, positive-controlled in the SAME invocation over the SAME two files by the sentinel-classification form returning a NON-ZERO count. Comment lines are stripped first so the files' own doc comments about not matching on message text cannot turn the gate red. |
| Cycle-3: terminate the vacuity class rather than fixing instances | — | **Incorporated phase-wide.** Five new descriptor-less entries in `must_haves.prohibitions`, identical in all eleven plans, forbid: a `git status --porcelain` check without a `test -d` existence guard; a positive control that targets a different file than the assertion; a count-matching regex that accepts zero; an unanchored identifier grep; and an unranged `git diff` emptiness assertion. Each is a shape cycle 3 found in this phase's own gates, so a future author cannot reintroduce one without violating a stated prohibition. |
| Cycle-7 H-7-1: the sixth prohibition's two halves do not entail its own headline — OWNERSHIP constrains who DECLARES the counted tests, not what those tests ASSERT, and HOMING accepts a named criterion that no gate executes | HIGH (phase-wide) | **Incorporated phase-wide by AMENDING the sixth prohibition, not replacing it.** The reviewer and Codex independently constructed the same counterexample: a task declaring one self-contained arithmetic test, a leg selecting only that test at a floor of 1, and every behavior bullet homed to a prose criterion, satisfies both halves literally while the deliverable is entirely absent. The prohibition was STRUCTURAL where it had to be CAUSAL. Half (a) is now OWNERSHIP AND CAUSATION: a qualifying leg must turn RED when the deliverable is REPLACED BY A NO-OP — an implementation that still compiles and keeps the same signature but does nothing and returns zero values, or for a generated or declarative artifact its empty or default-valued version. Replacement, not deletion, is the standard, because deletion is satisfied by a mere compile dependency and only replacement separates a test that ASSERTS an artifact's behavior from one that merely MENTIONS it. Three forms are named, and every floor derivation in this plan now states which one it uses: A1 own-test, which must name its artifact under test; A2 direct-artifact, which EXECUTES the deliverable and scores a number read out of that artifact's own output; and A3 observation, open only to a task that declares no test function at all, which must carry a named measurement, a written STOP CONDITION for a vacuous measurement, the strongest executable leg available to it, and a plain statement of what that leg cannot catch. A leg whose pattern also matches an earlier plan's tests must additionally show that the inherited count alone falls BELOW the floor, or it does not qualify. Half (b) now requires a criterion's command to appear in the task's `<automated>` gate before that criterion counts as a home — prose is not a home. The five shape prohibitions from cycle 4 are untouched; the amended sixth is byte-identical across all eleven plans and the `## Gate conventions` canon restates it once, still one digest across all copies. |
| Cycle-7 M-7-1 (applied here as well as in `01-05`): Task 3 declares no test function, so half (a) as written did not permit its gate either | MEDIUM (found by re-audit) | **Incorporated.** The cycle-7 review named only `01-05`, but re-auditing every task against the strictly-stronger amended rule found the same shape here and in `01-07` Task 2. Task 3's ownership bullet is restated as form A3 and discharges its four requirements: the named measurements are a per-RPC verdict for all nine RPCs under each holder plus Exercise C's post-shutdown `graphstore.Open` result; the STOP CONDITION — new, and added by this pass — is a transcript that records NO verdict for one or more of the nine RPCs, with the distinction stated that a `not observed, and expected not to be observed` verdict IS a recorded verdict and satisfies the requirement, so the stop condition is a MISSING row and never an unobserved outcome; the strongest executable leg is the whole-package `internal/uiserver` sweep, whose subject remains explicitly the union of every test the phase built; and the honest limit is written out rather than glossed. Task 1's inherited-atom justification also gained the arithmetic the amended clause now requires — the inherited test alone supplies 1 against a floor of 9, which is below it. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent `codegraph` processes → one Pebble directory lock | `codegraph daemon`, `serve --mcp` and `codegraph ui` contend for the same store directory by design. |
| degrade detail message → browser | An unauthenticated loopback caller receives the detail. Anything embedded in it crosses into a web page. |
| failed open → user-visible state | What the human is told when the index is busy is decided by one classification. A broader check would let an unrelated failure present as "indexing in progress". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | `IndexingInProgress.message` | medium | mitigate | The message is a fixed generic named constant. Asserted by exact equality against that constant and by a positive-controlled check that it carries no path separator. No process id, lock-file name or absolute path is embedded. |
| T-01-10 | Denial of Service | store lock contention | medium | accept | No second retry layer above `graphstore.Open`'s bounded budget (D-15). Adding one would change the exact condition SRV-04 names. Past-budget contention degrades to `CodeUnavailable` rather than blocking, and the no-second-layer property is proven by counting store opens per degraded RPC. |
| T-01-24 | Denial of Service | store handle retained across calls | high | mitigate | Proven structurally (no handle field on `uiService`, plan 01-01), behaviorally in-process (`TestAHolderAcquiresTheStoreAfterConcurrentRPCsComplete` under `-race`, which awaits every concurrent RPC to completion and then requires the holder's open to succeed on the first attempt — no fairness assumption), by instrumentation (`TestDegradedRPCOpensTheStoreExactlyOnce`), and against real `codegraph daemon` and `serve --mcp` processes with the transcript recorded. |
| T-01-25 | Tampering | error classification | medium | mitigate | Classification is `errors.Is` against the exported `graphstore.ErrStoreLocked` and `query.ErrNotInitialized` sentinels only, in an explicit ordered check. String matching is excluded, so a permission error cannot masquerade as lock contention — the reasoning `internal/cli/serve.go`'s own comment already records. |
| T-01-39 | Repudiation | degrade tests that cannot fail | high | mitigate | A plan-level prohibition forbids any "succeeds or degrades" assertion; each test names one required outcome, discriminated by a condition the test controls. The SUMMARY must state that every degrade test names its single outcome. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race -count=1 ./internal/uiserver/...` passes with at least 60 `--- PASS` lines.
- `task proto:drift` passes with an unchanged compared-count.
- `task test:unit` passes; `task test:golden` reports `attempted=26 completed=26 matched=26`.
- `go vet ./internal/uiserver/... ./internal/uiproto/...` is clean.
- All three multi-process exercise transcripts are present in the SUMMARY with their scopes stated.
</verification>

<success_criteria>
- With a concurrent `codegraph daemon` or `serve --mcp`, `codegraph ui` serves every RPC normally and holds no handle between calls.
- A hold that outlasts `graphstore.Open`'s retry budget renders as `CodeUnavailable` with a typed `IndexingInProgress` detail on every non-`Status` RPC, and `Status` still answers with a degraded but successful response.
- A repository with no `.codegraph/` reports a distinct not-initialized state, and the precedence between the two is encoded as an ordered check and tested where it is decidable.
- No second retry layer exists, proven by counting store opens rather than by measuring time.
- Every degrade assertion has a failing input.
</success_criteria>

<output>
Create `.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-11-SUMMARY.md` when done
</output>
