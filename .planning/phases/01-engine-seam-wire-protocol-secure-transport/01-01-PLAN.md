---
phase: 01-engine-seam-wire-protocol-secure-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - buf.yaml
  - buf.gen.yaml
  - go.tool-proto.mod
  - go.tool-proto.sum
  - go.mod
  - go.sum
  - Taskfile.yml
  - internal/schema/graph.pb.go
  - internal/uiproto/uiv1/ui.proto
  - internal/uiproto/uiv1/ui.pb.go
  - internal/uiproto/uiv1/uiv1connect/ui.connect.go
  - internal/uiserver/originguard.go
  - internal/uiserver/originguard_test.go
  - internal/uiserver/server.go
  - internal/uiserver/server_test.go
  - internal/uiserver/handlers.go
  - internal/cli/ui.go
  - internal/cli/ui_test.go
  - internal/cli/root.go
autonomous: true
requirements: [SRV-02, SRV-01, SRV-03, RPC-01, RPC-02]

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A request whose Host header is not one of the three exact admitted literals is answered 403 by middleware that runs before the mux is reached, with no RPC handler invoked (SRV-02)."
    - "A request carrying an Origin header that is not one of the three exact admitted origins is answered 403; a GET carrying no Origin header at all is admitted (SRV-02, the over-rejection failure mode research predicted)."
    - "Host and Origin are validated as a PAIR, not independently: a present Origin must equal `\"http://\" + r.Host` exactly, so `Host: 127.0.0.1:<port>` carrying `Origin: http://localhost:<port>` is REJECTED even though both members are separately admitted literals. ROADMAP success criterion 2 requires the three spellings be admitted by exact match rather than treated as interchangeable, and two independently-checked allowlists treat them as interchangeable (SRV-02)."
    - "`(*Server).Serve(ctx)` runs the blocking `http.Server.Serve` in a goroutine over a BUFFERED capacity-1 error channel and `select`s on that channel against `ctx.Done()`, shutting down under a `context.Background()`-derived 5-second budget and normalizing `http.ErrServerClosed` to nil — so cancellation is reachable, the serve goroutine cannot leak, and a clean Ctrl-C exits zero (SRV-01)."
    - "`uiserver.Listen` binds the listener and publishes the concrete bound port BEFORE anything serves, so `codegraph ui` prints a URL that is already connectable rather than one derived from an unbound `:0` (SRV-01, D-07)."
    - "`codegraph ui` prints a `http://127.0.0.1:<port>` URL on stdout and a Connect client at that exact printed URL gets a real GetStatus response from the repository's own index while the command is still running (SRV-01, RPC-02)."
    - "The `ui` command registers exactly two flags — `path` and `no-open` — so no v1 flag exposes a bind address or an auth credential (SRV-03, D-07/D-08)."
    - "Browser-launch policy and the launch itself live in `internal/cli` only; `uiserver.Options` carries no browser field, so there is exactly one place that can open a browser (SRV-01, review MEDIUM 01-01)."
    - "`uiService` holds no `*query.Engine`, `graphstore.GraphStore` or `graphstore.Reader` typed field — asserted structurally by reflecting over its field types, complementing the behavioral no-retained-handle test (SRV-04's per-call discipline)."
    - "The guard is stateless and evaluated per request: N concurrent requests, mixed admitted and foreign, each receive the verdict their own Host/Origin earns, under `-race` (SRV-02 / concurrency edge)."
    - statement: "A `buf generate` run that is interrupted or run concurrently with another leaves no partially-written committed generated file that compiles — detectable only by the BLD-04 drift guard landing in plan 01-07, not by an assertion this plan can write (RPC-01 / concurrency edge)."
      verification: backstop
  artifacts:
    - internal/uiproto/uiv1/ui.proto
    - internal/uiproto/uiv1/ui.pb.go
    - internal/uiproto/uiv1/uiv1connect/ui.connect.go
    - internal/uiserver/originguard.go
    - internal/uiserver/server.go
    - internal/uiserver/handlers.go
    - internal/cli/ui.go
    - buf.gen.yaml
  key_links:
    - "originHostGuard wraps the whole http.ServeMux, not an individual handler and not a connect interceptor — an interceptor runs after routing and parsing have begun."
    - "`Listen` binds; `Serve` serves. The port only exists after the bind, so URL publication and browser launch both sit between them. This is the ordering the whole command depends on."
    - "The Connect handler calls query.OpenAt per request and closes the returned io.Closer before returning; no Engine or GraphStore handle survives the call (SRV-04's per-call discipline)."
    - "internal/cli/root.go's root.AddCommand list is the single registration point — an unregistered ui.go is invisible."
    - "`task proto:gen` regenerates BOTH proto surfaces. The pre-existing `internal/schema/graph.pb.go` has never been regenerated by a checked-in toolchain, so its generated header will be rewritten on this first run — that diff is this plan's to absorb, verify header-confined, and commit."
  prohibitions:
    - "Never gate a task on a bare `go test ... | rg '--- PASS' | wc -l` count whose exit status is discarded. A shell pipeline's status is the LAST command's, so N passing subtests plus one `--- FAIL` satisfies a count-only gate. Every automated gate captures `go test`'s output and status BEFORE any pipe and requires BOTH: status 0 AND the `--- PASS` floor. The count alone is vacuous when a test fails; the status alone is vacuous when the `-run` pattern matches nothing. The sole scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence."
    - "Never renumber or reuse a protobuf field number once it has shipped; retiring a field means adding its number to a `reserved` clause."
    - "Never revert a working-tree mutation with `git checkout --` on a shared or dirty tree; capture an exact reverse patch or work in an isolated `git worktree` scratch tree."
---

<objective>
Stand up the phase's transport spine: an exact-match Origin/Host control, a protobuf
codegen pipeline, and a `codegraph ui` process that binds first, publishes its real URL,
and serves one read-only RPC end-to-end over that URL.

Purpose: this is the phase's tracer. It proves the whole architecture — cobra command →
ephemeral loopback bind → published URL → rebinding guard → Connect handler →
`internal/query.Engine` → typed response — on one path before any of it is built out. The
ROADMAP's blocking order is why the guard is Task 1 rather than part of the tracer: "no RPC
handler may ship before SRV-02's Origin/Host control exists", so the control is a
committed, RED-proven artifact before a handler exists to protect.

Output: a working `codegraph ui`, a generated `codegraph.ui.v1` wire surface, a
`Listen`/`Serve` lifecycle whose printed URL is provably connectable, and a guard whose
rejection has been watched fail against a build without it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md
</context>

## Artifacts this phase produces

Every symbol below is **new in this phase** — none of it exists on `main` today. Drift
verification must treat these as created here, not as pre-existing symbols that moved.

**Created by this plan (01-01):**

- Package `internal/uiproto/uiv1`, proto package `codegraph.ui.v1`, file `ui.proto`
- `service UIService` with rpc `GetStatus`
- Messages `GetStatusRequest`, `GetStatusResponse`, `IndexingInProgress`
- Generated `ui.pb.go` and `uiv1connect/ui.connect.go` (`UIServiceHandler`, `NewUIServiceHandler`, `NewUIServiceClient`, `UIServiceName`, `UIServiceGetStatusProcedure`)
- Package `internal/uiserver`: `Options` (`RepoPath`, `Addr`), `DefaultAddr`, `Server`, `Listen`, `(*Server).Serve`, `(*Server).URL`, `(*Server).Close`
- `internal/uiserver/originguard.go`: `originHostGuard`, `allowedHosts`, `allowedOrigins`
- `internal/uiserver/handlers.go`: `uiService` implementing `uiv1connect.UIServiceHandler`, plus the `openEngine` package var (the test seam every later handler opens through) and `withEngine`
- `internal/cli/ui.go`: `newUiCmd`, `shouldOpenBrowser`, flags `--path`/`-p` and `--no-open`
- `internal/cli/root.go`: `newUiCmd()` added to the `root.AddCommand` list
- Repo-root `buf.yaml`, `buf.gen.yaml`; `go.tool-proto.mod`/`.sum` if Task 2's MVS measurement forces a third modfile
- `Taskfile.yml` task `proto:gen`
- New module requirements: `connectrpc.com/connect`, `github.com/pkg/browser`
- Tests: `internal/uiserver/originguard_test.go` (`TestOriginHostGuard`, `TestOriginHostGuardAdmitsOriginlessGET`), `internal/uiserver/server_test.go` (`TestListenPublishesABoundPortBeforeServe`, `TestServeReturnsNilOnContextCancellation`, `TestUIServerServesGetStatusEndToEnd`, `TestUIServerRejectsForeignHostEndToEnd`, `TestUIServerHoldsNoStoreHandleBetweenCalls`, `TestUIServiceHoldsNoStoreTypedField`), `internal/cli/ui_test.go` (`TestUICommandFlagSetIsExactlyPathAndNoOpen`, `TestShouldOpenBrowserSuppression`, `TestUICommandPrintsConnectableURLBeforeServing`)

**Created elsewhere in this phase** (listed so drift verification sees the whole set):
`internal/goldenspec` (`PerCorpusArgs`, `LockedCorpusArgs`, `CallExploreViaMCP`,
`CallNodeViaMCP`, `MCPResultText`); `testdata/golden/byte_identity_test.go`;
`internal/mcp`'s `looksLikeJSONRPCResponse` / `sniffedOutbound` / `decrementPending` /
`pendingUnderflows`; `internal/query`'s `NodeDetail`, `NodeDetailMode` + constants,
`FileDetail`, `DefinitionDetail`, `MultiDefDetail` + `NewMultiDefDetail` +
`(*MultiDefDetail).Definition`, `(*Engine).NodeDetail`, `ExploreResult`,
`(*Engine).ExploreDetail`, `ExploreFileGroup`, `ExploreBlast`, `ErrNotFound`,
`ErrInvalidArgument`, `(*Engine).IndexMeta`, `(*Engine).SourceFor`, the `build*Detail` gathers and the
`buildReverseAdjacency` test seam; `schema.Meta.commit_sha` field 8 / `Meta.CommitSha` /
`GetCommitSha` / `schema.IndexedCommitSHA`; `internal/indexer/commit.go`'s
`resolveHeadCommitSHA` and `gitExecLookPath`; `internal/textutil.TruncateOnRuneBoundary`;
`internal/uiserver`'s `truncateSource`, `sourceLineCap`, `sourceByteCap`,
`transportSendMaxBytes`, `transportReadMaxBytes`, `errIndexingInProgress`,
`indexingInProgressMessage`, `degradedStatus`, `classifyDegrade`, `mapEngineError` and the
nine mappers; the remaining eight `UIService` rpcs with their request/response messages and
the shared `Node`/`Location`/`FileEntry`/`FileTreeNode`/`ExploreGroup`/`BlastEntry`/
`SourceBlob`/`NodeDetailMode` wire types; `Taskfile.yml` task `proto:drift`;
`internal/upgrade/proto_task_test.go`.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Origin/Host exact-match guard, demonstrated RED before the middleware exists</name>
  <files>internal/uiserver/originguard.go, internal/uiserver/originguard_test.go</files>
  <read_first>
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md — "Pitfall 5: Origin header is absent on GET requests" and the Security Domain table's DNS-rebinding row
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md — the `internal/uiserver/originguard.go` "No Analog Found" entry and the illustrative sketch there
    - .planning/ROADMAP.md lines 91-96 — the blocking-order note explaining why this control lands before any handler
    - internal/mcp/session_line_test.go — the table-driven `t.Run` subtest idiom and the "got/want plus why it matters" failure-message style this repo uses
  </read_first>
  <behavior>
    - Admitted Host: `127.0.0.1:<port>` → next handler runs, 200.
    - Admitted Host: `localhost:<port>` → next handler runs, 200.
    - Admitted Host: `[::1]:<port>` → next handler runs, 200.
    - Rejected Host: `evil.com` → 403, next handler never runs.
    - Rejected Host: `127.0.0.1.evil.com:<port>` → 403 (defeats a prefix match).
    - Rejected Host: `evil.com:<port>` → 403 (defeats a suffix match).
    - Rejected Host: `notlocalhost:<port>` → 403 (defeats a substring match).
    - Rejected Host: `127.0.0.1:<other-port>` → 403 (the port is part of the identity).
    - Rejected Host: empty string → 403.
    - Admitted PAIR: `Host: 127.0.0.1:<port>` + `Origin: http://127.0.0.1:<port>` on POST → 200.
    - Admitted PAIR: `Host: localhost:<port>` + `Origin: http://localhost:<port>` on POST → 200.
    - Admitted PAIR: `Host: [::1]:<port>` + `Origin: http://[::1]:<port>` on POST → 200.
    - Rejected Origin: `http://evil.com` with an admitted Host → 403, next handler never runs.
    - Rejected Origin: `http://127.0.0.1:<port>.evil.com` with an admitted Host → 403.
    - Rejected CROSS-SPELLING: `Host: 127.0.0.1:<port>` + `Origin: http://localhost:<port>` → 403. Both are individually admitted literals; the PAIR is not, because the three spellings are three different names and not three renderings of one.
    - Rejected CROSS-SPELLING: `Host: localhost:<port>` + `Origin: http://127.0.0.1:<port>` → 403 (the same rejection in the other direction).
    - Rejected CROSS-SPELLING: `Host: [::1]:<port>` + `Origin: http://localhost:<port>` → 403.
    - Rejected CROSS-SPELLING: `Host: localhost:<port>` + `Origin: http://[::1]:<port>` → 403.
    - No Origin header at all on a GET with an admitted Host → 200 (over-rejection is the predicted failure mode).
    - Concurrency: 64 goroutines issuing a mixed admitted/foreign workload under `-race` each receive the verdict their own request earns; no cross-request contamination.
  </behavior>
  <action>
    Write `internal/uiserver/originguard_test.go` FIRST and commit it in a state where it
    is RED. Structure it so the RED demonstration is real and observable, not narrated:
    give the test a `newGuardedHandler` seam that returns either the bare `next` handler
    or the guarded one. Before `originHostGuard` exists, stand up the matrix against a bare
    `http.HandlerFunc` that always writes 200, run it, and record the failing subtest count
    in the commit message — that count is the RED evidence success criterion 2 requires
    ("demonstrated RED against the pre-middleware build"). Use `httptest.NewRecorder` and
    `httptest.NewRequest`; assert on both the status code and a `nextCalled` boolean so
    "rejected" means the downstream handler genuinely never ran, not merely that the status
    happened to be 403.

    Then write `internal/uiserver/originguard.go`. `originHostGuard(port string, next
    http.Handler) http.Handler` builds two `map[string]struct{}` sets once at construction
    — `allowedHosts(port)` yielding exactly `127.0.0.1:<port>`, `localhost:<port>`,
    `[::1]:<port>`; and `allowedOrigins(port)` yielding exactly
    `http://127.0.0.1:<port>`, `http://localhost:<port>`, `http://[::1]:<port>` — and
    admits a request only on exact map-key equality. Matching is map lookup only: no
    partial-string test of any kind, no pattern or wildcard match, no case folding, no
    normalisation of the three loopback spellings into one another. Each of the three is
    admitted as its own literal because they are three different names, not three
    renderings of one name (D-07, SRV-02).

    Order inside the handler: check `r.Host` first and reject with
    `http.Error(w, "forbidden host", http.StatusForbidden)` on a miss; the Host header is
    mandatory in HTTP/1.1 and is the header that actually reveals a rebind. Then read
    `r.Header.Get("Origin")`; when it is the empty string, fall through to `next` — a
    same-origin GET legitimately carries none.

    When Origin IS present, apply BOTH conditions and reject with
    `http.Error(w, "forbidden origin", http.StatusForbidden)` if either fails:

    1. **Set membership.** The origin must be a key of `allowedOrigins(port)`. This states
       the closed universe of admissible origins in one place and is what the set-equality
       assertion in the acceptance criteria pins.
    2. **Pairing with this request's Host.** The origin must ALSO equal
       `"http://" + r.Host` exactly — a plain string comparison against the Host this
       request already got admitted on. Checking the two allowlists independently would
       admit `Host: 127.0.0.1:<port>` carrying `Origin: http://localhost:<port>`, because
       each member is separately admitted; ROADMAP success criterion 2 requires the three
       spellings be admitted "by exact match rather than treated as interchangeable", and
       an independently-checked pair treats them as interchangeable. Pairing closes that.
       Say so at the call site in one sentence, and name the cross-spelling case it
       rejects.

    Neither condition is redundant in the sense that matters here: membership is the
    enumerated contract a reader audits, pairing is the per-request tie. Keep both, and
    keep both cheap — this is two map/string comparisons on a loopback request path.

    Give the file a doc comment naming the two CVE classes (CVE-2024-28224,
    CVE-2025-66414/66416), stating that loopback binding is not the boundary, stating why
    Origin-absence on GET is not a rejection reason, and stating that an admitted Origin
    must belong to the same spelling as the admitted Host.
  </action>
  <verify>
    <automated>{ OUT=$(go test -v -count=1 -race -run 'TestOriginHostGuard' ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS subtests: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 19 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -race -run 'TestOriginHostGuard' ./internal/uiserver/` emits at least 19 `--- PASS` lines **and** exits 0. The gate is the CONJUNCTION of the two, and this plan's earlier "the gate is the PASS count, not the exit status" ruling is withdrawn as wrong. Each condition is vacuous alone and neither substitutes for the other: `go test -run PATTERN` exits 0 when the pattern matches nothing, so the status alone can pass against zero executed tests; and a shell pipeline's `$?` is the LAST command's, so a bare `go test … | rg … | wc -l` count alone passes with N passing subtests plus a `--- FAIL`. The gate therefore captures `go test`'s output into a variable, saves its status BEFORE any pipe, and requires status 0 AND the count floor. This does not override 01-CONTEXT.md's D-04: D-04's "never by exit status" governs the deliberate-mutation observation, where a non-zero exit is the expected evidence, and its rationale — a zero-match `-run` pattern exits 0 — is preserved intact because the count floor stays. See plan 01-05, which owns D-04's mutation proof and records the reconciliation.
    - The commit that introduces `originguard_test.go` records, in its message, the number of subtests observed failing against the un-guarded handler. A non-zero recorded RED count is the artifact success criterion 2 asks for.
    - The set of strings `allowedHosts(p)` returns is exactly `{"127.0.0.1:"+p, "localhost:"+p, "[::1]:"+p}` — asserted by set equality in the test, with a length assertion of 3, so an added or dropped entry fails.
    - The set of strings `allowedOrigins(p)` returns is exactly `{"http://127.0.0.1:"+p, "http://localhost:"+p, "http://[::1]:"+p}` — same set-equality and length-3 assertion.
    - `rg -v '^\s*//' internal/uiserver/originguard.go | rg -o -e 'strings\.(Contains|HasPrefix|HasSuffix|EqualFold)' | wc -l` prints `0`, positive-controlled by running the identical pipeline against `internal/query/files.go` in the same invocation and confirming it prints a non-zero count. Comment lines are stripped first so the file's own prose cannot invalidate its gate.
    - Every rejected case asserts `nextCalled == false`, so rejection is proven to happen before the downstream handler runs.
    - The rejection matrix contains at least four CROSS-SPELLING pairs — an admitted Host carrying a different admitted spelling's Origin — covering both directions between `127.0.0.1` and `localhost` and at least one pair involving `[::1]`. Each asserts 403 and `nextCalled == false`. A guard that checked the two allowlists independently passes every other case in this matrix and fails exactly these, which is what makes them worth their line count.
    - The three admitted PAIRS are asserted positively (`Host: X` with `Origin: http://X` for each of the three spellings, all 200), so the pairing rule cannot be satisfied by rejecting everything.
  </acceptance_criteria>
  <done>The guard exists as committed code, its full rejection matrix passes under `-race` with a counted PASS total, and the pre-middleware RED count is recorded in git history.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Tracer — bind, publish, then serve one Connect RPC end-to-end to the Engine</name>
  <files>buf.yaml, buf.gen.yaml, go.tool-proto.mod, go.mod, Taskfile.yml, internal/schema/graph.pb.go, internal/uiproto/uiv1/ui.proto, internal/uiproto/uiv1/ui.pb.go, internal/uiproto/uiv1/uiv1connect/ui.connect.go, internal/uiserver/server.go, internal/uiserver/handlers.go, internal/uiserver/server_test.go</files>
  <read_first>
    - internal/schema/graph.proto lines 130-152 — the `Meta` message and the `has_file_index = 7` doc-comment convention the new proto surface inherits
    - internal/schema/graph.pb.go lines 1-20 — the generated header block, which currently records `protoc-gen-go v1.36.11` / `protoc v7.35.1`; this is the block the first `buf generate` will rewrite
    - internal/schema/meta.go — the D-02a additive-only package comment stating field numbers are never renumbered or reused
    - internal/query/engine.go lines 150-200 — `OpenAt`'s exact signature `(start string) (*Engine, io.Closer, error)` and its idempotent closer contract
    - internal/query/status.go lines 40-90 — `StatusResult`'s exact field set, which `GetStatusResponse` maps from
    - internal/mcp/tools.go — the structural analog for "resolve repo path, open an Engine per call, call one Engine method, map the error"
    - go.tool.mod and go.tool-lint.mod headers — the documented MVS version-bid-loss precedent and the measurement procedure this task repeats
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md — "Pitfall 4: The tool-modfile MVS conflict precedent may recur for buf", the Code Examples section's connect-go mounting sketch, and the Anti-Patterns entry rejecting `http.Protocols`/`SetUnencryptedHTTP2`
  </read_first>
  <behavior>
    - `TestListenPublishesABoundPortBeforeServe`: `Listen` returns a `*Server` whose `URL()` parses to a `http` URL on `127.0.0.1` with a non-zero port, and a raw `net.Dial("tcp", …)` at that host:port succeeds — all before `Serve` is ever called. This is the property the whole command's URL-printing order rests on.
    - `TestUIServerServesGetStatusEndToEnd`: a real indexed fixture repo, a real listener obtained from `Listen`, a real `uiv1connect.NewUIServiceClient` over `http.DefaultClient` aimed at `srv.URL()` — `GetStatus` returns `initialized=true` and a `node_count` matching what `Engine.Status` reports for that fixture.
    - `TestUIServerRejectsForeignHostEndToEnd`: the same live server, a hand-built `http.Request` to the `GetStatus` procedure path with `Host` set to `evil.com`, returns 403 and the response body does not contain a serialized Connect envelope.
    - `TestUIServerHoldsNoStoreHandleBetweenCalls`: two sequential `GetStatus` calls both succeed, and after the second returns, opening the same store directly with `graphstore.Open` succeeds — proving the server released it.
    - `TestServeReturnsNilOnContextCancellation`: with a live server from `Listen`, cancelling the context returned from `Serve`'s caller makes `Serve` return `nil` — not `http.ErrServerClosed` — within the 5-second shutdown budget, and the serve goroutine is observed to have finished. A sequential implementation that never reaches the cancellation branch hangs here and fails on the test's own deadline.
    - `TestUIServiceHoldsNoStoreTypedField`: reflecting over `uiService`'s struct fields yields a set of field type names equal to a literal expected set that contains no `*query.Engine`, no `graphstore.GraphStore` and no `graphstore.Reader`. This is the structural complement the behavioral test above cannot supply: closure-at-the-observation-point does not by itself rule out a cached-but-temporarily-closed handle.
  </behavior>
  <action>
    Wire ONE entry point through every layer. No second RPC, no batching, no SPA, no
    truncation, no degrade path — those are later plans. Real error handling on this one
    path only.

    First, resolve where the codegen tools are pinned. Repeat the measurement `go.tool.mod`'s
    own header documents: copy `go.tool.mod` to a scratch file, add `tool` directives for
    `github.com/bufbuild/buf/cmd/buf`, `google.golang.org/protobuf/cmd/protoc-gen-go` and
    `connectrpc.com/connect/cmd/protoc-gen-connect-go`, run `go mod tidy -modfile=<scratch>`
    with `GOWORK=off`, and build each tool. If any tool fails to compile — the version-bid
    loss this repo has already hit once with `actionlint` — create `go.tool-proto.mod` as
    a third isolated modfile instead. Record which outcome occurred and why in the modfile's
    own header comment, matching how `go.tool.mod` and `go.tool-lint.mod` document theirs.
    Pin `github.com/bufbuild/buf/cmd/buf` at v1.72.0,
    `google.golang.org/protobuf/cmd/protoc-gen-go` at the same v1.36.x line as the main
    module's `google.golang.org/protobuf` runtime dependency (mismatched generator and
    runtime versions panic at binary init), and
    `connectrpc.com/connect/cmd/protoc-gen-connect-go` at v1.20.0. Add
    `connectrpc.com/connect@v1.20.0` and `github.com/pkg/browser` to the main `go.mod` as
    runtime dependencies. All four were verified against the Go module proxy in
    01-RESEARCH.md's Package Legitimacy Audit with an `OK`/`Approved` verdict; none is
    `[ASSUMED]` or `[SUS]`, so no legitimacy checkpoint is owed here. Immediately after the
    dependency lands, verify against the now-present module that
    `connect.WithSendMaxBytes`, `connect.WithReadMaxBytes`, `connect.CodeUnavailable`,
    `connect.CodeResourceExhausted` and `connect.NewErrorDetail` exist with the signatures
    plans 01-10 and 01-11 assume, and record the verified signatures in the SUMMARY — the
    review's source-grounding pass could not check them because the module was absent.

    Author `buf.yaml` (v2 config, module rooted so `internal/uiproto` resolves) and
    `buf.gen.yaml` emitting `protoc-gen-go` and `protoc-gen-connect-go` output into the
    source tree beside the `.proto`, with `paths=source_relative` so generated files land
    next to their input rather than under a re-rooted directory. Add a `Taskfile.yml` task
    `proto:gen` that invokes `buf generate` through the pinned modfile with `GOWORK=off`,
    and give it a `desc:` explaining that it regenerates both proto surfaces. Do not add a
    drift-checking task here — `proto:drift` is plan 01-07's deliverable and touching the
    same Taskfile region twice in one wave would collide.

    **Absorb the first-run generated-header rewrite, deliberately.** `internal/schema/
    graph.pb.go` has never been regenerated by any checked-in invocation — there is no
    `go:generate` directive, no Makefile, no Taskfile target and no CI step in the tree
    today — and its committed header records a `protoc` compiler identity that `buf
    generate` does not shell out to and will therefore restamp. So the first `task
    proto:gen` WILL dirty that file even when every byte of message code is identical.
    Handle it here rather than letting it ambush plan 01-07's drift gate: run `task
    proto:gen`, capture `git diff internal/schema/graph.pb.go`, and verify every changed
    hunk is confined to the leading generated-header comment block. Record the before and
    after header verbatim in the SUMMARY. **Ruling for the whole phase: a header-only
    rewrite IS drift.** The committed file must be exactly what the pinned toolchain emits,
    so the restamped header is committed here, once, in its own reviewed commit — and from
    that commit onward `proto:drift` is entitled to treat any header change as a failure.
    If ANY hunk falls outside the header block, stop and report it: that is genuine latent
    drift in message code and it is BLD-04's business, not something to commit past.

    Author `internal/uiproto/uiv1/ui.proto` with `syntax = "proto3"`, package
    `codegraph.ui.v1`, and `option go_package` pointing at
    `github.com/seanb4t/codegraph-go/internal/uiproto/uiv1;uiv1`. Define exactly:
    `service UIService { rpc GetStatus(GetStatusRequest) returns (GetStatusResponse); }`,
    a `GetStatusRequest` carrying `string path = 1`, a `GetStatusResponse` carrying the
    `StatusResult` projection this tracer needs (`bool initialized = 1`, `string version = 2`,
    `int64 node_count = 3`, `int64 edge_count = 4`, `int64 file_count = 5`, `bool stale = 6`),
    and an `IndexingInProgress` message carrying `string message = 1`, reserved for plan
    01-11's degrade path. Carry `internal/schema/graph.proto`'s doc-comment convention onto
    every field: name it, cite the decision that added it, and state its absent-value
    behavior. State the additive-only discipline (D-02a) in a file-level comment: within
    this package a field number is never renumbered and never reused, and a retired field
    gets a `reserved` clause. Run `task proto:gen` and commit the generated `ui.pb.go` and
    `uiv1connect/ui.connect.go` alongside the `.proto`.

    Author `internal/uiserver/handlers.go` with a `uiService` struct holding `repoPath
    string` and implementing `uiv1connect.UIServiceHandler`. Introduce two seams every
    later handler will reuse, so the lifecycle boilerplate exists in exactly one place
    rather than being copy-pasted nine times (addresses review MEDIUM 01-06):

    - `openEngine` is a package-level `var openEngine = query.OpenAt`, following the
      test-only control-seam convention `internal/graphstore`'s `openLockRetrySleep` and
      `internal/daemon`'s `onSyncStart` already established. Production behavior is
      unchanged; tests can count or substitute opens. Give it a doc comment saying exactly
      that, and that it has no exported setter.
    - `withEngine(ctx context.Context, repoPath string, fn func(*query.Engine) error) error`
      calls `openEngine`, defers the returned closer's `Close`, runs `fn`, and maps any
      error through a single `mapEngineError` helper. One open, one deferred close, one
      error-translation site.

    `GetStatus` calls `withEngine`, calls `Engine.Status(ctx)` inside it, and maps
    `StatusResult` onto `GetStatusResponse`. Nothing is cached on `uiService` and no
    `Engine` or `GraphStore` value outlives the method (SRV-04's per-call discipline — the
    property `TestUIServerHoldsNoStoreHandleBetweenCalls` asserts behaviorally and
    `TestUIServiceHoldsNoStoreTypedField` asserts structurally). Give the mapping a doc
    comment in the same "table of what each field becomes and why" style
    `internal/query/status.go` already uses.

    Author `internal/uiserver/server.go` with the bind-before-publish lifecycle. `Options`
    carries `RepoPath string` and `Addr string` and NOTHING ELSE — in particular it carries
    no browser field: browser policy and the launch itself belong to `internal/cli`, and
    one owner means neither an unused option nor a double launch. `DefaultAddr` is
    `127.0.0.1:0`. `Addr` is a struct field that nothing outside this package writes in v1
    — no flag, no environment variable, no configuration file reads it (D-08). Say so in
    the field's doc comment, and say that a later `--host` is therefore an override of
    something that already exists rather than a restructuring (SRV-03).

    Split the lifecycle in two, because with an ephemeral `:0` bind the port does not exist
    until the listener does, and the command must print a URL that is already real:

    - `Listen(o Options) (*Server, error)` defaults an empty `Addr` to `DefaultAddr`, calls
      `net.Listen("tcp", addr)`, extracts the concrete port from
      `ln.Addr().(*net.TCPAddr).Port`, builds the `http.ServeMux`, mounts
      `uiv1connect.NewUIServiceHandler(&uiService{...})` on it, wraps the WHOLE mux in
      `originHostGuard(port, mux)` — the guard is the outermost `http.Handler`, never a
      Connect interceptor, because an interceptor runs after routing and message parsing
      have begun and would not protect plan 02's static routes either — stores the
      listener and a plain `&http.Server{Handler: guarded}` on the `Server`, and returns.
      After `Listen` returns, the bound port is known and the guard's allowlists are
      already built from it.
    - `(*Server).URL() string` returns `http://127.0.0.1:<bound port>`, valid the instant
      `Listen` returns.
    - `(*Server).Serve(ctx context.Context) error` blocks until the server stops. Its
      concurrency shape is prescribed, not left to the executor, because `http.Server.Serve`
      blocks and a literal sequential "serve, then on `ctx.Done()` shut down" never reaches
      the cancellation branch until serving has already stopped on its own:

      1. Make a BUFFERED error channel of capacity 1 — `errCh := make(chan error, 1)`.
         Buffered, so the goroutine can always deposit its result and exit even if nobody
         is left reading; an unbuffered channel here leaks the goroutine on the
         cancellation path.
      2. Start `go func() { errCh <- srv.Serve(ln) }()`.
      3. `select` on `<-ctx.Done()` and `err := <-errCh`.
      4. On `errCh` first, the server stopped by itself: normalize and return — see below.
      5. On `ctx.Done()` first, build `shutdownCtx, cancel := context.WithTimeout(
         context.Background(), 5*time.Second); defer cancel()` — derived from
         `Background()` and NOT from the already-cancelled `ctx`, or `Shutdown` returns
         immediately without draining anything — call `srv.Shutdown(shutdownCtx)`, then
         drain `<-errCh` so the serve goroutine is known to have finished before `Serve`
         returns. Return `Shutdown`'s error if it is non-nil; otherwise return nil.
      6. Normalization: `http.ErrServerClosed` is what `Serve` returns after a clean
         `Shutdown` or `Close`, so map it to nil at every return path. Any other error is
         returned to the caller unchanged. `codegraph ui` returns this value straight from
         `RunE`, so a normalized nil is the difference between a clean Ctrl-C and a
         spurious non-zero exit code.

      Add a cancellation test that exercises exactly this: cancel the context while a
      request is in flight and assert `Serve` returns nil within the shutdown budget.
    - `(*Server).Close() error` closes the listener, for the caller that binds and then
      decides not to serve.

    Give `Listen`'s doc comment the reason for the split in one sentence: the URL cannot be
    published before the bind, and the bind cannot be inside the blocking call.

    Do not set `http.Protocols` and do not enable unencrypted HTTP/2: the Connect protocol
    works over plain HTTP/1.1 from a browser `fetch`, and this server will never see a gRPC
    client. Set no message-size options yet; `WithSendMaxBytes`/`WithReadMaxBytes` are plan
    01-10's deliverable (D-13) and belong with the application-level cap they sit above.
  </action>
  <verify>
    <automated>go build ./... && { OUT=$(go test -v -count=1 -run 'TestListenPublishesABoundPortBeforeServe|TestServeReturnsNilOnContextCancellation|TestUIServer|TestUIServiceHoldsNoStoreTypedField' ./internal/uiserver/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS tests: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 6 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -run 'TestListenPublishesABoundPortBeforeServe|TestServeReturnsNilOnContextCancellation|TestUIServer|TestUIServiceHoldsNoStoreTypedField' ./internal/uiserver/` emits at least 6 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - `TestListenPublishesABoundPortBeforeServe` asserts a non-zero port and a successful raw TCP dial with no `Serve` call anywhere before those assertions.
    - `TestServeReturnsNilOnContextCancellation` asserts `Serve` returns exactly `nil` after cancellation and returns within the shutdown budget. `rg -v '^\s*//' internal/uiserver/server.go | rg -o -e 'make\(chan error, 1\)' | wc -l` prints `1`, proving the serve-error channel is buffered rather than unbuffered; comment lines are stripped first.
    - `TestUIServerServesGetStatusEndToEnd` obtains its response through `uiv1connect.NewUIServiceClient` aimed at `srv.URL()` — not by calling the `uiService` method directly — so the assertion covers routing, protocol framing and the guard, and its `node_count` equals what `Engine.Status` independently reports for the same fixture.
    - `TestUIServerRejectsForeignHostEndToEnd` receives HTTP 403 for `Host: evil.com` against the live listener.
    - `TestUIServerHoldsNoStoreHandleBetweenCalls` successfully opens the fixture store with `graphstore.Open` after the server has answered two calls, and closes it.
    - `TestUIServiceHoldsNoStoreTypedField` asserts set equality of `uiService`'s reflected field type names against a literal expected set, with a length assertion, so both an added handle field and a removed expected field fail.
    - `git status --porcelain internal/uiproto internal/schema` is empty immediately after `task proto:gen` runs — both surfaces, not just the new one.
    - The SUMMARY records the `internal/schema/graph.pb.go` header before and after, and states that every changed hunk was confined to the generated-header comment block.
    - The `internal/uiproto/uiv1/ui.proto` `service UIService` block declares exactly one rpc. Asserted by a source check counting rpc declarations, so a second RPC slipping in ahead of plan 01-08 is caught.
    - `rg -v '^\s*//' internal/uiserver/handlers.go | rg -o -e 'query\.OpenAt' | wc -l` prints `1` — the single site, inside the `openEngine` var initialiser. Comment lines are stripped first.
    - `uiserver.Options` has exactly the fields `RepoPath` and `Addr`, asserted by reflected set equality with a length assertion of 2, so a browser field cannot be reintroduced.
    - The pinned tool versions appear literally in the chosen modfile: `buf` at `v1.72.0`, `protoc-gen-connect-go` at `v1.20.0`, and `protoc-gen-go` at the same `v1.36.x` version the main `go.mod` requires for `google.golang.org/protobuf`.
    - The chosen modfile carries a header comment recording whether the MVS measurement passed against an existing modfile or forced a third one, in the shape `go.tool.mod`'s and `go.tool-lint.mod`'s headers already use.
    - The SUMMARY records the verified real signatures of `connect.WithSendMaxBytes`, `connect.WithReadMaxBytes`, `connect.NewErrorDetail`, `connect.CodeUnavailable` and `connect.CodeResourceExhausted` against the now-present module.
  </acceptance_criteria>
  <done>A live `uiserver.Server` publishes a connectable URL from `Listen`, then answers a real Connect `GetStatus` call from a real client at that URL, rejects a foreign Host, and holds no store handle between calls — the single happy path works end-to-end and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `codegraph ui` — bind, print, open, then serve in the foreground</name>
  <files>internal/cli/ui.go, internal/cli/ui_test.go, internal/cli/root.go</files>
  <read_first>
    - internal/cli/daemon.go lines 119-180 — `newDaemonStartCmd`: the `signal.NotifyContext(cmd.Context(), syscall.SIGINT, syscall.SIGTERM)` foreground pattern and the `--path`/`-p` flag registration this command copies
    - internal/cli/serve.go lines 172-215 and 290-305 — `newServeCmd`'s shape, `resolveStartPath(path)`, and the flag-plus-env-twin convention documented on `--watch`
    - internal/cli/root.go lines 40-62 — the `root.AddCommand` registration list
    - internal/uiserver/server.go — the `Listen`/`URL`/`Serve` lifecycle Task 2 just built, and why the split exists
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md — D-07, D-08, D-09, D-10 and the "Explicitly rejected: a config file" reasoning
  </read_first>
  <behavior>
    - `TestUICommandFlagSetIsExactlyPathAndNoOpen`: the registered flag-name set equals `{"path", "no-open"}` with a length assertion of 2.
    - `TestShouldOpenBrowserSuppression`: `--no-open` set → false; `CI` set to a non-empty value → false; stdout not a character device → false; none of those → true.
    - `TestUICommandPrintsConnectableURLBeforeServing`: the command is run against an indexed fixture with a cancellable context and a captured `cmd.OutOrStdout()`; a `http://127.0.0.1:<non-zero port>` line appears on stdout, and a Connect `GetStatus` issued at that exact printed URL succeeds while the command is still running. Then the context is cancelled and `RunE` returns without error.
  </behavior>
  <action>
    Create `internal/cli/ui.go` with `newUiCmd() *cobra.Command`, `Use: "ui"`, and a
    `Short`/`Long` describing it as a local read-only web UI over the repository's own
    index. Its `RunE`:

    1. resolves the repo path through the existing `resolveStartPath(path)` helper exactly
       as `serve` and `daemon start` do;
    2. calls `uiserver.Listen(uiserver.Options{RepoPath: start})` — the bind happens here,
       so from this point the port is real;
    3. prints `srv.URL()` to `cmd.OutOrStdout()`, which a scripted caller can read;
    4. calls `shouldOpenBrowser(noOpen, cmd)` and, when it returns true, launches the
       browser at `srv.URL()`;
    5. sets up `ctx, stop := signal.NotifyContext(cmd.Context(), syscall.SIGINT,
       syscall.SIGTERM); defer stop()` and returns `srv.Serve(ctx)`.

    Steps 2, 3 and 4 are ordered and the order is load-bearing: with an ephemeral `:0` bind
    there is no port before step 2, and step 5 blocks, so publication and launch can only
    sit between them. Say so in a comment at the call site. If `Listen` fails, return its
    error before anything is printed and before any browser is launched.

    Foreground until interrupt, matching `serve` and `daemon start`: no PID file, no lock
    file, no `ui stop` verb, no lifecycle shared with `daemon` (D-10, SRV-01).

    Register exactly two flags and no others: `cmd.Flags().StringVarP(&path, "path", "p",
    "", "repo path (default: cwd)")` matching every other repo-scoped command, and
    `cmd.Flags().BoolVar(&noOpen, "no-open", false, ...)`. Register no flag for a bind
    address, a port, a hostname, a token, a password or any other credential — SRV-03
    requires that neither the bind address nor auth is exposed in v1, and D-08 keeps the
    bind address reachable only as the unwired `uiserver.Options.Addr` field.

    Implement the browser-open decision as `shouldOpenBrowser(noOpen bool, cmd
    *cobra.Command) bool`: false when `noOpen` is set, false when the `CI` environment
    variable is set to any non-empty value, false when stdout is not a character device,
    true otherwise (D-09). Detect the character device by stat-ing the underlying
    `*os.File` and testing `os.ModeCharDevice`; treat a non-`*os.File` stdout as
    not-a-TTY. Do the actual launch through `github.com/pkg/browser`'s `OpenURL` rather
    than a hand-written per-GOOS dispatch — the WSL case alone makes a three-branch switch
    wrong. A launch failure is reported to `cmd.ErrOrStderr()` as a warning and does not
    fail the command; the URL was already printed, so the user is not blocked. This CLI is
    the only place in the tree that decides to open a browser or opens one.

    Add `newUiCmd()` to `internal/cli/root.go`'s `root.AddCommand(...)` list.

    Write `internal/cli/ui_test.go` covering the behavior block.
  </action>
  <verify>
    <automated>go build ./... && { OUT=$(go test -v -count=1 -run 'TestUICommand|TestShouldOpenBrowserSuppression' ./internal/cli/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 6 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -run 'TestUICommand|TestShouldOpenBrowserSuppression' ./internal/cli/` emits at least 6 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - `TestUICommandPrintsConnectableURLBeforeServing` proves the printed URL is usable: it issues a real Connect `GetStatus` at the exact string that was printed and asserts a successful response. A URL that could not be connected to fails this test — which is the whole point of the `Listen`/`Serve` split.
    - `go run ./cmd/codegraph ui --help` exits 0 and its flag section lists exactly `--path`/`-p` and `--no-open`.
    - `TestUICommandFlagSetIsExactlyPathAndNoOpen` asserts set equality of the registered flag names against `{"path", "no-open"}` and asserts the set length is 2.
    - `TestShouldOpenBrowserSuppression` covers all four decision inputs and emits at least 4 `--- PASS` subtest lines.
    - `CI=1 go run ./cmd/codegraph ui --path <fixture>` prints a `http://127.0.0.1:` URL on stdout and launches no browser; the same command with stdout piped does the same.
    - `codegraph ui` appears in `go run ./cmd/codegraph --help`'s command list, proving the `root.AddCommand` registration landed.
    - Exactly one `ui` command is registered on the root, asserted by walking `root.Commands()` and counting entries whose `Name()` is `ui`; the count must be 1. This is a positive count assertion, not an absence check for a `ui stop` subcommand.
  </acceptance_criteria>
  <done>`codegraph ui` binds, prints a URL that already works, opens a browser only when a human is watching, serves in the foreground until interrupt, and exposes no bind-address or credential flag.</done>
</task>

</tasks>

## Flagged Assumptions (unresolved edge probes)

The spec-less edge probe classified three of this plan's requirements as `unclassified`.
Per the probe's own rules these are **not** auto-resolved into `must_haves` — they are
surfaced here as explicit assumptions the executor and reviewer should read as open:

- **SRV-01 / unclassified** — assumed: "runs as its own process, never sharing a lifecycle
  with `serve --mcp`" is fully discharged by having no PID file, no lock file and no shared
  registry entry. If `daemon`'s registry ever learns to enumerate UI processes, this
  assumption lapses.
- **SRV-03 / unclassified** — assumed: "read-only by construction" is discharged at the
  schema level (the service declares only read methods) plus the type level (`Engine`
  wraps a `graphstore.Reader`, which has no mutator). No runtime write-attempt interception
  is planned.
- **RPC-02 / unclassified** — assumed: "handlers mount on `net/http`" means a single
  `http.ServeMux` shared with plan 02's embedded SPA. If Phase 2 needs a second mux or a
  path-prefix router, this plan's mounting shape is what it must extend, not replace.

## Review feedback dispositions

| Finding | Severity | Disposition |
|---|---|---|
| Cycle-2 LOW: cross-plan artifact manifest drift — `(*Engine).SourceFor` missing from some "Created elsewhere in this phase" manifests | LOW | **Incorporated, and widened beyond what was flagged.** The review named 01-02 and 01-05. Auditing all eleven manifests found the same omission in 01-01, 01-03, 01-06 and 01-07 as well. `(*Engine).SourceFor` (declared by 01-04) is now named in every plan's manifest, so drift verification sees the whole phase-wide new-symbol set from any plan it starts at. |
| Cycle-2 MEDIUM: `allowedHosts` and `allowedOrigins` are checked independently, admitting cross-spelling pairs | MEDIUM (treated as a security finding against the phase's headline requirement) | **Incorporated.** When Origin is present the guard now requires BOTH set membership AND exact equality with `"http://" + r.Host`, so `Host: 127.0.0.1:P` carrying `Origin: http://localhost:P` is rejected. Four cross-spelling pairs in both directions, including one involving `[::1]`, were added to the rejection matrix; the three admitted pairs were added positively so the rule cannot be satisfied by rejecting everything. The PASS floor rose from 14 to 19. ROADMAP criterion 2's "admitted by exact match rather than treated as interchangeable" is now an asserted property rather than an implied one. |
| Cycle-2 MEDIUM: `Serve(ctx)`'s concurrency shape is unreachable read literally | MEDIUM | **Incorporated.** The action now prescribes the shape step by step: buffered capacity-1 serve-error channel, `go func(){ errCh <- srv.Serve(ln) }()`, `select` on `ctx.Done()` versus `errCh`, `Shutdown` under a `context.Background()`-derived 5-second budget (NOT derived from the already-cancelled ctx), a drain of `errCh` before returning, and `http.ErrServerClosed` normalized to nil at every return path. `TestServeReturnsNilOnContextCancellation` asserts it and a source gate asserts the channel is buffered; the PASS floor rose from 5 to 6. |
| Cycle-2 HIGH C2-2: automated PASS-count gates discard the test command's exit status | HIGH | **Incorporated across every gate in this plan.** A shell pipeline's `$?` is the LAST command's, so `COUNT=$(go test ... | rg '--- PASS' | wc -l); [ "$COUNT" -ge N ]` passed with N passing subtests plus a `--- FAIL`. Every `<automated>` gate now captures `go test`'s output into `OUT` and its status into `STATUS` BEFORE any pipe, counts from `OUT`, dumps `OUT` on failure, and requires `STATUS -eq 0` AND the `--- PASS` floor. The conjunction is the rule: the count alone is vacuous when a test fails, the status alone is vacuous when `-run` matches nothing. A plan-level prohibition against the bare count-only form is in `must_haves.prohibitions`. The only scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence. |
| URL printed before the listener binds | HIGH | **Incorporated.** `Listen`/`Serve` split; `Listen` binds and publishes, `Serve` blocks. `TestListenPublishesABoundPortBeforeServe` and `TestUICommandPrintsConnectableURLBeforeServing` both assert it. |
| Browser opening has the same sequencing problem | HIGH | **Incorporated.** Launch sits between `Listen` and `Serve`, at a documented ordered call site. |
| `OpenBrowser` in `Options` vs CLI-owned launch — ownership unclear | MEDIUM | **Incorporated by removal.** `Options` carries only `RepoPath` and `Addr`, asserted by reflected set equality. The CLI is the sole owner. |
| `TestUIServerHoldsNoStoreHandleBetweenCalls` proves only closure at the observation point | MEDIUM | **Incorporated.** `TestUIServiceHoldsNoStoreTypedField` added as the structural complement. |
| The interrupted-`buf generate` truth is unverifiable in this plan | LOW | **Retained deliberately, as a backstop.** It is already tagged `verification: backstop`, which is precisely the mechanism for a truth this plan cannot assert; plan 01-07 is named as the detector. |
| Generated-header stamp will produce a guaranteed first-run diff (added, not raised by Codex) | MEDIUM | **Incorporated.** Task 2 absorbs it explicitly, verifies the diff is header-confined, records it, and issues the phase-wide ruling that a header-only rewrite counts as drift. Acceptance now checks `internal/uiproto` **and** `internal/schema`. |
| `connect` API surface UNCHECKABLE at review time | — | **Incorporated.** Task 2 verifies the five load-bearing `connect` symbols the moment the dependency lands and records their real signatures. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any web page the developer visits → `127.0.0.1:<port>` | A browser on the developer's machine can be induced by an attacker-controlled page to issue requests at the loopback listener. This is the boundary SRV-02 exists for; loopback binding is not itself a boundary. |
| CLI process → `.codegraph/store` | The RPC handler opens the store per call; a concurrent `codegraph daemon` or `serve --mcp` holds the same directory lock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | `internal/uiserver/originguard.go` — `Host` header | critical | mitigate | Exact-match allowlist of `127.0.0.1:<port>`, `localhost:<port>`, `[::1]:<port>`, evaluated by map lookup in middleware wrapping the whole mux before any handler runs. Partial-string, pattern and wildcard matching are all excluded, and the negative matrix includes `127.0.0.1.evil.com`, `evil.com:<port>` and `notlocalhost:<port>` specifically to prove each of those shapes rejects. |
| T-01-02 | Spoofing | `internal/uiserver/originguard.go` — `Origin` header | high | mitigate | When the header is present, TWO conditions are required: exact membership in the three-origin allowlist, AND exact equality with `"http://" + r.Host` so the origin is paired to the Host this request was admitted on. Independent checks would admit a cross-spelling pair such as `Host: 127.0.0.1:<port>` + `Origin: http://localhost:<port>`, which is precisely the "treated as interchangeable" shape ROADMAP criterion 2 forbids; at least four cross-spelling pairs in both directions are in the rejection matrix. Absence on GET admits, because over-rejection would break the app's own navigation — the failure mode 01-RESEARCH.md Pitfall 5 predicts. |
| T-01-03 | Elevation of Privilege | `uiserver.Options.Addr` | high | mitigate | `DefaultAddr` is `127.0.0.1:0` and nothing outside the package writes the field in v1. The `ui` command's registered flag set is asserted by set equality against `{path, no-open}`, and `Options`' own field set is asserted by reflected set equality, so neither a bind-address flag nor a smuggled-in option field can be added without failing a test. |
| T-01-04 | Tampering | `codegraph.ui.v1.UIService` | high | mitigate | The service declares only `GetStatus` in this plan, and the underlying `query.Engine` holds a `graphstore.Reader` with no mutator, so no RPC has a write path to reach for. The full read-only assertion over the complete method set lands with plan 01-09. |
| T-01-06 | Information Disclosure | `IndexingInProgress.message` | medium | mitigate | Message reserved in this plan and populated in 01-11 with a generic constant. No OS lock-file path, process id or absolute filesystem path is ever embedded in a client-visible detail. |
| T-01-SC | Tampering | Go module installs (`connectrpc.com/connect`, `github.com/bufbuild/buf`, `github.com/pkg/browser`) | high | mitigate | 01-RESEARCH.md's Package Legitimacy Audit verified all four against `proxy.golang.org`, the Go ecosystem's registry-of-record, with `OK`/`Approved` verdicts and no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries — so no blocking human legitimacy checkpoint is owed. The `gsd-tools package-legitimacy` seam covers only npm/pypi/crates and could not run on Go modules. |
</threat_model>

<verification>
- `go build ./...` succeeds with the new packages.
- `go vet ./internal/uiserver/... ./internal/uiproto/... ./internal/cli/...` is clean.
- `task proto:gen` followed by `git status --porcelain internal/uiproto internal/schema` produces empty output.
- `go test -race -count=1 ./internal/uiserver/... ./internal/cli/...` passes: the command exits 0 AND its `--- PASS` count meets its floor — both, never either alone.
- `task test:unit` still passes — nothing in this plan touches `internal/query`, `internal/mcp` or `internal/schema`'s hand-written source.
</verification>

<success_criteria>
- `Listen` publishes a bound, connectable URL before anything serves, and `codegraph ui` prints exactly that URL.
- A Connect client at the printed loopback URL gets a real `GetStatus` answer derived from the repository's own index.
- A request carrying `Host: evil.com` against the live listener is answered 403 without any handler running, and the pre-middleware RED count is recorded in git history.
- `localhost`, `127.0.0.1` and `[::1]` are each admitted by their own exact literal, proven by a set-equality assertion of length 3 rather than by a spot check — and are NOT interchangeable with one another, proven by cross-spelling Host/Origin pairs rejecting in both directions.
- No v1 flag exposes a bind address or an auth credential, proven by flag-set equality; and `Options` carries no browser field, proven by reflected field-set equality.
- Both proto surfaces are regenerable with zero diff from the pinned toolchain, with the one-time header restamp absorbed and recorded here.
</success_criteria>

<output>
Create `.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-01-SUMMARY.md` when done
</output>
