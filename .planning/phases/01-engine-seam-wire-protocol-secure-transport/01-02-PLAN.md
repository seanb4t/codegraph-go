---
phase: 01-engine-seam-wire-protocol-secure-transport
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - testdata/golden/byte_identity_test.go
  - testdata/golden/golden_test.go
autonomous: true
requirements: [ENG-01, ENG-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "For each of the 26 frozen golden (corpus, filename) pairs, live `Engine.Node`/`Engine.Explore` output invoked with the SAME locked arguments the golden was captured with equals the golden's `Output` field byte for byte."
    - "The comparison test asserts a compared-pair COUNT of exactly 26 and fails when that count is short by even one, so a suite that enumerated nothing cannot read green (rule 84d1gfpywd)."
    - "The comparison test has been watched go RED against a deliberately perturbed input, with the observed failing count recorded — a passing suite alone cannot distinguish 'nothing changed' from 'nothing could be detected'."
    - "When `calls`/`calledBy` elements compare equal, live output preserves the pre-existing iteration order exactly: the oracle is the thing that makes reordering observable, because a reordered `calls` list changes rendered bytes (ENG-01 / ordering edge)."
    - "When Explore's ranked groups compare equal, live output preserves `groupMatchesByFile`'s pre-existing ordering exactly, for the same reason (ENG-02 / ordering edge)."
  artifacts:
    - testdata/golden/byte_identity_test.go
  key_links:
    - "The new test MUST read its symbol/query parameters from gocapture's `lockedCorpusArgs` table rather than re-deriving them — a re-derived argument silently compares a different question against the frozen answer."
    - "`buildEngineAt` copies a corpus into a fresh temp dir and opens it via `query.OpenAt`, so `repoRoot` and the store are the same directory. Constructing the Engine any other way reintroduces the CR-02 pollution bug."
    - "The golden suite is excluded from `go list ./...`. A `./...` run executes zero goldens; this test is only reached by `./testdata/golden/...`."
  prohibitions:
    - "Never let `codegraph node` or `codegraph explore` output bytes change; the frozen goldens are a promise to already-shipped installs, not an internal convenience."
---

<objective>
Build the byte-identity oracle that ENG-01 and ENG-02 lean on — and that does not exist
today.

Purpose: 01-CONTEXT.md's D-04 was decided believing the 26 frozen goldens already act as
an output-regression net. Verified in source, they do not: `TestReFrozenGoldensValid`
checks only that each golden file exists, is non-empty, starts with `{`, parses as
`goldenCapture` and has a non-empty `Output`; it never re-invokes the Engine.
`TestCorpusBehavior_Go`'s own doc comment states it asserts named behavioral properties,
"not byte-diffs against a frozen golden". So ROADMAP success criterion 1's "every frozen
golden covering them passes unchanged" is, as written, satisfiable WITHOUT the output
being unchanged — exactly the vacuous shape rule `84d1gfpywd` names. Until this oracle
exists, D-04's mutation-proof has nothing to turn RED and plan 01-04's extraction cannot
be proven safe.

This plan runs BEFORE any extraction, against unmodified `Node()`/`Explore()`, so the
oracle's own correctness is established while the code under test is known-good.

Output: a counted, non-vacuity-proven byte-diff comparison over all 26 golden pairs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-VALIDATION.md
</context>

## Artifacts this phase produces

**Created by this plan (01-02):**

- `testdata/golden/byte_identity_test.go` — new file
- `TestGoldensMatchLiveEngineOutput` — the 26-pair byte-diff comparison
- `TestGoldensMatchLiveEngineOutputIsNonVacuous` — the counted-enumeration guard
- `goldenIdentityCase` struct and `goldenIdentityCases()` enumerator (test-local)
- `lockedCorpusArgs`-equivalent argument table shared with, or lifted verbatim from, `testdata/golden/gocapture/main.go`

**Created elsewhere in this phase** (listed so drift verification sees the whole set):
`internal/uiproto/uiv1` (package, `ui.proto`, `UIService`, `GetStatus`, `Search`,
`Callers`, `Callees`, `Impact`, `Affected`, `Files`, `GetNodeDetail`, `Explore`,
`GetStatusRequest`/`Response`, `IndexingInProgress`, `SourceBlob`, generated `ui.pb.go`
and `uiv1connect/ui.connect.go`), `internal/uiserver` (`Options`, `DefaultAddr`,
`Server`, `New`, `Run`, `URL`, `originHostGuard`, `allowedHosts`, `allowedOrigins`,
`uiService`, `truncateSource`, `sourceLineCap`, `sourceByteCap`,
`truncateOnRuneBoundary`, `transportSendMaxBytes`, `transportReadMaxBytes`,
`degradedStatus`, `errIndexingInProgress`), `internal/cli/ui.go` (`newUiCmd`,
`shouldOpenBrowser`, flags `--path`/`--no-open`), `internal/query.NodeDetail`,
`internal/query.NodeDetailMode` and its constants, `(*Engine).NodeDetail`,
`internal/query.ExploreResult`, `(*Engine).ExploreResult`, `schema.Meta.commit_sha`
field 8 / `Meta.CommitSha` / `GetCommitSha`, `schema.IndexedCommitSHA`,
`internal/indexer/commit.go`'s `resolveHeadCommitSHA`, `internal/mcp`'s
`looksLikeJSONRPCResponse`, `buf.yaml`, `buf.gen.yaml`, `go.tool-proto.mod`, and
`Taskfile.yml` tasks `proto:gen` and `proto:drift`.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Byte-identity comparison over all 26 frozen golden pairs</name>
  <files>testdata/golden/byte_identity_test.go</files>
  <read_first>
    - testdata/golden/golden_test.go lines 150-300 — `expectedCorpusSet`, the `expectedGoCaptures` table (the authoritative 26-pair enumeration), `goldenCapture`'s envelope shape, `ExpectedGoldenScenarioCount`, and `TestReFrozenGoldensValid`'s counted-verification idiom
    - testdata/golden/gocapture/main.go lines 40-320 — `lockedCorpusArgs` (the exact symbol/query values each golden was captured with), `languageToLockedSlug`, `slugToRepo`, and `regenerateCorpus`'s exact invocation order: `Explore(baselineQuery, 1)` → go-explore, `Node(baselineSymbol, baselineSymbolFile, nil)` → go-node, `Explore(multiQuery, 0)` → go-explore-multi, `Node(multiSymbol, "", nil)` → go-node-multi, `callExploreViaMCP` → go-explore-mcp, `callNodeViaMCP` → go-node-mcp
    - testdata/golden/behavioral_test.go lines 55-260 — `lockedCorpusDir`'s never-skip, always-Fatalf resolution discipline, `buildEngineAt`'s CR-02 note on why the Engine must be opened via `query.OpenAt` against the copied tree, and `behavioralCorpusSrc`
    - testdata/golden/README.md — the "`go test ./...` never runs anything in this directory" note and the volatile-field list
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md — "Pitfall 1: The golden suite does not currently prove byte-identity"
  </read_first>
  <behavior>
    - For `(hugo, go-node.json)`: `Node("Page", "", nil)` against a live Engine over the hugo corpus equals the golden's `Output` byte for byte.
    - For `(hugo, go-explore.json)`: `Explore("page content", 1)` equals the golden's `Output` byte for byte.
    - For `(hugo, go-node-multi.json)`: `Node("Site", "", nil)` equals the golden's `Output`.
    - For `(hugo, go-explore-multi.json)`: `Explore("page content template", 0)` equals the golden's `Output`.
    - Same four shapes for `guava` (`Preconditions` / `check precondition` / `ImmutableList` / `immutable collection`), `serilog` (`LoggerConfiguration` / `configure logger` / `LogEvent` / `log configuration`), `requests` (`Session` / `http session` / `Request` / `http request session`).
    - For the two `-mcp` goldens per locked corpus: the MCP tool path's output equals the golden's `Output`, exercised through the same call shape `gocapture`'s `callExploreViaMCP`/`callNodeViaMCP` use.
    - For `(behavioral, go-node-multi.json)` and `(behavioral, go-explore-multi.json)`: the in-repo behavioral corpus's two goldens compare the same way.
    - The test reports a compared-pair count and fails when it is not exactly 26.
  </behavior>
  <action>
    Create `testdata/golden/byte_identity_test.go` in the same external test package the
    existing golden files use. Do not modify `TestReFrozenGoldensValid` — it guards a
    different property (envelope validity) and both should survive.

    Enumerate from an authoritative table, never from a filesystem glob, matching
    `TestReFrozenGoldensValid`'s stated discipline: a missing expected pair must fail the
    suite rather than silently shrink it. Build `goldenIdentityCases()` by joining
    `expectedGoCaptures`'s (slug, filename) pairs to the per-corpus argument table.
    `lockedCorpusArgs` currently lives in `package main` under
    `testdata/golden/gocapture/`, so it is not importable from the test package: lift the
    four entries into the test file as a literal table with a provenance comment naming
    `testdata/golden/gocapture/main.go` and the exact line range they were transcribed
    from, following the same "literal fixture with a provenance comment citing how and when
    it was verified" convention `internal/upgrade/taskfile_shape_test.go` uses. Add a guard
    subtest asserting the lifted table has an entry for every non-behavioral slug in
    `expectedGoCaptures`, so a future corpus addition cannot land with no arguments and be
    silently skipped.

    Resolve each locked corpus through the existing `lockedCorpusDir(t, language)` helper,
    which is already the single hermetic resolver, already fails with a named cause, and
    already never calls `t.Skip`. Do not add a skip path: this suite runs only in
    `corpora.yml`'s `golden` job where the pinned corpora are fetched and integrity-asserted
    first, and `TestCorpusBehavior_Java` already takes the same never-skip stance. Resolve
    the behavioral corpus through `behavioralCorpusSrc(t)`.

    Build each Engine with `buildEngineAt(t, corpusDir)` so `repoRoot` and the store are
    the same directory — constructing it any other way reintroduces the CR-02 pollution
    bug documented on that helper. Note that `gocapture` indexes the corpus source
    directly for the CLI captures and a copied staging tree for the MCP captures, while
    `buildEngineAt` always copies; if the copy changes any rendered path in the output,
    that is a real finding, not a reason to weaken the comparison — surface it and stop
    rather than normalising the diff away.

    For each case, read the golden with `os.ReadFile`, unmarshal it into the existing
    `goldenCapture` struct, invoke the matching live call with the locked arguments, and
    compare with `==` on the raw strings. On mismatch, report the first differing byte
    offset, a bounded window of context either side, and both lengths — a full-file dump of
    a large rendered node is unreadable. Use `t.Run(slug+"/"+name, ...)` subtests so the
    PASS lines are countable per pair, and increment a `compared` counter inside each
    subtest.

    After the loop, log the compared count and fail unless it equals exactly 26 with a
    message explaining that a short count means a pair was dropped or a subtest loop never
    ran. Derive the 26 from `len(expectedGoCaptures)`'s flattened total rather than
    hard-coding it in the comparison, and separately assert that flattened total is 26 —
    so the constant and the table are cross-checked against each other rather than the
    constant merely restating itself.
  </action>
  <verify>
    <automated>COUNT=$(go test -v -count=1 -run 'TestGoldensMatchLiveEngineOutput$' ./testdata/golden/ 2>&1 | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "compared-pair PASS lines: $COUNT"; [ "$COUNT" -ge 27 ]</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -run 'TestGoldensMatchLiveEngineOutput$' ./testdata/golden/` emits at least 27 `--- PASS` lines (26 pair subtests plus the parent). Scored by counting PASS lines: `go test -run PATTERN` exits 0 when the pattern matches nothing, and three plan-declared commands read green that way in v0.11.0.
    - The test logs a line of the form `compared N/26 golden pairs` and fails when N is not 26.
    - The flattened total of `expectedGoCaptures` is independently asserted to be 26 in the same test, so the count and the table cross-check each other.
    - The lifted argument table carries a provenance comment naming `testdata/golden/gocapture/main.go` and the line range it was transcribed from, and a guard subtest asserts every non-behavioral slug in `expectedGoCaptures` has an entry in it.
    - Comparison is on raw string equality against the golden's `Output` field — no trimming, no whitespace normalisation, no line-ending rewriting, no volatile-field stripping. The captures are already volatile-field-free by construction.
    - A mismatch failure message reports the first differing byte offset and both lengths, not a full dump.
    - No `t.Skip` appears in the new file, matching `lockedCorpusDir`'s stated discipline. Assert positively by confirming the test's own case count is 26 rather than by grepping for an absent call.
  </acceptance_criteria>
  <done>All 26 frozen golden pairs are byte-compared against live Engine output on unmodified `Node()`/`Explore()`, with the compared count asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the oracle non-vacuous — watch it go RED and record the count</name>
  <files>testdata/golden/byte_identity_test.go</files>
  <read_first>
    - testdata/golden/byte_identity_test.go — the file Task 1 just created
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md — D-04's mutation-proof procedure and its scoring rule
    - .planning/STATE.md — the standing decision "A gate is not trusted until it has been demonstrated RED against a confirmed-applied mutation" and "A guard must carry a positive assertion that it did its work"
    - internal/upgrade/taskfile_shape_test.go — the companion "prove it fails" test convention this repo uses alongside a structural guard
  </read_first>
  <action>
    A passing suite alone cannot distinguish "nothing changed" from "nothing could be
    detected". Establish, and record, that this oracle can fail.

    Run the mutation proof by hand, in the working tree, and revert byte-cleanly:
    temporarily edit `internal/query/render_markdown.go`'s `RenderNode` to drop the
    `calledBy` section from its output, confirm with `git diff --stat` that the mutation is
    actually applied, run
    `go test -v -count=1 -run 'TestGoldensMatchLiveEngineOutput$' ./testdata/golden/`, and
    record BOTH the resulting `--- PASS` count and the `--- FAIL` count. The expectation is
    that every node-bearing pair fails; if any node pair still passes, that is a hole in the
    oracle and it must be closed before proceeding, not explained. Then `git checkout --` the
    mutated file, confirm `git status --porcelain internal/query/` is empty, and re-run to
    confirm the count returns to 26. Write both numbers, the exact mutation applied, and the
    revert confirmation into the plan's SUMMARY as evidence — this is the artifact ROADMAP
    success criterion 1 and D-04 both depend on, and it is what makes plan 01-04's
    extraction provable rather than asserted.

    Then add `TestGoldensMatchLiveEngineOutputIsNonVacuous` as a permanent companion guard
    so the non-vacuity property survives after the hand-run proof scrolls out of history.
    It must not mutate production code. Instead it proves the comparison mechanism itself
    is live: it takes one real golden's `Output`, perturbs a copy of it (append a single
    byte, and separately flip one interior byte), runs the same comparison helper the real
    test uses against the unperturbed live output, and asserts the helper reports a
    mismatch in both cases with a first-differing-offset that matches where the byte was
    changed. Refactor Task 1's comparison into a named helper so both tests call the same
    code path — a companion guard that tests a different comparison than the real one
    proves nothing.
  </action>
  <verify>
    <automated>COUNT=$(go test -v -count=1 -run 'TestGoldensMatchLiveEngineOutput' ./testdata/golden/ 2>&1 | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "PASS lines across both tests: $COUNT"; [ "$COUNT" -ge 30 ]</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the mutation applied verbatim, the `--- FAIL` count observed under it, the `--- PASS` count observed under it, and the `git status --porcelain internal/query/` empty-output confirmation after revert.
    - The observed FAIL count under the dropped-`calledBy` mutation is greater than zero and covers every node-bearing pair. A node pair that still passed is treated as an oracle hole and fixed, not annotated.
    - After revert, `go test -v -count=1 -run 'TestGoldensMatchLiveEngineOutput$' ./testdata/golden/` again reports `compared 26/26 golden pairs`.
    - `TestGoldensMatchLiveEngineOutputIsNonVacuous` calls the same named comparison helper the real test calls, proven by the helper being referenced from both files' call sites in a single package.
    - `TestGoldensMatchLiveEngineOutputIsNonVacuous` asserts a mismatch for both an appended byte and a flipped interior byte, and asserts the reported first-differing offset equals the offset it perturbed.
    - Combined, both tests emit at least 30 `--- PASS` lines.
  </acceptance_criteria>
  <done>The oracle has been watched fail against a confirmed-applied mutation, the counts are recorded, the mutation is byte-cleanly reverted, and a permanent companion guard keeps the comparison mechanism honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched corpus tree → indexer/Engine | The locked corpora are third-party source trees fetched from GitHub at pinned SHAs. They cross into this process as parsed input. `corpora:assert-one`'s four-part integrity check is the existing control and this plan adds no new fetch path. |
| frozen golden file → test oracle | The committed goldens are the reference this test trusts. A silently-edited golden would make a real regression read green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | `testdata/golden/corpus/**/*.json` | medium | mitigate | The comparison is byte-exact with no normalisation, so an edited golden diverges from live output and fails rather than absorbing the edit. Combined with the counted enumeration, a deleted golden fails too. |
| T-01-12 | Repudiation | the oracle itself | high | mitigate | The non-vacuity proof: the guard is demonstrated RED against a confirmed-applied mutation with the count recorded, and a permanent companion test keeps the comparison helper provably live. This is rule `84d1gfpywd` applied to the highest-consequence guard in the phase. |
| T-01-13 | Denial of Service | corpus indexing in the test | low | accept | Indexing four real third-party repositories is slow by design and already the golden job's cost profile. This plan adds one more index-and-query pass per corpus; the job already tolerates that scale. |
</threat_model>

<verification>
- `task test:golden` passes. Note that `go test ./...` runs zero goldens — the Go tool ignores `testdata` directories — so a `./...` green is not evidence here.
- `go vet ./testdata/golden/...` is clean.
- The mutation-proof counts are present in the SUMMARY.
</verification>

<success_criteria>
- A byte-diff oracle exists over all 26 frozen golden pairs and asserts its own compared count.
- It has been watched fail against a confirmed-applied, byte-cleanly-reverted mutation, with the FAIL and PASS counts recorded.
- Plan 01-04's extraction now has something that can prove it safe.
</success_criteria>

<output>
Create `.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-02-SUMMARY.md` when done
</output>
