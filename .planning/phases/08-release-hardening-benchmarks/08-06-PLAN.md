---
phase: 08-release-hardening-benchmarks
plan: 06
type: tdd
wave: 2
depends_on: ["08-02"]
files_modified:
  - internal/bench/regression.go
  - internal/bench/regression_test.go
autonomous: true
requirements: [PERF-02, INDX-06]
must_haves:
  truths:
    - "A throughput regression beyond the tolerance band (default >10%) fails the gate; within-band passes"
    - "A peak-RSS growth beyond the tolerance band (default >15%) fails the gate; within-band passes"
    - "An absolute peak-RSS ceiling is enforced independently (INDX-06 bounded memory), not just a relative delta"
    - "The gate returns a descriptive error naming the metric, the observed delta, and the budget — fail loud, never panic"
  prohibitions:
    - "MUST NOT auto-rewrite baseline.json as a side effect of a gate run — re-blessing is a separate, explicit, human-reviewed action (D-05)"
    - "MUST NOT compare across mismatched units/OS without going through the normalized Metrics (peak RSS already normalized to bytes by Plan 08-02)"
    - "MUST NOT panic — this is a CI-gate library, return (error)"
  artifacts:
    - path: internal/bench/regression.go
      provides: "CheckRegression(baseline, current Metrics, ceiling PeakRSSCeiling) error + tolerance constants"
      contains: "func CheckRegression"
    - path: internal/bench/regression_test.go
      provides: "table tests for throughput/RSS band pass+fail and the absolute ceiling"
      contains: "func TestCheckRegression"
  key_links:
    - from: internal/bench/regression.go
      to: internal/bench/metrics.go
      via: "operates on the Metrics struct from Plan 08-02"
      pattern: "Metrics"
---

<objective>
Implement the committed-baseline + tolerance-band regression gate (PERF-02) that fails CI when indexing throughput regresses or peak RSS grows beyond budget, and additionally enforces an absolute peak-RSS ceiling for the 100k+ corpus (INDX-06 bounded memory). Test-first because the comparison arithmetic is exactly the pure logic that must be right before it can block a merge — and because an accidental auto-rewrite of the baseline would silently defeat the whole gate (D-05).

Purpose: This is the decision logic the CI gate (Plan 08-08) and the runner (Plan 08-07) call; it must be a clean, tested pure function separate from the I/O around it.
Output: `internal/bench/regression.go` with proven tolerance-band + ceiling logic.
</objective>

## Artifacts this phase produces (Plan 08-06)

- `internal/bench/regression.go` (new) — `CheckRegression(baseline, current Metrics, ceiling)` + `DefaultThroughputTolerance` / `DefaultRSSTolerance` constants + a documented re-bless note.
- `internal/bench/regression_test.go` (new) — throughput/RSS band pass+fail tests + absolute-ceiling test.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md

<interfaces>
<!-- Depends on Plan 08-02's Metrics struct. RESEARCH Pattern 3 is the reference. -->

internal/bench/metrics.go (Plan 08-02):
  type Metrics struct { ... FilesPerSec float64; BytesPerSec float64;
    QueryLatencyMedianMS float64; PeakRSSBytes int64; ColdStartMS float64; ... }

RESEARCH Pattern 3 reference logic (implement, tune-able constants):
  throughputDelta = (baseline.FilesPerSec - current.FilesPerSec)/baseline.FilesPerSec; fail if > 0.10
  rssDelta = (current.PeakRSSBytes - baseline.PeakRSSBytes)/baseline.PeakRSSBytes; fail if > 0.15
  re-bless path: an explicit `-rebless` flag on the runner (Plan 08-07) overwrites baseline.json — never automatic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing tolerance-band + absolute-ceiling tests</name>
  <files>internal/bench/regression_test.go</files>
  <behavior>
    - Throughput: current 9% slower than baseline → pass; 11% slower → fail with a throughput error.
    - Peak RSS: current 14% larger → pass; 16% larger → fail with an RSS error.
    - Absolute ceiling: current PeakRSSBytes above the ceiling → fail even if the relative delta is within band; below the ceiling → pass.
    - Every failure error names the metric and includes the observed percentage/value and the budget.
  </behavior>
  <read_first>
    - internal/bench/metrics.go (Plan 08-02 Metrics)
    - .planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md Pattern 3
  </read_first>
  <action>
    Create `internal/bench/regression_test.go` (package `bench`). Write `TestCheckRegression` as a table test constructing baseline + current `Metrics` pairs plus a peak-RSS ceiling, covering: (1) throughput within band passes, just-over fails; (2) RSS growth within band passes, just-over fails; (3) an above-ceiling current fails on the ceiling even when the relative RSS delta is inside the band; (4) a clean current (faster + smaller, under ceiling) passes with nil error. Assert the returned error string names the offending metric and includes both the observed delta and the budget. Tests fail now (no regression.go). Commit `test(08-06): failing tolerance-band + absolute-ceiling regression tests`.
  </action>
  <acceptance_criteria>
    - `internal/bench/regression_test.go` exists referencing `CheckRegression`.
    - `go test ./internal/bench/... -run TestCheckRegression` FAILS (undefined) — RED confirmed.
    - A `test(08-06):` commit contains only the test file.
  </acceptance_criteria>
  <verify>
    <automated>! go test ./internal/bench/... -run TestCheckRegression 2>/dev/null</automated>
  </verify>
  <done>The band + ceiling policy is encoded as failing tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement CheckRegression with tolerance constants + absolute ceiling</name>
  <files>internal/bench/regression.go</files>
  <read_first>
    - internal/bench/regression_test.go (RED tests)
    - .planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md Pattern 3 (re-bless discipline)
  </read_first>
  <action>
    Create `internal/bench/regression.go` (package `bench`) with exported tolerance constants `DefaultThroughputTolerance = 0.10` and `DefaultRSSTolerance = 0.15` (documented as D-05 starting points, tune-able), and `CheckRegression(baseline, current Metrics, ceilingBytes int64) error`. Compute the throughput regression as `(baseline.FilesPerSec - current.FilesPerSec)/baseline.FilesPerSec` and fail (wrapped error naming metric+delta+budget) if it exceeds the throughput tolerance; compute RSS growth as `(current.PeakRSSBytes - baseline.PeakRSSBytes)/baseline.PeakRSSBytes` and fail if it exceeds the RSS tolerance; independently fail if `current.PeakRSSBytes > ceilingBytes` (INDX-06 absolute bounded-memory ceiling) regardless of the relative delta. Guard against a zero/negative baseline (return a clear error rather than dividing by zero). Add a doc comment stating explicitly that this function NEVER mutates the baseline — re-blessing is a separate, explicit `-rebless` action on the runner (Plan 08-07), reviewable as a standalone baseline.json diff. Never panic. Run to GREEN. Commit `feat(08-06): tolerance-band + absolute-RSS-ceiling regression gate`.
  </action>
  <acceptance_criteria>
    - `go test ./internal/bench/... -run TestCheckRegression` exits 0 (GREEN) and `go vet ./internal/bench/...` is clean.
    - `CheckRegression` enforces throughput tolerance, RSS tolerance, AND an absolute ceiling; a zero baseline yields a clear error not a panic.
    - The doc comment states no-baseline-mutation and points at the explicit re-bless path.
    - `grep -q 'panic(' internal/bench/regression.go` finds NO match.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/bench/... -run TestCheckRegression && go vet ./internal/bench/...</automated>
  </verify>
  <done>The regression gate's decision logic is proven: relative bands plus an absolute INDX-06 ceiling, with re-blessing kept an explicit human action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed baseline → CI gate decision | the baseline is the trusted reference; an unintended auto-rewrite would erase the gate's meaning |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Tampering | baseline integrity | medium | mitigate | CheckRegression never mutates the baseline; re-blessing is an explicit, separately-reviewable action (D-05) |
| T-08-13 | Repudiation | ambiguous gate failures | low | mitigate | errors name the metric, observed delta, and budget so a failure is unambiguous and actionable |
</threat_model>

<verification>
- RED then GREEN on TestCheckRegression.
- Absolute ceiling enforced independently of relative delta.
- No baseline mutation, no panics.
</verification>

<success_criteria>
The PERF-02 regression decision logic and the INDX-06 bounded-memory ceiling are implemented and tested as a clean pure function ready for the CI gate and runner to consume.
</success_criteria>

<output>
Create `.planning/phases/08-release-hardening-benchmarks/08-06-SUMMARY.md` when done.
</output>
