---
phase: 08-release-hardening-benchmarks
plan: 05
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - internal/upgrade/verify_release_e2e_test.go
  - internal/upgrade/upgrade.go
autonomous: true
requirements: [DIST-02]
must_haves:
  truths:
    - "A test asserts releaseAssetName(tag) equals the exact string GoReleaser's name_template produces for the same tag/os/arch, for all 6 os/arch combinations"
    - "An e2e test hashes a signed release binary with sha256 and passes it + its .sigstore.json bundle through verifyRelease against the PRODUCTION identity constants (releaseWorkflowRefPattern, releaseOIDCIssuer)"
    - "The e2e test asserts the digest-over-the-binary path (not checksums), exercising Finding 1's exact call path"
    - "When no real signed artifact is available, the live-verify test skips cleanly (never fails spuriously) — the asset-name agreement test always runs"
  prohibitions:
    - "MUST NOT assert against the fixture SAN/regex used by the offline verify_test.go — the e2e test asserts against the real production releaseWorkflowRefPattern"
    - "MUST NOT reimplement or bypass sigstore-go verification — reuse the shipped verifyRelease exactly (never hand-roll crypto)"
    - "MUST NOT change releaseWorkflowRefPattern, releaseOIDCIssuer, or releaseRepoSlug"
  artifacts:
    - path: internal/upgrade/verify_release_e2e_test.go
      provides: "TestReleaseAssetNameMatchesGoReleaser + TestVerifyReleaseE2E (real-artifact, skip-if-absent)"
      contains: "func TestVerifyReleaseE2E"
  key_links:
    - from: internal/upgrade/verify_release_e2e_test.go
      to: internal/upgrade/verify.go
      via: "calls verifyRelease with releaseWorkflowRefPattern + releaseOIDCIssuer over sha256(binary)"
      pattern: "verifyRelease"
    - from: internal/upgrade/upgrade.go
      to: .goreleaser.yaml
      via: "releaseAssetName must equal name_template output — the D-14 finalization"
      pattern: "releaseAssetName"
---

<objective>
Close the loop on Finding 1 (the phase's defining inversion: the verifier shipped in Phase 6, the signer ships now). Add the end-to-end test CONTEXT.md `<specifics>` explicitly calls for — a real signed release artifact passing `verifyRelease` against the PRODUCTION identity — and finalize the `releaseAssetName` ⇔ GoReleaser `name_template` agreement (D-14) so `codegraph upgrade` can never 404 on an asset or fail on a checksums-only signature.

Purpose: A green release workflow (Plan 08-04) is necessary but not sufficient; only an actual `verifyRelease` pass against a real signed binary proves the whole chain. This is the executable-spec loop-closer.
Output: `internal/upgrade/verify_release_e2e_test.go`; a confirmed/finalized `releaseAssetName`.
</objective>

## Artifacts this phase produces (Plan 08-05)

- `internal/upgrade/verify_release_e2e_test.go` (new) — `TestReleaseAssetNameMatchesGoReleaser`, `TestVerifyReleaseE2E`.
- `internal/upgrade/upgrade.go` (modified only if a name mismatch is found) — `releaseAssetName` finalized to agree with the GoReleaser template.
- (optional) `internal/upgrade/testdata/` — a captured real signed artifact + bundle fixture, if the e2e test uses a committed fixture rather than a live fetch.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md
@.planning/phases/08-release-hardening-benchmarks/08-PATTERNS.md

<interfaces>
<!-- Reuse the shipped verifier + existing test patterns. Do NOT hand-roll crypto. -->

internal/upgrade/verify.go:
  func verifyRelease(b *bundle.Bundle, tr root.TrustedMaterial, digestAlgorithm string, artifactDigest []byte, sanRegex string) error
  releaseOIDCIssuer = "https://token.actions.githubusercontent.com"
  releaseWorkflowRefPattern = ^https://github\.com/seanb4t/codegraph-go/\.github/workflows/release\.ya?ml@refs/tags/v[0-9][^\s]*$

internal/upgrade/upgrade.go:
  func releaseAssetName(version string) string  // codegraph_<version>_<GOOS>_<GOARCH>[.exe]
  defaultVerify: digest := sha256.Sum256(binary); verifyRelease(..., "sha256", digest[:], releaseWorkflowRefPattern)

Existing test patterns to extend (internal/upgrade/verify_test.go):
  loadBundle([]byte) (*bundle.Bundle, error); loadFixtureBundle(t); loadFixtureTrustedRoot(t)
  TestVerifyRelease_AcceptsValidBundle asserts verifyRelease returns nil for a valid bundle+digest+SAN.

GoReleaser name_template (from Plan 08-01 .goreleaser.yaml):
  {{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}{{ if eq .Os "windows" }}.exe{{ end }}
  => codegraph_<tag>_<os>_<arch>[.exe]  (must equal releaseAssetName for every os/arch)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED→GREEN): Assert releaseAssetName agrees with the GoReleaser name_template for all 6 os/arch</name>
  <files>internal/upgrade/verify_release_e2e_test.go, internal/upgrade/upgrade.go</files>
  <behavior>
    - For each (os,arch) in {linux,windows,darwin}×{amd64,arm64} and a sample tag v1.2.3, the string GoReleaser's template resolves to equals releaseAssetName's output (with .exe only on windows).
  </behavior>
  <read_first>
    - internal/upgrade/upgrade.go (releaseAssetName)
    - .goreleaser.yaml (name_template — Plan 08-01)
    - internal/upgrade/verify_test.go (test-file conventions in this package)
  </read_first>
  <action>
    Create `internal/upgrade/verify_release_e2e_test.go` in package `upgrade`. Write `TestReleaseAssetNameMatchesGoReleaser`: implement a small local helper that reproduces the GoReleaser template result for a given (projectName, tag, os, arch) — `codegraph_<tag>_<os>_<arch>` plus `.exe` when os=="windows" — and, for a fixed tag `v1.2.3`, assert it equals the expected asset name for each of the 6 os/arch pairs. Because `releaseAssetName` reads `runtime.GOOS`/`runtime.GOARCH` (host-bound), the test compares the TEMPLATE helper's output against the SAME expected literal that `releaseAssetName` would produce for that os/arch (i.e. pin the expected literal per pair and assert the host's `releaseAssetName(v1.2.3)` matches the pair for the current host). If a mismatch is found (e.g. arch naming divergence), finalize `releaseAssetName` in upgrade.go to agree with the GoReleaser template and update its doc comment to state the D-14 agreement is now closed. Run to GREEN. Commit `test(08-05): pin releaseAssetName<->goreleaser name_template agreement` (and a `fix(08-05):` commit if upgrade.go changed).
  </action>
  <acceptance_criteria>
    - `go test ./internal/upgrade/ -run TestReleaseAssetNameMatchesGoReleaser` exits 0.
    - The test enumerates all 6 os/arch pairs and asserts the exact `codegraph_v1.2.3_<os>_<arch>[.exe]` string.
    - If upgrade.go was changed, its releaseAssetName doc comment records the finalized agreement (no longer a "placeholder").
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/upgrade/ -run TestReleaseAssetNameMatchesGoReleaser</automated>
  </verify>
  <done>The asset-name contract between GoReleaser and the upgrade downloader is pinned by a test, closing D-14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end verifyRelease against a real signed artifact (skip-if-absent), asserting the production identity</name>
  <files>internal/upgrade/verify_release_e2e_test.go</files>
  <behavior>
    - Given a real signed release binary + its .sigstore.json bundle, sha256(binary) + the bundle pass verifyRelease under releaseWorkflowRefPattern + releaseOIDCIssuer.
    - Given the same binary but the WRONG SAN regex, verifyRelease returns a non-nil error (identity mismatch is rejected).
    - When no artifact is provided, the test skips (t.Skip), never fails.
  </behavior>
  <read_first>
    - internal/upgrade/verify.go (verifyRelease, production constants)
    - internal/upgrade/verify_test.go (loadBundle, fixture-loading + assertion patterns to reuse)
  </read_first>
  <action>
    In `internal/upgrade/verify_release_e2e_test.go`, add `TestVerifyReleaseE2E`. It sources a real signed artifact via one of two paths (implement the fixture path as primary; the env path as an optional live variant): (a) a committed fixture pair under `internal/upgrade/testdata/` — a real `codegraph_<tag>_<os>_<arch>` binary and its `.sigstore.json` captured from an actual `seanb4t/codegraph-go` release (captured after Plan 08-04's pre-release checkpoint); or (b) paths supplied via env vars (e.g. `CODEGRAPH_E2E_BINARY` / `CODEGRAPH_E2E_BUNDLE`) for a live-artifact run. If neither the fixture nor the env vars are present, call `t.Skip` with a clear reason (the artifact only exists after a real signed release — see Manual-Only verifications in 08-VALIDATION.md). When present: read the binary, compute `sha256.Sum256`, `loadBundle` the sidecar JSON, fetch/`load` trusted material (reuse the offline trusted-root fixture loader from verify_test.go if the captured bundle's trust root is embeddable; otherwise fetch the live Sigstore root), and assert `verifyRelease(b, tr, "sha256", digest[:], releaseWorkflowRefPattern)` returns nil. Add a negative sub-case asserting that passing a deliberately-wrong SAN regex yields a non-nil error (proves identity scoping is enforced). Do NOT reimplement any crypto — call the shipped verifyRelease unchanged. Commit `test(08-05): e2e verifyRelease against a real signed artifact (production identity)`.
  </action>
  <acceptance_criteria>
    - `go test ./internal/upgrade/ -run TestVerifyReleaseE2E` exits 0 (passing when an artifact is present, skipping cleanly when absent — never a spurious failure).
    - The test references `verifyRelease`, `releaseWorkflowRefPattern`, and `releaseOIDCIssuer` (production constants), NOT the fixture SAN used by the offline tests.
    - The negative sub-case asserts a wrong-SAN input is rejected with a non-nil error.
    - `grep -q 'sha256' internal/upgrade/verify_release_e2e_test.go` — the digest is computed over the binary (Finding 1 path).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/upgrade/ -run TestVerifyReleaseE2E</automated>
  </verify>
  <done>The full sign→download→verify chain is exercised against a real artifact under the production identity, or skips cleanly until one exists — Finding 1's loop is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downloaded release artifact → verifier | the e2e test stands in for a real user's download, asserting only genuinely-signed-by-release.yml artifacts are accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | release artifact acceptance | high | mitigate | e2e test proves a real signed binary passes verifyRelease over sha256(binary) and a wrong-identity input is rejected — catches a checksums-only-signing regression (Finding 1) before users hit it |
| T-08-03 | Elevation of Privilege | identity scope | high | mitigate | asserts against the anchored production releaseWorkflowRefPattern, not the loose fixture regex; negative sub-case proves scope enforcement |
| T-08-11 | Spoofing | asset naming | high | mitigate | TestReleaseAssetNameMatchesGoReleaser pins the download name so upgrade cannot 404 onto an attacker-squattable wrong name |
</threat_model>

<verification>
- Asset-name agreement test green for all 6 os/arch.
- e2e verify test passes against a real artifact or skips cleanly; negative SAN case rejected.
- No crypto reimplemented; production constants asserted.
</verification>

<success_criteria>
DIST-02's verify path is proven end-to-end: the release the workflow produces is provably accepted by the shipped verifier, and the asset-name contract is pinned — the Finding 1 loop is closed.
</success_criteria>

<output>
Create `.planning/phases/08-release-hardening-benchmarks/08-05-SUMMARY.md` when done.
</output>
