---
phase: 08-release-hardening-benchmarks
plan: 09
type: execute
wave: 4
depends_on: ["08-04", "08-07"]
files_modified:
  - docs/RELEASE.md
  - docs/BENCHMARKS.md
autonomous: true
requirements: [DIST-05, DIST-02, DIST-03, PERF-01]
must_haves:
  truths:
    - "docs/RELEASE.md gives users copy-pasteable commands to verify a release's cosign signature, SLSA provenance, and SBOM"
    - "docs/RELEASE.md states the DIST-05 dependency-tree narrative: minimal + audited, with CGo (tree-sitter) as the sole documented exception, and explains the 134 direct requires are dominated by the 14 tree-sitter grammar modules"
    - "docs/RELEASE.md documents the reproducibility posture (linux/amd64 blocking; cross-targets best-effort/reported)"
    - "docs/BENCHMARKS.md documents the methodology (median-of-5, OS-level external peak RSS, from-scratch index throughput, cold start) and carries raw per-repo head-to-head numbers vs TS codegraph@1.3.1"
    - "docs/BENCHMARKS.md documents the regression gate and the explicit human re-bless path"
  prohibitions:
    - "MUST NOT document a verify command the CI/release pipeline does not actually produce artifacts for (verify commands must match the real .sigstore.json / SLSA / SBOM assets)"
    - "MUST NOT imply a small flat dependency tree — the SBOM narrative must make the tree-sitter grammar-module composition explicit (per CONTEXT specifics)"
    - "MUST NOT invent benchmark numbers — raw numbers come from a real head-to-head run (Plan 08-07/08-08); use clearly-marked placeholders if a run has not yet produced them"
  artifacts:
    - path: docs/RELEASE.md
      provides: "user-facing verify commands (cosign/slsa-verifier/syft) + DIST-05 audited-deps narrative + reproducibility posture"
      contains: "cosign"
    - path: docs/BENCHMARKS.md
      provides: "methodology + raw per-repo head-to-head numbers + regression-gate + re-bless path"
      contains: "median"
  key_links:
    - from: docs/RELEASE.md
      to: internal/upgrade/verify.go
      via: "verify commands reproduce the same identity the shipped verifier enforces (release.yml @ refs/tags/v*)"
      pattern: "release"
    - from: docs/BENCHMARKS.md
      to: tools/bench/runner
      via: "numbers are produced by the runner's headtohead mode"
      pattern: "runner"
---

<objective>
Write the two user-facing documents that make the phase's guarantees usable and honest: `docs/RELEASE.md` (how to verify signature + provenance + SBOM, the minimal-audited-dependency DIST-05 narrative, and the reproducibility posture) and `docs/BENCHMARKS.md` (methodology + raw head-to-head numbers + the regression gate and re-bless path). This also discharges DIST-05 (already Complete) as an explicit SBOM/audit narrative rather than new code.

Purpose: A verifiable supply chain is only trustworthy if users have the exact commands to verify it; benchmarks are only credible with documented methodology and raw numbers.
Output: `docs/RELEASE.md`, `docs/BENCHMARKS.md`.
</objective>

## Artifacts this phase produces (Plan 08-09)

- `docs/RELEASE.md` (new) — verify commands (cosign bundle verify, slsa-verifier, syft/SBOM), DIST-05 audited-deps narrative, reproducibility posture.
- `docs/BENCHMARKS.md` (new) — methodology, raw per-repo head-to-head numbers, regression gate + re-bless path.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-release-hardening-benchmarks/08-CONTEXT.md
@.planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md

<interfaces>
<!-- Documents must match what the pipeline actually produces + measures. -->

Release assets (Plan 08-04): per-platform raw binary codegraph_<tag>_<os>_<arch>[.exe],
  a sibling <asset>.sigstore.json (cosign v3 bundle), <asset>.spdx.json (syft SBOM),
  codegraph_<tag>_checksums.txt, and SLSA provenance (*.intoto.jsonl via generator_generic_slsa3.yml).
Verify identity (internal/upgrade/verify.go): issuer https://token.actions.githubusercontent.com,
  SAN release.yml @ refs/tags/v* — the verify commands documented must reproduce this.
Dependency composition (CONTEXT specifics + go.mod): ~134 direct requires dominated by 14 tree-sitter
  grammar modules; CGo (tree-sitter) is the sole documented exception (DIST-05).
Benchmark methodology (D-05): median-of-5, OS-level peak RSS (external, comparable to Node),
  from-scratch `index --force` throughput, query-latency median, cold start; numbers from Plan 08-07 runner.
Re-bless path (D-05): `go run ./tools/bench/runner -mode regression -rebless ...` (explicit human action).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/RELEASE.md — verify commands + DIST-05 audited-deps narrative + reproducibility posture</name>
  <files>docs/RELEASE.md</files>
  <read_first>
    - internal/upgrade/verify.go (the identity users must reproduce)
    - .github/workflows/release.yml (the actual assets published — Plan 08-04)
    - .planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md § "Don't Hand-Roll" + Package Legitimacy Audit + CONTEXT specifics (grammar-module composition)
  </read_first>
  <action>
    Create `docs/RELEASE.md`. Section 1 (Verifying a release): give exact, copy-pasteable commands to (a) verify a binary's cosign keyless signature from its `<asset>.sigstore.json` bundle, asserting the certificate identity issuer `https://token.actions.githubusercontent.com` and the SAN scoped to `release.yml @ refs/tags/v*` (the same identity internal/upgrade enforces); (b) verify SLSA provenance with `slsa-verifier verify-artifact` against the `*.intoto.jsonl` and the binary, noting the provenance is generated over the checksums file; (c) inspect the SBOM (`<asset>.spdx.json`). Only document commands whose artifacts the release.yml pipeline actually produces. Section 2 (Dependency tree — DIST-05): state that the tree is minimal and audited, that CGo via tree-sitter is the sole documented exception, and make the composition explicit — the ~134 direct requires are dominated by the 14 tree-sitter grammar modules (a wide-but-shallow, deliberately-audited tree, not a small flat one), pointing to the SBOM as the machine-readable inventory. Section 3 (Reproducibility): document that builds are reproducible via pinned Go + zig toolchains and `-trimpath`/`-buildid=`/`SOURCE_DATE_EPOCH`, that the double-build gate BLOCKS on linux/amd64 (canonical target) and is best-effort/reported on the CGo-cross targets (D-03), and how a user can reproduce a build locally. Cross-reference `codegraph upgrade` as the automated consumer of the same signatures.
  </action>
  <acceptance_criteria>
    - `docs/RELEASE.md` exists and contains a cosign verify command, a slsa-verifier command, and an SBOM inspection command.
    - It states CGo/tree-sitter as the sole documented dependency exception AND explains the grammar-module composition (mentions "tree-sitter" and the grammar-module dominance).
    - It documents the linux/amd64-blocking / cross-target-reported reproducibility posture.
    - The documented verify identity matches internal/upgrade/verify.go (issuer + release.yml/refs/tags/v* SAN).
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/RELEASE.md && grep -qi 'cosign' docs/RELEASE.md && grep -qi 'slsa' docs/RELEASE.md && grep -qi 'tree-sitter' docs/RELEASE.md && grep -qi 'reproduc' docs/RELEASE.md</automated>
  </verify>
  <done>Users have exact verify commands, an honest dependency-tree narrative (DIST-05), and the reproducibility posture.</done>
</task>

<task type="auto">
  <name>Task 2: docs/BENCHMARKS.md — methodology + raw head-to-head numbers + regression gate + re-bless</name>
  <files>docs/BENCHMARKS.md</files>
  <read_first>
    - tools/bench/runner/main.go and tools/bench/realcorpus/manifest.go (what is measured, on which repos — Plan 08-07)
    - .planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md Pattern 1/3 + D-05 methodology
  </read_first>
  <action>
    Create `docs/BENCHMARKS.md`. Section 1 (Methodology): document median-of-5 per metric; OS-level, external peak RSS via getrusage/ProcessState (explaining WHY in-process runtime.MemStats would be an unfair, non-comparable number against the TS Node process); the four metrics (from-scratch `index --force` throughput in files/s and bytes/s, query-latency median over a fixed query set, peak RSS, cold start); and that comparison is against the installed TS `@colbymchenry/codegraph@1.3.1`. List the pinned real repos (name + commit SHA) from the realcorpus manifest. Section 2 (Raw numbers): a per-repo table of Go-vs-TS raw numbers for each metric — populate from a real head-to-head run (Plan 08-07/08-08 output); if a real run has not yet produced numbers at execution time, use clearly-marked `TBD (populate from bench.yml run)` placeholders rather than inventing values, and note the exact command to regenerate them. Section 3 (Regression gate): document the synthetic 100k+ corpus, the committed baseline, the default tolerance bands (throughput >10%, RSS >15%), the absolute INDX-06 peak-RSS ceiling, and the explicit human re-bless command (`go run ./tools/bench/runner -mode regression -rebless ...`). State that the gate is offline/deterministic (never clones a remote repo).
  </action>
  <acceptance_criteria>
    - `docs/BENCHMARKS.md` exists and documents median-of-5 and OS-level (external) peak RSS with the rationale.
    - It lists the pinned real repos (with commit SHAs) and has a per-repo Go-vs-TS numbers table (real or clearly-marked TBD placeholders + regenerate command).
    - It documents the regression gate tolerance bands, the INDX-06 absolute ceiling, and the explicit re-bless command.
    - Any un-run numbers are marked TBD, never fabricated.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/BENCHMARKS.md && grep -qi 'median' docs/BENCHMARKS.md && grep -qi 'rss' docs/BENCHMARKS.md && grep -qi 'rebless\|re-bless' docs/BENCHMARKS.md && grep -qi '1.3.1' docs/BENCHMARKS.md</automated>
  </verify>
  <done>Head-to-head methodology + raw numbers are documented honestly, and the regression gate + re-bless path are user-discoverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → user verification | users rely on RELEASE.md's commands to independently verify authenticity; wrong commands would give false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-18 | Repudiation | verify-command accuracy | medium | mitigate | documented verify commands reproduce the exact identity internal/upgrade enforces and only reference artifacts the pipeline actually produces |
| T-08-19 | Information Disclosure | dependency-tree honesty | low | mitigate | DIST-05 narrative makes the tree-sitter grammar-module composition explicit rather than implying a small flat tree (CONTEXT specifics) |
| T-08-20 | Repudiation | fabricated benchmark numbers | low | mitigate | raw numbers come from a real runner run; un-run values are marked TBD, never invented |
</threat_model>

<verification>
- RELEASE.md has cosign + slsa-verifier + SBOM verify commands, the DIST-05 grammar-module narrative, and the reproducibility posture.
- BENCHMARKS.md has median-of-5 + external-RSS methodology, pinned repos, a Go-vs-TS numbers table, and the re-bless path.
</verification>

<success_criteria>
DIST-05's audited-dependency story is documented honestly, DIST-02/DIST-03 verify commands are user-usable and match the real pipeline, and PERF-01 methodology + raw numbers are published — the phase's guarantees are verifiable by users, not just asserted.
</success_criteria>

<output>
Create `.planning/phases/08-release-hardening-benchmarks/08-09-SUMMARY.md` when done.
</output>