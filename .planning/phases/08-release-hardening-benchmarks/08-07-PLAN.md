---
phase: 08-release-hardening-benchmarks
plan: 07
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-06"]
files_modified:
  - tools/bench/realcorpus/manifest.go
  - tools/bench/runner/main.go
  - tools/bench/baseline.json
autonomous: true
requirements: [PERF-01, PERF-02, INDX-06]
must_haves:
  truths:
    - "The runner has a headtohead mode that shells out to BOTH the freshly-built Go binary and the installed TS codegraph@1.3.1 and reports median-of-5 metrics per real repo (PERF-01)"
    - "The runner has a regression mode that generates the synthetic 100k+ corpus, indexes it with the Go binary, and compares metrics against the committed baseline.json via CheckRegression (PERF-02 + INDX-06 ceiling)"
    - "Peak RSS is captured at the OS level via bench.PeakRSSBytes on each child process — comparable across the Go and Node runtimes"
    - "The real-repo corpus manifest records source URL, pinned tag AND commit SHA, license, and exact file-selection rule per entry"
    - "An initial baseline.json is committed, established by a first real regression run"
  prohibitions:
    - "MUST NOT read peak RSS in-process — measure the child process externally via bench.PeakRSSBytes (D-05)"
    - "MUST NOT clone a remote repo in regression mode — use the offline generated corpus (D-04)"
    - "MUST NOT rewrite baseline.json except under an explicit -rebless flag (D-05)"
    - "MUST NOT pass unpinned/attacker-controlled arguments to the shelled-out binaries — args are fixed pinned repos + generated corpus paths (V5)"
  artifacts:
    - path: tools/bench/runner/main.go
      provides: "standalone CLI: -mode {headtohead,regression}, -rebless, median-of-5, OS-level RSS"
      contains: "func main"
    - path: tools/bench/realcorpus/manifest.go
      provides: "pinned real-repo manifest (URL+tag+commit SHA+license+selection) reusing the spike ATTRIBUTION pattern"
      contains: "Manifest"
    - path: tools/bench/baseline.json
      provides: "initial committed baseline metrics for the synthetic corpus"
      contains: "peak_rss_bytes"
  key_links:
    - from: tools/bench/runner/main.go
      to: internal/bench
      via: "uses bench.PeakRSSBytes, bench.Metrics, bench.CheckRegression"
      pattern: "internal/bench"
    - from: tools/bench/runner/main.go
      to: tools/bench/gencorpus
      via: "regression mode materializes the deterministic corpus before indexing"
      pattern: "gencorpus"
    - from: tools/bench/runner/main.go
      to: /opt/homebrew/bin/codegraph
      via: "headtohead mode shells out to the installed TS codegraph@1.3.1"
      pattern: "codegraph"
---

<objective>
Build the benchmark runner that turns the measurement core (Plan 08-02), the synthetic corpus (Plan 08-03), and the regression gate (Plan 08-06) into two real jobs: a published head-to-head comparison against the installed TS `codegraph@1.3.1` on pinned real repos (PERF-01), and a network-free CI regression run against the deterministic 100k+ corpus with an absolute peak-RSS ceiling (PERF-02 + INDX-06). Establish the initial committed baseline (D-04/D-05).

Purpose: This is where the whole project's core-value claim ("faster, single verifiable binary") is measured with a fair, external, apples-to-apples methodology.
Output: `tools/bench/runner`, `tools/bench/realcorpus/manifest.go`, `tools/bench/baseline.json`.
</objective>

## Artifacts this phase produces (Plan 08-07)

- `tools/bench/realcorpus/manifest.go` (new) — pinned real-repo `Manifest` (URL, tag, commit SHA, license, selection rule).
- `tools/bench/runner/main.go` (new) — CLI with `-mode headtohead|regression`, `-rebless`, median-of-5, OS-level RSS.
- `tools/bench/baseline.json` (new) — initial committed baseline for the synthetic corpus.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-release-hardening-benchmarks/08-CONTEXT.md
@.planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md
@.planning/phases/08-release-hardening-benchmarks/08-PATTERNS.md

<interfaces>
<!-- Consumes the three prior bench artifacts. Recover the manifest analog from git history. -->

internal/bench (Plans 08-02, 08-06):
  func PeakRSSBytes(state *os.ProcessState) (int64, error)
  type Metrics struct { Subject, Repo, GOOS, GOARCH string; FilesPerSec, BytesPerSec,
    QueryLatencyMedianMS float64; PeakRSSBytes int64; ColdStartMS float64 }
  func CheckRegression(baseline, current Metrics, ceilingBytes int64) error
  const DefaultThroughputTolerance, DefaultRSSTolerance

tools/bench/gencorpus (Plan 08-03):
  func Generate(opts Options) (Stats, error)  // Options{Seed, FileCount, OutDir}

Head-to-head reference (VERIFIED present): TS codegraph@1.3.1 at /opt/homebrew/bin/codegraph.
Reuse pinned-fixture discipline: git show e5da8e7:tools/spike/testdata/ATTRIBUTION.md (URL+tag+commit+license+selection).
Existing pinned corpora present in-repo: testdata/golden/corpus/colbymchenry-codegraph, testdata/golden/corpus/weft-go.
Metric capture: from-scratch `index --force` for throughput; a fixed query set for latency median; `--version`/`status` wall time for cold start; bench.PeakRSSBytes for peak RSS. median-of-5 per metric.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pinned real-repo corpus manifest</name>
  <files>tools/bench/realcorpus/manifest.go</files>
  <read_first>
    - git show e5da8e7:tools/spike/testdata/ATTRIBUTION.md and git show e5da8e7:tools/spike/corpus.go (pinned-fixture pattern)
    - .planning/phases/08-release-hardening-benchmarks/08-PATTERNS.md § realcorpus
  </read_first>
  <action>
    Create `tools/bench/realcorpus/manifest.go` (package `realcorpus`) defining a `Manifest` type and an exported `Corpora()` (or a package var) listing the pinned head-to-head repos. Each entry records: Name, SourceURL, Tag, CommitSHA, License, and the exact file-selection rule (mirroring the spike ATTRIBUTION discipline — reproducibility per D-04 extended to PERF-01). Include the already-pinned in-repo corpora (spf13/cobra, pallets/flask via the existing testdata/golden/corpus entries) plus at least one larger real repo pinned by commit SHA to exercise scale (Claude's discretion on which, per CONTEXT D-04). Provide a helper that resolves each entry to a local path (prefer the committed testdata copy where present; document that any not-yet-vendored larger repo is shallow-cloned by the bench.yml job at a pinned SHA, never at HEAD). Add doc comments explaining that every entry MUST carry a commit SHA so head-to-head numbers are reproducible.
  </action>
  <acceptance_criteria>
    - `go build ./tools/bench/realcorpus/` exits 0.
    - Every manifest entry has a non-empty CommitSHA, License, and SelectionRule field (assert via a small test or a `go vet`-clean struct-literal review).
    - The manifest references the existing testdata/golden/corpus entries by their recorded pinned commit.
  </acceptance_criteria>
  <verify>
    <automated>go build ./tools/bench/realcorpus/ && go vet ./tools/bench/realcorpus/</automated>
  </verify>
  <done>A reproducible, provenance-complete real-repo corpus manifest exists for the PERF-01 head-to-head.</done>
</task>

<task type="auto">
  <name>Task 2: The runner CLI — headtohead + regression modes, median-of-5, OS-level RSS</name>
  <files>tools/bench/runner/main.go</files>
  <read_first>
    - internal/bench (PeakRSSBytes, Metrics, CheckRegression — Plans 08-02/08-06)
    - tools/bench/gencorpus (Generate — Plan 08-03)
    - .planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md Pattern 1/2/3 + § "System Architecture Diagram" (bench.yml/ci.yml roles)
  </read_first>
  <action>
    Create `tools/bench/runner/main.go` (package `main`) with flags `-mode` (headtohead|regression), `-go-binary` (path to the freshly-built Go codegraph), `-ts-binary` (default /opt/homebrew/bin/codegraph), `-baseline` (default tools/bench/baseline.json), `-ceiling-bytes` (INDX-06 absolute peak-RSS ceiling), and `-rebless`. Implement a shared measurement primitive that runs a given `exec.Cmd`, calls `Wait()`, and reads peak RSS via `bench.PeakRSSBytes(cmd.ProcessState)` plus wall time — running each measured command 5 times and taking the median (per D-05). In headtohead mode: for each realcorpus entry, run a from-scratch `index --force` with both the Go binary and the TS binary, plus the fixed query set and a cold-start command, capture per-subject `bench.Metrics` (Subject "go"/"ts", Repo name), and emit raw per-repo numbers as JSON to stdout (consumed by bench.yml → docs/BENCHMARKS.md). In regression mode: materialize the synthetic corpus via `gencorpus.Generate` into a scratch temp dir (no network), run the Go binary's `index --force` capturing `bench.Metrics`, load the committed baseline, and call `bench.CheckRegression(baseline, current, ceilingBytes)` — exit non-zero on a returned error (this is the blocking gate). Add `-rebless`: when set, overwrite `-baseline` with the freshly-measured Metrics (the ONLY path that writes baseline.json) and exit 0 without gating. Only ever pass fixed pinned repos / generated corpus paths as subprocess arguments (V5). Never read RSS in-process. Add doc comments describing each mode and the re-bless discipline.
  </action>
  <acceptance_criteria>
    - `go build ./tools/bench/runner/` exits 0 and the binary prints usage listing `-mode`, `-rebless`, `-ceiling-bytes`.
    - `grep -q 'bench.PeakRSSBytes' tools/bench/runner/main.go` and `grep -q 'CheckRegression' tools/bench/runner/main.go` both match; `grep -q 'runtime.MemStats' tools/bench/runner/main.go` finds NO match.
    - regression mode calls `gencorpus.Generate` (no remote clone) and exits non-zero when CheckRegression returns an error.
    - baseline.json is written ONLY under `-rebless`.
  </acceptance_criteria>
  <verify>
    <automated>go build ./tools/bench/runner/ && go vet ./tools/bench/runner/ && grep -q 'bench.PeakRSSBytes' tools/bench/runner/main.go && ! grep -q 'runtime.MemStats' tools/bench/runner/main.go</automated>
  </verify>
  <done>The runner drives both binaries externally with median-of-5 OS-level RSS (headtohead) and gates the synthetic corpus against the baseline with an absolute ceiling (regression).</done>
</task>

<task type="auto">
  <name>Task 3: Establish and commit the initial baseline.json from a first real regression run</name>
  <files>tools/bench/baseline.json</files>
  <read_first>
    - tools/bench/runner/main.go (Task 2)
    - .planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md Pattern 3 (baseline shape + re-bless)
  </read_first>
  <action>
    Build the Go codegraph binary locally, then run the runner in regression mode with `-rebless` against a small-but-representative synthetic corpus size (large enough to be meaningful for the committed baseline; document the exact `-count` and `-seed` used inside baseline.json or an adjacent README note so the number is reproducible). Commit the resulting `tools/bench/baseline.json` containing the measured Metrics (files/sec, bytes/sec, query latency median, peak RSS bytes, cold start) plus the seed/count/host metadata needed to interpret it. Add a short note (in baseline.json as metadata fields or an adjacent comment file) documenting the explicit re-bless command a human runs to intentionally update it (`go run ./tools/bench/runner -mode regression -rebless ...`). Because the full 120k materialization is heavy, it is acceptable for the COMMITTED baseline to use a documented smaller deterministic count while the CI gate (Plan 08-08) runs the full 100k+; state clearly which count the committed baseline reflects so the gate compares like-for-like.
  </action>
  <acceptance_criteria>
    - `tools/bench/baseline.json` exists and parses as JSON with keys for files/sec, peak RSS bytes, and the seed/count metadata.
    - `python3 -c "import json; d=json.load(open('tools/bench/baseline.json')); assert 'peak_rss_bytes' in json.dumps(d).lower() or True"` exits 0 (valid JSON).
    - The file (or an adjacent note) documents the exact reproducible seed/count and the human re-bless command.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json; json.load(open('tools/bench/baseline.json'))"</automated>
  </verify>
  <done>A reproducible, documented committed baseline exists so the PERF-02 gate has a reference and a clear re-bless path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner → external subject binaries (Go + TS) | the runner spawns two external binaries with fixed, non-attacker-supplied args |
| baseline.json → gate | the committed baseline is the trusted reference for pass/fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-14 | Tampering | measurement fairness | medium | mitigate | OS-level RSS via bench.PeakRSSBytes (normalized) + median-of-5 keeps the Go-vs-Node comparison honest and noise-damped (D-05) |
| T-08-10 | Denial of Service | network in the gate | medium | mitigate | regression mode uses the offline generated corpus; no remote clone can flake the gate (D-04) |
| T-08-15 | Input Validation | subprocess arguments | low | mitigate | only fixed pinned repos / generated corpus paths are passed as args — no untrusted external input (V5) |
| T-08-12 | Tampering | baseline integrity | medium | mitigate | baseline.json is written only under an explicit -rebless flag |
</threat_model>

<verification>
- Runner builds; both modes present; OS-level RSS only; no network in regression.
- Manifest carries full provenance per entry.
- baseline.json committed, valid JSON, reproducible seed/count documented.
</verification>

<success_criteria>
PERF-01 head-to-head numbers can be produced against the installed TS 1.3.1, PERF-02's regression run gates the synthetic corpus against a committed baseline, and INDX-06's absolute peak-RSS ceiling is enforced — all measured externally and fairly.
</success_criteria>

<output>
Create `.planning/phases/08-release-hardening-benchmarks/08-07-SUMMARY.md` when done.
</output>
