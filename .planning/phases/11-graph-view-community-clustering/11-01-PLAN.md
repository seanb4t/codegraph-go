---
phase: 11-graph-view-community-clustering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - corpora/graph-cluster-threshold.json
  - go.mod
  - internal/query/community.go
  - internal/query/community_test.go
  - internal/query/traverse.go
  - internal/uiproto/uiv1/ui.proto
  - internal/uiserver/handlers.go
  - internal/uiserver/readonly_test.go
  - internal/uiserver/filegraph_test.go
autonomous: true
requirements: [GRF-09, GRF-08, GRF-06]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`corpora/graph-cluster-threshold.json` is the phase's FIRST commit and is ALONE in it: `git show --name-only --format= <that commit> | wc -l` prints 1, and `git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l` prints 1 at the end of this plan (D-05); it carries `metrics.clusteringTimeMs.max = 500`, `statistic = \"median\"`, `runs = 3`, `minNodes = 2`, the guava pin `94f39958baf7ad51ddf9c70e406ed6b188194daa`, and an `onFailure` string naming `Node` field 50 `community_id` as the index-time-persistence fallback (D-06, D-07)"
    - "`go.mod` gains exactly one line — `gonum.org/v1/gonum v0.17.0` in the DIRECT require block (`git diff --numstat <base> -- go.mod` prints `1 0`), `go.sum` is unchanged, and `GOTOOLCHAIN=go1.26.6 go list -m gonum.org/v1/gonum` prints `gonum.org/v1/gonum v0.17.0` (D-13; research's `// indirect`-removal wording is corrected — gonum was not in go.mod at all, only in go.sum)"
    - "`AssignCommunities(nodes, edges)` in `internal/query/community.go` returns exactly one 1-based id per input node, dense `1..N`, canonical by smallest member path, computed via `community.Modularize(g, 1.0, rand.NewPCG(communitySeed1, communitySeed2))` over a `simple.WeightedUndirectedGraph` whose node ids are assigned in ascending path order and whose pair weights SUM both directions of a file pair (D-01, D-02, D-03)"
    - "GRF-08: `TestAssignCommunitiesDeterministic` runs the algorithm at least 3 times (5) on the structured fixture AND on the standalone 4-cycle tie fixture, asserts element-for-element equality, and logs `compared %d runs` with an assertion that the count is >= 3; a seed perturbation through the unexported `communityOptions` seam flips the tie fixture (`TestAssignCommunitiesSeedPerturbationFlipsTheTieFixture` finds a flipping offset and logs it) — the discriminator the 11-05 mutation log turns RED"
    - "GRF-08 ordering: `FileGraph()` node output order is unchanged (ascending path), ids are canonical by smallest member path — on the structured fixture `a/x.go a/y.go a/z.go` = 1, `b/p.go b/q.go b/r.go` = 2, `lone.go` = 3, `s/1.go..s/4.go` = 4, `CommunityCount` = 4 (measured this session against gonum v0.17.0 with the committed seed)"
    - "GRF-08 empty: zero nodes yields an empty map and `CommunityCount` 0; one node yields `{only.go: 1}`; zero edges over N nodes yields ids `1..N` in ascending path order — `TestAssignCommunitiesDegenerate`"
    - "GRF-08 encoding: path comparison is byte-wise (`sort.Strings` / `<`), never case-folded or normalised — `B.go` sorts before `a.go`, so with both as singletons `B.go` = 1 and `a.go` = 2; `community.go` contains zero `strings.ToLower`/`unicode`/`norm` references"
    - "GRF-06 concurrency: every `FileGraph()` call builds its own gonum graph inside `AssignCommunities`; `community.go` declares exactly one package-level `var` (`communityDefaults`) and imports no `sync` package, so two concurrent `FileGraph()` calls share no graph state (D-15)"
    - "D-15 fresh-per-call: two consecutive `FileGraph()` calls over the same fake reader return identical `CommunityID`s and `CommunityCount`; `community.go`/`traverse.go` contain no cache, no `sync.Once`, no store write, and no reference to a `Node` field-50 accessor (`GetCommunityId`) — `TestFileGraphCommunitySourceIsFreshComputeOnly` (the assumption-delta invariant) with `AssignCommunities(` in `traverse.go` as its positive control"
    - "D-09 wire: `FileGraphNode.community_id = 5` and `FileGraphResponse.community_count = 7` exist in `ui.proto`; `task proto:gen` output, `uiProtoFieldFixtureLenAtPlan1101 = uiProtoFieldFixtureLenAtPlan1001 + 2`, the two fixture rows, and the rebuilt `web/build` land in ONE commit; `^  rpc ` count in `ui.proto` stays 16 and `TestUIServiceMethodSetIsExactlyTheReadSet` stays green; `TestFileGraphProjectsEngineResult` compares `community_id`/`community_count` field-for-field against an independent `eng.FileGraph()` and asserts every node's `community_id >= 1` and `community_count >= 1`"
    - statement: "GRF-08 adjacency: an equal-modularity tie between candidate communities resolves the same way on every run — gonum v0.17.0's `deltaQ` sorts its candidate list (`slices.Sort(candidates)`) and keeps the first strict-greater gain, so the tie-break is by ascending community index, which is by ascending node id, which is by ascending path (read from `$GOMODCACHE/gonum.org/v1/gonum@v0.17.0/graph/community/louvain_undirected.go` this session); the in-repo evidence is the tie sub-test's run-to-run equality, not a re-implementation of the rule"
      verification: backstop
  artifacts:
    - "corpora/graph-cluster-threshold.json — schemaVersion 1, purpose, corpus{repo, sha, note}, metrics.clusteringTimeMs{max 500, unit ms, statistic median, runs 3, definition}, minNodes 2, recordedNonBinding{fileGraphWallMs, nodeCount, edgeCount, communityCount}, measurementProtocol, onFailure, lockedBy, prohibitions[2]"
    - "internal/query/community.go — `communitySeed1`, `communitySeed2`, `communityResolution`, `communityOptions`, `communityDefaults`, `AssignCommunities`, `assignCommunitiesWith`, `undirectedPairWeights`, `canonicalCommunityLabels`"
    - "internal/query/traverse.go — `FileGraphNode.CommunityID int`, `FileGraphResult.CommunityCount int`, the `AssignCommunities` call site after the cycle block"
    - "internal/query/community_test.go — `communityStructuredFixture`, `communityTieFixture`, `TestAssignCommunitiesDeterministic`, `TestAssignCommunitiesSeedPerturbationFlipsTheTieFixture`, `TestAssignCommunitiesCanonicalRelabelNeutralisesInsertionOrder`, `TestAssignCommunitiesDegenerate`, `TestUndirectedPairWeightsSumBothDirections`, `TestFileGraphPopulatesCommunityFields`, `TestFileGraphCommunitySourceIsFreshComputeOnly`"
    - "internal/uiproto/uiv1/ui.proto — `FileGraphNode.community_id = 5`, `FileGraphResponse.community_count = 7` (regenerated `ui.pb.go`, `ui.connect.go`, `web/src/lib/gen/ui_pb.ts` committed alongside)"
    - "internal/uiserver/handlers.go — `CommunityId: int32(n.CommunityID)` in `fileGraphNodeToProto`, `CommunityCount: int32(result.CommunityCount)` in `fileGraphToProto`; internal/uiserver/readonly_test.go — `uiProtoFieldFixtureLenAtPlan1101`, two fixture rows"
  key_links:
    - "`FileGraph()` rollup (sorted `result.Nodes`, aggregated `result.Edges`) → `AssignCommunities` → `result.Nodes[i].CommunityID` / `result.CommunityCount` (the exact block shape of the `stronglyConnectedCycles` call site)"
    - "`FileGraphResult` → `fileGraphToProto` / `fileGraphNodeToProto` → `FileGraphResponse.community_count` (7) / `FileGraphNode.community_id` (5) → generated TS client (`communityCount`, `communityId`)"
    - "`corpora/graph-cluster-threshold.json` (this plan's first commit) → read by `tools/graphcluster` (11-02) → ancestor of every measurement commit (11-02's persisted test)"
  prohibitions:
    - "The threshold commit MUST contain no other file — not the harness, not the engine, not a doc line; and no task after Task 1 may edit `corpora/graph-cluster-threshold.json`"
    - "`FileGraph()` MUST NOT cache, memoise, or persist community assignments on the fresh path — no package-level map, no `sync.Once`, no store write (D-15)"
    - "`AssignCommunities` MUST NOT emit id 0 for any input node once it runs; 0 is `not computed` and appears only on a `FileGraphNode` that never passed through it (D-03)"
    - "No file under `web/src` other than the regenerated `web/src/lib/gen/ui_pb.ts` may change in this plan — no colouring, no toolbar line, no palette before the GRF-09 verdict (roadmap blocking rule, D-08)"
    - "No rpc may be added or renamed — `wantUIServiceMethods` stays at 16 entries and no other proto message changes (D-09)"
    - "Generated outputs (`*.pb.go`, `ui.connect.go`, `ui_pb.ts`, `web/build/**`) MUST NOT be hand-edited — only `task proto:gen` / `task web:build` produce them"
    - "The seed constants MUST NOT be described, stored, or treated as a secret — they are committed in the clear as determinism constants (11-RESEARCH V6)"
---

<objective>
Lock GRF-09's pass condition as the phase's first, solitary commit, then land the thinnest end-to-end slice that exists before the verdict: `gonum` promoted to a direct require, deterministic Louvain community assignment computed fresh inside `FileGraph()` (the `CycleID` precedent), the two additive wire fields, and the field-for-field projection proven over the real listener. Then harden GRF-08's proof with the RED-control seam, the canonical-relabel demonstration, the degenerate cases, and the assumption-delta invariant.

Purpose: the threshold must provably predate every measurement (D-05); the engine and wire must exist before `tools/graphcluster` (11-02) can time anything; and GRF-08's determinism must be a discriminating test — one the 11-05 mutation log can watch fail — before any UI colours depend on it.

Output: `corpora/graph-cluster-threshold.json` (alone, first); `go.mod` +1 line; `internal/query/community.go` + `community_test.go`; `CommunityID`/`CommunityCount` on `FileGraphNode`/`FileGraphResult`; `community_id = 5` / `community_count = 7` on the wire with regenerated Go + TS, the `uiProtoFieldFixtureLenAtPlan1101` fixture extension, the handler mapping, `TestFileGraphProjectsEngineResult` extended, and `web/build` rebuilt drift-clean.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-graph-view-community-clustering/11-CONTEXT.md
@.planning/phases/11-graph-view-community-clustering/11-RESEARCH.md
@.planning/phases/11-graph-view-community-clustering/11-PATTERNS.md
@.planning/phases/11-graph-view-community-clustering/11-VALIDATION.md
@.planning/phases/10-index-health-the-coverage-denominator/10-01-SUMMARY.md
@.planning/milestones/v0.12.0-phases/05-file-package-graph-view/05-CONTEXT.md

Toolchain landmine: every local `go build` / `go test` / `go vet` / `go list` MUST be prefixed `GOTOOLCHAIN=go1.26.6` (local Go is 1.27.1; `cockroachdb/swiss` breaks otherwise). `go mod tidy` is NOT usable on this branch (pre-existing `tree-sitter-swift` resolution failure, STATE.md Phase 4 precedent) — the `go.mod` line is added by hand. `task proto:gen` regenerates BOTH proto surfaces (Go + TS) in one invocation; `web/src/lib/gen/ui_pb.ts` is a hashed `web:drift` source, so the regenerated client must be followed by `task web:build` with the rebuilt `web/build/**` committed in the same commit. `rg -h` is `--help`; never `rg -c`; positive counts via `rg -o … | wc -l`.

Facts measured this planning session (gonum v0.17.0, `$GOMODCACHE`, a scratch module): (1) gonum's Louvain is deterministic given canonical node ids and a fixed `math/rand/v2` source — the only randomness is a Fisher-Yates shuffle of the node-processing order, and `deltaQ` sorts its candidate communities before a strict-`>` comparison; (2) `simple.WeightedUndirectedGraph.SetWeightedEdge` REPLACES the weight of an existing pair, so a file pair present in both directions must be summed before insertion; (3) on the structured fixture below the committed seed pair yields a/*=1, b/*=2, lone.go=3, s/*=4 and the result is invariant across 64 seeds; (4) on the standalone 4-cycle tie fixture (`a.go-b.go-c.go-d.go-a.go`, weights 1) the committed seed pair yields `{a,b}{c,d}` and seed offset +1 yields `{a,d}{b,c}` — the tie fixture is what a seed perturbation can flip; (5) `go list -deps -f '{{.Module}}' gonum.org/v1/gonum/graph/community` resolves to the gonum module alone, so no other `go.sum` entry is needed; (6) gonum is NOT in `go.mod` today (only `go.sum:537-538`), so the promotion is one ADDED line in the direct block, not an `// indirect` removal.
</context>

<assumption_delta_decision>
Detector fired (`pluralization`, term `fallback`, on the D-07 index-time-persistence fallback). Orchestrator decision, recorded here verbatim:

- **Noun now primary:** "community assignment source".
- **Decision:** `no-change` — the sole primary is fresh computation inside `FileGraph()` (D-15). `Node` field 50 stays reserved and unwritten. IF the GRF-09 verdict is FAIL, the fallback is executed as a `promote` (the persisted assignment becomes the primary and the fresh compute path is REMOVED), never `add-alongside` — two sources of truth for one colouring would let a stale persisted id contradict a fresh one.
- **Invariant test (accepted):** `TestFileGraphCommunitySourceIsFreshComputeOnly` (Task 3) — a file-scoped source scan asserting `internal/query/community.go` and `internal/query/traverse.go` reference no `Node` field-50 accessor (`GetCommunityId`, `CommunityId`) while the verdict is PASS, with `AssignCommunities(` in `traverse.go` as the positive control. 11-02 Task 3 inverts this test deliberately, and only, on a FAIL verdict.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Commit `corpora/graph-cluster-threshold.json` ALONE — GRF-09's pass condition locked before any clustering code exists (D-05, D-06, D-07)</name>
  <reversibility rating="costly">By protocol this artifact is never amended after measurement (D-05 — one-way by policy, the GRF-01 `T-05-01` discipline); the values themselves were locked at discuss-phase (D-06: 500 ms median-of-3 on guava; D-07: field-50 fallback), so no `checkpoint:decision` is emitted — the door was confirmed there.</reversibility>
  <files>corpora/graph-cluster-threshold.json</files>
  <read_first>
    - corpora/graph-render-threshold.json (whole — the shape to mirror: `schemaVersion`, `purpose`, `corpus`, `metrics.<name>.{max, definition}`, `recordedNonBinding`, `measurementProtocol`, `onFailure`, `lockedBy`, `prohibitions`)
    - .planning/milestones/v0.12.0-phases/05-file-package-graph-view/05-CONTEXT.md (the threshold-before-measurement decisions; `onFailure` written first, never widened)
    - .planning/phases/11-graph-view-community-clustering/11-CONTEXT.md (D-05, D-06, D-07, D-08; `<specifics>` — the `purpose` string must say amending/widening after measurement is forbidden and name the fallback field explicitly)
    - corpora/manifest.json (the guava entry — copy `sha` verbatim; it is the sole pin authority)
    - .planning/phases/11-graph-view-community-clustering/11-VALIDATION.md (Wave 0 row for the threshold: `maxClusteringMs: 500`, `runs: 3`, `statistic: median`, pre-written `onFailure`)
  </read_first>
  <action>
    Create `corpora/graph-cluster-threshold.json` (2-space indent, trailing newline, keys in the order listed) with EXACTLY these values — every one is read by `tools/graphcluster` in 11-02, which carries no defaults of its own:

    - `schemaVersion`: 1.
    - `purpose`: the string "GRF-09's pass condition, locked before any measurement exists (D-05, the GRF-01 protocol). tools/graphcluster reads every value here — corpus, metrics.clusteringTimeMs (max, statistic, runs), minNodes and onFailure — rather than carrying its own defaults. Amending, widening or relaxing any value in this file after tools/graphcluster measures against it is forbidden; on FAIL the remedy is onFailure (index-time persistence into Node field 50, community_id), never a raised bar."
    - `corpus`: `repo` "google/guava", `sha` "94f39958baf7ad51ddf9c70e406ed6b188194daa" (verbatim from `corpora/manifest.json`), `note` stating the SHA is copied from `corpora/manifest.json`, that the corpus is the largest locally indexed one at file-rollup scale (3,233 file nodes / 21,554 file-pair edges per GRF-01), and that the harness opens the already-indexed store under `~/.cache/codegraph/corpora/` READ-ONLY and never re-indexes it.
    - `metrics.clusteringTimeMs`: `max` 500, `unit` "ms", `statistic` "median", `runs` 3, `definition` the string "Elapsed wall-clock time of one AssignCommunities(nodes, edges) call over the FileGraphResult rollup of the pinned corpus — from the rollup being complete (FileGraph() returned) to the community assignment map being complete — measured in-process by tools/graphcluster, truncated to integer milliseconds from time.Duration, over `runs` cold calls (a fresh store snapshot and a fresh FileGraph() per run; no cache, no warm-up call excluded, no GC forced between runs), binding statistic the median (the sorted middle value, never an average). PASS iff median <= max; equality passes. The comparison is integer-only: no floats anywhere in the verdict path."
    - `minNodes`: 2, with a sibling `minNodesNote`: "The harness REFUSES (exit 2, no observation written) when the pinned corpus resolves to fewer than 2 file nodes — a 0 ms observation over an empty rollup is not a meaningful PASS."
    - `recordedNonBinding`: an object with four keys — `fileGraphWallMs` (definition: total `FileGraph()` wall time per run, includes one embedded clustering; recorded so the split between scan cost and clustering cost stays visible), `nodeCount`, `edgeCount`, `communityCount` — each with a one-sentence `definition` saying it is recorded, never a bar.
    - `measurementProtocol`: `harness` "tools/graphcluster", `storeSource` "the cached corpus store resolved through corpora/manifest.json + internal/corpora.Entry.Dir + .codegraph/store, opened with graphstore.Open + Snapshot, never indexer.Run", `runs` 3, `statistic` "median", `precision` "integer milliseconds truncated from time.Duration", `comparison` "PASS iff median <= max; equality passes", `observationFile` "corpora/graph-cluster-observations.json", `observationCarriesThresholdDigest` true with a note that the observation records a sha256 of this file's bytes so a later edit to this file is detected by a persisted test, and `ancestryProof` "tools/graphcluster/ancestry_test.go — the commit that added this file must be an ancestor (or the same commit, after a squash-merge) of every commit touching the observation file and the harness".
    - `onFailure`: the string "If clusteringTimeMs.median exceeds max, the observation records verdict FAIL verbatim and the phase executes the documented fallback: community assignment is PROMOTED to an index-time computation persisted on the file-kind Node record as int32 community_id = 50 (the first number of internal/schema/graph.proto's reserved 50-59 range), written in the indexer's commit batch and read back by FileGraph() IN PLACE OF the fresh per-call compute, which is removed — one source of truth, never two. Lowering, widening or re-scoping this threshold is NOT an available remedy."
    - `lockedBy`: "11-01-PLAN.md Task 1 (D-05); committed alone as the phase's first commit, before internal/query/community.go, tools/graphcluster or any web/src change exists."
    - `prohibitions`: exactly two strings — "MUST NOT amend, widen, or relax any value in this file after tools/graphcluster measures against it." and "MUST NOT judge GRF-09 against any value not present in this committed artifact."

    Commit it ALONE: `git add corpora/graph-cluster-threshold.json` and commit with message `feat(11-01): lock GRF-09 clustering-time threshold before any measurement` — nothing else staged, no other file in the diff. Record the commit SHA in the SUMMARY as `thresholdCommit`. Do not touch this file again in any later task of this phase.
  </action>
  <verify>
    <automated>test "$(git show --name-only --format= HEAD | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(git show --name-only --format= HEAD | rg -o '^corpora/graph-cluster-threshold\.json$' | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l | tr -d ' ')" = "1" &amp;&amp; git diff --quiet -- corpora/graph-cluster-threshold.json</automated>
    <fails_when>HEAD touches more or fewer than exactly one file, that file is not the threshold, the threshold has more than one commit, or the working tree copy differs from the committed one</fails_when>
    <automated>node -e 'const t=JSON.parse(require("fs").readFileSync("corpora/graph-cluster-threshold.json","utf8"));const m=t.metrics.clusteringTimeMs;const ok=t.schemaVersion===1&amp;&amp;m.max===500&amp;&amp;m.statistic==="median"&amp;&amp;m.runs===3&amp;&amp;m.unit==="ms"&amp;&amp;t.minNodes===2&amp;&amp;t.corpus.repo==="google/guava"&amp;&amp;t.corpus.sha==="94f39958baf7ad51ddf9c70e406ed6b188194daa"&amp;&amp;/community_id/.test(t.onFailure)&amp;&amp;/field 50|= 50/.test(t.onFailure)&amp;&amp;/PROMOTED/.test(t.onFailure)&amp;&amp;/forbidden/.test(t.purpose)&amp;&amp;Array.isArray(t.prohibitions)&amp;&amp;t.prohibitions.length===2&amp;&amp;t.measurementProtocol.harness==="tools/graphcluster"&amp;&amp;typeof t.recordedNonBinding.fileGraphWallMs==="object";console.log("threshold keys ok:",ok);process.exit(ok?0:1)'</automated>
    <fails_when>any locked value differs (max 500, median, 3 runs, ms, minNodes 2, guava pin), `onFailure` does not name `community_id` / field 50 / PROMOTED, `purpose` does not say forbidden, or the prohibitions array is not exactly two entries</fails_when>
    <automated>test "$(rg -o 'manifest\.json' corpora/graph-cluster-threshold.json | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(node -e 'const m=JSON.parse(require("fs").readFileSync("corpora/manifest.json","utf8"));const e=(m.corpora||m.entries||[]).find(x=>x.repo==="google/guava");process.stdout.write(e?e.sha:"MISSING")')" = "94f39958baf7ad51ddf9c70e406ed6b188194daa"</automated>
    <fails_when>the threshold does not cite the manifest as the pin authority, or the manifest's guava SHA is not the one the threshold pins (a pin drift between the two files)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l` prints 1 and that commit's `--name-only` list is exactly one path
    - The SUMMARY records `thresholdCommit: <sha>` and the verbatim `git show --stat` line count
    - `rg -o '"max": 500' corpora/graph-cluster-threshold.json | wc -l` prints 1; `rg -o '"runs": 3' corpora/graph-cluster-threshold.json | wc -l` prints 1; `rg -o '"statistic": "median"' corpora/graph-cluster-threshold.json | wc -l` prints 2 (metrics + measurementProtocol)
    - `rg -o 'never a raised bar' corpora/graph-cluster-threshold.json | wc -l` prints 1
    - No other file in the repository changed in this task (`git show --stat HEAD --format= | tail -1` reports `1 file changed`)
  </acceptance_criteria>
  <done>The threshold artifact exists in exactly one commit that contains nothing else, every value 11-02's harness needs is present with no defaults left to the code, and the fallback is written down before any measurement can run.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end "one file-level community assignment reaches the wire" — `go.mod` → `AssignCommunities` → `FileGraph()` → `community_id = 5` / `community_count = 7` over the real listener (D-01, D-02, D-03, D-09, D-13, D-15)</name>
  <precondition>`test -x web/node_modules/.bin/protoc-gen-es` succeeds (else `pnpm -C web install --frozen-lockfile`) and `GOTOOLCHAIN=go1.26.6 go version` prints `go1.26.6` — `task proto:gen`, `task web:build` and every Go gate below depend on both</precondition>
  <reversibility rating="costly">`community_id = 5` and `community_count = 7` are wire field numbers — additive, but renumbering later is a client break; both were LOCKED at discuss-phase (D-09) and research confirmed 5 and 7 are the next free numbers. The `go.mod` promotion is reversible (one line). No `checkpoint:decision` is emitted.</reversibility>
  <files>go.mod, internal/query/community.go, internal/query/community_test.go, internal/query/traverse.go, internal/uiproto/uiv1/ui.proto, internal/uiserver/handlers.go, internal/uiserver/readonly_test.go, internal/uiserver/filegraph_test.go</files>
  <read_first>
    - internal/query/filegraph_cycles.go (whole — the sibling precedent: package doc "does NOT read the store, does NOT know about edge kinds", pure function of already-scanned data, sorted-input-derived ids)
    - internal/query/traverse.go:73-129 (`FileGraphNode.CycleID` and `FileGraphResult.CycleCount` doc comments — the field shape to mirror) and :311-330 (the `stronglyConnectedCycles` call block: `distinctCycles` map, `result.Nodes[i].CycleID = id`, `result.CycleCount = len(distinctCycles)` — the exact shape the community block copies)
    - internal/query/filegraph_cycles_test.go:91-115 (`TestFileGraphCyclesDeterministicIds`) and :161-210 (`TestFileGraphPopulatesCycleFields` — the `traverseFakeReader{nodes, edges}` fixture shape), internal/query/traverse_test.go:24-60 (`traverseFakeReader`)
    - go.mod:5-39 (the DIRECT require block — insert the new line between `golang.org/x/tools v0.48.0` and `google.golang.org/protobuf v1.36.11`, module paths sorted), go.sum:537-538 (gonum already pinned)
    - $GOMODCACHE/gonum.org/v1/gonum@v0.17.0/graph/community/louvain_common.go:90 (`Modularize(g graph.Graph, resolution float64, src rand.Source) ReducedGraph`, `rand` = `math/rand/v2`), louvain_undirected.go (`reduceUndirected`'s `order.ByID` sort; `Communities() [][]graph.Node`), $GOMODCACHE/gonum.org/v1/gonum@v0.17.0/graph/simple/weighted_undirected.go (`NewWeightedUndirectedGraph(self, absent float64)`, `NewWeightedEdge`, `SetWeightedEdge` REPLACES an existing pair's weight, `AddNode`)
    - internal/uiproto/uiv1/ui.proto:856-870 (`FileGraphNode`, `cycle_id = 4`) and :913-934 (`FileGraphResponse`, `cycle_count = 6`), :50-146 (the 16 rpc lines — unchanged)
    - internal/uiserver/handlers.go:1015-1055 (`fileGraphToProto`, `fileGraphNodeToProto` — add one field each)
    - internal/uiserver/readonly_test.go:282-295 (`uiProtoFieldFixtureLenAtPlan1001` and its chained-extension doc comment), :504-521 (the `FileGraphNode`/`FileGraphResponse` fixture rows), :553-572 (Plan 10-01's rows — append after them), :597-600 (`TestUIProtoFieldNumbersAreStableAndUnique`'s length check — repoint at the new constant)
    - internal/uiserver/filegraph_test.go:29-125 (`TestFileGraphProjectsEngineResult` — extend its node loop and response comparison)
    - Taskfile.yml:305-368 (`proto:gen`), :739-760 (`web:build`), :1187-1300 (`web:drift` hashes `web/src/lib/gen/ui_pb.ts`)
    - .planning/phases/10-index-health-the-coverage-denominator/10-01-SUMMARY.md (the wire-commit grouping lesson: keep proto + gen + fixture + web/build in ONE commit; capture the wire RED transcript BEFORE committing rather than as a separate RED commit)
  </read_first>
  <behavior>
    - Test 1 (`internal/query/community_test.go` `TestAssignCommunitiesDeterministic`, GRF-08 / D-04): `communityStructuredFixture()` returns nodes `a/x.go a/y.go a/z.go b/p.go b/q.go b/r.go lone.go s/1.go s/2.go s/3.go s/4.go` (built as `[]FileGraphNode` in ascending path order) and edges: the triangle `a/x-a/y, a/y-a/z, a/x-a/z` at `TotalCount` 3, the triangle `b/p-b/q, b/q-b/r, b/p-b/r` at 3, the bridge `a/z.go -> b/p.go` at 1, and the 4-cycle `s/1-s/2, s/2-s/3, s/3-s/4, s/4-s/1` at 1 (each as a single directed `FileGraphEdge`); `lone.go` has no edge. Sub-test `structured`: call `AssignCommunities` 5 times, assert every run equals the first element-for-element AND equals the exact expected map `{a/x.go:1, a/y.go:1, a/z.go:1, b/p.go:2, b/q.go:2, b/r.go:2, lone.go:3, s/1.go:4, s/2.go:4, s/3.go:4, s/4.go:4}`; sub-test `tie`: `communityTieFixture()` = nodes `a.go b.go c.go d.go`, edges `a-b, b-c, c-d, d-a` at 1; call 5 times, assert run-to-run equality, exactly 2 distinct ids each with exactly 2 members, and `a.go` = 1. Both sub-tests `t.Logf("compared %d runs", n)` and `t.Fatalf` if `n < 3`.
    - Test 2 (`TestFileGraphPopulatesCommunityFields`, GRF-06 / D-15): a `traverseFakeReader` with file-kind nodes `a.go`, `b.go`, `c.go` and calls-edges `a->b`, `b->a`, `a->c` (mirroring `TestFileGraphPopulatesCycleFields`): every returned node has `CommunityID >= 1`, `CommunityCount == number of distinct ids`, `CommunityCount >= 1`, node paths are still ascending (`sort.StringsAreSorted`), and a SECOND `e.FileGraph()` call returns identical `CommunityID`s and `CommunityCount` (fresh per call, no drift).
    - Test 3 (`internal/uiserver/filegraph_test.go` `TestFileGraphProjectsEngineResult` EXTENDED, D-09): in the existing node loop assert `got.GetCommunityId() == int32(want.CommunityID)` and `got.GetCommunityId() >= 1` for every node; after the loop assert `got.GetCommunityCount() == int32(want.CommunityCount)` and `got.GetCommunityCount() >= 1` (positive control: the gofixture yields at least one community), plus `t.Logf("communities on the wire: %d over %d nodes", …)`.
    - Test 4 (`internal/uiserver/readonly_test.go` `TestUIProtoFieldNumbersAreStableAndUnique`, unchanged body): passes only when both new rows resolve in the generated descriptor and `len(uiProtoFieldNumbers) == uiProtoFieldFixtureLenAtPlan1101`; `TestUIServiceMethodSetIsExactlyTheReadSet` still reports 16.
  </behavior>
  <action>
    Commit boundaries: Commit A = `go.mod` + engine (`test(11-01): …` RED transcript pasted, then `feat(11-01): …`); Commit B = the wire slice in ONE commit (proto + `task proto:gen` output + fixture + handler + rebuilt `web/build`), with the wire RED transcript captured between `task proto:gen` and the handler edit and pasted in the SUMMARY — not committed as a separate RED commit (the 10-01 lesson). Every commit builds with `GOTOOLCHAIN=go1.26.6 go build ./...`.

    **Commit A — `go.mod` + `internal/query/community.go` + `traverse.go` (D-13, D-01, D-02, D-03, D-15).** Hand-add the line `gonum.org/v1/gonum v0.17.0` (tab-indented, no `// indirect`) to the FIRST `require (` block of `go.mod`, between `golang.org/x/tools v0.48.0` and `google.golang.org/protobuf v1.36.11` — `go mod tidy` is blocked on this branch (STATE.md, Phase 4 precedent) and research verified the closure needs no other module. Create `internal/query/community.go` with a package-doc header mirroring `filegraph_cycles.go` (does NOT read the store, does NOT know edge kinds, never runs client-side; computed fresh on every `FileGraph()` call, never cached, never persisted — D-15; the reserved `Node` 50-59 range stays unwritten unless GRF-09 fails). Declare: `communitySeed1 uint64 = 0x636f646567726170` and `communitySeed2 uint64 = 0x6c6f757661696e31` (ASCII "codegrap" / "louvain1") with a doc comment stating they are DETERMINISM constants committed in the clear, NOT secrets, chosen once and never changed (Claude's Discretion, 11-RESEARCH V6); `communityResolution = 1.0`; an unexported `communityOptions struct { seed1, seed2 uint64; reverseInsertion bool; skipCanonicalRelabel bool }` documented as the TEST-ONLY seam D-04 requires (production never sets the two bools); `var communityDefaults = communityOptions{seed1: communitySeed1, seed2: communitySeed2}` — the only package-level `var` in the file; `func AssignCommunities(nodes []FileGraphNode, edges []FileGraphEdge) map[string]int` (exported so `tools/graphcluster` can time it in isolation) that simply returns `assignCommunitiesWith(nodes, edges, communityDefaults)`; `func assignCommunitiesWith(nodes []FileGraphNode, edges []FileGraphEdge, opts communityOptions) map[string]int`; `func undirectedPairWeights(idByPath map[string]int64, edges []FileGraphEdge) map[[2]int64]float64`; `func canonicalCommunityLabels(members [][]string) map[string]int`.

    `assignCommunitiesWith`: return an empty (non-nil) map for zero nodes. Build `paths` as a copy of every `n.Path`, `sort.Strings(paths)` (byte-wise — D-02a is enforced INSIDE the function, not left to the caller), then — unless `opts.reverseInsertion` — assign `idByPath[paths[i]] = int64(i)` and `g.AddNode(simple.Node(int64(i)))` on a `simple.NewWeightedUndirectedGraph(0, 0)` in that order; with `reverseInsertion` assign ids from the reversed slice (the perturbation seam). Weights: `undirectedPairWeights` folds every edge into an unordered key `[2]int64{min(u,v), max(u,v)}` SUMMING `float64(e.TotalCount)` across both directions (measured this session: `SetWeightedEdge` replaces, so `a->b` 3 and `b->a` 2 must become one undirected weight 5), skipping `TotalCount <= 0`, skipping `u == v`, and skipping an endpoint absent from `idByPath`; then iterate the pair keys in sorted order and `g.SetWeightedEdge(g.NewWeightedEdge(simple.Node(u), simple.Node(v), w))`. Run `reduced := community.Modularize(g, communityResolution, rand.NewPCG(opts.seed1, opts.seed2))` (`math/rand/v2`). Convert `reduced.Communities()` to `[][]string` of member paths (via a reverse `pathByID` slice). If `opts.skipCanonicalRelabel`, label communities by their gonum index + 1 (test-only); else `canonicalCommunityLabels`: sort the communities by their lexically-smallest member path (byte-wise `<`), assign `1..N` in that order, and return `map[path]label` — every input node present, no zero.

    In `traverse.go` add `CommunityID int` to `FileGraphNode` after `CycleID` with a doc comment mirroring `CycleID`'s: 1-based canonical community id assigned by `AssignCommunities` fresh on every `FileGraph()` call; 0 means "not computed" and is never emitted once the assignment has run (D-03). Add `CommunityCount int` to `FileGraphResult` after `CycleCount`: the number of distinct community ids over `Nodes` (singletons count — Claude's Discretion resolved as "yes", so `CommunityCount == max id`). Immediately after `result.CycleCount = len(distinctCycles)` add the community block in the identical shape: `communityIDs := AssignCommunities(result.Nodes, result.Edges)`, then `for i := range result.Nodes { result.Nodes[i].CommunityID = communityIDs[result.Nodes[i].Path] }` (no `ok` check — every node has exactly one id, D-03) accumulating a `distinctCommunities` set, then `result.CommunityCount = len(distinctCommunities)`, with a comment: computed fresh alongside everything else `FileGraph()` derives, no cache, no store write (D-15).

    Write Tests 1–2 FIRST (RED: `undefined: AssignCommunities` / `CommunityID`), run the RED gate below, implement, run GREEN. Also run the full `./internal/query/...` package to prove nothing else moved.

    **Commit B — the wire slice, ONE commit (D-09, the 09-01/10-01 recipe).** In `internal/uiproto/uiv1/ui.proto` add `int32 community_id = 5;` to `FileGraphNode` after `cycle_id = 4` (doc: mirrors `FileGraphNode.CommunityID`, 1-based canonical, 0 only when not computed — GRF-06, D-03/D-09) and `int32 community_count = 7;` to `FileGraphResponse` after `cycle_count = 6` (doc: mirrors `FileGraphResult.CommunityCount`, singletons counted, so the UI reports the number without recounting — D-12a). No other proto change; no rpc. Run `task proto:gen` (regenerates `ui.pb.go`, `ui.connect.go`, `web/src/lib/gen/ui_pb.ts`). In `readonly_test.go`: add `const uiProtoFieldFixtureLenAtPlan1101 = uiProtoFieldFixtureLenAtPlan1001 + 2` with the chained-extension doc comment (plan 11-01, Phase 11 GRF-06/GRF-09: `FileGraphNode.community_id` + `FileGraphResponse.community_count` = 2, both additive on existing messages, no new rpc — the method set stays 16), append the rows `{"FileGraphNode", "community_id", 5}` and `{"FileGraphResponse", "community_count", 7}` after Plan 10-01's seventeen with a "Plan 11-01's two" comment, and repoint `TestUIProtoFieldNumbersAreStableAndUnique`'s length check (both the comparison and its message) at `uiProtoFieldFixtureLenAtPlan1101`. Extend `TestFileGraphProjectsEngineResult` per Test 3 and run it NOW — it must be RED (`community_id` 0 on every node, `community_count` 0) because the handler does not map the fields yet; paste that transcript. Then add `CommunityId: int32(n.CommunityID)` to `fileGraphNodeToProto` and `CommunityCount: int32(result.CommunityCount)` to `fileGraphToProto` (field-for-field, named mapper, same convention as `CycleId`/`CycleCount`). GREEN. Then `task web:build && task -s web:drift` and commit the regenerated Go + TS, the fixture, the handler, the extended test AND the rebuilt `web/build/**` (including `.build-manifest`) in this ONE commit: `feat(11-01): FileGraphNode.community_id=5, FileGraphResponse.community_count=7 on the wire`.

    Do NOT touch `web/src` except the regenerated `web/src/lib/gen/ui_pb.ts`; do NOT touch `corpora/graph-cluster-threshold.json`, `internal/schema/graph.proto`, `internal/indexer`, `internal/cli`, or `internal/mcp`.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestAssignCommunitiesDeterministic|TestFileGraphPopulatesCommunityFields' ./internal/query/... 2&gt;&amp;1 | tee /tmp/11-01-red-a.txt; test "$(rg -o -- '--- FAIL|undefined:|build failed' /tmp/11-01-red-a.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>RED gate (run BEFORE `community.go` exists): passes only if at least one FAIL/undefined/build-failed line appears; a green run here means the tests were not written first or do not reference the new symbols</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestAssignCommunitiesDeterministic|TestFileGraphPopulatesCommunityFields|TestFileGraphPopulatesCycleFields|TestFileGraphCyclesDeterministicIds' ./internal/query/... 2&gt;&amp;1 | tee /tmp/11-01-green-a.txt | tail -3; test "$(rg -o -- '--- PASS: Test(AssignCommunitiesDeterministic|FileGraphPopulatesCommunityFields|FileGraphPopulatesCycleFields|FileGraphCyclesDeterministicIds)' /tmp/11-01-green-a.txt | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/11-01-green-a.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'compared [0-9]+ runs' /tmp/11-01-green-a.txt | wc -l | tr -d ' ')" -ge 2 &amp;&amp; ! rg -q 'no tests to run' /tmp/11-01-green-a.txt</automated>
    <fails_when>fewer than 4 named PASS lines, any FAIL, fewer than two `compared N runs` log lines (both sub-tests must report their run count), or "no tests to run"</fails_when>
    <automated>test "$(git diff --numstat "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)" -- go.mod | tr -s '\t' ' ')" = "1 0 go.mod" &amp;&amp; git diff --quiet "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)" -- go.sum &amp;&amp; test "$(rg -o '^\tgonum\.org/v1/gonum v0\.17\.0$' go.mod | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(GOTOOLCHAIN=go1.26.6 go list -m gonum.org/v1/gonum)" = "gonum.org/v1/gonum v0.17.0" &amp;&amp; GOTOOLCHAIN=go1.26.6 go list -deps ./... &gt;/dev/null</automated>
    <fails_when>the go.mod diff since the threshold commit is not exactly one added line, go.sum moved, the line carries `// indirect` or a different version, `go list -m` resolves another version, or `go list -deps ./...` reports "updates to go.mod needed"</fails_when>
    <automated>test "$(rg -o 'rand\.NewPCG\(opts\.seed1, opts\.seed2\)' internal/query/community.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '"math/rand/v2"' internal/query/community.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'community\.Modularize\(' internal/query/community.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'sort\.Strings\(paths\)' internal/query/community.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'func AssignCommunities\(nodes \[\]FileGraphNode, edges \[\]FileGraphEdge\) map\[string\]int' internal/query/community.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^var ' internal/query/community.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '"sync"|sync\.Once|strings\.ToLower|unicode|norm\.' internal/query/community.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'AssignCommunities\(result\.Nodes, result\.Edges\)' internal/query/traverse.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'CommunityID int' internal/query/traverse.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'CommunityCount int' internal/query/traverse.go | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the fixed-seed PCG source, the `math/rand/v2` import, the single `Modularize` call, the in-function `sort.Strings(paths)`, the exported signature, the single package-level var, or the `FileGraph()` call site/fields are missing — or `community.go` grew a sync primitive or a case-folding/normalising call (GRF-08 encoding)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestFileGraphProjectsEngineResult|TestUIServiceMethodSetIsExactlyTheReadSet|TestUIProtoFieldNumbersAreStableAndUnique|TestUIServiceDeclaresNoMutatingMethod|TestFileGraphKindCountsAreSparse' ./internal/uiserver/... 2&gt;&amp;1 | tee /tmp/11-01-green-b.txt | tail -3; test "$(rg -o -- '--- PASS: Test(FileGraphProjectsEngineResult|UIServiceMethodSetIsExactlyTheReadSet|UIProtoFieldNumbersAreStableAndUnique|UIServiceDeclaresNoMutatingMethod|FileGraphKindCountsAreSparse)' /tmp/11-01-green-b.txt | wc -l | tr -d ' ')" = "5" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/11-01-green-b.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'communities on the wire: [1-9][0-9]* over' /tmp/11-01-green-b.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; ! rg -q 'no tests to run' /tmp/11-01-green-b.txt</automated>
    <fails_when>fewer than 5 named PASS lines, any FAIL, or the wire test does not log a positive community count (the positive control that the mapping carried real values)</fails_when>
    <automated>test "$(rg -o 'int32 community_id = 5;' internal/uiproto/uiv1/ui.proto | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'int32 community_count = 7;' internal/uiproto/uiv1/ui.proto | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^  rpc ' internal/uiproto/uiv1/ui.proto | wc -l | tr -d ' ')" = "16" &amp;&amp; test "$(rg -o 'uiProtoFieldFixtureLenAtPlan1101 = uiProtoFieldFixtureLenAtPlan1001 \+ 2' internal/uiserver/readonly_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '\{"FileGraphNode", "community_id", 5\}' internal/uiserver/readonly_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '\{"FileGraphResponse", "community_count", 7\}' internal/uiserver/readonly_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'CommunityId: *int32\(n\.CommunityID\)' internal/uiserver/handlers.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'CommunityCount: *int32\(result\.CommunityCount\)' internal/uiserver/handlers.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'communityCount' web/src/lib/gen/ui_pb.ts | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'communityId' web/src/lib/gen/ui_pb.ts | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>either field number/name differs, the rpc count moved off 16, the fixture rows or the +2 constant are missing, the handler does not map both fields, or the TS client lacks the new members (proto:gen not run or not committed)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task proto:drift &gt;/tmp/11-01-drift.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/11-01-drift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i 'mismatch|drift detected' /tmp/11-01-drift.txt | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>proto:drift exits non-zero or reports a mismatch — a generated file was hand-edited or not regenerated from the committed .proto</fails_when>
    <automated>task web:build &amp;&amp; task -s web:drift &gt;/tmp/11-01-webdrift.txt 2&gt;&amp;1; RC=$?; cat /tmp/11-01-webdrift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '\bhalf MATCH\b' /tmp/11-01-webdrift.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o 'web:drift: PASS' /tmp/11-01-webdrift.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test -z "$(git status --porcelain -- web/build web/src/lib/gen)"</automated>
    <fails_when>web:drift does not print both `half MATCH` lines and `PASS`, or `web/build`/`ui_pb.ts` has uncommitted changes — the regenerated client was not rebuilt and committed together</fails_when>
    <automated>test "$(git diff --name-only "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)" HEAD -- web/src | rg -v '^web/src/lib/gen/ui_pb\.ts$' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(git diff --name-only "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)" HEAD -- internal/schema internal/indexer internal/cli internal/mcp | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>any `web/src` file other than the regenerated client changed since the threshold commit (UI wiring before the verdict), the threshold gained a second commit, or the schema/indexer/cli/mcp trees were touched</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= <commit B>` lists `internal/uiproto/uiv1/ui.proto`, `internal/uiproto/uiv1/ui.pb.go`, `internal/uiproto/uiv1/uiv1connect/ui.connect.go`, `web/src/lib/gen/ui_pb.ts`, `internal/uiserver/readonly_test.go`, `internal/uiserver/handlers.go`, `internal/uiserver/filegraph_test.go` and `web/build/.build-manifest` — ONE commit
    - The SUMMARY pastes the engine RED transcript (`undefined: AssignCommunities` or equivalent), the engine GREEN transcript with both `compared 5 runs` lines, the wire RED transcript (community fields 0 before the handler mapping) and the wire GREEN transcript
    - `rg -o 'func assignCommunitiesWith\(nodes \[\]FileGraphNode, edges \[\]FileGraphEdge, opts communityOptions\) map\[string\]int' internal/query/community.go | wc -l` prints 1; `rg -o 'func undirectedPairWeights\(' internal/query/community.go | wc -l` prints 1; `rg -o 'func canonicalCommunityLabels\(' internal/query/community.go | wc -l` prints 1
    - `rg -o 'communitySeed1 uint64 = 0x636f646567726170' internal/query/community.go | wc -l` prints 1 and `rg -o 'communitySeed2 uint64 = 0x6c6f757661696e31' internal/query/community.go | wc -l` prints 1; the doc comment above them contains the literal `not a secret`
    - `rg -o 'assignCommunitiesWith\(' internal/query/traverse.go | wc -l` prints 0 (production goes through `AssignCommunities` only)
    - `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/query/... ./internal/uiserver/...` prints two `ok` lines and zero `FAIL`
  </acceptance_criteria>
  <done>A real indexed fixture served over the real listener returns a 1-based `community_id` on every `FileGraphNode` and a positive `community_count`, both equal field-for-field to an independent `eng.FileGraph()`; the assignment is computed fresh inside `FileGraph()` by `AssignCommunities` with sorted-path ids, summed undirected weights, a fixed PCG seed and canonical relabeling; `go.mod` carries gonum as one direct line; `proto:drift` and `web:drift` both MATCH; nothing under `web/src` was authored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GRF-08 hardening — the RED-control seam demonstrated, canonical relabel shown load-bearing, degenerate inputs, summed pair weights, and the assumption-delta invariant (D-02, D-03, D-04)</name>
  <reversibility rating="reversible">Tests and an unexported seam only; no wire or storage contract changes.</reversibility>
  <files>internal/query/community_test.go, internal/query/community.go</files>
  <read_first>
    - internal/query/community.go (as left by Task 2 — `communityOptions`, `assignCommunitiesWith`, `undirectedPairWeights`, `canonicalCommunityLabels`)
    - internal/query/community_test.go (Task 2's fixtures `communityStructuredFixture` / `communityTieFixture` — reuse, do not duplicate)
    - internal/query/coverage_test.go (`TestCoverageSourceNeverWalksDisk` — the file-scoped, positive-controlled source-scan shape for the invariant test)
    - internal/cli/editordiscovery_test.go:299-341 (`TestEditorDiscoverySourceNeverSpawnsAProcess` — the same shape, older precedent)
    - .planning/phases/11-graph-view-community-clustering/11-CONTEXT.md (D-04: perturbing order or seed through a test-only hook must turn the test red; D-02c: canonical relabeling is mandatory, not cosmetic)
  </read_first>
  <behavior>
    - Test 1 (`TestAssignCommunitiesSeedPerturbationFlipsTheTieFixture`, D-04 RED control): baseline = `AssignCommunities(tie fixture)`; for `k := 1; k <= 32; k++` call `assignCommunitiesWith(nodes, edges, communityOptions{seed1: communitySeed1 + uint64(k), seed2: communitySeed2})`; the FIRST `k` whose result differs from the baseline is logged (`t.Logf("seed offset %d flipped the tie fixture", k)`) and the test FAILS if no offset within 32 flips it (measured this session: offset 1 flips `{a,b}{c,d}` to `{a,d}{b,c}`). Then assert the STRUCTURED fixture does NOT change under that same offset (log "structured fixture invariant under the flipping offset") — documenting that a well-separated graph is seed-invariant while a modularity tie is not, which is exactly why the fixed seed is load-bearing.
    - Test 2 (`TestAssignCommunitiesCanonicalRelabelNeutralisesInsertionOrder`, D-02c): on the structured fixture, `reverseInsertion: true` with canonical relabel ON equals the default result element-for-element; `reverseInsertion: true` with `skipCanonicalRelabel: true` differs from `reverseInsertion: false, skipCanonicalRelabel: true` in at least one label (measured this session: raw gonum indices swap under reversed ids) — positive control that the relabel step is what neutralises insertion order, not gonum by itself.
    - Test 3 (`TestAssignCommunitiesDegenerate`): (a) zero nodes → `len(got) == 0`, non-nil map, and a `FileGraph()` over an empty fake reader reports `CommunityCount == 0`; (b) one node `only.go` → `{only.go: 1}`; (c) four nodes, zero edges → ids `1,2,3,4` in ascending path order; (d) encoding: nodes `B.go` and `a.go`, no edges → `B.go` = 1, `a.go` = 2 (byte order, uppercase first); (e) every id in every case is `>= 1` and the max id equals the distinct count (dense).
    - Test 4 (`TestUndirectedPairWeightsSumBothDirections`): `idByPath{a:0,b:1,c:2}`, edges `a->b` 3, `b->a` 2, `a->c` 1, `c->c` 9 (self, must be skipped), `a->zzz` 4 (unknown endpoint, skipped), `b->c` 0 (zero, skipped) → exactly `{[0 1]: 5, [0 2]: 1}`.
    - Test 5 (`TestFileGraphCommunitySourceIsFreshComputeOnly`, assumption-delta invariant): read `internal/query/community.go` and `internal/query/traverse.go`; forbidden substrings `GetCommunityId(`, `.CommunityId` (the schema `Node` field-50 accessor shapes), `sync.Once`, `NewWriter(`, `PutNode(` — zero occurrences across both files; positive control: `AssignCommunities(result.Nodes, result.Edges)` present exactly once in `traverse.go` and `community.Modularize(` present exactly once in `community.go`; the test logs the byte count inspected and fails if either file is empty. Comment in the test: `mutation: read Node field 50 instead of computing` so 11-05 can find it; note that 11-02 Task 3 is the ONLY sanctioned inversion (verdict FAIL → promote).
  </behavior>
  <action>
    Write Tests 1–5 (RED first for anything referencing a not-yet-existing helper; Tests 1–2 use the seam Task 2 already declared, so their RED is a failing assertion, not a compile error — if Task 2 left `reverseInsertion`/`skipCanonicalRelabel` unimplemented as no-ops, Test 2's positive-control leg goes RED, which is the point). Implement whatever the seam still lacks in `community.go` so the two bools genuinely change behaviour: `reverseInsertion` assigns ids from the reversed sorted slice; `skipCanonicalRelabel` returns gonum's own community index + 1. Keep both unreachable from production (`communityDefaults` leaves them false; `traverse.go` never calls `assignCommunitiesWith`). Add the `mutation:` anchor comment to Test 5 and, on `TestAssignCommunitiesDeterministic`'s tie sub-test, the comment `mutation: per-call seed counter` (11-05 family (a) replaces the fixed `seed1` with a per-call incrementing counter starting at `communitySeed1` — run 2 then uses the flipping offset and the tie sub-test goes RED). Run the named tests GREEN, then the whole `./internal/query/...` package.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestAssignCommunitiesSeedPerturbationFlipsTheTieFixture|TestAssignCommunitiesCanonicalRelabelNeutralisesInsertionOrder|TestAssignCommunitiesDegenerate|TestUndirectedPairWeightsSumBothDirections|TestFileGraphCommunitySourceIsFreshComputeOnly' ./internal/query/... 2&gt;&amp;1 | tee /tmp/11-01-t3-red.txt; test "$(rg -o -- '--- FAIL|undefined:|build failed' /tmp/11-01-t3-red.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>RED gate (before the seam behaviour / helpers are complete): no FAIL/undefined line means the tests were not written first or every seam leg was already implemented — in the latter case record that in the SUMMARY and proceed (the seam legs were implemented by Task 2), never fake a RED</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestAssignCommunitiesDeterministic|TestAssignCommunitiesSeedPerturbationFlipsTheTieFixture|TestAssignCommunitiesCanonicalRelabelNeutralisesInsertionOrder|TestAssignCommunitiesDegenerate|TestUndirectedPairWeightsSumBothDirections|TestFileGraphCommunitySourceIsFreshComputeOnly|TestFileGraphPopulatesCommunityFields' ./internal/query/... 2&gt;&amp;1 | tee /tmp/11-01-t3-green.txt | tail -3; test "$(rg -o -- '--- PASS: Test(AssignCommunitiesDeterministic|AssignCommunitiesSeedPerturbationFlipsTheTieFixture|AssignCommunitiesCanonicalRelabelNeutralisesInsertionOrder|AssignCommunitiesDegenerate|UndirectedPairWeightsSumBothDirections|FileGraphCommunitySourceIsFreshComputeOnly|FileGraphPopulatesCommunityFields)' /tmp/11-01-t3-green.txt | wc -l | tr -d ' ')" = "7" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/11-01-t3-green.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'seed offset [0-9]+ flipped the tie fixture' /tmp/11-01-t3-green.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; ! rg -q 'no tests to run' /tmp/11-01-t3-green.txt</automated>
    <fails_when>fewer than 7 named PASS lines, any FAIL, no logged flipping offset (the RED-control discriminator did not fire), or "no tests to run"</fails_when>
    <automated>test "$(rg -o 'mutation: per-call seed counter' internal/query/community_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'mutation: read Node field 50 instead of computing' internal/query/community_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'reverseInsertion' internal/query/community.go | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o 'skipCanonicalRelabel' internal/query/community.go | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o 'reverseInsertion|skipCanonicalRelabel|assignCommunitiesWith' internal/query/traverse.go | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>either mutation anchor is missing (11-05 cannot find its instrument), a seam bool is declared but never read in `community.go`, or the seam leaks into production `traverse.go`</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./internal/query/... &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/query/... ./internal/query/archtest/... ./internal/uiserver/... 2&gt;&amp;1 | tee /tmp/11-01-t3-pkgs.txt | tail -4; test "$(rg -o '^FAIL' /tmp/11-01-t3-pkgs.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^ok' /tmp/11-01-t3-pkgs.txt | wc -l | tr -d ' ')" = "3"</automated>
    <fails_when>vet fails, any of the three packages prints FAIL, or fewer than three `ok` lines appear (the archtest must still admit `internal/query` with its new gonum import)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'func Test(AssignCommunitiesSeedPerturbationFlipsTheTieFixture|AssignCommunitiesCanonicalRelabelNeutralisesInsertionOrder|AssignCommunitiesDegenerate|UndirectedPairWeightsSumBothDirections|FileGraphCommunitySourceIsFreshComputeOnly)\(' internal/query/community_test.go | wc -l` prints 5
    - The SUMMARY records the flipping seed offset the test found and the structured-fixture-invariant log line
    - `TestAssignCommunitiesDegenerate` covers zero nodes, one node, zero edges, and the `B.go`/`a.go` byte-order case (four `t.Run` sub-tests or equivalent named assertions)
    - No production file other than `internal/query/community.go` changed in this task
  </acceptance_criteria>
  <done>The determinism proof is discriminating: a seed perturbation through the test-only seam demonstrably flips a tie fixture, canonical relabeling is shown to be the mechanism that neutralises insertion order, degenerate and encoding cases are pinned, pair weights are proven summed, and a persisted source scan guards the fresh-compute-only invariant with a named positive control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store snapshot → `FileGraph()` → `AssignCommunities` | inputs are the store's OWN already-validated file paths and edge counts — no user-controlled input reaches the algorithm (D-16) |
| `gonum.org/v1/gonum` import closure → `internal/query` | a new direct dependency executes inside every `FileGraph()` call; its legitimacy and cgo-freedom are 11-03's controls |
| `internal/uiserver` → browser | two additive read-only int32 fields cross to the loopback SPA; nothing crosses back |
| `corpora/graph-cluster-threshold.json` → `tools/graphcluster` (11-02) | the tamper target: a widened bar after measurement would forge a PASS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering (supply chain) | `gonum.org/v1/gonum v0.17.0` direct require | high | mitigate | pinned to the version already in `go.sum` (no new module, no bump — one added `go.mod` line, `go.sum` unchanged, asserted); full closure verified 91 packages / zero cgo by research and re-proven by 11-03's `check:gonum` (govulncheck source mode + SBOM presence + cgo scan, each positive-controlled) |
| T-11-02 | Tampering | `corpora/graph-cluster-threshold.json` | high | mitigate | committed ALONE as the phase's first commit (Task 1 asserts one file, one commit); no later task edits it (every later plan asserts `git log … | wc -l` = 1); 11-02 records its sha256 in the observation and a persisted test compares them; the harness never opens it for writing |
| T-11-03 | Repudiation (a colouring that silently changes between renders misleads a reviewer about ownership boundaries) | `AssignCommunities` | medium | mitigate | three determinism mechanisms in code (sorted ids, fixed PCG seed, canonical relabel); `TestAssignCommunitiesDeterministic` (≥3 runs, count logged) + the seed-perturbation RED control (Task 3) + 11-05 family (a) watched RED |
| T-11-04 | Denial of Service | pathological file-pair graph runtime inside `FileGraph()` | low | accept | local, read-only developer tool over the user's own index; GRF-09 (11-02) measures the largest known corpus against a committed bar — the bar is the control, no additional hardening |
| T-11-05 | Information Disclosure | `communitySeed1/2` misread as a secret | low | accept | they are determinism constants committed in the clear by design (11-RESEARCH V6); the doc comment says `not a secret`; nothing cryptographic depends on them |
| T-11-06 | Spoofing | DNS rebinding against the FileGraph rpc that now carries two more fields | low | accept | inherited — no new rpc or mux entry; `originHostGuard` unchanged (03-SECURITY T-03-13 disposition) |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | none in this plan; the only dependency change is a Go module promotion of a package already resolved in `go.sum` — legitimacy recorded `[ASSUMED]` (long-lived gonum project, already transitive via sigstore); the `package-legitimacy check` seam lists npm/pypi/crates only, so no human-verify checkpoint is inserted (11-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/query/... ./internal/query/archtest/... ./internal/uiserver/...` — all `ok`
- `GOTOOLCHAIN=go1.26.6 task proto:drift` — MATCH for all four generated files
- `task -s web:drift` — both halves MATCH with `web/build/**` committed alongside `ui_pb.ts`
- `git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l` = 1, first commit of the phase, alone
- `git diff --name-only <threshold commit> HEAD -- web/src` lists only `web/src/lib/gen/ui_pb.ts`
</verification>

<success_criteria>
- The threshold is committed alone, first, with the fallback written down (GRF-09 success criterion 1's precondition)
- `FileGraph()` computes a deterministic, 1-based, canonical community id for every file node fresh on every call and reports the distinct count (GRF-06 engine half, GRF-08)
- `community_id = 5` / `community_count = 7` are on the wire, projected field-for-field, with the fixture chain extended by 2 and the method set unchanged at 16 (D-09)
- The determinism test is discriminating: a seed perturbation flips a tie fixture, and the assumption-delta invariant has a persisted, positive-controlled test
</success_criteria>

## Artifacts this phase produces (complete list — Plan 01 lands the items marked ★; later plans land the rest)

- Data: ★ `corpora/graph-cluster-threshold.json` (alone, first commit); `corpora/graph-cluster-observations.json` (11-02, harness-written, carries `thresholdDigest`)
- Go `go.mod`: ★ `gonum.org/v1/gonum v0.17.0` direct require (+1 line)
- Go `internal/query`: ★ `community.go` (`communitySeed1`, `communitySeed2`, `communityResolution`, `communityOptions`, `communityDefaults`, `AssignCommunities`, `assignCommunitiesWith`, `undirectedPairWeights`, `canonicalCommunityLabels`), ★ `FileGraphNode.CommunityID`, ★ `FileGraphResult.CommunityCount`, ★ `community_test.go` (`communityStructuredFixture`, `communityTieFixture`, `TestAssignCommunitiesDeterministic`, `TestAssignCommunitiesSeedPerturbationFlipsTheTieFixture`, `TestAssignCommunitiesCanonicalRelabelNeutralisesInsertionOrder`, `TestAssignCommunitiesDegenerate`, `TestUndirectedPairWeightsSumBothDirections`, `TestFileGraphPopulatesCommunityFields`, `TestFileGraphCommunitySourceIsFreshComputeOnly`)
- Proto (UI `codegraph.ui.v1`): ★ `FileGraphNode.community_id = 5`, ★ `FileGraphResponse.community_count = 7` (regenerated `ui.pb.go`, `ui.connect.go`, `web/src/lib/gen/ui_pb.ts` committed alongside; method set unchanged at 16)
- Go `internal/uiserver`: ★ `fileGraphNodeToProto` `CommunityId`, ★ `fileGraphToProto` `CommunityCount`, ★ `uiProtoFieldFixtureLenAtPlan1101`, ★ two fixture rows, ★ `TestFileGraphProjectsEngineResult` extended
- Go `tools/graphcluster` (11-02): `main.go` (`run`, `loadThreshold`, `resolveCorpusStore`, `measureRuns`, `medianInt64`, `judge`, `writeObservation`, `thresholdDigest`), `main_test.go`, `ancestry_test.go` (`TestClusterThresholdCommitIsAncestorOfEveryMeasurement`, `TestClusterThresholdDigestMatchesCommittedObservation`)
- Conditional, FAIL only (11-02 Task 3): `internal/schema/graph.proto` `Node.community_id = 50`, `internal/community` leaf package, indexer commit-time assignment, `FileGraph()` read-back (promote)
- Taskfile: `check:gonum` (11-03), `check:no-force-layout` (11-04)
- TS `web/src/lib/components/graph` (11-04): `community-palette.ts` (`COMMUNITY_PALETTE` 12 hex, `communityPaletteIndex`, `communityDiscriminatorClass`), `file-graph-transform.ts` `FileGraphNodeData.communityId`, `graph-style.ts` 12 `node[!isDirectory].graph-community-{0..11}` rules; `web/src/routes/graph/+page.svelte` `graph-community-summary` line; tests `web/tests/graph-communities.test.ts`, `web/tests/file-graph-transform.test.ts` extension; script `web/scripts/check-no-force-layout.mjs` (`--self-test`)
- Docs (11-05): `11-MUTATION-LOG.md` (families (a) seed counter, (b) threshold widened, (c) force-layout name injected, (d) cgo positive control neutered), `11-SECURITY.md`, `11-VALIDATION.md` map filled
- Rebuilt `web/build/**` (★ this plan, and 11-04)

<output>
Create `.planning/phases/11-graph-view-community-clustering/11-01-SUMMARY.md` when done — include `thresholdCommit: <sha>`, the pasted engine and wire RED/GREEN transcripts, the flipping seed offset Task 3 found, and the exact commit SHAs of Commit A and Commit B.
</output>
