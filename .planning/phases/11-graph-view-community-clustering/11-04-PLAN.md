---
phase: 11-graph-view-community-clustering
plan: 04
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - web/src/lib/components/graph/community-palette.ts
  - web/src/lib/components/graph/file-graph-transform.ts
  - web/src/lib/components/graph/graph-style.ts
  - web/src/routes/graph/+page.svelte
  - web/tests/file-graph-transform.test.ts
  - web/tests/graph-communities.test.ts
  - web/scripts/check-no-force-layout.mjs
  - Taskfile.yml
autonomous: true
requirements: [GRF-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "File nodes are coloured by `communityId` from a 12-hue deterministic palette: `COMMUNITY_PALETTE` in `community-palette.ts` is exactly `['#2a78d6','#eb6834','#1baf7a','#eda100','#e87ba4','#008300','#4a3aa7','#e34948','#12a0b0','#b06a1c','#a259d9','#86a800']`, index = `(communityId - 1) % 12`, cycling beyond 12; validated this session with the dataviz skill's `validate_palette.js` (light surface): lightness band PASS, chroma floor PASS, adjacent-pair CVD separation PASS (worst ΔE 9.1), normal-vision floor PASS (worst 19.6); contrast WARN on four hues is relieved by the label every file node already renders beneath it (D-10)"
    - "The layout is UNCHANGED: `web/src/lib/components/graph/GraphCanvas.svelte` is byte-identical between the threshold commit and HEAD (`git diff --quiet <thr> HEAD -- …GraphCanvas.svelte`) and contains exactly one `name: 'elk'`; the colouring is 12 static `node[!isDirectory].graph-community-{0..11}` style rules setting only `background-color` (literal hex — cytoscape cannot resolve `var()`), composing with the existing `.graph-cycle` border rules (D-10)"
    - "Directory compounds stay neutral: `expandedDirElement`/`collapsedDirElement` gain no `communityId` and no `graph-community-` class (D-11) — asserted in vitest over a mixed-community directory"
    - "The transform copies `communityId` from the wire and never computes it: `data.communityId = n.communityId ?? 0`; a 0/undefined id attaches no community class (so every existing fixture renders exactly as before), a non-zero id attaches exactly one `graph-community-N` class alongside any cycle classes"
    - "GRF-06 (a): the graph toolbar shows a `data-testid=\"graph-community-summary\"` line reading the wire `communityCount` (\"… N communities …\", singular for 1) without recounting — route-level vitest against the real `+page.svelte` (D-12a)"
    - "GRF-06 (b): `node web/scripts/check-no-force-layout.mjs` scans `web/src` (`.ts .js .svelte .mjs`) plus `web/package.json` dependency names, reports `scanned N files`, `elk layout refs E` (>= 1 — positive control), `forbidden matches F` (= 0) and exits 0 only on that combination; the forbidden regexes are scoped to layout-name positions (`name: '<x>'` literals and `cytoscape-<x>` import/dependency specifiers for `cose|cose-bilkent|fcose|cola|euler|spread|force`) so `spread`/`force` in identifiers or comments never false-positive; `--self-test` injects an in-memory `name: 'cose'` source and exits 0 only if the scan DETECTS it (D-12b)"
    - "GRF-06 (c): `web/tests/graph-communities.test.ts` renders the transform against a 14-file fixture with ids 1..14 and asserts distinct colours == distinct community ids for ids 1..12, equal ids → equal colours, and ids 13/14 cycle to the colours of 1/2 (D-12c)"
    - "`pnpm -C web check` exits 0 with `0 errors` (case-insensitive) after every web edit; `task web:build` + `task -s web:drift` both halves MATCH with the rebuilt `web/build/**` committed alongside the source (09-06 lesson)"
  artifacts:
    - "web/src/lib/components/graph/community-palette.ts — `COMMUNITY_PALETTE`, `COMMUNITY_CLASS_PREFIX`, `communityPaletteIndex`, `communityDiscriminatorClass`"
    - "web/src/lib/components/graph/file-graph-transform.ts — `FileGraphNodeData.communityId?: number`; `fileNodeElement` attaches the community class"
    - "web/src/lib/components/graph/graph-style.ts — 12 `node[!isDirectory].graph-community-{i}` rules generated from `COMMUNITY_PALETTE`"
    - "web/src/routes/graph/+page.svelte — the `graph-community-summary` paragraph"
    - "web/tests/graph-communities.test.ts — palette/sheet/colour==community (Task 1) and route-level count line (Task 2) tests; web/tests/file-graph-transform.test.ts — community passthrough + directory-neutral assertions"
    - "web/scripts/check-no-force-layout.mjs — positive-controlled scan with `--self-test`; Taskfile.yml `check:no-force-layout`"
  key_links:
    - "wire `FileGraphNode.communityId` → `fileNodeElement` → `data.communityId` + `graph-community-{(id-1)%12}` class → `graph-style.ts` rule → node `background-color` on the unchanged ELK layout"
    - "wire `FileGraphResponse.communityCount` → `graphState.response.communityCount` → `graph-community-summary` text"
    - "`check-no-force-layout.mjs` → `web/src` + `web/package.json` → `elk` refs >= 1 AND forbidden 0 → exit 0; `--self-test` → injected match detected → exit 0 (11-05 family (c) plants a real `name: 'cose'` in `web/src` and watches the scan fail)"
  prohibitions:
    - "`GraphCanvas.svelte` MUST NOT change — not the layout name, not an ELK option, not a comment (byte-identical across the phase)"
    - "Directory compound nodes MUST NOT be coloured by community — no majority-colour, no union list (D-11, deferred idea)"
    - "No client-side community computation of any kind — the transform copies wire values only (the `cycleId` discipline, D-06 of Phase 5)"
    - "No new JS dependency for colours, no CSS custom property inside a cytoscape style value, no runtime hash-to-colour function — 12 literal hex values"
    - "No force-directed layout name may appear in a layout-name position anywhere under `web/src` or in `web/package.json` dependencies"
    - "No legend beyond the count line and no clustering cache — both are deferred ideas in 11-CONTEXT.md"
---

<objective>
Render the communities the engine already computes: colour every file node by its wire `communityId` from a fixed 12-hue colour-blind-safe palette on the existing ELK layered layout with the layout configuration untouched, keep directory compounds neutral, add the "N communities" toolbar line fed by `communityCount`, and commit the positive-controlled source scan that proves no force-directed layout is reachable from any code path.

Purpose: GRF-06's user-visible half — the graph reads as groups rather than a flat mesh — landing only now, after the GRF-09 verdict resolved (11-02) and the supply-chain gate passed (11-03), exactly as the roadmap's blocking rule requires.

Output: `community-palette.ts`, `file-graph-transform.ts` + `graph-style.ts` changes, the `+page.svelte` line, `web/tests/graph-communities.test.ts` + `file-graph-transform.test.ts` extensions, `web/scripts/check-no-force-layout.mjs`, `check:no-force-layout` Taskfile target, rebuilt `web/build/**`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-graph-view-community-clustering/11-CONTEXT.md
@.planning/phases/11-graph-view-community-clustering/11-RESEARCH.md
@.planning/phases/11-graph-view-community-clustering/11-PATTERNS.md
@.planning/phases/11-graph-view-community-clustering/11-02-SUMMARY.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-06-SUMMARY.md

Preconditions carried from earlier plans: `corpora/graph-cluster-observations.json` exists with a verdict (11-02) — this plan runs whichever way it fell, since the UI reads `communityId` either way; `web/src/lib/gen/ui_pb.ts` already exposes `communityId`/`communityCount` (11-01). Landmines: `pnpm -C web check` prints its machine format in UPPERCASE when piped — gate on a case-insensitive `0 errors`; never leave a `codegraph ui` process running; `web/scripts/breadcrumb-check.mjs` rewrites `corpora/breadcrumb-check.json` (do not run it; revert if it ran); existing test fixtures build `FileGraphNode`s via `as unknown as FileGraphNode` WITHOUT `communityId`, so the transform must treat `undefined` exactly like 0 or every older graph test breaks.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Palette + transform + stylesheet — `communityId` copied onto file elements, 12 static colour rules on the unchanged layout, directories neutral, proven colour == community (D-10, D-11, D-12c)</name>
  <precondition>`test -d web/node_modules` and `pnpm -C web exec vitest --version` succeed (else `pnpm -C web install --frozen-lockfile`)</precondition>
  <reversibility rating="reversible">Presentation only; palette values are Claude's Discretion and can be re-stepped without touching the wire.</reversibility>
  <files>web/src/lib/components/graph/community-palette.ts, web/src/lib/components/graph/file-graph-transform.ts, web/src/lib/components/graph/graph-style.ts, web/tests/file-graph-transform.test.ts, web/tests/graph-communities.test.ts</files>
  <read_first>
    - web/src/lib/components/graph/file-graph-transform.ts:57-115 (`FileGraphNodeData`, `CYCLE_CLASS`, `cycleDiscriminatorClass`) and :160-215 (`fileNodeElement` — where `cycleId` becomes a class; `expandedDirElement`/`collapsedDirElement` — must NOT gain a community field)
    - web/src/lib/components/graph/graph-style.ts (whole — the header's `var()`/literal-hex rule, the selector-ORDER note, `node[!isDirectory]` base rule with `background-color: #2563eb`, `node[?isSymbol]`, the `.graph-cycle` rules placed last)
    - web/src/lib/components/graph/GraphCanvas.svelte:105-165 (`name: 'elk'` and the ELK options — READ ONLY, never edited)
    - web/tests/file-graph-transform.test.ts:1-60 (the `node()`/`edge()`/`response()` helpers and the DOM-free convention) and :153-177 ("cycle information is copied, never invented" — the describe to mirror)
    - web/tests/graph-cycles.test.ts:1-200 (Task-1 section: element-data assertions + the plain-data style-sheet check `fileGraphStyle.filter(entry => entry.selector …)`)
    - web/tests/graph-collapse.test.ts:20-70, :180-200 (collapsed-directory fixtures — the shape for the D-11 neutral assertion)
    - .planning/phases/11-graph-view-community-clustering/11-RESEARCH.md § Don't Hand-Roll (12 static hex values, `(id-1) mod 12`, no dependency) and Assumptions A4 (clash with `#2563eb` / `#dc2626`)
  </read_first>
  <behavior>
    - Test 1 (`web/tests/file-graph-transform.test.ts`, new describe `file-graph-transform: community information is copied, never invented`): (a) a file node with `communityId: 4` yields element `data.communityId === 4` and classes containing exactly one `graph-community-3`; (b) `communityId: 0` and a fixture built WITHOUT the field both yield `data.communityId === 0` and NO `graph-community-` class (positive control: the same fixture with id 1 yields `graph-community-0`); (c) ids 1, 12, 13 map to classes `graph-community-0`, `graph-community-11`, `graph-community-0` (cycling); (d) a node with `cycleId: 2` and `communityId: 5` carries `graph-cycle`, `graph-cycle-2` AND `graph-community-4` (composition); (e) with directory `a/` expanded and `b/` collapsed over files of mixed communities, the `a` compound and the `b` collapsed element have `communityId === undefined` and no `graph-community-` class (D-11).
    - Test 2 (`web/tests/graph-communities.test.ts`, describe `community palette and stylesheet (Task 1)`): `COMMUNITY_PALETTE.length === 12`, every entry matches `/^#[0-9a-f]{6}$/`, all 12 unique; `communityPaletteIndex(1) === 0`, `(12) === 11`, `(13) === 0`, `(25) === 0`; `fileGraphStyle` contains exactly 12 entries whose selector matches `/^node\[!isDirectory\]\.graph-community-(\d+)$/`, index `i` in `0..11` each exactly once, each `style['background-color'] === COMMUNITY_PALETTE[i]` and each rule sets ONLY `background-color`; those 12 entries appear AFTER the `node[!isDirectory]` base rule and BEFORE the first `.graph-cycle` rule (selector-order cascade); no entry's selector contains `isDirectory]` AND `graph-community-` with `?isDirectory` (no compound colouring rule exists).
    - Test 3 (`graph-communities.test.ts`, describe `colour == community (Task 1, D-12c)`): fixture of 14 files `d/f01.go … d/f14.go` with `communityId` 1..14 plus two more files `d/g01.go`/`d/g02.go` both with id 3, directory `d` expanded; resolve each file element's colour by finding its `graph-community-N` class and looking up the sheet rule; assert: over ids 1..12 the set of colours has size 12 and equals the set of distinct ids' size; `g01`/`g02` share `f03`'s colour; `f13`'s colour equals `f01`'s and `f14`'s equals `f02`'s; the compound `d` has no colour class; `t`-style log of `distinct colours: 12 over 12 distinct ids` via `expect` messages.
  </behavior>
  <action>
    Create `web/src/lib/components/graph/community-palette.ts` with a header explaining: presentation vocabulary for the wire `communityId` (server-computed, D-06 of Phase 5 discipline — the client never computes membership); 12 literal hex values (cytoscape cannot resolve `var()`); index `(id - 1) % 12`, cycling beyond 12 (D-10); the palette is the dataviz reference categorical palette's 8 slots (blue, orange, aqua, yellow, magenta, green, violet, red) extended by teal `#12a0b0`, brown `#b06a1c`, purple `#a259d9`, lime `#86a800`, validated with `validate_palette.js --mode light` at plan time (adjacent-pair CVD ΔE worst 9.1 ≥ 8, normal-vision worst 19.6 ≥ 15; contrast WARN on `#1baf7a #eda100 #e87ba4 #86a800` relieved by the file label rendered under every node); all-pairs separation is not achievable for 12 hues and is not claimed — position, directory grouping and the label are the secondary encodings. Export `COMMUNITY_PALETTE: readonly string[]` (the 12 values in the must_haves order), `COMMUNITY_CLASS_PREFIX = 'graph-community-'`, `communityPaletteIndex(communityId: number): number` (`(communityId - 1) % COMMUNITY_PALETTE.length`, callers pass ids ≥ 1), and `communityDiscriminatorClass(communityId: number): string`.

    In `file-graph-transform.ts`: add `communityId?: number;` to `FileGraphNodeData` after `cycleId` with a comment that it is present ONLY on file nodes (directory compounds stay neutral, D-11); import `communityDiscriminatorClass` from `./community-palette`; in `fileNodeElement` set `const communityId = n.communityId ?? 0;` (the `?? 0` is load-bearing: every pre-existing fixture omits the field), `data.communityId = communityId`, and build the class list as an array — push `CYCLE_CLASS` + `cycleDiscriminatorClass(n.cycleId)` when `n.cycleId !== 0`, push `communityDiscriminatorClass(communityId)` when `communityId > 0` — returning `{ data, classes: list.join(' ') }` when non-empty else `{ data }`. Do NOT touch `expandedDirElement`, `collapsedDirElement`, or any edge element. Update the module header's vocabulary paragraph to name the community class as the THIRD copied wire field.

    In `graph-style.ts`: import `COMMUNITY_PALETTE` and `COMMUNITY_CLASS_PREFIX`; insert, after the `node[?isSymbol]` rule and before the `edge` rule, the spread `...COMMUNITY_PALETTE.map((hex, i) => ({ selector: \`node[!isDirectory].${COMMUNITY_CLASS_PREFIX}${i}\`, style: { 'background-color': hex } }))` with a comment: colours file nodes by the wire community id (GRF-06, D-10); placed after the file base rule so it overrides `#2563eb`, before the cycle rules so the dashed red border still composes on a cycle member (a community-8 cycle node is red-on-red by colour but still distinguished by the dashed 4px border — the sheet's existing two-channel rule); symbols never carry the class; no `[?isDirectory]` variant exists by design (D-11). Extend the header comment's summary sentence accordingly.

    Write Tests 1–3 FIRST (RED: `community-palette` module missing / classes absent), implement, GREEN. Then run the WHOLE web suite once (`pnpm -C web test`) — every older graph test must stay green because `undefined` maps to 0 and attaches no class.
  </action>
  <verify>
    <automated>pnpm -C web test -- graph-communities 2&gt;&amp;1 | tee /tmp/11-04-t1-red.txt | tail -6; test "$(rg -o -i 'failed|Cannot find|does not provide an export' /tmp/11-04-t1-red.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>RED gate (before `community-palette.ts` exists): no failure/missing-module signal means the tests were not written first</fails_when>
    <automated>pnpm -C web test -- file-graph graph-communities 2&gt;&amp;1 | tee /tmp/11-04-t1-web.txt | tail -8; test "$(rg -o 'Tests +[0-9]+ passed' /tmp/11-04-t1-web.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o -i '\bfailed\b' /tmp/11-04-t1-web.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'Test Files +[0-9]+ passed' /tmp/11-04-t1-web.txt | rg -o '[0-9]+' | head -1)" -ge 2</automated>
    <fails_when>the two test files do not both pass, or any test fails</fails_when>
    <automated>pnpm -C web test 2&gt;&amp;1 | tee /tmp/11-04-t1-all.txt | tail -8; test "$(rg -o 'Tests +[0-9]+ passed' /tmp/11-04-t1-all.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o -i '\bfailed\b' /tmp/11-04-t1-all.txt | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>any pre-existing web test regressed (a fixture lacking `communityId` now attaches a class, or the sheet order broke a cycle test)</fails_when>
    <automated>test "$(rg -o "'#2a78d6', *'#eb6834', *'#1baf7a', *'#eda100', *'#e87ba4', *'#008300', *'#4a3aa7', *'#e34948', *'#12a0b0', *'#b06a1c', *'#a259d9', *'#86a800'" web/src/lib/components/graph/community-palette.ts | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'communityId\?: number' web/src/lib/components/graph/file-graph-transform.ts | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'n\.communityId \?\? 0' web/src/lib/components/graph/file-graph-transform.ts | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'COMMUNITY_PALETTE\.map\(' web/src/lib/components/graph/graph-style.ts | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'var\(' web/src/lib/components/graph/graph-style.ts web/src/lib/components/graph/community-palette.ts | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'communityId' web/src/lib/components/graph/file-graph-transform.ts | wc -l | tr -d ' ')" -ge 3</automated>
    <fails_when>the palette literal differs from the validated 12, the transform field/`?? 0` guard is missing, the sheet does not generate its rules from the palette, or a `var()` slipped into a cytoscape style</fails_when>
    <automated>git diff --quiet "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)" HEAD -- web/src/lib/components/graph/GraphCanvas.svelte &amp;&amp; git diff --quiet -- web/src/lib/components/graph/GraphCanvas.svelte &amp;&amp; test "$(rg -o "name: 'elk'" web/src/lib/components/graph/GraphCanvas.svelte | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>`GraphCanvas.svelte` differs from its state at the threshold commit (committed or in the working tree) or no longer carries exactly one `name: 'elk'` — the layout was touched</fails_when>
    <automated>pnpm -C web check &gt;/tmp/11-04-t1-check.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/11-04-t1-check.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i '\b0 errors\b' /tmp/11-04-t1-check.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>svelte-check exits non-zero or does not report `0 errors` (case-insensitive — the piped machine format is uppercase)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'export (const COMMUNITY_PALETTE|const COMMUNITY_CLASS_PREFIX|function communityPaletteIndex|function communityDiscriminatorClass)' web/src/lib/components/graph/community-palette.ts | wc -l` prints 4
    - `rg -o 'communityId|graph-community' web/src/lib/components/graph/file-graph-transform.ts` shows the field only inside `FileGraphNodeData`/`fileNodeElement` — `expandedDirElement` and `collapsedDirElement` bodies contain neither token (executor confirms by reading the diff; the D-11 vitest is the machine check)
    - `web/tests/graph-communities.test.ts` contains `distinct colours` in an assertion message and the 14-file fixture; `web/tests/file-graph-transform.test.ts` gained a describe whose name contains `community information is copied, never invented`
    - The SUMMARY pastes the RED and GREEN vitest tails and the `pnpm -C web check` tail
  </acceptance_criteria>
  <done>Every file node carries its wire community as data and as one of twelve static colour classes on the untouched ELK layout, directories stay neutral, older fixtures render unchanged, and a test proves distinct colours equal distinct community ids with cycling beyond twelve.</done>
</task>

<task type="auto">
  <name>Task 2: The "N communities" toolbar line fed by `communityCount`, route-level test, rebuilt SPA drift-clean (D-12a)</name>
  <reversibility rating="reversible">One paragraph in the route; placement is Claude's Discretion (beside the cycle summary).</reversibility>
  <files>web/src/routes/graph/+page.svelte, web/tests/graph-communities.test.ts</files>
  <read_first>
    - web/src/routes/graph/+page.svelte:555-585 (`graph-summary`, `graph-cycle-summary` paragraphs — insert the community line directly after the cycle summary, before the cycle-focus button)
    - web/tests/graph-cycles.test.ts:204-345 (the route-level mocking shape: `vi.doMock('$lib/client', …)`, the fake cytoscape core, `render(GraphPage)`, `waitFor(file-graph-canvas)`, `screen.getByTestId('graph-cycle-summary')`) — copy the scaffolding into `graph-communities.test.ts`'s Task-2 section; `response()` fixtures must now include `communityCount`
    - web/src/lib/gen/ui_pb.ts (`FileGraphResponse.communityCount`, `FileGraphNode.communityId` — generated in 11-01)
    - Taskfile.yml:739-760 (`web:build`), :1187-1300 (`web:drift`)
  </read_first>
  <action>
    In `+page.svelte`, immediately after the `graph-cycle-summary` paragraph, add a sibling `<p class="mt-1 text-xs text-muted-foreground" data-testid="graph-community-summary">` whose content is: when `graphState.response.communityCount > 0` — "Files fall into {communityCount} communit{y|ies}, shown as node colours on the same layout." (singular `community` for exactly 1); else — "No communities computed." Read the count from `graphState.response.communityCount` exactly as the cycle line reads `cycleCount` — no derived state, no recount over elements, no new rpc. No legend (deferred). Add to `graph-communities.test.ts` a `describe('route: community count line (Task 2)')` with: (a) a response with `communityCount: 3` renders the line containing `3` and `communities` without any interaction; (b) `communityCount: 1` renders `1 community` (singular) and not `communities`; (c) `communityCount: 0` renders `No communities computed`; (d) the count line's text does NOT depend on the elements — a response with 5 nodes all `communityId: 1` and `communityCount: 1` still shows `1 community` (the line reports the wire number, never a client recount). Then `pnpm -C web check`, `task web:build && task -s web:drift`, and commit the source + rebuilt `web/build/**` together.
  </action>
  <verify>
    <automated>pnpm -C web test -- graph-communities graph-cycles 2&gt;&amp;1 | tee /tmp/11-04-t2-web.txt | tail -8; test "$(rg -o 'Tests +[0-9]+ passed' /tmp/11-04-t2-web.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o -i '\bfailed\b' /tmp/11-04-t2-web.txt | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the community route tests or the existing cycle route tests fail (the new paragraph must not disturb the cycle summary or focus control)</fails_when>
    <automated>test "$(rg -o 'data-testid="graph-community-summary"' web/src/routes/graph/+page.svelte | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'graphState\.response\.communityCount' web/src/routes/graph/+page.svelte | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o 'No communities computed' web/src/routes/graph/+page.svelte | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'uiClient\.' web/src/routes/graph/+page.svelte | wc -l | tr -d ' ')" = "$(git show "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)":web/src/routes/graph/+page.svelte | rg -o 'uiClient\.' | wc -l | tr -d ' ')"</automated>
    <fails_when>the paragraph or its wire-count reads are missing, or the route gained an rpc call it did not have at the threshold commit (a recount or a second fetch)</fails_when>
    <automated>pnpm -C web check &gt;/tmp/11-04-t2-check.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/11-04-t2-check.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i '\b0 errors\b' /tmp/11-04-t2-check.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>svelte-check is not clean (uppercase machine format — case-insensitive match)</fails_when>
    <automated>task web:build &amp;&amp; task -s web:drift &gt;/tmp/11-04-webdrift.txt 2&gt;&amp;1; RC=$?; cat /tmp/11-04-webdrift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '\bhalf MATCH\b' /tmp/11-04-webdrift.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o 'web:drift: PASS' /tmp/11-04-webdrift.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test -z "$(git status --porcelain -- web/build web/src)"</automated>
    <fails_when>web:drift does not print both `half MATCH` lines and `PASS`, or `web/build`/`web/src` has uncommitted changes — the rebuilt SPA was not committed with its source</fails_when>
    <human-check>End-of-phase (workflow.human_verify_mode=end-of-phase): run `codegraph ui` on this repository, open the Graph view, confirm file nodes show distinct community colours on the SAME layered layout as before (no re-layout, directories neutral with borders only), the line "Files fall into N communities …" appears under the cycle summary, and the cycle-border red still reads on a coloured cycle node; then kill the `codegraph ui` process (`pkill -f 'codegraph ui'`) and confirm `git status --porcelain -- corpora/breadcrumb-check.json` is empty.</human-check>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= HEAD` for the Task 2 commit lists `web/src/routes/graph/+page.svelte`, `web/tests/graph-communities.test.ts` and `web/build/.build-manifest` together
    - The SUMMARY pastes the vitest tail, the `pnpm -C web check` tail and the `web:drift` output
    - `pgrep -f 'codegraph ui' | wc -l` prints 0 at the end of the task
  </acceptance_criteria>
  <done>The toolbar states how many communities the wire reported, in the same voice and position as the cycle summary, proven at route level against the real page; the SPA is rebuilt and drift-clean.</done>
</task>

<task type="auto">
  <name>Task 3: `web/scripts/check-no-force-layout.mjs` — positive-controlled layout-name scan with `--self-test`, plus the `check:no-force-layout` Taskfile target (D-12b)</name>
  <reversibility rating="reversible">A committed script and a Taskfile target.</reversibility>
  <files>web/scripts/check-no-force-layout.mjs, Taskfile.yml</files>
  <read_first>
    - web/scripts/graph-verdict.mjs:1-20 (the header doctrine: says what the file does NOT do; every ambiguity resolves to FAIL) and web/scripts/breadcrumb-check.mjs:1-12 (`#!/usr/bin/env node` shebang, header naming what it proves; this script is NOT Playwright — copy only the shebang/header/exit-code discipline)
    - web/src/lib/components/graph/GraphCanvas.svelte:31-34 (`import elk from 'cytoscape-elk'`, `cytoscapeLib.use(elk)`) and :140 (`name: 'elk'`) — the two positive-control positions
    - web/package.json (`dependencies` — `cytoscape-elk` present; no other `cytoscape-*` layout extension)
    - internal/cli/editordiscovery_test.go:299-341 (the required-substring positive-control scan shape, Go analog)
    - Taskfile.yml:657-716 (`web:test` — `node -e` + printed counts + floor precedent) and :4531-4546 (`lint:actions`, `lint:go` — the short-target shape)
    - web/src/lib/live/live-client.ts:18-32 (`LIVE_BACKOFF_JITTER_SPREAD` — the identifier that MUST NOT false-positive), web/src/lib/components/graph/GraphCanvas.svelte:111 (the comment "never force-directed" — likewise)
  </read_first>
  <action>
    Create `web/scripts/check-no-force-layout.mjs` (`#!/usr/bin/env node`, ESM, `node:fs`/`node:path`/`node:url` only — no browser, no Playwright). Header: proves GRF-06's "no force-directed mode reachable from any code path" (D-12b) by static scan; states what it does NOT do (does not run the SPA, does not parse the AST — a regex scoped to layout-name positions) and that an empty scan or a scan that cannot see `elk` is a FAIL, never a pass. Resolve the repo root from `import.meta.url` (`web/scripts/..` → `web/..`) so it works from any cwd. Walk `web/src` recursively collecting files with extensions `.ts .js .svelte .mjs` (skip nothing else — `lib/gen` included). Define `FORBIDDEN_LAYOUT_NAMES = ['cose', 'cose-bilkent', 'fcose', 'cola', 'euler', 'spread', 'force']` and two forbidden regexes: `LAYOUT_NAME_RE = /\bname\s*:\s*['"](cose|cose-bilkent|fcose|cola|euler|spread|force)['"]/g` (layout-option literal) and `LAYOUT_IMPORT_RE = /['"]cytoscape-(cose-bilkent|fcose|cola|euler|spread)['"]/g` (import/dependency specifier); required regexes `ELK_NAME_RE = /\bname\s*:\s*['"]elk['"]/g` and `ELK_IMPORT_RE = /['"]cytoscape-elk['"]/g`. Also scan `web/package.json`'s `dependencies` + `devDependencies` keys: any key matching `/^cytoscape-(cose-bilkent|fcose|cola|euler|spread)$/` is a forbidden match; `cytoscape-elk` as a key counts toward `elkImportRefs`. Produce a report `{ filesScanned, elkLayoutRefs, elkImportRefs, forbiddenMatches: [{file, line, match}], verdict }` printed as one JSON line to stdout plus a human line `check-no-force-layout: scanned N files; elk layout refs E; elk import refs I; forbidden matches F; verdict PASS|FAIL`; verdict PASS iff `filesScanned > 0 && elkLayoutRefs >= 1 && elkImportRefs >= 1 && forbiddenMatches.length === 0`; exit 0 on PASS, 1 on FAIL, every forbidden match printed with file:line. `--self-test`: run the same scan function over the real tree PLUS one injected in-memory source `{ file: '<self-test>/injected.ts', text: "const layout = { name: 'cose' };" }`; the self-test PASSES (exit 0, prints `check-no-force-layout self-test: PASS — injected 'cose' detected at <self-test>/injected.ts:1`) only if exactly that injected match is reported and the real tree contributed zero forbidden matches; otherwise exit 1 with `self-test: FAIL — the scan did not detect the injected layout name` (a scan that cannot see a planted match must not be trusted). Word boundaries and the `name:` anchor are what keep `LIVE_BACKOFF_JITTER_SPREAD`, `forced`, and the "never force-directed" comment from matching — assert in the self-test that `filesScanned` for the real tree is `>= 50` (a broken walk must fail loud).

    Add a `check:no-force-layout` target to `Taskfile.yml` after `check:gonum` (`desc:` GRF-06 D-12b — positive-controlled static scan; `preconditions:` `command -v node`; `cmds:` `node web/scripts/check-no-force-layout.mjs --self-test` then `node web/scripts/check-no-force-layout.mjs`). Do not wire it into ci.yml in this plan.
  </action>
  <verify>
    <automated>node web/scripts/check-no-force-layout.mjs --self-test &gt;/tmp/11-04-scan-self.txt 2&gt;&amp;1; RC=$?; cat /tmp/11-04-scan-self.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o "self-test: PASS — injected 'cose' detected at &lt;self-test&gt;/injected\.ts:1" /tmp/11-04-scan-self.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the self-test does not exit 0 with the exact detection line — the scan cannot see a planted layout name, so its clean verdict would be meaningless</fails_when>
    <automated>node web/scripts/check-no-force-layout.mjs &gt;/tmp/11-04-scan.txt 2&gt;&amp;1; RC=$?; cat /tmp/11-04-scan.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o 'verdict PASS' /tmp/11-04-scan.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'scanned [0-9]+ files' /tmp/11-04-scan.txt | rg -o '[0-9]+')" -ge 50 &amp;&amp; test "$(rg -o 'elk layout refs [1-9][0-9]*' /tmp/11-04-scan.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'elk import refs [1-9][0-9]*' /tmp/11-04-scan.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'forbidden matches 0' /tmp/11-04-scan.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the scan fails, scans fewer than 50 files (broken walk), finds no `elk` layout/import reference (positive control absent), or reports any forbidden match</fails_when>
    <automated>printf 'const layout = { name: %s };\n' "'cose'" &gt; web/src/lib/zz-planted-force-layout.ts &amp;&amp; node web/scripts/check-no-force-layout.mjs &gt;/tmp/11-04-scan-red.txt 2&gt;&amp;1; RC=$?; rm -f web/src/lib/zz-planted-force-layout.ts; cat /tmp/11-04-scan-red.txt; test "$RC" = "1" &amp;&amp; test "$(rg -o 'zz-planted-force-layout\.ts:1' /tmp/11-04-scan-red.txt | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'forbidden matches 1' /tmp/11-04-scan-red.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test -z "$(git status --porcelain -- web/src/lib/zz-planted-force-layout.ts)"</automated>
    <fails_when>a real planted `name: 'cose'` file under `web/src` does not turn the scan RED (exit 1, the file named with its line, exactly one forbidden match), or the planted file was not removed</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task -s check:no-force-layout &gt;/tmp/11-04-task-scan.txt 2&gt;&amp;1; RC=$?; tail -4 /tmp/11-04-task-scan.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o 'self-test: PASS' /tmp/11-04-task-scan.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'verdict PASS' /tmp/11-04-task-scan.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^  check:no-force-layout:$' Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -F -o "name\\s*:\\s*['\"](cose|cose-bilkent|fcose|cola|euler|spread|force)['\"]" web/scripts/check-no-force-layout.mjs | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'LAYOUT_NAME_RE = /\\bname' web/scripts/check-no-force-layout.mjs | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the Taskfile target is missing or does not run both the self-test and the scan green, or the layout-name regex is not the scoped one (an unscoped word regex would false-positive on `spread`/`force` identifiers and comments)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o "chromium|playwright" web/scripts/check-no-force-layout.mjs | wc -l` prints 0 (static scan, no browser)
    - `rg -o 'cytoscape-elk' web/package.json | wc -l` prints 1 and `rg -o 'cytoscape-(cose|fcose|cola|euler|spread)' web/package.json | wc -l` prints 0
    - The SUMMARY pastes the self-test line, the clean-scan line with its counts, and the planted-file RED transcript (the instrument 11-05 family (c) repeats with the cleanliness gate)
    - `git status --porcelain -- web/src` is empty after the planted-file rehearsal
  </acceptance_criteria>
  <done>A committed, browser-free scan proves — with a self-test that must detect a planted match and a real planted-file RED — that the only layout name reachable under `web/src` is `elk`, reporting how many files it inspected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire `communityId`/`communityCount` → transform → cytoscape stylesheet / toolbar text | server-computed integers rendered as a class index and a number; no string from the wire is interpolated into a selector |
| `web/src` → `check-no-force-layout.mjs` | source text scanned for a rejected layout family; the scan's own blindness is the risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-12 | Tampering (layout decision regression) | a force-directed layout re-entering via a layout name or a `cytoscape-*` extension | medium | mitigate | `check-no-force-layout.mjs` scoped regexes over `web/src` + `package.json` with `elk` positive control, `--self-test` injection, planted-file RED (Task 3 verify; 11-05 family (c)); `GraphCanvas.svelte` byte-identical across the phase (Task 1 verify) |
| T-11-13 | Tampering (selector injection) | a wire integer reaching a cytoscape selector string | low | mitigate | only `(id - 1) % 12` — a bounded integer 0..11 — is interpolated into a class name; the 12 selectors are static literals generated from the palette at module load, never from response data |
| T-11-14 | Repudiation (misleading grouping) | a directory compound coloured by one member's community | low | mitigate | D-11: `expandedDirElement`/`collapsedDirElement` gain no field/class (vitest Test 1e); no `[?isDirectory]` colour rule exists (Test 2) |
| T-11-06 | Spoofing | DNS rebinding against the existing FileGraph rpc | low | accept | inherited — no new rpc, no new fetch (`uiClient.` call count unchanged, Task 2 verify) |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | none — no dependency added (`web/package.json` `cytoscape-*` set unchanged, Task 3 acceptance) |
</threat_model>

<verification>
- `pnpm -C web test` — all green; `pnpm -C web check` — 0 errors
- `task web:build && task -s web:drift` — both halves MATCH, `web/build/**` committed
- `git diff --quiet <threshold commit> HEAD -- web/src/lib/components/graph/GraphCanvas.svelte`
- `node web/scripts/check-no-force-layout.mjs --self-test && node web/scripts/check-no-force-layout.mjs` — both PASS; `task check:no-force-layout` green
- No `codegraph ui` process left running; `corpora/breadcrumb-check.json` untouched
</verification>

<success_criteria>
- GRF-06 success criterion 2 verbatim: opening the graph view on a real repository shows nodes coloured by community on the existing layered layout with the layout algorithm unchanged — asserted by a check that reports the number of distinct communities rendered (the toolbar line + the colour==community vitest) and finds no force-directed mode reachable from any code path (the positive-controlled scan)
</success_criteria>

<output>
Create `.planning/phases/11-graph-view-community-clustering/11-04-SUMMARY.md` when done — include the vitest/svelte-check/web:drift tails, the scan's clean and RED transcripts, the palette validation note, and the commit SHAs.
</output>
