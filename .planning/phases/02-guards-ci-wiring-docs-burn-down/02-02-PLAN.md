---
phase: 02-guards-ci-wiring-docs-burn-down
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md
autonomous: true
requirements: [GRD-11]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`ci.yml`'s `test` job carries three new steps, each in house style (a `#`-prefixed rationale comment above a `- name:` block): `Install syft` (`uses: anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0`, the identical pin `release.yml` and `linux-cross-canary.yml` already use), `Gonum dependency-shape guard (GRD-11/GRF-10)` with `run: task check:gonum`, and `No-force-layout guard (GRD-11/GRF-06)` with `run: task check:no-force-layout` — one-line `task <target>` bodies only, no re-derived guard logic. D-04."
    - "All three steps sit AFTER `- name: Set up Node` and `Install syft` sits BEFORE the gonum step (both targets precondition on `node`; `check:gonum` preconditions on `syft`) — proven by line-number order, not by reading. D-04 (probe GRD-11/ordering, explicit)."
    - "Each new step name occurs exactly once in `ci.yml`, so `TestWorkflowRunBodiesInvokeTask`'s name-keyed matching and any future `runBodyExceptions` entry bind unambiguously. D-04 (probe GRD-11/adjacency, explicit)."
    - "`GOTOOLCHAIN=go1.26.6 go test ./internal/upgrade/ -count=1` passes after the edit — `TestWorkflowRunBodiesInvokeTask` accepts the two `run: task …` bodies and ignores the `uses:` step; `task lint:actions` (actionlint) passes. D-04."
    - "The wiring is proven able to fail: with a planted `name: 'cose'` literal under `web/src`, `task check:no-force-layout` — the exact command the new step runs — exits non-zero naming the planted file, and after `rm -f` of that file `git status --porcelain web/src` is empty (byte-clean). Recorded as Family (b) in `02-MUTATION-LOG.md`. D-04 / rule 84d1gfpywd."
    - "`task check:gonum` passes locally on the clean tree and its transcript shows its three built-in positive controls fired (the SBOM half's `positive control github.com/cockroachdb/pebble/v2 present`, the cgo half's `positive control` line, and `check:gonum: PASS`) — the target's own controls are the guard; no CI-only duplicate assertion is added. D-04 / Phase 7 D-07."
    - "The two guard steps run sequentially inside one job after `Install syft` (the download action is idempotent), a cancelled CI job is never a success conclusion, and the local planted-violation control runs in a wave where no other plan builds or reads `web/` with its cleanup asserted by an empty `git status --porcelain web/src` — so neither interruption nor parallel execution can read as PASS. D-04 (probe GRD-11/concurrency, explicit)."
    - statement: "Each wired target fails loudly on an empty subject set rather than passing vacuously — `check:gonum` asserts its cgo-closure package count and SBOM package count before comparing, and `check-no-force-layout.mjs`'s verdict requires `filesScanned > 0` — verified by reading each guard's own floor in the transcript, not re-implemented in the CI step."
      verification: backstop
  artifacts:
    - ".github/workflows/ci.yml — `test` job steps `Install syft`, `Gonum dependency-shape guard (GRD-11/GRF-10)`, `No-force-layout guard (GRD-11/GRF-06)`"
    - ".planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md — `## Family (b) — GRD-11` entry"
  key_links:
    - "pull request → ci.yml `test` job (already a required context in ruleset 20157557) → `Install syft` → `task check:gonum` (govulncheck source-mode half, syft SBOM half, cgo-closure half) → `task check:no-force-layout` (`--self-test` then real scan) — a failing target fails the required `test` check, so both block merges the moment they land with no ruleset edit and no fixture change"
  prohibitions:
    - statement: "No guard logic is re-derived in `ci.yml` — each new `run:` body is exactly one `task <target>` line (D-04; Phase 7 D-07: never a CI-side mirror of a script's own assertion). Gate: `TestWorkflowRunBodiesInvokeTask`."
      status: resolved
      verification: judgment
    - statement: "No `continue-on-error:`, `if:` condition, or `|| true` is attached to either new guard step; no `permissions:` line in `ci.yml` is widened."
      status: resolved
      verification: judgment
    - statement: "No change to `Taskfile.yml`'s `check:gonum` or `check:no-force-layout` targets and no change to `web/scripts/check-no-force-layout.mjs` — the targets are wired as they exist; the planted-violation control is a temporary untracked file removed before any commit."
      status: resolved
      verification: judgment
    - statement: "The planted violation is never committed and never co-scheduled with a `web/` build: this plan runs in wave 2, and no other wave-2 plan touches `web/`."
      status: resolved
      verification: judgment
    - statement: "No `[ci skip]` / `[skip ci]` in any commit message."
      status: resolved
      verification: judgment

---

<objective>
Run `check:gonum` and `check:no-force-layout` on every pull request by adding them as steps inside
`ci.yml`'s existing `test` job — next to the pnpm-audit / `web:drift` / `proto:drift` drift-guard
cluster — with the one net-new CI dependency (`syft`) installed via the SHA pin the release workflows
already trust, and prove the wiring can fail with a planted violation.

**What are we testing, and why?** We are testing that the two guards v0.13.0 Phase 11 left local-only
(GRF-10 supply-chain shape, GRF-06 no-force-layout) now gate merges — `test` is already a required
context, so no ruleset edit and no fixture change is needed (D-04). We are NOT re-testing what the
targets assert: their positive controls are the guard (Phase 7 D-07: "why are you proposing tests
for tests?"). The one thing that could still pass vacuously is the wiring itself, so the positive
control here is a planted forbidden-layout literal that makes the exact step command go RED.

Purpose: STATE.md's Phase-11 blocker records both targets as "NOT wired into ci.yml"; RESEARCH
Pitfall 2 found `syft` absent from the `test` job (it would hard-fail `check:gonum`'s precondition,
not skip); Pitfall 3 fixes the placement after `Set up Node`.

Output: three `test`-job steps in `ci.yml` and `02-MUTATION-LOG.md` Family (b).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-CONTEXT.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-PATTERNS.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire syft + both guards into the `test` job and run the same commands locally green</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 131-210 — `Set up Node` (line 131), the JS gate chain, `SPA build drift guard (BLD-03)`, `Proto codegen drift guard (BLD-04)`, `CLI reference drift guard (DOCS-05)` (lines 202-203) and the rationale-comment-above-every-step house style; the new steps go immediately after the DOCS-05 step, before `Test subprocess integration harness`.
    - .github/workflows/release.yml line 141 and .github/workflows/linux-cross-canary.yml line 128 — the exact `anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0` line to copy verbatim (RESEARCH Package Legitimacy Audit: reuse the pin, do not re-derive a SHA).
    - Taskfile.yml lines 1645-1795 (`check:gonum`: its `syft`/`node`/`awk`/`go` preconditions and the three halves' printed count lines) and lines 1796-1814 (`check:no-force-layout`: `node` precondition, `--self-test` then real scan).
    - internal/upgrade/taskfile_shape_test.go lines 152-211 (`inScopeJobs` includes `ci.yml`/`test`; `runBodyExceptions`; `taskCallLineRe` requires a bare `task <target>` body) and lines 1441+ (`TestWorkflowRunBodiesInvokeTask`) — so the two `run:` bodies are written in the one shape that test accepts and no exception entry is needed.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-PATTERNS.md § "new `test`-job steps" — the three-step YAML shape and the placement rule.
  </read_first>
  <action>
Edit `.github/workflows/ci.yml` `test` job only. Insert three steps IMMEDIATELY after the
`CLI reference drift guard (DOCS-05)` step — i.e. before `Ruleset drift check (GRD-12)`, which
plan 02-03 placed after DOCS-05 in wave 1, and before `Test subprocess integration harness
(test/integration)` — in house style, each with a `#` rationale comment above it that says why
the step exists and why it sits here:

  1. `- name: Install syft` / `uses: anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0`
     — comment: `check:gonum`'s SBOM half shells out to `syft` exactly as `.goreleaser.yaml`'s
     `sboms:` block does; GitHub-hosted runners do not ship it; same pin as `release.yml`.
  2. `- name: Gonum dependency-shape guard (GRD-11/GRF-10)` / `run: task check:gonum`
     — comment: GRF-10's three positive-controlled halves (govulncheck source mode over the main
     module, syft SBOM by name+version, cgo-closure walk) ran only locally since v0.13.0 Phase 11;
     `test` is a required context so this blocks merges with no ruleset edit (D-04). Needs `node`
     (after `Set up Node`) and `syft` (the step above).
  3. `- name: No-force-layout guard (GRD-11/GRF-06)` / `run: task check:no-force-layout`
     — comment: runs the script's `--self-test` first, then the real scan over `web/src`; needs
     only `node`, no `pnpm install`.

Do not add a soft-fail attribute, a step-level condition, an env block, or any permissions change.
Do not touch any other job. Do not modify `Taskfile.yml`.

Then prove the same commands the steps run are green on the clean tree, locally:
`GOTOOLCHAIN=go1.26.6 task check:gonum` (this machine has `syft`, `node`, `awk`; capture the
transcript — it must end in `check:gonum: PASS` and show the SBOM half's
`positive control github.com/cockroachdb/pebble/v2 present` line and the cgo half's
`positive control` line) and `task check:no-force-layout` (must print
`check-no-force-layout self-test: PASS` then the real-scan verdict line with `forbidden matches 0`).
Paste both transcripts' count/positive-control lines into the SUMMARY — these are the targets'
own positive controls, which D-04 names as the guard.

Run `GOTOOLCHAIN=go1.26.6 go test ./internal/upgrade/ -count=1` and
`GOTOOLCHAIN=go1.26.6 task lint:actions`; both must pass.

Commit: `ci(02-02): run check:gonum and check:no-force-layout in the test job on every PR (GRD-11)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && for n in 'Install syft' 'Gonum dependency-shape guard (GRD-11/GRF-10)' 'No-force-layout guard (GRD-11/GRF-06)'; do c=$(rg -c -F -- "- name: $n" .github/workflows/ci.yml); test "$c" = "1" || { echo "step '$n' count=$c"; exit 1; }; done; echo "names unique"</automated>
    <fails_when>non-zero exit or a printed `count=` other than 1 — a step is missing, or a step name is duplicated (which would make name-keyed matching in the shape tests ambiguous)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node_l=$(rg -n -F -- '- name: Set up Node' .github/workflows/ci.yml | head -1 | cut -d: -f1); syft_l=$(rg -n -F -- '- name: Install syft' .github/workflows/ci.yml | cut -d: -f1); gonum_l=$(rg -n -F -- 'run: task check:gonum' .github/workflows/ci.yml | cut -d: -f1); nfl_l=$(rg -n -F -- 'run: task check:no-force-layout' .github/workflows/ci.yml | cut -d: -f1); integ_l=$(rg -n -F -- '- name: Test subprocess integration harness' .github/workflows/ci.yml | cut -d: -f1); test -n "$node_l" -a -n "$syft_l" -a -n "$gonum_l" -a -n "$nfl_l" -a -n "$integ_l" && test "$node_l" -lt "$syft_l" -a "$syft_l" -lt "$gonum_l" -a "$gonum_l" -lt "$nfl_l" -a "$nfl_l" -lt "$integ_l" && echo "order ok $node_l<$syft_l<$gonum_l<$nfl_l<$integ_l"</automated>
    <fails_when>non-zero exit or no `order ok` line — one of the five anchors is absent, or the order is not Set up Node < Install syft < check:gonum < check:no-force-layout < integration harness (a guard placed before Node setup or before the syft install fails its own precondition in CI)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -c -F 'anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610' .github/workflows/ci.yml && ! rg -q 'continue-on-error' .github/workflows/ci.yml && test "$(rg -c '^permissions:' .github/workflows/ci.yml)" = "1" && rg -q '^  contents: read' .github/workflows/ci.yml</automated>
    <fails_when>non-zero exit or a printed pin count of 0 — the syft action is not pinned to the release workflows' SHA, a `continue-on-error` appeared in ci.yml, or the workflow-level `permissions:` block changed shape</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/upgrade/ -count=1 -run 'TestWorkflowRunBodiesInvokeTask|TestRequiredCheckNamesPreserved|TestWorkflowFilePopulationMatchesDisk' -v 2>&1 | tee /tmp/02-02-shape.txt | tail -5; rg -q '^ok ' /tmp/02-02-shape.txt && ! rg -q '^--- FAIL|^FAIL' /tmp/02-02-shape.txt && rg -c '^--- PASS' /tmp/02-02-shape.txt</automated>
    <fails_when>non-zero exit, `--- FAIL` present, no `ok` line, or a `--- PASS` count of 0 — a new `run:` body is not a bare `task <target>` call, or the workflow-shape guards are otherwise red</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 task lint:actions</automated>
    <fails_when>non-zero exit — actionlint rejects the edited workflow</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 task check:gonum > /tmp/02-02-gonum.txt 2>&1; s=$?; tail -3 /tmp/02-02-gonum.txt; test $s -eq 0 && rg -q 'check:gonum: PASS' /tmp/02-02-gonum.txt && rg -q 'positive control github.com/cockroachdb/pebble/v2 present' /tmp/02-02-gonum.txt && rg -q 'packages inspected' /tmp/02-02-gonum.txt</automated>
    <fails_when>non-zero exit or a missing `check:gonum: PASS` / `positive control … present` / `packages inspected` line — the exact command the new step runs is red on the clean tree, or one of the target's own positive controls did not print</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task check:no-force-layout > /tmp/02-02-nfl.txt 2>&1; s=$?; tail -3 /tmp/02-02-nfl.txt; test $s -eq 0 && rg -q 'self-test: PASS' /tmp/02-02-nfl.txt && rg -q 'scanned [1-9][0-9]* files' /tmp/02-02-nfl.txt && rg -q 'forbidden matches 0' /tmp/02-02-nfl.txt</automated>
    <fails_when>non-zero exit, no `self-test: PASS`, a zero/absent `scanned N files` count, or a non-zero `forbidden matches` — the exact command the new step runs is red or non-discriminating on the clean tree</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git show --format= --name-only HEAD > /tmp/02-02-t1-files.txt && test "$(sort /tmp/02-02-t1-files.txt | tr '\n' ' ')" = ".github/workflows/ci.yml " && git diff --quiet -- Taskfile.yml web/scripts/check-no-force-layout.mjs</automated>
    <fails_when>non-zero exit — the commit touched anything other than `ci.yml`, or `Taskfile.yml` / the scan script carry uncommitted edits (the targets are wired as they exist, never edited by this plan)</fails_when>
  </verify>
  <acceptance_criteria>
    - `ci.yml`'s `test` job contains exactly one each of `- name: Install syft`, `- name: Gonum dependency-shape guard (GRD-11/GRF-10)`, `- name: No-force-layout guard (GRD-11/GRF-06)`, each preceded by a `#` rationale comment.
    - Line order in `ci.yml`: `Set up Node` < `Install syft` < `run: task check:gonum` < `run: task check:no-force-layout` < `Test subprocess integration harness`.
    - The syft step uses `anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0`.
    - No `continue-on-error`, `if:`, or `permissions:` change anywhere in the diff.
    - `go test ./internal/upgrade/` shape guards and `task lint:actions` pass.
    - Local `task check:gonum` ends in `check:gonum: PASS` with its positive-control lines printed; local `task check:no-force-layout` prints `self-test: PASS`, a non-zero scanned count and `forbidden matches 0`.
    - The commit touches only `.github/workflows/ci.yml`.
  </acceptance_criteria>
  <done>Both guards run on every PR through the already-required `test` job, with syft installed before the one that needs it, and the identical commands pass locally on the clean tree.</done>
</task>

<task type="auto">
  <name>Task 2: Planted-violation positive control for the wiring, recorded as Family (b) and reverted byte-clean</name>
  <files>.planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md — Family (a)'s section shape written by 02-01; Family (b) is appended after it with the same subsections.
    - web/scripts/check-no-force-layout.mjs lines 62-68 (`SCAN_EXTENSIONS`, `FORBIDDEN_LAYOUT_NAMES` — `cose` is forbidden), lines 221-231 (verdict rule and the `scanned N files … forbidden matches N … verdict` line), and lines 270-306 (how the script's own self-test plants `const layout = { name: 'cose' };` — the same literal shape to plant on disk).
    - .planning/milestones/v0.13.0-phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md Family (b) — the mutate / observe / revert / post-revert-gate sequence for a tracked-tree mutation (here the mutation is an untracked file, so the pre-gate is `git status --porcelain web/src` empty).
  </read_first>
  <action>
Pre-mutation gate: assert `git status --porcelain web/src` is empty and record it.

Mutation: create an UNTRACKED file `web/src/lib/__planted-no-force-layout__.ts` whose content is
one line: `const layout = { name: 'cose' };` (the forbidden-name-at-`name:`-position shape the
scanner exists to catch). Do NOT `git add` it.

Observe: run `task check:no-force-layout` (the exact command the new CI step runs) and capture the
transcript and exit code. Expect the `--self-test` to PASS, then the real scan to exit non-zero
with a forbidden-match report that names `web/src/lib/__planted-no-force-layout__.ts` and `cose`.
If the scan passes with the planted file present, STOP — the wiring is vacuous — and report it
rather than reverting quietly.

Revert: `rm -f web/src/lib/__planted-no-force-layout__.ts`; post-revert gate:
`git status --porcelain web/src` empty; re-run `task check:no-force-layout` → exit 0 with
`forbidden matches 0`.

Append `## Family (b) — GRD-11: planted forbidden layout literal makes the wired
check:no-force-layout step fail` to `02-MUTATION-LOG.md` with: Test/guard (the `ci.yml` step
`No-force-layout guard (GRD-11/GRF-06)` → `task check:no-force-layout`), Pre-mutation gate,
Mutation applied (the untracked file and its one-line content), Observed failure (verbatim
transcript, exit code), Revert, Byte-clean proof (`git status --porcelain web/src` empty), and a
Green re-run. Add one paragraph for `check:gonum`: no planted mutation (it would require a
`go.mod`/dependency change), its three halves carry built-in positive controls that Task 1's
transcript shows firing — quote the two `positive control` lines and `check:gonum: PASS`.

Commit: `docs(02-02): record the planted no-force-layout RED for the GRD-11 CI wiring (Family b)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test -z "$(git status --porcelain web/src)" && printf "const layout = { name: 'cose' };\n" > web/src/lib/__planted-no-force-layout__.ts && (task check:no-force-layout > /tmp/02-02-planted.txt 2>&1; echo "exit=$?" >> /tmp/02-02-planted.txt); rm -f web/src/lib/__planted-no-force-layout__.ts; test -z "$(git status --porcelain web/src)" && rg -q 'self-test: PASS' /tmp/02-02-planted.txt && ! rg -q 'exit=0' /tmp/02-02-planted.txt && rg -q '__planted-no-force-layout__' /tmp/02-02-planted.txt</automated>
    <fails_when>non-zero exit — the tree was not clean before planting, the planted file survived cleanup, the self-test did not pass, the scan exited 0 with the planted violation present (vacuous wiring), or the failure output does not name the planted file</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -c '^## Family \(b\) — GRD-11' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md && rg -q '__planted-no-force-layout__' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md && rg -q -i 'byte-clean' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md && rg -q 'check:gonum: PASS' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md && rg -c '^## Family \(a\) — GRD-09' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md</automated>
    <fails_when>non-zero exit or a printed count of 0 — Family (b) is missing, does not name the planted file, lacks the byte-clean proof or the check:gonum positive-control quote, or Family (a) from 02-01 was clobbered</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log -2 --format=%s%n%b > /tmp/02-02-msgs.txt && test -s /tmp/02-02-msgs.txt && ! rg -q -i 'ci skip|skip ci' /tmp/02-02-msgs.txt && ! git ls-files --error-unmatch web/src/lib/__planted-no-force-layout__.ts >/dev/null 2>&1</automated>
    <fails_when>non-zero exit — a commit message carries a CI-skip marker, or the planted file was committed</fails_when>
  </verify>
  <acceptance_criteria>
    - With `web/src/lib/__planted-no-force-layout__.ts` present, `task check:no-force-layout` exits non-zero and its output names that path; with it removed, the same command exits 0.
    - `git status --porcelain web/src` is empty before planting and after removal; the planted path is never tracked.
    - `02-MUTATION-LOG.md` has both `## Family (a) — GRD-09` and `## Family (b) — GRD-11`, the latter with the verbatim RED transcript, revert, byte-clean proof, green re-run, and the quoted `check:gonum` positive-control lines.
    - No commit message contains a CI-skip marker.
  </acceptance_criteria>
  <done>The wired step's command has been watched fail on a planted violation and pass after a byte-clean revert, and the evidence is in the phase mutation log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party GitHub Action → `test` job runner | `anchore/sbom-action/download-syft` downloads a binary onto the runner; the job runs with `contents: read` only |
| pull-request head → `test` job | PR code is checked out and `task check:*` runs over it; both targets are read-only scans (no writes to `go.mod`, `ci.yml`, or `web/`) |
| planted local mutation → main working tree | an untracked file under `web/src` exists for one command's duration; nothing else may read `web/` while it exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | `anchore/sbom-action/download-syft` supply chain | high | mitigate | Pinned to the identical commit SHA `e22c3899…` already trusted by `release.yml` and `linux-cross-canary.yml` (RESEARCH audit: OK, reuse the pin); a verify gate greps the exact SHA in `ci.yml`. |
| T-02-02-02 | Elevation of Privilege | workflow `permissions:` | medium | mitigate | No change; verify asserts exactly one `permissions:` block with `contents: read`. |
| T-02-02-03 | Tampering | guard logic duplicated in CI (drift between two definitions) | medium | mitigate | `run:` bodies are bare `task <target>` calls; `TestWorkflowRunBodiesInvokeTask` is a verify gate, so any inline logic fails the shape test. |
| T-02-02-04 | Repudiation | "wired but never fires" | high | mitigate | Task 2 plants a forbidden literal and records the same step command going RED, then GREEN after a byte-clean revert (rule 84d1gfpywd); `check:gonum`'s built-in positive-control lines are quoted from a real run. |
| T-02-02-05 | Denial of Service | `check:gonum` precondition on a runner without `syft`/`node` | medium | mitigate | `Install syft` precedes the gonum step and both guards sit after `Set up Node`; verify asserts the line order. A precondition miss fails loudly, never skips. |
| T-02-02-06 | Denial of Service | `protect-main` ruleset | high | mitigate | Commit messages grepped for CI-skip markers. |
| T-02-02-07 | Tampering | planted file leaking into a build or a commit | medium | mitigate | Wave 2 has no other plan touching `web/`; verify asserts `git status --porcelain web/src` empty after removal and that the path is never tracked. |
| T-02-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install; the only new dependency is a SHA-pinned GitHub Action already vetted in this repo. |
</threat_model>

<verification>
- `ci.yml` `test` job: `Install syft` (pinned SHA), `Gonum dependency-shape guard (GRD-11/GRF-10)`, `No-force-layout guard (GRD-11/GRF-06)` in that order after `Set up Node`; names unique; no `continue-on-error`; permissions unchanged.
- Shape tests and actionlint pass; both target commands pass locally with their positive-control lines printed.
- Planted `name: 'cose'` literal makes `task check:no-force-layout` RED naming the file; removal is byte-clean; Family (b) recorded.
</verification>

<success_criteria>
A pull request that breaks `check:gonum` or `check:no-force-layout` now fails `ci.yml`'s required
`test` check, the wiring has been watched fail on a planted violation, and no guard logic was
duplicated outside its Taskfile target.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Path / name |
|---|---|---|
| `Install syft` | new CI step (`uses:`) in the `test` job | `.github/workflows/ci.yml` |
| `Gonum dependency-shape guard (GRD-11/GRF-10)` | new CI step (`run: task check:gonum`) | `.github/workflows/ci.yml` |
| `No-force-layout guard (GRD-11/GRF-06)` | new CI step (`run: task check:no-force-layout`) | `.github/workflows/ci.yml` |
| Family (b) entry | appended mutation-log section | `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md` |
</artifacts_produced>

<output>
Create `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-02-SUMMARY.md` when done.
</output>
