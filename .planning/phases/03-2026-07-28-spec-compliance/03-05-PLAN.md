---
phase: 03-2026-07-28-spec-compliance
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - internal/mcp/server.go
  - test/wireoracle/scenarios.go
  - test/wireoracle/COVERAGE-BASELINE.md
  - testdata/wireoracle/transcripts/
autonomous: false
requirements: [SPEC-01, SPEC-06, SPEC-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A client calling `server/discover` receives an `instructions` string carrying codegraph usage guidance, so an agent gets orientation without spending a tool call (SPEC-07)."
    - "A Legacy client completing a classic `initialize` also receives that same `instructions` string, because `ServerOptions.Instructions` reaches both results — which matters because zero of the eight roster agent clients speak `2026-07-28` today, so discover-only guidance would reach no real user."
    - "A client speaking `2024-11-05` — the oldest era the server recognizes, maximal distance from Modern — completes a session, lists tools, and successfully calls a tool, asserted by a frozen transcript rather than assumed from the SDK's documentation (SPEC-06)."
    - "Every frozen transcript whose bytes moved in this phase has a named cause recorded in the re-freeze commit message, and every changed line is accounted for by one of those causes."
    - "The wire oracle is demonstrated non-vacuous against the Phase-3-complete binary: Phase 1's Mutation 1 turns TestFrozenTranscriptsMatch red across the corpus, and the binary is byte-identical after revert."
    - "ExpectedScenarioCount is 27 and unchanged by this plan — no scenario is added or removed here."
  artifacts:
    - internal/mcp/server.go
    - test/wireoracle/scenarios.go
    - testdata/wireoracle/transcripts/
    - .planning/phases/03-2026-07-28-spec-compliance/03-05-SUMMARY.md
  key_links:
    - "ServerOptions.Instructions -> go-sdk's initialize handler AND its discover handler -> the `instructions` field in both results"
    - "legacy-2024-11-05's new tools/call step -> its frozen transcript -> SPEC-06's oldest-era tool-call evidence"
    - "capture-before / capture-after -> the human diff read -> the named causes in the re-freeze commit message (D-06's entire mechanism)"
    - statement: "The instructions text ships on every initialize and every discover, so its content is a wire contract: it must contain no host path, no repository content, and no value interpolated at runtime."
      verification: backstop
  prohibitions:
    - "No code may be written to suppress `instructions` from the initialize result. Setting the field once and letting both results carry it is the cheap mechanism; writing a middleware branch to strip it from Legacy clients would spend code to hide a client-visible improvement from every agent that actually exists today."
    - "The instructions string must be a compile-time constant with no interpolation — no repository path, no resolved index root, no hostname, no environment value."
    - "No transcript may be regenerated wholesale to make the suite pass. Bytes move only through the reviewed pass with a named cause (ROADMAP Phase 2 criterion 2, inherited)."
    - "`legacy-unsupported-2026-07-28` must NOT be renamed in this phase — see the recorded non-goals below."
    - "The `annotations` hint values must NOT be corrected in this phase — see the recorded non-goals below."
    - "No protocol-version branch may be added to any handler or middleware (D-02)."
    - "go-sdk issue #976's `code: 0` on the pre-initialize rejection is tracked upstream, not worked around, not anchored."
---

<objective>
Land the phase's last two wire-visible changes together, then move every frozen
byte they touch through exactly one reviewed diff pass.

Purpose: SPEC-07 is a one-field change plus a writing task (D-04: the mechanism is
`ServerOptions.Instructions`, which the SDK copies into the result with no extra
wiring, so the whole deliverable is the *content*), and SPEC-06 is one missing
`tools/call` step on the oldest era (D-07: asserted, not built). Both are grouped
here for a reason
that is structural, not stylistic: `ServerOptions.Instructions` is the only change
in this phase that moves the 26 pre-existing transcripts, and D-06 mandates exactly
one reviewed re-freeze pass. Landing SPEC-07 and SPEC-06 in separate plans would
force two.

The reviewed pass is the phase's actual acceptance mechanism — D-06 defines it in
one sentence (run the oracle before and after, read the diff, name every changed
line's cause in the commit message; no ledger file, no sign-off step), and Phase 2
proved it earns its keep by catching a ninth semantic divergence no research
document had predicted.

Output: the instructions string, one new `tools/call` step, the re-frozen corpus,
a commit message enumerating every cause, and a re-proof that the oracle can still
fail.

## Recorded non-goals (reconsidered here, declined with reasons)

03-CONTEXT.md `<deferred>` invited two items to be reconsidered "then" — meaning
now, in the phase that is already touching scenarios and transcripts. Both were
reconsidered and both are declined, recorded so the omissions read as decisions:

- **Renaming `legacy-unsupported-2026-07-28`.** Declined. It is cosmetic, and a
  rename lands in the reviewed diff as an `R` record plus a `.golden` filename
  change — noise inside the one pass that has to distinguish real semantic movement
  from mechanical movement across 26 files. Phase 2's cause list already records
  what that scenario now measures.
- **Correcting the `annotations` hints** (`destructiveHint: true` on read-only query
  tools). Declined. It is a semantic change to agent-visible tool metadata with no
  requirement backing it in this milestone, and folding it into this pass would mix
  an unrequired semantic change into the evidence for eight requirements. It remains
  a legitimate, cheap change for a phase whose diff it would not contaminate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-2026-07-28-spec-compliance/03-CONTEXT.md
@.planning/phases/03-2026-07-28-spec-compliance/03-RESEARCH.md
@.planning/phases/03-2026-07-28-spec-compliance/03-01-SUMMARY.md
@.planning/phases/03-2026-07-28-spec-compliance/03-04-SUMMARY.md
@.planning/phases/02-sdk-migration-official-go-sdk-on-the-existing-surface/02-05-SUMMARY.md
@internal/mcp/server.go
@test/wireoracle/scenarios.go
@test/wireoracle/MUTATION-PROOF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The instructions string, and the oldest era's tool call</name>
  <files>internal/mcp/server.go, test/wireoracle/scenarios.go</files>
  <read_first>
    - internal/mcp/server.go — the `mcp.NewServer` call and its `&mcp.ServerOptions{...}` literal, including D-11's doc comment explaining why `Capabilities` is set explicitly and unconditionally; the new field goes beside it
    - test/wireoracle/scenarios.go — the six-era Legacy block, specifically the `legacy-2024-11-05` scenario, and the at-most-one-`tools/call`-and-it-is-last concurrency constraint documented above `Scenarios()`
    - test/wireoracle/oracle_test.go § `TestLegacyEraBaselineIsDocumented` (it counts `legacy-`-prefixed scenarios and asserts offered == negotiated from the frozen bytes) and § `TestEveryRegisteredToolHasASuccessfulCallScenario`
    - .planning/phases/03-2026-07-28-spec-compliance/03-CONTEXT.md § "Claude's Discretion" — the instructions-text leaning this task implements, and § D-07 — SPEC-06 is asserted, not built
    - .planning/phases/03-2026-07-28-spec-compliance/03-RESEARCH.md § "SPEC-06 recommendation" — the LOW-confidence judgment call this task takes, and why
  </read_first>
  <action>
    Add `Instructions` to the existing `&mcp.ServerOptions{...}` literal in
    `BuildServer`, set from a new package-level string constant in
    `internal/mcp/server.go`. Author that constant's value to satisfy four content
    requirements and three form requirements.

    Content — the text must convey: (1) what codegraph is and what it indexes, framed
    so an agent knows when to reach for it, and that `codegraph_explore` is the tool
    to try first for a "where is X" or "how does Y work" question because it returns
    verbatim source plus call paths in one call; (2) that every tool takes an optional
    `path` argument and that omitting it means the server's own working directory —
    the repository it was started in; (3) that an empty tool list means this
    repository has no index yet and that running `codegraph init` is the fix, NOT
    that the server is broken — this is the milestone's named highest-severity
    ambiguity, and one sentence of guidance is the cheapest place it has ever been
    addressable; (4) that tools appear as soon as an index exists, without restarting
    the client — which is now true, because 03-04 made it true, and saying so turns
    SPEC-05 from a behavior into a discoverable one.

    Form: a single paragraph with no newline characters (the value is JSON-encoded
    into 27 frozen transcripts; embedded newlines become escape sequences that make
    every one of those diffs harder to read); at most roughly 600 characters; and a
    compile-time constant with no interpolation of any kind — no repository path, no
    resolved index root, no hostname, no environment value. It ships to every client
    on every session.

    Give the constant a doc comment recording: that this closes SPEC-07; that
    `ServerOptions.Instructions` reaches both the `initialize` result and the
    `server/discover` result through the identical field, which is deliberate rather
    than incidental, because zero of the eight roster agent clients speak
    `2026-07-28` today and a discover-only string would reach no real user; and that
    the value is a wire contract shipped on every session, so it must never carry
    interpolated or host-specific content.

    Separately, add a third request to the existing `legacy-2024-11-05` scenario in
    `test/wireoracle/scenarios.go`: `toolCallRequest(3, "codegraph_explore",
    map[string]any{"query": handshakeExploreQuery})`, placed last, satisfying the
    file's ordering constraint. Do not change the scenario's name, its
    `EraOfferedVersion`/`EraNegotiatedVersion`, its `ExpectTools`, or any other era
    scenario. Extend its doc comment to record what the added step buys: SPEC-06
    demands an assertion that a Legacy client completes a session AND calls a tool;
    `handshake-explore` already proves that at `2025-11-25`, the newest Legacy era,
    and this adds the same proof at `2024-11-05`, the oldest — maximal distance from
    Modern. Note the honest confidence level: go-sdk's `callTool`/`toolForErr` never
    consult the request's protocol version, so one success at each end of the Legacy
    range is judged sufficient evidence for the three eras between; that is a
    judgment call 03-RESEARCH.md flagged LOW confidence, not a proof, and recording
    it as such is the point.

    `ExpectedScenarioCount` does not change — no scenario is added or removed here.

    Expect the wire-oracle suite to be RED after this task: every transcript with an
    `initialize` or `discover` response now lacks a field the server emits. That is
    correct and expected; Task 2 reviews it and Task 3 re-freezes it. Do not
    regenerate anything in this task.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/mcp/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'Instructions:' internal/mcp/server.go` is 1, inside the `&mcp.ServerOptions{...}` literal.
    - The instructions constant contains zero newline characters and its length is at most 600 bytes, both confirmed by a one-off `go run`/`gofmt`-independent check quoted in the SUMMARY.
    - `rg -n 'const .*[Ii]nstructions' -A 3 internal/mcp/server.go` shows a plain string literal — no `fmt.Sprintf`, no `+` concatenation with a variable, no reference to `repoPath`, `startPath`, `os.Getenv`, or any runtime value.
    - `rg -c 'codegraph init' internal/mcp/server.go` is at least 1 and `rg -c 'codegraph_explore' internal/mcp/server.go` is at least 1 — content requirements (1) and (3) are present in the shipped string.
    - `go test ./internal/mcp/... -count=1` exits 0 — the in-process tests are unaffected by the new field.
    - `rg -n 'legacy-2024-11-05' -A 12 test/wireoracle/scenarios.go` shows exactly three requests, the third being a `tools/call`, and no change to `EraOfferedVersion`, `EraNegotiatedVersion` or `ExpectTools`.
    - `rg -c 'ExpectedScenarioCount = 27' test/wireoracle/scenarios.go` is 1.
    - `go test ./test/wireoracle/... -count=1 -run TestScenarioCountIsExact` exits 0.
  </acceptance_criteria>
  <done>`ServerOptions.Instructions` is set from a newline-free, interpolation-free constant covering all four content points; `legacy-2024-11-05` carries a trailing `tools/call`; the scenario count is unchanged; and the wire-oracle suite is expectedly red pending Task 2's review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The diff review — this phase's actual acceptance mechanism (D-06)</name>
  <what-built>
    Everything in Phase 3 that changes bytes on the wire is now in the tree:
    `cacheScope: "private"` on `server/discover` (03-01), four new scenarios and
    their frozen transcripts (03-01, 03-03, 03-04), the live tool catalog (03-04),
    the `instructions` string, and `legacy-2024-11-05`'s new `tools/call` (Task 1).
    Nothing has been re-frozen yet.

    Before this checkpoint, capture every one of the 27 scenarios against a freshly
    rebuilt binary using the oracle's own capture CLI, into a scratch directory —
    never over the frozen files — and produce a full diff against
    `testdata/wireoracle/transcripts/`. Present that diff.
  </what-built>
  <how-to-verify>
    Read the diff line by line and confirm every changed line is accounted for by a
    named cause. The predicted causes, to be confirmed or corrected rather than
    assumed:

    1. `"instructions":"..."` appears in every `initialize` result — expected in 22
       of the pre-existing transcripts (all but `edge-call-before-initialize`, which
       has no `initialize` response, and the three `modern-*` scenarios from 03-01
       and 03-03, which have no `initialize` either). Cosmetic-additive but
       client-visible.
    2. `"instructions":"..."` appears in the `server/discover` result of
       `modern-discover-explore` — 1 transcript, the SPEC-07 deliverable itself.
    3. `legacy-2024-11-05` gains an id-3 `tools/call` response — 1 transcript,
       additive, the SPEC-06 assertion.
    4. Anything else. A fourth cause is the outcome this checkpoint exists to catch:
       Phase 2's ninth cause was discovered exactly here, by exactly this reading,
       after eight had been predicted.

    Specific things to check rather than skim:
    - Does the `instructions` value appear byte-identically in every transcript that
      carries it? A per-transcript variation would mean it is being interpolated
      somewhere.
    - Does it contain any absolute path, temp directory, or hostname?
    - Did any transcript's `protocolVersion` move? None should.
    - Did `toolslist-no-index` keep `"tools":{"listChanged":true}` at zero registered
      tools? D-11's regression fix must still hold under the new dynamic-registration
      code path.
    - Did any `tools/list` transcript's tool array change membership? None should —
      03-04's re-check only acts on a state flip, and every pre-existing scenario has
      a steady index state.
    - Did any `cacheScope` become `"public"` anywhere?
    - Did any error code change? Both spec anchors and the two new ones must hold.

    Approve only if every changed line has a cause you can name. If a line has no
    explanation, that is a finding, not a formatting question — say so and stop.
  </how-to-verify>
  <acceptance_criteria>
    - The full before/after diff across all 27 scenarios was produced from a fresh capture into a scratch location, and the frozen files were not modified before this checkpoint.
    - Every changed line is attributed to a named cause, and the cause list — with per-cause transcript counts — is recorded in the SUMMARY.
    - The five must-not-have-changed properties (protocolVersion, `toolslist-no-index`'s capabilities object, tool-array membership, every `cacheScope`, every error code) are each confirmed with their observed values, not merely asserted unchanged.
    - Any unpredicted cause is escalated with its observed before/after bytes rather than silently attributed.
  </acceptance_criteria>
  <resume-signal>Type "approved" to re-freeze, or describe the unexplained lines</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Re-freeze through the reviewed pass, and re-prove the oracle can fail</name>
  <files>testdata/wireoracle/transcripts/</files>
  <read_first>
    - test/wireoracle/MUTATION-PROOF.md — Phase 1's four confirmed mutations, especially Mutation 1 (the stray stdout line) and its exact application point and expected red signature
    - .planning/phases/02-sdk-migration-official-go-sdk-on-the-existing-surface/02-05-SUMMARY.md — how the same re-freeze plus mutation-proof ran in Phase 2, including the `-count=1` requirement after a source edit (a `task test:wireoracle` invocation returned a stale cached `ok` immediately after a server edit)
    - The cause list approved at Task 2's checkpoint
  </read_first>
  <action>
    Re-freeze every transcript whose bytes moved, using the oracle's own capture CLI
    against a freshly rebuilt binary. No hand edits to any `.golden` file. Do not
    touch a transcript whose bytes did not move.

    Commit the re-freeze with a message that enumerates every named cause from Task
    2's approved list, with per-cause transcript counts, and states each
    must-not-have-changed property with its observed value. This commit message is
    D-06's entire mechanism — there is no ledger file, no expected-diff file, and no
    sign-off record, deliberately.

    Then re-prove the oracle's non-vacuity on the Phase-3-complete binary. Re-apply
    Phase 1's Mutation 1 (a stray non-JSON line written to stdout mid-session in
    `internal/mcp`), rebuild, and run `go test ./test/wireoracle/... -count=1` —
    always with `-count=1`, never relying on the cache, per Phase 2's recorded
    stale-cache anomaly. Confirm `TestFrozenTranscriptsMatch` goes red across the
    corpus and that `TestSpecAnchorsHold`'s framing invariant also fails. Revert,
    rebuild, confirm green, and confirm `git diff -- internal/mcp/` is empty so the
    revert is byte-clean rather than merely compiling.

    Record in the SUMMARY: the observed red signature, the confirmed-clean revert,
    and the fact that Phase 3's four new anchors (03-01's discover cache control,
    03-03's `-32602` and `-32022`) were each independently demonstrated red in their
    own plans, so the phase's non-vacuity evidence covers both the inherited corpus
    and every assertion this phase added.

    Finally, update `test/wireoracle/COVERAGE-BASELINE.md` to describe the 27-scenario
    set rather than the 23-scenario set, so the corpus documentation does not silently
    fall out of date with the constant that guards it.
  </action>
  <verify>
    <automated>go test ./test/wireoracle/... -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./test/wireoracle/... -count=1` exits 0 with all 27 scenarios passing.
    - `git diff --name-status HEAD~1 -- testdata/wireoracle/transcripts/` lists exactly the transcripts named in Task 2's approved cause list and no others.
    - The re-freeze commit message contains one line per named cause with a transcript count, plus the five must-not-have-changed properties with observed values. Quoted in the SUMMARY.
    - `rg -o '"instructions":"[^"]*"' testdata/wireoracle/transcripts/*.golden | rg -o '"instructions":"[^"]*"' | sort -u | wc -l` is 1 — the string is byte-identical everywhere it appears, proving it is not interpolated.
    - `rg -c '/Users/|/home/|/tmp/|/var/folders' testdata/wireoracle/transcripts/*.golden | rg -v ':0$'` returns nothing — no host path entered the corpus.
    - `rg -c '"cacheScope":"public"' testdata/wireoracle/transcripts/*.golden | rg -v ':0$'` returns nothing.
    - Mutation 1 was applied, `TestFrozenTranscriptsMatch` and `TestSpecAnchorsHold` were both observed RED with `-count=1`, the mutation was reverted, and `git diff -- internal/mcp/` is empty afterward. All observations quoted in the SUMMARY.
    - `rg -c '23' test/wireoracle/COVERAGE-BASELINE.md` no longer refers to the scenario-set size, and the document describes 27 scenarios.
    - `go test ./... -count=1` passes, with any `internal/daemon` failure identified as the documented pre-existing flake (issue #17 / MAINT-02, Phase 4) and confirmed to pass in isolation rather than investigated here.
  </acceptance_criteria>
  <done>Every moved transcript is re-frozen from a real capture with every changed line's cause named in one commit message, the oracle is re-proved able to fail against the Phase-3 binary with a byte-clean revert, and COVERAGE-BASELINE.md describes the corpus that now exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ServerOptions.Instructions` → every `initialize` and `server/discover` result | Server-authored text crossing to every client on every session, and into 23 committed repository artifacts. Anything interpolated here is published twice over. |
| Captured subprocess output → committed `.golden` files | The re-freeze writes machine-produced bytes into the repository. Normalization is the only thing standing between a capturing host's paths and a commit. |
| The reviewed diff read → the decision to re-freeze | Under 03-02's outcome this is the phase's primary anti-regeneration control, not its secondary one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-19 | Information Disclosure | The `instructions` string | medium | mitigate | A natural implementation would interpolate the resolved repository path into the guidance ("this server indexes /Users/…"), publishing the capturing host's filesystem layout to every client and into 23 committed transcripts. Mitigated three ways: the constant is required to be a compile-time literal with no interpolation, an acceptance criterion greps its declaration for `Sprintf`/concatenation/`repoPath`/`os.Getenv`, and Task 3 asserts the string is byte-identical across every transcript that carries it — a per-transcript variation is exactly what interpolation would produce. |
| T-03-20 | Information Disclosure | Host paths in re-frozen transcripts | medium | mitigate | 23 files are rewritten from live captures in one operation, the largest such write in the phase. Mitigated by the existing `NormalizeWithLedger` substitution (independently proven still firing by `TestEveryDeclaredFiringRuleActuallyFires`), by Task 2's explicit path check during the human read, and by Task 3's tree-wide grep for `/Users/`, `/home/`, `/tmp/` and `/var/folders` across every golden. |
| T-03-21 | Tampering | Wholesale regeneration laundering a regression | high | mitigate | This is PITFALLS Trap C, and after 03-02's decision the automated blocking check may no longer be the control. Mitigated by making the human diff read a blocking checkpoint that precedes any write to a frozen file; by requiring per-cause transcript counts and five named must-not-have-changed properties with observed values rather than assertions; by re-proving the oracle red against Mutation 1 on the post-change binary; and by an acceptance criterion that the set of files actually re-frozen equals the set the approved cause list names. |
| T-03-22 | Spoofing | Instructions text asserting behavior the server does not have | low | mitigate | Content point (4) tells agents that tools appear without a client restart. That is a claim about SPEC-05, and it is only true because 03-04 made it true — which is why this plan depends on 03-04 and why 03-04's wire evidence (`index-appears-mid-session`) exists. Writing the claim before the behavior would make the shipped guidance a lie to every client. |
| T-03-23 | Repudiation | An unpredicted cause silently attributed | high | mitigate | Phase 2's ninth cause — a full success-to-rejection semantic flip — was found only by this reading, after eight causes had been predicted by research. Task 2's `<how-to-verify>` names "anything else" as cause 4 explicitly and instructs escalation with observed bytes rather than attribution, and its acceptance criteria require escalation rather than absorption. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs and `go.mod` is unchanged. Recorded so the absence of a package-legitimacy checkpoint is visible rather than silent. |
</threat_model>

<verification>
- `go build ./... && go vet ./...` exit 0.
- `go test ./internal/mcp/... -count=1 -race` passes.
- `go test ./test/wireoracle/... -count=1` passes with 27 scenarios.
- `go test ./... -count=1` passes, modulo the documented pre-existing `internal/daemon` flake (issue #17 / MAINT-02), which is confirmed to pass in isolation and is not investigated here.
- Every re-frozen transcript's changed lines are covered by a named cause in the re-freeze commit message.
- Mutation 1 turned the oracle RED against the Phase-3 binary and the revert was byte-clean.

</verification>

<success_criteria>
- SPEC-07 ships as guidance every client actually receives, on both the Modern and the Legacy path.
- SPEC-06 is asserted at both ends of the Legacy range — `2025-11-25` by the pre-existing `handshake-explore` and `2024-11-05` by the new step — with the sufficiency judgment recorded at its real confidence level rather than overstated.
- The corpus moved exactly once, through exactly one reviewed pass, with every changed line's cause named in one commit message and no ledger file.
- The oracle is proven still able to fail against the binary this phase produced.
- Both invited-to-reconsider deferred items are decided and recorded, not silently omitted.
</success_criteria>

## Artifacts this phase produces

**New symbols (`internal/mcp`, package `mcp`)**

| Symbol | Kind | Notes |
|---|---|---|
| instructions string constant | package-level unexported const, `string` | a compile-time literal with no interpolation of any kind — no repository path, no resolved index root, no hostname, no environment value. Single paragraph, no newline characters, at most roughly 600 bytes, because it is JSON-encoded into 23 frozen transcripts. Covers all four content points: what codegraph indexes and that `codegraph_explore` is the first tool to try; that `path` defaults to the server's working directory; that an empty tool list means "run `codegraph init`", not "the server is broken"; and that tools appear without a client restart |

**New non-symbol artifacts**

| Artifact | Kind | Location |
|---|---|---|
| `Instructions:` entry | field in `BuildServer`'s `&mcp.ServerOptions{…}` literal, beside D-11's `Capabilities` | `internal/mcp/server.go` |
| `legacy-2024-11-05`'s id-3 `tools/call` | `Scenario` request appended last (satisfying the at-most-one-and-it-is-last ordering constraint) | `test/wireoracle/scenarios.go` |
| re-frozen transcripts | the exact set named in Task 2's approved cause list, and no others | `testdata/wireoracle/transcripts/` |
| the named-cause record | the re-freeze commit message — D-06's entire mechanism; no ledger file, no expected-diff file, no sign-off record | the re-freeze commit |
| 27-scenario corpus description | doc rewrite (currently describes 23) | `test/wireoracle/COVERAGE-BASELINE.md` |

**No new test symbol is added for SPEC-06**, deliberately. The oldest-era tool call
is asserted by the frozen `legacy-2024-11-05.golden` bytes plus two pre-existing
structural tests that already cover it without modification —
`TestEveryRegisteredToolHasASuccessfulCallScenario` (which decodes
`result.isError` from the real capture, so a call that silently degrades to an
error result cannot pass as coverage) and `TestLegacyEraBaselineIsDocumented`
(which still counts six `legacy-`-prefixed scenarios and still reads
offered == negotiated from the frozen file). Adding a bespoke test would duplicate
assertions that already exist and already fire. Recorded here so the absence reads
as a decision rather than an oversight.

**Changed behavior** — `instructions` now appears in every `initialize` result AND
every `server/discover` result, because `ServerOptions.Instructions` reaches both
through the identical field. That is deliberate, not incidental: zero of the eight
roster agent clients speak `2026-07-28` today, so a discover-only string would
reach no real user.

**Unchanged and load-bearing** — `ExpectedScenarioCount` stays 27 (no scenario is
added or removed here); `legacy-2024-11-05`'s name, `EraOfferedVersion`,
`EraNegotiatedVersion` and `ExpectTools`; every other era scenario;
`legacy-unsupported-2026-07-28`'s name, which is explicitly NOT renamed (see the
recorded non-goals in `<objective>`); the `annotations` hint values, explicitly NOT
corrected (same); `ServerOptions.Capabilities` (D-11); `confineToRepoRoot`; every
anchor added in 03-01 and 03-03; the wire-oracle harness code, which this plan does
not touch; and go-sdk issue #976's `code: 0` on the pre-initialize rejection, which
stays tracked upstream, unworked-around and unanchored.

<output>
Create `.planning/phases/03-2026-07-28-spec-compliance/03-05-SUMMARY.md` when done.
</output>
