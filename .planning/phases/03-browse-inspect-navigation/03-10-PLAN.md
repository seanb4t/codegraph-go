---
phase: 03-browse-inspect-navigation
plan: 10
type: execute
wave: 8
depends_on: ["03-02", "03-05", "03-09"]
# WAVE MOVED 7 -> 8 IN REVISION, following the 03-05/03-06 serialization of the
# generated-client directory. Dependency edges unchanged.
files_modified:
  - .golangci.yml
  - go.tool-golangci.mod
  - go.tool-golangci.sum
  - Taskfile.yml
  - .github/workflows/ci.yml
  - internal/upgrade/taskfile_shape_test.go
  - internal/query/files_status_test.go
  - .planning/todos/pending/2026-08-10-add-golangci-lint-with-gofmt-and-idiomatic-go-linters.md
autonomous: true
requirements: [TODO-CI-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  # DERIVED from `gsd query estimate-calibration`: sample_count 0, factor 1.0,
  # min_samples 3. `low` means "unsampled"; see 03-01's estimate note (L2).
  confidence: low

must_haves:
  truths:
    - "The CI-wiring verification asserts the two things that DEFINE the wiring — a `task lint:go` run body in `ci.yml` and a `lint-go` entry in the `inScopeJobs` fixture — inside the `<verify>` block itself. `TestWorkflowRunBodiesInvokeTask` iterates only `inScopeJobs`, so an unregistered job is not merely unguarded: the test passes BECAUSE it is absent. Both counts were confirmed 0 on the working tree, so the block is red in an unimplemented world."
    - "The linter is pinned in its OWN modfile, `go.tool-golangci.mod`, created from the outset rather than after a co-location attempt fails — `go.tool-lint.mod`'s own header records a live-verified MVS collision from exactly that pattern."
    - "`go.tool-golangci.mod` is REGISTERED with `TestToolModfilesRemainIsolated`, and that registration is asserted by Task 1's `<verify>` with a positive count rather than inferred from the guard's exit status. The guard iterates a hardcoded two-element slice (`internal/upgrade/taskfile_shape_test.go:928`), so a new modfile absent from it is not merely unguarded: the test passes BECAUSE it is absent. This is the SAME shape Task 3 diagnoses for `inScopeJobs`, one task earlier. Confirmed on the working tree: `rg -o 'golangciModfilePath' internal/upgrade/taskfile_shape_test.go` returns 0 against a control of 10 for `go\\.tool-lint\\.mod|lintModfilePath` — the search works, the thing is simply absent."
    - "The new tool binary is IN SCOPE for the `vuln` gate. `Taskfile.yml`'s `vuln` target builds a hardcoded set (three from `go.tool.mod`, one from `go.tool-lint.mod`) and its own `desc:` claims all of them stay in scope with no allowlist and no exclusion. Adding a tool modfile the gate does not enumerate would silently falsify that claim in the same commit that grows the tool surface — so the fifth build line, its loop entry, and the corrected `desc:` land together, and the new binary's scan verdict is recorded."
    - "The RED demonstration plants its violations in a file golangci-lint actually inspects, and that file is restored byte-identically afterwards, proven by a recorded hash on both sides."
    - "A `.golangci.yml` exists at the repository root and enables a formatting check plus an idiomatic-Go linter set."
    - "`task lint:go` exists, runs the pinned linter over the module, and exits non-zero on a finding."
    - "`task lint` runs `lint:go` as one of its legs."
    - "A CI job invokes `task lint:go`, and that job is bound by the single-definition guard so its run body cannot drift away from being a task invocation."
    - "The gate has been demonstrated RED against a planted formatting violation and a planted idiomatic violation, and green after both are reverted."
    - "The formatting backlog surfaced on first run is FIXED, not suppressed — in particular `internal/query/files_status_test.go`, which has carried an unformatted block since it was introduced."
    - "The linter is pinned in the linters tool modfile, not the build/release one, and `task lint:actions` still works afterwards — proving the added module did not lose an MVS version bid the way a previous co-location attempt did."
    - "The todo is moved to the completed directory with a phase marker. Two prior folds of this item produced nothing, so folding is demonstrably not what has been missing."
    - statement: "The enabled linter set is one the maintainer agrees is worth the ongoing friction, rather than the widest set that happened to run clean."
      verification: backstop
  artifacts:
    - .golangci.yml
  key_links:
    - "go.tool-golangci.mod tool directive -> GO_TOOL_GOLANGCI -> task lint:go -> ci.yml lint-go job -> merge gate"
    - "go.tool-golangci.mod -> `golangciModfilePath` -> the modfile set `TestToolModfilesRemainIsolated` iterates. Without the const AND the slice entry the guard has nothing to read and PASSES because the file is absent — which is why the const is asserted by a positive count in Task 1's `<verify>`, not inferred from the guard going green"
    - "go.tool-golangci.mod -> `vuln`'s fifth `go build -modfile=` line -> its `for name in …` loop entry -> `govulncheck -mode=binary` verdict recorded in the SUMMARY. The gate enumerates its binaries by hand, so an unenumerated modfile is scanned by nothing while the target's `desc:` still claims full coverage"
    - "internal/upgrade/taskfile_shape_test.go inScopeJobs `{Workflow: \"ci.yml\", JobID: \"lint-go\"}` -> the new job's run body is held to `task <target>`. Without this entry the guard has nothing to bind and PASSES because the job is absent — which is why the entry itself is asserted in Task 3's `<verify>`, not inferred from the guard's exit status"
  prohibitions:
    - statement: "Findings must NOT be silenced with blanket inline suppressions to get the gate green. The precedent this repository already recorded for `go vet` is that findings surfaced on first run are fixed, not suppressed, and a gate whose backlog was suppressed rather than cleared is a gate that guards nothing."
      status: unverified
      verification: flagged
    - statement: "The gate must NOT be written as a command whose output is captured and then discarded, or piped into another command. A pipeline returns the last command's status, and a formatting lister that exits 0 whether or not it listed files is the exact vacuous shape this repository has already written by accident once."
      status: unverified
      verification: flagged
---

<objective>
Land the golangci-lint gate that has been folded into two prior phases and implemented in
neither.

This repository has no formatting gate anywhere. `task lint` is vet plus actionlint; no
`.golangci.yml` exists; no workflow references a formatting check. That is not
hypothetical — it produced three separate findings, including a test file that is
unformatted right now and has been since the commit that introduced it, and an
orchestrator that wrote a formatting check which printed a filename AND the word "clean"
in the same breath because the lister exits 0 either way.

This is the THIRD fold. It went into Phase 1 and was not implemented; into Phase 2 and was
not implemented. Two prior folds produced nothing, so folding is demonstrably not what has
been missing. This time it lands as an actual task with a verifiable outcome: a config
file that exists, a task target that exists, a CI job that invokes it, and a demonstration
that it fails on a planted violation.

It has no backing requirement — that is recorded, and the maintainer reaffirmed the fold.
It runs LAST in the phase so its first pass covers the Go code this phase added: the
permalink derivation and the two regression tests.

**This plan's outcome is reported separately and does not gate any of this phase's five
browse success criteria.**

Output: a pinned linter, a config, a task target, a bound CI job, a cleared first-run
backlog, and a closed todo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-10-add-golangci-lint-with-gofmt-and-idiomatic-go-linters.md
@.planning/phases/03-browse-inspect-navigation/03-CONTEXT.md
@go.tool-lint.mod
@Taskfile.yml
</context>

## Artifacts this phase produces

| Kind | Name |
|---|---|
| file | `.golangci.yml` |
| file | `go.tool-golangci.mod` + `go.tool-golangci.sum` (a FOURTH isolated tool modfile — `go.tool.mod`, `go.tool-lint.mod` and `go.tool-proto.mod` already exist on disk — created from the outset, see Task 1) |
| Taskfile var | `GO_TOOL_GOLANGCI` |
| tool directive | `github.com/golangci/golangci-lint/v2/cmd/golangci-lint` in `go.tool-golangci.mod` |
| Taskfile target | `lint:go` |
| Taskfile wrapper leg | `lint:go` added to the `lint` wrapper's `cmds:` |
| CI job | `lint-go` in `.github/workflows/ci.yml` |
| test fixture const | `golangciModfilePath` in `internal/upgrade/taskfile_shape_test.go`, ADDED to the modfile set `TestToolModfilesRemainIsolated` iterates (Task 1) |
| test fixture entry | `github.com/golangci/golangci-lint` added to `forbiddenToolPackages` in the same file (Task 1) |
| Taskfile `vuln` leg | a fifth `GOWORK=off go build -modfile=go.tool-golangci.mod` line, its `for name in …` loop entry, and the target's `desc:` corrected from four binaries to five (Task 1) |
| test fixture entry | `{Workflow: "ci.yml", JobID: "lint-go"}` added to `inScopeJobs` in `internal/upgrade/taskfile_shape_test.go` (Task 3) |

<tasks>

<task type="auto">
  <name>Task 1: Pin the linter, author the config, and add the task target</name>
  <files>go.tool-golangci.mod, go.tool-golangci.sum, .golangci.yml, Taskfile.yml, internal/upgrade/taskfile_shape_test.go</files>
  <read_first>
    - go.tool-lint.mod (the whole file, especially its header: this modfile is isolated from `go.tool.mod` because MVS resolves one version per module across the graph and actionlint's expected YAML API lost the bid to what task and goreleaser drag in — verified live, with the exact compile error recorded; `GOWORK=off` is REQUIRED on every invocation because workspace mode is incompatible with `-modfile`)
    - Taskfile.yml lines 8-12 (the `GO_TOOL` and `GO_TOOL_LINT` variables) and the `lint:actions` target (the shape `lint:go` mirrors — copy the target NAME convention from the Taskfile, not from the CI check's display string)
    - Taskfile.yml lines 204-213 (`vet`, and its recorded precedent that findings surfaced on first run are fixed, not suppressed)
    - Taskfile.yml lines 4977-4982 (the `lint` contributor wrapper `lint:go` joins)
    - Taskfile.yml lines 1123-1197 (the `vuln` target — its `desc:`, its four hardcoded `go build -modfile=` lines, its hardcoded `for name in task goreleaser govulncheck actionlint` loop, and the three-way exit-code classification the fifth binary must flow through unchanged; step (f))
    - internal/upgrade/taskfile_shape_test.go lines 23-33 (the modfile-path const block), lines 73-80 (`forbiddenToolPackages`, three entries today) and lines 893-937 (`TestToolModfilesRemainIsolated` — note line 928's hardcoded `[]string{toolModfilePath, lintModfilePath}`, the two-element slice step (e) extends)
    - internal/upgrade/taskfile_shape_test.go lines 831-837 (`TestGateStancesAgree` asserts the `vuln` desc states ADVISORY — the word must survive step (f)'s rewrite)
    - .planning/todos/pending/2026-08-10-add-golangci-lint-with-gofmt-and-idiomatic-go-linters.md, the Solution section and its two named constraints
  </read_first>
  <action>
(a) Create a FOURTH isolated tool modfile, `go.tool-golangci.mod`, from the outset. Do NOT
attempt co-location in `go.tool-lint.mod` first. (Four, not three: `go.tool.mod`,
`go.tool-lint.mod` and `go.tool-proto.mod` all exist on disk today. The isolation guard
knows about only the first two — see step (e), which is why this plan registers the new
one explicitly instead of assuming a guard that is already blind to a sibling will notice.)

Cycle-1 review (Codex, MEDIUM) is right that trying the known-fragile combination first is
churn dressed as diligence: `go.tool-lint.mod`'s own header records a LIVE-VERIFIED MVS
collision from exactly this pattern, with the exact compile error it produced — actionlint's
expected YAML API lost the version bid to what task and goreleaser drag in. golangci-lint
pulls a much larger tree than actionlint does (it vendors a substantial fraction of the Go
static-analysis ecosystem), so it is the most likely thing in this repository to win or
lose a bid against something else. The isolation pattern is already documented and already
proven; follow it rather than re-deriving the reason for it.

Add the tool directive there, with the module path's `/v2` element intact — that element
means the v2 line and therefore the v2 configuration schema. Resolve with `GOWORK=off`
(REQUIRED on every invocation: workspace mode is incompatible with `-modfile`). Give it its
own `GO_TOOL_GOLANGCI` variable in the Taskfile beside `GO_TOOL` and `GO_TOOL_LINT`,
following their exact shape (`GOWORK=off go tool -modfile=go.tool-golangci.mod`), and
write a header comment in the new modfile stating why it is separate and citing
`go.tool-lint.mod`'s recorded collision, so the next person adding a tool finds the pattern
rather than the accident.

**That header comment has a machine-checked shape**, because step (e) puts this file under
`TestToolModfilesRemainIsolated` and the guard reads it: it must be one or more `//`-prefixed
lines ABOVE the `module` directive (`parseToolModfileHeaderComment`, `taskfile_shape_test.go:340`),
and the joined text must contain the substring `isolat` case-insensitively (`:934`). Write
the rationale in words that naturally contain "isolated" or "isolation" — which the honest
rationale does anyway — rather than discovering the constraint from a red test.

Then verify `task lint:actions` still passes — it should be untouched, since nothing was
added to its modfile, and confirming that is what makes the isolation claim evidence rather
than an assumption. Record the module count the new modfile pulls in; `go.tool-lint.mod`'s
header states actionlint's own count and this one belongs beside it.

**Supply-chain recording for the tool tree** (cycle-1 Codex, MEDIUM — "plan 03-01 requires
human approval for test dependencies while this much larger executable dependency tree gets
no corresponding review"). This is deliberately handled by RECORDING rather than by a second
blocking checkpoint, and here is the reasoning, so the decision is visible rather than
implicit: golangci-lint is not a runtime dependency and never enters the shipped binary or
`web/build/`; it is pinned by exact module path in a modfile with a checked-in `.sum`, so
its tree is verified against the Go checksum database on every resolution; and it executes
only in CI and on a developer's machine, where the Go toolchain's own module-integrity
guarantees apply. Nevertheless, record in the SUMMARY: the exact module path installed
(character for character, since a `/v2` typo or a lookalike path is the realistic threat),
the resolved version, the module count the new modfile gained, and explicit confirmation
that `GOFLAGS`/`GONOSUMDB`/`GONOSUMCHECK`/`GOPRIVATE` were NOT modified to resolve it. If
the resolution required relaxing checksum verification in any way, STOP and raise it as a
blocking question instead of proceeding — that would be a different risk than the one
assessed here.

(b) Author `.golangci.yml` at the repository root. The v2 schema requires an explicit
top-level version key; set it. Enable a formatting check plus an idiomatic set. Start with
the ones the todo names — unused-result checking, ineffectual assignments, static analysis,
unused identifiers — and treat the set as a maintainer-visible choice rather than
"everything that ran clean": record in the SUMMARY exactly which linters you enabled and
which you considered and left off, with a one-line reason each.

Decide gofmt versus the stricter superset, which the todo leaves as an open question.
Record the choice and its reason. Whichever you pick, the repository is currently
unformatted in at least one place, so expect Task 2 to have real work.

Make the enabled set MACHINE-READABLE from the config itself: the file's first line is a
comment declaring the count of linters the config enables, in the exact form
`# enabled-linters: N` with N a decimal integer and nothing else on the line. Task 2's
`<verify>` reads this line and separately proves the config is byte-unchanged, which is
what binds Task 2's green to THIS set rather than to whatever set happened to be cheapest
to satisfy. The number must agree with the list you actually enable; if you later change
the list, changing this line is the visible act that says so.

(c) Add a `lint:go` target mirroring `lint:actions`. The linter's own exit status is the
verdict: it exits non-zero on findings. Do not capture its output into a variable and then
test the variable, and do not pipe it into anything — a pipeline returns the last
command's status, which is precisely how a red result reads as green, and this repository
has already written that bug by accident in this very problem area. Invoke it directly and
let the exit status propagate.

(d) Add `lint:go` to the `lint` wrapper's `cmds:` list. That wrapper carries no
set-equality guard — the one in `internal/upgrade` binds the `test` wrapper's six legs
only — so this addition is safe, but confirm it by running that guard.

(e) REGISTER the new modfile with `TestToolModfilesRemainIsolated`. This is not
conditional and it is not discretionary — the source settles it. That guard iterates a
hardcoded two-element slice, `[]string{toolModfilePath, lintModfilePath}`
(`internal/upgrade/taskfile_shape_test.go:928`), reading only those two headers and
`os.SameFile`-comparing only those two paths. A new modfile absent from that slice is
inspected by NOTHING, and the test stays green — it does not merely fail to guard the new
file, it PASSES BECAUSE the file is absent. That is the identical shape Task 3 diagnoses
one task later for `inScopeJobs`, and it is why the acceptance criterion for this test in
the previous revision ("passes with exactly one `--- PASS:` line") was already true on the
untouched tree, before any work.

Make four edits in `internal/upgrade/taskfile_shape_test.go`:
  1. Declare `golangciModfilePath = "../../go.tool-golangci.mod"` beside `toolModfilePath`
     and `lintModfilePath` in the same const block.
  2. Add `golangciModfilePath` to the slice the header-rationale loop iterates, so the new
     modfile's header comment is required to be non-empty and to state the isolation
     rationale — the same bar the other two clear.
  3. Extend the `os.SameFile` distinctness check to cover the new path against BOTH
     existing ones, so three modfiles are proven to be three distinct files rather than
     two plus an alias.
  4. Add `github.com/golangci/golangci-lint` to `forbiddenToolPackages`, so the root
     `go.mod` is held to not requiring it directly — the property D-03 exists to keep.

The `<verify>` block below asserts edits 1/2 and 4 with positive counts. That is
deliberate: the guard's own exit status cannot report its own blindness, so registration
is asserted directly rather than inferred from green. Both counts are 0 on the working
tree today (`rg -o 'golangciModfilePath' internal/upgrade/taskfile_shape_test.go` returns
0, `rg -o 'golangci/golangci-lint' internal/upgrade/taskfile_shape_test.go` returns 0)
against a control of 10 for `go\.tool-lint\.mod|lintModfilePath` — the search works, the
thing is simply absent.

While making edit 1, you will observe that `go.tool-proto.mod` ALSO exists on disk and is
ALSO absent from this fixture. That is a pre-existing gap of the same shape and it is
OUTSIDE this plan's scope — do NOT fix it here. RECORD it in the SUMMARY as an observed
pre-existing gap with the two counts that establish it, so it is visible to the maintainer
rather than silently inherited.

(f) Put the new tool binary IN SCOPE for the `vuln` gate. `Taskfile.yml`'s `vuln` target
(around lines 1163-1172) builds a HARDCODED set of four binaries — `task`, `goreleaser`
and `govulncheck` from `go.tool.mod`, `actionlint` from `go.tool-lint.mod` — and iterates
a hardcoded `for name in task goreleaser govulncheck actionlint`. Its own `desc:` states
that all four stay in scope with no allowlist and no exclusion, because the point is
detection. golangci-lint vendors a substantial fraction of the Go static-analysis
ecosystem and is plausibly the largest third-party executable tree this repository would
carry; adding it behind a gate that enumerates its binaries by hand would silently narrow
VULN-01/VULN-03 coverage in the same commit that grows the tool surface, and would make
the target's own `desc:` a false statement.

Three edits to the `vuln` target:
  1. A fifth build line,
     `GOWORK=off go build -modfile=go.tool-golangci.mod -o "${scratch}/golangci-lint" github.com/golangci/golangci-lint/v2/cmd/golangci-lint`,
     beside the existing four.
  2. `golangci-lint` APPENDED to the `for name in …` loop's word list, so it flows through
     the SAME exit-code classification (0 clean / 3 detected / anything else a distinct
     scan error) as the other four. Do not give it a different arm. Append, do not
     reorder: the loop header must read exactly
     `for name in task goreleaser govulncheck actionlint golangci-lint; do`, which is the
     literal the `<verify>` block counts. A different order is not wrong in itself, but it
     is not what the gate asserts, so it will read as a failure.
  3. Update the `desc:` and the `echo "==> Building tool binaries …"` line so both state
     five binaries and name the third modfile. The `desc:` currently says four in two
     places (the opening sentence and the "All four binaries stay in scope" sentence) and
     the echo says `go.tool.mod x3, go.tool-lint.mod x1`. Leaving those is not cosmetic:
     an unchanged `desc:` is a claim about coverage that stopped being true.
     **Preserve the word ADVISORY.** `TestGateStancesAgree`
     (`internal/upgrade/taskfile_shape_test.go:831-837`) asserts the `vuln` desc states
     the advisory stance; a rewrite that drops it turns this edit red.

Record the new binary's scan verdict in the SUMMARY with the same three-way classification
the target uses, and note the module count the fifth modfile contributes. The gate is
ADVISORY, so a detection does NOT block this plan — it is reported, exactly as
goreleaser's known permanently-unfixed GO-2026-5932 match is. What is NOT acceptable is
adding the tool and leaving it unscanned.
  </action>
  <verify>
    <automated>task --list-all &gt;/tmp/tl.txt 2&gt;&amp;1 || { cat /tmp/tl.txt; exit 1; }; go test ./internal/upgrade/ -run '^TestToolModfilesRemainIsolated$' -count=1 -v &gt;/tmp/iso.txt 2&gt;&amp;1 || { cat /tmp/iso.txt; exit 1; }; test "$(rg -o '^\* lint:go:' /tmp/tl.txt | wc -l | tr -d ' ')" -eq 1 &amp;&amp; test "$(grep -Eo '^--- PASS: TestToolModfilesRemainIsolated ' /tmp/iso.txt | wc -l | tr -d ' ')" -eq 1 &amp;&amp; test -f .golangci.yml &amp;&amp; test -f go.tool-golangci.mod &amp;&amp; test -f go.tool-golangci.sum &amp;&amp; test "$(rg -o '^# enabled-linters: [0-9]+$' .golangci.yml | wc -l | tr -d ' ')" -eq 1 &amp;&amp; test "$(rg -o 'golangciModfilePath' internal/upgrade/taskfile_shape_test.go | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o 'golangci/golangci-lint' internal/upgrade/taskfile_shape_test.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'for name in task goreleaser govulncheck actionlint golangci-lint' Taskfile.yml | wc -l | tr -d ' ')" -eq 1 &amp;&amp; task lint:actions</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .golangci.yml` succeeds and the file declares an explicit v2 version key.
    - `task --list-all | rg -o '^\* lint:go:' | wc -l` returns 1. Counted with `rg -o ... | wc -l`, not piped into `rg -qx '[[:space:]]*1'`: BSD `wc` left-pads its output with spaces, and matching a padded number with an anchored pattern is a recorded failure shape in this repository. Comparing the trimmed value with `test ... -eq 1` has no such hazard.
    - `test -f go.tool-golangci.mod` and `test -f go.tool-golangci.sum` both succeed — the linter is in its OWN modfile, created from the outset rather than after a failed co-location.
    - `task lint:actions` exits 0 after the change. Because nothing was added to `go.tool-lint.mod`, this should be unaffected; confirming it is what turns the isolation claim into evidence.
    - **The isolation guard's registration is asserted DIRECTLY, not inferred from it going green.** Cycle-2 review, both lanes independently, and orchestrator-confirmed against the working tree: `TestToolModfilesRemainIsolated` iterates the hardcoded two-element slice `[]string{toolModfilePath, lintModfilePath}` (`internal/upgrade/taskfile_shape_test.go:928`), so a new modfile absent from it is inspected by nothing and the test stays green. The previous revision's criterion here — "passes with exactly one `--- PASS:` line" — was therefore ALREADY TRUE on the untouched tree, before any work, and the fixture update was conditional prose whose condition the source settles. Both halves are closed by the two counts below, which are the same structural fix Task 3 applies to `inScopeJobs`.
    - `rg -o 'golangciModfilePath' internal/upgrade/taskfile_shape_test.go | wc -l` returns at least 2 — the const declaration AND at least one use (the iterated slice entry). Observed **0** on the working tree, against a control of 10 for `go\.tool-lint\.mod|lintModfilePath`: the search works, the thing is simply absent.
    - `rg -o 'golangci/golangci-lint' internal/upgrade/taskfile_shape_test.go | wc -l` returns at least 1 — the linter is in `forbiddenToolPackages`, so the root `go.mod` is held to not requiring it directly. Observed **0** on the working tree.
    - `go test ./internal/upgrade/ -run '^TestToolModfilesRemainIsolated$' -count=1 -v` passes with exactly one `--- PASS:` line naming that test. Kept, but ONLY as a companion to the two counts above — on its own it is satisfied by a tree where the new modfile was never registered.
    - **The `vuln` gate covers the new binary.** `rg -o 'for name in task goreleaser govulncheck actionlint golangci-lint' Taskfile.yml | wc -l` returns exactly 1 — the fifth binary is in the scan loop and flows through the same three-way exit-code classification as the other four. Observed **0** on the working tree, where the loop enumerates four.
    - `rg -o 'go\.tool-golangci\.mod' Taskfile.yml | wc -l` is at least 2 — the `GO_TOOL_GOLANGCI` variable and the `vuln` target's fifth `go build -modfile=` line. Observed **0** on the working tree.
    - The `vuln` target's `desc:` and its `==> Building tool binaries` echo both state FIVE binaries and name the third modfile, and the `desc:` still contains the word ADVISORY — `go test ./internal/upgrade/ -run '^TestGateStancesAgree$' -count=1 -v` passes, which is what proves the rewrite did not drop the stance the guard binds.
    - The SUMMARY records the new binary's `govulncheck -mode=binary` verdict under the target's own classification (0 clean / 3 symbol-matched / other a distinct scan error). The gate is ADVISORY, so a detection reports rather than blocks; an UNSCANNED tool is what is unacceptable.
    - The SUMMARY records, as an observed pre-existing gap explicitly OUT of scope for this plan: `go.tool-proto.mod` exists on disk and is likewise absent from `TestToolModfilesRemainIsolated`'s fixture and from `vuln`'s binary set, with the counts that establish it. Recorded so the maintainer sees it; NOT fixed here.
    - `go test ./internal/upgrade/ -run '^TestTaskfileWrapperIsSerial$' -count=1 -v` passes.
    - `rg -o 'lint:go' Taskfile.yml | wc -l` is at least 2 (the target definition and the wrapper leg).
    - `rg -o '^# enabled-linters: [0-9]+$' .golangci.yml | wc -l` returns exactly 1, and the declared N equals the number of linters the config actually enables. This line is the anchor Task 2's `<verify>` binds its green to.
    - The SUMMARY records: the enabled linter list with reasons and its count N, the linters considered and rejected with reasons, the formatting-strictness choice with its reason, the exact module path and resolved version installed, the module count the new modfile gained, and explicit confirmation that no checksum-verification setting was relaxed.
  </acceptance_criteria>
  <done>The linter is pinned in its own isolated modfile that leaves actionlint untouched, that modfile is REGISTERED with the isolation guard and IN SCOPE for the vulnerability scan rather than invisible to both, the config exists and states its own choices and its own enabled count, and `task lint:go` runs it with the linter's exit status as the verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Clear the first-run backlog by fixing, never suppressing</name>
  <files>internal/query/files_status_test.go</files>
  <read_first>
    - internal/query/files_status_test.go (the known-unformatted file: a test block indented one level too deep, introduced by a phase-08 commit and never caught because no gate existed)
    - Taskfile.yml lines 204-213 (`vet`'s recorded precedent: findings surfaced on first run are fixed, not suppressed)
    - internal/gitmeta/permalink.go, internal/uiserver/permalink.go, internal/uiserver/confinement_test.go, web/highlight_coverage_test.go (the Go code this phase added — the real surface this gate was folded in to cover)
  </read_first>
  <action>
Run `task lint:go` and record the complete finding list BEFORE changing anything: the
total count, and the count per linter. That inventory is the evidence that the gate found
something, and it is what makes the eventual green meaningful.

Then fix them. Formatting findings are mechanical. Idiomatic findings are not: read each
one and fix the code, do not silence the check. If a specific finding is genuinely a false
positive or genuinely not worth fixing, an inline suppression is acceptable ONLY with a
comment stating why, and every such suppression must be listed in the SUMMARY with its
justification. A blanket file-level or repository-level exclusion added to get to green is
not acceptable — it converts the gate into a decoration.

If the backlog turns out to be large enough that fixing it all would swamp this plan,
STOP and report the count rather than narrowing the linter set to make it smaller.
Narrowing the set to fit the backlog is the same act as suppressing, one level up.

**Do not touch `.golangci.yml` in this task.** It is deliberately absent from this task's
`<files>`, and the `<verify>` block below proves it byte-unchanged against HEAD rather
than trusting the prose. Cycle-2 review (Claude, LOW) is right that the previous
revision's bare `task lint:go` bound this task's green to nothing: a narrowed config
produces the identical pass, so the prohibition existed only as prose. Byte-identity to
what Task 1 committed is strictly stronger than the equal-count check the review asked
for, and it is runnable. Run the block BEFORE committing this task, which is the standard
verify-then-commit order; the commit-count criterion below covers the same property
afterwards.

Confirm the phase's own new Go code is clean: the permalink derivation, the permalink
handler, the confinement regression test and the highlight coverage guard.
  </action>
  <verify>
    <automated>test -f .golangci.yml &amp;&amp; test "$(rg -o '^# enabled-linters: [0-9]+$' .golangci.yml | wc -l | tr -d ' ')" -eq 1 &amp;&amp; git diff --exit-code HEAD -- .golangci.yml &amp;&amp; task lint:go</automated>
  </verify>
  <acceptance_criteria>
    - **The green is BOUND to Task 1's linter set, not merely to a green run.** The `<verify>` block asserts, in order: `.golangci.yml` exists (a positive control — `git diff --exit-code` on an absent path exits 0 and would otherwise pass in a world where Task 1 never ran); its `# enabled-linters: N` anchor line is present exactly once; the file is byte-identical to what Task 1 committed; and only then that the gate is green. A run that reached green by narrowing the config fails the third conjunct. Cycle-2 review (Claude, LOW): the previous revision was a bare `task lint:go`, which a narrowed `.golangci.yml` satisfies identically.
    - `git log --oneline -- .golangci.yml | wc -l` returns exactly 1 across this whole plan — the config was authored once, by Task 1, and never amended to fit the backlog. This is the commit-order-independent form of the same property, checkable after this task's commit. It returns 0 on the pre-implementation tree.
    - The SUMMARY records the `# enabled-linters: N` value observed at the end of this task and confirms it equals the N Task 1 recorded.
    - `task lint:go` exits 0.
    - The SUMMARY records the pre-fix finding inventory: total count and per-linter counts, captured before any fix.
    - `git diff --stat` for this task shows the fixes, and the SUMMARY names each file touched.
    - Every inline suppression introduced, if any, is listed in the SUMMARY with its justification; if there are none, the SUMMARY states that explicitly rather than omitting the section.
    - `rg -o 'nolint' --glob '!vendor' . | wc -l` is recorded in the SUMMARY both before and after this task, and any increase is accounted for by the listed justifications.
    - `task test:unit` exits 0 and `go vet ./...` exits 0 after the fixes — a formatting or idiomatic fix that changed behaviour would fail here.
  </acceptance_criteria>
  <done>The gate is green because the code was fixed against the linter set Task 1 chose — proven by that config being byte-unchanged — with the pre-fix inventory recorded so the green is evidence rather than an assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Wire it into CI, bind it to the single-definition guard, and prove it RED</name>
  <files>.github/workflows/ci.yml, internal/upgrade/taskfile_shape_test.go, .planning/todos/pending/2026-08-10-add-golangci-lint-with-gofmt-and-idiomatic-go-linters.md</files>
  <read_first>
    - .github/workflows/ci.yml lines 405-445 (the `actionlint` job: checkout, Go setup with `go-version-file`, module cache, install-task, then a single `run: task lint:actions` step — the exact job shape to mirror)
    - internal/upgrade/taskfile_shape_test.go lines 109-140 (`inScopeJob`, `inScopeJobs` — the literal fixture naming every job whose step run bodies are held to being exactly `task <target>`; a job absent from this list is simply not bound, which is a silent gap rather than a failure)
    - internal/upgrade/taskfile_shape_test.go, the `runBodyExceptions` set (so the new job's step needs no exception)
    - Taskfile.yml `lint:go` (added in Task 1)
    - internal/query/files_status_test.go (the file Task 2 just fixed — this task plants its two RED-proof violations here, because it is a real file in a real included package and is therefore actually inspected; see step (c))
  </read_first>
  <action>
(a) Add a `lint-go` job to `.github/workflows/ci.yml` mirroring the `actionlint` job step
for step: checkout, Go setup from the module's own version file, module cache, install
Task, then one step whose run body is exactly `task lint:go`. Use the same SHA-pinned
action references the neighbouring job uses; do not introduce a new or floating pin.

(b) Add `{Workflow: "ci.yml", JobID: "lint-go"}` to `inScopeJobs` in
`internal/upgrade/taskfile_shape_test.go`. That fixture is an allowlist of the jobs the
single-definition guard binds, not a set-equality check over every job in the file — so a
new job that is not added there is simply unguarded, and its run body could later drift
into an inline command with nothing objecting. Adding it is the difference between a job
that is wired and a job that is wired AND held.

(c) Demonstrate the gate RED, twice, because the two failure classes come from different
linters and a config that enables one but silently mis-enables the other would still look
green.

**Plant in a file the linter actually inspects, and prove you put it back.** Cycle-1 review
(Codex, LOW) flagged "scratch Go file" as unspecified: a file written outside the paths
`.golangci.yml` includes — or outside any package `go list ./...` reports — is not
inspected at all, so a green after reverting it would be a green the linter never earned,
and the whole RED proof would be theatre. Use an EXISTING file in an existing included
package. `internal/query/files_status_test.go` is the natural choice: Task 2 has just
fixed it, so it is known to be inspected and known to be clean at this moment.

For each of the two plants:
  1. record `shasum -a 256 <file>` BEFORE the plant;
  2. plant the violation —
     - formatting class: an extra indentation level inside a real function body;
     - idiomatic class: an assignment whose value is never subsequently used;
  3. run `task lint:go`, confirm it exits NON-ZERO and that its output names that exact
     file, and record both the exit code and the naming line verbatim;
  4. revert;
  5. record `shasum -a 256 <file>` AFTER the revert and confirm it equals the before
     value — byte-identical restoration, not "looks the same";
  6. run `task lint:go` again, confirm exit 0, record it.

Record all observations: two before-hashes, two failure texts with their file names, two
non-zero exit codes, two after-hashes matching, and two restored-green exit codes. A RED
proof whose restoration was not verified is a RED proof that may have left the file
subtly different.

(d) Move the todo from `.planning/todos/pending/` to `.planning/todos/completed/`, adding a
`resolves_phase: 3` marker and a one-line record of what landed. This item was folded twice
before and resolved neither time; leaving it in pending would make this the fourth fold.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/ -run '^TestWorkflowRunBodiesInvokeTask$' -count=1 -v &gt;/tmp/wf.txt 2&gt;&amp;1 || { cat /tmp/wf.txt; exit 1; }; test "$(grep -Eo '^--- PASS: TestWorkflowRunBodiesInvokeTask' /tmp/wf.txt | wc -l | tr -d ' ')" -eq 1 &amp;&amp; test "$(rg -o 'task lint:go' .github/workflows/ci.yml | wc -l | tr -d ' ')" -eq 1 &amp;&amp; test "$(rg -o 'JobID: "lint-go"' internal/upgrade/taskfile_shape_test.go | wc -l | tr -d ' ')" -eq 1 &amp;&amp; task lint:actions</automated>
  </verify>
  <acceptance_criteria>
    - **The two `rg` count assertions are now INSIDE the `<verify>` block, not only in these criteria.** Cycle-1 review, BOTH lanes, and orchestrator-confirmed: the cycle-1 block was `task lint:actions && go test ... -run 'TestWorkflowRunBodiesInvokeTask' ... && grep -Eqo '--- PASS: ...'`, and every part of it is green TODAY. `TestWorkflowRunBodiesInvokeTask` iterates only `inScopeJobs` (`internal/upgrade/taskfile_shape_test.go:129-138`), so with no `lint-go` job in `ci.yml` and no fixture entry there is nothing new for it to bind — it does not merely fail to guard the job, it PASSES BECAUSE the job is absent. `task lint:actions` passes today too. The whole block exited 0 against a completely unimplemented task, and the only real assertions lived down here where no `<verify>` reads them. Orchestrator check on the working tree: `rg -o 'lint-go' .github/workflows/ci.yml` returns 0 and the fixture has no such entry, while the control `rg -o '\blint\b' .github/workflows/ci.yml` returns 4 — the search works, the thing is simply absent.
    - `rg -o 'task lint:go' .github/workflows/ci.yml | wc -l` returns exactly 1 — the job exists and invokes the target. Exactly-one, not at-least-one: two invocations would mean a duplicated step.
    - `rg -o 'JobID: "lint-go"' internal/upgrade/taskfile_shape_test.go | wc -l` returns exactly 1 — the job is REGISTERED with the single-definition guard. This is the assertion that turns the `go test` below from a tautology into a real check: only once the job is in `inScopeJobs` does that test have anything to say about it.
    - `go test ./internal/upgrade/ -run '^TestWorkflowRunBodiesInvokeTask$' -count=1 -v` exits 0 and its output contains exactly one `--- PASS:` line naming that test. Counted with `grep -Eo ... | wc -l -eq 1` rather than `grep -Eqo`, because `-q` reports only "matched something" and `go test -run PATTERN` exits 0 when the pattern matches nothing.
    - `task lint:actions` exits 0 (the new job is schema-valid).
    - The SUMMARY records all step-(c) observations: two before-hashes, two failure texts naming the planted file, two non-zero exit codes, two after-hashes equal to their before-hashes, and two restored-green exit codes.
    - `ls .planning/todos/completed/ | rg -o '2026-08-10-add-golangci-lint-with-gofmt-and-idiomatic-go-linters.md' | wc -l` returns 1.
    - `ls .planning/todos/pending/ | rg -o '2026-08-10-add-golangci-lint' | wc -l` returns 0.
  </acceptance_criteria>
  <done>CI runs the linter through the task target, that job is bound by the single-definition guard, the gate has been shown failing on both a formatting and an idiomatic violation, and the twice-folded todo is finally closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| linter tool modfile -> CI build environment | A new tool dependency tree is resolved and compiled in CI |
| lint gate verdict -> merge decision | A gate that cannot fail lets everything through while appearing to guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-36 | Tampering | a lint gate that cannot fail | high | mitigate | Two planted violations, one per failure class, each demonstrated RED with its exit code and failing file recorded, and each reverted to green; the target uses the linter's own exit status with no capture and no pipeline |
| T-03-37 | Tampering | a new CI job drifting away from the single-definition property | medium | mitigate | The job is added to the guard's `inScopeJobs` fixture, so its step run body is held to being exactly a task invocation |
| T-03-SC-d | Tampering | a structural guard that is blind to the artifact it is supposed to guard | high | mitigate | `TestToolModfilesRemainIsolated` iterates a hardcoded two-element slice, so a new modfile absent from it is inspected by nothing AND the test stays green — absence produces a pass, not a failure. Closed by registering `golangciModfilePath` in the const block, the iterated set, the pairwise `os.SameFile` distinctness check and `forbiddenToolPackages`, and by asserting that registration with positive `rg -o … \| wc -l` counts INSIDE Task 1's `<verify>` rather than inferring it from the guard's exit status. Both counts are 0 on the pre-implementation tree against a working control, so the block is red in an unimplemented world |
| T-03-SC-e | Tampering | a large executable dependency tree entering CI unscanned while the gate still claims full coverage | high | mitigate | The `vuln` target enumerates its binaries by hand and its `desc:` asserts no allowlist and no exclusion; a fifth build line, its loop entry and the corrected `desc:`/echo land in the SAME task that adds the modfile, and the new binary's `govulncheck -mode=binary` verdict is recorded under the target's existing three-way classification. The gate is ADVISORY so a detection reports rather than blocks — an UNSCANNED tool is the threat, not a detected one |
| T-03-SC-c | Tampering | the linter's own dependency tree entering the build environment | medium | mitigate | Pinned by EXACT module path (including the `/v2` element — a typo or lookalike path is the realistic threat) in its OWN modfile `go.tool-golangci.mod` with a checked-in `.sum`, resolved with workspace mode off, isolated from the other tool modfiles so a version bid in one cannot silently change another, and IN SCOPE for `vuln`'s binary scan (T-03-SC-e). Explicitly NOT gated behind a second blocking human checkpoint, and the reasoning is recorded in Task 1 rather than left implicit (cycle-1 Codex MEDIUM asked why 03-01's test deps get one and this larger tree does not): this is a build-time-only tool that never enters the shipped binary or `web/build/`, its tree is verified against the Go checksum database on every resolution, and the `.sum` file is committed. The compensating controls are recording obligations — exact module path, resolved version, module count gained, and explicit confirmation that no checksum-verification setting (`GOFLAGS`/`GONOSUMDB`/`GONOSUMCHECK`/`GOPRIVATE`) was relaxed to make it resolve. If any relaxation IS required, Task 1 stops and escalates, because that would be a materially different risk than the one assessed here |
| T-03-38 | Repudiation | suppressing findings to reach green | high | mitigate | Blanket exclusions are prohibited; the pre-fix inventory is recorded before any change; every inline suppression must be individually justified in the SUMMARY, and the repository-wide suppression count is recorded before and after |
</threat_model>

<verification>
- `task lint:go` exits 0.
- `task lint:actions` exits 0.
- `task lint` exits 0 (all legs).
- `go test ./internal/upgrade/ -count=1` exits 0 — the wrapper, tool-modfile-isolation and workflow-run-body guards all pass.
- **The CI wiring is asserted directly, never only through `TestWorkflowRunBodiesInvokeTask`:** `rg -o 'task lint:go' .github/workflows/ci.yml | wc -l` returns 1 AND `rg -o 'JobID: "lint-go"' internal/upgrade/taskfile_shape_test.go | wc -l` returns 1. Both are 0 on the pre-implementation tree (orchestrator-confirmed), and that test passes precisely BECAUSE the job is absent, so it cannot be the sole evidence.
- **The modfile registration is asserted directly, never only through `TestToolModfilesRemainIsolated`:** `rg -o 'golangciModfilePath' internal/upgrade/taskfile_shape_test.go | wc -l` is at least 2 AND `rg -o 'golangci/golangci-lint' internal/upgrade/taskfile_shape_test.go | wc -l` is at least 1. Both are 0 on the pre-implementation tree (orchestrator-confirmed). That guard iterates a hardcoded two-element slice, so like the CI guard above it passes precisely BECAUSE the new modfile is absent — the same shape, one task earlier.
- **The new tool binary is in scope for the vulnerability scan:** `rg -o 'for name in task goreleaser govulncheck actionlint golangci-lint' Taskfile.yml | wc -l` returns 1 (0 today, where the loop enumerates four), the `vuln` target's `desc:` and build echo both say five binaries, and `go test ./internal/upgrade/ -run '^TestGateStancesAgree$'` still passes — the rewrite kept the ADVISORY stance that guard binds.
- **Task 2's green is bound to Task 1's linter set:** `.golangci.yml` carries an `# enabled-linters: N` anchor, is byte-unchanged against HEAD when `task lint:go` goes green, and `git log --oneline -- .golangci.yml | wc -l` returns 1 across the plan.
- `task test:unit` exits 0; `go vet ./...` exits 0.
- The SUMMARY records: the enabled/rejected linter lists with reasons, the exact module path and version and the no-checksum-relaxation confirmation, the pre-fix finding inventory, the suppression count before and after, and every step-(c) observation including the before/after hashes proving byte-identical restoration.
</verification>

<success_criteria>
- A formatting and idiomatic-Go gate exists, runs in CI, and has been demonstrated failing on both classes of violation it claims to catch.
- The first-run backlog was fixed rather than silenced, with the inventory recorded.
- The twice-folded todo is closed with a phase marker.
- **This plan's outcome is reported separately and does not gate any of this phase's five browse success criteria.**
</success_criteria>

<output>
Create `.planning/phases/03-browse-inspect-navigation/03-10-SUMMARY.md` when done.
</output>
