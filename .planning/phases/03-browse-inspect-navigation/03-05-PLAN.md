---
phase: 03-browse-inspect-navigation
plan: 05
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/uiproto/uiv1/ui.proto
  - internal/uiproto/uiv1/ui.pb.go
  - internal/uiproto/uiv1/uiv1connect/ui.connect.go
  - web/src/lib/gen/ui_pb.ts
  - internal/query/node.go
  - internal/gitmeta/permalink.go
  - internal/gitmeta/permalink_test.go
  - internal/uiserver/permalink.go
  - internal/uiserver/permalink_test.go
autonomous: false
requirements: [BRW-09, SRV-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`GetPermalink` returns a `github.com/{owner}/{repo}/blob/{sha}/{path}` URL pinned to the commit recorded in the index's own `Meta`, not to `HEAD`, so the remote view matches what the UI showed."
    - "`availability` is three-valued and is never collapsed to a boolean: a commit found in a remote-tracking branch is linkable; a commit not found there is linkable-but-unverified with a reason naming the uncertainty; every non-linkable case is no-link with a reason."
    - "A commit that is present in no remote-tracking branch is reported as UNCERTAIN, never as a fact that the link will fail — `git branch -r --contains` is sound in one direction only, and a read-only tool must not fetch to find out."
    - "A non-GitHub remote host resolves to an explicit no-link with a reason naming the host — never a URL built with GitHub's path shape against another forge."
    - "Host matching is exact: only the literal `github.com` is accepted, following the same exact-match discipline SRV-02's Origin guard already uses."
    - "An index whose `Meta` carries no commit SHA — a pre-upgrade graph, which this repository's own long-lived index currently is — resolves to no-link with a reason that names re-indexing as the remedy, never to an error."
    - "A repository with no remote at all, and a machine with no `git` binary at all, each resolve to no-link with a reason, never to an error — the same degrade-to-unknown contract `resolveHeadCommitSHA` already follows."
    - "A remote URL containing embedded credentials never appears in `url` or in `reason`."
    - "When `end_line` is supplied the anchor is a range; when it is absent the anchor is a single line; when `line` is absent there is no anchor at all."
    - "A `path` that escapes the repository root is refused by the SAME confinement gate `GetNodeDetail` uses, with no second implementation, and the refusal is proven at the RPC boundary alongside a legitimate path that succeeds in the same test."
    - "`task proto:drift` is green and still reports exactly 4 compared generated files — this change adds messages and one method, not a new generated file, so the floor does not move."
    - statement: "Following the returned URL in a browser lands on the same file and line the UI was showing."
      verification: backstop
  artifacts:
    - internal/gitmeta/permalink.go
    - internal/uiserver/permalink.go
    - internal/uiproto/uiv1/ui.proto
  key_links:
    - "GetPermalink handler -> withEngine -> query.Engine.IndexMeta -> schema.IndexedCommitSHA (the INDEXED commit, not HEAD)"
    - "GetPermalink handler -> query.Engine.ValidateRepoRelativePath -> resolveSourcePath (the single confinement gate, reused not copied)"
    - "gitmeta remote derivation -> exact github.com host match -> owner/repo extraction -> URL assembly"
    - "gitmeta pushed check -> git branch -r --contains -> three-valued availability"
  prohibitions:
    - statement: "`GetPermalink` must NOT perform any network operation and must NOT mutate repository state — no fetch, no write, no config change. The tool is read-only by construction (SRV-03), and fetching on the user's behalf to answer a UI question is a side effect they did not ask for."
      status: unverified
      verification: flagged
    - statement: "The unverified state must NOT be presented as a verdict about the link. Saying a link is broken when the user simply has not fetched recently trains people to ignore the warning, and a warning that is wrong half the time is worse than no warning."
      status: unverified
      verification: flagged
    - statement: "A permalink must NOT be constructed for a forge whose URL shape has not been implemented. Emitting GitHub's path shape against GitLab or Bitbucket produces a confidently wrong link, which is worse than an explicit refusal with a reason."
      status: unverified
      verification: flagged
---

<objective>
Add the one new RPC this phase introduces: `GetPermalink`, which turns a repository path
and a line (or line range) into a GitHub permalink pinned to the commit the index was
built at.

The server owns all of it — remote-URL derivation, `url.<base>.insteadOf` expansion,
GitHub detection, and the pushed-commit hint — because the browser cannot read
`.git/config` and because only the server can say "this commit is in no remote-tracking
branch, so this link may 404", which is BRW-09's actual failure mode. A static template
string is a pure formatter; it can say where a file WOULD live but cannot express that.

This is also where SRV-05 meets the new endpoint. `GetPermalinkRequest.path` is a
client-steerable filesystem path, so it goes through the same confinement gate
`GetNodeDetail` uses — reused, never reimplemented — and that reuse is proven at the RPC
boundary with a passing control alongside the refusals.

Purpose: BRW-09 is what makes a truncated local view usable (D-20) and what makes a
shared observation checkable by someone else. Getting the uncertainty honest is the whole
design: for a local dev tool, the indexed commit is very frequently an unpushed local
commit, so the expected failure is ordinary daily use, not an edge case.

Output: an additive proto method and its messages, a `internal/gitmeta` remote/pushed
derivation layer following that package's existing degrade-to-empty contract, a
`internal/uiserver` handler, and tests covering every branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-browse-inspect-navigation/03-CONTEXT.md
@.planning/phases/03-browse-inspect-navigation/03-RESEARCH.md
@.planning/phases/03-browse-inspect-navigation/03-PATTERNS.md
@internal/gitmeta/worktree.go
@internal/indexer/commit.go
</context>

## Artifacts this phase produces

| Kind | Name |
|---|---|
| proto rpc | `UIService.GetPermalink` |
| proto message | `GetPermalinkRequest` (`path`=1, `line`=2, optional `end_line`=3) |
| proto message | `GetPermalinkResponse` (`url`=1, `availability`=2, `reason`=3) |
| proto enum | `PermalinkAvailability` with UNSPECIFIED / LINKABLE / LINKABLE_UNVERIFIED / NO_LINK |
| Go file | `internal/gitmeta/permalink.go` |
| Go func | `internal/gitmeta.RemoteGitHubRepo(ctx, dir) (owner, repo string)` |
| Go func | `internal/gitmeta.RemoteURL(ctx, dir) string` |
| Go func | `internal/gitmeta.CommitOnRemoteTrackingBranch(ctx, dir, sha string) (bool, error)` |
| Go file | `internal/uiserver/permalink.go` |
| Go method | `(*uiService).GetPermalink` |
| Go method | `(*query.Engine).ValidateRepoRelativePath(relPath string) error` |
| Go test files | `internal/gitmeta/permalink_test.go`, `internal/uiserver/permalink_test.go` |
| generated | regenerated `ui.pb.go`, `ui.connect.go`, `web/src/lib/gen/ui_pb.ts` |

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Freeze the GetPermalink wire shape before it is published</name>
  <files>internal/uiproto/uiv1/ui.proto</files>
  <read_first>
    - internal/uiproto/uiv1/ui.proto lines 1-60 (the additive-only header comment: within this package a field number is never renumbered and never reused; retired fields get a `reserved` clause) and lines 44-59 (the nine-method service block a tenth line joins)
    - .planning/phases/03-browse-inspect-navigation/03-CONTEXT.md D-06 through D-09 (including D-06's explicit one-way reversibility note)
    - .planning/phases/03-browse-inspect-navigation/03-RESEARCH.md, the `GetPermalink` proto sketch and the pushed-commit check snippet
    - internal/uiproto/uiv1/ui.proto lines 92-100 (`Location`: name/kind/file/start_line only — the message D-09 deliberately does NOT extend with `end_line`)
  </read_first>
  <decision>The exact published wire shape of `GetPermalink`: method name, both message shapes with their field numbers, how `availability` is represented, and what is returned when the remote-tracking-branch check cannot execute.</decision>
  <context>
D-06 records this decision as one-way. A published method on `uiv1.UIService` inherits
D-02a's additive-only discipline: it can be deprecated but never removed, renumbered or
reshaped once any client depends on it. That is why this is `gate="blocking-human"` — an
auto-selected default here is permanent.

Nothing is generated yet. Claude presents the shape from 03-RESEARCH.md's sketch:
`GetPermalinkRequest` with `path` = 1, `line` = 2, optional `end_line` = 3;
`GetPermalinkResponse` with `url` = 1, `availability` = 2, `reason` = 3; and
`PermalinkAvailability` as a proto enum with a zero-valued UNSPECIFIED member alongside
LINKABLE, LINKABLE_UNVERIFIED and NO_LINK.

Three of the four points below were left to discretion by CONTEXT.md. The fourth was
surfaced during planning and is not settled anywhere.
  </context>
  <options>
    <option id="shape-as-sketched">
      <name>Adopt the sketched shape verbatim: enum availability, server-read commit SHA, and unverified-on-check-failure</name>
      <pros>The enum is closed and exhaustively switchable on both ends, and its zero value is a named UNSPECIFIED rather than an empty string that could pass for a valid state. The server reading the commit SHA from the index metadata means a client cannot pin the link to the wrong commit. Returning unverified when the containment check itself fails keeps "I could not check" distinct from "there is no link", which is exactly what the third state exists to express.</pros>
      <cons>Commits the enum's member set permanently; a future forge would need new members rather than new strings.</cons>
    </option>
    <option id="shape-string-availability">
      <name>Same shape, but `availability` as a string</name>
      <pros>New states can be added without a proto change.</pros>
      <cons>An empty string is indistinguishable from an unset field and from an invalid state; no exhaustiveness checking on either end; the closed set is the point.</cons>
    </option>
    <option id="shape-nolink-on-check-failure">
      <name>Same shape, but return NO_LINK when the remote-tracking-branch check cannot execute (as 03-RESEARCH.md's snippet does)</name>
      <pros>Matches the researched snippet literally; a caller sees one fewer state in practice.</pros>
      <cons>Collapses "I could not check" into "there is no link" while the URL is perfectly constructible — re-introducing exactly the false certainty D-07 rejects.</cons>
    </option>
  </options>
  <reversibility rating="one-way">A published RPC method and its message field numbers on `uiv1.UIService` inherit D-02a's additive-only discipline: they can be deprecated but never removed, renumbered or reshaped once any client depends on them.</reversibility>
  <resume-signal>Reply with the chosen option id, plus any change to field names or numbers. That answer is what the next tasks write into the proto, permanently.</resume-signal>
  <acceptance_criteria>
    - The SUMMARY records a decision on all four points: field names, the availability representation, whether the server reads the commit SHA, and the disposition for a failed remote-tracking-branch check.
    - The recorded shape assigns explicit field numbers starting at 1 for both new messages.
    - The recorded shape includes a zero-valued UNSPECIFIED member if an enum was chosen.
  </acceptance_criteria>
  <done>The wire shape is approved by a human and written down, before any generated code exists.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Git remote derivation, exact GitHub detection, and the one-directional pushed check</name>
  <files>internal/gitmeta/permalink.go, internal/gitmeta/permalink_test.go</files>
  <read_first>
    - internal/gitmeta/worktree.go lines 1-90 (the package doc's degrade-to-safe-zero-value contract, `gitTimeout`, the `exec.CommandContext` + `cmd.Stdin = nil` shape every function here uses so git can never block on an interactive prompt)
    - internal/indexer/commit.go lines 1-80 (`gitExecLookPath` as the test-only seam, and `resolveHeadCommitSHA`'s "git absent -> empty, never an error" contract this extends)
    - internal/gitmeta/githooks_test.go lines 1-100 (the existing fixture shape for git-initialized temp repositories in this package)
    - internal/uiserver/originguard.go lines 1-60 (SRV-02's exact-match allowlist discipline — no Contains, no HasPrefix, no wildcard regex; the same discipline applies to matching the remote host)
    - .planning/phases/03-browse-inspect-navigation/03-CONTEXT.md D-07 and D-08
  </read_first>
  <behavior>
    - `https://github.com/owner/repo.git` -> owner `owner`, repo `repo`.
    - `https://github.com/owner/repo` (no `.git` suffix) -> same result.
    - `git@github.com:owner/repo.git` (scp-like syntax) -> same result.
    - `ssh://git@github.com/owner/repo.git` -> same result.
    - `https://someone:sometoken@github.com/owner/repo.git` -> same owner/repo, and the credentials appear in no returned value.
    - `https://gitlab.com/owner/repo.git` -> no owner/repo, and the reason names the host.
    - `https://github.example.com/owner/repo.git` -> no owner/repo: the host is not exactly `github.com`, and a suffix or substring match would wrongly accept it.
    - a repository with no `origin` remote configured -> no owner/repo, no error.
    - a directory that is not a git repository -> no owner/repo, no error.
    - `gitExecLookPath` returning an error (git absent) -> no owner/repo, no error, and no subprocess is attempted.
    - a commit reachable from a remote-tracking branch -> reported present.
    - a commit reachable from no remote-tracking branch -> reported absent, WITHOUT any network access.
    - a `url.<base>.insteadOf` rewrite configured for the remote -> the rewritten URL is what gets parsed.
  </behavior>
  <action>
Create `internal/gitmeta/permalink.go` following this package's existing shape exactly:
a file-level doc comment naming D-06/D-07/D-08/D-09, `exec.CommandContext` bounded by the
existing `gitTimeout`, `cmd.Stdin = nil` on every invocation, and a `gitExecLookPath`-style
package-level variable as the test-only availability seam.

`RemoteURL(ctx, dir)` reads the remote URL via git's own URL-expansion subcommand — the
one that applies `url.<base>.insteadOf` rewrites and, per git's own documentation, exits
WITHOUT talking to the remote. Do not use the plain remote-config read: it returns the
unexpanded value, so a developer with an `insteadOf` rewrite configured would get a
different answer from this function than git itself uses.

`RemoteGitHubRepo(ctx, dir)` parses that URL into owner and repository. Handle the three
transport shapes real repositories use: a URL with a scheme, the scp-like
`user@host:path` form, and a bare path form. Compare the host to the literal `github.com`
by exact equality. Do not use substring, prefix or suffix matching: this project already
closed a DNS-rebinding class by exact-match allowlisting, and a host-suffix match here
accepts an attacker-controlled lookalike host as if it were the real forge.

Strip any userinfo component before anything is returned or logged. A remote URL is a
common place for a personal access token to be embedded, and this value flows into a
response body an unauthenticated loopback caller can read.

`CommitOnRemoteTrackingBranch(ctx, dir, sha)` runs the remote-branch containment query
and reports presence as a boolean plus an error for "could not check". Document in the
function's own comment that the answer is sound in exactly ONE direction: a hit proves
the commit is on a remote; no hit proves only that the last fetch did not see it, since
someone else may have pushed it or the user may simply not have fetched. This function
must never fetch. Say so in the comment, so a future maintainer who is tempted to make
the check more accurate reads why it deliberately is not.

Every function degrades to its zero value on any failure — missing git, non-repo
directory, timeout, unparseable URL — never to an error the caller must handle. This is
the same contract `WorktreeRoot` and `resolveHeadCommitSHA` already keep, and a read
query must never fail or block on git being unavailable.

Write `internal/gitmeta/permalink_test.go` covering every case in `<behavior>`, using
real git-initialized temp repositories for the cases that need one (following the
existing fixture shape in this package) and the exec seam for the git-absent case.
Write the tests first and record the RED run.
  </action>
  <verify>
    <automated>go test ./internal/gitmeta/ -run Permalink -count=1 -v 2>&amp;1 | tee /tmp/gm.txt; test "${PIPESTATUS[0]}" -eq 0 &amp;&amp; test "$(grep -Eco -- '--- PASS: Test.*Permalink|--- PASS: .*Remote' /tmp/gm.txt)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/gitmeta/ -run Permalink -count=1 -v` exits 0 and its output contains at least one `--- PASS:` line, so a pattern matching nothing cannot read as a green exit.
    - Every case in `<behavior>` has a named subtest; the SUMMARY lists them.
    - `rg -o 'Contains|HasPrefix|HasSuffix' internal/gitmeta/permalink.go | wc -l` returns 0 for host comparison — host matching is exact equality. (Any occurrence for a non-host purpose must be justified in the SUMMARY.)
    - `rg -o 'cmd.Stdin = nil' internal/gitmeta/permalink.go | wc -l` equals the number of `exec.CommandContext` calls in that file.
    - `rg -o 'git fetch|"fetch"' internal/gitmeta/permalink.go | wc -l` returns 0 — no network operation exists.
    - A credential-bearing remote URL test asserts the token substring appears in NO returned value, paired in the same test with an assertion that the owner and repo WERE extracted, so the absence check is proven to be inspecting a populated result.
    - The SUMMARY records the RED run for at least the host-exactness case and the credential-stripping case.
  </acceptance_criteria>
  <done>Remote derivation handles every real-world URL shape, accepts only the exact GitHub host, never leaks credentials, never fetches, and degrades to zero values on every failure.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: The additive proto method, the handler, and its confinement proof</name>
  <files>internal/uiproto/uiv1/ui.proto, internal/uiproto/uiv1/ui.pb.go, internal/uiproto/uiv1/uiv1connect/ui.connect.go, web/src/lib/gen/ui_pb.ts, internal/query/node.go, internal/uiserver/permalink.go, internal/uiserver/permalink_test.go</files>
  <read_first>
    - internal/uiproto/uiv1/ui.proto lines 1-60 and 314-462 (the additive-only header, the service block, and the `reserved`-band convention new messages must not collide with)
    - internal/uiserver/handlers.go lines 25-135 and 720-760 (`openEngine`, `withEngine`, `mapEngineError`, and `GetNodeDetail`'s handler signature and body shape)
    - internal/uiserver/degrade.go (the answer-not-error degrade contract, and why `GetStatus` is the exception rather than the rule)
    - internal/query/node.go lines 29-95 (`resolveSourcePath` — the unexported gate the new exported wrapper delegates to; the wrapper must add no logic of its own)
    - internal/query/detail.go around line 258 (`SourceFor`, documented as "a wrapper over the existing confinement gate, not a second read path" — the exact precedent for how to expose the gate without duplicating it)
    - internal/uiserver/handlers.go around line 200 (how `GetStatus` reads the indexed commit SHA through the Engine's index metadata — the same read `GetPermalink` needs)
    - Taskfile.yml lines 278-350 (`proto:drift`: the floor is 4 generated files and moves ONLY if the generated FILE SET changes; adding messages and one method to an existing proto does not change it)
    - internal/uiserver/server_test.go lines 29-100 and internal/uiserver/handlers_test.go lines 85-120 (fixture and live-client helpers)
  </read_first>
  <behavior>
    - `GetPermalink{path: "pkga/pkga.go", line: 3}` against a GitHub-remote fixture at a pushed commit -> `availability` LINKABLE, `url` ends with `/blob/<sha>/pkga/pkga.go#L3`.
    - The same with `end_line: 9` -> `url` ends with `#L3-L9`.
    - The same with `line` unset -> `url` has no fragment at all.
    - Against a fixture whose commit is in no remote-tracking branch -> LINKABLE_UNVERIFIED, `url` still populated, `reason` names the uncertainty.
    - Against a fixture with a non-GitHub remote -> NO_LINK, `url` empty, `reason` names the host.
    - Against a fixture with no remote -> NO_LINK, `url` empty, `reason` non-empty.
    - Against an index whose `Meta` records no commit SHA -> NO_LINK, `url` empty, `reason` names re-indexing as the remedy.
    - A `path` containing a character that is significant in a URL (`#`, `?`, a space) -> that character is percent-encoded in the path portion of `url`, not emitted raw.
    - `GetPermalink{path: "../outside.txt"}` -> `CodeInvalidArgument`, message contains `escapes the repo root`.
    - `GetPermalink{path: "/etc/passwd"}` -> `CodeInvalidArgument`.
    - POSITIVE CONTROL in the same test: a legitimate in-repo path returns a populated response, proving the refusals discriminate.
  </behavior>
  <action>
(a) Add to `internal/uiproto/uiv1/ui.proto`, following the shape Task 1 froze: the two
new messages with field numbers starting at 1, the availability enum with a zero-valued
UNSPECIFIED member, and `GetPermalink` as a tenth line inside the existing service block.
This is purely additive: no existing field number is renumbered, no existing field is
removed, nothing enters a `reserved` band.

(b) Regenerate with `task proto:gen`, then confirm `task proto:drift` is green AND still
reports 4 compared files. Do not touch the drift floor. It is a floor over the generated
FILE SET, and this change adds no generated file — a floor moved without a file-set
change is a guard silently loosened.

(c) Add `(*query.Engine).ValidateRepoRelativePath(relPath string) error` to
`internal/query/node.go` as a one-line exported wrapper that calls the existing
unexported resolver and returns only its error. It must contain no path logic of its own.
This is the same "wrapper over the existing confinement gate, not a second read path"
pattern `SourceFor` already documents, and it is how `GetPermalink` satisfies SRV-05
without reading a whole file just to validate a path.

(d) Create `internal/uiserver/permalink.go` with a file-level doc comment citing
D-06/D-07/D-08/D-09 by name, mirroring how `degrade.go`, `truncate.go` and
`originguard.go` each open with the decision IDs they implement.

The handler goes through `withEngine`, because it needs the Engine for two things: the
indexed commit SHA from the index metadata (the whole point of BRW-09 is pinning to the
INDEXED commit, not to `HEAD`) and the confinement wrapper from (c). Store-degrade
therefore follows the standard path every non-`GetStatus` RPC uses — a locked store
surfaces as the indexing-in-progress error through `mapEngineError`, consistent with
D-16.

Everything else is an ANSWER, never an error: git absent, no remote, a non-GitHub forge,
an unpushed commit, an absent commit SHA. Each returns a successful response carrying
`NO_LINK` or `LINKABLE_UNVERIFIED` and a populated `reason`. This mirrors `Explore`'s
empty-is-a-successful-response contract: turning an ordinary "no link here" into an error
would make it indistinguishable from a genuine failure.

Percent-encode each path segment when assembling the URL and join with `/`. A file path
containing `#`, `?` or a space would otherwise silently produce a link to a different
place than the one requested.

(e) Write `internal/uiserver/permalink_test.go` covering every case in `<behavior>`,
driven through a real `uiv1connect` client against a real listener using the existing
`startedServer` helper — the boundary, not the function. Build the GitHub-remote and
non-GitHub-remote fixtures by configuring a remote on a git-initialized temp repository;
no network access is required or permitted.

Put the confinement cases and the legitimate-path control in ONE test function sharing
one server, and assert the control FIRST. A refusal-only test would pass against a
service that had broken entirely.
  </action>
  <verify>
    <automated>go test ./internal/uiserver/ -run Permalink -count=1 -v 2>&amp;1 | tee /tmp/pl.txt; test "${PIPESTATUS[0]}" -eq 0 &amp;&amp; test "$(grep -Eco -- '--- PASS: .*Permalink' /tmp/pl.txt)" -ge 1 &amp;&amp; task proto:drift</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/uiserver/ -run Permalink -count=1 -v` exits 0 with at least one `--- PASS:` line naming a permalink test.
    - `task proto:drift` exits 0 and its output reports `compared 4 generated files` — the same count as before this plan.
    - `git diff --stat -- internal/uiproto/uiv1/ui.proto` shows only additions; `rg -o '^\s*reserved' internal/uiproto/uiv1/ui.proto | wc -l` is unchanged from before the edit.
    - `rg -o 'func \(e \*Engine\) ValidateRepoRelativePath' internal/query/node.go | wc -l` returns 1, and the function body contains no call to any path-cleaning, symlink-resolving or prefix-comparing routine — it delegates only.
    - `rg -o 'escapes the repo root' internal/uiserver/permalink_test.go | wc -l` returns at least 1.
    - The percent-encoding case asserts the encoded form is present AND the raw significant character is absent from the path portion, so the absence check is proven to be inspecting a populated URL.
    - `go test ./internal/query/ ./internal/gitmeta/ ./internal/uiserver/ -count=1` exits 0.
    - `task test:unit` and `go vet ./...` both exit 0.
    - RED proof recorded in the SUMMARY for the unpushed-commit case and for the confinement case.
  </acceptance_criteria>
  <done>`GetPermalink` is on the wire additively, answers honestly in all four availability situations, encodes paths safely, and refuses escaping paths through the one existing confinement gate with a passing control alongside.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `GetPermalinkRequest.path` | A second client-steerable filesystem path joins the service; previously `GetNodeDetailRequest.file` was the only one |
| `.git/config` remote URL -> RPC response body | Repository configuration, which commonly carries embedded credentials, crosses into a body any loopback caller can read |
| `git` subprocess -> server | An external binary's output is parsed into a URL that a user will click |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01b | Tampering / Information Disclosure | `GetPermalinkRequest.path` | high | mitigate | Routed through `(*query.Engine).ValidateRepoRelativePath`, a delegating wrapper over the existing `resolveSourcePath` — SRV-05's reuse, not a second implementation; proven at the RPC boundary with escape and absolute-path refusals paired with a legitimate-path control |
| T-03-03 | Information Disclosure | remote URL userinfo in `url` / `reason` | high | mitigate | Userinfo is stripped during parsing before any value is returned; the test asserts the token substring is absent from every returned value while asserting owner and repo WERE extracted, so the absence check is proven non-vacuous |
| T-03-20 | Spoofing | remote host matching | high | mitigate | Exact equality against the literal `github.com`, following SRV-02's exact-match allowlist discipline; a lookalike host such as `github.example.com` is explicitly covered by a test and resolves to no-link |
| T-03-21 | Tampering | URL assembly from a path containing URL-significant characters | medium | mitigate | Each path segment is percent-encoded before joining; asserted both positively (encoded form present) and negatively (raw character absent) |
| T-03-22 | Tampering | `GetPermalink` performing network I/O or repository mutation | high | mitigate | The URL-expansion subcommand is git's documented non-network form; the containment query is local-only; an acceptance criterion asserts no fetch invocation exists in the file; every subprocess is timeout-bounded with stdin detached so git can never block on a prompt |
| T-03-23 | Spoofing | DNS rebinding against the new method | high | accept | `originHostGuard` wraps the whole mux; a method registered on the same Connect handler inherits it by construction, with no new mux entry created |
| T-03-24 | Repudiation | presenting an unverified link as verified | high | mitigate | Three-valued availability with a mandatory `reason`; the unverified state names the uncertainty rather than asserting failure, and a boolean collapse is explicitly prohibited |
</threat_model>

<verification>
- `go test ./internal/gitmeta/ ./internal/query/ ./internal/uiserver/ -count=1` exits 0.
- `task proto:drift` exits 0 and reports `compared 4 generated files`.
- `task test:unit` exits 0; `go vet ./...` exits 0.
- `cd web && pnpm check` exits 0 against the regenerated `web/src/lib/gen/ui_pb.ts`.
- The SUMMARY records the maintainer's answers to all four wire-shape questions and the RED proofs named in each task.
</verification>

<success_criteria>
- The current file and line open on GitHub, pinned to the commit the index was built at.
- Every non-linkable situation says which one it is, in words, and none of them is an error.
- The unpushed case is reported as uncertainty and never as a verdict.
- The new endpoint's path input is confined by the existing gate, proven at the boundary, with no second implementation anywhere.
</success_criteria>

<output>
Create `.planning/phases/03-browse-inspect-navigation/03-05-SUMMARY.md` when done.
</output>
