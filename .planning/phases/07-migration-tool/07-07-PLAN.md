---
phase: 07-migration-tool
plan: 07
type: execute
wave: 5
depends_on: ["07-06"]
files_modified:
  - internal/cli/migrate.go
  - internal/cli/migrate_test.go
  - internal/cli/root.go
autonomous: true
requirements: [MIGR-01, MIGR-02]
must_haves:
  truths:
    - "codegraph migrate [--from] [--to] [--force] [--drop-dangling] is a registered subcommand that delegates to migrate.Run and prints a reconciliation report"
    - "--from defaults to the TS .codegraph/ in cwd (autodetecting the SQLite DB inside); --to defaults to the new-format .codegraph/ in place"
    - "The command is non-destructive to the source and refuses to overwrite a non-empty, unrecognized target without --force"
    - "On success the command prints the count reconciliation and the note that the first sync/index will full-reindex (D-01 accept-and-document)"
    - "A non-TS or unsupported-version source fails loud with a clear message and a non-zero exit; existing init/index/sync/serve/install wiring is unperturbed"
  prohibitions:
    - "The command must NOT mutate or delete the source TS DB (D-08 non-destructive)"
    - "Registration must NOT reorder or drop any existing root.AddCommand entries — migrate is an additive single addition"
    - "A validation failure from migrate.Run must NOT be swallowed — the command returns the error and exits non-zero"
  artifacts:
    - path: internal/cli/migrate.go
      provides: "newMigrateCmd() with --from/--to/--force/--drop-dangling; path resolution + non-destructive refusal; reconciliation report print; ErrNotATSSource surfacing"
      contains: "func newMigrateCmd"
    - path: internal/cli/root.go
      provides: "newMigrateCmd() registered on the root command alongside existing subcommands"
      contains: "newMigrateCmd\\(\\)"
  key_links:
    - from: internal/cli/migrate.go
      to: internal/migrate
      via: "RunE resolves --from/--to then calls migrate.Run(from, to, migrate.Options{Force,DropDangling}) and prints Result.Report"
      pattern: "migrate.Run"
    - from: internal/cli/root.go
      to: internal/cli/migrate.go
      via: "root.AddCommand(..., newMigrateCmd())"
      pattern: "newMigrateCmd\\(\\)"
---

<objective>
Add the `codegraph migrate` cobra command (`internal/cli/migrate.go`) and register it on the root command. The command resolves `--from`/`--to` (with the D-08 defaults + SQLite autodetect), enforces the non-destructive / refuse-unrecognized-target-without-`--force` policy, delegates to `migrate.Run`, and prints the reconciliation report plus the D-01 first-sync note.

Purpose: MIGR-01's single-command surface — the user-facing entry that makes the whole phase a one-step, one-command migration.

Output: `internal/cli/migrate.go` + registration in `root.go` + a command test (registration, flag parsing, TS detection, non-destructive refusal, end-to-end migrate against a fixture).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-migration-tool/07-RESEARCH.md
@.planning/phases/07-migration-tool/07-PATTERNS.md
@internal/cli/sync.go
@internal/cli/index.go
@internal/cli/init.go
@internal/cli/root.go
@internal/migrate/migrate.go
@internal/migrate/migratetest/fixture.go
</context>

<artifacts_produced>
- `internal/cli/migrate.go`: `func newMigrateCmd() *cobra.Command`, plus any local path-resolution/report-printing helpers.
- `internal/cli/root.go`: `newMigrateCmd()` added to the `root.AddCommand(...)` call.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: newMigrateCmd — flags, path resolution, non-destructive policy, report</name>
  <files>internal/cli/migrate.go, internal/cli/migrate_test.go</files>
  <read_first>
    @internal/cli/sync.go (newSyncCmd structure: targetRoot, codegraphDir stat, delegate, printSyncSummary — the cobra pattern to mirror)
    @internal/cli/index.go (--force + confirm(cmd, prompt) non-destructive pattern)
    @internal/cli/init.go (codegraphDirName/storeDirName consts; targetRoot; confirm)
    @internal/cli/root.go (ErrNotInitialized sentinel shape; the AddCommand registration list — do not perturb existing entries)
    @internal/migrate/migrate.go (Run(from, to, Options) signature + Result/Report from 07-06; ErrNotATSSource/ErrUnsupportedSchemaVersion from reader.go)
    @.planning/phases/07-migration-tool/07-CONTEXT.md (D-08 command surface + non-destructive rules; D-01 first-sync documentation requirement)
  </read_first>
  <action>
    Create `internal/cli/migrate.go` (package `cli`). Implement `newMigrateCmd() *cobra.Command` with `Use: "migrate"`, `Args: cobra.NoArgs`, a Short/Long describing the one-way TS→new-format conversion AND the D-01 note that the first `sync`/`index` after migrating performs a full re-index (TS ids differ from native ids). Flags: `--from` (StringVar, default "" → TS `.codegraph/` in cwd, autodetect the DB via migrate/reader FindDBFile semantics through Run), `--to` (StringVar, default "" → new-format `.codegraph/` in place under cwd), `--force`/`-f` (BoolVarP: overwrite a non-empty, unrecognized target without confirming), `--drop-dangling` (BoolVar: drop dangling edges instead of failing loud, D-09.2). In RunE: resolve the effective from/to paths (default to `targetRoot(nil)`-based cwd `.codegraph/` when the flags are empty, mirroring sync/index). Before running, if the resolved target `.codegraph/` exists and is non-empty and is NOT recognizably a prior migration, and `!force`, call `confirm(cmd, prompt)`; on decline print an aborted message (mentioning `--force`) and return nil. Delegate to `migrate.Run(from, to, migrate.Options{Force: force, DropDangling: dropDangling})`. On error, surface it (return it — cobra SilenceErrors means main prints it; a non-TS source / unsupported version / dangling failure exits non-zero). On success, print the reconciliation report from `Result.Report` (per-table source/migrated counts, any dropped-dangling count) and the D-01 first-sync note to `cmd.OutOrStdout()`. Wrap detection errors so `ErrNotATSSource` reads clearly (e.g. "not a TypeScript CodeGraph index"). Add `migrate_test.go` covering: the command is registered and parses all four flags; running against a cwd containing a BuildTSIndex-materialized TS `.codegraph/` succeeds and the target opens as a healthy new-format store; a non-TS directory fails loud; a non-empty unrecognized target without `--force` refuses (no write).
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'TestMigrate' -count=1</automated>
  </verify>
  <done>codegraph migrate runs end-to-end against a fixture TS index, refuses unrecognized targets without --force, fails loud on non-TS sources, and prints the reconciliation + first-sync note.</done>
  <acceptance_criteria>
    - `rg -n 'func newMigrateCmd' internal/cli/migrate.go` matches
    - `rg -n '"from"|"to"|"force"|"drop-dangling"' internal/cli/migrate.go` matches all four flags
    - `rg -n 'migrate.Run' internal/cli/migrate.go` matches (delegation)
    - `go test ./internal/cli/ -run 'TestMigrate' -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register migrate on the root command</name>
  <files>internal/cli/root.go</files>
  <read_first>
    @internal/cli/root.go (newRootCmd's single root.AddCommand(...) call listing every subcommand — add newMigrateCmd() without reordering or dropping any existing entry)
  </read_first>
  <action>
    In `internal/cli/root.go`, add `newMigrateCmd()` to the existing `root.AddCommand(...)` argument list (a single additive entry, alongside newSyncCmd/newInstallCmd/etc.), leaving every other registration untouched. Update the package/newRootCmd doc comment minimally if it enumerates commands. Confirm `codegraph migrate --help` is reachable and `codegraph` still lists all prior commands.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -count=1 && go build ./...</automated>
  </verify>
  <done>migrate is a first-class subcommand; all existing commands remain registered; the binary builds.</done>
  <acceptance_criteria>
    - `rg -n 'newMigrateCmd\(\)' internal/cli/root.go` matches inside root.AddCommand
    - `rg -c 'newInstallCmd\(\)|newSyncCmd\(\)|newServeCmd\(\)' internal/cli/root.go` still matches (no existing registration dropped)
    - `go build ./...` exits 0 and `go test ./internal/cli/ -count=1` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → migration | User-supplied --from/--to paths and flags drive a filesystem-replacing operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07-01 | Tampering | Overwriting a non-empty, unrecognized target `.codegraph/` and destroying existing data | high | mitigate | The command refuses to overwrite a non-empty, unrecognized target without `--force` (confirm() prompt otherwise); the underlying temp+swap (07-06) means even a forced run only replaces on validated success. |
| T-07-07-02 | Tampering | Mutating/deleting the source TS index | high | mitigate | migrate.Run opens the source read-only and never writes it (D-08); the CLI passes paths only, performs no source writes. |
| T-07-07-03 | Denial of Service | A dangling/corrupt source silently producing a broken graph reported as success | medium | mitigate | migrate.Run's validation fails loud by default; the command returns the error and exits non-zero; --drop-dangling is an explicit opt-in surfaced in the report. |
</threat_model>

<verification>
- `go test ./internal/cli/ -count=1` green (registration, flag parse, end-to-end migrate, non-destructive refusal, non-TS fail-loud).
- `go build ./...` green; `codegraph migrate --help` reachable; all prior subcommands still registered.
</verification>

<success_criteria>
- `codegraph migrate` converts a TS index in one command with sensible defaults, non-destructive to the source, refusing unrecognized targets without --force.
- Success prints the reconciliation report + the first-sync full-reindex note (D-01); failures exit non-zero; existing CLI surface unperturbed.
</success_criteria>

<output>
Create `.planning/phases/07-migration-tool/07-07-SUMMARY.md` when done.
</output>
