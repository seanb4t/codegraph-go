---
phase: 05-git-sync-hooks
plan: 03
type: tdd
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - internal/githooks/githooks.go
  - internal/githooks/githooks_test.go
autonomous: true
requirements: [HOOK-01, HOOK-02]

must_haves:
  truths:
    - "githooks.Install writes each of post-commit/post-merge/post-checkout with the verbatim marker block; a fresh file is seeded `#!/bin/sh` + block + trailing newline, mode 0755"
    - "The marker block is byte-for-byte identical to TS's markerBlock() — begin marker `# >>> codegraph sync hook >>>`, the 3 comment lines, the `command -v codegraph` guard wrapping `( codegraph sync >/dev/null 2>&1 & ) >/dev/null 2>&1`, and end marker `# <<< codegraph sync hook <<<`"
    - "Installing over an existing user hook strips any prior codegraph block, trims trailing whitespace, and appends the block after a single blank-line separator, preserving user content byte-for-byte"
    - "Re-installing when a marker block already exists strips the prior block wherever it sits and re-appends the current block at end-of-file, never duplicating it; user content adjacent to the block is preserved with a single blank-line separator"
    - "Installing into an absent or effectively-empty hook file produces the `#!/bin/sh`-seeded form; a base that is empty after stripping produces the same seeded form"
    - "The three hooks are always processed in the fixed order post-commit, post-merge, post-checkout; InstallResult.Installed and RemoveResult.Removed list them deterministically in that order"
    - "Running install twice on an unmodified file is byte-identical; running remove twice is a no-op on the second run (file already deleted or block already absent) and never errors"
    - "githooks.Remove strips only codegraph's block; if the stripped remainder is effectively empty (every line blank or `#!`-prefixed) the file is deleted, otherwise the trimmed remainder + newline is written and re-chmod'd"
    - "githooks.Status reports per-hook installed state (a hook file exists and contains the begin marker); a TS-installed block (verbatim TS bytes) is detected as installed and is removable"
    - "githooks.Install / Remove / Status in a non-repo return a Skipped:\"not a git repository\" result (never an error)"
    - statement: "An interrupted githooks Install/Remove never leaves a truncated hook file — every write commits via fsatomic.WriteFile temp-file + os.Rename so only whole-file content is observable at the target; concurrent same-repo writers are outside the designed single-user local-CLI model"
      verification: backstop
  artifacts:
    - internal/githooks/githooks.go
    - internal/githooks/githooks_test.go
  key_links:
    - "internal/githooks.Install/Remove -> internal/fsatomic.WriteFile (every hook-file write, D-05)"
    - "internal/githooks -> gitmeta.HooksDir (hooks-dir resolution, never hand-joined)"
  prohibitions:
    - statement: "Do NOT reuse internal/agents' replaceOrAppendMarkedSection / removeMarkedSection — hook splice semantics differ (trimmed-line `#` markers, always-re-append-at-end, delete-when-effectively-empty, shebang-seeding); port TS git-hooks.js instead (D-02/D-09)"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT implement install as in-place block replacement — TS strips the block wherever it is and re-appends at end-of-file; an in-place 'simplification' silently diverges from TS-installed hooks (Pitfall 2, D-02)"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT alter the marker constants or the 7 inner block lines, and do NOT add a `githooks remove` mention to the block — verbatim TS bytes are a detection key and a cross-tool compatibility contract (Pitfall 1, D-03)"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT deviate from the exact subshell-backgrounding snippet `( codegraph sync >/dev/null 2>&1 & ) >/dev/null 2>&1` — a single `&` can let git block on the backgrounded job (Pitfall 5, D-03)"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT interpolate any runtime value (project path, username, env var) into the shell block — it is a fixed constant string with zero interpolation (command-injection prevention, V5)"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT treat os.Chmod failure as fatal — discard it explicitly (`_ = os.Chmod(...)`), matching TS's swallowed chmod (Pitfall 4)"
      status: flagged
      verification: flagged-unverified
---

<objective>
Port TS sync/git-hooks.js byte-for-byte into a new internal/githooks package (D-01/D-02): the verbatim marker constants + block (D-03), the trimmed-line strip / effectively-empty primitives, and Install / Remove / Status with Go-idiomatic result types. Every write funnels through fsatomic.WriteFile (D-05); the hooks dir comes from gitmeta.HooksDir (never hand-joined). This is the HOOK-01/HOOK-02 behavioral core.

Purpose: A drop-in-compatible hook manager — hooks installed by TS CodeGraph are detected, replaced, and removed seamlessly by the Go binary because the markers and block are byte-identical.
Output: internal/githooks/githooks.go (markers, markerBlock, stripMarkerBlock, isEffectivelyEmpty, Install, Remove, Status, result types) + real-git fixture tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-git-sync-hooks/05-CONTEXT.md
@.planning/phases/05-git-sync-hooks/05-RESEARCH.md
@.planning/phases/05-git-sync-hooks/05-PATTERNS.md
</context>

## Artifacts this phase produces

This plan creates these NEW symbols (excluded from drift/source-grounding checks):
- Package `internal/githooks`
- Constants `markerBegin`, `markerEnd`; var `defaultSyncHooks`
- `markerBlock() string`, `stripMarkerBlock(content string) string`, `isEffectivelyEmpty(content string) bool`
- `githooks.Install(ctx, projectRoot) InstallResult`, `githooks.Remove(ctx, projectRoot) RemoveResult`, `githooks.Status(ctx, projectRoot) StatusResult`
- Types `InstallResult`, `RemoveResult`, `StatusResult`, `HookStatus`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verbatim marker constants/block + strip/effectively-empty primitives</name>
  <files>internal/githooks/githooks.go, internal/githooks/githooks_test.go</files>
  <read_first>
    - .planning/phases/05-git-sync-hooks/05-RESEARCH.md ("Verbatim marker constants and block", "DEFAULT_SYNC_HOOKS", "Pattern 2: Trimmed-line marker matching", "Pattern 3: Effectively-empty detection" — the byte-exact source)
    - .planning/phases/05-git-sync-hooks/05-PATTERNS.md ("internal/githooks/githooks.go" — the ready-to-copy markerBlock/stripMarkerBlock/isEffectivelyEmpty and constants)
    - .planning/phases/05-git-sync-hooks/05-CONTEXT.md (D-02, D-03 — the locked verbatim-bytes contract)
  </read_first>
  <behavior>
    - markerBlock() returns exactly 8 lines joined by "\n": markerBegin, 3 comment lines (the "Keeps the CodeGraph index fresh…", "(e.g. WSL2 /mnt drives)…", "Managed by codegraph; remove with `codegraph uninit`…" lines), `if command -v codegraph >/dev/null 2>&1; then`, `  ( codegraph sync >/dev/null 2>&1 & ) >/dev/null 2>&1`, `fi`, markerEnd.
    - stripMarkerBlock removes lines between (and including) a begin/end marker matched on the TRIMMED line, so an indented marker is still stripped; content outside the block is preserved verbatim including blank lines.
    - stripMarkerBlock on content with no markers returns it unchanged.
    - isEffectivelyEmpty returns true when every trimmed line is empty or starts with `#!`, false otherwise.
  </behavior>
  <action>
    Create internal/githooks/githooks.go (package githooks) with the marker constants `markerBegin = "# >>> codegraph sync hook >>>"` and `markerEnd = "# <<< codegraph sync hook <<<"`, `var defaultSyncHooks = []string{"post-commit", "post-merge", "post-checkout"}`, and `markerBlock()` returning the 8-line block via strings.Join — copy the exact bytes from 05-RESEARCH.md's "Verbatim marker constants and block" example / 05-PATTERNS.md's markerBlock; do NOT hand-retype. Add `stripMarkerBlock(content string) string` (split on "\n", strings.TrimSpace(line) compared to the markers, keep non-block lines, re-join) and `isEffectivelyEmpty(content string) bool` (every trimmed line == "" or HasPrefix "#!"). Imports so far: strings. Write githooks_test.go table tests for markerBlock exact-string equality (assert the produced string contains the begin marker, the guard line, the exact backgrounding snippet, and the end marker — and equals the fixture reproduction), stripMarkerBlock (indented-marker stripping, no-marker passthrough, surrounding-content preservation), and isEffectivelyEmpty (blank+shebang → true, real content → false).
  </action>
  <verify>
    <automated>go test ./internal/githooks/... -run 'TestMarkerBlock|TestStripMarkerBlock|TestIsEffectivelyEmpty' -v</automated>
  </verify>
  <done>Marker constants + markerBlock (verbatim bytes) + stripMarkerBlock (trimmed-line) + isEffectivelyEmpty exist and pass their unit tests.</done>
  <acceptance_criteria>
    - Source: markerBegin/markerEnd equal the exact TS strings; markerBlock produces the 8-line block including `( codegraph sync >/dev/null 2>&1 & ) >/dev/null 2>&1`.
    - Behavior: a test asserts an indented begin/end marker is still stripped (trimmed-line matching).
    - Behavior: isEffectivelyEmpty returns true for a blank+`#!/bin/sh`-only remainder and false when a real command line remains.
    - Command: `go test ./internal/githooks/... -run 'TestMarkerBlock|TestStripMarkerBlock|TestIsEffectivelyEmpty' -v` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install (strip-then-append-at-end) + result types</name>
  <files>internal/githooks/githooks.go, internal/githooks/githooks_test.go</files>
  <read_first>
    - internal/githooks/githooks.go (the primitives from Task 1)
    - .planning/phases/05-git-sync-hooks/05-RESEARCH.md ("Pattern 1: Marker-fenced strip-then-append", the verbatim installGitSyncHook TS + illustrative Go port)
    - .planning/phases/05-git-sync-hooks/05-PATTERNS.md ("Install pattern" ready-to-copy Go, the result-type structs)
    - internal/gitmeta/githooks.go (HooksDir signature from 05-02) and internal/fsatomic/fsatomic.go (WriteFile signature from 05-01)
    - internal/gitmeta/fixtures_test.go (runGit/initRepo — reuse pattern; note githooks is a DIFFERENT package so define local test helpers or a small copy)
  </read_first>
  <behavior>
    - Install into a fresh repo with no pre-existing hook files: each of the 3 hook files exists afterward, content == `#!/bin/sh\n` + markerBlock() + `\n`, mode 0755; InstallResult.Installed == [post-commit, post-merge, post-checkout] in order.
    - Install over an existing user hook (e.g. content `#!/bin/sh\necho hi\n`): the user line is preserved, the block is appended after exactly one blank-line separator (`...echo hi\n\n` + block + `\n`).
    - Re-install on an unmodified installed file is byte-identical (idempotent).
    - Install after a prior/older block present: the old block is stripped and the current block re-appended at end, with no duplicate begin markers in the file.
    - Install in a non-repo (HooksDir == "") returns InstallResult{Skipped: "not a git repository"}, writes nothing.
  </behavior>
  <action>
    Add result types `InstallResult{Installed []string; HooksDir string; Skipped string}`, `RemoveResult{Removed []string; HooksDir string; Skipped string}`, `HookStatus{Name string; Installed bool}`, `StatusResult{Hooks []HookStatus; HooksDir string; Skipped string}` and `Install(ctx context.Context, projectRoot string) InstallResult` to internal/githooks/githooks.go, porting installGitSyncHook exactly (05-PATTERNS.md "Install pattern"): resolve gitmeta.HooksDir; if "" return Skipped "not a git repository"; os.MkdirAll(hooksDir, 0o755) (on error return Skipped "could not access the git hooks directory"); for each hook in defaultSyncHooks, read existing (if any) → base = strings.TrimRight(stripMarkerBlock(existing), " \t\n") → content = base != "" ? base+"\n\n"+block+"\n" : "#!/bin/sh\n"+block+"\n"; write via fsatomic.WriteFile; `_ = os.Chmod(file, 0o755)` best-effort; append hook to Installed. Add imports context, os, path/filepath, and the internal/fsatomic + internal/gitmeta packages. Write githooks_test.go install cases (fresh, over-user-content, idempotent re-install, prior-block replacement, non-repo skip) using a real-git fixture in t.TempDir(). Add an explanatory test comment on the prior-block/re-install case noting strip-then-append-at-end is deliberate TS parity, NOT a bug to "simplify" to in-place (per 05-CONTEXT.md Specific Ideas).
  </action>
  <verify>
    <automated>go test ./internal/githooks/... -run 'TestInstall' -v</automated>
  </verify>
  <done>githooks.Install ports installGitSyncHook exactly; fresh/over-user/idempotent/prior-block/non-repo cases all pass; every write goes through fsatomic.WriteFile and mode is 0755.</done>
  <acceptance_criteria>
    - Source: Install resolves the hooks dir via gitmeta.HooksDir and writes via fsatomic.WriteFile (no os.WriteFile, no exec.Command, no hand-joined .git/hooks).
    - Behavior: fresh-install file equals `#!/bin/sh\n` + block + `\n` with mode 0755; over-user-content preserves the user line and appends after one blank line.
    - Behavior: a second identical Install produces byte-identical files (idempotency test green).
    - Behavior: install-after-prior-block leaves exactly one begin marker in the file.
    - Command: `go test ./internal/githooks/... -run 'TestInstall' -v` exits 0 (or skips when git absent).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove (delete-when-empty) + Status (per-hook, TS-block detection)</name>
  <files>internal/githooks/githooks.go, internal/githooks/githooks_test.go</files>
  <read_first>
    - internal/githooks/githooks.go (Install + primitives + result types from Tasks 1-2)
    - .planning/phases/05-git-sync-hooks/05-RESEARCH.md ("Verbatim removeGitSyncHook", "Verbatim isSyncHookInstalled", and the result-naming note: use Removed, not the TS `{installed: removed}` quirk)
    - .planning/phases/05-git-sync-hooks/05-PATTERNS.md ("Remove pattern", "Status pattern" ready-to-copy Go)
    - .planning/phases/05-git-sync-hooks/05-CONTEXT.md (D-02 remove semantics, D-05 os.Remove for deletion, D-12 required TS-installed-block fixture)
  </read_first>
  <behavior>
    - Remove over a hook with user content: only codegraph's block is stripped, remainder written as strings.TrimRight(stripped, " \t\n")+"\n", re-chmod'd 0755; user bytes preserved.
    - Remove when the stripped remainder is effectively empty (only shebang/blank): the file is deleted (os.Remove).
    - Remove when a hook was never installed (no begin marker, or file absent): that hook is skipped, no error; RemoveResult.Removed does not include it.
    - Remove twice: second run is a no-op (files already gone/clean), no error.
    - Status reports per-hook Installed = (file exists AND contains markerBegin); a hook file containing the VERBATIM TS block bytes reports Installed=true and is subsequently removable by Remove.
    - Remove/Status in a non-repo return Skipped "not a git repository".
  </behavior>
  <action>
    Add `Remove(ctx context.Context, projectRoot string) RemoveResult` and `Status(ctx context.Context, projectRoot string) StatusResult` to internal/githooks/githooks.go, porting removeGitSyncHook and isSyncHookInstalled (05-PATTERNS.md "Remove pattern"/"Status pattern"): Remove resolves HooksDir (skip if ""); for each hook, read the file (continue on read error), continue if !strings.Contains(original, markerBegin), else stripped = stripMarkerBlock(original); if isEffectivelyEmpty(stripped) → os.Remove(file) (plain removal, D-05); else write strings.TrimRight(stripped," \t\n")+"\n" via fsatomic.WriteFile and `_ = os.Chmod(file, 0o755)`; append to Removed. Status resolves HooksDir (skip if ""); for each hook build HookStatus{Name, Installed: file exists && strings.Contains(content, markerBegin)}. Use the Go-idiomatic field name Removed (NOT TS's `{installed: removed}`). Add tests: remove-with-user-content (byte-preserved remainder), remove-effectively-empty (file deleted), remove-never-installed (untouched, no error), remove-twice no-op, and the D-12 TS-installed-block fixture — paste the verbatim TS block bytes into a hook file, assert Status detects it Installed=true, then Remove strips it. Also assert Status returns per-hook lines for a mixed installed/not-installed state.
  </action>
  <verify>
    <automated>go test ./internal/githooks/... -run 'TestRemove|TestStatus' -v</automated>
  </verify>
  <done>githooks.Remove and githooks.Status port TS exactly; user content preserved, effectively-empty deletion, never-installed no-op, and the TS-installed-block detection/removal fixture all pass.</done>
  <acceptance_criteria>
    - Source: Remove uses os.Remove for deletion and fsatomic.WriteFile for rewrites; Status detects via strings.Contains(content, markerBegin).
    - Behavior: remove-effectively-empty deletes the file; remove-with-user-content preserves the user bytes.
    - Behavior: a fixture with the verbatim TS block is detected by Status (Installed=true) and removed by Remove.
    - Behavior: RemoveResult uses field `Removed` (not `Installed`).
    - Command: `go test ./internal/githooks/... -run 'TestRemove|TestStatus' -v` exits 0 (or skips).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| githooks package → git hooks dir | writes 3 fixed-named script files into the resolved hooks dir; the shell content is a fixed constant, the target dir comes from gitmeta.HooksDir |
| existing hook file → strip/splice | pre-existing hook content (possibly TS-written, possibly user-authored) is parsed for markers and re-spliced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | markerBlock() shell content (command injection) | high | mitigate | the block is a fixed constant string with ZERO interpolation of any runtime value (no project path, username, or env var); copied byte-for-byte; a test asserts the exact block content |
| T-05-05 | Tampering | in-place-edit divergence corrupting a TS-installed hook | low | mitigate | strip-then-append-at-end parity + a verbatim-TS-block fixture test proving detect/replace/remove interop |
| T-05-04 | Tampering | partial/corrupt hook file on crash | low | mitigate | every write goes through fsatomic.WriteFile (temp+rename); deletion is a single os.Remove |
| T-05-01b | Elevation of Privilege | writing to an attacker-controlled path via hook name | low | mitigate | hook names are 3 fixed constants; write target is always filepath.Join(HooksDir, fixedName); HooksDir degrades to "" (skip) on any probe failure |

_No package installs in this plan (stdlib + internal packages only) — no supply-chain (T-05-SC) checkpoint applies._
</threat_model>

<verification>
- `go test ./internal/githooks/... -v` — full package: primitives, Install, Remove, Status, TS-block interop.
- `go test ./internal/fsatomic/... ./internal/gitmeta/...` — dependency packages still green.
- `go build ./...` — compiles.
- Content-level tests only; no hook-execution end-to-end (D-14) — the never-blocks property is by construction (subshell backgrounding).
</verification>

<success_criteria>
- internal/githooks exposes Install/Remove/Status with Go-idiomatic result types.
- Marker constants + block are verbatim TS bytes; a TS-installed hook is detected and managed.
- Install is strip-then-append-at-end (idempotent, no duplication); Remove preserves user content and deletes when effectively empty; non-repo → clean Skipped.
- Every write is atomic; deletion is os.Remove; chmod is best-effort.
</success_criteria>

<output>
Create `.planning/phases/05-git-sync-hooks/05-03-SUMMARY.md` when done.
</output>
