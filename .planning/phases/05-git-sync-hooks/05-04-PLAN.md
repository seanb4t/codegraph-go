---
phase: 05-git-sync-hooks
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - internal/cli/githooks.go
  - internal/cli/githooks_test.go
  - internal/cli/root.go
autonomous: true
requirements: [HOOK-01, HOOK-02]

must_haves:
  truths:
    - "`codegraph githooks install [path]` installs the 3 sync hooks and reports the installed hooks; run end-to-end through the real cobra command tree against a real-git fixture repo"
    - "`codegraph githooks remove [path]` strips codegraph's block from the installed hooks and reports what was removed"
    - "`codegraph githooks status [path]` prints per-hook install state plus the hooks-dir path and exits 0 whether or not hooks are installed"
    - "`githooks install` in a non-git directory prints a friendly `not a git repository` skip message and exits 0 (never a non-zero error)"
    - "The `[path]` argument is resolved via the existing targetRoot helper (cobra.MaximumNArgs(1)), matching init/uninit/sync"
    - "newGithooksCmd() with its 3 subcommands is registered in root.go's AddCommand list without reordering the existing entries"
  artifacts:
    - internal/cli/githooks.go
    - internal/cli/githooks_test.go
  key_links:
    - "internal/cli/githooks.go -> internal/githooks.Install/Remove/Status"
    - "internal/cli/root.go newRootCmd AddCommand -> newGithooksCmd()"
  prohibitions:
    - statement: "Do NOT add hook-selection flags in v1.0 — the hook trio is the fixed DEFAULT_SYNC_HOOKS set (D-11)"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT reimplement path resolution — reuse the package-level targetRoot (init.go:81-86); the [path] arg is the only user input and it flows through filepath.Abs (V5 path-traversal mitigation)"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT assert hook EXECUTION end-to-end (spawning git commits and racing a backgrounded sync) — content/reachability tests only in v1.0 (D-14)"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT reorder or drop existing root.go AddCommand entries — append newGithooksCmd() only"
      status: flagged
      verification: flagged-unverified
---

<objective>
Add the `codegraph githooks` command tree — `install` / `remove` / `status`, each taking an optional `[path]` (D-11) — as a Go-only surface extension (D-01) over the internal/githooks core, and register it in root.go. Drive the real cobra tree with mutation-proof reachability tests against real-git fixtures (D-12/D-13).

Purpose: The user-facing CLI surface for HOOK-01/HOOK-02; the command shapes mirror init/uninit/sync so nothing new is invented.
Output: internal/cli/githooks.go (newGithooksCmd + 3 subcommands + output formatting), its reachability test, and the root.go registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-git-sync-hooks/05-CONTEXT.md
@.planning/phases/05-git-sync-hooks/05-RESEARCH.md
@.planning/phases/05-git-sync-hooks/05-PATTERNS.md
@internal/cli/uninit.go
@internal/cli/root.go
</context>

## Artifacts this phase produces

This plan creates these NEW symbols (excluded from drift/source-grounding checks):
- `newGithooksCmd() *cobra.Command` and its 3 subcommand builders (install/remove/status) in internal/cli/githooks.go
- A new `codegraph githooks` command surface (documented as a Go-only extension per D-01, to be recorded in Phase 8 SURF-05)

<tasks>

<task type="auto">
  <name>Task 1: githooks command tree + output formatting + root registration</name>
  <files>internal/cli/githooks.go, internal/cli/root.go</files>
  <read_first>
    - internal/cli/uninit.go (the single-purpose subcommand shape: targetRoot reuse, cobra.MaximumNArgs(1), RunE closure, cmd.OutOrStdout() output, plural helper usage)
    - internal/cli/init.go (targetRoot at lines 81-86)
    - internal/cli/root.go (the AddCommand list at lines 45-50 — append target)
    - internal/githooks/githooks.go (Install/Remove/Status signatures + result types from 05-03)
    - .planning/phases/05-git-sync-hooks/05-PATTERNS.md ("internal/cli/githooks.go" — command-tree shape + root wiring)
    - .planning/phases/05-git-sync-hooks/05-CONTEXT.md (D-11 command shapes and success/skip wording)
  </read_first>
  <action>
    Create internal/cli/githooks.go (package cli) with `newGithooksCmd()` returning a parent `githooks` cobra.Command (Short: "Manage git sync hooks (post-commit/post-merge/post-checkout)") that AddCommand's newGithooksInstallCmd/newGithooksRemoveCmd/newGithooksStatusCmd. Each subcommand uses Use "install [path]" / "remove [path]" / "status [path]", Args: cobra.MaximumNArgs(1), and a RunE that resolves root via the package targetRoot(args), calls the matching githooks function with cmd.Context(), and writes to cmd.OutOrStdout(). Output wording (D-11): install success mirrors TS — `Installed git post-commit, post-merge, post-checkout hooks — the index refreshes in the background after each.` (join result.Installed), and when result.Skipped != "" print a friendly one-line skip (e.g. `Skipped: <skipped>`) and return nil (exit 0); remove reports `Removed git <joined> sync hook(s)` or a nothing-to-remove line; status prints a `hooks dir: <HooksDir>` line then one `<hook>: installed|not installed` line per HookStatus, exit 0 regardless (Open Question 2 shape — Claude's discretion). Register newGithooksCmd() by appending it to root.go's existing AddCommand call (do not reorder existing entries).
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/cli/...</automated>
  </verify>
  <done>internal/cli/githooks.go defines newGithooksCmd + 3 subcommands wired to githooks.Install/Remove/Status; root.go registers it; module builds and vets clean.</done>
  <acceptance_criteria>
    - Source: newGithooksCmd + newGithooksInstallCmd/newGithooksRemoveCmd/newGithooksStatusCmd exist; each subcommand has Args cobra.MaximumNArgs(1) and calls targetRoot(args).
    - Source: root.go's AddCommand list includes newGithooksCmd() and retains all prior entries (newInitCmd … newMigrateCmd).
    - Source: install RunE returns nil (not an error) when result.Skipped != "".
    - Command: `go build ./...` and `go vet ./internal/cli/...` exit 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mutation-proof CLI reachability tests (real cobra tree, real-git fixtures)</name>
  <files>internal/cli/githooks_test.go</files>
  <read_first>
    - internal/cli/githooks.go (the command tree from Task 1)
    - internal/cli/install_test.go (the execCmd real-cobra-tree driving pattern) and internal/cli/cli_test.go (execCmd helper)
    - internal/gitmeta/fixtures_test.go (runGit/initRepo — mirror for a real-git fixture dir passed as the [path] arg; githooks_test is package cli so replicate the minimal git-init helper)
    - .planning/phases/05-git-sync-hooks/05-RESEARCH.md ("Existing CLI reachability test pattern" and D-13 mutation-proof requirement)
  </read_first>
  <action>
    Write internal/cli/githooks_test.go driving the real cobra command tree via execCmd (D-13: not the githooks package functions directly). Build a real-git fixture repo in t.TempDir() (init + one commit, mirroring gitmeta's runGit/initRepo; t.Skip when git absent) and pass its path as the [path] arg. Cases: (1) `githooks install <repo>` succeeds and stdout names the installed hooks, and the on-disk post-commit file contains markerBegin with mode 0755; (2) `githooks status <repo>` after install reports the 3 hooks installed and prints the hooks-dir line, exit 0; (3) `githooks remove <repo>` reports removal and the hook files no longer contain markerBegin; (4) `githooks install <non-git-tempdir>` prints the friendly `not a git repository` skip and returns a nil error (exit 0). Assert on returned err == nil and captured stdout.
  </action>
  <verify>
    <automated>go test ./internal/cli/... -run 'TestGithooks' -v</automated>
  </verify>
  <done>Reachability tests drive `githooks install/status/remove` end-to-end through cobra against a real-git fixture and prove the non-repo skip exits 0; reverting the root.go registration or a subcommand wiring turns these red.</done>
  <acceptance_criteria>
    - Behavior: `githooks install <repo>` via execCmd returns nil err and the post-commit file on disk contains the begin marker with mode 0755.
    - Behavior: `githooks status <repo>` post-install shows all 3 hooks installed and a hooks-dir line, exit 0.
    - Behavior: `githooks install <non-git dir>` returns nil err and prints a `not a git repository` skip message (exit 0).
    - Command: `go test ./internal/cli/... -run 'TestGithooks' -v` exits 0 (or skips cleanly when git absent).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg [path] → filesystem | the optional [path] positional is the only user-supplied input; it resolves through targetRoot → filepath.Abs before reaching githooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | path traversal via [path] arg | low | mitigate | reuses the existing targetRoot(args) helper (filepath.Abs) shared with init/uninit/sync; no new path-handling code; the hook names remain fixed constants inside githooks |
| T-05-03b | Denial of Service | git subprocess hang during a command | low | mitigate | inherited gitmeta 5s timeout + cmd.Stdin nil; command degrades to a skip message, never hangs |

_No package installs in this plan (stdlib + cobra already vendored) — no supply-chain (T-05-SC) checkpoint applies._
</threat_model>

<verification>
- `go test ./internal/cli/... -run 'TestGithooks' -v` — real-cobra reachability + non-repo skip exit 0.
- `go build ./... && go vet ./...` — command tree compiles and vets.
- No hook-execution assertions (D-14).
</verification>

<success_criteria>
- `codegraph githooks install|remove|status [path]` exist, registered in root.go, mirroring init/uninit shape.
- Reachability tests drive the real cobra tree; non-repo install exits 0 with a friendly message.
- No hook-selection flags; targetRoot reused.
</success_criteria>

<output>
Create `.planning/phases/05-git-sync-hooks/05-04-SUMMARY.md` when done.
</output>
