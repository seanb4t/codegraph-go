---
phase: 10-index-health-the-coverage-denominator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/schema/graph.proto
  - internal/schema/graph.pb.go
  - internal/schema/exclusion.go
  - internal/schema/exclusion_test.go
  - internal/schema/meta_commit_test.go
  - internal/graphstore/keys.go
  - internal/graphstore/batch.go
  - internal/graphstore/pebble_store.go
  - internal/graphstore/store.go
  - internal/graphstore/excludedfile_test.go
  - internal/query/scoring_test.go
  - internal/query/traverse_test.go
  - internal/query/seeding_test.go
  - internal/query/search_test.go
  - internal/query/expand_test.go
  - internal/query/gather_test.go
  - internal/query/files_status_test.go
  - internal/indexer/discover.go
  - internal/indexer/discoverexclusion.go
  - internal/indexer/discoverexclusion_test.go
  - internal/indexer/pipeline.go
  - internal/indexer/pipeline_test.go
  - internal/indexer/resolve.go
  - internal/indexer/resolve_test.go
  - internal/indexer/sync.go
  - internal/uiproto/uiv1/ui.proto
  - internal/uiproto/uiv1/ui.pb.go
  - internal/uiproto/uiv1/uiv1connect/ui.connect.go
  - web/src/lib/gen/ui_pb.ts
  - web/build/.build-manifest
  - web/build/_app/**
  - web/build/index.html
  - internal/uiserver/readonly_test.go
  - internal/uiserver/coverage.go
  - internal/uiserver/coverage_test.go
  - internal/uiserver/handlers.go
  - internal/query/coverage.go
  - internal/query/coverage_test.go
autonomous: true
requirements: [HLT-04, HLT-05, HLT-06]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "After `indexer.Run` on a root containing one `//go:build ignore` Go file, the store holds exactly one `c/`-namespace ExcludedFile record for that path with reason EXCLUSION_REASON_BUILD_TAG, committed in the SAME graphstore.Writer batch as the File records (D-05, D-07) — `internal/indexer/resolve.go` contains exactly one `NewWriter(` call site"
    - "`Meta.has_coverage = 9` is stamped true by writeGraph on every from-scratch run; a store whose Meta lacks the field reads back `CoverageSummary{Known:false}` with every count zero and a nil by-reason map — never an error, never a claimed 0/0 (D-06, D-15)"
    - "`GetHealthResponse.coverage` is field 17 (D-09) and `GetCoverage` is UIService's 16th rpc (D-10); `wantUIServiceMethods` has 16 members asserted set-equal in both directions with the count from both sides, and a table test rejects the decoy `GetIndexCoverage` while accepting `GetCoverage` (D-16)"
    - "The UI-local `codegraph.ui.v1.ExclusionReason` enum and the schema `codegraph.v1.ExclusionReason` enum have identical (name, number) sets, proven by a descriptor test, so the wire mapper is a numeric cast that cannot silently skew (D-08)"
    - "`internal/query/coverage.go` reads exclusion records through `graphstore.Reader` only; the file-scoped source scan finds zero directory-walk / directory-read / file-stat calls in it and a positive count of `IterateExcludedFiles` (D-14b); `internal/query/archtest` stays green"
    - "HLT-06 adjacency/encoding: the read-only verb guard is a case-sensitive ASCII substring check (`strings.Contains(name, verb)` over `mutatingVerbs`) — `GetCoverage` passes and `GetIndexCoverage` fails on the `Index` member"
    - "HLT-06 empty: `GetCoverage` against a graph with `has_coverage` unset returns `known == false` with zero rows and an empty `next_page_token`; against a known graph with zero rows it returns `known == true`, zero rows, empty token"
    - "`codegraph status --json`'s `StatusResult` gains no field: `statusResultKnownFieldNames` in `internal/query/meta_test.go` is untouched and green, and `internal/query/status.go` is not modified by this plan (D-12)"
    - statement: "HLT-04 concurrency: every GetHealth/GetCoverage request reads its summary and rows from ONE `store.Snapshot()` Reader opened by `withEngine`, so a Sync committing mid-request cannot interleave the counts; consecutive pages may be read from different snapshots and are documented as such on `GetCoverageResponse.next_page_token`"
      verification: backstop
  artifacts:
    - "internal/schema/graph.proto — `ExcludedFile` message (path=1, reason=2, detail=3, size_bytes=4), closed `ExclusionReason` enum (UNSPECIFIED=0, DIR_VENDOR=1, DIR_DOTPREFIX=2, UNSUPPORTED_EXTENSION=3, BUILD_TAG=4, SIZE_LIMIT=5), `Meta.has_coverage = 9`"
    - "internal/graphstore/keys.go — `prefixExcludedFile byte = 'c'`, `excludedFileKey(path)`; batch.go `PutExcludedFile`; pebble_store.go `IterateExcludedFiles` + `pebbleExcludedFileIterator`; store.go `ExcludedFileIterator` interface"
    - "internal/indexer/discoverexclusion.go — `Discovery` struct, `DiscoverAll(root)`, `newExcludedFile`, `buildTagDetail`"
    - "internal/query/coverage.go — `CoverageSummary`, `CoverageRow`, `CoverageRowKind`, `CoverageRowsOptions`, `CoveragePage`, `(*Engine).CoverageSummary()`, `(*Engine).CoverageRows(opts)`, `CoverageDefaultPageSize`, `CoverageMaxPageSize`"
    - "internal/uiproto/uiv1/ui.proto — `Coverage` message, UI-local `ExclusionReason` and `CoverageRowKind` enums, `GetCoverageRequest`, `CoverageRow`, `GetCoverageResponse`, `GetHealthResponse.coverage = 17`, `rpc GetCoverage`"
    - "internal/uiserver/coverage.go — `coverageToProto`, `exclusionReasonToProto`, `coverageRowKindToProto`, `(*uiService).GetCoverage`"
  key_links:
    - "Discover decision point 3 (build-tag miss) → `Discovery.Excluded` → `writeGraph` `PutExcludedFile` loop on the same Writer as `PutFile` → `w.Commit()` once"
    - "`Engine.CoverageSummary` → `reader.GetMeta().GetHasCoverage()` gate → `IterateFiles` (indexed / extraction_failed) + `IterateExcludedFiles` (excluded, by-reason, file-level denominator share)"
    - "`GetHealth` handler → `eng.CoverageSummary()` inside the SAME withEngine closure as `eng.Status(ctx)` → `coverageToProto` → `GetHealthResponse.coverage` (17)"
  prohibitions:
    - "`internal/query` MUST NOT reconstruct exclusion reasons at read time — `coverage.go` contains no directory-walk, directory-read, or file-stat call and never imports the `internal/indexer` root"
    - "The pipeline MUST NOT open a second `graphstore.Writer` for exclusion records — one `NewWriter(`/`Commit()` pair in `resolve.go`'s writeGraph"
    - "`File` records MUST NOT be overloaded with a not-indexed flag — `graph.proto`'s `File` message gains no field"
    - "`SchemaVersion` MUST stay 1 — `internal/schema/meta.go` is not modified"
    - "`GetHealthResponse` MUST NOT inline the per-file row list — rows travel only through the paged `GetCoverage` rpc"
    - "`codegraph status` and the MCP surface MUST NOT change — `internal/query/status.go`, `internal/cli`, `internal/mcp` are not modified"
---

<objective>
Land the end-to-end tracer for the coverage denominator: ONE exclusion reason (BUILD_TAG) captured where `Discover` decides it, persisted in a new `c/` graphstore namespace inside the pipeline's single commit batch together with `Meta.has_coverage`, read back by `internal/query` through `graphstore` only, and projected onto the wire as `GetHealthResponse.coverage = 17` plus the paged `GetCoverage` rpc (UIService's 16th method) — verified through the real listener. Then expand the graphstore namespace to its full lifecycle surface (point delete, whole-namespace range delete, export/import framing) and wire the from-scratch "range-delete then rewrite" into writeGraph.

Purpose: prove the architecture on one path before Plans 02–05 expand it — the schema/wire field allocations (one-way doors, locked by D-05/D-06/D-09/D-10) land exactly once, with every fixture guard moved in the same commit (the 09-01 recipe), so no later plan can be blocked by a red guard mid-plan.

Output: `ExcludedFile`/`ExclusionReason`/`Meta.has_coverage` in `graph.proto`; the `c/` namespace in `internal/graphstore`; `DiscoverAll` + `Discovery` in `internal/indexer`; `Engine.CoverageSummary`/`CoverageRows` in `internal/query/coverage.go`; `Coverage`/`GetCoverage` in `ui.proto` with regenerated Go+TS; `coverageToProto`/`GetCoverage` handler in `internal/uiserver/coverage.go`; guards (`wantUIServiceMethods` 16, `uiProtoFieldFixtureLenAtPlan1001`, `knownMetaFieldNumbers` has_coverage, decoy table test, enum-agreement test, D-14b scan, D-15 old-graph test); `web/build` rebuilt and drift-clean.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-index-health-the-coverage-denominator/10-CONTEXT.md
@.planning/phases/10-index-health-the-coverage-denominator/10-RESEARCH.md
@.planning/phases/10-index-health-the-coverage-denominator/10-PATTERNS.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-01-SUMMARY.md
@.planning/phases/07-guards-that-cannot-fire/07-CONTEXT.md

Toolchain landmine: every local `go build` / `go test` / `go vet` MUST be prefixed `GOTOOLCHAIN=go1.26.6` (local Go is 1.27.1, `cockroachdb/swiss` breaks otherwise). `task proto:gen` regenerates BOTH proto surfaces (Go + TS) in one invocation; `web/src/lib/gen/ui_pb.ts` is a hashed `web:drift` source, so any regeneration must be followed by `task web:build` and the rebuilt `web/build/**` committed in the same commit.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "one BUILD_TAG exclusion becomes a recorded, persisted, wire-visible coverage gap" — schema → graphstore → Discover → one-batch commit → Engine → GetHealth.coverage/GetCoverage over the real listener</name>
  <precondition>`test -x web/node_modules/.bin/protoc-gen-es` succeeds (else run `pnpm -C web install --frozen-lockfile`) and `GOTOOLCHAIN=go1.26.6 go version` prints `go1.26.6` — `task proto:gen` and every Go gate below depend on both</precondition>
  <reversibility rating="one-way">Stored key namespace byte, `ExcludedFile` field numbers, `Meta.has_coverage = 9`, `GetHealthResponse.coverage = 17` and the rpc name `GetCoverage` are on-disk/wire contracts. All five were LOCKED at discuss-phase (D-05, D-06, D-08, D-09, D-10) with research's field-17 correction; no `checkpoint:decision` is emitted — the door was confirmed there.</reversibility>
  <files>internal/schema/graph.proto, internal/schema/graph.pb.go, internal/schema/exclusion.go, internal/schema/exclusion_test.go, internal/schema/meta_commit_test.go, internal/graphstore/keys.go, internal/graphstore/batch.go, internal/graphstore/pebble_store.go, internal/graphstore/store.go, internal/graphstore/excludedfile_test.go, internal/query/scoring_test.go, internal/query/traverse_test.go, internal/query/seeding_test.go, internal/query/search_test.go, internal/query/expand_test.go, internal/query/gather_test.go, internal/query/files_status_test.go, internal/indexer/discover.go, internal/indexer/discoverexclusion.go, internal/indexer/discoverexclusion_test.go, internal/indexer/pipeline.go, internal/indexer/pipeline_test.go, internal/indexer/resolve.go, internal/indexer/resolve_test.go, internal/indexer/sync.go, internal/uiproto/uiv1/ui.proto, internal/uiproto/uiv1/ui.pb.go, internal/uiproto/uiv1/uiv1connect/ui.connect.go, web/src/lib/gen/ui_pb.ts, web/build/.build-manifest, web/build/_app/**, web/build/index.html, internal/uiserver/readonly_test.go, internal/uiserver/coverage.go, internal/uiserver/coverage_test.go, internal/uiserver/handlers.go, internal/query/coverage.go, internal/query/coverage_test.go</files>
  <read_first>
    - internal/indexer/discover.go (whole file — the four decision points at lines 101–156, `ShouldSkipDir`, `pendingFile`, the post-walk descriptor pass)
    - internal/indexer/pipeline.go:55-161 (`resolveFunc`, `run`, `Stats`), internal/indexer/pipeline_test.go:84-110 (the injected `failingResolve` stub whose signature must follow `resolveFunc`)
    - internal/indexer/resolve.go:640-800 (`Resolve`, `writeGraph`, the `HasFileIndex`/`LastSyncUnixMs`/`CommitSha` stamping comments), internal/indexer/resolve_test.go:985-1160 (`stubWriter`, `stubStore`, the three `writeGraph`/`Resolve` call sites)
    - internal/indexer/sync.go:34-60 (backfill delegation), :85 (`Discover` call), :165-200 (mtime-refresh `NewMeta` site), :404-412 (main-commit `NewMeta` site)
    - internal/graphstore/keys.go (whole — `prefixFileIndex`, `appendSegment`, `fileKey`, `rangeUpperBound`), internal/graphstore/batch.go:38-111 (`PutNode`/`PutFile` shape, `deterministicMarshal`), internal/graphstore/pebble_store.go:289-297 and :399-436 (`IterateFiles`, `pebbleFileIterator`), internal/graphstore/store.go (`Reader`/`Writer`/`FileIterator` docs)
    - internal/query/scoring_test.go:21-50, internal/query/files_status_test.go:672-710 and the other five `*FakeReader` types (each implements `graphstore.Reader` explicitly — every one gains an `IterateExcludedFiles` stub returning a "not implemented" error, `globRefusingReader` included)
    - internal/schema/graph.proto (whole — `File` fields 1–8, `Meta` fields 1–8 + `reserved 50 to 59`), internal/schema/meta.go (`NewMeta`, `IndexedCommitSHA`'s absent-means-unknown contract), internal/schema/meta_commit_test.go:85-130 (`knownMetaFieldNumbers` subset fixture)
    - internal/query/engine.go:22-60 and :150-170 (`Engine` fields incl. `repoRoot`, `IndexMeta`'s nil-on-ErrNotFound contract), internal/query/errors.go (`ErrInvalidArgument`, `invalidArgumentf`), internal/query/meta_test.go:66-90 (`TestIndexMetaOnAGraphWithNoMeta` shape), internal/query/status.go:43-70 (`StatusResult` — NOT to be extended, D-12)
    - internal/query/archtest/import_direction_test.go (whole — the Phase-10 forward consequence in its package doc)
    - internal/cli/editordiscovery_test.go:299-341 (`TestEditorDiscoverySourceNeverSpawnsAProcess` — the file-scoped scan shape D-14b copies)
    - internal/uiproto/uiv1/ui.proto:50-136 (the `service UIService` block) and :756-822 (`GetHealthResponse`, `commit_sha = 16` already spent), :1038-1060 (`EditorLinkAvailability` — the closed-enum comment convention)
    - internal/uiserver/handlers.go:28-130 (`withEngine`, `mapEngineError`), :191-210 (`uiService`), :897-1000 (`healthToProto`, `worktreeMismatchToProto`, `GetHealth`)
    - internal/uiserver/readonly_test.go:60-160 (`wantUIServiceMethods`, `TestUIServiceMethodSetIsExactlyTheReadSet`'s two literal `15`s, `mutatingVerbs`, `TestUIServiceDeclaresNoMutatingMethod`), :260-330 (`uiProtoFieldFixtureLenAtPlan0901`, the fixture's doc trail), :510-560 (the plan 09-01 entries + `TestUIProtoFieldNumbersAreStableAndUnique`'s length check), internal/uiserver/rpcname_test.go (whole — the positive-control shape for the decoy test)
    - internal/uiserver/health_test.go:1-120 and internal/uiserver/server_test.go:20-75, internal/uiserver/handlers_test.go:80-100 (`copyGofixture`, `indexGofixture`, `startedServer` — the real-listener scaffolding)
    - Taskfile.yml:305-375 (`proto:gen`, `proto:drift`), :739-760 (`web:build`), :1187-1300 (`web:drift`'s hashed source set includes `web/src/lib/gen/ui_pb.ts`)
  </read_first>
  <behavior>
    - Test 1 (`internal/graphstore/excludedfile_test.go` `TestExcludedFileNamespaceRoundTrip`): PutExcludedFile for paths "b.md" then "a/tagged.go" on one Writer, Commit, Snapshot, IterateExcludedFiles yields exactly 2 records in byte order ("a/tagged.go", "b.md") with reason/detail/size_bytes intact; a fresh empty store yields 0 records and `Err() == nil`.
    - Test 2 (`excludedfile_test.go` `TestExcludedFileKeyStartsWithPrefixAndIsolatesNeighbours`): `excludedFileKey("foo")` and `excludedFileKey("foobar")` are distinct, both start with `prefixExcludedFile`, neither starts with any of {prefixMeta, prefixNode, prefixEdge, prefixFile, prefixAnnotation, prefixFileIndex}; a path containing "/", 0x00 and 0xFF round-trips through Put/Iterate unchanged.
    - Test 3 (`internal/indexer/discoverexclusion_test.go` `TestDiscoverAll_RecordsBuildTagExclusion`): temp root with `go.mod`, `real.go` (`package tmp`), `tagged.go` (`//go:build ignore` + blank line + `package tmp`) → `DiscoverAll(root)` returns `Files` == ["real.go"], and `Excluded` contains exactly ONE record whose Path == "tagged.go", Reason == EXCLUSION_REASON_BUILD_TAG, Detail == `build.Default.GOOS + "/" + build.Default.GOARCH` (the same context `Discover` matches against — never `runtime.GOOS`, which a `GOOS` env override would desynchronise), SizeBytes == the file's stat size; `Discover(root)` (the unchanged wrapper) still returns the same `Files`/modulePath as before.
    - Test 4 (`internal/indexer/resolve_test.go` `TestWriteGraphStagesExcludedFilesInTheSameBatch`): writeGraph with one File and two ExcludedFile records against `stubStore`/`stubWriter` → `stubWriter.excluded` has both records in path-sorted order, `commitCalls == 1`, `closeCalls == 0`, `meta.HasCoverage == true`; and the existing `TestPipelineRun_ClosesStoreOnResolveError` still passes with the widened `resolveFunc`.
    - Test 5 (`internal/query/coverage_test.go` `TestCoverageSummaryAfterRunOnBuildTagRepo`): index a temp repo (go.mod, main.go, tagged.go with `//go:build ignore`) via `indexer.Run` (in-package test files may import the indexer root — archtest scopes the rule to production code), then `New(snapshot).CoverageSummary()` → `Known == true`, `Indexed == 1`, `ExtractionFailed == 0`, `ExcludedByReason["EXCLUSION_REASON_BUILD_TAG"] == 1`, and `Discovered == Indexed + ExtractionFailed + (file-level excluded)` — asserted by-reason and by invariant, NEVER as a hard-coded total (later plans add more reasons to the same walk).
    - Test 6 (`coverage_test.go` `TestCoverageSummaryOnOldGraphIsUnknown`, D-15): a store whose committed Meta was built by `schema.NewMeta()` WITHOUT `HasCoverage` (NodeCount/HasFileIndex set, plus one PutFile) → `CoverageSummary()` returns `Known == false`, every int64 field == 0, `ExcludedByReason == nil`, `err == nil`; a store with NO Meta record at all → same; mutation note in the test comment: treating unset as zero flips this RED.
    - Test 7 (`coverage_test.go` `TestCoverageRowsUnknownGraphAndSingleRow`): old graph → `CoverageRows(CoverageRowsOptions{})` returns `Known == false`, zero rows, empty token, nil error; the tagged repo → `Known == true`, exactly one row {Path "tagged.go", Kind CoverageRowExcluded, Reason BUILD_TAG}, empty `NextPageToken`.
    - Test 8 (`coverage_test.go` `TestCoverageSourceNeverWalksDisk`, D-14b): reads `coverage.go` bytes; forbidden substrings `filepath.WalkDir(`, `os.ReadDir(`, `os.Stat(`, `os.Lstat(`, `os.ReadFile(`, `os.Open(` — zero occurrences; positive control: `IterateExcludedFiles` and `GetHasCoverage` present, inspected count logged and asserted > 0.
    - Test 9 (`internal/uiserver/coverage_test.go` `TestExclusionReasonEnumsAgree`): walk `uiv1.ExclusionReason(0).Descriptor().Values()` and `schema.ExclusionReason(0).Descriptor().Values()`; same length (log it, require == 6), and for every value the (name-with-prefix-stripped, number) pair matches in BOTH directions.
    - Test 10 (`internal/uiserver/coverage_test.go` `TestGetHealthCarriesCoverageForBuildTagExclusion`): write the tagged temp repo, index it at `<repo>/.codegraph/store`, `startedServer`, `GetHealth` → `resp.Msg.GetCoverage().GetKnown() == true`, `GetIndexed() == 1`, `GetExcludedByReason()["EXCLUSION_REASON_BUILD_TAG"] == 1`, and the same by-reason/invariant assertions as Test 5; plus a `GetCoverage` call with `PageSize: 10` → `GetKnown() == true`, exactly one row with `Path == "tagged.go"`, `Kind == COVERAGE_ROW_KIND_EXCLUDED`, `Reason == EXCLUSION_REASON_BUILD_TAG`, `GetNextPageToken() == ""`.
    - Test 11 (`internal/uiserver/readonly_test.go` `TestCoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected`, D-16): table `{name, wantClean}` = {"GetCoverage", true}, {"GetIndexCoverage", false}; asserts `len(mutatingVerbs) > 0`, that the table contains at least one clean AND one rejected row (so the discriminator is proven live), that `strings.Contains` verdicts match `wantClean` for every row, and that the clean name is a member of `wantUIServiceMethods`.
    - Test 12 (`internal/schema/exclusion_test.go` `TestIsDirectoryExclusionCoversTheClosedEnum`): iterates every value of the `ExclusionReason` descriptor (asserts count == 6), expects `IsDirectoryExclusion` true for exactly {DIR_VENDOR, DIR_DOTPREFIX} and false for every other defined value including UNSPECIFIED.
  </behavior>
  <action>
    Commit boundaries (every commit must build with `GOTOOLCHAIN=go1.26.6 go build ./...`; RED tests are committed before GREEN — Go-native transcripts pasted in the SUMMARY, never TAP-bent):

    **Commit A — schema + graphstore namespace (`test(10-01): …` then `feat(10-01): …`).** In `internal/schema/graph.proto` add, as siblings of `File` (never fields on it — D-05), `message ExcludedFile { string path = 1; ExclusionReason reason = 2; string detail = 3; int64 size_bytes = 4; }` and the closed `enum ExclusionReason { EXCLUSION_REASON_UNSPECIFIED = 0; EXCLUSION_REASON_DIR_VENDOR = 1; EXCLUSION_REASON_DIR_DOTPREFIX = 2; EXCLUSION_REASON_UNSUPPORTED_EXTENSION = 3; EXCLUSION_REASON_BUILD_TAG = 4; EXCLUSION_REASON_SIZE_LIMIT = 5; }` (D-08), each with a doc comment stating path is the slash-normalized repo-relative path exactly as `File.path` (a pruned directory's record carries the directory path with no trailing slash, D-02), detail is bounded free text (extension / byte size / GOOS-GOARCH / directory name) and size_bytes is the discovery-time stat size (0 for directory records). Add `bool has_coverage = 9;` to `Meta` after `commit_sha = 8` with the `HasFileIndex`-style comment: absent/false = coverage UNKNOWN, never 0/0, never inferred from `c/` key presence (D-06). `SchemaVersion` in `meta.go` stays 1 — do not touch that file. Add `{"has_coverage", 9}` to `knownMetaFieldNumbers` (Pitfall 5: the subset fixture would pass without it; add it for drift tracking). Create `internal/schema/exclusion.go` with `func IsDirectoryExclusion(r ExclusionReason) bool` (true for DIR_VENDOR and DIR_DOTPREFIX only) — the ONE definition of "directory-level record" that both the indexer and the query engine import, so the denominator math (research Pitfall 1) has a single owner. Run `task proto:gen` (regenerates `graph.pb.go`; the UI files are byte-unchanged at this commit).

    In `internal/graphstore/keys.go` add `prefixExcludedFile byte = 'c'` to the const block with a comment naming it the Phase-10 coverage namespace (`c/<path>` — chosen because `m n e f a x` are taken and `c` reads as "coverage"; directory-level records share this namespace, distinguished by the reason enum, no sub-prefix) and `func excludedFileKey(path string) []byte` copying `fileKey`'s exact shape via `appendSegment` (never string concatenation — T-01-02). In `store.go` add `ExcludedFileIterator` (Next/ExcludedFile/Err/Close, doc-commented like `FileIterator`), `Reader.IterateExcludedFiles() (ExcludedFileIterator, error)` and `Writer.PutExcludedFile(x *schema.ExcludedFile) error`. In `batch.go` implement `PutExcludedFile` exactly as `PutFile` (`deterministicMarshal` then `batch.Set(excludedFileKey(x.GetPath()), data, nil)`). In `pebble_store.go` implement `IterateExcludedFiles` + `pebbleExcludedFileIterator` as a renamed copy of `IterateFiles`/`pebbleFileIterator` unmarshaling `schema.ExcludedFile`. Update every explicit `graphstore.Reader` fake in `internal/query/*_test.go` (scoringFakeReader, traverseFakeReader, seedingFakeReader, searchFakeReader, expandFakeReader, gatherFakeReader, globRefusingReader) with an `IterateExcludedFiles` method returning a "not implemented" error in each fake's own style, and `internal/indexer/resolve_test.go`'s `stubWriter` with `excluded []*schema.ExcludedFile` + `PutExcludedFile` (honouring `failOn == "PutExcludedFile"`). Write `excludedfile_test.go` Tests 1–2 first (RED: methods undefined), then implement (GREEN).

    **Commit B — Discover captures BUILD_TAG; one-batch commit (`test(10-01): …` then `feat(10-01): …`).** Create `internal/indexer/discoverexclusion.go` holding: `type Discovery struct { Files []DiscoveredFile; Excluded []*schema.ExcludedFile; ModulePath string }` (one walk result feeding BOTH the denominator and the reason list, D-01); `func DiscoverAll(root string) (Discovery, error)` — MOVE the body of `Discover` here (the WalkDir callback, descriptor pass, sort) and make `Discover(root)` a three-line wrapper returning `d.Files, d.ModulePath, err` so all 18 existing call sites keep compiling unchanged; `func newExcludedFile(relPath string, reason schema.ExclusionReason, detail string, sizeBytes int64) *schema.ExcludedFile`; `func buildTagDetail(ctx build.Context) string` returning `ctx.GOOS + "/" + ctx.GOARCH`. In the walk callback, move the `d.Info()` call to run for every non-directory entry BEFORE the extension check (one lstat per visited file; its size is needed for every record kind), then at decision point 3 (the `!match` branch) append `newExcludedFile(relPath, EXCLUSION_REASON_BUILD_TAG, buildTagDetail(ctx), info.Size())` instead of a bare return — compute `relPath` (`filepath.Rel` + `ToSlash`) before the build-tag check so the record carries the same path shape as `File.path`. Leave decision points 1, 2 and the size pre-check as TODO-free plain returns for Plan 02 (this plan records ONE reason only — it is the tracer). Sort `Excluded` by Path after the walk (determinism, mirroring `Files`). In `pipeline.go` switch `run` to `DiscoverAll`, widen `resolveFunc` and `Resolve` to `(store, results, modulePath, commitSHA string, excluded []*schema.ExcludedFile) (int, error)`, thread `excluded` into `writeGraph(store, nodes, packageNodes, edges, files, commitSHA, excluded)`; update `pipeline_test.go`'s `failingResolve` and every `writeGraph`/`Resolve` call in `resolve_test.go` (pass nil where no records apply). In `writeGraph`, after the `PutEdge` loop and before `schema.NewMeta()`, stage every record of a path-sorted copy of `excluded` via `w.PutExcludedFile` on the SAME `w` (closing on error exactly like the other loops), then set `meta.HasCoverage = true` next to `meta.HasFileIndex = true` with a comment mirroring the D-02b rationale (D-07; Pitfall 3). `Stats` is unchanged. In `sync.go` switch the `Discover` call to `DiscoverAll` (use `.Files`/`.ModulePath`; the exclusion diff is Plan 03's work) and at BOTH `schema.NewMeta()` sites set `newMeta.HasCoverage = meta.GetHasCoverage()` — carry the prior value forward, never flip it to false and never claim true before Plan 03's upsert/prune exists; leave a one-line comment naming Plan 03 as the site that replaces this with the real stamp. Write Tests 3–4 first (RED), then implement (GREEN); run `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/indexer/... ./internal/graphstore/...` fully green, and `task test:golden` green (the frozen `status.json` goldens must not move — D-12).

    **Commit C — read-back and wire (`test(10-01): …` then `feat(10-01): …`; this is the same-commit fixture move of the 09-01 recipe).** Create `internal/query/coverage.go` (production code, imports `graphstore` and `schema` only — never the indexer root; archtest D-01): `type CoverageSummary struct { Known bool; Discovered, Indexed, Excluded, ExtractionFailed int64; ExcludedByReason map[string]int64 }` (keys are `schema.ExclusionReason.String()` full names such as `EXCLUSION_REASON_BUILD_TAG` — D-09 discretion: the generated TS client resolves numbers to the same full names via `ExclusionReasonSchema.values`, so full names are the easiest to display and cannot collide); `type CoverageRowKind uint8` with `CoverageRowExcluded`/`CoverageRowExtractionFailed`; `type CoverageRow struct { Path string; Kind CoverageRowKind; Reason schema.ExclusionReason; Detail string }`; `type CoverageRowsOptions struct { PageSize int; PageToken string; Reason schema.ExclusionReason }`; `type CoveragePage struct { Known bool; Rows []CoverageRow; NextPageToken string }`; `const CoverageDefaultPageSize = 200`, `CoverageMaxPageSize = 1000`, `coverageDetailMaxBytes = 256`. `(*Engine).CoverageSummary()`: `meta, err := e.IndexMeta()`; if `meta == nil || !meta.GetHasCoverage()` return `CoverageSummary{Known:false}` (D-06/D-15 — the unknown short-circuit runs BEFORE any scan); else one `IterateFiles` pass counting total files, `Indexed` (len(errors)==0) and `ExtractionFailed` (len(errors)>0 — derived from the existing `File.errors` signal, never a parallel tally), one `IterateExcludedFiles` pass counting `Excluded`, `ExcludedByReason[reason.String()]++` and `fileLevel++` when `!schema.IsDirectoryExclusion(reason)`; `Discovered = total files + fileLevel` (pruned directories contribute 0 — D-01, research Pitfall 1); counts are int64 additions only (HLT-05 precision — no division, no rounding). `(*Engine).CoverageRows(opts)`: same unknown short-circuit returning `CoveragePage{Known:false}`; clamp PageSize (<=0 → default, >max → max); decode the token via `decodeCoverageToken` (below); build the ordered sequence — segment `'f'` = extraction-failed File records in path (key) order, then segment `'x'` = ExcludedFile records in path (key) order (HLT-06 ordering: byte-ordered pebble key order within each segment, so paging is stable); when `opts.Reason != UNSPECIFIED` only segment `'x'` rows with that reason are emitted and a `'f'` token is invalid; skip rows whose (segment, path) is <= the cursor using in-memory string comparison — the token is NEVER used to build a Pebble bound and NEVER treated as a filesystem path; collect PageSize+1 candidates, trim to PageSize and set `NextPageToken = encodeCoverageToken(segment, lastPath)` only when a candidate remains. Extraction-failed rows carry `Detail = strings.Join(f.GetErrors(), "; ")` with `e.repoRoot` (when non-empty) replaced by `"."` and the result cut at `coverageDetailMaxBytes` on a rune boundary (T-10-05). `encodeCoverageToken(kind byte, path string) string` = `base64.RawURLEncoding.EncodeToString(append([]byte{kind}, path...))`; `decodeCoverageToken(tok string) (kind byte, path string, err error)` — empty token → first page; otherwise base64url-decode, require len >= 1, kind in {'f','x'}, `utf8.Valid(path)`, `len(path) <= 4096`, else return `invalidArgumentf("coverage: malformed page token")` (T-10-04). Doc-comment the file header with the D-14 rule (reasons are read back, never re-derived — this file is the target of a file-scoped source scan) and note that consecutive pages may come from different snapshots. Write `coverage_test.go` Tests 5–8 first (RED: package does not compile / scan file missing), then implement.

    Then the wire: in `internal/uiproto/uiv1/ui.proto` add `message Coverage { bool known = 1; int64 discovered = 2; int64 indexed = 3; int64 excluded = 4; int64 extraction_failed = 5; map<string, int64> excluded_by_reason = 6; }` with a comment stating the invariant `discovered == indexed + extraction_failed + (excluded minus directory-level records)` and that `known == false` means UNKNOWN (D-06, never 0/0); a UI-LOCAL closed `enum ExclusionReason` with the six values numbered identically to the schema enum (ui.proto imports nothing today and the TS generator is scoped to this one file — importing graph.proto would break `web/src/lib/gen/ui_pb.ts`; `TestExclusionReasonEnumsAgree` pins the two enums to each other); `enum CoverageRowKind { COVERAGE_ROW_KIND_UNSPECIFIED = 0; COVERAGE_ROW_KIND_EXCLUDED = 1; COVERAGE_ROW_KIND_EXTRACTION_FAILED = 2; }`; `message GetCoverageRequest { int32 page_size = 1; string page_token = 2; ExclusionReason reason = 3; }` (D-10 verbatim; comment: page_size <= 0 → 200, > 1000 → clamped to 1000; page_token is opaque, server-issued, validated on the way in, malformed → INVALID_ARGUMENT); `message CoverageRow { string path = 1; CoverageRowKind kind = 2; ExclusionReason reason = 3; string detail = 4; }`; `message GetCoverageResponse { repeated CoverageRow rows = 1; string next_page_token = 2; bool known = 3; }` (`known` is additive to D-10's shape and required by D-06's never-0/0 rule — an old graph must be distinguishable from a known graph with zero rows); `Coverage coverage = 17;` on `GetHealthResponse` (D-09 as corrected — 16 is `commit_sha`); and `rpc GetCoverage(GetCoverageRequest) returns (GetCoverageResponse);` as the 16th entry of `service UIService` with a comment recording why the name clears every `mutatingVerbs` member (no "Index"). Run `task proto:gen` (all four generated files move). In `readonly_test.go`, in the SAME commit: add `"GetCoverage": {}` to `wantUIServiceMethods` with an "UPDATED at plan 10-01" doc line, change both literal `15`s in `TestUIServiceMethodSetIsExactlyTheReadSet` to `16` (and "fifteen-name" to "sixteen-name"), add `const uiProtoFieldFixtureLenAtPlan1001 = uiProtoFieldFixtureLenAtPlan0901 + 17` (Coverage 6 + GetCoverageRequest 3 + CoverageRow 4 + GetCoverageResponse 3 + GetHealthResponse.coverage 1 = 17) with the chained-extension doc comment, append the 17 fixture entries, point `TestUIProtoFieldNumbersAreStableAndUnique`'s length check at the new constant, and add Test 11 (`wantCoverageRPCName = "GetCoverage"` as a package const). Create `internal/uiserver/coverage.go`: `coverageToProto(s query.CoverageSummary) *uiv1.Coverage` (named mapper, never inline — `healthToProto` convention; copies the map as-is), `exclusionReasonToProto(r schema.ExclusionReason) uiv1.ExclusionReason` and `coverageRowKindToProto` as numeric casts (guarded by Test 9), and `(*uiService).GetCoverage` using the ORDINARY `withEngine` shape (not GetStatus's degrade path): `eng.CoverageRows(query.CoverageRowsOptions{PageSize: int(req.Msg.GetPageSize()), PageToken: req.Msg.GetPageToken(), Reason: schema.ExclusionReason(req.Msg.GetReason())})`, mapping rows and `known`; a malformed token surfaces as `query.ErrInvalidArgument` → `CodeInvalidArgument` through `mapEngineError` unchanged. In `handlers.go`'s `GetHealth` closure call `eng.CoverageSummary()` right after `eng.Status(ctx)` and pass it into `healthToProto(result, commitSHA, coverage)`, setting `Coverage: coverageToProto(coverage)`; `healthToProto`'s doc comment gains one sentence on the new parameter. Write Tests 9–11 (RED), implement (GREEN). Finally `task web:build && task -s web:drift` and commit the rebuilt `web/build/**` (including `.build-manifest`) IN THIS SAME COMMIT with the regenerated `ui_pb.ts`.

    Do NOT modify `internal/query/status.go`, anything under `internal/cli` or `internal/mcp` (D-12), `internal/schema/meta.go`, or `internal/uiserver/rpcname_test.go`. Do not add a "re-index" action of any kind.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestExcludedFileNamespaceRoundTrip|TestExcludedFileKeyStartsWithPrefixAndIsolatesNeighbours|TestDiscoverAll_RecordsBuildTagExclusion|TestWriteGraphStagesExcludedFilesInTheSameBatch' ./internal/graphstore/... ./internal/indexer/... 2&gt;&amp;1 | tee /tmp/10-01-red-a.txt; test "$(rg -o -- '--- FAIL|build failed|undefined:' /tmp/10-01-red-a.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>RED gate (run BEFORE Commit A/B implementation): passes only if at least one FAIL/undefined/build-failed line appears; a green run here means the tests were not written first or do not exercise new symbols</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestExcludedFileNamespaceRoundTrip|TestExcludedFileKeyStartsWithPrefixAndIsolatesNeighbours|TestDiscoverAll_RecordsBuildTagExclusion|TestWriteGraphStagesExcludedFilesInTheSameBatch|TestPipelineRun_ClosesStoreOnResolveError|TestIsDirectoryExclusionCoversTheClosedEnum|TestKnownMetaFieldNumbersAreStable' ./internal/graphstore/... ./internal/indexer/... ./internal/schema/... 2&gt;&amp;1 | tee /tmp/10-01-green-a.txt | rg -c -- '^--- PASS: Test' &amp;&amp; test "$(rg -o -- '--- PASS: Test(ExcludedFileNamespaceRoundTrip|ExcludedFileKeyStartsWithPrefixAndIsolatesNeighbours|DiscoverAll_RecordsBuildTagExclusion|WriteGraphStagesExcludedFilesInTheSameBatch|PipelineRun_ClosesStoreOnResolveError|IsDirectoryExclusionCoversTheClosedEnum|KnownMetaFieldNumbersAreStable)' /tmp/10-01-green-a.txt | wc -l | tr -d ' ')" = "7" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/10-01-green-a.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; ! rg -q 'no tests to run' /tmp/10-01-green-a.txt</automated>
    <fails_when>fewer than 7 named PASS lines, any FAIL line, or a "no tests to run" line (a mistyped -run pattern silently matching nothing)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestCoverageSummaryAfterRunOnBuildTagRepo|TestCoverageSummaryOnOldGraphIsUnknown|TestCoverageRowsUnknownGraphAndSingleRow|TestCoverageSourceNeverWalksDisk|TestExclusionReasonEnumsAgree|TestGetHealthCarriesCoverageForBuildTagExclusion|TestCoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected|TestUIServiceMethodSetIsExactlyTheReadSet|TestUIServiceDeclaresNoMutatingMethod|TestUIProtoFieldNumbersAreStableAndUnique|TestUIServiceHoldsNoStoreTypedField|TestGetHealthProjectsStatusResult' ./internal/query/... ./internal/uiserver/... 2&gt;&amp;1 | tee /tmp/10-01-green-c.txt | rg -c -- '^--- PASS: Test' &amp;&amp; test "$(rg -o -- '--- PASS: Test(CoverageSummaryAfterRunOnBuildTagRepo|CoverageSummaryOnOldGraphIsUnknown|CoverageRowsUnknownGraphAndSingleRow|CoverageSourceNeverWalksDisk|ExclusionReasonEnumsAgree|GetHealthCarriesCoverageForBuildTagExclusion|CoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected|UIServiceMethodSetIsExactlyTheReadSet|UIServiceDeclaresNoMutatingMethod|UIProtoFieldNumbersAreStableAndUnique|UIServiceHoldsNoStoreTypedField|GetHealthProjectsStatusResult)' /tmp/10-01-green-c.txt | wc -l | tr -d ' ')" = "12" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/10-01-green-c.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; ! rg -q 'no tests to run' /tmp/10-01-green-c.txt</automated>
    <fails_when>fewer than 12 named PASS lines (a guard was dropped or renamed), any FAIL, or "no tests to run"</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/query/archtest/... ./internal/graphstore/archtest/... 2&gt;&amp;1 | tee /tmp/10-01-archtest.txt | rg -c '^ok' &amp;&amp; test "$(rg -o '^ok' /tmp/10-01-archtest.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o 'FAIL' /tmp/10-01-archtest.txt | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>either archtest package does not print `ok` — `internal/query/coverage.go` reached the indexer root or a wire package transitively</fails_when>
    <automated>test "$(rg -o 'filepath\.WalkDir\(|os\.ReadDir\(|os\.Stat\(|os\.Lstat\(|os\.ReadFile\(|os\.Open\(' internal/query/coverage.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'IterateExcludedFiles\(' internal/query/coverage.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'codegraph-go/internal/indexer"' internal/query/coverage.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'NewWriter\(' internal/indexer/resolve.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'PutExcludedFile\(' internal/indexer/resolve.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'HasCoverage = true' internal/indexer/resolve.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'HasCoverage = meta\.GetHasCoverage\(\)' internal/indexer/sync.go | wc -l | tr -d ' ')" = "2"</automated>
    <fails_when>coverage.go contains a filesystem call or an indexer-root import (positive control: IterateExcludedFiles absent means the scan targets the wrong file), resolve.go opens more or fewer than one Writer or does not stamp HasCoverage exactly once, or sync.go does not carry HasCoverage forward at both NewMeta sites</fails_when>
    <automated>test "$(rg -o 'Coverage coverage = 17;' internal/uiproto/uiv1/ui.proto | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'rpc GetCoverage\(GetCoverageRequest\) returns \(GetCoverageResponse\);' internal/uiproto/uiv1/ui.proto | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^  rpc ' internal/uiproto/uiv1/ui.proto | wc -l | tr -d ' ')" = "16" &amp;&amp; test "$(rg -o 'bool has_coverage = 9;' internal/schema/graph.proto | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'message ExcludedFile \{' internal/schema/graph.proto | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'EXCLUSION_REASON_[A-Z_]+ = [0-9];' internal/schema/graph.proto | wc -l | tr -d ' ')" = "6" &amp;&amp; test "$(rg -o 'EXCLUSION_REASON_[A-Z_]+ = [0-9];' internal/uiproto/uiv1/ui.proto | wc -l | tr -d ' ')" = "6" &amp;&amp; test "$(rg -o '"GetCoverage": *\{\}' internal/uiserver/readonly_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'uiProtoFieldFixtureLenAtPlan1001 = uiProtoFieldFixtureLenAtPlan0901 \+ 17' internal/uiserver/readonly_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '\{"has_coverage", 9\}' internal/schema/meta_commit_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'getCoverage' web/src/lib/gen/ui_pb.ts | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'const SchemaVersion uint32 = 1' internal/schema/meta.go | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>any allocated number/name differs (field 17, 16 rpcs, has_coverage 9, six enum values on each surface, fixture +17), the TS client lacks getCoverage (proto:gen not run or not committed), or SchemaVersion moved</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task proto:drift &gt;/tmp/10-01-drift.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/10-01-drift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i 'mismatch|drift detected' /tmp/10-01-drift.txt | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>proto:drift exits non-zero or reports a mismatch — a generated file was hand-edited or not regenerated from the committed .proto</fails_when>
    <automated>task web:build &amp;&amp; task -s web:drift &gt;/tmp/10-01-webdrift.txt 2&gt;&amp;1; RC=$?; cat /tmp/10-01-webdrift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '\bhalf MATCH\b' /tmp/10-01-webdrift.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o 'web:drift: PASS' /tmp/10-01-webdrift.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test -z "$(git status --porcelain -- web/build web/src/lib/gen)"</automated>
    <fails_when>web:drift does not print both `half MATCH` lines and `PASS`, or `web/build`/`ui_pb.ts` has uncommitted changes — the regenerated client was not rebuilt and committed together</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./... &amp;&amp; GOTOOLCHAIN=go1.26.6 go build ./... &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:unit &gt;/tmp/10-01-unit.txt 2&gt;&amp;1; RC=$?; tail -5 /tmp/10-01-unit.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^FAIL' /tmp/10-01-unit.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:golden &gt;/tmp/10-01-golden.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/10-01-golden.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^ok' /tmp/10-01-golden.txt | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -l 'CoverageSummary|CoverageRows|GetCoverage|ExcludedFile' internal/query/status.go internal/cli internal/mcp 2&gt;/dev/null | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>vet/build fail, test:unit or test:golden exit non-zero or print a FAIL line (golden must print at least one `ok`), or any coverage symbol leaks into `status.go`, `internal/cli` or `internal/mcp` (D-12)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD~2..HEAD` (or the plan's commit range) shows `internal/uiproto/uiv1/ui.proto`, `ui.pb.go`, `uiv1connect/ui.connect.go`, `web/src/lib/gen/ui_pb.ts`, `internal/uiserver/readonly_test.go` and `web/build/.build-manifest` changed in ONE commit (the 09-01 recipe)
    - `rg -n 'func Discover\(root string\)' internal/indexer/discoverexclusion.go internal/indexer/discover.go | wc -l` prints 1 and the wrapper body is at most 5 lines; `rg -o 'func DiscoverAll\(root string\) \(Discovery, error\)' internal/indexer/discoverexclusion.go | wc -l` prints 1
    - `rg -o 'func \(e \*Engine\) CoverageSummary\(\) \(CoverageSummary, error\)|func \(e \*Engine\) CoverageRows\(opts CoverageRowsOptions\) \(CoveragePage, error\)' internal/query/coverage.go | wc -l` prints 2
    - `rg -o 'func \(s \*uiService\) GetCoverage\(' internal/uiserver/coverage.go | wc -l` prints 1 and `rg -o 'withEngine\(' internal/uiserver/coverage.go | wc -l` prints 1
    - `rg -o 'func IsDirectoryExclusion\(r ExclusionReason\) bool' internal/schema/exclusion.go | wc -l` prints 1
    - Every `*FakeReader` and `globRefusingReader` in `internal/query/*_test.go` compiles with an `IterateExcludedFiles` method: `rg -l 'IterateExcludedFiles' internal/query/*_test.go | wc -l` prints at least 7
    - `TestCoverageSummaryOnOldGraphIsUnknown` contains the literal comment text `mutation: treat unset as zero` so the D-15 family in Plan 06 can find it: `rg -o 'mutation: treat unset as zero' internal/query/coverage_test.go | wc -l` prints 1
    - The SUMMARY pastes the Go RED transcript for Tests 1–4 and 5–11 (the `--- FAIL` / `undefined:` lines) and the GREEN transcript, plus the `task test:golden` tail
    - No file under `internal/query/status.go`, `internal/schema/meta.go`, `internal/cli/`, `internal/mcp/` is modified (D-12)
  </acceptance_criteria>
  <done>A `//go:build ignore` file in a fresh repo is recorded as an EXCLUSION_REASON_BUILD_TAG ExcludedFile in the `c/` namespace by `indexer.Run`, `Meta.has_coverage` is true, `GetHealth` over the real listener reports `coverage.known == true` with `excluded_by_reason["EXCLUSION_REASON_BUILD_TAG"] == 1`, `GetCoverage` returns that one row, an old graph reports `known == false` with no counts, every fixture guard (16 methods, +17 fields, has_coverage 9, decoy rejection, enum agreement, D-14b scan, archtest) is green, and `proto:drift` + `web:drift` both MATCH.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Namespace lifecycle — point delete, whole-namespace range delete wired into writeGraph, and the export/import record kind (research Open Question 1 decided: implement, with a lossless round-trip test)</name>
  <reversibility rating="costly">`exportKindExcludedFile = 5` becomes part of the ARCH-01 export framing every future reader must recognise; changing it later means a framing migration. Rated costly rather than one-way because Export/Import has no external consumer yet (research: no caller outside `internal/graphstore`).</reversibility>
  <files>internal/graphstore/batch.go, internal/graphstore/store.go, internal/graphstore/export.go, internal/graphstore/export_test.go, internal/graphstore/excludedfile_test.go, internal/indexer/resolve.go, internal/indexer/resolve_test.go</files>
  <read_first>
    - internal/graphstore/batch.go:73-135 (`DeleteNode`, `DeleteFileSubgraph`'s `DeleteRange` + `rangeUpperBound` shape), internal/graphstore/store.go:165-235 (Writer interface docs for the Delete* family)
    - internal/graphstore/export.go:15-25 (`exportKind*` tags), :85-125 (`Export`'s namespace sequence, `exportNamespace`), :180-240 (`Import`'s loop, `importRecord`'s switch)
    - internal/graphstore/export_test.go:18-140 (`TestBulkExportReimportsLosslessly` — extend it, do not fork it)
    - internal/graphstore/keyenc_test.go:60-80 (the foreign-prefix isolation loop to mirror)
    - internal/indexer/resolve.go:723-800 (writeGraph as left by Task 1), internal/indexer/resolve_test.go:985-1060 (`stubWriter`)
    - internal/indexer/sync.go:34-50 (the backfill path delegates to `run()` on an EXISTING store — the reason writeGraph must range-delete before rewriting)
    - .planning/phases/10-index-health-the-coverage-denominator/10-RESEARCH.md (Pitfall 2 and Open Question 1)
  </read_first>
  <behavior>
    - Test 1 (`excludedfile_test.go` `TestDeleteExcludedFileRemovesOnlyThatPath`): put "a.md", "a.mdx", "b.md"; commit; new Writer `DeleteExcludedFile("a.md")`; commit; iterate → exactly ["a.mdx", "b.md"] (the length-prefixed key isolates "a.md" from "a.mdx").
    - Test 2 (`excludedfile_test.go` `TestDeleteAllExcludedFilesClearsOnlyTheNamespace`): put 3 excluded records + 1 File + 1 Node + Meta; commit; `DeleteAllExcludedFiles()` then `PutExcludedFile("z.md")` on the SAME writer; commit; `IterateExcludedFiles` yields exactly ["z.md"]; `GetFile`, `GetNode`, `GetMeta` still succeed (adjacent namespaces untouched).
    - Test 3 (`export_test.go` extension of `TestBulkExportReimportsLosslessly`): the source store additionally holds two ExcludedFile records (one DIR_VENDOR "vendor", one UNSUPPORTED_EXTENSION "README.md" with detail ".md" and size 42); after Export → Import into a fresh store, `IterateExcludedFiles` on the destination yields both records `proto.Equal` to the originals, and the existing Node/Edge/File/Meta assertions still hold; a count assertion `excludedExported == 2` is printed via `t.Logf` and required > 0 (rule 84d1gfpywd — a round trip that silently exported zero exclusion records must fail).
    - Test 4 (`export_test.go` `TestImportRejectsUnknownRecordKind` — only if no equivalent exists; otherwise extend it): kind byte 6 is rejected with the existing "unknown record kind" error, proving the framing set is closed at 5.
    - Test 5 (`resolve_test.go` `TestWriteGraphRangeDeletesExcludedNamespaceBeforeRewriting`): `stubWriter` records call order; writeGraph with two excluded records → `deleteAllExcludedCalls == 1` and it precedes the first `PutExcludedFile` in the recorded order; `commitCalls == 1`.
  </behavior>
  <action>
    Add to `store.go`'s `Writer` interface and `batch.go`: `DeleteExcludedFile(path string) error` (point delete via `batch.Delete(excludedFileKey(path), nil)`, doc-commented as Plan 03's Sync prune primitive — D-07) and `DeleteAllExcludedFiles() error` (one `batch.DeleteRange([]byte{prefixExcludedFile}, rangeUpperBound([]byte{prefixExcludedFile}), nil)` — the D-07 "full index range-deletes the namespace then rewrites" primitive; document that it is namespace-scoped by the prefix byte and cannot reach `m/n/e/f/a/x`). Extend `internal/indexer/resolve_test.go`'s `stubWriter` with `DeleteExcludedFile`, `DeleteAllExcludedFiles` (counting into `deleteAllExcludedCalls` and appending "DeleteAllExcludedFiles"/"PutExcludedFile" to an `order []string` slice) so Test 5 can assert ordering.

    In `export.go` add `exportKindExcludedFile uint8 = 5` to the const block (comment: Phase 10, the `c/` namespace; unlike `x/`, nothing else regenerates these records on import — research Pitfall 2 — so they MUST be framed explicitly), stream the namespace in `Export` after files via `exportNamespace(bw, snap, prefixExcludedFile, exportKindExcludedFile, func() proto.Message { return &schema.ExcludedFile{} })`, and add the matching `case exportKindExcludedFile:` arm to `importRecord` unmarshaling `schema.ExcludedFile` and calling `w.PutExcludedFile`. Update `GraphStore.Export`'s doc comment in `store.go` ("meta, nodes, edges, files, excluded files").

    In `writeGraph` (resolve.go), stage `w.DeleteAllExcludedFiles()` BEFORE the `PutExcludedFile` loop (and after the PutEdge loop), closing the writer on error like every other staging step, with a comment: `run()` is also `Sync`'s backfill path on an EXISTING store (sync.go:34-50), so a from-scratch rewrite must clear stale `c/` records rather than layer on top of them (D-07); on the `codegraph index` path (`internal/cli/index.go` RemoveAll) the range is already empty and the delete is a no-op.

    Write Tests 1–5 first (RED), implement, GREEN. Determinism suites (`internal/indexer/determinism_test.go`, `sync_determinism_test.go`) must stay green — the export stream now carries a fifth record kind in stable key order.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestDeleteExcludedFileRemovesOnlyThatPath|TestDeleteAllExcludedFilesClearsOnlyTheNamespace|TestWriteGraphRangeDeletesExcludedNamespaceBeforeRewriting' ./internal/graphstore/... ./internal/indexer/... 2&gt;&amp;1 | tee /tmp/10-01-t2-red.txt; test "$(rg -o -- '--- FAIL|undefined:|build failed' /tmp/10-01-t2-red.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>RED gate (before implementation): no FAIL/undefined line means the new tests were not written first</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestDeleteExcludedFileRemovesOnlyThatPath|TestDeleteAllExcludedFilesClearsOnlyTheNamespace|TestBulkExportReimportsLosslessly|TestImportRejectsUnknownRecordKind|TestWriteGraphRangeDeletesExcludedNamespaceBeforeRewriting|TestDeterministicRebuild|TestBulkExportIsConsistentUnderConcurrentWrite' ./internal/graphstore/... ./internal/indexer/... 2&gt;&amp;1 | tee /tmp/10-01-t2-green.txt | rg -c -- '^--- PASS: Test' &amp;&amp; test "$(rg -o -- '--- PASS: Test(DeleteExcludedFileRemovesOnlyThatPath|DeleteAllExcludedFilesClearsOnlyTheNamespace|BulkExportReimportsLosslessly|ImportRejectsUnknownRecordKind|WriteGraphRangeDeletesExcludedNamespaceBeforeRewriting|DeterministicRebuild|BulkExportIsConsistentUnderConcurrentWrite)' /tmp/10-01-t2-green.txt | wc -l | tr -d ' ')" -ge 7 &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/10-01-t2-green.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'excludedExported=2|excluded records exported: 2' /tmp/10-01-t2-green.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>fewer than 7 named PASS lines, any FAIL, or the round-trip test does not log the exported-exclusion count of 2 (the positive control that the export stream actually carried the namespace)</fails_when>
    <automated>test "$(rg -o 'exportKindExcludedFile uint8 = 5' internal/graphstore/export.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'case exportKindExcludedFile:' internal/graphstore/export.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'exportNamespace\(bw, snap, prefixExcludedFile, exportKindExcludedFile' internal/graphstore/export.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'DeleteAllExcludedFiles\(\)' internal/indexer/resolve.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'NewWriter\(' internal/indexer/resolve.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'DeleteRange\(\[\]byte\{prefixExcludedFile\}' internal/graphstore/batch.go | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the export kind, its import arm, the Export stream call, writeGraph's single range-delete, or the single-Writer invariant is missing</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./internal/graphstore/... ./internal/indexer/... &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/graphstore/... ./internal/indexer/... ./internal/query/... ./internal/uiserver/... 2&gt;&amp;1 | tee /tmp/10-01-t2-pkgs.txt | tail -6; test "$(rg -o '^FAIL' /tmp/10-01-t2-pkgs.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^ok' /tmp/10-01-t2-pkgs.txt | wc -l | tr -d ' ')" -ge 4</automated>
    <fails_when>any package prints FAIL or fewer than four `ok` lines appear (a package failed to compile after the Writer interface grew)</fails_when>
  </verify>
  <acceptance_criteria>
    - `store.go`'s `Writer` interface declares `PutExcludedFile`, `DeleteExcludedFile`, `DeleteAllExcludedFiles` (`rg -o 'DeleteExcludedFile\(path string\) error|DeleteAllExcludedFiles\(\) error|PutExcludedFile\(x \*schema.ExcludedFile\) error' internal/graphstore/store.go | wc -l` prints 3)
    - `resolve_test.go`'s `stubWriter` implements all three and records call order (`rg -o 'deleteAllExcludedCalls' internal/indexer/resolve_test.go | wc -l` >= 2)
    - `TestBulkExportReimportsLosslessly` asserts the two re-imported ExcludedFile records with `proto.Equal` and logs the exported count
    - SUMMARY records the Open Question 1 decision verbatim: "Export/Import carries `exportKindExcludedFile = 5`; no WINDOWS.md entry needed"
  </acceptance_criteria>
  <done>The `c/` namespace has its full lifecycle surface (put, point delete, range delete), writeGraph clears it before every rewrite on one Writer, and an Export → Import round trip preserves every ExcludedFile record — proven by tests that report the counts they checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository filesystem → `Discover` → `c/` namespace | walker-produced path/detail strings from occasionally adversarial third-party trees are persisted verbatim |
| `c/` namespace → `internal/query` → `GetHealth`/`GetCoverage` → browser | persisted strings and counts cross to the loopback SPA; `page_token` crosses back in from the client |
| ARCH-01 export stream → `Import` | externally-produced framed records now include kind 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | `excludedFileKey` / `c/` namespace | medium | mitigate | `appendSegment` length-prefixed encoding (never concatenation); `TestExcludedFileKeyStartsWithPrefixAndIsolatesNeighbours` (foreign-prefix loop + `/`, NUL, 0xFF path round trip); `DeleteAllExcludedFiles` bounded by `rangeUpperBound([]byte{prefixExcludedFile})` and proven not to touch `f/n/m` in `TestDeleteAllExcludedFilesClearsOnlyTheNamespace` |
| T-10-03 | Denial of Service | `GetCoverage` page size | medium | mitigate | `CoverageMaxPageSize = 1000` clamp in `Engine.CoverageRows` (transport-cap lesson); exercised by Plan 05's clamp test |
| T-10-04 | Tampering / DoS | `GetCoverageRequest.page_token` | medium | mitigate | opaque base64url cursor decoded by `decodeCoverageToken`, validated (kind byte, UTF-8, ≤ 4096 bytes), used ONLY as an in-memory comparison — never a Pebble bound, never a filesystem path; malformed → `query.ErrInvalidArgument` → `CodeInvalidArgument` (Plan 05 test) |
| T-10-05 | Information Disclosure | extraction-failure `detail` carrying the absolute host path from `File.errors` | medium | mitigate | `Engine.CoverageRows` replaces `e.repoRoot` with `.` and cuts detail at 256 bytes (Plan 05 asserts no absolute root in any row) |
| T-10-07 | Repudiation | reasons re-derived at read time (a query-time walk cannot tell a build-tag exclusion from an absent file) | high | mitigate | `TestCoverageSourceNeverWalksDisk` (file-scoped, positive-controlled, D-14b) + `internal/query/archtest` (indexer root forbidden) in this plan; D-14a behavioural test in Plan 02 |
| T-10-08 | Tampering (schema/wire skew) | a UI enum value diverging from the schema enum | low | mitigate | closed enums on both surfaces; `TestExclusionReasonEnumsAgree` pins (name, number) sets both directions; `UNSPECIFIED = 0` is the explicit unknown |
| T-10-09 | Spoofing | DNS rebinding against the new `GetCoverage` mux entry | low | accept | inherited — registers on the same Connect handler `originHostGuard` already wraps (03-SECURITY T-03-13 disposition) |
| T-10-10 | Tampering (torn write) | exclusion records committing separately from `File` records | medium | mitigate | one `NewWriter(`/`Commit()` in `writeGraph` (source gate) + `TestWriteGraphStagesExcludedFilesInTheSameBatch` (`commitCalls == 1`) |
| T-10-11 | Tampering | `Import` of a stream with a forged/unknown record kind | low | mitigate | closed `exportKind*` switch — unknown kind returns the existing error (`TestImportRejectsUnknownRecordKind`); `maxImportRecordBytes` unchanged |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | not applicable — this phase adds no dependency and runs no package-manager install task (RESEARCH: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/schema/... ./internal/graphstore/... ./internal/indexer/... ./internal/query/... ./internal/uiserver/...` — all `ok`
- `GOTOOLCHAIN=go1.26.6 task test:unit` and `task test:golden` — green (the frozen `status.json` goldens unmoved, D-12)
- `GOTOOLCHAIN=go1.26.6 task proto:drift` — MATCH for all four generated files
- `task -s web:drift` — both halves MATCH with `web/build/**` committed alongside `ui_pb.ts`
- `GOTOOLCHAIN=go1.26.6 go vet ./...` clean
</verification>

<success_criteria>
- One `//go:build ignore` file traverses every layer: recorded at Discover's decision point, committed in the one `writeGraph` batch, read back via `graphstore.Reader` only, and visible as `GetHealthResponse.coverage.excluded_by_reason["EXCLUSION_REASON_BUILD_TAG"] == 1` and one `GetCoverage` row over the real listener
- An old graph (no `has_coverage`) reports `known == false` with zero counts and zero rows — never an error, never 0/0 (D-06, D-15)
- `wantUIServiceMethods` = 16 asserted from both sides; decoy `GetIndexCoverage` rejected, `GetCoverage` accepted (D-16); `uiProtoFieldFixtureLenAtPlan1001 = +17`; `has_coverage = 9` in `knownMetaFieldNumbers`
- The `c/` namespace supports put / point delete / range delete and survives Export → Import (Open Question 1: implemented)
</success_criteria>

## Artifacts this phase produces (complete list — Plan 01 lands the items marked ★; later plans land the rest)

- Proto (schema `codegraph.v1`): ★ `ExcludedFile { path=1, reason=2, detail=3, size_bytes=4 }`, ★ `ExclusionReason { EXCLUSION_REASON_UNSPECIFIED=0, _DIR_VENDOR=1, _DIR_DOTPREFIX=2, _UNSUPPORTED_EXTENSION=3, _BUILD_TAG=4, _SIZE_LIMIT=5 }`, ★ `Meta.has_coverage = 9`
- Proto (UI `codegraph.ui.v1`): ★ `Coverage { known=1, discovered=2, indexed=3, excluded=4, extraction_failed=5, excluded_by_reason=6 }`, ★ UI-local `ExclusionReason` (same six values), ★ `CoverageRowKind { UNSPECIFIED=0, EXCLUDED=1, EXTRACTION_FAILED=2 }`, ★ `GetCoverageRequest { page_size=1, page_token=2, reason=3 }`, ★ `CoverageRow { path=1, kind=2, reason=3, detail=4 }`, ★ `GetCoverageResponse { rows=1, next_page_token=2, known=3 }`, ★ `GetHealthResponse.coverage = 17`, ★ `rpc GetCoverage` (16th)
- Go `internal/schema`: ★ `IsDirectoryExclusion(ExclusionReason) bool` (`exclusion.go`), `exclusion_test.go`
- Go `internal/graphstore`: ★ `prefixExcludedFile = 'c'`, ★ `excludedFileKey`, ★ `ExcludedFileIterator`, ★ `Reader.IterateExcludedFiles`, ★ `Writer.PutExcludedFile`, ★ `Writer.DeleteExcludedFile`, ★ `Writer.DeleteAllExcludedFiles`, ★ `pebbleExcludedFileIterator`, ★ `exportKindExcludedFile = 5`, `excludedfile_test.go`
- Go `internal/indexer`: ★ `Discovery`, ★ `DiscoverAll`, ★ `newExcludedFile`, ★ `buildTagDetail`, `dirExclusionReason` (Plan 02), `exceedsSizeLimit` (Plan 02), `unsupportedExtensionDetail` (Plan 02), `sizeLimitDetail` (Plan 02), widened `Resolve`/`resolveFunc`/`writeGraph`, Sync exclusion diff (Plan 03), `discoverexclusion_test.go`, `coverage_fixture_test.go` (Plan 02), `sync_coverage_test.go` (Plan 03), fixture module `internal/indexer/testdata/coverage/{go.mod, main.go, vendor/x.go, .hidden/y.go, notes.md, tagged.go}` (Plan 02)
- Go `internal/query`: ★ `coverage.go` (`CoverageSummary`, `CoverageRow`, `CoverageRowKind`, `CoverageRowExcluded`, `CoverageRowExtractionFailed`, `CoverageRowsOptions`, `CoveragePage`, `CoverageDefaultPageSize`, `CoverageMaxPageSize`, `(*Engine).CoverageSummary`, `(*Engine).CoverageRows`, `encodeCoverageToken`, `decodeCoverageToken`), `coverage_test.go` (★ base; Plan 05 expands)
- Go `internal/uiserver`: ★ `coverage.go` (`coverageToProto`, `exclusionReasonToProto`, `coverageRowKindToProto`, `(*uiService).GetCoverage`), ★ `wantCoverageRPCName`, ★ `uiProtoFieldFixtureLenAtPlan1001`, `coverage_test.go` (★ base; Plan 05 expands)
- TS `web/src/lib`: `health-view.ts` additions `CoverageView`, `toCoverageView`, `reasonLabel`, `REASON_LABELS`, `CoverageGroup`, `groupCoverageRows`, `CoverageClient`, `fetchAllCoverageRows`, `reasonKeyOf` (Plan 04); `components/health/CoverageSection.svelte` (Plan 04); `routes/health/+page.svelte` Coverage section (Plan 04); tests `web/tests/health-view.test.ts`, `web/tests/health-page.test.ts` (Plan 04)
- Docs: `10-MUTATION-LOG.md`, `10-SECURITY.md` (Plan 06)
- Rebuilt `web/build/**` (★ this plan and Plan 04)

<output>
Create `.planning/phases/10-index-health-the-coverage-denominator/10-01-SUMMARY.md` when done — include the pasted Go RED/GREEN transcripts, the Open Question 1 decision, and the exact commit SHAs of the schema commit and the wire commit.
</output>
