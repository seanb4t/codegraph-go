---
phase: 10-index-health-the-coverage-denominator
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - internal/indexer/discover.go
  - internal/indexer/discoverexclusion.go
  - internal/indexer/discoverexclusion_test.go
  - internal/indexer/discover_test.go
  - internal/indexer/coverage_fixture_test.go
  - internal/indexer/testdata/coverage/go.mod
  - internal/indexer/testdata/coverage/main.go
  - internal/indexer/testdata/coverage/vendor/x.go
  - internal/indexer/testdata/coverage/.hidden/y.go
  - internal/indexer/testdata/coverage/notes.md
  - internal/indexer/testdata/coverage/tagged.go
autonomous: true
requirements: [HLT-04, HLT-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All four pre-extraction exclusion reasons are decided inside `DiscoverAll`'s single WalkDir pass and appear in `Discovery.Excluded`: a pruned `vendor` directory → one DIR_VENDOR record, a pruned dot-prefixed directory → one DIR_DOTPREFIX record (D-02, never phantom per-file rows), each unsupported-extension file → one UNSUPPORTED_EXTENSION record with the lower-cased extension in `detail` (D-03), each build-tag miss → one BUILD_TAG record, and each file whose stat size is strictly greater than `parser.MaxSourceBytes` → one SIZE_LIMIT record whose bytes were never read (D-04)"
    - "HLT-05 boundary: a file of exactly `parser.MaxSourceBytes` bytes is NOT excluded (it is discovered and extracted); a file of `parser.MaxSourceBytes + 1` bytes IS recorded as SIZE_LIMIT — the pre-check is strict `>`; the parser's own `ErrSourceTooLarge` remains the backstop and any file that still trips it lands in `File.errors` exactly as today"
    - "`dirExclusionReason(name)` agrees with `ShouldSkipDir(name)` for every name (ok ⇔ ShouldSkipDir), so the watcher and the coverage record can never diverge on which directories are pruned (D-01)"
    - "Against the committed fixture module `internal/indexer/testdata/coverage/` plus its two test-time-generated files, `indexer.Run` yields EXACTLY: indexed 1 (`main.go`), extraction_failed 1 (`broken.py`, a dangling symlink whose `os.ReadFile` fails inside Extract), excluded records {`vendor`:DIR_VENDOR, `.hidden`:DIR_DOTPREFIX, `go.mod`:UNSUPPORTED_EXTENSION `.mod`, `notes.md`:UNSUPPORTED_EXTENSION `.md`, `tagged.go`:BUILD_TAG, `huge.go`:SIZE_LIMIT} and discovered == 1 + 1 + 4 file-level exclusions == 6 — counts reported by the test, not implied (D-13, HLT-04 criterion 1)"
    - "After indexing the fixture, stripping the build tag from `tagged.go` and deleting `notes.md` on disk WITHOUT re-indexing, the store still reports `tagged.go` as BUILD_TAG and `notes.md` as UNSUPPORTED_EXTENSION — a query-time re-walk would report neither (D-14a, HLT-05 criterion 2)"
    - "The `[ASSUMED]` extraction-failure technique is VERIFIED before the fixture depends on it: a probe test proves `DiscoverAll` returns a dangling `.py` symlink as a DiscoveredFile (no walk error — `d.Info()` is lstat-based and Go's build-tag `MatchFile` open is never reached for a non-Go file) and `Extract` records a read error on it"
    - "HLT-05 precision: every count is an int64 produced by counting records — no division, rounding or subtraction anywhere in the summary path; the fixture test asserts the additive invariant `discovered == indexed + extraction_failed + file-level excluded` from the reported numbers"
  artifacts:
    - "internal/indexer/discoverexclusion.go — `dirExclusionReason(name string) (schema.ExclusionReason, bool)`, `exceedsSizeLimit(sizeBytes int64) bool`, `unsupportedExtensionDetail(ext string) string`, `sizeLimitDetail(sizeBytes int64) string`"
    - "internal/indexer/testdata/coverage/ — go.mod, main.go, vendor/x.go, .hidden/y.go, notes.md, tagged.go (committed); huge.go and broken.py generated at test time"
    - "internal/indexer/coverage_fixture_test.go — `TestCoverageFixtureCountsAndReasons`, `TestCoverageReasonsSurviveDiskMutationWithoutReindex`, `TestDanglingSymlinkIsDiscoveredAndFailsExtraction`"
  key_links:
    - "WalkDir callback → decision points 1 (dir prune), 2 (extension miss), 3 (build-tag miss), 4 (stat size > MaxSourceBytes) → `Discovery.Excluded` → Plan 01's writeGraph loop"
    - "`Discovery.Files` (unchanged semantics) → Extract → `File.errors` for `broken.py` → `CoverageSummary.ExtractionFailed`"
  prohibitions:
    - "SIZE_LIMIT MUST be decided from `d.Info().Size()` — the file's bytes are never read at discovery time"
    - "Pruned directories MUST NOT produce per-file records and MUST NOT count toward `discovered` — their contents are never enumerated (D-01)"
    - "The oversize file and the dangling symlink MUST NOT be committed — both are generated inside the test's temp copy of the fixture"
    - "`Discover(root)`'s return value MUST NOT change: it still returns only the files that reach extraction, in RelPath order"
---

<objective>
Expand `DiscoverAll` from the tracer's one reason to all four pre-extraction exclusion reasons plus the stat-based size pre-check, then prove HLT-04 criterion 1 and HLT-05 criterion 2 against a committed fixture module: exact counts and per-file reasons through the real pipeline, and reasons that survive a disk mutation with no re-index.

Purpose: D-01's "discovered" definition and D-02/D-03/D-04's record shapes are what every reported number rests on; pinning them in one helper with exhaustive unit tests and one end-to-end fixture is how the denominator and the reason list stay in agreement.

Output: the four decision points wired in `discoverexclusion.go`/`discover.go`; the `testdata/coverage/` fixture module; `coverage_fixture_test.go` with the counts test, the D-14a mutate-without-reindex test and the symlink probe.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-index-health-the-coverage-denominator/10-CONTEXT.md
@.planning/phases/10-index-health-the-coverage-denominator/10-RESEARCH.md
@.planning/phases/10-index-health-the-coverage-denominator/10-01-SUMMARY.md

Every Go command: `GOTOOLCHAIN=go1.26.6`. Plan 01 landed `Discovery`, `DiscoverAll`, `newExcludedFile`, `buildTagDetail`, the BUILD_TAG capture, and `IsDirectoryExclusion` in `internal/schema` — read `10-01-SUMMARY.md` for the exact names it committed before extending.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The other three reasons and the size pre-check — pure decision helpers with boundary tests, wired into the walk</name>
  <reversibility rating="costly">D-01's "discovered = every regular file the walker visits" is rated costly by CONTEXT — changing it later changes every reported number. This task implements it as specified; no checkpoint.</reversibility>
  <files>internal/indexer/discoverexclusion.go, internal/indexer/discover.go, internal/indexer/discoverexclusion_test.go, internal/indexer/discover_test.go</files>
  <read_first>
    - internal/indexer/discoverexclusion.go and internal/indexer/discover.go as committed by Plan 01 (the moved WalkDir body, `newExcludedFile`, `buildTagDetail`, where `d.Info()` now runs)
    - internal/indexer/discover.go `ShouldSkipDir` (the two OR'd branches the directory reason must re-inspect — research Pattern 1) and the doc comment tying it to the fsnotify watcher (Phase 4 D-04)
    - internal/indexer/discover_test.go:106-133 (`TestDiscover_SkipsVendorAndDotDirs`, `mustWrite`), :163-235 (mixed-language fixture tests — `config.json`/`README.md` in `testdata/mixedlangfixture` become UNSUPPORTED_EXTENSION records; their `Discover`-based assertions must be untouched)
    - internal/parser/parser.go:1-20 (`MaxSourceBytes = 4 * 1024 * 1024`, `ErrSourceTooLarge`), internal/indexer/extract.go:100-140 (the read path the size check now pre-empts), internal/indexer/extract_test.go:212-251 (`TestExtractPool_OversizedFileContained` — feeds Extract directly and MUST keep passing: the parser backstop is unchanged)
    - internal/indexer/languages.go (`lookupLanguageByExt` — the extension registry; note `.mod` is not registered so `go.mod` is an UNSUPPORTED_EXTENSION record)
    - internal/schema/exclusion.go (`IsDirectoryExclusion`)
  </read_first>
  <behavior>
    - Test 1 (`discoverexclusion_test.go` `TestDirExclusionReasonAgreesWithShouldSkipDir`): table {"vendor"→DIR_VENDOR,true; ".git"→DIR_DOTPREFIX,true; ".codegraph"→DIR_DOTPREFIX,true; "."→(the same verdict ShouldSkipDir gives); "src"→false; "vendored"→false; "node_modules"→false (pre-existing behaviour, deferred idea)}; for EVERY row `ok == ShouldSkipDir(name)`; the table has at least 7 rows and at least one row of each verdict (logged).
    - Test 2 (`TestExceedsSizeLimitIsStrictlyGreaterThan`): `exceedsSizeLimit(parser.MaxSourceBytes - 1) == false`, `exceedsSizeLimit(parser.MaxSourceBytes) == false`, `exceedsSizeLimit(parser.MaxSourceBytes + 1) == true`, `exceedsSizeLimit(0) == false` (HLT-05 boundary, one step either side).
    - Test 3 (`TestDiscoverAll_RecordsAllFourReasons`): temp root with `go.mod`, `real.go`, `notes.md`, `data.JSON` (upper-case extension → detail ".json", lower-cased like the registry lookup), `tagged.go` (`//go:build ignore`), `vendor/x.go`, `.hidden/y.go`, `.hidden/deep/z.go`, `vendor/sub/w.go`, and `huge.go` written with exactly `parser.MaxSourceBytes + 1` bytes (a `package tmp` line padded with a comment — generated in the test, never committed) → `Excluded` == exactly {`.hidden`:DIR_DOTPREFIX detail ".hidden" size 0, `data.JSON`:UNSUPPORTED_EXTENSION ".json", `go.mod`:UNSUPPORTED_EXTENSION ".mod", `huge.go`:SIZE_LIMIT detail = decimal byte count, size MaxSourceBytes+1, `notes.md`:UNSUPPORTED_EXTENSION ".md", `tagged.go`:BUILD_TAG, `vendor`:DIR_VENDOR detail "vendor" size 0} in byte-sorted path order (7 records — logged count asserted == 7), `Files` == ["real.go"], and NO record for any path under `vendor/` or `.hidden/` (the phantom-row check, D-02).
    - Test 4 (`TestDiscoverAll_ExactSizeLimitFileIsNotExcluded`): a `.go` file of exactly `parser.MaxSourceBytes` bytes is in `Files` and absent from `Excluded`.
    - Test 5 (`TestDiscoverAll_ExcludedSortedAndDeterministic`): two calls on the same tree return `proto.Equal`-equal `Excluded` slices in the same order.
    - Existing `TestDiscover_*` tests remain byte-identical and green (the `Discover` wrapper's contract is unchanged).
  </behavior>
  <action>
    In `discoverexclusion.go` add the pure helpers (unit-testable without a filesystem, per D-01 "pinned in ONE helper"): `dirExclusionReason(name string) (schema.ExclusionReason, bool)` — returns (DIR_VENDOR, true) when `name == "vendor"`, (DIR_DOTPREFIX, true) when `strings.HasPrefix(name, ".")`, else (UNSPECIFIED, false); its doc comment MUST state it re-inspects exactly the two branches `ShouldSkipDir` OR's together and that Test 1 pins the agreement (do not call `ShouldSkipDir` from inside it — the agreement is the guard, not a tautology); `exceedsSizeLimit(sizeBytes int64) bool` returning `sizeBytes > parser.MaxSourceBytes` (strict — D-04; the parser's `ErrSourceTooLarge` stays the backstop for a file that grows between the discovery stat and the read); `unsupportedExtensionDetail(ext string) string` returning the already-lower-cased extension as computed by the walk (empty extension → "(none)"); `sizeLimitDetail(sizeBytes int64) string` returning `strconv.FormatInt(sizeBytes, 10) + " bytes > " + strconv.Itoa(parser.MaxSourceBytes)`.

    Wire the walk callback in the function Plan 01 moved (keep decision points in the order research verified at discover.go:101-156): decision point 1 — inside `if d.IsDir()`, when `p != root` and `dirExclusionReason(d.Name())` is ok, append `newExcludedFile(relPath, reason, d.Name(), 0)` (compute `relPath` for the directory the same way as for files) and return `fs.SkipDir`; keep calling `ShouldSkipDir` as the prune predicate so the watcher-shared function remains the single prune authority (the reason helper only classifies). Decision point 2 — when `lookupLanguageByExt(ext)` misses, append UNSUPPORTED_EXTENSION with `unsupportedExtensionDetail(ext)` and `info.Size()`. Decision point 3 is already BUILD_TAG. Decision point 4 — after the build-tag check and before appending to `pending`, when `exceedsSizeLimit(info.Size())` append SIZE_LIMIT with `sizeLimitDetail(info.Size())` and return nil (the bytes are never read; `pendingFile` is not appended). Files under a pruned directory are never visited, so no record and no discovered count for them (D-01/D-02). Keep `Excluded` sorted by Path after the walk.

    Update `Discover`'s doc comment (the wrapper) to state it returns the extraction-bound subset of `DiscoverAll`. `Discover_test.go`'s `TestDiscover_SkipsVendorAndDotDirs` gets a sibling assertion (or a new sibling test) that `DiscoverAll` on the same tree yields exactly one DIR_VENDOR and one DIR_DOTPREFIX record — no per-file rows. Write Tests 1–5 RED first, then GREEN; run the whole `./internal/indexer/...` package plus `task test:golden` (the corpus repos contain `.md`/`.json` files — their `status.json` goldens must not move because `Stats.Files`, `FileCount` and every `f/` iterator keep their meaning).
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestDirExclusionReasonAgreesWithShouldSkipDir|TestExceedsSizeLimitIsStrictlyGreaterThan|TestDiscoverAll_RecordsAllFourReasons|TestDiscoverAll_ExactSizeLimitFileIsNotExcluded|TestDiscoverAll_ExcludedSortedAndDeterministic' ./internal/indexer/ 2&gt;&amp;1 | tee /tmp/10-02-t1-red.txt; test "$(rg -o -- '--- FAIL|undefined:|build failed' /tmp/10-02-t1-red.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>RED gate: no FAIL/undefined line means the tests were not written before the helpers</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestDirExclusionReasonAgreesWithShouldSkipDir|TestExceedsSizeLimitIsStrictlyGreaterThan|TestDiscoverAll_RecordsAllFourReasons|TestDiscoverAll_ExactSizeLimitFileIsNotExcluded|TestDiscoverAll_ExcludedSortedAndDeterministic|TestDiscover_SkipsVendorAndDotDirs|TestDiscover_MixedLanguage_ExtensionRegistry|TestExtractPool_OversizedFileContained' ./internal/indexer/ 2&gt;&amp;1 | tee /tmp/10-02-t1-green.txt | rg -c -- '^--- PASS: Test' &amp;&amp; test "$(rg -o -- '--- PASS: Test(DirExclusionReasonAgreesWithShouldSkipDir|ExceedsSizeLimitIsStrictlyGreaterThan|DiscoverAll_RecordsAllFourReasons|DiscoverAll_ExactSizeLimitFileIsNotExcluded|DiscoverAll_ExcludedSortedAndDeterministic|Discover_SkipsVendorAndDotDirs|Discover_MixedLanguage_ExtensionRegistry|ExtractPool_OversizedFileContained)' /tmp/10-02-t1-green.txt | wc -l | tr -d ' ')" = "8" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/10-02-t1-green.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; ! rg -q 'no tests to run' /tmp/10-02-t1-green.txt &amp;&amp; test "$(rg -o 'excluded records: 7' /tmp/10-02-t1-green.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>fewer than 8 named PASS lines, any FAIL, "no tests to run", or the four-reasons test does not log `excluded records: 7` (the positive count that proves it inspected real records)</fails_when>
    <automated>test "$(rg -o 'func dirExclusionReason\(name string\) \(schema.ExclusionReason, bool\)|func exceedsSizeLimit\(sizeBytes int64\) bool|func unsupportedExtensionDetail\(ext string\) string|func sizeLimitDetail\(sizeBytes int64\) string' internal/indexer/discoverexclusion.go | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o 'sizeBytes &gt; parser\.MaxSourceBytes' internal/indexer/discoverexclusion.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'EXCLUSION_REASON_(DIR_VENDOR|DIR_DOTPREFIX|UNSUPPORTED_EXTENSION|BUILD_TAG|SIZE_LIMIT)' internal/indexer/discoverexclusion.go internal/indexer/discover.go | sort -u | wc -l | tr -d ' ')" = "5" &amp;&amp; test "$(rg -o 'func ShouldSkipDir\(name string\) bool' internal/indexer/discover.go | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>a helper is missing or mis-signed, the size comparison is not strict `>`, fewer than all five reasons are produced by the discovery files, or `ShouldSkipDir` was moved/renamed (the watcher shares it)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/indexer/... ./internal/query/... ./internal/uiserver/... &gt;/tmp/10-02-t1-pkgs.txt 2&gt;&amp;1; RC=$?; tail -4 /tmp/10-02-t1-pkgs.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^FAIL' /tmp/10-02-t1-pkgs.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:golden &gt;/tmp/10-02-t1-golden.txt 2&gt;&amp;1; RC=$?; tail -2 /tmp/10-02-t1-golden.txt; test "$RC" = "0"</automated>
    <fails_when>any package or the golden suite fails — the Plan 01 tracer tests assert by reason and invariant, so they must survive the added reasons; a golden move means `Stats.Files`/`FileCount` semantics drifted</fails_when>
  </verify>
  <acceptance_criteria>
    - `TestDiscoverAll_RecordsAllFourReasons` asserts the full expected slice with `proto.Equal` per record AND path order, and logs `excluded records: 7`
    - `TestDirExclusionReasonAgreesWithShouldSkipDir` compares `ok` against `ShouldSkipDir(name)` on every row (`rg -o 'ShouldSkipDir\(' internal/indexer/discoverexclusion_test.go | wc -l` >= 1) while the BODY of `dirExclusionReason` itself does not call `ShouldSkipDir` (reviewed by reading the function — the walk callback in the same file legitimately keeps calling `ShouldSkipDir` as the prune predicate; the agreement test is the guard, not a tautology)
    - The oversize test files are created with `os.WriteFile` inside `t.TempDir()`; `git status --porcelain internal/indexer/testdata` is empty after the run
    - SUMMARY pastes the RED and GREEN transcripts
  </acceptance_criteria>
  <done>Every pre-extraction exclusion is recorded at its decision point with the right reason, detail and size, the size pre-check is strict at the boundary, directory prunes yield one record and no phantom rows, and the existing discovery/extraction tests are untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The committed coverage fixture module, the verified extraction-failure technique, the exact-counts test and the mutate-without-reindex test (D-13, D-14a)</name>
  <reversibility rating="reversible">Test data and tests only; the fixture can be reshaped freely as long as the counts test moves with it.</reversibility>
  <files>internal/indexer/testdata/coverage/go.mod, internal/indexer/testdata/coverage/main.go, internal/indexer/testdata/coverage/vendor/x.go, internal/indexer/testdata/coverage/.hidden/y.go, internal/indexer/testdata/coverage/notes.md, internal/indexer/testdata/coverage/tagged.go, internal/indexer/coverage_fixture_test.go</files>
  <read_first>
    - internal/indexer/testdata/gofixture/go.mod and internal/indexer/testdata/prunefixture/ (fixture-module conventions: `module example.com/<name>` + `go 1.26`)
    - internal/indexer/prune_fixtures_test.go:1-60 and internal/uiserver/server_test.go:29-72 (`copyGofixture` — the WalkDir-based fixture copy that must be replicated here for `testdata/coverage`; it copies dot-dirs and `vendor/` too because it never calls ShouldSkipDir)
    - internal/indexer/extract.go:100-135 (the per-file `os.ReadFile` failure path that records `FileResult.Err`), internal/indexer/resolve.go:285-305 (how `FileResult.Err` becomes `File.errors`)
    - internal/indexer/discover.go / discoverexclusion.go as left by Task 1 (`d.Info()` before the extension check; Go-only `MatchFile` — the reason the dangling symlink must NOT have a `.go` extension: `go/build.Context.MatchFile` opens `.go` files to read build constraints and a dangling target would turn into a walk error)
    - internal/indexer/languages_python.go:65-75 (`.py` is registered — the non-Go extension the symlink uses)
    - internal/graphstore/store.go (`Reader.IterateFiles`, `IterateExcludedFiles`, `GetFile`) — the read-back in these tests goes through graphstore directly, never through a second walk
    - .planning/phases/10-index-health-the-coverage-denominator/10-RESEARCH.md Pitfall 4 and Assumptions Log A1 (the `[ASSUMED]` technique this task must verify first)
  </read_first>
  <behavior>
    - Test 0 — the probe, written and run FIRST as a RED gate on the assumption (`TestDanglingSymlinkIsDiscoveredAndFailsExtraction`): in a temp root with `go.mod` and `main.go`, create `broken.py` via `os.Symlink("missing-target.py", filepath.Join(root, "broken.py"))`; `DiscoverAll(root)` returns nil error with `broken.py` in `Files` (Language "python") and NOT in `Excluded`; `Extract(files, 1)` returns a `FileResult` for `broken.py` whose `Err != nil` and `errors.Is(err, fs.ErrNotExist)`. If this probe FAILS on the first run, STOP: record the observed behaviour in the SUMMARY and switch the fixture to the fallback technique (a `.py` file made unreadable with `os.Chmod(0o000)`, skipped when `os.Geteuid() == 0`) — never commit an assumption as a fixture.
    - Test 1 (`TestCoverageFixtureCountsAndReasons`, D-13 / HLT-04 criterion 1): copy `testdata/coverage` to a temp dir (own WalkDir copy helper), generate `huge.go` (`parser.MaxSourceBytes + 1` bytes) and the `broken.py` dangling symlink inside the copy, index it with `indexer.Run(copy, t.TempDir()-store, Options{Quiet:true})` (store OUTSIDE the copy so `.codegraph` does not add a second DIR_DOTPREFIX record — the count table below is exact), then read back through `graphstore.Open` + `Snapshot`: (a) `IterateExcludedFiles` yields EXACTLY these (path → reason, detail) rows in this order: `.hidden`→DIR_DOTPREFIX ".hidden"; `go.mod`→UNSUPPORTED_EXTENSION ".mod"; `huge.go`→SIZE_LIMIT `<n> bytes > 4194304`; `notes.md`→UNSUPPORTED_EXTENSION ".md"; `tagged.go`→BUILD_TAG `GOOS/GOARCH`; `vendor`→DIR_VENDOR "vendor" (6 records, count logged and asserted == 6); (b) `IterateFiles` yields exactly `broken.py` (errors non-empty) and `main.go` (errors empty) — 2 File records; (c) the test COMPUTES and LOGS indexed=1, extraction_failed=1, excluded=6, file-level excluded=4, discovered=1+1+4=6 and asserts each, plus the invariant `discovered == indexed + extraction_failed + fileLevelExcluded`; (d) `GetMeta().GetHasCoverage() == true`; (e) NO File record and NO ExcludedFile record exists for `vendor/x.go` or `.hidden/y.go`.
    - Test 2 (`TestCoverageReasonsSurviveDiskMutationWithoutReindex`, D-14a / HLT-05 criterion 2): same setup as Test 1; after `Run`, close the store; on disk: rewrite `tagged.go` WITHOUT its `//go:build ignore` line and `os.Remove(notes.md)`; re-open the store (a fresh process-equivalent handle — `graphstore.Open` again) and assert `IterateExcludedFiles` STILL yields `tagged.go`→BUILD_TAG and `notes.md`→UNSUPPORTED_EXTENSION, and that `DiscoverAll(copy)` run NOW would report neither (call it and assert `tagged.go` is in `Files` and `notes.md` is absent everywhere) — the explicit contrast that a query-time walk gives the wrong answer; comment: `mutation: re-derive rows from a walk → RED` (Plan 06 family (c)).
  </behavior>
  <action>
    Commit the fixture module `internal/indexer/testdata/coverage/`: `go.mod` (`module example.com/coveragefixture` / `go 1.26`), `main.go` (`package main` with one exported func so `indexed == 1` carries at least one node), `vendor/x.go` (`package x`), `.hidden/y.go` (`package y`), `notes.md` (two lines of prose), `tagged.go` (`//go:build ignore`, blank line, `package main`). Do NOT commit an oversize file or a symlink (the repo has zero committed symlinks; keep it that way — both are generated per test in the temp copy). Verify the fixture files are git-tracked after the commit (`git ls-files internal/indexer/testdata/coverage` lists all six — dot-dir included).

    Create `internal/indexer/coverage_fixture_test.go` with a `copyCoverageFixture(t) string` helper (WalkDir copy of `testdata/coverage` into `t.TempDir()`, copying dot-dirs and vendor), `writeOversizeGoFile(t, dir, name)` (exactly `parser.MaxSourceBytes + 1` bytes: `package main\n// ` followed by padding), and `writeDanglingSymlink(t, dir, name)`. Order of work: write Test 0 first and RUN it before writing Tests 1–2 — it is the RED gate on research assumption A1. Then Tests 1–2. All read-backs go through `graphstore` (`Open` → `Snapshot` → iterators); the tests may import `indexer` (they are in-package) and `graphstore`, but must not implement any "expected reason" logic by re-walking — expected values are literal tables. Log every count with `t.Logf("indexed=%d extraction_failed=%d excluded=%d file_level=%d discovered=%d", ...)` BEFORE asserting (rule 84d1gfpywd: counts reported, not implied).
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestDanglingSymlinkIsDiscoveredAndFailsExtraction' ./internal/indexer/ 2&gt;&amp;1 | tee /tmp/10-02-probe.txt | rg -c -- '^--- (PASS|FAIL): Test' &amp;&amp; test "$(rg -o -- '--- PASS: TestDanglingSymlinkIsDiscoveredAndFailsExtraction' /tmp/10-02-probe.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the probe does not PASS — the `[ASSUMED]` symlink technique is invalid on this platform and the fallback (chmod-unreadable `.py`) must be used and recorded; a FAIL here is a legitimate research outcome, not a plan error, and must be written into the SUMMARY verbatim</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestCoverageFixtureCountsAndReasons|TestCoverageReasonsSurviveDiskMutationWithoutReindex' ./internal/indexer/ 2&gt;&amp;1 | tee /tmp/10-02-fixture.txt | rg -c -- '^--- PASS: Test' &amp;&amp; test "$(rg -o -- '--- PASS: Test(CoverageFixtureCountsAndReasons|CoverageReasonsSurviveDiskMutationWithoutReindex)' /tmp/10-02-fixture.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/10-02-fixture.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'indexed=1 extraction_failed=1 excluded=6 file_level=4 discovered=6' /tmp/10-02-fixture.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>either test fails or the logged count line `indexed=1 extraction_failed=1 excluded=6 file_level=4 discovered=6` is absent — the counts must be REPORTED by the test output, not merely asserted internally</fails_when>
    <automated>test "$(git ls-files internal/indexer/testdata/coverage | sort | tr '\n' ' ')" = "internal/indexer/testdata/coverage/.hidden/y.go internal/indexer/testdata/coverage/go.mod internal/indexer/testdata/coverage/main.go internal/indexer/testdata/coverage/notes.md internal/indexer/testdata/coverage/tagged.go internal/indexer/testdata/coverage/vendor/x.go " &amp;&amp; test "$(git ls-files -s internal/indexer/testdata/coverage | rg -o '^120000' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^//go:build ignore$' internal/indexer/testdata/coverage/tagged.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(find internal/indexer/testdata/coverage -type f -exec cat {} + | wc -c | tr -d ' ')" -lt 65536</automated>
    <fails_when>the tracked fixture set is not exactly the six committed files (a dot-dir dropped by a global gitignore, or an oversize/symlink file accidentally committed), a symlink mode is present, the build tag is missing, or the fixture directory on disk holds more than 64 KiB (an oversize file left behind)</fails_when>
    <automated>test "$(rg -o 'mutation: re-derive rows from a walk' internal/indexer/coverage_fixture_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'IterateExcludedFiles\(\)' internal/indexer/coverage_fixture_test.go | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o 'os\.Symlink\(' internal/indexer/coverage_fixture_test.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'parser\.MaxSourceBytes ?\+ ?1' internal/indexer/coverage_fixture_test.go | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>the Plan-06 mutation anchor comment, the graphstore read-back, the symlink generation or the MaxSourceBytes+1 generation is missing from the test file</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files internal/indexer/testdata/coverage` lists exactly 6 files, none with mode 120000
    - `TestCoverageFixtureCountsAndReasons` logs the count line and asserts the 6-row exclusion table by (path, reason, detail) in byte order, the 2 File records, and the additive invariant
    - `TestCoverageReasonsSurviveDiskMutationWithoutReindex` re-opens the store after mutating the disk and asserts both stale reasons persist while a fresh `DiscoverAll` disagrees
    - The SUMMARY records the probe's verdict on assumption A1 (PASS with the symlink technique, or the fallback adopted and why)
  </acceptance_criteria>
  <done>The fixture repo contains one file of every kind, the indexer reports exact counts and per-file reasons for it, extraction failures are distinguished from exclusions in the read-back, and the reasons survive a disk mutation with no re-index — each proven by a test that prints the numbers it checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository filesystem → `DiscoverAll` | untrusted directory/file names and sizes become `ExcludedFile.path`/`detail`/`size_bytes` |
| test-time fixture generation → temp dir | oversize file and dangling symlink are created only inside `t.TempDir()` copies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | walker-produced `path`/`detail` strings persisted for later rendering | high | mitigate | same disposition as `File.path` today (walker-produced, unsanitized at write time); the render boundary (Plan 04) uses Svelte text interpolation only and is tested with a path containing markup; `detail` is bounded by construction here (extension ≤ ext length, decimal byte count, `GOOS/GOARCH`, directory name) |
| T-10-06 | Denial of Service | a multi-megabyte file reaching the tree-sitter C scanner | medium | mitigate | D-04's stat-based `exceedsSizeLimit` pre-check short-circuits BEFORE any read; `parser.ErrSourceTooLarge` remains the backstop (`TestExtractPool_OversizedFileContained` unchanged and green) |
| T-10-07 | Repudiation | reasons re-derived at read time | high | mitigate | `TestCoverageReasonsSurviveDiskMutationWithoutReindex` (D-14a) proves the stored reason contradicts a fresh walk after a disk mutation |
| T-10-12 | Tampering | a dangling symlink or unreadable file turning a whole `Discover` into an error (availability of the index) | low | accept | pre-existing behaviour for `.go` symlinks (Go's `MatchFile` opens the file); the probe test documents the boundary; non-Go read failures are already per-file (`extract.go`) |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | not applicable — no package-manager install in this phase |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/indexer/...` green; `task test:golden` green (corpus repos gain exclusion records but `status.json` goldens do not move)
- The Plan 01 tracer tests (`TestCoverageSummaryAfterRunOnBuildTagRepo`, `TestGetHealthCarriesCoverageForBuildTagExclusion`) still pass — they assert by reason and invariant, never by hard-coded totals
</verification>

<success_criteria>
- Four reasons + size pre-check decided in one walk; boundary strict at `MaxSourceBytes`; helper/`ShouldSkipDir` agreement pinned
- Fixture module committed (6 files, no symlink, no oversize); exact counts `indexed=1 extraction_failed=1 excluded=6 file_level=4 discovered=6` reported by the test
- D-14a: stale reasons persist after disk mutation with no re-index; a fresh walk provably disagrees
- Assumption A1 verified (or replaced) by a probe test before any fixture depended on it
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `internal/indexer/discoverexclusion.go`: `dirExclusionReason`, `exceedsSizeLimit`, `unsupportedExtensionDetail`, `sizeLimitDetail`; decision points 1, 2, 4 wired in the walk (3 landed in Plan 01)
- `internal/indexer/testdata/coverage/` fixture module: `go.mod`, `main.go`, `vendor/x.go`, `.hidden/y.go`, `notes.md`, `tagged.go`
- `internal/indexer/coverage_fixture_test.go`: `copyCoverageFixture`, `writeOversizeGoFile`, `writeDanglingSymlink`, `TestDanglingSymlinkIsDiscoveredAndFailsExtraction`, `TestCoverageFixtureCountsAndReasons`, `TestCoverageReasonsSurviveDiskMutationWithoutReindex`
- `internal/indexer/discoverexclusion_test.go`: `TestDirExclusionReasonAgreesWithShouldSkipDir`, `TestExceedsSizeLimitIsStrictlyGreaterThan`, `TestDiscoverAll_RecordsAllFourReasons`, `TestDiscoverAll_ExactSizeLimitFileIsNotExcluded`, `TestDiscoverAll_ExcludedSortedAndDeterministic`

<output>
Create `.planning/phases/10-index-health-the-coverage-denominator/10-02-SUMMARY.md` when done — include the probe verdict on assumption A1, the pasted count line, and the RED/GREEN transcripts.
</output>
