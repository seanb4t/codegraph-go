---
phase: 10-index-health-the-coverage-denominator
plan: 06
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05"]
files_modified:
  - .planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md
  - .planning/phases/10-index-health-the-coverage-denominator/10-SECURITY.md
  - .planning/phases/10-index-health-the-coverage-denominator/10-VALIDATION.md
autonomous: true
requirements: [HLT-04, HLT-05, HLT-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`10-MUTATION-LOG.md` follows the 07/08/09 house format: a pre-mutation cleanliness gate (`git diff --quiet -- <file>` exit 0 before every mutation and before every revert), the exact commands run, the RED output pasted verbatim, and a byte-clean revert — for three families: (a) D-15 unset-`has_coverage` read as zero, (b) D-16 the rpc fixture renamed to the `GetIndexCoverage` decoy, (c) D-14 rows filtered by present-tense disk state (a query-time reconstruction) — every family RED on the named tests, then green again after revert (D-17)"
    - "`10-SECURITY.md` is authored in the 09-SECURITY.md house format (frontmatter `status: verified`, `threats_open: 0`, `asvs_level: 1`; Trust Boundaries table; the one-sentence boundary statement; a Threat Register where EVERY T-10-xx row from Plans 01–05 carries either a named test or an explicit verdict; Accepted Risks; Notes recording Open Questions 1 and 3 as decided) — the write path's new trust surface (walker-produced strings persisted then rendered) is the register's headline (D-17)"
    - "Phase-close gate: `GOTOOLCHAIN=go1.26.6 go vet ./...`, `task test:unit`, `task test:golden`, `task proto:drift`, `task web:test`, `pnpm -C web check` (0 errors), and `task -s web:drift` (both halves MATCH) are all green at the final commit, with the tails pasted in the SUMMARY"
    - "`10-VALIDATION.md`'s Per-Task Verification Map has every TBD cell filled with the real plan / task / test names and commands from Plans 01–05 (values only — no new headings or shapes), and every Wave 0 checklist item that landed is ticked"
  artifacts:
    - ".planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md"
    - ".planning/phases/10-index-health-the-coverage-denominator/10-SECURITY.md"
  key_links:
    - "family (a) → `internal/query/coverage.go` unknown guard → `TestCoverageSummaryOnOldGraphIsUnknown` + `TestGetCoverageAndGetHealthOnOldGraphAreKnownFalse`"
    - "family (b) → `internal/uiserver/readonly_test.go` `wantUIServiceMethods` entry → `TestUIServiceMethodSetIsExactlyTheReadSet` + `TestCoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected`"
    - "family (c) → `internal/query/coverage.go` row loop → `TestCoverageSourceNeverWalksDisk` + `TestCoverageRowsSurviveDiskMutationWithoutReindex`"
  prohibitions:
    - "A non-failing mutation MUST be reported as an observed negative result with its root cause — never rewritten until it fails (08-MUTATION-LOG family (d) precedent)"
    - "No mutation may be committed — every family ends with `git diff --quiet -- <file>` exit 0"
---

<objective>
Close the phase with evidence: three hand-authored RED demonstrations proving the guards discriminate (D-15, D-16, D-14), the security register with every threat row test-or-verdict, the validation map filled with what actually landed, and the full gate suite green.

Purpose: D-17 is the phase's own proof that its guards cannot go vacuous — a guard that was never watched failing is a guard whose green means nothing.

Output: `10-MUTATION-LOG.md`, `10-SECURITY.md`, `10-VALIDATION.md` map filled, phase-close gate transcripts in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-index-health-the-coverage-denominator/10-CONTEXT.md
@.planning/phases/10-index-health-the-coverage-denominator/10-VALIDATION.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md
@.planning/phases/10-index-health-the-coverage-denominator/10-01-SUMMARY.md
@.planning/phases/10-index-health-the-coverage-denominator/10-02-SUMMARY.md
@.planning/phases/10-index-health-the-coverage-denominator/10-03-SUMMARY.md
@.planning/phases/10-index-health-the-coverage-denominator/10-04-SUMMARY.md
@.planning/phases/10-index-health-the-coverage-denominator/10-05-SUMMARY.md

Every Go command: `GOTOOLCHAIN=go1.26.6`. Mutations are applied to tracked files with an editor, run, pasted, and reverted with `git checkout -- <file>`; agent commits MAY use `-c commit.gpgsign=false`; never `[ci skip]`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 10-MUTATION-LOG.md — families (a) D-15, (b) D-16, (c) D-14, each RED then byte-clean reverted</name>
  <reversibility rating="reversible">Documentation of temporary, reverted mutations; nothing persists in source.</reversibility>
  <files>.planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md (whole — the header, the pre-mutation cleanliness gate section, per-family "Instrument / Mutation / Exact commands / RED pasted verbatim / Revert" structure)
    - .planning/phases/08-tmux-real-pty-harness/08-MUTATION-LOG.md family (d) (how a non-failing demonstration is recorded honestly) — load only its family (d) section
    - internal/query/coverage.go (the unknown short-circuit in `CoverageSummary`/`CoverageRows`, the segment loops), internal/query/coverage_test.go (`TestCoverageSummaryOnOldGraphIsUnknown` — its `mutation: treat unset as zero` anchor; `TestCoverageRowsSurviveDiskMutationWithoutReindex` — its `mutation: filter rows by present-tense disk state` anchor; `TestCoverageSourceNeverWalksDisk`)
    - internal/uiserver/readonly_test.go (`wantUIServiceMethods`'s `"GetCoverage"` entry, `TestUIServiceMethodSetIsExactlyTheReadSet`, `TestCoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected`), internal/uiserver/coverage_test.go (`TestGetCoverageAndGetHealthOnOldGraphAreKnownFalse`)
  </read_first>
  <action>
    Author `10-MUTATION-LOG.md` with the 09 header block (Phase, Date, Scope naming the three families) and the pre-mutation cleanliness gate section, then one section per family. For each family: state the instrument (the tests that must go RED), apply the mutation to the tracked file with `git diff --quiet -- <file>` asserted BEFORE (paste `echo $?` = 0), run the named tests, paste the verbatim RED output (the `--- FAIL:` lines and the failure messages — enough to see WHICH assertion fired), revert with `git checkout -- <file>`, assert `git diff --quiet -- <file>` again (paste `echo $?` = 0), and re-run the same tests to paste the GREEN confirmation.

    Family (a) — D-15: in `internal/query/coverage.go` change the unknown short-circuit so an absent/false `has_coverage` falls through to counting (treat unset as zero). Instrument: `GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestCoverageSummaryOnOldGraphIsUnknown|TestCoverageRowsUnknownGraphAndSingleRow' ./internal/query/` and `GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestGetCoverageAndGetHealthOnOldGraphAreKnownFalse' ./internal/uiserver/` — both must print `--- FAIL`.

    Family (b) — D-16: in `internal/uiserver/readonly_test.go` replace the `"GetCoverage": {}` entry of `wantUIServiceMethods` with `"GetIndexCoverage": {}` (the decoy). Instrument: `GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestUIServiceMethodSetIsExactlyTheReadSet|TestCoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected' ./internal/uiserver/` — the set-equality guard must fail in BOTH directions (missing expected `GetIndexCoverage` / unexpected `GetCoverage` — paste whichever direction fires first, and note the other), and the table test must fail on the clean name no longer being a member.

    Family (c) — D-14: in `internal/query/coverage.go`'s exclusion-row loop add a present-tense disk filter that skips a row when a file-stat of `filepath.Join(e.repoRoot, path)` reports the path absent (a query-time reconstruction: an excluded file deleted from disk "disappears"). Instrument: `GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestCoverageSourceNeverWalksDisk|TestCoverageRowsSurviveDiskMutationWithoutReindex' ./internal/query/` — the structural scan must FAIL on the forbidden stat call AND the behavioural test must FAIL on the missing `notes.md` row; also run `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/query/archtest/` and record its result (it stays green — the mutation used `os`, not the indexer root; say so explicitly, that is what D-14b's file-scoped scan exists to catch).

    If any family does NOT go RED, record it as an observed negative result with the root cause and stop — do not adjust the test until the SUMMARY names the gap. Close the log with a "Closing" section listing the three instruments, the three RED confirmations, and the three clean reverts.
  </action>
  <verify>
    <automated>F=.planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md; test -s "$F" &amp;&amp; test "$(rg -o '^## Family \([abc]\)' "$F" | wc -l | tr -d ' ')" = "3" &amp;&amp; test "$(rg -o -- '--- FAIL: Test(CoverageSummaryOnOldGraphIsUnknown|GetCoverageAndGetHealthOnOldGraphAreKnownFalse|UIServiceMethodSetIsExactlyTheReadSet|CoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected|CoverageSourceNeverWalksDisk|CoverageRowsSurviveDiskMutationWithoutReindex)' "$F" | sort -u | wc -l | tr -d ' ')" -ge 5 &amp;&amp; test "$(rg -o 'git diff --quiet -- ' "$F" | wc -l | tr -d ' ')" -ge 6 &amp;&amp; test "$(rg -o 'git checkout -- ' "$F" | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test "$(rg -o '^## Pre-mutation cleanliness gate' "$F" | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the log lacks three family sections, fewer than five distinct pasted `--- FAIL` test names, fewer than six cleanliness-gate invocations (before + after per family), fewer than three reverts, or the house-format gate section</fails_when>
    <automated>test -z "$(git status --porcelain -- internal/query/coverage.go internal/uiserver/readonly_test.go)" &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestCoverageSummaryOnOldGraphIsUnknown|TestCoverageSourceNeverWalksDisk|TestCoverageRowsSurviveDiskMutationWithoutReindex' ./internal/query/ 2&gt;&amp;1 | tail -2 | rg -c '^ok' &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestUIServiceMethodSetIsExactlyTheReadSet|TestCoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected|TestGetCoverageAndGetHealthOnOldGraphAreKnownFalse' ./internal/uiserver/ 2&gt;&amp;1 | tail -2 | rg -c '^ok'</automated>
    <fails_when>a mutation survived (dirty tree on either mutated file) or the instruments are not green after the reverts</fails_when>
  </verify>
  <acceptance_criteria>
    - Each family section contains: instrument, the exact mutation (a unified diff hunk pasted), `git diff --quiet` results before and after, verbatim RED output, and the GREEN re-run
    - Family (c) explicitly records that `internal/query/archtest` stayed green under the mutation and why the file-scoped scan is the guard that fired
    - The working tree is clean on `internal/query/coverage.go` and `internal/uiserver/readonly_test.go` at the end
  </acceptance_criteria>
  <done>Three guard families have been watched failing against the exact mutation each exists to catch, and every mutation is byte-cleanly reverted.</done>
</task>

<task type="auto">
  <name>Task 2: 10-SECURITY.md (every T-10-xx row test-or-verdict), 10-VALIDATION.md map filled, and the phase-close gate</name>
  <reversibility rating="reversible">Documentation and verification only.</reversibility>
  <files>.planning/phases/10-index-health-the-coverage-denominator/10-SECURITY.md, .planning/phases/10-index-health-the-coverage-denominator/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md (whole — frontmatter keys, "Register origin" paragraph, Trust Boundaries table columns, the bold boundary sentence, Threat Register columns `Threat ID | Category | Component | Severity | Disposition | Mitigation | Status`, Accepted Risks and Notes sections)
    - The `<threat_model>` blocks of 10-01 through 10-05 PLAN.md (the union of T-10-01 … T-10-15 and T-10-SC — deduplicate by id; where two plans list the same id, merge the mitigation text)
    - .planning/phases/10-index-health-the-coverage-denominator/10-VALIDATION.md (the Per-Task Verification Map's TBD cells, the Wave 0 checklist, the Sign-Off list — fill values only; `status`/`nyquist_compliant` are set by validate-phase, do not touch them)
    - The five SUMMARY.md files (test names and commands that actually landed)
  </read_first>
  <action>
    Author `10-SECURITY.md` in the 09 format: frontmatter (`phase: "10"`, slug, `status: verified`, `threats_open: 0`, `asvs_level: 1`, `created`); the "Register origin" paragraph stating every plan carried a `<threat_model>`; Trust Boundaries: repository filesystem → `DiscoverAll` → `c/` namespace (walker strings persisted), `c/` → `internal/query` → `GetHealth`/`GetCoverage` → browser (strings and counts rendered; `page_token` inbound), ARCH-01 export stream → `Import` (kind 5), `CoverageSection` → user (text-only remedy); the bold boundary sentence: **The write path's new trust surface is a walker-produced string persisted then rendered — the same escaping discipline as `File.path`, enforced at the render boundary, not at write time.** Threat Register rows T-10-01 … T-10-15 and T-10-SC with Severity/Disposition copied from the plans and a Mitigation cell that names the TEST (file#TestName) or states `Verdict: …` for accepted rows (T-10-09 inherited origin guard, T-10-12 pre-existing `.go` symlink behaviour, T-10-SC not applicable) — no row may be prose-only; Status `closed` / `closed (accepted)`. Accepted Risks: T-10-09, T-10-12, T-10-SC, and the backstop concurrency truth (pages across snapshots) as an accepted, documented behaviour. Notes: Open Question 1 → implemented (`exportKindExcludedFile = 5`, round-trip test, no WINDOWS.md entry); Open Question 3 → vitest-level UI verification, no Playwright gate, with the rationale from 10-04; the key-prefix choice `'c'` and the full-name `excluded_by_reason` keys as recorded discretion decisions (D-05, D-09); D-12 untouched surfaces (`codegraph status`, MCP) confirmed by Plan 01's symbol-leak gate.

    Fill `10-VALIDATION.md`'s Per-Task Verification Map: replace each `TBD` Task ID / Plan / Wave cell with the real values (e.g. `10-01/T1`, `01`, `1`), replace the illustrative `-run` patterns with the test names that landed, set `File Exists` to ✅ and `Status` to ✅ green for rows whose tests are green, and set the optional live-browser row's Status to `⬜ n/a — Open Question 3: vitest-level (10-04)`; tick every Wave 0 checklist item that landed (leave the optional Playwright item unticked with the same note). Do not add headings, columns or new sections; do not edit the frontmatter.

    Run the phase-close gate and paste every tail into the SUMMARY: `GOTOOLCHAIN=go1.26.6 go vet ./...`, `GOTOOLCHAIN=go1.26.6 task test:unit`, `GOTOOLCHAIN=go1.26.6 task test:golden`, `GOTOOLCHAIN=go1.26.6 task proto:drift`, `task web:test`, `pnpm -C web check`, `task -s web:drift`. Commit the two docs and the VALIDATION.md edit with `docs(10-06): …`.
  </action>
  <verify>
    <automated>F=.planning/phases/10-index-health-the-coverage-denominator/10-SECURITY.md; test -s "$F" &amp;&amp; test "$(rg -o '^threats_open: 0$' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^asvs_level: 1$' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^\| T-10-(0[1-9]|1[0-5]|SC) \|' "$F" | sort -u | wc -l | tr -d ' ')" = "16" &amp;&amp; test "$(rg '^\| T-10-' "$F" | rg -v 'Test[A-Za-z_]+|Verdict:' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'exportKindExcludedFile = 5' "$F" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o -i 'no Playwright gate|vitest-level' "$F" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '^## (Trust Boundaries|Threat Register|Accepted Risks|Notes)' "$F" | wc -l | tr -d ' ')" = "4"</automated>
    <fails_when>a register row is missing (16 distinct ids: T-10-01…15 + SC), any row carries neither a `Test…` name nor a `Verdict:`, the Open Question decisions are absent, or a house-format section is missing</fails_when>
    <automated>V=.planning/phases/10-index-health-the-coverage-denominator/10-VALIDATION.md; test "$(rg -o '^\| TBD \|' "$V" | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^\| 10-0[1-5]/T[0-9] \|' "$V" | wc -l | tr -d ' ')" -ge 8 &amp;&amp; test "$(rg -o '^- \[x\]' "$V" | wc -l | tr -d ' ')" -ge 8 &amp;&amp; test "$(rg -o '^status: draft$' "$V" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^## ' "$V" | wc -l | tr -d ' ')" = "6"</automated>
    <fails_when>a TBD cell survives, fewer than 8 rows carry real task ids, fewer than 8 Wave 0 items are ticked, the frontmatter status was touched, or a heading was added/removed (the file's shape is tool-owned)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./... &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:unit &gt;/tmp/10-06-unit.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/10-06-unit.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^FAIL' /tmp/10-06-unit.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:golden &gt;/tmp/10-06-golden.txt 2&gt;&amp;1; RC=$?; tail -2 /tmp/10-06-golden.txt; test "$RC" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task proto:drift &gt;/tmp/10-06-proto.txt 2&gt;&amp;1; RC=$?; tail -2 /tmp/10-06-proto.txt; test "$RC" = "0"</automated>
    <fails_when>vet, the Go unit suite, the golden suite, or proto:drift exits non-zero or prints FAIL</fails_when>
    <automated>task web:test &gt;/tmp/10-06-webtest.txt 2&gt;&amp;1; RC=$?; tail -2 /tmp/10-06-webtest.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o 'web:test: PASS' /tmp/10-06-webtest.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; pnpm -C web check &gt;/tmp/10-06-check.txt 2&gt;&amp;1; RC=$?; tail -2 /tmp/10-06-check.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i '\b0 errors\b' /tmp/10-06-check.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; task -s web:drift &gt;/tmp/10-06-drift.txt 2&gt;&amp;1; RC=$?; cat /tmp/10-06-drift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '\bhalf MATCH\b' /tmp/10-06-drift.txt | wc -l | tr -d ' ')" = "2"</automated>
    <fails_when>the vitest suite, svelte-check, or web:drift is red at the final commit</fails_when>
    <automated>test "$(git ls-files internal/indexer/testdata/coverage | wc -l | tr -d ' ')" = "6" &amp;&amp; test "$(git ls-files .planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md .planning/phases/10-index-health-the-coverage-denominator/10-SECURITY.md | wc -l | tr -d ' ')" = "2" &amp;&amp; LOG=$(git log -n 40 --format=%s) &amp;&amp; test -n "$LOG" &amp;&amp; test "$(printf '%s\n' "$LOG" | rg -o '\[ci skip\]' | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the fixture is not fully tracked, a closing doc is untracked, or a commit message carries the forbidden ci-skip marker</fails_when>
  </verify>
  <acceptance_criteria>
    - `10-SECURITY.md` register: 16 rows, each with a test name or a `Verdict:`; Notes record Open Questions 1 and 3, the `'c'` prefix and the full-name map keys
    - `10-VALIDATION.md`: zero TBD cells, real task ids, Wave 0 items ticked, frontmatter untouched, six `##` headings exactly as seeded
    - SUMMARY pastes the seven gate tails and states `threats_open: 0`
  </acceptance_criteria>
  <done>The phase's security contract and validation map reflect what shipped, every threat is closed by a test or a verdict, and the complete gate suite is green at the closing commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan's docs → future readers | the register and mutation log are the audit trail later phases trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Repudiation | the pasted RED transcripts in `10-MUTATION-LOG.md` | medium | mitigate | each family names the exact command and pastes verbatim output; a non-failing demonstration is reported as an observed negative result (08 family (d) precedent); reverts asserted by `git diff --quiet` before and after |
| T-10-16 | Repudiation | a `10-SECURITY.md` row closed by prose alone | medium | mitigate | the verify gate rejects any `T-10-` row without a `Test…` name or a `Verdict:` |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | not applicable — no package-manager install in this phase |
</threat_model>

<verification>
- All seven phase-close commands green at the final commit (tails in the SUMMARY)
- `10-MUTATION-LOG.md` three families RED + reverted; `10-SECURITY.md` 16 rows test-or-verdict; `10-VALIDATION.md` filled
</verification>

<success_criteria>
- D-17 satisfied: mutation log in house format with three RED families; security register complete at ASVS L1 with `threats_open: 0`
- Phase gate green: Go unit + golden + proto drift + vitest + svelte-check + web drift + vet
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `.planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md` (families (a) D-15, (b) D-16, (c) D-14)
- `.planning/phases/10-index-health-the-coverage-denominator/10-SECURITY.md` (T-10-01 … T-10-15, T-10-SC)
- `10-VALIDATION.md` Per-Task Verification Map and Wave 0 checklist filled (values only)

<output>
Create `.planning/phases/10-index-health-the-coverage-denominator/10-06-SUMMARY.md` when done — include the three RED confirmations, the seven gate tails, and `threats_open: 0`.
</output>
