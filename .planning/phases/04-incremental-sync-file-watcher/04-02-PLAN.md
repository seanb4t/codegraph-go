---
phase: 04-incremental-sync-file-watcher
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/schema/graph.proto
  - internal/schema/graph.pb.go
  - internal/query/traverse.go
  - internal/query/explore.go
  - internal/query/node.go
autonomous: true
requirements: [INDX-03]

must_haves:
  truths:
    - "File records can carry mtime_unix_ns + size_bytes for the D-01a stat pre-filter"
    - "Meta carries has_file_index so Sync can detect a pre-Phase-4 graph and backfill (D-02b)"
    - "query.BuildReverseAdjacency is exported and importable from internal/indexer without a cycle"
    - "SchemaVersion stays 1 — all schema changes are additive"
  artifacts:
    - path: internal/schema/graph.proto
      provides: "File.mtime_unix_ns=7, File.size_bytes=8, Meta.has_file_index=7 (additive)"
      contains: "mtime_unix_ns"
    - path: internal/schema/graph.pb.go
      provides: "regenerated accessors GetMtimeUnixNs/GetSizeBytes/GetHasFileIndex"
      contains: "GetHasFileIndex"
    - path: internal/query/traverse.go
      provides: "exported BuildReverseAdjacency(r graphstore.Reader)"
      contains: "func BuildReverseAdjacency"
  key_links:
    - from: internal/query/explore.go
      to: internal/query/traverse.go
      via: "callers/impact/affected/explore/node call BuildReverseAdjacency (renamed)"
      pattern: "BuildReverseAdjacency\\("
---

<objective>
Lay the additive foundation the Sync engine (Plan 04-03) binds to:
1. Add the two `File` stat fields (`mtime_unix_ns`, `size_bytes`) D-01a's stat pre-filter compares
   against, and the `Meta.has_file_index` flag D-02b's backfill detection reads — all additive,
   `SchemaVersion` stays `1` (RESEARCH Pitfall 3 / finding 3).
2. Export `internal/query`'s currently-unexported `buildReverseAdjacency` as `BuildReverseAdjacency`
   so `internal/indexer.Sync()` can reuse Phase-3's D-04 reverse-adjacency scan for dependent-file
   detection (RESEARCH finding 5 / Pitfall 4). `internal/indexer → internal/query` is a NEW,
   verified non-circular import edge.

Purpose: everything else in the phase depends on these two seams; keeping them in a small,
mechanical prep plan lets the storage-index plan (04-01) run fully in parallel.
Output: regenerated `graph.pb.go` with new accessors; an exported reverse-adjacency function with
all in-package call sites updated and existing query tests still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-incremental-sync-file-watcher/04-RESEARCH.md
@.planning/phases/04-incremental-sync-file-watcher/04-PATTERNS.md
@internal/schema/graph.proto
@internal/schema/meta.go
@internal/query/traverse.go

<interfaces>
<!-- Current shapes to extend. protoc + protoc-gen-go are installed. -->

From internal/schema/graph.proto (additive-only; next free numbers):
- message File { path=1; content_hash=2; language=3; node_count=4; edge_count=5; repeated errors=6; }  // add 7,8
- message Meta { schema_version=1; node_count=2; edge_count=3; last_sync_unix_ms=4; healthy=5; health_message=6; }  // add 7

From internal/query/traverse.go (the function + its 6 in-package call sites):
- func buildReverseAdjacency(r graphstore.Reader) (map[string][]*schema.Edge, error)   // -> rename to BuildReverseAdjacency
- callers: traverse.go (Callers/Impact/Affected — 3 sites), explore.go:124, node.go:176

Proto regen command (from repo root; pitfall from 02-01: use --proto_path=.):
  protoc --go_out=paths=source_relative:. --proto_path=. internal/schema/graph.proto
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add additive File/Meta fields and regenerate graph.pb.go</name>
  <files>internal/schema/graph.proto, internal/schema/graph.pb.go</files>
  <read_first>internal/schema/graph.proto (File + Meta messages, additive-only doc comment), internal/schema/meta.go (SchemaVersion const + additive-only rule), 04-PATTERNS.md §graph.proto (doc-comment convention), 02-01-SUMMARY.md (protoc invocation pitfall — use --proto_path=.)</read_first>
  <action>
    In graph.proto, add to `message File`: `int64 mtime_unix_ns = 7;` and `int64 size_bytes = 8;`, each with a short doc comment citing D-01a and naming the writers (index + sync), in the same voice as the existing `errors` field. Add to `message Meta`: `bool has_file_index = 7;` with a doc comment citing D-02b (backfill detection: false means a pre-Phase-4 graph lacking the x/ index) in `last_sync_unix_ms`'s voice. Do NOT touch any reserved range; do NOT renumber. Regenerate graph.pb.go by running the protoc command from the interfaces block at the repo root (never hand-edit graph.pb.go — confirm the `// Code generated by protoc-gen-go. DO NOT EDIT.` header survives). Leave internal/schema/meta.go's `SchemaVersion = 1` unchanged; the additive change requires no bump.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && protoc --go_out=paths=source_relative:. --proto_path=. internal/schema/graph.proto && go build ./internal/schema/... && go test ./internal/schema/... -count=1 && grep -q 'func (x \*File) GetMtimeUnixNs()' internal/schema/graph.pb.go && grep -q 'func (x \*Meta) GetHasFileIndex()' internal/schema/graph.pb.go</automated>
  </verify>
  <acceptance_criteria>
    - graph.pb.go regenerates cleanly and exposes GetMtimeUnixNs(), GetSizeBytes(), GetHasFileIndex() accessors.
    - `grep -c 'SchemaVersion uint32 = 1' internal/schema/meta.go` is 1 (unchanged).
    - The Phase-1 round-trip test (`go test ./internal/schema/...`) passes unchanged — proves forward/backward compatibility of the additive fields.
    - graph.pb.go still carries the `Code generated by protoc-gen-go. DO NOT EDIT.` header.
    - Commit: `feat(04-02): add additive File.mtime/size + Meta.has_file_index fields`.
  </acceptance_criteria>
  <done>File carries mtime_unix_ns/size_bytes, Meta carries has_file_index; SchemaVersion still 1; schema tests green.</done>
</task>

<task type="auto">
  <name>Task 2: export query.BuildReverseAdjacency for indexer reuse</name>
  <files>internal/query/traverse.go, internal/query/explore.go, internal/query/node.go</files>
  <read_first>internal/query/traverse.go (buildReverseAdjacency + its doc comment + Callers/Impact/Affected call sites), 04-RESEARCH.md Pitfall 4 (call-site inventory + non-circular confirmation), 04-PATTERNS.md §traverse.go</read_first>
  <action>
    Rename `buildReverseAdjacency` to `BuildReverseAdjacency` in traverse.go (capital B is the only signature change) and update all six in-package call sites: the three in traverse.go (Callers, Impact, Affected), explore.go:124, and node.go:176. Preserve the existing doc comment's "built fresh inside every caller — no package-level cache, no sync.Once" discipline verbatim (Sync must follow it too). Do NOT change behavior: it still filters to goextract.RefKindCalls only and returns a map keyed by edge.Target. This is a pure mechanical rename — no new logic.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && go build ./... && go test ./internal/query/... -count=1 && grep -q 'func BuildReverseAdjacency' internal/query/traverse.go && ! grep -rn 'buildReverseAdjacency' internal/query/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds; no lingering lowercase `buildReverseAdjacency` reference remains (`grep -rn 'buildReverseAdjacency' internal/query/` empty).
    - `go test ./internal/query/...` passes — the existing callers/impact/affected/explore/node tests still green after the rename (behavior unchanged).
    - `BuildReverseAdjacency` is exported and its doc comment retains the "fresh per call, no cache" note.
    - Commit: `refactor(04-02): export query.BuildReverseAdjacency for indexer reuse`.
  </acceptance_criteria>
  <done>BuildReverseAdjacency is exported, all call sites updated, query tests green, ready for internal/indexer to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored schema ↔ readers | Additive proto fields must not break decoding of pre-Phase-4 records (forward/backward compat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Tampering | graph.proto additive evolution | medium | mitigate | New fields use next-free numbers only, no reserved range disturbed, SchemaVersion stays 1; the Phase-1 round-trip test proves old records still decode (ARCH-01 forward-compat) |
| T-04-02-02 | Information disclosure | File.content_hash (V6) | low | accept | This plan does not touch the hash; content_hash MUST stay SHA-256 (graph.proto V6 mandate) — reused unchanged, no new crypto surface |
| T-04-02-SC | Tampering | dependencies | low | accept | No package-manager installs; protoc/protoc-gen-go are pre-installed pinned tools (v7.35.1 / v1.36.11); no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `go build ./...` clean.
- `go test ./internal/schema/... ./internal/query/... -race -count=1` green.
- Additive fields present; SchemaVersion unchanged; no circular import introduced (indexer will import query in 04-03).
</verification>

<success_criteria>
- File.mtime_unix_ns / File.size_bytes / Meta.has_file_index exist and are accessible.
- query.BuildReverseAdjacency exported; all call sites updated; existing query tests green.
- SchemaVersion == 1.
</success_criteria>

## Artifacts this plan produces
- New schema fields `File.mtime_unix_ns` (7), `File.size_bytes` (8), `Meta.has_file_index` (7) with regenerated accessors `GetMtimeUnixNs`/`GetSizeBytes`/`GetHasFileIndex`.
- Exported `query.BuildReverseAdjacency` (renamed from `buildReverseAdjacency`).

<output>
Create `.planning/phases/04-incremental-sync-file-watcher/04-02-SUMMARY.md` when done.
</output>
