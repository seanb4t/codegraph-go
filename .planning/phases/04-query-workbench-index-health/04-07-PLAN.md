---
phase: 04-query-workbench-index-health
plan: 07
type: execute
wave: 4
depends_on: ["04-01", "04-04", "04-05", "04-06"]
files_modified:
  - Taskfile.yml
  - .github/workflows/components-drift.yml
  - internal/upgrade/taskfile_shape_test.go
  - web/tests/data-table-render-cost.test.ts
  - web/build/
  - .planning/todos/pending/2026-08-28-shadcn-svelte-registry-version-pinning-with-source-match.md
  - .planning/todos/completed/2026-08-28-shadcn-svelte-registry-version-pinning-with-source-match.md
  - .planning/todos/pending/2026-08-28-client-side-render-cost-measurement-for-browse-views.md
  - .planning/todos/completed/2026-08-28-client-side-render-cost-measurement-for-browse-views.md
autonomous: true
requirements: [WRK-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`task web:components:drift` regenerates every vendored shadcn-svelte component at the pinned CLI version into a scratch directory and byte-compares it against what is committed, failing on any difference."
    - "The drift target reports how many committed component files it enumerated BEFORE comparing anything, and refuses to pass on an empty or short enumeration — the enumeration itself is tested before the target is trusted."
    - "The drift target's subject set is derived from disk, never hand-listed, so a component vendored later is covered automatically."
    - "The drift target has been watched FAIL against a deliberately altered committed file, then watched pass again once reverted."
    - "`task web:components:drift` actually RUNS somewhere without a human remembering: a scheduled, manually-dispatchable, non-PR workflow invokes it, and that workflow declares no pull_request trigger."
    - "That scheduled workflow can REACH its run step: it checks the repository out, installs Task through the repository's own local action, and sets up Node 24 so Corepack can resolve the pinned pnpm — the three prerequisites without which the run step fails on every scheduled execution while the workflow still registers cleanly and passes actionlint."
    - "A recorded measurement — not an assumption — states what a 1000-row workbench table costs to render and to re-sort, and no wall-clock threshold is asserted inside the PR-required `task web:test`."
    - "`task web:drift` is GREEN again: the committed web/build/ bundle matches the source that produces it."
  artifacts:
    - Taskfile.yml (the new web:components:drift and web:render-cost targets)
    - .github/workflows/components-drift.yml
    - web/tests/data-table-render-cost.test.ts
    - web/build/ (rebuilt and re-committed)
    - .planning/todos/completed/2026-08-28-shadcn-svelte-registry-version-pinning-with-source-match.md
    - .planning/todos/completed/2026-08-28-client-side-render-cost-measurement-for-browse-views.md
  key_links:
    - "web:components:drift follows proto:drift's regenerate-into-scratch-and-byte-compare shape, NOT web:drift's hash-against-a-committed-manifest shape — vendored components have no manifest to hash against."
    - "The pinned CLI version in the drift target is the SAME literal 04-01 and 04-04 used to vendor; a mismatch between them is a guaranteed false failure."
    - "web:components:drift runs from a schedule/workflow_dispatch-only workflow — never from ci.yml's PR-triggered jobs, never from release.yml, and never in protect-main's required set (D-16)."
    - "The new workflow file is registered in inScopeWorkflowFiles and its job in inScopeJobs (internal/upgrade/taskfile_shape_test.go), because TestWorkflowFilePopulationMatchesDisk requires every file under .github/workflows/ to appear in exactly one of those lists."
    - "The three bootstrap steps are `uses:` steps, so checkStepInvokesTask (internal/upgrade/taskfile_shape_test.go:1399-1402) returns nil for them without inspection — the one-meaningful-run-step rule is satisfied and NO runBodyExceptions entry is required or permitted for them."
    - "The workflow's checkout and setup-node pins are the SAME full SHAs .github/workflows/ci.yml already uses for those actions; the acceptance gate extracts them from ci.yml rather than hardcoding them, so the assertion survives a pin bump."
  prohibitions:
    - "MUST NOT add web:components:drift to any required PR status check, to ci.yml's PR-triggered jobs, to release.yml, or to protect-main's required set (D-16) — a network-dependent required check is painful to un-wedge, and release.yml carries a LOCKED cosign-SAN contract that this plan does not touch."
    - "MUST NOT add the new workflow's job name to requiredCheckNames — that fixture mirrors a GitHub ruleset outside this repo and adding a name there does not create the check, it only makes the guard lie."
    - "MUST NOT give the new workflow a pull_request or push trigger; schedule + workflow_dispatch only."
    - "MUST NOT hand-enumerate the component subdirectories inside the drift target; the subject set is derived from disk."
    - "MUST NOT add a runBodyExceptions entry for any of the three bootstrap steps — they carry no run: body, so the guard never inspects them, and TestWorkflowRunBodiesInvokeTask FAILS any exception that never matches a real inspected step."
    - "MUST NOT pin the bootstrap actions to a different SHA than the one .github/workflows/ci.yml already uses for the same action — a second SHA for one action version is drift of exactly the kind this plan exists to detect."
    - "MUST NOT enumerate with a TRAILING-SLASH pathspec — `git ls-files -- 'web/src/lib/components/ui/*/'` returns ZERO files in this repository (git tracks files, not directory entries), and a floor derived from and checked against a zero-returning enumeration is exactly the vacuous pass T-04-29 exists to prevent."
    - "MUST NOT wire the drift target's pass/fail logic before Task 1 has PROVEN that re-running the CLI reproduces byte-identical output."
    - "MUST NOT use `@latest` for the CLI anywhere in the target."
    - "MUST NOT assert a wall-clock duration threshold in any test file that `task web:test` runs unconditionally — `task web:test` is a PR-triggered REQUIRED job (.github/workflows/ci.yml:149) and a load-sensitive threshold there is a flake generator (D-08 is measurement-first)."
    - "MUST NOT add a client-side row cap on top of MaxLimit as a response to the render-cost measurement — virtualize or accept, never re-cap what the server already bounded (D-08)."
    - "MUST NOT install @tanstack/svelte-virtual without its own [SUS] package-legitimacy checkpoint; this plan does not pre-authorize it."
    - "MUST NOT change web:drift's hashing pipeline or its SRC_FLOOR."
    - "MUST NOT write a verify gate in the shape `<cmd>; RC=$?; node -e '…'; test \"$RC\" -eq 0` — the compound's status is the LAST command's, so the floor is discarded. Chain floor and status as ONE final `&&` command (04-VALIDATION.md §The ONE correct shell shape)."
---

<objective>
Close the phase's three remaining obligations: a real drift guard over the
vendored component source, a recorded render-cost measurement, and a green
committed bundle.

**The drift guard (D-16).** `shadcn-svelte add` writes `.svelte` source into the
repo that never enters `pnpm-lock.yaml`, so `web:audit`, `web:deps:strict` and
`web:lockfile` structurally cannot see it — 03-CONTEXT.md D-22 recorded that gap
and its stated mitigation was "scope discipline, not new machinery". The
maintainer folded the real fix in here, after it was declined as scope growth
twice. This target replaces per-phase human reads with actual coverage.

**The determinism premise, proven before it is relied on.** 04-RESEARCH.md's Open
Question 2 is explicit: the CLI's `add` command was never exercised live against a
scratch tree this session, so its regeneration determinism is UNVERIFIED. Building
a byte-compare guard on an unproven premise produces either a permanently red gate
or a silently vacuous one. Task 1 proves it first; if it does not hold, the target's
design changes rather than the premise being assumed.

**The measurement (D-08).** A workbench table can return up to `MaxLimit` = 1000
rows. D-08's rule is measurement first, then virtualization only if a threshold is
crossed — never hand-rolled windowing and never a second client-side row cap over
results the server already bounded.

**The bundle.** Every frontend plan in this phase invalidated the committed
`web/build/`, exactly as 03-01 opened that window and 03-09 closed it. It closes here.

Purpose: the phase's supply-chain, performance and committed-artifact obligations.
Output: one new Taskfile target, one recorded measurement, a rebuilt bundle, and
two folded todos closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-query-workbench-index-health/04-CONTEXT.md
@.planning/phases/04-query-workbench-index-health/04-RESEARCH.md
@.planning/phases/04-query-workbench-index-health/04-PATTERNS.md
@.planning/phases/04-query-workbench-index-health/04-VALIDATION.md
@.planning/phases/04-query-workbench-index-health/04-01-SUMMARY.md
@.planning/phases/04-query-workbench-index-health/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the premise — does re-running the pinned CLI reproduce byte-identical output?</name>
  <files>(no repository file is modified by this task; it produces a recorded finding)</files>
  <read_first>
    - .planning/phases/04-query-workbench-index-health/04-RESEARCH.md §Open Questions #2 (the exact statement of what was NOT exercised live)
    - web/components.json (the style, registry URL and aliases the CLI reads — a scratch tree must carry an identical copy or the comparison is meaningless)
    - .planning/phases/04-query-workbench-index-health/04-01-SUMMARY.md and 04-04-SUMMARY.md (the exact CLI invocations used to vendor `table` and `tabs`)
  </read_first>
  <precondition>Network access to `shadcn-svelte.com`'s registry is available; the target and this probe both fetch from it. If unreachable, halt and report — do not weaken the comparison to work offline.</precondition>
  <action>
    Build a scratch tree that mirrors what the CLI needs — a `components.json`
    identical to `web/components.json`, and whatever minimal `package.json` /
    directory layout the CLI requires to resolve its aliases — then regenerate
    **every vendored component family, not just this phase's two**, and compare the
    output byte-for-byte against what is committed.

    Derive the family list from disk exactly as Task 2's target will:

    ```
    git ls-files -- 'web/src/lib/components/ui/' | awk -F/ '{print $6}' | sort -u
    ```

    (Note the pathspec has NO trailing slash. Verified in-tree: the trailing-slash
    form returns **0** files, the form above returns **36**. Git tracks files, not
    directory entries, and `$6` is the component directory in
    `web/src/lib/components/ui/<component>/<file>`.)

    That list is `button command dialog input input-group textarea` today, plus
    `table` from 04-01 and `tabs` from 04-04 — **eight** families, not two. Probing
    only this phase's two would leave the guard's real subject set unproven, and
    Task 2's target compares all eight.

    **Expect a HALT here and treat it as the likely outcome, not the unhappy one.**
    Six of the eight families were vendored by 03-06 and this plan assumes a single
    `shadcn-svelte@1.5.1` regeneration is authoritative for all of them. If any of
    those six came from a different registry revision — or if the registry has
    since republished any of their sources — the byte-compare will differ for that
    family through no fault of the tree, and question 5 below is where that is
    decided. Do not "fix" it by re-vendoring six components at 1.5.1 inside this
    plan: that would silently rewrite committed source that has been reviewed and
    shipped, in a plan whose job is to guard that source, not to change it.

    Answer these five questions and record each answer in the SUMMARY with the
    evidence that produced it:

    1. Is the output byte-identical to what is committed? If not, WHAT differs —
       import path rewriting driven by the alias config, formatting, a trailing
       newline, an ordering difference inside `index.ts`?
    2. Is it deterministic across two consecutive runs into two fresh scratch
       trees? Run it twice and compare the two outputs to each other, not only to
       the committed tree. A non-deterministic generator makes a byte-compare gate
       flaky regardless of what is committed.
    3. Does `-o` behave the same against a target directory that already contains
       DIFFERENT content as it does against an empty one? The drift target
       regenerates into an empty scratch, so the empty case is the one that binds —
       but a divergence here would mean the vendoring step and the guard disagree.
    4. Does the CLI mutate anything outside the component directory in the scratch
       tree (a `package.json` entry, a CSS import)? The target must scope its
       comparison to what it actually controls.

    5. **Per family, does 1.5.1 reproduce what is committed?** Report the answer as
       a per-family table (eight rows), not a single verdict. A single "yes" hides
       the case this question exists to catch: `table` and `tabs` reproducing
       cleanly while one 03-06 family does not, which is the difference between a
       guard that can be turned on today and one that cannot.

    **Branch on the answer, and record which branch was taken:**
    - **Any pre-existing family does not reproduce at 1.5.1 → HALT and report.**
      Name the families, show the diffs, and propose the options rather than
      choosing one: (a) pin that family to the CLI version it actually came from,
      if the target can carry a per-family version map; (b) re-vendor it at 1.5.1
      as a separate, reviewed change outside this plan; (c) scope the guard's
      subject set to the families that do reproduce, with the excluded ones named
      IN the target and their exclusion visible in its printed output — never
      silently absent, which is the T-04-29 shape. Do NOT re-vendor committed
      source inside this plan to make a guard pass.
    - Byte-identical and deterministic → Task 2 wires the byte-compare as designed.
    - Deterministic but differing from what is committed in a CHARACTERIZABLE,
      stable way (e.g. every file differs only by a trailing newline) → Task 2
      normalizes exactly that difference, and the normalization is named,
      justified in a comment, and narrow. A normalization broad enough to hide a
      real content change is not acceptable.
    - Non-deterministic, or differing unpredictably → STOP. Report the finding and
      propose an alternative design (for example: pin and compare the registry
      JSON responses rather than the CLI's rendered output). Do NOT build a
      byte-compare gate on a non-deterministic generator; that produces a flaky
      required-adjacent check, which teaches "re-run it" — the exact failure mode
      the maintainer rejected for the wire-oracle flake in 03-03.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-query-workbench-index-health/04-07-SUMMARY.md && test "$(rg -c 'determinis' .planning/phases/04-query-workbench-index-health/04-07-SUMMARY.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - All FIVE questions have a recorded answer in the SUMMARY, each with the command output that produced it, and question 5's answer is a per-family table with one row per family the disk enumeration returned.
    - The family list was DERIVED, not typed: the SUMMARY records the output of `git ls-files -- 'web/src/lib/components/ui/' | awk -F/ '{print $6}' | sort -u` and the probe covered every family in it.
    - The SUMMARY records the trailing-slash counter-check — `git ls-files -- 'web/src/lib/components/ui/*/' | wc -l` reporting `0` against the no-slash form's non-zero count — so Task 2's pathspec choice is evidence-backed in this repository rather than inherited from this plan's prose.
    - The two-consecutive-runs comparison was actually performed and its result recorded.
    - The chosen branch is named explicitly, and if a normalization is required its exact scope is stated.
    - No repository file outside `.planning/` is modified by this task.
  </acceptance_criteria>
  <done>The regeneration-determinism premise is proven or disproven by a live run, and Task 2's design follows the evidence rather than the assumption.</done>
</task>

<task type="auto">
  <name>Task 2: `task web:components:drift` — enumerate from disk, report the count, byte-compare, and prove it goes RED</name>
  <files>Taskfile.yml, .github/workflows/components-drift.yml, internal/upgrade/taskfile_shape_test.go, .planning/todos/pending/2026-08-28-shadcn-svelte-registry-version-pinning-with-source-match.md, .planning/todos/completed/2026-08-28-shadcn-svelte-registry-version-pinning-with-source-match.md</files>
  <read_first>
    - Taskfile.yml:279-390 (proto:drift, in full: the `git ls-files` enumeration, the count-report-before-comparing block, the `nfiles -lt 4` floor, the scratch build, the regenerate-never-in-place step, and the `cmp -s` loop with its drift_found accumulator — this is the shape to mirror)
    - internal/upgrade/taskfile_shape_test.go: `inScopeJobs` (~line 152), `inScopeWorkflowFiles` (~line 1526), `workflowFileExceptions` (~line 1549), `TestWorkflowFilePopulationMatchesDisk`, `TestInScopeJobsPopulationMatchesDisk`, `TestWorkflowRunBodiesInvokeTask`, and `requiredCheckNames` (~line 77 — a hand-written mirror of a GitHub ruleset that must NOT gain an entry here)
    - .github/workflows/corpora.yml (the closest precedent for a non-PR workflow whose jobs ARE in inScopeJobs and whose run bodies are exactly `task <target>`)
    - Taskfile.yml:1074-1181 (web:drift — the hash-against-a-committed-manifest shape this target must NOT copy, and its SRC_FLOOR convention, which must not be changed)
    - Taskfile.yml (the web:* target family's naming, `deps:` wiring and `preconditions:` conventions)
    - .planning/todos/pending/2026-08-28-shadcn-svelte-registry-version-pinning-with-source-match.md (the todo this closes, and its own stated framing of why a live-network regeneration differs in risk from a self-contained one)
    - .planning/phases/04-query-workbench-index-health/04-07-SUMMARY.md (Task 1's recorded findings — the design input)
  </read_first>
  <action>
    <!-- planner-discipline-allow: components/ui/*/ -->
    <!-- planner-discipline-allow: web:components:drift -->
    <!-- planner-discipline-allow: pull_request -->
    <!-- planner-discipline-allow: web:render-cost -->
    <!-- planner-discipline-allow: for c in -->
    <!-- planner-discipline-allow: table tabs -->
    **Executor note on negative greps in this task's acceptance criteria.** Several
    literals discussed below are things this task's criteria assert are ABSENT from
    a specific file: the trailing-slash pathspec must not appear in `Taskfile.yml`;
    `web:components:drift` must not appear in `ci.yml` or `release.yml`;
    `pull_request` must not appear in `components-drift.yml`; `web:render-cost` must
    not appear in any workflow; and no hand-enumerated component list may appear in
    the target. Every one of those is discussed HERE precisely so it does not need
    to be written into a comment in the file being guarded. Put the reasoning in the
    SUMMARY and in this plan — never in the guarded file.

    Add a `web:components:drift` target to `Taskfile.yml`, in the `web:*` family,
    following `proto:drift`'s shape in this exact order:

    (a) **Enumerate from disk, never by hand — and use the pathspec that returns
    files.** Derive the file set with

    ```
    git ls-files -- 'web/src/lib/components/ui/'
    ```

    and derive the component set from it with `awk -F/ '{print $6}' | sort -u`
    (the layout is `web/src/lib/components/ui/<component>/<file>`, so `$6` is the
    component directory).

    **Do NOT use a trailing-slash pathspec.** Verified in-tree this session:
    `git ls-files -- 'web/src/lib/components/ui/*/'` returns **0** files, while the
    form above returns **36**. Git tracks files, not directory entries. A subject
    set derived from — and a floor checked against — a zero-returning enumeration
    is precisely the vacuous pass T-04-29 exists to prevent: the loop would compare
    nothing and the target would report success.

    A hand-listed `for c in table tabs` loop narrows the same way for a different
    reason: a component vendored by Phase 5 would pass BECAUSE it is absent from
    the list. The subject set must come from disk so a new component is covered the
    moment it is committed.

    (b) **Assert a non-zero population, and report both counts, before comparing
    anything.** Print

    ```
    web:components:drift: compared ${nfiles} vendored component files across ${ncomponents} components
    ```

    BEFORE any comparison runs, mirroring `proto:drift`'s own `echo` at line 346.
    Then apply TWO floors, both structural minima and neither the observed count:
    `nfiles -lt 8` and `ncomponents -lt 2`. Fail with an explicit message naming the
    pathspec if either trips — "enumerated nothing" and "enumerated one thing" are
    the two ways this guard goes vacuous, and the pathspec is the thing that
    actually broke in review, so the error message should point at it.

    Use a SMALL structural minimum deliberately, not the count observed today
    (~44 files across 8 components). A floor equal to the current count ratchets on
    intentional removal: deleting a component that is genuinely no longer used
    would fail the guard and force an unrelated edit to a historical number. That
    is the same trap `web:test`'s FLOOR comment (`Taskfile.yml:577`) and
    `web:drift`'s `SRC_FLOOR` (`Taskfile.yml:1123`) already avoid. Carry the same
    wording in a comment: the floor exists ONLY to catch "enumerated nothing" and
    must never be raised as a growth ratchet.

    (c) **Regenerate into a scratch directory, never in place.** `mktemp -d` with a
    `trap` cleanup, a `components.json` copied from `web/components.json`, and
    `pnpm dlx shadcn-svelte@1.5.1 add <derived component list> -y -o` inside it.
    The version literal MUST be the same `1.5.1` 04-01 and 04-04 vendored with — a
    mismatch between the vendoring version and the guard's version is a guaranteed
    false failure. Never `@latest`.

    (d) **Byte-compare with `cmp -s`**, accumulating into a `drift_found` flag so
    EVERY difference is reported rather than only the first, and fail if the flag
    is set — `proto:drift`'s loop verbatim in structure. Report a missing
    regenerated counterpart as its own named failure, distinct from a content
    difference.

    (e) Apply exactly the normalization Task 1's finding requires, if any, with a
    comment naming what it normalizes and why. Nothing broader.

    (f) **Prove BOTH the enumeration and the comparison go RED.** A gate is not
    trusted until it has been demonstrated red against a confirmed-applied mutation
    (a standing decision in STATE.md), and this target has two independent ways to
    pass vacuously, so both need a demonstration:
      - **Comparison RED:** alter one byte in one committed vendored file, run the
        target, record that it failed AND named that specific file, then revert and
        record that it passed.
      - **Enumeration RED:** temporarily run the target with the trailing-slash
        pathspec (`'web/src/lib/components/ui/*/'`) and record that the population
        floor from step (b) FAILS with its pathspec-naming message rather than the
        target reporting a clean pass over zero files. This is the exact defect the
        cycle-1 review found; a floor that has never been watched reject an empty
        enumeration is a floor nobody has tested.
    All four observations go in the SUMMARY, together with the printed file and
    component counts from the green run.

    (g) **Wire it into a non-PR workflow so the mitigation actually runs.** The
    previous draft of this plan called the target "local and release-path" while
    requiring `rg -c 'web:components:drift' .github/workflows/*.yml` to report 0 —
    which made it local-only, so T-04-27 (a `high` threat whose only structural
    mitigation this target is) was mitigated by a gate nobody was obliged to run.
    Close that by adding `.github/workflows/components-drift.yml`:

      - triggers: `schedule:` (a weekly cron) and `workflow_dispatch:` — and NOTHING
        else. No `pull_request`, no `push`. That keeps D-16 intact: the target is
        still out of required checks, so an unreachable registry can never wedge a
        merge, and per rule `f18zrdsgx5` `protect-main` stays recoverable.
      - one job, one meaningful `run:` step whose body is exactly
        `task web:components:drift`, matching `TestWorkflowRunBodiesInvokeTask`'s
        `task <target>` shape so the job needs no exception entry.
      - **the three bootstrap steps that make that one step reachable**, in this
        order, before it:
          1. `actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3`
             — the repository must be on disk before anything else, and the local
             Task installer below is a path INSIDE the checkout.
          2. `uses: ./.github/actions/install-task` — the repository's own local
             installer, exactly as `.github/workflows/ci.yml:65` and
             `.github/workflows/corpora.yml:187` do it. Without it there is no
             `task` binary and the run step fails immediately.
          3. `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0`
             with `node-version: "24"` — the drift target shells out to
             `pnpm dlx shadcn-svelte@1.5.1`, and this repository resolves its pinned
             pnpm through Corepack, which is NOT distributed with Node 25+. The `24`
             pin is the same deliberate one `ci.yml:130-133` carries and for the same
             reason; a floating major silently removes the mechanism.
        Use the SAME action SHAs the existing workflows pin, not freshly-looked-up
        ones — a second SHA for the same action version is drift of its own.
      - pin every third-party action to a full commit SHA, as every other workflow
        in this repository does.

      **These three steps do NOT violate the "one meaningful `run:` step" rule, and
      no `runBodyExceptions` entry is needed for them.** `checkStepInvokesTask`
      (`internal/upgrade/taskfile_shape_test.go:1399-1414`) returns nil immediately
      for any step whose `Run` is empty, and all three bootstrap steps are `uses:`
      steps with no `run:` body — so the guard never inspects them. Adding an
      exception entry for a step the guard does not inspect would be worse than
      useless: `TestWorkflowRunBodiesInvokeTask` fails any exception that never
      matches a real inspected step, precisely so a stale entry cannot silently
      widen the allowlist.

      Without these three steps the workflow is syntactically valid, registers
      cleanly in both fixtures, passes `actionlint` — and fails every single
      scheduled run. That is the worst possible outcome for a mitigation: it looks
      wired and mitigates nothing.

      **The ORDER is asserted, not just the presence.** A workflow that carries all
      three steps but places `install-task` or `setup-node` AFTER
      `task web:components:drift` fails every scheduled run in exactly the same way
      a workflow missing them does. The acceptance criterion below therefore
      extracts a line number for each of `checkout`, `install-task`, `setup-node`,
      `node-version: "24"` and the run step, guards each extraction with `test -n`,
      and asserts the chain end to end. Write them in the order listed above and
      that gate is satisfied by construction.

    Do NOT add it to `release.yml`. That file carries a LOCKED contract — its name,
    path and tag-only trigger are anchored by `releaseWorkflowRefPattern` in
    `internal/upgrade/verify.go` for cosign OIDC SAN verification — and a
    network-dependent step inside the signing job could fail a release for a reason
    unrelated to the release. A scheduled job that emails on failure is the right
    urgency for vendored-source drift; a release blocker is not.

    Then register the new file in `internal/upgrade/taskfile_shape_test.go`:
    add `"components-drift.yml"` to `inScopeWorkflowFiles` and its
    `{Workflow: "components-drift.yml", JobID: "<job id>"}` entry to `inScopeJobs`.
    `TestWorkflowFilePopulationMatchesDisk` requires every file under
    `.github/workflows/` to appear in exactly one of `inScopeWorkflowFiles` or
    `workflowFileExceptions`, and `TestInScopeJobsPopulationMatchesDisk` requires
    every job in an in-scope file to be registered — so a new workflow that skipped
    this step would fail the suite, by design. Prefer `inScopeWorkflowFiles` over an
    exception: the job's body IS `task <target>`, which is exactly what that list is
    for, and an exception entry would be an unearned carve-out.

    Do NOT add the job's check name to `requiredCheckNames`. That fixture mirrors
    GitHub ruleset 20157557, which lives outside this repository; adding a name
    there does not create a required check, it only makes the guard assert
    something false.

    (h) `git mv` the registry-pinning todo into `.planning/todos/completed/` with a
    resolution record naming this target, the pinned CLI version, the observed file
    and component counts, the workflow that invokes it, and both RED-proofs.
  </action>
  <verify>
    <automated>task web:components:drift > /tmp/cdrift.log 2>&1; RC=$?; cat /tmp/cdrift.log; NFILES=$(rg -o 'compared [0-9]+ vendored' /tmp/cdrift.log | rg -o '[0-9]+'); echo "enumerated files: ${NFILES:-NONE}"; test "$RC" -eq 0 && test "$(rg -c 'compared [0-9]+ vendored component files across [0-9]+ components' /tmp/cdrift.log)" -ge 1 && test "${NFILES:-0}" -ge 8</automated>
  </verify>
  <acceptance_criteria>
    - `task web:components:drift` exits 0 AND prints a line matching `compared [0-9]+ vendored component files across [0-9]+ components`, AND the printed file count is at least 8 — the status, the report and a non-zero population, because a silent pass over an empty enumeration proves nothing.
    - The printed count equals `git ls-files -- 'web/src/lib/components/ui/' | wc -l` (NO trailing slash — the trailing-slash form returns 0 and is the defect this criterion exists to catch), and the printed component count equals `git ls-files -- 'web/src/lib/components/ui/' | awk -F/ '{print $6}' | sort -u | wc -l`.
    - BOTH RED proofs are recorded: (1) the target failed against a one-byte mutation and NAMED the mutated file, then passed after revert; (2) run with the trailing-slash pathspec, the population floor FAILED with its pathspec-naming message rather than passing over zero files.
    - `rg -n 'for c in|table tabs|"table" "tabs"' Taskfile.yml` shows no hand-enumerated component list inside the target; the subject set comes from `git ls-files`.
    - `rg -c "components/ui/\*/" Taskfile.yml` reports 0 — the trailing-slash pathspec appears nowhere in the target, positive-controlled by `rg -c "components/ui/" Taskfile.yml` reporting at least 1.
    - `rg -c 'shadcn-svelte@latest' Taskfile.yml` reports 0, positive-controlled by `rg -c 'shadcn-svelte@1.5.1' Taskfile.yml` reporting at least 1.
    - The mitigation is wired but not required: `rg -c 'web:components:drift' .github/workflows/ci.yml .github/workflows/release.yml` reports 0, AND `rg -c 'web:components:drift' .github/workflows/components-drift.yml` reports at least 1, AND `rg -c 'pull_request' .github/workflows/components-drift.yml` reports 0 while `rg -c 'workflow_dispatch' .github/workflows/components-drift.yml` reports at least 1.
    - **The scheduled workflow can reach its run step — in the right ORDER.** All four prerequisites are present, the two third-party pins are taken FROM `ci.yml` rather than retyped, and the full bootstrap chain `checkout < install-task < setup-node < run` holds:
      `WF=.github/workflows/components-drift.yml; CI=.github/workflows/ci.yml; CO=$(rg -o 'actions/checkout@[0-9a-f]{40}' "$CI" | head -1); SN=$(rg -o 'actions/setup-node@[0-9a-f]{40}' "$CI" | head -1); CL=$(rg -n 'actions/checkout@' "$WF" | head -1 | cut -d: -f1); IL=$(rg -n 'uses: ./.github/actions/install-task' "$WF" | head -1 | cut -d: -f1); NL=$(rg -n 'actions/setup-node@' "$WF" | head -1 | cut -d: -f1); VL=$(rg -n 'node-version: "24"' "$WF" | head -1 | cut -d: -f1); RL=$(rg -n 'task web:components:drift' "$WF" | head -1 | cut -d: -f1); echo "ci pins: ${CO:-NONE} ${SN:-NONE}; drift lines: checkout=${CL:-NONE} install-task=${IL:-NONE} setup-node=${NL:-NONE} node-version=${VL:-NONE} run=${RL:-NONE}"; test -n "$CO" && test -n "$SN" && test -n "$CL" && test -n "$IL" && test -n "$NL" && test -n "$VL" && test -n "$RL" && rg -q -F "$CO" "$WF" && rg -q -F "$SN" "$WF" && test "$CL" -lt "$IL" && test "$IL" -lt "$NL" && test "$NL" -lt "$RL" && test "$NL" -lt "$VL" && test "$VL" -lt "$RL"`
      The seven `test -n` guards are the positive controls, and each one is load-bearing for a different reason. For `$CO`/`$SN`, an empty extraction would make `rg -q -F ""` match anything and turn the pin check vacuous. For `$CL`/`$IL`/`$NL`/`$VL`/`$RL`, an empty extraction would make `test "" -lt ""` a malformed comparison whose behaviour must not be relied on to fail — an absent step has to fail LOUDLY on its own guard rather than by accident inside an ordering test. The pins are derived from `ci.yml` on disk, never from a literal in this plan.
      **Presence alone is not the property.** The cycle-2 fix asserted that `install-task`, `setup-node` and `node-version: "24"` occurred SOMEWHERE in the file, which a workflow placing either bootstrap step AFTER `task web:components:drift` satisfies — and that workflow then fails every scheduled run, which is precisely what this gate exists to prevent. Line numbers are extracted for `install-task` and `setup-node` too, and the chain is asserted end to end: `checkout` before `install-task` before `setup-node` before the run step, with `node-version: "24"` sitting inside the `setup-node` step (after its `uses:`, before the run step) rather than merely somewhere in the file.
    - `rg -c 'run:' .github/workflows/components-drift.yml` reports exactly `1` — one meaningful run step, positive-controlled by `rg -c 'uses:' .github/workflows/components-drift.yml` reporting at least 3, so the single `run:` sits among the three `uses:` bootstrap steps rather than in a workflow with no bootstrap at all.
    - `git diff internal/upgrade/taskfile_shape_test.go` shows the new workflow added to `inScopeWorkflowFiles` and its job to `inScopeJobs`, NO addition to `requiredCheckNames`, and NO addition to `runBodyExceptions` — the bootstrap steps carry no `run:` body, so `checkStepInvokesTask` never inspects them (`internal/upgrade/taskfile_shape_test.go:1399-1402`) and an exception entry for them would itself fail the suite as unmatched.
    - `git diff Taskfile.yml` shows no change to `web:drift`'s hashing pipeline or its `SRC_FLOOR`, and no change to `proto:drift`.
    - `GOTOOLCHAIN=go1.26.5 go test ./internal/upgrade/... -run 'TestWorkflowRunBodiesInvokeTask|TestWorkflowFilePopulationMatchesDisk|TestInScopeJobsPopulationMatchesDisk' -v > /tmp/wf.log 2>&1; RC=$?; test "$(rg -c '^--- PASS' /tmp/wf.log)" -ge 3 && test "$RC" -eq 0`.
    - `actionlint` (via `task` or the `ci.yml` actionlint job locally) accepts the new workflow file.
    - The registry-pinning todo exists only under `.planning/todos/completed/` with a resolution record naming both RED-proofs and the invoking workflow.
  </acceptance_criteria>
  <done>Vendored component source has real drift coverage, derived from disk, reporting what it compared, demonstrated to fail against a planted change, and invoked by a scheduled workflow that can actually reach its run step.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the 1000-row render cost, rebuild the committed bundle, and close the last todo</name>
  <files>web/tests/data-table-render-cost.test.ts, web/build/, .planning/todos/pending/2026-08-28-client-side-render-cost-measurement-for-browse-views.md, .planning/todos/completed/2026-08-28-client-side-render-cost-measurement-for-browse-views.md</files>
  <read_first>
    - .planning/todos/pending/2026-08-28-client-side-render-cost-measurement-for-browse-views.md (the todo's own framing of what it wanted measured)
    - web/src/lib/components/workbench/DataTable.svelte (the component under measurement)
    - internal/query/validate.go (MaxLimit = 1000 — the real upper bound on rows, which is what makes 1000 the right measurement point)
    - .github/workflows/ci.yml:149 (the `web unit tests (vitest)` step: `task web:test`, in the PR-triggered REQUIRED `test` job — the reason a wall-clock threshold must not be an unconditional assertion in any `web/tests/*.test.ts` file)
    - Taskfile.yml:567-600 (web:test — it runs the WHOLE vitest suite indiscriminately, so "put it in web/tests/" and "put it in a required check" are the same decision)
    - Taskfile.yml:626-757 (web:build and web:build:verify — how the committed bundle is produced and checked)
    - .planning/phases/03-browse-inspect-navigation/03-09-SUMMARY.md (how the previous intended-RED web:drift window was closed: rebuild, re-commit, verify green, and record both digests)
    - .planning/phases/04-query-workbench-index-health/04-RESEARCH.md (@tanstack/svelte-virtual 3.13.36 — confirmed available and Svelte-5-compatible, so no gap exists between "threshold trips" and "library is ready")
  </read_first>
  <action>
    (a) **Measure.** Write `web/tests/data-table-render-cost.test.ts`: mount
    `DataTable` with a generated 1000-row `Location` fixture — `MaxLimit` is 1000,
    so this is the real worst case, not an invented one — and record two numbers
    across five repetitions each, reporting the MEDIAN, not a single sample:
      - initial render duration (`performance.now()` around mount plus a settle),
      - one full sort-toggle duration on the `name` column.
    Also record the rendered `<tr>` count, so a measurement that silently rendered
    zero rows cannot read as a fast one — that is the positive control on the
    timing assertion.

    **The threshold, fixed here so the decision cannot be made after seeing the
    number:** median initial render > 400ms OR median sort toggle > 200ms, measured
    in this jsdom environment. jsdom timing is not browser timing; this threshold is
    a coarse detector for an order-of-magnitude problem, not a performance budget,
    and the test must say so in a comment.

    **Where that threshold may and may not be asserted.** `task web:test` runs every
    file under `web/tests/` indiscriminately (`Taskfile.yml:567-600`) and is a
    PR-triggered REQUIRED CI job (`.github/workflows/ci.yml:149`). A wall-clock
    duration compared against a fixed constant inside a required check varies with
    runner load, architecture, coverage instrumentation and accumulated DOM — it is
    a flake generator, and it is the same nondeterministic-gate shape this plan
    rejects for the drift target two tasks up. So:

      - **Unconditionally in the test file:** only DETERMINISTIC assertions — the
        rendered `<tr>` count is 1000, and a sort toggle genuinely reorders the
        rendered rows (compare the rendered order before and after, not a class).
        Both medians and the row count are PRINTED on every run, so `task web:test`
        still surfaces the numbers and a human reading CI output sees a regression.
      - **Behind an opt-in flag:** the threshold comparison runs only when
        `process.env.RENDER_COST_ASSERT === '1'`, with a comment naming
        `.github/workflows/ci.yml:149` as the reason. Add a `web:render-cost`
        target to `Taskfile.yml` that sets that variable and runs this one test
        file, and wire it into NO workflow — the same "runnable on purpose, never
        required" posture D-16 gives the drift target.

    D-08 is measurement-first, and this split is what preserves that: the number is
    always recorded, the decision is made from it once, here, and no future PR is
    blocked by a jsdom stopwatch.

    (b) **Branch on the measurement, and record which branch was taken:**
      - Under threshold → record the numbers, install nothing. D-08 is measurement
        first; a library that is not needed is not added.
      - Over threshold → HALT and report. Virtualizing means installing
        `@tanstack/svelte-virtual@3.13.36`, which carries the same `[SUS]`
        `too-new` verdict as this phase's other npm packages and therefore needs its
        own blocking package-legitimacy checkpoint, which THIS plan does not
        pre-authorize. Report the measured numbers and request that checkpoint.
        Under no circumstances add a client-side row cap instead — D-08 names that
        as rejected: never re-cap what the server already bounded.

    (c) **Rebuild and re-commit the bundle.** Every frontend plan in this phase
    changed `web/src/`, so the committed `web/build/` no longer matches its source
    and `task web:drift` has been intentionally RED since 04-01 — a scheduled
    window, exactly as 03-01 opened one and 03-09 closed it. Run `task web:build`,
    commit the regenerated `web/build/`, then run `task web:drift` and confirm
    GREEN. Record the before/after source and output digests and file counts, as
    03-09 did, so the window's closure is auditable rather than asserted.

    (d) `git mv` the render-cost todo into `.planning/todos/completed/` with a
    resolution record carrying the two median numbers, the row count, the threshold,
    and the branch taken.

    (e) Record in the SUMMARY the phase-level state of every gate:
    `task web:test`, `task web:drift`, `task web:components:drift`,
    `task web:render-cost`, `task proto:drift`, `task test:unit`,
    `cd web && pnpm check`, `task web:audit`, `task web:lockfile`,
    `task web:deps:strict` — each with its printed count where it prints one. This
    is the phase's hand-off to `/gsd-verify-work`.
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run --reporter=json --outputFile=/tmp/rc.json tests/data-table-render-cost.test.ts; RC=$?; node -e 'const r=JSON.parse(require("fs").readFileSync("/tmp/rc.json","utf8"));console.log("render-cost: total="+r.numTotalTests+" passed="+r.numPassedTests);if(r.numTotalTests<3||r.numPassedTests!==r.numTotalTests)process.exit(1)' && test "$RC" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The render-cost test reports `total >= 3` with `passed === total` and exit status 0 (floor and status `&&`-chained as ONE final command), and the run PRINTS both medians and the rendered row count.
    - The rendered row count is 1000 — the positive control proving the timing was measured over a real render, not an empty one.
    - No unconditional wall-clock threshold is asserted: `rg -c 'RENDER_COST_ASSERT' web/tests/data-table-render-cost.test.ts` reports at least 1, and every numeric duration comparison in the file is inside that guarded branch (readable in the diff). Positive-controlled by `rg -c 'performance.now' web/tests/data-table-render-cost.test.ts` reporting at least 1 — the timing IS measured, it is only the pass/fail comparison that is opt-in.
    - `task web:render-cost` exists, sets `RENDER_COST_ASSERT=1`, and appears in NO workflow: `rg -c 'web:render-cost' .github/workflows/*.yml` reports 0, positive-controlled by `rg -c 'web:render-cost' Taskfile.yml` reporting at least 1.
    - `task web:render-cost` was run once and its result — pass or the reported over-threshold numbers — is recorded in the SUMMARY.
    - The SUMMARY records both medians, the threshold, and which branch was taken.
    - `task web:drift` exits 0 — the scheduled RED window opened at 04-01 is closed, with before/after digests and file counts recorded as 03-09 did.
    - `task web:components:drift` still exits 0 after the rebuild.
    - `task web:test` reports `PASS — N of N` with N greater than the count recorded at the end of 04-06.
    - Both remaining folded todos exist only under `.planning/todos/completed/`, each with a resolution record; `ls .planning/todos/pending/ | wc -l` reports 4 (the four pre-existing unrelated todos), and none of the three Phase-4 todos is among them.
    - No `@tanstack/svelte-virtual` entry exists in `web/package.json` unless a legitimacy checkpoint for it was run and recorded.
  </acceptance_criteria>
  <done>A recorded measurement states what a full-limit table costs, the committed bundle matches its source again, and all three folded todos are closed with resolution records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `shadcn-svelte.com` registry → the drift target's scratch regeneration | A live external network service is consulted by a repository gate |
| committed `web/src/lib/components/ui/` → the drift comparison | The gate's subject set determines what is actually protected |
| `web/src/` → committed `web/build/` → `go:embed` → the signed release binary | Source changes must be reflected in the artifact that actually ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-27 | Tampering | an undetected modification to vendored component source, which no lockfile-based gate can see | high | mitigate | This target is the mitigation, and it is now WIRED: pinned-CLI regeneration into a scratch directory plus `cmp -s` byte comparison, with a disk-derived subject set so later components are covered automatically, a count reported before comparing, a demonstrated RED against a planted one-byte change, and a scheduled/dispatchable `components-drift.yml` workflow that invokes it without a human remembering. An earlier draft called the target "release-path" while requiring it to appear in no workflow at all — a `high` threat mitigated by a gate nobody was obliged to run is not mitigated |
| T-04-28 | Denial of Service | the drift target wedging a required PR check, or a release, when the registry is unreachable or changes | high | mitigate | D-16 keeps it out of required checks entirely. The invoking workflow declares ONLY `schedule` and `workflow_dispatch` — no `pull_request`, no `push` — asserted by a zero-match grep, and its job name is deliberately NOT added to `requiredCheckNames`. It is also kept out of `release.yml`, whose name/path/trigger are anchored by `releaseWorkflowRefPattern` (`internal/upgrade/verify.go`) for cosign SAN verification and whose failure would block a release for a reason unrelated to it. Rule `f18zrdsgx5` records why a wedged required check on `protect-main` is expensive to recover from |
| T-04-29 | Tampering | the guard passing vacuously because its subject set narrowed — or was empty from the start | high | mitigate | Three controls, all required: the subject set is derived from `git ls-files` rather than hand-listed (so a new component cannot pass by absence); the pathspec carries NO trailing slash, because the trailing-slash form returns zero files in this repository and cycle-1 review caught exactly that as a would-be silent pass; and both the file and component counts are printed and floor-checked against small structural minima BEFORE any comparison (rule `84d1gfpywd`). Task 2 step (f) additionally requires the enumeration floor to be WATCHED reject the trailing-slash pathspec, so the floor itself is tested rather than merely present |
| T-04-30 | Tampering | a stale committed `web/build/` shipping inside the signed binary while `web/src/` has moved on | high | mitigate | Task 3 rebuilds and re-commits the bundle and requires `task web:drift` GREEN with recorded before/after digests. The intentional RED window is declared in every intervening plan's verification section so it is never mistaken for a regression, and never left open |
| T-04-31 | Tampering | a `[SUS]` npm package entering the tree without a legitimacy decision | high | mitigate | The over-threshold branch HALTS rather than installing `@tanstack/svelte-virtual`; the plan's prohibitions and Task 3's acceptance criteria both state that this plan does not pre-authorize that install |
</threat_model>

<verification>
- `task web:components:drift` exits 0 and prints both its enumerated file and component counts; BOTH RED proofs (planted byte, trailing-slash pathspec) are recorded.
- `task web:drift` exits 0 — the committed bundle matches its source.
- `task proto:drift` exits 0 and prints `compared 4 generated files`.
- `task web:test` reports `PASS — N of N`; `GOTOOLCHAIN=go1.26.5 task test:unit` is green.
- `task web:render-cost` was run once and its outcome recorded (it is opt-in and in no workflow).
- `cd web && pnpm check`, `task web:lockfile`, `task web:deps:strict` and `task web:audit` are green or their findings recorded.
- `rg -c 'web:components:drift' .github/workflows/ci.yml .github/workflows/release.yml` reports 0, and `rg -c 'web:components:drift' .github/workflows/components-drift.yml` reports at least 1 — out of required checks, but genuinely invoked.
- `GOTOOLCHAIN=go1.26.5 go test ./internal/upgrade/... -run 'TestWorkflowRunBodiesInvokeTask|TestWorkflowFilePopulationMatchesDisk|TestInScopeJobsPopulationMatchesDisk'` is green with the new workflow registered.
</verification>

<success_criteria>
- Vendored component source is covered by a guard that reports what it compared, derives its subjects from disk, and has been watched fail.
- A recorded measurement — with medians and a row-count positive control — states what a 1000-row table costs.
- The committed bundle is current and `web:drift` is green.
- All three folded todos are in `.planning/todos/completed/` with resolution records.
</success_criteria>

## Artifacts this phase produces

Symbols and paths created by THIS plan (new — not drift):

**New Taskfile targets**
- `web:components:drift`
- `web:render-cost` (opt-in; sets `RENDER_COST_ASSERT=1`; in no workflow)

**New workflow**
- `.github/workflows/components-drift.yml` — `schedule` + `workflow_dispatch` only; one job whose run body is exactly `task web:components:drift`

**Changed test fixtures (`internal/upgrade/taskfile_shape_test.go`)**
- `inScopeWorkflowFiles` gains `"components-drift.yml"`; `inScopeJobs` gains its job. `requiredCheckNames` is deliberately UNCHANGED.

**New test file**
- `web/tests/data-table-render-cost.test.ts`

**Regenerated committed artifact**
- `web/build/` (rebuilt from the phase's frontend source)

**Relocated planning artifacts**
- `.planning/todos/completed/2026-08-28-shadcn-svelte-registry-version-pinning-with-source-match.md`
- `.planning/todos/completed/2026-08-28-client-side-render-cost-measurement-for-browse-views.md`

## Cycle-1 cross-AI review dispositions (this plan)

| Finding | Severity | Disposition |
|---|---|---|
| The enumeration command returns ZERO files, making the floor vacuous | HIGH | **Incorporated.** Verified in-tree: `git ls-files -- 'web/src/lib/components/ui/*/'` → **0**; without the trailing slash → **36**. Task 2 step (a) uses the no-slash pathspec and derives components with `awk -F/ '{print $6}'`; a prohibition names the trailing-slash form explicitly; step (f) requires the enumeration floor to be WATCHED reject it; Task 1 records the counter-check as evidence. |
| A wall-clock threshold inside a required PR check | HIGH | **Incorporated.** The test file keeps only deterministic assertions (1000 rendered rows; a sort toggle genuinely reorders) and PRINTS the medians; the threshold comparison is behind `RENDER_COST_ASSERT=1` and a new opt-in `task web:render-cost` that is in no workflow. D-08 is measurement-first, which this split preserves rather than weakens. |
| "release-path" claim unbacked — the mitigation runs nowhere | MEDIUM | **Incorporated by wiring, not by dropping the claim.** New `.github/workflows/components-drift.yml` (`schedule` + `workflow_dispatch` ONLY) invokes the target, registered in `inScopeWorkflowFiles`/`inScopeJobs` because `TestWorkflowFilePopulationMatchesDisk` requires it. Deliberately NOT `release.yml` — that file's name/path/trigger are anchored by `releaseWorkflowRefPattern` for cosign SAN verification, and a network step there could fail a release for an unrelated reason. `requiredCheckNames` is untouched, so D-16 holds. |
| One pinned CLI version vs six already-vendored families | MEDIUM | **Incorporated.** Task 1 now probes ALL families derived from disk (eight, not two), reports question 5 as a per-family table, and states HALT as the LIKELY outcome with three named options — none of which is re-vendoring committed source inside this plan to make a guard pass. |
| The floor equals today's count, so removal ratchets | LOW | **Incorporated.** Two small structural minima (`nfiles -lt 8`, `ncomponents -lt 2`) instead of the observed ~44, matching `web:test`'s FLOOR and `web:drift`'s `SRC_FLOOR` conventions, with the "never a growth ratchet" comment retained. |
| Verify gate discards its count floor | HIGH | **Incorporated.** Task 3's gate rewritten to the `&&`-chained shape (Task 2's was already correct); a prohibition forbids the broken form. |

## Cycle-2 cross-AI review dispositions (this plan)

| Finding | Severity | Disposition |
|---|---|---|
| `components-drift.yml` has no bootstrap and would fail every scheduled run | MEDIUM (NEW — introduced by the cycle-1 revision that added the workflow) | **Incorporated.** Task 2 step (g) now mandates three `uses:` bootstrap steps before the run step, in order: `actions/checkout`, the repository's own `./.github/actions/install-task`, and `actions/setup-node` at `node-version: "24"` — matching `ci.yml:51,65,130-133` and `corpora.yml:159,187`, because the drift target shells out to `pnpm dlx shadcn-svelte@1.5.1` and this repo resolves its pinned pnpm through Corepack, which Node 25+ does not ship. A new acceptance criterion asserts all four markers, EXTRACTING the two third-party SHAs from `ci.yml` on disk rather than hardcoding them (with `test -n` positive controls so an empty extraction fails loudly instead of making `rg -F ""` match everything), and asserts checkout precedes the run step. **Stated explicitly in the plan:** these steps do NOT violate the one-meaningful-`run:`-step rule and need no `runBodyExceptions` entry, because `checkStepInvokesTask` (`internal/upgrade/taskfile_shape_test.go:1399-1402`) returns nil for any step with an empty `Run` — and a new prohibition forbids adding such an entry, since `TestWorkflowRunBodiesInvokeTask` fails any exception that never matches a real inspected step. Without this, the workflow registers cleanly, passes `actionlint`, and mitigates nothing — the worst outcome available for a `high` threat's only structural control. |

## Cycle-3 cross-AI review dispositions (this plan)

| Finding | Severity | Disposition |
|---|---|---|
| The bootstrap gate asserts ordering only for `checkout → run`; `install-task`, `setup-node` and `node-version: "24"` are asserted to occur only SOMEWHERE in the file, so a workflow placing either bootstrap step after the run step passes the gate and still fails every scheduled run | MEDIUM (NEW — a gap in the cycle-2 fix, not in the original plan) | **Incorporated.** The criterion now extracts line numbers for `install-task` (`$IL`), `setup-node` (`$NL`) and `node-version: "24"` (`$VL`) alongside the existing `$CL`/`$RL`, guards all five with `test -n` under the same discipline as the four SHA guards, and asserts the full chain `checkout < install-task < setup-node < run` plus `setup-node < node-version < run` — so the version pin is proven to sit INSIDE the `setup-node` step rather than merely somewhere in the file. The `test -n` guards are load-bearing for a second reason here, spelled out in the criterion: an empty extraction makes `test "" -lt ""` a malformed comparison, and an absent step must fail loudly on its own guard rather than by accident inside an ordering test. The four pre-existing SHA `test -n` guards are RETAINED unchanged — cycle 3 verified they correctly precede every `rg -q -F "$VAR"` use. Task 2 step (g)'s authoring text now states that the order is gated by line number, so writing the steps in the listed order satisfies it by construction. No change to `checkStepInvokesTask` reasoning, `requiredCheckNames`, `release.yml`, the `web:components:drift` pathspec, or its 8-file / 2-component floors. |

<output>
Create `.planning/phases/04-query-workbench-index-health/04-07-SUMMARY.md` when done.
</output>
