---
phase: 04-query-workbench-index-health
plan: 05
type: tdd
wave: 2
depends_on: ["04-03"]
files_modified:
  - web/src/lib/health-view.ts
  - web/src/lib/components/health/TrustVerdict.svelte
  - web/src/lib/components/health/WorktreeMismatchWarning.svelte
  - web/src/lib/components/health/CountTable.svelte
  - web/src/routes/health/+page.svelte
  - web/tests/health-view.test.ts
  - web/tests/health-page.test.ts
autonomous: true
requirements: [HLT-01, HLT-02, HLT-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Index freshness, per-language file counts, and node and edge counts are all readable from the single /health view without navigating anywhere else (HLT-01)."
    - "The trust verdict element PRECEDES every numeric block in document order (HLT-02) — a property asserted with compareDocumentPosition, not judged by eye."
    - "A worktree mismatch renders as a first-class warning with an alert role, before the numbers, naming both the worktree root and the index root (HLT-03)."
    - "A clean repository renders NO worktree warning — the absence is asserted, so the warning cannot be permanently on."
    - "The verdict shown on /health is the SAME value the application-wide status banner shows; there is exactly one classifyStatus in the tree."
    - "When the GetStatus snapshot and the GetHealth snapshot disagree about the indexed commit, the page states which one the numbers came from and marks them as possibly superseded — it never silently renders two snapshots as one."
  artifacts:
    - web/src/lib/health-view.ts
    - web/src/lib/components/health/TrustVerdict.svelte
    - web/src/lib/components/health/WorktreeMismatchWarning.svelte
    - web/src/lib/components/health/CountTable.svelte
    - web/tests/health-view.test.ts
    - web/tests/health-page.test.ts
  key_links:
    - "/health reads the trust verdict from the shared status gate in context (set by web/src/routes/+layout.svelte) — it never constructs a second gate and never issues a second GetStatus."
    - "/health calls uiClient.getHealth exactly once per view open — the only caller of the new rpc."
    - "The count tables render through the ONE DataTable shell from 04-01 — which 04-01 makes GENERIC over its row type with an explicit getRowId prop, so CountRow{key,count} binds it without a second table implementation and without widening CountRow to look like a Location."
    - "classifyStatus in web/src/lib/status.ts remains the single verdict authority (D-04)."
    - "describeFreshness compares the gate's commit against GetHealth's commit and returns the disagreement as a named field — the page renders that field; it does not re-derive agreement in markup."
  prohibitions:
    - "MUST NOT define a second status/health verdict function alongside classifyStatus, and MUST NOT extend StatusVerdict with a sixth member (D-04)."
    - "MUST NOT re-derive staleness from IndexHealth.reindexRecommended, PendingChanges or the worktree mismatch — two verdict functions over one index can disagree, which is the repudiation failure HLT-02 exists to prevent."
    - "MUST NOT create a second status gate or issue a second GetStatus call from this route."
    - "MUST NOT poll, refetch on a timer, or use any timer API for either GetStatus or GetHealth (Phase 3 D-05's no-polling contract)."
    - "MUST NOT surface capability-matrix Coverage or an indexed-vs-on-disk ratio — both are explicitly deferred (D-03)."
    - "MUST NOT render projectPath or indexPath — they are not on the wire and must not be reintroduced; worktree_mismatch's two roots are the one scoped exception."
    - "MUST NOT build a second table implementation for the count listings."
    - "MUST NOT widen CountRow with filePath/startLine/name fields to satisfy a Location-typed DataTable — if the shell is not generic when this plan starts, that is a 04-01 defect to report, not a shape to work around here."
    - "MUST NOT write a verify gate in the shape `<cmd>; RC=$?; node -e '…'; test \"$RC\" -eq 0` — the compound's status is the LAST command's, so the floor is discarded. Chain floor and status as ONE final `&&` command (04-VALIDATION.md §The ONE correct shell shape)."
---

<objective>
Fill the `/health` placeholder so a developer can tell at a glance whether the
index they are reading is worth trusting.

Two facts shape this view. First, the verdict is NOT new work: Phase 3 already
produces a five-member `StatusVerdict` from `classifyStatus`, and
`StatusBanner.svelte` already renders it application-wide. D-04 reuses it
unchanged and forbids a second verdict function — two verdict functions over one
index can disagree, and the banner contradicting the health page about staleness
is precisely the repudiation failure HLT-02 exists to prevent. Second, every raw
number now arrives from one `GetHealth` call (04-03).

HLT-02's "above the raw numbers" and HLT-03's "impossible to miss" both look like
visual judgments and are not. Document order is a `compareDocumentPosition`
property; a loaded style hook is a `getComputedStyle` property. Phase 3 already
established that "requires a human" is a testable claim rather than a category —
two of its three `why_human` justifications turned out to be false. Both are
asserted in code here.

Purpose: HLT-01, HLT-02 and HLT-03 become true.
Output: a filled `/health` route with a trust verdict above the numbers, a loud
worktree warning, per-language file counts, and node and edge counts by kind.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-query-workbench-index-health/04-CONTEXT.md
@.planning/phases/04-query-workbench-index-health/04-RESEARCH.md
@.planning/phases/04-query-workbench-index-health/04-PATTERNS.md
@.planning/phases/04-query-workbench-index-health/04-VALIDATION.md
@.planning/phases/04-query-workbench-index-health/04-03-SUMMARY.md
</context>

<feature>
  <name>The index-health view</name>
  <files>web/src/lib/health-view.ts, web/src/routes/health/+page.svelte</files>
  <behavior>
    - Opening `/health` issues exactly one `GetHealth` call and renders, in this
      document order: the worktree warning (when present), the trust verdict, then
      the numeric blocks.
    - Per-language file counts render as a sortable two-column table; node counts
      by kind and edge counts by kind render as two more.
    - Freshness reads from the verdict plus the commit SHA, the schema version and
      the index-health block.
    - A `GetHealth` failure renders a named failure state, not a blank pane.
  </behavior>
  <implementation>A pure derivation module for the map-to-rows projections, three small presentational components, and a route that composes them with the shared status gate and the shared DataTable.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: health-view.ts — the pure projections from GetHealthResponse to rendered rows</name>
  <files>web/src/lib/health-view.ts, web/tests/health-view.test.ts</files>
  <read_first>
    - web/src/lib/gen/ui_pb.ts (the regenerated GetHealthResponse type — the exact generated field names and map types)
    - web/src/lib/status.ts (IndexStatus, StatusVerdict, classifyStatus — reused, never reimplemented)
    - web/src/lib/browse-state.ts:71-90 (the "minimal client interface declared independently of the generated client" convention every module in this codebase follows so a test stub needs no transport)
    - .planning/phases/04-query-workbench-index-health/04-03-SUMMARY.md (the frozen GetHealthResponse field list)
  </read_first>
  <behavior>
    `web/tests/health-view.test.ts`, written RED before the module exists:
    - `toCountRows({ go: 12, ts: 3, py: 3 })` returns rows `{ key, count }` sorted
      by descending count then ascending key — a deterministic order, so the
      rendered table has a stable default even before the user sorts it.
    - `toCountRows({})` returns an empty array, and `toCountRows(undefined)`
      returns an empty array without throwing — a pre-upgrade or degraded response
      must render an empty table, never a crash.
    - `hasWorktreeMismatch(response)` is true only when `worktreeMismatch` is
      present AND both roots are non-empty; a present-but-blank mismatch is FALSE.
      This matters: a false positive here makes the loudest warning in the app
      permanently on, which trains the user to ignore it.
    - `describeFreshness(response, status)` returns a value carrying the verdict
      (taken from the passed-in `IndexStatus`, never recomputed), the commit SHA or
      an explicit unknown marker when it is empty, the schema version, and whether
      a re-index is recommended — as separate named fields, so the renderer never
      has to parse a formatted string.
    - `describeFreshness` NEVER returns a verdict it computed itself: passing an
      `IndexStatus` of `{verdict:'ok'}` alongside a response with
      `indexHealth.reindexRecommended === true` still yields verdict `ok`, with
      the re-index recommendation as a SEPARATE field. This is the D-04 assertion
      in executable form.
    - `describeFreshness` reports SNAPSHOT AGREEMENT as a named field. `/health`
      renders two independently-fetched snapshots: the verdict comes from the
      shared `GetStatus` gate (fetched on construction and navigation,
      `status.ts:116`) and every number comes from a separate `GetHealth` call. A
      re-index landing between the two makes them legitimately disagree, and
      HLT-02's whole point is that the verdict is trustworthy — so the page must
      state which snapshot the numbers came from rather than blending them.
      Return `snapshotAgreement: 'agree' | 'differs' | 'unknown'`, computed ONLY by
      comparing the passed-in `IndexStatus.commit` with the response's
      `commitSha`: equal and both non-empty → `agree`; both non-empty and unequal →
      `differs`; either empty → `unknown`. Assert all three. This is a comparison,
      not a verdict: it adds no sixth `StatusVerdict` member and computes no
      staleness of its own, so D-04 is untouched.
  </behavior>
  <action>
    Write `web/tests/health-view.test.ts` from the `<behavior>` block, record its
    RED failing count in the SUMMARY, then create `web/src/lib/health-view.ts`
    exporting `CountRow`, `toCountRows`, `hasWorktreeMismatch`, `FreshnessView`
    and `describeFreshness`.

    Declare a minimal `HealthClient` interface in this module — the shape
    `{ getHealth(request, options?): Promise<GetHealthResponse> }` — following
    `browse-state.ts`'s `NodeDetailClient` / `ImpactClient` and `status.ts`'s
    `StatusClient` convention, so route tests can stub it with no Connect
    transport.

    <!-- planner-discipline-allow: classifyStatus -->
    `describeFreshness` takes the already-computed `IndexStatus` as a PARAMETER.
    It must not import or call `classifyStatus` itself and must not read
    `initialized`/`stale`/`storeExists` to derive a verdict. There is one verdict
    authority and this module is downstream of it (D-04). NOTE for the executor:
    an acceptance criterion negative-greps `classifyStatus|initialized|storeExists`
    over `health-view.ts`, so none of those words may appear in a comment in that
    file — this plan is where the rule is explained.
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run --reporter=json --outputFile=/tmp/hv.json tests/health-view.test.ts; RC=$?; node -e 'const r=JSON.parse(require("fs").readFileSync("/tmp/hv.json","utf8"));console.log("health-view: total="+r.numTotalTests+" passed="+r.numPassedTests);if(r.numTotalTests<8||r.numPassedTests!==r.numTotalTests)process.exit(1)' && test "$RC" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The verify command reports `total >= 8` with `passed === total` and exit status 0; the RED count is recorded in the SUMMARY. (Floor and status are ONE `&&`-chained final command.)
    - All three `snapshotAgreement` outcomes (`agree`, `differs`, `unknown`) are asserted.
    - `rg -n 'classifyStatus|initialized|storeExists|indexingInProgress' web/src/lib/health-view.ts` reports no match, positive-controlled by `rg -c 'IndexStatus' web/src/lib/health-view.ts` reporting at least 1 — the module consumes a verdict, it does not compute one.
    - The blank-roots case is asserted: `hasWorktreeMismatch` returns false for a mismatch whose roots are empty strings.
    - `rg -o 'export function classifyStatus' web/src/lib -g '*.ts' | wc -l` reports exactly `1`, and `rg -l 'StatusVerdict =' web/src/lib -g '*.ts'` names only `status.ts` — one verdict type, one verdict function (D-04).
    - `cd web && pnpm check` exits 0.
  </acceptance_criteria>
  <done>Every projection the health view renders is a pure, tested function that consumes the one verdict rather than inventing a second one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill /health — verdict above the numbers, worktree warning first</name>
  <files>web/src/lib/components/health/TrustVerdict.svelte, web/src/lib/components/health/WorktreeMismatchWarning.svelte, web/src/lib/components/health/CountTable.svelte, web/src/routes/health/+page.svelte</files>
  <read_first>
    - web/src/routes/health/+page.svelte (the D-18 placeholder being filled, not restructured)
    - web/src/lib/components/StatusBanner.svelte (the verdict-branch rendering idiom, its role="status" and data-testid conventions — the shape TrustVerdict extends rather than replaces)
    - web/src/routes/+layout.svelte (setContext('statusGate', …) — the gate this route subscribes to, and the reason a second gate must not be created)
    - web/src/lib/components/workbench/DataTable.svelte (the shared table shell the count listings reuse)
    - web/src/lib/health-view.ts (Task 1's projections)
    - web/src/lib/workbench-failure.ts (the shared failure taxonomy, reused for a GetHealth rejection)
  </read_first>
  <behavior>
    Rendered document order on `/health`, top to bottom:
    1. `WorktreeMismatchWarning` — rendered ONLY when `hasWorktreeMismatch` is
       true, carrying `role="alert"` and `data-testid="health-worktree-mismatch"`,
       naming both roots in its text.
    2. `TrustVerdict` — one branch per `StatusVerdict` member, `role="status"`,
       `data-testid="health-verdict-{verdict}"`, stating what the verdict means
       for trusting what follows.
    3. The freshness block — commit SHA (or an explicit unknown marker), schema
       version, re-index recommendation, pending changes, AND the snapshot rule:
       **the verdict in block 2 always wins.** It comes from the shared gate, it is
       the value the application-wide banner shows, and D-04 exists so those two
       can never contradict each other. When `snapshotAgreement === 'differs'` the
       numeric blocks carry a visible notice
       (`data-testid="health-snapshot-differs"`) stating that they were read at a
       different indexed commit than the verdict and may be superseded — the
       numbers are NOT suppressed and the verdict is NOT recomputed from them. A
       page that renders two snapshots as one is exactly the repudiation failure
       HLT-02 exists to prevent, and silence here is that failure.
    4. The three count tables — per-language file counts, node counts by kind,
       edge counts by kind — each a `DataTable` over `CountRow`.
    A `GetHealth` rejection renders `describeWorkbenchFailure`'s named state in
    place of blocks 3 and 4; blocks 1 and 2 still render from the status gate,
    because the whole point of HLT-02 is that a trust verdict is available exactly
    when the numbers are not.
  </behavior>
  <action>
    (a) `TrustVerdict.svelte` — props `{ status: IndexStatus }`. One `{#if}` /
    `{:else if}` branch per verdict, following `StatusBanner.svelte`'s existing
    idiom, `role="status"` and `data-testid="health-verdict-{verdict}"`. Unlike the
    banner it renders for ALL five verdicts including `ok`, because the health page's
    job is to answer "should I trust this" affirmatively too, not only to warn.

    (b) `WorktreeMismatchWarning.svelte` — props `{ worktreeRoot, indexRoot }`.
    `role="alert"` (not `status`: an alert is announced, which is what
    "impossible to miss" means for a screen-reader user), a distinct border/background
    class pair in the same family `StatusBanner.svelte` uses for its `stale` branch,
    and text naming both roots. HLT-03 is the loudest signal in the app and it must
    read as more urgent than the ordinary stale banner.

    (c) `CountTable.svelte` — props `{ title, rows: CountRow[], keyHeader }`. A thin
    wrapper over the GENERIC `DataTable` 04-01 built: instantiate it at
    `TRow = CountRow` with a two-column `ColumnDef<CountRow>[]` (`key`
    alphanumeric-sorted, `count` numeric), `getRowId={(row) => row.key}`, and an
    empty message. One implementation, three call sites — do not write three tables.

    `getRowId` is a PROP for exactly this reason: `CountRow` is `{key, count}` and
    has no `filePath`/`startLine`/`name`, so a shell that hard-coded a `Location`
    identity would fail `pnpm check` here — which criterion 1 below requires to
    exit 0. If `DataTable` arrives from 04-01 typed to `Location`, STOP and report
    it as a 04-01 defect; do NOT widen `CountRow` with dummy fields and do NOT
    fork a second table to route around it.

    (d) `web/src/routes/health/+page.svelte` — read the shared gate with
    `getContext('statusGate')` and subscribe to it for `IndexStatus`. Call
    `uiClient.getHealth({})` ONCE when the view opens, with an `AbortController`
    tied to component teardown. No timer, no polling, no refetch — Phase 3 D-05's
    contract. Compose the four blocks in the order above. Render a rejection
    through `describeWorkbenchFailure(err, status)`.

    (e) Layout is Claude's discretion (04-CONTEXT.md), but the ORDER is not:
    whatever visual arrangement is chosen, the warning and the verdict must precede
    every numeric element in DOM order, because that is what Task 3 asserts and what
    HLT-02 literally requires.
  </action>
  <verify>
    <automated>cd web && pnpm check && pnpm exec vitest run --reporter=json --outputFile=/tmp/hp0.json tests/health-view.test.ts; RC=$?; node -e 'const r=JSON.parse(require("fs").readFileSync("/tmp/hp0.json","utf8"));if(r.numPassedTests!==r.numTotalTests||r.numTotalTests<8)process.exit(1);console.log("health-view still green: "+r.numPassedTests+"/"+r.numTotalTests)' && test "$RC" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm check` exits 0 — this is the concrete proof that `CountTable` binds the generic `DataTable` at `CountRow`; a `Location`-typed shell fails here.
    - `rg -c 'getRowId' web/src/lib/components/health/CountTable.svelte` reports at least 1 — the row identity is supplied by the caller, not assumed by the shell.
    - `rg -c 'createStatusGate' web/src/routes/health/+page.svelte` reports 0, positive-controlled by `rg -c 'createStatusGate' web/src/routes/+layout.svelte` reporting at least 1 — exactly one gate exists in the app and this route is not it.
    - `rg -n 'setInterval|setTimeout|requestAnimationFrame|setImmediate' web/src/routes/health web/src/lib/components/health` reports no match, positive-controlled by `rg -c 'getHealth' web/src/routes/health/+page.svelte` reporting at least 1 — no timer API on the health path (D-05).
    - `rg -c 'role="alert"' web/src/lib/components/health/WorktreeMismatchWarning.svelte` reports at least 1.
    - `rg -n 'projectPath|indexPath' web/src/routes/health web/src/lib/components/health` reports no match.
    - `rg -o 'DataTable' web/src/lib/components/health/CountTable.svelte | wc -l` reports at least 1, and no `<table` literal appears in any file under `web/src/lib/components/health/`.
  </acceptance_criteria>
  <done>Opening /health shows a trust verdict and, when applicable, a loud worktree warning above every number, with per-language file counts and node and edge counts by kind all on one page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert the ordering, the loudness, and the absence — the three claims that look manual and are not</name>
  <files>web/tests/health-page.test.ts</files>
  <read_first>
    - web/src/routes/health/+page.svelte and the three components from Task 2
    - web/tests/degrade-states.test.ts (how an existing test drives a route through stubbed degraded states)
    - web/tests/browse-page.test.ts (route-level component testing with stub clients and context)
    - .planning/phases/04-query-workbench-index-health/04-VALIDATION.md (§Manual-Only Verifications — the "Explicitly NOT manual" list this task exists to discharge)
    - .planning/phases/03-browse-inspect-navigation/03-04-SUMMARY.md (the getComputedStyle technique that distinguishes a loaded theme from an unstyled fallback)
  </read_first>
  <behavior>
    `web/tests/health-page.test.ts`:
    - **HLT-02, document order.** With a stub `GetHealth` response and a stub
      status gate at verdict `stale`, the element carrying
      `data-testid="health-verdict-stale"` precedes every element in the numeric
      region. Assert with `compareDocumentPosition` (or index-in-parent over a
      common ancestor), not by reading text order out of `textContent`.
    - **HLT-03, presence and order.** A response with a populated
      `worktreeMismatch` renders `data-testid="health-worktree-mismatch"`, it
      carries `role="alert"`, its text contains BOTH root paths, and it precedes
      the verdict element.
    - **HLT-03, absence.** A response with a nil `worktreeMismatch` renders NO
      element with that testid. Both directions are mandatory: a presence-only
      test passes while the warning is permanently on, which is worse than no
      warning at all.
    - **HLT-03, loudness.** `getComputedStyle` on the warning element resolves a
      non-empty background or border colour distinguishable from the page default
      — the technique 03-04 used after markup that was structurally correct turned
      out to be invisible without a loaded theme. If the jsdom environment cannot
      resolve the class-derived value, assert instead that the element's class list
      differs from `TrustVerdict`'s and record why in the test's own comment; do
      not silently drop the assertion.
    - **HLT-01, completeness.** A single render shows: at least one per-language
      row, at least one node-kind row, at least one edge-kind row, the schema
      version, and the commit SHA — all without any navigation. Assert each by its
      rendered value, not by counting elements.
    - **One call.** Exactly one `getHealth` invocation is recorded for one view
      open, and zero additional `getStatus` invocations are made by this route.
    - **Failure path.** A rejected `GetHealth` renders a named failure state AND
      still renders the verdict — the numbers are gone, the trust signal is not.
    - **Snapshot disagreement, both directions.** A stub gate at commit `aaa…`
      paired with a `GetHealth` response carrying `commitSha: 'bbb…'` renders
      `data-testid="health-snapshot-differs"` AND still renders both the verdict
      and the numbers. The same test asserts that a matching pair renders NO such
      element. A present-only assertion would pass while the notice was permanently
      on, which is the same defect the worktree-warning absence case exists to
      catch one block up.
  </behavior>
  <action>
    Write `web/tests/health-page.test.ts` from the `<behavior>` block. Observe RED
    for any assertion Task 2 did not already satisfy, fix the components (not the
    assertions), and record both counts.

    The absence assertion and the four-way ordering assertion are the load-bearing
    ones. `04-VALIDATION.md` explicitly removes all three of these from the
    manual-verification list; only two entries remain manual for this phase and
    neither is here.

    Finish by running `task web:test` and recording the printed `PASS — N of N`.
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run --reporter=json --outputFile=/tmp/hp.json tests/health-page.test.ts; RC=$?; node -e 'const r=JSON.parse(require("fs").readFileSync("/tmp/hp.json","utf8"));console.log("health-page: total="+r.numTotalTests+" passed="+r.numPassedTests);if(r.numTotalTests<9||r.numPassedTests!==r.numTotalTests)process.exit(1)' && test "$RC" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The verify command reports `total >= 9` with `passed === total` and exit status 0 (floor and status `&&`-chained as ONE final command).
    - Both snapshot-disagreement directions are asserted: `rg -c 'health-snapshot-differs' web/tests/health-page.test.ts` reports at least 2, and the file contains an assertion that the element is ABSENT for a matching pair.
    - `rg -c 'compareDocumentPosition|indexOf' web/tests/health-page.test.ts` reports at least 1 — the ordering claim is a DOM-position assertion.
    - Both worktree directions are present: `rg -c 'health-worktree-mismatch' web/tests/health-page.test.ts` reports at least 2, and the file contains an assertion that the element is ABSENT for a nil mismatch.
    - `rg -c 'getComputedStyle' web/tests/health-page.test.ts` reports at least 1, or the test carries an explicit comment recording why the fallback class-list assertion was used instead.
    - The one-call assertion is present: exactly one recorded `getHealth` invocation and zero route-issued `getStatus` invocations.
    - `task web:test` reports `PASS — N of N` with N greater than the count recorded at the end of 04-04.
  </acceptance_criteria>
  <done>HLT-01's completeness, HLT-02's ordering and HLT-03's loudness and absence are all asserted by executing code, and the health view has exactly one status gate and one GetHealth call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GetHealth` response → browser DOM | Server-provided strings, including host-absolute worktree/index roots, are rendered |
| shared status gate → this route | A cross-route reactive value drives a security-adjacent trust signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Repudiation | the health page and the application-wide banner disagreeing about staleness | high | mitigate | D-04: exactly one `classifyStatus`, consumed by both. Task 1 asserts `health-view.ts` computes no verdict and that exactly one `classifyStatus` and one `StatusVerdict` type exist in `web/src/lib`; Task 2 asserts this route creates no second gate. A user who cannot tell which of two contradictory verdicts to believe has no trust signal at all, which is exactly what HLT-02 exists to prevent |
| T-04-18 | Information Disclosure | `worktreeRoot` and `indexRoot` rendered into the page as host-absolute paths | medium | accept | Inherited from T-04-09 (04-03), acknowledged at that plan's checkpoint: the warning is useless without naming both trees, it only renders when a mismatch genuinely exists, and the listener is loopback-bound with exact-match Origin/Host validation. `projectPath`/`indexPath` are neither on the wire nor rendered, and Task 2 asserts their absence |
| T-04-19 | Tampering | a language name or node-kind string from the store rendered as markup | low | accept | Svelte escapes interpolated text by construction; no raw-HTML directive or DOM raw-HTML sink is introduced, and the vendored `ui/` tree carries a positive-controlled zero count for both (04-01, 04-04) |
| T-04-20 | Denial of Service | repeated `GetHealth` calls each spawning git subprocesses | medium | mitigate | Exactly one call per view open, asserted in Task 3; no timer API on this path, asserted by a positive-controlled zero-count grep. The server side is additionally bounded by `Engine.WorktreeMismatch`'s once-per-Engine latch (04-03) |
| T-04-21 | Spoofing | a malformed commit SHA rendered as if it were the indexed commit | medium | mitigate | Validated server-side at the read site (04-03, `schema.IsCommitSHA`), so an unvalidated value arrives as `""`; `describeFreshness` renders an explicit unknown marker for the empty case rather than a blank that reads as "no problem" |
</threat_model>

<verification>
- `task web:test` reports `PASS — N of N` with a printed, non-zero executed count.
- `cd web && pnpm check` exits 0.
- Exactly one `classifyStatus` definition and exactly one `StatusVerdict` type exist under `web/src/lib`.
- `task web:drift` remains EXPECTED RED until 04-07 Task 3 rebuilds the committed bundle.
</verification>

<success_criteria>
- One page answers HLT-01's four questions with no navigation.
- The verdict provably precedes the numbers.
- The worktree warning is present, alert-roled and ordered first when a mismatch exists, and provably absent when it does not.
- No second verdict function and no second status gate exist.
</success_criteria>

## Artifacts this phase produces

Symbols and paths created by THIS plan (new — not drift):

**New TypeScript module and exports**
- `web/src/lib/health-view.ts` — `CountRow`, `toCountRows`, `hasWorktreeMismatch`, `FreshnessView` (carrying `snapshotAgreement: 'agree' | 'differs' | 'unknown'`), `describeFreshness`, `HealthClient`

**New Svelte components**
- `web/src/lib/components/health/TrustVerdict.svelte`
- `web/src/lib/components/health/WorktreeMismatchWarning.svelte`
- `web/src/lib/components/health/CountTable.svelte`

**Filled route**
- `web/src/routes/health/+page.svelte`

**New test files**
- `web/tests/health-view.test.ts`, `web/tests/health-page.test.ts`

## Cycle-1 cross-AI review dispositions (this plan)

| Finding | Severity | Disposition |
|---|---|---|
| `CountTable` cannot reuse a `Location`-typed `DataTable` | HIGH | **Incorporated.** 04-01 makes the shell generic with a `getRowId` prop; this plan instantiates it at `CountRow` with `getRowId={(row) => row.key}`, forbids widening `CountRow` to fake a `Location`, and requires reporting a non-generic shell as a 04-01 defect rather than routing around it. `pnpm check` is the discriminating gate. |
| Two freshness snapshots with no disagreement rule | MEDIUM | **Incorporated.** `describeFreshness` returns `snapshotAgreement`; the display rule is stated explicitly (the gate's verdict always wins; the numbers carry a `health-snapshot-differs` notice and are never suppressed, and the verdict is never recomputed from them); asserted in both directions in Task 3. It is a comparison, not a second verdict, so D-04 is untouched. |
| Verify gates discard their count floor | HIGH | **Incorporated.** All three gates rewritten to the `&&`-chained shape; a prohibition forbids the broken form. |
| Reusing `describeWorkbenchFailure` on `/health` is a naming leak | LOW | **Deferred, deliberately, with rationale.** The substantive requirement — one Connect-error classification for the whole app (Phase 3 D-04) — is already satisfied: `describeWorkbenchFailure` composes `classifyRpcError` and adds no second vocabulary. The finding is purely about the identifier reading as Workbench-owned. Renaming it to a neutral `describeRpcFailure` would touch four plans across three waves (04-01 defines it and its tests; 04-04, 04-05 and 04-06 consume it, and its `data-testid="workbench-failure-{kind}"` hooks are asserted in five test files), for zero behavioral change, during a revision pass whose other edits are load-bearing. The cost/benefit inverts once a third consumer outside the Workbench appears. **Recorded as the trigger:** when a route other than `/workbench` and `/health` needs this taxonomy, rename it to `describeRpcFailure` in `web/src/lib/rpc-failure.ts` in one mechanical commit and update the testids with it. |

<output>
Create `.planning/phases/04-query-workbench-index-health/04-05-SUMMARY.md` when done.
</output>
