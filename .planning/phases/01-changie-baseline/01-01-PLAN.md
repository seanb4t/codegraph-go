---
phase: 01-changie-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/upgrade/changie_shape_test.go
  - internal/upgrade/taskfile_shape_test.go
  - .changie.yaml
  - .changes/header.tpl.md
  - .changes/unreleased/.gitkeep
  - .changes/v0.14.0.md
  - .changes/v0.13.0.md
  - .changes/v0.12.0.md
  - .changes/v0.11.0.md
  - .changes/v0.10.0.md
  - .changes/v0.9.0.md
  - .changes/v0.8.0.md
  - .changes/v0.7.0.md
  - .changes/v0.6.0.md
  - .changes/v0.5.1.md
  - .changes/v0.5.0.md
  - .changes/v0.4.0.md
  - .changes/v0.3.0.md
  - .changes/v0.2.0.md
  - go.tool-changie.mod
  - go.tool-changie.sum
  - Taskfile.yml
  - CONTRIBUTING.md
autonomous: true
requirements: [CHG-01, CHG-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`task changie -- latest` prints exactly `v0.14.0`: changie derives it from the seeded `.changes/v*.md` filenames, and the `v` prefix comes from the filename (D-03)"
    - "`task changie -- merge --dry-run` output is byte-identical to `CHANGELOG.md`, and `CHANGELOG.md` is byte-identical to `main`. No historical entry is rewritten (CHG-02, D-01, D-02, D-09)"
    - "`.changie.yaml` declares exactly the kinds Breaking, Features, Fixes, Performance, Dependencies in that order with auto minor/minor/patch/patch/patch. Breaking carries the flip-to-major-at-1.0 note. The PR custom is type int with minInt 1 and has no optional key. The design note's versionFormat, kindFormat and changeFormat are carried verbatim. TestChangieConfigShape enforces all of it (CHG-01, D-10, D-11)"
    - "changie v1.26.0 is pinned in the isolated `go.tool-changie.mod` (module github.com/seanb4t/codegraph-go/tools-changie) and registered in both isolatedModfilePaths and forbiddenToolPackages. Contributors and CI run it through one recorded path: `task changie`, backed by the GO_TOOL_CHANGIE variable (CHG-01, D-04, D-05)"
    - "The TestChangie* guards were committed RED before any implementation: on `git log --reverse main..HEAD` the test(01-01) commit comes before the feat(01-01) commit, and 01-01-SUMMARY.md carries the pasted `--- FAIL:` transcript (D-06, rule x1cjy9vyhq)"
    - "Adjacent version blocks are separated exactly as in today's CHANGELOG.md. Each seed carries its own trailing blank-line separator, and newlines.afterChangelogHeader: 1 is the only newlines key, so any boundary drift fails the byte comparison (edge CHG-02/adjacency)"
    - "Empty inputs fail loudly or read as empty. They never pass vacuously. Fewer than 14 seeded version files or CHANGELOG.md headings fails TestChangieVersionSeedsMatchChangelog. When `.changes/unreleased/` holds only `.gitkeep`, `task changie -- next auto` exits non-zero with `no unreleased changes found for automatic bumping` (edge CHG-02/empty)"
    - "Order is specified and stable. Merged versions appear in descending semver order (0.5.1 above 0.5.0), exactly as in CHANGELOG.md. Seed versions are unique by filename. The kinds list order, which fixes the kind-section order of every future batch, is asserted by position (edge CHG-02/ordering)"
    - "A contributor finds the changie path in CONTRIBUTING.md's tool bullet (`task changie`), and a multi-word fragment body passes through the wrapper intact"
  prohibitions:
    - statement: "MUST NOT alter, re-render, reflow, reorder or drop any historical CHANGELOG.md entry. The published release history stays byte-identical to main, and the seeds are verbatim byte slices, never re-authored"
      requirement_id: CHG-02
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT make the PR custom field optional or give it a default or placeholder value to accommodate fragments written before a PR exists (D-11). How Phase 2 obtains a real PR number is Phase 2's decision"
      requirement_id: CHG-01
      category: values
      status: resolved
      verification: test
  artifacts:
    - path: ".changie.yaml"
      provides: "changie configuration: design-note vocabulary, sub-second fragmentFileFormat, single newlines override"
      contains: "afterChangelogHeader: 1"
    - path: ".changes/header.tpl.md"
      provides: "merged-changelog header, exactly the 12 bytes of '# Changelog' plus a newline"
      contains: "# Changelog"
    - path: ".changes/unreleased/.gitkeep"
      provides: "tracks the empty unreleased fragment directory"
    - path: ".changes/v0.14.0.md"
      provides: "baseline seed that makes changie latest answer v0.14.0"
      contains: "## [0.14.0](https://github.com/seanb4t/codegraph-go/compare/v0.13.0...v0.14.0) (2026-09-20)"
    - path: "go.tool-changie.mod"
      provides: "isolated pin of changie v1.26.0"
      contains: "tool github.com/miniscruff/changie"
    - path: "go.tool-changie.sum"
      provides: "checksums for the changie tool graph"
    - path: "internal/upgrade/changie_shape_test.go"
      provides: "D-06 static guards: config shape, baseline layout, seed/heading set equality, pin and wrapper"
      contains: "func TestChangieConfigShape"
    - path: "internal/upgrade/taskfile_shape_test.go"
      provides: "fifth isolated-modfile registration"
      contains: "changieModfilePath"
    - path: "Taskfile.yml"
      provides: "GO_TOOL_CHANGIE var and the changie wrapper target"
      contains: "GO_TOOL_CHANGIE: GOWORK=off go tool -modfile=go.tool-changie.mod"
    - path: "CONTRIBUTING.md"
      provides: "contributor-facing install path"
      contains: "`task changie`"
  key_links:
    - from: "Taskfile.yml (changie task)"
      to: "go.tool-changie.mod"
      via: "GO_TOOL_CHANGIE var"
      pattern: "GO_TOOL_CHANGIE: GOWORK=off go tool -modfile=go.tool-changie.mod"
    - from: ".changie.yaml"
      to: ".changes/header.tpl.md"
      via: "headerPath key"
      pattern: "headerPath: header.tpl.md"
    - from: ".changes/v*.md"
      to: "CHANGELOG.md"
      via: "changie merge (header + afterChangelogHeader newline + seeds newest-first) byte identity"
      pattern: "afterChangelogHeader: 1"
    - from: "internal/upgrade/taskfile_shape_test.go (isolatedModfilePaths, forbiddenToolPackages)"
      to: "go.tool-changie.mod"
      via: "changieModfilePath const declared in changie_shape_test.go"
      pattern: "changieModfilePath"
---

<objective>
Seed the repository with a changie configuration and a `v0.14.0` baseline. Pinned changie must answer `v0.14.0` as the latest version and reproduce today's `CHANGELOG.md` byte for byte. Contributors and CI run changie through one recorded path. The work leads with a production-quality tracer, config → seeds → pinned binary → wrapper, wired end to end. It is proven by `task changie -- latest` and by `task changie -- merge --dry-run | cmp - CHANGELOG.md`, and the D-06 static guards land RED first.

Purpose: every later phase writes fragments against this fixed vocabulary (kinds, `auto` bumps, required `PR`). The byte identity proves that adopting changie rewrote no history (ROADMAP Phase 1 goal; CHG-01, CHG-02).

Output: `.changie.yaml`, `.changes/header.tpl.md`, `.changes/unreleased/.gitkeep`, 14 verbatim seeds `.changes/v0.2.0.md` … `.changes/v0.14.0.md`, `go.tool-changie.mod` and `go.tool-changie.sum`, `internal/upgrade/changie_shape_test.go`, the registration edit in `taskfile_shape_test.go`, `GO_TOOL_CHANGIE` and the `changie` target in `Taskfile.yml`, and the CONTRIBUTING tool bullet.

Decisions implemented here: D-01, D-02, D-03, D-04, D-05, D-06, D-09, D-10, D-11. D-07 and D-08 are in 01-02-PLAN.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-changie-baseline/01-CONTEXT.md
@.planning/phases/01-changie-baseline/01-RESEARCH.md
@.planning/phases/01-changie-baseline/01-PATTERNS.md
@.planning/notes/changie-release-management.md
@CHANGELOG.md
@go.tool-lint.mod
@internal/upgrade/taskfile_shape_test.go

<interfaces>
Existing helpers and fixtures in package `upgrade` (internal/upgrade/taskfile_shape_test.go). Reuse them; do not re-implement them:

```go
const taskfilePath = "../../Taskfile.yml"      // const block, lines 29-66
const rootGoModPath = "../../go.mod"
var forbiddenToolPackages = []string{ /* lines 128-134 */ }
var isolatedModfilePaths = []string{toolModfilePath, lintModfilePath, protoModfilePath, golangciModfilePath} // line 1057
func parseGoModToolPackages(src string) ([]string, error)          // line 303: error when no tool directive
func parseGoModRequireVersion(src, pkg string) (string, error)      // line 338: prefix match on module path
func mustToolModfileHeaderComment(t *testing.T, src string) string  // line 437: leading // block above `module`
func mustParseTaskBlocks(t *testing.T, src string) map[string]string // line 499: keyed by task name
func mustTaskDescription(t *testing.T, block string) string         // line 562: folds `desc: >-`
func blockDeclaresKey(block, key string) bool                       // line 575
func blockReferencesToken(block, token string) bool                 // line 582: whole-word match
```

The test file imports `go.yaml.in/yaml/v3` as `yaml` (line 15). `golang.org/x/mod` v0.38.0 is a direct requirement in the root go.mod (line 35), so the test may import `golang.org/x/mod/semver`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end tracer: pinned changie renders the seeded v0.14.0 baseline byte-for-byte (D-06 guards RED first)</name>
  <reversibility rating="costly">D-04: every changie invocation site names go.tool-changie.mod. That covers the Taskfile targets, the ci.yml step, Phase 5's release workflow and Phase 2's capability, so moving the pin later touches all of them.</reversibility>
  <files>internal/upgrade/changie_shape_test.go, internal/upgrade/taskfile_shape_test.go, .changie.yaml, .changes/header.tpl.md, .changes/unreleased/.gitkeep, .changes/v0.14.0.md, .changes/v0.13.0.md, .changes/v0.12.0.md, .changes/v0.11.0.md, .changes/v0.10.0.md, .changes/v0.9.0.md, .changes/v0.8.0.md, .changes/v0.7.0.md, .changes/v0.6.0.md, .changes/v0.5.1.md, .changes/v0.5.0.md, .changes/v0.4.0.md, .changes/v0.3.0.md, .changes/v0.2.0.md, go.tool-changie.mod, go.tool-changie.sum, Taskfile.yml</files>
  <read_first>
    - .planning/notes/changie-release-management.md lines 71-97: the `.changie.yaml` sketch. It is the literal source (D-10); copy it, never retype it.
    - CHANGELOG.md, the whole file (272 lines): the byte oracle. It has 14 `## [x.y.z]` headings and ends with a single newline.
    - .planning/phases/01-changie-baseline/01-PATTERNS.md lines 27-241: `.changie.yaml`, header, seeds, modfile, the three registration sites, and the Go shape-test idiom.
    - .planning/phases/01-changie-baseline/01-RESEARCH.md lines 176-258 and 260-358: Pattern 1 and Pattern 2, Pitfalls 1-4, and the live transcripts.
    - go.tool-lint.mod lines 1-22: the header voice and module/go/tool layout to replicate.
    - go.tool-golangci.mod lines 1-41: how a newer modfile narrates its relationship to all of its siblings.
    - internal/upgrade/taskfile_shape_test.go lines 1-160: imports, const block, forbiddenToolPackages and crossToolchainTokens.
    - internal/upgrade/taskfile_shape_test.go lines 300-600: parsers, including parseTaskBlocks and parseTaskDescription.
    - internal/upgrade/taskfile_shape_test.go lines 1040-1220: isolatedModfilePaths, TestToolModfilesRemainIsolated, TestToolModfilesPopulationMatchesDisk and TestTaskfileShapeHelpersFailLoudly.
    - Taskfile.yml lines 1-12 (header doctrine and GO_TOOL* vars), 481-536 (docs:cli and docs:cli:drift; the new target goes right after it) and 3931-3944 (check:goreleaser's one-line cmds shape).
  </read_first>
  <behavior>
    - TestChangieConfigShape: the root mapping's key set equals exactly {changesDir, unreleasedDir, headerPath, changelogPath, versionExt, versionFormat, kindFormat, changeFormat, fragmentFileFormat, kinds, custom, newlines}. Scalar values: changesDir=.changes, unreleasedDir=unreleased, headerPath=header.tpl.md, changelogPath=CHANGELOG.md, versionExt=md. versionFormat, kindFormat and changeFormat equal the design note's strings byte for byte. fragmentFileFormat equals the planned value. kinds is, by position, (Breaking,minor), (Features,minor), (Fixes,patch), (Performance,patch), (Dependencies,patch), and each kind's key set is exactly {label, auto}. The LineComment on the Breaking kind's auto value node contains `Flip to major at 1.0` (yaml.v3 attaches the inline comment to the value node `minor`, probed at planning time). custom has exactly one entry: its key set is exactly {key, type, minInt}, so an added optional key fails (D-11), and key=PR, type=int, minInt=1. newlines has key set exactly {afterChangelogHeader}, value 1.
    - TestChangieBaselineLayout: `.changes/header.tpl.md` bytes equal "# Changelog\n" exactly (D-09). `.changes/unreleased/.gitkeep` is a regular file. `.changes/v0.14.0.md` exists and its first line begins `## [0.14.0](` (CHG-02 baseline, pinned permanently).
    - TestChangieVersionSeedsMatchChangelog: logs both counts, requires each to be at least 14, and fails naming every version present in one set but absent from the other, in both directions: seed stems from `.changes/v*.md` versus `## [v?x.y.z]` headings in CHANGELOG.md.
    - TestChangieToolPinnedInIsolatedModfile: the go.tool-changie.mod tool packages equal exactly [github.com/miniscruff/changie]. Its required changie version is valid semver and at least v1.26.0 (semver.Compare). changieModfilePath is an element of isolatedModfilePaths, and github.com/miniscruff/changie is an element of forbiddenToolPackages. The modfile header contains `isolat`, `GOWORK=off` and `task changie`.
    - TestChangieWrapperTaskRecordsInstallPath: Taskfile.yml contains the exact line `  GO_TOOL_CHANGIE: GOWORK=off go tool -modfile=go.tool-changie.mod`. A `changie` task exists, its desc mentions go.tool-changie.mod, the block declares `silent`, and the block contains `{{.GO_TOOL_CHANGIE}} changie {{.CLI_ARGS}}`.
    - TestChangieShapeParsersFailLoudly: parseChangieConfig returns an error for "" and for "kinds: []\n". parseChangelogVersionHeadings returns an error for "", for "# Changelog\n", and for a source repeating the same heading twice. listChangieVersionSeeds(t.TempDir()) returns an error. This test passes even in the RED state, because the helpers live in the test file.
  </behavior>
  <action>
RED: commit the guards before any implementation file exists (D-06, rule x1cjy9vyhq).

1. Create internal/upgrade/changie_shape_test.go (package upgrade) with these constants:
   - changieConfigPath = "../../.changie.yaml"
   - changieChangesDir = "../../.changes"
   - changieChangelogPath = "../../CHANGELOG.md"
   - changieModfilePath = "../../go.tool-changie.mod"
   - changieToolPackage = "github.com/miniscruff/changie"
   - changieMinVersion = "v1.26.0"
   - changieBaselineVersion = "v0.14.0"
   - changieSeedFloor = 14
   - raw-string consts changieVersionFormat, changieKindFormat and changieChangeFormat, copied byte for byte from .planning/notes/changie-release-management.md lines 79-81. versionFormat contains an em dash: copy the bytes, do not retype them.
   - changieFragmentFileFormat, whose value is the Go template `{{.Kind}}-{{.Time.Format "20060102-150405.000000000"}}`.

   changieModfilePath deliberately lives in this file, not in taskfile_shape_test.go's const block as PATTERNS Site 1 suggests. This way the RED commit compiles before the registration exists; a build failure is not `--- FAIL` evidence.

2. Add helpers. Each returns a non-nil error on its nothing-found branch, never a usable zero value (house style, rule 84d1gfpywd):
   - parseChangieConfig(src []byte) (*yaml.Node, error): unmarshal into a yaml.Node and return the root mapping. Return an error for empty input, a non-mapping root, or a missing or empty kinds sequence.
   - mustChangieConfig(t, src).
   - yamlMappingKeys(n *yaml.Node) []string: sorted keys.
   - yamlMappingValue(n *yaml.Node, key string) (*yaml.Node, bool).
   - parseChangelogVersionHeadings(src string) ([]string, error): every line matching `^## \[v?(\d+\.\d+\.\d+)\]`, returned as "v" plus the capture, in file order. Return an error on zero matches or on a duplicated version. The optional v inside the brackets accepts the `## [v0.15.0](…)` shape that changie batches write from Phase 5 on under the locked versionFormat (D-03), so the guard survives the first real release.
   - listChangieVersionSeeds(dir string) ([]string, error): filepath.Glob of dir/v*.md, as sorted stems. Return an error on zero matches.

3. Write the six tests exactly as the behavior block specifies. TestChangieToolPinnedInIsolatedModfile uses the existing parseGoModToolPackages, parseGoModRequireVersion and mustToolModfileHeaderComment, plus golang.org/x/mod/semver. TestChangieWrapperTaskRecordsInstallPath uses mustParseTaskBlocks, mustTaskDescription and blockDeclaresKey on taskfilePath. TestChangieShapeParsersFailLoudly is table-driven, like TestTaskfileShapeHelpersFailLoudly. Every failure message names the offending key, value or version.

4. Record the RED state:
   - Run `GOWORK=off go test ./internal/upgrade/ -run '^TestChangie' -count=1 -v`. Confirm five `--- FAIL: TestChangie…` top-level lines, caused by missing files, not a build failure.
   - Run `task changie -- latest` and capture its "task does not exist" failure. This is the end-to-end RED.
   - Keep both transcripts for 01-01-SUMMARY.md.
   - Commit only the test file: `test(01-01): add failing changie config-shape, seed-set and pin guards (CHG-01, CHG-02)`.
   - Do NOT run or gate on `gsd-tools check tdd-red-evidence`. It parses TAP only and always reports INVALID_RED for go test (rule x1cjy9vyhq). The RED evidence is this commit's position on main..HEAD plus the pasted transcript.

GREEN: the thinnest real path, config → seeds → pinned binary → wrapper.

5. .changie.yaml (D-10):
   - Copy .planning/notes/changie-release-management.md lines 73-97 verbatim. Keep the keys, their order and quoting, and the inline comment on Breaking's auto line, `pre-1.0: breaking bumps minor. Flip to major at 1.0.`.
   - Append fragmentFileFormat with the value from step 1.
   - Append a newlines block whose only child is `afterChangelogHeader: 1` (D-02, RESEARCH Pitfall 1). Add no other newlines key; seeds already carry their own trailing blank line.
   - Put a short `#` comment header at the top of the file. It states:
     - CHANGELOG.md is generated by `changie merge` from .changes/header.tpl.md plus every .changes/v*.md, newest first. Never hand-edit CHANGELOG.md or a .changes/v*.md file. D-09 puts this ownership note here, not in the header template.
     - Record a change with `task changie -- new -k <Kind> -b "<sentence>" -m PR=<n>`.
     - Kinds, auto bumps, PR and the three formats are locked by CHG-01 (D-10), and PR stays required (D-11).
     - afterChangelogHeader is the only newline override, and why.
     - fragmentFileFormat adds sub-second precision because changie v1.26.0's default, which has seconds resolution, silently overwrote a same-kind fragment written in the same second. This was measured at planning time, and it is the collision CONTEXT's Claude's Discretion said to act on.

6. Write .changes/header.tpl.md containing exactly `# Changelog` followed by one newline (D-09): no banner, no comment. Write .changes/unreleased/.gitkeep as an empty file.

7. Create the 14 seeds (D-01) as verbatim byte slices of CHANGELOG.md, using `sed -n '<start>,<end>p' CHANGELOG.md > .changes/v<X.Y.Z>.md`.

   The ranges were measured at planning time from `rg -n '^## \[' CHANGELOG.md`. Each range runs from its heading to the line before the next heading; the last runs to EOF at line 272.

   | Seed | Lines |
   |---|---|
   | v0.14.0 | 3-9 |
   | v0.13.0 | 10-16 |
   | v0.12.0 | 17-23 |
   | v0.11.0 | 24-30 |
   | v0.10.0 | 31-37 |
   | v0.9.0 | 38-44 |
   | v0.8.0 | 45-51 |
   | v0.7.0 | 52-63 |
   | v0.6.0 | 64-74 |
   | v0.5.1 | 75-81 |
   | v0.5.0 | 82-88 |
   | v0.4.0 | 89-99 |
   | v0.3.0 | 100-106 |
   | v0.2.0 | 107-272 |

   Re-run that rg first. If any range differs, CHANGELOG.md has changed on main: stop and surface it.

   Before building anything, prove the slices reassemble. Header.tpl.md, then one newline, then the 14 seeds newest first, concatenated, must cmp equal to CHANGELOG.md. Never re-render, reflow or trim a seed; seed content is not passed through versionFormat or changeFormat (D-01).

8. Create go.tool-changie.mod and go.tool-changie.sum (D-04). Generate them in a `mktemp -d` scratch directory, never the repo root.

   Running `go mod tidy -modfile=<file>` in the repo root scans the main module's packages and pulls their imports into the tool modfile. This was verified at planning time: `go mod tidy -modfile=go.tool-lint.mod -diff` in the repo started resolving internal/… packages. RESEARCH's "confirmed working" run used a scratch copy.

   Steps:
   1. In the scratch dir, write a go.mod with `module github.com/seanb4t/codegraph-go/tools-changie`, `go 1.26.5` and `tool github.com/miniscruff/changie`.
   2. Run `GOWORK=off GOTOOLCHAIN=go1.26.6 go get github.com/miniscruff/changie@v1.26.0`.
   3. Run `GOWORK=off GOTOOLCHAIN=go1.26.6 go mod tidy`.

   The planning-time run produced: the go line stays 1.26.5, no toolchain line, 64 modules, and changie required as `v1.26.0 // indirect`, the same as actionlint in go.tool-lint.mod. v1.26.0 is still the newest tag on proxy.golang.org as of 2026-09-25.

   Copy scratch go.mod to go.tool-changie.mod with a `//` header prepended, and scratch go.sum to go.tool-changie.sum unmodified. Write the header in go.tool-lint.mod's voice. It says:
   - The file is isolated from go.tool.mod, go.tool-lint.mod, go.tool-proto.mod and go.tool-golangci.mod.
   - The standalone graph is N modules, measured live with `GOWORK=off go list -m -modfile=go.tool-changie.mod all | wc -l` (64 at planning time).
   - 01-RESEARCH.md measured co-location with go.tool.mod at +4 modules and zero lost MVS bids. D-04 still keeps changie separate so that no future bump of changie or goreleaser can re-open the other's resolution. The measurement is recorded so that nobody re-derives it.
   - GOWORK=off is REQUIRED on every invocation.
   - Neither Dependabot nor Renovate manages the file, so bumps are manual.
   - Contributors and CI run the identical body via `task changie -- <args>`, CHG-01's recorded install path (D-05).
   - It cites .planning/phases/01-changie-baseline/01-CONTEXT.md D-04/D-05.

   The header must contain `isolat`, which TestToolModfilesRemainIsolated checks, and `GOWORK=off` and `task changie`, which TestChangieToolPinnedInIsolatedModfile checks.

9. Register the modfile in internal/upgrade/taskfile_shape_test.go in this same commit; otherwise TestToolModfilesPopulationMatchesDisk fails the moment the modfile exists. Append changieModfilePath to isolatedModfilePaths, and append "github.com/miniscruff/changie" to forbiddenToolPackages. Do both in the same edit: they are separate assertions (RESEARCH Pitfall 3). Give each a one-line comment naming 01-01-PLAN.md and noting that the path const lives in changie_shape_test.go. Add go.tool-changie.mod to forbiddenToolPackages' doc-comment list of modfiles.

10. Edit Taskfile.yml:
   - Add `GO_TOOL_CHANGIE: GOWORK=off go tool -modfile=go.tool-changie.mod` directly under GO_TOOL_GOLANGCI (line 12).
   - Insert a `changie` task immediately after the docs:cli:drift task block, which ends about line 535, before `web:deps:`.
   - Its `desc: >-` says the target is CHG-01's recorded install path (D-05), identical for contributors and CI. It runs the pinned changie v1.26.0 from the isolated go.tool-changie.mod (see that file's header) with whatever follows `--`. Examples: `task changie -- latest` and `task changie -- new -k Fixes -b "<sentence>" -m PR=<n>`.
   - Set `silent: true`, so stdout carries only changie's own output; the tracer pipes that output into cmp.
   - Give it a single `cmds:` entry, `"{{.GO_TOOL_CHANGIE}} changie {{.CLI_ARGS}}"`. go-task 3.52.0 re-quotes CLI_ARGS, so a multi-word `-b` value survives (verified at planning time).
   - Declare no status or platforms key; TestTaskfileGatesFailLoud forbids both.

11. Run the verify block, then commit `feat(01-01): pin changie and seed the v0.14.0 baseline (CHG-01, CHG-02)`. Agent commits may use `git -c commit.gpgsign=false`. Never put a CI-skip marker in a commit message (rule f18zrdsgx5). Commit no fragment under .changes/unreleased/; only `.gitkeep` is tracked there.
  </action>
  <verify>
    <automated>GOWORK=off go test ./internal/upgrade/ -run '^(TestChangie|TestToolModfiles)' -count=1 -v</automated>
    <fails_when>non-zero exit, or "--- FAIL" in the output, or fewer than 6 lines matching "^--- PASS: TestChangie", or "no tests to run" in the output</fails_when>
    <automated>test "$(task changie -- latest)" = "v0.14.0"</automated>
    <fails_when>non-zero exit: stdout is anything other than exactly v0.14.0, or the changie target does not exist</fails_when>
    <automated>task changie -- merge --dry-run | cmp - CHANGELOG.md</automated>
    <fails_when>non-zero exit, or cmp prints "differ" or "EOF on"</fails_when>
    <automated>git diff --quiet main -- CHANGELOG.md</automated>
    <fails_when>non-zero exit, meaning CHANGELOG.md is no longer byte-identical to main</fails_when>
    <automated>GOWORK=off go test ./internal/upgrade/ -count=1</automated>
    <fails_when>non-zero exit, or "FAIL" in the output (a pre-existing guard such as TestToolModfilesPopulationMatchesDisk went red)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --reverse --format=%s main..HEAD` shows a line starting `test(01-01):` above a line starting `feat(01-01):`. The main..HEAD range excludes earlier milestones' same-numbered test(01-01) commits, which live on main (STATE.md Phase 6 tooling gap).
    - 01-01-SUMMARY.md contains the pasted RED transcript, with at least five `--- FAIL: TestChangie` lines, plus the failing `task changie -- latest` output.
    - `tracked=$(git ls-files -- .changes) && test "$(printf '%s\n' "$tracked" | wc -l | tr -d ' ')" = 16` exits 0: 14 seeds, header.tpl.md and unreleased/.gitkeep. git's status is captured before counting.
    - `seeds=$(git ls-files -- '.changes/v*.md') && test "$(printf '%s\n' "$seeds" | wc -l | tr -d ' ')" = 14` exits 0.
    - `git ls-files -- .changes/unreleased` prints exactly `.changes/unreleased/.gitkeep`, so no fragment is committed.
    - `printf '# Changelog\n' | cmp - .changes/header.tpl.md` exits 0.
    - `{ cat .changes/header.tpl.md; printf '\n'; for v in 0.14.0 0.13.0 0.12.0 0.11.0 0.10.0 0.9.0 0.8.0 0.7.0 0.6.0 0.5.1 0.5.0 0.4.0 0.3.0 0.2.0; do cat ".changes/v$v.md"; done; } | cmp - CHANGELOG.md` exits 0. This reassembly check does not depend on changie.
    - `rg -F -o 'afterChangelogHeader: 1' .changie.yaml | wc -l` prints 1.
    - `rg -F -o 'Flip to major at 1.0.' .changie.yaml | wc -l` prints 1.
    - `rg -F -o '20060102-150405.000000000' .changie.yaml | wc -l` prints 1.
    - `rg -o '^tool github.com/miniscruff/changie$' go.tool-changie.mod | wc -l` prints 1.
    - `rg -o '^go 1\.26\.5$' go.tool-changie.mod | wc -l` prints 1.
    - `GOWORK=off go list -m -modfile=go.tool-changie.mod github.com/miniscruff/changie` prints `github.com/miniscruff/changie v1.26.0`.
    - `GOWORK=off go mod verify -modfile=go.tool-changie.mod` prints `all modules verified`.
    - The module count stated in the go.tool-changie.mod header equals the live output of `GOWORK=off go list -m -modfile=go.tool-changie.mod all | wc -l`.
    - `task changie -- next auto` exits 1, and its stderr contains `Error: no unreleased changes found for automatic bumping`.
    - `gofmt -l internal/upgrade/` prints nothing.
    - `GOWORK=off go vet ./internal/upgrade/` exits 0.
  </acceptance_criteria>
  <done>Pinned changie v1.26.0, run through `task changie`, answers v0.14.0 and reproduces CHANGELOG.md byte for byte from 14 verbatim seeds. The six TestChangie* guards, landed RED first, and every pre-existing internal/upgrade guard pass. The tracer is committed as test(01-01) followed by feat(01-01).</done>
</task>

<task type="auto">
  <name>Task 2: Record the contributor install path in CONTRIBUTING and prove the multi-word passthrough</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md lines 118-140: the tool bullet at lines 124-128, and §Pull requests from line 130, which must stay untouched (DOCS-12, Phase 5).
    - .planning/phases/01-changie-baseline/01-PATTERNS.md lines 351-361: the value-fill edit rule.
    - internal/upgrade/taskfile_shape_test.go lines 2005-2104: TestContributingReferencesRealTaskTargets. It only matches a backtick span that closes right after the target name.
    - Taskfile.yml: the `changie` task added by Task 1.
  </read_first>
  <action>
Per D-05, this is a value fill that completes CHG-01's recorded install path. The other two parts, the modfile header and the wrapper desc, landed in Task 1.

1. Rewrite the bullet at CONTRIBUTING.md lines 124-128 without changing its sentence structure:
   - The tools list becomes `task`, `goreleaser`, `actionlint`, and `changie`.
   - The files list becomes `go.tool.mod`, `go.tool-lint.mod`, and `go.tool-changie.mod`.
   - Add one clause saying changie runs as `task changie`, for example `task changie -- latest`. Write the bare `task changie` span with its own closing backtick. TestContributingReferencesRealTaskTargets checks that span; its regex does not match the longer example.
   - Change "those two files" to "those three files".

   Do not touch §Pull requests, the release-please prose, or the pre-existing omission of go.tool-proto.mod and go.tool-golangci.mod from this bullet. Those are out of scope; the prose rewrite is DOCS-12.

2. Prove the contributor path in the main working tree without writing a file. Run `CI=true task changie -- new --dry-run -k Fixes -b "contributor path probe with spaces" -m PR=1`. changie's `--dry-run` prints the fragment to stdout and writes nothing (verified at planning time). Paste the output into 01-01-SUMMARY.md.

3. Commit `docs(01-01): record the changie install path in CONTRIBUTING (CHG-01)`.
  </action>
  <verify>
    <automated>GOWORK=off go test ./internal/upgrade/ -run '^TestContributingReferencesRealTaskTargets$' -count=1 -v</automated>
    <fails_when>non-zero exit, or "--- FAIL" in the output, or "no tests to run" in the output</fails_when>
    <automated>CI=true task changie -- new --dry-run -k Fixes -b "contributor path probe with spaces" -m PR=1 | rg -x -F 'body: contributor path probe with spaces'</automated>
    <fails_when>non-zero exit: the exact body line is absent because CLI_ARGS quoting split the sentence, or the changie target failed</fails_when>
    <automated>test -z "$(git status --porcelain -- .changes)"</automated>
    <fails_when>non-zero exit, meaning the dry run wrote a fragment into the real tree</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -F -o '`task changie`' CONTRIBUTING.md | wc -l` prints at least 1.
    - `rg -F -o 'go.tool-changie.mod' CONTRIBUTING.md | wc -l` prints at least 1.
    - `rg -F -o 'those three files' CONTRIBUTING.md | wc -l` prints 1.
    - `d=$(git diff -U0 main -- CONTRIBUTING.md) && printf '%s\n' "$d" | rg '^@@'` shows hunks only inside the tool bullet, with no hunk at or after the `## Pull requests` heading. git's status is captured before filtering.
    - 01-01-SUMMARY.md contains the dry-run fragment output, including `body: contributor path probe with spaces` and `kind: Fixes`.
  </acceptance_criteria>
  <done>CONTRIBUTING.md names changie, go.tool-changie.mod and `task changie`. TestContributingReferencesRealTaskTargets covers the new reference, and a multi-word body passes through the wrapper intact without touching the real tree.</done>
</task>

</tasks>

## Artifacts this phase produces

This list is identical in both phase plans. The "Plan" column says which plan produces each item.

| Artifact | Kind | Plan |
|---|---|---|
| `.changie.yaml` (keys: changesDir, unreleasedDir, headerPath, changelogPath, versionExt, versionFormat, kindFormat, changeFormat, fragmentFileFormat, kinds, custom, newlines) | new file | 01-01 |
| `.changes/header.tpl.md` | new file | 01-01 |
| `.changes/unreleased/.gitkeep` | new file | 01-01 |
| `.changes/v0.2.0.md`, `v0.3.0.md`, `v0.4.0.md`, `v0.5.0.md`, `v0.5.1.md`, `v0.6.0.md`, `v0.7.0.md`, `v0.8.0.md`, `v0.9.0.md`, `v0.10.0.md`, `v0.11.0.md`, `v0.12.0.md`, `v0.13.0.md`, `v0.14.0.md` | 14 new seed files | 01-01 |
| `go.tool-changie.mod`, module path `github.com/seanb4t/codegraph-go/tools-changie`, `tool github.com/miniscruff/changie`, v1.26.0 | new file | 01-01 |
| `go.tool-changie.sum` | new file | 01-01 |
| `internal/upgrade/changie_shape_test.go` | new file | 01-01 (extended in 01-02) |
| Consts `changieConfigPath`, `changieChangesDir`, `changieChangelogPath`, `changieModfilePath`, `changieToolPackage`, `changieMinVersion`, `changieBaselineVersion`, `changieSeedFloor`, `changieVersionFormat`, `changieKindFormat`, `changieChangeFormat`, `changieFragmentFileFormat` | Go test symbols | 01-01 |
| Helpers `parseChangieConfig`, `mustChangieConfig`, `yamlMappingKeys`, `yamlMappingValue`, `parseChangelogVersionHeadings`, `listChangieVersionSeeds` | Go test symbols | 01-01 |
| `TestChangieConfigShape`, `TestChangieBaselineLayout`, `TestChangieVersionSeedsMatchChangelog`, `TestChangieToolPinnedInIsolatedModfile`, `TestChangieWrapperTaskRecordsInstallPath`, `TestChangieShapeParsersFailLoudly` | Go tests | 01-01 |
| `TestChangieCheckWiredIntoCI`, `TestChangieBinaryInToolVulnScan` | Go tests | 01-02 |
| `isolatedModfilePaths` += `changieModfilePath`; `forbiddenToolPackages` += `github.com/miniscruff/changie` (in `taskfile_shape_test.go`) | edited Go fixtures | 01-01 |
| `GO_TOOL_CHANGIE` | Taskfile var | 01-01 |
| `changie` | Taskfile target (wrapper) | 01-01 |
| `check:changie` | Taskfile target (live-tool guard, 11 legs) | 01-02 |
| `vuln` target: `changie` binary added to the build and scan list | edited Taskfile target | 01-02 |
| ci.yml `test` job step `Changie baseline and fragment guard (CHG-03/CHG-04)` running `task check:changie` | edited workflow | 01-02 |
| `CONTRIBUTING.md` tool bullet | edited doc | 01-01 |
| `.planning/phases/01-changie-baseline/01-MUTATION-LOG.md` | new planning artifact | 01-02 |

## Probe coverage ledger (spec-less fallback)

The ledger covers 8 edge-probe items: 7 are authored as explicit truths across the two plans, and 1 is flagged.

| Probe | Disposition | Where |
|---|---|---|
| CHG-02/adjacency | explicit truth | this plan |
| CHG-02/empty | explicit truth | this plan |
| CHG-02/ordering | explicit truth | this plan |
| CHG-01/concurrency | explicit truth | 01-02 |
| CHG-03/adjacency | explicit truth | 01-02 |
| CHG-03/empty | explicit truth | 01-02 |
| CHG-03/ordering | explicit truth | 01-02 |
| CHG-04/unclassified | unresolved, flagged assumption A-01 | 01-02 |

Prohibition recall kept 3 items across the phase: 2 here (CHG-02 transparency, CHG-01 values) and 1 in 01-02 (CHG-04). Canon items were dropped with a breadcrumb rather than minted:
- Supply-chain integrity of the changie module fetch is canon. It is covered by T-01-SC and /gsd-secure-phase.
- Path handling in scratch copies is canon (path traversal). It is covered by /gsd-secure-phase.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy.golang.org → local/CI build | Third-party changie v1.26.0 and 63 transitive modules are compiled into a build tool |
| CHANGELOG.md → .changes/v*.md | The published release history is moved into seed files that changie re-renders from |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | go.tool-changie.mod / go.tool-changie.sum (changie module fetch) | high | mitigate | Exact pin v1.26.0. go.tool-changie.sum checksums are sumdb-verified at generation. `go tool`/`go build` default to -mod=readonly and refuse a missing or mismatched sum. `go mod verify -modfile=go.tool-changie.mod` must print `all modules verified` (acceptance). The Package Legitimacy Audit in 01-RESEARCH.md is OK: 5.8-year-old MIT repo, active, v1.26.0 resolves on the proxy. No marketplace action, release download, install script or brew path (D-04). |
| T-01-01 | Elevation of Privilege | root go.mod | medium | mitigate | `github.com/miniscruff/changie` is added to forbiddenToolPackages, so TestToolModfilesRemainIsolated fails if the root module ever requires the build tool or declares it as a tool. The build tool cannot leak into the shipped binary. |
| T-01-02 | Tampering | .changes/v*.md seeds versus CHANGELOG.md | medium | mitigate | Seeds are byte slices only. Defenses: the reassembly cmp, `task changie -- merge --dry-run \| cmp - CHANGELOG.md`, `git diff --quiet main -- CHANGELOG.md`, and TestChangieVersionSeedsMatchChangelog set equality in both directions. |
| T-01-03 | Tampering | .changie.yaml vocabulary (PR requirement, kinds, newlines) | medium | mitigate | TestChangieConfigShape asserts exact key sets: the PR custom's keys must be exactly {key, type, minInt} and newlines exactly {afterChangelogHeader}. Any relaxation, such as making PR optional, is a failing test. |
| T-01-04 | Tampering | fragment body text rendered into CHANGELOG.md | low | accept | Fragments land only through reviewed PRs. changie inserts `{{.Body}}` as data and does not re-parse it as a template (01-RESEARCH.md Security Domain). The output is markdown, never executed. |
| T-01-05 | Spoofing | `task changie` argument passthrough | low | accept | A developer and CI tool with no secrets in scope. go-task re-quotes CLI_ARGS, so a body cannot inject extra arguments by splitting on spaces. |
</threat_model>

<verification>
- `GOWORK=off go test ./internal/upgrade/ -count=1` passes: the new TestChangie* guards and every pre-existing guard.
- `task changie -- latest` prints v0.14.0. `task changie -- merge --dry-run | cmp - CHANGELOG.md` exits 0. `git diff --quiet main -- CHANGELOG.md` exits 0.
- `task lint:go` passes: gofmt, errcheck, staticcheck and unused over the new test file.
- Hand-off notes. Record them in 01-01-SUMMARY.md; they are not work for this plan:
  - `.github/workflows/release-please.yml` is still live and still writes CHANGELOG.md until Phase 5 retires it. No release may be cut between Phase 1 and Phase 5 without reconciling the two writers. If one were, the new guards would go red loudly (a new `## [0.14.1]` heading with no seed), which is the intended signal.
  - Future batch spacing, measured at planning time: `changie batch auto --dry-run` renders no blank line after the version heading or the kind heading, and a merged new version file would abut the `## [0.14.0]` seed with no blank line between them. Batch-time newlines keys are Phase 5's decision (REL-10/REL-13). TestChangieConfigShape's exact newlines key set makes adding them a deliberate, test-visible change.
  - `.changie.yaml`, `.changes/**` and `go.tool-changie.{mod,sum}` are not in the require-issue-link or pr_template_policy exemption lists. That is deferred to Phase 4/5 per CONTEXT.
</verification>

<success_criteria>
- ROADMAP Phase 1 SC2 (CHG-01): `.changie.yaml` declares the five kinds with their auto bumps and the flip note, the required int PR with minInt 1, and the design-note formats. changie is pinned at v1.26.0 with one recorded path for CI and contributors: the go.tool-changie.mod header, the `changie` target desc, and the CONTRIBUTING bullet.
- ROADMAP Phase 1 SC3 (CHG-02): header.tpl.md, unreleased/.gitkeep and v0.14.0.md exist, and CHANGELOG.md is byte-identical to main.
- The tracer's end-to-end path is proven: `task changie -- latest` prints v0.14.0 and the merge dry run matches CHANGELOG.md byte for byte. The committed, CI-wired form of SC1 and SC4 is 01-02-PLAN.md.
</success_criteria>

<output>
Create `.planning/phases/01-changie-baseline/01-01-SUMMARY.md` when done. It must include:
- the RED transcript (`--- FAIL: TestChangie…` lines plus the failing `task changie -- latest`);
- the GREEN transcript;
- the live module count;
- the dry-run fragment output from Task 2;
- the hand-off notes from the verification section.
</output>
