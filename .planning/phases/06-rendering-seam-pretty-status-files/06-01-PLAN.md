---
phase: 06-rendering-seam-pretty-status-files
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/cli/present/archtest/import_graph_test.go
  - internal/cli/present/styles.go
  - internal/cli/present/tty.go
  - internal/cli/present/tty_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [TUI-01]
must_haves:
  truths:
    - "go test ./internal/cli/present/archtest/... is RED (self-defeat guard fires) after Task 1 lands and BEFORE any charm dependency exists — the D-12 fail-closed proof"
    - "go test ./internal/cli/present/archtest/... is GREEN once internal/cli/present imports charm.land/lipgloss/v2 AND none of the six serve-reachable guarded packages transitively import any /v2 charm path (TUI-01, ROADMAP SC1)"
    - "ChoosePresentation(true, \"\") == true and ChoosePresentation(false, \"\") == ChoosePresentation(true, \"1\") == false — the D-04/D-05 pretty-branch decision is a pure function unit-tested without a TTY"
    - "go.mod requires charm.land/lipgloss/v2 as a direct dependency and golang.org/x/term as a direct (no longer // indirect) require; no charm bubbletea or bubbles require line exists (D-13)"
  artifacts:
    - internal/cli/present/archtest/import_graph_test.go
    - internal/cli/present/tty.go
    - internal/cli/present/styles.go
  key_links:
    - "the archtest's guardedPackages set matches internal/graphstore/archtest/stdout_confinement_test.go verbatim (six serve-reachable packages) so TUI-01 defends the whole agent path, not only query/mcp"
    - "the self-defeat guard binds the archtest to a real charm importer (internal/cli/present) so a future refactor that drops styling cannot silently make the test vacuously green"
  prohibitions:
    - "no bare charm.land/lipgloss (without /v2) reference in go.mod, any import, or the archtest forbidden-path list — the bare path resolves to the WRONG v1 module (RESEARCH Finding 1)"
    - "no charm.land/bubbles or charm.land/bubbletea dependency added in this phase (D-13)"
    - "internal/cli/present must NOT read os.Getenv/term.IsTerminal itself — the TTY/NO_COLOR decision lives only at the RunE call sites (D-03)"
---

<objective>
Land the TUI-01 ANSI-isolation archtest FIRST (fail-closed), THEN introduce the
sole Charm home (`internal/cli/present`) with the pure TTY/NO_COLOR branch
selector — establishing the build-enforced rendering seam every later plan
builds on. Implements D-01, D-04, D-05, D-10, D-11, D-12, D-13.

Purpose: A build-time guarantee that `charm.land/lipgloss/v2` (and bubbletea/
bubbles) can never reach `internal/query` or `internal/mcp` — the agent/MCP
surface stays ANSI-free forever (ROADMAP SC1 + SC4-archtest-half).
Output: `internal/cli/present/archtest/import_graph_test.go` (guarded-set +
self-defeat guard), `internal/cli/present/tty.go` (`ChoosePresentation`),
`internal/cli/present/styles.go` (shared lipgloss palette — the charm importer
that satisfies the self-defeat guard), and the `charm.land/lipgloss/v2` +
promoted `golang.org/x/term` go.mod requires.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rendering-seam-pretty-status-files/06-CONTEXT.md
@.planning/phases/06-rendering-seam-pretty-status-files/06-RESEARCH.md
@.planning/phases/06-rendering-seam-pretty-status-files/06-PATTERNS.md
@.planning/phases/06-rendering-seam-pretty-status-files/06-VALIDATION.md

# Archtest precedents to mirror (mechanism, not content)
@internal/graphstore/archtest/stdout_confinement_test.go
@internal/graphstore/archtest/import_graph_test.go
</context>

<artifacts_this_phase_produces>
Phase-wide symbol/artifact set (this plan delivers the ones marked ← 06-01):

- Package `internal/cli/present` — sole home for `charm.land/lipgloss/v2` (← 06-01 skeleton)
  - `present.ChoosePresentation(isTTY bool, noColor string) bool` — pure branch selector ← 06-01
  - shared lipgloss style palette in `present/styles.go` ← 06-01
  - `present.RenderStatus(r query.StatusResult, projectPath string, w io.Writer) error` — 06-02
  - `present.RenderFiles(r query.FilesResult, w io.Writer) error` — 06-02
  - `present.NewProgress`/spinner type (hand-rolled ticker, stderr) — 06-03
- Archtest package `internal/cli/present/archtest` + `import_graph_test.go` (TUI-01) ← 06-01
- go.mod: new `charm.land/lipgloss/v2` require + promoted direct `golang.org/x/term` ← 06-01
- CLI wiring: isTTY branch in `internal/cli/status.go`, `internal/cli/files.go` — 06-02
- CLI wiring: TTY-gated stderr spinner in `internal/cli/init.go`, `index.go`, `sync.go` — 06-03
- New test files: `present/tty_test.go` ← 06-01; `present/status_test.go`, `files_test.go`, `test/integration/status_files_plain_test.go` — 06-02; `present/progress_test.go`, `internal/cli/progress_cli_test.go` — 06-03
</artifacts_this_phase_produces>

<flagged_assumptions>
Spec-less edge probe returned TUI-01 as `unclassified/unresolved` (backstop miss,
not an edge-free requirement). Surfaced explicitly per the no-silent-drop contract:

- **[ASSUMPTION — TUI-01 unclassified]** The archtest's acceptance is derived
  directly from ROADMAP SC1 ("fails the build if charm is reachable from
  internal/query or internal/mcp") and D-11 (extend to the six-package
  serve-reachable set). Treated as: the archtest must (a) fail-closed before
  the dep exists and (b) go green only when the guarded set is clean AND a real
  charm importer exists. If a reviewer intended a narrower guarded set (only
  query+mcp), this is a superset and still satisfies the letter of TUI-01.
</flagged_assumptions>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Land the fail-closed TUI-01 archtest (RED-first)</name>
  <files>internal/cli/present/archtest/import_graph_test.go</files>
  <read_first>
    - internal/graphstore/archtest/stdout_confinement_test.go (PRIMARY analog — copy guardedPackages, isModuleInternalPackage, closeOverServeReachableImports shape)
    - internal/graphstore/archtest/import_graph_test.go (SECONDARY analog — copy the self-defeat guard at :64 and stripTestVariant at :83)
    - .planning/phases/06-rendering-seam-pretty-status-files/06-PATTERNS.md (§ archtest section — forbidden /v2 list + build order)
  </read_first>
  <behavior>
    - RED (no charm dep yet): running the archtest fails via the self-defeat guard — NO package in the module imports charm.land/lipgloss/v2, so enforcement cannot be verified. This is the D-12 fail-closed proof.
    - After Task 2 (charm importer exists): the forbidden-path check passes for all six guarded packages AND the self-defeat guard is satisfied → GREEN.
    - A hypothetical violation (any guarded package transitively importing a /v2 charm path) produces a t.Errorf naming the offending package and path.
  </behavior>
  <action>
    Create `internal/cli/present/archtest/import_graph_test.go` in `package archtest`.
    Mirror the MECHANISM of the two precedents verbatim (per D-10 — go/packages,
    never regex/source scanning): copy `guardedPackages` (the exact six paths:
    internal/mcp, internal/graphstore, internal/daemon, internal/watch,
    internal/indexer, internal/query), `isModuleInternalPackage`,
    `closeOverServeReachableImports` (guarded-set + NeedDeps transitive
    closure walk, internal/cli excluded), and `stripTestVariant` from the
    analogs. Define `forbiddenImportPaths` as the three /v2-suffixed vanity paths
    per D-11 and RESEARCH Finding 1 (lipgloss/v2, bubbletea/v2, bubbles/v2) —
    the bare non-/v2 paths are the wrong module and MUST NOT appear.
    Write `TestNoCharmInServeReachablePackages`: walk the closure, t.Errorf if
    any guarded/closure package's transitive Imports contains any forbidden path.
    Add the self-defeat guard per D-12: a whole-module `packages.Load(cfg,
    "github.com/seanb4t/codegraph-go/...")` scan (Tests:true) that t.Fatal's if
    NO package imports charm.land/lipgloss/v2 (renamed from the precedent's
    foundGraphstoreImporter → foundCharmImporter). This guard is what makes the
    test RED today and honest tomorrow. Import only golang.org/x/tools/go/packages
    + stdlib — the archtest never imports internal/cli/present itself.
  </action>
  <verify>
    <automated>go test ./internal/cli/present/archtest/... 2>&1 | grep -q "no package.*charm.land/lipgloss/v2\|cannot verify enforcement" && echo "RED-as-expected (self-defeat guard fired)"</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/present/archtest/import_graph_test.go` exists and compiles.
    - `go test ./internal/cli/present/archtest/...` FAILS with the self-defeat
      guard message (no charm importer yet) — the fail-closed proof.
    - The forbidden-path list contains exactly the three `/v2`-suffixed paths;
      `grep -c '"charm.land/[a-z]*"' internal/cli/present/archtest/import_graph_test.go`
      returns 0 (no bare non-/v2 charm path literal).
    - guardedPackages contains all six serve-reachable package paths.
  </acceptance_criteria>
  <done>Archtest lands FIRST, fails-closed via the self-defeat guard, and is ready to enforce isolation once the charm importer arrives in Task 2 (D-12 build order).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add charm.land/lipgloss/v2 + present skeleton — archtest goes GREEN</name>
  <files>go.mod, go.sum, internal/cli/present/styles.go</files>
  <read_first>
    - go.mod (line 134: `golang.org/x/term v0.45.0 // indirect` — to be promoted)
    - .planning/phases/06-rendering-seam-pretty-status-files/06-RESEARCH.md (§ Standard Stack install block; § Pattern 2 — lipgloss v2 has no Renderer type)
    - internal/query/render_status.go (the section wording/order the palette will later decorate — context only)
  </read_first>
  <behavior>
    - After this task, exactly one package (internal/cli/present) imports charm.land/lipgloss/v2; the archtest's self-defeat guard is satisfied.
    - None of the six guarded packages import any /v2 charm path → forbidden-path check passes.
    - go.mod carries lipgloss/v2 as a direct require and x/term promoted to direct; no bubbletea/bubbles line exists.
  </behavior>
  <action>
    Run `go get charm.land/lipgloss/v2@v2.0.5` then `go mod tidy` (per D-13 — pin
    the exact version; tidy promotes golang.org/x/term from // indirect to a
    direct require since present will import it downstream). Create
    `internal/cli/present/styles.go` in `package present` defining a small shared
    lipgloss style palette (Claude's discretion per D-01: e.g. a bold header
    style, a muted label style, a section-border style) using
    `import lipgloss "charm.land/lipgloss/v2"`. Do NOT construct any
    lipgloss.Renderer / NewRenderer / DefaultRenderer — those symbols were
    removed in v2 (RESEARCH State of the Art); use `lipgloss.NewStyle()` and
    `Style.Render()` only. This file is the charm importer that flips the
    archtest green; the actual RenderStatus/RenderFiles bodies land in 06-02.
    Do NOT add charm.land/bubbles or charm.land/bubbletea (any version) — the
    hand-rolled progress path in 06-03 avoids them entirely (D-13, RESEARCH
    Finding 2 / Pitfall 1).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/cli/present/archtest/... && go vet ./internal/cli/present/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/cli/present/archtest/...` now PASSES (self-defeat guard satisfied + six guarded packages clean).
    - `grep -c 'charm.land/lipgloss/v2' go.mod` returns at least 1 (direct require).
    - `grep -E 'bubbletea|bubbles' go.mod` returns nothing (exit 1) — completion check per D-13 / RESEARCH Pitfall 1 warning sign.
    - `golang.org/x/term` appears in go.mod WITHOUT a trailing `// indirect` comment.
    - No occurrence of `Renderer`/`NewRenderer`/`DefaultRenderer` in internal/cli/present (v2 API discipline).
  </acceptance_criteria>
  <done>The rendering seam is build-enforced and live: charm is confined to internal/cli/present, the archtest is green, and go.mod carries only lipgloss/v2 (+ promoted x/term) — no bubbletea/bubbles.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: ChoosePresentation pure selector (D-04/D-05)</name>
  <files>internal/cli/present/tty.go, internal/cli/present/tty_test.go</files>
  <read_first>
    - .planning/phases/06-rendering-seam-pretty-status-files/06-RESEARCH.md (§ Pattern 1 — ChoosePresentation; § Pitfall 3 — why a pure fn, not an inline fd check)
    - .planning/phases/06-rendering-seam-pretty-status-files/06-PATTERNS.md (§ internal/cli/present/tty.go)
  </read_first>
  <behavior>
    - Test the full (isTTY × NO_COLOR) matrix:
      - Test 1: ChoosePresentation(true, "") == true (TTY, color allowed → pretty)
      - Test 2: ChoosePresentation(false, "") == false (non-TTY → plain, regardless of NO_COLOR)
      - Test 3: ChoosePresentation(true, "1") == false (NO_COLOR set → plain even on TTY, D-05)
      - Test 4: ChoosePresentation(false, "1") == false
  </behavior>
  <action>
    Create `internal/cli/present/tty.go` with the pure, side-effect-free
    `ChoosePresentation(isTTY bool, noColor string) bool` returning
    `isTTY && noColor == ""` (D-04 TTY gate AND D-05 NO_COLOR honored). It must
    NOT read os.Getenv or call term.IsTerminal itself — real fd/env values are
    read ONLY at the RunE call sites in 06-02/06-03 (D-03; RESEARCH anti-pattern
    "checking NO_COLOR/TTY inside present"). Create `internal/cli/present/tty_test.go`
    covering the four-case truth matrix above. This is the canonical unit-testable
    selector that makes TUI-02/TUI-05 branch logic verifiable without a real pty
    (Pitfall 3).
  </action>
  <verify>
    <automated>go test ./internal/cli/present/... -run TestChoosePresentation</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/cli/present/... -run TestChoosePresentation` passes all four cases.
    - `ChoosePresentation` has no import of `os` or `golang.org/x/term` (grep the file — pure fn).
    - `go test ./internal/cli/present/archtest/...` remains green.
  </acceptance_criteria>
  <done>The one shared, unit-tested branch selector exists; 06-02 and 06-03 wire it identically at their RunE boundaries (D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/test tier → agent path | The archtest is the enforcement point: it proves no styling code (charm) is reachable from the serve-side (`internal/mcp`/`internal/query`/…) closure |
| dependency supply chain → go.mod | Adding `charm.land/lipgloss/v2` via the module proxy/vanity domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / Info Disclosure | charm/ANSI reaching internal/mcp or internal/query (garbled/misleading bytes in an agent's context) | high | mitigate | The TUI-01 archtest itself — build-time enforcement over the six-package serve-reachable closure (Task 1) |
| T-06-02 | Tampering | archtest becoming vacuously green after a refactor drops the only charm import | medium | mitigate | Self-defeat guard (D-12) fails if no package imports charm.land/lipgloss/v2 (Task 1); a real importer (present/styles.go) satisfies it (Task 2) |
| T-06-03 | Tampering | bare `charm.land/lipgloss` (no /v2) silently resolving to the wrong v1 module, defeating both the archtest list and the API assumptions | medium | mitigate | /v2-suffixed paths everywhere; go.mod grep completion checks (Task 1 + Task 2 acceptance criteria) |
| T-06-SC | Tampering | supply-chain integrity of the new go.mod require | low | accept | Per RESEARCH Package Legitimacy Audit, `charm.land/lipgloss/v2` and `golang.org/x/term` are both **OK/Approved** (Origin = official charmbracelet/lipgloss + Go team); no [ASSUMED]/[SUS] package installed (creack/pty explicitly NOT added), so no blocking-human checkpoint is required. Full CGo/govulncheck/SBOM closure audit is Phase 8 (REL-01). |
</threat_model>

<verification>
- `go test ./internal/cli/present/archtest/...` — RED after Task 1, GREEN after Task 2, stays GREEN after Task 3.
- `go test ./internal/cli/present/...` — ChoosePresentation matrix green (Task 3).
- `go build ./...` succeeds; `grep -E 'bubbletea|bubbles' go.mod` returns nothing.
- `go test ./... && go test ./testdata/golden/...` — full suite + golden corpus stay green (no agent-surface bytes changed).
</verification>

<success_criteria>
- TUI-01 archtest exists, fails-closed before the dep, and enforces the six-package isolation once charm lands (ROADMAP SC1).
- Charm is confined to `internal/cli/present`; go.mod carries lipgloss/v2 + promoted x/term, no bubbletea/bubbles (D-13).
- `ChoosePresentation` is a pure, unit-tested selector (D-04/D-05).
</success_criteria>

<output>
Create `.planning/phases/06-rendering-seam-pretty-status-files/06-01-SUMMARY.md` when done
</output>
