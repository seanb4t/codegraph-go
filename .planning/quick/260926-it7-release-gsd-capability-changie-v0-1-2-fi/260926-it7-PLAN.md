---
quick_id: 260926-it7
slug: release-gsd-capability-changie-v0-1-2-fi
created: 2026-09-26
mode: quick
phase: quick-260926-it7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh
  - gsd-capability-changie:test/run.sh
  - gsd-capability-changie:README.md
  - gsd-capability-changie:skills/changie-fragments/SKILL.md
  - gsd-capability-changie:capability.json
  - Taskfile.yml
  - CONTRIBUTING.md
  - .planning/phases/02-phase-close-fragment-capability/02-REVIEW.md
autonomous: true
requirements: [CAP-03, CAP-04, CHG-04]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "write-fragments.sh validates `--pr` as a whole string: `--pr` with an embedded newline (`12` newline `DANGEROUS`) or a trailing newline exits 2 with `changie-fragments: error: bad-pr:` and prints no `changie-fragments: pr ` line. `--pr 0`, `abc` and `-5` are still refused, and `--pr 77` and `--pr 1` are still accepted. The looked-up PR uses the same whole-string helper, and `[note:pr-lookup-invalid]` stays green (WR-01, CAP-03, T-02-03)"
    - "gsd-capability-changie's test/run.sh passes 33 of 33 legs. `[bad-pr:multiline]` is the 20th leg, right after `[bad-pr]`. Its test commit precedes the fix commit, and the pasted RED transcript shows 19 `ok [` lines and then `::error::run.sh: [bad-pr:multiline] expected exit 2, got 0`. shellcheck is clean on all four executables (rule x1cjy9vyhq)"
    - "The annotated tag `v0.1.2` sits on `$CAP`'s `main` tip. `main` and the tag were pushed fast-forward over HTTPS with `GIT_TERMINAL_PROMPT=0`. On the remote, `v0.1.0` (tag object `60d4967a…`, peeling to `5700e60f…`) and `v0.1.1` (tag object `7305b9e4…`, peeling to `35b674eb…`) are unchanged, visibility is still PRIVATE, and `refs/heads/main` equals `v0.1.2^{}`. A clean HTTPS clone at `v0.1.2` passes 33 of 33. The README installs from `$SPEC` only (D-10, D-11 amendment, D-13)"
    - "codegraph-go's project install is at 0.1.2 from `$SPEC`, installed with `gsd-tools capability install` over the existing entry. The installed `capability.json` and the bundled `write-fragments.sh` are each `cmp`-equal to the tag's copies. The installed script refuses a multi-line `--pr` live in codegraph-go. `workflow.changie_command` is still `task changie --`. render-hooks verify:post shows exactly one changie step (CAP-04, D-13)"
    - "The maintainer's global install is at 0.1.2 from `$SPEC`, re-materialized with `capability set changie --enable --runtime claude --scope global`. `$SKILLS/gsd-changie-fragments/SKILL.md` is `cmp`-equal to `git show v0.1.2:skills/changie-fragments/SKILL.md`, and the marker names `changie`. In `$SKILLS`, the names listing, the top-level symlink targets and a `find -L` checksum snapshot of every file outside `gsd-changie-fragments/` are byte-equal before and after (D-14, T-02-16)"
    - "`task check:changie` ends `check:changie: 13 of 13 checks passed against a scratch copy (source tree byte-unchanged)`. Leg 13 runs the real `task changie -- new --dry-run -k Fixes -b <payload> -m PR=1` from the scratch copy. The payload carries a `$(touch <marker>)` substitution, the same touch in backticks, and `; touch <marker>;`. changie prints the exact line `body: <payload>`, and the marker is never created (02-REVIEW CR-01)"
    - "Leg 13 goes RED at `[13/13]` against a confirmed-applied mutation in a disposable detached worktree. The mutation splices each forwarded argument into `changie:` unquoted. The worktree is then reverted byte-cleanly and runs GREEN, and the worktree listing and the main-tree status are byte-equal before and after (rule 84d1gfpywd, D-09)"
    - "All thirteen `# Leg N/13:` comments in check:changie match the new total. No `# Leg N/11:` or `/12` comment or `[N/12]` string remains (IN-01)"
    - "CONTRIBUTING.md's two install specs name `#v0.1.2`. `GOTOOLCHAIN=go1.26.6 GOWORK=off go test ./internal/upgrade/ -count=1` passes, including TestContributingReferencesRealTaskTargets (D-12)"
    - "02-REVIEW.md ends with an appended `## Resolution (quick 260926-it7, 2026-09-26)` section covering WR-01, CR-01 and IN-01. Its bytes up to the old end of file are identical to the file at `a335232d` (append-only; the frontmatter is untouched)"
  prohibitions:
    - statement: "MUST NOT re-point, delete or re-push tags v0.1.0 or v0.1.1. MUST NOT use --force, --tags, --delete or --mirror. MUST NOT run `gh repo edit` or change visibility. MUST NOT use an SSH remote or URL (D-10, D-11 amendment, D-13)"
      requirement_id: CAP-04
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT commit to $CAP after tagging v0.1.2. gsd-core installs only the tag at main's tip, so main's tip must stay equal to v0.1.2 (02-06 SUMMARY Upstream gap)"
      requirement_id: CAP-04
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT hand-copy, link or edit anything under $SKILLS or ~/.gsd, and MUST NOT set GSD_HOME to redirect gsd-core. Installs and materialization go only through gsd-core verbs (T-02-16, planning-artifacts rule)"
      requirement_id: CAP-04
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT use `[ci skip]` in any commit message, in either repository"
      requirement_id: CHG-04
      category: process
      status: resolved
      verification: test
  artifacts:
    - path: "gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh"
      provides: "whole-string positive-integer helper used by the --pr check and the looked-up PR check"
      contains: "is_positive_int"
    - path: "gsd-capability-changie:test/run.sh"
      provides: "33 labelled legs including [bad-pr:multiline]"
      contains: "EXPECTED_LEGS=33"
    - path: "gsd-capability-changie:README.md"
      provides: "v0.1.2 install spec"
      contains: "gsd-capability-changie.git#v0.1.2"
    - path: "Taskfile.yml"
      provides: "check:changie leg 13 (real task changie -- path) and /13 renumbering"
      contains: "13 of 13 checks passed"
    - path: "CONTRIBUTING.md"
      provides: "per-clone and per-machine install specs at v0.1.2"
      contains: "gsd-capability-changie.git#v0.1.2"
    - path: ".planning/phases/02-phase-close-fragment-capability/02-REVIEW.md"
      provides: "appended resolution note for WR-01, CR-01, IN-01"
      contains: "## Resolution (quick 260926-it7, 2026-09-26)"
  key_links:
    - from: "write-fragments.sh is_positive_int"
      to: "`changie new -m PR=<n>` argv"
      via: "only a whole-string positive integer reaches PR"
      pattern: "is_positive_int"
    - from: "Taskfile.yml check:changie leg 13"
      to: "Taskfile.yml `changie:` task `{{.CLI_ARGS}}`"
      via: "`{{.TASK_EXE}} --taskfile {{.ROOT_TASKFILE}} changie --` drives the Taskfile under test"
      pattern: "{{.ROOT_TASKFILE}}"
    - from: "codegraph-go .gsd/capabilities/changie/ and ~/.gsd/capabilities/changie/"
      to: "gsd-capability-changie tag v0.1.2"
      via: "gsd-tools capability install $SPEC over the existing entry (upgrade swap)"
      pattern: "gsd-capability-changie.git#v0.1.2"
---

<objective>
Close the three findings in Phase 2's code review (`02-REVIEW.md`: WR-01, CR-01 residual, IN-01). Release gsd-capability-changie `v0.1.2` with the WR-01 fix. Repoint codegraph-go's project install and the maintainer's global install to it.

Purpose:
- WR-01 is a real validation hole. A multi-line `--pr` is accepted as `pr 12`, which breaks T-02-03's closure premise.
- CR-01 could not be reproduced. go-task 3.52.0 shell-quotes `CLI_ARGS`. The safety of the configured `task changie --` path was proven only by a manual check, so this plan commits a guard for it and proves that guard can fail.
- IN-01 is stale comment text.

This plan follows the v0.1.1 publish-and-upgrade procedure from 02-06 exactly. The maintainer pre-approved the push on 2026-09-26, so there is no checkpoint.

Shape: the plan leads with a tracer. Task 1 carries the WR-01 fix end to end:
- RED-first leg;
- fix;
- `v0.1.2` metadata;
- tag and push;
- clean-clone proof;
- codegraph-go's project install refusing a multi-line `--pr` live.

Task 2 adds codegraph-go's leg 13 and the IN-01 renumbering, with RED proven by mutation. Task 3 repoints the global install, updates CONTRIBUTING and records the resolutions in 02-REVIEW.

Names used below:
- `$CG` means `/Volumes/Code/github.com/seanb4t/codegraph-go`.
- `$CAP` means `/Volumes/Code/github.com/seanb4t/gsd-capability-changie`. Files in `$CAP` carry the `gsd-capability-changie:` prefix. Evidence from `$CAP` goes into this quick task's SUMMARY in codegraph-go (D-09).
- `$CHB` means `$(cd $CG && GOWORK=off go tool -modfile=go.tool-changie.mod -n changie)`, codegraph-go's pinned changie v1.26.0 build.
- `$SPEC` means `https://github.com/seanb4t/gsd-capability-changie.git#v0.1.2`.
- `$SKILLS` means `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills`.

Rules that apply to every task:
- Commit scope is `quick-260926-it7` in both repositories. No commit message contains `[ci skip]`. Commits may use `-c commit.gpgsign=false`, and the tag may use `-c tag.gpgsign=false`.
- `go test` always runs with `GOTOOLCHAIN=go1.26.6 GOWORK=off`. The local default is go1.27.1.
- The executor runs sequentially on the main tree (`workflow.use_worktrees=false`). Task 2's mutation worktree is disposable, detached, and removed before the task ends.

Output:
- `$CAP`: three commits and annotated tag `v0.1.2`, pushed.
- codegraph-go: the Taskfile.yml leg and renumbering, the CONTRIBUTING spec, and the 02-REVIEW resolution note.
- Machine state: both installs at 0.1.2.
- `260926-it7-SUMMARY.md`: all transcripts.
</objective>

<execution_context>
@/Users/sean/.claude/gsd-core/workflows/execute-plan.md
@/Users/sean/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-phase-close-fragment-capability/02-REVIEW.md
@.planning/phases/02-phase-close-fragment-capability/02-06-SUMMARY.md

<interfaces>
Planning-time live observations (2026-09-26, this machine). Task 1's precondition re-checks the mutable ones. If any of them has moved, STOP and report rather than adapting.

`$CAP` state:
- The working tree is clean, and `main` = `origin/main` = `v0.1.1^{commit}` = `35b674eb13a7dbd64932648840dc9f447c95dda3`.
- `origin` is `https://github.com/seanb4t/gsd-capability-changie.git`, for both fetch and push.
- The remote `ls-remote` lists these refs:
  - `refs/heads/main` `35b674eb…`;
  - `refs/tags/v0.1.0` `60d4967ad300643b30f4d947e64a459974c9ed9f`, peeling to `5700e60fb497e033d87ff953c4e957444993628a`;
  - `refs/tags/v0.1.1` `7305b9e46a5a60e62a2b9ec4b94b6561c3367ea9`, peeling to `35b674eb…`;
  - no `v0.1.2`.
- Visibility is `PRIVATE`, and the default branch is `main`.

WR-01:
- `write-fragments.sh --phase 2 --repo $CG --pr <12 newline DANGEROUS> --list` exits 0 (reproduced).
- The two per-line regex pipelines are at `write-fragments.sh` lines 133 (argument parsing, inside the `case` at lines 130-141) and 419 (the looked-up PR `elif`). The `bad_pr()` helper is at line 60.
- `test/run.sh` has `EXPECTED_LEGS=32` at line 130 and the `check_error_line` helper at line 149. `[pr-override]` is at line 543 and `[bad-pr]` at lines 577-586. The `[write]` banner follows.
- Leg order (the `ok [` lines): ordering, install, manifest, dispatch-default, dispatch-false, dispatch-true, then the seven `skip:*` legs, then the four `note:*` legs, then pr-override and bad-pr (the 19th), then write, and so on.

Baselines:
- `CHANGIE_BIN=$CHB bash test/run.sh` gives `run.sh: 32 of 32 legs passed against a scratch project` in about 2m42s.
- shellcheck 0.11.0 is clean on `write-fragments.sh`, `test/run.sh`, `test/fixtures/bin/gh` and `test/fixtures/bin/fake-task`.
- `task check:changie` gives 12 legs green in about 3s.
- `GOTOOLCHAIN=go1.26.6 GOWORK=off go test ./internal/upgrade/ -count=1` passes in about 3s.

go-task 3.52.0 (`/Users/sean/.local/bin/task`), probed:
- `{{.TASK_EXE}}` renders `task`. `{{.ROOT_TASKFILE}}` and `{{.ROOT_DIR}}` render the invoking Taskfile's absolute path and directory.
- `(cd <scratch> && task --taskfile $CG/Taskfile.yml changie -- new --dry-run …)` exits 0. go-task runs `changie:` in the Taskfile's own directory, so changie reads `$CG/.changie.yaml`.
- `task -d <scratch> --taskfile $CG/Taskfile.yml changie …` exits 201 with `go: cannot find main module, but -modfile was set`, because `GO_TOOL_CHANGIE` uses a relative `-modfile`. The real `changie:` task therefore cannot execute inside the scratch directory itself.
- The leg-13 payload was run through the real `task changie -- new --dry-run -k Fixes -b "$payload" -m PR=1`, invoked from a scratch dir. Result:
  - status 0;
  - the marker was not created;
  - stdout holds the exact line `body: <payload>` as a plain YAML scalar;
  - the `.changie.yaml`/`.changes`/`CHANGELOG.md` cksum snapshot was equal before and after;
  - the scratch dir stayed empty.
- On a throwaway Taskfile, `printf %s\\n {{.CLI_ARGS}}` printed an embedded `$(touch M)` literally with no marker. The mutation form `{{range .CLI_ARGS_LIST}} {{.}}{{end}}` executed it: the marker was created.
- `Taskfile.yml`:
  - The `changie:` task is at line 538 and its only cmd is at line 549: `      - "{{.GO_TOOL_CHANGIE}} changie {{.CLI_ARGS}}"`.
  - `check:changie` spans lines 551-879. Leg 12 ends with `passed=$((passed + 1))` just before the `after=` snapshot.
  - The legs carry 53 `[N/12]` strings.
  - The comments are `# Leg 1/11:` … `# Leg 11/11:` plus `# Leg 12/12:`.
  - The spelled-out count appears once in `desc:`, at line 557.
- CI's `test` job runs `task check:changie` on `namespace-profile-linux-amd64-4x8`, and `internal/upgrade/changie_shape_test.go` pins its step position and preconditions. Neither is touched.

Installs:
- Both ledgers (`$CG/.gsd-capabilities.json` and `~/.gsd-capabilities.json`) read 0.1.1 from the `#v0.1.1` spec, and both `capability.json` files and the materialized SKILL.md are `cmp`-equal to `v0.1.1`.
- `$SKILLS` has 142 top-level entries: 67 symlinks and 75 directories. `find -L` reaches 1933 files outside `gsd-changie-fragments/`; `find` without `-L` reaches 1193. `gsd-changie-fragments/` is a real directory holding `SKILL.md` and `.gsd-capability-skill`, whose content is `changie`.
- render-hooks verify:post shows 1 changie step. Draft PR #88 (`gsd/v0.15.0-milestone`) is OPEN.
- `$CG/.gsd/` and `$CG/.gsd-capabilities.json` are gitignored (`.gitignore` lines 46 and 51).

gsd-core upgrade semantics (02-06, verified there):
- `capability install <new spec>` over an existing entry is the upgrade swap. `capability update` re-fetches the recorded spec and cannot move to a new tag.
- A git source is fetched as a depth-1 clone of the default branch, so only the tag at `main`'s tip installs. Once `main` moves to `v0.1.2`, `#v0.1.1` stops installing. This is expected, and is why CONTRIBUTING moves in the same run.
- `capability set changie --enable --runtime claude --scope global` re-stages SKILL.md from the global capability root into `$SKILLS/gsd-changie-fragments/`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer, RED first. A multi-line --pr is refused end to end, from the capability fix through published v0.1.2 to codegraph-go's installed bundle</name>
  <precondition>`gh auth status` exits 0. `git -C $CAP status --porcelain` is empty. `git -C $CAP rev-parse main origin/main` prints `35b674eb13a7dbd64932648840dc9f447c95dda3` twice. `GIT_TERMINAL_PROMPT=0 git ls-remote https://github.com/seanb4t/gsd-capability-changie.git` shows `refs/heads/main` at that SHA, `refs/tags/v0.1.0` `60d4967ad300643b30f4d947e64a459974c9ed9f`, `refs/tags/v0.1.1` `7305b9e46a5a60e62a2b9ec4b94b6561c3367ea9`, and no `v0.1.2` ref. `git -C $CAP tag -l v0.1.2` prints nothing. `$CG/.gsd-capabilities.json`'s changie entry reads version `0.1.1`. If any of this differs, STOP and report.</precondition>
  <reversibility rating="costly">Pushing `main` past `v0.1.1` stops gsd-core from installing `#v0.1.1`, because it uses a depth-1 clone. Tag `v0.1.2` becomes the spec that CONTRIBUTING and SHIP-04 name, so moving it later means re-installing and re-recording (D-10). The push is pre-approved by the maintainer (2026-09-26, following the D-13 part 3 pattern).</reversibility>
  <files>gsd-capability-changie:test/run.sh, gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh, gsd-capability-changie:capability.json, gsd-capability-changie:skills/changie-fragments/SKILL.md, gsd-capability-changie:README.md, $CG/.gsd-capabilities.json and $CG/.gsd/capabilities/changie/ (gitignored install state)</files>
  <read_first>
    - gsd-capability-changie:skills/changie-fragments/scripts/write-fragments.sh lines 1-70 (header bash-3.2 rules, helpers), 125-145 (`--pr` check), 390-430 (PR resolution)
    - gsd-capability-changie:test/run.sh lines 125-160 (EXPECTED_LEGS, check_error_line) and 540-600 ([pr-override], [bad-pr])
    - gsd-capability-changie:README.md lines 35-75 (install sections) and 164-176 (Errors table)
    - gsd-capability-changie:skills/changie-fragments/SKILL.md lines 1-12, and gsd-capability-changie:capability.json
    - .planning/phases/02-phase-close-fragment-capability/02-06-SUMMARY.md `## Upgrade` and `## Upstream gap`
  </read_first>
  <behavior>
    - Test [bad-pr:multiline]: `--pr` given `12` + newline + `DANGEROUS`, and `12` + a trailing newline, each exits 2, prints `changie-fragments: error: bad-pr:`, and prints no line starting `changie-fragments: pr `. It is RED against v0.1.1, where both exit 0.
    - Unchanged: [bad-pr] (0, abc, -5 refused), [pr-override] (77 and 1 accepted, gh not consulted) and [note:pr-lookup-invalid] (lookup `0` and `12abc` become a note with `pr none`).
    - The installed v0.1.2 bundle in codegraph-go refuses a multi-line `--pr` the same way.
  </behavior>
  <action>
All `$CAP` work uses `git -C "$CAP"`. Keep every transcript (RED, GREEN, shellcheck, push, ls-remote, clone run, install before and after) in the executor's scratch directory for the SUMMARY (D-09).

1. RED first (WR-01; rule x1cjy9vyhq).
   - In `$CAP/test/run.sh`, add a leg labelled `[bad-pr:multiline]` immediately after the `[bad-pr]` leg's `passed` increment and before the `[write]` banner. Shape it like `[bad-pr]`.
   - Loop over two values written with bash ANSI-C quoting: `12`, a newline, then `DANGEROUS`; and `12` followed by a trailing newline.
   - For each value, under `set +e`, capture `out=$(bash "${SCRIPT}" --phase 1 --repo "${work}" --pr "${badval}" --list 2>&1)` and its status. Then call `check_error_line "bad-pr:multiline" 2 "bad-pr" "${out}" "${status}"`.
   - Also fail with `::error::run.sh: [bad-pr:multiline] a multi-line --pr printed a pr line: ${out}` (exit 1) if `out` has a line starting with `changie-fragments: pr `. Use the same `grep -q` anchor style as the rest of the file.
   - After the loop, echo `ok [bad-pr:multiline] a --pr value with an embedded or trailing newline exits 2 with error: bad-pr and prints no pr line` and increment `passed`.
   - Set `EXPECTED_LEGS=33`.
   - Run `CHANGIE_BIN="$CHB" bash test/run.sh`. It must fail with exactly 19 `ok [` lines followed by `::error::run.sh: [bad-pr:multiline] expected exit 2, got 0`. If it fails anywhere else, stop and investigate. Do not commit a different RED.
   - Run `shellcheck test/run.sh`; it must be clean.
   - Commit only `test/run.sh` as `test(quick-260926-it7): add failing multi-line --pr refusal leg (WR-01)`.

2. GREEN (WR-01, T-02-03, T-02-23).
   - In `write-fragments.sh`, add a helper `is_positive_int` next to `bad_pr()`. It is a single `case "$1" in` whose only rejecting arm is `''|0*|*[!0123456789]*` and returns 1; otherwise it returns 0.
     - `case` matches the whole string, so an embedded or trailing newline, a sign, a leading zero or any non-digit fails.
     - The digit set is spelled out, not written as a range, so bash 3.2's locale-dependent range collation cannot widen it. This is the maintainer's suggested pattern with explicit digits.
   - Replace the argument-parsing `--pr` block (the `case` plus the per-line regex pipeline at about lines 130-141) with one test: when `PR_ARG` is non-empty and `is_positive_int "${PR_ARG}"` fails, call `bad_pr "not a positive integer: ${PR_ARG}"`. The message, the exit 2 and the position before any other work stay as they are.
   - Replace the looked-up PR's `elif` pipeline (about line 419) with `elif ! is_positive_int "${LOOKED_UP_PR}"; then`. Keep its fixed `pr-lookup-invalid` note text. This is defense in depth, because the lookup is already whitespace-stripped.
   - After the edit, neither validation site pipes through grep. Change nothing else, and keep to the header's bash 3.2 rules.
   - Run `CHANGIE_BIN="$CHB" bash test/run.sh`. Its last line must be `run.sh: 33 of 33 legs passed against a scratch project`.
   - Run `shellcheck` on `skills/changie-fragments/scripts/write-fragments.sh`, `test/run.sh`, `test/fixtures/bin/gh` and `test/fixtures/bin/fake-task`; it must be clean.
   - Commit only `write-fragments.sh` as `fix(quick-260926-it7): validate --pr and the looked-up PR as whole strings (WR-01)`.

3. v0.1.2 metadata (D-10, D-11 amendment).
   - `capability.json`: set `version` to `"0.1.2"` and change nothing else.
   - `SKILL.md`: set `metadata.version` to `"0.1.2"` and change nothing else.
   - `README.md`:
     - change both install specs (the project line under `## Install, per clone` and the global line under `## Making the skill dispatchable in Claude Code, per machine`) to `$SPEC`;
     - in the `## Errors` table, change the `bad-pr` cause to: `--pr` is not a positive integer, checked as a whole value, so a leading zero, a sign or any non-digit character (including an embedded newline) is refused;
     - make no other README change.
   - Commit these three files as `docs(quick-260926-it7): prepare v0.1.2 metadata and install spec`.

4. Tag and publish. This follows the D-13 part 3 pattern and is pre-approved; there is no checkpoint.
   - Write the release note with the file-writing tool into a temp file:
     - title line `gsd-capability-changie v0.1.2`;
     - `--pr` is now validated as a whole string (WR-01; a multi-line `--pr` previously passed as `pr 12`);
     - the looked-up PR uses the same check;
     - new `[bad-pr:multiline]` leg, 33 legs;
     - install spec `#v0.1.2`;
     - older tags stop installing once `main` moves, because gsd-core installs from a depth-1 clone.
     The repository has no CHANGELOG, so the annotated tag message is the release note (the 02-06 precedent).
   - Run `git -C "$CAP" -c tag.gpgsign=false tag -a v0.1.2 -F <file>` on HEAD.
   - Run `GIT_TERMINAL_PROMPT=0 git -C "$CAP" fetch origin`, then require `git -C "$CAP" merge-base --is-ancestor origin/main main`.
   - Push `GIT_TERMINAL_PROMPT=0 git -C "$CAP" push origin main`, then `GIT_TERMINAL_PROMPT=0 git -C "$CAP" push origin refs/tags/v0.1.2`, over the existing HTTPS origin.
   - Never use `--force`, `--tags`, `--delete` or `--mirror`. Never touch `v0.1.0` or `v0.1.1`. Never run `gh repo edit`. Never use an SSH URL.
   - Make no further `$CAP` commit after the tag.

5. Prove the tag.
   - `gh repo view seanb4t/gsd-capability-changie --json visibility --jq .visibility` prints `PRIVATE`.
   - `ls-remote` shows `v0.1.0` and `v0.1.1` exactly as in `<interfaces>`. `refs/tags/v0.1.2` equals the local `git rev-parse v0.1.2`, which is a tag object. `refs/tags/v0.1.2^{}` and `refs/heads/main` both equal the local `v0.1.2^{commit}`.
   - Clone with `GIT_TERMINAL_PROMPT=0 git clone --branch v0.1.2 https://github.com/seanb4t/gsd-capability-changie.git` into a fresh `mktemp -d`, then run that clone's `test/run.sh` with `CHANGIE_BIN="$CHB"`. It must reach 33 of 33.

6. Repoint codegraph-go's project install (CAP-04, following the D-13 part 4 pattern).
   - Record the before state:
     - the changie entry's version and source in `$CG/.gsd-capabilities.json`, via `node -e`;
     - `git -C "$CG" status --porcelain --untracked-files=all -- . ':(exclude).planning'`, which must be empty.
   - From `$CG`, run `gsd-tools capability install "$SPEC" --scope project`. It must exit 0 with status installed at version 0.1.2. This is the reinstall-as-upgrade swap; `capability update` cannot move tags.
   - Prove the result:
     - `.gsd/capabilities/changie/capability.json` `cmp`s equal to `git -C "$CAP" show v0.1.2:capability.json`, and `.gsd/capabilities/changie/skills/changie-fragments/scripts/write-fragments.sh` `cmp`s equal to the tag's copy;
     - the ledger reads 0.1.2 with source `$SPEC`;
     - `gsd-tools query config-get workflow.changie_fragments --raw` prints `true`, and `workflow.changie_command` prints `task changie --`;
     - the render-hooks verify:post changie-step count, parsed with node, is 1.
   - Live WR-01 proof: run the installed script from `$CG` with `--phase 2 --pr` set to `12`, a newline, then `DANGEROUS`, and `--list`. It must exit 2, print `changie-fragments: error: bad-pr:`, and print no `changie-fragments: pr ` line.
   - Also run it with `--phase 2 --list` only.
     - If `gh pr view 88 --json state --jq .state` prints `OPEN` at that moment, it must print `changie-fragments: pr 88`.
     - Otherwise it must print exactly one `changie-fragments: note:` line and `changie-fragments: pr none`.
   - The porcelain status must be identical before and after the install and both runs.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/gsd-capability-changie && test "$(rg -F -o '[1-9][0-9]*$' skills/changie-fragments/scripts/write-fragments.sh | wc -l | tr -d ' ')" = 0 && test "$(rg -F -o 'is_positive_int' skills/changie-fragments/scripts/write-fragments.sh | wc -l | tr -d ' ')" -ge 3 && rg -q -F 'EXPECTED_LEGS=33' test/run.sh && rg -q -F '[bad-pr:multiline]' test/run.sh && shellcheck skills/changie-fragments/scripts/write-fragments.sh test/run.sh test/fixtures/bin/gh test/fixtures/bin/fake-task && CHANGIE_BIN="$(cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOWORK=off go tool -modfile=go.tool-changie.mod -n changie)" bash test/run.sh 2>&1 | tail -1 | rg -x -F 'run.sh: 33 of 33 legs passed against a scratch project'</automated>
    <fails_when>non-zero exit: a per-line regex check survives, the helper is not used at both sites, the leg or count is missing, shellcheck flags anything, or the suite is not 33 of 33 (runtime about 3 minutes)</fails_when>
    <automated>C=/Volumes/Code/github.com/seanb4t/gsd-capability-changie && l=$(git -C "$C" log --reverse --format=%s v0.1.1..v0.1.2) && test "$(printf '%s\n' "$l" | rg -o '^(test|fix|docs)\(quick-260926-it7\)' | tr '\n' ' ')" = "test(quick-260926-it7) fix(quick-260926-it7) docs(quick-260926-it7) " && test "$(printf '%s\n' "$l" | rg -F -o '[ci skip]' | wc -l | tr -d ' ')" = 0 && rd=$(git -C "$C" show v0.1.2:README.md) && test "$(printf '%s\n' "$rd" | rg -F -o 'gsd-capability-changie.git#v0.1.2' | wc -l | tr -d ' ')" = 2 && test "$(printf '%s\n' "$rd" | rg -F -o 'gsd-capability-changie.git#v0.1.1' | wc -l | tr -d ' ')" = 0 && sk=$(git -C "$C" show v0.1.2:skills/changie-fragments/SKILL.md) && printf '%s\n' "$sk" | rg -q -F 'version: "0.1.2"' && cj=$(git -C "$C" show v0.1.2:capability.json) && printf '%s' "$cj" | node -e 'process.exit(JSON.parse(require("fs").readFileSync(0,"utf8")).version==="0.1.2"?0:1)'</automated>
    <fails_when>non-zero exit: the test/fix/docs order is wrong, a commit carries [ci skip], or the tagged README/SKILL.md/capability.json does not carry v0.1.2</fails_when>
    <automated>C=/Volumes/Code/github.com/seanb4t/gsd-capability-changie && test "$(gh repo view seanb4t/gsd-capability-changie --json visibility --jq .visibility)" = PRIVATE && r=$(GIT_TERMINAL_PROMPT=0 git ls-remote https://github.com/seanb4t/gsd-capability-changie.git) && printf '%s\n' "$r" | rg -x -F "$(printf '60d4967ad300643b30f4d947e64a459974c9ed9f\trefs/tags/v0.1.0')" && printf '%s\n' "$r" | rg -x -F "$(printf '5700e60fb497e033d87ff953c4e957444993628a\trefs/tags/v0.1.0^{}')" && printf '%s\n' "$r" | rg -x -F "$(printf '7305b9e46a5a60e62a2b9ec4b94b6561c3367ea9\trefs/tags/v0.1.1')" && printf '%s\n' "$r" | rg -x -F "$(printf '35b674eb13a7dbd64932648840dc9f447c95dda3\trefs/tags/v0.1.1^{}')" && test "$(git -C "$C" cat-file -t v0.1.2)" = tag && o=$(git -C "$C" rev-parse v0.1.2) && c=$(git -C "$C" rev-parse 'v0.1.2^{commit}') && printf '%s\n' "$r" | rg -x -F "$(printf '%s\trefs/tags/v0.1.2' "$o")" && printf '%s\n' "$r" | rg -x -F "$(printf '%s\trefs/tags/v0.1.2^{}' "$c")" && printf '%s\n' "$r" | rg -x -F "$(printf '%s\trefs/heads/main' "$c")" && test "$(git -C "$C" rev-parse main)" = "$c" && test -z "$(git -C "$C" status --porcelain)"</automated>
    <fails_when>non-zero exit: the repository is not private, v0.1.0 or v0.1.1 moved, v0.1.2 is missing or lightweight, remote or local main is not at v0.1.2, or $CAP has uncommitted changes</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && C=/Volumes/Code/github.com/seanb4t/gsd-capability-changie && t=$(mktemp) && git -C "$C" show v0.1.2:capability.json > "$t" && cmp "$t" .gsd/capabilities/changie/capability.json && git -C "$C" show v0.1.2:skills/changie-fragments/scripts/write-fragments.sh > "$t" && cmp "$t" .gsd/capabilities/changie/skills/changie-fragments/scripts/write-fragments.sh && node -e 'const e=require(process.argv[1]).entries.changie;process.exit(e.version==="0.1.2"&&e.source==="https://github.com/seanb4t/gsd-capability-changie.git#v0.1.2"?0:1)' "$PWD/.gsd-capabilities.json" && n=$(gsd-tools loop render-hooks verify:post --raw | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log((j.activeHooks||[]).filter(h=>h.capId==="changie").length)') && test "$n" = 1 && test "$(gsd-tools query config-get workflow.changie_command --raw)" = "task changie --"</automated>
    <fails_when>non-zero exit: the project bundle or its script differs from the tag, the ledger is not 0.1.2 from $SPEC, the changie step count is not 1, or the changie command changed</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && o=$(bash .gsd/capabilities/changie/skills/changie-fragments/scripts/write-fragments.sh --phase 2 --pr "$(printf '12\nDANGEROUS')" --list 2>&1); s=$?; test "$s" = 2 && printf '%s\n' "$o" | rg -q -F 'changie-fragments: error: bad-pr:' && ! printf '%s\n' "$o" | rg -q '^changie-fragments: pr ' && test -z "$(git status --porcelain --untracked-files=all -- . ':(exclude).planning')"</automated>
    <fails_when>non-zero exit: the installed script does not refuse a multi-line --pr with exit 2 and error: bad-pr, it printed a pr line, or the tree changed outside .planning/</fails_when>
  </verify>
  <acceptance_criteria>
    - The RED transcript pasted in the SUMMARY shows 19 `ok [` lines, then `::error::run.sh: [bad-pr:multiline] expected exit 2, got 0`. It was taken at the `test(quick-260926-it7)` commit, before the `fix(quick-260926-it7)` commit existed.
    - The clean-clone transcript ends `run.sh: 33 of 33 legs passed against a scratch project`.
    - The project install's before record reads 0.1.1 from `#v0.1.1` and its after record reads 0.1.2 from `$SPEC`. The live `--list` result matches PR #88's state observed at that moment.
  </acceptance_criteria>
  <done>
- v0.1.2 fixes WR-01 with a RED-first leg and is published privately as `main`'s annotated tip. v0.1.0 and v0.1.1 are untouched, and a clean HTTPS clone passes 33 of 33.
- codegraph-go's project install runs v0.1.2, installed with gsd-core's verb, and refuses a multi-line `--pr` live.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit the CR-01 guard. check:changie leg 13 drives the real task changie -- path with a hostile body, IN-01 renumbers the legs, and RED is proven by mutation</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml lines 538-603 (`changie:` task, check:changie `desc:` and setup) and 830-879 (leg 12, the snapshot, the pass-count guard and the closing line)
    - .planning/phases/02-phase-close-fragment-capability/02-REVIEW.md CR-01 and the Orchestrator Verification detail 1
  </read_first>
  <behavior>
    - Leg 13 on HEAD: the real `task changie --` prints `body: <payload>` byte-literal, exits 0 and never creates the marker, so `check:changie` reaches 13 of 13.
    - Leg 13 against the unquoted-splice mutation of `changie:`: the marker is created and the check fails at `[13/13]`.
    - The guarded property already holds in production (CR-01 was not reproduced), so the RED gate is the mutation (rule 84d1gfpywd), not a pre-change failure.
  </behavior>
  <action>
1. Add leg 13 to `check:changie` (02-REVIEW CR-01 residual). Place it after leg 12's `passed=$((passed + 1))` and before the `after=` snapshot line.
   - Add the comment `# Leg 13/13: the real task changie -- path passes a hostile body byte-literal (02-REVIEW CR-01)`.
   - Set `marker="${scratch}/leg13.marker"`.
   - Build `payload` from single-quoted literal segments joined to the double-quoted `"${marker}"`, so the check's own shell never expands anything. In order, it is:
     - the text `check:changie injection probe `;
     - a `$(touch <marker>)` command substitution;
     - a space and the same touch command wrapped in backticks;
     - the text ` ; touch <marker>;`.
     The final `;` matters. Under the mutation, the trailing `-m PR=1` then runs as its own failing command, instead of making `touch` create stray files named `-m` and `PR=1` in the worktree. This exact payload was probed at planning time and prints as a plain YAML scalar.
   - Under `set +e`, run `(cd "${work}" && CI=true "{{.TASK_EXE}}" --taskfile "{{.ROOT_TASKFILE}}" changie -- new --dry-run -k Fixes -b "${payload}" -m PR=1) >"${scratch}/leg13.out" 2>"${scratch}/leg13.err"` and capture the status.
     - `TASK_EXE` is the go-task binary running this check, and `ROOT_TASKFILE` is the Taskfile under test, so a worktree run tests the worktree's own `changie:` task.
     - The invocation starts from the scratch copy. go-task runs `changie:` in the Taskfile's own directory; `-d <scratch>` cannot work because of the relative `-modfile` (see `<interfaces>`). So changie reads the repository's `.changie.yaml`.
     - `--dry-run` writes nothing, and the existing before/after cksum snapshot proves `.changie.yaml`, `.changes/` and `CHANGELOG.md` byte-unchanged.
   - Assert in this order, each failing with `exit 1`:
     1. If `${marker}` exists: `::error::check:changie: [13/13] task changie -- re-shelled the fragment body: the injected command created ${marker}`.
     2. If the status is non-zero: `::error::check:changie: [13/13] task changie -- new --dry-run exited ${status}: $(cat "${scratch}/leg13.err")`.
     3. If `grep -qxF -- "body: ${payload}" "${scratch}/leg13.out"` fails: `::error::check:changie: [13/13] task changie -- new --dry-run did not print the body byte-literal`, then `cat` the output file.
   - Then echo `check:changie: [13/13] task changie -- passed a body carrying command substitution, backticks and ; byte-literal, and nothing executed — ok` and increment `passed`.

2. Renumber for the new total (IN-01).
   - Every `[N/…]` echo and error string in `check:changie` becomes `[N/13]`.
   - Every `# Leg N/…:` comment becomes `# Leg N/13:`, including leg 12's.
   - The pass-count guard becomes `[ "${passed}" -ne 13 ]`, and its error text and the closing success line say 13 of 13.
   - In `desc:`:
     - the spelled-out leg count becomes `thirteen`;
     - `an executed-leg counter must reach` takes 13;
     - add one sentence: leg 13 drives the real `task changie --` path (the configured `workflow.changie_command`) with a body carrying command substitution, backticks and `;`, and proves that the body arrives byte-literal and that nothing executes (02-REVIEW CR-01).
     Do not put a double-brace template sequence in `desc:`.
   - Do not change the `changie:` task, legs 1-12's logic, `.github/workflows/ci.yml` or any Go test.

3. Run GREEN on the main tree. `task check:changie` must end `check:changie: 13 of 13 checks passed against a scratch copy (source tree byte-unchanged)`, and `GOTOOLCHAIN=go1.26.6 GOWORK=off go test ./internal/upgrade/ -count=1` must pass. Commit `Taskfile.yml` alone as `test(quick-260926-it7): prove task changie -- passes a hostile body literally and renumber check:changie legs (CR-01, IN-01)`.

4. RED by mutation (rule 84d1gfpywd, D-09), in a disposable detached worktree. Never mutate the main tree.
   1. Capture `git -C "$CG" worktree list --porcelain` and `git -C "$CG" status --porcelain --untracked-files=all` before starting. A pre-existing, unrelated worktree `worktree-brave-river-a240` belongs to another session; never touch it.
   2. Set `WT="$(mktemp -d)/wt"` and run `git -C "$CG" worktree add --detach "$WT" HEAD`.
   3. Pre-mutation gate: `(cd "$WT" && task check:changie)` ends 13 of 13.
   4. Mutate only `$WT/Taskfile.yml`'s `changie:` cmd line. Replace its trailing ` {{.CLI_ARGS}}` with `{{range .CLI_ARGS_LIST}} {{.}}{{end}}`, which splices each forwarded argument into the shell unquoted. This is the re-shelling CR-01 feared, and a planning-time probe confirmed that this form executes an embedded `$(touch …)`.
   5. Confirm the mutation is applied:
      - `git -C "$WT" diff --numstat` prints exactly one line, `1`, `1`, `Taskfile.yml`;
      - the mutated line is present once;
      - the original `changie {{.CLI_ARGS}}` line is absent from `$WT/Taskfile.yml`.
   6. RED: `(cd "$WT" && task check:changie)` exits non-zero. Legs 1-12 print `— ok`, and the failure line starts with `::error::check:changie: [13/13] task changie -- re-shelled the fragment body`. Save the transcript and the exit status.
   7. Revert with `git -C "$WT" checkout -- Taskfile.yml`. Then confirm:
      - `git -C "$WT" status --porcelain --untracked-files=all` is empty;
      - `$WT/Taskfile.yml` `cmp`s equal to `git -C "$CG" show HEAD:Taskfile.yml`.
   8. GREEN re-run in `$WT`: 13 of 13.
   9. Run `git -C "$CG" worktree remove "$WT"` without `--force`. If it refuses, STOP and report. Then run `git -C "$CG" worktree prune`.
   10. The worktree listing and the main-tree porcelain must be byte-equal to the step 1 captures.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task check:changie 2>&1 | tail -1 | rg -x -F 'check:changie: 13 of 13 checks passed against a scratch copy (source tree byte-unchanged)'</automated>
    <fails_when>non-zero exit: check:changie does not reach 13 of 13 on the committed Taskfile</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(rg -o '# Leg [0-9]+/13:' Taskfile.yml | wc -l | tr -d ' ')" = 13 && test "$(rg -o '# Leg [0-9]+/1[12]:' Taskfile.yml | wc -l | tr -d ' ')" = 0 && test "$(rg -o '\[[0-9]+/12\]' Taskfile.yml | wc -l | tr -d ' ')" = 0 && test "$(rg -o '\[13/13\]' Taskfile.yml | wc -l | tr -d ' ')" -ge 4 && test "$(rg -F -o 'twelve' Taskfile.yml | wc -l | tr -d ' ')" = 0 && rg -q -F 'thirteen' Taskfile.yml && rg -q -F 'must reach 13' Taskfile.yml && rg -q -F -- '"{{.TASK_EXE}}" --taskfile "{{.ROOT_TASKFILE}}" changie -- new --dry-run -k Fixes -b "${payload}" -m PR=1' Taskfile.yml && test "$(rg -x -F '      - "{{.GO_TOOL_CHANGIE}} changie {{.CLI_ARGS}}"' Taskfile.yml | wc -l | tr -d ' ')" = 1</automated>
    <fails_when>non-zero exit: a stale leg comment or [N/12] string remains, leg 13's strings or invocation are missing, the desc still carries the old count, or the production changie: cmd line is not byte-identical to its original</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 GOWORK=off go test ./internal/upgrade/ -count=1 && m=$(git log -1 --format=%s -- Taskfile.yml) && test "$(printf '%s\n' "$m" | rg -F -o '(quick-260926-it7)' | wc -l | tr -d ' ')" = 1 && h=$(git log -1 --format=%H -- Taskfile.yml) && fl=$(git show --name-only --format= "$h") && test "$fl" = Taskfile.yml</automated>
    <fails_when>non-zero exit: an internal/upgrade guard fails, or the Taskfile commit is missing or carries more than Taskfile.yml</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY pastes, in order:
      - the pre-mutation GREEN (13 of 13 in `$WT`);
      - the numstat and presence/absence proof that the mutation was applied;
      - the RED transcript, with a non-zero exit, legs 1-12 `— ok`, and the `[13/13] task changie -- re-shelled the fragment body` line;
      - the byte-clean revert proof (empty porcelain, `cmp` equal to HEAD);
      - the GREEN re-run (13 of 13);
      - the before/after worktree listing and main-tree porcelain comparison, both equal.
    - `git -C /Volumes/Code/github.com/seanb4t/codegraph-go worktree list --porcelain` contains no path under the mutation's `mktemp -d` parent.
  </acceptance_criteria>
  <done>
- check:changie proves the configured `task changie --` path passes a hostile body byte-literally, and the guard is proven able to fail.
- The leg comments, strings, desc and pass count all say 13.
- The main tree was never mutated.
</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the global install through gsd-core verbs with a skills-directory guard, point CONTRIBUTING at v0.1.2, and record the resolutions in 02-REVIEW</name>
  <files>CONTRIBUTING.md, .planning/phases/02-phase-close-fragment-capability/02-REVIEW.md, ~/.gsd-capabilities.json and ~/.gsd/capabilities/changie/ and $SKILLS/gsd-changie-fragments/ (machine state, via gsd-core verbs only)</files>
  <read_first>
    - CONTRIBUTING.md lines 206-245 (`### Changelog fragments at phase close`)
    - .planning/phases/02-phase-close-fragment-capability/02-REVIEW.md (whole file; the appended note goes after the last line)
    - .planning/phases/02-phase-close-fragment-capability/02-06-SUMMARY.md `## Upgrade` (the global commands and guard shape)
  </read_first>
  <action>
1. Repoint the global install (D-14, following the D-13 part 4 pattern). Use gsd-core verbs only: never copy, link or write into `$SKILLS` or `~/.gsd` by hand, and never set GSD_HOME.
   1. Capture the before state in `B=$(mktemp -d)`:
      - the names listing: `ls -1A "$SKILLS" | LC_ALL=C sort`;
      - the symlink map: for every top-level symlink in `$SKILLS`, its name and its `readlink` target, sorted;
      - a content snapshot: `find -L "$SKILLS" -type f ! -path "$SKILLS/gsd-changie-fragments/*" -exec cksum {} + | LC_ALL=C sort`. `-L` is required because 67 top-level entries are symlinks. Print the snapshot's line count before any comparison, and require it to be above zero. The planning-time observation was 1933, but that is not a gate;
      - a copy of the current `$SKILLS/gsd-changie-fragments/SKILL.md`;
      - the global ledger's changie entry, expected to read 0.1.1 from `#v0.1.1`.
   2. Run `gsd-tools capability install "$SPEC" --scope global`. It must exit 0 at version 0.1.2.
   3. Run `gsd-tools capability set changie --enable --runtime claude --scope global`, with no `--config-dir`.
   4. Prove the result:
      - `comm -3` of the two names listings prints nothing;
      - the symlink maps are equal;
      - the content snapshots are byte-equal;
      - the materialized SKILL.md differs from the saved copy, and `cmp`s equal to `git -C "$CAP" show v0.1.2:skills/changie-fragments/SKILL.md`;
      - the `.gsd-capability-skill` marker contains `changie`;
      - `~/.gsd/capabilities/changie/capability.json` `cmp`s equal to the tag's copy;
      - the global ledger reads 0.1.2 with source `$SPEC`;
      - codegraph-go's render-hooks changie-step count is still 1.
   5. If anything in `$SKILLS` changed other than that one SKILL.md's content, STOP and report the full diff. Do not repair it.

2. CONTRIBUTING (D-12, CAP-04).
   - In `### Changelog fragments at phase close`, change the tag in both install specs, the per-clone line and the per-machine `--scope global` line, from `v0.1.1` to `v0.1.2`. These are value edits only; change no other text, heading or code fence.
   - Run `GOTOOLCHAIN=go1.26.6 GOWORK=off go test ./internal/upgrade/ -run TestContributingReferencesRealTaskTargets -count=1 -v`. It must print `--- PASS: TestContributingReferencesRealTaskTargets`.
   - Then run the whole `./internal/upgrade/` package with the same env; it must pass.
   - Commit `CONTRIBUTING.md` alone as `docs(quick-260926-it7): point CONTRIBUTING at capability v0.1.2 (CAP-04)`.

3. Append a resolution note to `02-REVIEW.md`.
   - This is append-only. The frontmatter (`status`, `findings`), the reviewer's text and the Orchestrator Verification section stay byte-unchanged, because they are the review's record.
   - After the file's last line, add a blank line and `## Resolution (quick 260926-it7, 2026-09-26)`, followed by short prose or a three-row table (finding, resolution, evidence):
     - **WR-01:** fixed in gsd-capability-changie `v0.1.2`. `--pr` and the looked-up PR are validated as whole strings. `[bad-pr:multiline]` went RED first, at the test commit, then GREEN at the fix commit (give both `$CAP` short SHAs). The suite passes 33 of 33 at HEAD and from a clean HTTPS clone. T-02-03's premise now holds.
     - **CR-01 residual:** closed as test coverage. Name codegraph-go's Taskfile commit SHA. check:changie leg 13 drives the real `task changie --` with a body carrying `$(…)`, backticks and `;`, and proves byte-literal arrival and no execution. The leg was proven RED against a confirmed-applied unquoted-splice mutation of the `changie:` task, reverted byte-cleanly.
     - **IN-01:** the leg comments are renumbered to `/13` with the new total.
   - Close with one line:
     - both installs are repointed to `v0.1.2` through gsd-core verbs;
     - CONTRIBUTING names `#v0.1.2`;
     - the full transcripts are in `.planning/quick/260926-it7-release-gsd-capability-changie-v0-1-2-fi/260926-it7-SUMMARY.md`.
   - Commit as `docs(quick-260926-it7): record WR-01, CR-01 and IN-01 resolutions in 02-REVIEW`.
  </action>
  <verify>
    <automated>S="${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills" && C=/Volumes/Code/github.com/seanb4t/gsd-capability-changie && t=$(mktemp) && git -C "$C" show v0.1.2:skills/changie-fragments/SKILL.md > "$t" && cmp "$t" "$S/gsd-changie-fragments/SKILL.md" && rg -x -F changie "$S/gsd-changie-fragments/.gsd-capability-skill" && git -C "$C" show v0.1.2:capability.json > "$t" && cmp "$t" "$HOME/.gsd/capabilities/changie/capability.json" && node -e 'const e=require(process.argv[1]).entries.changie;process.exit(e.version==="0.1.2"&&e.source==="https://github.com/seanb4t/gsd-capability-changie.git#v0.1.2"?0:1)' "$HOME/.gsd-capabilities.json"</automated>
    <fails_when>non-zero exit: the materialized skill or the global bundle is not v0.1.2 byte for byte, the marker does not name changie, or the global ledger is not 0.1.2 from $SPEC</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(rg -F -o 'gsd-capability-changie.git#v0.1.2' CONTRIBUTING.md | wc -l | tr -d ' ')" = 2 && test "$(rg -F -o 'gsd-capability-changie.git#v0.1.1' CONTRIBUTING.md | wc -l | tr -d ' ')" = 0 && GOTOOLCHAIN=go1.26.6 GOWORK=off go test ./internal/upgrade/ -run TestContributingReferencesRealTaskTargets -count=1 -v 2>&1 | rg -q -F -- '--- PASS: TestContributingReferencesRealTaskTargets' && GOTOOLCHAIN=go1.26.6 GOWORK=off go test ./internal/upgrade/ -count=1</automated>
    <fails_when>non-zero exit: CONTRIBUTING does not name v0.1.2 exactly twice, still names v0.1.1, or an internal/upgrade test fails</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && f=.planning/phases/02-phase-close-fragment-capability/02-REVIEW.md && o=$(mktemp) && a=$(mktemp) && git show a335232d:"$f" > "$o" && n=$(wc -c < "$o" | tr -d ' ') && head -c "$n" "$f" | cmp - "$o" && test "$(wc -c < "$f" | tr -d ' ')" -gt "$n" && tail -c +"$((n + 1))" "$f" > "$a" && test "$(rg -F -o '## Resolution (quick 260926-it7, 2026-09-26)' "$a" | wc -l | tr -d ' ')" = 1 && rg -q -F 'WR-01' "$a" && rg -q -F 'CR-01' "$a" && rg -q -F 'IN-01' "$a" && rg -q -F 'v0.1.2' "$a" && rg -q -F '260926-it7-SUMMARY.md' "$a"</automated>
    <fails_when>non-zero exit: the review's existing bytes changed (not append-only), nothing was appended, or the note omits a finding, the tag, or the evidence pointer</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && n=$(gsd-tools loop render-hooks verify:post --raw | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log((j.activeHooks||[]).filter(h=>h.capId==="changie").length)') && test "$n" = 1 && ps=$(git status --porcelain --untracked-files=all -- . ':(exclude).planning/quick') && test -z "$ps" && sl=$(git log --format=%s a335232d..HEAD) && test "$(printf '%s\n' "$sl" | rg -F -o '[ci skip]' | wc -l | tr -d ' ')" = 0</automated>
    <fails_when>non-zero exit: the changie step count is not 1, a tracked or unignored change is left outside .planning/quick/, or a codegraph-go commit carries [ci skip]</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records all of the following for the global surface pass:
      - the snapshot line count, above zero;
      - an empty `comm -3`;
      - equal symlink maps;
      - equal content snapshots;
      - both `cmp` results;
      - the marker;
      - the global ledger before (0.1.1, `#v0.1.1`) and after (0.1.2, `$SPEC`).
    - `git log --format=%s a335232d..HEAD` in codegraph-go contains the `test(quick-260926-it7)`, `docs(quick-260926-it7): point CONTRIBUTING…` and `docs(quick-260926-it7): record WR-01…` commits.
  </acceptance_criteria>
  <done>
- The maintainer's global install runs v0.1.2, re-materialized by gsd-core, with nothing else in the skills directory changed.
- CONTRIBUTING tells every clone and machine to install `#v0.1.2`.
- 02-REVIEW.md carries an append-only resolution note for all three findings.
</done>
</task>

</tasks>

<source_audit>
| Source | Item | Plan location | Status |
|---|---|---|---|
| GOAL 1 | WR-01: whole-string `--pr` check, bash-3.2-safe | Task 1 step 2 (`is_positive_int`, `case` whole-string) | COVERED |
| GOAL 1 | The same discipline on the looked-up PR (~line 419) | Task 1 step 2 | COVERED |
| GOAL 1 | RED-first `[bad-pr:multiline]`, 32 → 33 legs, all green, shellcheck clean | Task 1 steps 1-2, verify 1 | COVERED |
| GOAL 2 | CR-01 residual: check:changie leg runs the real `task changie -- new --dry-run -k Fixes -b '<payload>' -m PR=1` from the scratch copy, body byte-literal, no marker | Task 2 step 1 | COVERED. The invocation starts from the scratch copy; go-task runs the task in the Taskfile's own directory (observed), and `--dry-run` plus the snapshot keep the tree unchanged |
| GOAL 2 | 12 → 13 legs, desc and count strings updated | Task 2 step 2 | COVERED |
| GOAL 2 | RED against a confirmed-applied mutation, byte-clean revert (84d1gfpywd) | Task 2 step 4 | COVERED |
| GOAL 3 | IN-01: `# Leg N/11` comments renumbered to the new total | Task 2 step 2, verify 2 | COVERED |
| GOAL 4 | Fast-forward push of `main` and annotated `v0.1.2`, HTTPS, `GIT_TERMINAL_PROMPT=0`, pre-approved | Task 1 step 4 | COVERED |
| GOAL 4 | Never re-point v0.1.0/v0.1.1; never change visibility | Task 1 steps 4-5, prohibitions, verify 3 | COVERED |
| GOAL 4 | Clean HTTPS clone proof, 33/33; `main` tip = `v0.1.2`; README `#v0.1.2` | Task 1 steps 3 and 5, verify 2-3 | COVERED |
| GOAL 5 | Re-run `capability install … --scope project` and `--scope global`, plus `capability set … --runtime claude` | Task 1 step 6, Task 3 step 1 | COVERED |
| GOAL 5 | Byte-compare project `capability.json` and `$SKILLS/gsd-changie-fragments/SKILL.md` with the tag; global-skills guard | Task 1 verify 4, Task 3 step 1, verify 1 | COVERED |
| GOAL 5 | CONTRIBUTING `#v0.1.2`; TestContributingReferencesRealTaskTargets green | Task 3 step 2, verify 2 | COVERED |
| Constraint | No `[ci skip]`; `-c commit.gpgsign=false` allowed; `GOTOOLCHAIN=go1.26.6` for `go test` | objective rules; Task 1 verify 2, Task 3 verify 4; every `go test` | COVERED |
| Constraint | Resolution note for WR-01, CR-01 and IN-01 appended to 02-REVIEW.md | Task 3 step 3, verify 3 | COVERED |
| CONTEXT D-09 | Evidence pasted into codegraph-go artifacts; RED by a mutation that is confirmed applied and reverted | Tasks 1-2, `<output>` | COVERED |
| CONTEXT D-10 | Annotated tags, private repository, tag is costly | Task 1 steps 4-5, reversibility | COVERED |
| CONTEXT D-11 amendment | HTTPS-only install spec | Task 1 steps 3-4 | COVERED |
| CONTEXT D-12 | CONTRIBUTING subsection names the install | Task 3 step 2 | COVERED |
| CONTEXT D-13 | Publish and upgrade pattern: pre-approved push, clean-clone proof, gsd-core verbs | Task 1 steps 4-6, Task 3 step 1 | COVERED |
| CONTEXT D-14 | Global install materializes `/gsd-changie-fragments` | Task 3 step 1 | COVERED |
| REQ | CAP-03 (script PR validation), CAP-04 (install at tag, CONTRIBUTING), CHG-04 (check:changie guard) | frontmatter `requirements` | COVERED |

Excluded (not gaps):
- The historical `v0.1.1` mentions in PROJECT.md. They record what Phase 2 shipped, and PROJECT evolution happens at phase transitions.
- `.github/workflows/ci.yml`'s stale step comment. It was not requested, and `changie_shape_test.go` pins that step.
- Extending the capability's own `[argv-literal]` leg to drive the real `task`. The maintainer scoped the CR-01 residual to codegraph-go's `check:changie`.
- Filing the depth-1-clone gap upstream. It needs separate authorization and is already recorded in STATE.md.
- Rewording `bad_pr`'s echo of the raw value. It is accepted as T-it7-07.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → `--pr` argument | An operator-typed `--pr` value crosses into `-m PR=<n>` of `changie new` |
| SUMMARY text → `task changie --` → go-task shell | A model-chosen, SUMMARY-derived body crosses `write-fragments.sh`'s argv, then go-task's `CLI_ARGS` rendering into its embedded shell |
| capability checkout → private GitHub repository | A push of `main` and an annotated tag over HTTPS, using gh's credential helper |
| private tag → project and global installs | The bundle and SKILL.md are installed verbatim, and the global surface pass rewrites the skills directory |
| mutation worktree → main tree | A deliberately broken Taskfile runs next to the real checkout |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-it7-01 | Tampering | `write-fragments.sh` `--pr` validation (WR-01) | medium | mitigate | `is_positive_int` uses one `case` whole-string match with an enumerated digit set, so an embedded or trailing newline, a sign, a leading zero or any non-digit exits 2 with `bad-pr` before anything runs. `[bad-pr:multiline]` went RED first and is GREEN at v0.1.2. It is re-proven live against codegraph-go's installed bundle (Task 1 verify 5). |
| T-it7-02 | Tampering / Spoofing | looked-up PR number | low | mitigate | The same helper replaces the per-line check on the gh lookup, which is already whitespace-stripped. The fixed `pr-lookup-invalid` note never echoes gh output. `[note:pr-lookup-invalid]` (0, 12abc) stays green. |
| T-it7-03 | Elevation of Privilege / Tampering | SUMMARY-derived body through `task changie --` (CR-01) | high | mitigate | go-task 3.52.0 shell-quotes `{{.CLI_ARGS}}` (observed). Leg 13 now commits that property: the real `task changie --` gets a body with `$(…)`, backticks and `;`, the body must come back as the exact `body:` line, and the marker must not exist. The unquoted-splice mutation proves the leg fails if the quoting is ever lost. CI runs `task check:changie` on every push. |
| T-it7-04 | Tampering (supply chain) | v0.1.2 source for both installs | high | mitigate | The tag is annotated on the tested `main` tip and pushed fast-forward only (never `--force`, `--tags`, `--delete` or `--mirror`). ls-remote verifies v0.1.0 and v0.1.1 unchanged. A clean HTTPS clone passes 33 of 33. Both installed `capability.json` files, the project's bundled script and the materialized SKILL.md are `cmp`-equal to `git show v0.1.2:…`. The manifest declares no hooks. No npm, pip or cargo install occurs, so the package-legitimacy gate does not apply. |
| T-it7-05 | Elevation of Privilege / Tampering | global surface pass over `$SKILLS` | high | mitigate | gsd-core verbs only; never a hand copy or a GSD_HOME redirect. The names listing, the top-level symlink map and a `find -L` checksum snapshot of every file outside `gsd-changie-fragments/` must be byte-equal, and the one changed file must equal v0.1.2's SKILL.md. Any other change is a stop, not a repair. |
| T-it7-06 | Information Disclosure | HTTPS credentials and visibility during push and clone | medium | mitigate | `GIT_TERMINAL_PROMPT=0` on every network call. The URLs carry no embedded credential. Transcripts hold only URLs and SHAs. `gh repo edit` is never run, and visibility is verified `PRIVATE` after the push. |
| T-it7-07 | Spoofing (log) | `bad_pr` echoing a raw multi-line `--pr` to stderr | low | accept | The value is operator-typed, goes to stderr only, and the script exits 2 before any work. An extra echoed line cannot reach a fragment, a commit or stdout parsing. Accepted, with no change beyond WR-01's scope. |
| T-it7-08 | Tampering | mutation run leaking into the main tree | low | mitigate | Mutation happens only in a detached `git worktree add --detach` under `mktemp -d`. The trailing `;` in the payload stops stray files. The revert is proven with empty porcelain and a `cmp` against HEAD. `worktree remove` runs without `--force`. The worktree listing and main-tree porcelain are compared byte-equal before and after, and the other session's worktree is never touched. |
</threat_model>

<verification>
- `$CAP`:
  - `CHANGIE_BIN=$CHB bash test/run.sh` passes 33 of 33 at HEAD (= `v0.1.2`) and from a clean HTTPS clone of `v0.1.2`;
  - shellcheck is clean on all four executables;
  - `git log --reverse v0.1.1..v0.1.2` is test, fix, docs under `quick-260926-it7`.
- Remote: v0.1.0 and v0.1.1 are unchanged, v0.1.2 is `main`'s annotated tip, and visibility is PRIVATE.
- codegraph-go:
  - `task check:changie` passes 13 of 13;
  - `GOTOOLCHAIN=go1.26.6 GOWORK=off go test ./internal/upgrade/ -count=1` passes;
  - `task changie -- merge --dry-run | cmp - CHANGELOG.md` exits 0 (Phase 1's byte reproduction is untouched).
- Installs:
  - project and global ledgers are at 0.1.2 from `$SPEC`;
  - bundles and the materialized SKILL.md are byte-identical to the tag;
  - render-hooks shows one changie step;
  - the skills directory is otherwise unchanged.
- 02-REVIEW.md is append-only and carries the resolution note. CONTRIBUTING names `#v0.1.2` twice.
</verification>

<success_criteria>
- WR-01 is fixed, RED-proven and released as v0.1.2, and the installed bundle in codegraph-go refuses a multi-line `--pr`.
- The CR-01 residual is closed: the configured `task changie --` path's argv-literal guarantee is guarded in CI by leg 13, and the guard is proven able to fail.
- IN-01 is fixed.
- Both installs and CONTRIBUTING are on v0.1.2. `main`'s tip equals v0.1.2, so the documented spec installs.
</success_criteria>

<output>
Create `.planning/quick/260926-it7-release-gsd-capability-changie-v0-1-2-fi/260926-it7-SUMMARY.md` when done. It must include:
- both commit trails: codegraph-go `a335232d..HEAD` and `$CAP` `v0.1.1..v0.1.2`, plus the v0.1.2 tag object SHA;
- a `## TDD Gate Compliance` section with the pasted `[bad-pr:multiline]` RED transcript (19 `ok [` lines, then the failure) and the GREEN `33 of 33` line (rule x1cjy9vyhq);
- a `## Mutation (CR-01 leg 13)` section: pre-mutation GREEN, applied proof, RED transcript, byte-clean revert, GREEN re-run, and the worktree and porcelain before/after (rule 84d1gfpywd);
- the publish proof: the visibility, the ls-remote lines and the clean-clone `33 of 33`;
- the project and global before/after records: the ledgers, `cmp` results, `comm -3` output, symlink-map and snapshot comparisons with the snapshot line count, the marker and render-hooks counts, and the live `--pr` refusal and `--list` PR line;
- an `## Upgrade` section giving the exact three commands used, and noting that `#v0.1.1` no longer installs now that `main` has moved.
</output>
