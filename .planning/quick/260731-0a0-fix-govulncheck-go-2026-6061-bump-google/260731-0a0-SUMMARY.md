---
phase: quick-260731-0a0
plan: 01
subsystem: infra
tags: [go-modules, grpc, govulncheck, supply-chain, dependency-bump]

# Dependency graph
requires:
  - phase: 09-release-please-and-goreleaser
    provides: "The red-CI triage on main@a1c298f that identified GO-2026-6061 as one of three pre-existing blocking-gate failures"
provides:
  - "google.golang.org/grpc raised from v1.82.0 to v1.82.1, closing GO-2026-6061 (GHSA-hrxh-6v49-42gf)"
  - "Proven RED->GREEN govulncheck evidence (before/after scan output) for the fix"
affects: [09-07, 09-08, ci.yml govulncheck gate]

actuals:
  tokens: 371
  tasks: 2
  commits: 1

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - go.mod
    - go.sum

key-decisions:
  - "Took google.golang.org/grpc@v1.82.1 (minimal patch fix), not v1.83.0 — smallest blast radius on a repo already red in two other places, per plan's decision record"
  - "Did not run go mod tidy / go mod tidy -e — go build ./... succeeded cleanly after go get, so the pre-existing tree-sitter-swift tidy workaround was not needed"

patterns-established: []

requirements-completed: [DIST-03]

coverage:
  - id: D1
    description: "google.golang.org/grpc bumped v1.82.0 -> v1.82.1 in go.mod, one-line diff, go.sum updated by go get"
    requirement: "DIST-03"
    verification:
      - kind: other
        ref: "rg '^\\tgoogle\\.golang\\.org/grpc v1\\.82\\.1 // indirect$' go.mod (exit 0); git diff --numstat HEAD~1 go.mod = 1 line added / 1 line removed"
        status: pass
    human_judgment: false
  - id: D2
    description: "RED baseline proven: govulncheck reports GO-2026-6061 before the bump (found v1.82.0, fixed v1.82.1, exit status 3)"
    requirement: "DIST-03"
    verification:
      - kind: other
        ref: "govulncheck ./... captured at /tmp/govulncheck-before.txt, contains GO-2026-6061 advisory block"
        status: pass
    human_judgment: false
  - id: D3
    description: "GREEN proven: fresh govulncheck run over the same package selector CI uses exits 0 with zero GO-2026-6061 occurrences"
    requirement: "DIST-03"
    verification:
      - kind: other
        ref: "govulncheck ./... captured at /tmp/govulncheck-after.txt, exit=0, 'No vulnerabilities found.', rg -q 'GO-2026-6061' returns no match"
        status: pass
    human_judgment: false
  - id: D4
    description: "CGo build and the only real grpc call-path test both pass post-bump"
    requirement: "DIST-03"
    verification:
      - kind: other
        ref: "go build ./... (exit 0, CGo enabled/default); go test ./internal/upgrade/... (ok, 1.021s)"
        status: pass
    human_judgment: false

duration: 20min
completed: 2026-07-31
status: complete
---

# Quick Task 260731-0a0: Fix govulncheck GO-2026-6061 Summary

**Bumped `google.golang.org/grpc` from v1.82.0 to v1.82.1 (one-line go.mod change), closing advisory GO-2026-6061 and proving the closure with real before/after govulncheck scans.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-07-31T04:18:26Z
- **Tasks:** 2
- **Files modified:** 2 (go.mod, go.sum)

## Accomplishments
- Captured a genuine RED baseline: `govulncheck ./...` before the change reported GO-2026-6061 (found in `google.golang.org/grpc@v1.82.0`, fixed in `v1.82.1`), exit status 3
- Bumped the indirect `google.golang.org/grpc` requirement to v1.82.1 via `go get`, exactly a one-line `go.mod` diff plus matching `go.sum` hash lines — no collateral requirement changes
- Re-ran the identical scan post-bump: exit 0, "No vulnerabilities found.", zero occurrences of GO-2026-6061 in the captured output
- Confirmed `go build ./...` succeeds with CGo left enabled (the repo's tree-sitter grammar bindings), and `go test ./internal/upgrade/...` — the only real grpc call path (`internal/upgrade -> sigstore-go/pkg/verify -> rekor-tiles/v2/pkg/generated/protobuf -> grpc`) — passes

## Task Commits

Each task was committed atomically:

1. **Task 1: Capture the RED baseline, then bump grpc to v1.82.1** - `a0cfceb` (fix)
2. **Task 2: Re-run CI's govulncheck gate and the CGo build/test sanity checks** - no commit (verification only, no file changes, per plan)

**Plan metadata:** committed separately by orchestrator (docs commit, `commit_docs: true`)

## Files Created/Modified
- `go.mod` - `google.golang.org/grpc` indirect requirement raised v1.82.0 -> v1.82.1 (exactly one line changed)
- `go.sum` - two new hash lines for `google.golang.org/grpc@v1.82.1`, written by `go get`, not hand-edited

## Decisions Made
- Took v1.82.1, the minimal patch fix, over v1.83.0 — per the plan's decision record, this changes the fewest things on a repo already red in two other unrelated CI gates. No other `go.mod` requirement forces a floor above v1.82.1.
- `go mod tidy` / `go mod tidy -e` was NOT needed: `go build ./...` succeeded cleanly immediately after `go get`, with no go.mod/go.sum inconsistency. The pre-existing `tree-sitter-swift` tidy workaround (recorded in STATE.md Phase-6 notes) was not triggered by this change.

## Deviations from Plan

None - plan executed exactly as written. No unexpected collateral `go.mod` changes; the diff was exactly the one grpc version line plus its two go.sum hash lines, matching the plan's expected shape precisely.

## Govulncheck Evidence (RED -> GREEN)

**Before (`/tmp/govulncheck-before.txt`), exit status 3:**
```
Vulnerability #1: GO-2026-6061
  Module: google.golang.org/grpc
    Found in: google.golang.org/grpc@v1.82.0
    Fixed in: google.golang.org/grpc@v1.82.1
    Example traces found:
      #1: internal/parser/cgo/parser_cgo.go:178:16: cgo.CGoParser.Close calls sync.Once.Do, which eventually calls transport.NewHTTP2Client
      ...
Your code is affected by 1 vulnerability from 1 module.
```

**After (`/tmp/govulncheck-after.txt`), exit status 0:**
```
=== Symbol Results ===

No vulnerabilities found.

Your code is affected by 0 vulnerabilities.
This scan also found 1 vulnerability in packages you import and 1 vulnerability
in modules you require, but your code doesn't appear to call these
vulnerabilities.
```

The remaining "1 + 1" informational-only findings referenced in the after-scan's footer are **not GO-2026-6061** and were present at the same counts in the before-scan too (unrelated to this bump). Verified via `-show verbose`: `GO-2026-5841` (`github.com/klauspost/compress/s2`, unmaintained/OOB-read advisory, not called by our code) and `GO-2026-5932` (`golang.org/x/crypto/openpgp`, unmaintained package advisory, not called by our code). Both are pre-existing, out of scope for this task, and untouched.

## Out-of-Scope Items — Explicitly Still Open

This task closed exactly one of the three pre-existing red-CI failures blocking phase 09-07's live release proof. The other two remain **OPEN and were NOT touched**:

1. **9 Linux-only daemon-lock test failures** (pass on macOS CI, fail on Linux CI) — Phase-8 gap #5, untouched.
2. **Perf-gate throughput regression at -11.2% against a 10.0% budget** — `tools/bench/baseline.json` is on the forbidden-files list and was not modified.

No workflow file, `.goreleaser.yaml`, or release-please config/manifest was touched. `git diff --name-only HEAD~1` contains only `go.mod` and `go.sum`.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- One of three red-CI gates on `main@a1c298f` is now closed (govulncheck DIST-03 blocking job). The other two (daemon-lock tests, perf-gate regression) still block 09-07's live release proof and 09-08's real cut — see STATE.md's `stopped_at` note for the maintainer's fix-first decision.
- This change is a straightforward `git revert` if needed; no API surface or release artifact is affected.

---
*Phase: quick-260731-0a0*
*Completed: 2026-07-31*

## Self-Check: PASSED

- FOUND: go.mod
- FOUND: .planning/quick/260731-0a0-fix-govulncheck-go-2026-6061-bump-google/260731-0a0-SUMMARY.md
- FOUND: commit a0cfceb
