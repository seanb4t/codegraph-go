# CodeGraph Go

## What This Is

A ground-up Go rewrite of [CodeGraph](https://github.com/colbymchenry/codegraph) — the pre-indexed code knowledge graph for coding agents (Claude Code, Cursor, Codex, Gemini, etc.). One static Go binary replaces the TypeScript version's bundled-Node distribution, delivering the same agent-facing experience (CLI, MCP server, auto-sync) with better performance, a verifiable supply chain, and an architecture designed to grow into team-scale usage.

## Core Value

An agent user can uninstall TypeScript CodeGraph, install the Go binary, migrate their indexes, and everything works the same or better — faster, from a single verifiably-built binary.

## Current State

**v0.3.0 — MCP Protocol Currency: SHIPPED 2026-08-06, RELEASED 2026-08-07.** All 5 phases independently verified, 25/25 requirements, 21 plans over 136 commits in 4 days (174 files, +32,372/−1,901). The stdio MCP server is current with spec revision `2026-07-28` on `modelcontextprotocol/go-sdk@v1.7.0`; `mark3labs/mcp-go` is gone; Legacy clients are unbroken, asserted on the wire (a `2024-11-05` client completes a session and calls a tool). The verification oracle grew 23 → 28 frozen transcripts and still never imports the SDK it tests. The `v0.3.0` tag and GitHub Release were cut by release-please with **no human running `git tag`** (D-06R held end-to-end), and were **verified locally rather than inferred from a green check**: sha256 matched the manifest, `slsa-verifier verify-artifact` passed at builder `generator_generic_slsa3.yml@v2.1.0`, and `cosign verify-blob` returned Verified OK against the GitHub OIDC issuer. **The v1.0 residual darwin release-path check is now closed by measurement** — both darwin arches built, signed, and SBOM'd on the real macOS runner, outside the canary.

**`v0.4.0` released since.** `#29` (`feat(platform)!: drop native Windows support — WSL2 only`) drove the bump. Release targets are now **4**, not 6: linux/{amd64,arm64} + darwin/{amd64,arm64}.

**v0.5.0 — macOS Distribution & Homebrew: SHIPPED 2026-08-11, RELEASED as `v0.9.0` 2026-08-12.** All 4 phases independently verified, 18/18 requirements, 24 plans in 3 days (76 source files, +11,066/−1,054), six releases cut across the milestone (`v0.5.0` … `v0.9.0`). codegraph is now *installable by convention* on macOS: `spctl -a -vv -t exec` returns **accepted** on both darwin arches against a genuinely quarantined download, and `brew tap seanb4t/tap && brew install codegraph` works on a clean machine.

The milestone's single unproven claim — whether `zig cc` cross-compiles this repo's CGo tree-sitter dependency to linux/amd64 **and** linux/arm64 from a macOS host, with the results *running on real Linux* — was spiked first and blocking, and **passed on variation V1 at first dispatch** (canary run 31273571889; `files=430 symbols=4281` on both non-emulated arches). The costed GoReleaser Pro fallback was never bought, so `check:goreleaser`/DIST-01, `TestGoreleaserPinParity` and `tool-vuln`/VULN-01-03 all stayed OSS-visible. The release pipeline is now one `goreleaser release` invocation owning archives, checksums, notarization, signing and SBOMs, with the hand-rolled `sha256sum` writer deleted in the same change that made `checksum:` live.

`internal/upgrade/brew.go` detects a Homebrew-managed install **structurally** — resolved symlinks, Caskroom/Cellar shape, and Homebrew's own `INSTALL_RECEIPT.json` — so it is correct at any prefix rather than only `/opt/homebrew`, and keys on a signal Homebrew authors rather than one this project's own pipeline writes and could omit. `Run()` refuses before any network call; `--check` steps aside; `--force` is powerless *by construction* (`Options.Force` is never read — its absence is the enforcement, not a runtime check). Zero `exec.LookPath`/`exec.Command` on `brew` in the production paths.

**The Phase-4 residual is now closed, by measurement rather than by argument.** `04-EVIDENCE.md`'s `leg3_executed=no` — "a released binary carrying this code, installed through the fully natural brew path, is not yet proved" — was closed on 2026-08-12: `brew list --cask --versions` → `codegraph 0.9.0`, realpath `/opt/homebrew/Caskroom/codegraph/0.9.0/codegraph`, Homebrew-authored receipt reading `{"source":"seanb4t/tap","version":"0.9.0"}`, and the released binary (`version --json` → commit `f19e0e0`) refusing with exit 1 and pointing at `brew upgrade codegraph`. `post-release-verify` passed 7/7 against re-downloaded published assets, including byte-identical self-upgrade from v0.8.0 on darwin/arm64 and linux/amd64.

**Next milestone: unscoped.** Run `/gsd-new-milestone` to define it. Still parked in `ROADMAP.md` → Backlog: 999.2 (tmux TTY harness) and 999.4 (`CheckRegression` positivity guard) — plus the deferred Team Scale work, SEED-001 (Svelte web UI), SEED-003 (markdown in the index), and the v0.5.x deferrals DIST-06 (stapled offline-safe container) and BREW-07 (homebrew-core submission).

**Accepted limitations carried out of v0.3.0, not closed:** the daemon extreme-load timeout tail and its feedback-latency tradeoff (CI load ruled the governing standard for MAINT-02), and `GO-2026-5932` — a real, unmitigated vulnerability reachable in `goreleaser`'s binary through cosign/rekor's unmaintained openpgp, surfaced by the advisory scan rather than resolved. A latent flake is also open: `TestFrozenTranscriptsMatch/toolslist-repeat` freezes JSON-RPC response *arrival* order, which the protocol does not guarantee and go-sdk's async dispatch does not provide (tracked in `todos/pending/`).

**v1.0 — Drop-in Parity & Human UX: SHIPPED 2026-08-03.** All 10 phases complete and independently verified (`verification_status: passed` on every one); 48/48 requirements satisfied. codegraph-go is now a true drop-in replacement for TS CodeGraph v1.3.x: behavioral parity on `explore`/`node`/`status`, watcher-on-MCP by default, git/worktree awareness, output hygiene, a human-facing Charm TUI behind a build-enforced rendering seam, systematic flag reconciliation, fully automated signed releases via release-please + GoReleaser, and contributor-facing local build tooling.

The artifact line at v1.0's close was `v0.2.0` (signed, SBOM'd, SLSA-attested, cut entirely by release-please); it has since advanced to `v0.3.0` and `v0.4.0`. **There is deliberately no `v1.0.0` git tag** — see the amendment below. "v1.0" is a planning-milestone name only, and carries **no git tag at all**: since Phase 9, release-please is the sole tag authority (D-06R), so the milestone record lives in `MILESTONES.md` and `milestones/v1.0-*` rather than in a hand-created tag.

**Milestone-label convention changed at the v1.0 → v0.3.0 boundary.** v0.1 and v1.0 were planning labels decoupled from the shipped artifact line, which shipped `v0.2.0` under a milestone called "v1.0" and required a standing paragraph to explain. From v0.3.0 onward the milestone label tracks the **actual release line** (maintainer directive, 2026-08-03). This is still a planning label carrying **no git tag** — release-please remains the sole tag authority (D-06R) and computes the real version from Conventional Commits, so the label is a prediction: it holds if the milestone lands `feat:` commits, and a fixes-only outcome would cut `v0.2.1` instead.

<details>
<summary>v1.0 milestone goal as originally stated</summary>

**Goal:** Close the behavioral and surface gaps between codegraph-go and TS CodeGraph v1.3.x so an existing user can swap binaries with zero change in experience, then cut the first real signed release — parity closed and the caveat v0.1 shipped with retired.

> **Amended 2026-07-29 (maintainer directive), status updated 2026-08-01.** This goal previously read "cut the first real signed `v1.0.0` release … the signed `v1.0.0` tag is **not yet cut**". The maintainer rejected forcing a version: *"We are **not** going to jump to 1.0 … We'll follow things as release-please and conventional commits requires."* REL-02 was recategorized from an event into a property and names no version, so the `v1.0.0` target was removed rather than deferred (D-06R supersedes D-06). **REL-02 is SATISFIED as of 2026-08-01**: Phase 9 shipped `v0.2.0`, computed by release-please from Conventional Commits with no human `git tag`, signed + SBOM'd + SLSA-attested, and proven upgradeable from a genuinely shipped prior binary on two platforms. There is still no `v1.0.0` tag, and by this directive that is correct, not outstanding. "v1.0" remains a planning-milestone name, never a git tag.

**Target features:**
- **Agent-output behavioral parity** — `explore` semantic-relevance selection (+ `⚠️ no covering tests` warnings), `node` multi-definition disambiguation, multi-word `<query...>` arity, and richer `status` content (DB size, nodes-by-kind, files-by-language). v0.1's golden test proved template shape but never the selection/relevance algorithms.
- **Watcher-on-MCP by default** — `serve --mcp` runs the fsnotify watcher automatically with a `--no-watch` opt-out, matching TS's live auto-sync (our `install` already writes the byte-identical `serve --mcp` invocation; only the watch default differs).
- **Git/worktree awareness** — borrowed-index detection (compute the currently-inert `worktreeMismatch`: a `status` warning + a compact inline MCP-result notice) plus opt-in git sync hooks (post-commit/merge/checkout). Directly fixes the silent "worktree queries the main branch's graph" correctness gap.
- **Output hygiene** — silence Pebble WAL log noise on stderr; TTY-gate all styling.
- **Human-facing TUI (Charm/bubbletea)** — lipgloss-styled `status`/`files` (plain when piped), an interactive `daemon` picker, `install`/`uninstall` multi-select, and `init`/`index`/`sync` progress. The agent/MCP output path stays plain, stable, parseable text.
- **Surface reconciliation** — systematic per-command flag parity; decide the `search` stance; keep `serve`/`migrate` as documented (`serve` is not a divergence — TS has it too).
- **Behavioral parity test harness** — fixtures for ambiguous names, multi-word queries, and relevance, closing the v0.1 golden-test blind spot.
- **First signed `v1.0.0` release** — closes v0.1's still-pending DIST-02 (real `v*` tag) and PERF-01 (published numbers); audits the new Charm deps via govulncheck/SBOM.

</details>

## Current Milestone: v0.10.0 Agent Onboarding Skill & MCP Resources

**Goal:** Give agent harnesses (Claude Code, Cursor, Codex, Gemini, etc.) a thin, high-signal skill/plugin that teaches WHEN and HOW to use codegraph's tools — leading with a decision procedure, not a tool catalog — while pushing detailed reference content out of the skill and into resources served by the MCP server itself, backed by session/prompt hooks that nudge and guard toward codegraph tools over grep/find.

**Target features:**
- A SKILL.md (and/or Claude Code plugin) authored per current skill-authoring best practices (researched fresh, not memory) — decision-procedure-first ("which tool for which question"), 2-3 worked examples, minimal tool catalog
- MCP server gains a Resources capability (`resources/list` + `resources/read` via `modelcontextprotocol/go-sdk`) serving detailed reference content (tool-by-tool docs, `CODEGRAPH_MCP_TOOLS` semantics, index-state preconditions) that the skill points to instead of embedding
- Rewritten `instructions` wire-string and `codegraph install` marker block, correctly deferring to the skill + resources instead of the stale "Phase 3" promise `internal/agents/instructions.go` currently makes
- SessionStart hook injecting a short codegraph-availability nudge, plus a PreToolUse/UserPromptSubmit guard that redirects grep/find/Read on where-is-X questions toward `codegraph_explore` in `.codegraph/`-indexed repos — shipped as part of the plugin/skill package (hooks.json + scripts), installed by `codegraph install`
- Distribution decision: does `codegraph install` write the skill/hooks into the target agent's skills directory (versioned with the binary, updated by `codegraph upgrade`), or ship in-repo for manual install?
- Guard the claims: any tool counts/defaults/flags named in the skill, resources, or hooks must be derived or gated by a test, never hand-typed prose nothing verifies

Builds directly on the pending todo `2026-08-08-author-a-codegraph-usage-skill-for-agents.md`, which records the incident (a stale `instructions` string that actively misdirected a debug session) that motivated this milestone.

## Shipped Milestone: v0.5.0 macOS Distribution & Homebrew

**Shipped 2026-08-11, released as `v0.9.0`.** Full scope, phase detail and the milestone
audit are archived under [`milestones/`](./milestones/). The original scoping notes are kept
below because several of their "four checks that feel like verification and are not" and
stapling-impossibility findings remain live constraints for DIST-06.

<details>
<summary>v0.5.0 scoping notes (2026-08-07) — retained for the constraints they record</summary>

**Goal:** Make installing codegraph on macOS trustworthy and conventional — a browser-downloaded release asset passes Gatekeeper, and `brew install` works from a tap we control — by moving the release pipeline onto `goreleaser release` so its native notarization and Homebrew support become available, without breaking the raw-binary contract `codegraph upgrade` depends on.

**Target features:**
- **`goreleaser release` migration** — restructure `release.yml` from the per-platform `goreleaser build --single-target` matrix to a `goreleaser release` invocation, with cosign keyless signatures, syft SBOMs, and SLSA3 provenance all still verified end-to-end afterward. This is the enabling change: the `notarize:` and `brews:` blocks do not execute under `build`
- **Apple codesign + notarization** (backlog 999.5) — GoReleaser's OSS `notarize.macos` (Quill-backed, runs on any OS) with a Developer ID Application certificate + App Store Connect API key as CI secrets. **Notarized but deliberately not stapled** — Gatekeeper passes via an online ticket lookup; offline first launch is a documented known limitation, matching what GoReleaser itself ships for its own CLI. The bar is `spctl -a -vv -t exec` reporting `source=Notarized Developer ID` on a file actually carrying `com.apple.quarantine` — not on the pipeline being wired
- **Archives alongside raw binaries** — a second `archives:` entry keyed by `id` produces a `.zip` for the browser-download and Homebrew paths, while the raw per-platform binaries `internal/upgrade` hashes and swaps stay byte-unchanged. Multiple `archives:` entries are natively supported, so D-02/Finding 1 is preserved untouched, not amended
- **Homebrew tap** (SEED-002) — `seanb4t/homebrew-tap`, published via GoReleaser's `homebrew_casks:` block (`brews:` is deprecated as of v2.10); `brew tap seanb4t/tap && brew install codegraph` works on a clean machine. Needs a dedicated PAT — the default `GITHUB_TOKEN` cannot write cross-repo
- **`codegraph upgrade` brew detection** — detect a brew-managed install, refuse, and point at `brew upgrade codegraph`, never mutating the Caskroom (the tree the cask actually stages into) or the Cellar (the formula tree, kept for a future formula path). Detection must be tested against a real brew-managed layout, not a path-prefix guess

**Key context:**
- **The single-runner question is ANSWERED, and it is now a constraint rather than a risk** (research 2026-08-07). `goreleaser release` refuses to consume a `dist/` built elsewhere, and BOTH escape hatches — `release --split`/`continue --merge` and the `prebuilt` builder — are GoReleaser **Pro**. The only OSS path is running the whole release on one `macos-latest` runner with `zig cc` cross-compiling **both** linux legs. The residual unknown is narrower and empirical: whether zig-cross links this repo's CGo tree-sitter dependency for linux from a macOS host. Today only linux/arm64 is zig-crossed, and from a Linux host. **This is the milestone's single highest-leverage risk and must be spiked in Phase 1 before any restructuring is committed.** A costed fallback exists and is deliberately not taken yet — see the GoReleaser Pro decision below. The spike's pass condition is explicit: `zig cc` cross-compiles CGo tree-sitter to linux/amd64 AND linux/arm64 from a macOS host, and the resulting binaries **run on real Linux**. Anything less is a fail and triggers the fallback
- **The checksums collision is already latent, not hypothetical.** `.goreleaser.yaml`'s dead `checksum:` block and `release.yml`'s hand-rolled `sha256sum` step both emit `codegraph_<tag>_checksums.txt`. The moment `goreleaser release` runs, both wake up and race. Resolution is **deletion** of the hand-rolled step in the same change, not reconciliation of two writers
- **Gatekeeper impact is genuinely narrow, and that is the point.** Quarantine is set by browsers, not by programmatic downloaders — a binary fetched by the real `codegraph upgrade` path was measured to carry only `com.apple.provenance`. The affected population is browser downloaders from the GitHub Releases page. cosign is a *different* mechanism and does nothing for Gatekeeper
- **Stapling is impossible here, so it is deliberately out of scope.** Apple's `stapler` attaches tickets only to `.app`/`.pkg`/`.dmg` — never a bare Mach-O and never a `.zip` — and Quill has no staple command at all. A container is required to *submit* for notarization but is not sufficient to *staple*. Offline first launch therefore fails; that is accepted and documented, not a defect to fix this milestone
- **Four checks will feel like verification and are not:** a green CI step; `codesign -dvv` passing (**it already passes today on the adhoc-signed darwin/arm64 binary that `spctl` rejects**); `notarytool history` showing Accepted; and `spctl` run on a file that was never quarantined. The only trustworthy gate is forcing a real `com.apple.quarantine` xattr on the actually-published asset. Per the standing repo rule it must be demonstrated RED against the current un-notarized binary before it is trusted green
- **`notarize` runs before `archive`/`checksum`/`sign`/`sbom` by design**, so nothing signs pre-notarization bytes while shipping post-notarization ones — but notarization *mutates* the Mach-O, which the current pipeline never does, so this must become a measured gate (sha256 diffed per stage), not a property trusted from documentation
- **D-06R holds.** release-please remains the sole tag authority; `v0.5.0` is a prediction that holds if this milestone lands `feat:` commits, never a tag to create by hand
- Standing repo rule applies throughout: a gate is not trusted until demonstrated RED against a confirmed-applied mutation. Here that means showing `spctl` move `rejected` → `accepted` on a genuinely quarantined download, and showing SLSA/cosign verification still fails when it should after the pipeline restructure

</details>

## Requirements

### Validated

- Schema-versioned Pebble `GraphStore` substrate behind a concurrency-tested interface + benchmarked CGo tree-sitter parser decision — **validated in Phase 1**
- Go indexing pipeline (`init`/`index`/`uninit`, deterministic two-pass parallel-extract → sequential-resolve cross-file resolution) — **validated in Phase 2**
- Read-only query engine + parity stdio MCP server: `query`/`node`/`search`/`callers`/`callees`/`impact`/`affected`/`files`/`status`/`explore` plus `serve --mcp` with `codegraph_explore`-default tool gating (`CODEGRAPH_MCP_TOOLS` allowlist, zero tools without `.codegraph/`); output shapes verified against the TS v1.3.1 golden corpus — **validated in Phase 3**. **The default half of this is superseded as of 2026-08-09 (PR #44):** `CODEGRAPH_MCP_TOOLS` inverted from an opt-in allowlist to an opt-out *narrowing filter*, so an unset variable now registers all 8 tools and a set one registers only what it names plus the always-visible `codegraph_explore`. The zero-tools-without-`.codegraph/` half (MCP-03) is unchanged. See Key Decisions
- Incremental sync + native file watcher: `codegraph sync` (stat→content-hash diff + dependent-file recomputation), correct rename/delete/move pruning via an additive `x/` file-owned secondary index (no orphaned nodes / dangling edges, incl. cross-file `contains` edges), fsnotify debounced watcher + agent-facing staleness banner, MCP-reconnect stat+hash reconcile, shared `codegraph daemon` (single-writer lockfile) + `codegraph unlock`, and a goroutine-leak-free goleak soak; sync-equals-reindex determinism preserved — **validated in Phase 4** (a deep code review caught and fixed 4 concurrency/prune bugs the green suite had missed)
- Multi-language coverage & resolution breadth: the Go-only extractor generalized to a `LanguageSpec` registry + per-language extractor packages + extension→language discovery with per-language `ModuleKey` hooks (Go behavior byte-identical throughout). 14 registered languages — priority-4 (Java/C#/Python/TS-JS) at full extraction + cross-file resolution validated on real repos (Java: temporal `sdk-java` 1223 files; C#: serilog; Python: litellm/types; TS-JS: ccstatusline 13k files); mainstream-6 (Rust/Ruby/PHP/C/C++/Swift/Kotlin) at full-or-documented-partial per the committed D-11 capability matrix. Interface→implementation dispatch synthesized as `implements` edges (Go structural method-set match, Java/C# declared) traversed at query time; every synthesized edge carries `provenance: heuristic` + source location (no SchemaVersion bump — `Edge` fields were pre-reserved). Framework-aware routing (`route` nodes via heuristic `calls` edges) for Gin/Spring/ASP.NET/Django-Flask-FastAPI/Express-NestJS. The three deferred Go resolution items (WR-01, WR-02) fixed; the call-as-argument gap confirmed already-correct. A latent `proto.Marshal` map-ordering determinism bug was found and fixed store-wide. Two `[SUS]` community grammars (Swift/Kotlin) admitted only through a human-gated supply-chain review — **validated in Phase 5**
- Agent integrations & CLI lifecycle: `codegraph install`/`uninstall` configure all **8** roster agents (Claude Code, Cursor, Codex CLI, opencode, Gemini CLI, Hermes, Antigravity, Kiro) via a self-registering `AgentTarget` registry — surgical MCP-config writes (JSON / JSONC-via-`tailscale/hujson` / hand-rolled TOML / YAML) plus marker-fenced (`<!-- CODEGRAPH_START/END -->`) instruction injection for the 4 agents that take one, idempotent + byte-invariant install→uninstall round-trip, `os.Executable()` absolute-path binding, and per-agent quirks (Cursor `--path`, Antigravity no-`type`, Gemini root-level `GEMINI.md`, Codex global-only, opencode comment-preserving JSONC). `codegraph version`/`--version`/`version --json` (ldflags-injected), `codegraph telemetry` (honest zero-passive-phone-home, names `upgrade` as the sole network path), and `codegraph upgrade [version] [--check]` (embedded `sigstore-go` keyless verify-**before**-swap, fail-closed, atomic self-replace). A deep code review caught + fixed 3 Criticals the green TDD suite missed (swallowed install/uninstall I/O errors, Antigravity migration data-loss, Hermes CRLF idempotency) plus security warnings; the security audit then closed 22/22 STRIDE threats. New pure-Go deps `sigstore-go` + `tailscale/hujson` (no new CGo — tree-sitter stays the sole exception). The live MCP handshake was verified end-to-end (install → `codegraph_explore` advertised over stdio → uninstall restores config). Research corrected a stale assumption pre-execution: TS parity covers all 8 agents (not 5), only 4 take instruction files — **validated in Phase 6**
- Migration tool: `codegraph migrate [--from][--to][--force][--drop-dangling]` — a one-way, one-step, resumable, validated converter from a TS CodeGraph `.codegraph/` SQLite index to the new Pebble/protobuf format. Read path via a CGo-free `modernc.org/sqlite` v1.53.0 reader opened read-only (`mode=ro`+`query_only`, source never mutated), confined to `internal/migrate` by an import-graph archtest (no new CGo). Preserves TS node ids verbatim (faithful row-for-row conversion, not a lossy native re-derivation); carries `start_col`/`end_col`; drops FTS/vocab/unresolved_refs/sqlite-internals; builds the Phase-4 `x/` file-index during write (`Meta.has_file_index=true`). Resumable via an additive `PutMigration`/`GetMigration` `m/migration` cursor record + temp-dir→atomic-rename swap (with interrupted-swap self-heal). Fail-loud D-09 validation: de-dup-aware edge-count reconciliation (the new key collapses line/col), `file:`-exempt zero-dangling check (`--drop-dangling` explicit opt-in), schema-version-range guard, gating `Meta.Healthy`. Aged-`.codegraph/` fixtures reconstructed in-Go from the captured TS dump (no external `sqlite3`). A deep code review found 0 Criticals + 5 real Warnings the green TDD suite missed (stale `edge_count` after drop-dangling, un-`Close()`d trailing batch, overwrite-probe mutating the target dir, no interrupted-swap recovery, unescaped DSN path) — all fixed with regression tests. One new pure-Go dep (`modernc.org/sqlite`), no new CGo — **validated in Phase 7**
- Release hardening & benchmarks: single static CGo binary per platform (6 targets, native darwin matrix + zig cross for linux/windows), `release.yml` publishing per-binary cosign keyless signatures + syft SBOMs + SLSA provenance (all Actions SHA-pinned), `ci.yml` gating `govulncheck` + a `-mod=readonly` reproducible double-build + a network-free 120k-file perf-regression gate with an absolute peak-RSS ceiling, minimal audited deps (CGo tree-sitter the sole exception). Proven end-to-end on the real `v0.0.0-rc.3` release (`cosign verify-blob` OK against the shipped verifier's identity, wrong identity rejected). Median-of-3 benchmarks vs installed TS 1.3.1: Go wins every measured metric (indexing throughput 6.1×–59.7×, query latency ~2.3–2.8× lower, peak RSS 3.6×–5.5× lighter, cold start ~6× faster) — **validated in Phase 8**

- **v1.0 Phase 1 — Behavioral parity for `explore` & `node`**: TS-1.3.1-faithful port in the shared `internal/query.Engine` (CLI + `codegraph_explore` MCP byte-identical). Multi-word variadic `<query...>` tokenization (EXPL-01); graph-relevance ranking via deterministic Random-Walk-with-Restart (α=0.25, fixed 25 iters, 9 undirected/unweighted `RankEdges` kinds) with a 5-way file-relevance gate that demotes weakly-connected `Test*` funcs (EXPL-02/03); per-root "⚠️ no covering tests found" warning (EXPL-04); full multi-definition `node` (generated-files-last, two-line header, ≤16-def/12,000-char budget + overflow, substring-file/line narrowing that never empties, NODE-01/02/03, single-def byte-parity NODE-04). The user chose the **maximum-fidelity path**: D-09 expanded extraction so all 5 priority-4 languages (Go/Java/C#/Python/TS-JS) emit the full 9-kind edge set (additive proto3-string `Edge.kind` — no SchemaVersion bump, migration untouched), and D-10 ported TS's full H1–H21 heuristic stack. A behavioral fixture harness (TEST-01) diffs Go vs frozen TS 1.3.1 goldens under a normalized-equivalence oracle on BOTH surfaces, logging (not hiding) 4 real divergences (AD-01..04) as future work. A deep code review + verifier caught 2 real Criticals the green TDD suite missed (`overrides` overload-collapse corrupting RWR ranking; NODE-03 unreachable/unwired) — both fixed with FAIL→PASS regression tests before completion — **validated in v1.0 Phase 1** (11/11 must-haves)

- **v1.0 Phase 2 — status content & git/worktree awareness**: TS-parity sectioned `status` output (DB size, nodes-by-kind, files-by-language), live borrowed-index detection (4-gate `detectWorktreeIndexMismatch` port) with the CLI warning + inline MCP-result notice, and the SURF-06 MCP JSON→markdown conversion for the 5 read tools. Two deep-review rounds fixed 3 Criticals a green suite missed (dead-code MCP notice wiring, golden mis-measurement, emergent cache poisoning) — **validated in v1.0 Phase 2**

- **v1.0 Phase 3 — Watcher-on-MCP Default**: `serve --mcp` now runs the fsnotify watcher by default (`--no-watch` opt-out; `--watch` repurposed as explicit force-on), with the TS `watch-policy.js` port (`CODEGRAPH_NO_WATCH`/`CODEGRAPH_FORCE_WATCH` env precedence, WSL2 `/mnt` auto-off) enforced at the shared `daemon.Run` seam, all watcher startup off the MCP handshake path by construction (`serveWatchStart`, mutation-proof seam test), and defer-and-retry lock convergence for concurrent sessions (goleak-clean). TEST-04 landed the `test/integration/` subprocess harness (real binary, real stdio JSON-RPC, CR-01 worktree-notice anchor mutation-proven) + explicit CI steps. Three review rounds fixed 2 Criticals + 7 Warnings a green suite missed (pebble lock contention made default-path by the flip — now bounded-retried behind a `graphstore.ErrStoreLocked` sentinel with build-tagged unix/windows classifiers); `internal/agents/` byte-untouched, so existing installs light up with zero config change — **validated in v1.0 Phase 3** (5/5 must-haves)

- **v1.0 Phase 4 — Output Hygiene**: Pebble's WAL/INFO chatter is routed to discard via a three-method `quietLogger` injected at the module's single `pebble.Open` seam (`Options.Logger` only — `EnsureDefaults` derives the EventListener from it), with `Errorf` preserved through a mutex-guarded injectable stderr seam and `Fatalf` semantics untouched (HYG-01, mutation-proven: reverting the injection turns the wiring test red on real "Found 0 WALs" noise). HYG-02 is enforced two ways: a `go/packages`-based archtest fails the build on any `os.Stdout` / bare `fmt.Print*` / `log.SetOutput` reference across the full serve-reachable import closure of the six guarded packages (`internal/cli` excluded), and a hand-rolled raw-stdio integration case asserts every `serve --mcp` stdout line parses as a JSON-RPC frame (deliberately NOT built on mcp-go's client, which silently skips malformed lines). A deep review + fix loop closed 1 Critical (the archtest originally never descended into transitive imports — proven with a green suite over a polluted `internal/schema`) and 3 Warnings before an iteration-2 CLEAN re-review; verifier passed 9/9 must-haves — **validated in v1.0 Phase 4**

- **v1.0 Phase 5 — Git Sync Hooks**: `codegraph githooks install|remove|status` (a documented Go-only surface extension — TS 1.3.1 has no githooks command, only init's interactive offer + uninit cleanup) writes the byte-verbatim TS marker-fenced `post-commit`/`post-merge`/`post-checkout` hooks that background-run `codegraph sync` (`command -v` guarded, never blocks git), via a verbatim port of TS `sync/git-hooks.js` semantics: strip-then-re-append-at-end, delete-when-effectively-empty on remove, `#!/bin/sh` seeding, best-effort chmod 0755, `git rev-parse --git-path hooks` resolution (honors `core.hooksPath`; linked worktrees share the common hooks dir). TS-installed hooks are detected/managed seamlessly (drop-in win, fixture-proven). HOOK-03 surfacing: a non-interactive port of TS's `offerWatchFallback` on `init`'s success path (fires only when the watch policy disables the watcher) + TS-parity best-effort hook cleanup in `uninit`; the Phase-3 D-12 serve message stayed byte-untouched. New `internal/fsatomic` (atomicWriteFile extracted from `internal/agents`, byte-invariance suite unmodified) + `gitmeta.IsGitRepo`/`HooksDir` probes. A 7-round deep-review/fix loop (10th consecutive green-suite-hid-real-defects recurrence) found and fixed 2 reproduced data-loss Criticals — TS's own inherited stripMarkerBlock truncation bug, then a NEW fix-composition Critical (round-1's guard + Install's fallback = delayed user-content deletion, the Phase-2 BL-01 pattern again) plus a ReadFile-error-as-absent overwrite — converging on the invariant "no Install/Remove sequence ever destroys content it could not read+parse" and a complete {Install,Remove,Status} × {read-error,malformed-marker} error-surfacing matrix. Verifier drove the real binary end-to-end: 9/9 must-haves — **validated in v1.0 Phase 5**

- **v1.0 Phase 6 — Rendering Seam & Pretty `status`/`files`**: a build-enforced rendering seam permanently isolates all Charm styling from the agent/MCP path. TUI-01 landed a fail-closed ANSI-isolation archtest over six guarded packages (forbidding lipgloss/bubbletea imports on the serve-reachable closure, with a self-defeat guard so the test cannot pass vacuously) plus the `internal/cli/present` seam (`ChoosePresentation`). TUI-02 added `present.RenderStatus`/`RenderFiles` lipgloss renderers wired behind an isTTY check, proven byte-identical to the plain path when piped by an integration test. TUI-05 added a hand-rolled stderr progress writer (ticker-based, deliberately no bubbletea/bubbles dependency) wired into `init`/`index`/`sync` — **validated in v1.0 Phase 6**

- **v1.0 Phase 7 — Interactive TUI (daemon picker & install multi-select)**: `daemon` became an interactive bubbletea picker backed by a Charm-free global daemon registry (`Record` + `Register`/`Deregister`/`List`, self-healing via stale-lock detection), explicit start/stop lifecycle, and a PPID watchdog; `install`/`uninstall` present a checkbox multi-select; every interactive surface auto-falls back to non-interactive behavior when piped and never hangs (`InteractiveAllowed` dual-TTY gate). Human UAT on a live TTY caught two user-visible bugs that both the full piped suite and a deep multi-agent review missed — leaked DECRQM capability-probe escapes on bare `daemon`, and inline (non-alt-screen) picker rendering causing flicker — the finding that motivated backlog 999.2 — **validated in v1.0 Phase 7**

- **v1.0 Phase 8 — Surface Reconciliation & benchmarks**: every TS flag name and default is present or a documented divergence (`docs/FLAG-PARITY.md`), the new Charm dependency closure audited via govulncheck/SBOM, and the drop-in-parity claim validated — retiring v0.1's "not yet drop-in" caveat and closing its pending PERF-01 published numbers — **validated in v1.0 Phase 8**

- **v1.0 Phase 9 — release-please + GoReleaser**: maintainer-manual tagging replaced by automated release management — release-please owns version bumps, `CHANGELOG.md`, and tag creation from Conventional Commits; GoReleaser owns building and uploading artifacts. Proven end-to-end on a real release: **`v0.2.0` published**, tag and Release authored by `fzy-release-please[bot]` with no human `git tag`, all 11 `release.yml` jobs green, 20 assets, `cosign` Verified OK, SLSA verified at builder `@v2.1.0`, and `TestVerifyReleaseE2E` executing against a real artifact for the first time. The installed binary is byte-identical to the SLSA-attested subject, the cosign-verified blob, and the artifact the E2E test passed against. A genuinely shipped v0.1.0 binary self-upgraded to v0.2.0 on darwin/arm64 and linux/amd64. REL-02 satisfied on both halves — **validated in v1.0 Phase 9**

- **v1.0 Phase 10 — Local Build Tooling & CONTRIBUTING**: a `Taskfile.yml` (go-task) wrapping build/test/lint/release-check as the *single definition* of each workflow, with CI job bodies rewired to invoke the task targets and `TestWorkflowRunBodiesInvokeTask` enforcing that as a demonstrably non-vacuous invariant; `CONTRIBUTING.md` documents the CGo toolchain prerequisites, verified by a real scratch-clone run proving `task build`/`test`/`lint` succeed with only Go and a C toolchain on `PATH`. `check:cross` became the sole definition of the 6-target `go list -mod=readonly` sweep with a real-YAML set-equality guard against `.goreleaser.yaml`'s build matrix. A permanent `darwin-toolchain-canary.yml` machine-proves the release pipeline's macOS runner class and native Apple Silicon toolchain outside the tag-only `release.yml` loop. Incidentally found and fixed a **51.5%-stale perf baseline** that had left the regression gate only able to fire on a ~38% drop, and hardened `CheckRegression` to refuse runner/scratch_fs mismatch as a category error — closing the blind spot behind this repo's retracted fictitious 10.6% regression — **validated in v1.0 Phase 10**

- **v0.3.0 Phase 1 — Protocol Scoping & the SDK-Independent Wire Oracle**: the project now owns a wire-level regression oracle that reads the actual bytes on stdio and is proven able to fail. 23 frozen transcripts captured from the **current, unmodified** `mark3labs`-backed `serve --mcp` (VRFY-04) by a standalone capture tool that never uses the SDK under test as its own oracle (VRFY-01) — covering all 8 tools, three `tools/list` variants, four error/edge shapes, and a multi-era Legacy handshake baseline. Two captures were deliberately one-way, taken before `mark3labs/mcp-go` can leave `go.mod`. `serve --mcp` gained an always-on stderr session line reporting requested vs negotiated protocol version, with no flag or env var to enable it (VRFY-03) — the only available mitigation for a spec-sanctioned silent version mismatch, hardened against hostile `clientInfo` (nine injection shapes pinned). The declared protocol version is asserted against a repo-owned literal with an archtest forbidding any `LATEST_PROTOCOL_VERSION`-style SDK constant tree-wide (VRFY-02), and `internal/cli/serve.go` now bootstraps entirely through the narrow `internal/mcp.Server` seam, importing no MCP SDK package (SDK-02) — the one production-code SDK leak, closed while it was still cheap. A dated 8-agent audit measured what each roster client actually negotiates via a byte-exact proxying shim rather than reading their docs (VRFY-05). Non-vacuity was proven, not assumed: four one-time mutations demonstrated red against the real binary, plus permanent structural guards for the exact scenario count, the empty transcript, and per-rule normalization scope. A cross-AI review round found a real blocker (a malformed tracer `explore` call that would have frozen an error as the 8th tool's coverage) before capture ran. UAT found and closed one further gap — the session-line mutex had no test and `internal/mcp` was outside the race-detector's package set — **validated in v0.3.0 Phase 1** (6/6 requirements, 30/30 threats closed)

- **v0.3.0 Phase 2 — SDK Migration (official go-sdk on the existing surface)**: `mark3labs/mcp-go` replaced by `modelcontextprotocol/go-sdk@v1.7.0` (SDK-01/03/04/05 closed). The maintainer relaxed the acceptance bar mid-phase from byte-identity to semantic equivalence plus one human diff read, having called the milestone over-engineered — and the *relaxed* bar immediately earned itself, catching a ninth unpredicted **semantic** divergence no byte comparator would have interpreted: `tools/call` before `initialize` flipped from success to rejection, because go-sdk enforces MCP's initialize-first ordering that mark3labs never gated. Phase 1 had deliberately frozen that permissiveness as "an asset for Phase 3"; it was explicitly retracted in `scenarios.go` rather than silently dropped. Nine named causes total (2 semantic, 7 cosmetic), zero unattributed. D-05 was **confirmed falsified at runtime** — `legacy-unsupported-2026-07-28` did not move; the *omitted*-version scenario is the one that did. go-sdk carries a real stdin-close race that deterministically loses in-flight responses and exposes no `StdioTransport` customization hook, forcing a custom `IOTransport` — which then tripped v1.0's HYG-02 stdout archtest, closed with a function-scoped allowlist plus a self-test — **validated in v0.3.0 Phase 2** (5/5)

- **v0.3.0 Phase 3 — `2026-07-28` Spec Compliance**: all 8 SPEC requirements closed, but the phase was far smaller than its requirement count implied — Phase 2's SDK swap had already delivered SPEC-01/03/06/08 and **all of SPEC-02 with zero server code**. The one real build was SPEC-05's live tool catalog: a per-request recheck in `AddReceivingMiddleware` driving `AddTool`/`RemoveTools` (both `s.mu`-guarded and safe mid-flight), which emits `listChanged` to Legacy sessions for free. Modern-vs-Legacy gating was confirmed to be the SDK's job — a version branch in a handler is the wrong shape. Two probe failures produced confident wrong conclusions before source-reading corrected them: SEP-2575 signals protocol version via `_meta`, not `params.protocolVersion` (a wrong probe makes an implemented method look unimplemented), and a version sorting *lexically* before `2026-07-28` reads as pre-modern and yields `-32601` rather than `-32022`. D-03's anti-regeneration guard was made **advisory** because its two-PR remedy is structurally impossible and leaving it would have passed silently for the wrong reason; detection was preserved and proven by mutation. Scenario count 23 → 27 — **validated in v0.3.0 Phase 3** (5/5)

- **v0.3.0 Phase 4 — Supply-Chain Coverage & Daemon Substrate Fixes**: tool-modfile vulnerability scanning closed the ~400-module credentialed-CI-tooling gap (VULN-01/02/03), landing **advisory** rather than blocking — a decision reversed mid-milestone and correctly so, since `GO-2026-5932` is `Fixed-in: N/A` and a blocking gate would have been permanently red on day one. The split that survives is worth reusing: findings advisory, *detection regression* still fails. Daemon test-seam fixes closed issues #13 (`getppid` race) and #17 (watchdog flakes under full-suite load); the extreme-load timeout tail was accepted as a limitation with CI load ruled the governing standard for MAINT-02 (52/52 real CI runs clean — the flake was always local). A retroactive security audit later found the threat register had asserted a control the implementation did not have (T-04-01 declared "exits non-zero and fails the build" against an advisory-only impl) and a "transfer" disposition never accepted by the receiving party (live `gh api rulesets` proved `tool-vuln` absent from the required contexts) — both reconciled to `accept` — **validated in v0.3.0 Phase 4** (3/3)

- **v0.3.0 Phase 5 — Live Tool-Catalog Change Notification**: Modern-session `subscriptions/listen` completed the catalog-change surface Phase 3 had delivered for Legacy sessions. Three malformed probes were caught here too — `toolListChanged` (singular) is silently dead, a typo'd opt-in is acknowledged with an empty set and no error, and a live subscription emits only the ack, so **frame count, not payload, distinguishes a live subscription from a dead one**. `SubscriptionsListenResult` arrives only on graceful teardown — **validated in v0.3.0 Phase 5** (1/1)

- **v0.5.0 Phase 1 — Cross-Compile Spike & `goreleaser release` Migration**: the milestone's blocking spike, and it **passed on variation V1 at first dispatch** — one `goreleaser release --snapshot` on a macOS runner produced all four binaries with both linux legs `zig cc`-crossed, and both then EXECUTED on real non-emulated Namespace hardware indexing a real tree (`files=430 symbols=4281` on x86_64 and aarch64 alike). V2–V5 of the pre-declared FAIL-bar list were never needed; GoReleaser Pro was not bought. The pipeline collapsed to one `goreleaser release` owning archives/checksums/signing/SBOMs, with the hand-rolled `sha256sum` writer deleted in the same change that made `checksum:` live. Headline trap, found four times across review cycles and once more in execution: GoReleaser expands `${artifact}` from an artifact's **Path** but publishes under its **Name**, and for `formats: [binary]` those differ because the rename lives only in metadata — so GoReleaser's own documented `signature: "${artifact}.sigstore.json"` idiom collapses all four platforms onto one filename. Following upstream docs faithfully *was* the bug. Fixed with Go-template **fields**, and the oracle rewritten to read published names from `dist/artifacts.json` rather than filesystem basenames, which are identical under broken and correct configs — **validated in v0.5.0 Phase 1** (5/5: REL-05…09)

- **v0.5.0 Phase 2 — Apple Signing & Notarization**: `spctl -a -vv -t exec` moved `rejected` → `accepted` on both darwin arches, demonstrated against a file actually carrying `com.apple.quarantine` rather than one that was never quarantined. Notarized but deliberately **not stapled** — Apple's `stapler` attaches tickets only to `.app`/`.pkg`/`.dmg`, never a bare Mach-O or `.zip`, and Quill has no staple command — so offline first launch remains a documented limitation (DIST-06), not a defect. cosign moved off the build-scoped `binary_signs:` pipe onto the release-scoped `signs:` pipe (D-18) so it signs post-notarization bytes *by construction* rather than by ordering luck; that also fixed a pre-existing canary breakage, since `goreleaser build` has no `--skip=sign` while `goreleaser release` does — **validated in v0.5.0 Phase 2** (4/4: SIGN-01…04)

- **v0.5.0 Phase 3 — Homebrew Tap & Cask**: `brew tap seanb4t/tap && brew install codegraph` works from a cold machine, proven by a real cold install on a torn-down host with genuine bash/zsh/fish Tab-completion and man-page rendering. `homebrew_casks:` (not the deprecated `brews:`) publishes via a dedicated least-privilege App credential, whose write scope was *watched refuse* a cross-repo write with a resource-scope 403 after first succeeding against the tap. The install gate was watched fail for both reasons it exists to catch — a binary that cannot execute, and one that executes and reports the wrong version. Measured constraint worth keeping: Homebrew Cask **unconditionally quarantines** every download via LaunchServices FFI regardless of URL scheme, and `spctl --add` was removed in macOS 15.0 — so an ad-hoc-signed binary is SIGKILLed the moment a post-install hook runs it. Real Developer ID credentials are load-bearing even for local cask rehearsal — **validated in v0.5.0 Phase 3** (6/6: BREW-01…06)

- **v0.5.0 Phase 4 — `codegraph upgrade` × Homebrew**: a brew-managed install now refuses to self-update and points at `brew upgrade codegraph`. Detection is **structural and prefix-agnostic** — resolved symlinks plus Homebrew's own `INSTALL_RECEIPT.json` at the tree-specific location (`<token>/.metadata/` for Caskroom, `<version>/` for Cellar) — replacing Phase 3's self-authored sentinel, which a release could silently forget to write. The strongest mitigations here all **remove** a code path rather than guard it: `Options.Force` is never read (its absence is the enforcement), an `EvalSymlinks` error returns not-detected immediately with no fallback, and a nonzero man-page baseline halts rather than reimplementing Homebrew's bookkeeping. A guard is a policy someone can reimplement wrongly later; an absent path has nothing to get wrong, and is cheaper to audit — **validated in v0.5.0 Phase 4** (3/3: UPGR-01…03)

- **v0.10.0 Phase 5 — MCP Resources Capability & Claims Drift Guard**: the MCP server now serves 10 embedded resources (9 per-tool fact-sheets + an `index-state` behavior doc) over `resources/list`/`resources/read`, registered unconditionally before the `hasIndex` branch so they're readable even in a never-indexed repository — proven on the wire by 13 new frozen wire-oracle scenarios covering both index states plus the unregistered-URI error shape. A structural claims-drift guard (`resources_schema_drift_test.go`) ties resource prose to the actual tool schemas — numeric claims, resource-file-set membership (bidirectional), env-var names, and host-fact leakage — demonstrated red-then-reverted against 5 real mutations (tool rename, code-side default change, prose-side default change, and two more). Built tracer-first: plan 05-01 proved the whole architecture end-to-end on one resource before fanning out to the other 9 in 05-02. Code review found one in-scope doc-drift bug (`index-state.md` overstated its re-check trigger set as "every request" when only 4 methods trigger it) and one real but pre-existing, out-of-scope bug in `server.go`'s `pendingWriter`/EOF-race mechanism (predates this phase; not caused by it) — the former fixed same-day with a proper wire-oracle re-freeze, the latter flagged for separate follow-up — **validated in v0.10.0 Phase 5** (5/5: RSRC-01…03, GUARD-01…02)

- **v0.10.0 Phase 6 — Agent Skill Package (SKILL.md & SessionStart Nudge)**: SKILL.md and a SessionStart hook package were dogfooded into this repo's own `.claude/` (the canonical source, not a copy — Phase 7's `go:embed` later points directly at it). SKILL.md leads with a decision table ("which tool for which question") before any tool catalog, with three worked examples including the class of misdirection incident (2026-08-08 debug session) that motivated this whole milestone; SKILL-03 was proven by a committed rehearsal transcript diff, not asserted. The nudge fires once per SessionStart matcher (`startup` and `resume`), file-existence-check only, no MCP round-trip. A live-session debug cluster (three sessions, 2026-08-13) chased what looked like a regression of the skill-catalog-listing bug and a resume-matcher failure — both retracted as false findings once the actual oracle (a side-channel log, not transcript grep) was used, but the investigation hardened a durable lesson (`RULE: two instruments not one`) into the skill and its own verification rehearsal — **validated in v0.10.0 Phase 6** (5/5: SKILL-01…03, NUDGE-01…02)

- **v0.10.0 Phase 7 — `codegraph install` Skill + Hooks Distribution (Claude Code)**: `codegraph install` now writes the binary's own embedded SKILL.md, an executable `session-nudge.sh`, and a SessionStart hooks registration into Claude Code's real read locations (`~/.claude/...` or `./.claude/...`), byte-identical/idempotent across repeated installs, with a sidecar manifest (version + per-artifact sha256) making "which version is installed" observable from disk rather than inferred — `codegraph upgrade` refreshes every previously-configured location automatically after a successful swap, reporting a refresh failure as a warning distinct from upgrade's own success. Roadmap's explicit "highest-risk phase" framing proved warranted: research corrected a load-bearing wrong assumption pre-execution (Claude Code reads project hooks from `.claude/settings.json`, never a bare `hooks.json`, discovered by Phase 6's own prior research and re-confirmed live; `go:embed` cannot cross the `.claude/`↔`internal/` sibling boundary via `..`, proven by a live experiment, forcing the embed source to a new repo-root package). Mid-execution, a background security review caught a real authorization-differential a Wave-3 fix had introduced — a matcher+shape ownership-recovery heuristic that could let codegraph silently overwrite an unrelated user hook sharing a matcher name — independently traced, confirmed RED against the vulnerable code, and reverted same-day (commit `242ec0a`); ownership is exact-command-string-only again, by design, matching the original threat model's own T-07-04 disposition. A subsequent deep code review (post-execution) found 2 CRITICAL + 4 WARNING findings — a manifest write gated on content *resolution* rather than write *success*, and a lost executable bit that could never self-heal once content matched — fixed same-day with RED→GREEN regression tests (commit `c2ea00f`), plus 2 of 4 WARNINGs; the remaining 2 are documented follow-up debt, not silent drops. UAT closed with live CLI execution (real binaries built at two versions, real scratch-`$HOME` install/refresh/read-error runs) rather than a human click-through, after the user pushed back on being asked to do mechanical verification a shell could do directly — **validated in v0.10.0 Phase 7** (3/3: AGENT-01…03)

- **v0.10.0 Phase 8 — Instructions & Marker-Block Rewrite**: the MCP `initialize`/`server/discover` `instructions` const (`internal/mcp/server.go`) and the marker-fenced `codegraphInstructionsBlock` `codegraph install` injects into 4 agent targets (`internal/agents/instructions.go`) were both rewritten to point at what Phases 5-7 actually shipped — the MCP resources capability and, for Claude Code specifically, the installed codegraph skill — retiring the stale "defers full tool guidance to the MCP initialize response (Phase 3)" promise that had never been built. The wire `instructions` const stayed within its existing 600-byte/no-newline budget (measured 554 bytes, down from 580) rather than being raised, per research showing industry consensus favors short pointer-style server instructions over exhaustive detail; it names `resources/list` and the skill generically rather than enumerating individual `codegraph://` URIs. The skill claim is Claude-Code-scoped (`"in Claude Code, codegraph install also adds the codegraph skill"`) since only Claude Code received the actual install in Phase 7 — the shared marker block used identically by Codex/opencode/Gemini stays skill-agnostic by design, so it never promises something those three targets don't have. New WIRE-03 "every named capability is resolvable at test time" guards derive from live sources (`claudeassets.SkillMarkdown()`, a live `resources/list`/`resources/read` session round-trip) rather than hand-typed values, following the repo's established drift-guard discipline. All 38 affected wire-oracle transcripts were re-frozen through the project's own capture CLI in one reviewed-diff pass, each attributable to the single `instructions`-string cause. Deep code review found 0 critical issues; the 2 warnings were a pre-existing, already-tracked wire-oracle response-ordering flake (unrelated to this phase) and a low-severity gap in an absolute-path denylist — **validated in v0.10.0 Phase 8** (3/3: WIRE-01…03). This was the milestone's last phase — all 4 phases (5-8) are now complete.

### Active

**v0.10.0 — Agent Onboarding Skill & MCP Resources** (started 2026-08-12): All 4 phases (5, 6, 7, 8) complete, 16/16 requirements validated. Ready for `/gsd-complete-milestone` — not yet run. See "Current Milestone" above.

Remaining backlog (see `ROADMAP.md` → Backlog, preserved across the milestone close):

- [ ] 999.2 — tmux-driven real-PTY e2e/UAT harness for the interactive TUI (the missing rung between piped TTY-blind integration tests and manual human UAT; motivated by two TTY-only bugs v1.0 Phase 7 UAT caught)
- [ ] 999.4 — `CheckRegression` current-metrics positivity guard (degenerate `current.PeakRSSBytes = 0` silently passes both the relative and absolute checks)

Open threads carried out of v0.3.0, deliberately NOT in v0.5.0:

- [ ] `TestFrozenTranscriptsMatch/toolslist-repeat` freezes JSON-RPC response *arrival order*, which the protocol does not guarantee and go-sdk's async dispatch does not provide — failed once on a main push, payloads byte-identical (tracked in `todos/pending/`)
- [ ] Sweep for other `cmd.Wait`-races-`StdoutPipe` sites (`internal/daemon`, `test/wireoracle`) sharing the shape fixed in `tools/mcpaudit`
- [ ] `stdinLingerReader`'s unbounded `bufio.ReadBytes` (UF-2) — a long line without a newline is a memory-growth path
- [ ] `requiredCheckNames` fixture has drifted from the live ruleset across three milestones; the test never compares against the real thing
- [ ] Root `SECURITY.md` overstates govulncheck coverage — claims it gates every merge without the advisory caveat
- [ ] `GO-2026-5932` accepted-unmitigated (unmaintained openpgp reachable in `goreleaser`'s binary via cosign/rekor) — **note this milestone touches `goreleaser` directly, so it may be revisited on evidence**

Deferred to later releases:

- [ ] Central graph server — multi-user, remote queries, auth (Team Scale; v0.1's architecture was designed to accommodate it without a rewrite)
- [ ] CI-built shared index distribution / caching (Team Scale)
- [ ] Graph annotations the v0.1 schema reserved space for — embedding vectors, community assignments, bulk export for visualization
- [ ] Local Svelte web UI for browsing/querying the graph (SEED-001 — triggers once parity lands; the v1.0 bubbletea TUI is a distinct terminal surface)
- [ ] Worktree support **beyond** TS parity — auto-init or `git-common-dir` index sharing (v1.0 ships TS-parity detect+warn+notice only; going further is a deliberate later call)

### Out of Scope

- Hosted platform features (getcodegraph.com-style PR analysis) — different product; not this project's goal
- Bundling any runtime with the binary — antithesis of the design; the static binary IS the distribution
- Embeddings/vector search, community detection, graph visualization UI — planned future milestones; the schema anticipates them (annotations, export) but v0.1 does not implement them. Embeddings will be local-model-first; cloud-API embeddings are permanently out

## Context

**Source project:** colbymchenry/codegraph, TypeScript (~4.5 MB source), v1.3.1, ~59k stars, MIT license. Builds a SQLite knowledge graph in `.codegraph/` per project, exposes it via CLI and MCP server, auto-syncs via file watcher, supports ~12 languages with cross-file resolution (framework-aware routes, mixed iOS/React Native bridging). Distributes via `curl | sh` installer or npm, bundling its own Node.js runtime — the supply-chain surface this port eliminates.

**User environment:** Sean runs TS CodeGraph daily as an MCP server across his projects, so parity gaps will be felt immediately — a strength for validation. Work team uses Java/C# heavily (hence its priority position); open-source release is intended from day one, so docs, release engineering, and compatibility promises matter early.

**Repo state:** **v1.0 (Drop-in Parity & Human UX) SHIPPED 2026-08-03** — all 10 phases complete and verified, 48/48 requirements, ~84.9k lines of Go. The milestone spans 594 commits over 20 days (2026-07-14 → 2026-08-03), 755 files changed, +98,761/−1,942 lines. Archived to `milestones/v1.0-ROADMAP.md` / `v1.0-REQUIREMENTS.md`; no milestone git tag (release-please is the sole tag authority). Prior milestone: **v0.1 (Initial Release) SHIPPED 2026-07-14** — all 8 phases complete. The core capabilities (index/query/MCP/sync/migrate) work in a single static Go binary, from a real cosign-signed / SLSA-attested / SBOM'd release (`v0.0.0-rc.3`, verified end-to-end via `cosign verify-blob` against the shipped verifier's identity) that outperforms TS 1.3.1 on every measured benchmark (median-of-3: 6.1×–59.7× indexing throughput, ~3× lower query latency, 3.5×–5.5× lighter peak RSS). **Parity gates green; automated signed releases LIVE as of 2026-08-01** — the v1.0 milestone (Phases 1-8) closed the behavioral and surface gaps against TS CodeGraph v1.3.x (the Phase-1 TEST-01 behavioral harness and the `docs/FLAG-PARITY.md` flag audit are both green), retiring the drop-in-parity caveat below. Phase 9 then replaced maintainer-manual tagging with release-please: **`v0.2.0` is published** — signed, SBOM'd, SLSA-attested, with its tag and Release authored by `fzy-release-please[bot]` and no human running `git tag`. REL-02 is satisfied on both halves; a genuinely shipped prior binary self-upgrades to it through the unchanged cosign identity on darwin/arm64 and linux/amd64, byte-identical to the attested subject. The repository is public and community health is 100%. **No `v1.0.0` tag exists, and that is deliberate** — the maintainer removed the version target on 2026-07-29 (D-06R); versions follow Conventional Commits. Current tags: `v0.2.0`, `v0.1.0`, `v0.0.0-rc.3`. Earlier milestone history follows. Phase 1 landed the substrate (Pebble-backed `GraphStore` behind a concurrency-tested interface, protobuf schema-versioned records, CGo tree-sitter parser seam, golden TS ground-truth corpus). Phase 2 shipped the working Go indexer: `codegraph init`/`index`/`uninit` build a correct, cross-file-resolved graph from scratch via a deterministic two-pass (parallel-extract → sequential-resolve) pipeline — validated end-to-end (self-indexing this repo: files=48 nodes=414 edges=660, byte-identical rebuild). Phase 3 opened the graph to agents: the full read-only query command suite plus a stdio MCP server (`serve --mcp`, `mark3labs/mcp-go`), reading through the frozen Phase-2 graph via an additively-extended `Reader` (node/file iterators; query-time reverse adjacency). Output-shape parity with TS v1.3.1 is proven by a live golden-corpus diff (7/7 tools against the pinned `weft` checkout); a deep code review's two Critical findings (default-`--limit` DoS; MCP `path` confinement) were fixed before completion.

**Known open question (RESOLVED in Phase 1):** Parser strategy was decided by a benchmarked spike — **Option A, CGo tree-sitter** (`tree-sitter/go-tree-sitter` + per-language grammars), the single documented CGo exception (DIST-05). See `PARSER-DECISION.md`. wazero WASM remains a monitored future option behind the narrow `parser.Parser` seam.

## Constraints

- **Tech stack**: Go (latest stable), single static binary per platform — the performance and supply-chain story both depend on it
- **Supply chain**: Minimal, audited dependencies; prefer pure Go (CGo only with explicit justification pending parser research); signed + attested + reproducible + SBOM'd releases
- **Compatibility**: TS CodeGraph v1.3.x is a **functionality baseline, not a binding constraint** (maintainer ruling 2026-08-09) — match its agent-facing capability surface (MCP tools, CLI semantics), but deliberate, documented divergence is acceptable where its behavior is worse. A TS behavior is evidence about what users expect, not a veto; do not cite this line as a blocker without weighing that ruling. One-way migration from its SQLite format still binds. See Key Decisions
- **Architecture**: v1 storage and process design must accommodate milestone-2 team features (central server, CI-distributed indexes, concurrent access) without a rewrite
- **Licensing**: Original is MIT — port with attribution

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Full parity in v1 (not core-first) | Drop-in swap is the success bar; partial parity can't validate it | ✓ Parity complete, automated releases live — v0.1 shipped the core capabilities + a signed release; v1.0 (Phase 8) closed the remaining CLI-surface gap (drop-in gate green: TEST-01 harness + `docs/FLAG-PARITY.md`), so it IS now a drop-in parity replacement. Phase 9 shipped `v0.2.0` via release-please. No `v1.0.0` tag exists by deliberate directive (D-06R), not as an outstanding item. |
| New index format + migration tool | Optimize schema for concurrency/perf/scale; converter keeps existing users | ✓ Converter shipped (Phase 7): faithful TS-id-preserving one-way migrate, resumable + fail-loud validated |
| Parity v1 → team features v2 | Ship replacement value first; architect so server/CI features bolt on | ✓ v0.1 shipped core value, v1.0 (Phase 8) closed parity, Phase 9 automated signed releases (`v0.2.0` live). No `v1.0.0` tag by directive (D-06R). Team features (server/CI) remain deferred to a later milestone as designed. |
| Language priority: Go → Java/C# → Python → TS/JS | Matches Sean's daily usage and work-team stack | ✓ Delivered (Phase 5): 14 registered languages, priority-4 at full extraction + resolution on real repos, mainstream-6 full-or-documented-partial |
| Full supply-chain suite from first release | Signing, SLSA, SBOM, reproducibility are the differentiator, not an afterthought | ✓ Shipped (Phase 8): per-binary cosign keyless + SLSA provenance + syft SBOM + govulncheck + reproducible double-build, proven on real `v0.0.0-rc.3` |
| Parser strategy (CGo tree-sitter vs wazero WASM vs native Go) | Performance vs purity vs sandboxing — needs quantified research | ✓ Resolved (Phase 1): CGo tree-sitter, benchmarked; single documented CGo exception (PARSER-DECISION.md) |
| Milestone v1.0 = drop-in parity + human UX | v0.1 shipped core value but diverges from TS *behavior*; parity is the honest 1.0 bar. Scope derived from a live dual-indexed bake-off, not docs | ✓ **Shipped 2026-08-03** — all 10 phases verified, 48/48 requirements. Behavioral parity (`explore`/`node`), watcher-on-MCP default, git/worktree awareness, output hygiene, Charm TUI, surface reconciliation, release automation (`v0.2.0`), and local build tooling all delivered. No `v1.0.0` tag by directive (D-06R) |
| Include bubbletea/Charm TUI in v1.0 | TS's human output is colorized + interactive (`daemon` picker) — prettiness is part of parity, not gold-plating | ✓ Delivered (Phases 6–7): lipgloss `status`/`files`, bubbletea daemon picker + install multi-select, stderr progress. Agent/MCP path stays plain — enforced by a fail-closed ANSI-isolation archtest, not convention |
| Worktree awareness scoped at parity for v1.0 | TS `sync/worktree.js` only detects a borrowed index + warns; matching that is the 1.0 bar. Auto-init/share is a larger design | ✓ Delivered (Phase 2): 4-gate detect + CLI warning + inline MCP notice, at TS parity. Going beyond (auto-init / `git-common-dir` sharing) remains deliberately deferred |
| **No milestone git tags** — release-please is the sole tag authority | Since Phase 9, release-please owns all tag creation from Conventional Commits (D-06R); a hand-created planning tag reintroduces exactly the manual-tagging practice that phase removed. The milestone record is already durable in `MILESTONES.md` + `milestones/v1.0-*` without one. `milestone-v0.1` (2026-07-14) **predates release-please** (`v0.2.0`, 2026-07-31) and is a pre-automation artifact, not a convention to continue | ✓ Decided at v1.0 close — no `milestone-v1.0` tag created. Corollary if this is ever revisited: a planning tag must NOT match `release.yml`'s `push: tags: "v[0-9]*"` trigger, or it fires the full release pipeline and falsely claims a release release-please never cut |
| Plan for embeddings, communities, graph-viz UI as future milestones | Long-term product direction; v1 schema versioned + annotation-ready so they bolt on | — Pending (unchanged; still deferred after v1.0) |
| Build the wire oracle **before** any SDK code moves | v1.0 Phase 4 already established that an SDK's own client silently skips malformed stdout lines and therefore cannot fail a purity test. A harness written after the swap and validated with the new SDK's client would describe the new behavior rather than test it | ✓ Delivered (v0.3.0 Phase 1): 23 transcripts frozen against the pre-migration server, non-vacuity mutation-proven. Two captures (all-8-tool coverage, multi-era Legacy baseline) were one-way — unrecapturable once `mark3labs/mcp-go` leaves `go.mod` |
| VRFY-02 is an **asserted pin**, not an injection point, for Phase 1 | `mark3labs/mcp-go` v0.56.0 decides the negotiated revision in an unexported method and exposes no `WithProtocolVersion` option, so no caller can inject a repo-owned literal while it is the backend. The pin still delivers the property that matters: a dependency bump turns the oracle red instead of moving wire behavior silently | ✓ Decided at v0.3.0 Phase 1 UAT — ROADMAP criterion 3 restated from "reads from" to "asserted against" to match REQUIREMENTS.md and the shipped mechanism. The stricter "reads from" property is carried to Phase 2, when the official go-sdk supplies a backend a caller can actually supply a revision to |
| Milestone v0.5.0 = macOS distribution & Homebrew | 999.5 and SEED-002 collide on one constraint — a bare Mach-O can be notarized but not *stapled*, brew conventionally wants a container, and D-02 pins release assets as raw binaries because `internal/upgrade` hashes and swaps the binary directly. Scoping them together resolves the asset shape once instead of twice | — Scoped 2026-08-07. Deliberately tight: 999.2, 999.4, and the v0.3.0 open threads all stay parked |
| Move to `goreleaser release` (rather than extend the build matrix) | GoReleaser's `notarize:` and `brews:` blocks execute only under `release`; today `release.yml` runs `goreleaser build --single-target` per platform and never `release`, which is why `.goreleaser.yaml`'s `archives:`/`checksum:` blocks are documented dead configuration. Extending the matrix with hand-wired `codesign`/`notarytool`/formula steps was the lower-risk option and was recommended; the maintainer chose the idiomatic path | — Decided 2026-08-07 (maintainer directive, against recommendation). **Risk resolved by research the same day, and it hardened rather than cleared:** `goreleaser release` refuses a `dist/` built elsewhere, and both `--split`/`--merge` and the `prebuilt` builder are Pro-only — so the single `macos-latest` runner with `zig cc` on both linux legs is now the *only* OSS path, not the likely one. The remaining unknown is narrow and empirical: zig-cross-linux-from-macOS with CGo tree-sitter. Phase 1 spike, blocking |
| Archives *alongside* raw binaries, not instead of them | Stapling needs a container, brew wants a tarball, but `internal/upgrade`'s cosign/SLSA-verified contract is built on the raw byte stream (D-02/Finding 1). Publishing both keeps a shipped, attested upgrade path byte-unchanged while giving the browser-download and brew paths what they need | — Decided 2026-08-07, **confirmed cheap by research**: multiple `archives:` entries keyed by `id` are natively supported, so the raw-binary archive is untouched. Cost: asset count roughly doubles, and `checksum:` becomes live alongside `release.yml`'s hand-rolled equivalent — resolved by **deleting** the hand-rolled step, not reconciling two writers. `binary_signs:` replaces `signs: {artifacts: binary}` once both shapes coexist |
| Own tap (`seanb4t/homebrew-tap`), not homebrew-core | A tap is entirely under our control and ships on our schedule; homebrew-core adds an external review queue and notability criteria whose outcome we cannot schedule. The repo went public 2026-08-01, which is the prerequisite for a public tap | — Decided 2026-08-07. homebrew-core recorded as a deferred follow-up once adoption justifies it |
| **`homebrew_casks:`, not `brews:`** (corrects the 2026-08-07 scoping decision) | This milestone was scoped naming a *formula* via `brews:`. Three researchers independently found `brews:` deprecated in GoReleaser v2.10, `homebrew_casks:` the current OSS block, and Homebrew's own maintainers (Homebrew/brew #20291, merged 2025-07-22) recommending casks for taps distributing GoReleaser-precompiled binaries. GoReleaser's own tap dogfoods `notarize.macos` + `homebrew_casks` — the exact shape targeted here | ✓ Corrected 2026-08-07 with maintainer sign-off, before any planning was drawn over the falsified assumption. Consequence: `gh`/`lazygit`'s build-time updater-disable ldflag does not transfer (casks ship precompiled binaries), so the cask-compatible analogue is a `homebrew_casks.hooks.post.install` sentinel |
| **GoReleaser Pro evaluated, costed, and held as a fallback — not adopted** | Pro's `release --split`/`continue --merge` would **preserve the native darwin matrix** and eliminate the zig-cross spike entirely (merge does checksum/sign/SBOM/publish on one runner) — a *smaller* change to the existing pipeline than the OSS restructure. `notarize.macos_native` + `dmg:`/`pkg:` would also make stapling reachable. Pricing verified 2026-08-07: Personal $165/yr (no stated repo cap), Startup $247/yr (≤20 people, ≤5 repos) — roughly $41–62 per project per year across 4+ Go projects. **Against that:** Pro is not `go install`-able from a public module, so it detaches from `go.tool.mod` (which pins `goreleaser/v2 v2.17.1` today) and degrades three existing gates — `check:goreleaser`/DIST-01 validates with the OSS binary and would reject Pro-only keys while fork PRs cannot read `GORELEASER_KEY`; `TestGoreleaserPinParity` loses one side of its equality; and `tool-vuln`/VULN-01-02-03 stops covering the releaser that actually ships releases, turning `GO-2026-5932` from **accepted and measured** into **unmeasurable**. That last one is this repo's own recurring failure mode — a gate going blind, not a vulnerability getting worse. A closed-source binary also enters the trusted build path of a project whose differentiator is verifiability (it stays out of `go.mod` and the SBOM, but SLSA provenance describes the builder) | — Decided 2026-08-07: **not adopted now; held as a costed fallback.** The Phase 1 spike decides. **Pass:** `zig cc` cross-compiles CGo tree-sitter to linux/amd64 AND linux/arm64 from a macOS host, and the binaries run on real Linux → stay OSS, all three gates intact. **Fail:** buy Pro (tier per EULA) and reshape around `release --split`/`continue --merge`, with the three gate repairs as **explicit added scope**, not discovered mid-phase. Stapling stays deferred either way unless separately revisited — Pro makes it *reachable*, not automatic: GoReleaser's docs document stapling for neither notarize method, so an explicit `xcrun stapler` step may still be required. Note `release.yml` already carries a deliberate comment that no Pro directive is used anywhere; reversing that is a recorded decision, not a silent config change |
| Notarize but do **not** staple; document the offline gap | `stapler` attaches tickets only to `.app`/`.pkg`/`.dmg`, never a bare Mach-O or a `.zip`, and Quill has no staple command. Stapling would require GoReleaser Pro's `dmg:` block or a hand-built `.app` wrapper — the latter carrying a documented "deemed damaged" trap on inner-binary-only signing. GoReleaser itself ships its own CLI notarized-and-unstapled | ✓ Decided 2026-08-07 with maintainer sign-off. Gatekeeper passes via **online** ticket lookup; offline first launch fails and is recorded as a known limitation. Revisitable later on evidence of real user impact, not deferred by omission |
| `codegraph upgrade` refuses under a brew-managed install | Self-replacing the binary mutates the Caskroom (or Cellar) behind brew's back, leaving brew's manifest lying about the installed version. Refusing with a pointer to `brew upgrade codegraph` is the only option that keeps both install paths honest. **Amended 2026-08-11 (D-01, D-03, plan 04-03):** the mechanism is a path-shape match AND a Homebrew-authored `INSTALL_RECEIPT.json` install receipt at the matching ancestor — "resolve symlinks" alone understates what defeats the false-positive leg; the receipt is what a spoofed directory name cannot forge | — Decided 2026-08-07. Detection must be tested against a real brew-managed layout; a path-prefix guess is exactly the shape of gate this repo keeps finding cannot fire |
| **All 8 MCP tools register by default; `CODEGRAPH_MCP_TOOLS` narrows instead of widening** (supersedes v0.1 MCP-01) | v0.1's MCP-01 made `codegraph_explore` the only default-visible tool, gated behind an opt-in allowlist **no user-facing artifact named** — not `README.md`, not `serve --help`, not `docs/`. That default was implemented correctly and was still a failure in practice, because two other surfaces described a different system: `README.md` asserted "Eight tools are exposed" directly beneath the install snippet, and the MCP `instructions` string blamed visibility solely on index state — the one cause that *structurally cannot* produce a 1-tool list, since a missing index produces zero. An AND-gate of three; remove any one and it is caught in seconds. Offered the docs-only fix, the maintainer chose to invert the spec instead: make the advertised surface and the actual surface agree | ✓ Decided 2026-08-09, shipped in PR #44 / issue #43, human-verified in the live client. Read via `os.LookupEnv` so "unset" and "set to empty" answer differently. MCP-03 untouched. Folded in: all 8 tools re-annotated `readOnlyHint:true`/`openWorldHint:false` after a per-tool call-graph audit, discharging a Phase-3 deferral carried across two milestones. **Deliberate TS divergence, signed off** — `testdata/golden/README.md` records that TS v1.3.1 also gates `codegraph_node` off by default, so the opt-in default was inherited from upstream without its explanation and then contradicted in `README.md`; the maintainer ruled parity is a *functionality baseline*, not a binding constraint. Guarded against recurrence by `internal/mcp/instructions_contract_test.go`, which closes the SURF-01 bug class at the server level — the prior drift gate only ever compared per-tool *descriptions*, never the `instructions` string where the visibility contract lives. **The archived `v0.1-REQUIREMENTS.md` was deliberately NOT edited** — it accurately records what shipped |
| **zig-cross-from-macOS proven before restructuring** (D-01…D-04, Phase 1) | The whole single-runner migration rested on one unproven claim; a costed GoReleaser Pro fallback was named in advance and a FAIL-bar variation list (V1–V5) written into the canary header *before* the first dispatch, so exhausting it would declare failure falsifiably rather than by post-hoc rationalization | ✓ Good — PASSED on V1, first dispatch (run 31273571889), both linux legs executing on real non-emulated hardware. V2–V5 never needed; Pro never bought; the canary is permanent and re-fires on zig/runner/config/module changes |
| **Assert the relationship, not the cardinal** (emergent, Phases 1–4) | Count-based assertions encode today's answer and go red on correct change; property-based ones encode the rule | ✓ Good — `verify:release-assets` asserts set equality both directions rather than a fixed 17-asset total; T-01-38's guard invariant survived jobs growing 3 → 5 because it was written 1:1 rather than `== 3`; `TestCosignRequiringJobsInstallCosign` derives its job list from Taskfile preconditions instead of hardcoding it |
| **Remove the code path rather than guard it** (D-02, Phase 4) | A guard is a policy someone can reimplement incorrectly later; an absent path has nothing to get wrong, and is cheaper to audit | ✓ Good — `Options.Force` is never read (absence *is* the enforcement), `EvalSymlinks` errors return not-detected with no fallback, and Phase 3's self-authored `.codegraph-brew-install` sentinel was deleted in favour of Homebrew's own `INSTALL_RECEIPT.json` — a signal we cannot forget to write |
| **A gate is not trusted until demonstrated RED against a confirmed-applied mutation** (standing rule, exercised throughout v0.5.0) | Green means nothing if the gate could not have failed | ✓ Good — and it repeatedly caught real defects: a test that *pinned the broken* SBOM template demonstrated-RED would have resisted correction; the `dry-run-signed` additions-only diff guard is now a tracked todo precisely because it passes vacuously. Corollary learned the hard way, four times in one session: a query that does not match reality reports **absence**, not truth |
| **Ownership of a shared-array entry (JSON hooks, config blocks) must be exact-identity, never shape/position** (v0.10.0 Phase 7) | A "recovery" heuristic that re-claims ownership by matcher name + shape (rather than exact command-string match) cannot distinguish "our own hand-edited entry" from "an unrelated entry that happens to occupy the same slot" — the two are structurally indistinguishable without a positive identity marker. Guessing wrong in the unsafe direction (silently overwriting someone else's content) is strictly worse than guessing wrong safely (a harmless duplicate entry) | ✓ Learned the hard way: introduced mid-Wave-3 to fix a real duplicate-append bug, caught by an independent background security review before merge, confirmed RED against the vulnerable code, reverted same-day (commit `242ec0a`). `writeHookEntry`/`removeHookEntry` now match by exact command string only; a hand-edited own-entry duplicates rather than overwrites — the accepted, safe tradeoff. Same failure class Phase 7's own RESEARCH.md had already named going in (Pitfall 1) — naming a risk in research does not, by itself, stop an implementation detail from reintroducing it under a narrower, more plausible-looking gate |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-08-13 — v0.10.0 Phase 8 (Instructions & Marker-Block Rewrite) complete: 3/3 requirements validated, 3 plans/2 waves, the wire `instructions` const and marker block rewritten to point at the skill + resources Phases 5-7 shipped instead of a never-built "Phase 3" hand-off, 38 wire-oracle transcripts re-frozen in one reviewed-diff pass, deep code review clean (0 critical). **This was the milestone's last phase — all 4 phases (5-8) are now complete, 16/16 requirements validated; `/gsd-complete-milestone` has not yet been run.** Prior entry: 2026-08-13, v0.10.0 Phase 7 (`codegraph install` Skill + Hooks Distribution) complete: 3/3 requirements validated, 4 plans/4 waves, a real hook-ownership vulnerability found by independent security review and reverted same-day, a deep code review's 2 CRITICAL + 2/4 WARNING findings fixed with regression tests, UAT closed via live CLI execution rather than a human click-through, security audit closed 17/17 threats. New Key Decision recorded (exact-identity ownership for shared-array entries). Also backfilled: Phase 6 (Agent Skill Package — SKILL.md & SessionStart Nudge) validated entry, missed at the time of its own completion. Earlier entry: 2026-08-12, v0.10.0 Phase 5 (MCP Resources Capability & Claims Drift Guard) complete: 5/5 requirements validated, 4 plans, 10 resources served over the wire, structural drift guard proven red-then-reverted against 5 mutations. Earlier: 2026-08-12, milestone v0.10.0 started; defining requirements. Earlier still: 2026-08-12 after the **v0.5.0 milestone close** — macOS Distribution & Homebrew shipped 2026-08-11 and released as `v0.9.0`, 4 phases / 24 plans / 18 requirements, all four phases' requirements moved to Validated and four new Key Decisions recorded. Phase 4's `leg3_executed=no` residual closed by measurement on 2026-08-12 (real brew-installed v0.9.0 refusing correctly).*
