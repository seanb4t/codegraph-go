---
phase: 03-query-engine-mcp-server
plan: 06
type: tdd
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - internal/query/render_markdown.go
  - internal/query/node.go
  - internal/query/explore.go
  - internal/query/render_markdown_test.go
autonomous: true
requirements: [QRY-02, QRY-08]
must_haves:
  truths:
    - "node <symbol> renders the golden node.json markdown shape (name/kind, Location, Signature, Trail, Calls →, Called by ←)"
    - "node <file> renders a line-numbered, tab-indented verbatim read of the file"
    - "explore <query> renders the golden explore.json shape: exploration header, blast-radius bullets, the verbatim-source disclaimer, and per-file line-numbered source read fresh from disk"
  artifacts:
    - "internal/query/render_markdown.go — RenderNode/RenderExplore templates + the line-number/tab helper"
    - "internal/query/node.go — Engine.Node (symbol detail or file read)"
    - "internal/query/explore.go — Engine.Explore (selection + blast radius + fresh source)"
    - "internal/query/render_markdown_test.go — TestNode / TestExplore byte-shape assertions"
  key_links:
    - "explore/node read source fresh from disk (os.ReadFile) relative to the repo root, confined to it (path-traversal defense)"
    - "explore selection reuses the 03-03 matcher; blast radius reuses the 03-04 reverse adjacency; node Calls → uses forward IterateEdges, Called by ← uses reverse map"
    - "The disclaimer paragraph is copied verbatim from testdata/golden/corpus/weft-go/explore.json — not paraphrased (D-05a)"
---

<objective>
Implement the two markdown-only commands (D-05a/D-05b): `Engine.Node` (QRY-02 — symbol detail or line-numbered file read) and `Engine.Explore` (QRY-08 — one-round-trip verbatim source + call paths + blast radius). Both reproduce the golden markdown templates byte-for-byte in shape, including the exact section order, the tab-indented line-numbered fenced source blocks, and the verbatim-source disclaimer paragraph copied verbatim from `explore.json`.

Purpose: These are the flagship agent-facing outputs and the highest byte-fidelity risk (Pitfall 3: template drift). `node`/`explore` emit markdown text only (no `--json`) — the golden wraps them as `{command, output}` precisely because TS has no native `--json` for them (D-01b). Source is read fresh from disk on every call (D-05a) — the disk read is confined to the repo root (path-traversal defense-in-depth).
Output: `RenderNode`/`RenderExplore` + `Engine.Node`/`Engine.Explore` + the line-numbering helper, tested for byte-shape fidelity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-engine-mcp-server/03-PATTERNS.md
@internal/query/engine.go
@internal/query/search.go
@internal/query/traverse.go
@testdata/golden/corpus/weft-go/node.json
@testdata/golden/corpus/weft-go/explore.json
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): TestNode/TestExplore pin the golden markdown templates byte-for-byte</name>
  <read_first>
    - testdata/golden/corpus/weft-go/node.json (exact section order: **name** (kind), Location, Signature, Trail, Calls →, Called by ←)
    - testdata/golden/corpus/weft-go/explore.json (Exploration header, Found N symbol(s), Blast radius bullets, the disclaimer blockquote, per-file source blocks with tab-indented line numbers)
    - internal/query/search.go (Location + matcher to reuse for explore selection)
    - internal/query/traverse.go (reverse adjacency for callers/blast; forward edges for Calls →)
    - .planning/phases/03-query-engine-mcp-server/03-RESEARCH.md (Code Examples §explore.json / §node.json; Pitfall 3 template drift)
  </read_first>
  <files>internal/query/render_markdown_test.go</files>
  <behavior>
    - TestNode (symbol): rendering a fixture symbol produces the fixed sections in order — `**name** (kind)`, `**Location:** path:line`, `**Signature:** \`sig\``, the Trail line, `**Calls →**` (forward, comma-joined `name (path:line)`), `**Called by ←**` (reverse, same format).
    - TestNode (file): passing a file target produces a tab-indented, line-numbered verbatim read of that file.
    - TestExplore: rendering produces `**Exploration: <query>**`, blank line, `Found N symbol(s) across M file(s).`, the `**Blast radius ...**` header + one bullet per symbol (`- \`name\` (path:line) — N callers in \`path\`; tests: \`path\``), `**Source Code**`, the disclaimer blockquote (asserted equal to the exact substring extracted from explore.json — not a paraphrase), then per-file `**\`path\`** — sym(kind)` blocks with fenced source whose lines are `<n>\t<source>`.
    - The disclaimer assertion reads the expected text out of explore.json at test time so drift in either direction fails.
  </behavior>
  <action>
    Add `internal/query/render_markdown_test.go` (package `query`). Load `explore.json`/`node.json`, extract the `output` strings, and assert the rendered output matches on the fixed-text regions (headers, disclaimer, section labels, tab-and-line-number formatting) while allowing interpolated fields (names, paths, counts, source bytes) to vary with the fixture. For the disclaimer specifically, extract the exact paragraph from explore.json and assert byte-equality against the rendered block. RED — RenderNode/RenderExplore/Engine.Node/Engine.Explore do not exist yet.
  </action>
  <verify>
    <automated>go test ./internal/query/... -run 'TestNode|TestExplore' -count=1 2>&1 | tail -5; echo "expect: undefined Node/Explore (RED)"</automated>
  </verify>
  <acceptance_criteria>
    - `internal/query/render_markdown_test.go` references `e.Node(` and `e.Explore(`
    - The test extracts the disclaimer from explore.json and asserts byte-equality (no hardcoded paraphrase)
    - RED confirmed on undefined methods
    - Commit message prefix `test(03-06):`
  </acceptance_criteria>
  <done>Failing TestNode/TestExplore pin the byte-shape of both markdown templates against the golden.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement RenderNode/RenderExplore + Engine.Node/Engine.Explore</name>
  <read_first>
    - internal/query/render_markdown_test.go (RED byte-shape expectations)
    - internal/query/search.go (matcher for explore symbol selection)
    - internal/query/traverse.go (Callers/Callees + buildReverseAdjacency)
    - internal/query/resolve.go (repo root = parent of the resolved .codegraph dir, for the disk read)
  </read_first>
  <files>internal/query/render_markdown.go, internal/query/node.go, internal/query/explore.go</files>
  <action>
    Implement in `render_markdown.go` a shared line-numbering helper that renders a file's on-disk bytes as `<lineNo>\t<source>` inside a fenced block (tab indentation, matching the golden), plus `RenderNode` and `RenderExplore` producing the exact section order the tests pin. Copy the disclaimer paragraph verbatim from explore.json into `RenderExplore` (do not paraphrase; treat it as an agent-facing contract). In `node.go` implement `func (e *Engine) Node(symbol, file string) (string, error)`: when the target resolves to a symbol, render name/kind, Location, Signature, Trail, `Calls →` (forward `IterateEdges(srcID)`), `Called by ←` (reverse map); when the target is a file, render the line-numbered verbatim read. In `explore.go` implement `func (e *Engine) Explore(query string, maxFiles int) (string, error)`: select relevant symbols via the 03-03 matcher (capped by `maxFiles`/`validateMaxFiles`), compute the blast-radius bullets from the 03-04 reverse adjacency (caller counts grouped by file + a `tests:` list via the test-file heuristic), then render each file's verbatim source read fresh from disk. Resolve every on-disk read relative to the repo root (parent of the resolved `.codegraph`) and reject any path that escapes it (clean + confine — path-traversal defense). Colocate any node/explore result structs in these files. Do not run `go mod tidy`.
  </action>
  <verify>
    <automated>go test ./internal/query/... -run 'TestNode|TestExplore' -count=1 && go test ./internal/query/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/query/... -run TestNode -count=1` exits 0
    - `go test ./internal/query/... -run TestExplore -count=1` exits 0
    - `internal/query/explore.go` contains `func (e *Engine) Explore(` and reads source via `os.ReadFile`
    - `internal/query/node.go` contains `func (e *Engine) Node(`
    - Disk reads are confined to the repo root (a `..`-escaping file arg is rejected — asserted in test)
    - Full `go test ./internal/query/... -count=1` green (all engine verbs now present)
    - Commit message prefix `feat(03-06):`
  </acceptance_criteria>
  <done>node and explore reproduce the golden markdown shapes byte-for-byte, read source fresh from disk, and confine reads to the repo root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP → Node/Explore | `symbol`, `file`, `query`, `max-files` untrusted; node/explore then read files from disk |
| Engine → filesystem | os.ReadFile of source files during rendering — must stay within the repo root |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-Path | Tampering / Information Disclosure | explore/node disk read | high | mitigate | Resolve every source path relative to the repo root and reject any path that escapes it via `filepath.Clean` + prefix confinement; node `filePath` values come from the trusted graph but the `-f` arg is user input |
| T-03-06-DoS | Denial of Service | explore max-files | medium | mitigate | Cap files rendered via `validateMaxFiles` (03-02) before reading/allocating |
| T-03-06-Drift | Tampering | disclaimer paragraph | low | mitigate | Copy the disclaimer verbatim from explore.json; the byte-equality test prevents silent contract drift |
| T-03-SC | Tampering | package installs | low | accept | No dependency added here |
</threat_model>

<verification>
- `go test ./internal/query/... -count=1` fully green (every engine verb implemented)
- Disclaimer byte-equality and tab/line-number formatting asserted against the golden
- Path-escape rejection asserted
</verification>

<success_criteria>
`node` and `explore` produce output whose markdown shape matches the golden `node.json`/`explore.json` byte-for-byte in their fixed regions, read verbatim source fresh from disk, include the exact disclaimer, and cannot read outside the repo root.
</success_criteria>

<output>
Create `.planning/phases/03-query-engine-mcp-server/03-06-SUMMARY.md` when done.
</output>
