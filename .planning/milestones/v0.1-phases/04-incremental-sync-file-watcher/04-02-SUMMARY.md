---
phase: 04-incremental-sync-file-watcher
plan: 02
subsystem: database
tags: [protobuf, schema, query-engine, additive-schema, graphstore]

# Dependency graph
requires:
  - phase: 04-incremental-sync-file-watcher
    provides: "Plan 04-01's x/ file-owned secondary index namespace, IterateFileIndex, DeleteNode/DeleteEdge, extended DeleteFileSubgraph — the pruning storage substrate this plan's Meta.has_file_index flag detects the absence of"
  - phase: 03-query-engine-mcp-server
    provides: "internal/query's D-04 query-time reverse-adjacency scan (unexported buildReverseAdjacency) and its 6 in-package call sites (Callers/Impact/Affected/explore/node)"
provides:
  - "File.mtime_unix_ns (7) / File.size_bytes (8) — D-01a stat pre-filter fields, additive"
  - "Meta.has_file_index (7) — D-02b backfill-detection flag, additive"
  - "regenerated graph.pb.go with GetMtimeUnixNs/GetSizeBytes/GetHasFileIndex accessors"
  - "exported query.BuildReverseAdjacency — importable by internal/indexer without a circular dependency"
affects: [04-03, sync-engine, indexer-sync]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive protobuf field extension: next-free field numbers below the reserved 50..59 range, doc comment cites the decision id and writer, SchemaVersion never bumped"
    - "Mechanical exported-rename: capitalize the identifier, update every in-package call site (including doc-comment prose references), preserve the existing behavioral discipline verbatim"

key-files:
  created: []
  modified:
    - internal/schema/graph.proto
    - internal/schema/graph.pb.go
    - internal/query/traverse.go
    - internal/query/traverse_test.go
    - internal/query/explore.go
    - internal/query/node.go

key-decisions:
  - "Meta.has_file_index landed as field 7 (next free number after health_message=6) — 04-01 did not already add this flag, confirmed by reading graph.proto before editing, so no reconciliation was needed"
  - "Fixed a stray lowercase buildReverseAdjacency reference inside a traverse_test.go doc comment (not a call site) so the plan's verification grep (`! grep -rn 'buildReverseAdjacency' internal/query/`) passes cleanly — comment-only, no behavior change"

patterns-established: []

requirements-completed: [INDX-03]

coverage:
  - id: D1
    description: "File.mtime_unix_ns and File.size_bytes fields exist, additive, with regenerated GetMtimeUnixNs/GetSizeBytes accessors"
    requirement: "INDX-03"
    verification:
      - kind: unit
        ref: "go test ./internal/schema/... -count=1"
        status: pass
      - kind: other
        ref: "grep -q 'func (x *File) GetMtimeUnixNs()' internal/schema/graph.pb.go"
        status: pass
    human_judgment: false
  - id: D2
    description: "Meta.has_file_index field exists, additive, with regenerated GetHasFileIndex accessor; SchemaVersion stays 1"
    requirement: "INDX-03"
    verification:
      - kind: unit
        ref: "go test ./internal/schema/... -count=1"
        status: pass
      - kind: other
        ref: "grep -q 'func (x *Meta) GetHasFileIndex()' internal/schema/graph.pb.go; grep -c 'SchemaVersion uint32 = 1' internal/schema/meta.go == 1"
        status: pass
    human_judgment: false
  - id: D3
    description: "query.BuildReverseAdjacency is exported and importable from internal/indexer without a circular dependency; all in-package call sites updated; query tests still green"
    requirement: "INDX-03"
    verification:
      - kind: unit
        ref: "go build ./... && go test ./internal/query/... -race -count=1"
        status: pass
      - kind: other
        ref: "grep -q 'func BuildReverseAdjacency' internal/query/traverse.go && ! grep -rn 'buildReverseAdjacency' internal/query/"
        status: pass
    human_judgment: false

duration: 8min
completed: 2026-07-11
status: complete
---

# Phase 4 Plan 2: Additive Sync Schema Fields + Exported Reverse-Adjacency Summary

**Additive `File.mtime_unix_ns`/`size_bytes` + `Meta.has_file_index` protobuf fields regenerated into `graph.pb.go`, plus `query.buildReverseAdjacency` exported as `BuildReverseAdjacency` for Plan 04-03's `indexer.Sync()` to reuse without a circular import.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-11T18:55:44Z
- **Completed:** 2026-07-11T18:59:05Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- `File.mtime_unix_ns` (field 7) and `File.size_bytes` (field 8) added to `graph.proto`, regenerated via `protoc --go_out=paths=source_relative:. --proto_path=.`; `graph.pb.go` exposes `GetMtimeUnixNs()`/`GetSizeBytes()` and still carries the `// Code generated by protoc-gen-go. DO NOT EDIT.` header
- `Meta.has_file_index` (field 7) added for D-02b backfill detection; `GetHasFileIndex()` accessor confirmed present
- `SchemaVersion` unchanged at 1 (`grep -c 'SchemaVersion uint32 = 1' internal/schema/meta.go` == 1); Phase-1 round-trip test (`go test ./internal/schema/...`) passes unchanged, proving forward/backward compatibility
- `query.buildReverseAdjacency` renamed to `BuildReverseAdjacency` — a pure mechanical capitalize-and-update-call-sites change (no logic touched); all 6 in-package call sites (traverse.go's Callers/Impact/Affected, explore.go:124, node.go:176) updated, plus one stray doc-comment reference in traverse_test.go
- `go build ./...` and `go test ./internal/schema/... ./internal/query/... -race -count=1` both green

## Task Commits

Each task was committed atomically:

1. **Task 1: add additive File/Meta fields and regenerate graph.pb.go** - `9f52b1a` (feat)
2. **Task 2: export query.BuildReverseAdjacency for indexer reuse** - `7de3c6e` (refactor)

## Files Created/Modified
- `internal/schema/graph.proto` - Added `File.mtime_unix_ns=7`, `File.size_bytes=8`, `Meta.has_file_index=7`, each with a D-01a/D-02b-citing doc comment matching the existing field-comment voice
- `internal/schema/graph.pb.go` - Regenerated via protoc; new `GetMtimeUnixNs`/`GetSizeBytes`/`GetHasFileIndex` accessors, generated-header preserved
- `internal/query/traverse.go` - Renamed `buildReverseAdjacency` -> `BuildReverseAdjacency`; doc comment gained a note on the new external caller (internal/indexer.Sync()) and the fresh-per-call discipline it must also follow; 3 in-file call sites updated (Callers/Impact/Affected)
- `internal/query/traverse_test.go` - One doc-comment prose reference updated to match the rename (no test logic changed)
- `internal/query/explore.go` - Call site at line 124 updated
- `internal/query/node.go` - Call site at line 176 updated

## Decisions Made
- Confirmed via direct read of `graph.proto` (not assumed from RESEARCH) that 04-01 had not already added `Meta.has_file_index` — no reconciliation needed, field 7 was genuinely free
- Extended the doc-comment fix beyond the two files named in the plan's `<files>` block (`traverse.go`/`explore.go`/`node.go`) to also touch `traverse_test.go`, since the plan's own verification command (`! grep -rn 'buildReverseAdjacency' internal/query/`) required every occurrence — including a prose comment — to be renamed; this is comment-only and changes no behavior

## Deviations from Plan

None - plan executed exactly as written. The one adjustment (fixing the stray lowercase reference in `traverse_test.go`'s doc comment) was required to satisfy the plan's own stated verification command and is comment-only — not a Rule 1-4 deviation in the functional sense, just completing the mechanical rename the plan specified.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `File.mtime_unix_ns`/`size_bytes` and `Meta.has_file_index` are in place for Plan 04-03's `Sync()` stat pre-filter (D-01a) and backfill detection (D-02b)
- `query.BuildReverseAdjacency` is exported and ready for `internal/indexer.Sync()` to import for D-02a dependent-file recomputation — confirmed as a new, non-circular `internal/indexer -> internal/query` import edge (no code in `internal/indexer` imports it yet; that wiring is Plan 04-03's work)
- No backfill/detection logic itself (reading `Meta.has_file_index` and triggering a one-time reindex) was built in this plan — that remains for whichever plan implements `Sync()`, per 04-01's summary noting the same for the storage-side backfill mechanism

---
*Phase: 04-incremental-sync-file-watcher*
*Completed: 2026-07-11*

## Self-Check: PASSED

All key files and both task commits (9f52b1a feat, 7de3c6e refactor) verified present.
