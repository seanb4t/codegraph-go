---
phase: 05-language-coverage-resolution-breadth
plan: 07
type: tdd
wave: 4
depends_on: [05-03]
files_modified:
  - internal/indexer/tsextract/tsextract.go
  - internal/indexer/tsextract/types.go
  - internal/indexer/tsextract/tsextract_test.go
  - internal/indexer/languages_typescript.go
  - testdata/golden/parity_tsjs_test.go
autonomous: true
requirements: [LANG-05]
must_haves:
  truths:
    - "TypeScript, TSX, and JavaScript files all get full structural extraction through one shared tsextract package (three grammars, one extractor)"
    - "Cross-file resolution links TS/JS imports, calls, and class inheritance via tsconfig-aware resolved module specifiers"
    - "class extends/implements clauses are emitted as supertype refs for resolve-time promotion"
    - "TS/JS parity is validated shape-not-byte against a real TS/JS repo (or source-as-spec fallback)"
  artifacts:
    - internal/indexer/tsextract/tsextract.go
    - internal/indexer/languages_typescript.go
    - testdata/golden/parity_tsjs_test.go
  key_links:
    - "languages_typescript.go init() → registerLanguage for typescript (.ts), tsx (.tsx), javascript (.js/.jsx) → shared tsextract.Extract"
    - "tsconfig paths/baseUrl + relative specifiers → resolved module specifier → symbolIndex outer key"
---

<objective>
Deliver full TypeScript/JavaScript structural extraction + cross-file resolution (LANG-05): one shared `tsextract` package serving three grammars (TypeScript, TSX, JavaScript — two constructors from 05-01 plus JavaScript), the `LanguageSpec` registrations, a tsconfig-aware module-specifier resolver, and shape-not-byte golden parity against a real TS/JS repo. TS/JS module resolution is the least directory-structure-stable of the priority-4 and needs `tsconfig.json`/`package.json` grounding.

Purpose: Fourth priority-4 language; exercises the hardest cross-file module-resolution model.
Output: `tsextract.Extract`, TS/TSX/JS registry entries, TS/JS golden-parity test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@.planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md
@internal/indexer/goextract/goextract.go
@internal/indexer/goextract/types.go
@internal/indexer/languages.go
@internal/parser/cgo/parser_cgo.go
@testdata/golden/golden_parity_test.go
</context>

## Artifacts this phase produces (this plan's contribution)

- `internal/indexer/tsextract/tsextract.go` — one `Extract(p, moduleKey, relPath, src)` shared across TypeScript/TSX/JavaScript
- `internal/indexer/languages_typescript.go` — three `LanguageSpec` registrations (ID "typescript" .ts→NewTypeScriptParser, ID "tsx" .tsx→NewTSXParser, ID "javascript" .js/.jsx→NewJavaScriptParser), all Extract→tsextract.Extract, tsconfig-aware `ModuleKey`, `tsconfig.json`+`package.json` `Descriptor`
- `testdata/golden/parity_tsjs_test.go` — `TestGoldenParity_TSJS`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shared tsextract package + TS/TSX/JS LanguageSpec registrations</name>
  <behavior>
    - A .ts file (class, interface, functions, methods), a .tsx file (with JSX), and a .js file all extract nodes mapped to the shared vocabulary through one extractor
    - `import`/`export ... from` produce RefKindImports; calls produce RefKindCalls; `class X extends Y implements Z` produces RefKindEmbeds-shaped supertype refs for both extends and implements (Pattern 2)
    - JSX elements in a .tsx file parse without error (via the tsx grammar) and do not break extraction of surrounding declarations
    - The three languages resolve by their IDs and extensions and all dispatch to tsextract.Extract
    - Parse-failure skip contract preserved (JS/TS/TSX all carry external scanners)
  </behavior>
  <read_first>
    - internal/indexer/goextract/goextract.go (skip contract + ordered collect phases + call collection)
    - internal/indexer/goextract/types.go (shared vocabulary)
    - internal/parser/cgo/parser_cgo.go (NewTypeScriptParser/NewTSXParser from 05-01 + NewJavaScriptParser)
    - internal/indexer/languages.go + languages_go.go (registration shape; multiple registrations per file)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Standard Stack" (TS two-grammar note), §"Alternatives Considered" (TSX), §"Pattern 2", §"Don't Hand-Roll" (tsconfig paths/baseUrl + exports resolution), Assumptions Log A1
  </read_first>
  <action>
    Create package tsextract with ONE `Extract(p, moduleKey, relPath, src)` that walks the ECMAScript/TypeScript node vocabulary shared across the three grammars (their node types overlap heavily): `class_declaration`→KindStruct, `interface_declaration`→KindInterface (TS only; absent in JS — fine), `function_declaration`/`method_definition`→KindFunction/KindMethod, `import_statement`/`export_statement` with `from`→RefKindImports, `call_expression`→RefKindCalls, `type_alias_declaration`→KindTypeAlias. For `class_heritage` (`extends X implements Y`) emit RefKindEmbeds-shaped supertype refs for each clause (Pattern 2 resolve-time promotion). Reproduce goextract's skip/error contract. Import the shared `goextract` vocabulary; never redefine it. Create languages_typescript.go (package indexer) with `init()` registering THREE specs sharing tsextract.Extract: ID "typescript" Extensions [".ts"] NewParser `cgo.NewTypeScriptParser`; ID "tsx" Extensions [".tsx"] NewParser `cgo.NewTSXParser`; ID "javascript" Extensions [".js", ".jsx"] NewParser `cgo.NewJavaScriptParser`. Implement a tsconfig-aware ModuleKey: resolve a file's module specifier using `tsconfig.json` `paths`/`baseUrl` remapping + relative-specifier + `package.json` `main`/`exports` order (Don't Hand-Roll table); Descriptor parses `tsconfig.json`+`package.json` once per root (path-identity fallback when absent). Note `exports`-map conditional resolution as a documented risk (A1).
  </action>
  <verify>
    <automated>go test ./internal/indexer/tsextract/ ./internal/indexer/ -run 'TestTS|TestLanguageRegistry' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - tsextract_test.go (table-driven) asserts node-kind mapping across .ts/.tsx/.js sources and RefKind emission for import/call/extends/implements
    - A .tsx JSX fixture parses and extracts surrounding declarations without error
    - `lookupLanguageByID` returns specs for "typescript"/"tsx"/"javascript"; `lookupLanguageByExt` maps .ts/.tsx/.js/.jsx correctly, all to tsextract.Extract
    - tsconfig-aware ModuleKey resolves a `paths`-mapped and a relative specifier; path-identity fallback without a descriptor
  </acceptance_criteria>
  <done>TS, TSX, and JS all extract through one shared package and are registered with tsconfig-aware resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TS/JS cross-file resolution validation + golden parity</name>
  <behavior>
    - A multi-file TS/JS fixture with relative-specifier imports, a tsconfig `paths`-aliased import, and cross-file calls resolves into imports/calls edges via the resolved module specifier
    - The golden-parity test self-skips when the TS/JS corpus is absent, otherwise diffs shape-not-byte
  </behavior>
  <read_first>
    - testdata/golden/golden_parity_test.go (skip-if-absent pattern, shape comparison)
    - internal/indexer/resolve.go (module-key resolution)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Environment Availability", §"Don't Hand-Roll" (TS resolution order)
    - .planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md D-12
  </read_first>
  <action>
    Add a multi-file TS/JS resolution fixture asserting relative-specifier, `paths`-aliased, and cross-file calls resolve into edges via the resolved module specifier. Create testdata/golden/parity_tsjs_test.go with `TestGoldenParity_TSJS` mirroring the resolveWeftCorpus skip-if-absent discipline and shape-not-byte comparison; the corpus should include both `.ts` and `.js` (and ideally `.tsx`) files. Select a representative, license-clean real TS/JS repo (Claude's Discretion) and document it. Apply the live-TS-CLI-unavailable fallback and record it if fresh golden capture is not possible.
  </action>
  <verify>
    <automated>go test ./internal/indexer/... ./testdata/golden/ -run 'TestTS|TestGoldenParity_TSJS' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TS/JS resolution fixture asserts relative, paths-aliased, and cross-file references resolve into edges
    - `TestGoldenParity_TSJS` self-skips cleanly when the corpus is absent
    - When present, shape diff is within documented tolerance
  </acceptance_criteria>
  <done>TS/JS is validated at full extraction + tsconfig-aware cross-file resolution, shape-not-byte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS/TSX/JS source → grammars | untrusted source parsed in-process (external scanners: template literals, regex) |
| tsconfig.json/package.json → Descriptor | untrusted config read for module resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-DoS | Denial of Service | tsextract external-scanner grammars | high | mitigate | `MaxSourceBytes` bounds every parse across all three grammars; in-process crash risk is the accepted contract (Pitfall 4) |
| T-05-Manifest | Spoofing | tsconfig/package.json Descriptor | low | accept | Malformed config → path-identity fallback; no crash/code-exec |
| T-05-SC | Tampering | tree-sitter-javascript/typescript modules | low | accept | `tree-sitter`-org modules pinned (v0.25.0 / v0.23.2), VERIFIED |
</threat_model>

<verification>
- `go test ./internal/indexer/tsextract/ ./internal/indexer/ ./testdata/golden/ -count=1` green
- Go/Java/C#/Python fixtures unaffected by TS/JS registration
</verification>

<success_criteria>
TypeScript, TSX, and JavaScript have full extraction through one shared extractor with tsconfig-aware cross-file resolution validated shape-not-byte.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-07-SUMMARY.md` when done.
</output>
