---
phase: 05-language-coverage-resolution-breadth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/parser/cgo/parser_cgo.go
  - internal/indexer/goextract/types.go
  - internal/indexer/languages.go
  - internal/indexer/languages_go.go
  - internal/indexer/languages_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [LANG-02, LANG-03, LANG-04, LANG-05]
must_haves:
  truths:
    - "A LanguageSpec registry keyed by language ID is the single source of truth for parser+extractor+ModuleKey selection"
    - "Priority-4 grammar parsers (Java, C#, JavaScript, TypeScript, TSX) construct without error through the existing parser.Parser seam"
    - "Go's own extraction path is registered as the first LanguageSpec, proving the seam before any new language uses it"
  artifacts:
    - internal/indexer/languages.go
    - internal/indexer/languages_go.go
    - "internal/parser/cgo/parser_cgo.go (NewJavaParser, NewCSharpParser, NewJavaScriptParser, NewTypeScriptParser, NewTSXParser)"
    - "goextract/types.go KindRoute constant"
  key_links:
    - "registry lookup by extension and by language ID → LanguageSpec.NewParser / LanguageSpec.Extract"
    - "each New<Lang>Parser routes through newCGoParser + parser.MaxSourceBytes ceiling (no bypass)"
---

<objective>
Establish the D-01 multi-language seam: a `LanguageSpec` registry keyed by language ID, the priority-4 CGo grammar parser constructors, the priority-4 grammar `go.mod` pins, and the additive shared-vocabulary extension (`KindRoute`). Register Go as the first `LanguageSpec` so the seam is proven end-to-end against an existing, working extractor before any new language is added.

Purpose: Every subsequent Wave (discovery, extract, resolve, per-language extractors, dispatch, routing) reads this registry. Nothing else can land before it.
Output: `internal/indexer/languages.go` + `languages_go.go`, five new `cgo.New<Lang>Parser` constructors, four grammar pins in `go.mod`, and the `KindRoute` vocabulary constant.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PARSER-DECISION.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@.planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md
@internal/parser/cgo/parser_cgo.go
@internal/parser/parser.go
@internal/indexer/goextract/types.go
@internal/indexer/goextract/goextract.go
@internal/indexer/discover.go
@internal/indexer/extract.go
</context>

## Artifacts this phase produces (this plan's contribution)

- `LanguageSpec` struct + package-level `registry` (`map[string]LanguageSpec`) + `registerLanguage(spec)` + `lookupLanguageByID(id)` + `lookupLanguageByExt(ext)` in `internal/indexer/languages.go`
- `internal/indexer/languages_go.go` — Go's `LanguageSpec` registration (`init()` calling `registerLanguage`)
- `cgo.NewJavaParser`, `cgo.NewCSharpParser`, `cgo.NewJavaScriptParser`, `cgo.NewTypeScriptParser`, `cgo.NewTSXParser` in `internal/parser/cgo/parser_cgo.go`
- `goextract.KindRoute = "route"` node-kind constant in `internal/indexer/goextract/types.go`
- Grammar `go.mod` requires: `tree-sitter-java@v0.23.5`, `tree-sitter-c-sharp@v0.23.5`, `tree-sitter-javascript@v0.25.0`, `tree-sitter-typescript@v0.23.2`

<tasks>

<task type="auto">
  <name>Task 1: Priority-4 CGo parser constructors + grammar pins</name>
  <read_first>
    - internal/parser/cgo/parser_cgo.go (mirror NewGoParser/NewPythonParser lines 32-42; newCGoParser lines 44-52)
    - internal/parser/parser.go (the parser.Parser seam + MaxSourceBytes ceiling contract)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Package Legitimacy Audit" and §"Installation (priority-4)" (exact module paths + pinned versions)
    - go.mod (existing tree-sitter-go / tree-sitter-python requires to mirror)
  </read_first>
  <action>
    Add five constructors to internal/parser/cgo/parser_cgo.go following the exact one-liner shape of NewGoParser/NewPythonParser — each returns `newCGoParser(tree_sitter_<lang>.Language())`: `NewJavaParser`, `NewCSharpParser`, `NewJavaScriptParser`, `NewTypeScriptParser`, `NewTSXParser`. TypeScript ships two grammars in one module (`typescript/` and `tsx/` subdirs) — `NewTypeScriptParser` uses the typescript language accessor and `NewTSXParser` uses the tsx accessor from the `tree-sitter-typescript/bindings/go` binding (confirm the exact exported accessor names against the module's bindings/go package before wiring; do NOT guess). Pin grammar modules via `go get`: `github.com/tree-sitter/tree-sitter-java@v0.23.5`, `github.com/tree-sitter/tree-sitter-c-sharp@v0.23.5`, `github.com/tree-sitter/tree-sitter-javascript@v0.25.0`, `github.com/tree-sitter/tree-sitter-typescript@v0.23.2`. Add the import blocks (`tree_sitter_java "github.com/tree-sitter/tree-sitter-java/bindings/go"`, etc.). Do NOT run `go mod tidy` (project convention — it would strip still-unimported pins); promote to direct requires by hand if needed. Every constructor MUST route through the existing `newCGoParser` so the `parser.MaxSourceBytes` (4 MiB) ceiling in `Parse` is enforced uniformly — never add a parser path that bypasses `Parse` (Security Domain V5, threat T-05-DoS).
  </action>
  <verify>
    <automated>go build ./internal/parser/... && go test ./internal/parser/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/parser/...` succeeds with all five new constructors compiled
    - Each of `NewJavaParser`, `NewCSharpParser`, `NewJavaScriptParser`, `NewTypeScriptParser`, `NewTSXParser` returns a non-nil `*CGoParser` and a nil error when called
    - `go.mod` contains the four pinned grammar requires at the exact versions above; `go.sum` updated
    - No new code path parses source without going through `CGoParser.Parse` (grep confirms every constructor calls `newCGoParser`)
  </acceptance_criteria>
  <done>Priority-4 grammars parse a trivial valid source snippet per language without error, all through the size-ceiling-guarded Parse seam.</done>
</task>

<task type="auto">
  <name>Task 2: LanguageSpec registry + additive vocabulary + Go registration</name>
  <read_first>
    - internal/indexer/goextract/types.go (Kind*/RefKind* constants + FileResult/ExtractedNode/IntraEdge/UnresolvedRef — extend additively, do NOT fork)
    - internal/indexer/goextract/goextract.go (Extract signature lines 19-51 — Go's registry Extract entry point)
    - internal/indexer/discover.go (importPathFor / readModulePath — Go's ModuleKey + Descriptor becomes the first hook implementation)
    - internal/indexer/extract.go (defaultParserFactory / goextract.Extract call site the registry replaces)
    - .planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md §"internal/indexer/languages.go" and §"Registry-keyed-by-ID"
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Code Examples" LanguageSpec shape
  </read_first>
  <action>
    Add `KindRoute = "route"` to the node-kind const block in internal/indexer/goextract/types.go — additive only, do not rename/remove existing Kind*/RefKind* constants (D-02a additive discipline; D-01 mandates reusing FileResult/ExtractedNode/IntraEdge/UnresolvedRef unchanged). Create internal/indexer/languages.go (package indexer) defining: `LanguageSpec` struct with fields `ID string`, `Extensions []string`, `NewParser func() (parser.Parser, error)`, `Extract func(p parser.Parser, moduleKey, relPath string, src []byte) (goextract.FileResult, error)`, `ModuleKey func(descriptor ProjectDescriptor, relPath string) string`, and `Descriptor func(root string) (ProjectDescriptor, error)`; a package-level `var registry = map[string]LanguageSpec{}`; `registerLanguage(spec LanguageSpec)` that indexes the spec by `spec.ID` and by each `spec.Extensions` entry into a companion `var extToLang = map[string]string{}`; `lookupLanguageByID(id string) (LanguageSpec, bool)`; and `lookupLanguageByExt(ext string) (LanguageSpec, bool)`. Declare a minimal placeholder `ProjectDescriptor` type here (an interface or opaque struct — Wave 2 fleshes it out in discover.go; keep it forward-compatible, e.g. an interface with a `ModulePath() string` accessor or a struct the descriptor hook returns). Create internal/indexer/languages_go.go (package indexer) with an `init()` calling `registerLanguage` for `ID: "go"`, `Extensions: []string{".go"}`, `NewParser: func() (parser.Parser, error) { return cgo.NewGoParser() }`, `Extract: goextract.Extract` (signatures already match — importPath == moduleKey), and Go's ModuleKey/Descriptor delegating to the existing `importPathFor`/`readModulePath` logic (Wave 2 finalizes the hook shape; for now wire what compiles against the placeholder ProjectDescriptor). Follow the "package-level var registry, looked up by stable string ID, never rebuilt per call" shape (PATTERNS §Registry-keyed-by-ID).
  </action>
  <verify>
    <automated>go build ./internal/indexer/... && go test ./internal/indexer/ -run TestLanguageRegistry -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/indexer/...` succeeds
    - languages_test.go asserts `lookupLanguageByID("go")` returns ok=true with a spec whose Extensions contains ".go"
    - languages_test.go asserts `lookupLanguageByExt(".go")` returns the Go spec
    - languages_test.go asserts the Go spec's `NewParser()` returns a non-nil parser and nil error, and `Extract` is non-nil
    - `goextract.KindRoute == "route"` and every pre-existing Kind*/RefKind* constant is unchanged (grep confirms no deletions)
  </acceptance_criteria>
  <done>The registry resolves Go by both ID and extension and hands back a working parser+extractor; the shared vocabulary carries the new KindRoute constant additively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| indexed source → grammar parser | arbitrary third-party source bytes cross into the CGo tree-sitter grammars |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-DoS | Denial of Service | new CGo grammars (4 of 4 priority-4 carry external scanners except Java) | high | mitigate | Every New<Lang>Parser routes through `newCGoParser`/`Parse`, which enforces `parser.MaxSourceBytes` (4 MiB) before any grammar sees bytes — no parser path bypasses the ceiling (Security Domain V5) |
| T-05-Crash | Tampering | external-scanner grammars (C#, JS, TS/TSX) run in-process, segfault not recover()-able | medium | accept | Accepted risk with `MaxSourceBytes` front-line mitigation (Pitfall 4); documented as a shared caveat in the D-11 matrix (Wave 7), not introduced-new by this plan |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package-manager installs in this plan — all deps are `tree-sitter`-org Go modules pinned by exact semver (VERIFIED against proxy.golang.org in RESEARCH); Swift/Kotlin `[SUS]` grammars are handled behind a human-verify gate in a later plan |
</threat_model>

<verification>
- `go build ./...` succeeds with the four new grammar pins compiled in (CGo toolchain present per Phase 1)
- `go test ./internal/parser/... ./internal/indexer/ -count=1` green
- Registry resolves Go by ID and by extension; no existing Go extraction behavior changed (Go golden-parity fixtures untouched this plan)
</verification>

<success_criteria>
The multi-language seam exists and is proven against Go: a language-ID-keyed registry returns a working parser + extractor + ModuleKey/Descriptor hook, five priority-4 parser constructors compile and parse, and the shared vocabulary carries KindRoute additively with zero schema-version bump.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-01-SUMMARY.md` when done.
</output>
