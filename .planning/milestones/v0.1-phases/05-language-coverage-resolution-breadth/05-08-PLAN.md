---
phase: 05-language-coverage-resolution-breadth
plan: 08
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - internal/parser/cgo/parser_cgo.go
  - go.mod
  - go.sum
autonomous: false
requirements: [LANG-06]
user_setup: []
must_haves:
  truths:
    - "All seven mainstream-tier grammar parsers (Rust, Ruby, PHP, C, C++, Swift, Kotlin) construct through the existing parser.Parser seam with the MaxSourceBytes ceiling enforced"
    - "The five tree-sitter-org mainstream grammars are pinned at their VERIFIED semver versions"
    - "The two [SUS] community grammars (Swift, Kotlin) are only pinned after an explicit human-verify approval, by exact commit pseudo-version, never @latest"
  artifacts:
    - "cgo.NewRustParser, NewRubyParser, NewPHPParser, NewCParser, NewCppParser, NewSwiftParser, NewKotlinParser"
    - "mainstream grammar go.mod pins"
  key_links:
    - "each New<Lang>Parser routes through newCGoParser + MaxSourceBytes (no bypass)"
    - "Swift/Kotlin go get is gated behind the checkpoint (supply-chain, DIST-05 audited-dependency discipline)"
---

<objective>
Wire the mainstream-tier grammar layer: seven CGo parser constructors + `go.mod` pins for Rust, Ruby, PHP, C, C++, Swift, and Kotlin. The five `tree-sitter`-org grammars are pinned by exact semver; the two `[SUS]` community grammars (Swift, Kotlin) are gated behind a blocking human-verify checkpoint before pinning, per the RESEARCH Package Legitimacy Audit and DIST-05 audited-dependency discipline.

Purpose: This is the file-disjoint enabler for the mainstream extractor plans (05-10, 05-11) — centralizing all `parser_cgo.go`/`go.mod` edits here lets the extractor plans run parallel without touching build config.
Output: seven `cgo.New<Lang>Parser` constructors and the mainstream grammar pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PARSER-DECISION.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@internal/parser/cgo/parser_cgo.go
@internal/parser/parser.go
</context>

## Artifacts this phase produces (this plan's contribution)

- `cgo.NewRustParser`, `cgo.NewRubyParser`, `cgo.NewPHPParser`, `cgo.NewCParser`, `cgo.NewCppParser`, `cgo.NewSwiftParser`, `cgo.NewKotlinParser`
- `go.mod` pins: `tree-sitter-rust@v0.24.2`, `tree-sitter-ruby@v0.23.1`, `tree-sitter-php@v0.24.2`, `tree-sitter-c@v0.24.2`, `tree-sitter-cpp@v0.23.4`, `alex-pinkus/tree-sitter-swift@<pinned-commit>`, `fwcd/tree-sitter-kotlin/bindings/go@<pinned-commit>`

<tasks>

<task type="auto">
  <name>Task 1: tree-sitter-org mainstream constructors + pins (Rust, Ruby, PHP, C, C++)</name>
  <read_first>
    - internal/parser/cgo/parser_cgo.go (NewGoParser/NewPythonParser shape + newCGoParser)
    - internal/parser/parser.go (MaxSourceBytes ceiling contract)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Package Legitimacy Audit" + §"Installation (mainstream-6)" (exact module paths, versions, php/src not php_only)
  </read_first>
  <action>
    Add `NewRustParser`, `NewRubyParser`, `NewPHPParser`, `NewCParser`, `NewCppParser` to internal/parser/cgo/parser_cgo.go following the exact one-liner `newCGoParser(tree_sitter_<lang>.Language())` shape. For PHP, use the `php/src` grammar (the `php/bindings/go` accessor), NOT `php_only`. `go get` the pins: `github.com/tree-sitter/tree-sitter-rust@v0.24.2`, `github.com/tree-sitter/tree-sitter-ruby@v0.23.1`, `github.com/tree-sitter/tree-sitter-php@v0.24.2`, `github.com/tree-sitter/tree-sitter-c@v0.24.2`, `github.com/tree-sitter/tree-sitter-cpp@v0.23.4`. Add the import lines; do NOT run `go mod tidy` (project convention). Confirm each grammar module's own `go.mod` requires `go-tree-sitter` in the v0.24.0–v0.25.0 range so MVS keeps the already-pinned v0.25.0 (RESEARCH verified no conflict). Every constructor routes through `newCGoParser`/`Parse` so `MaxSourceBytes` is enforced.
  </action>
  <verify>
    <automated>go build ./internal/parser/... && go test ./internal/parser/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The five constructors compile and each returns a non-nil parser + nil error
    - `go.mod` contains the five pins at the exact versions; `go.sum` updated
    - PHP uses the `php/src` grammar accessor (grep confirms)
    - No parser path bypasses `newCGoParser`/`Parse`
  </acceptance_criteria>
  <done>The five tree-sitter-org mainstream grammars construct through the size-ceiling-guarded seam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human-verify gate for [SUS] Swift/Kotlin grammar pins</name>
  <action>Blocking supply-chain checkpoint (threat T-05-SC): pause before pinning the two community-maintained [SUS] grammars. The human verifies each grammar's legitimacy on pkg.go.dev, chooses exact commit pseudo-versions to pin (never @latest), and approves or rejects. Not auto-approvable regardless of auto_advance. On approval, Task 3 pins by the approved commits; on rejection, Swift/Kotlin are recorded as none-coverage for the D-11 matrix.</action>
  <what-built>
    The five tree-sitter-org mainstream grammars are pinned and constructing. The next task pins the two `[SUS]` community grammars — `alex-pinkus/tree-sitter-swift` and `fwcd/tree-sitter-kotlin/bindings/go` — which the RESEARCH Package Legitimacy Audit flagged: individual-maintainer repos, NOT `tree-sitter`-org, with no proper semver Go releases (commit pseudo-version pins only). This is a real supply-chain quality step-down (threat T-05-SC).
  </what-built>
  <how-to-verify>
    1. Open https://pkg.go.dev/github.com/alex-pinkus/tree-sitter-swift and confirm the module exists, is actively maintained, and pick the exact commit to pin (record the pseudo-version, e.g. `v0.0.0-YYYYMMDDHHMMSS-<commit>`).
    2. Open https://pkg.go.dev/github.com/fwcd/tree-sitter-kotlin and confirm the importable package is the `bindings/go` subpath (the root `v0.3.2` tag has no `require` line, so the real package is the unversioned `bindings/go` — pin that subpath by exact commit).
    3. Review each repo's recent activity, star count, and open issues for red flags.
    4. Approve pinning BOTH at their exact commit hashes (never `@latest`), or reject and instead document Swift/Kotlin as `none`-coverage in the D-11 matrix.
  </how-to-verify>
  <resume-signal>Type "approved" with the two exact commit pseudo-versions to pin, or "reject" to drop Swift/Kotlin to none-coverage.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Swift + Kotlin constructors + pins (post-approval)</name>
  <read_first>
    - internal/parser/cgo/parser_cgo.go (constructor shape)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Package Legitimacy Audit" (Swift/Kotlin rows, bindings/go subpath note)
  </read_first>
  <action>
    Using the exact commit pseudo-versions approved in the checkpoint, `go get github.com/alex-pinkus/tree-sitter-swift@<approved-commit>` and `go get github.com/fwcd/tree-sitter-kotlin/bindings/go@<approved-commit>` (never `@latest` — DIST-05 audited-dependency discipline). Add `NewSwiftParser` and `NewKotlinParser` to parser_cgo.go in the same one-liner shape, routing through `newCGoParser`/`Parse`. Add the import lines; do not run `go mod tidy`. If the checkpoint was rejected, SKIP this task and record Swift/Kotlin as `none`-coverage for the D-11 matrix instead.
  </action>
  <verify>
    <automated>go build ./internal/parser/... && go test ./internal/parser/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `NewSwiftParser` and `NewKotlinParser` compile and return non-nil parsers (if approved)
    - `go.mod` pins Swift/Kotlin by the exact approved commit pseudo-version, never `@latest` (grep confirms no floating pins)
    - Kotlin import targets the `bindings/go` subpath
  </acceptance_criteria>
  <done>Swift and Kotlin grammars are pinned by audited exact commit and construct through the seam (or are documented as none-coverage if rejected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grammar module supply chain → build | third-party grammar C code is compiled into the binary |
| mainstream source → grammars | untrusted source parsed in-process (8 of the 9 remaining grammars carry external scanners) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | `alex-pinkus/tree-sitter-swift`, `fwcd/tree-sitter-kotlin` `[SUS]` grammars | high | mitigate | Blocking human-verify checkpoint before `go get`; pin by exact commit pseudo-version (never `@latest`); DIST-05 audited-dependency discipline. Not auto-approvable regardless of `auto_advance`. |
| T-05-DoS | Denial of Service | mainstream external-scanner grammars (Rust/Ruby/PHP/C++/Swift/Kotlin) | high | mitigate | Every constructor routes through `MaxSourceBytes`; in-process crash risk is the accepted Phase-1 contract (Pitfall 4), documented as a shared caveat in the D-11 matrix |
| T-05-SC-org | Tampering | 5 tree-sitter-org grammars | low | accept | Official org modules pinned by exact semver, VERIFIED against proxy.golang.org |
</threat_model>

<verification>
- `go build ./... && go test ./internal/parser/... -count=1` green with all seven mainstream grammars compiled
- `go.mod` shows exact-commit pins for Swift/Kotlin, exact-semver pins for the five org grammars
- No `@latest` anywhere in the grammar requires
</verification>

<success_criteria>
All seven mainstream grammars construct through the MaxSourceBytes-guarded parser seam, with the two [SUS] community grammars pinned only after explicit human approval by exact commit.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-08-SUMMARY.md` when done.
</output>
