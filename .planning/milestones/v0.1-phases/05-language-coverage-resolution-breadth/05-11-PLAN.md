---
phase: 05-language-coverage-resolution-breadth
plan: 11
type: tdd
wave: 5
depends_on: [05-08]
files_modified:
  - internal/indexer/mainstream/cextract/cextract.go
  - internal/indexer/mainstream/cextract/cextract_test.go
  - internal/indexer/mainstream/swiftextract/swiftextract.go
  - internal/indexer/mainstream/swiftextract/swiftextract_test.go
  - internal/indexer/mainstream/kotlinextract/kotlinextract.go
  - internal/indexer/mainstream/kotlinextract/kotlinextract_test.go
  - internal/indexer/languages_c.go
  - internal/indexer/languages_cpp.go
  - internal/indexer/languages_swift.go
  - internal/indexer/languages_kotlin.go
autonomous: true
requirements: [LANG-06]
must_haves:
  truths:
    - "C and C++ share one cextract package (two grammars, one extractor) at documented-partial coverage"
    - "Swift and Kotlin get documented-partial extraction (only if their [SUS] grammars were approved in 05-08's checkpoint; otherwise recorded as none-coverage)"
    - "Each is registered through the language registry with a project-descriptor hook and path-identity fallback"
    - "Every documented gap is named for the D-11 capability matrix"
  artifacts:
    - internal/indexer/mainstream/cextract/cextract.go
    - internal/indexer/mainstream/swiftextract/swiftextract.go
    - internal/indexer/mainstream/kotlinextract/kotlinextract.go
    - "languages_c.go / languages_cpp.go / languages_swift.go / languages_kotlin.go registrations"
  key_links:
    - "languages_c.go + languages_cpp.go → shared cextract.Extract with different parser constructors"
    - "languages_swift.go / languages_kotlin.go gated on 05-08 approval"
---

<objective>
Deliver mainstream-tier extraction for C/C++ (shared `cextract`) and Swift/Kotlin (LANG-06) at documented-partial coverage. Swift/Kotlin land only if their `[SUS]` community grammars were approved in 05-08's human-verify checkpoint; otherwise they are recorded as `none`-coverage in the D-11 matrix.

Purpose: Second half of the mainstream-6 tier; uses the grammar constructors from 05-08.
Output: `cextract` (C+C++), `swiftextract`, `kotlinextract` + registrations + self-consistency tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@.planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md
@internal/indexer/goextract/goextract.go
@internal/indexer/goextract/types.go
@internal/indexer/languages.go
@internal/parser/cgo/parser_cgo.go
</context>

## Artifacts this phase produces (this plan's contribution)

- `internal/indexer/mainstream/cextract/cextract.go` — one `Extract` shared by C and C++
- `internal/indexer/mainstream/swiftextract/swiftextract.go`, `kotlinextract/kotlinextract.go`
- `internal/indexer/languages_c.go`, `languages_cpp.go`, `languages_swift.go`, `languages_kotlin.go`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shared cextract (C + C++) + registrations</name>
  <behavior>
    - A C file (structs, functions, typedefs) and a C++ file (classes, structs, methods, namespaces, templates) both extract nodes through ONE cextract package mapped to the shared vocabulary
    - `#include`→RefKindImports (best-effort); calls→RefKindCalls; C++ base-specifier→RefKindEmbeds-shaped supertype refs
    - C registers via NewCParser (.c/.h), C++ via NewCppParser (.cpp/.cc/.cxx/.hpp/.hh), both dispatching to cextract.Extract
    - Parse-failure skip contract preserved (C++ has an external scanner; C does not)
    - Coverage documented-partial: preprocessor-macro-generated symbols and template instantiations named as gaps
  </behavior>
  <read_first>
    - internal/indexer/goextract/goextract.go (skip contract + call collection)
    - internal/indexer/goextract/types.go (shared vocabulary)
    - internal/indexer/languages.go + languages_go.go (registration; two registrations sharing one Extract)
    - .planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md §"mainstream" (shared C/C++ extractor, two grammars one package)
    - .planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md D-04, D-11
  </read_first>
  <action>
    Create package cextract with ONE `Extract(p, moduleKey, relPath, src)` handling the overlapping C/C++ node vocabulary: `struct_specifier`/`class_specifier`→KindStruct, `function_definition`/`declaration` functions→KindFunction, C++ methods→KindMethod, `type_definition`→KindTypeAlias, `namespace_definition`→a documented mapping, `preproc_include`→RefKindImports (best-effort), `call_expression`→RefKindCalls, C++ `base_class_clause`→RefKindEmbeds-shaped supertype refs. Reproduce the skip contract. ModuleKey best-effort from the file's directory / include path (path-identity fallback — C/C++ have no in-source module declaration). Create languages_c.go (ID "c", Extensions [".c", ".h"], NewParser cgo.NewCParser, Extract cextract.Extract) and languages_cpp.go (ID "cpp", Extensions [".cpp", ".cc", ".cxx", ".hpp", ".hh"], NewParser cgo.NewCppParser, Extract cextract.Extract). Note: `.h` header ambiguity (C vs C++) — document the chosen disposition (default `.h`→C) as a matrix gap. Enumerate documented gaps (macros, templates) for the D-11 matrix.
  </action>
  <verify>
    <automated>go test ./internal/indexer/mainstream/cextract/ ./internal/indexer/ -run 'TestC|TestCpp|TestLanguageRegistry' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cextract_test.go (table-driven) asserts node-kind mapping across a C and a C++ source through the shared extractor
    - `lookupLanguageByID` resolves "c" and "cpp"; extensions map correctly, both to cextract.Extract
    - Parse-failure skip contract asserted; documented gaps enumerated
  </acceptance_criteria>
  <done>C and C++ extract through one shared package at documented-partial coverage and are registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swift + Kotlin extractors + registrations (conditional on 05-08 approval)</name>
  <behavior>
    - If the 05-08 checkpoint approved the [SUS] grammars: a Swift file (classes, structs, protocols, funcs) and a Kotlin file (classes, interfaces, funcs) extract nodes mapped to the shared vocabulary; both register and dispatch to their extractor; parse-failure skip contract preserved
    - If the checkpoint was rejected: no extractor/registration is added; Swift and Kotlin are recorded as none-coverage for the D-11 matrix
    - Coverage documented-partial: Swift protocol-witness/extension resolution and Kotlin extension-function/companion-object resolution named as gaps
  </behavior>
  <read_first>
    - internal/indexer/goextract/goextract.go (skip contract)
    - internal/indexer/goextract/types.go (shared vocabulary)
    - internal/indexer/languages.go (registration)
    - internal/parser/cgo/parser_cgo.go (NewSwiftParser/NewKotlinParser from 05-08 — present only if approved)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Package Legitimacy Audit" (Swift/Kotlin [SUS])
    - .planning/phases/05-language-coverage-resolution-breadth/05-08-SUMMARY.md (checkpoint outcome — approved/rejected)
  </read_first>
  <action>
    First read 05-08's SUMMARY to determine the checkpoint outcome. If approved: create packages swiftextract and kotlinextract, each with `Extract(p, moduleKey, relPath, src)` reproducing the skip contract and importing the shared vocabulary. Swift: map `class_declaration`/`struct_declaration`→KindStruct, `protocol_declaration`→KindInterface, `function_declaration`→KindFunction/KindMethod, `import_declaration`→RefKindImports, calls→RefKindCalls, protocol/inheritance clause→RefKindEmbeds-shaped supertype refs. Kotlin: map `class_declaration`→KindStruct, `interface`/`class_declaration` with interface modifier→KindInterface, `function_declaration`→KindFunction/KindMethod, `import`→RefKindImports, `call_expression`→RefKindCalls, delegation/supertype→RefKindEmbeds-shaped refs. ModuleKey best-effort (Swift module = target dir; Kotlin package declaration; path-identity fallback). Create languages_swift.go (ID "swift"/".swift"/NewSwiftParser) and languages_kotlin.go (ID "kotlin"/".kt"+".kts"/NewKotlinParser). If REJECTED: skip all of the above and record Swift/Kotlin as none-coverage in the SUMMARY for the D-11 matrix. Enumerate documented gaps for the matrix.
  </action>
  <verify>
    <automated>go test ./internal/indexer/mainstream/... ./internal/indexer/ -run 'TestSwift|TestKotlin|TestLanguageRegistry' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - If approved: swiftextract_test.go and kotlinextract_test.go (table-driven) assert node-kind mapping + RefKind emission; `lookupLanguageByID` resolves "swift"/"kotlin"
    - If rejected: no Swift/Kotlin packages exist and the SUMMARY records none-coverage for the matrix
    - Parse-failure skip contract asserted (when built); documented gaps enumerated either way
  </acceptance_criteria>
  <done>Swift and Kotlin extract at documented-partial coverage when their grammars were approved, or are cleanly recorded as none-coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C/C++/Swift/Kotlin source → grammars | untrusted source parsed in-process (C++/Swift/Kotlin carry external scanners) |
| supply chain | Swift/Kotlin grammars are community-maintained [SUS] |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | Swift/Kotlin `[SUS]` grammars | high | mitigate | Already gated by 05-08's blocking human-verify checkpoint + exact-commit pins; this plan only builds extractors on top of approved pins (or records none-coverage) |
| T-05-DoS | Denial of Service | C++/Swift/Kotlin external-scanner grammars | high | mitigate | `MaxSourceBytes` bounds every parse; in-process crash risk is the accepted Phase-1 contract (Pitfall 4), documented in the D-11 matrix |
| T-05-HeaderAmbig | Spoofing | `.h` C/C++ ambiguity | low | accept | Documented default disposition (`.h`→C) recorded as a matrix gap; no correctness/security impact beyond coverage |
</threat_model>

<verification>
- `go test ./internal/indexer/mainstream/... ./internal/indexer/ -count=1` green
- Priority-4 + Go + Rust/Ruby/PHP fixtures unaffected
- Swift/Kotlin outcome consistent with 05-08's checkpoint decision
</verification>

<success_criteria>
C/C++ extract through one shared package and Swift/Kotlin extract at documented-partial coverage (when approved) or are recorded as none-coverage, all with gaps named for the D-11 matrix.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-11-SUMMARY.md` when done.
</output>
