---
phase: 05-language-coverage-resolution-breadth
plan: 06
type: tdd
wave: 4
depends_on: [05-03]
files_modified:
  - internal/indexer/pyextract/pyextract.go
  - internal/indexer/pyextract/types.go
  - internal/indexer/pyextract/pyextract_test.go
  - internal/indexer/languages_python.go
  - testdata/golden/parity_python_test.go
autonomous: true
requirements: [LANG-04]
must_haves:
  truths:
    - "A Python repo gets full structural extraction: classes, functions/methods, imports mapped to the shared vocabulary"
    - "Cross-file resolution links Python imports, calls, and class inheritance via the dotted-module-path module key"
    - "Python inheritance (base classes in the argument_list of a class) is emitted as supertype refs for resolve-time promotion"
    - "Python parity is validated shape-not-byte against a real Python repo (or source-as-spec fallback)"
  artifacts:
    - internal/indexer/pyextract/pyextract.go
    - internal/indexer/languages_python.go
    - testdata/golden/parity_python_test.go
  key_links:
    - "languages_python.go init() → registerLanguage(python spec: NewPythonParser + pyextract.Extract + dotted-module ModuleKey)"
    - "Python package root (pyproject/setup.py) → dotted module path → symbolIndex outer key"
---

<objective>
Deliver full Python structural extraction + cross-file resolution (LANG-04): a `pyextract` package (the Python parser already exists from Phase 1 — only the extractor + resolver are new), a Python `LanguageSpec` (dotted-module-path module key, `pyproject.toml`/`setup.py` descriptor), and shape-not-byte golden parity against a real Python repo.

Purpose: Third priority-4 language; exercises directory-structure-derived dotted-module resolution and the INDENT/DEDENT external-scanner grammar.
Output: `pyextract.Extract`, Python registry entry, Python golden-parity test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@.planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md
@internal/indexer/goextract/goextract.go
@internal/indexer/goextract/types.go
@internal/indexer/languages.go
@internal/parser/cgo/parser_cgo.go
@testdata/golden/golden_parity_test.go
</context>

## Artifacts this phase produces (this plan's contribution)

- `internal/indexer/pyextract/pyextract.go` — `Extract(p, moduleKey, relPath, src)`
- `internal/indexer/languages_python.go` — Python `LanguageSpec` (ID "python", extension ".py", `cgo.NewPythonParser`, dotted-module-path `ModuleKey`, `pyproject.toml`/`setup.py` `Descriptor`)
- `testdata/golden/parity_python_test.go` — `TestGoldenParity_Python`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pyextract package + Python LanguageSpec registration</name>
  <behavior>
    - A Python file with classes, functions, and methods extracts nodes mapped to the shared vocabulary (class_definition→KindStruct or a documented Python class kind, function_definition→KindFunction/KindMethod depending on enclosing scope)
    - `import`/`from ... import ...` statements produce RefKindImports; calls produce RefKindCalls; class base classes (in the class's argument_list) produce RefKindEmbeds-shaped supertype refs
    - The dotted-module ModuleKey is derived from the package root + directory structure (rooted at the pyproject/setup.py package root); path-identity fallback when no descriptor
    - The INDENT/DEDENT external-scanner parse-failure path still yields FileResult.Err + nil error (per-file skip; accepted in-process crash caveat)
  </behavior>
  <read_first>
    - internal/indexer/goextract/goextract.go (skip contract + ordered collect phases + call collection)
    - internal/indexer/goextract/types.go (shared vocabulary)
    - internal/parser/cgo/parser_cgo.go (NewPythonParser — already exists, external scanner note lines 37-42)
    - internal/indexer/languages.go + languages_go.go (registration shape)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Don't Hand-Roll" (Python sys.path/package-root dotted paths, relative-import dots), §"Pattern 2", Assumptions Log A1 (PEP 420 namespace packages risk)
  </read_first>
  <action>
    Create package pyextract with `Extract(p, moduleKey, relPath, src)` reproducing goextract's skip/error contract and importing the shared `goextract` vocabulary. Walk the Python tree-sitter grammar (`tree-sitter-python` v0.25.0, already pinned Phase 1, HAS INDENT/DEDENT external scanner) mapping: `class_definition`→KindStruct (document the class→struct mapping), `function_definition`→KindMethod when nested in a class body else KindFunction, `import_statement`/`import_from_statement`→RefKindImports, `call`→RefKindCalls. For a class's base classes (identifiers in the class `argument_list`) emit RefKindEmbeds-shaped supertype refs for resolve-time promotion (Pattern 2). Collect classes before methods. Implement the Python ModuleKey as the dotted module path derived from the file's path relative to the package root (the directory containing the top-level `pyproject.toml`/`setup.py`, or the highest directory without `__init__.py` per package rules); Descriptor parses `pyproject.toml`/`setup.py` for the package root (path-identity fallback when absent, per D-03). Note the PEP 420 implicit-namespace-package and relative-import-dot edge cases as documented risks (Assumptions Log A1) — surface any under-resolution via the golden diff rather than hand-rolling every edge case. Create languages_python.go (package indexer) registering ID "python", Extensions [".py"], NewParser `cgo.NewPythonParser`, Extract `pyextract.Extract`, dotted-module ModuleKey, `pyproject.toml`/`setup.py` Descriptor.
  </action>
  <verify>
    <automated>go test ./internal/indexer/pyextract/ ./internal/indexer/ -run 'TestPython|TestLanguageRegistry' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - pyextract_test.go (table-driven) asserts node-kind mapping for class/function/method and RefKind emission for import/call/base-class
    - `lookupLanguageByID("python")` / `lookupLanguageByExt(".py")` return the Python spec
    - dotted-module ModuleKey resolves a nested-package file to its dotted path (e.g. `pkg.sub.mod`) and falls back to path identity without a descriptor
    - INDENT/DEDENT parse-failure skip contract asserted
  </acceptance_criteria>
  <done>Python files extract into the shared vocabulary and Python is registered with dotted-module resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Python cross-file resolution validation + golden parity</name>
  <behavior>
    - A multi-file Python fixture with cross-module imports and calls resolves into imports/calls edges via the dotted-module key
    - A subclass calling an inherited method surfaces (or is documented as needing the Wave-6 conformance-retry pass) — do not claim inheritance-dispatch here; that lands with RES-02
    - The golden-parity test self-skips when the Python corpus is absent, otherwise diffs shape-not-byte
  </behavior>
  <read_first>
    - testdata/golden/golden_parity_test.go (skip-if-absent pattern, shape comparison)
    - internal/indexer/resolve.go (module-key resolution)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Environment Availability", §"Pitfall 3" (inherited-method calls need the Wave-6 conformance retry), §"Don't Hand-Roll"
    - .planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md D-12
  </read_first>
  <action>
    Add a multi-file Python resolution fixture asserting cross-module imports and calls resolve into edges via the dotted-module key. Create testdata/golden/parity_python_test.go with `TestGoldenParity_Python` mirroring the resolveWeftCorpus skip-if-absent discipline and shape-not-byte comparison. Select a representative, license-clean real Python repo (Claude's Discretion) and document it. Apply the live-TS-CLI-unavailable fallback (source-as-spec + self-consistency) and record it if fresh golden capture is not possible. Explicitly note in the SUMMARY that inheritance-dispatch (inherited-method call resolution) is delivered by the Wave-6 dispatch/conformance-retry work (RES-02), not this plan — so a higher unresolvedCount on inherited calls here is expected and closed later (Pitfall 3).
  </action>
  <verify>
    <automated>go test ./internal/indexer/... ./testdata/golden/ -run 'TestPython|TestGoldenParity_Python' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Python resolution fixture asserts cross-module imports and calls resolve into edges
    - `TestGoldenParity_Python` self-skips cleanly when the corpus is absent
    - When present, shape diff is within documented tolerance (with inherited-method-call under-resolution noted as expected pre-RES-02)
  </acceptance_criteria>
  <done>Python is validated at full extraction + dotted-module cross-file resolution, shape-not-byte, with inheritance-dispatch explicitly deferred to Wave 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python source → tree-sitter-python | untrusted Python source parsed in-process (INDENT/DEDENT external scanner) |
| pyproject.toml/setup.py → Descriptor | untrusted manifest read for package root |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-DoS | Denial of Service | pyextract INDENT/DEDENT external scanner | high | mitigate | `MaxSourceBytes` bounds input; the Phase-1 spike-partner grammar's in-process crash risk is the accepted contract (Pitfall 4) |
| T-05-Manifest | Spoofing | pyproject/setup.py Descriptor | low | accept | Malformed manifest → path-identity fallback; no crash/code-exec |
| T-05-SC | Tampering | tree-sitter-python module | low | accept | `tree-sitter`-org module, already pinned in Phase 1 |
</threat_model>

<verification>
- `go test ./internal/indexer/pyextract/ ./internal/indexer/ ./testdata/golden/ -count=1` green
- Go/Java/C# fixtures unaffected by Python registration
</verification>

<success_criteria>
Python has full extraction + dotted-module cross-file resolution validated shape-not-byte, with inheritance-dispatch cleanly scoped to the RES-02 wave.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-06-SUMMARY.md` when done.
</output>
