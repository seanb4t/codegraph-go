---
phase: 05-language-coverage-resolution-breadth
plan: 03
type: tdd
wave: 3
depends_on: [05-02]
files_modified:
  - internal/indexer/symbolindex.go
  - internal/indexer/symbolindex_test.go
  - internal/indexer/resolve.go
  - internal/indexer/resolve_test.go
  - internal/indexer/goextract/goextract.go
  - internal/indexer/goextract/goextract_test.go
autonomous: true
requirements: [LANG-02, LANG-03, LANG-04, LANG-05]
must_haves:
  truths:
    - "The global symbol index is keyed by a per-language module key (ModuleKey), not Go's importPath concept — the two-level map structure is unchanged, only the outer-key computation is per-language"
    - "A same-module name collision no longer silently last-write-wins (WR-01) — collisions are detected/handled deterministically"
    - "A selector call on a non-identifier operand is not emitted as a same-package unqualified reference (WR-02)"
    - "A call passed as an argument to another call is resolved into a calls edge (call-as-argument extraction gap)"
    - "Determinism is preserved: collapseEdges remains the single edge-collapse path; edge ordering is total-ordered and idempotent"
  artifacts:
    - "per-language module-key resolution seam in symbolindex.go/resolve.go"
    - "WR-01 collision handling in symbolIndex.overlay"
    - "WR-02 + call-as-argument fixes in goextract recordCall/collectCalls"
  key_links:
    - "resolve.go resolveNameRef → symbolIndex keyed by per-language ModuleKey"
    - "goextract.recordCall → correct PkgAlias emission (only real import aliases)"
---

<objective>
Generalize Pass 2 (resolution) so the global symbol index is keyed by a per-language module key rather than Go's `importPath` concept (Pitfall 2), and fold in the three deferred Go resolution fixes (D-05: WR-01 same-module name collision, WR-02 selector-on-non-identifier, and the call-as-argument extraction gap). These fixes reshape `symbolindex.go`/`resolve.go`/`goextract.go` — every language added in Wave 4 inherits whatever shape they land in, so they MUST be fixed here (before new languages exist) or they become N-place fixes later.

Purpose: Establish the resolution seam and the correct resolution patterns the priority-4 languages inherit ("Resolution Breadth").
Output: a per-language module-key seam, WR-01/WR-02 fixes, and call-as-argument resolution — all determinism-preserving.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md
@.planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md
@.planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md
@internal/indexer/symbolindex.go
@internal/indexer/resolve.go
@internal/indexer/goextract/goextract.go
@internal/indexer/goextract/types.go
</context>

## Artifacts this phase produces (this plan's contribution)

- Per-language module-key seam: `symbolIndex.byImportAndName` reinterpreted as `map[moduleKey]map[name]nodeID` where `moduleKey` is `LanguageSpec.ModuleKey`-computed (Go's import path is the first instance)
- WR-01: collision-aware `symbolIndex.overlay`
- WR-02 + call-as-argument fixes in `goextract.recordCall`/`collectCalls`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-language module-key seam + WR-01 collision handling</name>
  <behavior>
    - Two files sharing the same module key both contribute symbols; a same-name collision between them is handled deterministically (WR-01) — not silently overwritten by whichever file was processed last
    - Cross-file resolution still resolves an unqualified reference into a same-module file (Go behavior preserved)
    - The two-level map structure (moduleKey → name → nodeID) is unchanged; only the outer key source generalizes
    - resolveSelector's alias-membership boundary is preserved unchanged (a non-import-alias operand never resolves)
  </behavior>
  <read_first>
    - internal/indexer/symbolindex.go (whole file — byImportAndName lines 8-20, overlay 40-61 [WR-01 site is line 58], resolveSelector 105-124, resolveUnqualified 126-137, newSymbolIndexFromStore 77-103)
    - internal/indexer/resolve.go (resolveRefsWithIndex 52-147, resolveNameRef 155-160)
    - .planning/phases/05-language-coverage-resolution-breadth/05-PATTERNS.md §"internal/indexer/resolve.go / symbolindex.go" (WR-01 site, ModuleKey generalization, collapseEdges reuse)
    - .planning/phases/05-language-coverage-resolution-breadth/05-RESEARCH.md §"Pitfall 2" (do NOT redesign the two-level map)
    - STATE.md decision referencing WR-01 origin (esdnwn12gg)
  </read_first>
  <action>
    Reinterpret `symbolIndex.byImportAndName` as a per-language module-key index: the outer key is whatever `DiscoveredFile.ImportPath` now carries (the per-language ModuleKey computed in 05-02) — the two-level `map[string]map[string]string` STRUCTURE stays; only its semantic source generalizes (Pitfall 2 — do NOT redesign into a different shape). Rename the field/doc comments to reflect "module key" semantics (keep it mechanical; do not change the lookup algorithm). Fix WR-01 in `overlay` (line 58 `names[en.Node.Name] = en.Node.Id`): a same-(moduleKey, name) collision must NOT silently last-write-wins. Handle it deterministically — detect the collision and keep a deterministic winner (e.g. lowest node Id, mirroring `resolveSymbolNode`'s lowest-Id tie-break in query/traverse.go) so the resolved edge target is stable across runs regardless of file-processing order; surface the collision count so it is never silently dropped (consistent with D-06a "never silently dropped"). Apply the same collision discipline in `newSymbolIndexFromStore`'s store-seeded overlay so Sync and from-scratch agree. Preserve `resolveSelector`'s alias-membership check verbatim as the narrowest-safe-set boundary (RQ-2). Do not touch `collapseEdges` — it stays the single deterministic edge-collapse path.
  </action>
  <verify>
    <automated>go test ./internal/indexer/ -run 'TestSymbolIndex|TestResolve' -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A symbolindex_test.go case with two same-module files declaring a same-named symbol asserts a deterministic, order-independent resolved target (shuffle input order → same result)
    - The collision is counted/surfaced, not silently dropped
    - Existing Go resolution tests pass unchanged (unqualified + selector resolution behavior preserved)
    - `-race` clean
  </acceptance_criteria>
  <done>The symbol index is per-language-module-keyed and collision-deterministic; Go resolution behavior is otherwise unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 selector-on-non-identifier + call-as-argument extraction gap</name>
  <behavior>
    - A selector call whose operand is a non-identifier (e.g. `foo().Bar()`, `arr[i].Bar()`) is NOT emitted with a PkgAlias that would let it mis-resolve as a same-package/qualified reference (WR-02)
    - A local-variable-receiver call (`w.Describe()`) still falls through to unresolved via the existing resolveSelector alias boundary (unchanged)
    - A call passed as an argument to another call (`outer(inner())`) resolves the inner call into its own calls edge (call-as-argument gap) — both outer and inner produce calls references
    - The deliberate RefKindCalls-only callees scoping is preserved (non-call references stay excluded — architectural, not reopened)
  </behavior>
  <read_first>
    - internal/indexer/goextract/goextract.go (collectCalls 564-574, recordCall 576-617, walkDescendants 619+)
    - internal/indexer/goextract/goextract_test.go (table-driven node-kind + call-collection test shape)
    - internal/indexer/resolve.go (resolveNameRef 155-160 — how PkgAlias drives selector vs unqualified resolution)
    - .planning/phases/05-language-coverage-resolution-breadth/05-CONTEXT.md D-05 (three deferred items, exact scoping)
    - STATE.md decisions referencing WR-02 (esdnwn12gg) and call-as-argument gap (252e2sav94)
  </read_first>
  <action>
    Fix WR-02 in `recordCall`'s `selector_expression` branch: only set `PkgAlias` when the operand is a genuine identifier that could be a package alias — a non-identifier operand (call_expression, index_expression, etc.) must emit a bare/unqualified reference OR no qualified-alias reference, never a `PkgAlias`-bearing UnresolvedRef that `resolveNameRef` would route to `resolveSelector` and (incorrectly) match. The current code already guards `if operand != nil && operand.Kind() == "identifier"` for pkgAlias — verify and tighten so a non-identifier operand does not leak a stale/empty PkgAlias that mis-resolves as same-package unqualified; the correct outcome for a non-identifier receiver is "unresolved" (matches WR-02's intent), consistent with resolveSelector's narrowest-safe-set boundary. Fix the call-as-argument gap: ensure `collectCalls`/`walkDescendants` records a `call_expression` that appears as an ARGUMENT to another call (nested inside the outer call's argument_list) as its own RefKindCalls UnresolvedRef — confirm the descendant walk actually visits argument-position call_expression nodes and that `recordCall` is invoked for each (the gap from 252e2sav94 is that these were not resolved into calls edges; add the extraction so they are). Do NOT change the RefKindCalls-only callees scoping in query — that exclusion of non-call references is architectural and explicitly out of scope. All new/changed edges flow through the existing `collapseEdges` unchanged.
  </action>
  <verify>
    <automated>go test ./internal/indexer/goextract/ ./internal/indexer/ -run 'TestExtract|TestCall|TestResolve' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A goextract_test.go case asserts a selector call on a non-identifier operand does NOT produce a PkgAlias-qualified same-package resolution (WR-02)
    - A goextract_test.go case asserts `outer(inner())` yields calls references for BOTH outer and inner (call-as-argument)
    - A local-variable-receiver call still resolves to unresolved (behavior unchanged)
    - Full Go golden-parity fixture re-run is still green (self-skips if corpus absent)
  </acceptance_criteria>
  <done>The three D-05 Go resolution fixes are landed with tests; determinism and the callees-scoping decision are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extracted references → resolved edges | resolution keys and edge targets derived from untrusted source structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-Nondet | Tampering | symbol-index collision resolution (WR-01) | medium | mitigate | Deterministic lowest-Id tie-break + collapseEdges total-order guarantee keeps byte-identical rebuild (determinism is load-bearing); a collision-count surface prevents silent drops |
| T-05-Misresolve | Repudiation | selector resolution (WR-02) | low | mitigate | Non-identifier operands never resolve as same-package — narrowest-safe-set boundary preserved; prevents fabricated (wrong-target) call edges |
| T-05-DoS | Denial of Service | resolution over large graphs | medium | accept | Resolution is bounded by extracted node/edge counts already capped upstream by MaxSourceBytes; no new unbounded traversal introduced |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package-manager installs in this plan |
</threat_model>

<verification>
- `go test ./internal/indexer/... -race -count=1` green
- Go golden-parity fixture (weft-go) re-run green (structural shape unchanged or improved by call-as-argument resolution — document any intentional edge-count delta in SUMMARY)
- Determinism gate: two from-scratch rebuilds of a fixture produce byte-identical export (existing INDX-02 gate still passes)
</verification>

<success_criteria>
Pass 2 is per-language-module-keyed and the three deferred Go resolution fixes (WR-01/WR-02/call-as-argument) are landed with tests — the resolution patterns every Wave-4 language inherits are correct and deterministic before any new language exists.
</success_criteria>

<output>
Create `.planning/phases/05-language-coverage-resolution-breadth/05-03-SUMMARY.md` when done.
</output>
