---
phase: 06-agent-integrations-cli-lifecycle
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/agents/toml.go
  - internal/agents/codex.go
  - internal/agents/opencode.go
  - internal/agents/hermes.go
  - internal/agents/toml_test.go
  - internal/agents/codex_test.go
  - internal/agents/opencode_test.go
  - internal/agents/hermes_test.go
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03]
must_haves:
  truths:
    - "Codex install splices [mcp_servers.codegraph] into ~/.codex/config.toml preserving every other table verbatim, and uninstall removes only that table"
    - "opencode install edits opencode.jsonc preserving comments (hujson patch), writing mcp.codegraph with a combined [binary,...args] command array"
    - "Hermes install edits ~/.hermes/config.yaml adding mcp_servers.codegraph and appending mcp-codegraph to platform_toolsets.cli, matching the existing list-item indent"
    - "Codex, opencode, and Hermes install→uninstall round-trips are byte-invariant modulo the codegraph section"
  prohibitions:
    - "opencode must NOT be edited by round-tripping through a plain JSON marshal (that drops comments) — use hujson Parse→Patch→Format→Pack"
    - "opencode config-dir resolution must NOT special-case Windows/%APPDATA% (opencode reads only XDG_CONFIG_HOME/~/.config on every OS — Pitfall 4)"
    - "Codex and Hermes must NOT support local scope (both are global-only); Codex must NOT pull in a general TOML library for a single-table edit"
    - "Hermes YAML surgery must NOT assume a fixed 4-space list indent (PyYAML default emits list items at the parent's indent — Pitfall 5)"
  artifacts:
    - path: internal/agents/toml.go
      provides: "hand-rolled TOML [mcp_servers.codegraph] table splice/strip (mirrors TS toml.ts), used only by Codex"
    - path: internal/agents/codex.go
      provides: "Codex CLI target: ~/.codex/config.toml table + ~/.codex/AGENTS.md marker block, global-only (D-05a)"
    - path: internal/agents/opencode.go
      provides: "opencode target: opencode.jsonc via hujson + AGENTS.md, XDG resolution, combined command array, %APPDATA% self-heal (D-05a)"
    - path: internal/agents/hermes.go
      provides: "Hermes target: config.yaml mcp_servers + platform_toolsets.cli line-range surgery, global-only (D-06)"
  key_links:
    - from: internal/agents/codex.go
      to: internal/agents/toml.go
      via: "codex install/uninstall calls the TOML table splice/strip helper"
      pattern: "mcp_servers"
    - from: internal/agents/opencode.go
      to: github.com/tailscale/hujson
      via: "hujson.Parse → hujson Patch (RFC 6902) → Format → Pack preserves comments"
      pattern: "hujson"
---

<objective>
Implement the three format-specific agent targets whose configs are NOT plain JSON: Codex CLI (TOML), opencode (comment-preserving JSONC via hujson), and Hermes Agent (YAML line-range surgery). Each is a mechanical port of the TS parity-oracle's target, reproducing the exact splice/patch strategy TS itself chose to avoid over-general dependencies (D-05a, D-06 — RESEARCH Corrected Per-Agent Parity Table).

Reconciliation note: RESEARCH.md confirms TS source implements Hermes fully — there is no "documented-partial" fallback. Only Codex and opencode among these three write a marker-fenced AGENTS.md; Hermes has no instructions surface.

Purpose: Complete the 8-agent roster with the three non-JSON formats, each editing surgically so comments/tables/list-indent survive.
Output: toml.go splice utility + 3 per-agent files (Codex/opencode/Hermes), each self-registering, with comment/indent/round-trip tests. Imports the hujson dep pinned by 06-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md
@.planning/phases/06-agent-integrations-cli-lifecycle/06-PATTERNS.md
@internal/agents/types.go
@internal/agents/shared.go
@internal/agents/registry.go
@internal/agents/instructions.go

<interfaces>
<!-- From 06-01 (already built). Use directly. -->
- AgentTarget interface + WriteResult/FileResult/FileAction/Location/TargetID
- upsertInstructionsEntry(file) / removeMarkedSection(file) — Codex + opencode AGENTS.md only
- atomicWriteFile — the sole write path
- registerTarget(t) — call from each init()
- InstallOptions.ExecPath (absolute binary path, D-04)
- github.com/tailscale/hujson (pinned unimported by 06-01): Parse([]byte) (Value, error), Value.Pack() []byte, hujson Patch (RFC-6902 JSON Patch), Format — the opencode comment-preserving edit surface (see 06-RESEARCH.md Standard Stack + Don't Hand-Roll)

<!-- Exact paths/formats/quirks: 06-RESEARCH.md "Corrected Per-Agent Parity Table" rows for Codex / opencode / Hermes; TS toml.ts algorithm in Alternatives Considered. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: toml.go splice + codex.go target (RED→GREEN)</name>
  <files>internal/agents/toml.go, internal/agents/codex.go, internal/agents/toml_test.go, internal/agents/codex_test.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (Alternatives Considered: the TS toml.ts "find [mcp_servers.codegraph] header, splice to next top-level [...] or EOF" algorithm; Corrected Parity Table Codex row)
    - internal/agents/shared.go + instructions.go (atomicWriteFile, upsertInstructionsEntry)
  </read_first>
  <behavior>
    - spliceTOMLTable on a file with no [mcp_servers.codegraph] → appends the table (with command/args lines) preserving all existing tables verbatim
    - spliceTOMLTable when the table already present with identical content → no-op (byte-identical)
    - spliceTOMLTable when present with different content → replaces only that table block (from its header to the next top-level [...] header or EOF), everything else byte-preserved
    - stripTOMLTable removes only the [mcp_servers.codegraph] block → file returns to pre-splice bytes
    - Codex Install (global only): ~/.codex/config.toml gets [mcp_servers.codegraph] with command = ExecPath, args = ["serve","--mcp"]; ~/.codex/AGENTS.md gets the marker block
    - Codex SupportsLocation(LocationLocal) is false; Install/Uninstall at local → unsupported (no write)
    - Round-trip byte-invariant; re-run idempotent
  </behavior>
  <action>
    Write toml_test.go + codex_test.go FIRST (RED). Then implement toml.go (GREEN): spliceTOMLTable(content, tableName, bodyLines) and stripTOMLTable(content, tableName) as pure string functions mirroring TS toml.ts — locate the header line, find the block end at the next line beginning with "[" at column 0 or EOF, splice/replace/remove, preserving surrounding text exactly. Implement codex.go: codexTarget self-registered in init(); SupportsLocation false for local; Install reads config.toml (empty on missing), splices the table via toml.go, writes via atomicWriteFile, and calls upsertInstructionsEntry for AGENTS.md; Uninstall strips the table + removes the AGENTS.md marker span, returning per-file status. Doc-comment citing D-05a (global-only, hand-rolled TOML) / D-07. REFACTOR only if obvious.
  </action>
  <verify>
    <automated>go test ./internal/agents/ -run 'TestTOML|TestCodex' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -run 'TestTOML|TestCodex' -race` exits 0
    - A test asserts splicing the codegraph table into a config.toml with a pre-existing unrelated table preserves that unrelated table byte-for-byte
    - A test asserts Codex SupportsLocation(LocationLocal) is false
    - A test asserts Codex Install→Uninstall round-trip returns config.toml to pre-install bytes
    - git log shows `test(06-03):` before `feat(06-03):` for these files
  </acceptance_criteria>
  <done>TOML table splice/strip + Codex global-only target pass; RED preceded GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: opencode.go — comment-preserving JSONC via hujson (RED→GREEN)</name>
  <files>internal/agents/opencode.go, internal/agents/opencode_test.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (Corrected Parity Table opencode row; Pitfall 4 XDG-not-%APPDATA%; Don't Hand-Roll hujson entry; Standard Stack hujson API)
    - internal/agents/shared.go + instructions.go (atomicWriteFile, upsertInstructionsEntry)
  </read_first>
  <behavior>
    - Config-dir resolution: XDG_CONFIG_HOME if set and non-empty, else ~/.config — unconditionally on every OS (no runtime.GOOS branch). Global file: <cfgdir>/opencode/opencode.jsonc (or .json if that's what exists); local: ./opencode.jsonc (or ./opencode.json), default .jsonc for new files
    - Install writes mcp.codegraph = {type:"local", command:[ExecPath,"serve","--mcp"], enabled:true} plus a top-level $schema if absent; comments in the existing JSONC are preserved through the edit (hujson Parse→Patch→Format→Pack)
    - AGENTS.md (global: <cfgdir>/opencode/AGENTS.md; local: ./AGENTS.md) gets the marker block
    - A stale %APPDATA%/opencode codegraph entry is swept only if APPDATA is set and differs from the resolved XDG path (self-heal, Pitfall 4)
    - Uninstall removes mcp.codegraph + AGENTS.md marker span, comments preserved; round-trip byte-invariant modulo the entry; re-run idempotent
  </behavior>
  <action>
    Write opencode_test.go FIRST (RED) — include a fixture opencode.jsonc containing line/block comments and assert they survive install + an idempotent re-run. Then implement opencode.go (GREEN): opencodeTarget self-registered in init(); resolveConfigDir (XDG-or-~/.config, no Windows branch — Pitfall 4); the .jsonc/.json existing-file preference; edit via hujson (Parse the existing bytes, apply an RFC-6902 patch that sets mcp.codegraph and $schema, Format, Pack, write via atomicWriteFile) — note opencode's command is the combined [binary, ...args] array, unlike the other JSON agents; upsertInstructionsEntry for AGENTS.md; the %APPDATA% stale-entry sweep. Doc-comment citing D-05a / Pitfall-4. REFACTOR only if obvious.
  </action>
  <verify>
    <automated>go test ./internal/agents/ -run 'TestOpencode' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -run 'TestOpencode' -race` exits 0
    - A test asserts a comment present in the pre-install opencode.jsonc is still present after install and after an idempotent re-run
    - A test asserts the mcp.codegraph command is a combined array whose first element is the ExecPath
    - A test asserts no runtime.GOOS/Windows special-case changes the resolved config dir (XDG/~/.config on every OS) — e.g. by exercising resolution with XDG_CONFIG_HOME set
    - git log shows `test(06-03):` before `feat(06-03):` for opencode.go
  </acceptance_criteria>
  <done>opencode JSONC comment-preserving install/uninstall + XDG resolution + combined command array pass; RED preceded GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: hermes.go — YAML line-range surgery (RED→GREEN)</name>
  <files>internal/agents/hermes.go, internal/agents/hermes_test.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (Corrected Parity Table Hermes row; Pitfall 5 PyYAML list-indent; the platform_toolsets.cli append requirement)
    - internal/agents/shared.go (atomicWriteFile)
  </read_first>
  <behavior>
    - Config path: $HERMES_HOME/config.yaml (default ~/.hermes if HERMES_HOME unset); global-only (SupportsLocation local → false)
    - Install adds a top-level mcp_servers.codegraph block {command, args:[serve,--mcp], timeout:120, connect_timeout:60, enabled:true} via line-range surgery, preserving all other YAML verbatim
    - Install appends "mcp-codegraph" to the platform_toolsets.cli list, detecting and matching the EXISTING list-item indent (PyYAML default emits "- item" at the parent key's indent, not deeper — Pitfall 5); if the item already present, no duplicate
    - Uninstall removes the mcp_servers.codegraph block and the mcp-codegraph cli list entry; round-trip byte-invariant modulo the codegraph additions; re-run idempotent
  </behavior>
  <action>
    Write hermes_test.go FIRST (RED) — include a PyYAML-default fixture (list items at the same indent as their parent key, e.g. cli: then "- other" at 0 extra indent) AND a hand-authored deeper-indent fixture, asserting the appended item matches each fixture's actual existing indent. Then implement hermes.go (GREEN): hermesTarget self-registered in init(); global-only; resolveHermesConfig ($HERMES_HOME or ~/.hermes); a line-range inserter for the mcp_servers.codegraph block and a listChildBlock-style routine that detects the existing indent of items under platform_toolsets.cli and appends mcp-codegraph at that same indent (idempotent if present); write via atomicWriteFile. Doc-comment citing D-06 / Pitfall-5. REFACTOR only if obvious.
  </action>
  <verify>
    <automated>go test ./internal/agents/ -run 'TestHermes' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -run 'TestHermes' -race` exits 0
    - A test with a PyYAML-default (parent-indent) cli list asserts the appended mcp-codegraph item matches that indent (not a hardcoded 4-space indent)
    - A test asserts Hermes SupportsLocation(LocationLocal) is false and that install adds mcp-codegraph to platform_toolsets.cli exactly once on re-run
    - A test asserts Install→Uninstall round-trip returns config.yaml to pre-install bytes
    - git log shows `test(06-03):` before `feat(06-03):` for hermes.go
  </acceptance_criteria>
  <done>Hermes YAML mcp_servers + platform_toolsets.cli indent-matched append + global-only pass; RED preceded GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codegraph → Codex/opencode/Hermes config files | TOML / JSONC / YAML files the user owns and hand-edits (comments, unrelated tables, list ordering matter) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Tampering | Non-surgical edit drops user comments (opencode) or reorders/loses unrelated TOML tables / YAML keys | high | mitigate | hujson comment-preserving patch for opencode; single-table string splice for TOML; line-range surgery for YAML; per-format round-trip byte-invariant tests assert unrelated content survives. |
| T-06-03-02 | Tampering / DoS | Malformed existing config causes panic or truncation on parse | medium | mitigate | Defensive read (empty content on missing/unparseable), atomicWriteFile temp+rename so a crash mid-write cannot truncate the live file (V5, V12). |
| T-06-03-03 | Tampering | Windows-special-casing opencode config dir so the write is silently never read | medium | mitigate | XDG/~/.config resolution on every OS (no GOOS branch), test asserts it (Pitfall 4). |
| T-06-03-04 | Tampering | Hermes list-item written at the wrong indent, corrupting the YAML | medium | mitigate | Detect existing list-item indent and match it; test covers PyYAML-default and deep-indent fixtures (Pitfall 5). |
</threat_model>

<verification>
- `go test ./internal/agents/... -race` green (all 8 targets registered + helpers)
- `AllTargetIDs()` now includes codex/opencode/hermes alongside the 06-02 five
- Comment/table/indent preservation + round-trip byte-invariance asserted per format
</verification>

<success_criteria>
- Codex TOML single-table splice, opencode JSONC comment-preserving hujson edit, Hermes YAML line-range surgery each install/uninstall byte-invariantly
- Codex + Hermes global-only; opencode XDG resolution with no Windows branch
- opencode combined command array; Hermes platform_toolsets.cli indent-matched append
- Only Codex + opencode write AGENTS.md; Hermes writes no instructions file
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-03)

- `internal/agents/toml.go`: `spliceTOMLTable`, `stripTOMLTable` (hand-rolled single-table TOML edit)
- `codexTarget` (internal/agents/codex.go) — Codex CLI, global-only
- `opencodeTarget` (internal/agents/opencode.go) — opencode, hujson JSONC edit + XDG resolver + %APPDATA% self-heal
- `hermesTarget` (internal/agents/hermes.go) — Hermes, YAML line-range surgery + platform_toolsets.cli appender
- First import of `github.com/tailscale/hujson` (pinned by 06-01)
All newly-created symbols — exclude from cross-phase drift verification.
</artifacts_produced>

<output>
Create `.planning/phases/06-agent-integrations-cli-lifecycle/06-03-SUMMARY.md` when done.
</output>
