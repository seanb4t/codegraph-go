---
phase: 06-agent-integrations-cli-lifecycle
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/version/version.go
  - internal/cli/version.go
  - internal/cli/telemetry.go
  - internal/cli/version_test.go
  - internal/cli/telemetry_test.go
  - internal/cli/root.go
autonomous: true
requirements: [CLI-01, CLI-03]
must_haves:
  truths:
    - "codegraph version prints semver + git commit + build date + Go version + os/arch from ldflags-injected vars"
    - "codegraph version --json emits valid JSON with those fields; codegraph --version on root prints the same version line"
    - "codegraph help [command] prints command-specific usage for every registered command (Cobra built-in)"
    - "codegraph telemetry prints a static statement that this build has zero passive/background telemetry and names codegraph upgrade as the one intentional, user-initiated network path"
  prohibitions:
    - "telemetry must NOT perform any network I/O, read any state, or import any network package — it is a static print (CLI-03 proves the negative)"
    - "version must NOT compute values at runtime beyond runtime.Version()/GOOS/GOARCH — semver/commit/date come only from ldflags-injected vars with dev/unknown defaults"
    - "the telemetry statement must NOT claim the binary never touches the network (that would be dishonest — upgrade is a real, user-initiated path per D-15)"
  artifacts:
    - path: internal/version/version.go
      provides: "ldflags-injected Version/Commit/Date vars + Info() struct assembling them with runtime Go version/os/arch (D-09)"
      contains: "var Version"
    - path: internal/cli/version.go
      provides: "codegraph version subcommand (+ --json) and the root --version wiring (D-09, D-10)"
    - path: internal/cli/telemetry.go
      provides: "codegraph telemetry static-statement subcommand (D-15)"
  key_links:
    - from: internal/cli/version.go
      to: internal/version/version.go
      via: "reads version.Info() and formats it for the version command and root --version"
      pattern: "version\\.Info"
    - from: internal/cli/root.go
      to: internal/cli/version.go
      via: "root registers newVersionCmd() and sets Version so --version works"
      pattern: "newVersionCmd|newTelemetryCmd"
---

<objective>
Deliver the CLI lifecycle ergonomics that carry no network or crypto surface: `codegraph version` / `--version` (D-09), Cobra built-in `help [command]` (D-10, CLI-01), and `codegraph telemetry` (D-15, CLI-03). Create the greenfield `internal/version` package whose `Version`/`Commit`/`Date` vars are injected at build time via `-ldflags -X` (with `dev`/`unknown` defaults for `go run`), and wire the two new commands into root.

This plan is Wave 1 because it has no dependency on the agents work, and the `internal/version` package it creates is consumed by 06-06's `upgrade --check` version comparison.

Purpose: Complete standard CLI ergonomics + the zero-telemetry trust claim, and expose the version package upgrade depends on.
Output: internal/version/version.go + version/telemetry commands + root wiring, tested for output/JSON/no-network.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md
@.planning/phases/06-agent-integrations-cli-lifecycle/06-PATTERNS.md
@internal/cli/root.go
@internal/cli/init.go
@internal/cli/uninit.go
@cmd/codegraph/main.go

<interfaces>
<!-- Existing substrate (already built). Use directly. -->
- internal/cli/root.go newRootCmd(): the AddCommand(...) call to extend + SilenceUsage/SilenceErrors set once on root (do NOT re-set on subcommands). Set root.Version to enable --version.
- internal/cli/init.go printSummary(): the "read a small struct, fmt.Fprintf a line to cmd.OutOrStdout(), extra branch under a flag" shape version.go mirrors; targetRoot pattern.
- internal/cli/uninit.go: the "print a fixed message via cmd.OutOrStdout() and return nil, no side effects" shape telemetry.go mirrors.
- cmd/codegraph/main.go: 18-line entrypoint; NO change needed — only the build's -ldflags -X sets internal/version vars; main only delegates to cli.Execute().
- Tests use cmd.SetOut(&bytes.Buffer{}) / cmd.SetArgs(...) / Execute() (see internal/cli/cli_test.go).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: internal/version package + version command + root --version (RED→GREEN)</name>
  <files>internal/version/version.go, internal/cli/version.go, internal/cli/version_test.go, internal/cli/root.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (D-09 version fields; PATTERNS.md "main.go ldflags injection seam" note — main.go needs no change, only build ldflags)
    - internal/cli/init.go (printSummary formatting-and-printing shape)
    - internal/cli/root.go (newRootCmd AddCommand call + where to set root.Version)
    - internal/cli/cli_test.go (the cmd.SetOut/SetArgs/Execute test idiom)
  </read_first>
  <behavior>
    - version.Info() returns a struct with Version, Commit, Date (from package vars), plus GoVersion (runtime.Version()), OS (runtime.GOOS), Arch (runtime.GOARCH)
    - With no ldflags set (go run/test), Version=="dev", Commit=="unknown", Date=="unknown" (defaults)
    - codegraph version prints one line containing the version, commit, date, go version, and os/arch
    - codegraph version --json prints valid JSON that unmarshals to all six fields
    - codegraph --version (root flag) prints a version line (Cobra Version wiring)
  </behavior>
  <action>
    Write version_test.go FIRST (RED): assert version.Info() defaults; assert `codegraph version` output contains the semver/commit/date/go/os-arch tokens; assert `codegraph version --json` unmarshals into a struct with all six fields; assert `codegraph --version` prints a version line. Then implement (GREEN): internal/version/version.go declaring `var (Version = "dev"; Commit = "unknown"; Date = "unknown")` and an Info() returning an exported Info struct (json-tagged fields Version/Commit/Date/GoVersion/OS/Arch), doc-comment naming the -X ldflags targets (github.com/seanb4t/codegraph-go/internal/version.Version etc., D-09). internal/cli/version.go: newVersionCmd() with a --json bool flag, RunE reading version.Info() and printing the formatted line or json.NewEncoder(cmd.OutOrStdout()).Encode(info); carries Short/Long/Example (D-10). In root.go: register newVersionCmd() in the AddCommand call and set root.Version to a formatted version string (so --version works) — do not touch SilenceUsage/SilenceErrors. REFACTOR only if obvious.
  </action>
  <verify>
    <automated>go test ./internal/version/... ./internal/cli/ -run 'TestVersion' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/version/... ./internal/cli/ -run 'TestVersion' -race` exits 0
    - A test asserts `codegraph version --json` output unmarshals into a struct carrying Version/Commit/Date/GoVersion/OS/Arch
    - A test asserts the plain `codegraph version` line contains runtime.GOOS and runtime.GOARCH values
    - A test asserts `codegraph --version` prints a non-empty version line
    - git log shows `test(06-05):` before `feat(06-05):` for the version work
  </acceptance_criteria>
  <done>Version package + version subcommand (+ --json) + root --version print ldflags-injected values with dev/unknown defaults; RED preceded GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: telemetry command (RED→GREEN) + help ergonomics check</name>
  <files>internal/cli/telemetry.go, internal/cli/telemetry_test.go, internal/cli/root.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-CONTEXT.md (D-15 exact intent: honest about upgrade being the sole network path, non-marketing wording, point at SBOM+source)
    - internal/cli/uninit.go (the "print a fixed message via cmd.OutOrStdout(), no side effects" shape)
    - internal/cli/root.go (AddCommand call)
  </read_first>
  <behavior>
    - codegraph telemetry prints a static statement asserting: this build collects zero telemetry / contains no passive or background phone-home code; the ONE intentional outbound path is codegraph upgrade (explicit, user-initiated, to GitHub Releases), which runs only when invoked; the reader is pointed at the SBOM + source as verifiable proof
    - The telemetry command performs no network I/O and reads no state — output is a single package-level const printed to cmd.OutOrStdout()
    - Every registered command exposes non-empty help (Short/Long) so `codegraph help <command>` and `<command> --help` are useful (D-10)
  </behavior>
  <action>
    Write telemetry_test.go FIRST (RED): assert the output contains the zero-passive-telemetry claim AND names `codegraph upgrade` as the sole intentional network path (both must be present — the honesty requirement of D-15); assert (by construction/coverage) the RunE performs no I/O other than the single Fprintln — e.g. keep the statement a package-level const and assert the command's RunE body references only that const + cmd.OutOrStdout(). Optionally add a help-smoke assertion that `codegraph help version` prints version-specific text. Then implement (GREEN): telemetry.go with a package-level telemetryStatement const (D-15 wording: auditable, non-marketing) and newTelemetryCmd() whose RunE is a single fmt.Fprintln(cmd.OutOrStdout(), telemetryStatement); Short/Long/Example set. Register newTelemetryCmd() in root.go's AddCommand call. REFACTOR only if obvious.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'TestTelemetry|TestHelp' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/cli/ -run 'TestTelemetry|TestHelp' -race` exits 0
    - A test asserts the telemetry output states zero passive/background telemetry
    - A test asserts the telemetry output explicitly names codegraph upgrade as the one intentional network path (honesty per D-15)
    - `grep -n 'net/http\|net\.' internal/cli/telemetry.go` returns no matches (no network surface)
    - git log shows `test(06-05):` before `feat(06-05):` for telemetry
  </acceptance_criteria>
  <done>telemetry prints the honest zero-passive-telemetry statement naming upgrade as the sole network path, with no network surface; help ergonomics confirmed; RED preceded GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → codegraph CLI (read-only info commands) | version/telemetry emit static/computed local info; no external input is trusted, no external system is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05-01 | Repudiation / Info | telemetry making a false "zero telemetry" claim (dishonest trust statement) | low | mitigate | The statement is honest by construction — it names codegraph upgrade as the sole user-initiated network path (D-15); a test asserts both the zero-passive claim and the upgrade-disclosure are present; a grep asserts telemetry.go has no network import. |
| T-06-05-02 | Tampering | version reporting a spoofed build identity | low | accept | Version metadata is injected by the trusted release build (-ldflags) and defaults to dev/unknown otherwise; there is no untrusted input path to version reporting. Provenance of the build itself is Phase 8's signing/attestation scope (DIST-02). |
</threat_model>

<verification>
- `go test ./internal/version/... ./internal/cli/ -race` green
- `codegraph version`, `codegraph version --json`, `codegraph --version`, `codegraph telemetry`, `codegraph help version` all produce sensible output
- telemetry.go imports no network package
</verification>

<success_criteria>
- internal/version exposes ldflags-injected vars with dev/unknown defaults and an Info() with Go version/os/arch
- version subcommand + --json + root --version all work
- help [command] gives command-specific output for every command (Short/Long present)
- telemetry prints the honest, auditable zero-passive-telemetry statement naming upgrade as the sole network path, with zero network code
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-05)

- New package `internal/version`: `var Version`, `var Commit`, `var Date` (ldflags targets), `Info` struct (Version/Commit/Date/GoVersion/OS/Arch), `Info()` function
- `internal/cli/version.go`: `newVersionCmd()` (with `--json`)
- `internal/cli/telemetry.go`: `newTelemetryCmd()`, `telemetryStatement` const
- `internal/cli/root.go` MODIFIED: registers `newVersionCmd()` + `newTelemetryCmd()`, sets `root.Version`
All newly-created symbols — exclude from cross-phase drift verification.
</artifacts_produced>

<output>
Create `.planning/phases/06-agent-integrations-cli-lifecycle/06-05-SUMMARY.md` when done.
</output>
