---
phase: 06-agent-integrations-cli-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/agents/claude.go
  - internal/agents/cursor.go
  - internal/agents/gemini.go
  - internal/agents/kiro.go
  - internal/agents/antigravity.go
  - internal/agents/claude_test.go
  - internal/agents/cursor_test.go
  - internal/agents/gemini_test.go
  - internal/agents/kiro_test.go
  - internal/agents/antigravity_test.go
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03]
must_haves:
  truths:
    - "install writes mcpServers.codegraph for Claude/Cursor/Gemini/Kiro/Antigravity at their exact TS-parity paths, and uninstall reverses it"
    - "install→uninstall round-trip returns each JSON config to its pre-install bytes modulo the codegraph entry"
    - "Cursor's MCP command carries a --path arg: absolute cwd for local, the literal ${workspaceFolder} for global"
    - "Antigravity's entry omits the type field; Claude/Cursor/Gemini/Kiro entries carry type:stdio"
    - "Claude writes the local entry to ./.mcp.json (never ./.claude.json) and adds mcp__codegraph__* to settings.json allow when AutoAllow"
  prohibitions:
    - "Cursor, Kiro, and Antigravity must NOT get a marker-fenced instructions file written (only Claude+Gemini here do); Cursor and Kiro must self-heal-DELETE any legacy instructions/steering file a prior install left"
    - "Antigravity must NOT be routed through the shared type:stdio entry builder (it rejects a type field — Pitfall 6)"
    - "Claude local scope must NOT write to ./.claude.json (Claude Code never reads it — Pitfall 3)"
  artifacts:
    - path: internal/agents/claude.go
      provides: "Claude Code target: ~/.claude.json / ./.mcp.json + settings.json permissions + CLAUDE.md marker block + legacy .claude.json migration (D-05)"
    - path: internal/agents/cursor.go
      provides: "Cursor target: ~/.cursor/mcp.json / ./.cursor/mcp.json + --path quirk, no instructions file, legacy .mdc self-heal (D-05a)"
    - path: internal/agents/gemini.go
      provides: "Gemini target: ~/.gemini/settings.json / ./.gemini/settings.json + GEMINI.md at project root for local (D-05a)"
    - path: internal/agents/kiro.go
      provides: "Kiro target: ~/.kiro/settings/mcp.json / ./.kiro/settings/mcp.json + steering self-heal-delete + MCP-disabled-by-default note (D-06)"
    - path: internal/agents/antigravity.go
      provides: "Antigravity target: unified/legacy ~/.gemini path detection, no-type entry, global-only (D-06)"
  key_links:
    - from: internal/agents/claude.go
      to: internal/agents/shared.go
      via: "writeMcpEntry + upsertInstructionsEntry + registerTarget in init()"
      pattern: "writeMcpEntry|registerTarget"
    - from: internal/agents/cursor.go
      to: internal/cli/serve.go
      via: "the --path arg it injects is consumed by `codegraph serve --mcp --path X` (AGNT-03 quirk substrate)"
      pattern: "\\-\\-path"
---

<objective>
Implement the five JSON-shaped agent targets (Claude Code, Cursor, Gemini CLI, Kiro, Antigravity) against the `AgentTarget` interface and shared helpers from 06-01. Each is a faithful, tested Go port of the TS parity-oracle's per-target file, reproducing exact config paths, entry shapes, and per-agent quirks (D-05, D-05a, D-06 — using the RESEARCH Corrected Per-Agent Parity Table, which supersedes CONTEXT.md D-05/D-06: TS source covers all 8 agents).

Reconciliation note (per planning context): RESEARCH.md corrects CONTEXT.md — Kiro and Antigravity get NO marker-fenced instructions file (Cursor never did either); only Claude and Gemini among this plan's five write an instructions file. Writing one uniformly would be a parity regression.

Purpose: Deliver the drop-in-swap config writers for the JSON agents, byte-idempotent and TS-round-trip-compatible.
Output: 5 per-agent files, each self-registering via init(), with per-agent install/uninstall/round-trip/quirk tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md
@.planning/phases/06-agent-integrations-cli-lifecycle/06-PATTERNS.md
@internal/agents/types.go
@internal/agents/shared.go
@internal/agents/registry.go
@internal/agents/instructions.go

<interfaces>
<!-- From 06-01 (already built). Use these directly — do not re-derive. -->
- AgentTarget interface (ID/DisplayName/SupportsLocation/Detect/Install/Uninstall/DescribePaths)
- writeMcpEntry(file string, buildEntry func() any) (FileResult, error) — Pattern 2 idempotent JSON set
- readJSONFile / writeJSONFile / jsonDeepEqual — surgical JSON preserve-siblings
- upsertInstructionsEntry(file) — writes codegraphInstructionsBlock fenced by the markers (Claude + Gemini only)
- removeMarkedSection(file) — uninstall strip
- registerTarget(t AgentTarget) — call from each file's init()
- InstallOptions.ExecPath (absolute binary path, D-04) / InstallOptions.AutoAllow (Claude permissions)

<!-- Corrected Per-Agent Parity Table lives in 06-RESEARCH.md ("Corrected Per-Agent Parity Table") — the authoritative source for every path/format/quirk in this plan. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: claude.go — the four-surface target (RED→GREEN)</name>
  <files>internal/agents/claude.go, internal/agents/claude_test.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (Corrected Per-Agent Parity Table: Claude Code row; Pitfall 3 local-file bug; Pattern 2 writeMcpEntry)
    - internal/indexer/languages_go.go (the per-variant init()-self-registration shape to mirror)
    - internal/agents/shared.go + instructions.go (helpers built in 06-01)
  </read_first>
  <behavior>
    - Install global: writes ~/.claude.json mcpServers.codegraph = {type:"stdio", command: ExecPath, args:["serve","--mcp"]}; writes ~/.claude/CLAUDE.md marker block; with AutoAllow, ~/.claude/settings.json permissions.allow gains "mcp__codegraph__*" (only once, idempotent)
    - Install local: writes ./.mcp.json (NOT ./.claude.json) mcpServers.codegraph; ./.claude/CLAUDE.md marker block; ./.claude/settings.json permission when AutoAllow
    - A pre-existing legacy ./.claude.json codegraph entry is migrated into ./.mcp.json and stripped from ./.claude.json (Pitfall 3)
    - Re-running Install is byte-idempotent (all FileResults Unchanged)
    - Uninstall removes mcpServers.codegraph (and empties→removes mcpServers), removes the CLAUDE.md marker span, removes the mcp__codegraph__* allow entry; returns per-file removed/not-found; unrelated keys + user permissions preserved
    - Round-trip: Install then Uninstall returns each file to pre-install bytes modulo the codegraph section
  </behavior>
  <action>
    Write claude_test.go FIRST (RED) using the fakeHome harness, covering the behavior cases (global+local, AutoAllow on/off, legacy migration, idempotent re-run, round-trip byte-invariant, sibling-preservation). Then implement claude.go (GREEN): a claudeTarget struct implementing AgentTarget, self-registered in init() via registerTarget. SupportsLocation returns true for both. Build the entry via writeMcpEntry with the {type:"stdio", command: opts.ExecPath, args:["serve","--mcp"]} shape. Call upsertInstructionsEntry for CLAUDE.md. Add a settings.json permissions helper that appends the mcp__codegraph__* allow token only when absent (AutoAllow gated). Implement the legacy ./.claude.json→./.mcp.json migration on install and strip-from-both on uninstall. DescribePaths lists the config file(s) + CLAUDE.md for the given location. Doc-comment each method citing D-05/D-07/D-08/Pitfall-3. REFACTOR only if obvious; keep green.
  </action>
  <verify>
    <automated>go test ./internal/agents/ -run 'TestClaude' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -run 'TestClaude' -race` exits 0
    - A test asserts the local entry lands in ./.mcp.json and NOT ./.claude.json
    - A test asserts AutoAllow adds exactly one mcp__codegraph__* allow token and re-run does not duplicate it
    - A test asserts Install→Uninstall round-trip byte-invariance on ~/.claude.json with a pre-existing sibling MCP server
    - git log shows `test(06-02):` before `feat(06-02):` for claude.go
  </acceptance_criteria>
  <done>Claude target passes all four-surface + migration + round-trip tests; RED preceded GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cursor.go + antigravity.go — the entry-shape quirks (RED→GREEN)</name>
  <files>internal/agents/cursor.go, internal/agents/antigravity.go, internal/agents/cursor_test.go, internal/agents/antigravity_test.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (Corrected Per-Agent Parity Table: Cursor + Antigravity rows; Pitfall 6 Antigravity type field; anti-pattern "instructions file for every agent")
    - internal/cli/serve.go (the `serve --mcp --path` flag the Cursor quirk targets)
    - internal/agents/shared.go (writeMcpEntry, removeMarkedSection)
  </read_first>
  <behavior>
    - Cursor Install local: mcpServers.codegraph args = ["serve","--mcp","--path", <absolute cwd>]; Install global: args end ["--path","${workspaceFolder}"] (literal string Cursor expands)
    - Cursor writes NO instructions file; a legacy .cursor/rules/codegraph.mdc left by a prior install is self-heal-DELETED on install; uninstall removes only the MCP entry
    - Antigravity entry = {command: ExecPath, args:["serve","--mcp"]} with NO type key (Pitfall 6); global-only (SupportsLocation local → false)
    - Antigravity resolves the unified path (~/.gemini/config/mcp_config.json) vs legacy (~/.gemini/antigravity/mcp_config.json) via a ~/.gemini/config/.migrated marker or an existing unified file; on install a stale legacy entry is swept into the unified path
    - Antigravity writes NO instructions file (shares Gemini's GEMINI.md, which only the Gemini target writes)
    - Both round-trip: Install→Uninstall returns config to pre-install bytes modulo the codegraph entry; re-run byte-idempotent
  </behavior>
  <action>
    Write cursor_test.go + antigravity_test.go FIRST (RED). Then implement cursor.go (GREEN): cursorTarget self-registered in init(); build the entry with the location-dependent --path arg (absolute cwd for local via os.Getwd, the ${workspaceFolder} literal for global); on Install, delete a legacy .cursor/rules/codegraph.mdc if present (self-heal); DescribePaths lists only the MCP config path (the legacy .mdc is a self-heal check target, not an ongoing write). Implement antigravity.go: antigravityTarget with its OWN entry builder that omits the type key (do NOT route through the shared stdio builder — Pitfall 6); SupportsLocation returns false for local; path detection for unified-vs-legacy with the .migrated marker + stale-legacy sweep. Neither calls upsertInstructionsEntry. Doc-comment citing D-05a/D-06/Pitfall-6. REFACTOR only if obvious.
  </action>
  <verify>
    <automated>go test ./internal/agents/ -run 'TestCursor|TestAntigravity' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -run 'TestCursor|TestAntigravity' -race` exits 0
    - A test asserts the Cursor local entry contains "--path" followed by an absolute path, and the global entry contains "--path" followed by "${workspaceFolder}"
    - A test asserts the Antigravity entry decodes to an object with no "type" member and that SupportsLocation(LocationLocal) is false
    - A test asserts a legacy .cursor/rules/codegraph.mdc present before install is absent after install
    - git log shows `test(06-02):` before `feat(06-02):` for these targets
  </acceptance_criteria>
  <done>Cursor --path quirk + legacy .mdc self-heal and Antigravity no-type + global-only + unified/legacy detection pass; RED preceded GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gemini.go + kiro.go — project-root instructions + steering self-heal (RED→GREEN)</name>
  <files>internal/agents/gemini.go, internal/agents/kiro.go, internal/agents/gemini_test.go, internal/agents/kiro_test.go</files>
  <read_first>
    - .planning/phases/06-agent-integrations-cli-lifecycle/06-RESEARCH.md (Corrected Per-Agent Parity Table: Gemini + Kiro rows; Pitfall 2 no-instructions-for-Kiro)
    - internal/agents/shared.go + instructions.go (upsertInstructionsEntry, removeMarkedSection)
    - internal/agents/claude.go (Task 1 — the JSON-stdio target shape to mirror for Gemini/Kiro)
  </read_first>
  <behavior>
    - Gemini Install global: ~/.gemini/settings.json mcpServers.codegraph {type:"stdio", command, args:["serve","--mcp"]} + ~/.gemini/GEMINI.md marker block; Install local: ./.gemini/settings.json entry + GEMINI.md at PROJECT ROOT (./GEMINI.md, NOT ./.gemini/GEMINI.md)
    - Kiro Install global: ~/.kiro/settings/mcp.json mcpServers.codegraph {type:"stdio", ...}; local: ./.kiro/settings/mcp.json
    - Kiro writes NO instructions file; a legacy ~/.kiro/steering/codegraph.md (or ./.kiro/steering/codegraph.md) is self-heal-DELETED on install; a WriteResult Note surfaces the "Kiro IDE ships MCP disabled by default — enable in Settings" hint (Pitfall table)
    - Both round-trip byte-invariant; re-run byte-idempotent; Gemini uninstall removes the entry + GEMINI.md marker span; Kiro uninstall removes only the entry
  </behavior>
  <action>
    Write gemini_test.go + kiro_test.go FIRST (RED). Then implement gemini.go (GREEN): geminiTarget self-registered; JSON-stdio entry via writeMcpEntry; upsertInstructionsEntry pointed at ~/.gemini/GEMINI.md for global and ./GEMINI.md (project root) for local — assert the local path is project-root, not under .gemini/. Implement kiro.go: kiroTarget self-registered; JSON-stdio entry at the settings/mcp.json path; NO upsertInstructionsEntry; on install delete a legacy steering/codegraph.md if present; append the MCP-disabled-by-default hint to WriteResult.Notes. Doc-comment citing D-05a (Gemini project-root) / D-06 (Kiro) / Pitfall-2. REFACTOR only if obvious.
  </action>
  <verify>
    <automated>go test ./internal/agents/ -run 'TestGemini|TestKiro' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -run 'TestGemini|TestKiro' -race` exits 0
    - A test asserts the Gemini local instructions file is written at ./GEMINI.md (project root), not ./.gemini/GEMINI.md
    - A test asserts Kiro writes no instructions/steering file and that a pre-existing legacy steering/codegraph.md is deleted on install
    - A test asserts the Kiro install WriteResult carries the "MCP disabled by default" note
    - git log shows `test(06-02):` before `feat(06-02):` for these targets
  </acceptance_criteria>
  <done>Gemini project-root GEMINI.md + Kiro no-instructions/steering-self-heal/disabled-note pass; RED preceded GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codegraph → each agent's config files | JSON MCP config + Markdown instructions files the user owns/edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Tampering | Install clobbering a user's unrelated mcpServers entry or settings.allow list | high | mitigate | All writes go through writeMcpEntry / surgical settings-permission append (from 06-01) which preserve siblings; per-agent round-trip byte-invariant tests assert unrelated keys and user permissions survive. |
| T-06-02-02 | Tampering | Writing the Claude local entry to the wrong file (./.claude.json) so it silently never applies | medium | mitigate | Explicit ./.mcp.json path + a test asserting the exact filename (Pitfall 3). |
| T-06-02-03 | Tampering | Antigravity entry carrying a type field it rejects (silent non-registration) | medium | mitigate | Antigravity has its own no-type entry builder; a test decodes the entry and asserts no type member (Pitfall 6). |
| T-06-02-04 | Tampering | Leaving a stale legacy instructions/steering/.mdc file that breaks TS round-trip | low | mitigate | Cursor/Kiro self-heal-DELETE the legacy file on install; a test asserts absence post-install. |
</threat_model>

<verification>
- `go test ./internal/agents/... -race` green (all five targets + 06-01 helpers)
- Each target self-registers (AllTargetIDs includes claude/cursor/gemini/kiro/antigravity)
- Round-trip byte-invariance asserted per target
</verification>

<success_criteria>
- Five JSON targets install/uninstall at exact TS-parity paths with correct entry shapes
- Cursor --path (local abs / global ${workspaceFolder}) and Antigravity no-type + global-only quirks reproduced
- Only Claude + Gemini write an instructions file; Cursor + Kiro self-heal legacy files; Antigravity/Kiro write none
- Claude local→./.mcp.json + settings permissions + legacy migration correct
- All round-trip byte-invariant and re-run idempotent
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-02)

New per-agent target types (each self-registering via init(), implementing AgentTarget):
- `claudeTarget` (internal/agents/claude.go) — Claude Code
- `cursorTarget` (internal/agents/cursor.go) — Cursor, with the injected --path arg
- `geminiTarget` (internal/agents/gemini.go) — Gemini CLI, project-root GEMINI.md for local
- `kiroTarget` (internal/agents/kiro.go) — Kiro
- `antigravityTarget` (internal/agents/antigravity.go) — Antigravity, no-type entry builder
Plus any target-private helpers (e.g. Claude's settings-permission appender, Antigravity's unified/legacy path resolver, the no-type entry builder). These are newly-created symbols — exclude from cross-phase drift verification.
</artifacts_produced>

<output>
Create `.planning/phases/06-agent-integrations-cli-lifecycle/06-02-SUMMARY.md` when done.
</output>
