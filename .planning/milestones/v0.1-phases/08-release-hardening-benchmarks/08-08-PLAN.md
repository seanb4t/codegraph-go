---
phase: 08-release-hardening-benchmarks
plan: 08
type: execute
wave: 4
depends_on: ["08-01", "08-07"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/bench.yml
autonomous: true
requirements: [DIST-03, DIST-04, PERF-01, PERF-02, INDX-06]
must_haves:
  truths:
    - "ci.yml runs the full test suite, a blocking govulncheck ./... step (separate from any SBOM step), a reproducibility double-build hash-diff gate, and the perf-regression gate on PR/push"
    - "The double-build gate blocks on linux/amd64 (canonical target) and runs best-effort/reported on the other targets (D-03)"
    - "The perf-regression gate runs `go run ./tools/bench/runner -mode regression` against the committed baseline with the INDX-06 peak-RSS ceiling"
    - "bench.yml runs on-demand/scheduled (not blocking), installs TS codegraph@1.3.1, runs the headtohead runner, and surfaces raw numbers"
    - "internal/daemon's known-flaky tests are isolated with -count=1 (documented STATE.md flake), not papered over"
    - "Every third-party Action is pinned to a full commit SHA or exact semver"
  prohibitions:
    - "MUST NOT combine SBOM generation and vulnerability scanning into one step — govulncheck is its own distinct, separately-reported gate (RESEARCH anti-pattern)"
    - "MUST NOT make the double-build gate silently green across all targets — only linux/amd64 is the hard block; cross-targets are reported (D-03)"
    - "MUST NOT clone a remote repo in the perf-regression gate — the synthetic corpus is offline (D-04)"
    - "MUST NOT use a floating Action major tag — pin full SHA/semver"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "test + govulncheck + double-build reproducibility gate + perf-regression gate on PR/push"
      contains: "govulncheck"
    - path: .github/workflows/bench.yml
      provides: "on-demand/scheduled head-to-head publish (non-blocking)"
      contains: "workflow_dispatch"
  key_links:
    - from: .github/workflows/ci.yml
      to: tools/bench/runner
      via: "perf-regression gate invokes the runner in regression mode against baseline.json"
      pattern: "runner"
    - from: .github/workflows/ci.yml
      to: .goreleaser.yaml
      via: "double-build gate rebuilds linux/amd64 with the same reproducibility flags and diffs the hash"
      pattern: "trimpath"
---

<objective>
Wire the CI gates that enforce this phase's guarantees on every PR/push: the vulnerability gate (DIST-03), the reproducible-build double-build hash-diff (DIST-04), and the performance-regression gate over the synthetic 100k+ corpus with the peak-RSS ceiling (PERF-02 + INDX-06). Add the on-demand head-to-head publish workflow (PERF-01). Together these turn "green CI" into actual proof rather than claimed intent (RESEARCH Pitfall 5).

Purpose: The harness (Plan 08-07) and build config (Plan 08-01) are only load-bearing once CI actually runs them. This plan makes the gates real.
Output: `.github/workflows/ci.yml`, `.github/workflows/bench.yml`.
</objective>

## Artifacts this phase produces (Plan 08-08)

- `.github/workflows/ci.yml` (new) — test + govulncheck + double-build reproducibility gate + perf-regression gate.
- `.github/workflows/bench.yml` (new) — on-demand/scheduled head-to-head publish (non-blocking).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-release-hardening-benchmarks/08-CONTEXT.md
@.planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md

<interfaces>
<!-- Consumes Plan 08-01 (build config/flags) and Plan 08-07 (runner). -->

Reproducibility double-build (RESEARCH "Code Examples"): with SOURCE_DATE_EPOCH pinned to the tag/commit date,
  build linux/amd64 twice with -trimpath -ldflags "-s -w -buildid= -X .../version.{Version,Commit,Date}=...",
  sha256 both, diff → fail if they differ. Block on linux/amd64; report-only on other targets.

Vuln gate: `govulncheck ./...` (call-graph-aware; VERIFIED present locally) as a distinct blocking step; or the
  pinned golang/govulncheck-action. Keep it separate from any SBOM step.

Perf gate: `go run ./tools/bench/runner -mode regression -baseline tools/bench/baseline.json -ceiling-bytes <N>`
  — offline synthetic corpus (Plan 08-03/08-07), non-zero exit fails the gate.

Head-to-head (bench.yml): install TS via `npm install -g @colbymchenry/codegraph@1.3.1`, build the Go binary,
  `go run ./tools/bench/runner -mode headtohead ...`, surface raw JSON numbers as a job artifact / summary.

Known flake (STATE.md / CONTEXT.md): internal/daemon TestSoak + flush-lock under full-suite parallel load —
  isolate with `go test ./internal/daemon/ -count=1` (documented retry), don't paper over.
Action pins (RESEARCH OQ2): re-resolve actions/checkout, actions/setup-go, actions/setup-node,
  golang/govulncheck-action at execution time; pin to full SHA/semver.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ci.yml — test suite + govulncheck + reproducibility double-build gate + perf-regression gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md "Double-build reproducibility gate" + Pitfall 5 + "Anti-Patterns to Avoid"
    - .goreleaser.yaml (reproducibility flags to mirror in the double-build)
    - tools/bench/runner/main.go (regression-mode invocation)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` triggered on `pull_request` and `push` to the default branch. Job 1 (tests): checkout (pinned), setup-go 1.26.5 (pinned), `go build ./...`, `go test ./...`, and a separate isolated `go test ./internal/daemon/ -count=1` step (documented flake handling — do not remove the flaky tests, isolate them). Job 2 (vuln gate): run `govulncheck ./...` as its own blocking step (or the pinned golang/govulncheck-action), explicitly SEPARATE from any SBOM step (RESEARCH anti-pattern: never merge SBOM + scan). Job 3 (reproducibility, ubuntu-latest): set `SOURCE_DATE_EPOCH` from the commit date, build the linux/amd64 binary twice with CGO_ENABLED=1 and the identical reproducibility flags from .goreleaser.yaml (-trimpath, -ldflags "-s -w -buildid= -X internal/version.{Version,Commit,Date}=..."), sha256 both, and FAIL if the two hashes differ — this is the hard block. Add a non-blocking (`continue-on-error: true`) reported step that double-builds one cross target (e.g. linux/arm64 via zig) and records whether it matched, WITHOUT failing the job (D-03: linux/amd64 blocks, others reported). Job 4 (perf gate, ubuntu-latest): build the Go binary, then `go run ./tools/bench/runner -mode regression -baseline tools/bench/baseline.json -ceiling-bytes <ceiling>` — a non-zero exit fails the gate (PERF-02 + INDX-06). Pin every Action to full SHA/semver; add comments citing D-03's blocking scope and the SBOM/scan separation.
  </action>
  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
    - The file contains a `govulncheck` step that is NOT combined with an SBOM/syft step (`grep -n 'govulncheck' .github/workflows/ci.yml` present; no syft in the same step).
    - The double-build job blocks on linux/amd64 (a hash-diff failure fails the job) and marks cross-target reproducibility with `continue-on-error: true`.
    - The perf gate invokes `tools/bench/runner -mode regression` against `tools/bench/baseline.json`.
    - `go test ./internal/daemon/ -count=1` appears as an isolated step.
    - No floating-major Action pins: `grep -nE 'uses:.*@v[0-9]+$' .github/workflows/ci.yml` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -q 'govulncheck' .github/workflows/ci.yml && grep -q 'runner -mode regression' .github/workflows/ci.yml && ! grep -nE 'uses:.*@v[0-9]+$' .github/workflows/ci.yml</automated>
  </verify>
  <done>Every PR/push runs tests, a blocking vuln gate, a linux/amd64-blocking reproducibility gate, and the offline perf-regression gate.</done>
</task>

<task type="auto">
  <name>Task 2: bench.yml — on-demand/scheduled head-to-head publish (non-blocking)</name>
  <files>.github/workflows/bench.yml</files>
  <read_first>
    - tools/bench/runner/main.go (headtohead mode)
    - .planning/phases/08-release-hardening-benchmarks/08-RESEARCH.md § "System Architecture Diagram" (bench.yml role) + Pattern 1 Windows/RSS scoping note
  </read_first>
  <action>
    Create `.github/workflows/bench.yml` triggered on `workflow_dispatch` and (optionally) a `schedule` cron — never on PR/push, and never blocking. On a Linux (and/or macOS) runner: checkout (pinned), setup-go 1.26.5 (pinned), setup-node (pinned) + `npm install -g @colbymchenry/codegraph@1.3.1` to provide the TS head-to-head reference, build the Go codegraph binary, then run `go run ./tools/bench/runner -mode headtohead -go-binary <built> -ts-binary $(command -v codegraph)` against the realcorpus manifest, capturing the raw per-repo median-of-5 JSON as a job artifact and printing it to the job summary (this is what docs/BENCHMARKS.md is refreshed from — Plan 08-09). Document in a comment that the RSS metric is scoped to Linux/macOS (Windows RSS is out of scope per RESEARCH Pattern 1's Windows note) and that this workflow publishes numbers but never gates. Pin every Action to full SHA/semver.
  </action>
  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/bench.yml'))"` exits 0.
    - Triggers are `workflow_dispatch` (and optionally `schedule`) — NOT `pull_request`/`push` (`grep -n 'workflow_dispatch' .github/workflows/bench.yml` present; no `pull_request:` trigger).
    - The job installs `@colbymchenry/codegraph@1.3.1` and runs `runner -mode headtohead`.
    - No floating-major Action pins.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/bench.yml'))" && grep -q 'workflow_dispatch' .github/workflows/bench.yml && grep -q 'headtohead' .github/workflows/bench.yml && ! grep -nE 'uses:.*@v[0-9]+$' .github/workflows/bench.yml</automated>
  </verify>
  <done>The head-to-head benchmark publishes raw per-repo numbers on demand without ever blocking a merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → merge decision | a green PR must actually mean the vuln/reproducibility/perf properties hold, not just that a workflow ran |
| bench.yml → installed TS reference | shells out to an npm-installed TS binary with fixed args |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-16 | Tampering | reproducibility claim | high | mitigate | linux/amd64 double-build hash-diff actually runs and blocks — the property is verified, not asserted (RESEARCH Pitfall 5) |
| T-08-02 | Tampering / EoP | third-party CI Actions | high | mitigate | every Action pinned to full SHA/semver |
| T-08-17 | Denial of Service | vuln exposure | medium | mitigate | govulncheck ./... is a distinct blocking gate, separate from SBOM, so reachable vulns block the merge (DIST-03) |
| T-08-10 | Denial of Service | network in the perf gate | medium | mitigate | offline synthetic corpus; the gate never clones a remote repo (D-04) |
</threat_model>

<verification>
- ci.yml + bench.yml parse as valid workflow YAML.
- govulncheck is its own gate; double-build blocks on linux/amd64 and reports cross-targets; perf gate is offline.
- bench.yml is dispatch/schedule only, non-blocking; all Actions pinned.
</verification>

<success_criteria>
DIST-03 (vuln gate), DIST-04 (reproducibility gate), and PERF-02/INDX-06 (perf-regression + peak-RSS ceiling) are enforced on every PR/push; PERF-01 head-to-head numbers publish on demand — all verified, not merely claimed.
</success_criteria>

<output>
Create `.planning/phases/08-release-hardening-benchmarks/08-08-SUMMARY.md` when done.
</output>