---
phase: 01-engine-seam-wire-protocol-secure-transport
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/schema/graph.proto
  - internal/schema/graph.pb.go
  - internal/schema/meta.go
  - internal/schema/meta_commit_test.go
  - internal/indexer/commit.go
  - internal/indexer/commit_test.go
  - internal/indexer/resolve.go
  - internal/indexer/sync.go
  - internal/query/engine.go
  - internal/query/meta_test.go
autonomous: false
requirements: [ENG-04]

estimate:
  tokens: 63000
  raw_tokens: 63000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`schema.Meta` carries the commit SHA the index was built at as additive field 8, following the `has_file_index = 7` precedent; a graph written before this field existed decodes fine and reports an absent SHA rather than an error (ENG-04, D-05)."
    - "HEAD is resolved ONCE per index or sync operation and threaded to every `Meta` construction in that operation, so all three write sites agree and a HEAD that moves mid-run cannot produce two different recorded commits."
    - "Every one of the three `PutMeta` write sites populates the field, so a graph built by `codegraph index`, by an incremental sync, and by a mtime-refresh-only sync all carry it."
    - "A repository that is not a git checkout, or a machine with no `git` on PATH, indexes successfully with an absent SHA — never a failure (D-05's degrade-gracefully requirement)."
    - "`(*Engine).IndexMeta` exists as the exported accessor that carries the stored `*schema.Meta` out of `internal/query`, so the UI Status handler has an actual data path from `Meta.commit_sha` to the wire — the path that did not exist and made 01-09's Status must-have unachievable."
    - "`codegraph status`'s output bytes are unchanged: `StatusResult` gains no field, `status.go` gains no projection, and the SHA is surfaced only through the UI `Status` RPC. No golden covers `status`, so a CLI change there would be silent (D-06)."
  artifacts:
    - internal/indexer/commit.go
    - internal/schema/meta_commit_test.go
    - internal/query/meta_test.go
  key_links:
    - "`schema.NewMeta()` is the single construction point but has no repo path to read, so the new field's population belongs beside `HasFileIndex` and `LastSyncUnixMs` at each write site — fed by ONE resolution performed at the top of the operation, not by a subprocess per site."
    - "`(*Engine).IndexMeta` lives in `internal/query/engine.go`, NOT in `status.go`. D-06 forbids changing `codegraph status`'s output; it does not forbid the Engine exposing the metadata it already reads. Keeping the accessor out of `status.go` keeps that distinction visible in the file layout."
    - "`Engine.reader` is unexported and `graphstore.Reader.GetMeta()` is unreachable from `internal/uiserver`; a second in-process `graphstore.Open` on the same directory is itself lock-refused. The accessor is the only viable path."
  prohibitions:
    - "Never gate a task on a bare `go test ... | rg '--- PASS' | wc -l` count whose exit status is discarded. A shell pipeline's status is the LAST command's, so N passing subtests plus one `--- FAIL` satisfies a count-only gate. Every automated gate captures `go test`'s output and status BEFORE any pipe and requires BOTH: status 0 AND the `--- PASS` floor. The count alone is vacuous when a test fails; the status alone is vacuous when the `-run` pattern matches nothing. The sole scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence."
    - "Never renumber or reuse a protobuf field number once it has shipped; retiring a field means adding its number to a `reserved` clause. The on-disk format is a published contract to already-installed graphs."
    - "Never add a field to `query.StatusResult` or a projection to `internal/query/status.go` in this plan; `codegraph status`'s bytes are unguarded by any golden and must not move (D-06)."
    - "Never write `test -z \"$(git status --porcelain <path>)\"` without a preceding `test -d <path> &&` existence guard. `git status --porcelain` on a missing or typo'd pathspec exits 0 with EMPTY stdout — the 'could not open directory' warning goes to stderr — so the check passes vacuously against a path that was never opened."
    - "Never positive-control a zero-count gate against a DIFFERENT file than the one the gate guards. `rg` prints nothing for a nonexistent path just as it does for a clean file, so a typo'd or renamed target reads `0` while the guarded file is never opened. The control must run the identical pipeline against the SAME file with a pattern that file genuinely contains."
    - "Never score a count with a regex that accepts zero. `rg -q -e 'compared [0-9]+ generated files'` matches `compared 0 generated files`, so it is a presence check on the count and not a floor. Capture the number and assert the floor numerically."
    - "Never write an unanchored identifier grep as a forbidden-pattern gate. Anchor it to the declaration form — `func gather[A-Z]`, never `\\bgather[A-Z]` — because a trailing comment or a `/* */` block survives `rg -v '^\\s*//'` and turns the gate red, or green, for the wrong reason."
    - "Never assert emptiness with an unranged `git diff`. Under atomic-commit-per-task the working tree is clean at verification time, so `git diff --stat <path>` is empty by construction whether or not the file was edited. Assert over an explicit `<phase-base>..HEAD` revision range."
    - "Never write a gate whose floor can be met without the deliverable it names existing. This is the SCOPE half of gate hygiene and no shape check reaches it: a scope-vacuous gate is well-formed, parses, honors exit status and carries a same-file positive control, and still scores nothing the task owns. Cycle 5's form of this prohibition was STRUCTURAL where it had to be CAUSAL — OWNERSHIP constrained who DECLARES the counted tests, not what those tests ASSERT, and HOMING accepted a named criterion no gate executes — so a task declaring one self-contained arithmetic test and homing every behavior bullet to prose satisfied both halves literally while the deliverable was entirely absent. The amended rule closes that. Two requirements, both mandatory and both checkable by reading the plan. (a) OWNERSHIP AND CAUSATION — every gate carries at least one leg that turns RED when this task's named deliverable is absent or replaced by a NO-OP, in exactly ONE of three named forms, and the floor derivation must say which form it uses. A no-op is an implementation that still compiles and keeps the same signature but does nothing and returns zero values; for a generated file, schema or other declarative artifact it is the empty or default-valued version of it. REPLACEMENT, not deletion, is the standard, because deletion is satisfied by a mere compile dependency and only replacement separates a test that ASSERTS an artifact's behavior from one that merely MENTIONS it. FORM A1, OWN-TEST, the default: the leg's test-selection pattern matches ONLY test functions this task itself declares, its floor is derived from that leg's own functions and subtests alone, AND the derivation names the ARTIFACT UNDER TEST — a specific function, type, constant, generated file or task, cited from this task's files list, this plan's files_modified, this phase's artifact manifest, or this task's read_first — and states why replacing that artifact with a no-op turns this leg RED. A counted test that would still pass against that no-op does not satisfy A1 however exclusively this task declares it, and a counted test with no artifact under test at all — a self-contained arithmetic assertion is the canonical form — never satisfies it. Where the named artifact is one this task does not itself modify, which is the characterization-assertion case, the derivation must say so and name where the artifact is cited. FORM A2, DIRECT-ARTIFACT, for a deliverable that is a command, generated file or task rather than a test and is named in this task's files list, this plan's files_modified or this phase's artifact manifest: the leg EXECUTES that artifact and scores a numeric floor read out of that artifact's OWN output, and the derivation names it and states that a no-op replacement drives the measured number below the floor. FORM A3, OBSERVATION, available ONLY to a task that declares no test function of its own and whose named deliverable is a recorded observation rather than persisted code — a deliberate-mutation RED observation, or a by-hand exercise against real processes. Such a task writes no assertion, so no `--- PASS` floor can ever be its evidence, and D-04's scoped exception to the conjunction lives here; it qualifies only when ALL FOUR hold: every observation is scored by at least one NAMED numeric or verdict-bearing measurement taken from a named command's own output and recorded verbatim; a vacuous measurement — a zero failure count, a missing per-item verdict — is written down as a STOP CONDITION and not as a note; the gate still carries the strongest executable leg available to it; and the derivation states plainly which failures of THIS task that leg CAN catch and which it CANNOT. A task that declares even one test function of its own may never use A3, and A3 is never a way to avoid writing one. Where a leg's pattern ALSO matches an earlier plan's tests, the derivation must say so, say why those inherited tests are the intended subject of that leg, and show the leg still turns RED without this task's own tests by stating the count the inherited tests alone supply and showing it falls BELOW the floor; a leg whose floor the inherited tests alone already clear NEVER counts as the leg this requirement demands. (b) HOMING — every behavior bullet is named by, or names, a test function or subtest that the task's own floor derivation counts; or it is homed to a named acceptance criterion WHOSE COMMAND APPEARS IN THIS TASK'S `<automated>` GATE, because prose is not a home and a criterion no gate executes is not an executable owner; or the derivation states in writing that the bullet is deliberately out of scope and names the plan or phase that owns it instead, which a bullet describing work THIS task performs may never claim. A bullet no derivation reaches is a described behavior with no executable owner, and a leg whose floor another plan's tests already clear cannot fail when this task writes nothing."
---

<objective>
Give the graph a commit identity, and give that identity a real path out of
`internal/query` to the wire.

Purpose: `schema.Meta` records no commit SHA today. A GitHub permalink that matches what
the UI just showed must point at the *indexed* commit, not at whatever HEAD happens to be
when the link is clicked — so `BRW-09` in Phase 3 is gated on this field existing. It is
sequenced here, with the protobuf work, so it inherits the same additive-only discipline
(D-02a) rather than being invented alongside a UI feature.

The field alone is not enough, and the first review of this phase caught why. `Engine.Status`
returns a `StatusResult` projection that has no commit field; `Engine.reader` is unexported;
`graphstore.Reader.GetMeta()` is unreachable from `internal/uiserver`; and a second
in-process `graphstore.Open` on the same directory is lock-refused. Adding the field
without adding an accessor would leave plan 01-09's "the Status response carries the commit
SHA" unachievable. So this plan ships both halves: the field, and
`(*Engine).IndexMeta` — an accessor that reads the metadata the Engine already reads,
placed deliberately outside `status.go` so D-06's "the CLI stays untouched" stays true and
visibly so.

Output: `Meta` field 8, one HEAD resolution per operation threaded to all three write
sites, an exported metadata accessor, and a recorded proof that `codegraph status`'s bytes
did not move.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-CONTEXT.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-RESEARCH.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-PATTERNS.md
@.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

**Created by this plan (01-06):**

- `internal/schema/graph.proto` — `Meta` gains `string commit_sha = 8`
- `internal/schema/graph.pb.go` — regenerated: `Meta.CommitSha` and `(*Meta).GetCommitSha()`
- `internal/schema/meta.go` — `IndexedCommitSHA(m *Meta) (string, bool)`
- `internal/schema/meta_commit_test.go` — `TestMetaCommitSHARoundTrips`, `TestMetaCommitSHAAbsentDegrades`, `TestKnownMetaFieldNumbersAreStable`
- `internal/indexer/commit.go` — `resolveHeadCommitSHA(repoPath string) string` and the `gitExecLookPath` test seam
- `internal/indexer/commit_test.go` — `TestResolveHeadCommitSHA`, `TestResolveHeadCommitSHAOnNonGitTree`, `TestResolveHeadCommitSHAWithNoGitBinary`, `TestHeadIsResolvedOncePerOperation`, `TestIndexRunStampsHeadCommitSHA`
- `internal/query/engine.go` — `(*Engine).IndexMeta() (*schema.Meta, error)`
- `internal/query/meta_test.go` — `TestIndexMetaCarriesTheStoredMeta`, `TestIndexMetaOnAGraphWithNoMeta`, `TestStatusResultFieldSetIsUnchanged`

**Created elsewhere in this phase** (listed so drift verification sees the whole set):
`internal/goldenspec`; `testdata/golden/byte_identity_test.go`; `internal/uiproto/uiv1`
(package, `ui.proto`, `UIService` and its nine rpcs, their request/response messages, the
shared `Node`/`Location`/`FileEntry`/`FileTreeNode`/`ExploreGroup`/`BlastEntry`/
`SourceBlob`/`IndexingInProgress`/`NodeDetailMode` wire types, generated `ui.pb.go` and
`uiv1connect/ui.connect.go`); `internal/uiserver` (`Options`, `DefaultAddr`, `Server`,
`Listen`, `Serve`, `URL`, `Close`, `originHostGuard`, `allowedHosts`, `allowedOrigins`,
`uiService`, `openEngine`, `withEngine`, `mapEngineError`, the nine mappers,
`uiMultiDefCap`, `truncateSource`, `sourceLineCap`, `sourceByteCap`,
`transportSendMaxBytes`, `transportReadMaxBytes`, `errIndexingInProgress`,
`indexingInProgressMessage`, `degradedStatus`, `classifyDegrade`); `internal/cli/ui.go`;
`internal/textutil.TruncateOnRuneBoundary`; `internal/query`'s `NodeDetail` family,
`ExploreResult` family, `ErrNotFound`, `ErrInvalidArgument`, `(*Engine).SourceFor` and
the `build*` builders;
`internal/mcp`'s `looksLikeJSONRPCResponse`, `sniffedOutbound`, `decrementPending`,
`pendingUnderflows`; `buf.yaml`, `buf.gen.yaml`, `go.tool-proto.mod`, `Taskfile.yml` tasks
`proto:gen` and `proto:drift`, `internal/upgrade/proto_task_test.go`.

## Gate conventions (phase-wide canon — byte-identical in all eleven plans, and checkable)

This section is replicated verbatim across `01-01-PLAN.md` … `01-11-PLAN.md`. The claim is
checkable rather than merely asserted — one command, run from the phase directory, prints
**one** distinct digest if the eleven copies agree and more than one if any has drifted:

```
for f in 01-*-PLAN.md; do sed -n '/^## Gate conventions/,/^<tasks>$/p' "$f" | shasum; done | sort -u | wc -l
```

A future planner editing one copy must edit all eleven, and this is how a reviewer catches it
if they do not. (Nothing at execution time changes this section, so no `<automated>` gate owns
it — the drift risk is at planning time, which is where the check belongs.)

**`--- PASS` counting.** `go test -v` prints one `--- PASS:` line for the parent test
function AND one for every `t.Run` subtest, including nested ones. Every PASS floor in this
phase counts BOTH: a test function with three subtests contributes FOUR lines, so
`rg -o -e '--- PASS' | wc -l` over `go test -v` output equals
`len(test functions) + len(all subtests)`. Derive every floor that way and show the
arithmetic in the acceptance criterion that states it, so a reviewer can check the floor
instead of re-deriving it.

**Echo label.** Every gate prints `PASS lines:` — never `PASS tests:`, never
`PASS subtests:`. The label names what is counted, and what is counted is lines.

**The conjunction (D-04 as corrected in `01-CONTEXT.md`).** A gate passes only when the
test command's exit status is 0 AND the `--- PASS` floor is met. Neither is sufficient
alone: `go test -run PATTERN` exits 0 when the pattern matches nothing, and a shell
pipeline's `$?` is the LAST command's, so a bare count passes with N passing subtests plus
one `--- FAIL`. The single scoped exception is a deliberate-mutation RED observation, where
a non-zero exit IS the expected evidence and the observation is scored by `--- FAIL` /
`--- PASS` counts rather than by status.

**The five vacuity shapes cycle 3 found in this phase's own gates.** Each is a prohibition
in `must_haves.prohibitions`. They are restated here together because they are one class —
a guard that reads green against a file it never opened:

1. `git status --porcelain <path>` exits 0 with empty stdout on a MISSING or typo'd path
   (the warning goes to stderr). Always prefix `test -d <path> &&`.
2. A zero-count gate positive-controlled against a DIFFERENT file proves nothing about the
   guarded file: `rg` prints nothing for a nonexistent path too. Control on the SAME file.
3. `rg -q 'compared [0-9]+ generated files'` accepts `compared 0 generated files`. Capture
   the number; assert the floor numerically.
4. An unanchored identifier grep (`\bgather[A-Z]`) is satisfiable by a trailing or `/* */`
   comment that `rg -v '^\s*//'` does not strip. Anchor to the declaration form.
5. An unranged `git diff` is empty by construction at verification time under
   atomic-commit-per-task. Assert over `<phase-base>..HEAD`.

**The scope shape cycle 5 found, which none of the five above reaches — and the cycle 7
amendment that made it causal.** The five shapes are all about how a gate is WRITTEN. A gate can
satisfy every one of them and still be vacuous because of what it is SCOPED to: if its floor is
already cleared by test functions belonging to an EARLIER plan that happen to share the selection
pattern, then the pattern matching none of this task's own tests costs nothing — `go test` exits 0
on a pattern that matches nothing, and the inherited tests supply the count. The sixth prohibition
in `must_haves.prohibitions` closes it. Cycle 5's form of that prohibition was STRUCTURAL where it
had to be CAUSAL: OWNERSHIP constrained who DECLARES the counted tests, not what those tests
ASSERT, and HOMING accepted a named criterion that no gate executes. Both halves were therefore
satisfiable literally by a task declaring one self-contained arithmetic test and homing every
behavior bullet to prose, with the deliverable entirely absent. The amended prohibition supplies
the missing causal clause. Half (a) now requires the leg to turn RED when the deliverable is
REPLACED BY A NO-OP — replacement, not deletion, because deletion is satisfied by a mere compile
dependency — and names the three forms a qualifying leg may take: A1, an own-test leg that names
its artifact under test; A2, a direct-artifact leg scoring a number read out of that artifact's own
output; and A3, an observation leg, open only to a task that declares no test at all and whose
deliverable is a recorded observation, which must carry a named measurement, a written STOP
CONDITION for a vacuous measurement, the strongest executable leg available to it, and a plain
statement of what that leg cannot catch. A3 is what keeps D-04's deliberate-mutation exception
representable. A leg whose pattern also matches an earlier plan's tests must show the inherited
count alone falls BELOW the floor, or it does not qualify. Half (b) now requires a criterion's
command to appear in the task's `<automated>` gate before that criterion counts as a home. Every
floor derivation in this phase names its form. All of it is checkable by reading the plan, which is
where the defect lives.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: `Meta` field 8 is a one-way decision</name>
  <reversibility rating="one-way">Under D-02a's additive-only discipline, a shipped field number can never be renumbered or reused. Retiring field 8 later requires adding 8 to a `reserved` clause, not deleting it. The on-disk format is a published contract to every already-installed graph.</reversibility>
  <decision>Lock the `Meta` field-8 assignment: the indexed commit SHA takes field number 8, typed `string`, named `commit_sha`, following the `has_file_index = 7` precedent (D-05, ENG-04).</decision>
  <context>
    **Autonomy note, recorded because the cross-AI review flagged it.** 01-CONTEXT.md's
    D-05 already locks the number, the type, the name and the precedent. This checkpoint
    therefore adds no new information and costs one blocking pause. It is kept anyway for
    one reason and one only: the field number is permanently unrecoverable once a build
    stamps it into a real store, and `REVERSIBILITY_GATES` is enabled precisely so that
    class of decision gets a human eye before it ships. If you have no change to make,
    reply "proceed" — that is the expected answer.

    What is being locked:

    - **Field number 8 is spent permanently.** Once a build stamps it into a real store,
      no future schema change may reuse 8 for anything else, and no future change may
      renumber it. If this turns out to be the wrong field, the only correction is
      `reserved 8;` plus a new number.
    - **Type is `string`, not `bytes`.** A lowercase hex object ID — 40 characters under
      Git's SHA-1 object format, 64 under SHA-256. Both lengths are accepted, because a
      40-only check would silently record an ABSENT commit on a SHA-256 repository and the
      permalink would just quietly stop working with no error anywhere. Chosen for
      legibility in decoded output and direct use in a GitHub permalink without
      re-encoding. A `bytes` field would be half the size but would need encoding at every
      read site, and would have to carry the object format out of band to be decodable.
    - **Absent means a pre-upgrade graph**, matching `has_file_index`'s precedent exactly.
      An empty string is indistinguishable from absent in proto3, and that is deliberate:
      both cases degrade the same way.
    - **No `SchemaVersion` bump.** `internal/schema/meta.go` states that a well-formed
      additive change never requires one.

    Alternatives considered and rejected: a separate `meta/commit` key outside the `Meta`
    record (invents a second staleness mechanism and a second read path); storing the full
    `git describe` output (not stable, not permalinkable); deferring the field until Phase
    3 needs it (would land it beside a UI feature instead of inside the schema discipline,
    which is the sequencing the ROADMAP explicitly chose against).
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with `string commit_sha = 8` as specified</name>
      <pros>Follows the `has_file_index = 7` precedent exactly; legible in decoded output; directly permalinkable; unblocks Phase 3's BRW-09; no SchemaVersion bump; matches D-05 as already locked</pros>
      <cons>Field number 8 is spent permanently; 40 bytes on disk per Meta record rather than 20</cons>
    </option>
    <option id="rename-or-retype">
      <name>Change the field name or type before it ships</name>
      <pros>Free to change now; impossible to change after any build stamps it into a real store</pros>
      <cons>Diverges from the `has_file_index` doc-comment precedent unless the new shape is documented the same way; a `bytes` type needs encoding at every read site; reopens a decision CONTEXT already closed</cons>
    </option>
    <option id="defer">
      <name>Defer ENG-04 out of this phase</name>
      <pros>Nothing is locked; the field can be designed later with more information</pros>
      <cons>Phase 3's BRW-09 becomes blocked; plan 01-09's Status response loses its commit SHA; the field would then land beside a UI feature instead of inside the schema discipline, which is the sequencing the ROADMAP explicitly chose against</cons>
    </option>
  </options>
  <resume-signal>Reply "proceed" to lock `string commit_sha = 8`, or name the alternative and the field name/type/number to use instead.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: `Meta` field 8, its absent-value contract, and a subset-stable field-number guard</name>
  <files>internal/schema/graph.proto, internal/schema/graph.pb.go, internal/schema/meta.go, internal/schema/meta_commit_test.go</files>
  <read_first>
    - internal/schema/graph.proto lines 130-152 — the `Meta` message, fields 1-7, and `has_file_index = 7`'s doc-comment convention: name the field, cite the phase and decision that added it, state the absence behavior
    - internal/schema/meta.go — the D-02a package comment stating field numbers are never renumbered or reused, `SchemaVersion`, `NewMeta()`, and `IsCurrentSchemaVersion`'s nil-safe getter shape
    - internal/schema/roundtrip_test.go — the existing serialization round-trip convention this plan's new test sits beside without replacing
    - .planning/phases/01-engine-seam-wire-protocol-secure-transport/01-01-SUMMARY.md — the recorded generated-header restamp and the phase ruling that a header-only rewrite counts as drift
  </read_first>
  <behavior>
    - `TestMetaCommitSHARoundTrips`: a `Meta` with the commit field set marshals and unmarshals with the value intact.
    - `TestMetaCommitSHAAbsentDegrades`: a `Meta` deserialized from raw bytes that genuinely contain no field 8 unmarshals successfully, the getter returns the empty string, and `IndexedCommitSHA` returns `("", false)`.
    - `TestKnownMetaFieldNumbersAreStable`: every KNOWN field name maps to its expected number in the generated descriptor, no two fields share a number, and the count of known fields checked is logged and asserted non-zero. Additional fields beyond the known set are permitted.
  </behavior>
  <action>
    Add field 8 to the `Meta` message in `internal/schema/graph.proto`: a `string` named
    `commit_sha`. Give it a doc comment in exactly `has_file_index = 7`'s shape — name what
    it records (the git commit the index was built at), cite this phase and D-05, and state
    the degrade behavior verbatim: absent or empty means a pre-upgrade graph or a
    non-git-checkout, and consumers must treat it as unknown rather than as an error. State
    the additive-only consequence in the same comment: this number is now spent
    permanently. Do not bump `SchemaVersion` — `internal/schema/meta.go`'s own package
    comment states a well-formed additive change never requires one.

    Regenerate with plan 01-01's `task proto:gen`. Plan 01-01 already absorbed and committed
    the one-time generated-header restamp, so from here the ONLY expected diff in
    `internal/schema/graph.pb.go` is the new field and its getter. If the regeneration
    produces anything beyond that, stop and report it rather than committing it: that diff
    is latent pre-existing drift and it is exactly what BLD-04 exists to surface.

    Add `IndexedCommitSHA(m *Meta) (string, bool)` to `internal/schema/meta.go`, following
    `IsCurrentSchemaVersion`'s nil-safe getter shape: a nil `m` returns `("", false)`, an
    empty value returns `("", false)`, otherwise `(sha, true)`. Callers get one place to ask
    "do we know the indexed commit" rather than each re-deriving the empty-means-absent
    convention.

    Write `internal/schema/meta_commit_test.go`.

    `TestMetaCommitSHAAbsentDegrades` must construct its input from raw wire bytes that
    genuinely omit field 8 — not from a Go struct with the field left at its zero value —
    so it proves wire-level backward compatibility rather than Go-level zero-value
    behavior.

    `TestKnownMetaFieldNumbersAreStable` is deliberately SUBSET-based, not
    exact-cardinality. An assertion that `Meta` has exactly eight fields would fail on every
    legitimate future additive field, which turns the guard into an obstacle rather than a
    net and guarantees someone eventually edits the expectation without thinking. Instead:
    read the generated descriptor, take a literal fixture of known field-name to
    field-number mappings (carrying a provenance comment naming `graph.proto` and the date
    transcribed), assert each known name still carries its expected number, assert no two
    fields in the message share a number, and log plus assert a non-zero count of known
    mappings checked. That catches a renumber and a collision — the two things D-02a
    actually forbids — while an added field 9 passes, as it should.
  </action>
  <verify>
    <automated>go build ./... && { OUT=$(go test -v -count=1 -run 'TestMetaCommitSHA|TestKnownMetaFieldNumbersAreStable' ./internal/schema/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 3 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -run 'TestMetaCommitSHA|TestKnownMetaFieldNumbersAreStable' ./internal/schema/` emits at least 3 `--- PASS` lines, and the command exits 0 — both conditions, never either alone.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** Three test functions match and none declares subtests: `TestMetaCommitSHARoundTrips`, `TestMetaCommitSHAAbsentDegrades`, `TestKnownMetaFieldNumbersAreStable`. Total **3** against a floor of 3: zero headroom by design. **Ownership:** every atom in this gate's `-run` pattern names a test function THIS task declares; no test from any other plan matches any atom, so the whole count is this task's own deliverable and the floor goes unmet if that deliverable is absent. Behavior-bullet homes: all 3 bullets in this task's behavior block name their own test function and all are counted here, so no bullet is orphaned. **Form A1 (own-test), causation.** The artifact under test is `Meta`'s field 8 `commit_sha` in `internal/schema/graph.proto` and its generated accessor in `graph.pb.go`, both named in this task's files list. For a generated or declarative artifact the no-op is the empty or default-valued version: leave field 8 unset and always default, and `TestMetaCommitSHARoundTrips` turns RED on the value it wrote and read back while `TestMetaCommitSHAAbsentDegrades` turns RED on the degraded contract it asserts; strip the field from the descriptor and `TestKnownMetaFieldNumbersAreStable` turns RED on its subset fixture.
    - `TestKnownMetaFieldNumbersAreStable` reads field names and numbers from the generated protobuf descriptor rather than from a source grep, asserts each known name keeps its number, asserts no duplicate numbers, and logs a non-zero count of mappings checked. It does NOT assert a total field count.
    - `TestMetaCommitSHAAbsentDegrades` constructs its input from raw bytes that omit field 8.
    - `git status --porcelain internal/schema/` is empty — run behind the `test -d internal/schema &&` existence guard, without which the check passes vacuously on a missing or typo'd path immediately after `task proto:gen` runs.
    - The SUMMARY records the `git diff internal/schema/graph.pb.go` from this regeneration and states that it contains the new field and its getter and nothing else.
    - `internal/schema/roundtrip_test.go` still passes untouched.
  </acceptance_criteria>
  <done>The indexed commit SHA has a permanent, documented home in `Meta` with an explicit absent-value contract and a guard that survives future additive fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve HEAD once, stamp it at all three sites, and expose the metadata to the wire</name>
  <files>internal/indexer/commit.go, internal/indexer/commit_test.go, internal/indexer/resolve.go, internal/indexer/sync.go, internal/query/engine.go, internal/query/meta_test.go</files>
  <read_first>
    - internal/indexer/resolve.go around `writeGraph`'s Meta construction — including the Phase-4 D-02b comment on why `HasFileIndex` is stamped there and the D-04a comment on why `LastSyncUnixMs` is too
    - internal/indexer/sync.go around the mtime-refresh-only `PutMeta` site and the incremental `PutMeta` site — the two other write sites, and what each function already has in scope
    - internal/query/engine.go lines 30-60 and 150-200 — the `Engine` struct's unexported `reader` field, and `OpenAt`'s construction path
    - internal/graphstore/store.go around `Reader.GetMeta` — its signature and its `ErrNotFound` behavior on a graph with no Meta record
    - internal/query/status.go lines 40-90 (`StatusResult`, declared at `:47`) and the `Status(ctx)` body at `:247` — how `Status` projects `Meta` selectively and the note that `edgesByKind` is deliberately not stored in `Meta`; this is the file that must NOT change
    - tools/corpora/main.go around line 331 — `runGit(dir, "rev-parse", "HEAD")`, the in-repo precedent for shelling out to git, as a shape reference only (it lives in `tools/`, not importable from `internal/`)
  </read_first>
  <behavior>
    - `TestResolveHeadCommitSHA`: against a temp git repo with one commit, returns that commit's lowercase hex SHA. Covered for BOTH object formats: a default (SHA-1) repository yields 40 characters, and a repository created with `git init --object-format=sha256` yields 64. If the local `git` is too old to support `--object-format`, skip that subtest with an explicit `t.Skip` naming the reason — and assert a length-validation unit case over a synthetic 64-character lowercase hex string regardless, so the 64 path is never left entirely unexercised.
    - A 39-character, a 41-character, a 63-character, a 65-character and an uppercase-hex output each yield the empty string, so accepting two lengths did not turn into accepting any length.
    - `TestResolveHeadCommitSHAOnNonGitTree`: against a temp directory that is not a git repo, returns the empty string and no panic.
    - `TestResolveHeadCommitSHAWithNoGitBinary`: with the injected executable lookup returning "not found", returns the empty string and no panic. The lookup is injected; `PATH` is not emptied, because emptying `PATH` can also break the test's own helper processes and would make the test's failure mode ambiguous.
    - `TestHeadIsResolvedOncePerOperation`: with the injected lookup counting invocations, one full index run resolves HEAD exactly once, and one incremental sync run resolves it exactly once — not once per `PutMeta`.
    - `TestIndexRunStampsHeadCommitSHA`: a real `codegraph index` run over a real temp git checkout produces a `Meta` whose commit field equals `git rev-parse HEAD` for that checkout, read back from the store after the run; and over a non-git directory the run succeeds with an empty value. Two `t.Run` subtests, `git-checkout` and `non-git-tree`. This is ENG-04's end-to-end property and it previously had no owner: counting resolutions (`TestHeadIsResolvedOncePerOperation`) proves the lookup happened, and the three-assignment source grep proves assignment statements exist, but neither proves the resolved SHA actually reaches `Meta`. `TestIndexMetaCarriesTheStoredMeta` reads back whatever the store already holds for a fixture, which does not pin the value to `git rev-parse HEAD` either. This test is the only one that closes end to end.
    - `TestIndexMetaCarriesTheStoredMeta`: `(*Engine).IndexMeta()` returns the `*schema.Meta` the store holds, including the commit field, for an indexed fixture.
    - `TestIndexMetaOnAGraphWithNoMeta`: a store with no Meta record yields `(nil, nil)` — absent, not an error — so a pre-upgrade graph degrades rather than failing the Status RPC.
    - `TestStatusResultFieldSetIsUnchanged`: `StatusResult`'s reflected field-name set equals a literal fixture recorded from before this plan, with a length assertion.
  </behavior>
  <action>
    Create `internal/indexer/commit.go` with `resolveHeadCommitSHA(repoPath string) string`.
    Shell out to `git -C <repoPath> rev-parse HEAD` with a bounded `exec.CommandContext`
    timeout and a fixed argument vector — never a shell string, and never with the path
    interpolated into an argument. Trim the output and return it only when it is exactly 40 OR
    exactly 64 characters of lowercase hex — 40 for Git's SHA-1 object format and 64 for
    SHA-256, both of which `git rev-parse HEAD` can legitimately emit. Accepting only 40
    would silently record an absent commit on a SHA-256 repository, which surfaces as a
    permalink that never appears rather than as any error. Otherwise return the empty
    string. Name both lengths as named constants or as one validated set, not as two bare
    numeric literals at the comparison site. Return the empty
    string, never an error, on every failure path: `git` absent, the directory not being a
    git checkout, a repository with no commits yet, a detached or broken HEAD, or the
    timeout firing. Indexing must never fail because a commit could not be resolved.
    Document that reasoning on the function, since "returns a string, never an error" is an
    unusual signature that will otherwise look like a swallowed error.

    Add a package-level `gitExecLookPath` var initialised to `exec.LookPath`, doc-commented
    in the shape `internal/graphstore`'s `openLockRetrySleep` uses — an unexported test-only
    control seam, no exported setter, no production behavior change. `resolveHeadCommitSHA`
    consults it before invoking git. This is what makes the no-git-binary case testable
    deterministically: emptying `PATH` for the whole process would also affect any helper
    the test itself needs and would make a failure ambiguous about its own cause.

    **Resolve HEAD once per operation and thread it.** Do not call `resolveHeadCommitSHA`
    at each `PutMeta` site. HEAD can move between two calls in one run — a rebase, a
    checkout, a concurrent commit — and two write sites in the same operation recording
    different commits is worse than recording none. It also adds a subprocess to the
    mtime-refresh-only sync path, which otherwise does no process work at all. Resolve once
    at the top of each operation (the full index run, and each sync run), carry the value
    down to whichever functions construct `Meta`, and stamp it beside the existing
    `HasFileIndex` and `LastSyncUnixMs` assignments — for the same stated reason
    `writeGraph`'s Phase-4 comment gives: stamping at only one site leaves graphs built
    through the other paths permanently missing it. The three sites are
    `internal/indexer/resolve.go`'s `writeGraph`, `internal/indexer/sync.go`'s
    mtime-refresh-only path, and `internal/indexer/sync.go`'s incremental path. Where a
    function already has the repository path in scope, prefer threading the resolved value
    over threading the path.

    **Add the data path to the wire.** Add `(*Engine).IndexMeta() (*schema.Meta, error)` to
    `internal/query/engine.go`. It calls `e.reader.GetMeta()` and returns `(nil, nil)` when
    the store reports not-found, so a pre-upgrade graph is absent rather than an error. Its
    doc comment must state three things: that it exists because `Engine.reader` is
    unexported and `graphstore.Reader.GetMeta` is otherwise unreachable from a wire layer;
    that a second `graphstore.Open` on the same directory is lock-refused, so there is no
    alternative path; and that it is deliberately NOT a `StatusResult` field, because
    `codegraph status`'s output bytes are unguarded by any golden and D-06 keeps them
    frozen. Put it in `engine.go` rather than `status.go` so the file layout itself records
    that `status.go` was not touched.

    Do not touch `internal/query/status.go`. Do not add a field to `StatusResult`. Do not
    surface the SHA in `codegraph status` output. `status.go` already projects `Meta`
    selectively, so adding a field to `Meta` does not leak into CLI output on its own — but
    confirm that rather than assuming it, by capturing `codegraph status --json` for a
    fixture repository before and after and diffing them.

    Write `internal/query/meta_test.go` and `internal/indexer/commit_test.go` covering the
    behavior block. `TestIndexRunStampsHeadCommitSHA` goes in `internal/indexer/commit_test.go`
    and must drive a REAL index run over a REAL temp git checkout it creates itself, then read
    the commit field back out of the store and compare it against `git rev-parse HEAD` executed
    independently in the test against that same checkout. Do not assert it against the value the
    resolver returned — that would compare the code to itself. Its second subtest runs the same
    index over a directory that is not a git repository and requires success with an empty
    value, so degradation is proven and not assumed.
  </action>
  <verify>
    <automated>go build ./... && { OUT=$(go test -v -count=1 -run 'TestResolveHeadCommitSHA|TestHeadIsResolvedOncePerOperation|TestIndexRunStampsHeadCommitSHA|TestIndexMeta|TestStatusResultFieldSetIsUnchanged' ./internal/indexer/ ./internal/query/ 2>&1); STATUS=$?; COUNT=$(printf '%s\n' "$OUT" | rg -o -e '--- PASS' | wc -l | tr -d ' '); echo "exit=$STATUS PASS lines: $COUNT"; [ "$STATUS" -eq 0 ] || printf '%s\n' "$OUT"; [ "$STATUS" -eq 0 ] && [ "$COUNT" -ge 15 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -count=1 -run 'TestResolveHeadCommitSHA|TestHeadIsResolvedOncePerOperation|TestIndexRunStampsHeadCommitSHA|TestIndexMeta|TestStatusResultFieldSetIsUnchanged' ./internal/indexer/ ./internal/query/` emits at least 15 `--- PASS` lines, and the command exits 0 — both conditions, never either alone. The `TestIndexRunStampsHeadCommitSHA` atom is NEW in the pattern: adding the test without adding the atom would have left it unrun by the only gate that scores this task, which is the same shape of defect as writing no test at all.
    - **Floor derivation (phase-wide counting convention: one `--- PASS` line per test function PLUS one per `t.Run` subtest).** EIGHT test functions match, enumerated in full so the count word and the list agree — the earlier form said "eight" and then listed seven, which is a one-word defect in a derivation whose whole purpose is to be checked. Of the eight, two declare subtests. (1) `TestResolveHeadCommitSHA` runs one `t.Run` per case — `sha1-40-hex`, `sha256-64-hex`, and the five invalid forms `39`, `41`, `63`, `65`, `uppercase` — giving 1 + 7 = 8. (2) `TestIndexRunStampsHeadCommitSHA` runs two (`git-checkout`, `non-git-tree`) = 3. Plus (3) `TestResolveHeadCommitSHAOnNonGitTree`, (4) `TestResolveHeadCommitSHAWithNoGitBinary`, (5) `TestHeadIsResolvedOncePerOperation`, (6) `TestIndexMetaCarriesTheStoredMeta`, (7) `TestIndexMetaOnAGraphWithNoMeta`, (8) `TestStatusResultFieldSetIsUnchanged` at 1 each = 6. Total **17** against a floor of 15. If the `--object-format=sha256` subtest skips on a `git` that does not support it, a skip prints `--- SKIP` and not `--- PASS`, leaving 16 — still above the floor, which is why the floor is 15 and not 17. The floor is 15 rather than the earlier 9 SO THAT `TestIndexRunStampsHeadCommitSHA` is load-bearing: without it the total is 14, or 13 with the skip, and the gate goes red. At a floor of 9 the new test would have been headroom and its absence unscoreable — the exact shape this plan's sixth prohibition forbids. All eight functions are declared by THIS task, so no inherited test contributes to the count. **Form A1 (own-test), causation.** The artifacts under test are `resolveHeadCommitSHA` and the three stamp sites this task wires, named in this task's files list. Replace `resolveHeadCommitSHA` with a no-op that still compiles and returns the empty string and all seven `TestResolveHeadCommitSHA` subtests turn RED on the SHA each asserts, `TestHeadIsResolvedOncePerOperation` turns RED on its counted invocations, and `TestIndexRunStampsHeadCommitSHA` turns RED on the stamped value — which is exactly why the floor is 15 and that test is load-bearing rather than headroom.
    - **Behavior-bullet homes.** Every bullet in this task's behavior block is owned by a test function or named subtest the derivation above counts. Seven bullets name their own function. The one remaining plain-prose bullet — the five invalid SHA lengths and the uppercase-hex form — is owned by `TestResolveHeadCommitSHA`'s `39`, `41`, `63`, `65` and `uppercase` subtests, named explicitly in the derivation. The ENG-04 end-to-end bullet, which had no owner at all in the earlier form, now names `TestIndexRunStampsHeadCommitSHA`. No bullet is orphaned.
    - `TestHeadIsResolvedOncePerOperation` reports the observed resolution count and asserts it is exactly 1 for a full index run and exactly 1 for a sync run. A count equal to the number of write sites fails.
    - `TestResolveHeadCommitSHAWithNoGitBinary` drives the injected `gitExecLookPath` seam rather than emptying `PATH`, and asserts an empty-string return with no panic.
    - `TestResolveHeadCommitSHA` covers both Git object formats: a default repository returns 40 lowercase hex characters and a `--object-format=sha256` repository returns 64. If `--object-format` is unsupported by the local `git`, that subtest calls `t.Skip` with the reason named, and the synthetic 64-character length-validation case runs unconditionally so the 64 path is never silently untested.
    - The length check rejects 39, 41, 63, 65 and uppercase-hex outputs, each asserted to yield the empty string, so accepting two lengths did not become accepting any length.
    - All three `PutMeta` call sites assign the commit field. Asserted by a source check over `internal/indexer/` counting assignments of the generated field and requiring exactly 3, positive-controlled against the count of `HasFileIndex` assignments at the same sites in the same invocation, with comment lines stripped first.
    - `TestIndexMetaOnAGraphWithNoMeta` asserts `(nil, nil)` — absent, not an error.
    - `TestStatusResultFieldSetIsUnchanged` asserts set equality of `StatusResult`'s reflected field names against a literal fixture with a provenance comment and a length assertion, so a field added there fails.
    - `internal/query/status.go` is unmodified by this plan, asserted over a revision RANGE and not over the working tree: `PB=$(git merge-base HEAD main); git diff --stat "$PB"..HEAD -- internal/query/status.go` prints NOTHING, positive-controlled in the same invocation by `git diff --stat "$PB"..HEAD -- internal/indexer/` printing a NON-EMPTY stat, which proves the range genuinely spans this plan's commits. An unranged `git diff --stat <path>` would have asserted nothing at all here: GSD commits atomically per task, so the working tree is clean at verification time and the command is empty by construction whether or not the file was edited. `main` is the phase base because this milestone's phase 1 branches directly from it; if the phase base is recorded elsewhere in the manifest, use that commit instead of the merge-base.
    - `codegraph status --json` output for a fixture repository is byte-identical before and after this change, captured and diffed by hand and the diff result recorded in the SUMMARY.
    - `go test -v -count=1 -run 'TestGoldensMatchLiveEngineOutput$' ./testdata/golden/` exits 0 AND still reports `attempted=26 completed=26 matched=26` — a `Meta` field cannot affect node or explore output, and this confirms it.
  </acceptance_criteria>
  <done>HEAD is resolved once per operation and recorded at every write site, degrades to unknown wherever git cannot answer, reaches no CLI output, and is reachable by the wire layer through one exported accessor.</done>
</task>

</tasks>

## Flagged Assumptions (unresolved edge probes)

The spec-less edge probe classified ENG-04 as `unclassified`. Per the probe's rules it is
not auto-resolved into `must_haves`; it is surfaced here as an open assumption. There is
exactly ONE open assumption below, so the phase's probe ledger stays at 21 — 15
edge-tagged `must_haves` (including 01-01's `verification: backstop`) plus 6 flagged
assumptions.

Cycle 2's SHA-1-versus-SHA-256 object-format question is deliberately NOT listed here as a
seventh assumption: it was CLOSED by widening `resolveHeadCommitSHA` to accept both 40- and
64-character lowercase hex, with negative cases at 39, 41, 63, 65 and uppercase. See the
Review feedback dispositions table below. Recording it as an assumption would have been the
wrong disposition anyway, because its failure mode is silent — an absent SHA is
indistinguishable from a pre-upgrade graph by design — so it would have surfaced in Phase 3
as a permalink that simply never renders.

- **ENG-04 / unclassified** — assumed: "the indexed commit" means `HEAD` of the working
  tree at the START of the index run, resolved once. A repository indexed mid-rebase, in a
  detached-HEAD state, or with uncommitted changes records the SHA of whatever `HEAD`
  pointed at then, with no dirty-tree marker, and a HEAD that moves during the run is
  deliberately ignored rather than re-read. `Engine.WorktreeMismatch` is the existing,
  separate mechanism for the dirty case, and this field deliberately does not duplicate
  it. If Phase 3's permalink needs a dirty-tree signal, it composes the two rather than
  extending this field.

## Review feedback dispositions

| Finding | Severity | Disposition |
|---|---|---|
| Cycle-2 LOW: cross-plan artifact manifest drift — `(*Engine).SourceFor` missing from some "Created elsewhere in this phase" manifests | LOW | **Incorporated, and widened beyond what was flagged.** The review named 01-02 and 01-05. Auditing all eleven manifests found the same omission in 01-01, 01-03, 01-06 and 01-07 as well. `(*Engine).SourceFor` (declared by 01-04) is now named in every plan's manifest, so drift verification sees the whole phase-wide new-symbol set from any plan it starts at. |
| Cycle-2 HIGH C2-2: automated PASS-count gates discard the test command's exit status | HIGH | **Incorporated across every gate in this plan.** A shell pipeline's `$?` is the LAST command's, so `COUNT=$(go test ... | rg '--- PASS' | wc -l); [ "$COUNT" -ge N ]` passed with N passing subtests plus a `--- FAIL`. Every `<automated>` gate now captures `go test`'s output into `OUT` and its status into `STATUS` BEFORE any pipe, counts from `OUT`, dumps `OUT` on failure, and requires `STATUS -eq 0` AND the `--- PASS` floor. The conjunction is the rule: the count alone is vacuous when a test fails, the status alone is vacuous when `-run` matches nothing. A plan-level prohibition against the bare count-only form is in `must_haves.prohibitions`. The only scoped exception is a deliberate-mutation RED observation, where a non-zero status is the expected evidence. |
| No data path from `Meta.commit_sha` to the Status RPC; 01-06's must_have unachievable as planned | HIGH | **Incorporated.** `(*Engine).IndexMeta` is added in `engine.go`, deliberately not in `status.go`, with a doc comment recording why no alternative path exists. D-06 stays intact: `StatusResult` is unchanged and pinned by a reflected field-set test, and `codegraph status --json` byte-identity is captured and recorded. |
| `git rev-parse` at each `PutMeta` site adds a subprocess and risks inconsistent resolution | MEDIUM | **Incorporated.** HEAD is resolved once per operation and threaded; `TestHeadIsResolvedOncePerOperation` counts resolutions through the injected seam and asserts exactly 1. |
| `TestMetaFieldNumbersAreStable` asserting exactly eight fields breaks on every future additive field | MEDIUM | **Incorporated.** Replaced with a subset-based known-mapping fixture plus duplicate-number detection plus a non-zero inspected count. No total-field assertion. |
| Cycle-2 LOW: SHA validation assumes the SHA-1 object format | LOW | **Incorporated by widening rather than by documenting.** `resolveHeadCommitSHA` accepts 40- OR 64-character lowercase hex, so a SHA-256 repository records its commit instead of silently recording none. The alternative offered — leaving it at 40 and stating the limitation in ENG-04's flagged assumption — was rejected: the failure mode is silent (an absent SHA is indistinguishable from a pre-upgrade graph by design), so a documented limitation would be discovered in Phase 3 as a permalink that never renders. Negative cases at 39, 41, 63, 65 and uppercase keep the widening an enumerated set rather than a relaxation. |
| Emptying `PATH` for the no-git test may break helper processes | LOW | **Incorporated.** `gitExecLookPath` injection seam; `PATH` is left alone. |
| Generate into a temp tree rather than in place before comparing | MEDIUM | **Deferred to plan 01-07 by design.** That finding is about the DRIFT GUARD, which is 01-07's deliverable; this plan performs an intentional regeneration whose diff is expected and reviewed, not a comparison. 01-07 carries the temp-tree requirement. |
| Both blocking checkpoints only reconfirm already-locked decisions | LOW (cross-plan) | **Kept, with the cost recorded.** The checkpoint's own context now states plainly that D-05 already locks everything, that it exists solely because `REVERSIBILITY_GATES` is enabled and the field number is unrecoverable, and that "proceed" is the expected reply. |
| 01-05 (old) was 90k with four tasks | — | **Split.** BLD-04's drift guard moved to plan 01-07; this plan is ENG-04 only, 2 tasks plus the checkpoint, 60k. |
| Cycle-3 M5: `git diff --stat internal/query/status.go` has no revision range, so it is empty by construction | MEDIUM | **Incorporated.** The criterion now asserts over `$(git merge-base HEAD main)..HEAD`, positive-controlled in the same invocation by requiring `internal/indexer/` to show a NON-EMPTY stat over the same range — which proves the range genuinely spans this plan's commits rather than being empty for a second reason. The criterion states why the unranged form asserted nothing: GSD commits atomically per task, so the working tree is clean at verification time. Phase-wide census: **one** `git diff` emptiness assertion existed; the other eleven `git diff` mentions are `--stat` applied-CONFIRMATIONS during deliberate mutations, where a non-empty result is the expected evidence and no range is wanted. |
| Cycle-3 L4: `status.go` 40-90 is cited for `Status(ctx)`, which is at `:247` | LOW | **Incorporated.** The bullet now names `StatusResult` at `:47` for the 40-90 range and `Status(ctx)` at `:247` separately. |
| Cycle-3 L1: the `--- PASS` counting convention is never stated anywhere in the phase, and echo labels contradict each other | LOW | **Incorporated phase-wide.** A byte-identical `## Gate conventions (phase-wide canon)` section now sits immediately above `<tasks>` in ALL ELEVEN plans. It states the convention once and canonically — one `--- PASS` line per test function PLUS one per `t.Run` subtest — mandates the single echo label `PASS lines:`, restates the D-04 conjunction with its one scoped exception, and enumerates the five vacuity shapes cycle 3 found. Every floor in this plan now carries an explicit **Floor derivation** bullet showing the arithmetic, so a reviewer checks the derivation rather than re-deriving it. Label census: six distinct labels existed across the phase (`PASS subtests:`, `PASS tests:`, `PASS:`, `post-restore PASS lines:`, `PASS lines across both tests:`, `uiserver PASS lines:`); all six were normalised to `PASS lines:`. |
| Cycle-5: terminate the SCOPE class — a gate can satisfy all five shape prohibitions and still be unable to fail | HIGH (phase-wide) | **Incorporated phase-wide as a sixth, SEMANTIC prohibition**, identical in all eleven plans and restated in the `## Gate conventions` canon. The five existing prohibitions are all shape-based: they catch a malformed command. A gate can be well-formed, parse, honor exit status and carry a same-file positive control and STILL be unable to fail, because its floor is already cleared by test functions belonging to an EARLIER plan that share its selection pattern — the pattern matching none of this task's own tests then costs nothing. The sixth prohibition bans exactly that, with two checkable requirements: (a) OWNERSHIP — every gate carries at least one leg scoped to test functions the task itself declares, with a floor derived from that leg alone, and any pattern that also matches an earlier plan's tests must say so and say why those tests are the intended subject; (b) HOMING — every behavior bullet is named by, or names, a test function or subtest the task's own floor derivation counts, or the derivation states in writing that it is covered by a named criterion or deliberately out of scope. Unlike the five shape prohibitions it is not satisfiable by rewriting a command; it requires a structural leg and a written mapping. Every floor derivation in this plan now carries its ownership statement, and every task with a behavior block carries its bullet-home mapping. |
| Cycle-5 M5: ENG-04's end-to-end stamping property — a `codegraph index` run over a git checkout produces a `Meta` whose commit field equals `git rev-parse HEAD` — is owned by no test | MEDIUM | **Incorporated by adding the test AND extending the `-run` pattern, because either alone would have left it unscored.** All three candidate owners fail for it: `TestHeadIsResolvedOncePerOperation` asserts the injected lookup's INVOCATION COUNT, which is not the resolved value landing in `Meta`; `TestIndexMetaCarriesTheStoredMeta` reads back whatever the store already holds for a fixture; and the three-assignment source grep proves assignment statements exist, not that the SHA reaches `Meta`. `TestIndexRunStampsHeadCommitSHA` is added to `internal/indexer/commit_test.go` with two subtests (`git-checkout`, `non-git-tree`), driving a real index over a real temp checkout and comparing against `git rev-parse HEAD` executed independently in the test — never against the value the resolver returned, which would compare the code to itself. Its atom is added to the gate's `-run` pattern and the floor rises from 9 to 15 SO THAT the new test is load-bearing: without it the total is 14, or 13 with the sha256 skip, and the gate goes red. At the old floor of 9 the new test would have been headroom and its absence unscoreable — the exact shape the sixth prohibition forbids. |
| Cycle-5 L2: Task 2's derivation says "Eight test functions match" and then enumerates seven | LOW | **Incorporated, and the count word is now true rather than merely corrected downward.** With `TestIndexRunStampsHeadCommitSHA` added there really are eight, and the derivation now enumerates all eight explicitly and numbered, so the list and the count word can be checked against each other. Independently found by Codex in the same cycle. |
| Cycle-3: terminate the vacuity class rather than fixing instances | — | **Incorporated phase-wide.** Five new descriptor-less entries in `must_haves.prohibitions`, identical in all eleven plans, forbid: a `git status --porcelain` check without a `test -d` existence guard; a positive control that targets a different file than the assertion; a count-matching regex that accepts zero; an unanchored identifier grep; and an unranged `git diff` emptiness assertion. Each is a shape cycle 3 found in this phase's own gates, so a future author cannot reintroduce one without violating a stated prohibition. |
| Cycle-7 H-7-1: the sixth prohibition's two halves do not entail its own headline — OWNERSHIP constrains who DECLARES the counted tests, not what those tests ASSERT, and HOMING accepts a named criterion that no gate executes | HIGH (phase-wide) | **Incorporated phase-wide by AMENDING the sixth prohibition, not replacing it.** The reviewer and Codex independently constructed the same counterexample: a task declaring one self-contained arithmetic test, a leg selecting only that test at a floor of 1, and every behavior bullet homed to a prose criterion, satisfies both halves literally while the deliverable is entirely absent. The prohibition was STRUCTURAL where it had to be CAUSAL. Half (a) is now OWNERSHIP AND CAUSATION: a qualifying leg must turn RED when the deliverable is REPLACED BY A NO-OP — an implementation that still compiles and keeps the same signature but does nothing and returns zero values, or for a generated or declarative artifact its empty or default-valued version. Replacement, not deletion, is the standard, because deletion is satisfied by a mere compile dependency and only replacement separates a test that ASSERTS an artifact's behavior from one that merely MENTIONS it. Three forms are named, and every floor derivation in this plan now states which one it uses: A1 own-test, which must name its artifact under test; A2 direct-artifact, which EXECUTES the deliverable and scores a number read out of that artifact's own output; and A3 observation, open only to a task that declares no test function at all, which must carry a named measurement, a written STOP CONDITION for a vacuous measurement, the strongest executable leg available to it, and a plain statement of what that leg cannot catch. A leg whose pattern also matches an earlier plan's tests must additionally show that the inherited count alone falls BELOW the floor, or it does not qualify. Half (b) now requires a criterion's command to appear in the task's `<automated>` gate before that criterion counts as a home — prose is not a home. The five shape prohibitions from cycle 4 are untouched; the amended sixth is byte-identical across all eleven plans and the `## Gate conventions` canon restates it once, still one digest across all copies. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository working tree → `git rev-parse` subprocess | Indexing shells out to `git` against a caller-supplied repository path. The path originates from a CLI flag or the current working directory. |
| stored `Meta` → wire layer | `(*Engine).IndexMeta` is a new export that carries on-disk metadata out of `internal/query`. What it carries is what the developer's own index already records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Elevation of Privilege | the `git rev-parse` subprocess in `resolveHeadCommitSHA` | medium | mitigate | Invoked with a fixed argument vector through `exec.CommandContext` — never a shell string — with the repository path passed as a `-C` argument rather than interpolated. Output is accepted only when it is exactly 40 or exactly 64 characters of lowercase hex — the two Git object-ID formats — and anything else, including any other length or any uppercase character, yields the empty string. Widening from one length to two is an enumerated set, not a relaxation: the negative cases at 39, 41, 63, 65 and uppercase are all asserted. A bounded context timeout prevents a hung git from stalling an index run. The executable lookup goes through an injectable seam, so the absent-binary path is tested rather than assumed. |
| T-01-20 | Information Disclosure | `Meta.commit_sha` reaching a client | low | accept | A commit SHA of the repository the developer is already indexing on their own machine, served only over the loopback listener the SRV-02 guard protects. No new exposure beyond what the developer already has. |
| T-01-30 | Information Disclosure | `(*Engine).IndexMeta` widening the exported surface | low | mitigate | It returns the stored `*schema.Meta` and nothing more — the same record `Status` already reads. It adds no filesystem access, no store handle escape and no writer. Its doc comment names the exact reason it exists so a future contributor does not widen it further by analogy. |
| T-01-21 | Denial of Service | indexing blocked by an unavailable git | medium | mitigate | `resolveHeadCommitSHA` returns the empty string and never an error on every failure path, so no index run can fail because a commit could not be resolved. Asserted by the non-git-tree and injected-absent-binary tests. |
| T-01-31 | Tampering | `codegraph status` output drifting silently | medium | mitigate | No golden covers `status`. Three independent controls: `status.go` is asserted untouched by diff, `StatusResult`'s field set is pinned by reflected set equality against a literal fixture, and a before/after `--json` capture is diffed by hand and recorded. |
</threat_model>

<verification>
- `task test:unit` passes.
- `task test:golden` still reports `attempted=26 completed=26 matched=26`.
- `go vet ./internal/schema/... ./internal/indexer/... ./internal/query/...` is clean.
- `git status --porcelain internal/schema/` is empty — run behind the `test -d internal/schema &&` existence guard, without which the check passes vacuously on a missing or typo'd path after `task proto:gen`.
- `codegraph status --json` output is byte-identical to its pre-change capture.
</verification>

<success_criteria>
- `schema.Meta` carries the indexed commit SHA as additive field 8, resolved once per operation and populated at all three write sites, degrading to unknown wherever git cannot answer.
- `(*Engine).IndexMeta` gives the wire layer a real path to that value, and the path is proven by test rather than assumed.
- `codegraph status` output is unchanged, proven three independent ways.
- The `Meta` field-number guard catches a renumber and a collision without obstructing a future additive field.
</success_criteria>

<output>
Create `.planning/phases/01-engine-seam-wire-protocol-secure-transport/01-06-SUMMARY.md` when done
</output>
