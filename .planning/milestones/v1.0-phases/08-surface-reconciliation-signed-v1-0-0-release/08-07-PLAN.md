---
phase: 08-surface-reconciliation-signed-v1-0-0-release
plan: 07
type: tdd
wave: 5
depends_on: [08-06]
files_modified:
  - internal/cli/present/archtest/charm_cgo_test.go
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "the charm.land/* dependency closure reachable from internal/cli contains ZERO packages with CgoFiles — proven by a go-toolchain-authoritative check (go list -deps / go/packages), not by assertion"
    - "the check is non-vacuous: the charm.land closure is non-empty (>0 charm packages reachable), so a passing result means 'audited and clean', not 'nothing to audit'"
    - "the closure audit result is a set-membership property (which packages have CgoFiles), order-independent and stable across runs"
    - "the pre-existing CGo exception (tree-sitter via internal/indexer/internal/daemon) is disjoint from the charm.land closure — REL-01 proves 'no NEW CGo', not 'no CGo'"
    - "govulncheck ./... reports no findings on the current module graph (existing CI gate re-run post-Charm)"
    - "the reproducible double-build (ci.yml reproducibility job) still matches post-Charm; the SBOM (goreleaser Syft block) regenerates without error"
  artifacts:
    - internal/cli/present/archtest/charm_cgo_test.go
  key_links:
    - "internal/cli/present/archtest/charm_cgo_test.go → go/packages closure over charm.land/* → CgoFiles assertion (the durable regression guard for REL-01)"
  prohibitions:
    - "MUST NOT prove 'no new CGo' with a blanket CGO_ENABLED=0 build — the binary already links CGo tree-sitter, so a clean-0 build fails on pre-existing code and proves nothing about the Charm closure (RESEARCH D-07 anti-pattern)"
    - "MUST NOT reintroduce a CGO_ENABLED=0 expectation anywhere in CI — internal/daemon transitively imports CGo tree-sitter; Windows cross-vet needs gcc-mingw-w64 + CGO_ENABLED=1, not zig"
    - "MUST NOT add a new dependency to satisfy the audit — go list/go/packages are toolchain baseline"
---

<objective>
Audit the Phase-6/7 Charm/TUI dependency closure (REL-01, CONTEXT D-07): prove it introduces NO new CGo via a dependency-closure inspection (not a clean CGO_ENABLED=0 build), confirm `govulncheck ./...` is clean, regenerate the SBOM, and confirm the reproducible double-build still passes. Land the "no new CGo" proof as a durable, fail-closed Go guard test so a future Charm bump that pulls in a CGo package fails the build.

Purpose: The single-static-binary / no-new-CGo supply-chain story is load-bearing for v1.0.0; make it self-verifying rather than a one-time manual check.
Output: a charm-closure CGo guard test + recorded evidence that govulncheck/SBOM/double-build gates still pass post-Charm.

## Artifacts this phase produces (this plan)
- New test: `internal/cli/present/archtest/charm_cgo_test.go` (charm.land closure has zero CgoFiles).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-RESEARCH.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-PATTERNS.md
@internal/cli/present/archtest/import_graph_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: charm.land closure CGo guard test (REL-01, D-07)</name>
  <files>internal/cli/present/archtest/charm_cgo_test.go</files>
  <read_first>
    - internal/cli/present/archtest/import_graph_test.go (the go/packages load + closure-walk + fail-closed pattern to reuse)
    - 08-RESEARCH.md §"Package Legitimacy Audit" (the verified `go list -deps -json | jq` command and its actual output: charm_pkg_count 10, cgo_in_closure [])
    - 08-CONTEXT.md D-07 (closure inspection, NOT a CGO_ENABLED=0 build; the internal/daemon CGo carry-forward gotcha)
  </read_first>
  <behavior>
    - loading the transitive closure reachable from internal/cli, filter to ImportPath starting "charm.land"
    - assert that set is non-empty (charm_pkg_count > 0) — a vacuous pass (zero charm packages found) is itself a failure
    - assert no package in the charm.land closure has CgoFiles (len(pkg.CgoFiles) == 0 for every charm.land package and its charm-reachable deps)
    - the tree-sitter CGo packages (internal/indexer/... reachable) are NOT in the charm.land closure — the test scopes strictly to charm.land, so pre-existing CGo does not trip it
  </behavior>
  <action>
    Add charm_cgo_test.go in the archtest package. Using golang.org/x/tools/go/packages (as import_graph_test.go does) with NeedName|NeedFiles|NeedImports|NeedDeps, load "github.com/seanb4t/codegraph-go/internal/cli/...", walk the full transitive dep set, select packages whose PkgPath begins "charm.land", and assert: (a) the selected set is non-empty (guard against a vacuous pass); (b) every selected package has zero CgoFiles. Fail loudly listing any charm.land package that has CgoFiles. Keep the fail-closed philosophy: if package loading errors, fail the test rather than skip.
  </action>
  <verify>
    <automated>go test ./internal/cli/present/archtest/... -run CharmCgo -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/present/archtest/charm_cgo_test.go` loads the closure via go/packages and scopes to charm.land
    - the test asserts the charm.land set is non-empty AND every such package has zero CgoFiles
    - `go test ./internal/cli/present/archtest/... -run CharmCgo -count=1` exits 0
    - manually pointing the scope at a known-CGo package (e.g. a tree-sitter import path) during execution makes the test fail — proving the CgoFiles check is real — then reverted
  </acceptance_criteria>
  <done>The "no new CGo in the Charm closure" claim is a durable, non-vacuous, fail-closed Go test.</done>
</task>

<task type="auto">
  <name>Task 2: re-run govulncheck / SBOM / double-build gates post-Charm</name>
  <files>internal/cli/present/archtest/charm_cgo_test.go</files>
  <read_first>
    - 08-RESEARCH.md §"Package Legitimacy Audit" (govulncheck v1.6.0 already a CI gate; SBOM via goreleaser Syft; ci.yml reproducibility job — linux/amd64 hard-blocks)
    - .github/workflows/ci.yml (the govulncheck + reproducibility jobs — REUSE, do not edit)
    - .goreleaser.yaml (the sbom Syft block — REUSE)
  </read_first>
  <action>
    Re-run the existing supply-chain gates against the current (post-Charm) module graph and record the evidence in the SUMMARY: run `govulncheck ./...` (or the pinned CI binary) and confirm no findings; confirm the `go list -deps -json ./internal/cli/... | jq` closure command from RESEARCH still returns `cgo_in_closure: []`; confirm the goreleaser Syft SBOM block emits without error (`goreleaser check` / a dry SBOM run) and the ci.yml reproducibility (double-build) job still passes. Make NO edits to ci.yml, release.yml, or .goreleaser.yaml unless a gate genuinely fails and the fix is in-scope; if a gate fails, stop and record it as a blocker rather than silently patching CI. This task modifies no source file — its output is the recorded audit evidence next to the Task-1 guard test.
  </action>
  <verify>
    <automated>go test ./internal/cli/present/archtest/... -run CharmCgo -count=1</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records: govulncheck clean, `cgo_in_closure: []` reproduced, SBOM emits, double-build matches
    - no changes to ci.yml / release.yml / .goreleaser.yaml (or, if a gate failed, an explicit blocker is recorded instead of a silent CI patch)
    - the CGO_ENABLED=0 anti-pattern was NOT used as the proof
  </acceptance_criteria>
  <done>All pre-existing REL-01 gates confirmed green on the post-Charm graph, with the no-new-CGo claim proven by closure inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party Charm deps → single static binary | new transitive packages could introduce CGo, memory-unsafe scanners, or known vulns into the release artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07-01 | Tampering (supply chain) | charm.land closure | high | mitigate | Fail-closed guard test asserts zero CgoFiles in the (non-empty) charm.land closure; govulncheck gate re-run clean; SBOM regenerated; reproducible double-build confirmed — the full V14 control set |
| T-08-07-02 | Denial of Service (build integrity) | CI CGo expectation | medium | mitigate | Do not reintroduce CGO_ENABLED=0 anywhere; Windows cross-vet stays on gcc-mingw-w64 + CGO_ENABLED=1 (Phase-7 carry-forward) |
</threat_model>

<verification>
- `go test ./internal/cli/present/archtest/... -run CharmCgo -count=1` green (non-vacuous, zero CgoFiles)
- govulncheck clean; `go list -deps -json ./internal/cli/... | jq` reproduces `cgo_in_closure: []`
- ci.yml reproducibility job green; goreleaser SBOM emits
</verification>

<success_criteria>
The Charm closure is proven CGo-free by a durable fail-closed test, govulncheck/SBOM/double-build gates pass post-Charm, and no CGO_ENABLED=0 expectation is introduced.
</success_criteria>

<output>
Create `.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-07-SUMMARY.md` when done.
</output>
