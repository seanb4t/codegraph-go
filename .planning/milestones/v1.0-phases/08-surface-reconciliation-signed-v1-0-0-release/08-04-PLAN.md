---
phase: 08-surface-reconciliation-signed-v1-0-0-release
plan: 04
type: tdd
wave: 2
depends_on: [08-01]
files_modified:
  - internal/query/validate.go
  - internal/query/traverse.go
  - internal/query/traverse_test.go
  - internal/cli/affected.go
autonomous: true
requirements: [SURF-04]
must_haves:
  truths:
    - "Engine.Affected performs a depth-bounded BFS through file/symbol dependents (not the current single hop), bounded by clampAffectedDepth(depth) with defaultAffectedDepth=5 (NOT impact's 2) and the shared MaxDepth=50 ceiling"
    - "a dependent that passes isTestSymbol is recorded as an affected test AND is a LEAF — it is not expanded to the next depth level; only non-test dependents get queued for further expansion (TS test-files-as-leaves semantics)"
    - "affected --depth 0 uses defaultAffectedDepth=5; negative depth is rejected by validateDepth; depth>50 clamps to 50 — the same validation posture as impact but with the affected-specific default"
    - "the isTestSymbol heuristic is kept as-is (Go's _test.go / Test*/Benchmark* rule) — TS's JS path-suffix heuristic is NOT ported (accepted per-language divergence recorded by SURF-05)"
    - "a dangling reverse edge during BFS is skipped (errors.Is ErrNotFound → continue), never aborting the whole traversal — reusing Impact's exact skip-not-abort convention"
    - "defaultAffectedDepth lives beside defaultDepth in validate.go and is deliberately distinct from it (impact=2, affected=5); clampDepth is NOT reused unmodified for affected"
    - { statement: "Affected output ordering is deterministic across runs for identical input (BFS frontier + reverse-adjacency map iteration)", verification: backstop }
  artifacts:
    - internal/query/validate.go
    - internal/query/traverse.go
  key_links:
    - "internal/query/validate.go defaultAffectedDepth/clampAffectedDepth → internal/query/traverse.go Affected BFS → internal/cli/affected.go (call site) and future --depth flag (08-05)"
  prohibitions:
    - "MUST NOT reuse defaultDepth/clampDepth (now 2) for affected — affected's default is 5; a naive clampDepth call would silently apply impact's default"
    - "MUST NOT port TS's JS-specific isTestFile path heuristic — keep Go's isTestSymbol; the divergence is documented, not closed"
    - "MUST NOT introduce a second, inconsistent depth ceiling — reuse MaxDepth=50"
---

<objective>
Extend `Engine.Affected` from its current single-hop reverse-adjacency lookup into a real depth-bounded BFS with test-files-as-leaves pruning (SURF-04, CONTEXT D-05, RESEARCH Pitfall 2), and add an affected-specific `defaultAffectedDepth=5` constant with its own clamp — deliberately NOT reusing `impact`'s now-2 default. Update the single existing `Affected` call site in affected.go minimally so the `internal/cli` package still compiles (full flag wiring lands in 08-05).

Purpose: TS 1.3.1's `affected` is a depth-bounded BFS (default 5), not a one-hop lookup — the `--depth` flag genuinely changes traversal shape, not just the CLI surface. This is the engine half; the CLI half is 08-05.
Output: `Affected(files, depth)` BFS + `defaultAffectedDepth`/`clampAffectedDepth`, leaf-pruning on `isTestSymbol`, verified with new depth/multi-hop/leaf subtests.

## Artifacts this phase produces (this plan)
- New engine constant: `internal/query.defaultAffectedDepth = 5`.
- New engine helper: `clampAffectedDepth` (or a shared `clampDepthWithDefault(n, def)` reused by both clamps).
- Changed engine signature: `Engine.Affected(files []string, depth int)` (was `Affected(files []string)`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-RESEARCH.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-PATTERNS.md
@internal/query/validate.go
@internal/query/traverse.go
@internal/cli/affected.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: defaultAffectedDepth + clampAffectedDepth (SURF-04 engine constant)</name>
  <files>internal/query/validate.go, internal/query/traverse_test.go</files>
  <read_first>
    - internal/query/validate.go (defaultDepth — now 2 after 08-01 — and clampDepth lines 44-55; MaxDepth line 22)
    - 08-PATTERNS.md §"internal/query/validate.go (SURF-01 depth default)" — the SURF-04 companion-constant guidance
    - 08-RESEARCH.md §"Anti-Patterns to Avoid" (do not reuse clampDepth/defaultDepth for affected)
  </read_first>
  <behavior>
    - clampAffectedDepth(0) == 5 (defaultAffectedDepth, NOT 2)
    - clampAffectedDepth(3) == 3
    - clampAffectedDepth(50) == 50; clampAffectedDepth(51) == 50 (MaxDepth ceiling)
  </behavior>
  <action>
    Add a `defaultAffectedDepth = 5` constant beside `defaultDepth`, with a doc comment stating affected's default differs from impact's per TS 1.3.1 (impact=2, affected=5) and shares MaxDepth as its ceiling. Add either a `clampAffectedDepth(n int) int` mirroring clampDepth's body with defaultAffectedDepth, OR refactor both into a shared `clampDepthWithDefault(n, def int) int` and have clampDepth/clampAffectedDepth call it. Write the clamp unit test first (RED).
  </action>
  <verify>
    <automated>go test ./internal/query/... -run 'ClampAffectedDepth|ClampDepth' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/query/validate.go` declares `defaultAffectedDepth = 5`
    - clampAffectedDepth(0)==5 and clampDepth(0)==2 both hold (distinct defaults, shared ceiling)
    - `go test ./internal/query/...` exits 0
  </acceptance_criteria>
  <done>affected has its own default-5 clamp reusing MaxDepth; impact's default-2 is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Affected → depth-bounded BFS with test-leaf pruning (SURF-04)</name>
  <files>internal/query/traverse.go, internal/query/traverse_test.go, internal/cli/affected.go</files>
  <read_first>
    - internal/query/traverse.go (`Impact` BFS frontier loop lines 424-456 — the shape to crib; current single-hop `Affected` lines 485-538; `isTestSymbol` lines 471-476; `AffectedResult` lines 258-261)
    - internal/cli/affected.go (the single `eng.Affected(args)` call site, line 38)
    - 08-RESEARCH.md §"Common Pitfalls Pitfall 2" (TS BFS pseudocode: test files are leaves, non-test dependents keep expanding)
    - 08-PATTERNS.md §"internal/query/traverse.go (SURF-04 Affected BFS extension)"
  </read_first>
  <behavior>
    - depth=1: only direct dependents of the changed files' symbols are considered (one hop)
    - depth>=2: non-test dependents are expanded to the next level; a test dependent is recorded and NOT expanded (leaf)
    - a changed file with a test 2 hops away via a non-test intermediary IS found at depth>=2, but the same test is NOT found at depth=1
    - depth=0 → uses defaultAffectedDepth=5
    - dangling reverse edge mid-BFS → skipped, traversal continues
    - empty files input → empty AffectedTests, no error
  </behavior>
  <action>
    Rewrite `Affected` to take `(files []string, depth int)`: validateDepth(depth) then `depth = clampAffectedDepth(depth)`. Seed the frontier from every symbol defined in the changed files (the existing IterateNodes seed scan). Port Impact's frontier/next-frontier loop bounded by depth over the reverse-adjacency map, but with the leaf rule: for each newly-visited dependent, if `isTestSymbol(dep)` add it to the affected-tests result and do NOT append it to `next`; otherwise append it to `next` for further expansion (and do not record it as a test). Reuse Impact's `errors.Is(err, graphstore.ErrNotFound) { continue }` dangling-edge skip verbatim. Keep `isTestSymbol` unchanged. Update the existing `TestAffected` to the new signature and add depth-1-vs-deep and leaf-pruning subtests (RED first). Update affected.go's call site to `eng.Affected(args, 0)` as a minimal compile-fix (full flag wiring is 08-05).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/query/... -run TestAffected -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `Engine.Affected` signature is `Affected(files []string, depth int) (AffectedResult, error)`
    - traverse.go contains a bounded `for d := 0; d < depth && len(frontier) > 0; d++` loop for Affected (grep: an Affected-scoped frontier loop exists)
    - a subtest proves a test symbol reachable only at depth 2 is absent at depth=1 and present at depth>=2
    - a subtest proves a test dependent is not expanded past (its own non-test dependents are not pulled in through it)
    - `go build ./...` exits 0 (affected.go call site updated)
    - `go test ./internal/query/... -run TestAffected` exits 0
  </acceptance_criteria>
  <done>`Affected` is a depth-bounded BFS with test-leaf pruning and affected-specific default; the package compiles; engine tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Affected(files, depth) | untrusted depth bounds BFS work; untrusted file list seeds the traversal over the frozen index |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Denial of Service | Affected BFS depth | medium | mitigate | validateDepth rejects negatives; clampAffectedDepth caps at MaxDepth=50 — the same V5 ceiling as impact, no second inconsistent bound introduced |
| T-08-04-02 | Denial of Service | reverse-adjacency expansion | low | mitigate | test-leaf pruning strictly reduces frontier growth; visited-set prevents revisiting; dangling edges skipped not aborted |
</threat_model>

<verification>
- `go build ./... && go test ./internal/query/... -run TestAffected -count=1` green
- `go test ./internal/query/... -count=1` green (whole package, no regressions)
- backstop truth (Affected ordering determinism) surfaced for human confirmation in validate-phase — not asserted as covered
</verification>

<success_criteria>
`Affected` is a depth-bounded BFS with TS test-leaf semantics, affected-specific default 5, shared MaxDepth ceiling, and the package still builds for downstream 08-05 flag wiring.
</success_criteria>

<output>
Create `.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-04-SUMMARY.md` when done.
</output>
