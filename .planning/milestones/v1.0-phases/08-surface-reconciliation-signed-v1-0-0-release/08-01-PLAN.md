---
phase: 08-surface-reconciliation-signed-v1-0-0-release
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/query/validate.go
  - internal/query/engine_test.go
  - internal/cli/impact.go
autonomous: true
requirements: [SURF-01, SURF-03]
must_haves:
  truths:
    - "impact with no --depth reports depth=2 (engine default), on both CLI and the codegraph_impact MCP tool (shared clampDepth)"
    - "impact --depth 0 clamps to the new default 2; --depth negative is rejected by validateDepth (error, not silent default); --depth 51+ clamps to MaxDepth=50 (unchanged)"
    - "the SURF-01 change is a single-constant edit: defaultDepth 5→2 in internal/query/validate.go, sole non-test consumer is clampDepth→Impact (re-verify grep before editing)"
    - "depth is an integer clamp (min/max comparison) — no rounding, truncation, or precision loss occurs on any boundary value"
    - "impact.go registers -d (depth) and -j (json) short flags with no cobra shorthand collision in the command"
    - "the golden impact.json fixtures already encode depth:2; after the change go test ./testdata/golden/... stays green (Go no-flag impact now matches the TS oracle default)"
  artifacts:
    - internal/query/validate.go
    - internal/cli/impact.go
  key_links:
    - "internal/query/validate.go defaultDepth constant → clampDepth → internal/query/traverse.go Impact() → both internal/cli/impact.go and internal/mcp codegraph_impact inherit the default"
  prohibitions:
    - "MUST NOT change MaxDepth=50 or the clamp ceiling — SURF-01/D-02 changes only the zero-value default"
    - "MUST NOT introduce a per-surface (CLI-only or MCP-only) depth default — the default lives once in the shared engine"
    - "MUST NOT remove or weaken validateDepth's negative-depth rejection"
---

<objective>
Change `impact`'s default BFS depth from 5 to 2 in the shared query engine (SURF-01, CONTEXT D-02) so both the CLI `impact` command and the `codegraph_impact` MCP tool inherit depth-2 in one commit, and add the `-d`/`-j` short-flag aliases to `impact.go` (SURF-03) matching TS 1.3.1.

Purpose: Match TS CodeGraph 1.3.1's `impact` default (`Math.min(Math.max(parseInt(options.depth||'2'),1),10)`), the first step of the D-01-locked surface reconciliation that must be green before the release.
Output: One-constant engine change + two short-flag additions, verified against the already-correct golden corpus.

## Artifacts this phase produces (this plan)
- Changed engine default: `internal/query/validate.go` `defaultDepth` 5 → 2 (behavioral default for CLI + MCP `impact`).
- New CLI short flags: `impact -d` (alias of `--depth`), `impact -j` (alias of `--json`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-RESEARCH.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-PATTERNS.md
@internal/query/validate.go
@internal/cli/impact.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN — engine defaultDepth 5→2 (SURF-01, D-02)</name>
  <files>internal/query/validate.go, internal/query/engine_test.go</files>
  <read_first>
    - internal/query/validate.go (the `defaultDepth = 5` constant, lines 31-34, and `clampDepth`, lines 44-55)
    - internal/query/engine_test.go (existing `clampDepth` table test around line 170)
    - 08-RESEARCH.md §"Pattern 1: Shared-engine default, single consumer" and §"Common Pitfalls Pitfall 1"
    - 08-PATTERNS.md §"internal/query/validate.go (SURF-01 depth default)"
  </read_first>
  <behavior>
    - clampDepth(0) == 2 (was 5) — the "caller omitted --depth" path
    - clampDepth(-1): validateDepth(-1) returns an error (negative rejected upstream); clampDepth itself, if reached with a non-positive, still yields the new default 2
    - clampDepth(1) == 1 (explicit small depth preserved)
    - clampDepth(50) == 50; clampDepth(51) == 50 (MaxDepth ceiling unchanged)
  </behavior>
  <action>
    First re-run `rg -n "clampDepth\(" internal/query/*.go` and confirm the only non-test consumer is `traverse.go` inside `Impact()` (Pitfall 1 drift guard). Update the `clampDepth` table test in engine_test.go so the zero-input case expects 2 (RED — fails against the current `defaultDepth = 5`). Then change `defaultDepth = 5` to `defaultDepth = 2` in validate.go, updating its doc comment to reference SURF-01/D-02. Leave `MaxDepth = 50`, `validateDepth`, and every other clamp untouched.
  </action>
  <verify>
    <automated>go test ./internal/query/... -run 'TestClampDepth|TestImpact' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/query/validate.go` contains `defaultDepth = 2` and no longer contains `defaultDepth = 5`
    - `internal/query/validate.go` still contains `MaxDepth = 50`
    - `go test ./internal/query/...` exits 0 with the updated zero-input expectation of 2
    - `rg -n "clampDepth\(" internal/query/*.go` shows the only non-test call site is inside `Impact` (traverse.go)
  </acceptance_criteria>
  <done>The shared engine defaults omitted `impact` depth to 2; the max-50 clamp and negative-rejection are unchanged; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: impact.go -d/-j short flags + stale help text (SURF-03)</name>
  <files>internal/cli/impact.go</files>
  <read_first>
    - internal/cli/impact.go (current `Flags()` block, lines 64-66 — note the "default 5" help string on `--depth`)
    - 08-PATTERNS.md §"internal/cli/impact.go (SURF-01/03: short flags, no behavior change)"
    - 08-RESEARCH.md §"SURF-03 full per-command flag map" (impact row: `-d`,`-j` both free)
  </read_first>
  <action>
    Change the `--depth` registration from `IntVar` to `IntVarP(&depth, "depth", "d", 0, ...)` and update its help text from "default 5" to "default 2, max 50" (per D-02). Change the `--json` registration from `BoolVar` to `BoolVarP(&jsonOut, "json", "j", false, ...)`. Make no change to `RunE` or the `-p`/`--path` flag. Both `-d` and `-j` are free in this command per RESEARCH's letter map.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/cli/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/impact.go` registers `IntVarP(..., "depth", "d", ...)` and `BoolVarP(..., "json", "j", ...)`
    - the `--depth` help string no longer says "default 5" (says default 2)
    - `go build ./...` exits 0 (cobra panics at build/first-run on any duplicate shorthand — none here)
    - `codegraph impact -h` shows `-d, --depth` and `-j, --json`
  </acceptance_criteria>
  <done>`impact` accepts `-d`/`-j` short aliases and its help text reflects the depth-2 default; build and CLI tests green.</done>
</task>

<task type="auto">
  <name>Task 3: golden-corpus parity guard (impact default now 2)</name>
  <files>testdata/golden/corpus/weft-go/impact.json, testdata/golden/corpus/colbymchenry-codegraph/impact.json</files>
  <read_first>
    - testdata/golden/corpus/weft-go/impact.json (line 3: `"depth": 2`)
    - testdata/golden/README.md (regen mechanics; `go test ./...` skips testdata — run `go test ./testdata/golden/...` explicitly)
    - 08-CONTEXT.md D-02 ("Verify the golden/MCP corpus reflects the new default; regen if impact appears")
  </read_first>
  <action>
    Run the golden parity suite explicitly. The committed `impact.json` fixtures already encode `"depth": 2` (the TS oracle default), so the SURF-01 change makes Go's no-flag `impact` output match rather than diverge — no regen is expected. If the suite reports an impact mismatch, regenerate ONLY the impact fixtures per README.md's capture procedure (do not touch explore/node/status fixtures) and record the regen in the SUMMARY. Do NOT modify the golden harness itself (REL-04 consumes it).
  </action>
  <verify>
    <automated>go test ./testdata/golden/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./testdata/golden/...` exits 0
    - both `impact.json` fixtures still show `"depth": 2`
    - no file under `testdata/golden/` other than (at most) the two `impact.json` fixtures is modified
  </acceptance_criteria>
  <done>The golden corpus confirms the depth-2 default matches the TS oracle; parity suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → codegraph_impact → shared engine | An untrusted/compromised MCP client passes `--depth`; the clamp bounds traversal work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Denial of Service | `clampDepth` / `Impact` BFS | medium | mitigate | Keep MaxDepth=50 ceiling and validateDepth negative-rejection intact; only the default moves 5→2 (strictly reduces default work) |
| T-08-01-02 | Tampering | golden fixtures | low | mitigate | Only impact.json may change (and only if a mismatch is proven); harness code untouched; parity suite gates the change |
</threat_model>

<verification>
- `go test ./internal/query/... ./internal/cli/... -count=1` green
- `go test ./testdata/golden/... -count=1` green
- `go build ./...` green (no cobra shorthand collision)
</verification>

<success_criteria>
`impact` (CLI and MCP) defaults omitted depth to 2, rejects negative depth, clamps at 50; `-d`/`-j` shorts work; golden corpus green.
</success_criteria>

<output>
Create `.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-01-SUMMARY.md` when done.
</output>
