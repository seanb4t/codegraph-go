---
phase: 08-surface-reconciliation-signed-v1-0-0-release
plan: 06
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05]
files_modified:
  - docs/FLAG-PARITY.md
  - internal/cli/flag_parity_test.go
autonomous: true
requirements: [SURF-05]
must_haves:
  truths:
    - "docs/FLAG-PARITY.md is a per-command matrix: TS 1.3.1 flag+default → Go flag+default → status (present / divergence(reason) / Go-only), covering every command in newRootCmd()"
    - "the drift test walks newRootCmd().Commands() and each command's Flags().VisitAll, asserting every registered long-flag name appears verbatim in docs/FLAG-PARITY.md — so the doc cannot silently drift from the real cobra surface"
    - "a command with zero flags (unlock, telemetry) still appears as a row/section in the matrix; the drift test does not fail on flag-less commands"
    - "flag-name presence is an exact ASCII substring match — no normalization, so a typo'd flag name in the doc fails the test"
    - "the matrix is grouped per command in a stable order; the drift test is order-independent (presence, not sequence)"
    - "every documented divergence is recorded: dual --filter(language)/--dir(directory), the short-flag divergences and preserved Go bindings, install --auto-allow default-off behavioral divergence, files --format flat-vs-tree default, files --depth vs TS --max-depth naming, node missing file-mode, upgrade --force addition, init/index --force semantic divergence, and search/migrate/githooks/serve --watch/version --json as Go-only extensions"
  artifacts:
    - docs/FLAG-PARITY.md
    - internal/cli/flag_parity_test.go
  key_links:
    - "internal/cli/flag_parity_test.go (walks newRootCmd() Flags) → docs/FLAG-PARITY.md (the parity oracle REL-04's drop-in gate reads)"
  prohibitions:
    - "MUST NOT change any flag, default, or command behavior in this plan — SURF-05 is an audit + doc + drift-guard; behavioral divergences are RECORDED, not resolved (install --auto-allow, files --format default, node file-mode stay as-is)"
    - "MUST NOT weaken the drift test to a warning — a registered flag absent from the doc is a hard test failure"
---

<objective>
Produce `docs/FLAG-PARITY.md` (SURF-05, CONTEXT D-06) — the systematic per-command TS↔Go flag/default/status matrix that is the single artifact REL-04's drop-in gate reads to declare surface parity — and back it with a lightweight cobra-tree-walk drift test so the doc can never silently diverge from the registered flag surface.

Purpose: Prove every TS 1.3.1 flag name + default is present or a documented divergence, and make that claim self-verifying.
Output: the parity matrix doc + `internal/cli/flag_parity_test.go` asserting doc↔tree completeness.

## Artifacts this phase produces (this plan)
- New doc: `docs/FLAG-PARITY.md`.
- New test: `internal/cli/flag_parity_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-RESEARCH.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-PATTERNS.md
@internal/cli/root.go
@docs/RELEASE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/FLAG-PARITY.md per-command matrix (SURF-05, D-06)</name>
  <files>docs/FLAG-PARITY.md</files>
  <read_first>
    - 08-RESEARCH.md §"SURF-03 full per-command flag map" (the complete verified TS↔Go per-command table — the source of truth for this doc) and §"Common Pitfalls" 4/5 (auto-allow, files --format, files --depth naming, node file-mode divergences) and §"Assumptions Log" A1-A4
    - internal/cli/root.go (the command list registered on newRootCmd — the exact set the matrix must cover)
    - docs/RELEASE.md (prose/structure style: numbered `##` sections, status-note blockquote, path-referenced claims) — 08-PATTERNS.md §"docs/FLAG-PARITY.md"
  </read_first>
  <action>
    Author docs/FLAG-PARITY.md as a per-command matrix mirroring RESEARCH's verified table, one row per (command, flag) with columns: TS flag+short+default | Go flag+short+default | status (present / divergence(reason) / Go-only). Reflect the flags AS FINALIZED by plans 08-01..08-05 (impact -d/-j + depth 2; files --dir + -j; status/query/callers/callees/install/uninstall shorts; upgrade --force; affected --stdin/--depth/--filter/--quiet/-j). Explicitly record every divergence named in RESEARCH: the dual --filter(language)/--dir(directory) pair; each preserved Go short binding vs the TS letter it declines; install --auto-allow default-off (security-conservative, deliberate); files --format flat-vs-tree default; files --depth vs TS --max-depth naming; TS --no-metadata not ported; node missing TS file-mode (--offset/--limit/--symbols-only); upgrade --force added; init/index --force home-root-guard vs skip-confirm semantic divergence; and search/migrate/githooks + serve --watch + version --json + daemon subcommands as Go-only extensions. Include a short "how this doc is enforced" note pointing at the drift test.
  </action>
  <verify>
    <automated>test -f docs/FLAG-PARITY.md && grep -q -- "--dir" docs/FLAG-PARITY.md && grep -q -- "--stdin" docs/FLAG-PARITY.md && grep -q "auto-allow" docs/FLAG-PARITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/FLAG-PARITY.md` exists and has a section/rows for every command registered in newRootCmd()
    - it records the dual --filter/--dir divergence, install --auto-allow divergence, files --format default divergence, node missing file-mode, upgrade --force addition, and search/migrate as Go-only/accepted
    - every long flag added by 08-01..08-05 (--dir, affected --stdin/--depth/--filter/--quiet, upgrade --force) appears in the doc
  </acceptance_criteria>
  <done>The parity matrix documents every TS flag+default as present or a reasoned divergence, in the finalized post-SURF flag surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: flag-parity drift test (tree-walk over newRootCmd)</name>
  <files>internal/cli/flag_parity_test.go</files>
  <read_first>
    - internal/cli/root.go (newRootCmd + AddCommand list)
    - internal/cli/present/archtest/import_graph_test.go (the "walk + assert, fail-closed" precedent — 08-PATTERNS.md §"Optional SURF-05 drift test")
    - the relative path from internal/cli to the doc: `../../docs/FLAG-PARITY.md`
  </read_first>
  <behavior>
    - the test reads docs/FLAG-PARITY.md once, walks newRootCmd() commands + subcommands, and for each command's Flags().VisitAll asserts the long flag name (e.g. "depth", "dir", "stdin", "force") appears as a substring in the doc bytes
    - a flag-less command (unlock/telemetry) contributes no assertion and does not fail
    - deliberately omitting a known flag from a scratch copy of the doc would make the test fail (self-defeat guard: the assertion is real)
  </behavior>
  <action>
    Add flag_parity_test.go in package cli: read the doc via os.ReadFile("../../docs/FLAG-PARITY.md") (fail the test loudly if the file is missing — fail-closed), build newRootCmd(), recursively visit every command and subcommand, and for each `pflag.Flag` name assert `strings.Contains(docText, flag.Name)`. Skip the auto-generated `help` flag. Report every missing flag name in one failure message (not the first only) so a drift shows the full gap.
  </action>
  <verify>
    <automated>go test ./internal/cli/... -run FlagParity -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/flag_parity_test.go` exists, reads `../../docs/FLAG-PARITY.md`, and walks newRootCmd() recursively
    - the test fails loudly if the doc file is absent (fail-closed)
    - `go test ./internal/cli/... -run FlagParity -count=1` exits 0 against the finalized doc
    - temporarily removing a flag name from the doc makes the test fail (verified once during execution, then reverted)
  </acceptance_criteria>
  <done>The parity doc is self-verifying: any registered flag missing from docs/FLAG-PARITY.md fails the build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag surface ↔ parity doc | drift between the registered cobra flags and the documented parity claim REL-04 gates on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Repudiation / Integrity | FLAG-PARITY.md accuracy | low | mitigate | The drift test walks the real command tree and hard-fails on any undocumented registered flag, so the parity claim REL-04 reads cannot silently rot |
| T-08-06-02 | Tampering | install --auto-allow default | low | accept | Documented as a deliberate security-conservative divergence; NOT flipped in a mechanical reconciliation phase (RESEARCH Pitfall 4 / Open Question 1) |
</threat_model>

<verification>
- `go test ./internal/cli/... -run FlagParity -count=1` green
- `docs/FLAG-PARITY.md` covers every newRootCmd command and records every RESEARCH-enumerated divergence
- self-defeat guard exercised: a removed flag name fails the test
</verification>

<success_criteria>
docs/FLAG-PARITY.md is a complete, self-verifying per-command TS↔Go parity matrix with every divergence documented; the drift test keeps it honest.
</success_criteria>

<output>
Create `.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-06-SUMMARY.md` when done.
</output>
