---
phase: 08-surface-reconciliation-signed-v1-0-0-release
plan: 09
type: execute
wave: 6
depends_on: [08-06, 08-07, 08-08]
files_modified:
  - docs/RELEASE-PROCEDURES.md
  - .planning/PROJECT.md
autonomous: false
requirements: [REL-04, REL-02]
must_haves:
  truths:
    - "the drop-in gate is BOTH: the Phase-1 TEST-01 behavioral harness green against real TS 1.3.1 (go test ./testdata/golden/...) AND docs/FLAG-PARITY.md green (drift test passes, zero undocumented divergences) — only then is the caveat retired"
    - "PROJECT.md's 'not yet drop-in' caveat is retired at EVERY occurrence: rg 'not yet drop-in|drop-in parity' .planning/PROJECT.md returns zero remaining 'not yet' hits after the edit (all located sites reworded to 'parity closed / v1.0.0 shipped')"
    - "caveat retirement is site-independent — all 4 confirmed sites (milestone goal line, repo-state paragraph, Key Decisions row, the 'In progress' decision-log row) are swept, and if any remained the gate is not met"
    - "docs/RELEASE-PROCEDURES.md is the maintainer runbook covering the pre-tag 6-target `go list -mod=readonly ./...` sweep (D-09), tag conventions (rc v0.0.0-rc.N=prerelease / vX.Y.Z=full / milestone-v*=never fires), what the tag push triggers, the verify.go LOCKED contract, post-release cosign/slsa verification, rc-tag rollback, and the -c commit.gpgsign=false pipeline-only caveat"
    - "internal/upgrade/verify.go's releaseWorkflowRefPattern/releaseOIDCIssuer/releaseRepoSlug are byte-unchanged by Phase 8 — the runbook cites them verbatim as the LOCKED contract but does not edit them, and release.yml is neither renamed nor re-triggered"
    - "the signed v1.0.0 tag is the LAST action of the phase (and of v1.0): a maintainer-manual `git tag v1.0.0 && git push` on main after the pre-tag 6-target gate passes, triggering release.yml's signed build"
  artifacts:
    - docs/RELEASE-PROCEDURES.md
    - .planning/PROJECT.md
  key_links:
    - "docs/FLAG-PARITY.md (08-06) + testdata/golden harness (Phase 1) → REL-04 drop-in gate → PROJECT.md caveat retirement → docs/RELEASE-PROCEDURES.md runbook → maintainer-manual v1.0.0 tag → release.yml signed build"
  prohibitions:
    - "the release readiness work MUST NOT alter internal/upgrade/verify.go's LOCKED cosign identity constants (releaseWorkflowRefPattern/releaseOIDCIssuer/releaseRepoSlug) — a mis-edit silently breaks `codegraph upgrade` verification for every user"
    - "MUST NOT rename .github/workflows/release.yml or change its v[0-9]* trigger without editing verify.go's three constants in the same commit (D-08 LOCKED contract)"
    - "MUST NOT retire the PROJECT.md caveat until BOTH gate halves are green (TEST-01 harness + FLAG-PARITY)"
    - "MUST NOT weaken the runbook's post-release cosign identity regex to a prefix match — it stays a full anchored ^...$ match"
    - "the executor MUST NOT push the v1.0.0 tag — the tag push is the maintainer's manual runbook action (human checkpoint)"
---

<objective>
Finalize release readiness and cut the first signed `v1.0.0` (REL-02 + REL-04, CONTEXT D-08/D-09/D-11 + folded runbook todo): write the maintainer runbook, run the drop-in gate (TEST-01 behavioral harness + FLAG-PARITY both green), retire PROJECT.md's "not yet drop-in" caveat at every site, then hand off the maintainer-manual pre-tag gate + `v1.0.0` tag push — the very last action of the phase and of the v1.0 milestone.

Purpose: Close v0.1's pending DIST-02 (real signed tag) and validate/retire the drop-in caveat, without ever touching the LOCKED release-verification contract.
Output: docs/RELEASE-PROCEDURES.md, a retired caveat in PROJECT.md, and a green, human-gated tag handoff.

## Artifacts this phase produces (this plan)
- New doc: `docs/RELEASE-PROCEDURES.md` (maintainer release runbook).
- Edited: `.planning/PROJECT.md` (caveat retired at all sites).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-RESEARCH.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-PATTERNS.md
@docs/RELEASE.md
@internal/upgrade/verify.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/RELEASE-PROCEDURES.md maintainer runbook (REL-02 folded todo)</name>
  <files>docs/RELEASE-PROCEDURES.md</files>
  <read_first>
    - docs/RELEASE.md (the user-facing verification sibling — reuse its numbered-section + fenced-command + status-note style)
    - internal/upgrade/verify.go (the LOCKED constants: releaseOIDCIssuer / releaseRepoSlug / releaseWorkflowRefPattern — cite verbatim)
    - 08-RESEARCH.md §"Pattern 3: Release cut sequence" (the exact D-08/D-09 sequence + post-release cosign/slsa commands) and §"Folded todo" contents
    - 08-CONTEXT.md D-08/D-09 + Folded Todos
  </read_first>
  <action>
    Author docs/RELEASE-PROCEDURES.md as the maintainer runbook, in docs/RELEASE.md's prose/command style, covering: (1) the mandatory pre-tag gate — `GOOS=<os> GOARCH=<arch> go list -mod=readonly ./...` for all 6 targets (linux/windows/darwin × amd64/arm64), the check that would have caught the v0.1 rc.1 linux-only go.sum failure; (2) tag conventions — `v0.0.0-rc.N` prerelease, `vX.Y.Z` full release + `codegraph upgrade` latest, `milestone-v*` deliberately non-matching (never fires release.yml); (3) the branch/tag model (integration branch → squash-merge main → tag on main); (4) what the tag push triggers (release.yml build/assemble/provenance jobs); (5) the verify.go LOCKED contract — cite the three constants verbatim and state that any release.yml rename/trigger/identity change requires editing them in lockstep or `codegraph upgrade` breaks for every user; (6) post-release verification — the `cosign verify-blob` (full anchored identity regexp) and `slsa-verifier verify-artifact` commands from RESEARCH Pattern 3; (7) rollback/cleanup of a failed rc tag; (8) the `-c commit.gpgsign=false` fallback allowed for automated pipeline commits ONLY (repo rule xmz3xknbj0 — never bypass signing when a human asks for a commit).
  </action>
  <verify>
    <automated>test -f docs/RELEASE-PROCEDURES.md && grep -q "go list -mod=readonly" docs/RELEASE-PROCEDURES.md && grep -q "releaseWorkflowRefPattern" docs/RELEASE-PROCEDURES.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/RELEASE-PROCEDURES.md exists and documents the 6-target pre-tag `go list -mod=readonly` sweep, tag conventions, the verify.go LOCKED contract (3 constants verbatim), post-release cosign/slsa verification, rc-tag rollback, and the gpgsign pipeline-only caveat
    - the cited cosign identity is a full anchored `^...$` regexp, not a prefix
    - `internal/upgrade/verify.go` is NOT modified by this task (grep the git diff — verify.go untouched)
  </acceptance_criteria>
  <done>The maintainer runbook exists and cites the LOCKED contract verbatim without editing it.</done>
</task>

<task type="auto">
  <name>Task 2: REL-04 drop-in gate + PROJECT.md caveat retirement</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (the 4 caveat sites — 08-PATTERNS.md §".planning/PROJECT.md (REL-04 caveat retirement)" lists them; re-grep to confirm current line numbers)
    - 08-RESEARCH.md §"Common Pitfalls Pitfall 6" (all caveat sites + the warning-sign zero-hit check)
    - testdata/golden/README.md (the harness is run, not modified; `go test ./...` skips testdata — invoke `go test ./testdata/golden/...` explicitly)
  </read_first>
  <action>
    First run BOTH gate halves and confirm green: `go test ./testdata/golden/...` (Phase-1 TEST-01 behavioral harness vs TS 1.3.1) AND `go test ./internal/cli/... -run FlagParity` (docs/FLAG-PARITY.md drift, 08-06) with zero undocumented divergences. Only if both pass, edit .planning/PROJECT.md to retire the "not yet drop-in" caveat at every site with context-appropriate rewording (milestone goal line, repo-state paragraph, Key Decisions row) and flip the related "In progress" decision-log row to Complete/✓ — reflecting "parity closed, v1.0.0 shipped". Do NOT modify the harness or verify.go. Re-run `rg -n "not yet drop-in" .planning/PROJECT.md` and confirm zero hits.
  </action>
  <verify>
    <automated>go test ./testdata/golden/... -count=1 && bash -c '! rg -q "not yet drop-in" .planning/PROJECT.md'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./testdata/golden/...` exits 0 (behavioral harness green)
    - `go test ./internal/cli/... -run FlagParity` exits 0 (FLAG-PARITY green)
    - `rg "not yet drop-in" .planning/PROJECT.md` returns zero hits
    - the milestone/decision rows reflect parity-closed / v1.0.0; no harness file and no verify.go constant changed
  </acceptance_criteria>
  <done>Both drop-in gate halves are green and the caveat is retired at every PROJECT.md site.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Maintainer-manual pre-tag gate + signed v1.0.0 tag (LAST action)</name>
  <files>(none — maintainer git action; triggers release.yml)</files>
  <what-built>All SURF + REL readiness is green: flags reconciled (08-01..06), Charm closure audited CGo-free + govulncheck/SBOM/double-build clean (08-07), benchmarks published (08-08), runbook written (Task 1), drop-in gate green + caveat retired (Task 2). The signed release pipeline (release.yml) is unchanged and its LOCKED verify.go contract is intact.</what-built>
  <action>
    Maintainer performs the release cut manually per docs/RELEASE-PROCEDURES.md — the executor MUST NOT push the tag. Steps in how-to-verify; the tag push triggers release.yml's signed build. This is the final action of the phase and of v1.0.
  </action>
  <how-to-verify>
    Perform the maintainer-manual release cut — the LAST action of the phase and of v1.0 (per docs/RELEASE-PROCEDURES.md):
    1. Pre-tag gate (D-09): for all 6 targets run `GOOS=<os> GOARCH=<arch> go list -mod=readonly ./...` (linux/windows/darwin × amd64/arm64) — all must succeed.
    2. Squash-merge the integration branch `gsd/v1.0-drop-in-parity-human-ux` to `main`.
    3. `git tag v1.0.0 && git push origin v1.0.0` (stable tag, no `-` suffix). This triggers release.yml's `v[0-9]*` build/assemble(cosign)/provenance(SLSA)/SBOM jobs. Never use a `milestone-v*` name.
    4. After the release publishes, verify per RESEARCH Pattern 3 / docs/RELEASE.md: `cosign verify-blob --bundle ... --certificate-identity-regexp '^https://github\.com/seanb4t/codegraph-go/\.github/workflows/release\.ya?ml@refs/tags/v[0-9][^[:space:]]*$' ...` and `slsa-verifier verify-artifact ...`.
  </how-to-verify>
  <verify>
    <human-check>The 6-target pre-tag `go list -mod=readonly` gate passed, v1.0.0 is pushed on main, release.yml published the signed artifacts, and cosign/slsa verification succeeds.</human-check>
  </verify>
  <done>The signed v1.0.0 release is cut and independently verified — the final action of the phase and of v1.0; DIST-02 closed.</done>
  <resume-signal>Type "tagged" once v1.0.0 is pushed and the signed release verifies, or describe any pre-tag gate failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release edits → internal/upgrade/verify.go LOCKED contract | any change to release.yml identity/trigger must move verify.go's 3 constants in lockstep or `codegraph upgrade` breaks for every user |
| maintainer tag push → release.yml signed build | the tag ref binds the cosign keyless identity (per-binary signing + SLSA provenance) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09-01 | Tampering | internal/upgrade/verify.go LOCKED constants | high | mitigate | This plan never edits verify.go or release.yml; the runbook documents the lockstep contract and cites the 3 constants verbatim; git-diff acceptance criterion confirms verify.go untouched |
| T-08-09-02 | Spoofing | post-release cosign identity | high | mitigate | Runbook mandates the full anchored `^...$` workflow-ref regexp (never a prefix), matching verify.go; a differently-triggered same-repo workflow cannot satisfy the SAN |
| T-08-09-03 | Tampering (supply chain) | release artifacts | high | mitigate | The signed pipeline (per-binary cosign keyless + SLSA3 provenance + SBOM) is exercised for the first stable tag; post-release cosign/slsa verification is a runbook step |
| T-08-09-04 | Repudiation | drop-in caveat retirement | medium | mitigate | Caveat retired only after BOTH gate halves (harness + FLAG-PARITY) are green; zero-hit grep confirms full sweep |
</threat_model>

<verification>
- `go test ./testdata/golden/... -count=1` green (TEST-01 harness) AND `go test ./internal/cli/... -run FlagParity` green
- `rg "not yet drop-in" .planning/PROJECT.md` → zero hits
- git diff confirms `internal/upgrade/verify.go` and `.github/workflows/release.yml` are untouched
- docs/RELEASE-PROCEDURES.md complete; maintainer tag handoff green at the checkpoint
</verification>

<success_criteria>
Drop-in parity is validated (harness + flag audit green), the caveat is retired everywhere, the maintainer runbook is written, and the signed v1.0.0 tag is cut as the final action — with the LOCKED verify.go contract untouched.
</success_criteria>

<output>
Create `.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-09-SUMMARY.md` when done.
</output>
