---
phase: 08-surface-reconciliation-signed-v1-0-0-release
plan: 08
type: execute
wave: 5
depends_on: [08-06]
files_modified:
  - docs/BENCHMARKS.md
autonomous: false
requirements: [REL-03]
must_haves:
  truths:
    - "the head-to-head benchmarks vs pinned TS 1.3.1 are re-run via the existing bench.yml harness (tools/bench/runner -mode headtohead), NOT a new bench framework"
    - "REL-03 reuses v0.1's methodology (median-of-3, same real-repo corpora, same pinned TS 1.3.1) so the numbers are comparable across releases"
    - "docs/BENCHMARKS.md's provisional local-machine table is replaced with the CI-run (ubuntu-latest) numbers; ratios are expected to hold, absolutes will shift to standardized hardware"
    - "the refreshed numbers are surfaced in the v1.0.0 release notes (consumed by 08-09/REL-02)"
  artifacts:
    - docs/BENCHMARKS.md
  key_links:
    - ".github/workflows/bench.yml (tools/bench/runner -mode headtohead) → docs/BENCHMARKS.md refreshed table → v1.0.0 release notes"
  prohibitions:
    - "MUST NOT author a new benchmark framework or change the methodology — re-run the existing harness (D-10)"
    - "MUST NOT hand-fabricate benchmark numbers — the table is transcribed from a real bench.yml/harness run"
---

<objective>
Re-run the existing head-to-head benchmark harness against the pinned TS CodeGraph 1.3.1 and refresh `docs/BENCHMARKS.md` with the reproducible CI numbers (REL-03, CONTEXT D-10), closing v0.1's pending PERF-01. Reuse `bench.yml` / `tools/bench/runner` and v0.1's methodology exactly — this is a publish action, not new code.

Purpose: Replace the provisional local-machine (darwin/arm64) figures with standardized CI numbers for the v1.0.0 release notes.
Output: refreshed `docs/BENCHMARKS.md` table + numbers ready for the release notes.

## Artifacts this phase produces (this plan)
- Refreshed doc: `docs/BENCHMARKS.md` (CI head-to-head numbers).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md
@.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-RESEARCH.md
@docs/BENCHMARKS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Trigger the head-to-head benchmark run vs TS 1.3.1</name>
  <files>(none — produces benchmark numbers for Task 2 to transcribe)</files>
  <what-built>All SURF reconciliation is green (plans 08-01..08-06). The bench harness (bench.yml / tools/bench/runner -mode headtohead) exists and is unmodified.</what-built>
  <action>
    Human triggers the reproducible head-to-head run and provides the numbers back — the harness needs a live TS 1.3.1 install + real corpora on standardized hardware (the point of REL-03: replace provisional darwin-only figures with CI numbers). Steps in how-to-verify; nothing is automated further until the numbers are provided.
  </action>
  <how-to-verify>
    Trigger the reproducible head-to-head run on standardized CI hardware and provide the results back:
    1. Dispatch the harness: `gh workflow run bench.yml` (or wait for the weekly schedule), OR run locally per RESEARCH §"Re-running the head-to-head bench" (`npm install -g @colbymchenry/codegraph@1.3.1`; `go build -o codegraph-bench ./cmd/codegraph`; `go run ./tools/bench/runner -mode headtohead -go-binary ... -ts-binary $(command -v codegraph) | tee headtohead-results.json`).
    2. Confirm it completes against TS 1.3.1 (median-of-3, same corpora as v0.1).
    3. Paste/provide the resulting numbers (the CI job summary or headtohead-results.json) so Task 2 can transcribe them.
  </how-to-verify>
  <verify>
    <human-check>The bench harness completed against TS 1.3.1 and produced numbers (CI job summary or headtohead-results.json) ready to transcribe.</human-check>
  </verify>
  <done>Reproducible head-to-head numbers vs TS 1.3.1 are provided for transcription into docs/BENCHMARKS.md.</done>
  <resume-signal>Provide the CI/harness benchmark numbers (or the results JSON), then type "numbers ready".</resume-signal>
</task>

<task type="auto">
  <name>Task: transcribe CI numbers into docs/BENCHMARKS.md</name>
  <files>docs/BENCHMARKS.md</files>
  <read_first>
    - docs/BENCHMARKS.md (current provisional table + its §"Regenerating the numbers" note flagging the local figures as provisional)
    - 08-RESEARCH.md §"State of the Art" (local→CI numbers shift; ratios hold, absolutes move) and §"Re-running the head-to-head bench"
  </read_first>
  <action>
    Replace the provisional local-machine table in docs/BENCHMARKS.md with the CI (or reproducible harness) numbers provided at the checkpoint, keeping the same table structure, corpora, and median-of-3 methodology description. Update the provenance note to state the numbers are from a standardized CI run at the v1.0.0 cut, and reference them for the release notes. Do not change the methodology prose.
  </action>
  <verify>
    <automated>test -f docs/BENCHMARKS.md && grep -qi "1.3.1" docs/BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/BENCHMARKS.md's table reflects the provided CI/harness numbers (not the old provisional local figures)
    - the provenance note states the source is a standardized/CI run for v1.0.0
    - methodology (median-of-3, same corpora, pinned TS 1.3.1) is unchanged
  </acceptance_criteria>
  <done>docs/BENCHMARKS.md publishes reproducible head-to-head numbers vs TS 1.3.1, ready for the v1.0.0 release notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published benchmark numbers → users/release notes | integrity of the performance claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08-01 | Repudiation / Integrity | BENCHMARKS.md numbers | low | mitigate | Numbers transcribed from a real reproducible harness run (median-of-3, pinned TS 1.3.1), not hand-authored; methodology documented for independent re-run |
</threat_model>

<verification>
- docs/BENCHMARKS.md updated with reproducible numbers vs TS 1.3.1
- methodology unchanged; provenance note updated
</verification>

<success_criteria>
Head-to-head benchmarks are re-run on standardized hardware and published in docs/BENCHMARKS.md, ready for the v1.0.0 release notes, closing PERF-01.
</success_criteria>

<output>
Create `.planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-08-SUMMARY.md` when done.
</output>
