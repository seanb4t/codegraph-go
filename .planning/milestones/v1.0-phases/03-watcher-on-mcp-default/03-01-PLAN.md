---
phase: 03-watcher-on-mcp-default
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/watch/policy.go
  - internal/watch/policy_test.go
autonomous: true
requirements: [WATCH-03]
must_haves:
  truths:
    - "WatchDisabledReason returns \"\" by default (watch runs) and the verbatim WSL/NO_WATCH reason strings when disabled (D-12/D-13)"
    - "Only the exact env string \"1\" triggers CODEGRAPH_NO_WATCH / CODEGRAPH_FORCE_WATCH — strict equality, never truthiness (D-10)"
    - "Precedence, first match wins: NoWatch(flag|env) → off; ForceWatch(flag|env) → on; WSL2 + /mnt/[a-z] → off; default → on (D-04)"
    - "DetectWSL is cached and returns false off-linux and on any /proc/version read failure (D-10)"
  artifacts:
    - internal/watch/policy.go
    - internal/watch/policy_test.go
  key_links:
    - "watch.WatchDisabledReason is consumed by internal/daemon.Run (03-02, enforcement) and internal/cli/serve.go (03-03, human message)"
    - "watch.ErrWatchDisabled is the sentinel internal/cli/serve.go errors.Is against (03-03)"
---

<objective>
Port TS 1.3.1's `sync/watch-policy.js` verbatim into a new pure decision function `internal/watch/policy.go`, implementing WATCH-03's WSL2/slow-filesystem auto-off with exact env precedence. This is the load-bearing decision the whole phase gates on: the default flip (03-03) and daemon enforcement (03-02) both consume this one function.

Implements D-09 (policy lives with the watcher, one exported function), D-10 (detection semantics ported exactly: WSL via env→/proc/version cached, `/mnt/[a-z]` single-letter regex, strict `== "1"`), D-12/D-13 (verbatim reason strings, with the one documented `fs.watch`→`file watching` divergence), and the D-05 rule that the function takes explicit inputs and never mutates process env.

Purpose: a single source of truth for "should the watcher run here", table-tested deterministically without needing a real WSL2 host (injectable probes).
Output: `internal/watch/policy.go` (Probe, WatchDisabledReason, DetectWSL, ErrWatchDisabled, test-reset hook) + `internal/watch/policy_test.go`.

TDD: this is a pure decision function with a locked precedence table — the textbook RED→GREEN candidate. Write the failing table first, then the implementation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-watcher-on-mcp-default/03-CONTEXT.md
@.planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md
@.planning/phases/03-watcher-on-mcp-default/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — table-driven policy_test.go asserting the full precedence + WSL detection + strict-env contract</name>
  <files>internal/watch/policy_test.go</files>
  <read_first>
    - internal/watch/debounce.go (env-parse-with-fallback style + package doc/D-number comment convention to mirror; note it uses Atoi/truthy which policy MUST NOT copy)
    - internal/watch/debounce_test.go (env-injection test idiom in this package)
    - internal/daemon/lock.go lines 82-92 (isStale — the pure-predicate-of-resolved-inputs shape to mirror)
    - .planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md "Pattern 1" (verbatim TS watchDisabledReason + detectWsl + isWindowsDriveMount source, and the bin/codegraph.js finding that --no-watch flag and CODEGRAPH_NO_WATCH env produce the SAME reason string)
    - .planning/phases/03-watcher-on-mcp-default/03-PATTERNS.md "internal/watch/policy.go (new)" (Probe field set, verbatim reason strings)
  </read_first>
  <action>
    Write `internal/watch/policy_test.go` (package watch) as a table-driven test that references the not-yet-existing symbols `Probe`, `WatchDisabledReason`, `DetectWSL`, `ErrWatchDisabled`, and the unexported WSL-cache reset hook — so the package fails to build (RED) until Task 2 lands.

    Cover, as a slice of cases each with an injected `Probe{Env: func(string) string, IsWSL: func() bool, NoWatch bool, ForceWatch bool}` and a projectRoot:
    - Default (no flags, Env returns "", IsWSL false) → WatchDisabledReason returns "".
    - NoWatch flag true → returns exactly `CODEGRAPH_NO_WATCH=1 is set`.
    - Env("CODEGRAPH_NO_WATCH")=="1" (flag false) → returns the SAME `CODEGRAPH_NO_WATCH=1 is set` (proves flag and env are two inputs to one tier-1 check, per the bin/codegraph.js finding).
    - NoWatch beats ForceWatch: NoWatch true AND ForceWatch true → disabled (opt-out always wins, tier 1 before tier 2).
    - ForceWatch flag true, IsWSL true, projectRoot "/mnt/c/repo" → returns "" (force-on beats WSL auto-detect).
    - Env("CODEGRAPH_FORCE_WATCH")=="1" behaves identically to the ForceWatch flag.
    - WSL true + projectRoot "/mnt/c/repo" (no flags/force) → returns exactly `project is on a WSL2 /mnt/ drive, where recursive file watching is too slow to be reliable`.
    - WSL true + projectRoot "/mnt/wsl/anything" → returns "" (single-letter regex `^/mnt/[a-z](/|$)` excludes /mnt/wsl).
    - WSL true + projectRoot with backslashes (e.g. `\mnt\c\repo`-style) normalized to slashes still matches (normalizePath == filepath.ToSlash equivalence).
    - IsWSL false + projectRoot "/mnt/c/repo" → "" (not WSL, mount check irrelevant).
    - Strict env: Env("CODEGRAPH_NO_WATCH")=="true"/"yes"/"0"/" 1" → NOT disabled (only the exact string "1" triggers).

    Add a separate test for `DetectWSL` caching + off-linux behavior: because DetectWSL reads real os.Getenv/GOOS/`/proc/version`, drive it through the reset hook — call the reset hook, then assert two successive DetectWSL calls return the same value (cached), and document via a t.Skip-or-GOOS-guard that on non-linux GOOS it is always false. Keep this test deterministic: do not assert a specific true/false on the host; assert the caching property and the reset-hook re-evaluation property only.

    Do NOT put fenced implementation code in the source of policy.go from this task — this task writes tests only.
  </action>
  <verify>
    <automated>go test ./internal/watch/ -run 'TestWatchDisabledReason|TestDetectWSL' 2>&1 | grep -Eiq 'undefined|cannot find|build failed|WatchDisabledReason' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `internal/watch/policy_test.go` exists, `package watch`, and references `Probe`, `WatchDisabledReason`, `DetectWSL`, `ErrWatchDisabled`.
    - `go test ./internal/watch/` fails to build (RED) because those symbols are undefined — the failure names one of them.
    - The table includes at minimum one case per precedence tier (NO_WATCH, FORCE_WATCH, WSL+/mnt, default), the /mnt/wsl exclusion case, and at least three strict-`"1"` negative cases (`"true"`, `"0"`, `" 1"`).
    - A DetectWSL test exercises the reset hook and asserts the caching property (two calls agree) without hard-coding a host-specific boolean.
  </acceptance_criteria>
  <done>policy_test.go compiles-would-pass against the intended API but currently fails the build because policy.go does not yet exist; the precedence table and strict-env cases are all present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement internal/watch/policy.go (Probe, WatchDisabledReason, DetectWSL cached, ErrWatchDisabled)</name>
  <files>internal/watch/policy.go</files>
  <read_first>
    - internal/watch/policy_test.go (the RED contract just written — implement exactly to it)
    - internal/watch/debounce.go (doc-comment/D-number convention; os.Getenv default pattern to adapt to strict == "1")
    - internal/daemon/lock.go lines 27-30, 82-113 (ErrLockLive sentinel shape to model ErrWatchDisabled on; isStale/startTimesCorroborate pure-predicate style)
    - .planning/phases/03-watcher-on-mcp-default/03-RESEARCH.md "Pattern 1" (verbatim TS precedence + WSL detection + normalizePath == filepath.ToSlash)
  </read_first>
  <action>
    Create `internal/watch/policy.go` (package watch) implementing exactly the RED contract. Structure (identifiers are load-bearing — D-05 forbids env mutation, so inputs are explicit):

    - Exported struct `Probe` with fields: `Env func(string) string`, `IsWSL func() bool`, `NoWatch bool`, `ForceWatch bool`. Document each field with its D-reference (NoWatch=--no-watch/D-01/D-02, ForceWatch=--watch repurposed force-on/D-03).
    - `func WatchDisabledReason(projectRoot string, p Probe) string`. First, default nil probe funcs internally: if `p.Env == nil` set it to `os.Getenv`; if `p.IsWSL == nil` set it to `DetectWSL`. Then apply first-match-wins precedence (D-04):
      1. `p.NoWatch || p.Env("CODEGRAPH_NO_WATCH") == "1"` → return the string `CODEGRAPH_NO_WATCH=1 is set`.
      2. `p.ForceWatch || p.Env("CODEGRAPH_FORCE_WATCH") == "1"` → return "".
      3. `p.IsWSL() && isWindowsDriveMount(projectRoot)` → return the string `project is on a WSL2 /mnt/ drive, where recursive file watching is too slow to be reliable`.
      4. return "".
      Use strict `== "1"` comparison only (D-10) — never strconv.ParseBool or non-empty truthiness. Cite the D-13 divergence in a comment: TS's source says `fs.watch`; we say `file watching` because naming a Node API in a Go binary is wrong (documented allowed divergence, human-facing stderr only, never parsed).
    - Unexported `isWindowsDriveMount(projectRoot string) bool`: match case-insensitive regex `^/mnt/[a-z](/|$)` against `filepath.ToSlash(projectRoot)` (single-letter only — deliberately excludes /mnt/wsl/…). Compile the regexp once at package scope (var, not per-call).
    - `func DetectWSL() bool`: cached (sync.Once-guarded package-level `wslCached`/`wslValue` pair, or an equivalent once+value). Logic: if `runtime.GOOS != "linux"` → false; else if `os.Getenv("WSL_DISTRO_NAME") != "" || os.Getenv("WSL_INTEROP") != ""` → true; else read `/proc/version`, lowercase it, true if it contains "microsoft" or "wsl"; any read error → false. No panic on read failure (V5/V12: read failure degrades to false, never crashes).
    - Unexported test-reset hook (mirror onSyncStart's "unexported, no exported setter" convention) that resets the sync.Once + cached bool so policy_test.go can re-drive DetectWSL. Name it e.g. `resetWSLCacheForTests` — analogue of TS's `__resetWslCacheForTests`.
    - Exported sentinel `var ErrWatchDisabled = errors.New("daemon: watching is disabled by policy")` — modelled on lock.go's ErrLockLive; lives HERE in internal/watch (not internal/daemon) so both internal/daemon (wrap it, 03-02) and internal/cli (errors.Is it, 03-03) import it without a cycle.
  </action>
  <verify>
    <automated>go test ./internal/watch/ -run 'TestWatchDisabledReason|TestDetectWSL' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/watch/` passes (GREEN); Task 1's whole table is green.
    - `grep -F 'CODEGRAPH_NO_WATCH=1 is set' internal/watch/policy.go` matches and `grep -F 'project is on a WSL2 /mnt/ drive, where recursive file watching is too slow to be reliable' internal/watch/policy.go` matches (verbatim reason strings, D-12/D-13).
    - `grep -F 'fs.watch' internal/watch/policy.go` is absent from the emitted reason string (the WSL reason uses `file watching`); any mention is only in an explanatory comment about the divergence.
    - policy.go contains no `os.Setenv` / `os.Setenv`-style env mutation (D-05) and no `strconv.ParseBool` for the toggles (D-10 strict equality).
    - `ErrWatchDisabled` is exported from package watch; `WatchDisabledReason` and `DetectWSL` are exported.
    - `go vet ./internal/watch/` is clean.
  </acceptance_criteria>
  <done>WATCH-03's decision function is implemented, verbatim-string-accurate, strict-env, WSL-cached, and fully table-green; ErrWatchDisabled sentinel is exported for downstream enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → policy | `CODEGRAPH_NO_WATCH` / `CODEGRAPH_FORCE_WATCH` values cross from the (untrusted) process environment into the watch decision |
| filesystem → policy | `/proc/version` contents cross into DetectWSL |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing/Tampering | env toggle parsing (WatchDisabledReason) | low | mitigate | Strict `== "1"` equality (D-10) — arbitrary/truthy env noise cannot silently flip watch state; asserted by the strict-env negative cases in Task 1 |
| T-03-02 | Denial of Service | DetectWSL `/proc/version` read | low | mitigate | Read failure returns false (never panics/blocks); result cached via sync.Once so a hostile/slow /proc is read at most once per process |
| T-03-SC | Tampering (supply chain) | package installs | n/a | accept | Zero new dependencies this phase (RESEARCH Package Legitimacy Audit: N/A) — no npm/pip/cargo install task exists to gate |
</threat_model>

<verification>
- `go test ./internal/watch/ -count=1` green.
- `go vet ./internal/watch/` clean.
- Verbatim reason strings present (grep, above); no env mutation; no truthy env parsing.
</verification>

<success_criteria>
WATCH-03's policy decision is a pure, injectable, table-tested function returning the verbatim TS reason strings under the exact TS precedence, with `ErrWatchDisabled` exported for 03-02/03-03 to build on. No real WSL2 host required.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **Files:** `internal/watch/policy.go`, `internal/watch/policy_test.go`
- **Functions:** `watch.WatchDisabledReason(projectRoot string, p watch.Probe) string`, `watch.DetectWSL() bool`, unexported `isWindowsDriveMount`, unexported `resetWSLCacheForTests`
- **Types:** `watch.Probe{ Env func(string) string; IsWSL func() bool; NoWatch bool; ForceWatch bool }`
- **Sentinel errors:** `watch.ErrWatchDisabled`
- **Env vars honored (read, never mutated):** `CODEGRAPH_NO_WATCH`, `CODEGRAPH_FORCE_WATCH`, `WSL_DISTRO_NAME`, `WSL_INTEROP`
- **Verbatim strings:** `CODEGRAPH_NO_WATCH=1 is set`; `project is on a WSL2 /mnt/ drive, where recursive file watching is too slow to be reliable`
</artifacts_produced>

<output>
Create `.planning/phases/03-watcher-on-mcp-default/03-01-SUMMARY.md` when done.
</output>
