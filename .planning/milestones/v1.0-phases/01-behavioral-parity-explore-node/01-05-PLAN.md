---
phase: 01-behavioral-parity-explore-node
plan: 05
type: tdd
wave: 2
depends_on: ["01-02"]
files_modified:
  - internal/indexer/resolve.go
  - internal/indexer/resolve_test.go
  - internal/indexer/goextract/extract.go
  - internal/indexer/goextract/extract_test.go
autonomous: true
requirements: [EXPL-02]

must_haves:
  truths:
    - "The Go extractor + resolve pass emit all 6 new edge kinds: extends (Pass-2 split from embeds), overrides (Pass-2 synthesis), references/instantiates/type_of/returns (Pass-1 capture)"
    - "A class/struct extending a class/struct now produces an extends edge, distinct from embeds and from the interface->implements promotion"
  artifacts:
    - "internal/indexer/resolve.go (extends split, overrides synthesis, 4 new Pass-2 case arms)"
    - "internal/indexer/goextract/ Pass-1 emit sites for references/instantiates/type_of/returns"
  key_links:
    - "goextract Pass-1 UnresolvedRef{Kind} -> resolve.go Pass-2 switch -> schema.Edge{Kind}; extends/overrides carry Provenance=heuristic + synthesizedBy metadata"
---

<objective>
F3 (reference-language slice): make the Go extractor + resolve pass emit all 6 new D-09 edge kinds, establishing the exact pattern the other language extractors (plans 08/09) mirror. RESEARCH §B decomposition:
- `extends` (LOW): a Pass-2 reclassification — split resolve.go's embeds->promotion branch so a class/struct target that today stays "embeds" becomes "extends" (interface target still promotes to "implements", unchanged). No new tree-walking.
- `overrides` (MEDIUM): Pass-2 synthesis over already-built contains + extends/implements/embeds edges (mirror synthesizeGoImplements / walkSupertypesForMethod). No new tree-walking. Go is structural (name+arity) — a documented per-language precision note, NOT a drop.
- `references`, `instantiates`, `type_of` (MEDIUM): genuinely new Pass-1 capture, each mirroring the RefKindCalls extract->resolve shape.
- `returns` (LOW-MEDIUM): reuse the already-parsed return type (Node.ReturnType) — emit a Pass-1 ref to the named return type.

TDD: synthesis/resolution logic is an algorithm with defined I/O — RED tests assert edge presence/kind on a fixture before implementation.

Depends on plan 02 (the shared constants). resolve.go must handle the new Pass-1 kinds (else refs drop as unresolved) — so resolve Pass-2 + Go Pass-1 land together here.

Purpose: produces the graph data the RWR ranks over (F2/plan 06). This is the single largest scope item in the phase.
Output: extended resolve.go + goextract + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-CONTEXT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/indexer/resolve.go
@internal/indexer/goextract/types.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN Pass-2 extends split + overrides synthesis</name>
  <files>internal/indexer/resolve.go, internal/indexer/resolve_test.go</files>
  <read_first>
    - internal/indexer/resolve.go RefKindEmbeds promotion branch (lines 116-150 — the interface->implements split point) and synthesizeGoImplements + walkSupertypesForMethod (lines ~249-381, the Pass-2 synthesis template)
    - 01-RESEARCH.md §B rows for extends + overrides (exact split rule + synthesis derivation) and §A F1/F3
    - 01-PATTERNS.md "internal/indexer/resolve.go (extend)" + "Provenance discipline"
  </read_first>
  <behavior>
    - class B extends class A -> one extends edge B->A (NOT embeds, NOT implements)
    - class C implements interface I -> implements edge (unchanged)
    - Go struct embeds interface value -> implements (unchanged)
    - method M on B with same name+arity as a method on supertype A -> overrides edge M->A.M, Provenance=heuristic, synthesizedBy set
  </behavior>
  <action>
    Split the existing embeds->promotion branch (resolve.go:137): keep interface-target -> EdgeKindImplements exactly as-is; add a new branch where a class/struct target (non-interface, and source is a class/struct) emits EdgeKindExtends instead of the plain "embeds" fallback. Stamp extends with Provenance="heuristic" + Metadata{"synthesizedBy":"declared-extends"}, mirroring the existing declared-implements metadata stamp. Then add an overrides Pass-2 synthesis pass modeled on synthesizeGoImplements: for each type with an extends/implements/embeds supertype declaring a same-named method (reuse the walkSupertypesForMethod supertype-walk helper, do not write a new BFS), emit EdgeKindOverrides method->supertype-method with Provenance="heuristic" + synthesizedBy. Flag Go's structural (name+arity) matching as a documented precision note in the code comment (per RESEARCH §B — a divergence note, not a drop). Write RED tests on a small Go fixture first.
  </action>
  <verify>
    <automated>go test ./internal/indexer/ -run 'TestResolveExtends|TestResolveOverrides' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: resolve.go emits EdgeKindExtends for class/struct-extends-class/struct and EdgeKindOverrides via supertype-method synthesis, both Provenance=heuristic with synthesizedBy metadata
    - behavior: the interface->implements promotion and interface-embed cases are unchanged (regression-tested)
    - test-command: `go test ./internal/indexer/ -run 'TestResolveExtends|TestResolveOverrides' -count=1`
  </acceptance_criteria>
  <done>extends split + overrides synthesis land as Pass-2 over already-captured data, with provenance discipline and the interface path unregressed; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN Pass-1 capture + Pass-2 resolution for references/instantiates/type_of/returns (Go)</name>
  <files>internal/indexer/goextract/extract.go, internal/indexer/goextract/extract_test.go, internal/indexer/resolve.go</files>
  <read_first>
    - internal/indexer/goextract/extract.go (the RefKindCalls / RefKindEmbeds Pass-1 emit sites to mirror) and internal/indexer/resolve.go RefKindCalls case (lines ~100-114, the Pass-2 case-arm shape)
    - 01-RESEARCH.md §B rows for references/instantiates/type_of/returns (tree-sitter anchors + the de-dup-vs-calls and Kind-check disambiguation rules)
    - 01-PATTERNS.md "internal/indexer/resolve.go" case-arm example + per-language extractor notes
  </read_first>
  <behavior>
    - Go composite_literal `T{}` / `&T{}` where T is a class/struct-Kind node -> instantiates edge (target Kind = type)
    - a package-level identifier used but not called and not imported -> references edge (de-duped: a called symbol is NOT also a references edge)
    - `var x T` / field of type T -> type_of edge to T's node
    - `func F() T` -> returns edge F->T using the already-parsed ReturnType
  </behavior>
  <action>
    Add Pass-1 emit sites in goextract for the three new capture kinds: RefKindInstantiates (composite_literal / &T{} anchors), RefKindReferences (an identifier resolving to a package-level symbol that is neither a call callee nor an import — apply the de-dup filter so a called symbol does not also emit references), RefKindTypeOf (var/field declared type). For RefKindReturns, reuse the already-parsed return type (Node.ReturnType per graph.proto:58) — isolate the type NAME and emit a ref (generics/unions resolve to the outer named type — documented precision note). Add the matching Pass-2 case arms in resolve.go mirroring the RefKindCalls arm exactly (resolveNameRef -> schema.Edge{Kind, Provenance:"ast"}), applying the per-kind Kind-check disambiguation from RESEARCH §B (instantiates target must be a type-Kind node). Un-annotated/undeterminable cases emit NO edge (absence, not error). Write RED tests on a Go fixture asserting each kind's presence + de-dup first.
  </action>
  <verify>
    <automated>go test ./internal/indexer/ ./internal/indexer/goextract/ -run 'TestReferences|TestInstantiates|TestTypeOf|TestReturns|TestNewKindDedup' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: goextract emits UnresolvedRef with the 3 new Pass-1 kinds; resolve.go has matching case arms with Kind-check disambiguation
    - behavior: a called symbol does not also produce a references edge (de-dup); instantiates only targets type-Kind nodes; un-annotated returns/vars emit nothing
    - test-command: `go test ./internal/indexer/ ./internal/indexer/goextract/ -run 'TestReferences|TestInstantiates|TestTypeOf|TestReturns|TestNewKindDedup' -count=1`
  </acceptance_criteria>
  <done>Go emits references/instantiates/type_of/returns with correct de-dup and Kind disambiguation; resolve resolves them; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| source file -> extractor | Arbitrary third-party monorepo code is parsed; new kinds add no new parse surface (same tree-sitter walk) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Denial of Service | high-volume references/type_of edges | medium | mitigate | references/type_of are the highest-volume new kinds; de-dup vs calls/imports prevents edge duplication and the existing per-file resolve bounds cap growth; edges land at fresh non-colliding Pebble keys |
| T-01-09 | Tampering | edge provenance integrity | low | mitigate | Synthesized edges (extends/overrides) carry Provenance="heuristic" + synthesizedBy metadata, distinguishable from Provenance="ast" captured edges — no silent conflation |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — reuses existing tree-sitter grammars and resolve pipeline |
</threat_model>

<verification>
- `go test ./internal/indexer/... -count=1` green (including existing extractor/resolve regression tests).
- All 6 new kinds observable on the Go test fixtures.
- Existing implements-promotion behavior unregressed.
</verification>

<success_criteria>
The Go extractor + resolve pass emit all 6 new edge kinds with correct provenance, de-dup, and Kind disambiguation, establishing the reference pattern for plans 08/09 and the data plan 06's RWR ranks over.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-05-SUMMARY.md` when done.
</output>
