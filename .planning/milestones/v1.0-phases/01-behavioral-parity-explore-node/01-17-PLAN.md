---
phase: 01-behavioral-parity-explore-node
plan: 17
type: execute
wave: 6
depends_on: ["01-01", "01-15", "01-16"]
files_modified:
  - testdata/golden/golden_parity_test.go
  - testdata/golden/golden_test.go
  - testdata/golden/corpus/
autonomous: true
requirements: [EXPL-05, NODE-04, TEST-01]

must_haves:
  truths:
    - "The Go-side expected fixtures are regenerated (F5) against the re-indexed corpora and the new explore/node code"
    - "The behavioral harness diffs Go explore/node against the TS 1.3.1 goldens using the D-02 oracle (ordering + set membership + warning presence + header text + budget counts, after canonicalizing volatile bits) with a documented allowed-divergence list"
    - "Go explore/node output is byte-identical across the CLI command and the MCP tool surface (shared engine)"
  artifacts:
    - "testdata/golden/golden_parity_test.go + golden_test.go (extended behavioral + CLI==MCP assertions)"
    - "testdata/golden/corpus/**/ regenerated Go-side expected fixtures"
  key_links:
    - "re-indexed graph (plan 15) + wired explore (plan 16) -> Go output -> D-02 diff against TS goldens (plan 01) -> parity proven on CLI + MCP"
---

<objective>
Close the phase's central contract (STATE.md's highest-risk item): F5 golden regeneration + the behavioral parity harness that proves Go's explore/node match TS 1.3.1 on the exact behaviors v0.1 never tested. This is the drop-in-parity gate the phase exists for.

Two things:
1. F5 (RESEARCH §A): regenerate the Go-side EXPECTED fixtures. The committed explore.json fixtures went stale the moment the extractors changed output (plan 05/08/09) and were re-indexed (plan 15); regenerate them by running the NEW Go explore/node code against the re-indexed corpora. The TS-side goldens (plan 01) are unchanged (TS already emits all 9 kinds) and remain the oracle.
2. The behavioral harness: extend golden_parity_test.go / golden_test.go to diff Go against the TS goldens using the D-02 oracle — assert ordering + set membership + warning presence + header text + budget counts after canonicalizing volatile bits (absolute->repo-relative paths, whitespace, float-jitter tie-breaks), with every allowed divergence recorded and justified (the D-09 edge-kind coverage note, per-language extraction precision notes, and any H-item documented divergence). Assert on BOTH surfaces: Go CLI == Go MCP (byte-identical, EXPL-05/NODE-04) AND Go vs TS (structural, D-02) for explore-multi, node-multi, the Test*-heavy case, and the structural-beats-lexical case.

TEST-01 note: Phase 8's REL-04 re-runs THIS harness as the drop-in gate; it does not own it. This plan owns building it.

Purpose: proves behavioral parity was actually achieved (not just template-parity) — the whole point of fixtures-first.
Output: regenerated Go fixtures + extended parity harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-CONTEXT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-VALIDATION.md
@testdata/golden/golden_parity_test.go
@testdata/golden/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: F5 — regenerate the Go-side expected fixtures against the re-indexed corpora</name>
  <files>testdata/golden/corpus/**/explore-multi.json, testdata/golden/corpus/**/node-multi.json, testdata/golden/corpus/**/explore.json</files>
  <read_first>
    - testdata/golden/README.md (the frozen-ground-truth policy: TS goldens are the oracle; Go-side expected fixtures are regenerated by running the Go binary, never hand-edited)
    - 01-RESEARCH.md §A F5 (why the committed explore fixtures are stale after the re-index) and 01-VALIDATION.md Wave 0 (F5 regen is an explicit task)
    - testdata/golden/capture.sh (the Go-side capture path to re-run)
  </read_first>
  <action>
    Regenerate the Go-side expected fixtures by running the NEW Go explore/node code (plan 16) against the re-indexed corpora (plan 15): the existing single-symbol explore.json baseline (now reflecting new-kind ranking) plus the new behavioral explore-multi.json / node-multi.json. Do this via the Go-side branch of capture.sh (or an equivalent Go-binary run) so the fixtures are machine-generated, never hand-authored. Commit the regenerated Go-side fixtures. The TS-side goldens (explore-mcp/node-mcp and the TS captures from plan 01) are the oracle and are NOT regenerated here.
  </action>
  <verify>
    <automated>go build ./... && test -f testdata/golden/corpus/synthetic-parity/node-multi.json && jq -e '.output | contains("definitions named")' testdata/golden/corpus/synthetic-parity/node-multi.json >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - source: Go-side explore/node fixtures are regenerated from the re-indexed corpora via the binary (not hand-edited)
    - behavior: node-multi.json shows Go enumerating 2+ defs with the two-line header; explore fixtures reflect RWR (not lexical) ordering
    - test-command: `jq -e '.output | contains("definitions named")' testdata/golden/corpus/synthetic-parity/node-multi.json`
  </acceptance_criteria>
  <done>Go-side expected fixtures are regenerated (F5) against the re-indexed corpora and committed; TS goldens remain the untouched oracle.</done>
</task>

<task type="auto">
  <name>Task 2: Behavioral parity harness — Go vs TS (D-02 oracle) with allowed-divergence list</name>
  <files>testdata/golden/golden_parity_test.go, testdata/golden/golden_test.go</files>
  <read_first>
    - testdata/golden/golden_parity_test.go + golden_test.go (the existing diff harness to extend, not replace)
    - 01-CONTEXT.md D-02 (the normalized/structural-equivalence oracle: ordering + set membership + warning presence + header text + budget counts, after canonicalizing volatile bits; every divergence recorded/justified)
    - 01-RESEARCH.md Validation Architecture Test Map (per-requirement commands)
  </read_first>
  <action>
    Extend the harness to diff Go explore/node output against the TS 1.3.1 goldens (plan 01) per the D-02 oracle: for ranked explore lists, assert file ORDERING + set MEMBERSHIP + the no-covering-tests WARNING presence + budget/header COUNTS after canonicalizing volatile bits (absolute->repo-relative paths, whitespace, float-jitter tie-breaks rounded per D-04); for node, assert byte-identical where TS is deterministic (the two-line header, single-def output, budget counts) and set membership for the enumerated defs. Encode the allowed-divergence list explicitly in the harness (the D-09 edge-kind coverage note, per-language extraction precision notes from plans 08/09, and any documented H-item divergence) with a justification per entry so the harness is honest, not flaky. Run against explore-multi, node-multi, the Test*-heavy case, and the structural-beats-lexical case on all three corpora.
  </action>
  <verify>
    <automated>go test ./testdata/golden/... -run 'TestGoldenParity|TestGoldenBehavioral' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - source: the harness implements the D-02 oracle (ordering+membership+warning+header+counts, canonicalized) with an explicit allowed-divergence list
    - behavior: Go vs TS parity holds on all four behavioral cases; documented divergences are asserted-around, not silently ignored
    - test-command: `go test ./testdata/golden/... -run 'TestGoldenParity|TestGoldenBehavioral' -count=1`
  </acceptance_criteria>
  <done>The behavioral harness diffs Go vs TS under the D-02 oracle with a justified allowed-divergence list, green on all four cases.</done>
</task>

<task type="auto">
  <name>Task 3: CLI==MCP byte-identity assertions (EXPL-05 / NODE-04) on both surfaces</name>
  <files>testdata/golden/golden_parity_test.go</files>
  <read_first>
    - internal/mcp/tools.go exploreHandler + companionHandler["node"] (already delegate to Engine.Explore/Engine.Node — the shared code path proving EXPL-05/NODE-04 structurally)
    - 01-PATTERNS.md "Shared-Engine CLI/MCP parity" (no new MCP plumbing; assert equality)
  </read_first>
  <action>
    Add assertions that Go's explore and node output is BYTE-IDENTICAL between the CLI command path and the MCP tool handler path (exploreHandler / node case) for each behavioral fixture — the shared-Engine guarantee (EXPL-05/NODE-04). Drive both the CLI (Engine.Explore/Engine.Node directly, as the CLI does) and the MCP handler (via the tool handler function) with the same query and assert the returned text is identical. This is structural (both call the same Engine methods) — the test locks it in so a future divergence is caught.
  </action>
  <verify>
    <automated>go test ./testdata/golden/... -run 'TestExploreCLIMatchesMCP|TestNodeCLIMatchesMCP' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: the harness invokes both the CLI-equivalent Engine call and the MCP handler and asserts byte-identical output
    - behavior: explore and node output is identical across CLI and MCP for every behavioral fixture
    - test-command: `go test ./testdata/golden/... -run 'TestExploreCLIMatchesMCP|TestNodeCLIMatchesMCP' -count=1`
  </acceptance_criteria>
  <done>CLI==MCP byte-identity is asserted for explore + node on all behavioral fixtures (EXPL-05/NODE-04), green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness -> committed fixtures | The parity gate; fixtures are machine-generated from the oracle |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28 | Tampering | fixture integrity / score-fudging | high | mitigate | The D-02 oracle asserts ordering+membership+warning+counts after canonicalization (not raw float equality) so parity cannot be faked by score-fudging; Go fixtures are machine-regenerated, TS goldens untouched |
| T-01-29 | Repudiation | undocumented divergence | medium | mitigate | Every allowed divergence is recorded + justified in the harness (D-02) — no silent behavioral gap; the harness explicitly does not assert on documented-divergence surfaces |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — Go stdlib testing + existing harness |
</threat_model>

<verification>
- `go test ./testdata/golden/... -count=1` green (behavioral parity + CLI==MCP).
- `go test ./... -count=1` full suite green (per VALIDATION.md; re-run `go test ./internal/daemon/ -count=1` in isolation on a daemon flake before calling it a regression — Phase 1 touches no daemon files).
- Go-side fixtures regenerated from the re-indexed corpora (F5); TS goldens unchanged.
- The allowed-divergence list is present and justified in the harness.
</verification>

<success_criteria>
The behavioral fixture harness proves Go's explore/node match TS 1.3.1 (D-02 oracle) for ambiguous names, multi-word queries, relevance ordering, and coverage warnings on BOTH CLI and MCP surfaces, with Go-side fixtures regenerated against the 9-kind re-indexed corpora and every divergence documented — closing v0.1's single-symbol blind spot (TEST-01) and locking EXPL-05/NODE-04.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-17-SUMMARY.md` when done.
</output>
