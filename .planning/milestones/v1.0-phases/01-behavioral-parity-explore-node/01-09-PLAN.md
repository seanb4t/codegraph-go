---
phase: 01-behavioral-parity-explore-node
plan: 09
type: tdd
wave: 3
depends_on: ["01-05"]
files_modified:
  - internal/indexer/pyextract/
  - internal/indexer/tsextract/
autonomous: true
requirements: [EXPL-02]

must_haves:
  truths:
    - "The Python and TS/JS extractors emit the new Pass-1 edge kinds (references, instantiates, type_of, returns) at their language-specific anchors, mirroring the Go reference implementation"
    - "Un-annotated Python vars/returns emit NO edge (absence, not error) — a documented dynamic-typing divergence"
  artifacts:
    - "internal/indexer/pyextract/ Pass-1 emit sites for the 4 new capture kinds"
    - "internal/indexer/tsextract/ Pass-1 emit sites for the 4 new capture kinds"
  key_links:
    - "pyextract/tsextract UnresolvedRef{Kind} -> shared resolve.go Pass-2 case arms (plan 05)"
---

<objective>
F3 (Python + TS/JS slice of the priority-4 languages): emit the new Pass-1 edge kinds from the Python and TS/JS extractors at their language-specific tree-sitter anchors, mirroring the Go reference implementation (plan 05). Pass-2 resolution + extends/overrides synthesis is shared in resolve.go.

Anchors (RESEARCH §B table):
- instantiates: Python a `call` whose callee resolves to a class Kind (overlaps `calls` syntactically — resolve-time Kind check disambiguates); TS `new_expression`.
- references: Python `attribute`/`identifier` load; TS `identifier` in expression position (de-dup vs calls).
- type_of: Python annotated assignment `x: T`; TS type annotation.
- returns: Python `-> T`; TS return type annotation.

Python's dynamic typing: un-annotated returns/vars emit nothing (absence, not error) — a documented D-02 divergence, NOT a heuristic drop.

TDD per kind per language. Depends on plan 05. Disjoint packages from plan 08 — parallel in Wave 3.

Purpose: complete the priority-4 (Go/Java/C#/Python/TS-JS) coverage of the 9-kind graph; mainstream-6 follow the existing D-11 matrix (out of scope this phase per RESEARCH §B).
Output: extended pyextract + tsextract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/indexer/goextract/extract.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN Python Pass-1 emit (instantiates/references/type_of/returns) with dynamic-typing absence</name>
  <files>internal/indexer/pyextract/, internal/indexer/pyextract/*_test.go</files>
  <read_first>
    - internal/indexer/goextract/extract.go (reference pattern) + pyextract existing emit sites
    - 01-RESEARCH.md §B Python rows (call-to-class instantiates via resolve-time Kind check; attribute/identifier references; `x: T` type_of; `-> T` returns; un-annotated -> no edge)
  </read_first>
  <behavior>
    - Python `Foo()` where Foo is a class -> instantiates edge (resolve-time Kind check, not a plain call)
    - Python `x: Foo` -> type_of edge
    - Python `def f() -> Foo:` -> returns edge
    - Python `def f():` (no annotation) -> NO returns edge (absence)
    - Python `obj.attr` load not called -> references edge (de-dup vs calls)
  </behavior>
  <action>
    Add Python Pass-1 emit sites at the §B anchors: a `call` whose callee resolves to a class-Kind node -> RefKindInstantiates (the resolve-time Kind check disambiguates instantiation from a plain call — reuse the plan-05 Kind-check pattern); annotated assignment `x: T` -> RefKindTypeOf; `-> T` -> RefKindReturns; attribute/identifier load not a call callee or import -> RefKindReferences (de-dup). For un-annotated vars/returns, emit NOTHING (absence) and add a code comment documenting this as a D-02 dynamic-typing divergence. Generic/composite types resolve to the outer named type (precision note). Write RED fixture tests including the un-annotated absence cases first.
  </action>
  <verify>
    <automated>go test ./internal/indexer/pyextract/ -run 'TestPyReferences|TestPyInstantiates|TestPyTypeOf|TestPyReturns|TestPyUnannotatedAbsence' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: pyextract emits the 4 kinds at §B anchors; un-annotated cases emit no edge with a documented D-02 note
    - behavior: class-call -> instantiates; annotated -> type_of/returns; un-annotated -> nothing (no error)
    - test-command: `go test ./internal/indexer/pyextract/ -run 'TestPy(References|Instantiates|TypeOf|Returns|UnannotatedAbsence)' -count=1`
  </acceptance_criteria>
  <done>Python emits the 4 new kinds with correct disambiguation and documented dynamic-typing absence; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN TS/JS Pass-1 emit (new_expression/references/type_of/returns)</name>
  <files>internal/indexer/tsextract/, internal/indexer/tsextract/*_test.go</files>
  <read_first>
    - internal/indexer/tsextract existing emit sites + goextract reference pattern
    - 01-RESEARCH.md §B TS rows (new_expression instantiates; identifier references; type annotation type_of; return type annotation returns)
  </read_first>
  <behavior>
    - TS `new Foo()` -> instantiates edge
    - TS `const x: Foo` -> type_of edge
    - TS `function f(): Foo` -> returns edge
    - TS identifier in expression position not called/imported -> references edge (de-dup)
    - untyped JS var -> no type_of edge (absence)
  </behavior>
  <action>
    Add TS/JS Pass-1 emit sites at the §B anchors: new_expression -> RefKindInstantiates, type annotation -> RefKindTypeOf, return type annotation -> RefKindReturns, expression-position identifier not a call callee/import -> RefKindReferences (de-dup). Untyped JS emits no type_of/returns (absence, documented D-02 note). Reuse shared constants + UnresolvedRef shape; shared resolve arms resolve them. Write RED fixture tests per kind first.
  </action>
  <verify>
    <automated>go test ./internal/indexer/tsextract/ -run 'TestTSReferences|TestTSInstantiates|TestTSTypeOf|TestTSReturns' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: tsextract emits the 4 kinds at §B TS anchors; untyped JS absence documented
    - behavior: new_expression -> instantiates; annotations -> type_of/returns; identifier -> references (de-duped)
    - test-command: `go test ./internal/indexer/tsextract/ -run 'TestTS(References|Instantiates|TypeOf|Returns)' -count=1`
  </acceptance_criteria>
  <done>TS/JS emits the 4 new kinds at correct anchors with documented untyped-JS absence; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| source file -> extractor | Arbitrary Python/TS/JS parsed via existing grammars; no new parse surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-15 | Denial of Service | high-volume references/type_of | medium | mitigate | de-dup vs calls + existing per-file resolve bounds; new kinds at fresh Pebble keys |
| T-01-16 | Tampering | dynamic-typing false edges | low | mitigate | Python/JS un-annotated cases emit nothing rather than guessing a type — no fabricated edges; documented D-02 divergence |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — existing python/typescript grammars |
</threat_model>

<verification>
- `go test ./internal/indexer/pyextract/ ./internal/indexer/tsextract/ -count=1` green.
- All 4 new kinds observable on Python + TS fixtures; un-annotated absence verified.
</verification>

<success_criteria>
Python and TS/JS emit the 4 new Pass-1 edge kinds at their correct anchors with resolve-time Kind disambiguation and documented dynamic-typing absence, completing priority-4 coverage, TDD-verified.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-09-SUMMARY.md` when done.
</output>
