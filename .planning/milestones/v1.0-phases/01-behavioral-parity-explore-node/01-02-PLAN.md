---
phase: 01-behavioral-parity-explore-node
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/indexer/goextract/types.go
autonomous: true
requirements: [EXPL-02]

must_haves:
  truths:
    - "The 6 D-09 edge-kind names exist as single shared string constants, referenced (not re-declared) by resolve.go, the language extractors, and query/rwr.go"
    - "No SchemaVersion bump and no proto regeneration occur — Edge.kind is a free-form proto3 string, so new kinds are additive DATA"
  artifacts:
    - "internal/indexer/goextract/types.go (6 new RefKind*/EdgeKind* constants + updated UnresolvedRef.Kind doc)"
  key_links:
    - "goextract constants -> resolve.go emit sites (plan 05) + rwr.go RankEdges set (plan 06) — one definition, not three copies"
---

<objective>
F1 of the D-09 foundation wave: add the 6 missing edge-kind names to goextract's shared vocabulary so every downstream consumer (resolve.go's Pass-2 emission, the per-language extractors' Pass-1 emission, and query/rwr.go's RANK_EDGES set) references ONE definition. This is the smallest but most load-bearing foundation task — it unblocks both the extraction work (plans 05/08/09) and the RWR ranking (plan 06).

Critical scope fact (RESEARCH §A, VERIFIED): `Edge.kind` is `string kind = 3;` in graph.proto — a free-form proto3 string, NOT an enum. New kinds are additive string VALUES. Do NOT bump SchemaVersion, do NOT regenerate proto, do NOT touch internal/migrate/translate.go (it already passes all 9 kinds through verbatim). Adding a version-gate task here would be a no-op that wrongly signals older indexes incompatible.

The 6 kinds (RESEARCH §B, correcting the coordinator's 5): `extends`, `references`, `overrides`, `instantiates`, `returns`, `type_of`. (`calls`/`implements`/`imports` already exist.)

Purpose: one shared constant per kind is the "one definition not three copies" discipline (PATTERNS.md Shared Patterns) — the entire edge-kind expansion depends on this being right first.
Output: extended goextract/types.go.

Artifacts this plan produces: 4 new Pass-1 RefKind* consts + 2 Pass-2 EdgeKind* consts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-behavioral-parity-explore-node/01-CONTEXT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/indexer/goextract/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 6 D-09 edge-kind constants to goextract vocabulary</name>
  <files>internal/indexer/goextract/types.go</files>
  <read_first>
    - internal/indexer/goextract/types.go (the existing RefKindCalls/Imports/Embeds/Contains block + EdgeKindImplements doc-comment discipline at lines 32-50 — mirror it exactly)
    - 01-RESEARCH.md §C.1 (the 9 RANK_EDGES members + which 6 are new) and §B (Pass-1 vs Pass-2 distinction: references/instantiates/returns/type_of are Pass-1 captured; extends/overrides are Pass-2 synthesis)
    - 01-PATTERNS.md "internal/indexer/goextract/types.go (extend)" (the exact const pattern to mirror)
  </read_first>
  <action>
    Add to types.go, mirroring the existing RefKind*/EdgeKindImplements style verbatim: four Pass-1-captured kinds as RefKind* constants — RefKindReferences = "references", RefKindInstantiates = "instantiates", RefKindReturns = "returns", RefKindTypeOf = "type_of"; and two Pass-2-synthesis-only kinds as EdgeKind* constants — EdgeKindExtends = "extends" (split from the embeds->promotion branch, see plan 05), EdgeKindOverrides = "overrides" (synthesized from contains+extends/implements, see plan 05). Give EdgeKindExtends and EdgeKindOverrides the same "named here so resolve.go's check and query/rwr.go's RankEdges share ONE definition rather than copies" doc-comment discipline the existing EdgeKindImplements block carries. Update the UnresolvedRef.Kind doc comment to enumerate the new Pass-1 kinds alongside the existing ones. Do NOT declare a proto enum, do NOT edit graph.proto or graph.pb.go, do NOT change SchemaVersion in internal/schema/meta.go.
  </action>
  <verify>
    <automated>go build ./internal/indexer/goextract/ && go doc ./internal/indexer/goextract 2>/dev/null | grep -c -E 'RefKindReferences|RefKindInstantiates|RefKindReturns|RefKindTypeOf|EdgeKindExtends|EdgeKindOverrides' | awk '$1>=6{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - source: types.go declares exactly 6 new exported constants with values "references","instantiates","returns","type_of","extends","overrides"
    - source: internal/schema/meta.go SchemaVersion is UNCHANGED (still 1); graph.proto/graph.pb.go untouched
    - behavior: the package compiles and the 6 constants are visible to importers
    - test-command: `go build ./internal/indexer/goextract/ && go vet ./internal/indexer/goextract/`
  </acceptance_criteria>
  <done>All 6 edge-kind names exist as single shared constants with mirror-of-EdgeKindImplements doc discipline; no schema/proto/version change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds string constants; no input crosses a boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | on-disk index compatibility | low | accept | New kinds are additive proto3 string values landing at fresh, non-colliding Pebble key ranges (edgeKey folds kind); no existing edge key migrates and no SchemaVersion bump falsely invalidates older indexes (RESEARCH §A VERIFIED) |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — pure constant additions to an existing package |
</threat_model>

<verification>
- `go build ./internal/indexer/goextract/` compiles.
- All 6 constants resolvable via `go doc`.
- `git diff internal/schema/` is empty (no proto/version change).
</verification>

<success_criteria>
The 6 D-09 edge-kind names exist as single shared goextract constants, ready for resolve.go emission and rwr.go's RankEdges, with zero schema/proto/SchemaVersion change.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-02-SUMMARY.md` when done.
</output>
