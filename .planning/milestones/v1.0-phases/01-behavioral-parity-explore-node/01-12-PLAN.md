---
phase: 01-behavioral-parity-explore-node
plan: 12
type: tdd
wave: 3
depends_on: ["01-03"]
files_modified:
  - internal/query/seeding.go
  - internal/query/seeding_test.go
autonomous: true
requirements: [EXPL-02]

must_haves:
  truths:
    - "Named-symbol seeding resolves EVERY exact-name definition of each query identifier (via full-scan getNodesByName, not FTS) and disambiguates overloads by tier"
    - "Overload disambiguation follows TS's tiers exactly: <=3 defs inject all + co-named callers>=0.25*maxCallers; >3 defs use type-token corroboration (<=4) else top-1 by body substance"
  artifacts:
    - "internal/query/seeding.go (H13 named-symbol seeding + per-overload disambiguation tiers)"
  key_links:
    - "extractSymbolsFromQuery (plan 03) -> seeding -> seed set feeding RWR restart vector (plan 06) + the +50 named-seed file score (plan 13)"
---

<objective>
Port H13 — named-symbol seeding with per-overload disambiguation tiers (RESEARCH §C.2, mcp/tools.js:2477-2562) — the stage that resolves the agent's named symbols into RWR seeds and gives their files the dominant +50 score (plan 13). This is one of the more intricate single heuristics; port it faithfully per D-10.

Exact rules (VERBATIM from §C.2 H13):
- Tokenize the query again; tokens must be >=3 chars, at most 16 tokens.
- Resolve each bare name via getNodesByName (full-scan exact match, NOT FTS).
- Overload disambiguation: if <=3 defs -> inject ALL; the seed tier = def0 plus any co-named def whose caller count >= 0.25*maxCallers. If >3 defs -> keep type-token-corroborated defs (<=4), else the top-1 by body substance.
- PascalCase type tokens (excluding the project name) bias overload selection.

Consumes extractSymbolsFromQuery (plan 03). Uses the full-scan name lookup (the resolveSymbolNode base). Pure function over a Reader + query — TDD against a synthetic overloaded-symbol index. New file seeding.go — no same-wave conflict.

Purpose: named seeding is what makes an agent-named symbol's file dominate the ranking (the +50 tier) and steers RWR's restart vector — central to EXPL-02's "structural beats lexical."
Output: internal/query/seeding.go + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/query/traverse.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN name resolution + the <=3-defs seeding tier</name>
  <files>internal/query/seeding.go, internal/query/seeding_test.go</files>
  <read_first>
    - 01-RESEARCH.md §C.2 row H13 (tokens>=3 & <=16; getNodesByName full-scan; <=3 defs inject all + co-named callers>=0.25*maxCallers)
    - internal/query/traverse.go resolveSymbolNode + BuildReverseAdjacency (full-scan name lookup + caller counts) and tokenize.go extractSymbolsFromQuery
  </read_first>
  <behavior>
    - a query naming a symbol with 2 defs -> both injected; the seed tier includes def0 + any def with callers>=0.25*maxCallers
    - tokens shorter than 3 chars or beyond the 16-token limit -> ignored
    - resolution uses full-scan exact-name match, not FTS
  </behavior>
  <action>
    Create seeding.go (package query). Tokenize the query via extractSymbolsFromQuery, filter to tokens >=3 chars capped at 16. For each token, resolve ALL exact-name definitions via a full-scan getNodesByName-equivalent (the resolveSymbolNode base — reuse IterateNodes). For a name with <=3 defs, inject all defs, computing the seed tier as def0 plus any co-named def whose caller count (via BuildReverseAdjacency) is >= 0.25*maxCallers among that name's defs. Deterministic ordering throughout (sort). Write RED tests for the <=3-def tier arithmetic and the token filters first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestSeedingResolve|TestSeedingSmallOverload' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: seeding.go resolves exact names via full-scan and applies the <=3-def inject-all + callers>=0.25*maxCallers tier
    - behavior: token >=3/<=16 filters honored; caller-threshold tiering matches §C.2
    - test-command: `go test ./internal/query/ -run 'TestSeedingResolve|TestSeedingSmallOverload' -count=1`
  </acceptance_criteria>
  <done>Name resolution + the <=3-def seeding tier land verbatim, deterministic, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>RED+GREEN the >3-defs disambiguation (type-token corroboration / top-1 by substance)</name>
  <files>internal/query/seeding.go, internal/query/seeding_test.go</files>
  <read_first>
    - 01-RESEARCH.md §C.2 row H13 (>3 defs: type-token-corroborated <=4 else top-1 by body substance; PascalCase type tokens excluding project name bias selection)
  </read_first>
  <behavior>
    - a name with 6 defs, query carries a PascalCase type token matching 2 of them -> those <=4 corroborated defs seeded
    - a name with 6 defs, no corroborating type token -> top-1 by body substance seeded
    - the project name as a PascalCase token -> excluded from biasing
  </behavior>
  <action>
    Implement the >3-def branch: when a name has >3 defs, prefer defs corroborated by a PascalCase type token from the query (excluding the project name), keeping at most 4; if none corroborate, seed the single top-1 def by body substance (the RESEARCH §C.2 "body substance" measure). Extract PascalCase type tokens from the query and exclude the project name. Deterministic tie-breaks (lowest-Id). Write RED tests for the corroborated-<=4 case, the no-corroboration top-1 case, and the project-name exclusion first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestSeedingLargeOverload|TestSeedingProjectNameExcluded' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: seeding.go implements the >3-def type-token-corroborated (<=4) else top-1-by-substance disambiguation, excluding the project name
    - behavior: corroboration caps at 4; no-corroboration falls to top-1; project name never biases
    - test-command: `go test ./internal/query/ -run 'TestSeedingLargeOverload|TestSeedingProjectNameExcluded' -count=1`
  </acceptance_criteria>
  <done>The >3-def disambiguation tiers land verbatim with project-name exclusion, deterministic, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| query identifiers -> full-scan name lookup | Untrusted identifiers drive a bounded name scan |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Denial of Service | many-overload name explosion | medium | mitigate | Token count capped at 16; overload seeding capped at <=4 (corroborated) or 1 (top-1) per name — bounds the seed set regardless of overload count |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — reuses traverse.go scan/adjacency |
</threat_model>

<verification>
- `go test ./internal/query/ -run 'TestSeeding' -count=1` green.
- Both overload branches (<=3 and >3) match §C.2 exactly on synthetic overloaded fixtures.
</verification>

<success_criteria>
Named-symbol seeding with the full per-overload disambiguation tiers (H13) is ported verbatim — full-scan resolution, the caller-threshold small-overload tier, and the type-token/top-1 large-overload tiers — TDD-verified.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-12-SUMMARY.md` when done.
</output>
