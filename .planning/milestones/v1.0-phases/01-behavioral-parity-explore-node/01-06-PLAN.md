---
phase: 01-behavioral-parity-explore-node
plan: 06
type: tdd
wave: 2
depends_on: ["01-02"]
files_modified:
  - internal/query/rwr.go
  - internal/query/rwr_test.go
autonomous: true
requirements: [EXPL-02]

must_haves:
  truths:
    - "computeGraphRelevance runs fixed-25-iteration undirected unweighted power-iteration RWR (alpha=0.25) over the 9-kind RANK_EDGES set, restarting to a uniform distribution over seeds present in the candidate set"
    - "Ranking is deterministic and reproducible: no convergence early-exit, deterministic seed iteration order, scores rounded to fixed precision, score-desc-then-Id-asc tie-break"
  artifacts:
    - "internal/query/rwr.go (computeGraphRelevance + RankEdges set)"
    - "internal/query/rwr_test.go"
  key_links:
    - "RankEdges references goextract constants (one definition) -> adjacency build -> per-node mass consumed by scoring (plan 13) + gate (plan 14)"
---

<objective>
Port EXPL-02's load-bearing algorithm: computeGraphRelevance (RWR, RESEARCH §3) as a pure function over an in-memory subgraph. This is the single hardest, highest-risk item in the phase (STATE.md blocker) — the golden-corpus contract depends on its determinism (D-04).

Exact spec (RESEARCH §3 + §C.1, all VERIFIED): undirected, UNWEIGHTED (plain Set membership over the 9 RANK_EDGES kinds — NO per-kind weights), alpha=0.25, FIXED 25 iterations with no early-exit, dangling nodes keep their own mass, restart vector uniform over seeds present in the candidate set (fallback uniform-over-all if no seed lands).

D-04 determinism (the golden contract): fixed 25 iters (no convergence threshold), deterministic node iteration order for seeding (sort any map iteration), round scores to a fixed precision before any comparison, and score-descending-then-lexicographic-Id-ascending tie-break (reuse the codebase lowest-Id convention).

RankEdges is the 9-kind set from RESEARCH §C.1, referencing goextract's shared constants (one definition, not re-declared literals) — hence the depends_on plan 02.

Pure function, unit-testable on synthetic in-memory subgraphs (no store, no re-index needed) — so this lands in Wave 2 and is fully TDD'd against hand-built adjacency fixtures. Golden validation on the real re-indexed corpus happens in plan 17.

Purpose: structurally-connected symbols must outrank lexical name-matches; this is the math that makes that true.
Output: internal/query/rwr.go + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-CONTEXT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/query/traverse.go
@internal/indexer/goextract/types.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN RankEdges set + undirected adjacency build</name>
  <files>internal/query/rwr.go, internal/query/rwr_test.go</files>
  <read_first>
    - 01-RESEARCH.md §3 (adjacency build: skip edges not in RANK_EDGES, skip self-loops, push both directions) and §C.1 (the 9 members) and Pitfall 2 (edge-kind gap — now closed by D-09)
    - internal/query/traverse.go BuildReverseAdjacency (fresh-per-call, WR-04 dangling-skip discipline to mirror)
    - 01-PATTERNS.md "internal/query/rwr.go (new)" (imports, RankEdges referencing goextract consts, fresh-per-call doc discipline)
    - internal/indexer/goextract/types.go (the RefKind*/EdgeKind* constants to reference)
  </read_first>
  <behavior>
    - an edge whose Kind is not in RankEdges -> excluded from adjacency
    - a self-loop (i==j) -> excluded
    - an edge to a node absent from the candidate set -> skipped (WR-04), not an error
    - RankEdges has exactly the 9 members from §C.1, sourced from goextract constants
  </behavior>
  <action>
    Create rwr.go (package query). Declare RankEdges as a map[string]bool with exactly the 9 members from RESEARCH §C.1, referencing goextract.RefKindCalls/RefKindReferences/EdgeKindExtends/EdgeKindImplements/EdgeKindOverrides/RefKindInstantiates/RefKindReturns/RefKindTypeOf/RefKindImports (one shared definition — never re-declare the string literals). Implement the adjacency build from §3: index nodeIDs->position, for each edge skip if Kind not in RankEdges or i==j or either endpoint absent from the candidate set (WR-04 skip, mirroring BuildReverseAdjacency), else push j onto adj[i] AND i onto adj[j] (undirected). Build fresh every call — no package-level cache (mirror BuildReverseAdjacency's doc discipline: a long-lived MCP process must never serve a stale view). Write RED tests on synthetic edge slices first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestRankEdges|TestRWRAdjacency' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: RankEdges has 9 members sourced from goextract constants; adjacency build is undirected and skips non-RANK/self/dangling edges
    - behavior: excluded/self/dangling edges do not appear in adjacency; no panic on unknown endpoints
    - test-command: `go test ./internal/query/ -run 'TestRankEdges|TestRWRAdjacency' -count=1`
  </acceptance_criteria>
  <done>RankEdges (9 kinds, shared constants) + undirected WR-04-tolerant adjacency build land, fresh-per-call, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN fixed-25-iteration deterministic power iteration</name>
  <files>internal/query/rwr.go, internal/query/rwr_test.go</files>
  <read_first>
    - 01-RESEARCH.md §3 (the power-iteration loop: restart vector over seeds, alpha=0.25, 25 fixed iters, dangling keeps mass, s = (1-alpha)*next + alpha*r) and D-04 determinism rules
    - internal/query/traverse.go resolveSymbolNode lowest-Id tie-break (the codebase convention to reuse)
  </read_first>
  <behavior>
    - two runs on the same subgraph produce bit-identical (post-rounding) score maps
    - a seed node ends with higher mass than a distant node
    - no seed present -> uniform restart, all nodes get non-zero mass
    - a dangling node (degree 0) retains its own mass across iterations
    - score ties resolve score-desc then Id-asc
  </behavior>
  <action>
    Implement computeGraphRelevance(nodeIDs []string, edges []*schema.Edge, seedIDs map[string]bool) map[string]float64 porting §3's loop verbatim: build restart vector r uniform over seeds present in the candidate set (fallback uniform-over-all when rsum==0), then run EXACTLY 25 iterations (no convergence early-exit) of the documented redistribution with alpha=0.25, where degree-0 nodes keep their mass (next[i]+=si). Enforce D-04 determinism: iterate seedIDs in a sorted order (sort the keys — never range the map directly into r), and round each final score to a fixed precision (define a single rounding helper, e.g. to 1e-9) before returning so float jitter can never reorder downstream. Provide a stable ordering helper for consumers: score-descending then Id-ascending. Write RED determinism tests (two runs equal, seed>distant, dangling retention, tie-break) first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestComputeGraphRelevance|TestRWRDeterminism' -count=5</automated>
  </verify>
  <acceptance_criteria>
    - source: exactly 25 iterations, alpha=0.25, sorted seed iteration, fixed-precision rounding, score-desc-then-Id-asc ordering helper
    - behavior: repeated runs (count=5) yield identical rounded scores; seeds outrank distant nodes; dangling mass retained
    - test-command: `go test ./internal/query/ -run 'TestComputeGraphRelevance|TestRWRDeterminism' -count=5`
  </acceptance_criteria>
  <done>Deterministic fixed-25-iter RWR power iteration lands per §3 + D-04; repeated runs are bit-identical post-rounding; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| candidate subgraph -> RWR | A gathered subgraph (bounded upstream) is ranked in memory |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Denial of Service | unbounded RWR graph size | high | mitigate | computeGraphRelevance is O(iters * edges) = O(25 * edges); the subgraph is bounded UPSTREAM by maxNodes=200 / GLUE_NODE_CAP=60 (ported in plans 11/16). This plan's function assumes a bounded input; the caps are enforced before it is called (documented in the doc comment as a precondition) |
| T-01-11 | Tampering | non-deterministic ranking | high | mitigate | Fixed 25 iters + sorted seed order + fixed-precision rounding + score-desc/Id-asc tie-break guarantee reproducibility (D-04) — the golden-corpus contract; asserted by count=5 determinism test |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — hand-rolled power iteration over []float64 (RESEARCH Don't Hand-Roll: matches TS's own choice; gonum rejected) |
</threat_model>

<verification>
- `go test ./internal/query/ -run 'TestRankEdges|TestRWRAdjacency|TestComputeGraphRelevance|TestRWRDeterminism' -count=5` green (determinism proven by repeated runs).
- RankEdges membership == the 9 §C.1 kinds via shared constants.
</verification>

<success_criteria>
computeGraphRelevance ports RESEARCH §3 exactly — undirected, unweighted, fixed 25 iters, alpha=0.25, deterministic and reproducible per D-04 — over the full 9-kind RANK_EDGES set, TDD-verified on synthetic subgraphs.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-06-SUMMARY.md` when done.
</output>
