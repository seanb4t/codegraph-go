---
phase: 01-behavioral-parity-explore-node
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/query/node.go
  - internal/query/node_test.go
  - internal/query/render_markdown.go
  - internal/query/render_markdown_test.go
autonomous: true
requirements: [NODE-01, NODE-02, NODE-03, NODE-04]

must_haves:
  truths:
    - "node enumerates ALL exact-name definitions of an overloaded symbol, generated-files-last (D-07: TS's verbatim isGeneratedFile predicate from RESEARCH §7), not just the lowest-Id one"
    - "multi-def output renders the two-line header + full bodies to budget (<=16 defs / 12000 chars) + an overflow list"
    - "file/line narrowing filters the set but NEVER empties it (best-effort hints)"
    - "single-definition node output stays byte-comparable to TS (CLI + MCP)"
  artifacts:
    - "internal/query/node.go (multi-def enumeration, isGeneratedFile predicate, never-empty narrowing)"
    - "internal/query/render_markdown.go (two-line multi-def header + overflow render)"
  key_links:
    - "resolveSymbolNode full-scan (D-03) -> multi-def enumeration base; isGeneratedFile -> primary sort key; lowest-Id -> secondary tie-break"
---

<objective>
Deliver NODE-01/02/03/04: extend `node` from single-definition to full multi-definition enumeration, matching TS's findSymbolMatches (RESEARCH §6-9). Four mechanical, fully-extracted behaviors (low-risk per RESEARCH — port byte-for-byte): (NODE-01) enumerate ALL exact-name defs sorted generated-files-last; (NODE-02) the TWO-LINE header + HARD_CAP=16 / BODY_BUDGET=12000 budget + LIST_CAP=20 overflow list; (NODE-03) file/line narrowing that never empties the set; (NODE-04) single-def output stays byte-comparable (already substantially satisfied — protect it with a regression test).

Independent of the edge-kind work (this is pure same-name enumeration over the node scan) — Wave 1. Reuses resolveSymbolNode's D-03 full-scan + lowest-Id determinism as the resolution base per D-05 (do NOT introduce a parallel resolver).

RESEARCH Pitfall 4 (must obey): the header is TWO separate lines joined by a blank line, NOT one em-dash sentence. Port §8 exactly.

Purpose: closes v0.1's single-symbol blind spot for `node`; the harness (plan 01) already captured the TS multi-def oracle.
Output: extended node.go + render_markdown.go + tests.

Artifacts this plan produces: multi-def enumeration, isGeneratedFile (verbatim regex list), two-line header renderer, never-empty narrowing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-CONTEXT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/query/node.go
@internal/query/render_markdown.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN multi-def enumeration + isGeneratedFile (NODE-01)</name>
  <files>internal/query/node.go, internal/query/node_test.go</files>
  <read_first>
    - internal/query/node.go (resolveNodeForDetail at :99, Node at :132, resolveSourcePath gate) — extend, do not replace
    - internal/query/traverse.go resolveSymbolNode (the D-03 full-scan + lowest-Id base to build on) — via IterateNodes()
    - 01-RESEARCH.md §6 (findSymbolMatches: getNodesByName exact matches, generated-files-last STABLE sort, no secondary tie-break in TS) and §7 (isGeneratedFile verbatim regex list) and Pattern 2 (lowest-Id as documented secondary tie-break)
    - 01-PATTERNS.md "internal/query/node.go (extend)"
  </read_first>
  <behavior>
    - two nodes named "Handler" in different non-generated files -> both returned, stable scan order preserved
    - one node in a *.pb.go file + one in a plain .go file -> plain .go first, generated last
    - single node named "X" -> single-element slice (NODE-04 path unchanged)
    - tie among non-generated -> lowest-Id first (documented secondary key)
  </behavior>
  <action>
    Add a multi-def enumeration path to node.go: when no file/line hint is given, collect ALL nodes whose Name equals the symbol (full IterateNodes scan, the resolveSymbolNode base) instead of returning only the lowest-Id winner. Sort with sort.SliceStable keyed PRIMARY on isGeneratedFile (non-generated first) and SECONDARY on lowest-Id (documented divergence per RESEARCH Pattern 2 — TS's implicit row order is non-deterministic across re-indexes anyway). Add isGeneratedFile(filePath string) bool porting the GENERATED_PATTERNS regex list from RESEARCH §7 VERBATIM (all ~24 patterns: .pb.go, _grpc.pb.go, _mock.go, .generated.[jt]sx?, .min.mjs, _pb2*.py, .g.cs, Grpc.cs, OuterClass.java, .freezed.dart, etc.). Keep the existing single-def resolveNodeForDetail path intact for NODE-04 (add a regression test asserting single-def output is unchanged). Reuse the errors.Is(err, graphstore.ErrNotFound){continue} WR-04 skip for any dangling reads.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestNodeMultiDef|TestIsGeneratedFile' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: node.go declares isGeneratedFile with all patterns from RESEARCH §7 and a multi-def enumeration returning []*schema.Node
    - behavior: overloaded name -> all defs, generated-last; single name -> single def; deterministic order
    - test-command: `go test ./internal/query/ -run 'TestNodeMultiDef|TestIsGeneratedFile' -count=1`
  </acceptance_criteria>
  <done>Multi-def enumeration + verbatim isGeneratedFile land, generated-files-last, lowest-Id secondary, single-def path preserved, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN never-empty file/line narrowing (NODE-03)</name>
  <files>internal/query/node.go, internal/query/node_test.go</files>
  <read_first>
    - 01-RESEARCH.md §9 (narrowing: fileHint endsWith/includes filter only-if-non-empty; lineHint containing-def filter else nearest-by-startLine; final never-assign-empty guard)
    - internal/query/node.go existing resolveNodeForDetail -f handling (the narrowing surface to extend)
  </read_first>
  <behavior>
    - 3 defs, fileHint matches 1 -> that 1
    - fileHint matches 0 -> all 3 retained (never empties)
    - lineHint inside def B's [startLine,endLine] -> B
    - lineHint inside no def -> nearest def by |startLine-lineHint| (exactly 1), never empty
    - both hints absent -> full set (task 1's enumeration)
  </behavior>
  <action>
    Port §9's narrowing into the multi-def path as a PURE IN-MEMORY filter over the already-enumerated node slice (V5/T-01-Path: it must NOT open any new file handle or do a fresh disk read keyed on the raw hint — the file-READ path stays behind resolveSourcePath). Normalize paths (backslash->slash, lowercase), apply the fileHint endsWith-or-includes filter but only replace the working set if the filtered result is non-empty; then apply the lineHint containing-def filter, falling back to nearest-by-startLine (single element) when nothing contains the line; final guard assigns the narrowed slice only when len>0. Write RED tests for all behavior cases first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run TestNarrowNeverEmpty -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: narrowing is a pure filter over the in-memory node slice — no os.ReadFile / resolveSourcePath call keyed on the raw hint inside the narrow step
    - behavior: no hint combination ever yields an empty result set
    - test-command: `go test ./internal/query/ -run TestNarrowNeverEmpty -count=1`
  </acceptance_criteria>
  <done>File/line narrowing filters but never empties the set; stays a pure in-memory filter (no new path-traversal surface); tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED+GREEN two-line multi-def header + budget + overflow render (NODE-02)</name>
  <files>internal/query/render_markdown.go, internal/query/render_markdown_test.go</files>
  <read_first>
    - internal/query/render_markdown.go RenderNode (:87) — extend or add a multi-def renderer in the same fmt.Fprintf-per-line, strings.Builder style
    - 01-RESEARCH.md §8 (two-line header, BODY_BUDGET=12000, HARD_CAP=16, LIST_CAP=20, exact overflow strings) and Pitfall 4 (two lines, NOT one sentence)
    - 01-PATTERNS.md "internal/query/render_markdown.go (extend)" (plain-text/no-ANSI constraint)
  </read_first>
  <behavior>
    - 2 defs -> line1 `**2 definitions named "X"**`, blank line, line2 `Returning 2 in full — pick the one you need (no Read required).`
    - 20 defs (>HARD_CAP) -> 16 full + a `**Other definitions**` list of the rest (capped LIST_CAP=20) + line2 says `Returning 16 in full; 4 more listed below — ...`
    - bodies exceeding BODY_BUDGET -> stop rendering full bodies at the budget (but always render at least the first), rest go to the listed overflow
    - single def -> existing RenderNode output byte-unchanged (NODE-04)
  </behavior>
  <action>
    Add the multi-def render path porting RESEARCH §8 EXACTLY: header line `**N definitions named "X"**`, a BLANK line, then the SEPARATE second line `Returning M in full[; K more listed below] — pick the one you need (no Read required).` (two lines joined by a blank line — Pitfall 4: never one em-dash sentence). Render full bodies with the HARD_CAP=16 / BODY_BUDGET=12000 loop (always render the first body regardless of budget), overflow into a `**Other definitions**` list capped at LIST_CAP=20 with the `- ... +K more` tail and the closing `> Need one of these in full? ...` hint line, all strings verbatim from §8. Keep it plain-text markdown only (no ANSI — Phase 6 archtest enforces later). Ensure the single-def path routes to the existing RenderNode unchanged (NODE-04 regression test).
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestRenderNodeMultiDef|TestRenderNodeSingleDefUnchanged' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: the multi-def renderer emits header and the "Returning M in full" line as two separate lines separated by a blank line; constants HARD_CAP=16, BODY_BUDGET=12000, LIST_CAP=20 present
    - behavior: >16 defs produces exactly 16 full bodies + an overflow list; single-def output is byte-identical to current RenderNode
    - test-command: `go test ./internal/query/ -run 'TestRenderNodeMultiDef|TestRenderNodeSingleDefUnchanged' -count=1`
  </acceptance_criteria>
  <done>Two-line header + budget + overflow render ported verbatim, plain-text, single-def byte-comparable preserved, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| narrowing hint (file/line) -> node filter | Untrusted hint filters an already-resolved in-memory node list |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering / Path Traversal | NODE-03 narrowing | high | mitigate | Narrowing is a pure in-memory filter on the already-resolved node slice — it opens no file handle; the file-READ path stays behind the existing resolveSourcePath/EvalSymlinks gate (T-03-06-Path). Asserted by task 2's acceptance criterion |
| T-01-07 | Denial of Service | multi-def render budget | medium | mitigate | HARD_CAP=16 + BODY_BUDGET=12000 + LIST_CAP=20 bound output size verbatim per TS; overflow is listed, not rendered |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies — stdlib only |
</threat_model>

<verification>
- `go test ./internal/query/ -run 'TestNodeMultiDef|TestIsGeneratedFile|TestNarrowNeverEmpty|TestRenderNodeMultiDef|TestRenderNodeSingleDefUnchanged' -count=1` green.
- `go test ./internal/query/ -count=1` full package green (no regression to single-def).
</verification>

<success_criteria>
`node` enumerates all overloaded defs (generated-last), renders the exact two-line header + budget + overflow, narrows without ever emptying, and keeps single-def output byte-comparable to TS — all TDD-verified.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-04-SUMMARY.md` when done.
</output>
