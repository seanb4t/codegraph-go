---
phase: 01-behavioral-parity-explore-node
plan: 10
type: tdd
wave: 3
depends_on: ["01-07"]
files_modified:
  - internal/query/gather.go
  - internal/query/gather_test.go
autonomous: true
requirements: [EXPL-02, EXPL-03]

must_haves:
  truths:
    - "Test-file nodes are dampened score *= 0.3 unless the query mentions test/spec"
    - "A dominant-directory core-directory boost of +25 applies to results sharing a dominant file's directory prefix"
    - "Multi-term co-occurrence re-rank multiplies score by (1 + matchCount*0.5) when >=2 stem-grouped term groups match, exempting distinctive-identifier exact matches from dampening"
  artifacts:
    - "internal/query/gather.go (H7 test dampening, H8 core-dir boost, H9 multi-term co-occurrence)"
  key_links:
    - "gather channels+merge (plan 07) -> these post-merge re-rankers -> scored candidate set for expansion/scoring"
---

<objective>
Port the three post-merge gather re-rankers — H7, H8, H9 (RESEARCH §C.2, context/index.js) — that run over the merged candidate set from plan 07. These are the boosts/dampeners that shape which candidates dominate before subgraph construction. D-10 full-fidelity requires all three.

Heuristics (constants VERBATIM):
- H7 Test-file dampening: `score *= 0.3` for test-file nodes UNLESS the query mentions test/spec.
- H8 Core-directory boost: if a dominant file holds >=3x the next file's edge count, `+25` to results sharing its directory prefix.
- H9 Multi-term co-occurrence re-rank: stem-grouped term groups; `score *= 1 + matchCount*0.5` when >=2 groups match name/dir; distinctive-identifier exact matches are EXEMPT from the H7 dampening.

Same file as plan 07 (gather.go) but a later wave (07 is Wave 2) — no same-wave conflict. Depends on plan 07's merged candidate structure. Pure functions over the candidate set, TDD against synthetic candidates.

Note the H9 exemption interacts with H7: a distinctive-identifier exact match must NOT be test-dampened. Implement H7 and H9 so the exemption is honored (order-sensitive — see RESEARCH §C.2 note).

Purpose: without these, weakly-connected Test* funcs and off-core files rank too high — directly serving EXPL-03's intent alongside the gate.
Output: extended gather.go + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-behavioral-parity-explore-node/01-RESEARCH.md
@.planning/phases/01-behavioral-parity-explore-node/01-PATTERNS.md
@internal/query/traverse.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN H7 test-file dampening + H8 core-directory boost</name>
  <files>internal/query/gather.go, internal/query/gather_test.go</files>
  <read_first>
    - 01-RESEARCH.md §C.2 rows H7 (score*=0.3 unless query mentions test/spec) and H8 (dominant-file >=3x next -> +25 to same directory prefix)
    - internal/query/gather.go (the merged candidate structure + the shared isTestFile path predicate, both from plan 07 — reuse isTestFile, do not redefine it)
  </read_first>
  <behavior>
    - a test-file node, query has no test/spec token -> score *= 0.3
    - a test-file node, query contains "test" -> no dampening
    - a directory whose dominant file has >=3x the next file's edge count -> +25 to same-prefix results
  </behavior>
  <action>
    Add H7: multiply a candidate's score by 0.3 when its file is a test file (reuse the shared isTestFile predicate from gather.go / plan 07) AND the query does not mention test/spec. Add H8: compute per-file edge counts, detect a dominant file (>=3x the next file's count), and add +25 to every candidate whose file shares the dominant file's directory prefix. Keep both deterministic. Write RED tests asserting the *0.3 and +25 arithmetic and the test/spec exemption first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestGatherTestDampen|TestGatherCoreDirBoost' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: gather.go applies score*=0.3 (guarded by query test/spec mention) and a +25 same-directory boost keyed on the >=3x dominant-file rule
    - behavior: exact arithmetic matches §C.2; test/spec query disables dampening
    - test-command: `go test ./internal/query/ -run 'TestGatherTestDampen|TestGatherCoreDirBoost' -count=1`
  </acceptance_criteria>
  <done>H7 dampening + H8 core-dir boost land with verbatim constants, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN H9 multi-term co-occurrence re-rank + distinctive-identifier exemption</name>
  <files>internal/query/gather.go, internal/query/gather_test.go</files>
  <read_first>
    - 01-RESEARCH.md §C.2 row H9 (stem-grouped term groups; score*=1+matchCount*0.5 when >=2 groups match name/dir; distinctive-identifier exact matches exempt from dampening) and the isDistinctiveIdentifier reference (query-utils.js)
  </read_first>
  <behavior>
    - a candidate whose name/dir matches 3 stem-grouped term groups -> score *= 1 + 3*0.5 = 2.5
    - a candidate matching <2 groups -> no multiplier
    - a distinctive-identifier exact match that is also a test file -> NOT dampened by H7 (exemption honored)
  </behavior>
  <action>
    Add H9: group query terms by stem, count how many distinct groups a candidate matches across its name and directory, and multiply score by `1 + matchCount*0.5` when >=2 groups match. Implement the distinctive-identifier exemption: a candidate that is an exact distinctive-identifier match is exempt from H7's test dampening — ensure H7 and H9 compose so the exemption wins (apply the exemption flag before/around the dampening step per RESEARCH §C.2). Port isDistinctiveIdentifier's rule from query-utils.js. Write RED tests for the 2.5x arithmetic, the <2-group no-op, and the exemption-beats-dampening case first.
  </action>
  <verify>
    <automated>go test ./internal/query/ -run 'TestGatherMultiTermReRank|TestDistinctiveIdentifierExemption' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - source: gather.go applies *1+matchCount*0.5 for >=2 stem-grouped groups and exempts distinctive-identifier exact matches from H7
    - behavior: exact arithmetic matches §C.2; exemption prevents test-dampening of a distinctive exact match
    - test-command: `go test ./internal/query/ -run 'TestGatherMultiTermReRank|TestDistinctiveIdentifierExemption' -count=1`
  </acceptance_criteria>
  <done>H9 multi-term re-rank + distinctive-identifier exemption land with verbatim constants and correct composition with H7; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| merged candidates -> re-rankers | In-memory re-scoring of a bounded candidate set |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Denial of Service | re-rank over candidate set | low | accept | Operates on the already-bounded merged candidate set (searchLimit-capped upstream); pure arithmetic, no unbounded work |
| T-01-SC | Tampering | package installs | low | accept | No new dependencies |
</threat_model>

<verification>
- `go test ./internal/query/ -run 'TestGatherTestDampen|TestGatherCoreDirBoost|TestGatherMultiTermReRank|TestDistinctiveIdentifierExemption' -count=1` green.
- Arithmetic matches §C.2 exactly.
</verification>

<success_criteria>
The three post-merge re-rankers (H7-H9) are ported with verbatim constants and correct H7/H9 exemption composition, TDD-verified.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-parity-explore-node/01-10-SUMMARY.md` when done.
</output>
