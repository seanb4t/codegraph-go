---
phase: 05-git-sync-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/fsatomic/fsatomic.go
  - internal/fsatomic/fsatomic_test.go
  - internal/agents/shared.go
autonomous: true
requirements: [HOOK-01, HOOK-02]

must_haves:
  truths:
    - "internal/fsatomic.WriteFile writes content via a temp file in the target directory followed by os.Rename, so a crash mid-write never leaves a truncated or corrupted hook/config file on disk"
    - "Writing to a path that already exists preserves that file's existing permission bits; writing to a new path uses 0644"
    - "internal/agents' existing install/uninstall byte-invariance tests stay green unmodified after atomicWriteFile is rewired to delegate to fsatomic.WriteFile"
  artifacts:
    - internal/fsatomic/fsatomic.go
    - internal/fsatomic/fsatomic_test.go
  key_links:
    - "internal/agents/shared.go atomicWriteFile -> internal/fsatomic.WriteFile (behavioral no-op delegation)"
  prohibitions:
    - statement: "Do NOT extract the marker-splice helpers (replaceOrAppendMarkedSection / removeMarkedSection, internal/agents/shared.go:194-295) into fsatomic — D-09 narrows the extraction to the atomic write ONLY"
      status: flagged
      verification: flagged-unverified
    - statement: "Do NOT alter the on-disk byte result or mode-preservation behavior of the extracted write — only crash-safety differs from a plain write; the agents byte-invariance tests must pass unmodified"
      status: flagged
      verification: flagged-unverified
---

<objective>
Extract the production-hardened atomic-write primitive out of internal/agents/shared.go into a new internal/fsatomic package (D-09), then rewire internal/agents to consume it with zero behavior change. This is the shared crash-safe write seam that internal/githooks (05-03) will funnel every hook-file write through per D-05.

Purpose: One correct copy of the temp-file + rename crash-safety logic, imported by both the agent installer and the git-hooks installer, so the two never drift.
Output: internal/fsatomic/fsatomic.go (exported WriteFile), its test file, and a behaviorally-identical internal/agents/shared.go.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-git-sync-hooks/05-CONTEXT.md
@.planning/phases/05-git-sync-hooks/05-RESEARCH.md
@.planning/phases/05-git-sync-hooks/05-PATTERNS.md
@internal/agents/shared.go
</context>

## Artifacts this phase produces

This plan creates these NEW symbols (excluded from drift/source-grounding checks):
- Package `internal/fsatomic`
- `fsatomic.WriteFile(path, content string) error` — exported extraction of `internal/agents/shared.go`'s `atomicWriteFile`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create internal/fsatomic package with WriteFile</name>
  <files>internal/fsatomic/fsatomic.go, internal/fsatomic/fsatomic_test.go</files>
  <read_first>
    - internal/agents/shared.go (the atomicWriteFile body at lines 312-360 — the exact extraction target, and the surrounding imports)
    - .planning/phases/05-git-sync-hooks/05-PATTERNS.md (the "internal/fsatomic/fsatomic.go" section — the ready-to-copy WriteFile body and package-doc rationale)
    - .planning/phases/05-git-sync-hooks/05-RESEARCH.md ("The Go extraction target" code example, D-09 narrowing)
  </read_first>
  <behavior>
    - WriteFile(path, "content") on a new path creates parent dirs (MkdirAll 0755), writes exact bytes, and the resulting file has mode 0644.
    - WriteFile on an existing path with mode 0600 preserves 0600 after the write (mode of the pre-existing file is read via os.Stat and applied to the temp file before rename).
    - Written content is byte-exact (no trailing newline injected, no transformation).
    - On success the target contains only the full content — the mechanism is temp-file-in-same-dir + os.Rename, so no partial-write state is ever observable at the target path.
    - A failed write cleans up its temp file (no `.codegraph-write-*` leftovers in the target dir).
  </behavior>
  <action>
    Create package internal/fsatomic in internal/fsatomic/fsatomic.go with a single exported function `WriteFile(path, content string) error`, whose body is the verbatim extraction of internal/agents/shared.go's `atomicWriteFile` (temp file via os.CreateTemp with prefix `.codegraph-write-*` in filepath.Dir(path), WriteString, Close, mode-resolution via os.Stat defaulting to 0644, os.Chmod, os.Rename, os.Remove cleanup on every error path). Imports are `os` and `path/filepath` only. Add a package doc comment documenting the D-09 narrowing: this package extracts the atomic-write primitive ONLY; the marker-splice helpers stay in internal/agents and are deliberately NOT shared (per D-05/D-09). Write internal/fsatomic/fsatomic_test.go with t.TempDir()-based cases for each behavior above (new-file 0644, existing-file mode preservation, byte-exact content, parent-dir creation, no temp-file leftover on the happy path).
  </action>
  <verify>
    <automated>go test ./internal/fsatomic/... -v</automated>
  </verify>
  <done>internal/fsatomic.WriteFile exists with the exported signature; all fsatomic tests green; mode-preservation and new-file-0644 behaviors both asserted.</done>
  <acceptance_criteria>
    - Source: `internal/fsatomic/fsatomic.go` declares `func WriteFile(path, content string) error` and imports only `os` + `path/filepath`.
    - Behavior: a test writing to a pre-existing 0600 file asserts the file is still 0600 afterward; a test writing to a new file asserts 0644.
    - Behavior: a test asserts written bytes equal the input string exactly.
    - Command: `go test ./internal/fsatomic/... -v` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewire internal/agents to consume fsatomic (zero behavior change)</name>
  <files>internal/agents/shared.go</files>
  <read_first>
    - internal/agents/shared.go (the full file — imports block lines 1-9, atomicWriteFile at 312-360, and every caller: writeJSONFile line 76, replaceOrAppendMarkedSection lines 205/225/237, removeMarkedSection line 291)
    - .planning/phases/05-git-sync-hooks/05-PATTERNS.md ("Rewiring internal/agents/shared.go" — the thin call-through shape)
  </read_first>
  <action>
    Replace the body of `atomicWriteFile(path, content string) error` in internal/agents/shared.go with a single call-through: `return fsatomic.WriteFile(path, content)`. Add `"github.com/seanb4t/codegraph-go/internal/fsatomic"` to the existing import block. Keep the `atomicWriteFile` function name and signature so every existing call site (writeJSONFile, replaceOrAppendMarkedSection, removeMarkedSection) is untouched. Do NOT modify replaceOrAppendMarkedSection or removeMarkedSection (lines 194-295) — those agent-specific splice helpers stay in internal/agents and are explicitly NOT reused by githooks (D-02/D-09). If the `os` or `path/filepath` imports become unused after removing the body, drop them; otherwise leave imports as-is.
  </action>
  <verify>
    <automated>go test ./internal/agents/... && go build ./...</automated>
  </verify>
  <done>internal/agents/shared.go's atomicWriteFile delegates to fsatomic.WriteFile; the entire internal/agents test suite passes unmodified; the module builds.</done>
  <acceptance_criteria>
    - Source: `atomicWriteFile` in internal/agents/shared.go is a one-line delegation to `fsatomic.WriteFile`.
    - Source: `replaceOrAppendMarkedSection` and `removeMarkedSection` bodies are byte-unchanged (no diff to lines 194-295 beyond import adjustments).
    - Command: `go test ./internal/agents/...` exits 0 with no test-file edits.
    - Command: `go build ./...` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → filesystem | fsatomic.WriteFile writes to a caller-supplied path; the path originates from trusted internal callers (agents config paths, later githooks hook-dir paths), not from untrusted network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | fsatomic.WriteFile (partial/corrupt write on crash) | low | mitigate | temp-file-in-same-dir + os.Rename means only whole-file content is ever observable at the target; every error path removes the temp file |
| T-05-04b | Tampering | mode-tightening regression on rewrite | low | mitigate | os.Stat reads and re-applies the existing file's mode before rename, preventing a 0644→0600 silent tightening; asserted by test |

_No package installs in this plan (stdlib os/path only) — no supply-chain (T-05-SC) checkpoint applies._
</threat_model>

<verification>
- `go test ./internal/fsatomic/... -v` — new package behavior.
- `go test ./internal/agents/...` — regression gate: agents byte-invariance unaffected.
- `go build ./...` — module compiles with the new import edge.
</verification>

<success_criteria>
- internal/fsatomic.WriteFile exists, exported, tested for mode-preservation + new-file-0644 + byte-exact content.
- internal/agents/shared.go delegates to it; agents tests pass with zero test-file edits.
- No marker-splice helpers were extracted (D-09 narrowing intact).
</success_criteria>

<output>
Create `.planning/phases/05-git-sync-hooks/05-01-SUMMARY.md` when done.
</output>
