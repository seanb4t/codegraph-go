---
phase: 10-local-build-contribution-and-taskfile-yml-setup
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - Taskfile.yml
  - .github/workflows/release-please.yml
  - internal/upgrade/taskfile_shape_test.go
  - docs/RELEASE-PROCEDURES.md
autonomous: true
requirements: [DEV-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`task check:cross` runs `go list -mod=readonly ./...` for all six release targets and exits non-zero, naming the failing pair, when any one of them cannot resolve."
    - "The six `GOOS/GOARCH` pairs `task check:cross` sweeps are set-equal to the six build targets declared in `.goreleaser.yaml` — a target added to one and not the other fails a test."
    - "`release-please.yml`'s `pretag-gate` job invokes `task check:cross` and no longer carries its own copy of the sweep."
    - "`release-please.yml` still runs on GitHub-hosted `ubuntu-latest` — it is outside the Namespace migration's scope, and gaining the tool bootstrap did not change that."
    - "The job name `pre-tag 6-target go list sanity sweep` is unchanged."
  artifacts:
    - Taskfile.yml
    - .github/workflows/release-please.yml
    - internal/upgrade/taskfile_shape_test.go
    - docs/RELEASE-PROCEDURES.md
  key_links:
    - "`release-please.yml` job `pretag-gate` -> `./.github/actions/install-task` -> `task check:cross`"
    - "`Taskfile.yml` `check:cross` target pair list <-> `.goreleaser.yaml` `builds[].goos`/`goarch` (set equality, test-enforced)"
    - "`pretag-gate` -> `release-please` job (`needs:`) -> tag creation — a bootstrap failure here blocks releases entirely"
  prohibitions:
    - statement: "MUST NOT let a release-gating check become unable to fail — this repo has shipped a gate that passed unconditionally because its condition was inverted, and a gate that has never been observed rejecting a bad input is indistinguishable from one that cannot."
---

<objective>
Give the pre-tag cross-platform sweep exactly one definition and route the release-gating workflow through it.

Purpose: `release-please.yml:46-54` currently carries its own copy of the six-target `go list -mod=readonly` loop. That loop is already a gate — it exists because a linux-only `go.sum` failure reached a v0.1 release candidate. D-15 moves the body into `Taskfile.yml` so a contributor can run the same check before pushing, and so the target list has one home.
Output: a `check:cross` target ported verbatim, a `pretag-gate` job that bootstraps `task` and calls it, a set-equality guard against `.goreleaser.yaml`, and updated release-procedure prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-local-build-contribution-and-taskfile-yml-setup/10-CONTEXT.md
@.planning/phases/10-local-build-contribution-and-taskfile-yml-setup/10-RESEARCH.md
@.planning/phases/10-local-build-contribution-and-taskfile-yml-setup/10-PATTERNS.md
@.planning/phases/10-local-build-contribution-and-taskfile-yml-setup/10-01-SUMMARY.md
@.planning/phases/10-local-build-contribution-and-taskfile-yml-setup/10-02-SUMMARY.md
</context>

<flagged_assumptions>
- **`release-please.yml` is NOT in D-06's Namespace scope, but still needs the tool bootstrap.** D-06 names exactly three files (`ci.yml`, `bench.yml`, `release.yml`); D-15 independently makes this workflow depend on `task`. Those are separate decisions that intersect on one job. `pretag-gate` therefore stays on `ubuntu-latest` and gains an `Install Task` step but no `nscloud-cache-action`.
- **RESEARCH.md Open Question 2 — cold tool build on every push to `main`.** This job runs on every push to `main`, not only on release pushes, and will now build `task` from the module proxy each time without Namespace's cache. Task 2 measures the added wall-clock cost and acts on the measurement rather than guessing.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Port the six-target sweep into a check:cross target, verbatim</name>
  <files>Taskfile.yml</files>
  <read_first>
    - `.github/workflows/release-please.yml` lines 25-55 (the job, its preceding comment about why it runs on every push to `main`, and the exact sweep body)
    - `.goreleaser.yaml` lines 39-125 (the six `builds:` entries, each declaring `goos`/`goarch` in inline flow-sequence form)
    - `Taskfile.yml` (state after plan 10-02)
    - `.planning/phases/10-local-build-contribution-and-taskfile-yml-setup/10-RESEARCH.md` "Code Examples" -> the `check:cross` example
  </read_first>
  <action>
Add a `check:cross` target to `Taskfile.yml` whose command body is the sweep from `release-please.yml` lines 47-54, **ported character-for-character**: `set -euo pipefail`, a `for pair in linux/amd64 linux/arm64 windows/amd64 windows/arm64 darwin/amd64 darwin/arm64` loop, each iteration setting `GOOS`/`GOARCH` from the pair and running `go list -mod=readonly ./...` with output discarded, and on failure emitting a GitHub-annotation error line naming the failing `GOOS`/`GOARCH` before `exit 1`.

Do not rewrite the loop "more cleanly" in Taskfile syntax, do not replace the `for` loop with a task matrix, and do not reorder the pairs. The whole point of D-15 is that the behaviour is unchanged and only the definition site moves; reimplementing it is the one way to silently change the target list.

Give it a `desc:` naming it as the pre-tag sanity sweep and stating that it is the same check `release-please.yml` runs before a tag can be created.
  </action>
  <acceptance_criteria>
    - `task check:cross` exits 0 on the current tree.
    - **Non-vacuity (RED then GREEN):** temporarily introduce a build-constraint or import that cannot resolve for one non-host target (for example a file with a `//go:build windows` constraint importing a package that does not build for windows), confirm by reading the file back that the mutation landed, run `task check:cross` and observe a non-zero exit whose output names that specific `GOOS`/`GOARCH` pair — not a generic failure; revert and observe exit 0. Capture the exit code from the command itself, never from the tail of a pipeline.
    - The target's command text, with comment lines stripped, contains all six pair literals in the same order as `release-please.yml`'s original loop. Compare the extracted pair list against the original as one sorted string, so both an omission and an addition fail.
    - `go build ./...` and `go test ./internal/upgrade/` still pass (no regression from the Taskfile edit).
  </acceptance_criteria>
  <verify>
    <automated>GOWORK=off go tool -modfile=go.tool.mod task check:cross</automated>
  </verify>
  <done>The sweep exists once, in `Taskfile.yml`, byte-faithful to the workflow body it will replace, and has been observed rejecting a genuinely unresolvable target.</done>
</task>

<task type="auto">
  <name>Task 2: Route release-please.yml's pretag-gate through task check:cross</name>
  <reversibility rating="costly">`pretag-gate` is a `needs:` dependency of the `release-please` job, so it gates tag creation; a bootstrap failure here blocks releases entirely until reverted.</reversibility>
  <files>.github/workflows/release-please.yml, docs/RELEASE-PROCEDURES.md</files>
  <read_first>
    - `.github/workflows/release-please.yml` lines 25-60 (the job and the `needs: pretag-gate` relationship below it)
    - `.github/actions/install-task/action.yml` (the composite action being wired in)
    - `Taskfile.yml` `check:cross` (as written in Task 1)
  </read_first>
  <action>
Edit `.github/workflows/release-please.yml`, `pretag-gate` job only:

- Keep `runs-on: ubuntu-latest` unchanged. Add a comment stating this workflow is deliberately outside the Namespace migration's scope, so a future reader does not treat the inconsistency with `ci.yml` as an oversight.
- Keep the job key `pretag-gate` and the `name:` string `pre-tag 6-target go list sanity sweep` byte-identical, and keep the multi-line comment above the job verbatim.
- After `Set up Go`, insert a step `Install Task` using `./.github/actions/install-task`.
- Replace the `6-target go list -mod=readonly sweep` step's eight-line body with the single line `task check:cross`, renaming the step to `Run check:cross sweep`.
- Do not add `nscloud-cache-action` here.

Then measure: run the workflow on the branch and read the `Install Task` step's duration from the run's timing. Record it in the SUMMARY. If that step exceeds 60 seconds, add an `actions/cache` step (SHA-pinned with a fully-qualified tag comment, per this repo's convention) keyed on the hash of `go.tool.sum`, restoring the Go module and build caches, placed before `Install Task`. If it is under 60 seconds, record the measurement and add nothing — RESEARCH.md flagged this as a measure-then-decide item, not a guess.

Finally, update `docs/RELEASE-PROCEDURES.md` section 1 so its description of the pre-tag sweep names `task check:cross` as the invocation and tells a maintainer they can run the same check locally. Change only the invocation prose; the procedure itself is unchanged.
  </action>
  <acceptance_criteria>
    - `task lint:actions` exits 0 against the edited `release-please.yml`.
    - The `pretag-gate` job's `run:` bodies, with comments stripped, consist of exactly one line: `task check:cross`.
    - The job `name:` set in `release-please.yml` is set-equal before and after the edit.
    - `runs-on` for `pretag-gate` still reads `ubuntu-latest`, and the file contains no `namespace-profile-` label.
    - A pushed run shows `pre-tag 6-target go list sanity sweep` green, containing an `Install Task` step and a `Run check:cross sweep` step.
    - **The gate still gates (RED then GREEN):** push a branch carrying the same unresolvable-target mutation used in Task 1, confirm the mutation is present in the pushed tree, and confirm `pretag-gate` fails at the `Run check:cross sweep` step; revert and confirm green. Without this, the sweep's move from inline bash to a task call is unverified as still-functional.
    - The `Install Task` step duration is recorded in the SUMMARY, with the caching decision and its threshold stated.
    - `docs/RELEASE-PROCEDURES.md` section 1 names `task check:cross`; no other section is modified.
  </acceptance_criteria>
  <verify>
    <automated>GOWORK=off go tool -modfile=go.tool.mod task lint:actions</automated>
  </verify>
  <done>The release-gating sweep has one definition, is invoked through it, still runs on GitHub-hosted infrastructure, and has been observed failing on a genuinely broken target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard that check:cross and .goreleaser.yaml enumerate the same six targets</name>
  <files>internal/upgrade/taskfile_shape_test.go</files>
  <read_first>
    - `internal/upgrade/taskfile_shape_test.go` (the `parseX`/`mustX` convention and the parsers already written in plans 10-01 and 10-02)
    - `.goreleaser.yaml` lines 39-125 — note that `goos`/`goarch` are declared in **inline flow-sequence** form (`goos: [linux]`), not block-sequence form. A regex written for block style matches nothing here and produces a test that silently passes without asserting anything; this repo has already shipped one mutation test that no-opped for exactly this reason.
    - `Taskfile.yml` `check:cross` (as written in Task 1)
  </read_first>
  <behavior>
    - `TestCheckCrossMatchesGoreleaserTargets`: the set of `GOOS/GOARCH` pairs extracted from `Taskfile.yml`'s `check:cross` command body equals the set of `{goos, goarch}` pairs across `.goreleaser.yaml`'s `builds` entries. Compared as sorted set equality, so a pair present in one and absent from the other fails in either direction, with the offending pair named.
    - Parsing a `.goreleaser.yaml` with zero `builds` entries returns a non-nil error, never an empty-set match against an empty sweep.
    - Parsing a `Taskfile.yml` with no `check:cross` target returns a non-nil error naming the missing target.
  </behavior>
  <action>
Extend `internal/upgrade/taskfile_shape_test.go` with `TestCheckCrossMatchesGoreleaserTargets`, following the file's `parseX`/`mustX` convention (non-nil error, never a usable zero value).

Parse `.goreleaser.yaml` with a real YAML decoder so both inline and block sequence forms are handled — do not pattern-match the raw text for a sequence marker. Extract pairs from the Taskfile side from the target's command text.

Assert set equality of the two pair sets as a single sorted-string comparison, which carries "no more" and "no fewer" in one check.
  </action>
  <acceptance_criteria>
    - `go test ./internal/upgrade/ -run TestCheckCrossMatchesGoreleaserTargets -v` passes.
    - **Non-vacuity in both directions (RED then GREEN):** (a) temporarily delete one pair from `check:cross`, confirm the edit landed, observe a failure naming that pair; (b) restore, then temporarily add a seventh `builds` entry with a new `goos`/`goarch` to `.goreleaser.yaml`, confirm the edit landed, observe a failure naming it; restore and observe a pass.
    - **The parser is proven to actually read the file's real syntax:** a unit case feeds the parser a fixture in inline flow-sequence form (`goos: [linux]`) and asserts it yields a non-empty pair set. A parser that returns an empty set here would make the whole comparison vacuous.
    - Every new parser has a case feeding it empty input and asserting `err != nil`.
    - `go test ./internal/upgrade/...` passes as a whole.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/upgrade/ -run TestCheckCrossMatchesGoreleaserTargets</automated>
  </verify>
  <done>Adding a release target to `.goreleaser.yaml` without adding it to the pre-tag sweep now fails a test, and the guard has been observed failing in both directions and proven to parse the file's actual syntax.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pretag-gate` -> tag creation | This job is a `needs:` dependency of the job that creates release tags; its verdict decides whether a release can proceed |
| Tool bootstrap -> release-gating workflow | A release-critical workflow newly depends on building `task` from the module proxy |
| `.goreleaser.yaml` target list -> pre-tag sweep | Two independently-edited files must enumerate the same set, or a release target ships unswept |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-01 | Tampering | `pretag-gate` newly depending on a module-proxy tool build | high | mitigate | `task` is built from `go.tool.mod` with `go.tool.sum` checksum-database verification — the same trust root every other dependency in this repo uses — rather than from a release CDN. A bootstrap failure fails the job loudly and blocks tag creation; it cannot silently pass. |
| T-10-05-02 | Repudiation | The moved sweep becoming a gate that cannot fail | high | mitigate | Task 1 and Task 2 each require a demonstrated RED against a genuinely unresolvable target, once locally and once in CI. Task 3's set-equality guard is demonstrated failing in both directions. |
| T-10-05-03 | Tampering | Divergence between the sweep's target list and the release build's target list | high | mitigate | `TestCheckCrossMatchesGoreleaserTargets` enforces set equality with a real YAML parser, and includes an explicit case proving the parser reads this file's inline sequence syntax — closing the failure mode where a syntax-mismatched matcher makes the comparison silently vacuous. |
| T-10-05-04 | Denial of Service | Cold tool build on every push to `main` | low | mitigate | Measured rather than assumed; caching added only if the measurement exceeds the stated threshold, keyed on `go.tool.sum`. |
| T-10-05-05 | Elevation of Privilege | `release-please.yml` runner scope | low | accept | The workflow's runner class and permissions are unchanged by this plan; only a build step is added to an existing job. |
| T-10-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no npm, pip, or cargo package; `task` resolves through the Go module proxy and was audited in RESEARCH.md's Package Legitimacy Audit (verdict `OK`). |
</threat_model>

<artifacts_this_phase_produces>
Symbols created by Phase 10 (excluded from drift verification):

**Files (new):** `Taskfile.yml`, `go.tool.mod`, `go.tool.sum`, `go.tool-lint.mod`, `go.tool-lint.sum`, `.github/actions/install-task/action.yml`, `internal/upgrade/taskfile_shape_test.go`

**Task target names:** `default`, `build`, `build:release`, `test`, `test:unit`, `test:golden`, `test:integration`, `test:daemon`, `test:race`, `lint`, `vet`, `vet:windows`, `vet:daemon-windows`, `lint:actions`, `vuln`, `check:goreleaser`, `check:cross`, `check:reproducibility`, `check:reproducibility:arm64`, `bench:regression`

**Taskfile variables:** `GO_TOOL`, `GO_TOOL_LINT`

**Composite action:** `./.github/actions/install-task`

**New CI step names:** `Cache Go modules and build`, `Install Task`, `Run goreleaser check`, `Run actionlint`, `Vet (go vet ./...)`, `Run check:cross sweep`

**New Go symbols:** `internal/bench.Metrics.Runner`; `tools/bench/runner` flag `-runner` and env `CODEGRAPH_BENCH_RUNNER`; `TestRequiredCheckNamesPreserved`, `TestToolModfilesRemainIsolated`, `TestTaskfileGatesFailLoud`, `TestTaskfileWrapperIsSerial`, `TestDarwinLegsBuildNatively`, `TestProvenanceJobUsesTaggedSLSAGenerator`, `TestCheckCrossMatchesGoreleaserTargets`, `TestWorkflowRunBodiesInvokeTask`, `TestCheckRegressionRunnerMismatch`

**New data key:** `tools/bench/baseline.json` -> `runner`
</artifacts_this_phase_produces>

<verification>
- `task check:cross` exits 0 locally and non-zero against a deliberately unresolvable target, both observed.
- `pretag-gate` is green in a real run, with `Install Task` and `Run check:cross sweep` steps, still on `ubuntu-latest`.
- `go test ./internal/upgrade/` passes, including the set-equality guard demonstrated failing in both directions.
- `docs/RELEASE-PROCEDURES.md` section 1 names the new invocation.
</verification>

<success_criteria>
The pre-tag sweep has exactly one definition, a contributor can run it before pushing, and the release-gating workflow calls it — with the gate proven to still reject a genuinely broken cross-target.
</success_criteria>

<output>
Create `.planning/phases/10-local-build-contribution-and-taskfile-yml-setup/10-05-SUMMARY.md` when done.
</output>
