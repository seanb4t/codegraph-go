---
phase: 09-release-please-and-goreleaser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/upgrade/release_workflow_shape_test.go
  - release-please-config.json
  - .release-please-manifest.json
  - .github/workflows/release-please.yml
autonomous: true
requirements: [REL-02]
user_setup:
  - service: github-app
    why: "release-please must push the vX.Y.Z tag with a GitHub App installation token — tags pushed with the default GITHUB_TOKEN do not start other workflow runs, so release.yml would never fire (D-02). The setup itself is plan 09-05's blocking human checkpoint; it is declared here because release-please.yml references the secret names."
    env_vars:
      - name: APP_ID
        source: "GitHub -> Settings -> Developer settings -> GitHub Apps -> <app> -> About -> App ID"
      - name: APP_PRIVATE_KEY
        source: "Same page -> Private keys -> Generate a private key; store the full PEM contents"
    dashboard_config:
      - task: "Install the App on seanb4t/codegraph-go with installation permissions Contents=Read&write, Pull requests=Read&write, Issues=Read&write"
        location: "GitHub App settings -> Install App -> seanb4t/codegraph-go"

must_haves:
  truths:
    - "release.yml's literal on-disk `name:` value and workflow filename, read off disk by a Go test, reconstruct a SAN string that internal/upgrade's releaseWorkflowRefPattern accepts — and the same guard rejects a renamed-workflow SAN and a branch-ref SAN, so it is non-vacuous in both directions"
    - "release.yml's `on:` block has exactly one top-level trigger key (`push`) and exactly one tag pattern (`v[0-9]*`); the guard goes red the instant a second trigger is added, forcing release.yml's header comment and D-03's ref-shape guard to land in the same change (RESEARCH Pitfall 3)"
    - "every workflow-source helper fails the test when its target is absent and never returns a zero value on a miss — the pure parse cores are unit-tested against synthetic YAML for missing key, empty list, and step-without-run-block"
    - "release-please resolves this repo's baseline as 0.1.0 from .release-please-manifest.json plus the already-existing v0.1.0 GitHub Release, with no bootstrap PR, no bootstrap-sha and no last-release-sha key"
    - "release-please-config.json contains no `release-as` key — the 1.0.0 forcing is a one-shot commit footer (D-06), never a sticky config pin that would repeat on every subsequent release"
    - "release-please-config.json contains no `extra-files` entry — ldflags stay the sole build-time version source and no Go file is rewritten by release-please (D-07)"
    - "release-please.yml mints its token with actions/create-github-app-token and passes it to release-please-action's `token:` input, so the tag it creates is App-authored and therefore starts a release.yml run (D-02)"
    - "release-please can never open a release PR from a `main` state that fails the 6-target `go list -mod=readonly` sweep — the sweep is a `needs:`-gating job, and its failure path is proven against a rejecting GOOS/GOARCH pair"
    - "every third-party action referenced by release-please.yml is pinned to a full 40-hex-character commit SHA with the resolved tag in a trailing comment"
    - statement: "an App-token-authored tag push actually starts a release.yml run in this repo's live GitHub Actions configuration"
      verification: backstop
  artifacts:
    - internal/upgrade/release_workflow_shape_test.go
    - release-please-config.json
    - .release-please-manifest.json
    - .github/workflows/release-please.yml
  key_links:
    - "conventional commits on main -> release-please.yml (App token) -> release PR -> merge -> tag vX.Y.Z + GitHub Release created by one createRelease API call -> release.yml's unchanged push.tags trigger -> cosign OIDC SAN -> internal/upgrade/verify.go releaseWorkflowRefPattern"
    - "internal/upgrade/release_workflow_shape_test.go <-> .github/workflows/release.yml — the only mechanical link that turns a filename/trigger drift into a red test instead of a silent `codegraph upgrade` break for every shipped binary"
    - "pretag-gate job -> `needs:` -> release-please job — the automation of docs/RELEASE-PROCEDURES.md §1 now that no human runs the sweep by hand"
  prohibitions:
    - "MUST NOT rename .github/workflows/release.yml, alter its push.tags v[0-9]* trigger, or add a second trigger key to it (D-01 LOCKED contract)"
    - "MUST NOT edit internal/upgrade/verify.go's releaseOIDCIssuer, releaseRepoSlug or releaseWorkflowRefPattern constants"
    - "MUST NOT add a release-as key to release-please-config.json — it takes precedence over the one-shot footer and would pin every future release to the same version"
    - "MUST NOT add internal/version/version.go or any other Go source to release-please extra-files (D-07)"
    - "MUST NOT interpolate a GitHub Actions expression directly into any run: script — workflow context reaches shell only through an env: block"
    - "MUST NOT reference a third-party action by tag or branch; full commit SHA only, with the resolved tag in a trailing comment"
    - "MUST NOT write the App private key anywhere except a GitHub repository secret — not into workflow YAML, not into a committed file, not into a log line"
    - "MUST NOT collapse release-please and the signed build into one workflow (engram's model) — the resulting signature would carry a branch ref and every already-shipped binary would reject it"
---

<objective>
TRACER: stand up the mechanical spine of release-please — config, manifest, and the App-token workflow — and pin the LOCKED release identity to a guard that fails on drift. This is the thinnest path that touches every layer this phase modifies except the live tag push: conventional commits -> release-please config -> release-please workflow -> the tag shape -> `internal/upgrade`'s cosign SAN contract.

Purpose: prove end-to-end, before any irreversible action, that release-please will resolve this repo's real 0.1.0 baseline and that the tag shape it produces still satisfies the contract every shipped binary hard-codes.
Output: a non-vacuous workflow-shape drift guard, `release-please-config.json`, `.release-please-manifest.json`, and `.github/workflows/release-please.yml` with its blocking 6-target pre-tag gate.

## Artifacts this phase produces (whole phase)

**New files**
- `.github/workflows/release-please.yml` (jobs: `pretag-gate`, `release-please`) — plan 09-01
- `release-please-config.json` — plan 09-01
- `.release-please-manifest.json` — plan 09-01
- `internal/upgrade/release_workflow_shape_test.go` — plan 09-01
- `internal/upgrade/release_publish_step_test.go` — plan 09-02
- `internal/upgrade/pr_title_lint_test.go` — plan 09-03
- `.github/workflows/pr-title.yml` (job: `pr-title`) — plan 09-03

**New Go test functions**
- `TestReleaseWorkflowFileMatchesPattern` — plan 09-01
- `TestReleaseWorkflowTriggerIsTagPushOnly` — plan 09-01
- `TestWorkflowSourceHelpersFailLoudly` — plan 09-01
- `TestPublishReleaseStepBranches` — plan 09-02
- `TestPRTitleLintAcceptsAndRejects` — plan 09-03

**New Go test helpers (package `upgrade`, test-only)**
- `parseWorkflowTopLevelName` / `mustWorkflowTopLevelName` — plan 09-01
- `parseWorkflowOnKeys` / `mustWorkflowOnKeys` — plan 09-01
- `parseWorkflowPushTagPatterns` / `mustWorkflowPushTagPatterns` — plan 09-01
- `parseWorkflowStepRunBlock` / `mustWorkflowStepRunBlock` — plan 09-01 (consumed by 09-02 and 09-03)
- `stubGHDir` / `runExtractedStep` — plan 09-02

**New CI job names**
- `pretag-gate` (in `release-please.yml`) — plan 09-01
- `release-please` (in `release-please.yml`) — plan 09-01
- `pr-title` (in `pr-title.yml`) — plan 09-03
- `actionlint` (in `ci.yml`) — plan 09-03

**Modified files**
- `.github/workflows/release.yml` — one step (`Publish GitHub release`) — plan 09-02
- `.github/workflows/ci.yml` — one new job (`actionlint`) — plan 09-03
- `docs/RELEASE-PROCEDURES.md` — §3/§4/§7 rewritten, §9 and §10 added — plan 09-04
- `.planning/ROADMAP.md` — Phase 9 criterion-3 amendment recorded — plan 09-04
- `.planning/todos/2026-07-14-document-release-cut-procedures-runbook.md` — marked resolved — plan 09-04

**New doc sections**
- `docs/RELEASE-PROCEDURES.md` §9 "GitHub App prerequisite (D-02)" — plan 09-04
- `docs/RELEASE-PROCEDURES.md` §10 "Recorded divergences" (D-05 GoReleaser role, D-08 lint file location, D-03 Path B recipe) — plan 09-04

## Artifacts this plan produces
- `internal/upgrade/release_workflow_shape_test.go` (helpers + 3 test functions)
- `release-please-config.json`
- `.release-please-manifest.json`
- `.github/workflows/release-please.yml`

## Multi-Source Coverage Audit (phase-wide)

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Automated version/changelog/tag management replacing the maintainer-manual tag | 09-01, 09-07 | COVERED | |
| GOAL | — | GoReleaser owns building and uploading artifacts | 09-04 | COVERED | Amended by D-05 — `goreleaser build --single-target` is retained; the amendment is recorded in ROADMAP.md, not applied silently |
| REQ | REL-02 | release-please cuts releases from Conventional Commits and the signed artifacts still satisfy `releaseWorkflowRefPattern` | 09-01..09-07 | COVERED | Half A (automation) 09-01/02/03/07; half B (trust) 09-01 guard + 09-06/09-07 live proof |
| RESEARCH | — | App-token wiring (Pattern 1) | 09-01 | COVERED | |
| RESEARCH | — | Manifest seeding, no bootstrap PR (Pattern 2) | 09-01 | COVERED | |
| RESEARCH | — | `Release-As:` footer precedence (Pattern 3) | 09-07 | COVERED | |
| RESEARCH | — | create-if-absent-else-upload-clobber (Pattern 4) | 09-02 | COVERED | |
| RESEARCH | — | Automated pre-tag 6-target gate (Pattern 5) | 09-01 | COVERED | |
| RESEARCH | — | Pitfall 1 — PR-title lint never re-fires on a title-only edit | 09-03 | COVERED | Resolved to a dedicated workflow file with `types: [opened, edited, synchronize, reopened]`; divergence from D-08's literal "in ci.yml" wording recorded in 09-03 and 09-04 |
| RESEARCH | — | Pitfall 2 — App installation permissions are a different scope from `permissions:` | 09-01, 09-05 | COVERED | |
| RESEARCH | — | Pitfall 3 — release.yml's header comment vs a second trigger | 09-01 | COVERED | `TestReleaseWorkflowTriggerIsTagPushOnly` makes the comment mechanically true |
| RESEARCH | — | Pitfall 4 — disposable test tag resolving as "latest" | 09-06 | COVERED | |
| RESEARCH | — | `actionlint` as a static workflow gate | 09-03 | COVERED | |
| RESEARCH | — | Assumption A1 (App scopes), A2 (prerelease `Release-As:`), A3 (`gh release view` exit-status idiom) | 09-05, 09-06, 09-02 | COVERED | A3 is discharged by 09-02's stubbed-`gh` test |
| CONTEXT | D-01 | SAN / trigger / filename frozen | 09-01 | COVERED | Enforced by two guards, not by prose |
| CONTEXT | D-02 | GitHub App token as the tag-push identity | 09-01, 09-05 | COVERED | |
| CONTEXT | D-03 | `workflow_dispatch` fallback (Path B) | 09-01, 09-04 | COVERED | Deliberately NOT implemented — CONTEXT scopes it to "only if the App is unavailable" and rates it `costly`; the recipe is documented in `docs/RELEASE-PROCEDURES.md` §10 and `TestReleaseWorkflowTriggerIsTagPushOnly` makes implementing it impossible without also updating the header comment and adding its ref-shape guard |
| CONTEXT | D-04 | create-if-absent-else-upload-clobber | 09-02 | COVERED | |
| CONTEXT | D-05 | GoReleaser build-only preserved; criterion 3 divergence recorded | 09-04 | COVERED | |
| CONTEXT | D-06 | Manifest seeded 0.1.0 + one-shot `Release-As: 1.0.0` | 09-01 (seed), 09-07 (footer) | COVERED | |
| CONTEXT | D-07 | ldflags only, no `extra-files` | 09-01 | COVERED | |
| CONTEXT | D-08 | PR-title conventional-commit gate | 09-03 | COVERED | Substance delivered in full; file location diverges from the literal "in ci.yml" wording — recorded, see 09-03 |
| CONTEXT | D-09 | Fast-forward merge preserving history | 09-07 | COVERED | `checkpoint:decision` gate (one-way) |
| CONTEXT | D-10 | Stable-only automation, manual `rc` path retained | 09-02, 09-04 | COVERED | The create branch is what preserves it |
| CONTEXT | — | Folded todo: `docs/RELEASE-PROCEDURES.md` update | 09-04 | COVERED | |

All rows COVERED. No `⚠ MISSING` rows.

## Spec-less edge-probe disposition (3 probe items, 0 silent drops)

| Probe category | Domain mapping | Disposition |
|---|---|---|
| `adjacency` — when two things just touch, do they merge, collide, or separate? | release-please's Release object and release.yml's asset publish touch on the same tag | RESOLVED into `must_haves.truths` in plan 09-02 (they merge, via `upload --clobber`) |
| `ordering` — when elements compare equal, is output order specified and stable? | Does the tag ref exist before the Release object, letting `assemble` take the create branch and lose the changelog body? | RESOLVED into `must_haves.truths` in plan 09-02 — verified from release-please's own source: the tag is created *by* the `repos.createRelease` API call (`tag_name` + `target_commitish`), so the Release object exists at the instant the tag ref does; there is no window in which the tag exists and the Release does not |
| `empty` — what is the result for empty / single / null input? | (a) zero release-worthy commits since the last release; (b) the asset glob matches nothing | (a) RESOLVED into a documented truth + runbook line in plan 09-04; (b) RESOLVED into a hard guard + a dedicated stubbed-`gh` test case in plan 09-02 |

3 probe items surfaced = 3 authored into `must_haves` + 0 flagged assumptions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-please-and-goreleaser/09-CONTEXT.md
@.planning/phases/09-release-please-and-goreleaser/09-RESEARCH.md
@.planning/phases/09-release-please-and-goreleaser/09-PATTERNS.md
@.github/workflows/release.yml
@internal/upgrade/verify.go
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drift guard + release-please config/manifest/workflow (end-to-end spine, one path)</name>
  <files>internal/upgrade/release_workflow_shape_test.go, release-please-config.json, .release-please-manifest.json, .github/workflows/release-please.yml</files>
  <read_first>
    - .planning/phases/09-release-please-and-goreleaser/09-PATTERNS.md — the SHA-pin, env-indirection, `set -euo pipefail`, and "minimal top-level permissions with a per-job why-comment" conventions, plus the verbatim accept/reject test pair being mirrored
    - .github/workflows/release.yml — the LOCKED file this guard reads: header comment lines 1-36, `name:` line 38, `on.push.tags` lines 40-43, and the `permissions:` shape at lines 45-46 / 196-201
    - internal/upgrade/verify.go lines 20-45 — releaseOIDCIssuer, releaseRepoSlug, releaseWorkflowRefPattern (the constants the guard compiles against; same package, no export needed)
    - internal/upgrade/verify_test.go lines 114-137 — TestReleaseWorkflowRefPattern_RejectsNonReleaseWorkflowInSameRepo / _AcceptsReleaseWorkflowTagRef, the exact accept/reject shape to mirror
    - internal/upgrade/verify_release_e2e_test.go — the same package's existing release-contract guard: pinned literals held independently of the implementation so a shared bug cannot hide
    - .planning/phases/09-release-please-and-goreleaser/09-RESEARCH.md — Architecture Patterns 1 and 2, plus the Code Examples section
  </read_first>
  <behavior>
    - TestReleaseWorkflowFileMatchesPattern: reading the real release.yml off disk and reconstructing the SAN a tag push would produce yields a string that releaseWorkflowRefPattern matches.
    - TestReleaseWorkflowFileMatchesPattern (reject 1): a SAN naming a different workflow filename in the same repo at a tag ref does NOT match.
    - TestReleaseWorkflowFileMatchesPattern (reject 2): a SAN naming release.yml at a branch ref does NOT match.
    - TestReleaseWorkflowTriggerIsTagPushOnly: release.yml's `on:` block yields exactly one top-level key, and exactly one tag pattern.
    - TestWorkflowSourceHelpersFailLoudly: each pure parse core returns a non-nil error (never a usable zero value) for synthetic sources with a missing top-level key, an empty tag list, and a named step that has no run block.
  </behavior>
  <action>
    Write the test file FIRST, demonstrate it red against a mutated release.yml, restore, then create the three release-please files.

    (a) Create `internal/upgrade/release_workflow_shape_test.go` (`package upgrade`, test-only). Define the workflow-source helpers as PAIRS: a pure `parseX(src string) (T, error)` core plus a thin `mustX(t *testing.T, src string) T` wrapper that calls `t.Fatalf` on error. Cores: `parseWorkflowTopLevelName` (the value of the column-0 `name:` key), `parseWorkflowOnKeys` (the two-space-indented keys directly under the column-0 `on:` key), `parseWorkflowPushTagPatterns` (the list items under `on:` -> `push:` -> `tags:`, quote-stripped), and `parseWorkflowStepRunBlock(src, stepName)` (locate the `- name: <stepName>` step, return that step's `run:` block body with its common leading indentation removed). Every core MUST return an error rather than a usable zero value when its target is absent — a silently-empty return is the exact defect class (`CR-01`) this phase exists to stop. Keep `parseWorkflowStepRunBlock` general: it is consumed unchanged by plans 09-02 and 09-03. Do not add a YAML library dependency — hand-rolled line scanning matches this package's existing self-contained style (see `TestVerifyGo_NoExecUsage`).

    (b) `TestReleaseWorkflowFileMatchesPattern`: define a package-level path constant for `../../.github/workflows/release.yml`; read it; assert `mustWorkflowTopLevelName` equals the literal `release`; build the SAN as `"https://github.com/" + releaseRepoSlug + "/.github/workflows/" + filepath.Base(<the path constant>) + "@refs/tags/v1.2.3"`; assert `regexp.MustCompile(releaseWorkflowRefPattern).MatchString(san)` is true. Add two reject subtests using the same compiled regexp: one substituting the basename `release-please.yml`, one substituting the ref `refs/heads/main`. Both must not match. Failure messages must print the reconstructed SAN so a real drift is diagnosable from the test output alone.

    (c) `TestReleaseWorkflowTriggerIsTagPushOnly`: assert `mustWorkflowOnKeys` returns exactly one element, `push`, and `mustWorkflowPushTagPatterns` returns exactly one element equal to the literal tag glob release.yml already uses. Document in the test's doc comment that this is the mechanical enforcement of release.yml's header claim that it triggers only on tag pushes, and that D-03's fallback trigger cannot be added without this test going red.

    (d) `TestWorkflowSourceHelpersFailLoudly`: table-driven, against short synthetic YAML strings held in the test — no disk reads. One case per core proving an error is returned for: no column-0 `name:` key; an `on:` block with no `push:` child; a `tags:` list with zero items; a step name that exists with no run block; a step name that does not exist at all.

    (e) Demonstrate non-vacuity and record the observed output in the SUMMARY: edit release.yml's `name:` value to any other string, run the guard, observe TestReleaseWorkflowFileMatchesPattern fail with the reconstructed SAN in the message, restore. Then add a second key under release.yml's `on:` block, run, observe TestReleaseWorkflowTriggerIsTagPushOnly fail, restore. Confirm `git diff -- .github/workflows/release.yml` is empty before moving on.

    (f) Create `release-please-config.json` at repo root with exactly four keys: `$schema` pointing at release-please's published config schema URL, `release-type` set to `go`, `include-component-in-tag` set to false (guarantees the tag stays `v<version>`, matching the existing v0.1.0 / v0.0.0-rc.3 shape), and `packages` mapping `.` to an empty object. Do not add a version-forcing key and do not add a file-rewriting key — the version forcing is a one-shot commit footer in plan 09-07 and the build-time version stays ldflags-only. Do not override `changelog-sections`: release-please's defaults already hide `docs`/`chore`/`ci`/`test`, which is what filters roughly 217 planning commits out of the v1.0.0 changelog while keeping the ~160 `feat`/`fix`/`perf` entries.

    (g) Create `.release-please-manifest.json` at repo root mapping `.` to the string `0.1.0` (D-06 — the repo's real newest release tag). Do NOT add `bootstrap-sha` or `last-release-sha`: this repo already has a real GitHub Release object for `v0.1.0` at commit `5bce6bf8c690d9b5afcc05f0b8fcceab3147555f`, so release-please's manifest-mode SHA discovery finds it directly.

    (h) Create `.github/workflows/release-please.yml`. Open with a header comment in release.yml's style stating: this file exists solely to produce the App-authored tag push that release.yml's LOCKED trigger consumes; it MUST NOT build, sign, or publish anything itself; and the App token (not the default token) is load-bearing because default-token-authored refs do not start other workflow runs. Then `name: release-please`; `on:` with `push:` -> `branches: [main]` only; top-level `permissions:` granting read access to contents only. One job named `release-please` on ubuntu-latest with job-level `permissions:` granting write to contents, issues and pull-requests, each carrying an inline comment naming what needs it — contents for tag and release creation, pull-requests for the release PR, issues for the `autorelease: pending` / `autorelease: tagged` PR labels which GitHub governs under the Issues scope — plus a note that this block governs the default token and is a DIFFERENT scope from the App's own installation permissions (RESEARCH Pitfall 2). Steps: `actions/create-github-app-token` with `id: app-token`, `app-id` wired to the `APP_ID` secret and `private-key` wired to the `APP_PRIVATE_KEY` secret (D-02's literal secret names), carrying an inline note that `app-id` is deprecated in favour of `client-id` and that migrating requires re-seeding the secret with the App's Client ID rather than its numeric App ID; then `googleapis/release-please-action` with `id: release`, `token` wired to the app-token step's `token` output, `config-file` set to `release-please-config.json` and `manifest-file` set to `.release-please-manifest.json`.

    (i) Action SHA pins. Use `actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1` with a trailing `# v3.2.0` comment and `googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7` with a trailing `# v5.0.0` comment. **CORRECTION TO 09-RESEARCH.md:** its Code Examples section pins release-please-action at `8b8fd2cc23b2e18957157a9d923d75aa0c6f6ad5`, which does not exist in that repository — `gh api repos/googleapis/release-please-action/commits/8b8fd2cc23b2e18957157a9d923d75aa0c6f6ad5` returns HTTP 422 "No commit found for SHA". The correct v5.0.0 commit is `45996ed1f6d02564a971a2fa1b5860e934307cf7` ("chore(main): release 5.0.0 (#1200)"). Re-resolve both pins with `gh api repos/<owner>/<repo>/git/ref/tags/<tag> --jq .object.sha` before writing them and use whatever that returns; if a resolved SHA differs from the value above, record the delta in the SUMMARY rather than silently substituting.
  </action>
  <verify>
    <automated>go test ./internal/upgrade/ -run 'TestReleaseWorkflow|TestWorkflowSourceHelpers' -count=1 -v && jq -e '."release-type"=="go" and ."include-component-in-tag"==false and (has("release-as")|not) and (has("extra-files")|not) and (.packages|has("."))' release-please-config.json && jq -e '.["."]=="0.1.0" and (has("bootstrap-sha")|not)' .release-please-manifest.json && actionlint .github/workflows/release-please.yml</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/upgrade/ -run TestReleaseWorkflowFileMatchesPattern -count=1 -v` exits 0 and its output shows the accept subtest and both reject subtests running (three named subtests, not one).
    - Non-vacuity (drift guard): with release.yml's `name:` value changed to any other string, `go test ./internal/upgrade/ -run TestReleaseWorkflowFileMatchesPattern -count=1` exits non-zero and the failure message contains the reconstructed SAN string; after restoring release.yml, the same command exits 0. Both observed outputs recorded verbatim in the SUMMARY.
    - Non-vacuity (trigger guard): with a second key added under release.yml's `on:` block, `go test ./internal/upgrade/ -run TestReleaseWorkflowTriggerIsTagPushOnly -count=1` exits non-zero; after restoring, it exits 0. Both observed outputs recorded in the SUMMARY.
    - `git diff -- .github/workflows/release.yml` produces no output at task end — the LOCKED file is byte-for-byte unchanged.
    - `go test ./internal/upgrade/ -run TestWorkflowSourceHelpersFailLoudly -count=1` exits 0, covering at least five error cases across the four parse cores.
    - `git diff -- internal/upgrade/verify.go` produces no output — the LOCKED constants are untouched.
    - `jq -e '."release-type"=="go"' release-please-config.json` exits 0; `jq -e 'has("release-as")|not' release-please-config.json` exits 0; `jq -e 'has("extra-files")|not' release-please-config.json` exits 0.
    - `jq -e '.["."]=="0.1.0"' .release-please-manifest.json` exits 0.
    - `grep -Ec 'uses: [^@]+@[0-9a-f]{40} # v' .github/workflows/release-please.yml` returns 2.
    - `actionlint .github/workflows/release-please.yml` exits 0 (CI-pinned equivalent: `go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12`).
    - End-to-end spine proof: `npx -y release-please@latest release-pr --dry-run --debug --repo-url=seanb4t/codegraph-go --config-file=release-please-config.json --manifest-file=.release-please-manifest.json --token="$(gh auth token)"` reports a resolved latest release of `0.1.0` (baseline discovered from the existing GitHub Release, no bootstrap) and prints a candidate next version. Record the resolved baseline and the candidate version verbatim in the SUMMARY. If the command cannot run (no network, npx unavailable, token scope), record that explicitly in the SUMMARY as an UNMET criterion — do not mark it satisfied.
  </acceptance_criteria>
  <reversibility rating="reversible">Config, manifest and one new workflow file; nothing is published from them. D-01's `one-way` rating attaches to the already-shipped SAN, which this task freezes rather than changes — the gate is discharged by the two non-vacuous guards, not by a checkpoint that would re-open a locked decision.</reversibility>
  <done>The release-please spine exists, dry-run-resolves this repo's real 0.1.0 baseline, and release.yml's on-disk shape is mechanically pinned to the LOCKED SAN by a guard proven to fail on drift in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Blocking 6-target pre-tag gate in release-please.yml</name>
  <files>.github/workflows/release-please.yml</files>
  <read_first>
    - docs/RELEASE-PROCEDURES.md §1 lines 11-29 — the exact manual sweep being automated, and the v0.1 `rc.1` linux-only `go.sum` incident that motivates it
    - .github/workflows/release.yml lines 101-120 — the checkout and setup-go SHA pins to reuse verbatim
    - .planning/phases/09-release-please-and-goreleaser/09-RESEARCH.md — Architecture Pattern 5 and Open Question 2
    - .github/workflows/release-please.yml — the file created in Task 1
  </read_first>
  <action>
    Add a job named `pretag-gate` to `.github/workflows/release-please.yml` and make the existing `release-please` job declare `needs: pretag-gate`, so release-please can never open a release PR (and therefore never cut a tag) from a `main` state that fails cross-platform module resolution.

    The job runs on ubuntu-latest and reuses release.yml's already-pinned actions verbatim so no new action enters the supply chain: `actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10` with a trailing `# v6.0.3` comment, then `actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16` with a trailing `# v6.5.0` comment and `go-version-file: go.mod`.

    The sweep step is a multi-line `run:` opening with `set -euo pipefail`, iterating the six pairs `linux/amd64 linux/arm64 windows/amd64 windows/arm64 darwin/amd64 darwin/arm64` and running `go list -mod=readonly ./...` for each with GOOS and GOARCH set from the pair, discarding stdout. Before exiting on a failure, emit a workflow error annotation naming the failing GOOS/GOARCH pair so the failure is diagnosable from the run summary rather than from raw log scrollback. Do not use a matrix — a single sequential loop keeps the whole sweep one required check and matches the runbook's own shape.

    RESEARCH Open Question 2 resolves to gating every push to `main` rather than only "about to release": the sweep is a handful of `go list` calls, and gating only at release time would need a pre-check hook release-please does not expose.
  </action>
  <verify>
    <automated>actionlint .github/workflows/release-please.yml && bash -c 'set -euo pipefail; for pair in linux/amd64 linux/arm64 windows/amd64 windows/arm64 darwin/amd64 darwin/arm64; do GOOS="${pair%/*}" GOARCH="${pair#*/}" go list -mod=readonly ./... > /dev/null; done'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pretag-gate' .github/workflows/release-please.yml` returns at least 2 (the job key and the `needs:` reference).
    - `grep -c 'darwin/arm64' .github/workflows/release-please.yml` returns at least 1, and the six pairs appear together in the loop.
    - All six pairs pass locally: the loop in the verify command exits 0.
    - Non-vacuity (gate failure path): `GOOS=linux GOARCH=bogusarch go list -mod=readonly ./... > /dev/null` exits non-zero, and running the sweep loop with that pair substituted for one of the six aborts the loop with a non-zero exit — proving `set -euo pipefail` propagates the failure rather than logging it and continuing. Record the observed exit status in the SUMMARY.
    - `actionlint .github/workflows/release-please.yml` exits 0.
    - `grep -Ec 'uses: [^@]+@[0-9a-f]{40} # v' .github/workflows/release-please.yml` returns 4 (two from Task 1, two added here).
  </acceptance_criteria>
  <reversibility rating="reversible">A CI job; removable with no artifact impact.</reversibility>
  <done>release-please cannot process a `main` state that fails the 6-target module-resolution sweep, and the gate's failure path is proven against a rejecting input rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub App private key -> workflow runtime | the App's PEM crosses from repo-secret storage into a runner process that mints an installation token |
| release-please workflow -> `main` ref | an automated actor gains write access to `main` (release PR, manifest bump) and to tag refs |
| workflow context (`github.*`) -> `run:` shell | untrusted-shaped values would reach a shell interpreter if interpolated directly |
| release.yml file shape -> shipped binaries' cosign SAN | every already-published binary hard-codes the workflow filename + tag-ref pattern it will accept |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Spoofing | `.github/workflows/release.yml` filename + trigger vs `releaseWorkflowRefPattern` | critical | mitigate | `TestReleaseWorkflowFileMatchesPattern` reads the workflow off disk and reconstructs the SAN; `TestReleaseWorkflowTriggerIsTagPushOnly` pins the `on:` block to a single tag-push trigger. Both proven non-vacuous by mutating release.yml and observing failure |
| T-09-01-02 | Elevation of Privilege | GitHub App installation permissions (ASVS V4) | high | mitigate | Job-level `permissions:` limited to contents/issues/pull-requests write with an inline comment recording that this block governs the default token and NOT the App's installation scope; the App's own scope is set to the same three at 09-05's checkpoint |
| T-09-01-03 | Information Disclosure | `APP_PRIVATE_KEY` | high | mitigate | The PEM exists only as a repo secret consumed by `actions/create-github-app-token`'s `private-key` input; it is never echoed, never written to a file, and no `run:` step reads it. Installation tokens are short-lived by design, bounding the blast radius of a leaked *token* even before the *key* is rotated |
| T-09-01-04 | Tampering | workflow-YAML script injection via a directly-interpolated expression (ASVS V5) | high | mitigate | No `run:` step in release-please.yml interpolates a GitHub expression; the pretag-gate loop derives GOOS/GOARCH from a literal in-script list, not from workflow context. `actionlint` runs as a static gate on the file |
| T-09-01-05 | Tampering | supply chain — two new third-party GitHub Actions | high | mitigate | Both pinned to full 40-hex commit SHAs re-resolved live via `gh api`, with the resolved tag in a trailing comment. Both publishers are the canonical orgs (`actions`, `googleapis`). RESEARCH's stale/nonexistent release-please-action SHA is corrected in Task 1's action body |
| T-09-01-06 | Denial of Service | pre-tag gate on every push to `main` | low | accept | Six `go list` invocations per push; the cost is bounded and strictly dominated by the risk of a platform-specific `go.sum` failure reaching a release PR unnoticed |
| T-09-01-07 | Repudiation | a guard that is present but never fires (Phase-8 `CR-01`/`WR-02` class) | high | mitigate | Every guard in this plan carries an explicit break-observe-restore acceptance criterion; the parse helpers themselves are error-returning and covered by `TestWorkflowSourceHelpersFailLoudly` so a silent empty return cannot masquerade as a pass |
</threat_model>

<verification>
- `go test ./internal/upgrade/ -count=1` green (existing tests plus the three new ones)
- `actionlint .github/workflows/release-please.yml` exits 0
- `jq -e` shape assertions on both new JSON files exit 0
- `git diff -- .github/workflows/release.yml internal/upgrade/verify.go` produces no output
- The `release-please --dry-run` baseline resolution is recorded verbatim in the SUMMARY (or explicitly recorded as unmet)
- Every non-vacuity demonstration (drift guard, trigger guard, pretag gate) has its observed failure output recorded in the SUMMARY
</verification>

<success_criteria>
The release-please spine (config, manifest, App-token workflow, blocking pre-tag gate) exists and mechanically resolves this repo's real `0.1.0` baseline, while the LOCKED release identity is pinned by two guards that have each been demonstrated failing against a rejecting input and passing after restoration.
</success_criteria>

<output>
Create `.planning/phases/09-release-please-and-goreleaser/09-01-SUMMARY.md` when done.
</output>