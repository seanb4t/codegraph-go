---
phase: 09-release-please-and-goreleaser
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - docs/RELEASE-PROCEDURES.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-07-14-document-release-cut-procedures-runbook.md
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "docs/RELEASE-PROCEDURES.md no longer instructs a maintainer to create or push a version tag by hand as the normal path — §4 documents the release-PR-merge flow, and the hand-pushed tag survives only as the explicitly-labelled `rc` escape hatch (D-10)"
    - "§3 records the fast-forward merge model with the repo evidence that established it — `main` has zero merge commits and is fast-forwardable today — and explicitly supersedes Phase-8 08-CONTEXT.md's squash-merge wording rather than silently contradicting it (D-09)"
    - "§7 rollback covers the three artifacts a release-please cut now leaves behind: the tag, the GitHub Release, and the manifest/changelog bump commit plus its release PR — deleting the tag alone is no longer sufficient"
    - "§1's pre-tag sweep is documented as now-automated in release-please.yml's pretag-gate job, with the runbook explicitly demoted from enforcement point to documentation, so the D-09 lesson is not silently dropped"
    - "a new section documents the GitHub App prerequisite end to end: creation, the three installation permissions, that installation scope is distinct from a workflow's `permissions:` block, the two secret names, the deprecated-input note, and the branch-protection bypass-actor requirement if rulesets are ever added"
    - "a new section records every divergence this phase accepted, each with the source decision it diverges from and the reason — roadmap criterion 3's GoReleaser upload model (D-05), the PR-title lint's file location (D-08), and D-03's Path B recipe with its non-negotiable guard-plus-test requirement"
    - "ROADMAP.md's Phase 9 success criterion 3 carries a recorded amendment in the same style as Phase 8's goal amendment — the criterion is amended in place with its rationale, never deleted and never left standing as an unmet claim"
    - "when there are zero release-worthy commits since the last release, release-please opens no PR at all — documented in §4 as an expected silent no-op so a maintainer does not read the absence of a release PR as a broken pipeline"
    - "docs/RELEASE.md's user-facing verification commands are confirmed still correct against the unchanged cosign identity, and the confirmation is recorded rather than assumed"
    - "the folded todo is marked resolved with a pointer to the sections that closed it"
  artifacts:
    - docs/RELEASE-PROCEDURES.md
    - .planning/ROADMAP.md
    - .planning/todos/pending/2026-07-14-document-release-cut-procedures-runbook.md
  key_links:
    - "docs/RELEASE-PROCEDURES.md §9 (App prerequisite) -> plan 09-05's human checkpoint -> the APP_ID / APP_PRIVATE_KEY secrets -> release-please.yml's token minting -> the App-authored tag push"
    - "docs/RELEASE-PROCEDURES.md §7 (rollback) -> plan 09-06's disposable live-proof teardown"
    - "docs/RELEASE-PROCEDURES.md §4 (release-PR-merge flow) -> plan 09-07's v1.0.0 cut sequence"
    - "ROADMAP.md Phase 9 criterion 3 <-> D-05 -> the `--clobber` idempotency delivered in 09-02"
  prohibitions:
    - "MUST NOT weaken the post-release cosign identity regular expression quoted in §6 to a prefix match — it stays a full anchored match identical to verify.go's constant"
    - "MUST NOT edit internal/upgrade/verify.go, .github/workflows/, .goreleaser.yaml or any Go source in this plan — it is documentation and planning-artifact work only"
    - "MUST NOT delete ROADMAP.md criterion 3 or restate it as met — amend it in place with the divergence recorded"
    - "MUST NOT rewrite ROADMAP.md wholesale — use scoped edits confined to the Phase 9 section"
    - "MUST NOT rewrite §1, §2, §5, §6 or §8 of the runbook beyond the two additive notes named in the task actions — those five sections survive Phase 9"
    - "MUST NOT paraphrase the verify.go constants when quoting them — copy them verbatim and repeat §5's statement that the source wins on disagreement"
---

<objective>
Bring the maintainer runbook in line with what this phase actually built, and record every divergence in writing rather than letting it drift (D-05, D-08, D-09, D-03, and the folded todo).

Purpose: `docs/RELEASE-PROCEDURES.md` currently tells a maintainer to `git tag` by hand and to squash-merge — this phase invalidates both. It also has no GitHub App section, and the roadmap still carries a GoReleaser criterion this repo will never satisfy as written.
Output: a rewritten §3/§4/§7, two new sections, an amended roadmap criterion, and a resolved todo.

## Artifacts this plan produces
- `docs/RELEASE-PROCEDURES.md` — §3, §4, §7 rewritten; §1 and §6 gain additive notes; new §9 "GitHub App prerequisite (D-02)"; new §10 "Recorded divergences"
- `.planning/ROADMAP.md` — a recorded amendment to Phase 9 success criterion 3
- `.planning/todos/pending/2026-07-14-document-release-cut-procedures-runbook.md` — marked resolved
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-please-and-goreleaser/09-CONTEXT.md
@.planning/phases/09-release-please-and-goreleaser/09-RESEARCH.md
@.planning/phases/09-release-please-and-goreleaser/09-PATTERNS.md
@docs/RELEASE-PROCEDURES.md
@docs/RELEASE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite §3 / §4 / §7 for the release-please flow</name>
  <files>docs/RELEASE-PROCEDURES.md</files>
  <read_first>
    - docs/RELEASE-PROCEDURES.md in full — §1 lines 11-29, §2 lines 31-44, §3 lines 46-56, §4 lines 58-83, §5 lines 85-115, §6 lines 117-141, §7 lines 143-165, §8 lines 167-183. Sections 1, 2, 5, 6 and 8 survive; the rewrite must match their heading-plus-prose-plus-fenced-command-plus-interpretation rhythm exactly
    - .planning/phases/09-release-please-and-goreleaser/09-PATTERNS.md §"docs/RELEASE-PROCEDURES.md" — the documented section rhythm and the "source wins" convention from §5
    - .planning/phases/09-release-please-and-goreleaser/09-CONTEXT.md — Folded Todos (which sections are invalidated and what replaces them), D-06, D-09, D-10
    - .github/workflows/release-please.yml — the workflow the rewritten §4 describes, so the prose matches what actually ships
    - .planning/phases/08-surface-reconciliation-signed-v1-0-0-release/08-CONTEXT.md §D-08 — the squash-merge wording being superseded, so the supersession is cited precisely
  </read_first>
  <action>
    Rewrite three sections in place. Leave §1, §2, §5, §6 and §8 otherwise untouched.

    **§3 Branch/tag model.** Replace the current four-step list. The new model: land phase work on the integration branch; the pre-tag sweep now runs automatically on every push to `main` (§1, see Task 2's note); **fast-forward** the integration branch onto `main`, preserving full history — do not squash; the tag is then created by release-please when its release PR merges, never by hand. Include the repo evidence that settled this: `main` contains zero merge commits (`git log --merges main` is empty), `main` is an ancestor of the integration branch today so a fast-forward is possible as-is, and the branch carries roughly 477 commits of which about 160 are `feat`/`fix`/`perf`. Explain the consequence: under fast-forward, release-please sees all of those commits and generates a real changelog, while its default changelog sections hide the roughly 217 planning-only `docs:` commits automatically; a squash would collapse everything into a single message and reduce the changelog to one line. State explicitly that this supersedes the "squash-merge to `main`" wording in Phase 8's `08-CONTEXT.md` D-08, that only the merge mechanic is superseded and D-08's substance (integration branch → `main` → tag on `main`) is honoured in full, and that the supersession rests on repo evidence rather than preference.

    **§4 Cutting a release.** Replace the manual `git tag` block. Retitle to reflect that this is now automated, and document the flow in order: (1) merge to `main`; (2) `release-please.yml` runs its `pretag-gate` job and then release-please, which opens or updates a "chore(main): release X.Y.Z" PR; (3) review the PR's proposed version and changelog; (4) merge it; (5) release-please creates the tag and the GitHub Release in a single API call — the Release object therefore exists the instant the tag ref does; (6) the tag push, authored by the GitHub App token, starts `release.yml`, whose three jobs (`build`, `assemble`, `provenance`) are unchanged and are still described where §4 already described them; (7) `assemble`'s publish step finds the existing Release and uploads the signed binaries into it with the clobber flag, leaving release-please's changelog body and prerelease flag untouched. Add an explicit note that the version bump comes from the accumulated Conventional Commits and can be forced for a specific cut with a one-shot `Release-As:` footer on an empty `chore:` commit, and that this footer must never be moved into `release-please-config.json` because a config-level setting is sticky and would pin every subsequent release. Add the empty-input note: **if there are no release-worthy commits since the last release, release-please opens no PR at all** — this is an expected silent no-op, not a broken pipeline; check the workflow run's log rather than assuming failure. Finally, retain the hand-pushed tag as an explicitly-labelled escape hatch for `rc` cuts only (D-10): a `v0.0.0-rc.N` tag pushed by hand still matches `release.yml`'s trigger, still fires the signed build, and takes the publish step's *create* branch instead of its upload branch.

    **§7 Rollback / cleanup.** Broaden it. A release-please cut now leaves three things behind rather than one, and all three must be undone: the tag, the GitHub Release object, and the version-bump commit on `main` (the `CHANGELOG.md` entry plus the `.release-please-manifest.json` bump) together with its merged release PR. Document, in order: delete the Release and its tag with the existing `gh release delete --cleanup-tag` command already in the section; then revert the release commit on `main` so release-please's next run does not believe the version already shipped; then note that release-please re-labels its PR `autorelease: tagged` on success, so a partially-completed cut may need that label removed before it will retry. Keep the existing warning that a stable tag name must never be reused after deletion, and keep the prerelease-tags-are-safer-to-retag note. Add a pointer that this same procedure is what tears down the disposable live-proof release described in §9.
  </action>
  <verify>
    <automated>grep -q 'fast-forward' docs/RELEASE-PROCEDURES.md && grep -q 'release-please' docs/RELEASE-PROCEDURES.md && grep -q 'autorelease' docs/RELEASE-PROCEDURES.md && grep -q 'releaseWorkflowRefPattern' docs/RELEASE-PROCEDURES.md && git diff --quiet -- internal/upgrade .github .goreleaser.yaml</automated>
  </verify>
  <acceptance_criteria>
    - §3 contains the word "fast-forward", cites the zero-merge-commits evidence, and names `08-CONTEXT.md` D-08 as the wording it supersedes.
    - §4 no longer presents a hand-run tag command as the normal path: the `git tag` invocation appears only inside the explicitly-labelled `rc` escape-hatch subsection, and the section's opening prose names the release-PR merge as the trigger.
    - §4 states that the tag and the GitHub Release are created by one API call, and states the zero-release-worthy-commits no-op.
    - §7 names all three artifacts a cut leaves behind (tag, Release, version-bump commit plus its PR) and documents the `autorelease` label consideration.
    - §5's verbatim `verify.go` constants block is byte-unchanged: `git diff -U0 -- docs/RELEASE-PROCEDURES.md` shows no hunk inside the §5 code fence, and `grep -c 'releaseWorkflowRefPattern' docs/RELEASE-PROCEDURES.md` returns at least 1.
    - §6's cosign identity regular expression is byte-unchanged and still fully anchored.
    - `git diff --quiet -- internal/upgrade .github .goreleaser.yaml` exits 0 — no source, workflow or GoReleaser file is touched by this plan.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation edits.</reversibility>
  <done>The runbook describes the pipeline that now exists: automated tag creation, a fast-forward merge model with its evidence, and a rollback procedure covering all three artifacts a release-please cut produces.</done>
</task>

<task type="auto">
  <name>Task 2: New §9 GitHub App prerequisite + §10 recorded divergences + §1/§6 notes</name>
  <files>docs/RELEASE-PROCEDURES.md</files>
  <read_first>
    - docs/RELEASE-PROCEDURES.md §1 lines 11-29 and §5 lines 85-115 — the two sections gaining notes, and the "source wins" convention the new sections must adopt
    - .planning/phases/09-release-please-and-goreleaser/09-RESEARCH.md — Pitfall 2 (App installation permissions vs workflow permissions), Pitfall 3 (the header-comment claim vs D-03), Pitfall 4 (a disposable tag resolving as latest), the State-of-the-Art row on the deprecated token input, and the Validation Architecture live-proof procedure
    - .planning/phases/09-release-please-and-goreleaser/09-CONTEXT.md — D-03, D-05, D-08, and the Claude's-Discretion row on where the App procedure lives
    - .planning/phases/09-release-please-and-goreleaser/09-03-PLAN.md `<recorded_divergence>` — the D-08 file-location divergence to restate here
    - .github/workflows/release-please.yml — the secret names and inputs the new section must match
  </read_first>
  <action>
    Add two new sections at the end of `docs/RELEASE-PROCEDURES.md`, matching the existing heading-plus-prose-plus-fenced-command rhythm, and add two short notes to existing sections. Keep the App procedure in this file rather than a sibling document — it is a maintainer runbook step, and splitting it would create a second place to forget to update (CONTEXT's discretion row).

    **New §9 — GitHub App prerequisite (D-02).** State up front why an App is required and not optional: a tag pushed with the workflow's default token does not start other workflow runs, so `release.yml` would never fire; an App installation token does. Then document, as an ordered procedure a maintainer can follow once: create the App under the account's Developer settings; grant it the three installation permissions Contents (read and write), Pull requests (read and write) and Issues (read and write) — and state explicitly that Issues is needed because release-please labels its PR `autorelease: pending` / `autorelease: tagged` and GitHub governs PR labels under the Issues scope; install the App on `seanb4t/codegraph-go`; generate a private key and store the full PEM as the repository secret `APP_PRIVATE_KEY`, and the numeric App ID as the repository secret `APP_ID`. Add a "these are two different permission systems" note reproducing RESEARCH Pitfall 2: the App's *installation* permissions are set in the App's own settings and authorise everything release-please does with the App token, while the `permissions:` block in `release-please.yml` governs only the default token — configuring one correctly while under-scoping the other produces a runtime authorisation failure on first real use, not a config-time error. Add a deprecation note: the token action's current input is `client-id` and the `app-id` input this repo uses is deprecated-but-accepted; migrating means re-seeding the secret with the App's *Client ID* string rather than its numeric App ID, so the two are not interchangeable. Add a pre-flight command block: check the repo's Actions permissions with `gh api repos/seanb4t/codegraph-go/actions/permissions` and confirm the secrets landed with `gh secret list`. Finally add a branch-protection note: the repo has no rulesets today, but if branch protection is ever added to `main`, the App must be on the bypass-actor list or release-please will be unable to push its version-bump commit.

    **New §10 — Recorded divergences (Phase 9).** Three entries, each naming the source decision, what shipped instead, and why. (a) **Roadmap Phase 9 criterion 3 / D-05:** the criterion assumes a `goreleaser release` upload model with a replace-existing-artifacts setting and no changelog block. This repo has never run `goreleaser release`; `.goreleaser.yaml`'s archive and checksum blocks are documented-inert, and signing, SBOM and publishing live in `release.yml`'s hand-written assemble job because GoReleaser OSS cannot express per-binary cosign (the upgrade path hashes the binary itself, not a checksums file), the native two-OS matrix that keeps darwin off a cross-link, or the SLSA generic-generator handoff. The criterion's *intent* — idempotent artifact replacement — is satisfied by the publish step's clobber flag instead. Recorded, not dropped. (b) **D-08 lint file location:** the decision names `ci.yml`; the check ships in a dedicated `pr-title.yml`, because `ci.yml`'s shared pull-request trigger excludes the title-edit event type and widening it would re-run the heavy reproducibility and perf-regression jobs on every title or description edit. Substance unchanged, location diverged. (c) **D-03 Path B is not implemented.** State that the dispatch-based fallback is deliberately unimplemented, that this keeps `release.yml`'s header claim about triggering only on tag pushes true by construction, and record the full recipe for anyone who later needs it: add the dispatch trigger *alongside* — never instead of — the existing tag trigger; dispatch it at the tag ref, because dispatching at a branch produces a branch-shaped signing identity and silently unverifiable binaries; add a guard step that hard-fails when the ref does not start with `refs/tags/v`; add a test that proves that guard fires on a rejecting input; and update `release.yml`'s header comment in the same commit. Note that `TestReleaseWorkflowTriggerIsTagPushOnly` in `internal/upgrade/release_workflow_shape_test.go` goes red on any second trigger, so this recipe cannot be skipped by accident.

    **Note added to §1.** State that this sweep now also runs automatically as the `pretag-gate` job in `.github/workflows/release-please.yml` on every push to `main`, gating release-please via `needs:`; the manual command block is retained for pre-merge use on an integration branch, but the enforcement point is now CI, not this document.

    **Note added to §6.** State that post-release verification is mandatory, not optional, for the first automated cut and for the disposable live proof — it is the only evidence that the cosign identity survived the rewiring, and it is what REL-02's second half is verified against. Leave the two command blocks byte-unchanged.
  </action>
  <verify>
    <automated>grep -q 'APP_PRIVATE_KEY' docs/RELEASE-PROCEDURES.md && grep -q 'pretag-gate' docs/RELEASE-PROCEDURES.md && grep -q 'bypass' docs/RELEASE-PROCEDURES.md && grep -qi 'divergence' docs/RELEASE-PROCEDURES.md && git diff --quiet -- internal/upgrade .github .goreleaser.yaml</automated>
  </verify>
  <acceptance_criteria>
    - §9 exists and names all three installation permissions, both secret names, the installation-scope-vs-workflow-permissions distinction, the deprecated-input note, the two pre-flight `gh` commands, and the branch-protection bypass-actor requirement.
    - §10 exists with exactly three divergence entries, each naming its source decision identifier.
    - §10's D-03 entry lists all five Path B requirements (alongside-not-instead-of, dispatch at the tag ref, the ref-shape guard, a test proving the guard fires, the header-comment update).
    - §1 carries the automation note naming the `pretag-gate` job, and §1's original command block is byte-unchanged.
    - §6's two command blocks are byte-unchanged: `git diff -U0 -- docs/RELEASE-PROCEDURES.md` shows no hunk inside them.
    - `docs/RELEASE.md` is confirmed still correct against the unchanged cosign identity — run `grep -c 'release\\.ya\\?ml@refs/tags' docs/RELEASE.md` and confirm the identity pattern it documents is the anchored one from `internal/upgrade/verify.go`; record the comparison result in the SUMMARY and leave the file unchanged unless the comparison shows a real mismatch.
    - `git diff --quiet -- internal/upgrade .github .goreleaser.yaml` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation edits.</reversibility>
  <done>A maintainer can set up the GitHub App from the runbook alone, and every divergence this phase accepted is written down with its source decision and reason.</done>
</task>

<task type="auto">
  <name>Task 3: Amend ROADMAP Phase 9 criterion 3 and resolve the folded todo</name>
  <files>.planning/ROADMAP.md, .planning/todos/pending/2026-07-14-document-release-cut-procedures-runbook.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 9: release-please + GoReleaser" — the four success criteria and the Notes paragraph
    - .planning/ROADMAP.md §"Phase 8" — the recorded goal amendment blockquote, whose style and placement this amendment must match
    - .planning/todos/pending/2026-07-14-document-release-cut-procedures-runbook.md — the todo's own front matter and resolution convention
    - .planning/phases/09-release-please-and-goreleaser/09-CONTEXT.md — D-05 and Folded Todos
  </read_first>
  <action>
    Amend `.planning/ROADMAP.md` using scoped edits only — never a whole-file rewrite, and never touching any phase other than 9.

    Rewrite Phase 9's success criterion 3 in place so it states what this repo actually delivers: artifacts are uploaded into the release-please-created GitHub Release idempotently via the publish step's clobber flag, with `goreleaser build --single-target` retained and no `goreleaser release` migration. Immediately below the criteria list, add a blockquote amendment note in the same style and position as Phase 8's recorded goal amendment: state that criterion 3 was written against engram's `goreleaser release` model, that this repo has never run that command and cannot express per-binary cosign, the native two-OS matrix or the SLSA handoff through it, that the criterion's idempotency intent is met by the clobber flag instead (D-05), and that the amendment is recorded rather than applied silently because the phase is verified goal-backward and closing it against the original wording would claim a GoReleaser upload path that does not exist. Date the amendment.

    Then mark the folded todo resolved following the file's own convention: set its status/resolution fields, name Phase 9 as the resolving phase, and point at the specific sections that closed it (`docs/RELEASE-PROCEDURES.md` §3, §4, §7, §9, §10). Move it to the resolved location only if the repo's todo convention has one; otherwise mark it in place. Do not delete it.
  </action>
  <verify>
    <automated>grep -q 'criterion 3' .planning/ROADMAP.md && grep -c '### Phase' .planning/ROADMAP.md && git diff --quiet -- internal/upgrade .github .goreleaser.yaml docs</automated>
  </verify>
  <acceptance_criteria>
    - Phase 9's criterion 3 in `.planning/ROADMAP.md` no longer asserts a `goreleaser release` upload model, and a dated amendment blockquote appears directly below Phase 9's criteria list naming D-05 as the source decision.
    - Scoped-edit proof: `git diff -U0 -- .planning/ROADMAP.md` shows hunks only inside the Phase 9 section — no hunk touches any other phase's Goal, Requirements, Success Criteria or Plans list.
    - Phase count is unchanged: `grep -c '^### Phase' .planning/ROADMAP.md` returns the same value before and after the edit. Record both counts in the SUMMARY.
    - The folded todo file records a resolution naming Phase 9 and the runbook sections that closed it, and the file still exists.
    - `git diff --quiet -- internal/upgrade .github .goreleaser.yaml` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Planning-artifact edits.</reversibility>
  <done>The roadmap's GoReleaser criterion is amended in writing with its rationale, and the folded runbook todo is closed against the sections that satisfied it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook prose -> maintainer action on a live repo | the document is the only instruction set for irreversible actions (tag creation, merges, rollbacks) |
| runbook's quoted `verify.go` constants -> the real constants | a paraphrased or stale quote would teach a maintainer the wrong trust contract |
| §9's App setup instructions -> the App's real installation scope | over-granting here is the phase's main standing privilege risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Spoofing | the cosign identity regular expression quoted in §6 | high | mitigate | §5 and §6's blocks are asserted byte-unchanged by a diff-scope acceptance criterion; §5's "the source wins on disagreement" statement is preserved verbatim |
| T-09-04-02 | Elevation of Privilege | over-scoped GitHub App installation permissions (ASVS V4) | high | mitigate | §9 enumerates exactly three permissions and states why each is needed, so a maintainer has no reason to reach for a broader grant; the workflow-vs-installation-scope distinction is called out explicitly |
| T-09-04-03 | Information Disclosure | the App private key | high | mitigate | §9 instructs storing the PEM only as a repository secret and never in a file or workflow; the pre-flight step is `gh secret list`, which lists names without values |
| T-09-04-04 | Repudiation | a divergence resolved silently instead of recorded | medium | mitigate | §10 exists specifically to hold them, and the roadmap amendment is required to be an in-place edit plus a dated blockquote rather than a deletion |
| T-09-04-05 | Tampering | a whole-file ROADMAP rewrite destroying other phases | high | mitigate | Scoped-edit-only instruction plus a hunk-scope acceptance criterion and a before/after phase-count check |
| T-09-04-06 | Denial of Service | a maintainer reading "no release PR opened" as a broken pipeline and forcing a manual tag | low | accept | §4's zero-release-worthy-commits note documents the no-op; the residual risk is a wasted investigation, not a bad artifact |
</threat_model>

<verification>
- `git diff --quiet -- internal/upgrade .github .goreleaser.yaml` exits 0 for the whole plan
- `git diff -U0 -- docs/RELEASE-PROCEDURES.md` shows no hunk inside §5's or §6's code fences
- `git diff -U0 -- .planning/ROADMAP.md` shows hunks only inside the Phase 9 section, and `grep -c '^### Phase' .planning/ROADMAP.md` is unchanged
- `go test ./internal/upgrade/ -count=1` and `actionlint .github/workflows/*.yml` still green (regression check — this plan should not be able to affect either)
- The `docs/RELEASE.md` comparison result is recorded in the SUMMARY
</verification>

<success_criteria>
The runbook instructs the pipeline that exists rather than the one it replaced, a maintainer can stand up the GitHub App from it alone, and every divergence this phase accepted — GoReleaser's role, the lint's file location, and Path B's deliberate absence — is written down with its source decision and reason.
</success_criteria>

<output>
Create `.planning/phases/09-release-please-and-goreleaser/09-04-SUMMARY.md` when done.
</output>
