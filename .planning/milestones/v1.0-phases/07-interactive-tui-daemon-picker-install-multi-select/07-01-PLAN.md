---
phase: 07-interactive-tui-daemon-picker-install-multi-select
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/cli/tui/tty.go
  - internal/cli/tui/tty_test.go
autonomous: true
requirements: [TUI-04]
must_haves:
  truths:
    - "The TUI-01 archtest (internal/cli/present/archtest) stays GREEN after charm.land/bubbletea/v2 + charm.land/bubbles/v2 are added — internal/daemon and the other five guarded packages remain charm-free by construction (D-09/D-11)."
    - "internal/cli/tui.InteractiveAllowed(cmd) returns true only when BOTH stdin is a real char-device TTY AND stdout is a TTY AND NO_COLOR is unset — a single shared gate every tea.NewProgram() call site checks first (D-10)."
    - "When stdin is a TTY but stdout is piped (or vice-versa), InteractiveAllowed returns false (both fds must be terminals to go interactive) — TUI-04 adjacency edge."
    - statement: "Empty/EOF or piped stdin makes InteractiveAllowed return false so no interactive path ever issues a blocking stdin read (never-hang invariant); exercised via the injectable seam, not a real pty."
      verification: backstop
  artifacts:
    - "internal/cli/tui/tty.go — new sibling package hosting the interactive layer; InteractiveAllowed gate composing present.ChoosePresentation."
    - "go.mod requires charm.land/bubbletea/v2 and charm.land/bubbles/v2 at pinned versions."
  key_links:
    - "internal/cli/tui.InteractiveAllowed → present.ChoosePresentation (reuse the existing stdout+NO_COLOR seam, add the stdin-TTY requirement)."
  prohibitions:
    - "An interactive component must never read stdin when stdin/stdout is not a TTY (the never-hang invariant)."
    - "internal/daemon / internal/agents / the guarded engine set must never import charm.land/{bubbletea,bubbles,lipgloss}/v2 (archtest guard)."
---

<objective>
Add the interactive-layer dependencies and the shared TTY gate that every bubbletea entry point checks before constructing a Program, without breaking the Phase-6 ANSI-isolation archtest.

Purpose: This is the foundation the daemon picker (07-07) and the agent multi-select (07-06) build on. Per D-11 it adds `charm.land/bubbletea/v2` + `charm.land/bubbles/v2` (Charm v2 vanity import, not `github.com/charmbracelet/...`); per D-09 the interactive code lives ONLY in a new `internal/cli/tui` sibling package so the guarded engine set stays charm-free; per D-10 a single `InteractiveAllowed` gate requires BOTH stdin and stdout to be real terminals before any `tea.NewProgram()` is reached (TUI-04's never-hang core).

Output: go.mod/go.sum with the two new deps; a new `internal/cli/tui` package containing `InteractiveAllowed`; the existing TUI-01 archtest confirmed green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-CONTEXT.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-RESEARCH.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-PATTERNS.md
@internal/cli/present/tty.go
@internal/cli/install.go
@internal/cli/present/archtest/import_graph_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bubbletea/v2 + bubbles/v2 deps and confirm the ANSI-isolation archtest stays green</name>
  <files>go.mod, go.sum</files>
  <read_first>
    - go.mod (current charm.land/lipgloss/v2 v2.0.5, golang.org/x/term v0.45.0, golang.org/x/sys v0.47.0 // indirect)
    - internal/cli/present/archtest/import_graph_test.go (guardedPackages already lists internal/daemon; forbiddenImportPaths already lists charm.land/bubbles/v2 and charm.land/bubbletea/v2; excludedInternalPackagePrefixes already excludes internal/cli — so internal/cli/tui is outside the guarded closure by construction)
    - STATE.md note "[Phase 6] go mod tidy needs -e flag due to a pre-existing tree-sitter-swift resolution error"
  </read_first>
  <action>
    Run `go list -m -versions charm.land/bubbletea/v2` and `charm.land/bubbles/v2` to reconfirm the current stable versions (RESEARCH names v2.0.8 and v2.1.1; re-verify because RESEARCH's "valid until" for exact versions is ~14 days). Then `go get charm.land/bubbletea/v2@<version> charm.land/bubbles/v2@<version>` (D-11: the Charm v2 vanity import path, NOT github.com/charmbracelet/...). Run `go mod tidy -e` (the -e flag per the STATE.md Phase-6 precedent for the unrelated tree-sitter-swift resolution error). Do NOT promote golang.org/x/sys to direct here — that promotion is owned by 07-03 when it adds the windows watchdog file. Confirm the two new modules are pure Go (no CGo) by grepping the added go.sum lines are the expected charmbracelet modules only — the full REL-01 closure audit is Phase 8, not this task.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/cli/present/archtest/ -run TestNoCharmInServeReachablePackages</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` contains `charm.land/bubbletea/v2` and `charm.land/bubbles/v2` require lines at pinned versions.
    - `go build ./...` succeeds.
    - The existing archtest `go test ./internal/cli/present/archtest/` passes GREEN with no edits to import_graph_test.go (the guard already covers the new paths — D-09/D-11).
  </acceptance_criteria>
  <done>The two interactive deps are present and the build-enforced ANSI isolation is confirmed intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create internal/cli/tui with the InteractiveAllowed dual-TTY gate</name>
  <files>internal/cli/tui/tty.go, internal/cli/tui/tty_test.go</files>
  <read_first>
    - internal/cli/install.go (installStdinIsInteractive, lines 24-33 — the os.Stdin + os.ModeCharDevice stdin-TTY shape and its injectable-package-var pattern)
    - internal/cli/present/tty.go (ChoosePresentation(isTTY, noColor) — the pure stdout+NO_COLOR seam to compose, D-04/D-10)
    - 07-RESEARCH.md Pattern 1 "TTY-gate before tea.NewProgram() — gate BOTH stdin and stdout"
  </read_first>
  <behavior>
    - Piped stdin (not os.Stdin, or not a char device), TTY stdout, NO_COLOR empty → InteractiveAllowed returns false.
    - TTY stdin, piped stdout → returns false.
    - TTY stdin, TTY stdout, NO_COLOR="1" → returns false.
    - TTY stdin, TTY stdout, NO_COLOR empty → returns true.
    - The three inputs (stdin-tty, stdout-tty, no-color) are injectable package-level func vars so the test drives every branch without a real pty (mirroring install.go's installStdinIsInteractive var).
  </behavior>
  <action>
    Create `internal/cli/tui/tty.go` (package `tui`). Add `InteractiveAllowed(cmd *cobra.Command) bool` (D-10) that returns true only when the stdin-is-real-TTY predicate AND `present.ChoosePresentation(stdoutIsTTY, noColorEnv)` are both true. Factor the three probes behind unexported package-level func vars — `stdinIsInteractive func(*cobra.Command) bool` (copy install.go's cmd.InOrStdin()==os.Stdin + os.Stdin.Stat() char-device check), `stdoutIsTTY func() bool` (term.IsTerminal(int(os.Stdout.Fd()))), `noColor func() string` (os.Getenv("NO_COLOR")) — so tty_test.go swaps them per case. Reuse internal/cli/present.ChoosePresentation for the stdout+NO_COLOR half rather than re-deriving it (D-10: the SAME seam). Write tty_test.go FIRST covering the four behavior cases above (RED), then implement to green.
  </action>
  <verify>
    <automated>go test ./internal/cli/tui/ -run TestInteractiveAllowed</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/tui/tty.go` exports `InteractiveAllowed(cmd *cobra.Command) bool`.
    - `internal/cli/tui/tty_test.go` proves all four behavior rows (both-TTY→true; each single-piped→false; NO_COLOR set→false) via injected seams.
    - `internal/cli/tui` imports internal/cli/present (allowed; tui is in the cli layer) and does NOT import internal/daemon or internal/agents in this file.
  </acceptance_criteria>
  <done>Every interactive entry point in later plans has one shared, unit-tested gate to call before tea.NewProgram().</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- NEW package `internal/cli/tui` (the ONLY package that will import bubbletea/bubbles).
- NEW `internal/cli/tui/tty.go` → `InteractiveAllowed(cmd *cobra.Command) bool`.
- NEW `internal/cli/tui/tty_test.go`.
- go.mod / go.sum: `charm.land/bubbletea/v2`, `charm.land/bubbles/v2` (pinned).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain → build | Two new third-party modules enter the dependency closure |
| terminal → process | stdin/stdout fds decide whether a blocking interactive read is issued |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-01 | Tampering | new charm deps (bubbletea/bubbles) | medium | mitigate | Same already-vetted charmbracelet org as Phase-6 lipgloss/v2; pinned exact versions; pure-Go (no CGo) confirmed; full REL-01 closure audit deferred to Phase 8 by decision |
| T-07-01-02 | Denial of Service | interactive gate | high | mitigate | InteractiveAllowed requires BOTH fds be terminals before any tea.NewProgram(); a piped/CI invocation can never reach a blocking stdin read (TUI-04 never-hang) |
| T-07-01-03 | Elevation of Privilege | archtest guard | low | mitigate | Guarded engine set stays charm-free by construction; archtest fails the build if charm leaks into internal/daemon et al. |
</threat_model>

<verification>
- `go build ./...` green.
- `go test ./internal/cli/present/archtest/` green (ANSI isolation intact).
- `go test ./internal/cli/tui/` green.
</verification>

<success_criteria>
- Both deps pinned in go.mod; archtest green with zero edits to it.
- `InteractiveAllowed` gates on stdin AND stdout AND NO_COLOR, all branches unit-proven.
</success_criteria>

<output>
Create `.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-01-SUMMARY.md` when done.
</output>
