---
phase: 07-interactive-tui-daemon-picker-install-multi-select
plan: 07
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-04", "07-05"]
files_modified:
  - internal/cli/tui/daemonpicker.go
  - internal/cli/tui/daemonpicker_test.go
  - internal/cli/daemon.go
  - internal/cli/daemon_test.go
autonomous: true
requirements: [DMON-01, DMON-02, TUI-04]
must_haves:
  truths:
    - "`codegraph daemon` becomes a cobra tree: bare (TTY→bubbletea picker; non-TTY→plain running-daemon list, exit 0), plus `daemon start` (the current foreground RunE moved verbatim, DisabledError branch preserved) and `daemon stop [--all]` (D-01)."
    - "The bare picker/list orders running daemons current-project-first, then a stable secondary order (repoRoot then pid) — DMON-01 ordering edge; the plain non-TTY list uses the SAME ordering (TUI-04 ordering edge)."
    - "The picker offers stop-one / stop-all / cancel, dispatching to daemon.StopMatching/StopAll (07-04) which re-corroborate each target before signaling; cancel signals nothing (DMON-01)."
    - "An empty registry makes bare `daemon` show a 'no running daemons' message and exit 0 on BOTH the TTY path (picker opens empty / exits) and the non-TTY path (plain list) — DMON-01/DMON-04 empty edge; DMON-02 stop-with-none is a 'no running daemon' notice, exit 0."
    - "`daemon stop`/`stop --all` are non-interactive and never open a Program; `stop` defaults to the current repo via -p/--path (Assumption A2, flagged); `--all` stops every live daemon."
    - statement: "The bare-daemon TTY branch calls tui.InteractiveAllowed before tea.NewProgram(); a piped/closed-stdio invocation takes the plain-list branch and exits promptly (never-hang) — enforced end-to-end by 07-08."
      verification: backstop
  artifacts:
    - "internal/cli/tui/daemonpicker.go — RunDaemonPicker(cmd, records) over the []daemon.Record, dispatching stop-one/stop-all/cancel."
    - "internal/cli/daemon.go — newDaemonCmd() cobra tree: bare RunE + newDaemonStartCmd() + newDaemonStopCmd()."
  key_links:
    - "daemon.go bare RunE → tui.InteractiveAllowed (07-01) → RunDaemonPicker OR plain list; both via daemon.List() (07-02)."
    - "picker/stop cmd → daemon.StopMatching/StopAll (07-04, corroborated); daemon start → daemon.Run (07-05, now registers + watchdogs)."
  prohibitions:
    - "No silent auto-spawn: bare `daemon` and `daemon stop` must never start a daemon; only `daemon start` runs one (D-03)."
    - "An interactive component must never read stdin when stdin/stdout is not a TTY — the bare RunE gates on tui.InteractiveAllowed before tea.NewProgram()."
    - "internal/daemon must never import charm.land/{bubbletea,bubbles,lipgloss}/v2 — the picker lives in internal/cli/tui (archtest guard)."
---

<objective>
Restructure `codegraph daemon` into the DMON-01/DMON-02 command tree — a bubbletea picker on a TTY (plain list off-TTY), `daemon start` (the existing foreground server, renamed), and `daemon stop [--all]` — resolving the TS name collision.

Purpose: Per D-01 the bare `daemon` is a picker (TTY) / plain list (non-TTY, D-12); the current foreground `RunE` (with its `watch.DisabledError` friendly-exit branch) moves verbatim into `daemon start` (D-02); `daemon stop`/`stop --all` (D-02) drive the corroborated `daemon.StopMatching`/`StopAll` (07-04). The picker Model lives in `internal/cli/tui` (D-09), gated by `tui.InteractiveAllowed` before `tea.NewProgram()` (D-10, TUI-04). Following the `internal/cli/githooks.go` `AddCommand` tree shape.

Output: `internal/cli/tui/daemonpicker.go` + the restructured `internal/cli/daemon.go` + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-CONTEXT.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-RESEARCH.md
@.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-PATTERNS.md
@internal/cli/daemon.go
@internal/cli/githooks.go
@internal/cli/tui/tty.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Daemon picker bubbletea Model (list + stop-one/stop-all/cancel)</name>
  <files>internal/cli/tui/daemonpicker.go, internal/cli/tui/daemonpicker_test.go</files>
  <read_first>
    - 07-RESEARCH.md Architecture Patterns "Daemon lifecycle diagram" (bare TTY: List → self-heal → current-repo-first → picker → stop-one/stop-all/cancel) and Pattern 1 (gate before NewProgram)
    - internal/daemon/registry.go (Record{PID,StartedAt,RepoRoot}, List — 07-02) and internal/daemon/stop.go (StopMatching/StopAll — 07-04)
    - internal/cli/present/progress.go (charm-in-cli deterministic-lifecycle precedent) and internal/cli/tui/agentpicker.go (07-06, the bubbles list.Model Model shape to mirror)
  </read_first>
  <behavior>
    - Given []daemon.Record, the Model sorts current-project-first (by matching a passed current-repoRoot), then stable by (RepoRoot, PID); renders one row per daemon (repo / pid / age columns — column layout Claude's discretion).
    - Selecting a row + confirming "stop" dispatches a stop for that record's repo (StopMatching) or that pid; "stop-all" dispatches StopAll; "cancel"/quit signals nothing.
    - An empty record set renders a "no running daemons" view and exits without signaling.
    - Model.Update transitions (navigate, stop-one, stop-all, cancel) are unit-testable via synthetic tea.Msg — no pty; the actual stop call is injected behind a func field/var so the test asserts the intended action without signaling a real process.
  </behavior>
  <action>
    Create `internal/cli/tui/daemonpicker.go` (package tui). Build a bubbletea Model over a bubbles `list.Model` whose items wrap `daemon.Record` (DisplayName-style row = repo basename + pid + age from time.Since(StartedAt)). Add `RunDaemonPicker(cmd *cobra.Command, currentRepo string, records []daemon.Record) error`: sort records current-repo-first then (RepoRoot,PID) stable, construct the Model, `tea.NewProgram(model, tea.WithInput(cmd.InOrStdin()), tea.WithOutput(cmd.OutOrStdout()))`, and on the final action dispatch stop-one (daemon.StopMatching(record.RepoRoot)) / stop-all (daemon.StopAll()) / cancel (no-op). Inject the stop functions behind unexported package-level func vars (`stopMatching = daemon.StopMatching`, `stopAll = daemon.StopAll`) so daemonpicker_test.go asserts dispatch without real signals. RunDaemonPicker does NOT re-check TTY (caller gates). Write daemonpicker_test.go RED first: feed synthetic key msgs for select+stop, stop-all, and cancel; assert the injected stop funcs are called with the right args (or not at all for cancel); assert current-repo-first ordering.
  </action>
  <verify>
    <automated>go test ./internal/cli/tui/ -run TestDaemonPicker</automated>
  </verify>
  <acceptance_criteria>
    - `RunDaemonPicker` exists; Model handles stop-one/stop-all/cancel and current-repo-first ordering, unit-proven via synthetic tea.Msg.
    - Cancel signals nothing; empty records → "no running daemons" view.
    - Picker imports daemon (allowed) + bubbles/bubbletea (only in tui).
  </acceptance_criteria>
  <done>A TTY user can pick a running daemon to stop, stop all, or cancel, entirely in the cli layer.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure daemon.go into a tree — bare (picker/plain list) + daemon start</name>
  <files>internal/cli/daemon.go, internal/cli/daemon_test.go</files>
  <read_first>
    - internal/cli/daemon.go (the whole current RunE lines 27-90, especially the signal.NotifyContext + d.Run(ctx) + watch.DisabledError branch lines 51-80 — moves verbatim into daemon start)
    - internal/cli/githooks.go (newGithooksCmd lines 18-25 — the parent + AddCommand tree shape to replicate)
    - internal/cli/tui/tty.go (InteractiveAllowed — 07-01) and daemonpicker.go (RunDaemonPicker — Task 1)
    - internal/daemon/registry.go (List — 07-02); internal/cli/query.go (resolveStartPath) for the current-repo path resolution
  </read_first>
  <action>
    Restructure `newDaemonCmd()` (internal/cli/daemon.go) into a tree (D-01), following githooks.go's shape. The PARENT command's bare RunE: resolve the current repo path (resolveStartPath), call `daemon.List()` (self-healed), sort current-repo-first; if `tui.InteractiveAllowed(cmd)` → `tui.RunDaemonPicker(cmd, currentRepo, records)` (D-10 gate before tea.NewProgram); else print a plain `pid  repo  started` table (or a "no running daemons" line when empty) to cmd.OutOrStdout() and return nil, exit 0 (D-12). Move the ENTIRE existing foreground RunE (the daemon.New/signal.NotifyContext/d.Run(ctx)/errors.As watch.DisabledError friendly-exit block) verbatim into a new `newDaemonStartCmd()` with the same --path/--quiet/--verbose/--workers flags (D-01/D-02 — same behavior, new name). Wire `cmd.AddCommand(newDaemonStartCmd(), newDaemonStopCmd())` (stop cmd from Task 3). root.go already registers newDaemonCmd() — no root change needed. Extend daemon_test.go: assert `daemon start` still hits the DisabledError friendly-exit branch; assert bare `daemon` on a forced-non-TTY prints the plain list and exits 0 (empty registry → "no running daemons"); assert bare `daemon` never blocks (the InteractiveAllowed=false branch takes the plain path). Use the registryDir test seam (07-02) to seed records.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'TestDaemon' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `daemon` is a tree: bare (picker/plain list), `daemon start` (verbatim old RunE incl. DisabledError branch), `daemon stop`.
    - Bare non-TTY prints the plain list + exit 0; empty registry → "no running daemons".
    - No auto-spawn: only `daemon start` calls daemon.Run.
  </acceptance_criteria>
  <done>`codegraph daemon` is a picker/list, and the old foreground server lives on as `daemon start`.</done>
</task>

<task type="auto">
  <name>Task 3: daemon stop / stop --all subcommand</name>
  <files>internal/cli/daemon.go, internal/cli/daemon_test.go</files>
  <read_first>
    - internal/daemon/stop.go (StopMatching/StopAll — 07-04) and registry.go (List)
    - internal/cli/githooks.go (newGithooksInstallCmd RunE lines 28-54 — the single-purpose subcommand shape: resolve path, call domain, print status lines)
    - internal/cli/daemon.go (the --path/-p flag registration to mirror for stop; resolveStartPath in query.go)
    - 07-RESEARCH.md Open Question #3 (stop's target flag: default current repo via -p/--path, plus --all — Assumption A2)
  </read_first>
  <action>
    Add `newDaemonStopCmd()` to internal/cli/daemon.go (D-02). Flags: `-p`/`--path` (current repo default, mirroring daemon start) and `--all` (bool). RunE: if `--all`, call `daemon.StopAll()`; else resolve the repo path and call `daemon.StopMatching(repoRoot)`. Print one line per stopped daemon (pid + repo) and a "no running daemon for <repo>" / "no running daemons" notice + exit 0 when the result is empty (DMON-02 adjacency/empty edges — not an error). Surface any aggregated stop error as a non-zero exit. This subcommand is strictly non-interactive — it never opens a Program. Note in a comment that stop relies on 07-04's per-target corroboration (never signals an uncorroborated pid). Extend daemon_test.go: `stop --all` on an empty registry → clean no-op exit 0; `stop` with no matching daemon → notice + exit 0; `stop` against a seeded (injected-stop) live record → dispatches StopMatching. Use the injected stopSignal seam (07-04) so no real process is signaled.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'TestDaemonStop' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `daemon stop` (‑p default) and `daemon stop --all` exist; both non-interactive.
    - Empty/no-match → notice + exit 0 (not an error); a stop error exits non-zero.
    - Dispatches to daemon.StopMatching/StopAll (07-04, corroborated).
  </acceptance_criteria>
  <done>Users can explicitly stop the current-repo daemon or all daemons without the picker.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- NEW `internal/cli/tui/daemonpicker.go` → `RunDaemonPicker(cmd, currentRepo, records)` (+ injected `stopMatching`/`stopAll`).
- NEW `internal/cli/tui/daemonpicker_test.go`.
- MODIFIED `internal/cli/daemon.go` → `newDaemonCmd()` tree + `newDaemonStartCmd()` + `newDaemonStopCmd()`.
- MODIFIED `internal/cli/daemon_test.go`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal → daemon command | Bare daemon may open a Program; must gate on TTY |
| user intent → cross-process stop | The picker/stop cmd triggers real signals to registry pids |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07-01 | Denial of Service | bare daemon on non-TTY / piped | high | mitigate | tui.InteractiveAllowed gates before tea.NewProgram; non-TTY takes the plain-list-exit-0 path (D-12); enforced end-to-end by 07-08 |
| T-07-07-02 | Tampering / EoP | stop targeting a forged/stale pid | high | mitigate | stop-one/stop-all route through daemon.StopMatching/StopAll (07-04), which re-corroborate each pid via isStale before signaling — a poisoned record is never signaled |
| T-07-07-03 | Spoofing | implicit daemon spawn | low | mitigate | Bare daemon and stop never call daemon.Run; only `daemon start` does (D-03 no auto-spawn) |
</threat_model>

<verification>
- `go test ./internal/cli/ ./internal/cli/tui/` green.
- `go build ./...` green; archtest green.
- Bare daemon non-TTY exits 0 with the plain list; start preserves the DisabledError branch.
</verification>

<success_criteria>
- daemon is a picker/list + start + stop [--all]; TTY-gated; corroborated stops; no auto-spawn.
</success_criteria>

<output>
Create `.planning/phases/07-interactive-tui-daemon-picker-install-multi-select/07-07-SUMMARY.md` when done.
</output>
