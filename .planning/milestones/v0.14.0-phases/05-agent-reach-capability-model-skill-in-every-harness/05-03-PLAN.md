---
phase: 05-agent-reach-capability-model-skill-in-every-harness
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - internal/agents/claude.go
  - internal/agents/claude_symlink_test.go
  - internal/agents/claude_skillpackage_test.go
  - internal/agents/manifest.go
  - internal/agents/skillshared.go
autonomous: true
requirements: [AGENT-09, AGENT-13]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-17: Claude's installer compares `.claude/skills/codegraph` with the shared `.agents/skills/codegraph` for the same location using `sameSkillDir` (absolute, `filepath.EvalSymlinks`-resolved, dangling links followed) BEFORE writing; when they are one physical directory there is ONE package with ONE manifest, Claude is recorded in its `targets` like any other requester, and the shared-dir D-14 rule applies (`refuseUnmanifested`); when they are distinct, Claude's non-shared dir keeps its v0.10.0 behaviour (`adoptUnmanifested`, D-05 — untouched)."
    - "Claude's SKILL.md write and manifest go through 05-02's `writeSkillFile`/`recordSkillManifest` (manifest keys: SKILL.md, session-nudge.sh, SessionStart hooks fragment — unchanged); its Uninstall goes through `uninstallSkillPackage(…, Claude, [script key, hooks key], policy)`, so in a symlinked layout Claude's uninstall removes only itself (and its two exclusive keys) from the manifest and leaves the package for Cursor/opencode (D-08's last-requester rule); the session-nudge script and the SessionStart hook entry live outside the skill directory and are removed exactly as today."
    - "A user's symlink `~/.claude/skills/codegraph -> ../../.agents/skills/codegraph` is never unlinked by any install or uninstall, and a Claude install through that link after every requester uninstalled (dangling link) succeeds and recreates the target directory."
    - "`ConfiguredSkillLocations(Claude)` includes a location only when the manifest at Claude's manifest path lists `claude` among its requesters (legacy/no-targets and corrupt manifests still count — WR-04 preserved), so `codegraph upgrade`'s refresh never installs Claude where only Cursor/opencode wrote a package through the symlink (upgrade.go refresh prohibition: never touch a target the user had not installed)."
    - "Both writers compare (D-17 literal): the shared/harness writer (`installSkillPackage` for any requester other than Claude) also calls `sameSkillDir` against Claude's dir and, when equal, adds one WriteResult note naming both paths so the user sees that one package serves both."
    - "Every pre-existing Claude test passes with at most ADDITIVE assertion changes, and the `install-local` / `uninstall-local` plain goldens stay byte-identical without regeneration — Claude's reported files and their order are unchanged in the non-symlinked layout."
  artifacts:
    - path: "internal/agents/claude_symlink_test.go"
      provides: "TestSymlinkedSkillDir_ClaudeAndSharedAreOnePackage, TestSymlinkedSkillDir_ClaudeUninstallKeepsOtherRequester, TestSymlinkedSkillDir_DanglingLinkReinstall, TestSymlinkedSkillDir_ForeignContentKeptForeign, TestConfiguredSkillLocations_RequiresClaudeInTargets, TestSharedSkillWriter_NotesSameDirAsClaude"
    - path: "internal/agents/claude.go"
      provides: "Install/Uninstall skill steps on writeSkillFile/recordSkillManifest/uninstallSkillPackage with the sameSkillDir policy"
      contains: "sameSkillDir("
    - path: "internal/agents/manifest.go"
      provides: "ConfiguredSkillLocations requiring claude ∈ manifestRequesters"
  key_links:
    - from: "internal/agents/claude.go Install"
      to: "internal/agents/skillshared.go sameSkillDir + writeSkillFile + recordSkillManifest"
      via: "policy = refuseUnmanifested when same dir, else adoptUnmanifested"
      pattern: "sameSkillDir\\(|recordSkillManifest\\("
    - from: "internal/agents/claude.go Uninstall"
      to: "internal/agents/skillshared.go uninstallSkillPackage"
      via: "exclusiveKeys = manifestKeyScript, manifestKeyHooksFrag"
      pattern: "uninstallSkillPackage\\("
    - from: "internal/cli/upgrade.go refreshInstalledSkills"
      to: "internal/agents/manifest.go ConfiguredSkillLocations"
      via: "claude ∈ targets gate"
      pattern: "ConfiguredSkillLocations\\(agents\\.Claude\\)"
  prohibitions:
    - statement: "MUST NOT delete the shared skill package out from under Cursor/opencode when Claude is uninstalled in a symlinked layout (D-17) — TestSymlinkedSkillDir_ClaudeUninstallKeepsOtherRequester is the gate."
      status: resolved
      verification: test
    - statement: "MUST NOT make `codegraph upgrade` re-install Claude at a location where the user only installed other agents — TestConfiguredSkillLocations_RequiresClaudeInTargets is the gate."
      status: resolved
      verification: test
    - statement: "MUST NOT migrate Claude's package to `.agents/skills/` (deferred idea, A2 Q1) or change what Claude writes in the non-symlinked layout (D-05) — the unchanged install-local/uninstall-local goldens and pre-existing Claude tests are the gate."
      status: resolved
      verification: test
---

<objective>
Implement D-17, the correctness case the accepted D-05/D-08 did not cover: on the maintainer's machine
(and anywhere the `npx skills` convention is used) `~/.claude/skills/codegraph` is a symlink into
`~/.agents/skills/codegraph`, so Claude's existing package and the new shared package are ONE physical
directory. Move Claude's SKILL.md and manifest steps onto 05-02's manifest-owned primitives, choose the
foreign-content policy with a `filepath.EvalSymlinks`-based comparison, make Claude's uninstall follow
the last-requester rule, stop `codegraph upgrade` from treating another agent's manifest as proof Claude
was installed, and have the shared writer note the shared directory. This lands BEFORE any target writes
the shared path (05-04), so no released state ever has a shared write without D-17.

Purpose: AGENT-09 (one package, correctly shared) and AGENT-13 (Claude's uninstall reverses only its own
writes). Claude's non-symlinked behaviour is untouched (D-05): the install-local/uninstall-local goldens
must not change.

TDD: each task's failing tests first as `test(05-03):` (compile placeholders only), pasted `--- FAIL:`
transcript in the SUMMARY; GREEN as `feat(05-03):`/`fix(05-03):`. Never `[ci skip]`.

Output: `claude_symlink_test.go`, D-17-aware claude.go, ConfiguredSkillLocations fix, same-dir note.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-02-SUMMARY.md
@internal/agents/claude.go
@internal/agents/skillshared.go
@internal/agents/manifest.go
@internal/cli/upgrade.go
</context>

<interfaces>
From 05-02 (internal/agents/skillshared.go, manifest.go):
```go
type unmanifestedPolicy int // refuseUnmanifested, adoptUnmanifested
func sharedSkillDirPath(loc Location) (string, error)
func sameSkillDir(a, b string) (bool, error)
func writeSkillFile(result *WriteResult, dir string, policy unmanifestedPolicy) (content []byte, ok bool)
func recordSkillManifest(result *WriteResult, dir string, loc Location, requester TargetID, ownFiles map[string]string)
func installSkillPackage(result *WriteResult, dir string, loc Location, requester TargetID, policy unmanifestedPolicy)
func uninstallSkillPackage(result *WriteResult, dir string, requester TargetID, exclusiveKeys []string, policy unmanifestedPolicy)
func manifestRequesters(m skillManifest, present bool, readErr error) []TargetID // legacy/corrupt → [Claude]
const ActionKeptForeign FileAction = "kept (foreign)"
```
From internal/agents/claude.go (existing):
```go
func claudeSkillDirPath(loc Location) (string, error)
func claudeManifestPath(loc Location) (string, error)
func claudeHooksScriptPath(loc Location) (string, error)
func claudeSessionStartBlocks(loc Location) ([]any, []string, error)
func hashOwnedHookBlocks(blocks []any) (string, error)
const manifestKeySkillMD, manifestKeyScript, manifestKeyHooksFrag
```
</interfaces>

## Artifacts this phase produces

See `05-01-PLAN.md` → "Artifacts this phase produces". This plan creates `claude_symlink_test.go`, the
D-17 policy in claude.go, the `claude ∈ targets` gate in `ConfiguredSkillLocations`, and the same-dir
note in `installSkillPackage`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Claude's skill package on the manifest-owned primitives, policy chosen by an EvalSymlinks comparison (D-17)</name>
  <files>internal/agents/claude.go, internal/agents/claude_symlink_test.go, internal/agents/claude_skillpackage_test.go</files>
  <read_first>
    - internal/agents/claude.go lines 366-589 (Install's skill block with the have-flags, the manifest step, Uninstall's manifest-then-SKILL.md removal order)
    - internal/agents/skillshared.go (05-02 — the primitives and their reporting order)
    - internal/agents/claude_skillpackage_test.go (every Claude package test that must stay green — especially TestClaude_Uninstall_NeverInstalledIsNotFound, TestClaude_Uninstall_PreservesUserFileInSkillDir, TestClaude_Install_MissingManifestIsTreatedAsDrifted, TestClaude_Install_ManifestNotWrittenOnPartialWriteFailure, TestClaude_Install_WritesManifest)
    - internal/cli/testdata/plain/install-local.golden and uninstall-local.golden (the reported-file order that must not change)
    - internal/agents/testhelpers_test.go (fakeHome)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md Pitfall 2 (the real symlink: relative target `../../.agents/skills/codegraph`)
  </read_first>
  <behavior>
    - Fixture helper symlinkedClaudeLayout(t) (in claude_symlink_test.go): fakeHome, mkdir `<home>/.agents/skills/codegraph` and `<home>/.claude/skills`, os.Symlink("../../.agents/skills/codegraph", `<home>/.claude/skills/codegraph`) — the maintainer's exact relative shape
    - TestSymlinkedSkillDir_ClaudeAndSharedAreOnePackage: claude Install(global) → no errors; installSkillPackage(shared global dir, global, Cursor, refuse) → no errors; walking `<home>` WITHOUT following symlinks finds exactly one `.codegraph-manifest.json`; its targets set is {claude, cursor} and its files keys are the SKILL.md, script and hooks keys; the claude path is still a symlink (os.Lstat ModeSymlink)
    - TestSymlinkedSkillDir_ClaudeUninstallKeepsOtherRequester: continuing, claude Uninstall(global) → no errors; shared SKILL.md still equals claudeassets.SkillMarkdown(); manifest targets ["cursor"] with only the SKILL.md key; `<home>/.claude/hooks/session-nudge.sh` gone; settings.json carries no SessionStart command equal to claudeHookCommand(global); the link is still a symlink; then uninstallSkillPackage(shared, Cursor, nil, refuse) → SKILL.md and manifest gone, the real dir gone, the link still present (dangling)
    - TestSymlinkedSkillDir_DanglingLinkReinstall: from that dangling state, claude Install(global) → no errors; the real dir exists again with SKILL.md and a manifest whose targets are ["claude"]; the link resolves to it
    - TestSymlinkedSkillDir_ForeignContentKeptForeign: the real dir holds a foreign SKILL.md and no manifest → claude Install(global) reports a `kept (foreign)` entry for the claude skill dir, the foreign bytes are unchanged, no manifest exists anywhere under `<home>/.agents` or `<home>/.claude/skills`, and Claude's MCP config, CLAUDE.md, script and SessionStart hooks are still written; claude Uninstall(global) leaves the foreign SKILL.md byte-identical
    - TestClaude_Install_WritesManifest (existing, ADDITIVE assertion only): the decoded manifest's Targets equals ["claude"]
  </behavior>
  <action>
    RED first: add claude_symlink_test.go with the four TestSymlinkedSkillDir_* tests (they compile against 05-02's API with no placeholders needed) and the additive Targets assertion; commit `test(05-03): add failing symlinked-skill-dir tests for Claude (D-17)`; confirm they fail on assertions against today's Claude writer (its uninstall deletes the shared package; it overwrites foreign content; it errors through a dangling link); paste the `--- FAIL:` lines into the SUMMARY.

    GREEN — claude.go (D-17, D-05, D-08, D-14):
    - Add `claudeSkillPolicy(loc Location) (unmanifestedPolicy, error)`: resolve claudeSkillDirPath(loc) and sharedSkillDirPath(loc), `same, err := sameSkillDir(claudeDir, sharedDir)`; same → refuseUnmanifested; distinct → adoptUnmanifested; a comparison error is returned and the caller records it in result.Errors and uses refuseUnmanifested (the conservative choice — never adopt content we cannot place). Doc comment: D-17, the maintainer's real link shape, why the comparison uses filepath.EvalSymlinks.
    - Install: replace the inline SKILL.md write with `content, ok := writeSkillFile(&result, claudeDir, policy)` feeding skillMDContent/haveSkillMDContent; keep the script and SessionStart steps exactly as they are; replace the manual skillManifest build + writeManifest with `recordSkillManifest(&result, claudeDir, loc, Claude, map[string]string{manifestKeySkillMD: …, manifestKeyScript: …, manifestKeyHooksFrag: hooksHash})`, still gated on all three have-flags (CR-01 unchanged). When writeSkillFile reported `kept (foreign)`, the manifest step is skipped (the have-flag is false) while every other Claude step still runs.
    - Uninstall: replace the manifest-removal and SKILL.md-removal blocks with `uninstallSkillPackage(&result, claudeDir, Claude, []string{manifestKeyScript, manifestKeyHooksFrag}, policy)` in the same position; the script removal and removeHookEntry steps stay after it, unchanged.
    - Keep claudeManifestPath/claudeSkillFilePath (DescribePaths and ConfiguredSkillLocations still use them).
    Run the WHOLE agents package and TestPlainGolden/TestNoColorNonTTYRegression: every pre-existing test must pass with no edit other than the additive Targets assertion, and install-local/uninstall-local goldens must match WITHOUT -update-plain-goldens. Commit `fix(05-03): Claude's skill package shares one manifest with the shared dir when they are one directory (D-17)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(05-03\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestSymlinkedSkillDir_|TestClaude_' -v 2>&1 | tee /tmp/05-03-t1.txt | tail -3; rg -q '^ok\s' /tmp/05-03-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-03-t1.txt && test "$(rg -c '^--- PASS: TestSymlinkedSkillDir_' /tmp/05-03-t1.txt)" = "4" && rg -q 'sameSkillDir\(' internal/agents/claude.go && rg -q 'uninstallSkillPackage\(' internal/agents/claude.go && rg -q 'recordSkillManifest\(' internal/agents/claude.go && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestPlainGolden$|TestNoColorNonTTYRegression$' -v 2>&1 | tee /tmp/05-03-golden.txt | tail -2; rg -q '^ok\s' /tmp/05-03-golden.txt && rg -q -- '--- PASS: TestPlainGolden/install-local ' /tmp/05-03-golden.txt && rg -q -- '--- PASS: TestPlainGolden/uninstall-local ' /tmp/05-03-golden.txt && git diff --quiet HEAD -- internal/cli/testdata/plain/ && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 2>&1 | tee /tmp/05-03-agents.txt | tail -2; rg -q '^ok\s' /tmp/05-03-agents.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-03-agents.txt</automated>
    <fails_when>non-zero exit; no "test(05-03):" commit; build/gofmt/vet red; any "--- FAIL"/"FAIL" line; fewer than the 4 TestSymlinkedSkillDir_ PASS lines; claude.go not calling sameSkillDir, uninstallSkillPackage and recordSkillManifest; the install-local or uninstall-local golden subtest not PASS or any golden file modified; or the full agents package not "ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - no golden under internal/cli/testdata/plain/ changed in any 05-03 commit: `C=$(git log --format=%H -E --grep='^(test|fix|feat)\(05-03\)') && test -n "$C" && N=0 && for c in $C; do [ -z "$(git show --name-only --format= "$c" -- internal/cli/testdata/plain/)" ] || N=$((N+1)); done && test "$N" = 0`
    - the only edit to claude_skillpackage_test.go is additive (the Targets assertion) — `git log -p` for that file in 05-03 shows no removed assertion line
    - the SUMMARY pastes the RED `--- FAIL:` lines for all four symlink tests
  </acceptance_criteria>
  <done>In a symlinked layout Claude and the shared writer share one manifest, Claude's uninstall leaves other requesters' package intact, foreign content is kept, and a dangling link is healed — with Claude's ordinary behaviour byte-for-byte unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfiguredSkillLocations requires claude ∈ targets; the shared writer notes a shared directory</name>
  <files>internal/agents/manifest.go, internal/agents/skillshared.go, internal/agents/claude_symlink_test.go</files>
  <read_first>
    - internal/agents/manifest.go lines 166-198 (ConfiguredSkillLocations and its WR-04 corrupted-counts-as-present rule)
    - internal/cli/upgrade.go lines 30-73 (refreshInstalledSkills — "never a location or target the user had not already installed to")
    - internal/agents/manifest_test.go lines 195-270, 368-409 (the existing ConfiguredSkillLocations tests that must stay green)
    - internal/agents/skillshared.go installSkillPackage (05-02)
  </read_first>
  <behavior>
    - TestConfiguredSkillLocations_RequiresClaudeInTargets: symlinked layout with ONLY installSkillPackage(shared, global, Cursor) → ConfiguredSkillLocations(Claude) does not contain global; after claude Install(global) → contains global; in a plain fakeHome, a manifest at Claude's global manifest path with targets ["cursor"] only → excluded; a legacy manifest (no targets key) → included; a corrupt manifest → included
    - TestSharedSkillWriter_NotesSameDirAsClaude: symlinked layout → installSkillPackage(shared, global, Cursor) result.Notes has exactly one entry containing both the shared dir and the Claude dir paths; plain fakeHome → zero notes; requester Claude → zero notes
  </behavior>
  <action>
    RED first: add both tests to claude_symlink_test.go, commit `test(05-03): add failing upgrade-refresh gate and same-dir note tests`, confirm assertion failures, paste them.

    GREEN:
    - manifest.go ConfiguredSkillLocations: keep the two fixed candidate paths; a read error → include (WR-04); absent → skip; present → include only if Claude is among manifestRequesters(m, true, nil). Rewrite the doc comment's reasoning: with D-17 a manifest at Claude's path can belong only to other agents (the symlinked shared dir), and upgrade's refresh must never install Claude where the user never did.
    - skillshared.go installSkillPackage: when requester != Claude, resolve claudeSkillDirPath(loc) and, if sameSkillDir(dir, claudeDir) is true, append one note: `<dir> is the same directory as Claude Code's <claudeDir> — one skill package, one manifest listing every agent that installed it`. This is D-17's "both writers compare" on the shared-writer side; a comparison error is recorded in result.Errors.
    Run the whole agents package and internal/cli's upgrade tests (TestUpgrade*, refresh seam). Commit `fix(05-03): upgrade refresh requires Claude in the manifest's requesters; note a shared skill directory`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(git log --format=%s -20 | rg -c '^test\(05-03\): ')" -ge 2 && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/)" && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestConfiguredSkillLocations_|TestSharedSkillWriter_NotesSameDirAsClaude$' -v 2>&1 | tee /tmp/05-03-t2.txt | tail -3; rg -q '^ok\s' /tmp/05-03-t2.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-03-t2.txt && rg -q -- '--- PASS: TestConfiguredSkillLocations_RequiresClaudeInTargets ' /tmp/05-03-t2.txt && rg -q -- '--- PASS: TestSharedSkillWriter_NotesSameDirAsClaude ' /tmp/05-03-t2.txt && test "$(rg -c '^--- PASS: TestConfiguredSkillLocations_' /tmp/05-03-t2.txt)" -ge 4 && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 2>&1 | tee /tmp/05-03-pkgs.txt | tail -3; test "$(rg -c '^ok\s' /tmp/05-03-pkgs.txt)" = "2" && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-03-pkgs.txt</automated>
    <fails_when>non-zero exit; fewer than 2 "test(05-03):" commits; build/gofmt red; any "--- FAIL"/"FAIL" line; the two named tests not PASS; fewer than 4 TestConfiguredSkillLocations_ PASS lines (the three pre-existing ones plus the new one); or agents/cli not both "ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - manifest.go ConfiguredSkillLocations calls `manifestRequesters(`
    - TestConfiguredSkillLocations_ProbesFixedPaths, _IncludesLocationWithCorruptedManifest and _ExcludesGenuinelyAbsentLocation pass unmodified
    - `rg -q 'same directory as Claude Code' internal/agents/skillshared.go`
  </acceptance_criteria>
  <done>`codegraph upgrade` refreshes Claude only where Claude is a recorded requester, and a shared-directory install says so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-managed skill-root symlinks (`npx skills` convention) → Claude's installer | Claude's path may be another agent's package |
| manifest at Claude's path → `codegraph upgrade` refresh | refresh consent is inferred from the manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Denial of Service / Tampering | claudeTarget.Uninstall in a symlinked layout | high | mitigate | uninstallSkillPackage last-requester rule; TestSymlinkedSkillDir_ClaudeUninstallKeepsOtherRequester |
| T-05-12 | Tampering | claudeTarget.Install overwriting foreign content through a symlink | high | mitigate | refuseUnmanifested when sameSkillDir; TestSymlinkedSkillDir_ForeignContentKeptForeign |
| T-05-13 | Elevation of Privilege (consent) | upgrade refresh installing Claude where only other agents wrote | medium | mitigate | ConfiguredSkillLocations requires claude ∈ requesters; TestConfiguredSkillLocations_RequiresClaudeInTargets |
| T-05-14 | Tampering | unlinking the user's symlink | medium | mitigate | removeSkillDirIfEmpty Lstat guard (05-02) exercised end-to-end here; link asserted present after every uninstall |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1` green; plain goldens unchanged
</verification>

<success_criteria>
- D-17 implemented with a RED-first symlinked-layout test, D-05's non-symlinked Claude behaviour byte-identical, D-08 honoured for Claude
</success_criteria>

<output>
Create `.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-03-SUMMARY.md` when done — paste both RED transcripts and the golden PASS lines.
</output>
