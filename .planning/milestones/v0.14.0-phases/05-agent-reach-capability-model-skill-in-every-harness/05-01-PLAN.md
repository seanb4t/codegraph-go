---
phase: 05-agent-reach-capability-model-skill-in-every-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agents/types.go
  - internal/agents/capabilities.go
  - internal/agents/capabilities_test.go
  - internal/agents/claude.go
  - internal/agents/cursor.go
  - internal/agents/codex.go
  - internal/agents/opencode.go
  - internal/agents/hermes.go
  - internal/agents/gemini.go
  - internal/agents/antigravity.go
  - internal/agents/kiro.go
  - internal/agents/registry_test.go
  - internal/cli/tui/agentpicker_test.go
  - internal/cli/install.go
  - internal/cli/printconfigstyle.go
  - internal/cli/printconfigstyle_test.go
  - internal/cli/plain_golden_test.go
  - internal/cli/testdata/plain/print-config-style.golden
  - internal/cli/testdata/plain/print-config-style-local.golden
  - docs/CLI-REFERENCE.md
  - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md
autonomous: true
requirements: [AGENT-08]
user_setup: []

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`AgentTarget` (internal/agents/types.go) gains `Capabilities() Capabilities`; each of the eight target files returns exactly ONE `Capabilities{…}` struct literal whose path fields reference that target's EXISTING path functions (no path string restated), and the two interface fakes — `fakeTarget` in internal/agents/registry_test.go and `fakeAgentTarget` in internal/cli/tui/agentpicker_test.go — gain `Capabilities()` in the SAME commit as the interface change (D-01, D-02, RESEARCH Pitfall 1)."
    - "`Capabilities` declares `Scopes []Location`, `ConfigFormat` (json|jsonc|toml|yaml), `Hooks` (`HookMechanism`: none|claude-json|codex-json — exactly D-02's three values, RESEARCH Open Question 3), and per-location `MCPConfig`, `Instructions` (none allowed) and ordered `SkillDirs` whose index 0 is the directory written and whose later entries are documented read-paths only (D-02)."
    - "The table states TODAY's writes at this plan's HEAD (later plans edit single literals as they add writes): only Claude declares a skill dir; Codex is global-only, toml, instructions `~/.codex/AGENTS.md`, no skill, hooks none — the shape Phase 7 edits (CONTEXT domain); Cursor, Kiro, Antigravity and Hermes declare no instructions file (matching the code: Cursor's and Kiro's legacy rules/steering files are self-heal-DELETED, never written); Antigravity's MCPConfig resolves to the unified `~/.gemini/config/mcp_config.json` on a machine with no Antigravity config at all (the path Install actually writes), and to the legacy path only while an unmigrated legacy file exists."
    - "`SupportsLocation`, `DescribePaths` and `Detect` are derivations of `Capabilities()` in all eight targets: `SupportsLocation(loc)` returns `t.Capabilities().Supports(loc)`, `DescribePaths(loc)` is the one-line body `return describeDeclaredPaths(t, loc)`, and `Detect(loc).ConfigPath` equals `Capabilities().MCPConfig(loc)` for every supported location (zero `DetectionResult` for an unsupported one); Detect's installed-evidence fallback directory is computed as an ancestor of a table path, never a restated literal (D-02)."
    - "D-03 guard (ours, D-00): `TestCapabilitiesTableDrivesDerivations` (8 targets × 2 locations) flattens the table INDEPENDENTLY of `describeDeclaredPaths` and asserts set-equality with `DescribePaths(loc)`, `SupportsLocation` agreement and `Detect(loc).ConfigPath` agreement; `TestCapabilitiesMatchInstallWrites` asserts, under a fresh fake home and fresh cwd, that every declared path appears among `Install(loc)`'s reported files AND every created/updated/unchanged path Install reports is declared — the sole named exception being Antigravity's `~/.gemini/config/.migrated` migration marker."
    - "Family (a) in 05-MUTATION-LOG.md: (a1) a hand-written extra path appended to cursor's `DescribePaths` turns `TestCapabilitiesTableDrivesDerivations/cursor/…` RED; (a2) deleting `Instructions` from Claude's literal turns `TestCapabilitiesMatchInstallWrites/claude/…` RED; each reverted byte-clean with a green control after (D-03, rule 84d1gfpywd)."
    - "`codegraph install --print-config-style` is read-only (D-04): its branch runs immediately after `--location` parsing and before `--yes`/`--target`/picker resolution; it opens no picker, writes nothing (the real binary run with an empty HOME and an empty cwd leaves both empty), honours `-l/--location` and `-t/--target` (unchanged `--target` means every registered target), and prints one line per target in `AllTargetIDs` order: `<id>: scopes=<global,local> mcp=<path> format=<style> instructions=<path|none> skill=<dir|none> hooks=<mechanism>`, or `<id>: scopes=<…> (<loc> not supported)` for an unsupported location."
    - "The plain output is golden-frozen as ours (`print-config-style.golden` global, `print-config-style-local.golden` local) and byte-identical under `NO_COLOR=1`/`banana`; the styled branch goes through `resolveColor(cmd)` + `present.KV` and strips to the plain bytes; every printed path passes `sanitizePathForDisplay` in BOTH branches (D-04)."
    - "`docs/CLI-REFERENCE.md` is regenerated only through `task docs:cli` in the same commit that adds the flag; `task docs:cli:drift` and `TestEveryRegisteredFlagIsAccountedFor` are green with no allowlist entry for the new flag (D-04)."
    - "Install/Uninstall semantics are unchanged by this plan (D-01): every pre-existing `internal/agents` and `internal/cli` test, including all 29 prior plain goldens, passes unmodified."
    - "AGENT-08 (probe precision, explicit): a path-resolution error while printing is returned from RunE naming the target and field (non-zero exit) — never rendered as `none`."
    - "AGENT-08 (probe empty, explicit): `--target none` with `--print-config-style` prints `no agents selected` and exits 0; an unknown id errors before any output, exactly as `install --target` does today."
    - "AGENT-08 (probe ordering, explicit): scopes render in the fixed order global,local regardless of literal order; targets render in `AllTargetIDs` order for the default set and in `ResolveTargetFlag` order for an explicit list."
  artifacts:
    - path: "internal/agents/capabilities.go"
      provides: "HookMechanism (HooksNone/HooksClaudeJSON/HooksCodexJSON), ConfigFormat (ConfigFormatJSON/JSONC/TOML/YAML), PathFunc, PathsFunc, Capabilities{Scopes, ConfigFormat, Hooks, MCPConfig, Instructions, SkillDirs}, (Capabilities).Supports/InstructionsPath/WrittenSkillDir/ReadOnlySkillDirs/HookFiles, skillFileName, skillManifestFileName, globalOnlyPath, describeDeclaredPaths"
      contains: "func describeDeclaredPaths("
    - path: "internal/agents/capabilities_test.go"
      provides: "TestCapabilitiesDeclared, TestCapabilitiesTableDrivesDerivations, TestCapabilitiesMatchInstallWrites"
    - path: "internal/cli/printconfigstyle.go"
      provides: "printConfigStyle(cmd, targets, loc), configStyleFields(t, loc)"
    - path: "internal/cli/printconfigstyle_test.go"
      provides: "TestInstallPrintConfigStyle, TestInstallPrintConfigStyle_ReadOnly, TestInstallPrintConfigStyle_Filters, TestInstallPrintConfigStyle_StyledStripsToPlain"
    - path: "internal/cli/testdata/plain/print-config-style.golden"
      provides: "frozen plain --print-config-style output, global"
    - path: ".planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md"
      provides: "Phase 5 mutation log, Family (a)"
      contains: "## Family (a)"
  key_links:
    - from: "internal/agents/types.go AgentTarget"
      to: "internal/agents/<target>.go Capabilities()"
      via: "interface method, one literal per target file"
      pattern: "func \\(t? ?[a-z]+Target\\) Capabilities\\(\\) Capabilities"
    - from: "internal/agents/<target>.go DescribePaths"
      to: "internal/agents/capabilities.go describeDeclaredPaths"
      via: "one-line derivation body"
      pattern: "return describeDeclaredPaths\\(t, loc\\)"
    - from: "internal/cli/install.go RunE"
      to: "internal/cli/printconfigstyle.go printConfigStyle"
      via: "--print-config-style early branch after parseLocationFlag"
      pattern: "printConfigStyle\\("
  prohibitions:
    - statement: "MUST NOT write, create or truncate any file, or open the interactive picker, when --print-config-style is set — the real-binary empty-HOME/empty-cwd check and TestInstallPrintConfigStyle_ReadOnly are the gates (D-04)."
      status: resolved
      verification: test
    - statement: "MUST NOT keep a second hand-written copy of any path the table holds — no Capabilities literal restates a path string and no DescribePaths/Detect/SupportsLocation body calls a target path function directly; the D-03 guard plus Family (a) are the gate (D-02, D-03)."
      status: resolved
      verification: test
    - statement: "MUST NOT change what Install or Uninstall writes or removes because the table exists (D-01) — every pre-existing agents/cli test and all prior plain goldens pass unmodified."
      status: resolved
      verification: test
---

<objective>
The tracer for the whole phase: prove on the thinnest end-to-end slice that ONE per-target capability
table drives the installer's reporting surfaces before any new write path exists. Add
`Capabilities()` to `AgentTarget` with one literal per target file (D-01/D-02), make `SupportsLocation`,
`DescribePaths` and `Detect`'s path inputs derivations of it, guard the derivation with the D-03
table test (Family (a) RED), and ship `codegraph install --print-config-style` as a read-only,
golden-frozen rendering of the table through Phase 4's palette (D-04), with `docs/CLI-REFERENCE.md`
regenerated through the drift gate in the same commit so the flag-accounting test never goes red.

Purpose: AGENT-08. Codex (Phase 7) is the table's first real consumer and every later plan in this
phase adds a skill write by editing ONE literal; if the table's shape is wrong, better to learn it on a
read-only report than after five harnesses are wired. D-00 applies throughout: tests assert only what
this repo owns — the paths the table declares, the bytes the flag prints — never a harness's behaviour.

Planner notes (read before executing):
- File count: 11 of this plan's 21 files are the compile-atomic interface change Go forces into one
  commit (types.go, eight one-literal target edits, the two fakes — RESEARCH Pitfall 1); the rest are
  the new capabilities/printconfigstyle files, their tests, two goldens, the generated reference and the
  mutation log. Every internal/agents plan in this phase is sequential (one Go package, committed RED
  tests and planted-mutation windows make same-wave execution in one working tree unsafe).
- CONTEXT's code-insights line says Cursor writes `.cursor/rules/codegraph.mdc` and Kiro writes
  `.kiro/steering/codegraph.md`. The code does the opposite: both files are LEGACY and are self-heal
  DELETED on install (cursor.go:9-16, kiro.go:14-18); neither target writes an instructions file. The
  table therefore declares `instructions=none` for both. D-06(d)'s and D-11's operative content ("no
  change" / "the default needs no code change") is honoured exactly by this.
- `antigravityConfigPath()` returns the LEGACY path on a machine with no Antigravity config at all,
  while `Install` always writes the unified path — so the table would print a path install never
  writes. This plan narrows that one function (fresh machine → unified; unmigrated legacy file present
  → legacy; migrated → unified). Detect's `Installed`/`AlreadyConfigured` outcomes and Uninstall's
  results are unchanged in every case (both files absent on a fresh machine either way).
- TDD (`workflow.tdd_mode`): Task 1's failing tests land as a `test(05-01):` commit with only the
  compile placeholders they need (Phase 3 03-02 precedent), the pasted `--- FAIL:` transcript in the
  SUMMARY is the RED evidence (rule x1cjy9vyhq); never gate on `gsd-tools check tdd-red-evidence`.
  Task 2 is a behaviour-preserving derivation refactor whose guard is a characterization test — its
  positive control is Family (a), not a RED-first commit. Never `[ci skip]`/`[skip ci]`.

Output: `test(05-01):` RED commit, `feat(05-01):` GREEN commit(s), `refactor(05-01):` derivation
commit, `05-MUTATION-LOG.md` Family (a), regenerated `docs/CLI-REFERENCE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-PATTERNS.md
@.planning/phases/04-cli-glow-up/04-MUTATION-LOG.md
@internal/agents/types.go
@internal/agents/registry.go
@internal/agents/claude.go
@internal/agents/cursor.go
@internal/agents/codex.go
@internal/agents/opencode.go
@internal/agents/hermes.go
@internal/agents/gemini.go
@internal/agents/antigravity.go
@internal/agents/kiro.go
@internal/cli/install.go
@internal/cli/plain_golden_test.go
</context>

<interfaces>
<!-- Extracted from the codebase this planning session. Use directly. -->

From internal/agents/types.go (unchanged surface this plan extends):
```go
type Location string            // LocationGlobal = "global", LocationLocal = "local"
type TargetID string            // Claude, Cursor, Codex, Opencode, Hermes, Gemini, Antigravity, Kiro
type AgentTarget interface {
	ID() TargetID
	DisplayName() string
	SupportsLocation(loc Location) bool
	Detect(loc Location) DetectionResult   // DetectionResult{Installed, AlreadyConfigured bool; ConfigPath string}
	Install(loc Location, opts InstallOptions) WriteResult
	Uninstall(loc Location) WriteResult
	DescribePaths(loc Location) []string
	// NEW (this plan): Capabilities() Capabilities
}
```

Existing path functions each literal references (never restate their strings):
```go
claudeConfigPath(loc) (string, error)        // ~/.claude.json | .mcp.json
claudeInstructionsPath(loc) (string, error)  // ~/.claude/CLAUDE.md | .claude/CLAUDE.md
claudeSkillDirPath(loc) (string, error)      // ~/.claude/skills/codegraph | .claude/skills/codegraph
claudeSettingsPath(loc), claudeHooksScriptPath(loc)   // HookFiles for HooksClaudeJSON
cursorConfigPath(loc)                         // ~/.cursor/mcp.json | .cursor/mcp.json
codexConfigPath() / codexInstructionsPath()   // global-only, no Location arg → globalOnlyPath adapter
opencodeConfigPath(loc) / opencodeInstructionsPath(loc)
hermesConfigPath()                            // global-only
geminiConfigPath(loc) / geminiInstructionsPath(loc)   // GEMINI.md at project ROOT for local
antigravityConfigPath()                       // global-only, migration-aware (narrowed this plan)
kiroConfigPath(loc)
```

From internal/cli (reused, unchanged):
```go
func resolveColor(cmd *cobra.Command) colorMode            // colorflag.go — single env read; mode.Styled, mode.Dark, mode.Writer(w)
func present.NewPalette(dark bool) present.Palette
func present.KV(w io.Writer, pal present.Palette, label, value string) error   // label + " " + value + "\n", value sanitized
func sanitizePathForDisplay(s string) string               // uninit.go — strips control runes
func agents.ResolveTargetFlag(spec string, loc agents.Location) ([]agents.AgentTarget, error)
func agents.AllTargets() []agents.AgentTarget              // sorted by ID: antigravity, claude, codex, cursor, gemini, hermes, kiro, opencode
```

Target shape this plan creates in internal/agents/capabilities.go (names are binding — later plans and
the Family (a) mutations target them):
```go
type HookMechanism string // HooksNone "none", HooksClaudeJSON "claude-json", HooksCodexJSON "codex-json"
type ConfigFormat string  // ConfigFormatJSON "json", ConfigFormatJSONC "jsonc", ConfigFormatTOML "toml", ConfigFormatYAML "yaml"
type PathFunc func(Location) (string, error)
type PathsFunc func(Location) ([]string, error)
type Capabilities struct {
	Scopes       []Location
	ConfigFormat ConfigFormat
	Hooks        HookMechanism
	MCPConfig    PathFunc  // required for every scope in Scopes
	Instructions PathFunc  // nil, or "" at a scope = no instructions file
	SkillDirs    PathsFunc // nil/empty = none; [0] written, [1:] documented read-paths only
}
func (c Capabilities) Supports(loc Location) bool
func (c Capabilities) InstructionsPath(loc Location) (string, error)    // "" when none
func (c Capabilities) WrittenSkillDir(loc Location) (string, error)     // "" when none
func (c Capabilities) ReadOnlySkillDirs(loc Location) ([]string, error) // SkillDirs[1:]
func (c Capabilities) HookFiles(loc Location) ([]string, error)         // claude-json → [claudeSettingsPath, claudeHooksScriptPath]; none → nil; codex-json → errHookFilesUndeclared
const skillFileName = "SKILL.md"
const skillManifestFileName = ".codegraph-manifest.json"
func globalOnlyPath(fn func() (string, error)) PathFunc
func describeDeclaredPaths(t AgentTarget, loc Location) []string
```
</interfaces>

## Artifacts this phase produces

Phase-wide list (every later plan points here and lists its own subset):

| Artifact | Kind | Plan |
|---|---|---|
| `Capabilities` type, `(Capabilities).Supports/InstructionsPath/WrittenSkillDir/ReadOnlySkillDirs/HookFiles` | Go API, internal/agents/capabilities.go | 05-01 |
| `HookMechanism` + `HooksNone`/`HooksClaudeJSON`/`HooksCodexJSON`; `ConfigFormat` + `ConfigFormatJSON`/`JSONC`/`TOML`/`YAML`; `PathFunc`, `PathsFunc` | Go types/consts | 05-01 |
| `AgentTarget.Capabilities()` + eight literals + two fake stubs | interface method | 05-01 |
| `describeDeclaredPaths`, `globalOnlyPath`, `skillFileName`, `skillManifestFileName` | Go helpers/consts | 05-01 |
| `--print-config-style` flag on `install`; `printConfigStyle`, `configStyleFields` | CLI, internal/cli/printconfigstyle.go | 05-01 |
| `print-config-style.golden`, `print-config-style-local.golden` | plain goldens | 05-01 (re-frozen 05-04, 05-05, maybe 05-07) |
| `TestCapabilitiesDeclared`, `TestCapabilitiesTableDrivesDerivations`, `TestCapabilitiesMatchInstallWrites` | tests | 05-01 |
| `05-MUTATION-LOG.md` (Families (a), (b)) | evidence | 05-01, 05-04 |
| `skillManifest.Targets` (`targets`, omitempty), `manifestSchemaVersion = 2`, `manifestRequesters`, `targetSetEqual` | manifest schema (D-07) | 05-02 |
| `ActionKeptForeign` (`"kept (foreign)"`), `unmanifestedPolicy` (`refuseUnmanifested`/`adoptUnmanifested`) | Go consts | 05-02 |
| `sharedSkillDirPath`, `sharedSkillDirs`, `skillManifestPath`, `skillDirIsForeign`, `writeSkillFile`, `recordSkillManifest`, `installSkillPackage`, `uninstallSkillPackage`, `resolveSkillDir`, `sameSkillDir` | shared writer, internal/agents/skillshared.go | 05-02 |
| `removeSkillDirIfEmpty` never unlinks a symlink | shared.go guard | 05-02 |
| `TestSharedSkillPackage_*`, `TestSkillPackage_*`, `TestRemoveSkillDirIfEmpty_NeverUnlinksSymlink`, `TestManifest_TargetsRoundTripAndSetEquality` | tests | 05-02 |
| Claude writer on the manifest-owned primitives with the EvalSymlinks same-dir policy; `ConfiguredSkillLocations` requires `claude ∈ targets`; same-dir note | D-17 | 05-03 |
| `claude_symlink_test.go` (`TestSymlinkedSkillDir_*`, `TestConfiguredSkillLocations_RequiresClaudeInTargets`) | tests | 05-03 |
| `installDeclaredSkill`, `uninstallDeclaredSkill` | table-driven skill wiring, capabilities.go | 05-04 |
| `ownership_test.go` (`TestOwnershipExactIdentity`, 242ec0a cited) | D-13 guard | 05-04 (oracle extended 05-05) |
| `skillfrontmatter_test.go` (`TestSkillFrontmatterMatchesEveryWrittenDir`) | test | 05-04 |
| Cursor + opencode shared `.agents/skills/codegraph/` writes | per-harness wiring | 05-04 |
| `geminiSkillDirs`, `kiroSkillDirs`, `antigravitySkillDirs` + their writes | per-harness wiring | 05-05 |
| `05-LIVE-SESSIONS.md` | live evidence (D-09…D-12) | 05-06 |
| Cursor repo-root `AGENTS.md` instructions + `installDeclaredInstructions`/`uninstallDeclaredInstructions` shared-file rule — ONLY if the D-11 verdict is `pickup observed` | conditional | 05-07 |
| regenerated `docs/CLI-REFERENCE.md`; `install`/`uninstall` help text naming the skill package | docs | 05-01, 05-07 |

## Flagged assumptions (spec-less edge probe — phase-wide, no silent drop)

The edge probe classified 12 rows (0 resolved). Six are resolved as explicit truths in the plans
that own them (AGENT-09 adjacency/empty/ordering → 05-02 `must_haves.truths`; AGENT-13
adjacency/empty/ordering → 05-04 `must_haves.truths`). The six `unclassified` rows are surfaced here,
never dropped (12 = 6 explicit + 6 flagged):

1. **AGENT-08 (unclassified)** — a table-shape requirement, not a data-shape one; its checkable
   content is the D-03 derivation-equality guard and the golden (this plan). No additional edge test.
2. **AGENT-04 (unclassified)** — Cursor: the write is unit-tested (05-04), the READ is live evidence only
   (05-06, D-00); the D-11 AGENTS.md verdict is recorded either way.
3. **AGENT-06 (unclassified)** — opencode: write + frontmatter bytes unit-tested (05-04); loading and
   the duplicate-name behaviour are live evidence (05-06, RESEARCH Pitfall 3).
4. **AGENT-07 (unclassified)** — Antigravity: write unit-tested (05-05); `agy` reading
   `~/.gemini/antigravity-cli/skills/` and `~/.gemini/GEMINI.md` is live evidence (05-06); the 2.0/IDE
   path stays `[ASSUMED]`, documented-only, not written (D-06(b)).
5. **AGENT-10 (unclassified)** — Gemini CLI is not installed (D-10); write unit-tested (05-05), read
   recorded `[ASSUMED]` with doc URL + fetch date (05-06).
6. **AGENT-11 (unclassified)** — Kiro is not installed (D-10); same treatment as AGENT-10; "AGENTS.md
   retained as a steering source" means NO change to Kiro's instructions (D-06(d)).

Additional flagged assumption: the D-13 foreign-instructions fixture must end in exactly ONE trailing
newline — `replaceOrAppendMarkedSection` (pre-existing, T-06-02) normalizes trailing whitespace on the
append path, so other shapes do not round-trip byte-for-byte. Recorded in 05-04, not changed here.

<!-- planner-discipline-allow: print-config-style -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "install --print-config-style renders the capability table" — plain path, all eight targets</name>
  <files>internal/agents/types.go, internal/agents/capabilities.go, internal/agents/capabilities_test.go, internal/agents/claude.go, internal/agents/cursor.go, internal/agents/codex.go, internal/agents/opencode.go, internal/agents/hermes.go, internal/agents/gemini.go, internal/agents/antigravity.go, internal/agents/kiro.go, internal/agents/registry_test.go, internal/cli/tui/agentpicker_test.go, internal/cli/install.go, internal/cli/printconfigstyle.go, internal/cli/printconfigstyle_test.go</files>
  <read_first>
    - internal/agents/types.go (AgentTarget interface lines 124-160 — add the method after DescribePaths with a doc comment naming D-01/D-02 and "SupportsLocation, DescribePaths and Detect's path inputs are derivations of this table")
    - internal/agents/codex.go lines 26-44 (the one-line-per-property shape and the no-arg global path functions — the template literal)
    - every target file's path functions (claude.go 80-178, cursor.go 27-51, opencode.go 36-88, hermes.go 33-45, gemini.go 24-46, antigravity.go 29-76, kiro.go 29-52) — the literals reference these, never restate them
    - internal/agents/registry_test.go lines 10-26 and internal/cli/tui/agentpicker_test.go lines 12-33 (the two fakes — Pitfall 1: both packages stop compiling otherwise)
    - internal/agents/antigravity_test.go (existing migration expectations the antigravityConfigPath narrowing must keep green)
    - internal/cli/install.go (flag registration lines 107-110, the RunE switch lines 79-98, printAgentResults' styled/plain branch lines 141-209)
    - internal/cli/install_test.go lines 1-80 (fakeHome + t.Chdir, execCmd, the interactiveAllowed/runAgentPicker seam idiom)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-01, D-02, D-04)
  </read_first>
  <behavior>
    - TestCapabilitiesDeclared pins, per registered target, an explicit expected row (an independent oracle, not read back from the code): antigravity scopes [global] json none, instructions none, skill none; claude [global,local] json claude-json, instructions `<HOME>/.claude/CLAUDE.md` | `.claude/CLAUDE.md`, skill `<HOME>/.claude/skills/codegraph` | `.claude/skills/codegraph`; codex [global] toml none, instructions `<HOME>/.codex/AGENTS.md`, skill none; cursor [global,local] json none, instructions none, skill none; gemini [global,local] json none, instructions `<HOME>/.gemini/GEMINI.md` | `GEMINI.md`, skill none; hermes [global] yaml none, instructions none, skill none; kiro [global,local] json none, instructions none, skill none; opencode [global,local] jsonc none, instructions `<HOME>/.config/opencode/AGENTS.md` | `AGENTS.md`, skill none; MCPConfig resolves without error at every declared scope; Antigravity MCPConfig under a fresh fake home == `<HOME>/.gemini/config/mcp_config.json`, and == `<HOME>/.gemini/antigravity/mcp_config.json` once only that legacy file exists
    - TestInstallPrintConfigStyle: under cli's fakeHome, `install --print-config-style` stdout has exactly 8 lines, equal (in AllTargetIDs order) to lines built by a TEST-LOCAL formatter over agents.AllTargets()' Capabilities; the claude line is pinned literally as `claude: scopes=global,local mcp=<HOME>/.claude.json format=json instructions=<HOME>/.claude/CLAUDE.md skill=<HOME>/.claude/skills/codegraph hooks=claude-json`
    - TestInstallPrintConfigStyle_ReadOnly: with interactiveAllowed forced true and a runAgentPicker stub that calls t.Fatal, `install --print-config-style` (and again with `--yes`) succeeds, the picker stub is never called, and a recursive walk of the fake home and the project dir finds zero entries afterwards
    - TestInstallPrintConfigStyle_Filters: `-t claude,codex -l local` prints exactly two lines — the claude local line and `codex: scopes=global (local not supported)`; `-t none` prints `no agents selected`; `-t bogus` returns an error containing `unknown agent target` and prints no target line
  </behavior>
  <action>
    RED first (TDD, rule x1cjy9vyhq): write internal/agents/capabilities_test.go (TestCapabilitiesDeclared) and internal/cli/printconfigstyle_test.go (the three CLI tests) per the behavior block, plus ONLY the compile placeholders they need: the `Capabilities() Capabilities` method on the AgentTarget interface; internal/agents/capabilities.go declaring the types, consts and methods from the interfaces block with zero-value bodies; `Capabilities()` returning `Capabilities{}` in all eight target files AND both fakes (registry_test.go fakeTarget, agentpicker_test.go fakeAgentTarget — same commit, Pitfall 1); and the `--print-config-style` bool flag registered on install with no branch yet. Commit as `test(05-01): add failing capability-table and --print-config-style tests`. Run the named tests and confirm each fails on an ASSERTION (not a build error); paste the `--- FAIL:` lines into the SUMMARY.

    GREEN — internal/agents/capabilities.go (D-01, D-02): implement the interfaces-block surface. `Supports` is membership in Scopes. `InstructionsPath`/`WrittenSkillDir`/`ReadOnlySkillDirs` return ""/nil for a nil func or an empty result. `HookFiles`: HooksClaudeJSON → [claudeSettingsPath(loc), claudeHooksScriptPath(loc)]; HooksNone → nil; HooksCodexJSON → the package error `errHookFilesUndeclared` (no target declares it this phase; a Phase 7 literal that switches Codex without declaring its files must fail the D-03 guard loudly rather than print nothing). `globalOnlyPath(fn)` returns a PathFunc that calls fn only for LocationGlobal. `describeDeclaredPaths(t, loc)`: nil when `!t.Capabilities().Supports(loc)`; otherwise, in order, MCPConfig, InstructionsPath (if non-empty), HookFiles, then `filepath.Join(WrittenSkillDir, skillFileName)` and `filepath.Join(WrittenSkillDir, skillManifestFileName)` when a skill dir is declared — each resolution error skips that entry exactly as today's per-target DescribePaths do, duplicates removed. Doc comment: D-02's derivation contract and that `ReadOnlySkillDirs` are documented read paths only, never written.

    GREEN — one literal per target file (D-01), placed directly under SupportsLocation, referencing path functions by name: claude {Scopes global+local, JSON, HooksClaudeJSON, MCPConfig claudeConfigPath, Instructions claudeInstructionsPath, SkillDirs: a small PathsFunc wrapping claudeSkillDirPath}; cursor {global+local, JSON, none, cursorConfigPath}; codex {global, TOML, none, globalOnlyPath(codexConfigPath), globalOnlyPath(codexInstructionsPath)} with a comment that Phase 7 (CODEX-01..04) edits this literal; opencode {global+local, JSONC, none, opencodeConfigPath, opencodeInstructionsPath}; hermes {global, YAML, none, globalOnlyPath(hermesConfigPath)} (no instructions by design, hermes.go:20-22); gemini {global+local, JSON, none, geminiConfigPath, geminiInstructionsPath}; antigravity {global, JSON, none, globalOnlyPath(antigravityConfigPath)} with a comment that its instructions reach Antigravity only through Gemini's `~/.gemini/GEMINI.md` (D-06(c)) so it declares none of its own; kiro {global+local, JSON, none, kiroConfigPath} with a comment that "AGENTS.md retained as a steering source" is Kiro reading files it already reads — no instructions write (D-06(d)). Switch claudeSkillFilePath/claudeManifestPath to join with skillFileName/skillManifestFileName (no behaviour change). Narrow `antigravityConfigPath()`: return the unified path when the migrated marker exists, the unified file exists, OR the legacy file does NOT exist; return legacy only for an unmigrated machine whose legacy file exists — keep the existing doc comment and add one sentence explaining the fresh-machine case (the table must print the path Install writes).

    GREEN — internal/cli/printconfigstyle.go (D-04): `func configStyleFields(t agents.AgentTarget, loc agents.Location) (string, error)` builds `scopes=<csv> mcp=<p> format=<f> instructions=<p|none> skill=<dir|none> hooks=<h>` (scopes rendered in the fixed order global,local filtered by membership; every path through sanitizePathForDisplay), or `scopes=<csv> (<loc> not supported)` when unsupported; a resolution error is returned wrapped as `<id>: resolve <field> path: <err>`. `func printConfigStyle(cmd *cobra.Command, targets []agents.AgentTarget, loc agents.Location) error`: zero targets → `no agents selected`; else for each target `fmt.Fprintf(out, "%s: %s\n", t.ID(), fields)` (plain only in this task — Task 3 adds the styled branch). In install.go add `var printCfgStyle bool` registered as `--print-config-style` (no shorthand) with usage "print each agent's capability table (scopes, MCP config, format, instructions, skill dir, hooks) and exit without writing anything"; in RunE, immediately after parseLocationFlag and BEFORE os.Executable and the target switch: if set, targets = agents.AllTargets() when `--target` was not changed, else agents.ResolveTargetFlag(target, loc) (error → `codegraph install: %w`), then `return printConfigStyle(cmd, targets, loc)`. Commit GREEN as `feat(05-01): capability table on AgentTarget and read-only install --print-config-style`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(05-01\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/ internal/cli/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ ./internal/cli/... && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ ./internal/cli/tui/ -count=1 -run 'TestCapabilitiesDeclared$|TestInstallPrintConfigStyle$|TestInstallPrintConfigStyle_ReadOnly$|TestInstallPrintConfigStyle_Filters$|TestAgentPickerModel_' -v 2>&1 | tee /tmp/05-01-t1.txt | tail -4; test "$(rg -c '^ok\s' /tmp/05-01-t1.txt)" = "3" && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-01-t1.txt && test "$(rg -c '^--- PASS: (TestCapabilitiesDeclared|TestInstallPrintConfigStyle|TestInstallPrintConfigStyle_ReadOnly|TestInstallPrintConfigStyle_Filters) ' /tmp/05-01-t1.txt)" = "4" && GOTOOLCHAIN=go1.26.6 go build -o /tmp/05-01-bin ./cmd/codegraph && H=$(mktemp -d) && P=$(mktemp -d) && (cd "$P" && HOME="$H" XDG_CONFIG_HOME="$H/.config" HERMES_HOME="$H/.hermes" /tmp/05-01-bin install --print-config-style > /tmp/05-01-pcs.txt) && test "$(rg -c '^[a-z]+: scopes=' /tmp/05-01-pcs.txt)" = "8" && rg -q '^codex: scopes=global mcp=.*format=toml instructions=.*AGENTS\.md skill=none hooks=none$' /tmp/05-01-pcs.txt && test -z "$(find "$H" "$P" -mindepth 1 -print -quit)" && rm -rf "$H" "$P" && echo "tracer green: 8 table lines from the real binary, zero files written"</automated>
    <fails_when>non-zero exit; no "test(05-01):" commit in the last 20; build/gofmt/vet red; fewer than 3 "ok" package lines or any "--- FAIL"/"FAIL" line in /tmp/05-01-t1.txt; fewer than the 4 named top-level PASS lines; the real binary printing other than 8 "<id>: scopes=" lines, a codex line not matching the global-only toml shape, or ANY file appearing under the scratch HOME or cwd</fails_when>
  </verify>
  <acceptance_criteria>
    - internal/agents/types.go's AgentTarget interface contains `Capabilities() Capabilities`
    - internal/agents/capabilities.go defines `type Capabilities struct`, `HooksNone`, `HooksClaudeJSON`, `HooksCodexJSON`, `ConfigFormatJSON`, `ConfigFormatJSONC`, `ConfigFormatTOML`, `ConfigFormatYAML`, `func describeDeclaredPaths(`, `func globalOnlyPath(`, `skillFileName`, `skillManifestFileName`
    - each of the eight target files contains exactly one `Capabilities() Capabilities {` method: `test "$(rg -o 'Target\) Capabilities\(\) Capabilities \{' internal/agents/{claude,cursor,codex,opencode,hermes,gemini,antigravity,kiro}.go | wc -l | tr -d ' ')" = "8"`
    - both fakes define `Capabilities()`: `rg -q 'func \(f fakeTarget\) Capabilities\(\)' internal/agents/registry_test.go && rg -q 'func \(f fakeAgentTarget\) Capabilities\(\)' internal/cli/tui/agentpicker_test.go`
    - the `test(05-01):` commit exists and contains the two new test files; the SUMMARY carries pasted `--- FAIL:` lines for all four tests
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1` is green — every pre-existing test unmodified (D-01)
  </acceptance_criteria>
  <done>One capability table exists on every target, the two fakes compile, and `codegraph install --print-config-style` prints the table from the real binary without writing a byte — committed RED then GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: SupportsLocation, DescribePaths and Detect become derivations of the table; the D-03 guard</name>
  <files>internal/agents/claude.go, internal/agents/cursor.go, internal/agents/codex.go, internal/agents/opencode.go, internal/agents/hermes.go, internal/agents/gemini.go, internal/agents/antigravity.go, internal/agents/kiro.go, internal/agents/capabilities_test.go</files>
  <read_first>
    - each target's current SupportsLocation, Detect and DescribePaths bodies (the hand-written versions this task replaces)
    - internal/agents/capabilities.go (Task 1 output — describeDeclaredPaths, Supports, HookFiles)
    - internal/agents/testhelpers_test.go (fakeHome — isolates HOME/XDG_CONFIG_HOME/HERMES_HOME)
    - internal/agents/claude_skillpackage_test.go lines 779-956 (TestClaude_DescribePaths_NoDuplicates, TestClaude_DescribePaths_IncludesManifest — the 6-path Claude expectation must stay green unmodified)
    - internal/agents/antigravity.go Install (lines 122-210 — the `.migrated` marker is the one bookkeeping file Install writes that the table does not declare)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Rewrite, in all eight target files, with a named receiver `t` (D-02 — the table is the single source; no second hand-written copy):
    - `SupportsLocation(loc Location) bool` → body `return t.Capabilities().Supports(loc)`.
    - `DescribePaths(loc Location) []string` → the exact one-line body `return describeDeclaredPaths(t, loc)` (Family (a1) in Task 3 targets this literal in cursor.go).
    - `Detect(loc Location) DetectionResult` → `caps := t.Capabilities()`; unsupported loc → zero DetectionResult; `configPath, err := caps.MCPConfig(loc)` (error → zero result, as today); keep each target's own AlreadyConfigured probe (mcpEntryPresent / opencodeMcpEntryPresent / findTOMLTableRange / hermesConfigured) on configPath; compute the installed-evidence fallback as an ancestor of a table path instead of a restated literal: claude global → filepath.Dir of caps.InstructionsPath(global) (= ~/.claude); cursor, codex, opencode, gemini, hermes → filepath.Dir(configPath); kiro → filepath.Dir(filepath.Dir(configPath)); antigravity → filepath.Dir(filepath.Dir(configPath)) (= ~/.gemini for both unified and legacy). Installed/AlreadyConfigured outcomes must be identical to today's for every existing test.

    Add to internal/agents/capabilities_test.go (these assert only OUR contract — D-00):
    - `TestCapabilitiesTableDrivesDerivations`: for every agents.AllTargets() target × {global, local}, subtest `<id>/<loc>` under fakeHome + t.Chdir(t.TempDir()): `SupportsLocation(loc)` == membership of loc in `Capabilities().Scopes`; for a supported loc, build the expected set INDEPENDENTLY of describeDeclaredPaths — MCPConfig(loc); InstructionsPath(loc) if non-empty; for HooksClaudeJSON call claudeSettingsPath and claudeHooksScriptPath directly; for a declared skill dir, dir joined with "SKILL.md" and ".codegraph-manifest.json" — and assert set-equality with DescribePaths(loc) plus no duplicate entry, and `Detect(loc).ConfigPath == MCPConfig(loc)`; for an unsupported loc assert DescribePaths(loc) is empty and Detect returns the zero value. Positive floor: count executed leaf subtests and t.Fatalf if fewer than 16.
    - `TestCapabilitiesMatchInstallWrites`: same 8 × 2 grid (supported locations only, positive floor ≥ 13 leaves — 5 global+local targets and 3 global-only): fresh fakeHome + t.Chdir(t.TempDir()), `Install(loc, InstallOptions{ExecPath: "/usr/local/bin/codegraph"})` with zero Errors, then compute DescribePaths(loc) AFTER install (Antigravity's path is migration-aware) and assert (i) every declared path appears among WriteResult.Files paths, (ii) every WriteResult.Files entry whose Action is created, updated or unchanged is declared — the ONLY exception is Antigravity's migrated-marker path, named in a comment as migration bookkeeping, not configuration; removed/not-found entries (legacy self-heal) are ignored.

    Run the whole agents package: every pre-existing test (DescribePaths counts, Detect tests, antigravity migration tests) must pass UNMODIFIED — if one fails, the derivation is wrong, not the test. Commit as `refactor(05-01): derive SupportsLocation, DescribePaths and Detect from the capability table`. This task is behaviour-preserving and its guard is a characterization test, so it has no RED-first commit; its positive control is Family (a) in Task 3.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestCapabilitiesTableDrivesDerivations$|TestCapabilitiesMatchInstallWrites$' -v 2>&1 | tee /tmp/05-01-t2.txt | tail -3; rg -q '^ok\s' /tmp/05-01-t2.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-01-t2.txt && test "$(rg -c -- '--- PASS: TestCapabilitiesTableDrivesDerivations/[a-z]+/(global|local) ' /tmp/05-01-t2.txt)" = "16" && test "$(rg -c -- '--- PASS: TestCapabilitiesMatchInstallWrites/[a-z]+/(global|local) ' /tmp/05-01-t2.txt)" = "13" && test "$(rg -o 'return describeDeclaredPaths\(t, loc\)' internal/agents/{claude,cursor,codex,opencode,hermes,gemini,antigravity,kiro}.go | wc -l | tr -d ' ')" = "8" && test "$(rg -o 'return t\.Capabilities\(\)\.Supports\(loc\)' internal/agents/{claude,cursor,codex,opencode,hermes,gemini,antigravity,kiro}.go | wc -l | tr -d ' ')" = "8" && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 2>&1 | tee /tmp/05-01-agents.txt | tail -2; rg -q '^ok\s' /tmp/05-01-agents.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-01-agents.txt && S=$(git log --format=%s -10) && printf '%s\n' "$S" | rg -q '^refactor\(05-01\): '</automated>
    <fails_when>non-zero exit; build/gofmt/vet red; any "--- FAIL"/"FAIL" line in either transcript; fewer or more than 16 derivation leaf PASS lines or 13 install-writes leaf PASS lines; fewer than 8 one-line DescribePaths derivations or 8 SupportsLocation derivations across the eight target files; the whole agents package not "ok"; or no "refactor(05-01):" commit</fails_when>
  </verify>
  <acceptance_criteria>
    - every target's DescribePaths body is exactly `return describeDeclaredPaths(t, loc)` and every SupportsLocation body is exactly `return t.Capabilities().Supports(loc)` (8 each, counted above)
    - no target's Detect calls its own config path function directly: `! rg -n 'func \(t [a-z]+Target\) Detect' -A6 internal/agents/*.go | rg -q '(claude|cursor|opencode|gemini|kiro)ConfigPath\(loc\)|codexConfigPath\(\)|hermesConfigPath\(\)|antigravityConfigPath\(\)'`
    - TestCapabilitiesTableDrivesDerivations runs 16 leaves and TestCapabilitiesMatchInstallWrites 13 leaves, all PASS
    - the full `internal/agents` package is green with zero pre-existing test edits: `C=$(git log -1 --format=%H -E --grep='^refactor\(05-01\): ') && test -n "$C" && S=$(git show --stat --format= "$C" -- internal/agents/) && test -n "$S" && test -z "$(printf '%s\n' "$S" | rg '_test\.go' | rg -v 'capabilities_test\.go')"` exits 0 (the uncommitted-tree `git diff --quiet` gates in Task 3 are intentionally range-less: they assert the working tree is clean around a mutation)
  </acceptance_criteria>
  <done>The table is the single source for SupportsLocation, DescribePaths and Detect's path inputs, and a guard ties the table to what Install actually writes in both directions.</done>
</task>

<task type="auto">
  <name>Task 3: Styled branch, frozen goldens, reference regeneration, and Family (a) RED</name>
  <files>internal/cli/printconfigstyle.go, internal/cli/printconfigstyle_test.go, internal/cli/install.go, internal/cli/plain_golden_test.go, internal/cli/testdata/plain/print-config-style.golden, internal/cli/testdata/plain/print-config-style-local.golden, docs/CLI-REFERENCE.md, .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md</files>
  <read_first>
    - internal/cli/printconfigstyle.go (Task 1 output)
    - internal/cli/install.go printAgentResults (lines 141-209 — the resolveColor/palette/mode.Writer idiom to mirror) and install_test.go TestInstall_StyledOutputStripsToPlain (the stripped-styled == plain test shape and installSGRSequence helper)
    - internal/cli/present/line.go (KV — label unsanitized fixed literal, value sanitized)
    - internal/cli/plain_golden_test.go (plainCases, the install-local case shape, the -update-plain-goldens flow; goldens are never hand-edited)
    - Taskfile.yml docs:cli / docs:cli:drift (lines 481-535)
    - .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md (the family entry shape and pre-mutation cleanliness gate to reproduce)
  </read_first>
  <action>
    Styled branch (D-04): in printConfigStyle resolve `mode := resolveColor(cmd)` once; when mode.Styled, `pal := present.NewPalette(mode.Dark)`, `w := mode.Writer(out)` and emit each target via `present.KV(w, pal, string(t.ID())+":", fields)` and the zero-target case via `present.Line(w, pal, present.RoleLabel, "no agents selected")`; plain stays as Task 1. Add `TestInstallPrintConfigStyle_StyledStripsToPlain` mirroring TestInstall_StyledOutputStripsToPlain: `--color=always` output contains an ESC byte and, SGR-stripped, equals the `--color=never` output byte for byte.

    Goldens (D-04 — ours, frozen): add two cases to plainCases — `print-config-style` (fakeHome; args `install --print-config-style`; normalize home → `<HOME>`) and `print-config-style-local` (args `install --print-config-style --location local`; normalize home → `<HOME>`, project → `<PROJECT>`) — and update plainCases' doc comment count (29 → 31). Write the two golden files ONLY via `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -run 'TestPlainGolden$' -update-plain-goldens -count=1`, review their bytes, then re-run TestPlainGolden and TestNoColorNonTTYRegression without the flag. Add one Example line to install: `codegraph install --print-config-style --location local`.

    Reference (D-04): run `GOTOOLCHAIN=go1.26.6 task docs:cli`, review the diff of docs/CLI-REFERENCE.md (only the install section's new flag and example may change), and commit it in the SAME commit as the styled branch and goldens — `feat(05-01): styled --print-config-style, frozen goldens and regenerated CLI reference`. Do not add an allowlist line to internal/cli/testdata/cli-reference-allowlist.txt; the flag is visible and documented.

    Family (a) (D-03, rule 84d1gfpywd) — after Task 2's commit, so both mutated files are clean: create 05-MUTATION-LOG.md in the 04-MUTATION-LOG.md shape (title, Phase, Date, Scope, the pre-mutation cleanliness-gate convention section, one section per family). (a1) gate `git diff --quiet -- internal/agents/cursor.go`; apply `perl -0pi -e 's/return describeDeclaredPaths\(t, loc\)/return append(describeDeclaredPaths(t, loc), filepath.Join(".cursor", "rules", "codegraph.mdc"))/' internal/agents/cursor.go`; run `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestCapabilitiesTableDrivesDerivations$' -v` capturing the exit code; record the diff, the verbatim `--- FAIL: TestCapabilitiesTableDrivesDerivations/cursor/…` lines and the exit code; revert with `git checkout -- internal/agents/cursor.go` and prove `git diff --quiet`; re-run green as the control. (a2) same protocol on internal/agents/claude.go with `perl -0pi -e 's/\n\s*Instructions:\s+claudeInstructionsPath,//' internal/agents/claude.go` against `TestCapabilitiesMatchInstallWrites$`, expecting `--- FAIL: TestCapabilitiesMatchInstallWrites/claude/…` (Install writes CLAUDE.md the table no longer declares). Commit the log as `docs(05-01): record Family (a) capability-table guard RED demonstrations`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestPlainGolden$|TestNoColorNonTTYRegression$|TestInstallPrintConfigStyle_StyledStripsToPlain$|TestEveryRegisteredFlagIsAccountedFor$' -v 2>&1 | tee /tmp/05-01-t3.txt | tail -3; rg -q '^ok\s' /tmp/05-01-t3.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-01-t3.txt && rg -q -- '--- PASS: TestPlainGolden/print-config-style ' /tmp/05-01-t3.txt && rg -q -- '--- PASS: TestPlainGolden/print-config-style-local ' /tmp/05-01-t3.txt && rg -q -- '--- PASS: TestInstallPrintConfigStyle_StyledStripsToPlain ' /tmp/05-01-t3.txt && test "$(rg -c '^[a-z]+: scopes=' internal/cli/testdata/plain/print-config-style.golden)" = "8" && (GOTOOLCHAIN=go1.26.6 task docs:cli:drift > /tmp/05-01-drift.txt 2>&1; echo "exit=$?" >> /tmp/05-01-drift.txt); rg -q '^exit=0$' /tmp/05-01-drift.txt && rg -q 'byte-identical to a fresh regeneration' /tmp/05-01-drift.txt && rg -q -- '--print-config-style' docs/CLI-REFERENCE.md && ! rg -q 'print-config-style' internal/cli/testdata/cli-reference-allowlist.txt && rm -f /tmp/05-01-red-a1.txt && git diff --quiet -- internal/agents/cursor.go && perl -0pi -e 's/return describeDeclaredPaths\(t, loc\)/return append(describeDeclaredPaths(t, loc), filepath.Join(".cursor", "rules", "codegraph.mdc"))/' internal/agents/cursor.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestCapabilitiesTableDrivesDerivations$' -v > /tmp/05-01-red-a1.txt 2>&1; echo "exit=$?" >> /tmp/05-01-red-a1.txt); git checkout -- internal/agents/cursor.go; git diff --quiet -- internal/agents/cursor.go && rg -q -- '--- FAIL: TestCapabilitiesTableDrivesDerivations/cursor' /tmp/05-01-red-a1.txt && ! rg -q '^exit=0$' /tmp/05-01-red-a1.txt && rg -q '^## Family \(a' .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md && rg -q -- '--- FAIL: TestCapabilitiesMatchInstallWrites/claude' .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md && echo "goldens frozen, reference current, Family (a) RED re-demonstrated and reverted byte-clean"</automated>
    <fails_when>non-zero exit; any "--- FAIL"/"FAIL" in /tmp/05-01-t3.txt; either print-config-style golden subtest or the styled-strip test not PASS; the global golden not holding 8 table lines; docs:cli:drift exit not 0 or its byte-identical line absent; the flag missing from docs/CLI-REFERENCE.md or present in the allowlist; cursor.go not clean before the plant or after the revert; the (a1) plant not producing a "--- FAIL: TestCapabilitiesTableDrivesDerivations/cursor" line or exiting 0; or 05-MUTATION-LOG.md missing the Family (a) section or the (a2) claude FAIL transcript</fails_when>
  </verify>
  <acceptance_criteria>
    - internal/cli/testdata/plain/print-config-style.golden and print-config-style-local.golden exist, were written by -update-plain-goldens, contain no ESC byte, and the local golden has `codex: scopes=global (local not supported)`, `antigravity: scopes=global (local not supported)` and `hermes: scopes=global (local not supported)` lines
    - printconfigstyle.go calls `resolveColor(cmd)` exactly once and `present.KV(` for the per-target line
    - docs/CLI-REFERENCE.md contains `--print-config-style`; `task docs:cli:drift` exit 0; TestEveryRegisteredFlagIsAccountedFor PASS
    - 05-MUTATION-LOG.md has sections for (a1) and (a2), each with the pre-mutation gate result, the applied diff, verbatim FAIL lines with the exit code, the revert command, the clean `git diff --quiet` and a green control
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1` green; no `[ci skip]`/`[skip ci]` in any 05-01 commit message
  </acceptance_criteria>
  <done>The table renders styled and plain, the plain bytes are frozen, the reference is current through the drift gate, and the D-03 guard is proven able to fail in both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process environment (HOME, XDG_CONFIG_HOME, HERMES_HOME) → path resolution → terminal | resolved paths are printed; a control rune in an env-derived path reaches the user's terminal |
| CLI flags → install RunE | `--print-config-style` must short-circuit before any write-capable branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `install --print-config-style` (install.go RunE) | medium | mitigate | branch runs right after parseLocationFlag and before os.Executable/target switch/picker; TestInstallPrintConfigStyle_ReadOnly (picker stub fatal + empty-walk) and the real-binary empty-HOME/empty-cwd `find` check |
| T-05-02 | Tampering (terminal injection) | printed paths (printconfigstyle.go) | low | mitigate | every path through sanitizePathForDisplay in BOTH branches; the styled value additionally through present.KV's sanitizeControl |
| T-05-03 | Tampering | table drift — a path written but not declared, or declared but not written (DescribePaths/Detect hand copies) | medium | mitigate | D-03 guard in both directions (TestCapabilitiesTableDrivesDerivations, TestCapabilitiesMatchInstallWrites) proven RED by Family (a1)/(a2) |
| T-05-04 | Tampering | antigravityConfigPath narrowing | low | mitigate | only the both-files-absent case changes; existing antigravity migration tests pass unmodified; TestCapabilitiesDeclared pins fresh vs legacy-present resolution |
| T-05-SC | Tampering | package installs | low | accept | none — this phase adds no dependency (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go build ./... && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1` green, 31 plain goldens
- `GOTOOLCHAIN=go1.26.6 task docs:cli:drift` exit 0
- 05-MUTATION-LOG.md Family (a1)/(a2) recorded with byte-clean reverts
- Real binary: `install --print-config-style` prints 8 lines and writes nothing
</verification>

<success_criteria>
- AGENT-08: one per-target capability table drives SupportsLocation, DescribePaths, Detect's path inputs and `--print-config-style`; `--print-config-style` reports exactly what the table says for all eight targets (roadmap criterion 1)
- D-01…D-04 implemented as locked; D-00 respected (only our paths/bytes asserted)
</success_criteria>

<output>
Create `.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-01-SUMMARY.md` when done — paste the Task 1 `--- FAIL:` RED lines, the 16/13 leaf counts, the two golden files' bytes, and the Family (a) outcome.
</output>
