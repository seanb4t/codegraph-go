---
phase: 05-agent-reach-capability-model-skill-in-every-harness
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - internal/agents/gemini.go
  - internal/agents/kiro.go
  - internal/agents/antigravity.go
  - internal/agents/gemini_test.go
  - internal/agents/kiro_test.go
  - internal/agents/antigravity_test.go
  - internal/agents/ownership_test.go
  - internal/agents/skillfrontmatter_test.go
  - internal/cli/testdata/plain/print-config-style.golden
  - internal/cli/testdata/plain/print-config-style-local.golden
autonomous: true
requirements: [AGENT-10, AGENT-11, AGENT-07, AGENT-13]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Gemini CLI (AGENT-10, D-06 + correction (a)): `SkillDirs` = [`.gemini/skills/codegraph`, `.agents/skills/codegraph`] locally and [`~/.gemini/skills/codegraph`, `~/.agents/skills/codegraph`] globally; index 0 (the harness-specific dir AGENT-10 names) is written through `installDeclaredSkill`, and the shared alias is a DOCUMENTED read path only (Gemini reads `.agents/skills/` and it wins same-tier ties — google-gemini/gemini-cli docs/cli/skills.md, fetched 2026-09-18), so a Gemini-only install writes nothing under `.agents/skills/`; its existing GEMINI.md block is retained."
    - "Kiro (AGENT-11, D-06 + correction (d)): `SkillDirs` = [`.kiro/skills/codegraph`] / [`~/.kiro/skills/codegraph`], written through `installDeclaredSkill`; Kiro's instructions are UNCHANGED — it writes no instructions file and never writes `AGENTS.md` (project root or `~/.kiro/steering/AGENTS.md`); 'AGENTS.md retained as a steering source' is Kiro reading files it already reads (kiro.dev/docs/steering, fetched 2026-09-18), recorded as an advisory (possibly doubled instructions when opencode/Codex also write AGENTS.md), not changed."
    - "Antigravity (AGENT-07, D-06 + corrections (b)(c)): global-only `SkillDirs` = [`~/.gemini/antigravity-cli/skills/codegraph`, `~/.gemini/config/skills/codegraph`]; index 0 (the path the `agy` CLI documents and 05-06's live session exercises) is written; the 2.0/IDE path is `[ASSUMED]`, documented-only and never created; no `~/.agents/skills/` write (Antigravity reads it only at workspace scope) and no `AGENTS.md` write — its instructions block is the shared one Antigravity already receives via Gemini's `~/.gemini/GEMINI.md` (antigravity.google/docs/skills.md and rules-workflows.md, fetched 2026-09-18)."
    - "Each harness-specific dir has a single requester and is removed with its target (D-08 one-element case of `targets`); every write and removal goes through 05-02's manifest-owned writer with `refuseUnmanifested` (D-14)."
    - "The ownership oracle in ownership_test.go is extended RED-first to gemini (.gemini/skills), kiro (.kiro/skills) and antigravity (antigravity-cli, global) so the D-13 guard's foreign-dir `kept (foreign)` and clean-dir own-package assertions now cover all six skill-writing targets; TestOwnershipExactIdentity stays 32/32 and its table-vs-oracle cross-check passes."
    - "`--print-config-style`'s goldens are re-frozen in a reviewed diff showing only the gemini, kiro and antigravity lines' `skill=` field changing."
  artifacts:
    - path: "internal/agents/gemini.go"
      provides: "geminiSkillDirs + table-driven skill write"
      contains: "installDeclaredSkill(&result, t, loc)"
    - path: "internal/agents/kiro.go"
      provides: "kiroSkillDirs + table-driven skill write"
      contains: "installDeclaredSkill(&result, t, loc)"
    - path: "internal/agents/antigravity.go"
      provides: "antigravitySkillDirs ([ASSUMED] 2.0/IDE read path documented, not written) + table-driven skill write"
      contains: "antigravity-cli"
  key_links:
    - from: "internal/agents/{gemini,kiro,antigravity}.go Capabilities().SkillDirs"
      to: "internal/agents/capabilities.go installDeclaredSkill"
      via: "WrittenSkillDir = SkillDirs[0]"
      pattern: "SkillDirs:\\s+(gemini|kiro|antigravity)SkillDirs"
  prohibitions:
    - statement: "MUST NOT write an AGENTS.md for Kiro or Antigravity, and MUST NOT write the shared `.agents/skills/` dir for Gemini, Kiro or Antigravity (D-06 corrections (a)(c)(d)); the per-harness no-write tests are the gate."
      status: resolved
      verification: test
    - statement: "MUST NOT create Antigravity's `[ASSUMED]` 2.0/IDE skill path `~/.gemini/config/skills/codegraph` — documented only, no live surface verifies it (D-06(b))."
      status: resolved
      verification: test
---

<objective>
Wire the three harnesses whose documented read paths are harness-specific: Gemini CLI (AGENT-10) and
Kiro (AGENT-11) at both scopes, Antigravity (AGENT-07) at its global-only scope, each through the
capability table and 05-02's manifest-owned writer, with the D-13 ownership oracle extended RED-first so
the guard covers every skill write this phase ships. After this plan every harness with a skill
mechanism receives the codegraph skill; 05-06 establishes by live session which of those reads are real.

Planner notes (binding reconciliations — read before executing):
- Gemini "writes both"? D-06 correction (a) says writing both `.gemini/skills/` and `.agents/skills/` is
  harmless, and 05-VALIDATION's draft map says "Gemini .gemini/skills + shared". D-02 is locked:
  SkillDirs index 0 is the ONE dir written, later entries are documented read paths only. Gemini
  therefore writes `.gemini/skills/codegraph` (the dir AGENT-10 names) and DOCUMENTS the shared alias;
  when Cursor/opencode are also installed the shared package exists too, and Gemini's same-tier
  precedence rule makes the duplicate harmless — exactly the correction's point. No Gemini write to
  `.agents/skills/`.
- Kiro: CONTEXT's code-insights line says Kiro writes `.kiro/steering/codegraph.md`; the code
  self-heal-DELETES that legacy file and writes no instructions (kiro.go:14-18). D-06(d)'s "no change to
  Kiro's instructions" is honoured by leaving that behaviour exactly as it is. RESEARCH Finding 4's
  proposed Kiro AGENTS.md write is superseded by the locked D-06(d) and 05-PATTERNS' matching suggestion
  is not implemented.
- Antigravity: D-06(c) — no AGENTS.md write; its instructions arrive via Gemini's `~/.gemini/GEMINI.md`
  only when the Gemini target is installed; 05-06 probes whether `agy` reads that file.
- Gemini CLI and Kiro are not installed on the maintainer's machine (D-10): their reads stay `[ASSUMED]`
  in 05-06 with doc URL + fetch date; the unit tests here assert only the paths we write (D-00).
- TDD: `test(05-05):` RED commit per task, pasted `--- FAIL:` transcript; GREEN `feat(05-05):`.

Output: harness-specific skill writes for Gemini, Kiro and Antigravity; extended ownership oracle;
re-frozen goldens.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-04-SUMMARY.md
@internal/agents/capabilities.go
@internal/agents/gemini.go
@internal/agents/kiro.go
@internal/agents/antigravity.go
@internal/agents/ownership_test.go
</context>

<interfaces>
From 05-01/05-04 (internal/agents/capabilities.go):
```go
type PathsFunc func(Location) ([]string, error)
func installDeclaredSkill(result *WriteResult, t AgentTarget, loc Location)
func uninstallDeclaredSkill(result *WriteResult, t AgentTarget, loc Location)
func sharedSkillDirPath(loc Location) (string, error) // skillshared.go (05-02)
```
From internal/agents/ownership_test.go (05-04): `ownershipWantSkillDir map[TargetID]map[Location]string`
(`<HOME>` placeholder), `newSkillDirs(home, loc)`.

Surface this plan creates:
```go
func geminiSkillDirs(loc Location) ([]string, error)      // [.gemini/skills/codegraph | ~/.gemini/skills/codegraph, shared dir]
func kiroSkillDirs(loc Location) ([]string, error)        // [.kiro/skills/codegraph | ~/.kiro/skills/codegraph]
func antigravitySkillDirs(loc Location) ([]string, error) // global: [~/.gemini/antigravity-cli/skills/codegraph, ~/.gemini/config/skills/codegraph]
```
</interfaces>

## Artifacts this phase produces

See `05-01-PLAN.md` → "Artifacts this phase produces". This plan creates `geminiSkillDirs`,
`kiroSkillDirs`, `antigravitySkillDirs`, their table-driven writes, and the extended ownership oracle.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gemini CLI and Kiro install the skill package at their own skill dirs, both scopes</name>
  <files>internal/agents/gemini.go, internal/agents/kiro.go, internal/agents/gemini_test.go, internal/agents/kiro_test.go, internal/agents/ownership_test.go, internal/cli/testdata/plain/print-config-style.golden, internal/cli/testdata/plain/print-config-style-local.golden</files>
  <read_first>
    - internal/agents/gemini.go and kiro.go (current literals, Install/Uninstall; Kiro's legacy steering self-heal stays exactly as is)
    - internal/agents/gemini_test.go line 110-118 and kiro_test.go lines 125-134 (DescribePaths tests whose pinned counts change: gemini 2 → 4, kiro 1 → 3)
    - internal/agents/cursor.go (05-04 — the installDeclaredSkill/uninstallDeclaredSkill wiring shape to mirror)
    - internal/agents/ownership_test.go (05-04 — the oracle to extend)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md Critical Findings 1 and 4
  </read_first>
  <behavior>
    - TestGemini_Install_WritesHarnessSkillDir (global, local): `.gemini/skills/codegraph/SKILL.md` (under `<HOME>` for global) equals the embed, its manifest targets are ["gemini"], NOTHING exists under `.agents/skills/` afterwards, and the GEMINI.md block is still written; Uninstall removes the package and the empty dir, and the GEMINI.md block
    - TestGemini_DescribePaths: 4 paths (settings.json, GEMINI.md, `.gemini/skills/codegraph/SKILL.md`, its manifest)
    - TestKiro_Install_WritesHarnessSkillDir (global, local): `.kiro/skills/codegraph/SKILL.md` + manifest targets ["kiro"]; Uninstall removes them
    - TestKiro_Install_WritesNoAgentsMd (global, local): after Install, neither `./AGENTS.md` nor `<HOME>/.kiro/steering/AGENTS.md` exists and nothing exists under `.agents/skills/`
    - TestKiro_DescribePaths_ListsMcpConfigAndSkill: 3 paths (mcp.json, the Kiro SKILL.md, its manifest)
    - TestOwnershipExactIdentity: with the oracle extended to gemini and kiro, the gemini/kiro foreign-variant leaves assert `kept (foreign)` for their dirs and the clean-variant leaves assert their own package — RED until wired
  </behavior>
  <action>
    RED first: extend `ownershipWantSkillDir` with gemini (`<HOME>/.gemini/skills/codegraph` | `.gemini/skills/codegraph`) and kiro (`<HOME>/.kiro/skills/codegraph` | `.kiro/skills/codegraph`), add the per-harness tests, update the two DescribePaths count tests; commit `test(05-05): add failing Gemini and Kiro skill-dir tests`; confirm assertion failures (including the ownership cross-check for gemini/kiro); paste them.

    GREEN:
    - gemini.go: `geminiSkillDirs(loc)` returns [harness dir, sharedSkillDirPath(loc)] — doc comment cites D-06 correction (a) with the Gemini docs URL and fetch date, states index 0 is written and the shared alias is a documented read path (Gemini's same-tier precedence makes a coexisting shared package harmless); literal gains `SkillDirs: geminiSkillDirs`; Install/Uninstall call installDeclaredSkill/uninstallDeclaredSkill after the instructions step.
    - kiro.go: `kiroSkillDirs(loc)` returns [harness dir]; doc comment cites D-06 correction (d) and kiro.dev/docs/steering (fetched 2026-09-18) — Kiro reads `./AGENTS.md` and `~/.kiro/steering/AGENTS.md` on its own, so a codegraph block another target wrote there may reach Kiro twice (advisory, not changed); literal gains `SkillDirs: kiroSkillDirs`; Install calls installDeclaredSkill after the MCP step (the legacy steering self-heal stays first and unchanged; the kiroDisabledByDefaultNote stays); Uninstall calls uninstallDeclaredSkill after removeMcpEntry (restructure its early return on a config-path error so the skill step still runs and errors are recorded, CR-01).
    - Regenerate the two print-config-style goldens via `-update-plain-goldens`; review: only gemini/kiro `skill=` fields change.
    Commit `feat(05-05): Gemini CLI and Kiro install the codegraph skill at their own skill dirs`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(05-05\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestGemini_|TestKiro_|TestOwnershipExactIdentity$|TestCapabilities|TestSkillFrontmatterMatchesEveryWrittenDir$' -v 2>&1 | tee /tmp/05-05-t1.txt | tail -3; rg -q '^ok\s' /tmp/05-05-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-05-t1.txt && rg -q -- '--- PASS: TestKiro_Install_WritesNoAgentsMd ' /tmp/05-05-t1.txt && rg -q -- '--- PASS: TestGemini_Install_WritesHarnessSkillDir ' /tmp/05-05-t1.txt && test "$(rg -c -- '--- PASS: TestOwnershipExactIdentity/[a-z]+/(global|local)/(foreign-codegraph-dir|clean) ' /tmp/05-05-t1.txt)" = "32" && rg -q '^gemini: .* skill=\.gemini/skills/codegraph hooks=none$' internal/cli/testdata/plain/print-config-style-local.golden && rg -q '^kiro: .* instructions=none skill=\.kiro/skills/codegraph hooks=none$' internal/cli/testdata/plain/print-config-style-local.golden && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 2>&1 | tee /tmp/05-05-pkgs.txt | tail -3; test "$(rg -c '^ok\s' /tmp/05-05-pkgs.txt)" = "2" && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-05-pkgs.txt</automated>
    <fails_when>non-zero exit; no "test(05-05):" commit; build/gofmt/vet red; any "--- FAIL"/"FAIL" line; the Kiro no-AGENTS.md or Gemini harness-dir tests not PASS; the ownership test not 32 PASS leaves; the local golden's gemini line not showing `.gemini/skills/codegraph` or the kiro line not showing `instructions=none skill=.kiro/skills/codegraph`; or agents/cli not both "ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -q 'SkillDirs:\s+geminiSkillDirs' internal/agents/gemini.go && rg -q 'SkillDirs:\s+kiroSkillDirs' internal/agents/kiro.go`
    - kiro.go's Capabilities literal has no Instructions entry (Kiro still declares none) and its legacy steering self-heal code is unchanged (`TestKiro_Install_SelfHealsLegacySteeringFile` passes unmodified)
    - gemini.go and kiro.go doc comments carry the doc URL and `2026-09-18`
    - the SUMMARY pastes the RED `--- FAIL:` lines
  </acceptance_criteria>
  <done>Gemini CLI and Kiro receive the codegraph skill at the directories their docs name, owned by manifest, reversed by uninstall, with Kiro's instructions untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Antigravity installs the skill at the agy CLI's global skill dir; the 2.0/IDE path stays documented-only</name>
  <files>internal/agents/antigravity.go, internal/agents/antigravity_test.go, internal/agents/ownership_test.go, internal/agents/skillfrontmatter_test.go, internal/cli/testdata/plain/print-config-style.golden, internal/cli/testdata/plain/print-config-style-local.golden</files>
  <read_first>
    - internal/agents/antigravity.go (global-only Install with its migration and CR-02 marker logic, Uninstall; the 05-01 antigravityConfigPath narrowing)
    - internal/agents/antigravity_test.go (existing migration tests — must pass unmodified)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md Critical Findings 2 and 3, Open Question 2
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md D-06 corrections (b)(c)
  </read_first>
  <behavior>
    - TestAntigravity_Install_WritesCliSkillDir: global Install → `<HOME>/.gemini/antigravity-cli/skills/codegraph/SKILL.md` equals the embed with a manifest targets ["antigravity"]; `<HOME>/.gemini/config/skills/codegraph` does NOT exist; nothing exists under `<HOME>/.agents/skills/`; `<HOME>/.gemini/AGENTS.md` does NOT exist; Uninstall removes the package and the empty `codegraph` dir
    - TestAntigravity_ReadOnlySkillDirDocumented: Capabilities().ReadOnlySkillDirs(global) == [`<HOME>/.gemini/config/skills/codegraph`]
    - TestAntigravity_Local_WritesNothing (existing or added): Install(local) and Uninstall(local) return empty results and create nothing
    - TestOwnershipExactIdentity: oracle extended with antigravity global → the antigravity-cli dir; the foreign leaf asserts `kept (foreign)` there — RED until wired
  </behavior>
  <action>
    RED first: extend the ownership oracle with antigravity (global only), add the tests above; commit `test(05-05): add failing Antigravity CLI skill-dir tests`; confirm assertion failures; paste them.

    GREEN — antigravity.go: `antigravitySkillDirs(loc)` returns, for LocationGlobal only, [`~/.gemini/antigravity-cli/skills/codegraph`, `~/.gemini/config/skills/codegraph`]; doc comment cites antigravity.google/docs/skills.md (fetched 2026-09-18): the CLI's documented global path is index 0 and is the surface 05-06's `agy` session verifies; the 2.0/IDE path is `[ASSUMED]`, documented only, never written (D-06(b)); `.agents/skills/` is read by Antigravity only at workspace scope, which this global-only target never writes; its instructions come from Gemini's `~/.gemini/GEMINI.md` (D-06(c), rules-workflows.md) — no AGENTS.md. Literal gains `SkillDirs: antigravitySkillDirs`. Install calls installDeclaredSkill at the end of the global path (after the marker step); Uninstall calls uninstallDeclaredSkill after removeMcpEntry. The migration/CR-02 logic is untouched. Regenerate the goldens via `-update-plain-goldens`; review: only the antigravity line's `skill=` changes (global golden). Commit `feat(05-05): Antigravity installs the codegraph skill at the agy CLI global skill dir`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(git log --format=%s -20 | rg -c '^test\(05-05\): ')" -ge 2 && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/)" && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestAntigravity_|TestOwnershipExactIdentity$|TestCapabilities|TestSkillFrontmatterMatchesEveryWrittenDir$' -v 2>&1 | tee /tmp/05-05-t2.txt | tail -3; rg -q '^ok\s' /tmp/05-05-t2.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-05-t2.txt && rg -q -- '--- PASS: TestAntigravity_Install_WritesCliSkillDir ' /tmp/05-05-t2.txt && rg -q -- '--- PASS: TestAntigravity_ReadOnlySkillDirDocumented ' /tmp/05-05-t2.txt && test "$(rg -c -- '--- PASS: TestOwnershipExactIdentity/[a-z]+/(global|local)/(foreign-codegraph-dir|clean) ' /tmp/05-05-t2.txt)" = "32" && rg -q '^antigravity: scopes=global mcp=<HOME>/\.gemini/config/mcp_config\.json format=json instructions=none skill=<HOME>/\.gemini/antigravity-cli/skills/codegraph hooks=none$' internal/cli/testdata/plain/print-config-style.golden && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1 2>&1 | tee /tmp/05-05-pkgs.txt | tail -4; test "$(rg -c '^ok\s' /tmp/05-05-pkgs.txt)" -ge 3 && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-05-pkgs.txt</automated>
    <fails_when>non-zero exit; fewer than 2 "test(05-05):" commits; build/gofmt red; any "--- FAIL"/"FAIL" line; either named Antigravity test not PASS; the ownership test not 32 PASS leaves; the global golden's antigravity line not exactly the unified-MCP, no-instructions, antigravity-cli-skill shape; or fewer than 3 "ok" packages across agents + cli/...</fails_when>
  </verify>
  <acceptance_criteria>
    - antigravity.go carries `[ASSUMED]`, `antigravity.google/docs/skills.md` and `2026-09-18` in antigravitySkillDirs' doc comment
    - every pre-existing antigravity migration test passes unmodified
    - TestSkillFrontmatterMatchesEveryWrittenDir now checks ≥ 11 written dirs (claude, cursor, opencode, gemini, kiro × 2 + antigravity × 1) — raise its floor from 6 to 11 in this commit
  </acceptance_criteria>
  <done>Every harness with a skill mechanism now receives the codegraph skill at the path its docs name, the ownership guard covers all six skill-writing targets, and the table prints exactly what ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/other-tool content in `~/.gemini`, `~/.kiro`, project `.gemini`/`.kiro` → install/uninstall | these trees are shared with the harnesses and with Antigravity/Gemini's own writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Tampering / Elevation of Privilege | Gemini/Kiro/Antigravity skill writes | high | mitigate | installDeclaredSkill → refuseUnmanifested; ownership oracle extended RED-first; foreign dirs `kept (foreign)` in all three |
| T-05-21 | Tampering | writing an unverified path (Antigravity 2.0/IDE, or an inert AGENTS.md) | low | mitigate | documented-only read paths never created; no-write tests for `~/.gemini/config/skills/codegraph`, `~/.gemini/AGENTS.md`, Kiro AGENTS.md |
| T-05-22 | Information Disclosure / confusion | doubled instructions reaching Kiro via AGENTS.md written by other targets | low | accept | Kiro's own documented read behaviour (D-06(d) advisory); identical content; recorded, not changed |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1` green
- TestOwnershipExactIdentity 32/32 with all six skill writers in the oracle
</verification>

<success_criteria>
- AGENT-10, AGENT-11, AGENT-07 skill writes per D-06 as corrected; AGENT-13 guard covers them
</success_criteria>

<output>
Create `.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-05-SUMMARY.md` when done — paste both RED transcripts and the golden diffs.
</output>
