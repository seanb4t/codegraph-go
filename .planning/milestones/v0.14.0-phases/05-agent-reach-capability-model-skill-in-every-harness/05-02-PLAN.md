---
phase: 05-agent-reach-capability-model-skill-in-every-harness
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/agents/manifest.go
  - internal/agents/manifest_test.go
  - internal/agents/skillshared.go
  - internal/agents/skillshared_test.go
  - internal/agents/shared.go
autonomous: true
requirements: [AGENT-09, AGENT-13]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`skillManifest` gains `Targets []TargetID` serialized as `targets` with `omitempty`; `manifestSchemaVersion` is 2; `writeManifest` treats two manifests as unchanged only when schema_version, codegraph_version, location, files AND the targets SET are equal (order-insensitive), so a re-run in any requester order is a byte-level no-op (D-07, D-07 idempotency of v0.10.0)."
    - "D-07's 'an older manifest without targets is read as owned, requester set unknown' is resolved concretely by `manifestRequesters`: a readable manifest with no `targets` field, AND a present-but-unreadable manifest, both read as requesters {claude} — Claude's installer is the only writer of every manifest before this phase (manifest.go ConfiguredSkillLocations doc, v0.10.0 Phase 7), so {claude} is the known historical requester set, and it is the only reading that never deletes a package out from under Claude (D-17); the next install upgrades it in place to that set ∪ {requester}."
    - "One writer, one code path (assumption-delta promote): `installSkillPackage(result, dir, loc, requester, policy)` writes `<dir>/SKILL.md` from `claudeassets.SkillMarkdown()` (no new embed) via `writeEmbeddedFile` and records the requester in `<dir>/.codegraph-manifest.json`; the SAME function serves the shared `.agents/skills/codegraph/` dir and every single-requester harness dir — a single requester is the one-element case of `targets` (D-05, D-07)."
    - "D-05 within one install run: the second and later requesters of the same dir see `SKILL.md: unchanged` and `manifest: updated` (their id appended to `targets`); a full re-run reports every file `unchanged`. No package-level 'already wrote this run' state exists — the manifest is read fresh on every call (RESEARCH anti-pattern)."
    - "D-08: `uninstallSkillPackage(result, dir, requester, exclusiveKeys, policy)` removes ONLY the requester from `targets`; while other requesters remain it rewrites the manifest (dropping the requester's `exclusiveKeys` from `files`) and reports SKILL.md `kept`; when `targets` becomes empty it removes the manifest, then SKILL.md, then calls `removeSkillDirIfEmpty(dir)` (semantics kept — a user file keeps the directory); a requester not in `targets` changes nothing and reports `not-found`."
    - "D-14: under `refuseUnmanifested` a `codegraph/` skill dir that exists, holds at least one entry and has NO manifest file is foreign — install and uninstall report `{Path: dir, Action: \"kept (foreign)\"}` and leave every byte in it untouched; an empty existing dir is not foreign. Ownership is manifest-file PRESENCE only (a corrupt manifest still marks the dir as ours, matching writeManifest's WR-01 self-heal), never a SKILL.md name or shape — the `242ec0a` differential. `adoptUnmanifested` (reserved for Claude's own non-shared dir, D-05) skips the check, preserving v0.10.0 behaviour."
    - "D-16: a hand-edited own SKILL.md (manifest present) is rewritten to the embedded bytes and reported `updated`; the manifest (which records what codegraph wrote) stays `unchanged` — a hash mismatch is a drift signal, never a security event (manifest.go doc)."
    - "Symlink safety (D-17 groundwork): `removeSkillDirIfEmpty` never unlinks or removes a path that is itself a symlink (Lstat first) — the link is user-owned structure; `writeSkillFile` follows a DANGLING directory symlink to its target and creates it, so a user's `.claude/skills/codegraph -> ../../.agents/skills/codegraph` link keeps working after the target was removed; `sameSkillDir(a, b)` compares absolute, `filepath.EvalSymlinks`-resolved paths, resolving a dangling link through `os.Readlink`."
    - "AGENT-09 (probe adjacency, explicit): two requesters whose dirs are the same path, or resolve to the same physical dir through a symlink, MERGE into one package with one manifest whose `targets` is the union and whose `files` keeps every requester's keys — `TestSkillPackage_WritesThroughSymlinkedDirAsOnePackage` and `TestSharedSkillPackage_WritesOnceAndAccumulatesTargets` pin it."
    - "AGENT-09 (probe empty, explicit): zero requesters ⇔ no package (manifest and SKILL.md absent); an uninstall that empties `targets` deletes the package; an absent manifest with an absent/empty dir reports `not-found` for both files and creates nothing; a legacy manifest without `targets` reads as {claude}."
    - "AGENT-09 (probe ordering, explicit): `targets` is written in first-install order and compared as a set, so any install order yields an `unchanged` re-run; `TestSharedSkillPackage_TargetsInvariantOverAllSequences` exhaustively runs every install/uninstall sequence of length 1–4 over three requesters (1554 sequences) and asserts after EVERY step: package present ⇔ modelled requester set non-empty, and manifest `targets` set == model."
  artifacts:
    - path: "internal/agents/skillshared.go"
      provides: "ActionKeptForeign, unmanifestedPolicy consts refuseUnmanifested/adoptUnmanifested, sharedSkillDirPath, sharedSkillDirs, skillManifestPath, skillDirIsForeign, writeSkillFile, recordSkillManifest, installSkillPackage, uninstallSkillPackage, resolveSkillDir, sameSkillDir"
      contains: "if fileExists(skillManifestPath(dir)) {"
    - path: "internal/agents/skillshared_test.go"
      provides: "TestSharedSkillPackage_* (12), TestSkillPackage_* , TestSameSkillDir_*, TestRemoveSkillDirIfEmpty_NeverUnlinksSymlink"
    - path: "internal/agents/manifest.go"
      provides: "skillManifest.Targets, manifestSchemaVersion = 2, manifestRequesters, targetSetEqual"
      contains: "json:\"targets,omitempty\""
    - path: "internal/agents/shared.go"
      provides: "removeSkillDirIfEmpty with an Lstat symlink guard"
  key_links:
    - from: "internal/agents/skillshared.go installSkillPackage"
      to: "internal/agents/shared.go writeEmbeddedFile + internal/agents/manifest.go writeManifest"
      via: "reused verbatim (RESEARCH Don't Hand-Roll)"
      pattern: "writeEmbeddedFile\\(|writeManifest\\("
    - from: "internal/agents/skillshared.go uninstallSkillPackage"
      to: "internal/agents/shared.go removeSkillDirIfEmpty"
      via: "only after targets empties"
      pattern: "removeSkillDirIfEmpty\\("
    - from: "internal/agents/skillshared.go recordSkillManifest"
      to: "internal/agents/manifest.go manifestRequesters"
      via: "legacy/corrupt → {claude}, then ∪ requester"
      pattern: "manifestRequesters\\("
  prohibitions:
    - statement: "MUST NOT delete a skill package while any other requester still lists it in `targets` — uninstalling one harness must never silently break another (D-08); TestSharedSkillPackage_UninstallRemovesOnlyRequester and the exhaustive invariant test are the gate."
      status: resolved
      verification: test
    - statement: "MUST NOT overwrite, adopt, or remove a codegraph-named skill directory that carries no codegraph manifest, and MUST NOT infer ownership from a SKILL.md's presence, name or content (D-14, the 242ec0a differential); TestSharedSkillPackage_ForeignDirKeptForeign is the gate here and Family (b1) in 05-04 proves it can fail."
      status: resolved
      verification: test
    - statement: "MUST NOT unlink, rename or rewrite a user's symlink under a skill root; MUST NOT recursively delete a skill directory (removeSkillDirIfEmpty only)."
      status: resolved
      verification: test
---

<objective>
Build the one manifest-owned skill-package writer every later plan wires targets onto: the manifest
gains a `targets` requester set (D-07, schema 2), `installSkillPackage`/`uninstallSkillPackage` write
and reverse `<dir>/SKILL.md` + `<dir>/.codegraph-manifest.json` for a requester with D-08's
last-requester rule, D-14's manifest-presence ownership (`kept (foreign)`), D-16's hand-edited-own-file
rewrite, and the symlink-safety groundwork D-17 needs (never unlink a user's symlink; follow a dangling
directory link to its target). No target calls it yet — 05-03 moves Claude onto it, 05-04/05-05 wire
the five harnesses — so every existing test stays green and nothing on disk changes for users.

Purpose: AGENT-09 (the shared `.agents/skills/codegraph/` package, written once, owned jointly) and
AGENT-13 (exact-identity ownership of every new skill write). This is the single code path the
assumption-delta decision promotes: the `targets` set is the ownership identity for every skill
directory, a single requester is its one-element case.

TDD (`type: tdd`): each task's failing tests land first as a `test(05-02):` commit with only the
compile placeholders they need, the pasted `--- FAIL:` transcript in the SUMMARY is the RED evidence,
GREEN lands as `feat(05-02):`. Never gate on `gsd-tools check tdd-red-evidence`; never `[ci skip]`.

Reuse, never re-derive (RESEARCH Don't Hand-Roll): `writeEmbeddedFile`, `removeEmbeddedFile`,
`writeManifest`/`readManifest`, `hashContent`, `recordFile`/`recordAction`, `removeSkillDirIfEmpty`,
`fileExists`, `atomicWriteFile` — all existing and already tested.

Output: `test(05-02):` RED commit(s), `feat(05-02): manifest-owned skill package writer with a requester set`,
`feat(05-02): symlink-safe skill directory resolution and removal`.
</objective>

<assumption_delta_decision>
noun: the skill manifest's `targets` set (`skillManifest.Targets`)
transition: singular → plural — skill-directory ownership moves from one owner (Claude's installer) to a SET of requesting targets (D-07, D-08, D-17)
decision: promote
rationale: the manifest's `targets` list IS the ownership identity for every skill directory the shared writer manages; a single-requester directory (Claude's own non-shared dir, Gemini/Kiro/Antigravity harness dirs) is the one-element case of the same shape, not a second code path — every writer and remover in this phase goes through installSkillPackage/uninstallSkillPackage (or their two primitives writeSkillFile/recordSkillManifest).
invariant_test: TestSharedSkillPackage_TargetsInvariantOverAllSequences — every install→uninstall sequence over any subset/order of requesting targets leaves the package present iff `targets` is non-empty.
</assumption_delta_decision>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-PATTERNS.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-01-SUMMARY.md
@internal/agents/manifest.go
@internal/agents/shared.go
@internal/agents/claude.go
@internal/agents/capabilities.go
@claudeassets.go
</context>

<interfaces>
<!-- Existing, reused verbatim (internal/agents). -->
```go
func writeEmbeddedFile(path, content string, executable bool) (FileResult, error) // raw-byte idempotent; created|updated|unchanged
func removeEmbeddedFile(path string) (FileResult, error)                          // removed | not-found
func removeSkillDirIfEmpty(dir string) error                                      // os.Remove; entries remain → nil (no-op)
func readManifest(path string) (skillManifest, bool, error)                       // absent → (zero,false,nil); unreadable/undecodable → error
func writeManifest(path string, m skillManifest) (FileResult, error)              // no-needless-rewrite; preserves installed_at; self-heals corrupt
func hashContent(b []byte) string                                                  // "sha256:<hex>"
func recordFile(result *WriteResult, path string, fr FileResult, err error)
func recordAction(result *WriteResult, path string, action FileAction, err error)
func fileExists(path string) bool
const manifestKeySkillMD = "skills/codegraph/SKILL.md"; manifestKeyScript = "hooks/session-nudge.sh"; manifestKeyHooksFrag = "settings.json#hooks.SessionStart"
// from 05-01 capabilities.go:
const skillFileName = "SKILL.md"; const skillManifestFileName = ".codegraph-manifest.json"
// from the repo root package (named import):  claudeassets "github.com/seanb4t/codegraph-go"
func claudeassets.SkillMarkdown() ([]byte, error)
// internal/version
func version.Info() version.BuildInfo   // .Version
```

Surface this plan creates (names are binding — 05-03/05-04/05-05 call them and Family (b1) targets the foreign-check literal):
```go
const ActionKeptForeign FileAction = "kept (foreign)"
type unmanifestedPolicy int
const ( refuseUnmanifested unmanifestedPolicy = iota; adoptUnmanifested )
func sharedSkillDirPath(loc Location) (string, error)   // ".agents/skills/codegraph" | <home>/.agents/skills/codegraph
func sharedSkillDirs(loc Location) ([]string, error)     // PathsFunc: one element, the shared dir
func skillManifestPath(dir string) string                // filepath.Join(dir, skillManifestFileName)
func skillDirIsForeign(dir string) (bool, error)         // contains the single line `if fileExists(skillManifestPath(dir)) {`
func writeSkillFile(result *WriteResult, dir string, policy unmanifestedPolicy) (content []byte, ok bool)
func recordSkillManifest(result *WriteResult, dir string, loc Location, requester TargetID, ownFiles map[string]string)
func installSkillPackage(result *WriteResult, dir string, loc Location, requester TargetID, policy unmanifestedPolicy)
func uninstallSkillPackage(result *WriteResult, dir string, requester TargetID, exclusiveKeys []string, policy unmanifestedPolicy)
func resolveSkillDir(dir string) (string, error)         // Task 2
func sameSkillDir(a, b string) (bool, error)             // Task 2
// manifest.go
Targets []TargetID `json:"targets,omitempty"`
func manifestRequesters(m skillManifest, present bool, readErr error) []TargetID
func targetSetEqual(a, b []TargetID) bool
```
</interfaces>

## Artifacts this phase produces

See `05-01-PLAN.md` → "Artifacts this phase produces" (phase-wide). This plan creates:
`skillshared.go` (the writer and its helpers), `ActionKeptForeign`, `unmanifestedPolicy`,
`skillManifest.Targets` + schema 2, `manifestRequesters`, `targetSetEqual`, the `removeSkillDirIfEmpty`
symlink guard, and `skillshared_test.go` / `TestManifest_TargetsRoundTripAndSetEquality`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Manifest `targets` (schema 2) and the manifest-owned skill package writer</name>
  <reversibility rating="costly">schema_version 2 manifests are read by every later codegraph binary, and an older binary rewrites one at schema 1 dropping `targets`; it self-heals on the next new-binary install and the loss mode is confined to D-17 symlinked layouts, but undoing the field later means another versioned manifest change.</reversibility>
  <files>internal/agents/manifest.go, internal/agents/manifest_test.go, internal/agents/skillshared.go, internal/agents/skillshared_test.go</files>
  <read_first>
    - internal/agents/manifest.go (whole file — skillManifest, readManifest's three outcomes, writeManifest's equality check lines 143-149, the drift-not-tamper doc lines 9-15 that must stay true)
    - internal/agents/claude.go lines 405-589 (the skill-install and uninstall blocks this writer generalizes; the have*-flag rule: record a hash only after its own write succeeded, CR-01)
    - internal/agents/shared.go lines 12-35, 286-341, 464-500 (recordFile/recordAction, writeEmbeddedFile, removeEmbeddedFile, removeSkillDirIfEmpty)
    - internal/agents/manifest_test.go (round-trip, idempotency and self-heal test shapes to mirror)
    - internal/agents/claude_skillpackage_test.go lines 22-150 (TestClaude_Install_WritesSkillPackage_EndToEnd — the tracer-test shape: fakeHome, install, assert bytes/mode/Files)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-05, D-07, D-08, D-14, D-16)
  </read_first>
  <behavior>
    - TestSharedSkillPackage_WritesOnceAndAccumulatesTargets (global and local subtests): installSkillPackage(shared dir, loc, Cursor, refuse) → SKILL.md bytes == claudeassets.SkillMarkdown(), manifest schema_version 2, targets ["cursor"], files[manifestKeySkillMD] == hashContent(content), Files report SKILL.md created + manifest created; then Opencode → SKILL.md unchanged, manifest updated, targets ["cursor","opencode"]; then Cursor again and Opencode again → every reported file unchanged and the manifest bytes identical
    - TestSharedSkillPackage_UninstallRemovesOnlyRequester: after cursor+opencode, uninstall Cursor → targets ["opencode"], SKILL.md still equal to the embed and reported `kept`, manifest reported `updated`
    - TestSharedSkillPackage_LastRequesterDeletesPackage: then uninstall Opencode → manifest `removed`, SKILL.md `removed`, the dir no longer exists
    - TestSharedSkillPackage_UninstallNonRequesterIsNoop: manifest targets ["opencode"]; uninstall Cursor → manifest and SKILL.md `not-found`, both files byte-identical
    - TestSharedSkillPackage_ForeignDirKeptForeign: dir holds a foreign SKILL.md ("# someone else's skill\n") and no manifest → install (refuse) reports exactly one `{dir, "kept (foreign)"}` entry, SKILL.md bytes unchanged, no manifest created; uninstall (refuse) reports `kept (foreign)` and changes nothing
    - TestSharedSkillPackage_EmptyDirIsNotForeign: an existing empty dir → install writes SKILL.md + manifest (created)
    - TestSharedSkillPackage_AdoptPolicyAdoptsUnmanifested: same foreign fixture under adoptUnmanifested → SKILL.md rewritten (updated) and manifest created with targets ["claude"] when requester is Claude
    - TestSharedSkillPackage_HandEditedOwnFileRewritten: install Cursor, overwrite SKILL.md with "edited\n", install Cursor → SKILL.md `updated` to the embed, manifest `unchanged`
    - TestSharedSkillPackage_LegacyAndCorruptManifestReadAsClaude: a manifest JSON with schema_version 1 and no `targets` key → install Cursor → targets ["claude","cursor"]; uninstall Cursor → targets ["claude"], package kept; the same with a manifest file containing `{not json` → install self-heals to targets ["claude","cursor"]
    - TestSharedSkillPackage_UserFileKeepsDir: a user `notes.md` in the dir → last-requester uninstall removes only SKILL.md + manifest; notes.md and the dir remain
    - TestSharedSkillPackage_ExclusiveKeysDroppedWhenRequesterLeaves: recordSkillManifest(dir, loc, Claude, {SKILL, script, hooks keys}) then installSkillPackage(..., Cursor) → files has all three keys, targets [claude, cursor]; uninstallSkillPackage(Claude, exclusiveKeys [script, hooks]) → files has only the SKILL key, targets [cursor]
    - TestSharedSkillPackage_TargetsInvariantOverAllSequences: every sequence of length 1..4 over {install, uninstall} × {Cursor, Opencode, Gemini} (1554 sequences, each in a fresh temp dir) — after every step, (SKILL.md exists AND manifest exists) == (model set non-empty), manifest targets set == model set, WriteResult.Errors empty; the test asserts it executed exactly 1554 sequences
    - TestManifest_TargetsRoundTripAndSetEquality: writeManifest with targets [cursor, opencode] then [opencode, cursor] → second call unchanged; adding gemini → updated; a manifest with nil Targets serializes with NO `targets` key
  </behavior>
  <action>
    RED first: write skillshared_test.go and the new manifest_test.go case per the behavior block, plus compile placeholders only (skillshared.go declaring the interfaces-block surface with no-op bodies; the Targets field and zero-body manifestRequesters/targetSetEqual in manifest.go). Commit `test(05-02): add failing manifest-owned skill package writer tests`; confirm each named test fails on an assertion; paste the `--- FAIL:` lines into the SUMMARY.

    GREEN — manifest.go (D-07): add `Targets []TargetID` with tag `json:"targets,omitempty"` to skillManifest (after Files); set manifestSchemaVersion to 2 and extend its comment: 1 = v0.10.0 Phase 7 through v0.14.0 Phase 4 (Claude-only writer, no requester set), 2 = requester set (D-07). `manifestRequesters(m, present, readErr)`: readErr != nil → [Claude]; !present → nil; present with nil Targets → [Claude]; else a copy of m.Targets — doc comment states the {claude} reading and why it is the only non-destructive one (Claude was the sole historical writer). `targetSetEqual(a, b)` compares as sets. writeManifest's unchanged condition additionally requires targetSetEqual(existing.Targets, m.Targets). Keep the drift-not-tamper doc intact.

    GREEN — skillshared.go (new file, D-05/D-07/D-08/D-14/D-16):
    - Package doc comment: this is the single manifest-owned writer for EVERY codegraph skill directory (assumption-delta: targets is the ownership identity); ownership is manifest-file presence, never shape — cite commit 242ec0a and the differential it closed (a same-named unrelated entry was claimed because a manifest existed nearby).
    - `ActionKeptForeign FileAction = "kept (foreign)"` with a comment citing D-14.
    - `sharedSkillDirPath(loc)`: local `filepath.Join(".agents", "skills", "codegraph")`; global `filepath.Join(home, ".agents", "skills", "codegraph")` via os.UserHomeDir. `sharedSkillDirs(loc)` wraps it as a one-element slice (the PathsFunc 05-04 puts in Cursor's/opencode's literals).
    - `skillManifestPath(dir)` = filepath.Join(dir, skillManifestFileName).
    - `skillDirIsForeign(dir) (bool, error)`: false when dir does not exist; its manifest check is written as the single line `if fileExists(skillManifestPath(dir)) {` returning false (Family (b1) in 05-04 plants a shape-based recovery on exactly this line); otherwise os.ReadDir — foreign iff at least one entry.
    - `writeSkillFile(result, dir, policy)`: under refuseUnmanifested, a foreign dir → `result.Files = append(…, FileResult{Path: dir, Action: ActionKeptForeign})`, return (nil, false). Otherwise read claudeassets.SkillMarkdown() (error → result.Errors naming the path), writeEmbeddedFile(filepath.Join(dir, skillFileName), content, false) through recordFile; return (content, werr == nil).
    - `recordSkillManifest(result, dir, loc, requester, ownFiles)`: readManifest(skillManifestPath(dir)); requesters := manifestRequesters(...), append requester if absent (first-install order); files := a copy of the existing Files when readable, then set every ownFiles key; build skillManifest{SchemaVersion: manifestSchemaVersion, CodegraphVersion: version.Info().Version, Location: string(loc), Files: files, Targets: requesters}; writeManifest through recordFile.
    - `installSkillPackage(result, dir, loc, requester, policy)`: content, ok := writeSkillFile(...); only when ok, recordSkillManifest with ownFiles {manifestKeySkillMD: hashContent(content)} — never record a hash for a write that did not happen (CR-01 have-flag rule).
    - `uninstallSkillPackage(result, dir, requester, exclusiveKeys, policy)`: manifest absent → under refuse with a foreign dir report kept (foreign) and stop; under adopt with SKILL.md present remove SKILL.md (the v0.10.0 Claude behaviour) and removeSkillDirIfEmpty; else report not-found for manifest and SKILL.md. Manifest present → requesters via manifestRequesters; requester not among them → not-found for both, nothing written; remaining empty → removeEmbeddedFile(manifest) then removeEmbeddedFile(SKILL.md) (manifest first so the dir-empty sweep sees it gone) then removeSkillDirIfEmpty(dir) with its error into result.Errors; remaining non-empty → writeManifest with Targets = remaining and Files minus exclusiveKeys (recordFile), and append FileResult{SKILL.md path, ActionKept}.
    Commit GREEN as `feat(05-02): manifest-owned skill package writer with a requester set`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(05-02\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestSharedSkillPackage_|TestManifest_' -v 2>&1 | tee /tmp/05-02-t1.txt | tail -3; rg -q '^ok\s' /tmp/05-02-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-02-t1.txt && test "$(rg -c '^--- PASS: TestSharedSkillPackage_' /tmp/05-02-t1.txt)" -ge 11 && rg -q -- '--- PASS: TestSharedSkillPackage_TargetsInvariantOverAllSequences ' /tmp/05-02-t1.txt && rg -q -- '--- PASS: TestManifest_TargetsRoundTripAndSetEquality ' /tmp/05-02-t1.txt && rg -q 'manifestSchemaVersion\s+=\s+2' internal/agents/manifest.go && rg -q 'json:"targets,omitempty"' internal/agents/manifest.go && test "$(rg -o 'if fileExists\(skillManifestPath\(dir\)\) \{' internal/agents/skillshared.go | wc -l | tr -d ' ')" = "1" && rg -q '242ec0a' internal/agents/skillshared.go && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1 2>&1 | tee /tmp/05-02-pkgs.txt | tail -4; test "$(rg -c '^ok\s' /tmp/05-02-pkgs.txt)" -ge 3 && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-02-pkgs.txt && S=$(git log --format=%s -10) && printf '%s\n' "$S" | rg -q '^feat\(05-02\): '</automated>
    <fails_when>non-zero exit; no "test(05-02):" commit; build/gofmt/vet red; any "--- FAIL"/"FAIL" line in either transcript; fewer than 11 top-level TestSharedSkillPackage_ PASS lines or the invariant/manifest-set tests missing from the PASS lines; schema_version not 2, the omitempty targets tag absent, the foreign-check literal not present exactly once, or no 242ec0a citation in skillshared.go; fewer than 3 "ok" package lines across agents+cli; or no "feat(05-02):" commit</fails_when>
  </verify>
  <acceptance_criteria>
    - internal/agents/skillshared.go defines every function and constant in the interfaces block except resolveSkillDir/sameSkillDir (Task 2)
    - `rg -q 'claudeassets\.SkillMarkdown\(\)' internal/agents/skillshared.go` matches, and no 05-02 commit touches the embed: `L=$(git log --format=%s -- claudeassets.go) && test -z "$(printf '%s\n' "$L" | rg '^(test|feat)\(05-02\)')"`
    - the invariant test logs or asserts the executed-sequence count 1554
    - no package-level mutable state records "already wrote this run": `! rg -n '^var .*(wrote|written|done|seen)' internal/agents/skillshared.go`
    - every pre-existing internal/agents and internal/cli test passes unmodified (no call site uses the writer yet)
  </acceptance_criteria>
  <done>A single manifest-owned writer exists whose requester set decides deletion, whose ownership is manifest presence only, and whose invariant (present ⇔ targets non-empty) is proven over every short sequence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Symlink-safe skill directory resolution and removal (D-17 groundwork)</name>
  <files>internal/agents/skillshared.go, internal/agents/skillshared_test.go, internal/agents/shared.go</files>
  <read_first>
    - internal/agents/shared.go lines 479-500 (removeSkillDirIfEmpty — os.Remove on a symlink path unlinks the LINK even when its target still has entries, violating "only when empty")
    - internal/agents/skillshared.go (Task 1 output)
    - internal/fsatomic (WriteFile — how it creates parent directories; a dangling directory symlink makes MkdirAll fail)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md Pitfall 2 (the maintainer's real `~/.claude/skills/codegraph -> ../../.agents/skills/codegraph` link)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-17)
  </read_first>
  <behavior>
    - TestRemoveSkillDirIfEmpty_NeverUnlinksSymlink: link → an EMPTY real dir, and separately link → a NON-empty real dir; removeSkillDirIfEmpty(link) returns nil and the link still exists (os.Lstat reports ModeSymlink) and its target is untouched, in both cases; a plain empty dir is still removed and a plain non-empty dir still kept (existing semantics)
    - TestSameSkillDir_ResolvesSymlinksAndDanglingLinks: (a, a) true; a relative path and its absolute form true; `claude/skills/codegraph -> ../../agents/skills/codegraph` vs the real target true; same with the target not yet created (dangling) true; two unrelated dirs false
    - TestSkillPackage_DanglingSymlinkDirIsRecreated: a relative link whose target does not exist → installSkillPackage(link, …, Cursor, refuse) reports no errors, the target dir now exists with SKILL.md and a manifest (targets ["cursor"]), and the link is still a symlink that resolves to it
    - TestSkillPackage_WritesThroughSymlinkedDirAsOnePackage: link L → real dir R; installSkillPackage(R, …, Cursor) then installSkillPackage(L, …, Opencode) → exactly one manifest file exists (in R), targets ["cursor","opencode"]; uninstallSkillPackage(L, Opencode) then (R, Cursor) → package gone, L still a symlink (dangling)
  </behavior>
  <action>
    RED first: add the four tests (placeholders already compile from Task 1 — add zero-body resolveSkillDir/sameSkillDir only), commit `test(05-02): add failing symlink-safety tests for skill directories`, confirm assertion failures (removeSkillDirIfEmpty currently unlinks the link; the dangling install errors), paste them into the SUMMARY.

    GREEN:
    - shared.go removeSkillDirIfEmpty: before os.Remove, `os.Lstat(dir)`; a symlink → return nil (a user-owned link is never removed; the resolved directory is cleaned only when a writer is handed its real path). Extend the doc comment: why unlinking would break a user's `npx skills`-style layout (RESEARCH Pitfall 2) and that "only when empty" is thereby restored, not changed (D-08 semantics kept).
    - skillshared.go `resolveSkillDir(dir) (string, error)`: filepath.Abs, then filepath.EvalSymlinks; if that fails and os.Lstat shows dir itself is a symlink, os.Readlink it, join a relative target onto filepath.Dir(abs), Clean, and resolve again recursively (bounded depth 8, error beyond); if dir simply does not exist and is not a link, resolve its parent the same way and join the base name.
    - `sameSkillDir(a, b)` = resolveSkillDir(a) == resolveSkillDir(b) (errors returned).
    - writeSkillFile: when os.Lstat(dir) is a symlink whose target does not exist, os.MkdirAll(resolved target, 0o755) before writing (so the user's link keeps working); FileResult paths keep the caller's dir unchanged for a non-link and for a live link.
    - Doc comments cite D-17 and RESEARCH Pitfall 2.
    Commit GREEN as `feat(05-02): symlink-safe skill directory resolution and removal`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(git log --format=%s -20 | rg -c '^test\(05-02\): ')" -ge 2 && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestRemoveSkillDirIfEmpty_NeverUnlinksSymlink$|TestSameSkillDir_ResolvesSymlinksAndDanglingLinks$|TestSkillPackage_DanglingSymlinkDirIsRecreated$|TestSkillPackage_WritesThroughSymlinkedDirAsOnePackage$' -v 2>&1 | tee /tmp/05-02-t2.txt | tail -3; rg -q '^ok\s' /tmp/05-02-t2.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-02-t2.txt && test "$(rg -c '^--- PASS: Test(RemoveSkillDirIfEmpty_NeverUnlinksSymlink|SameSkillDir_ResolvesSymlinksAndDanglingLinks|SkillPackage_DanglingSymlinkDirIsRecreated|SkillPackage_WritesThroughSymlinkedDirAsOnePackage) ' /tmp/05-02-t2.txt)" = "4" && rg -q 'os\.Lstat' internal/agents/shared.go && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 2>&1 | tee /tmp/05-02-agents.txt | tail -2; rg -q '^ok\s' /tmp/05-02-agents.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-02-agents.txt</automated>
    <fails_when>non-zero exit; fewer than 2 "test(05-02):" commits; build/gofmt/vet red; any "--- FAIL"/"FAIL" line; fewer than the 4 named PASS lines; no os.Lstat guard in shared.go; or the full agents package not "ok" (TestClaude_Uninstall_KeepsEmptyParentsClean and every other pre-existing test must pass unmodified)</fails_when>
  </verify>
  <acceptance_criteria>
    - removeSkillDirIfEmpty returns before os.Remove for a symlink path; pre-existing TestClaude_Uninstall_KeepsEmptyParentsClean and TestClaude_Uninstall_PreservesUserFileInSkillDir pass unmodified
    - resolveSkillDir handles a dangling relative link (test above) and is bounded (a self-referential link returns an error, not a hang — covered by a sub-case)
    - `feat(05-02): symlink-safe skill directory resolution and removal` commit exists after its `test(05-02):` commit
  </acceptance_criteria>
  <done>No codegraph uninstall can unlink a user's symlink, a dangling symlinked skill dir is recreated rather than failing, and two paths that are one physical directory are recognized as one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem state under a skill root (user, other tools, cloned repos) → writer | existing `codegraph/` dirs, symlinks and manifests may be foreign or hand-edited |
| one requester's uninstall → another requester's installed package | shared directory, shared manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering / Elevation of Privilege | installSkillPackage / uninstallSkillPackage ownership (the 242ec0a class) | high | mitigate | ownership = manifest-file presence only (`skillDirIsForeign`'s single literal check); foreign dir reported `kept (foreign)` and untouched; TestSharedSkillPackage_ForeignDirKeptForeign here, Family (b1) RED in 05-04 |
| T-05-06 | Denial of Service / Tampering | uninstall of one harness deleting a package another still uses | high | mitigate | D-08 requester set; delete only when `targets` empties; exhaustive TestSharedSkillPackage_TargetsInvariantOverAllSequences (1554 sequences) |
| T-05-07 | Tampering | removeSkillDirIfEmpty unlinking a user's symlink | medium | mitigate | Lstat guard; TestRemoveSkillDirIfEmpty_NeverUnlinksSymlink |
| T-05-08 | Tampering | writes following a directory symlink planted in a cloned repo's `.agents/skills/codegraph` | medium | mitigate | a non-empty unmanifested target is foreign (kept); file-level links are replaced by fsatomic's temp+rename, never written through; residual (an empty or absent link target receives the package) accepted — local scope runs in the user's own checkout on an explicit command |
| T-05-09 | Repudiation | manifest hash treated as authenticity | low | accept | manifest.go doc (drift signal only) preserved; D-16 hand-edit rewrite reports `updated`, no security event |
| T-05-10 | Tampering | an older binary rewrites a schema-2 manifest at schema 1, dropping `targets` | medium | accept | flagged `costly` above; self-heals on the next new-binary install; loss mode limited to symlinked layouts (legacy read → {claude}) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1` green — nothing on disk changes for users yet
- the invariant test reports 1554 sequences
</verification>

<success_criteria>
- D-05, D-07, D-08, D-14, D-16 implemented at the writer level; D-17's symlink groundwork in place
- AGENT-09 adjacency/empty/ordering edges resolved as explicit truths
</success_criteria>

<output>
Create `.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-02-SUMMARY.md` when done — paste both RED transcripts, the invariant sequence count, and record the {claude} legacy reading as a decision for the maintainer.
</output>
