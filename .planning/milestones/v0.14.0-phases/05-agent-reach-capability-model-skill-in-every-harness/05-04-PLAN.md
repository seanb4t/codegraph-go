---
phase: 05-agent-reach-capability-model-skill-in-every-harness
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - internal/agents/ownership_test.go
  - internal/agents/capabilities.go
  - internal/agents/cursor.go
  - internal/agents/opencode.go
  - internal/agents/cursor_test.go
  - internal/agents/opencode_test.go
  - internal/agents/skillfrontmatter_test.go
  - internal/cli/install.go
  - internal/cli/install_test.go
  - internal/cli/testdata/plain/print-config-style.golden
  - internal/cli/testdata/plain/print-config-style-local.golden
  - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md
autonomous: true
requirements: [AGENT-13, AGENT-04, AGENT-06, AGENT-09]
user_setup: []

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-13: `TestOwnershipExactIdentity` is ONE table test over all 8 targets × {global, local} × {foreign-codegraph-dir, clean} (32 leaves), written RED-first, that seeds a foreign MCP server entry in the table's MCPConfig file, a foreign marker-less section in the table's instructions file (when declared), a foreign `.agents/skills/other/SKILL.md`, and — in the foreign variant — a foreign manifest-less `codegraph/` dir at EVERY new skill location this phase introduces (shared, `.gemini/skills`, `.kiro/skills`, and globally `~/.gemini/antigravity-cli/skills`, `~/.gemini/config/skills`), then runs Install → Uninstall and asserts every planted foreign byte identical and every own entry gone."
    - "D-15: ownership_test.go's doc comment cites commit `242ec0a` by SHA and names the differential it closed — ownership had been granted by (a manifest present at this location) + (matcher name and shape), letting codegraph silently claim and overwrite an unrelated user hook under the same `startup` matcher — and the Claude subtests reproduce that exact precondition (install once so a manifest exists, plant an unrelated SessionStart block under `startup`, re-install, uninstall) (RESEARCH Pitfall 5: the same-name precondition, not merely an unrelated name)."
    - "The ownership test's written-skill-dir expectations come from an INDEPENDENT oracle map in the test (claude → `.claude/skills/codegraph`, cursor and opencode → `.agents/skills/codegraph`, both scopes), cross-checked against `Capabilities().WrittenSkillDir` for every target × supported location — so the table and the oracle cannot drift; 05-05 extends the oracle RED-first."
    - "Cursor and opencode declare `SkillDirs: sharedSkillDirs` and write the shared package through the table: `installDeclaredSkill(&result, t, loc)` / `uninstallDeclaredSkill(&result, t, loc)` (capabilities.go) call 05-02's writer with `refuseUnmanifested` for `Capabilities().WrittenSkillDir(loc)` — install and uninstall derive the skill step from the one table (AGENT-08, D-05, D-06); Cursor's and opencode's instructions handling is unchanged (Cursor: none; opencode: existing AGENTS.md block retained, AGENT-06)."
    - "Installing cursor and opencode at one location yields ONE package: one manifest with targets [cursor, opencode]; the second writer reports SKILL.md `unchanged` and the manifest `updated`; uninstalling cursor keeps the package for opencode; uninstalling opencode removes it (D-05, D-08)."
    - "The embedded SKILL.md's frontmatter `name` equals `codegraph`, matches `^[a-z0-9]+(-[a-z0-9]+)*$` within 1–64 chars, its `description` is non-empty and ≤ 1024 chars, and `name` equals the base name of EVERY directory the table declares as written — the name-must-match-folder rule Cursor, opencode and Kiro enforce, asserted on OUR bytes only (RESEARCH Pitfall 4, AGENT-06, D-00)."
    - "`install`'s per-agent headline treats `kept (foreign)` as not-a-change (a re-run with foreign content present reads `unchanged`), while the per-file line still prints `kept (foreign): <path>` in the Warning role."
    - "Family (b) in 05-MUTATION-LOG.md: (b1) a shape-based ownership recovery planted on skillDirIsForeign's manifest check (a SKILL.md's presence counts as ours) turns `TestOwnershipExactIdentity/cursor/…/foreign-codegraph-dir` RED; (b2) the literal 242ec0a reintroduction — writeHookEntry treating any `startup`-matcher block as owned — turns `TestOwnershipExactIdentity/claude/…` RED; each confirmed applied, byte-clean reverted, green control after (D-13, rule 84d1gfpywd)."
    - "AGENT-13 (probe adjacency, explicit): a foreign entry sharing codegraph's exact key, directory name or hook matcher is never merged into or claimed by codegraph — the foreign `codegraph/` dir is reported `kept (foreign)` and untouched, and an unrelated `startup` SessionStart block survives install→re-install→uninstall as a separate array element."
    - "AGENT-13 (probe empty, explicit): at an unsupported location Install and Uninstall write nothing and every planted byte is identical (codex/hermes/antigravity local rows); a planted foreign file that holds no codegraph content is never rewritten."
    - "AGENT-13 (probe ordering, explicit): install→uninstall restores every planted foreign file byte-for-byte when the fixture is in its writer's canonical form (encoding/json MarshalIndent two-space + trailing newline; hujson Format for opencode's JSONC; the existing TOML/YAML splice fixtures; instructions text ending in exactly one newline), and unowned hook blocks keep their original relative order."
  artifacts:
    - path: "internal/agents/ownership_test.go"
      provides: "TestOwnershipExactIdentity (32 leaves), ownershipWantSkillDir oracle, newSkillDirs"
      contains: "242ec0a"
    - path: "internal/agents/capabilities.go"
      provides: "installDeclaredSkill, uninstallDeclaredSkill"
      contains: "func installDeclaredSkill("
    - path: "internal/agents/skillfrontmatter_test.go"
      provides: "TestSkillFrontmatterMatchesEveryWrittenDir"
    - path: ".planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md"
      provides: "Family (b1), (b2)"
      contains: "## Family (b"
  key_links:
    - from: "internal/agents/cursor.go / opencode.go Install+Uninstall"
      to: "internal/agents/capabilities.go installDeclaredSkill/uninstallDeclaredSkill"
      via: "table-driven skill step"
      pattern: "installDeclaredSkill\\(&result, t, loc\\)"
    - from: "internal/agents/capabilities.go installDeclaredSkill"
      to: "internal/agents/skillshared.go installSkillPackage"
      via: "WrittenSkillDir(loc), refuseUnmanifested"
      pattern: "installSkillPackage\\("
  prohibitions:
    - statement: "MUST NOT overwrite, adopt or remove any foreign entry — MCP server, instructions section, sibling skill dir, manifest-less codegraph dir, or unrelated hook block — during install or uninstall (AGENT-13, D-13, D-14); TestOwnershipExactIdentity is the gate and Family (b) proves it can fail."
      status: resolved
      verification: test
    - statement: "MUST NOT add an instructions write for Cursor in this plan — its repo-root AGENTS.md target is decided only by the D-11 live probe (05-06/05-07)."
      status: resolved
      verification: test
---

<objective>
Write the D-13 ownership guard RED-first, then wire the first two harnesses onto the shared
`.agents/skills/codegraph/` package through the table: Cursor (AGENT-04's skill half) and opencode
(AGENT-06), both documented to read `.agents/skills/` (RESEARCH per-harness findings). Add the SKILL.md
frontmatter contract (our bytes, three harnesses' name-must-match-folder rule), make `kept (foreign)` a
non-change in install's headline, and prove the ownership guard can fail against both shapes of the
historical vulnerability (Family (b)). The live proof that Cursor and opencode actually READ the path is
05-06's, never a unit test's (D-00).

Purpose: AGENT-13 (exact-identity ownership, reversal byte-identical, 242ec0a cited), AGENT-09 (the
shared write, once per run), AGENT-04/AGENT-06 (skill packages installed).

Planner notes:
- Ordering vs CONTEXT's discretion list ("ownership test RED-first → writers → wiring"): the ownership
  guard is RED-first at BOTH layers before the code it guards — at the writer layer in 05-02
  (TestSharedSkillPackage_ForeignDirKeptForeign preceded skillshared.go) and at the harness layer here
  (TestOwnershipExactIdentity precedes every per-harness write). A harness-layer ownership test cannot be
  RED before the writer exists and GREEN before wiring, so this is the only ordering that keeps every
  commit's package suite green outside its own RED commit.
- The two legacy self-heal files (`.cursor/rules/codegraph.mdc`, `.kiro/steering/codegraph.md`) are
  deleted by NAME on install today (pre-existing, pinned by TestCursor_Install_SelfHealsLegacyRulesFile /
  TestKiro_Install_SelfHealsLegacySteeringFile). That is a same-name deletion outside this phase's new
  writes; the ownership test does not plant at those two paths, and the gap is recorded in the SUMMARY
  as an advisory for a follow-up todo rather than changed here.
- TDD: `test(05-04):` RED commit(s) with the pasted `--- FAIL:` transcript; GREEN `feat(05-04):`; never
  `[ci skip]`.

Output: ownership_test.go, table-driven skill wiring for Cursor/opencode, frontmatter test, Family (b).
</objective>

## Review notes (D-15 — AGENT-13's "cited in review")

Commit `242ec0a` ("fix(07-03): revert hook-block ownership recovery, close authorization-differential",
v0.10.0) reverted a matcher+shape recovery fallback in `writeHookEntry` that let codegraph silently claim
and overwrite an unrelated, user-authored hook block sharing Claude's own matcher (`startup`) whenever a
codegraph manifest already existed at that location — ownership granted by manifest-presence plus
shape, not by the block's own command-string identity. Every skill write this phase adds uses the
narrow identity that revert restored: a skill directory is ours only if OUR manifest file is in it
(D-14), never because its name is `codegraph` or it contains a SKILL.md. Reviewers of 05-02…05-05 should
check exactly that, and Family (b1)/(b2) below are the recorded proof that the guard fails if either
identity is widened again.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-01-SUMMARY.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-02-SUMMARY.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-03-SUMMARY.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md
@internal/agents/capabilities.go
@internal/agents/skillshared.go
@internal/agents/cursor.go
@internal/agents/opencode.go
@internal/agents/shared.go
</context>

<interfaces>
From 05-01/05-02/05-03 (internal/agents):
```go
func (c Capabilities) WrittenSkillDir(loc Location) (string, error)
func (c Capabilities) InstructionsPath(loc Location) (string, error)
func (c Capabilities) Supports(loc Location) bool
// c.MCPConfig(loc), c.ConfigFormat (ConfigFormatJSON/JSONC/TOML/YAML)
func sharedSkillDirs(loc Location) ([]string, error)
func installSkillPackage(result *WriteResult, dir string, loc Location, requester TargetID, policy unmanifestedPolicy)
func uninstallSkillPackage(result *WriteResult, dir string, requester TargetID, exclusiveKeys []string, policy unmanifestedPolicy)
const ActionKeptForeign FileAction = "kept (foreign)"
func readManifest(path string) (skillManifest, bool, error)
func claudeHookCommand(loc Location) (string, error)
func hermesConfigured(content string) bool
func findTOMLTableRange(content, table string) (start, end int, found bool)
const codegraphSectionStart = "<!-- CODEGRAPH_START -->"
```
Existing fixture shapes to reuse (canonical, already byte-invariant under their writers): toml_test.go's
unrelated-table fixture, hermes_test.go's round-trip fixture, opencode_test.go's JSONC fixture (normalize
with hujson Parse → Format → Pack before planting), claude_skillpackage_test.go:1130-1228 (the 242ec0a
regression test body).

Surface this plan creates (capabilities.go):
```go
func installDeclaredSkill(result *WriteResult, t AgentTarget, loc Location)
func uninstallDeclaredSkill(result *WriteResult, t AgentTarget, loc Location)
```
</interfaces>

## Artifacts this phase produces

See `05-01-PLAN.md` → "Artifacts this phase produces". This plan creates ownership_test.go,
skillfrontmatter_test.go, installDeclaredSkill/uninstallDeclaredSkill, the Cursor/opencode shared-skill
writes, the kept-foreign headline rule, and Family (b).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-13 ownership table test RED-first, then Cursor and opencode write the shared skill package through the table</name>
  <files>internal/agents/ownership_test.go, internal/agents/capabilities.go, internal/agents/cursor.go, internal/agents/opencode.go, internal/agents/cursor_test.go, internal/agents/opencode_test.go, internal/cli/testdata/plain/print-config-style.golden, internal/cli/testdata/plain/print-config-style-local.golden</files>
  <read_first>
    - internal/agents/claude_skillpackage_test.go lines 1130-1228 (TestClaude_Install_NeverClaimsOwnershipOfUnrelatedHookUnderSameMatcher — the precondition shape to reproduce inside the Claude subtests)
    - internal/agents/cursor.go and opencode.go (Install/Uninstall bodies — cursor's Install returns early on a config-path error; restructure so the skill step still runs and every failure lands in result.Errors, CR-01)
    - internal/agents/cursor_test.go lines 125-141 and opencode_test.go lines 256-262 (DescribePaths tests whose pinned counts change: cursor 1 → 3, opencode 2 → 4)
    - internal/agents/toml_test.go, hermes_test.go, opencode_test.go (canonical foreign fixtures to reuse)
    - internal/agents/skillshared.go (05-02) and capabilities.go (05-01)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-CONTEXT.md (D-05, D-06, D-13, D-14, D-15)
  </read_first>
  <behavior>
    - TestOwnershipExactIdentity: 32 leaves `<id>/<loc>/<variant>`; the test asserts it executed 32. Unsupported rows (codex, hermes, antigravity at local): plant everything, Install and Uninstall both return a WriteResult with no Files and no Errors, and a sorted listing of every path under the fake home and project is identical before and after. Supported rows: after Install — foreign variant: for the oracle dir when it is one of newSkillDirs, WriteResult contains `{dir, "kept (foreign)"}`, no `.codegraph-manifest.json` exists in ANY planted foreign dir, and each planted foreign SKILL.md is byte-identical; clean variant: the oracle dir (if any) holds SKILL.md equal to claudeassets.SkillMarkdown() and a manifest whose targets include the id (the positive "did its work"), and a target with no oracle dir left no manifest under any skill root. After Uninstall (both variants): every planted foreign file byte-identical to its planted bytes; own entries gone — json: no `mcpServers.codegraph`; jsonc: no `mcp.codegraph`; toml: findTOMLTableRange(`mcp_servers.codegraph`) not found; yaml: hermesConfigured false; no `<!-- CODEGRAPH_START -->` in the instructions file; no manifest in the oracle dir; Claude: settings.json SessionStart holds exactly the planted unrelated `startup` block and no command equal to claudeHookCommand(loc). Cross-check subtest: for every target × supported loc, Capabilities().WrittenSkillDir(loc) equals the oracle (`<HOME>` substituted), "" when the oracle has no entry.
    - TestCursor_Install_WritesSharedSkillPackage (global, local): `.agents/skills/codegraph/SKILL.md` (under `<HOME>` for global) equals the embed and its manifest's targets are ["cursor"]; Uninstall removes both and the empty dir
    - TestCursor_DescribePaths_ListsMcpConfigAndSharedSkill: 3 paths — `.cursor/mcp.json`, the shared SKILL.md, the shared manifest
    - TestOpencode_Install_WritesSharedSkillPackage: same for opencode; its AGENTS.md block is still written (AGENT-06 "existing instructions block retained")
    - TestOpencode_CursorShareOnePackage (local): Install cursor then opencode → one manifest, targets ["cursor","opencode"]; opencode's result reports SKILL.md `unchanged` and the manifest `updated`; Uninstall cursor → SKILL.md present, targets ["opencode"]; Uninstall opencode → package and dir gone
    - TestOpencode_DescribePaths: 4 paths (config, AGENTS.md, shared SKILL.md, shared manifest)
  </behavior>
  <action>
    RED first (D-13 "written RED-first"): write internal/agents/ownership_test.go per the behavior block. Doc comment (D-15): cite commit 242ec0a by SHA, quote its subject, and name the differential — ownership granted by manifest-presence plus matcher/shape let codegraph claim and overwrite an unrelated user `startup` hook — and state that this test extends exact-identity ownership (manifest file for skill dirs, command string for hooks) to every harness write. Define `ownershipWantSkillDir` (claude, cursor, opencode × both scopes — the oracle 05-05 extends) and `newSkillDirs(home, loc)` (shared; `.gemini/skills/codegraph`; `.kiro/skills/codegraph`; plus at global `~/.gemini/antigravity-cli/skills/codegraph` and `~/.gemini/config/skills/codegraph`). Plant foreign MCP content at `Capabilities().MCPConfig(loc)` in the writer's canonical form for its ConfigFormat (json: `{"mcpServers":{"other-server":{"args":["serve"],"command":"/opt/other/bin/tool"}}}` via json.MarshalIndent two-space + "\n"; jsonc: opencode's own shape including `$schema`, normalized through hujson Parse/Format/Pack; toml/yaml: reuse the existing byte-invariant fixtures), a foreign instructions section `# My own notes\n\nKeep this paragraph exactly.\n` at InstructionsPath(loc) when non-empty (exactly one trailing newline — flagged assumption, pre-existing T-06-02 append normalization), `.agents/skills/other/SKILL.md`, and in the foreign variant a manifest-less `SKILL.md` ("# someone else's codegraph skill\n") in every newSkillDirs entry. Do NOT plant at the two legacy self-heal paths (planner note). Add the per-harness tests from the behavior block and update the two DescribePaths count tests. Commit `test(05-04): add failing exact-identity ownership table and shared-skill tests (D-13)`; confirm the cursor/opencode ownership leaves, the cross-check and the per-harness tests fail on assertions; paste the `--- FAIL:` lines.

    GREEN:
    - capabilities.go: `installDeclaredSkill(result, t, loc)` — `dir, err := t.Capabilities().WrittenSkillDir(loc)`; err → result.Errors (`resolve <id> skill dir path: %w`); "" → return; else installSkillPackage(result, dir, loc, t.ID(), refuseUnmanifested). `uninstallDeclaredSkill(result, t, loc)` mirrors with uninstallSkillPackage(result, dir, t.ID(), nil, refuseUnmanifested). Doc comment: AGENT-08 — install and uninstall derive the skill step from the one table; every harness except Claude uses these two calls, never its own path literal.
    - cursor.go: literal gains `SkillDirs: sharedSkillDirs` (D-06: Cursor relies on the shared path; a harness-specific dir only if the 05-06 live session shows it is not read); Install calls `installDeclaredSkill(&result, t, loc)` after the MCP step (restructured so a config-path error no longer returns before it); Uninstall calls `uninstallDeclaredSkill(&result, t, loc)` after removeMcpEntry. Instructions: none — unchanged.
    - opencode.go: literal gains `SkillDirs: sharedSkillDirs`; Install/Uninstall call the two helpers after the instructions step; the AGENTS.md block is unchanged.
    - Regenerate the two print-config-style goldens via `-update-plain-goldens` and review: only the cursor and opencode lines change (`skill=<HOME>/.agents/skills/codegraph` / `skill=.agents/skills/codegraph`).
    Commit `feat(05-04): Cursor and opencode install the shared codegraph skill package through the capability table`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(05-04\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestOwnershipExactIdentity$|TestCursor_|TestOpencode_|TestCapabilities' -v 2>&1 | tee /tmp/05-04-t1.txt | tail -3; rg -q '^ok\s' /tmp/05-04-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-04-t1.txt && test "$(rg -c -- '--- PASS: TestOwnershipExactIdentity/[a-z]+/(global|local)/(foreign-codegraph-dir|clean) ' /tmp/05-04-t1.txt)" = "32" && rg -q -- '--- PASS: TestOpencode_CursorShareOnePackage ' /tmp/05-04-t1.txt && rg -q '242ec0a' internal/agents/ownership_test.go && test "$(rg -o 'installDeclaredSkill\(&result, t, loc\)' internal/agents/cursor.go internal/agents/opencode.go | wc -l | tr -d ' ')" = "2" && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 2>&1 | tee /tmp/05-04-pkgs.txt | tail -3; test "$(rg -c '^ok\s' /tmp/05-04-pkgs.txt)" = "2" && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-04-pkgs.txt && rg -q '^cursor: .* skill=\.agents/skills/codegraph hooks=none$' internal/cli/testdata/plain/print-config-style-local.golden && rg -q '^opencode: .* skill=\.agents/skills/codegraph hooks=none$' internal/cli/testdata/plain/print-config-style-local.golden</automated>
    <fails_when>non-zero exit; no "test(05-04):" commit; build/gofmt/vet red; any "--- FAIL"/"FAIL" line; the ownership test executing other than 32 PASS leaves; TestOpencode_CursorShareOnePackage not PASS; no 242ec0a citation in ownership_test.go; cursor.go and opencode.go not each calling installDeclaredSkill; agents/cli not both "ok"; or the local golden's cursor/opencode lines not showing the shared skill dir</fails_when>
  </verify>
  <acceptance_criteria>
    - ownership_test.go's doc comment contains `242ec0a` and the phrase `startup` in its description of the differential
    - the SUMMARY pastes the RED `--- FAIL:` lines from the `test(05-04):` commit, including at least one `TestOwnershipExactIdentity/cursor/` leaf
    - cursor.go declares no instructions path (Instructions field absent or nil) — Cursor's instructions decision waits for D-11
    - opencode's AGENTS.md block is still written by Install (TestOpencode_Install_WritesSharedSkillPackage asserts it)
    - print-config-style goldens were rewritten only via -update-plain-goldens and differ from their previous commit only on the cursor and opencode lines
  </acceptance_criteria>
  <done>A 32-leaf ownership guard over every harness and scope is green after being RED, and Cursor and opencode share one codegraph skill package written and reversed through the table.</done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md frontmatter contract, kept-foreign headline, and Family (b) RED against both shapes of the 242ec0a vulnerability</name>
  <files>internal/agents/skillfrontmatter_test.go, internal/cli/install.go, internal/cli/install_test.go, .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md</files>
  <read_first>
    - .claude/skills/codegraph/SKILL.md lines 1-4 (frontmatter: name, description only)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-RESEARCH.md Pitfall 4 (cursor.com/docs/skills.md, opencode.ai/docs/skills.md, kiro.dev/docs/skills/ — name must match the folder; fetched 2026-09-18)
    - internal/cli/install.go installStatus (lines 115-125) and printAgentResults' action-role switch (lines 181-185)
    - internal/agents/skillshared.go skillDirIsForeign (the single `if fileExists(skillManifestPath(dir)) {` line) and internal/agents/shared.go writeHookEntry's isOwned closure (lines 214-236)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md (Family (a) entries — same shape)
  </read_first>
  <action>
    - internal/agents/skillfrontmatter_test.go — `TestSkillFrontmatterMatchesEveryWrittenDir`: parse claudeassets.SkillMarkdown()'s leading `---` block with the standard library only (split lines, `key: value`); assert name == "codegraph", name matches `^[a-z0-9]+(-[a-z0-9]+)*$` and is 1–64 chars, description non-empty and ≤ 1024 chars; then for every agents target × supported location with a non-empty WrittenSkillDir, filepath.Base(dir) == name; t.Fatalf if fewer than 6 dirs were checked (claude, cursor, opencode × 2 scopes — the floor rises as 05-05 wires more). Comment cites the three docs with the fetch date and D-00 (our bytes only; whether a harness loads the skill is 05-06's live evidence).
    - internal/cli/install.go installStatus: treat agents.ActionKeptForeign like ActionUnchanged when deciding the headline (a foreign dir is not a change codegraph made); leave printAgentResults' role switch as is so the per-file `kept (foreign)` line keeps the Warning role. Add `TestInstallStatus_KeptForeignIsNotAChange` to install_test.go: a WriteResult of {unchanged, kept (foreign)} → "unchanged"; {created, kept (foreign)} → "configured".
    - Commit `feat(05-04): SKILL.md frontmatter contract and kept-foreign install headline`.
    - Family (b) (D-13 "a planted ownership-recovery mutation goes RED", rule 84d1gfpywd), appended to 05-MUTATION-LOG.md in the Family (a) shape, each with pre-mutation `git diff --quiet` gate, a `! git diff --quiet` proof the mutation applied, the diff, verbatim FAIL lines + exit code, `git checkout --` revert, clean `git diff --quiet`, and a green control:
      (b1) skill-dir ownership by shape: `perl -0pi -e 's/if fileExists\(skillManifestPath\(dir\)\) \{/if fileExists(skillManifestPath(dir)) || fileExists(filepath.Join(dir, skillFileName)) {/' internal/agents/skillshared.go`; run `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestOwnershipExactIdentity$' -v`; expect `--- FAIL: TestOwnershipExactIdentity/cursor/` and `/opencode/` foreign-codegraph-dir leaves (the foreign SKILL.md is now "ours" and gets overwritten).
      (b2) the literal 242ec0a reintroduction — hook ownership by matcher: `perl -0pi -e 's/(\tisOwned := func\(block any\) bool \{\n\t\tobj, ok := block\.\(map\[string\]any\)\n\t\tif !ok \{\n\t\t\treturn false\n\t\t\}\n)/$1\t\tif m, _ := obj["matcher"].(string); m == "startup" {\n\t\t\treturn true\n\t\t}\n/' internal/agents/shared.go`; same test run; expect `--- FAIL: TestOwnershipExactIdentity/claude/` leaves (the unrelated `startup` block is claimed and replaced).
      Commit the log as `docs(05-04): record Family (b) ownership-guard RED demonstrations`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 -run 'TestSkillFrontmatterMatchesEveryWrittenDir$|TestInstallStatus_KeptForeignIsNotAChange$' -v 2>&1 | tee /tmp/05-04-t2.txt | tail -3; test "$(rg -c '^ok\s' /tmp/05-04-t2.txt)" = "2" && ! rg -q '^(--- FAIL|FAIL)' /tmp/05-04-t2.txt && test "$(rg -c '^--- PASS: Test(SkillFrontmatterMatchesEveryWrittenDir|InstallStatus_KeptForeignIsNotAChange) ' /tmp/05-04-t2.txt)" = "2" && rm -f /tmp/05-04-red-b2.txt && git diff --quiet -- internal/agents/shared.go && perl -0pi -e 's/(\tisOwned := func\(block any\) bool \{\n\t\tobj, ok := block\.\(map\[string\]any\)\n\t\tif !ok \{\n\t\t\treturn false\n\t\t\}\n)/$1\t\tif m, _ := obj["matcher"].(string); m == "startup" {\n\t\t\treturn true\n\t\t}\n/' internal/agents/shared.go && ! git diff --quiet -- internal/agents/shared.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestOwnershipExactIdentity$' -v > /tmp/05-04-red-b2.txt 2>&1; echo "exit=$?" >> /tmp/05-04-red-b2.txt); git checkout -- internal/agents/shared.go; git diff --quiet -- internal/agents/shared.go && rg -q -- '--- FAIL: TestOwnershipExactIdentity/claude/' /tmp/05-04-red-b2.txt && ! rg -q '^exit=0$' /tmp/05-04-red-b2.txt && rm -f /tmp/05-04-red-b1.txt && git diff --quiet -- internal/agents/skillshared.go && perl -0pi -e 's/if fileExists\(skillManifestPath\(dir\)\) \{/if fileExists(skillManifestPath(dir)) || fileExists(filepath.Join(dir, skillFileName)) {/' internal/agents/skillshared.go && ! git diff --quiet -- internal/agents/skillshared.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestOwnershipExactIdentity$' -v > /tmp/05-04-red-b1.txt 2>&1; echo "exit=$?" >> /tmp/05-04-red-b1.txt); git checkout -- internal/agents/skillshared.go; git diff --quiet -- internal/agents/skillshared.go && rg -q -- '--- FAIL: TestOwnershipExactIdentity/cursor/' /tmp/05-04-red-b1.txt && ! rg -q '^exit=0$' /tmp/05-04-red-b1.txt && rg -q '^## Family \(b' .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestOwnershipExactIdentity$' 2>&1 | tail -1 | rg -q '^ok\s' && echo "Family (b1)/(b2) RED re-demonstrated, both reverted byte-clean, guard green again"</automated>
    <fails_when>non-zero exit; either new test not PASS or any "--- FAIL"/"FAIL" line in /tmp/05-04-t2.txt; shared.go or skillshared.go dirty before a plant or after its revert; a perl plant that did not change the file; the (b2) run lacking a "--- FAIL: TestOwnershipExactIdentity/claude/" line or exiting 0; the (b1) run lacking a "--- FAIL: TestOwnershipExactIdentity/cursor/" line or exiting 0; no Family (b) section in 05-MUTATION-LOG.md; or the ownership test not "ok" after the reverts</fails_when>
  </verify>
  <acceptance_criteria>
    - 05-MUTATION-LOG.md contains (b1) and (b2) entries, each with the pre-mutation gate, the applied diff, verbatim `--- FAIL:` lines and exit code, the revert and a green control
    - skillfrontmatter_test.go cites the three doc URLs and `2026-09-18`
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1` green
  </acceptance_criteria>
  <done>The shipped SKILL.md is valid for every harness that validates names, foreign content never flips install's headline, and the ownership guard is proven to fail against both historical shapes of the ownership vulnerability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/other-tool content in agent config files and skill roots → install/uninstall | every harness's config and skill dirs are shared with the user and other tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Tampering / Elevation of Privilege | Cursor/opencode shared skill writes (the 242ec0a class) | high | mitigate | installDeclaredSkill → refuseUnmanifested; TestOwnershipExactIdentity foreign variant (kept (foreign), bytes identical); Family (b1) RED |
| T-05-16 | Tampering | Claude SessionStart hook ownership by matcher | high | mitigate | exact command-string identity unchanged; Claude ownership leaves reproduce the 242ec0a precondition; Family (b2) RED |
| T-05-17 | Denial of Service | Cursor uninstall removing opencode's package (shared dir) | high | mitigate | D-08 via uninstallDeclaredSkill; TestOpencode_CursorShareOnePackage |
| T-05-18 | Tampering | foreign MCP/instructions content altered by install→uninstall | medium | mitigate | byte-identity assertions on canonical fixtures in all 32 leaves |
| T-05-19 | Tampering | name-based deletion of `.cursor/rules/codegraph.mdc` / `.kiro/steering/codegraph.md` (pre-existing self-heal) | medium | accept | pre-existing behaviour pinned by existing tests, not a write this phase adds; recorded as an advisory follow-up in the SUMMARY |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1` green
- TestOwnershipExactIdentity 32/32; Family (b1)/(b2) recorded and re-demonstrated
</verification>

<success_criteria>
- AGENT-13 roadmap criterion 4 for every harness wired so far, 242ec0a cited in the test and in this plan's review notes (D-13, D-14, D-15)
- AGENT-04/AGENT-06 skill packages installed at the shared path; AGENT-09 shared write once per run
</success_criteria>

<output>
Create `.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-04-SUMMARY.md` when done — paste the RED transcript, the 32-leaf count, Family (b) outcomes, and the legacy self-heal advisory.
</output>
