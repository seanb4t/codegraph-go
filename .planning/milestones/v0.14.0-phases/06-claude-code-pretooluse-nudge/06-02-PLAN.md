---
phase: 06-claude-code-pretooluse-nudge
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/nudge/classify.go
  - internal/nudge/classify_test.go
  - internal/nudge/testdata/true-positives.json
  - internal/nudge/testdata/false-positives.json
  - internal/mcp/skill_claims_drift_test.go
  - .planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md
autonomous: true
requirements: [NUDGE-05, NUDGE-03]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`nudge.Qualifies(ToolShell, cmd)` is true iff the command's first whitespace-separated word, after skipping leading `NAME=value` assignment words, is exactly one of `grep`, `egrep`, `fgrep`, `rg`, `find`; an assignment word containing a quote, backtick, `$` or backslash, an empty command, or a command made only of assignments is parse doubt and returns false (D-02) — so `git log | grep x`, `sudo find …`, `\"grep\" …` and `FOO=\"a b\" grep …` stay silent."
    - "`Qualifies(ToolGrep, _)` and `Qualifies(ToolGlob, _)` are always true; `Qualifies(ToolRead, path)` is true unless the path is empty or its lower-cased extension is one of `.md`, `.json`, `.yaml`, `.yml`, `.toml`, `.txt`, `.lock` (D-03); any other tool is false."
    - "D-15: two harness-neutral corpora, `internal/nudge/testdata/true-positives.json` (where-is-X) and `false-positives.json` (legitimate grep use: literals, logs, config, non-code reads), rows `{tool, input, want, note}`; `TestQualifiesCorpora` drives every row through `Qualifies` as its own subtest, and a row whose `want` disagrees with its corpus's class (an accepted miss or an accepted false positive) must carry a non-empty `note` naming why."
    - "`TestCorporaShape` is the corpora's positive floor: each file has ≥ 14 rows; the true-positive corpus covers all four tools and ≥ 12 of its rows fire; the false-positive corpus covers shell and read and ≥ 12 of its rows stay silent; the test logs both measured rates (NUDGE-05's unit-level half — the live fire rate is 06-06)."
    - "D-14 drift guards over the Go constant itself (never a file read — RESEARCH Pitfall 4): `TestPreToolUseNudgeTextNamesOnlyRealTools` (every `codegraph_<name>` token is in allToolNames(), at least one exists, and docNamesCompanionsWithoutTheFilter is nil), `TestPreToolUseNudgeTextCarriesNoUnpinnedFacts` (no host path, no numeric or count claim, no env token other than CODEGRAPH_MCP_TOOLS), `TestPreToolUseNudgeTextIsFactualOneLiner` (one line, names `codegraph_explore` and `` `codegraph explore` ``, none of the imperative/injection-adjacent words on its denylist)."
    - "Family (b) in 06-MUTATION-LOG.md: (b1) substring matching instead of first-word equality turns a false-positive row RED; (b2) removing the assignment-skip turns the `LC_ALL=C grep` true-positive row RED; (b3) one character flipped inside `nudge.Text`'s tool name turns TestPreToolUseNudgeTextNamesOnlyRealTools RED; each reverted byte-clean with a green control."
  artifacts:
    - path: "internal/nudge/classify.go"
      provides: "Qualifies with the D-02 first-word rule and the D-03 Read exclusion list"
      contains: "func Qualifies("
    - path: "internal/nudge/testdata/true-positives.json"
      provides: "D-15 where-is-X corpus"
    - path: "internal/nudge/testdata/false-positives.json"
      provides: "D-15 legitimate-grep-use corpus"
    - path: "internal/nudge/classify_test.go"
      provides: "TestQualifiesCorpora, TestCorporaShape, TestQualifiesShellFirstWord, TestQualifiesReadNonCodeExtensions"
    - path: "internal/mcp/skill_claims_drift_test.go"
      provides: "TestPreToolUseNudgeTextNamesOnlyRealTools, TestPreToolUseNudgeTextCarriesNoUnpinnedFacts, TestPreToolUseNudgeTextIsFactualOneLiner"
  key_links:
    - from: "internal/nudge/classify_test.go"
      to: "internal/nudge/testdata/*.json"
      via: "encoding/json fixture load, one t.Run per row"
      pattern: "testdata/(true|false)-positives\\.json"
    - from: "internal/mcp/skill_claims_drift_test.go"
      to: "internal/nudge Text"
      via: "the drift helpers applied to the constant's value, no os.ReadFile"
      pattern: "nudge\\.Text"
  prohibitions:
    - statement: "MUST NOT fire on a shell command whose first word is not grep/egrep/fgrep/rg/find, and MUST stay silent on any parse doubt (D-02) — the false-positive corpus and Family (b1) are the gate."
      status: resolved
      verification: test
    - statement: "MUST NOT phrase the nudge as an instruction or a system-style command, and MUST NOT state a fact the repo does not pin (a tool name outside allToolNames, a count, a host path, an env token) — D-14; the three drift guards are the mechanical half, the factual-tone judgement is the reviewer's."
      status: resolved
      verification: judgment
---

<objective>
Fill the tracer's classifier stub with D-02's two-layer Bash rule and D-03's Read exclusion, pin it with the
two D-15 harness-neutral corpora (true positives: where-is-X; false positives: legitimate grep use), and
extend the existing nudge-text drift guards to the new Go constant (D-14). Prove every new guard RED
(Family (b)).

Purpose: NUDGE-05's corpus validation (the live fire rate is 06-06) and NUDGE-03's "pointing at
codegraph_explore" text honesty. The corpora are the contract CODEX-05 reuses in Phase 7 through its own
envelope adapter, so their rows name harness-neutral tools (`shell`, `grep`, `glob`, `read`), never
Claude's tool names.

Planner notes:
- The corpora pin what the heuristic DOES, and label where that differs from the ideal: D-02 inspects only
  the first word, so `grep -c ERROR app.log` fires (an accepted false positive the 60 s cooldown bounds)
  and `git grep …` does not (an accepted miss — Claude's own `Bash(grep *)` rule never matches it
  either). Those rows carry `want` = what `Qualifies` returns plus a required `note`; inventing argument
  inspection to "fix" them would exceed D-02 and is out of scope.
- TDD: Task 1's new tests land RED against the tracer stub (shell/read always false) in a `test(06-02):`
  commit; paste the `--- FAIL:` lines. Task 2's drift guards pass against the tracer's constant on first
  run (characterization of a pinned string) — their positive control is Family (b3).
- Sequential by design (see 06-01 planner notes). GOTOOLCHAIN=go1.26.6 on every Go command.

Output: `test(06-02):` RED commit, `feat(06-02):` GREEN commit, `test(06-02):` drift-guard commit,
06-MUTATION-LOG.md Family (b).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-RESEARCH.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-PATTERNS.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-01-SUMMARY.md
@internal/nudge/classify.go
@internal/nudge/text.go
@internal/mcp/skill_claims_drift_test.go
</context>

<interfaces>
From 06-01 (unchanged signatures this plan fills):
```go
package nudge
type Tool string // ToolShell "shell", ToolGrep "grep", ToolGlob "glob", ToolRead "read"
func Qualifies(tool Tool, input string) bool
const Text = "This repo has a codegraph index: codegraph_explore (CLI: `codegraph explore`) returns the matching symbols' source and call paths for where-is-X and how-does-Y questions."
```
Corpus row (both files are a JSON array of these):
```json
{"tool": "shell", "input": "grep -rn \"func NewServer\" .", "want": true, "note": ""}
```
Existing drift helpers in package mcp (test files), each taking a plain string:
`toolNameTokenRe` (codegraph_[a-z]+), `allToolNames()`, `docNamesCompanionsWithoutTheFilter(doc) error`,
`hostFactsIn(doc) []string`, `numericClaimsMultiset(text) map[string]int`, `countClaimsIn(doc) []countClaim`,
`envVarTokenRe`, `allowlistEnvName` (= "CODEGRAPH_MCP_TOOLS").
</interfaces>

## Artifacts this phase produces

See `06-01-PLAN.md` → "Artifacts this phase produces". This plan: `Qualifies` (completed), the two corpora,
`TestQualifiesCorpora`, `TestCorporaShape`, `TestQualifiesShellFirstWord`, `TestQualifiesReadNonCodeExtensions`,
the three `TestPreToolUseNudgeText*` drift guards, and 06-MUTATION-LOG.md Family (b).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02/D-03 classifier and the D-15 true/false-positive corpora</name>
  <files>internal/nudge/classify.go, internal/nudge/classify_test.go, internal/nudge/testdata/true-positives.json, internal/nudge/testdata/false-positives.json</files>
  <read_first>
    - internal/nudge/classify.go (the 06-01 stub and its doc comment)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md (D-02, D-03, D-15; the specifics on why the `if` rules also match pipe tails)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-PATTERNS.md (classify.go section: the `{name, body, want}` table-test shape to mirror, with the fixture loaded by encoding/json)
  </read_first>
  <behavior>
    - TestQualifiesCorpora: loads both files; subtest names `true-positives/<index>` and `false-positives/<index>`; each asserts `Qualifies(Tool(row.Tool), row.Input) == row.Want` with a failure message quoting the row
    - TestCorporaShape: each file ≥ 14 rows; every row's tool is one of shell/grep/glob/read OR the literal `webfetch` (the unknown-tool row); a true-positive row with want=false or a false-positive row with want=true has a non-empty note; true-positives cover all four tools and ≥ 12 rows want=true; false-positives cover shell and read and ≥ 12 rows want=false; logs `true-positive corpus: X/Y fire; false-positive corpus: X/Y fire (accepted, noted)`
    - TestQualifiesShellFirstWord (inline table, one subtest per case): `grep x`, `  rg x`, `find . -name x`, `egrep x`, `fgrep x`, `LC_ALL=C grep x`, `A=1 B=2 rg x`, `grep x | head` → true; ``, `   `, `A=1`, `git log | grep x`, `cd d && grep x`, `sudo find /`, `"grep" x`, `$(which grep) x`, `FOO="a b" grep x`, `X=$(pwd) grep x`, `GREP x`, `grep;ls` → false
    - TestQualifiesReadNonCodeExtensions: `.md`, `.MD`, `.json`, `.yaml`, `.yml`, `.toml`, `.txt`, `.lock` → false; `.go`, `.tsx`, `.py`, `Makefile` (no extension), `.mdx`, `.json5` → true; empty path → false
  </behavior>
  <action>
    RED: write classify_test.go (the four tests above) and the two corpora with exactly these rows (JSON strings escaped as needed; `note` "" unless stated):
    true-positives.json — shell: `grep -rn "func NewServer" .` (true); `grep -rn 'type Engine struct' internal/` (true); `rg -n "func ParseConfig"` (true); `rg --type go "Execute\(" internal/cli` (true); `find . -name "*.go" -path "*handler*"` (true); `LC_ALL=C grep -rn handleRequest internal` (true); `egrep -rn "class .*Service" src` (true); `fgrep -rn "NewClient(" .` (true); `rg -l "implements Repository" | head -20` (true); `git grep -n "func Parse"` (false, note: accepted miss — first word is git; D-02 checks only the first word and Claude's Bash(grep *) rule never matches git grep); `cd internal && grep -rn Alpha .` (false, note: accepted miss — compound command, first word cd; D-02 parse-doubt rule); grep: `func Execute\(` (true), `type .*Store interface` (true); glob: `**/*handler*.go` (true), `internal/**/server.go` (true); read: `/repo/internal/cli/root.go` (true), `/repo/src/components/App.tsx` (true), `/repo/lib/parser.py` (true).
    false-positives.json — shell: `git log --oneline | grep fix` (false); `ps aux | grep codegraph` (false); `cat build.log | grep -i error` (false); `kubectl get pods -A | grep Running` (false); `FOO="a b" grep -r x .` (false); `sudo find / -name core` (false); `"grep" -rn x .` (false); `$(which grep) -rn x .` (false); `ls -la` (false); `grep -c ERROR app.log` (true, note: accepted false positive — first word is grep; D-02 does not inspect arguments; the 60 s cooldown bounds the cost); `find /tmp -mtime +7 -delete` (true, note: accepted false positive — first word is find; same reason); read: `/repo/README.md`, `/repo/package.json`, `/repo/.github/workflows/ci.yml`, `/repo/config/app.yaml`, `/repo/Cargo.toml`, `/repo/notes.txt`, `/repo/yarn.lock`, `/repo/docs/GUIDE.MD`, `` (empty) (all false); grep: `TODO` (true, note: accepted false positive — the Grep tool always qualifies, D-03); tool `webfetch` input `https://example.com` (false).
    Run them against the 06-01 stub: the shell/read true-positive rows and the first-word/extension cases fail on assertions. Commit `test(06-02): add D-15 corpora and failing classifier tests`; paste the `--- FAIL:` lines.

    GREEN (classify.go, D-02/D-03): shell → `fields := strings.Fields(input)`; skip leading fields that match `^[A-Za-z_][A-Za-z0-9_]*=` — if such a field contains any of the characters quote, double quote, backtick, `$` or backslash, return false (parse doubt); if no field remains, false; otherwise true iff the first remaining field is exactly one of grep/egrep/fgrep/rg/find (a package-level set). grep/glob → true. read → false for empty; else `strings.ToLower(filepath.Ext(input))` ∈ {.md, .json, .yaml, .yml, .toml, .txt, .lock} → false, otherwise true. Anything else → false. No regexp is compiled per call (package-level vars only). Update the doc comments: cite D-02 (two layers: Claude's `if` pre-filter, this function authoritative; the `if` rule also matches pipe tails such as `git log | grep`, which is why only the first word counts), D-03, D-15, and that CODEX-05 reuses this function and the corpora. Commit `feat(06-02): D-02 first-word shell rule and D-03 Read exclusions for the nudge classifier`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(06-02\): ' && printf '%s\n' "$S" | rg -q '^feat\(06-02\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/nudge)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/nudge/ && GOTOOLCHAIN=go1.26.6 go test ./internal/nudge/ -count=1 -run 'TestQualifiesCorpora$|TestCorporaShape$|TestQualifiesShellFirstWord$|TestQualifiesReadNonCodeExtensions$' -v 2>&1 | tee /tmp/06-02-t1.txt | tail -3; rg -q '^ok\s' /tmp/06-02-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/06-02-t1.txt && test "$(rg -c '^--- PASS: (TestQualifiesCorpora|TestCorporaShape|TestQualifiesShellFirstWord|TestQualifiesReadNonCodeExtensions) ' /tmp/06-02-t1.txt)" = "4" && test "$(rg -c -- '--- PASS: TestQualifiesCorpora/true-positives/' /tmp/06-02-t1.txt)" -ge 14 && test "$(rg -c -- '--- PASS: TestQualifiesCorpora/false-positives/' /tmp/06-02-t1.txt)" -ge 14 && rg -q 'true-positive corpus: [0-9]+/[0-9]+ fire' /tmp/06-02-t1.txt && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestHookPreToolUse_' 2>&1 | tail -1 | rg -q '^ok\s'</automated>
    <fails_when>non-zero exit; no "test(06-02):"/"feat(06-02):" commit; build/gofmt/vet red; any "--- FAIL"/"FAIL" line; fewer than the 4 named top-level PASS lines; fewer than 14 PASS row subtests in either corpus; the rate log line absent; or the 06-01 hook tests no longer "ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - both corpora parse as JSON arrays of objects with exactly the keys tool, input, want, note: `jq -e 'type=="array" and all(.[]; (keys == ["input","note","tool","want"]))' internal/nudge/testdata/true-positives.json > /dev/null && jq -e 'type=="array" and all(.[]; (keys == ["input","note","tool","want"]))' internal/nudge/testdata/false-positives.json > /dev/null`
    - every disagreeing row is noted: `jq -e 'all(.[]; .want == true or (.note|length) > 0)' internal/nudge/testdata/true-positives.json` and `jq -e 'all(.[]; .want == false or (.note|length) > 0)' internal/nudge/testdata/false-positives.json`
    - classify.go compiles no regexp inside Qualifies: `! awk '/^func Qualifies/,/^}/' internal/nudge/classify.go | rg -q 'regexp\.(MustCompile|Compile)'`
    - the SUMMARY carries the pasted RED `--- FAIL:` lines and the two measured corpus rates
  </acceptance_criteria>
  <done>The classifier implements D-02 and D-03 exactly, and two harness-neutral corpora pin its behaviour row by row with every accepted miss and false positive explained.</done>
</task>

<task type="auto">
  <name>Task 2: D-14 drift guards over the Go constant, and Family (b) RED</name>
  <files>internal/mcp/skill_claims_drift_test.go, .planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md</files>
  <read_first>
    - internal/mcp/skill_claims_drift_test.go lines 95-130 and 795-860 (TestNudgeTextNamesOnlyRealTools / TestNudgeTextCarriesNoUnpinnedFacts — the two tests to mirror)
    - internal/mcp/resources_schema_drift_test.go lines 100-110, 300-420, 500-580 and internal/mcp/instructions_contract_test.go lines 380-410 (the helpers, each taking a plain string)
    - internal/nudge/text.go
    - .planning/phases/06-claude-code-pretooluse-nudge/06-RESEARCH.md Pitfall 4 (the new text is a Go constant, not a file)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md (Family (a) entry shape)
  </read_first>
  <action>
    In internal/mcp/skill_claims_drift_test.go add (import `github.com/seanb4t/codegraph-go/internal/nudge`; these tests read no file at all — the doc string is `nudge.Text` itself, and their comments say so by concept rather than naming the file-reading call, which the acceptance gate greps for):
    - `TestPreToolUseNudgeTextNamesOnlyRealTools`: same body as TestNudgeTextNamesOnlyRealTools with `doc := nudge.Text` — zero toolNameTokenRe matches is a t.Fatal (the test would verify nothing); each match must be in allToolNames(); docNamesCompanionsWithoutTheFilter(doc) must be nil.
    - `TestPreToolUseNudgeTextCarriesNoUnpinnedFacts`: same body as TestNudgeTextCarriesNoUnpinnedFacts over `nudge.Text` (hostFactsIn, numericClaimsMultiset, countClaimsIn all empty; every envVarTokenRe match equals allowlistEnvName).
    - `TestPreToolUseNudgeTextIsFactualOneLiner` (D-14's "factual one-liner" wording rule, ours): no "\n" in the text; it contains `codegraph_explore` and the backticked `codegraph explore`; and, case-insensitively, it contains none of: `important`, `system`, `must`, `always`, `never`, `ignore`, `instead of`, `do not`, `don't`, `you should`, `stop`. Doc comment: the hooks docs advise factual wording because imperative "system-command" phrasing can trip prompt-injection defences (06-CONTEXT D-14); this denylist is the mechanical floor, review is the rest.
    Doc comment on the first two: they are the SECOND layer over the PreToolUse text — the FIRST (byte pin) is the hand-typed JSON oracle in internal/cli/hook_pretooluse_test.go. Run them (they pass against the pinned constant — characterization). Commit `test(06-02): extend nudge-text drift guards to the PreToolUse constant`.

    Family (b) (same protocol as Family (a): pre-mutation `git diff --quiet -- <file>`, perl plant, capture the verbatim FAIL line + exit code, `git checkout -- <file>`, `git diff --quiet`, green control):
    - (b1) internal/nudge/classify.go: make the shell branch return true when the input merely CONTAINS one of the five words (e.g. replace the first-field membership test with a `strings.Contains(input, w)` loop; record the exact diff) → `--- FAIL: TestQualifiesCorpora/false-positives/…` (the `git log --oneline | grep fix` row).
    - (b2) internal/nudge/classify.go: delete the assignment-skip loop so the first field is always tested → `--- FAIL: TestQualifiesCorpora/true-positives/…` (the `LC_ALL=C grep …` row).
    - (b3) internal/nudge/text.go: `perl -pi -e 's/codegraph_explore \(/codegraph_explorer (/' internal/nudge/text.go` → `--- FAIL: TestPreToolUseNudgeTextNamesOnlyRealTools` (run `go test ./internal/mcp/ -run 'TestPreToolUseNudgeTextNamesOnlyRealTools$'`).
    Append the three sections to 06-MUTATION-LOG.md; commit `docs(06-02): record Family (b) classifier and nudge-text RED demonstrations`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/mcp/ -count=1 -run 'TestPreToolUseNudgeTextNamesOnlyRealTools$|TestPreToolUseNudgeTextCarriesNoUnpinnedFacts$|TestPreToolUseNudgeTextIsFactualOneLiner$|TestNudgeTextNamesOnlyRealTools$|TestNudgeTextCarriesNoUnpinnedFacts$' -v 2>&1 | tee /tmp/06-02-t2.txt | tail -3; rg -q '^ok\s' /tmp/06-02-t2.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/06-02-t2.txt && test "$(rg -c '^--- PASS: (TestPreToolUseNudgeTextNamesOnlyRealTools|TestPreToolUseNudgeTextCarriesNoUnpinnedFacts|TestPreToolUseNudgeTextIsFactualOneLiner|TestNudgeTextNamesOnlyRealTools|TestNudgeTextCarriesNoUnpinnedFacts) ' /tmp/06-02-t2.txt)" = "5" && rm -f /tmp/06-02-red-b3.txt && git diff --quiet -- internal/nudge/text.go && perl -pi -e 's/codegraph_explore \(/codegraph_explorer (/' internal/nudge/text.go && ! git diff --quiet -- internal/nudge/text.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/mcp/ -count=1 -run 'TestPreToolUseNudgeTextNamesOnlyRealTools$' -v > /tmp/06-02-red-b3.txt 2>&1; echo "exit=$?" >> /tmp/06-02-red-b3.txt); git checkout -- internal/nudge/text.go; git diff --quiet -- internal/nudge/text.go && rg -q -- '--- FAIL: TestPreToolUseNudgeTextNamesOnlyRealTools' /tmp/06-02-red-b3.txt && ! rg -q '^exit=0$' /tmp/06-02-red-b3.txt && M=.planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md && rg -q '^## Family \(b1\)' "$M" && rg -q '^## Family \(b2\)' "$M" && rg -q '^## Family \(b3\)' "$M" && rg -q -- '--- FAIL: TestQualifiesCorpora/false-positives/' "$M" && rg -q -- '--- FAIL: TestQualifiesCorpora/true-positives/' "$M" && GOTOOLCHAIN=go1.26.6 go test ./internal/nudge/ ./internal/mcp/ -count=1 2>&1 | tee /tmp/06-02-pkgs.txt | tail -2; test "$(rg -c '^ok\s' /tmp/06-02-pkgs.txt)" = "2" && ! rg -q '^(--- FAIL|FAIL)' /tmp/06-02-pkgs.txt</automated>
    <fails_when>non-zero exit; any "--- FAIL"/"FAIL" in /tmp/06-02-t2.txt; fewer than the 5 named PASS lines (3 new + the 2 pre-existing SessionStart guards); text.go not clean before the plant or after the revert; the (b3) plant not producing the NamesOnlyRealTools FAIL line or exiting 0; 06-MUTATION-LOG.md missing a (b1)/(b2)/(b3) section or the corpus FAIL transcripts; or internal/nudge and internal/mcp not both "ok" afterwards</fails_when>
  </verify>
  <acceptance_criteria>
    - the new drift tests read no file: `! awk '/^func TestPreToolUseNudgeText/,/^}/' internal/mcp/skill_claims_drift_test.go | rg -q 'os\.ReadFile'`
    - the new tests reference the constant: `test "$(rg -o 'nudge\.Text' internal/mcp/skill_claims_drift_test.go | wc -l | tr -d ' ')" -ge 3`
    - `internal/nudge/text.go` is byte-identical to its 06-01 commit (this plan changes no text): `C=$(git log -1 --format=%H -E --grep='^feat\(06-01\): ') && test -n "$C" && git diff --quiet "$C" HEAD -- internal/nudge/text.go`
    - no `[ci skip]`/`[skip ci]` in any 06-02 commit: `L=$(git log --format=%B -E --grep='^(test|feat|docs)\(06-02\)') && test -n "$L" && ! printf '%s\n' "$L" | rg -q -i '\[(ci skip|skip ci)\]'`
  </acceptance_criteria>
  <done>The PreToolUse text is held to the same honesty guards as the SessionStart nudge, plus a factual-wording floor, and every classifier and text guard is shown able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude tool input → Qualifies | shell commands and file paths are untrusted strings |
| nudge text → agent context | the injected sentence is read by the model as context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Tampering (misclassification by crafted input) | Qualifies shell branch | low | mitigate | exact first-word equality; any quote/backtick/`$`/backslash in a leading assignment is parse doubt → silent; corpora + Family (b1)/(b2) |
| T-06-10 | Spoofing (context that reads like a system instruction) | nudge.Text | medium | mitigate | factual wording pinned byte-for-byte; TestPreToolUseNudgeTextIsFactualOneLiner denylist; tool names pinned to allToolNames(); Family (b3) |
| T-06-11 | Denial of Service (regex cost per call) | Qualifies | low | mitigate | no per-call regexp compilation (acceptance check); string splitting only |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/nudge/ ./internal/mcp/ ./internal/cli/ -count=1` green
- both corpora ≥ 14 rows, every disagreeing row noted, measured rates in the SUMMARY
- 06-MUTATION-LOG.md Family (b1)-(b3) recorded with byte-clean reverts
</verification>

<success_criteria>
- NUDGE-05 (corpus half): the classifier is validated against a false-positive and a true-positive corpus (roadmap criterion 3; the live fire rate is 06-06)
- D-02, D-03, D-14 (drift guards), D-15 implemented as locked; D-00 respected
</success_criteria>

<output>
Create `.planning/phases/06-claude-code-pretooluse-nudge/06-02-SUMMARY.md` when done — the RED lines, both measured corpus rates, and the Family (b) outcome.
</output>
