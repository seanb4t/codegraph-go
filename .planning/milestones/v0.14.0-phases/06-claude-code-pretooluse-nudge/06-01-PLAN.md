---
phase: 06-claude-code-pretooluse-nudge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/nudge/classify.go
  - internal/nudge/text.go
  - internal/cli/hook_pretooluse.go
  - internal/cli/hook_pretooluse_test.go
  - internal/cli/root.go
  - internal/cli/testdata/cli-reference-allowlist.txt
  - internal/cli/install.go
  - docs/CLI-REFERENCE.md
  - .claude/hooks/pretooluse-nudge.sh
  - .claude/hooks/hooks.json
  - claudeassets.go
  - internal/agents/types.go
  - internal/agents/claude.go
  - internal/agents/claude_pretooluse.go
  - internal/agents/claude_pretooluse_test.go
  - .planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md
autonomous: true
requirements: [NUDGE-03, NUDGE-04, NUDGE-06]
user_setup: []

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`codegraph install --target claude --location local --pretool-nudge`, run by the real binary, writes an executable `.claude/hooks/pretooluse-nudge.sh` whose binary path is the installing binary's absolute ExecPath (single-quoted), and adds four codegraph-owned `hooks.PreToolUse` blocks to `.claude/settings.json` — `Bash` (three handlers, `if` = `Bash(grep *)`, `Bash(rg *)`, `Bash(find *)`), `Grep`, `Glob`, `Read` — every handler `{type: command, command: ${CLAUDE_PROJECT_DIR}/.claude/hooks/pretooluse-nudge.sh, timeout: 5}` with no `statusMessage` (D-01, D-01b, D-02, D-12)."
    - "Piping a Claude `PreToolUse` Grep event with a session id through the registered command in an indexed directory prints exactly one line, the pinned `{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"<nudge.Text>\"}}`, and exits 0; with `.codegraph/` removed it prints nothing and exits 0 (NUDGE-03, D-04)."
    - "A default install (no `--pretool-nudge`) writes no guard script and no `hooks.PreToolUse` key — the nudge is opt-in (D-09, NUDGE-06)."
    - "The logic lives in the hidden two-level command `codegraph hook pretooluse`: both `hook` and `pretooluse` are `Hidden`, absent from `commandGroups`, covered by the two bare allowlist lines `codegraph hook` and `codegraph hook pretooluse`; `RunE` recovers panics around its whole body (stdin read included) and always returns nil, so `cmd/codegraph/main.go`'s exit-1 path is unreachable from it (D-01a)."
    - "`internal/nudge` is a stdlib-only, harness-neutral package (no `internal/` import): `Qualifies(tool, input)` and the byte-pinned `Text` constant; the Claude stdin/stdout envelope lives only in `internal/cli/hook_pretooluse.go` (D-01a, CODEX-05 reuses the core)."
    - "The guard (D-01 jobs a-c): the `[ -d \"${CLAUDE_PROJECT_DIR:-.}/.codegraph\" ]` check runs first and exits 0 before any process starts or stdin is read; a missing, non-regular or non-executable binary exits 0 silently; the binary runs as a child process with stdin inherited and the script's last line is `exit 0` whatever the binary did — there is no errexit option and the shell process is never replaced."
    - "Guard-level D-16 suite (`TestPreToolUseGuard`, 9 subtests over the REAL embedded template rendered with stub binaries): `.codegraph` missing and `.codegraph` a regular file start no binary; binary missing, not executable, exiting 3, and crashing on SIGSEGV all exit 0; `CLAUDE_PROJECT_DIR` unset resolves against the working directory; stdin reaches the binary byte-identical and the binary's stdout is the guard's stdout, unmodified."
    - "`renderPreToolGuard` replaces the template's single token `'@codegraph-exec-path@'` with a POSIX single-quoted ExecPath (`'` becomes `'\\''`), rejects an empty or relative path, and a rendered guard whose ExecPath contains a space and a single quote still runs that binary; the embedded template carries the token exactly once."
    - "The checked-in, unrendered `.claude/hooks/pretooluse-nudge.sh` (this repo's dogfood source) falls back to `command -v codegraph` ONLY while the token is unrendered — installed guards never consult PATH (D-01b)."
    - "Family (a) in 06-MUTATION-LOG.md: (a1) the guard's final `exit 0` turned into `exit 2` makes `TestPreToolUseGuard/binary_exits_nonzero` RED; (a2) replacing `shellSingleQuote` with naive wrapping makes `TestRenderPreToolGuard/quote_and_space_in_path` RED; each reverted byte-clean with a green control (D-00, D-16)."
  artifacts:
    - path: "internal/nudge/classify.go"
      provides: "Tool, ToolShell/ToolGrep/ToolGlob/ToolRead, Qualifies (Grep/Glob true in this plan; Bash/Read filled by 06-02)"
      contains: "func Qualifies("
    - path: "internal/nudge/text.go"
      provides: "Text — the byte-pinned D-14 nudge sentence"
      contains: "const Text ="
    - path: "internal/cli/hook_pretooluse.go"
      provides: "newHookCmd, newHookPreToolUseCmd, runHookPreToolUse, maxHookStdinBytes, the Claude envelope types"
      contains: "Hidden: true"
    - path: ".claude/hooks/pretooluse-nudge.sh"
      provides: "the embedded guard template and this repo's dogfood guard (mode 100755)"
      contains: "'@codegraph-exec-path@'"
    - path: "internal/agents/claude_pretooluse.go"
      provides: "claudePreToolFragmentCommand, claudePreToolGuardPath, claudePreToolHookCommand, claudePreToolUseBlocks, renderPreToolGuard, shellSingleQuote, preToolGuardExecPathToken"
      contains: "func renderPreToolGuard("
    - path: "internal/agents/claude_pretooluse_test.go"
      provides: "TestClaude_Install_PreToolNudgeOn_WritesGuardAndBlocks, TestClaude_Install_DefaultWritesNoPreToolUse, TestPreToolUseGuard, TestRenderPreToolGuard, TestPreToolUseGuardSourceFallsBackToPATH"
    - path: ".planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md"
      provides: "Phase 6 mutation log, Family (a)"
      contains: "## Family (a"
  key_links:
    - from: ".claude/settings.json hooks.PreToolUse (as written by install)"
      to: ".claude/hooks/pretooluse-nudge.sh"
      via: "registered command string = guard path (ownership identity, never the binary path — D-01b)"
      pattern: "pretooluse-nudge\\.sh"
    - from: ".claude/hooks/pretooluse-nudge.sh"
      to: "internal/cli/hook_pretooluse.go newHookPreToolUseCmd"
      via: "rendered ExecPath invoked as `hook pretooluse` with stdin inherited"
      pattern: "hook pretooluse"
    - from: "internal/cli/hook_pretooluse.go runHookPreToolUse"
      to: "internal/nudge Qualifies / Text"
      via: "pure harness-neutral core behind the Claude envelope adapter"
      pattern: "nudge\\.(Qualifies|Text)"
    - from: "internal/agents/claude.go Install"
      to: "internal/agents/shared.go writeHookEntry(settingsPath, \"PreToolUse\", …)"
      via: "opt-in gate on InstallOptions.PreToolNudge == PreToolNudgeOn"
      pattern: "writeHookEntry\\(settingsPath, \"PreToolUse\""
  prohibitions:
    - statement: "MUST NOT deny, block or ask: nothing this phase ships emits `permissionDecision`, `decision` or `continue`, exits 2, or exits non-zero from the guard on any path (NUDGE-03 contract, 2026-09-14 reframe) — TestPreToolUseGuard and Family (a1) are the gate here; 06-03 adds the subcommand-level gate."
      status: resolved
      verification: test
    - statement: "MUST NOT register the PreToolUse hook without the explicit opt-in — a default install writes no guard and no PreToolUse entry (D-09); TestClaude_Install_DefaultWritesNoPreToolUse is the gate."
      status: resolved
      verification: test
    - statement: "MUST NOT start a process or read stdin in an un-indexed repository (D-04, NUDGE-02's zero-overhead property) — TestPreToolUseGuard/not_indexed and /codegraph_is_file assert the stub binary's start marker is absent."
      status: resolved
      verification: test
---

<objective>
The tracer for the whole phase: prove on the thinnest end-to-end slice that ONE opt-in install writes a
guard and a PreToolUse registration, and that the registered command, fed a real Claude PreToolUse Grep
event in an indexed repo, prints only the pinned `additionalContext` object and exits 0 — through every
layer this phase touches: the harness-neutral core (`internal/nudge`), the hidden `codegraph hook
pretooluse` subcommand (`internal/cli`), the embedded POSIX guard template (`claudeassets`), the install
writer (`internal/agents`), and the `--pretool-nudge` flag. Then pin the guard's own D-16 contract with an
exec suite over the real embedded template and prove it can fail (Family (a)).

Purpose: NUDGE-03 (additionalContext-only, exit 0), NUDGE-04's un-indexed half (the D-04 guard check),
NUDGE-06's registration half (opt-in through the unchanged exact-identity `writeHookEntry`). Later plans
expand this slice: 06-02 fills `Qualifies` for Bash/Read and pins the corpora and text drift guards;
06-03 adds the D-05…D-08 cooldown and the subcommand-level D-16 suite; 06-04 makes the opt-in sticky in
the manifest and removable; 06-05 dogfoods the registration and completes the CLI opt-in; 06-06 is the
D-18 live session; 06-07 regenerates the reference and closes the phase.

Planner notes (read before executing):
- Deliberate tracer stubs (each fillable without an architectural change): `Qualifies` returns true for
  `ToolGrep`/`ToolGlob` and false for `ToolShell`/`ToolRead` until 06-02; the adapter has no cooldown
  yet — it fires whenever the call qualifies and a session id exists (06-03 inserts the D-08 gate between
  classify and emit); `Install` handles only `PreToolNudgeOn` (writes) — `PreToolNudgeKeep` and
  `PreToolNudgeOff` touch nothing until 06-04 gives them their D-10 meaning; the CLI maps `--pretool-nudge`
  (true) to On and leaves every other invocation at Keep until 06-05 adds the cobra `Changed` mapping.
- Allowlist deviation, recorded on purpose: D-01a says "one bare allowlist line", but D-01's own
  two-level name `codegraph hook pretooluse` gives BOTH hidden commands a `--help` flag that
  `TestEveryRegisteredFlagIsAccountedFor` walks (`cli_reference_test.go` visits every command, and a bare
  entry covers only its own command path). Two bare lines — `codegraph hook` and `codegraph hook
  pretooluse` — are the minimum that keeps the guard green without a flag-level entry.
- Discretion choices made here (CONTEXT "Claude's Discretion"): guard name `pretooluse-nudge.sh`; flag
  `--pretool-nudge`; `timeout: 5` (seconds — Claude Code's unit; the default is 600 and a PreToolUse hook
  delays the tool call); core package `internal/nudge`; ExecPath delivery = a single-token template
  rendered with a plain replace plus explicit POSIX single-quote escaping (RESEARCH A1 recommended
  `text/template`, which does NO shell escaping and whose `{{ }}` syntax would make the unrendered
  dogfood file invalid sh — the checked-in file must itself be a valid, executable guard because
  `TestHookRegistrationMatchesFragmentAndScript` requires every registered command to resolve to one).
  The unrendered-copy PATH fallback exists so this repository's own registration (06-05, D-13) actually
  runs here; an installed (rendered) guard never reaches it, so D-01b's "PATH alone is rejected" holds
  for every user install.
- TDD (`workflow.tdd_mode`): Task 1 lands its failing tests as a `test(06-01):` commit with only the
  compile placeholders they need; the pasted `--- FAIL:` transcript in the SUMMARY is the RED evidence
  (never gate on `gsd-tools check tdd-red-evidence` — TAP-only). Task 2 is a characterization suite over
  Task 1's guard; its positive control is Family (a), not a RED-first commit. Never `[ci skip]`/`[skip
  ci]`. Every Go command runs with `GOTOOLCHAIN=go1.26.6`.
- Every hook test clears `CLAUDE_CODE_SESSION_ID` with `t.Setenv("CLAUDE_CODE_SESSION_ID", "")` first —
  the executor itself runs inside Claude Code and inherits a real session id — and points `TMPDIR` at
  `t.TempDir()`, so that once 06-03 adds the sentinel cooldown no test ever reads another run's sentinel.
- Plans in this phase run strictly in sequence (waves 1-7): `workflow.use_worktrees` is false, so
  executors share one working tree; every plan appends to the one `06-MUTATION-LOG.md` (D-00) and plants
  mutations in shared packages.

Output: `test(06-01):` RED commit, `feat(06-01):` GREEN commit (flag + regenerated reference in the same
commit so flag accounting never goes red), `test(06-01):` guard-suite commit, `06-MUTATION-LOG.md` Family (a).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-RESEARCH.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-PATTERNS.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-VALIDATION.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md
@claudeassets.go
@.claude/hooks/session-nudge.sh
@.claude/hooks/hooks.json
@internal/agents/claude.go
@internal/agents/shared.go
@internal/agents/types.go
@internal/agents/hookpackage_test.go
@internal/cli/root.go
@internal/cli/man.go
@internal/cli/install.go
@internal/cli/cli_reference_test.go
@internal/cli/testdata/cli-reference-allowlist.txt
</context>

<interfaces>
<!-- Binding names — later plans, the mutation families and the verify commands target them. -->

internal/nudge (new, stdlib-only, harness-neutral — CODEX-05 reuses it):
```go
package nudge
type Tool string
const (
	ToolShell Tool = "shell" // Claude Bash; input = tool_input.command
	ToolGrep  Tool = "grep"  // Claude Grep; input = tool_input.pattern (ignored)
	ToolGlob  Tool = "glob"  // Claude Glob; input = tool_input.pattern (ignored)
	ToolRead  Tool = "read"  // Claude Read; input = tool_input.file_path
)
func Qualifies(tool Tool, input string) bool // 06-01: grep/glob true, shell/read false; 06-02: D-02/D-03
const Text = "This repo has a codegraph index: codegraph_explore (CLI: `codegraph explore`) returns the matching symbols' source and call paths for where-is-X and how-does-Y questions."
```

internal/cli/hook_pretooluse.go (new):
```go
const maxHookStdinBytes = 1 << 20
type claudePreToolUseInput struct {
	SessionID     string `json:"session_id"`
	AgentID       string `json:"agent_id"`
	HookEventName string `json:"hook_event_name"`
	ToolName      string `json:"tool_name"`
	ToolInput     struct {
		Command  string `json:"command"`
		FilePath string `json:"file_path"`
		Pattern  string `json:"pattern"`
	} `json:"tool_input"`
}
type claudeHookSpecificOutput struct {
	HookEventName     string `json:"hookEventName"`
	AdditionalContext string `json:"additionalContext"`
}
type claudeHookOutput struct {
	HookSpecificOutput claudeHookSpecificOutput `json:"hookSpecificOutput"`
}
func newHookCmd() *cobra.Command            // Use "hook", Hidden, no RunE, AddCommand(newHookPreToolUseCmd())
func newHookPreToolUseCmd() *cobra.Command  // Use "pretooluse", Hidden, cobra.NoArgs, RunE: recover + runHookPreToolUse + return nil
func runHookPreToolUse(in io.Reader, out io.Writer, getenv func(string) string)
```
Pinned stdout on fire (one line, then "\n"):
`{"hookSpecificOutput":{"hookEventName":"PreToolUse","additionalContext":"This repo has a codegraph index: codegraph_explore (CLI: ` + "`codegraph explore`" + `) returns the matching symbols' source and call paths for where-is-X and how-does-Y questions."}}`

internal/agents (types.go + new claude_pretooluse.go):
```go
type PreToolNudgeMode int
const (
	PreToolNudgeKeep PreToolNudgeMode = iota // zero value; D-10 sticky meaning lands in 06-04
	PreToolNudgeOn
	PreToolNudgeOff // D-10 meaning lands in 06-04
)
// InstallOptions gains: PreToolNudge PreToolNudgeMode
const claudePreToolFragmentCommand = "${CLAUDE_PROJECT_DIR}/.claude/hooks/pretooluse-nudge.sh"
const preToolGuardExecPathToken = "'@codegraph-exec-path@'"
func claudePreToolGuardPath(loc Location) (string, error)    // .claude/hooks/pretooluse-nudge.sh | ~/.claude/hooks/pretooluse-nudge.sh
func claudePreToolHookCommand(loc Location) (string, error)  // local: claudePreToolFragmentCommand; global: claudePreToolGuardPath(global)
func claudePreToolUseBlocks(loc Location) ([]any, []string, error)
func renderPreToolGuard(execPath string) (string, error)
func shellSingleQuote(s string) string
// claude.go: extracted, shared by SessionStart and PreToolUse
func claudeFragmentEventBlocks(event, fragmentCommand, ownCommand string) ([]any, error)
```

claudeassets.go gains: `PreToolUseGuardPath = ".claude/hooks/pretooluse-nudge.sh"` and
`func PreToolUseGuardTemplate() ([]byte, error)`.

Existing, unchanged, reused verbatim: `writeHookEntry(path, event string, ownBlocks []any, ownCommands []string)`,
`removeHookEntry(path, event, ownCommands)`, `writeEmbeddedFile(path, content string, executable bool)`,
`recordFile(&result, path, fr, err)`, `claudeSettingsPath(loc)`, `execCmdWithInput(input, args...)` (internal/cli
cli_test.go), `fakeHome(t)` (internal/agents testhelpers_test.go and internal/cli install_test.go).
</interfaces>

## Artifacts this phase produces

Phase-wide list (every later plan points here and lists its own subset):

| Artifact | Kind | Plan |
|---|---|---|
| `internal/nudge` package: `Tool`, `ToolShell`/`ToolGrep`/`ToolGlob`/`ToolRead`, `Qualifies`, `Text` | Go API (harness-neutral core, D-01a) | 06-01 (Qualifies completed 06-02) |
| `internal/nudge/testdata/true-positives.json`, `false-positives.json` (`{tool, input, want, note}` rows) | D-15 corpora | 06-02 |
| `TestQualifiesCorpora`, `TestQualifiesShellFirstWord`, `TestQualifiesReadNonCodeExtensions`, `TestCorporaShape` | tests | 06-02 |
| `TestPreToolUseNudgeTextNamesOnlyRealTools`, `TestPreToolUseNudgeTextCarriesNoUnpinnedFacts`, `TestPreToolUseNudgeTextIsFactualOneLiner` | D-14 drift guards (internal/mcp) | 06-02 |
| `CooldownWindow`, `SessionKey`, `DefaultDir`, `Gate{Dir, Now}`, `(Gate).Due`, unexported `currentUID` seam, `sentinelName`, `checkSentinel`, `recordFire` | Go API, internal/nudge/cooldown.go | 06-03 |
| `TestGate_*`, `TestSentinel_*`, `TestSessionKey`, `TestDefaultDirHonoursTMPDIR` | tests | 06-03 |
| hidden `codegraph hook` + `codegraph hook pretooluse`; `newHookCmd`, `newHookPreToolUseCmd`, `runHookPreToolUse`, `maxHookStdinBytes`, `claudePreToolUseInput`, `claudeHookOutput`, `claudeHookSpecificOutput`; seams `hookNow`, `hookQualifies` (06-03) | CLI, internal/cli/hook_pretooluse.go | 06-01, 06-03 |
| `TestHookPreToolUse_*`, `TestHookCmd_HiddenTwoLevel` | tests | 06-01, 06-03 |
| allowlist lines `codegraph hook`, `codegraph hook pretooluse` | internal/cli/testdata/cli-reference-allowlist.txt | 06-01 |
| `.claude/hooks/pretooluse-nudge.sh` (template + dogfood guard) and `hooks.PreToolUse` in `.claude/hooks/hooks.json` | embedded assets | 06-01 |
| `claudeassets.PreToolUseGuardPath`, `claudeassets.PreToolUseGuardTemplate()` | embed accessor | 06-01 |
| `PreToolNudgeMode` + `PreToolNudgeKeep`/`PreToolNudgeOn`/`PreToolNudgeOff`; `InstallOptions.PreToolNudge` | Go API, internal/agents/types.go | 06-01 (semantics 06-04) |
| `claudePreToolFragmentCommand`, `preToolGuardExecPathToken`, `claudePreToolGuardPath`, `claudePreToolHookCommand`, `claudePreToolUseBlocks`, `renderPreToolGuard`, `shellSingleQuote`, `claudeFragmentEventBlocks` | internal/agents | 06-01 |
| `--pretool-nudge` flag on `install` (Claude-only; true → On; `Changed` tri-state + `note:` in 06-05) | CLI flag | 06-01, 06-05 |
| `manifestKeyPreToolGuard` (`hooks/pretooluse-nudge.sh`), `manifestKeyPreToolFrag` (`settings.json#hooks.PreToolUse`), `preToolNudgeRecorded`, `dropKeys` on the manifest writer; `HookFiles` names the guard | sticky opt-in (D-10), D-11, D-13 | 06-04 |
| `TestPreToolNudge_*` (lifecycle), extended `TestOwnershipExactIdentity`, `TestCapabilitiesMatchInstallWrites` | tests | 06-04 |
| `hooks.PreToolUse` in this repo's `.claude/settings.json`; `TestHookRegistrationMatchesFragmentAndScript/PreToolUse`, `TestPreToolUseRegistrationShape` | dogfood (D-13) | 06-05 |
| `TestInstall_PreToolNudge_*`, `TestRefreshInstalledSkills_CarriesPreToolNudge`; `upgrade` passes `PreToolNudgeKeep` | CLI stickiness (D-09, D-10) | 06-05 |
| `06-MUTATION-LOG.md` Families (a)-(e) | evidence (D-00, D-16) | 06-01…06-05 |
| `06-LIVE-SESSIONS.md` | D-18 live evidence | 06-06 |
| regenerated `docs/CLI-REFERENCE.md`; `install`/`uninstall` help text naming the opt-in | docs | 06-01, 06-05, 06-07 |

## Flagged assumptions (spec-less edge probe — phase-wide, no silent drop)

The edge probe classified 6 rows (0 resolved at probe time). Four are resolved in the plans that own them
— NUDGE-05 boundary (explicit truth) and NUDGE-05 precision (backstop marker) in 06-03; NUDGE-06
idempotency (explicit truth) and NUDGE-06 concurrency (backstop marker) in 06-04. The two `unclassified`
rows stay unresolved and are surfaced here, never dropped (6 = 4 authored + 2 flagged):

1. **NUDGE-03 (unclassified)** — the "never deny, always exit 0" contract is a behavioural invariant, not a
   data-shape edge; its checkable content is the guard-level suite (this plan), the subcommand-level
   forced-error suite (06-03) and Families (a1)/(c4)/(c5). Claude Code's delivery of the context is live
   evidence only (06-06, D-00).
2. **NUDGE-04 (unclassified)** — the cooldown/scope requirement's edges are the NUDGE-05 boundary and
   precision rows resolved in 06-03; the un-indexed zero-overhead half is TestPreToolUseGuard/not_indexed
   (this plan). The real-session fire spacing is live evidence (06-06 criteria 2-5).

Additional flagged assumptions (from CONTEXT/RESEARCH, recorded rather than silently absorbed):
- D-12 mirrors SessionStart's shell form, so the global command is an UNQUOTED absolute guard path and the
  local one an unquoted `${CLAUDE_PROJECT_DIR}` expansion; a home or project path containing a space would
  make Claude Code's `sh -c` fail with a "hook error". Accepted as the locked mirror; the fix is the
  deferred "exec form / quoting" idea, which covers both events.
- A local-scope install commits the installing machine's absolute ExecPath into
  `.claude/hooks/pretooluse-nudge.sh` if the user commits `.claude/`; a teammate's guard then finds no
  binary and exits 0 silently. Same exposure the local `.mcp.json` entry already has (`stdioMcpEntry`).
- Running `codegraph install --location local --pretool-nudge` INSIDE this repository would overwrite the
  checked-in template with a rendered guard; `TestRenderPreToolGuard/template_token_exactly_once` goes RED
  if that ever gets committed.

<assumption_delta_decision>
Detector fired (`pluralization`: "alongside", "second writeHookEntry"). Primary noun: the codegraph-owned
hook entry, identified by (event, exact command string). Decision: `promote` for the fragment derivation —
`claudeSessionStartBlocks`'s decode-and-rewrite loop becomes the event-generic
`claudeFragmentEventBlocks(event, fragmentCommand, ownCommand)` with SessionStart as one caller and
PreToolUse as the second; `no-change` for `writeHookEntry`/`removeHookEntry`/`hashOwnedHookBlocks` and the
manifest's `Files` map, which are already event-generic (no schema bump). Invariant test accepted: the
ownership table extension in 06-04 (every event codegraph registers round-trips install→uninstall with an
unrelated same-event, same-matcher entry byte-identical).
</assumption_delta_decision>

<!-- planner-discipline-allow: '@codegraph-exec-path@' -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "install --pretool-nudge registers a guard that nudges on Grep" — one path through every layer</name>
  <reversibility rating="costly">The guard filename `pretooluse-nudge.sh` becomes the registered command string — the ownership identity in every opted-in user's settings.json — and `--pretool-nudge` becomes a published flag; renaming either later needs a migration that owns both names (the same cost the deferred SessionStart-quoting idea names).</reversibility>
  <files>internal/nudge/classify.go, internal/nudge/text.go, internal/cli/hook_pretooluse.go, internal/cli/hook_pretooluse_test.go, internal/cli/root.go, internal/cli/testdata/cli-reference-allowlist.txt, internal/cli/install.go, docs/CLI-REFERENCE.md, .claude/hooks/pretooluse-nudge.sh, .claude/hooks/hooks.json, claudeassets.go, internal/agents/types.go, internal/agents/claude.go, internal/agents/claude_pretooluse.go, internal/agents/claude_pretooluse_test.go</files>
  <read_first>
    - .planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md (D-01, D-01a, D-01b, D-02, D-04, D-09, D-12, D-14 and the "Current hooks reference" block — output shape, `if` field, timeout unit)
    - claudeassets.go (the three-part embed shape: directive, path const, accessor; the "must stay at the repo root" doc comment)
    - .claude/hooks/session-nudge.sh and .claude/hooks/hooks.json (the stateless precedent and the fragment the SessionStart blocks derive from)
    - internal/agents/claude.go lines 147-322 (claudeFragmentCommand, claudeHooksScriptPath, claudeHookCommand, claudeSessionStartBlocks — the loop to extract) and 436-583 (Install order; the SessionStart script + writeHookEntry pair to mirror)
    - internal/agents/shared.go lines 174-341 (writeHookEntry's exact-command ownership, writeEmbeddedFile's byte-identity + executable self-heal)
    - internal/agents/types.go lines 110-122 (InstallOptions doc-comment style)
    - internal/cli/man.go lines 46-67, internal/cli/renamed.go lines 59-87, internal/cli/root.go lines 55-130 (hidden-command precedent; commandGroups deliberately omits hidden commands; AddCommand list)
    - internal/cli/install.go lines 50-135 (flag block and InstallOptions construction)
    - internal/cli/cli_reference_test.go lines 150-270 and internal/cli/testdata/cli-reference-allowlist.txt (bare entry = hidden command's flags; every walked command has --help)
    - internal/cli/cli_test.go lines 50-70 (execCmdWithInput) and internal/agents/testhelpers_test.go (fakeHome)
    - Taskfile.yml docs:cli / docs:cli:drift (lines 481-535)
  </read_first>
  <behavior>
    - TestHookPreToolUse_GrepFiresPinnedContext: with `CLAUDE_CODE_SESSION_ID` cleared, `execCmdWithInput(<{"session_id":"s-1","hook_event_name":"PreToolUse","tool_name":"Grep","tool_input":{"pattern":"Alpha"}}>, "hook", "pretooluse")` returns err == nil and stdout byte-equal to a HAND-TYPED pinned constant (the interfaces-block JSON line + "\n" — an independent oracle, not built from nudge.Text); the same with tool_name "Glob" and session_id "s-2" also fires (a distinct session, so the test stays valid once 06-03 adds the per-key cooldown)
    - TestHookPreToolUse_NoSessionIsSilent: the same Grep event with no `session_id` and `CLAUDE_CODE_SESSION_ID` cleared → err nil, stdout ""; with `t.Setenv("CLAUDE_CODE_SESSION_ID", "env-1")` and no stdin session → fires (D-07 env source)
    - TestHookCmd_HiddenTwoLevel: in newRootCmd(), `hook` and `hook pretooluse` are found, both `.Hidden == true`, neither name is a key of `commandGroups`, and `codegraph --help` output (execCmd) contains no line whose first word is `hook`
    - TestClaude_Install_PreToolNudgeOn_WritesGuardAndBlocks (fakeHome + t.Chdir(t.TempDir()), local): `Install(LocationLocal, InstallOptions{ExecPath: "/usr/local/bin/codegraph", PreToolNudge: PreToolNudgeOn})` has zero Errors; `.claude/hooks/pretooluse-nudge.sh` exists, mode has 0o111, starts with `#!/bin/sh`, contains the line `codegraph_bin='/usr/local/bin/codegraph'` and not the template token; settings.json `hooks.PreToolUse` has exactly 4 blocks with matchers Bash, Grep, Glob, Read in that order, the Bash block's 3 handlers carry `if` values `Bash(grep *)`, `Bash(rg *)`, `Bash(find *)`, every handler's command == `${CLAUDE_PROJECT_DIR}/.claude/hooks/pretooluse-nudge.sh`, timeout == 5, no `statusMessage`; `hooks.SessionStart` is still present; the global variant's command == the absolute `~/.claude/hooks/pretooluse-nudge.sh` under the fake home
    - TestClaude_Install_DefaultWritesNoPreToolUse: `Install(loc, InstallOptions{ExecPath: "/usr/local/bin/codegraph"})` for both locations writes no guard file and settings.json has no `PreToolUse` key
  </behavior>
  <action>
    RED first (TDD): write internal/cli/hook_pretooluse_test.go and internal/agents/claude_pretooluse_test.go per the behavior block plus ONLY the compile placeholders they need (the `internal/nudge` package with `Tool` consts, a `Qualifies` returning false and an empty `Text`; `PreToolNudgeMode` + consts + the `InstallOptions.PreToolNudge` field; empty `newHookCmd`/`newHookPreToolUseCmd` registered in root.go; placeholder `claudePreToolGuardPath`/`renderPreToolGuard` returning errors). Commit `test(06-01): add failing PreToolUse tracer tests`; run the five named tests, confirm each fails on an ASSERTION (not a build error), paste the `--- FAIL:` lines into the SUMMARY.

    GREEN — core (internal/nudge, D-01a, D-14): classify.go — `Qualifies` returns true for ToolGrep and ToolGlob, false otherwise (06-02 fills shell/read; doc comment says so and cites D-02/D-03). text.go — `const Text` exactly as the interfaces block states, with a doc comment: D-14, factual one-liner, names only codegraph_explore and the `codegraph explore` CLI fallback, byte-pinned (06-02 adds the drift guards). The package imports nothing from this module.

    GREEN — subcommand (internal/cli/hook_pretooluse.go, D-01a): `newHookCmd` (Use "hook", Short "Agent hook entry points (invoked by installed hooks, not by hand)", Hidden) adding `newHookPreToolUseCmd` (Use "pretooluse", Hidden, Args cobra.NoArgs). RunE: the FIRST statement defers a recover that swallows any panic; then `runHookPreToolUse(cmd.InOrStdin(), cmd.OutOrStdout(), os.Getenv)`; then `return nil` — no path returns an error. `runHookPreToolUse`: read `io.LimitReader(in, maxHookStdinBytes+1)` fully; empty, read error, or more than maxHookStdinBytes → return silently; json.Unmarshal into claudePreToolUseInput (error → silent); map tool_name Bash→(ToolShell, Command), Grep→(ToolGrep, Pattern), Glob→(ToolGlob, Pattern), Read→(ToolRead, FilePath), anything else → silent; not `nudge.Qualifies` → silent; session = getenv("CLAUDE_CODE_SESSION_ID"), falling back to input.SessionID, both empty → silent (D-07: never fire unkeyed); otherwise json.Marshal a claudeHookOutput{HookSpecificOutput: {HookEventName: "PreToolUse", AdditionalContext: nudge.Text}} and write it plus "\n" in ONE Write call. Nothing is ever written to stderr. Imports: stdlib, cobra, internal/nudge only (no graphstore/query/daemon/indexer — D-01a). Register `newHookCmd()` in root.go's AddCommand list after `newManCmd()`; do NOT add it to commandGroups; extend root.go's "man, query and unlock are deliberately absent" comment to name hook. Append two lines to cli-reference-allowlist.txt, same `<path>\t<reason>` shape: `codegraph hook` (hidden parent of the PreToolUse nudge entry point, Phase 6 D-01a — only cobra's --help) and `codegraph hook pretooluse` (hidden; reached only through the installed guard script, never typed by a human — Phase 6 D-01/D-01a).

    GREEN — guard template (.claude/hooks/pretooluse-nudge.sh, D-01/D-01b/D-04; create with `git add --chmod=+x` so the index mode is 100755): line 1 `#!/bin/sh`; a header comment naming NUDGE-03/NUDGE-04, D-01/D-01b/D-04, that this file is both the embedded install template and this repo's dogfood guard, that un-indexed repos exit before any process starts or stdin is read, that the binary path is rendered at install time, that ONLY the unrendered repository copy falls back to the codegraph on PATH, and that the script always exits 0 (comments must NOT repeat the single-quoted token — it appears exactly once, on the assignment line). Body, in order: `[ -d "${CLAUDE_PROJECT_DIR:-.}/.codegraph" ] || exit 0`; `codegraph_bin='@codegraph-exec-path@'`; `case $codegraph_bin in @*@) codegraph_bin=$(command -v codegraph 2>/dev/null) || exit 0 ;; esac`; `if [ ! -f "$codegraph_bin" ] || [ ! -x "$codegraph_bin" ]; then exit 0; fi` (an if-statement, not an and-or chain, so shellcheck reports nothing); `"$codegraph_bin" hook pretooluse`; final line `exit 0` followed by exactly one newline. No errexit option, no pipefail, the binary is run as a child (the shell process is never replaced), stdin is inherited, stdout/stderr are not redirected. POSIX sh only (no bashisms) — it must run under macOS /bin/sh and Linux dash.

    GREEN — embed (claudeassets.go): a fourth `//go:embed .claude/hooks/pretooluse-nudge.sh` directive, `PreToolUseGuardPath` const, `PreToolUseGuardTemplate() ([]byte, error)` accessor, each with a doc comment; update FS's "exactly three files" comment to four and say this one is a template rendered at install time (D-01b).

    GREEN — fragment (.claude/hooks/hooks.json): add a `PreToolUse` array beside `SessionStart`: block `{"matcher":"Bash","hooks":[h(if "Bash(grep *)"), h(if "Bash(rg *)"), h(if "Bash(find *)")]}` then single-handler blocks for matchers `Grep`, `Glob`, `Read`; every handler is `{"type":"command","command":"${CLAUDE_PROJECT_DIR}/.claude/hooks/pretooluse-nudge.sh","timeout":5}` plus its `if` for the Bash three; no statusMessage (D-02, D-12). Leave SessionStart byte-identical. Do NOT touch .claude/settings.json in this plan (06-05 dogfoods it with its own test).

    GREEN — install writer (internal/agents): types.go — `PreToolNudgeMode` with `PreToolNudgeKeep` (zero), `PreToolNudgeOn`, `PreToolNudgeOff`, and `InstallOptions.PreToolNudge` with a doc comment in AutoAllow's style (Claude-only; no-op for every other target; Keep/Off semantics land with the sticky manifest record in 06-04, D-10). claude.go — extract `claudeFragmentEventBlocks(event, fragmentCommand, ownCommand)` from claudeSessionStartBlocks (decode `hooks.<event>` from claudeassets.HooksFragment(); error if the event key is absent; deep-copy each block and rewrite every handler whose command equals fragmentCommand into ownCommand) and make claudeSessionStartBlocks a caller of it with unchanged results (every existing SessionStart test stays green unmodified). New internal/agents/claude_pretooluse.go — the interfaces-block helpers: `claudePreToolGuardPath` mirrors claudeHooksScriptPath with file name `pretooluse-nudge.sh`; `claudePreToolHookCommand` mirrors claudeHookCommand (local → the fragment literal, global → the absolute guard path, never the binary path — D-01b/D-12); `claudePreToolUseBlocks(loc)` = claudeFragmentEventBlocks("PreToolUse", claudePreToolFragmentCommand, claudePreToolHookCommand(loc)) returning `[]string{ownCommand}`; `shellSingleQuote(s)` = `'` + s with every `'` replaced by `'\''` + `'`; `renderPreToolGuard(execPath)` errors on an empty or non-absolute path, reads claudeassets.PreToolUseGuardTemplate(), errors unless `strings.Count(tmpl, preToolGuardExecPathToken) == 1`, and returns strings.Replace(tmpl, token, shellSingleQuote(execPath), 1). In claudeTarget.Install, immediately after the SessionStart writeHookEntry step, when `opts.PreToolNudge == PreToolNudgeOn`: resolve claudePreToolGuardPath (error → result.Errors), render (error → result.Errors, skip both writes), `writeEmbeddedFile(guardPath, rendered, true)` via recordFile, then resolve settings path + claudePreToolUseBlocks and `writeHookEntry(settingsPath, "PreToolUse", blocks, ownCommands)` via recordFile. Every other mode touches nothing in this plan. No manifest change in this plan (06-04 records the keys).

    GREEN — flag (internal/cli/install.go, D-09): `var pretoolNudge bool`, registered right after `--auto-allow` as `cmd.Flags().BoolVar(&pretoolNudge, "pretool-nudge", false, "Claude Code only: also register a PreToolUse hook that points Claude at codegraph_explore when it searches")`; when true, set `opts.PreToolNudge = agents.PreToolNudgeOn` (otherwise leave Keep). Run `GOTOOLCHAIN=go1.26.6 task docs:cli`, review that docs/CLI-REFERENCE.md changes only in the install section (the new flag line), and commit it in the SAME commit as the flag so TestEveryRegisteredFlagIsAccountedFor never goes red: `feat(06-01): opt-in PreToolUse nudge — hidden hook subcommand, embedded guard, install --pretool-nudge`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(06-01\): ' && printf '%s\n' "$S" | rg -q '^feat\(06-01\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/nudge internal/agents internal/cli claudeassets.go)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/nudge/ ./internal/agents/ ./internal/cli/ && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ ./internal/agents/ -count=1 -run 'TestHookPreToolUse_GrepFiresPinnedContext$|TestHookPreToolUse_NoSessionIsSilent$|TestHookCmd_HiddenTwoLevel$|TestClaude_Install_PreToolNudgeOn_WritesGuardAndBlocks$|TestClaude_Install_DefaultWritesNoPreToolUse$|TestEveryRegisteredFlagIsAccountedFor$' -v 2>&1 | tee /tmp/06-01-t1.txt | tail -4; test "$(rg -c '^ok\s' /tmp/06-01-t1.txt)" = "2" && ! rg -q '^(--- FAIL|FAIL)' /tmp/06-01-t1.txt && test "$(rg -c '^--- PASS: (TestHookPreToolUse_GrepFiresPinnedContext|TestHookPreToolUse_NoSessionIsSilent|TestHookCmd_HiddenTwoLevel|TestClaude_Install_PreToolNudgeOn_WritesGuardAndBlocks|TestClaude_Install_DefaultWritesNoPreToolUse|TestEveryRegisteredFlagIsAccountedFor) ' /tmp/06-01-t1.txt)" = "6" && GOTOOLCHAIN=go1.26.6 go build -o /tmp/06-01-bin ./cmd/codegraph && H=$(mktemp -d) && P=$(mktemp -d) && T=$(mktemp -d) && mkdir "$P/.codegraph" && (cd "$P" && HOME="$H" XDG_CONFIG_HOME="$H/.config" HERMES_HOME="$H/.hermes" /tmp/06-01-bin install --target claude --location local --pretool-nudge > /tmp/06-01-install.txt) && test -x "$P/.claude/hooks/pretooluse-nudge.sh" && rg -q "^codegraph_bin='.*06-01-bin'" "$P/.claude/hooks/pretooluse-nudge.sh" && rg -q '"PreToolUse"' "$P/.claude/settings.json" && printf '%s' '{"session_id":"tracer-1","hook_event_name":"PreToolUse","tool_name":"Grep","tool_input":{"pattern":"Alpha"}}' | env -u CLAUDE_CODE_SESSION_ID CLAUDE_PROJECT_DIR="$P" TMPDIR="$T" sh -c '${CLAUDE_PROJECT_DIR}/.claude/hooks/pretooluse-nudge.sh' > /tmp/06-01-fire.txt; echo "exit=$?" >> /tmp/06-01-fire.txt; test "$(rg -c '^\{"hookSpecificOutput":\{"hookEventName":"PreToolUse","additionalContext":"This repo has a codegraph index: codegraph_explore ' /tmp/06-01-fire.txt)" = "1" && rg -q '^exit=0$' /tmp/06-01-fire.txt && test "$(wc -l < /tmp/06-01-fire.txt | tr -d ' ')" = "2" && ! rg -q 'permissionDecision|"decision"|"continue"' /tmp/06-01-fire.txt && rm -rf "$P/.codegraph" && printf '%s' '{"session_id":"tracer-1","hook_event_name":"PreToolUse","tool_name":"Grep","tool_input":{"pattern":"Alpha"}}' | env -u CLAUDE_CODE_SESSION_ID CLAUDE_PROJECT_DIR="$P" TMPDIR="$T" sh -c '${CLAUDE_PROJECT_DIR}/.claude/hooks/pretooluse-nudge.sh' > /tmp/06-01-silent.txt; echo "exit=$?" >> /tmp/06-01-silent.txt; test "$(cat /tmp/06-01-silent.txt)" = "exit=0" && rm -rf "$H" "$P" "$T" && (GOTOOLCHAIN=go1.26.6 task docs:cli:drift > /tmp/06-01-drift.txt 2>&1; echo "exit=$?" >> /tmp/06-01-drift.txt); rg -q '^exit=0$' /tmp/06-01-drift.txt && rg -q -- '--pretool-nudge' docs/CLI-REFERENCE.md && echo "tracer green: real binary installs, fires once, silent un-indexed"</automated>
    <fails_when>non-zero exit; no "test(06-01):" or "feat(06-01):" commit; build/gofmt/vet red; fewer than 2 "ok" package lines, any "--- FAIL"/"FAIL" line, or fewer than the 6 named top-level PASS lines in /tmp/06-01-t1.txt; the real binary not writing an executable guard ending in the quoted binary path or no PreToolUse key; the fire run not printing exactly one pinned-prefix line plus "exit=0" (2 lines total) or printing a decision key; the un-indexed run printing anything besides "exit=0"; docs:cli:drift not exit 0; or the flag missing from docs/CLI-REFERENCE.md</fails_when>
  </verify>
  <acceptance_criteria>
    - `go list -deps ./internal/nudge` names no `github.com/seanb4t/codegraph-go/internal/` package other than internal/nudge itself: `test "$(GOTOOLCHAIN=go1.26.6 go list -deps ./internal/nudge | rg -c 'codegraph-go/internal/')" = "1"`
    - internal/cli/hook_pretooluse.go imports exactly one module package: `test "$(rg -c '"github.com/seanb4t/codegraph-go/internal/' internal/cli/hook_pretooluse.go)" = "1" && rg -q '"github.com/seanb4t/codegraph-go/internal/nudge"' internal/cli/hook_pretooluse.go`
    - the guard is tracked executable and structurally safe: `M=$(git ls-files -s .claude/hooks/pretooluse-nudge.sh) && printf '%s\n' "$M" | rg -q '^100755'` and `test "$(rg -o "'@codegraph-exec-path@'" .claude/hooks/pretooluse-nudge.sh | wc -l | tr -d ' ')" = "1"` and `test "$(tail -n 1 .claude/hooks/pretooluse-nudge.sh)" = "exit 0"` and `! rg -q '^\s*exec\s' .claude/hooks/pretooluse-nudge.sh` and `! rg -q '^\s*set\s+-[a-z]*e' .claude/hooks/pretooluse-nudge.sh` and `shellcheck -s sh .claude/hooks/pretooluse-nudge.sh` exits 0
    - `rg -q 'go:embed \.claude/hooks/pretooluse-nudge\.sh' claudeassets.go && rg -q 'func PreToolUseGuardTemplate\(\)' claudeassets.go`
    - the allowlist has exactly the two new bare entries: `rg -q '^codegraph hook\t' internal/cli/testdata/cli-reference-allowlist.txt && rg -q '^codegraph hook pretooluse\t' internal/cli/testdata/cli-reference-allowlist.txt`
    - `hook` is not grouped: `! rg -q '"hook":' internal/cli/root.go`
    - claudeSessionStartBlocks delegates: `rg -q 'claudeFragmentEventBlocks\("SessionStart"' internal/agents/claude.go` and `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1` is green with no pre-existing test edited
    - the SUMMARY carries the pasted `--- FAIL:` lines for all five RED tests
  </acceptance_criteria>
  <done>A real binary installs an opt-in guard + PreToolUse registration, and the registered command prints the pinned additionalContext once on a Grep in an indexed repo and nothing elsewhere, always exiting 0 — committed RED then GREEN with the reference regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Guard-level D-16 exec suite over the real embedded template, render safety, the dogfood PATH fallback, and Family (a) RED</name>
  <files>internal/agents/claude_pretooluse_test.go, .planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md</files>
  <read_first>
    - internal/agents/claude_pretooluse.go and .claude/hooks/pretooluse-nudge.sh (Task 1 output)
    - internal/agents/hookpackage_test.go lines 43-110 and 220-300 (runSessionNudge: env filtering, cmd.Dir vs env, *exec.ExitError handling, never t.Fatalf from a helper that goroutines call)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md (D-01 guard jobs, D-04, D-16 guard level)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-MUTATION-LOG.md (title/Scope/pre-mutation cleanliness-gate convention and the per-family entry shape to reproduce)
  </read_first>
  <action>
    Add to internal/agents/claude_pretooluse_test.go (tests assert only OUR guard's bytes and behaviour — D-00):
    - A helper `runPreToolGuard(t, guardPath, dir string, useEnv bool, extraEnv []string, stdin string) (stdout, stderr string, exit int, err error)` in runSessionNudge's shape: strips any inherited CLAUDE_PROJECT_DIR and CLAUDE_CODE_SESSION_ID, sets CLAUDE_PROJECT_DIR=dir when useEnv else cmd.Dir=dir, feeds stdin via strings.NewReader, returns the ExitError code, never calls t.Fatalf itself.
    - Stub binaries written into t.TempDir() as `#!/bin/sh` scripts reading env STUB_DIR: `stub-ok` copies stdin to `$STUB_DIR/stdin`, touches `$STUB_DIR/started`, prints `STUB-OUT` and exits 0; `stub-fail` touches started and exits 3; `stub-crash` touches started then sends itself SIGSEGV (`kill -SEGV $$`). Each guard under test is `renderPreToolGuard(<stub path>)` written 0755 into t.TempDir() — the REAL embedded template, rendered.
    - `TestPreToolUseGuard` with exactly these subtests: `indexed_binary_ok` (.codegraph dir, env set, stub-ok: exit 0, started present, `$STUB_DIR/stdin` byte-equal to the input JSON, stdout exactly `STUB-OUT\n`, stderr empty); `not_indexed` (no .codegraph: exit 0, stdout/stderr empty, started ABSENT — no process started, D-04); `codegraph_is_file` (.codegraph is a regular file: same as not_indexed); `binary_missing` (rendered path does not exist: exit 0, stdout/stderr empty); `binary_not_executable` (stub-ok chmod 0644: exit 0, stdout empty, started absent); `binary_exits_nonzero` (stub-fail: exit 0, started present); `binary_crashes` (stub-crash: exit 0, started present); `project_dir_unset_indexed` (useEnv false, cmd.Dir indexed: exit 0, started present); `project_dir_unset_not_indexed` (useEnv false, cmd.Dir un-indexed: exit 0, started absent). Count executed subtests and t.Fatalf if fewer than 9.
    - `TestRenderPreToolGuard` with subtests `plain_path` (render "/usr/local/bin/codegraph": contains `codegraph_bin='/usr/local/bin/codegraph'`, token absent, `sh -n` on the rendered file exits 0), `quote_and_space_in_path` (create stub-ok at `<tmp>/it's a dir/codegraph`, render, run as indexed_binary_ok: exit 0, started present — proves the quoting; `sh -n` exits 0), `relative_path_rejected` (render "codegraph" → error), `empty_path_rejected` (render "" → error), `template_token_exactly_once` (strings.Count over claudeassets.PreToolUseGuardTemplate() == 1).
    - `TestPreToolUseGuardSourceFallsBackToPATH` running the checked-in `../../.claude/hooks/pretooluse-nudge.sh` UNRENDERED: `stub_on_path` (PATH = <dir containing stub-ok named `codegraph`>:/usr/bin:/bin, indexed CLAUDE_PROJECT_DIR: exit 0, started present) and `no_codegraph_on_path` (PATH=/usr/bin:/bin: exit 0, stdout empty — first assert `exec.LookPath` finds no codegraph under that PATH, else t.Fatalf so the case can never pass vacuously).
    Run the suite; it characterizes Task 1's guard, so it passes without a RED-first commit (the 05-01 Task 2 precedent) — its positive control is Family (a). Commit `test(06-01): guard-level D-16 exec suite and render safety for the PreToolUse guard`.

    Family (a) (D-00, D-16, rule 84d1gfpywd): create .planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md in the 05-MUTATION-LOG.md shape (title, Phase, Date, Scope listing every family the phase will add, the pre-mutation cleanliness-gate convention section, one section per family). (a1) gate `git diff --quiet -- .claude/hooks/pretooluse-nudge.sh`; plant `perl -0pi -e 's/\nexit 0\n\z/\nexit 2\n/' .claude/hooks/pretooluse-nudge.sh` (the embed picks it up at test build); run `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestPreToolUseGuard$' -v`, capture exit code and the verbatim `--- FAIL: TestPreToolUseGuard/binary_exits_nonzero` line; revert with `git checkout -- .claude/hooks/pretooluse-nudge.sh`, prove `git diff --quiet`, re-run green as the control. (a2) same protocol on internal/agents/claude_pretooluse.go: replace shellSingleQuote's body so it returns `"'" + s + "'"` (a perl substitution on the function body; record the exact diff) against `TestRenderPreToolGuard$`, expecting `--- FAIL: TestRenderPreToolGuard/quote_and_space_in_path`. Commit `docs(06-01): record Family (a) guard and render RED demonstrations`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestPreToolUseGuard$|TestRenderPreToolGuard$|TestPreToolUseGuardSourceFallsBackToPATH$' -v 2>&1 | tee /tmp/06-01-t2.txt | tail -3; rg -q '^ok\s' /tmp/06-01-t2.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/06-01-t2.txt && test "$(rg -c -- '--- PASS: TestPreToolUseGuard/(indexed_binary_ok|not_indexed|codegraph_is_file|binary_missing|binary_not_executable|binary_exits_nonzero|binary_crashes|project_dir_unset_indexed|project_dir_unset_not_indexed) ' /tmp/06-01-t2.txt)" = "9" && test "$(rg -c -- '--- PASS: TestRenderPreToolGuard/(plain_path|quote_and_space_in_path|relative_path_rejected|empty_path_rejected|template_token_exactly_once) ' /tmp/06-01-t2.txt)" = "5" && test "$(rg -c -- '--- PASS: TestPreToolUseGuardSourceFallsBackToPATH/(stub_on_path|no_codegraph_on_path) ' /tmp/06-01-t2.txt)" = "2" && rm -f /tmp/06-01-red-a1.txt && git diff --quiet -- .claude/hooks/pretooluse-nudge.sh && perl -0pi -e 's/\nexit 0\n\z/\nexit 2\n/' .claude/hooks/pretooluse-nudge.sh && ! git diff --quiet -- .claude/hooks/pretooluse-nudge.sh && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestPreToolUseGuard$' -v > /tmp/06-01-red-a1.txt 2>&1; echo "exit=$?" >> /tmp/06-01-red-a1.txt); git checkout -- .claude/hooks/pretooluse-nudge.sh; git diff --quiet -- .claude/hooks/pretooluse-nudge.sh && rg -q -- '--- FAIL: TestPreToolUseGuard/binary_exits_nonzero' /tmp/06-01-red-a1.txt && ! rg -q '^exit=0$' /tmp/06-01-red-a1.txt && M=.planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md && rg -q '^## Family \(a1\)' "$M" && rg -q '^## Family \(a2\)' "$M" && rg -q -- '--- FAIL: TestPreToolUseGuard/binary_exits_nonzero' "$M" && rg -q -- '--- FAIL: TestRenderPreToolGuard/quote_and_space_in_path' "$M" && GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 2>&1 | tail -1 | rg -q '^ok\s' && echo "guard suite green; Family (a1) re-demonstrated and reverted byte-clean"</automated>
    <fails_when>non-zero exit; any "--- FAIL"/"FAIL" in /tmp/06-01-t2.txt; fewer than 9 TestPreToolUseGuard, 5 TestRenderPreToolGuard or 2 fallback subtest PASS lines; the guard not clean before the plant or after the revert; the (a1) plant not producing the binary_exits_nonzero FAIL line or exiting 0; 06-MUTATION-LOG.md missing the (a1)/(a2) sections or either verbatim FAIL line; or the agents package not "ok" after the revert</fails_when>
  </verify>
  <acceptance_criteria>
    - every guard subtest asserts exit code 0 explicitly and the not-indexed/not-executable subtests assert the start marker is ABSENT (positive assertion that no process started — rule 84d1gfpywd)
    - `rg -q 'LookPath' internal/agents/claude_pretooluse_test.go` (the no-codegraph-on-PATH case proves its own precondition)
    - 06-MUTATION-LOG.md's (a1) and (a2) sections each record the pre-mutation gate result, the applied diff, the verbatim FAIL line with the exit code, the revert command, the clean `git diff --quiet`, and a green control
    - no `[ci skip]`/`[skip ci]` in any 06-01 commit: `L=$(git log --format=%B -E --grep='^(test|feat|docs)\(06-01\)') && test -n "$L" && ! printf '%s\n' "$L" | rg -q -i '\[(ci skip|skip ci)\]'`
    - the range-less `git diff --quiet -- <file>` gates in this task's verify are intentionally about the uncommitted working tree around a planted mutation (the 05-MUTATION-LOG cleanliness convention), not about committed history
  </acceptance_criteria>
  <done>The guard's exit-0 / zero-overhead / stdin-passthrough contract is pinned against the real embedded template, the ExecPath quoting is proven, and both guards are shown able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code → guard/subcommand stdin | the PreToolUse event JSON (tool inputs, session/agent ids) is untrusted input to our process |
| install-time ExecPath → guard script bytes | a path string is rendered into an executable shell script |
| guard → Claude Code (exit code, stdout) | a non-zero exit or a decision key would block, deny or prompt the user's tool call |
| user settings.json → codegraph writer | the file holds the user's own PreToolUse entries next to ours |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering (shell injection) | renderPreToolGuard / guard template | high | mitigate | ExecPath rendered only through shellSingleQuote into a single-quoted assignment; empty/relative paths rejected; token count asserted == 1; TestRenderPreToolGuard/quote_and_space_in_path executes a quote+space path; Family (a2) proves the test catches naive quoting |
| T-06-02 | Denial of Service (blocking the user's tool call) | guard exit path | high | mitigate | no errexit, child process (never replaced), unconditional final `exit 0`; TestPreToolUseGuard covers missing/non-exec/failing/crashing binaries; Family (a1) |
| T-06-03 | Elevation / DoS (decision keys) | runHookPreToolUse output | high | mitigate | the only output struct is claudeHookOutput with hookEventName + additionalContext; tracer verify greps the real output for permissionDecision/decision/continue; 06-03 adds the subcommand-level contract suite and Families (c4)/(c5) |
| T-06-04 | Denial of Service (overhead in un-indexed repos) | guard D-04 check | medium | mitigate | directory check is the first statement, before any process or stdin read; TestPreToolUseGuard/not_indexed and /codegraph_is_file assert the stub never started |
| T-06-05 | Tampering (ownership) | writeHookEntry("PreToolUse", …) | high | mitigate | reused unmodified — exact command-string ownership (242ec0a); 06-04 extends TestOwnershipExactIdentity with a planted same-matcher PreToolUse block |
| T-06-06 | Denial of Service (oversized/malformed stdin) | runHookPreToolUse | low | mitigate | io.LimitReader(maxHookStdinBytes+1), over-limit or undecodable → silent; 06-03 forces both |
| T-06-07 | Information Disclosure | local-scope rendered guard | low | accept | carries the installing machine's absolute ExecPath, the same exposure the local `.mcp.json` entry already has (stdioMcpEntry) |
| T-06-08 | Elevation of Privilege | unrendered dogfood guard's PATH fallback | low | accept | reachable only in this repository's checked-in copy; every installed guard is rendered with an absolute ExecPath and never consults PATH (D-01b) |
| T-06-SC | Tampering | package installs | low | accept | none — this phase adds no dependency (RESEARCH Package Legitimacy Audit: no rows) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go build ./... && GOTOOLCHAIN=go1.26.6 go test ./internal/nudge/ ./internal/agents/ ./internal/cli/... -count=1` green
- `GOTOOLCHAIN=go1.26.6 task docs:cli:drift` exit 0
- real binary: `install --pretool-nudge` then a piped Grep event → one pinned line, exit 0; un-indexed → nothing, exit 0
- 06-MUTATION-LOG.md Family (a1)/(a2) recorded with byte-clean reverts
</verification>

<success_criteria>
- NUDGE-03: the registered hook returns only hookSpecificOutput.additionalContext and exits 0 (tracer + guard suite); NUDGE-04 un-indexed half: zero process starts without `.codegraph/`; NUDGE-06 registration half: opt-in install through the unchanged writeHookEntry
- D-01, D-01a, D-01b, D-02 (registration `if` rules), D-04, D-09 (flag), D-12, D-14 (pinned text) implemented as locked; D-00 respected
</success_criteria>

<output>
Create `.planning/phases/06-claude-code-pretooluse-nudge/06-01-SUMMARY.md` when done — paste the Task 1 `--- FAIL:` RED lines, the real-binary fire/silent transcripts, the 9/5/2 subtest counts, and the Family (a) outcome.
</output>
