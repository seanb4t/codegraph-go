---
phase: 06-claude-code-pretooluse-nudge
plan: 07
type: execute
wave: 7
depends_on: ["06-06"]
files_modified:
  - internal/cli/install.go
  - internal/cli/uninstall.go
  - docs/CLI-REFERENCE.md
  - .planning/phases/06-claude-code-pretooluse-nudge/06-07-SUMMARY.md
autonomous: true
requirements: [NUDGE-03, NUDGE-04, NUDGE-05, NUDGE-06]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`install`'s Long help says that `--pretool-nudge` (Claude Code only) also registers a PreToolUse hook adding a one-line pointer to codegraph_explore when Claude searches an indexed repository, that it never blocks a tool call, and that the choice is remembered until `--pretool-nudge=false` or uninstall; `uninstall`'s Long help says it also removes that hook and its guard script; every help line stays ≤ 80 columns."
    - "docs/CLI-REFERENCE.md is regenerated only through `task docs:cli` (after the live session, the 05-07 precedent); `task docs:cli:drift` and TestEveryRegisteredFlagIsAccountedFor are green."
    - "Phase gate: `go build ./...` and the full suite are green — every package except internal/daemon in one run (≥ 53 `ok` packages: 52 measured at planning plus internal/nudge), then internal/daemon alone (WINDOWS #37) — `task docs:cli:drift` exits 0, 06-MUTATION-LOG.md holds all 18 family sections (a1-a2, b1-b3, c1-c6, d1-d4, e1-e3), and 06-LIVE-SESSIONS.md has no PENDING and reads `D-18 verdict: PASS`."
    - "Out of scope and untouched across the phase (Phase 7 AGENT-14 and D-13): production files under internal/mcp (only the drift TEST gained cases), internal/agents/instructions.go, the SessionStart script `.claude/hooks/session-nudge.sh`, and both print-config-style goldens — `git diff` of each from the phase's first commit is empty."
  artifacts:
    - path: "internal/cli/install.go"
      provides: "Long help naming the PreToolUse opt-in"
      contains: "--pretool-nudge"
    - path: "internal/cli/uninstall.go"
      provides: "Long help naming the PreToolUse hook removal"
    - path: "docs/CLI-REFERENCE.md"
      provides: "regenerated reference"
  key_links:
    - from: ".planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md"
      to: "this plan's phase gate"
      via: "precondition: D-18 verdict PASS"
      pattern: "^D-18 verdict: PASS$"
  prohibitions:
    - statement: "MUST NOT touch AGENT-14's surfaces (internal/mcp production text, instructions.go, the published capability table) or the print-config-style goldens (D-13) — Phase 7 publishes the per-harness table after the last harness write."
      status: resolved
      verification: test
    - statement: "MUST NOT hand-edit docs/CLI-REFERENCE.md; regeneration only via `task docs:cli`, proven by `task docs:cli:drift`."
      status: resolved
      verification: test
---

<objective>
Close the phase: describe the shipped opt-in in `install`/`uninstall` help, regenerate docs/CLI-REFERENCE.md
through the drift gate after the live session, run the full phase gate, and record the advisories the phase
surfaced for the maintainer.

Purpose: NUDGE-06's user-facing documentation and the phase-level proof that NUDGE-03…06 hold together.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-CONTEXT.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-06-SUMMARY.md
@internal/cli/install.go
@internal/cli/uninstall.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` → "Artifacts this phase produces". This plan: the install/uninstall Long help text and
the regenerated docs/CLI-REFERENCE.md.

<tasks>

<task type="auto">
  <name>Task 1: Help text for the opt-in and its removal; regenerate the reference through the drift gate</name>
  <precondition>06-LIVE-SESSIONS.md reads `D-18 verdict: PASS` (06-06 complete).</precondition>
  <files>internal/cli/install.go, internal/cli/uninstall.go, docs/CLI-REFERENCE.md</files>
  <read_first>
    - internal/cli/install.go and internal/cli/uninstall.go (Long/Example text — the current sentences stay)
    - Taskfile.yml docs:cli / docs:cli:drift (lines 481-535)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md (the verdict — describe only what shipped and passed)
  </read_first>
  <action>
    - install Long: after the `--print-config-style` sentence, add: With --pretool-nudge (Claude Code only), install also registers a PreToolUse hook that adds a one-line pointer to codegraph_explore when Claude searches an indexed repository; it never blocks a tool call, and the choice is remembered until --pretool-nudge=false or uninstall. Add the Example line `codegraph install --target claude --pretool-nudge`.
    - uninstall Long: after the skill-package sentence, add: It also removes the Claude Code PreToolUse nudge hook and its guard script when present.
    - Keep every existing sentence; wrap new text at ≤ 80 columns like the current strings.
    - `GOTOOLCHAIN=go1.26.6 task docs:cli`; review that docs/CLI-REFERENCE.md changes only in the install and uninstall sections; commit `docs(06-07): describe the PreToolUse nudge opt-in in install/uninstall help; regenerate CLI reference`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -q '^D-18 verdict: PASS$' .planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli)" && (GOTOOLCHAIN=go1.26.6 task docs:cli:drift > /tmp/06-07-drift.txt 2>&1; echo "exit=$?" >> /tmp/06-07-drift.txt); rg -q '^exit=0$' /tmp/06-07-drift.txt && rg -q 'byte-identical to a fresh regeneration' /tmp/06-07-drift.txt && rg -q 'never blocks a tool call' docs/CLI-REFERENCE.md && rg -q 'PreToolUse nudge hook and its guard script' docs/CLI-REFERENCE.md && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryRegisteredFlagIsAccountedFor$|TestPlainGolden$' -v 2>&1 | tee /tmp/06-07-t1.txt | tail -2; rg -q '^ok\s' /tmp/06-07-t1.txt && test "$(rg -c '^--- PASS: (TestEveryRegisteredFlagIsAccountedFor|TestPlainGolden) ' /tmp/06-07-t1.txt)" = "2" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(06-07\): describe the PreToolUse'</automated>
    <fails_when>non-zero exit; the D-18 verdict not PASS; build/gofmt red; docs:cli:drift not exit 0 or its byte-identical line absent; either new help sentence missing from the reference; flag accounting or TestPlainGolden not PASS; or no docs(06-07) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - no Long-help line exceeds 80 columns: `test -z "$(awk '/Long:/,/Example:/' internal/cli/install.go internal/cli/uninstall.go | rg -o '"[^"]*\\n"' | awk 'length($0) > 84')"`
    - the reference diff in this commit is only the two help additions and the Example line (a size bound against unrelated drift): `C=$(git log -1 --format=%H -E --grep='^docs\(06-07\): ') && test -n "$C" && D=$(git show --format= "$C" -- docs/CLI-REFERENCE.md) && test -n "$D" && test "$(printf '%s\n' "$D" | rg -c '^[+-][^+-]')" -le 16`
  </acceptance_criteria>
  <done>The help text and the generated reference describe the opt-in exactly as it shipped and passed live.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate and the maintainer advisories</name>
  <files>.planning/phases/06-claude-code-pretooluse-nudge/06-07-SUMMARY.md</files>
  <read_first>
    - .planning/STATE.md (Blockers: WINDOWS #37 — run internal/daemon alone)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md and 06-LIVE-SESSIONS.md (completeness)
    - .planning/REQUIREMENTS.md (CODEX-05's wording) and .planning/ROADMAP.md § Phase 7 criterion 3
    - every 06-0N-SUMMARY.md (advisories and deviations to roll up)
  </read_first>
  <action>
    Run the gate, transcripts to /tmp/06-07-*.txt, exit codes summarized in the SUMMARY (no source change is expected; a gate failure is diagnosed and reported, and fixed only if it is a regression this phase introduced — otherwise it is recorded as pre-existing, per the WINDOWS #37 precedent):
    - `GOTOOLCHAIN=go1.26.6 go build ./...`
    - `GOTOOLCHAIN=go1.26.6 go test -count=1 $(GOTOOLCHAIN=go1.26.6 go list ./... | rg -v 'internal/daemon$')`
    - `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/daemon/` (alone — WINDOWS #37)
    - `GOTOOLCHAIN=go1.26.6 task docs:cli:drift`
    - 06-MUTATION-LOG.md: 18 family sections; 06-LIVE-SESSIONS.md: no PENDING, `D-18 verdict: PASS`
    - untouched-surface checks from the phase's first commit (see verify).
    Record in the SUMMARY, as advisories for the maintainer (not changed here): (1) REQUIREMENTS.md CODEX-05 and ROADMAP Phase 7 criterion 3 still say "once-per-session contract", while D-05 (2026-09-19) made the contract "first call, then at most once a minute per (session, agent)" — reword before Phase 7 plans against it; (2) D-12's mirrored shell form leaves the global command an unquoted absolute path and the local one an unquoted `${CLAUDE_PROJECT_DIR}` expansion, so paths with spaces produce a hook error for BOTH events — the deferred exec-form/quoting idea should cover PreToolUse too; (3) a committed local-scope guard carries the installing machine's ExecPath (same exposure as the local .mcp.json); (4) never run `install --location local --pretool-nudge` inside this repository — it rewrites the embedded template (TestRenderPreToolGuard/template_token_exactly_once catches a committed rendering); (5) an opt-in written into a foreign (kept) Claude skill directory is not recorded, so Keep neither refreshes nor removes it (uninstall still does); (6) D-01a's "one allowlist line" became two because D-01's two-level command gives both hidden commands a --help flag; (7) the D-18 findings on the five open points, copied from 06-LIVE-SESSIONS.md, with what each means for CODEX-05; (8) the pre-existing `install --yes` discards `--target` todo is still open and affects which targets the flag's note considers.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build ./... && (GOTOOLCHAIN=go1.26.6 go test -count=1 $(GOTOOLCHAIN=go1.26.6 go list ./... | rg -v 'internal/daemon$') > /tmp/06-07-suite.txt 2>&1; echo "exit=$?" >> /tmp/06-07-suite.txt); rg -q '^exit=0$' /tmp/06-07-suite.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/06-07-suite.txt && test "$(rg -c '^ok\s' /tmp/06-07-suite.txt)" -ge 53 && rg -q '^ok\s+github\.com/seanb4t/codegraph-go/internal/nudge' /tmp/06-07-suite.txt && (GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/daemon/ > /tmp/06-07-daemon.txt 2>&1; echo "exit=$?" >> /tmp/06-07-daemon.txt); rg -q '^exit=0$' /tmp/06-07-daemon.txt && (GOTOOLCHAIN=go1.26.6 task docs:cli:drift > /tmp/06-07-drift2.txt 2>&1; echo "exit=$?" >> /tmp/06-07-drift2.txt); rg -q '^exit=0$' /tmp/06-07-drift2.txt && M=.planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md && test "$(rg -c '^## Family \([a-e][1-6]\)' "$M")" = "18" && L=.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md && ! rg -q 'PENDING' "$L" && rg -q '^D-18 verdict: PASS$' "$L" && H=$(git log --format=%H -E --grep='^test\(06-01\): ') && B=$(printf '%s\n' "$H" | tail -1) && test -n "$B" && N=$(git diff --name-only "$B^" HEAD -- internal/mcp/) && printf '%s\n' "$N" | rg -q '_test\.go$' && test -z "$(printf '%s\n' "$N" | rg -v '_test\.go$' | rg -v '^$')" && git diff --quiet "$B^" HEAD -- internal/agents/instructions.go .claude/hooks/session-nudge.sh internal/cli/testdata/plain/print-config-style.golden internal/cli/testdata/plain/print-config-style-local.golden && echo "phase gate green"</automated>
    <fails_when>non-zero exit; build red; the module suite (minus internal/daemon) exiting non-zero, any "--- FAIL"/"FAIL" line, fewer than 53 "ok" packages, or no "ok" for internal/nudge; the daemon run exiting non-zero; docs:cli:drift not exit 0; fewer than 18 mutation-log family sections; any PENDING or a non-PASS verdict in 06-LIVE-SESSIONS.md; the internal/mcp diff since the phase's first commit not listing the drift test (a vacuous diff); or a production file under internal/mcp, instructions.go, session-nudge.sh or a print-config-style golden changed since the phase's first commit</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY lists the eight advisories and the build/suite/daemon/drift exit codes
    - no `[ci skip]`/`[skip ci]` in any Phase 6 commit: `L=$(git log --format=%B -E --grep='^(test|feat|fix|refactor|docs)\(06-0[1-7]\)') && test -n "$L" && ! printf '%s\n' "$L" | rg -q -i '\[(ci skip|skip ci)\]'`
  </acceptance_criteria>
  <done>The phase closes with every automated gate green, the live verdict PASS, every guard positive-controlled, and every advisory on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated reference ↔ live Cobra tree | a hand edit would drift silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-31 | Tampering | docs/CLI-REFERENCE.md drift | low | mitigate | `task docs:cli` only; `task docs:cli:drift` + TestEveryRegisteredFlagIsAccountedFor |
| T-06-32 | Repudiation (a phase closed on a partial record) | phase gate | low | mitigate | 18 mutation families counted, the live verdict gated on PASS, untouched-surface diffs from the first phase commit |
</threat_model>

<verification>
- full suite (module minus internal/daemon, then internal/daemon alone), docs:cli:drift, mutation-log and live-evidence completeness — all green (Task 2 verify)
</verification>

<success_criteria>
- NUDGE-03, NUDGE-04, NUDGE-05, NUDGE-06: roadmap criteria 1-4 each backed by the unit suites (06-01…06-05), the mutation log and the live D-18 evidence (06-06); help and reference describe what shipped
</success_criteria>

<output>
Create `.planning/phases/06-claude-code-pretooluse-nudge/06-07-SUMMARY.md` when done — the gate exit codes, the 18-family count, the D-18 verdict, and the eight advisories.
</output>
