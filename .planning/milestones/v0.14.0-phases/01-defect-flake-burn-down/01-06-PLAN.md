---
phase: 01-defect-flake-burn-down
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - web/static/favicon.svg
  - web/static/favicon-32.png
  - web/static/apple-touch-icon.png
  - web/static/codegraph-mark.svg
  - web/src/routes/+layout.svelte
  - web/src/lib/assets/favicon.svg
  - web/build/
autonomous: true
requirements: [FIX-02]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every UI route serves a codegraph favicon that loads under the UNCHANGED `default-src 'self'` CSP: `internal/uiserver/spa.go`'s `spaCSPBaseDirectives` and `internal/uiserver/spa_test.go`'s `default-src 'self'` assertion are byte-identical before and after this plan. D-05 / WINDOWS #30 / Pitfall 18."
    - "The shipped icons are static files under `web/static/`, served at `'self'` root-relative paths — no `data:` URI anywhere in the built output's icon links, and no `img-src` directive added."
    - "`web/src/lib/assets/favicon.svg` (the stock Svelte logo) no longer exists in the tree, and nothing imports it — `task web:drift` cannot pass with it still present."
    - "The shipped `web/static/favicon.svg` carries no C2PA `<metadata>` block, no `preserveAspectRatio` attribute, no `style=\"display: block;\"` and no full-canvas white background rect; its `viewBox` is square. D-03."
    - "The three `<link>` tags appear in `web/src/routes/+layout.svelte` in a fixed order — SVG icon, then 32x32 PNG icon, then apple-touch-icon — and the built HTML under `web/build/` preserves that order, so a browser's first-match selection is deterministic rather than incidental."
    - "Both `/favicon.svg` and `/favicon-32.png` resolve to distinct, non-identical files; neither link shadows the other, and the three tags carry three distinct `href` values."
    - "Because the three links are declared once in the ROOT layout, every route under the SPA fallback serves the same icon set — there is no per-route head override, and no route can resolve the deleted Svelte logo."
    - "The bare mark variant is committed alongside the tile as the app's mark for future in-app use, cleaned to the same D-03 standard, with no UI consuming it this phase. D-02."
    - statement: "Concurrent GET requests for the same icon return identical bytes: the icons are read-only files inside the `//go:embed all:build` tree, so no request can observe a partially-written asset."
      verification: backstop
  artifacts:
    - web/static/favicon.svg
    - web/static/favicon-32.png
    - web/static/apple-touch-icon.png
    - web/static/codegraph-mark.svg
  key_links:
    - "web/static/<file> → SvelteKit adapter-static copies it to web/build/<file> → //go:embed all:build → internal/uiserver serves it at /<file> under default-src 'self' → the <link> tags in the root +layout.svelte resolve it"
  prohibitions:
    - "`internal/uiserver/spa.go` and `internal/uiserver/spa_test.go` are NOT modified by this plan. No `img-src` directive, no `data:` source, no CSP relaxation of any kind (D-05)."
    - "No image-processing devDependency (sharp, rsvg-convert, svgo, imagemin) is added to web/package.json — the raster icons are rendered once outside the build pipeline and committed (RESEARCH.md Package Legitimacy Audit; Assumptions Log A3)."
    - "The tile's corner radius and colours stay exactly as generated: the blue is `rgb(29,78,216)` (#1d4ed8) and the bare mark's edge colour is `rgb(15,23,42)` (#0f172a). No recolouring, no redrawing (D-03)."
    - "No in-app header or brand row placement for the bare mark — that is an explicit Deferred Idea in 01-CONTEXT.md."
---

<objective>
Replace the stock SvelteKit favicon with the locked codegraph mark, shipped as static files that
load under the CSP exactly as it already stands.

Purpose: WINDOWS #30's finding is that the asset is the defect, not the policy. The current icon is
imported through `$lib/assets/`, which Vite inlines as a `data:` URI — blocked by
`default-src 'self'` with no `img-src` and no `data:` source. The fix is to stop inlining, not to
widen the directive. `internal/uiserver/spa_test.go`'s exact `default-src 'self'` assertion is
deliberate (T-02-02-06) and must come out of this plan byte-identical.

Output: four cleaned static assets under `web/static/`, three `<link>` tags in the root layout, the
deleted Svelte logo, and a rebuilt `web/build/`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-defect-flake-burn-down/01-CONTEXT.md
@.planning/phases/01-defect-flake-burn-down/01-RESEARCH.md
@.planning/phases/01-defect-flake-burn-down/01-PATTERNS.md
@.planning/phases/01-defect-flake-burn-down/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean the locked mark SVGs and render the two raster icons</name>
  <files>web/static/favicon.svg, web/static/codegraph-mark.svg, web/static/favicon-32.png, web/static/apple-touch-icon.png</files>
  <read_first>
    - .planning/phases/01-defect-flake-burn-down/assets/01-mark-favicon-tile.raw.svg — the locked tile source. Its root element carries `version`, `xmlns`, `style="display: block;"`, `viewBox="0 0 2048 2048"`, `width="1024"`, `height="1024"`, `preserveAspectRatio="none"` and `xmlns:c2pa`, followed by a ~17 KB `<metadata><c2pa:manifest>` block, then the blue `rgb(29,78,216)` tile path, four white corner-carve paths, and the white ligature paths.
    - .planning/phases/01-defect-flake-burn-down/assets/01-mark-bare.raw.svg — the locked bare source. Same root-element shape and a ~17 KB metadata block, and its FIRST path is a full-canvas white rect (`M 0 0 L 2048 0 L 2048 2048 L 0 2048 L 0 0 z`) — that is the white background rect D-03 says to remove. Its mark paths use `rgb(15,23,42)` edges and blue nodes.
    - .planning/phases/01-defect-flake-burn-down/assets/01-mark-round2-sheet.png — the comparison sheet the choice was made from; Task 2's `<human-check>` legibility review compares against it.
    - web/static/robots.txt — the only existing file in this directory; confirms there is no serve/build code to add, only files.
    - web/src/app.css — the one chromatic value in the app (`oklch(0.488 0.243 264.376)`), which is the Tailwind `blue-700` the tile matches. Read it to confirm, do not change it.
  </read_first>
  <action>
Produce four files under `web/static/`.

**`web/static/favicon.svg`** from the tile source, and **`web/static/codegraph-mark.svg`** from the
bare source. For each, apply exactly the D-03 cleanup and nothing else:
  - delete the entire `<metadata>` element and its `c2pa:manifest` payload,
  - delete the `xmlns:c2pa` namespace declaration,
  - delete `preserveAspectRatio="none"`,
  - delete `style="display: block;"`,
  - set a square `viewBox` (the source is already `0 0 2048 2048`; keep it) and drop the
    `width`/`height` attributes so the icon scales to its container rather than asserting 1024 px,
  - for the bare mark only, delete the leading full-canvas white background rect path,
  - keep every remaining `<path>` byte-identical, including the `transform="translate(0,0)"`
    attributes and every `fill="rgb(...)"` value. Do not run an SVG optimiser, do not re-round
    coordinates, do not merge paths. The corner radius and colours are locked.

**`web/static/favicon-32.png`** (32x32) and **`web/static/apple-touch-icon.png`** (180x180),
rendered from the CLEANED tile SVG. Render them once with a local tool that is already on this
machine — `rsvg-convert`, ImageMagick `magick`, macOS `sips`/`qlmanage`, or a headless Chromium
screenshot via the Playwright browser this repo already installs — and commit the results. Do NOT
add an image-processing package to `web/package.json`: RESEARCH.md's Package Legitimacy Audit
records zero new packages for this phase, and D-04's discretion explicitly permits committed files.
Record in the SUMMARY which tool produced them and the exact command, so the rasters are
reproducible.

If no local rasteriser is available, STOP and say so in the SUMMARY rather than adding a
devDependency — that is a decision for the operator, not for this task (RESEARCH Assumptions Log A3).

Verify the rasters are real images of the mark, not blank canvases: check each file's PNG header
reports the expected dimensions AND that the file has non-trivial size and more than one distinct
pixel colour. A 32x32 PNG that is 200 bytes of solid blue would satisfy a dimension check alone.

Commit as `feat(01-06): add the codegraph mark as static favicon assets`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && for f in web/static/favicon.svg web/static/codegraph-mark.svg; do rg -q '<path' "$f" || exit 1; rg -q 'metadata|c2pa|preserveAspectRatio|display: block' "$f" && exit 1; done; echo "svg-clean ok"</automated>
    <fails_when>non-zero exit, or the output does not print `svg-clean ok` — either a cleaned SVG has no path element left (the cleanup deleted the artwork), or one of the four forbidden tokens survived</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -c 'fill="rgb\(29,78,216\)"' web/static/favicon.svg && rg -c 'fill="rgb\(15,23,42\)"' web/static/codegraph-mark.svg</automated>
    <fails_when>either printed count is 0, or rg exits non-zero — the locked tile blue or the locked bare-mark edge colour is missing, meaning the artwork was recoloured or replaced</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && python3 -c "
import struct,sys,zlib,collections
for p,exp in (('web/static/favicon-32.png',32),('web/static/apple-touch-icon.png',180)):
    b=open(p,'rb').read()
    assert b[:8]==b'\x89PNG\r\n\x1a\n', p+': not a PNG'
    w,h=struct.unpack('>II',b[16:24])
    assert (w,h)==(exp,exp), '%s: %dx%d want %dx%d'%(p,w,h,exp,exp)
    assert len(b)>400, p+': suspiciously small for a rendered mark'
    idat=b''.join(b[i+8:i+8+struct.unpack('>I',b[i-4:i])[0]] for i in range(8,len(b)) if b[i:i+4]==b'IDAT')
    raw=zlib.decompress(idat)
    assert len(collections.Counter(raw).keys())>4, p+': fewer than 5 distinct byte values — looks like a blank canvas'
    print(p,'ok',w,h,len(b),'bytes')
"</automated>
    <fails_when>non-zero exit, or any assertion message printed — a raster is not a PNG, is the wrong size, is under 400 bytes, or decodes to fewer than five distinct byte values (a blank or solid-colour canvas)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && ! rg -q 'sharp|rsvg|svgo|imagemin' web/package.json && rg -q '"@playwright/test"' web/package.json</automated>
    <fails_when>non-zero exit — either an image-processing devDependency was added, or `web/package.json` no longer parses/contains the pinned Playwright entry (the positive half, proving the file was actually read)</fails_when>
  </verify>
  <acceptance_criteria>
    - `web/static/favicon.svg` and `web/static/codegraph-mark.svg` exist, contain `<path>` elements, and contain none of: `metadata`, `c2pa`, `preserveAspectRatio`, `display: block`.
    - Both cleaned SVGs keep a square `viewBox` and carry no `width`/`height` attributes on the root element.
    - `web/static/favicon.svg` still contains at least one `fill="rgb(29,78,216)"`; `web/static/codegraph-mark.svg` still contains at least one `fill="rgb(15,23,42)"` and contains no full-canvas white rect path.
    - `web/static/favicon-32.png` is a 32x32 PNG and `web/static/apple-touch-icon.png` is a 180x180 PNG; both are over 400 bytes and decode to more than four distinct byte values.
    - `web/package.json` gained no image-processing dependency.
    - The SUMMARY records the exact rasterisation tool and command used.
  </acceptance_criteria>
  <done>Four cleaned, locked-colour static assets exist under `web/static/`, and the two rasters are provably real renders rather than blank canvases.</done>
  <reversibility rating="costly">D-01 rates the mark choice costly: once a release ships it, the tile is the icon users recognise in their tab bar, and replacing it later is identity churn rather than a migration.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Link the static icons from the root layout and delete the Svelte logo</name>
  <files>web/src/routes/+layout.svelte, web/src/lib/assets/favicon.svg, web/build/</files>
  <read_first>
    - web/src/routes/+layout.svelte — the whole file: the import at line 7, the `<svelte:head>` block at line ~90 containing the single `<link rel="icon" href={favicon} />`, and the surrounding script so the import removal does not disturb the `app.css` import immediately above it.
    - web/static/robots.txt — confirms that a file placed in `web/static/` is reachable at the site root with zero routing code.
    - internal/uiserver/spa.go line ~99 — `spaCSPBaseDirectives`, the exact directive string that must not change.
    - internal/uiserver/spa_test.go line ~439 — the exact `default-src 'self'` assertion that must not change, and line ~163's comment about the embed going RED by construction.
    - Taskfile.yml `web:build` (line ~795) and `web:drift` (line ~1243) — `web:build` runs `pnpm build` then writes the four-line `web/build/.build-manifest`; `web:drift` re-hashes both the source tree and the committed output tree in place. A source change without a `task web:build` leaves `web:drift` red.
  </read_first>
  <action>
In `web/src/routes/+layout.svelte`:
  - delete the module-level import of the stock logo from the `$lib/assets/` path (line 7), leaving
    the `app.css` import above it untouched;
  - replace the single `<link rel="icon" href={favicon} />` inside `<svelte:head>` with exactly three
    link tags, in this order, using plain root-relative hrefs and no Svelte expression bindings:
      1. `rel="icon"` `type="image/svg+xml"` `href="/favicon.svg"`
      2. `rel="icon"` `type="image/png"` `sizes="32x32"` `href="/favicon-32.png"`
      3. `rel="apple-touch-icon"` `href="/apple-touch-icon.png"`
    The order is load-bearing: a browser picks the first icon link whose type it supports, so
    SVG-first is what makes the shipped icon deterministic rather than user-agent-incidental. State
    that in a one-line comment above the block.

Delete `web/src/lib/assets/favicon.svg` from the tree with `git rm`, and confirm by search that no
remaining file under `web/src` references that path. If `web/src/lib/assets/` becomes empty, let it
disappear — do not leave a placeholder.

Do NOT touch `internal/uiserver/spa.go` or `internal/uiserver/spa_test.go`. The CSP is correct as it
stands; a static same-origin file needs no directive change (D-05). This is a negative-space
requirement — prove it did not move rather than making a change and checking it still works.

Then run `task web:build` to regenerate `web/build/` and its `.build-manifest`, and commit the
regenerated output alongside the source change. `web/build/` is a committed, `//go:embed`-ed tree;
a source change without the rebuild leaves `task web:drift` red and ships the old bytes.

Finally, rebuild the binary (`task build:release`) and run the console gate built in plan 01-01
against this repo's index, writing to a scratch path rather than the committed verdict (plan 01-09
owns that file). A CSP-blocked icon surfaces as a console error, so this run is FIX-02's live check.
The run will still fail overall on the FIX-04/FIX-05 entries, which are not fixed yet — what this
task asserts is the absence of any icon-related or CSP-related entry, not overall success.

Commit as `fix(01-06): serve the codegraph favicon as static files under the unchanged CSP`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -c '<link rel="icon"|rel="apple-touch-icon"' web/src/routes/+layout.svelte</automated>
    <fails_when>the printed count is not 3, or rg exits non-zero — the three link tags are not all present in the root layout</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -n 'href="/favicon.svg"|href="/favicon-32.png"|href="/apple-touch-icon.png"' web/src/routes/+layout.svelte | awk -F: '{print $1}' | sort -c -n && echo "link-order ok"</automated>
    <fails_when>non-zero exit, or `link-order ok` is not printed — one of the three hrefs is missing, or they do not appear in SVG → PNG → apple-touch-icon source order (`sort -c` fails on an unsorted line-number sequence only if the greps matched out of order)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test ! -e web/src/lib/assets/favicon.svg && ! rg -q 'lib/assets/favicon' web/src/ && rg -q 'favicon.svg' web/static/</automated>
    <fails_when>non-zero exit — the stock logo file still exists, something under `web/src` still references it, or `web/static/` has no favicon (the positive half, so the check cannot pass by deleting both)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && printf '%s  %s\n%s  %s\n' c2c9f1daa401006bfe53c6db1c562aac63473eaaa78cbd4cc5f6e52604193987 internal/uiserver/spa.go 312dbe62feaff6a5547dea2fec55713e869c58db04cfbfe9dada3cbe11a23e71 internal/uiserver/spa_test.go | shasum -a 256 -c -</automated>
    <fails_when>non-zero exit, or either line of the output does not end in `: OK` — a CSP file's bytes changed from the digest recorded at planning time, which D-05 forbids</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/uiserver/... -count=1</automated>
    <fails_when>non-zero exit, or `no test files` in the output — the uiserver package's tests, including the exact CSP assertion, are red</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task web:drift</automated>
    <fails_when>non-zero exit, or the output reports a source/output digest mismatch — `web/build/` was not regenerated after the source change, so the embedded bytes are stale</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && ! rg -q 'data:image' web/build/index.html && rg -q '/favicon.svg' web/build/index.html</automated>
    <fails_when>non-zero exit — the built HTML still inlines an icon as a `data:` URI, or it does not reference the static `/favicon.svg` path at all</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task build:release && node web/scripts/graph-console-check.mjs --corpus self --out /tmp/fix02-console.json; jq -e '[.runs[0].consoleMessages[], .runs[0].pageErrors[]] | map(tostring) | map(select(test("favicon|Content Security Policy|Refused to load"))) | length == 0' /tmp/fix02-console.json</automated>
    <fails_when>non-zero exit from jq — a captured console entry or page error mentions the favicon, the Content Security Policy, or a refused load, meaning the icon is still being blocked</fails_when>
    <human-check>
      Legibility at favicon size is a visual judgement, not an assertion, and the mark was CHOSEN by
      comparing candidates rendered at real 16 px with 8x nearest-neighbour magnification
      (01-CONTEXT.md § Specific Ideas). Task 1's cleanup changed the root element's sizing attributes,
      so the check must be carried forward to the shipped file.
      1. Render `web/static/favicon.svg` at 16 px and at 32 px, magnify both 8x nearest-neighbour, and
         compare against the corresponding tile in
         `.planning/phases/01-defect-flake-burn-down/assets/01-mark-round2-sheet.png`.
      2. At 16 px confirm: the C and G are still separable, the shared vertical edge does not collapse
         into a single blob, and the corner nodes still read as nodes rather than smeared strokes.
      3. Confirm the tile's rounded corners are intact and the blue matches the app's own primary
         colour when held next to it.
      4. Load `/` and `/graph` in a real browser and look at the actual tab bar at whatever size the
         browser picks.
      If it does not read at 16 px, the remedy is a viewBox or stroke-weight adjustment in the cleaned
      SVG — NOT a different mark. D-01 locks the artwork.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `web/src/routes/+layout.svelte` contains exactly three icon link tags, with `/favicon.svg` before `/favicon-32.png` before `/apple-touch-icon.png` in source order, and no Svelte expression in any `href`.
    - A comment above the block states why SVG-first order is deterministic.
    - `web/src/lib/assets/favicon.svg` no longer exists and no file under `web/src` references that path.
    - `internal/uiserver/spa.go` and `internal/uiserver/spa_test.go` are byte-unchanged across this plan, and `go test ./internal/uiserver/...` passes.
    - `task web:drift` passes.
    - `web/build/index.html` references `/favicon.svg` and contains no `data:image` URI.
    - A live console run against this repo's index reports zero entries mentioning the favicon, the Content Security Policy, or a refused load.
    - The `<human-check>` 16 px / 32 px legibility review is answered at end-of-phase: the SUMMARY records that the shipped SVG was rendered at 16 px and 32 px at magnification, compared against `01-mark-round2-sheet.png`, and viewed in a real tab bar on `/` and `/graph` — with the approval, or the named failure and the sizing adjustment made in response.
  </acceptance_criteria>
  <done>The app serves the codegraph mark from static `'self'` files on every route, the stock logo is gone, and the CSP came out of it byte-identical.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SPA under `default-src 'self'` | the CSP is the app's only client-side content-injection control; every asset delivery choice either respects it or forces it wider |
| externally-generated asset → committed repository content | the mark SVGs came from a third-party generation service and arrived carrying a ~17 KB embedded C2PA metadata payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06-01 | Tampering | `internal/uiserver/spa.go` CSP directives | high | mitigate | The defect is fixed at the asset, never at the policy: static same-origin files need no directive change. Two verify gates assert `spa.go` and `spa_test.go` are byte-unchanged across the plan's commits, and a third asserts the built HTML contains no `data:image` URI — so "make it work" cannot quietly become "widen `img-src`". Pitfall 18 names exactly this as the out-of-scope entry. |
| T-01-06-02 | Information Disclosure | C2PA `<metadata>` payload in the raw SVGs | medium | mitigate | The raw generator output embeds a ~17 KB base64 C2PA manifest describing the generation provenance. D-03 requires it stripped before shipping; a verify gate asserts neither shipped SVG contains `metadata` or `c2pa`. Shipping it would publish generation metadata to every page load for no functional benefit. |
| T-01-06-03 | Tampering | committed `web/build/` embedded tree | medium | mitigate | `web/build/` is embedded verbatim into every shipped binary. `task web:drift` hashes the source tree and the committed output tree independently and is run as a verify gate, so a source change that skipped the rebuild — or an output byte edited without a source change — fails loudly. |
| T-01-06-04 | Spoofing | the shipped mark | low | accept | A visually wrong or illegible icon is a brand/UX defect, not a security one. Gated by the Task 3 human-verify checkpoint rather than an automated assertion, because legibility at 16 px is a judgement. |
| T-01-06-SC | Tampering | npm/pnpm installs | medium | mitigate | The tempting shortcut here is adding `sharp` or `svgo` to render/optimise the rasters. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase and no audit row exists for any image library, so any such install would be `[ASSUMED]` and would require a blocking legitimacy checkpoint. This plan forbids it outright and a verify gate asserts `web/package.json` gained no image dependency; the rasters are rendered once with a local tool and committed (D-04 discretion). |
</threat_model>

<verification>
- Both shipped SVGs are metadata-free, keep their locked fills, and retain their paths.
- Both PNGs are the right size, non-trivial, and not blank canvases.
- The root layout carries exactly three icon links in SVG → PNG → apple-touch order; the Svelte logo is gone and unreferenced.
- `spa.go` and `spa_test.go` are byte-unchanged; `go test ./internal/uiserver/...` is green.
- `task web:drift` passes; `web/build/index.html` has no `data:image` URI.
- A live console run reports zero favicon/CSP/refused-load entries.
- The end-of-phase `<human-check>` review of the mark at 16 px and 32 px is answered.
</verification>

<success_criteria>
Every UI route serves the codegraph mark from a static `'self'` file, the browser console shows no
CSP-blocked favicon entry, and `internal/uiserver`'s CSP string and its exact test assertion are
byte-identical to what they were before this plan started.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Path / name |
|---|---|---|
| `favicon.svg` | new static asset (cleaned tile mark) | `web/static/favicon.svg` |
| `favicon-32.png` | new static asset (32x32 raster) | `web/static/favicon-32.png` |
| `apple-touch-icon.png` | new static asset (180x180 raster) | `web/static/apple-touch-icon.png` |
| `codegraph-mark.svg` | new static asset (cleaned bare mark, no consumer this phase) | `web/static/codegraph-mark.svg` |
| three icon `<link>` tags | new markup in the root layout head | `web/src/routes/+layout.svelte` |
| stock Svelte logo | REMOVED file | `web/src/lib/assets/favicon.svg` |
| `favicon` module import | REMOVED symbol | `web/src/routes/+layout.svelte` |
</artifacts_produced>

<output>
Create `.planning/phases/01-defect-flake-burn-down/01-06-SUMMARY.md` when done.
</output>