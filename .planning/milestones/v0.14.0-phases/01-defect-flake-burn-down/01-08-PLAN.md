---
phase: 01-defect-flake-burn-down
plan: 08
type: execute
wave: 3
depends_on: ["01-01", "01-06"]
files_modified:
  - web/src/lib/components/graph/graph-style.ts
  - web/src/lib/components/graph/GraphCanvas.svelte
  - web/build/
autonomous: true
requirements: [FIX-04, FIX-05]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `node[?isSymbol]` selector in `web/src/lib/components/graph/graph-style.ts` no longer sets a horizontal value on the vertical-alignment property; `text-halign: 'right'` and `text-margin-x: 4` are retained, so symbol labels still sit to the right of their node. D-08."
    - "After the stylesheet fix, `graph-console-check.mjs` reports ZERO console entries naming the vertical-alignment property on either corpus — the two warnings 01-01 recorded as RED are gone."
    - "`/graph` loads with zero uncaught page errors on this repo's index: the cytoscape `Cannot read properties of null (reading 'notify')` TypeError no longer fires. FIX-04 / WINDOWS #26."
    - "The FIX-04 fix lives entirely in `GraphCanvas.svelte`'s own teardown timing. No `pnpm patch`, no `patches/` directory, no cytoscape or cytoscape-elk version change, and no edit to anything under `web/node_modules/`. D-06."
    - "The exact trigger sequence for the crash is OBSERVED live and recorded — which effect double-fires, under what navigation or throttling condition — not inferred from source reading alone. RESEARCH Assumptions Log A4 tags the fix shape as medium-confidence precisely because no live reproduction had been driven."
    - "A navigation away from `/graph` that interrupts an in-flight ELK layout produces zero page errors and zero our-code console warn/error entries — the concurrency edge, which is the exact window the crash fires in."
    - "`web/build/` is regenerated and committed after every source change in this plan, so `task web:drift` passes and the embedded bytes match the source."
  artifacts:
    - "an ELK-layout-in-flight guard in web/src/lib/components/graph/GraphCanvas.svelte"
  key_links:
    - "runLayout() sets the in-flight marker → cytoscape-elk's elk.layout() promise resolves → layoutPositions → nodes.positions() → cy.startBatch/endBatch → renderer.notify. The mount effect's cleanup must not have nulled the renderer before that chain completes."
    - "the mount $effect's returned cleanup at GraphCanvas.svelte:895-897 → cy.destroy() → destroyRenderer() → cy._private.renderer = null — the single line whose timing is the whole defect"
  prohibitions:
    - "No `pnpm patch`, no `patches/` directory, no edit to any file under `web/node_modules/` (D-06). The missing guard in cytoscape's `endBatch()` is a real library gap but patching it is explicitly out of scope."
    - "No cytoscape, cytoscape-elk or elkjs version bump — a dependency upgrade is not the first move (D-06)."
    - "No `try { } catch { }` swallowing of the TypeError. Suppressing the symptom is the failure mode this whole phase exists to avoid; the fix is teardown timing."
    - "No hand-rolled liveness tracker duplicating `cy.destroyed()` — cytoscape already exposes that predicate and its own `notify()` guard uses it (RESEARCH § Don't Hand-Roll)."
    - "No change to `LAYOUT_OPTIONS`' ELK keys in this plan — the spacing/sizing change is plan 01-09's scope and would confound this plan's before/after comparison."

flagged_assumptions:
  - id: EDGE-FIX-04-01
    requirement: FIX-04
    probe: "unclassified — review manually"
    status: unresolved
    note: "Deterministic edge probe returned `unclassified` for FIX-04; per the spec-less fallback rules an unclassified row is never auto-resolved with a backstop. Flagged for human review: the planner's reading is 'what happens to the deferred cytoscape instance if the component never remounts' — the gated-destroy approach leaves an instance alive until its pending promise settles, and no acceptance criterion pins how long that may be or asserts it is eventually collected. Task 3's action requires a bounded fallback, but the bound itself is unspecified and is a judgement the implementer must make and record."
---

<objective>
Close the two `/graph` console defects that live in this repository's own code: the invalid
stylesheet value, and the cytoscape teardown-timing TypeError.

Purpose: 01-01 built the gate and recorded both defects RED. The stylesheet half is a one-line
certainty — `right` is not a valid vertical-alignment value, and the sibling selectors in the same
file show the correct pairing. The TypeError half has a mechanism confirmed from vendored source
(cytoscape 3.34.2's `endBatch()` dispatches to the renderer with none of the null/destroyed guard its
own sibling `notify()` carries, and cytoscape-elk's `stop()`/`destroy()` are literal no-ops that
cannot cancel the pending elkjs promise) but no observed trigger — so this plan reproduces it before
fixing it.

Output: a corrected stylesheet, a recorded live reproduction, and an in-flight-layout guard on the
mount effect's teardown.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-defect-flake-burn-down/01-CONTEXT.md
@.planning/phases/01-defect-flake-burn-down/01-RESEARCH.md
@.planning/phases/01-defect-flake-burn-down/01-PATTERNS.md
@.planning/phases/01-defect-flake-burn-down/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the symbol-node label alignment</name>
  <files>web/src/lib/components/graph/graph-style.ts, web/build/</files>
  <read_first>
    - web/src/lib/components/graph/graph-style.ts lines 120-145 — the `node[?isSymbol]` selector block in full, including the comment above it about cytoscape not resolving CSS custom properties, and the three label-positioning keys it sets.
    - web/src/lib/components/graph/graph-style.ts lines 83-111 — the sibling `node[!isDirectory]` selector, which shows the valid pairing this file already uses: a vertical value on the vertical property plus the matching margin key.
    - corpora/graph-console-check.json — the RED verdict plan 01-01 committed, specifically the console entries naming the vertical-alignment property, so the fix can be checked against the exact captured text.
  </read_first>
  <action>
In the `node[?isSymbol]` selector, remove the vertical-alignment key that is set to a horizontal
value. Keep `'text-halign': 'right'` and `'text-margin-x': 4` exactly as they are — those two are
what actually place the label to the right of the symbol node, and the invalid key was never doing
anything except emitting a warning per element.

Decide deliberately between removing the key outright and setting it to a real vertical value
(`'center'` is cytoscape's default for this property). Prefer removal: the sibling selectors set the
vertical key only when they want a non-default value, so adding an explicit default here would be
new noise. Record the choice in one short comment naming why the invalid value existed and what
replaced it, so a future reader does not re-add it.

Change nothing else in this file. In particular do not touch the community-palette map immediately
below the selector.

Then `task web:build` to regenerate `web/build/`, and rebuild the binary with `task build:release`.
Run the console gate against BOTH corpora to a scratch output path (plan 01-09 owns the committed
verdict) and confirm the vertical-alignment entries are gone. The run will still fail overall on the
FIX-04 TypeError and the guava invalid-endpoints warnings — this task asserts the disappearance of
one specific entry class, not overall success.

Commit as `fix(01-08): drop the invalid vertical-alignment value from the symbol-node style`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -A14 "selector: 'node\[\?isSymbol\]'" web/src/lib/components/graph/graph-style.ts | tee /tmp/sym-sel.txt; rg -q "'text-halign': 'right'" /tmp/sym-sel.txt && rg -q "'text-margin-x': 4" /tmp/sym-sel.txt && ! rg -q "'text-valign': 'right'" /tmp/sym-sel.txt</automated>
    <fails_when>non-zero exit — the selector lost the two keys that actually position the label (the positive half), or the invalid vertical value is still present within the selector block</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task web:build && task web:drift && task build:release</automated>
    <fails_when>non-zero exit from any of the three — the SPA did not rebuild, the committed output tree does not match the source, or the binary did not link</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node web/scripts/graph-console-check.mjs --out /tmp/fix05a-console.json; jq -e '[.runs[].consoleMessages[] | select(.text | test("valign"))] | length == 0' /tmp/fix05a-console.json</automated>
    <fails_when>non-zero exit from jq — a console entry naming the vertical-alignment property survives on at least one corpus, so the stylesheet fix did not reach the running build</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && jq -e '(.runs | length) == 2' /tmp/fix05a-console.json</automated>
    <fails_when>non-zero exit — fewer than two corpus runs were recorded, so the "zero valign entries" assertion above may have passed only because one corpus never loaded</fails_when>
  </verify>
  <acceptance_criteria>
    - The `node[?isSymbol]` selector retains `'text-halign': 'right'` and `'text-margin-x': 4` and no longer sets a horizontal value on the vertical-alignment property.
    - A comment in the selector records what the invalid value was and why it was removed.
    - `task web:drift` passes after the rebuild.
    - A two-corpus console run records zero entries naming the vertical-alignment property, on both corpora.
    - No other selector in `graph-style.ts` changed: `rg -c "selector:" web/src/lib/components/graph/graph-style.ts` returns the same count it did before the task, recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Symbol-node labels are positioned by the property that actually controls horizontal placement, and the two warnings 01-01 recorded are gone from both corpora.</done>
</task>

<task type="auto">
  <name>Task 2: Reproduce the teardown TypeError live and record its trigger</name>
  <files>web/src/lib/components/graph/GraphCanvas.svelte</files>
  <read_first>
    - web/src/lib/components/graph/GraphCanvas.svelte lines 264-375 — the existing `layoutGeneration` / `activeLayoutRun` in-flight token, `runLayout()`, the `cy.one('layoutstop', ...)` guarded callback, and the comment at 322-328 already flagging `activeLayoutRun.stop()` as best-effort and NOT the correctness mechanism.
    - web/src/lib/components/graph/GraphCanvas.svelte lines 819-899 — the mount `$effect` from its start through the returned cleanup that calls `resizeObserver?.disconnect()`, nulls the renderer and calls `cy.destroy()`.
    - .planning/phases/01-defect-flake-burn-down/01-RESEARCH.md § Investigation 1 and its call-chain diagram — the verbatim guarded `notify()` versus unguarded `endBatch()` quotes, the `destroyRenderer()` null assignment with its "to be extra safe" comment, and the note that `layoutPositions`' non-animate branch calls `nodes.positions()` BEFORE emitting `layoutstop`.
    - .planning/phases/01-defect-flake-burn-down/01-RESEARCH.md § Open Questions item 2 — WINDOWS #26's own observation that the crash does NOT fire when navigating away mid-layout, which means the trigger is more specific than plain unmount timing.
    - .planning/STATE.md Phase 06 decisions — the two previously-fixed Svelte 5 double-invocation effect bugs, found only by real-browser testing at guava scale.
  </read_first>
  <action>
Reproduce the crash in a real browser before changing any teardown code. RESEARCH tags the fix shape
as medium-confidence (Assumptions Log A4) specifically because the trigger was traced by reading, not
observed.

Add TEMPORARY instrumentation to `GraphCanvas.svelte`'s mount `$effect`: a console log on effect
entry and on cleanup entry, each carrying a monotonically-increasing instance id and the current
`layoutGeneration`, plus a log immediately before `cy.destroy()` reporting whether a layout is
outstanding. This instrumentation is scaffolding and must be removed in Task 3 — say so in a comment
on each line so it cannot be mistaken for permanent diagnostics.

Drive the reproduction with a real Chromium session, starting with this repository's own smaller and
faster-laying-out index where the window is easier to hit deliberately, then guava. Try, in order,
and record which one fires it:
  1. CPU throttling (Chrome DevTools Protocol `Emulation.setCPUThrottlingRate`, reachable from
     Playwright via a CDP session) to stretch the ELK promise's resolution window, then navigate away
     from `/graph` mid-layout;
  2. rapid route re-navigation into and out of `/graph` before the first layout settles;
  3. a Svelte dev-mode double effect invocation, if the dev server reproduces what the embedded
     production build does not.

Record in the SUMMARY: which sequence fired it, the instance ids and generations logged at the moment
of the crash, the full error text, and whether the crash instance was the first or a later mount.
If NONE of the three reproduces it, say so plainly and record what was tried — Task 3 then implements
the guard on the source-traced mechanism with the failed reproduction attempts on the record, which
is an honest outcome and must not be dressed up as a confirmed one.

Do not fix anything in this task. Commit the instrumentation only if it is needed to hand off to
Task 3 in a separate session; otherwise keep it in the working tree and let Task 3 remove it.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task web:build && task build:release && node web/scripts/graph-console-check.mjs --corpus self --out /tmp/fix04-repro.json; jq -e '.runs[0].pageErrors | length >= 0' /tmp/fix04-repro.json</automated>
    <fails_when>non-zero exit from jq, or the verdict file is absent — the instrumented build did not produce a readable verdict, so the reproduction session has no recorded baseline to reason from</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -c 'TEMPORARY|remove in Task 3' web/src/lib/components/graph/GraphCanvas.svelte</automated>
    <fails_when>the printed count is 0, or rg exits non-zero — the instrumentation carries no removal marker, so Task 3 cannot prove it stripped all of it</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records which of the three trigger sequences fired the TypeError, or states explicitly that none did and lists what was tried.
    - If reproduced: the SUMMARY carries the logged instance ids, the `layoutGeneration` values at crash time, the full error text, and whether the crashing instance was the first mount.
    - Every instrumentation line in `GraphCanvas.svelte` carries a removal marker naming Task 3.
    - No teardown, layout or lifecycle behaviour was changed in this task — only log statements were added.
  </acceptance_criteria>
  <done>The crash's trigger has been observed and recorded, or the failure to observe it has been recorded with the attempts, so Task 3's guard is built on evidence rather than on inference alone.</done>
</task>

<task type="auto">
  <name>Task 3: Gate `cy.destroy()` on an outstanding ELK layout</name>
  <files>web/src/lib/components/graph/GraphCanvas.svelte, web/build/</files>
  <read_first>
    - web/src/lib/components/graph/GraphCanvas.svelte lines 264-375 and 819-899 — the same two regions as Task 2, now with Task 2's instrumentation in place and its findings known.
    - The Task 2 reproduction findings recorded in this plan's SUMMARY — which sequence fires the crash and on which instance.
    - .planning/phases/01-defect-flake-burn-down/01-RESEARCH.md § Investigation 1 "Fix direction" — the reasoning that "cancel on destroy" cannot work because cytoscape-elk's `stop()`/`destroy()` are no-ops against the pending elkjs promise, so option (a), deferring or gating the destroy, is the only realistic path.
    - .planning/phases/01-defect-flake-burn-down/01-PATTERNS.md § GraphCanvas.svelte — the instruction to use `cy.destroyed()`, the predicate cytoscape's own `notify()` guard checks, rather than inventing a parallel liveness tracker.
  </read_first>
  <action>
Remove every line of Task 2's temporary instrumentation first, so the diff that ships is the fix and
nothing else.

Then extend the file's OWN existing in-flight-layout pattern rather than introducing a second
mechanism. Alongside `layoutGeneration` and `activeLayoutRun`, track whether an ELK-backed layout is
currently outstanding: set the marker immediately before `.run()` in `runLayout()`, and clear it in
the same `cy.one('layoutstop', ...)` callback that already checks the generation token — clearing it
only for the generation that is still current, so a superseded run's late `layoutstop` cannot clear a
newer run's marker.

In the mount effect's cleanup, gate the `cy.destroy()` call on that marker:
  - if no layout is outstanding, tear down exactly as today;
  - if one is outstanding, do the parts of teardown that are safe — disconnect the ResizeObserver,
    drop this component's own event handlers, detach the container's DOM node — and DEFER the
    `cy.destroy()` until the outstanding layout settles, re-checking `cy.destroyed()` before calling
    it so a second cleanup cannot double-destroy.
  - carry a bounded fallback so a promise that never settles cannot leak the instance forever. CHOOSE
    the bound deliberately, state it in a comment with the reasoning, and record it in the SUMMARY —
    this is the flagged-unresolved edge EDGE-FIX-04-01 and it must be a considered number, not a
    copied one.

Use `cy.destroyed()` for every liveness question; do not build a parallel tracker. Do not wrap
anything in a `try`/`catch` that swallows the TypeError — a caught crash is a hidden crash, and the
whole phase's constraint is to fix causes.

Write a comment above the gate stating the mechanism in one paragraph: cytoscape's `endBatch()`
dispatches to the renderer without the null/destroyed guard its sibling `notify()` carries; that
dispatch happens inside the position write that cytoscape-elk's async resolution triggers, BEFORE
`layoutstop` is emitted, so the existing generation token cannot reach it; and cytoscape-elk's
`stop()`/`destroy()` are no-ops, so the layout cannot be cancelled and the destroy must instead wait.
Name WINDOWS #26. This comment is the "isolate with the root cause recorded" half of D-06 and is a
required deliverable.

Then `task web:build`, `task web:drift`, `task build:release`, and re-run the reproduction sequence
Task 2 identified — the fix must be checked against the sequence that actually fires the crash, not
only against a clean page load. Finish with a two-corpus console run to a scratch path.

Commit as `fix(01-08): defer cytoscape teardown while an ELK layout is still in flight`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && ! rg -q 'TEMPORARY|remove in Task 3' web/src/lib/components/graph/GraphCanvas.svelte && rg -q 'destroyed\(\)' web/src/lib/components/graph/GraphCanvas.svelte</automated>
    <fails_when>non-zero exit — Task 2's instrumentation survived into the shipped file, or the file contains no `destroyed()` liveness check (the positive half, proving the guard was actually added)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && ! rg -q 'pnpm patch' web/package.json Taskfile.yml && test ! -d web/patches && test ! -d patches && printf '%s  %s\n%s  %s\n' a450d23bcf05f2513e068f3e8c8ba5165506b73f4090a937d6f145079d01b5f3 web/package.json e665384780919e5882ee8ea39896f1ec43bc985f5c7ccc1ef34235db0731018e web/pnpm-lock.yaml | shasum -a 256 -c -</automated>
    <fails_when>non-zero exit, or either digest line does not end in `: OK` — a dependency patch mechanism appeared, a patches directory exists, or the manifest/lockfile bytes moved from the digests recorded at planning time, any of which violates D-06's no-dependency-patch constraint</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task web:build && task web:drift && task build:release</automated>
    <fails_when>non-zero exit from any of the three — the SPA did not rebuild, the committed output tree does not match the source, or the binary did not link</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node web/scripts/graph-console-check.mjs --out /tmp/fix04-green.json; jq -e '(.runs | length) == 2 and ([.runs[] | select(.corpus == "self")] | map(.pageErrorCount) | add) == 0' /tmp/fix04-green.json</automated>
    <fails_when>non-zero exit from jq — either fewer than two corpus runs were recorded, or this repository's own corpus still produced at least one uncaught page error, meaning the TypeError still fires</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && jq -e '[.runs[].pageErrors[]] | map(select(test("reading .notify."))) | length == 0' /tmp/fix04-green.json</automated>
    <fails_when>non-zero exit from jq — a captured page error still names the null-renderer notify read on either corpus</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log --format=%H -- web/src/lib/components/graph/GraphCanvas.svelte > /tmp/gc-hist.txt || exit 1; BASE=$(tail -1 /tmp/gc-hist.txt); test -n "$BASE" || exit 1; git show "$BASE":web/src/lib/components/graph/GraphCanvas.svelte > /tmp/gc-base.svelte || exit 1; B=$(rg -c 'catch' /tmp/gc-base.svelte || echo 0); N=$(rg -c 'catch' web/src/lib/components/graph/GraphCanvas.svelte || echo 0); echo "catch-count base=$B now=$N"; test "$N" -le "$B"</automated>
    <fails_when>non-zero exit, or the printed `now=` count exceeds the printed `base=` count — a catch block was added to the component, which would mean the TypeError is being swallowed rather than prevented</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -o "'(elk|org[.]eclipse[.]elk)[^']*'" web/src/lib/components/graph/GraphCanvas.svelte | sort -u > /tmp/elk-now.txt; printf "%s\n" "'elk.hierarchyHandling'" "'elk.padding'" "'org.eclipse.elk.interactive'" "'org.eclipse.elk.layered.crossingMinimization.semiInteractive'" "'org.eclipse.elk.layered.cycleBreaking.strategy'" "'org.eclipse.elk.layered.layering.strategy'" | sort -u > /tmp/elk-expected.txt; cat /tmp/elk-now.txt; diff -u /tmp/elk-expected.txt /tmp/elk-now.txt</automated>
    <fails_when>non-zero exit from `diff`, or any `+`/`-` line in its output — the set of ELK option keys in the component differs from the six recorded at planning time, meaning a layout option changed in this plan; that belongs to plan 01-09 and would confound this plan's before/after comparison</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && cd web && pnpm check 2>&1 | tee /tmp/svelte-check.txt; rg -q '0 errors' /tmp/svelte-check.txt</automated>
    <fails_when>non-zero exit, or the output does not report zero errors — the teardown change introduced a type or Svelte compile error</fails_when>
  </verify>
  <acceptance_criteria>
    - No instrumentation removal marker remains in `GraphCanvas.svelte`, and the file contains at least one `destroyed()` liveness check.
    - The in-flight marker is set immediately before `.run()` and cleared only inside the generation-checked `layoutstop` callback.
    - The cleanup defers `cy.destroy()` when a layout is outstanding, re-checks `cy.destroyed()` before the deferred call, and carries a bounded fallback whose value and reasoning are stated in a comment and in the SUMMARY.
    - A comment above the gate names the `endBatch()` versus `notify()` guard asymmetry, the pre-`layoutstop` position write, cytoscape-elk's no-op `stop()`/`destroy()`, and WINDOWS #26.
    - No `pnpm patch`, no `patches/` directory, and `web/package.json` / `web/pnpm-lock.yaml` are unchanged across the plan.
    - No `catch (` block was added to `GraphCanvas.svelte`.
    - The set of ELK option keys in `GraphCanvas.svelte` is unchanged from the six recorded at planning time.
    - `task web:drift` passes; a two-corpus console run reports zero page errors on this repository's corpus and no page error naming the notify read on either corpus.
    - `pnpm check` reports zero errors.
    - The reproduction sequence Task 2 identified was re-run after the fix and is recorded in the SUMMARY as no longer firing.
  </acceptance_criteria>
  <done>`/graph` loads and tears down with zero uncaught page errors, fixed at the teardown timing in this repository's own component, with the library-side root cause recorded in the file where the guard lives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party corpus content → cytoscape node labels and ids | guava's file and symbol names become rendered graph element data; the stylesheet and layout code consume them |
| vendored dependency internals → this repository's code | the defect's mechanism lives in `web/node_modules/` but the fix must live on this side of the boundary (D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08-01 | Tampering | `web/node_modules/cytoscape` | high | mitigate | The one-line patch that adds the missing `endBatch()` guard is genuinely tempting and is exactly what D-06 forbids: a vendored edit is invisible to `pnpm install`, unreviewed, and silently lost on the next lockfile change. The fix is confined to `GraphCanvas.svelte`'s teardown timing, and verify gates assert no `patches/` directory, no `pnpm patch`, and an unchanged manifest and lockfile. |
| T-01-08-02 | Denial of Service | deferred `cy.destroy()` | medium | mitigate | Gating the destroy on an outstanding layout risks leaking a cytoscape instance whose promise never settles, which at guava scale is a real memory cost. Mitigated by a bounded fallback whose value must be chosen deliberately and recorded; tracked as flagged assumption EDGE-FIX-04-01 because no acceptance criterion pins the bound itself. |
| T-01-08-03 | Repudiation | a swallowed TypeError | high | mitigate | Wrapping the position write in a catch would make the console clean while the defect remains — indistinguishable, from the gate's point of view, from a real fix. Explicitly prohibited and asserted by a verify gate that flags any newly-added catch block in the component. |
| T-01-08-04 | Tampering | committed `web/build/` embedded tree | medium | mitigate | Every source change in this plan is followed by `task web:build` and gated by `task web:drift`, so the embedded bytes shipped to users always match the reviewed source. |
| T-01-08-SC | Tampering | npm/pnpm installs | medium | mitigate | The alternative fix path RESEARCH considered and rejected is a dependency upgrade (cytoscape/cytoscape-elk/elkjs). Any such bump would be a package change with no legitimacy audit row in RESEARCH.md, therefore `[ASSUMED]` and requiring a blocking checkpoint. This plan forbids it and a verify gate asserts the lockfile is unchanged. |
</threat_model>

<verification>
- The symbol-node selector keeps its horizontal-placement keys and drops the invalid vertical value; zero vertical-alignment console entries remain on either corpus.
- The crash's trigger sequence is recorded, or the failure to reproduce it is recorded with the attempts.
- No instrumentation survives; the guard uses `cy.destroyed()`; a bounded fallback is documented.
- Zero uncaught page errors on this repository's corpus; no page error names the notify read on either corpus.
- No dependency patch, no lockfile change, no added catch block, no ELK option change.
- `task web:drift` and `pnpm check` both pass.
</verification>

<success_criteria>
`/graph` produces zero uncaught page errors and zero vertical-alignment warnings on both corpora,
fixed at the teardown timing in this repository's own component with the library-side cause recorded
in place — and without patching, upgrading or swallowing anything.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Path / name |
|---|---|---|
| ELK-layout-in-flight marker | new component-scope state in `GraphCanvas.svelte` | `web/src/lib/components/graph/GraphCanvas.svelte` |
| deferred-destroy gate | new logic in the mount `$effect` cleanup | `web/src/lib/components/graph/GraphCanvas.svelte` |
| root-cause comment (WINDOWS #26, `endBatch` guard asymmetry) | new in-file documentation | `web/src/lib/components/graph/GraphCanvas.svelte` |
| invalid vertical-alignment key on `node[?isSymbol]` | REMOVED style key | `web/src/lib/components/graph/graph-style.ts` |
</artifacts_produced>

<output>
Create `.planning/phases/01-defect-flake-burn-down/01-08-SUMMARY.md` when done.
</output>