---
phase: 04-cli-glow-up
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - internal/cli/colorflag.go
  - internal/cli/colorflag_test.go
  - internal/cli/root.go
  - internal/cli/status.go
  - internal/cli/files.go
  - internal/cli/present/palette.go
  - internal/cli/present/palette_test.go
  - internal/cli/present/styles.go
  - internal/cli/present/status.go
  - internal/cli/present/files.go
  - internal/cli/present/status_test.go
  - internal/cli/present/files_test.go
  - internal/cli/present/ansistrip_test.go
  - test/integration/status_files_plain_test.go
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One shared resolver in `internal/cli/colorflag.go` performs the phase's SINGLE environment read: `resolveColor(cmd)` reads `os.Environ()` once, rewrites it per the `--color` choice, calls `colorprofile.Detect(cmd.OutOrStdout(), environ)` once, derives `styled` through the UNCHANGED two-arg `present.ChoosePresentation(isTTY, noColor)` (`isTTY := p > colorprofile.NoTTY`, `noColor := \"\"` when `p > colorprofile.ASCII` else `\"1\"`), and returns `colorMode{Styled, Profile, Dark}`; every styled RunE wraps its writer in `colorprofile.Writer{Forward: cmd.OutOrStdout(), Profile: p}` at the RunE boundary and `present` never sees the profile or reads env/fd (D-09, D-10, CLI-02, CLI-03)."
    - "`rewriteEnviron(choice, environ)` is pure and pinned by `TestRewriteEnviron`: `always` drops every `NO_COLOR=`/`CLICOLOR=` entry and appends `CLICOLOR_FORCE=1`; `never` appends `NO_COLOR=1` AND drops every `CLICOLOR_FORCE=`/`CLICOLOR=` entry (planner amendment A1, verified this session against colorprofile v0.4.3: `colorProfile` honours `cliColorForced` before `NO_COLOR` on a non-TTY, so `NO_COLOR=1 CLICOLOR_FORCE=1 … | cat` still yields ANSI256 — without the drop, `--color=never` on a pipe under a user's `CLICOLOR_FORCE=1` would be styled); `auto` passes through except two normalizations of OUR input: a non-empty `NO_COLOR=<v>` becomes `NO_COLOR=1` (D-09 research correction: colorprofile ParseBool-gates it) and — amendment A2 — a `CLICOLOR=<v>` with non-empty `v` that `strconv.ParseBool` reads as false (`0`, `false`…) appends `NO_COLOR=1` UNLESS `CLICOLOR_FORCE` is ParseBool-true (verified this session: `CLICOLOR=0` alone is a no-op in colorprofile v0.4.3, so CLI-03's stated `NO_COLOR > CLICOLOR_FORCE > CLICOLOR=0 > TTY` order is otherwise not delivered). Both amendments are asserted as rewritten-environ rows, never as colorprofile tests (D-00)."
    - "`--color` is a persistent root flag (`addColorFlag(root)`), default `auto`, backed by a `pflag.Value` (`colorChoice`) whose `Set` rejects anything but `auto|always|never` — so `codegraph status --color=bogus` exits 1 through `main.go`'s single error path with a message naming all three values; `--color=` (empty) is rejected the same way; a command built standalone (no root, e.g. `newUiCmd()` in tests) resolves as `auto` because `colorChoiceOf(cmd)` falls back when `cmd.Flags().Lookup(\"color\")` is nil (D-11, CLI-03)."
    - "`TestResolveColorMatrix` covers the layer we own — `--color ∈ {auto, always, never}` × `TTY ∈ {yes (environ carries TTY_FORCE=1, colorprofile v0.4.3's documented `isTTYForced` hook used purely as a test input), no (a bytes.Buffer writer)}` — with positive assertions both ways: `always` on a pipe → Styled true (CLICOLOR_FORCE beats a pipe, NO_COLOR=1 and TERM=dumb); `never` on a TTY with CLICOLOR_FORCE=1 → Styled false; `never` on a pipe with CLICOLOR_FORCE=1 → false (A1); `auto` on a pipe → false; `auto` on a TTY with TERM=xterm-256color → true (the positive control); `auto` + `NO_COLOR=banana` on a TTY → false and the rewritten environ carries `NO_COLOR=1`; `auto` + `NO_COLOR=` (empty) on a TTY → true; `auto` + `CLICOLOR=0` on a TTY → false (A2) and `auto` + `CLICOLOR=0` + `CLICOLOR_FORCE=1` → true; `TestColorFlagInvalidValue` → `bogus` and empty rejected via execCmd with the three names in the error (D-09 (a)(b)(c), CLI-03)."
    - "`lipgloss.HasDarkBackground(in, out)` is called at most ONCE per process, only when `Styled` AND `cmd.OutOrStdout()` is an `*os.File` that `fdIsTerminal` reports as a terminal AND `cmd.InOrStdin()` is an `*os.File` that is a terminal, passing those real files; otherwise `Dark` defaults to true with no query (D-11 research correction — the query puts stdin into raw mode and blocks up to 2 s on a non-answering terminal; never on a pipe, never with redirected stdin). `TestDarkBackgroundQueryGate` swaps the `fdIsTerminal`/`queryDarkBackground` seams (the install.go func-var idiom) and asserts call counts 1/0/0 for (both TTY)/(stdout only)/(pipe) and `Dark == true` when unqueried (CLI-04)."
    - "`present.Palette` has exactly seven `lipgloss.Style` fields — Header, Label, Value, Path, Count, Warning, Error — built by `present.NewPalette(dark bool)` from one `lipgloss.LightDark(dark)` closure over a `paletteHex` table of truecolor light/dark hex pairs (Header bold, Label faint, Warning bold, Error bold; hues per Task 2); `styles.go`'s three package-level vars are deleted (headerStyle and sectionStyle fold into Header, labelStyle into Label — no eighth role); no mutable package-level style remains in the renderers and there is no setter; `RenderStatus`/`RenderFiles` take `Palette` as a parameter (D-05, D-06, CLI-04)."
    - "Tracer slice, end to end and production-quality: `codegraph status` resolves colour through the resolver, wraps stdout in `colorprofile.Writer`, renders `present.RenderStatus(result, start, pal, w)` when styled, and leaves the `--json` early return and the plain `query.RenderStatusText` fallback structurally untouched — proven by `TestPlainGolden`/`TestNoColorNonTTYRegression` byte-identical (28/28) after the change, by `status --color=always | cat` emitting ESC bytes from the real binary while `status --color=never | cat`, `NO_COLOR=1 … | cat` and a bare pipe emit none, and by `--color=bogus` exiting 1 (CLI-01, CLI-05)."
    - "CLI-02 (probe boundary, explicit): the styled/plain boundary is exactly `p > colorprofile.ASCII` — `ASCII` (NO_COLOR on a TTY) and `NoTTY` both select the plain branch, `ANSI` is the first styled profile; pinned by the matrix rows `never`+TTY → false and `auto`+`TERM=xterm` (16-colour) TTY → true."
    - "CLI-02 (probe adjacency, explicit): there is no third 'decorated but uncoloured' branch — `ASCII` collapses to the same plain path as `NoTTY`, byte-identical to the goldens."
    - "CLI-02 (probe empty, explicit): unset `TERM` or `TERM=dumb` with `--color=auto` → plain; with `--color=always` → styled at ANSI (colorprofile floors a forced profile at ANSI) — both are matrix rows."
    - "CLI-02 (probe ordering, explicit): `--color` is applied by rewriting the environ BEFORE the single `Detect` call, so the flag's precedence over every env var is structural; the order among env vars below the flag is colorprofile's own (NO_COLOR > CLICOLOR_FORCE > CLICOLOR > TTY), cited in `colorflag.go`'s comment with `v0.4.3` and NOT re-tested (D-00, D-09)."
    - "CLI-02 (probe precision, explicit): the profile handed to `colorprofile.Writer` is the exact value `Detect` returned — no re-mapping, no clamping — asserted by the matrix rows checking `Profile` alongside `Styled`; downsampling fidelity itself is colorprofile's and is a manual UAT item (D-06)."
    - "CLI-03 (probe adjacency, explicit): the two 'touching' cases are pinned — `--color=never` with `CLICOLOR_FORCE=1` → plain; `--color=always` with `NO_COLOR=1` → styled; the flag wins both ways."
    - "CLI-03 (probe empty, explicit): `--color=` (empty value) is a usage error; `--color` absent → `auto`; `NO_COLOR=` (empty) is 'unset' — the normalizer rewrites only a non-empty value."
    - "CLI-03 (probe ordering, explicit): the matrix is a fixed table keyed (choice, TTY, environ); the resolver has no order-dependent state — every row is independent and the environ slice is copied before rewriting (the caller's `os.Environ()` result is never mutated)."
    - "CLI-04 (probe adjacency, explicit): luminance at exactly the light/dark threshold is decided by lipgloss's `isDarkColor` (cited, not tested); our palette has exactly two variants per role and `NewPalette` is called once per RunE with the resolver's single `Dark` answer."
    - "CLI-04 (probe empty, explicit): a terminal that does not answer OSC-11 (timeout/error) yields `Dark == true` inside lipgloss and the resolver issues no retry; a pipe or redirected stdin never issues the query at all (call-count 0 rows)."
    - "CLI-04 (probe ordering, explicit): `Detect` runs first, the background query second and only if styled, `NewPalette` third, the render last — one fixed sequence in every RunE."
    - "CLI-01 (probe concurrency, explicit): rendering is synchronous on the RunE goroutine and the resolver has no shared mutable state beyond the two test seams (package-level func vars, never reassigned in production); `GOTOOLCHAIN=go1.26.6 go test -race -count=1 ./internal/cli/ ./internal/cli/present/` is clean at this plan's HEAD."
    - statement: "Every hue is legible on Solarized Light (or macOS light Terminal) AND on a dark theme — header, label, value, path, count, warning and error visibly distinct in both; a ~2 s pause before styled output over SSH/tmux is the OSC-11 query timing out, not a hang (D-06/D-11 human UAT, harvested from the `<human-check>` below at end of phase)."
      verification: backstop
  artifacts:
    - "internal/cli/colorflag.go — `colorFlagName`, `colorChoice` (+ `String`/`Set`/`Type`), `colorAuto`/`colorAlways`/`colorNever`, `addColorFlag(root)`, `colorChoiceOf(cmd)`, `rewriteEnviron`, `colorMode`, `resolveColor(cmd)`, `resolveColorFrom(choice, environ, out io.Writer, in io.Reader)`, `resolveColorStderr(cmd)` (Detect on `cmd.ErrOrStderr()`, never queries the background — plan 07's serve banners), `(colorMode).Writer(w)`, seams `fdIsTerminal`, `queryDarkBackground`"
    - "internal/cli/colorflag_test.go — `TestRewriteEnviron`, `TestResolveColorMatrix`, `TestColorFlagInvalidValue`, `TestDarkBackgroundQueryGate`"
    - "internal/cli/present/palette.go — `Role`, `RoleHeader…RoleError`, `Palette`, `NewPalette`, `(Palette).Style`, `paletteHex`; palette_test.go — `TestNewPaletteRolesFollowDark`"
    - "internal/cli/present/styles.go — the three vars removed (file keeps the package doc comment)"
    - "internal/cli/present/ansistrip_test.go — `stripANSI(s string) string` (package-local test helper over `regexp.MustCompile(\"\\x1b\\\\[[0-9;]*m\")`) shared by every later renderer contract test in this package; no new module"
    - "internal/cli/root.go — `addColorFlag(root)` call in `newRootCmd`; internal/cli/status.go, files.go — resolver + Writer + palette wiring"
    - "test/integration/status_files_plain_test.go — a `--color=always` piped positive-control case and a `--color=never`/`--color=bogus` case"
  key_links:
    - "`root.go` `addColorFlag(root)` → `root.PersistentFlags().VarP(&choice, \"color\", \"\", …)` → `colorChoiceOf(cmd)` via `cmd.Flags().Lookup(\"color\")` after cobra's mergePersistentFlags → `resolveColor`"
    - "`resolveColor` → `colorprofile.Detect(cmd.OutOrStdout(), rewriteEnviron(choice, os.Environ()))` → `present.ChoosePresentation(p > NoTTY, noColorMarker(p))` (unchanged signature) → `colorprofile.Writer{Forward, Profile}` → `present.RenderStatus(result, start, present.NewPalette(mode.Dark), w)`"
    - "`present.NewPalette(dark)` → `lipgloss.LightDark(dark)` closure → seven `lipgloss.Style` values → `RenderStatus`/`RenderFiles` chrome (was headerStyle/labelStyle/sectionStyle)"
  prohibitions:
    - statement: "MUST NOT issue the OSC-11 background query or put stdin into raw mode when stdin or stdout is not an interactive terminal — in particular never when stdout is a pipe (an agent's JSON-RPC stream) — the `TestDarkBackgroundQueryGate` 0-call rows are the gate. D-11."
      status: resolved
      verification: test
    - statement: "MUST NOT read `os.Getenv`, `os.Environ` or call `term.IsTerminal` anywhere under `internal/cli/present` (present stays env-blind, v1.0 Phase 6 D-03/D-10); the RunE boundary owns every fd/env read. Gate: `rg -n 'os\\.Getenv|os\\.Environ|term\\.IsTerminal|colorprofile' internal/cli/present/*.go` matches nothing outside `_test.go`."
      status: resolved
      verification: test
    - statement: "MUST NOT widen `present.ChoosePresentation`'s two-arg signature or add a second env-precedence decision tree — the resolver hands it two derived values and delegates precedence to colorprofile (cited), the only additions being the three documented input normalizations. D-09, RESEARCH Don't-Hand-Roll."
      status: resolved
      verification: test
    - statement: "MUST NOT move the `--json` early return or the plain fallback in any RunE; MUST NOT golden-freeze styled bytes (D-06)."
      status: resolved
      verification: test
---

<objective>
The tracer: prove the whole glow-up architecture on ONE verb before expanding. Build the single
shared colour resolver (`--color` persistent flag → environ rewrite → one `colorprofile.Detect` →
the unchanged `present.ChoosePresentation` → a `colorprofile.Writer` at the RunE boundary → a
gated, at-most-once `lipgloss.HasDarkBackground`), the seven-role `present.Palette`, and restyle
`codegraph status` end to end through it — with the plain path proven byte-identical to the frozen
goldens and the styled path proven live from the real binary.

Purpose: CLI-02/03/04 are one mechanism (D-09/D-10/D-11) that every later renderer consumes; if
its shape is wrong, better to learn on `status` (which already has a renderer) than after
twenty verbs are wired. Two colorprofile facts verified this session amend D-09's rewrite list
(A1: `never` must drop `CLICOLOR_FORCE`; A2: `CLICOLOR=0` needs normalization for CLI-03's stated
order) — both are recorded here as amendments for the maintainer, implemented as input rewrites,
and asserted only as rewritten-environ rows (D-00).

TDD: `workflow.tdd_mode` is on. Each task's failing tests land as a `test(04-03): …` commit BEFORE
the implementation — test files plus the minimal placeholder declarations needed to compile
(Phase 3's 03-02 precedent) — and the pasted `--- FAIL:` transcript in the SUMMARY is the RED
evidence. Never gate on `gsd-tools check tdd-red-evidence` (TAP-only; always INVALID_RED for
`go test`). GREEN lands as `feat(04-03): …`.

Output: `test(04-03):` RED commit(s), then `feat(04-03): shared colour resolver, --color flag and
palette; status restyled end to end`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-glow-up/04-CONTEXT.md
@.planning/phases/04-cli-glow-up/04-RESEARCH.md
@.planning/phases/04-cli-glow-up/04-PATTERNS.md
@.planning/phases/04-cli-glow-up/04-FANG-VERDICT.md
@internal/cli/status.go
@internal/cli/files.go
@internal/cli/root.go
@internal/cli/present/styles.go
@internal/cli/present/tty.go
@internal/cli/present/status.go
@internal/cli/present/files.go
@internal/cli/install.go
@internal/cli/tui/tty.go
</context>

<interfaces>
<!-- Verified this planning session by reading the pinned module sources and running real code against colorprofile v0.4.3. Cite in code comments; do not re-test. -->

From github.com/charmbracelet/colorprofile@v0.4.3:
```go
type Profile byte
const ( Unknown Profile = iota; NoTTY; ASCII; ANSI; ANSI256; TrueColor )   // profile.go — ordered, comparable with >
const Ascii = ASCII
func Detect(output io.Writer, env []string) Profile   // env.go:33 — isatty := isTTYForced(env) || (output.(term.File) && term.IsTerminal(fd)); TERM unset/dumb ⇒ NoTTY unless forced
type Writer struct { Forward io.Writer; Profile Profile }   // writer.go:32 — Write downsamples for ASCII/ANSI/ANSI256, strips for <= NoTTY, passes through for TrueColor
// env.go: envNoColor/cliColor/cliColorForced are ALL strconv.ParseBool-gated; isTTYForced reads TTY_FORCE
// Empirical (this session, scratch program): tty+CLICOLOR=0 → ANSI256; pipe+NO_COLOR=1+CLICOLOR_FORCE=1 → ANSI256; tty+NO_COLOR=1+CLICOLOR_FORCE=1 → Ascii; pipe+CLICOLOR_FORCE=1+TERM=dumb → ANSI; tty+NO_COLOR=banana → ANSI256; tty+NO_COLOR= → ANSI256; tty+TERM=xterm → ANSI; tty+TERM=dumb → NoTTY; pipe → NoTTY
```

From charm.land/lipgloss/v2@v2.0.5:
```go
type LightDarkFunc func(light, dark color.Color) color.Color   // color.go:174
func LightDark(isDark bool) LightDarkFunc                       // color.go:205
func HasDarkBackground(in term.File, out term.File) bool        // query.go:86 — BOTH must be TTYs; raw-mode stdin; 2 s defaultQueryTimeout; true on any error
```

From internal/cli/present/tty.go (UNCHANGED):
```go
func ChoosePresentation(isTTY bool, noColor string) bool   // isTTY && noColor == ""
```

From internal/cli/present (current signatures this plan changes):
```go
func RenderStatus(r query.StatusResult, projectPath string, w io.Writer) error   // → (r, projectPath, pal Palette, w)
func RenderFiles(r query.FilesResult, w io.Writer) error                        // → (r, pal Palette, w)
```
</interfaces>

## Artifacts this phase produces

See `04-01-PLAN.md` → "Artifacts this phase produces" (phase-wide list). This plan creates `colorflag.go`'s whole surface, `present.Palette`/`NewPalette`/`Role`, the `--color` flag, and the `Palette` parameter on `RenderStatus`/`RenderFiles`.

<!-- planner-discipline-allow: headerStyle -->
<!-- planner-discipline-allow: labelStyle -->
<!-- planner-discipline-allow: sectionStyle -->
<!-- planner-discipline-allow: os.Getenv -->
<!-- planner-discipline-allow: term.IsTerminal -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "status renders through the resolver + colorprofile.Writer" — one verb only</name>
  <files>internal/cli/colorflag.go, internal/cli/colorflag_test.go, internal/cli/root.go, internal/cli/status.go, test/integration/status_files_plain_test.go</files>
  <read_first>
    - internal/cli/status.go (lines 78-96 — the three-branch idiom; the styled branch is the only thing that changes)
    - internal/cli/root.go (newRootCmd — add the persistent flag; Execute() may already be fang-wrapped per the verdict)
    - internal/cli/present/tty.go (ChoosePresentation — unchanged, its two args are what the resolver derives)
    - internal/cli/install.go lines 22-36 (the func-var seam idiom and parseLocationFlag's error wording)
    - internal/cli/tui/tty.go (stdinIsInteractive / stdoutIsTTY — the same fd checks, do not duplicate their names)
    - internal/cli/present/tty_test.go (the table-test shape for the matrix)
    - test/integration/status_files_plain_test.go (extend with the real-binary --color cases)
    - .planning/phases/04-cli-glow-up/04-CONTEXT.md (D-09 + correction, D-10, D-11 + correction)
    - .planning/phases/04-cli-glow-up/04-RESEARCH.md (Code Examples: Detect's documented rules block — copy its wording into colorflag.go's comment with the version)
    - /Users/sean/go/pkg/mod/github.com/charmbracelet/colorprofile@v0.4.3/env.go (colorProfile, envNoColor, cliColor, cliColorForced, isTTYForced — cite line numbers)
  </read_first>
  <behavior>
    - TestRewriteEnviron: `always` over {NO_COLOR=1, CLICOLOR=1, TERM=x} → {TERM=x, CLICOLOR_FORCE=1}; `never` over {CLICOLOR_FORCE=1, CLICOLOR=1, TERM=x} → {TERM=x, NO_COLOR=1}; `auto` over {NO_COLOR=banana} → {NO_COLOR=1}; `auto` over {NO_COLOR=} → unchanged; `auto` over {CLICOLOR=0, TERM=x} → {CLICOLOR=0, TERM=x, NO_COLOR=1}; `auto` over {CLICOLOR=0, CLICOLOR_FORCE=1} → unchanged; `auto` over {CLICOLOR=1} → unchanged; the input slice is not mutated (compare a copy)
    - TestResolveColorMatrix (resolveColorFrom(choice, environ, out, in)): always/pipe/{} → Styled=true, Profile>=ANSI; always/pipe/{NO_COLOR=1} → true; always/pipe/{TERM=dumb} → true, Profile=ANSI; never/tty/{CLICOLOR_FORCE=1,TERM=xterm-256color} → false; never/pipe/{CLICOLOR_FORCE=1} → false; auto/pipe/{TERM=xterm-256color} → false, Profile=NoTTY; auto/tty/{TERM=xterm-256color} → true, Profile=ANSI256; auto/tty/{TERM=xterm} → true, Profile=ANSI; auto/tty/{TERM=dumb} → false; auto/tty/{NO_COLOR=banana,TERM=xterm-256color} → false; auto/tty/{NO_COLOR=,TERM=xterm-256color} → true; auto/tty/{CLICOLOR=0,TERM=xterm-256color} → false; auto/tty/{CLICOLOR=0,CLICOLOR_FORCE=1,TERM=xterm-256color} → true (tty rows put TTY_FORCE=1 in environ; pipe rows use a bytes.Buffer out)
    - TestColorFlagInvalidValue: execCmd("status", "--color=bogus", "-p", dir) returns an error whose text contains `auto`, `always` and `never`; execCmd("status", "--color=", …) errors likewise; execCmd("status", "--color=never", "-p", dir) succeeds with output equal to the status golden's normalized bytes
    - TestDarkBackgroundQueryGate: with fdIsTerminal forced true and out/in = os.Stdout/os.Stdin and environ {TTY_FORCE=1, TERM=xterm-256color}: queryDarkBackground called exactly 1 time and Dark equals the stub's answer (false); with fdIsTerminal true for out only: 0 calls, Dark=true; with a bytes.Buffer out: 0 calls, Dark=true; with Styled false (never): 0 calls
  </behavior>
  <action>
    RED first: commit `colorflag_test.go` plus a compiling placeholder `colorflag.go` (type/func declarations returning zero values — `rewriteEnviron` returns its input, `resolveColorFrom` returns `colorMode{}`, `addColorFlag` registers nothing) as `test(04-03): add failing resolver matrix, environ-rewrite, invalid-flag and background-gate tests`. Observe every named test `--- FAIL` on an assertion (not a compile error); paste the lines into the SUMMARY.

    GREEN — `internal/cli/colorflag.go` (D-09, D-10, D-11, CLI-02, CLI-03, CLI-04):
    - `const colorFlagName = "color"`; `type colorChoice string` implementing `pflag.Value`: `String()`, `Type()` returns `"string"`, `Set(v)` accepts exactly `auto`, `always`, `never` and otherwise returns `fmt.Errorf("--color must be \"auto\", \"always\" or \"never\" (got %q)", v)` (parseLocationFlag's wording shape). Consts `colorAuto`, `colorAlways`, `colorNever`.
    - `func addColorFlag(root *cobra.Command)`: `choice := colorAuto; root.PersistentFlags().Var(&choice, colorFlagName, "colour output: auto (detect), always, or never")`. Called from `newRootCmd()` right after `root := &cobra.Command{…}`. No short form (`-c` is not one of D-12's four).
    - `func colorChoiceOf(cmd *cobra.Command) colorChoice`: `cmd.Flags().Lookup(colorFlagName)`; nil → `colorAuto` (standalone commands in tests); else `colorChoice(f.Value.String())`.
    - `func rewriteEnviron(choice colorChoice, environ []string) []string` — copies the slice, then per the `<behavior>` rules. Implement key matching on the `KEY=` prefix (`strings.HasPrefix(e, "NO_COLOR=")` etc.). Comment block (D-00/D-09): cite `github.com/charmbracelet/colorprofile v0.4.3 env.go` — Detect's documented rules verbatim from RESEARCH's Code Examples; the ParseBool gating of NO_COLOR/CLICOLOR/CLICOLOR_FORCE (`envNoColor`, `cliColor`, `cliColorForced`); why `never` drops CLICOLOR_FORCE (A1: `colorProfile` returns on `cliColorForced` before honouring NO_COLOR when `!isatty`); why `CLICOLOR=0` is normalized (A2: colorprofile only floors on a truthy CLICOLOR, never lowers on a falsy one); and the note that Node's `FORCE_COLOR` convention is the opposite so nobody "fixes" the precedence (CONTEXT Specific Ideas).
    - `type colorMode struct { Styled bool; Profile colorprofile.Profile; Dark bool }` and `func (m colorMode) Writer(w io.Writer) io.Writer { return &colorprofile.Writer{Forward: w, Profile: m.Profile} }`.
    - Seams (install.go idiom): `var fdIsTerminal = func(f *os.File) bool { return term.IsTerminal(int(f.Fd())) }` (golang.org/x/term, already imported by status.go) and `var queryDarkBackground = func(in, out *os.File) bool { return lipgloss.HasDarkBackground(in, out) }` — the ONE lipgloss import outside `present`, justified in its comment by D-11 (the query is a terminal side effect the RunE boundary owns, not styling).
    - `func resolveColorFrom(choice colorChoice, environ []string, out io.Writer, in io.Reader) colorMode`: `env := rewriteEnviron(choice, environ)`; `p := colorprofile.Detect(out, env)`; `isTTY := p > colorprofile.NoTTY`; `noColor := ""`; `if p <= colorprofile.ASCII { noColor = "1" }`; `styled := present.ChoosePresentation(isTTY, noColor)`; `dark := true`; if styled: `of, ok1 := out.(*os.File)`, `inf, ok2 := in.(*os.File)`; if both ok and `fdIsTerminal(of) && fdIsTerminal(inf)` → `dark = queryDarkBackground(inf, of)` (exactly once). Return `colorMode{styled, p, dark}`. Comment: D-11 corrected gate (both TTYs; 2 s worst case cited from lipgloss v2.0.5 `query.go` `defaultQueryTimeout`).
    - `func resolveColor(cmd *cobra.Command) colorMode { return resolveColorFrom(colorChoiceOf(cmd), os.Environ(), cmd.OutOrStdout(), cmd.InOrStdin()) }` — the single environment read.
    - `func resolveColorStderr(cmd *cobra.Command) colorMode`: same rewrite, `Detect(cmd.ErrOrStderr(), env)`, NEVER queries the background (Dark true) — documented as plan 07's serve-banner entry point; `progress_cli.go`'s own stderr resolver stays untouched (out of scope by CONTEXT domain).

    Wire `status.go`: replace the inline `present.ChoosePresentation(term.IsTerminal(int(os.Stdout.Fd())), os.Getenv("NO_COLOR"))` with `mode := resolveColor(cmd); if mode.Styled { return present.RenderStatus(result, start, mode.Writer(cmd.OutOrStdout())) }` — RenderStatus keeps its CURRENT signature in this task (Task 2 adds the palette parameter); the `--json` early return and the plain `RenderStatusText` fallback are untouched. Drop the now-unused `os`/`term` imports from status.go if nothing else uses them.

    Extend `test/integration/status_files_plain_test.go` with a new `TestStatusColorFlagRealBinary`: `status --path dir --color=always` piped → stdout contains `\x1b[`; `status --path dir --color=never` → contains none and equals the bare-pipe output; `status --path dir --color=bogus` → exit 1 and stderr contains `auto`, `always`, `never`. (RED-first too: it is in the same `test(04-03):` commit.)

    Update `docs/CLI-REFERENCE.md`? NO — plan 08 regenerates it once through the drift gate; until then `task docs:cli:drift` is expectedly RED (record this in the SUMMARY as the scheduled window, the 03-01 precedent), and `TestEveryRegisteredFlagIsAccountedFor` will report `--color` as unaccounted — also expected until plan 08 (note it; do not add an allowlist line).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log --format=%s -20 | rg -q '^test\(04-03\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/ && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestRewriteEnviron$|TestResolveColorMatrix$|TestColorFlagInvalidValue$|TestDarkBackgroundQueryGate$|TestPlainGolden$|TestNoColorNonTTYRegression$' -v 2>&1 | tee /tmp/04-03-t1.txt | tail -3; rg -q '^ok\s' /tmp/04-03-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/04-03-t1.txt && test "$(rg -c '^--- PASS: Test(RewriteEnviron|ResolveColorMatrix|ColorFlagInvalidValue|DarkBackgroundQueryGate|PlainGolden|NoColorNonTTYRegression) ' /tmp/04-03-t1.txt)" = "6" && test "$(rg -c '^\s+--- PASS: TestResolveColorMatrix/' /tmp/04-03-t1.txt)" -ge 13 && GOTOOLCHAIN=go1.26.6 go build -o /tmp/04-03-bin ./cmd/codegraph && F=$(mktemp -d) && cp -R internal/indexer/testdata/gofixture/. "$F/" && /tmp/04-03-bin init "$F" >/dev/null 2>&1 && /tmp/04-03-bin status -p "$F" --color=always | rg -q $'\x1b\\[' && ! /tmp/04-03-bin status -p "$F" --color=never | rg -q $'\x1b' && ! NO_COLOR=1 /tmp/04-03-bin status -p "$F" | rg -q $'\x1b' && ! /tmp/04-03-bin status -p "$F" | rg -q $'\x1b' && (/tmp/04-03-bin status -p "$F" --color=bogus >/dev/null 2>/tmp/04-03-bogus.txt; test $? -eq 1) && rg -q 'auto' /tmp/04-03-bogus.txt && rg -q 'always' /tmp/04-03-bogus.txt && rg -q 'never' /tmp/04-03-bogus.txt && rm -rf "$F" && test -z "$(rg -l 'os\.Getenv|os\.Environ|term\.IsTerminal|colorprofile' internal/cli/present/*.go | rg -v '_test\.go')" && echo "tracer green: matrix (13+ rows), goldens 28/28, real binary styled only under --color=always on a pipe, bogus exits 1"</automated>
    <fails_when>non-zero exit; no "test(04-03):" commit in the last 20; build/gofmt/vet red; "--- FAIL"/"FAIL" in /tmp/04-03-t1.txt; fewer than the 6 named top-level PASS lines or fewer than 13 matrix subtest PASS lines; the real binary emitting no ESC under --color=always on a pipe, or ANY ESC under --color=never / NO_COLOR=1 / bare pipe; --color=bogus not exiting 1 or its stderr lacking one of auto/always/never; or a non-test file under internal/cli/present reading env/fd/colorprofile</fails_when>
  </verify>
  <acceptance_criteria>
    - `internal/cli/colorflag.go` contains `func resolveColor(cmd *cobra.Command) colorMode`, `func resolveColorFrom(`, `func rewriteEnviron(`, `func addColorFlag(`, `func resolveColorStderr(`, `var fdIsTerminal`, `var queryDarkBackground`, the literal `colorprofile.Detect(` exactly once in `resolveColorFrom` and once in `resolveColorStderr`, and a comment containing `v0.4.3`
    - `internal/cli/root.go` contains `addColorFlag(root)`; `internal/cli/status.go` contains `resolveColor(cmd)` and `mode.Writer(` and no longer contains `os.Getenv("NO_COLOR")`
    - `internal/cli/present/tty.go` is unchanged: `B=$(git log --format=%H -E --grep='^test\(04-03\): ' | tail -1) && git diff "$B^..HEAD" -- internal/cli/present/tty.go` prints nothing
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -run 'TestResolveColorMatrix' -v -count=1` shows ≥13 passing subtests including names for `never/pipe/CLICOLOR_FORCE` and `auto/tty/CLICOLOR=0`
    - `GOTOOLCHAIN=go1.26.6 go test ./test/integration/ -run TestStatusColorFlagRealBinary -count=1` exits 0
    - `TestPlainGolden`/`TestNoColorNonTTYRegression` still 28/28 green
    - The `test(04-03):` commit touches only `colorflag_test.go`, `status_files_plain_test.go` and the placeholder `colorflag.go`
  </acceptance_criteria>
  <done>`codegraph status` is styled end to end through the shared resolver and colorprofile.Writer, plain output byte-identical to the goldens, `--color` validated, background query gated and counted — the architecture proven on one verb and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: present.Palette (seven roles, LightDark) folding the three style vars; status/files take the palette</name>
  <files>internal/cli/present/palette.go, internal/cli/present/palette_test.go, internal/cli/present/styles.go, internal/cli/present/status.go, internal/cli/present/files.go, internal/cli/present/status_test.go, internal/cli/present/files_test.go, internal/cli/present/ansistrip_test.go, internal/cli/status.go, internal/cli/files.go</files>
  <read_first>
    - internal/cli/present/styles.go (the three vars and the package doc — keep the doc, delete the vars)
    - internal/cli/present/status.go and files.go (every `headerStyle`/`labelStyle`/`sectionStyle` use → palette field)
    - internal/cli/present/status_test.go, files_test.go (every RenderStatus/RenderFiles call gains a palette argument — `NewPalette(true)` in tests)
    - internal/cli/present/progress.go (progressStyle is NOT one of the seven roles and progress is out of scope — leave it)
    - internal/cli/files.go (the inline ChoosePresentation at the styled branch → resolver + Writer + palette, mirroring status.go)
    - .planning/phases/04-cli-glow-up/04-CONTEXT.md (D-05, D-06)
    - /Users/sean/go/pkg/mod/charm.land/lipgloss/v2@v2.0.5/color.go (LightDark, Color) and style.go (Bold/Faint/Foreground/GetForeground)
  </read_first>
  <behavior>
    - TestNewPaletteRolesFollowDark: for each of the seven roles, `NewPalette(true).Style(role).GetForeground()` equals `lipgloss.Color(paletteHex[role].dark)` and `NewPalette(false).Style(role).GetForeground()` equals `lipgloss.Color(paletteHex[role].light)`; Header/Warning/Error styles report `GetBold() == true`, Label reports `GetFaint() == true`; the seven light hexes are pairwise distinct and the seven dark hexes are pairwise distinct (our data, read back through lipgloss getters — not a render test)
    - existing RenderStatus/RenderFiles tests keep passing with `NewPalette(true)` passed in (content assertions unchanged)
  </behavior>
  <action>
    RED first: commit `palette_test.go` plus a compiling placeholder `palette.go` (types, `NewPalette` returning zero Palette, `paletteHex` empty) as `test(04-03): add failing palette role/hue tests`; observe `--- FAIL: TestNewPaletteRolesFollowDark`; paste into the SUMMARY.

    GREEN — `internal/cli/present/palette.go` (D-05, D-06, CLI-04):
    - `type Role int` with `RoleHeader, RoleLabel, RoleValue, RolePath, RoleCount, RoleWarning, RoleError` (iota, in that order).
    - `var paletteHex = map[Role]struct{ light, dark string }{…}` — Claude's Discretion, subject to the D-06 UAT; initial pairs (light-background hue / dark-background hue): Header `#005f87` / `#5fd7ff`; Label `#586e75` / `#93a1a1`; Value `#073642` / `#eee8d5`; Path `#5f5faf` / `#af87ff`; Count `#875f00` / `#ffd75f`; Warning `#af5f00` / `#ffaf00`; Error `#af0000` / `#ff5f5f`. Comment: chosen for legibility on Solarized Light and a dark theme; truecolor hex, downsampled by colorprofile at the RunE boundary (D-10), never inside present.
    - `type Palette struct { Header, Label, Value, Path, Count, Warning, Error lipgloss.Style }`; `func NewPalette(dark bool) Palette` builds ONE `ld := lipgloss.LightDark(dark)` and sets each field `lipgloss.NewStyle().Foreground(ld(lipgloss.Color(light), lipgloss.Color(dark)))`, adding `.Bold(true)` to Header, Warning, Error and `.Faint(true)` to Label. `func (p Palette) Style(r Role) lipgloss.Style` switches on the role. No package-level style var, no setter.
    - `styles.go`: delete the `var (…)` block (headerStyle, labelStyle, sectionStyle); keep the package doc comment, updated to say the palette is constructed per call by `NewPalette` (D-05) and that `present` still never reads env/fd.
    - `status.go`: `RenderStatus(r query.StatusResult, projectPath string, pal Palette, w io.Writer) error`; `headerStyle.Render("CodeGraph Status")` → `pal.Header.Render(…)`; `sectionStyle.Render(header)` in `writeBreakdownText` → `pal.Header` (section headings fold into Header — the underline is dropped, there is no eighth role); `labelStyle` → `pal.Label`; thread `pal` through `writeStatLine`/`writeBreakdownText`; render the advisory labels (`Pending Changes:`, `Reindex recommended:`) through `pal.Warning` and the numbers written by `writeStatLine`/`writeBreakdownText` through `pal.Count` (values stay data-only otherwise). `files.go`: `RenderFiles(r query.FilesResult, pal Palette, w io.Writer) error`; directory names → `pal.Path`, file names → `pal.Path`, language → `pal.Label`.
    - Update `status_test.go`/`files_test.go` call sites to pass `NewPalette(true)`; keep their existing assertions.
    - Add `internal/cli/present/ansistrip_test.go` (package `present`, test-only): `var sgrSequence = regexp.MustCompile("\\x1b\\[[0-9;]*m")` and `func stripANSI(s string) string { return sgrSequence.ReplaceAllString(s, "") }` — the ONE shared ANSI stripper every later renderer contract test in this package reuses (plans 04/05/06 run in parallel and must not each define it); a local regexp, never `charmbracelet/x/ansi`, so go.mod stays untouched (D-15). Use it in `palette_test.go` for a positive control that `NewPalette(true).Header.Render("x")` contains `\x1b[` and strips back to `x`.
    - `internal/cli/status.go`: `present.RenderStatus(result, start, present.NewPalette(mode.Dark), mode.Writer(cmd.OutOrStdout()))`. `internal/cli/files.go`: replace the inline ChoosePresentation with `mode := resolveColor(cmd); if mode.Styled { return present.RenderFiles(result, present.NewPalette(mode.Dark), mode.Writer(out)) }` — note files.go prints the worktree notice BEFORE the styled branch today; keep that order (the notice is plain text on both paths, plan 04 gives the query verbs a styled `RenderNotice`; files keeps its current placement to stay minimal). Drop unused imports.
    - Commit GREEN as `feat(04-03): shared colour resolver, --color flag and seven-role palette; status and files restyled through colorprofile.Writer` (includes Task 1's GREEN if not yet committed — one feat commit per plan is fine; the RED commits precede it).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/... && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1 2>&1 | tee /tmp/04-03-t2.txt | tail -8; ! rg -q '^(--- FAIL|FAIL)' /tmp/04-03-t2.txt && test "$(rg -c '^ok\s' /tmp/04-03-t2.txt)" -ge 4 && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/ -count=1 -run 'TestNewPaletteRolesFollowDark$' -v 2>&1 | tee /tmp/04-03-pal.txt | tail -2; rg -q -- '--- PASS: TestNewPaletteRolesFollowDark' /tmp/04-03-pal.txt && test "$(rg -c 'RoleHeader|RoleLabel|RoleValue|RolePath|RoleCount|RoleWarning|RoleError' internal/cli/present/palette.go)" -ge 7 && test "$(rg -c '^\s+(headerStyle|labelStyle|sectionStyle)\s*=' internal/cli/present/styles.go)" = "0" && ! rg -q 'headerStyle|labelStyle|sectionStyle' internal/cli/present/status.go internal/cli/present/files.go && rg -q 'func NewPalette\(dark bool\) Palette' internal/cli/present/palette.go && rg -q 'lipgloss\.LightDark\(dark\)' internal/cli/present/palette.go && rg -q 'func RenderStatus\(r query\.StatusResult, projectPath string, pal Palette, w io\.Writer\) error' internal/cli/present/status.go && rg -q 'func RenderFiles\(r query\.FilesResult, pal Palette, w io\.Writer\) error' internal/cli/present/files.go && GOTOOLCHAIN=go1.26.6 go test -race -count=1 ./internal/cli/ ./internal/cli/present/ 2>&1 | tee /tmp/04-03-race.txt | tail -2; ! rg -q 'DATA RACE|FAIL' /tmp/04-03-race.txt && git log --format=%s -30 | rg -q '^feat\(04-03\): ' && test "$(git log --format=%s -30 | rg -c '^test\(04-03\): ')" -ge 2 && echo "palette green, style vars folded, status/files take Palette, race clean, RED commits precede feat"</automated>
    <fails_when>non-zero exit; "--- FAIL"/"FAIL" in the package run or fewer than 4 "ok" package lines; TestNewPaletteRolesFollowDark not PASS; fewer than 7 Role constants; a surviving headerStyle/labelStyle/sectionStyle assignment in styles.go or use in status.go/files.go; the NewPalette/LightDark/RenderStatus/RenderFiles signature greps missing; "DATA RACE" or "FAIL" in the -race run; no "feat(04-03):" commit; or fewer than 2 "test(04-03):" commits</fails_when>
    <human-check>D-06/CLI-04 palette readability (also CLI-02's terminal matrix, both harvested at end of phase): build `/tmp/04-03-bin` and run `codegraph status` and `codegraph files --format tree` in (a) Solarized Light or macOS light Terminal and (b) a dark theme. Confirm header/label/value/path/count/warning/error are visibly distinct and legible in both. Then repeat under `TERM=dumb` (must be plain), `TERM=xterm` (16-colour, no garbled escapes), `TERM=xterm-256color`, `COLORTERM=truecolor`, ideally one run over SSH and one inside tmux — a ~2 s pause before styled output over SSH/tmux is the OSC-11 query timing out (D-11 correction), not a hang; note the observed wall time. `codegraph status --color=always | less -R` shows colour; `codegraph status --color=never` in a terminal is plain.</human-check>
  </verify>
  <acceptance_criteria>
    - `internal/cli/present/palette.go` defines `Role`, the seven `Role…` constants, `Palette` with exactly seven `lipgloss.Style` fields, `NewPalette(dark bool) Palette`, `(Palette).Style(Role)`, and `paletteHex` with seven light/dark hex pairs
    - `internal/cli/present/ansistrip_test.go` defines `func stripANSI(s string) string` and imports no `charmbracelet` package
    - `internal/cli/present/styles.go` contains no `var (` block and no `lipgloss.NewStyle()` call
    - `rg -n 'headerStyle|labelStyle|sectionStyle' internal/cli/present/` matches nothing outside this plan's SUMMARY
    - `RenderStatus`/`RenderFiles` signatures carry `pal Palette`; `internal/cli/status.go` and `files.go` call `present.NewPalette(mode.Dark)`
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1` and `-race` over cli+present are green; `TestPlainGolden` 28/28
    - Commits: ≥2 `test(04-03):` RED commits precede one `feat(04-03):` GREEN commit; no `[ci skip]`
  </acceptance_criteria>
  <done>The seven-role adaptive palette exists with no mutable package styles; status and files render through it; RED-first commits and the feat commit are in place; readability queued for end-of-phase eyes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process environment + `--color` → resolver | user/CI-controlled env vars and a flag value select the styled branch; the flag is a closed enum |
| resolver → user's terminal (OSC-11) | the background query writes to the terminal and puts stdin in raw mode — must never touch a pipe or an agent's stdin |
| repository-derived strings → styled writer | paths/names reach lipgloss-styled output; sanitizeControl remains the CR-01 gate in present |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering (V5 input validation) | `--color` flag (`colorChoice.Set`) | medium | mitigate | closed enum `auto|always|never`; anything else (incl. empty) is a parse-time usage error, exit 1 via main.go's single path; real-binary test |
| T-04-08 | Denial of Service | `lipgloss.HasDarkBackground` (raw-mode stdin, 2 s timeout) | high | mitigate | gated on Styled AND stdout AND stdin being real TTY `*os.File`s; never on a pipe or redirected stdin; `resolveColorStderr` never queries; call-count test rows 1/0/0/0 |
| T-04-09 | Tampering | ANSI on the agent/JSON/piped path | high | mitigate | `--json` early return and plain fallback structurally precede the styled branch; `TestPlainGolden`+`TestNoColorNonTTYRegression` 28/28 byte-identical with zero ESC; integration test on the real binary |
| T-04-10 | Tampering | adversarial path/name strings in styled status/files output | medium | mitigate | `sanitizeControl` retained at every call site in `present/status.go`/`files.go` (CR-01); unchanged this task |
| T-04-11 | Information Disclosure | OSC-11 response parsing | low | accept | third-party (lipgloss v2.0.5); worst case is the wrong light/dark variant — cosmetic; the response only selects between two pre-defined hexes |
| T-04-SC | Tampering | new module installs | low | accept | none in this plan — colorprofile was promoted (already resolved transitively) in plan 02 |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1` green, incl. 28/28 goldens, the ≥13-row matrix, the invalid-flag test and the query-gate call counts
- `GOTOOLCHAIN=go1.26.6 go test ./test/integration/ -run 'TestStatusColorFlagRealBinary|TestStatusFilesPlainByteIdentity' -count=1` green
- `GOTOOLCHAIN=go1.26.6 go test -race -count=1 ./internal/cli/ ./internal/cli/present/` clean
- no env/fd/colorprofile read under `internal/cli/present` non-test files
- `task docs:cli:drift` is expectedly RED from this plan until plan 08 regenerates the reference (scheduled window, recorded in the SUMMARY)
</verification>

<success_criteria>
- CLI-02/CLI-03: one resolver, one Detect, environ rewrite with the D-09 correction and amendments A1/A2, `--color` on root validated, `colorprofile.Writer` at the RunE boundary, present env-blind
- CLI-04: seven-role LightDark palette, background queried at most once and only with both TTYs
- CLI-01/CLI-05: status (tracer) and files styled end to end with the plain path byte-identical to the frozen goldens
</success_criteria>

<output>
Create `.planning/phases/04-cli-glow-up/04-03-SUMMARY.md` when done (paste the `--- FAIL:` RED lines for every new test, the matrix row names, the real-binary ESC checks, and record amendments A1/A2 as decisions for the maintainer).
</output>
