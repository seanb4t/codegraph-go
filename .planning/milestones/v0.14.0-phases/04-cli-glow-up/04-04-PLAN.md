---
phase: 04-cli-glow-up
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - internal/cli/present/results.go
  - internal/cli/present/results_test.go
  - internal/cli/search.go
  - internal/cli/callers.go
  - internal/cli/callees.go
  - internal/cli/impact.go
  - internal/cli/affected.go
autonomous: true
requirements: [CLI-01, CLI-05]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`present/results.go` holds six bespoke renderers over the existing plain-struct seams — `RenderSearch([]query.Location)`, `RenderSearchFull([]*schema.Node)`, `RenderCallers(query.CallersResult)`, `RenderCallees(query.CalleesResult)`, `RenderImpact(query.ImpactResult)`, `RenderAffected(query.AffectedResult)` — each `(…, pal Palette, w io.Writer) error`, plus `RenderNotice(notice string, pal Palette, w io.Writer) error` for the WORK-02 worktree notice; every user/repo-derived string (Name, Kind, FilePath, QualifiedName, Signature, Symbol) passes through `sanitizeControl` before styling; `internal/query` and `internal/mcp` are unchanged (D-08, CLI-01)."
    - "Content contract (the only test we own, D-00): for every renderer and every fixture (0, 1 and N entries), stripping ANSI SGR sequences from the styled output yields EXACTLY the plain text the corresponding RunE prints today — the same lines, same order, same wording, same `%s (%s) %s:%d` shape, same `    QualifiedName  Signature` second line for `--full`, same `X has N caller(s):` / `X calls N callee(s):` / `X impact (depth=D): N node(s), M edge(s)` / `N affected test(s):` / `no test files affected` headers — pinned by `TestRenderResultsStrippedEqualsPlain` against expected strings built with the plain `fmt` formats (never against a lipgloss render). Hue only; no re-layout (D-07's principle applied to the list verbs)."
    - "Role assignment is fixed: symbol/name → Value; `(kind)` → Label; `path:line` → Path; every numeric count in a header (`N caller(s)`, `depth=D`, `N node(s)`, `M edge(s)`, `N affected test(s)`) → Count; the `--full` second line's QualifiedName → Value and Signature → Label; the worktree notice → Warning; `no test files affected` → Label. Whitespace, parentheses, colons and indentation are written unstyled so the stripped bytes equal the plain bytes."
    - "Each of the five RunE sites (search default + `--full`, callers, callees, impact, affected) gains ONLY a styled branch inserted after the `--json` early return (and after `--quiet` for affected): `mode := resolveColor(cmd); if mode.Styled { w := mode.Writer(out); pal := present.NewPalette(mode.Dark); present.RenderNotice(notice, pal, w); return present.RenderX(result, pal, w) }`; the `--json` return, the plain `fmt.Fprintf` loop and the notice's plain print are structurally untouched, so `TestPlainGolden`/`TestNoColorNonTTYRegression` stay 28/28 byte-identical and the golden oracle (`task test:golden`) is unchanged (CLI-05)."
    - "CLI-01 (probe boundary, explicit): the renderers introduce no threshold, limit or truncation of their own — every entry the engine returns is rendered, and the count printed is `len(result.X)` exactly as the plain path computes it; pinned by the 0/1/N fixtures."
    - "CLI-01 (probe adjacency, explicit): two entries with identical Name/Kind/FilePath/StartLine render as two identical lines in engine order — the styled path never merges, dedupes or sorts; pinned by a fixture with a duplicated Location."
    - "CLI-01 (probe empty, explicit): zero results render exactly the plain empty wording (`X has 0 caller(s):` with no entry lines; `no test files affected`; an empty search prints nothing but the notice) — pinned by the 0-entry fixtures."
    - "CLI-01 (probe encoding, explicit): strings are passed through byte-for-byte except `sanitizeControl` (control runes dropped, CR-01) — no width padding, grapheme handling or normalization is added; a fixture with a non-ASCII signature (`(x ∑) → ünïcode`) round-trips through strip == plain."
    - "CLI-01 (probe ordering, explicit): output order is the engine's slice order; the stripped-equals-plain contract over an N=3 fixture in a deliberately non-alphabetical order pins it."
    - statement: "A styled run of each verb from the real binary (`--color=always | cat`) shows hues on the symbol, kind, path and count segments as assigned above — checked by eye once at end of phase alongside the CLI-04 UAT; not golden-frozen (D-06)."
      verification: backstop
  artifacts:
    - "internal/cli/present/results.go — `RenderSearch`, `RenderSearchFull`, `RenderCallers`, `RenderCallees`, `RenderImpact`, `RenderAffected`, `RenderNotice`, `writeLocationLine(b, pal, indent, l query.Location)`, `writeNodeLine(b, pal, n *schema.Node)`"
    - "internal/cli/present/results_test.go — `TestRenderResultsStrippedEqualsPlain` (table over the six renderers × {0,1,N,duplicate,unicode}); uses `stripANSI` from plan 03's `ansistrip_test.go` (not redefined)"
    - "internal/cli/search.go, callers.go, callees.go, impact.go, affected.go — the styled branch per RunE"
  key_links:
    - "`search.go` `--full` branch → `eng.Query` → `present.RenderSearchFull(nodes, pal, w)`; default → `eng.Search` → `present.RenderSearch(locs, pal, w)`; both after the `--json` return and using `resolveColor(cmd)` from plan 03"
    - "`affected.go`: `--json` → `--quiet` (plain, unstyled, one path per line — WR-03, untouched) → styled branch → plain branch"
    - "`present.RenderNotice` ← `query.WorktreeNotice(eng.WorktreeMismatch(ctx))` — the same string the plain path prints, rendered in Warning when non-empty, nothing when empty"
  prohibitions:
    - statement: "MUST NOT change any line's wording, order or count relative to the plain path when styling — the stripped-equals-plain test is the gate; MUST NOT colourise via string manipulation of a pre-rendered markdown/plain string (renderers consume the structs)."
      status: resolved
      verification: test
    - statement: "MUST NOT let an ANSI byte reach stdout on the `--json`, `--quiet` (affected) or non-TTY path — `TestPlainGolden` 28/28 and zero-ESC is the gate; MUST NOT touch `internal/query`, `internal/mcp`, `testdata/golden`, `testdata/wireoracle`."
      status: resolved
      verification: test
    - statement: "MUST NOT import `github.com/charmbracelet/x/ansi` (or any new module) for the test's ANSI stripper — a local regexp keeps go.mod unchanged (D-15 discipline)."
      status: resolved
      verification: test
---

<objective>
Expand the proven tracer to the six list-shaped query verbs: write bespoke `present` renderers for
`search` (default and `--full`), `callers`, `callees`, `impact` and `affected` over their existing
plain structs, pinned by a stripped-styled-equals-plain content contract, and wire each RunE's
styled branch through the plan-03 resolver + `colorprofile.Writer` + palette — leaving every
`--json`, `--quiet` and plain branch structurally untouched.

Purpose: CLI-01 wants every human-output verb through one palette; these six share one shape
(`Name (Kind) Path:Line` lines under a counted header), so one file and one contract test cover
them. The test asserts our content decision (hue only, nothing else changes), never lipgloss's
encoding (D-00).

TDD: `test(04-04):` RED commit (results_test.go + placeholder results.go) before the GREEN
`feat(04-04):`; `--- FAIL:` transcript in the SUMMARY; never `check tdd-red-evidence`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-glow-up/04-CONTEXT.md
@.planning/phases/04-cli-glow-up/04-PATTERNS.md
@.planning/phases/04-cli-glow-up/04-03-SUMMARY.md
@internal/cli/present/files.go
@internal/cli/present/palette.go
@internal/cli/present/sanitize.go
@internal/cli/search.go
@internal/cli/callers.go
@internal/cli/affected.go
@internal/query/search.go
@internal/query/traverse.go
</context>

<interfaces>
<!-- Extracted this session; the plain formats are the contract the styled output must strip back to. -->

From internal/query/search.go:16 and traverse.go:562-590:
```go
type Location struct { Name, Kind, FilePath string; StartLine int32 /* + fields the renderers do not print */ }
type CallersResult struct { Symbol string; Callers []Location }
type CalleesResult struct { Symbol string; Callees []Location }
type ImpactResult struct { Symbol string; Depth, NodeCount, EdgeCount int; Affected []Location }
type AffectedResult struct { AffectedTests []Location /* + input fields */ }
```
(Read the real struct definitions for exact field types before writing the renderers — `StartLine` may be `int32`.)

Plain formats (verbatim from the RunE files, the strip target):
```go
// search default:      fmt.Fprintf(out, "%s (%s) %s:%d\n", l.Name, l.Kind, l.FilePath, l.StartLine)
// search --full:       line 1 as above from *schema.Node, then fmt.Fprintln(out, renderFullLine(n))  // "    " + QualifiedName [+ "  " + Signature]
// callers:             fmt.Fprintf(out, "%s has %d caller(s):\n", result.Symbol, len(result.Callers)); entries "  %s (%s) %s:%d\n"
// callees:             fmt.Fprintf(out, "%s calls %d callee(s):\n", …); entries as above
// impact:              fmt.Fprintf(out, "%s impact (depth=%d): %d node(s), %d edge(s)\n", Symbol, Depth, NodeCount, EdgeCount); entries as above
// affected:            len==0 → "no test files affected\n"; else "%d affected test(s):\n" + entries
// notice:              fmt.Fprint(out, query.WorktreeNotice(mismatch))   // "" when clean
```

From internal/cli/colorflag.go (plan 03):
```go
func resolveColor(cmd *cobra.Command) colorMode      // {Styled bool; Profile colorprofile.Profile; Dark bool}
func (m colorMode) Writer(w io.Writer) io.Writer
```
From internal/cli/present/palette.go (plan 03):
```go
type Palette struct { Header, Label, Value, Path, Count, Warning, Error lipgloss.Style }
func NewPalette(dark bool) Palette
```
</interfaces>

## Artifacts this phase produces

See `04-01-PLAN.md` → "Artifacts this phase produces" (phase-wide list). This plan creates `present/results.go`'s seven exported renderers and `writeLocationLine`/`writeNodeLine`, plus `results_test.go`'s `stripANSI`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Six list renderers + RenderNotice, pinned by stripped-styled == plain</name>
  <files>internal/cli/present/results.go, internal/cli/present/results_test.go</files>
  <read_first>
    - internal/cli/present/files.go (RenderFiles — the flat list-of-entries shape and its sanitizeControl discipline)
    - internal/cli/present/palette.go (the seven roles)
    - internal/cli/present/sanitize.go (sanitizeControl)
    - internal/cli/present/files_test.go (test file conventions in this package)
    - internal/cli/present/ansistrip_test.go (plan 03's shared `stripANSI` — reuse it; do NOT redefine it in results_test.go)
    - internal/cli/search.go (renderFullLine — the exact second-line concatenation to reproduce), callers.go, callees.go, impact.go, affected.go (plain formats)
    - internal/query/search.go (Location), internal/query/traverse.go (CallersResult/CalleesResult/ImpactResult/AffectedResult), internal/schema (Node fields Name/Kind/FilePath/StartLine/QualifiedName/Signature)
  </read_first>
  <behavior>
    - For each renderer R in {RenderSearch, RenderSearchFull, RenderCallers, RenderCallees, RenderImpact, RenderAffected} and each fixture F in {empty, single, three-in-non-alphabetical-order, duplicated-entry, unicode-signature}: `stripANSI(R(F, NewPalette(true), &buf))` == the plain expectation built in the test with the RunE's own fmt formats (copied verbatim into the test as `plainSearch(locs)`, `plainSearchFull(nodes)`, … helpers)
    - RenderNotice("", pal, w) writes nothing; RenderNotice("⚠ …\n", pal, w) writes bytes that strip back to the same notice string
    - a Location whose Name contains "\x1b[31m" and "\n" renders with those control runes dropped (sanitizeControl) — the plain expectation for that row is built from the sanitized name
    - every renderer returns only the io.WriteString error (no validation of its own)
  </behavior>
  <action>
    RED first: commit `results_test.go` + placeholder `results.go` (all seven funcs declared, writing nothing, returning nil) as `test(04-04): add failing stripped-equals-plain tests for the six list renderers`; every subtest must `--- FAIL` on the byte comparison (empty output ≠ expected) — paste the FAIL lines.

    GREEN — `internal/cli/present/results.go` (D-08, CLI-01):
    - `func writeLocationLine(b *strings.Builder, pal Palette, indent string, name, kind, filePath string, line int32)`: writes `indent + pal.Value.Render(sanitizeControl(name)) + " (" + pal.Label.Render(sanitizeControl(kind)) + ") " + pal.Path.Render(sanitizeControl(filePath)+":"+strconv.Itoa(int(line))) + "\n"` — the unstyled separators (` (`, `) `, newline) are what make the strip equal the plain `%s (%s) %s:%d`.
    - `RenderSearch(locs []query.Location, pal Palette, w io.Writer) error`: one `writeLocationLine` per entry, indent "".
    - `RenderSearchFull(nodes []*schema.Node, pal Palette, w io.Writer) error`: per node, line 1 via `writeLocationLine` (indent ""), then line 2 `"    " + pal.Value.Render(sanitizeControl(n.QualifiedName))` and, if `n.Signature != ""`, `"  " + pal.Label.Render(sanitizeControl(n.Signature))`, then "\n" — exactly `renderFullLine`'s concatenation.
    - `RenderCallers(r query.CallersResult, …)`: header `pal.Value.Render(sym) + " has " + pal.Count.Render(strconv.Itoa(len(r.Callers))) + " caller(s):\n"`, then entries with indent "  ". `RenderCallees`: `" calls "` … `" callee(s):\n"`. `RenderImpact`: `sym + " impact (depth=" + Count(D) + "): " + Count(N) + " node(s), " + Count(M) + " edge(s)\n"` then entries. `RenderAffected`: `len == 0` → `pal.Label.Render("no test files affected") + "\n"`; else `Count(N) + " affected test(s):\n"` then entries.
    - `RenderNotice(notice string, pal Palette, w io.Writer) error`: empty → write nothing; else `pal.Warning.Render(strings.TrimRight(sanitizeControl-per-line…))` — simplest faithful form: split on "\n", style each non-empty line with Warning, rejoin with "\n" preserving the trailing newline, so the strip equals the input.
    - Doc comment on the file: consumes plain structs read-only (D-02 precedent), never recomputes counts/order, present stays env-blind.

    `results_test.go`: reuse `stripANSI` from `ansistrip_test.go` (plan 03); plain-expectation helpers mirroring the RunE formats; the table described in `<behavior>`; include the positive control that the STYLED output of the N=3 fixture does contain `\x1b[` (so a renderer that forgot to style would fail too — rule 84d1gfpywd).

    Commit GREEN as part of this plan's `feat(04-04):` (after Task 2, or now — one feat commit per plan is fine).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log --format=%s -20 | rg -q '^test\(04-04\): ' && test -z "$(gofmt -l internal/cli/present/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/present/ && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/ -count=1 -run 'TestRenderResultsStrippedEqualsPlain$' -v 2>&1 | tee /tmp/04-04-t1.txt | tail -3; rg -q '^ok\s' /tmp/04-04-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/04-04-t1.txt && test "$(rg -c '^\s+--- PASS: TestRenderResultsStrippedEqualsPlain/' /tmp/04-04-t1.txt)" -ge 30 && test "$(rg -c '^func Render(Search|SearchFull|Callers|Callees|Impact|Affected|Notice)\(' internal/cli/present/results.go)" = "7" && test "$(rg -c 'sanitizeControl\(' internal/cli/present/results.go)" -ge 6 && ! rg -q 'charmbracelet/x/ansi' internal/cli/present/results_test.go && test "$(rg -c '^func stripANSI\(' internal/cli/present/*_test.go | awk -F: '{s+=$2} END {print s}')" = "1" && test -z "$(git diff --name-only -- go.mod go.sum)" && echo "7 renderers, ≥30 contract subtests green, no new module"</automated>
    <fails_when>non-zero exit; no "test(04-04):" commit; gofmt/vet red; "--- FAIL"/"FAIL" in /tmp/04-04-t1.txt; fewer than 30 passing contract subtests; not exactly 7 `func RenderX(` declarations; fewer than 6 sanitizeControl call sites; the test importing charmbracelet/x/ansi; more than one `func stripANSI(` across the package's test files; or go.mod/go.sum modified</fails_when>
  </verify>
  <acceptance_criteria>
    - `internal/cli/present/results.go` declares exactly `RenderSearch`, `RenderSearchFull`, `RenderCallers`, `RenderCallees`, `RenderImpact`, `RenderAffected`, `RenderNotice` with `pal Palette, w io.Writer` parameters
    - `internal/cli/present/results_test.go` contains `func TestRenderResultsStrippedEqualsPlain(` and does not redefine `stripANSI` (it lives in `ansistrip_test.go`) with ≥30 subtests including names containing `empty`, `duplicate` and `unicode`, and a positive-control assertion that styled N=3 output contains `\x1b[`
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/ -count=1` exits 0
    - The `test(04-04):` commit precedes the `feat(04-04):` commit and touches only `results_test.go` and the placeholder `results.go`
  </acceptance_criteria>
  <done>Six list renderers plus the notice renderer exist, each proven to strip back to the plain bytes over 0/1/N/duplicate/unicode fixtures, RED-first.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the five RunE styled branches; goldens and real binary prove both paths</name>
  <files>internal/cli/search.go, internal/cli/callers.go, internal/cli/callees.go, internal/cli/impact.go, internal/cli/affected.go</files>
  <read_first>
    - internal/cli/status.go (plan 03's wired shape — copy it exactly)
    - internal/cli/search.go (two human branches: --full and default, each after its own --json return; the notice print sits first in each)
    - internal/cli/affected.go (the --json → --quiet → human order; the WR-03 quiet branch must stay plain and unstyled)
    - internal/cli/callers.go, callees.go, impact.go
    - internal/cli/present/results.go (Task 1's signatures)
    - internal/cli/plain_golden_test.go (the 28 cases this must keep byte-identical)
  </read_first>
  <action>
    In each RunE, immediately AFTER the `--json` early return (and, in affected.go, after the `--quiet` return) and BEFORE the plain notice print, insert the styled branch:

    `mode := resolveColor(cmd)` then `if mode.Styled { w := mode.Writer(out); pal := present.NewPalette(mode.Dark); if err := present.RenderNotice(query.WorktreeNotice(eng.WorktreeMismatch(cmd.Context())), pal, w); err != nil { return err }; return present.RenderX(result, pal, w) }` — with `RenderSearch(locs …)` / `RenderSearchFull(nodes …)` (search's two branches each get their own), `RenderCallers(result …)`, `RenderCallees(result …)`, `RenderImpact(result …)`, `RenderAffected(result …)`. Add the `present` import where missing. The existing plain `fmt.Fprint(out, query.WorktreeNotice(…))` + `fmt.Fprintf` loops stay exactly as they are below the branch (they are the frozen plain path).

    Do not touch `--json` marshalling, `--quiet`, flag registrations or the notice's plain placement. Run the full package tests and the real-binary checks. Commit `feat(04-04): render search/callers/callees/impact/affected through the palette (hue only; plain path byte-identical)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/... && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1 2>&1 | tee /tmp/04-04-t2.txt | tail -8; ! rg -q '^(--- FAIL|FAIL)' /tmp/04-04-t2.txt && test "$(rg -c '^ok\s' /tmp/04-04-t2.txt)" -ge 4 && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestPlainGolden$|TestNoColorNonTTYRegression$' -v 2>&1 | tee /tmp/04-04-golden.txt | tail -2; test "$(rg -c '^\s+--- PASS: TestPlainGolden/' /tmp/04-04-golden.txt)" -ge 28 && GOTOOLCHAIN=go1.26.6 go build -o /tmp/04-04-bin ./cmd/codegraph && F=$(mktemp -d) && cp -R internal/indexer/testdata/gofixture/. "$F/" && /tmp/04-04-bin init "$F" >/dev/null 2>&1 && n=0 && for args in "search Alpha" "search Alpha --full" "callers Alpha" "callees Alpha" "impact Alpha" "affected pkga/pkga.go"; do /tmp/04-04-bin $args -p "$F" --color=always > /tmp/04-04-styled.txt; /tmp/04-04-bin $args -p "$F" > /tmp/04-04-plain.txt; test -s /tmp/04-04-plain.txt || { echo "EMPTY plain for: $args"; exit 1; }; rg -q $'\x1b\\[' /tmp/04-04-styled.txt || { echo "NO ESC under --color=always for: $args"; exit 1; }; ! rg -q $'\x1b' /tmp/04-04-plain.txt || { echo "ESC on plain pipe for: $args"; exit 1; }; cmp -s /tmp/04-04-plain.txt <(perl -pe 's/\e\[[0-9;]*m//g' /tmp/04-04-styled.txt) || { echo "STRIPPED STYLED != PLAIN for: $args"; exit 1; }; n=$((n+1)); done; /tmp/04-04-bin affected pkga/pkga.go -p "$F" --quiet --color=always > /tmp/04-04-quiet.txt; ! rg -q $'\x1b' /tmp/04-04-quiet.txt && rm -rf "$F" && test -z "$(git diff --name-only -- internal/query internal/mcp testdata/golden testdata/wireoracle)" && git log -1 --format=%s | rg -q '^feat\(04-04\): ' && echo "5 verbs styled under --color=always, plain pipes ESC-free, stripped styled == plain on $n invocations, quiet stays plain, goldens 28/28"</automated>
    <fails_when>non-zero exit; "--- FAIL"/"FAIL" in the package run or fewer than 4 "ok" lines; fewer than 28 golden subtests passing; any of the six real-binary invocations printing nothing on the plain pipe, no ESC under --color=always, any ESC on the bare pipe, or a stripped-styled/plain mismatch; ESC in `affected --quiet --color=always` output; a diff under internal/query, internal/mcp, testdata/golden or testdata/wireoracle; or HEAD not a "feat(04-04):" commit</fails_when>
  </verify>
  <acceptance_criteria>
    - `internal/cli/search.go` contains two `resolveColor(cmd)` calls (the `--full` and default branches) and both `present.RenderSearchFull(` and `present.RenderSearch(`; callers.go/callees.go/impact.go/affected.go each contain one `resolveColor(cmd)` and their `present.RenderX(` call
    - In `affected.go` the `if quiet {` block precedes `resolveColor(cmd)` (the quiet branch is never styled)
    - Every plain `fmt.Fprintf(out, "%s (%s) %s:%d\n"` / `"  %s (%s) %s:%d\n"` loop and every plain `fmt.Fprint(out, query.WorktreeNotice(` remain present (grep count unchanged versus the commit before this plan's RED commit: `B=$(git log --format=%H -E --grep='^test\(04-04\): ' | tail -1)` and compare `git show "$B^":<file> | rg -c <format>` with the working-tree count for each file)
    - `TestPlainGolden` and `TestNoColorNonTTYRegression` 28/28; the real-binary loop passes 6/6 with stripped styled == plain
    - `B=$(git log --format=%H -E --grep='^test\(04-04\): ' | tail -1) && git diff --name-only "$B^..HEAD" -- internal/query internal/mcp` is empty
  </acceptance_criteria>
  <done>search, callers, callees, impact and affected render through the palette on a TTY (or `--color=always`) and stay byte-identical to their goldens on every plain path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| indexed repository → styled terminal output | symbol names, kinds, paths, qualified names and signatures are repo-derived and may carry control bytes |
| styled branch ↔ `--json`/`--quiet`/plain branches | the agent/scripting paths must never see an escape byte |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Tampering (terminal escape injection) | present/results.go renderers | high | mitigate | every repo-derived string passes `sanitizeControl` before `Style.Render` (CR-01); the contract test includes a control-byte fixture |
| T-04-13 | Tampering | `--json`, `affected --quiet` and non-TTY paths | high | mitigate | styled branch inserted strictly after those returns; 28/28 goldens + zero-ESC; real-binary `--quiet --color=always` ESC-free check |
| T-04-14 | Information Disclosure | styled vs plain content divergence | medium | mitigate | stripped-styled == plain contract test per renderer and per fixture; real-binary cmp over six invocations |
| T-04-SC | Tampering | new module installs | low | accept | none — the test's ANSI stripper is a local regexp; go.mod unchanged (verified in Task 1's gate) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1` green; goldens 28/28
- real binary: six invocations styled under `--color=always`, ESC-free on a bare pipe, `perl`-stripped styled == plain via `cmp`
- `git diff --name-only -- internal/query internal/mcp testdata/golden testdata/wireoracle` empty
</verification>

<success_criteria>
- CLI-01: search (both shapes), callers, callees, impact, affected render through `present` with the shared palette, hue only
- CLI-05: plain, `--json` and `--quiet` paths byte-identical; golden/wire oracles untouched
</success_criteria>

<output>
Create `.planning/phases/04-cli-glow-up/04-04-SUMMARY.md` when done (paste the RED `--- FAIL` lines and the real-binary loop's 6/6 result).
</output>
