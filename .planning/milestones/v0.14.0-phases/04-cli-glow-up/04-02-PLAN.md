---
phase: 04-cli-glow-up
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/phases/04-cli-glow-up/04-FANG-VERDICT.md
  - .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md
  - internal/cli/present/archtest/import_graph_test.go
  - go.mod
  - go.sum
  - internal/cli/root.go
  - cmd/codegraph/main.go
autonomous: true
requirements: [CLI-08, GRD-13]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`04-FANG-VERDICT.md` exists in the phase directory, committed BEFORE any go.mod change, renderer, palette, resolver or help template, and records a single `**Verdict:** adopted` or `**Verdict:** declined` line derived mechanically from four conjunctive criteria, each with its evidence transcript tail: (1) `test/wireoracle` green under the wrapped `Execute()` — `GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1` `ok` line pasted (D-01); (2) zero new `govulncheck ./...` findings versus the 2026-09-17 baseline AND `TestCharmCgoClosure` green over the widened tree (D-02); (3) `TestRenamedStubsPrintExactlyOnce` (real binary) green with `cmd/codegraph/main.go` no longer printing — the stub's two lines reach stderr exactly once (D-03); (4) composition read operationally per the D-04 research correction: with the candidate call set `fang.Execute(ctx, newRootCmd(), fang.WithoutManpage(), fang.WithVersion(versionLine()))` — `WithoutCompletions()` deliberately NOT called because it sets `CompletionOptions.DisableDefaultCmd` and deletes cobra's own `completion` command GoReleaser's `generate_completions_from_executable` needs — the hidden `codegraph man <dir>` still writes pages and `codegraph completion bash|zsh|fish` still emit scripts (CLI-08, D-04)."
    - "The verdict additionally records, as information not gates: the list of modules fang would add to `go.mod` (D-02, no count assertion); `codegraph --version` output identity before/after the wrap; whether `--color=never` governs fang's help/error chrome (expected: it does not — fang.go v2.0.1 lines 131/174 read `os.Environ()` directly; plan 08's adopted branch mirrors the flag into the process env); that `TestPlainGolden`/`TestNoColorNonTTYRegression` stay green under the wrap; and the observed TTY-stderr shape of a stub error under fang (`err.Error()+\".\"` inside a styled box — cosmetic, non-TTY is byte-identical). D-04."
    - "If adopted, `cli.Execute()` in `root.go` becomes the fang wrap exactly as above (a `context.Background()` context; no `WithoutCompletions()`; no `WithTheme` — deprecated at v2.0.1), `cmd/codegraph/main.go` only `os.Exit(1)`s on error (fang printed it once), and `NewRootCmd()` stays byte-identical so `tools/clidoc` and `cli_reference_test.go` see the same tree. If declined, `root.go`, `main.go`, `go.mod` and `go.sum` carry NO fang trace and plan 08 hand-rolls help (D-04, D-14)."
    - "`internal/cli/present/archtest/import_graph_test.go`'s `forbiddenImportPaths` literal list is replaced by prefix matching on `charm.land/` and `github.com/charmbracelet/` (both vanity roots — `colorprofile` and `x/ansi` live under the second), the match loop iterating every `pkg.Imports` key with `strings.HasPrefix` and reporting EVERY hit via `t.Errorf` (never first-hit return); `charmImporterProbePath` (`charm.land/lipgloss/v2`, exact) and `assertCharmImporterExists` are unchanged as the positive control; `charm_cgo_test.go`'s `charmClosurePathPrefix` is untouched (D-15, GRD-13)."
    - "The prefix widening lands in the SAME commit as the phase's go.mod change(s): `github.com/charmbracelet/colorprofile v0.4.3` promoted from `// indirect` to the direct require block (a hand edit — `go mod tidy` is blocked on this branch by the pre-existing tree-sitter-swift resolution failure, STATE.md) and, if adopted, the `charm.land/fang/v2 v2.0.1` require plus its `go.sum` lines (D-15 commit discipline, recorded in 04-MUTATION-LOG.md, not tested — D-00)."
    - "`04-MUTATION-LOG.md` gains Family (c): a planted `import _ \"github.com/charmbracelet/colorprofile\"` in an UNTRACKED file under a guarded package (`internal/query/zz_planted_charm_test_probe.go`, no build tag) turns `TestNoCharmInServeReachablePackages` RED naming `internal/query` and `github.com/charmbracelet/colorprofile`; removed; `git status --porcelain internal/query` empty. If adopted, Family (d) does the same with `charm.land/fang/v2` (D-15, GRD-13)."
    - "GRD-13 (probe adjacency, explicit): the prefixes end in `/`, so `charm.land/lipgloss` (the wrong v1 module, no `/v2`), `charm.land/lipgloss/v2` and any future `charm.land/<x>` are all caught, while a hypothetical `charm.landing/…` is not; the probe path stays the exact `/v2` literal."
    - "GRD-13 (probe empty, explicit): a guarded package with zero third-party imports passes trivially — vacuity is prevented by `assertCharmImporterExists`, which still requires at least one module package importing `charm.land/lipgloss/v2`."
    - "GRD-13 (probe ordering, explicit): map iteration order over `pkg.Imports` is irrelevant because every forbidden import is reported with `t.Errorf` and the test only fails after the full walk; Family (c)'s transcript shows the package AND the import path in the message."
    - "CLI-08 (probe unclassified, surfaced assumption): the four D-01…D-04 criteria are read operationally per the research correction — the shipped call set omits `WithoutCompletions()`; if a future fang release adds its own completion command the composition criterion must be re-read. Recorded in the verdict, not proven beyond v2.0.1."
    - statement: "The spike's govulncheck comparison is against the exact 2026-09-17 baseline (`0 vulnerabilities in called code; 3 in imported-but-uncalled packages, pre-existing`) — the executor pastes both outputs so a reviewer can confirm 'zero new' rather than trusting the word."
      verification: backstop
  artifacts:
    - ".planning/phases/04-cli-glow-up/04-FANG-VERDICT.md — four criteria with evidence, information section, the verdict line, the shipped call set"
    - "internal/cli/present/archtest/import_graph_test.go — `forbiddenImportPathPrefixes = []string{\"charm.land/\", \"github.com/charmbracelet/\"}`, prefix walk in `TestNoCharmInServeReachablePackages`"
    - "go.mod — colorprofile direct; fang/v2 only if adopted"
    - "internal/cli/root.go — `Execute()` fang wrap (adopted only); cmd/codegraph/main.go — no print (adopted only)"
    - ".planning/phases/04-cli-glow-up/04-MUTATION-LOG.md — Family (c) (+ (d) if adopted)"
  key_links:
    - "`cmd/codegraph/main.go` → `cli.Execute()` → (adopted) `fang.Execute(ctx, newRootCmd(), …)` → `root.ExecuteContext(ctx)`; fang's `DefaultErrorHandler` prints `err.Error()` once on a non-TTY stderr → `TestRenamedStubsPrintExactlyOnce` is the D-03 proof"
    - "`test/wireoracle` spawns the real binary via os/exec → the wrapped `Execute()` is on the path of every one of the 38 frozen transcripts → `task test:wireoracle` green is the D-01 proof"
    - "`import_graph_test.go` prefix walk ← `go.mod` change in the same commit ← Family (c)/(d) planted-import RED"
  prohibitions:
    - statement: "MUST NOT land any renderer, palette, `--color` flag, `SetHelpFunc` or help template in this plan — the verdict commit precedes all of them (CLI-08); MUST NOT adopt fang partially (if any criterion fails, no fang line reaches go.mod, root.go or main.go)."
      status: resolved
      verification: test
    - statement: "MUST NOT call `fang.WithoutCompletions()` in the shipped call set, and MUST NOT widen `charm_cgo_test.go`'s `charmClosurePathPrefix` (a separate, narrower guard D-02 only requires to stay green)."
      status: resolved
      verification: test
    - statement: "MUST NOT edit `internal/query`, `internal/mcp`, `test/wireoracle` or `testdata/wireoracle` — the planted probe files are untracked and deleted before any commit."
      status: resolved
      verification: test
---

<objective>
Run the fang spike against the pinned candidate (`charm.land/fang/v2@v2.0.1`) exactly as the
four conjunctive D-01…D-04 criteria define it — with the D-04 research correction applied (no
`WithoutCompletions()`) — record the verdict in `04-FANG-VERDICT.md` and commit that file alone;
then land the one D-15 commit: the `present` archtest widened to prefix matching on both charm
vanity roots, `colorprofile` promoted to a direct requirement, and (only if adopted) the fang
require, the `Execute()` wrap and the `main.go` change — each newly-required module proven RED
against a planted import in `04-MUTATION-LOG.md`.

Purpose: CLI-08 makes the verdict the gate every renderer waits on (ROADMAP: "hand-rolling before
the verdict is the out-of-scope entry it names"); GRD-13 requires the denylist to catch every
charm-family path added this milestone, and the only honest moment to widen it is the commit that
changes `go.mod`. The spike is evidence-carrying so the decision is auditable without re-running.

Output: commit 1 `docs(04-02): record fang verdict` (verdict file only); commit 2 `feat(04-02): …`
(archtest prefix + go.mod promotion [+ fang wrap]) with the mutation log's Family (c) [+ (d)].
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-glow-up/04-CONTEXT.md
@.planning/phases/04-cli-glow-up/04-RESEARCH.md
@.planning/phases/04-cli-glow-up/04-PATTERNS.md
@.planning/phases/04-cli-glow-up/04-MUTATION-LOG.md
@internal/cli/root.go
@cmd/codegraph/main.go
@internal/cli/present/archtest/import_graph_test.go
@internal/cli/present/archtest/charm_cgo_test.go
@test/integration/renamed_stubs_test.go
@go.mod
</context>

<interfaces>
<!-- Verified this planning session from the module cache; cite, do not re-derive. -->

From charm.land/fang/v2@v2.0.1/fang.go:
```go
func Execute(ctx context.Context, root *cobra.Command, options ...Option) error
func WithoutManpage() Option        // fang adds its own hidden "man" only when manpages==true
func WithoutCompletions() Option    // sets root.CompletionOptions.DisableDefaultCmd = true — REMOVES cobra's completion cmd; do not call
func WithVersion(version string) Option
func WithErrorHandler(handler ErrorHandler) Option
// Execute: root.SetHelpFunc(helpFunc) unconditionally; on error: opts.errHandler(colorprofile.NewWriter(root.ErrOrStderr(), os.Environ()), styles, err) — prints err.Error() exactly once
```

From charm.land/fang/v2@v2.0.1/help.go:
```go
func DefaultErrorHandler(w io.Writer, styles Styles, err error) // non-TTY stderr: fmt.Fprintln(w, err.Error()); TTY: styled box, err.Error()+"."
// helpFn renders c.Groups() titles via evalGroups — cobra groups ARE honoured
```

From internal/cli/root.go (current):
```go
func Execute() error { return newRootCmd().Execute() }
func NewRootCmd() *cobra.Command { return newRootCmd() }   // clidoc entry — must stay byte-identical
func versionLine() string  // version.go:45 — "%s (commit %s, built %s)"
```
</interfaces>

## Artifacts this phase produces

See `04-01-PLAN.md` → "Artifacts this phase produces" (the phase-wide list; this plan creates `04-FANG-VERDICT.md`, `forbiddenImportPathPrefixes`, the go.mod promotion and — adopted only — the fang wrap in `Execute()` and `applyColorFlagToProcessEnv`'s later home).

<tasks>

<task type="auto">
  <name>Task 1: Run the fang spike in the working tree, record 04-FANG-VERDICT.md, commit the verdict alone</name>
  <files>.planning/phases/04-cli-glow-up/04-FANG-VERDICT.md</files>
  <read_first>
    - .planning/phases/04-cli-glow-up/04-CONTEXT.md (D-01…D-04 including the D-04 research correction sub-bullet)
    - .planning/phases/04-cli-glow-up/04-RESEARCH.md (Common Pitfalls #3, Code Examples "Pattern 5" and "The fang double-print risk", Package Legitimacy Audit, the govulncheck baseline sentence under Sources)
    - internal/cli/root.go, cmd/codegraph/main.go (the two files the spike edits in the working tree)
    - test/integration/renamed_stubs_test.go (the D-03 contract), test/integration/main_test.go (TestMain builds the real binary)
    - internal/cli/present/archtest/charm_cgo_test.go (TestCharmCgoClosure — must stay green)
    - Taskfile.yml `test:wireoracle` (line 168) and `docs:cli:drift` (line 494)
    - /Users/sean/go/pkg/mod/charm.land/fang/v2@v2.0.1/fang.go and help.go (read `Execute`, `DefaultErrorHandler`, `helpFn`/`evalGroups` — cite line numbers in the verdict)
  </read_first>
  <action>
    Spike in the WORKING TREE only (nothing from this task is committed except the verdict file):

    1. Baseline first, on the clean tree: run `GOTOOLCHAIN=go1.26.6 govulncheck ./...` and save the output to `/tmp/04-02-vuln-before.txt`; run `codegraph --version` (from a `go build -o /tmp/04-02-before-bin ./cmd/codegraph`) and save the line; run `/tmp/04-02-before-bin completion bash | head -c 200` and `/tmp/04-02-before-bin man $(mktemp -d)` and record that both work today.
    2. `GOTOOLCHAIN=go1.26.6 go get charm.land/fang/v2@v2.0.1` (this edits go.mod/go.sum in the working tree; record the `go: added …` module list verbatim for the verdict's information section — D-02, no count assertion).
    3. Edit `internal/cli/root.go`: `Execute()` becomes `fang.Execute(context.Background(), newRootCmd(), fang.WithoutManpage(), fang.WithVersion(versionLine()))` with imports `context` and `"charm.land/fang/v2"`. Do NOT call `WithoutCompletions()` (D-04 correction). `NewRootCmd()` and `newRootCmd()` untouched.
    4. Edit `cmd/codegraph/main.go`: on error just `os.Exit(1)` — remove the `fmt.Fprintln(os.Stderr, err)` (fang's DefaultErrorHandler already printed it once; keeping it would double-print and break D-03). Drop the now-unused `fmt` import.
    5. Evidence runs (tee each to `/tmp/04-02-*.txt`, paste the tail into the verdict):
       - D-01: `GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1` — the `ok` line and `test -z "$(git status --porcelain testdata/wireoracle)"`.
       - D-02: `GOTOOLCHAIN=go1.26.6 govulncheck ./...` → `/tmp/04-02-vuln-after.txt`; compare to before: zero NEW findings (diff the "Vulnerability #" headers). `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/archtest/ -run TestCharmCgoClosure -count=1 -v` green.
       - D-03: `GOTOOLCHAIN=go1.26.6 go test ./test/integration/ -run TestRenamedStubsPrintExactlyOnce -count=1 -v` green (real binary, main.go changed).
       - Composition (D-04 corrected): build `/tmp/04-02-spike-bin`; `completion bash`, `completion zsh`, `completion fish` each emit a non-empty script (`wc -c` > 1000); `man <tmpdir>` writes ≥ 1 `.1` file; `--help` renders and lists `completion` among the commands; `--version` output byte-identical to the baseline line.
       - Information: `TestPlainGolden`/`TestNoColorNonTTYRegression` green under the wrap (in-process — unaffected by design, record anyway); `NO_COLOR=1 /tmp/04-02-spike-bin --help | rg -c $'\x1b'` vs `CLICOLOR_FORCE=1 … --help | rg -c $'\x1b'` to record whether fang's chrome honours the environment (it reads os.Environ(), so it will) and note that `--color` (not yet existing) cannot reach it without the plan-08 env mirror; `/tmp/04-02-spike-bin query main 2>&1 >/dev/null | cat -v` to record the non-TTY stub shape.
    6. Write `.planning/phases/04-cli-glow-up/04-FANG-VERDICT.md`: title, date, the candidate (`charm.land/fang/v2 v2.0.1`, resolved via proxy per RESEARCH), the shipped call set with the explicit "WithoutCompletions() deliberately not called — sets CompletionOptions.DisableDefaultCmd (fang.go v2.0.1, `if !opts.completions` block); cobra's own completion command is what GoReleaser's generate_completions_from_executable runs" sentence, then `## Criterion 1 — serve --mcp byte-identical (D-01)` … `## Criterion 4 — composition (D-04, corrected)` each with a verdict line `PASS`/`FAIL` and the pasted evidence tail, `## Information (not gates)` with the module list, --version identity, the --color observation, the stub TTY shape, and finally `**Verdict:** adopted` iff all four PASS, else `**Verdict:** declined` naming the failing criterion. Add the PROJECT.md Key Decisions row text (one table row) at the bottom under `## Key Decisions row (for phase close)` so plan 08 copies it verbatim.
    7. Revert the spike's code and module edits from the working tree byte-clean: `git checkout -- go.mod go.sum internal/cli/root.go cmd/codegraph/main.go`; confirm `git status --porcelain go.mod go.sum internal/ cmd/` is empty. Task 2 re-applies the adopted wiring deliberately inside the D-15 commit.
    8. Commit ONLY the verdict: `docs(04-02): record fang verdict (<adopted|declined>)`. No CI-skip token.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && V=.planning/phases/04-cli-glow-up/04-FANG-VERDICT.md && test -f "$V" && rg -q '^\*\*Verdict:\*\* (adopted|declined)$' "$V" && test "$(rg -c '^## Criterion [1-4]' "$V")" = "4" && rg -q 'WithoutCompletions\(\)' "$V" && rg -q 'DisableDefaultCmd' "$V" && rg -q 'test/wireoracle|wireoracle' "$V" && rg -q 'govulncheck' "$V" && rg -q 'TestCharmCgoClosure' "$V" && rg -q 'TestRenamedStubsPrintExactlyOnce' "$V" && rg -q 'completion (bash|zsh|fish)' "$V" && rg -q '## Key Decisions row' "$V" && git log -1 --format=%s | rg -q '^docs\(04-02\): record fang verdict' && test "$(git show --format= --name-only HEAD | tr '\n' ' ')" = ".planning/phases/04-cli-glow-up/04-FANG-VERDICT.md " && test -z "$(git status --porcelain go.mod go.sum internal/ cmd/)" && ! rg -q 'charm.land/fang' go.mod && echo "verdict recorded and committed alone; working tree byte-clean of the spike"</automated>
    <fails_when>non-zero exit; the verdict file missing a single "**Verdict:** adopted|declined" line, fewer than 4 "## Criterion" headings, or any of the named evidence tokens (WithoutCompletions(), DisableDefaultCmd, wireoracle, govulncheck, TestCharmCgoClosure, TestRenamedStubsPrintExactlyOnce, completion bash/zsh/fish, "## Key Decisions row"); HEAD's subject not "docs(04-02): record fang verdict…"; HEAD touching any file other than the verdict; go.mod/go.sum/internal/cmd dirty; or go.mod already containing charm.land/fang before Task 2</fails_when>
  </verify>
  <acceptance_criteria>
    - `04-FANG-VERDICT.md` contains exactly one `**Verdict:** adopted` or `**Verdict:** declined` line, four `## Criterion N` sections each with a PASS/FAIL line and a pasted transcript tail, an `## Information (not gates)` section listing the `go: added …` modules, and a `## Key Decisions row` section with one markdown table row
    - The verdict's shipped call set is `fang.Execute(context.Background(), newRootCmd(), fang.WithoutManpage(), fang.WithVersion(versionLine()))` and the file states why `WithoutCompletions()` is not called
    - `git show --name-only HEAD` lists only `04-FANG-VERDICT.md`; `git status --porcelain go.mod go.sum internal/ cmd/` is empty; `rg 'charm.land/fang' go.mod` matches nothing
  </acceptance_criteria>
  <done>The fang decision is recorded, evidence-carrying and committed before any code change; the working tree carries no spike residue.</done>
</task>

<task type="auto">
  <name>Task 2: The D-15 commit — prefix denylist + colorprofile promotion (+ fang wiring if adopted), each module proven RED</name>
  <reversibility rating="costly">If adopted, the `charm.land/fang/v2` require and the `Execute()` boundary change ride the next release (D-04); undo is a coordinated revert of go.mod, root.go, main.go and plan 08's help branch, not a one-file change. The decision itself was made by the verdict rule in CONTEXT D-04 — recorded here, not re-asked.</reversibility>
  <files>internal/cli/present/archtest/import_graph_test.go, go.mod, go.sum, internal/cli/root.go, cmd/codegraph/main.go, .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/04-cli-glow-up/04-FANG-VERDICT.md (the verdict line decides this task's adopted/declined branch)
    - internal/cli/present/archtest/import_graph_test.go (forbiddenImportPaths lines 51-55, the match loop lines 187-199, charmImporterProbePath, assertCharmImporterExists)
    - internal/cli/present/archtest/charm_cgo_test.go (charmClosurePathPrefix — do NOT change)
    - go.mod (the `// indirect` block; colorprofile at line 53)
    - .planning/phases/04-cli-glow-up/04-CONTEXT.md (D-15 verbatim)
    - .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md (append Families (c)/(d) after (b))
    - .planning/STATE.md (the "go mod tidy is blocked by tree-sitter-swift" carry-over — hand-edit go.mod)
  </read_first>
  <action>
    1. `import_graph_test.go` (D-15, GRD-13): replace `var forbiddenImportPaths = []string{…}` with `var forbiddenImportPathPrefixes = []string{"charm.land/", "github.com/charmbracelet/"}` and update its doc comment to say both charm vanity roots are forbidden by prefix (colorprofile and x/ansi live under `github.com/charmbracelet/`, so an exact list on `charm.land/` alone would miss them — RESEARCH Pitfall 4); rewrite the loop in `TestNoCharmInServeReachablePackages` to `for path := range pkg.Imports { for _, prefix := range forbiddenImportPathPrefixes { if strings.HasPrefix(path, prefix) { t.Errorf("package %s imports %s — …", pkg.PkgPath, path) } } }` reporting every hit. Keep `charmImporterProbePath = "charm.land/lipgloss/v2"` and `assertCharmImporterExists` exactly as they are. Update the package doc comment's "may ever import charm.land/lipgloss/v2, …" sentence to name the two prefixes. Do not touch `charm_cgo_test.go`.
    2. `go.mod`: move `github.com/charmbracelet/colorprofile v0.4.3` from the `// indirect` block into the direct `require (…)` block (delete the `// indirect` suffix; keep the version). Hand edit — `go mod tidy` is blocked on this branch. `GOTOOLCHAIN=go1.26.6 go build ./...` must still succeed.
    3. If the verdict is `adopted`: `GOTOOLCHAIN=go1.26.6 go get charm.land/fang/v2@v2.0.1`, then re-apply Task 1's `root.go` `Execute()` wrap and the `main.go` change exactly as the verdict's shipped call set records them (imports `context` and `"charm.land/fang/v2"` in root.go; `fmt` import dropped from main.go). If `declined`: touch neither root.go nor main.go and add no fang line.
    4. Mutation log Family (c) — GRD-13/D-15 colorprofile: gate `git status --porcelain internal/query` empty; create UNTRACKED `internal/query/zz_planted_charm_probe.go` containing `package query` and `import _ "github.com/charmbracelet/colorprofile"`; run `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/archtest/ -run TestNoCharmInServeReachablePackages -count=1 -v`; observed RED naming `github.com/seanb4t/codegraph-go/internal/query` and `github.com/charmbracelet/colorprofile`; delete the file; `git status --porcelain internal/query` empty; green control run. Paste verbatim.
    5. If adopted, Family (d) — the same with `import _ "charm.land/fang/v2"` (file `internal/query/zz_planted_fang_probe.go`), RED naming `charm.land/fang/v2`, removed byte-clean, green control.
    6. Full gates before the commit: `GOTOOLCHAIN=go1.26.6 go build ./...`, `gofmt -l` empty on the touched dirs, `go vet ./internal/cli/... ./cmd/...`, `go test ./internal/cli/... -count=1` (includes TestPlainGolden — must stay green: no renderer landed), `go test ./internal/cli/present/archtest/... -count=1` (both archtests green), and if adopted `go test ./test/integration/ -run TestRenamedStubsPrintExactlyOnce -count=1` and `GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1`.
    7. ONE commit: `feat(04-02): widen present archtest to charm-family prefixes; promote colorprofile to direct[; adopt fang at the Execute boundary]` — body cites D-15/GRD-13, names Family (c) [(d)], and states the denylist and go.mod changed together. No CI-skip token.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rm -f /tmp/04-02-red-c.txt && test -z "$(git status --porcelain internal/query)" && printf 'package query\n\nimport _ "github.com/charmbracelet/colorprofile"\n' > internal/query/zz_planted_charm_probe.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/archtest/ -count=1 -run 'TestNoCharmInServeReachablePackages$' -v > /tmp/04-02-red-c.txt 2>&1; echo "exit=$?" >> /tmp/04-02-red-c.txt); rm -f internal/query/zz_planted_charm_probe.go; test -z "$(git status --porcelain internal/query)" && rg -q -- '--- FAIL: TestNoCharmInServeReachablePackages' /tmp/04-02-red-c.txt && rg -q 'internal/query imports github.com/charmbracelet/colorprofile' /tmp/04-02-red-c.txt && ! rg -q '^exit=0$' /tmp/04-02-red-c.txt && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/archtest/... -count=1 2>&1 | tee /tmp/04-02-arch.txt | tail -2; rg -q '^ok\s' /tmp/04-02-arch.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/04-02-arch.txt && rg -q 'forbiddenImportPathPrefixes' internal/cli/present/archtest/import_graph_test.go && rg -q '"github.com/charmbracelet/"' internal/cli/present/archtest/import_graph_test.go && rg -q 'charmImporterProbePath = "charm.land/lipgloss/v2"' internal/cli/present/archtest/import_graph_test.go && rg -q 'charmClosurePathPrefix = "charm.land"' internal/cli/present/archtest/charm_cgo_test.go && rg -q '^\s+github.com/charmbracelet/colorprofile v0.4.3$' go.mod && rg -q 'Family \(c\)' .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md && GOTOOLCHAIN=go1.26.6 go build ./... && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestPlainGolden$' 2>&1 | tee /tmp/04-02-golden.txt | tail -1; rg -q '^ok\s' /tmp/04-02-golden.txt && git log -1 --format=%s | rg -q '^feat\(04-02\): ' && git show --format= --name-only HEAD | rg -q 'import_graph_test.go' && git show --format= --name-only HEAD | rg -q '^go.mod$' && echo "Family (c) RED then green; prefix denylist + go.mod promotion in one commit; goldens still green"</automated>
    <fails_when>non-zero exit; the planted run lacking "--- FAIL: TestNoCharmInServeReachablePackages" or the "internal/query imports github.com/charmbracelet/colorprofile" message or recording exit=0; internal/query not clean after removal; archtest package not "ok"; any of the four source greps (prefix var, charmbracelet prefix literal, unchanged probe literal, unchanged cgo prefix) missing; colorprofile not in the direct require block (line without "// indirect"); "Family (c)" absent from the log; build red; TestPlainGolden not "ok"; HEAD not a "feat(04-02):" commit touching both import_graph_test.go and go.mod</fails_when>
  </verify>
  <acceptance_criteria>
    - `import_graph_test.go` contains `forbiddenImportPathPrefixes` with exactly the two literals `"charm.land/"` and `"github.com/charmbracelet/"`, uses `strings.HasPrefix` in the closure loop, and still contains `charmImporterProbePath = "charm.land/lipgloss/v2"` and `func assertCharmImporterExists(`
    - `charm_cgo_test.go` is byte-identical to its pre-plan state: `B=$(git log --format=%H -E --grep='^docs\(04-02\): record fang verdict' -1) && git diff "$B^..HEAD" -- internal/cli/present/archtest/charm_cgo_test.go` prints nothing
    - `go.mod` has `github.com/charmbracelet/colorprofile v0.4.3` in the direct require block with no `// indirect` marker
    - If the verdict is adopted: `go.mod` has `charm.land/fang/v2 v2.0.1`, `internal/cli/root.go` contains `fang.Execute(context.Background(), newRootCmd(), fang.WithoutManpage(), fang.WithVersion(versionLine()))` and does NOT contain `WithoutCompletions`, `cmd/codegraph/main.go` contains no `Fprintln`, and `04-MUTATION-LOG.md` contains `## Family (d)` with a RED naming `charm.land/fang/v2`. If declined: `rg 'fang' go.mod internal/cli/root.go cmd/codegraph/main.go` matches nothing
    - `04-MUTATION-LOG.md` contains `## Family (c)` with the pasted `--- FAIL: TestNoCharmInServeReachablePackages` line naming `internal/query` and `github.com/charmbracelet/colorprofile`
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... ./internal/cli/present/archtest/... -count=1` exits 0; `git status --porcelain` shows no untracked file under `internal/query`
  </acceptance_criteria>
  <done>The denylist is prefix-matched on both charm roots and was proven RED per newly-required module; colorprofile is a direct requirement in the same commit; fang is wired in exactly per the verdict or absent entirely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go module proxy → go.mod/go.sum | a new third-party module (fang, adopted only) enters the release binary's closure |
| serve-reachable packages → charm family | the archtest is the only mechanical guard keeping ANSI-emitting code out of the MCP/agent closure |
| cmd/codegraph/main.go → stderr | the single error-printing exit path moves into fang's DefaultErrorHandler if adopted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | present archtest denylist | high | mitigate | prefix matching on both vanity roots, self-defeat probe retained, Family (c)/(d) planted-import RED per module in the same commit as the go.mod change (D-15, GRD-13) |
| T-04-05 | Denial of Service / Repudiation | fang DefaultErrorHandler + main.go | medium | mitigate | D-03 real-binary test proves the stub message prints exactly once and exit stays 1; main.go stops printing only in the adopted branch and in the same commit |
| T-04-06 | Information Disclosure | fang help/error chrome | low | accept | renders the identical `err.Error()`/help strings already reaching stderr/stdout today; no new attacker-controlled data path (RESEARCH Security Domain) |
| T-04-SC | Tampering | `go get charm.land/fang/v2@v2.0.1` (adopted only) | high | mitigate | package-legitimacy: 04-RESEARCH.md audit table verdict `OK` (charmbracelet org, resolved via proxy.golang.org, source read from the module cache); pinned exact version; `govulncheck ./...` zero-new-findings gate and `TestCharmCgoClosure` green are D-02's conjunctive criteria; no `[ASSUMED]`/`[SUS]` package exists so no blocking-human checkpoint is inserted |
</threat_model>

<verification>
- `04-FANG-VERDICT.md` committed alone, before any renderer; verdict line present with four evidenced criteria
- `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/archtest/... -count=1` green; Family (c) (+ (d)) recorded RED and byte-clean
- `GOTOOLCHAIN=go1.26.6 go build ./... && go test ./internal/cli/... -count=1` green (goldens untouched)
- adopted branch only: `go test ./test/integration/ -run TestRenamedStubsPrintExactlyOnce` and `go test ./test/wireoracle/... -count=1` green at HEAD
</verification>

<success_criteria>
- CLI-08: a recorded, evidence-carrying verdict precedes every renderer; no renderer or help template in this plan
- GRD-13: the present archtest catches every charm-family import path by prefix and was demonstrated RED per new module; denylist and go.mod changed in one commit
- `charm_cgo_test.go` untouched; `internal/query`/`internal/mcp` untouched
</success_criteria>

<output>
Create `.planning/phases/04-cli-glow-up/04-02-SUMMARY.md` when done (state the verdict, paste the four criterion tails and the Family (c)/(d) FAIL lines, list the modules fang added if adopted).
</output>
