---
phase: 04-cli-glow-up
plan: 08
type: execute
wave: 6
depends_on: ["04-04", "04-05", "04-07"]
files_modified:
  - internal/cli/root.go
  - internal/cli/cli_reference_test.go
  - internal/cli/present/help.go
  - internal/cli/present/help_test.go
  - docs/CLI-REFERENCE.md
  - .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CLI-06, CLI-01, CLI-02, CLI-04]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`codegraph --help` groups commands into four `cobra.Group`s registered in this order with these IDs and titles: `query` \"Query the graph:\", `build` \"Build the index:\", `agents` \"Agents & serving:\", `maintenance` \"Maintenance:\"; membership is D-13's table exactly — query: explore, search, node, callers, callees, impact, affected, files, status · build: init, index, sync, daemon, githooks, uninit · agents: serve, ui, install, uninstall · maintenance: version, upgrade, telemetry, plus `help` and `completion` via `root.SetHelpCommandGroupID(\"maintenance\")`/`root.SetCompletionCommandGroupID(\"maintenance\")`; hidden `man`/`query`/`unlock` stay groupless — assigned from ONE `commandGroups` name→id table in `root.go` applied after `AddCommand` (D-13, CLI-06)."
    - "`TestEveryCommandHasGroupID` (cli_reference_test.go) builds the tree with `InitDefaultHelpCmd()` + `InitDefaultCompletionCmd()`, walks `root.Commands()` (root's direct children only — cobra groups are per-parent and `daemon start|stop|unlock`/`githooks install|remove|status` are not grouped), and asserts every VISIBLE command (`IsAvailableCommand() && !IsAdditionalHelpTopicCommand()`) carries a `GroupID` that is one of the four registered `root.Groups()` IDs, that every hidden command carries none, that each of the four groups has ≥1 member, and reports a positive count (≥24 visible commands inspected); it was RED before the groups existed (the natural TDD RED: the ungrouped tree fails on the first assertion) and RED again against a planted visible ungrouped command — Family (e) in `04-MUTATION-LOG.md`; whether cobra draws the titles is NOT tested (D-00, D-13)."
    - "Help styling follows the verdict in `04-FANG-VERDICT.md`: ADOPTED → fang's `helpFunc` (installed by `fang.Execute`) renders the grouped help; `Execute()` additionally calls `applyColorFlagToProcessEnv(os.Args)` before `fang.Execute` — a tiny pre-scan for `--color=<v>`/`--color <v>` that applies `rewriteEnviron`'s result to the process env via `os.Setenv`/`os.Unsetenv` so fang's own `os.Environ()` reads (fang.go v2.0.1:131/174) honour `--color=never|always` for help and error chrome (invalid values are left for cobra's parse to reject); DECLINED → `installHelpFunc(root)` captures cobra's stock help func and sets `root.SetHelpFunc` to: `mode := resolveColor(c); if mode.Styled { present.RenderHelp(c, present.NewPalette(mode.Dark), mode.Writer(c.OutOrStdout())) } else { stock(c, args) }` so `<verb> --help` inherits root's styling and non-TTY help is cobra's stock grouped template byte-for-byte (D-14, CLI-06)."
    - "DECLINED branch only: `present.RenderHelp(c *cobra.Command, pal Palette, w io.Writer) error` walks cobra metadata — `Long` or `Short` (Value), `Usage:` (Header) + `c.UseLine()`/`[command]` lines (Value), then for the ROOT each of `c.Groups()` in registration order as a Header title followed by its visible commands `Name` (Value, padded to `c.NamePadding()`) + `Short` (Label), ungrouped visible commands last under `Additional Commands:` (cobra's own wording) if any, then `Flags:` (Header) with `c.LocalFlags().FlagUsages()` lines (flag names in Path, usage in Label — or the whole line in Value if splitting the usage string is fragile), `Global Flags:` when `HasAvailableInheritedFlags()`, and the `Use \"codegraph [command] --help\" for more information about a command.` trailer (Label); `TestRenderHelpGroupsInOrder` asserts the four titles appear in D-13 order with their members beneath and that stripping ANSI yields non-empty lines containing every visible command name (D-14)."
    - "`docs/CLI-REFERENCE.md` is regenerated ONCE through `task docs:cli` as a reviewed diff whose only changes are the `--color` persistent flag (`--color string   colour output: auto (detect), always, or never (default \"auto\")` under root's Options and under every subcommand's inherited-options block) — cobra/doc renders no group titles, so no other hunk is expected; `task docs:cli:drift` and `TestEveryRegisteredFlagIsAccountedFor` are green at the commit (the `--color` flag is accounted at root via `inheritedFromAncestor`; no allowlist line is added); the scheduled RED window opened by plan 03 closes here (VERB-06 precedent)."
    - "Phase-close gates green at HEAD: `GOTOOLCHAIN=go1.26.6 go build ./... && go test -count=1 ./...` (minus `internal/daemon`'s isolated leg — run `task test:unit` shape) plus `go test ./test/integration/... -count=1`, `go test ./test/wireoracle/... -count=1`, `task test:golden`, `task docs:cli:drift`, `go test ./internal/cli/present/archtest/... -count=1`, `go test -race -count=1 ./internal/cli/ ./internal/cli/present/`, `gofmt -l` empty, `go vet ./...`; `TestPlainGolden` 28/28; `git diff --name-only <phase-start>..HEAD -- internal/query internal/mcp testdata/wireoracle testdata/golden` empty (CLI-05, GRD-13)."
    - "`PROJECT.md` → `## Key Decisions` gains ONE table row (the verdict file's prepared `## Key Decisions row` text, verbatim) recording the fang verdict — a value filled into an existing tool-shape, no new heading (D-04, planning-artifacts rule)."
    - "CLI-06 (probe adjacency, explicit): a command belongs to exactly one group — `GroupID` is a single string; the guard rejects an ID outside the four registered IDs (cobra would also panic at Execute on an unregistered ID — `checkCommandGroups`, cited not tested)."
    - "CLI-06 (probe empty, explicit): no group is empty — the guard asserts ≥1 visible member per group (an empty group would render no title in cobra and silently hide a category)."
    - "CLI-06 (probe ordering, explicit): group order is `AddGroup` registration order (query, build, agents, maintenance); order of commands inside a group is cobra's own sorted order (accepted, not asserted); the hand-rolled `RenderHelp` iterates `c.Groups()` in that order (asserted when declined)."
    - "CLI-06 (probe concurrency, explicit): help rendering is synchronous on the calling goroutine with no shared mutable state; the phase's final `-race` run over cli+present is clean."
    - statement: "Human UAT, harvested at end of phase from every `<human-check>` in this phase (plans 03 and here): (1) CLI-04 palette readability on Solarized Light / macOS light Terminal and a dark theme across `status`, `explore`, `node`, `search --full`, `install`, `--help`; (2) CLI-02 rendering under `TERM=dumb`, `TERM=xterm`, `TERM=xterm-256color`, `COLORTERM=truecolor`, one run over SSH and one in tmux with the OSC-11 ~2 s pause noted; (3) `--help` and `<verb> --help` styled consistently on a TTY and stock on a pipe; (4) `--color=always | less -R` shows colour, `--color=never` on a TTY is plain — expected `human_needed` pause, not a defect (D-06)."
      verification: backstop
  artifacts:
    - "internal/cli/root.go — `groupQuery`/`groupBuild`/`groupAgents`/`groupMaintenance` consts, `commandGroups` map, `root.AddGroup(...)`, `SetHelpCommandGroupID`/`SetCompletionCommandGroupID`, `installHelpFunc` (declined) or `applyColorFlagToProcessEnv` (adopted)"
    - "internal/cli/cli_reference_test.go — `TestEveryCommandHasGroupID`, `commandGroupIDs` helper"
    - "internal/cli/present/help.go + help_test.go (declined branch only) — `RenderHelp`, `TestRenderHelpGroupsInOrder`"
    - "docs/CLI-REFERENCE.md — regenerated (the `--color` hunks only)"
    - ".planning/phases/04-cli-glow-up/04-MUTATION-LOG.md — Family (e); .planning/PROJECT.md — one Key Decisions row"
  key_links:
    - "`newRootCmd()` → `root.AddGroup(...)` → `for _, c := range root.Commands() { c.GroupID = commandGroups[c.Name()] }` → `SetHelpCommandGroupID`/`SetCompletionCommandGroupID` → cobra's usage template / fang's `evalGroups` / `present.RenderHelp` all read the same `Groups()`/`GroupID` data"
    - "`TestEveryCommandHasGroupID` → `NewRootCmd()` + `InitDefaultHelpCmd()` + `InitDefaultCompletionCmd()` → the same tree `tools/clidoc` and `cliReferenceTree()` walk"
    - "`task docs:cli` → `tools/clidoc` → `NewRootCmd()` (unchanged entry) → `docs/CLI-REFERENCE.md` → `task docs:cli:drift` + `TestEveryRegisteredFlagIsAccountedFor`"
  prohibitions:
    - statement: "MUST NOT hand-edit `docs/CLI-REFERENCE.md` (regenerate only), MUST NOT add an allowlist line for `--color`, and MUST NOT touch `tools/clidoc` (v0.13.0 Phase 12 ruling)."
      status: resolved
      verification: test
    - statement: "MUST NOT test cobra's or fang's help rendering (whether titles are drawn, column widths, wrapping) — only our GroupID data and, when declined, our RenderHelp's group order and presence of names (D-00)."
      status: resolved
      verification: test
    - statement: "MUST NOT invent a heading in `PROJECT.md`, `ROADMAP.md` or `STATE.md` — the verdict lands as one row in the existing Key Decisions table (planning-artifacts rule)."
      status: resolved
      verification: judgment
---

<objective>
Close the phase: group the command tree into D-13's four titled sections (one table in `root.go`,
guarded RED-first by a walk that every visible command carries one of the four IDs), style help
per the recorded fang verdict (fang's chrome with `--color` mirrored into the process env, or the
hand-rolled `present.RenderHelp` behind the same resolver), regenerate `docs/CLI-REFERENCE.md`
through the drift gate as a reviewed `--color`-only diff, run every phase gate at HEAD, and record
the verdict as a PROJECT.md Key Decisions row.

Purpose: CLI-06 is the last user-visible piece; it must land AFTER every verb is styled (plan 07)
so a `<verb> --help` check sees the final tree, and the docs regeneration runs once, at the end,
closing the drift window plan 03 opened. The human-verify items CONTEXT expects (CLI-02, CLI-04)
are consolidated here for end-of-phase harvesting.

TDD: `TestEveryCommandHasGroupID` is a genuine RED-first test (the ungrouped tree fails it) —
`test(04-08):` commit before `feat(04-08):`; declined-branch `help_test.go` likewise; `--- FAIL:`
transcripts in the SUMMARY; never `check tdd-red-evidence`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-glow-up/04-CONTEXT.md
@.planning/phases/04-cli-glow-up/04-RESEARCH.md
@.planning/phases/04-cli-glow-up/04-FANG-VERDICT.md
@.planning/phases/04-cli-glow-up/04-MUTATION-LOG.md
@.planning/phases/04-cli-glow-up/04-07-SUMMARY.md
@internal/cli/root.go
@internal/cli/cli_reference_test.go
@internal/cli/colorflag.go
@internal/cli/present/palette.go
@tools/clidoc/main.go
@Taskfile.yml
</context>

<interfaces>
<!-- Verified this session from github.com/spf13/cobra@v1.10.2/command.go and completions.go. -->
```go
type Group struct { ID string; Title string }                       // command.go:45
// Command.GroupID string                                            // command.go:77
func (c *Command) AddGroup(groups ...*Group)                         // command.go:1396
func (c *Command) Groups() []*Group
func (c *Command) SetHelpFunc(f func(*Command, []string))            // command.go:333
func (c *Command) HelpFunc() func(*Command, []string)                // returns the current (stock) help func — capture it BEFORE SetHelpFunc
func (c *Command) SetHelpCommandGroupID(groupID string)              // command.go:343 — applied to the default help cmd at InitDefaultHelpCmd (command.go:47 region)
func (c *Command) SetCompletionCommandGroupID(groupID string)        // command.go:352 — applied at InitDefaultCompletionCmd (completions.go:778)
func (c *Command) InitDefaultHelpCmd(); func (c *Command) InitDefaultCompletionCmd(args ...string)
// checkCommandGroups (command.go:1205) panics at Execute on a GroupID not registered via AddGroup — every ID in commandGroups MUST be one of the four
func (c *Command) IsAvailableCommand() bool; func (c *Command) IsAdditionalHelpTopicCommand() bool; func (c *Command) NamePadding() int; func (c *Command) UseLine() string; func (c *Command) HasAvailableInheritedFlags() bool
```
From plan 03: `resolveColor(cmd)`, `rewriteEnviron(choice, environ)`, `colorChoice`; plan 02: the verdict line; fang (adopted): `fang.Execute` installs its own help func that honours `c.Groups()` (help.go `evalGroups`) and reads `os.Environ()` itself.
</interfaces>

## Artifacts this phase produces

See `04-01-PLAN.md` → "Artifacts this phase produces" (phase-wide list). This plan creates `commandGroups`, the four group ids, `TestEveryCommandHasGroupID`, `installHelpFunc`/`present.RenderHelp` (declined) or `applyColorFlagToProcessEnv` (adopted), and the regenerated reference.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four cobra groups from one table; TestEveryCommandHasGroupID RED-first; Family (e)</name>
  <files>internal/cli/root.go, internal/cli/cli_reference_test.go, .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md</files>
  <read_first>
    - internal/cli/root.go (newRootCmd's AddCommand list — the names; Execute() as left by plan 02)
    - internal/cli/cli_reference_test.go (cliReferenceTree, documentedByReference, the walk idiom, the positive-floor convention)
    - internal/cli/renamed.go, man.go (the three hidden commands that must stay groupless)
    - .planning/phases/04-cli-glow-up/04-CONTEXT.md (D-13 table verbatim)
    - .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md (append Family (e))
  </read_first>
  <behavior>
    - TestEveryCommandHasGroupID: tree = NewRootCmd() + InitDefaultHelpCmd() + InitDefaultCompletionCmd(); registered := {g.ID for g in root.Groups()} must equal exactly {"query","build","agents","maintenance"} in that order; for each c in root.Commands(): visible (IsAvailableCommand && !IsAdditionalHelpTopicCommand) ⇒ c.GroupID ∈ registered (t.Errorf naming the command otherwise); hidden ⇒ c.GroupID == ""; members[c.GroupID]++; each of the four groups has ≥1 member; `help` and `completion` are in "maintenance"; inspected ≥ 24 visible commands (t.Fatalf below the floor; t.Logf the count)
    - the same test fails RED on the current ungrouped tree with a message naming the first ungrouped visible command
  </behavior>
  <action>
    RED first: add `TestEveryCommandHasGroupID` to `cli_reference_test.go` (plus a `commandGroupIDs(root) map[string]bool` helper) and commit `test(04-08): add failing GroupID coverage guard for every visible command`; observe `--- FAIL: TestEveryCommandHasGroupID` (no groups registered / commands ungrouped); paste the lines.

    GREEN — `root.go` (D-13, CLI-06): consts `groupQuery = "query"`, `groupBuild = "build"`, `groupAgents = "agents"`, `groupMaintenance = "maintenance"`; `var commandGroups = map[string]string{ "explore": groupQuery, "search": groupQuery, "node": groupQuery, "callers": groupQuery, "callees": groupQuery, "impact": groupQuery, "affected": groupQuery, "files": groupQuery, "status": groupQuery, "init": groupBuild, "index": groupBuild, "sync": groupBuild, "daemon": groupBuild, "githooks": groupBuild, "uninit": groupBuild, "serve": groupAgents, "ui": groupAgents, "install": groupAgents, "uninstall": groupAgents, "version": groupMaintenance, "upgrade": groupMaintenance, "telemetry": groupMaintenance }` (22 entries; `man`, `query`, `unlock` deliberately absent). In `newRootCmd()` after `AddCommand(...)`: `root.AddGroup(&cobra.Group{ID: groupQuery, Title: "Query the graph:"}, &cobra.Group{ID: groupBuild, Title: "Build the index:"}, &cobra.Group{ID: groupAgents, Title: "Agents & serving:"}, &cobra.Group{ID: groupMaintenance, Title: "Maintenance:"})`; `for _, c := range root.Commands() { if id, ok := commandGroups[c.Name()]; ok { c.GroupID = id } }`; `root.SetHelpCommandGroupID(groupMaintenance)`; `root.SetCompletionCommandGroupID(groupMaintenance)`. Update root.go's doc comment to name the four groups and D-13.

    Family (e) — CLI-06/D-13: gate `git diff --quiet -- internal/cli/root.go`; mutation `perl -0pi -e 's/"telemetry": groupMaintenance,\n//' internal/cli/root.go` (telemetry becomes a visible ungrouped command); observed `--- FAIL: TestEveryCommandHasGroupID` naming `telemetry`; revert `git checkout -- internal/cli/root.go`; byte-clean proof; green control. Paste verbatim. Note in the log that cobra's own `checkCommandGroups` panic covers the other direction (an unregistered ID) and is cited, not tested.

    Commit GREEN as `feat(04-08): group the command tree into Query/Build/Agents/Maintenance (D-13)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log --format=%s -20 | rg -q '^test\(04-08\): ' && rm -f /tmp/04-08-red-e.txt && git diff --quiet -- internal/cli/root.go && perl -0pi -e 's/"telemetry": groupMaintenance,\n//' internal/cli/root.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryCommandHasGroupID$' -v > /tmp/04-08-red-e.txt 2>&1; echo "exit=$?" >> /tmp/04-08-red-e.txt); git checkout -- internal/cli/root.go; git diff --quiet -- internal/cli/root.go && rg -q -- '--- FAIL: TestEveryCommandHasGroupID' /tmp/04-08-red-e.txt && rg -q 'telemetry' /tmp/04-08-red-e.txt && ! rg -q '^exit=0$' /tmp/04-08-red-e.txt && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryCommandHasGroupID$|TestHelpEveryCommand$|TestHelpVersionCommand$' -v 2>&1 | tee /tmp/04-08-t1.txt | tail -3; rg -q '^ok\s' /tmp/04-08-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/04-08-t1.txt && rg -q 'inspected (2[4-9]|[3-9][0-9]) visible' /tmp/04-08-t1.txt && test "$(rg -c 'group(Query|Build|Agents|Maintenance),' internal/cli/root.go)" -ge 22 && rg -q 'Title: "Query the graph:"' internal/cli/root.go && rg -q 'Title: "Build the index:"' internal/cli/root.go && rg -q 'Title: "Agents & serving:"' internal/cli/root.go && rg -q 'Title: "Maintenance:"' internal/cli/root.go && rg -q 'SetHelpCommandGroupID\(groupMaintenance\)' internal/cli/root.go && rg -q 'SetCompletionCommandGroupID\(groupMaintenance\)' internal/cli/root.go && ! rg -q '"man":|"query":|"unlock":' internal/cli/root.go && GOTOOLCHAIN=go1.26.6 go build -o /tmp/04-08-bin ./cmd/codegraph && /tmp/04-08-bin --help > /tmp/04-08-help.txt && rg -q '^Query the graph:' /tmp/04-08-help.txt && rg -q '^Build the index:' /tmp/04-08-help.txt && rg -q '^Agents & serving:' /tmp/04-08-help.txt && rg -q '^Maintenance:' /tmp/04-08-help.txt && awk '/^Maintenance:/,0' /tmp/04-08-help.txt | rg -q '^\s+completion\s' && awk '/^Maintenance:/,0' /tmp/04-08-help.txt | rg -q '^\s+help\s' && rg -q 'Family \(e\)' .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md && echo "groups registered, guard RED on planted ungrouped telemetry then green (≥24 inspected), help/completion under Maintenance in the real --help"</automated>
    <fails_when>non-zero exit; no "test(04-08):" commit; the planted run lacking "--- FAIL: TestEveryCommandHasGroupID" or "telemetry" or recording exit=0; root.go not byte-clean after revert; the green run showing FAIL or lacking an "inspected N visible" line with N ≥ 24; fewer than 22 group-map entries, any of the four Title literals, or either SetXCommandGroupID call missing; man/query/unlock present in the map; the real `--help` lacking any of the four titles, or `completion`/`help` not listed under Maintenance; or "Family (e)" absent from the log</fails_when>
  </verify>
  <acceptance_criteria>
    - `internal/cli/root.go` contains the four `group*` consts, the 22-entry `commandGroups` map, one `root.AddGroup(` call with the four titles in order, the GroupID application loop, and both `Set*CommandGroupID(groupMaintenance)` calls
    - `cli_reference_test.go` contains `func TestEveryCommandHasGroupID(` with the ≥24 positive floor and the hidden-command `GroupID == ""` assertion
    - Real `codegraph --help` output contains the four titles in order and lists `help` and `completion` under `Maintenance:`
    - `04-MUTATION-LOG.md` contains `## Family (e)` with the pasted FAIL line naming `telemetry` and a byte-clean revert
  </acceptance_criteria>
  <done>The tree is grouped per D-13 from one table, guarded by a positive-counted walk that was RED before the groups existed and RED against a planted ungrouped command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Styled help per the verdict — fang chrome honouring --color, or present.RenderHelp behind the resolver</name>
  <files>internal/cli/root.go, internal/cli/present/help.go, internal/cli/present/help_test.go</files>
  <read_first>
    - .planning/phases/04-cli-glow-up/04-FANG-VERDICT.md (the verdict line and the `--color` observation in Information)
    - internal/cli/root.go (Execute(); Task 1's groups)
    - internal/cli/colorflag.go (rewriteEnviron, colorChoice, resolveColor)
    - internal/cli/present/palette.go, line.go
    - /Users/sean/go/pkg/mod/github.com/spf13/cobra@v1.10.2/command.go (the usage template around line 1950-1970 for the stock section wording: `Available Commands:`, `Additional Commands:`, `Flags:`, `Global Flags:`, `Use "… [command] --help" …`; `HelpFunc()`, `UsageTemplate()`)
    - /Users/sean/go/pkg/mod/charm.land/fang/v2@v2.0.1/fang.go lines 110-140 (helpFunc reads os.Environ) — adopted branch only
  </read_first>
  <behavior>
    - ADOPTED: `applyColorFlagToProcessEnv([]string{"codegraph","--color=never","--help"})` results in `os.Getenv("NO_COLOR") == "1"` and no `CLICOLOR_FORCE`; with `--color=always` → `CLICOLOR_FORCE=1` set and `NO_COLOR` unset; with `--color`,`always` (two args) the same; with no flag or `--color=bogus` → env untouched (cobra rejects bogus later); `--color=never` followed by `--color=always` → last wins (a table test in colorflag_test.go, restoring env via t.Setenv)
    - DECLINED: `TestRenderHelpGroupsInOrder`: on the real tree (cliReferenceTree + InitDefaultHelpCmd), `stripANSI(RenderHelp(root, pal, &b))` contains the four titles in D-13 order (index of each title increasing), every visible root command name appears after its group's title and before the next title, `completion` and `help` appear under `Maintenance:`, the stripped output contains `Flags:` and `--color`, and the styled output contains `\x1b[`; on a subcommand (`status`) it contains `Usage:` and `status`'s flags and no group titles
  </behavior>
  <action>
    Read the verdict line. Then EITHER:

    ADOPTED — in `root.go`: `func applyColorFlagToProcessEnv(args []string)` scanning `args[1:]` for `--color=<v>` or `--color` followed by `<v>` (last occurrence wins; stop scanning at a bare `--`), and, when `v` is one of `auto|always|never`, computing `rewriteEnviron(colorChoice(v), os.Environ())` and applying it: unset every key that the rewrite dropped (`NO_COLOR`, `CLICOLOR`, `CLICOLOR_FORCE` as applicable) and `os.Setenv` every key it added (`NO_COLOR=1` / `CLICOLOR_FORCE=1`). Call it as the first line of `Execute()` before `fang.Execute(...)`. Comment: fang reads `os.Environ()` for its help/error writers (fang.go v2.0.1:131,174), so the flag is mirrored into the process env once; the resolver's own rewrite is idempotent over the mirrored env (the D-09 rewrite applied twice yields the same keys). Add the table test to `colorflag_test.go` (RED-first: `test(04-08):` commit). No `present/help.go` in this branch — do not create it.

    DECLINED — RED first: commit `help_test.go` + placeholder `present/help.go` as `test(04-08): add failing RenderHelp group-order test`. GREEN: `present.RenderHelp(c *cobra.Command, pal Palette, w io.Writer) error` per the must_haves layout (Long/Short → Value; `Usage:` Header + `  ` + `c.UseLine()` [+ `  codegraph [command]` when `c.HasAvailableSubCommands()`]; for each `g := range c.Groups()` with ≥1 visible member: blank line, `pal.Header.Render(g.Title)`, then each visible `sub` with `sub.GroupID == g.ID`: `"  " + pal.Value.Render(rpad(sub.Name(), c.NamePadding())) + " " + pal.Label.Render(sub.Short)`; visible ungrouped subs under `Additional Commands:`; `Flags:` Header + each line of `c.LocalFlags().FlagUsages()` through `pal.Value`; `Global Flags:` + `c.InheritedFlags().FlagUsages()` when `HasAvailableInheritedFlags()`; trailer `Use "<root> [command] --help" for more information about a command.` in Label when `HasAvailableSubCommands()`). Every string through `sanitizeControl`. Then in `root.go`: `func installHelpFunc(root *cobra.Command)` capturing `stock := root.HelpFunc()` BEFORE `root.SetHelpFunc(func(c *cobra.Command, args []string) { mode := resolveColor(c); if !mode.Styled { stock(c, args); return }; _ = present.RenderHelp(c, present.NewPalette(mode.Dark), mode.Writer(c.OutOrStdout())) })`; call `installHelpFunc(root)` at the end of `newRootCmd()` (cobra propagates root's help func to every subcommand via `HelpFunc()`'s parent walk, so `<verb> --help` inherits it — D-14). Note: `--help` is processed by cobra before PersistentPreRun; `resolveColor(c)` reads the already-parsed `--color` from `c.Flags()` — verify with execCmd("--color=always", "--help") containing ESC and execCmd("--help") (pipe) equal to cobra's stock template bytes.

    Both branches: real-binary checks that `--help | cat` and `status --help | cat` are ESC-free and contain the stock wording, and that `--color=always --help | cat` and `status --color=always --help | cat` contain ESC bytes. Commit `feat(04-08): styled help (<fang chrome honouring --color | hand-rolled RenderHelp behind the resolver>)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && V=.planning/phases/04-cli-glow-up/04-FANG-VERDICT.md && VERDICT=$(rg -o '^\*\*Verdict:\*\* (adopted|declined)$' "$V" | rg -o 'adopted|declined') && test -n "$VERDICT" && git log --format=%s -30 | rg -q '^test\(04-08\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/... && if [ "$VERDICT" = adopted ]; then rg -q 'func applyColorFlagToProcessEnv\(' internal/cli/root.go && rg -q 'applyColorFlagToProcessEnv\(os\.Args\)' internal/cli/root.go && test ! -e internal/cli/present/help.go && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestApplyColorFlagToProcessEnv$' -v 2>&1 | tee /tmp/04-08-t2.txt | tail -2; rg -q -- '--- PASS: TestApplyColorFlagToProcessEnv' /tmp/04-08-t2.txt; else rg -q 'func installHelpFunc\(' internal/cli/root.go && rg -q 'func RenderHelp\(c \*cobra\.Command, pal Palette, w io\.Writer\) error' internal/cli/present/help.go && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/ -count=1 -run 'TestRenderHelpGroupsInOrder$' -v 2>&1 | tee /tmp/04-08-t2.txt | tail -2; rg -q -- '--- PASS: TestRenderHelpGroupsInOrder' /tmp/04-08-t2.txt; fi && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1 2>&1 | tee /tmp/04-08-pkg.txt | tail -6; ! rg -q '^(--- FAIL|FAIL)' /tmp/04-08-pkg.txt && GOTOOLCHAIN=go1.26.6 go build -o /tmp/04-08-bin2 ./cmd/codegraph && ! /tmp/04-08-bin2 --help | rg -q $'\x1b' && ! /tmp/04-08-bin2 status --help | rg -q $'\x1b' && /tmp/04-08-bin2 --help | rg -q '^Query the graph:' && /tmp/04-08-bin2 --color=always --help | rg -q $'\x1b\\[' && /tmp/04-08-bin2 status --color=always --help | rg -q $'\x1b\\[' && /tmp/04-08-bin2 --color=always --help | perl -pe 's/\e\[[0-9;]*m//gi' | rg -qi 'query the graph' && ! /tmp/04-08-bin2 --color=never --help | rg -q $'\x1b' && echo "help styled per verdict ($VERDICT): stock on a pipe, ESC under --color=always for root and status, plain under --color=never"</automated>
    <fails_when>non-zero exit; the verdict line unreadable; no "test(04-08):" commit; build/gofmt/vet red; the branch-specific artefact missing (adopted: applyColorFlagToProcessEnv + its call + a PASS for TestApplyColorFlagToProcessEnv, and present/help.go must NOT exist; declined: installHelpFunc + RenderHelp signature + a PASS for TestRenderHelpGroupsInOrder); FAIL in the package run; `--help`/`status --help` on a pipe carrying ESC or lacking "Query the graph:"; `--color=always --help` (root or status) lacking ESC or its stripped output lacking the query-group title; or `--color=never --help` carrying ESC</fails_when>
    <human-check>CLI-06/CLI-02/CLI-04 end-of-phase UAT (consolidates this phase's human items): on a real TTY run `codegraph --help`, `codegraph status --help`, `codegraph explore --help` — grouped, hued consistently between root and verb help; then the palette readability pass on Solarized Light / macOS light Terminal AND a dark theme over `status`, `explore Alpha`, `node Alpha`, `search Alpha --full`, `install --target claude-code --location local` (fake HOME), `--help`; then `TERM=dumb`, `TERM=xterm`, `TERM=xterm-256color`, `COLORTERM=truecolor`, one run over SSH and one inside tmux — note whether a ~2 s pause precedes styled output (OSC-11 timeout, expected); `codegraph status --color=always | less -R` shows colour; `codegraph status --color=never` on the TTY is plain. Record observed wall times and any illegible hue.</human-check>
  </verify>
  <acceptance_criteria>
    - Exactly one branch is implemented, matching `04-FANG-VERDICT.md`'s verdict line
    - Adopted: `root.go` calls `applyColorFlagToProcessEnv(os.Args)` before `fang.Execute`; `colorflag_test.go` has `TestApplyColorFlagToProcessEnv` with never/always/absent/bogus/last-wins rows; `internal/cli/present/help.go` does not exist
    - Declined: `present/help.go` defines `RenderHelp`, `help_test.go` defines `TestRenderHelpGroupsInOrder`, `root.go` defines `installHelpFunc` capturing the stock help func before `SetHelpFunc`
    - Real binary: `--help` and `status --help` on a pipe are ESC-free stock text with the four group titles; `--color=always --help` and `status --color=always --help` contain ESC; `--color=never --help` is ESC-free
  </acceptance_criteria>
  <done>Help is styled consistently for root and every verb per the recorded verdict, stock on pipes, and `--color` governs it either way.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate docs/CLI-REFERENCE.md through the drift gate, PROJECT.md Key Decisions row, full phase gates</name>
  <files>docs/CLI-REFERENCE.md, .planning/PROJECT.md</files>
  <read_first>
    - Taskfile.yml lines 481-520 (`docs:cli`, `docs:cli:drift`)
    - internal/cli/cli_reference_test.go (TestEveryRegisteredFlagIsAccountedFor; inheritedFromAncestor — why `--color` needs no allowlist line)
    - internal/cli/testdata/cli-reference-allowlist.txt (must stay at its current three entries)
    - .planning/phases/04-cli-glow-up/04-FANG-VERDICT.md (`## Key Decisions row` text)
    - .planning/PROJECT.md (`## Key Decisions` table — line 452 region; append one row at the end of the table)
    - .planning/phases/04-cli-glow-up/04-VALIDATION.md (the full-suite command)
  </read_first>
  <action>
    1. `GOTOOLCHAIN=go1.26.6 task docs:cli` — regenerate in place. Review `git diff --stat docs/CLI-REFERENCE.md` and `git diff docs/CLI-REFERENCE.md`: every added line must be a `--color string` option line (root's Options block and each subcommand's `Options inherited from parent commands` block) or its blank-line neighbour; no removed lines, no other wording. If a hunk falls outside that shape, STOP and report (do not hand-edit). Record the hunk count and the `+/-` line counts in the SUMMARY.
    2. `GOTOOLCHAIN=go1.26.6 task docs:cli:drift` green (`compared 1 generated file`, `byte-identical to a fresh regeneration`); `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -run 'TestEveryRegisteredFlagIsAccountedFor$' -count=1 -v` green and its `walked N commands …, inspected M flags: … accepted via ../../docs/CLI-REFERENCE.md, 3 accepted via testdata/cli-reference-allowlist.txt` line shows `3 accepted via` (no new allowlist entry) and N ≥ 37.
    3. `.planning/PROJECT.md` → `## Key Decisions` table: append the verdict's prepared row verbatim (one `| … | … | … |` line, no heading, no other edit) with `Edit`, never `Write`.
    4. Full phase gates at HEAD (paste each tail into the SUMMARY): `GOTOOLCHAIN=go1.26.6 go build ./...`; `test -z "$(gofmt -l .)"` over `internal/ cmd/ test/ tools/`; `GOTOOLCHAIN=go1.26.6 go vet ./...`; `GOTOOLCHAIN=go1.26.6 task test:unit`; `GOTOOLCHAIN=go1.26.6 task test:golden`; `GOTOOLCHAIN=go1.26.6 go test ./test/integration/... -count=1`; `GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1`; `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/archtest/... -count=1`; `GOTOOLCHAIN=go1.26.6 go test -race -count=1 ./internal/cli/ ./internal/cli/present/`; `GOTOOLCHAIN=go1.26.6 govulncheck ./...` (zero new findings vs the verdict's baseline); `git diff --name-only $(git log --format=%H --grep='^test(04-01): ' -1)^..HEAD -- internal/query internal/mcp testdata/wireoracle testdata/golden` empty; `test -z "$(git status --porcelain)"` after the commit.
    5. Commit `docs(04-08): regenerate CLI reference for --color; record fang verdict in PROJECT.md key decisions` (reference + PROJECT.md only). No CI-skip token.
    6. Phase commit-history audit for the SUMMARY: `git log --format=%s <phase-start>..HEAD` — every subject matches `^(test|feat|docs|refactor|chore)\(04-0[1-8]\)` (plus plan 02's `docs(04-02)` and `feat(04-02)`), no `[ci skip]`, every `feat(04-NN)` for a tdd plan is preceded by at least one `test(04-NN)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && (GOTOOLCHAIN=go1.26.6 task docs:cli:drift > /tmp/04-08-drift.txt 2>&1; echo "exit=$?" >> /tmp/04-08-drift.txt); rg -q '^exit=0$' /tmp/04-08-drift.txt && rg -q 'compared 1 generated file' /tmp/04-08-drift.txt && rg -q 'byte-identical to a fresh regeneration' /tmp/04-08-drift.txt && rg -q -- '--color string' docs/CLI-REFERENCE.md && test "$(rg -v '^#' internal/cli/testdata/cli-reference-allowlist.txt | rg -c '\S')" = "3" && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryRegisteredFlagIsAccountedFor$|TestPlainGolden$|TestEveryCommandHasGroupID$|TestShortFlagsConsistent$' -v 2>&1 | tee /tmp/04-08-t3.txt | tail -3; rg -q '^ok\s' /tmp/04-08-t3.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/04-08-t3.txt && rg -q 'walked (3[7-9]|[4-9][0-9]) commands \(hidden included\), inspected [0-9]+ flags: [0-9]+ accepted via \.\./\.\./docs/CLI-REFERENCE\.md, 3 accepted via testdata/cli-reference-allowlist\.txt' /tmp/04-08-t3.txt && test "$(rg -c '^\s+--- PASS: TestPlainGolden/' /tmp/04-08-t3.txt)" -ge 28 && rg -q 'fang' .planning/PROJECT.md && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/ cmd/ test/ tools/)" && GOTOOLCHAIN=go1.26.6 go vet ./... && GOTOOLCHAIN=go1.26.6 task test:golden > /tmp/04-08-golden.txt 2>&1 && GOTOOLCHAIN=go1.26.6 go test ./test/integration/... -count=1 2>&1 | tee /tmp/04-08-integ.txt | tail -2; rg -q '^ok\s' /tmp/04-08-integ.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/04-08-integ.txt && GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1 2>&1 | tee /tmp/04-08-wo.txt | tail -2; rg -q '^ok\s' /tmp/04-08-wo.txt && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/present/archtest/... -count=1 2>&1 | tail -1 | rg -q '^ok\s' && GOTOOLCHAIN=go1.26.6 go test -race -count=1 ./internal/cli/ ./internal/cli/present/ 2>&1 | tee /tmp/04-08-race.txt | tail -2; ! rg -q 'DATA RACE|FAIL' /tmp/04-08-race.txt && S=$(git log --format=%H -E --grep='^test\(04-01\): ' -1) && test -n "$S" && test -z "$(git diff --name-only "$S^..HEAD" -- internal/query internal/mcp testdata/wireoracle testdata/golden)" && git log -1 --format=%s | rg -q '^docs\(04-08\): ' && ! git log --format=%B "$S^..HEAD" | rg -qi 'ci skip|skip ci' && test -z "$(git status --porcelain)" && echo "drift green (3 allowlisted, 37+ commands), goldens 28/28, integration+wireoracle+archtest+race green, internal/query & mcp untouched all phase, tree clean"</automated>
    <fails_when>non-zero exit; docs:cli:drift recording a non-zero exit or lacking "compared 1 generated file" / "byte-identical to a fresh regeneration"; `--color string` absent from docs/CLI-REFERENCE.md; the allowlist not holding exactly 3 non-comment entries; "--- FAIL"/"FAIL" in /tmp/04-08-t3.txt; the flag-accounting summary line missing, reporting fewer than 37 commands, or not "3 accepted via" the allowlist; fewer than 28 golden subtests; "fang" absent from PROJECT.md; build/gofmt/vet red; task test:golden non-zero; integration, wire-oracle or archtest packages not "ok"; "DATA RACE"/"FAIL" in the race run; any file under internal/query, internal/mcp, testdata/wireoracle or testdata/golden changed since the phase's first commit; HEAD not a "docs(04-08):" commit; a CI-skip token anywhere in the phase's commit messages; or a dirty working tree</fails_when>
  </verify>
  <acceptance_criteria>
    - `D=$(git log --format=%H -E --grep='^docs\(04-08\): ' -1) && git diff "$D^..$D" -- docs/CLI-REFERENCE.md` contains only `+` lines that mention `--color string` (plus blank-line/`Options inherited from parent commands` context) and zero `-` lines
    - `internal/cli/testdata/cli-reference-allowlist.txt` is unchanged (3 entries)
    - `.planning/PROJECT.md`'s `## Key Decisions` table has exactly one new row and no new heading (`D=$(git log --format=%H -E --grep='^docs\(04-08\): ' -1) && git diff "$D^..$D" -- .planning/PROJECT.md` shows one `+|` line and no `+#` line)
    - `GOTOOLCHAIN=go1.26.6 task docs:cli:drift` exits 0; `TestEveryRegisteredFlagIsAccountedFor` reports `3 accepted via testdata/cli-reference-allowlist.txt`
    - Every phase gate listed in the action passes at HEAD and its tail is pasted in the SUMMARY; the phase commit-history audit lists every subject with its plan scope
  </acceptance_criteria>
  <done>The generated reference carries `--color` through the drift gate, the fang verdict is a Key Decisions row, and every phase gate is green at HEAD with the agent path provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `os.Args` → process env (adopted branch) | a user-controlled flag value is mirrored into env vars fang reads — closed enum only |
| cobra help metadata → styled writer | command names, Short/Long and flag usage strings (repo-owned, not user input) are styled |
| generated docs ↔ live tree | `docs/CLI-REFERENCE.md` must be regenerated, never hand-edited |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-26 | Tampering (V5 input validation) | `applyColorFlagToProcessEnv` (adopted only) | medium | mitigate | only `auto|always|never` are acted on; any other value leaves the env untouched and is rejected by cobra's parse; only the three documented keys are ever set/unset; table-tested incl. bogus and last-wins |
| T-04-27 | Elevation of Privilege | GroupID → cobra panic on unregistered id | low | mitigate | ids come from four consts and one table; the guard asserts membership in `root.Groups()`; cobra's `checkCommandGroups` is the backstop |
| T-04-28 | Tampering | docs/CLI-REFERENCE.md | medium | mitigate | regenerated via `task docs:cli` only; `task docs:cli:drift` green; diff reviewed for `--color`-only hunks; no allowlist growth |
| T-04-29 | Denial of Service | `--help` on a TTY triggering the background query | low | accept | `resolveColor(c)` in the help func follows the same gate (both TTYs, at most once); on a pipe no query — accepted as the same cost every styled verb pays |
| T-04-SC | Tampering | new module installs | low | accept | none in this plan |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 task docs:cli:drift` green; `TestEveryRegisteredFlagIsAccountedFor` green with 3 allowlisted; `TestEveryCommandHasGroupID` green (≥24 inspected)
- full phase gates at HEAD: build, gofmt, vet, `task test:unit`, `task test:golden`, integration, wire oracle, archtest, `-race` over cli+present, govulncheck zero-new
- `git diff --name-only <first phase commit>^..HEAD -- internal/query internal/mcp testdata/wireoracle testdata/golden` empty
- human UAT items harvested from the `<human-check>` blocks (plans 03 and 08) → `04-UAT.md` at end of phase
</verification>

<success_criteria>
- CLI-06: four titled groups per D-13 with `help`/`completion` filed, `<verb> --help` styled consistently with root, guard RED-first and RED against a planted ungrouped command
- CLI-01/CLI-05: every verb styled; plain/`--json`/MCP paths byte-identical all phase; reference regenerated through the drift gate
- CLI-02/CLI-04: the two expected `human_needed` items are queued with exact instructions, not silently passed
- D-04: the fang verdict is recorded in PROJECT.md's Key Decisions
</success_criteria>

<output>
Create `.planning/phases/04-cli-glow-up/04-08-SUMMARY.md` when done (paste the RED `--- FAIL` lines for the GroupID guard and the help test, the reference diff stats, every phase-gate tail, and the commit-history audit table).
</output>
