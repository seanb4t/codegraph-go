---
phase: 04-cli-glow-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/plain_golden_test.go
  - internal/cli/testdata/plain/status.golden
  - internal/cli/testdata/plain/search.golden
  - internal/cli/testdata/plain/search-full.golden
  - internal/cli/testdata/plain/callers.golden
  - internal/cli/testdata/plain/callees.golden
  - internal/cli/testdata/plain/impact.golden
  - internal/cli/testdata/plain/affected-empty.golden
  - internal/cli/testdata/plain/files-flat.golden
  - internal/cli/testdata/plain/files-tree.golden
  - internal/cli/testdata/plain/explore.golden
  - internal/cli/testdata/plain/node-symbol.golden
  - internal/cli/testdata/plain/node-file.golden
  - internal/cli/testdata/plain/version.golden
  - internal/cli/testdata/plain/telemetry.golden
  - internal/cli/testdata/plain/init.golden
  - internal/cli/testdata/plain/index-force.golden
  - internal/cli/testdata/plain/sync.golden
  - internal/cli/testdata/plain/uninit-force.golden
  - internal/cli/testdata/plain/githooks-install.golden
  - internal/cli/testdata/plain/githooks-status.golden
  - internal/cli/testdata/plain/githooks-remove.golden
  - internal/cli/testdata/plain/install-local.golden
  - internal/cli/testdata/plain/uninstall-local.golden
  - internal/cli/testdata/plain/daemon-list-empty.golden
  - internal/cli/testdata/plain/daemon-stop-nomatch.golden
  - internal/cli/testdata/plain/daemon-unlock.golden
  - internal/cli/testdata/plain/upgrade-refresh-warning.golden
  - internal/cli/testdata/plain/ui-url.golden
  - internal/cli/testdata/plain/serve-mcp-stderr.golden
  - internal/cli/short_flags_test.go
  - .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md
autonomous: true
requirements: [CLI-05, CLI-07]
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`internal/cli/testdata/plain/*.golden` holds the byte-exact, normalized plain stdout (stderr for the one `serve --mcp` case) of every verb that gains a styled branch this phase — status, search, search --full, callers, callees, impact, affected, files (flat and tree), explore, node (symbol and file mode), version, telemetry, init, index --force, sync, uninit --force, githooks install/status/remove, install, uninstall, daemon (list, stop, unlock), upgrade, ui, serve --mcp — captured through `execCmd`'s non-TTY `bytes.Buffer` against the existing gofixture/fakeHome/initGitRepo corpus BEFORE any renderer, resolver or palette lands, in this plan's first commit (D-16, CLI-05)."
    - "`TestPlainGolden` asserts, for every case, that the current normalized output is byte-equal to its frozen golden AND contains zero ESC (0x1b) bytes; it is positive-floored (at least 28 cases, every `testdata/plain/*.golden` file has exactly one case and every case has exactly one file — set equality, never a bare count) so a table that silently shrinks or an orphaned golden cannot read as green (rule 84d1gfpywd). D-16."
    - "`TestNoColorNonTTYRegression` re-runs the same table under `NO_COLOR=1` and under `NO_COLOR=banana` and asserts byte-equality to the SAME goldens plus zero ESC bytes — the gh #13335 lesson pinned end-to-end and, for `banana`, the D-09 research correction pinned before the resolver exists (today `ChoosePresentation` treats any non-empty `NO_COLOR` as off; after plan 03 the resolver's normalization keeps it so). CLI-05, D-09."
    - "Normalization is explicit, minimal and documented per case: the copied fixture/home/project temp-dir absolute paths → `<FIXTURE>`/`<HOME>`/`<PROJECT>`; `DB Size:` value → `<SIZE>`; `duration=<d>` → `duration=<DURATION>`; the `version` line's runtime values (from `version.Info()`) → `<VERSION>`/`<COMMIT>`/`<DATE>`/`<GO>`/`<OS>/<ARCH>`; the `ui` URL port → `<PORT>`. Nothing else is rewritten — a normalizer that rewrote whole lines would freeze nothing. D-16."
    - "`TestShortFlagsConsistent` walks the real cobra tree and, for every Query-the-graph verb (explore, search, node, callers, callees, impact, affected, files, status — D-13's group membership), asserts that each of `--json`, `--limit`, `--kind`, `--path` that the verb registers ALSO has its short form `-j`, `-l`, `-k`, `-p` (`pflag.Flag.Shorthand`), reporting a positive count of (verb, flag) pairs checked (at least 18) so a walk that inspects nothing cannot pass; no new short flag is added and `-d`/`-m` stay absent (D-12, CLI-07)."
    - "`04-MUTATION-LOG.md` exists in the Phase 2/3 shape (pre-mutation cleanliness gate, 'What are we testing, and why?', verbatim observed output, revert + byte-clean proof) with Family (a) — an ESC byte planted into `status.go`'s plain branch turns `TestPlainGolden/status` RED on both assertions — and Family (b) — `callers.go`'s `--limit` shorthand removed turns `TestShortFlagsConsistent` RED naming `callers --limit` — each reverted byte-clean (D-16, D-12)."
    - "CLI-01 (probe boundary, explicit): the goldens freeze the engine's own counts at 0, 1 and N results on the gofixture (callers=1, impact=3, affected=0, files=4) so a renderer that later introduces a threshold of its own diverges from a frozen value."
    - "CLI-01 (probe precision, explicit): no numeric rendering changes are made this phase — `formatNumber`/`formatMB` in `present/status.go` and `internal/query` are untouched, and the status/init/index/sync goldens (which carry every number the CLI prints) stay byte-identical through the whole phase."
    - "CLI-05 (probe unclassified, surfaced assumption): byte-identity is defined over the plain path's STDOUT bytes with `NO_COLOR` unset, `=1` and `=banana`, plus stderr for `serve --mcp`'s banner only; `--json` output is covered by the existing golden oracle (`task test:golden`) and the wire oracle, not re-frozen here. This scoping is an assumption recorded here, not a proven property."
    - "CLI-07 (probe unclassified, surfaced assumption): 'query verbs' is read as D-13's Query-the-graph group; `node`'s `-l` is `--line` (it has no `--limit`) and `explore` registers none of the four long forms, so both contribute zero pairs by design and the ≥18 floor is computed from the other seven verbs."
    - statement: "Every golden was reviewed by eye in the commit diff for a leaked absolute path, timestamp, port or machine-specific string that the normalizers missed (a golden that only passes on this machine is a latent CI red)."
      verification: backstop
  artifacts:
    - "internal/cli/plain_golden_test.go — `updatePlainGoldens` flag (`-update-plain-goldens`), `plainCase` struct, `plainCases(t)`, `normalizePlain`, `TestPlainGolden`, `TestNoColorNonTTYRegression`"
    - "internal/cli/testdata/plain/*.golden — 28 frozen plain outputs (one per case above)"
    - "internal/cli/short_flags_test.go — `TestShortFlagsConsistent`, `queryVerbNames` (the nine D-13 Query-the-graph names), `shortFlagPairs` (json→j, limit→l, kind→k, path→p)"
    - ".planning/phases/04-cli-glow-up/04-MUTATION-LOG.md — Families (a) and (b)"
  key_links:
    - "`plainCases` → `execCmd`/`execCmdWithInput` (cli_test.go) + `copyFixture`/`setupIndexedFixture` (query_cli_test.go) + `fakeHome` (install_test.go) + `initGitRepo` (githooks_test.go) + `upgradeRunFunc`/`refreshInstalledSkillsFunc` seams (upgrade.go) + the io.Pipe/cancel shape of `TestUICommandPrintsConnectableURLBeforeServing` (ui_test.go) — the existing corpus, reused, never a second harness"
    - "`TestPlainGolden` reads `testdata/plain/<name>.golden` via `os.ReadFile(filepath.Join(\"testdata\", \"plain\", name+\".golden\"))` — the same package-relative testdata convention `cliReferenceAllowlistPath` already uses"
    - "`TestShortFlagsConsistent` → `cliReferenceTree()` (cli_reference_test.go) → walks `root.Commands()` → `cmd.Flags().Lookup(long).Shorthand`"
  prohibitions:
    - statement: "MUST NOT capture any golden through a styled or TTY path, and MUST NOT normalize away content (only the enumerated path/size/duration/version/port tokens) — a golden that has been 'cleaned' beyond those tokens freezes less than the requirement promises. D-16."
      status: resolved
      verification: judgment
    - statement: "MUST NOT add, rename or remove any short flag while pinning CLI-07 — D-12 records the invariant as already true; `-d`/`-m` are explicitly not added."
      status: resolved
      verification: test
    - statement: "MUST NOT edit `internal/query`, `internal/mcp`, `test/wireoracle`, `testdata/wireoracle` or any production file in this plan (the only production-file edits are the two planted mutations, each reverted byte-clean before the plan's commit)."
      status: resolved
      verification: test
---

<objective>
Freeze the bar before anything is measured: capture every human-output verb's plain, non-TTY
stdout to `internal/cli/testdata/plain/<case>.golden` from the CURRENT tree (before any renderer,
palette, resolver or `--color` flag exists), pin it with a byte-equality + zero-ESC test that also
runs under `NO_COLOR=1` and `NO_COLOR=banana`, pin the already-true CLI-07 short-flag invariant
with a walk-the-tree table test, and prove both guards RED against a planted mutation in
`04-MUTATION-LOG.md`.

Purpose: CLI-05's byte-identity promise ("the agent/MCP path, `--json` and piped output are
byte-identical before and after the glow-up") is only checkable if "before" is frozen first — the
GRF-09 "bar before measurement" discipline D-16 names. Every later plan's `<verify>` runs
`TestPlainGolden`; a renderer that leaks an escape byte, reorders a line or paraphrases a
wording onto the plain path is caught by a frozen file, not by a reviewer's memory. CLI-07 needs
no implementation (D-12: every `--json/--limit/--kind/--path` on a query verb already carries its
short) — the deliverable is the pinning test plus its RED demonstration.

Output: one `test(04-01):` commit carrying the goldens, the two test files and the mutation log
(Families (a) and (b)). No production source file changes in this plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-glow-up/04-CONTEXT.md
@.planning/phases/04-cli-glow-up/04-PATTERNS.md
@.planning/phases/04-cli-glow-up/04-VALIDATION.md
@.planning/phases/03-verb-fold/03-MUTATION-LOG.md
@internal/cli/cli_test.go
@internal/cli/query_cli_test.go
@internal/cli/cli_reference_test.go
@test/integration/status_files_plain_test.go
</context>

## Artifacts this phase produces

New symbols/files created across Phase 4 (all plans), listed so the source-grounding pass
excludes them from drift checks:

- `internal/cli/colorflag.go`: `colorFlagName`, `colorChoice` (pflag.Value: `String`/`Set`/`Type`), `colorAuto`/`colorAlways`/`colorNever`, `addColorFlag`, `colorChoiceOf`, `rewriteEnviron`, `colorMode` (`Styled`, `Profile`, `Dark`), `resolveColor`, `resolveColorFrom`, `resolveColorStderr`, `(colorMode).Writer`, seams `fdIsTerminal`, `queryDarkBackground`
- `internal/cli/colorflag_test.go`: `TestResolveColorMatrix`, `TestRewriteEnviron`, `TestColorFlagInvalidValue`, `TestDarkBackgroundQueryGate`
- `internal/cli/plain_golden_test.go`: `updatePlainGoldens`, `plainCase`, `plainCases`, `normalizePlain`, `TestPlainGolden`, `TestNoColorNonTTYRegression`; `internal/cli/testdata/plain/*.golden`
- `internal/cli/short_flags_test.go`: `TestShortFlagsConsistent`, `queryVerbNames`, `shortFlagPairs`
- `internal/cli/cli_reference_test.go`: `TestEveryCommandHasGroupID`, `commandGroupIDs`
- `internal/cli/root.go`: `commandGroups` (name→group id map), `groupQuery`/`groupBuild`/`groupAgents`/`groupMaintenance` ids, `installHelpFunc` (declined branch) or the fang wrap in `Execute()` (adopted branch), `applyColorFlagToProcessEnv` (adopted branch only)
- `internal/cli/present/palette.go`: `Role`, `RoleHeader`/`RoleLabel`/`RoleValue`/`RolePath`/`RoleCount`/`RoleWarning`/`RoleError`, `Palette` (7 `lipgloss.Style` fields), `NewPalette(dark bool)`, `(Palette).Style(Role)`, `paletteHex` table
- `internal/cli/present/palette_test.go`: `TestNewPaletteRolesFollowDark`
- `internal/cli/present/line.go`: `Line`, `Lines`, `KV`, `LineWriter`/`NewLineWriter`
- `internal/cli/present/line_test.go`: `TestLineSanitizesAndStyles`, `TestLineWriterStylesEachLine`
- `internal/cli/present/results.go`: `RenderSearch`, `RenderSearchFull`, `RenderCallers`, `RenderCallees`, `RenderImpact`, `RenderAffected`, `RenderNotice`, `writeLocationLine`
- `internal/cli/present/results_test.go`: `TestRenderResultsStrippedEqualsPlain`, `stripANSI` (test helper)
- `internal/cli/present/explore.go`: `RenderExplore`, `writeNumberedSource`
- `internal/cli/present/node.go`: `RenderNode`, `writeNodeSection`, `nodeMultiDefHardCap`/`nodeMultiDefBodyBudget`/`nodeMultiDefListCap` (package-local duplicates)
- `internal/cli/present/explore_test.go`, `node_test.go`: `TestRenderExploreStrippedEqualsMarkdownContract`, `TestRenderNodeStrippedEqualsMarkdownContract`, `markdownToPlainContract` (test helper)
- `internal/cli/present/help.go` (declined branch only): `RenderHelp`
- `internal/cli/present/help_test.go` (declined branch only): `TestRenderHelpGroupsInOrder`
- `internal/cli/install.go`: styled branch inside `printAgentResults`
- `.planning/phases/04-cli-glow-up/04-FANG-VERDICT.md`, `.planning/phases/04-cli-glow-up/04-MUTATION-LOG.md`
- `go.mod`: `github.com/charmbracelet/colorprofile` promoted to direct; `charm.land/fang/v2 v2.0.1` only if adopted
- The persistent `--color` flag on root (`auto|always|never`)

<tasks>

<task type="auto">
  <name>Task 1: Golden harness + the twelve query-verb plain goldens</name>
  <files>internal/cli/plain_golden_test.go, internal/cli/testdata/plain/status.golden, internal/cli/testdata/plain/search.golden, internal/cli/testdata/plain/search-full.golden, internal/cli/testdata/plain/callers.golden, internal/cli/testdata/plain/callees.golden, internal/cli/testdata/plain/impact.golden, internal/cli/testdata/plain/affected-empty.golden, internal/cli/testdata/plain/files-flat.golden, internal/cli/testdata/plain/files-tree.golden, internal/cli/testdata/plain/explore.golden, internal/cli/testdata/plain/node-symbol.golden, internal/cli/testdata/plain/node-file.golden</files>
  <read_first>
    - internal/cli/cli_test.go (copyFixture, execCmd, execCmdWithInput — the in-process harness; stdout is a bytes.Buffer, never a TTY)
    - internal/cli/query_cli_test.go (setupIndexedFixture; the existing search/callers/callees/impact/affected/files/node/explore invocations to copy args from)
    - internal/cli/cli_reference_test.go (cliReferenceAllowlistPath — the package-relative testdata path convention)
    - test/integration/status_files_plain_test.go (the zero-ESC assertion shape and the --path rationale to copy)
    - internal/cli/status.go (the plain branch that prints `Project: <path>` and `DB Size:`)
    - .planning/phases/04-cli-glow-up/04-CONTEXT.md (D-16 verbatim)
  </read_first>
  <action>
    Create `internal/cli/plain_golden_test.go` (package `cli`, D-16, CLI-05):

    1. `var updatePlainGoldens = flag.Bool("update-plain-goldens", false, "rewrite internal/cli/testdata/plain/*.golden from the current plain output")` — the only sanctioned way to (re)write a golden; a golden is never hand-edited.
    2. `type plainCase struct { name string; setup func(t *testing.T) plainRun }` and `type plainRun struct { args []string; env []string; stderr bool; run func(t *testing.T) string; normalize func(string) string }`. Default execution: `t.Setenv` each `KEY=VALUE` in `env`, then `execCmd(args...)`; capture stdout, or stderr when `stderr` is true; when `run` is non-nil call it instead (used by the `ui` case in Task 2). `normalize` defaults to the identity.
    3. `func plainCases(t *testing.T) []plainCase` returns the full table. This task adds these twelve cases, each calling `setupIndexedFixture(t)` (fresh gofixture copy + `init`) and normalizing the fixture dir with `strings.ReplaceAll(out, dir, "<FIXTURE>")`:
       - `status`: `status -p <dir>` — additionally `regexp.MustCompile(`DB Size:(\s+)[0-9.]+ MB`)` → `DB Size:${1}<SIZE> MB` (pebble's on-disk size is not byte-stable across runs; everything else on the status page is).
       - `search`: `search Alpha -p <dir>`; `search-full`: `search Alpha -p <dir> --full`.
       - `callers`: `callers Alpha -p <dir>`; `callees`: `callees Alpha -p <dir>`; `impact`: `impact Alpha -p <dir>`.
       - `affected-empty`: `affected pkga/pkga.go -p <dir>` (the fixture has no tests: the frozen wording is `no test files affected`).
       - `files-flat`: `files -p <dir>`; `files-tree`: `files -p <dir> --format tree`.
       - `explore`: `explore Alpha -p <dir>`; `node-symbol`: `node Alpha -p <dir>`; `node-file`: `node -p <dir> -f pkga/pkga.go`.
    4. `func normalizePlain(run plainRun, out string) string` applies `run.normalize` when set.
    5. `func TestPlainGolden(t *testing.T)`: for each case (subtest by name): `t.Setenv("NO_COLOR", "")` (unset semantics), run, normalize, then if `*updatePlainGoldens` write `testdata/plain/<name>.golden` (0o644, `os.MkdirAll` the dir) and `t.Logf` the path; else `os.ReadFile` the golden (a missing file is a hard `t.Fatalf` naming the `-update-plain-goldens` flag), assert `got == string(want)` (on mismatch print a unified line diff via `strings.Split` comparison — the first differing line number and both lines), and assert `!strings.Contains(got, "\x1b")`. After the loop: positive floors — `len(cases) >= 28` (Task 2 brings the table to 28; write the floor now, it goes green in Task 2 — until then run this task's verify with the interim floor 12 and raise it in Task 2), and set-equality between `{c.name}` and the basenames of `testdata/plain/*.golden` (`filepath.Glob`), failing on any orphan golden or case without a file.
    6. `func TestNoColorNonTTYRegression(t *testing.T)`: the same table run twice more, under `NO_COLOR=1` and under `NO_COLOR=banana`, asserting byte-equality to the same golden and zero ESC bytes (the gh #13335 lesson; `banana` pins the D-09 research correction end to end).

    Run `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -run 'TestPlainGolden$' -update-plain-goldens -count=1` ONCE to write the twelve goldens, then run without the flag to prove they compare green. Inspect every golden by eye: no absolute path, no `/var/folders`, no `T/tmp.` fragment, no timestamp may remain (the status golden must read `Project: <FIXTURE>` and `DB Size:   <SIZE> MB`).

    Do not touch any production file. Do not add a case for `node`'s multi-definition mode (the gofixture has no overloaded symbol; plan 05's unit test covers that shape from constructed structs).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test -z "$(git status --porcelain internal/cli/*.go internal/cli/present/ internal/query/ internal/mcp/ | rg -v '_test\.go')" && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestPlainGolden$|TestNoColorNonTTYRegression$' -v 2>&1 | tee /tmp/04-01-t1.txt | tail -3; rg -q '^ok\s' /tmp/04-01-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/04-01-t1.txt && test "$(rg -c '^\s+--- PASS: TestPlainGolden/' /tmp/04-01-t1.txt)" -ge 12 && test "$(ls internal/cli/testdata/plain/*.golden | wc -l | tr -d ' ')" -ge 12 && ! rg -l '/var/folders|/tmp/|T/tmp\.' internal/cli/testdata/plain/ && ! rg -l $'\x1b' internal/cli/testdata/plain/ && rg -q 'Project: <FIXTURE>' internal/cli/testdata/plain/status.golden && rg -q 'DB Size:\s+<SIZE> MB' internal/cli/testdata/plain/status.golden && echo "12+ goldens frozen, compare green under NO_COLOR unset/1/banana, no leaked paths, no ESC"</automated>
    <fails_when>non-zero exit; "--- FAIL:" or "FAIL" in /tmp/04-01-t1.txt; fewer than 12 "--- PASS: TestPlainGolden/" subtest lines; fewer than 12 golden files; any golden containing "/var/folders", "/tmp/" or "T/tmp."; any golden containing an ESC byte; the status golden lacking "Project: <FIXTURE>" or "DB Size: <SIZE> MB"; or a non-test production file dirty in the working tree</fails_when>
  </verify>
  <acceptance_criteria>
    - `internal/cli/plain_golden_test.go` contains `func TestPlainGolden(` and `func TestNoColorNonTTYRegression(` and `flag.Bool("update-plain-goldens"`
    - `internal/cli/testdata/plain/` contains exactly these twelve files after this task: status, search, search-full, callers, callees, impact, affected-empty, files-flat, files-tree, explore, node-symbol, node-file (each `.golden`, each non-empty)
    - `internal/cli/testdata/plain/status.golden` first line is `CodeGraph Status` and contains `Project: <FIXTURE>`
    - `internal/cli/testdata/plain/explore.golden` contains `**Exploration: Alpha**` and `pkga/pkga.go` and no `/var/folders`
    - `internal/cli/testdata/plain/node-symbol.golden` is exactly the six-line `**Alpha** (function)` … `**Called by ←** Run (pkgb/pkgb.go:8)` block
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -run 'TestPlainGolden$|TestNoColorNonTTYRegression$' -count=1` exits 0
    - `git status --porcelain` shows no modified non-test `.go` file
  </acceptance_criteria>
  <done>Twelve plain goldens frozen from the pre-glow-up tree, compared byte-exact with zero ESC under NO_COLOR unset, 1 and banana; the harness flag documented; no production file touched.</done>
</task>

<task type="auto">
  <name>Task 2: The sixteen lifecycle/agent/one-line goldens + Family (a) RED</name>
  <files>internal/cli/plain_golden_test.go, internal/cli/testdata/plain/version.golden, internal/cli/testdata/plain/telemetry.golden, internal/cli/testdata/plain/init.golden, internal/cli/testdata/plain/index-force.golden, internal/cli/testdata/plain/sync.golden, internal/cli/testdata/plain/uninit-force.golden, internal/cli/testdata/plain/githooks-install.golden, internal/cli/testdata/plain/githooks-status.golden, internal/cli/testdata/plain/githooks-remove.golden, internal/cli/testdata/plain/install-local.golden, internal/cli/testdata/plain/uninstall-local.golden, internal/cli/testdata/plain/daemon-list-empty.golden, internal/cli/testdata/plain/daemon-stop-nomatch.golden, internal/cli/testdata/plain/daemon-unlock.golden, internal/cli/testdata/plain/upgrade-refresh-warning.golden, internal/cli/testdata/plain/ui-url.golden, internal/cli/testdata/plain/serve-mcp-stderr.golden, .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md</files>
  <read_first>
    - internal/cli/plain_golden_test.go (Task 1's table — extend it, do not fork it)
    - internal/cli/install_test.go (fakeHome: HOME/XDG_CONFIG_HOME/HERMES_HOME + t.Chdir into a fresh project dir)
    - internal/cli/githooks_test.go (initGitRepo, runGit)
    - internal/cli/daemon_test.go (fakeHome usage for the bare `daemon` list; daemon stop tests)
    - internal/cli/upgrade_test.go (the upgradeRunFunc / refreshInstalledSkillsFunc seam-swap-and-restore shape)
    - internal/cli/ui_test.go (TestUICommandPrintsConnectableURLBeforeServing — the io.Pipe + ExecuteContext + cancel shape; copyGofixtureForCLI, indexGofixtureForCLI)
    - internal/cli/serve_test.go (TestServeWatchStartDisabledPrintsVerbatimMessage — the exact banner text)
    - internal/cli/init.go (printSummary's `duration=` token; printWatchFallbackAdvisory's CODEGRAPH_NO_WATCH seam)
    - internal/cli/version.go (versionLine / version.Info() fields)
    - .planning/phases/03-verb-fold/03-MUTATION-LOG.md (the Family entry shape to reproduce verbatim)
  </read_first>
  <action>
    Extend `plainCases` with these sixteen cases (D-16 covers "every verb that gains a styled branch" — CLI-01's full list), then raise `TestPlainGolden`'s floor to `len(cases) >= 28`:

    - `version`: `version`; normalize by replacing the exact current line `fmt.Sprintf("codegraph %s (commit %s, built %s) %s %s/%s\n", i.Version, i.Commit, i.Date, i.GoVersion, i.OS, i.Arch)` (from `version.Info()`) with `codegraph <VERSION> (commit <COMMIT>, built <DATE>) <GO> <OS>/<ARCH>\n` — replace the whole formatted line, never the individual tokens (`Commit` and `Date` are both `unknown` in a dev build and would collide).
    - `telemetry`: `telemetry` (fixed text; identity normalizer).
    - `init`: fresh `copyFixture(t)` (NOT indexed), env `CODEGRAPH_NO_WATCH=1` (forces the watch-fallback advisory deterministically; the fixture is not a git repo so the advisory's third line is `Run \`codegraph sync\` after changing files to refresh the index.`), args `init <dir>`; normalize `<dir>` → `<FIXTURE>` and `regexp.MustCompile(`duration=\S+`)` → `duration=<DURATION>`.
    - `index-force`: `setupIndexedFixture` then `index <dir> --force`; same two normalizers.
    - `sync`: `setupIndexedFixture` then `sync <dir>`; same two normalizers (two lines: the summary and the `reparsed=0 pruned=0 …` line).
    - `uninit-force`: `setupIndexedFixture` then `uninit <dir> --force`; normalize `<dir>` (expected `removed <FIXTURE>/.codegraph`).
    - `githooks-install`, `githooks-status`, `githooks-remove`: `dir := initGitRepo(t, t.TempDir())` (or the helper's own signature — read it), args `githooks install <dir>` / `githooks status <dir>` (run AFTER an install in the same setup so the frozen states read `installed`) / `githooks remove <dir>` (after install); normalize `<dir>` → `<FIXTURE>`.
    - `install-local`: `fakeHome(t)` (returns home; t.Chdir'd into a project dir — capture that with `os.Getwd()`), args `install --target claude-code --location local`; normalize home → `<HOME>` and project → `<PROJECT>`. `uninstall-local`: same setup, run `install …` first (discard), then `uninstall --target claude-code --location local`.
    - `daemon-list-empty`: `fakeHome(t)`, args `daemon`. `daemon-stop-nomatch`: `fakeHome(t)` + `setupIndexedFixture`, args `daemon stop -p <dir>` (empty registry → `no running daemon for <FIXTURE>`), normalize dir. `daemon-unlock`: `setupIndexedFixture`, args `daemon unlock <dir>` (no lock → `no lock present at <FIXTURE>/.codegraph/daemon.lock — nothing to do`), normalize dir.
    - `upgrade-refresh-warning`: swap `upgradeRunFunc` to return nil and `refreshInstalledSkillsFunc` to return `errors.New("simulated refresh failure")` (restore both in `t.Cleanup`, exactly as upgrade_test.go does), args `upgrade`; identity normalizer — the frozen text is the two-line `warning: codegraph upgrade succeeded, but refreshing the installed agent skill package failed: …` message.
    - `ui-url`: `run` override reproducing TestUICommandPrintsConnectableURLBeforeServing's shape (io.Pipe, `newUiCmd()` with `--path <dir> --no-open`, read the first line, cancel, wait ≤6s); normalize `regexp.MustCompile(`http://127\.0\.0\.1:\d+`)` → `http://127.0.0.1:<PORT>`.
    - `serve-mcp-stderr`: `fakeHome(t)` + `setupIndexedFixture`, env `CODEGRAPH_NO_WATCH=1`, args `serve --mcp -p <dir>`, `stderr: true` (execCmd's empty stdin makes the MCP loop exit on EOF; the frozen stderr is the one-line verbatim `[CodeGraph MCP] File watcher disabled — CODEGRAPH_NO_WATCH=1 is set. …` banner serve_test.go pins).

    Write the goldens with `-update-plain-goldens` once, then run green without it. Eyeball every new golden for leaked machine-specific strings.

    Then create `.planning/phases/04-cli-glow-up/04-MUTATION-LOG.md` in the 03-MUTATION-LOG.md shape (header, the pre-mutation cleanliness-gate paragraph, then Families) and record **Family (a) — D-16/CLI-05: a planted ESC byte turns TestPlainGolden RED**: gate `git diff --quiet -- internal/cli/status.go`; mutation via `perl -0pi` prefixing the plain-branch `query.RenderStatusText(result, start)` argument with the literal Go string `"\x1b[1m"+` in `internal/cli/status.go`; observed: `--- FAIL: TestPlainGolden/status` with BOTH the byte-mismatch and the ESC-present messages in the transcript, and `--- FAIL: TestNoColorNonTTYRegression/status` too; revert `git checkout -- internal/cli/status.go`; byte-clean proof `git diff --quiet -- internal/cli/status.go`. Paste the verbatim `--- FAIL` lines.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rm -f /tmp/04-01-red-a.txt && git diff --quiet -- internal/cli/status.go && perl -0pi -e 's/fmt\.Fprint\(cmd\.OutOrStdout\(\), query\.RenderStatusText\(result, start\)\)/fmt.Fprint(cmd.OutOrStdout(), "\\x1b[1m"+query.RenderStatusText(result, start))/' internal/cli/status.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestPlainGolden$|TestNoColorNonTTYRegression$' -v > /tmp/04-01-red-a.txt 2>&1; echo "exit=$?" >> /tmp/04-01-red-a.txt); git checkout -- internal/cli/status.go; git diff --quiet -- internal/cli/status.go && rg -q -- '--- FAIL: TestPlainGolden/status' /tmp/04-01-red-a.txt && rg -q -- '--- FAIL: TestNoColorNonTTYRegression/status' /tmp/04-01-red-a.txt && ! rg -q '^exit=0$' /tmp/04-01-red-a.txt && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestPlainGolden$|TestNoColorNonTTYRegression$' -v 2>&1 | tee /tmp/04-01-t2.txt | tail -3; rg -q '^ok\s' /tmp/04-01-t2.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/04-01-t2.txt && test "$(rg -c '^\s+--- PASS: TestPlainGolden/' /tmp/04-01-t2.txt)" -ge 28 && test "$(ls internal/cli/testdata/plain/*.golden | wc -l | tr -d ' ')" -ge 28 && ! rg -l '/var/folders|/tmp/|T/tmp\.' internal/cli/testdata/plain/ && rg -q 'Family \(a\)' .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md && echo "Family (a) RED on planted ESC, status.go reverted byte-clean; 28 goldens green"</automated>
    <fails_when>non-zero exit; the planted run's transcript lacking "--- FAIL: TestPlainGolden/status" or "--- FAIL: TestNoColorNonTTYRegression/status" or recording exit=0; status.go not byte-clean after the revert; the green run showing "--- FAIL"/"FAIL" or fewer than 28 "--- PASS: TestPlainGolden/" lines; fewer than 28 golden files; a leaked temp path in any golden; or "Family (a)" absent from 04-MUTATION-LOG.md</fails_when>
  </verify>
  <acceptance_criteria>
    - `internal/cli/testdata/plain/` contains 28 `.golden` files, including `version.golden` reading exactly `codegraph <VERSION> (commit <COMMIT>, built <DATE>) <GO> <OS>/<ARCH>` plus newline, `init.golden` containing `duration=<DURATION>` and `Live file watching is disabled here — CODEGRAPH_NO_WATCH=1 is set.`, `serve-mcp-stderr.golden` containing `[CodeGraph MCP] File watcher disabled — CODEGRAPH_NO_WATCH=1 is set.`, `ui-url.golden` reading `http://127.0.0.1:<PORT>` plus newline, `daemon-unlock.golden` containing `no lock present at <FIXTURE>/.codegraph/daemon.lock`
    - `internal/cli/plain_golden_test.go` contains the literal `>= 28`
    - `.planning/phases/04-cli-glow-up/04-MUTATION-LOG.md` contains `## Family (a)` with a pasted `--- FAIL: TestPlainGolden/status` line and a `git diff --quiet` byte-clean statement
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -run 'TestPlainGolden$|TestNoColorNonTTYRegression$' -count=1` exits 0 with 28 subtests passing
  </acceptance_criteria>
  <done>All 28 plain goldens frozen and green; the golden test demonstrated RED against a planted escape byte and the mutation reverted byte-clean, recorded as Family (a).</done>
</task>

<task type="auto">
  <name>Task 3: CLI-07 short-flag pinning test + Family (b) RED + the plan's single commit</name>
  <files>internal/cli/short_flags_test.go, .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md</files>
  <read_first>
    - internal/cli/cli_reference_test.go (cliReferenceTree, the `walk` idiom over cmd.Commands() and cmd.Flags().VisitAll, documentedByReference)
    - internal/cli/search.go, internal/cli/callers.go, internal/cli/node.go, internal/cli/explore.go, internal/cli/files.go, internal/cli/status.go (the current StringVarP/IntVarP/BoolVarP registrations — D-12's discovery finding)
    - .planning/phases/04-cli-glow-up/04-CONTEXT.md (D-12, D-13's Query-the-graph membership)
    - .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md (Task 2's Family (a) — append Family (b) after it)
  </read_first>
  <action>
    Create `internal/cli/short_flags_test.go` (package `cli`, D-12, CLI-07):

    - `var queryVerbNames = []string{"explore", "search", "node", "callers", "callees", "impact", "affected", "files", "status"}` — exactly D-13's Query-the-graph group.
    - `var shortFlagPairs = map[string]string{"json": "j", "limit": "l", "kind": "k", "path": "p"}`.
    - `func TestShortFlagsConsistent(t *testing.T)`: build `cliReferenceTree()`, index `root.Commands()` by `Name()`, `t.Fatalf` if any `queryVerbNames` entry is missing from the tree (the tree changed under the test). For each query verb and each long name in `shortFlagPairs`: `f := cmd.Flags().Lookup(long)`; if nil, continue (the long form does not exist on this verb — `explore` has none of the four, `node` has `--path` only); else assert `f.Shorthand == want` with `t.Errorf("%s --%s has shorthand %q, want -%s", verb, long, f.Shorthand, want)`. Count every (verb, long) pair actually inspected and finish with `if checked < 18 { t.Fatalf(...) }` plus `t.Logf("inspected %d (verb, flag) pairs across %d query verbs", checked, len(queryVerbNames))` — the positive assertion (rule 84d1gfpywd; today's tree yields 20: search 4, callers 3, callees 3, impact 2, affected 2, files 2, status 2, node 1, explore 0 — recompute from the real tree and state the observed number in the SUMMARY, do not hardcode 20 as the floor).

    Record **Family (b) — D-12/CLI-07: a long-only flag turns TestShortFlagsConsistent RED** in `04-MUTATION-LOG.md`: gate `git diff --quiet -- internal/cli/callers.go`; mutation `perl -0pi -e 's/IntVarP\(&limit, "limit", "l", 0,/IntVar(&limit, "limit", 0,/' internal/cli/callers.go`; observed `--- FAIL: TestShortFlagsConsistent` with a message naming `callers --limit`; revert `git checkout -- internal/cli/callers.go`; byte-clean proof. Paste the verbatim FAIL line.

    Then commit everything from Tasks 1–3 as ONE commit `test(04-01): freeze plain goldens, pin NO_COLOR non-TTY regression and short-flag invariant` (test files, 28 goldens, mutation log; the commit body names D-16, D-12, CLI-05, CLI-07 and the two Families). No `[ci skip]`. No production source file may be in the commit.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rm -f /tmp/04-01-red-b.txt && git diff --quiet -- internal/cli/callers.go && perl -0pi -e 's/IntVarP\(&limit, "limit", "l", 0,/IntVar(&limit, "limit", 0,/' internal/cli/callers.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestShortFlagsConsistent$' -v > /tmp/04-01-red-b.txt 2>&1; echo "exit=$?" >> /tmp/04-01-red-b.txt); git checkout -- internal/cli/callers.go; git diff --quiet -- internal/cli/callers.go && rg -q -- '--- FAIL: TestShortFlagsConsistent' /tmp/04-01-red-b.txt && rg -q 'callers --limit' /tmp/04-01-red-b.txt && ! rg -q '^exit=0$' /tmp/04-01-red-b.txt && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestShortFlagsConsistent$' -v 2>&1 | tee /tmp/04-01-t3.txt | tail -3; rg -q '^ok\s' /tmp/04-01-t3.txt && rg -q 'inspected (1[89]|[2-9][0-9]) \(verb, flag\) pairs' /tmp/04-01-t3.txt && rg -q 'Family \(b\)' .planning/phases/04-cli-glow-up/04-MUTATION-LOG.md && git log -1 --format=%s | rg -q '^test\(04-01\): ' && test -z "$(git show --format= --name-only HEAD | rg -v '_test\.go$|testdata/plain/.*\.golden$|04-MUTATION-LOG\.md$')" && ! git log -1 --format=%B | rg -qi 'ci skip|skip ci' && test -z "$(git status --porcelain internal/ cmd/ docs/ go.mod)" && echo "Family (b) RED, callers.go byte-clean, ≥18 pairs inspected, one test(04-01) commit of tests+goldens+log only"</automated>
    <fails_when>non-zero exit; the planted run lacking "--- FAIL: TestShortFlagsConsistent" or the "callers --limit" name or recording exit=0; callers.go not byte-clean after revert; the green run lacking "ok" or an "inspected N (verb, flag) pairs" line with N ≥ 18; "Family (b)" absent from the log; HEAD's subject not starting with "test(04-01): "; any file in HEAD outside *_test.go / testdata/plain/*.golden / 04-MUTATION-LOG.md; a CI-skip token in the message; or a dirty tree under internal/, cmd/, docs/ or go.mod</fails_when>
  </verify>
  <acceptance_criteria>
    - `internal/cli/short_flags_test.go` contains `func TestShortFlagsConsistent(`, the nine-name `queryVerbNames` slice and the four-entry `shortFlagPairs` map
    - `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -run TestShortFlagsConsistent -count=1 -v` prints `inspected N (verb, flag) pairs` with N ≥ 18 and exits 0
    - `04-MUTATION-LOG.md` contains `## Family (b)` with a pasted `--- FAIL: TestShortFlagsConsistent` line naming `callers --limit`
    - `git log -1 --format=%s` starts with `test(04-01): ` and `git show --name-only HEAD` lists only `*_test.go`, `internal/cli/testdata/plain/*.golden` and `04-MUTATION-LOG.md`
    - No `-d`/`-m` short flag was added anywhere under `internal/cli/` (`git show HEAD` touches no production `.go` file)
  </acceptance_criteria>
  <done>CLI-07's invariant is pinned by a positive-counted walk that went RED against a planted long-only flag; the phase's first commit is tests, goldens and the mutation log only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| indexed repository → test process | fixture file names/paths and symbol names flow into the captured stdout; the goldens freeze them as data, never execute them |
| developer machine → committed goldens | temp-dir paths, ports, timestamps and build identity would leak machine-specific bytes into the repo if not normalized |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | testdata/plain/*.golden | low | mitigate | normalizers replace `$TMPDIR` fixture/home/project paths, the ui port and the build identity with placeholders; the verify greps every golden for `/var/folders`, `/tmp/`, `T/tmp.` |
| T-04-02 | Tampering | plain_golden_test.go (-update-plain-goldens) | medium | mitigate | goldens are rewritten only through the explicit flag, reviewed in the commit diff, and the set-equality floor rejects orphaned or missing files; Family (a) proves the compare is live |
| T-04-03 | Repudiation | 04-MUTATION-LOG.md | low | mitigate | verbatim `--- FAIL` transcripts and `git diff --quiet` byte-clean proofs are pasted, matching the Phase 2/3 shape |
| T-04-SC | Tampering | go module installs | low | accept | this plan installs nothing; the phase's only new module (`charm.land/fang/v2`, gated on plan 02's verdict) and the promoted `colorprofile` are `OK` in 04-RESEARCH.md's Package Legitimacy Audit — no `[ASSUMED]`/`[SUS]` entries, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestPlainGolden$|TestNoColorNonTTYRegression$|TestShortFlagsConsistent$' -v` green with ≥28 golden subtests and ≥18 flag pairs inspected
- `04-MUTATION-LOG.md` carries Families (a) and (b) with verbatim RED transcripts and byte-clean reverts
- exactly one `test(04-01):` commit; `git status --porcelain internal/ cmd/ docs/ go.mod` empty
</verification>

<success_criteria>
- 28 plain goldens frozen before any renderer exists (D-16); byte-equality + zero-ESC pinned under NO_COLOR unset/1/banana (CLI-05)
- CLI-07 pinned by a positive-counted tree walk (D-12); both guards demonstrated RED and reverted byte-clean
- No production file changed; no `internal/query`/`internal/mcp`/wire-oracle file touched
</success_criteria>

<output>
Create `.planning/phases/04-cli-glow-up/04-01-SUMMARY.md` when done (paste the Family (a)/(b) `--- FAIL` lines and the observed `inspected N` count).
</output>
