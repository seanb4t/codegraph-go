---
phase: 07-codex-parity
plan: 07
type: execute
wave: 7
depends_on: ["07-06"]
files_modified:
  - .codex/hooks/hooks.json
  - .codex/hooks/codegraph-pretooluse-local.sh
  - .codex/hooks/codegraph-pretooluse-global.sh
  - codexassets.go
  - internal/agents/codex_pretooluse.go
  - internal/agents/codex_pretooluse_test.go
  - internal/agents/codex.go
  - internal/agents/capabilities.go
  - internal/agents/capabilities_test.go
  - internal/cli/hook_pretooluse.go
  - internal/cli/hook_pretooluse_codex_test.go
  - internal/cli/testdata/plain/print-config-style.golden
  - internal/cli/testdata/plain/print-config-style-local.golden
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
autonomous: true
requirements: [CODEX-05]
user_setup: []

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`codegraph install --target codex --location local --pretool-nudge --yes`, run by the real binary in a git repo, writes an executable `.codex/hooks/codegraph-pretooluse.sh` (the local template with the installing binary's absolute ExecPath single-quoted) and appends ONE codegraph-owned group to `.codex/hooks.json`: `matcher` `^Bash$`, one handler `{type: command, command: \"$(git rev-parse --show-toplevel)/.codex/hooks/codegraph-pretooluse.sh\", timeout: 5}` — quoted from day one, no `if`, no statusMessage (D-20)."
    - "At global scope the handler command is the single-quoted absolute path of `~/.codex/hooks/codegraph-pretooluse.sh` and the guard is the global template (D-20); at both scopes the registered command never contains the binary path, which lives only in the guard (D-19)."
    - "Running the registered local command through `sh -c` from the repo root with a Codex `PreToolUse` Bash event (`rg -n Alpha .`, a session id, `cwd` = the repo) prints exactly one line — the same pinned `{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"<nudge.Text>\"}}` object Claude gets — and exits 0; with `.codegraph/` removed it prints nothing and exits 0 (CODEX-05, D-21)."
    - "The Codex envelope is the same hidden subcommand, `codegraph hook pretooluse --harness codex`: it maps `tool_name` `Bash`, `exec_command` and `shell` to the shell rule; takes `tool_input.command` as a string or as an argv array's last element (null, empty, non-string, object or empty array = silent); keys on stdin `session_id` plus `agent_id`, else `main` (never an invented field, never `CLAUDE_CODE_SESSION_ID`); re-checks that stdin `cwd` is absolute and holds a `.codegraph` directory; reuses `nudge.Qualifies`, `nudge.Gate`, `nudge.SessionKey` and `nudge.Text` unchanged; exits 0 on every path and never writes stderr (D-21, D-22, Phase 6 contract)."
    - "The local guard derives the repo root from its own path with POSIX parameter expansion only (it runs with an empty PATH), the global guard checks `$PWD`; both exit 0 on every path, start no binary in an un-indexed repo, and exit 0 when the binary is missing, not executable, exits non-zero or crashes (D-22, D-16 guard level)."
    - "Codex's capability literal declares `Hooks: codex-json` and `HookFiles` names `.codex/hooks.json` and the guard (local) / `~/.codex/hooks.json` and `~/.codex/hooks/codegraph-pretooluse.sh` (global); an unknown hook mechanism still panics loudly in `describeDeclaredPaths` (D-09, WR-01); `--print-config-style` shows `hooks=codex-json` (D-28: no new field)."
    - "A default install (no `--pretool-nudge`) writes no hooks.json and no guard; uninstall always attempts removal of codegraph's own group and guard (not-found when absent), leaving foreign groups untouched and deleting a hooks.json it empties."
    - "CODEX-05 empty (explicit): the adapter is silent (no stdout, nil error) for empty stdin, a null or missing `tool_input.command`, an empty-string command and an empty argv array; a single-element argv such as `[\"rg -n Alpha .\"]` is read as its only element and fires."
    - "CODEX-05 encoding (explicit): stdin is bounded by `maxHookStdinBytes` counted in bytes (a max-size event fires, one byte over is silent), and tool names match byte-exactly and case-sensitively (`bash` is silent)."
    - "The shell rows of both D-15 corpora, driven through the Codex envelope as a string and as `[\"bash\",\"-lc\",<row>]`, fire exactly when their `want` is true (D-21)."
    - "Family (f) in 07-MUTATION-LOG.md: an `exit 2` guard tail, a `$PWD`-based local root, an adapter that reads `CLAUDE_CODE_SESSION_ID`, an argv read from the first element, and a dropped `cwd` re-check each turn a named guard RED, reverted byte-clean."
  artifacts:
    - path: ".codex/hooks/hooks.json"
      provides: "embedded Codex PreToolUse fragment"
      contains: "\"^Bash$\""
    - path: ".codex/hooks/codegraph-pretooluse-local.sh"
      provides: "local guard template (mode 100755)"
      contains: "'@codegraph-exec-path@'"
    - path: ".codex/hooks/codegraph-pretooluse-global.sh"
      provides: "global guard template (mode 100755)"
      contains: "'@codegraph-exec-path@'"
    - path: "codexassets.go"
      provides: "package claudeassets: CodexFS, CodexHooksFragmentPath, CodexPreToolUseGuardLocalPath, CodexPreToolUseGuardGlobalPath and their accessors"
      contains: "CodexFS"
    - path: "internal/agents/codex_pretooluse.go"
      provides: "codexPreToolFragmentCommand, codexHooksJSONPath, codexPreToolGuardPath, codexPreToolHookCommand, codexPreToolUseBlocks, renderCodexPreToolGuard, installCodexPreToolNudge, uninstallCodexPreToolNudge"
      contains: "func renderCodexPreToolGuard("
    - path: "internal/cli/hook_pretooluse.go"
      provides: "--harness flag, codexPreToolUseInput, codexShellCommand, runHookPreToolUseCodex"
      contains: "func runHookPreToolUseCodex("
  key_links:
    - from: ".codex/hooks.json (as written by install)"
      to: ".codex/hooks/codegraph-pretooluse.sh"
      via: "registered command string = guard path (ownership identity; never the binary path)"
      pattern: "codegraph-pretooluse\\.sh"
    - from: ".codex/hooks/codegraph-pretooluse-local.sh"
      to: "internal/cli/hook_pretooluse.go runHookPreToolUseCodex"
      via: "rendered ExecPath invoked as `hook pretooluse --harness codex` with stdin inherited"
      pattern: "hook pretooluse --harness codex"
    - from: "internal/cli/hook_pretooluse.go runHookPreToolUseCodex"
      to: "internal/nudge Qualifies / Gate / SessionKey / Text"
      via: "harness-neutral core reused unchanged"
      pattern: "nudge\\.(Qualifies|SessionKey|Text)"
    - from: "internal/agents/codex.go Install"
      to: "internal/agents/shared.go writeHookEntry(hooksPath, \"PreToolUse\", …)"
      via: "opt-in gate on InstallOptions.PreToolNudge == PreToolNudgeOn; group appended last (D-23)"
      pattern: "writeHookEntry\\("
  prohibitions:
    - statement: "MUST NOT deny, block or ask: the Codex hook never emits a permission decision, a `decision` or `continue` key, never exits 2, and the guard exits 0 on every path (Phase 6 contract carried verbatim, CODEX-05)."
      status: resolved
      verification: test
    - statement: "MUST NOT put the codegraph binary's absolute path into `hooks.json` (a possibly-committed, team-shared file) — it lives only in the rendered guard (D-19)."
      status: resolved
      verification: test
    - statement: "MUST NOT key the Codex cooldown on a field Codex does not send; with no `agent_id` the key is the session plus `main` (A4, D-21)."
      status: resolved
      verification: test
---

<objective>
The tracer for the Codex nudge: prove on the thinnest end-to-end slice that ONE opt-in local install writes a
guard and a Codex `hooks.json` group, and that the registered command, fed a real Codex PreToolUse Bash event in
an indexed repo, prints only the pinned `additionalContext` object and exits 0 — through every layer: the
embedded fragment and guard templates (`codexassets.go`), the Codex hook helpers and table literal
(`internal/agents`), and the `--harness codex` envelope on the hidden subcommand (`internal/cli`), all reusing the
harness-neutral `internal/nudge` core unchanged. Then pin the guard's D-16 contract, the adapter's forced-error
contract and the shell corpora, and prove the guards can fail (Family (f)).

Purpose: CODEX-05 (runtime half) and 07-VALIDATION row `07-NUDGE`. 07-08 adds the lifecycle (Keep/Off, the
`[features] hooks = false` skip, the trust Notes) and widens the CLI flag and help; 07-09 proves delivery live.

Planner notes:
- Precondition: 07-LIVE-SESSIONS.md records `A1 local command form shell-expanded: yes` and
  `A1 global quoted command form runs: yes`. If either is `no`, stop and follow the recorded `Maintainer
  decision:` — D-20's form is locked and replacing it is a new decision, not an executor call.
- Deliberate tracer stubs (each fillable without an architectural change): Install handles `PreToolNudgeOn`
  only (writes); `Keep` and `Off` touch nothing until 07-08 gives them their meaning. Uninstall already always
  attempts removal (so the ownership and install-writes tables stay green once `Hooks` flips).
- `--pretool-nudge` already maps to On for every target; its Claude-only help text and note are 07-08's.
- Discretion (07-CONTEXT): installed guard name `codegraph-pretooluse.sh`; two embedded templates (local and
  global) because D-22 gives them different indexed checks, named differently from the installed file so an
  install inside this repository never overwrites a template; flag spelling `--harness codex`; `timeout: 5`
  (seconds — Codex's `timeout_sec`, 07-RESEARCH Pattern 5); no PATH fallback in the Codex templates (they are
  not dogfooded here, so an unrendered template simply exits 0).
- Tests follow the Phase 6 isolation rule: every hook test clears `CLAUDE_CODE_SESSION_ID` and points
  `TMPDIR` at `t.TempDir()`.
- TDD: RED `test(07-07):` commits that compile, then `feat(07-07):`; paste `--- FAIL:` in the SUMMARY; never
  gate on `gsd-tools check tdd-red-evidence`. `GOTOOLCHAIN=go1.26.6`; commits end with `Co-Authored-By: Claude
  Opus 5 <noreply@anthropic.com>`; never `[ci skip]`. Goldens change only via `-update-plain-goldens`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-RESEARCH.md
@.planning/phases/07-codex-parity/07-PATTERNS.md
@.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-01-PLAN.md
@claudeassets.go
@.claude/hooks/hooks.json
@.claude/hooks/pretooluse-nudge.sh
@internal/agents/claude_pretooluse.go
@internal/agents/claude_pretooluse_test.go
@internal/agents/claude.go
@internal/agents/shared.go
@internal/agents/capabilities.go
@internal/agents/codex.go
@internal/cli/hook_pretooluse.go
@internal/cli/hook_pretooluse_test.go
@internal/nudge/classify.go
@internal/nudge/cooldown.go
</context>

<interfaces>
Existing (reuse unchanged): `preToolGuardExecPathToken` (`'@codegraph-exec-path@'`), `shellSingleQuote`,
`writeEmbeddedFile(path, content, executable)`, `removeEmbeddedFile`, `writeHookEntry(path, event, ownBlocks, ownCommands)`,
`removeHookEntry(path, event, ownCommands)`, `recordFile`; in internal/cli: `maxHookStdinBytes`, `hookNow`,
`hookQualifies`, `claudeHookOutput`/`claudeHookSpecificOutput` (the output object, byte-identical for Codex),
`execCmdWithInput`, `pinnedPreToolUseFire`, `assertHookContract`; in internal/nudge: `ToolShell`, `Qualifies`,
`SessionKey`, `Gate{Dir, Now}.Due`, `DefaultDir`, `Text`; corpora `internal/nudge/testdata/{true,false}-positives.json`
rows `{tool, input, want, note}`.

New (this plan):
- `codexassets.go` at the repo root, `package claudeassets` (the sibling-of-root embed rule, golang/go#46056):
  `//go:embed` of exactly `.codex/hooks/hooks.json`, `.codex/hooks/codegraph-pretooluse-local.sh`,
  `.codex/hooks/codegraph-pretooluse-global.sh` into `var CodexFS embed.FS`; consts `CodexHooksFragmentPath`,
  `CodexPreToolUseGuardLocalPath`, `CodexPreToolUseGuardGlobalPath`; funcs `CodexHooksFragment()`,
  `CodexPreToolUseGuardLocalTemplate()`, `CodexPreToolUseGuardGlobalTemplate()` (each `([]byte, error)`).
- internal/agents/codex_pretooluse.go: `const codexPreToolFragmentCommand = "\"$(git rev-parse --show-toplevel)/.codex/hooks/codegraph-pretooluse.sh\""`;
  `codexHooksJSONPath(loc)` (`.codex/hooks.json` / `~/.codex/hooks.json`); `codexPreToolGuardPath(loc)`
  (`.codex/hooks/codegraph-pretooluse.sh` / `~/.codex/hooks/codegraph-pretooluse.sh`); `codexPreToolHookCommand(loc)`
  (the fragment literal locally, `shellSingleQuote(<abs global guard path>)` globally); `codexPreToolUseBlocks(loc) ([]any, []string, error)`
  (decode the embedded fragment's `hooks.PreToolUse`, rewrite each handler's command to the own command — a
  Codex-specific copy of the claudeFragmentEventBlocks pattern, not shared code); `renderCodexPreToolGuard(loc Location, execPath string) (string, error)`
  (empty/relative path rejected; token exactly once; `shellSingleQuote`); `installCodexPreToolNudge(result *WriteResult, loc Location, execPath string)`;
  `uninstallCodexPreToolNudge(result *WriteResult, loc Location)`.
- internal/cli/hook_pretooluse.go: string flag `--harness` (default `claude`) on the hidden `pretooluse`
  command; `codexPreToolUseInput` (`session_id`, `agent_id`, `hook_event_name`, `tool_name`, `cwd`,
  `tool_input.command` as `json.RawMessage`); `codexShellCommand(raw json.RawMessage) (string, bool)`;
  `runHookPreToolUseCodex(in io.Reader, out io.Writer)`; an unknown `--harness` value is silent.
Fragment shape (pretty-printed, 2-space indent): `{"hooks": {"PreToolUse": [{"matcher": "^Bash$", "hooks": [{"type": "command", "command": <codexPreToolFragmentCommand>, "timeout": 5}]}]}}`.
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan adds the three `.codex/hooks/` assets,
`codexassets.go`, `internal/agents/codex_pretooluse.go`, the `--harness` flag and Codex envelope, the Codex
`Hooks` literal and `HookFiles` case, the listed tests and Family (f).

<!-- planner-discipline-allow: permissionDecision -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "install --pretool-nudge registers a Codex guard that nudges on a Bash search" — one path through every layer</name>
  <precondition>07-LIVE-SESSIONS.md contains `A1 local command form shell-expanded: yes` and `A1 global quoted command form runs: yes`.</precondition>
  <files>.codex/hooks/hooks.json, .codex/hooks/codegraph-pretooluse-local.sh, .codex/hooks/codegraph-pretooluse-global.sh, codexassets.go, internal/agents/codex_pretooluse.go, internal/agents/codex_pretooluse_test.go, internal/agents/codex.go, internal/agents/capabilities.go, internal/agents/capabilities_test.go, internal/cli/hook_pretooluse.go, internal/cli/hook_pretooluse_codex_test.go, internal/cli/testdata/plain/print-config-style.golden, internal/cli/testdata/plain/print-config-style-local.golden</files>
  <read_first>
    - claudeassets.go (embed rule and accessor style), .claude/hooks/hooks.json, .claude/hooks/pretooluse-nudge.sh
    - internal/agents/claude_pretooluse.go (renderPreToolGuard, claudePreToolUseBlocks, shellSingleQuote) and claude.go claudeFragmentEventBlocks (:288)
    - internal/agents/shared.go (writeHookEntry :248, removeHookEntry :388, writeEmbeddedFile :323, removeEmbeddedFile)
    - internal/agents/capabilities.go (HookFiles :159-180, errHookFilesUndeclared, describeDeclaredPaths' panic branch) and capabilities_test.go (fakeHooksCodexJSONTarget, TestDescribeDeclaredPaths_PanicsOnUndeclaredHookFiles, TestCapabilitiesDeclared codex row)
    - internal/agents/codex.go (after 07-06)
    - internal/cli/hook_pretooluse.go (the Claude envelope) and hook_pretooluse_test.go (isolateHookEnv, pinnedPreToolUseFire, assertHookContract)
    - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md (A1 lines and the recorded PreToolUse stdin fields / command type)
  </read_first>
  <behavior>
    - TestCodexHooksFragmentShape: the embedded fragment has exactly one event key `PreToolUse`, one group, matcher `^Bash$`, one handler whose keys are exactly {type, command, timeout}, type `command`, timeout 5, command == codexPreToolFragmentCommand.
    - TestCodex_Install_PreToolNudgeOn_WritesGuardAndGroup: subtests local and global — guard file mode has 0o111, its binary line is `codegraph_bin='<ExecPath>'`; hooks.json's last PreToolUse group is codegraph's with the scope's command; hooks.json does not contain the ExecPath string; a foreign `^Bash$` group planted first is still first and unchanged.
    - TestCodex_Install_DefaultWritesNoHooks: an install with the zero PreToolNudge value writes neither hooks.json nor the guard.
    - TestCodex_Uninstall_RemovesHooks: after an On install, Uninstall removes the own group (deleting a hooks.json it empties) and the guard; on a never-opted location it reports not-found and no error.
    - TestHookPreToolUseCodex_BashFiresPinnedContext: `execCmdWithInput(<Codex Bash event, cwd = an indexed temp dir>, "hook", "pretooluse", "--harness", "codex")` → stdout == pinnedPreToolUseFire, stderr empty, nil error.
    - TestHookPreToolUseCodex_NotIndexedCwdIsSilent: the same event with a cwd lacking `.codegraph` → empty stdout, nil error.
    - Companion (RED at the test commit): TestCapabilitiesDeclared codex hooks = codex-json; the undeclared-mechanism panic test uses a fake mechanism `undeclared-test-mechanism` instead of codex-json.
  </behavior>
  <action>
    RED commit `test(07-07): add failing Codex PreToolUse install and envelope tests` (compiles: add only the minimal stubs the tests reference).
    GREEN commit `feat(07-07): Codex PreToolUse nudge through hooks.json and the harness envelope`:
    - Assets: the fragment exactly as the interfaces block shapes it. Two guard templates (mode 0755, POSIX sh, header comment stating the Phase 6 contract): the local one sets the root by three successive `%/*` parameter-expansion strips of `$0` (script → hooks dir → .codex → repo root) and exits 0 unless `<root>/.codegraph` is a directory; the global one exits 0 unless `${PWD:-.}/.codegraph` is a directory; both then set `codegraph_bin='@codegraph-exec-path@'` (the token exactly once), exit 0 when it is not a regular executable file, run `"$codegraph_bin" hook pretooluse --harness codex` as a child with stdin inherited, and end with `exit 0`. No `set -e`, no `exec`.
    - `codexassets.go` per the interfaces block, with a doc comment pointing at claudeassets.go's rationale.
    - `internal/agents/codex_pretooluse.go` per the interfaces block. `installCodexPreToolNudge` renders and writes the guard (executable) first, then `writeHookEntry(codexHooksJSONPath(loc), "PreToolUse", blocks, own)`; `uninstallCodexPreToolNudge` calls `removeHookEntry` then `removeEmbeddedFile`; every step through `recordFile`.
    - `capabilities.go`: `HookFiles` gains the `HooksCodexJSON` case returning `[codexHooksJSONPath(loc), codexPreToolGuardPath(loc)]`; `HooksNone` returns nil; any other mechanism returns an error wrapping `errHookFilesUndeclared` (update its message and doc comment — the loud-failure contract now covers unknown mechanisms). Update the fake test target accordingly.
    - `codex.go`: `Hooks: HooksCodexJSON` in the literal; Install calls `installCodexPreToolNudge` when `opts.PreToolNudge == PreToolNudgeOn`; Uninstall always calls `uninstallCodexPreToolNudge`.
    - `hook_pretooluse.go`: the `--harness` flag and `runHookPreToolUseCodex` per D-21/D-22 (read capped like Claude; decode; event must be `PreToolUse` or empty; tool map; `codexShellCommand`; `hookQualifies(nudge.ToolShell, cmd)`; `cwd` absolute with a `.codegraph` directory; key `nudge.SessionKey(event.SessionID, event.AgentID)`; `Gate.Due`; marshal the same output object). Update the command's Short to name both harnesses. The RunE keeps its recover-first body and always returns nil.
    - Regenerate both print-config-style goldens (`hooks=codex-json` on the codex lines only).
    Verify through the real binary as below.
  </action>
  <reversibility rating="costly">The registered Codex hook command is codegraph's ownership identity in hooks.json; changing it later leaves every installed group foreign and duplicates on upgrade (the AR-06-08 lesson). D-20 fixes the quoted form from day one.</reversibility>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-07\): ' && printf '%s\n' "$S" | rg -q '^feat\(07-07\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents internal/cli codexassets.go)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ ./internal/cli/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 -run 'TestCodexHooksFragmentShape$|TestCodex_Install_PreToolNudgeOn_WritesGuardAndGroup$|TestCodex_Install_DefaultWritesNoHooks$|TestCodex_Uninstall_RemovesHooks$|TestHookPreToolUseCodex_BashFiresPinnedContext$|TestHookPreToolUseCodex_NotIndexedCwdIsSilent$|TestDescribeDeclaredPaths_PanicsOnUndeclaredHookFiles$|TestCapabilitiesDeclared$|TestCapabilitiesMatchInstallWrites$|TestOwnershipExactIdentity$|TestPlainGolden$|TestHookCmd_HiddenTwoLevel$|TestEveryRegisteredFlagIsAccountedFor$|TestHookPreToolUse_GrepFiresPinnedContext$' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -4 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c '^--- PASS: (TestCodexHooksFragmentShape|TestCodex_Install_PreToolNudgeOn_WritesGuardAndGroup|TestCodex_Install_DefaultWritesNoHooks|TestCodex_Uninstall_RemovesHooks|TestHookPreToolUseCodex_BashFiresPinnedContext|TestHookPreToolUseCodex_NotIndexedCwdIsSilent|TestDescribeDeclaredPaths_PanicsOnUndeclaredHookFiles|TestHookCmd_HiddenTwoLevel|TestEveryRegisteredFlagIsAccountedFor|TestHookPreToolUse_GrepFiresPinnedContext) ' "$O/t1.txt")" = "10" && rg -q 'hooks=codex-json' internal/cli/testdata/plain/print-config-style-local.golden && rg -q 'hooks=codex-json' internal/cli/testdata/plain/print-config-style.golden && LS=$(git ls-files -s -- .codex/hooks/codegraph-pretooluse-local.sh .codex/hooks/codegraph-pretooluse-global.sh) && test "$(printf '%s\n' "$LS" | rg -c '^100755 ')" = "2" && GOTOOLCHAIN=go1.26.6 go build -o "$O/codegraph" ./cmd/codegraph && H="$O/home" && P="$O/proj" && T="$O/tmp" && mkdir -p "$H" "$P/.codegraph" "$T" && git -C "$P" init -q && (cd "$P" && HOME="$H" XDG_CONFIG_HOME="$H/.config" HERMES_HOME="$H/.hermes" "$O/codegraph" install --target codex --location local --pretool-nudge --yes > "$O/install.txt") && test -x "$P/.codex/hooks/codegraph-pretooluse.sh" && rg -q "^codegraph_bin='.*/codegraph'$" "$P/.codex/hooks/codegraph-pretooluse.sh" && test "$(jq '[.hooks.PreToolUse[] | select(.matcher == "^Bash$")] | length' "$P/.codex/hooks.json")" = "1" && CMD=$(jq -r '.hooks.PreToolUse[-1].hooks[0].command' "$P/.codex/hooks.json") && test "$CMD" = '"$(git rev-parse --show-toplevel)/.codex/hooks/codegraph-pretooluse.sh"' && test "$(jq '.hooks.PreToolUse[-1].hooks[0].timeout' "$P/.codex/hooks.json")" = "5" && ! rg -q -F "$O/codegraph" "$P/.codex/hooks.json" && EV='{"session_id":"tracer-c1","hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"rg -n Alpha ."},"cwd":"'"$P"'"}' && (cd "$P" && printf '%s' "$EV" | env -u CLAUDE_CODE_SESSION_ID TMPDIR="$T" sh -c "$CMD" > "$O/fire.txt"; echo "exit=$?" >> "$O/fire.txt") && test "$(rg -c '^\{"hookSpecificOutput":\{"hookEventName":"PreToolUse","additionalContext":"This repo has a codegraph index: codegraph_explore ' "$O/fire.txt")" = "1" && rg -q '^exit=0$' "$O/fire.txt" && test "$(wc -l < "$O/fire.txt" | tr -d ' ')" = "2" && ! rg -q 'permissionDecision|"decision"|"continue"' "$O/fire.txt" && rm -rf "$P/.codegraph" && (cd "$P" && printf '%s' "$EV" | env -u CLAUDE_CODE_SESSION_ID TMPDIR="$T" sh -c "$CMD" > "$O/silent.txt"; echo "exit=$?" >> "$O/silent.txt") && test "$(cat "$O/silent.txt")" = "exit=0" && echo "tracer green: Codex guard installs, fires once, silent un-indexed"</automated>
    <fails_when>non-zero exit; a missing test(07-07)/feat(07-07) subject; build/gofmt/vet failure; fewer than 10 named PASS lines or any FAIL line; a golden without hooks=codex-json; a template not committed 100755; the real install not writing an executable rendered guard, not exactly one `^Bash$` group, a command other than the exact D-20 local form, a timeout other than 5, or the ExecPath appearing in hooks.json; the fire run not printing exactly one pinned line then exit=0, or printing a decision key; or the un-indexed run printing anything</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY pastes the RED `--- FAIL:` lines for the install and envelope tests
    - the body of `runHookPreToolUseCodex` reads no environment variable (`awk '/^func runHookPreToolUseCodex/,/^}/' internal/cli/hook_pretooluse.go | rg -q 'getenv|Getenv'` exits 1 — no match)
    - `git show --stat <feat(07-07) sha> -- internal/nudge/` lists no file (the core is reused unchanged)
  </acceptance_criteria>
  <done>A real opt-in local install registers a quoted-from-day-one Codex hook whose guard fires the pinned context once in an indexed repo and stays silent otherwise, through every layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The guard exec suite over both real templates, render safety, the adapter's forced-error contract, per-agent keys and the shell corpora</name>
  <files>internal/agents/codex_pretooluse_test.go, internal/cli/hook_pretooluse_codex_test.go, internal/agents/codex_pretooluse.go, internal/cli/hook_pretooluse.go</files>
  <read_first>
    - internal/agents/claude_pretooluse_test.go (TestPreToolUseGuard, runPreToolGuard, writeStub, the stub scripts, TestRenderPreToolGuard, shSyntaxOK)
    - internal/cli/hook_pretooluse_test.go (TestHookPreToolUse_ForcedErrorContract, CooldownPerAgent, PanicIsRecovered)
    - internal/nudge/classify_test.go (corpusRow, loadCorpus) and internal/nudge/testdata/*.json
  </read_first>
  <behavior>
    - TestCodexPreToolUseGuard (11 subtests over the REAL embedded templates rendered with stub binaries that record argv and stdin): local/indexed_binary_ok (invoked by absolute path from an unrelated cwd; stub started with argv `hook pretooluse --harness codex`; stdin byte-identical; stub stdout is the guard's stdout), local/not_indexed, local/codegraph_is_file, local/binary_missing, local/binary_not_executable, local/binary_exits_nonzero, local/binary_crashes, local/root_from_own_path_with_empty_PATH (run with `PATH=` and `/bin/sh`; the stub still starts), global/indexed_pwd, global/not_indexed_pwd, global/binary_exits_nonzero — every one exits 0, and the un-indexed ones never start the stub.
    - TestRenderCodexPreToolGuard: plain_path, quote_and_space_in_path (a stub at `<tmp>/it's a dir/codegraph` runs; `sh -n` passes), relative_path_rejected, empty_path_rejected, template_token_exactly_once (both templates).
    - TestHookPreToolUseCodex_ForcedErrorContract (24 subtests, each through `assertHookContract` and an exact stdout): no_stdin, malformed_json, oversized_input, max_size_input_fires, no_session_id, claude_env_session_ignored (env set, stdin session absent → silent), non_pretooluse_event, unmapped_tool (`apply_patch`), lowercase_bash, exec_command_tool_fires, shell_tool_fires, command_null, command_empty_string, command_empty_array, command_nonstring_array, command_object, command_single_element_argv_fires, command_argv_last_element_fires, cwd_missing, cwd_relative, cwd_not_indexed, cwd_codegraph_is_file, unwritable_sentinel_base, symlinked_sentinel_dir.
    - TestHookPreToolUseCodex_CooldownPerAgent: one session — main fires, main again silent, agent `a1` fires, `a1` again silent; a second session's main fires (pinned clock via hookNow).
    - TestHookPreToolUseCodex_ShellCorpora: every `tool == "shell"` row of both corpora, as a string and as `["bash","-lc",<input>]`, fires iff `want`; logs the per-corpus fire counts; asserts at least 14 rows per corpus were driven.
    - TestHookPreToolUseCodex_PanicIsRecovered and TestHookPreToolUse_UnknownHarnessIsSilent.
  </behavior>
  <action>
    RED commit `test(07-07): add the Codex guard, render and envelope contract suites` (tests that expose any gap fail on assertions; if a suite already passes against Task 1's code, keep it as a characterization guard and say so in the SUMMARY — its positive control is Family (f)). GREEN commit `feat(07-07): harden the Codex envelope and guard` for whatever the suites require. Keep the Claude path byte-for-byte unchanged (its existing suites must stay green).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && test "$(printf '%s\n' "$S" | rg -c '^test\(07-07\): ')" -ge 2 && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents internal/cli)" && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 -race -run 'TestCodexPreToolUseGuard$|TestRenderCodexPreToolGuard$|TestHookPreToolUseCodex_|TestHookPreToolUse_UnknownHarnessIsSilent$|TestHookPreToolUse_ForcedErrorContract$|TestPreToolUseGuard$' -v > "$O/t2.txt" 2>&1; echo "exit=$?" >> "$O/t2.txt") && tail -3 "$O/t2.txt" && rg -q '^exit=0$' "$O/t2.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t2.txt" && test "$(rg -c -- '--- PASS: TestCodexPreToolUseGuard/(local|global)/[a-z_]+ ' "$O/t2.txt")" = "11" && test "$(rg -c -- '--- PASS: TestRenderCodexPreToolGuard/(plain_path|quote_and_space_in_path|relative_path_rejected|empty_path_rejected|template_token_exactly_once) ' "$O/t2.txt")" = "5" && test "$(rg -c -- '--- PASS: TestHookPreToolUseCodex_ForcedErrorContract/[a-z_]+ ' "$O/t2.txt")" = "24" && test "$(rg -c '^--- PASS: (TestHookPreToolUseCodex_CooldownPerAgent|TestHookPreToolUseCodex_ShellCorpora|TestHookPreToolUseCodex_PanicIsRecovered|TestHookPreToolUse_UnknownHarnessIsSilent|TestHookPreToolUse_ForcedErrorContract|TestPreToolUseGuard) ' "$O/t2.txt")" = "6" && rg -q 'fire' "$O/t2.txt" && test "$(rg -o 'time\.Sleep' internal/cli/hook_pretooluse_codex_test.go internal/agents/codex_pretooluse_test.go | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>non-zero exit; fewer than two test(07-07) subjects; build/gofmt failure; any FAIL line or a race report; not exactly 11 guard subtests, 5 render subtests or 24 forced-error subtests; fewer than 6 named top-level PASS lines (the Claude suites included); no logged corpus fire counts; or a sleep in the new tests</fails_when>
  </verify>
  <acceptance_criteria>
    - the corpora test reads the SAME `internal/nudge/testdata/*.json` files the Claude path uses (no copied rows)
    - every forced-error subtest asserts nil error, empty stderr and stdout either empty or exactly `pinnedPreToolUseFire`
  </acceptance_criteria>
  <done>The Codex guard, render and envelope contracts are pinned at both levels, reusing the Phase 6 core and corpora.</done>
</task>

<task type="auto">
  <name>Task 3: Family (f) — prove the Codex guard and envelope guards can fail</name>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
    - .codex/hooks/codegraph-pretooluse-local.sh, internal/cli/hook_pretooluse.go (final code)
  </read_first>
  <action>
    Append in the established shape, each reverted byte-clean with a green control:
    - `## Family (f1)` — `perl -0pi -e 's/\nexit 0\n\z/\nexit 2\n/' .codex/hooks/codegraph-pretooluse-local.sh` → `TestCodexPreToolUseGuard/local/binary_exits_nonzero` RED.
    - `## Family (f2)` — the local template's root derivation replaced by `${PWD:-.}` → `TestCodexPreToolUseGuard/local/root_from_own_path_with_empty_PATH` or `/local/indexed_binary_ok` RED.
    - `## Family (f3)` — the Codex branch takes its session from `CLAUDE_CODE_SESSION_ID` when stdin's is empty → `TestHookPreToolUseCodex_ForcedErrorContract/claude_env_session_ignored` RED.
    - `## Family (f4)` — `codexShellCommand` returns the argv's first element → `…/command_argv_last_element_fires` RED.
    - `## Family (f5)` — the `cwd` re-check removed → `…/cwd_not_indexed` RED.
    Commit `docs(07-07): Family (f) mutation log`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && G=.codex/hooks/codegraph-pretooluse-local.sh && git diff --quiet -- "$G" && perl -0pi -e 's/\nexit 0\n\z/\nexit 2\n/' "$G" && ! git diff --quiet -- "$G" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestCodexPreToolUseGuard$' -v > "$O/red-f1.txt" 2>&1; echo "exit=$?" >> "$O/red-f1.txt"); git checkout -- "$G"; git diff --quiet -- "$G" && rg -q -- '--- FAIL: TestCodexPreToolUseGuard/local/binary_exits_nonzero' "$O/red-f1.txt" && ! rg -q '^exit=0$' "$O/red-f1.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(f[1-5]\)' "$M")" = "5" && rg -q -- '--- FAIL: TestCodexPreToolUseGuard/local/binary_exits_nonzero' "$M" && rg -q -- '--- FAIL: TestCodexPreToolUseGuard/local/(root_from_own_path_with_empty_PATH|indexed_binary_ok)' "$M" && rg -q -- '--- FAIL: TestHookPreToolUseCodex_ForcedErrorContract/claude_env_session_ignored' "$M" && rg -q -- '--- FAIL: TestHookPreToolUseCodex_ForcedErrorContract/command_argv_last_element_fires' "$M" && rg -q -- '--- FAIL: TestHookPreToolUseCodex_ForcedErrorContract/cwd_not_indexed' "$M" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 > "$O/pkgs.txt" 2>&1; echo "exit=$?" >> "$O/pkgs.txt") && rg -q '^exit=0$' "$O/pkgs.txt" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-07\): Family \(f\)'</automated>
    <fails_when>non-zero exit; the template dirty before the plant or after the revert; the planted run exiting 0 or lacking the FAIL line; fewer than 5 Family (f) sections or any FAIL excerpt missing; the packages not green; or no docs(07-07) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - each Family (f) section carries the six convention headings and a byte-clean revert
  </acceptance_criteria>
  <done>The Codex nudge's guard and envelope guards are proven able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex's hook engine → the guard and `codegraph hook pretooluse --harness codex` | untrusted stdin JSON on every Bash call |
| install → a possibly-committed `.codex/hooks.json` | team-shared file the user's teammates' Codex reads |
| install → a user's own hooks in hooks.json | foreign groups codegraph must never overwrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-17 | Elevation of Privilege | the Codex hook blocking or denying a tool call | high | mitigate | additionalContext-only output; exit 0 everywhere; forced-error suite asserts no decision key; guard `exit 2` mutation proven RED (f1) |
| T-07-18 | Denial of Service | malformed/oversized PreToolUse stdin | medium | mitigate | maxHookStdinBytes cap, tolerant decode, recover-first RunE, any doubt silent; 24 forced-error subtests |
| T-07-19 | Tampering | ExecPath injection into the guard | high | mitigate | shellSingleQuote; absolute path required; quote-and-space render test |
| T-07-20 | Tampering | foreign hooks.json groups | high | mitigate | writeHookEntry/removeHookEntry exact-command identity (242ec0a) reused unchanged; foreign group planted first survives |
| T-07-21 | Information Disclosure | binary path in hooks.json | low | mitigate | the command names the guard only; test asserts the ExecPath string is absent from hooks.json |
| T-07-22 | Spoofing | sentinel directory attacks | medium | mitigate | the Phase 6 Gate (0700, Lstat, no symlink writes) reused unchanged; symlinked/unwritable subtests |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ ./internal/nudge/ -count=1` green; the real-binary tracer fires once and stays silent un-indexed
</verification>

<success_criteria>
- CODEX-05 runtime: opt-in registration, quoted command, harness envelope, exit-0/context-only contract, per-(session, agent) key with the no-agent-id fallback
- 07-VALIDATION row 07-NUDGE (runtime half) satisfiable
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-07-SUMMARY.md` when done — RED transcripts, corpus fire counts, and the A1/stdin-field evidence the design relied on.
</output>
