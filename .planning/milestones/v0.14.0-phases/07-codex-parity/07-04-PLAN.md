---
phase: 07-codex-parity
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md
autonomous: false
requirements: [CODEX-01]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The CODEX-01 evidence is gathered by the ORCHESTRATOR (Herdr pane TUI plus scripted `codex` runs), never by an executor subagent; the executor scaffolds and runs the completeness gate (05-06/06-06 precedent)."
    - "Everything runs in a genuinely fresh Codex: `HOME=$S/home`, `CODEX_HOME=$S/home/.codex`, `auth.json` SYMLINKED from the real `~/.codex` (D-02); the real `~/.codex/{config.toml,hooks.json,AGENTS.md}` and `~/.agents/skills/codegraph/SKILL.md` are sha256-recorded before and after and unchanged (L7)."
    - "The D-06 pass bar (L1-L7) is copied verbatim from 07-CONTEXT into 07-LIVE-SESSIONS.md before any session runs; each CODEX-01 line is recorded in a fixed form with a verbatim excerpt; `CODEX-01 verdict: PASS` requires L1 (project and global), L2, L3, L4 and L7 all PASS."
    - "Model-free tiers run first (D-03): `codex features list`, `codex mcp list --json` and `codex debug prompt-input`; the only model turns are the A1 hook probes and the A2 real-session checks, whose evidence is the session JSONL's injected context, never the model's paraphrase."
    - "The untrusted project is checked first as a negative control and never trusted (D-05, L2); the trusted project is trusted through the real TUI prompt; whether a `-c 'projects.\"<path>\".trust_level=\"trusted\"'` override grants trust is recorded separately."
    - "The project layer's `codegraph` entry points at a distinct copy of the binary (`$S/codegraph-project`) so `codex mcp list --json` shows which layer won without a scope column (07-RESEARCH anti-pattern 3)."
    - "A1 is settled by a side-effect marker: a probe hook registered with the exact D-20 local command form (and, after the global install, the quoted absolute global form) appends to a marker log only if Codex expanded and ran the command; the probe also captures raw PreToolUse stdin (field list and `tool_input.command` type)."
    - "A2, D-15 and D-16 are settled in REAL sessions (untrusted vs trusted JSONL), not only by `debug prompt-input`."
    - "Every absence claim is backed by a search first shown to find the thing when present (STATE standing rule)."
    - "No `internal/agents/codex.go` change exists at this plan's end (D-01; the comment correction lands with the first codex.go change in 07-05)."
  artifacts:
    - path: ".planning/phases/07-codex-parity/07-LIVE-SESSIONS.md"
      provides: "CODEX-01 live evidence: pass bar, pre-flight, Codex reference quotes, scaffold, protocols, verdict lines"
      contains: "CODEX-01 verdict:"
  key_links:
    - from: "07-LIVE-SESSIONS.md `CODEX-01 verdict:`, `D-15`, `D-16` and `A2` lines"
      to: "07-05-PLAN.md (scope flip preconditions: SkillDirs read-only entries and the trust Note wording)"
      via: "precondition read by the next plan"
      pattern: "^CODEX-01 verdict: PASS$"
    - from: "07-LIVE-SESSIONS.md `A1 local command form shell-expanded:` line"
      to: "07-07-PLAN.md (the Codex hook command form)"
      via: "precondition read by the nudge plan"
      pattern: "^A1 local command form shell-expanded: yes"
  prohibitions:
    - statement: "MUST NOT run `codegraph install` or `uninstall` with `--target codex|all` against the real HOME, and MUST NOT print the contents of the real `~/.codex/config.toml` or `auth.json` anywhere (terminal, transcript, evidence file) (D-02, D-08)."
      status: resolved
      verification: test
    - statement: "MUST NOT record a pass-bar line PASS without a verbatim excerpt whose search was positive-controlled, and MUST NOT trust the negative-control project (D-05, D-06)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT change `internal/agents/codex.go` or patch any code in this plan; a FAIL is recorded and escalated as a `Maintainer decision:` line."
      status: resolved
      verification: test
---

<objective>
Settle, in a genuinely fresh Codex (D-02) and BEFORE any `codex.go` line changes (D-01), what the current Codex
CLI actually does with a project-local `.codex/config.toml`, which skill paths it reads, whether `AGENTS.md`
and project skills are trust-gated in a real session, and whether a `hooks.json` command in the D-20 form is
shell-expanded and run — against the pass bar locked in 07-CONTEXT D-06. The executor scaffolds; the
orchestrator (Herdr pane access) drives Codex and writes the verdicts; the executor then gates completeness.

Purpose: CODEX-01 ("with dated citations"), the A1/A2 assumptions and the D-15/D-16 questions 07-05 and
07-07 depend on, and 07-VALIDATION row `07-LIVE1`.

Before `codex.go` changes, codegraph cannot write a project `.codex/config.toml`, so the project layer is
hand-planted with exactly the bytes `codexTableBody` renders, the repo-root `AGENTS.md` block and the shared
`.agents/skills/codegraph` package come from codegraph's own opencode local install, and the global layer
comes from the HEAD binary's (already fixed, 07-01/07-02) `install --target codex --location global --yes`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-RESEARCH.md
@.planning/phases/07-codex-parity/07-VALIDATION.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-06-PLAN.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md
</context>

<interfaces>
Scratch root: `S=$(cd /tmp && pwd -P)/07-live` (the real path, so trust keys never differ by the macOS
`/tmp` symlink). Env for every Codex run: `HOME=$S/home CODEX_HOME=$S/home/.codex` (plus
`XDG_CONFIG_HOME=$S/home/.config` for codegraph runs). Repos: `$S/trusted`, `$S/untrusted`, `$S/bare`
(each a copy of `internal/indexer/testdata/gofixture/` plus `git init -q`). Local codex-cli at research time:
0.155.0 (record the version used). Codex probes (07-RESEARCH "Live Codex CLI probes"): `codex features list`,
`codex mcp list --json` (no scope field — `name`, `enabled`, `transport`, …), `codex debug prompt-input`
(renders `<skills_instructions>` with roots `r0`, `r1`, … and the `# AGENTS.md instructions for <path>`
message), `codex exec --json -C <repo>`; transcripts under `$CODEX_HOME/sessions/YYYY/MM/DD/`.
Herdr: `herdr agent start <name> --kind codex --pane <id>`, `herdr agent prompt <name> "<text>" --wait`,
`herdr agent read <name> --source recent-unwrapped`.
The pinned codegraph block marker to search for: `<!-- CODEGRAPH_START -->` and the heading `## CodeGraph`;
the skill name: `codegraph`.
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan creates `07-LIVE-SESSIONS.md` (CODEX-01 section).

<!-- planner-discipline-allow: PENDING -->

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the fresh Codex scratch, the pre-flight checksums, the Codex reference quotes, the locked pass bar and the evidence skeleton</name>
  <precondition>07-01..07-03 are committed; `command -v codex` succeeds; the real `~/.codex/auth.json` exists (`test -e "$HOME/.codex/auth.json"`).</precondition>
  <files>.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-CONTEXT.md (D-02..D-06 in full, the "Current Codex reference" block and its "Not confirmed" list)
    - .planning/phases/07-codex-parity/07-RESEARCH.md (Assumptions A1/A2/A4, Pitfalls 4/5, the live-probe section)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md (document layout, negative-space rule wording, verdict-line style)
    - internal/agents/codex.go (codexTableBody — the exact project-layer bytes to hand-plant)
  </read_first>
  <action>
    Build the scratch, writing nothing under the real `$HOME`:
    - `S=$(cd /tmp && pwd -P)/07-live; rm -rf "$S"; mkdir -p "$S/home/.codex" "$S/probes" "$S/transcripts"`; `GOTOOLCHAIN=go1.26.6 go build -o "$S/codegraph" ./cmd/codegraph`; `cp "$S/codegraph" "$S/codegraph-project"`; `ln -s "$HOME/.codex/auth.json" "$S/home/.codex/auth.json"` (a symlink, never a copy — D-02).
    - Repos `trusted`, `untrusted`, `bare`: copy `internal/indexer/testdata/gofixture/.` into `$S/<name>` and `git -C "$S/<name>" init -q`; index `trusted` and `untrusted` with `CODEGRAPH_NO_WATCH=1 "$S/codegraph" init <dir>`.
    - Project layer in `trusted` and `untrusted` only: `(cd <dir> && HOME="$S/home" CODEX_HOME="$S/home/.codex" XDG_CONFIG_HOME="$S/home/.config" "$S/codegraph" install --target opencode --location local --yes)` (repo-root `AGENTS.md` block + `.agents/skills/codegraph`), then write `<dir>/.codex/config.toml` containing exactly three lines: `[mcp_servers.codegraph]`, `command = "<S>/codegraph-project"`, `args = ["serve", "--mcp"]` (codexTableBody's format with the distinct path).
    - D-15 probes: `<trusted>/.codex/skills/cgprobe-dotcodex/SKILL.md` and `<untrusted>/.codex/skills/cgprobe-dotcodex/SKILL.md` (frontmatter `name: cgprobe-dotcodex`, `description: D-15 probe for the project .codex/skills root`), and `$S/home/.codex/skills/cgprobe-codexhome/SKILL.md` (`name: cgprobe-codexhome`).
    - A1 local probe in `trusted` only: `.codex/hooks/a1-probe.sh` (mode 0755; POSIX sh; appends one line `a1-local <date -u +%FT%TZ> <$PWD>` to `$S/probes/a1-local.log`, copies stdin to `$S/probes/a1-local-stdin-<date +%s>.json`, prints nothing, exits 0 — absolute `$S` paths written in) and `.codex/hooks.json` with one `PreToolUse` group: `matcher` `^Bash$`, one handler `type` `command`, `timeout` 5, and `command` equal to the D-20 local form `"$(git rev-parse --show-toplevel)/.codex/hooks/a1-probe.sh"` (the double quotes are part of the string).
    - Read-only pre-flight: `codex --version`; `HOME="$S/home" CODEX_HOME="$S/home/.codex" codex features list` (record the `hooks` line); `shasum -a 256` of each of `~/.codex/config.toml`, `~/.codex/hooks.json`, `~/.codex/AGENTS.md`, `~/.agents/skills/codegraph/SKILL.md` (record `absent` for any missing file; never print contents); `readlink "$S/home/.codex/auth.json"`.
    - Codex reference (dated citations for CODEX-01): fetch and quote verbatim, with URL and fetch date, the current docs lines for (a) project `.codex/config.toml` loading only for trusted projects and the `projects.<path>.trust_level` key, (b) skill locations (`.agents/skills`, `$HOME/.agents/skills`, and any `.codex/skills` / `$CODEX_HOME/skills` mention), (c) hooks.json locations, trust review in `/hooks`, the PreToolUse input fields and `hookSpecificOutput.additionalContext`. Use learn.chatgpt.com/docs/hooks and the developers.openai.com/codex pages; if a fetch fails, quote Context7 `/openai/codex` with its fetch date and say so.
    Write 07-LIVE-SESSIONS.md: title "Phase 7: Live-Session Evidence — Codex parity"; a `## CODEX-01 (before any codex.go change)` section with Method (D-02..D-05, orchestrator-run, model-free first, evidence = JSONL injected context, the negative-space rule, the distinct `$S/codegraph-project` path), `### Pass bar (07-CONTEXT D-06, locked 2026-09-19)` with the D-06 table copied verbatim (rows L1..L7), Pre-flight, Codex reference, Scaffold (commands + a positive-assertion listing), Protocol A (model-free: bare, untrusted, trust override), Protocol B (TUI trust, A1 local, A2/D-15/D-16/D-17 real sessions, L1 project, L3), Protocol C (global install, L1 global, A1 global, L7), and `### CODEX-01 verdicts` holding these 18 lines, each exactly once, each ending `: PENDING`:
    `L1 project config loads when trusted`, `L1 global entry shown (global install)`, `L2 untrusted project layer not loaded`, `L3 prompt-input lists skill and AGENTS.md block`, `L4 uninstalled repo shows no codegraph surface`, `L7 real HOME unchanged (CODEX-01)`, `Untrusted warning`, `Trust override (-c projects trust_level) grants trust`, `A1 local command form shell-expanded`, `A1 global quoted command form runs`, `A2 AGENTS.md trust-gated in a real session`, `D-16 project .agents/skills trust-gated`, `D-15 .codex/skills read`, `D-15 CODEX_HOME/skills read`, `D-17 skill description as listed`, `Hooks.json runs behind features.hooks`, `PreToolUse stdin fields (main thread)`, `CODEX-01 verdict`.
    Commit `docs(07-04): scaffold CODEX-01 live evidence`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(cd /tmp && pwd -P)/07-live && D=.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md && test -x "$S/codegraph" && test -x "$S/codegraph-project" && test -L "$S/home/.codex/auth.json" && test "$(readlink "$S/home/.codex/auth.json")" = "$HOME/.codex/auth.json" && test -d "$S/trusted/.codegraph" && test -d "$S/untrusted/.codegraph" && test ! -e "$S/bare/.codegraph" && test ! -e "$S/bare/AGENTS.md" && test ! -e "$S/bare/.codex" && test "$(rg -c '^command = ".*/codegraph-project"$' "$S/trusted/.codex/config.toml")" = "1" && test "$(rg -c '^command = ".*/codegraph-project"$' "$S/untrusted/.codex/config.toml")" = "1" && rg -q -F '<!-- CODEGRAPH_START -->' "$S/trusted/AGENTS.md" && test -f "$S/trusted/.agents/skills/codegraph/SKILL.md" && test -x "$S/trusted/.codex/hooks/a1-probe.sh" && test "$(jq -r '.hooks.PreToolUse[0].hooks[0].command' "$S/trusted/.codex/hooks.json")" = '"$(git rev-parse --show-toplevel)/.codex/hooks/a1-probe.sh"' && test ! -e "$S/untrusted/.codex/hooks.json" && test -f "$S/home/.codex/skills/cgprobe-codexhome/SKILL.md" && test ! -e "$S/home/.codex/config.toml" && test "$(rg -c ': PENDING$' "$D")" = "18" && rg -q '^CODEX-01 verdict: PENDING$' "$D" && test "$(rg -c '^\| L[1-7]( \(CODEX-0[56]\))? \|' "$D")" = "7" && rg -q 'shasum -a 256' "$D" && rg -q 'codex-cli [0-9]' "$D" && rg -q 'learn\.chatgpt\.com|developers\.openai\.com|/openai/codex' "$D" && S2=$(git log --format=%s -5) && printf '%s\n' "$S2" | rg -q '^docs\(07-04\): scaffold CODEX-01'</automated>
    <fails_when>non-zero exit; either scratch binary missing; auth.json not a symlink to the real one; trusted/untrusted unindexed or bare carrying any codegraph surface; a project config not pointing at codegraph-project; the AGENTS.md block, shared skill or A1 probe missing; the probe command not the exact D-20 form; the untrusted repo carrying a hooks.json; a pre-existing scratch config.toml; not exactly 18 PENDING lines; the D-06 table not 7 rows; the checksum command, codex version or doc citation absent; or no scaffold commit</fails_when>
  </verify>
  <acceptance_criteria>
    - nothing under the real `$HOME` was written (the only real-HOME reads are the four sha256 lines, `readlink`, `codex --version`)
    - the D-06 rows in the document are byte-identical to 07-CONTEXT's (whitespace-normalized)
    - each of the 18 PENDING lines appears exactly once
  </acceptance_criteria>
  <done>A fresh, isolated Codex scratch and an evidence skeleton with the locked pass bar and dated citations are ready for the orchestrator.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: ORCHESTRATOR — Protocols A and B: the untrusted negative control, the bare repo, the trust override, the real TUI trust, A1 (local), A2/D-15/D-16/D-17 in real sessions, L1 (project) and L3</name>
  <precondition>Task 1's scaffold exists at `$(cd /tmp && pwd -P)/07-live` and no global codegraph install exists in the scratch HOME (`test ! -e "$S/home/.codex/config.toml"`); the orchestrating session has Herdr control.</precondition>
  <files>.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md (the skeleton and protocols)
    - .planning/phases/07-codex-parity/07-CONTEXT.md (D-03..D-06)
  </read_first>
  <action>The orchestrator (not an executor subagent) runs the model-free probes, then one interactive Codex TUI in a sibling Herdr pane plus minimal `codex exec` turns, all in the scratch env, and writes 14 of the 18 CODEX-01 lines with verbatim excerpts.</action>
  <instructions>
    Env for every command: `HOME=$S/home CODEX_HOME=$S/home/.codex`. Save every JSON/transcript to `$S/transcripts/`.
    Protocol A (model-free, before any trust exists):
    1. `$S/bare`: `codex mcp list --json` and `codex debug prompt-input`. Record the output excerpt. L4 is recorded after step 7's positive control.
    2. `$S/untrusted`: `codex mcp list --json` (stdout and stderr) and `codex debug prompt-input`. `L2 untrusted project layer not loaded: PASS` when no `codegraph` entry appears (no global install exists yet); record any warning verbatim on the `Untrusted warning:` line (or `none observed`).
    3. Trust override: `codex -c 'projects."'"$S"'/untrusted".trust_level="trusted"' mcp list --json` in `$S/untrusted` → `Trust override (-c projects trust_level) grants trust: yes|no (<excerpt>)`. Do not trust `untrusted` any other way, ever.
    Protocol B (one interactive TUI + minimal model turns):
    4. Start the Codex TUI in a sibling Herdr pane with the scratch env and cwd `$S/trusted` (e.g. export both variables in the pane, then `herdr agent start c01 --kind codex --pane <id>`). Accept the trust prompt for the folder. Show only the `[projects."…"]` lines Codex wrote to the scratch config.
    5. In the TUI run `/hooks`, trust the a1-probe hook, then prompt: `Run this exact shell command and nothing else: echo a1-probe`. `A1 local command form shell-expanded: yes` only if `$S/probes/a1-local.log` gained a line for this turn (quote it); `no` otherwise (then ESCALATE: 07-07's command form depends on it — record a `Maintainer decision:` line). From the captured stdin JSON record the key list and whether `tool_input.command` is a string or an array on `PreToolUse stdin fields (main thread):`. `Hooks.json runs behind features.hooks: yes|no` from the same marker (features list showed `hooks … true`); optionally repeat with `codex -c features.hooks=false exec --json -C "$S/trusted" 'Run this exact shell command and nothing else: echo a1-off'` and note whether the marker stayed silent.
    6. L1 project: `codex mcp list --json` in `$S/trusted` → `L1 project config loads when trusted: PASS` when `codegraph` appears with `$S/codegraph-project` in its transport; FAIL otherwise (ESCALATE).
    7. L3 and D-15: `codex debug prompt-input` in `$S/trusted` → L3 PASS when `<skills_instructions>` lists `codegraph` and the AGENTS.md message contains `## CodeGraph` (this is the positive control for step 1's absence searches — now record `L4 uninstalled repo shows no codegraph surface: PASS|FAIL` using the SAME searches on step 1's output). `D-15 .codex/skills read: yes|no` (is `cgprobe-dotcodex` listed?), `D-15 CODEX_HOME/skills read: yes|no` (`cgprobe-codexhome`). `D-17 skill description as listed:` the codegraph description exactly as the listing renders it (truncation is an advisory, no SKILL.md change).
    8. A2 and D-16 in REAL sessions: `codex exec --json -C "$S/untrusted" 'Reply with the single word ok.'` and the TUI session in `$S/trusted`; in each session's JSONL under `$S/home/.codex/sessions/` search the injected context for the codegraph skill listing and `## CodeGraph` (positive control: the trusted JSONL). `A2 AGENTS.md trust-gated in a real session: yes|no`, `D-16 project .agents/skills trust-gated: yes|no`, each with the excerpt from both sessions.
    Every line uses its fixed form: L-lines `PASS` or `FAIL (<reason>)`; yes/no lines `yes`, `no` or `not observed`, optionally followed by ` (<evidence pointer>)`. Paste at most 40 verbatim lines per item. ESCALATE (patch nothing) on any FAIL or `A1 … no`, recording `Maintainer decision: …` under the item.
  </instructions>
  <verification>The 14 lines of this task carry no PENDING, each in its allowed form with an excerpt; the untrusted repo was never trusted.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md && test "$(rg -c '^L[1-4] [^:]+: (PASS|FAIL \(.+\))$' "$D")" = "4" && rg -q '^L1 project config loads when trusted: (PASS|FAIL \(.+\))$' "$D" && test "$(rg -c '^(Trust override \(-c projects trust_level\) grants trust|A1 local command form shell-expanded|A2 AGENTS\.md trust-gated in a real session|D-16 project \.agents/skills trust-gated|D-15 \.codex/skills read|D-15 CODEX_HOME/skills read|Hooks\.json runs behind features\.hooks): (yes|no|not observed)( \(.+\))?$' "$D")" = "7" && rg -q '^Untrusted warning: .+$' "$D" && rg -q '^D-17 skill description as listed: .+$' "$D" && rg -q '^PreToolUse stdin fields \(main thread\): .+$' "$D" && ! rg -q '^(L1 project|L2 |L3 |L4 |Untrusted warning|Trust override|A1 local|A2 |D-1[5-7] |Hooks\.json|PreToolUse stdin).*: PENDING$' "$D" && if rg -q '^(L[1-4] [^:]+: FAIL|A1 local command form shell-expanded: no)' "$D"; then rg -q '^Maintainer decision: ' "$D"; fi && S=$(cd /tmp && pwd -P)/07-live && test -f "$S/home/.codex/config.toml" && ! rg -q -F "$S/untrusted" "$S/home/.codex/config.toml"</automated>
    <fails_when>non-zero exit; fewer than 4 L1-L4 lines in allowed form or L1 (project) missing; fewer than 7 yes/no lines in allowed form; the Untrusted warning, D-17 or stdin-fields line missing; any of these still PENDING; a FAIL or an A1 "no" with no Maintainer decision line; or the scratch config trusting the untrusted repo</fails_when>
  </verify>
  <acceptance_criteria>
    - every absence (L2, L4, a "no") quotes the positive-control search that found the same thing in the trusted session
    - the A1 verdict quotes the marker-log line (or its absence) for the probe turn, not the model's claim
  </acceptance_criteria>
  <resume-signal>Reply with the lines as written, e.g. "L1p=PASS L2=PASS L3=PASS L4=PASS A1local=yes A2=no D-16=no D-15=no/yes" — only after they are in 07-LIVE-SESSIONS.md.</resume-signal>
  <done>Project config loading, trust gating, skill roots and the hook command form are live evidence against the locked bar.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: ORCHESTRATOR — Protocol C: the global install, L1 (global), A1 (global quoted form), L7 and the CODEX-01 verdict; then the executor completeness gate</name>
  <precondition>Task 2 has been resumed with its 14 lines written.</precondition>
  <files>.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md
  </read_first>
  <action>The orchestrator runs the HEAD binary's global Codex install into the scratch HOME, records L1 (global) and the quoted global hook form, re-checks the real-HOME checksums and writes the verdict; the executor then runs the completeness gate and commits.</action>
  <instructions>
    1. `(cd "$S/bare" && HOME="$S/home" CODEX_HOME="$S/home/.codex" XDG_CONFIG_HOME="$S/home/.config" "$S/codegraph" install --target codex --location global --yes)`; record the report and that the scratch config's `[projects."…"]` trust lines survived the splice (count before and after).
    2. L1 global: `codex mcp list --json` in `$S/bare` shows `codegraph` with `$S/codegraph` (the global layer) and in `$S/trusted` shows `$S/codegraph-project` (the project layer wins) → `L1 global entry shown (global install): PASS|FAIL (…)`.
    3. A1 global: write `$S/home/.codex/hooks/a1-probe-global.sh` (same shape, logging to `$S/probes/a1-global.log`) and `$S/home/.codex/hooks.json` with one `PreToolUse` group (`^Bash$`, one handler, `timeout` 5) whose `command` is the single-quoted absolute script path. Trust it in the TUI's `/hooks` (restart the TUI if needed), prompt `Run this exact shell command and nothing else: echo a1-global` → `A1 global quoted command form runs: yes|no (<marker line>)`.
    4. L7: re-run the four `shasum -a 256` commands → `L7 real HOME unchanged (CODEX-01): PASS` only when every value (or `absent`) equals the pre-flight record.
    5. `CODEX-01 verdict: PASS` only if the six L-lines (L1 project, L1 global, L2, L3, L4, L7) are all PASS; otherwise `CODEX-01 verdict: FAIL` plus a `Maintainer decision:` line (07-05 requires PASS).
    After resume the executor runs the completeness gate below and commits `docs(07-04): record CODEX-01 live evidence`.
  </instructions>
  <verification>No PENDING anywhere; all 18 lines in allowed forms; the verdict consistent with the six L-lines; real HOME unchanged; no codex.go change since the plans commit.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md && ! rg -q 'PENDING' "$D" && test "$(rg -c '^L[1-7] [^:]+: (PASS|FAIL \(.+\))$' "$D")" = "6" && rg -q '^A1 global quoted command form runs: (yes|no|not observed)( \(.+\))?$' "$D" && rg -q '^L7 real HOME unchanged \(CODEX-01\): PASS$' "$D" && rg -q '^CODEX-01 verdict: (PASS|FAIL)$' "$D" && if rg -q '^CODEX-01 verdict: PASS$' "$D"; then test "$(rg -c '^L[1-7] [^:]+: PASS$' "$D")" = "6"; else rg -q '^Maintainer decision: ' "$D"; fi && L=$(git log --diff-filter=A --format=%H -- .planning/phases/07-codex-parity/07-01-PLAN.md) && B=$(printf "%s\n" "$L" | tail -1) && test -n "$B" && test -z "$(git log --format=%H "$B"..HEAD -- internal/agents/codex.go)" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-04\): record CODEX-01 live evidence'</automated>
    <fails_when>non-zero exit; any PENDING left; not exactly 6 L-lines in allowed form; the A1 global line missing or malformed; L7 not PASS; the verdict line missing, PASS with fewer than 6 PASS L-lines, or FAIL without a Maintainer decision; any codex.go commit since the plans commit; or no evidence commit</fails_when>
  </verify>
  <acceptance_criteria>
    - the real-HOME checksums after equal the pre-flight values (L7), and no real-HOME Codex/agents file was printed
    - the SUMMARY lists what each settled line means for 07-05 (D-15 SkillDirs, D-16/A2 Note wording) and 07-07 (A1 command form, stdin field list)
  </acceptance_criteria>
  <resume-signal>Reply "L1g=PASS A1global=yes L7=PASS CODEX-01=PASS" (or the FAIL lines plus the maintainer decision) once written.</resume-signal>
  <done>CODEX-01 is settled live with dated citations before any codex.go change, and every question 07-05/07-07 depend on has a recorded answer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scratch Codex → the maintainer's real Codex/agents config | a wrong HOME/CODEX_HOME would let a probe or an install touch real files |
| scratch `auth.json` symlink → real credentials | Codex may refresh tokens through the link |
| transcripts → evidence file | a summary could overstate what Codex did, or leak private config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering | real ~/.codex and ~/.agents files | medium | mitigate | every command carries HOME/CODEX_HOME=$S; only the HEAD (fixed) binary runs; sha256 before/after (L7) |
| T-07-08 | Information Disclosure | real config.toml / auth.json contents | medium | mitigate | auth.json symlinked, never read or printed; real files only checksummed; only scratch `[projects…]` lines are shown |
| T-07-09 | Spoofing (of evidence) | verdict lines | medium | mitigate | pass bar locked in CONTEXT before any session; fixed line forms; JSONL-injected context as evidence; positive-controlled absence searches; the untrusted negative control |
| T-07-10 | Tampering | auth.json via the symlink | low | accept | D-02 chose a symlink so token refresh keeps working; the credential store is Codex's own |
</threat_model>

<verification>
- 07-LIVE-SESSIONS.md: CODEX-01 section complete, no PENDING, verdict recorded; codex.go untouched
</verification>

<success_criteria>
- CODEX-01's live half: project config loading, skill paths and hooks.json behaviour recorded with dated citations before any codex.go change
- A1, A2, D-15, D-16, D-17 and the trust override answered for 07-05/07-07
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-04-SUMMARY.md` when done — every verdict line, and what each means for 07-05 and 07-07.
</output>
