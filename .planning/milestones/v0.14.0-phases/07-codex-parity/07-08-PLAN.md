---
phase: 07-codex-parity
plan: 08
type: execute
wave: 8
depends_on: ["07-07"]
files_modified:
  - internal/agents/codex_pretooluse.go
  - internal/agents/codex_pretooluse_test.go
  - internal/agents/codex.go
  - internal/agents/toml.go
  - internal/agents/toml_test.go
  - internal/agents/ownership_test.go
  - internal/cli/install.go
  - internal/cli/uninstall.go
  - internal/cli/install_test.go
  - docs/CLI-REFERENCE.md
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
autonomous: true
requirements: [CODEX-05]
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`--pretool-nudge` configures Claude Code AND Codex CLI: given true it opts Codex in (On); not given it keeps a Codex opt-in whose evidence is codegraph's own exact-identity group in that location's hooks.json, re-rendering the guard with the current ExecPath and adding nothing where no own group exists (Keep); `--pretool-nudge=false` removes the group and the guard (Off); uninstall always removes them (D-18, D-23)."
    - "When the user has explicitly disabled Codex hooks — `[features] hooks = false`, the deprecated `codex_hooks = false`, a root dotted `features.hooks = false` or an inline `features = { hooks = false }` — in the config that governs the location (the project `.codex/config.toml` when it sets the key, else `~/.codex/config.toml`; the global config for a global install), every hooks write is skipped and a Note names the file and key; nothing is written (D-18)."
    - "Every opt-in Codex install, and every Keep refresh that changed hooks.json, carries a Note that Codex skips a new or changed hook until it is trusted in `/hooks`; no shipped Note, help text or reference line tells users to bypass hook trust (D-19)."
    - "The registered definition is byte-stable across a change of binary location: a Keep refresh after moving the binary rewrites only the guard and reports hooks.json `unchanged`, so Codex's trust hash stays valid (D-19)."
    - "A hand-edited codegraph handler command in hooks.json stops matching by exact identity and a fresh group is appended after it; an unrelated `^Bash$` group survives install and uninstall unchanged (242ec0a, D-23)."
    - "The D-09 note widens: `--pretool-nudge` given while neither Claude Code nor Codex CLI is selected prints one stderr note naming both; no note when either is selected; `install --help` and `uninstall --help` describe the Codex hook, its `/hooks` trust step and its removal, and `docs/CLI-REFERENCE.md` is regenerated only through `task docs:cli` under `task docs:cli:drift` (CODEX-05 docs statement)."
    - "The ownership table's codex leaves plant a foreign `^Bash$` PreToolUse group in hooks.json and assert it survives while codegraph's own group and guard are gone after uninstall."
    - "Family (g) in 07-MUTATION-LOG.md: Keep treated as Off, a disabled hooks-off check, the Claude-only note, and matcher-shape ownership each turn a named guard RED, reverted byte-clean."
  artifacts:
    - path: "internal/agents/codex_pretooluse.go"
      provides: "codexHooksExplicitlyDisabled, codexHookTrustNote, codexHooksDisabledNote, the On/Keep/Off dispatcher"
      contains: "func codexHooksExplicitlyDisabled(loc Location) (bool, string, error) {"
    - path: "internal/agents/toml.go"
      provides: "tomlBoolSetting(content, table, key string) (value, set bool)"
      contains: "func tomlBoolSetting("
    - path: "docs/CLI-REFERENCE.md"
      provides: "regenerated reference naming the Codex opt-in and /hooks trust"
      contains: "/hooks"
  key_links:
    - from: "internal/cli/install.go --pretool-nudge tri-state"
      to: "internal/agents/codex.go Install (PreToolNudgeOn/Keep/Off)"
      via: "InstallOptions.PreToolNudge"
      pattern: "PreToolNudge"
    - from: "internal/agents/codex_pretooluse.go Keep"
      to: "internal/agents/shared.go hasOwnHookBlock"
      via: "stickiness evidence = codegraph's own exact-identity group (D-23), not the shared skill manifest"
      pattern: "hasOwnHookBlock\\("
  prohibitions:
    - statement: "MUST NOT register the Codex hook without the explicit `--pretool-nudge` opt-in, nor when the user explicitly disabled Codex hooks (D-18)."
      status: resolved
      verification: test
    - statement: "MUST NOT tell users, in any Note, help text or document codegraph ships, to bypass Codex's hook trust review (D-19)."
      status: resolved
      verification: test
---

<objective>
Give the Codex nudge its lifecycle and its user-facing surface: sticky Keep/On/Off keyed on codegraph's own
hooks.json group (D-23), the explicit `hooks = false` skip (D-18), the `/hooks` trust Note (D-19), the widened
`--pretool-nudge` flag, note and help text, and the regenerated CLI reference — then prove the guards can fail.

Purpose: CODEX-05's "installed only on the --pretool-nudge opt-in, skipped with a message when the user has
explicitly disabled Codex hooks, and the docs state that Codex skips a new or changed hook until it is trusted
in /hooks"; 07-VALIDATION row `07-NUDGE` (lifecycle half).

Output: `test(07-08):`/`feat(07-08):` commits, `docs/CLI-REFERENCE.md` regenerated, Family (g).

Notes: `codegraph upgrade` still refreshes Claude only (deferred in 07-CONTEXT); a plain `codegraph install`
(Keep) re-renders the Codex guard. `GOTOOLCHAIN=go1.26.6`; commits end with `Co-Authored-By: Claude Opus 5
<noreply@anthropic.com>`; never `[ci skip]`; never gate on `gsd-tools check tdd-red-evidence`. Help text that
the generated reference is searched for keeps each checked phrase on one source line (06-07 pattern). Tests
that check for the trust-bypass flag build its name by concatenation so the test source itself never matches.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-07-SUMMARY.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-04-PLAN.md
@internal/agents/codex_pretooluse.go
@internal/agents/codex.go
@internal/agents/shared.go
@internal/agents/toml.go
@internal/agents/ownership_test.go
@internal/cli/install.go
@internal/cli/uninstall.go
@internal/cli/install_test.go
@internal/cli/cli_reference_test.go
</context>

<interfaces>
- Existing: `PreToolNudgeKeep/On/Off` (types.go), `hasOwnHookBlock(path, event, ownCommands) (bool, error)`,
  `writeHookEntry`, `removeHookEntry`, `installCodexPreToolNudge`, `uninstallCodexPreToolNudge`,
  `codexPreToolUseBlocks`, `codexHooksJSONPath`, `codexConfigPath`, the 07-01 TOML scanner.
- New: `func tomlBoolSetting(content, table, key string) (value, set bool)` (toml.go);
  `func codexHooksExplicitlyDisabled(loc Location) (bool, string, error) {` (pinned signature line — Family g2
  inserts an early return after it); `codexHookTrustNote(loc Location) string`; `codexHooksDisabledNote(source string) string`.
- CLI today (install.go:131-147): the note text `note: --pretool-nudge only configures Claude Code, which is not
  among the selected agents; nothing was changed for it`, printed once to stderr when the flag is Changed and
  Claude is not selected; install_test.go's `preToolNudgeNote` const mirrors it.
- Reference regeneration: `GOTOOLCHAIN=go1.26.6 task docs:cli`, gate `GOTOOLCHAIN=go1.26.6 task docs:cli:drift`
  ("byte-identical to a fresh regeneration").
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan adds `tomlBoolSetting`,
`codexHooksExplicitlyDisabled`, the trust and disabled Notes, the Keep/Off semantics, the widened CLI flag/help,
the regenerated reference, the listed tests and Family (g).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sticky Keep/On/Off on hooks.json evidence, the explicit hooks-off skip, and the /hooks trust Note</name>
  <files>internal/agents/codex_pretooluse.go, internal/agents/codex_pretooluse_test.go, internal/agents/codex.go, internal/agents/toml.go, internal/agents/toml_test.go</files>
  <read_first>
    - internal/agents/codex_pretooluse.go and codex.go (07-07 result: On only)
    - internal/agents/claude.go Install's PreToolNudge handling and internal/agents/claude_pretooluse_lifecycle_test.go (the Keep/On/Off precedent and test names)
    - internal/agents/shared.go (hasOwnHookBlock :495)
    - internal/agents/toml.go (the 07-01 scanner to reuse for tomlBoolSetting)
  </read_first>
  <behavior>
    - TestTOMLBoolSetting: subtests table_false, table_true, table_indented_header, trailing_comment, dotted_root, inline_table, not_bool_is_unset, absent_is_unset, key_in_other_table_is_unset, inside_multiline_string_is_unset.
    - TestCodexPreToolNudge_OnWritesAndNotesTrust: local and global — group and guard written, exactly one Note mentioning `/hooks`.
    - TestCodexPreToolNudge_KeepRefreshesWhenOwnGroupPresent: On with ExecPath A, then Keep with ExecPath B → guard renders B, hooks.json FileResult `unchanged` and bytes identical, no trust Note (definition unchanged).
    - TestCodexPreToolNudge_KeepNoopWhenNotOptedIn: Keep on a fresh location writes nothing.
    - TestCodexPreToolNudge_KeepWithMalformedHooksJSONTouchesNothing: a malformed hooks.json with Keep → no write, no error, bytes identical.
    - TestCodexPreToolNudge_OffRemovesAndForgets: On then Off → group and guard gone; a later Keep adds nothing.
    - TestCodexPreToolNudge_SkippedWhenHooksDisabled: subtests features_hooks_false_local, global_false_applies_to_local, codex_hooks_false_deprecated, dotted_root_key, inline_table, local_true_overrides_global_false (writes), hooks_true_writes — skip cases write no hooks.json/guard and carry one Note naming the file and key.
    - TestCodexPreToolNudge_ReinstallIsIdempotent: On twice → second run all `unchanged`.
    - TestCodexPreToolNudge_HandEditedOwnGroupDuplicates: an own handler's command hand-edited → On appends a fresh group after it; the edited one survives.
    - TestCodexNotesNeverAdviseTrustBypass: every Note produced by Codex installs across modes, scopes and the skip cases contains no `--dangerously-` + `bypass-hook-trust` token (built by concatenation).
  </behavior>
  <action>
    RED commit `test(07-08): add failing Codex nudge lifecycle and hooks-off tests`. GREEN commit `feat(07-08): sticky Codex PreToolUse opt-in with the hooks-off skip and trust note`:
    - `tomlBoolSetting` reuses the 07-01 scanner and its `isTOMLHeaderLine` predicate (add no second `strings.TrimLeft(line, " \t")` to toml.go — it is Family (a1)'s single mutation site); it recognizes `key = true|false` inside `[table]`, a root dotted `table.key = …`, and a single-line inline `table = { … key = … }`; any other value is unset.
    - `codexHooksExplicitlyDisabled(loc)`: for local, read the project `.codex/config.toml`; if it sets `features.hooks` use it, else if it sets `features.codex_hooks` use it, else fall back to the global `~/.codex/config.toml` the same way; for global read only the global file. Return the governing file path and key as `source`. A read error other than not-exist is returned (the caller records it and writes nothing).
    - Dispatcher in codex.go Install: On → disabled? skip + disabled Note : install + trust Note. Keep → `hasOwnHookBlock` true (and not disabled) → install (refresh), trust Note only if the hooks.json FileResult is created/updated; false or a read error → nothing. Off → `uninstallCodexPreToolNudge`. Uninstall unchanged (always attempts removal).
    - Note wording (discretion): the trust Note says Codex skips a new or changed hook until it is trusted in `/hooks`, so open `/hooks` in Codex and trust the codegraph PreToolUse hook; the disabled Note names the file and key and says the nudge was not installed.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-08\): ' && printf '%s\n' "$S" | rg -q '^feat\(07-08\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestTOMLBoolSetting$|TestCodexPreToolNudge_|TestCodexNotesNeverAdviseTrustBypass$' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -3 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c '^--- PASS: TestCodexPreToolNudge_(OnWritesAndNotesTrust|KeepRefreshesWhenOwnGroupPresent|KeepNoopWhenNotOptedIn|KeepWithMalformedHooksJSONTouchesNothing|OffRemovesAndForgets|SkippedWhenHooksDisabled|ReinstallIsIdempotent|HandEditedOwnGroupDuplicates) ' "$O/t1.txt")" = "8" && test "$(rg -c -- '--- PASS: TestCodexPreToolNudge_SkippedWhenHooksDisabled/(features_hooks_false_local|global_false_applies_to_local|codex_hooks_false_deprecated|dotted_root_key|inline_table|local_true_overrides_global_false|hooks_true_writes) ' "$O/t1.txt")" = "7" && test "$(rg -c -- '--- PASS: TestTOMLBoolSetting/[a-z_]+ ' "$O/t1.txt")" = "10" && rg -q '^--- PASS: TestCodexNotesNeverAdviseTrustBypass ' "$O/t1.txt" && test "$(rg -c '^func codexHooksExplicitlyDisabled\(loc Location\) \(bool, string, error\) \{$' internal/agents/codex_pretooluse.go)" = "1" && test "$(rg -c -F 'strings.TrimLeft(line, " \t")' internal/agents/toml.go)" = "1"</automated>
    <fails_when>non-zero exit; a missing test(07-08)/feat(07-08) subject; build/gofmt/vet failure; any FAIL line; fewer than 8 lifecycle PASS lines, 7 hooks-off subtests or 10 TOML-bool subtests; the trust-bypass test absent; or the pinned signature line missing</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY pastes the RED `--- FAIL:` lines for Keep, Off and the hooks-off skip
    - `go test ./internal/agents/ -count=1` is `ok`
  </acceptance_criteria>
  <done>The Codex opt-in is sticky on its own hooks.json evidence, removable, skipped with a Note when hooks are explicitly off, and every opt-in names the /hooks trust step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen --pretool-nudge in the CLI, its note and help; regenerate the reference; cover Codex hooks in the ownership table</name>
  <files>internal/cli/install.go, internal/cli/uninstall.go, internal/cli/install_test.go, docs/CLI-REFERENCE.md, internal/agents/ownership_test.go</files>
  <read_first>
    - internal/cli/install.go (Long, Example, the D-09 note at :131-147, the flag help at :158)
    - internal/cli/uninstall.go (Long)
    - internal/cli/install_test.go (preToolNudgeNote, TestInstall_PreToolNudge_*)
    - internal/cli/cli_reference_test.go (TestEveryRegisteredFlagIsAccountedFor)
    - internal/agents/ownership_test.go (plantForeignFixtures, reproduce242ec0aPrecondition, assertOwnEntriesGoneAfterUninstall)
  </read_first>
  <behavior>
    - TestInstall_PreToolNudge_NoteWhenNeitherClaudeNorCodexSelected (renamed from …WhenClaudeNotSelected; `--target cursor`): the widened note exactly once on stderr for both flag values.
    - TestInstall_PreToolNudge_NoNoteWhenCodexSelected: `--target codex,cursor --location local --pretool-nudge` → no note; `.codex/hooks.json` and the guard exist.
    - TestInstall_PreToolNudge_CodexOptInRegisters: CLI-level On → registered; plain install → kept; `--pretool-nudge=false` → removed.
    - TestInstallHelpNeverAdvisesTrustBypass: `install --help`, `uninstall --help` and docs/CLI-REFERENCE.md contain no `--dangerously-` + `bypass-hook-trust` token; `install --help` mentions `Codex` and `/hooks`.
    - TestOwnershipExactIdentity codex leaves (still 32 leaves): a foreign `{"matcher":"^Bash$","hooks":[{"type":"command","command":"/opt/some-other-tool/on-startup.sh"}]}` group planted in the location's hooks.json before Install survives install and uninstall (decoded JSON equal); after uninstall no hooks.json group carries codegraph's command and the guard is absent.
  </behavior>
  <action>
    RED commit `test(07-08): expect --pretool-nudge to configure Codex too`. GREEN commit `feat(07-08): --pretool-nudge configures Codex CLI too`:
    - install.go: the note fires when the flag is Changed and neither Claude nor Codex is among the targets; text `note: --pretool-nudge only configures Claude Code and Codex CLI, neither of which is among the selected agents; nothing was changed for them`. Flag help names both agents and stays true (Codex's opt-in is kept across install; upgrade refreshes Claude's only). Long help: the pretool-nudge sentence covers both agents, keeps "never blocks a tool call" on one source line, and adds that Codex skips a new or changed hook until it is trusted in /hooks. Add the Example `codegraph install --target codex --location local --pretool-nudge`.
    - uninstall.go Long: it also removes the Claude Code and Codex CLI PreToolUse nudge hooks and their guard scripts when present.
    - Regenerate docs/CLI-REFERENCE.md with `GOTOOLCHAIN=go1.26.6 task docs:cli` in the same commit.
    - ownership_test.go: plant the foreign group for codex leaves and extend `assertOwnEntriesGoneAfterUninstall` for Codex hooks, keeping the 32-leaf count and the 242ec0a citation.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && test "$(printf '%s\n' "$S" | rg -c '^test\(07-08\): ')" -ge 2 && test "$(printf '%s\n' "$S" | rg -c '^feat\(07-08\): ')" -ge 2 && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli internal/agents)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/ ./internal/agents/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ ./internal/agents/ -count=1 -run 'TestInstall_PreToolNudge_|TestInstallHelpNeverAdvisesTrustBypass$|TestEveryRegisteredFlagIsAccountedFor$|TestPlainGolden$|TestOwnershipExactIdentity$' -v > "$O/t2.txt" 2>&1; echo "exit=$?" >> "$O/t2.txt") && tail -3 "$O/t2.txt" && rg -q '^exit=0$' "$O/t2.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t2.txt" && test "$(rg -c '^--- PASS: TestInstall_PreToolNudge_(NoteWhenNeitherClaudeNorCodexSelected|NoNoteWhenCodexSelected|CodexOptInRegisters|OptInRegisters|StickyAcrossPlainInstall|ExplicitFalseRemoves|NoNoteWhenClaudeSelected) ' "$O/t2.txt")" = "7" && rg -q '^--- PASS: TestInstallHelpNeverAdvisesTrustBypass ' "$O/t2.txt" && test "$(rg -c -- '--- PASS: TestOwnershipExactIdentity/[a-z]+/(global|local)/(foreign-codegraph-dir|clean) ' "$O/t2.txt")" = "32" && (GOTOOLCHAIN=go1.26.6 task docs:cli:drift > "$O/drift.txt" 2>&1; echo "exit=$?" >> "$O/drift.txt") && rg -q '^exit=0$' "$O/drift.txt" && rg -q 'Codex' docs/CLI-REFERENCE.md && rg -q '/hooks' docs/CLI-REFERENCE.md && rg -q 'never blocks a tool call' docs/CLI-REFERENCE.md && rg -q '242ec0a' internal/agents/ownership_test.go</automated>
    <fails_when>non-zero exit; fewer than two test(07-08) or feat(07-08) subjects; build/gofmt/vet failure; any FAIL line; fewer than 7 named pretool-nudge PASS lines, the help-bypass test absent, or not 32 ownership leaves; docs:cli:drift not exit 0; the reference not naming Codex, /hooks or "never blocks a tool call"; or the 242ec0a citation gone</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --stat <the feat(07-08) CLI commit> -- docs/CLI-REFERENCE.md` lists the file (regenerated in the same commit as the help change), and only the install and uninstall sections changed
    - long-help lines stay within 80 columns
  </acceptance_criteria>
  <done>The CLI opt-in, note, help and generated reference describe the Codex nudge truthfully, and ownership covers Codex's hooks.json.</done>
</task>

<task type="auto">
  <name>Task 3: Family (g) — prove the lifecycle, hooks-off, note and ownership guards can fail</name>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
    - internal/agents/codex_pretooluse.go, internal/agents/codex.go, internal/cli/install.go, internal/agents/shared.go (final code)
  </read_first>
  <action>
    Append in the established shape, each reverted byte-clean with a green control:
    - `## Family (g1)` — the dispatcher treats Keep as Off → `TestCodexPreToolNudge_KeepRefreshesWhenOwnGroupPresent` RED.
    - `## Family (g2)` — an early `return false, "", nil` after the pinned `codexHooksExplicitlyDisabled` signature line → `TestCodexPreToolNudge_SkippedWhenHooksDisabled` RED.
    - `## Family (g3)` — the note condition narrowed back to Claude only → `TestInstall_PreToolNudge_NoNoteWhenCodexSelected` RED.
    - `## Family (g4)` — `blockOwnsAnyCommand` also treats a `^Bash$` matcher as owned (the 242ec0a shape) → `TestOwnershipExactIdentity/codex/` RED.
    Commit `docs(07-08): Family (g) mutation log`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && F=internal/agents/codex_pretooluse.go && git diff --quiet -- "$F" && perl -0pi -e 's/(func codexHooksExplicitlyDisabled\(loc Location\) \(bool, string, error\) \{\n)/$1\treturn false, "", nil\n/' "$F" && ! git diff --quiet -- "$F" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestCodexPreToolNudge_SkippedWhenHooksDisabled$' -v > "$O/red-g2.txt" 2>&1; echo "exit=$?" >> "$O/red-g2.txt"); git checkout -- "$F"; git diff --quiet -- "$F" && rg -q -- '--- FAIL: TestCodexPreToolNudge_SkippedWhenHooksDisabled' "$O/red-g2.txt" && ! rg -q '^exit=0$' "$O/red-g2.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(g[1-4]\)' "$M")" = "4" && rg -q -- '--- FAIL: TestCodexPreToolNudge_KeepRefreshesWhenOwnGroupPresent' "$M" && rg -q -- '--- FAIL: TestCodexPreToolNudge_SkippedWhenHooksDisabled' "$M" && rg -q -- '--- FAIL: TestInstall_PreToolNudge_NoNoteWhenCodexSelected' "$M" && rg -q -- '--- FAIL: TestOwnershipExactIdentity/codex/' "$M" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1 > "$O/pkgs.txt" 2>&1; echo "exit=$?" >> "$O/pkgs.txt") && rg -q '^exit=0$' "$O/pkgs.txt" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-08\): Family \(g\)'</automated>
    <fails_when>non-zero exit; the file dirty before the plant or after the revert; the planted run exiting 0 or lacking the FAIL line; fewer than 4 Family (g) sections or any FAIL excerpt missing; the packages not green; or no docs(07-08) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - each Family (g) section carries the six convention headings and a byte-clean revert
  </acceptance_criteria>
  <done>The Codex nudge's lifecycle, consent and ownership guards are proven able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags / user config → whether a hook is installed | consent: opt-in only, and never against an explicit hooks-off setting |
| codegraph's shipped text → user behaviour | advice that weakens Codex's trust review would be a security regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-23 | Elevation of Privilege | hook installed without consent or against hooks=false | medium | mitigate | opt-in only (Keep never adds); codexHooksExplicitlyDisabled skip; Family (g1)/(g2) |
| T-07-24 | Elevation of Privilege | users told to bypass Codex hook trust | medium | mitigate | TestCodexNotesNeverAdviseTrustBypass, TestInstallHelpNeverAdvisesTrustBypass over Notes, help and the reference |
| T-07-25 | Tampering | malformed hooks.json on Keep | low | mitigate | Keep touches nothing when the file cannot be read |
| T-07-26 | Tampering | foreign `^Bash$` groups | high | mitigate | exact-identity ownership; foreign group planted in every codex ownership leaf; Family (g4) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ -count=1` green; `task docs:cli:drift` exit 0
</verification>

<success_criteria>
- CODEX-05 lifecycle and docs statement delivered; D-18, D-19, D-23 implemented and positive-controlled
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-08-SUMMARY.md` when done — RED transcripts and the reference diff summary.
</output>
