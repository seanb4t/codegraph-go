---
phase: 07-codex-parity
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - internal/cli/tui/agentpicker.go
  - internal/cli/tui/daemonpicker.go
  - internal/cli/tui/agentpicker_test.go
  - internal/cli/tui/daemonpicker_test.go
  - test/tmux/install_cancel_test.go
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
autonomous: false
requirements: [FIX-03]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "At a 100x30 window with the real registry's 8 targets, the agent picker's rendered view (`lipgloss.Height(View().Content)`) is at most 30 lines and contains the footer text `space: toggle` and every target's display name (D-25, D-26)."
    - "Each rendered row costs exactly one line: `checkboxDelegate.Render` and `daemonDelegate.Render` no longer write a trailing newline, because bubbles v2's list inserts its own separator; nothing else is changed — not `helpFooterLines`, not the list sizing, not pagination (D-24)."
    - "FIX-03 boundary (explicit): for window heights 29, 30 and 31 at width 100 with all 8 targets the view is at most that many lines and contains `space: toggle`."
    - "FIX-03 adjacency (explicit): the 8 target rows are 8 consecutive lines of the view, with no blank line between two adjacent rows."
    - "FIX-03 empty (explicit): a picker over an empty roster renders at a 100x30 window without panicking, within 30 lines, and still shows the footer."
    - "FIX-03 ordering (explicit): the rows appear in `agents.AllTargets()` order (TargetID-sorted)."
    - "FIX-03 precision (explicit): at window heights 1 and 2 the list height floors at 1 (never zero or negative) and `View()` returns without panicking."
    - "The daemon picker gets the equivalent guard: 8 records at 100x30 render within 30 lines with its footer `enter: stop selected` visible."
    - "TTY-05 in the tmux harness is re-anchored on the footer text `space: toggle` (and all 8 display names) instead of the title workaround; `TMUX_EXPECTED_TESTS` stays 6 because no top-level test is added."
    - "Family (c) in 07-MUTATION-LOG.md: re-adding the trailing newline turns the model-level tests RED (c1 agent, c2 daemon) and the real-PTY TTY-05 assertion RED under local tmux (c3, orchestrator), each reverted byte-clean; the orchestrator records a GREEN `task test:tmux` run (executed=6) at the fixed HEAD."
  artifacts:
    - path: "internal/cli/tui/agentpicker_test.go"
      provides: "TestAgentPickerFootprintFitsDefaultPane, TestAgentPickerFootprint_HeightBoundaries, TestAgentPickerFootprint_EmptyRoster"
      contains: "TestAgentPickerFootprintFitsDefaultPane"
    - path: "internal/cli/tui/daemonpicker_test.go"
      provides: "TestDaemonPickerFootprintFitsDefaultPane"
      contains: "TestDaemonPickerFootprintFitsDefaultPane"
    - path: "test/tmux/install_cancel_test.go"
      provides: "TTY-05 anchored on the footer text"
      contains: "space: toggle"
  key_links:
    - from: "internal/cli/tui/agentpicker.go checkboxDelegate.Render"
      to: "charm.land/bubbles/v2 list populatedView separator"
      via: "one line per item: the delegate writes no newline, the list supplies the separator"
      pattern: "Fprintf\\(w, \"%s%s %s\", cursor, box, ai\\.target\\.DisplayName\\(\\)\\)"
    - from: "07-09 (after CODEX-02)"
      to: "test/tmux/install_cancel_test.go TTY-05"
      via: "the tmux assertion is re-run after the scope flip (D-25)"
      pattern: "space: toggle"
  prohibitions:
    - statement: "MUST NOT mask the overflow by shrinking the footer, changing `helpFooterLines`, resizing the list, or disabling pagination — the fix is the delegate's own newline, proven by a failing test first (D-24)."
      status: resolved
      verification: test
---

<objective>
Fix FIX-03 at its cause (D-24): each picker row currently costs two lines because the delegate writes its own
newline and bubbles v2's `populatedView` adds a separator, so 8 targets overflow a 100x30 pane by 7 lines and
the help footer never shows. Prove it with a model-level test first, re-anchor the tmux TTY-05 assertion on
the footer (D-25), and have the orchestrator show the real-PTY assertion RED on the pre-fix code and GREEN
after. This lands before CODEX-02 (D-01); 07-09 re-runs the tmux assertion after the scope flip.

Purpose: FIX-03 and 07-VALIDATION row `07-FIX03`. D-26: the picker keeps all 8 rows; nothing here filters
rows per location.

Output: `test(07-03):` RED commit (model tests + tmux re-anchor), `fix(07-03):` GREEN commit, Family (c1)/(c2)
by the executor, Family (c3) and the tmux GREEN run by the orchestrator.

Notes: `GOTOOLCHAIN=go1.26.6` on every Go command; never gate on `gsd-tools check tdd-red-evidence`; commits
end with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never `[ci skip]`. tmux is NOT installed on
this machine (07-RESEARCH Environment Availability): the executor never runs `task test:tmux`; it only
compiles the tmux package with `go vet -tags tmux`. Task 3 is the orchestrator's environment step.
If the model-level test is still RED after the newline fix, STOP and report — D-24 is a code-reading
hypothesis and the footer budget must not be patched to make it pass.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-RESEARCH.md
@internal/cli/tui/agentpicker.go
@internal/cli/tui/daemonpicker.go
@internal/cli/tui/agentpicker_test.go
@internal/cli/tui/daemonpicker_test.go
@test/tmux/install_cancel_test.go
@Taskfile.yml
</context>

<interfaces>
- `newAgentPickerModel(all []agents.AgentTarget, detection map[agents.TargetID]agents.DetectionResult) agentPickerModel` (agentpicker.go:85); `Update(tea.WindowSizeMsg{Width, Height})` sizes the list to Height-`helpFooterLines`; `View() tea.View` whose `Content` field is the plain string (bubbletea v2.0.8 tea.go:84-96).
- `newDaemonPickerModel(currentRepo string, records []daemon.Record) daemonPickerModel` (daemonpicker.go, line ~161); `rec(repoRoot, pid)` builder in daemonpicker_test.go.
- `lipgloss.Height(string) int` (charm.land/lipgloss/v2 v2.0.5 size.go:29).
- `const helpFooterLines = 2` (daemonpicker.go) — unchanged by this plan.
- After the fix the two delegate calls are exactly
  `fmt.Fprintf(w, "%s%s %s", cursor, box, ai.target.DisplayName())` and
  `fmt.Fprintf(w, "%s%s (pid %d, up %s)", cursor, filepath.Base(di.record.RepoRoot), di.record.PID, age)`
  (Family (c) re-plants the newline against these exact lines).
- Real display names (for the tmux test, which cannot import the registry's test fakes): Antigravity, Claude Code, Codex CLI, Cursor, Gemini CLI, Hermes Agent, Kiro, opencode.
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan adds the four footprint tests, the TTY-05
footer anchor, and Family (c).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A failing model-level footprint test, the tmux re-anchor, then the one-line delegate fix</name>
  <files>internal/cli/tui/agentpicker.go, internal/cli/tui/daemonpicker.go, internal/cli/tui/agentpicker_test.go, internal/cli/tui/daemonpicker_test.go, test/tmux/install_cancel_test.go</files>
  <read_first>
    - internal/cli/tui/agentpicker.go (Render at :51-65, Update's WindowSizeMsg branch at :116-125, View at :141-149)
    - internal/cli/tui/daemonpicker.go (helpFooterLines, daemonDelegate.Render, Update, View)
    - internal/cli/tui/agentpicker_test.go and daemonpicker_test.go (fakeTargets, rec, key-message builders)
    - test/tmux/install_cancel_test.go (the TTY-05 title workaround and its comment)
    - /Users/sean/go/pkg/mod/charm.land/bubbles/v2@v2.1.1/list/list.go lines 1210-1230 (populatedView's separator) — read-only, to confirm the cause
  </read_first>
  <behavior>
    - TestAgentPickerFootprintFitsDefaultPane: `all := agents.AllTargets()`, assert `len(all) == 8` (D-26), build the model with empty detection, send `tea.WindowSizeMsg{Width: 100, Height: 30}`, then `lipgloss.Height(view.Content) <= 30`, the content contains `space: toggle`, every display name appears, the 8 rows are consecutive lines, and they appear in `AllTargets()` order.
    - TestAgentPickerFootprint_HeightBoundaries: subtests h29/h30/h31 (height <= h and footer present) and h1/h2 (no panic, non-empty content).
    - TestAgentPickerFootprint_EmptyRoster: `newAgentPickerModel(nil, nil)` at 100x30 renders within 30 lines with the footer.
    - TestDaemonPickerFootprintFitsDefaultPane: 8 `rec(...)` records at 100x30 render within 30 lines and contain `enter: stop selected`.
  </behavior>
  <action>
    RED commit `test(07-03): add failing picker footprint tests and anchor TTY-05 on the footer`:
    - Add the four tests above (package tui; import charm.land/lipgloss/v2 in the tests only).
    - In test/tmux/install_cancel_test.go replace the title-text positive control with the footer text: assert the converged capture contains `space: toggle` and each of the 8 display names listed in the interfaces block, and rewrite the explanatory comment to say the footer is the positive control since FIX-03 (drop the "footer never appears" explanation). Do not add a top-level Test function (TMUX_EXPECTED_TESTS stays 6). The tmux package must still compile: `GOTOOLCHAIN=go1.26.6 go vet -tags tmux ./test/tmux/`.
    - Run the model tests and capture the RED transcript (expected: height 35 > 30 at 8 targets, the footer missing) for the SUMMARY.
    GREEN commit `fix(07-03): render one line per picker row`: drop the trailing newline from the two delegate `Fprintf` format strings so they read exactly as the interfaces block shows, and update each Render doc comment to say the list supplies the row separator. Change nothing else in either non-test file.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-03\): ' && printf '%s\n' "$S" | rg -q '^fix\(07-03\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli/tui test/tmux)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/tui/ && GOTOOLCHAIN=go1.26.6 go vet -tags tmux ./test/tmux/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/tui/ -count=1 -run 'TestAgentPicker|TestDaemonPicker' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -3 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c '^--- PASS: (TestAgentPickerFootprintFitsDefaultPane|TestAgentPickerFootprint_HeightBoundaries|TestAgentPickerFootprint_EmptyRoster|TestDaemonPickerFootprintFitsDefaultPane) ' "$O/t1.txt")" = "4" && test "$(rg -c -- '--- PASS: TestAgentPickerFootprint_HeightBoundaries/h(1|2|29|30|31) ' "$O/t1.txt")" = "5" && test "$(rg -c -F 'fmt.Fprintf(w, "%s%s %s", cursor, box, ai.target.DisplayName())' internal/cli/tui/agentpicker.go)" = "1" && test "$(rg -c -F 'fmt.Fprintf(w, "%s%s (pid %d, up %s)", cursor, filepath.Base(di.record.RepoRoot), di.record.PID, age)' internal/cli/tui/daemonpicker.go)" = "1" && test "$(rg -c '^const helpFooterLines = 2$' internal/cli/tui/daemonpicker.go)" = "1" && rg -q -F 'space: toggle' test/tmux/install_cancel_test.go && ! rg -q -F 'strings.Contains(capture, "Select agents to configure")' test/tmux/install_cancel_test.go && test "$(rg -c '^func Test' test/tmux/install_cancel_test.go)" = "1" && rg -q 'TMUX_EXPECTED_TESTS: 6$' Taskfile.yml</automated>
    <fails_when>non-zero exit; a missing test(07-03)/fix(07-03) subject; build/gofmt/vet failure (including the tmux-tagged package); fewer than 4 named footprint PASS lines or 5 boundary subtests; any FAIL line; either fixed Fprintf line absent; helpFooterLines changed; the tmux test not anchored on `space: toggle`, still asserting the title, or gaining a top-level test; or TMUX_EXPECTED_TESTS changed</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY pastes the RED `--- FAIL: TestAgentPickerFootprintFitsDefaultPane` line with the measured height (35 expected at 8 targets)
    - `git show <fix(07-03) sha> --stat` lists only internal/cli/tui/agentpicker.go and internal/cli/tui/daemonpicker.go
    - in that fix commit, every changed non-comment line is one of the two Fprintf calls (`git show <sha> -U0` minus comment and header lines leaves only those)
  </acceptance_criteria>
  <done>The two-lines-per-row overflow is proven by a failing model test and fixed at the delegate, with the tmux assertion re-anchored on the footer.</done>
</task>

<task type="auto">
  <name>Task 2: Family (c1)/(c2) — prove the model-level footprint guards can fail</name>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
    - internal/cli/tui/agentpicker.go, internal/cli/tui/daemonpicker.go (the fixed Fprintf lines)
  </read_first>
  <action>
    Append `## Family (c1)` and `## Family (c2)` in the established shape. (c1): `perl -pi -e 's/Fprintf\(w, "%s%s %s", cursor, box/Fprintf(w, "%s%s %s\\n", cursor, box/' internal/cli/tui/agentpicker.go` — the pre-fix code byte for byte — against `TestAgentPickerFootprintFitsDefaultPane`. (c2): `perl -pi -e 's/Fprintf\(w, "%s%s \(pid %d, up %s\)"/Fprintf(w, "%s%s (pid %d, up %s)\\n"/' internal/cli/tui/daemonpicker.go` against `TestDaemonPickerFootprintFitsDefaultPane`. Paste verbatim `--- FAIL:` lines plus `exit=`, revert with `git checkout -- <file>` behind the cleanliness gates, green control. Leave a `## Family (c3)` heading with the line `Orchestrator step — pending Task 3.` for the tmux run. Commit `docs(07-03): Family (c1-c2) mutation log`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && git diff --quiet -- internal/cli/tui/agentpicker.go && perl -pi -e 's/Fprintf\(w, "%s%s %s", cursor, box/Fprintf(w, "%s%s %s\\n", cursor, box/' internal/cli/tui/agentpicker.go && ! git diff --quiet -- internal/cli/tui/agentpicker.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/tui/ -count=1 -run 'TestAgentPickerFootprintFitsDefaultPane$' -v > "$O/red-c1.txt" 2>&1; echo "exit=$?" >> "$O/red-c1.txt"); git checkout -- internal/cli/tui/agentpicker.go; git diff --quiet -- internal/cli/tui/agentpicker.go && rg -q -- '--- FAIL: TestAgentPickerFootprintFitsDefaultPane' "$O/red-c1.txt" && ! rg -q '^exit=0$' "$O/red-c1.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(c[1-3]\)' "$M")" = "3" && rg -q -- '--- FAIL: TestAgentPickerFootprintFitsDefaultPane' "$M" && rg -q -- '--- FAIL: TestDaemonPickerFootprintFitsDefaultPane' "$M" && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/tui/ -count=1 > "$O/pkg.txt" 2>&1; echo "exit=$?" >> "$O/pkg.txt") && rg -q '^exit=0$' "$O/pkg.txt" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-03\): Family \(c1-c2\)'</automated>
    <fails_when>non-zero exit; agentpicker.go dirty before the plant or after the revert; the planted run exiting 0 or lacking the FAIL line; the log missing a (c1)/(c2)/(c3) heading or either FAIL excerpt; the tui package not green; or no docs(07-03) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - both sections carry Pre-mutation gate, Mutation applied, Observed failure, Pre-revert gate, Revert, Green control
    - `git diff --quiet -- internal/cli/tui/` exits 0 at the end
  </acceptance_criteria>
  <done>The model-level guards are proven able to fail against the exact pre-fix code.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: ORCHESTRATOR — install tmux locally, show TTY-05 RED on the pre-fix delegate and GREEN at HEAD (Family c3)</name>
  <precondition>Tasks 1-2 are committed and `git diff --quiet -- internal/cli/tui/ test/tmux/` exits 0; Homebrew is available on this machine (`command -v brew`).</precondition>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - Taskfile.yml (`test:tmux`: TMUX_EXPECTED_TESTS 6; TMUX_EXPECTED_VERSION is asserted only when CI is set)
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md (the (c3) placeholder)
  </read_first>
  <action>The orchestrator installs tmux locally (an environment step no executor subagent performs), runs the real-PTY suite GREEN at HEAD, re-plants the pre-fix delegate newline (Family (c1)'s exact mutation) to show TTY-05 RED, reverts byte-clean, and records Family (c3) in 07-MUTATION-LOG.md; CI's `tmux-e2e` job (tmux 3.4) re-confirms at PR time.</action>
  <instructions>
    1. `brew install tmux` (or the maintainer's dotfiles tooling); record `tmux -V` (3.7c expected) in (c3).
    2. GREEN at HEAD: `GOTOOLCHAIN=go1.26.6 task test:tmux` with CI unset → record the `test:tmux: executed=6 skipped=0 expected=6` line and exit 0.
    3. RED: apply Family (c1)'s mutation to agentpicker.go (behind `git diff --quiet` gates), run `GOTOOLCHAIN=go1.26.6 task test:tmux` again → record the verbatim `--- FAIL: TestInstallPickerCancelWritesNoConfig` excerpt showing `space: toggle` absent from the capture, and the non-zero exit. Revert with `git checkout -- internal/cli/tui/agentpicker.go` and show `git diff --quiet` exits 0.
    4. Replace the (c3) placeholder line with: tmux version, both runs' commands and excerpts, the gates, and the line `Family (c3) verdict: RED on the pre-fix delegate, GREEN at HEAD (executed=6)`. Commit `docs(07-03): Family (c3) tmux RED/GREEN`.
    If step 2 is not GREEN, stop and escalate to the maintainer (record `Maintainer decision: …` in (c3)); do not patch the footer budget.
  </instructions>
  <verification>(c3) holds the tmux version, a GREEN executed=6 excerpt, a RED excerpt naming TestInstallPickerCancelWritesNoConfig, and the revert proof.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && ! rg -q 'Orchestrator step — pending Task 3' "$M" && rg -q -- '--- FAIL: TestInstallPickerCancelWritesNoConfig' "$M" && rg -q 'executed=6 skipped=0 expected=6' "$M" && rg -q '^Family \(c3\) verdict: RED on the pre-fix delegate, GREEN at HEAD \(executed=6\)$' "$M" && rg -q 'tmux 3\.' "$M" && git diff --quiet -- internal/cli/tui/ test/tmux/ && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-03\): Family \(c3\)'</automated>
    <fails_when>non-zero exit; the (c3) placeholder still present; no TTY-05 FAIL excerpt, no executed=6 GREEN line, no verdict line or no tmux version in the log; tui/tmux files dirty; or no docs(07-03) Family (c3) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - the RED run is on the byte-exact pre-fix delegate (Family (c1)'s mutation), and the revert returns agentpicker.go byte-clean
    - the GREEN run reports executed=6 (the anti-vacuity equality, not a floor)
  </acceptance_criteria>
  <resume-signal>Reply "c3 recorded: tmux <version>, RED on pre-fix, GREEN executed=6" once the section and commit exist.</resume-signal>
  <done>The real-PTY footer assertion is shown RED on the pre-fix code and GREEN after the fix, on local tmux.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interactive picker → which agents' configs are written | a hidden footer hides the cancel keys; a user can confirm a selection without seeing how to back out |
| orchestrator → the maintainer's machine | a package install (tmux) outside the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Denial of Service | picker footer overflow | low | mitigate | one line per row; model-level and real-PTY guards; Family (c) |
| T-07-SC | Tampering | tmux package install | low | accept | Homebrew core formula, the same tool CI installs from the Ubuntu archive; no npm/pip/cargo install in this phase |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/tui/ -count=1` green; tmux package compiles; Family (c1)-(c3) recorded
</verification>

<success_criteria>
- FIX-03 fixed at its cause and asserted by the model-level test and by TTY-05 under real tmux
- 07-VALIDATION row 07-FIX03 satisfiable
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-03-SUMMARY.md` when done — the RED transcript, the measured heights before/after, and the (c3) outcome.
</output>
