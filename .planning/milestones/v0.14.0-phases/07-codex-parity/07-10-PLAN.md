---
phase: 07-codex-parity
plan: 10
type: execute
wave: 10
depends_on: ["07-09"]
files_modified:
  - docs/AGENT-CAPABILITIES.md
  - internal/agents/capability_doc_test.go
  - README.md
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
autonomous: true
requirements: [AGENT-14]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`docs/AGENT-CAPABILITIES.md` publishes one row per registered target per scope (8 x 2 = 16) with the code-derived columns scope, MCP config, format, instructions, skill, hooks and nudge, plus a hand-kept verification column, and is linked from the README's \"Use it from an agent\" section (D-27, AGENT-14)."
    - "`TestCapabilityDoc_MirrorsCapabilities` computes every code-derived cell from `Capabilities()` (paths under the fake HOME rendered as `~/…`, local paths relative) and fails on any differing cell, a missing, extra or reordered row, or a row count other than 16; the nudge column is derived in the test from `Hooks` — claude-json → `SessionStart + opt-in PreToolUse`, codex-json → `opt-in PreToolUse`, none → `none` — with no new `Capabilities` or `--print-config-style` field (D-27, D-28)."
    - "`TestCapabilityDoc_VerificationColumn` requires every supported row's cell to read `verified <YYYY-MM-DD> (<evidence file>)` or `[ASSUMED] (<source>, fetched <YYYY-MM-DD>)`, every unsupported row's cell to read `n/a`, Cursor, Gemini CLI and Kiro to be `[ASSUMED]`, and both Codex rows to be `verified … (07-LIVE-SESSIONS.md)` (D-27)."
    - "The doc states that Codex loads project config (and, per the recorded verdicts, the skill and AGENTS.md) only for a trusted project, that Codex skips a new or changed hook until it is trusted in `/hooks`, that the nudge is opt-in via `--pretool-nudge`, the subagent cooldown granularity 07-09 recorded (A4), the shared paths, and never advises bypassing hook trust (CODEX-05 docs statement, D-10, D-19)."
    - "AGENT-14 adjacency (explicit): two targets sharing one path (Cursor/opencode/Codex on `.agents/skills/codegraph`; Codex/opencode on repo-root `AGENTS.md`) appear as separate rows each naming the path; no row is merged."
    - "AGENT-14 empty (explicit): a declared-none cell renders `none`; an unsupported scope renders `not supported` in every code-derived cell and `n/a` in the verification cell — never a blank cell."
    - "AGENT-14 ordering (explicit): rows follow `AllTargets()` order with global before local; the drift test fails on a reordered row."
    - "Family (h) in 07-MUTATION-LOG.md: a planted doc drift (codex local hooks `codex-json` → `none`) and a planted overclaim (Cursor marked verified) each turn the drift guard RED, reverted byte-clean (D-27)."
  artifacts:
    - path: "docs/AGENT-CAPABILITIES.md"
      provides: "the published per-harness capability table"
      contains: "| `codex` | local |"
    - path: "internal/agents/capability_doc_test.go"
      provides: "TestCapabilityDoc_MirrorsCapabilities, TestCapabilityDoc_VerificationColumn"
      contains: "TestCapabilityDoc_MirrorsCapabilities"
  key_links:
    - from: "docs/AGENT-CAPABILITIES.md table rows"
      to: "every target's Capabilities() literal"
      via: "internal/agents/capability_doc_test.go drift test"
      pattern: "Capabilities\\(\\)"
    - from: "README.md \"Use it from an agent\""
      to: "docs/AGENT-CAPABILITIES.md"
      via: "a relative link"
      pattern: "docs/AGENT-CAPABILITIES\\.md"
  prohibitions:
    - statement: "MUST NOT mark a harness `verified` without a live evidence file for it; Cursor, Gemini CLI and Kiro stay `[ASSUMED]` with their source and fetch date (D-27, D-10 of Phase 5)."
      status: resolved
      verification: test
---

<objective>
Close the milestone's docs with an honest, drift-tested per-harness capability table (D-27/D-28): what each of
the 8 targets receives at each scope — MCP config, instructions, skill, hooks, nudge — derived from the one
capability table and kept honest by a Go test that fails when code and doc diverge, with a hand-kept
verification column that says `verified` only where a live session proved it.

Purpose: AGENT-14 (the table and its honesty guard); 07-VALIDATION row `07-DOCS` (table half). The MCP
`instructions` sentence and the "4 of 8" comments are 07-11.

Output: `docs/AGENT-CAPABILITIES.md`, `internal/agents/capability_doc_test.go`, the README link, Family (h).

Notes: precondition — 07-LIVE-SESSIONS.md reads `CODEX-06 verdict: PASS` and `CODEX-05 live verdict: PASS`
(or a recorded `Maintainer decision:` saying how to document a FAIL). The test mirrors
`TestMatrix_DocMirrorsDescriptor` (internal/indexer/capability/matrix_test.go:108-165) and its `repoRoot`
helper, adjusted for `internal/agents` (two levels up). Table-row format is the planner's choice (D-27 discretion):
a row is a line starting with ``| `<id>` |``, split on `|` into exactly 9 trimmed cells. `GOTOOLCHAIN=go1.26.6`;
commits end with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never `[ci skip]`. The test builds the
trust-bypass flag name by concatenation when it checks the doc for it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md
@.planning/phases/07-codex-parity/07-09-SUMMARY.md
@.planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md
@docs/LANGUAGE-CAPABILITY-MATRIX.md
@internal/indexer/capability/matrix_test.go
@internal/agents/capabilities.go
@internal/cli/testdata/plain/print-config-style.golden
@internal/cli/testdata/plain/print-config-style-local.golden
@README.md
</context>

<interfaces>
- `AllTargets()` (TargetID-sorted: antigravity, claude, codex, cursor, gemini, hermes, kiro, opencode);
  `Capabilities{Scopes, ConfigFormat, Hooks, MCPConfig, Instructions, SkillDirs}`; `Supports(loc)`,
  `InstructionsPath(loc)`, `WrittenSkillDir(loc)`; `fakeHome(t)` sets HOME, XDG_CONFIG_HOME=<home>/.config,
  HERMES_HOME=<home>/.hermes.
- The current capability lines (after 07-07) are in the two print-config-style goldens (HOME shown as `<HOME>`).
- Row shape: `| `<id>` | <global|local> | <mcp> | <format> | <instructions> | <skill> | <hooks> | <nudge> | <verification> |`
  with path cells backticked (`` `~/.codex/config.toml` ``, `` `.codex/config.toml` ``), `none` for a declared-none
  cell, `not supported` in every code-derived cell of an unsupported scope, and `n/a` as its verification.
- Known evidence and sources: Codex — 07-LIVE-SESSIONS.md (CODEX-01, CODEX-05/06); Claude local — 06-LIVE-SESSIONS.md
  (2026-09-19); opencode local and Antigravity global — 05-LIVE-SESSIONS.md (2026-09-18); Cursor
  `cursor.com/docs/skills.md`, opencode global `opencode.ai/docs/skills.md`, Gemini CLI
  `raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/skills.md`, Kiro `kiro.dev/docs/skills/`
  (all fetched 2026-09-18); Hermes `hermes-agent.ai/blog/hermes-mcp-integration-guide` (re-fetch; cite the date).
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan adds `docs/AGENT-CAPABILITIES.md`,
`internal/agents/capability_doc_test.go`, the README link and Family (h).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A failing drift test first, then the published capability table and the README link</name>
  <precondition>07-LIVE-SESSIONS.md contains `CODEX-06 verdict: PASS` and `CODEX-05 live verdict: PASS`, or a `Maintainer decision:` line directing how a FAIL is documented.</precondition>
  <files>internal/agents/capability_doc_test.go, docs/AGENT-CAPABILITIES.md, README.md</files>
  <read_first>
    - internal/indexer/capability/matrix_test.go (repoRoot at :17-28, mdTableRowPattern and TestMatrix_DocMirrorsDescriptor at :103-165)
    - docs/LANGUAGE-CAPABILITY-MATRIX.md (the framing paragraph to mirror)
    - internal/agents/capabilities.go and every target file's Capabilities() literal
    - internal/cli/testdata/plain/print-config-style.golden and print-config-style-local.golden (the current values)
    - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md (verdicts, dates, codex-cli version, D-16/A2/A4/D-17 findings)
    - .planning/phases/05-agent-reach-capability-model-skill-in-every-harness/05-LIVE-SESSIONS.md (Summary table: verified vs [ASSUMED] and sources)
    - README.md ("## Use it from an agent")
  </read_first>
  <behavior>
    - TestCapabilityDoc_MirrorsCapabilities: fakeHome + t.Chdir(t.TempDir()); reads docs/AGENT-CAPABILITIES.md from the repo root; collects the rows; asserts exactly 16 rows in AllTargets() x [global, local] order; for each row compares the 7 code-derived cells (scope, mcp, format, instructions, skill, hooks, nudge) against values computed from Capabilities() with the rendering rules above; reports every mismatching cell with target, scope and column.
    - TestCapabilityDoc_VerificationColumn: the form rules and the Cursor/Gemini/Kiro/Codex rules above; the doc mentions `/hooks` and `--pretool-nudge` and contains no trust-bypass flag (name built by concatenation).
  </behavior>
  <action>
    RED commit `test(07-10): add the failing capability-doc drift test` — the doc does not exist yet, so the test fails on its read (a test failure, not a build error).
    GREEN commit `docs(07-10): publish the per-harness capability table`:
    - `docs/AGENT-CAPABILITIES.md`: title, a framing paragraph in the LANGUAGE-CAPABILITY-MATRIX style (human half here, machine half the `Capabilities()` literals in `internal/agents/*.go`, drift proven by `internal/agents/capability_doc_test.go`); a legend (`~` = your home directory; relative paths are relative to the repository root for a project-local install; `none`; `not supported`; the nudge column's derivation; the verification column's three forms); the 16-row table; then Notes: the shared paths (one skill package and one manifest listing every agent that installed it; repo-root AGENTS.md shared by Codex and opencode, kept until the last of them is uninstalled); Codex project config (and, per the recorded D-16/A2 verdicts, the skill and AGENTS.md) load only in a trusted project, and codegraph never writes the trust entry; the Codex nudge is opt-in via `--pretool-nudge`, is skipped when `[features] hooks = false`, and Codex skips a new or changed hook until it is trusted in `/hooks`; the subagent cooldown granularity exactly as 07-09 recorded A4; codegraph resolves Codex's global files under `~/.codex` (a non-default `CODEX_HOME` is not honoured); Codex truncates the skill description in its list (D-17 advisory, no SKILL.md change); Hermes receives only the MCP entry; Cursor, Gemini CLI and Kiro are `[ASSUMED]` because no live session could run.
    - Verification cells: the most specific live evidence file that exercised that target at that scope (`verified <date> (<file name>)`); otherwise `[ASSUMED] (<source URL>, fetched <date>)`; `n/a` for unsupported rows. Search `.planning/phases/0[5-7]-*/0[5-7]-LIVE-SESSIONS.md` and `.planning/milestones/` live-session files before choosing `[ASSUMED]` for Claude's global row.
    - README.md: under "## Use it from an agent", one sentence linking `docs/AGENT-CAPABILITIES.md` for what each agent receives at each scope.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-10\): ' && printf '%s\n' "$S" | rg -q '^docs\(07-10\): publish' && test -z "$(gofmt -l internal/agents)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/mcp/ -count=1 -run 'TestCapabilityDoc_MirrorsCapabilities$|TestCapabilityDoc_VerificationColumn$|TestREADMEDocumentsToolVisibilityGate$' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -3 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c '^--- PASS: (TestCapabilityDoc_MirrorsCapabilities|TestCapabilityDoc_VerificationColumn|TestREADMEDocumentsToolVisibilityGate) ' "$O/t1.txt")" = "3" && test "$(rg -c '^\| `(antigravity|claude|codex|cursor|gemini|hermes|kiro|opencode)` \| (global|local) \|' docs/AGENT-CAPABILITIES.md)" = "16" && rg -q '^\| `codex` \| local \| `\.codex/config\.toml` \| toml \| `AGENTS\.md` \| `\.agents/skills/codegraph` \| codex-json \| opt-in PreToolUse \| verified [0-9]{4}-[0-9]{2}-[0-9]{2} \(07-LIVE-SESSIONS\.md\) \|$' docs/AGENT-CAPABILITIES.md && test "$(rg -c '^\| `(cursor|gemini|kiro)` \| (global|local) \|.*\| \[ASSUMED\] \(' docs/AGENT-CAPABILITIES.md)" = "6" && R=$(awk '/^## Use it from an agent/,/^## Commands/' README.md) && printf '%s\n' "$R" | rg -q 'docs/AGENT-CAPABILITIES\.md'</automated>
    <fails_when>non-zero exit; a missing test(07-10) or docs(07-10) publish subject; gofmt/vet failure; any FAIL line or fewer than 3 named PASS lines; not exactly 16 table rows; the codex local row not the exact expected cells with a verified 07-LIVE-SESSIONS.md cell; fewer than 6 [ASSUMED] Cursor/Gemini/Kiro rows; or the README agent section not linking the doc</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY pastes the RED `--- FAIL: TestCapabilityDoc_MirrorsCapabilities` line from the test commit
    - every `verified` cell names an evidence file that exists under `.planning/`
    - the doc says nothing the recorded live verdicts contradict (the A4 sentence matches the recorded A4 line)
  </acceptance_criteria>
  <done>The per-harness capability table is published, linked from the README, and cannot drift from the code or overclaim verification.</done>
</task>

<task type="auto">
  <name>Task 2: Family (h) — prove the capability-doc guard can fail</name>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
    - docs/AGENT-CAPABILITIES.md
  </read_first>
  <action>
    Append in the established shape, each reverted with `git checkout -- docs/AGENT-CAPABILITIES.md` behind the gates, with a green control:
    - `## Family (h1)` — D-27's planted doc drift: `perl -pi -e 's/^(\| `codex` \| local \|.*\| )codex-json( \|)/${1}none$2/' docs/AGENT-CAPABILITIES.md` → `TestCapabilityDoc_MirrorsCapabilities` RED naming codex/local/hooks.
    - `## Family (h2)` — a planted overclaim: Cursor's global verification cell changed to `verified 2026-09-18 (05-LIVE-SESSIONS.md)` → `TestCapabilityDoc_VerificationColumn` RED.
    Commit `docs(07-10): Family (h) mutation log`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && F=docs/AGENT-CAPABILITIES.md && git diff --quiet -- "$F" && test "$(rg -c '^\| `codex` \| local \|.*\| codex-json \|' "$F")" = "1" && perl -pi -e 's/^(\| `codex` \| local \|.*\| )codex-json( \|)/${1}none$2/' "$F" && ! git diff --quiet -- "$F" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestCapabilityDoc_MirrorsCapabilities$' -v > "$O/red-h1.txt" 2>&1; echo "exit=$?" >> "$O/red-h1.txt"); git checkout -- "$F"; git diff --quiet -- "$F" && rg -q -- '--- FAIL: TestCapabilityDoc_MirrorsCapabilities' "$O/red-h1.txt" && ! rg -q '^exit=0$' "$O/red-h1.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(h[12]\)' "$M")" = "2" && rg -q -- '--- FAIL: TestCapabilityDoc_MirrorsCapabilities' "$M" && rg -q -- '--- FAIL: TestCapabilityDoc_VerificationColumn' "$M" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 > "$O/pkg.txt" 2>&1; echo "exit=$?" >> "$O/pkg.txt") && rg -q '^exit=0$' "$O/pkg.txt" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-10\): Family \(h\)'</automated>
    <fails_when>non-zero exit; the doc dirty before the plant or after the revert; the codex local row's hooks cell not present exactly once; the planted run exiting 0 or lacking the FAIL line; fewer than 2 Family (h) sections or either FAIL excerpt missing; the agents package not green; or no docs(07-10) Family commit</fails_when>
  </verify>
  <acceptance_criteria>
    - both sections carry the six convention headings and a byte-clean revert
  </acceptance_criteria>
  <done>The capability doc's drift and honesty guards are proven able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs → users' trust decisions | an overclaimed "verified" or an unsafe instruction misleads users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-31 | Repudiation | capability table overclaiming verification | medium | mitigate | verification-column form and [ASSUMED] rules enforced by test; Family (h2) |
| T-07-32 | Tampering | doc drifting from the shipped table | medium | mitigate | code-derived cells compared to Capabilities() for 8x2; Family (h1) |
| T-07-33 | Elevation of Privilege | doc advising a hook-trust bypass | low | mitigate | the verification test rejects the bypass flag name in the doc |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/mcp/ -count=1` green; 16 rows; README links the doc
</verification>

<success_criteria>
- AGENT-14's table published, drift-tested and honest about [ASSUMED] rows; 07-VALIDATION row 07-DOCS (table half) satisfiable with `go test ./internal/agents/ -run 'TestCapabilityDoc' -count=1`
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-10-SUMMARY.md` when done — the RED transcript and the verification-column sources chosen per row.
</output>
