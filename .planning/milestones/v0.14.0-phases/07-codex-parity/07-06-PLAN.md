---
phase: 07-codex-parity
plan: 06
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - internal/agents/shared.go
  - internal/agents/codex.go
  - internal/agents/opencode.go
  - internal/agents/codex_test.go
  - internal/agents/shared_test.go
  - internal/agents/ownership_test.go
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
autonomous: true
requirements: [CODEX-04]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Uninstalling Codex or opencode at a location removes the shared marker block from their common instructions file only when no OTHER registered target declaring the same instructions path at that location still reports `Detect(loc).AlreadyConfigured`; otherwise the file is reported `kept`, left byte-identical, and a Note names the target(s) still using it (D-11)."
    - "Every uninstall order — codex then opencode, opencode then codex, and `--target all` — leaves repo-root `AGENTS.md` byte-identical to its pre-install bytes once the last of them is gone, whether AGENTS.md pre-existed with foreign content or did not exist (then it is removed) (D-11, CODEX-04)."
    - "Project-local `install --target codex` writes the marker-fenced block into repo-root `AGENTS.md` and global `~/.codex/AGENTS.md` behaviour is unchanged (CODEX-04)."
    - "When an `AGENTS.override.md` sits beside the instructions file Codex reads (repo root locally; `~/.codex` globally), a Codex install adds a Note that the override shadows `AGENTS.md` for Codex; the block is still written to `AGENTS.md` and the override file is never created, written or modified (D-12)."
    - "The ownership table covers the shared file: `TestOwnershipSharedInstructions` plants foreign content in AGENTS.md, a foreign MCP entry in each config and a foreign sibling skill, runs install for both sharers and every uninstall order, and finds every foreign byte identical and every own entry gone (D-11, the D-13 guard)."
    - statement: "CODEX-04 concurrency: an interrupted or concurrent codex/opencode install or uninstall never leaves AGENTS.md truncated — every write is atomicWriteFile (temp file + rename); two concurrent codegraph processes may interleave so the last writer wins, and the next install restores the block."
      verification: backstop
    - "Family (e) in 07-MUTATION-LOG.md: a helper that never reports another requester, a missing gate in opencode's Uninstall, and a dropped override Note each turn a named guard RED, reverted byte-clean."
  artifacts:
    - path: "internal/agents/shared.go"
      provides: "instructionsRequestedElsewhere(path string, loc Location, self TargetID) []TargetID"
      contains: "func instructionsRequestedElsewhere("
    - path: "internal/agents/shared_test.go"
      provides: "TestSharedAgentsMD_UninstallOrders, TestSharedAgentsMD_KeptWhileOtherConfigured"
      contains: "TestSharedAgentsMD_UninstallOrders"
    - path: "internal/agents/ownership_test.go"
      provides: "TestOwnershipSharedInstructions"
      contains: "TestOwnershipSharedInstructions"
  key_links:
    - from: "internal/agents/codex.go Uninstall and internal/agents/opencode.go Uninstall"
      to: "internal/agents/shared.go instructionsRequestedElsewhere"
      via: "gate called before removeMarkedSection"
      pattern: "instructionsRequestedElsewhere\\("
    - from: "instructionsRequestedElsewhere"
      to: "registry AllTargets() + Capabilities().InstructionsPath(loc) + Detect(loc).AlreadyConfigured"
      via: "derived requester set, never a stored index (Don't Hand-Roll)"
      pattern: "AllTargets\\(\\)"
  prohibitions:
    - statement: "MUST NOT remove the codegraph block from a shared instructions file while another configured agent still relies on it (D-11)."
      status: resolved
      verification: test
    - statement: "MUST NOT create, write or modify a user's `AGENTS.override.md` (D-12)."
      status: resolved
      verification: test
---

<objective>
Build the shared-instructions rule 05-07 planned but never built (D-11): repo-root `AGENTS.md` is written by both
Codex and opencode at local scope, so an uninstall removes the codegraph block only when no other configured
target still declares that file there. Add D-12's `AGENTS.override.md` Note. Every uninstall order must restore
`AGENTS.md` byte for byte once the last sharer is gone, and the ownership guard must cover it.

Purpose: CODEX-04 ("uninstall removes it leaving the rest of the file byte-identical") and the D-11/D-12
decisions; 07-VALIDATION row `07-SCOPE` (`TestSharedInstructions` family, named here `TestSharedAgentsMD_*` and
`TestOwnershipSharedInstructions`).

Output: `test(07-06):`/`feat(07-06):` commits, Family (e).

Notes: `GOTOOLCHAIN=go1.26.6`; commits end with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never
`[ci skip]`; never gate on `gsd-tools check tdd-red-evidence`. The helper derives the requester set from the
registry on every call (no stored index — 07-RESEARCH Don't Hand-Roll).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-RESEARCH.md
@.planning/phases/07-codex-parity/07-PATTERNS.md
@.planning/phases/07-codex-parity/07-05-SUMMARY.md
@internal/agents/shared.go
@internal/agents/codex.go
@internal/agents/opencode.go
@internal/agents/capabilities.go
@internal/agents/registry.go
@internal/agents/ownership_test.go
</context>

<interfaces>
- New (shared.go): `func instructionsRequestedElsewhere(path string, loc Location, self TargetID) []TargetID` —
  iterates `AllTargets()` (TargetID order), skips `self` and targets whose `Capabilities().Supports(loc)` is
  false, resolves `Capabilities().InstructionsPath(loc)` (skip on error or ""), compares `filepath.Abs` of both
  paths after `filepath.Clean`, and returns the ids whose `Detect(loc).AlreadyConfigured` is true.
- Existing: `removeMarkedSection(path, start, end) (FileAction, error)`, `recordAction(&result, path, action, err)`,
  `ActionKept`, `WriteResult.Notes`, `upsertInstructionsEntry`.
- Local sharers today: codex and opencode both declare `AGENTS.md` at local; at global their paths differ
  (`~/.codex/AGENTS.md` vs `~/.config/opencode/AGENTS.md`), so the helper returns nothing there.
- Codex reads `AGENTS.override.md` instead of `AGENTS.md` in the same directory (07-CONTEXT Current Codex reference).
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan adds `instructionsRequestedElsewhere`, the
Uninstall gates in codex.go/opencode.go, the D-12 override Note, the listed tests and Family (e).

<assumption_delta_decision>
Pluralization (a second writer of one file): the primary noun becomes "the marker block in a (path, location)
instructions file, owned while any registered target declaring that path still reports AlreadyConfigured".
Decision: `promote` — ownership moves from "the uninstalling target" to the derived requester set; nothing is
added alongside (no manifest for instructions files). Invariant test accepted: every uninstall order restores
AGENTS.md byte-identical (`TestSharedAgentsMD_UninstallOrders`).
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "uninstalling one sharer keeps AGENTS.md; the last one restores it byte for byte" — through the real binary</name>
  <files>internal/agents/shared.go, internal/agents/codex.go, internal/agents/opencode.go, internal/agents/shared_test.go, internal/agents/codex_test.go</files>
  <read_first>
    - internal/agents/shared.go (removeMarkedSection :695-739, upsertInstructionsEntry :747)
    - internal/agents/codex.go (Uninstall after 07-05), internal/agents/opencode.go (Uninstall :325-345)
    - internal/agents/capabilities.go (declaredSkillFallback — the AllTargets/Capabilities iteration precedent)
    - internal/agents/registry.go (AllTargets)
  </read_first>
  <behavior>
    - TestSharedAgentsMD_UninstallOrders: subtests `<order>/<pre>` for order in {codex_then_opencode, opencode_then_codex, target_all} and pre in {preexisting, absent} (6 subtests). fakeHome + t.Chdir(tmp); `preexisting` writes AGENTS.md with a foreign two-paragraph section first; install codex and opencode at local; for the two-step orders the first Uninstall reports AGENTS.md as `kept` and the block is still present; after the last uninstall (for target_all: every registered target's Uninstall(local) in AllTargets order) AGENTS.md equals the pre-install bytes (preexisting) or does not exist (absent).
    - TestSharedAgentsMD_KeptWhileOtherConfigured: after installing both, `codexTarget{}.Uninstall(LocationLocal)` has an AGENTS.md FileResult `kept`, a Note naming opencode, and AGENTS.md is byte-identical to its pre-uninstall bytes; the codex config table is still removed.
    - TestSharedAgentsMD_GlobalPathsNotShared: at global, uninstalling codex removes its block from `~/.codex/AGENTS.md` even while opencode is configured globally (different paths).
    - TestCodex_Install_OverrideNote: local — `AGENTS.override.md` planted in cwd → exactly one Note naming `AGENTS.override.md`; the override file byte-identical after install and uninstall; the block written to AGENTS.md. Global — the same with `~/.codex/AGENTS.override.md`. Without an override, no such Note.
  </behavior>
  <action>
    RED commit `test(07-06): expect the shared AGENTS.md block to survive while another agent uses it`.
    GREEN commit `feat(07-06): keep a shared instructions block while another agent still uses it`:
    - Add `instructionsRequestedElsewhere` (interfaces block) to shared.go with a doc comment citing D-11 and the derived-not-stored rule.
    - In codexTarget.Uninstall and opencodeTarget.Uninstall, resolve the instructions path, call the helper first; when it returns ids, record `ActionKept` for the file (no removal) and append a Note such as "<path> keeps its codegraph block: still used by <display names>"; otherwise call `removeMarkedSection` as today.
    - In codexTarget.Install, after the instructions step, add the D-12 Note when `AGENTS.override.md` exists in the instructions file's directory (wording: the override shadows AGENTS.md for Codex, so Codex will not see the codegraph block there until the override includes it). Never touch the override file.
    Then run the real-binary check in the verify below.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-06\): ' && printf '%s\n' "$S" | rg -q '^feat\(07-06\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestSharedAgentsMD_|TestCodex_Install_OverrideNote$|TestOpencode_|TestCodex_' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -3 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c -- '--- PASS: TestSharedAgentsMD_UninstallOrders/(codex_then_opencode|opencode_then_codex|target_all)/(preexisting|absent) ' "$O/t1.txt")" = "6" && test "$(rg -c '^--- PASS: (TestSharedAgentsMD_KeptWhileOtherConfigured|TestSharedAgentsMD_GlobalPathsNotShared|TestCodex_Install_OverrideNote) ' "$O/t1.txt")" = "3" && GOTOOLCHAIN=go1.26.6 go build -o "$O/codegraph" ./cmd/codegraph && H="$O/home" && P="$O/proj" && mkdir -p "$H" "$P" && git -C "$P" init -q && printf '# Team notes\n\nKeep this paragraph exactly.\n' > "$P/AGENTS.md" && cp "$P/AGENTS.md" "$O/agents.orig" && E="HOME=$H XDG_CONFIG_HOME=$H/.config HERMES_HOME=$H/.hermes" && (cd "$P" && env $E "$O/codegraph" install --target codex,opencode --location local --yes > "$O/i1.txt") && rg -q -F '<!-- CODEGRAPH_START -->' "$P/AGENTS.md" && (cd "$P" && env $E "$O/codegraph" uninstall --target codex --location local --yes > "$O/u1.txt") && rg -q '^  kept: AGENTS\.md$' "$O/u1.txt" && rg -q -F '<!-- CODEGRAPH_START -->' "$P/AGENTS.md" && (cd "$P" && env $E "$O/codegraph" uninstall --target opencode --location local --yes > "$O/u2.txt") && cmp "$P/AGENTS.md" "$O/agents.orig" && (cd "$P" && env $E "$O/codegraph" install --target codex,opencode --location local --yes > "$O/i2.txt") && (cd "$P" && env $E "$O/codegraph" uninstall --target all --location local --yes > "$O/u3.txt") && cmp "$P/AGENTS.md" "$O/agents.orig" && printf 'override\n' > "$P/AGENTS.override.md" && cp "$P/AGENTS.override.md" "$O/override.orig" && (cd "$P" && env $E "$O/codegraph" install --target codex --location local --yes > "$O/i3.txt") && rg -q 'note: .*AGENTS\.override\.md' "$O/i3.txt" && cmp "$P/AGENTS.override.md" "$O/override.orig"</automated>
    <fails_when>non-zero exit; a missing test(07-06)/feat(07-06) subject; build/gofmt/vet failure; fewer than 6 uninstall-order subtests or 3 named PASS lines; any FAIL line; the real first uninstall not reporting `kept: AGENTS.md` or removing the block; AGENTS.md differing from its original after the last sharer's uninstall or after `--target all`; the override Note absent; or AGENTS.override.md modified</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY pastes the RED `--- FAIL: TestSharedAgentsMD_KeptWhileOtherConfigured` line
    - `rg -c 'instructionsRequestedElsewhere\(' internal/agents/codex.go internal/agents/opencode.go` shows 1 in each file
    - no code path writes `AGENTS.override.md` (`rg -n 'AGENTS\.override\.md' internal/agents/*.go` hits only a path join and Note text, never a write call argument)
  </acceptance_criteria>
  <done>The shared AGENTS.md block is kept while another agent uses it and restored byte-for-byte by every uninstall order, proven through the real binary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The ownership guard covers the shared file</name>
  <files>internal/agents/ownership_test.go</files>
  <read_first>
    - internal/agents/ownership_test.go (plantForeignFixtures, assertForeignBytesUnchangedAfterUninstall, assertOwnEntriesGoneAfterUninstall, the 242ec0a doc comment)
  </read_first>
  <behavior>
    - TestOwnershipSharedInstructions: subtests for each of the three orders x {clean, foreign-codegraph-dir}: plant (via the existing helpers) a foreign MCP entry in `.codex/config.toml` and in `opencode.jsonc`, the foreign marker-less section in `AGENTS.md`, the foreign sibling skill and, for the foreign variant, a manifest-less `.agents/skills/codegraph/SKILL.md`; install codex and opencode at local; uninstall in the order; assert every planted byte identical, no codegraph table, no opencode entry, no marker block, no manifest; assert the executed leaf count is 6.
  </behavior>
  <action>
    Commit `test(07-06): cover the shared AGENTS.md in the ownership table` adding the test (it passes against Task 1's code — it is a guard, positive-controlled by Family (e2) in Task 3; say so in its doc comment and cite 242ec0a like the existing table). Reuse the existing planting/assertion helpers; do not change the 32-leaf `TestOwnershipExactIdentity` count.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && test "$(printf '%s\n' "$S" | rg -c '^test\(07-06\): ')" -ge 2 && test -z "$(gofmt -l internal/agents)" && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestOwnershipSharedInstructions$|TestOwnershipExactIdentity$' -v > "$O/t2.txt" 2>&1; echo "exit=$?" >> "$O/t2.txt") && tail -3 "$O/t2.txt" && rg -q '^exit=0$' "$O/t2.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t2.txt" && test "$(rg -c -- '--- PASS: TestOwnershipSharedInstructions/(codex_then_opencode|opencode_then_codex|target_all)/(clean|foreign-codegraph-dir) ' "$O/t2.txt")" = "6" && test "$(rg -c -- '--- PASS: TestOwnershipExactIdentity/[a-z]+/(global|local)/(clean|foreign-codegraph-dir) ' "$O/t2.txt")" = "32" && rg -q '242ec0a' internal/agents/ownership_test.go</automated>
    <fails_when>non-zero exit; fewer than two test(07-06) subjects; gofmt failure; any FAIL line; fewer than 6 shared-instructions leaves or 32 exact-identity leaves; or the 242ec0a citation gone</fails_when>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agents/ -count=1` is `ok`
  </acceptance_criteria>
  <done>The D-13 ownership guard now covers the shared AGENTS.md across every uninstall order.</done>
</task>

<task type="auto">
  <name>Task 3: Family (e) — prove the shared-instructions guards can fail</name>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
    - internal/agents/shared.go, internal/agents/opencode.go, internal/agents/codex.go (final code)
  </read_first>
  <action>
    Append, in the established shape: `## Family (e1)` — an early `return nil` after the `instructionsRequestedElsewhere` signature line → `TestSharedAgentsMD_KeptWhileOtherConfigured` RED; `## Family (e2)` — remove opencode.go's helper call so its Uninstall always removes → `TestSharedAgentsMD_UninstallOrders/opencode_then_codex` and `TestOwnershipSharedInstructions/opencode_then_codex` RED; `## Family (e3)` — drop the override Note → `TestCodex_Install_OverrideNote` RED. Revert each byte-clean with a green control. Commit `docs(07-06): Family (e) mutation log`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && git diff --quiet -- internal/agents/shared.go && test "$(rg -c '^func instructionsRequestedElsewhere\(path string, loc Location, self TargetID\) \[\]TargetID \{$' internal/agents/shared.go)" = "1" && perl -0pi -e 's/(func instructionsRequestedElsewhere\(path string, loc Location, self TargetID\) \[\]TargetID \{\n)/$1\treturn nil\n/' internal/agents/shared.go && ! git diff --quiet -- internal/agents/shared.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestSharedAgentsMD_KeptWhileOtherConfigured$' -v > "$O/red-e1.txt" 2>&1; echo "exit=$?" >> "$O/red-e1.txt"); git checkout -- internal/agents/shared.go; git diff --quiet -- internal/agents/shared.go && rg -q -- '--- FAIL: TestSharedAgentsMD_KeptWhileOtherConfigured' "$O/red-e1.txt" && ! rg -q '^exit=0$' "$O/red-e1.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(e[1-3]\)' "$M")" = "3" && rg -q -- '--- FAIL: TestSharedAgentsMD_KeptWhileOtherConfigured' "$M" && rg -q -- '--- FAIL: TestSharedAgentsMD_UninstallOrders/opencode_then_codex|--- FAIL: TestOwnershipSharedInstructions/opencode_then_codex' "$M" && rg -q -- '--- FAIL: TestCodex_Install_OverrideNote' "$M" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 > "$O/pkg.txt" 2>&1; echo "exit=$?" >> "$O/pkg.txt") && rg -q '^exit=0$' "$O/pkg.txt" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-06\): Family \(e\)'</automated>
    <fails_when>non-zero exit; shared.go dirty before the plant or after the revert; the pinned helper signature absent; the planted run exiting 0 or lacking the FAIL line; fewer than 3 Family (e) sections or any FAIL excerpt missing; the agents package not green; or no docs(07-06) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - each Family (e) section carries the six convention headings and a byte-clean revert
  </acceptance_criteria>
  <done>The shared-instructions guards are proven able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| one agent's uninstall → a file another agent still reads | removing a shared block silently degrades the other agent |
| codegraph → a user's `AGENTS.override.md` | a user-authored file codegraph must never write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-15 | Tampering | shared repo-root AGENTS.md | medium | mitigate | instructionsRequestedElsewhere gate in both Uninstalls; every-order byte-identity tests; ownership table; Family (e1)/(e2) |
| T-07-16 | Tampering | AGENTS.override.md | low | mitigate | read-only existence check for the Note; tests assert the file byte-identical (Family e3 covers the Note) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1` green; real-binary sequences restore AGENTS.md byte-for-byte
</verification>

<success_criteria>
- CODEX-04 delivered: local block written, global unchanged, uninstall restores the rest of the file byte-identical in every order
- D-11 and D-12 implemented and positive-controlled
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-06-SUMMARY.md` when done — RED transcripts and the real-binary sequence output.
</output>
