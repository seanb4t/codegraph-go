---
phase: 07-codex-parity
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - internal/cli/install.go
  - internal/cli/uninstall.go
  - internal/cli/install_test.go
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
  - .planning/todos/pending/2026-09-18-install-yes-discards-explicit-target.md
  - .planning/todos/completed/2026-09-18-install-yes-discards-explicit-target.md
autonomous: true
requirements: [CODEX-02]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`codegraph install --target codex --yes` configures exactly Codex: with a fresh HOME where `auto` would fall back to Claude Code, the report lists `Codex CLI:` and not `Claude Code:`, `~/.codex/config.toml` exists and `~/.claude.json` does not (D-13)."
    - "`codegraph uninstall --target codex --yes` reverses exactly Codex: after installing Claude Code and Codex, Claude's `~/.claude.json` still carries `mcpServers.codegraph` (D-13, uninstall checked too)."
    - "`--yes` without `--target` still short-circuits before the interactive picker and resolves to `auto` (install) / `all` (uninstall) — the Pitfall-6 regression tests stay green."
    - "Family (b) in 07-MUTATION-LOG.md: re-shadowing the explicit target behind `--yes` turns both new tests RED, reverted byte-clean."
    - "The todo `2026-09-18-install-yes-discards-explicit-target.md` is completed through the `todo complete` verb."
  artifacts:
    - path: "internal/cli/install_test.go"
      provides: "TestInstall_YesWithExplicitTarget_HonoursTarget, TestUninstall_YesWithExplicitTarget_HonoursTarget"
      contains: "TestInstall_YesWithExplicitTarget_HonoursTarget"
    - path: "internal/cli/install.go"
      provides: "target resolution switch with an explicit --target checked before --yes"
      contains: "case cmd.Flags().Changed(\"target\"):"
  key_links:
    - from: "07-04 / 07-09 live scaffolds"
      to: "internal/cli/install.go + uninstall.go target switch"
      via: "the live scripts run `install --target codex --yes`, which must configure only Codex"
      pattern: "Changed\\(\"target\"\\)"
  prohibitions:
    - statement: "MUST NOT let `--yes` silently widen an explicit `--target` to other agents' configuration files (writing or deleting config for agents the user did not name)."
      status: resolved
      verification: test
---

<objective>
Fix the `install --yes` discards `--target` todo at its cause, RED-first as the todo prescribes, for both
`install` and `uninstall` (D-13). It lands before the CODEX-01 live verification because every live script
in this phase runs `codegraph install --target codex --yes` and must configure Codex alone.

Purpose: CODEX-02's `--target` resolution correctness and 07-VALIDATION row `07-YES`. Independent of the
TOML fix; sequenced after it only because plans share one working tree.

Output: `test(07-02):` RED commit, `fix(07-02):` GREEN commit, Family (b), the completed todo.

TDD: RED commit first (tests compile and fail on assertions), then the fix; paste the `--- FAIL:` lines in
the SUMMARY; never gate on `gsd-tools check tdd-red-evidence`. `GOTOOLCHAIN=go1.26.6` on every Go command;
commits end with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never `[ci skip]`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-PATTERNS.md
@.planning/todos/pending/2026-09-18-install-yes-discards-explicit-target.md
@internal/cli/install.go
@internal/cli/uninstall.go
@internal/cli/install_test.go
</context>

<interfaces>
internal/cli/install.go (current, lines 110-126) and uninstall.go (lines 49-64): the target switch checks
`case yes:` before `case cmd.Flags().Changed("target"):`. After this plan the order is: explicit target,
then `yes`, then `interactiveAllowed(cmd)`, then the default. install.go contains exactly one
`case cmd.Flags().Changed("target"):` line (the print-config-style branch uses an `if`), which Family (b)
re-plants against.
Test helpers already in install_test.go: `fakeHome(t)`, `execCmd(args...)`, `withStubbedPicker(t, fn)`.
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan adds `TestInstall_YesWithExplicitTarget_HonoursTarget`,
`TestUninstall_YesWithExplicitTarget_HonoursTarget`, Family (b), and moves the D-13 todo to `completed/`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: An explicit --target wins over --yes in install and uninstall</name>
  <files>internal/cli/install.go, internal/cli/uninstall.go, internal/cli/install_test.go</files>
  <read_first>
    - internal/cli/install.go (the RunE target switch, lines 105-130)
    - internal/cli/uninstall.go (the identical switch, lines 49-64)
    - internal/cli/install_test.go (TestInstall_Yes_ShortCircuitsBeforeInteractiveBranch at :496, TestUninstall_Yes_ShortCircuitsBeforeInteractiveBranch at :551 — the idioms to reuse)
    - .planning/todos/pending/2026-09-18-install-yes-discards-explicit-target.md (root cause and prescribed fix)
  </read_first>
  <behavior>
    - TestInstall_YesWithExplicitTarget_HonoursTarget: fakeHome; runAgentPicker stubbed to fail the test if called; `execCmd("install", "--target", "codex", "-y", "--location", "global")` succeeds; stdout contains `Codex CLI:` and not `Claude Code:`; `<home>/.codex/config.toml` exists; `<home>/.claude.json` does not exist.
    - TestUninstall_YesWithExplicitTarget_HonoursTarget: fakeHome; install `--target claude,codex --location global`; runAgentPicker stubbed to fail; `execCmd("uninstall", "--target", "codex", "--yes", "--location", "global")` succeeds; stdout contains `Codex CLI:` and not `Claude Code:`; `<home>/.claude.json` still contains `mcpServers.codegraph` (decode and check); the codegraph table is gone from `<home>/.codex/config.toml`.
  </behavior>
  <action>
    RED commit `test(07-02): add failing --target with --yes resolution tests` adding the two tests above to install_test.go (uninstall's tests already live there). They fail on the current ordering (install resolves `auto` → Claude; uninstall resolves `all` → Claude removed too).
    GREEN commit `fix(07-02): honour an explicit --target under --yes`: in both install.go and uninstall.go move the `cmd.Flags().Changed("target")` case above `case yes:`. Rewrite the `case yes:` comment so it states the intent precisely: `--yes` still short-circuits BEFORE the interactive picker branch (Pitfall 6), and an explicit `--target` is honoured first. Change nothing else in either RunE. Then complete the todo with the verb: `gsd_run todo complete 2026-09-18-install-yes-discards-explicit-target.md` (the gsd-tools launcher), committed with the fix or as `docs(07-02): complete the --yes/--target todo`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-02\): ' && printf '%s\n' "$S" | rg -q '^fix\(07-02\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestInstall_YesWithExplicitTarget_HonoursTarget$|TestUninstall_YesWithExplicitTarget_HonoursTarget$|TestInstall_Yes_ShortCircuitsBeforeInteractiveBranch$|TestUninstall_Yes_ShortCircuitsBeforeInteractiveBranch$|TestInstall_InteractiveAllowed_CallsRunAgentPicker$' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -3 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c '^--- PASS: (TestInstall_YesWithExplicitTarget_HonoursTarget|TestUninstall_YesWithExplicitTarget_HonoursTarget|TestInstall_Yes_ShortCircuitsBeforeInteractiveBranch|TestUninstall_Yes_ShortCircuitsBeforeInteractiveBranch|TestInstall_InteractiveAllowed_CallsRunAgentPicker) ' "$O/t1.txt")" = "5" && GOTOOLCHAIN=go1.26.6 go build -o "$O/codegraph" ./cmd/codegraph && H="$O/home" && mkdir -p "$H" && HOME="$H" XDG_CONFIG_HOME="$H/.config" HERMES_HOME="$H/.hermes" "$O/codegraph" install --target codex --yes --location global > "$O/install.txt" && rg -q '^Codex CLI: ' "$O/install.txt" && ! rg -q '^Claude Code: ' "$O/install.txt" && test -f "$H/.codex/config.toml" && test ! -e "$H/.claude.json" && test -f .planning/todos/completed/2026-09-18-install-yes-discards-explicit-target.md && test ! -e .planning/todos/pending/2026-09-18-install-yes-discards-explicit-target.md</automated>
    <fails_when>non-zero exit; a missing test(07-02)/fix(07-02) subject; build/gofmt/vet failure; fewer than 5 named PASS lines or any FAIL line; the real binary's `--target codex --yes` report lacking `Codex CLI:`, listing `Claude Code:`, not writing ~/.codex/config.toml or writing ~/.claude.json; or the todo not moved to completed/</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -c -F 'case cmd.Flags().Changed("target"):' internal/cli/install.go` prints `1`, and in both files that line precedes `case yes:` (`rg -n` line numbers ascending)
    - the SUMMARY pastes `--- FAIL: TestInstall_YesWithExplicitTarget_HonoursTarget` and `--- FAIL: TestUninstall_YesWithExplicitTarget_HonoursTarget` from the RED commit
    - `go test ./internal/cli/ -count=1` is `ok`
  </acceptance_criteria>
  <done>`--target X --yes` configures and removes exactly X in both commands, proven RED first and through the real binary.</done>
</task>

<task type="auto">
  <name>Task 2: Family (b) — prove the ordering guard can fail</name>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md (convention section and Family (a) shape)
    - internal/cli/install.go, internal/cli/uninstall.go (the fixed switches)
  </read_first>
  <action>
    Append `## Family (b1)` (install.go) and `## Family (b2)` (uninstall.go) in the established shape. Mutation for each: `perl -pi -e 's/case cmd\.Flags\(\)\.Changed\("target"\):/case !yes \&\& cmd.Flags().Changed("target"):/' <file>` — `--yes` shadows the explicit target again. Observed failure: `TestInstall_YesWithExplicitTarget_HonoursTarget` (b1) and `TestUninstall_YesWithExplicitTarget_HonoursTarget` (b2), verbatim `--- FAIL:` lines plus `exit=`. Revert each with `git checkout -- <file>` behind the cleanliness gates and a green control. Commit `docs(07-02): Family (b) mutation log`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && git diff --quiet -- internal/cli/install.go && test "$(rg -c -F 'case cmd.Flags().Changed("target"):' internal/cli/install.go)" = "1" && perl -pi -e 's/case cmd\.Flags\(\)\.Changed\("target"\):/case !yes \&\& cmd.Flags().Changed("target"):/' internal/cli/install.go && ! git diff --quiet -- internal/cli/install.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestInstall_YesWithExplicitTarget_HonoursTarget$' -v > "$O/red-b1.txt" 2>&1; echo "exit=$?" >> "$O/red-b1.txt"); git checkout -- internal/cli/install.go; git diff --quiet -- internal/cli/install.go && rg -q -- '--- FAIL: TestInstall_YesWithExplicitTarget_HonoursTarget' "$O/red-b1.txt" && ! rg -q '^exit=0$' "$O/red-b1.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(b[12]\)' "$M")" = "2" && rg -q -- '--- FAIL: TestInstall_YesWithExplicitTarget_HonoursTarget' "$M" && rg -q -- '--- FAIL: TestUninstall_YesWithExplicitTarget_HonoursTarget' "$M" && (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 > "$O/pkg.txt" 2>&1; echo "exit=$?" >> "$O/pkg.txt") && rg -q '^exit=0$' "$O/pkg.txt" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-02\): Family \(b\)'</automated>
    <fails_when>non-zero exit; install.go dirty before the plant or after the revert; the planted run exiting 0 or lacking the named FAIL line; fewer than 2 Family (b) sections or either FAIL excerpt missing from the log; the cli package not green after revert; or no docs(07-02) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - both Family (b) sections carry Pre-mutation gate, Mutation applied, Observed failure, Pre-revert gate, Revert, Green control
    - `git diff --quiet -- internal/cli/install.go internal/cli/uninstall.go` exits 0 at the end
  </acceptance_criteria>
  <done>The resolution-order guard is proven able to fail in both commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → which agents' config files are written or deleted | an unintended target set rewrites or removes other agents' files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | install/uninstall target switch | medium | mitigate | explicit --target checked before --yes in both commands; RED-first tests; Family (b) |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1` green; the real binary honours `--target codex --yes`
</verification>

<success_criteria>
- D-13 closed in install and uninstall; the todo completed through the verb; 07-VALIDATION row 07-YES satisfiable with `go test ./internal/cli/ -run 'TestInstall.*Yes.*Target|TestUninstall.*Yes.*Target' -count=1`
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-02-SUMMARY.md` when done — include the RED transcript.
</output>
