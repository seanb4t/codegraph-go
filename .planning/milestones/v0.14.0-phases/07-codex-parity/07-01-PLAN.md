---
phase: 07-codex-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agents/toml.go
  - internal/agents/toml_test.go
  - internal/agents/testdata/toml/codex-indented-layout.toml
  - internal/agents/testdata/toml/codex-indented-layout.installed.toml
  - internal/agents/testdata/toml/codex-indented-layout.uninstalled.toml
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [CODEX-02]
user_setup: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On a config.toml shaped like the maintainer's (a 2-space-indented `[mcp_servers.codegraph]` followed by indented sibling MCP tables, a multi-line array, an inline table, a comment line and, much later, a column-0 `[memories]`), `codegraph install --target codex --location global` and the matching uninstall change only codegraph's own table: the result equals the hand-authored `codex-indented-layout.installed.toml` / `.uninstalled.toml` byte for byte (D-07, the data-loss finding in 07-CONTEXT specifics)."
    - "`findTOMLTableRange` ends codegraph's table at the next table header at ANY indentation, and never at a line inside a multi-line basic/literal string or an unfinished multi-line array (D-07)."
    - "codegraph's own `[mcp_servers.codegraph.*]` subtables that follow its header are inside its range for both splice and strip (D-07)."
    - "A run of blank and comment-only lines immediately before the terminating header (or EOF) stays with what follows, so a user's comment above the next table survives splice and strip (CODEX-02: comments preserved)."
    - "CRLF input keeps CRLF: a spliced block written into a CRLF file uses CRLF line endings, and strip after splice restores the CRLF file byte for byte (D-07)."
    - "`tomlTableConflict` returns an error wrapping `errTOMLTableConflict`, naming the 1-based line, for an inline `codegraph = {…}` under `[mcp_servers]`, a dotted `mcp_servers.codegraph.*` key at root or under `[mcp_servers]`, a quoted or spaced variant of the header, a `[[mcp_servers.codegraph]]` array-of-tables, a duplicate `[mcp_servers.codegraph]` header, or an own subtable detached from the header; on any conflict `spliceTOMLTable` and `stripTOMLTable` return the input unchanged, never a duplicate key (D-07). The CLI-visible install/uninstall error lands with the first `codex.go` change in 07-05 (D-01 ordering)."
    - "Family (a) in 07-MUTATION-LOG.md: (a1) the D-07-mandated column-0-only end scan turns `TestSpliceTOMLTable_MaintainerIndentedLayout` RED; (a2) a disabled conflict check and (a3) an LF-only line ending each turn their guard RED; every mutation is reverted byte-clean."
    - "A WINDOWS ledger row (kind `deviation`) records that released binaries still carry the bug with no patch release (D-08, maintainer decision B2), written through the `windows append` verb, never by hand."
    - "`internal/agents/codex.go` is not modified by this plan (D-01: no `codex.go` change before the CODEX-01 live verification)."
    - "Range offsets are byte offsets that always land on line starts; on a CRLF file the carriage return stays inside its line, so a splice or strip never splits a CR from its LF."
  artifacts:
    - path: "internal/agents/toml.go"
      provides: "findTOMLTableRange (rewritten scanner, signature unchanged), isTOMLHeaderLine, tomlLineEnding, tomlTableConflict, errTOMLTableConflict, the line scanner"
      contains: "func tomlTableConflict("
    - path: "internal/agents/toml_test.go"
      provides: "D-07 fixtures: maintainer layout, indentation, CRLF, subtables, multi-line strings/arrays, trailing comments, conflicts"
      contains: "TestSpliceTOMLTable_MaintainerIndentedLayout"
    - path: "internal/agents/testdata/toml/codex-indented-layout.toml"
      provides: "synthetic fixture mirroring the maintainer's indented layout (never the real file)"
    - path: ".planning/phases/07-codex-parity/07-MUTATION-LOG.md"
      provides: "Phase 7 mutation log, Family (a)"
      contains: "## Family (a1)"
  key_links:
    - from: "internal/agents/codex.go Install/Uninstall (unchanged)"
      to: "internal/agents/toml.go spliceTOMLTable/stripTOMLTable"
      via: "unchanged call sites; the fix is entirely inside toml.go"
      pattern: "spliceTOMLTable\\(|stripTOMLTable\\("
    - from: "internal/agents/toml.go findTOMLTableRange"
      to: "isTOMLHeaderLine"
      via: "the single header-recognition predicate the range end scan uses (Family a1 mutation site)"
      pattern: "isTOMLHeaderLine\\("
  prohibitions:
    - statement: "MUST NOT delete, reorder or rewrite any byte of a user's Codex config.toml outside codegraph's own `[mcp_servers.codegraph]` table and its own subtables — the released-binary data-loss class (D-07)."
      status: resolved
      verification: test
    - statement: "MUST NOT answer an inline, dotted, quoted, array-of-tables or duplicate codegraph definition with a second `[mcp_servers.codegraph]` table (a duplicate key makes the user's whole config invalid) — refuse and leave the file untouched (D-07)."
      status: resolved
      verification: test
    - statement: "MUST NOT cut a patch release, create a git tag, or edit WINDOWS.md/STATE.md by hand for the D-08 window — the ledger row goes through the tool verb and release-please stays the sole tag authority (D-08, D-06R)."
      status: resolved
      verification: judgment
---

<objective>
Fix the Codex TOML splice at its cause, RED-first (D-07), so no released-binary-shaped config can lose a
sibling MCP server again, and record the released-binary window in the ledger (D-08). This is Phase 7's
first plan by the locked order (D-01): it touches `toml.go` only — `codex.go` stays untouched until the
CODEX-01 live verification (07-04) has run.

Purpose: CODEX-02's "existing tables, inline tables, comments and CRLF preserved" half, and the
07-VALIDATION row `07-TOML`. The inline/dotted refusal is built here as a `toml.go` refusal (content
returned unchanged, conflict reported by `tomlTableConflict`); 07-05 surfaces it as an install error in the
same commit as the first `codex.go` change.

Output: `test(07-01):` RED commit, `fix(07-01):` GREEN commit, a second RED/GREEN pair for the remaining
D-07 cases, `07-MUTATION-LOG.md` (created here, Family (a)), one WINDOWS ledger row.

Planner notes (read before executing):
- The tracer (Task 1) runs the REAL binary against a synthetic fixture shaped like the maintainer's
  `~/.codex/config.toml` (07-CONTEXT specifics: `[mcp_servers.codegraph]` 2-space indented, the next
  column-0 header far below). NEVER read, copy or print the real `~/.codex/config.toml`; never run
  `codegraph install/uninstall --target codex|all` against the real HOME (07-CONTEXT D-08, STATE warning).
  Every run in this plan uses a `mktemp -d` HOME.
- Expected-output fixtures (`.installed.toml`, `.uninstalled.toml`) are an INDEPENDENT oracle: author them
  by hand from the input file and the rules below, never by running the code under test.
- Pinned mutation sites (the verify commands re-plant them, so keep these exact shapes):
  `func isTOMLHeaderLine(line string) bool` contains the file's ONLY occurrence of the exact expression
  `strings.TrimLeft(line, " \t")` (Family a1 turns it into `line`);
  `func tomlTableConflict(content, tableName string) error {` and
  `func tomlLineEnding(content string) string {` keep exactly these signature lines (Families a2/a3 insert
  an early return after them).
- TDD (`workflow.tdd_mode`): each task lands its failing tests first as a `test(07-01):` commit that
  COMPILES (RED on assertions, never on a build error — add only the minimal stubs the tests reference),
  then the implementation as `fix(07-01):`. Paste the `--- FAIL:` transcript into the SUMMARY. Never gate on
  `gsd-tools check tdd-red-evidence` (TAP-only). Every Go command runs with `GOTOOLCHAIN=go1.26.6`. Commits
  end with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never `[ci skip]`/`[skip ci]`;
  `git -c commit.gpgsign=false commit` is allowed. No new `go.mod` dependency — the TOML handling stays
  hand-rolled (D-05a, minimal deps).
- Plans in this phase run strictly in sequence (waves 1-11): `workflow.use_worktrees` is false, executors
  share one working tree, and every plan appends to the one `07-MUTATION-LOG.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-RESEARCH.md
@.planning/phases/07-codex-parity/07-PATTERNS.md
@.planning/phases/07-codex-parity/07-VALIDATION.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md
@internal/agents/toml.go
@internal/agents/toml_test.go
@internal/agents/codex.go
@internal/agents/testhelpers_test.go
</context>

<interfaces>
Binding names (later plans, the mutation families and the verify commands target them):

internal/agents/toml.go (package agents):
- `func findTOMLTableRange(content, tableName string) (start, end int, found bool)` — SIGNATURE UNCHANGED
  (codex.go:94 and ownership_test.go:408 call it). `found` is true only for a line whose text, with
  surrounding whitespace and any trailing `# comment` removed, is exactly `[` + tableName + `]`.
- `func isTOMLHeaderLine(line string) bool` — the one predicate that recognizes a `[x]` / `[[x]]` table
  header; strips leading spaces/tabs with `strings.TrimLeft(line, " \t")` (the file's only such expression).
- `func tomlLineEnding(content string) string` — `"\r\n"` when content contains `"\r\n"`, else `"\n"`.
- `var errTOMLTableConflict = errors.New(...)` and `func tomlTableConflict(content, tableName string) error`.
- `spliceTOMLTable(content, tableName string, bodyLines []string) string` and
  `stripTOMLTable(content, tableName string) string` — SIGNATURES UNCHANGED; both return `content`
  unchanged when `tomlTableConflict` reports a conflict.
- The line scanner type/function names are the executor's choice (list them in the SUMMARY).

Current behaviour being replaced (toml.go:96-101): the end scan uses `strings.HasPrefix(lines[i], "[")`,
i.e. column 0 only — the released bug.
</interfaces>

## Artifacts this phase produces

Phase-wide list (every later plan points here and lists its own subset):

| Artifact | Kind | Plan |
|---|---|---|
| `isTOMLHeaderLine`, `tomlLineEnding`, `tomlTableConflict`, `errTOMLTableConflict`, the rewritten `findTOMLTableRange` scanner; `internal/agents/testdata/toml/codex-indented-layout{,.installed,.uninstalled}.toml` | Go API + fixtures (internal/agents/toml.go) | 07-01 |
| `TestFindTOMLTableRange_*`, `TestSpliceTOMLTable_*`, `TestStripTOMLTable_*`, `TestTOMLTableConflict`, `TestCodexGlobal_MaintainerIndentedLayoutRoundTrip` | tests | 07-01 |
| `07-MUTATION-LOG.md` Families (a)-(i) | evidence (D-00) | 07-01 … 07-11 |
| WINDOWS ledger row "Codex TOML splice data loss in released binaries" | ledger (D-08) | 07-01 |
| `TestInstall_YesWithExplicitTarget_HonoursTarget`, `TestUninstall_YesWithExplicitTarget_HonoursTarget`; completed todo `2026-09-18-install-yes-discards-explicit-target.md` | tests + todo (D-13) | 07-02 |
| `TestAgentPickerFootprintFitsDefaultPane`, `TestAgentPickerFootprint_HeightBoundaries`, `TestAgentPickerFootprint_EmptyRoster`, `TestDaemonPickerFootprintFitsDefaultPane`; TTY-05 footer anchor in `test/tmux/install_cancel_test.go` | tests (D-24, D-25) | 07-03 |
| `.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md` (CODEX-01 section; CODEX-05/06 section) | live evidence (D-02…D-06) | 07-04, 07-09 |
| `codexConfigPath(loc Location)`, `codexInstructionsPath(loc Location)`, `codexSkillDirs(loc Location)`, `codexTrustNote(loc Location) (string, error)`; Codex `Capabilities` with both scopes; conflict error surfaced by Install/Uninstall; emptied config removed | Go API (internal/agents/codex.go) | 07-05 |
| `TestCodex_SupportsLocation_GlobalAndLocal`, `TestCodex_Install_Local_*`, `TestCodex_Install_RefusesConflictingCodegraphTable`, `TestCodex_Uninstall_EmptiedConfigIsRemoved`, `TestCodex_DescribePaths_Local`, `TestCodex_Detect_Local_*`, `TestResolveTargetFlag_AutoDetectsCodexAtLocal`, `TestAgentPickerModel_PreChecksCodexAtLocal`, `TestCodex_SharedSkillPackage_LastRequester`, `TestCodex_ReadOnlySkillDirsFollowLiveVerdict` | tests | 07-05 |
| `instructionsRequestedElsewhere(path string, loc Location, self TargetID) bool`; `codexOverrideNote`; opencode/codex Uninstall `kept` gate | Go API (D-11, D-12) | 07-06 |
| `TestSharedAgentsMD_UninstallOrders`, `TestSharedAgentsMD_KeptWhileOtherConfigured`, `TestCodex_Install_OverrideNote`, `TestOwnershipSharedInstructions` | tests | 07-06 |
| `.codex/hooks/hooks.json` (PreToolUse fragment), `.codex/hooks/codegraph-pretooluse-local.sh`, `.codex/hooks/codegraph-pretooluse-global.sh` (guard templates) | embedded assets (D-20, D-22) | 07-07 |
| `codexassets.go` (package `claudeassets`): `CodexFS`, `CodexHooksFragmentPath`, `CodexPreToolUseGuardLocalPath`, `CodexPreToolUseGuardGlobalPath`, `CodexHooksFragment()`, `CodexPreToolUseGuardLocalTemplate()`, `CodexPreToolUseGuardGlobalTemplate()` | embed accessors | 07-07 |
| `internal/agents/codex_pretooluse.go`: `codexPreToolFragmentCommand`, `codexHooksJSONPath`, `codexPreToolGuardPath`, `codexPreToolHookCommand`, `codexPreToolUseBlocks`, `renderCodexPreToolGuard`, `installCodexPreToolNudge`, `uninstallCodexPreToolNudge`; `HookFiles` case for `HooksCodexJSON`; Codex `Hooks: HooksCodexJSON` | Go API (D-09, D-19, D-20, D-22, D-23) | 07-07 |
| hidden flag `codegraph hook pretooluse --harness <claude\|codex>`; `codexPreToolUseInput`, `codexShellCommand`, `runHookPreToolUseCodex` | CLI (internal/cli/hook_pretooluse.go, D-21) | 07-07 |
| `TestCodexHooksFragmentShape`, `TestCodex_Install_PreToolNudgeOn_WritesGuardAndGroup`, `TestCodex_Install_DefaultWritesNoHooks`, `TestCodexPreToolUseGuard`, `TestRenderCodexPreToolGuard`, `TestHookPreToolUseCodex_*` | tests | 07-07 |
| `codexHooksExplicitlyDisabled(loc Location) (bool, string, error)`, `tomlBoolSetting(content, table, key string) (value, set bool)`, `codexHookTrustNote`, `codexHooksDisabledNote`; Codex Keep/Off semantics | Go API (D-18, D-19, D-23) | 07-08 |
| `--pretool-nudge` widened to Claude Code and Codex CLI (help text, D-09 note); regenerated `docs/CLI-REFERENCE.md` | CLI + docs | 07-08 |
| `TestCodexPreToolNudge_*`, `TestInstall_PreToolNudge_NoteWhenNeitherClaudeNorCodexSelected`, `TestInstall_PreToolNudge_CodexOptInRegisters`, `TestCodexNotesNeverAdviseTrustBypass`, `TestInstallHelpNeverAdvisesTrustBypass` | tests | 07-08 |
| `docs/AGENT-CAPABILITIES.md`; `internal/agents/capability_doc_test.go` (`TestCapabilityDoc_MirrorsCapabilities`, `TestCapabilityDoc_VerificationColumn`); README link | docs + drift guard (D-27, D-28) | 07-10 |
| rewritten `instructions` const (internal/mcp/server.go); `TestInstructionsSkillSentenceWithinFirst512Bytes`; re-frozen wire transcripts; D-30 comments | wire contract + docs (D-29, D-30) | 07-11 |

## Flagged assumptions (spec-less edge probe — phase-wide, no silent drop)

The edge probe surfaced 17 applicable rows (0 resolved at probe time). Fourteen are authored into the
`must_haves` of the plans that own them — CODEX-04 concurrency (backstop) in 07-06; CODEX-05 empty and
encoding (explicit) in 07-07; CODEX-06 adjacency, empty and ordering (backstop) in 07-09; FIX-03 boundary,
adjacency, empty, ordering and precision (explicit) in 07-03; AGENT-14 adjacency, empty and ordering
(explicit) in 07-10. The three `unclassified` rows stay
unresolved and are surfaced here, never dropped (17 = 14 authored + 3 flagged):

1. **CODEX-01 (unclassified)** — a live-evidence requirement, not a data-shape edge; its checkable content is
   07-04's locked pass bar (L1-L4, L7) and the A1/A2/D-15/D-16 lines, backed by D-00's rule that harness
   behaviour is proven only live.
2. **CODEX-02 (unclassified)** — its data edges are the D-07 TOML cases pinned in this plan and the
   `--yes`/`--target` resolution pinned in 07-02; the trust-gated loading half is live evidence (07-04 L1/L2).
3. **CODEX-03 (unclassified)** — its edges (idempotency, sibling byte-invariance, the shared manifest's
   last-requester rule) are pinned in 07-05 by `TestCodex_SharedSkillPackage_LastRequester` and the ownership
   table; whether Codex reads the path is live evidence (07-04 L3, D-15).

Additional flagged assumptions (recorded rather than silently absorbed):
- **CODEX_HOME is not honoured.** codegraph resolves every global Codex path (config, `AGENTS.md`,
  `hooks.json`, guard) under `~/.codex`, as `codexConfigPath` already does; D-20's "`$CODEX_HOME/hooks/…`"
  is read as that same directory. A user with a non-default `CODEX_HOME` gets files Codex never reads
  (pre-existing behaviour, advisory only). The live sessions set `CODEX_HOME=$HOME/.codex`, so both coincide.
- **D-07's refusal is split by D-01's ordering.** `toml.go` refuses (content unchanged, conflict reported) in
  07-01; the install/uninstall error that names the conflict lands with the first `codex.go` change (07-05).
- **D-09's literal completes in two plans.** Scopes/MCPConfig/Instructions/SkillDirs flip in 07-05; `Hooks:
  HooksCodexJSON` plus its `HookFiles` case land in 07-07 with the hook writes, because declaring a hook
  mechanism before Install writes it would turn `TestCapabilitiesMatchInstallWrites` RED between plans.
- **Trailing comments belong to what follows.** CODEX-02's "comments preserved" is implemented by ending
  codegraph's range before the run of blank/comment lines that precedes the next header; a comment placed
  INSIDE codegraph's own table body is codegraph's and is replaced with it.
- **D-22 and a subdirectory cwd.** The Go core re-checks stdin `cwd` for `<cwd>/.codegraph`; a Codex session
  started in a subdirectory of an indexed repo stays silent, the same parity Claude's `CLAUDE_PROJECT_DIR`
  check has.
- **A1 and A4 are live questions.** The Codex hook command form assumes Codex runs `command` through a shell
  (A1, settled by 07-04 before 07-07 depends on it); the adapter keys on `session_id` + `agent_id`, falling
  back to `main` when Codex sends no `agent_id`, which 07-09 records either way (A4).
- **`codegraph upgrade` does not refresh Codex's guard** (deferred in 07-CONTEXT); a plain `codegraph
  install` (Keep) re-renders it.

<assumption_delta_decision>
Detector fired (`pluralization`: "second file" — Codex gains a project-local config file beside the global
one; "also"; `chosen`: "selectable"). Primary noun: the per-location config file resolved from the capability
table (`Capabilities().MCPConfig(loc)`), not "the Codex config". Decision: `no-change` for the identity model —
Phase 5 already promoted per-location `PathFunc`s to the primary representation, and Codex's `codexConfigPath`
becomes a real `PathFunc(Location)` like opencode's (07-05); `toml.go` keeps operating on content, so a second
file needs no new identity. The shared repo-root `AGENTS.md` (a second writer of one file) is decided in 07-06:
`promote` from "the uninstalling target owns the block" to "the block is owned while any registered target
declaring that path still reports `Detect(loc).AlreadyConfigured`" (D-11), derived from the registry, never
stored. Suggested invariant test (accepted, 07-06): every uninstall order restores `AGENTS.md` byte-identical.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a released-bug-shaped config survives install and uninstall" — the maintainer layout, through the real binary</name>
  <files>internal/agents/toml.go, internal/agents/toml_test.go, internal/agents/testdata/toml/codex-indented-layout.toml, internal/agents/testdata/toml/codex-indented-layout.installed.toml, internal/agents/testdata/toml/codex-indented-layout.uninstalled.toml</files>
  <read_first>
    - internal/agents/toml.go (whole file — the scanner being replaced, lines 71-103)
    - internal/agents/toml_test.go (existing fixtures and the indexOf/containsAll helpers)
    - internal/agents/codex.go (Install/Uninstall call sites that must keep working UNCHANGED)
    - internal/agents/testhelpers_test.go (fakeHome, readFile, writeFile)
    - .planning/phases/07-codex-parity/07-CONTEXT.md (D-07, D-08, and the Specific Ideas blocking finding)
    - .planning/phases/07-codex-parity/07-RESEARCH.md (Pitfall 1, "TOML header-scanner extension shape")
  </read_first>
  <behavior>
    - TestFindTOMLTableRange_MaintainerIndentedLayout: on codex-indented-layout.toml, `found` is true, `start` is the byte offset of the indented `  [mcp_servers.codegraph]` line, and `content[end:]` begins with the blank line that precedes the next indented sibling header (never `[memories]`).
    - TestSpliceTOMLTable_MaintainerIndentedLayout: `spliceTOMLTable(input, "mcp_servers.codegraph", codexBody())` equals codex-indented-layout.installed.toml byte for byte.
    - TestStripTOMLTable_MaintainerIndentedLayout: `stripTOMLTable(installed, "mcp_servers.codegraph")` equals codex-indented-layout.uninstalled.toml byte for byte.
    - TestCodexGlobal_MaintainerIndentedLayoutRoundTrip: with fakeHome, input written to `~/.codex/config.toml`, `codexTarget{}.Install(LocationGlobal, InstallOptions{ExecPath: "/usr/local/bin/codegraph"})` leaves exactly the installed bytes and `Uninstall(LocationGlobal)` exactly the uninstalled bytes (codex.go itself unchanged).
  </behavior>
  <action>
    Fixtures (LF endings, synthetic — never derived from the real `~/.codex/config.toml`):
    - `codex-indented-layout.toml`: two root keys (e.g. `model = "gpt-test"`, `approval_policy = "on-request"`); a blank line; a column-0 `[features]` table with `hooks = true`; a blank line; then 2-space-indented sibling tables separated by single blank lines: `  [mcp_servers.alpha]` (command, `args = ["a"]`), `  [mcp_servers.codegraph]` (`  command = "/old/codegraph"`, `  args = ["serve", "--mcp"]`), `  [mcp_servers.context7]` whose `args = [` spans several indented lines closing with `  ]`, `  [mcp_servers.engram]` carrying an inline table `env = { MEMORY_DIR = "/tmp/m" }` and preceded directly by an indented comment line `  # engram: local memory`; then a blank line, a column-0 comment `# memories`, and a column-0 `[memories]` with one key. At least 20 lines separate the codegraph header from `[memories]`.
    - `.installed.toml`: the input with the old codegraph header line and its two body lines replaced by exactly three column-0 lines — `[mcp_servers.codegraph]`, `command = "/usr/local/bin/codegraph"`, `args = ["serve", "--mcp"]` — everything else byte-identical (both blank lines around the block kept).
    - `.uninstalled.toml`: the input with the old codegraph header and body lines AND exactly one of the two blank lines around them removed (alpha's body, one blank line, then context7), everything else byte-identical.
    RED: add the four behaviour tests (plus a `readTestdata(t, name)` helper if useful) and commit `test(07-01): add failing maintainer-layout TOML splice tests` — they must compile against the current `toml.go` and fail on assertions (the current column-0 scan swallows context7/engram/`# memories`).
    GREEN (commit `fix(07-01): end the Codex TOML table at any-indent headers`): rewrite `findTOMLTableRange` over a line scanner that records each line's byte start offset (splitting on `"\n"`; a trailing `"\r"` stays part of the line text), and classifies each line as header / blank-or-comment / other while tracking two states — inside a multi-line basic (`"""`) or literal (`'''`) string, and the bracket/brace depth of an unfinished value array or inline table (count `[` `]` `{` `}` only outside strings and before a `#` comment). A line is a header only when both states are neutral and `isTOMLHeaderLine` reports it. Codegraph's header is the first line whose text, trimmed and stripped of a trailing `# comment`, equals exactly `[mcp_servers.codegraph]`. The range end is the start of the first following header whose dotted path is neither the table itself nor prefixed by `mcp_servers.codegraph.` (own subtables stay inside), backed off to the start of the contiguous run of blank/comment-only lines immediately before that header; with no such header, the same back-off applies from EOF (the empty element after a final newline is not a line). Update the function's doc comment to describe these rules (the old comment names the column-0 rule). Do not change `spliceTOMLTable`/`stripTOMLTable` signatures, and do not touch `internal/agents/codex.go`.
    Verify through the real binary as below.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-01\): ' && printf '%s\n' "$S" | rg -q '^fix\(07-01\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestFindTOMLTableRange_MaintainerIndentedLayout$|TestSpliceTOMLTable_MaintainerIndentedLayout$|TestStripTOMLTable_MaintainerIndentedLayout$|TestCodexGlobal_MaintainerIndentedLayoutRoundTrip$' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -4 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c '^--- PASS: (TestFindTOMLTableRange_MaintainerIndentedLayout|TestSpliceTOMLTable_MaintainerIndentedLayout|TestStripTOMLTable_MaintainerIndentedLayout|TestCodexGlobal_MaintainerIndentedLayoutRoundTrip) ' "$O/t1.txt")" = "4" && GOTOOLCHAIN=go1.26.6 go build -o "$O/codegraph" ./cmd/codegraph && H="$O/home" && mkdir -p "$H/.codex" && cp internal/agents/testdata/toml/codex-indented-layout.toml "$H/.codex/config.toml" && HOME="$H" XDG_CONFIG_HOME="$H/.config" HERMES_HOME="$H/.hermes" "$O/codegraph" install --target codex --location global > "$O/install.txt" && test "$(rg -c '^\s*\[mcp_servers\.(alpha|context7|engram)\]$' "$H/.codex/config.toml")" = "3" && test "$(rg -c '^\[memories\]$' "$H/.codex/config.toml")" = "1" && test "$(rg -c '^  # engram: local memory$' "$H/.codex/config.toml")" = "1" && test "$(rg -c '^command = ".*/codegraph"$' "$H/.codex/config.toml")" = "1" && HOME="$H" XDG_CONFIG_HOME="$H/.config" HERMES_HOME="$H/.hermes" "$O/codegraph" uninstall --target codex --location global > "$O/uninstall.txt" && cmp "$H/.codex/config.toml" internal/agents/testdata/toml/codex-indented-layout.uninstalled.toml && L=$(git log --diff-filter=A --format=%H -- .planning/phases/07-codex-parity/07-01-PLAN.md) && B=$(printf "%s\n" "$L" | tail -1) && test -n "$B" && test -z "$(git log --format=%H "$B"..HEAD -- internal/agents/codex.go)" && echo "tracer green: the maintainer layout survives the real binary"</automated>
    <fails_when>non-zero exit; either commit subject missing from the last 20; a build/gofmt/vet failure; fewer than 4 named PASS lines or any FAIL line; after the real install fewer than 3 sibling headers, no `[memories]`, the engram comment gone, or no rewritten codegraph command line; `cmp` reporting a difference after uninstall; or any commit since the plans commit touching internal/agents/codex.go</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --stat` of the `fix(07-01)` commit lists no file outside `internal/agents/toml.go` (the fixtures and tests land in the RED commit)
    - `rg -c -F 'strings.TrimLeft(line, " \t")' internal/agents/toml.go` prints `1`
    - the RED transcript pasted in the SUMMARY shows `--- FAIL: TestSpliceTOMLTable_MaintainerIndentedLayout` with context7/engram missing from the got-bytes
    - every pre-existing test in toml_test.go and codex_test.go still passes unchanged
  </acceptance_criteria>
  <done>The released data-loss shape is reproduced RED and fixed at its cause, proven through the real binary on a maintainer-shaped fixture, with codex.go untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The remaining D-07 cases — multi-line strings/arrays, own subtables, trailing comments, CRLF, and the conflict refusal</name>
  <files>internal/agents/toml.go, internal/agents/toml_test.go</files>
  <read_first>
    - internal/agents/toml.go (Task 1's scanner)
    - internal/agents/toml_test.go
    - .planning/phases/07-codex-parity/07-CONTEXT.md (D-07 bullet list)
  </read_first>
  <behavior>
    - TestFindTOMLTableRange_HeaderLikeLinesInsideMultilineStringsAndArrays: subtests basic_ml_string, literal_ml_string, multiline_array, nested_array_element — a line beginning with `[` inside each never ends codegraph's range.
    - TestFindTOMLTableRange_OwnSubtablesInRange: `[mcp_servers.codegraph.env]` after the header is inside the range; splice drops it with the old body; strip removes it; a following `[mcp_servers.codegraph_other]` (prefix without the dot) ends the range.
    - TestFindTOMLTableRange_TrailingCommentsStayWithNextTable: blank + comment lines before the next header (and before EOF) are outside the range; splice and strip keep them.
    - TestFindTOMLTableRange_HeaderWithTrailingComment: `[mcp_servers.codegraph] # managed` is found.
    - TestSpliceTOMLTable_CRLFPreserved: on a CRLF input (built in Go with strings.ReplaceAll of an LF literal) the result contains no LF that is not preceded by CR, both when appending and when replacing; TestStripTOMLTable_CRLFRoundTrip: strip(splice(x)) == x for a CRLF x.
    - TestTOMLTableConflict: subtests inline_table_under_parent, dotted_key_at_root, dotted_key_under_parent, quoted_header, spaced_header, array_of_tables, duplicate_header, detached_own_subtable each return an error wrapping errTOMLTableConflict whose text names the 1-based line; subtests clean_own_table, prefix_lookalike_table (`[mcp_servers.codegraph_other]`), unrelated_key_named_codegraph (`codegraph_path = "x"` under another table) and quoted_project_trust_table (`[projects."/tmp/a.b/repo"]` with `trust_level = "trusted"`) return nil.
    - TestSpliceTOMLTable_ConflictLeavesContentUnchanged and TestStripTOMLTable_ConflictLeavesContentUnchanged: for every conflicting subtest input, splice and strip return the input byte-identical.
  </behavior>
  <action>
    RED commit `test(07-01): add failing D-07 multi-line, subtable, comment, CRLF and conflict cases` (compiling — add a stub `tomlTableConflict` returning nil and a stub `tomlLineEnding` returning "\n" only if the tests need them to compile; `errTOMLTableConflict` may be declared in the RED commit).
    GREEN commit `fix(07-01): preserve CRLF and refuse conflicting codegraph TOML forms`:
    - `tomlLineEnding(content)` returns `"\r\n"` when content contains `"\r\n"`, else `"\n"`. `spliceTOMLTable` builds the new block with that ending, and its append path trims trailing CR/LF from the existing content and joins with two endings; `stripTOMLTable` trims CR/LF on both sides of the removed span and joins with two endings (one when either side is empty), preserving today's LF behaviour byte for byte for LF input.
    - `tomlTableConflict(content, tableName)` walks the same scanner, tracking the current table's dotted path. Dotted paths are split on `.` outside quotes, each segment trimmed of spaces and unquoted (basic `"…"` or literal `'…'`). It reports: a header whose normalized path equals tableName but whose trimmed, comment-stripped text is not exactly `[` + tableName + `]`; any `[[…]]` header whose path equals tableName or starts with tableName + "."; more than one exact header; a key line whose effective path (current table path + the key's own dotted path, taken before the first `=` outside quotes) equals tableName or starts with tableName + "." while the current table is neither tableName nor one of its own contiguous subtables (this covers inline `codegraph = {…}` under `[mcp_servers]` and dotted `mcp_servers.codegraph.command = …` at root); and a header prefixed by tableName + "." that is not inside the range `findTOMLTableRange` returns. The error wraps `errTOMLTableConflict` via `fmt.Errorf("%w: line %d: %q", …)` with the trimmed line text.
    - `spliceTOMLTable` and `stripTOMLTable` call `tomlTableConflict` first and return `content` unchanged on a non-nil result. Keep the pinned signature lines listed in the objective.
    Leave `internal/agents/codex.go` untouched (07-05 surfaces the conflict as an install error).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && test "$(printf '%s\n' "$S" | rg -c '^test\(07-01\): ')" -ge 2 && test "$(printf '%s\n' "$S" | rg -c '^fix\(07-01\): ')" -ge 2 && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestSpliceTOMLTable|TestStripTOMLTable|TestFindTOMLTableRange|TestTOMLTableConflict|TestCodexGlobal_|TestCodex_' -v > "$O/t2.txt" 2>&1; echo "exit=$?" >> "$O/t2.txt") && tail -3 "$O/t2.txt" && rg -q '^exit=0$' "$O/t2.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t2.txt" && test "$(rg -c -- '--- PASS: TestTOMLTableConflict/(inline_table_under_parent|dotted_key_at_root|dotted_key_under_parent|quoted_header|spaced_header|array_of_tables|duplicate_header|detached_own_subtable|clean_own_table|prefix_lookalike_table|unrelated_key_named_codegraph|quoted_project_trust_table) ' "$O/t2.txt")" = "12" && test "$(rg -c -- '--- PASS: TestFindTOMLTableRange_HeaderLikeLinesInsideMultilineStringsAndArrays/(basic_ml_string|literal_ml_string|multiline_array|nested_array_element) ' "$O/t2.txt")" = "4" && test "$(rg -c '^--- PASS: (TestFindTOMLTableRange_OwnSubtablesInRange|TestFindTOMLTableRange_TrailingCommentsStayWithNextTable|TestFindTOMLTableRange_HeaderWithTrailingComment|TestSpliceTOMLTable_CRLFPreserved|TestStripTOMLTable_CRLFRoundTrip|TestSpliceTOMLTable_ConflictLeavesContentUnchanged|TestStripTOMLTable_ConflictLeavesContentUnchanged|TestStripTOMLTable_RemovesOnlyCodegraphBlock_RoundTrip|TestCodex_GlobalRoundTrip_ByteInvariant) ' "$O/t2.txt")" = "9" && test "$(rg -c '^func (tomlTableConflict\(content, tableName string\) error|tomlLineEnding\(content string\) string) \{$' internal/agents/toml.go)" = "2" && L=$(git log --diff-filter=A --format=%H -- .planning/phases/07-codex-parity/07-01-PLAN.md) && B=$(printf "%s\n" "$L" | tail -1) && test -z "$(git log --format=%H "$B"..HEAD -- internal/agents/codex.go)"</automated>
    <fails_when>non-zero exit; fewer than two test(07-01) or fix(07-01) subjects; any FAIL line; fewer than 12 conflict subtests, 4 multi-line subtests or 9 named top-level PASS lines; either pinned signature line missing; or internal/agents/codex.go touched</fails_when>
  </verify>
  <acceptance_criteria>
    - the RED transcript in the SUMMARY shows at least `--- FAIL: TestSpliceTOMLTable_CRLFPreserved` and `--- FAIL: TestTOMLTableConflict/inline_table_under_parent`
    - `go test ./internal/agents/ -count=1` is `ok` (every pre-existing agents test still green, including TestOwnershipExactIdentity)
    - `rg -c 'errTOMLTableConflict' internal/agents/toml.go` is at least 2 (declaration + wrap)
  </acceptance_criteria>
  <done>Every D-07 case is pinned by a test that was RED first, and every conflicting form is refused without touching the file.</done>
</task>

<task type="auto">
  <name>Task 3: Family (a) — prove the TOML guards can fail — and record the D-08 released-binary window in the ledger</name>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md, .planning/WINDOWS.md</files>
  <read_first>
    - .planning/phases/06-claude-code-pretooluse-nudge/06-MUTATION-LOG.md (header, pre-mutation cleanliness gate convention, family entry shape)
    - internal/agents/toml.go (the three pinned mutation sites)
    - .planning/phases/07-codex-parity/07-CONTEXT.md (D-07 planted-regression requirement, D-08)
  </read_first>
  <action>
    Create `07-MUTATION-LOG.md` with the 06-MUTATION-LOG header shape: title `# 07-MUTATION-LOG — Codex Parity`, phase, date, the scope sentence (every Phase 7 guard is positive-controlled here, D-00, rule `84d1gfpywd`), the family index — (a) 07-01 TOML splice, (b) 07-02 `--yes`/`--target`, (c) 07-03 picker footer, (d) 07-05 scope flip, (e) 07-06 shared AGENTS.md, (f) 07-07 Codex nudge runtime, (g) 07-08 Codex nudge lifecycle and CLI, (h) 07-10 capability-doc drift, (i) 07-11 MCP instructions budget — and the "Pre-mutation cleanliness gate" convention section copied in substance from 06 (git diff --quiet before plant = 0, before revert = 1, after revert = 0; GOTOOLCHAIN=go1.26.6; temp paths abbreviated).
    Then one section per family member, each with: Test/guard, "What are we testing, and why?", Pre-mutation gate, Mutation applied (the exact perl command and the diff), Observed failure (verbatim `--- FAIL:` lines with the `exit=` line appended), Pre-revert gate, Revert (`git checkout -- internal/agents/toml.go` then the clean gate), Green control:
    - `## Family (a1)` — D-07's planted regression: `perl -0pi -e 's/strings\.TrimLeft\(line, " \\t"\)/line/' internal/agents/toml.go` (column-0-only end scan) against `TestSpliceTOMLTable_MaintainerIndentedLayout` (show the got-bytes missing the context7/engram tables).
    - `## Family (a2)` — insert an early `return nil` after the `tomlTableConflict` signature line against `TestTOMLTableConflict` and `TestSpliceTOMLTable_ConflictLeavesContentUnchanged`.
    - `## Family (a3)` — insert an early `return "\n"` after the `tomlLineEnding` signature line against `TestSpliceTOMLTable_CRLFPreserved`.
    Record the D-08 window through the ledger verb (never a hand edit of WINDOWS.md): `gsd_run windows append --kind deviation --phase 07 --file internal/agents/toml.go --line <line of findTOMLTableRange> --description "Codex TOML splice data loss in released binaries: findTOMLTableRange ended a table only at a column-0 '[' header, so an indented [mcp_servers.codegraph] swallowed every following sibling table up to the next column-0 header on install/uninstall --target codex|all. Fixed on gsd/v0.14.0-milestone by 07-01 (fix(07-01) commits); no patch release by maintainer decision B2 (2026-09-19, 07-CONTEXT D-08). Stays open until the v0.14.0 release ships, then close with windows fixed."` (the gsd-tools launcher as resolved in the planner/executor bootstrap). Commit both files as `docs(07-01): Family (a) mutation log and the D-08 ledger row`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && git diff --quiet -- internal/agents/toml.go && test "$(rg -c -F 'strings.TrimLeft(line, " \t")' internal/agents/toml.go)" = "1" && perl -0pi -e 's/strings\.TrimLeft\(line, " \\t"\)/line/' internal/agents/toml.go && ! git diff --quiet -- internal/agents/toml.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestSpliceTOMLTable_MaintainerIndentedLayout$' -v > "$O/red-a1.txt" 2>&1; echo "exit=$?" >> "$O/red-a1.txt"); git checkout -- internal/agents/toml.go; git diff --quiet -- internal/agents/toml.go && rg -q -- '--- FAIL: TestSpliceTOMLTable_MaintainerIndentedLayout' "$O/red-a1.txt" && ! rg -q '^exit=0$' "$O/red-a1.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(a[1-3]\)' "$M")" = "3" && rg -q -- '--- FAIL: TestSpliceTOMLTable_MaintainerIndentedLayout' "$M" && rg -q -- '--- FAIL: TestTOMLTableConflict' "$M" && rg -q -- '--- FAIL: TestSpliceTOMLTable_CRLFPreserved' "$M" && rg -q 'Pre-mutation cleanliness gate' "$M" && test "$(rg -c 'Codex TOML splice data loss in released binaries' .planning/WINDOWS.md)" = "1" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 > "$O/pkg.txt" 2>&1; echo "exit=$?" >> "$O/pkg.txt") && rg -q '^exit=0$' "$O/pkg.txt" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-01\): Family \(a\)'</automated>
    <fails_when>non-zero exit; toml.go dirty before the plant or after the revert; the pinned TrimLeft site not present exactly once; the planted run exiting 0 or lacking the named FAIL line; fewer than 3 Family (a) sections or a missing FAIL excerpt in the log; the WINDOWS row absent or duplicated; the agents package not green after the revert; or no docs(07-01) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - each Family (a) section carries all of: Pre-mutation gate, Mutation applied, Observed failure, Pre-revert gate, Revert, Green control
    - the ledger row was appended by the verb (the SUMMARY pastes the verb's JSON `entry` output with its id)
    - `git diff --quiet -- internal/agents/toml.go` exits 0 at the end
  </acceptance_criteria>
  <done>Every TOML guard is proven able to fail against a confirmed-applied, byte-cleanly reverted mutation, and the released-binary window is on the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user's hand-edited Codex config.toml → codegraph's splice/strip | arbitrary third-party TOML codegraph edits in place |
| released binaries → users' machines | the unfixed scanner stays in circulation until the milestone ships (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | findTOMLTableRange end scan | high | mitigate | any-indent header recognition outside strings/arrays; maintainer-layout fixture RED-first; real-binary tracer; Family (a1) column-0 mutation proven RED |
| T-07-02 | Tampering | spliceTOMLTable on inline/dotted/quoted/duplicate codegraph forms | medium | mitigate | tomlTableConflict refuses and returns content unchanged (no duplicate key); Family (a2) |
| T-07-03 | Tampering | CRLF configs | low | mitigate | tomlLineEnding; CRLF round-trip test; Family (a3) |
| T-07-04 | Repudiation | released binaries carrying the bug | medium | accept | maintainer decision B2 (no patch release); WINDOWS deviation row via the verb; STATE.md warning already present; the fix lands first in this phase |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1` green
- the real binary preserves the maintainer-shaped fixture through install and uninstall
- 07-MUTATION-LOG.md has Families (a1)-(a3); WINDOWS row present; codex.go untouched since the plans commit
</verification>

<success_criteria>
- D-07 fully pinned in toml.go (indentation, strings/arrays, subtables, comments, CRLF, refusal)
- D-08 recorded through the ledger verb; no release or tag created
- 07-VALIDATION row 07-TOML satisfiable with `go test ./internal/agents/ -run 'TestSpliceTOMLTable|TestStripTOMLTable|TestFindTOMLTableRange' -count=1`
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-01-SUMMARY.md` when done — include both RED transcripts, the ledger row id, and the scanner's type/function names.
</output>
