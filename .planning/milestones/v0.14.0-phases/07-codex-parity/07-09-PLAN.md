---
phase: 07-codex-parity
plan: 09
type: execute
wave: 9
depends_on: ["07-08"]
files_modified:
  - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md
autonomous: false
requirements: [CODEX-05, CODEX-06, FIX-03]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The CODEX-05/06 sessions are run by the ORCHESTRATOR in a second, genuinely fresh scratch (`HOME=$S2/home`, `CODEX_HOME=$S2/home/.codex`, `auth.json` symlinked) with the HEAD binary's real installs — local with `--pretool-nudge` in an indexed and an un-indexed repo, global without it — never by an executor subagent (D-02, D-04)."
    - "Pass-bar lines L1 (post-flip, both scopes), L5, L6 and L7 are recorded against the D-06 table locked in 07-LIVE-SESSIONS.md before any session; `CODEX-06 verdict: PASS` requires L1-post, L5 and L7 PASS and `CODEX-05 live verdict: PASS` requires L6 and L7 PASS."
    - "L5 comes from a fresh `codex exec --json` session whose prompt never names codegraph; its evidence is the session JSONL (skill listed in the injected context, and a codegraph_explore MCP call or a `codegraph explore` shell run), with every grep/rg/find the agent ran logged as negative space."
    - "L6 is observed inside ONE session: the first matched Bash search fires exactly once, a matched call inside 60 s is silent, a matched call after at least 65 s fires again; the un-indexed repo, whose hooks are shown to run by a foreign probe hook, gets 0 fires; no hook error or block is attributable to codegraph's hook."
    - "A4 is settled from RAW PreToolUse stdin captured by a foreign probe hook during a subagent's Bash call; the adapter already handles either outcome (07-07), and a `no` is escalated for the maintainer to accept the per-session granularity that 07-10 documents."
    - "Before `/hooks` trust, Codex skips the newly installed hook (the docs statement CODEX-05 requires); D-23's position question — does removing codegraph's group re-flag a later foreign hook in `/hooks` — is recorded."
    - "The picker's tmux TTY-05 assertion is re-run after the scope flip and passes with executed=6 (D-25, FIX-03)."
    - statement: "CODEX-06 adjacency: the project and global codegraph entries share the name `codegraph` and Codex shows one merged entry (closest file wins), so 'both scopes' is evidenced by `codex mcp list --json` from two cwds plus the two config files, never by a scope field."
      verification: backstop
    - statement: "CODEX-06 empty: the un-indexed repo carrying the same local install gets 0 nudge fires while its hooks demonstrably run, and the uninstall leaves the scratch repo with no codegraph surface."
      verification: backstop
    - statement: "CODEX-06 ordering: L5 requires the skill listed AND at least one codegraph call in the same session, in either order; the observed order (skill/instructions first, or after a nudge fire) is recorded, not asserted."
      verification: backstop
  artifacts:
    - path: ".planning/phases/07-codex-parity/07-LIVE-SESSIONS.md"
      provides: "CODEX-05/06 section: scaffold, protocols, fire log, verdict lines, A4, D-23, tmux re-run"
      contains: "CODEX-06 verdict:"
  key_links:
    - from: "07-LIVE-SESSIONS.md `CODEX-06 verdict:`, `CODEX-05 live verdict:` and `A4 …` lines"
      to: "07-10-PLAN.md (verification column and the documented subagent granularity)"
      via: "precondition read by the docs plan"
      pattern: "^CODEX-06 verdict: PASS$"
  prohibitions:
    - statement: "MUST NOT run `codegraph install`/`uninstall --target codex|all` against the real HOME, print the real `~/.codex/config.toml` or `auth.json`, or use `--dangerously-bypass-hook-trust` before the TUI has proven the real `/hooks` trust path (D-02, D-04, D-19)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT record an absence (0 fires, no hook error, no skill) without first showing the same search find the thing when present, and MUST NOT patch code on a FAIL — record it and escalate (`Maintainer decision:`)."
      status: resolved
      verification: judgment
---

<objective>
Prove live, in a genuinely fresh Codex, what no unit test may claim (D-00): that a fresh session in an indexed
repo reaches for codegraph unprompted with the entry present at both scopes (CODEX-06), that Codex delivers
the nudge's added context once and then honours the 60 s per-key cooldown, stays silent un-indexed and never
errors or blocks (CODEX-05), what Codex's PreToolUse stdin carries for a subagent (A4), and how `/hooks`
trust behaves for a new hook and for position shifts (D-19, D-23). Re-run the picker's tmux assertion after
the scope flip (D-25). The executor scaffolds; the orchestrator drives Codex and writes verdicts; the executor
gates completeness.

Purpose: CODEX-06, CODEX-05's live half, FIX-03's post-flip re-run; 07-VALIDATION row `07-LIVE2`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md
@.planning/phases/07-codex-parity/07-04-SUMMARY.md
@.planning/phases/07-codex-parity/07-08-SUMMARY.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md
</context>

<interfaces>
Scratch: `S2=$(cd /tmp && pwd -P)/07-live2`; env `HOME=$S2/home CODEX_HOME=$S2/home/.codex` (plus
`XDG_CONFIG_HOME=$S2/home/.config` for codegraph). Repos `$S2/indexed` (gofixture + `codegraph init`), `$S2/unindexed`
(gofixture, no `.codegraph`), `$S2/bare` (gofixture, nothing installed). The pinned nudge substring for fire
counting: `returns the matching symbols' source and call paths`. Sentinels: `${TMPDIR}/codegraph-nudge-$(id -u)/`.
Codex transcripts: `$S2/home/.codex/sessions/YYYY/MM/DD/*.jsonl`. The installed hook command (local):
`"$(git rev-parse --show-toplevel)/.codex/hooks/codegraph-pretooluse.sh"`.
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan appends the CODEX-05/06 section to `07-LIVE-SESSIONS.md`.

<!-- planner-discipline-allow: PENDING -->

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the second fresh scratch with the real installs, the probe hook, the pre-flight and the CODEX-05/06 skeleton</name>
  <precondition>07-01..07-08 are committed; 07-LIVE-SESSIONS.md has `CODEX-01 verdict: PASS`; `command -v codex` and `command -v tmux` succeed; `test -e "$HOME/.codex/auth.json"`.</precondition>
  <files>.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md (the CODEX-01 section, its pass bar and pre-flight checksums)
    - .planning/phases/07-codex-parity/07-CONTEXT.md (D-02..D-06, D-19, D-21, D-23)
    - .planning/phases/06-claude-code-pretooluse-nudge/06-LIVE-SESSIONS.md (fire log and verdict style)
  </read_first>
  <action>
    Build, writing nothing under the real `$HOME`:
    - `S2=$(cd /tmp && pwd -P)/07-live2; rm -rf "$S2"; mkdir -p "$S2/home/.codex" "$S2/probes" "$S2/transcripts"`; `GOTOOLCHAIN=go1.26.6 go build -o "$S2/codegraph" ./cmd/codegraph`; `ln -s "$HOME/.codex/auth.json" "$S2/home/.codex/auth.json"`.
    - `indexed`, `unindexed`, `bare`: copy the gofixture, `git init -q`; `CODEGRAPH_NO_WATCH=1 "$S2/codegraph" init "$S2/indexed"`.
    - Real installs with the scratch env: in `indexed` and `unindexed` run `codegraph install --target codex --location local --pretool-nudge --yes`; once, from `bare`, `codegraph install --target codex --location global --yes` (no nudge globally). Save each report to `$S2/transcripts/`.
    - A4/D-23 probe: `$S2/probes/a4-probe.sh` (0755; copies stdin to `$S2/probes/a4-<date +%s>-$$.json`, prints nothing, exits 0) appended with `jq` as the LAST `PreToolUse` group of both repos' `.codex/hooks.json`: matcher `^Bash$`, one handler, `type` command, `timeout` 5, command the single-quoted absolute probe path (after codegraph's group).
    - Pre-flight (read-only): `codex --version`; the four real-HOME `shasum -a 256` lines (compare with the CODEX-01 pre-flight); `echo "$TMPDIR"`, `id -u`; `tmux -V`.
    Append to 07-LIVE-SESSIONS.md a `## CODEX-05 and CODEX-06 (after the scope flip and the nudge)` section: Method (D-02, D-04, the orchestrator runs it, JSONL evidence, fire counting by the pinned substring with a positive control first, attribution of errors by hook command), Pre-flight, Scaffold (commands + positive-assertion listing: both guards rendered with `$S2/codegraph`, one codegraph group then the probe group, `.codegraph` only in `indexed`, the global config holding the codegraph table), Protocol L1/L5 (Task 2), Protocol L6/A4/D-23/uninstall/tmux (Task 3), an empty Fire log table (key | fire timestamps | gaps), and `### CODEX-05/06 verdicts` with these 16 lines, each exactly once, each ending `: PENDING`:
    `L1 post-flip both scopes (codegraph-installed)`, `L5 fresh session reaches for codegraph unprompted`, `L6 nudge fires once then cools down; un-indexed 0 fires`, `L7 real HOME unchanged (CODEX-05/06)`, `Untrusted hook skipped before /hooks trust`, `A4 PreToolUse stdin carries a subagent id`, `tool_input.command type`, `D-23 removing our group re-flags later foreign hooks`, `Negative space (grep/rg/find runs in the L5 session)`, `Matched Bash search calls`, `Fires`, `Uptake`, `Uninstall leaves the scratch repo clean`, `Picker tmux re-run after flip`, `CODEX-05 live verdict`, `CODEX-06 verdict`.
    Commit `docs(07-09): scaffold CODEX-05/06 live evidence`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S2=$(cd /tmp && pwd -P)/07-live2 && D=.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md && test -x "$S2/codegraph" && test -L "$S2/home/.codex/auth.json" && test -d "$S2/indexed/.codegraph" && test ! -e "$S2/unindexed/.codegraph" && test -x "$S2/indexed/.codex/hooks/codegraph-pretooluse.sh" && test -x "$S2/unindexed/.codex/hooks/codegraph-pretooluse.sh" && rg -q "^codegraph_bin='.*/07-live2/codegraph'$" "$S2/indexed/.codex/hooks/codegraph-pretooluse.sh" && test "$(jq -r '.hooks.PreToolUse[0].hooks[0].command' "$S2/indexed/.codex/hooks.json")" = '"$(git rev-parse --show-toplevel)/.codex/hooks/codegraph-pretooluse.sh"' && test "$(jq '.hooks.PreToolUse | length' "$S2/indexed/.codex/hooks.json")" = "2" && jq -e '.hooks.PreToolUse[1].hooks[0].command | test("a4-probe\\.sh")' "$S2/indexed/.codex/hooks.json" > /dev/null && test "$(rg -c '^\[mcp_servers\.codegraph\]$' "$S2/home/.codex/config.toml")" = "1" && test ! -e "$S2/home/.codex/hooks.json" && test ! -e "$S2/bare/.codex" && test "$(rg -c ': PENDING$' "$D")" = "16" && rg -q '^CODEX-06 verdict: PENDING$' "$D" && rg -q '^CODEX-05 live verdict: PENDING$' "$D" && rg -q '^CODEX-01 verdict: PASS$' "$D" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-09\): scaffold CODEX-05/06'</automated>
    <fails_when>non-zero exit; the scratch binary or auth symlink missing; `.codegraph` wrong in either repo; either guard missing, non-executable or not rendered with the scratch binary; the first group's command not the exact local form, not exactly two groups, or the probe not last; the global config lacking exactly one codegraph table or a global hooks.json present; bare carrying `.codex`; not exactly 16 PENDING lines; either verdict line missing; the CODEX-01 verdict not PASS; or no scaffold commit</fails_when>
  </verify>
  <acceptance_criteria>
    - nothing under the real `$HOME` was written; the four checksums match the CODEX-01 pre-flight values
    - each of the 16 PENDING lines appears exactly once
  </acceptance_criteria>
  <done>A second fresh scratch with the real post-flip installs, a stdin probe and a skeleton with fixed verdict lines is ready.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: ORCHESTRATOR — trust, the untrusted-hook check, L1 (both scopes), L5 uptake with its negative space</name>
  <precondition>Task 1's scaffold exists at `$(cd /tmp && pwd -P)/07-live2`; the orchestrating session has Herdr control.</precondition>
  <files>.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md (both sections)
  </read_first>
  <action>The orchestrator (not an executor subagent) trusts the two repos through the real TUI, checks that a not-yet-trusted hook is skipped, records L1 at both scopes and runs the fresh L5 session, writing 5 of the 16 lines with verbatim excerpts.</action>
  <instructions>
    Env for every command: `HOME=$S2/home CODEX_HOME=$S2/home/.codex`; clear stale sentinels first: `rm -rf "${TMPDIR}/codegraph-nudge-$(id -u)"`.
    1. In a sibling Herdr pane start the Codex TUI in `$S2/indexed`; accept the folder trust prompt; DO NOT open `/hooks` yet; quit. Same for `$S2/unindexed`.
    2. Untrusted-hook check: `codex exec --json -C "$S2/indexed" 'Run exactly this shell command and nothing else: rg -n Alpha .'` → `Untrusted hook skipped before /hooks trust: yes` when neither the pinned substring appears in that session's JSONL nor any `$S2/probes/a4-*.json` was written for the turn (the probe is the positive control that would show a hook ran); record what Codex reported about untrusted hooks.
    3. Reopen the TUI in each repo, run `/hooks`, trust codegraph's group and the probe group (record what `/hooks` shows for each).
    4. L1 post-flip: `codex mcp list --json` in `$S2/bare` (global layer only) and in `$S2/indexed` (project layer, trusted); show `[mcp_servers.codegraph]` in both `$S2/home/.codex/config.toml` and `$S2/indexed/.codex/config.toml` → `L1 post-flip both scopes (codegraph-installed): PASS|FAIL (…)`.
    5. L5: `codex exec --json -C "$S2/indexed" 'Where is the function Alpha defined in this repository, and what calls it? Give file:line references.'` (the prompt never names codegraph). From the session JSONL: the injected skill list includes `codegraph` (quote it), and the agent made a call to the codegraph MCP server's `codegraph_explore` tool (quote Codex's own tool-name form) or ran `codegraph explore` → `L5 fresh session reaches for codegraph unprompted: PASS|FAIL (…)`. First show the searches find the thing when present (e.g. the skill name in the injected context of the step-2 session).
    6. `Negative space (grep/rg/find runs in the L5 session): <n> — <each command>`; `Uptake: <what the agent did first, and whether a nudge fire preceded the codegraph call>`.
    ESCALATE (patch nothing) on any FAIL: record `Maintainer decision: …` under it.
  </instructions>
  <verification>The five lines of this task carry no PENDING and each has an excerpt; the absence searches are positive-controlled.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md && rg -q '^L1 post-flip both scopes \(codegraph-installed\): (PASS|FAIL \(.+\))$' "$D" && rg -q '^L5 fresh session reaches for codegraph unprompted: (PASS|FAIL \(.+\))$' "$D" && rg -q '^Untrusted hook skipped before /hooks trust: (yes|no|not observed)( \(.+\))?$' "$D" && rg -q '^Negative space \(grep/rg/find runs in the L5 session\): [0-9]+' "$D" && rg -q '^Uptake: .+$' "$D" && ! rg -q '^(L1 post-flip|L5 |Untrusted hook skipped|Negative space|Uptake).*: PENDING$' "$D" && if rg -q '^L(1 post-flip|5 )[^:]+: FAIL' "$D"; then rg -q '^Maintainer decision: ' "$D"; fi</automated>
    <fails_when>non-zero exit; the L1-post, L5, untrusted-hook, negative-space or uptake line missing, malformed or still PENDING; or a FAIL without a Maintainer decision line</fails_when>
  </verify>
  <acceptance_criteria>
    - the L5 evidence quotes the JSONL (skill listing and the tool call), not the model's summary
    - the untrusted-hook claim quotes the probe's absence alongside a later probe file that proves hooks run once trusted
  </acceptance_criteria>
  <resume-signal>Reply "L1post=PASS L5=PASS untrusted-skipped=yes neg=<n>" once written.</resume-signal>
  <done>CODEX-06's uptake and both-scopes evidence, and Codex's untrusted-hook behaviour, are recorded against the locked bar.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: ORCHESTRATOR — L6 fire/cooldown and the un-indexed control, A4 from raw stdin, D-23, uninstall, the tmux re-run, L7 and the verdicts; then the completeness gate</name>
  <precondition>Task 2 has been resumed with its five lines written.</precondition>
  <files>.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md
  </read_first>
  <action>The orchestrator runs the timed nudge protocol in one TUI session, the un-indexed control, the subagent stdin probe, the D-23 position check, the uninstall, the post-flip tmux run and the real-HOME check; writes the remaining 11 lines; the executor then gates completeness and commits.</action>
  <instructions>
    1. L6 in ONE session (the Herdr TUI in `$S2/indexed`, noting wall-clock times): prompt `Run exactly this shell command and nothing else: rg -n Alpha .` → expect one fire; immediately `Run exactly this shell command and nothing else: grep -rn Beta .` → expect none; after at least 65 s (`sleep 65` in the orchestrator's shell) `Run exactly this shell command and nothing else: find . -name '*.go'` → expect one fire. Count fires by the pinned substring in the session JSONL (positive control: the first fire), fill the Fire log table, then the un-indexed control in `$S2/unindexed` (`rg -n Alpha .`): 0 fires while a new `$S2/probes/a4-*.json` proves the hooks ran. Search both JSONLs and the TUI output for a hook error or block naming `codegraph-pretooluse.sh` (positive control: its command string in the hook listing). → `L6 …: PASS|FAIL (…)`, `Matched Bash search calls: <n>`, `Fires: <n>`.
    2. A4: in the indexed TUI, `Use a subagent to run exactly this shell command and report its output: rg -n Gamma .`; inspect the probe's raw stdin files for the subagent's call → `A4 PreToolUse stdin carries a subagent id: yes (<field name and value shape>)|no|not observed (<reason>)`; `tool_input.command type: string|array (<excerpt>)`. If A4 is `no` or `not observed`, ESCALATE: record `Maintainer decision: …` (accepting per-session cooldown granularity for Codex subagents, which 07-10 documents) — the adapter already keys on `session_id` + `main` in that case.
    3. D-23: `codegraph install --target codex --location local --pretool-nudge=false --yes` in `$S2/indexed` (the probe group becomes first); reopen `/hooks` → `D-23 removing our group re-flags later foreign hooks: yes|no (<what /hooks showed>)`.
    4. Uninstall: `codegraph uninstall --target codex --location local --yes` in `$S2/indexed` and `--location global` → `Uninstall leaves the scratch repo clean: PASS|FAIL (…)` (no codegraph table, no AGENTS.md block, no shared skill, no codegraph group or guard; the probe group left intact).
    5. `GOTOOLCHAIN=go1.26.6 task test:tmux` at HEAD → `Picker tmux re-run after flip: PASS (executed=6)` or `FAIL (…)`.
    6. L7: re-run the four `shasum -a 256` lines → `L7 real HOME unchanged (CODEX-05/06): PASS` only when all equal the CODEX-01 pre-flight.
    7. `CODEX-05 live verdict: PASS` only if L6 and L7 are PASS; `CODEX-06 verdict: PASS` only if L1-post, L5 and L7 are PASS; otherwise FAIL plus `Maintainer decision:`.
    After resume the executor runs the completeness gate below and commits `docs(07-09): record CODEX-05/06 live evidence`.
  </instructions>
  <verification>No PENDING anywhere in the document; every line in its allowed form; verdicts consistent with their L-lines; real HOME unchanged; tmux executed=6.</verification>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && D=.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md && ! rg -q 'PENDING' "$D" && rg -q '^L6 nudge fires once then cools down; un-indexed 0 fires: (PASS|FAIL \(.+\))$' "$D" && rg -q '^L7 real HOME unchanged \(CODEX-05/06\): PASS$' "$D" && rg -q '^A4 PreToolUse stdin carries a subagent id: (yes|no|not observed)( \(.+\))?$' "$D" && rg -q '^tool_input\.command type: (string|array)( \(.+\))?$' "$D" && rg -q '^D-23 removing our group re-flags later foreign hooks: (yes|no|not observed)( \(.+\))?$' "$D" && rg -q '^Matched Bash search calls: [0-9]+$' "$D" && rg -q '^Fires: [0-9]+$' "$D" && rg -q '^Uninstall leaves the scratch repo clean: (PASS|FAIL \(.+\))$' "$D" && rg -q '^Picker tmux re-run after flip: PASS \(executed=6\)$' "$D" && rg -q '^CODEX-05 live verdict: (PASS|FAIL)$' "$D" && rg -q '^CODEX-06 verdict: (PASS|FAIL)$' "$D" && if rg -q '^CODEX-05 live verdict: PASS$' "$D"; then rg -q '^L6 [^:]+: PASS$' "$D"; else rg -q '^Maintainer decision: ' "$D"; fi && if rg -q '^CODEX-06 verdict: PASS$' "$D"; then rg -q '^L1 post-flip [^:]+: PASS$' "$D" && rg -q '^L5 [^:]+: PASS$' "$D"; else rg -q '^Maintainer decision: ' "$D"; fi && if rg -q '^A4 PreToolUse stdin carries a subagent id: (no|not observed)' "$D"; then rg -q '^Maintainer decision: ' "$D"; fi && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-09\): record CODEX-05/06 live evidence'</automated>
    <fails_when>non-zero exit; any PENDING left; any of the L6, L7, A4, command-type, D-23, metric, uninstall, tmux or verdict lines missing or outside its allowed form; L7 not PASS; the tmux line not PASS with executed=6; a PASS verdict inconsistent with its L-lines; a FAIL verdict or an A4 no/not-observed without a Maintainer decision line; or no evidence commit</fails_when>
  </verify>
  <acceptance_criteria>
    - the Fire log lists every fire with its timestamp and key, and every same-key gap
    - every zero-count claim quotes the positive-control search that found the same thing when present
  </acceptance_criteria>
  <resume-signal>Reply "L6=PASS fires=<n>/<m> A4=<yes|no> D-23=<yes|no> tmux=PASS L7=PASS CODEX-05=PASS CODEX-06=PASS" (or the FAIL lines plus the maintainer decision) once written.</resume-signal>
  <done>Codex's delivery, cooldown, silence, subagent stdin and trust behaviour are live evidence against the locked bar, the picker passes under real tmux after the flip, and the real HOME is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scratch Codex → the maintainer's real config | a wrong HOME would let installs or hooks touch real files |
| live model sessions → evidence | a summary could overstate delivery or uptake |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-27 | Tampering | real ~/.codex and ~/.agents files | medium | mitigate | scratch HOME/CODEX_HOME on every command; L7 checksums against the CODEX-01 pre-flight |
| T-07-28 | Spoofing (of evidence) | L5/L6 verdicts | medium | mitigate | JSONL excerpts; positive-controlled absence searches; a probe hook proving hooks ran in the un-indexed control; pass bar locked before any session |
| T-07-29 | Information Disclosure | transcripts in the repo | low | accept | gofixture scratch repos only; the orchestrator redacts unrelated paths or tokens before pasting |
| T-07-30 | Elevation of Privilege | hook-trust bypass in scripted runs | low | mitigate | the real `/hooks` trust path is exercised first in the TUI; the bypass flag is never needed for the recorded runs |
</threat_model>

<verification>
- 07-LIVE-SESSIONS.md complete (both sections, no PENDING); CODEX-05/06 verdicts recorded; tmux executed=6 after the flip
</verification>

<success_criteria>
- CODEX-06: a fresh Codex session reaches for codegraph unprompted, the entry shows at both scopes
- CODEX-05 live: one fire, 60 s cooldown, un-indexed silence, no hook error or block; A4 and D-23 recorded
- FIX-03: TTY-05 re-run after CODEX-02
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-09-SUMMARY.md` when done — every verdict line, A4 and D-23 outcomes, and what they mean for 07-10's documentation.
</output>
