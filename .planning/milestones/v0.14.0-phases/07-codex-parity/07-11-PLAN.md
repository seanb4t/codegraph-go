---
phase: 07-codex-parity
plan: 11
type: execute
wave: 11
depends_on: ["07-10"]
files_modified:
  - internal/mcp/server.go
  - internal/mcp/instructions_contract_test.go
  - testdata/wireoracle/transcripts/
  - internal/agents/instructions.go
  - internal/agents/shared.go
  - internal/agents/registry_test.go
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
autonomous: true
requirements: [AGENT-14]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The MCP `instructions` const carries a harness-neutral skill sentence that is true for the 7 skill-receiving targets (every target but Hermes), contains the `codegraph skill` anchor, names no single harness, and ends at or before byte 512; the whole const stays at most 600 bytes, single-line and pure ASCII (D-29; re-measured, 07-RESEARCH A5)."
    - "WIRE-03 is updated, not weakened: `TestInstructionsSkillSentenceWithinFirst512Bytes` pins the 512-byte position and the harness-neutral wording, `TestInstructionsSkillClaimIsResolvable` still resolves the anchor against the embedded SKILL.md, and the skill anchor's doc comment describes the 7-of-8 reality (D-29)."
    - "The wire transcripts that embed the instructions string — counted before the edit, expected 38 of the 42 goldens — are re-frozen in ONE reviewed commit through the sanctioned capture command; in that commit every changed transcript changes exactly one line and every changed line carries the instructions string; `go test ./test/wireoracle/...` is green (D-29, research Open Question 3)."
    - "The installed marker-block text (`codegraphInstructionsBlock`) is byte-identical to its value at the start of the phase, so no user's block is rewritten; only the stale comments at instructions.go:13-28, shared.go:741-746 and registry_test.go's blockNamesUnshippedCapability doc/message change (D-30)."
    - "Family (i) in 07-MUTATION-LOG.md: a planted padding that pushes the skill sentence past byte 512 turns the budget guard RED, and a stale (pre-freeze) transcript turns the wire oracle RED, each reverted byte-clean."
    - "The phase gate is green: build, the module suite minus internal/daemon, internal/daemon alone, `task docs:cli:drift`, the mutation log's 28 families (a1-a3, b1-b2, c1-c3, d1-d4, e1-e3, f1-f5, g1-g4, h1-h2, i1-i2), 07-LIVE-SESSIONS.md with no PENDING and CODEX-01/05/06 verdicts PASS, and no `[ci skip]`/`[skip ci]` in any Phase 7 commit."
  artifacts:
    - path: "internal/mcp/server.go"
      provides: "the rewritten instructions const"
      contains: "codegraph skill"
    - path: "internal/mcp/instructions_contract_test.go"
      provides: "TestInstructionsSkillSentenceWithinFirst512Bytes; updated skillAnchor comment and transcript count"
      contains: "TestInstructionsSkillSentenceWithinFirst512Bytes"
  key_links:
    - from: "internal/mcp/server.go instructions"
      to: "testdata/wireoracle/transcripts/*.golden"
      via: "JSON-encoded into every initialize/discover result, re-frozen by the capture command"
      pattern: "codegraph indexes this repository"
  prohibitions:
    - statement: "MUST NOT change the installed marker-block text, and MUST NOT hand-edit a wire transcript — re-freeze only through the capture command in one reviewed diff (D-29, D-30)."
      status: resolved
      verification: test
    - statement: "MUST NOT claim the skill for Hermes, or scope the sentence to one harness, in the instructions every MCP client receives (D-29)."
      status: resolved
      verification: judgment
---

<objective>
Make the MCP `instructions` skill sentence true for what now ships (D-29), re-freeze the wire transcripts that
carry it in one reviewed diff, correct the stale "4 of 8"/"only Claude Code gets the skill" comments without
touching the installed block (D-30), prove the budget guard can fail, and run the phase gate.

Purpose: AGENT-14's "the instructions.go '4 of 8' comment and the MCP instructions skill sentence updated to
match what ships"; 07-VALIDATION row `07-DOCS` (instructions half); the phase close.

Output: `test(07-11):`/`feat(07-11):` commits, one `test(07-11): re-freeze …` transcript commit, a `docs(07-11):`
comment commit, Family (i), the phase-gate record in the SUMMARY.

Notes: re-measure every byte figure after the edit (07-RESEARCH A5 — the 554/490 figures are pre-rewrite).
Count the transcripts that carry the instructions string BEFORE editing and record the number (research Open
Question 3: 42 goldens on disk; comments say 38 and 24). Transcripts are re-frozen only with
`go run ./test/wireoracle/cmd/wireoracle -bin <HEAD binary> -fixture testdata/wireoracle/fixture -scenario <name> > testdata/wireoracle/transcripts/<name>.golden`
(the human-redirect path the command's doc comment sanctions); if a re-capture differs from the old golden in any
line other than the instructions line (the known `toolslist-repeat` arrival-order variance the oracle canonicalizes
at compare time), re-capture that scenario — never hand-edit. `GOTOOLCHAIN=go1.26.6`; commits end with
`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never `[ci skip]`; never gate on `gsd-tools check tdd-red-evidence`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-RESEARCH.md
@.planning/phases/06-claude-code-pretooluse-nudge/06-07-PLAN.md
@internal/mcp/server.go
@internal/mcp/instructions_contract_test.go
@internal/agents/instructions.go
@internal/agents/shared.go
@internal/agents/registry_test.go
@test/wireoracle/cmd/wireoracle/main.go
@test/wireoracle/oracle_test.go
</context>

<interfaces>
- `const instructions` (internal/mcp/server.go:57): one line, currently 554 bytes, ending
  `…Call resources/list for tool-by-tool reference docs; in Claude Code, codegraph install also adds the codegraph skill.`
- WIRE-03 in instructions_contract_test.go: `instructionsMaxBytes = 600`, `resourcesAnchor = "resources/list"`,
  `skillAnchor = "codegraph skill"`, `skillClaimResolves`, `TestInstructionsStaysWithinWireBudget`,
  `TestInstructionsCarriesNoWireContractViolation`, `TestInstructionsReachesTheWireVerbatim`,
  `TestInstructionsClaimGuardsAreNotVacuous`.
- Skill-receiving targets after this phase (07-10's table): Antigravity, Claude Code, Codex CLI, Cursor, Gemini CLI,
  Kiro, opencode — every target but Hermes.
- A suggested sentence (wording is the executor's): `codegraph install also adds the codegraph skill for every agent it configures except Hermes.` placed directly after the first sentence.
- Every transcript embeds the instructions on exactly one line (one initialize/discover result per transcript).
- The phase's plans commit is the one that added `.planning/phases/07-codex-parity/07-01-PLAN.md`.
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan adds the rewritten `instructions` const,
`TestInstructionsSkillSentenceWithinFirst512Bytes`, the re-frozen transcripts, the D-30 comments and Family (i).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A failing 512-byte/harness-neutral guard, the harness-neutral skill sentence, and the one-diff transcript re-freeze</name>
  <files>internal/mcp/instructions_contract_test.go, internal/mcp/server.go, testdata/wireoracle/transcripts/</files>
  <read_first>
    - internal/mcp/server.go (the instructions const and its doc comment, :42-57)
    - internal/mcp/instructions_contract_test.go (the WIRE-03 constants, comments naming 38 and 24 transcripts, the budget test)
    - test/wireoracle/cmd/wireoracle/main.go and test/wireoracle/oracle_test.go (capture path, fixture path, scenario ↔ golden naming)
    - docs/AGENT-CAPABILITIES.md (which targets receive the skill)
  </read_first>
  <behavior>
    - TestInstructionsSkillSentenceWithinFirst512Bytes: the byte offset just past the `.` ending the sentence that contains `codegraph skill` is at most 512; the const does not contain `Claude Code`; it contains `Hermes` only as the stated exception (or no harness name at all).
  </behavior>
  <action>
    Before editing, record: `rg -l -F 'in Claude Code, codegraph install also adds the codegraph skill' testdata/wireoracle/transcripts | wc -l` (expected 38) and the list of those scenario names, plus the total `ls testdata/wireoracle/transcripts/*.golden | wc -l` (expected 42).
    RED commit `test(07-11): pin the skill sentence inside the first 512 bytes` — the new test fails on the current const (its skill sentence ends at byte 554).
    GREEN commit `feat(07-11): harness-neutral MCP skill sentence`: rewrite the const so the skill sentence is harness-neutral, true for the 7 skill-receiving targets, and sits directly after the first sentence; keep every other sentence's meaning (the eight-tools default, the empty-list meaning, `CODEGRAPH_MCP_TOOLS`, `resources/list`). Update the server.go doc comment if it describes the old scoping, the `skillAnchor` doc comment (7 of 8 targets receive the skill; Hermes does not), and the stale transcript counts in the test comments to the measured number. Re-measure and record `len(instructions)` and the skill-sentence end offset in the SUMMARY.
    Re-freeze commit `test(07-11): re-freeze the wire transcripts for the new instructions`: build the HEAD binary, then for each recorded scenario name run the capture command in the objective. Confirm `go test ./test/wireoracle/... -count=1` passes and the diff meets the acceptance criteria below.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-11\): pin the skill sentence' && printf '%s\n' "$S" | rg -q '^feat\(07-11\): ' && printf '%s\n' "$S" | rg -q '^test\(07-11\): re-freeze the wire transcripts' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/mcp)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/mcp/ && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/mcp/ -count=1 -run 'TestInstructions' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -3 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c '^--- PASS: (TestInstructionsSkillSentenceWithinFirst512Bytes|TestInstructionsStaysWithinWireBudget|TestInstructionsSkillClaimIsResolvable|TestInstructionsCarriesNoWireContractViolation|TestInstructionsReachesTheWireVerbatim|TestInstructionsClaimGuardsAreNotVacuous) ' "$O/t1.txt")" = "6" && ! rg -q -F 'in Claude Code, codegraph install also adds' internal/mcp/server.go && test "$(rg -l -F 'in Claude Code, codegraph install also adds' testdata/wireoracle/transcripts | wc -l | tr -d ' ')" = "0" && L=$(git log --format='%H %s' -20) && RF=$(printf '%s\n' "$L" | rg ' test\(07-11\): re-freeze the wire transcripts' | cut -d' ' -f1) && test -n "$RF" && NS=$(git show --numstat --format= "$RF" -- testdata/wireoracle/transcripts) && test -n "$NS" && test "$(printf '%s\n' "$NS" | awk '$1 != 1 || $2 != 1' | wc -l | tr -d ' ')" = "0" && test "$(printf '%s\n' "$NS" | wc -l | tr -d ' ')" -ge 30 && DF=$(git show -U0 --format= "$RF" -- testdata/wireoracle/transcripts) && test "$(printf '%s\n' "$DF" | rg '^[-+][^-+]' | rg -v 'codegraph indexes this repository' | wc -l | tr -d ' ')" = "0" && (GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1 > "$O/wire.txt" 2>&1; echo "exit=$?" >> "$O/wire.txt") && rg -q '^exit=0$' "$O/wire.txt"</automated>
    <fails_when>non-zero exit; any of the three commit subjects missing; build/gofmt/vet failure; any FAIL line or fewer than 6 named instructions PASS lines; the old Claude-Code-scoped sentence still in server.go or in any transcript; the re-freeze commit absent, touching a transcript by anything other than exactly one line out and one line in, touching fewer than 30 transcripts, or changing a line that does not carry the instructions string; or the wire oracle not green</fails_when>
  </verify>
  <acceptance_criteria>
    - the number of transcripts in the re-freeze commit equals the pre-edit count recorded in the SUMMARY (expected 38)
    - `len(instructions) <= 600` and the skill sentence's end offset `<= 512`, both re-measured numbers pasted in the SUMMARY
    - the SUMMARY pastes the RED `--- FAIL: TestInstructionsSkillSentenceWithinFirst512Bytes` line
  </acceptance_criteria>
  <done>Every MCP client is told a true, harness-neutral skill sentence within Codex's 512-byte window, and the wire contract is re-frozen in one reviewed diff.</done>
</task>

<task type="auto">
  <name>Task 2: D-30 comments and Family (i)</name>
  <files>internal/agents/instructions.go, internal/agents/shared.go, internal/agents/registry_test.go, .planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - internal/agents/instructions.go (:13-28 doc comment; the codegraphInstructionsBlock const that must not change)
    - internal/agents/shared.go (upsertInstructionsEntry doc comment :741-746)
    - internal/agents/registry_test.go (blockNamesUnshippedCapability doc comment and its skill-row error message, :190-215)
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
  </read_first>
  <action>
    Comment-only commit `docs(07-11): correct the instructions-block comments for what ships`:
    - instructions.go:13-28: the block goes to the 4 of 8 targets that declare an instructions file (Claude, Codex, opencode, Gemini); Codex and opencode share the repo-root AGENTS.md at local scope (instructionsRequestedElsewhere); the block still names no skill on purpose — the skill package now reaches 7 of 8 targets (all but Hermes) and is announced by the MCP instructions const, while keeping the installed block text unchanged means no user's block is rewritten.
    - shared.go upsertInstructionsEntry comment: the same 4-of-8 statement plus the shared AGENTS.md note.
    - registry_test.go: blockNamesUnshippedCapability's doc comment and its skill-row error text describe the real reason the block stays skill-agnostic (the block text is frozen so installed blocks are never rewritten; the skill is announced elsewhere) — the checker's logic is unchanged.
    Then append Family (i) in the established shape, each reverted byte-clean with a green control, and commit `docs(07-11): Family (i) mutation log`:
    - `## Family (i1)` — `perl -pi -e 's/^(const instructions = ")/$1 . ("padding " x 70)/e' internal/mcp/server.go` → `TestInstructionsSkillSentenceWithinFirst512Bytes` (and the 600-byte budget test) RED.
    - `## Family (i2)` — one re-frozen transcript restored to its pre-freeze bytes (`git show <re-freeze sha>^:testdata/wireoracle/transcripts/call-callers.golden > testdata/wireoracle/transcripts/call-callers.golden`) → the wire oracle's `call-callers` scenario RED.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && L=$(git log --diff-filter=A --format=%H -- .planning/phases/07-codex-parity/07-01-PLAN.md) && B=$(printf "%s\n" "$L" | tail -1) && test -n "$B" && OLD=$(git show "$B":internal/agents/instructions.go) && test "$(printf '%s\n' "$OLD" | sed -n '/^const codegraphInstructionsBlock = /,/codegraphSectionEnd$/p')" = "$(sed -n '/^const codegraphInstructionsBlock = /,/codegraphSectionEnd$/p' internal/agents/instructions.go)" && ! rg -q 'only Claude Code receives the embedded skill package' internal/agents/instructions.go && git diff --quiet -- internal/mcp/server.go && perl -pi -e 's/^(const instructions = ")/$1 . ("padding " x 70)/e' internal/mcp/server.go && ! git diff --quiet -- internal/mcp/server.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/mcp/ -count=1 -run 'TestInstructionsSkillSentenceWithinFirst512Bytes$' -v > "$O/red-i1.txt" 2>&1; echo "exit=$?" >> "$O/red-i1.txt"); git checkout -- internal/mcp/server.go; git diff --quiet -- internal/mcp/server.go && rg -q -- '--- FAIL: TestInstructionsSkillSentenceWithinFirst512Bytes' "$O/red-i1.txt" && ! rg -q '^exit=0$' "$O/red-i1.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(i[12]\)' "$M")" = "2" && rg -q -- '--- FAIL: TestInstructionsSkillSentenceWithinFirst512Bytes' "$M" && rg -q -- '--- FAIL: .*call-callers' "$M" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/mcp/ ./test/wireoracle/... -count=1 > "$O/pkgs.txt" 2>&1; echo "exit=$?" >> "$O/pkgs.txt") && rg -q '^exit=0$' "$O/pkgs.txt" && S=$(git log --format=%s -10) && printf '%s\n' "$S" | rg -q '^docs\(07-11\): correct the instructions-block comments' && printf '%s\n' "$S" | rg -q '^docs\(07-11\): Family \(i\)'</automated>
    <fails_when>non-zero exit; the installed marker-block const differing from its phase-start text; the stale "only Claude Code receives" comment still present; server.go dirty before the plant or after the revert; the planted run exiting 0 or lacking the FAIL line; fewer than 2 Family (i) sections or either FAIL excerpt missing; the agents/mcp/wireoracle packages not green; or either docs(07-11) commit missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --stat` of the comment commit lists only instructions.go, shared.go and registry_test.go, and every changed line in it is a comment line or the checker's error-message string
    - `go test ./internal/agents/ -run 'TestInstructionsBlock' -count=1` passes (the checker unchanged)
  </acceptance_criteria>
  <done>The stale comments match what ships, the installed block is untouched, and both new guards are proven able to fail.</done>
</task>

<task type="auto">
  <name>Task 3: The Phase 7 gate</name>
  <files>.planning/phases/07-codex-parity/07-11-SUMMARY.md</files>
  <read_first>
    - .planning/phases/06-claude-code-pretooluse-nudge/06-07-PLAN.md and 06-07-SUMMARY.md (the phase-gate shape and its deviations)
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md, 07-LIVE-SESSIONS.md
  </read_first>
  <action>
    Run the gate below and record every exit code and count in the SUMMARY (a table like 06-07's): build; the module suite minus internal/daemon (≥ 53 `ok` packages, none FAIL); internal/daemon alone (WINDOWS #37 is a known load flake — report a failure separately and re-run it alone once, never widen a timeout); `task docs:cli:drift`; the 28 mutation families; the live evidence verdicts; the no-`[ci skip]` check over every commit since the plans commit; and that the D-08 WINDOWS row is still open (it closes when the release ships, via `windows fixed`). Advisories for the maintainer go in the SUMMARY: the CODEX_HOME limitation, the subagent granularity per A4, D-23's outcome, `codegraph upgrade` not refreshing Codex's guard, the D-17 truncation, and anything the live sessions surfaced. No source change is expected; if the gate finds one, stop and report instead of patching.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build ./... && O=$(mktemp -d) && PK=$(GOTOOLCHAIN=go1.26.6 go list ./...) && (GOTOOLCHAIN=go1.26.6 go test -count=1 $(printf '%s\n' "$PK" | rg -v 'internal/daemon$') > "$O/suite.txt" 2>&1; echo "exit=$?" >> "$O/suite.txt") && rg -q '^exit=0$' "$O/suite.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/suite.txt" && test "$(rg -c '^ok\s' "$O/suite.txt")" -ge 53 && (GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/daemon/ > "$O/daemon.txt" 2>&1; echo "exit=$?" >> "$O/daemon.txt") && rg -q '^exit=0$' "$O/daemon.txt" && (GOTOOLCHAIN=go1.26.6 task docs:cli:drift > "$O/drift.txt" 2>&1; echo "exit=$?" >> "$O/drift.txt") && rg -q '^exit=0$' "$O/drift.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(([a][1-3]|b[12]|c[1-3]|d[1-4]|e[1-3]|f[1-5]|g[1-4]|h[12]|i[12])\)' "$M")" = "28" && LV=.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md && ! rg -q 'PENDING' "$LV" && rg -q '^CODEX-01 verdict: PASS$' "$LV" && rg -q '^CODEX-05 live verdict: PASS$' "$LV" && rg -q '^CODEX-06 verdict: PASS$' "$LV" && rg -q '^Picker tmux re-run after flip: PASS \(executed=6\)$' "$LV" && L=$(git log --diff-filter=A --format=%H -- .planning/phases/07-codex-parity/07-01-PLAN.md) && B=$(printf "%s\n" "$L" | tail -1) && test -n "$B" && MSGS=$(git log --format=%B "$B"..HEAD) && ! printf '%s\n' "$MSGS" | rg -q '\[(ci skip|skip ci)\]' && rg -q 'Codex TOML splice data loss in released binaries' .planning/WINDOWS.md && echo "phase gate green"</automated>
    <fails_when>non-zero exit; a build failure; the suite (minus daemon) not exit 0, carrying a FAIL line, or fewer than 53 ok packages; internal/daemon not exit 0 on its own; docs:cli:drift not exit 0; not exactly 28 mutation families; any PENDING in the live evidence or a CODEX-01/05/06 verdict or the tmux re-run not PASS; a [ci skip]/[skip ci] in any Phase 7 commit message; or the D-08 WINDOWS row missing</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY's gate table lists every command above with its exit code and count
    - the SUMMARY lists the maintainer advisories named in the action
  </acceptance_criteria>
  <done>Phase 7 is gated green and every requirement (CODEX-01…06, FIX-03, AGENT-14) has its evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server → every client session | the instructions string ships to every agent on every session |
| frozen transcripts → future regressions | a careless re-freeze could hide a real wire change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-34 | Information Disclosure | instructions const | medium | mitigate | stays a compile-time ASCII literal with no host path (TestInstructionsCarriesNoWireContractViolation) |
| T-07-35 | Repudiation | a skill claim that is false for some clients | low | mitigate | harness-neutral sentence true for the 7 skill-receiving targets; Hermes excluded; the anchor resolves against the embedded SKILL.md |
| T-07-36 | Tampering | transcript re-freeze masking an unrelated wire change | medium | mitigate | capture command only; one commit where every changed line carries the instructions string and each file changes one line; Family (i2) shows a stale transcript goes RED |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/mcp/ ./test/wireoracle/... ./internal/agents/ -count=1` green; the phase gate green
</verification>

<success_criteria>
- AGENT-14 closed: the capability table (07-10) plus the instructions sentence and comments match what ships
- Phase 7's gate recorded; D-08's window left open for the release
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-11-SUMMARY.md` when done — the re-measured byte figures, the transcript count, the gate table and the maintainer advisories.
</output>
