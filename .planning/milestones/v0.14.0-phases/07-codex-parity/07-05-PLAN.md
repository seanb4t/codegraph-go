---
phase: 07-codex-parity
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - internal/agents/codex.go
  - internal/agents/codex_test.go
  - internal/agents/capabilities_test.go
  - internal/agents/ownership_test.go
  - internal/agents/registry_test.go
  - internal/cli/tui/agentpicker_test.go
  - internal/cli/printconfigstyle_test.go
  - internal/cli/install_test.go
  - internal/cli/testdata/plain/print-config-style.golden
  - internal/cli/testdata/plain/print-config-style-local.golden
  - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
autonomous: true
requirements: [CODEX-01, CODEX-02, CODEX-03]
user_setup: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Codex's `Capabilities()` literal declares `Scopes {global, local}`, `ConfigFormat: toml`, `MCPConfig: codexConfigPath` (`.codex/config.toml` locally, `~/.codex/config.toml` globally), `Instructions: codexInstructionsPath` (repo-root `AGENTS.md` locally, `~/.codex/AGENTS.md` globally, unchanged), and `SkillDirs: codexSkillDirs` whose written entry is the shared `.agents/skills/codegraph` / `~/.agents/skills/codegraph` (D-09, D-14); `Hooks` stays `none` until 07-07."
    - "`Install`/`Uninstall` read every path from the table: the `loc != LocationGlobal` early returns and the direct global-only path calls are gone, and the skill step goes through `installDeclaredSkill`/`uninstallDeclaredSkill` (D-09, D-14); `SupportsLocation`, `Detect`, `DescribePaths` and `--print-config-style` follow automatically."
    - "`codegraph install --target codex --location local --yes`, run by the real binary in a git repo, writes `.codex/config.toml` (the codegraph table), the marker block in repo-root `AGENTS.md`, and `.agents/skills/codegraph/SKILL.md` with a manifest whose `targets` include `codex`, prints a trust Note, and writes nothing under HOME; the matching uninstall removes all three (CODEX-02, CODEX-03)."
    - "Every local Codex install carries a `WriteResult.Notes` line saying Codex loads the project config only once the project is trusted, naming the TUI trust prompt and `[projects.\"<absolute repo root>\"] trust_level = \"trusted\"`; its wording about the skill and AGENTS.md follows the recorded D-16/A2 verdicts; codegraph never writes the trust entry itself (D-10, D-16)."
    - "An inline, dotted, quoted, array-of-tables or duplicate codegraph definition in the target config.toml makes Install and Uninstall report an error naming the file and line and leave that file byte-identical, while the instructions and skill steps still run (D-07 surfaced; CR-01 independent steps)."
    - "An uninstall that empties `config.toml` removes the file (the removeMarkedSection/removeHookEntry keep-clean precedent) instead of leaving an empty file."
    - "`codexSkillDirs` lists `.codex/skills/codegraph` / `~/.codex/skills/codegraph` as read-only entries only where 07-LIVE-SESSIONS.md records `D-15 … read: yes`; codegraph never writes a `.codex/skills` copy (D-14, D-15)."
    - "`--target auto --location local` and the picker's pre-check detect Codex through the local `.codex/` directory; the picker still lists all 8 targets (CODEX-02, D-26)."
    - "The first `internal/agents/codex.go` commit of this phase lands after the CODEX-01 evidence commit and rewrites the file's doc comment with a dated citation of 07-LIVE-SESSIONS.md, removing the global-only claim (CODEX-01, D-30 for codex.go)."
    - "Family (d) in 07-MUTATION-LOG.md: reverting the scope, dropping the skill step, dropping the conflict check and dropping the trust Note each turn a named guard RED, reverted byte-clean."
  artifacts:
    - path: "internal/agents/codex.go"
      provides: "codexConfigPath(loc), codexInstructionsPath(loc), codexSkillDirs(loc), codexTrustNote(loc), dual-scope Capabilities, table-driven Install/Uninstall"
      contains: "07-LIVE-SESSIONS.md"
    - path: "internal/agents/codex_test.go"
      provides: "TestCodex_SupportsLocation_GlobalAndLocal, TestCodex_Install_Local_WritesConfigInstructionsAndSkill, TestCodex_Install_Local_TrustNote, TestCodex_Install_RefusesConflictingCodegraphTable, TestCodex_Uninstall_EmptiedConfigIsRemoved, TestCodex_DescribePaths_Local, TestCodex_Detect_Local_AfterInstallReportsConfigured, TestCodex_SharedSkillPackage_LastRequester, TestCodex_ReadOnlySkillDirsFollowLiveVerdict"
      contains: "TestCodex_Install_Local_WritesConfigInstructionsAndSkill"
    - path: "internal/cli/testdata/plain/print-config-style-local.golden"
      provides: "codex's local capability line"
      contains: "codex: scopes=global,local mcp=.codex/config.toml format=toml instructions=AGENTS.md skill=.agents/skills/codegraph hooks=none"
  key_links:
    - from: "internal/agents/codex.go Capabilities()"
      to: "codexConfigPath / codexInstructionsPath / codexSkillDirs"
      via: "PathFunc(Location) table entries; Install/Uninstall resolve through t.Capabilities()"
      pattern: "MCPConfig:\\s+codexConfigPath"
    - from: "internal/agents/codex.go Install"
      to: "internal/agents/toml.go tomlTableConflict"
      via: "conflict checked before splice/strip; reported as a WriteResult error"
      pattern: "tomlTableConflict\\("
    - from: "internal/agents/codex.go Install"
      to: "internal/agents/capabilities.go installDeclaredSkill"
      via: "shared skill package, codex joins the manifest targets"
      pattern: "installDeclaredSkill\\(&result, t, loc\\)"
  prohibitions:
    - statement: "MUST NOT write a project trust entry (`[projects.\"…\"] trust_level`) into any Codex config on the user's behalf — trust is the user's decision, codegraph only tells them how (D-10)."
      status: resolved
      verification: test
    - statement: "MUST NOT write a second codegraph skill copy under `.codex/skills` or `$CODEX_HOME/skills` (Codex does not merge same-name skills, so it would list twice) (D-14)."
      status: resolved
      verification: test
---

<objective>
Flip Codex from global-only to {global, local} through the capability table (D-09) — the first `codex.go`
change of the phase, which also rewrites its stale doc comment with a dated citation of the CODEX-01 live
evidence (CODEX-01's "corrected in the same commit"). Local install writes `.codex/config.toml` through the
fixed splice, the repo-root `AGENTS.md` block and the shared skill package; every local install tells the user
the project must be trusted (D-10); the D-07 conflict refusal becomes a visible install/uninstall error.

Purpose: CODEX-02 (local scope, trust Note, `--target auto`/picker), CODEX-03 (the shared skill at both
scopes), CODEX-01's comment correction, 07-VALIDATION row `07-SCOPE`. D-11's shared-`AGENTS.md` uninstall rule
and D-12's override Note are 07-06; the hook half of D-09 (`Hooks: codex-json`) is 07-07.

Output: `test(07-05):` RED commit(s), `feat(07-05):` GREEN commit(s), Family (d).

Planner notes:
- Precondition: 07-LIVE-SESSIONS.md records `CODEX-01 verdict: PASS` (and at least `L1 project config
  loads when trusted: PASS`). If it reads FAIL, stop — the maintainer decision recorded there decides.
- D-15 branch: read `D-15 .codex/skills read:` and `D-15 CODEX_HOME/skills read:`. `codexSkillDirs(loc)`
  returns `sharedSkillDirs(loc)` followed by `.codex/skills/codegraph` (local) only when the first is `yes`,
  and `~/.codex/skills/codegraph` (global) only when the second is `yes`; otherwise the shared dir alone.
  `TestCodex_ReadOnlySkillDirsFollowLiveVerdict` pins whichever branch the evidence selected and quotes the
  verdict line in its doc comment.
- D-16 branch: `codexTrustNote` names the MCP server (project config) as trust-gated always; it adds "the
  codegraph skill and the AGENTS.md block also load only once the project is trusted" only when
  `D-16 project .agents/skills trust-gated: yes` / `A2 AGENTS.md trust-gated in a real session: yes`
  respectively; otherwise it says those are read regardless. Wording is otherwise the executor's (D-10
  discretion); it must never mention bypassing trust.
- TDD: every test that must change because the scope flips (the global-only tests in codex_test.go,
  TestCapabilitiesDeclared's codex row, TestCapabilitiesMatchInstallWrites' leaf floor 13 → 14,
  `ownershipWantSkillDir`, TestInstallPrintConfigStyle_Filters and TestUninstall_ReportsUnsupportedForWrongLocation
  — both switch their global-only example from codex to hermes) is updated in the RED commit, so the GREEN
  commit leaves the tree green. `GOTOOLCHAIN=go1.26.6`; commits end with `Co-Authored-By: Claude Opus 5
  <noreply@anthropic.com>`; never `[ci skip]`; never gate on `gsd-tools check tdd-red-evidence`.
- Goldens change only through `go test ./internal/cli/ -run 'TestPlainGolden$' -update-plain-goldens -count=1`
  and are reviewed in the diff (only the two codex lines change).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-codex-parity/07-CONTEXT.md
@.planning/phases/07-codex-parity/07-RESEARCH.md
@.planning/phases/07-codex-parity/07-PATTERNS.md
@.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md
@.planning/phases/07-codex-parity/07-04-SUMMARY.md
@internal/agents/codex.go
@internal/agents/opencode.go
@internal/agents/capabilities.go
@internal/agents/skillshared.go
@internal/agents/toml.go
@internal/agents/codex_test.go
@internal/agents/capabilities_test.go
@internal/agents/ownership_test.go
</context>

<interfaces>
After this plan (package agents):
- `func codexConfigPath(loc Location) (string, error)` — local `filepath.Join(".codex", "config.toml")`; global `~/.codex/config.toml`.
- `func codexInstructionsPath(loc Location) (string, error)` — local `"AGENTS.md"`; global `~/.codex/AGENTS.md`.
- `func codexSkillDirs(loc Location) ([]string, error)` — shared dir first, then D-15 read-only entries.
- `func codexTrustNote(loc Location) (string, error)` — "" at global; the D-10 note at local (absolute repo root from `filepath.Abs(".")`).
- Existing helpers reused unchanged: `spliceTOMLTable`, `stripTOMLTable`, `tomlTableConflict`, `codexTableBody`, `upsertInstructionsEntry`, `removeMarkedSection`, `installDeclaredSkill`, `uninstallDeclaredSkill`, `recordFile`, `recordAction`, `atomicWriteFile`.
- Golden lines after this plan:
  global `codex: scopes=global,local mcp=<HOME>/.codex/config.toml format=toml instructions=<HOME>/.codex/AGENTS.md skill=<HOME>/.agents/skills/codegraph hooks=none`
  local  `codex: scopes=global,local mcp=.codex/config.toml format=toml instructions=AGENTS.md skill=.agents/skills/codegraph hooks=none`
- The CODEX-01 record commit subject: `docs(07-04): record CODEX-01 live evidence`.
</interfaces>

## Artifacts this phase produces

See `07-01-PLAN.md` → "Artifacts this phase produces". This plan adds the four Codex path functions, the dual-scope
literal, the table-driven Install/Uninstall, the listed tests and Family (d).

<assumption_delta_decision>
Pluralization ("a second file"): the primary noun is the per-location config path `Capabilities().MCPConfig(loc)`;
decision `no-change` — Phase 5 already promoted per-location `PathFunc`s, and `codexConfigPath` now becomes one
(the opencode shape). The shared skill directory gains a third requester (codex) under the existing manifest
`targets` identity (D-07/D-08 of Phase 5) — `no-change`.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "install --target codex --location local writes the project config, AGENTS.md and the shared skill" — the table flip through the real binary</name>
  <precondition>`.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md` contains `CODEX-01 verdict: PASS` and the D-15/D-16/A2 lines are not PENDING.</precondition>
  <files>internal/agents/codex.go, internal/agents/codex_test.go, internal/agents/capabilities_test.go, internal/agents/ownership_test.go, internal/agents/registry_test.go, internal/cli/tui/agentpicker_test.go, internal/cli/printconfigstyle_test.go, internal/cli/install_test.go, internal/cli/testdata/plain/print-config-style.golden, internal/cli/testdata/plain/print-config-style-local.golden</files>
  <read_first>
    - internal/agents/codex.go (whole file: the literal at :37-45, global-only path funcs at :47-61, Install/Uninstall at :102-167)
    - internal/agents/opencode.go (:45-54 and :297-345 — the dual-scope literal and Install/Uninstall shape to mirror)
    - internal/agents/capabilities.go (installDeclaredSkill/uninstallDeclaredSkill, describeDeclaredPaths, globalOnlyPath)
    - internal/agents/skillshared.go (sharedSkillDirs)
    - internal/agents/codex_test.go, capabilities_test.go (TestCapabilitiesDeclared codex row, TestCapabilitiesMatchInstallWrites floor), ownership_test.go (ownershipWantSkillDir)
    - internal/cli/printconfigstyle_test.go (TestInstallPrintConfigStyle_Filters), internal/cli/install_test.go (TestUninstall_ReportsUnsupportedForWrongLocation)
    - .planning/phases/07-codex-parity/07-LIVE-SESSIONS.md (the CODEX-01 verdicts: D-15, D-16, A2, the codex-cli version and date)
  </read_first>
  <behavior>
    - TestCodex_SupportsLocation_GlobalAndLocal: both true (replaces TestCodex_SupportsLocation_GlobalOnly).
    - TestCodex_Install_Local_WritesConfigInstructionsAndSkill (replaces TestCodex_Install_Local_IsUnsupportedNoWrite): fakeHome + t.Chdir(tmp); Install(LocationLocal) writes `.codex/config.toml` with the codegraph table, `AGENTS.md` with `<!-- CODEGRAPH_START -->`, `.agents/skills/codegraph/SKILL.md` and a manifest whose targets include codex; nothing is created under the fake HOME.
    - TestCodex_Install_Local_TrustNote: exactly one Note containing `trust_level = "trusted"` and the absolute cwd; a global install carries no trust Note; after either install neither `.codex/config.toml` nor the fake HOME's `~/.codex/config.toml` contains a `[projects` header (codegraph never writes trust).
    - TestCodex_Install_RefusesConflictingCodegraphTable: a config.toml with an inline `codegraph = {…}` under `[mcp_servers]` → result.Errors non-empty naming the path, the file byte-identical, the AGENTS.md step still ran; the same for Uninstall.
    - TestCodex_Uninstall_EmptiedConfigIsRemoved: local install into an empty dir then uninstall → `.codex/config.toml` does not exist and its FileResult is `removed`.
    - TestCodex_DescribePaths_Local (replaces TestCodex_DescribePaths_LocalEmpty): contains `.codex/config.toml`, `AGENTS.md`, `.agents/skills/codegraph/SKILL.md` and the manifest path.
    - TestCodex_Detect_Local_AfterInstallReportsConfigured; TestResolveTargetFlag_AutoDetectsCodexAtLocal (a scratch cwd with a `.codex/` dir → `auto` at local includes codex); TestAgentPickerModel_PreChecksCodexAtLocal (real registry, `agents.DetectAll(agents.LocationLocal)` in a scratch cwd with `.codex/` → the Codex row starts checked, 8 rows total).
    - Companion updates (RED at the test commit): TestCapabilitiesDeclared codex row = both scopes, instructions `AGENTS.md` local / `~/.codex/AGENTS.md` global, skillDir shared at both; TestCapabilitiesMatchInstallWrites floor 14 (6 dual-scope targets x 2 + 2 global-only); `ownershipWantSkillDir` groups Codex with Cursor/opencode; TestInstallPrintConfigStyle_Filters and TestUninstall_ReportsUnsupportedForWrongLocation use hermes as the global-only example.
  </behavior>
  <action>
    RED commit `test(07-05): expect Codex at local scope through the capability table` with the behaviour above (compiles against the current code; fails on assertions). Confirm `git log` shows no codex.go commit yet in this phase.
    GREEN commit `feat(07-05): Codex project-local scope through the capability table` — the phase's FIRST codex.go change:
    - Rewrite the codexTarget doc comment (codex.go:13-19): both scopes; global `~/.codex/config.toml` + `~/.codex/AGENTS.md`; local `<repo>/.codex/config.toml`, which Codex loads only for a trusted project, plus the repo-root `AGENTS.md` shared with opencode; the shared `.agents/skills/codegraph` package; "verified live <date> against codex-cli <version> (.planning/phases/07-codex-parity/07-LIVE-SESSIONS.md, CODEX-01)" with the date and version from the evidence; drop the "4 of 8 targets" count (D-30).
    - Replace `codexConfigPath()`/`codexInstructionsPath()` with the `PathFunc(Location)` forms in the interfaces block; add `codexSkillDirs` per the D-15 branch and `codexTrustNote` per the D-16/A2 branch. Update the Capabilities literal and its comment (Hooks stays `HooksNone` — "07-07 adds codex-json").
    - Install: resolve every path via `t.Capabilities()`; config step = read existing, `tomlTableConflict` → on error append `fmt.Errorf("%s: %w", configPath, err)` and skip the write, else splice and write as today; instructions step unchanged in shape; then `installDeclaredSkill(&result, t, loc)`; then append the `codexTrustNote(loc)` Note when non-empty. Each step records its own outcome (a failed step never short-circuits a later one).
    - Uninstall: same resolution; conflict → error and no write; a strip that leaves "" removes the file (ActionRemoved); instructions step unchanged (07-06 adds the D-11 gate); `uninstallDeclaredSkill(&result, t, loc)`.
    - Regenerate the two print-config-style goldens with `-update-plain-goldens` and confirm only the codex lines changed.
    Never add a `.codex/skills` write, and never write a `[projects…]` entry.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && printf '%s\n' "$S" | rg -q '^test\(07-05\): ' && printf '%s\n' "$S" | rg -q '^feat\(07-05\): ' && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents internal/cli)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/agents/ ./internal/cli/... && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/ ./internal/cli/tui/ -count=1 -run 'TestCodex_|TestResolveTargetFlag_AutoDetectsCodexAtLocal$|TestAgentPickerModel_PreChecksCodexAtLocal$|TestCapabilitiesDeclared$|TestCapabilitiesMatchInstallWrites$|TestCapabilitiesTableDrivesDerivations$|TestOwnershipExactIdentity$|TestOwnershipExactIdentity_CrossCheckWrittenSkillDir$|TestInstallPrintConfigStyle|TestPlainGolden$|TestUninstall_ReportsUnsupportedForWrongLocation$' -v > "$O/t1.txt" 2>&1; echo "exit=$?" >> "$O/t1.txt") && tail -4 "$O/t1.txt" && rg -q '^exit=0$' "$O/t1.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t1.txt" && test "$(rg -c '^--- PASS: (TestCodex_SupportsLocation_GlobalAndLocal|TestCodex_Install_Local_WritesConfigInstructionsAndSkill|TestCodex_Install_Local_TrustNote|TestCodex_Install_RefusesConflictingCodegraphTable|TestCodex_Uninstall_EmptiedConfigIsRemoved|TestCodex_DescribePaths_Local|TestCodex_Detect_Local_AfterInstallReportsConfigured|TestResolveTargetFlag_AutoDetectsCodexAtLocal|TestAgentPickerModel_PreChecksCodexAtLocal) ' "$O/t1.txt")" = "9" && test "$(rg -c -- '--- PASS: TestOwnershipExactIdentity/codex/(global|local)/(clean|foreign-codegraph-dir) ' "$O/t1.txt")" = "4" && test "$(rg -c -- '--- PASS: TestCapabilitiesMatchInstallWrites/[a-z]+/(global|local) ' "$O/t1.txt")" = "14" && rg -q -x -F 'codex: scopes=global,local mcp=.codex/config.toml format=toml instructions=AGENTS.md skill=.agents/skills/codegraph hooks=none' internal/cli/testdata/plain/print-config-style-local.golden && rg -q -x -F 'codex: scopes=global,local mcp=<HOME>/.codex/config.toml format=toml instructions=<HOME>/.codex/AGENTS.md skill=<HOME>/.agents/skills/codegraph hooks=none' internal/cli/testdata/plain/print-config-style.golden && GOTOOLCHAIN=go1.26.6 go build -o "$O/codegraph" ./cmd/codegraph && H="$O/home" && P="$O/proj" && mkdir -p "$H" "$P" && git -C "$P" init -q && (cd "$P" && HOME="$H" XDG_CONFIG_HOME="$H/.config" HERMES_HOME="$H/.hermes" "$O/codegraph" install --target codex --location local --yes > "$O/install.txt") && test "$(rg -c '^command = ".*/codegraph"$' "$P/.codex/config.toml")" = "1" && rg -q -F '<!-- CODEGRAPH_START -->' "$P/AGENTS.md" && test -f "$P/.agents/skills/codegraph/SKILL.md" && jq -e '.targets | index("codex") != null' "$P/.agents/skills/codegraph/.codegraph-manifest.json" > /dev/null && rg -q 'note: .*trust_level = "trusted"' "$O/install.txt" && test ! -e "$H/.codex" && test ! -e "$H/.agents" && (cd "$P" && HOME="$H" XDG_CONFIG_HOME="$H/.config" HERMES_HOME="$H/.hermes" "$O/codegraph" uninstall --target codex --location local --yes > "$O/uninstall.txt") && test ! -e "$P/.codex/config.toml" && test ! -e "$P/AGENTS.md" && test ! -e "$P/.agents/skills/codegraph/SKILL.md" && L=$(git log --diff-filter=A --format=%H -- .planning/phases/07-codex-parity/07-01-PLAN.md) && B=$(printf "%s\n" "$L" | tail -1) && C=$(git log --reverse --format=%H "$B"..HEAD -- internal/agents/codex.go) && F=$(printf "%s\n" "$C" | head -1) && test -n "$F" && R=$(git log --format='%H %s' -50) && RC=$(printf '%s\n' "$R" | rg ' docs\(07-04\): record CODEX-01 live evidence' | cut -d' ' -f1) && test -n "$RC" && git merge-base --is-ancestor "$RC" "$F" && FS=$(git show -s --format=%s "$F") && printf '%s\n' "$FS" | rg -q '^feat\(07-05\): ' && G=$(git show "$F" -- internal/agents/codex.go) && printf '%s\n' "$G" | rg -q '^\+.*07-LIVE-SESSIONS\.md' && ! rg -q 'no per-project config' internal/agents/codex.go && ! rg -q 'loc != LocationGlobal' internal/agents/codex.go</automated>
    <fails_when>non-zero exit; a missing test(07-05)/feat(07-05) subject; build/gofmt/vet failure; fewer than 9 named Codex PASS lines, 4 codex ownership leaves or 14 install-writes leaves; any FAIL line; either golden lacking the new codex line; the real local install not writing the config table, AGENTS.md block, shared SKILL.md with codex in the manifest targets or the trust note, or touching HOME; the uninstall leaving any of the three; the first codex.go commit not a feat(07-05) commit, not after the CODEX-01 record, or not citing 07-LIVE-SESSIONS.md; or the global-only claim/early return still present</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -q 'codexConfigPath\(\)|codexInstructionsPath\(\)|globalOnlyPath\(codex' internal/agents/codex.go` exits 1 (no match)
    - `git show --stat <feat(07-05) sha> -- internal/cli/testdata/plain/` lists only the two print-config-style goldens, each with exactly one line changed
    - the SUMMARY pastes the RED `--- FAIL:` lines and quotes the D-15/D-16/A2 verdict lines the branches followed
    - no file under `.codex/skills` is written by any test or by the real-binary run
  </acceptance_criteria>
  <done>Codex installs and uninstalls at local scope through the one table, tells the user about trust, surfaces TOML conflicts, and the stale comment is corrected with a dated live citation in the first codex.go commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codex as a requester of the shared skill package, and the D-15 read-only entries</name>
  <files>internal/agents/codex_test.go, internal/agents/codex.go</files>
  <read_first>
    - internal/agents/skillshared.go (installSkillPackageWithFallback / uninstallSkillPackageWithFallback, manifest targets)
    - internal/agents/manifest.go (skillManifest.Targets)
    - internal/agents/codex.go (Task 1's result)
  </read_first>
  <behavior>
    - TestCodex_SharedSkillPackage_LastRequester: subtests local and global — install opencode then codex at loc → one manifest at the shared dir with targets exactly {codex, opencode}; uninstall codex → SKILL.md and manifest stay, targets {opencode}; uninstall opencode → SKILL.md and manifest gone; a sibling `.agents/skills/other/SKILL.md` planted first is byte-identical throughout.
    - TestCodex_Install_Local_IsIdempotent: a second local install reports every file `unchanged` and changes no byte (CODEX-03 idempotency).
    - TestCodex_ReadOnlySkillDirsFollowLiveVerdict: `Capabilities().ReadOnlySkillDirs(loc)` at both locations equals exactly the D-15 branch 07-LIVE-SESSIONS.md selected (doc comment quotes the two verdict lines); `DescribePaths(loc)` never lists a `.codex/skills` path; no install writes one.
  </behavior>
  <action>
    RED commit `test(07-05): pin Codex's shared-skill requester and read-only skill dirs` (tests that fail on assertions against the current code only where behaviour is missing — if Task 1 already made them pass, record that honestly in the SUMMARY with the reason, as 05-03 did, and keep the tests). GREEN commit `feat(07-05): Codex shares the skill package and declares only verified read paths` for any code the tests require (expected: none or the D-15 entries only).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && S=$(git log --format=%s -20) && test "$(printf '%s\n' "$S" | rg -c '^test\(07-05\): ')" -ge 2 && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/agents)" && O=$(mktemp -d) && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestCodex_SharedSkillPackage_LastRequester$|TestCodex_Install_Local_IsIdempotent$|TestCodex_ReadOnlySkillDirsFollowLiveVerdict$' -v > "$O/t2.txt" 2>&1; echo "exit=$?" >> "$O/t2.txt") && tail -3 "$O/t2.txt" && rg -q '^exit=0$' "$O/t2.txt" && ! rg -q '^(--- FAIL|FAIL)' "$O/t2.txt" && test "$(rg -c -- '--- PASS: TestCodex_SharedSkillPackage_LastRequester/(local|global) ' "$O/t2.txt")" = "2" && test "$(rg -c '^--- PASS: (TestCodex_Install_Local_IsIdempotent|TestCodex_ReadOnlySkillDirsFollowLiveVerdict) ' "$O/t2.txt")" = "2" && rg -q 'D-15 \.codex/skills read' internal/agents/codex_test.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1 > "$O/pkgs.txt" 2>&1; echo "exit=$?" >> "$O/pkgs.txt") && rg -q '^exit=0$' "$O/pkgs.txt"</automated>
    <fails_when>non-zero exit; fewer than two test(07-05) subjects; build/gofmt failure; fewer than 2 last-requester subtests or 2 named PASS lines; any FAIL line; the read-only-dirs test not quoting the D-15 verdict; or the agents/cli packages not green</fails_when>
  </verify>
  <acceptance_criteria>
    - the manifest after installing opencode then codex lists both ids and nothing else (asserted, not assumed)
    - `rg -q '\.codex", "skills"' internal/agents/codex.go` finds no match unless the D-15 verdict is `yes` (then the path appears only inside codexSkillDirs)
  </acceptance_criteria>
  <done>Codex is one more requester of the one shared skill package, idempotent and sibling-safe, and declares only the read paths the live evidence showed.</done>
</task>

<task type="auto">
  <name>Task 3: Family (d) — prove the scope-flip guards can fail</name>
  <files>.planning/phases/07-codex-parity/07-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/07-codex-parity/07-MUTATION-LOG.md
    - internal/agents/codex.go (the final literal and Install body)
  </read_first>
  <action>
    Append four families in the established shape (exact perl commands chosen against the final code, each asserted to change the file, each reverted with `git checkout -- internal/agents/codex.go` behind the gates):
    - `## Family (d1)` — the literal's `Scopes` back to global-only → `TestCapabilitiesDeclared` (codex row) and `TestCodex_SupportsLocation_GlobalAndLocal` RED.
    - `## Family (d2)` — delete the `installDeclaredSkill(&result, t, loc)` call → `TestCapabilitiesMatchInstallWrites/codex/local` and `TestCodex_Install_Local_WritesConfigInstructionsAndSkill` RED.
    - `## Family (d3)` — drop the `tomlTableConflict` check in Install → `TestCodex_Install_RefusesConflictingCodegraphTable` RED.
    - `## Family (d4)` — drop the trust Note append → `TestCodex_Install_Local_TrustNote` RED.
    Commit `docs(07-05): Family (d) mutation log`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && O=$(mktemp -d) && git diff --quiet -- internal/agents/codex.go && test "$(rg -c -F 'installDeclaredSkill(&result, t, loc)' internal/agents/codex.go)" = "1" && perl -0pi -e 's/\n\s*installDeclaredSkill\(&result, t, loc\)\n/\n/' internal/agents/codex.go && ! git diff --quiet -- internal/agents/codex.go && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 -run 'TestCodex_Install_Local_WritesConfigInstructionsAndSkill$' -v > "$O/red-d2.txt" 2>&1; echo "exit=$?" >> "$O/red-d2.txt"); git checkout -- internal/agents/codex.go; git diff --quiet -- internal/agents/codex.go && rg -q -- '--- FAIL: TestCodex_Install_Local_WritesConfigInstructionsAndSkill' "$O/red-d2.txt" && ! rg -q '^exit=0$' "$O/red-d2.txt" && M=.planning/phases/07-codex-parity/07-MUTATION-LOG.md && test "$(rg -c '^## Family \(d[1-4]\)' "$M")" = "4" && rg -q -- '--- FAIL: TestCodex_SupportsLocation_GlobalAndLocal|--- FAIL: TestCapabilitiesDeclared' "$M" && rg -q -- '--- FAIL: TestCodex_Install_Local_WritesConfigInstructionsAndSkill|--- FAIL: TestCapabilitiesMatchInstallWrites/codex/local' "$M" && rg -q -- '--- FAIL: TestCodex_Install_RefusesConflictingCodegraphTable' "$M" && rg -q -- '--- FAIL: TestCodex_Install_Local_TrustNote' "$M" && (GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ -count=1 > "$O/pkg.txt" 2>&1; echo "exit=$?" >> "$O/pkg.txt") && rg -q '^exit=0$' "$O/pkg.txt" && S=$(git log --format=%s -5) && printf '%s\n' "$S" | rg -q '^docs\(07-05\): Family \(d\)'</automated>
    <fails_when>non-zero exit; codex.go dirty before the plant or after the revert; the skill-step call not present exactly once; the planted run exiting 0 or lacking the FAIL line; fewer than 4 Family (d) sections or any family's FAIL excerpt missing; the agents package not green; or no docs(07-05) commit</fails_when>
  </verify>
  <acceptance_criteria>
    - each Family (d) section carries all six convention headings and a byte-clean revert
  </acceptance_criteria>
  <done>Every scope-flip guard is proven able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codegraph → a user's repository (`.codex/`, `AGENTS.md`, `.agents/skills`) | files that may be committed and shared with a team |
| codegraph → Codex's trust model | only the user may trust a project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Elevation of Privilege | Codex project trust | high | mitigate | codegraph never writes `projects.*.trust_level`; the Note tells the user how; TestCodex_Install_Local_TrustNote asserts the config carries no projects table after install |
| T-07-12 | Tampering | conflicting config.toml forms | medium | mitigate | tomlTableConflict surfaced as an error, file byte-identical (Family d3) |
| T-07-13 | Tampering | local writes landing outside the repo or in HOME | medium | mitigate | every local path relative to cwd; the real-binary tracer asserts nothing under HOME |
| T-07-14 | Information Disclosure | ExecPath in a committed `.codex/config.toml` | low | accept | same exposure as the local `.mcp.json` entry (stdioMcpEntry); a teammate's Codex fails to start the server, never runs another binary |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/agents/ ./internal/cli/... -count=1` green
- first codex.go commit after the CODEX-01 record, citing the evidence; goldens changed by one line each
</verification>

<success_criteria>
- CODEX-02 and CODEX-03 delivered through the table; CODEX-01's comment correction landed in the same commit as the first codex.go change
- 07-VALIDATION row 07-SCOPE satisfiable
</success_criteria>

<output>
Create `.planning/phases/07-codex-parity/07-05-SUMMARY.md` when done — RED transcripts, the D-15/D-16 branches taken, and the golden diff.
</output>
