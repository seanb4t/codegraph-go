---
phase: 03-verb-fold
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - .planning/phases/03-verb-fold/03-MUTATION-LOG.md
  - .planning/phases/03-verb-fold/COVERAGE.md
autonomous: true
requirements: [VERB-06, VERB-07]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The re-frozen generated reference is a reviewed diff whose gates have been demonstrated RED: with the `query` stub made VISIBLE again (`Hidden: true` → `Hidden: false` on that one command in `internal/cli/renamed.go`, applied in the working tree), `task docs:cli:drift` exits non-zero reporting that `docs/CLI-REFERENCE.md` differs from a fresh regeneration (the diff shows a `+## codegraph query` section) AND `TestEveryRegisteredFlagIsAccountedFor` fails naming `stale allowlist entry: codegraph query`; after `git checkout -- internal/cli/renamed.go` the file is byte-clean (`git diff --quiet`) and both gates are green again. Recorded as `## Family (b) — VERB-07` in `03-MUTATION-LOG.md` in the 02-MUTATION-LOG.md shape. D-11, D-13 (executor reviews; maintainer at end of phase; no mid-plan checkpoint)."
    - "A re-freeze with zero diff is not a pass: the feat commit's `docs/CLI-REFERENCE.md` change is non-empty (`git show --numstat` reports added and deleted lines for it), so the RED above is a property of the fold's real surface change, not of a no-op regeneration. D-11 (probe VERB-07/adjacency, explicit: the touched golden and the untouched tree are distinguishable — the drift gate is RED on the mutated tree and GREEN on HEAD, both observed)."
    - "The generated-reference gate cannot pass on an empty enumeration: `task docs:cli:drift` prints `compared 1 generated file` before comparing, and `TestEveryRegisteredFlagIsAccountedFor` logs `walked 37 commands` (above its 26-command floor) with `3 accepted via testdata/cli-reference-allowlist.txt` — both counts printed, not assumed. D-11 (probe VERB-07/empty, explicit)."
    - "Regeneration is deterministic and order-stable: two consecutive `go run ./tools/clidoc` runs into scratch files are `cmp`-identical, and identical to the committed reference — the generator's own traversal order is the order, stable across runs. D-11 (probe VERB-07/ordering, explicit)."
    - "Shell completions and man pages reflect the new surface without any regeneration or committed artefact: `codegraph __complete \"\"` (the entry point every generated completion script calls) lists 24 top-level commands — `search` present, `query` and `unlock` absent, and the hidden `man` absent as the pre-existing positive control that `Hidden: true` is what removes a command from completions; `codegraph __complete daemon \"\"` lists `start`, `stop`, `unlock`; `codegraph man <dir>` writes 29 pages including `codegraph-daemon-unlock.1` and excluding `codegraph-query.1`, `codegraph-unlock.1` and `codegraph-man.1`; `git ls-files` carries no `*.1`, `*.bash`, `*.zsh`, `*.fish` or `completions/` entry (nothing to regenerate — RESEARCH Pattern 4: generated from the installed binary at Homebrew-cask install time). VERB-06 second half; Claude's Discretion → no new committed artefact."
    - "The 8-tool MCP set (`codegraph_explore/node/search/callers/callees/impact/files/status`) and the wire-oracle transcripts are untouched by the phase: `git diff --quiet <feat>^ HEAD -- internal/mcp testdata/wireoracle test/wireoracle` holds, the eight names extracted from `internal/mcp/tools.go` equal 03-01's Baseline list, `codegraph_search`'s handler still calls `eng.Search` (never `eng.Query`), and `GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1` plus `task test:wireoracle` are green with `git status --porcelain testdata/wireoracle` empty (byte-identical by the oracle's own comparison). D-14."
    - "`.planning/phases/03-verb-fold/COVERAGE.md` carries the reasoned declaration `No external API integration: the phase folds two CLI verbs and freezes the existing 8-tool MCP set; no API/SDK/service is integrated` — the seal-time gate's accepted form in place of a matrix (orchestrator-resolved API-coverage false positive on the phrase 'wire oracle … MCP set')."
  artifacts:
    - ".planning/phases/03-verb-fold/03-MUTATION-LOG.md — `## Family (b) — VERB-07: a re-visible query command turns docs:cli:drift and TestEveryRegisteredFlagIsAccountedFor RED` with verbatim transcripts, byte-clean revert, green control"
    - ".planning/phases/03-verb-fold/COVERAGE.md — the one-line no-external-API declaration"
  key_links:
    - "`Hidden: true` (renamed.go) → `IsAvailableCommand()` → `tools/clidoc`'s `documented()`, `cli_reference_test.go`'s `documentedByReference()`, cobra's `__complete` and `doc.GenManTree` — one field, four consumers; flipping it on one stub is the single mutation that must turn the two committed gates RED and would re-list the command in completions and man"
    - "feat commit `F` → `F^..HEAD -- internal/mcp testdata/wireoracle test/wireoracle` empty → the MCP/wire surface has zero diff across the whole phase, so VERB-07's 'unchanged' is a git fact, not a test's silence"
  prohibitions:
    - statement: "The RED mutation is applied only in the working tree, on `internal/cli/renamed.go` only, and is reverted byte-clean before anything is staged; no commit in this plan touches a Go file, `docs/`, `internal/mcp/`, or `testdata/wireoracle/`. D-11."
      status: resolved
      verification: test
    - statement: "No blanket regenerate and no re-baseline: `docs/CLI-REFERENCE.md` is not regenerated by this plan (03-02 already froze it); `task test:wireoracle` goldens are compared, never rewritten. D-11, D-14."
      status: resolved
      verification: test
    - statement: "No new committed completion or man artefact and no new generator/Taskfile target for them — they are proven from the live binary. Claude's Discretion, RESEARCH Pattern 4."
      status: resolved
      verification: test
    - statement: "No mid-plan human checkpoint (D-13; `human_verify_mode: end-of-phase`); no `[ci skip]` / `[skip ci]`."
      status: resolved
      verification: test
---

<objective>
Prove the generated surface half of the fold the way this repo proves gates: show the two
committed reference gates go RED against a re-visible `query` command and return GREEN after a
byte-clean revert (Family (b)), show the completion and man-page surfaces already reflect the new
verb tree from the live binary with `man`'s hidden precedent as the positive control, and show the
8-tool MCP set and the wire-oracle transcripts have zero diff across the phase. Write the
`COVERAGE.md` no-external-API declaration.

Purpose: VERB-06's "completions and man pages reflect the new surface" and VERB-07's "goldens
re-frozen in a reviewed diff with a RED demonstration; MCP set and wire oracle unchanged" are
claims about gates and generators — each is only evidence once the gate has been watched fail
(STATE standing rule; Pitfall 7) and once the "unchanged" is a git diff rather than a test that
would pass either way (RESEARCH Pitfall 6).

Output: Family (b) appended to `03-MUTATION-LOG.md`; `COVERAGE.md`; two `docs(03-03):` commits;
no source change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verb-fold/03-CONTEXT.md
@.planning/phases/03-verb-fold/03-RESEARCH.md
@.planning/phases/03-verb-fold/03-MUTATION-LOG.md
@.planning/phases/03-verb-fold/03-02-SUMMARY.md
@internal/cli/renamed.go
</context>

<!-- planner-discipline-allow: codegraph query -->
<!-- planner-discipline-allow: codegraph unlock -->

<tasks>

<task type="tracer">
  <name>Task 1: Family (b) — make the `query` stub visible in the working tree, watch `task docs:cli:drift` and `TestEveryRegisteredFlagIsAccountedFor` go RED, revert byte-clean, record with the green control and the adjacency/empty/ordering proofs</name>
  <files>.planning/phases/03-verb-fold/03-MUTATION-LOG.md</files>
  <precondition>The `feat(cli)!:` commit exists on the branch and the tree is clean: `git log --format=%H --grep='^feat(cli)!: fold query into search --full and unlock into daemon unlock$' -1` prints a SHA and `git diff --quiet -- internal/cli/renamed.go` holds. Halt if either fails.</precondition>
  <read_first>
    - internal/cli/renamed.go — the `query` stub's `Hidden: true` line (the one token to flip) and the `unlock` stub's (left untouched).
    - internal/cli/cli_reference_test.go lines 200–275 — why a visible `query` produces `stale allowlist entry: codegraph query` (the command-level entry is only honoured for a hidden command) and how `walked N commands` / `accepted via` are logged.
    - Taskfile.yml § `docs:cli:drift` — `compared N generated file`, the temp-dir regeneration, the `differs from a fresh regeneration` error and the `diff -u … | head -40` it prints.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md Family (b) — the section order to reproduce (Test/guard, What are we testing and why, Pre-mutation gate, Mutation applied, Observed failure verbatim + exit code, Revert, Byte-clean proof, Green re-run, Verdict).
    - .planning/phases/03-verb-fold/03-MUTATION-LOG.md — Family (a) and Baseline (append after them; do not alter them).
    - .planning/phases/03-verb-fold/03-CONTEXT.md D-11, D-13.
  </read_first>
  <action>
Pre-mutation gate: `git diff --quiet -- internal/cli/renamed.go docs/CLI-REFERENCE.md internal/cli/testdata/cli-reference-allowlist.txt`
holds; record it.

Mutation: in `internal/cli/renamed.go`, on the `query` stub ONLY, change `Hidden: true` to
`Hidden: false` (one token; leave the `unlock` stub hidden). This is D-11's "reintroduce a visible
`query` command" — chosen over "remove `--full`" because it turns BOTH gates red (removing `--full`
would only trip the drift gate; the flag-accounting test does not check for flags the doc mentions
but the tree lacks).

Observe, capturing verbatim transcripts and exit codes:
  1. `task docs:cli:drift` → exit non-zero (task's wrapper reports the target's `exit status 1`);
     output contains `compared 1 generated file`, the `::error::docs:cli:drift: docs/CLI-REFERENCE.md differs from a fresh regeneration`
     line, and the printed diff head includes `+## codegraph query`.
  2. `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryRegisteredFlagIsAccountedFor$' -v`
     → `--- FAIL: TestEveryRegisteredFlagIsAccountedFor`, the failure text names
     `stale allowlist entry: codegraph query`.
  If either gate stays green with the stub visible, STOP — the gate is vacuous — and report that
  rather than reverting quietly.

Revert: `git checkout -- internal/cli/renamed.go`; byte-clean proof: `git diff --quiet -- internal/cli/renamed.go`
and `git status --porcelain internal/ docs/` empty.

Green control (on HEAD, after the revert): `task docs:cli:drift` → exit 0, `compared 1 generated file`,
`byte-identical to a fresh regeneration`; the flag test → `--- PASS`, log line `walked 37 commands …
3 accepted via testdata/cli-reference-allowlist.txt`.

Adjacency proof: `F=$(git log --format=%H --grep='^feat(cli)!: fold query' -1)`;
`git show --format= --numstat "$F" -- docs/CLI-REFERENCE.md` prints non-zero added and deleted
counts (the fold's reference diff is real; a zero-diff re-freeze would have been no re-freeze).
Ordering proof: `GOTOOLCHAIN=go1.26.6 go run ./tools/clidoc -out /tmp/03-03-gen-a.md` then again to
`/tmp/03-03-gen-b.md`; `cmp` both to each other and to `docs/CLI-REFERENCE.md` — identical.

Append `## Family (b) — VERB-07: a re-visible query command turns docs:cli:drift and TestEveryRegisteredFlagIsAccountedFor RED`
to `03-MUTATION-LOG.md` in the Family (b) shape from 02-MUTATION-LOG.md, with the two verbatim RED
transcripts and exit codes, the revert command, the byte-clean proof, the two green re-runs
verbatim, and a Verdict that also records the adjacency/empty/ordering results (numstat line,
`compared 1 generated file`, `walked 37 commands`, the two-run `cmp`). Note that the "What are we
testing" answer is the gate pair, not cobra's `Hidden` semantics or the generator itself
(v0.13.0 Phase 12 ruling: never reimplement the generator).

Commit: `docs(03-03): record the VERB-07 RED demonstration against a re-visible query stub (Family b)` — one file. No `[ci skip]`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rm -f /tmp/03-03-red-drift.txt /tmp/03-03-red-flags.txt && git diff --quiet -- internal/cli/renamed.go && test "$(rg -c 'Hidden:\s+true' internal/cli/renamed.go)" = "2" && perl -0pi -e 's/(Use:\s+"query",.*?Hidden:\s+)true/${1}false/s' internal/cli/renamed.go && test "$(rg -c 'Hidden:\s+false' internal/cli/renamed.go)" = "1" && (task docs:cli:drift > /tmp/03-03-red-drift.txt 2>&1; echo "exit=$?" >> /tmp/03-03-red-drift.txt); (GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryRegisteredFlagIsAccountedFor$' -v > /tmp/03-03-red-flags.txt 2>&1; echo "exit=$?" >> /tmp/03-03-red-flags.txt); git checkout -- internal/cli/renamed.go; git diff --quiet -- internal/cli/renamed.go && test "$(rg -c 'Hidden:\s+true' internal/cli/renamed.go)" = "2" && ! rg -q '^exit=0$' /tmp/03-03-red-drift.txt && rg -q 'compared 1 generated file' /tmp/03-03-red-drift.txt && rg -q 'differs from a fresh regeneration' /tmp/03-03-red-drift.txt && rg -q '^\+## codegraph query$' /tmp/03-03-red-drift.txt && ! rg -q '^exit=0$' /tmp/03-03-red-flags.txt && rg -q -- '--- FAIL: TestEveryRegisteredFlagIsAccountedFor' /tmp/03-03-red-flags.txt && rg -q 'stale allowlist entry: codegraph query' /tmp/03-03-red-flags.txt && echo "RED on both gates with a visible query stub; renamed.go reverted byte-clean"</automated>
    <fails_when>non-zero exit or no closing line — the tree was dirty before the mutation, the one-token flip did not apply (or applied to both stubs), either gate exited 0 with the stub visible (vacuous gate), the drift diff does not show `+## codegraph query`, the flag test does not name the stale allowlist entry, or `renamed.go` is not byte-clean after the revert</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git diff --quiet -- internal/cli/renamed.go && (task docs:cli:drift > /tmp/03-03-green-drift.txt 2>&1; echo "exit=$?" >> /tmp/03-03-green-drift.txt); rg -q '^exit=0$' /tmp/03-03-green-drift.txt && rg -q 'compared 1 generated file' /tmp/03-03-green-drift.txt && rg -q 'byte-identical to a fresh regeneration' /tmp/03-03-green-drift.txt && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryRegisteredFlagIsAccountedFor$' -v 2>&1 | tee /tmp/03-03-green-flags.txt | tail -2; rg -q -- '--- PASS: TestEveryRegisteredFlagIsAccountedFor' /tmp/03-03-green-flags.txt && rg -q 'walked 37 commands' /tmp/03-03-green-flags.txt && rg -q '3 accepted via testdata/cli-reference-allowlist\.txt' /tmp/03-03-green-flags.txt && F=$(git log --format=%H --grep='^feat(cli)!: fold query into search --full and unlock into daemon unlock$' -1) && test -n "$F" && git show --format= --numstat "$F" -- docs/CLI-REFERENCE.md | rg -q '^[1-9][0-9]*\s+[1-9][0-9]*\s+docs/CLI-REFERENCE\.md$' && GOTOOLCHAIN=go1.26.6 go run ./tools/clidoc -out /tmp/03-03-gen-a.md && GOTOOLCHAIN=go1.26.6 go run ./tools/clidoc -out /tmp/03-03-gen-b.md && cmp -s /tmp/03-03-gen-a.md /tmp/03-03-gen-b.md && cmp -s /tmp/03-03-gen-a.md docs/CLI-REFERENCE.md && echo "green control on HEAD; reference diff non-empty at the feat commit; two regenerations identical"</automated>
    <fails_when>non-zero exit or no closing line — the drift gate or the flag test is red on the clean HEAD (the fold left the reference stale), `compared 1 generated file` / `walked 37 commands` / `3 accepted via` did not print, the feat commit's reference diff has zero added or zero deleted lines (no real re-freeze), or two consecutive regenerations differ from each other or from the committed file</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && L=.planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -c '^## Family \(b\) — VERB-07' "$L" && rg -c '^## Family \(a\) — VERB-05' "$L" && rg -q 'stale allowlist entry: codegraph query' "$L" && rg -q '\+## codegraph query' "$L" && rg -q 'differs from a fresh regeneration' "$L" && rg -q -i 'byte-clean' "$L" && rg -q 'byte-identical to a fresh regeneration' "$L" && rg -q 'walked 37 commands' "$L" && rg -q 'compared 1 generated file' "$L" && git show --format=%s --name-only HEAD > /tmp/03-03-c1.txt && rg -q '^docs\(03-03\): ' /tmp/03-03-c1.txt && test "$(rg -v '^docs\(03-03\)' /tmp/03-03-c1.txt | rg -v '^$' | tr '\n' ' ')" = ".planning/phases/03-verb-fold/03-MUTATION-LOG.md " && ! git log -1 --format=%s%n%b | rg -q -i 'ci skip|skip ci' && git diff --quiet -- internal docs && echo "family (b) recorded and committed alone"</automated>
    <fails_when>non-zero exit or a family count of 0 — Family (b) is missing or lacks the RED strings / byte-clean proof / green-control strings / counts, Family (a) was clobbered, the commit is not a `docs(03-03):` commit touching only the mutation log, a CI-skip marker is present, or a source/doc file is dirty</fails_when>
  </verify>
  <acceptance_criteria>
    - With the `query` stub's `Hidden` flipped to `false` in the working tree: `task docs:cli:drift` exits non-zero printing `compared 1 generated file`, `differs from a fresh regeneration` and `+## codegraph query`; `TestEveryRegisteredFlagIsAccountedFor` fails with `stale allowlist entry: codegraph query`.
    - After `git checkout -- internal/cli/renamed.go`: `git diff --quiet` holds; both gates green with `byte-identical to a fresh regeneration` and `walked 37 commands … 3 accepted via testdata/cli-reference-allowlist.txt`.
    - The feat commit's `docs/CLI-REFERENCE.md` numstat has non-zero added and deleted lines; two consecutive `tools/clidoc` runs are `cmp`-identical to each other and to the committed file.
    - `03-MUTATION-LOG.md` gains `## Family (b) — VERB-07` (Family (a) intact) with verbatim RED/green transcripts; one `docs(03-03):` commit touching only the log.
  </acceptance_criteria>
  <done>The two gates that freeze the generated reference have been watched fail on the exact regression they exist to catch and pass again on the real tree, and the re-freeze is on record as a real, deterministic, non-empty diff.</done>
</task>

<task type="auto">
  <name>Task 2: Completions and man pages reflect the new surface from the live binary (with `man`'s hidden precedent as positive control); the 8-tool MCP set and wire-oracle transcripts have zero diff across the phase; write `COVERAGE.md`</name>
  <files>.planning/phases/03-verb-fold/COVERAGE.md, .planning/phases/03-verb-fold/03-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/03-verb-fold/03-MUTATION-LOG.md § Baseline — the pre-fold `__complete ""` list (26), `__complete daemon ""` (2), the 30-page man listing, the 8 tool names, the wire-oracle `ok` line.
    - .planning/phases/03-verb-fold/03-RESEARCH.md § Pattern 4 (completions/man generated at Homebrew-cask install time from the installed binary via `codegraph completion <shell>` / `codegraph man <dir>`; nothing committed) and the VERB-07 row (`codegraph_search`'s handler calls `eng.Search` at `internal/mcp/tools.go:447`).
    - internal/mcp/tools.go lines 173 and 309–345 (the eight `Name:` registrations) and line 447 (`eng.Search`).
    - Taskfile.yml § `test:wireoracle`.
    - The `codegraph completion <shell>` scripts contain NO command names — cobra's generated scripts call back into `codegraph __complete <args…>` at completion time (verified this planning session: `completion bash|zsh|fish | rg -c -w 'query|unlock'` is 0 both before and after). The honest probe of "what a completion would offer" is therefore `__complete`, not a grep of the script.
  </read_first>
  <action>
Build the HEAD binary: `GOTOOLCHAIN=go1.26.6 go build -o /tmp/03-03-bin ./cmd/codegraph`.

Completions (VERB-06): `/tmp/03-03-bin __complete ""` → strip the trailing `:N` directive line;
expect 24 entries (Baseline had 26; `query` and `unlock` gone); `search` and `daemon` present;
`query`, `unlock` absent; `man` absent (the positive control — the pre-existing hidden command
is missing from completions for the same reason the stubs are). `/tmp/03-03-bin __complete daemon ""`
→ `start`, `stop`, `unlock` (3; Baseline had 2). Also record that
`/tmp/03-03-bin completion bash | rg -c -w 'query|unlock'` is 0 AND
`/tmp/03-03-bin completion bash | rg -c -w 'search|daemon'` is also 0 — i.e. the script names no
commands at all, which is why `__complete` is the probe (state this in the SUMMARY so nobody later
mistakes the first zero for evidence on its own).

Man pages (VERB-06): `/tmp/03-03-bin man <tmpdir>` → 29 pages (30 − 2 + 1); `codegraph-daemon-unlock.1`
present; `codegraph-query.1`, `codegraph-unlock.1` absent; `codegraph-man.1` absent (positive
control again). `git ls-files | rg '(\.1|\.bash|\.zsh|\.fish)$|(^|/)completions?/'` → nothing
(no committed artefact exists to regenerate); pair it with `git ls-files | rg -c '^docs/CLI-REFERENCE\.md$'` = 1
so the empty listing is a real query, not a broken one.

MCP set and wire oracle (VERB-07, D-14): `F=$(git log … --grep='^feat(cli)!: fold query' -1)`;
`git diff --quiet "$F^" HEAD -- internal/mcp testdata/wireoracle test/wireoracle` holds; the eight
names extracted from `internal/mcp/tools.go` equal the Baseline list; `rg -n 'eng\.Search\(' internal/mcp/tools.go`
finds the `codegraph_search` handler call and `rg -c 'eng\.Query\(' internal/mcp/tools.go` finds
none; `GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1` → `ok` (uncached);
`GOTOOLCHAIN=go1.26.6 task test:wireoracle` → exit 0; `git status --porcelain testdata/wireoracle` empty.

Write `.planning/phases/03-verb-fold/COVERAGE.md` containing exactly the declaration line
`No external API integration: the phase folds two CLI verbs and freezes the existing 8-tool MCP set; no API/SDK/service is integrated`
under a `# Phase 3 — API coverage` heading, plus one sentence pointing at this task's MCP/wire-oracle
evidence in the SUMMARY.

Append a short `## Generated-surface and MCP proofs (03-03 Task 2)` section to `03-MUTATION-LOG.md`
(after Family (b)) with the `__complete` lists, the man listing counts and names, the `git ls-files`
negative + positive, the eight names, the `git diff --quiet` range, and the wire-oracle `ok` line —
verbatim. These are not mutations; they are the "after" half of 03-01's Baseline and belong beside it.

Commit both files: `docs(03-03): declare no external API integration; record completions/man/MCP/wire-oracle proofs (VERB-06, VERB-07)`. No `[ci skip]`. Remove `/tmp/03-03-bin` and the temp man dir.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build -o /tmp/03-03-bin ./cmd/codegraph && /tmp/03-03-bin __complete "" 2>/dev/null | rg -v '^:' > /tmp/03-03-top.txt && test "$(wc -l < /tmp/03-03-top.txt | tr -d ' ')" = "24" && rg -q '^search\t' /tmp/03-03-top.txt && rg -q '^daemon\t' /tmp/03-03-top.txt && ! rg -q '^(query|unlock|man)\t' /tmp/03-03-top.txt && /tmp/03-03-bin __complete daemon "" 2>/dev/null | rg -v '^:' > /tmp/03-03-daemon.txt && test "$(wc -l < /tmp/03-03-daemon.txt | tr -d ' ')" = "3" && rg -q '^unlock\t' /tmp/03-03-daemon.txt && rg -q '^start\t' /tmp/03-03-daemon.txt && rg -q '^stop\t' /tmp/03-03-daemon.txt && M=$(mktemp -d) && /tmp/03-03-bin man "$M" >/dev/null && test "$(ls "$M" | wc -l | tr -d ' ')" = "29" && test -f "$M/codegraph-daemon-unlock.1" && test ! -e "$M/codegraph-query.1" && test ! -e "$M/codegraph-unlock.1" && test ! -e "$M/codegraph-man.1" && rm -rf "$M" && test "$(git ls-files | rg '(\.1|\.bash|\.zsh|\.fish)$|(^|/)completions?/' | wc -l | tr -d ' ')" = "0" && test "$(git ls-files | rg -c '^docs/CLI-REFERENCE\.md$')" = "1" && echo "completions: 24 top-level (query/unlock/man absent), daemon has unlock; man: 29 pages, daemon-unlock present, stubs+man absent; no committed completion/man artefact"</automated>
    <fails_when>non-zero exit or no closing line — the top-level completion list is not exactly 24 or still offers `query`/`unlock` (or offers hidden `man`, which would mean `Hidden` no longer gates completions), `daemon` does not offer `unlock`, the man tree is not 29 pages with `codegraph-daemon-unlock.1` present and the stub/`man` pages absent, a committed completion/man artefact exists, or the positive-control `git ls-files` count is not 1</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && F=$(git log --format=%H --grep='^feat(cli)!: fold query into search --full and unlock into daemon unlock$' -1) && test -n "$F" && git diff --quiet "$F^" HEAD -- internal/mcp testdata/wireoracle test/wireoracle && test "$(rg -o 'Name:\s+"codegraph_[a-z]+"' internal/mcp/tools.go | rg -o 'codegraph_[a-z]+' | sort -u | tr '\n' ' ')" = "codegraph_callees codegraph_callers codegraph_explore codegraph_files codegraph_impact codegraph_node codegraph_search codegraph_status " && test "$(rg -c 'eng\.Search\(' internal/mcp/tools.go)" = "1" && ! rg -q 'eng\.Query\(' internal/mcp/tools.go && GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1 2>&1 | tee /tmp/03-03-wo.txt | tail -2; rg -q '^ok\s+github.com/seanb4t/codegraph-go/test/wireoracle' /tmp/03-03-wo.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/03-03-wo.txt && GOTOOLCHAIN=go1.26.6 task test:wireoracle > /tmp/03-03-wo-task.txt 2>&1 && test -z "$(git status --porcelain testdata/wireoracle)" && echo "mcp/wireoracle zero-diff across the phase; 8 tools; codegraph_search still eng.Search; oracle green uncached"</automated>
    <fails_when>non-zero exit or no closing line — something under `internal/mcp`, `testdata/wireoracle` or `test/wireoracle` changed between the feat commit's parent and HEAD, the tool set is not the eight names, `codegraph_search`'s handler no longer calls `eng.Search` exactly once or now calls `eng.Query`, the uncached oracle run is red, `task test:wireoracle` is red, or a transcript is dirty</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test -f .planning/phases/03-verb-fold/COVERAGE.md && rg -q -F 'No external API integration: the phase folds two CLI verbs and freezes the existing 8-tool MCP set; no API/SDK/service is integrated' .planning/phases/03-verb-fold/COVERAGE.md && L=.planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -c '^## Generated-surface and MCP proofs \(03-03 Task 2\)' "$L" && rg -q 'codegraph-daemon-unlock\.1' "$L" && rg -q 'codegraph_status' "$L" && git show --format=%s --name-only HEAD > /tmp/03-03-c2.txt && rg -q '^docs\(03-03\): ' /tmp/03-03-c2.txt && test "$(rg -v '^docs\(03-03\)' /tmp/03-03-c2.txt | rg -v '^$' | sort | tr '\n' ' ')" = ".planning/phases/03-verb-fold/03-MUTATION-LOG.md .planning/phases/03-verb-fold/COVERAGE.md " && ! git log -2 --format=%s%n%b | rg -q -i 'ci skip|skip ci' && git diff --quiet -- internal docs cmd && echo "COVERAGE.md declaration + proofs section committed; tree clean"</automated>
    <fails_when>non-zero exit or a section count of 0 — `COVERAGE.md` is missing or does not carry the exact declaration line, the proofs section is absent from the log, the commit is not a `docs(03-03):` commit touching exactly those two files, a CI-skip marker is present, or a source/doc file is dirty</fails_when>
  </verify>
  <acceptance_criteria>
    - `__complete ""` lists 24 entries with `search`/`daemon` present and `query`/`unlock`/`man` absent; `__complete daemon ""` lists `start`, `stop`, `unlock`.
    - `codegraph man <dir>` writes 29 pages including `codegraph-daemon-unlock.1`, excluding `codegraph-query.1`, `codegraph-unlock.1`, `codegraph-man.1`; `git ls-files` has no completion/man artefact (positive control: `docs/CLI-REFERENCE.md` count 1).
    - `git diff --quiet F^ HEAD -- internal/mcp testdata/wireoracle test/wireoracle`; eight tool names unchanged; `codegraph_search` → `eng.Search` only; uncached oracle `ok`; `task test:wireoracle` exit 0; no dirty transcript.
    - `COVERAGE.md` carries the exact declaration; the log gains the proofs section; one `docs(03-03):` commit with exactly those two files.
  </acceptance_criteria>
  <done>The new verb surface is what completions and man pages offer from the live binary — with the hidden `man` command proving the exclusion mechanism — and the MCP/wire surface is a zero-diff git fact across the whole phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working-tree mutation → committed history | `renamed.go` is edited for two gate runs and must be reverted before any `git add` |
| generated reference / allowlist → `TestEveryRegisteredFlagIsAccountedFor` and `task docs:cli:drift` | the gates decide whether a hidden-vs-visible change is caught; they are only trusted after a RED |
| live binary → completion/man consumers | `__complete` and `man` output is what an installed shell and `man(1)` see; nothing committed mediates it |
| phase diff → MCP/wire surface | any change under `internal/mcp` or the transcripts would change what agents see over stdio |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Repudiation | reference gates never shown RED (vacuous re-freeze) | high | mitigate | Family (b): one-token `Hidden` flip → both gates RED with named output (`+## codegraph query`, `stale allowlist entry: codegraph query`) → revert → both GREEN; verify asserts every string and exit code. |
| T-03-03-02 | Tampering | RED mutation leaking into a commit | medium | mitigate | `git checkout -- internal/cli/renamed.go` then `git diff --quiet` asserted before the commit; the commit's file list is asserted to be the log only. |
| T-03-03-03 | Tampering | blanket regenerate / re-baseline hiding a surface change | high | mitigate | This plan regenerates nothing into the tree (scratch only); `docs/CLI-REFERENCE.md` numstat at the feat commit proves the re-freeze was a real, reviewed diff; two-run `cmp` proves determinism. |
| T-03-03-04 | Tampering | MCP tool set or wire transcripts changed by the fold | high | mitigate | `git diff --quiet F^ HEAD -- internal/mcp testdata/wireoracle test/wireoracle`; eight names asserted; `eng.Search` call count 1 / `eng.Query` 0 in tools.go; uncached oracle run. |
| T-03-03-05 | Repudiation | completion "absence" claimed from a script that never names commands | medium | mitigate | `__complete` is the probe; the hidden `man` command's absence is the positive control; the script-grep zero is recorded as non-evidence. |
| T-03-03-06 | Denial of Service | `protect-main` blocked by a `[ci skip]` marker | high | mitigate | Commit messages grepped for CI-skip markers. |
| T-03-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install. |
</threat_model>

<verification>
- Family (b): both gates RED with the `query` stub visible, GREEN after a byte-clean revert; numstat non-empty; two regenerations identical.
- `__complete ""` = 24 (no query/unlock/man), `__complete daemon ""` = 3 (with unlock); man = 29 pages with `codegraph-daemon-unlock.1`.
- `git diff --quiet F^ HEAD -- internal/mcp testdata/wireoracle test/wireoracle`; oracle green uncached; `COVERAGE.md` declaration present.
</verification>

<success_criteria>
- VERB-07's RED demonstration and unchanged-MCP/wire-oracle proofs are recorded with verbatim transcripts.
- VERB-06's completions/man half is proven from the live binary with a positive control and no committed artefact.
- Two `docs(03-03):` commits; no source change.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/03-verb-fold/03-MUTATION-LOG.md` — `## Family (b) — VERB-07`, `## Generated-surface and MCP proofs (03-03 Task 2)` (this plan)
- `.planning/phases/03-verb-fold/COVERAGE.md` — no-external-API declaration (this plan)
- Source/test/reference artefacts: see 03-02 (`search.go`, `renamed.go`, `daemon.go`, tests, allowlist, `docs/CLI-REFERENCE.md`); census families (a)/(c) and the backlog row: 03-01 / 03-04

## Flagged assumptions (probe rows left unresolved — surfaced, not dropped)

- VERB-06 (unclassified): completions and man pages are generated at Homebrew-cask install time from the installed binary (`.goreleaser.yaml` `generate_completions_from_executable`, the cask post-install `codegraph man` hook — RESEARCH Pattern 4). This plan proves what the binary generates; it cannot exercise the cask install itself. Assumption: the cask stanza still invokes the binary's `completion`/`man` commands at release time (unchanged by this phase; `git diff --quiet F^ HEAD -- .goreleaser.yaml` is a cheap cross-check the executor may add to the SUMMARY).
- VERB-07 (adjacency/empty/ordering): resolved as explicit truths above.

<output>
Create `.planning/phases/03-verb-fold/03-03-SUMMARY.md` when done — include the two RED transcripts and the green control verbatim, the `__complete`/man listings, and the `git diff --quiet` range with the feat commit SHA.
</output>
