---
phase: 03-verb-fold
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/cli/search.go
  - internal/cli/query_cli_test.go
  - internal/cli/notice_test.go
  - internal/cli/daemon.go
  - internal/cli/daemon_test.go
  - internal/cli/index.go
  - internal/daemon/lock.go
  - internal/cli/index_lock_test.go
  - internal/cli/root.go
  - internal/cli/renamed.go
  - internal/cli/renamed_test.go
  - internal/cli/testdata/cli-reference-allowlist.txt
  - docs/CLI-REFERENCE.md
files_deleted:
  - internal/cli/query.go
  - internal/cli/unlock.go
autonomous: true
requirements: [VERB-01, VERB-02, VERB-03, VERB-04, VERB-06, VERB-08]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`codegraph search --full <term>` prints two lines per hit in the human branch: line 1 is byte-identical to default `search`'s `Name (Kind) FilePath:StartLine` line; line 2 is exactly four ASCII spaces, `QualifiedName`, two ASCII spaces, `Signature` — or just four spaces and `QualifiedName` when `Signature` is empty (packages, files), with no trailing whitespace. Hence `search --full … | rg '^\\S'` reproduces default output byte-for-byte (superset). On the gofixture: `search Alpha --full` is `Alpha (function) pkga/pkga.go:13` then `    Alpha  () int`; `search pkga --full` renders `    example.com/gofixture/pkga` under the package line. D-01 (probe VERB-01/encoding, explicit: line 2 is Go string concatenation of the node's `QualifiedName` and `Signature` bytes — no truncation, escaping, normalisation or width padding; `TestRenderFullLine` pins a signature containing spaces and a non-ASCII byte sequence verbatim)."
    - "`search --full` with zero hits prints `[]` under `--json` (the same `[]` default `search --json` prints) and prints nothing in the human branch on a clean tree, exiting 0; an empty/blank term errors exactly as default `search` does (the engine's own validation, unchanged); a single hit is exactly two lines. D-01/D-03 (probe VERB-01/empty, explicit)."
    - "The WORK-02 worktree-mismatch notice appears at the top of `search --full`'s human output in the same position as default `search` (after the `--json` early return) and never in either JSON branch — `noticeCommandCases` and the JSON-suppression table each carry a `search --full` row in place of the old `query` row, and all three notice tests pass. D-02."
    - "`search --full --json` emits `query.MarshalQueryJSON(nodes)` — the old `query --json` envelope `[{\"node\":{…}}]` unchanged in shape — while default `search --json` stays `json.Marshal(locs)` (`[{\"name\":…}]`, no `node` key); `--full` dispatches to `eng.Query`, default to `eng.Search`, both over `matchNodes`. D-03."
    - "`search` accepts `-k` (`--kind`), `-l` (`--limit`), `-j` (`--json`) alongside the existing `-p` (`--path`); `TestSearchFlagShortForms` proves each long/short pair yields byte-identical, non-empty output on both `search` and `search --full`, and that `-j` selects the envelope only under `--full`. D-04 (VERB-02's flag-parse test)."
    - "`codegraph query …` and `codegraph unlock …` are hidden dedicated stubs (`Hidden: true`, `DisableFlagParsing: true`) that print D-06's two stderr lines, write nothing to stdout, execute nothing (an initialised index is not opened; a stale lockfile is left in place), return a non-nil error so `cmd/codegraph/main.go` exits 1, ignore every arg/flag (`--json`, `-p x`, no args), and use neither cobra's deprecation field nor a process-exit call. D-05, D-06."
    - "`codegraph daemon unlock [path]` is `unlock.go`'s body moved verbatim under `daemon` (`Use: \"unlock [path]\"`, `MaximumNArgs(1)`, zero flags of its own — `-p` is rejected exactly as it was on the old verb, `daemon`'s own `-p` is non-persistent); absent lock → `no lock present at … — nothing to do`, dead-pid lock → `removed stale lock (pid=N)` and the file is gone; `TestDaemonUnlockCmd` is the net-new CLI-level test (none existed before). D-07."
    - "`internal/daemon/lock.go` (the `ErrLockLive` message and the `Unlock` doc comment) and `internal/cli/index.go` (the `ErrStoreLocked` message and its comment) say `codegraph daemon unlock`; `index_lock_test.go`'s assertion was flipped to expect it FIRST (RED observed as `--- FAIL: TestIndexForceRefusesWhileStoreIsHeld` and committed as part of Task 2's `test(03-02):` RED commit), then the messages changed (GREEN) inside the one `feat(cli)!:` commit. D-08."
    - "Every new test was RED before its implementation, the RED was recorded (a `--- FAIL:` line per test in the SUMMARY plus a `check tdd-red-evidence` record per task with verdict `RED_EVIDENCE_OK`), and each task's RED landed as its own `test(03-02): …` commit touching only test files (plus, for Task 1, the `renderFullLine` placeholder declaration that lets `TestRenderFullLine` compile) BEFORE any implementation — the TDD runtime gate's required evidence (`workflow.tdd_mode: true`; the shape Phases 1–2 used: `test(01-03): add failing …`, `test(02-03): add failing …`). These RED commits are the sanctioned exception to 03-CONTEXT's Claude's-Discretion clause that every commit leaves `go test ./internal/cli/...` green; every commit still leaves `task docs:cli:drift` green. GREEN is never committed separately — it lands in the single `feat(cli)!:` commit. D-08/D-15."
    - "Exactly ONE `feat(cli)!: fold query into search --full and unlock into daemon unlock` commit carries the surface change — `--full` + shorthands, `daemon unlock`, the two stubs, the D-08 messages, the GREEN state of their tests, the two allowlist lines and the regenerated `docs/CLI-REFERENCE.md` — preceded only by the plan's `test(03-02):` RED commits, with a `BREAKING CHANGE:` footer naming the removed verbs, their replacements, and that the `query`/`unlock` stubs are removed in v0.15.0. The subject matches `.github/workflows/pr-title.yml`'s regex (`type(scope)!:` ordering — RESEARCH Pitfall 1 corrects D-15's literal `feat!(cli)`). D-15, D-09 (the footer half of the removal record)."
    - "Default `search` and `search --json` are byte-identical before and after the fold: the pre-fold binary (built from the feat! commit's parent in a scratch worktree) and the HEAD binary produce `cmp`-identical output for five fixed invocations on the repo's own index and four on a fresh gofixture, at the same instant against the same index. D-01, D-03."
    - "`docs/CLI-REFERENCE.md` is regenerated by `task docs:cli` as a reviewed diff whose only changes are: the `query` and `unlock` index bullets and sections removed, the `search` bullet/section gaining `--full`, `-k`, `-l`, `-j` and the new Short text, and a new `codegraph daemon unlock` bullet + section; `task docs:cli:drift` and `TestEveryRegisteredFlagIsAccountedFor` (walked 37 commands, 3 accepted via the allowlist) are green at the commit. D-11, D-12, D-13 (executor reviews and commits; maintainer reviews at end of phase; no mid-plan checkpoint)."
    - "Each stub has one command-level allowlist line whose reason carries the rename target and the removal minor: `codegraph query<TAB>hidden rename stub for `search --full` (VERB-03); removed in v0.15.0` and `codegraph unlock<TAB>hidden rename stub for `daemon unlock` (VERB-04); removed in v0.15.0` — the `docs/CLI-REFERENCE.md`-side record of the stubs' lifetime. D-12, D-09."
    - statement: "The per-file diff summary of the regenerated reference and of every touched source file is in the SUMMARY for the maintainer's end-of-phase review, and nothing in the regenerated reference diff falls outside the three expected change groups."
      verification: backstop
  artifacts:
    - "internal/cli/search.go — `full bool`, `BoolVar(&full, \"full\", …)`, `StringVarP(&kind, \"kind\", \"k\", …)`, `IntVarP(&limit, \"limit\", \"l\", …)`, `BoolVarP(&jsonOut, \"json\", \"j\", …)`, `renderFullLine(n *schema.Node) string`, relocated `resolveStartPath`/`writeJSONLine`"
    - "internal/cli/renamed.go — `newQueryCmd()`, `newUnlockCmd()` (Hidden, DisableFlagParsing, D-06 stderr text, non-nil error)"
    - "internal/cli/daemon.go — `newDaemonUnlockCmd()` and `cmd.AddCommand(newDaemonStartCmd(), newDaemonStopCmd(), newDaemonUnlockCmd())`"
    - "internal/cli/query_cli_test.go — `TestSearchFullCmd`, `TestRenderFullLine`, `TestSearchFlagShortForms`; internal/cli/renamed_test.go — `TestQueryStub`, `TestUnlockStub`; internal/cli/daemon_test.go — `TestDaemonUnlockCmd`"
    - "internal/cli/testdata/cli-reference-allowlist.txt — two new lines; docs/CLI-REFERENCE.md — regenerated"
  key_links:
    - "`root.go`'s unchanged `AddCommand(… newQueryCmd(), newSearchCmd(), … newUnlockCmd() …)` list → `renamed.go` now defines both constructors → the stubs are registered at the same call sites with no root.go registration edit"
    - "`search --full` → `eng.Query` → `query.MarshalQueryJSON` / `renderFullLine`; default → `eng.Search` → `json.Marshal(locs)` — one command, two tails, shared prefix (path resolution, `query.OpenAt`, notice placement)"
    - "`Hidden: true` on the stubs → `IsAvailableCommand()` false → `tools/clidoc`, `cli_reference_test.go`'s `documentedByReference`, cobra's `__complete` and `doc.GenManTree` all exclude them → the only remaining registration is `--help` from `InitDefaultHelpFlag()` → the command-level allowlist lines"
    - "`index.go` `ErrStoreLocked` message → `index_lock_test.go` assertion (RED first) — the only test that names the message; `lock.go`'s sibling message has no asserting test (a wording change gets no new test, v0.13.0 Phase 12 ruling) and is covered by the 03-04 census"
  prohibitions:
    - statement: "The stubs do not set cobra's deprecation field (it would still execute the command), do not call a process-exit function (main.go's single `os.Exit(1)` is the tree's only error exit), do not open an engine or touch the filesystem, and do not forward anything to the new verbs. No new exit code is introduced. D-05, D-06. Gate: negative greps on `renamed.go` paired with positive assertions on `Hidden: true` (2) and `DisableFlagParsing: true` (2)."
      status: resolved
      verification: test
    - statement: "`search`'s default human and JSON branches are not edited: `eng.Search`, `json.Marshal(locs)` and the one-line render stay as they are; the byte-identity `cmp` against the pre-fold binary is the gate. D-01/D-03, Pitfall 4 (both JSON shapes survive)."
      status: resolved
      verification: test
    - statement: "`daemon unlock` inherits nothing new from the `daemon` parent and gains no flag; `unlock.go`'s RunE body and Args are moved unchanged (only the constructor name and doc comment change). D-07."
      status: resolved
      verification: test
    - statement: "Nothing under `internal/mcp/`, `testdata/wireoracle/`, `test/wireoracle/` or `web/` is touched; `codegraph_search`'s MCP handler keeps calling `eng.Search`. D-14."
      status: resolved
      verification: test
    - statement: "No commit other than the single `feat(cli)!:` commit touches a non-test source file, `docs/`, or the allowlist (the Task 1 RED commit's `renderFullLine` placeholder declaration in `search.go` is the one named exception); the only other commits this plan makes are its `test(03-02): …` RED commits — one per task, each committed BEFORE that task's implementation step, each compiling against the tree it lands on and failing on an assertion (never a build error). No GREEN test state is committed separately from the feat! commit. No `[ci skip]` / `[skip ci]`. D-15."
      status: resolved
      verification: test
    - statement: "No blanket regenerate: `task docs:cli` output is reviewed against the three expected change groups before it is staged (D-11); no new committed completion/man artefact is introduced (Claude's Discretion → RESEARCH Pattern 4)."
      status: resolved
      verification: judgment
---

<objective>
Land the whole user-facing verb fold as one `feat(cli)!:` commit: `search` gains `--full` (the old
`query` behaviour — `eng.Query`, the `MarshalQueryJSON` envelope under `--json`, a two-line human
render) and `query`'s `-k/-l/-j` shorthands; `unlock` moves verbatim to `daemon unlock`; `query` and
`unlock` become hidden stubs that print the rename to stderr and exit 1 executing nothing; the two
live messages that name the old verb say `codegraph daemon unlock`; every test that names any of
this is written RED-first; the generated reference and its allowlist are re-frozen in the same
commit so the commit itself is green on `task docs:cli:drift` and `go test ./internal/cli/...`.

Purpose: VERB-01…VERB-04 and VERB-08's commit shape are one user-facing event (03-CONTEXT
"Specific Ideas": one `feat!:`, one BREAKING CHANGES entry). D-15 lists reference regeneration among
the ordinary commits, but the same decision's discretion clause requires every commit to leave
`task docs:cli:drift` green — which the feat! commit cannot do without carrying the regenerated
reference and the two allowlist lines. The plan honours both by folding the reviewed regeneration
into the single commit and recording the per-file diff for the maintainer (D-13). TDD's RED commit
is NOT overridden: `workflow.tdd_mode` is on, so the executor's runtime gate
(`gsd-core/references/execute-mvp-tdd.md`) halts any `tdd="true"` task whose failing test has not
been committed as `test(03-02): …` before its implementation step. Each task therefore commits its
RED tests (test files only — plus Task 1's `renderFullLine` placeholder so `TestRenderFullLine`
compiles) as one `test(03-02):` commit, persists the RED evidence record and verifies it with
`gsd_run check tdd-red-evidence <record.json> --raw` (verdict must be `RED_EVIDENCE_OK`), and then
implements GREEN in the working tree without committing. Those RED commits are the sanctioned
exception to 03-CONTEXT's Claude's-Discretion clause that every commit leaves
`go test ./internal/cli/...` green (Phases 1–2 landed the same `test(NN-PP): add failing …` shape);
every commit still leaves `task docs:cli:drift` and `go build ./...` green. Under the repo's
squash-merge model none of the intermediate commits reach `main`.

Output: three `test(03-02):` RED commits, then the single `feat(cli)!:` commit (1 added, 7 modified,
2 deleted — plus any test file that needed a GREEN-time edit, which must then appear as `M`, never
`A`) and a before/after byte-identity proof for default `search`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verb-fold/03-CONTEXT.md
@.planning/phases/03-verb-fold/03-RESEARCH.md
@.planning/phases/03-verb-fold/03-PATTERNS.md
@.planning/phases/03-verb-fold/03-MUTATION-LOG.md
@internal/cli/query.go
@internal/cli/search.go
@internal/cli/unlock.go
@internal/cli/man.go
</context>

<!-- planner-discipline-allow: codegraph query -->
<!-- planner-discipline-allow: codegraph unlock -->
<!-- planner-discipline-allow: Deprecated -->
<!-- planner-discipline-allow: os.Exit -->
<!-- planner-discipline-allow: os\.Exit -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: `search --full` end-to-end — envelope under `--json`, two-line human render, WORK-02 notice, `-k/-l/-j` shorthands — tests RED first (committed as `test(03-02):`), then GREEN (working tree only, no commit)</name>
  <files>internal/cli/search.go, internal/cli/query_cli_test.go, internal/cli/notice_test.go</files>
  <precondition>`.planning/phases/03-verb-fold/03-MUTATION-LOG.md` contains `## Family (a) — VERB-05` and `before: 14 lines across 6 files` — the pre-fold census (03-01) ran before this first `internal/cli/` edit. Check with `rg -q 'before: 14 lines across 6 files' .planning/phases/03-verb-fold/03-MUTATION-LOG.md`; halt if absent.</precondition>
  <read_first>
    - internal/cli/query.go (all 94 lines) — `resolveStartPath` (lines 12–22) and `writeJSONLine` (24–33) move VERBATIM, doc comments included, into `search.go`; the RunE tail (61–84) is the `--full` branch; the four `…VarP` flag lines (88–91) are the shorthand registration to adopt.
    - internal/cli/search.go (all 71 lines) — the default branch that must not change; `StringVar`/`IntVar`/`BoolVar` at 66–68 become the `VarP` forms; the notice call at 57 is the D-02 placement to mirror.
    - internal/cli/query_cli_test.go lines 1–98 — `setupIndexedFixture`, `TestQueryCmd` (to be removed — its subject folds into `TestSearchFullCmd`; the stub gets its own test in Task 3), `TestSearchCmd` (keep).
    - internal/cli/notice_test.go lines 130–142 (`noticeCommandCases`, the `query` row) and 213–226 (the `--json` suppression table, the `query` row).
    - internal/cli/cli_test.go lines 55–71 — `execCmd`/`execCmdWithInput` (captures stdout, stderr, err).
    - internal/schema/graph.pb.go lines 104–120 — `Node.Name/Kind/QualifiedName/FilePath/StartLine/Signature`.
    - .planning/phases/03-verb-fold/03-PATTERNS.md § "`internal/cli/search.go` … gains `--full`" — the dual-branch shape, the `renderFullLine` helper, the flag lines.
    - .planning/phases/03-verb-fold/03-CONTEXT.md D-01…D-04.
  </read_first>
  <behavior>
    - TestSearchFullCmd/"--full --json emits the MarshalQueryJSON envelope": `search main -p dir --full --json` → output starts with `[{"node":` and unmarshals into `[]struct{Node struct{Name,Kind,QualifiedName,Signature string}}` containing `main`.
    - TestSearchFullCmd/"default --json stays the Location array": `search main -p dir --json` → starts with `[{"name":`, contains no `"node"` key.
    - TestSearchFullCmd/"--full human branch is two lines per hit (D-01)": `search Alpha -p dir --full` == default `search Alpha -p dir` output + `    Alpha  () int\n` (line 1 taken from the default run, so the test asserts the superset, not a hardcoded line number).
    - TestSearchFullCmd/"empty Signature renders just the qualified name": `search pkga -p dir --full` contains the line `    example.com/gofixture/pkga` and no line has trailing whitespace.
    - TestSearchFullCmd/"non-indented lines of --full equal default output": filtering `--full` output to lines not starting with a space yields default output byte-for-byte, for `Alpha`, `pkga`, `helper`.
    - TestSearchFullCmd/"zero hits: --json prints [] and human prints nothing": term `zzz-no-such-symbol` → `[]\n` under `--full --json`, `""` under `--full`, err nil (and the same `[]\n` under default `--json`).
    - TestSearchFullCmd/"--kind rejects an unknown kind" and "not initialized directory errors" under `--full` (carried over from TestQueryCmd).
    - TestRenderFullLine: `{QualifiedName:"a.B", Signature:"(x int) error"}` → `    a.B  (x int) error`; `{QualifiedName:"example.com/x", Signature:""}` → `    example.com/x`; `{QualifiedName:"q", Signature:"(s string) (résumé, error)"}` → the signature bytes verbatim after two spaces (encoding: no truncation/normalisation).
    - TestSearchFlagShortForms: for mode ∈ {none, `--full`} and pairs (`--kind function`/`-k function`), (`--limit 1`/`-l 1`), (`--json`/`-j`), (`--path dir`/`-p dir`): both forms of `search main …` return err nil and byte-identical non-empty output; under `--full` the `-j` output starts with `[{"node":`, otherwise with `[{"name":`.
    - Notice tests: `noticeCommandCases` row `{"search --full", []string{"search", "Alpha", "--full", "-p", path}}` replaces the `query` row; the JSON table row `{"search --full", []string{"search", "Alpha", "--full", "-p", wt, "--json"}}` replaces its `query` row; `TestNoticeOnWorktreeMismatch`, `TestNoticeAbsentOnCleanTree`, `TestNoticeSuppressedInJSON` pass.
  </behavior>
  <action>
RED first. In `query_cli_test.go` delete `TestQueryCmd` and add `TestSearchFullCmd`,
`TestRenderFullLine` and `TestSearchFlagShortForms` exactly as the `<behavior>` block specifies.
So that `TestRenderFullLine` compiles and fails on its ASSERTION (not on a missing symbol — a
compile error is not a RED), add to `search.go` a declaration of `renderFullLine(n *schema.Node)
string` whose body is just `return ""` (the RED state — the real body is written in GREEN below), then run
`GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestSearchFullCmd|TestRenderFullLine|TestSearchFlagShortForms' -v`
and record the `--- FAIL:` lines (expected: all three top-level tests fail — `--full` is an
unknown flag, `-k` is an unknown shorthand, the helper returns the wrong string). Paste the count
into the SUMMARY. Also retarget the two `query` rows in `notice_test.go` to the `search --full`
rows and run `-run 'TestNotice'` — record that the `search --full` subtests are RED (unknown flag).

RED COMMIT (TDD gate, `workflow.tdd_mode: true`). Persist the RED evidence record (command, exit
code, the failing target tests, expected result from `<behavior>`, actual `--- FAIL:` lines) to
`.planning/phases/03-verb-fold/03-02-t1-red.json` and verify it:
`gsd_run check tdd-red-evidence .planning/phases/03-verb-fold/03-02-t1-red.json --raw` → verdict
`RED_EVIDENCE_OK` (reason `target_test_failed`); any `INVALID_RED` verdict means the RED is a build
error or a vacuous failure — fix the test identity, never proceed. Then commit exactly
`internal/cli/query_cli_test.go`, `internal/cli/notice_test.go` and `internal/cli/search.go` (the
placeholder declaration only — `git diff --cached -- internal/cli/search.go` must show a single added
`func renderFullLine(n *schema.Node) string { return "" }` with its one-line doc comment and nothing
else; if the `schema` import is new, that import line too) as
`test(03-02): add failing search --full, shorthand and full-line render tests` (no CI-skip marker).
`GOTOOLCHAIN=go1.26.6 go build ./...` is green at that commit; `go test ./internal/cli/` is red by
design. The evidence record is a `.planning/` artefact — it goes into the plan-completion
`docs(03-02):` commit with the SUMMARY, never into the RED or feat! commit.

GREEN. Edit `search.go` only:
  - Move `resolveStartPath` and `writeJSONLine` from `query.go` into `search.go` verbatim (their doc
    comments too) and add the `os` import; delete them from `query.go` (same package, so `query.go`
    still compiles until Task 3 removes it).
  - Add `var full bool`; register `cmd.Flags().BoolVar(&full, "full", false, "return full node records (signature, qualified name) instead of locations")`;
    replace the three shorthand-less registrations with `StringVarP(&kind, "kind", "k", …)`,
    `IntVarP(&limit, "limit", "l", …)`, `BoolVarP(&jsonOut, "json", "j", …)` (usage strings unchanged);
    keep `StringVarP(&path, "path", "p", …)`.
  - Change `Short` to `Lexically search symbol names/qualified names` (the "(locations only)" suffix
    is no longer true; `--full`'s usage string says what the flag adds).
  - In RunE, after `defer closer.Close()`, branch on `full`: the `--full` tail is `query.go`'s tail
    verbatim — `eng.Query(args[0], kind, limit)`, then `if jsonOut { query.MarshalQueryJSON → writeJSONLine }`,
    then `out := cmd.OutOrStdout()`, the WORK-02 notice `fmt.Fprint(out, query.WorktreeNotice(eng.WorktreeMismatch(cmd.Context())))`
    in exactly the position the default branch uses (after the `--json` early return — D-02), then per
    node: the existing `"%s (%s) %s:%d\n"` line followed by `fmt.Fprintln(out, renderFullLine(n))`.
    The default tail below it is untouched.
  - Implement `renderFullLine`: `"    " + n.QualifiedName` when `n.Signature == ""`, else
    `"    " + n.QualifiedName + "  " + n.Signature` — plain concatenation, no `%q`, no `%-*s`.
  - Update the file's doc comment: `search` is the one lexical-search verb; `--full` (VERB-01, D-01/D-03)
    selects the full-record shape that `query` used to render; the WORK-02 notice sits in both
    human branches at the same position (D-02).

Run the three new tests plus `TestSearchCmd` and `TestNotice*` — all green; then
`GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1` (the `query` command still exists at this
point, so the whole package must still be green) and `gofmt -l internal/cli/` (empty) and
`GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/`.

Do NOT commit the GREEN state at the end of this task — D-15 requires one `feat(cli)!:` commit,
made in Task 3. Leave the working tree green and dirty (only `search.go` and `query.go` modified;
the test files are already committed in the RED commit and must not need further edits — if GREEN
reveals a test defect, fix it in the working tree and it lands as `M` in the feat! commit, recorded
in the SUMMARY).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestSearchFullCmd|TestRenderFullLine|TestSearchFlagShortForms|TestSearchCmd|TestNoticeOnWorktreeMismatch|TestNoticeAbsentOnCleanTree|TestNoticeSuppressedInJSON' -v 2>&1 | tee /tmp/03-02-t1.txt | tail -3; rg -q '^ok\s' /tmp/03-02-t1.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/03-02-t1.txt && test "$(rg -c '^--- PASS: Test(SearchFullCmd|RenderFullLine|SearchFlagShortForms|SearchCmd|NoticeOnWorktreeMismatch|NoticeAbsentOnCleanTree|NoticeSuppressedInJSON) ' /tmp/03-02-t1.txt)" = "7" && rg -q -- '--- PASS: TestNoticeOnWorktreeMismatch/search_--full' /tmp/03-02-t1.txt && rg -q -- '--- PASS: TestNoticeSuppressedInJSON/search_--full' /tmp/03-02-t1.txt && echo "7 top-level tests PASS incl. search --full notice rows"</automated>
    <fails_when>non-zero exit, a `--- FAIL`, no `ok` line, a `--- PASS` count other than 7 for the seven named top-level tests, or no `search_--full` subtest under the notice tests — a new test is missing/red, or the `query` notice rows were not retargeted</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build -o /tmp/03-02-t1-bin ./cmd/codegraph && F=$(mktemp -d) && cp -R internal/indexer/testdata/gofixture/. "$F/" && /tmp/03-02-t1-bin init "$F" >/dev/null 2>&1 && /tmp/03-02-t1-bin search Alpha -p "$F" > /tmp/03-02-def.txt && /tmp/03-02-t1-bin search Alpha -p "$F" --full > /tmp/03-02-full.txt && test "$(cat /tmp/03-02-full.txt)" = "$(printf 'Alpha (function) pkga/pkga.go:13\n    Alpha  () int')" && cmp -s /tmp/03-02-def.txt <(rg '^\S' /tmp/03-02-full.txt) && /tmp/03-02-t1-bin search pkga -p "$F" --full | rg -q '^    example\.com/gofixture/pkga$' && ! /tmp/03-02-t1-bin search pkga -p "$F" --full | rg -q ' $' && test "$(/tmp/03-02-t1-bin search zzz-no-such-symbol -p "$F" --full --json)" = "[]" && test -z "$(/tmp/03-02-t1-bin search zzz-no-such-symbol -p "$F" --full)" && cmp -s <(/tmp/03-02-t1-bin search main -p "$F" --full -k function -l 1 -j) <(/tmp/03-02-t1-bin search main -p "$F" --full --kind function --limit 1 --json) && /tmp/03-02-t1-bin search main -p "$F" --full -j | rg -q '^\[\{"node":' && /tmp/03-02-t1-bin search main -p "$F" -j | rg -q '^\[\{"name":' && rm -rf "$F" && echo "binary-level D-01/D-03/D-04 checks pass"</automated>
    <fails_when>non-zero exit or no `binary-level … pass` line — the two-line render is not exactly `Alpha (function) pkga/pkga.go:13` + `    Alpha  () int`, the `^\S` filter of `--full` is not byte-identical to default output, the empty-Signature line is wrong or has trailing whitespace, zero hits do not give `[]` / empty, a shorthand form differs from its long form, or `-j` selects the wrong JSON shape</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test -z "$(gofmt -l internal/cli/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/ && rg -c 'func resolveStartPath|func writeJSONLine|func renderFullLine' internal/cli/search.go && ! rg -q 'func resolveStartPath|func writeJSONLine' internal/cli/query.go && rg -q 'StringVarP\(&kind, "kind", "k"' internal/cli/search.go && rg -q 'IntVarP\(&limit, "limit", "l"' internal/cli/search.go && rg -q 'BoolVarP\(&jsonOut, "json", "j"' internal/cli/search.go && rg -q 'BoolVar\(&full, "full"' internal/cli/search.go && test "$(rg -c 'query\.WorktreeNotice\(eng\.WorktreeMismatch' internal/cli/search.go)" = "2" && rg -q 'eng\.Query\(' internal/cli/search.go && rg -q 'eng\.Search\(' internal/cli/search.go && rg -q 'json\.Marshal\(locs\)' internal/cli/search.go && rg -q 'query\.MarshalQueryJSON\(' internal/cli/search.go && ! rg -q 'func TestQueryCmd' internal/cli/query_cli_test.go && SINCE="$(git log -1 --format=%H -- .planning/phases/03-verb-fold/03-MUTATION-LOG.md)" && test "$(git log --format=%H "$SINCE..HEAD" -E --grep='^test\(03-02\): ' -- internal/cli | wc -l | tr -d ' ')" = "1" && R="$(git log -1 --format=%H "$SINCE..HEAD" -E --grep='^test\(03-02\): ')" && test "$(git show --format= --name-only "$R" | sort | tr '\n' ' ')" = "internal/cli/notice_test.go internal/cli/query_cli_test.go internal/cli/search.go " && test "$(git show --format= "$R" -- internal/cli/search.go | rg -c '^\+func renderFullLine')" = "1" && test "$(git show --format= "$R" -- internal/cli/search.go | rg -c '^\+[^+]' )" -le 4 && test "$(git log --format=%H "$SINCE..HEAD" -E --invert-grep --grep='^test\(03-02\): ' -- internal cmd docs | wc -l | tr -d ' ')" = "0" && test "$(git status --porcelain -- internal cmd docs | awk '{print $2}' | sort | tr '\n' ' ')" = "internal/cli/query.go internal/cli/search.go " && echo "search.go shape ok; one test(03-02) RED commit (tests + placeholder only); GREEN dirty in exactly search.go + query.go; no other code commit"</automated>
    <fails_when>non-zero exit or a helper count of 0 — gofmt/vet complain, the helpers were not relocated, a shorthand/`--full` registration is missing, the notice call is not present exactly twice (one per human branch), one of the two engine paths or JSON shapes is gone, `TestQueryCmd` survived, the RED commit is absent or not exactly one, the RED commit's file set is not exactly notice_test.go + query_cli_test.go + search.go, its search.go hunk is not the single placeholder declaration (no `+func renderFullLine` line or more than 4 added lines), a non-`test(03-02)` commit touching internal/cmd/docs landed since 03-01 (GREEN must not be committed here), or the dirty set under internal/cmd/docs is not exactly query.go + search.go</fails_when>
  </verify>
  <acceptance_criteria>
    - RED recorded and committed: the SUMMARY names the `--- FAIL:` lines observed for `TestSearchFullCmd`, `TestRenderFullLine`, `TestSearchFlagShortForms` and the `search --full` notice subtests before the implementation; `03-02-t1-red.json` verified `RED_EVIDENCE_OK` by `check tdd-red-evidence`; one `test(03-02): add failing search --full, shorthand and full-line render tests` commit exists whose file set is exactly `query_cli_test.go`, `notice_test.go`, `search.go` (placeholder declaration only).
    - `search.go` registers `--full` (bool), `-k/--kind`, `-l/--limit`, `-j/--json`, `-p/--path`; `Short` is `Lexically search symbol names/qualified names`; RunE has the `--full` tail (`eng.Query`, `MarshalQueryJSON`, notice, two-line render) above the untouched default tail; `renderFullLine` implements D-01's exact whitespace; `resolveStartPath`/`writeJSONLine` live in `search.go`.
    - On the gofixture binary: `search Alpha --full` prints exactly `Alpha (function) pkga/pkga.go:13` then `    Alpha  () int`; `search pkga --full` prints `    example.com/gofixture/pkga` with no trailing whitespace; `--full --json` on zero hits prints `[]`; `-k/-l/-j` equal their long forms.
    - `TestSearchCmd`, the three new tests and the three notice tests (each with a `search --full` subtest) pass; `go test ./internal/cli/ -count=1` is green with the live `query` still registered.
    - No GREEN commit made; after the RED commit only `search.go` and `query.go` are dirty under `internal/`.
  </acceptance_criteria>
  <done>`search --full` works end-to-end — engine, envelope, two-line render, notice, shorthands — proven by RED-then-GREEN tests and a binary run on the fixture; committed later by Task 3 as part of the single `feat(cli)!:` commit (D-15).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `daemon unlock` (verbatim move) + the D-08 message retargets, RED first on `index_lock_test.go` and a net-new `TestDaemonUnlockCmd` (RED committed as `test(03-02):`; GREEN working tree only, no commit)</name>
  <files>internal/cli/daemon.go, internal/cli/daemon_test.go, internal/cli/index.go, internal/daemon/lock.go, internal/cli/index_lock_test.go, internal/cli/root.go</files>
  <read_first>
    - internal/cli/unlock.go (all 43 lines) — the body that moves verbatim; note it registers zero flags and resolves the path positionally via `targetRoot(args)` (init.go:91–96).
    - internal/cli/daemon.go lines 35–94 — `newDaemonCmd`, the non-persistent `-p` at line 90, `cmd.AddCommand(newDaemonStartCmd(), newDaemonStopCmd())` at line 91; `newDaemonStartCmd`/`newDaemonStopCmd` further down are the constructor idiom to mirror.
    - internal/cli/daemon_test.go lines 241–267 — `TestDaemonStopCmd_DispatchesToStopMatching` (the dispatch-test shape; `fakeHome`, `execCmd`).
    - internal/daemon/lock.go lines 41–45 (`lockInfo{PID, StartedAt}` JSON fields `pid`/`startedAt`), 47–49 (`lockPath` = `<codegraphDir>/daemon.lock`), 82–95 (`isStale`), 200–210 (the `ErrLockLive` message at line 208), 274–295 (`Unlock` and its doc comment naming the old verb at 274; the `no lock present at %s — nothing to do` / `removed stale lock (pid=%d)` messages).
    - internal/daemon/lock_test.go lines 60–70 — `deadPID` (spawn `os.Args[0] -test.run=^$`, take its exited pid) — replicate in `daemon_test.go` (package `cli` cannot import a `_test.go` helper).
    - internal/cli/index.go lines 123–128 — the `ErrStoreLocked` comment and message.
    - internal/cli/index_lock_test.go lines 88–128 — `TestIndexForceRefusesWhileStoreIsHeld`; the three `strings.Contains` assertions at 120–128.
    - internal/cli/root.go lines 1–16 and 33–43 — the package and constructor doc comments that describe `unlock` as a live top-level verb.
    - .planning/phases/03-verb-fold/03-PATTERNS.md § "`internal/cli/daemon.go` … gains `newDaemonUnlockCmd`" and § "D-08".
  </read_first>
  <behavior>
    - TestIndexForceRefusesWhileStoreIsHeld: the refusal error names `codegraph daemon stop` (unchanged) and `codegraph daemon unlock`; the former "must NOT name daemon unlock" assertion is deleted (that verb now exists).
    - TestDaemonUnlockCmd/"absent lock is a clean no-op": `daemon unlock <tmpdir>` → err nil, stdout is `no lock present at <tmpdir>/.codegraph/daemon.lock — nothing to do`.
    - TestDaemonUnlockCmd/"dead-pid lock is removed": write `{"pid":<deadPID>,"startedAt":"<now RFC3339>"}` to `<tmpdir>/.codegraph/daemon.lock` → `daemon unlock <tmpdir>` → err nil, stdout `removed stale lock (pid=<deadPID>)`, the file no longer exists.
    - TestDaemonUnlockCmd/"at most one positional arg": `daemon unlock a b` → err non-nil.
    - TestDaemonUnlockCmd/"no flags of its own (identical to the old verb)": `daemon unlock -p <tmpdir>` → err non-nil whose text contains `unknown shorthand flag`.
  </behavior>
  <action>
RED first, in this order, recording each `--- FAIL:` line for the SUMMARY:
  1. In `index_lock_test.go` change the second assertion to
     `!strings.Contains(err.Error(), "codegraph daemon unlock")` with a message that says the error
     must name `codegraph daemon unlock` (VERB-04/D-08), and delete the third assertion block
     (the one forbidding `daemon unlock`). Run
     `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestIndexForceRefusesWhileStoreIsHeld$' -v`
     → `--- FAIL: TestIndexForceRefusesWhileStoreIsHeld` (the message still says the old verb).
  2. Add `TestDaemonUnlockCmd` to `daemon_test.go` per `<behavior>` (a local `deadPID(t)` helper as in
     lock_test.go; write the lockfile with `os.MkdirAll(<tmp>/.codegraph)` + `os.WriteFile` of the
     JSON). Run `-run 'TestDaemonUnlockCmd$' -v` → `--- FAIL` (`unknown command "unlock" for "codegraph daemon"`).
  3. RED COMMIT (TDD gate). Persist the evidence record to
     `.planning/phases/03-verb-fold/03-02-t2-red.json` and verify
     `gsd_run check tdd-red-evidence .planning/phases/03-verb-fold/03-02-t2-red.json --raw` →
     `RED_EVIDENCE_OK`. Commit exactly `internal/cli/index_lock_test.go` and `internal/cli/daemon_test.go`
     as `test(03-02): add failing daemon unlock and lock-message tests` (no CI-skip marker). Both files
     compile against the unchanged tree — the failures are assertion failures, never build errors.
     Task 1's GREEN edits to `search.go`/`query.go` stay unstaged and uncommitted (stage the two test
     files by path; never `git add -A`).

GREEN:
  - `daemon.go`: add `newDaemonUnlockCmd()` — `unlock.go`'s `newUnlockCmd` body moved verbatim
    (`Use: "unlock [path]"`, `Short: "Clear a stale daemon lock"`, `Args: cobra.MaximumNArgs(1)`, the
    identical RunE: `targetRoot(args)` → `filepath.Join(root, codegraphDirName)` → `daemon.Unlock` →
    `fmt.Fprintln(cmd.OutOrStdout(), msg)`), carrying over `unlock.go`'s doc comment with one added
    sentence: moved verbatim under `daemon` (Phase 3 VERB-04, D-07). Register it:
    `cmd.AddCommand(newDaemonStartCmd(), newDaemonStopCmd(), newDaemonUnlockCmd())`. Add no flag; the
    parent's `-p` is non-persistent and must stay that way. Update `newDaemonCmd`'s `Long` string to
    mention `daemon unlock [path]` (clear a stale lock left by a crash) alongside `daemon start`/`stop`.
    Leave `unlock.go` in place until Task 3 replaces it — both paths coexist until then.
  - `internal/daemon/lock.go` line 208: the `ErrLockLive` message's remedy becomes
    `run \`codegraph daemon unlock\` once it has exited`; line 274's doc comment becomes
    `Unlock implements \`codegraph daemon unlock\`'s engine (SYNC-05; moved under daemon in Phase 3 VERB-04)`.
  - `internal/cli/index.go` line 128: the remedy becomes `run \`codegraph daemon unlock\` once it has exited`;
    replace the two comment lines at 126–127 with one that says `daemon unlock` is the live verb
    (Phase 3 VERB-04, D-08). Nothing else in the switch changes.
  - `root.go`: rewrite only the prose in the package doc comment (lines 6–8) and `newRootCmd`'s doc
    comment (lines 34–37) so they describe the surface after the fold — `search` (with `--full`) is
    the lexical-search verb, `daemon start|stop|unlock` is the daemon lifecycle, and `query`/`unlock`
    remain registered only as hidden rename stubs removed in v0.15.0 (Phase 3, VERB-03/VERB-04).
    The `AddCommand` list itself is not edited.

Run `-run 'TestIndexForceRefusesWhileStoreIsHeld$|TestDaemonUnlockCmd$' -v` → both `--- PASS`; then
`GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1` and
`GOTOOLCHAIN=go1.26.6 go test ./internal/daemon/ -count=1 -run 'TestUnlock|TestAcquire|TestIsStale' -v`
(green; report a `--- PASS` count). If the full `./internal/daemon/` package is also run and
`TestDaemonSharedWriter` times out, re-run it alone — it is the recorded load flake (03-01 Baseline
note), reported separately, not a fold regression.

Do NOT commit the GREEN state — Task 3 makes the single `feat(cli)!:` commit. The dirty set under
`internal/` is now exactly `search.go`, `query.go`, `daemon.go`, `index.go`, `root.go` and
`internal/daemon/lock.go`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestIndexForceRefusesWhileStoreIsHeld$|TestDaemonUnlockCmd$' -v 2>&1 | tee /tmp/03-02-t2.txt | tail -3; rg -q '^ok\s' /tmp/03-02-t2.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/03-02-t2.txt && test "$(rg -c '^--- PASS: Test(IndexForceRefusesWhileStoreIsHeld|DaemonUnlockCmd) ' /tmp/03-02-t2.txt)" = "2" && test "$(rg -c '^    --- PASS: TestDaemonUnlockCmd/' /tmp/03-02-t2.txt)" -ge 4 && echo "index-lock message and daemon unlock dispatch green"</automated>
    <fails_when>non-zero exit, a `--- FAIL`, a top-level `--- PASS` count other than 2, or fewer than 4 `TestDaemonUnlockCmd/` subtests passing — the message was not retargeted, `daemon unlock` is not registered, or a behaviour case (absent lock, dead-pid lock, arg cap, no `-p`) is missing or red</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(rg -c -w 'codegraph daemon unlock' internal/daemon/lock.go)" = "2" && ! rg -q -w 'codegraph unlock' internal/daemon/lock.go && test "$(rg -c -w 'codegraph daemon unlock' internal/cli/index.go)" -ge 1 && ! rg -q -w 'codegraph unlock' internal/cli/index.go && ! rg -q -w 'codegraph unlock' internal/cli/index_lock_test.go && rg -q -w 'codegraph daemon unlock' internal/cli/index_lock_test.go && ! rg -q 'must NOT name' internal/cli/index_lock_test.go && rg -q 'func newDaemonUnlockCmd\(\)' internal/cli/daemon.go && rg -q 'newDaemonStartCmd\(\), newDaemonStopCmd\(\), newDaemonUnlockCmd\(\)' internal/cli/daemon.go && rg -q 'Use:\s+"unlock \[path\]"' internal/cli/daemon.go && rg -q 'cobra\.MaximumNArgs\(1\)' internal/cli/daemon.go && test "$(rg -c 'daemon\.Unlock\(codegraphDir\)' internal/cli/daemon.go)" = "1" && test "$(rg -c 'daemon\.Unlock\(codegraphDir\)' internal/cli/unlock.go)" = "1" && ! rg -q 'PersistentFlags\(\)' internal/cli/daemon.go && test -z "$(gofmt -l internal/cli/ internal/daemon/)" && echo "D-07/D-08 source shape ok"</automated>
    <fails_when>non-zero exit or a printed count other than expected — an old-verb message survives in `lock.go`/`index.go`/`index_lock_test.go` (or the obsolete must-NOT assertion does), the `daemon daemon-unlock` constructor/registration/Use/Args are not as specified, the RunE body is not the same `daemon.Unlock(codegraphDir)` call in both files (verbatim move), a persistent flag appeared on `daemon`, or gofmt complains</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/daemon/ -count=1 -run 'TestUnlock|TestAcquire|TestIsStale' -v 2>&1 | tee /tmp/03-02-daemon.txt | tail -2; rg -q '^ok\s' /tmp/03-02-daemon.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/03-02-daemon.txt && test "$(rg -c '^--- PASS' /tmp/03-02-daemon.txt)" -ge 4 && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 2>&1 | tail -1 | rg -q '^ok\s' && git diff --quiet -- internal/mcp testdata/wireoracle web && SINCE="$(git log -1 --format=%H -- .planning/phases/03-verb-fold/03-MUTATION-LOG.md)" && test "$(git log --format=%H "$SINCE..HEAD" -E --grep='^test\(03-02\): ' -- internal/cli | wc -l | tr -d ' ')" = "2" && R="$(git log -1 --format=%H "$SINCE..HEAD" -E --grep='^test\(03-02\): add failing daemon unlock')" && test "$(git show --format= --name-only "$R" | sort | tr '\n' ' ')" = "internal/cli/daemon_test.go internal/cli/index_lock_test.go " && test "$(git log --format=%H "$SINCE..HEAD" -E --invert-grep --grep='^test\(03-02\): ' -- internal cmd docs | wc -l | tr -d ' ')" = "0" && test "$(git status --porcelain -- internal cmd docs | awk '{print $2}' | sort | tr '\n' ' ')" = "internal/cli/daemon.go internal/cli/index.go internal/cli/query.go internal/cli/root.go internal/cli/search.go internal/daemon/lock.go " && echo "daemon+cli packages green; two test(03-02) RED commits (second = daemon_test.go + index_lock_test.go only); GREEN dirty in exactly the six source files; mcp/wireoracle/web untouched"</automated>
    <fails_when>non-zero exit, a `--- FAIL`, a `--- PASS` count below 4 in the daemon lock tests, a red `./internal/cli/` package, a change under `internal/mcp`/`testdata/wireoracle`/`web`, fewer or more than two `test(03-02)` RED commits since 03-01, a second RED commit whose file set is not exactly daemon_test.go + index_lock_test.go, a non-`test(03-02)` commit touching internal/cmd/docs since 03-01 (GREEN must not be committed here), or a dirty set under internal/cmd/docs other than the six named source files</fails_when>
  </verify>
  <acceptance_criteria>
    - RED recorded and committed: `--- FAIL: TestIndexForceRefusesWhileStoreIsHeld` (assertion flipped before the message changed) and `--- FAIL: TestDaemonUnlockCmd` (before registration) are named in the SUMMARY; `03-02-t2-red.json` verified `RED_EVIDENCE_OK`; one `test(03-02): add failing daemon unlock and lock-message tests` commit exists containing exactly `index_lock_test.go` and `daemon_test.go`.
    - `daemon.go` has `newDaemonUnlockCmd()` with `Use: "unlock [path]"`, `MaximumNArgs(1)`, the verbatim RunE, no flags, registered after `newDaemonStopCmd()`; `daemon`'s `-p` stays non-persistent.
    - `lock.go` (message + doc comment) and `index.go` (message + comment) say `codegraph daemon unlock`; `index_lock_test.go` expects it and no longer forbids it.
    - `root.go`'s doc prose describes the post-fold surface; its `AddCommand` list is byte-unchanged.
    - `TestDaemonUnlockCmd` passes all four subtests; `./internal/cli/` and the daemon lock tests are green; no GREEN commit made.
  </acceptance_criteria>
  <done>`codegraph daemon unlock [path]` behaves exactly as the old top-level verb did, every live message points at it, and both facts are pinned by tests that were red first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The hidden rename stubs (RED first), the allowlist lines, the reviewed reference regeneration, and the single `feat(cli)!:` commit with its BREAKING CHANGE footer — then the before/after byte-identity proof</name>
  <files>internal/cli/renamed.go, internal/cli/renamed_test.go, internal/cli/query.go, internal/cli/unlock.go, internal/cli/testdata/cli-reference-allowlist.txt, docs/CLI-REFERENCE.md</files>
  <reversibility rating="costly">D-15: the `feat(cli)!:` commit and its `BREAKING CHANGE:` footer are what release-please copies into the CHANGELOG and what the v0.15.0 removal schedule hangs on; amending it after it is pushed rewrites published history.</reversibility>
  <read_first>
    - internal/cli/man.go lines 46–51 — the `Hidden: true` precedent and its doc-comment style.
    - internal/cli/query.go (post-Task-1: only `newQueryCmd` remains) and internal/cli/unlock.go — both files are deleted by this task; nothing in them is still needed (helpers moved in Task 1, body moved in Task 2).
    - internal/cli/root.go lines 55–61 — `newQueryCmd()` and `newUnlockCmd()` stay in the `AddCommand` list; `renamed.go` must define both names.
    - internal/cli/testdata/cli-reference-allowlist.txt — the header grammar (`<full command path>[ --flag]<TAB><reason>`; a bare command path covers a HIDDEN command's flags) and the existing `codegraph man` line (a literal TAB separates key and reason).
    - internal/cli/cli_reference_test.go lines 147–275 — `TestEveryRegisteredFlagIsAccountedFor`: hidden command → `--help` (from `InitDefaultHelpFlag`) needs a command-level entry; a stale entry fails; today it logs `walked 36 commands … 115 flags … 1 accepted via testdata/cli-reference-allowlist.txt`.
    - Taskfile.yml § `docs:cli` (regenerates in place via `tools/clidoc`) and § `docs:cli:drift`.
    - docs/CLI-REFERENCE.md lines 28–46 (the command index bullets), 293–362 (the `daemon` family sections — `## codegraph daemon start` is the section-title shape `## codegraph daemon unlock` will take), 604–620 and 853–870 (the `query`/`unlock` sections that disappear).
    - .github/workflows/pr-title.yml line 47 — the regex `^(feat|fix|perf|refactor|docs|chore|ci|test|build|revert)(\([a-z0-9_-]+\))?!?: .+`.
    - .planning/phases/03-verb-fold/03-CONTEXT.md D-05, D-06 (the four stderr strings, verbatim, em dash included), D-09, D-11, D-12, D-13, D-15; 03-RESEARCH.md Pitfall 1 (`feat(cli)!:`), Pitfall 2 (`DisableFlagParsing`), Pattern 2.
    - .planning/phases/03-verb-fold/03-MUTATION-LOG.md § Baseline — the pre-fold digests this task's byte-identity check must reproduce.
  </read_first>
  <behavior>
    - TestQueryStub: on an initialised fixture, `query main -p dir` → err non-nil whose text contains `renamed to "search --full"`; stdout `""`; stderr is exactly `"query" has been renamed to "search --full" — run: codegraph search --full <term>` newline `the "query" stub is removed in the next minor release (v0.15.0)` newline. `query --json main -p dir` and `query` (no args) → the same stderr, same err, empty stdout (flags/args ignored — nothing is forwarded, no unknown-flag/arg error surfaces).
    - TestUnlockStub: with a dead-pid lockfile at `<tmp>/.codegraph/daemon.lock`, `unlock <tmp>` → err non-nil containing `renamed to "daemon unlock"`; stdout `""`; stderr exactly `"unlock" has been renamed to "daemon unlock" — run: codegraph daemon unlock [path]` newline `the "unlock" stub is removed in the next minor release (v0.15.0)` newline; the lockfile STILL EXISTS afterwards (nothing executed). `unlock --json <tmp>` → same.
    - Both stub commands report `Hidden == true` and `DisableFlagParsing == true` when looked up on `newRootCmd()` (`root.Find([]string{"query"})`), and neither has a deprecation string set.
  </behavior>
  <action>
RED first: create `internal/cli/renamed_test.go` with `TestQueryStub` and `TestUnlockStub` per
`<behavior>` (reuse Task 2's `deadPID` helper; in this test file refer to the commands as "the query
stub"/"the unlock stub" in messages rather than spelling the full old invocation — 03-04's census
allows hits only in `renamed.go` and the allowlist). Run
`GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestQueryStub$|TestUnlockStub$' -v`
→ both `--- FAIL` (the live commands succeed, print to stdout, and clear the stale lock). Record.
RED COMMIT (TDD gate): persist `.planning/phases/03-verb-fold/03-02-t3-red.json`, verify
`gsd_run check tdd-red-evidence .planning/phases/03-verb-fold/03-02-t3-red.json --raw` →
`RED_EVIDENCE_OK`, then commit exactly `internal/cli/renamed_test.go` as
`test(03-02): add failing rename-stub tests` (stage by path; Tasks 1–2's GREEN edits stay
uncommitted; no CI-skip marker).

GREEN:
  - Create `internal/cli/renamed.go` (package `cli`, imports `fmt` and cobra) defining
    `newQueryCmd()` and `newUnlockCmd()` — the SAME constructor names root.go already calls — each
    returning `&cobra.Command{ Use: "query" | "unlock", Short: …, Hidden: true, DisableFlagParsing: true, RunE: … }`
    whose RunE writes the two D-06 lines for that stub to `cmd.ErrOrStderr()` with `fmt.Fprintln`
    (verbatim, em dash U+2014, straight double quotes) and returns
    `fmt.Errorf(`codegraph: "query" has been renamed to "search --full"`)` /
    `fmt.Errorf(`codegraph: "unlock" has been renamed to "daemon unlock"`)` — nothing else: no engine,
    no filesystem, no forwarding, no `Args` validator (nil = arbitrary args, needed so a bare or
    over-supplied invocation still reaches RunE). `Short` strings: `Renamed to "search --full" — stub removed in v0.15.0`
    and `Renamed to "daemon unlock" — stub removed in v0.15.0`. The file's doc comment (man.go style)
    states, per stub, the old invocation in backticks (e.g. `codegraph query <term>`), the replacement,
    why `Hidden: true` (out of the reference/completions/man — VERB-06) and why `DisableFlagParsing: true`
    (Pitfall 2: without it `query --json x` dies in pflag before RunE), that cobra's deprecation field is
    deliberately unused (it would still run the command — D-05), that the non-nil error is what makes
    main.go's single exit path fire (no exit call here), and the v0.15.0 removal (D-09).
  - `git rm internal/cli/query.go internal/cli/unlock.go`.
  - Append to `internal/cli/testdata/cli-reference-allowlist.txt` (TAB-separated, one line each):
    `codegraph query<TAB>hidden rename stub for `search --full` (VERB-03); removed in v0.15.0`
    `codegraph unlock<TAB>hidden rename stub for `daemon unlock` (VERB-04); removed in v0.15.0`
  - `task docs:cli` (regenerates `docs/CLI-REFERENCE.md` in place). REVIEW `git diff -- docs/CLI-REFERENCE.md`
    (D-11: reviewed diff, never a blanket accept). Expected and ONLY expected changes: (1) the
    `* [codegraph query](#codegraph-query)` and `* [codegraph unlock](#codegraph-unlock)` index bullets
    and the `## codegraph query` / `## codegraph unlock` sections are gone; (2) the `search` bullet's
    Short text changes and the `## codegraph search` Options block gains `--full` and shows `-j, --json`,
    `-k, --kind string`, `-l, --limit int`; (3) the `## codegraph daemon` SEE ALSO gains
    `* [codegraph daemon unlock](#codegraph-daemon-unlock)` and a new `## codegraph daemon unlock` section
    (usage `codegraph daemon unlock [path] [flags]`, Options: only `-h, --help`) appears after
    `## codegraph daemon stop`. Anything else in the diff means the fold changed more than it should —
    stop and investigate before staging. Write the per-file diff summary (reference + every source file)
    into the SUMMARY for the maintainer's end-of-phase review (D-13).
  - Gates, all green: `task docs:cli:drift`;
    `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestEveryRegisteredFlagIsAccountedFor$' -v`
    (log line: `walked 37 commands … 3 accepted via testdata/cli-reference-allowlist.txt` — 36+1 for
    `daemon unlock`, 1+2 allowlist entries); `GOTOOLCHAIN=go1.26.6 go build ./...`;
    `GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/ ./internal/daemon/`; `gofmt -l` empty;
    `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1` (all packages under it).
  - Binary check: `GOTOOLCHAIN=go1.26.6 go build -o /tmp/03-02-head-bin ./cmd/codegraph`;
    `/tmp/03-02-head-bin query main; echo exit=$?` → stderr has the two D-06 lines then main.go's echo of
    the returned error, stdout empty, `exit=1`; same for `unlock`; `daemon unlock <emptytmp>` → exit 0.

THE COMMIT (the only non-`test(03-02)` commit this plan makes). Stage exactly: `internal/cli/search.go`,
`internal/cli/daemon.go`, `internal/cli/index.go`, `internal/daemon/lock.go`, `internal/cli/root.go`,
`internal/cli/renamed.go`, `internal/cli/testdata/cli-reference-allowlist.txt`, `docs/CLI-REFERENCE.md`,
and the two deletions — 1 added, 7 modified, 2 deleted. The five test files
(`query_cli_test.go`, `notice_test.go`, `daemon_test.go`, `index_lock_test.go`, `renamed_test.go`)
are already in the RED commits; stage one of them here ONLY if GREEN required an edit to it, in
which case it appears as `M` (never `A`) and the SUMMARY names the edit. Stage by path; never
`git add -A`. Subject (verbatim, D-15 as corrected by RESEARCH
Pitfall 1 — scope before bang):

  feat(cli)!: fold query into search --full and unlock into daemon unlock

Body: three short paragraphs — what `search --full` is (same ranking, `MarshalQueryJSON` envelope
under `--json`, two-line human render, `-k/-l/-j` shorthands; default `search` byte-identical),
what moved (`unlock` → `daemon unlock`, identical flags/behaviour; the two live messages now name
it), and the stubs (hidden, stderr rename message, exit 1, nothing executed) — then the footer, as
the last paragraph, beginning at column 0:

  BREAKING CHANGE: `codegraph query <term>` is removed in favour of `codegraph search --full <term>`, and `codegraph unlock [path]` is removed in favour of `codegraph daemon unlock [path]`. Both old verbs remain as hidden stubs that print the rename to stderr and exit 1 without executing anything; the `query`/`unlock` stubs are removed in v0.15.0 (the next minor).

Any authorship trailer goes AFTER the footer paragraph. No `[ci skip]`. Do not amend afterwards.

After the commit, the byte-identity proof (D-01/D-03): resolve the feat commit `F` by subject,
`git worktree add --detach /tmp/03-02-prefold "$F^"`, build `/tmp/03-02-prefold-bin` there and
`/tmp/03-02-head-bin` from HEAD, remove the worktree, and `cmp` the two binaries' output for the
five repo-index invocations and the four gofixture invocations 03-01's Baseline recorded (same
index, same instant). Record the digests and the `byte-identical on 9 invocations` line in the
SUMMARY next to 03-01's "before" digests.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/ -count=1 -run 'TestQueryStub$|TestUnlockStub$|TestEveryRegisteredFlagIsAccountedFor$' -v 2>&1 | tee /tmp/03-02-t3.txt | tail -3; rg -q '^ok\s' /tmp/03-02-t3.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/03-02-t3.txt && test "$(rg -c '^--- PASS: Test(QueryStub|UnlockStub|EveryRegisteredFlagIsAccountedFor) ' /tmp/03-02-t3.txt)" = "3" && rg -q 'walked 37 commands \(hidden included\), inspected [0-9]+ flags: [0-9]+ accepted via \.\./\.\./docs/CLI-REFERENCE\.md, 3 accepted via testdata/cli-reference-allowlist\.txt' /tmp/03-02-t3.txt && echo "stubs + flag accounting green (37 commands, 3 allowlisted)"</automated>
    <fails_when>non-zero exit, a `--- FAIL`, a `--- PASS` count other than 3, or a `walked …` line that is not exactly 37 commands with 3 allowlist acceptances — a stub test is red, a stub's `--help` is unaccounted, an allowlist line is stale/missing, or `daemon unlock` is not registered</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test -f internal/cli/renamed.go && test ! -e internal/cli/query.go && test ! -e internal/cli/unlock.go && test "$(rg -c 'Hidden:\s+true' internal/cli/renamed.go)" = "2" && test "$(rg -c 'DisableFlagParsing:\s+true' internal/cli/renamed.go)" = "2" && rg -q 'func newQueryCmd\(\)' internal/cli/renamed.go && rg -q 'func newUnlockCmd\(\)' internal/cli/renamed.go && rg -q -F '"query" has been renamed to "search --full" — run: codegraph search --full <term>' internal/cli/renamed.go && rg -q -F 'the "query" stub is removed in the next minor release (v0.15.0)' internal/cli/renamed.go && rg -q -F '"unlock" has been renamed to "daemon unlock" — run: codegraph daemon unlock [path]' internal/cli/renamed.go && rg -q -F 'the "unlock" stub is removed in the next minor release (v0.15.0)' internal/cli/renamed.go && ! rg -q 'Deprecated' internal/cli/renamed.go && ! rg -q 'os\.Exit' internal/cli/renamed.go && ! rg -q 'query\.OpenAt|daemon\.Unlock|os\.(Remove|WriteFile|ReadFile)' internal/cli/renamed.go && test "$(rg -c '^codegraph (query|unlock)	hidden rename stub for `(search --full|daemon unlock)` \(VERB-0[34]\); removed in v0\.15\.0$' internal/cli/testdata/cli-reference-allowlist.txt)" = "2" && rg -q 'newQueryCmd\(\), newSearchCmd\(\)' internal/cli/root.go && rg -q 'newDaemonCmd\(\), newUnlockCmd\(\)' internal/cli/root.go && echo "stub file, allowlist and root registration shape ok"</automated>
    <fails_when>non-zero exit or a count other than 2 — a stub file/registration is missing, the old files survive, a D-06 string is not verbatim, the deprecation field / an exit call / engine or filesystem calls appear in the stub file, an allowlist line does not match D-12's exact grammar (TAB, reason with target + `removed in v0.15.0`), or root.go's constructor names changed</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task docs:cli:drift > /tmp/03-02-drift.txt 2>&1; s=$?; tail -1 /tmp/03-02-drift.txt; test $s -eq 0 && rg -q 'compared 1 generated file' /tmp/03-02-drift.txt && rg -q 'byte-identical to a fresh regeneration' /tmp/03-02-drift.txt && ! rg -q '^## codegraph query$|^## codegraph unlock$|\(#codegraph-query\)|\(#codegraph-unlock\)' docs/CLI-REFERENCE.md && rg -q '^## codegraph daemon unlock$' docs/CLI-REFERENCE.md && rg -q '^codegraph daemon unlock \[path\] \[flags\]$' docs/CLI-REFERENCE.md && rg -q '\(#codegraph-daemon-unlock\)' docs/CLI-REFERENCE.md && awk '/^## codegraph search$/{f=1} f && /^## codegraph serve$/{exit} f' docs/CLI-REFERENCE.md > /tmp/03-02-search-sec.txt && rg -q -- '--full' /tmp/03-02-search-sec.txt && rg -q -- '-j, --json' /tmp/03-02-search-sec.txt && rg -q -- '-k, --kind string' /tmp/03-02-search-sec.txt && rg -q -- '-l, --limit int' /tmp/03-02-search-sec.txt && rg -q -- '-p, --path string' /tmp/03-02-search-sec.txt && echo "reference regenerated: stubs absent, daemon unlock present, search options complete, drift green"</automated>
    <fails_when>non-zero exit or a missing line — `task docs:cli:drift` is red (the committed reference is not the generator's output, or `compared 1 generated file` did not print), a `query`/`unlock` section or bullet survived, the `daemon unlock` section/bullet is absent, or the `search` section lacks `--full` / a shorthand</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build ./... && test -z "$(gofmt -l internal/cli/ internal/daemon/)" && GOTOOLCHAIN=go1.26.6 go vet ./internal/cli/ ./internal/daemon/ && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1 2>&1 | tee /tmp/03-02-cli-all.txt | tail -6; ! rg -q '^(--- FAIL|FAIL)' /tmp/03-02-cli-all.txt && test "$(rg -c '^ok\s' /tmp/03-02-cli-all.txt)" -ge 5 && GOTOOLCHAIN=go1.26.6 go build -o /tmp/03-02-head-bin ./cmd/codegraph && (/tmp/03-02-head-bin query main > /tmp/03-02-q-out.txt 2> /tmp/03-02-q-err.txt; echo "exit=$?" > /tmp/03-02-q-exit.txt) && test ! -s /tmp/03-02-q-out.txt && test "$(cat /tmp/03-02-q-exit.txt)" = "exit=1" && test "$(sed -n 1p /tmp/03-02-q-err.txt)" = '"query" has been renamed to "search --full" — run: codegraph search --full <term>' && test "$(sed -n 2p /tmp/03-02-q-err.txt)" = 'the "query" stub is removed in the next minor release (v0.15.0)' && (/tmp/03-02-head-bin unlock --json /tmp > /tmp/03-02-u-out.txt 2> /tmp/03-02-u-err.txt; echo "exit=$?" > /tmp/03-02-u-exit.txt) && test ! -s /tmp/03-02-u-out.txt && test "$(cat /tmp/03-02-u-exit.txt)" = "exit=1" && test "$(sed -n 1p /tmp/03-02-u-err.txt)" = '"unlock" has been renamed to "daemon unlock" — run: codegraph daemon unlock [path]' && T=$(mktemp -d) && /tmp/03-02-head-bin daemon unlock "$T" | rg -q '^no lock present at ' && rm -rf "$T" && echo "build/vet/tests green; binary: stubs exit 1 with D-06 stderr, daemon unlock exit 0"</automated>
    <fails_when>non-zero exit or no closing line — the build/vet/gofmt/tests are red (fewer than 5 `ok` packages under internal/cli), a stub writes to stdout, exits 0, or prints anything other than the D-06 lines first on stderr (with flags/args present), or `daemon unlock` on an empty dir does not print the no-lock message</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && F=$(git log --format=%H --grep='^feat(cli)!: fold query into search --full and unlock into daemon unlock$' -1) && test -n "$F" && test "$(git log --format=%H --grep='^feat(' "$(git log -1 --format=%H -- .planning/phases/03-verb-fold/03-MUTATION-LOG.md)..HEAD" | wc -l | tr -d ' ')" = "1" && git log -1 --format=%s "$F" | grep -qE '^(feat|fix|perf|refactor|docs|chore|ci|test|build|revert)(\([a-z0-9_-]+\))?!?: .+' && git log -1 --format=%b "$F" | rg -q '^BREAKING CHANGE: ' && git log -1 --format=%b "$F" | rg -q -F 'removed in v0.15.0' && git log -1 --format=%b "$F" | rg -q -F 'search --full' && git log -1 --format=%b "$F" | rg -q -F 'daemon unlock' && ! git log -1 --format=%s%n%b "$F" | rg -q -i 'ci skip|skip ci' && git show --format= --name-status --no-renames "$F" | sort > /tmp/03-02-files.txt && test "$(rg -c '^D\s' /tmp/03-02-files.txt)" = "2" && rg -q '^D\s+internal/cli/query\.go$' /tmp/03-02-files.txt && rg -q '^D\s+internal/cli/unlock\.go$' /tmp/03-02-files.txt && test "$(rg -c '^A\s' /tmp/03-02-files.txt)" = "1" && rg -q '^A\s+internal/cli/renamed\.go$' /tmp/03-02-files.txt && for f in internal/cli/search.go internal/cli/daemon.go internal/cli/index.go internal/daemon/lock.go internal/cli/root.go internal/cli/testdata/cli-reference-allowlist.txt docs/CLI-REFERENCE.md; do rg -q "^M\s+${f}$" /tmp/03-02-files.txt || { echo "missing M $f"; exit 1; }; done && test "$(rg -c '^M\s' /tmp/03-02-files.txt)" -ge 7 && test "$(rg -c '^M\s' /tmp/03-02-files.txt)" -le 12 && ! rg -q '^[AD]\s+.*_test\.go$' /tmp/03-02-files.txt && ! rg -q 'internal/mcp|testdata/wireoracle|web/' /tmp/03-02-files.txt && SINCE="$(git log -1 --format=%H -- .planning/phases/03-verb-fold/03-MUTATION-LOG.md)" && test "$(git log --format=%H "$SINCE..HEAD" -E --grep='^test\(03-02\): ' | wc -l | tr -d ' ')" = "3" && test "$(git log --format=%H "$SINCE..HEAD" -E --invert-grep --grep='^test\(03-02\): ' -- internal cmd docs | wc -l | tr -d ' ')" = "1" && for R in $(git log --format=%H "$SINCE..HEAD" -E --grep='^test\(03-02\): '); do git merge-base --is-ancestor "$R" "$F" || { echo "RED commit $R is not before the feat commit"; exit 1; }; done && test -z "$(git status --porcelain -- internal docs cmd)" && echo "one feat(cli)!: commit, regex-conformant subject, BREAKING CHANGE footer, 1 A / 7-12 M / 2 D with no added/deleted test file, three test(03-02) RED commits all before it, tree clean"</automated>
    <fails_when>non-zero exit — the feat commit is absent, more than one `feat(` commit landed since 03-01, the subject fails pr-title.yml's regex, the `BREAKING CHANGE:` footer is missing or does not name `search --full`/`daemon unlock`/`removed in v0.15.0`, a CI-skip marker is present, the file set is not 1 added (renamed.go) + the seven named modified files (+ at most five `M` test files) + 2 deleted (query.go/unlock.go), a test file appears as `A` or `D` (its RED version was not committed first), the commit touched mcp/wireoracle/web, the `test(03-02)` RED commits since 03-01 are not exactly three or any of them is not an ancestor of the feat commit, a second non-RED commit touched internal/cmd/docs, or uncommitted edits remain under internal/docs/cmd</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && F=$(git log --format=%H --grep='^feat(cli)!: fold query into search --full and unlock into daemon unlock$' -1) && test -n "$F" && (git worktree remove --force /tmp/03-02-prefold >/dev/null 2>&1 || true) && git worktree add --detach /tmp/03-02-prefold "$F^" >/dev/null 2>&1 && (cd /tmp/03-02-prefold && GOTOOLCHAIN=go1.26.6 go build -o /tmp/03-02-prefold-bin ./cmd/codegraph) && GOTOOLCHAIN=go1.26.6 go build -o /tmp/03-02-head-bin ./cmd/codegraph && git worktree remove --force /tmp/03-02-prefold && git worktree prune && R=/Volumes/Code/github.com/seanb4t/codegraph-go && G=$(mktemp -d) && cp -R internal/indexer/testdata/gofixture/. "$G/" && /tmp/03-02-prefold-bin init "$G" >/dev/null 2>&1 && n=0 && for args in "search matchNodes -p $R" "search matchNodes -p $R --json" "search lexicalTier -p $R --limit 2" "search ValidateKind -p $R --kind function" "search ValidateKind -p $R --json --limit 5" "search Alpha -p $G" "search Alpha -p $G --json" "search pkga -p $G" "search helper -p $G --kind function"; do /tmp/03-02-prefold-bin $args > /tmp/03-02-a.txt 2>&1; /tmp/03-02-head-bin $args > /tmp/03-02-b.txt 2>&1; test -s /tmp/03-02-a.txt || { echo "EMPTY output for: $args"; exit 1; }; cmp -s /tmp/03-02-a.txt /tmp/03-02-b.txt || { echo "DIFF for: $args"; diff /tmp/03-02-a.txt /tmp/03-02-b.txt; exit 1; }; n=$((n+1)); done; rm -rf "$G"; ! git worktree list | rg -q '03-02-prefold' && echo "byte-identical on $n invocations (pre-fold $F^ vs HEAD)"</automated>
    <fails_when>non-zero exit, an `EMPTY output` or `DIFF for:` line, or a final count other than 9 — default `search`/`search --json` output changed between the pre-fold binary and HEAD on the same index (D-01/D-03 violated), a probe returned nothing (non-discriminating), or the scratch worktree was left behind</fails_when>
  </verify>
  <acceptance_criteria>
    - RED recorded and committed: `--- FAIL: TestQueryStub` and `--- FAIL: TestUnlockStub` named in the SUMMARY before `renamed.go` existed; `03-02-t3-red.json` verified `RED_EVIDENCE_OK`; one `test(03-02): add failing rename-stub tests` commit exists containing exactly `renamed_test.go`.
    - `internal/cli/renamed.go` defines `newQueryCmd`/`newUnlockCmd` with `Hidden: true`, `DisableFlagParsing: true`, the four D-06 lines verbatim, a returned non-nil error, and no deprecation field / exit call / engine or filesystem call; `query.go` and `unlock.go` are deleted; root.go's constructor names are unchanged.
    - The allowlist has exactly the two D-12 lines; `TestEveryRegisteredFlagIsAccountedFor` logs `walked 37 commands … 3 accepted via testdata/cli-reference-allowlist.txt`; `task docs:cli:drift` is green; the reference has no `query`/`unlock` section or bullet, a `## codegraph daemon unlock` section, and a `search` Options block with `--full`, `-j`, `-k`, `-l`, `-p`.
    - Binary: `query …`/`unlock …` exit 1 with empty stdout and the D-06 lines first on stderr regardless of flags; `daemon unlock <emptydir>` exits 0 with the no-lock message.
    - Exactly one non-`test(03-02)` commit since 03-01, subject `feat(cli)!: fold query into search --full and unlock into daemon unlock`, regex-conformant, `BREAKING CHANGE:` footer naming both replacements and `removed in v0.15.0`; file set 1 A (`renamed.go`) / 7 M (the seven named source, allowlist and reference files; up to five more `M` test files only if GREEN needed an edit) / 2 D (`query.go`, `unlock.go`); no `A`/`D` test file; the three `test(03-02)` RED commits are all ancestors of it; no CI-skip marker.
    - Byte-identity: 9/9 invocations `cmp`-identical between the `F^` binary and HEAD; the SUMMARY carries the digests beside 03-01's, plus the per-file diff summary (reference + sources) for D-13.
  </acceptance_criteria>
  <done>The verb fold is one green, regex-conformant `feat(cli)!:` commit whose stubs, tests, reference and allowlist all landed together, and default `search` is proven byte-identical to the pre-fold binary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell → `codegraph query|unlock` stubs | arbitrary args/flags reach RunE unparsed (`DisableFlagParsing`); the stub must do nothing with them |
| user shell → `codegraph daemon unlock [path]` | a positional path selects a `.codegraph/daemon.lock` to remove; the stale-vs-live decision stays in `daemon.Unlock` |
| `search --full` → `internal/query` engine → stdout | already-validated term/kind/limit; output now includes `Signature`/`QualifiedName` (previously exposed by `query`) |
| feat! commit → release-please / CHANGELOG | the subject and footer are parsed by machines at merge time; a malformed shape silently mis-categorises the release |
| scratch worktree + two binaries → the repo's own `.codegraph/` index | read-only `search` calls; the worktree is removed after use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Tampering | stub RunE executing partial logic (engine open, lock removal) before erroring | high | mitigate | RunE is two `Fprintln` + one `return fmt.Errorf`; verify negative-greps `query.OpenAt`/`daemon.Unlock`/`os.Remove|WriteFile|ReadFile` in `renamed.go` (paired with positive counts of `Hidden`/`DisableFlagParsing`); `TestUnlockStub` proves a stale lockfile survives; `TestQueryStub` proves empty stdout on an initialised index. |
| T-03-02-02 | Tampering | unparsed args forwarded or interpreted by the stub | medium | mitigate | Nothing reads `args`; tests run the stubs with `--json`, `-p x`, no args and assert identical behaviour. |
| T-03-02-03 | Elevation of Privilege | a second process-exit path bypassing `SilenceUsage`/`SilenceErrors` | medium | mitigate | No exit call in the stub (negative grep); exit 1 comes only from `cmd/codegraph/main.go`; binary check asserts `exit=1`. |
| T-03-02-04 | Denial of Service | `daemon unlock` removing a live daemon's lock | high | mitigate | Body moved verbatim — `daemon.Unlock`'s `isStale` guard and `TestUnlockStaleOnly` are unchanged; the CLI test removes only a dead-pid lock; verify asserts the same `daemon.Unlock(codegraphDir)` call in both files during the move. |
| T-03-02-05 | Information Disclosure | `search --full` prints `Signature`/`QualifiedName` | low | accept | Same data the removed `query` verb printed and the MCP `codegraph_node` tool returns; local index, local user. |
| T-03-02-06 | Repudiation | message-site tests flipped without a RED, or new tests passing vacuously | medium | mitigate | Each RED (`--- FAIL:` line) is observed and recorded before its GREEN; `go test -run` results are asserted by `--- PASS` counts, never bare exit codes (STATE standing rule). |
| T-03-02-07 | Tampering | malformed `feat!` subject/footer mis-categorising the release or breaking `pr-title.yml` | medium | mitigate | Subject checked against the workflow's own regex; `BREAKING CHANGE:` footer presence and content asserted; single-`feat(` invariant asserted. |
| T-03-02-08 | Tampering | blanket regenerate of `docs/CLI-REFERENCE.md` hiding an unintended surface change | high | mitigate | Diff reviewed against three named change groups; verify asserts the stubs' sections are gone, `daemon unlock`'s present, `search` options complete; `docs:cli:drift` green. |
| T-03-02-09 | Denial of Service | `protect-main` blocked by a `[ci skip]` marker | high | mitigate | Commit message grepped for CI-skip markers. |
| T-03-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install; no `go.mod` change (RESEARCH: Package Legitimacy Audit not applicable). |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1` green; `TestEveryRegisteredFlagIsAccountedFor` logs 37 commands / 3 allowlisted; `task docs:cli:drift` green.
- Binary: stubs exit 1 with D-06 stderr; `daemon unlock` works; `search --full` two-line render and envelope; shorthands equal long forms.
- Exactly one `feat(cli)!:` commit with the footer; byte-identity 9/9 against the pre-fold binary.
</verification>

<success_criteria>
- VERB-01, VERB-02, VERB-03, VERB-04 observable on the binary and pinned by RED-first tests.
- VERB-06's reference half (regenerated reference + two reasoned allowlist lines) green in the same commit.
- VERB-08's commit shape: one regex-conformant `feat(cli)!:` commit with a `BREAKING CHANGE:` footer naming v0.15.0.
</success_criteria>

## Artifacts this phase produces

- `internal/cli/search.go` — `--full`, `-k/-l/-j`, `renderFullLine`, relocated `resolveStartPath`/`writeJSONLine` (this plan)
- `internal/cli/renamed.go` (new: `newQueryCmd`, `newUnlockCmd` stubs); `internal/cli/query.go`, `internal/cli/unlock.go` deleted (this plan)
- `internal/cli/daemon.go` — `newDaemonUnlockCmd` (this plan)
- Tests: `internal/cli/renamed_test.go` (new), `TestDaemonUnlockCmd`, `TestSearchFullCmd`, `TestRenderFullLine`, `TestSearchFlagShortForms`, retargeted notice rows and `index_lock_test.go` assertion (this plan)
- `internal/daemon/lock.go`, `internal/cli/index.go` messages; `internal/cli/root.go` doc prose (this plan)
- `internal/cli/testdata/cli-reference-allowlist.txt` (+2), `docs/CLI-REFERENCE.md` regenerated (this plan)
- `03-MUTATION-LOG.md` families (03-01/03-03/03-04), `COVERAGE.md` (03-03), ROADMAP backlog row 999.5 (03-04)

## Flagged assumptions (probe rows left unresolved — surfaced, not dropped)

- VERB-02 (unclassified): the flag-parse test exercises each shorthand in one position (after the term). Assumption: pflag treats a shorthand identically before and after the positional term (interspersed parsing is cobra's default); not separately asserted.
- VERB-03 (unclassified): no caller is known to depend on the old verbs exiting 0; a `set -e` script that still runs `codegraph query` now stops at the stub (intended by D-05). Not verifiable from inside the repo.
- VERB-04 (unclassified): `daemon unlock` inherits nothing because `daemon` registers its `-p` via `Flags()` not `PersistentFlags()` (verified); the assumption that no persistent flag is added to `daemon` later is guarded only by the `! rg PersistentFlags` check in this plan.
- VERB-08 (unclassified): under the repo's squash-merge model the PR title, not this commit's subject, is what release-please parses (RESEARCH Pitfall 1). Assumption: the maintainer sets the PR title to the same `feat(cli)!:` subject at ship time — outside this phase's control; recorded for `/gsd-ship`.

<output>
Create `.planning/phases/03-verb-fold/03-02-SUMMARY.md` when done — include every RED `--- FAIL:` line observed, a `## TDD Gate Compliance` section listing the three `test(03-02):` RED commit SHAs, their `check tdd-red-evidence` verdicts and the feat! commit as the GREEN (per `gsd-core/references/tdd.md`), the per-file diff summary of `docs/CLI-REFERENCE.md` and each source file (D-13), the feat commit SHA and full message, and the nine byte-identity digests beside 03-01's baseline. Commit the three `03-02-t*-red.json` evidence records together with the SUMMARY in the plan-completion `docs(03-02):` commit.
</output>
