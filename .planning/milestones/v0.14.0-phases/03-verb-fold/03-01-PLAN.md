---
phase: 03-verb-fold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-verb-fold/03-MUTATION-LOG.md
autonomous: true
requirements: [VERB-05]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The VERB-05 census ran BEFORE any `internal/cli/` edit, as a positive-controlled, word-boundary, multiline `rg -nU -w --hidden` over `codegraph\\s+(query|unlock)` with D-10's exclusions (`.planning/`, `CHANGELOG.md`, `web/build/`, `.git/`), and its 'before' result is exactly RESEARCH's live baseline — 14 lines across 6 files: `docs/CLI-REFERENCE.md` (6), `internal/daemon/lock.go` (2), `internal/cli/index.go` (2), `internal/cli/index_lock_test.go` (2), `internal/cli/query.go` (1), `internal/cli/unlock.go` (1). Any other count STOPS the plan for investigation. D-10 (probe VERB-05/precision, explicit: the count is `wc -l` over `rg -n` output, printed before it is asserted; zero or a different number is never accepted silently)."
    - "The planted positive control was found before the baseline was trusted: an UNTRACKED file `.github/__census_control__.md` carrying both a single-line old-verb sentence and a line-wrapped one (`codegraph` at end of line, `query` on the next) is reported by the identical invocation on lines 1, 2 and 3 of that file, then removed byte-clean (`git status --porcelain .github` empty). Planting inside `.github/` proves the hidden-directory scope D-10 names (`.github/workflows/`, `.claude/hooks/*`, `.claude/skills/codegraph/SKILL.md`) is actually scanned — RESEARCH's invocation lacked `--hidden` and would have skipped every one of those paths. D-10 / Phase 2 D-14 (probe VERB-05/boundary, explicit: the same invocation on the real tree finds no hit in `internal/query`, in a JSON `\"query\"` key, in a `query.json` fixture name, or in `codegraph_search` — the `-w` boundary plus the literal `codegraph` prefix exclude them, and the planted control is the boundary proof)."
    - "A pre-fold baseline is on the record for the later byte-identity and generated-surface proofs: the pre-fold HEAD SHA, the sha256 of default `search` / `search --json` output for fixed terms on the repo's own index and on the gofixture, the `__complete \"\"` top-level list (26 entries, `query` and `unlock` present, hidden `man` absent), the `codegraph man <dir>` page list (30 pages, `codegraph-query.1` and `codegraph-unlock.1` present, `codegraph-man.1` absent, no `codegraph-daemon-unlock.1`), the 8 MCP tool names from `internal/mcp/tools.go`, and `task test:wireoracle` green under `GOTOOLCHAIN=go1.26.6` with `-count=1`. D-14."
    - "`03-MUTATION-LOG.md` exists in the `02-MUTATION-LOG.md` shape (header, pre-mutation cleanliness convention, `## Family (a) — VERB-05 …` with Test/guard, What are we testing and why, Pre-mutation gate, Mutation applied, Observed result verbatim, Revert, Byte-clean proof, Verdict) plus a `## Baseline (pre-fold, captured by 03-01)` section. D-11's log shape."
  artifacts:
    - ".planning/phases/03-verb-fold/03-MUTATION-LOG.md — `## Family (a) — VERB-05: positive-controlled census BEFORE the fold` and `## Baseline (pre-fold, captured by 03-01)`"
  key_links:
    - "the census invocation (this plan) → the identical invocation re-run by 03-04 after every edit → the two transcripts side by side are VERB-05's evidence; if the invocations differ the comparison is meaningless, so 03-04 copies this plan's command verbatim"
    - "the pre-fold HEAD SHA recorded here → 03-02 Task 3 builds that commit's binary in a scratch worktree and diffs its `search` output against HEAD's (VERB-01 byte-identity)"
  prohibitions:
    - statement: "No file under `internal/cli/` (or anywhere in the Go tree) is edited by this plan — the census is only meaningful if it precedes the fold (D-10, RESEARCH Pitfall 4). Gate: `git diff --quiet -- internal/ cmd/ docs/` at the plan's final commit."
      status: resolved
      verification: test
    - statement: "The planted control is never `git add`ed, never left on disk after its run, and never placed under `.planning/` or another excluded path (where it could not be found and would prove nothing)."
      status: resolved
      verification: test
    - statement: "No `[ci skip]` / `[skip ci]` in any commit message (protect-main blocks the PR forever)."
      status: resolved
      verification: test
    - statement: "No hand-invented ROADMAP/STATE structure; this plan writes only its own mutation log."
      status: resolved
      verification: judgment
---

<objective>
Run the VERB-05 census once, positively controlled, BEFORE `internal/cli/` is touched, and freeze the
pre-fold baseline the later plans compare against: the 14-line/6-file old-verb consumer set, the
default `search` output digests (for VERB-01's byte-identity proof), the completion and man-page
listings (for VERB-06's "reflects the new surface" proof), the 8 MCP tool names and a green wire
oracle (for VERB-07/D-14).

Purpose: a "zero references after" claim is only evidence next to a trusted "before" produced by the
same instrument — and the instrument itself is only trusted once it has found a planted control
(Phase 2 D-14, STATE standing rule "a transcript grep is a claim about the grep, not the product").
RESEARCH's invocation omitted `--hidden`, so it never scanned `.github/`, `.claude/hooks/` or
`.claude/skills/codegraph/SKILL.md` — three of the paths D-10 explicitly lists. This plan corrects
the instrument and proves the correction with a control planted inside `.github/`.

Output: `.planning/phases/03-verb-fold/03-MUTATION-LOG.md` (new) carrying Family (a) and the
Baseline section; nothing else changes on disk.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verb-fold/03-CONTEXT.md
@.planning/phases/03-verb-fold/03-RESEARCH.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md
</context>

<!-- planner-discipline-allow: codegraph query -->
<!-- planner-discipline-allow: codegraph unlock -->

<tasks>

<task type="tracer">
  <name>Task 1: Positive-controlled VERB-05 census BEFORE the fold — planted control in `.github/` found, removed byte-clean, then the 14-line/6-file baseline recorded as Family (a)</name>
  <files>.planning/phases/03-verb-fold/03-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/03-verb-fold/03-CONTEXT.md D-10 (scope, exclusions, the planted-control rule, "run before `internal/cli/` is edited and again after") and D-08's last sentence.
    - .planning/phases/03-verb-fold/03-RESEARCH.md § "Code Examples → The verified census invocation" (the 14-line baseline with file:line detail) and § "Common Pitfalls → Pitfall 4".
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md — the header, the "Pre-mutation cleanliness gate" convention paragraph, and Family (b)'s section order (Test/guard → What are we testing → Pre-mutation gate → Mutation applied → Observed … verbatim → Revert → Byte-clean proof → Green re-run → Verdict). `03-MUTATION-LOG.md` reproduces this shape; it does not invent a new one.
    - `git ls-files | rg -o '^\.[^/]+/' | sort -u` — the tracked hidden directories (`.claude/`, `.github/`, `.planning/`); `.claude/hooks/*.sh` and `.claude/skills/codegraph/SKILL.md` are the "hook scripts" and "SKILL.md" D-10 names, and `rg` skips them without `--hidden`.
  </read_first>
  <action>
Pre-mutation gate: `git status --porcelain` shows no entry under `.github/` and no file named
`__census_control__.md` anywhere; record the empty result.

The instrument (use this exact invocation everywhere in this phase — 03-04 re-runs it verbatim):

  rg -nU -w --hidden 'codegraph\s+(query|unlock)' --glob '!.planning/**' --glob '!CHANGELOG.md' --glob '!web/build/**' --glob '!.git/**' .

`-w` gives the word boundary D-10 requires (so `internal/query`, a JSON `"query"` key, `query.json`,
`codegraph_search` and `codegraph daemon unlock` are not hits — the literal `codegraph` immediately
before the verb is what makes the match); `-U` plus `\s+` is the multiline half (a sentence wrapped
between `codegraph` and the verb is still found); `--hidden` is the correction to RESEARCH's
invocation (without it `.github/`, `.claude/hooks/`, `.claude/skills/` are silently skipped —
verified this planning session by planting a control under `.github/` and watching the un-hidden
invocation miss it). Run it from the repository root with `.` as the path so every reported path
starts with `./`.

Mutation (the positive control): create the UNTRACKED file `.github/__census_control__.md` with
exactly three lines — line 1 `run codegraph unlock now`, line 2 `and also run codegraph`, line 3
`query now` — so both the single-line form and the line-wrapped form are present. Never `git add`
it. Wrap the whole step in a bash `trap 'rm -f .github/__census_control__.md' EXIT` so the control
cannot outlive the step.

Observe: run the instrument, capture the transcript. It MUST report the control file on lines
`:1:`, `:2:` and `:3:` (the wrapped match spans lines 2–3). If the control is not reported, STOP —
the instrument is broken; do not proceed to the real census, do not trust anything downstream.

Revert: remove the control; assert `git status --porcelain .github` is empty and the file is gone.

Real census: run the identical instrument on the clean tree into a transcript; print
`before: N lines across F files` where N is `wc -l` over the transcript and F is the count of
distinct path prefixes. Expected: N=14, F=6, and the sorted file set is exactly
`./docs/CLI-REFERENCE.md ./internal/cli/index.go ./internal/cli/index_lock_test.go
./internal/cli/query.go ./internal/cli/unlock.go ./internal/daemon/lock.go`. Any other N, F or
file set means a consumer RESEARCH did not enumerate has appeared (or one disappeared): STOP,
enumerate it, and report it back through the SUMMARY before 03-02 is allowed to start — the fold
must know every consumer it has to retarget (D-08: "zero old-verb references outside the stubs —
no exceptions").

Write `.planning/phases/03-verb-fold/03-MUTATION-LOG.md` in the 02-MUTATION-LOG.md shape: title
`# 03-MUTATION-LOG — Verb Fold`, Phase/Date/Scope header naming the three families this phase
appends — (a) VERB-05 census BEFORE (this plan), (b) VERB-07 RED against a re-visible `query`
command (03-03), (c) VERB-05 census AFTER (03-04) — the "Pre-mutation cleanliness gate" convention
paragraph, then `## Family (a) — VERB-05: positive-controlled census BEFORE the fold` carrying:
Test/guard (the instrument, quoted verbatim), What are we testing and why (that the census can
find an old-verb reference in a hidden directory and a wrapped sentence before its zero is
believed; that the 14/6 baseline is the exact consumer set 03-02 must retarget), Pre-mutation
gate, Mutation applied (the control's path and three lines), Observed result (the planted-run
transcript verbatim, the three control lines), Revert, Byte-clean proof, Real census (the
14-line transcript verbatim, the `before: 14 lines across 6 files` line), and Verdict. Note in the
Verdict that the `--hidden` correction to RESEARCH's invocation changed nothing on today's tree
(still 14/6) but was required for D-10's scope to be real.

Do not commit yet — Task 2 adds the Baseline section and commits the file once.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && C=.github/__census_control__.md && test ! -e "$C" && test -z "$(git status --porcelain .github)" && trap 'rm -f "$C"' EXIT && printf 'run codegraph unlock now\nand also run codegraph\nquery now\n' > "$C" && (rg -nU -w --hidden 'codegraph\s+(query|unlock)' --glob '!.planning/**' --glob '!CHANGELOG.md' --glob '!web/build/**' --glob '!.git/**' . > /tmp/03-01-census-planted.txt; echo "exit=$?" >> /tmp/03-01-census-planted.txt); rm -f "$C"; test ! -e "$C" && test -z "$(git status --porcelain .github)" && rg -q '^\./\.github/__census_control__\.md:1:run codegraph unlock now$' /tmp/03-01-census-planted.txt && rg -q '^\./\.github/__census_control__\.md:2:and also run codegraph$' /tmp/03-01-census-planted.txt && rg -q '^\./\.github/__census_control__\.md:3:query now$' /tmp/03-01-census-planted.txt && rg -q '^exit=0$' /tmp/03-01-census-planted.txt && echo "positive control: single-line and wrapped forms found under .github/, control removed byte-clean"</automated>
    <fails_when>non-zero exit or no `positive control:` line — the tree was not clean under `.github/`, the instrument did not report the control on lines 1, 2 and 3 (hidden-dir scope or the multiline/word-boundary half is broken), the run exited non-zero, or the control survived removal</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test ! -e .github/__census_control__.md && rg -nU -w --hidden 'codegraph\s+(query|unlock)' --glob '!.planning/**' --glob '!CHANGELOG.md' --glob '!web/build/**' --glob '!.git/**' . > /tmp/03-01-census-before.txt; n=$(wc -l < /tmp/03-01-census-before.txt | tr -d ' '); f=$(cut -d: -f1 /tmp/03-01-census-before.txt | sort -u | wc -l | tr -d ' '); echo "before: $n lines across $f files"; test "$n" = "14" && test "$f" = "6" && test "$(cut -d: -f1 /tmp/03-01-census-before.txt | sort -u | tr '\n' ' ')" = "./docs/CLI-REFERENCE.md ./internal/cli/index.go ./internal/cli/index_lock_test.go ./internal/cli/query.go ./internal/cli/unlock.go ./internal/daemon/lock.go " && ! rg -q '__census_control__|internal/query/|\.github/|\.claude/' /tmp/03-01-census-before.txt && echo "baseline matches RESEARCH: 14/6"</automated>
    <fails_when>non-zero exit, a printed count other than `before: 14 lines across 6 files`, a file set other than the six RESEARCH enumerated, or a hit under `internal/query/`, `.github/`, `.claude/` or the control's name — a consumer appeared/disappeared since RESEARCH (stop and enumerate it), the boundary exclusions are not holding, or the control leaked into the real run</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test -f .planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -c '^## Family \(a\) — VERB-05' .planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -q -F -- "rg -nU -w --hidden 'codegraph\s+(query|unlock)'" .planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -q '__census_control__' .planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -q 'before: 14 lines across 6 files' .planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -q -i 'byte-clean' .planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -q 'Pre-mutation cleanliness gate' .planning/phases/03-verb-fold/03-MUTATION-LOG.md && git diff --quiet -- internal/ cmd/ docs/ && echo "family (a) recorded; Go tree untouched"</automated>
    <fails_when>non-zero exit or a `Family (a)` count of 0 — the log is missing, does not quote the exact instrument, the control path, the `before: 14 lines across 6 files` line or the byte-clean proof, lacks the cleanliness-gate convention paragraph, or something under `internal/`, `cmd/` or `docs/` was modified before the fold</fails_when>
  </verify>
  <acceptance_criteria>
    - With `.github/__census_control__.md` present (untracked, three lines), the instrument reports that file on `:1:`, `:2:` and `:3:`; after `rm -f`, `git status --porcelain .github` is empty.
    - On the clean tree the instrument reports exactly 14 lines across exactly the six files `docs/CLI-REFERENCE.md`, `internal/cli/index.go`, `internal/cli/index_lock_test.go`, `internal/cli/query.go`, `internal/cli/unlock.go`, `internal/daemon/lock.go`, with no hit under `internal/query/`, `.github/` or `.claude/`.
    - `03-MUTATION-LOG.md` has the 02-MUTATION-LOG.md header/convention paragraph and a `## Family (a) — VERB-05` section quoting the instrument verbatim, the planted transcript, the 14-line transcript and the `before: 14 lines across 6 files` line.
    - `git diff --quiet -- internal/ cmd/ docs/` holds (nothing in the Go tree or generated docs changed).
  </acceptance_criteria>
  <done>The census instrument has been proven to find a planted old-verb reference in a hidden directory (single-line and wrapped) and the untouched tree's 14-line/6-file consumer set is recorded as the "before" half of VERB-05.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the pre-fold baseline (HEAD SHA, `search` output digests, completion and man listings, 8 MCP tool names, green wire oracle) into the mutation log and commit it</name>
  <files>.planning/phases/03-verb-fold/03-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/03-verb-fold/03-CONTEXT.md D-14 (8-tool MCP set + wire-oracle transcripts untouched; `task test:wireoracle` byte-identical) and D-01 (default `search` output must stay byte-identical — this task captures the "before" digests).
    - .planning/phases/03-verb-fold/03-RESEARCH.md § "Architecture Patterns → Pattern 4" (completions and man pages are generated from the live binary at install time; nothing committed) and § "Phase Requirements" VERB-07 row (the 8 tool names).
    - internal/mcp/tools.go — the eight `Name: "codegraph_…"` registrations (lines 173, 309–345).
    - Taskfile.yml § `test:wireoracle` (`go test ./test/wireoracle/...`; needs `GOTOOLCHAIN=go1.26.6` locally because `cockroachdb/swiss` is `!go1.27` — verified this planning session: the unpinned run is `[build failed]`).
  </read_first>
  <action>
Append `## Baseline (pre-fold, captured by 03-01)` to `03-MUTATION-LOG.md` with these recorded
values, each produced by the command shown and pasted verbatim:

  1. `pre-fold HEAD: <40-hex sha>` from `git rev-parse HEAD` (03-02 Task 3 also derives the pre-fold
     tree as the feat! commit's parent; recording the SHA here is the human-readable cross-check).
  2. Build the CURRENT binary: `GOTOOLCHAIN=go1.26.6 go build -o /tmp/03-01-prefold-bin ./cmd/codegraph`.
     Default-branch digests on the repo's own index (`-p /Volumes/Code/github.com/seanb4t/codegraph-go`),
     terms chosen because every hit lives in `internal/query/` (files this phase never touches):
     `search matchNodes`, `search matchNodes --json`, `search lexicalTier --limit 2`,
     `search ValidateKind --kind function`, `search ValidateKind --json --limit 5` — record
     `sha256(<stdout>)` for each and the first line of each human output. Then on a fresh gofixture
     (`cp -R internal/indexer/testdata/gofixture/. <tmp>/ && /tmp/03-01-prefold-bin init <tmp>`):
     `search Alpha`, `search Alpha --json`, `search pkga`, `search helper --kind function` — record the
     full outputs verbatim (they are short) and their sha256.
  3. `/tmp/03-01-prefold-bin __complete ""` — record the full list (26 entries excluding the trailing
     `:4` directive line); note that `query` and `unlock` are present and the hidden `man` is absent
     (the existing proof that `Hidden: true` keeps a command out of completions).
     `/tmp/03-01-prefold-bin __complete daemon ""` — `start`, `stop` (2 entries).
  4. `/tmp/03-01-prefold-bin man <tmpdir>` — record `ls <tmpdir>` (30 pages), noting
     `codegraph-query.1` and `codegraph-unlock.1` present, `codegraph-man.1` absent,
     `codegraph-daemon-unlock.1` absent.
  5. The 8 MCP tool names, from `rg -o 'Name:\s+"codegraph_[a-z]+"' internal/mcp/tools.go | rg -o 'codegraph_[a-z]+' | sort -u`
     — expected exactly `codegraph_callees codegraph_callers codegraph_explore codegraph_files
     codegraph_impact codegraph_node codegraph_search codegraph_status`.
  6. `GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1` — record the `ok` line (uncached,
     so it is a real run) and `git status --porcelain testdata/wireoracle` (empty).

Also add one sentence under the Baseline noting the known, unrelated `internal/daemon` load flake
(`TestDaemonSharedWriter` timed out under full parallel load during planning and passed in
isolation in 0.23s; STATE.md deferred_items already records the class) so 03-02's executor reports
it separately rather than as a fold regression if it recurs.

Commit exactly one file: `test(03-01): record the pre-fold VERB-05 census and baseline (Family a)`.
No `[ci skip]`. Remove `/tmp/03-01-prefold-bin` and the temp fixture afterwards.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && L=.planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -c '^## Baseline \(pre-fold, captured by 03-01\)' "$L" && rg -q "pre-fold HEAD: $(git rev-parse HEAD^)" "$L" && rg -q 'codegraph-query\.1' "$L" && rg -q 'codegraph-unlock\.1' "$L" && rg -q 'search matchNodes' "$L" && rg -q '[0-9a-f]{64}' "$L" && for t in codegraph_callees codegraph_callers codegraph_explore codegraph_files codegraph_impact codegraph_node codegraph_search codegraph_status; do rg -q -F "$t" "$L" || { echo "missing tool name $t"; exit 1; }; done && rg -q 'ok\s+github.com/seanb4t/codegraph-go/test/wireoracle' "$L" && rg -q 'TestDaemonSharedWriter' "$L" && echo "baseline section complete"</automated>
    <fails_when>non-zero exit or a `Baseline` count of 0 — the section is missing, the recorded pre-fold SHA is not the commit this task's own commit sits on top of, a digest/listing/tool name/wire-oracle `ok` line is absent, or the flake note is missing</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(rg -o 'Name:\s+"codegraph_[a-z]+"' internal/mcp/tools.go | rg -o 'codegraph_[a-z]+' | sort -u | tr '\n' ' ')" = "codegraph_callees codegraph_callers codegraph_explore codegraph_files codegraph_impact codegraph_node codegraph_search codegraph_status " && GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1 2>&1 | tee /tmp/03-01-wo.txt | tail -2; rg -q '^ok\s+github.com/seanb4t/codegraph-go/test/wireoracle' /tmp/03-01-wo.txt && ! rg -q '^(--- FAIL|FAIL)' /tmp/03-01-wo.txt && test -z "$(git status --porcelain testdata/wireoracle)" && echo "8 tools, wire oracle green uncached"</automated>
    <fails_when>non-zero exit or no `8 tools, wire oracle green uncached` line — the tool set is not exactly the eight names, the uncached wire-oracle run is red or absent, or a transcript under `testdata/wireoracle/` is dirty before the fold even starts</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git show --format=%s --name-only HEAD > /tmp/03-01-commit.txt && rg -q '^test\(03-01\): ' /tmp/03-01-commit.txt && test "$(rg -v '^test\(03-01\)' /tmp/03-01-commit.txt | rg -v '^$' | tr '\n' ' ')" = ".planning/phases/03-verb-fold/03-MUTATION-LOG.md " && git log -2 --format=%s%n%b > /tmp/03-01-msgs.txt && ! rg -q -i 'ci skip|skip ci' /tmp/03-01-msgs.txt && git diff --quiet -- internal/ cmd/ docs/ && test ! -e .github/__census_control__.md && ! git ls-files --error-unmatch .github/__census_control__.md >/dev/null 2>&1 && echo "commit shape ok, tree clean"</automated>
    <fails_when>non-zero exit — the commit is not a `test(03-01):` commit touching only the mutation log, a CI-skip marker is present, the Go tree or `docs/` changed, or the census control exists on disk or in the index</fails_when>
  </verify>
  <acceptance_criteria>
    - `03-MUTATION-LOG.md` has a `## Baseline (pre-fold, captured by 03-01)` section recording: the pre-fold HEAD SHA; sha256 digests (and first lines / verbatim short outputs) for the listed `search` invocations on the repo index and on the gofixture; the `__complete ""` (26 entries, `query`/`unlock` present, `man` absent) and `__complete daemon ""` (`start`, `stop`) lists; the 30-page man listing naming `codegraph-query.1`/`codegraph-unlock.1` present and `codegraph-man.1`/`codegraph-daemon-unlock.1` absent; the eight tool names; the uncached wire-oracle `ok` line; the `TestDaemonSharedWriter` flake note.
    - The eight names extracted from `internal/mcp/tools.go` are exactly `codegraph_callees codegraph_callers codegraph_explore codegraph_files codegraph_impact codegraph_node codegraph_search codegraph_status`.
    - The plan's only commit is `test(03-01): …` touching only `03-MUTATION-LOG.md`; no CI-skip marker; the census control is neither on disk nor tracked.
  </acceptance_criteria>
  <done>Every "after" comparison the later plans make (VERB-01 byte-identity, VERB-05 zero-references, VERB-06 completions/man, VERB-07 MCP set and wire oracle) has a recorded, reproducible "before".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planted control file → working tree | an untracked file under `.github/` exists for one shell step; it must never be staged, committed, or left behind |
| `rg` invocation → the census verdict | the census is the only evidence for VERB-05; its scope (hidden dirs, exclusions, boundary) decides whether a real consumer can hide from it |
| scratch binary / temp fixture → the repo's own `.codegraph/` index | `search` is read-only; the repo index (which carries a stale `daemon.lock` from a dead pid) is read, never written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-01 | Repudiation | census "zero/14 hits" verdict trusted without a control | high | mitigate | The control (single-line + wrapped, under `.github/`) must be reported on `:1:`/`:2:`/`:3:` before the real run; a missing control stops the plan. |
| T-03-01-02 | Tampering | `rg` default hidden-dir skip hides `.github/`, `.claude/hooks/`, `SKILL.md` consumers | high | mitigate | `--hidden` is part of the instrument; the control is planted in a hidden directory so the correction is proven, not assumed. |
| T-03-01-03 | Tampering | planted control leaking into a commit | medium | mitigate | `trap … EXIT` removal; verify asserts `git status --porcelain .github` empty and the path is untracked. |
| T-03-01-04 | Tampering | the "before" baseline captured after an `internal/cli/` edit | medium | mitigate | Wave 1, no other plan runs; verify asserts `git diff --quiet -- internal/ cmd/ docs/`. |
| T-03-01-05 | Denial of Service | `protect-main` blocked by a `[ci skip]` marker | high | mitigate | Commit messages grepped for CI-skip markers. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install in this plan (no new dependencies anywhere in the phase — RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `rg -nU -w --hidden 'codegraph\s+(query|unlock)' …` finds the planted control (3 lines) and, on the clean tree, exactly 14 lines / 6 files.
- `GOTOOLCHAIN=go1.26.6 go test ./test/wireoracle/... -count=1` green; 8 MCP tool names.
- `git diff --quiet -- internal/ cmd/ docs/` at the plan's final commit.
</verification>

<success_criteria>
- Family (a) and the Baseline section exist in `03-MUTATION-LOG.md` with verbatim transcripts.
- The "before" census is 14/6 with the control found first; the Go tree is untouched.
- One `test(03-01):` commit, no CI-skip marker.
</success_criteria>

## Artifacts this phase produces

Files/symbols created or changed across 03-01…03-04 (for `.planning/intel/API-SURFACE.md`, which has no entries):
- `internal/cli/search.go` — `--full` flag, `-k/-l/-j` shorthands, `renderFullLine(*schema.Node) string`, relocated `resolveStartPath`/`writeJSONLine` (03-02)
- `internal/cli/renamed.go` (new) — `newQueryCmd()` / `newUnlockCmd()` hidden rename stubs; `internal/cli/query.go` and `internal/cli/unlock.go` deleted (03-02)
- `internal/cli/daemon.go` — `newDaemonUnlockCmd()` registered under `daemon` (03-02)
- `internal/cli/renamed_test.go` (new), `internal/cli/daemon_test.go` (`TestDaemonUnlockCmd`), `internal/cli/query_cli_test.go` (`TestSearchFullCmd`, `TestRenderFullLine`, `TestSearchFlagShortForms`), `internal/cli/notice_test.go`, `internal/cli/index_lock_test.go` (03-02)
- `internal/daemon/lock.go`, `internal/cli/index.go` — messages name `codegraph daemon unlock` (03-02)
- `internal/cli/testdata/cli-reference-allowlist.txt` (+2 lines), `docs/CLI-REFERENCE.md` (regenerated) (03-02)
- `.planning/phases/03-verb-fold/03-MUTATION-LOG.md` (03-01, appended by 03-03 and 03-04), `.planning/phases/03-verb-fold/COVERAGE.md` (03-03), ROADMAP `## Backlog` row `999.5` + its `.gitkeep` (03-04)

## Flagged assumptions (probe rows left unresolved — surfaced, not dropped)

- VERB-05 has no unresolved rows: boundary and precision are explicit truths above.

<output>
Create `.planning/phases/03-verb-fold/03-01-SUMMARY.md` when done — include the planted-control transcript, the 14-line census transcript, and the Baseline digests verbatim.
</output>
