---
phase: 03-verb-fold
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/999.5-remove-the-query-unlock-rename-stubs/.gitkeep
  - .planning/phases/03-verb-fold/03-MUTATION-LOG.md
autonomous: true
requirements: [VERB-05, VERB-08]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The stubs' removal is tracked in ROADMAP's `## Backlog` as a `999.x` row written ONLY by the gsd-tools phase verb (`phase add \"remove the query/unlock rename stubs\" --id <phase next-decimal 999>`, expected `999.5`), never hand-authored: the new `### Phase 999.5:` heading sits after the `## Backlog` heading and after the existing `999.4` block, the git diff adds exactly one entry block and changes no other line, `roadmap validate` is clean, and the active-milestone phase filter still selects the same phase set as before the row was added. The heading carries no version token (`v0.15.0` lives in the `**Goal:**` value, a value fill in a tool-written shape — `planning-artifacts.md`). D-09 (the ROADMAP half of the removal record; RESEARCH's `999.5` verified free)."
    - "The `**Goal:**` value of the new row names what v0.15.0 removes and how: `internal/cli/renamed.go` + `renamed_test.go`, the `newQueryCmd()`/`newUnlockCmd()` registrations in `root.go`, the two `cli-reference-allowlist.txt` lines, then `task docs:cli` — and cites Phase 3 VERB-03/VERB-04 and the feat commit SHA. D-09."
    - "The VERB-05 census ran AGAIN after the last `internal/cli/` edit of the phase (after 03-03's byte-clean revert), with the identical instrument 03-01 used and the identical planted control under `.github/` found first and removed byte-clean; on the real tree every remaining hit lies inside the stubs' own declaration — `internal/cli/renamed.go` (its doc comment naming the old invocations) and the two D-12 lines of `internal/cli/testdata/cli-reference-allowlist.txt` (whose key IS cobra's `CommandPath()`, `codegraph query` / `codegraph unlock`, fixed by the allowlist grammar) — and nowhere else: not README, `docs/` (the regenerated reference has zero), `SKILL.md`, `internal/mcp/resources/*.md`, hook scripts, `Taskfile.yml`, `.github/workflows/`, tests, `internal/daemon/lock.go`, `internal/cli/index.go`. The counts are printed (`after: N lines across F files`, allowlist hits exactly 2) before being asserted. Recorded as `## Family (c) — VERB-05` beside Family (a). D-10, D-08 (probe VERB-05/boundary+precision, explicit — same instrument, same control, same counting rule as 03-01)."
    - "Every commit the phase produced, from the feat commit's parent to HEAD, is Conventional-Commits shaped per `pr-title.yml`'s regex; exactly one subject begins `feat(` and exactly one contains `!:` (the same commit), whose body carries the `BREAKING CHANGE:` footer; every other subject is `test(`, `docs(` or `chore(`; none contains a CI-skip marker. D-15 (VERB-08's commit discipline; probe VERB-08 — the release-notes half is the footer release-please copies into the CHANGELOG, inspected post-merge, not asserted in-phase)."
    - "The phase gate is green at HEAD under the pinned toolchain: `go build ./...`, `go test ./internal/cli/... ./internal/daemon/... -count=1` (the known `TestDaemonSharedWriter` load flake, if it recurs, is re-run alone and reported separately per 03-01's Baseline note), `task docs:cli:drift`, `task test:wireoracle`."
    - statement: "The `## Backlog` row's wording is what the maintainer would write for the v0.15.0 removal, and the SUMMARY's per-file diff summaries from 03-02 are sufficient for the end-of-phase review D-13 hands to the maintainer."
      verification: backstop
  artifacts:
    - ".planning/ROADMAP.md — `### Phase 999.5: remove the query/unlock rename stubs` under `## Backlog`, Goal value filled"
    - ".planning/phases/999.5-remove-the-query-unlock-rename-stubs/.gitkeep — created by the verb"
    - ".planning/phases/03-verb-fold/03-MUTATION-LOG.md — `## Family (c) — VERB-05: positive-controlled census AFTER the fold`"
  key_links:
    - "03-01's instrument (verbatim) → this plan's after-run → the two transcripts are VERB-05's evidence pair; the allowed-hit set is the stub declaration (renamed.go + its two allowlist lines), which is why the allowlist count is asserted at exactly 2"
    - "`phase next-decimal 999` → `phase add … --id` → `git diff -- .planning/ROADMAP.md` → `roadmap validate` + the milestone phase filter — the row is trusted only after the tool's own write is inspected and the parser's view is unchanged"
  prohibitions:
    - statement: "No hand-invented ROADMAP structure: the backlog row is written by the phase verb; the only hand edit is the `**Goal:**` value inside the block the verb wrote; no heading, list, or field is added, renamed, or reordered; no version-bearing `###` heading is introduced. `planning-artifacts.md`, D-09."
      status: resolved
      verification: test
    - statement: "No `internal/`, `cmd/`, `docs/` file is touched by this plan; the census is the last thing the phase does to the Go tree (which is: nothing). D-10."
      status: resolved
      verification: test
    - statement: "The planted control is never staged, never left behind, never placed under an excluded path; a zero-hit result is never recorded before the control was found."
      status: resolved
      verification: test
    - statement: "No `[ci skip]` / `[skip ci]` in any commit; no amend of the feat commit."
      status: resolved
      verification: test
---

<objective>
Close the phase: record the stubs' v0.15.0 removal as a ROADMAP backlog row written by the gsd-tools
phase verb (D-09), re-run the VERB-05 census with the same instrument and control after the last
`internal/cli/` edit and show every remaining old-verb reference lives inside the stubs' own
declaration, audit the phase's commit history for D-15's shape, and confirm the full gate is green.

Purpose: VERB-05 is a before/after pair — the "after" is only comparable if it is the same
instrument; VERB-08's removal record has three halves (the `BREAKING CHANGE:` footer from 03-02,
the allowlist reason from 03-02, and this backlog row) and the row must come from the tool that
owns ROADMAP.md, not from a hand-written heading (`planning-artifacts.md`).

Output: the `999.5` backlog row + its `.gitkeep`, Family (c) in the mutation log, two
`docs(03-04):` commits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verb-fold/03-CONTEXT.md
@.planning/phases/03-verb-fold/03-MUTATION-LOG.md
@.planning/phases/03-verb-fold/03-02-SUMMARY.md
</context>

<!-- planner-discipline-allow: codegraph query -->
<!-- planner-discipline-allow: codegraph unlock -->

<tasks>

<task type="tracer">
  <name>Task 1: Backlog row `999.5` for the v0.15.0 stub removal, written by `gsd-tools phase add --id`, Goal value filled, parser view unchanged</name>
  <files>.planning/ROADMAP.md, .planning/phases/999.5-remove-the-query-unlock-rename-stubs/.gitkeep</files>
  <read_first>
    - /Users/sean/.claude/rules/planning-artifacts.md — value fills in tool-written shapes are allowed; invented headings are not; verify with `git diff` and the parser's own phase filter, not with `roadmap validate` alone.
    - .planning/ROADMAP.md `## Backlog` section (the `999.2` and `999.4` blocks — the shape the tool has written before; the new block will be the `phase add` template: `### Phase 999.5: …`, `**Goal:** [To be planned]`, `**Requirements**: TBD`, `**Depends on:** Phase TBD`, `**Plans:** 0 plans`, `Plans:` list).
    - .planning/phases/03-verb-fold/03-RESEARCH.md § "Code Examples → The ROADMAP backlog verb (D-09)" — `phase next-decimal 999` → `999.5`; `phase add "<description>" --id 999.5`. Verified this planning session on a scratch copy: the verb inserts the block at the end of `## Backlog` (after `999.4`) and creates `.planning/phases/999.5-<slug>/.gitkeep`.
    - .planning/phases/03-verb-fold/03-CONTEXT.md D-09 and "Claude's Discretion → the 999.x row's number and wording".
    - .planning/phases/03-verb-fold/03-02-SUMMARY.md — the feat commit SHA to cite in the Goal value.
  </read_first>
  <action>
Use the gsd_run launcher from the execute-plan workflow. First `NEXT=$(gsd_run phase next-decimal 999 --pick next)`
— expect `999.5`; if it is something else, use what it returns (never guess) and adjust the
directory name below accordingly.

Write the row through the verb only:
`gsd_run phase add "remove the query/unlock rename stubs" --id "$NEXT"`.
The description deliberately carries no version token: a version-bearing `###` heading is the
shape `planning-artifacts.md` forbids in ROADMAP.md, so `v0.15.0` goes into the Goal value instead.

Inspect `git diff -- .planning/ROADMAP.md`: exactly one added block, `### Phase 999.5: remove the query/unlock rename stubs`,
located after the `## Backlog` heading and after the `### Phase 999.4:` block; zero removed lines;
no other hunk. The verb also created `.planning/phases/999.5-remove-the-query-unlock-rename-stubs/.gitkeep`.

Value fill (the only hand edit, inside the block the verb wrote): replace `[To be planned]` in
that block's `**Goal:**` line with:
`Remove the hidden `query` and `unlock` rename stubs registered by Phase 3 (VERB-03/VERB-04, D-05/D-09; feat commit <SHA>) in v0.15.0, the minor after the fold: delete `internal/cli/renamed.go` and `internal/cli/renamed_test.go`, drop `newQueryCmd()` and `newUnlockCmd()` from `internal/cli/root.go`'s AddCommand list, remove the two `codegraph query` / `codegraph unlock` lines from `internal/cli/testdata/cli-reference-allowlist.txt`, run `task docs:cli` (no reference change expected — the stubs are hidden), and re-run the Phase 3 census instrument (03-MUTATION-LOG.md Family (c)) expecting zero hits.`
Leave `**Requirements**: TBD`, `**Depends on:** Phase TBD`, `**Plans:** 0 plans` and the `Plans:`
list exactly as the verb wrote them.

Checks: `gsd_run roadmap validate` → `{"warnings":[]}`; the milestone phase filter from
`planning-artifacts.md` (`getMilestonePhaseFilter`) applied to `ls .planning/phases` returns the
same set as on the parent commit (the new `999.5-…` directory is a sentinel and must NOT appear in
the active milestone set; the seven `0N-…` phase directories must all still appear).

Commit both paths: `docs(03-04): add backlog row 999.5 for the v0.15.0 removal of the query/unlock stubs (D-09)`. No `[ci skip]`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && R=.planning/ROADMAP.md && test "$(rg -c '^### Phase 999\.5: remove the query/unlock rename stubs$' "$R")" = "1" && b=$(rg -n '^## Backlog$' "$R" | cut -d: -f1) && p4=$(rg -n '^### Phase 999\.4:' "$R" | cut -d: -f1) && p5=$(rg -n '^### Phase 999\.5:' "$R" | cut -d: -f1) && test -n "$b" -a -n "$p4" -a -n "$p5" && test "$b" -lt "$p4" -a "$p4" -lt "$p5" && ! sed -n "${p5},\$p" "$R" | rg -q '^## ' && awk -v s="$p5" 'NR>s && /^\*\*Goal:\*\* /{print; exit}' "$R" > /tmp/03-04-goal.txt && rg -q -F 'v0.15.0' /tmp/03-04-goal.txt && rg -q -F 'internal/cli/renamed.go' /tmp/03-04-goal.txt && rg -q -F 'cli-reference-allowlist.txt' /tmp/03-04-goal.txt && rg -q 'VERB-03/VERB-04' /tmp/03-04-goal.txt && rg -q '[0-9a-f]{7,40}' /tmp/03-04-goal.txt && ! rg -q '\[To be planned\]' /tmp/03-04-goal.txt && ! rg -q '^### Phase 999\.5:.*v[0-9]+\.[0-9]+' "$R" && test -f .planning/phases/999.5-remove-the-query-unlock-rename-stubs/.gitkeep && echo "999.5 row under ## Backlog after 999.4; Goal filled; heading version-free"</automated>
    <fails_when>non-zero exit or a heading count other than 1 — the row is missing/duplicated, not positioned after `## Backlog` and the `999.4` block, followed by an unexpected `## ` heading, its Goal still reads `[To be planned]` or lacks `v0.15.0` / the file names / the requirement ids / a commit SHA, the heading carries a version token, or the verb's `.gitkeep` is absent</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && C=$(git log -1 --format=%H --grep='^docs(03-04): add backlog row 999\.5') && test -n "$C" && git show --format= --numstat "$C" -- .planning/ROADMAP.md | rg -q '^[0-9]+\s+0\s+\.planning/ROADMAP\.md$' && test "$(git show --format= --name-only "$C" | sort | tr '\n' ' ')" = ".planning/ROADMAP.md .planning/phases/999.5-remove-the-query-unlock-rename-stubs/.gitkeep " && test "$(git show "$C" -- .planning/ROADMAP.md | rg -c '^\+### ')" = "1" && ! git show "$C" -- .planning/ROADMAP.md | rg -q '^-[^-]' && node -e 'const p=require(process.env.HOME+"/.claude/gsd-core/bin/lib/roadmap-parser.cjs");const fs=require("fs");const f=p.getMilestonePhaseFilter(process.cwd());const d=fs.readdirSync(".planning/phases").filter(f);if(d.some(x=>x.startsWith("999.")))process.exit(1);if(d.filter(x=>/^0[1-7]-/.test(x)).length<3)process.exit(2);console.log("milestone set:",d.join(" "))' && node ~/.claude/gsd-core/bin/gsd-tools.cjs roadmap validate 2>/dev/null | rg -q '"warnings":\s*\[\]' && ! git log -1 --format=%s%n%b "$C" | rg -q -i 'ci skip|skip ci' && echo "row added by the verb (additions only, one heading), parser view unchanged, validate clean"</automated>
    <fails_when>non-zero exit or no closing line — the commit is missing, touches anything besides ROADMAP.md and the `.gitkeep`, removes a ROADMAP line or adds more than one `###` heading (a hand edit beyond the value fill), the `999.5-…` directory leaks into the active milestone phase set or the `0N-` phases drop out of it, `roadmap validate` warns, or a CI-skip marker is present</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` has exactly one `### Phase 999.5: remove the query/unlock rename stubs` heading, after `## Backlog` and after the `999.4` block, with no version token in the heading and a filled `**Goal:**` naming `v0.15.0`, `internal/cli/renamed.go`, the allowlist lines, VERB-03/VERB-04 and the feat commit SHA.
    - The commit adds lines only to ROADMAP.md (one `+###` line, zero `-` lines) plus the verb-created `.gitkeep`; the milestone phase filter excludes `999.5-…` and keeps the `0N-` phases; `roadmap validate` is clean.
  </acceptance_criteria>
  <done>The v0.15.0 removal has its ROADMAP record, written by the tool that owns the file and verified through the parser's own view.</done>
</task>

<task type="auto">
  <name>Task 2: VERB-05 census AFTER the fold (same instrument, same control) — hits only inside the stubs' declaration — plus the phase commit-history audit and the final gate, recorded as Family (c)</name>
  <files>.planning/phases/03-verb-fold/03-MUTATION-LOG.md</files>
  <precondition>03-03 is complete and the tree is clean: `rg -q '^## Family \(b\) — VERB-07' .planning/phases/03-verb-fold/03-MUTATION-LOG.md` and `git diff --quiet -- internal docs cmd` both hold. Halt if either fails.</precondition>
  <read_first>
    - .planning/phases/03-verb-fold/03-MUTATION-LOG.md § Family (a) — the instrument (copy it verbatim), the control's path and three lines, the counting rule, and the section shape to mirror for Family (c).
    - internal/cli/renamed.go (the doc comment naming the old invocations — the expected hits) and internal/cli/testdata/cli-reference-allowlist.txt (the two D-12 lines whose keys are `codegraph query` / `codegraph unlock`).
    - .planning/phases/03-verb-fold/03-CONTEXT.md D-08 (last sentence), D-10, D-15; .github/workflows/pr-title.yml line 47 (the regex).
    - .planning/phases/03-verb-fold/03-01-PLAN.md → Task 2's Baseline flake note (`TestDaemonSharedWriter`).
  </read_first>
  <action>
Pre-mutation gate: `git status --porcelain .github` empty; `test ! -e .github/__census_control__.md`.

Mutation (positive control, identical to 03-01): the untracked `.github/__census_control__.md` with
the same three lines (`run codegraph unlock now` / `and also run codegraph` / `query now`), under an
EXIT trap; run the instrument verbatim from Family (a):
`rg -nU -w --hidden 'codegraph\s+(query|unlock)' --glob '!.planning/**' --glob '!CHANGELOG.md' --glob '!web/build/**' --glob '!.git/**' .`
→ the control is reported on `:1:`, `:2:`, `:3:`. If not, STOP. Remove the control; byte-clean proof.

Real census (the "after"): run the instrument on the clean tree; print `after: N lines across F files`.
Every reported path must be one of `./internal/cli/renamed.go` or
`./internal/cli/testdata/cli-reference-allowlist.txt`; the allowlist contributes exactly 2 lines
(the D-12 keys), `renamed.go` contributes ≥ 1 (its doc comment). Anything else — README, `docs/`,
`.claude/skills/codegraph/SKILL.md`, `.claude/hooks/`, `internal/mcp/resources/`, `Taskfile.yml`,
`.github/workflows/`, any `_test.go`, `internal/daemon/lock.go`, `internal/cli/index.go` — is a
consumer the fold missed: STOP and report it (do not fix it silently in this plan; it belongs in the
feat commit's scope and needs a maintainer decision on how to land it).

State the allowed-hit rule and its reason in the Verdict: D-08 says "zero old-verb references
outside the stubs"; the stub declaration spans the stub source file and its two allowlist lines,
because `cli_reference_test.go` keys command-level entries by cobra's `CommandPath()` — the old
command name is the only key the allowlist grammar accepts. This is a planner-recorded
interpretation, surfaced for the maintainer's end-of-phase review (D-13).

Commit-history audit (D-15 / VERB-08): `F=$(git log --format=%H --grep='^feat(cli)!: fold query' -1)`;
over `git log --format=%s "$F^..HEAD"`: every subject matches
`^(feat|fix|perf|refactor|docs|chore|ci|test|build|revert)(\([a-z0-9_-]+\))?!?: .+`; exactly one
subject starts with `feat(`; exactly one contains `!:`; that commit's body has a `BREAKING CHANGE: `
line; every other subject starts with `test(`, `docs(` or `chore(`; no subject or body contains
`ci skip`/`skip ci`. Paste the subject list into Family (c)'s Verdict.

Final gate (pinned toolchain): `GOTOOLCHAIN=go1.26.6 go build ./...`;
`GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... ./internal/daemon/... -count=1` (if
`TestDaemonSharedWriter` times out, re-run `-run 'TestDaemonSharedWriter$'` alone and record both
results — a pass in isolation is the known load flake, reported separately, not a phase failure);
`task docs:cli:drift`; `GOTOOLCHAIN=go1.26.6 task test:wireoracle`.

Append `## Family (c) — VERB-05: positive-controlled census AFTER the fold` to `03-MUTATION-LOG.md`
(after the 03-03 sections) with the same subsections as Family (a): Test/guard, What are we testing
and why (that no consumer outside the stub declaration survived the fold, and that the instrument
still discriminates), Pre-mutation gate, Mutation applied, Observed result (control transcript
verbatim), Revert, Byte-clean proof, Real census (transcript verbatim, the `after:` line, the
per-file counts), the commit-history audit (subject list), the final-gate results, and Verdict.

Commit: `docs(03-04): record the post-fold VERB-05 census and the phase commit audit (Family c)` — one file. No `[ci skip]`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && C=.github/__census_control__.md && test ! -e "$C" && test -z "$(git status --porcelain .github)" && trap 'rm -f "$C"' EXIT && printf 'run codegraph unlock now\nand also run codegraph\nquery now\n' > "$C" && (rg -nU -w --hidden 'codegraph\s+(query|unlock)' --glob '!.planning/**' --glob '!CHANGELOG.md' --glob '!web/build/**' --glob '!.git/**' . > /tmp/03-04-census-planted.txt; echo "exit=$?" >> /tmp/03-04-census-planted.txt); rm -f "$C"; test ! -e "$C" && test -z "$(git status --porcelain .github)" && rg -q '^\./\.github/__census_control__\.md:1:run codegraph unlock now$' /tmp/03-04-census-planted.txt && rg -q '^\./\.github/__census_control__\.md:3:query now$' /tmp/03-04-census-planted.txt && rg -q '^exit=0$' /tmp/03-04-census-planted.txt && echo "positive control found (after-run) and removed byte-clean"</automated>
    <fails_when>non-zero exit or no `positive control found` line — the tree was dirty under `.github/`, the instrument no longer reports the control on lines 1 and 3, or the control survived</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test ! -e .github/__census_control__.md && rg -nU -w --hidden 'codegraph\s+(query|unlock)' --glob '!.planning/**' --glob '!CHANGELOG.md' --glob '!web/build/**' --glob '!.git/**' . > /tmp/03-04-census-after.txt; n=$(wc -l < /tmp/03-04-census-after.txt | tr -d ' '); f=$(cut -d: -f1 /tmp/03-04-census-after.txt | sort -u | wc -l | tr -d ' '); a=$(rg -c '^\./internal/cli/testdata/cli-reference-allowlist\.txt:' /tmp/03-04-census-after.txt || echo 0); s=$(rg -c '^\./internal/cli/renamed\.go:' /tmp/03-04-census-after.txt || echo 0); echo "after: $n lines across $f files (renamed.go=$s, allowlist=$a)"; test "$a" = "2" && test "$s" -ge 1 && test "$((a + s))" = "$n" && ! rg -v '^\./internal/cli/(renamed\.go|testdata/cli-reference-allowlist\.txt):' /tmp/03-04-census-after.txt | rg -q . && ! rg -q '^\./(docs/|README|\.claude/|\.github/|internal/mcp/|internal/daemon/|internal/cli/index|Taskfile|internal/cli/.*_test\.go)' /tmp/03-04-census-after.txt && echo "zero old-verb references outside the stub declaration"</automated>
    <fails_when>non-zero exit, a printed `allowlist=` other than 2, `renamed.go=` of 0, a total that is not renamed.go + allowlist, or any path outside those two files — a consumer of the old verb names survived the fold (docs, README, SKILL.md, hooks, MCP resources, Taskfile, workflows, tests, `lock.go`, `index.go`) or the stub declaration is not where the plan expects it</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && F=$(git log --format=%H --grep='^feat(cli)!: fold query into search --full and unlock into daemon unlock$' -1) && test -n "$F" && git log --format=%s "$F^..HEAD" > /tmp/03-04-subjects.txt && test -s /tmp/03-04-subjects.txt && test "$(grep -cvE '^(feat|fix|perf|refactor|docs|chore|ci|test|build|revert)(\([a-z0-9_-]+\))?!?: .+' /tmp/03-04-subjects.txt || true)" = "0" && test "$(rg -c '^feat\(' /tmp/03-04-subjects.txt)" = "1" && test "$(rg -c '!:' /tmp/03-04-subjects.txt)" = "1" && test "$(rg -v '^(feat\(cli\)!:|test\(|docs\(|chore\()' /tmp/03-04-subjects.txt | wc -l | tr -d ' ')" = "0" && git log -1 --format=%b "$F" | rg -q '^BREAKING CHANGE: ' && ! git log --format=%s%n%b "$F^..HEAD" | rg -q -i 'ci skip|skip ci' && echo "commit audit: $(wc -l < /tmp/03-04-subjects.txt | tr -d ' ') commits, one feat(cli)!:, rest test/docs/chore, no CI-skip"</automated>
    <fails_when>non-zero exit or no `commit audit:` line — a subject fails pr-title.yml's regex, there is not exactly one `feat(` / one `!:` subject, another commit type than test/docs/chore landed, the feat commit lacks its `BREAKING CHANGE:` footer, or a CI-skip marker exists anywhere in the phase's history</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go build ./... && GOTOOLCHAIN=go1.26.6 go test ./internal/cli/... -count=1 2>&1 | tee /tmp/03-04-cli.txt | tail -3; ! rg -q '^(--- FAIL|FAIL)' /tmp/03-04-cli.txt && test "$(rg -c '^ok\s' /tmp/03-04-cli.txt)" -ge 5 && GOTOOLCHAIN=go1.26.6 go test ./internal/daemon/ -count=1 -run 'TestUnlock|TestAcquire|TestIsStale' 2>&1 | tail -1 | rg -q '^ok\s' && (task docs:cli:drift > /tmp/03-04-drift.txt 2>&1; echo "exit=$?" >> /tmp/03-04-drift.txt); rg -q '^exit=0$' /tmp/03-04-drift.txt && rg -q 'compared 1 generated file' /tmp/03-04-drift.txt && GOTOOLCHAIN=go1.26.6 task test:wireoracle > /tmp/03-04-wo.txt 2>&1 && rg -q '^ok\s+github.com/seanb4t/codegraph-go/test/wireoracle' /tmp/03-04-wo.txt && test -z "$(git status --porcelain -- internal docs cmd testdata)" && echo "final gate green: build, cli tests, daemon lock tests, docs:cli:drift, wireoracle"</automated>
    <fails_when>non-zero exit or no `final gate green` line — the build or a cli/daemon-lock test is red, `docs:cli:drift` exits non-zero or fails to print its count, the wire oracle is red, or the source tree is dirty at the end of the phase</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && L=.planning/phases/03-verb-fold/03-MUTATION-LOG.md && rg -c '^## Family \(c\) — VERB-05' "$L" && rg -c '^## Family \(a\) — VERB-05' "$L" && rg -c '^## Family \(b\) — VERB-07' "$L" && rg -q 'after: [0-9]+ lines across [0-9]+ files' "$L" && rg -q 'allowlist=2' "$L" && rg -q -F 'feat(cli)!: fold query into search --full and unlock into daemon unlock' "$L" && rg -q -i 'byte-clean' "$L" && git show --format=%s --name-only HEAD > /tmp/03-04-c2.txt && rg -q '^docs\(03-04\): record the post-fold' /tmp/03-04-c2.txt && test "$(rg -v '^docs\(03-04\)' /tmp/03-04-c2.txt | rg -v '^$' | tr '\n' ' ')" = ".planning/phases/03-verb-fold/03-MUTATION-LOG.md " && ! git log -1 --format=%s%n%b | rg -q -i 'ci skip|skip ci' && test ! -e .github/__census_control__.md && ! git ls-files --error-unmatch .github/__census_control__.md >/dev/null 2>&1 && echo "family (c) recorded; three families present; control gone"</automated>
    <fails_when>non-zero exit or a family count of 0 — Family (c) is missing or lacks the `after:` line / `allowlist=2` / the feat subject / the byte-clean proof, an earlier family was clobbered, the commit is not the `docs(03-04):` log-only commit, a CI-skip marker is present, or the census control exists on disk or in the index</fails_when>
  </verify>
  <acceptance_criteria>
    - The planted control is found on `:1:` and `:3:` by the identical instrument before the after-run and removed byte-clean.
    - The after-run reports hits only in `./internal/cli/renamed.go` (≥1) and `./internal/cli/testdata/cli-reference-allowlist.txt` (exactly 2); counts printed as `after: N lines across F files (renamed.go=S, allowlist=2)`.
    - Commit audit from `F^..HEAD`: all subjects regex-conformant; one `feat(cli)!:` with a `BREAKING CHANGE:` footer; all others `test(`/`docs(`/`chore(`; no CI-skip marker.
    - Final gate green: build, `./internal/cli/...`, daemon lock tests, `task docs:cli:drift` (with `compared 1 generated file`), `task test:wireoracle`; tree clean.
    - `03-MUTATION-LOG.md` carries Families (a), (b), (c); the last commit is `docs(03-04): record the post-fold …` touching only the log.
  </acceptance_criteria>
  <done>The "after" half of VERB-05 is on the record with the same instrument and control as the "before", the phase's commit history has D-15's exact shape, and every gate is green at the phase's final commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gsd-tools phase verb → `.planning/ROADMAP.md` | a scope-sensitive parser reads this file; only the tool's own shapes may be written, plus value fills |
| planted control file → working tree | an untracked file under `.github/` for one step; never staged |
| `rg` instrument → VERB-05 verdict | the "after" is only comparable to the "before" if the instrument is byte-identical |
| phase commit history → release-please | the single `feat(cli)!:` commit is what categorises the release; extra `feat` commits or a missing footer change the CHANGELOG |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | Tampering | hand-authored ROADMAP structure truncating the milestone window | high | mitigate | Row written by `phase add --id`; verify asserts additions-only, one `+###` line, no version token in the heading, and runs the parser's own `getMilestonePhaseFilter` to prove the active set is unchanged. |
| T-03-04-02 | Repudiation | after-census "zero" trusted without a control | high | mitigate | Same control, same instrument, found on `:1:`/`:3:` before the real run; the allowed-hit set is asserted exactly (allowlist=2, renamed.go≥1, nothing else). |
| T-03-04-03 | Tampering | planted control leaking into a commit | medium | mitigate | EXIT trap; `git status --porcelain .github` empty; path asserted untracked. |
| T-03-04-04 | Tampering | a second `feat` commit or a footer-less breaking change mis-categorising the release | medium | mitigate | Commit audit over `F^..HEAD`: one `feat(`, one `!:`, footer present, others test/docs/chore. |
| T-03-04-05 | Denial of Service | `protect-main` blocked by a `[ci skip]` marker | high | mitigate | Every subject and body in the phase's range grepped for CI-skip markers. |
| T-03-04-06 | Denial of Service | a known load flake read as a phase failure (or a real failure hidden as a flake) | medium | mitigate | `TestDaemonSharedWriter` re-run alone if it times out; both results recorded; only an isolated pass is classified as the known flake. |
| T-03-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install. |
</threat_model>

<verification>
- `### Phase 999.5:` under `## Backlog`, additions-only, parser view unchanged, `roadmap validate` clean.
- After-census: control found; hits only in `renamed.go` + 2 allowlist lines.
- Commit audit: one `feat(cli)!:` with footer; all subjects regex-conformant; no CI-skip.
- Final gate green under `GOTOOLCHAIN=go1.26.6`.
</verification>

<success_criteria>
- VERB-05's after half and VERB-08's ROADMAP half are on the record; the phase ends with a clean tree and green gates.
</success_criteria>

## Artifacts this phase produces

- `.planning/ROADMAP.md` — `## Backlog` row `### Phase 999.5: remove the query/unlock rename stubs` (this plan); `.planning/phases/999.5-remove-the-query-unlock-rename-stubs/.gitkeep` (this plan)
- `.planning/phases/03-verb-fold/03-MUTATION-LOG.md` — `## Family (c) — VERB-05` (this plan)
- Source/test/reference artefacts: 03-02; Families (a)/(b), Baseline, `COVERAGE.md`: 03-01 / 03-03

## Flagged assumptions (probe rows left unresolved — surfaced, not dropped)

- VERB-08 (unclassified): the CHANGELOG's BREAKING CHANGES entry is produced by release-please after merge and is inspected then, not asserted here (D-15). The PR title must equal the feat subject under the squash-merge model (RESEARCH Pitfall 1) — a `/gsd-ship` step, outside this phase.
- Planner interpretation surfaced for the maintainer: "outside the stubs" (D-08) is read as outside the stub declaration = `internal/cli/renamed.go` + its two `cli-reference-allowlist.txt` lines, because the allowlist grammar's key is cobra's `CommandPath()` and no other spelling of the key is accepted by `TestEveryRegisteredFlagIsAccountedFor`.

<output>
Create `.planning/phases/03-verb-fold/03-04-SUMMARY.md` when done — include the ROADMAP diff, the after-census transcript with its `after:` line, the commit subject list, and the final-gate lines verbatim.
</output>
