---
phase: "2"
slug: "guards-ci-wiring-docs-burn-down"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: "2026-09-16"
---

# Phase 2 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

Register origin: authored at plan time — all seven PLAN.md files carry a `<threat_model>` block (53 rows: 42 mitigate / 11 accept; 25 high / 15 medium / 13 low). Verified at HEAD `d4057523` on `gsd/v0.14.0-milestone` by `gsd-security-auditor` (ASVS L1, block on `high`), after the code-review fix (`97bb6a13`, WR-01 curl timeouts) and the human review recorded in `02-UAT.md`. The auditor verified mitigations only (re-running checks itself, not reading SUMMARY claims) and did not scan for new threats.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| scratch worktree → main working tree | the `98cd41dd` replay runs only in a detached worktree; the main tree, `Taskfile.yml` and `web/` are never checked out to the incident commit | none by construction |
| GitHub REST (rulesets) → CI `test` job | a read-only `GET /repos/…/rulesets/20157557` (Metadata:read; unauthenticated works) compared against a committed fixture; hard-fails on any non-2xx/empty/unparseable/non-active/zero response and on a stall (curl timeouts) | ruleset JSON (name, enforcement, contexts only) |
| fork PR → `ci.yml` `test` job | the new steps run on `pull_request` with the workflow's sole `permissions: contents: read`; no `pull_request_target`, no write scope, no secret beyond `GITHUB_TOKEN` on the drift step | PR head source |
| `anchore/sbom-action/download-syft` → runner | SHA-pinned to the same commit `release.yml` and `linux-cross-canary.yml` trust | syft binary |
| maintainer → `protect-main` ruleset | the D-08 PUT body is built from an authenticated read (so `bypass_actors` survives), prepared by the executor, applied only on the maintainer's explicit authorization at a `blocking-human` halt, verified read-only afterwards | repository settings |
| shadcn-svelte registry → vendored `web/src/lib/components/ui/**` | regenerated by the pinned CLI (`shadcn-svelte@1.5.1`) into one snapshot; no `pnpm patch`, lockfile untouched; the diff was scanned for injected logic / removed a11y / changed exports | third-party component source |
| `web/src` → committed `web/build/**` | rebuilt and committed in the same commit as the source change; `web:drift` guards both halves on every PR | generated bundle |
| tool-owned planning files (`WINDOWS.md`, `STATE.md`, todos, seeds) → executor | written only through `gsd-tools` verbs (`windows fixed`, `state add-blocker`, `state.resolve-blocker`, `init todos`); no invented statuses or shapes; single writer per wave | ledger/state values |
| documentation claims → readers | census and edits are positive-controlled; no exposure IDs named in `SECURITY.md`; no raw dependency counts left to rot | prose |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-02-01-01 | Tampering | `98cd41dd` replay vs main tree | high | mitigate | worktree-only replay (`git worktree add --detach`), removed + pruned; `git status --porcelain -- Taskfile.yml web/` empty; no checkout/stash/reset in the plan | closed |
| T-02-01-02 | Repudiation | mutation-log evidence | medium | mitigate | `02-MUTATION-LOG.md` Family (a): exit 201, 32/`8658e6fe…` vs 22/`a76add11…`, plus green control on HEAD; 02-VERIFICATION re-ran it byte-identical | closed |
| T-02-01-03 | Tampering | `web:drift` Taskfile target | high | mitigate | `git diff 1b1184ca..HEAD -- Taskfile.yml` empty across all phase commits (D-01) | closed |
| T-02-01-04 | Tampering | `REQUIREMENTS.md` / `ROADMAP.md` rewording | high | mitigate | `cd8b4bc9` touches exactly those two files; 7 `#### Phase N:` headings intact; no "paired assertion" language survives | closed |
| T-02-01-05 | Denial of Service | `protect-main` ruleset | high | mitigate | 34 phase commits scanned: no `[ci skip]`/`[skip ci]` (rule f18zrdsgx5) | closed |
| T-02-01-06 | Information Disclosure | replay network egress | low | accept | R-02-01 — `98cd41dd:Taskfile.yml`'s `web:drift` body only hashes; no curl/wget/pnpm/go-get | closed |
| T-02-01-SC | Tampering | package-manager installs | low | accept | R-02-02 — 02-01 commits touch only `.planning/` | closed |
| T-02-02-01 | Tampering | syft download | high | mitigate | `ci.yml:210` `download-syft@e22c3899…` — same SHA as `release.yml:141`, `linux-cross-canary.yml:128,261` | closed |
| T-02-02-02 | Elevation of Privilege | `test` job permissions | medium | mitigate | sole `permissions: contents: read` (`ci.yml:42-43`); triggers `pull_request` + `push: main` only | closed |
| T-02-02-03 | Tampering | step run bodies | medium | mitigate | bare `run: task check:gonum` / `task check:no-force-layout`; `TestWorkflowRunBodiesInvokeTask` green | closed |
| T-02-02-04 | Repudiation | planted-violation RED | high | mitigate | Family (b): planted `name: 'cose'` → exit 201 naming the file; removed → exit 0 (auditor re-ran) | closed |
| T-02-02-05 | Denial of Service | step ordering / runner deps | medium | mitigate | `Set up Node` < `Install syft` < `check:gonum` < `check:no-force-layout`; Taskfile preconditions `command -v go/syft/node/awk` fail loud | closed |
| T-02-02-06 | Denial of Service | `protect-main` ruleset | high | mitigate | same CI-skip scan: none | closed |
| T-02-02-07 | Tampering | planted file leaking into the tree | medium | mitigate | never tracked (`git ls-files --error-unmatch` fails; `git log --all` empty); tree clean before/after | closed |
| T-02-02-SC | Tampering | go module bump | low | accept | R-02-03 — `0e9ffb5b` is version bumps only (grpc 1.82.1→1.83.2, x/crypto, x/net, x/text, genproto), zero new modules | closed |
| T-02-03-01 | Tampering / Repudiation | ruleset script fail-closed | high | mitigate | `check-ruleset-drift.sh` hard-fails non-200/empty (:100-103), unparseable/name/enforcement (:108-124), zero-live (:138-141); re-ran 404 and `{not json` → exit 1, no PASS line | closed |
| T-02-03-02 | Tampering | fixture loader / empty fixture | high | mitigate | `readRequiredCheckNames` errors on read/zero/duplicate; `t.Fatalf` on error; 4 loader tests green; script exits before fetch on an empty fixture | closed |
| T-02-03-03 | Tampering | vacuous self-check | high | mitigate | planted-context self-check runs before the real compare; live `self-check PASS`; planted extra fixture line → diff, exit 1 | closed |
| T-02-03-04 | Information Disclosure | ruleset body in logs | low | mitigate | body only piped into `jq`; only counts, `.name`, `.enforcement`, contexts and the diff are printed | closed |
| T-02-03-05 | Elevation of Privilege | `GITHUB_TOKEN` scope | medium | mitigate | step-level `env: GITHUB_TOKEN` is the only secret reference; `permissions:` unchanged; unauthenticated run → PASS (Metadata:read suffices) | closed |
| T-02-03-06 | Denial of Service | API 403 / rate limit | low | accept | R-02-04 — a 403 takes the same named hard-fail branch as the verified 404; never a skip | closed |
| T-02-03-07 | Denial of Service | `protect-main` ruleset | high | mitigate | same CI-skip scan: none | closed |
| T-02-03-SC | Tampering | curl/jq availability | low | accept | R-02-05 — `command -v curl jq` precondition with a named error | closed |
| T-02-04-01 | Elevation of Privilege | executor applying the ruleset change unprompted | high | mitigate | `<precondition>` + `user_setup`; fixture still 7 at `6025f848`; 8.5 h halt between package (22:55) and live `updated_at` (07:29) with the maintainer's authorization recorded in Family (c) and `02-UAT.md` test 2 | closed |
| T-02-04-02 | Tampering | PUT body dropping `bypass_actors` / a rule type | high | mitigate | body: `bypass_actors` (1), 5 rule types, 8 contexts, `integration_id` 15368, no `id`/`_links`; authenticated read of live 20157557 → `bypass_actors` still 1, 8 contexts, `active` | closed |
| T-02-04-03 | Repudiation | "GREEN because the fixture was edited" | high | mitigate | fixture 7→8 in `8e4b2aae` 4 min AFTER live `updated_at`; Family (c) RED (6 vs 7) → GREEN (8 vs 8); auditor re-ran → `PASS — 8 contexts identical` | closed |
| T-02-04-04 | Information Disclosure | secrets in the committed body | low | accept | R-02-06 — no token/bearer/authorization strings in the body | closed |
| T-02-04-05 | Denial of Service | `tmux e2e` required on every PR | low | accept | R-02-07 — tmux job green on main (run 34859136748); the maintainer's informed choice | closed |
| T-02-04-06 | Tampering | STATE.md blocker resolution | medium | mitigate | `8e4b2aae` STATE.md hunk removes exactly one `[Phase 08-03] user_setup…` bullet; `### Blockers/Concerns` intact | closed |
| T-02-04-07 | Denial of Service | `protect-main` ruleset | high | mitigate | same CI-skip scan: none | closed |
| T-02-04-SC | Tampering | installs | low | accept | R-02-08 — commits touch only the fixture + `.planning/` | closed |
| T-02-05-01 | Tampering | regenerated third-party components | high | mitigate | pinned `shadcn-svelte@1.5.1` (no `@latest`); 24 `.svelte` files only; zero added `fetch/eval/innerHTML/{@html}/import()/on*=/<script`; no import/export/`$props`/handler changes; every removed `aria-*`/`[role]` selector reappears in the class string; `index.ts` byte-identical; `CommandLinkItem` has no external caller (IN-02) | closed |
| T-02-05-02 | Tampering | lockfile / patches | medium | mitigate | `web/package.json`, `web/pnpm-lock.yaml` unchanged since phase start; no `patchedDependencies`/`web/patches` | closed |
| T-02-05-03 | Repudiation | drift monitor honesty | high | mitigate | `pnpm --version` in `web/` → 11.23.0; `task web:components:drift` → 50/50 byte-identical, PASS (auditor re-ran); RED named the same 24 files as CI run 34820878640 | closed |
| T-02-05-04 | Tampering | source/build split commit | high | mitigate | `75f5633c` carries 24 `web/src` + 21 `web/build` paths; `task web:drift` at HEAD → 119/36 MATCH | closed |
| T-02-05-05 | Denial of Service | runtime UI breakage | medium | mitigate | `./codegraph` embeds the HEAD bundle; `breadcrumb-check.mjs` success (418 links); `graph-console-check.mjs` self+guava → 0 errors | closed |
| T-02-05-06 | Denial of Service | `protect-main` ruleset | high | mitigate | same CI-skip scan: none; no `@latest` in the commit body | closed |
| T-02-05-SC | Tampering | registry supply chain | medium | accept | R-02-09 — CLI pinned `@1.5.1` in the commit body and the Taskfile target; lockfile unchanged | closed |
| T-02-06-01 | Repudiation | census vacuity | high | mitigate | planted control seen NARROW=1 BROAD=1, removed → 0, file absent; the sole narrow hit is inside a `Corrected 2026-08-01` block; 7 broad hits classified | closed |
| T-02-06-02 | Information Disclosure | exposure IDs in SECURITY.md | medium | mitigate | no `GO-20xx-`/`CVE-`/`GHSA-` in `SECURITY.md`; the one sentence names `tool-vuln`/`go.tool*.mod`/never fails; `ci.yml:351-372` confirm advisory | closed |
| T-02-06-03 | Tampering | rotting dependency counts | low | mitigate | § 2 has no `N (direct\|indirect\|total\|module)`; `mark3labs` absent; `modelcontextprotocol/go-sdk` present | closed |
| T-02-06-04 | Tampering | historical correction blocks | medium | mitigate | `corrected 2026-08-01` occurs exactly once each in `docs/RELEASE.md` and `docs/RELEASE-PROCEDURES.md` | closed |
| T-02-06-05 | Tampering | census control file leaking | medium | mitigate | never tracked; `git log --all` empty; `git status --porcelain docs/` empty after the auditor's re-run | closed |
| T-02-06-06 | Spoofing | GH #14 closure | medium | mitigate | `gh issue view 14` → CLOSED 2026-09-16T02:08:32Z with the census comment | closed |
| T-02-06-07 | Denial of Service | `protect-main` ruleset | high | mitigate | same CI-skip scan: none | closed |
| T-02-06-SC | Tampering | installs | low | accept | R-02-10 — `99bcb512`/`62e8b299` touch only the two docs | closed |
| T-02-07-01 | Tampering | `WINDOWS.md` writes | high | mitigate | `9f6603a2` touches only `.planning/WINDOWS.md`, value-only diff with five sub-second `resolved_at` stamps (sequential verb calls); `windows status` → open 6, 13/16/29/31/33 fixed, 20/21/34 open | closed |
| T-02-07-02 | Repudiation | closure evidence | high | mitigate | 02-07-SUMMARY evidence table re-checked: TS-framing grep none; stale-count grep none; Family (a) present; PR #71 MERGED; tmux run success | closed |
| T-02-07-03 | Tampering | STATE.md todo table / blockers | high | mitigate | `init todos` `pending_read_ok true, todo_count 1`; Pending Todos body byte-identical to the tool render; `3d45ed65` adds 2 bullets, removes 0 | closed |
| T-02-07-04 | Spoofing | unsolicited upstream issue | medium | mitigate | no issue opened on open-gsd/gsd-core (`gh issue list --author @me` → 0); draft lives in 02-07-SUMMARY | closed |
| T-02-07-05 | Tampering | SEED-001 frontmatter | low | mitigate | `status: implemented`, `consumed_by`, `consumed_on: 2026-09-07` in SEED-002's field order; `planted`/`audit_acknowledged` intact; `list-seeds implemented` → SEED-001, SEED-002 | closed |
| T-02-07-06 | Denial of Service | `protect-main` ruleset | high | mitigate | same CI-skip scan: none | closed |
| T-02-07-SC | Tampering | installs | low | accept | R-02-11 — `9f6603a2`/`3d45ed65`/`ff500512` touch only `.planning/` | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

Cross-check: the live `protect-main` ruleset (20157557) reads 8 contexts with `enforcement: active`; WINDOWS #29/#31/#13/#16/#33 read `fixed`; GH #14/#15/#16/#20 are CLOSED.

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| R-02-01 | T-02-01-06 | The replay's `web:drift` body (at `98cd41dd`) only hashes files with `sha256sum`/`shasum`; it makes no network call, so a scratch checkout of an old commit cannot exfiltrate anything | plan 02-01 register (maintainer-approved at discuss) | 2026-09-16 |
| R-02-02 | T-02-01-SC | 02-01 changed no manifest or lockfile — commits touch only `.planning/` | plan 02-01 register | 2026-09-16 |
| R-02-03 | T-02-02-SC | The only manifest-touching commit (`0e9ffb5b`) is a scoped version bump for GO-2026-6348 (grpc, x/crypto, x/net, x/text, genproto) with zero new modules; `go mod verify` and `go build ./...` pass. IN-01 (orphaned pre-bump `go.sum` lines) is cosmetic | plan 02-02 register + 02-REVIEW.md | 2026-09-16 |
| R-02-04 | T-02-03-06 | A GitHub 403/rate-limit reaches the same named non-2xx hard-fail branch as a 404 — the step fails loud; a rerun after the window is the remedy, never a skip | plan 02-03 register | 2026-09-16 |
| R-02-05 | T-02-03-SC | `curl` and `jq` are ubuntu-latest runner defaults; the script's `command -v` precondition fails with a named error if either is absent | plan 02-03 register | 2026-09-16 |
| R-02-06 | T-02-04-04 | The committed PUT body carries ruleset structure only — no token, bearer or authorization material (grep verified) | plan 02-04 register | 2026-09-16 |
| R-02-07 | T-02-04-05 | Requiring `tmux e2e` on every PR was the maintainer's informed choice (D-08; affirmed in `02-UAT.md` test 2); the job is green on `main` (run 34859136748), and a future flake is a CI-reliability item, not a security one | maintainer (discuss D-08; UAT 2026-09-16) | 2026-09-16 |
| R-02-08 | T-02-04-SC | 02-04 commits touch only the fixture and `.planning/` | plan 02-04 register | 2026-09-16 |
| R-02-09 | T-02-05-SC | The shadcn-svelte registry is unversioned by design; the CLI is pinned (`@1.5.1`), the lockfile is unchanged, and the regenerated diff was scanned for injected logic/removed a11y/changed exports before commit. The weekly `components-drift.yml` monitor re-checks the snapshot; its `pnpm dlx` step still runs under bare pnpm 12.x (pre-existing, disclosed in 02-05) | plan 02-05 register; maintainer UAT (visual deltas accepted) | 2026-09-16 |
| R-02-10 | T-02-06-SC | 02-06 commits touch only `docs/RELEASE.md` and `SECURITY.md` | plan 02-06 register | 2026-09-16 |
| R-02-11 | T-02-07-SC | 02-07 commits touch only `.planning/`, through tool verbs | plan 02-07 register | 2026-09-16 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-16 | 53 | 53 | 0 | gsd-security-auditor (opus) via /gsd-secure-phase 2, autonomous run — HEAD `d4057523` |

Informational observations from the audit (not threats): (1) the four new `ci.yml` steps have executed only locally — the milestone branch has no PR yet, so the first push is their first runner execution; (2) `web:components:drift`'s `pnpm dlx` step runs under bare pnpm 12.4.1 while its install steps honor the 11.23.0 pin — a pre-existing property of the unmodified target, settled by the next scheduled monitor run; (3) IN-01 orphaned `go.sum` lines (Info, carried from 02-REVIEW.md).

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-16
