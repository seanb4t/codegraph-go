---
phase: 02-guards-ci-wiring-docs-burn-down
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/RELEASE.md
  - SECURITY.md
autonomous: true
requirements: [DOCS-08, DOCS-09, DOCS-10]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "DOCS-09 is a positive-controlled census, not an assumed rewrite: a planted sentence in a scratch file inside the census scope is found by the census BEFORE its zero-live-hit result is trusted, the scratch file is removed by an EXIT trap, and the final census run proves the planted path is absent — so an interrupted run cannot leave the control behind and a census that looks at nothing cannot read as clean. D-14 (probe DOCS-09/concurrency, explicit; rule 84d1gfpywd)."
    - "Every co-occurrence of provenance/SLSA/attest wording with checksums-file wording across `README.md`, `docs/`, `.github/workflows/`, `SECURITY.md` and `internal/upgrade/` (excluding `CHANGELOG.md` and `.planning/`) is listed and classified in the SUMMARY as one of: live-incorrect (reworded), correct transport wording (kept), historical `Corrected 2026-08-01` narration (kept); RESEARCH found zero live-incorrect claims and the census either confirms that or rewords the hit to 'provenance is attested over each platform binary; the checksums file is the transport for the subject list, not a subject'. D-14."
    - "GH #14 is closed with `gh issue close 14 --comment` citing the census command and its classified output; its stale line numbers are not relied on. D-14."
    - "`docs/RELEASE.md` § 2 'Dependency tree (DIST-05)' carries no raw dependency counts (none of `27 direct`, `134`, `107`, `remaining 13`, `14 are`), keeps the wide-but-shallow framing (one grammar module per supported language plus the shared `tree-sitter/go-tree-sitter` binding, named), names a short list of deliberately chosen direct requires from the CURRENT `go.mod` — `cockroachdb/pebble/v2`, `modelcontextprotocol/go-sdk`, `spf13/cobra`, `fsnotify/fsnotify`, `sigstore/sigstore-go`, `tailscale/hujson`, `gonum.org/v1/gonum`, `google.golang.org/protobuf`, `go.uber.org/goleak`, the `charm.land/*/v2` TUI trio, and a handful of `golang.org/x/*` — credits `modelcontextprotocol/go-sdk` (the string `mark3labs` no longer appears anywhere in `docs/RELEASE.md`), and points at `go list -m all` / `go mod graph` for live numbers. No drift test. D-13."
    - "Root `SECURITY.md`'s two-scanner bullet gains exactly one sentence: `govulncheck` blocks merges on the main module graph; the isolated tool modfiles (`go.tool*.mod`) are scanned by the advisory `tool-vuln` job, which reports and never fails the build — naming the job as `ci.yml` names it, with no named exposure and no drift assertion; the bullet's existing sentences are unchanged. D-15."
  artifacts:
    - "docs/RELEASE.md — § 2 dependency paragraph reshaped (D-13); provenance wording census-verified (D-14)"
    - "SECURITY.md — one added sentence in the 'Dependency vulnerabilities' bullet (D-15)"
  key_links:
    - "current `go.mod` direct requires → the named list in docs/RELEASE.md § 2 (names only, never counts) → `go list -m all` / `go mod graph` as the pointer for live numbers"
    - "`.github/workflows/ci.yml` job `tool-vuln (VULN-01/02/03, advisory)` → the one SECURITY.md sentence naming it"
    - "census output (classified) → GH #14 close comment"
  prohibitions:
    - statement: "No drift test, docs-grep guard, or Taskfile target is added for any of the three wording changes — a wording change gets no test (v0.13.0 Phase 12 ruling; D-13/D-15)."
      status: resolved
      verification: judgment
    - statement: "No raw dependency count of any kind is written into docs/RELEASE.md § 2 — not the stale 27/134/107/13/14 and not the current 34/116/150/20 either; numbers come from `go list -m all` at read time (D-13)."
      status: resolved
      verification: judgment
    - statement: "The two historical-correction callouts (`docs/RELEASE.md` ~line 145 and `docs/RELEASE-PROCEDURES.md` ~line 260, both framed `Corrected 2026-08-01 … Both were wrong`) are NOT reworded — they are deliberate record, not live claims (RESEARCH Pitfall 6)."
      status: resolved
      verification: judgment
    - statement: "SECURITY.md names no specific exposure (no GO-2026-5932, no package name) and gains exactly one sentence; the existing disjoint-scope paragraph and the pnpm-audit blocking description stay as written (D-15)."
      status: resolved
      verification: judgment
    - statement: "The planted census control is never committed: it lives at an untracked scratch path removed by an EXIT trap, and `git status --porcelain docs/` is empty afterwards."
      status: resolved
      verification: judgment
    - statement: "No `[ci skip]` / `[skip ci]` in any commit message."
      status: resolved
      verification: judgment

flagged_assumptions:
  - id: EDGE-DOCS-08-01
    requirement: DOCS-08
    probe: "unclassified — review manually"
    status: unresolved
    note: "Deterministic edge probe returned `unclassified` for DOCS-08; not auto-resolved. Planner's reading: the named direct-require list will itself go stale on the next `go.mod` change (a new direct require, or one removed). D-13 accepts this by pointing readers at `go list -m all` for anything live; no acceptance criterion tracks future additions. Surfaced for the maintainer."
  - id: EDGE-DOCS-10-01
    requirement: DOCS-10
    probe: "unclassified — review manually"
    status: unresolved
    note: "Deterministic edge probe returned `unclassified` for DOCS-10; not auto-resolved. Planner's reading: if the `tool-vuln` job is ever renamed or made blocking, the one sentence becomes wrong with no gate (D-15: no drift assertion by decision). The sentence names the job exactly as `ci.yml` does so a rename is greppable; surfaced, not dropped."
---

<objective>
Bring three documentation claims back to what ships: the dependency paragraph stops quoting counts that
drift on every bump and credits the SDK actually in `go.mod`; the provenance wording is proven correct
everywhere by a positive-controlled census (closing GH #14); and `SECURITY.md` says in one sentence
what the advisory `tool-vuln` job covers.

**What are we testing, and why?** For DOCS-09 we are testing whether ANY live sentence still says
provenance is attested over the checksums file — by looking everywhere the claim could live, with a
planted control proving the search can see. RESEARCH found zero live hits (the three sites GH #14
cited were rewritten by the `actions/attest-build-provenance` migration), so this is a verification
task with a rewrite branch that is expected to be empty. For DOCS-08 and DOCS-10 we are testing
nothing mechanically: they are wording changes verified by reading the diff (Phase 12 ruling — a
wording change gets no test), with the one durable fix being to remove the numbers that made the
paragraph wrong twice already (WINDOWS #13 at 32 direct; today at 34).

Purpose: DOCS-08 closes the cause of WINDOWS #13 (02-07 closes the row); DOCS-09 closes GH #14;
DOCS-10 promotes GRD-08 (declined at v0.13.0) as one sentence, no guard.

Output: reworded `docs/RELEASE.md` § 2, census evidence + GH #14 closed, one sentence in `SECURITY.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-CONTEXT.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Positive-controlled provenance census across the whole repo, classified, and GH #14 closed on its output</name>
  <files>docs/RELEASE.md, docs/RELEASE-PROCEDURES.md, README.md, SECURITY.md, .github/workflows/release.yml (census scope — edited only if a live-incorrect hit is found)</files>
  <read_first>
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md § "Code Examples → DOCS-09" and § "Pitfall 6" — the narrow claim-shape pattern, the one historical hit at `docs/RELEASE.md:145`, and the already-correct live sentences (README.md:178-179, SECURITY.md:45-46, docs/RELEASE.md:101, release.yml's Attest step comment).
    - docs/RELEASE.md lines 95-150 and docs/RELEASE-PROCEDURES.md lines 250-265 — the correct-wording standard ("subjects are the binaries") and the two `Corrected 2026-08-01` callouts that must NOT be reworded.
    - `gh issue view 14` — the issue text and its three (stale) cited sites, so the close comment can say what became of each.
    - /Users/sean/.claude/rules/grepping.md — durable-gate checklist for the `rg` invocation (prove RED with the control; count occurrences with `-o | wc -l`, never `-c`).
  </read_first>
  <action>
Scope (D-14): `README.md SECURITY.md docs/ .github/workflows/ internal/upgrade/` with
`--glob '!CHANGELOG.md'`; `.planning/` is never in scope. Two patterns, both `rg -n -U -i`:
  - NARROW (the incorrect claim shape, from RESEARCH):
    `provenance[^.]{0,80}(generated|attested)[^.]{0,40}over[^.]{0,40}checksums file`
  - BROAD (every candidate co-occurrence, either order, within 160 chars, word-boundaried):
    `\b(provenance|slsa|attest\w*)\b[^.]{0,160}\b(checksums?(\.txt| file)|sha256sums)\b` OR the
    same two groups in the opposite order. At plan time the BROAD pattern matched 12 spans.

Sequence, in one shell script written to the scratchpad (not committed):
  1. Pre-gate: `git status --porcelain docs/ README.md SECURITY.md .github/workflows/ internal/upgrade/` empty.
  2. Plant the control: write `docs/zz-census-control.md` (untracked) containing one sentence that
     matches the NARROW pattern — e.g. that SLSA provenance is generated over the checksums file —
     and install `trap 'rm -f docs/zz-census-control.md' EXIT`.
  3. Run both patterns over the scope; assert the planted path appears in BOTH outputs (count
     matches with `-o | wc -l`, never `-c`). If either misses it, stop and report — the census
     cannot see.
  4. Remove the control (`rm -f`), run both patterns again, assert the planted path is absent from
     both outputs; save the outputs as the census evidence.
  5. Classify every BROAD hit in a table (file:line | quoted span | class ∈ {live-incorrect,
     correct-transport-wording, historical-correction} | action). The NARROW pattern's only expected
     hit is `docs/RELEASE.md:145` inside a `Corrected 2026-08-01` block → historical-correction, keep.
  6. For any live-incorrect hit: reword the sentence to the standard — provenance is attested over
     each platform binary (and `.zip` archive); the checksums file is the transport for the subject
     list, not a subject — matching `docs/RELEASE.md:101`'s voice; re-run the NARROW census until it
     reports only historical-correction hits. Expected: zero rewrites (RESEARCH).
  7. Post-gate: `git status --porcelain docs/` shows no untracked control file.
  8. Close the issue: `gh issue close 14 --comment "<body>"` where the body states the census
     command (scope, both patterns), the positive-control result, the classified hit table, what
     became of each of the issue's three cited sites (rewritten by the attest-build-provenance
     migration; line numbers stale), and that zero live claims remain. Confirm `gh issue view 14
     --json state` reports `CLOSED`.

If a rewrite happened, commit it: `docs(02-06): reword provenance claims found by the census (DOCS-09, GH #14)`;
if none, there is no commit for this task — the SUMMARY carries the census evidence and the
issue-close link.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test -z "$(git status --porcelain docs/)" && printf '%s\n' '# control' 'SLSA provenance is generated over the checksums file in this control sentence.' > docs/zz-census-control.md && n1=$(rg -n -U -i 'provenance[^.]{0,80}(generated|attested)[^.]{0,40}over[^.]{0,40}checksums file' README.md SECURITY.md docs/ .github/workflows/ internal/upgrade/ --glob '!CHANGELOG.md' -o | rg -c 'zz-census-control'); rm -f docs/zz-census-control.md; n2=$(rg -n -U -i 'provenance[^.]{0,80}(generated|attested)[^.]{0,40}over[^.]{0,40}checksums file' README.md SECURITY.md docs/ .github/workflows/ internal/upgrade/ --glob '!CHANGELOG.md' -o | rg -c 'zz-census-control' || true); test "${n1:-0}" -ge 1 && test "${n2:-0}" = "0" && test ! -e docs/zz-census-control.md && echo "control seen=$n1 after-removal=$n2"</automated>
    <fails_when>non-zero exit or no `control seen=` line with a first count ≥ 1 and a second count of 0 — the census cannot see a planted incorrect claim (vacuous), or the control survived removal</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -n -U -i 'provenance[^.]{0,80}(generated|attested)[^.]{0,40}over[^.]{0,40}checksums file' README.md SECURITY.md docs/ .github/workflows/ internal/upgrade/ --glob '!CHANGELOG.md' > /tmp/02-06-narrow.txt; cat /tmp/02-06-narrow.txt; awk -F: '{print $1":"$2}' /tmp/02-06-narrow.txt | while IFS=: read -r f l; do [ -z "$f" ] && continue; s=$((l>12?l-12:1)); sed -n "${s},${l}p" "$f" | rg -q -i 'corrected 2026-08-01' || { echo "LIVE HIT at $f:$l"; exit 1; }; done</automated>
    <fails_when>non-zero exit or a `LIVE HIT at` line — a narrow-pattern hit exists that is not inside a `Corrected 2026-08-01` historical block, i.e. a live incorrect provenance claim survives</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && gh issue view 14 --json state,comments --jq '.state + " " + ([.comments[].body] | map(select(test("census"; "i"))) | length | tostring)' | tee /tmp/02-06-gh14.txt && rg -q '^CLOSED [1-9]' /tmp/02-06-gh14.txt</automated>
    <fails_when>non-zero exit or output not matching `CLOSED <n≥1>` — GH #14 is still open, or it was closed without a comment citing the census</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -n -i 'corrected 2026-08-01' docs/RELEASE.md docs/RELEASE-PROCEDURES.md -o | wc -l | tr -d ' ' | rg -q '^2$' && test -z "$(git status --porcelain docs/ README.md SECURITY.md)"</automated>
    <fails_when>non-zero exit — one of the two historical-correction callouts was removed or reworded, or uncommitted changes/untracked control files remain in the census scope</fails_when>
  </verify>
  <acceptance_criteria>
    - The planted control at `docs/zz-census-control.md` is matched by both patterns while present and by neither after removal; the file does not exist afterwards and is never tracked.
    - The SUMMARY has the classified table for every BROAD hit; every NARROW hit is inside a `Corrected 2026-08-01` block; both historical callouts are intact.
    - Any live-incorrect hit is reworded to the binaries-as-subjects standard (expected: none).
    - `gh issue view 14` reports `CLOSED` with a comment citing the census.
  </acceptance_criteria>
  <done>The repo is shown, by a census that was watched detect a planted claim, to carry no live "provenance over the checksums file" wording, and GH #14 is closed on that evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape docs/RELEASE.md § 2 — drop the raw counts, name the deliberate direct requires, credit the SDK in go.mod (D-13)</name>
  <files>docs/RELEASE.md</files>
  <read_first>
    - docs/RELEASE.md lines 326-375 — § 2 "Dependency tree (DIST-05)" in full: the counts paragraph (134/107/27/14/13), the named grammar-module list, the "remaining 13" list crediting `mark3labs/mcp-go`, the CGo paragraph and the SBOM-is-authoritative closing paragraph (the last two stay).
    - go.mod `require (` block — the 34 non-`// indirect` lines as of plan time: the 13 grammar modules plus `tree-sitter/go-tree-sitter`, `cockroachdb/pebble/v2`, `modelcontextprotocol/go-sdk`, `spf13/cobra`, `fsnotify/fsnotify`, `sigstore/sigstore-go`, `tailscale/hujson`, `google.golang.org/protobuf`, `go.uber.org/goleak`, `gonum.org/v1/gonum`, `charm.land/{bubbletea,bubbles,lipgloss}/v2`, `bmatcuk/doublestar/v4`, `google/go-cmp`, `google/jsonschema-go`, `go.yaml.in/yaml/v3`, `golang.org/x/{mod,sync,term,tools}` — read it fresh; names, never counts, go into the doc.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md § "Code Examples → DOCS-08" and § "State of the Art" — why every count is stale and why D-13 drops them instead of patching.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-PATTERNS.md § "docs/RELEASE.md — dependency paragraph reword" — target shape.
  </read_first>
  <action>
Rewrite ONLY the prose between the § 2 heading's bold lead sentence and the `**CGo is the sole
documented exception.**` paragraph, keeping the heading, the bold lead, the CGo paragraph and the
SBOM-is-authoritative paragraph byte-identical. The new prose:
  - opens by saying `go.mod` lists many more indirect than direct requirements and that the raw
    numbers move with every dependency bump, so this document deliberately quotes none — run
    `go list -m all` (full closure) or `go mod graph` (who pulls in what) for the live picture;
  - keeps the wide-but-shallow framing: one grammar module per supported language, listed by name
    as today (`tree-sitter-go`, `tree-sitter-java`, `tree-sitter-c-sharp`, `tree-sitter-python`,
    `tree-sitter-typescript`, `tree-sitter-javascript`, `tree-sitter-rust`, `tree-sitter-ruby`,
    `tree-sitter-php`, `tree-sitter-c`, `tree-sitter-cpp`, `tree-sitter-kotlin`, `tree-sitter-swift`)
    plus the shared `tree-sitter/go-tree-sitter` binding, pointing at `docs/LANGUAGE-CAPABILITY-MATRIX.md`;
  - names the deliberately chosen non-grammar direct requires as the supply-chain surface worth
    auditing individually — the storage engine `cockroachdb/pebble/v2`, the MCP server
    `modelcontextprotocol/go-sdk`, the CLI framework `spf13/cobra`, the file watcher
    `fsnotify/fsnotify`, the release-verification client `sigstore/sigstore-go`, the JSONC parser
    `tailscale/hujson`, protobuf runtime `google.golang.org/protobuf`, the community-detection
    library `gonum.org/v1/gonum`, the TUI stack `charm.land/bubbletea/v2`, `charm.land/bubbles/v2`,
    `charm.land/lipgloss/v2`, the goroutine-leak test harness `go.uber.org/goleak`, and a handful of
    `golang.org/x/*` toolchain packages — as a named list, with no count word ("thirteen", "13",
    "twenty") attached;
  - replaces every occurrence of `mcp-go` with the go-sdk credit (the string `mark3labs` must not
    survive anywhere in the file).
Do not touch the `Corrected 2026-08-01` callout at ~line 145 or anything outside § 2. No test, no
guard, no Taskfile change (D-13). Read the final paragraph once end-to-end for voice consistency
with the rest of the document.

Commit: `docs(02-06): drop raw dependency counts from RELEASE.md and credit modelcontextprotocol/go-sdk (DOCS-08, D-13)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && awk '/^## 2\. Dependency tree/{f=1} /^## 3\. /{f=0} f' docs/RELEASE.md > /tmp/02-06-sec2.txt && test -s /tmp/02-06-sec2.txt && ! rg -q -i '\b(27|134|107|13|14|34|116|150|20) (direct|indirect|total|are|module)' /tmp/02-06-sec2.txt && ! rg -q -i 'remaining (13|20|thirteen|twenty)' /tmp/02-06-sec2.txt && ! rg -q 'mark3labs' docs/RELEASE.md</automated>
    <fails_when>non-zero exit — § 2 still quotes a raw dependency count (stale or current) or still credits `mark3labs` anywhere in the file</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && for s in 'modelcontextprotocol/go-sdk' 'cockroachdb/pebble/v2' 'go list -m all' 'go mod graph' 'tree-sitter/go-tree-sitter' 'gonum.org/v1/gonum' 'sigstore/sigstore-go' 'wide-but-shallow' 'CGo is the sole documented exception' 'SBOM published with every release binary'; do rg -q -F "$s" /tmp/02-06-sec2.txt || { echo "missing: $s"; exit 1; }; done; echo "sec2 ok"</automated>
    <fails_when>non-zero exit or a `missing:` line — § 2 lacks the go-sdk credit, pebble, the two live-number commands, the binding, a named direct require, the wide-but-shallow framing, or one of the two paragraphs that must survive unchanged</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -n -i 'corrected 2026-08-01' docs/RELEASE.md -o | wc -l | tr -d ' ' | rg -q '^1$' && git show --format= --name-only HEAD > /tmp/02-06-t2-files.txt && test "$(sort /tmp/02-06-t2-files.txt | tr '\n' ' ')" = "docs/RELEASE.md " && git diff --quiet -- Taskfile.yml</automated>
    <fails_when>non-zero exit — the historical callout in RELEASE.md was touched, the commit touched a file other than `docs/RELEASE.md`, or `Taskfile.yml` carries an edit (no drift test may be added)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log -1 --format=%s%n%b > /tmp/02-06-msg2.txt && rg -q '^docs\(02-06\)' /tmp/02-06-msg2.txt && ! rg -q -i 'ci skip|skip ci' /tmp/02-06-msg2.txt</automated>
    <fails_when>non-zero exit — the commit is not this task's docs commit or carries a CI-skip marker</fails_when>
  </verify>
  <acceptance_criteria>
    - § 2 contains no raw count paired with direct/indirect/total/module and no "remaining N"; `mark3labs` is absent from the whole file.
    - § 2 names `modelcontextprotocol/go-sdk`, `cockroachdb/pebble/v2`, `tree-sitter/go-tree-sitter`, `gonum.org/v1/gonum`, `sigstore/sigstore-go`, the `go list -m all` and `go mod graph` pointers, and keeps the `wide-but-shallow`, CGo-exception and SBOM-authoritative sentences.
    - The `Corrected 2026-08-01` callout is intact; the commit touches only `docs/RELEASE.md`; `Taskfile.yml` unchanged.
  </acceptance_criteria>
  <done>The dependency paragraph is true today and cannot go stale by count again, and the MCP credit matches `go.mod`.</done>
</task>

<task type="auto">
  <name>Task 3: One sentence in SECURITY.md on the advisory tool-vuln scope (D-15)</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md lines 61-73 — the `Dependency vulnerabilities — two scanners, two disjoint trees` bullet in full; the new sentence goes at the END of this bullet (after `gap noted below.`), as added lines only, so the diff is additions-only inside this one bullet.
    - .github/workflows/ci.yml lines 311-336 — the `tool-vuln` job: `name: tool-vuln (VULN-01/02/03, advisory)` and its step `Tool-modfile vulnerability scan (advisory — reports, never fails the build)`; the sentence must match this wording, not paraphrase a new claim.
    - go.tool.mod, go.tool-lint.mod, go.tool-proto.mod, go.tool-golangci.mod (names only) — the `go.tool*.mod` glob the sentence refers to.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md § "Code Examples → DOCS-10" — no named exposure, no drift assertion.
  </read_first>
  <action>
Append exactly one sentence to the two-scanner bullet in `SECURITY.md`, in the bullet's existing
voice, stating: `govulncheck` blocks merges on the main module graph only; the build tools pinned in
the isolated tool modfiles (`go.tool*.mod`) are scanned separately by the advisory `tool-vuln` job
in `ci.yml`, which reports findings and never fails the build. Name no vulnerability id and no
package. Add the sentence as new lines at the END of the bullet (after `gap noted below.`),
wrapped at the file's existing column width, WITHOUT re-wrapping any existing line — the diff must
be additions only. Change no other sentence — the disjoint-scope framing and the pnpm-audit
blocking description stay as written. No test, no guard (D-15).

Commit: `docs(02-06): state the advisory tool-vuln scope in SECURITY.md (DOCS-10, D-15)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git show --format= HEAD -- SECURITY.md > /tmp/02-06-sec.diff && test "$(rg -c '^-[^-]' /tmp/02-06-sec.diff || echo 0)" -le 1 && test "$(rg -c '^\+[^+]' /tmp/02-06-sec.diff)" -ge 1 && test "$(rg -c '^\+[^+]' /tmp/02-06-sec.diff)" -le 4 && rg -q 'tool-vuln' SECURITY.md && rg -q -F 'go.tool*.mod' SECURITY.md && rg -q -i 'never fails the build' SECURITY.md && rg -q -i 'advisory' SECURITY.md</automated>
    <fails_when>non-zero exit — the diff is larger than one appended sentence (more than one removed line or more than four added lines), or the added text does not name `tool-vuln`, `go.tool*.mod`, `advisory` and `never fails the build`</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && ! rg -q 'GO-2026-|GO-20[0-9][0-9]-[0-9]+' SECURITY.md && rg -q -F 'two scanners, two disjoint trees' SECURITY.md && rg -q -F 'declared in `web/pnpm-lock.yaml`' SECURITY.md && git show --format= --name-only HEAD > /tmp/02-06-t3-files.txt && test "$(sort /tmp/02-06-t3-files.txt | tr '\n' ' ')" = "SECURITY.md "</automated>
    <fails_when>non-zero exit — a vulnerability id was named, the existing disjoint-scope or pnpm-audit sentences changed, or the commit touched a file other than `SECURITY.md`</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log -3 --format=%s%n%b > /tmp/02-06-msgs.txt && test -s /tmp/02-06-msgs.txt && ! rg -q -i 'ci skip|skip ci' /tmp/02-06-msgs.txt</automated>
    <fails_when>non-zero exit — a commit message in this plan carries a CI-skip marker</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --format= HEAD -- SECURITY.md` shows at most one removed line and one to four added lines, all inside the two-scanner bullet.
    - SECURITY.md names `tool-vuln`, `go.tool*.mod`, `advisory`, and `never fails the build`; names no `GO-YYYY-NNNN` id; keeps `two scanners, two disjoint trees` and the pnpm-lock sentence verbatim.
    - The commit touches only `SECURITY.md`; no CI-skip marker.
  </acceptance_criteria>
  <done>Root SECURITY.md states, in one sentence, exactly what the advisory scanner covers and that it never blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository docs → readers verifying a release | `SECURITY.md` and `docs/RELEASE.md` are the statements users rely on to know what is signed, scanned and attested; a wrong claim here is a security-relevant misstatement |
| executor → GitHub issue tracker | `gh issue close 14 --comment` is an outward-facing write, authorised by D-14 |
| planted census control → working tree | an untracked file exists inside the census scope for the duration of one script |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Repudiation | provenance claim census that looks at nothing | high | mitigate | Planted control must be matched by both patterns before the zero-hit result is trusted; the control's absence is asserted afterwards; every BROAD hit is classified in the SUMMARY (rule 84d1gfpywd). |
| T-02-06-02 | Information Disclosure | naming a specific unmitigated exposure in SECURITY.md | medium | mitigate | D-15: no vulnerability id or package name; verify greps SECURITY.md for `GO-YYYY-NNNN` ids. |
| T-02-06-03 | Tampering | dependency paragraph re-staled by counts | low | mitigate | D-13 removes every count and points at `go list -m all`; verify negative-greps stale AND current counts in § 2. |
| T-02-06-04 | Tampering | historical `Corrected 2026-08-01` callouts reworded away | medium | mitigate | Verify asserts both callouts survive (`-o | wc -l` = 2 across the two files; 1 in RELEASE.md). |
| T-02-06-05 | Tampering | planted control committed by accident | medium | mitigate | EXIT trap removes it; verify asserts `git status --porcelain docs/` empty and the path absent. |
| T-02-06-06 | Spoofing | closing GH #14 without evidence | medium | mitigate | Close comment must cite the census; verify asserts the issue is CLOSED with ≥1 comment containing "census". |
| T-02-06-07 | Denial of Service | `protect-main` ruleset | high | mitigate | Commit messages grepped for CI-skip markers. |
| T-02-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed. |
</threat_model>

<verification>
- Census: control seen by both patterns, absent after removal; narrow hits only inside historical blocks; GH #14 CLOSED with a census comment.
- RELEASE.md § 2: no counts, go-sdk credited, named requires, live-number pointers, historical callout intact; commit touches only that file.
- SECURITY.md: one appended sentence naming `tool-vuln`, `go.tool*.mod`, advisory, never fails the build; no exposure id; other sentences unchanged.
</verification>

<success_criteria>
Every documentation claim about dependencies, provenance scope and scanner coverage states what is
true today, the one claim that needed proof was proven by a census that was watched detect a planted
error, and no wording change acquired a test.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Path / name |
|---|---|---|
| reshaped § 2 "Dependency tree (DIST-05)" | doc prose (value change, no new heading) | `docs/RELEASE.md` |
| one appended sentence in the two-scanner bullet | doc prose | `SECURITY.md` |
| classified census table + control transcript | SUMMARY section | `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-06-SUMMARY.md` |
| GH #14 close comment | external record | github.com/seanb4t/codegraph-go/issues/14 |
</artifacts_produced>

<output>
Create `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-06-SUMMARY.md` when done.
</output>
