---
phase: 02-guards-ci-wiring-docs-burn-down
plan: 07
type: execute
wave: 2
depends_on: ["02-01", "02-05", "02-06"]
files_modified:
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/seeds/SEED-001-local-svelte-shadcn-graph-browsing-ui.md
  - .planning/todos/pending/2026-08-14-bench-pinnedat-validates-a-checkout-by-git-rev-parse-head-alone.md
autonomous: true
requirements: [GRD-14, DOCS-11, GRD-09, GRD-10, DOCS-08]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WINDOWS #16 and #33 read `fixed` (with `resolved_at` set) after `gsd-tools windows fixed 16` / `fixed 33`, each preceded by read-only verification recorded in the SUMMARY: #16 — the TS-comparison framing is absent from `internal/bench/rss.go` and `tools/bench/runner/main.go` (grep returns nothing) and v0.11.0 Phase 6 BENCH-02 is the resolving change; #33 — PR #71 is merged and a real `ci.yml` run on `main` (e.g. run 34859136748, job `tmux e2e (real-pty harness, TTY-01..TTY-07)`) shows the tmux version assertion and the executed-count line firing. GRD-14 (Claude's Discretion: the verb takes no note, evidence lives in the SUMMARY)."
    - "WINDOWS #29, #31 and #13 read `fixed` on the strength of this phase's own evidence — 02-01's Family (a) transcript (D-02), 02-05's `web:components:drift` PASS (D-10), 02-06's count-free RELEASE.md § 2 (D-13) — closed here because this plan is the phase's single writer of `.planning/WINDOWS.md`; after the five closures `windows status` reports `open_count` 6 with ids 20, 21, 22, 32, 34, 35 still open."
    - "#20, #21 and #34 stay `open` — not fixed, not waived — and their record-only status (deviations by design / a mutation-log finding, not defects awaiting work) is stated in the SUMMARY and as one bullet in STATE.md's Blockers/Concerns added through `gsd-tools state add-blocker`, because the ledger has no annotate verb and no record-only status (planning-artifacts rule: never invent a shape in a tool-owned file)."
    - "The tooling gaps are reported in the repo's established shape — a `Tooling gaps` Blockers bullet added via `state add-blocker` naming: no `windows` annotate/record-only verb or status; `windows fixed` accepts no note; no CLI verb creates a pending-todo file; STATE.md's Pending Todos renderer emits bullets so a hand-authored table is not a tool shape — and the SUMMARY carries a ready-to-file upstream issue body for open-gsd/gsd-core. The executor does NOT open the external issue itself (repo precedent: STATE.md's existing `Tooling gaps` bullet; the maintainer files it if wanted)."
    - "RESEARCH open question 2 is decided: the still-real `tools/bench/runner/main.go` `pinnedAt()` HEAD-only validation is filed as a pending todo in the add-todo workflow's own file shape (frontmatter `created`/`title`/`area`/`severity`/`files`, body `## Problem`/`## Solution`), NOT folded into Blockers prose — because a real unfixed todo belongs in the todo system, the file template is the tool's sanctioned shape, and it makes the rendered Pending Todos section truthfully non-empty. DOCS-11."
    - "STATE.md's `### Pending Todos` body is replaced wholesale with the literal `pending_todos_markdown` value from `gsd-tools init todos` (the `check-todos.md` / `add-todo.md` update_state contract): one rendered bullet for the filed todo, no hand-authored intro paragraph, no `| Created | Area | Severity | Title |` table, no `Resolved and filed` table — the removed rows' closure notes are carried into the SUMMARY and remain discoverable in `.planning/todos/completed/`. `init todos` reports `todo_count` 1 and `pending_dir_exists` true. DOCS-11."
    - "SEED-001's frontmatter records its consumption in SEED-002's exact field set: `status: implemented`, `consumed_by: v0.12.0 — Local Graph UI (Phases 1–6)`, `consumed_on: 2026-09-07`; every other existing field (including the tool-written `audit_acknowledged` block) is untouched; `gsd-tools list-seeds implemented` lists SEED-001. DOCS-11 (SEED-002 precedent)."
  artifacts:
    - ".planning/WINDOWS.md — rows 13, 16, 29, 31, 33 `fixed` via the tool verb"
    - ".planning/STATE.md — Pending Todos body regenerated from `init todos`; two Blockers bullets added via `state add-blocker`"
    - .planning/todos/pending/2026-08-14-bench-pinnedat-validates-a-checkout-by-git-rev-parse-head-alone.md
    - ".planning/seeds/SEED-001-local-svelte-shadcn-graph-browsing-ui.md — `status`, `consumed_by`, `consumed_on` values"
  key_links:
    - "02-01 Family (a) → `windows fixed 29`; 02-05 PASS transcript → `windows fixed 31`; 02-06 § 2 rewrite → `windows fixed 13`; grep + Phase 6 SUMMARY → `windows fixed 16`; PR #71 + main run log → `windows fixed 33`"
    - "new pending-todo file → `gsd-tools init todos` → `pending_todos_markdown` → STATE.md `### Pending Todos` body (the only sanctioned writer)"
  prohibitions:
    - statement: "`.planning/WINDOWS.md` is never opened in an editor: every change goes through `gsd-tools windows fixed <id>`; no `windows waive` is used on #20/#21/#34 and no `fixed` is applied to them (Claude's Discretion in CONTEXT)."
      status: resolved
      verification: judgment
    - statement: "No heading, status value, table or row shape is invented in STATE.md, WINDOWS.md or any seed file — Pending Todos is regenerated from the tool's renderer, Blockers are added by verb, seed fields are copied from SEED-002 (planning-artifacts rule)."
      status: resolved
      verification: judgment
    - statement: "No external issue is opened by the executor — the upstream report is drafted in the SUMMARY and recorded as a Tooling-gaps bullet; filing on open-gsd/gsd-core is the maintainer's action."
      status: resolved
      verification: judgment
    - statement: "No code change to `tools/bench/runner/main.go` — filing the todo is bookkeeping; the fix is not in this phase's scope."
      status: resolved
      verification: judgment
    - statement: "No `[ci skip]` / `[skip ci]` in any commit message."
      status: resolved
      verification: judgment

flagged_assumptions:
  - id: EDGE-GRD-14-01
    requirement: GRD-14
    probe: "unclassified — review manually"
    status: unresolved
    note: "Deterministic edge probe returned `unclassified` for GRD-14; not auto-resolved. Planner's reading: #20/#21/#34 staying `open` keeps `open_count` above zero, and `workflow.windows_enforce` (if enabled) blocks `/gsd-ship` while `open_count > 0` — the record-only choice may collide with the ship gate at milestone end. Surfaced for the maintainer; no acceptance criterion resolves it here."
  - id: EDGE-DOCS-11-01
    requirement: DOCS-11
    probe: "unclassified — review manually"
    status: unresolved
    note: "Deterministic edge probe returned `unclassified` for DOCS-11; not auto-resolved. Planner's reading: the executor's own post-plan STATE.md writes (record-metric, add-decision) run concurrently with wave-2 sibling 02-02's; the tool verbs are section-scoped and sequential per invocation, but no acceptance criterion asserts that two same-wave plans cannot interleave STATE.md writes. Surfaced, not dropped."
---

<objective>
Bring every planning claim about window status, the todo table and seed status back to what is
true — through the tool's own verbs wherever one exists, and through the tool's own file shapes
where a verb does not — and report the tool gaps instead of papering over them.

**What are we testing, and why?** Nothing mechanical: this is bookkeeping, and the phase-wide test
says a bookkeeping change gets no guard. What we are enforcing is provenance: every `fixed` row
points at verification evidence recorded in the SUMMARY (the verb cannot carry a note), and every
edit to a tool-owned file is either a tool verb or a value in a shape the tool already writes.
STATE.md's Pending Todos table was a hand-authored shape no tool renders (RESEARCH Pitfall 4) and
`.planning/todos/pending/` does not exist at all — the section was wrong in both directions.

This plan is the phase's ONLY writer of `.planning/WINDOWS.md`, so it also closes #29, #31 and #13
on behalf of 02-01, 02-05 and 02-06 (their evidence, this plan's verb calls). It runs in wave 2
after those three plans and does not depend on 02-04 (the Phase 08-03 blocker is 02-04's own).

Output: five `fixed` rows, two Blockers bullets, a regenerated Pending Todos section, one filed todo,
SEED-001's consumption fields.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-CONTEXT.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-01-SUMMARY.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-05-SUMMARY.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-06-SUMMARY.md
@/Users/sean/.claude/rules/planning-artifacts.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Verify, then close, WINDOWS #16, #33, #29, #31 and #13 through the tool verb</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - `gsd-tools windows status` output (never the file directly) — rows 13, 16, 29, 31, 33 (`open`) and 20, 21, 34 (stay `open`); note `open_count` 11 before.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md § "Pitfall 5" — `windows fixed <id>` takes exactly one positional and no note; do not pass a second argument.
    - internal/bench/rss.go and tools/bench/runner/main.go — grep targets for #16's framing; `.planning/milestones/v0.11.0-phases/06-*/` SUMMARYs for the BENCH-02 change that removed it.
    - .planning/STATE.md line ~280 (`[Phase 08]: verify-work 2026-09-11 …` — the TTY-07 backstop fired at CI run 34658987243: executed=6 skipped=0 expected=6, tmux 3.4) and `.planning/milestones/v0.13.0-phases/08-*/08-03-SUMMARY.md` — what #33's human-check asked for and what was already recorded.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-01-SUMMARY.md, 02-05-SUMMARY.md, 02-06-SUMMARY.md — the evidence for #29, #31, #13.
  </read_first>
  <action>
Gather evidence read-only, one row at a time, and write an evidence table into the SUMMARY (id |
claim in the row | verification command | observed result | resolving change):
  - #16: `rg -n -i 'TS Node process|TS binary|compared fairly' internal/bench/rss.go tools/bench/runner/main.go`
    returns nothing; name the v0.11.0 Phase 6 BENCH-02 SUMMARY that removed the comparison.
  - #33: `gh pr view 71 --json state,mergedAt` → MERGED; `gh run view 34859136748 --json jobs` → the
    `tmux e2e (real-pty harness, TTY-01..TTY-07)` job succeeded; `gh run view --job <that job id>
    --log` → quote the tmux version line (`tmux -V` output) and the executed-count line. State
    whether the observed `tmux -V` string was already recorded (08-VERIFICATION / STATE.md line
    ~280 say `tmux 3.4`); if it is recorded nowhere committed, record it in this SUMMARY — do not
    edit test code.
  - #29: `02-MUTATION-LOG.md` Family (a) exists with both digests (02-01).
  - #31: 02-05-SUMMARY's `web:components:drift` PASS line (50 files / 8 components, pnpm v11.23.0).
  - #13: `rg -n 'mark3labs|27 direct|134 total|remaining 13' docs/RELEASE.md` returns nothing (02-06).
Then, in this order, run exactly `gsd-tools windows fixed 16`, `… fixed 33`, `… fixed 29`,
`… fixed 31`, `… fixed 13` (one id per call, nothing else on the line). After each, confirm the
JSON reports the row as fixed. Finally `gsd-tools windows status` → `open_count` 6, ids
20/21/22/32/34/35 open, the five ids `fixed` with `resolved_at` populated. Do not touch #20/#21/#34.

Commit: `docs(02-07): close WINDOWS #13/#16/#29/#31/#33 with recorded verification (GRD-14)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && ! rg -q -i 'TS Node process|TS binary|compared fairly' internal/bench/rss.go tools/bench/runner/main.go && ! rg -q 'mark3labs|27 direct|134 total|remaining 13' docs/RELEASE.md && rg -q '^## Family \(a\) — GRD-09' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md && rg -q 'byte-identical' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-05-SUMMARY.md && gh pr view 71 --json state --jq .state | rg -q '^MERGED$'</automated>
    <fails_when>non-zero exit — one of the five rows' evidence is absent: the TS framing or a stale RELEASE.md count still exists, Family (a) is missing, 02-05's PASS is not recorded, or PR #71 is not merged</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status > /tmp/02-07-windows.json 2>/dev/null && jq -e '.ledger.open_count == 6 and ([.ledger.entries[] | select(.id==13 or .id==16 or .id==29 or .id==31 or .id==33) | select(.status=="fixed" and (.resolved_at|length)>0)] | length == 5) and ([.ledger.entries[] | select(.id==20 or .id==21 or .id==34) | select(.status=="open")] | length == 3)' /tmp/02-07-windows.json >/dev/null && echo "ledger ok"</automated>
    <fails_when>non-zero exit or no `ledger ok` — a target row is not `fixed` with a `resolved_at`, one of #20/#21/#34 changed status, or `open_count` is not 6</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git show --format= --name-only HEAD > /tmp/02-07-t1-files.txt && test "$(sort /tmp/02-07-t1-files.txt | tr '\n' ' ')" = ".planning/WINDOWS.md " && git log -1 --format=%s%n%b > /tmp/02-07-msg1.txt && test -s /tmp/02-07-msg1.txt && ! rg -q -i 'ci skip|skip ci' /tmp/02-07-msg1.txt</automated>
    <fails_when>non-zero exit — the commit touched anything other than `.planning/WINDOWS.md` (a hand edit elsewhere), or the message carries a CI-skip marker</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY has a five-row evidence table with a verification command and observed result per closed window.
    - `windows status`: #13, #16, #29, #31, #33 `fixed` with `resolved_at`; #20, #21, #34 `open`; `open_count` 6.
    - Only `.planning/WINDOWS.md` is in the commit; no CI-skip marker.
  </acceptance_criteria>
  <done>Every window this phase resolves reads fixed through the tool, with its evidence on the record, and the three record-only rows are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Record-only rows and tooling gaps stated through `state add-blocker`; upstream report drafted</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md `### Blockers/Concerns` — the existing `Tooling gaps (not blocking work, and not hand-edited per the planning-artifacts rule):` bullet (the precedent shape and voice) and the `[Phase 08-03]` bullet (02-04 removes that one; do not touch it here).
    - `gsd-tools windows status` — rows 20, 21, 34's descriptions, so the bullet summarises them accurately.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md § "Pitfall 4" and § "Pitfall 5" — the four verified gaps to report, with the source locations RESEARCH cites (`broken-windows.cjs:286-293,1068-1088`; `init.cjs:2024`).
    - /Users/sean/.claude/rules/planning-artifacts.md § "When the tool genuinely cannot express it" — report, record the gap, never a local workaround.
  </read_first>
  <action>
Add two bullets with the tool verb (each `gsd-tools state add-blocker --text "…"`, confirming the
JSON reports success and that `git diff -- .planning/STATE.md` shows only added lines):
  1. A record-only bullet: `[Phase 2] WINDOWS #20, #21, #34 are open by decision, not pending
     work:` #20 and #21 are Phase-2 (v0.12.0) deviations by design (no `svelte.config.js` in this
     SvelteKit toolchain; TypeScript pinned at 6.0.3 by the scaffold), #34 is a TTY-06 mutation-log
     finding whose assertion measures post-settle stability by design; the ledger has no
     annotate/record-only verb or status, so they stay `open` rather than being waived (a waiver
     reads as a deferred defect) or fixed (nothing was fixed). Name plan 02-07 and GRD-14.
  2. A tooling-gaps bullet in the existing `Tooling gaps` voice, reported upstream by note per repo
     precedent: (a) `gsd-tools windows` has no annotate/record-only verb or status; (b) `windows
     fixed <id>` accepts no note (`broken-windows.cjs` `markFixed`/`cmdWindowsMarkFixed`: one
     positional, zero flags) so verification evidence can only live in SUMMARYs; (c) no CLI verb
     creates a pending-todo file (the add-todo workflow is agent-authored); (d) STATE.md's Pending
     Todos renderer (`init.cjs` `renderPendingTodosMarkdown`) emits bullets, so the previous
     hand-authored table was not a tool shape and was replaced by the rendered body in 02-07.
     End with: filing on open-gsd/gsd-core is the maintainer's call; the issue body is in 02-07-SUMMARY.
In the SUMMARY, draft the complete upstream issue body (title, the four gaps with source citations,
the ask: an `annotate`/`record-only` status or a `--note` on `fixed`, a `todo add` verb, and a
STATE.md todo renderer that matches what workflows write). Do NOT run `gh issue create`.

Commit: `docs(02-07): record the record-only windows and the gsd-tools gaps in STATE.md blockers`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && awk '/^### Blockers\/Concerns/{f=1} /^### Quick Tasks Completed/{f=0} f' .planning/STATE.md > /tmp/02-07-blockers.txt && rg -q '#20, #21, #34' /tmp/02-07-blockers.txt && rg -q -i 'record-only|open by decision' /tmp/02-07-blockers.txt && rg -q -i 'no annotate' /tmp/02-07-blockers.txt && rg -q -i 'accepts no note|no note' /tmp/02-07-blockers.txt && rg -q -i 'pending-todo file|pending todo file' /tmp/02-07-blockers.txt && rg -q -i 'renderPendingTodosMarkdown|emits bullets' /tmp/02-07-blockers.txt && rg -q -F 'Tooling gaps (not blocking work' /tmp/02-07-blockers.txt</automated>
    <fails_when>non-zero exit — the Blockers section lacks the record-only bullet naming #20, #21, #34, or the tooling-gaps bullet does not name all four gaps, or the pre-existing `Tooling gaps` bullet was removed</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git show --format= HEAD -- .planning/STATE.md > /tmp/02-07-state.diff && test "$(rg -c '^-[^-]' /tmp/02-07-state.diff || echo 0)" = "0" && test "$(rg -c '^\+- ' /tmp/02-07-state.diff)" = "2" && git show --format= --name-only HEAD > /tmp/02-07-t2-files.txt && test "$(sort /tmp/02-07-t2-files.txt | tr '\n' ' ')" = ".planning/STATE.md "</automated>
    <fails_when>non-zero exit — the STATE.md diff removed a line or added a number of bullets other than exactly two (a hand edit rather than two verb calls), or the commit touched another file</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && gh issue list --repo open-gsd/gsd-core --author @me --state open --json number,title --jq 'map(select(.title | test("annotate|record-only|windows fixed|todo add"; "i"))) | length' | rg -q '^0$'</automated>
    <fails_when>non-zero exit — an open issue matching the tooling gaps exists under the current gh account on open-gsd/gsd-core (the executor filed it, which is the maintainer's action), or the query failed</fails_when>
  </verify>
  <acceptance_criteria>
    - Two new `- ` bullets in Blockers/Concerns, added by `state add-blocker`, zero removed lines: one names #20, #21, #34 as open by decision; one is the tooling-gaps report naming the four gaps with the `broken-windows.cjs` / `init.cjs` citations.
    - The SUMMARY contains the drafted upstream issue body; no issue was created on open-gsd/gsd-core by the executor.
    - The commit touches only `.planning/STATE.md`.
  </acceptance_criteria>
  <done>The record-only rows and the tool gaps are stated in the repo's established shape, and the upstream report is ready for the maintainer to file.</done>
</task>

<task type="auto">
  <name>Task 3: File the bench pinnedAt todo, regenerate Pending Todos from the tool, record SEED-001's consumption</name>
  <files>.planning/todos/pending/2026-08-14-bench-pinnedat-validates-a-checkout-by-git-rev-parse-head-alone.md, .planning/STATE.md, .planning/seeds/SEED-001-local-svelte-shadcn-graph-browsing-ui.md</files>
  <read_first>
    - ~/.claude/gsd-core/workflows/add-todo.md step `create_file` (the pending-todo file template: frontmatter `created`, `title`, `area`, `severity`, `files`; body `## Problem`, `## Solution`) and step `update_state` (replace the ENTIRE `### Pending Todos` body with `pending_todos_markdown` verbatim; fail-safe if `pending_read_ok` is not true).
    - .planning/todos/completed/2026-09-08-graphstore-archtest-ignores-per-package-load-errors.md — a real file in that shape (frontmatter keys and body headings) to mirror.
    - tools/bench/runner/main.go lines 430-441 — `pinnedAt()` runs `git -C dir rev-parse HEAD` alone (verified at plan time); the todo's `files:` entry points here.
    - .planning/STATE.md lines 319-343 — the current `### Pending Todos` body (intro paragraph, the 4-row table, the `Resolved and filed` table) that is replaced wholesale; copy its rows into the SUMMARY first.
    - .planning/seeds/SEED-002-homebrew-installation-path.md frontmatter — the exact field names `status`, `consumed_by`, `consumed_on`; .planning/seeds/SEED-001-local-svelte-shadcn-graph-browsing-ui.md frontmatter — the current fields to keep (`id`, `planted`, `planted_during`, `trigger_when`, `scope`, `audit_acknowledged`).
    - .planning/ROADMAP.md line ~23 (`✅ v0.12.0 — Local Graph UI — Phases 1–6 (shipped 2026-09-07) … Consumed SEED-001`) — the consumption values.
  </read_first>
  <action>
1. Decide open question 2 on the record (SUMMARY): the `pinnedAt` HEAD-only check is real and
   unfixed, so it is filed as a pending todo in the tool's own file shape rather than folded into
   Blockers prose. `mkdir -p .planning/todos/pending` and write
   `.planning/todos/pending/2026-08-14-bench-pinnedat-validates-a-checkout-by-git-rev-parse-head-alone.md`
   with frontmatter `created: 2026-08-14T00:00:00.000Z` (the date the STATE.md row records — preserve
   provenance), `title:` `tools/bench/runner pinnedAt() validates a checkout by git rev-parse HEAD
   alone — the HEAD-only anti-pattern Phase 1's four-part integrity check replaces`, `area: bench`,
   `severity: minor`, `files:` `- tools/bench/runner/main.go:435-441`; body `## Problem` (what
   `pinnedAt` checks, why HEAD alone can pass on a dirty or detached-but-moved checkout, where the
   four-part integrity check lives) and `## Solution` (mirror the four-part check; TBD on timing).
2. Run `gsd-tools init todos`; assert `pending_read_ok` true, `todo_count` 1, and take
   `pending_todos_markdown` verbatim. Copy the current Pending Todos tables' rows into the SUMMARY
   (the four "pending" rows with their real disposition per RESEARCH Pitfall 4 — three already
   resolved, one now filed — and the nine "Resolved and filed" rows with their closure notes). Then
   replace the ENTIRE body of `### Pending Todos` (everything between that heading and
   `### Blockers/Concerns`) with the literal `pending_todos_markdown` value — no intro paragraph, no
   table, no reformatting of the rendered bullet. If `init todos` does not report `pending_read_ok`
   true, do not touch the section and report it.
3. SEED-001: with scoped `Edit`, change `status: dormant` to `status: implemented` and add
   `consumed_by: v0.12.0 — Local Graph UI (Phases 1–6)` and `consumed_on: 2026-09-07` directly
   after `planted_during:` (SEED-002's field order); keep every other field including the
   `audit_acknowledged` block byte-identical. Confirm `gsd-tools list-seeds implemented` lists
   SEED-001 alongside SEED-002.

Commit: `docs(02-07): file the bench pinnedAt todo, regenerate STATE.md Pending Todos, record SEED-001 consumption (DOCS-11)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && f=.planning/todos/pending/2026-08-14-bench-pinnedat-validates-a-checkout-by-git-rev-parse-head-alone.md; test -f "$f" && head -1 "$f" | rg -q '^---$' && rg -q '^created: 2026-08-14' "$f" && rg -q '^title: ' "$f" && rg -q '^area: bench' "$f" && rg -q '^severity: ' "$f" && rg -q 'tools/bench/runner/main.go' "$f" && rg -q '^## Problem' "$f" && rg -q '^## Solution' "$f" && git ls-files --error-unmatch "$f" >/dev/null</automated>
    <fails_when>non-zero exit — the todo file is missing, not in the add-todo template shape (frontmatter keys / Problem / Solution), does not point at the bench runner, or is not tracked</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" init todos 2>/dev/null > /tmp/02-07-todos.json && jq -e '.pending_read_ok == true and .todo_count == 1 and .pending_dir_exists == true' /tmp/02-07-todos.json >/dev/null && awk '/^### Pending Todos/{f=1; next} /^### Blockers\/Concerns/{f=0} f' .planning/STATE.md | sed '/^[[:space:]]*$/d' > /tmp/02-07-section.txt && jq -r '.pending_todos_markdown' /tmp/02-07-todos.json | sed '/^[[:space:]]*$/d' > /tmp/02-07-render.txt && diff /tmp/02-07-section.txt /tmp/02-07-render.txt && ! rg -q '^\| Created \| Area' /tmp/02-07-section.txt</automated>
    <fails_when>non-zero exit or a diff — `init todos` does not see exactly one pending todo, or STATE.md's Pending Todos body is not byte-equal (ignoring blank lines) to the tool's `pending_todos_markdown`, or the hand-authored table survives</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && s=.planning/seeds/SEED-001-local-svelte-shadcn-graph-browsing-ui.md; rg -q '^status: implemented$' "$s" && rg -q '^consumed_by: v0\.12\.0' "$s" && rg -q '^consumed_on: 2026-09-07$' "$s" && rg -q '^audit_acknowledged:' "$s" && rg -q '^planted: 2026-07-14$' "$s" && ! rg -q '^status: dormant$' "$s" && node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" list-seeds implemented 2>/dev/null | rg -q 'SEED-001'</automated>
    <fails_when>non-zero exit — SEED-001 lacks `status: implemented`, `consumed_by`, or `consumed_on`, lost its `audit_acknowledged` or `planted` fields, or `list-seeds implemented` does not list it</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git show --format= --name-only HEAD > /tmp/02-07-t3.txt && rg -q '^\.planning/STATE\.md$' /tmp/02-07-t3.txt && rg -q '^\.planning/seeds/SEED-001' /tmp/02-07-t3.txt && rg -q '^\.planning/todos/pending/2026-08-14-bench' /tmp/02-07-t3.txt && test "$(wc -l < /tmp/02-07-t3.txt | tr -d ' ')" = "3" && ! rg -q '^tools/bench/runner/main\.go$' /tmp/02-07-t3.txt && git log -3 --format=%s%n%b > /tmp/02-07-msgs.txt && test -s /tmp/02-07-msgs.txt && ! rg -q -i 'ci skip|skip ci' /tmp/02-07-msgs.txt</automated>
    <fails_when>non-zero exit — the commit does not contain exactly STATE.md, the seed file and the todo file, or `tools/bench/runner/main.go` changed (out of scope), or a commit message carries a CI-skip marker</fails_when>
  </verify>
  <acceptance_criteria>
    - The pending-todo file exists in the add-todo template shape, `created: 2026-08-14…`, `area: bench`, pointing at `tools/bench/runner/main.go`, and is tracked.
    - `gsd-tools init todos` reports `pending_read_ok` true, `pending_dir_exists` true, `todo_count` 1; STATE.md's `### Pending Todos` body equals `pending_todos_markdown` (blank lines aside) and contains no table.
    - The SUMMARY carries the removed table rows with their dispositions and the open-question-2 decision with its reason.
    - SEED-001 has `status: implemented`, `consumed_by: v0.12.0 — Local Graph UI (Phases 1–6)`, `consumed_on: 2026-09-07`, and every pre-existing field intact; `list-seeds implemented` lists it.
    - The commit contains exactly the three files; no code change; no CI-skip marker.
  </acceptance_criteria>
  <done>The todo section is rendered by the tool from a real pending file, the one genuinely open todo is filed in the sanctioned shape, and SEED-001 records its consumption the way SEED-002 does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → tool-owned planning files | `WINDOWS.md`, `STATE.md` sections and seed frontmatter are parsed by scope-sensitive readers; an invented shape silently changes what the tool reports |
| executor → external issue tracker (open-gsd/gsd-core) | an outward-facing publish action; deliberately NOT taken by the executor |
| evidence in SUMMARYs → ledger status | a `fixed` row with no evidence is a false claim of verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Tampering | `.planning/WINDOWS.md` | high | mitigate | Only `gsd-tools windows fixed <id>` writes it; verify asserts the commit touches only that file and the JSON status reflects exactly five fixed / three untouched rows. |
| T-02-07-02 | Repudiation | `fixed` without evidence | high | mitigate | Task 1 gathers read-only evidence per row (grep, `gh pr view`, `gh run view`, prior SUMMARYs) into a SUMMARY table before any verb call; verify re-checks the greps and PR state. |
| T-02-07-03 | Tampering | STATE.md structure (invented table/heading) | high | mitigate | Pending Todos body replaced with the tool's `pending_todos_markdown` verbatim (verify diffs the section against the render); Blockers added only via `state add-blocker` (verify asserts zero removed lines, exactly two added bullets). |
| T-02-07-04 | Spoofing | executor publishing an upstream issue as the maintainer | medium | mitigate | Explicitly prohibited; the issue body is drafted in the SUMMARY; verify queries open-gsd/gsd-core for a matching issue by the current author and requires an explanation if one appears. |
| T-02-07-05 | Tampering | seed frontmatter shape drift | low | mitigate | Field names copied from SEED-002; verify asserts the three values and that `audit_acknowledged` / `planted` survive; `list-seeds implemented` must parse it. |
| T-02-07-06 | Denial of Service | `protect-main` ruleset | high | mitigate | Commit messages grepped for CI-skip markers. |
| T-02-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed. |
</threat_model>

<verification>
- `windows status`: 13/16/29/31/33 fixed with `resolved_at`; 20/21/34 open; `open_count` 6; evidence table in SUMMARY.
- Blockers: two verb-added bullets (record-only rows; tooling gaps), zero removed lines; upstream issue drafted, not filed.
- Pending Todos body == `pending_todos_markdown` from `init todos` (1 todo); the bench todo file exists in template shape; SEED-001 carries the SEED-002 consumption fields.
</verification>

<success_criteria>
Window status, the todo section and seed status all state what is true today, every write went
through a tool verb or a tool-written shape, and every gap in the tool's vocabulary is recorded and
drafted for upstream rather than worked around.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Path / name |
|---|---|---|
| five `fixed` ledger rows (#13, #16, #29, #31, #33) | tool-verb status changes | `.planning/WINDOWS.md` |
| record-only bullet + tooling-gaps bullet | `state add-blocker` additions | `.planning/STATE.md` `### Blockers/Concerns` |
| regenerated Pending Todos body | tool-rendered section | `.planning/STATE.md` `### Pending Todos` |
| bench `pinnedAt` pending todo | new file in the add-todo template shape | `.planning/todos/pending/2026-08-14-bench-pinnedat-validates-a-checkout-by-git-rev-parse-head-alone.md` |
| `status` / `consumed_by` / `consumed_on` values | seed frontmatter values (SEED-002 field set) | `.planning/seeds/SEED-001-local-svelte-shadcn-graph-browsing-ui.md` |
| upstream issue draft (open-gsd/gsd-core) | SUMMARY section | `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-07-SUMMARY.md` |
</artifacts_produced>

<output>
Create `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-07-SUMMARY.md` when done.
</output>
