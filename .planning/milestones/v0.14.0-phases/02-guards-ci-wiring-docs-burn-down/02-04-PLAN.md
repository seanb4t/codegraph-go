---
phase: 02-guards-ci-wiring-docs-burn-down
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - .github/required-status-checks.txt
  - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md
  - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-ruleset-put-body.json
  - .planning/STATE.md
autonomous: false
requirements: [GRD-12]
user_setup:
  - service: github
    why: "D-08: the maintainer adds two required-status-check contexts to ruleset 20157557 (protect-main) — a repository-settings action no agent may perform silently; the drift step turns GREEN only after it lands"
    dashboard_config:
      - task: "Add `goreleaser check (config validation, DIST-01)` and `tmux e2e (real-pty harness, TTY-01..TTY-07)` (integration_id 15368 = GitHub Actions) to the ruleset's required_status_checks rule, bringing the live set to 8"
        location: "GitHub → seanb4t/codegraph-go → Settings → Rules → Rulesets → protect-main; OR review and run the executor-prepared `gh api --method PUT repos/seanb4t/codegraph-go/rulesets/20157557 --input .planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-ruleset-put-body.json`"

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Before the flip, `bash scripts/check-ruleset-drift.sh` is re-run and still RED (`live has 6 contexts, fixture has 7 contexts`, diff naming `goreleaser check (config validation, DIST-01)`), and that transcript opens `02-MUTATION-LOG.md` Family (c) — the guard is watched fail against the real divergence immediately before the change that resolves it. D-05 / D-06."
    - "The maintainer receives a one-glance review package: the two exact context strings, the exact `gh api --method PUT … --input` command, and a committed body file `02-04-ruleset-put-body.json` derived from the live ruleset (name/target/enforcement/bypass_actors/conditions/rules carried through, ONLY the two `{context, integration_id: 15368}` entries appended) — the executor prepares, the maintainer applies (UI or that command), never the executor unprompted. D-08."
    - "Task 2 does not start until a read-only `curl` of ruleset 20157557 shows `enforcement == \"active\"` and exactly 8 required contexts including both D-08 strings; until then the executor halts `blocking-human` with the package. D-08 (costly to reverse: a later removal is a settings change plus a fixture edit plus this step RED until both agree)."
    - "After the flip, `.github/required-status-checks.txt` has exactly 8 non-blank lines (the 7 seeds plus `tmux e2e (real-pty harness, TTY-01..TTY-07)`), the script prints `live has 8 contexts, fixture has 8 contexts` and `ruleset-drift: PASS`, and `TestRequiredCheckNamesPreserved` logs `read 8` and passes (the tmux job's `name:` exists in `ci.yml`). D-07 / D-08."
    - "STATE.md's `[Phase 08-03] user_setup NOT completed` blocker is removed through `gsd-tools state resolve-blocker` (the tool's own verb, never a hand edit) once the live set is verified, and the plan SUMMARY records the live 8-context set as observed. D-08."
    - "Family (c) in `02-MUTATION-LOG.md` carries both halves — the pre-flip RED and the post-flip GREEN — with exit codes, so the step's discriminating power is on the record, not asserted."
  artifacts:
    - ".github/required-status-checks.txt — 8 entries"
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-ruleset-put-body.json
    - ".planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md — `## Family (c) — GRD-12` entry"
  key_links:
    - "maintainer's ruleset change (D-08) → live `required_status_checks[].context` = 8 → `scripts/check-ruleset-drift.sh` GREEN only if `.github/required-status-checks.txt` also = 8 → `ci.yml` `test` check GREEN; and the same 8 strings must each be a job `name:` (`TestRequiredCheckNamesPreserved`) — `tmux e2e (real-pty harness, TTY-01..TTY-07)` is `ci.yml` job `tmux-e2e`'s `name:`"
  prohibitions:
    - statement: "The executor never applies the ruleset change on its own initiative: the PUT is run only by the maintainer, or by the executor only after the maintainer explicitly says so in reply to the checkpoint (D-08; CONTEXT: 'the maintainer's call at the checkpoint')."
      status: resolved
      verification: judgment
    - statement: "The fixture is not edited to 8 before the live set reads 8 — no 'edit the file first and wait for CI to catch up' shortcut; the precondition is checked read-only before Task 2 starts (D-08)."
      status: resolved
      verification: judgment
    - statement: "No skip, waiver, or soft-fail is added to make the step green while D-08 is pending — the step stays RED on the PR until the live set matches, and that is recorded (D-06)."
      status: resolved
      verification: judgment
    - statement: "STATE.md and WINDOWS.md are touched only through `gsd-tools` verbs (`state resolve-blocker`); no hand edit of a tool-owned section (planning-artifacts rule)."
      status: resolved
      verification: judgment
    - statement: "No `[ci skip]` / `[skip ci]` in any commit message."
      status: resolved
      verification: judgment
---

<objective>
Turn the ruleset-drift step from RED to GREEN the honest way: watch it fail once more against the real
divergence, hand the maintainer an exact, reviewable ruleset update that adds `goreleaser check` and
`tmux e2e` to `protect-main`, and only after the live set reads 8 grow the shared fixture to 8 and
record the GREEN.

**What are we testing, and why?** That the fixture and the live ruleset agree in both directions
(D-05) — and that the agreement was reached by changing the *world* (the maintainer's ruleset) to
match the *claim*, not by editing the claim to match whatever the world happened to be. The
executor's job is to make D-08 a one-glance review (exact strings, exact API body) and to verify the
live set afterwards, not to apply it silently.

Purpose: D-08 closes the Phase 08-03 `user_setup` item that has sat in STATE.md's Blockers since
v0.13.0 (the tmux context) and the fixture-only `goreleaser check` context RESEARCH found. Once
`tmux e2e` is required, every PR runs the tmux job — the maintainer's informed choice.

Output: `02-04-ruleset-put-body.json`, the 8-entry fixture, Family (c), and the resolved blocker.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-CONTEXT.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-03-SUMMARY.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Re-capture the RED, then build the maintainer's one-glance ruleset update package</name>
  <files>.planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-ruleset-put-body.json, .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md</files>
  <read_first>
    - scripts/check-ruleset-drift.sh (written by 02-03) — its env contract and printed lines, so the RED transcript is captured from the real script.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md § "Pattern 2" and § "Code Examples → GRD-12" — the live 6-context set and the fact that `bypass_actors` is withheld from unauthenticated reads (so the body MUST be built from an authenticated `gh api` read, or the PUT would silently drop the bypass list).
    - .planning/STATE.md Blockers line beginning `- [Phase 08-03] user_setup NOT completed:` — the standing item this plan resolves; note its exact leading text for `state resolve-blocker`.
    - .github/workflows/ci.yml lines 482-484 — `tmux-e2e:` job's `name: tmux e2e (real-pty harness, TTY-01..TTY-07)`, the exact string (verify it byte-for-byte; a mismatch here would make the Go test RED after the flip).
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md — Families (a) and (b) shapes; Family (c) is appended after (b).
  </read_first>
  <action>
1. Re-run `bash scripts/check-ruleset-drift.sh` (unauthenticated is fine) and capture the transcript
   and exit code. Expect the same RED as 02-03 (`live has 6 contexts, fixture has 7 contexts`, diff
   naming `goreleaser check (config validation, DIST-01)` as fixture-only, exit 1). If the live set
   has already changed (the maintainer moved early), record what is actually printed and continue —
   the package below is still built from the live state.

2. Build the review package from an AUTHENTICATED read so `bypass_actors` is preserved:
   `gh api repos/seanb4t/codegraph-go/rulesets/20157557` → pipe through `jq` selecting only the
   PUT-body fields `name`, `target`, `enforcement`, `bypass_actors`, `conditions`, `rules`, and
   inside `rules` map the one rule whose `type == "required_status_checks"` so that
   `.parameters.required_status_checks` gains two appended objects
   `{"context": "goreleaser check (config validation, DIST-01)", "integration_id": 15368}` and
   `{"context": "tmux e2e (real-pty harness, TTY-01..TTY-07)", "integration_id": 15368}` (15368 is
   the GitHub Actions integration id every existing entry carries — read it from the live entries
   rather than hard-coding if they all agree; fail loudly if they do not). Write the result,
   pretty-printed, to `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-ruleset-put-body.json`.
   Assert with `jq -e` that the body has exactly 8 contexts in that rule, that the other rule types
   (`deletion`, `non_fast_forward`, `pull_request`, `required_linear_history`) are all still present,
   and that `enforcement` is `active`.

3. Append `## Family (c) — GRD-12: ruleset-drift step RED against the live 6-vs-7 divergence` to
   `02-MUTATION-LOG.md`: Test/guard (`ci.yml` step `Ruleset drift check (GRD-12)` →
   `scripts/check-ruleset-drift.sh`), Pre-mutation gate (fixture file `git diff --quiet` clean),
   Mutation applied (None — the RED condition is the live ruleset's own divergence from the fixture,
   the same "historical/external state" shape as Family (a)), Observed failure (verbatim transcript,
   exit 1), and a placeholder heading `**Green re-run (after D-08):**` that Task 2 fills in.

4. Print the package for the maintainer in the task output and the SUMMARY: the two exact context
   strings; the two ways to apply (UI path: Settings → Rules → Rulesets → protect-main → add the two
   required checks; or the exact command
   `gh api --method PUT repos/seanb4t/codegraph-go/rulesets/20157557 --input .planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-ruleset-put-body.json`);
   the read-only verification command
   (`curl -s https://api.github.com/repos/seanb4t/codegraph-go/rulesets/20157557 | jq -r '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'`)
   and the consequence sentence from D-08 (every PR will run the tmux job).

Commit: `docs(02-04): prepare the D-08 protect-main ruleset update for maintainer review (GRD-12 RED recorded)`.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && f=.planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-ruleset-put-body.json; test -s "$f" && jq -e '(.enforcement == "active") and (.name == "protect-main") and ([.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | length == 8) and ([.rules[].type] | sort == ["deletion","non_fast_forward","pull_request","required_linear_history","required_status_checks"]) and (has("bypass_actors")) and (has("id") | not)' "$f" >/dev/null && jq -r '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context' "$f" | rg -c -F -e 'goreleaser check (config validation, DIST-01)' -e 'tmux e2e (real-pty harness, TTY-01..TTY-07)'</automated>
    <fails_when>non-zero exit or a printed count other than 2 — the body is missing, not a valid PUT body (carries read-only fields like `id`, drops `bypass_actors`, is not `active`), does not carry all five rule types, or does not add exactly the two D-08 contexts on top of the existing six</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && rg -c '^## Family \(c\) — GRD-12' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md && rg -q 'live has 6 contexts, fixture has 7 contexts' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md && rg -q 'Green re-run' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md && rg -c '^## Family \(b\) — GRD-11' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md</automated>
    <fails_when>non-zero exit or a printed count of 0 — Family (c) is missing, does not carry the 6-vs-7 RED line, lacks the green-re-run heading for Task 2, or Family (b) was clobbered</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(awk 'NF' .github/required-status-checks.txt | wc -l | tr -d ' ')" = "7" && git show --format= --name-only HEAD > /tmp/02-04-t1-files.txt && ! rg -q 'required-status-checks\.txt' /tmp/02-04-t1-files.txt && git diff --quiet -- .github/required-status-checks.txt</automated>
    <fails_when>non-zero exit — the fixture was edited before the live set changed (the forbidden shortcut); Task 1 must leave it at 7</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log -1 --format=%s%n%b > /tmp/02-04-msg1.txt && test -s /tmp/02-04-msg1.txt && ! rg -q -i 'ci skip|skip ci' /tmp/02-04-msg1.txt</automated>
    <fails_when>non-zero exit — the commit message carries a CI-skip marker</fails_when>
  </verify>
  <acceptance_criteria>
    - `02-04-ruleset-put-body.json` is a valid PUT body (no `id`/`_links`/timestamps), `name` `protect-main`, `enforcement` `active`, five rule types present, `bypass_actors` preserved, and exactly 8 contexts including both D-08 strings.
    - Family (c) in the mutation log records the pre-flip RED transcript (6 vs 7, exit 1) and has a `Green re-run (after D-08)` heading awaiting Task 2.
    - The fixture still has 7 entries at the end of Task 1.
    - The task output and SUMMARY carry the two exact context strings, the UI path, the exact `gh api --method PUT … --input …` command, and the read-only verification command.
  </acceptance_criteria>
  <done>The RED is on the record and the maintainer can apply D-08 by reading one file and one command.</done>
</task>

<task type="auto">
  <name>Task 2: After D-08 lands — grow the fixture to 8, watch the step go GREEN, resolve the STATE blocker</name>
  <precondition>A read-only `curl -s https://api.github.com/repos/seanb4t/codegraph-go/rulesets/20157557` returns HTTP 200 with `.enforcement == "active"` and its `required_status_checks[].context` list has exactly 8 entries including both `goreleaser check (config validation, DIST-01)` and `tmux e2e (real-pty harness, TTY-01..TTY-07)` — i.e. the maintainer's D-08 change (user_setup step 1) has landed. If not met, halt `blocking-human` and present Task 1's package (`02-04-ruleset-put-body.json`, the two strings, the UI path and the exact `gh api --method PUT` command); if the maintainer replies that the executor should run the prepared command, run it exactly as prepared, then re-check this precondition read-only before continuing.</precondition>
  <reversibility rating="costly">D-08 adds two required contexts to the live `protect-main` ruleset; undoing it later is a repository-settings change plus a fixture edit plus this step RED until both agree, and once `tmux e2e` is required every PR runs the tmux job — the maintainer's informed choice, confirmed at the halt.</reversibility>
  <files>.github/required-status-checks.txt, .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md, .planning/STATE.md</files>
  <read_first>
    - .github/required-status-checks.txt — the 7 current lines; the eighth is appended, nothing reordered.
    - internal/upgrade/taskfile_shape_test.go — `TestRequiredCheckNamesPreserved` (now reading the data file, per 02-03) and its logged count line, so the post-flip run can be asserted as `read 8`.
    - .planning/STATE.md `### Blockers/Concerns` — the `- [Phase 08-03] user_setup NOT completed:` bullet; `gsd-tools state resolve-blocker --text` matches a bullet by case-insensitive substring, so pass a distinctive fragment of that line.
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md — Family (c)'s `Green re-run (after D-08)` placeholder written by Task 1.
  </read_first>
  <action>
1. Record the live set as observed (the verification `curl` output, 8 contexts) in the SUMMARY with
   a timestamp.
2. Append exactly one line, `tmux e2e (real-pty harness, TTY-01..TTY-07)`, to
   `.github/required-status-checks.txt` (8 non-blank lines, newline-terminated, no blank line).
3. Run `bash scripts/check-ruleset-drift.sh` — expect `live has 8 contexts, fixture has 8 contexts`,
   `self-check PASS`, `ruleset-drift: PASS`, exit 0 — and
   `GOTOOLCHAIN=go1.26.6 go test ./internal/upgrade/ -count=1 -run 'TestRequiredCheckNamesPreserved$' -v`
   — expect PASS with `read 8`. Paste both under Family (c)'s `Green re-run (after D-08)` heading
   with exit codes.
4. Resolve the standing blocker through the tool:
   `gsd-tools state resolve-blocker --text "[Phase 08-03] user_setup NOT completed"`; confirm the
   JSON reports `resolved: true` and that no other Blockers bullet changed (`git diff -- .planning/STATE.md`
   shows one removed bullet only).
5. Commit: `ci(02-04): grow the required-status-check fixture to 8 after the D-08 ruleset change (GRD-12 GREEN)`.
   State in the SUMMARY whether the maintainer applied D-08 via the UI or authorised the prepared
   `gh api` command, and the consequence (the tmux job now runs on every PR).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && curl -sS -H 'Accept: application/vnd.github+json' https://api.github.com/repos/seanb4t/codegraph-go/rulesets/20157557 > /tmp/02-04-live.json && jq -e '(.enforcement=="active") and ([.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | length == 8)' /tmp/02-04-live.json >/dev/null && jq -r '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context' /tmp/02-04-live.json | rg -c -F -e 'goreleaser check (config validation, DIST-01)' -e 'tmux e2e (real-pty harness, TTY-01..TTY-07)'</automated>
    <fails_when>non-zero exit or a printed count other than 2 — the live ruleset does not yet carry exactly 8 active required contexts including both D-08 strings (this task must not have run; the precondition halts it)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test "$(awk 'NF' .github/required-status-checks.txt | wc -l | tr -d ' ')" = "8" && test "$(wc -l < .github/required-status-checks.txt | tr -d ' ')" = "8" && tail -1 .github/required-status-checks.txt | rg -q -F 'tmux e2e (real-pty harness, TTY-01..TTY-07)' && (bash scripts/check-ruleset-drift.sh > /tmp/02-04-green.txt 2>&1; echo "exit=$?" >> /tmp/02-04-green.txt); tail -4 /tmp/02-04-green.txt; rg -q 'live has 8 contexts, fixture has 8 contexts' /tmp/02-04-green.txt && rg -q 'self-check PASS' /tmp/02-04-green.txt && rg -q 'ruleset-drift: PASS' /tmp/02-04-green.txt && rg -q 'exit=0' /tmp/02-04-green.txt</automated>
    <fails_when>non-zero exit — the fixture is not exactly 8 lines ending in the tmux context, or the script does not print 8/8, its self-check did not pass, or it did not print PASS with exit 0</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && GOTOOLCHAIN=go1.26.6 go test ./internal/upgrade/ -count=1 -run 'TestRequiredCheckNamesPreserved$' -v 2>&1 | tee /tmp/02-04-go.txt | tail -4; rg -q '^--- PASS: TestRequiredCheckNamesPreserved' /tmp/02-04-go.txt && rg -q 'read 8 required contexts' /tmp/02-04-go.txt</automated>
    <fails_when>non-zero exit, no PASS line, or no `read 8 required contexts` log — the tmux context is not a job `name:` in any workflow file, or the loader did not read 8 entries</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && ! rg -q -F '[Phase 08-03] user_setup NOT completed' .planning/STATE.md && rg -q '^### Blockers/Concerns' .planning/STATE.md && rg -c '^- ' .planning/STATE.md >/dev/null</automated>
    <fails_when>non-zero exit — the Phase 08-03 blocker bullet survives in STATE.md, or the Blockers/Concerns section itself disappeared (a hand edit gone wrong rather than the tool verb)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && awk '/^## Family \(c\)/{f=1} f' .planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md > /tmp/02-04-famc.txt && rg -q 'live has 6 contexts, fixture has 7 contexts' /tmp/02-04-famc.txt && rg -q 'live has 8 contexts, fixture has 8 contexts' /tmp/02-04-famc.txt && rg -q 'ruleset-drift: PASS' /tmp/02-04-famc.txt</automated>
    <fails_when>non-zero exit — Family (c) does not carry both the pre-flip RED (6 vs 7) and the post-flip GREEN (8 vs 8, PASS) transcripts</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log -1 --format=%s%n%b > /tmp/02-04-msg2.txt && test -s /tmp/02-04-msg2.txt && ! rg -q -i 'ci skip|skip ci' /tmp/02-04-msg2.txt && git show --format= --name-only HEAD > /tmp/02-04-files.txt && rg -q '\.github/required-status-checks\.txt' /tmp/02-04-files.txt && rg -q '02-MUTATION-LOG\.md' /tmp/02-04-files.txt && rg -q '\.planning/STATE\.md' /tmp/02-04-files.txt</automated>
    <fails_when>non-zero exit — the commit is missing one of the three expected files, or carries a CI-skip marker</fails_when>
  </verify>
  <acceptance_criteria>
    - The live ruleset (read-only curl) has 8 active required contexts including both D-08 strings before any fixture edit.
    - `.github/required-status-checks.txt` has exactly 8 lines, the last being the tmux context; `scripts/check-ruleset-drift.sh` prints 8/8, `self-check PASS`, `ruleset-drift: PASS`, exit 0.
    - `TestRequiredCheckNamesPreserved` passes and logs `read 8 required contexts`.
    - STATE.md no longer carries the `[Phase 08-03] user_setup NOT completed` bullet; the Blockers section still exists.
    - Family (c) carries both RED and GREEN transcripts.
    - The SUMMARY states how D-08 was applied and the live set observed; the commit has no CI-skip marker.
  </acceptance_criteria>
  <done>Fixture and live ruleset agree at 8, the step is GREEN by changing the world to match the claim, and the v0.13.0 blocker is closed through the tool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → GitHub repository settings (ruleset PUT) | a write to branch protection; only the maintainer may authorise it, and only via a body the maintainer has read |
| authenticated `gh api` read → committed body file | the body carries `bypass_actors` (actor ids, not secrets) and is committed for review; it must contain no token |
| live ruleset state → fixture edit | the fixture may only follow the live set, never lead it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Elevation of Privilege | executor applying the ruleset change unprompted | high | mitigate | The PUT is a `user_setup` item behind a `<precondition>` that halts `blocking-human`; the action states the executor runs the prepared command only on the maintainer's explicit reply; Task 1's verify asserts the fixture is still 7 (no shortcut). |
| T-02-04-02 | Tampering | PUT body silently dropping `bypass_actors` or a rule type | high | mitigate | Body built from an authenticated read; `jq -e` verify asserts `bypass_actors` present, all five rule types present, `enforcement` active, and exactly 8 contexts. |
| T-02-04-03 | Repudiation | "GREEN because the fixture was edited to match" | high | mitigate | Precondition requires the live set to read 8 BEFORE the edit; Family (c) records RED (6 vs 7) then GREEN (8 vs 8) with exit codes. |
| T-02-04-04 | Information Disclosure | committed body file | low | accept | Contains only ruleset configuration and actor ids already visible to repo admins; no token or secret is ever written to it (the read uses `gh`'s own auth, the body is data only). |
| T-02-04-05 | Denial of Service | every PR now runs the tmux job | low | accept | D-08: the maintainer's informed choice, restated at the halt; the tmux job is already green on `main` (run 34859136748). |
| T-02-04-06 | Tampering | STATE.md hand edit | medium | mitigate | Blocker removed via `gsd-tools state resolve-blocker`; verify asserts the bullet is gone and the section survives. |
| T-02-04-07 | Denial of Service | `protect-main` ruleset | high | mitigate | Commit messages grepped for CI-skip markers. |
| T-02-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed. |
</threat_model>

<verification>
- Family (c): RED (6 vs 7) before, GREEN (8 vs 8, PASS) after; both from the real script.
- `02-04-ruleset-put-body.json` is a valid, complete PUT body adding exactly the two D-08 contexts.
- Task 2 ran only after the live set read 8; fixture is 8 lines; Go test reads 8; STATE blocker resolved by verb.
</verification>

<success_criteria>
The live `protect-main` ruleset and the shared fixture agree at 8 contexts, the drift step is GREEN on
the record after being RED on the record, and the maintainer applied the change from a one-glance
package rather than the executor applying it silently.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Path / name |
|---|---|---|
| `02-04-ruleset-put-body.json` | new review artifact (ruleset PUT body) | `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-ruleset-put-body.json` |
| eighth fixture line `tmux e2e (real-pty harness, TTY-01..TTY-07)` | value added to the data file | `.github/required-status-checks.txt` |
| Family (c) entry | appended mutation-log section (RED + GREEN) | `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-MUTATION-LOG.md` |
| resolved `[Phase 08-03] user_setup` blocker | tool-verb removal | `.planning/STATE.md` |
</artifacts_produced>

<output>
Create `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-04-SUMMARY.md` when done.
</output>
