---
phase: 02-guards-ci-wiring-docs-burn-down
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/components/ui/button/**
  - web/src/lib/components/ui/command/**
  - web/src/lib/components/ui/dialog/**
  - web/src/lib/components/ui/input/**
  - web/src/lib/components/ui/input-group/**
  - web/src/lib/components/ui/table/**
  - web/src/lib/components/ui/tabs/**
  - web/src/lib/components/ui/textarea/**
  - web/build/**
autonomous: true
requirements: [GRD-10]
user_setup: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The drift is re-measured fresh before anything is regenerated: `task web:components:drift` run locally prints `compared 50 vendored component files across 8 components`, a pnpm line confirming the pinned `v11.23.0` (pnpm 12's package-manager self-management honours `web/package.json`'s `packageManager` — verified at plan time: `cd web && pnpm --version` → `11.23.0`), and a `::error::` line per differing file; that list is the fresh evidence D-09 asks for. If the printed pnpm version is NOT 11.23.0, the fallback is a `workflow_dispatch` of `components-drift.yml` and its log is the evidence instead — never a re-vendor under an unpinned pnpm. D-09."
    - "All eight families (`button command dialog input input-group table tabs textarea` — derived from `git ls-files -- 'web/src/lib/components/ui/'`, never hand-listed) are regenerated in ONE `pnpm dlx shadcn-svelte@1.5.1 add … -y -o` invocation against the real `web/` tree, so every vendored component sits at one registry snapshot; families that regenerate byte-identically show no diff. D-10 / D-11."
    - "Every gate D-12 names is green before the commit: `cd web && pnpm check` exit 0 with a case-insensitive zero-error count; `cd web && pnpm vitest run` exit 0 with zero lines matching `unhandled errors`; `task web:build && task web:drift` green; `task build:release`; `task check:graph-console` (self-test then both corpora) and `node web/scripts/breadcrumb-check.mjs` green against the rebuilt binary; `task web:components:drift` prints `PASS — all 50 vendored component files across 8 components byte-identical`. D-12."
    - "`web/build/**` is rebuilt and committed in the SAME commit as the `web/src` change (RESEARCH Pitfall 8: otherwise `web:drift`'s SOURCE half legitimately goes RED) and the incidental `web/package.json` devDependency caret bump the CLI's install step makes is NOT committed unless it is a genuine, reviewed version change — `web/pnpm-lock.yaml` is unchanged. D-10 / D-12."
    - "The SUMMARY carries a per-file diff table for every changed file (24 expected per RESEARCH — the real current scope, not CONTEXT's 3-file undercount): file, lines +/-, whether the change is visual (class/markup), API (props/exports/events), or internal, with any visual change named — the maintainer reviews at end of phase, no mid-plan checkpoint. D-12."
    - "One commit in the lineage of `205da685`, authored as a human-approved re-vendor, and WINDOWS #31 is closed by 02-07 (`gsd-tools windows fixed 31`) citing this plan's `PASS` transcript — the local-toolchain hypothesis in #31 was already refuted by run 34820878640 (Corepack pnpm 11.23.0) and is refuted again by this run. D-09 / D-10."
  artifacts:
    - "web/src/lib/components/ui/{button,command,dialog,input,input-group,table,tabs,textarea}/*.svelte — regenerated to one shadcn-svelte@1.5.1 registry snapshot"
    - "web/build/** — rebuilt bundle and .build-manifest in the same commit"
  key_links:
    - "shadcn-svelte@1.5.1 registry → `pnpm dlx shadcn-svelte@1.5.1 add <8 families> -y -o` in `web/` → `web/src/lib/components/ui/**` → `task web:build` → `web/build/**` + `.build-manifest` → `task web:drift` GREEN → `go build` embeds `web/build` → `task check:graph-console` / `breadcrumb-check.mjs` drive the rebuilt binary in Chromium"
    - "`task web:components:drift` (scratch regeneration, `cmp -s`) GREEN is the closing evidence — the same target that is RED at the start of the plan"
  prohibitions:
    - statement: "No waiver of WINDOWS #31, no allowlist, no per-file exclusion added to `web:components:drift` — vendored components that drift are re-vendored with a reviewed diff, never waived (D-10)."
      status: resolved
      verification: judgment
    - statement: "No vendored `.svelte` file is hand-edited; the only writer of `web/src/lib/components/ui/**` in this plan is the shadcn-svelte CLI at the pinned `@1.5.1` — never `@latest` (D-10/D-11; RESEARCH Don't-Hand-Roll)."
      status: resolved
      verification: judgment
    - statement: "No partial re-vendor: never a tree with some families at the new snapshot and others at the old — one CLI invocation covering all eight, one commit (D-11)."
      status: resolved
      verification: judgment
    - statement: "No dependency change is committed as a side effect: `web/package.json` and `web/pnpm-lock.yaml` are restored unless a version bump is genuinely reviewed and named in the SUMMARY; no new npm package is added."
      status: resolved
      verification: judgment
    - statement: "No re-vendor under an unpinned pnpm: if the local run does not print `v11.23.0`, regeneration is NOT attempted locally (WINDOWS #31's refuted confound would be reintroduced); the CI `workflow_dispatch` path is used instead."
      status: resolved
      verification: judgment
    - statement: "No mid-plan human checkpoint (D-12) and no `[ci skip]` / `[skip ci]` in the commit message."
      status: resolved
      verification: judgment

flagged_assumptions:
  - id: EDGE-GRD-10-01
    requirement: GRD-10
    probe: "unclassified — review manually"
    status: unresolved
    note: "Deterministic edge probe returned `unclassified` for GRD-10; not auto-resolved. Planner's reading: the registry may move again between this plan's commit and the next scheduled Monday run of `components-drift.yml`, which would re-open the same window shape. No acceptance criterion covers a future registry move — that is the deferred 'pin the registry snapshot' idea in CONTEXT, out of scope by decision, and the monitor's weekly run is the control. Surfaced, not dropped."
---

<objective>
Re-vendor all eight shadcn-svelte component families to one registry snapshot with a reviewed diff,
rebuild the committed bundle in the same commit, and run every gate — closing the drift
`web:components:drift` has reported since 2026-09-07.

**What are we testing, and why?** We are testing that the vendored component source equals what the
pinned CLI (`shadcn-svelte@1.5.1`) regenerates from the registry today — the one property the
drift monitor exists to hold — and that the regenerated UI still builds, type-checks, passes its
unit tests and drives correctly in a real browser. We are NOT diagnosing the drift's cause: D-09
records that the 2026-09-14 scheduled run under Corepack-pinned pnpm 11.23.0 already refuted the
local-toolchain hypothesis; the registry moved under a pinned CLI, which is what the monitor detects.

RESEARCH open question 1 is answered here: a fresh `workflow_dispatch` is triggered ONLY if the
local regeneration cannot run under the pinned pnpm. At plan time `cd web && pnpm --version`
prints `11.23.0` on this machine (pnpm 12's self-management honours `packageManager`), and
`web:components:drift`'s own `elif` branch handles the no-Corepack host — so the local run IS the
fresh evidence and the dispatch is the fallback, not the default.

RESEARCH's scope correction stands: 24 of 50 files across 7 of 8 families differed on 2026-09-14,
not the 3 files CONTEXT's summary names. Estimate accordingly.

Output: regenerated `web/src/lib/components/ui/**`, rebuilt `web/build/**`, one commit, a per-file
diff table in the SUMMARY, and a `web:components:drift` PASS transcript for 02-07 to close #31.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-CONTEXT.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md
@.planning/phases/02-guards-ci-wiring-docs-burn-down/02-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Fresh drift measurement under the pinned pnpm — the RED that names every differing file</name>
  <files>.planning/phases/02-guards-ci-wiring-docs-burn-down/02-05-SUMMARY.md (read-only measurement — no source file is modified; the transcript is recorded in the SUMMARY)</files>
  <read_first>
    - Taskfile.yml lines 1352-1516 (`web:components:drift`): the Corepack/pnpm resolution branch at step (0) and the exact message it prints on a no-Corepack host, the `git ls-files`-derived family list at (a), the count line at (b), the scratch regeneration at (c) (`pnpm dlx shadcn-svelte@1.5.1 add ${components} -y -o`), and the per-file `::error::` loop at (d).
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-RESEARCH.md § "Code Examples → GRD-10" — the 24-file list from run 34820878640 to compare against, and § "Environment Availability" (its local-pnpm caution, superseded by the plan-time `11.23.0` observation inside `web/`).
    - .github/workflows/components-drift.yml lines 58-91 — the `workflow_dispatch` trigger and the job that runs the same target (the fallback path).
    - web/package.json line 6 — `"packageManager": "pnpm@11.23.0"`.
  </read_first>
  <action>
1. Confirm the pinned toolchain resolves locally: `cd web && pnpm --version` must print `11.23.0`
   (pnpm's self-management of `packageManager`). Record it. Also record `node --version`.
2. Pre-gate: `git status --porcelain web/` must be empty; record it.
3. Run `task web:components:drift` from the repo root and capture the full transcript and exit code.
   Expected: the step-(0) line about pnpm resolution, `compared 50 vendored component files across
   8 components`, a `pnpm` install line ending in `using pnpm v11.23.0`, one `::error::…differs from
   shadcn-svelte@1.5.1's regeneration` line per drifted file, exit non-zero. Extract the differing
   file list, sort it, and compare it with RESEARCH's 24-file list; record the two lists and any
   delta in the SUMMARY (a delta means the registry moved again since 2026-09-14 — still re-vendor,
   the decision rule does not change).
4. Fallback ONLY if the transcript does not show `v11.23.0`: do not regenerate locally; instead
   `gh workflow run components-drift.yml --ref $(git branch --show-current)`, `gh run watch` the new
   run, `gh run view <id> --log` and take the differing-file list and pnpm version from that log.
   Record which path produced the evidence.
5. Assert the source tree is still untouched (`git status --porcelain web/` empty — the target
   regenerates into a scratch copy). No commit in this task; the transcript goes into the SUMMARY.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go/web && test "$(pnpm --version)" = "11.23.0"</automated>
    <fails_when>non-zero exit — inside `web/` bare `pnpm` does not resolve to the pinned 11.23.0, so a local regeneration would reintroduce the local-toolchain confound; the CI dispatch fallback must be used and this gate re-judged from the run log</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && test -z "$(git status --porcelain web/)" && (task web:components:drift > /tmp/02-05-red.txt 2>&1; echo "exit=$?" >> /tmp/02-05-red.txt); tail -3 /tmp/02-05-red.txt; rg -q 'compared 50 vendored component files across 8 components' /tmp/02-05-red.txt && rg -q 'pnpm v11\.23\.0' /tmp/02-05-red.txt && test "$(rg -c 'differs from shadcn-svelte@1.5.1' /tmp/02-05-red.txt)" -ge 1 && ! rg -q 'exit=0' /tmp/02-05-red.txt && test -z "$(git status --porcelain web/)"</automated>
    <fails_when>non-zero exit — the tree was dirty, the target did not enumerate 50 files / 8 families, the run was not under pnpm v11.23.0, zero files were reported as differing (nothing to re-vendor — re-check before proceeding), the target exited 0, or the scratch regeneration leaked into the source tree</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm --version` prints `11.23.0`; `git status --porcelain web/` is empty before and after the drift run.
    - The drift transcript shows `compared 50 vendored component files across 8 components`, `pnpm v11.23.0`, at least one `differs from shadcn-svelte@1.5.1` line, and a non-zero exit.
    - The SUMMARY lists the differing files (sorted) beside RESEARCH's 24-file list with any delta named, and states which path (local run or CI dispatch) produced the evidence.
  </acceptance_criteria>
  <done>The current drift is measured fresh under the pinned pnpm and every differing file is named before any regeneration touches the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate all eight families in place, review the diff, run every gate, rebuild the bundle, commit once</name>
  <files>web/src/lib/components/ui/button/**, web/src/lib/components/ui/command/**, web/src/lib/components/ui/dialog/**, web/src/lib/components/ui/input/**, web/src/lib/components/ui/input-group/**, web/src/lib/components/ui/table/**, web/src/lib/components/ui/tabs/**, web/src/lib/components/ui/textarea/**, web/build/**</files>
  <read_first>
    - .planning/phases/02-guards-ci-wiring-docs-burn-down/02-PATTERNS.md § "web/src/lib/components/ui/{8 families}" — the exact CLI invocation, the D-12 gate order, and the `package.json` exclusion rule.
    - Taskfile.yml `web:build` (line ~795), `web:build:verify` (~855), `web:drift` (~1243), `check:graph-console` (~1838, its preconditions: `./codegraph` built, `.codegraph/store` present, guava corpus fetched) and `build:release`.
    - web/scripts/breadcrumb-check.mjs header comment (lines 1-30) — it owns its own lifecycle against the built `./codegraph` and a real index; `--out` defaults to `corpora/breadcrumb-check.json`.
    - .planning/phases/01-defect-flake-burn-down/01-CONTEXT.md — WR-01 precedent: vitest is judged by exit 0 AND zero `unhandled errors` lines, never by the pass count; STATE.md Blockers `[Phase 1]` last bullet — the plan-level `pnpm check`-only gate that missed 11 unhandled errors.
    - git show --stat 205da685 — the lineage commit shape for a human-approved vendoring.
  </read_first>
  <action>
1. Derive the family list exactly as the drift target does:
   `git ls-files -- 'web/src/lib/components/ui/' | awk -F/ '{print $6}' | sort -u` — expect the
   eight names `button command dialog input input-group table tabs textarea`; fail loudly if the
   count is not 8.
2. Snapshot `web/package.json` and `web/pnpm-lock.yaml` hashes. In `web/`, run ONE invocation:
   `pnpm dlx shadcn-svelte@1.5.1 add <the eight names> -y -o` (pinned version; `-o` overwrites in
   place). Confirm the CLI's own output names `shadcn-svelte@1.5.1` and that `pnpm --version` was
   `11.23.0` for the run.
3. If `web/package.json` or `web/pnpm-lock.yaml` changed, inspect the diff: if it is only the
   incidental devDependency caret-range bump RESEARCH describes, `git checkout -- web/package.json
   web/pnpm-lock.yaml` and record the reverted hunk in the SUMMARY; if it is a genuine version change
   you can justify, keep it, name it in the SUMMARY as a reviewed bump, and say why. Then
   `cd web && pnpm install --frozen-lockfile` must succeed (lockfile consistent).
4. Review: `git diff --stat -- web/src/lib/components/ui/` and, per changed file, `git diff` it.
   Build the SUMMARY table: file | +/- | kind (visual: class strings/markup; API: props, exports,
   events, `$props()` shape; internal: imports, helpers) | note naming any visual or API change.
   Families with zero diff are listed as "byte-identical". Expected changed set ≈ Task 1's list.
5. Gates, in this order, each captured to a file with its exit code:
   - `cd web && pnpm check` → exit 0 AND a case-insensitive zero-error count in the output.
   - `cd web && pnpm vitest run` → exit 0 AND zero lines matching `unhandled errors`.
   - `task web:build && task web:drift` → both exit 0 (`web/build/**` now regenerated).
   - `task build:release` → `./codegraph` rebuilt with the new embedded bundle.
   - `task check:graph-console` → exit 0 (self-test, then the committed two-corpus verdict); if
     its preconditions fail because the index or guava corpus is absent on this machine, run
     `./codegraph index --force .` and `task corpora:fetch` first — never skip the gate.
   - `node web/scripts/breadcrumb-check.mjs` → exit 0 (browse view drives against the new build).
   - `task web:components:drift` → exit 0, `PASS — all 50 vendored component files across 8 components byte-identical`.
6. Commit ONCE, staging `web/src/lib/components/ui/` and `web/build/` together (plus a reviewed
   `web/package.json`/lockfile change only if step 3 kept one), message:
   `fix(02-05): re-vendor all eight shadcn-svelte families to one registry snapshot, human-approved (GRD-10, closes WINDOWS #31)`
   with a body naming the registry CLI version, the pnpm version, the changed-file count, and the
   gates run. Do not close WINDOWS #31 here — plan 02-07 does, citing this SUMMARY.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && (task web:components:drift > /tmp/02-05-green.txt 2>&1; echo "exit=$?" >> /tmp/02-05-green.txt); tail -3 /tmp/02-05-green.txt; rg -q 'PASS — all 50 vendored component files across 8 components byte-identical' /tmp/02-05-green.txt && rg -q 'pnpm v11\.23\.0' /tmp/02-05-green.txt && rg -q 'exit=0' /tmp/02-05-green.txt</automated>
    <fails_when>non-zero exit — the drift target is not GREEN over 50 files / 8 families under pnpm v11.23.0 after the re-vendor (a partial or wrong-version regeneration)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go/web && pnpm check > /tmp/02-05-check.txt 2>&1; s=$?; tail -3 /tmp/02-05-check.txt; test $s -eq 0 && rg -q -i '\b0 errors\b' /tmp/02-05-check.txt</automated>
    <fails_when>non-zero exit or no case-insensitive `0 errors` line — svelte-check reports type errors in the regenerated components or their consumers (piped output is MACHINE format with uppercase ERRORS; the case-insensitive match covers both)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go/web && pnpm vitest run > /tmp/02-05-vitest.txt 2>&1; s=$?; tail -5 /tmp/02-05-vitest.txt; test $s -eq 0 && ! rg -q -i 'unhandled errors' /tmp/02-05-vitest.txt</automated>
    <fails_when>non-zero exit or any `unhandled errors` line — a test fake or component consumer broke under the new component source, regardless of how many tests passed (Phase 1 WR-01)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task web:build && task web:drift && task build:release</automated>
    <fails_when>non-zero exit — the SPA does not build, the committed `web/build/**` is not the bundle the source produces (SOURCE-half or OUTPUT-half mismatch), or the release binary does not build with the embedded bundle</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && task check:graph-console</automated>
    <fails_when>non-zero exit — the live-Chromium graph gate (self-test, then this repo's graph and the pinned guava corpus) reports console errors or a missing precondition against the rebuilt binary</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && node web/scripts/breadcrumb-check.mjs</automated>
    <fails_when>non-zero exit — the browse-view breadcrumb check fails against the rebuilt binary (the regenerated tabs/table/input/button/command/dialog components broke the source view)</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git show --format= --name-only HEAD > /tmp/02-05-files.txt && test "$(rg -c '^web/src/lib/components/ui/' /tmp/02-05-files.txt)" -ge 1 && test "$(rg -c '^web/build/' /tmp/02-05-files.txt)" -ge 1 && ! rg -q '^web/pnpm-lock\.yaml$' /tmp/02-05-files.txt && test -z "$(git status --porcelain web/)"</automated>
    <fails_when>non-zero exit — the single commit does not carry both the regenerated components and the rebuilt `web/build/**`, the lockfile was committed as an unreviewed side effect, or uncommitted changes remain under `web/`</fails_when>
    <automated>cd /Volumes/Code/github.com/seanb4t/codegraph-go && git log -1 --format=%s%n%b > /tmp/02-05-msg.txt && rg -q '^fix\(02-05\): re-vendor' /tmp/02-05-msg.txt && ! rg -q -i 'ci skip|skip ci' /tmp/02-05-msg.txt && ! rg -q '@latest' /tmp/02-05-msg.txt</automated>
    <fails_when>non-zero exit — the commit subject is not the re-vendor commit, carries a CI-skip marker, or mentions an unpinned `@latest` regeneration</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly eight families were passed to one `pnpm dlx shadcn-svelte@1.5.1 add … -y -o` run under pnpm 11.23.0.
    - `task web:components:drift` prints the 50-file / 8-component PASS line and exits 0.
    - `pnpm check` exit 0 with a zero-error count; `pnpm vitest run` exit 0 with no `unhandled errors` line; `task web:build && task web:drift && task build:release` exit 0; `task check:graph-console` and `node web/scripts/breadcrumb-check.mjs` exit 0.
    - One commit contains both `web/src/lib/components/ui/**` and `web/build/**`; `web/pnpm-lock.yaml` is not in it unless the SUMMARY names a reviewed bump; `git status --porcelain web/` is empty afterwards.
    - The SUMMARY has the per-file diff table with kind and named visual/API changes for every changed file, and the pnpm/CLI versions used.
    - The commit message has no CI-skip marker and no `@latest`.
  </acceptance_criteria>
  <done>All eight vendored families sit at one registry snapshot, every D-12 gate is green, the bundle is rebuilt in the same commit, and the reviewed diff is on the record for the end-of-phase review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn-svelte registry (network) → `web/src/lib/components/ui/**` | third-party source is written into the repository by the CLI; it ships in the embedded SPA |
| CLI dependency-install side effect → `web/package.json` / `pnpm-lock.yaml` | the CLI may bump a devDependency range as a side effect of vendoring |
| regenerated SPA → embedded binary → loopback UI | the vendored components render inside the read-only, loopback-bound UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering | registry-sourced component code | high | mitigate | Pinned CLI `@1.5.1` (never `@latest`); every changed file is diffed and classified in the SUMMARY with visual/API changes named; the maintainer reviews at end of phase (D-10/D-12); `pnpm check`, vitest, and two live-browser gates run against the rebuilt binary. |
| T-02-05-02 | Tampering | unreviewed dependency bump via CLI side effect | medium | mitigate | `web/package.json`/`pnpm-lock.yaml` hashes snapshotted; incidental caret bump reverted; `pnpm install --frozen-lockfile` must pass; verify asserts the lockfile is not in the commit unless reviewed. |
| T-02-05-03 | Repudiation | "green because regenerated under the wrong toolchain" | high | mitigate | pnpm version asserted `11.23.0` inside `web/` before regeneration and in both drift transcripts; fallback to the Corepack CI run if not; the RED transcript names every file before the change. |
| T-02-05-04 | Tampering | `web/build/**` out of step with `web/src` | high | mitigate | `task web:build && task web:drift` gate; verify asserts the commit carries both paths. |
| T-02-05-05 | Denial of Service | live-browser gates skipped for missing preconditions | medium | mitigate | The action names the setup commands (`./codegraph index --force .`, `task corpora:fetch`) and forbids skipping; the gates are verify commands with non-zero-exit failure signals. |
| T-02-05-06 | Denial of Service | `protect-main` ruleset | high | mitigate | Commit message grepped for CI-skip markers. |
| T-02-05-SC | Tampering | npm/pip/cargo installs | medium | accept | No new package is added to `web/package.json`; `pnpm dlx shadcn-svelte@1.5.1` executes the already-vetted pinned CLI version used by `web:components:drift` and prior vendoring commits (RESEARCH Package Legitimacy Audit: no new package). Any devDependency bump is reverted unless reviewed and named. |
</threat_model>

<verification>
- Fresh RED under pnpm 11.23.0 naming every differing file; then one pinned regeneration of all eight families.
- All D-12 gates green: check, vitest (no unhandled errors), build+drift, release build, graph-console, breadcrumb-check, components-drift PASS.
- One commit with components and bundle; lockfile untouched unless reviewed; SUMMARY diff table complete.
</verification>

<success_criteria>
Every vendored component family matches the pinned CLI's regeneration at one registry snapshot, the
rebuilt UI passes its type, unit and live-browser gates, and the reviewed diff plus the PASS transcript
are on the record for 02-07 to close WINDOWS #31.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Path / name |
|---|---|---|
| regenerated component files (expected ≈24 changed of 50) | re-vendored source | `web/src/lib/components/ui/{button,command,dialog,input,input-group,table,tabs,textarea}/*.svelte` |
| rebuilt bundle + manifest | regenerated committed output | `web/build/**` (incl. `web/build/.build-manifest`) |
| per-file diff table | SUMMARY section | `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-05-SUMMARY.md` |
</artifacts_produced>

<output>
Create `.planning/phases/02-guards-ci-wiring-docs-burn-down/02-05-SUMMARY.md` when done.
</output>
