---
phase: 05-process-ci-in-tree-sweep
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agents/instructions.go
  - internal/agents/gemini.go
  - internal/agents/hermes.go
  - internal/agents/hermes_test.go
  - internal/agents/kiro.go
  - internal/agents/registry.go
  - internal/agents/registry_test.go
  - internal/agents/toml.go
  - internal/agents/testhelpers_test.go
  - internal/agents/types.go
  - internal/cli/affected_test.go
  - internal/cli/daemon.go
  - internal/cli/explore.go
  - internal/cli/githooks.go
  - internal/cli/init.go
  - internal/cli/install_test.go
  - internal/cli/man.go
  - internal/cli/present/status_test.go
  - internal/cli/serve_test.go
  - internal/cli/status.go
  - internal/githooks/githooks.go
  - internal/githooks/githooks_test.go
  - internal/gitmeta/detect.go
  - internal/gitmeta/githooks.go
  - internal/gitmeta/notice.go
  - internal/gitmeta/worktree.go
  - internal/watch/policy.go
  - internal/indexer/nodeid/nodeid_test.go
  - internal/indexer/resolve.go
  - internal/indexer/routes/walk.go
  - internal/indexer/languages_python.go
  - internal/indexer/pyextract/types.go
  - internal/indexer/mainstream/phpextract/resolution_test.go
  - internal/corpora/record.go
  - internal/mcp/archtest/protocol_version_test.go
  - internal/mcp/markdown_test.go
  - internal/schema/graph.proto
  - internal/schema/graph.pb.go
  - tools/bench/realcorpus/manifest.go
  - .claude/CLAUDE.md
autonomous: true
requirements: [CODE-01]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Comments across `internal/agents` no longer carry comparison framing: the 'Corrected Per-Agent Parity Table' header (instructions.go:16) is reworded to a neutral per-agent table name; the hard-format-contract paragraphs (instructions.go:8-10, registry_test.go:182) keep the byte-format contract truth — a Go uninstall must recognize the marker block any prior install wrote — without the 'a TS install wrote' / 'cross-implementation parity' phrasing; every other parity-oracle row (toml.go:6-9, types.go:58, testhelpers_test.go:13, registry.go:70, hermes.go:21, hermes_test.go:313) is reworded to name the real artifact (the marker block) rather than any second implementation; past-tense history (instructions.go:19, the #529/#704 row) is KEEP with a recorded reason (D-02)."
    - "Comments across `internal/cli` (explore.go:56-57 stdout-parity, daemon.go:162 banner-parity, status.go:28 golden-parity oracle, affected_test.go:246 TS-parity glob, install_test.go:328 parity-fallback — now in both files_modified and the task, review H4, present/status_test.go:103 structural-parity, serve_test.go:141/:157 verbatim-TS messages, init.go:123 offerWatchFallback port, man.go:14 Go-only surface extension) are reworded to own-terms behavior descriptions — no flag, output, or test assertion changes, only prose (research §4.3 row by row)."
    - "`internal/cli/githooks.go:15` (review H4, verified present) — the comment 'This is a Go-only surface extension over internal/githooks — TS 1.3.1 has no standalone githooks command' — is reworded to drop the origin comparison while keeping the real fact (githooks is a project-native command built over internal/githooks), and `githooks.go` is now in files_modified."
    - "Comments across `internal/githooks`, `internal/gitmeta`, `internal/watch` lose the type-origin framing: githooks.go package doc and body (githooks.go:2, :22-24, :198, :309, :408-410), gitmeta/detect.go (:18), gitmeta/notice.go (:7-16), gitmeta/worktree.go (:22-26, :86-89), gitmeta/githooks.go (:14), watch/policy.go (:62, :66-71, :77, :86, :90, :109-112, :125, :156) describe the hook/watcher behavior on their own terms — the byte-identical marker contract and WSL2 path normalization behavior survive, the 'ported / byte-parity / divergence-from-TS' wording goes (research §4.4 rows)."
    - "The remaining internal/indexer rows are swept: nodeid_test.go:10 ('TS-parity <kind>:<32-hex>'), resolve.go:629 ('TS-parity behavior', if still present), routes/walk.go:15 ('kept for parity with the pattern this mirrors'), languages_python.go:39, pyextract/types.go:74, mainstream/phpextract/resolution_test.go:159 — with the stale golden-filename citations re-pointed to the current behavioral_*_test.go names (constraint 4) — nothing else in indexer changes (tsextract + languages + capability matrix are product surface, D-05, untouched except the D-03 matrix comments handled by 05-04)."
    - "internal/corpora/record.go:104 ("see parity_tsjs_test.go's identical group") is fixed to name the file it actually argues from (behavioral_tsjs_test.go) — a stale-path fix, not a framing test (research §4.5 R)."
    - "internal/mcp comments: markdown_test.go:25-32 and archtest/protocol_version_test.go:56/:250 re-point the stale `golden_parity_test.go` citation to the current golden-plus-behavioral file names in their PROSE ONLY — the `packages.Load` runtime line (protocol_version_test.go:261) is NOT touched (research §4.6 'do not change the packages.Load line, only the prose')."
    - "tools/bench/realcorpus/manifest.go stale references to the golden harness are re-pointed: the four `golden_parity_test.go` citations (:16, :74, :92, :156, :160) become the current behavioral golden-file names or the 'golden suite' — comments only, the PERF-01 corpus manifest itself stays untouched (Phase 6 owns its de-coupling, not this comment edit), and the past-tense BASELINE.md row (:241) is recorded KEEP."
    - "internal/schema doc comments stay locked: every 'Go-parity' field comment in graph.proto (:36/:41/:46/:51/:56/:92) and its generated twin in graph.pb.go (:54/:58/:62/:66/:70/:234) is rewritten to a neutral additive-field description in BOTH files in the same edit — the repository has no protoc pipeline, so a one-sided comment edit would create a GoDoc/.proto mismatch (research Pitfall 3); the D-03 comment text (additive schema fields) is present in both files, with no remnant comparison wording."
    - "`.claude/CLAUDE.md` core-value sentence (D-10) is rewritten on the project's own terms: the 'uninstall TS CodeGraph ... migrate their indexes ... works the same or better' sentence no longer instructs uninstalling TS, migrating indexes, or dropping to migrate; 'the parity command surface (… , migrate)' phrase and the migration-reader tech-stack note (modernc row) are removed/adjusted because the migrate capability is removed (D-04) — codegraph-go is now defined as a pre-indexed query surface, not a replacement."
    - "`go test -count=1 ./...` stays green throughout (the sweep is comment/string-only in shipped code paths; only string literals in output messages are touched with care)."
  prohibitions:
    - "no file/function/identifier renames except where the census calls for the stale path fix / comment naming — no 'syntheticParity'-trouble identifiers in this plan (that family is 05-04's; this plan is agents/cli/githooks/…)"
    - "no regex over any bare word (each row hand-authored from research §4)"
    - "no de-listing tsextract or a language; no removal of the capability matrix"
    - "no editing of the Go marker-block constant text (agents) — only the comment around it"
    - "no removal of past-tense historical statements (KEEP-with-reason, D-02)"
  artifacts:
    - internal/agents/* (ten files) comment-swept on own terms
    - internal/cli/* comment-swept; man.go KEEP row recorded
    - internal/githooks + gitmeta + watch (all rows) comment-swept
    - internal/indexer remaining rows + corpora stale path fixed
    - internal/mcp prose-only stale path fixes
    - internal/schema graph.proto + graph.pb.go lockstep field comments
    - tools/bench/realcorpus/manifest.go stale golden citations re-pointed (capability untouched)
    - .claude/CLAUDE.md core-value/parity-frames rewritten
  key_links:
    - "graph.proto ↔ graph.pb.go field doc comments — one edit per field in both files or the docs drift (no protoc in repo)"
    - "the `packages.Load` path in mcp/archtest/protocol_version_test ↔ the changed golden file names — the SCAN must keep loading the same package; only prose changes"
    - "the agents marker-block text ↔ the byte contract — constant bytes change only in text, never here"
---

<objective>
Complete the CODE-01 sweep of the remaining in-tree comment surfaces — `internal/agents`, `internal/cli` non-migrate rows, `internal/githooks` / `internal/gitmeta` / `internal/watch`, the remaining `internal/indexer` rows, `internal/corpora`, `internal/mcp` stale citations, `internal/schema` proto/pb doc lockstep — plus the D-10 `.claude/CLAUDE.md` core-value reword. Every hit is resolved term-by-term with a recorded reason, never with a find-replace over a bare word (D-02); the comparison-baseline vocabulary goes, the TypeScript-as-indexed-language and past-tense history stay.

Purpose: this is the rest of the "an agent reading the source" surface after plan 05-04 finishes `internal/query`. It reaches into the installer layer, the git hooks layer, the watcher, the schema serialization, and the repo's own CLAUDE.md (the file an agent reads first). The two tricky couplings are (a) schema doc comments that must stay in lockstep across proto/pb (no protoc pipeline) and (b) the mcp archtest scan, whose runtime path must not change even though its prose mentions a renamed golden file.

Output: the swept internal packages read as own-terms behavioral descriptions; `.claude/CLAUDE.md` describes the project on its own terms; the schema field comments no longer say "Go parity".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-process-ci-in-tree-sweep/05-CONTEXT.md
@.planning/phases/05-process-ci-in-tree-sweep/05-RESEARCH.md

# The census is the work list (research §4.2-§4.7)
@internal/agents (research §4.2)
@internal/cli (§4.3)
@internal/githooks/gitmeta/watch (§4.4)
@internal/indexer + corpora (§4.5)
@internal/mcp (§4.6) + internal/schema (graph.proto/graph.pb.go)
@.claude/CLAUDE.md (D-10 rows)
</context>

<tasks>

<task type="auto">
  <name>internal/agents — de-frame the installer comments (parity table, parity oracle, marker-contract prose)</name>
  <files>internal/agents/instructions.go, internal/agents/gemini.go, internal/agents/hermes.go, internal/agents/hermes_test.go, internal/agents/kiro.go, internal/agents/registry.go, internal/agents/registry_test.go, internal/agents/toml.go, internal/agents/testhelpers_test.go, internal/agents/types.go</files>
  <read_first>
    internal/agents/instructions.go — the "Corrected Per-Agent Parity Table" heading (:16), the hard cross-implementation contract paragraph (:7-10), and the past-tense history row (:19) that will be KEEP
    internal/agents/toml.go (:6-10 re: TS parity oracle's toml.ts), types.go (:58 per-file action enum), testhelpers_test.go (:13)
    internal/agents/registry.go (:70), hermes.go (:12/:21), kiro.go (:11)
    internal/agents/registry_test.go (:182 the cross-implementation parity contract row), hermes_test.go (:313 'parity regression' row)
  </read_first>
  <action>
    Reword every framing row in the ten agents files to own-terms wording, keeping the byte-format contract and the past-tense history:

    1. instructions.go:16 — "Corrected Per-Agent Parity Table" -> "Per-agent install-coverage table" (matching the table's real content: which agent gets an instructions file).
    2. instructions.go:7-10 — the paragraph saying "a Go uninstall must recognize a marker block a TS install wrote, and vice versa" — keep the byte-format contract, but reword to "an uninstall must recognize the marker block an earlier install wrote, regardless of which agent wrote it" (the "TS install wrote" is framing; the marker format is the project's own stable contract).
    3. The "TS parity oracle" family — toml.go:6/:8, types.go:58, testhelpers_test.go:13, registry.go:70, gemini.go/kiro.go /hermes.go rows — every "mirrors the TS parity oracle's own X" -> "uses the same per-agent X the marker contract defines" (there is no second implementation in the truth; the correspondence is to the installed marker surface).
    4. hermes_test.go:313 "parity regression" -> "marker regression".
    5. instructions.go:19 past-tense history row (refering to the playbook TS removed in #559-22-04) — KEEP the statement (past-tense history, D-02), record the reason in the SUMMARY. Do not reword historical fact.
    Do NOT touch the constant bodies (codegraphSectionStart/End strings stay byte-identical — they are the installed contract D-01a).
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -c -e "parity" -e "Parity" -e "a TS install" internal/agents/ | awk -F: "{s+=$2} END {print s+0}"); echo "framing rows left in agents: $n"; test "$n" = "0"; go test -count=1 ./internal/agents/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'parity|Parity|TS-install|cross-implementation parity' internal/agents/` returns zero aggregate
    - marker-block bytes unchanged (`git diff internal/groups/...` shows only comment lines, no string changes in `codegraphSectionStart`/`End`)
    - the past-tense row (#… PR note) is intact and the SUMMARY records its KEEP reason
    - `go test -count=1 ./internal/agents/...` green
  </acceptance_criteria>
  <done>The installer package comments describe the marker contract and install-coverage on the project's own terms; parity/oracle vocabulary gone; the byte-format contract and history intact.</done>
</task>

<task type="auto">
  <name>cli + githooks + gitmeta + watch + indexer: resolve the remaining framing rows package by package</name>
  <files>internal/cli/explore.go, internal/cli/daemon.go, internal/cli/status.go, internal/cli/githooks.go, internal/cli/init.go, internal/cli/serve_test.go, internal/cli/affected_test.go, internal/cli/install_test.go, internal/cli/present/status_test.go, internal/cli/man.go, internal/githooks/githooks.go, internal/githooks/githooks_test.go (framing rows only), internal/gitmeta/detect.go, internal/gitmeta/githooks.go, internal/gitmeta/notice.go, internal/gitmeta/worktree.go, internal/watch/policy.go</files>
  <read_first>
    internal/cli/explore.go (:30 WorktreeNotice nil-safe row, :55-60 stdout-parity row), daemon.go (:44-45 TS name-collision prior-art note), status.go (:28 the golden-parity oracle in the --json comment), githooks.go (:15 the "Go-only surface extension over internal/githooks — TS 1.3.1 has no standalone githooks command" comment — review H4), init.go (:123 offerWatchFallback "port" claim), serve_test.go (:141-164 the verbatim TS-parity disabled-message rows), affected_test.go (:246 TS-parity glob row), install_test.go (:328 parity-fallback row), present/status_test.go (:103 structural-parity row)
    internal/githooks/githooks.go (:2 package-doc "verbatim port of the TS" sync/git-hooks.js, :22-24 "marker markers are verbatim TS bytes", :198/:309/:408/:410)
    internal/gitmeta/… detect.go (:18), notice.go (:7-16), worktree.go (:11-26), githooks.go (:14)
    internal/watch/policy.go (:62-66 :86 :109-112 :125 :156)
  </read_first>
  <action>
    Sweep the rows above row-by-row, per research §4.3 and §4.4:

    - cli: "stdout parity", "for verbatim TS parity", "golden-parity oracle" -> the natural own-terms wording (e.g. status.go drops "golden-parity oracle" from the "CLI --json contract" comment; serve_test.go descriptions of the disabled-message become "the documented disabled message" and keep the assertion as-is; affected_test.go says "the filepath.Match glob semantics"). Change only prose.
    - cli/githooks.go:15 (review H4, verified present) — the sentence "This is a Go-only surface extension over internal/githooks — TS 1.3.1 has no standalone githooks command" keeps the real fact (githooks is a project-native command wrapping internal/githooks) and drops the origin comparison: e.g. "githooks is a project-native command surface wrapping internal/githooks" (or the natural own-terms version you choose).
    - man.go:14 "Go-only surface extension (D-01)" — the sentence is a surface claim; record a KEEP reason if it states the repo's reality without comparison, and if it names the prior project's absence, replace with "no other command provides this" and record.
    - githooks/githooks.go: "verbatim port of the TS sync/git-hooks.js" -> "implements the hooking contract"; "marker bytes are the documented marker" (keep the byte contract, drop the origin); githooks_test.go framing rows become "behavioral test" rows.
    - gitmeta: "Ported verbatim from TS sync/...js" -> "implements the same worktree/git-hook semantics"; the divergence-from-TS notes become divergence-from-prior-behavior wording.
    - watch/policy.go: "Ported verbatim from TS's '//'" -> "implements the same WSL mount-host normalization"; "TS's --no-watch" -> "the documented CODEGRAPH_NO_WATCH flag"; "verbatim TS strings" -> "the documented human-readable messages".
    There are no runtime changes: the WSL2 path regexes, the glob rules, the hook markers, and the env flag names stay byte-identical. Record each reworded row in the SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -c -e "ported verbatim from TS" -e "TS parity" -e "TS-parity" -e "Golden parity" -e "for parity" -e "verbatim port of" internal/cli internal/githooks internal/gitmeta internal/watch | awk -F: "{s+=$2} END {print s+0}"); echo "framing rows left: $n"; test "$n" = "0"; go test -count=1 ./internal/cli/... ./internal/githooks/... ./internal/gitmeta/... ./internal/watch/... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - the aggregate grep of the seven framing phrase families over the four packages returns zero
    - diff review shows no runtime/assert/flag change — only comments
    - `go test -count=1 ./internal/cli/... ./internal/githooks/... ./internal/gitmeta/... ./internal/watch/...` green
    - the SUMMARY records each row rewrote + the man.go claim row reason
  </acceptance_criteria>
  <done>cli/githooks/gitmeta/watch comments read on the project's own terms; hook behavior, message texts, glob/timeout rule-sets unchanged; tests green.</done>
</task>

<task type="auto">
  <name>indexer + corpora + mcp probes + schema .proto lockstep + .claude/CLAUDE.md core-value</name>
  <files>internal/indexer/nodeid/nodeid_test.go, internal/indexer/resolve.go, internal/indexer/routes/walk.go, internal/indexer/languages_python.go, internal/indexer/pyextract/types.go, internal/indexer/mainstream/phpextract/resolution_test.go, internal/corpora/record.go, internal/mcp/archtest/protocol_version_test.go, internal/mcp/markdown_test.go, internal/schema/graph.proto, internal/schema/graph.pb.go, tools/bench/realcorpus/manifest.go, .claude/CLAUDE.md</files>
  <read_first>
    §4.5 remaining rows — nodeid_test.go:10, resolve.go:630, walk.go:15, pyextract/types.go:74, languages_python.go:39, mainstream/phpextract/resolution_test.go:159, corpora/record.go:104
    §4.7 — tools/bench/realcorpus/manifest.go (:16/:74/:92/:156/:160 stale golden citations, :108 the origin claim); tools/bench/BASELINE.md:241 is KEEP (past-tense bench history)
    §4.6 mcp — mcp/archtest/protocol_version_test.go:56-58 (:252-262 non-code), mcp/markdown_test.go:25-32 stale golden river
    §4.6. schema — the "Go-parity" field comments proto:36/:41/:46/:51/:56/:92 ↔ pb.go:54/:58/:62/:66/:70/:234 lockstep rows
    .claude/CLAUDE.md — the core-value sentence (:9), the parity command surface (:35), the modernc migration-reader tableNote (if present)
  </read_first>
  <action>

    1. indexer/corpora rows: fix every reference to the old test file name (e.g. "testdata/golden/parity_tsjs_test.go", "D-12 golden-parity diff") onto the actual current names; reword "TS-parity/Golden parity" phrases in code comments on the project's own terms while leaving nodeid's SHA output and matching behavior unchanged.
    2. mcp archtest: change only the PROSE of the two comments (protocol_version_test.go:56/:250 and markdown_test.go:25/:28/:32) that cite "golden_parity_test.go" — re-point to the current suite file name — do NOT change the packages.Load runtime line; the scan keeps scanning the same package (research §4.6).
    3. schema: replace the "Go-parity" field header comments with plain behavioral first lines in BOTH graph.proto (:36/:41/:46/:51/:56/:92) and graph.pb.go (:54/:58/:62/:66/:70/:234) in the same edit per field (the two files must stay in lockstep; there is no protoc in this repo — research Pitfall 3). Only the comment sentences change; field numbers/types/tags stay identical.
    4. tools/bench/realcorpus/manifest.go: re-point the stale `golden_parity_test.go` citations (:16/:74/:92/:156/:160) to the current golden/behavioral-file names, and reword the origin claim at :108 ("the original TS CodeGraph project this Go port replaces") to describe the pinned real corpus on its own terms — the manifest's pinned rows and Resolve behavior stay untouched (Phase 6 owns decoupling). tools/bench/BASELINE.md:241 is KEEP (past-tense bench history), recorded in the SUMMARY.
    5. .claude/CLAUDE.md: rewrite the core-value sentence (D-10) — from "uninstall TS CodeGraph / install the Go binary / migrate their indexes / works the same or better" to a definition of the project's own value ("a code knowledge graph with a pre-indexed query surface..."); drop the parity-command-surface phrase (incl. the migrate entry) and the migration-reader/modernc tech-stack rows (capability gone, D-04). Keep the tech-stack language rows (TypeScript-as-indexed-language) intact. Also address the review-LOW rows: the "One-way migration from its SQLite format still binds" sentence (line 15, adjacent to the compatibility line) is SWEPT with the migrate removal — the migration contract no longer exists post-CODE-03, so the sentence is not a past-tense record but a live false claim; and if a "TS CodeGraph v1.3.x ... baseline" sentence is present in the tech-note, record either a KEEP reason (it documents the retired compatibility ruling, past-tense-adjacent) or sweep it to "prior-compat behavior was a guide, not a contract" at your discretion — record the choice in the SUMMARY.
    - If a corpus-source reword is needed (corpus/behavioral/src/** comments) — that is D-07 and belongs to plan 05-06 (with the golden re-freeze); THIS plan does NOT reword corpus src comments nor touch the frozen goldens.
  </action>
  <verify>
    <automated>bash -c 'set -e; n=$(rg -c -e "Go-parity" -e "golden-parity" -e "parity_tsjs_test" -e "parity_ts_test" internal/indexer internal/corpora internal/mcp internal/schema --glob "!internal/indexer/capability/**" | awk -F: "{s+=$2} END {print s+0}"); echo "framing rows (indexer minus capability, which 05-04 wave-2 owns): $n"; test "$n" = "0"; m=$(rg -c -e "golden_parity_test.go" -e "the original TS CodeGraph" tools/bench/realcorpus/manifest.go | awk -F: "{s+=$2} END {print s+0}"); echo "manifest stale refs: $m"; test "$m" = "0"; go test -count=1 ./internal/indexer/... >/dev/null && go test -count=1 ./internal/mcp/... >/dev/null && go test -count=1 ./internal/schema/... >/dev/null && go test -count=1 ./tools/bench/realcorpus/... >/dev/null; c=$(rg -c -e "uninstall TS CodeGraph" -e "works the same" -e "migrate their indexes" -e "parity command surface" -e "One-way migration from its SQLite format still binds" .claude/CLAUDE.md | awk -F: "{s+=$2} END {print s+0}"); echo "claude framing rows: $c"; test "$c" = "0"'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Go-parity|golden-parity|parity_ts' internal/indexer --glob '!internal/indexer/capability/**' internal/corpora internal/mcp internal/schema` returns zero — the indexer scan EXCLUDES `capability/` because its golden-parity rows are owned by wave-2 plan 05-04 (review H2-2: this wave-1 gate must be passable at its wave)
    - `rg -n 'golden_parity_test.go|the original TS CodeGraph project' tools/bench/realcorpus/manifest.go` returns zero (stale path + origin framing gone; the pinned manifest rows stay intact for Phase 6)
    - `go test -count=1 ./internal/indexer/... ./internal/mcp/... ./internal/schema/... ./tools/bench/realcorpus/...` green
    - the two schema files' matching field-doc comments read as matching pairs (same text between graph.proto and graph.pb.go)
    - `.claude/CLAUDE.md` no longer contains "uninstall TS CodeGraph", "works the same", "migrate their indexes", the parity-command-surface line, or "One-way migration from its SQLite format still binds"; the migration-reader/modernc tech-stack rows are gone; the TS/TSX/JS indexed-language rows intact; the "TS CodeGraph v1.3.x baseline" sentence is either swept or recorded KEEP with its reason in the SUMMARY
  </acceptance_criteria>
  <done>index/corpora/mcp/schema comment rows and the .claude core-value sentence now read on the project's own terms; the schema doc remains locked in tandem; the MCP runtime scan path untouched; tests green.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| agents comments | edited | the ten internal/agents files de-framed (parity-table title, parity-oracle rows, contract prose) |
| cli + githooks + gitmeta + watch | edited | internal/cli non-migrate rows (incl. githooks.go:15, install_test.go:328 — review H4) + internal/git* + watch/policy.go on own terms |
| indexer rows | edited | nodeid_test, resolve, walk, languages_python, pyextract/types, phpextract rows |
| internal/corpora/record.go | edited | stale parity_tsjs_test citation re-pointed |
| internal/mcp prose | edited | the archtest + golden_parity tests comment stale-path fixed, runtime scan untouched |
| schema graph.proto + graph.pb.go | edited (lockstep) | "Go parity" comments both sides, matched text, no protoc |
| tools/bench/realcorpus/manifest.go | edited | stale golden citations re-pointed + origin claim reworded; pinned rows intact (Phase 6 decouples) |
| .claude/CLAUDE.md | edited | core value + parity-command-surface + migrate/modernc rows removed (D-10) |

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema .proto ↔ graph.pb.go | comment drift across the pair is invisible to the test suite (no protoc, no doc-reg knowledge); an editor that changes once side breaks the doc contract |
| mcp scan prose ↔ packages.Load path | changing the scan runtime (the path string) would break the archtest scan even though the golden filename in the prose changed |
| agents marker bytes | the installed contract must stay byte-identical while the comments around it de-frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------------|-----------------|
| T-05-17 | Tampering | schema doc comments drift (proto/pb mislock) | high | mitigate | the task edits BOTH files in the same commit — every change is a side-by-side paired change with a diff check at verify |
| T-05-18 | Tampering | mcp archtest packages.Load edited instead of prose | high | mitigate | the action says do-not-touch-$load; verify greps it out (expected literal unchanged) |
| T-05-19 | Tampering | agents byte contract mutated | high | mitigate | only comments change; the marker constant bodies stay byte-identical, asserted by the marker tests in internal/agents |
| T-05-20 | DoS | the sweep de-listing a capability (tsextract/languages) | high | mitigate | product surface (tsextract, language registries, capability matrix) stays untouched — no name or listing deletion; verify runs the indexer + query tests green |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | no new deps this phase; comments/strings only |

<verification>
- `go test -count=1 ./internal/agents/... ./internal/cli/... ./internal/githooks/... ./internal/gitmeta/... ./internal/watch/... ./internal/indexer/... ./internal/mcp/... ./internal/schema/... ./internal/corpora/... ./tools/bench/realcorpus/...` — green
- term-family greps over the swept packages return zero (positive-controlled per family)
- the two schema files' matching field-doc comments are identical after the paired edit
- `.claude/CLAUDE.md` no longer carries the "uninstall TS CodeGraph / migrate their indexes / works the same" sentence, the parity-command-surface phrase, the migration-reader (modernc) rows, or the "One-way migration ... still binds" claim
- A3 retained-use inventory (phase-wide): run `rg -n -e "TS" -e "parity" -e "port" -e "original" -e "upstream"` over `internal/ tools/ test/` and classify EVERY hit as IMPORTANT (indexed-language rows, in-repo pin guards, past-tense history, dataflow terms) or REMAINING (retired framing) in the SUMMARY. Any REMAINING hit outside the recorded KEEP set is a census miss to fix here — this is the positive inventory that proves "nothing left referencing" (review actionable 3) instead of an absence-based grep
</verification>

<success_criteria>
- No comparison framing remains in internal/{agents,cli (incl. githooks.go:15 + install_test),githooks,gitmeta,watch,indexer,corpora,mcp,schema} code comments; the retained-use inventory classifies every remaining occurrence with a reason
- The stale golden-file references in indexer/mcp/corpora/manifest are re-pointed to the current file names
- `go test -count=1 ./...` green (with 05-01 removing the gone migrate module)
- `.claude/CLAUDE.md` describes the project on its own terms and no longer references the removed migrate capability; its compatibility-constraint sentence is either swept or its KEEP reason recorded
</success_criteria>

<output>
Create `.planning/phases/05-process-ci-in-tree-sweep/05-05-SUMMARY.md` when done
</output>