---
phase: 02-golden-harness-re-authoring-re-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/golden/golden_parity_test.go
  - testdata/golden/parity_java_test.go
  - testdata/golden/parity_tsjs_test.go
  - testdata/golden/parity_csharp_test.go
  - testdata/golden/parity_python_test.go
  - testdata/golden/README.md
  - internal/indexer/capability/matrix_test.go
  - internal/indexer/capability/matrix.go
  - docs/LANGUAGE-CAPABILITY-MATRIX.md
  - internal/corpora/coverage_test.go
autonomous: true
requirements: [CODE-02]
user_setup: []
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "No comparison-framing test identifier survives: TestGoldenParity*, TestGoldenBehavioral*, and the parity_*_test.go filenames are renamed to behavioral vocabulary (D-01/D-02)."
    - "The matrix gate and the rename land in the same commit: internal/indexer/capability/matrix_test.go goldenTestFuncsByLanguage maps every priority-4 language to a declared renamed test, and internal/indexer/capability/matrix.go doc strings + docs/LANGUAGE-CAPABILITY-MATRIX.md gap strings carry the same names (constraint 1, TestMatrix_FullPriority4EntriesHaveGoldenTest + TestMatrix_DocMirrorsDescriptor stay green)."
    - "The rename moves zero golden bytes: no file under testdata/golden/corpus/**/*.json appears in the diff, and all five go test invocations pass."
    - "go test -count=1 ./... AND go test -count=1 ./testdata/golden/... both exit 0 (CODE-02 criterion 1)."
    - "The oracle is exact byte-equality: goldens are frozen as bytes and compared byte-for-byte, never as graphemes or normalized forms (CODE-02/encoding)."
    - "Mid-transition determinism: because this diff moves no golden byte, any test run interleaved between this rename diff and the later re-freeze diff is green; the two diffs never co-modify the same golden file (CODE-02/concurrency)."
  artifacts:
    - testdata/golden/behavioral_test.go (renamed from golden_parity_test.go)
    - testdata/golden/behavioral_java_test.go (renamed from parity_java_test.go)
    - testdata/golden/behavioral_tsjs_test.go (renamed from parity_tsjs_test.go)
    - testdata/golden/behavioral_csharp_test.go (renamed from parity_csharp_test.go)
    - testdata/golden/behavioral_python_test.go (renamed from parity_python_test.go)
  key_links:
    - "matrix_test.go goldenTestFuncsByLanguage <-> the declared TestCorpusBehavior_<lang> functions in testdata/golden/ (TestMatrix_FullPriority4EntriesHaveGoldenTest)"
    - "matrix.go gap strings <-> docs/LANGUAGE-CAPABILITY-MATRIX.md gap strings (TestMatrix_DocMirrorsDescriptor verbatim check)"
  prohibitions:
    - "Never touch tools/bench/realcorpus, bench.yml, tools/bench/headtohead-*.json, docs/BENCHMARKS.md, the repo-root README bench table or its origin attribution, or NOTICE (T-02-01, D-08) — this diff must leave every file under tools/bench/ and NOTICE byte-identical."
    - "Never delete or re-word ts-schema.sql / ts-schema.dump.sql / ts-version.txt — they are legacy-index migration ground truth (CODE-03), not comparison framing."
    - "Never put [ci skip] / [skip ci] in any commit message (rule f18zrdsgx5)."
---

<objective>
Re-author the golden suite's identifiers from comparison framing to behavioral vocabulary — the first of D-06's two reviewed diffs, which changes no golden byte. Every parity-named test file and `TestGoldenParity*` / `TestGoldenBehavioral*` function is renamed (D-01/D-02), and every mechanical dependent that reads those names — the capability matrix gate, its doc strings, the doc-mirror, and stale references in testdata/golden/README.md and internal/corpora/coverage_test.go — is carried in the SAME commit so `go test ./...` stays green (constraint 1).

Purpose: This is the tracer slice. It proves, end-to-end on the earliest slice, the phase's defining architectural claim: that the golden harness can be re-authored in identity (file names + function names + every gate that references them) while moving zero golden bytes and keeping every test green. Renaming the identifiers in isolation is impossible — the capability-matrix gate (`TestMatrix_FullPriority4EntriesHaveGoldenTest`) and the doc-mirror gate (`TestMatrix_DocMirrorsDescriptor`) fail on a stale name — so the rename and its dependents are one atomic diff. Landing it first makes the later re-freeze diff (which must change no identifier) attributable: any regression from here onward is either the rename (this diff) or the re-baseline (diff 2), never both.

Output: Five renamed golden test files; the capability matrix gate + doc strings + doc-mirror synchronized; zero golden bytes changed.

IMPORTANT — this plan touches ONLY identifiers, file names, and the references that name them. It does NOT delete the TS-era capture path (FIXT-04, plan 02-02), does NOT move the behavioral corpus or author CASES.json (FIXT-05, plan 02-02), and does NOT re-point tests to the locked corpora or change any assertion body (D-09/D-10, plans 02-03/02-04). Test bodies, corpus fixtures, and gocapture are left byte-identical here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-CONTEXT.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-RESEARCH.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-PATTERNS.md

# Prior-plan context (only the rename's forced dependents, which this diff couples to)
@testdata/golden/golden_parity_test.go
@testdata/golden/golden_test.go
@testdata/golden/README.md
@internal/indexer/capability/matrix_test.go
@internal/indexer/capability/matrix.go
@docs/LANGUAGE-CAPABILITY-MATRIX.md
@internal/corpora/coverage_test.go
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Rename the golden harness identifiers to behavioral vocabulary, carrying every gate that names them in one atomic diff</name>
  <files>
    testdata/golden/golden_parity_test.go -> testdata/golden/behavioral_test.go
    testdata/golden/parity_java_test.go -> testdata/golden/behavioral_java_test.go
    testdata/golden/parity_tsjs_test.go -> testdata/golden/behavioral_tsjs_test.go
    testdata/golden/parity_csharp_test.go -> testdata/golden/behavioral_csharp_test.go
    testdata/golden/parity_python_test.go -> testdata/golden/behavioral_python_test.go
    internal/indexer/capability/matrix_test.go
    internal/indexer/capability/matrix.go
    docs/LANGUAGE-CAPABILITY-MATRIX.md
    testdata/golden/README.md
    internal/corpora/coverage_test.go
  </files>
  <read_first>
    testdata/golden/golden_parity_test.go
    testdata/golden/parity_java_test.go
    testdata/golden/parity_tsjs_test.go
    testdata/golden/parity_csharp_test.go
    testdata/golden/parity_python_test.go
    internal/indexer/capability/matrix_test.go   # goldenTestFuncsByLanguage (lines 172-180), TestMatrix_FullPriority4EntriesHaveGoldenTest (223-238), TestMatrix_DocMirrorsDescriptor (113-165)
    internal/indexer/capability/matrix.go        # doc strings mentioning TestGoldenParity_Java/_CSharp/_Python/_TSJS (lines 113,124,136,147,158,170)
    docs/LANGUAGE-CAPABILITY-MATRIX.md           # gap bullets that mirror matrix.go gap strings verbatim
    internal/corpora/coverage_test.go            # stale "golden_parity_test.go" doc-comment reference (line 7)
    testdata/golden/README.md                     # test-name references in the corpus/provenance tables
  </read_first>
  <action>
    This is ONE atomic commit: renames files and function identifiers AND updates every mechanical dependent, because `TestMatrix_FullPriority4EntriesHaveGoldenTest` (internal/indexer/capability/matrix_test.go:223-238) parses every `testdata/golden/*_test.go` via go/parser and fails unless `goldenTestFuncsByLanguage` (172-180) maps each priority-4 language to a function that is actually declared there. Splitting the rename from the matrix update across commits (or plans) lands a red `go test ./...` — that is exactly constraint 1.

    File renames (D-01/D-02, `git mv` so history and zero golden bytes are preserved):
    - golden_parity_test.go -> behavioral_test.go
    - parity_java_test.go -> behavioral_java_test.go
    - parity_tsjs_test.go -> behavioral_tsjs_test.go
    - parity_csharp_test.go -> behavioral_csharp_test.go
    - parity_python_test.go -> behavioral_python_test.go

    Identifier renames inside the moved files (this diff renames ONLY identifiers — NO assertion body, NO fixture read, NO goto a different corpus changes):
    - `TestGoldenParity` -> `TestCorpusBehavior_Go` (in behavioral_test.go)
    - `TestGoldenBehavioralSyntheticParity` -> `TestCorpusBehaviorSynthetic` (behavioral_test.go)
    - `TestGoldenBehavioralRealCorpora` -> `TestCorpusBehaviorLockedCorpora` (behavioral_test.go)
    - `TestGoldenParity_Java` -> `TestCorpusBehavior_Java` (behavioral_java_test.go)
    - `TestGoldenParity_CSharp` -> `TestCorpusBehavior_CSharp` (behavioral_csharp_test.go)
    - `TestGoldenParity_Python` -> `TestCorpusBehavior_Python` (behavioral_python_test.go)
    - `TestGoldenParity_TSJS` -> `TestCorpusBehavior_TSJS` (behavioral_tsjs_test.go)
    - The self-consistency trio `TestExploreCLIMatchesMCP` / `TestNodeCLIMatchesMCP` / `TestNodeLineHintCLIMatchesMCP` KEEP their names — they assert CLI==MCP byte identity, a self-consistency oracle with no TS comparison target (D-02 / research Pattern 1).
    - Update every in-file self-reference: package doc comments naming the old test, `t.Run` nesting that embeds a renamed name, and any identifier string that is itself the old function name. Where a doc comment inside a moved file references the old test name, rename it in place (cosmetic comment edits ride the rename diff per CONTEXT.md Claude's Discretion).
    - Do NOT rename the corpus-resolution helpers here (resolveWeftCorpus, resolveColbymchenryCorpus, resolveWeftGoCorpusLoose, defaultWeftRepo, syntheticParitySrc, pinnedWeftCommit): those are retired or re-pointed in plan 02-02 (deletion) and 02-03 (locked-corpus resolution). Their names are not the CODE-02 comparison-framing surface; leave their identifiers and bodies untouched in this diff so the tracer stays a pure identifier move.

    Forced dependent updates, ALL in this same commit (constraint 1 + the doc-mirror gate):
    - internal/indexer/capability/matrix_test.go `goldenTestFuncsByLanguage` (172-180): remap to the renamed functions: "go"->TestCorpusBehavior_Go, "java"->TestCorpusBehavior_Java, "csharp"->TestCorpusBehavior_CSharp, "python"->TestCorpusBehavior_Python, "typescript"/"tsx"/"javascript"->TestCorpusBehavior_TSJS.
    - internal/indexer/capability/matrix.go: in the gap strings at lines 113 (java), 124 (csharp), 136 (python), 147 (typescript), 158 (tsx), 170 (javascript) that name "TestGoldenParity_Java/_CSharp/_Python/_TSJS", rename the test identifier to TestCorpusBehavior_<Lang>; leave the surrounding gap prose untouched (broader de-framing of the matrix doc is Phase 4 / CODE-01).
    - docs/LANGUAGE-CAPABILITY-MATRIX.md: update only the gap bullets that mirror matrix.go's renamed gap strings, so `TestMatrix_DocMirrorsDescriptor` (matrix_test.go:158-164) finds each renamed gap verbatim. Do not re-write the document's framing here; that is Phase 4 (DOCS-03).
    - testdata/golden/README.md: rename any mention of the old test function / file names to the new behavioral names. Do not re-author the corpus/provenance narrative here (that is plan 02-02).
    - internal/corpora/coverage_test.go: fix the stale doc-comment reference to "testdata/golden/golden_parity_test.go" (line 7) to the renamed "behavioral_test.go" so the comment does not name a file that no longer exists (research Pitfall 3 — this is a cosmetic comment fix riding the rename, not an `internal/` framing sweep; leave all other internal/ comments to Phase 4).

    Hard constraints for this diff:
    - ZERO golden-byte change: no file under testdata/golden/corpus/** changes content, is added, or is removed.
    - ZERO assertion-body change: you rename identifiers and comments; you do not re-point, delete, or rewrite any `loadGoldenFixture*`, `resolveWeft*`, subset-vs-TS, or shape check.
    - Do NOT touch: ts-schema.sql, ts-schema.dump.sql, ts-version.txt, testdata/golden/golden_test.go, testdata/golden/capture.sh, testdata/golden/mcp-capture.mjs, testdata/golden/gocapture/main.go, any file under tools/bench/, bench.yml, NOTICE.
    - No `[ci skip]` / `[skip ci]` (rule f18zrdsgx5); every test command carries `-count=1`.

    Commit (one atomic commit — the rename + its forced dependents): conventional `test(02-01):` message naming the single cause: golden harness identifiers renamed to behavioral vocabulary (CODE-02), with the matrix gate, its doc strings, the doc-mirror, README and coverage_test references carried in the same change (constraint 1).
  </action>
  <verify>
    <automated>git diff --name-only -- 'testdata/golden/corpus/**/*.json' HEAD | wc -l | grep -x 0 && go test -count=1 ./... && go test -count=1 ./testdata/golden/...</automated>
  </verify>
  <done>
    - All five golden test files renamed to behavioral_*_test.go, with no parity file left under testdata/golden/.
    - `go test -count=1 ./...` AND `go test -count=1 ./testdata/golden/...` both exit 0.
    - `git show --stat` on the commit lists the five file moves + the four forced-dependent files; it lists no path under testdata/golden/corpus/.
    - internal/indexer/capability/matrix_test.go goldenTestFuncsByLanguage references TestCorpusBehavior_Go/_Java/_CSharp/_Python/_TSJS; matrix.go + docs/LANGUAGE-CAPABILITY-MATRIX.md carry the matching names; TestMatrix_FullPriority4EntriesHaveGoldenTest and TestMatrix_DocMirrorsDescriptor pass.
    - No `[ci skip]` / `[skip ci]` in the commit message (rule f18zrdsgx5).
  </done>
  <acceptance_criteria>
    - `rg "TestGoldenParity|TestGoldenBehavioral" testdata/golden/ internal/indexer/capability/ docs/LANGUAGE-CAPABILITY-MATRIX.md` returns no hits.
    - `find testdata/golden -name 'parity_*_test.go' -o -name 'golden_parity_test.go'` returns nothing.
    - `go test -count=1 ./internal/indexer/capability/... -run TestMatrix` passes (both the full-priority gate and the doc-mirror gate).
    - `git diff --name-only HEAD~1 HEAD -- testdata/golden/corpus/` is empty (no golden byte touched).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root -> tools/bench + NOTICE | This rename diff must not sweep the retained bench corpus pin or the colbymchenry MIT attribution in NOTICE (licence, not framing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | sweep scope vs tools/bench + NOTICE | high | mitigate | Diff is scoped to testdata/golden/ + the four named forced dependents; verify block asserts no path under tools/bench/, bench.yml, NOTICE or repo-root README bench table changes. Documented boundary (D-08, Pitfall 2), no automated cross-file guard needed. |
| T-02-02 | Spoofing/supply-chain | capture path | medium | mitigate | This diff cannot re-add a TS/capture dependency (it renames identifiers only); gocapture is untouched here and is extended on the Go-side path in 02-03. No install/network step in this plan. |
| T-02-03 | Tampering (integrity) | golden bytes | high | mitigate | The rename diff is byte-preserving: the verify gate fails the diff if any corpus .json path appears, and capture-to-temp-then-move (which 02-03 adds) is not yet in play. |
| T-02-04 | Tampering | corpus resolution | low | accept | Not touched in this identifier-only diff; integrity verification of fetched corpora is owned by 02-03/02-04 via `task corpora:assert` (four-part). |
| T-02-SC | Tampering | go.mod / installs | low | accept | No package is added or removed this plan (gocapture depends only on in-repo packages). Package-legitimacy gate not triggered. |
</threat_model>

<verification>
[Phase-level checks this plan satisfies]
- CODE-02 criterion 1's rename half: no parity-named test file or function survives, and `go test -count=1 ./...` + `go test -count=1 ./testdata/golden/...` both pass.
- Criterion 2's first half: this is the rename diff — it changes no golden byte and no assertion body, every changed line attributable to "identifier de-framing" (constraint 3 / D-06).
- The BYTE-EQUALITY oracle (not grapheme equality) governs the goldens' interpretation (COVERAGE/encoding), satisfied by keeping golden bytes byte-identical through the diff.
</verification>

<success_criteria>
[Measurable completion]
- All five golden test files renamed; zero parity-named identifiers remain in the golden harness.
- The capability-matrix gate, its doc strings, and the doc-mirror all reference the renamed behavioral functions and pass.
- Zero golden bytes changed; zero assertion bodies changed; nothing under tools/bench/, NOTICE, or bench.yml touched.
- The full and golden test suites both green with `-count=1`.
</success_criteria>

<output>
Create `.planning/phases/02-golden-harness-re-authoring-re-freeze/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase plan produces

- **Renamed files** (git mv, zero byte change): `testdata/golden/behavioral_test.go`, `testdata/golden/behavioral_java_test.go`, `testdata/golden/behavioral_tsjs_test.go`, `testdata/golden/behavioral_csharp_test.go`, `testdata/golden/behavioral_python_test.go`.
- **Renamed test functions**: `TestCorpusBehavior_Go`, `TestCorpusBehavior_Java`, `TestCorpusBehavior_CSharp`, `TestCorpusBehavior_Python`, `TestCorpusBehavior_TSJS`, `TestCorpusBehaviorSynthetic`, `TestCorpusBehaviorLockedCorpora`. (Kept: `TestExploreCLIMatchesMCP`, `TestNodeCLIMatchesMCP`, `TestNodeLineHintCLIMatchesMCP`.)
- **Modified**: `internal/indexer/capability/matrix_test.go` (goldenTestFuncsByLanguage map), `internal/indexer/capability/matrix.go` (gap doc strings), `docs/LANGUAGE-CAPABILITY-MATRIX.md` (mirrored gap strings), `testdata/golden/README.md` (test-name refs), `internal/corpora/coverage_test.go` (stale comment).
- **Unchanged (deliberately):** `testdata/golden/golden_test.go`, `testdata/golden/capture.sh`, `testdata/golden/mcp-capture.mjs`, `testdata/golden/gocapture/main.go`, `ts-schema.*`, `ts-version.txt`, all corpus `.json` fixtures, all `tools/bench/**`, `NOTICE`.
