---
phase: 02-golden-harness-re-authoring-re-freeze
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - testdata/golden/capture.sh
  - testdata/golden/mcp-capture.mjs
  - testdata/golden/corpus/weft-go/
  - testdata/golden/corpus/colbymchenry-codegraph/
  - testdata/golden/corpus/synthetic-parity/
  - testdata/golden/behavioral_test.go
  - testdata/golden/golden_test.go
  - testdata/golden/gocapture/main.go
  - testdata/golden/README.md
  - testdata/golden/behavioral_java_test.go
  - testdata/golden/behavioral_tsjs_test.go
  - testdata/golden/behavioral_csharp_test.go
  - testdata/golden/behavioral_python_test.go
  - corpus/behavioral/CASES.json
  - corpus/behavioral/src/
  - corpus/behavioral/src/go.mod
  - corpus/behavioral/go-explore-multi.json
  - corpus/behavioral/go-node-multi.json
  - internal/corpora/coverage_test.go
  - internal/query/explore_test.go
  - internal/query/render_markdown_test.go
  - internal/query/traverse_test.go
autonomous: true
requirements: [CODE-02, FIXT-04, FIXT-05]
user_setup: []
estimate:
  tokens: 115000
  raw_tokens: 57500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The TS-era capture path is gone: capture.sh and mcp-capture.mjs are deleted, and nothing in the tree references them or the weft-go / colbymchenry-codegraph corpora (FIXT-04)."
    - "The purpose-built behavioral corpus survives at corpus/behavioral/ with its src/ tree, its two go-captured goldens, and a committed case map CASES.json naming all four cases verbatim (FIXT-05, D-03/D-04)."
    - "Every targeted behavioral case is exercised by a named test that loads CASES.json and asserts the case against live output in the D-09 property-assertion style, not by byte-diffing a frozen golden (FIXT-05, D-09)."
    - "go test -count=1 ./... AND go test -count=1 ./testdata/golden/... both pass after the deletion + move + re-author (CODE-02 criterion 1)."
    - "A corpus case that legitimately yields zero output still produces a non-empty {command,output} JSON envelope that the existence/volatility guard checks; the oracle is envelope byte-integrity, not non-empty output everywhere (COVERAGE/empty, backstop)."
    - "Moved fixtures are byte-identical: go-explore-multi.json and go-node-multi.json keep their exact bytes across the move from testdata/golden/corpus/synthetic-parity/ to corpus/behavioral/ (FIXT-05)."
    - "The framing leak is closed for the identifiers/paths/names the move actually controls: corpus/behavioral/src/go.mod's module line no longer reads 'synthetic-parity', and no file/dir NAME under corpus/ and no CASES.json identifier carries framing. The framing gate is scoped to those identifiers/paths/names + authored data (module line, CASES.json), NOT to golden content (MED: corpus/behavioral/src/go.mod module synthetic-parity; H2-1)."
    - "The two moved goldens' embedded `-p synthetic-parity` command-field byte is PARKED, explicitly OUT of 02-02's framing gate (the byte-identical move cannot legally change it and re-capture/editing it here would break the D-06 two-diff attribution). It is retired by 02-04's re-freeze, which re-captures `command` from gocapture's new spec name (H2-1). Rule: a framing gate checks identifiers/paths/names; a golden-content gate checks the re-frozen output; never conflate the two."
    - "Every live test reader of the three deleted paths is re-pointed in this same diff: internal/query/explore_test.go (copies synthetic-parity/src -> corpus/behavioral/src), internal/query/render_markdown_test.go (loads weft-go/explore.json -> corpus/behavioral/go-explore-multi.json), and internal/query/traverse_test.go (comment-only weft-go/impact.json reference -> neutralized), so go test ./... stays green at Wave 2 (H1). The only remaining internal/ refs are doc-comment prose in internal/query/{traverse,status,render_markdown}.go, which Phase 4 / CODE-01 owns."
    - "This diff is a cleanup + byte-identical move, NOT a re-baseline: it deletes TS-era captured fixtures (removal) and moves two go-* goldens byte-identically (rename); it RE-CAPTURES or ALTERS no golden byte. No new golden byte is produced here — the re-freeze (02-04) is the only net-new-byte producer (MED: 02-02 'zero golden byte' classification)."
  artifacts:
    - corpus/behavioral/CASES.json (new, D-04 case map for the four cases, cases c/d now carry query + assertion-mode)
    - corpus/behavioral/src/ (moved, .codegraph/ excluded) and corpus/behavioral/src/go.mod (module line renamed off 'synthetic-parity')
    - corpus/behavioral/go-explore-multi.json, corpus/behavioral/go-node-multi.json (moved byte-identical)
    - TestCorpusBehaviorSynthetic (re-authored, D-09 property style — case (d) asserts the surfacing contract TestExploreStructuralBeatsLexical documents, H4)
    - TestCorpusBehaviorLockedCorpora (scaffold; locked-corpus subtests land in 02-03)
  key_links:
    - "CASES.json <-> the loader helper in behavioral_test.go (mirrors internal/corpora/coverage_test.go committed-doc loading shape)"
    - "corpus/behavioral/src <-> syntheticParitySpec.resolveSource in gocapture/main.go (re-pointed, H2 path fix) and TestGoSideFixturesRegenerated in golden_test.go (re-pointed) and copySyntheticParityFixture in internal/query/explore_test.go (re-pointed, H1)"
  prohibitions:
    - "Never delete or re-word ts-schema.sql / ts-schema.dump.sql / ts-version.txt — legacy-index migration ground truth (CODE-03)."
    - "Never touch tools/bench/realcorpus, bench.yml, tools/bench/headtohead-*.json, docs/BENCHMARKS.md, repo-root README bench table / origin attribution, or NOTICE (T-02-01, D-08) — a wide delete/re-grep of colbymchenry must NOT sweep the retained bench corpus."
    - "Never re-add a TS/capture dependency or re-execute unvetted binaries (T-02-02); gocapture is the only capture path going forward."
    - "Do not add any t.Skip for a missing corpus; the migrated behavioral corpus tests resolve in-repo and must run (constraint 2). ts-schema remains untouched."
    - "Do not RE-CAPTURE or ALTER a golden byte in this diff: TS-era fixtures are DELETED and the two go-* goldens are MOVED byte-identically. Anything that produces new golden bytes belongs to 02-04."
---

<objective>
Complete the first reviewed diff (D-06) by retiring the TS-era capture path and deleting the weft-go / colbymchenry-codegraph corpora and every reference to them (FIXT-04, D-07), moving the purpose-built behavioral corpus to an in-tree `corpus/behavioral/` with its case map committed as `CASES.json` (FIXT-05, D-03/D-04), and re-authoring the synthetic behavioral test to the D-09 property-assertion style (assert named behavioral properties of live output; goldens become regression snapshots, not the primary oracle). This is the second half of the rename/delete/move diff — it continues to change ZERO golden bytes (it moves two behavioral goldens byte-identically and deletes TS-era fixtures; it does not re-capture anything).

Purpose: FIXT-04 removes the TS-CodeGraph-heightened capture path the milestone exists to retire; FIXT-05 guarantees the four purpose-built behavioral cases survive the rename — the case map, not the directory name, is the acceptance artifact (ROADMAP Notes). Re-authoring TestCorpusBehaviorSynthetic to property assertions (D-09) decouples the suite's strongest oracle from its own captured bytes, so normal indexer evolution no longer forces a golden re-freeze for the behavioral corpus.

CLASSIFICATION (MED: 02-02 'zero golden byte' — reframed): this plan is a cleanup + move diff, NOT a re-baseline. It DELETES the TS-era captured fixtures (four synthetic-parity JSON + weft-go + colbymchenry-codegraph, all removals), MOVES two go-* goldens byte-identically (pure rename), and RE-CAPTURES or ALTERS nothing. Where an earlier draft said "changes zero golden bytes", the honest invariant is: this diff introduces NO net-new golden byte and re-freezes nothing — the only net-new-byte producer in the phase is the 02-04 re-freeze. Every changed line here traces to one of {delete TS-era fixture, move behavioral golden byte-identically, re-point a reader, rename an identifier}.

Output: A tree with the TS-era capture path and external corpora gone, a committed authored behavioral corpus with a data-driven case map whose four cases each carry a query + assertion mode, a property-based behavioral test green against live output, and every internal/query test reader re-pointed so go test ./... stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-CONTEXT.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-RESEARCH.md
@.planning/phases/02-golden-harness-re-authoring-re-freeze/02-PATTERNS.md

# Prior-plan SUMMARY context (only what this plan couples to)
@.planning/phases/01-corpus-selection-by-measurement/01-06-SUMMARY.md   # locked set the re-freeze (02-03/04, not here) freezes against

# Surfaces this plan rewrites / deletes
@testdata/golden/capture.sh
@testdata/golden/mcp-capture.mjs
@testdata/golden/golden_parity_test.go -> behavioral_test.go
@testdata/golden/golden_test.go
@testdata/golden/gocapture/main.go
@testdata/golden/README.md
@testdata/golden/corpus/synthetic-parity/README.md   # the verbatim a/b/c/d case map -> CASES.json
@internal/corpora/coverage_test.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the TS-era capture path and the weft-go / colbymchenry-codegraph corpora, and retire every in-tree reference to them</name>
  <files>
    testdata/golden/capture.sh
    testdata/golden/mcp-capture.mjs
    testdata/golden/corpus/weft-go/ (15 files, all)
    testdata/golden/corpus/colbymchenry-codegraph/ (14 files, all)
    testdata/golden/behavioral_test.go
    testdata/golden/golden_test.go
    testdata/golden/gocapture/main.go
    testdata/golden/README.md
    internal/query/explore_test.go
    internal/query/render_markdown_test.go
    internal/query/traverse_test.go
  </files>
  <read_first>
    testdata/golden/capture.sh
    testdata/golden/mcp-capture.mjs
    testdata/golden/behavioral_test.go   # resolver funcs resolveWeftCorpus/resolveWeftGoCorpusLoose/resolveColbymchenryCorpus/defaultWeftRepo/pinnedWeftCommit (lines 61-71,110-162,240-271) and TestCorpusBehavior_Go / TestCorpusBehaviorLockedCorpora weft fixture reads
    testdata/golden/golden_test.go       # TestGoldenFixturesExist glob + volatile guard; TestGoSideFixturesRegenerated
    testdata/golden/gocapture/main.go    # weftGoSpec (224-244), colbymchenryCodegraphSpec (249-271), specs slice (99-103)
    internal/query/explore_test.go        # H1: copySyntheticParityFixture (lines 33-71) + syntheticParityEngine (76-...) read testdata/golden/corpus/synthetic-parity/src
    internal/query/render_markdown_test.go # H1: loadGoldenOutput (line 23-39) loads testdata/golden/corpus/weft-go/<name>; call at line 207 loadGoldenOutput("explore.json")
    internal/query/traverse_test.go        # H1: comment-only ref to weft-go/impact.json (line ~503)
  </read_first>
  <action>
    Per D-07, exclude scope: delete `testdata/golden/capture.sh`, `testdata/golden/mcp-capture.mjs`, and the entire directories `testdata/golden/corpus/weft-go/` and `testdata/golden/corpus/colbymchenry-codegraph/` (git rm -r). Then retire EVERY in-tree reference so nothing dangles:

    In testdata/golden/behavioral_test.go (the renamed golden_parity_test.go):
    - Delete the resolver functions that locate the deleted corpora: `resolveWeftCorpus`, `resolveWeftGoCorpusLoose`, `resolveColbymchenryCorpus`, `defaultWeftRepo`, `dirExists`, and the `pinnedWeftCommit` / `corpusCandidate` constants/types that only they use. These are FIXT-04 deletion scope — the corpora they resolve no longer exist.
    - In `TestCorpusBehavior_Go` (was TestGoldenParity): delete every subtest that reads a deleted weft fixture via `loadGoldenFixture`/`loadGoldenOutput`/`loadGoldenFixtureIn` for the weft corpus (the status.json/query.json/callers.json/callees.json/impact.json/explore.json/node.json key-loop subtests) and the `resolveWeftCorpus` engine setup. KEEP the self-standing, non-fixture property assertions — e.g. `status.Backend == "pebble"` and the `--json`/`edgesByKind`/`filesByLanguage` key-presence assertions that assert on Go engine output directly (these carry D-09 property style and remain named `TestCorpusBehavior_Go` so the matrix "go" gate stays green).
    - In `TestCorpusBehaviorLockedCorpora` (was TestGoldenBehavioralRealCorpora): delete the "weft-go" and "colbymchenry-codegraph" `t.Run` subtests that resolve the deleted corpora (buildEngineAt over resolveWeftGoCorpusLoose / resolveColbymchenryCorpus and their explore/node-multi shape checks). Leave the outer function declared but empty-body after the weft subtests are removed; plan 02-03 fills it with locked-corpus subtests. Do NOT add any `t.Skip`.
    - In the CLI==MCP trio case lists (`TestExploreCLIMatchesMCP` / `TestNodeCLIMatchesMCP` case slices that gate on `os.Getenv("WEFT_REPO")` / `dirExists(defaultWeftRepo())`): remove the weft-go rows and the env-gating `if` that guards them (the retention of those case rows is FIXT-04 "nothing left referencing weft" scope). Keep the synthetic-corpus rows.
    - Delete any doc comments whose subject is the deleted corpora or the TS incremental process (carry the cosmetic-comment edit in this diff).

    In testdata/golden/gocapture/main.go:
    - Delete `weftGoSpec` and `colbymchenryCodegraphSpec` and remove them from the `specs` slice in main() (lines 99-103), leaving `syntheticParitySpec` in place. Remove the now-unused `os/exec` import if `git clone` was its only use.

    In internal/query/ — the LIVE TEST READERS this deletion breaks (H1, source-verified): deleting the three paths without re-pointing them makes `go test ./...` fail at Wave 2. Re-point each in this same diff:
    - internal/query/explore_test.go: `copySyntheticParityFixture` (lines 33-71) resolves `filepath.Join("..","..","testdata","golden","corpus","synthetic-parity","src")`; change it to the moved in-repo corpus `filepath.Join("..","..","corpus","behavioral","src")` (top-level `corpus/behavioral/`, per D-03). Update its doc comment and any other in-file `synthetic-parity` references (`syntheticParityEngine` stays — it copies the re-pointed source). Confirmed via repo sweep: this is the only other reader of synthetic-parity/src.
    - internal/query/render_markdown_test.go: `loadGoldenOutput` (lines 23-39) resolves `filepath.Join("..","..","testdata","golden","corpus","weft-go", name)`, called with "explore.json" at line 207 (the TestExplore disclaimer-verbatim subtest). Re-point the base to the moved Go-produced golden: load `filepath.Join("..","..","corpus","behavioral","go-explore-multi.json")` — an in-repo behavior golden that exists at this plan and carries the static D-05a explore disclaimer. Update the call site's `name` argument accordingly (e.g. load by the specific path or change the name to "go-explore-multi.json" with the base set to corpus/behavioral).
    - internal/query/traverse_test.go: line ~503 is a comment-only reference to `weft-go/impact.json`'s arithmetic; neutralize/re-point the comment (a documented constant or a neutral "a committed golden" wording) — cosmetic comment fix, no assertion change.
    - The remaining internal/ refs (internal/query/{traverse,status,render_markdown}.go doc comments) are Phase-4 / CODE-01 prose, explicitly deferred here; they do not break compilation and are out of Phase-2 scope.

    In testdata/golden/golden_test.go:
    - `TestGoldenFixturesExist`: the glob `corpus/*/*.json` and the volatile-field check will now FAIL with "no corpus/*/*.json fixtures found" because the only surviving corpus fixtures are re-pointed to the top-level `corpus/behavioral/` (handled in Task 2 of this plan). See Task 2 — the glob is extended to cover `../../corpus/behavioral/*.json` in the same deletion diff so no test goes red.

    In testdata/golden/README.md:
    - Remove the weft-go and colbymchenry-codegraph rows/tables and any prose describing them as live corpora; re-author the corpus table to name only corpus/behavioral + the (Phase-2-later) locked corpora without comparison framing. Delete the weft provenance entries.

    D-08 boundary — do NOT touch (a wide delete or re-grep must not sweep these): `tools/bench/realcorpus/manifest.go` + manifest_test.go, `bench.yml`, `tools/bench/headtohead-*.json`, `docs/BENCHMARKS.md`, repo-root README (its bench table and colbymchenry origin attribution), and `NOTICE` (colbymchenry MIT attribution MUST remain — licence, not framing). Also leave `ts-schema.sql` / `ts-schema.dump.sql` / `ts-version.txt` untouched. Any colbymchenry string that survives in `tools/bench/**` or `NOTICE` is intentional and retained.

    No `[ci skip]` / `[skip ci]` (rule f18zrdsgx5); every test command carries `-count=1`.

    Commit: conventional `test(02-02):` message naming the single cause (FIXT-04): TS-era capture path and weft-go / colbymchenry-codegraph corpora deleted, references retired.
  </action>
  <verify>
    <automated>rg -i "weft|colbymchenry|mcp-capture|capture\.sh" testdata/golden/ | grep -v -E "ts-schema" | wc -l | grep -x 0 && go test -count=1 ./internal/query/... && go test -count=1 ./...</automated>
  </verify>
  <done>
    - capture.sh, mcp-capture.mjs, weft-go/, colbymchenry-codegraph/ are absent from the tree.
    - `go test -count=1 ./internal/query/...`, `go test -count=1 ./...` and `go test -count=1 ./testdata/golden/...` all exit 0 — the internal/query readers (explore_test.go / render_markdown_test.go) are re-pointed, traverse_test.go comment neutralized (H1).
    - No reference to weft, colbymchenry, mcp-capture, or capture.sh remains under testdata/golden/.
    - tools/bench/** , bench.yml, NOTICE, repo-root README bench table + origin attribution are byte-identical (git diff empty for those paths).
  </done>
  <acceptance_criteria>
    - `test ! -e testdata/golden/capture.sh && test ! -e testdata/golden/mcp-capture.mjs && test ! -d testdata/golden/corpus/weft-go && test ! -d testdata/golden/corpus/colbymchenry-codegraph`.
    - `rg -n "testdata/golden/corpus/(synthetic-parity|weft-go|colbymchenry-codegraph)" internal/query/explore_test.go internal/query/render_markdown_test.go` returns no hits (paths fully re-pointed).
    - `go test -count=1 ./internal/query/... -run 'TestExploreStructuralBeatsLexical|TestNode|TestExplore' -v` passes against the re-pointed corpus/behavioral/src.
    - `git diff --name-only HEAD -- tools/bench/ NOTICE bench.yml 'README.md'` is empty.
    - `go test -count=1 ./internal/corpora/...` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Move the behavioral corpus to corpus/behavioral/, author the CASES.json case map, and re-point every reader</name>
  <files>
    corpus/behavioral/CASES.json
    corpus/behavioral/src/ (moved from testdata/golden/corpus/synthetic-parity/src/, .codegraph/ excluded)
    corpus/behavioral/go-explore-multi.json
    corpus/behavioral/go-node-multi.json
    testdata/golden/golden_test.go
    testdata/golden/gocapture/main.go
    testdata/golden/README.md
    testdata/golden/corpus/synthetic-parity/ (deleted after move; the 4 TS-captured fixtures explore-multi/node-multi/explore-mcp/node-mcp + README.md are deleted, NOT moved)
  </files>
  <read_first>
    testdata/golden/corpus/synthetic-parity/README.md   # the verbatim a/b/c/d case map (source for CASES.json)
    testdata/golden/golden_test.go          # TestGoSideFixturesRegenerated (line ~158) + TestGoldenFixturesExist glob
    testdata/golden/gocapture/main.go       # syntheticParitySpec src literal (276-290) + goldenDir/corpusDir derivation (96-97)
    internal/corpora/coverage_test.go       # TestCorpusCoverageClaim (123-152) — the committed-doc loading shape CASES.json loading mirrors
  </read_first>
  <action>
    Per D-03/D-04/FIXT-05: move the purpose-built corpus to a committed top-level `corpus/behavioral/`, deliberately distinct from Phase-1's `corpora/` (which is the pinned third-party fetch authority).

    - `git mv` the following from `testdata/golden/corpus/synthetic-parity/` to `corpus/behavioral/`, preserving bytes exactly:
      - `src/` (the Go module with accounts/{validate.go,manager.go}, orders/validate.go, recovery/{recovery.go,recovery_test.go}, ledger/ledger.go, go.mod). EXCLUDE `src/.codegraph/` from the move (it is gitignored local index data — per research Runtime State Inventory the existing copyDir skips `.codegraph`; excluding it here is equivalent). Confirm via `git check-ignore testdata/golden/corpus/synthetic-parity/src/.codegraph` that it is ignored, then delete it rather than moving it.
      - `go-explore-multi.json`, `go-node-multi.json` (the two Go-captured behavioral goldens, moved byte-identical).
    - DELETE (do not move) the four TS-captured fixtures `explore-multi.json`, `node-multi.json`, `explore-mcp.json`, `node-mcp.json`, and the old `README.md`, per FIXT-04/D-07 — the re-authored behavioral test (Task 3) is property-based and no longer byte-diffs these; the re-freeze (02-03/02-04) re-captures the CLI/MCP explore+node surfaces for corpus/behavioral/.
    - Author `corpus/behavioral/CASES.json` (D-04): transcribe the four-case map verbatim from the synthetic-parity README (research Code Examples 293-313) into the schema the tests will load, PLUS the query and assertion-mode each case needs so the test asserts the named behavior rather than guessing args (MED: cases c/d under-specified). Each case carries `query` and `assertion` fields. The JSON MUST name all four cases with symbols, files, query, and assertion: (a) overloaded-same-named-symbols: symbol "Validate", files accounts/validate.go + orders/validate.go, assertion-mode "overloaded-defs-distinct"; (b) multi-word-query: symbol "UserAccountManager", file accounts/manager.go, query "user account", assertion-mode "multi-word-tokenization"; (c) test-heavy-weakly-connected-cluster: zero-edge "TestAccountRecovery" (recovery/recovery_test.go) vs connected recoverAccount/validateRecovery (recovery/recovery.go), query "user account", assertion-mode "cluster-surfaces-connected-non-test"; (d) structural-beats-lexical: isolated "AccountBalanceHelper" vs structurally-central "ReconcileLedger" / bridge "GetBalance" (ledger/ledger.go), query "account balance", assertion-mode "structural-surfaces-zero-lexical-match". Exact schema shape is planner discretion inside D-04, but it must be loadable as one committed JSON document, carry each case's symbols/files/query/assertion (analog: corpora/manifest.json + coverage_test.go committed-doc loading), and be the sole source the Task-3 test derives every symbol, file, and query from (D-04).
    - Add a loader helper in testdata/golden/ (behavioral_test.go, same package) that reads `../../corpus/behavioral/CASES.json` relative to the test's working dir — `filepath.Join("..", "..", "corpus", "behavioral", "CASES.json")` — and decodes the case list, mirroring the committed-doc loading shape of internal/corpora/coverage_test.go (`corpora.Load(filepath.Join("..","..","corpora","manifest.json"))`). A new typed struct (`type behavioralCase struct { ID, Name, What, Symbol, File, Query string; ZeroEdge string; Connected []string; Files []string }` shape is discretion) + a `loadBehavioralCases(t *testing.T) []behavioralCase`. The Task 3 test uses it; a later case author edits data, not test code.
    - Re-point all remaining readers to the new top-level path, in this same diff:
      - `testdata/golden/golden_test.go`: `TestGoSideFixturesRegenerated` (current `filepath.Join("corpus","synthetic-parity",name)`) -> `filepath.Join("..","..","corpus","behavioral",name)` for the two go-* names; `TestGoldenFixturesExist` glob `corpus/*/*.json` -> extend to also glob `../../corpus/behavioral/*.json` (merge both sets) so the existence check + volatile-field check cover the moved behavioral goldens and do not report "no fixtures found".
      - `testdata/golden/gocapture/main.go`: `syntheticParitySpec` (rename to `behavioralCorpusSpec`) `src` literal `filepath.Join(goldenDir,"corpus","synthetic-parity","src")` -> a CORRECT repo-root path (H2). `goldenDir` is `.../testdata/golden`, so it needs TWO `Dir` hops to reach the repo root, NOT one (which would land at `testdata/corpus/behavioral`). Compute `repoRoot := filepath.Dir(filepath.Dir(goldenDir))` once and resolve `filepath.Join(repoRoot, "corpus", "behavioral", "src")`. (Equivalently a tested `repoRoot` helper shared with the resolved-path acceptance `os.Stat` below.) Update the spec's `name` to "behavioral" (the corpus is no longer named synthetic-parity). Do NOT change any other gocapture behavior here (the locked-corpus specs + temp-then-move land in 02-03).
      - `internal/query/explore_test.go` (already re-pointed in Task 1 to `corpus/behavioral/src`) — confirm its resolved path uses the same two-hop repo-root rule; it uses `filepath.Abs(filepath.Join("..","..","corpus","behavioral","src"))` from the `internal/query` package dir, which correctly resolves to the top-level `corpus/behavioral/src` (H2 consistent).
      - `corpus/behavioral/src/go.mod` (MED: module line retains framing): change the module directive from the existing `module synthetic-parity` to a neutral, non-framing path, e.g. `module behavioral` (or any non-comparison path such as `module github.com/seanb4t/codegraph-go/corpus/behavioral`). This closes the framing leak the `rg "parity" testdata/golden/` gate can no longer see once the corpus lives outside testdata/golden/.
      - (H2-1 carve-out — the parked command-field byte.) `go-explore-multi.json` and `go-node-multi.json` embed `-p synthetic-parity` in their `command` field (verified on-disk: `explore "user account" -p synthetic-parity` / `node "Validate" -p synthetic-parity`; gocapture's `writeCapture` builds `command` from `spec.name`, which this diff renames off `synthetic-parity`). They are MOVED byte-identically, so that embedded command-field byte is explicitly OUT of this plan's framing gate — do NOT re-capture or edit these goldens here to "clean" them. Doing so would break the byte-identical move, the `R`-rename acceptance, and the D-06 two-diff attribution (the goldens are a rename-diff artifact). Their command-field framing is retired by 02-04's re-freeze re-capture, which rebuilds `command` from the new spec name. Scope the framing gate to identifiers/paths/names (module line, file/dir names, CASES.json) plus authored data — never the byte content of a moved golden (H2-1).
      - `testdata/golden/README.md`: re-author to a neutral fixture authority — describe `corpus/behavioral/` as the committed authored test input with its CASES.json case map, describe the goldens as the storage mechanism (neutral "golden" concept per D-02), and remove any remaining "live TS / capture.sh / mcp-capture.mjs" capture narrative that survived Task 1. Do not re-describe the locked corpora yet (02-03/04 own those).
      - `internal/corpora/coverage_test.go`: update its doc comment if it references the moved corpus path/name (line ~7 references the golden harness); cosmetic-comment fix only.
    - Confirm the moved goldens are byte-identical: `git diff` on the move shows no content change for go-explore-multi.json / go-node-multi.json (only path moves).
    - No `[ci skip]` / `[skip ci]`; `-count=1` everywhere.

    Commit: conventional `test(02-02):` naming the single cause (FIXT-05/D-03/D-04): behavioral corpus moved to corpus/behavioral with committed CASES.json case map, readers re-pointed.
  </action>
  <verify>
    <automated>test -f corpus/behavioral/CASES.json && test -d corpus/behavioral/src/accounts && ! rg -qi "module synthetic" corpus/behavioral/src/go.mod && ! rg -qi "synthetic-parity" corpus/behavioral/CASES.json && test -z "$(find corpus/behavioral -iname '*synthetic*' -print -quit)" && go test -count=1 ./testdata/golden/... && go test -count=1 ./internal/query/... && go test -count=1 ./...</automated>
  </verify>
  <done>
    - `corpus/behavioral/` exists at the TOP LEVEL (repo root) with `src/`, `CASES.json`, `go-explore-multi.json`, `go-node-multi.json`; `testdata/golden/corpus/synthetic-parity/` is gone. The resolved behavioral path is `corpus/behavioral/src`, NOT `testdata/corpus/behavioral` (H2).
    - CASES.json names all four cases (a/b/c/d) each with symbols + files + a `query` + an `assertion` mode.
    - `corpus/behavioral/src/go.mod` module line is renamed off `synthetic-parity`, and no file/dir NAME under corpus/ and no CASES.json identifier carries framing — the framing gate is scoped to identifiers/paths/names + authored data, NOT to golden content (MED, H2-1). The two moved goldens' embedded `-p synthetic-parity` command field is parked here and retired by 02-04's re-capture.
    - go-explore-multi.json and go-node-multi.json moved with identical bytes (git status shows rename, not modify).
    - Both go test invocations + internal/query green with `-count=1`.
  </done>
  <acceptance_criteria>
    - `python3 -c "import json;d=json.load(open('corpus/behavioral/CASES.json'));assert len(d['cases'])==4;assert all(('query' in c and 'assertion' in c) for c in d['cases'])"` — every case carries a query + assertion mode.
    - `test -d corpus/behavioral/src && test ! -e testdata/corpus/behavioral` — the corpus sits at repo-root `corpus/behavioral`, not the off-by-one `testdata/corpus/behavioral` (H2).
    - `rg -qi "module synthetic" corpus/behavioral/src/go.mod` and `rg -qi "synthetic-parity" corpus/behavioral/CASES.json` return nothing, and `find corpus/behavioral -iname '*synthetic*' -print -quit` is empty — the framing gate is scoped to identifiers/paths/names + authored data (module line, CASES.json case-map identifiers), NOT to golden content (H2-1). The two moved goldens' embedded `-p synthetic-parity` command-field byte is PARKED (out of this gate) and is retired by 02-04's re-freeze re-capture.
    - `git diff --name-status HEAD~1 HEAD` shows `R` (rename) for corpus/behavioral/go-explore-multi.json and go-node-multi.json, not `M`.
    - `go test -count=1 ./internal/query/... -run TestExploreStructuralBeatsLexical -v` passes against the re-pointed corpus/behavioral/src.
    - `go test -count=1 ./testdata/golden/... -run 'TestGoldenFixturesExist|TestGoSideFixturesRegenerated'` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-author TestCorpusBehaviorSynthetic to assert named behavioral properties of live output, driven by CASES.json</name>
  <files>testdata/golden/behavioral_test.go</files>
  <read_first>
    testdata/golden/behavioral_test.go   # current TestCorpusBehaviorSynthetic (was TestGoldenBehavioralSyntheticParity, lines ~1180-1290) + helpers parseNodeMultiDefBlocks/exploreSelectedFiles/locationSet/buildEngineAt/syntheticParitySrc
    internal/query/explore_test.go        # AUTHORITATIVE: TestExploreStructuralBeatsLexical (144-183) — the exact EXPL-02 RWR contract case (d) must match (H4)
    corpus/behavioral/CASES.json           # the four-case loader source (now with per-case query + assertion)
    internal/indexer/routes_detect_test.go  # runAndSnapshot/collectRouteNodes (16-60, 91-97) — the D-09 property-assertion analog
  </read_first>
  <behavior>
    - Test 1: overloaded-same-named-symbols — Node("Validate") over corpus/behavioral/src returns exactly 2 distinct definitions whose locations are accounts/validate.go AND orders/validate.go (overloaded dedup: same-name defs stay distinct, never merged — COVERAGE/FIXT-06 adjacency).
    - Test 2: multi-word-query — Explore("user account") surfaces UserAccountManager and selects accounts/manager.go (EXPL-01 camelCase multi-word tokenization, case b).
    - Test 3: test-heavy-weakly-connected-cluster — Explore("user account") surfaces the structurally-connected non-test symbols recoverAccount/validateRecovery over the zero-inbound TestAccountRecovery; the output carries a "tests: recovery/recovery_test.go" clause for the connected symbol (EXPL-03/04, case c). Parse so "recovery_test.go appears as a selected file / in the tests: clause of the connected symbol" is distinguished from a mere text mention of the symbol (MED: case-c parsing).
    - Test 4: structural-beats-lexical — assert the contract TestExploreStructuralBeatsLexical (internal/query/explore_test.go:144-183) documents, NOT a ranking (H4): Explore("account balance") over corpus/behavioral/src must (a) select ledger/ledger.go, (b) surface GetBalance (the partial-lexical structural bridge) as a matched candidate despite its lower raw gather score than the isolated full-match AccountBalanceHelper, and (c) surface ReconcileLedger (a symbol with ZERO lexical match) purely through structural expansion. A degree-0 node retains 100% of its RWR restart injection, so a connected seed's self-mass CANNOT outrank a same-weighted dangling seed — the property is SURFACING (present and reachable), not strict ranking above AccountBalanceHelper.
    - Loading: the test reads corpus/behavioral/CASES.json via the Task-2 loader and derives the symbols/files/query/assertion per case from data, not from a Go literal table (D-04).
  </behavior>
  <action>
    Replace the byte-diff assertion body of `TestCorpusBehaviorSynthetic` (currently asserts Go output against `loadGoldenOutputIn("synthetic-parity","node-multi.json")` / `explore-multi.json` fixtures) with D-09 property assertions over live output. The goldens (go-node-multi.json / go-explore-multi.json, now under corpus/behavioral/) remain as regression snapshots but are NOT the primary oracle: a failing test names which behavior broke, not merely "a golden diff appeared".
    - Drive the engine with `buildEngineAt(t, corpus/behavioral/src)` (the committed authored corpus; no locked corpora, no network, never `t.Skip` — this corpus is in-repo so the test must always run, constraint 2).
    - Load `corpus/behavioral/CASES.json` via the Task-2 helper; for each case, pick the assertion named by the case's `what`:
      - case a (overloaded-same-named-symbols): `eng.Node(case.Symbol, "", nil)`; assert the definition set (via the existing parseNodeMultiDefBlocks/locationSet) equals exactly {accounts/validate.go, orders/validate.go} — two distinct defs, no merging (property: overloaded dedup keeps same-named symbols distinct).
      - case b (multi-word-query): `eng.Explore(case.Query, 0)`; assert the output header contains the corner symbol UserAccountManager and that exploreSelectedFiles includes accounts/manager.go (property: multi-word camelCase tokenization).
      - case c (test-heavy-weakly-connected-cluster): `eng.Explore(..., 0)` such that the structurally-connected non-test symbol (recoverAccount/validateRecovery) is surfaced with a `tests:`/`recovery_test.go` clause while the zero-inbound TestAccountRecovery is NOT promoted over it (property: file-relevance gate prefers a structurally-connected non-test symbol over a Test* with zero inbound edges).
      - case d (structural-beats-lexical): assert the SURFACING contract from the authoritative `TestExploreStructuralBeatsLexical` (internal/query/explore_test.go:144-183), derived from CASES.json's case-(d) `query` ("account balance") and `assertion` ("structural-surfaces-zero-lexical-match"): `eng.Explore("account balance", <n>)` must (a) select `ledger/ledger.go`, (b) surface `GetBalance` (the partial-lexical structural bridge into ReconcileLedger) as a matched candidate, and (c) surface `ReconcileLedger` (zero lexical match) via structural expansion. Do NOT assert ReconcileLedger "ranks above" AccountBalanceHelper — under this RWR formulation a connected seed's self-mass cannot outrank a same-weighted dangling seed (a degree-0 node retains 100% of its restart injection); the property is reachability/surfacing, exactly as the authoritative test documents (H4).
    - Keep the deterministic-format checks that are pure property (e.g. `eng.Node` header template asserting `**N definitions named "..."**` where N==2 for case a), asserting the named behavioral property rather than a frozen byte-for-byte golden.
    - Delete references to the deleted TS fixtures in this test body (explore-multi.json / node-multi.json are gone from Task 2). If any subtest still decoded a moved golden as its primary assertion, convert it to property assertion; the go-* snapshot may be loaded only as a secondary regression cross-check, never as the pass/fail oracle.
    - Run the whole `TestCorpusBehaviorSynthetic` and confirm it is RED against a seeded wrong behavior first if feasible (mutation of the assertion to expect 1 def should fail), then GREEN. If a mutation-rehearsal is impractical in this task's budget, at minimum confirm the four case assertions each execute (not silently skipped) and pass.
    - No `[ci skip]` / `[skip ci]`; `-count=1`.

    Commit: conventional `test(02-02):` naming the cause (D-09): TestCorpusBehaviorSynthetic re-authored to assert named behavioral properties of live output (CASES.json driven).
  </action>
  <verify>
    <automated>go test -count=1 ./testdata/golden/... -run TestCorpusBehaviorSynthetic -v 2>&1 | grep -q "PASS" && go test -count=1 ./internal/query/... -run TestExploreStructuralBeatsLexical && go test -count=1 ./testdata/golden/...</automated>
  </verify>
  <done>
    - TestCorpusBehaviorSynthetic passes and every one of its case subtests executes (no skip).
    - Each of the four named behavioral properties (overloaded dedup, multi-word query, weakly-connected-cluster preference, structural-surfacing) is asserted directly against live engine output and derived from CASES.json data — case (d) asserts the surfacing contract TestExploreStructuralBeatsLexical documents (H4).
    - `go test -count=1 ./internal/query/... -run TestExploreStructuralBeatsLexical` also passes (the authoritative RWR contract over the re-pointed corpus).
    - `go test -count=1 ./testdata/golden/...` green.
  </done>
  <acceptance_criteria>
    - `go test -count=1 ./testdata/golden/... -run TestCorpusBehaviorSynthetic/case_a -v` reports exactly 2 definitions and both expected locations.
    - The case-(d) subtest asserts none of {ReconcileLedger "ranks above", "outranks", "above AccountBalanceHelper"}; it asserts surfacing of ledger/ledger.go + GetBalance + ReconcileLedger (grep the test body for the ranking-vs-surfacing wording as a negative check).
    - The test body contains no reference to "explore-multi.json" / "node-multi.json" (the deleted TS fixtures) as a load target.
    - `go vet ./testdata/golden/...` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness sweep -> tools/bench + NOTICE | A wide delete/re-grep of "colbymchenry" must not sweep the retained bench corpus pin or NOTICE attribution |
| corpus/behavioral (committed authored input) -> tests | Committed test input is loaded as trusted data by tests; a case map that names a wrong symbol silently weakens the assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | sweep scope vs tools/bench + NOTICE | high | mitigate | Task 1's delete/re-grep is scoped to testdata/golden/ only; the verify block asserts tools/bench/**, bench.yml, NOTICE and repo-root README bench table + origin attribution are byte-identical after the sweep. The colbymchenry MIT attribution in NOTICE is licence, not framing, and must survive (D-08). |
| T-02-02 | Spoofing/supply-chain | capture path | high | mitigate | Deleting capture.sh/mcp-capture.mjs removes the TS/network capture path; gocapture remains the sole Go-side capture authority and is NOT re-linked to any TS dependency here. No new package/install in this plan. |
| T-02-03 | Tampering (integrity) | golden bytes on delete/move | medium | mitigate | Deleted fixtures are TS-era captures retired by D-07; the two moved go-*.json fixtures keep byte-identical content (verify via git rename detection). Re-capture (which 02-03 guardrails via temp-then-move) does not happen in this plan. |
| T-02-04 | Tampering | corpus resolution | low | accept | Not active here (no locked corpora read this plan); 02-03/02-04 verify fetched trees via `task corpora:assert` four-part check before any read. |
| T-02-SC | Tampering | go.mod / installs | low | accept | No dependency added or removed. Package-legitimacy gate not triggered. |
</threat_model>

<verification>
[Phase-level checks this plan satisfies]
- FIXT-04: TS-era capture path + weft/colbymchenry corpora deleted, nothing references them.
- FIXT-05: the four targeted cases survive as named tests driven by the committed corpus/behavioral/CASES.json; no case lost (the case map is the acceptance artifact).
- CODE-02 criterion 1: both go test invocations green at the end of the rename/delete/move diff.
- D-09: the synthetic behavioral test asserts named behavioral properties of live output; goldens are regression snapshots, not the primary oracle.
</verification>

<success_criteria>
[Measurable completion]
- The tree has no capture.sh, mcp-capture.mjs, weft-go, or colbymchenry-codegraph; nothing under testdata/golden/ references them.
- corpus/behavioral/ holds src/, CASES.json, and the two byte-identical go-* goldens; synthetic-parity is gone.
- TestCorpusBehaviorSynthetic passes as a CASES.json-driven property assertion over live output with all four cases exercising named behaviors.
- Both go test invocations green with -count=1.
</success_criteria>

<output>
Create `.planning/phases/02-golden-harness-re-authoring-re-freeze/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase plan produces

- **New files**: `corpus/behavioral/CASES.json` (D-04 case map, four cases, each with query + assertion mode), plus `corpus/behavioral/go-explore-multi.json` / `corpus/behavioral/go-node-multi.json` (moved, byte-identical) and `corpus/behavioral/src/**` (moved, .codegraph/ excluded) with `corpus/behavioral/src/go.mod` module line renamed off `synthetic-parity`.
- **New test symbols**: `TestCorpusBehaviorSynthetic` (re-authored property-assertion body with case (d) matching the TestExploreStructuralBeatsLexical surfacing contract), loader helper `loadBehavioralCases(t) []behavioralCase`, typed `behavioralCase` struct (carries Query + Assertion per case), renamed `behavioralCorpusSpec` (was syntheticParitySpec) in gocapture.
- **Modified (H1, re-pointed so go test ./... stays green)**: `internal/query/explore_test.go` (`copySyntheticParityFixture` src -> `../..`/`corpus/behavioral/src`), `internal/query/render_markdown_test.go` (`loadGoldenOutput` base -> `corpus/behavioral/go-explore-multi.json` for the explore disclaimer), `internal/query/traverse_test.go` (comment-only weft-go reference neutralized).
- **Modified**: `testdata/golden/golden_test.go` (glob + TestGoSideFixturesRegenerated path), `testdata/golden/gocapture/main.go` (spec name/src path, two-hop repo-root fix), `testdata/golden/README.md` (neutral re-author), `internal/corpora/coverage_test.go` (comment).
- **Deleted**: `testdata/golden/capture.sh`, `testdata/golden/mcp-capture.mjs`, `testdata/golden/corpus/weft-go/**`, `testdata/golden/corpus/colbymchenry-codegraph/**`, `testdata/golden/corpus/synthetic-parity/**` (4 TS fixtures + README).
- **Retired identifiers (deleted)**: `resolveWeftCorpus`, `resolveWeftGoCorpusLoose`, `resolveColbymchenryCorpus`, `defaultWeftRepo`, `dirExists`, `pinnedWeftCommit`, `corpusCandidate`, `weftGoSpec`, `colbymchenryCodegraphSpec`.
