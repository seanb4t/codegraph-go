---
phase: 04-attribution-documentation-sweep
plan: 02
type: execute
wave: 2
depends_on: [04-01, 04-03]
files_modified:
  - docs/FLAG-PARITY.md
  - internal/cli/flag_parity_test.go
  - .github/pull_request_template.md
  - .github/PULL_REQUEST_TEMPLATE/feature.md
  - .github/ISSUE_TEMPLATE/enhancement.yml
  - .github/workflows/auto-close-unsolicited-prs.yml
  - internal/mcp/instructions_contract_test.go
  - internal/mcp/tools_schema_drift_test.go
  - internal/cli/man.go
autonomous: true
requirements: [DOCS-02]
user_setup: []

estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/FLAG-PARITY.md and internal/cli/flag_parity_test.go are absent from the tree, and `go test -count=1 ./...` still passes (49 packages) — TestWorkflowRunBodiesInvokeTask and the capability matrix are unaffected because no Taskfile target and no CI workflow step referenced either artifact (research-verified zero references)."
    - "No in-scope file references either deleted artifact, across the FULL identifier family (cycle-1 review finding 2): `FLAG-PARITY`, `flag_parity`, `flag-parity`, `flagParityDocPath`, and `TestFlagParityDocCoversRegisteredFlags` are all swept — NOTICE:25/:33 (by plan 04-01's trim), CONTRIBUTING.md:13 (by plan 04-03's rewrite), and the 9 edit-lines across 7 files this plan owns (.github pull_request_template:37, feature.md:42, enhancement.yml:77, auto-close-unsolicited-prs.yml:91, internal/mcp/instructions_contract_test.go:16/:375, internal/mcp/tools_schema_drift_test.go:36/:39, internal/cli/man.go:27)."
    - "The census correction stands: internal/cli/man.go:27 is an 11th reference site the research's enumerated 10 missed — the positive-controlled full-tree grep is the authority, not the table; the sweep of this plan includes man.go."
    - "The knowing coverage reduction is recorded, not hidden: deleting flag_parity_test.go removes a live drift guard whose replacement (DOCS-05, a self-authored CLI reference with its own guard) is deliberately deferred to a later milestone (D-03)."
    - "Recorded exclusions hold: .planning/**, graphify-out/**, and .claude/** (incl. .claude/worktrees/**) are NOT swept — they are append-only project history, a gitignored generated index, and untracked agent scratch respectively; the sweep grep excludes them explicitly."
    - "The two deleted artifacts' own mutual references are removed by deleting the files wholesale — no in-file cleanup needed inside them."
    - "An executable phase-final gate exists (task 3, cycle-1 review finding 3): after plans 04-01 and 04-03 land (this plan is wave 2 depending on both), it runs the full-tree reference sweep (broadened identifier family) + the exact-phrase greps for ATTR-01/02/DOCS-01/03 + the LICENSE byte-identity check + the live gh api MIT check + go test, and it REPORTS A POSITIVE COUNT (13/13) of what it checked (rule 84d1gfpywd — a negative-only gate that ran zero checks must not pass)."
  artifacts:
    - docs/FLAG-PARITY.md — deleted (the SURF-05 per-command TS 1.3.1 vs Go flag matrix, 320 lines)
    - internal/cli/flag_parity_test.go — deleted (TestFlagParityDocCoversRegisteredFlags, flagParityDocPath const)
    - .github/pull_request_template.md — FLAG-PARITY link removed at :37
    - .github/PULL_REQUEST_TEMPLATE/feature.md — FLAG-PARITY checklist item removed at :42
    - .github/ISSUE_TEMPLATE/enhancement.yml — FLAG-PARITY option label removed at :77
    - .github/workflows/auto-close-unsolicited-prs.yml — FLAG-PARITY link removed from the comment-body string at :91
    - internal/mcp/instructions_contract_test.go — deleted-file name dropped from comments at :16 and :375
    - internal/mcp/tools_schema_drift_test.go — deleted-artifact names dropped from comments at :36 and :39
    - internal/cli/man.go — FLAG-PARITY reference dropped from the doc comment at :27 (census correction)
  key_links:
    - "the phase-final gate (task 3) → the full-tree reference sweep → the exact-phrase greps for plans 04-01/04-03 — the gate re-asserts the OTHER plans' end-state (NOTICE/README/CONTRIBUTING/matrix) with a positive count so the phase goal is enforced by an executable owned step, not prose."
    - "deleted flag_parity_test.go ↔ TestWorkflowRunBodiesInvokeTask — the workflow-shape guard is unaffected because the SOURCE CENSUS shows zero Taskfile targets and zero CI steps referenced the deleted test (research-verified); `go test -count=1 ./...` green confirms the suite stays green, but the specific non-dependency is established by the census, not inferred from the suite (cycle-1 review LOW)."
    - "each swept .github template / workflow comment string ↔ docs/FLAG-PARITY.md — removing the dangling link before the file disappears prevents a broken-reference trap for contributors."
    - "the internal/mcp comment rewrites ↔ the deleted test — the reworded comments must still read correctly and reference the actual surviving guards (TestMCPToolSchemaNumericClaimsMatchEngineConstants, docNamesCompanionsWithoutTheFilter)."
  prohibitions:
    - "no sweeping .planning/**, graphify-out/**, or .claude/** (recorded exclusions)"
    - "no in-scope reference to either deleted artifact left in the tree after this plan"
    - "no bundling the deletion with attribution (04-01) or doc-sweep (04-03) edits — one named cause per diff"
    - "no regex over 'parity'/'original'/'TS' — every edit is a hand-authored one-line reword from the research census"
    - "no Taskfile.yml or CI step changes (research verified zero references — do not invent any)"
---

<objective>
Delete `docs/FLAG-PARITY.md` and `internal/cli/flag_parity_test.go` (DOCS-02) and sweep every in-scope reference to either — the 9 edit-lines across 7 files this plan owns directly, with the NOTICE sites (plan 04-01) and the CONTRIBUTING site (plan 04-03) handled by their owning plans, then run an executable phase-final gate (task 3) that re-asserts the whole phase's end-state after plans 04-01 and 04-03 land.

Purpose: The flag-parity doc and its drift guard are comparison-framed by construction (a per-command TS 1.3.1 vs Go matrix plus a guard that keeps the doc honest). Deleting them removes the phase's largest framing surface. The deletion is safe because research verified zero Taskfile targets, zero CI steps, and zero other tests depend on either artifact — only the reference sites enumerated here need mechanical cleanup. This is a knowing, recorded reduction in flag-documentation coverage (DOCS-05 deferred), not an oversight (D-03). Because this plan is wave 2 (depends_on [04-01, 04-03]), the NOTICE and CONTRIBUTING sites are already swept when task 2 runs, and task 3's phase-final gate can assert the full cross-plan end-state with a positive count (cycle-1 review findings 2 and 3).

Output: both files gone, the 9 edit-lines across 7 owned files clean, an executable phase-final gate with a positive assertion count, and `go test -count=1 ./...` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attribution-documentation-sweep/04-CONTEXT.md
@.planning/phases/04-attribution-documentation-sweep/04-RESEARCH.md
@.planning/phases/04-attribution-documentation-sweep/04-VALIDATION.md

# The two deletion targets (read in full first)
@docs/FLAG-PARITY.md (320 lines, read in full — the SURF-05 per-command matrix)
@internal/cli/flag_parity_test.go (71 lines, read in full — TestFlagParityDocCoversRegisteredFlags + flagParityDocPath const)

# The reference sites this plan sweeps
@.github/pull_request_template.md (:37)
@.github/PULL_REQUEST_TEMPLATE/feature.md (:42)
@.github/ISSUE_TEMPLATE/enhancement.yml (:77)
@.github/workflows/auto-close-unsolicited-prs.yml (:91)
@internal/mcp/instructions_contract_test.go (:16, :375)
@internal/mcp/tools_schema_drift_test.go (:36, :39)
@internal/cli/man.go (:27 — the 11th site, a census correction)

# The unaffected guard this deletion must not break
@internal/upgrade/taskfile_shape_test.go (TestWorkflowRunBodiesInvokeTask at :1345 — reads workflow YAML + Taskfile bodies; neither references the deleted artifact)
</context>

<tasks>

<task type="auto">
  <name>Delete docs/FLAG-PARITY.md and internal/cli/flag_parity_test.go</name>
  <files>docs/FLAG-PARITY.md, internal/cli/flag_parity_test.go</files>
  <read_first>
    docs/FLAG-PARITY.md — read in full (the SURF-05 per-command TS 1.3.1 vs Go flag/default/status matrix, comparison-framed by construction)
    internal/cli/flag_parity_test.go — read in full (TestFlagParityDocCoversRegisteredFlags walks newRootCmd() and asserts every registered long flag name appears in docs/FLAG-PARITY.md; fail-closed on a missing doc)
  </read_first>
  <action>
    Delete both files with `git rm docs/FLAG-PARITY.md internal/cli/flag_parity_test.go`. Do NOT create replacements — docs/CLI-REFERENCE.md (DOCS-05) is deliberately deferred to a later milestone and is out of scope here (D-03). Do NOT touch Taskfile.yml or any CI workflow (research verified zero references — the only CI artifact was the auto-close comment string, which is swept in task 2). Do NOT run a regex over the deleted files' content — they are removed wholesale.

    Record the knowing coverage reduction in the SUMMARY: deleting flag_parity_test.go removes the live drift guard that asserted every registered flag appears in a doc; its replacement DOCS-05 is deferred, so flag-documentation coverage is intentionally reduced this milestone — a recorded decision, not an oversight.

    After deletion, `CGO_ENABLED=1 go test -count=1 ./...` must pass — this proves TestWorkflowRunBodiesInvokeTask (which reads workflow YAML + Taskfile bodies) and the capability matrix are unaffected, exactly as research verified.
  </action>
  <verify>
    <automated>bash -c 'set -e; test ! -e docs/FLAG-PARITY.md; test ! -e internal/cli/flag_parity_test.go; test -z "$(git ls-files docs/FLAG-PARITY.md internal/cli/flag_parity_test.go)"; CGO_ENABLED=1 go test -count=1 ./... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e docs/FLAG-PARITY.md` passes (file absent)
    - `test ! -e internal/cli/flag_parity_test.go` passes (file absent)
    - `git ls-files docs/FLAG-PARITY.md internal/cli/flag_parity_test.go` returns nothing (both removed from the index, not just the working tree)
    - `CGO_ENABLED=1 go test -count=1 ./...` exits 0 (49 packages — TestWorkflowRunBodiesInvokeTask and the capability matrix survive)
    - the SUMMARY records the DOCS-05 coverage reduction explicitly
  </acceptance_criteria>
  <done>Both deletion targets are gone from the index and the working tree, the full suite is green, and the knowing reduction in flag-documentation coverage (DOCS-05 deferred) is recorded.</done>
</task>

<task type="auto">
  <name>Sweep the 9 edit-lines across 7 owned files — .github templates/workflow + internal/mcp comments + internal/cli/man.go (census correction)</name>
  <files>.github/pull_request_template.md, .github/PULL_REQUEST_TEMPLATE/feature.md, .github/ISSUE_TEMPLATE/enhancement.yml, .github/workflows/auto-close-unsolicited-prs.yml, internal/mcp/instructions_contract_test.go, internal/mcp/tools_schema_drift_test.go, internal/cli/man.go</files>
  <read_first>
    .github/pull_request_template.md — the FLAG-PARITY link at :37 in the "deliberate parity decisions recorded in `docs/FLAG-PARITY.md` or `.planning/`" sentence
    .github/PULL_REQUEST_TEMPLATE/feature.md — the checklist line at :42 "(`docs/FLAG-PARITY.md`, `docs/LANGUAGE-CAPABILITY-MATRIX.md`, README)"
    .github/ISSUE_TEMPLATE/enhancement.yml — the checkbox option label at :77 "I checked whether this is a documented divergence in docs/FLAG-PARITY.md"
    .github/workflows/auto-close-unsolicited-prs.yml — the comment-body string at :91 containing "deliberate parity decisions recorded in `docs/FLAG-PARITY.md` or `.planning/`"
    internal/mcp/instructions_contract_test.go — :16 "Mirrors internal/cli/flag_parity_test.go's flagParityDocPath convention" and :375 "the self-defeat property internal/cli's flag-parity test could only verify by hand"
    internal/mcp/tools_schema_drift_test.go — :36 "updated the CLI help and docs/FLAG-PARITY.md" and :39 "internal/cli's TestFlagParityDocCoversRegisteredFlags structurally cannot catch this"
    internal/cli/man.go — :27 "so the FLAG-PARITY divergence footprint stays one documented hidden command" (the 11th site, missing from the research's enumerated 10)
  </read_first>
  <action>
    Make one-line mechanical rewrites at each site to drop the reference to the deleted artifacts (docs/FLAG-PARITY.md, flag_parity_test.go, TestFlagParityDocCoversRegisteredFlags, flagParityDocPath, "flag-parity test", "FLAG-PARITY divergence footprint"). Do NOT rewrite the surrounding *framing* vocabulary (parity/ports) — that is Phase 5 (PROC-01/02/03, CODE-01). Each edit removes the dangling link/name only:

    1. `.github/pull_request_template.md:37` — change "recorded in `docs/FLAG-PARITY.md` or `.planning/`" to "recorded in `.planning/`" (drop the FLAG-PARITY link; keep "or `.planning/`" semantics by retaining `.planning/`).
    2. `.github/PULL_REQUEST_TEMPLATE/feature.md:42` — change "(`docs/FLAG-PARITY.md`, `docs/LANGUAGE-CAPABILITY-MATRIX.md`, README)" to "(`docs/LANGUAGE-CAPABILITY-MATRIX.md`, README)". The `## Parity` heading and TS-check item above/below are Phase 5 PROC-02 — do not touch them here.
    3. `.github/ISSUE_TEMPLATE/enhancement.yml:77` — change "I checked whether this is a documented divergence in docs/FLAG-PARITY.md" to "I checked whether this is a documented divergence" (drop the link). Phase 5 PROC-01 owns the rest of the template.
    4. `.github/workflows/auto-close-unsolicited-prs.yml:91` — in the comment-body string, change "recorded in `docs/FLAG-PARITY.md` or `.planning/`" to "recorded in `.planning/`". Phase 5 PROC-03 owns the parity/ports vocabulary in this workflow.
    5. `internal/mcp/instructions_contract_test.go:16` — reword "Mirrors internal/cli/flag_parity_test.go's flagParityDocPath convention: a relative path read directly" to reference the surviving convention without the deleted file name, e.g. "Mirrors the internal/cli convention of a relative doc path read directly".
    6. `internal/mcp/instructions_contract_test.go:375` — reword "the self-defeat property internal/cli's flag-parity test could only verify by hand" to drop the deleted-test reference, e.g. "the self-defeat property a hand-verified drift test could only confirm".
    7. `internal/mcp/tools_schema_drift_test.go:36` — change "updated the CLI help and docs/FLAG-PARITY.md" to "updated the CLI help".
    8. `internal/mcp/tools_schema_drift_test.go:39` — change "internal/cli's TestFlagParityDocCoversRegisteredFlags structurally cannot catch this" to "a CLI flag-doc drift guard structurally cannot catch this".
    9. `internal/cli/man.go:27` — reword "so the FLAG-PARITY divergence footprint stays one documented hidden command" to drop the deleted-doc name, e.g. "so the divergence footprint stays one documented hidden command". Record in the SUMMARY that this is a census correction: research enumerated 10 sites but the positive-controlled grep finds an 11th at man.go:27 — the grep is the authority, not the table.

    After the edits, run the positive-controlled full-tree sweep with the recorded exclusions, using the BROADENED identifier family (cycle-1 review finding 2) — `FLAG-PARITY`, `flag_parity`, `flag-parity`, `flagParityDocPath`, `TestFlagParityDocCoversRegisteredFlags`:
    (a) PROVE the grep can find the family: `rg -c -e 'FLAG-PARITY' -e 'flag_parity' -e 'flag-parity' -e 'flagParity' -e 'TestFlagParityDocCoversRegisteredFlags' .github/pull_request_template.md` must return >= 1 BEFORE the edit (or point at any pre-edit file known to contain one).
    (b) RUN the real sweep: `rg -n -e 'FLAG-PARITY' -e 'flag_parity' -e 'flag-parity' -e 'flagParity' -e 'TestFlagParityDocCoversRegisteredFlags' --hidden --no-ignore -g '!.git/**' -g '!.codegraph/**' -g '!dist/**' -g '!.planning/**' -g '!graphify-out/**' -g '!.claude/**' .` — after all three phase plans land, this must be empty. Because this plan is wave 2 (depends_on [04-01, 04-03]), NOTICE and CONTRIBUTING are already swept when this task runs, so the full-tree sweep is asserted now, not deferred. The family now includes the hyphenated `flag-parity` spelling (cycle-2 review finding 1 — the surviving `internal/mcp/instructions_contract_test.go:375` comment says "flag-parity test", which the un-hyphenated patterns did not match).
  </action>
  <verify>
    <automated>bash -c 'set -e; rg -n -e "FLAG-PARITY" -e "flag_parity" -e "flag-parity" -e "flagParity" -e "TestFlagParityDocCoversRegisteredFlags" .github/pull_request_template.md .github/PULL_REQUEST_TEMPLATE/feature.md .github/ISSUE_TEMPLATE/enhancement.yml .github/workflows/auto-close-unsolicited-prs.yml internal/mcp/instructions_contract_test.go internal/mcp/tools_schema_drift_test.go internal/cli/man.go | wc -l | grep -qx "0"; CGO_ENABLED=1 go test -count=1 ./... >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n -e "FLAG-PARITY" -e "flag_parity" -e "flag-parity" -e "flagParity" -e "TestFlagParityDocCoversRegisteredFlags" .github/pull_request_template.md .github/PULL_REQUEST_TEMPLATE/feature.md .github/ISSUE_TEMPLATE/enhancement.yml .github/workflows/auto-close-unsolicited-prs.yml internal/mcp/instructions_contract_test.go internal/mcp/tools_schema_drift_test.go internal/cli/man.go` returns zero hits — the broadened family catches camelCase artifact names like `TestFlagParityDocCoversRegisteredFlags` and `flagParityDocPath` AND the hyphenated `flag-parity` spelling (cycle-2 review finding 1); positive-controlled (step a above proves the grep finds the family when present)
    - `CGO_ENABLED=1 go test -count=1 ./...` exits 0 — the reworded internal/mcp comments compile and the surviving tests (TestMCPToolSchemaNumericClaimsMatchEngineConstants, the README-doc-visibility gates) still pass
    - `git diff -- .github/` shows only the FLAG-PARITY link removed, no parity/framing vocabulary changed (that is Phase 5)
    - `git diff -- internal/mcp/ internal/cli/man.go` shows only comment rewordings, no test logic changed
    - the SUMMARY records the man.go:27 census correction (11th site beyond the research's enumerated 10)
  </acceptance_criteria>
  <done>The 9 edit-lines across 7 owned files carry no mention of either deleted artifact in ANY identifier spelling, the full suite is green with the reworded comments intact, and the census correction (man.go:27) is recorded.</done>
</task>

<task type="auto">
  <name>Phase-final gate — full-tree reference sweep + exact-phrase greps + LICENSE byte-identity + live license check, with a positive count (rule 84d1gfpywd)</name>
  <files>NOTICE, README.md, CONTRIBUTING.md, docs/LANGUAGE-CAPABILITY-MATRIX.md, LICENSE</files>
  <read_first>
    NOTICE — the ATTR-01 end-state (transcription + one origin sentence; argument gone)
    README.md — the ATTR-02 end-state (no Relationship section; one License clause; Performance/Status swept)
    CONTRIBUTING.md — the DOCS-01 end-state (Issue-first rewritten; golden list; Tests paragraph)
    docs/LANGUAGE-CAPABILITY-MATRIX.md — the DOCS-03 end-state (no golden-parity/reference-implementation/weft)
    LICENSE — the ATTR-03 invariant (never edited; byte-identical to HEAD)
  </read_first>
  <action>
    Run the executable phase-final gate that this plan owns (cycle-1 review finding 3). This task runs only after plans 04-01 and 04-03 have landed (this plan is wave 2, depends_on [04-01, 04-03]), so every cross-plan truth is assertable now. The gate verifies the phase's end-state with a POSITIVE COUNT of what it checked (rule 84d1gfpywd — a negative-only gate that ran zero checks must not report success). Run a single bash script that:

    1. Full-tree deleted-artifact sweep (broadened family, recorded exclusions) — must be EMPTY:
       `rg -n -e "FLAG-PARITY" -e "flag_parity" -e "flag-parity" -e "flagParity" -e "TestFlagParityDocCoversRegisteredFlags" --hidden --no-ignore -g '!.git/**' -g '!.codegraph/**' -g '!dist/**' -g '!.planning/**' -g '!graphify-out/**' -g '!.claude/**' .`
       Count the hits; assert == 0.
    2. ATTR-01 NOTICE exact-phrase greps: `rg -c "Copyright \(c\) 2026 Colby Mchenry" NOTICE` == 1; `rg -c "began as a ground-up Go rewrite" NOTICE` == 1; `rg -c "Third-party dependencies" NOTICE` == 1; `rg -n "drop-in|\bported\b|FLAG-PARITY|BENCHMARKS" NOTICE` == 0.
    3. ATTR-02 README exact-phrase greps: `rg -c "Relationship to the original" README.md` == 0; `rg -c "began as a Go rewrite of CodeGraph" README.md` == 1 (the exact mandated clause — cycle-2 review finding 2; 04-01's action mandates this exact text, no discretion); `rg -n "the TS original|it ports|including the flattering one|\bparity\b|divergence" README.md` == 0 (term-scoped `\bparity\b` — cycle-2 review finding 3).
    4. DOCS-01 CONTRIBUTING exact-phrase greps: `rg -n "ports observable behavior|parity|FLAG-PARITY|frozen TypeScript CodeGraph" CONTRIBUTING.md` == 0.
    5. DOCS-03 matrix exact-phrase greps: `rg -n "golden-parity|reference implementation|\bweft\b" docs/LANGUAGE-CAPABILITY-MATRIX.md` == 0.
    6. ATTR-03 LICENSE byte-identity: `git diff --exit-code HEAD -- LICENSE` exits 0 (the working-tree LICENSE is unchanged).
    7. ATTR-03 live hosted license: `gh api repos/seanb4t/codegraph-go/license --jq .license.spdx_id` returns `MIT`.
    8. Full suite: `CGO_ENABLED=1 go test -count=1 ./...` exits 0 (49 packages).
    9. POSITIVE COUNT (rule 84d1gfpywd): accumulate a counter that increments on each successful assertion above and FAIL the gate if the final count is not exactly the expected number (the individual greps in steps 2-5 count as their own assertions — NOTICE 4 + README 3 + CONTRIBUTING 1 + matrix 1 = 9 grep assertions, plus the sweep count (1), the LICENSE diff (1), the live license (1), and the full suite (1) = 13 total). Echo the count: "phase-final gate: N/13 assertions passed" and exit non-zero unless N == 13. A gate that ran zero assertions must fail.
  </action>
  <verify>
    <automated>bash -c 'set -e; n=0; c=$(rg -n -e "FLAG-PARITY" -e "flag_parity" -e "flag-parity" -e "flagParity" -e "TestFlagParityDocCoversRegisteredFlags" --hidden --no-ignore -g "!.git/**" -g "!.codegraph/**" -g "!dist/**" -g "!.planning/**" -g "!graphify-out/**" -g "!.claude/**" . | wc -l | tr -d " "); test "$c" = "0" && n=$((n+1)); test "$(rg -c "Copyright \(c\) 2026 Colby Mchenry" NOTICE)" = "1" && n=$((n+1)); test "$(rg -c "began as a ground-up Go rewrite" NOTICE)" = "1" && n=$((n+1)); test "$(rg -c "Third-party dependencies" NOTICE)" = "1" && n=$((n+1)); test "$(rg -n "drop-in|\bported\b|FLAG-PARITY|BENCHMARKS" NOTICE | wc -l | tr -d " ")" = "0" && n=$((n+1)); test "$(rg -c "Relationship to the original" README.md)" = "0" && n=$((n+1)); test "$(rg -c "began as a Go rewrite of CodeGraph" README.md)" = "1" && n=$((n+1)); test "$(rg -n "the TS original|it ports|including the flattering one|\bparity\b|divergence" README.md | wc -l | tr -d " ")" = "0" && n=$((n+1)); test "$(rg -n "ports observable behavior|parity|FLAG-PARITY|frozen TypeScript CodeGraph" CONTRIBUTING.md | wc -l | tr -d " ")" = "0" && n=$((n+1)); test "$(rg -n "golden-parity|reference implementation|\bweft\b" docs/LANGUAGE-CAPABILITY-MATRIX.md | wc -l | tr -d " ")" = "0" && n=$((n+1)); git diff --exit-code HEAD -- LICENSE && n=$((n+1)); test "$(gh api repos/seanb4t/codegraph-go/license --jq .license.spdx_id)" = "MIT" && n=$((n+1)); CGO_ENABLED=1 go test -count=1 ./... >/dev/null && n=$((n+1)); echo "phase-final gate: ${n}/13 assertions passed"; test "$n" = "13"'</automated>
  </verify>
  <acceptance_criteria>
    - the full-tree sweep (broadened family, recorded exclusions) returns zero hits
    - NOTICE end-state: transcription=1, origin sentence=1, Third-party deps=1, no argument terms=0
    - README end-state: no Relationship section, the one mandated License clause (`rg -c "began as a Go rewrite of CodeGraph" README.md` = 1, cycle-2 finding 2), no framing words incl. term-scoped `\bparity\b` and "divergence"=0 (cycle-2 finding 3)
    - CONTRIBUTING end-state: no ports/parity/FLAG-PARITY/frozen-TS framing=0
    - matrix end-state: no golden-parity/reference-implementation/weft=0
    - `git diff --exit-code HEAD -- LICENSE` exits 0 and `gh api ... --jq .license.spdx_id` returns `MIT`
    - `CGO_ENABLED=1 go test -count=1 ./...` exits 0
    - the gate prints "phase-final gate: 13/13 assertions passed" and exits 0 — a run that checked fewer than 13 things fails (rule 84d1gfpywd)
  </acceptance_criteria>
  <done>The phase-final gate runs all 13 cross-plan assertions after plans 04-01 and 04-03 land, reports a positive count, and fails if any assertion or if the count is short — the phase goal is enforced by an executable owned step, not prose.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Where it lives |
|----------|------|----------------|
| `docs/FLAG-PARITY.md` | deleted file | `docs/` — the SURF-05 per-command TS 1.3.1 vs Go flag/default/status matrix, removed wholesale |
| `internal/cli/flag_parity_test.go` | deleted file | `internal/cli/` — removed wholesale |
| `flagParityDocPath` | deleted symbol | was `internal/cli/flag_parity_test.go:16` (const) — gone with the file |
| `TestFlagParityDocCoversRegisteredFlags` | deleted symbol | was `internal/cli/flag_parity_test.go:40` (test func) — gone with the file |
| `.github/pull_request_template.md` | edited | `:37` — FLAG-PARITY link dropped, `.planning/` retained |
| `.github/PULL_REQUEST_TEMPLATE/feature.md` | edited | `:42` — FLAG-PARITY checklist item dropped |
| `.github/ISSUE_TEMPLATE/enhancement.yml` | edited | `:77` — FLAG-PARITY divergence option label shortened |
| `.github/workflows/auto-close-unsolicited-prs.yml` | edited | `:91` — FLAG-PARITY link dropped from the comment-body string |
| `internal/mcp/instructions_contract_test.go` | edited | `:16`, `:375` — deleted-file/deleted-test names dropped from comments |
| `internal/mcp/tools_schema_drift_test.go` | edited | `:36`, `:39` — deleted-artifact names dropped from comments |
| `internal/cli/man.go` | edited | `:27` — FLAG-PARITY name dropped from the doc comment (census correction) |
| phase-final gate | verify task (new) | task 3 of this plan — full-tree broadened-family sweep + exact-phrase greps for ATTR-01/02/DOCS-01/03 + LICENSE byte-identity + live license + full suite, asserting a positive 13/13 count (cycle-1 review finding 3) |

## Spec-less probe resolutions (COVERAGE)

The 6 applicable probe edges, per the fallback contract (no 04-SPEC.md):

- **DOCS-02/concurrency** (research's probe labeled this plan's deletion DOCS-04; the same reasoning applies): the deletion + sweep is sequential term-by-term edits; a CI run mid-plan sees a partial sweep. The phase-final gate (task 3, positive-controlled full-tree `rg` of the broadened identifier family with exclusions → empty, plus `go test -count=1 ./...`) is the only point at which the "nothing references either" end-state is asserted — and it is an executable owned step, not prose (cycle-1 review finding 3).
- **The 5 unclassified edges (ATTR-01/02/03, DOCS-01, DOCS-03)** → flagged unresolved with the reason: doc-editing has no probe-shaped edge; the verification is the static `rg` sweep plus the live license check. Tracked across plans 04-01 and 04-03 so nothing drops.

**6 counted: 1 concurrency explicit + 5 flagged.**

## Deferred to Phase 5 (flagged, not touched)

- The **parity/ports vocabulary** at the swept sites — `.github/pull_request_template.md:37` ("deliberate parity decisions"), `feature.md` `## Parity` heading + TS-check item, `enhancement.yml` remaining options, `auto-close-unsolicited-prs.yml` "ports observable behavior" framing, and the `internal/mcp` comment framing — is Phase 5 PROC-01/02/03 and CODE-01 territory. This plan removed only the dangling links/names. Recorded so the two-phase same-line edit is explicit and the executors do not collide.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deleted drift guard → CI shape guard | The deletion removes `flag_parity_test.go`; it must not break `TestWorkflowRunBodiesInvokeTask`, which parses workflow YAML + Taskfile bodies. |
| docs/FLAG-PARITY.md → contributor-facing templates | Every `.github` template and the auto-close comment string that linked to the deleted doc is a dangling-reference trap if not swept. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | dangling reference to deleted FLAG-PARITY/flag_parity_test.go | high | mitigate | The full blast radius is swept (the 9 edit-lines across 7 owned files here + NOTICE by 04-01 + CONTRIBUTING by 04-03), verified by a positive-controlled full-tree grep of the broadened identifier family with recorded exclusions, plus the executable phase-final gate (task 3); a leftover link in ANY spelling is red. |
| T-04-05 | Tampering | deleted guard breaking CI / capability matrix | high | mitigate | Research verified zero Taskfile targets and zero CI steps reference either artifact; the deletion's own verify is `go test -count=1 ./...` green, which includes TestWorkflowRunBodiesInvokeTask and the capability matrix tests. |
| T-04-06 | DoS (coverage loss) | removing the live drift guard silently | medium | mitigate | The DOCS-05 deferral is recorded as a knowing reduction (D-03) — flag-documentation coverage is intentionally reduced this milestone, not an oversight; the SUMMARY records it. |
| T-04-07 | Tampering | over-broad sweep of parity/ports framing in the same lines | medium | mitigate | Each edit is a one-line dangling-link/name removal; the acceptance criteria assert `git diff` shows no framing vocabulary changed (that is Phase 5's named scope). |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No new external packages this phase — the deletion touches only in-git files. Package-legitimacy gate N/A. |
</threat_model>

<verification>
- `test ! -e docs/FLAG-PARITY.md && test ! -e internal/cli/flag_parity_test.go` — both absent
- `CGO_ENABLED=1 go test -count=1 ./...` green (49 packages)
- positive-controlled `rg -n -e "FLAG-PARITY" -e "flag_parity" -e "flag-parity" -e "flagParity" -e "TestFlagParityDocCoversRegisteredFlags"` over the 7 owned files → empty (full family incl. hyphenated `flag-parity`, cycle-2 finding 1)
- the phase-final gate (task 3) prints "phase-final gate: 13/13 assertions passed" and exits 0 — the full-tree broadened-family sweep + exact-phrase greps + LICENSE byte-identity + live license check + full suite, owned by this plan (wave 2, after 04-01/04-03)
</verification>

<success_criteria>
- Both deleted files are gone from the index; the full test suite passes.
- The 9 edit-lines across 7 owned files carry no mention of either artifact in ANY identifier spelling, and the NOTICE (04-01) and CONTRIBUTING (04-03) sites are handled by their owning plans so the phase-level "nothing references either" gate holds.
- An executable phase-final gate (task 3) enforces the whole phase's end-state with a positive 13/13 count after all three plans land (cycle-1 review finding 3) — the gate cannot pass having checked nothing.
- The man.go:27 census correction is recorded — the research's enumerated-10 table was incomplete, and the positive-controlled grep is the authority.
- The DOCS-05 coverage reduction is recorded as a knowing decision.
</success_criteria>

<output>
Create `.planning/phases/04-attribution-documentation-sweep/04-02-SUMMARY.md` when done
</output>
