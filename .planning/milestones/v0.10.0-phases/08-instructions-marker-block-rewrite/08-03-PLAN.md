---
phase: 08-instructions-marker-block-rewrite
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - testdata/wireoracle/transcripts/
autonomous: true
requirements: [WIRE-01]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every frozen wire transcript that carries the instructions string carries the REWRITTEN string byte-identically to what a live client receives (ROADMAP success criterion 4)"
    - "No transcript is left describing the retired text — go test ./test/wireoracle/... is green against a freshly rebuilt binary"
    - "Exactly 38 of the 42 transcripts changed; the 4 that structurally never echo server-level instructions (edge-call-before-initialize, modern-listen-catalog-change, modern-meta-invalid-params, modern-meta-unsupported-version) show zero diff"
    - "Every changed line's cause is named in the commit message, per the reviewed-diff mechanism (D-06) COVERAGE-BASELINE.md documents — no ledger file, no sign-off step"
    - "Every re-frozen .golden file was produced by the capture CLI redirecting stdout, never by a hand edit or a scripted substring replacement"
    - "The scenario set is unchanged: ExpectedScenarioCount is still 42 and no scenario was added, removed, or given a new request"
  prohibitions:
    - statement: "MUST NOT hand-edit, sed, or scripted-substring-replace a .golden transcript — the capture CLI's NormalizeWithLedger pass is what makes the file evidence about the wire, and a substring edit produces a file that looks captured and is not"
      category: transparency
    - statement: "MUST NOT silently absorb the separately-tracked wire-oracle response-ordering flake into a re-frozen transcript — if toolslist-repeat shows an arrival-order difference beyond the instructions line, halt and surface it rather than freezing the reordering as the new expected wire behaviour"
      category: safety
  artifacts:
    - path: "testdata/wireoracle/transcripts/"
      provides: "38 re-frozen golden transcripts carrying the rewritten instructions string"
      contains: "resources/list"
  key_links:
    - from: "testdata/wireoracle/transcripts/"
      to: "internal/mcp/server.go"
      via: "the capture CLI spawns the freshly built binary over real stdio; the instructions const is JSON-encoded verbatim into each initialize/discover result"
      pattern: "resources/list"
    - from: "test/wireoracle/cmd/wireoracle/main.go"
      to: "testdata/wireoracle/transcripts/"
      via: "human-run stdout redirect only — there is deliberately no flag or env var that writes a transcript file in place"
      pattern: "wireoracle -bin .* -scenario .* >"
---

<objective>
Re-freeze the frozen wire-oracle transcripts so they carry plan 08-01's rewritten `instructions` string byte-identically to what a live client receives, through one reviewed-diff pass with every changed line's cause attributed.

Purpose: `instructions` is JSON-encoded into the `initialize` / `server/discover` result of every scenario that performs a handshake. Once 08-01 changes the const, 38 of the 42 committed transcripts describe text no client will ever receive again — and `TestFrozenTranscriptsMatch` fails until they are re-captured. ROADMAP success criterion 4 requires this happen in the same change, not as follow-up cleanup.

Output: 38 re-captured `.golden` files under `testdata/wireoracle/transcripts/`, a verified 38-changed / 4-untouched diff, and a commit message naming every changed line's cause.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-instructions-marker-block-rewrite/08-CONTEXT.md
@.planning/phases/08-instructions-marker-block-rewrite/08-RESEARCH.md
@test/wireoracle/cmd/wireoracle/main.go
@test/wireoracle/COVERAGE-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-capture the affected transcripts against a freshly built binary</name>

  <precondition>`git status --porcelain testdata/wireoracle/transcripts/` prints nothing — the transcript tree is clean before the pass begins, so the reviewed diff isolates only this pass's byte movement.</precondition>

  <read_first>
    - `test/wireoracle/cmd/wireoracle/main.go` in full (59 lines) — the `-bin` / `-fixture` / `-scenario` flag contract, the stderr rule-hit ledger, and the head comment stating there is deliberately no in-place write path
    - `test/wireoracle/COVERAGE-BASELINE.md` section "Instruction for whoever next extends this corpus" — the four rules any frozen-byte change must satisfy
    - `test/wireoracle/scenarios.go` around line 490 and line 540 — `ExpectedScenarioCount = 42` and its doc comment's bump rule
    - `internal/mcp/server.go` as left by plan 08-01 — the exact string the transcripts must now carry
    - `.planning/phases/08-instructions-marker-block-rewrite/08-CONTEXT.md` "Reviewed Todos" — why the `toolslist-repeat` ordering flake must not be absorbed by this pass
  </read_first>

  <files>testdata/wireoracle/transcripts/</files>

  <action>
    Derive the scenario list first, never from memory: run `rg -l "codegraph indexes this repository" testdata/wireoracle/transcripts/` and confirm it returns exactly 38 files. Both the retired and the rewritten string open with that same prefix, so this derivation is stable before or after the capture. The 4 files it must NOT return are `edge-call-before-initialize.golden`, `modern-listen-catalog-change.golden`, `modern-meta-invalid-params.golden`, and `modern-meta-unsupported-version.golden` — these either skip the handshake deliberately or use a `_meta`-only Modern flow that never echoes server-level instructions. If the count is not 38, or if any of those 4 appears, stop and report before capturing anything: that would mean the rewritten string leaked somewhere it structurally should not be.

    Build the binary once with `go build -o /tmp/codegraph-recapture ./cmd/codegraph`. Then, for each of the 38 derived scenario names (the basename with `.golden` stripped), run `go run ./test/wireoracle/cmd/wireoracle -bin /tmp/codegraph-recapture -fixture testdata/wireoracle/fixture -scenario <name> > testdata/wireoracle/transcripts/<name>.golden`. Capture sequentially, not in parallel — the corpus carries a documented arrival-order flake under concurrent load and a parallel capture would seed it into the frozen bytes.

    Never hand-edit a `.golden` file and never reach for `sed`, `perl -pi`, or any substring replacement over the transcript tree. The capture CLI runs `NormalizeWithLedger` before emitting, and a substring edit skips that pass — producing a file that looks captured and is not (COVERAGE-BASELINE.md's own stated rule). `index-appears-mid-session` drives a real `codegraph init` subprocess mid-capture and is expected to take noticeably longer than the others; that is normal, not a hang.

    Do not touch `test/wireoracle/scenarios.go`: `ExpectedScenarioCount` stays 42 because this pass adds no scenario and adds no request to an existing one. Do not edit `COVERAGE-BASELINE.md`'s category tables or Total line for the same reason — its rule (4) binds only changes that move the scenario set, which this is not.
  </action>

  <verify>
    <automated>test "$(git status --porcelain testdata/wireoracle/transcripts/ | wc -l | tr -d ' ')" = "38" && go test ./test/wireoracle/... -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `rg -l "codegraph indexes this repository" testdata/wireoracle/transcripts/ | wc -l` returned exactly `38` before capture, and the SUMMARY records that number.
    - `test "$(git status --porcelain testdata/wireoracle/transcripts/ | wc -l | tr -d ' ')" = "38"` — exactly 38 files are modified.
    - `test "$(git status --porcelain testdata/wireoracle/transcripts/ | rg -o '(edge-call-before-initialize|modern-listen-catalog-change|modern-meta-invalid-params|modern-meta-unsupported-version)' | wc -l | tr -d ' ')" = "0"` — none of the 4 handshake-free transcripts moved.
    - `test "$(rg -l 'resources/list' testdata/wireoracle/transcripts/ | wc -l | tr -d ' ')" = "38"` — every re-frozen transcript carries the new claim.
    - `test "$(rg -l 'optional path argument' testdata/wireoracle/transcripts/ | wc -l | tr -d ' ')" = "0"` — no transcript still describes the retired text.
    - `go test ./test/wireoracle/... -count=1` exits 0, including `TestFrozenTranscriptsMatch` and the `len(Scenarios()) == ExpectedScenarioCount` assertion.
    - `git diff --stat test/wireoracle/` is empty — no harness or scenario source changed.
    - The shell history / SUMMARY shows 38 `wireoracle -scenario … >` redirect invocations and zero `sed`/`perl`/editor writes into `testdata/wireoracle/transcripts/`.
  </acceptance_criteria>

  <done>All 38 affected transcripts are re-captured from a freshly built binary through the capture CLI, the other 4 are untouched, and the frozen-transcript comparison is green.</done>
</task>

<task type="auto">
  <name>Task 2: Reviewed-diff pass — attribute every changed line and rule out the known ordering flake</name>

  <read_first>
    - `test/wireoracle/COVERAGE-BASELINE.md` section "Instruction for whoever next extends this corpus" — rule (3), the reviewed-diff mechanism (D-06): capture before and after, read the diff, name every changed line's cause in the commit message; no ledger file, no sign-off step
    - `.planning/todos/2026-08-07-wire-oracle-toolslist-repeat-response-ordering-flake.md` — the pre-existing, separately-tracked `tools/list` arrival-order nondeterminism this pass must not absorb
    - `git diff testdata/wireoracle/transcripts/` in full, as produced by Task 1
  </read_first>

  <files>testdata/wireoracle/transcripts/</files>

  <action>
    Read the complete diff from Task 1 and classify every changed line. The expected and only cause is one: the `instructions` field's value inside each scenario's `initialize` or `server/discover` result moved from the retired string to plan 08-01's rewritten one. Confirm the changed-line count per file matches that single cause — a transcript showing more than the instructions field moving is an unattributed change and must be investigated before commit, not explained away.

    Give `toolslist-repeat.golden` explicit scrutiny: if its diff shows JSON-RPC responses arriving in a different id order than before, that is the separately-tracked harness flake carried in PROJECT.md's open threads, NOT a consequence of this change. Halt and surface it rather than freezing a reordering as the new expected wire behaviour — CONTEXT.md records that this phase's re-capture must not silently absorb or fix that todo.

    Write the commit message so it names the single cause, the scenario count it applies to, and the two scenario groups that did not move and why. State plainly that every re-frozen file came from the capture CLI against a freshly built binary and that no `.golden` file was hand-edited. Follow the repository's Conventional Commits convention with the phase-plan scope, e.g. `test(08-03): re-freeze 38 wire transcripts for the rewritten instructions string`.
  </action>

  <verify>
    <automated>go test ./test/wireoracle/... -count=1 && test "$(git diff -U0 testdata/wireoracle/transcripts/ | rg '^[+-]' | rg -v '^(\+\+\+|---)' | rg -vc 'codegraph indexes this repository' | tr -d ' ')" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `test "$(git diff -U0 testdata/wireoracle/transcripts/ | rg '^[+-]' | rg -v '^(\+\+\+|---)' | rg -v 'codegraph indexes this repository' | wc -l | tr -d ' ')" = "0"` — every added and removed line in the whole transcript diff is an instructions-string line; there is no unattributed change.
    - `test "$(git diff -U0 testdata/wireoracle/transcripts/ | rg -c '^\+.*codegraph indexes this repository' | tr -d ' ')" = "38"` and the matching removed-line count is also 38 — one line moved per file, no more.
    - `go test ./test/wireoracle/... -count=1` exits 0.
    - `go test ./internal/mcp/ ./internal/agents/ -count=1` exits 0 — the re-freeze did not require weakening any upstream guard.
    - The commit message names: the single cause, the 38-scenario scope, the 4 unmoved scenarios and why they are structurally exempt, and an explicit statement that no `.golden` file was hand-edited.
    - The SUMMARY records the `toolslist-repeat.golden` inspection outcome explicitly — either "instructions line only, no ordering difference" or a halt with the observed ordering surfaced.
  </acceptance_criteria>

  <done>The transcript diff is fully attributed to one named cause, the known ordering flake is ruled out by inspection rather than by assumption, and the frozen corpus once again describes exactly what a live client receives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capture host → committed repository | The capture CLI runs the real binary on a developer machine and its normalized stdout is committed as evidence; anything the normalizer misses becomes a permanent public artifact |
| Frozen transcript → future regression gate | `TestFrozenTranscriptsMatch` treats these bytes as ground truth for every later change, so a wrong freeze silently redefines "correct" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Repudiation | `.golden` transcripts as evidence | medium | mitigate | Every file is produced by the capture CLI's stdout redirect against a freshly built binary, never a hand edit or substring replacement, so `NormalizeWithLedger` always runs; the acceptance criteria assert the invocation shape and that zero editor/`sed` writes touched the tree |
| T-08-08 | Information Disclosure | Normalized transcript contents | medium | mitigate | The capture CLI's `NormalizeWithLedger` substitutes the repo dir before emitting, and 08-01's `TestInstructionsCarriesNoWireContractViolation` independently proves the instructions value carries no absolute-path token — the two together keep host filesystem layout out of the committed corpus (T-03-19) |
| T-08-09 | Tampering | The pre-existing `toolslist-repeat` arrival-order flake | medium | mitigate | Task 2 inspects that transcript explicitly and halts on any ordering difference rather than freezing it, so a latent harness nondeterminism is never promoted into the expected-wire contract |
| T-08-SC | Tampering | Package-manager installs | low | accept | No packages are installed or added in this phase — `go.mod` and `go.sum` are untouched, so the package-legitimacy gate is not applicable (08-RESEARCH.md, "Standard Stack") |
</threat_model>

## Artifacts this phase produces

This plan creates **no new symbols**. It re-captures existing fixture files only:

| Artifact | Kind | Path |
|----------|------|------|
| 38 re-frozen transcripts | test fixture (data) | `testdata/wireoracle/transcripts/*.golden` |

No Go source file is created or modified by this plan. `test/wireoracle/scenarios.go` (`ExpectedScenarioCount = 42`) and `test/wireoracle/COVERAGE-BASELINE.md` are deliberately unchanged, since the scenario set does not move.

## Flagged Assumptions

- **A-04:** The 38/4 split was verified against the working tree during planning (`rg -l` plus a `comm` against the full file list, both run this planning session). Task 1 re-derives it rather than trusting this number, because any earlier phase landing on `server.go` or on `scenarios.go` between planning and execution could move it. A mismatch is a stop condition, not a number to adjust.

<verification>
- `go test ./test/wireoracle/... -count=1` exits 0
- `go test ./internal/mcp/ ./internal/agents/ -count=1` exits 0
- Exactly 38 transcript files modified; the 4 handshake-free scenarios show zero diff
- Every added/removed line in the transcript diff is an instructions-string line
- `git diff --stat test/wireoracle/` is empty — no harness, scenario, or documentation source changed
- `go build ./...` exits 0
</verification>

<success_criteria>
- The frozen wire transcripts carry the rewritten instructions string byte-identically to what a live client receives (ROADMAP success criterion 4)
- No transcript is left describing the retired text
- The diff is fully attributed to one named cause in the commit message, per the reviewed-diff mechanism the corpus has used for every prior frozen-byte change
- The known `toolslist-repeat` ordering flake was inspected and explicitly ruled in or out, never silently absorbed
</success_criteria>

<output>
Create `.planning/phases/08-instructions-marker-block-rewrite/08-03-SUMMARY.md` when done. Record: the derived scenario count, the per-file changed-line profile, the `toolslist-repeat` inspection outcome, and the commit message's cause attribution verbatim.
</output>
