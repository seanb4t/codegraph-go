---
phase: 05-mcp-resources-capability-claims-drift-guard
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/mcp/resources/node.md
  - internal/mcp/resources/search.md
  - internal/mcp/resources/callers.md
  - internal/mcp/resources/callees.md
  - internal/mcp/resources/impact.md
  - internal/mcp/resources/files.md
  - internal/mcp/resources/status.md
  - internal/mcp/resources/tools-filter.md
  - internal/mcp/resources/index-state.md
  - internal/mcp/resources.go
  - testdata/wireoracle/transcripts/resources-list.golden
autonomous: true
requirements: [RSRC-01, RSRC-02]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`resources/list` advertises exactly 10 entries: one per registered tool (8) plus `codegraph://tools-filter` and `codegraph://index-state` (RSRC-01)."
    - "`resources/read` returns non-empty `text/markdown` for all 10 URIs (RSRC-02)."
    - "Every fact stated in a per-tool fact-sheet is traceable to `internal/mcp/tools.go`; every fact in the two behavior docs is traceable to `internal/mcp/server.go`."
    - "The frozen `resources-list` transcript shows all 10 URIs on the wire."
  artifacts:
    - internal/mcp/resources/node.md
    - internal/mcp/resources/search.md
    - internal/mcp/resources/callers.md
    - internal/mcp/resources/callees.md
    - internal/mcp/resources/impact.md
    - internal/mcp/resources/files.md
    - internal/mcp/resources/status.md
    - internal/mcp/resources/tools-filter.md
    - internal/mcp/resources/index-state.md
  key_links:
    - "Every new file gets a `resourceURIFor` entry in the SAME edit — `registerResources` panics at server construction on a file with no entry, so a missed entry fails every test in `internal/mcp` immediately rather than shipping."
    - "The 8 per-tool URIs use the SHORT companion name (`codegraph://tools/node`), not the registered tool name (D-09); the 2 behavior-doc URIs name what the doc is about, not the mechanism (D-10)."
    - "`resourceDescriptionFor` returns the two behavior-doc descriptions from its explicit literal map and every tool description from `exploreTool()`/`companionTool()` — the tool half stays underivable-by-hand on purpose."
---

<objective>
Fan out from the proven tracer slice to the full catalog: seven more per-tool fact-sheets and the
two behavior docs, so `resources/list` advertises one entry for each of the 8 tools plus
`CODEGRAPH_MCP_TOOLS` filter semantics and index-state preconditions.

Purpose: this is the content this whole phase exists to serve. The architecture is already
proven end-to-end by plan 05-01; every task here reuses that seam unchanged and adds only
markdown plus a `resourceURIFor` entry.

Output: 10 resource files under `internal/mcp/resources/`, 10 `resourceURIFor` entries, and a
re-frozen `resources-list` transcript showing all 10 URIs on the wire.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-CONTEXT.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-RESEARCH.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan: `internal/mcp/resources/{node,search,callers,callees,impact,files,status,tools-filter,index-state}.md`
(9 files), and 9 new entries in the existing `resourceURIFor` map plus the two behavior-doc
entries in `resourceDescriptionFor`'s literal map. No new Go symbols. See 05-01-PLAN.md's
"Artifacts this phase produces" table for the full phase inventory.

<tasks>

<task type="auto">
  <name>Task 1: Fact-sheets for node, search, callers, callees</name>

  <files>internal/mcp/resources/node.md, internal/mcp/resources/search.md, internal/mcp/resources/callers.md, internal/mcp/resources/callees.md, internal/mcp/resources.go</files>

  <read_first>
    - `internal/mcp/resources/explore.md` — plan 05-01's fact-sheet. It is the format these four copy; do not invent a second structure.
    - `internal/mcp/resources.go` — the `resourceURIFor` map and `resourceDescriptionFor`, so you extend rather than duplicate them.
    - `internal/mcp/tools.go` lines 222-259 (`NodeArgs`, `SearchArgs`, `CallersArgs`, `CalleesArgs`) and lines 294-320 (`companionTool`'s `node`/`search`/`callers`/`callees` branches). These jsonschema tags and `Description` strings are the ONLY permitted source of facts for these four files.
    - `internal/mcp/server.go` lines 36-56 — the `instructions` constant's no-interpolation constraint, which every resource file inherits.
  </read_first>

  <action>
Write four fact-sheets in `internal/mcp/resources/`, each restating only what `tools.go` already
carries (D-01), in the same markdown structure `explore.md` established.

`node.md` — tool `codegraph_node`, description "Show a symbol's signature, calls, and callers, or
a line-numbered file read". All four arguments are optional: `file` (string, "File path —
disambiguates symbol, or selects file-mode when symbol is omitted"), `line` (integer, "Line number
— narrows an overloaded symbol to the definition containing (or nearest) this line"), `path`
(string, "Repo path (default: server cwd)"), `symbol` (string, "Symbol name to look up (omit for a
file-mode read)").

`search.md` — tool `codegraph_search`, description "Lexically search symbol names/qualified names,
returning locations only". `query` (string) is the only required argument, "Search term"; `kind`
(string, "Restrict to one node kind"), `limit` (integer, "Cap on results returned"), and `path`
(string, "Repo path (default: server cwd)") are optional.

`callers.md` — tool `codegraph_callers`, description "List a symbol's reverse callers". `symbol`
(string, "Symbol name") is the only required argument; `limit` (integer, "Cap on results
returned") and `path` (string, "Repo path (default: server cwd)") are optional.

`callees.md` — tool `codegraph_callees`, description "List a symbol's forward call targets".
Same argument shape as `callers.md`: `symbol` required, `limit` and `path` optional.

Each file states that the result is markdown text. Four content constraints bind all of them:
no worked examples and no cross-tool recommendations (D-01); no "empty until indexed" caveat
(D-02) — `index-state.md` owns it; no restatement that the tool is read-only or safe (D-04) —
`ToolAnnotations` carries that over the protocol; and no interpolation of any kind, meaning no
absolute filesystem path, hostname, username, or environment value anywhere in the bytes.

Add the four matching entries to `resourceURIFor` in the same edit: `node.md` maps to
`codegraph://tools/node`, and likewise for `search`, `callers`, and `callees` (D-08/D-09 — the
short companion name from `companionNames`, never the registered `codegraph_`-prefixed name).
Adding a `.md` file without its map entry makes `registerResources` panic at server construction,
so a missed entry fails every test in this package rather than shipping quietly.
  </action>

  <verify>
    <automated>go test ./internal/mcp/... -run 'TestResources' -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/mcp/... -count=1` passes. `TestResourcesReadReturnsMarkdown` drives its loop off the live `resources/list` result, so it now covers five URIs with no test edit.
    - `go run ./cmd/codegraph --help` (or any command that constructs a server) does not panic — proving every new file has a `resourceURIFor` entry.
    - `rg -c 'codegraph://tools/' internal/mcp/resources.go` returns 5.
    - `rg -l 'CODEGRAPH_MCP_TOOLS' internal/mcp/resources/` produces no match yet — the filter semantics live only in `tools-filter.md`, added in Task 3 (D-05).
    - For each of the four new files, every argument name it documents appears in the corresponding `*Args` struct in `internal/mcp/tools.go`: verified by reading both side by side, and mechanically enforced from plan 05-03 onward.
    - EXPECTED at the end of this task: `go test ./test/wireoracle/...` is RED because `resources-list.golden` still froze a single-entry catalog. Task 3 re-freezes it once, after all 10 files exist.
  </acceptance_criteria>

  <done>
    `resources/list` advertises five URIs; `resources/read` returns non-empty `text/markdown` for every one of them; `go test ./internal/mcp/...` is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fact-sheets for impact, files, status</name>

  <files>internal/mcp/resources/impact.md, internal/mcp/resources/files.md, internal/mcp/resources/status.md, internal/mcp/resources.go</files>

  <read_first>
    - `internal/mcp/resources/explore.md` and one file from Task 1 — the established format.
    - `internal/mcp/tools.go` lines 261-285 (`ImpactArgs`, `FilesArgs`, `StatusArgs`) and lines 320-337 (`companionTool`'s `impact`/`files`/`status` branches).
    - `internal/mcp/tools_schema_drift_test.go` lines 16-32 — `numericClaimRe` and `engineConstantFor`. `impact.md` is the file whose numeric claims plan 05-03's guard pins, so its wording has to be matchable by that exact regex.
    - `internal/query/validate.go` — confirm the live values of `defaultDepth` and `MaxDepth` before writing any number into prose.
  </read_first>

  <action>
Write three more fact-sheets in the same format.

`impact.md` — tool `codegraph_impact`, description "Depth-bounded reverse blast radius of a
symbol". `symbol` (string, "Symbol name") is the only required argument; `depth` (integer, "BFS
depth (default 2, max 50)") and `path` (string, "Repo path (default: server cwd)") are optional.
The two numeric claims must be written so that the regex `(?i)\b(default|max)\s+(\d+)` matches
them, and their values must equal what `internal/query/validate.go`'s `defaultDepth` and
`MaxDepth` hold right now — read those constants, do not trust the numbers quoted here. Plan
05-03 pins this file's claims against the tool schema's own claims in both directions, so a
mismatch here becomes a red test rather than a shipped contradiction.

`files.md` — tool `codegraph_files`, description "Browse the indexed file structure". No argument
is required. `depth` (integer, "Directory-nesting cap (0 = unlimited)"), `filter` (string,
"Restrict to one language"), `format` (string, the two accepted values being flat, which is the
default, and tree), `path` (string, "Repo path (default: server cwd)"), and `pattern` (string,
"Shell glob narrowing the result set"). Note that `format`'s description in `tools.go` embeds
quoted literals; carry the two accepted values across as markdown prose rather than re-typing the
Go string's escaping.

`status.md` — tool `codegraph_status`, description "Report index health and counts". `path`
(string, "Repo path (default: server cwd)") is its only argument and is optional.

The same four content constraints from Task 1 bind these three: no examples or cross-tool
recommendations, no index-state caveat, no read-only restatement, no interpolation. Add the three
matching `resourceURIFor` entries — `codegraph://tools/impact`, `codegraph://tools/files`,
`codegraph://tools/status` — in the same edit.
  </action>

  <verify>
    <automated>go test ./internal/mcp/... -run 'TestResources' -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/mcp/... -count=1` passes.
    - `rg -c 'codegraph://tools/' internal/mcp/resources.go` returns 8 — one per registered tool.
    - `rg -o -i '\b(default|max)\s+[0-9]+' internal/mcp/resources/impact.md | wc -l` returns 2, and each matched value equals the corresponding constant in `internal/query/validate.go` (`rg -n 'defaultDepth|MaxDepth' internal/query/validate.go` to read them).
    - `rg -o -i '\b(default|max)\s+[0-9]+' internal/mcp/resources/explore.md | wc -l` returns 1, matching `defaultMaxFiles`.
    - `go test ./internal/mcp/... -run TestMCPToolSchemaNumericClaimsMatchEngineConstants -count=1` still passes — `tools.go` was not touched.
    - EXPECTED at the end of this task: `go test ./test/wireoracle/...` is still RED for the same single reason as Task 1.
  </acceptance_criteria>

  <done>
    All 8 per-tool fact-sheets exist and are served; every numeric claim in them agrees with the engine constant it describes.
  </done>
</task>

<task type="auto">
  <name>Task 3: The two behavior docs, and re-freeze the resources-list transcript</name>

  <files>internal/mcp/resources/tools-filter.md, internal/mcp/resources/index-state.md, internal/mcp/resources.go, testdata/wireoracle/transcripts/resources-list.golden</files>

  <read_first>
    - `internal/mcp/server.go` lines 58-145 — `companionNames`, `ResolveCompanions`, and `ParseToolFilter`. These three are the ONLY source of fact for `tools-filter.md`; read the full doc comments, which spell the unset / named-subset / set-empty trichotomy out precisely.
    - `internal/mcp/server.go` lines 453-561 and 563-630 — `BuildServer`'s `if hasIndex` gate and `recheckCatalog`. These are the only source of fact for `index-state.md`.
    - `internal/mcp/server.go` line 56 — the live `instructions` string. `index-state.md` and it describe the same mechanisms; they must not contradict each other. Phase 8 rewrites `instructions`, not this phase.
    - `internal/mcp/resources.go` — `resourceDescriptionFor`'s literal map for behavior-doc stems, which gains its two real entries here.
    - `test/wireoracle/COVERAGE-BASELINE.md`'s closing "Instruction for whoever next extends this corpus" section, and `test/wireoracle/cmd/wireoracle/main.go` — the capture protocol for the re-freeze.
  </read_first>

  <action>
Write `internal/mcp/resources/tools-filter.md`, documenting the CURRENT, real behavior of the
`CODEGRAPH_MCP_TOOLS` narrowing filter with no forward-looking caveat about its planned removal
(D-06 — hedging now would document a decision that has not been made). State: the variable
narrows which of the 7 companion tools register; leaving it unset registers all 7 companions, for
8 tools in total alongside `codegraph_explore`; setting it to a comma-separated list registers
only the companions it names; setting it to the empty string registers no companion at all,
leaving `codegraph_explore` alone. State that `codegraph_explore` is never removable by the
filter. State that entries are trimmed of surrounding whitespace and that an unrecognized name is
ignored with a warning on standard error and never fails startup. State that whether the variable
is SET is distinct from what it holds — unset and set-to-empty are two different answers, which is
why the server reads presence rather than value. Name each of the 7 companion tools explicitly,
so plan 05-03's guard can derive the expected set from `companionNames` and compare rather than
matching a hand-typed count alone. Write the two count claims in the forms "7 companion tools"
and "8 tools" respectively — plan 05-03 pins integer-plus-noun claims of exactly that shape
against `len(companionNames)` and `len(allToolNames())`.

Write `internal/mcp/resources/index-state.md`, documenting index-state preconditions from
`BuildServer` and `recheckCatalog`. State: a tool registers only when a `.codegraph/` index
resolves for the server's working directory, so an unindexed repository advertises zero tools
while `initialize` still succeeds; the remedy is to run `codegraph init`; the catalog is
re-checked on every request, so an index created part-way through a session appears without a
client restart or reconnect; and resources are served regardless of index state, so this document
and every fact-sheet remain readable in a repository that has never been indexed. That last
sentence is the one an agent in an unindexed repository actually needs, and it is why D-02 keeps
this caveat here instead of copying it into all 8 fact-sheets. The same no-interpolation
constraint binds both files.

Add both `resourceURIFor` entries — `tools-filter.md` maps to `codegraph://tools-filter` and
`index-state.md` maps to `codegraph://index-state`. These deliberately do not encode the env var
name in the URI (D-10). Fill in `resourceDescriptionFor`'s literal map for these two stems with a
one-line summary each.

Then re-freeze the single stale golden. Rebuild the binary, run the capture CLI for the
`resources-list` scenario only, and redirect over
`testdata/wireoracle/transcripts/resources-list.golden`. There is exactly ONE named cause for this
diff: `resources/list` now advertises 10 entries where the tracer froze 1, because this plan added
the other 9 resource files. Record that cause and its count of 1 changed transcript in the commit
message. `resources-read-explore.golden` must come back byte-identical and is not re-captured.
Confirm no other golden moved.
  </action>

  <verify>
    <automated>go test ./... -count=1 &amp;&amp; go test ./test/wireoracle/... -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./... -count=1` passes and `go test ./test/wireoracle/... -count=1` passes.
    - `ls internal/mcp/resources/*.md | wc -l` returns 10.
    - `rg -c 'codegraph://' internal/mcp/resources.go` returns 10.
    - `rg -o 'codegraph://[a-z/-]*' testdata/wireoracle/transcripts/resources-list.golden | sort -u | wc -l` returns 10 — all ten URIs are advertised on the wire.
    - For each name in `companionNames`, `rg -c "codegraph_<name>" internal/mcp/resources/tools-filter.md` returns at least 1; `rg -c 'CODEGRAPH_MCP_TOOLS' internal/mcp/resources/tools-filter.md` returns at least 1.
    - `rg -c 'codegraph init' internal/mcp/resources/index-state.md` returns at least 1.
    - `git diff --name-only testdata/wireoracle/transcripts/` lists exactly `resources-list.golden`.
    - `go test ./internal/mcp/... -run 'TestResourcesRegisterWithoutIndex' -count=1 -v` passes with all 10 URIs — RSRC-03 still holds at full catalog size.
  </acceptance_criteria>

  <done>
    All 10 resources exist, register, list, and read; the frozen `resources-list` transcript shows all 10 URIs on the wire; main is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build tree → binary | The 9 new markdown files are compiled into the binary; their bytes reach every MCP client that reads a resource |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | The 9 new `internal/mcp/resources/*.md` files | medium | mitigate | Each file's action text carries the explicit no-interpolation constraint inherited from `instructions` (`server.go:47-49`): no absolute filesystem path, resolved index root, hostname, username, or environment value. Content is authored from `tools.go` and `server.go` doc comments only, never from a live capture of a developer machine. Enforced mechanically by `TestResourceContentCarriesNoHostFacts` in plan 05-03. |
| T-05-04 | Repudiation | `tools-filter.md` describing `CODEGRAPH_MCP_TOOLS` | low | accept | D-06 records that the variable is slated for removal in a future release. Documenting its current behavior with no hedge is the deliberate choice; when it is removed, plan 05-03's GUARD-02 set-equality and the `companionNames`-derived checks turn red rather than letting the doc go quietly stale. The control is the guard, not the prose. |
| T-05-02 | Tampering / Information Disclosure | `resources/read` URI lookup | low | accept | Unchanged from plan 05-01: the 9 new resources register through the same exact-match `resourceURIFor` seam and close over registration-time bytes. No new request-time path resolution is introduced. |
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` clean.
- `go test ./... -count=1` green.
- `task test:wireoracle` green at 31 scenarios.
- Exactly one golden transcript moved, with its single cause and count recorded in the commit message.
</verification>

<success_criteria>
- `resources/list` returns one entry for each of the 8 tools plus `codegraph://tools-filter` and `codegraph://index-state` (RSRC-01).
- `resources/read` returns non-empty `text/markdown` for all 10 (RSRC-02).
- Every claim in every file is traceable to `internal/mcp/tools.go` or `internal/mcp/server.go`.
</success_criteria>

<output>
Create `.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-02-SUMMARY.md` when done.
</output>
