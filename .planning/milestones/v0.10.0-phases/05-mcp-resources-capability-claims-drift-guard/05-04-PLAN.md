---
phase: 05-mcp-resources-capability-claims-drift-guard
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - test/wireoracle/scenarios.go
  - test/wireoracle/oracle_test.go
  - test/wireoracle/anchors.go
  - test/wireoracle/COVERAGE-BASELINE.md
  - test/wireoracle/MUTATION-PROOF.md
  - testdata/wireoracle/transcripts/
autonomous: true
requirements: [RSRC-01, RSRC-02, RSRC-03]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A live client against a real `serve --mcp` subprocess gets non-empty `text/markdown` from `resources/read` on EVERY URI that `resources/list` advertised — all 10, observed on the wire, never via the server's own Go API (RSRC-01, RSRC-02)."
    - "In a directory with no `.codegraph/`, `resources/list` returns the full 10-entry catalog and `resources/read` serves content — frozen on the wire alongside the existing `toolslist-no-index` transcript that froze the empty tool list (RSRC-03)."
    - "`resources/read` on an unregistered URI returns `-32602`, matching the codebase's existing error-shape scenarios."
    - "The oracle is re-proved non-vacuous against the re-frozen corpus by a confirmed-applied mutation, reverted byte-clean."
    - "`COVERAGE-BASELINE.md` and `ExpectedScenarioCount` agree at 42, and main is green at the phase boundary."
  artifacts:
    - testdata/wireoracle/transcripts/resources-read-node.golden
    - testdata/wireoracle/transcripts/resources-read-tools-filter.golden
    - testdata/wireoracle/transcripts/resources-read-index-state.golden
    - testdata/wireoracle/transcripts/resources-list-no-index.golden
    - testdata/wireoracle/transcripts/resources-read-unknown.golden
  key_links:
    - "`TestEveryAdvertisedResourceURIHasASuccessfulReadScenario` derives the required URI set from the `resources-list` CAPTURE, not from a hand-typed list — so adding an 11th resource in a future phase turns it red until that resource also has a wire read scenario."
    - "Every new scenario carries AT MOST ONE async request and it is LAST. go-sdk dispatches every call except `initialize` through `jsonrpc2.Async` (server.go:1910-1915), so `resources/read` races exactly as `tools/call` does."
    - "`ExpectedScenarioCount` ↔ `Scenarios()` ↔ transcripts directory ↔ `COVERAGE-BASELINE.md` must all reach 42 in the same change."
---

<objective>
Close success criterion 1 in its literal wording: every URI that `resources/list` advertises is
read back as non-empty `text/markdown` over the wire, by the SDK-independent oracle, from a real
`serve --mcp` subprocess — plus the unindexed-repository case that criterion 2 names, plus the
unknown-URI error shape, plus the non-vacuity re-proof criterion 5 requires.

Purpose: this is the half that cannot be satisfied by the in-package tests. Those call the
server's own Go API and are therefore the server as its own witness — explicitly excluded by the
roadmap's own wording. Only a spawned subprocess driven over raw stdio counts here.

Output: 11 new frozen transcripts, a structural test that keeps per-URI wire coverage complete
as the resource set grows, and two demonstrated-red-then-reverted mutations proving the corpus
is not vacuous.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-CONTEXT.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-RESEARCH.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-01-SUMMARY.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-03-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan: 11 new `Scenario` entries in `test/wireoracle/scenarios.go` —
`resources-read-node`, `resources-read-search`, `resources-read-callers`,
`resources-read-callees`, `resources-read-impact`, `resources-read-files`,
`resources-read-status`, `resources-read-tools-filter`, `resources-read-index-state`,
`resources-list-no-index`, `resources-read-unknown` — their 11 `.golden` transcripts,
`resourceURIsFromCapture` and `TestEveryAdvertisedResourceURIHasASuccessfulReadScenario` in
`test/wireoracle/oracle_test.go`, an unknown-resource error anchor in `test/wireoracle/anchors.go`,
and Mutations 9 and 10 in `test/wireoracle/MUTATION-PROOF.md`. See 05-01-PLAN.md for the full
phase inventory.

**Why this plan runs after 05-03 rather than beside it:** plan 05-03 applies real mutations to
`internal/mcp` and `internal/query` and reverts them. A wire capture running concurrently with an
applied mutation would freeze mutated bytes into a golden file. Sequencing is a correctness
requirement here, not a scheduling preference — and it also means the fast unit guards exist
before 11 expensive captures encode any content mistake.

<tasks>

<task type="auto">
  <name>Task 1: One wire read scenario per remaining resource URI</name>

  <files>test/wireoracle/scenarios.go, testdata/wireoracle/transcripts/</files>

  <read_first>
    - `test/wireoracle/scenarios.go` — the `resourcesListRequest`/`resourceReadRequest` helpers and the two `resources-*` scenarios plan 05-01 added; these nine copy that shape exactly.
    - `test/wireoracle/scenarios.go` lines 430-505 — the async-dispatch invariant comment and `ExpectedScenarioCount`'s running-tally doc comment, which must gain an entry for this batch.
    - `test/wireoracle/scenarios.go` lines 602-617 — `toolslist-no-index`, the existing `Index: false` scenario, for how an unindexed scenario is declared and what `ExpectTools` it carries.
    - `test/wireoracle/COVERAGE-BASELINE.md`'s closing "Instruction for whoever next extends this corpus" — its four rules bind this task.
    - `internal/mcp/resources.go` — `resourceURIFor`, so every URI literal you write into a scenario is copied from the map rather than retyped from memory.
  </read_first>

  <action>
Add nine `Scenario` entries to `test/wireoracle/scenarios.go`, one per resource URI not already
covered by plan 05-01's `resources-read-explore`: `resources-read-node`, `resources-read-search`,
`resources-read-callers`, `resources-read-callees`, `resources-read-impact`,
`resources-read-files`, `resources-read-status`, `resources-read-tools-filter`, and
`resources-read-index-state`. Each carries `Index: false` and exactly two requests —
`initializeRequest(1)` then `resourceReadRequest(2, <the URI from resourceURIFor>)` — with
`ExpectTools: 0`.

`Index: false` is a deliberate double-duty choice, not a cost shortcut, and it should be
commented as such on the group: these nine scenarios skip `codegraph init` entirely, which makes
them the cheapest scenarios in the corpus, and at the same time each one independently proves
criterion 2's property that `resources/read` serves content in a repository that has never been
indexed. The indexed read path is already frozen by plan 05-01's `resources-read-explore`, which
carries `Index: true`, so both index states are covered across the corpus rather than in a single
scenario.

Each scenario carries exactly one async request and it is last. This is a verified constraint,
not caution: go-sdk routes every call except `initialize` through `jsonrpc2.Async(ctx)`
(`$(go env GOMODCACHE)/github.com/modelcontextprotocol/go-sdk@v1.7.0/mcp/server.go:1910-1915`),
so two `resources/read` requests in one scenario would race each other exactly as two `tools/call`
requests already do — the same class as the `toolslist-repeat` ordering flake still open as a
MAJOR todo in STATE.md. Do not batch reads to save scenarios. Cite that source line in the
group's comment so the next author does not re-derive it or, worse, relax it.

Bump `ExpectedScenarioCount` from 31 to 40 in the same change and extend its doc comment with
this batch and its reason, in the running-tally style the comment already uses.

Capture all nine goldens with the capture CLI, one scenario at a time, redirecting stdout over
`testdata/wireoracle/transcripts/<name>.golden`. Never hand-write a golden. Confirm no
pre-existing golden moved — this task adds files and changes no bytes in any transcript frozen
before it.
  </action>

  <verify>
    <automated>go test ./test/wireoracle/... -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./test/wireoracle/... -count=1` passes, including `TestScenarioCountIsExact` at 40 and `TestTranscriptSetMatchesScenarioSet`.
    - `rg -n 'ExpectedScenarioCount = 40' test/wireoracle/scenarios.go` matches.
    - `ls testdata/wireoracle/transcripts/ | wc -l` returns 40.
    - `ls testdata/wireoracle/transcripts/resources-read-*.golden | wc -l` returns 10.
    - For each of the nine new goldens, `rg -c '"mimeType":"text/markdown"' <file>` returns at least 1 and `rg -c '"text":""' <file>` returns 0 — every read came back non-empty markdown.
    - `git diff --name-only testdata/wireoracle/transcripts/` lists only newly added files (`git status --porcelain testdata/wireoracle/transcripts/` shows `??` or `A` entries and no `M` entries).
    - `go test ./... -count=1` passes.
  </acceptance_criteria>

  <done>
    All 10 advertised resource URIs have a frozen wire transcript proving `resources/read` returns non-empty `text/markdown` from a real subprocess; nine of them prove it in a repository with no index.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unindexed catalog, unknown-URI error shape, and the structural coverage test</name>

  <files>test/wireoracle/scenarios.go, test/wireoracle/oracle_test.go, test/wireoracle/anchors.go, testdata/wireoracle/transcripts/resources-list-no-index.golden, testdata/wireoracle/transcripts/resources-read-unknown.golden</files>

  <read_first>
    - `test/wireoracle/oracle_test.go` lines 640-700 — `toolNamesFromCapture`, the JSON-frame decoding helper `resourceURIsFromCapture` mirrors.
    - `test/wireoracle/oracle_test.go` lines 813-860 — `TestEveryRegisteredToolHasASuccessfulCallScenario`, including how it finds a request inside a scenario and how it fails naming exactly what is uncovered. The new test is its bidirectionally-sourced sibling.
    - `test/wireoracle/anchors.go` lines 140-262 — how anchors are declared and registered, and `assertDiscoverCacheControl`'s frame-decoding style.
    - `test/wireoracle/COVERAGE-BASELINE.md` "Error shapes (4)" — the existing `error-unknown-tool` and `error-malformed-args` entries, both `-32602`. The new unknown-URI scenario must agree with them.
    - `.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-RESEARCH.md` "Pitfall 3" — go-sdk v1.7.0 returns `-32602` for a resource-not-found by default under SEP-2164, and deliberately does not distinguish an unregistered URI from a registered one whose handler could not find it. The pre-1.7.0 `-32002` is reachable only via a `MCPGODEBUG` compatibility flag, which must not be set in the capture environment.
  </read_first>

  <action>
Add two more `Scenario` entries. `resources-list-no-index`: `Index: false`, requests
`initializeRequest(1)` then `resourcesListRequest(2)`, `ExpectTools: 0`. Comment it against its
partner: the existing `toolslist-no-index` transcript already froze the empty tool list for the
same on-disk state, so the two transcripts read together are criterion 2's proof — an agent in a
repository with no index sees zero tools and the complete ten-entry resource catalog on the same
wire, from the same server, in the same condition. `resources-read-unknown`: `Index: false`,
requests `initializeRequest(1)` then `resourceReadRequest(2, "codegraph://tools/does-not-exist")`,
`ExpectTools: 0`.

Bump `ExpectedScenarioCount` from 40 to 42 and extend its doc comment for both.

Add an anchor in `test/wireoracle/anchors.go` for `resources-read-unknown` asserting the id-2
response frame carries a JSON-RPC `error` object with code `-32602`, hand-authored in this
package as a literal exactly the way the existing error-shape anchors are, never read from an SDK
constant. Name in the anchor's comment why `-32602` and not `-32002`: SEP-2164 made invalid-params
the default in go-sdk v1.7.0, this repository's other two unknown-target error scenarios already
freeze `-32602`, and a transcript showing `-32002` would mean the deprecated `MCPGODEBUG`
compatibility flag leaked into the capture environment. Also record in that comment that the SDK
deliberately returns the same error for an unregistered URI and for a registered one whose handler
could not find it, so the client learns nothing about server configuration from the failure —
this is threat T-05-02's mitigation observed on the wire rather than asserted from source.

Add `resourceURIsFromCapture(t, scenarioName, wantID)` to `test/wireoracle/oracle_test.go`,
mirroring `toolNamesFromCapture`: capture the named scenario, decode the response frame with the
given id, and return the sorted `uri` values from `result.resources`.

Add `TestEveryAdvertisedResourceURIHasASuccessfulReadScenario`. Derive the required URI set by
calling `resourceURIsFromCapture` against `resources-list` — a live capture of what the server
actually advertises, never a hand-typed list, so an eleventh resource added in a future phase
turns this red until it also has a wire read scenario. Fail immediately if that set is empty or
smaller than the ten this phase ships, so the test cannot pass vacuously against a broken capture.
Then walk `Scenarios()`, find each scenario containing a `resources/read` request, capture it, and
delete a URI from the remaining set only when its response frame carries a `contents` entry with
non-empty `text` and a `mimeType` of `text/markdown`. Fail naming exactly which URIs have no
successful wire read, in the same shape `TestEveryRegisteredToolHasASuccessfulCallScenario` uses.
Exclude `resources-read-unknown` from satisfying anything — its URI is deliberately not in the
advertised set, so it will simply never match.

Capture both new goldens with the capture CLI. Confirm no pre-existing golden moved.
  </action>

  <verify>
    <automated>go test ./test/wireoracle/... -count=1 -v -run 'TestEveryAdvertisedResourceURIHasASuccessfulReadScenario|TestScenarioCountIsExact|TestTranscriptSetMatchesScenarioSet|TestSpecAnchorsHold'</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./test/wireoracle/... -count=1` passes, including `TestScenarioCountIsExact` at 42.
    - `rg -n 'ExpectedScenarioCount = 42' test/wireoracle/scenarios.go` matches and `ls testdata/wireoracle/transcripts/ | wc -l` returns 42.
    - `go test ./test/wireoracle/... -run TestEveryAdvertisedResourceURIHasASuccessfulReadScenario -count=1 -v` passes and its output shows 10 URIs resolved.
    - `rg -o 'codegraph://[a-z/-]*' testdata/wireoracle/transcripts/resources-list-no-index.golden | sort -u | wc -l` returns 10 — the full catalog with no index.
    - `rg -c '"code":-32602' testdata/wireoracle/transcripts/resources-read-unknown.golden` returns at least 1.
    - `rg -c '"cacheScope":"private"' testdata/wireoracle/transcripts/resources-list-no-index.golden` returns at least 1.
    - `git status --porcelain testdata/wireoracle/transcripts/` shows no `M` entries — only additions.
    - `go test ./... -count=1` passes.
  </acceptance_criteria>

  <done>
    The unindexed full catalog, the unknown-URI `-32602` error shape, and a structural test that keeps per-URI wire coverage complete are all frozen and green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-prove the oracle non-vacuous, then finalize the coverage baseline</name>

  <files>test/wireoracle/MUTATION-PROOF.md, test/wireoracle/COVERAGE-BASELINE.md</files>

  <read_first>
    - `test/wireoracle/MUTATION-PROOF.md` lines 335-420 — Mutation 5, the previous capability-flip proof. These are Mutations 9 and 10, following the same record format: Edit, Confirmed applied, Gate that went red, verbatim failure, blast radius, Revert confirmation.
    - `test/wireoracle/MUTATION-PROOF.md` as extended by plan 05-03 — confirm the current highest mutation number before numbering these.
    - `internal/mcp/server.go` — the `Capabilities.Resources` field and the `registerResources(s)` call site plan 05-01 added. These are the two mutation points.
    - `test/wireoracle/COVERAGE-BASELINE.md` — the whole file: header count, category tables, Total line, History section, and the closing four-rule instruction.
  </read_first>

  <action>
Apply two mutations, one at a time, each confirmed applied, observed red, and reverted byte-clean
before the next begins.

Mutation 9 closes criterion 5's "the oracle re-proved non-vacuous". Delete the
`Resources: &mcp.ResourceCapabilities{}` line from `BuildServer`'s `mcp.ServerCapabilities`
literal in `internal/mcp/server.go`, changing nothing else. Note what this does and record it
precisely: it does NOT remove the capability from the wire, because go-sdk's `capabilities()`
auto-populates `caps.Resources` with `ListChanged: true` once any resource is registered
(`.../mcp/server.go:645-653`) — so the advertised capability silently becomes a promise of
`listChanged` support this server does not implement. That is a strictly more interesting failure
than absence, and it is the concrete justification for D-11's rule that a capability is never left
to SDK inference. Run `go test ./test/wireoracle/... -count=1`; expect `TestFrozenTranscriptsMatch`
red across every transcript carrying a `capabilities` object. Record the measured blast radius as
a count, and record the observed wire value the mutation produced.

Mutation 10 proves RSRC-03 is structural rather than incidental. Move the `registerResources(s)`
call from its unconditional position into the `if hasIndex {` block. Run `go test
./test/wireoracle/... -count=1`; expect `resources-list-no-index` and all nine `Index: false`
read scenarios red, and the two `Index: true` resource scenarios still green — an asymmetry that
demonstrates the guard is measuring index-independence specifically, not merely that resources
exist. Record which scenarios failed and which did not, and the count of each. Also record that
`TestResourcesRegisterWithoutIndex` in `internal/mcp` goes red for the same reason, so the
property is caught at both the unit and wire tiers.

Append both to `test/wireoracle/MUTATION-PROOF.md` in Mutation 5's format with verbatim failure
text and revert confirmations quoting the clean `git status --porcelain` output.

Then finalize `test/wireoracle/COVERAGE-BASELINE.md`. Set the header scenario count to 42. Replace
the partial "MCP Resources" category plan 05-01 seeded with the complete table of all 13 scenarios
— one `resources/list` in an indexed tree, one in an unindexed tree, ten per-URI reads, and the
unknown-URI error shape — noting that the error-shape scenario is kept in this category rather
than in "Error shapes" so the resources surface reads as one block. Update the Total line's
arithmetic to sum to 42. Complete the History bullet plan 05-01 opened so it describes the whole
phase: `capabilities.resources` turned on, 25 transcripts re-frozen under one named cause with 4
provably unaffected, and 13 scenarios added across plans 05-01 and 05-04. Confirm the file's four
closing rules were all satisfied by this phase and leave them unchanged.
  </action>

  <verify>
    <automated>git status --porcelain -- internal/ &amp;&amp; go test ./... -count=1 &amp;&amp; go test ./test/wireoracle/... -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `git status --porcelain -- internal/` produces no output and `git diff -- internal/` produces no output — both mutations reverted byte-clean.
    - `go test ./... -count=1` passes and `go test ./test/wireoracle/... -count=1` passes.
    - `rg -c '^## Mutation' test/wireoracle/MUTATION-PROOF.md` returns 10.
    - `rg -n '^## Mutation 9' -A 50 test/wireoracle/MUTATION-PROOF.md` contains a verbatim `TestFrozenTranscriptsMatch` failure excerpt, a measured blast-radius count, and the observed wire value the mutation produced.
    - `rg -n '^## Mutation 10' -A 50 test/wireoracle/MUTATION-PROOF.md` names both the scenarios that went red and the scenarios that stayed green, each with a count.
    - `rg -n 'Scenario count' test/wireoracle/COVERAGE-BASELINE.md` shows 42, and the file's Total line arithmetic sums to 42.
    - `rg -c 'resources-read-' test/wireoracle/COVERAGE-BASELINE.md` returns 10, and `rg -c 'resources-list' test/wireoracle/COVERAGE-BASELINE.md` returns at least 2.
    - `go test ./internal/mcp/... -count=1` passes — plan 05-03's guards are unaffected.
  </acceptance_criteria>

  <done>
    The oracle is re-proved non-vacuous against the re-frozen corpus, RSRC-03's index-independence is proved on the wire by a demonstrated-red mutation, the coverage baseline agrees with `ExpectedScenarioCount` at 42, and main is green at the phase boundary with no re-freeze outstanding.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → `serve --mcp` stdio subprocess | The only boundary this plan exercises: raw JSON-RPC bytes over real stdio, including a deliberately unregistered `resources/read` URI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Tampering / Information Disclosure | `resources/read` URI lookup | low | mitigate | Task 2's `resources-read-unknown` scenario freezes the observed wire behavior for an unregistered URI: `-32602`, with no server-configuration detail in the message. go-sdk deliberately returns the same error for an unregistered URI and a registered-but-unfound one (`server.go:1020-1025`), so the response discloses nothing about what is registered. Frozen on the wire rather than asserted from source. |
| T-05-03 | Spoofing | `capabilities.resources` advertisement | medium | mitigate | Mutation 9 in Task 3 demonstrates what the explicit zero `ResourceCapabilities{}` prevents: removing it makes go-sdk auto-advertise `listChanged: true`, a protocol promise this server cannot keep. The 25 re-frozen transcripts plus `TestFrozenTranscriptsMatch` are the standing control; the mutation is the proof that control can fire. |
| T-05-07 | Denial of Service | Wire-oracle scenario ordering | low | accept | Every new scenario carries at most one async request, placed last, because go-sdk dispatches all calls but `initialize` through `jsonrpc2.Async` (`server.go:1910-1915`). Violating this reintroduces the `toolslist-repeat` ordering flake already open as a MAJOR todo — a CI-reliability problem, not a server defect. Accepted and structurally avoided by the scenario shape rather than mitigated at runtime. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this phase; zero new Go module dependencies (see `COVERAGE.md`). |
</threat_model>

<verification>
- `go test ./... -count=1` green.
- `task test:wireoracle` green at 42 scenarios / 42 transcripts.
- `git status --porcelain -- internal/` empty after Task 3's mutations.
- `MUTATION-PROOF.md` carries 10 mutations; `COVERAGE-BASELINE.md` and `ExpectedScenarioCount` both read 42.
</verification>

<success_criteria>
- `resources/read` returns non-empty `text/markdown` on the wire for every one of the 10 URIs `resources/list` advertised, enforced structurally so an added resource cannot skip it (RSRC-01, RSRC-02, criterion 1).
- The unindexed-repository case is frozen: full 10-entry catalog and served content alongside the existing empty-tool-list transcript (RSRC-03, criterion 2).
- The oracle is re-proved non-vacuous and RSRC-03's index-independence is proved by demonstrated-red mutations reverted byte-clean (criterion 5).
- Main is green at the phase boundary with no re-freeze deferred to a later phase (criterion 5).
</success_criteria>

<output>
Create `.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-04-SUMMARY.md` when done.
</output>
