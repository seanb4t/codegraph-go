---
phase: 06-agent-skill-package-skill-md-sessionstart-nudge
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/mcp/skill_claims_drift_test.go
  - .claude/skills/codegraph/SKILL.md
autonomous: true
requirements: [SKILL-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SKILL.md's frontmatter carries exactly the two agentskills.io-required fields, its name equals the containing directory name codegraph, and its description is a single trigger-shaped line of at most 1024 characters (SKILL-01)"
    - "A markdown decision table appears after the first level-2 heading and before the second level-2 heading — the decision procedure structurally precedes every other section, including any pointer to per-tool reference material (SKILL-01)"
    - "Boundary (edge-probe resolution for SKILL-01): the body's size ceiling is enforced at 20000 bytes, the four-bytes-per-token approximation of the agentskills.io 5000-token body budget, and separately at 500 lines as the community-guidance proxy; a body one byte over either limit fails, a body at exactly the limit passes"
    - "Every codegraph:// URI SKILL.md names is a value present in resourceURIFor — the skill cannot point an agent at a resource the server does not serve"
    - "Every codegraph_<name> token in SKILL.md is a member of allToolNames() — a renamed or removed tool left behind in the skill fails the build"
    - "SKILL.md states no default or max numeric value at all; every such fact is deferred to its codegraph://tools/<name> resource (Phase 5 D-01 anti-duplication, enforced not merely intended)"
    - "SKILL.md's one tool-count claim equals len(allToolNames()), derived at test time rather than trusted"
    - "The only CODEGRAPH_-prefixed token SKILL.md may carry is allowlistEnvName, and SKILL.md names that filter because its decision table names filterable companion tools (docNamesCompanionsWithoutTheFilter)"
    - "SKILL.md carries no absolute machine-rooted filesystem path (T-06-04)"
    - "Every new checker in the guard is proven to discriminate by a synthetic non-vacuity sub-test before its verdict about the real file is trusted"
  artifacts:
    - "internal/mcp/skill_claims_drift_test.go"
    - ".claude/skills/codegraph/SKILL.md (frontmatter, decision table, skip-condition, resource-URI pointers)"
  key_links:
    - "SKILL.md codegraph:// URIs -> resourceURIFor's value set (a dead URI is the exact broken-promise class this milestone retires)"
    - "SKILL.md codegraph_<name> tokens -> allToolNames()"
    - "SKILL.md count claim -> len(allToolNames())"
    - "skillPath -> ../../.claude/skills/codegraph/SKILL.md, following instructions_contract_test.go's readmePath convention"
  prohibitions:
    - "SKILL.md must not restate any tool parameter, default value, maximum, or return shape that a codegraph://tools/* resource already owns — it names the URI instead. This is Phase 5 D-01's division of labour, and every restated fact doubles GUARD-01's pinning surface for no reader benefit."
    - "SKILL.md must not open with, or lead into, a per-tool catalog. The decision procedure comes first structurally, not merely in intent — a catalog-first skill reads well and changes nothing (RESEARCH Pitfall 1)."
    - "The guard must not hand-type a tool name, tool count, or environment variable name on either side of any comparison. Both the claimed value and the expected value are read from their real source, matching every existing guard in internal/mcp."
---

<objective>
Write the executable contract for SKILL.md first, watch it fail against a file that does not exist, then author the skill skeleton — frontmatter, decision table, skip condition, and resource-URI pointers — until it passes.

Purpose: SKILL-01's success criterion is structural ("opens with a decision table *before* any per-tool catalog, stays within budget, defers detail to the Phase-5 resource URIs"). Structural criteria written down are review notes; structural criteria written as a test are gates. RESEARCH Pitfall 3 names this file as the third candidate occurrence of this repository's documented wire-claim-drift bug class — it ships with a guard or it does not ship.

Output: `internal/mcp/skill_claims_drift_test.go`, `.claude/skills/codegraph/SKILL.md` (skeleton; 06-03 adds the worked examples).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-CONTEXT.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-RESEARCH.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-PATTERNS.md
@.planning/phases/05-mcp-resources-capability-claims-drift-guard/05-03-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 6 (not pre-existing — exclude from drift verification against the current tree):

| Artifact | Kind | Created by |
|---|---|---|
| `internal/mcp/skill_claims_drift_test.go` | Go test file | 06-02 (extended in 06-03) |
| `skillPath`, `skillDirName`, `skillBodyMaxBytes`, `skillBodyMaxLines`, `skillDescriptionMaxChars` | Go test consts | 06-02 |
| `splitSkillFrontmatter`, `skillFrontmatterField`, `decisionTablePrecedesSecondSection` | Go test helpers | 06-02 |
| `resourceURIRe` | Go test var | 06-02 |
| `TestSkillFrontmatterIsSpecCompliant` | Go test func | 06-02 |
| `TestSkillLeadsWithDecisionTable` | Go test func | 06-02 |
| `TestSkillStaysWithinBudget` | Go test func | 06-02 |
| `TestSkillNamesOnlyRealTools` | Go test func | 06-02 |
| `TestSkillResourceURIsResolve` | Go test func | 06-02 |
| `TestSkillDefersNumericFactsToResources` | Go test func | 06-02 |
| `TestSkillCountClaimsMatchSourceSets` | Go test func | 06-02 |
| `TestSkillEnvVarNamesAreReal` | Go test func | 06-02 |
| `TestSkillCarriesNoHostFacts` | Go test func | 06-02 |
| `TestSkillNamesTheFilterWhenItNamesCompanions` | Go test func | 06-02 |
| `TestSkillStructureCheckersAreNotVacuous` | Go test func | 06-02 |
| `.claude/skills/codegraph/SKILL.md` | agent skill content | 06-02 (worked examples in 06-03) |
| `.claude/hooks/session-nudge.sh`, `.claude/settings.json`, `.claude/hooks/hooks.json`, `internal/agents/hookpackage_test.go` | hook package | 06-01 |
| `.claude/skills/codegraph/verification/*.md` | rehearsal artifacts | 06-04 |

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — the SKILL.md contract as a guard test</name>
  <files>internal/mcp/skill_claims_drift_test.go</files>
  <read_first>
    - `internal/mcp/resources_schema_drift_test.go` (all 609 lines) — this is the file whose checkers you reuse and whose shape you mirror. Note in particular: the header doc comment at lines 17-25 explaining the SURF-01 lesson, `toolNameTokenRe` (line 105), `numericClaimsMultiset` (line 309), `countClaimsIn` / `countClaim` (lines 386-417), `envVarTokenRe` (line 507), `hostFactsIn` (line 556), and the "guard the guard" fatal checks that precede every real comparison.
    - `internal/mcp/instructions_contract_test.go` lines 10-33 and 124-224 — `readmePath`'s repo-root-relative convention, `allowlistEnvName`'s single-literal justification, and `docNamesCompanionsWithoutTheFilter` plus its non-vacuity test, which you call on SKILL.md rather than reimplement.
    - `internal/mcp/resources.go` lines 12-40 — `resourceURIFor`'s shape and full value set; these values are the only legal `codegraph://` targets SKILL.md may name.
    - `internal/mcp/server.go` lines 38-62 — the `instructions` constant's no-interpolation doc comment (the convention `TestSkillCarriesNoHostFacts` extends to SKILL.md) and `companionNames`.
    - `internal/mcp/tools_schema_drift_test.go` lines 16-25 — `numericClaimRe`'s exact pattern, so you state accurately in the new file's doc comments what a "numeric claim" is.
  </read_first>
  <behavior>
    Every test below must fail loudly right now, because `.claude/skills/codegraph/SKILL.md` does not exist. A missing file is a `t.Fatalf`, never a `t.Skip`.

    - `TestSkillFrontmatterIsSpecCompliant`: the document starts with a `---` fence; the frontmatter carries a `name` field equal to `skillDirName`; it carries a `description` field that is non-empty, at most `skillDescriptionMaxChars` characters, contains no newline, and contains the word "when" case-insensitively (the trigger-shaped requirement — a description that only summarises never triggers).
    - `TestSkillLeadsWithDecisionTable`: the body has at least two `## ` headings; the first markdown table separator row (a line consisting only of pipes, hyphens, colons and whitespace, with at least one pipe) appears strictly after the first `## ` heading and strictly before the second. Failure message must state which index each landed at.
    - `TestSkillStaysWithinBudget`: body length at most `skillBodyMaxBytes` and body line count at most `skillBodyMaxLines`, with the failure message reporting both actuals and both limits and naming the 4-bytes-per-token approximation it encodes.
    - `TestSkillNamesOnlyRealTools`: every `toolNameTokenRe` match in the whole document is a member of `allToolNames()`; fatal if zero matches were found.
    - `TestSkillResourceURIsResolve`: every `resourceURIRe` match is present in `resourceURIFor`'s value set; fatal if zero matches were found.
    - `TestSkillDefersNumericFactsToResources`: `numericClaimsMultiset(body)` is empty; the failure message must name the offending claims and instruct the author to point at the tool's `codegraph://tools/<stem>` resource instead.
    - `TestSkillCountClaimsMatchSourceSets`: every `countClaimsIn(body)` claim tagged `companion` equals `len(companionNames)` and every claim tagged `tool` equals `len(allToolNames())`; fatal if zero claims were found.
    - `TestSkillEnvVarNamesAreReal`: every `envVarTokenRe` match equals `allowlistEnvName`; fatal if zero matches were found.
    - `TestSkillCarriesNoHostFacts`: `hostFactsIn(document)` returns nothing.
    - `TestSkillNamesTheFilterWhenItNamesCompanions`: `docNamesCompanionsWithoutTheFilter(document)` returns nil.
    - `TestSkillStructureCheckersAreNotVacuous`: synthetic table cases proving each NEW helper discriminates — `splitSkillFrontmatter` on a document with no fence, an unterminated fence, and a well-formed fence; `skillFrontmatterField` on a present field, an absent field, and a field whose name is a prefix of another; `decisionTablePrecedesSecondSection` on a table before the second heading, a table after it, a document with only one heading, and a document with no table; `resourceURIRe` on a real URI, a URI with a trailing punctuation mark, and a bare word.
  </behavior>
  <action>
    Create `internal/mcp/skill_claims_drift_test.go` in package `mcp`. Open it with a header doc comment in the register of `resources_schema_drift_test.go` lines 17-25: state that SKILL.md is the third surface in this repository's documented wire-claim-drift bug class (after `tools.go`'s SURF-01 "default 5" and the 2026-08-08 misdirection incident), that the phase notes make it subject to GUARD-01 discipline explicitly, and that every checker below reads both the claimed and the expected value from their real source.

    Declare, each with a doc comment giving its justification:
    - `skillPath = "../../.claude/skills/codegraph/SKILL.md"` — cite `readmePath` as the convention; a missing file is a regression, not a skip.
    - `skillDirName = "codegraph"` — the agentskills.io rule that `name` must match the containing directory; this literal is the directory name itself, and the test asserts the frontmatter agrees with it.
    - `skillBodyMaxBytes = 20000` — the four-bytes-per-token approximation of agentskills.io's 5000-token body budget. Record in the comment that RESEARCH Open Question 2 resolves the token budget as authoritative and the line count as a proxy, and that the byte gate is the mechanically checkable stand-in because this repository carries no tokenizer and this milestone adds no dependency.
    - `skillBodyMaxLines = 500` — the community-guidance proxy, checked alongside rather than instead of the byte gate.
    - `skillDescriptionMaxChars = 1024` — agentskills.io's stated maximum.
    - `resourceURIRe = regexp.MustCompile("codegraph://[a-z0-9][a-z0-9/_-]*")` — deliberately excludes trailing punctuation so a URI at the end of a sentence still matches exactly.

    Write the three new helpers:
    - `splitSkillFrontmatter(doc string) (front, body string, err error)` — requires the document to begin with a `---` line, finds the next `---` line, returns the text between as `front` and everything after as `body`, and returns a descriptive error otherwise. Do not add a YAML dependency; this milestone adds zero Go modules.
    - `skillFrontmatterField(front, key string) (string, bool)` — scans lines for the exact prefix `key + ":"` at column zero and returns the trimmed remainder. The exact-prefix rule is what the prefix-collision non-vacuity case pins.
    - `decisionTablePrecedesSecondSection(body string) error` — implements the ordering rule in `<behavior>` and returns an error naming the three line indices on failure.

    Then write the eleven test functions listed in `<behavior>`, each reusing the existing package helpers by name rather than reimplementing them: `toolNameTokenRe`, `allToolNames`, `resourceURIFor`, `numericClaimsMultiset`, `countClaimsIn`, `companionNames`, `envVarTokenRe`, `allowlistEnvName`, `hostFactsIn`, `docNamesCompanionsWithoutTheFilter`. Where a checker you reuse already has a non-vacuity test in `resources_schema_drift_test.go` or `instructions_contract_test.go`, say so in the reusing test's doc comment and do not duplicate that sub-test — only the three NEW helpers need new non-vacuity cases.

    Run the package tests and record the failure output verbatim. This is the RED observation; the plan summary must quote it.
  </action>
  <verify>
    <automated>go test ./internal/mcp/... -run 'TestSkill' -count=1 -v 2>&1 | tail -40</automated>
    <automated>go vet ./internal/mcp/...</automated>
    <automated>go test ./internal/mcp/... -run 'TestSkillStructureCheckersAreNotVacuous' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/mcp/...` exits 0 — the new file compiles and is vet-clean even while its assertions fail.
    - `go test ./internal/mcp/... -run 'TestSkill' -count=1` exits NON-ZERO, and the captured output names the missing `../../.claude/skills/codegraph/SKILL.md` path. This RED observation is quoted verbatim in the plan summary; a task that never observed red has not proven its gate.
    - `TestSkillStructureCheckersAreNotVacuous` passes even in the RED state, because it operates on synthetic inputs and never touches the real file. If it does not pass here, the non-vacuity cases are wrongly coupled to the real document.
    - The file declares no tool name, tool count, resource URI, or environment variable name as a literal on the expected side of any comparison. Confirm with `grep -cE 'codegraph_(explore|node|search|callers|callees|impact|files|status)' internal/mcp/skill_claims_drift_test.go || true` printing `0`, and with `grep -cE '"codegraph://' internal/mcp/skill_claims_drift_test.go || true` printing `0`.
    - Each of `splitSkillFrontmatter`, `skillFrontmatterField` and `decisionTablePrecedesSecondSection` has at least three synthetic sub-cases including at least one that must produce an error.
    - No test in the new file calls `t.Skip`.
  </acceptance_criteria>
  <done>A compiling, vet-clean, demonstrably-red guard that states SKILL-01's structural criteria as executable assertions, with every new checker already proven to discriminate.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — SKILL.md frontmatter, decision table, skip condition, resource pointers</name>
  <files>.claude/skills/codegraph/SKILL.md</files>
  <read_first>
    - `internal/mcp/skill_claims_drift_test.go` — the file Task 1 just wrote; it is the specification this task satisfies.
    - `.claude/CLAUDE.md`, the `## CodeGraph` section — the decision-procedure-first framing already live in this repository, per 06-PATTERNS.md Analog B. Copy its imperative register and its explicit skip-if-no-index escape hatch; the decision table is that framing expanded, not a rewrite of its tone.
    - `internal/agents/instructions.go` lines 1-40 — the `codegraphInstructionsBlock` marker-fenced pointer this skill supersedes in depth while the block stays short (06-PATTERNS.md Analog C). SKILL.md must not contradict it. Do not edit this file; Phase 8 owns it.
    - `internal/mcp/resources.go` lines 21-40 — the exact `codegraph://` URI values you are permitted to reference.
    - `internal/mcp/server.go` line 56, the `instructions` constant — the current, already-corrected wire string. SKILL.md must be consistent with it.
    - `internal/mcp/tools.go` — each tool's `Description`, to phrase each decision-table row in the tool's own vocabulary without copying any parameter, default, or maximum.
  </read_first>
  <action>
    Create `.claude/skills/codegraph/SKILL.md` with this structure. Exact wording within each section is yours; the ordering and the gated properties are not.

    Frontmatter, fenced by `---`, exactly two fields:
    - `name: codegraph`
    - `description:` a single line, at most 1024 characters, third person, trigger-shaped — it must open on the trigger condition (the shapes of user question this skill answers: where a symbol is defined, how a function works, what calls or is called by a symbol, what breaks if a signature changes) and state that the skill routes those to codegraph's call and symbol graph rather than to grep, find, or file reads, in any repository carrying a `.codegraph/` index. Note that this field is the ONLY part of the skill always resident in context, so it is a trigger, not a summary.

    First `## ` section — the decision table (SKILL-01's load-bearing property). A markdown table whose rows map question shapes to the tool to reach for. Cover at minimum: where-is-X-defined and how-does-Y-work routed to `codegraph_explore` with `codegraph explore` named as the shell fallback; what-calls-X and what-does-X-call routed to `codegraph_callers` and `codegraph_callees`; what-breaks-if-I-change-X routed to `codegraph_impact`; fuzzy-name lookup routed to `codegraph_search`; one-symbol detail routed to `codegraph_node`; which-files-are-involved routed to `codegraph_files`; is-the-index-current routed to `codegraph_status`. State NO parameter, NO default, NO maximum, NO return shape anywhere in this table — the guard rejects any of them and the resource URIs already own them.

    Second `## ` section — when NOT to reach for codegraph. Carry the escape hatch verbatim in spirit from `.claude/CLAUDE.md`: with no `.codegraph/` directory, skip codegraph entirely and use the ordinary tools; indexing is the user's decision. In the same section, account for a shorter-than-expected tool list by naming `CODEGRAPH_MCP_TOOLS` as the narrowing filter and stating that unsetting it restores the full surface. This section is what makes `docNamesCompanionsWithoutTheFilter` return nil, and it is the exact reader-facing gap the 2026-08-08 incident left open — it is a correctness requirement, not a nicety.

    Third `## ` section — the reference pointers. One short list mapping each tool to its `codegraph://tools/<stem>` URI, plus `codegraph://tools-filter` and `codegraph://index-state`, with one sentence stating that these resources carry the parameters, defaults and return shapes deliberately absent from this file, and that an agent reads them on demand through its MCP resource-read tool. Use the stems exactly as `resourceURIFor` spells them.

    Place the phase's single tool-count claim in whichever of the last two sections reads best, phrased so `countClaimsIn` extracts it, and make the number equal `len(allToolNames())` as computed from `companionNames` plus `codegraph_explore`. The guard pins it; do not hand-verify.

    Leave a placeholder-free structure — do NOT add an empty worked-examples heading. Plan 06-03 adds that section and its three examples. An empty heading now would sit between the decision table and the reference pointers and change nothing except the section indices the ordering test reasons about.

    Iterate until every `TestSkill*` assertion passes.
  </action>
  <verify>
    <automated>go test ./internal/mcp/... -run 'TestSkill' -count=1 -v</automated>
    <automated>go test ./internal/mcp/... -count=1</automated>
    <automated>awk 'f&amp;&amp;/^---$/{exit} /^---$/{f=1;next} f' .claude/skills/codegraph/SKILL.md | grep -cE '^(name|description):' </automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/mcp/... -count=1` exits 0 — the whole package, so the new guard has not disturbed the Phase-5 resource guards.
    - All eleven `TestSkill*` functions pass, having been observed failing in Task 1.
    - The `awk`/`grep` frontmatter check prints `2` — exactly the two required fields, no more.
    - `TestSkillDefersNumericFactsToResources` passes, meaning SKILL.md states zero default-or-maximum numeric claims.
    - Demonstrated red, then reverted byte-clean, recorded in the plan summary with verbatim failure text for each: (a) change one decision-table entry to a tool name that is not registered and observe `TestSkillNamesOnlyRealTools` fail; (b) change one reference pointer to a `codegraph://` URI absent from `resourceURIFor` and observe `TestSkillResourceURIsResolve` fail; (c) move the decision table below the second `## ` heading and observe `TestSkillLeadsWithDecisionTable` fail naming the indices.
    - `.claude/skills/codegraph/SKILL.md` contains no worked-examples section yet — `grep -ci 'worked example' .claude/skills/codegraph/SKILL.md || true` prints `0`.
    - The decision table's first data row routes a where-is-X question to `codegraph_explore`.
  </acceptance_criteria>
  <done>A skill file an agent harness loads, whose first substantive content is a decision procedure, whose every tool name and resource URI resolves against the running server, and which restates nothing the Phase-5 resources already own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md body -> agent context | Content authored here is loaded verbatim into a model's working context on trigger and carries directive weight |
| SKILL.md claims -> `internal/mcp` source of truth | Facts asserted in prose about tools, counts, URIs and env vars must correspond to values the server actually implements |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | SKILL.md factual claims | medium | mitigate | `TestSkillNamesOnlyRealTools`, `TestSkillResourceURIsResolve`, `TestSkillCountClaimsMatchSourceSets`, `TestSkillEnvVarNamesAreReal` and `TestSkillDefersNumericFactsToResources` each read the expected value from `allToolNames()`, `resourceURIFor`, `companionNames` or `allowlistEnvName` at test time. Nothing is hand-typed on the expected side; Task 1's acceptance criteria grep-gate that. |
| T-06-04 | Information Disclosure | SKILL.md body | low | mitigate | `TestSkillCarriesNoHostFacts` applies `hostFactsIn` — the same checker Phase 5 applied to resource content — so no authoring host's filesystem layout can be published into an agent-facing file. |
| T-06-06 | Spoofing | SKILL.md `codegraph://` pointers | medium | mitigate | A URI absent from `resourceURIFor` would send an agent to a resource read that fails, and an agent taught to distrust the skill's pointers falls back to grep — precisely the outcome this phase exists to prevent. `TestSkillResourceURIsResolve` makes the dead pointer a build failure, and Task 2 requires it be demonstrated red. |
| T-06-07 | Repudiation | guard non-vacuity | medium | mitigate | Every new helper carries synthetic non-vacuity cases, and Task 2 requires three real-tree demonstrated-red mutations reverted byte-clean, following `test/wireoracle/MUTATION-PROOF.md` discipline. A guard nobody proved can fire is a claim, not a control. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no external package and adds no Go module (RESEARCH.md "Package Legitimacy Audit": skipped per its own scope condition). |
</threat_model>

<verification>
- `go test ./internal/mcp/... -count=1` green, covering both the new skill guard and Phase 5's existing resource guards.
- `go vet ./internal/mcp/...` clean.
- RED observed and quoted before GREEN; three demonstrated-red mutations applied, observed, and reverted byte-clean.
- `task test` green at plan close.
</verification>

<success_criteria>
- SKILL-01: SKILL.md opens with a decision table before any per-tool reference material, stays inside the stated body budget, and defers every parameter, default and return shape to a `codegraph://` URI — all four properties enforced by a test, not asserted in a review note.
- The skill's tool names, resource URIs, count claim and environment-variable mention are each pinned to a value read from `internal/mcp` at test time.
</success_criteria>

<output>
Create `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-02-SUMMARY.md` when done.
</output>
