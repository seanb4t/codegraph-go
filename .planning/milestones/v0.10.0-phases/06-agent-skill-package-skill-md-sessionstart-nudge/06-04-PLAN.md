---
phase: 06-agent-skill-package-skill-md-sessionstart-nudge
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - .claude/skills/codegraph/verification/SKILL-03-rehearsal.md
  - .claude/skills/codegraph/verification/NUDGE-live-session.md
autonomous: false
requirements: [SKILL-03, NUDGE-01, NUDGE-02]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "Given a fresh Claude Code session in this repository with the skill installed and a where-is-X prompt, the agent's first code-search action is codegraph_explore or codegraph explore rather than grep, find, or a file read — shown by a captured session record, not asserted from the skill's own text (SKILL-03)"
      verification: backstop
    - statement: "Starting a Claude Code session in this repository produces the nudge line in the agent's context, and resuming a session produces it again — observed in a real session, not inferred from the JSON (NUDGE-01, RESEARCH Pitfall 2)"
      verification: backstop
    - "The committed rehearsal artifact pairs a before half and an after half in parallel structure so the diff between them is legible: the before half cites the 2026-08-08 debug log's own timeline, evidence and root cause (D-02) and the after half records a freshly captured session against the same class of prompt with the skill installed"
    - "The rehearsal artifact records the prompt used, the agent's first code-search action, and the tool call that produced the answer — the three facts SKILL-03's criterion turns on — and nothing that would go stale on the next unrelated code change"
    - "The nudge evidence artifact carries the two stdout captures produced by executing the shipped script in an indexed tree and an unindexed tree, and their diff, satisfying the roadmap's requirement that NUDGE-01 and NUDGE-02 be established by executing and diffing rather than by reading (criteria 4 and 5)"
    - "Both artifacts live under .claude/skills/codegraph/verification/ and are committed, so the ship-time review has a fixed thing to review (D-01, Claude's Discretion on filing location resolved here)"
    - "Flagged assumption, carried openly: SKILL-03 has no automated gate and will not acquire one in this phase. This repository has no harness for driving a real agent session from a test, D-01 explicitly declines to invent one, and the committed transcript is the agreed substitute. The requirement is verified once, at ship time, by a human reading the artifact — anyone later treating it as continuously enforced is mistaken."
  artifacts:
    - ".claude/skills/codegraph/verification/SKILL-03-rehearsal.md"
    - ".claude/skills/codegraph/verification/NUDGE-live-session.md"
  key_links:
    - "rehearsal before half -> .planning/debug/resolved/mcp-server-one-tool-only.md (cited, not re-summarised)"
    - "rehearsal after half -> a session started after .claude/settings.json and .claude/skills/codegraph/SKILL.md were committed"
    - "nudge evidence -> the same .claude/hooks/session-nudge.sh the Go test executes, run by hand in both trees"
  prohibitions:
    - "The rehearsal artifact must not be written from what the skill says it will do. It records what a session actually did; a rehearsal composed from the skill's own text proves the thing SKILL-03 was written to stop being asserted."
    - "The after half must not be captured before .claude/settings.json and .claude/skills/codegraph/SKILL.md are committed and a genuinely new session has been started. A session that already carries this phase's authoring context in its history is not a fresh session and its transcript proves nothing."
    - "Neither artifact may record an absolute host path, a hostname, or any other machine-specific fact — the same no-interpolation rule the instructions constant and the resource docs already carry."
---

<objective>
Close SKILL-03 and the live half of NUDGE-01/02 the only way this phase's own decisions permit: run a real session, capture what actually happened, and commit it as a reviewable artifact.

Purpose: SKILL-03's criterion is deliberately worded as "verified by transcript diff, not asserted". Every other requirement in this phase has a gate that fires on every `go test`; this one has a human reading a committed record at ship time, and the honest way to deliver it is to say so plainly in the artifact rather than to dress a content review up as a behavioural proof.

Output: two committed verification artifacts under `.claude/skills/codegraph/verification/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-CONTEXT.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-RESEARCH.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-01-SUMMARY.md
@.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-03-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 6 (not pre-existing — exclude from drift verification against the current tree):

| Artifact | Kind | Created by |
|---|---|---|
| `.claude/skills/codegraph/verification/SKILL-03-rehearsal.md` | verification record | 06-04 |
| `.claude/skills/codegraph/verification/NUDGE-live-session.md` | verification record | 06-04 |
| `.claude/hooks/session-nudge.sh`, `.claude/settings.json`, `.claude/hooks/hooks.json`, `internal/agents/hookpackage_test.go` | hook package | 06-01 |
| `.claude/skills/codegraph/SKILL.md` | agent skill content | 06-02, 06-03 |
| `internal/mcp/skill_claims_drift_test.go` | Go test file | 06-02, 06-03 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live session rehearsal — nudge observed, and a where-is-X prompt routed</name>
  <precondition>`.claude/settings.json`, `.claude/hooks/session-nudge.sh` and `.claude/skills/codegraph/SKILL.md` are committed on the current branch and a `.codegraph/` index exists at the repository root; check with `git ls-files .claude/settings.json .claude/hooks/session-nudge.sh .claude/skills/codegraph/SKILL.md` listing all three and `test -d .codegraph`.</precondition>
  <precondition>The `codegraph` MCP server is connected in the session used for the capture, so `codegraph_explore` is actually callable; a session where the tool is unavailable cannot demonstrate the agent choosing it.</precondition>
  <read_first>
    - `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-CONTEXT.md` — D-01 and D-02, which lock this verification method and the reuse of the debug log as the before half.
    - `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-RESEARCH.md` — Pitfall 2, which states plainly that NUDGE-01/02 must be verified by starting a real session and observing the injected text, not by re-reading the JSON.
    - `.planning/debug/resolved/mcp-server-one-tool-only.md` — the before half's source, and the class of prompt the after half should echo.
  </read_first>
  <what-built>
    A dogfooded agent skill package in this repository: `.claude/skills/codegraph/SKILL.md` (decision table, skip condition, three worked examples, resource pointers), `.claude/hooks/session-nudge.sh` registered on the `startup` and `resume` SessionStart matchers via `.claude/settings.json`, and a `.claude/hooks/hooks.json` fragment held equal to that registration for Phase 7. Every mechanical property is already gated by `go test ./internal/mcp/... ./internal/agents/...`. What automation cannot show is the two behavioural outcomes this phase exists for.
  </what-built>
  <how-to-verify>
    Part A — the nudge, on both matchers.
    1. Start a brand-new Claude Code session with this repository as the project directory. Confirm the nudge line appears in the session's context. Copy it verbatim.
    2. End that session and resume it. Confirm the nudge line appears again for the resume event. Copy it verbatim.
    3. Copy this repository's `.claude/` directory into a scratch tree that has NO `.codegraph/` directory, start a session there, and confirm no nudge text appears at all.
    4. Independently of the sessions, run the script by hand in both trees and capture stdout each time:
       `CLAUDE_PROJECT_DIR=$PWD .claude/hooks/session-nudge.sh > /tmp/nudge-indexed.txt 2>/tmp/nudge-indexed.err; echo "exit=$?"`
       then the same with `CLAUDE_PROJECT_DIR` pointed at the unindexed scratch tree, writing to a second pair of files. Run `diff` between the two stdout captures and keep its output.

    Part B — the where-is-X routing.
    5. In a genuinely fresh session in this repository (not the one you have been working in), ask a question of the same class as the 2026-08-08 incident — a where-is-X or how-does-Y-work question about this codebase, phrased the way a user would actually phrase it, without naming any tool.
    6. Record: the exact prompt used; whether the codegraph skill was triggered; and the agent's FIRST code-search action, naming the tool it called. The criterion is met if that first action is `codegraph_explore` (or `codegraph explore`) and not grep, find, or a file read.
    7. If the first action was a text search or a file read, that is the real result — record it as such and stop. A failing rehearsal is information about the skill's description or its decision table, and it must be reported, not retried until it passes.

    Paste back for each of the seven steps: what you ran or asked, and what you observed.
  </how-to-verify>
  <resume-signal>Paste the captured nudge lines, the two stdout captures and their diff, the prompt used, and the agent's first code-search action. Then type "approved" — or describe what differed.</resume-signal>
  <acceptance_criteria>
    - The nudge line observed at session start is byte-identical to the one observed on resume and to the script's own stdout capture.
    - The unindexed-tree session produced no nudge text, and the unindexed-tree stdout capture is zero bytes with exit status 0.
    - The `diff` between the two stdout captures is non-empty in exactly one direction: one line present in the indexed capture, nothing present in the unindexed capture.
    - The Part B prompt names no tool, and the recorded first code-search action is stated as a specific tool call rather than as a judgement about the agent's intent.
    - If Part B's first action was not a codegraph call, that outcome is recorded verbatim and reported rather than re-rolled.
  </acceptance_criteria>
  <done>Both behavioural outcomes have been observed in real sessions and the raw evidence is in hand, including the case where the outcome was not the desired one.</done>
</task>

<task type="auto">
  <name>Task 2: File the rehearsal and nudge evidence as committed artifacts</name>
  <files>.claude/skills/codegraph/verification/SKILL-03-rehearsal.md, .claude/skills/codegraph/verification/NUDGE-live-session.md</files>
  <read_first>
    - The raw evidence pasted back in Task 1's checkpoint.
    - `.planning/debug/resolved/mcp-server-one-tool-only.md` — the before half's source and the section structure (`## Symptoms`, timeline, evidence, root cause) the after half mirrors for a legible diff, per 06-PATTERNS.md.
    - `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-CONTEXT.md` — D-01 and D-02.
    - `internal/mcp/skill_claims_drift_test.go` — note that these artifacts live under `.claude/skills/codegraph/` but the guard reads only `SKILL.md` by exact path, so a `verification/` subdirectory adds no guarded surface. Confirm this by re-running the package tests after the files land.
  </read_first>
  <action>
    Create `.claude/skills/codegraph/verification/SKILL-03-rehearsal.md` with two halves in parallel structure so the diff between them reads at a glance.

    Before half: cite `.planning/debug/resolved/mcp-server-one-tool-only.md` by repository-relative path and quote its own timeline, evidence and root-cause entries for the grep-first path — do not re-summarise it in your own words (D-02). State the class of question that was asked and the sequence of actions the session actually took.

    After half: the freshly captured session from Task 1 Part B. Record the exact prompt, whether the skill triggered, the first code-search action with the tool named, and the answer path taken. Use the same section headings as the before half.

    Close with a short verdict section stating whether SKILL-03's criterion is met, and a standing note that this artifact is reviewed once at ship time and is not re-run by CI — with the reason (this repository has no harness for driving a real agent session from a test, and D-01 declines to build one). Anyone reading this file later must not be able to mistake it for a continuously enforced gate.

    Create `.claude/skills/codegraph/verification/NUDGE-live-session.md` with: the observed session-start nudge line, the observed resume nudge line, the confirmation that the unindexed-tree session produced none, the two by-hand stdout captures with their exit statuses, and the `diff` between them. Add one line pointing at the automated gates that cover the same properties continuously — `TestSessionNudgeBehavesPerIndexPresence` and `TestSessionNudgeOutputIsPinnedAndStateless` in `internal/agents/hookpackage_test.go` — so this artifact reads as the live-session complement to those tests rather than as their replacement.

    Neither file may carry an absolute host path, a hostname, or a username. Where the raw capture contained one, replace it with a repository-relative path or a clearly-marked placeholder and say that you did.

    Finally, close the folded origin todo `.planning/todos/pending/2026-08-08-author-a-codegraph-usage-skill-for-agents.md` per CONTEXT.md's Folded Todos note, using `/gsd-capture`'s own verb rather than by moving the file by hand.
  </action>
  <verify>
    <automated>go test ./internal/mcp/... ./internal/agents/... -count=1</automated>
    <automated>test -f .claude/skills/codegraph/verification/SKILL-03-rehearsal.md &amp;&amp; test -f .claude/skills/codegraph/verification/NUDGE-live-session.md &amp;&amp; echo ARTIFACTS_PRESENT</automated>
    <automated>grep -cE '/(Users|home|private|var|tmp|opt|etc)/' .claude/skills/codegraph/verification/*.md || true</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/mcp/... ./internal/agents/... -count=1` exits 0 with the new files present, confirming the `verification/` subdirectory adds no guarded surface and breaks no existing guard.
    - The `ARTIFACTS_PRESENT` check succeeds.
    - The host-path grep prints `0` — neither artifact carries an absolute machine-rooted path.
    - `SKILL-03-rehearsal.md`'s before and after halves use the same section headings, and the before half quotes the debug log rather than paraphrasing it.
    - `SKILL-03-rehearsal.md` carries an explicit verdict and an explicit statement that it is not re-run by CI, with the reason given.
    - `NUDGE-live-session.md` carries both stdout captures, both exit statuses, and their diff, and names the two automated tests that cover the same properties.
    - The folded origin todo is closed and the plan summary records how.
  </acceptance_criteria>
  <done>SKILL-03 and the live half of NUDGE-01/02 have a fixed, committed, reviewable record, and that record is honest about the strength of the proof it carries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Captured session transcript -> committed repository file | Text produced by a live session on a developer machine is written into a file that ships with the repository |
| Manual verification verdict -> phase completion | A human judgement, recorded once, stands in for an automated gate for one requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information Disclosure | rehearsal and nudge artifacts | medium | mitigate | A raw session capture routinely contains the capturing machine's absolute paths and username. Task 2 requires replacement with repository-relative paths or marked placeholders, and the acceptance criteria grep-gate the same root-directory set `hostFactsPosixRe` already enforces on resource content. |
| T-06-10 | Repudiation | SKILL-03's verification strength | high | mitigate | The artifact must state in its own text that it is a once-at-ship-time review with no CI gate, and why. The failure mode being mitigated is a future reader treating a one-off record as a standing guarantee — the same misplaced confidence the stale `instructions` string produced in the 2026-08-08 incident. Task 1 additionally forbids retrying a failed rehearsal until it passes, which would turn the record into a selection artifact. |
| T-06-11 | Spoofing | the "fresh session" claim | medium | mitigate | Task 1 Part B requires a session distinct from the authoring session, and the prohibitions state why: a session already carrying this phase's authoring context in its history is not fresh and its transcript is not evidence. The checkpoint's resume-signal requires the operator to state which session was used. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs no external package and adds no Go module. |
</threat_model>

<verification>
- `go test ./internal/mcp/... ./internal/agents/... -count=1` green with both artifacts present.
- `task test` green at plan close.
- Both artifacts committed, host-fact-free, and reviewed at the checkpoint.
- The phase's five requirements traced: NUDGE-01 and NUDGE-02 by the 06-01 automated gates plus this plan's live-session evidence; SKILL-01 and SKILL-02 by the 06-02 and 06-03 guards; SKILL-03 by the committed rehearsal artifact.
</verification>

<success_criteria>
- SKILL-03: a recorded session pair shows the agent's first code-search action for a where-is-X prompt, with the skill installed, and the record states plainly whether the criterion was met.
- NUDGE-01 and NUDGE-02: the nudge was observed appearing in a real session on both the `startup` and `resume` matchers in an indexed repository, and observed absent in an unindexed one, with the shipped script's stdout executed and diffed in both trees.
- Neither artifact carries a machine-specific fact, and neither overstates the strength of the proof it records.
</success_criteria>

<output>
Create `.planning/phases/06-agent-skill-package-skill-md-sessionstart-nudge/06-04-SUMMARY.md` when done.
</output>
