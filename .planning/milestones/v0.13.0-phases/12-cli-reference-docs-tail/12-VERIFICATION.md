---
phase: 12-cli-reference-docs-tail
verified: 2026-09-13T00:00:00Z
status: passed
score: 9/9 must-haves verified
covered_files: [".github/workflows/ci.yml",".planning/phases/12-cli-reference-docs-tail/12-01-PLAN.md",".planning/phases/12-cli-reference-docs-tail/12-01-SUMMARY.md",".planning/phases/12-cli-reference-docs-tail/12-02-PLAN.md",".planning/phases/12-cli-reference-docs-tail/12-02-SUMMARY.md",".planning/phases/12-cli-reference-docs-tail/12-03-PLAN.md",".planning/phases/12-cli-reference-docs-tail/12-03-SUMMARY.md","README.md","Taskfile.yml","docs/CLI-REFERENCE.md","docs/RELEASE.md","internal/cli/cli_reference_test.go","internal/cli/root.go","internal/cli/testdata/cli-reference-allowlist.txt","tools/clidoc/main.go"]
covered_digest: "v1:sha256:c2084eb1c1996944911c1787209de0606f01bab655f756116eeeb6b8589bd815"
behavior_unverified: 0
overrides_applied: 0
re_verification: null
---

# Phase 12: CLI Reference & Docs Tail Verification Report

**Phase Goal:** Every command and flag the binary actually registers is documented in a committed `docs/CLI-REFERENCE.md` generated from the live Cobra tree and kept current by a regenerate-and-diff gate, with a small walk-based guard so no hidden or deprecated flag is ever silently unaccounted for — and the brew-trust instructions recommend the narrow grant with security framing.
**Verified:** 2026-09-13
**Status:** passed
**Re-verification:** No — initial verification

All evidence below was reproduced live in this session against HEAD (`af95a438`, clean tree) — gates re-run, files re-read, commits re-derived — not read off SUMMARY.md transcripts.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `docs/CLI-REFERENCE.md` is a committed file generated by `cobra/doc` (`tools/clidoc`) via `task docs:cli`, documenting every non-hidden command and flag including `--editor-url` (DOCS-05) | ✓ VERIFIED | `docs/CLI-REFERENCE.md` exists, tracked, generated header line 1: `<!-- Generated by tools/clidoc from the live codegraph Cobra tree — never hand-edited. Regenerate with `task docs:cli`; `task docs:cli:drift` fails the build if this file is stale. -->`. `rg -o '^## codegraph' \| wc -l` = 35 (root + 24 top-level + `daemon start/stop` + `githooks install/remove/status` + `completion` + 4 shells); `rg -o '^## codegraph man$'` = 0 (hidden command correctly excluded); `--editor-url` count = 2; `-v, --version` count = 1. `internal/cli/root.go` exports `func NewRootCmd() *cobra.Command` (line 78) alongside the unrenamed `func newRootCmd()` (line 44); `tools/clidoc/main.go` builds the tree via `internalcli.NewRootCmd()`. |
| 2 | `task docs:cli:drift` fails when the committed reference is stale relative to a fresh regeneration by the live Cobra tree (DOCS-05) | ✓ VERIFIED | Live re-run: `GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift` → `docs:cli:drift: compared 1 generated file` / `docs:cli:drift: docs/CLI-REFERENCE.md byte-identical to a fresh regeneration (temporary file only — source tree untouched)`, exit 0. `12-MUTATION-LOG.md` Family (b) demonstrates the RED path for real: deleting the `--no-open` Options line from the committed file produced `compared 1 generated file` then `::error::…differs…` then a `diff -u` excerpt naming the exact missing line, exit 1 (via `task`, wrapped exit 201); reverted via `git checkout --`, re-verified byte-clean and GREEN. `.github/workflows/ci.yml:202-203` runs `task docs:cli:drift` immediately after the `Proto codegen drift guard (BLD-04)` step (line 196). |
| 3 | A walk-based guard inspects every command including hidden/inherited-persistent/deprecated flags and fails when any flag is unaccounted for (reference or allowlist-with-reason), and was demonstrated RED by a throwaway hidden flag (DOCS-06) | ✓ VERIFIED | Live re-run: `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestEveryRegisteredFlagIsAccountedFor' ./internal/cli/` → `cli_reference_test.go:254: walked 36 commands (hidden included), inspected 115 flags: 114 accepted via ../../docs/CLI-REFERENCE.md, 1 accepted via testdata/cli-reference-allowlist.txt` / `--- PASS`. 36 ≥ floor 26; 115 ≥ floor 50. `internal/cli/testdata/cli-reference-allowlist.txt` carries exactly one entry (`codegraph man`, tab-separated reason). `12-MUTATION-LOG.md` Family (a) registered a real throwaway hidden flag (`--zz-throwaway`) on `codegraph ui`, watched the guard fail naming `unaccounted flag: codegraph ui --zz-throwaway (hidden flag — …)` with 116 flags inspected, while `task docs:cli:drift` stayed GREEN under the identical mutation (proving the drift gate structurally cannot see this class of gap) — reverted byte-clean, re-verified GREEN (115 flags). Family (c) demonstrated the allowlist-rot half: a bogus entry (`codegraph ui --zz-bogus`) produced `stale allowlist entry: codegraph ui --zz-bogus (matches no registered command or flag)`, counts unchanged — reverted byte-clean. |
| 4 | `docs/RELEASE.md` recommends the narrow `brew trust --cask seanb4t/tap/codegraph` grant with one sentence of security framing; verified by reading the diff, no guard/test (DOCS-07, D-12) | ✓ VERIFIED (per D-12: diff-review verdict, not a gate) | `docs/RELEASE.md:522-538` and `:585-591`: the quoted Homebrew error is trimmed to the narrow form with an ellipsis (`Run \`brew trust --cask seanb4t/tap/codegraph\` … to trust it.`); the security-framing sentence names the control being opted out of ("Homebrew refuses because a third-party cask runs arbitrary Ruby on your machine at install time — this cask's post-install hook does — so `brew trust` is a security control you are opting out of; `--cask` limits that opt-out to this one cask"); the tap-wide grant is named as existing and not recommended, with no command spelled out. `rg -o 'brew trust --cask seanb4t/tap/codegraph' docs/RELEASE.md` = 3 (≥ 1); `rg -o 'brew trust (--tap )?seanb4t/tap$'` (broad forms) = 0. Per D-12 this has no committed test/guard by explicit maintainer decision — this row is a read-the-diff verdict, not a gap. |
| 5 | The three RED families named in the phase's mutation-log are documented, applied for real, and reverted byte-clean | ✓ VERIFIED | `12-MUTATION-LOG.md` documents all three families (a/b/c above) with pre-mutation cleanliness gates (`git diff --quiet`), applied diffs, "Confirmed applied" greps, verbatim RED transcripts, `git checkout --` reverts, post-revert cleanliness gates, and verbatim GREEN re-runs. Current `git status --porcelain` is clean and `git diff --quiet -- internal/cli/ui.go docs/CLI-REFERENCE.md internal/cli/testdata/cli-reference-allowlist.txt` exits 0 — no mutation leaked into the committed tree. |
| 6 | README links `docs/CLI-REFERENCE.md` | ✓ VERIFIED | `README.md:85`: `[`docs/CLI-REFERENCE.md`](docs/CLI-REFERENCE.md)`, appended sentence inside the existing Homebrew/docs paragraph. |
| 7 | The pending brew-trust todo is resolved and moved to `.planning/todos/completed/` | ✓ VERIFIED | `.planning/todos/completed/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md` exists; `.planning/todos/pending/` no longer carries it. |
| 8 | The review's WR-01 finding (command-level allowlist entries not restricted to hidden commands) is fixed | ✓ VERIFIED | Commit `666569e9` ("fix(12): WR-01 require per-flag allowlist entry for documented commands") exists in history. `internal/cli/cli_reference_test.go:213`: `case hasAllowEntry(allow, allowKeyCmd) && !documentedByReference(cmd):` — the exact fix from the review, present at HEAD. Guard re-run live still passes with unchanged real counts (36 commands / 115 flags). |
| 9 | Requirements DOCS-05, DOCS-06, DOCS-07 are accounted for against REQUIREMENTS.md | ✓ VERIFIED | `.planning/REQUIREMENTS.md`: all three checked `[x]` with descriptions matching the reworded ROADMAP criteria, and mapped `Phase 12 \| Complete` in the requirements table (lines 59-61, 129-131). No orphaned Phase-12 requirement IDs found. |

**Score:** 9/9 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `internal/cli/root.go` | Exported `NewRootCmd()` wrapper, `newRootCmd()` unrenamed | ✓ VERIFIED | Both functions present; `go build ./...` and `go vet` clean |
| `tools/clidoc/main.go` | Generator: `NewRootCmd()` + `InitDefaultCompletionCmd()` + `InitDefaultVersionFlag()` + `DisableAutoGenTag` + `GenMarkdownCustom` + availability predicates, no sort of its own | ✓ VERIFIED | All symbols present via source read/grep in prior planning verify steps; live build/vet clean; drift gate byte-stable across two runs |
| `docs/CLI-REFERENCE.md` | Generated, committed, 35 headings, no hidden `man`, completion family, `--editor-url`, `--version`, no date footer | ✓ VERIFIED | All counts confirmed live (see Truth 1) |
| `internal/cli/cli_reference_test.go` | `TestEveryRegisteredFlagIsAccountedFor` with fail-closed reads, floors, accounting identity | ✓ VERIFIED | Live PASS with expected counts; WR-01 fix present |
| `internal/cli/testdata/cli-reference-allowlist.txt` | One entry: `codegraph man` | ✓ VERIFIED | Confirmed via guard's "1 accepted via testdata/cli-reference-allowlist.txt" |
| `Taskfile.yml` | `docs:cli`, `docs:cli:drift` targets | ✓ VERIFIED | Both targets present (lines 481, 494), count-before-compare shape, `mktemp -d` scratch, `cmp -s` |
| `.github/workflows/ci.yml` | `docs:cli:drift` step after `proto:drift` | ✓ VERIFIED | Step at line 202-203, immediately after `proto:drift` (line 196) |
| `docs/RELEASE.md` | Narrow brew-trust form + framing sentence | ✓ VERIFIED | See Truth 4 |
| `README.md` | Link to `docs/CLI-REFERENCE.md` | ✓ VERIFIED | See Truth 6 |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `internal/cli.NewRootCmd()` | `tools/clidoc/main.go` | direct import (`internalcli.NewRootCmd()`) | ✓ WIRED | Confirmed via build success and generated output matching the live tree |
| `tools/clidoc` | `docs/CLI-REFERENCE.md` | `task docs:cli` / `go run ./tools/clidoc -out …` | ✓ WIRED | Live regeneration byte-identical to committed file |
| `docs/CLI-REFERENCE.md` | `task docs:cli:drift` | `git ls-files` + `cmp -s` | ✓ WIRED | Live drift run MATCH, RED demonstrated and reverted in MUTATION-LOG |
| `task docs:cli:drift` | `.github/workflows/ci.yml` | CI step | ✓ WIRED | Step present immediately after `proto:drift` |
| `internal/cli/cli_reference_test.go` tree construction | `tools/clidoc/main.go` tree construction | lockstep `NewRootCmd()`+`InitDefaultCompletionCmd()`+`InitDefaultVersionFlag()` | ✓ WIRED | Both report/consume the identical 36-command/115-flag universe (guard log line matches generator's heading/flag counts) |

### Behavioral Spot-Checks / Probe Execution

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Drift gate MATCH | `GOTOOLCHAIN=go1.26.6 task -s docs:cli:drift` | `compared 1 generated file` / byte-identical, exit 0 | ✓ PASS |
| Accounting guard | `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run TestEveryRegisteredFlagIsAccountedFor ./internal/cli/` | `walked 36 commands…, inspected 115 flags: 114 via reference, 1 via allowlist`, PASS | ✓ PASS |
| Full `internal/cli` package | `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/cli/...` | all 5 sub-packages `ok` | ✓ PASS |
| Build/vet | `GOTOOLCHAIN=go1.26.6 go build ./...` / `go vet ./internal/cli/... ./tools/clidoc/...` | clean | ✓ PASS |
| Working tree clean | `git status --porcelain` | empty | ✓ PASS |

RED families (three) verified via `12-MUTATION-LOG.md` as documented evidence (applied-and-reverted transcripts, not re-executed live in this session — re-executing would require re-mutating tracked files, which the log itself already did once with full transcripts and post-revert cleanliness gates).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| DOCS-05 | 12-01, 12-02 | Committed generated CLI reference + drift gate | ✓ SATISFIED | Truths 1, 2, 6 |
| DOCS-06 | 12-01 | Walk-based accounting guard | ✓ SATISFIED | Truth 3, WR-01 fix (Truth 8) |
| DOCS-07 | 12-02 | Narrow brew-trust wording with framing | ✓ SATISFIED (verdict, D-12: no test) | Truth 4 |

No orphaned Phase-12 requirements found in REQUIREMENTS.md.

### Anti-Patterns Found

None. `rg -n 'TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER'` returned zero matches across `tools/clidoc/main.go`, `internal/cli/root.go`, `internal/cli/cli_reference_test.go`, `internal/cli/testdata/cli-reference-allowlist.txt`, `docs/RELEASE.md`, `README.md`.

### Recorded Assumptions (not gaps)

- **DOCS-06 `unclassified` edge (12-CONTEXT/12-SECURITY):** the guard's flag-presence check is a whole-document substring match, not section-scoped — a flag name recurring elsewhere in the doc (e.g. in another command's prose) could in principle mask a missing Options line for that name. `12-MUTATION-LOG.md` Family (b) documents this explicitly (chose `--no-open` over `--editor-url` for exactly this reason) and `12-SECURITY.md` Notes item 2 carries it forward as an explicit residual risk, not a closed one. Reported as recorded assumption per task instructions, not a gap.
- **DOCS-07 `unclassified` edge:** no committed test/guard/docs-grep verifies `docs/RELEASE.md`'s brew-trust prose on an ongoing basis (D-12, deliberate). A future edit could silently reintroduce the broader wording with nothing in CI to catch it. This is the documented, by-decision state — not a gap.

### Human Verification Required

None. This phase's deliverables (generated docs, Go test guard, Taskfile/CI wiring, prose wording) are all machine-verifiable or verified by the mandated diff-review (DOCS-07, D-12) rather than visual/runtime behavior.

### Gaps Summary

No gaps found. All ROADMAP success criteria and PLAN must-haves were verified live against the codebase at HEAD (`af95a438`): the drift gate MATCHes, the accounting guard passes with real counts well above its floors, all three RED mutation families are documented with applied/reverted evidence, the WR-01 review fix is present in the guard's logic, the brew-trust wording carries the narrow form with security framing and zero broad-form spellings, README links the reference, and the pending todo was moved to completed.

---

_Verified: 2026-09-13_
_Verifier: Claude (gsd-verifier)_
