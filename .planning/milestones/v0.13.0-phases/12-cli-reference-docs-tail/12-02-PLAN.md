---
phase: 12-cli-reference-docs-tail
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - docs/RELEASE.md
  - README.md
  - .planning/todos/pending/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md
  - .planning/todos/completed/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md
  - .planning/STATE.md
autonomous: true
requirements: [DOCS-07, DOCS-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-11: `docs/RELEASE.md`'s untrusted-tap instructions (the bold paragraph beginning `If brew install codegraph refuses with untrusted tap`, its quoted Homebrew error, and its `sh` block) recommend `brew trust --cask seanb4t/tap/codegraph` as THE command to run, followed by exactly one sentence naming the control — Homebrew refuses because a third-party cask runs arbitrary Ruby on your machine at install time, this cask's own post-install hook does (it generates man pages and checks the installed version), so `brew trust` is a security control you are opting out of and the `--cask` form limits that opt-out to this one cask — and the tap-wide grant is named as existing and not recommended by this project WITHOUT its command being spelled anywhere in the file"
    - "D-11: the verbatim Homebrew error quote is trimmed to the narrow form with an ellipsis (the quoted second line becomes `Run brew trust --cask seanb4t/tap/codegraph … to trust it.` with the command in backticks) so the docs never carry the broad form even as a quotation; the BREW-01 verification blockquote's parenthetical is narrowed the same way (it records what Homebrew's error offered — naming only the narrow form plus 'a tap-wide form' keeps that record true) — after the edit `docs/RELEASE.md` contains zero occurrences of the tap-wide command in either spelling and zero occurrences of the string `--tap`"
    - "D-11 (research finding 3, ROADMAP criterion 3): `README.md` carries no `brew trust` text before or after this plan — its Homebrew paragraph keeps pointing readers at `docs/RELEASE.md` for the untrusted-tap step; the DOCS-07 edit is entirely inside `docs/RELEASE.md`"
    - "D-12: no test, guard, or docs-grep is committed for DOCS-07 — the wording is verified by reading the diff (pasted in the SUMMARY) plus the one-time source assertions in this plan's `<verify>`, which live here and nowhere in the repository; the pending todo `2026-08-10-brew-trust-instructions-…md` is moved to `.planning/todos/completed/` by the GSD todo tool with `completed:` and `status: completed` stamped, in the same commit as the wording"
    - "D-06: `README.md` gains exactly one markdown link whose target is `docs/CLI-REFERENCE.md` (link text in backticks, the paragraph's existing style), as one added sentence inside the existing Homebrew paragraph that already points at `docs/RELEASE.md` (README.md:74-83) — no new heading, section, table row or other restructuring; the `## Commands` section is untouched"
    - "ASSUMPTION (DOCS-07 edge row `unclassified`, unresolved — recorded, not resolved): the narrow-form wording is protected only by this plan's one-time assertions and the diff review (D-12) — a later edit that reintroduces the tap-wide command as an instruction would not be caught by any committed gate. Accepted by maintainer decision; surfaced here so it is a visible choice, not a silent gap."
  artifacts:
    - "docs/RELEASE.md — rewritten untrusted-tap paragraph (~517-531): trimmed quote, one framing sentence, `brew trust --cask seanb4t/tap/codegraph` + `brew install codegraph` in the `sh` block, one prose sentence naming the tap-wide grant as not recommended; narrowed BREW-01 blockquote parenthetical (~577-586)"
    - "README.md — one sentence appended to the Homebrew paragraph linking `docs/CLI-REFERENCE.md`"
    - ".planning/todos/completed/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md — moved from pending, frontmatter stamped `completed: 2026-09-…` and `status: completed`"
    - ".planning/STATE.md — the brew-trust row moved from the Pending Todos table to the `Resolved and filed` table (values only; no heading, column or shape change)"
  key_links:
    - "README.md Homebrew paragraph → `docs/RELEASE.md` untrusted-tap paragraph (the pointer README already carries is the only README-side DOCS-07 surface — research finding 3)"
    - "README.md Homebrew paragraph → `docs/CLI-REFERENCE.md` (D-06 — the generated reference 12-01 committed; `task docs:cli:drift` keeps the link target current)"
    - "`.planning/todos/pending/2026-08-10-brew-trust-…md` → `gsd_run todo complete` → `.planning/todos/completed/…` (D-12 — resolved through the tool, same commit as the edit)"
  prohibitions:
    - "`docs/RELEASE.md` MUST NOT spell the tap-wide grant command in any form a reader could copy — it is named in prose as existing and not recommended, never written as a command, and the string `--tap` MUST NOT appear anywhere in the file after this plan (D-11)"
    - "No test, guard, Taskfile target, CI step or docs-grep MUST be committed for DOCS-07 — the source assertions in this plan are one-time acceptance checks, not a gate (D-12)"
    - "README.md MUST NOT be restructured — exactly one added sentence carrying one link, in the existing Homebrew paragraph; no new heading, section, list or table row (D-06)"
    - "The quoted Homebrew error MUST NOT carry the broad alternative, even as a quotation — trimmed to the narrow form with an ellipsis (D-11)"
    - "`docs/CLI-REFERENCE.md` MUST NOT be touched by this plan — it is generated; the README link points at it, nothing edits it (D-01)"
    - "STATE.md and the todo files MUST NOT gain invented headings or shapes — the todo moves via the tool; STATE.md's row moves between two existing tables"
---

<objective>
Land the docs tail proper: the brew-trust instructions in `docs/RELEASE.md` recommend the narrow `--cask` grant with one sentence of security framing and never spell the tap-wide command; README gains its one link to the generated CLI reference; the 2026-08-10 todo that raised the brew-trust problem is resolved through the todo tool in the same commit.

Purpose: the trust step is mandatory for every Homebrew user (likelihood 100%), and the blast radius of the user-side grant is set by which form the docs tell them to run — today the broad one, with no statement of what control they are switching off (todo, UF-2). A wording change with no test (D-12), verified by reading the diff.

Decision coverage (all locked, `12-CONTEXT.md`): implemented here — D-06: README link · D-11: brew-trust wording, corrected by research finding 3 (README carries no `brew trust` text) · D-12: no DOCS-07 test, todo resolved via the tool. Honored here — D-01: the generated reference is linked, never edited · D-14: `rg -o … | wc -l` counts only. Landed by 12-01 — D-02: the generator · D-03: the drift gate · D-04: prose in `Short`/`Long` · D-05: hidden stays hidden · D-07: the walk · D-08: the one rule · D-09: the counts. Landed by 12-03 — D-10: the RED families · D-13: the house artefacts.

Output: rewritten `docs/RELEASE.md` untrusted-tap paragraph and narrowed BREW-01 blockquote; `README.md` +1 sentence; the todo moved to `completed/`; STATE.md's todo row moved.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cli-reference-docs-tail/12-CONTEXT.md
@.planning/phases/12-cli-reference-docs-tail/12-PATTERNS.md
@.planning/todos/pending/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md

Prose only — no Go, no Taskfile, no CI. Counts via `rg -o … | wc -l | tr -d ' '` (never `rg -c`, never `rg -h`). The plan-time base commit is `ca015c4b`; "unchanged" assertions diff against it. `docs/RELEASE.md` line numbers below were read at plan time (the untrusted-tap paragraph starts at 517; the BREW-01 blockquote spans ~565-590) — re-locate by content, not by number, before editing. The Homebrew error text quoted in the doc is Homebrew 6.0.16's; keep its first line verbatim.
</context>

<tasks>

<task type="auto">
  <name>Task 1: `docs/RELEASE.md` — narrow `brew trust --cask` grant as THE command, one sentence of security framing, tap-wide grant named but never spelled, quoted error trimmed; todo resolved via the tool (D-11, D-12)</name>
  <reversibility rating="reversible">Wording in one doc and a planning-file move; `git revert` restores both.</reversibility>
  <files>docs/RELEASE.md, .planning/todos/pending/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md, .planning/todos/completed/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md, .planning/STATE.md</files>
  <read_first>
    - docs/RELEASE.md:505-540 (the untrusted-tap paragraph: bold lead-in, three-line explanation, the fenced quoted error whose second line offers two forms, the "Run the command the error names" sentence, and the two-line `sh` block whose first line is today's tap-wide command — the whole block is what changes) and :565-590 (the BREW-01 verification blockquote: "**One documentation gap this verification found, closed above and repeated here for visibility.**" — its parenthetical lists both forms and its last sentence says to run the command the error names)
    - .planning/todos/pending/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md (whole — Problem 1 "No security framing" and Problem 2 "We recommend the broader grant"; the Fix shape; note its third bullet — keep the broad form documented as a labelled convenience — is SUPERSEDED by D-11: named as not recommended, never spelled)
    - internal/cli/man.go:11-30 (what the cask post-install hook actually runs — the man-page generator — so the framing sentence's claim about arbitrary Ruby at install time is grounded in this repo's own hook; `.goreleaser.yaml` `homebrew_casks.hooks.post.install` is the hook itself)
    - .planning/STATE.md:294-317 (the Pending Todos table row `| 2026-08-10 | docs | — | \`brew trust\` instructions … |` and the `Resolved and filed` table with columns `| Resolved | Area | Title |` — the row moves from the first table to the second, values only)
    - .planning/phases/07-guards-that-cannot-fire/07-04-SUMMARY.md (how Phase 7 recorded three resolved todos — the commit-grouping precedent: todo move in the same commit as the fix)
  </read_first>
  <action>
    **Untrusted-tap paragraph (D-11).** Keep the bold lead-in and the three-line explanation ("The first install from any newly-tapped, non-official tap … not specific to this cask:") verbatim. In the fenced quoted error keep line 1 (`Error: Refusing to load cask seanb4t/tap/codegraph from untrusted tap seanb4t/tap.`) verbatim and rewrite line 2 to `Run \`brew trust --cask seanb4t/tap/codegraph\` … to trust it.` — the Unicode ellipsis `…` replaces the `or …` alternative Homebrew prints, so the quotation carries the narrow form only. Replace the sentence that tells the reader to run whichever command the error names (the line directly below the quote) with ONE framing sentence followed by the instruction: Homebrew refuses because a third-party cask runs arbitrary Ruby on your machine at install time — this cask's own post-install hook does exactly that (it generates man pages and checks the installed version) — so `brew trust` is a security control you are opting out of, and the `--cask` form limits that opt-out to this one cask. Then: "Trust this cask alone, then re-run `brew install codegraph`:" and the `sh` block becomes `brew trust --cask seanb4t/tap/codegraph` on line 1 and `brew install codegraph` on line 2. After the block add one prose sentence: Homebrew also offers a tap-wide grant that trusts every current and future cask and command in the tap; this project does not recommend it — with NO command, NO flag name, nothing to copy-paste (D-11: named as existing and not recommended, never spelled; this supersedes the todo's own "labelled convenience" suggestion). No flag-form spelling of the tap-wide grant may survive anywhere in the file — the first `<verify>` gate greps for it — and no command may be broken across a line (the same gate counts the narrow form on single lines).

    **BREW-01 blockquote (D-11, planner call on PATTERNS.md's open point: narrow it).** It is a record of what the verification found, so change only the two spots that carry the broad form: the parenthetical after "until the tap is explicitly trusted" becomes `(\`brew trust --cask seanb4t/tap/codegraph\` — Homebrew's error also offers a tap-wide form)`, which stays true to what the error printed; and the closing sentence becomes: If `brew install codegraph` refuses with that message, run `brew trust --cask seanb4t/tap/codegraph`, then re-run `brew install codegraph`. Nothing else in the blockquote changes (the numbered findings, the "one release only" paragraph, the `03-EVIDENCE.md` references stay byte-identical).

    **Diff review (D-12).** Read `git diff -- docs/RELEASE.md` end to end before committing and paste it in the SUMMARY — this IS the verification for DOCS-07; do not add a test, a Taskfile target, a CI step or a docs-grep anywhere. The one-time checks in `<verify>` below exist only in this plan.

    **Todo (D-12).** Resolve through the GSD todo tool, never by hand-writing frontmatter: first `gsd_run todo complete 2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md --dry-run` (preview: `would_move` pending → completed, `would_set` `completed`/`status`), then the real run (`gsd_run` resolves to `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs"` when the launcher function is not in scope). The tool moves the file to `.planning/todos/completed/` and stamps `completed: <today>` and `status: completed` in its frontmatter; if the tool is unavailable, fall back to `git mv` plus adding those two frontmatter keys by hand and say so in the SUMMARY. Then, in `.planning/STATE.md`, delete the brew-trust row from the Pending Todos table and append one row to the `Resolved and filed` table: `| 2026-09-13 | docs | \`brew trust\` instructions recommend broader tap grant with no security framing — closed by Phase 12 DOCS-07 (docs/RELEASE.md recommends the narrow \`--cask\` grant with one sentence of security framing; no test by decision D-12) |`. Values only: no heading, column, or table added or removed; the "5 listed" prose and the other five pending rows are left exactly as they are (the deferred reconciliation pass owns them — 12-CONTEXT Deferred Ideas).

    **Commit** the four paths together: `docs(12-02): recommend the narrow brew trust --cask grant with security framing; resolve the brew-trust todo`.
  </action>
  <verify>
    <automated>test "$(rg -o 'brew trust --cask seanb4t/tap/codegraph' docs/RELEASE.md | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test "$(rg -o 'brew trust (--tap )?seanb4t/tap([^/]|$)' docs/RELEASE.md | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o -- '--tap' docs/RELEASE.md | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'seanb4t/tap/codegraph` … to trust it\.' docs/RELEASE.md | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'arbitrary Ruby' docs/RELEASE.md | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'opting out' docs/RELEASE.md | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'tap-wide' docs/RELEASE.md | wc -l | tr -d ' ')" -ge 2 &amp;&amp; test "$(rg -o '^Error: Refusing to load cask seanb4t/tap/codegraph from untrusted tap seanb4t/tap\.$' docs/RELEASE.md | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'Run the command the error names' docs/RELEASE.md | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'command it names' docs/RELEASE.md | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the narrow command does not appear on single lines in the quote, the `sh` block and the blockquote (≥ 3), either spelling of the tap-wide command or its flag-form string survives anywhere in the file, the quoted error's second line was not trimmed with the ellipsis, the framing sentence does not name arbitrary Ruby at install time and the opt-out, the tap-wide grant is not named in prose in both places, the quoted error's first line was altered, or either old "run the command it/the error names" instruction survived</fails_when>
    <automated>test "$(rg -o 'brew trust' README.md | wc -l | tr -d ' ')" = "0" &amp;&amp; git diff --quiet ca015c4b -- README.md &amp;&amp; test "$(git show --name-only --format= HEAD | rg -o '^(docs/CLI-REFERENCE\.md|internal/|tools/|Taskfile\.yml|\.github/|go\.mod|go\.sum)' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(git diff --numstat ca015c4b -- docs/RELEASE.md | cut -f1)" -le 14 &amp;&amp; test "$(git diff --numstat ca015c4b -- docs/RELEASE.md | cut -f2)" -le 12 &amp;&amp; test "$(rg -o '^## |^### ' docs/RELEASE.md | wc -l | tr -d ' ')" = "$(git show ca015c4b:docs/RELEASE.md | rg -o '^## |^### ' | wc -l | tr -d ' ')" &amp;&amp; test "$(rg -o '03-EVIDENCE\.md' docs/RELEASE.md | wc -l | tr -d ' ')" = "$(git show ca015c4b:docs/RELEASE.md | rg -o '03-EVIDENCE\.md' | wc -l | tr -d ' ')"</automated>
    <fails_when>README gained brew-trust text (it must only point at RELEASE.md — research finding 3), any non-doc file or the generated reference changed in this task, the RELEASE.md diff is larger than the two targeted spots (more than ~14 added / ~12 removed lines means the blockquote record or another section was rewritten), or a heading or evidence reference count moved</fails_when>
    <automated>test ! -f .planning/todos/pending/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md &amp;&amp; F=.planning/todos/completed/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing.md &amp;&amp; test -f "$F" &amp;&amp; test "$(rg -o '^status: completed' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^completed: 2026-' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^resolves_phase: 12' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(ls .planning/todos/pending | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the todo still sits in pending, the completed copy lacks the tool-stamped `status: completed` / `completed:` keys, its original `resolves_phase: 12` was lost, or pending holds other than the one remaining graphstore-archtest todo</fails_when>
    <automated>S=.planning/STATE.md &amp;&amp; test "$(rg -o '^\| 2026-08-10 \| docs \| — \| `brew trust`' "$S" | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^\| 2026-09-[0-9]{2} \| docs \| `brew trust` instructions .* closed by Phase 12 DOCS-07' "$S" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^## |^### ' "$S" | wc -l | tr -d ' ')" = "$(git show ca015c4b:.planning/STATE.md | rg -o '^## |^### ' | wc -l | tr -d ' ')" &amp;&amp; test "$(rg -o '^\| 2026-09-08 \| testing \|' "$S" | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the pending row survived, the resolved row is missing or not in the `| Resolved | Area | Title |` shape, STATE.md's heading set changed (invented structure in a tool-owned file), or a neighbouring pending row was disturbed</fails_when>
    <automated>test "$(git show --name-only --format= HEAD | rg -o '^(docs/RELEASE\.md|\.planning/STATE\.md|\.planning/todos/completed/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing\.md|\.planning/todos/pending/2026-08-10-brew-trust-instructions-recommend-the-broader-tap-grant-with-no-security-framing\.md)$' | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test -z "$(git status --porcelain)"</automated>
    <fails_when>the wording, the todo move and the STATE.md row are not in one commit (git lists a rename as one or two paths — three or four matches are both correct), or the tree is dirty at task end</fails_when>
  </verify>
  <acceptance_criteria>
    - The full `git diff -- docs/RELEASE.md` is pasted in the SUMMARY and reads: narrow grant first, one framing sentence, tap-wide grant named without a command, quoted error trimmed with `…`, blockquote parenthetical narrowed, nothing else changed
    - `docs/RELEASE.md` contains no tap-wide command in either spelling and no `--tap` string; README contains no `brew trust` text
    - The todo is in `completed/` with tool-stamped fields; STATE.md's row moved between the two existing tables with no structural change
    - The SUMMARY states explicitly that no test/guard/grep was added for DOCS-07 (D-12) and names the todo-tool invocation used (or the `git mv` fallback)
  </acceptance_criteria>
  <done>A Homebrew user following `docs/RELEASE.md` is told to trust this one cask, why Homebrew asked, and what the grant switches off — and is never handed the tap-wide command; the todo that raised it is closed through the tool in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: README.md — one link to the generated `docs/CLI-REFERENCE.md` in the existing docs-pointer paragraph, no restructuring (D-06)</name>
  <reversibility rating="reversible">One sentence in the README.</reversibility>
  <files>README.md</files>
  <read_first>
    - README.md:66-83 (the `### Homebrew (macOS)` block: the install line and the paragraph beginning "Installs the binary, shell completions, and man pages in one step — see [`docs/RELEASE.md`](docs/RELEASE.md) …" — the paragraph D-06 names; its last sentence ends "…reports the same pointer and exits zero.")
    - README.md:124-139 (the `## Commands` table and "Every command has `--help`." — read to confirm it is NOT edited: D-06 allows exactly one link in the existing pointer paragraph and no other restructuring)
    - docs/CLI-REFERENCE.md:1-12 (12-01's banner, H1 and intro line — the link text should describe the target as the generated command-and-flag reference)
  </read_first>
  <action>
    Append ONE sentence to the end of the Homebrew paragraph (after "…reports the same pointer and exits zero."), in the paragraph's existing inline-link style, e.g.: The full command and flag reference, generated from the binary's own command tree, is [`docs/CLI-REFERENCE.md`](docs/CLI-REFERENCE.md). Wrap to the paragraph's existing line width. Change nothing else in README.md: no new heading, section, bullet, table row, or edit to `## Commands` (D-06: "no other README restructuring"). Do not touch `docs/CLI-REFERENCE.md` (generated — D-01). Commit alone: `docs(12-02): link docs/CLI-REFERENCE.md from README's docs pointers`.
  </action>
  <verify>
    <automated>test "$(rg -o '\]\(docs/CLI-REFERENCE\.md\)' README.md | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'docs/CLI-REFERENCE\.md' README.md | wc -l | tr -d ' ')" = "2" &amp;&amp; LREF=$(rg -n '\]\(docs/CLI-REFERENCE\.md\)' README.md | cut -d: -f1) &amp;&amp; LREL=$(rg -n '\]\(docs/RELEASE\.md\) for exactly what the cask guarantees' README.md | cut -d: -f1) &amp;&amp; test "$LREF" -gt "$LREL" &amp;&amp; test $((LREF - LREL)) -le 12 &amp;&amp; test "$(git diff --numstat ca015c4b -- README.md | cut -f1)" -le 3 &amp;&amp; test "$(git diff --numstat ca015c4b -- README.md | cut -f2)" -le 2 &amp;&amp; test "$(rg -o '^#{1,3} ' README.md | wc -l | tr -d ' ')" = "$(git show ca015c4b:README.md | rg -o '^#{1,3} ' | wc -l | tr -d ' ')" &amp;&amp; test "$(rg -o '^\| `' README.md | wc -l | tr -d ' ')" = "$(git show ca015c4b:README.md | rg -o '^\| `' | wc -l | tr -d ' ')" &amp;&amp; test "$(rg -o 'brew trust' README.md | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(git show --name-only --format= HEAD | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(git show --name-only --format= HEAD | rg -o '^README\.md$' | wc -l | tr -d ' ')" = "1" &amp;&amp; git diff --quiet -- docs/CLI-REFERENCE.md &amp;&amp; test -z "$(git status --porcelain)"</automated>
    <fails_when>the link is absent or duplicated (exactly one markdown link, two path mentions counting the link text), it landed outside the Homebrew pointer paragraph (more than a dozen lines below the RELEASE.md pointer), the README diff is larger than one added sentence, a heading or `## Commands` table row was added or removed, brew-trust text appeared, the generated reference was touched, or the commit carries more than README.md</fails_when>
  </verify>
  <acceptance_criteria>
    - README.md has exactly one link to `docs/CLI-REFERENCE.md`, inside the Homebrew paragraph that already links `docs/RELEASE.md`
    - The README diff against `ca015c4b` is at most three added and two removed lines; heading and command-table counts unchanged
    - Committed alone, tree clean
  </acceptance_criteria>
  <done>A README reader who reaches the docs pointers can open the generated, drift-guarded CLI reference in one click, and nothing else about the README moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/RELEASE.md` → a Homebrew user's shell | the instruction tells a user to switch off a Homebrew security control; which form we name sets the blast radius of that grant (UF-2, todo of 2026-08-10) |
| `README.md` → reader | a pointer to a generated file; staleness of the target is 12-01's drift gate's problem, not the link's |
| this plan → `.planning/` files | a todo move and a STATE.md table row — tool-owned artefacts that must not gain invented structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08 | Elevation of Privilege (user-side; UF-2 docs-as-security-guidance) | `docs/RELEASE.md` untrusted-tap instructions — recommending the tap-wide grant hands every current and future cask/command in the tap arbitrary-Ruby-at-install rights on the user's machine, with no statement of the control being bypassed | high | mitigate | D-11 wording (Task 1): `brew trust --cask seanb4t/tap/codegraph` is THE command in the quote, the prose and the `sh` block; one sentence names the control (arbitrary Ruby at install time via this cask's own post-install hook) and that `--cask` scopes the opt-out to one cask; the tap-wide grant is named as not recommended and never spelled; one-time source assertions (narrow form ≥ 4, tap-wide spellings 0, `--tap` 0) plus diff review — the register's verdict, since D-12 rules out a committed gate |
| T-12-09 | Repudiation | the quoted Homebrew error still carrying the broad alternative, so a reader copies it "because the error said so" | medium | mitigate | the quote is trimmed to the narrow form with an ellipsis (Task 1, asserted exactly once) and the "run the command the error names" instruction is removed (asserted absent) |
| T-12-10 | Information Disclosure | README link to a generated file that could drift from the binary | low | accept | 12-01's `docs:cli:drift` (CI-wired) keeps the target current; README itself carries no CLI facts to go stale |
| T-12-11 | Tampering | STATE.md / todo files edited into a shape their tools no longer parse | low | mitigate | the todo moves via `gsd_run todo complete`; STATE.md changes are one row moved between two existing tables; heading counts asserted unchanged against `ca015c4b` |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | none — prose-only plan |
</threat_model>

<verification>
- `rg -o 'brew trust --cask seanb4t/tap/codegraph' docs/RELEASE.md | wc -l` ≥ 4; both tap-wide spellings and `--tap` absent from docs/RELEASE.md; `brew trust` absent from README.md
- `.planning/todos/pending/` holds one file (graphstore archtest); the brew-trust todo is in `completed/` with `status: completed`
- README.md links `docs/CLI-REFERENCE.md` exactly once, inside the Homebrew pointer paragraph; headings and the Commands table unchanged
- `git diff --quiet ca015c4b -- docs/CLI-REFERENCE.md internal Taskfile.yml .github` (nothing but prose moved)
</verification>

<success_criteria>
- ROADMAP criterion 3 (DOCS-07): `docs/RELEASE.md` recommends the narrow grant with one sentence of security framing rather than the broader grant; verified by reading the diff; no guard, no test
- D-06 delivered: one README link to the generated reference, no restructuring
- The 2026-08-10 todo is resolved through the tool in the commit that lands the wording (D-12)
</success_criteria>

## Artifacts this phase produces (complete list — Plan 02 lands the items marked ★; 12-01 landed the generator chain; 12-03 lands the close-out)

- Go `internal/cli` (12-01): `NewRootCmd()`; `cli_reference_test.go` (`TestEveryRegisteredFlagIsAccountedFor`); `testdata/cli-reference-allowlist.txt` (one entry: `codegraph man`)
- Go `tools/clidoc` (12-01): `main.go`
- Docs: `docs/CLI-REFERENCE.md` (12-01, generated); ★ `README.md` one link in the Homebrew paragraph; ★ `docs/RELEASE.md` brew-trust wording — narrow `--cask` grant, one framing sentence, tap-wide grant named not spelled, quoted error trimmed, blockquote parenthetical narrowed
- Taskfile (12-01): `docs:cli`, `docs:cli:drift`; CI (12-01): `CLI reference drift guard (DOCS-05)` step
- Planning: ★ `.planning/todos/completed/2026-08-10-brew-trust-…md` (via `gsd_run todo complete`); ★ STATE.md todo row moved to `Resolved and filed`; `12-MUTATION-LOG.md` families (a)(b)(c), `12-SECURITY.md`, `12-VALIDATION.md` filled (12-03)

<output>
Create `.planning/phases/12-cli-reference-docs-tail/12-02-SUMMARY.md` when done — include the full `docs/RELEASE.md` diff, the README diff, the todo-tool invocation and its output, and both commit SHAs.
</output>
