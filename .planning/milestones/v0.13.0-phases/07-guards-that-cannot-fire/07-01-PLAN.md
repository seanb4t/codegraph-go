---
phase: 07-guards-that-cannot-fire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/bench/regression.go
  - internal/bench/regression_test.go
  - .planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md
autonomous: true
requirements: [GRD-01, GRD-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CheckRegression(baseline, current, ceiling=1) with current.PeakRSSBytes = 0 and an otherwise-matching frame returns a non-nil error naming PeakRSSBytes, where the pre-fix build returned nil (ROADMAP criterion 1, GRD-01, D-10)"
    - "CheckRegression with current.FilesPerSec = 0 returns an error naming FilesPerSec as an invalid current reading, not the pre-fix 'throughput regressed 100%' message that misattributes a broken measurement to a real regression (D-10 companion case)"
    - "The current-metrics positivity checks sit immediately after the two baseline positivity checks and before the delta math, so frame attribution (platform, runner, scratch FS) is still evaluated first (D-10)"
    - "07-MUTATION-LOG.md exists and carries the GRD-01 family entry with verbatim pasted failing output from a real pre-fix run, a pre-mutation cleanliness gate, and a byte-clean proof, following 03-MUTATION-LOG.md's shape (GRD-06, D-11)"
  artifacts:
    - internal/bench/regression.go
    - internal/bench/regression_test.go
    - .planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md
  key_links:
    - "regression_test.go errHint strings must match the literal error text regression.go emits, or the table row asserts nothing"
    - "The RED transcript captured in Task 1 is the only source for the log entry's pasted output in Task 3 — it is not re-derived or paraphrased"
    - "tools/bench/runner/main.go:627 is the only caller of CheckRegression and already prints metrics on any gate error; it needs no change (D-10)"
  prohibitions:
    - statement: "The GRD-01 fix MUST NOT downgrade a degenerate current reading to a t.Skip, a log line, or a warning — it returns a non-nil error"
      status: flagged-unverified
      verification: "PITFALLS.md 999.4 names t.Skip-on-zero as the fix-pass anti-pattern; the table rows assert wantErr: true, so a skip or warning fails the test"
    - statement: "The GRD-01 RED case MUST NOT substitute a synthetic frame for the historical Phase 10 audit frame (ceiling=1, current.PeakRSSBytes = 0, otherwise-matching)"
      status: flagged-unverified
      verification: "The table row's ceiling field is the literal 1, not the file's ceiling constant (1_000_000_000); ROADMAP Notes and backlog 999.4 both require the historical frame"
    - statement: "The errHint for either new row MUST NOT be the bare substring 'current' — the pre-fix throughput-regression message already contains 'current=0.00 files/s', so a bare 'current' hint would match the wrong error and the row would pass for the wrong reason"
      status: flagged-unverified
      verification: "errHint values are 'invalid current: PeakRSSBytes' and 'invalid current: FilesPerSec'; the RED run in Task 1 proves the FilesPerSec row fails pre-fix despite the pre-fix build already returning an error"
    - statement: "No mutation-log entry may summarise, paraphrase, or elide the observed failing output in place of the verbatim transcript"
      status: flagged-unverified
      verification: "ROADMAP criterion 5 ('none of them summarised away'); Task 3 acceptance criteria require a fenced block containing the literal '--- FAIL' line"
    - statement: "No commit message in this phase may contain the literal ci-skip directive"
      status: flagged-unverified
      verification: "Project CLAUDE.md instruction; checked by reading the commit log at phase close"
---

<objective>
Close the degenerate-current-input bypass in `internal/bench.CheckRegression` (GRD-01, backlog 999.4), test-first: the two table cases land and are watched fail against the unmodified production function before the fix is written, and that observed failure is the first entry in this phase's mutation log.

This is the phase's tracer. It is the thinnest path that touches every layer the phase will use — a failing test, the production change that makes it pass, and a committed mutation-log entry carrying the verbatim transcript — proving the RED → GREEN → log loop end to end before the other three guards repeat it.

Purpose: `CheckRegression` is exported, its doc comment claims it never misleads, and today a zero current peak-RSS reading passes BOTH the relative RSS check and the absolute INDX-06 ceiling silently. A broken measurement currently reads as "no regression".
Output: two new table rows, two new production error paths, and `07-MUTATION-LOG.md` with its header and family (a) entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-guards-that-cannot-fire/07-CONTEXT.md
@.planning/phases/07-guards-that-cannot-fire/07-PATTERNS.md
</context>

<flagged_assumptions>
Surfaced from the spec-less probe fallback (§C) — every unresolved edge is recorded here rather than dropped. Four items belong to this plan (GRD-01 x2, GRD-06 x2).

1. **GRD-01 / boundary sign.** The check is `<= 0`, not `< 0` — a zero reading is as degenerate as a negative one and is the exact historical case. Assumed, mirroring the existing baseline lines verbatim. Not independently specified anywhere.
2. **GRD-01 / ordering against frame attribution.** A frame with BOTH a platform mismatch AND a zero current RSS reports the platform mismatch, not the degenerate reading, because D-10 places the new lines after the attribution checks. Assumed acceptable: attribution is a category error and outranks numeric validity. No source artifact states which error wins.
3. **GRD-06 / log location and ownership.** `07-MUTATION-LOG.md` is created by this plan and appended to by plans 07-02, 07-03, 07-04. No source artifact says who creates it; assumed the first guard to run does.
4. **GRD-06 / "byte-clean proof" for a fix that is not a revert.** GRD-01's RED is watched against the pre-fix build rather than against a mutation of a tracked file, so there is nothing to revert. Assumed: the entry records the pre-fix cleanliness gate on `internal/bench/regression.go` and the green re-run after the fix, and states explicitly that this family has no revert step because the RED was the absence of the fix. This is a shape deviation from 03-MUTATION-LOG.md and is recorded as such in the entry.
</flagged_assumptions>

<artifacts_this_phase_produces>
Phase-wide symbol inventory. Items marked **[this plan]** are produced here.

| Symbol / path | Kind | Plan |
|---|---|---|
| `internal/bench/regression.go` error `bench: invalid current: FilesPerSec must be positive, got %.4f` | new error string **[this plan]** | 07-01 |
| `internal/bench/regression.go` error `bench: invalid current: PeakRSSBytes must be positive, got %d` | new error string **[this plan]** | 07-01 |
| `TestCheckRegression` table rows `degenerate current PeakRSSBytes …` / `degenerate current FilesPerSec …` | new test cases **[this plan]** | 07-01 |
| `.planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md` | new evidence artifact **[this plan creates; 02/03/04 append]** | 07-01..04 |
| `internal/query/archtest/` (package `archtest`) | new package | 07-02 |
| `TestQueryImportsNoWireLayerOrIndexerRoot` | new test function | 07-02 |
| `scripts/inject-cosign-key.sh` (argv: `<committed-config> <generated-config> <cosign-key>`) | new script | 07-03 |
| `fullWorkflowJob.If` (`yaml:"if"`) | new struct field | 07-04 |
| `postReleaseConclusionGuard` const | new const | 07-04 |
| `TestPostReleaseJobsDeclareConclusionGuard` + `_EmptyDocIsError` | new test functions | 07-04 |
| `TestHomebrewTapAppSecretsDistinctFromReleasePleaseAppSecrets` | **removal** (D-09) | 07-04 |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED — land the two degenerate-current cases and watch them fail against the unfixed CheckRegression</name>
  <files>internal/bench/regression_test.go</files>
  <read_first>
    - `internal/bench/regression_test.go` — the whole file: the `tests := []struct{...}` shape (name/baseline/current/ceiling/wantErr/errHint), `defaultBaseline` (FilesPerSec 100.0, PeakRSSBytes 500_000_000), the `const ceiling = int64(1_000_000_000)`, the `b == (Metrics{})` zero-baseline fallback, and the assertion loop's `containsFold(err.Error(), tt.errHint)` check
    - `internal/bench/regression.go` — the current (unfixed) body of `CheckRegression`: the frame-attribution checks, the two baseline positivity checks, the throughput delta, the relative RSS delta, and the absolute ceiling check. This is the code the new rows must fail against.
    - `.planning/ROADMAP.md` §Backlog 999.4 — the exact reproduction the RED case replays
    - `.planning/phases/07-guards-that-cannot-fire/07-PATTERNS.md` §`internal/bench/regression_test.go` — the table row shape and the instruction to use `ceiling: 1` for the historical replay rather than the file's `ceiling` constant
  </read_first>
  <behavior>
    - Test 1 (the historical Phase 10 audit frame): baseline is the zero `Metrics{}` sentinel so the loop substitutes `defaultBaseline`; `current` is `FilesPerSec: 100.0, PeakRSSBytes: 0`; `ceiling: 1`; `wantErr: true`; `errHint: "invalid current: PeakRSSBytes"`. Pre-fix this returns nil (throughput delta 0, RSS delta -1, and 0 is not greater than the ceiling of 1), so the row fails on `CheckRegression() = nil, want error`.
    - Test 2 (companion): baseline is the zero sentinel; `current` is `FilesPerSec: 0, PeakRSSBytes: 500_000_000`; `ceiling` is the file's existing `ceiling` constant; `wantErr: true`; `errHint: "invalid current: FilesPerSec"`. Pre-fix this DOES return an error, but the wrong one — `bench: throughput regressed 100.0% …` — so the row fails on the errHint mismatch, not on a nil error. That difference is the point: a broken measurement must not be reported as a real regression.
  </behavior>
  <action>
    Append exactly two rows to the existing `tests` slice in `TestCheckRegression`, immediately after the existing degenerate-baseline rows (the ones with `errHint: "baseline"`) so the degenerate-input cases sit together. Do not add a new test function, a new table, or a new helper — the existing assertion loop and `containsFold` already carry both rows for free.

    Name the rows `"degenerate current PeakRSSBytes is refused rather than read as no regression"` and `"degenerate current FilesPerSec is refused rather than reported as a throughput regression"`. Both names begin with the words `degenerate current`, so Go's subtest naming yields the shared prefix `degenerate_current_` that this task's verify command counts.

    Set the fields exactly as the `<behavior>` block specifies. The first row's `ceiling` field is the literal `1`, NOT the file's `ceiling` constant — backlog 999.4 and ROADMAP Notes both require the historical frame, and a fresh synthetic ceiling would not reproduce the reported bypass.

    Set `errHint` to `"invalid current: PeakRSSBytes"` and `"invalid current: FilesPerSec"` respectively. These are prefixes of the literal error text Task 2 will emit. A shorter hint such as the bare word `current` is not acceptable: the pre-fix throughput-regression message already contains `current=0.00 files/s`, so a bare hint would match the wrong error and the second row would pass against the unfixed build.

    Add a short comment above the pair naming backlog 999.4 and stating that the first row is the reproduced Phase 10 audit frame, not a synthetic one. Do NOT modify `internal/bench/regression.go` in this task — the whole point is to watch these rows fail against the shipped function.

    Run the test and capture the full verbatim output (both `--- FAIL` subtest lines and the trailing `FAIL` summary line) into the scratch directory as `red-grd-01.txt`. Task 3 pastes that transcript into the mutation log; it is not re-derived or paraphrased later.

    Before running, assert `git diff --quiet -- internal/bench/regression.go` exits 0, and record that result — it is this family's pre-mutation cleanliness gate and proves the RED was watched against the committed production code rather than a locally edited one.
  </action>
  <acceptance_criteria>
    - `git diff --quiet -- internal/bench/regression.go` exits 0 immediately before the RED run, and that fact is recorded for Task 3.
    - `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/bench/ -run TestCheckRegression` exits non-zero and its output contains exactly two lines matching `--- FAIL: TestCheckRegression/degenerate_current`.
    - The first failure line reports `CheckRegression() = nil, want error` (the PeakRSSBytes row got nil from the unfixed build).
    - The second failure line reports an errHint mismatch quoting `throughput regressed` (the FilesPerSec row got the wrong error, not no error).
    - `internal/bench/regression.go` is byte-identical to HEAD at the moment of the RED run.
    - The verbatim transcript is saved to the scratch directory as `red-grd-01.txt`.
    - `rg -c 'ceiling: 1,' internal/bench/regression_test.go` returns at least 1, confirming the historical ceiling was used rather than the file constant.
  </acceptance_criteria>
  <verify>
    <automated>test "$(GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/bench/ -run TestCheckRegression 2>&1 | rg -c -- '--- FAIL: TestCheckRegression/degenerate_current' || true)" = "2"</automated>
    <fails_when>non-zero exit, meaning the counted number of failing `degenerate_current` subtests was not exactly 2 — either both new rows passed against the unfixed build (the rows are not discriminating and assert nothing), or only one of the two failed (the other row is vacuous)</fails_when>
    <automated>git diff --quiet -- internal/bench/regression.go</automated>
    <fails_when>non-zero exit, meaning `regression.go` was edited before the RED run and the observed failure cannot be attributed to the shipped production code</fails_when>
  </verify>
  <done>Two new table rows exist, both fail against the unmodified `CheckRegression`, the failure modes are the two distinct ones described in `<behavior>`, and the verbatim transcript is saved for Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — add the two current-metrics positivity checks and extend the function doc</name>
  <files>internal/bench/regression.go</files>
  <read_first>
    - `internal/bench/regression.go` — specifically the two baseline positivity checks (`baseline.FilesPerSec <= 0` and `baseline.PeakRSSBytes <= 0`) that the new lines mirror, the throughput/RSS delta math immediately below them, and the `CheckRegression` doc comment's sentence beginning "CheckRegression NEVER mutates baseline or current, and it never panics: a degenerate baseline …"
    - `internal/bench/regression_test.go` — the two rows added in Task 1, to confirm the emitted error strings match the errHint prefixes exactly
    - `.planning/phases/07-guards-that-cannot-fire/07-PATTERNS.md` §`internal/bench/regression.go` — the exact target lines and the `bench: invalid <side>: <Field> must be positive, got <value>` wording convention
  </read_first>
  <action>
    Insert two checks into `CheckRegression`, immediately after the `baseline.PeakRSSBytes <= 0` check and before the `throughputDelta` assignment. Per D-10 the frame-attribution checks (platform, runner, scratch FS) stay first and this ordering is not to be rearranged.

    Each check gets its own `if` and its own returned error naming one field and its value, mirroring the two baseline lines exactly: `current.FilesPerSec <= 0` returns `bench: invalid current: FilesPerSec must be positive, got %.4f` formatted with `current.FilesPerSec`, and `current.PeakRSSBytes <= 0` returns `bench: invalid current: PeakRSSBytes must be positive, got %d` formatted with `current.PeakRSSBytes`. Order them FilesPerSec first then PeakRSSBytes, matching the baseline pair's order. Do not collapse them into one combined check — one error per field is what makes the message name the degenerate reading.

    Extend the existing doc-comment sentence about a degenerate baseline so it also covers a degenerate *current* reading: state that a non-positive current throughput or peak-RSS value is refused as an unusable measurement rather than passing as "no regression", and name backlog 999.4 as the reproduction. Keep the existing sentence's claims intact.

    Do not touch `tools/bench/runner/main.go` — it is the only caller, and it already wraps and prints metrics on any gate error.
  </action>
  <acceptance_criteria>
    - `rg -v '^\s*//' internal/bench/regression.go | rg -c 'bench: invalid current: FilesPerSec must be positive'` returns 1.
    - `rg -v '^\s*//' internal/bench/regression.go | rg -c 'bench: invalid current: PeakRSSBytes must be positive'` returns 1.
    - Both new checks appear after the line containing `invalid baseline: PeakRSSBytes` and before the line assigning `throughputDelta`, verifiable by line numbers from `rg -n`.
    - `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/bench/...` passes, including both Task 1 rows.
    - `GOTOOLCHAIN=go1.26.6 go build ./...` succeeds with no change to `tools/bench/runner/main.go`.
    - The `CheckRegression` doc comment mentions a degenerate current reading and cites 999.4.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/bench/...</automated>
    <fails_when>non-zero exit, or `--- FAIL` appears in the output — the two degenerate-current rows still do not get the error they assert, or an existing row regressed</fails_when>
    <automated>test "$(rg -v '^\s*//' internal/bench/regression.go | rg -c 'bench: invalid current: (FilesPerSec|PeakRSSBytes) must be positive' || true)" = "2"</automated>
    <fails_when>non-zero exit, meaning the two error strings are not both present in non-comment source — they were merged into one check, reworded away from the convention, or left only in a comment</fails_when>
    <automated>git diff --quiet -- tools/bench/runner/main.go</automated>
    <fails_when>non-zero exit, meaning the only caller of CheckRegression was modified, which this plan forbids</fails_when>
  </verify>
  <done>`CheckRegression` refuses a non-positive current throughput or peak-RSS reading with an error naming the field, the two Task 1 rows pass, and the whole bench package is green.</done>
</task>

<task type="auto">
  <name>Task 3: Create 07-MUTATION-LOG.md with its header and the GRD-01 family entry</name>
  <files>.planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md</files>
  <read_first>
    - `.planning/milestones/v0.11.0-phases/03-non-vacuity-proof-unconditional-ci-execution/03-MUTATION-LOG.md` — the header block, the "Pre-mutation cleanliness gate" section, and family (a)'s full shape: test name line, mutation applied, pre-mutation gate, pasted observed failure in a fenced block, revert command, byte-clean proof
    - `.planning/phases/07-guards-that-cannot-fire/07-CONTEXT.md` §D-11 — four entries plus a one-line GRD-05 deletion record
    - `.planning/phases/07-guards-that-cannot-fire/07-PATTERNS.md` §Mutation-log structure — the per-entry template
    - the scratch file `red-grd-01.txt` saved by Task 1 — the transcript to paste
  </read_first>
  <action>
    Create `.planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md`.

    Write a header naming the phase (`07-guards-that-cannot-fire`), the date, and the scope: four RED demonstrations covering GRD-01 through GRD-04, plus a one-line record of the GRD-05 deletion. State that GRD-05 is a deletion decision (D-09) and deliberately carries no demonstration.

    Write a "Pre-mutation cleanliness gate" header section stating the convention this log follows: before every tracked-file mutation AND before every revert, `git diff --quiet -- <file>` is asserted to exit 0, so no pre-existing tracked edit is overwritten and no revert is a destructive blind checkout.

    Write the family (a) section for GRD-01. Give it the test name `TestCheckRegression` and name the two subtests. Record the pre-mutation gate result from Task 1. Under "Mutation applied", state plainly that this family has NO tracked-file mutation and NO revert step, because the RED condition was the *absence* of the fix: the two table rows were committed first and watched fail against the byte-identical committed `internal/bench/regression.go`. Call this shape deviation out explicitly so a reader does not mistake the missing revert for an omission.

    Paste the transcript from `red-grd-01.txt` verbatim into a fenced block, including both `--- FAIL` subtest lines and the trailing `FAIL` package line. Do not summarise it, trim it to one line, or replace it with a count.

    Add the green re-run: the exact command and its `ok` output after Task 2's fix landed.

    Leave the file ready for plans 07-02, 07-03 and 07-04 to append families (b), (c) and (d) plus the GRD-05 record — end the file with a horizontal rule and nothing else, so an append is a pure addition.

    Commit this file: `commit_docs` is true for this project, so planning artifacts are committed alongside code.
  </action>
  <acceptance_criteria>
    - `.planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md` exists and is tracked by git.
    - The file contains a section heading for family (a) naming GRD-01.
    - The file contains a fenced code block whose content includes the literal `--- FAIL: TestCheckRegression/degenerate_current` at least twice.
    - The fenced block's content is byte-identical to the corresponding region of the scratch file `red-grd-01.txt`.
    - The file contains the phrase `git diff --quiet -- internal/bench/regression.go` recording the cleanliness gate.
    - The file explicitly states that family (a) has no revert step and why.
    - The file records the green re-run command and its `ok` result.
    - The file ends with a horizontal rule so 07-02 can append without editing existing content.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c -- '--- FAIL: TestCheckRegression/degenerate_current' .planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md || true)" -ge 2</automated>
    <fails_when>non-zero exit, meaning fewer than two verbatim failing-subtest lines are present — the transcript was summarised away, which ROADMAP criterion 5 forbids</fails_when>
    <automated>rg -q 'git diff --quiet -- internal/bench/regression.go' .planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md</automated>
    <fails_when>non-zero exit, meaning the pre-mutation cleanliness gate for this family is not recorded</fails_when>
    <automated>test -n "$(git ls-files -- .planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md)"</automated>
    <fails_when>empty output and non-zero exit, meaning the log was written but never staged/committed, so the evidence is not actually in the repo</fails_when>
  </verify>
  <done>`07-MUTATION-LOG.md` is committed with its header, the cleanliness-gate convention, and a complete family (a) entry carrying verbatim pre-fix failing output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark harness → `CheckRegression` | An untrusted numeric measurement (possibly degenerate: 0, negative, or from a broken probe) crosses into a gate that decides pass/fail for the PERF-02 and INDX-06 budgets |
| RED transcript → mutation log | Text produced by a test run crosses into a committed evidence artifact that later readers treat as proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `CheckRegression` current-metrics path | high | mitigate | Positive-value checks on both current fields, each returning its own named error; proven by two table rows watched failing against the pre-fix build (Task 1) rather than added and assumed |
| T-07-02 | Repudiation | GRD-01 fix that is itself vacuous | high | mitigate | The fix is inadmissible unless the RED run in Task 1 produced exactly two failing `degenerate_current` subtests against a byte-identical `regression.go`; a `t.Skip`, a warning, or a floor that some other measurement bug also satisfies would not produce that transcript |
| T-07-03 | Information disclosure | mutation-log transcript | low | accept | The transcript contains only test names, error strings and byte counts from a local run; no credentials, paths outside the repo, or environment values are pasted. Accepted rather than redacted |
| T-07-04 | Tampering | errHint too weak to discriminate | high | mitigate | errHint is the multi-word prefix `invalid current: <Field>`; the bare word `current` is explicitly forbidden because the pre-fix throughput message contains `current=0.00 files/s` and would satisfy it |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. `golang.org/x/tools` is already a direct go.mod dependency used by `internal/graphstore/archtest`; no new module is added anywhere in this phase, so no package-legitimacy checkpoint is required |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/bench/...` is green after Task 2.
- `GOTOOLCHAIN=go1.26.6 go build ./...` succeeds.
- `git diff --stat -- tools/bench/runner/main.go` is empty (the only caller is untouched).
- `07-MUTATION-LOG.md` is committed and carries a complete family (a) entry with pasted verbatim pre-fix output.
- No `go.mod`/`go.sum` change: `git diff --quiet -- go.mod go.sum` exits 0.
</verification>

<success_criteria>
1. `CheckRegression(defaultBaseline, Metrics{FilesPerSec: 100.0, PeakRSSBytes: 0}, 1)` returns a non-nil error naming `PeakRSSBytes`, where the pre-fix build returned nil.
2. `CheckRegression` with `current.FilesPerSec = 0` returns an error naming `FilesPerSec` as an invalid current reading rather than a 100% throughput regression.
3. Both behaviours were watched failing against the byte-identical committed `regression.go` before the fix landed, and that failure is pasted verbatim in `07-MUTATION-LOG.md`.
4. `07-MUTATION-LOG.md` exists, is committed, and is ready for three appended families.
</success_criteria>

<output>
Create `.planning/phases/07-guards-that-cannot-fire/07-01-SUMMARY.md` when done.
</output>
