---
phase: 11-graph-view-community-clustering
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md
  - .planning/phases/11-graph-view-community-clustering/11-SECURITY.md
  - .planning/phases/11-graph-view-community-clustering/11-VALIDATION.md
autonomous: true
requirements: [GRF-06, GRF-08, GRF-09, GRF-10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`11-MUTATION-LOG.md` follows the 10-MUTATION-LOG house format (header Phase/Date/Scope; the pre-mutation cleanliness gate section; per family `**Instrument:**`, `**Pre-mutation cleanliness gate:**`, the mutation as a diff, the RED output pasted verbatim, the revert with a second `git diff --quiet` exit 0) for four families: (a) GRF-08 — the fixed seed replaced by a per-call counter so run 2 uses the flipping offset, `TestAssignCommunitiesDeterministic/tie` RED; (b) GRF-09 — `max` widened 500 → 5000 in the working tree, `TestClusterThresholdDigestMatchesCommittedObservation` RED; (c) GRF-06 — `name: 'cose'` planted in a tracked `web/src` file, `check-no-force-layout.mjs` RED; (d) GRF-10 — `check:gonum`'s cgo positive control swapped to `github.com/spf13/cobra`, the target RED on `positive control found no cgo` (D-16)"
    - "A family that does NOT go RED is reported as an observed negative with its root cause (08-MUTATION-LOG family (d) precedent) — never rewritten until it fails; family (a)'s `structured` sub-test staying green under the counter mutation is EXPECTED and recorded as the seed-invariance of a well-separated graph, with the `tie` sub-test carrying the RED"
    - "`11-SECURITY.md` is in the 10-SECURITY house format (frontmatter `phase`, `slug`, `status`, `threats_open`, `asvs_level: 1`, `created`; Register origin paragraph; Trust Boundaries table with the one-sentence boundary statement; Threat Register where EVERY T-11-xx row from Plans 01–04 carries a named test, a verify command, or an explicit verdict; Accepted Risks; Notes) — `threats_open` is the honest count of OPEN rows at or above `high`, which must be 0 only because T-11-01, T-11-02 and T-11-10 each cite the landed gate that closed them; the GRF-10 `[ASSUMED]` legitimacy assumption is recorded verbatim under T-11-01 / Notes"
    - "`11-VALIDATION.md`'s Per-Task Verification Map has every `TBD` cell replaced with the real plan / task / test names and commands that landed (values only — no new headings, columns or shapes), every Wave 0 item that landed is ticked, `wave_0_complete: true` and `nyquist_compliant: true` are set, and `status` is left for validate-phase"
    - "Phase-close gate at the final commit: `GOTOOLCHAIN=go1.26.6 go vet ./...`, `go build ./...`, `task test:unit`, `task test:golden`, `task lint:go`, `task proto:drift`, `task -s web:drift` (both halves MATCH), `task web:test`, `pnpm -C web check` (0 errors), `GOTOOLCHAIN=go1.26.6 task -s check:gonum` (PASS), `task -s check:no-force-layout` (PASS), and the two `tools/graphcluster` ancestry/digest tests PASS — tails pasted in the SUMMARY"
    - "The threshold still has exactly one commit and a clean working tree at the final commit; no mutation from any family was committed (`git status --porcelain` empty except the three planning artefacts before their own commit)"
  artifacts:
    - ".planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md"
    - ".planning/phases/11-graph-view-community-clustering/11-SECURITY.md"
    - ".planning/phases/11-graph-view-community-clustering/11-VALIDATION.md (map filled, Wave 0 ticked)"
  key_links:
    - "family (a) → `internal/query/community.go` `AssignCommunities`/`communityDefaults` → `TestAssignCommunitiesDeterministic` (`mutation: per-call seed counter` anchor)"
    - "family (b) → `corpora/graph-cluster-threshold.json` `max` → `TestClusterThresholdDigestMatchesCommittedObservation` (`mutation: widen max after measurement` anchor)"
    - "family (c) → a tracked `web/src` file → `web/scripts/check-no-force-layout.mjs` exit 1 naming file:line"
    - "family (d) → `Taskfile.yml` `check:gonum` control package → `task -s check:gonum` exit 1 with `positive control found no cgo`"
  prohibitions:
    - "No mutation may be committed — every family ends with `git diff --quiet -- <file>` exit 0 and `git status --porcelain` clean for that path"
    - "A non-failing mutation MUST NOT be rewritten until it fails — report it as an observed negative with root cause"
    - "`11-VALIDATION.md`, `STATE.md` and `ROADMAP.md` MUST NOT gain invented headings, columns or shapes — values only in tool-owned files"
    - "The threshold file MUST NOT be edited except transiently by family (b), reverted byte-clean before anything else runs"
---

<objective>
Close the phase with evidence: four hand-authored RED demonstrations proving the phase's guards discriminate (seed determinism, threshold digest, force-layout scan, cgo positive control), the security register with every threat row test-or-verdict and the GRF-10 legitimacy assumption recorded honestly, the validation map filled with what actually landed, and the full gate suite green at the final commit.

Purpose: D-16 is the phase's own proof that its guards cannot go vacuous — a guard never watched failing is a guard whose green means nothing (STATE.md standing decision).

Output: `11-MUTATION-LOG.md`, `11-SECURITY.md`, `11-VALIDATION.md` map filled, phase-close transcripts in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-graph-view-community-clustering/11-CONTEXT.md
@.planning/phases/11-graph-view-community-clustering/11-VALIDATION.md
@.planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md
@.planning/phases/10-index-health-the-coverage-denominator/10-SECURITY.md
@.planning/phases/11-graph-view-community-clustering/11-01-SUMMARY.md
@.planning/phases/11-graph-view-community-clustering/11-02-SUMMARY.md
@.planning/phases/11-graph-view-community-clustering/11-03-SUMMARY.md
@.planning/phases/11-graph-view-community-clustering/11-04-SUMMARY.md

Every Go command: `GOTOOLCHAIN=go1.26.6`. Mutations are applied to tracked files with an editor, run, pasted, and reverted with `git checkout -- <file>`; agent commits MAY use `-c commit.gpgsign=false`; never `[ci skip]`. If 11-02's verdict was FAIL, family (a)'s instrument is `./internal/community/...` (the promoted leaf package) and its file is `internal/community/community.go` — adjust the paths, keep the shape.
</context>

<tasks>

<task type="auto">
  <name>Task 1: `11-MUTATION-LOG.md` — families (a) seed counter, (b) threshold widened, (c) force-layout name planted, (d) cgo control neutered — each RED then byte-clean reverted (D-16)</name>
  <reversibility rating="reversible">Documentation of temporary, reverted mutations; nothing persists in source.</reversibility>
  <files>.planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md</files>
  <read_first>
    - .planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md (whole — header, pre-mutation cleanliness gate section, per-family Instrument / cleanliness gate / Mutation applied (diff) / RED pasted verbatim / Revert structure)
    - .planning/phases/08-tmux-real-pty-harness/08-MUTATION-LOG.md family (d) (how a non-failing demonstration is recorded honestly) — load only that section
    - internal/query/community.go (`AssignCommunities`, `communityDefaults`, `communitySeed1`), internal/query/community_test.go (`TestAssignCommunitiesDeterministic`'s `tie` sub-test with the `mutation: per-call seed counter` anchor; the flipping offset 11-01 Task 3 logged)
    - tools/graphcluster/ancestry_test.go (`TestClusterThresholdDigestMatchesCommittedObservation`, `mutation: widen max after measurement` anchor), corpora/graph-cluster-threshold.json (`"max": 500`)
    - web/scripts/check-no-force-layout.mjs (the scan; 11-04's planted-file RED transcript in 11-04-SUMMARY.md)
    - Taskfile.yml `check:gonum` (the control line `go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}' github.com/tree-sitter/go-tree-sitter`; 11-03-SUMMARY.md's (d1) rehearsal)
  </read_first>
  <action>
    Author `11-MUTATION-LOG.md` in the 10-MUTATION-LOG house format (header `# 11-MUTATION-LOG — Graph View: Community Clustering`, Phase/Date/Scope naming the four families, the cleanliness-gate convention section with a real `git diff --quiet -- <the four files>; echo $?` = 0 transcript). Then, one family at a time, gate → mutate → run → paste → revert → gate:

    (a) GRF-08 seed determinism. Cleanliness gate on `internal/query/community.go`. Mutation: add `var communityCallCounter uint64` and change `AssignCommunities` to call `assignCommunitiesWith(nodes, edges, communityOptions{seed1: communitySeed1 + communityCallCounter, seed2: communitySeed2})` with `communityCallCounter++` before the call — run 1 uses offset 0, run 2 offset 1 (the flipping offset 11-01 Task 3 logged; if the logged offset was k > 1, increment by k instead so run 2 lands on it). Instrument: `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestAssignCommunitiesDeterministic' ./internal/query/`. Expected: `--- FAIL: TestAssignCommunitiesDeterministic/tie` (element-for-element mismatch on run 2) while `structured` stays PASS — paste both lines and explain the `structured` green as the documented seed-invariance of a well-separated graph (11-01 Task 3's second assertion). Revert `git checkout -- internal/query/community.go`; gate; re-run GREEN.

    (b) GRF-09 threshold widened after measurement. Cleanliness gate on `corpora/graph-cluster-threshold.json`. Mutation: `"max": 500` → `"max": 5000` (a widening, exactly what the protocol forbids). Instrument: `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestClusterThresholdDigestMatchesCommittedObservation' ./tools/graphcluster/`. Expected: RED with the digest mismatch naming both digests. Also show `git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l` = 1 before and after (the mutation was never committed). Revert; gate; GREEN.

    (c) GRF-06 force-layout name planted. Cleanliness gate on `web/src/lib/components/graph/graph-style.ts`. Mutation: append the line `const plantedLayout = { name: 'cose' };` (a real layout-name position in a tracked file — not the untracked file 11-04 used). Instrument: `node web/scripts/check-no-force-layout.mjs; echo "exit=$?"`. Expected: exit 1, `forbidden matches 1`, the file and line named. Revert; gate; re-run PASS.

    (d) GRF-10 cgo positive control neutered. Cleanliness gate on `Taskfile.yml`. Mutation: in `check:gonum`, replace the control package `github.com/tree-sitter/go-tree-sitter` with `github.com/spf13/cobra` (pure Go). Instrument: `GOTOOLCHAIN=go1.26.6 task -s check:gonum; echo "exit=$?"`. Expected: exit 1 with `positive control found no cgo` (the first two halves still print their lines — paste them too, so the log shows the target reached the third half). Revert; gate; re-run `check:gonum: PASS`.

    Close with a `## Closing` section: all four files `git diff --quiet` exit 0, `git status --porcelain` shows only the new log. Commit the log alone: `docs(11-05): 11-MUTATION-LOG.md — four families RED and reverted`.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md &amp;&amp; test "$(rg -o '^## Family \([abcd]\)' .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o '\*\*Instrument:\*\*' .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o '\*\*Pre-mutation cleanliness gate:\*\*' .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md | wc -l | tr -d ' ')" -ge 4 &amp;&amp; test "$(rg -o -- '--- FAIL: TestAssignCommunitiesDeterministic/tie' .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o -- '--- FAIL: TestClusterThresholdDigestMatchesCommittedObservation' .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'forbidden matches 1' .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'positive control found no cgo' .planning/phases/11-graph-view-community-clustering/11-MUTATION-LOG.md | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>the log lacks four family sections with instruments and cleanliness gates, or any family's expected RED line is not pasted (a family that did not go RED must instead carry an "observed negative" paragraph — if so, this gate is adjusted ONLY by the executor recording the negative honestly, never by weakening the guard)</fails_when>
    <automated>git diff --quiet -- internal/query/community.go corpora/graph-cluster-threshold.json web/src/lib/components/graph/graph-style.ts Taskfile.yml &amp;&amp; test "$(git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l | tr -d ' ')" = "1" &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestAssignCommunitiesDeterministic|TestClusterThresholdDigestMatchesCommittedObservation' ./internal/query/... ./tools/graphcluster/... 2&gt;&amp;1 | tee /tmp/11-05-revert.txt | tail -3; test "$(rg -o '^ok' /tmp/11-05-revert.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; node web/scripts/check-no-force-layout.mjs &gt;/dev/null 2&gt;&amp;1</automated>
    <fails_when>any mutated file still differs from HEAD (a mutation leaked), the threshold gained a commit, or the instruments are not green again after the reverts</fails_when>
  </verify>
  <acceptance_criteria>
    - Each family section shows the cleanliness gate BEFORE the mutation and BEFORE the revert, both `0`
    - Family (a) explicitly records the `structured` sub-test staying PASS under the counter mutation and why
    - Family (d) pastes the first two halves' reporting lines before the failing third half
    - The log's Closing section shows `git status --porcelain` with only the new log file
  </acceptance_criteria>
  <done>Four guards have been watched failing against confirmed-applied mutations and restored byte-clean, with every transcript pasted — the phase's proof that its gates discriminate.</done>
</task>

<task type="auto">
  <name>Task 2: `11-SECURITY.md` (every threat row test-or-verdict, `threats_open` honest), `11-VALIDATION.md` map filled (values only), and the phase-close gate green at the final commit</name>
  <reversibility rating="reversible">Planning artefacts and a gate run.</reversibility>
  <files>.planning/phases/11-graph-view-community-clustering/11-SECURITY.md, .planning/phases/11-graph-view-community-clustering/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-index-health-the-coverage-denominator/10-SECURITY.md (whole — frontmatter keys, Register origin paragraph, Trust Boundaries table + boundary sentence, Threat Register columns incl. Status, Accepted Risks, Notes; how colliding ids across plans were merged into single rows citing each plan)
    - The `<threat_model>` blocks of 11-01-PLAN.md, 11-02-PLAN.md, 11-03-PLAN.md, 11-04-PLAN.md (T-11-01 … T-11-14, T-11-SC; T-11-09 applies only if the FAIL branch ran)
    - .planning/phases/11-graph-view-community-clustering/11-VALIDATION.md (the Per-Task Verification Map's `TBD` cells and the Wave 0 checklist; the Validation Sign-Off list)
    - .planning/phases/11-graph-view-community-clustering/11-0{1,2,3,4}-SUMMARY.md (the real test names, commands, commit SHAs, the GRF-09 verdict and which Task 3 branch ran)
    - .planning/phases/11-graph-view-community-clustering/11-RESEARCH.md § Package Legitimacy Audit and § Security Domain (the `[ASSUMED]` legitimacy row and the V6 "not a secret" note to carry into the register)
  </read_first>
  <action>
    Author `11-SECURITY.md` in the 10-SECURITY house format: frontmatter (`phase: "11"`, `slug: "graph-view-community-clustering"`, `status: verified`, `threats_open: <honest count of OPEN rows at severity >= high>`, `asvs_level: 1`, `created: <today>`); Register origin (`register_authored_at_plan_time: true`, four plans carry `<threat_model>`; union deduplicated by id, merged mitigation text tagged `(11-0N)`); Trust Boundaries: (1) store snapshot → `FileGraph()` → `AssignCommunities` (no external input — the store's own file paths and counts, D-16); (2) `gonum.org/v1/gonum` import closure → `internal/query` (the new trust surface; cgo absence, GRF-10); (3) `corpora/graph-cluster-threshold.json` → `tools/graphcluster` (the tamper target; digest + ancestry); (4) `internal/uiserver` → browser (`community_id`/`community_count`, read-only, no new input); (5) `web/src` → `check-no-force-layout.mjs`; boundary sentence: the phase's new trust surface is a numerical dependency executing inside a read-only query over the store's own data, and a committed bar that must outlive the measurement it judges. Threat Register rows T-11-01 … T-11-14 and T-11-SC, each with Severity, Disposition, the Mitigation citing the landed test/verify/target by name and plan tag, and Status `closed` / `closed (accepted)` / `open` — T-11-09 present only if 11-02 Task 3 ran (else a one-line Notes entry "not applicable — verdict PASS"). Accepted Risks: T-11-04, T-11-05 (as scoped), T-11-06, T-11-SC. Notes: the GRF-10 package-legitimacy assumption recorded verbatim (`[ASSUMED]`, evidence, no seam for Go modules, no checkpoint in an autonomous run — an explicit unresolved edge row, not a resolved one); the seed constants are determinism constants, not secrets; the GRF-09 verdict and which branch ran; CI wiring of `check:gonum`/`check:no-force-layout` recorded as NOT done this phase (local gates; ci.yml's blocking govulncheck job remains the CI supply-chain gate) so it is a visible follow-up, not a silent gap.

    Fill `11-VALIDATION.md` (values only): every `TBD` in the Per-Task Verification Map replaced with the real Plan/Task/test names and commands from the SUMMARIES (e.g. `11-02 / T2 / TestClusterThresholdCommitIsAncestorOfEveryMeasurement`, `11-03 / T1 / task -s check:gonum`, `11-01 / T2+T3 / TestAssignCommunitiesDeterministic`, `11-04 / T3 / node web/scripts/check-no-force-layout.mjs`, `11-04 / T2 / pnpm -C web test -- graph-communities`); the illustrative test names replaced by the ones that exist (`TestAssignCommunitiesDegenerate` stays; `TestFileGraphPopulatesCommunityFields` stays; `graph-page` → `graph-communities`); the `File Exists` column set to ✅ and `Status` to ✅ green for every row whose gate passed in the phase-close run below; Wave 0 checklist items ticked; frontmatter `wave_0_complete: true`, `nyquist_compliant: true`; `status` untouched (validate-phase owns it). Do not add headings, columns, or rows of a new shape.

    Then run the phase-close gate in this order and paste every tail in the SUMMARY: `GOTOOLCHAIN=go1.26.6 go vet ./...`; `GOTOOLCHAIN=go1.26.6 go build ./...`; `GOTOOLCHAIN=go1.26.6 task test:unit`; `GOTOOLCHAIN=go1.26.6 task test:golden`; `GOTOOLCHAIN=go1.26.6 task lint:go`; `GOTOOLCHAIN=go1.26.6 task proto:drift`; `task web:test`; `pnpm -C web check`; `task web:build && task -s web:drift`; `GOTOOLCHAIN=go1.26.6 task -s check:gonum`; `task -s check:no-force-layout`; `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestClusterThreshold' ./tools/graphcluster/...`. Commit the two artefacts: `docs(11-05): 11-SECURITY.md register and 11-VALIDATION.md map`.
  </action>
  <verify>
    <automated>F=.planning/phases/11-graph-view-community-clustering/11-SECURITY.md; test -f "$F" &amp;&amp; test "$(rg -o '^threats_open: [0-9]+' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^asvs_level: 1' "$F" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^\| T-11-(0[1-8]|1[0-4]|SC) ' "$F" | wc -l | tr -d ' ')" -ge 13 &amp;&amp; test "$(rg -o '## Trust Boundaries|## Threat Register|## Accepted Risks|## Notes' "$F" | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(rg -o '\[ASSUMED\]' "$F" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'not a secret|determinism constant' "$F" | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '\| high \| mitigate \|[^|]*\| closed' "$F" | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test "$(rg -o '\| high \| [a-z]+ \|[^|]*\| open' "$F" | wc -l | tr -d ' ')" = "$(rg -o '^threats_open: [0-9]+' "$F" | rg -o '[0-9]+')"</automated>
    <fails_when>the register lacks the house sections, fewer than 13 T-11 rows, the `[ASSUMED]` legitimacy note or the seed-constant note, fewer than three closed high rows (T-11-01, T-11-02, T-11-10), or `threats_open` disagrees with the count of open high rows</fails_when>
    <automated>V=.planning/phases/11-graph-view-community-clustering/11-VALIDATION.md; test "$(rg -o '\| TBD \|' "$V" | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o '^wave_0_complete: true' "$V" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^nyquist_compliant: true' "$V" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^status: draft' "$V" | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '^- \[x\]' "$V" | wc -l | tr -d ' ')" -ge 9 &amp;&amp; test "$(rg -o '^## ' "$V" | wc -l | tr -d ' ')" = "$(git show "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)":.planning/phases/11-graph-view-community-clustering/11-VALIDATION.md | rg -o '^## ' | wc -l | tr -d ' ')"</automated>
    <fails_when>a `TBD` cell remains, the two frontmatter flags are not set, `status` was changed (validate-phase owns it), fewer than nine Wave 0 items are ticked, or the number of `##` headings changed (invented structure in a tool-owned file)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./... &amp;&amp; GOTOOLCHAIN=go1.26.6 go build ./... &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:unit &gt;/tmp/11-05-unit.txt 2&gt;&amp;1; RC=$?; tail -5 /tmp/11-05-unit.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^FAIL' /tmp/11-05-unit.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:golden &gt;/tmp/11-05-golden.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/11-05-golden.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^ok' /tmp/11-05-golden.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>vet/build fail, test:unit or test:golden exit non-zero or print a FAIL line (golden must print at least one `ok`)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task lint:go &gt;/tmp/11-05-lint.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/11-05-lint.txt; test "$RC" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task proto:drift &gt;/tmp/11-05-drift.txt 2&gt;&amp;1; RC=$?; tail -2 /tmp/11-05-drift.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i 'mismatch|drift detected' /tmp/11-05-drift.txt | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>golangci-lint reports an issue in the new Go files, or proto:drift finds a generated file out of step</fails_when>
    <automated>task web:test &gt;/tmp/11-05-webtest.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/11-05-webtest.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o 'web:test: PASS' /tmp/11-05-webtest.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; pnpm -C web check &gt;/tmp/11-05-check.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/11-05-check.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o -i '\b0 errors\b' /tmp/11-05-check.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; task web:build &amp;&amp; task -s web:drift &gt;/tmp/11-05-webdrift.txt 2&gt;&amp;1; RC=$?; test "$RC" = "0" &amp;&amp; test "$(rg -o '\bhalf MATCH\b' /tmp/11-05-webdrift.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test -z "$(git status --porcelain -- web/build web/src)"</automated>
    <fails_when>web:test is not PASS, svelte-check is not 0 errors, or web:drift does not MATCH both halves with a clean `web/build`/`web/src`</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task -s check:gonum &gt;/tmp/11-05-gonum.txt 2&gt;&amp;1; RC=$?; tail -5 /tmp/11-05-gonum.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^check:gonum: PASS$' /tmp/11-05-gonum.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; task -s check:no-force-layout &gt;/tmp/11-05-scan.txt 2&gt;&amp;1; RC=$?; tail -3 /tmp/11-05-scan.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o 'verdict PASS' /tmp/11-05-scan.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestClusterThreshold' ./tools/graphcluster/... 2&gt;&amp;1 | tee /tmp/11-05-anc.txt | tail -4; test "$(rg -o -- '--- PASS: TestClusterThreshold' /tmp/11-05-anc.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o -- '--- (FAIL|SKIP)' /tmp/11-05-anc.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l | tr -d ' ')" = "1" &amp;&amp; git diff --quiet -- corpora/graph-cluster-threshold.json</automated>
    <fails_when>either supply-chain/layout gate is not PASS, either ancestry/digest test is not PASS (a SKIP on this full clone is a failure), or the threshold's single-commit / clean-tree invariant broke at phase close</fails_when>
  </verify>
  <acceptance_criteria>
    - `11-SECURITY.md`'s `threats_open` equals the number of `open` rows at severity `high` (expected 0, with T-11-01/T-11-02/T-11-10 closed by name)
    - `11-SECURITY.md` Notes record: the GRF-10 `[ASSUMED]` legitimacy assumption, the GRF-09 verdict and branch, and the not-yet-CI-wired status of the two new targets
    - `11-VALIDATION.md` has zero `TBD`, ticked Wave 0, both flags true, `status: draft`, and its heading set unchanged
    - The SUMMARY pastes all twelve phase-close tails and states `git status --porcelain` is empty at the final commit
  </acceptance_criteria>
  <done>The register carries every threat with a named test or verdict and an honest open count, the validation map records what actually landed, and every gate in the phase — Go, web, drift, supply chain, layout scan, ancestry — is green at the final commit with the threshold still in its one, untouched commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan's transient mutations → tracked files | four deliberate, reverted edits; the risk is one leaking into a commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-15 | Tampering | a mutation from families (a)–(d) committed by accident | medium | mitigate | cleanliness gate before every mutation and revert; Task 1 verify asserts `git diff --quiet` on all four files and the threshold's single commit; Task 2's phase-close gate re-runs every guard green |
| T-11-16 | Repudiation | a `threats_open: 0` written without the closing evidence | medium | mitigate | Task 2 verify ties `threats_open` to the count of `open` high rows and requires three `high | mitigate | … | closed` rows |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `11-MUTATION-LOG.md`: four families, each RED pasted and reverted byte-clean
- `11-SECURITY.md`: every T-11 row test-or-verdict; `threats_open` honest
- `11-VALIDATION.md`: zero `TBD`, Wave 0 ticked, flags set, headings unchanged
- Phase-close gate: vet, build, test:unit, test:golden, lint:go, proto:drift, web:test, svelte-check, web:drift, check:gonum, check:no-force-layout, ancestry/digest tests — all green; threshold in one untouched commit
</verification>

<success_criteria>
- D-16 delivered: the phase's guards were each watched failing and restored; the threat register and validation contract reflect what landed; all four roadmap success criteria have their gates green at the final commit
</success_criteria>

<output>
Create `.planning/phases/11-graph-view-community-clustering/11-05-SUMMARY.md` when done — include the four family RED lines, the phase-close gate tails, `threats_open`, and the final commit SHA.
</output>
