---
phase: 11-graph-view-community-clustering
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - Taskfile.yml
autonomous: true
requirements: [GRF-10]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`GOTOOLCHAIN=go1.26.6 task -s check:gonum` exits 0 and prints `check:gonum: PASS` exactly once, preceded by three reporting lines — one per half — each carrying the count it inspected (GRF-10, D-14)"
    - "govulncheck half: a govulncheck binary built from `go.tool.mod` (`GOWORK=off go build -modfile=go.tool.mod … golang.org/x/vuln/cmd/govulncheck`, the `vuln:` precedent) runs in SOURCE mode over the MAIN module (`govulncheck ./...`, matching ci.yml's blocking job — never `-mode=binary`), and the target reports `N gonum packages in the scanned set` with `N >= 1` from `go list -deps ./cmd/codegraph` (positive control: gonum is actually in what was scanned)"
    - "SBOM half: the SBOM is generated the way the release does (`.goreleaser.yaml` `sboms:` — `syft <built binary> --output spdx-json=…`), the target reports the total package count (`> 0`), finds `\"name\": \"gonum.org/v1/gonum\"` exactly once with `versionInfo` `v0.17.0`, and finds the positive control `\"name\": \"github.com/cockroachdb/pebble/v2\"` exactly once — measured this session: syft over the built `codegraph` binary lists 147 Go-module packages by module path, and gonum is ABSENT until `internal/query` links it, so presence is a real signal"
    - "cgo half: `go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}' gonum.org/v1/gonum/graph/community` reports `N packages inspected` with `N >= 50` (research measured 91) and `0 with cgo`, and `gonum.org/v1/gonum/blas/cgo` appears nowhere in the closure; the positive control runs the SAME template over `github.com/tree-sitter/go-tree-sitter` and reports `>= 1 with cgo` (measured this session: 68 packages, 3 with cgo) — an empty or blind scan cannot read as clean"
    - "ASSUMPTION (GRF-10 edge row `unclassified`, unresolved — recorded, not resolved): `gonum.org/v1/gonum`'s package legitimacy stays `[ASSUMED]` — evidence: long-lived org-maintained project (github.com/gonum/gonum), already resolved transitively in this module's `go.sum` via sigstore before this phase, no version bump; the `gsd_run query package-legitimacy check` seam covers npm/pypi/crates only, so no machine verdict and no human-verify checkpoint exists for a Go module in an autonomous run; 11-05's `11-SECURITY.md` records this verbatim under T-11-01"
    - "The target is byte-honest about scope: its `desc` states that ci.yml's `govulncheck (DIST-03, blocking)` job stays the CI gate and that this target is the LOCAL, positive-controlled GRF-10 proof — it does not modify `vuln:` or ci.yml"
  artifacts:
    - "Taskfile.yml — `check:gonum` target (govulncheck half, SBOM half, cgo half, each with a positive control and a reported count)"
  key_links:
    - "`go.tool.mod` (pinned govulncheck) → `${scratch}/govulncheck ./...` → exit 0 required; `go list -deps ./cmd/codegraph | grep -o '^gonum.org/v1/gonum'` → N >= 1"
    - "`go build -o ${scratch}/codegraph ./cmd/codegraph` → `syft` spdx-json → `node -e` extraction of `gonum.org/v1/gonum` + `github.com/cockroachdb/pebble/v2` names/versions"
    - "`go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}' gonum.org/v1/gonum/graph/community` → awk non-zero count 0 of N; same template over `github.com/tree-sitter/go-tree-sitter` → non-zero count >= 1 (11-05 family (d) swaps the control package for a pure-Go one and watches the target fail)"
  prohibitions:
    - "`gonum`'s version MUST NOT be bumped and no other module may enter `go.mod`/`go.sum` in this plan (D-13)"
    - "The govulncheck half MUST NOT use `-mode=binary` or scan tool binaries — source mode over the main module only; the existing `vuln:` target and ci.yml MUST NOT be modified"
    - "No half may pass on an empty input: every count is printed BEFORE it is compared and each has a floor (`N >= 1` packages, SBOM packages `> 0`, closure `>= 50`)"
    - "The cgo half MUST NOT allowlist or exclude any package from the closure — `blas/cgo` absence is asserted, not assumed"
---

<objective>
Prove GRF-10 end-to-end as a single re-runnable Taskfile target: the milestone's only new direct require passes govulncheck in source mode over the main module, appears by name and version in an SBOM generated the way the release does, and its `graph/community` import closure is scanned for cgo with the package count reported and a positive control proving the scan can detect cgo when it is there.

Purpose: a supply-chain claim that was checked once by hand in research is not a gate; this target is the persisted, positive-controlled proof that can be re-run at every bump — and it runs before any UI colours depend on gonum, so a rejection here would route to D-01's zero-dependency fallback before 11-04.

Output: `Taskfile.yml` `check:gonum`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-graph-view-community-clustering/11-CONTEXT.md
@.planning/phases/11-graph-view-community-clustering/11-RESEARCH.md
@.planning/phases/11-graph-view-community-clustering/11-PATTERNS.md
@.planning/phases/11-graph-view-community-clustering/11-01-SUMMARY.md

Every `go`/`govulncheck` invocation: `GOTOOLCHAIN=go1.26.6` (the Taskfile inherits the caller's environment — invoke as `GOTOOLCHAIN=go1.26.6 task -s check:gonum`; `-s` so grep-counted output is not polluted by task's echo). `syft` is at `/opt/homebrew/bin/syft` (1.51.1) and `govulncheck` is built from `go.tool.mod` — never the ambient PATH binary. `.goreleaser.yaml:365-374` runs `syft $artifact --output spdx-json=$document` over each built binary — copy that shape over a locally built `./cmd/codegraph`. Measured this session: syft over the built binary yields 147 packages named by module path with `versionInfo` (e.g. `github.com/cockroachdb/pebble/v2` `v2.1.6`); `go list -deps` over `github.com/tree-sitter/go-tree-sitter` yields 68 packages, 3 with `CgoFiles`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: `check:gonum` Taskfile target — govulncheck (source mode, pinned build), SBOM presence with a known-module control, cgo-closure scan with a known-cgo control; every half reports its count before comparing (D-13, D-14, GRF-10)</name>
  <precondition>`command -v syft` succeeds and `GOTOOLCHAIN=go1.26.6 go list -m gonum.org/v1/gonum` prints `gonum.org/v1/gonum v0.17.0` (11-01 landed the direct require)</precondition>
  <reversibility rating="reversible">A Taskfile target; no contract.</reversibility>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml:1461-1546 (`vuln:` — the `GOWORK=off go build -modfile=go.tool.mod -o "${scratch}/govulncheck" golang.org/x/vuln/cmd/govulncheck` build line, the `set -euo pipefail` + `mktemp -d` + `trap` scaffold, the `set +e`/`status=$?`/`set -e` exit-code capture; its desc explains why IT is advisory and binary-mode — this target is the opposite on both counts)
    - Taskfile.yml:369-480 (`proto:drift` — the positive-count-floor pattern: print `compared N` BEFORE comparing, `::error::` + `exit 1` when N is below the floor)
    - Taskfile.yml:657-716 (`web:test` — `node -e` used to parse a JSON report; the `set +e` capture shape)
    - .goreleaser.yaml:365-374 (`sboms:` block — `syft`, `spdx-json`, per-binary)
    - .github/workflows/ci.yml:246-296 (the blocking main-module govulncheck job — the scope this target mirrors locally; do not edit)
    - go.tool.mod (govulncheck is pinned there; `GO_TOOL` var at Taskfile.yml:9)
    - .planning/phases/11-graph-view-community-clustering/11-RESEARCH.md § Code Examples (the verified `go list -deps -f` command, 91 packages, `blas/cgo` never reached)
  </read_first>
  <action>
    Add a `check:gonum` target to `Taskfile.yml` directly after `vuln:selftest:` with a `desc:` that states: GRF-10's local, BLOCKING, positive-controlled proof for the milestone's one new direct require (`gonum.org/v1/gonum v0.17.0`, D-13); three halves each reporting the count it inspected before asserting (rule `84d1gfpywd`); the govulncheck half is SOURCE mode over the MAIN module — the same scope as ci.yml's `govulncheck (DIST-03, blocking)` job, which stays the CI gate unchanged — NOT the `vuln:` target's advisory `-mode=binary` scan of tool binaries; the SBOM half generates the document the way `release.yml`/`.goreleaser.yaml` does (syft over a built binary, spdx-json); the cgo half is the literal `go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}'` closure walk with a known-cgo positive control so an empty or blind closure cannot read as clean. `preconditions:` `command -v go`, `command -v syft` (msg naming `.goreleaser.yaml`'s `sboms:` block as the reason syft is the SBOM tool), `command -v node`, `command -v awk`.

    One `cmds:` shell block (`set -euo pipefail`; `scratch=$(mktemp -d)`; `trap 'rm -rf "${scratch}"' EXIT`):

    Half 1 — govulncheck. `GOWORK=off go build -modfile=go.tool.mod -o "${scratch}/govulncheck" golang.org/x/vuln/cmd/govulncheck` (verbatim from `vuln:`). `set +e; "${scratch}/govulncheck" ./... > "${scratch}/govulncheck.txt" 2>&1; status=$?; set -e`; `cat "${scratch}/govulncheck.txt"`; any non-zero status → `::error::check:gonum: govulncheck (source mode, main module) exited ${status}` and `exit 1` (BLOCKING — this is the difference from `vuln:`). Positive control: `ngonum=$(go list -deps ./cmd/codegraph | grep -o '^gonum\.org/v1/gonum' | wc -l | tr -d ' ')`; print `check:gonum: govulncheck (source mode, main module) clean — ${ngonum} gonum packages in the scanned set`; `[ "${ngonum}" -ge 1 ]` else `::error::… gonum is not in the main module's package graph — nothing was scanned for it` + `exit 1`.

    Half 2 — SBOM. `go build -o "${scratch}/codegraph" ./cmd/codegraph`; `syft "${scratch}/codegraph" --output "spdx-json=${scratch}/codegraph.spdx.json" -q`; extract with `node -e` reading the document: `npkg = packages.length`, `gonum = packages.filter(p => p.name === "gonum.org/v1/gonum")`, `pebble = packages.filter(p => p.name === "github.com/cockroachdb/pebble/v2")`; print `check:gonum: SBOM lists ${npkg} packages; gonum.org/v1/gonum present ${gonum.length} time(s) at ${gonum[0]?.versionInfo}; positive control github.com/cockroachdb/pebble/v2 present ${pebble.length} time(s)`; require `npkg > 0`, `gonum.length === 1`, `gonum[0].versionInfo === "v0.17.0"`, `pebble.length === 1` — node exits 1 with a `::error::` naming which condition failed.

    Half 3 — cgo closure. `go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}' gonum.org/v1/gonum/graph/community > "${scratch}/closure.txt"`; `n=$(wc -l < "${scratch}/closure.txt" | tr -d ' ')`; `nonzero=$(awk '$2 != "0"' "${scratch}/closure.txt" | wc -l | tr -d ' ')`; `blascgo=$(grep -o 'gonum.org/v1/gonum/blas/cgo' "${scratch}/closure.txt" | wc -l | tr -d ' ')`; positive control: `go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}' github.com/tree-sitter/go-tree-sitter > "${scratch}/control.txt"`; `cnon=$(awk '$2 != "0"' "${scratch}/control.txt" | wc -l | tr -d ' ')`; print `check:gonum: cgo closure over gonum.org/v1/gonum/graph/community — ${n} packages inspected, ${nonzero} with cgo (want 0), blas/cgo references ${blascgo} (want 0); positive control github.com/tree-sitter/go-tree-sitter — ${cnon} with cgo (want >= 1)`; require `n >= 50` (`::error::… enumerated only ${n} packages — a broken enumeration must fail loud`), `nonzero = 0`, `blascgo = 0`, and `cnon >= 1` (`::error::check:gonum: positive control found no cgo in go-tree-sitter — the scan cannot detect cgo, its clean verdict is meaningless`). Print `awk '$2 != "0"'` rows when `nonzero != 0` so a failure names the package.

    Final line: `echo "check:gonum: PASS"`. Every literal above is the exact string the verify commands grep for. Do not add the target to `test:`/`lint:` wrappers or to ci.yml in this plan (11-05 decides CI wiring in `11-SECURITY.md`'s notes; `TestWorkflowRunBodiesInvokeTask` would need a matching job body).
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 task -s check:gonum &gt;/tmp/11-03-gonum.txt 2&gt;&amp;1; RC=$?; tail -6 /tmp/11-03-gonum.txt; test "$RC" = "0" &amp;&amp; test "$(rg -o '^check:gonum: PASS$' /tmp/11-03-gonum.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'govulncheck \(source mode, main module\) clean — [1-9][0-9]* gonum packages in the scanned set' /tmp/11-03-gonum.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'gonum\.org/v1/gonum present 1 time\(s\) at v0\.17\.0' /tmp/11-03-gonum.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'pebble/v2 present 1 time\(s\)' /tmp/11-03-gonum.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '[0-9]+ packages inspected' /tmp/11-03-gonum.txt | rg -o '^[0-9]+')" -ge 50 &amp;&amp; test "$(rg -o '0 with cgo \(want 0\)' /tmp/11-03-gonum.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'blas/cgo references 0' /tmp/11-03-gonum.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'go-tree-sitter — [1-9][0-9]* with cgo' /tmp/11-03-gonum.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the target exits non-zero, does not print exactly one PASS line, reports zero gonum packages scanned, misses gonum v0.17.0 or the pebble control in the SBOM, inspects fewer than 50 closure packages, finds any cgo (or blas/cgo) in gonum's closure, or the go-tree-sitter control reports zero cgo (the scan is blind)</fails_when>
    <automated>test "$(rg -o '^  check:gonum:$' Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'go build -modfile=go.tool.mod -o "\$\{scratch\}/govulncheck" golang.org/x/vuln/cmd/govulncheck' Taskfile.yml | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o "go list -deps -f '\{\{\.ImportPath\}\} \{\{len \.CgoFiles\}\}' gonum\.org/v1/gonum/graph/community" Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o "go list -deps -f '\{\{\.ImportPath\}\} \{\{len \.CgoFiles\}\}' github\.com/tree-sitter/go-tree-sitter" Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'spdx-json=\$\{scratch\}/codegraph\.spdx\.json' Taskfile.yml | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(git diff --name-only "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)" HEAD -- .github/workflows/ci.yml go.mod go.sum | rg -v '^go\.mod$' | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the target name is absent or duplicated, the pinned govulncheck build line is not the `vuln:` one (count 2 = `vuln:` + `check:gonum`), either `go list -deps` template is missing, the SBOM is not spdx-json over the built binary, or ci.yml/go.sum changed (go.mod's only change since the threshold commit is 11-01's one line)</fails_when>
    <automated>awk '/^  check:gonum:$/{f=1;print;next} f&amp;&amp;/^  [a-z:_-]+:$/{f=0} f' Taskfile.yml &gt;/tmp/11-03-body.txt; test "$(wc -l &lt;/tmp/11-03-body.txt | tr -d ' ')" -ge 40 &amp;&amp; test "$(rg -o 'mode=binary' /tmp/11-03-body.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'govulncheck" \./\.\.\.' /tmp/11-03-body.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the `check:gonum` body uses `-mode=binary` (the wrong scope) or does not run `govulncheck ./...` exactly once (region-scoped by awk to the target's own body, so `vuln:`'s binary-mode lines cannot false-positive)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task --list-all 2&gt;/dev/null | rg -o 'check:gonum' | wc -l | tr -d ' ' | rg -x '1' &amp;&amp; GOTOOLCHAIN=go1.26.6 go build ./... &amp;&amp; git diff --quiet -- corpora/graph-cluster-threshold.json</automated>
    <fails_when>task cannot parse the Taskfile (the new target broke YAML), the build broke, or the threshold moved</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY pastes the four reporting lines and the PASS line from a real run, with the actual numbers (gonum packages in the scanned set, SBOM package total, closure package count, control cgo count)
    - `rg -o 'rule 84d1gfpywd|84d1gfpywd' Taskfile.yml` count increased by exactly 1 relative to the threshold commit (the target's desc cites the positive-assertion rule)
    - `rg -o 'DIST-03, blocking' Taskfile.yml | wc -l` printed at least 1 inside the new desc (scope honesty)
    - `git diff --name-only <11-02 last commit> HEAD` lists only `Taskfile.yml`
  </acceptance_criteria>
  <done>One target proves GRF-10's three claims with reported counts and positive controls: govulncheck source-mode clean with gonum in the scanned set, gonum named at v0.17.0 in a release-shaped SBOM beside a known module, and a 91-package cgo-free closure beside a control that the same scan sees as cgo-bearing.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the target discriminates — a RED rehearsal of each half in a scratch copy (never committed), recorded for 11-05 family (d)</name>
  <reversibility rating="reversible">Scratch-copy rehearsal only; nothing persists.</reversibility>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (the `check:gonum` target as landed by Task 1)
    - .planning/phases/07-guards-that-cannot-fire/07-MUTATION-LOG.md family (c) (the precedent for perturbing a COPY of a committed config in a temp dir rather than the committed file) — load only that family's section
    - .planning/phases/10-index-health-the-coverage-denominator/10-MUTATION-LOG.md (pre-mutation cleanliness gate convention)
  </read_first>
  <action>
    Without editing the committed `Taskfile.yml` (no temp copy of the Taskfile either — the target's assertions are plain shell, so each is rehearsed directly), run three RED rehearsals as direct shell commands that reproduce each half's assertion with a perturbed input, and record the outputs in the SUMMARY for 11-05 to cite: (d1) cgo positive control neutered — run the control template over a pure-Go package (`go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}' github.com/spf13/cobra | awk '$2 != "0"' | wc -l`) and show it prints 0, i.e. the target's `cnon >= 1` assertion would fail loud if the control were swapped (this is what 11-05 family (d) mutates in the real Taskfile and reverts); (d2) SBOM name perturbed — run the SBOM `node -e` extraction with the name `gonum.org/v1/gonumx` and show it reports 0 present (exit 1); (d3) closure floor — feed a one-line file to the floor check and show `enumerated only 1 packages` fires. Assert before and after: `git diff --quiet -- Taskfile.yml` exits 0 (the committed target was never edited). Paste all three transcripts under `## RED rehearsals` in the SUMMARY.
  </action>
  <verify>
    <automated>git diff --quiet -- Taskfile.yml &amp;&amp; test "$(GOTOOLCHAIN=go1.26.6 go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}' github.com/spf13/cobra | awk '$2 != "0"' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(GOTOOLCHAIN=go1.26.6 go list -deps -f '{{.ImportPath}} {{len .CgoFiles}}' github.com/tree-sitter/go-tree-sitter | awk '$2 != "0"' | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>the committed Taskfile has working-tree edits (the rehearsal leaked), the pure-Go decoy unexpectedly shows cgo (the rehearsal would not discriminate), or the real control shows none (the scan is blind)</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY `## RED rehearsals` has three transcripts (d1, d2, d3) each showing the assertion firing on the perturbed input
    - `git status --porcelain -- Taskfile.yml` is empty at the end of the task
  </acceptance_criteria>
  <done>Each half of `check:gonum` has been shown to fire on a perturbed input without touching the committed target, giving 11-05 family (d) its instrument and expected RED text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go module proxy → `go.sum` → `gonum.org/v1/gonum v0.17.0` | the milestone's only new direct dependency; executes inside every `FileGraph()` call |
| `check:gonum` → developer | a local gate whose green must mean something — every half reports its count and has a positive control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering (supply chain) | `gonum.org/v1/gonum` | high | mitigate | `check:gonum`: govulncheck SOURCE mode over the main module with gonum proven in the scanned set (blocking, unlike `vuln:`); SBOM presence at the pinned version beside a known-module control; cgo closure scan with count floor and a known-cgo control; `go.sum` unchanged since the threshold commit (verify) |
| T-11-10 | Elevation of Privilege | cgo entering the query engine's hot path through a transitive gonum package (`blas/cgo`) | high | mitigate | closure scan asserts `0 with cgo` and `blas/cgo references 0` over `>= 50` packages; control proves detection; 11-05 family (d) watches the control-neutered target fail |
| T-11-11 | Repudiation | a vacuous green from an empty closure, an empty SBOM, or a scan that never included gonum | medium | mitigate | printed counts with floors before every comparison (`proto:drift` pattern); `ngonum >= 1`, `npkg > 0`, `n >= 50`, `cnon >= 1` |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | none; gonum legitimacy `[ASSUMED]` with evidence (see must_haves) — no npm/pypi/crates seam applies to a Go module |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 task -s check:gonum` — `check:gonum: PASS` with four reporting lines
- `git diff --quiet -- go.sum corpora/graph-cluster-threshold.json .github/workflows/ci.yml` since the threshold commit
- `GOTOOLCHAIN=go1.26.6 task --list-all` parses and lists `check:gonum`
</verification>

<success_criteria>
- GRF-10 success criterion 4 verbatim: gonum passes govulncheck, appears by name in the generated SBOM, and a check over its import closure reports the number of packages it inspected and finds zero cgo — so an empty closure cannot read as a clean one
</success_criteria>

<output>
Create `.planning/phases/11-graph-view-community-clustering/11-03-SUMMARY.md` when done — include the real `check:gonum` transcript with its numbers, the three RED rehearsal transcripts, and the commit SHA.
</output>
