---
phase: 11-graph-view-community-clustering
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tools/graphcluster/main.go
  - tools/graphcluster/main_test.go
  - tools/graphcluster/ancestry_test.go
  - corpora/graph-cluster-observations.json
  # The paths below change ONLY on a GRF-09 FAIL verdict (Task 3, the D-07 promote fallback);
  # on PASS none of them is touched and Task 3 records "not executed".
  - internal/schema/graph.proto
  - internal/community/community.go
  - internal/community/community_test.go
  - internal/query/community.go
  - internal/query/community_test.go
  - internal/query/traverse.go
  - internal/indexer/resolve.go
  - internal/indexer/resolve_test.go
  - internal/indexer/sync.go
  - internal/indexer/sync_test.go
  - internal/schema/meta_commit_test.go
autonomous: true
requirements: [GRF-09]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`tools/graphcluster` reads EVERY bar from `corpora/graph-cluster-threshold.json` (`max`, `statistic`, `runs`, `minNodes`, `corpus.repo/sha`) and refuses to run (exit 2, no observation written) when any is missing — `TestLoadThresholdRefusesMissingBar`; the file carries no default bar of its own (`rg -o '500' tools/graphcluster/main.go | wc -l` = 0)"
    - "The harness opens the pinned guava store resolved through `corpora/manifest.json` + `corpora.Entry.Dir` + `.codegraph/store` with `graphstore.Open` + `Snapshot` and NEVER re-indexes it (`indexer.Run(` count 0, no `internal/indexer` import) nor writes to it (no `NewWriter(`); it verifies the checkout's `git rev-parse HEAD` equals the pinned SHA before measuring (D-06)"
    - "GRF-09 boundary: `judge(median, max)` returns PASS for `median <= max` and FAIL for `median > max` — 499 PASS, 500 PASS (exactly equal passes), 501 FAIL (`TestJudgeBoundary`)"
    - "GRF-09 adjacency: the binding statistic is the sorted MIDDLE of the three integer values, never an average — `[1, 2, 1000]` yields 2, not 334 (`TestMedianInt64IsTheSortedMiddle`)"
    - "GRF-09 ordering: the observation records the runs in run order (`[900, 100, 400]` stays `[900, 100, 400]`) and the verdict is taken from the sorted middle (400) — `TestRunRecordsFailVerbatimAndRunsInOrder`"
    - "GRF-09 precision: timings are integer milliseconds truncated from `time.Duration.Milliseconds()`; the verdict path is int64-only — `rg -o 'float' tools/graphcluster/main.go | wc -l` = 0"
    - "GRF-09 empty: a store resolving to fewer than `minNodes` (2) file nodes makes the harness REFUSE with exit 2 and write no observation — `TestRunRefusesFewerThanMinNodes`; a 0 ms observation over an empty rollup can never read as PASS"
    - "The measurement against guava ran, `corpora/graph-cluster-observations.json` is committed with `verdict` PASS or FAIL preserved VERBATIM (the harness wrote the word, not a hand), `thresholdDigest` equal to the sha256 of the committed threshold bytes, `storeSource: manifest`, three runs, `nodeCount >= 1000`, `edgeCount >= 1000`, `communityCount >= 1`, and the process exit code matched the verdict (0 PASS / 1 FAIL) (D-08)"
    - "Ancestry is checked by a persisted test, not prose: `TestClusterThresholdCommitIsAncestorOfEveryMeasurement` resolves the threshold's single adding commit, asserts `git merge-base --is-ancestor <thr> <c>` for every commit touching `corpora/graph-cluster-observations.json` and `tools/graphcluster/main.go`, logs `threshold commit %s; compared %d measurement commits` with `%d >= 1`, and skips (named reason) only on a shallow clone; `TestClusterThresholdDigestMatchesCommittedObservation` needs no history and never skips (D-05, D-08)"
    - "`corpora/graph-cluster-threshold.json` still has exactly one commit at the end of this plan and is byte-identical to its committed form (`git diff --quiet`)"
    - "Either verdict leaves the phase able to proceed: on PASS, `Node` field 50 stays reserved and `TestFileGraphCommunitySourceIsFreshComputeOnly` stays green; on FAIL, Task 3's promote fallback is executed and its own acceptance criteria hold — the UI (11-04) reads `community_id` either way (D-07)"
  artifacts:
    - "tools/graphcluster/main.go — `run`, `loadThreshold`, `thresholdDigest`, `resolveCorpusStore`, `measureRuns` (via the `measureRunsFn` seam), `medianInt64`, `judge`, `writeObservation`, `observation`, `runResult`, `threshold`"
    - "tools/graphcluster/main_test.go — `TestJudgeBoundary`, `TestMedianInt64IsTheSortedMiddle`, `TestLoadThresholdRefusesMissingBar`, `TestRunRefusesFewerThanMinNodes`, `TestRunWritesObservationWithDigestAndVerdict`, `TestRunRecordsFailVerbatimAndRunsInOrder`, `TestHarnessSourceNeverIndexesOrWritesTheStore`"
    - "tools/graphcluster/ancestry_test.go — `TestClusterThresholdCommitIsAncestorOfEveryMeasurement`, `TestClusterThresholdDigestMatchesCommittedObservation`"
    - "corpora/graph-cluster-observations.json — the committed measurement with its verdict"
  key_links:
    - "`corpora/graph-cluster-threshold.json` → `loadThreshold` → `judge(medianInt64(values), max)` → `observation.verdict` → process exit code"
    - "`corpora/manifest.json` guava entry → `corpora.Entry.Dir(root)/.codegraph/store` → `graphstore.Open` → `Snapshot` → `query.New(reader).FileGraph()` → timed `query.AssignCommunities(res.Nodes, res.Edges)`"
    - "threshold bytes → `thresholdDigest` → `observation.thresholdDigest` → `TestClusterThresholdDigestMatchesCommittedObservation` (11-05 family (b) turns this RED by widening the bar in the working tree)"
  prohibitions:
    - "The threshold file MUST NOT be rewritten, widened, relaxed, or re-scoped by any task — on FAIL the ONLY remedy is Task 3's fallback"
    - "The harness MUST NOT re-index the corpus (`indexer.Run`), open a `Writer` on its store, or import `internal/indexer`; the cached guava index is read-only"
    - "The observation MUST NOT carry a host-local absolute path (no `/Users/`, no `$HOME`, no corpus-root string) — only the repo-relative store suffix `.codegraph/store`"
    - "The verdict word MUST be written by the harness, never edited by hand; a hand-edited observation is a forged measurement"
    - "On FAIL the fallback MUST be a promote (persisted id becomes the primary, fresh compute removed) — never add-alongside with two colouring sources"
    - "Nothing under `web/src` may change in this plan — UI wiring waits for 11-04"
---

<objective>
Measure GRF-09's premise instead of assuming it: a Go in-process harness opens the already-indexed guava store, times `AssignCommunities` in isolation as the median of three cold runs, judges it against the committed threshold with equality passing, writes the observation with the verdict verbatim, and exits with that verdict. A persisted git-backed test proves the threshold commit predates every measurement commit, and a digest test proves the threshold was not touched afterwards. If the verdict is FAIL, the pre-written fallback (index-time persistence into `Node` field 50, as a promote) is executed here — never a raised bar.

Purpose: the maintainer settled fresh-per-call by decision; its cost is the one thing the decision did not settle, and this plan is the measurement that makes 11-04's UI wiring affordable — or triggers the documented fallback.

Output: `tools/graphcluster/{main.go, main_test.go, ancestry_test.go}`, `corpora/graph-cluster-observations.json` committed with its verdict, and (FAIL only) the promote fallback.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-graph-view-community-clustering/11-CONTEXT.md
@.planning/phases/11-graph-view-community-clustering/11-RESEARCH.md
@.planning/phases/11-graph-view-community-clustering/11-PATTERNS.md
@.planning/phases/11-graph-view-community-clustering/11-01-SUMMARY.md
@.planning/milestones/v0.12.0-phases/05-file-package-graph-view/05-04-PLAN.md

Every Go command: `GOTOOLCHAIN=go1.26.6`. The guava store lives at `~/.cache/codegraph/corpora/google-guava-2b0cb53f@94f39958baf7ad51ddf9c70e406ed6b188194daa/.codegraph/store` (read-only; 25 MB Pebble store, verified present this session); `tools/corpora -mode measure` is NOT reused because it re-indexes into a throwaway store (`measureOne` calls `indexer.Run`) — exactly what D-06 forbids for this metric. CI's `test` job checks out shallow (`fetch-depth` default), so the ancestry test skips there with a named reason and runs for real on every full clone; the digest test needs no history. `go test ./tools/...` is inside `task test:unit`'s package set, so the unit tests must never open the guava corpus.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `tools/graphcluster` — the GRF-09 harness: threshold-driven, read-only over the cached store, median-of-3 integer ms, fail-closed comparator, observation with digest and verdict (D-06, D-08)</name>
  <reversibility rating="reversible">A tool under `tools/`; no wire or storage contract.</reversibility>
  <files>tools/graphcluster/main.go, tools/graphcluster/main_test.go</files>
  <read_first>
    - tools/corpora/main.go:1-110 (package doc, `run(args, stdout, stderr) int` testable-core shape, flag parsing) and :360-455 (`measureOne` — copy the `graphstore.Open` → `Snapshot` → `query.New…` sequence but NOT the `indexer.Run` line; `writeObservations`'s MarshalIndent + trailing newline), :326-355 (`verifyAndCountTrackedFiles` / `runGit` — the `git -C dir rev-parse HEAD` check to copy)
    - tools/corpora/measure_test.go:1-120 (temp-file test conventions for a `tools/` command)
    - internal/corpora/manifest.go:115-218 (`Load`, `Entry.Dir(root)`, `CorpusRoot()` — resolution formula, `CODEGRAPH_CORPUS_DIR` / `XDG_CACHE_HOME` overrides)
    - internal/graphstore/pebble_store.go:141-170 (`Open`, `Snapshot`), internal/graphstore/store.go:15-30 and :216-235 (`NewWriter`, `PutNode`, `PutEdge(e, ownerPath)` — for the TEST fixture store only)
    - internal/query/community.go (`AssignCommunities` — the timed call), internal/query/traverse.go (`FileGraph()` returns the rollup; `FileGraphResult.Nodes/Edges`)
    - internal/query/filegraph_cycles_test.go:161-180 (the three-node calls-edge fixture shape to rebuild through a real temp store in the tests)
    - corpora/graph-cluster-threshold.json (every key the loader reads; `measurementProtocol.observationFile`, `observationCarriesThresholdDigest`)
    - web/scripts/graph-verdict.mjs:1-20 (the fail-closed comparator doctrine: every ambiguity resolves to FAIL; never writes the threshold)
  </read_first>
  <behavior>
    - Test 1 (`TestJudgeBoundary`): `judge(499, 500) == "PASS"`, `judge(500, 500) == "PASS"`, `judge(501, 500) == "FAIL"`; the test logs the three verdicts.
    - Test 2 (`TestMedianInt64IsTheSortedMiddle`): `medianInt64([]int64{1, 500, 1000}) == 500`; `medianInt64([]int64{1, 2, 1000}) == 2` (an average would give 334 — assert `!= 334` explicitly); `medianInt64([]int64{900, 100, 400}) == 400`; the input slice is NOT mutated (compare before/after).
    - Test 3 (`TestLoadThresholdRefusesMissingBar`): a temp JSON lacking `metrics.clusteringTimeMs.max` → error containing `clusteringTimeMs.max`; `runs: 2` → error containing `runs`; `statistic: "mean"` → error containing `statistic`; `minNodes` absent → error containing `minNodes`; the committed `../../corpora/graph-cluster-threshold.json` loads with `Max == 500`, `Runs == 3`, `MinNodes == 2`, `Corpus.SHA == "94f39958baf7ad51ddf9c70e406ed6b188194daa"`, and `thresholdDigest(raw)` starts with `sha256:` and is 71 characters.
    - Test 4 (`TestRunRefusesFewerThanMinNodes`): a temp Pebble store holding ONE file-kind node (`Id "only"`, `Kind goextract.KindFile`, `FilePath "only.go"`) → `run([]string{"-threshold", thr, "-out", out, "-store-dir", dir}, …)` returns 2, stderr contains `refused` and `1 file nodes`, and `out` does not exist afterwards.
    - Test 5 (`TestRunWritesObservationWithDigestAndVerdict`): a temp store with three file-kind nodes `a.go b.go c.go` and calls-edges `a->b`, `b->a`, `a->c` (`PutEdge(e, "")`), a temp copy of the committed threshold → exit 0; the observation decodes with `verdict == "PASS"`, `len(runs) == 3`, `clusteringTimeMs.median == medianInt64(values)`, `thresholdDigest == thresholdDigest(bytes of the temp threshold)`, `storeSource == "explicit"`, `nodeCount == 3`, `communityCount >= 1`, `measuredAt` parses as RFC3339, and the file bytes contain no `/Users/`, no `/home/` and not the temp dir path (host-local path leak, T-11-05).
    - Test 6 (`TestRunRecordsFailVerbatimAndRunsInOrder`): swap the package-level seam `measureRunsFn` (restored via `t.Cleanup`) for a stub returning runs `[900, 100, 400]` ms with 10 nodes / 20 edges / 3 communities, threshold copy with `max: 300` → exit 1; the observation has `verdict == "FAIL"` (verbatim, not "fail"/"FAILED"), `clusteringTimeMs.values == [900, 100, 400]` in that order, `median == 400`, `max == 300`, `comparison == "median <= max"`; stdout contains `verdict FAIL`.
    - Test 7 (`TestHarnessSourceNeverIndexesOrWritesTheStore`): reads `main.go`; forbidden substrings `indexer.Run(`, `"github.com/seanb4t/codegraph-go/internal/indexer"`, `NewWriter(`, `float32`, `float64`, `"500"` — zero occurrences; positive control: `graphstore.Open(`, `.Snapshot()`, `query.AssignCommunities(` each present exactly once; `os.WriteFile(` present exactly once (the observation); logs the byte count inspected.
  </behavior>
  <action>
    Create `tools/graphcluster/main.go` (`package main`) with a package doc modelled on `tools/corpora/main.go`'s: GRF-09's measurement instrument; reads every bar from the threshold file (never its own defaults); opens the pinned corpus's cached store READ-ONLY (`graphstore.Open` + `Snapshot`; never `indexer.Run`, never a `Writer`); times `query.AssignCommunities` in isolation after a fresh `FileGraph()` per run; writes ONE observation file; never writes the threshold; exit 0 = PASS, 1 = FAIL, 2 = refused/error. Flags via `flag.NewFlagSet("graphcluster", flag.ContinueOnError)`: `-threshold` (default `corpora/graph-cluster-threshold.json`), `-out` (default `corpora/graph-cluster-observations.json`), `-manifest` (default `corpora/manifest.json`), `-corpus-root` (default empty → `corpora.CorpusRoot()`), `-store-dir` (default empty; when set, the manifest/corpus-root resolution and the checkout-SHA check are SKIPPED and the observation records `storeSource: "explicit"` — tests only; the committed observation must say `manifest`).

    Types and functions: `type threshold struct` decoding `schemaVersion`, `corpus{repo, sha}`, `metrics.clusteringTimeMs{max int64, unit, statistic, runs int}`, `minNodes int` (unknown keys ignored — the file carries prose); `func loadThreshold(path string) (threshold, []byte, error)` returning the raw bytes and refusing with a named error when `max <= 0` ("threshold: metrics.clusteringTimeMs.max missing or not positive"), `statistic != "median"`, `runs < 3 || runs%2 == 0` ("runs must be an odd integer >= 3"), `minNodes < 2`, or `corpus.repo`/`corpus.sha` empty; `func thresholdDigest(raw []byte) string` = `"sha256:" + hex(sha256.Sum256(raw))`; `func resolveCorpusStore(manifestPath, corpusRoot, repo, sha string) (string, error)` — `corpora.Load`, find the entry whose `Repo == repo && SHA == sha` (refuse naming both otherwise), `dir := entry.Dir(root)`, run `git -C dir rev-parse HEAD` (copy `runGit`'s shape) and refuse unless it equals `sha`, return `filepath.Join(dir, ".codegraph", "store")` after `os.Stat` succeeds; `type runResult struct { Run int; ClusteringTimeMs int64; FileGraphWallMs int64 }` (json tags `run`, `clusteringTimeMs`, `fileGraphWallMs`); `func measureRuns(storeDir string, runs, minNodes int) ([]runResult, int, int, int, error)` — `graphstore.Open`, deferred `Close`; for each run: a FRESH `Snapshot()` (closed at the end of the run), `t0 := time.Now()`, `res, err := query.New(reader).FileGraph()`, `wall := time.Since(t0)`; if `len(res.Nodes) < minNodes` return a sentinel `errRefused` wrapping the message `refused: corpus resolved to %d file nodes (< minNodes %d)`; `t1 := time.Now()`, `ids := query.AssignCommunities(res.Nodes, res.Edges)`, `d := time.Since(t1)`; append `runResult{Run: i+1, ClusteringTimeMs: d.Milliseconds(), FileGraphWallMs: wall.Milliseconds()}`; return also `len(res.Nodes)`, `len(res.Edges)`, and the distinct count of `ids`; no `runtime.GC()`, no warm-up — the protocol's "cold" is a fresh snapshot and rollup per run; `var measureRunsFn = measureRuns` (the ONLY package-level var besides flags-free constants; the test seam); `func medianInt64(vals []int64) int64` over a sorted COPY (`slices.Sort`), returning `sorted[len/2]`; `func judge(median, max int64) string` returning `"PASS"` when `median <= max` else `"FAIL"` — int64 only, no float anywhere in the file; `type observation struct` with json fields `schemaVersion` 1, `thresholdRef` (the `-threshold` value as given, repo-relative), `thresholdDigest`, `corpus{repo, sha}`, `storeSource` (`"manifest"` | `"explicit"`), `storeRelative` (`".codegraph/store"`), `nodeCount`, `edgeCount`, `communityCount`, `runs []runResult`, `clusteringTimeMs{values []int64, median, max int64, unit "ms", statistic "median", comparison "median <= max", verdict}`, `verdict`, `measuredAt` (RFC3339 UTC), `goVersion` (`runtime.Version()`), `goos`, `goarch`; `func writeObservation(path string, obs observation) error` — `json.MarshalIndent(obs, "", "  ")` + `'\n'`, `os.MkdirAll(dir)`, the file's ONLY `os.WriteFile`. `run`: parse flags → `loadThreshold` (exit 2 on error) → resolve the store (or `-store-dir`) → `measureRunsFn` (exit 2 on `errRefused` or error, nothing written) → `median := medianInt64(values)`, `verdict := judge(median, max)` → build and write the observation → print ONE stdout line `graphcluster: verdict %s — median %d ms vs max %d ms over %d runs %v; %d nodes, %d edges, %d communities` → return 0 for PASS, 1 for FAIL. `main()` = `os.Exit(run(os.Args[1:], os.Stdout, os.Stderr))`.

    Write Tests 1–7 in `main_test.go` FIRST (RED: `undefined: judge` etc.), then implement, then GREEN. Temp stores in Tests 4–5 use `graphstore.Open(t.TempDir())` + `NewWriter` + `PutNode`/`PutEdge` + `Commit` (test-side only — the harness itself never writes). Never open the guava corpus from a test.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -run 'TestJudgeBoundary|TestMedianInt64IsTheSortedMiddle|TestLoadThresholdRefusesMissingBar|TestRunRefusesFewerThanMinNodes' ./tools/graphcluster/... 2&gt;&amp;1 | tee /tmp/11-02-red.txt; test "$(rg -o -- '--- FAIL|undefined:|build failed|no Go files|cannot find package' /tmp/11-02-red.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>RED gate (before `main.go` exists): passes only if a FAIL/undefined/build-failed/no-package line appears — a green run means the tests were not written first</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestJudgeBoundary|TestMedianInt64IsTheSortedMiddle|TestLoadThresholdRefusesMissingBar|TestRunRefusesFewerThanMinNodes|TestRunWritesObservationWithDigestAndVerdict|TestRunRecordsFailVerbatimAndRunsInOrder|TestHarnessSourceNeverIndexesOrWritesTheStore' ./tools/graphcluster/... 2&gt;&amp;1 | tee /tmp/11-02-green.txt | tail -3; test "$(rg -o -- '--- PASS: Test(JudgeBoundary|MedianInt64IsTheSortedMiddle|LoadThresholdRefusesMissingBar|RunRefusesFewerThanMinNodes|RunWritesObservationWithDigestAndVerdict|RunRecordsFailVerbatimAndRunsInOrder|HarnessSourceNeverIndexesOrWritesTheStore)' /tmp/11-02-green.txt | wc -l | tr -d ' ')" = "7" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/11-02-green.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; ! rg -q 'no tests to run' /tmp/11-02-green.txt</automated>
    <fails_when>fewer than 7 named PASS lines, any FAIL, or "no tests to run"</fails_when>
    <automated>test "$(rg -o 'indexer\.Run\(|"github.com/seanb4t/codegraph-go/internal/indexer"|NewWriter\(|float32|float64' tools/graphcluster/main.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'graphstore\.Open\(' tools/graphcluster/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'query\.AssignCommunities\(' tools/graphcluster/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'os\.WriteFile\(' tools/graphcluster/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o '\b500\b' tools/graphcluster/main.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'var measureRunsFn = measureRuns' tools/graphcluster/main.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'runtime\.GC\(' tools/graphcluster/main.go | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the harness re-indexes, writes, or uses floats; the single Open/AssignCommunities/WriteFile invariants are broken; a literal 500 default crept in; the seam is missing; or a GC call skews the timing window</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go vet ./tools/graphcluster/... &amp;&amp; GOTOOLCHAIN=go1.26.6 go build ./... &amp;&amp; test "$(git diff --name-only "$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1)" HEAD -- web/src corpora/graph-cluster-threshold.json | rg -v '^web/src/lib/gen/ui_pb\.ts$' | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>vet or build fails, the threshold file changed, or any authored `web/src` file changed before the verdict</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'func (run|loadThreshold|thresholdDigest|resolveCorpusStore|measureRuns|medianInt64|judge|writeObservation)\(' tools/graphcluster/main.go | wc -l` prints 8
    - `rg -o 'flag\.String\("(threshold|out|manifest|corpus-root|store-dir)"' tools/graphcluster/main.go | wc -l` prints 5
    - `rg -o '"PASS"|"FAIL"' tools/graphcluster/main.go | wc -l` prints at least 2 and `rg -o 'median <= max' tools/graphcluster/main.go | wc -l` prints at least 1
    - The SUMMARY pastes the RED transcript and the GREEN transcript
    - No file under `corpora/`, `internal/`, or `web/` changed in this task
  </acceptance_criteria>
  <done>A re-runnable Go harness exists that reads its bars from the committed threshold, refuses degenerate corpora, times `AssignCommunities` in isolation as median-of-3 integer milliseconds, judges with equality passing, and writes a digest-carrying observation whose verdict word drives the exit code — proven by seven unit tests that never touch the real corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Run the measurement against the pinned guava corpus, commit the observation with its verdict VERBATIM, and persist the ancestry + digest tests (D-06, D-08) — acceptance is satisfiable by EITHER verdict</name>
  <precondition>`test -d "$HOME/.cache/codegraph/corpora/google-guava-2b0cb53f@94f39958baf7ad51ddf9c70e406ed6b188194daa/.codegraph/store"` succeeds and `git -C "$HOME/.cache/codegraph/corpora/google-guava-2b0cb53f@94f39958baf7ad51ddf9c70e406ed6b188194daa" rev-parse HEAD` prints `94f39958baf7ad51ddf9c70e406ed6b188194daa` (else run `task corpora:fetch` — never re-index by hand); `CODEGRAPH_CORPUS_DIR` and `XDG_CACHE_HOME` are unset or point at that root</precondition>
  <reversibility rating="reversible">An observation record and two tests; the verdict itself is a fact, not a decision.</reversibility>
  <files>corpora/graph-cluster-observations.json, tools/graphcluster/ancestry_test.go</files>
  <read_first>
    - tools/graphcluster/main.go (Task 1 — the stdout line format, exit codes, observation shape)
    - internal/query/engine_worktree_test.go:13-36 (`runGitW` — the `exec.Command("git", …)` + `cmd.Dir` + skip-on-failure shape to copy)
    - .planning/milestones/v0.12.0-phases/05-file-package-graph-view/05-04-PLAN.md:808 (GRF-01's manual `git merge-base` one-liner — the check this task persists as a test instead)
    - .github/workflows/ci.yml:47-52 (the `test` job's default shallow checkout — why the ancestry test must skip, by name, on `git rev-parse --is-shallow-repository` = true)
    - corpora/graph-cluster-threshold.json (`measurementProtocol.ancestryProof` names this test file)
  </read_first>
  <action>
    Run the measurement from the repository root: `GOTOOLCHAIN=go1.26.6 go run ./tools/graphcluster -threshold corpora/graph-cluster-threshold.json -out corpora/graph-cluster-observations.json > /tmp/11-02-measure.txt 2>&1; RC=$?; cat /tmp/11-02-measure.txt; echo "exit=$RC"` — redirect to a file and read `$?` directly so the recorded status is the harness's own, never `tee`'s. Record the exit code and the stdout line verbatim in the SUMMARY under `## GRF-09 verdict`. Whatever the verdict, `git add corpora/graph-cluster-observations.json` and commit with message `feat(11-02): GRF-09 clustering-time measurement against google/guava — verdict <PASS|FAIL>` (the word copied from the file, never typed from memory). Do NOT open the threshold file. If the harness exits 2 (refused/error), fix the cause (corpus not fetched, SHA mismatch) and re-run; never edit the threshold and never hand-write an observation.

    Then create `tools/graphcluster/ancestry_test.go` (package `main`, same package as the harness) with a local `runGit(t, dir, args...)` copied from `runGitW`'s shape (hermetic `-c` flags; `t.Skipf` on git failure), a `repoRoot(t)` helper (`git rev-parse --show-toplevel`), and two tests:

    `TestClusterThresholdCommitIsAncestorOfEveryMeasurement` (D-05/D-08): if `git rev-parse --is-shallow-repository` prints `true`, `t.Skip("shallow clone: ancestry cannot be proven without history (CI test job checks out fetch-depth 1); run on a full clone")`; else `thr := git log --format=%H --diff-filter=A -- corpora/graph-cluster-threshold.json` — require EXACTLY one line, and `git log --format=%H -- corpora/graph-cluster-threshold.json` also exactly one line (never modified after being added); `measurements := git log --format=%H -- corpora/graph-cluster-observations.json tools/graphcluster/main.go` — require `n >= 1`; for each commit `c`: `git merge-base --is-ancestor thr c` must exit 0 (a commit is its own ancestor, so a later squash-merge that folds both into one commit still passes — this is deliberate and documented in the test comment); `t.Logf("threshold commit %s; compared %d measurement commits", thr, n)`; `t.Fatalf` if `n == 0`.

    `TestClusterThresholdDigestMatchesCommittedObservation` (never skips; no history needed): read `<repoRoot>/corpora/graph-cluster-threshold.json` bytes, compute `thresholdDigest(raw)` (the harness's own function — same package), decode `<repoRoot>/corpora/graph-cluster-observations.json`, require `obs.ThresholdDigest == digest`, `obs.Verdict` is exactly `PASS` or `FAIL`, `obs.ClusteringTimeMs.Verdict == obs.Verdict`, `len(obs.Runs) == 3`, `obs.StoreSource == "manifest"`, `obs.Corpus.SHA == "94f39958baf7ad51ddf9c70e406ed6b188194daa"`, and that the raw observation bytes contain no `/Users/` or `/home/`; `t.Logf("threshold digest %s matches; verdict %s; median %d ms", …)`. Add the comment `mutation: widen max after measurement` above it so 11-05 family (b) can find its instrument (editing `max` in the working tree changes the digest and turns this RED without any commit).

    Commit the test file with message `test(11-02): persist GRF-09 threshold ancestry and digest checks`. Both tests must be GREEN at HEAD on this full clone.
  </action>
  <verify>
    <automated>test -f corpora/graph-cluster-observations.json &amp;&amp; node -e 'const o=JSON.parse(require("fs").readFileSync("corpora/graph-cluster-observations.json","utf8"));const v=o.verdict;const ok=(v==="PASS"||v==="FAIL")&amp;&amp;o.clusteringTimeMs.verdict===v&amp;&amp;Array.isArray(o.runs)&amp;&amp;o.runs.length===3&amp;&amp;o.clusteringTimeMs.values.length===3&amp;&amp;Number.isInteger(o.clusteringTimeMs.median)&amp;&amp;o.clusteringTimeMs.max===500&amp;&amp;o.storeSource==="manifest"&amp;&amp;o.corpus.sha==="94f39958baf7ad51ddf9c70e406ed6b188194daa"&amp;&amp;o.nodeCount&gt;=1000&amp;&amp;o.edgeCount&gt;=1000&amp;&amp;o.communityCount&gt;=1&amp;&amp;/^sha256:[0-9a-f]{64}$/.test(o.thresholdDigest)&amp;&amp;((v==="PASS")===(o.clusteringTimeMs.median&lt;=500));console.log("verdict",v,"median",o.clusteringTimeMs.median,"nodes",o.nodeCount,"edges",o.edgeCount,"communities",o.communityCount,"ok",ok);process.exit(ok?0:1)' &amp;&amp; test "$(rg -o '/Users/|/home/' corpora/graph-cluster-observations.json | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>the observation is missing, its verdict is not exactly PASS/FAIL, the verdict disagrees with `median <= 500`, it has other than three runs, it was taken from an explicit store rather than the manifest-resolved corpus, the node/edge counts are not guava-scale, the digest is malformed, or a host-local path leaked</fails_when>
    <automated>test "$(git log --format=%H -- corpora/graph-cluster-observations.json | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l | tr -d ' ')" = "1" &amp;&amp; git diff --quiet -- corpora/graph-cluster-threshold.json &amp;&amp; THR=$(git log --format=%H -- corpora/graph-cluster-threshold.json | tail -1) &amp;&amp; OBS=$(git log -1 --format=%H -- corpora/graph-cluster-observations.json) &amp;&amp; git merge-base --is-ancestor "$THR" "$OBS" &amp;&amp; test "$(git log -1 --format=%s -- corpora/graph-cluster-observations.json | rg -o 'verdict (PASS|FAIL)' | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(git log -1 --format=%s -- corpora/graph-cluster-observations.json | rg -o 'verdict (PASS|FAIL)' | rg -o 'PASS|FAIL')" = "$(node -e 'process.stdout.write(JSON.parse(require("fs").readFileSync("corpora/graph-cluster-observations.json","utf8")).verdict)')"</automated>
    <fails_when>the observation is uncommitted, the threshold gained a second commit or has working-tree edits, the threshold commit is not an ancestor of the observation commit, or the commit message's verdict word does not match the file's</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestClusterThresholdCommitIsAncestorOfEveryMeasurement|TestClusterThresholdDigestMatchesCommittedObservation' ./tools/graphcluster/... 2&gt;&amp;1 | tee /tmp/11-02-ancestry.txt | tail -3; test "$(rg -o -- '--- PASS: Test(ClusterThresholdCommitIsAncestorOfEveryMeasurement|ClusterThresholdDigestMatchesCommittedObservation)' /tmp/11-02-ancestry.txt | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o -- '--- (FAIL|SKIP)' /tmp/11-02-ancestry.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'compared [1-9][0-9]* measurement commits' /tmp/11-02-ancestry.txt | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'threshold digest sha256:[0-9a-f]{64} matches' /tmp/11-02-ancestry.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>either test is not PASS on this full clone (a SKIP here means the shallow guard misfired), the ancestry test did not log a positive compared-commit count, or the digest test did not log the matching digest</fails_when>
    <automated>test "$(rg -o 'is-shallow-repository' tools/graphcluster/ancestry_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'merge-base' tools/graphcluster/ancestry_test.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o -- '--diff-filter=A' tools/graphcluster/ancestry_test.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'mutation: widen max after measurement' tools/graphcluster/ancestry_test.go | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>the shallow guard, the merge-base check, the first-commit resolution, or the 11-05 mutation anchor is missing from the persisted test</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY's `## GRF-09 verdict` section records: the exact command line, the harness's stdout line verbatim, the exit code (0 or 1), the three raw values, the median, `nodeCount`/`edgeCount`/`communityCount`, and whether they match GRF-01's 3,233 / 21,554 (a difference is REPORTED with a one-line reason, not failed)
    - IF verdict PASS: the SUMMARY states `Task 3 not executed — verdict PASS; Node field 50 stays reserved` and `rg -o 'reserved 50 to 59' internal/schema/graph.proto | wc -l` still prints 3
    - IF verdict FAIL: Task 3 is executed and its acceptance criteria hold; the SUMMARY states `Task 3 executed — verdict FAIL; fallback promoted`
    - `git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l` prints 1 (the threshold was never rewritten, whichever way the verdict fell)
    - `git show --name-only --format= $(git log -1 --format=%H -- corpora/graph-cluster-observations.json)` lists `corpora/graph-cluster-observations.json` and no threshold
  </acceptance_criteria>
  <done>The clustering-time premise is measured, not assumed: the observation is committed with the harness-written verdict, the threshold commit is proven — by a persisted test that reports what it compared — to be an ancestor of every measurement commit, and the threshold's digest is pinned so a later widening cannot pass silently.</done>
</task>

<task type="auto">
  <name>Task 3 (CONDITIONAL — execute ONLY if `corpora/graph-cluster-observations.json` records `verdict: FAIL`): the pre-written D-07 fallback as a PROMOTE — index-time persistence into `Node.community_id = 50`, read back by `FileGraph()`, fresh compute path removed</name>
  <reversibility rating="costly">Claims field 50 of `Node`'s reserved range as an on-disk schema field — additive and pre-agreed at schema-design time (the range's own comment names "community/cluster assignment"), but a stored contract once written. The decision itself (fallback = persist on FAIL, promote not add-alongside) was locked at discuss-phase (D-07) and by the assumption-delta decision recorded in 11-01; no `checkpoint:decision` is emitted.</reversibility>
  <files>internal/schema/graph.proto, internal/community/community.go, internal/community/community_test.go, internal/query/community.go, internal/query/community_test.go, internal/query/traverse.go, internal/indexer/resolve.go, internal/indexer/resolve_test.go, internal/indexer/sync.go, internal/indexer/sync_test.go, internal/schema/meta_commit_test.go</files>
  <read_first>
    - corpora/graph-cluster-observations.json (`verdict` — this task runs only on `FAIL`; on `PASS` record "not executed" in the SUMMARY and stop)
    - corpora/graph-cluster-threshold.json (`onFailure` — the contract this task implements verbatim: field 50, indexer commit batch, read back IN PLACE OF the fresh compute)
    - .planning/phases/11-graph-view-community-clustering/11-01-PLAN.md `<assumption_delta_decision>` (promote semantics; the invariant test this task inverts)
    - internal/schema/graph.proto:55-66 (`Node`'s `reserved 50 to 59` — split into `int32 community_id = 50;` + `reserved 51 to 59;`), internal/schema/roundtrip_test.go:15-45 (uses field 55 for its unknown-field injection — stays inside the remaining reserved range)
    - internal/indexer/resolve.go:640-800 (`writeGraph` — the one `NewWriter(`/`Commit()` pair; `PutNode` loop; where in-memory `nodes`/`edges` are available before commit), internal/indexer/resolve_test.go:985-1160 (`stubWriter`, `stubStore`)
    - internal/indexer/sync.go:165-260 (the two `NewWriter` sites; 10-SECURITY T-10-10 pins `NewWriter(` in `sync.go` at 2 — any change must update that pin deliberately in 11-SECURITY)
    - internal/query/traverse.go (`FileGraph()`'s scan-one: the file-kind record is where `agg.language` is read — read `n.GetCommunityId()` there too), internal/query/community.go, internal/query/community_test.go (`TestFileGraphCommunitySourceIsFreshComputeOnly` — inverted by this task, deliberately)
    - internal/query/archtest/import_direction_test.go (a new leaf package `internal/community` must not import `internal/indexer` root or any wire package; `internal/query` may import it)
  </read_first>
  <action>
    Skip entirely (and say so in the SUMMARY) when the verdict is PASS. On FAIL, in this order, each step green before the next:

    1. **Extract the algorithm into a leaf package** `internal/community` (imports gonum + stdlib only): `type WeightedPair struct { A, B string; Weight int64 }`, `func Assign(paths []string, pairs []WeightedPair) map[string]int` carrying the exact D-02 mechanisms (sorted-path ids, summed undirected weights, fixed PCG seed, canonical relabel) and the seed constants moved here; `internal/query/community.go` keeps `AssignCommunities(nodes, edges)` as a thin adapter over it (the harness keeps working); move the algorithm tests to `internal/community/community_test.go` (same names, same fixtures, same `compared N runs` logs — GRF-08 is unchanged by the promote).
    2. **Schema:** in `graph.proto` replace `reserved 50 to 59;` on `Node` with `int32 community_id = 50;` (doc: 1-based canonical community id computed at index time by `internal/community.Assign` over the file-level rollup; present only on file-kind records; 0 = not computed, which `FileGraph()` reports as-is for an index written before this field existed) and `reserved 51 to 59;`; `task proto:gen`; add `{"community_id", 50}` to a `knownNodeFieldNumbers`-style fixture if one exists (else extend `roundtrip_test.go`'s comment that 55 stays reserved).
    3. **Indexer (the primary):** in `writeGraph`, after nodes and edges are materialised and BEFORE `PutNode`, build the file-level rollup from the in-memory `nodes`/`edges` with the SAME exclusions `FileGraph()` applies (skip `contains`, skip package pseudo-nodes / empty `FilePath`, skip same-file pairs, aggregate `TotalCount` per file pair), call `community.Assign`, and set `CommunityId` on every file-kind node record before it is staged — same Writer, same `Commit()` (`NewWriter(` in `resolve.go` stays 1). In `sync.go`, recompute over the post-sync node/edge set inside the main-commit writer the same way (if the rollup needs the committed snapshot, document why and pin the new `NewWriter(` count explicitly in `11-SECURITY.md`); a test `TestWriteGraphPersistsCommunityIdOnFileNodes` (stubWriter captures nodes; every file-kind node has `CommunityId >= 1`; non-file records 0) and `TestSyncRewritesCommunityIdAfterAChange`.
    4. **Query (read-back, promote):** `FileGraph()` reads `n.GetCommunityId()` from the file-kind record into `fileAgg`, populates `CommunityID` from it and `CommunityCount` as the distinct count; the `AssignCommunities(result.Nodes, result.Edges)` call site is REMOVED from `traverse.go` (one source of truth); `TestFileGraphPopulatesCommunityFields` is rewritten to feed persisted ids through `traverseFakeReader` and to assert a 0 for a record lacking the field (old index) is passed through, never recomputed; `TestFileGraphCommunitySourceIsFreshComputeOnly` is INVERTED and renamed `TestFileGraphCommunitySourceIsPersistedOnly` (forbidden: `AssignCommunities(` / `community.Assign(` in `traverse.go`; positive control: `GetCommunityId()` present exactly once), with the comment `inverted by 11-02 Task 3 on the GRF-09 FAIL verdict — promote, not add-alongside`.
    5. Update the threshold `onFailure` reference in `11-SECURITY.md` at 11-05 (not here); never touch the threshold file.
  </action>
  <verify>
    <automated>V=$(node -e 'process.stdout.write(JSON.parse(require("fs").readFileSync("corpora/graph-cluster-observations.json","utf8")).verdict)'); if [ "$V" = "PASS" ]; then test "$(rg -o 'reserved 50 to 59' internal/schema/graph.proto | wc -l | tr -d ' ')" = "3" &amp;&amp; test "$(rg -o 'AssignCommunities\(result\.Nodes, result\.Edges\)' internal/query/traverse.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'GetCommunityId' internal/query/traverse.go internal/query/community.go | wc -l | tr -d ' ')" = "0" &amp;&amp; echo "verdict PASS — fallback correctly NOT executed"; else test "$(rg -o 'int32 community_id = 50;' internal/schema/graph.proto | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'reserved 51 to 59;' internal/schema/graph.proto | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'AssignCommunities\(result\.Nodes, result\.Edges\)|community\.Assign\(' internal/query/traverse.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'GetCommunityId\(\)' internal/query/traverse.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test "$(rg -o 'NewWriter\(' internal/indexer/resolve.go | wc -l | tr -d ' ')" = "1" &amp;&amp; test -d internal/community &amp;&amp; echo "verdict FAIL — fallback promoted"; fi</automated>
    <fails_when>on PASS: any fallback artefact exists (field 50 claimed, read-back in traverse.go, fresh call site removed); on FAIL: field 50 not claimed, the fresh call site still present (add-alongside), no read-back, a second Writer in `resolve.go`, or no leaf package</fails_when>
    <automated>V=$(node -e 'process.stdout.write(JSON.parse(require("fs").readFileSync("corpora/graph-cluster-observations.json","utf8")).verdict)'); if [ "$V" = "PASS" ]; then echo "skipped (PASS)"; else GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestAssignCommunitiesDeterministic|TestWriteGraphPersistsCommunityIdOnFileNodes|TestSyncRewritesCommunityIdAfterAChange|TestFileGraphPopulatesCommunityFields|TestFileGraphCommunitySourceIsPersistedOnly|TestFileGraphProjectsEngineResult' ./internal/community/... ./internal/indexer/... ./internal/query/... ./internal/uiserver/... 2&gt;&amp;1 | tee /tmp/11-02-fallback.txt | tail -3; test "$(rg -o -- '--- PASS: Test(AssignCommunitiesDeterministic|WriteGraphPersistsCommunityIdOnFileNodes|SyncRewritesCommunityIdAfterAChange|FileGraphPopulatesCommunityFields|FileGraphCommunitySourceIsPersistedOnly|FileGraphProjectsEngineResult)' /tmp/11-02-fallback.txt | wc -l | tr -d ' ')" = "6" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/11-02-fallback.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 task proto:drift &gt;/tmp/11-02-fb-drift.txt 2&gt;&amp;1 &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:golden &gt;/tmp/11-02-fb-golden.txt 2&gt;&amp;1 &amp;&amp; test "$(rg -o '^ok' /tmp/11-02-fb-golden.txt | wc -l | tr -d ' ')" -ge 1; fi</automated>
    <fails_when>(FAIL branch only) fewer than 6 named PASS lines across the four packages, any FAIL, proto:drift red, or the frozen goldens moved</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one of the two branches is recorded in the SUMMARY, matching the committed verdict word
    - PASS branch: no file listed in this task's `<files>` changed (`git diff --name-only <observation commit> HEAD -- internal/schema internal/indexer internal/community | wc -l` prints 0)
    - FAIL branch: `git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l` still prints 1; `internal/community` exists with the moved tests logging `compared N runs`; the wire (`community_id = 5`, `community_count = 7`) and `TestFileGraphProjectsEngineResult` are unchanged and green; the SUMMARY names the `NewWriter(` count in `sync.go` after the change and why
    - Neither branch edits `web/src`
  </acceptance_criteria>
  <done>The phase can proceed to the UI whichever way the verdict fell: on PASS nothing here ran and the fresh path stands; on FAIL the persisted assignment is the single primary, computed in the indexer's commit batch and read back by `FileGraph()`, with the fresh call site removed and the invariant test inverted on purpose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `corpora/graph-cluster-threshold.json` → `tools/graphcluster` | the tamper target — read only; every bar comes from here |
| `~/.cache/codegraph/corpora/…/.codegraph/store` → harness | a third-party corpus's index opened read-only; never re-indexed or written |
| harness → `corpora/graph-cluster-observations.json` → git | a committed measurement record; must carry no host-local path |
| (FAIL only) indexer commit batch → `Node.community_id = 50` → `FileGraph()` | a new stored field crossing from write side to read side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02 | Tampering | threshold widened/relaxed after measurement | high | mitigate | harness has no write path to the threshold (`os.WriteFile` count 1, observation only — `TestHarnessSourceNeverIndexesOrWritesTheStore`); `thresholdDigest` recorded in the observation and re-checked by `TestClusterThresholdDigestMatchesCommittedObservation`; ancestry test asserts exactly one threshold commit and `merge-base --is-ancestor` for every measurement commit; 11-05 family (b) watches the digest test go RED |
| T-11-07 | Repudiation | a hand-typed or averaged verdict | medium | mitigate | verdict word written only by `judge` from `medianInt64` (sorted middle, int64); `TestRunRecordsFailVerbatimAndRunsInOrder` proves FAIL is preserved verbatim with runs in order; commit message verdict must equal the file's (verify) |
| T-11-05 | Information Disclosure | host-local paths in the committed observation | low | mitigate | observation records `storeRelative: .codegraph/store` only; source/verify scans for `/Users/`, `/home/` (Test 5 + Task 2 verify) |
| T-11-08 | Tampering | harness re-indexing or writing the shared corpus store | medium | mitigate | `graphstore.Open` + `Snapshot` only; `indexer.Run(`/`NewWriter(`/indexer import count 0 (source test + verify); checkout SHA verified before measuring |
| T-11-04 | Denial of Service | pathological runtime on a real corpus | low | accept | this plan IS the measurement; a FAIL routes to the documented fallback, never a hang — the harness has no timeout because a slow-but-finishing run must record its number (GRF-01 lesson: deadlines catch hangs, comparators enforce bars) |
| T-11-09 | Tampering (FAIL only) | `Node.community_id` written by a second writer or left stale after Sync | medium | mitigate | fallback keeps `NewWriter(` in `resolve.go` at 1; Sync recompute tested (`TestSyncRewritesCommunityIdAfterAChange`); the fresh call site is removed so a stale id cannot be contradicted by a fresh one (promote) |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | none in this plan |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test -count=1 ./tools/graphcluster/...` — `ok`, ancestry and digest tests PASS (not SKIP) on this full clone
- `corpora/graph-cluster-observations.json` committed; verdict word equals the commit message's; `median <= 500` iff PASS
- `git log --format=%H -- corpora/graph-cluster-threshold.json | wc -l` = 1 and `git diff --quiet -- corpora/graph-cluster-threshold.json`
- `GOTOOLCHAIN=go1.26.6 go build ./... && go vet ./tools/...` clean
</verification>

<success_criteria>
- GRF-09 success criterion 1 in full: threshold as its own earlier commit (proven by a persisted test that reports the count it compared), the verdict preserved verbatim whichever way it fell, and a FAIL routed to the documented persistence fallback — never a raised bar
- The harness is re-runnable, reads every bar from the threshold, refuses degenerate corpora, and never touches the corpus store or the threshold
</success_criteria>

<output>
Create `.planning/phases/11-graph-view-community-clustering/11-02-SUMMARY.md` when done — include the `## GRF-09 verdict` section (command, stdout line, exit code, three values, median, counts), which Task 3 branch ran, the ancestry test's `compared N measurement commits` line, and the commit SHAs of the observation and test commits.
</output>
