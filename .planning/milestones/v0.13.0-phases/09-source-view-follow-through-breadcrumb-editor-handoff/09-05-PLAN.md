---
phase: 09-source-view-follow-through-breadcrumb-editor-handoff
plan: 05
type: execute
wave: 4
depends_on: ["09-02", "09-04"]
files_modified:
  - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md
  - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md
autonomous: true
requirements: [BRW-10, BRW-11, BRW-13]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "09-MUTATION-LOG.md carries four families, each with a pre-mutation cleanliness gate, a confirmed-applied mutation (or the pre-fix build for family (a)), pasted verbatim failing output, the revert, and a byte-clean proof: (a) breadcrumb absent against the pre-fix binary [09-03], (b) breadcrumb STALE — innermostSymbolAt mutated to return the nearest preceding symbol, watched fail in the live gate at a gap line, (c) the rejected-path test — the ValidateRepoRelativePath call removed from GetEditorLink, watched fail on `../outside.txt`, (d) the scheme allowlist — `javascript` added to editorURLSchemes, watched fail in TestEditorTemplateSchemeAllowlist (BRW-10 SC1 'stale', BRW-11 SC2)."
    - "09-SECURITY.md follows 08-SECURITY.md's house format (frontmatter with phase/slug/status/threats_open/asvs_level/created; Trust Boundaries; Threat Register with Threat ID/Category/Component/Severity/Disposition/Mitigation/Status; Accepted Risks Log; Security Audit Trail) and states, in the Trust Boundaries or Notes section, that the browser's external-protocol prompt — NOT CSP — is the security boundary, with the sentence that CSP does not govern href navigation so the only thing between a malicious template and an unintended scheme is D-13's positive scheme check (BRW-13)."
    - "Every threat row in 09-SECURITY.md carries either a test name (a Go Test* or vitest file#case) or a recorded verdict in its Mitigation cell — no cell is empty, TBD, or prose alone; the six threats CONTEXT.md names at minimum are present: path traversal/symlink escape (test), scheme injection via flag/env/override (test), placeholder encoding/template smuggling (test), the override text field as self-XSS (verdict), discovery as a probe never a launch (verdict + guard), and the external-protocol prompt as consent boundary (verdict)."
    - "BRW-13 boundary edge: every threat row rated high carries disposition mitigate and names a test — the security_block_on threshold is high, so an unmitigated high would block; a check over the register counts the high rows and asserts each has a Test* or .test.ts reference."
    - "The [ASSUMED] Cursor and JetBrains templates (09-RESEARCH.md A1/A2) and the Safari transient-activation question (A3 / Open Question 2) are recorded in 09-SECURITY.md's Notes as assumptions with their execution-time status (confirmed / not tested — remains [ASSUMED]) taken from the plan 09-02 human-check outcome — never silently shipped as verified."
    - "At phase close: task web:drift, task proto:drift, GOTOOLCHAIN=go1.26.6 task test:unit, task web:test and node web/scripts/breadcrumb-check.mjs are all green; the repo-wide Zed check reports 0 matches under web/src and internal/uiserver/editorpresets.go with a positive preset count of 3 from EditorPresets(); git status --porcelain is empty."
    - statement: "BRW-13 precision edge: each threat row carries exactly one of {test name, recorded verdict} in its Mitigation cell and no Mitigation cell is empty or reads TBD."
      verification: backstop
    - statement: "BRW-13 boundary edge: every row whose Severity is high has Disposition mitigate and a test reference — an unmitigated high row would trip the security_block_on: high gate."
      verification: backstop
  artifacts:
    - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md
    - .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md
  key_links:
    - "Family (b) mutates web/src/lib/breadcrumb.ts, rebuilds web/build and ./codegraph, runs breadcrumb-check.mjs, then restores BOTH breadcrumb.ts and web/build/ with `git checkout --` (Vite output is not reproducible, so the committed bytes are restored, never rebuilt) and rebuilds the binary from the restored tree."
    - "Families (c) and (d) mutate internal/uiserver/editorlink.go one at a time (shared file — never overlapped, 08-MUTATION-LOG.md's rule) and are watched fail by the Go tests plan 09-01 wrote."
    - "09-SECURITY.md's register is the union of every plan's `<threat_model>` rows (T-09-01 … T-09-20, T-09-SC), deduplicated by id, each cell pointing at the test that closed it or the verdict that accepted it."
  prohibitions:
    - statement: "No RED demonstration may be narrated or summarised — each family pastes the failing output verbatim; a demonstration that does not fail is reported as an observed negative result with its root cause, never rewritten until it does (08-MUTATION-LOG.md family (d) precedent)."
      status: resolved
      verification: judgment
---

<objective>
Close the phase with the two artifacts the ROADMAP demands as proof rather than prose: the remaining
three RED demonstrations in `09-MUTATION-LOG.md` (a stale breadcrumb watched fail in the live browser,
the rejected-path test watched fail with the gate removed, the scheme allowlist watched fail with
`javascript` admitted) and `09-SECURITY.md` in the house format, naming the browser's external-protocol
prompt — not CSP — as the boundary, with every threat carrying a test name or a recorded verdict.
Then run every phase gate once, green, on a clean tree.

Purpose: BRW-13 is not optional paperwork (v0.12.0 Phase 1 shipped without a SECURITY.md and the
milestone audit caught it), and this milestone's standing rule is that a gate is not trusted until it
has been demonstrated RED against a confirmed-applied, byte-cleanly-reverted mutation.

Output: `09-MUTATION-LOG.md` closed with four families and a phase-wide byte-clean proof;
`09-SECURITY.md`; a green phase-close transcript in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-CONTEXT.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-RESEARCH.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-01-PLAN.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-02-PLAN.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-03-PLAN.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-04-PLAN.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-02-SUMMARY.md
@.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-04-SUMMARY.md
@.planning/phases/08-tmux-real-pty-harness/08-SECURITY.md
@.planning/phases/08-tmux-real-pty-harness/08-MUTATION-LOG.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's subset of the phase inventory (the complete list is in 09-01-PLAN.md):

- `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md` —
  families (b), (c), (d) appended; `## Closing` with the phase-wide byte-clean proof, the non-vacuity
  assertion and the family-to-requirement table
- `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Families (b), (c), (d) — a stale breadcrumb, a bypassed confinement gate and a poisoned allowlist, each watched fail and byte-cleanly reverted</name>
  <precondition>`./codegraph` builds from the current tree (`GOTOOLCHAIN=go1.26.6 task build:release` succeeds), this repository's `.codegraph/store` exists, and Playwright's Chromium is installed — the same three facts plan 09-03 Task 3 relied on.</precondition>
  <files>.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md</files>
  <read_first>
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md` (plan 09-03) — the header, the cleanliness-gate convention and family (a); append after it, never rewrite it.
    - `.planning/phases/08-tmux-real-pty-harness/08-MUTATION-LOG.md` lines 1-15 (the shared-file one-at-a-time rule), 92-172 (family (b)'s full entry shape), 301-340 (`## Closing`: phase-wide byte-clean proof, non-vacuity assertion, family-to-requirement table).
    - `web/src/lib/breadcrumb.ts` (plan 09-03) — `innermostSymbolAt`; the family (b) mutation replaces its selection with "the symbol with the greatest startLine <= line" (the nearest-preceding lie D-03 forbids).
    - `internal/uiserver/editorlink.go` (plan 09-01) — the `eng.ValidateRepoRelativePath(path)` call (family (c) deletes the call and its error handling so the path flows straight to the join) and `editorURLSchemes` (family (d) adds a `"javascript": {}` entry).
    - `internal/uiserver/editorlink_test.go` — `TestGetEditorLinkPathConfinementAtRPCBoundary` and `TestEditorTemplateSchemeAllowlist`, the instruments for (c) and (d).
    - `web/scripts/breadcrumb-check.mjs` and `corpora/breadcrumb-check.json` (plans 09-03/09-04) — the instrument for (b); its observations print expected vs shown at each line.
    - `Taskfile.yml` `web:build` (739-800) — Vite output is NOT reproducible, so the revert of `web/build/` is `git checkout -- web/build`, never a rebuild.
  </read_first>
  <action>
Append three families to `09-MUTATION-LOG.md`, each in the 08-MUTATION-LOG.md entry shape: test/instrument name, pre-mutation gate (`git diff --quiet -- <file>` exit 0, recorded), the mutation as a diff, a confirmed-applied grep, the exact command, the verbatim failing output, the revert command, the post-revert gate, and a green re-run.

**Family (b) — BRW-10: a STALE breadcrumb.** Gate `web/src/lib/breadcrumb.ts` and `web/build` clean. Mutate `innermostSymbolAt` to return, among symbols with `startLine <= line`, the one with the greatest `startLine` (ignoring `endLine` entirely) — the nearest-preceding symbol. Confirm applied by grep. Rebuild: `task web:build` then `GOTOOLCHAIN=go1.26.6 task build:release`. Run `node web/scripts/breadcrumb-check.mjs --out "$TMPDIR/breadcrumb-stale.json"` (do NOT overwrite the committed record) and paste the transcript: at the gap line the oracle expects `null` and the bar shows the preceding function's name, `agrees: false`, `success=false`, exit 1. Revert with `git checkout -- web/src/lib/breadcrumb.ts web/build`, gate both clean, rebuild the binary from the restored tree (`task build:release` only — web/build is already the committed bytes), re-run the gate green against the committed record path and paste the PASS line. This is success criterion 1's "stale" half; family (a) was the "empty" half.

**Family (c) — BRW-11: the rejected-path test with the gate removed.** Gate `internal/uiserver/editorlink.go` clean. Delete the `eng.ValidateRepoRelativePath(path)` call and its `if verr != nil { … }` block inside `GetEditorLink`'s `withEngine` closure (keep everything else, so the path flows straight into the join). Confirm applied by grepping for zero `ValidateRepoRelativePath` occurrences in the file. Run `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run TestGetEditorLinkPathConfinementAtRPCBoundary ./internal/uiserver/...` and paste the transcript: the `escape` subtest fails with `GetEditorLink(path="../outside.txt") succeeded, want a refusal` (and `absolute`, `symlink-escape` likewise), while the `in-repo control` still passes — this is the clause "fails if the request succeeds", watched. Revert with `git checkout -- internal/uiserver/editorlink.go`, gate clean, re-run green.

**Family (d) — BRW-11/BRW-12: the scheme allowlist poisoned.** Gate the same file clean (fresh, after (c)'s revert — never overlapped). Add `"javascript": {}` to `editorURLSchemes`. Confirm applied by grep. (Never write that quoted key in a comment when reverting or annotating — Task 1's post-revert gate counts the quoted form.) Run `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestEditorTemplateSchemeAllowlist|TestGetEditorLinkAvailabilityStatesAreAnswersNotErrors' ./internal/uiserver/...` and paste the transcript: the allowlist test fails on `len(editorURLSchemes) == 15` (16) AND on `javascript:alert({path})` being accepted; the answers test fails because the override `javascript:{path}` is now BUILDABLE. Both failure lines are the point — the count guard and the membership guard fire independently. Revert, gate clean, re-run green.

Close the log with `## Closing`: a phase-wide byte-clean proof (`git diff --quiet <first-09-05-commit>..HEAD -- web/src/lib/breadcrumb.ts web/build internal/uiserver/editorlink.go` exit 0, and `git status --porcelain` empty), a non-vacuity assertion naming the three instruments and the fact each was watched fail on the specific assertion it exists for, and a family-to-requirement table: (a) BRW-10 SC1 empty; (b) BRW-10 SC1 stale; (c) BRW-11 SC2; (d) BRW-11/BRW-12 D-13.

If any mutation does NOT produce a failure, stop, do not adjust the instrument, and record the observed negative result with its root cause exactly as 08-MUTATION-LOG.md family (d) did.
  </action>
  <verify>
    <automated>L=.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md; test "$(rg -c '^## Family \(' "$L")" = "4" &amp;&amp; rg -q '^## Family \(b\)' "$L" &amp;&amp; rg -q '^## Family \(c\)' "$L" &amp;&amp; rg -q '^## Family \(d\)' "$L" &amp;&amp; rg -q '^## Closing' "$L" &amp;&amp; rg -q 'succeeded, want a refusal' "$L" &amp;&amp; rg -q '"agrees": false|agrees: false|agrees=false' "$L" &amp;&amp; test "$(rg -c 'git diff --quiet' "$L")" -ge 8</automated>
    <fails_when>a family heading count other than exactly 4, any of (b)/(c)/(d)/Closing absent, the pasted confinement failure line missing (family (c) was narrated), no disagreeing observation in the stale transcript (family (b) was narrated), or fewer than 8 cleanliness-gate records (each of the three new families needs a pre- and post-gate, plus family (a)'s and the closing proof)</fails_when>
    <automated>git diff --quiet -- web/src/lib/breadcrumb.ts web/build internal/uiserver/editorlink.go &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; test "$(rg -o 'ValidateRepoRelativePath' internal/uiserver/editorlink.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o '"javascript"' internal/uiserver/editorlink.go | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>any of the three mutated paths differs from HEAD, an unclean tree, the confinement call absent (family (c) not reverted), or a `"javascript"` key present (family (d) not reverted)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestGetEditorLinkPathConfinementAtRPCBoundary|TestEditorTemplateSchemeAllowlist' ./internal/uiserver/... 2>&amp;1 | tee /tmp/09-05-green.txt | rg -c -- '--- PASS: Test' &amp;&amp; test "$(rg -c -- '--- PASS: Test(GetEditorLinkPathConfinementAtRPCBoundary|EditorTemplateSchemeAllowlist)$' /tmp/09-05-green.txt)" = "2" &amp;&amp; test "$(rg -c -- '--- FAIL' /tmp/09-05-green.txt)" = "0"</automated>
    <fails_when>the two instruments do not both report a top-level PASS after the reverts, or any FAIL line remains</fails_when>
  </verify>
  <acceptance_criteria>
    - `09-MUTATION-LOG.md` has exactly four `## Family (` headings and a `## Closing` section with the phase-wide byte-clean proof, the non-vacuity assertion and the family-to-requirement table.
    - Family (b) pastes a breadcrumb-check transcript showing a gap-line observation with `expected: null` and a non-empty `shown` name, `success=false`; family (c) pastes `GetEditorLink(path="../outside.txt") succeeded, want a refusal`; family (d) pastes both the count failure (16 != 15) and the `javascript` acceptance failure.
    - Every family records both cleanliness gates and a green re-run; the three mutated paths are byte-identical to HEAD at plan close.
    - The committed `corpora/breadcrumb-check.json` still records `success: true` (the stale run wrote to a temp path).
  </acceptance_criteria>
  <done>The stale-breadcrumb, bypassed-confinement and poisoned-allowlist mutations were each watched fail on the assertion that exists for them, every mutation is byte-cleanly reverted, and the log closes with a phase-wide proof.</done>
</task>

<task type="auto">
  <name>Task 2: `09-SECURITY.md` — the browser's external-protocol prompt named as the boundary, every threat with a test or a verdict</name>
  <files>.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md</files>
  <read_first>
    - `.planning/phases/08-tmux-real-pty-harness/08-SECURITY.md` in full — the house format to reproduce section for section: frontmatter (`phase`, `slug`, `status`, `threats_open`, `asvs_level`, `created`), `## Trust Boundaries` (Boundary / Description / Data Crossing), `## Threat Register` (Threat ID / Category / Component / Severity / Disposition / Mitigation / Status) with its three status/severity/disposition legend lines, `## Accepted Risks Log`, `## Security Audit Trail`, and the audit note's verification-by-execution discipline.
    - `.planning/milestones/v0.12.0-phases/03-browse-inspect-navigation/03-SECURITY.md` — the browse-phase precedent with a path-confinement threat (T-03-01/T-03-01b/T-03-17) and its `## Notes` section shape (assumptions and verdicts live there).
    - The `<threat_model>` blocks of 09-01, 09-02, 09-03 and 09-04 — the register is their union (T-09-01…T-09-20 and T-09-SC), deduplicated by id.
    - `internal/uiserver/editorlink_test.go`, `internal/cli/editordiscovery_test.go`, `web/tests/source-lines.test.ts`, `web/tests/editor-prefs.test.ts`, `web/tests/source-pane-editor-link.test.ts` — the test names each mitigation cell cites; cite what exists, verified by grep, never from memory.
    - `09-02-SUMMARY.md` — the human-check outcome for the Cursor/JetBrains presets (confirmed / not tested); `09-04-SUMMARY.md` — the Chromium observation of the gutter click→rpc→assign sequence.
    - `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-CONTEXT.md` § "BRW-13 — SECURITY.md shape" and `09-RESEARCH.md` § Security Domain, § Assumptions Log, § Open Questions.
  </read_first>
  <action>
Write `09-SECURITY.md` in 08-SECURITY.md's exact section order with frontmatter `phase: "09"`, `slug: "source-view-follow-through-breadcrumb-editor-handoff"`, `status: verified` only if every high-severity row is closed (else `open`), `threats_open` = the count of open rows at or above `high`, `asvs_level: 1`, `created` today.

`## Trust Boundaries`: at minimum — browser → GetEditorLink (path/line/col/override); operator shell → flags/env; GetEditorLinkResponse.url → browser → OS URI handler; repository bytes → highlight.js → per-line splitter → `{@html}`; localStorage → request template; `codegraph ui` → host filesystem/PATH (discovery). Immediately below the table, a paragraph headed **The boundary is the browser's external-protocol prompt, not CSP** containing these two sentences verbatim: "CSP does not govern href navigation, so the only thing between a malicious template and an unintended scheme is D-13's positive scheme check on the server." and "The browser's external-protocol prompt is the consent boundary; this phase's job is never to construct a URL that reaches an unintended scheme, not to control browser chrome."

`## Threat Register`: one row per unique id from the four plans' `<threat_model>` blocks. Every Mitigation cell names either a test (`TestGetEditorLinkPathConfinementAtRPCBoundary`, `TestEditorTemplateSchemeAllowlist`, `TestEditorLinkPathEncodingPerPosition`, `TestDiscoverEditorNeverExecutes`, `web/tests/source-lines.test.ts#never un-escapes`, `web/tests/editor-prefs.test.ts#throwing storage`, `web/tests/source-pane-editor-link.test.ts#double-click guard`, …) or a recorded verdict prefixed `Verdict:` (self-XSS via Svelte text interpolation; discovery is a probe — supported by the source guard; the prompt as consent boundary; DNS rebinding inherited from originHostGuard; localStorage holds a template not a path). Include the six threats CONTEXT.md names at minimum, mapped to ids: path traversal/symlink escape → T-09-01 (test); scheme injection → T-09-02 (test); placeholder encoding/template smuggling → T-09-04 (test); override text field as self-XSS → T-09-06 (verdict); discovery probe-never-launch → T-09-07 (verdict + `TestDiscoverEditorNeverExecutes`); external-protocol prompt as consent boundary → T-09-09 (verdict, disposition transfer). Every `high` row must be `mitigate` with a test name (security_block_on: high). Verify each cited test exists by grep and each mitigation by reading the file:line at HEAD, as 08-SECURITY.md's audit note did — never by trusting the plan text.

`## Accepted Risks Log`: one R-09-NN row per `accept`/`transfer` disposition with rationale, accepted-by (plan-time disposition) and date.

`## Notes`: (1) **Assumptions carried** — A1 (Cursor template) and A2 (JetBrains template) with their status from 09-02's human-check ("confirmed against <IDE version>" or "not tested — remains [ASSUMED]; the picker shows the unverified note"), and the reasoning for choosing `idea://open?file=&line=` (absolute path, spatie/ignition-corroborated) over the Toolbox form; (2) **Transient user activation** — Chromium observed the gutter click→rpc→`location.assign` sequence issuing the rpc and the navigation attempt (corpora/breadcrumb-check.json `gutterClickIssuedRpc`); Safari/WebKit and Firefox are UNVERIFIED and recorded as such (research A3 / Open Question 2); the header link is a resolved `<a href>` and carries no such risk; (3) **Server-side shell-out** is out of scope by construction (SRV-03) — the verdict, restated.

`## Security Audit Trail`: one row for this plan's own pass with totals; state which rows were checked by execution (re-running the named tests) and which by reading.
  </action>
  <verify>
    <automated>S=.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md; test -f "$S" &amp;&amp; rg -q 'CSP does not govern href navigation' "$S" &amp;&amp; rg -q "external-protocol prompt is the consent boundary" "$S" &amp;&amp; rg -q '^## Trust Boundaries' "$S" &amp;&amp; rg -q '^## Threat Register' "$S" &amp;&amp; rg -q '^## Accepted Risks Log' "$S" &amp;&amp; rg -q '^## Security Audit Trail' "$S" &amp;&amp; rg -q '^asvs_level: 1' "$S" &amp;&amp; test "$(rg -c '^\| T-09-' "$S")" -ge 15</automated>
    <fails_when>the file is missing, either boundary sentence is absent (the prompt-not-CSP statement is the criterion), any of the four house sections is absent, the ASVS level is not 1, or fewer than 15 threat rows (the union of the four plans' registers is at least 15 unique ids plus T-09-SC)</fails_when>
    <automated>S=.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md; ROWS=$(rg '^\| T-09-' "$S"); test -n "$ROWS" &amp;&amp; test "$(printf '%s\n' "$ROWS" | rg -v 'Test[A-Z][A-Za-z]+|\.test\.ts|Verdict:' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(printf '%s\n' "$ROWS" | rg -i 'TBD|\|\s*\|' | wc -l | tr -d ' ')" = "0" &amp;&amp; HIGH=$(printf '%s\n' "$ROWS" | rg '\| high \|'); test -n "$HIGH" &amp;&amp; test "$(printf '%s\n' "$HIGH" | rg -v 'mitigate' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(printf '%s\n' "$HIGH" | rg -v 'Test[A-Z][A-Za-z]+|\.test\.ts' | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>zero threat rows (vacuous), any row lacking both a `Test…` name and a `Verdict:` marker, any row containing TBD or an empty cell, zero high rows (the positive control — T-09-01/02/05/07 are high), or any high row lacking `mitigate` or a test reference (the security_block_on: high gate)</fails_when>
    <automated>S=.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-SECURITY.md; for t in $(rg -o 'Test[A-Z][A-Za-z]+' "$S" | sort -u); do rg -q "func $t\(" internal/ web/ || { echo "cited test $t not found"; exit 1; }; done; test "$(rg -o 'Test[A-Z][A-Za-z]+' "$S" | sort -u | wc -l | tr -d ' ')" -ge 6</automated>
    <fails_when>any cited Go test name has no `func Test…(` definition under internal/ or web/, or fewer than 6 distinct Go test names are cited (a register that cites nothing real)</fails_when>
  </verify>
  <acceptance_criteria>
    - `09-SECURITY.md` has the 08-SECURITY.md frontmatter keys and sections in order, both verbatim boundary sentences, at least 15 `T-09-` rows each with a test name or a `Verdict:` marker, every high row `mitigate` + test, an Accepted Risks Log row per accept/transfer, and Notes carrying A1/A2's execution-time status, the Safari-unverified statement, and the shell-out verdict.
    - Every Go test cited exists at HEAD; every vitest citation names an existing file under web/tests/.
    - `threats_open` equals the number of open rows at or above high (expected 0).
  </acceptance_criteria>
  <done>The phase's threat model exists in the house format, names the browser's external-protocol prompt as the boundary in so many words, and leaves no threat as prose alone.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — every gate green on a clean tree, Zed nowhere, presets counted from the binary</name>
  <files>.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md</files>
  <read_first>
    - `Taskfile.yml` — `test:unit` (117), `web:test` (657), `web:drift` (1187), `proto:drift` (369), `web:components:drift` (1296).
    - `web/scripts/breadcrumb-check.mjs` and `corpora/breadcrumb-check.json` (plans 09-03/09-04).
    - `internal/uiserver/editorpresets.go` (plan 09-01) — `EditorPresets()`.
    - `.planning/ROADMAP.md` § Phase 9 success criterion 3 — "a check reports the preset count and finds Zed in neither the preset list nor any claim the UI makes".
  </read_first>
  <action>
Run the full phase gate set once, in this order, and paste the transcript (with the counts each gate prints) into the SUMMARY's `## Phase close` section: `GOTOOLCHAIN=go1.26.6 task proto:drift`; `task web:drift`; `task web:components:drift`; `GOTOOLCHAIN=go1.26.6 task test:unit`; `task web:test`; `GOTOOLCHAIN=go1.26.6 task build:release && node web/scripts/breadcrumb-check.mjs --out "$TMPDIR/breadcrumb-close.json"` (a fresh live run; the committed record from 09-04 stands). Then the two success-criterion-3 checks: the preset count from the BINARY, not from source — `GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run TestEditorPresetsAreExactlyThreeAndNameNoZed ./internal/uiserver/...` (the test logs `inspected 9 strings, presets=3`); and the repo-wide Zed absence with its positive control — zero case-insensitive matches under `web/src` and `internal/uiserver/editorpresets.go`, paired with a preset-scheme count of exactly 3 in `editorpresets.go` and a non-zero count of `editor-preset-` test ids in the picker. Finally confirm `git status --porcelain` is empty and append one line to `09-MUTATION-LOG.md`'s `## Closing` recording the phase-close run's date and the HEAD hash it was executed at. Commit as `docs(09-05): record phase-close gate run`.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 task proto:drift &amp;&amp; task web:drift &amp;&amp; task web:components:drift &amp;&amp; GOTOOLCHAIN=go1.26.6 task test:unit &amp;&amp; task web:test</automated>
    <fails_when>non-zero exit from any target; web:test additionally fails on passed != total or a zero-test run by its own design</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 task build:release &amp;&amp; node web/scripts/breadcrumb-check.mjs --out "${TMPDIR:-/tmp}/breadcrumb-close.json"; node -e 'const r=require(process.env.TMPDIR?process.env.TMPDIR+"/breadcrumb-close.json":"/tmp/breadcrumb-close.json");console.log(JSON.stringify({success:r.success,obs:r.observations.length,href:r.editorLinkHref,gutter:r.gutterLinkCount,click:r.gutterClickIssuedRpc}));if(r.success!==true){console.error("FAIL");process.exit(1)}console.log("PASS")'</automated>
    <fails_when>the fresh live run reports success false (the comparator prints the numbers first and runs even when the script exited non-zero)</fails_when>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run TestEditorPresetsAreExactlyThreeAndNameNoZed ./internal/uiserver/... 2>&amp;1 | tee /tmp/09-05-presets.txt | rg -q -- '--- PASS: TestEditorPresetsAreExactlyThreeAndNameNoZed' &amp;&amp; test "$(printf 'Zed zed://x' | rg -o -i '\bzed\b|zed://' | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(rg -o -i '\bzed\b|zed://' web/src internal/uiserver/editorpresets.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'vscode://|cursor://|idea://' internal/uiserver/editorpresets.go | wc -l | tr -d ' ')" = "3" &amp;&amp; test "$(rg -o 'editor-preset-' web/src/lib/components/browse/EditorLinkPicker.svelte | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>the preset test's PASS line is absent, any case-insensitive "zed" under web/src or in the preset file, a preset-scheme count other than exactly 3 (the positive control that the right file is inspected), or no picker test-id prefix (the positive control that the UI's claims were inspected)</fails_when>
    <automated>test -z "$(git status --porcelain)" &amp;&amp; rg -q 'phase-close' .planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-MUTATION-LOG.md</automated>
    <fails_when>an unclean tree at plan close, or no phase-close line recorded in the log</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY's `## Phase close` section pastes each gate's own count line (proto:drift's four files, web:test's `numTotalTests`/`numPassedTests`, test:unit's package lines, the live gate's observation count and editor-link fields, the preset test's `presets=3`).
    - `rg -i zed web/src internal/uiserver/editorpresets.go` reports 0 alongside the positive controls.
    - `git status --porcelain` is empty and `09-MUTATION-LOG.md`'s Closing carries the phase-close date and HEAD hash.
  </acceptance_criteria>
  <done>Every phase gate is green on a clean tree, the preset count is reported from the binary, and Zed is found in neither the preset list nor any string the UI ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree ↔ deliberate product mutations | families (b)-(d) mutate shipped code to prove instruments can fail, bracketed by cleanliness gates |
| 09-SECURITY.md ↔ the tests it cites | the register claims closure by naming tests; Task 2's verify checks each cited test exists at HEAD |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11 | Repudiation | the pasted RED transcripts in 09-MUTATION-LOG.md | medium | mitigate | each family names the exact command and pastes verbatim output; a non-failing mutation is reported as an observed negative with root cause, never rewritten (08-MUTATION-LOG.md family (d) precedent) |
| T-09-21 | Tampering | a mutation surviving into HEAD (breadcrumb.ts, web/build, editorlink.go) | high | mitigate | pre- and post-gates per family, shared-file mutations strictly one at a time, Task 1's verify re-checks `git diff --quiet` on all three paths and greps the restored `ValidateRepoRelativePath` call and the absent `"javascript"` key at HEAD |
| T-09-22 | Repudiation | a SECURITY.md register citing tests that do not exist | medium | mitigate | Task 2's verify resolves every cited `Test…` name to a `func Test…(` definition and requires at least 6 distinct names |
| T-09-19 | Tampering | `web/build/` drift after family (b)'s rebuild | medium | mitigate | the revert is `git checkout -- web/build` (committed bytes restored), and Task 3 runs `task web:drift` |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed by this plan |
</threat_model>

<verification>
- `09-MUTATION-LOG.md` has four families and a Closing with the byte-clean proof and the phase-close line.
- `09-SECURITY.md` passes both structural gates (boundary sentences, sections, ≥15 rows, every row test-or-verdict, every high row mitigated with a test, every cited test resolvable).
- All phase gates green on a clean tree; Zed check 0 with positive controls; presets=3 from the binary.
</verification>

<success_criteria>
- Success criterion 1's "stale" demonstration and success criterion 2's rejected-path RED are committed as pasted transcripts; success criterion 3's preset count and Zed absence are checked; success criterion 4's SECURITY.md exists in the house format naming the prompt, not CSP, with every threat carrying a test or a verdict.
</success_criteria>

<output>
Create `.planning/phases/09-source-view-follow-through-breadcrumb-editor-handoff/09-05-SUMMARY.md` when done, with `coverage:` entries whose `verification.ref` values name `09-MUTATION-LOG.md#Family (b)`, `#Family (c)`, `#Family (d)`, `09-SECURITY.md` and the phase-close transcript, following `08-04-SUMMARY.md`'s shape.
</output>
