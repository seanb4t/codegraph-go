---
phase: 10-index-health-the-coverage-denominator
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - internal/query/coverage_test.go
  - internal/query/coverage.go
  - internal/uiserver/coverage_test.go
  - internal/uiserver/coverage.go
autonomous: true
requirements: [HLT-04, HLT-05, HLT-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Against the full coverage fixture (Plan 02's six committed files plus the generated oversize file and dangling symlink), `Engine.CoverageSummary()` reports EXACTLY discovered 6, indexed 1, excluded 6, extraction_failed 1 and by-reason {UNSUPPORTED_EXTENSION 2, DIR_DOTPREFIX 1, DIR_VENDOR 1, BUILD_TAG 1, SIZE_LIMIT 1} when the store lives outside the repo — and the identical numbers over the real listener except excluded 7 / DIR_DOTPREFIX 2 (the in-repo `.codegraph` store directory is itself a pruned dot-directory; `discovered` stays 6 because directory records never count — D-01)"
    - "HLT-06 ordering: `GetCoverage` rows come in two byte-ordered segments — extraction-failed File records by path, then ExcludedFile records by path — so three pages of size 3 concatenate to exactly the single page of size 1000, and replaying a page token returns the identical page"
    - "HLT-06 empty: a known graph with zero exclusions (one Go file, no go.mod) answers `known == true`, zero rows, empty token, and a summary of discovered 1 / indexed 1 / excluded 0; an old graph answers `known == false`, zero rows, zero counts (D-15)"
    - "`page_size <= 0` yields 200 rows and `page_size > 1000` yields exactly 1000 rows with a non-empty token on a repo with 1100 excluded files (T-10-03)"
    - "A malformed, wrong-kind, oversized, or filter-inconsistent page token is refused with `query.ErrInvalidArgument` → `connect.CodeInvalidArgument`, never a panic and never a silent restart from page one; the refusal message never carries the absolute repo path (T-10-04)"
    - "An extraction-failed row's `detail` never contains the absolute repository root and is at most 256 bytes of valid UTF-8 (T-10-05)"
    - "HLT-05 precision: the fixture summary satisfies `discovered == indexed + extraction_failed + (excluded − directory-level)` exactly — the summary is additive counting over int64, and the test asserts it from the reported values"
    - statement: "HLT-04 concurrency: summary and first page inside one request share one snapshot Reader; a Sync landing between two page requests may shift later pages — documented on `next_page_token`, not directly testable without a race harness"
      verification: backstop
  artifacts:
    - "internal/query/coverage_test.go — fixture summary, ordering/paging stability, reason filter, malformed-token table, page-size clamp, empty-known and old-graph cases, detail scrubbing/bounding, the Engine-level mutate-without-reindex twin (D-14a)"
    - "internal/uiserver/coverage_test.go — listener-level twins of the above plus the CodeInvalidArgument mapping and the in-repo `.codegraph` DIR_DOTPREFIX effect"
  key_links:
    - "`copyCoverageFixture` (per package, WalkDir copy of `internal/indexer/testdata/coverage`) + generated `huge.go`/`broken.py` → `indexer.Run` → `Engine`/listener under test"
    - "`decodeCoverageToken` → `invalidArgumentf` → `mapEngineError` → `connect.CodeInvalidArgument`"
  prohibitions:
    - "Tests MUST NOT derive expected reasons by walking the fixture — expected rows are literal tables"
    - "`GetCoverage` MUST NOT be given a `kind` filter or any field beyond D-10's three plus the additive `known` on the response — the request shape is frozen by Plan 01's fixture"
---

<objective>
Harden the read path and the wire against the full fixture: exact summary numbers, stable two-segment ordering across pages, the reason filter, page-size clamping, the closed set of token refusals, detail scrubbing, and the empty/known-versus-unknown distinction — at the Engine level and again through the real listener with the Connect error code asserted.

Purpose: Plan 01 proved one path; these tests pin the contract every UI decision rests on (D-06, D-09, D-10, D-15, D-16) and close the threat rows T-10-03/04/05 with tests rather than prose.

Output: expanded `internal/query/coverage_test.go` and `internal/uiserver/coverage_test.go`; any fix they force in `coverage.go` (either package) is small and local.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-index-health-the-coverage-denominator/10-CONTEXT.md
@.planning/phases/10-index-health-the-coverage-denominator/10-01-SUMMARY.md
@.planning/phases/10-index-health-the-coverage-denominator/10-02-SUMMARY.md
@.planning/phases/10-index-health-the-coverage-denominator/10-03-SUMMARY.md

Every Go command: `GOTOOLCHAIN=go1.26.6`. Plan 02's SUMMARY records whether the dangling-symlink technique passed its probe or the chmod fallback was adopted — replicate whichever technique it landed when generating `broken.py` here (test helpers are unexported and per-package; copy, do not import).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine-level contract — fixture summary, ordering/paging stability, reason filter, token refusals, clamp, empty-known, detail scrubbing</name>
  <reversibility rating="reversible">Tests over an internal API; any fix is confined to `internal/query/coverage.go`.</reversibility>
  <files>internal/query/coverage_test.go, internal/query/coverage.go</files>
  <read_first>
    - internal/query/coverage.go (as committed by Plan 01 — the two segments, `decodeCoverageToken`'s validation list, the clamp constants, the detail scrub)
    - internal/query/coverage_test.go (Plan 01's base tests — extend the file; keep `TestCoverageSourceNeverWalksDisk` and `TestCoverageSummaryOnOldGraphIsUnknown` intact)
    - internal/query/engine_test.go (`copyFixture`/`indexFixture` scaffolding for this package), internal/indexer/coverage_fixture_test.go (Plan 02's `copyCoverageFixture`, `writeOversizeGoFile`, `writeDanglingSymlink` — replicate in package query)
    - internal/query/errors.go (`ErrInvalidArgument`, `invalidArgumentf`)
    - internal/graphstore/store.go (`Writer.PutFile` — used to fabricate a File record with a 1000-byte error string)
  </read_first>
  <behavior>
    - `TestCoverageSummaryMatchesFixture`: copy fixture + generate the two files; `indexer.Run(copy, t.TempDir())` (store OUTSIDE the copy); `NewWithRoot(snap, copy).CoverageSummary()` == {Known true, Discovered 6, Indexed 1, Excluded 6, ExtractionFailed 1, ExcludedByReason exactly the five keys with counts 2/1/1/1/1}; invariant `Discovered == Indexed + ExtractionFailed + (Excluded − ByReason[DIR_VENDOR] − ByReason[DIR_DOTPREFIX])`; the test logs every number.
    - `TestCoverageRowsOrderingAndPagingAreStable`: single page (PageSize 1000) == exactly [broken.py(EXTRACTION_FAILED), .hidden, go.mod, huge.go, notes.md, tagged.go, vendor] (7 rows, empty token); PageSize 3 → pages of [3, 3, 1] whose concatenation equals the single page; replaying page 2's token twice yields `reflect.DeepEqual` pages; every token is non-empty except the last and none contains a row path verbatim (opaque).
    - `TestCoverageRowsReasonFilter`: Reason UNSUPPORTED_EXTENSION → exactly [go.mod, notes.md] (no extraction-failed row leaks into a filtered listing); Reason DIR_VENDOR → [vendor]; Reason SIZE_LIMIT → [huge.go] with Detail beginning `4194305 bytes`.
    - `TestCoverageRowsRejectsMalformedTokens`: table — `"not*base64*"`, base64url of `""`, base64url of `"q" + "path"` (unknown kind byte), base64url of `"x" + strings.Repeat("a", 4097)`, base64url of `"x\xff\xfe"` (invalid UTF-8), and a valid `'f'` token combined with `Reason: UNSUPPORTED_EXTENSION` → every row returns `errors.Is(err, ErrInvalidArgument)`; the table has ≥ 6 rows (logged) and a positive control: a valid `'x'` token for `go.mod` returns the rows after it with nil error.
    - `TestCoverageRowsPageSizeClamp`: a generated repo with `main.go` + 1100 files `n0000.md … n1099.md` (no go.mod → 1100 exclusions) → `PageSize: 5000` returns exactly 1000 rows and a non-empty token; `PageSize: 0` returns exactly 200; `PageSize: -7` returns 200.
    - `TestCoverageRowsEmptyPageOnKnownGraphWithZeroExclusions`: repo of only `main.go` (no go.mod) → summary {Known true, Discovered 1, Indexed 1, Excluded 0, ExtractionFailed 0, ExcludedByReason empty-or-nil} and `CoverageRows` → Known true, zero rows, empty token (contrast with the old-graph test already in the file: Known false).
    - `TestCoverageRowsDetailIsScrubbedAndBounded`: from the fixture run, the `broken.py` row's Detail does not contain the absolute copy path (`!strings.Contains(detail, copy)`) and DOES contain `./broken.py`; separately, fabricate via a Writer a File record `{Path: "long.py", Errors: []string{strings.Repeat("é", 600)}}` on a store whose Meta has HasCoverage true → its row Detail has `len <= 256` and `utf8.ValidString`.
    - `TestCoverageRowsSurviveDiskMutationWithoutReindex` (the Engine-level twin of Plan 02's D-14a test — the one Plan 06's family (c) mutates against): index the fixture; strip the build tag from `tagged.go` and delete `notes.md` on disk; re-open the store and `NewWithRoot(snap, copy).CoverageRows(CoverageRowsOptions{PageSize: 1000})` STILL lists `notes.md` (UNSUPPORTED_EXTENSION) and `tagged.go` (BUILD_TAG), and `CoverageSummary()` still reports excluded 6 — a reader that consulted the disk would drop both; comment anchor: `mutation: filter rows by present-tense disk state → RED`.
  </behavior>
  <action>
    Add the helpers and the eight tests to `internal/query/coverage_test.go`. Expected rows are literal tables (path, kind, reason, detail-prefix). If any test exposes a defect in `coverage.go` (e.g. a filtered listing leaking extraction-failed rows, a non-rune-safe truncation, an `'f'` token accepted with a filter), fix it there in the same task with a one-line comment naming the test — keep `TestCoverageSourceNeverWalksDisk`'s forbidden list satisfied (the fix must not add filesystem calls). Write all tests first; the RED transcript will show at least the compile errors for the new helpers.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestCoverageSummaryMatchesFixture|TestCoverageRowsOrderingAndPagingAreStable|TestCoverageRowsReasonFilter|TestCoverageRowsRejectsMalformedTokens|TestCoverageRowsPageSizeClamp|TestCoverageRowsEmptyPageOnKnownGraphWithZeroExclusions|TestCoverageRowsDetailIsScrubbedAndBounded|TestCoverageRowsSurviveDiskMutationWithoutReindex|TestCoverageSummaryOnOldGraphIsUnknown|TestCoverageSourceNeverWalksDisk' ./internal/query/ 2&gt;&amp;1 | tee /tmp/10-05-t1.txt | rg -c -- '^--- PASS: Test' &amp;&amp; test "$(rg -o -- '--- PASS: Test(CoverageSummaryMatchesFixture|CoverageRowsOrderingAndPagingAreStable|CoverageRowsReasonFilter|CoverageRowsRejectsMalformedTokens|CoverageRowsPageSizeClamp|CoverageRowsEmptyPageOnKnownGraphWithZeroExclusions|CoverageRowsDetailIsScrubbedAndBounded|CoverageRowsSurviveDiskMutationWithoutReindex|CoverageSummaryOnOldGraphIsUnknown|CoverageSourceNeverWalksDisk)' /tmp/10-05-t1.txt | wc -l | tr -d ' ')" = "10" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/10-05-t1.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; ! rg -q 'no tests to run' /tmp/10-05-t1.txt &amp;&amp; test "$(rg -o 'discovered=6 indexed=1 excluded=6 extraction_failed=1' /tmp/10-05-t1.txt | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'malformed tokens inspected: [6-9]' /tmp/10-05-t1.txt | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>fewer than 10 named PASS lines, any FAIL, "no tests to run", the fixture numbers are not logged exactly, or the malformed-token table logs fewer than 6 inspected rows</fails_when>
    <automated>test "$(rg -o 'filepath\.WalkDir\(|os\.ReadDir\(|os\.Stat\(|os\.Lstat\(|os\.ReadFile\(|os\.Open\(' internal/query/coverage.go | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(rg -o 'CoverageMaxPageSize = 1000|CoverageDefaultPageSize = 200|coverageDetailMaxBytes = 256' internal/query/coverage.go | wc -l | tr -d ' ')" = "3" &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/query/... 2&gt;&amp;1 | tail -3 | rg -c '^ok' &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/query/archtest/ 2&gt;&amp;1 | rg -c '^ok'</automated>
    <fails_when>a filesystem call crept into coverage.go while fixing a test, a constant moved, or the query package / archtest is not green</fails_when>
  </verify>
  <acceptance_criteria>
    - `TestCoverageRowsOrderingAndPagingAreStable` asserts the concatenation equality with `reflect.DeepEqual` and the opaque-token property (no row path substring in any token)
    - `TestCoverageRowsRejectsMalformedTokens` has a positive-control row (valid token accepted) and logs `malformed tokens inspected: N` with N ≥ 6
    - `TestCoverageSummaryMatchesFixture` logs `discovered=6 indexed=1 excluded=6 extraction_failed=1` and asserts the additive invariant from those reported values
  </acceptance_criteria>
  <done>The Engine's coverage contract is pinned end to end: exact fixture numbers, stable paging, filter semantics, a closed refusal set with a positive control, clamps, the empty-known case, and scrubbed bounded detail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Listener-level contract — fixture numbers over GetHealth, GetCoverage paging round trip, CodeInvalidArgument mapping, clamp, filter, old graph — through the real server</name>
  <reversibility rating="reversible">Tests over the wire; any fix is confined to `internal/uiserver/coverage.go`.</reversibility>
  <files>internal/uiserver/coverage_test.go, internal/uiserver/coverage.go</files>
  <read_first>
    - internal/uiserver/coverage.go and coverage_test.go (Plan 01's base — extend the file; keep `TestExclusionReasonEnumsAgree` and `TestGetHealthCarriesCoverageForBuildTagExclusion`)
    - internal/uiserver/server_test.go:29-72 (`copyGofixture`/`indexGofixture` — the in-repo `.codegraph/store` placement `Listen` requires), internal/uiserver/handlers_test.go:80-100 (`startedServer`)
    - internal/uiserver/permalink_test.go or editorlink_test.go (how existing tests assert `connect.CodeOf(err)` and that error text excludes the absolute fixture path — the T-09-03 discipline)
    - internal/uiserver/handlers.go:81-130 (`mapEngineError`'s `ErrInvalidArgument` arm)
    - Plan 02's `coverage_fixture_test.go` helpers (replicate `copyCoverageFixture`, `writeOversizeGoFile`, `writeDanglingSymlink` in package uiserver)
  </read_first>
  <behavior>
    - `TestGetHealthCoverageMatchesFixture`: fixture copy + generated files, indexed at `<copy>/.codegraph/store`, `startedServer`; `GetHealth().coverage` == {known true, discovered 6, indexed 1, excluded 7, extraction_failed 1, excluded_by_reason {UNSUPPORTED_EXTENSION 2, DIR_DOTPREFIX 2, DIR_VENDOR 1, BUILD_TAG 1, SIZE_LIMIT 1}} — the extra DIR_DOTPREFIX is `.codegraph` (logged and asserted as a row in `GetCoverage` with detail `.codegraph`); the additive invariant holds from the wire values; every other `GetHealth` field still matches `eng.Status` as `TestGetHealthProjectsStatusResult` requires (that test stays green).
    - `TestGetCoveragePagesRoundTripThroughTheListener`: `page_size: 3` pages until `next_page_token == ""` → 8 rows total whose concatenation equals a single `page_size: 1000` call; kinds: exactly one COVERAGE_ROW_KIND_EXTRACTION_FAILED row (broken.py) and it is FIRST; tokens are non-empty except the last and contain no row path verbatim.
    - `TestGetCoverageMalformedTokenIsInvalidArgument`: `page_token: "not*a*token"` → `connect.CodeOf(err) == connect.CodeInvalidArgument`, and `err.Error()` does not contain the absolute fixture path; a valid token replay succeeds (positive control).
    - `TestGetCoveragePageSizeIsClampedOnTheWire`: repo with 1100 `.md` files → `page_size: 5000` → exactly 1000 rows + non-empty token; `page_size: 0` → 200.
    - `TestGetCoverageReasonFilterOnTheWire`: `reason: EXCLUSION_REASON_UNSUPPORTED_EXTENSION` → exactly [go.mod, notes.md], each with kind EXCLUDED; the `broken.py` row (unfiltered call) has kind EXTRACTION_FAILED, `reason == UNSPECIFIED`, detail containing `./broken.py` and not the absolute copy path.
    - `TestGetCoverageAndGetHealthOnOldGraphAreKnownFalse`: after indexing, fabricate `HasCoverage = false` on the Meta (Writer: PutMeta clone) and clear the namespace → `GetHealth().coverage.known == false` with all counts 0 and an empty map; `GetCoverage` → `known == false`, zero rows, empty token; neither call errors.
  </behavior>
  <action>
    Add the helpers and six tests to `internal/uiserver/coverage_test.go`. Use `uiv1connect.NewUIServiceClient(http.DefaultClient, srv.URL())` exactly like `health_test.go`. If a test exposes a handler defect (e.g. the enum cast for a filter value, `known` not propagated), fix it in `internal/uiserver/coverage.go` with a comment naming the test. Do not touch `readonly_test.go` (the fixture there is frozen by Plan 01) or `handlers.go`.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=go1.26.6 go test -count=1 -v -run 'TestGetHealthCoverageMatchesFixture|TestGetCoveragePagesRoundTripThroughTheListener|TestGetCoverageMalformedTokenIsInvalidArgument|TestGetCoveragePageSizeIsClampedOnTheWire|TestGetCoverageReasonFilterOnTheWire|TestGetCoverageAndGetHealthOnOldGraphAreKnownFalse|TestGetHealthProjectsStatusResult|TestExclusionReasonEnumsAgree|TestUIServiceMethodSetIsExactlyTheReadSet|TestCoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected' ./internal/uiserver/ 2&gt;&amp;1 | tee /tmp/10-05-t2.txt | rg -c -- '^--- PASS: Test' &amp;&amp; test "$(rg -o -- '--- PASS: Test(GetHealthCoverageMatchesFixture|GetCoveragePagesRoundTripThroughTheListener|GetCoverageMalformedTokenIsInvalidArgument|GetCoveragePageSizeIsClampedOnTheWire|GetCoverageReasonFilterOnTheWire|GetCoverageAndGetHealthOnOldGraphAreKnownFalse|GetHealthProjectsStatusResult|ExclusionReasonEnumsAgree|UIServiceMethodSetIsExactlyTheReadSet|CoverageRPCNameClearsMutatingVerbsWhileDecoyIsRejected)' /tmp/10-05-t2.txt | wc -l | tr -d ' ')" = "10" &amp;&amp; test "$(rg -o -- '--- FAIL' /tmp/10-05-t2.txt | wc -l | tr -d ' ')" = "0" &amp;&amp; ! rg -q 'no tests to run' /tmp/10-05-t2.txt &amp;&amp; test "$(rg -o 'wire: discovered=6 indexed=1 excluded=7 extraction_failed=1' /tmp/10-05-t2.txt | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>fewer than 10 named PASS lines, any FAIL, "no tests to run", or the wire numbers are not logged exactly (the `.codegraph` dot-dir effect on `excluded` must be visible in the output)</fails_when>
    <automated>test "$(rg -o 'connect\.CodeInvalidArgument' internal/uiserver/coverage_test.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; test "$(rg -o 'HasCoverage = false' internal/uiserver/coverage_test.go | wc -l | tr -d ' ')" -ge 1 &amp;&amp; LOG=$(git log -n 30 --format=%s -- internal/uiserver/readonly_test.go internal/uiserver/handlers.go internal/uiproto) &amp;&amp; test -n "$LOG" &amp;&amp; test "$(printf '%s\n' "$LOG" | rg -o '\(10-05\)' | wc -l | tr -d ' ')" = "0" &amp;&amp; GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/uiserver/... 2&gt;&amp;1 | tail -2 | rg -c '^ok'</automated>
    <fails_when>the error-code assertion or the old-graph fabrication is missing, a commit of this plan touched a frozen file (readonly_test.go, handlers.go, the proto surface), or the uiserver package is not fully green</fails_when>
  </verify>
  <acceptance_criteria>
    - `TestGetHealthCoverageMatchesFixture` logs `wire: discovered=6 indexed=1 excluded=7 extraction_failed=1` and asserts the `.codegraph` DIR_DOTPREFIX row exists
    - `TestGetCoveragePagesRoundTripThroughTheListener` asserts concatenation equality and the EXTRACTION_FAILED-first ordering
    - `TestGetCoverageMalformedTokenIsInvalidArgument` asserts both the Connect code and the absence of the absolute path in the error text
  </acceptance_criteria>
  <done>The wire contract for coverage is pinned through the real listener: exact numbers, stable pages, the filter, the clamp, the invalid-argument mapping with no path leak, and the old-graph unknown state on both rpcs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GetCoverage` (`page_size`, `page_token`, `reason`) | client-steerable inputs on a loopback rpc |
| `File.errors` text → `CoverageRow.detail` → browser | error strings that may embed absolute host paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Denial of Service | `page_size` | medium | mitigate | clamp to 1000 proven at Engine and wire level (`TestCoverageRowsPageSizeClamp`, `TestGetCoveragePageSizeIsClampedOnTheWire`) |
| T-10-04 | Tampering / DoS | `page_token` | medium | mitigate | closed refusal table (≥ 6 malformed shapes) + positive control; wire mapping to `CodeInvalidArgument`; tokens are opaque (no path substring) and never a Pebble bound |
| T-10-05 | Information Disclosure | absolute repo root in `detail` or in a refusal message | medium | mitigate | `TestCoverageRowsDetailIsScrubbedAndBounded`, `TestGetCoverageReasonFilterOnTheWire` (detail), `TestGetCoverageMalformedTokenIsInvalidArgument` (error text) |
| T-10-15 | Repudiation | an old graph read as 0/0 over the wire | medium | mitigate | `TestGetCoverageAndGetHealthOnOldGraphAreKnownFalse` — both rpcs answer `known == false` with no counts |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | not applicable — no package-manager install in this phase |
</threat_model>

<verification>
- `GOTOOLCHAIN=go1.26.6 go test -count=1 ./internal/query/... ./internal/uiserver/...` green
- `internal/query/archtest` green; `coverage.go`'s filesystem-call count still 0
</verification>

<success_criteria>
- Exact fixture numbers at Engine and wire level, with the `.codegraph` dot-dir effect explained by the tests themselves
- Two-segment byte ordering and page stability proven; filter, clamp, refusal set, scrubbing, and empty-vs-unknown all pinned by tests that report what they inspected
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `internal/query/coverage_test.go`: `copyCoverageFixtureForQuery`, `TestCoverageSummaryMatchesFixture`, `TestCoverageRowsOrderingAndPagingAreStable`, `TestCoverageRowsReasonFilter`, `TestCoverageRowsRejectsMalformedTokens`, `TestCoverageRowsPageSizeClamp`, `TestCoverageRowsEmptyPageOnKnownGraphWithZeroExclusions`, `TestCoverageRowsDetailIsScrubbedAndBounded`, `TestCoverageRowsSurviveDiskMutationWithoutReindex`
- `internal/uiserver/coverage_test.go`: `copyCoverageFixtureForUI`, `TestGetHealthCoverageMatchesFixture`, `TestGetCoveragePagesRoundTripThroughTheListener`, `TestGetCoverageMalformedTokenIsInvalidArgument`, `TestGetCoveragePageSizeIsClampedOnTheWire`, `TestGetCoverageReasonFilterOnTheWire`, `TestGetCoverageAndGetHealthOnOldGraphAreKnownFalse`

<output>
Create `.planning/phases/10-index-health-the-coverage-denominator/10-05-SUMMARY.md` when done — include the logged fixture numbers at both levels and any `coverage.go` fix a test forced.
</output>
