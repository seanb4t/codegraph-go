node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit

# NOTE: no "/build" rule here — web/build/ is deliberately COMMITTED (D-03)
# and go:embed'd by web/embed.go. The upstream sv/SvelteKit scaffold template
# ships a bare "/build" line here; it is pruned deliberately (RESEARCH.md
# Pitfall 2). Do not re-add it.

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
