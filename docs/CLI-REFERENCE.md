<!-- Generated by tools/clidoc from the live codegraph Cobra tree — never hand-edited. Regenerate with `task docs:cli`; `task docs:cli:drift` fails the build if this file is stale. -->

# codegraph CLI reference

This file documents every non-hidden codegraph command and flag; it is generated from the same command tree the binary runs, so `codegraph <command> --help` prints the same text as the matching section below (hidden commands are intentionally omitted).

## codegraph

Pre-indexed code knowledge graph for coding agents

### Synopsis

codegraph builds and maintains a local knowledge graph of a repository's symbols, edges, and files so coding agents can query code structure without repeated grep/read passes.

### Options

```
      --color string   colour output: auto (detect), always, or never (default "auto")
  -h, --help           help for codegraph
  -v, --version        version for codegraph
```

### SEE ALSO

* [codegraph affected](#codegraph-affected)	 - List test symbols impacted by changes to the given files
* [codegraph callees](#codegraph-callees)	 - List a symbol's forward call targets
* [codegraph callers](#codegraph-callers)	 - List a symbol's reverse callers
* [codegraph completion](#codegraph-completion)	 - Generate the autocompletion script for the specified shell
* [codegraph daemon](#codegraph-daemon)	 - List and manage running codegraph daemons
* [codegraph explore](#codegraph-explore)	 - Explore relevant symbols: verbatim source, call paths, blast radius
* [codegraph files](#codegraph-files)	 - Browse the indexed file structure
* [codegraph githooks](#codegraph-githooks)	 - Manage git sync hooks (post-commit/post-merge/post-checkout)
* [codegraph impact](#codegraph-impact)	 - Depth-bounded reverse blast radius of a symbol
* [codegraph index](#codegraph-index)	 - Deterministically rebuild the graph from scratch
* [codegraph init](#codegraph-init)	 - Create .codegraph/ and build the full graph in one step
* [codegraph install](#codegraph-install)	 - Configure coding agents to use this codegraph binary as their MCP server
* [codegraph node](#codegraph-node)	 - Show a symbol's signature, calls, and callers, or a line-numbered file read
* [codegraph search](#codegraph-search)	 - Lexically search symbol names/qualified names
* [codegraph serve](#codegraph-serve)	 - Run the codegraph MCP server
* [codegraph status](#codegraph-status)	 - Report index health and counts
* [codegraph sync](#codegraph-sync)	 - Incrementally update the graph from changed files
* [codegraph telemetry](#codegraph-telemetry)	 - Print this build's telemetry/network-behavior statement
* [codegraph ui](#codegraph-ui)	 - Run a local, read-only web UI over the repository's own index
* [codegraph uninit](#codegraph-uninit)	 - Remove .codegraph/
* [codegraph uninstall](#codegraph-uninstall)	 - Remove codegraph's configuration from coding agents
* [codegraph upgrade](#codegraph-upgrade)	 - Download, verify, and install a new codegraph release
* [codegraph version](#codegraph-version)	 - Print build version information

## codegraph affected

List test symbols impacted by changes to the given files

```
codegraph affected [files...] [flags]
```

### Options

```
  -d, --depth int       BFS depth (default 5, max 50)
  -f, --filter string   glob to narrow affected test paths (filepath.Match syntax)
  -h, --help            help for affected
  -j, --json            emit JSON output
  -p, --path string     repo path (default: cwd)
  -q, --quiet           emit only affected test file paths, one per line (no summary, no worktree notice)
      --stdin           read changed file paths from stdin, one per line (union'd with positional args)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph callees

List a symbol's forward call targets

```
codegraph callees <symbol> [flags]
```

### Options

```
  -h, --help          help for callees
  -j, --json          emit JSON output
  -l, --limit int     cap on results returned
  -p, --path string   repo path (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph callers

List a symbol's reverse callers

```
codegraph callers <symbol> [flags]
```

### Options

```
  -h, --help          help for callers
  -j, --json          emit JSON output
  -l, --limit int     cap on results returned
  -p, --path string   repo path (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph completion

Generate the autocompletion script for the specified shell

### Synopsis

Generate the autocompletion script for codegraph for the specified shell.
See each sub-command's help for details on how to use the generated script.


### Options

```
  -h, --help   help for completion
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents
* [codegraph completion bash](#codegraph-completion-bash)	 - Generate the autocompletion script for bash
* [codegraph completion fish](#codegraph-completion-fish)	 - Generate the autocompletion script for fish
* [codegraph completion powershell](#codegraph-completion-powershell)	 - Generate the autocompletion script for powershell
* [codegraph completion zsh](#codegraph-completion-zsh)	 - Generate the autocompletion script for zsh

## codegraph completion bash

Generate the autocompletion script for bash

### Synopsis

Generate the autocompletion script for the bash shell.

This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

To load completions in your current shell session:

	source <(codegraph completion bash)

To load completions for every new session, execute once:

#### Linux:

	codegraph completion bash > /etc/bash_completion.d/codegraph

#### macOS:

	codegraph completion bash > $(brew --prefix)/etc/bash_completion.d/codegraph

You will need to start a new shell for this setup to take effect.


```
codegraph completion bash
```

### Options

```
  -h, --help              help for bash
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph completion](#codegraph-completion)	 - Generate the autocompletion script for the specified shell

## codegraph completion fish

Generate the autocompletion script for fish

### Synopsis

Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

	codegraph completion fish | source

To load completions for every new session, execute once:

	codegraph completion fish > ~/.config/fish/completions/codegraph.fish

You will need to start a new shell for this setup to take effect.


```
codegraph completion fish [flags]
```

### Options

```
  -h, --help              help for fish
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph completion](#codegraph-completion)	 - Generate the autocompletion script for the specified shell

## codegraph completion powershell

Generate the autocompletion script for powershell

### Synopsis

Generate the autocompletion script for powershell.

To load completions in your current shell session:

	codegraph completion powershell | Out-String | Invoke-Expression

To load completions for every new session, add the output of the above command
to your powershell profile.


```
codegraph completion powershell [flags]
```

### Options

```
  -h, --help              help for powershell
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph completion](#codegraph-completion)	 - Generate the autocompletion script for the specified shell

## codegraph completion zsh

Generate the autocompletion script for zsh

### Synopsis

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need
to enable it.  You can execute the following once:

	echo "autoload -U compinit; compinit" >> ~/.zshrc

To load completions in your current shell session:

	source <(codegraph completion zsh)

To load completions for every new session, execute once:

#### Linux:

	codegraph completion zsh > "${fpath[1]}/_codegraph"

#### macOS:

	codegraph completion zsh > $(brew --prefix)/share/zsh/site-functions/_codegraph

You will need to start a new shell for this setup to take effect.


```
codegraph completion zsh [flags]
```

### Options

```
  -h, --help              help for zsh
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph completion](#codegraph-completion)	 - Generate the autocompletion script for the specified shell

## codegraph daemon

List and manage running codegraph daemons

### Synopsis

With no subcommand: on a TTY, open an interactive picker of every
running daemon (current project first) to stop one, stop all, or
cancel; off a TTY, print the same list and exit 0. Use `daemon start`
to run the shared watch/index server in the foreground,
`daemon stop [--all]` to stop it non-interactively, and
`daemon unlock [path]` to clear a stale lock left by a crash.

```
codegraph daemon [flags]
```

### Options

```
  -h, --help          help for daemon
  -p, --path string   repo path for current-project-first ordering (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents
* [codegraph daemon start](#codegraph-daemon-start)	 - Run the shared watch/index server in the foreground
* [codegraph daemon stop](#codegraph-daemon-stop)	 - Stop the current-repo daemon, or every running daemon (--all)
* [codegraph daemon unlock](#codegraph-daemon-unlock)	 - Clear a stale daemon lock

## codegraph daemon start

Run the shared watch/index server in the foreground

```
codegraph daemon start [flags]
```

### Options

```
  -h, --help          help for start
  -p, --path string   repo path (default: cwd)
  -q, --quiet         suppress daemon-driven sync progress output
  -v, --verbose       emit per-file/per-pass detail for daemon-driven syncs
      --workers int   bound the daemon's extraction worker pool (default: number of CPUs)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph daemon](#codegraph-daemon)	 - List and manage running codegraph daemons

## codegraph daemon stop

Stop the current-repo daemon, or every running daemon (--all)

```
codegraph daemon stop [flags]
```

### Options

```
      --all           stop every running daemon, not just the current repo's
  -h, --help          help for stop
  -p, --path string   repo path to stop (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph daemon](#codegraph-daemon)	 - List and manage running codegraph daemons

## codegraph daemon unlock

Clear a stale daemon lock

```
codegraph daemon unlock [path] [flags]
```

### Options

```
  -h, --help   help for unlock
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph daemon](#codegraph-daemon)	 - List and manage running codegraph daemons

## codegraph explore

Explore relevant symbols: verbatim source, call paths, blast radius

```
codegraph explore <query...> [flags]
```

### Options

```
  -h, --help            help for explore
      --max-files int   cap on distinct files returned (default 5)
  -p, --path string     repo path (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph files

Browse the indexed file structure

```
codegraph files [flags]
```

### Options

```
      --depth int        directory-nesting cap (0 = unlimited)
      --dir string       directory-path prefix filter
      --filter string    restrict to one language
      --format string    "flat" (default) or "tree"
  -h, --help             help for files
  -j, --json             emit JSON output
  -p, --path string      repo path (default: cwd)
      --pattern string   shell glob narrowing the result set
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph githooks

Manage git sync hooks (post-commit/post-merge/post-checkout)

### Options

```
  -h, --help   help for githooks
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents
* [codegraph githooks install](#codegraph-githooks-install)	 - Install marker-fenced git sync hooks
* [codegraph githooks remove](#codegraph-githooks-remove)	 - Remove codegraph's git sync hook blocks
* [codegraph githooks status](#codegraph-githooks-status)	 - Show git sync hook install state

## codegraph githooks install

Install marker-fenced git sync hooks

```
codegraph githooks install [path] [flags]
```

### Options

```
  -h, --help   help for install
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph githooks](#codegraph-githooks)	 - Manage git sync hooks (post-commit/post-merge/post-checkout)

## codegraph githooks remove

Remove codegraph's git sync hook blocks

```
codegraph githooks remove [path] [flags]
```

### Options

```
  -h, --help   help for remove
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph githooks](#codegraph-githooks)	 - Manage git sync hooks (post-commit/post-merge/post-checkout)

## codegraph githooks status

Show git sync hook install state

```
codegraph githooks status [path] [flags]
```

### Options

```
  -h, --help   help for status
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph githooks](#codegraph-githooks)	 - Manage git sync hooks (post-commit/post-merge/post-checkout)

## codegraph impact

Depth-bounded reverse blast radius of a symbol

```
codegraph impact <symbol> [flags]
```

### Options

```
  -d, --depth int     BFS depth (default 2, max 50)
  -h, --help          help for impact
  -j, --json          emit JSON output
  -p, --path string   repo path (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph index

Deterministically rebuild the graph from scratch

```
codegraph index [path] [flags]
```

### Options

```
  -f, --force         rebuild without prompting for confirmation
  -h, --help          help for index
  -q, --quiet         suppress progress and summary output
  -v, --verbose       emit per-file/per-pass detail
      --workers int   bound the extraction worker pool (default: number of CPUs)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph init

Create .codegraph/ and build the full graph in one step

```
codegraph init [path] [flags]
```

### Options

```
  -h, --help          help for init
  -q, --quiet         suppress progress and summary output
  -v, --verbose       emit per-file/per-pass detail
      --workers int   bound the extraction worker pool (default: number of CPUs)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph install

Configure coding agents to use this codegraph binary as their MCP server

### Synopsis

Detect and configure the agent roster (Claude Code, Cursor, Codex CLI,
opencode, Gemini CLI, Antigravity, Hermes, Kiro): write each agent's MCP
server entry plus, for the agents that support it, a short marker-fenced
instruction block. Idempotent — re-running install is a no-op when
nothing changed.

```
codegraph install [flags]
```

### Examples

```
  codegraph install
  codegraph install --target all --location global
  codegraph install --target claude,cursor
  codegraph install --target none
  codegraph install --print-config-style --location local
```

### Options

```
      --auto-allow           also add mcp__codegraph__* to Claude Code's permissions.allow list
  -h, --help                 help for install
  -l, --location string      config scope: global|local (default "global")
      --print-config-style   print each agent's capability table (scopes, MCP config, format, instructions, skill dir, hooks) and exit without writing anything
  -t, --target string        which agents to configure: auto|all|none|<comma-separated ids> (default "auto")
  -y, --yes                  skip the interactive picker; use the non-interactive default set (auto)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph node

Show a symbol's signature, calls, and callers, or a line-numbered file read

```
codegraph node [symbol] [flags]
```

### Options

```
  -f, --file string   file path — disambiguates symbol, or selects file-mode when symbol is omitted
  -h, --help          help for node
  -l, --line int      line number — narrows an overloaded symbol to the definition containing (or nearest) this line (NODE-03)
  -p, --path string   repo path (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph search

Lexically search symbol names/qualified names

```
codegraph search <term> [flags]
```

### Options

```
      --full          return full node records (signature, qualified name) instead of locations
  -h, --help          help for search
  -j, --json          emit JSON output
  -k, --kind string   restrict to one node kind
  -l, --limit int     cap on results returned
  -p, --path string   repo path (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph serve

Run the codegraph MCP server

### Synopsis

Run the codegraph MCP server over stdio.

All eight tools are exposed by default: codegraph_explore plus the seven
companions (node, search, callers, callees, impact, files, status).

Set CODEGRAPH_MCP_TOOLS to expose fewer — a smaller tool surface leaves more
of an agent's context for the work. It is a NARROWING filter, not an
allowlist: leaving it unset gives you all eight, and setting it removes every
companion it does not name. Unrecognized names are ignored with a warning on
stderr. codegraph_explore is always present and cannot be filtered out.

A repository with no .codegraph/ index exposes zero tools; the server still
starts and completes the MCP handshake, so agents fall back to their built-ins
instead of the connection being refused. Run codegraph init to fix that — the
tools appear on the next request, with no client restart.

```
codegraph serve [flags]
```

### Examples

```
  # all eight tools (the default)
  codegraph serve --mcp

  # explore + node + status only
  CODEGRAPH_MCP_TOOLS=node,status codegraph serve --mcp

  # codegraph_explore alone
  CODEGRAPH_MCP_TOOLS= codegraph serve --mcp
```

### Options

```
  -h, --help          help for serve
      --mcp           run the stdio MCP server
      --no-watch      Disable the file watcher (no auto-sync; useful on slow filesystems like WSL2 /mnt drives)
  -p, --path string   repo path (default: cwd)
      --watch         Force the file watcher on, overriding the WSL2/slow-filesystem auto-off (the CLI twin of CODEGRAPH_FORCE_WATCH=1)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph status

Report index health and counts

```
codegraph status [flags]
```

### Options

```
      --all-kinds     include every RANK_EDGES edge kind in the Edges by Kind breakdown, with explicit zeros
  -h, --help          help for status
  -j, --json          emit JSON output
  -p, --path string   repo path (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph sync

Incrementally update the graph from changed files

```
codegraph sync [path] [flags]
```

### Options

```
  -h, --help          help for sync
  -q, --quiet         suppress progress and summary output
  -v, --verbose       emit per-file/per-pass detail
      --workers int   bound the extraction worker pool (default: number of CPUs)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph telemetry

Print this build's telemetry/network-behavior statement

### Synopsis

Print a static, auditable statement about codegraph's telemetry and network behavior: zero passive collection, with `codegraph upgrade` disclosed as the sole user-initiated exception.

```
codegraph telemetry [flags]
```

### Examples

```
  codegraph telemetry
```

### Options

```
  -h, --help   help for telemetry
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph ui

Run a local, read-only web UI over the repository's own index

### Synopsis

codegraph ui starts a local web server over the repository's already-indexed graph and opens it in your default browser. It is read-only: it never writes to the graph. The server binds an ephemeral loopback port and runs in the foreground until interrupted (Ctrl-C) — there is no separate `ui stop` command and no shared lifecycle with `codegraph daemon`. An editor-link default is resolved once at startup, in order: --editor-url, then CODEGRAPH_EDITOR_URL, then a discovered editor, then unconfigured; --no-editor-url or CODEGRAPH_NO_EDITOR_URL=true|1|yes disables it. A malformed explicit value refuses to start before the port is bound.

```
codegraph ui [flags]
```

### Options

```
      --editor-url string   editor URI template with {path}, {line} and {col} placeholders (e.g. 'vscode://file/{path}:{line}:{col}'); overrides CODEGRAPH_EDITOR_URL and startup editor discovery
  -h, --help                help for ui
      --no-editor-url       disable editor links entirely (also CODEGRAPH_NO_EDITOR_URL=true|1|yes); skips editor discovery
      --no-open             do not open a browser automatically
  -p, --path string         repo path (default: cwd)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph uninit

Remove .codegraph/

```
codegraph uninit [path] [flags]
```

### Options

```
  -f, --force   remove without prompting for confirmation
  -h, --help    help for uninit
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph uninstall

Remove codegraph's configuration from coding agents

### Synopsis

Reverse everything `codegraph install` wrote for the selected agents —
the MCP server entry and, where present, the marker-fenced instruction
block — while preserving every unrelated key, entry, and section in
every file it touches. Reports removed / not-configured / unsupported
per agent and never errors on an agent that was never installed.

```
codegraph uninstall [flags]
```

### Examples

```
  codegraph uninstall
  codegraph uninstall --target all --location global
  codegraph uninstall --target claude,cursor
```

### Options

```
  -h, --help              help for uninstall
  -l, --location string   config scope: global|local (default "global")
  -t, --target string     which agents to reverse: auto|all|none|<comma-separated ids> (default "all")
  -y, --yes               skip the interactive picker; use the non-interactive default set (all)
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph upgrade

Download, verify, and install a new codegraph release

### Synopsis

Download the target-platform binary from GitHub Releases, verify its
cosign-keyless signature/provenance in-process (never a cosign CLI), and
only then atomically replace the running binary. --check reports whether
a newer release is available without downloading anything.

A Homebrew-managed install is detected from the resolved location of
the running binary. codegraph upgrade refuses to run there and exits
non-zero, because it was asked for a mutation it declines to perform.
codegraph upgrade --check steps aside with the same pointer and exits
zero, because it only answered a question. Upgrade a Homebrew-managed
install with: brew upgrade codegraph.

```
codegraph upgrade [version] [flags]
```

### Examples

```
  codegraph upgrade --check
  codegraph upgrade
  codegraph upgrade v1.4.0
  codegraph upgrade --check  # brew-managed install: prints the pointer, exits 0
```

### Options

```
      --check   report whether a newer release is available, without downloading
  -f, --force   reinstall even if already on the latest version
  -h, --help    help for upgrade
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

## codegraph version

Print build version information

### Synopsis

Print the codegraph binary's build identity: semver, git commit, build date, Go version, and os/arch.

```
codegraph version [flags]
```

### Examples

```
  codegraph version
  codegraph version --json
```

### Options

```
  -h, --help   help for version
      --json   emit build identity as JSON
```

### Options inherited from parent commands

```
      --color string   colour output: auto (detect), always, or never (default "auto")
```

### SEE ALSO

* [codegraph](#codegraph)	 - Pre-indexed code knowledge graph for coding agents

