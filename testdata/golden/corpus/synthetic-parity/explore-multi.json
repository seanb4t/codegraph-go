{
  "command": "explore \"user account\" -p synthetic-parity",
  "output": "**Exploration: user account**\n\nFound 13 symbols across 4 files.\n\n**Blast radius — what depends on these (update/verify before editing)**\n\n- `UserAccountManager` (accounts/manager.go:7) — 1 caller in `accounts/validate.go`; ⚠️ no covering tests found\n- `recoverAccount` (recovery/recovery.go:8) — 1 caller; tests: `recovery/recovery_test.go`\n\n**Source Code**\n\n> The code below is the **verbatim, current on-disk source** of these files — re-read from disk on this call and line-numbered, byte-for-byte identical to what the Read tool returns. It is NOT a summary, outline, or stale cache. Treat each block as a Read you have already performed: do not Read a file shown here.\n\n**`accounts/manager.go`** — UserAccountManager(struct), Describe(method)\n\n```go\n1\tpackage accounts\n2\t\n3\t// UserAccountManager is case (b) of the synthetic-parity corpus: a\n4\t// multi-word query like \"explore user account\" must tokenize this\n5\t// CamelCase type name into User/Account/Manager (RESEARCH.md §1-2's two\n6\t// tokenizers) and match it, once EXPL-01's multi-word `<query...>` lands.\n7\ttype UserAccountManager struct {\n8\t\tBalance int\n9\t\tName    string\n10\t}\n11\t\n12\t// Describe gives UserAccountManager at least one connected method so the\n13\t// type is not a pure, edge-free data type.\n14\tfunc (m *UserAccountManager) Describe() string {\n15\t\treturn m.Name\n16\t}\n```\n\n**`ledger/ledger.go`** — AccountBalanceHelper(function), applyAdjustment(function), PostTransaction(function), ReconcileLedger(function), AuditEntry(function), +1 more\n\n```go\n1\tpackage ledger\n2\t\n3\t// AccountBalanceHelper is case (d)'s symbol A: its NAME lexically matches\n4\t// a query like \"account balance\", but it is deliberately graph-isolated —\n5\t// zero calls in, zero calls out. Once EXPL-02's RWR ranking lands, a\n6\t// structurally-connected symbol (ReconcileLedger, below) that does NOT\n7\t// lexically match the query must still outrank this one.\n8\tfunc AccountBalanceHelper() string {\n9\t\treturn \"account balance helper — deliberately disconnected\"\n10\t}\n11\t\n12\t// GetBalance is the \"true\" target of a query like \"account balance\": it\n13\t// does not itself carry heavy connectivity, but delegates into\n14\t// ReconcileLedger, the structurally central symbol.\n15\tfunc GetBalance(accountID string) int {\n16\t\treturn ReconcileLedger(accountID)\n17\t}\n18\t\n19\t// ReconcileLedger is case (d)'s symbol B: its name does NOT lexically\n20\t// match \"account balance\" at all, but it is heavily structurally\n21\t// connected — called by GetBalance and PostTransaction, and itself calls\n22\t// applyAdjustment and AuditEntry — giving it strictly more graph edges\n23\t// than AccountBalanceHelper (which has zero).\n24\tfunc ReconcileLedger(accountID string) int {\n25\t\tapplyAdjustment(accountID)\n26\t\tAuditEntry(accountID)\n27\t\treturn 0\n28\t}\n29\t\n30\t// PostTransaction is a second caller into ReconcileLedger, reinforcing its\n31\t// structural centrality relative to the isolated AccountBalanceHelper.\n32\tfunc PostTransaction(accountID string, amount int) int {\n33\t\treturn ReconcileLedger(accountID)\n34\t}\n35\t\n36\tfunc applyAdjustment(accountID string) {}\n37\t\n38\t// AuditEntry is exported so it also appears as an independently callable\n39\t// symbol in the graph, adding to ReconcileLedger's outbound edge count.\n40\tfunc AuditEntry(accountID string) {}\n```\n\n**`recovery/recovery.go`** — recoverAccount(function), validateRecovery(function)\n\n```go\n1\tpackage recovery\n2\t\n3\t// recoverAccount is case (c)'s structurally-connected non-test symbol: it\n4\t// is called by TestAccountRecovery (recovery_test.go) and itself calls\n5\t// validateRecovery — giving the file-relevance gate (EXPL-03) a\n6\t// structurally-connected symbol to prefer over the weakly-connected\n7\t// Test*-named function in the same cluster.\n8\tfunc recoverAccount(accountID string) bool {\n9\t\treturn validateRecovery(accountID)\n10\t}\n11\t\n12\tfunc validateRecovery(accountID string) bool {\n13\t\treturn accountID != \"\"\n14\t}\n```\n\n**`accounts/validate.go`** — Validate(function), validateBalance(function), errNegativeBalance(variable)\n\n```go\n1\tpackage accounts\n2\t\n3\timport \"errors\"\n4\t\n5\t// Validate is one of two identically-named top-level definitions that make\n6\t// up case (a) of the synthetic-parity corpus — see orders/validate.go for\n7\t// the duplicate. Different package, identical exported symbol name:\n8\t// `node Validate` (once NODE-01 lands) must enumerate BOTH as distinct\n9\t// definitions rather than resolving to just the lowest-Id one.\n10\tfunc Validate(m *UserAccountManager) error {\n11\t\treturn validateBalance(m.Balance)\n12\t}\n13\t\n14\tfunc validateBalance(balance int) error {\n15\t\tif balance < 0 {\n16\t\t\treturn errNegativeBalance\n17\t\t}\n18\t\treturn nil\n19\t}\n20\t\n21\tvar errNegativeBalance = errors.New(\"negative balance\")\n```\n\n"
}
